[package]
name = "destack_std_lsp"
description = "Destack Standard Library Language Server"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
rust-version.workspace = true

[lib]
path = "rust/lib.rs"

[[bin]]
name = "destack_std_lsp"
path = "rust/main.rs"

[dependencies]
destack_lang_lex = { workspace = true }
destack_lang_parse = { workspace = true }

# TODO(destack_std_lsp): vendor tokio and tower-lsp-server
tokio = { version = "1.47.1", features = [
	"rt-multi-thread",
	"macros",
	"io-util",
	"sync",
	"io-std",
] }
tower-lsp-server = { version = "0.22.1" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
