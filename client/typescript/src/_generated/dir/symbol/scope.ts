/* generated by destack_client_generate, do not edit */

import {
    BinaryReader,
    BinaryWriter,
    Json,
    SerdeError,
    compareBytes,
    jsonArray,
    jsonField,
    jsonInteger,
    jsonObject,
    jsonOptional,
    jsonString,
    nestedBytes,
} from "../../../protocol/serde.js";
import type { StaticKey } from "./key.js";
import type { LocalSymbolId } from "./symbol.js";
import type { ModuleId } from "../../source/file/model/module.js";
import { decodeStaticKey, encodeStaticKey, fromJsonStaticKey, toJsonStaticKey } from "./key.js";
import {
    decodeLocalSymbolId,
    encodeLocalSymbolId,
    fromJsonLocalSymbolId,
    toJsonLocalSymbolId,
} from "./symbol.js";
import {
    decodeModuleId,
    encodeModuleId,
    fromJsonModuleId,
    toJsonModuleId,
} from "../../source/file/model/module.js";

/** Global scope id across modules. */
export type GlobalScopeId = {
    /** The module id of the global scope. */
    readonly moduleId: ModuleId;
    /** The local id of the global scope. */
    readonly localId: LocalScopeId;
};

export const GlobalScopeId = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: GlobalScopeId): void {
        encodeGlobalScopeId(writer, value);
    },

    /** Decode one GlobalScopeId. */
    decode(reader: BinaryReader): GlobalScopeId {
        return decodeGlobalScopeId(reader);
    },

    /** Return this value as JSON. */
    toJson(value: GlobalScopeId): Json {
        return toJsonGlobalScopeId(value);
    },

    /** Return one GlobalScopeId from one JSON value. */
    fromJson(value: Json): GlobalScopeId {
        return fromJsonGlobalScopeId(value);
    },
};

/** Encode one GlobalScopeId. */
export function encodeGlobalScopeId(writer: BinaryWriter, value: GlobalScopeId): void {
    encodeModuleId(writer, value.moduleId);
    encodeLocalScopeId(writer, value.localId);
}

/** Decode one GlobalScopeId. */
export function decodeGlobalScopeId(reader: BinaryReader): GlobalScopeId {
    const moduleId = decodeModuleId(reader);
    const localId = decodeLocalScopeId(reader);

    return {
        moduleId,
        localId,
    };
}

/** Return one JSON value for one GlobalScopeId. */
export function toJsonGlobalScopeId(value: GlobalScopeId): Json {
    return {
        moduleId: toJsonModuleId(value.moduleId),
        localId: toJsonLocalScopeId(value.localId),
    };
}

/** Return one GlobalScopeId from one JSON value. */
export function fromJsonGlobalScopeId(value: Json): GlobalScopeId {
    const object = jsonObject(value);

    return {
        moduleId: fromJsonModuleId(jsonField(object, "moduleId")),
        localId: fromJsonLocalScopeId(jsonField(object, "localId")),
    };
}

/** Local scope id and mark pair used for node and symbol insertion. */
export type LocalScope = {
    /** The scope id. */
    readonly id: LocalScopeId;
    /** The visible binding mark. */
    readonly mark: LocalScopeMark;
};

export const LocalScope = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: LocalScope): void {
        encodeLocalScope(writer, value);
    },

    /** Decode one LocalScope. */
    decode(reader: BinaryReader): LocalScope {
        return decodeLocalScope(reader);
    },

    /** Return this value as JSON. */
    toJson(value: LocalScope): Json {
        return toJsonLocalScope(value);
    },

    /** Return one LocalScope from one JSON value. */
    fromJson(value: Json): LocalScope {
        return fromJsonLocalScope(value);
    },
};

/** Encode one LocalScope. */
export function encodeLocalScope(writer: BinaryWriter, value: LocalScope): void {
    encodeLocalScopeId(writer, value.id);
    encodeLocalScopeMark(writer, value.mark);
}

/** Decode one LocalScope. */
export function decodeLocalScope(reader: BinaryReader): LocalScope {
    const id = decodeLocalScopeId(reader);
    const mark = decodeLocalScopeMark(reader);

    return {
        id,
        mark,
    };
}

/** Return one JSON value for one LocalScope. */
export function toJsonLocalScope(value: LocalScope): Json {
    return {
        id: toJsonLocalScopeId(value.id),
        mark: toJsonLocalScopeMark(value.mark),
    };
}

/** Return one LocalScope from one JSON value. */
export function fromJsonLocalScope(value: Json): LocalScope {
    const object = jsonObject(value);

    return {
        id: fromJsonLocalScopeId(jsonField(object, "id")),
        mark: fromJsonLocalScopeMark(jsonField(object, "mark")),
    };
}

/** Unique identifier for local scopes. */
export type LocalScopeId = number;

export const LocalScopeId = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: LocalScopeId): void {
        encodeLocalScopeId(writer, value);
    },

    /** Decode one LocalScopeId. */
    decode(reader: BinaryReader): LocalScopeId {
        return decodeLocalScopeId(reader);
    },

    /** Return this value as JSON. */
    toJson(value: LocalScopeId): Json {
        return toJsonLocalScopeId(value);
    },

    /** Return one LocalScopeId from one JSON value. */
    fromJson(value: Json): LocalScopeId {
        return fromJsonLocalScopeId(value);
    },
};

/** Encode one LocalScopeId. */
export function encodeLocalScopeId(writer: BinaryWriter, value: LocalScopeId): void {
    writer.writeUnsigned(value);
}

/** Decode one LocalScopeId. */
export function decodeLocalScopeId(reader: BinaryReader): LocalScopeId {
    return reader.readNumber();
}

/** Return one JSON value for one LocalScopeId. */
export function toJsonLocalScopeId(value: LocalScopeId): Json {
    return value;
}

/** Return one LocalScopeId from one JSON value. */
export function fromJsonLocalScopeId(value: Json): LocalScopeId {
    return jsonInteger(value);
}

/** Mark a position in a scope. */
export type LocalScopeMark = number;

export const LocalScopeMark = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: LocalScopeMark): void {
        encodeLocalScopeMark(writer, value);
    },

    /** Decode one LocalScopeMark. */
    decode(reader: BinaryReader): LocalScopeMark {
        return decodeLocalScopeMark(reader);
    },

    /** Return this value as JSON. */
    toJson(value: LocalScopeMark): Json {
        return toJsonLocalScopeMark(value);
    },

    /** Return one LocalScopeMark from one JSON value. */
    fromJson(value: Json): LocalScopeMark {
        return fromJsonLocalScopeMark(value);
    },
};

/** Encode one LocalScopeMark. */
export function encodeLocalScopeMark(writer: BinaryWriter, value: LocalScopeMark): void {
    writer.writeUnsigned(value);
}

/** Decode one LocalScopeMark. */
export function decodeLocalScopeMark(reader: BinaryReader): LocalScopeMark {
    return reader.readNumber();
}

/** Return one JSON value for one LocalScopeMark. */
export function toJsonLocalScopeMark(value: LocalScopeMark): Json {
    return value;
}

/** Return one LocalScopeMark from one JSON value. */
export function fromJsonLocalScopeMark(value: Json): LocalScopeMark {
    return jsonInteger(value);
}

/** A lexical container for symbols. */
export type Scope = {
    /** The kind of the scope. */
    readonly kind: ScopeKind;
    /** The parent scope. */
    readonly parent?: LocalScope;
    /** The owner of the scope. */
    readonly owner?: LocalSymbolId;
    /** The bindings in lexical order. */
    readonly bindings: ReadonlyArray<ScopeBinding>;
    /** Index for named bindings. */
    readonly index: ScopeIndex;
    /** The children scopes. */
    readonly children: ReadonlyArray<LocalScopeId>;
};

export const Scope = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: Scope): void {
        encodeScope(writer, value);
    },

    /** Decode one Scope. */
    decode(reader: BinaryReader): Scope {
        return decodeScope(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Scope): Json {
        return toJsonScope(value);
    },

    /** Return one Scope from one JSON value. */
    fromJson(value: Json): Scope {
        return fromJsonScope(value);
    },
};

/** Encode one Scope. */
export function encodeScope(writer: BinaryWriter, value: Scope): void {
    encodeScopeKind(writer, value.kind);
    writer.writeOption(value.parent, (value1) => {
        encodeLocalScope(writer, value1);
    });
    writer.writeOption(value.owner, (value2) => {
        encodeLocalSymbolId(writer, value2);
    });
    writer.writeUnsigned(value.bindings.length);
    for (const item3 of value.bindings) {
        encodeScopeBinding(writer, item3);
    }
    encodeScopeIndex(writer, value.index);
    writer.writeUnsigned(value.children.length);
    for (const item5 of value.children) {
        encodeLocalScopeId(writer, item5);
    }
}

/** Decode one Scope. */
export function decodeScope(reader: BinaryReader): Scope {
    const kind = decodeScopeKind(reader);
    const parent = reader.readOption(() => decodeLocalScope(reader));
    const owner = reader.readOption(() => decodeLocalSymbolId(reader));
    const bindings = (() => {
        const length3 = reader.readNumber();
        const items3: Array<ScopeBinding> = [];
        for (let index = 0; index < length3; index += 1) {
            items3.push(decodeScopeBinding(reader));
        }
        return items3;
    })();
    const index = decodeScopeIndex(reader);
    const children = (() => {
        const length5 = reader.readNumber();
        const items5: Array<LocalScopeId> = [];
        for (let index = 0; index < length5; index += 1) {
            items5.push(decodeLocalScopeId(reader));
        }
        return items5;
    })();

    return {
        kind,
        ...(parent === undefined ? {} : { parent }),
        ...(owner === undefined ? {} : { owner }),
        bindings,
        index,
        children,
    };
}

/** Return one JSON value for one Scope. */
export function toJsonScope(value: Scope): Json {
    return {
        kind: toJsonScopeKind(value.kind),
        ...(value.parent === undefined ? {} : { parent: toJsonLocalScope(value.parent) }),
        ...(value.owner === undefined ? {} : { owner: toJsonLocalSymbolId(value.owner) }),
        bindings: value.bindings.map((item0) => toJsonScopeBinding(item0)),
        index: toJsonScopeIndex(value.index),
        children: value.children.map((item0) => toJsonLocalScopeId(item0)),
    };
}

/** Return one Scope from one JSON value. */
export function fromJsonScope(value: Json): Scope {
    const object = jsonObject(value);

    return {
        kind: fromJsonScopeKind(jsonField(object, "kind")),
        parent: jsonOptional(object, "parent", (value) => fromJsonLocalScope(value)),
        owner: jsonOptional(object, "owner", (value) => fromJsonLocalSymbolId(value)),
        bindings: jsonArray(jsonField(object, "bindings")).map((item0) =>
            fromJsonScopeBinding(item0),
        ),
        index: fromJsonScopeIndex(jsonField(object, "index")),
        children: jsonArray(jsonField(object, "children")).map((item0) =>
            fromJsonLocalScopeId(item0),
        ),
    };
}

/** One binding entry in lexical order. */
export type ScopeBinding = {
    /** The binding key. */
    readonly key?: StaticKey;
    /** The bound symbol. */
    readonly symbol: LocalSymbolId;
};

export const ScopeBinding = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ScopeBinding): void {
        encodeScopeBinding(writer, value);
    },

    /** Decode one ScopeBinding. */
    decode(reader: BinaryReader): ScopeBinding {
        return decodeScopeBinding(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ScopeBinding): Json {
        return toJsonScopeBinding(value);
    },

    /** Return one ScopeBinding from one JSON value. */
    fromJson(value: Json): ScopeBinding {
        return fromJsonScopeBinding(value);
    },
};

/** Encode one ScopeBinding. */
export function encodeScopeBinding(writer: BinaryWriter, value: ScopeBinding): void {
    writer.writeOption(value.key, (value0) => {
        encodeStaticKey(writer, value0);
    });
    encodeLocalSymbolId(writer, value.symbol);
}

/** Decode one ScopeBinding. */
export function decodeScopeBinding(reader: BinaryReader): ScopeBinding {
    const key = reader.readOption(() => decodeStaticKey(reader));
    const symbol_ = decodeLocalSymbolId(reader);

    return {
        ...(key === undefined ? {} : { key }),
        symbol: symbol_,
    };
}

/** Return one JSON value for one ScopeBinding. */
export function toJsonScopeBinding(value: ScopeBinding): Json {
    return {
        ...(value.key === undefined ? {} : { key: toJsonStaticKey(value.key) }),
        symbol: toJsonLocalSymbolId(value.symbol),
    };
}

/** Return one ScopeBinding from one JSON value. */
export function fromJsonScopeBinding(value: Json): ScopeBinding {
    const object = jsonObject(value);

    return {
        key: jsonOptional(object, "key", (value) => fromJsonStaticKey(value)),
        symbol: fromJsonLocalSymbolId(jsonField(object, "symbol")),
    };
}

/** A compact name index for one lexical scope. */
export type ScopeIndex =
    /** No named bindings. */
    | {
          readonly kind: "empty";
      }
    /** A small inline table for common tiny scopes. */
    | {
          readonly kind: "small";
          /** Binding indices grouped by key. */
          readonly entries: ReadonlyArray<ScopeIndexEntry>;
      }
    /** A keyed table for larger scopes. */
    | {
          readonly kind: "large";
          /** Binding indices grouped by key. */
          readonly table: ReadonlyMap<StaticKey, ReadonlyArray<number>>;
      };

export const ScopeIndex = {
    /** No named bindings. */
    empty(): ScopeIndex {
        return { kind: "empty" };
    },

    /** A small inline table for common tiny scopes. */
    small(entries: ReadonlyArray<ScopeIndexEntry>): ScopeIndex {
        return { kind: "small", entries };
    },

    /** A keyed table for larger scopes. */
    large(table: ReadonlyMap<StaticKey, ReadonlyArray<number>>): ScopeIndex {
        return { kind: "large", table };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: ScopeIndex): void {
        encodeScopeIndex(writer, value);
    },

    /** Decode one ScopeIndex. */
    decode(reader: BinaryReader): ScopeIndex {
        return decodeScopeIndex(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ScopeIndex): Json {
        return toJsonScopeIndex(value);
    },

    /** Return one ScopeIndex from one JSON value. */
    fromJson(value: Json): ScopeIndex {
        return fromJsonScopeIndex(value);
    },
};

/** Encode one ScopeIndex. */
export function encodeScopeIndex(writer: BinaryWriter, value: ScopeIndex): void {
    switch (value.kind) {
        case "empty":
            writer.writeUnsigned(0);
            return;
        case "small":
            writer.writeUnsigned(1);
            writer.writeUnsigned(value.entries.length);
            for (const item0 of value.entries) {
                encodeScopeIndexEntry(writer, item0);
            }
            return;
        case "large":
            writer.writeUnsigned(2);
            const entries0 = Array.from(value.table.entries()).map(([key0, item0]) => {
                const keyBytes = nestedBytes((writer) => {
                    encodeStaticKey(writer, key0);
                });
                return { key0, item0, keyBytes };
            });
            entries0.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
            writer.writeUnsigned(entries0.length);
            for (const entry0 of entries0) {
                encodeStaticKey(writer, entry0.key0);
                writer.writeUnsigned(entry0.item0.length);
                for (const item1 of entry0.item0) {
                    writer.writeUnsigned(item1);
                }
            }
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one ScopeIndex. */
export function decodeScopeIndex(reader: BinaryReader): ScopeIndex {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            return { kind: "empty" };
        }
        case 1: {
            const entries = (() => {
                const length0 = reader.readNumber();
                const items0: Array<ScopeIndexEntry> = [];
                for (let index = 0; index < length0; index += 1) {
                    items0.push(decodeScopeIndexEntry(reader));
                }
                return items0;
            })();

            return {
                kind: "small",
                entries,
            };
        }
        case 2: {
            const table = (() => {
                const length0 = reader.readNumber();
                const items0 = new Map<StaticKey, ReadonlyArray<number>>();
                for (let index = 0; index < length0; index += 1) {
                    items0.set(
                        decodeStaticKey(reader),
                        (() => {
                            const length2 = reader.readNumber();
                            const items2: Array<number> = [];
                            for (let index = 0; index < length2; index += 1) {
                                items2.push(reader.readNumber());
                            }
                            return items2;
                        })(),
                    );
                }
                return items0;
            })();

            return {
                kind: "large",
                table,
            };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one ScopeIndex. */
export function toJsonScopeIndex(value: ScopeIndex): Json {
    switch (value.kind) {
        case "empty":
            return {
                kind: "empty",
            };
        case "small":
            return {
                kind: "small",
                entries: value.entries.map((item0) => toJsonScopeIndexEntry(item0)),
            };
        case "large":
            return {
                kind: "large",
                table: Array.from(value.table.entries()).map(
                    ([key0, item0]) =>
                        [toJsonStaticKey(key0), item0.map((item1) => item1)] as const,
                ),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one ScopeIndex from one JSON value. */
export function fromJsonScopeIndex(value: Json): ScopeIndex {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "empty":
            return {
                kind,
            };
        case "small":
            return {
                kind,
                entries: jsonArray(jsonField(object, "entries")).map((item0) =>
                    fromJsonScopeIndexEntry(item0),
                ),
            };
        case "large":
            return {
                kind,
                table: new Map(
                    jsonArray(jsonField(object, "table")).map((entry) => {
                        const items = jsonArray(entry);
                        if (items.length !== 2) {
                            throw new SerdeError(
                                `expected JSON map entry length 2: ${items.length}`,
                            );
                        }
                        const key0 = items[0];
                        const item0 = items[1];
                        return [
                            fromJsonStaticKey(key0),
                            jsonArray(item0).map((item1) => jsonInteger(item1)),
                        ] as const;
                    }),
                ),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** Binding indices for one key in a small scope lookup. */
export type ScopeIndexEntry = {
    /** The binding key. */
    readonly key: StaticKey;
    /** Binding indices in lexical order. */
    readonly indices: ReadonlyArray<number>;
};

export const ScopeIndexEntry = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ScopeIndexEntry): void {
        encodeScopeIndexEntry(writer, value);
    },

    /** Decode one ScopeIndexEntry. */
    decode(reader: BinaryReader): ScopeIndexEntry {
        return decodeScopeIndexEntry(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ScopeIndexEntry): Json {
        return toJsonScopeIndexEntry(value);
    },

    /** Return one ScopeIndexEntry from one JSON value. */
    fromJson(value: Json): ScopeIndexEntry {
        return fromJsonScopeIndexEntry(value);
    },
};

/** Encode one ScopeIndexEntry. */
export function encodeScopeIndexEntry(writer: BinaryWriter, value: ScopeIndexEntry): void {
    encodeStaticKey(writer, value.key);
    writer.writeUnsigned(value.indices.length);
    for (const item1 of value.indices) {
        writer.writeUnsigned(item1);
    }
}

/** Decode one ScopeIndexEntry. */
export function decodeScopeIndexEntry(reader: BinaryReader): ScopeIndexEntry {
    const key = decodeStaticKey(reader);
    const indices = (() => {
        const length1 = reader.readNumber();
        const items1: Array<number> = [];
        for (let index = 0; index < length1; index += 1) {
            items1.push(reader.readNumber());
        }
        return items1;
    })();

    return {
        key,
        indices,
    };
}

/** Return one JSON value for one ScopeIndexEntry. */
export function toJsonScopeIndexEntry(value: ScopeIndexEntry): Json {
    return {
        key: toJsonStaticKey(value.key),
        indices: value.indices.map((item0) => item0),
    };
}

/** Return one ScopeIndexEntry from one JSON value. */
export function fromJsonScopeIndexEntry(value: Json): ScopeIndexEntry {
    const object = jsonObject(value);

    return {
        key: fromJsonStaticKey(jsonField(object, "key")),
        indices: jsonArray(jsonField(object, "indices")).map((item0) => jsonInteger(item0)),
    };
}

/** The kind of a scope. */
export type ScopeKind =
    | "module"
    | "global"
    | "namespace"
    | "function"
    | "type"
    | "typeConditional"
    | "block";

export const ScopeKind = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ScopeKind): void {
        encodeScopeKind(writer, value);
    },

    /** Decode one ScopeKind. */
    decode(reader: BinaryReader): ScopeKind {
        return decodeScopeKind(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ScopeKind): Json {
        return toJsonScopeKind(value);
    },

    /** Return one ScopeKind from one JSON value. */
    fromJson(value: Json): ScopeKind {
        return fromJsonScopeKind(value);
    },
};

/** Encode one ScopeKind. */
export function encodeScopeKind(writer: BinaryWriter, value: ScopeKind): void {
    switch (value) {
        case "module":
            writer.writeUnsigned(0);
            return;
        case "global":
            writer.writeUnsigned(1);
            return;
        case "namespace":
            writer.writeUnsigned(2);
            return;
        case "function":
            writer.writeUnsigned(3);
            return;
        case "type":
            writer.writeUnsigned(4);
            return;
        case "typeConditional":
            writer.writeUnsigned(5);
            return;
        case "block":
            writer.writeUnsigned(6);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one ScopeKind. */
export function decodeScopeKind(reader: BinaryReader): ScopeKind {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "module";
        case 1:
            return "global";
        case 2:
            return "namespace";
        case 3:
            return "function";
        case 4:
            return "type";
        case 5:
            return "typeConditional";
        case 6:
            return "block";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one ScopeKind. */
export function toJsonScopeKind(value: ScopeKind): Json {
    return value;
}

/** Return one ScopeKind from one JSON value. */
export function fromJsonScopeKind(value: Json): ScopeKind {
    const variant = jsonString(value);

    switch (variant) {
        case "module":
            return "module";
        case "global":
            return "global";
        case "namespace":
            return "namespace";
        case "function":
            return "function";
        case "type":
            return "type";
        case "typeConditional":
            return "typeConditional";
        case "block":
            return "block";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}
