/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, jsonField, jsonObject, jsonOptional, jsonString } from "../../../protocol/serde.js";
import type { StringId } from "../../core/string.js";
import type { GlobalNodeIdAny } from "../tree/node.js";
import type { Loader } from "../../source/file/model/loader.js";
import type { ModuleId } from "../../source/file/model/module.js";
import { decodeStringId, encodeStringId, fromJsonStringId, toJsonStringId } from "../../core/string.js";
import { decodeGlobalNodeIdAny, encodeGlobalNodeIdAny, fromJsonGlobalNodeIdAny, toJsonGlobalNodeIdAny } from "../tree/node.js";
import { decodeLoader, encodeLoader, fromJsonLoader, toJsonLoader } from "../../source/file/model/loader.js";
import { decodeModuleId, encodeModuleId, fromJsonModuleId, toJsonModuleId } from "../../source/file/model/module.js";

/** One resolved module import edge. */
export type ModuleEdge = {
    /** The DIR node that declared the dependency. */
    readonly source: GlobalNodeIdAny;
    /** The static import specifier. */
    readonly specifier: StringId;
    /** The module import relation. */
    readonly relation: ModuleRelation;
    /** The loader override selected for the import. */
    readonly loader?: Loader;
    /** The resolved module. */
    readonly target?: ModuleId;
};

export const ModuleEdge = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ModuleEdge): void {
        encodeModuleEdge(writer, value);
    },

    /** Decode one ModuleEdge. */
    decode(reader: BinaryReader): ModuleEdge {
        return decodeModuleEdge(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ModuleEdge): Json {
        return toJsonModuleEdge(value);
    },

    /** Return one ModuleEdge from one JSON value. */
    fromJson(value: Json): ModuleEdge {
        return fromJsonModuleEdge(value);
    },
};

/** Encode one ModuleEdge. */
export function encodeModuleEdge(writer: BinaryWriter, value: ModuleEdge): void {
    encodeGlobalNodeIdAny(writer, value.source);
    encodeStringId(writer, value.specifier);
    encodeModuleRelation(writer, value.relation);
    writer.writeOption(value.loader, (value3) => {
        encodeLoader(writer, value3);
    });
    writer.writeOption(value.target, (value4) => {
        encodeModuleId(writer, value4);
    });
}

/** Decode one ModuleEdge. */
export function decodeModuleEdge(reader: BinaryReader): ModuleEdge {
    const source = decodeGlobalNodeIdAny(reader);
    const specifier = decodeStringId(reader);
    const relation = decodeModuleRelation(reader);
    const loader = reader.readOption(() => decodeLoader(reader));
    const target = reader.readOption(() => decodeModuleId(reader));

    return {
        source,
        specifier,
        relation,
        ...(loader === undefined ? {} : { loader }),
        ...(target === undefined ? {} : { target }),
    };
}

/** Return one JSON value for one ModuleEdge. */
export function toJsonModuleEdge(value: ModuleEdge): Json {
    return {
        source: toJsonGlobalNodeIdAny(value.source),
        specifier: toJsonStringId(value.specifier),
        relation: toJsonModuleRelation(value.relation),
        ...(value.loader === undefined ? {} : { loader: toJsonLoader(value.loader) }),
        ...(value.target === undefined ? {} : { target: toJsonModuleId(value.target) }),
    };
}

/** Return one ModuleEdge from one JSON value. */
export function fromJsonModuleEdge(value: Json): ModuleEdge {
    const object = jsonObject(value);

    return {
        source: fromJsonGlobalNodeIdAny(jsonField(object, "source")),
        specifier: fromJsonStringId(jsonField(object, "specifier")),
        relation: fromJsonModuleRelation(jsonField(object, "relation")),
        loader: jsonOptional(object, "loader", (value) => fromJsonLoader(value)),
        target: jsonOptional(object, "target", (value) => fromJsonModuleId(value)),
    };
}

/** The relation declared by a resolved module import edge. */
export type ModuleRelation = "import" | "reExport";

export const ModuleRelation = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ModuleRelation): void {
        encodeModuleRelation(writer, value);
    },

    /** Decode one ModuleRelation. */
    decode(reader: BinaryReader): ModuleRelation {
        return decodeModuleRelation(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ModuleRelation): Json {
        return toJsonModuleRelation(value);
    },

    /** Return one ModuleRelation from one JSON value. */
    fromJson(value: Json): ModuleRelation {
        return fromJsonModuleRelation(value);
    },
};

/** Encode one ModuleRelation. */
export function encodeModuleRelation(writer: BinaryWriter, value: ModuleRelation): void {
    switch (value) {
        case "import":
            writer.writeUnsigned(0);
            return;
        case "reExport":
            writer.writeUnsigned(1);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one ModuleRelation. */
export function decodeModuleRelation(reader: BinaryReader): ModuleRelation {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "import";
        case 1:
            return "reExport";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one ModuleRelation. */
export function toJsonModuleRelation(value: ModuleRelation): Json {
    return value;
}

/** Return one ModuleRelation from one JSON value. */
export function fromJsonModuleRelation(value: Json): ModuleRelation {
    const variant = jsonString(value);

    switch (variant) {
        case "import":
            return "import";
        case "reExport":
            return "reExport";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}
