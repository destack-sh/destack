/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, jsonField, jsonInteger, jsonObject, jsonString } from "../../../protocol/serde.js";
import type { StringId } from "../../core/string.js";
import { decodeStringId, encodeStringId, fromJsonStringId, toJsonStringId } from "../../core/string.js";

/** Key for some static identifier or positional index. */
export type StaticKey =
    /** Regular name key (like `x` or `"weird identifier"`). */
    | {
          readonly kind: "name";
          readonly name: StringId;
      }
    /** Positional index key (like `0` or `1`). */
    | {
          readonly kind: "index";
          readonly index: number;
      }
;

export const StaticKey = {
    /** Regular name key (like `x` or `"weird identifier"`). */
    name(name: StringId): StaticKey {
        return { kind: "name", name };
    },

    /** Positional index key (like `0` or `1`). */
    index(index: number): StaticKey {
        return { kind: "index", index };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: StaticKey): void {
        encodeStaticKey(writer, value);
    },

    /** Decode one StaticKey. */
    decode(reader: BinaryReader): StaticKey {
        return decodeStaticKey(reader);
    },

    /** Return this value as JSON. */
    toJson(value: StaticKey): Json {
        return toJsonStaticKey(value);
    },

    /** Return one StaticKey from one JSON value. */
    fromJson(value: Json): StaticKey {
        return fromJsonStaticKey(value);
    },
};

/** Encode one StaticKey. */
export function encodeStaticKey(writer: BinaryWriter, value: StaticKey): void {
    switch (value.kind) {
        case "name":
            writer.writeUnsigned(0);
            encodeStringId(writer, value.name);
            return;
        case "index":
            writer.writeUnsigned(1);
            writer.writeUnsigned(value.index);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one StaticKey. */
export function decodeStaticKey(reader: BinaryReader): StaticKey {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const name = decodeStringId(reader);

            return { kind: "name", name };
        }
        case 1: {
            const index = reader.readNumber();

            return { kind: "index", index };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one StaticKey. */
export function toJsonStaticKey(value: StaticKey): Json {
    switch (value.kind) {
        case "name":
            return {
                kind: "name",
                name: toJsonStringId(value.name),
            };
        case "index":
            return {
                kind: "index",
                index: value.index,
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one StaticKey from one JSON value. */
export function fromJsonStaticKey(value: Json): StaticKey {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "name":
            return {
                kind,
                name: fromJsonStringId(jsonField(object, "name")),
            };
        case "index":
            return {
                kind,
                index: jsonInteger(jsonField(object, "index")),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}
