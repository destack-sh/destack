/* generated by destack_client_generate, do not edit */

import {
    BinaryReader,
    BinaryWriter,
    Json,
    SerdeError,
    jsonArray,
    jsonField,
    jsonObject,
    jsonOptional,
    jsonString,
} from "../../../protocol/serde.js";
import type { StaticKey } from "./key.js";
import type { GlobalSymbolId } from "./symbol.js";
import type { LocalSymbolId } from "./symbol.js";
import type { LocalNodeId } from "../tree/node.js";
import type { ModuleId } from "../../source/file/model/module.js";
import { decodeStaticKey, encodeStaticKey, fromJsonStaticKey, toJsonStaticKey } from "./key.js";
import {
    decodeGlobalSymbolId,
    encodeGlobalSymbolId,
    fromJsonGlobalSymbolId,
    toJsonGlobalSymbolId,
} from "./symbol.js";
import {
    decodeLocalSymbolId,
    encodeLocalSymbolId,
    fromJsonLocalSymbolId,
    toJsonLocalSymbolId,
} from "./symbol.js";
import {
    decodeLocalNodeId,
    encodeLocalNodeId,
    fromJsonLocalNodeId,
    toJsonLocalNodeId,
} from "../tree/node.js";
import {
    decodeModuleId,
    encodeModuleId,
    fromJsonModuleId,
    toJsonModuleId,
} from "../../source/file/model/module.js";

/** How one exported name is bound. */
export type ExportBinding =
    /** Declarations from the exporting module. */
    | {
          readonly kind: "local";
          /** The local declaration overload group. */
          readonly symbols: ReadonlyArray<LocalSymbolId>;
      }
    /** A local import exported by this module. */
    | {
          readonly kind: "import";
          /** The local import symbol. */
          readonly local: LocalSymbolId;
          /** The imported module. */
          readonly module?: ModuleId;
          /** The selected exported name. */
          readonly selector: ExportSelector;
      }
    /** A declaration re-exported directly from another module. */
    | {
          readonly kind: "reExport";
          /** The imported module. */
          readonly module?: ModuleId;
          /** The selected exported name. */
          readonly selector: ExportSelector;
      };

export const ExportBinding = {
    /** Declarations from the exporting module. */
    local(symbols: ReadonlyArray<LocalSymbolId>): ExportBinding {
        return { kind: "local", symbols };
    },

    /** A local import exported by this module. */
    import(
        local: LocalSymbolId,
        module_: ModuleId | undefined,
        selector: ExportSelector,
    ): ExportBinding {
        return { kind: "import", local, module: module_, selector };
    },

    /** A declaration re-exported directly from another module. */
    reExport(module_: ModuleId | undefined, selector: ExportSelector): ExportBinding {
        return { kind: "reExport", module: module_, selector };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: ExportBinding): void {
        encodeExportBinding(writer, value);
    },

    /** Decode one ExportBinding. */
    decode(reader: BinaryReader): ExportBinding {
        return decodeExportBinding(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ExportBinding): Json {
        return toJsonExportBinding(value);
    },

    /** Return one ExportBinding from one JSON value. */
    fromJson(value: Json): ExportBinding {
        return fromJsonExportBinding(value);
    },
};

/** Encode one ExportBinding. */
export function encodeExportBinding(writer: BinaryWriter, value: ExportBinding): void {
    switch (value.kind) {
        case "local":
            writer.writeUnsigned(0);
            writer.writeUnsigned(value.symbols.length);
            for (const item0 of value.symbols) {
                encodeLocalSymbolId(writer, item0);
            }
            return;
        case "import":
            writer.writeUnsigned(1);
            encodeLocalSymbolId(writer, value.local);
            writer.writeOption(value.module, (value1) => {
                encodeModuleId(writer, value1);
            });
            encodeExportSelector(writer, value.selector);
            return;
        case "reExport":
            writer.writeUnsigned(2);
            writer.writeOption(value.module, (value0) => {
                encodeModuleId(writer, value0);
            });
            encodeExportSelector(writer, value.selector);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one ExportBinding. */
export function decodeExportBinding(reader: BinaryReader): ExportBinding {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const symbols = (() => {
                const length0 = reader.readNumber();
                const items0: Array<LocalSymbolId> = [];
                for (let index = 0; index < length0; index += 1) {
                    items0.push(decodeLocalSymbolId(reader));
                }
                return items0;
            })();

            return {
                kind: "local",
                symbols,
            };
        }
        case 1: {
            const local = decodeLocalSymbolId(reader);
            const module_ = reader.readOption(() => decodeModuleId(reader));
            const selector = decodeExportSelector(reader);

            return {
                kind: "import",
                local,
                ...(module_ === undefined ? {} : { module: module_ }),
                selector,
            };
        }
        case 2: {
            const module_ = reader.readOption(() => decodeModuleId(reader));
            const selector = decodeExportSelector(reader);

            return {
                kind: "reExport",
                ...(module_ === undefined ? {} : { module: module_ }),
                selector,
            };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one ExportBinding. */
export function toJsonExportBinding(value: ExportBinding): Json {
    switch (value.kind) {
        case "local":
            return {
                kind: "local",
                symbols: value.symbols.map((item0) => toJsonLocalSymbolId(item0)),
            };
        case "import":
            return {
                kind: "import",
                local: toJsonLocalSymbolId(value.local),
                ...(value.module === undefined ? {} : { module: toJsonModuleId(value.module) }),
                selector: toJsonExportSelector(value.selector),
            };
        case "reExport":
            return {
                kind: "reExport",
                ...(value.module === undefined ? {} : { module: toJsonModuleId(value.module) }),
                selector: toJsonExportSelector(value.selector),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one ExportBinding from one JSON value. */
export function fromJsonExportBinding(value: Json): ExportBinding {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "local":
            return {
                kind,
                symbols: jsonArray(jsonField(object, "symbols")).map((item0) =>
                    fromJsonLocalSymbolId(item0),
                ),
            };
        case "import":
            return {
                kind,
                local: fromJsonLocalSymbolId(jsonField(object, "local")),
                module: jsonOptional(object, "module", (value) => fromJsonModuleId(value)),
                selector: fromJsonExportSelector(jsonField(object, "selector")),
            };
        case "reExport":
            return {
                kind,
                module: jsonOptional(object, "module", (value) => fromJsonModuleId(value)),
                selector: fromJsonExportSelector(jsonField(object, "selector")),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** How one exported symbol's type reaches its consumers. */
export type ExportForm = "declared" | "literal" | "inferred";

export const ExportForm = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ExportForm): void {
        encodeExportForm(writer, value);
    },

    /** Decode one ExportForm. */
    decode(reader: BinaryReader): ExportForm {
        return decodeExportForm(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ExportForm): Json {
        return toJsonExportForm(value);
    },

    /** Return one ExportForm from one JSON value. */
    fromJson(value: Json): ExportForm {
        return fromJsonExportForm(value);
    },
};

/** Encode one ExportForm. */
export function encodeExportForm(writer: BinaryWriter, value: ExportForm): void {
    switch (value) {
        case "declared":
            writer.writeUnsigned(0);
            return;
        case "literal":
            writer.writeUnsigned(1);
            return;
        case "inferred":
            writer.writeUnsigned(2);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one ExportForm. */
export function decodeExportForm(reader: BinaryReader): ExportForm {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "declared";
        case 1:
            return "literal";
        case 2:
            return "inferred";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one ExportForm. */
export function toJsonExportForm(value: ExportForm): Json {
    return value;
}

/** Return one ExportForm from one JSON value. */
export function fromJsonExportForm(value: Json): ExportForm {
    const variant = jsonString(value);

    switch (variant) {
        case "declared":
            return "declared";
        case "literal":
            return "literal";
        case "inferred":
            return "inferred";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** The exported name in one module record. */
export type ExportKey =
    /** The ECMAScript default export name. */
    | {
          readonly kind: "default";
      }
    /** A named export key. */
    | {
          readonly kind: "named";
          readonly named: StaticKey;
      };

export const ExportKey = {
    /** The ECMAScript default export name. */
    default(): ExportKey {
        return { kind: "default" };
    },

    /** A named export key. */
    named(named: StaticKey): ExportKey {
        return { kind: "named", named };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: ExportKey): void {
        encodeExportKey(writer, value);
    },

    /** Decode one ExportKey. */
    decode(reader: BinaryReader): ExportKey {
        return decodeExportKey(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ExportKey): Json {
        return toJsonExportKey(value);
    },

    /** Return one ExportKey from one JSON value. */
    fromJson(value: Json): ExportKey {
        return fromJsonExportKey(value);
    },
};

/** Encode one ExportKey. */
export function encodeExportKey(writer: BinaryWriter, value: ExportKey): void {
    switch (value.kind) {
        case "default":
            writer.writeUnsigned(0);
            return;
        case "named":
            writer.writeUnsigned(1);
            encodeStaticKey(writer, value.named);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one ExportKey. */
export function decodeExportKey(reader: BinaryReader): ExportKey {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            return { kind: "default" };
        }
        case 1: {
            const named = decodeStaticKey(reader);

            return { kind: "named", named };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one ExportKey. */
export function toJsonExportKey(value: ExportKey): Json {
    switch (value.kind) {
        case "default":
            return {
                kind: "default",
            };
        case "named":
            return {
                kind: "named",
                named: toJsonStaticKey(value.named),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one ExportKey from one JSON value. */
export function fromJsonExportKey(value: Json): ExportKey {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "default":
            return {
                kind,
            };
        case "named":
            return {
                kind,
                named: fromJsonStaticKey(jsonField(object, "named")),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** One resolved export declaration and its final compiler target. */
export type ExportResolution = {
    /** The declaration selected by the exported name. */
    readonly declaration: ExportTarget;
    /** The final exported target. */
    readonly target: ExportTarget;
};

export const ExportResolution = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ExportResolution): void {
        encodeExportResolution(writer, value);
    },

    /** Decode one ExportResolution. */
    decode(reader: BinaryReader): ExportResolution {
        return decodeExportResolution(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ExportResolution): Json {
        return toJsonExportResolution(value);
    },

    /** Return one ExportResolution from one JSON value. */
    fromJson(value: Json): ExportResolution {
        return fromJsonExportResolution(value);
    },
};

/** Encode one ExportResolution. */
export function encodeExportResolution(writer: BinaryWriter, value: ExportResolution): void {
    encodeExportTarget(writer, value.declaration);
    encodeExportTarget(writer, value.target);
}

/** Decode one ExportResolution. */
export function decodeExportResolution(reader: BinaryReader): ExportResolution {
    const declaration = decodeExportTarget(reader);
    const target = decodeExportTarget(reader);

    return {
        declaration,
        target,
    };
}

/** Return one JSON value for one ExportResolution. */
export function toJsonExportResolution(value: ExportResolution): Json {
    return {
        declaration: toJsonExportTarget(value.declaration),
        target: toJsonExportTarget(value.target),
    };
}

/** Return one ExportResolution from one JSON value. */
export function fromJsonExportResolution(value: Json): ExportResolution {
    const object = jsonObject(value);

    return {
        declaration: fromJsonExportTarget(jsonField(object, "declaration")),
        target: fromJsonExportTarget(jsonField(object, "target")),
    };
}

/** Which binding a re-export selects from the target module. */
export type ExportSelector =
    /** A named target export. */
    | {
          readonly kind: "named";
          readonly named: StaticKey;
      }
    /** The target module default export. */
    | {
          readonly kind: "default";
      }
    /** The target module namespace object. */
    | {
          readonly kind: "namespace";
      };

export const ExportSelector = {
    /** A named target export. */
    named(named: StaticKey): ExportSelector {
        return { kind: "named", named };
    },

    /** The target module default export. */
    default(): ExportSelector {
        return { kind: "default" };
    },

    /** The target module namespace object. */
    namespace(): ExportSelector {
        return { kind: "namespace" };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: ExportSelector): void {
        encodeExportSelector(writer, value);
    },

    /** Decode one ExportSelector. */
    decode(reader: BinaryReader): ExportSelector {
        return decodeExportSelector(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ExportSelector): Json {
        return toJsonExportSelector(value);
    },

    /** Return one ExportSelector from one JSON value. */
    fromJson(value: Json): ExportSelector {
        return fromJsonExportSelector(value);
    },
};

/** Encode one ExportSelector. */
export function encodeExportSelector(writer: BinaryWriter, value: ExportSelector): void {
    switch (value.kind) {
        case "named":
            writer.writeUnsigned(0);
            encodeStaticKey(writer, value.named);
            return;
        case "default":
            writer.writeUnsigned(1);
            return;
        case "namespace":
            writer.writeUnsigned(2);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one ExportSelector. */
export function decodeExportSelector(reader: BinaryReader): ExportSelector {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const named = decodeStaticKey(reader);

            return { kind: "named", named };
        }
        case 1: {
            return { kind: "default" };
        }
        case 2: {
            return { kind: "namespace" };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one ExportSelector. */
export function toJsonExportSelector(value: ExportSelector): Json {
    switch (value.kind) {
        case "named":
            return {
                kind: "named",
                named: toJsonStaticKey(value.named),
            };
        case "default":
            return {
                kind: "default",
            };
        case "namespace":
            return {
                kind: "namespace",
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one ExportSelector from one JSON value. */
export function fromJsonExportSelector(value: Json): ExportSelector {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "named":
            return {
                kind,
                named: fromJsonStaticKey(jsonField(object, "named")),
            };
        case "default":
            return {
                kind,
            };
        case "namespace":
            return {
                kind,
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** One resolved target exposed through an exported name. */
export type ExportTarget =
    /** One declaration overload group. */
    | {
          readonly kind: "symbols";
          readonly symbols: ReadonlyArray<GlobalSymbolId>;
      }
    /** One module namespace object. */
    | {
          readonly kind: "namespace";
          readonly namespace: ModuleId;
      };

export const ExportTarget = {
    /** One declaration overload group. */
    symbols(symbols: ReadonlyArray<GlobalSymbolId>): ExportTarget {
        return { kind: "symbols", symbols };
    },

    /** One module namespace object. */
    namespace(namespace_: ModuleId): ExportTarget {
        return { kind: "namespace", namespace: namespace_ };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: ExportTarget): void {
        encodeExportTarget(writer, value);
    },

    /** Decode one ExportTarget. */
    decode(reader: BinaryReader): ExportTarget {
        return decodeExportTarget(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ExportTarget): Json {
        return toJsonExportTarget(value);
    },

    /** Return one ExportTarget from one JSON value. */
    fromJson(value: Json): ExportTarget {
        return fromJsonExportTarget(value);
    },
};

/** Encode one ExportTarget. */
export function encodeExportTarget(writer: BinaryWriter, value: ExportTarget): void {
    switch (value.kind) {
        case "symbols":
            writer.writeUnsigned(0);
            writer.writeUnsigned(value.symbols.length);
            for (const item0 of value.symbols) {
                encodeGlobalSymbolId(writer, item0);
            }
            return;
        case "namespace":
            writer.writeUnsigned(1);
            encodeModuleId(writer, value.namespace);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one ExportTarget. */
export function decodeExportTarget(reader: BinaryReader): ExportTarget {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const symbols = (() => {
                const length0 = reader.readNumber();
                const items0: Array<GlobalSymbolId> = [];
                for (let index = 0; index < length0; index += 1) {
                    items0.push(decodeGlobalSymbolId(reader));
                }
                return items0;
            })();

            return { kind: "symbols", symbols };
        }
        case 1: {
            const namespace_ = decodeModuleId(reader);

            return { kind: "namespace", namespace: namespace_ };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one ExportTarget. */
export function toJsonExportTarget(value: ExportTarget): Json {
    switch (value.kind) {
        case "symbols":
            return {
                kind: "symbols",
                symbols: value.symbols.map((item0) => toJsonGlobalSymbolId(item0)),
            };
        case "namespace":
            return {
                kind: "namespace",
                namespace: toJsonModuleId(value.namespace),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one ExportTarget from one JSON value. */
export function fromJsonExportTarget(value: Json): ExportTarget {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "symbols":
            return {
                kind,
                symbols: jsonArray(jsonField(object, "symbols")).map((item0) =>
                    fromJsonGlobalSymbolId(item0),
                ),
            };
        case "namespace":
            return {
                kind,
                namespace: fromJsonModuleId(jsonField(object, "namespace")),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** One named export entry. */
export type NamedExport = {
    /** The exported name. */
    readonly key: ExportKey;
    /** The dependency item that declared this export. */
    readonly item?: LocalNodeId;
    /** The local declaration selected by the exported name. */
    readonly declaration?: LocalSymbolId;
    /** How the exported name is bound. */
    readonly binding: ExportBinding;
};

export const NamedExport = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: NamedExport): void {
        encodeNamedExport(writer, value);
    },

    /** Decode one NamedExport. */
    decode(reader: BinaryReader): NamedExport {
        return decodeNamedExport(reader);
    },

    /** Return this value as JSON. */
    toJson(value: NamedExport): Json {
        return toJsonNamedExport(value);
    },

    /** Return one NamedExport from one JSON value. */
    fromJson(value: Json): NamedExport {
        return fromJsonNamedExport(value);
    },
};

/** Encode one NamedExport. */
export function encodeNamedExport(writer: BinaryWriter, value: NamedExport): void {
    encodeExportKey(writer, value.key);
    writer.writeOption(value.item, (value1) => {
        encodeLocalNodeId(writer, value1);
    });
    writer.writeOption(value.declaration, (value2) => {
        encodeLocalSymbolId(writer, value2);
    });
    encodeExportBinding(writer, value.binding);
}

/** Decode one NamedExport. */
export function decodeNamedExport(reader: BinaryReader): NamedExport {
    const key = decodeExportKey(reader);
    const item = reader.readOption(() => decodeLocalNodeId(reader));
    const declaration = reader.readOption(() => decodeLocalSymbolId(reader));
    const binding = decodeExportBinding(reader);

    return {
        key,
        ...(item === undefined ? {} : { item }),
        ...(declaration === undefined ? {} : { declaration }),
        binding,
    };
}

/** Return one JSON value for one NamedExport. */
export function toJsonNamedExport(value: NamedExport): Json {
    return {
        key: toJsonExportKey(value.key),
        ...(value.item === undefined ? {} : { item: toJsonLocalNodeId(value.item) }),
        ...(value.declaration === undefined
            ? {}
            : { declaration: toJsonLocalSymbolId(value.declaration) }),
        binding: toJsonExportBinding(value.binding),
    };
}

/** Return one NamedExport from one JSON value. */
export function fromJsonNamedExport(value: Json): NamedExport {
    const object = jsonObject(value);

    return {
        key: fromJsonExportKey(jsonField(object, "key")),
        item: jsonOptional(object, "item", (value) => fromJsonLocalNodeId(value)),
        declaration: jsonOptional(object, "declaration", (value) => fromJsonLocalSymbolId(value)),
        binding: fromJsonExportBinding(jsonField(object, "binding")),
    };
}

/** One `export * from` edge. */
export type StarExport = {
    /** The dependency item that declared the star export. */
    readonly item: LocalNodeId;
    /** The target module selected by the export. */
    readonly target?: ModuleId;
};

export const StarExport = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: StarExport): void {
        encodeStarExport(writer, value);
    },

    /** Decode one StarExport. */
    decode(reader: BinaryReader): StarExport {
        return decodeStarExport(reader);
    },

    /** Return this value as JSON. */
    toJson(value: StarExport): Json {
        return toJsonStarExport(value);
    },

    /** Return one StarExport from one JSON value. */
    fromJson(value: Json): StarExport {
        return fromJsonStarExport(value);
    },
};

/** Encode one StarExport. */
export function encodeStarExport(writer: BinaryWriter, value: StarExport): void {
    encodeLocalNodeId(writer, value.item);
    writer.writeOption(value.target, (value1) => {
        encodeModuleId(writer, value1);
    });
}

/** Decode one StarExport. */
export function decodeStarExport(reader: BinaryReader): StarExport {
    const item = decodeLocalNodeId(reader);
    const target = reader.readOption(() => decodeModuleId(reader));

    return {
        item,
        ...(target === undefined ? {} : { target }),
    };
}

/** Return one JSON value for one StarExport. */
export function toJsonStarExport(value: StarExport): Json {
    return {
        item: toJsonLocalNodeId(value.item),
        ...(value.target === undefined ? {} : { target: toJsonModuleId(value.target) }),
    };
}

/** Return one StarExport from one JSON value. */
export function fromJsonStarExport(value: Json): StarExport {
    const object = jsonObject(value);

    return {
        item: fromJsonLocalNodeId(jsonField(object, "item")),
        target: jsonOptional(object, "target", (value) => fromJsonModuleId(value)),
    };
}
