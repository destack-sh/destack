/* generated by destack_client_generate, do not edit */

import {
    BinaryReader,
    BinaryWriter,
    Json,
    SerdeError,
    jsonBool,
    jsonField,
    jsonObject,
    jsonOptional,
    jsonString,
} from "../../../protocol/serde.js";
import type { StringId } from "../../core/string.js";
import type { LocalNodeId } from "./node.js";
import type { VarianceModifier } from "./property.js";
import {
    decodeStringId,
    encodeStringId,
    fromJsonStringId,
    toJsonStringId,
} from "../../core/string.js";
import {
    decodeLocalNodeId,
    encodeLocalNodeId,
    fromJsonLocalNodeId,
    toJsonLocalNodeId,
} from "./node.js";
import {
    decodeVarianceModifier,
    encodeVarianceModifier,
    fromJsonVarianceModifier,
    toJsonVarianceModifier,
} from "./property.js";

/** An argument to a call, sequence literal, or template interpolation. */
export type Argument =
    /** Positional argument. */
    | {
          readonly kind: "positional";
          readonly value: LocalNodeId;
      }
    /** Spread argument. */
    | {
          readonly kind: "spread";
          readonly value: LocalNodeId;
      }
    /** Elided array element. */
    | {
          readonly kind: "elision";
      }
    /** Malformed argument slot. */
    | {
          readonly kind: "error";
      };

export const Argument = {
    /** Positional argument. */
    positional(value: LocalNodeId): Argument {
        return { kind: "positional", value };
    },

    /** Spread argument. */
    spread(value: LocalNodeId): Argument {
        return { kind: "spread", value };
    },

    /** Elided array element. */
    elision(): Argument {
        return { kind: "elision" };
    },

    /** Malformed argument slot. */
    error(): Argument {
        return { kind: "error" };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: Argument): void {
        encodeArgument(writer, value);
    },

    /** Decode one Argument. */
    decode(reader: BinaryReader): Argument {
        return decodeArgument(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Argument): Json {
        return toJsonArgument(value);
    },

    /** Return one Argument from one JSON value. */
    fromJson(value: Json): Argument {
        return fromJsonArgument(value);
    },
};

/** Encode one Argument. */
export function encodeArgument(writer: BinaryWriter, value: Argument): void {
    switch (value.kind) {
        case "positional":
            writer.writeUnsigned(0);
            encodeLocalNodeId(writer, value.value);
            return;
        case "spread":
            writer.writeUnsigned(1);
            encodeLocalNodeId(writer, value.value);
            return;
        case "elision":
            writer.writeUnsigned(2);
            return;
        case "error":
            writer.writeUnsigned(3);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one Argument. */
export function decodeArgument(reader: BinaryReader): Argument {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const value = decodeLocalNodeId(reader);

            return {
                kind: "positional",
                value,
            };
        }
        case 1: {
            const value = decodeLocalNodeId(reader);

            return {
                kind: "spread",
                value,
            };
        }
        case 2: {
            return { kind: "elision" };
        }
        case 3: {
            return { kind: "error" };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one Argument. */
export function toJsonArgument(value: Argument): Json {
    switch (value.kind) {
        case "positional":
            return {
                kind: "positional",
                value: toJsonLocalNodeId(value.value),
            };
        case "spread":
            return {
                kind: "spread",
                value: toJsonLocalNodeId(value.value),
            };
        case "elision":
            return {
                kind: "elision",
            };
        case "error":
            return {
                kind: "error",
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one Argument from one JSON value. */
export function fromJsonArgument(value: Json): Argument {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "positional":
            return {
                kind,
                value: fromJsonLocalNodeId(jsonField(object, "value")),
            };
        case "spread":
            return {
                kind,
                value: fromJsonLocalNodeId(jsonField(object, "value")),
            };
        case "elision":
            return {
                kind,
            };
        case "error":
            return {
                kind,
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** A generic argument in static argument position. */
export type GenericArgument =
    /** Type generic argument. */
    | {
          readonly kind: "type";
          readonly value: LocalNodeId;
      }
    /** Spread type generic argument. */
    | {
          readonly kind: "spreadType";
          readonly value: LocalNodeId;
      }
    /** Associated type refinement. */
    | {
          readonly kind: "associatedType";
          readonly name: StringId;
          readonly value: LocalNodeId;
      }
    /** Associated const refinement. */
    | {
          readonly kind: "associatedConst";
          readonly name: StringId;
          readonly value: LocalNodeId;
      }
    /** Malformed generic argument slot. */
    | {
          readonly kind: "error";
      };

export const GenericArgument = {
    /** Type generic argument. */
    type(value: LocalNodeId): GenericArgument {
        return { kind: "type", value };
    },

    /** Spread type generic argument. */
    spreadType(value: LocalNodeId): GenericArgument {
        return { kind: "spreadType", value };
    },

    /** Associated type refinement. */
    associatedType(name: StringId, value: LocalNodeId): GenericArgument {
        return { kind: "associatedType", name, value };
    },

    /** Associated const refinement. */
    associatedConst(name: StringId, value: LocalNodeId): GenericArgument {
        return { kind: "associatedConst", name, value };
    },

    /** Malformed generic argument slot. */
    error(): GenericArgument {
        return { kind: "error" };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: GenericArgument): void {
        encodeGenericArgument(writer, value);
    },

    /** Decode one GenericArgument. */
    decode(reader: BinaryReader): GenericArgument {
        return decodeGenericArgument(reader);
    },

    /** Return this value as JSON. */
    toJson(value: GenericArgument): Json {
        return toJsonGenericArgument(value);
    },

    /** Return one GenericArgument from one JSON value. */
    fromJson(value: Json): GenericArgument {
        return fromJsonGenericArgument(value);
    },
};

/** Encode one GenericArgument. */
export function encodeGenericArgument(writer: BinaryWriter, value: GenericArgument): void {
    switch (value.kind) {
        case "type":
            writer.writeUnsigned(0);
            encodeLocalNodeId(writer, value.value);
            return;
        case "spreadType":
            writer.writeUnsigned(1);
            encodeLocalNodeId(writer, value.value);
            return;
        case "associatedType":
            writer.writeUnsigned(2);
            encodeStringId(writer, value.name);
            encodeLocalNodeId(writer, value.value);
            return;
        case "associatedConst":
            writer.writeUnsigned(3);
            encodeStringId(writer, value.name);
            encodeLocalNodeId(writer, value.value);
            return;
        case "error":
            writer.writeUnsigned(4);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one GenericArgument. */
export function decodeGenericArgument(reader: BinaryReader): GenericArgument {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const value = decodeLocalNodeId(reader);

            return {
                kind: "type",
                value,
            };
        }
        case 1: {
            const value = decodeLocalNodeId(reader);

            return {
                kind: "spreadType",
                value,
            };
        }
        case 2: {
            const name = decodeStringId(reader);
            const value = decodeLocalNodeId(reader);

            return {
                kind: "associatedType",
                name,
                value,
            };
        }
        case 3: {
            const name = decodeStringId(reader);
            const value = decodeLocalNodeId(reader);

            return {
                kind: "associatedConst",
                name,
                value,
            };
        }
        case 4: {
            return { kind: "error" };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one GenericArgument. */
export function toJsonGenericArgument(value: GenericArgument): Json {
    switch (value.kind) {
        case "type":
            return {
                kind: "type",
                value: toJsonLocalNodeId(value.value),
            };
        case "spreadType":
            return {
                kind: "spreadType",
                value: toJsonLocalNodeId(value.value),
            };
        case "associatedType":
            return {
                kind: "associatedType",
                name: toJsonStringId(value.name),
                value: toJsonLocalNodeId(value.value),
            };
        case "associatedConst":
            return {
                kind: "associatedConst",
                name: toJsonStringId(value.name),
                value: toJsonLocalNodeId(value.value),
            };
        case "error":
            return {
                kind: "error",
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one GenericArgument from one JSON value. */
export function fromJsonGenericArgument(value: Json): GenericArgument {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "type":
            return {
                kind,
                value: fromJsonLocalNodeId(jsonField(object, "value")),
            };
        case "spreadType":
            return {
                kind,
                value: fromJsonLocalNodeId(jsonField(object, "value")),
            };
        case "associatedType":
            return {
                kind,
                name: fromJsonStringId(jsonField(object, "name")),
                value: fromJsonLocalNodeId(jsonField(object, "value")),
            };
        case "associatedConst":
            return {
                kind,
                name: fromJsonStringId(jsonField(object, "name")),
                value: fromJsonLocalNodeId(jsonField(object, "value")),
            };
        case "error":
            return {
                kind,
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** A declared generic parameter in source. */
export type GenericParameter =
    /** Type parameter. */
    | {
          readonly kind: "type";
          readonly name: StringId;
          readonly variance?: VarianceModifier;
          readonly constraint?: LocalNodeId;
          readonly default?: LocalNodeId;
          readonly isConst: boolean;
      }
    /** Variadic type parameter. */
    | {
          readonly kind: "variadicType";
          readonly name: StringId;
          readonly variance?: VarianceModifier;
          readonly constraint?: LocalNodeId;
          readonly default?: LocalNodeId;
          readonly isConst: boolean;
      }
    /** Lifetime parameter. */
    | {
          readonly kind: "lifetime";
          readonly name: StringId;
      }
    /** Malformed generic parameter. */
    | {
          readonly kind: "error";
      };

export const GenericParameter = {
    /** Type parameter. */
    type(
        name: StringId,
        variance: VarianceModifier | undefined,
        constraint: LocalNodeId | undefined,
        default_: LocalNodeId | undefined,
        isConst: boolean,
    ): GenericParameter {
        return { kind: "type", name, variance, constraint, default: default_, isConst };
    },

    /** Variadic type parameter. */
    variadicType(
        name: StringId,
        variance: VarianceModifier | undefined,
        constraint: LocalNodeId | undefined,
        default_: LocalNodeId | undefined,
        isConst: boolean,
    ): GenericParameter {
        return { kind: "variadicType", name, variance, constraint, default: default_, isConst };
    },

    /** Lifetime parameter. */
    lifetime(name: StringId): GenericParameter {
        return { kind: "lifetime", name };
    },

    /** Malformed generic parameter. */
    error(): GenericParameter {
        return { kind: "error" };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: GenericParameter): void {
        encodeGenericParameter(writer, value);
    },

    /** Decode one GenericParameter. */
    decode(reader: BinaryReader): GenericParameter {
        return decodeGenericParameter(reader);
    },

    /** Return this value as JSON. */
    toJson(value: GenericParameter): Json {
        return toJsonGenericParameter(value);
    },

    /** Return one GenericParameter from one JSON value. */
    fromJson(value: Json): GenericParameter {
        return fromJsonGenericParameter(value);
    },
};

/** Encode one GenericParameter. */
export function encodeGenericParameter(writer: BinaryWriter, value: GenericParameter): void {
    switch (value.kind) {
        case "type":
            writer.writeUnsigned(0);
            encodeStringId(writer, value.name);
            writer.writeOption(value.variance, (value1) => {
                encodeVarianceModifier(writer, value1);
            });
            writer.writeOption(value.constraint, (value2) => {
                encodeLocalNodeId(writer, value2);
            });
            writer.writeOption(value.default, (value3) => {
                encodeLocalNodeId(writer, value3);
            });
            writer.writeBool(value.isConst);
            return;
        case "variadicType":
            writer.writeUnsigned(1);
            encodeStringId(writer, value.name);
            writer.writeOption(value.variance, (value1) => {
                encodeVarianceModifier(writer, value1);
            });
            writer.writeOption(value.constraint, (value2) => {
                encodeLocalNodeId(writer, value2);
            });
            writer.writeOption(value.default, (value3) => {
                encodeLocalNodeId(writer, value3);
            });
            writer.writeBool(value.isConst);
            return;
        case "lifetime":
            writer.writeUnsigned(2);
            encodeStringId(writer, value.name);
            return;
        case "error":
            writer.writeUnsigned(3);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one GenericParameter. */
export function decodeGenericParameter(reader: BinaryReader): GenericParameter {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const name = decodeStringId(reader);
            const variance = reader.readOption(() => decodeVarianceModifier(reader));
            const constraint = reader.readOption(() => decodeLocalNodeId(reader));
            const default_ = reader.readOption(() => decodeLocalNodeId(reader));
            const isConst = reader.readBool();

            return {
                kind: "type",
                name,
                ...(variance === undefined ? {} : { variance }),
                ...(constraint === undefined ? {} : { constraint }),
                ...(default_ === undefined ? {} : { default: default_ }),
                isConst,
            };
        }
        case 1: {
            const name = decodeStringId(reader);
            const variance = reader.readOption(() => decodeVarianceModifier(reader));
            const constraint = reader.readOption(() => decodeLocalNodeId(reader));
            const default_ = reader.readOption(() => decodeLocalNodeId(reader));
            const isConst = reader.readBool();

            return {
                kind: "variadicType",
                name,
                ...(variance === undefined ? {} : { variance }),
                ...(constraint === undefined ? {} : { constraint }),
                ...(default_ === undefined ? {} : { default: default_ }),
                isConst,
            };
        }
        case 2: {
            const name = decodeStringId(reader);

            return {
                kind: "lifetime",
                name,
            };
        }
        case 3: {
            return { kind: "error" };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one GenericParameter. */
export function toJsonGenericParameter(value: GenericParameter): Json {
    switch (value.kind) {
        case "type":
            return {
                kind: "type",
                name: toJsonStringId(value.name),
                ...(value.variance === undefined
                    ? {}
                    : { variance: toJsonVarianceModifier(value.variance) }),
                ...(value.constraint === undefined
                    ? {}
                    : { constraint: toJsonLocalNodeId(value.constraint) }),
                ...(value.default === undefined
                    ? {}
                    : { default: toJsonLocalNodeId(value.default) }),
                isConst: value.isConst,
            };
        case "variadicType":
            return {
                kind: "variadicType",
                name: toJsonStringId(value.name),
                ...(value.variance === undefined
                    ? {}
                    : { variance: toJsonVarianceModifier(value.variance) }),
                ...(value.constraint === undefined
                    ? {}
                    : { constraint: toJsonLocalNodeId(value.constraint) }),
                ...(value.default === undefined
                    ? {}
                    : { default: toJsonLocalNodeId(value.default) }),
                isConst: value.isConst,
            };
        case "lifetime":
            return {
                kind: "lifetime",
                name: toJsonStringId(value.name),
            };
        case "error":
            return {
                kind: "error",
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one GenericParameter from one JSON value. */
export function fromJsonGenericParameter(value: Json): GenericParameter {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "type":
            return {
                kind,
                name: fromJsonStringId(jsonField(object, "name")),
                variance: jsonOptional(object, "variance", (value) =>
                    fromJsonVarianceModifier(value),
                ),
                constraint: jsonOptional(object, "constraint", (value) =>
                    fromJsonLocalNodeId(value),
                ),
                default: jsonOptional(object, "default", (value) => fromJsonLocalNodeId(value)),
                isConst: jsonBool(jsonField(object, "isConst")),
            };
        case "variadicType":
            return {
                kind,
                name: fromJsonStringId(jsonField(object, "name")),
                variance: jsonOptional(object, "variance", (value) =>
                    fromJsonVarianceModifier(value),
                ),
                constraint: jsonOptional(object, "constraint", (value) =>
                    fromJsonLocalNodeId(value),
                ),
                default: jsonOptional(object, "default", (value) => fromJsonLocalNodeId(value)),
                isConst: jsonBool(jsonField(object, "isConst")),
            };
        case "lifetime":
            return {
                kind,
                name: fromJsonStringId(jsonField(object, "name")),
            };
        case "error":
            return {
                kind,
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** A parameter to a callable construct. */
export type Parameter =
    /** Named scalar parameter. */
    | {
          readonly kind: "named";
          readonly name: StringId;
          readonly declaredType?: LocalNodeId;
          readonly default?: LocalNodeId;
          readonly isOptional: boolean;
      }
    /** Pattern parameter. */
    | {
          readonly kind: "pattern";
          readonly pattern: LocalNodeId;
          readonly declaredType?: LocalNodeId;
          readonly default?: LocalNodeId;
          readonly isOptional: boolean;
      }
    /** Variadic named parameter. */
    | {
          readonly kind: "variadicNamed";
          readonly name: StringId;
          readonly declaredType?: LocalNodeId;
      }
    /** Variadic pattern parameter. */
    | {
          readonly kind: "variadicPattern";
          readonly pattern: LocalNodeId;
          readonly declaredType?: LocalNodeId;
      }
    /** Malformed parameter. */
    | {
          readonly kind: "error";
      };

export const Parameter = {
    /** Named scalar parameter. */
    named(
        name: StringId,
        declaredType: LocalNodeId | undefined,
        default_: LocalNodeId | undefined,
        isOptional: boolean,
    ): Parameter {
        return { kind: "named", name, declaredType, default: default_, isOptional };
    },

    /** Pattern parameter. */
    pattern(
        pattern: LocalNodeId,
        declaredType: LocalNodeId | undefined,
        default_: LocalNodeId | undefined,
        isOptional: boolean,
    ): Parameter {
        return { kind: "pattern", pattern, declaredType, default: default_, isOptional };
    },

    /** Variadic named parameter. */
    variadicNamed(name: StringId, declaredType: LocalNodeId | undefined): Parameter {
        return { kind: "variadicNamed", name, declaredType };
    },

    /** Variadic pattern parameter. */
    variadicPattern(pattern: LocalNodeId, declaredType: LocalNodeId | undefined): Parameter {
        return { kind: "variadicPattern", pattern, declaredType };
    },

    /** Malformed parameter. */
    error(): Parameter {
        return { kind: "error" };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: Parameter): void {
        encodeParameter(writer, value);
    },

    /** Decode one Parameter. */
    decode(reader: BinaryReader): Parameter {
        return decodeParameter(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Parameter): Json {
        return toJsonParameter(value);
    },

    /** Return one Parameter from one JSON value. */
    fromJson(value: Json): Parameter {
        return fromJsonParameter(value);
    },
};

/** Encode one Parameter. */
export function encodeParameter(writer: BinaryWriter, value: Parameter): void {
    switch (value.kind) {
        case "named":
            writer.writeUnsigned(0);
            encodeStringId(writer, value.name);
            writer.writeOption(value.declaredType, (value1) => {
                encodeLocalNodeId(writer, value1);
            });
            writer.writeOption(value.default, (value2) => {
                encodeLocalNodeId(writer, value2);
            });
            writer.writeBool(value.isOptional);
            return;
        case "pattern":
            writer.writeUnsigned(1);
            encodeLocalNodeId(writer, value.pattern);
            writer.writeOption(value.declaredType, (value1) => {
                encodeLocalNodeId(writer, value1);
            });
            writer.writeOption(value.default, (value2) => {
                encodeLocalNodeId(writer, value2);
            });
            writer.writeBool(value.isOptional);
            return;
        case "variadicNamed":
            writer.writeUnsigned(2);
            encodeStringId(writer, value.name);
            writer.writeOption(value.declaredType, (value1) => {
                encodeLocalNodeId(writer, value1);
            });
            return;
        case "variadicPattern":
            writer.writeUnsigned(3);
            encodeLocalNodeId(writer, value.pattern);
            writer.writeOption(value.declaredType, (value1) => {
                encodeLocalNodeId(writer, value1);
            });
            return;
        case "error":
            writer.writeUnsigned(4);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one Parameter. */
export function decodeParameter(reader: BinaryReader): Parameter {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const name = decodeStringId(reader);
            const declaredType = reader.readOption(() => decodeLocalNodeId(reader));
            const default_ = reader.readOption(() => decodeLocalNodeId(reader));
            const isOptional = reader.readBool();

            return {
                kind: "named",
                name,
                ...(declaredType === undefined ? {} : { declaredType }),
                ...(default_ === undefined ? {} : { default: default_ }),
                isOptional,
            };
        }
        case 1: {
            const pattern = decodeLocalNodeId(reader);
            const declaredType = reader.readOption(() => decodeLocalNodeId(reader));
            const default_ = reader.readOption(() => decodeLocalNodeId(reader));
            const isOptional = reader.readBool();

            return {
                kind: "pattern",
                pattern,
                ...(declaredType === undefined ? {} : { declaredType }),
                ...(default_ === undefined ? {} : { default: default_ }),
                isOptional,
            };
        }
        case 2: {
            const name = decodeStringId(reader);
            const declaredType = reader.readOption(() => decodeLocalNodeId(reader));

            return {
                kind: "variadicNamed",
                name,
                ...(declaredType === undefined ? {} : { declaredType }),
            };
        }
        case 3: {
            const pattern = decodeLocalNodeId(reader);
            const declaredType = reader.readOption(() => decodeLocalNodeId(reader));

            return {
                kind: "variadicPattern",
                pattern,
                ...(declaredType === undefined ? {} : { declaredType }),
            };
        }
        case 4: {
            return { kind: "error" };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one Parameter. */
export function toJsonParameter(value: Parameter): Json {
    switch (value.kind) {
        case "named":
            return {
                kind: "named",
                name: toJsonStringId(value.name),
                ...(value.declaredType === undefined
                    ? {}
                    : { declaredType: toJsonLocalNodeId(value.declaredType) }),
                ...(value.default === undefined
                    ? {}
                    : { default: toJsonLocalNodeId(value.default) }),
                isOptional: value.isOptional,
            };
        case "pattern":
            return {
                kind: "pattern",
                pattern: toJsonLocalNodeId(value.pattern),
                ...(value.declaredType === undefined
                    ? {}
                    : { declaredType: toJsonLocalNodeId(value.declaredType) }),
                ...(value.default === undefined
                    ? {}
                    : { default: toJsonLocalNodeId(value.default) }),
                isOptional: value.isOptional,
            };
        case "variadicNamed":
            return {
                kind: "variadicNamed",
                name: toJsonStringId(value.name),
                ...(value.declaredType === undefined
                    ? {}
                    : { declaredType: toJsonLocalNodeId(value.declaredType) }),
            };
        case "variadicPattern":
            return {
                kind: "variadicPattern",
                pattern: toJsonLocalNodeId(value.pattern),
                ...(value.declaredType === undefined
                    ? {}
                    : { declaredType: toJsonLocalNodeId(value.declaredType) }),
            };
        case "error":
            return {
                kind: "error",
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one Parameter from one JSON value. */
export function fromJsonParameter(value: Json): Parameter {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "named":
            return {
                kind,
                name: fromJsonStringId(jsonField(object, "name")),
                declaredType: jsonOptional(object, "declaredType", (value) =>
                    fromJsonLocalNodeId(value),
                ),
                default: jsonOptional(object, "default", (value) => fromJsonLocalNodeId(value)),
                isOptional: jsonBool(jsonField(object, "isOptional")),
            };
        case "pattern":
            return {
                kind,
                pattern: fromJsonLocalNodeId(jsonField(object, "pattern")),
                declaredType: jsonOptional(object, "declaredType", (value) =>
                    fromJsonLocalNodeId(value),
                ),
                default: jsonOptional(object, "default", (value) => fromJsonLocalNodeId(value)),
                isOptional: jsonBool(jsonField(object, "isOptional")),
            };
        case "variadicNamed":
            return {
                kind,
                name: fromJsonStringId(jsonField(object, "name")),
                declaredType: jsonOptional(object, "declaredType", (value) =>
                    fromJsonLocalNodeId(value),
                ),
            };
        case "variadicPattern":
            return {
                kind,
                pattern: fromJsonLocalNodeId(jsonField(object, "pattern")),
                declaredType: jsonOptional(object, "declaredType", (value) =>
                    fromJsonLocalNodeId(value),
                ),
            };
        case "error":
            return {
                kind,
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** One tuple type element. */
export type TupleElement =
    /** One non-spread tuple element. */
    | {
          readonly kind: "element";
          readonly label?: StringId;
          readonly value: LocalNodeId;
          readonly isOptional: boolean;
          readonly isReadonly: boolean;
      }
    /** One spread tuple element. */
    | {
          readonly kind: "spread";
          readonly label?: StringId;
          readonly value: LocalNodeId;
      }
    /** Malformed tuple element slot. */
    | {
          readonly kind: "error";
      };

export const TupleElement = {
    /** One non-spread tuple element. */
    element(
        label: StringId | undefined,
        value: LocalNodeId,
        isOptional: boolean,
        isReadonly: boolean,
    ): TupleElement {
        return { kind: "element", label, value, isOptional, isReadonly };
    },

    /** One spread tuple element. */
    spread(label: StringId | undefined, value: LocalNodeId): TupleElement {
        return { kind: "spread", label, value };
    },

    /** Malformed tuple element slot. */
    error(): TupleElement {
        return { kind: "error" };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: TupleElement): void {
        encodeTupleElement(writer, value);
    },

    /** Decode one TupleElement. */
    decode(reader: BinaryReader): TupleElement {
        return decodeTupleElement(reader);
    },

    /** Return this value as JSON. */
    toJson(value: TupleElement): Json {
        return toJsonTupleElement(value);
    },

    /** Return one TupleElement from one JSON value. */
    fromJson(value: Json): TupleElement {
        return fromJsonTupleElement(value);
    },
};

/** Encode one TupleElement. */
export function encodeTupleElement(writer: BinaryWriter, value: TupleElement): void {
    switch (value.kind) {
        case "element":
            writer.writeUnsigned(0);
            writer.writeOption(value.label, (value0) => {
                encodeStringId(writer, value0);
            });
            encodeLocalNodeId(writer, value.value);
            writer.writeBool(value.isOptional);
            writer.writeBool(value.isReadonly);
            return;
        case "spread":
            writer.writeUnsigned(1);
            writer.writeOption(value.label, (value0) => {
                encodeStringId(writer, value0);
            });
            encodeLocalNodeId(writer, value.value);
            return;
        case "error":
            writer.writeUnsigned(2);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one TupleElement. */
export function decodeTupleElement(reader: BinaryReader): TupleElement {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const label = reader.readOption(() => decodeStringId(reader));
            const value = decodeLocalNodeId(reader);
            const isOptional = reader.readBool();
            const isReadonly = reader.readBool();

            return {
                kind: "element",
                ...(label === undefined ? {} : { label }),
                value,
                isOptional,
                isReadonly,
            };
        }
        case 1: {
            const label = reader.readOption(() => decodeStringId(reader));
            const value = decodeLocalNodeId(reader);

            return {
                kind: "spread",
                ...(label === undefined ? {} : { label }),
                value,
            };
        }
        case 2: {
            return { kind: "error" };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one TupleElement. */
export function toJsonTupleElement(value: TupleElement): Json {
    switch (value.kind) {
        case "element":
            return {
                kind: "element",
                ...(value.label === undefined ? {} : { label: toJsonStringId(value.label) }),
                value: toJsonLocalNodeId(value.value),
                isOptional: value.isOptional,
                isReadonly: value.isReadonly,
            };
        case "spread":
            return {
                kind: "spread",
                ...(value.label === undefined ? {} : { label: toJsonStringId(value.label) }),
                value: toJsonLocalNodeId(value.value),
            };
        case "error":
            return {
                kind: "error",
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one TupleElement from one JSON value. */
export function fromJsonTupleElement(value: Json): TupleElement {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "element":
            return {
                kind,
                label: jsonOptional(object, "label", (value) => fromJsonStringId(value)),
                value: fromJsonLocalNodeId(jsonField(object, "value")),
                isOptional: jsonBool(jsonField(object, "isOptional")),
                isReadonly: jsonBool(jsonField(object, "isReadonly")),
            };
        case "spread":
            return {
                kind,
                label: jsonOptional(object, "label", (value) => fromJsonStringId(value)),
                value: fromJsonLocalNodeId(jsonField(object, "value")),
            };
        case "error":
            return {
                kind,
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}
