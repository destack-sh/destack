/* generated by destack_client_generate, do not edit */

import {
    BinaryReader,
    BinaryWriter,
    Json,
    SerdeError,
    jsonArray,
    jsonBool,
    jsonField,
    jsonObject,
    jsonOptional,
    jsonString,
} from "../../../protocol/serde.js";
import type { Asynchrony } from "./node.js";
import type { LocalNodeId } from "./node.js";
import type { FunctionRole } from "./property.js";
import {
    decodeAsynchrony,
    encodeAsynchrony,
    fromJsonAsynchrony,
    toJsonAsynchrony,
} from "./node.js";
import {
    decodeLocalNodeId,
    encodeLocalNodeId,
    fromJsonLocalNodeId,
    toJsonLocalNodeId,
} from "./node.js";
import {
    decodeFunctionRole,
    encodeFunctionRole,
    fromJsonFunctionRole,
    toJsonFunctionRole,
} from "./property.js";

/** The source form of a function. */
export type FunctionForm = "function" | "lambda";

export const FunctionForm = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: FunctionForm): void {
        encodeFunctionForm(writer, value);
    },

    /** Decode one FunctionForm. */
    decode(reader: BinaryReader): FunctionForm {
        return decodeFunctionForm(reader);
    },

    /** Return this value as JSON. */
    toJson(value: FunctionForm): Json {
        return toJsonFunctionForm(value);
    },

    /** Return one FunctionForm from one JSON value. */
    fromJson(value: Json): FunctionForm {
        return fromJsonFunctionForm(value);
    },
};

/** Encode one FunctionForm. */
export function encodeFunctionForm(writer: BinaryWriter, value: FunctionForm): void {
    switch (value) {
        case "function":
            writer.writeUnsigned(0);
            return;
        case "lambda":
            writer.writeUnsigned(1);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one FunctionForm. */
export function decodeFunctionForm(reader: BinaryReader): FunctionForm {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "function";
        case 1:
            return "lambda";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one FunctionForm. */
export function toJsonFunctionForm(value: FunctionForm): Json {
    return value;
}

/** Return one FunctionForm from one JSON value. */
export function fromJsonFunctionForm(value: Json): FunctionForm {
    const variant = jsonString(value);

    switch (variant) {
        case "function":
            return "function";
        case "lambda":
            return "lambda";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** When a function may be called. */
export type FunctionPhase = "normal" | "const";

export const FunctionPhase = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: FunctionPhase): void {
        encodeFunctionPhase(writer, value);
    },

    /** Decode one FunctionPhase. */
    decode(reader: BinaryReader): FunctionPhase {
        return decodeFunctionPhase(reader);
    },

    /** Return this value as JSON. */
    toJson(value: FunctionPhase): Json {
        return toJsonFunctionPhase(value);
    },

    /** Return one FunctionPhase from one JSON value. */
    fromJson(value: Json): FunctionPhase {
        return fromJsonFunctionPhase(value);
    },
};

/** Encode one FunctionPhase. */
export function encodeFunctionPhase(writer: BinaryWriter, value: FunctionPhase): void {
    switch (value) {
        case "normal":
            writer.writeUnsigned(0);
            return;
        case "const":
            writer.writeUnsigned(1);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one FunctionPhase. */
export function decodeFunctionPhase(reader: BinaryReader): FunctionPhase {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "normal";
        case 1:
            return "const";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one FunctionPhase. */
export function toJsonFunctionPhase(value: FunctionPhase): Json {
    return value;
}

/** Return one FunctionPhase from one JSON value. */
export function fromJsonFunctionPhase(value: Json): FunctionPhase {
    const variant = jsonString(value);

    switch (variant) {
        case "normal":
            return "normal";
        case "const":
            return "const";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** The signature of a function. */
export type FunctionSignature = {
    /** The asynchrony of the function. */
    readonly asynchrony: Asynchrony;
    /** The special role of the function. */
    readonly role?: FunctionRole;
    /** The source form of the function. */
    readonly form: FunctionForm;
    /** When the function may be called. */
    readonly phase: FunctionPhase;
    /** The generic parameters of the function. */
    readonly genericParameters: ReadonlyArray<LocalNodeId>;
    /** The where clauses of the function. */
    readonly whereClauses: ReadonlyArray<LocalNodeId>;
    /** The source form used for the receiver. */
    readonly thisForm?: ThisForm;
    /** The optional `this` parameter. */
    readonly thisParameter?: LocalNodeId;
    /** The value parameter source nodes of the function. */
    readonly parameters: ReadonlyArray<LocalNodeId>;
    /** The return type of the function. */
    readonly returnType?: LocalNodeId;
    /** Whether the function is abstract. */
    readonly isAbstract: boolean;
    /** Whether the function is an override. */
    readonly isOverride: boolean;
    /** Whether the function is a generator. */
    readonly isGenerator: boolean;
};

export const FunctionSignature = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: FunctionSignature): void {
        encodeFunctionSignature(writer, value);
    },

    /** Decode one FunctionSignature. */
    decode(reader: BinaryReader): FunctionSignature {
        return decodeFunctionSignature(reader);
    },

    /** Return this value as JSON. */
    toJson(value: FunctionSignature): Json {
        return toJsonFunctionSignature(value);
    },

    /** Return one FunctionSignature from one JSON value. */
    fromJson(value: Json): FunctionSignature {
        return fromJsonFunctionSignature(value);
    },
};

/** Encode one FunctionSignature. */
export function encodeFunctionSignature(writer: BinaryWriter, value: FunctionSignature): void {
    encodeAsynchrony(writer, value.asynchrony);
    writer.writeOption(value.role, (value1) => {
        encodeFunctionRole(writer, value1);
    });
    encodeFunctionForm(writer, value.form);
    encodeFunctionPhase(writer, value.phase);
    writer.writeUnsigned(value.genericParameters.length);
    for (const item4 of value.genericParameters) {
        encodeLocalNodeId(writer, item4);
    }
    writer.writeUnsigned(value.whereClauses.length);
    for (const item5 of value.whereClauses) {
        encodeLocalNodeId(writer, item5);
    }
    writer.writeOption(value.thisForm, (value6) => {
        encodeThisForm(writer, value6);
    });
    writer.writeOption(value.thisParameter, (value7) => {
        encodeLocalNodeId(writer, value7);
    });
    writer.writeUnsigned(value.parameters.length);
    for (const item8 of value.parameters) {
        encodeLocalNodeId(writer, item8);
    }
    writer.writeOption(value.returnType, (value9) => {
        encodeLocalNodeId(writer, value9);
    });
    writer.writeBool(value.isAbstract);
    writer.writeBool(value.isOverride);
    writer.writeBool(value.isGenerator);
}

/** Decode one FunctionSignature. */
export function decodeFunctionSignature(reader: BinaryReader): FunctionSignature {
    const asynchrony = decodeAsynchrony(reader);
    const role = reader.readOption(() => decodeFunctionRole(reader));
    const form = decodeFunctionForm(reader);
    const phase = decodeFunctionPhase(reader);
    const genericParameters = (() => {
        const length4 = reader.readNumber();
        const items4: Array<LocalNodeId> = [];
        for (let index = 0; index < length4; index += 1) {
            items4.push(decodeLocalNodeId(reader));
        }
        return items4;
    })();
    const whereClauses = (() => {
        const length5 = reader.readNumber();
        const items5: Array<LocalNodeId> = [];
        for (let index = 0; index < length5; index += 1) {
            items5.push(decodeLocalNodeId(reader));
        }
        return items5;
    })();
    const thisForm = reader.readOption(() => decodeThisForm(reader));
    const thisParameter = reader.readOption(() => decodeLocalNodeId(reader));
    const parameters = (() => {
        const length8 = reader.readNumber();
        const items8: Array<LocalNodeId> = [];
        for (let index = 0; index < length8; index += 1) {
            items8.push(decodeLocalNodeId(reader));
        }
        return items8;
    })();
    const returnType = reader.readOption(() => decodeLocalNodeId(reader));
    const isAbstract = reader.readBool();
    const isOverride = reader.readBool();
    const isGenerator = reader.readBool();

    return {
        asynchrony,
        ...(role === undefined ? {} : { role }),
        form,
        phase,
        genericParameters,
        whereClauses,
        ...(thisForm === undefined ? {} : { thisForm }),
        ...(thisParameter === undefined ? {} : { thisParameter }),
        parameters,
        ...(returnType === undefined ? {} : { returnType }),
        isAbstract,
        isOverride,
        isGenerator,
    };
}

/** Return one JSON value for one FunctionSignature. */
export function toJsonFunctionSignature(value: FunctionSignature): Json {
    return {
        asynchrony: toJsonAsynchrony(value.asynchrony),
        ...(value.role === undefined ? {} : { role: toJsonFunctionRole(value.role) }),
        form: toJsonFunctionForm(value.form),
        phase: toJsonFunctionPhase(value.phase),
        genericParameters: value.genericParameters.map((item0) => toJsonLocalNodeId(item0)),
        whereClauses: value.whereClauses.map((item0) => toJsonLocalNodeId(item0)),
        ...(value.thisForm === undefined ? {} : { thisForm: toJsonThisForm(value.thisForm) }),
        ...(value.thisParameter === undefined
            ? {}
            : { thisParameter: toJsonLocalNodeId(value.thisParameter) }),
        parameters: value.parameters.map((item0) => toJsonLocalNodeId(item0)),
        ...(value.returnType === undefined
            ? {}
            : { returnType: toJsonLocalNodeId(value.returnType) }),
        isAbstract: value.isAbstract,
        isOverride: value.isOverride,
        isGenerator: value.isGenerator,
    };
}

/** Return one FunctionSignature from one JSON value. */
export function fromJsonFunctionSignature(value: Json): FunctionSignature {
    const object = jsonObject(value);

    return {
        asynchrony: fromJsonAsynchrony(jsonField(object, "asynchrony")),
        role: jsonOptional(object, "role", (value) => fromJsonFunctionRole(value)),
        form: fromJsonFunctionForm(jsonField(object, "form")),
        phase: fromJsonFunctionPhase(jsonField(object, "phase")),
        genericParameters: jsonArray(jsonField(object, "genericParameters")).map((item0) =>
            fromJsonLocalNodeId(item0),
        ),
        whereClauses: jsonArray(jsonField(object, "whereClauses")).map((item0) =>
            fromJsonLocalNodeId(item0),
        ),
        thisForm: jsonOptional(object, "thisForm", (value) => fromJsonThisForm(value)),
        thisParameter: jsonOptional(object, "thisParameter", (value) => fromJsonLocalNodeId(value)),
        parameters: jsonArray(jsonField(object, "parameters")).map((item0) =>
            fromJsonLocalNodeId(item0),
        ),
        returnType: jsonOptional(object, "returnType", (value) => fromJsonLocalNodeId(value)),
        isAbstract: jsonBool(jsonField(object, "isAbstract")),
        isOverride: jsonBool(jsonField(object, "isOverride")),
        isGenerator: jsonBool(jsonField(object, "isGenerator")),
    };
}

/** The source form used to spell a receiver. */
export type ThisForm = "implicit" | "explicit";

export const ThisForm = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ThisForm): void {
        encodeThisForm(writer, value);
    },

    /** Decode one ThisForm. */
    decode(reader: BinaryReader): ThisForm {
        return decodeThisForm(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ThisForm): Json {
        return toJsonThisForm(value);
    },

    /** Return one ThisForm from one JSON value. */
    fromJson(value: Json): ThisForm {
        return fromJsonThisForm(value);
    },
};

/** Encode one ThisForm. */
export function encodeThisForm(writer: BinaryWriter, value: ThisForm): void {
    switch (value) {
        case "implicit":
            writer.writeUnsigned(0);
            return;
        case "explicit":
            writer.writeUnsigned(1);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one ThisForm. */
export function decodeThisForm(reader: BinaryReader): ThisForm {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "implicit";
        case 1:
            return "explicit";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one ThisForm. */
export function toJsonThisForm(value: ThisForm): Json {
    return value;
}

/** Return one ThisForm from one JSON value. */
export function fromJsonThisForm(value: Json): ThisForm {
    const variant = jsonString(value);

    switch (variant) {
        case "implicit":
            return "implicit";
        case "explicit":
            return "explicit";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}
