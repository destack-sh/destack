/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, jsonArray, jsonBool, jsonField, jsonObject, jsonOptional, jsonString } from "../../../protocol/serde.js";
import type { ExportKind } from "./dependency.js";
import type { FunctionSignature } from "./function.js";
import type { Name } from "./name.js";
import type { LocalNodeId } from "./node.js";
import type { Mutability } from "./node.js";
import { decodeExportKind, encodeExportKind, fromJsonExportKind, toJsonExportKind } from "./dependency.js";
import { decodeFunctionSignature, encodeFunctionSignature, fromJsonFunctionSignature, toJsonFunctionSignature } from "./function.js";
import { decodeName, encodeName, fromJsonName, toJsonName } from "./name.js";
import { decodeLocalNodeId, encodeLocalNodeId, fromJsonLocalNodeId, toJsonLocalNodeId } from "./node.js";
import { decodeMutability, encodeMutability, fromJsonMutability, toJsonMutability } from "./node.js";

/** A class declaration. */
export type ClassDeclaration = {
    /** The declared name. */
    readonly name?: Name;
    /** The export kind of the declaration. */
    readonly export?: ExportKind;
    /** The explicit placement modifier. */
    readonly place?: PlaceModifier;
    /** The generic parameters of the declaration. */
    readonly genericParameters: ReadonlyArray<LocalNodeId>;
    /** The where clauses of the declaration. */
    readonly whereClauses: ReadonlyArray<LocalNodeId>;
    /** The extended class type. */
    readonly extendsType?: LocalNodeId;
    /** The implemented interfaces. */
    readonly implementsTypes: ReadonlyArray<LocalNodeId>;
    /** The class members. */
    readonly members: ReadonlyArray<LocalNodeId>;
    /** Whether the declaration is ambient. */
    readonly isAmbient: boolean;
    /** Whether the declaration is abstract. */
    readonly isAbstract: boolean;
    /** Whether the declaration is final. */
    readonly isFinal: boolean;
};

export const ClassDeclaration = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ClassDeclaration): void {
        encodeClassDeclaration(writer, value);
    },

    /** Decode one ClassDeclaration. */
    decode(reader: BinaryReader): ClassDeclaration {
        return decodeClassDeclaration(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ClassDeclaration): Json {
        return toJsonClassDeclaration(value);
    },

    /** Return one ClassDeclaration from one JSON value. */
    fromJson(value: Json): ClassDeclaration {
        return fromJsonClassDeclaration(value);
    },
};

/** Encode one ClassDeclaration. */
export function encodeClassDeclaration(writer: BinaryWriter, value: ClassDeclaration): void {
    writer.writeOption(value.name, (value0) => {
        encodeName(writer, value0);
    });
    writer.writeOption(value.export, (value1) => {
        encodeExportKind(writer, value1);
    });
    writer.writeOption(value.place, (value2) => {
        encodePlaceModifier(writer, value2);
    });
    writer.writeUnsigned(value.genericParameters.length);
    for (const item3 of value.genericParameters) {
        encodeLocalNodeId(writer, item3);
    }
    writer.writeUnsigned(value.whereClauses.length);
    for (const item4 of value.whereClauses) {
        encodeLocalNodeId(writer, item4);
    }
    writer.writeOption(value.extendsType, (value5) => {
        encodeLocalNodeId(writer, value5);
    });
    writer.writeUnsigned(value.implementsTypes.length);
    for (const item6 of value.implementsTypes) {
        encodeLocalNodeId(writer, item6);
    }
    writer.writeUnsigned(value.members.length);
    for (const item7 of value.members) {
        encodeLocalNodeId(writer, item7);
    }
    writer.writeBool(value.isAmbient);
    writer.writeBool(value.isAbstract);
    writer.writeBool(value.isFinal);
}

/** Decode one ClassDeclaration. */
export function decodeClassDeclaration(reader: BinaryReader): ClassDeclaration {
    const name = reader.readOption(() => decodeName(reader));
    const export_ = reader.readOption(() => decodeExportKind(reader));
    const place = reader.readOption(() => decodePlaceModifier(reader));
    const genericParameters = (() => { const length3 = reader.readNumber(); const items3: Array<LocalNodeId> = []; for (let index = 0; index < length3; index += 1) { items3.push(decodeLocalNodeId(reader)); } return items3; })();
    const whereClauses = (() => { const length4 = reader.readNumber(); const items4: Array<LocalNodeId> = []; for (let index = 0; index < length4; index += 1) { items4.push(decodeLocalNodeId(reader)); } return items4; })();
    const extendsType = reader.readOption(() => decodeLocalNodeId(reader));
    const implementsTypes = (() => { const length6 = reader.readNumber(); const items6: Array<LocalNodeId> = []; for (let index = 0; index < length6; index += 1) { items6.push(decodeLocalNodeId(reader)); } return items6; })();
    const members = (() => { const length7 = reader.readNumber(); const items7: Array<LocalNodeId> = []; for (let index = 0; index < length7; index += 1) { items7.push(decodeLocalNodeId(reader)); } return items7; })();
    const isAmbient = reader.readBool();
    const isAbstract = reader.readBool();
    const isFinal = reader.readBool();

    return {
        ...(name === undefined ? {} : { name }),
        ...(export_ === undefined ? {} : { export: export_ }),
        ...(place === undefined ? {} : { place }),
        genericParameters,
        whereClauses,
        ...(extendsType === undefined ? {} : { extendsType }),
        implementsTypes,
        members,
        isAmbient,
        isAbstract,
        isFinal,
    };
}

/** Return one JSON value for one ClassDeclaration. */
export function toJsonClassDeclaration(value: ClassDeclaration): Json {
    return {
        ...(value.name === undefined ? {} : { name: toJsonName(value.name) }),
        ...(value.export === undefined ? {} : { export: toJsonExportKind(value.export) }),
        ...(value.place === undefined ? {} : { place: toJsonPlaceModifier(value.place) }),
        genericParameters: value.genericParameters.map((item0) => toJsonLocalNodeId(item0)),
        whereClauses: value.whereClauses.map((item0) => toJsonLocalNodeId(item0)),
        ...(value.extendsType === undefined ? {} : { extendsType: toJsonLocalNodeId(value.extendsType) }),
        implementsTypes: value.implementsTypes.map((item0) => toJsonLocalNodeId(item0)),
        members: value.members.map((item0) => toJsonLocalNodeId(item0)),
        isAmbient: value.isAmbient,
        isAbstract: value.isAbstract,
        isFinal: value.isFinal,
    };
}

/** Return one ClassDeclaration from one JSON value. */
export function fromJsonClassDeclaration(value: Json): ClassDeclaration {
    const object = jsonObject(value);

    return {
        name: jsonOptional(object, "name", (value) => fromJsonName(value)),
        export: jsonOptional(object, "export", (value) => fromJsonExportKind(value)),
        place: jsonOptional(object, "place", (value) => fromJsonPlaceModifier(value)),
        genericParameters: jsonArray(jsonField(object, "genericParameters")).map((item0) => fromJsonLocalNodeId(item0)),
        whereClauses: jsonArray(jsonField(object, "whereClauses")).map((item0) => fromJsonLocalNodeId(item0)),
        extendsType: jsonOptional(object, "extendsType", (value) => fromJsonLocalNodeId(value)),
        implementsTypes: jsonArray(jsonField(object, "implementsTypes")).map((item0) => fromJsonLocalNodeId(item0)),
        members: jsonArray(jsonField(object, "members")).map((item0) => fromJsonLocalNodeId(item0)),
        isAmbient: jsonBool(jsonField(object, "isAmbient")),
        isAbstract: jsonBool(jsonField(object, "isAbstract")),
        isFinal: jsonBool(jsonField(object, "isFinal")),
    };
}

/** Declaration introduces a type or such into a scope. */
export type Declaration =
    /** Global declaration block. */
    | {
          readonly kind: "global";
          readonly global: GlobalDeclaration;
      }
    /** Module declaration block. */
    | {
          readonly kind: "module";
          readonly module: ModuleDeclaration;
      }
    /** Type declaration. */
    | {
          readonly kind: "type";
          readonly type: TypeDeclaration;
      }
    /** Struct declaration. */
    | {
          readonly kind: "struct";
          readonly struct: StructDeclaration;
      }
    /** Class declaration. */
    | {
          readonly kind: "class";
          readonly class: ClassDeclaration;
      }
    /** Enum declaration. */
    | {
          readonly kind: "enum";
          readonly enum: EnumDeclaration;
      }
    /** Interface declaration. */
    | {
          readonly kind: "interface";
          readonly interface: InterfaceDeclaration;
      }
    /** Extension declaration. */
    | {
          readonly kind: "extension";
          readonly extension: ExtensionDeclaration;
      }
    /** Function declaration. */
    | {
          readonly kind: "function";
          readonly function: FunctionDeclaration;
      }
;

export const Declaration = {
    /** Global declaration block. */
    global(global: GlobalDeclaration): Declaration {
        return { kind: "global", global };
    },

    /** Module declaration block. */
    "module"(module_: ModuleDeclaration): Declaration {
        return { kind: "module", module: module_ };
    },

    /** Type declaration. */
    "type"(type_: TypeDeclaration): Declaration {
        return { kind: "type", type: type_ };
    },

    /** Struct declaration. */
    struct(struct: StructDeclaration): Declaration {
        return { kind: "struct", struct };
    },

    /** Class declaration. */
    "class"(class_: ClassDeclaration): Declaration {
        return { kind: "class", class: class_ };
    },

    /** Enum declaration. */
    "enum"(enum_: EnumDeclaration): Declaration {
        return { kind: "enum", enum: enum_ };
    },

    /** Interface declaration. */
    "interface"(interface_: InterfaceDeclaration): Declaration {
        return { kind: "interface", interface: interface_ };
    },

    /** Extension declaration. */
    extension(extension: ExtensionDeclaration): Declaration {
        return { kind: "extension", extension };
    },

    /** Function declaration. */
    "function"(function_: FunctionDeclaration): Declaration {
        return { kind: "function", function: function_ };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: Declaration): void {
        encodeDeclaration(writer, value);
    },

    /** Decode one Declaration. */
    decode(reader: BinaryReader): Declaration {
        return decodeDeclaration(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Declaration): Json {
        return toJsonDeclaration(value);
    },

    /** Return one Declaration from one JSON value. */
    fromJson(value: Json): Declaration {
        return fromJsonDeclaration(value);
    },
};

/** Encode one Declaration. */
export function encodeDeclaration(writer: BinaryWriter, value: Declaration): void {
    switch (value.kind) {
        case "global":
            writer.writeUnsigned(0);
            encodeGlobalDeclaration(writer, value.global);
            return;
        case "module":
            writer.writeUnsigned(1);
            encodeModuleDeclaration(writer, value.module);
            return;
        case "type":
            writer.writeUnsigned(2);
            encodeTypeDeclaration(writer, value.type);
            return;
        case "struct":
            writer.writeUnsigned(3);
            encodeStructDeclaration(writer, value.struct);
            return;
        case "class":
            writer.writeUnsigned(4);
            encodeClassDeclaration(writer, value.class);
            return;
        case "enum":
            writer.writeUnsigned(5);
            encodeEnumDeclaration(writer, value.enum);
            return;
        case "interface":
            writer.writeUnsigned(6);
            encodeInterfaceDeclaration(writer, value.interface);
            return;
        case "extension":
            writer.writeUnsigned(7);
            encodeExtensionDeclaration(writer, value.extension);
            return;
        case "function":
            writer.writeUnsigned(8);
            encodeFunctionDeclaration(writer, value.function);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one Declaration. */
export function decodeDeclaration(reader: BinaryReader): Declaration {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const global = decodeGlobalDeclaration(reader);

            return { kind: "global", global };
        }
        case 1: {
            const module_ = decodeModuleDeclaration(reader);

            return { kind: "module", module: module_ };
        }
        case 2: {
            const type_ = decodeTypeDeclaration(reader);

            return { kind: "type", type: type_ };
        }
        case 3: {
            const struct = decodeStructDeclaration(reader);

            return { kind: "struct", struct };
        }
        case 4: {
            const class_ = decodeClassDeclaration(reader);

            return { kind: "class", class: class_ };
        }
        case 5: {
            const enum_ = decodeEnumDeclaration(reader);

            return { kind: "enum", enum: enum_ };
        }
        case 6: {
            const interface_ = decodeInterfaceDeclaration(reader);

            return { kind: "interface", interface: interface_ };
        }
        case 7: {
            const extension = decodeExtensionDeclaration(reader);

            return { kind: "extension", extension };
        }
        case 8: {
            const function_ = decodeFunctionDeclaration(reader);

            return { kind: "function", function: function_ };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one Declaration. */
export function toJsonDeclaration(value: Declaration): Json {
    switch (value.kind) {
        case "global":
            return {
                kind: "global",
                global: toJsonGlobalDeclaration(value.global),
            };
        case "module":
            return {
                kind: "module",
                module: toJsonModuleDeclaration(value.module),
            };
        case "type":
            return {
                kind: "type",
                type: toJsonTypeDeclaration(value.type),
            };
        case "struct":
            return {
                kind: "struct",
                struct: toJsonStructDeclaration(value.struct),
            };
        case "class":
            return {
                kind: "class",
                class: toJsonClassDeclaration(value.class),
            };
        case "enum":
            return {
                kind: "enum",
                enum: toJsonEnumDeclaration(value.enum),
            };
        case "interface":
            return {
                kind: "interface",
                interface: toJsonInterfaceDeclaration(value.interface),
            };
        case "extension":
            return {
                kind: "extension",
                extension: toJsonExtensionDeclaration(value.extension),
            };
        case "function":
            return {
                kind: "function",
                function: toJsonFunctionDeclaration(value.function),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one Declaration from one JSON value. */
export function fromJsonDeclaration(value: Json): Declaration {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "global":
            return {
                kind,
                global: fromJsonGlobalDeclaration(jsonField(object, "global")),
            };
        case "module":
            return {
                kind,
                module: fromJsonModuleDeclaration(jsonField(object, "module")),
            };
        case "type":
            return {
                kind,
                type: fromJsonTypeDeclaration(jsonField(object, "type")),
            };
        case "struct":
            return {
                kind,
                struct: fromJsonStructDeclaration(jsonField(object, "struct")),
            };
        case "class":
            return {
                kind,
                class: fromJsonClassDeclaration(jsonField(object, "class")),
            };
        case "enum":
            return {
                kind,
                enum: fromJsonEnumDeclaration(jsonField(object, "enum")),
            };
        case "interface":
            return {
                kind,
                interface: fromJsonInterfaceDeclaration(jsonField(object, "interface")),
            };
        case "extension":
            return {
                kind,
                extension: fromJsonExtensionDeclaration(jsonField(object, "extension")),
            };
        case "function":
            return {
                kind,
                function: fromJsonFunctionDeclaration(jsonField(object, "function")),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** An enum declaration. */
export type EnumDeclaration = {
    /** The declared name. */
    readonly name?: Name;
    /** The export kind of the declaration. */
    readonly export?: ExportKind;
    /** The explicit placement modifier. */
    readonly place?: PlaceModifier;
    /** The enum kind. */
    readonly kind: EnumKind;
    /** The generic parameters of the declaration. */
    readonly genericParameters: ReadonlyArray<LocalNodeId>;
    /** The where clauses of the declaration. */
    readonly whereClauses: ReadonlyArray<LocalNodeId>;
    /** The implemented interfaces. */
    readonly implementsTypes: ReadonlyArray<LocalNodeId>;
    /** The enum fields. */
    readonly fields: ReadonlyArray<LocalNodeId>;
    /** The enum members. */
    readonly members: ReadonlyArray<LocalNodeId>;
    /** Whether the declaration is ambient. */
    readonly isAmbient: boolean;
};

export const EnumDeclaration = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: EnumDeclaration): void {
        encodeEnumDeclaration(writer, value);
    },

    /** Decode one EnumDeclaration. */
    decode(reader: BinaryReader): EnumDeclaration {
        return decodeEnumDeclaration(reader);
    },

    /** Return this value as JSON. */
    toJson(value: EnumDeclaration): Json {
        return toJsonEnumDeclaration(value);
    },

    /** Return one EnumDeclaration from one JSON value. */
    fromJson(value: Json): EnumDeclaration {
        return fromJsonEnumDeclaration(value);
    },
};

/** Encode one EnumDeclaration. */
export function encodeEnumDeclaration(writer: BinaryWriter, value: EnumDeclaration): void {
    writer.writeOption(value.name, (value0) => {
        encodeName(writer, value0);
    });
    writer.writeOption(value.export, (value1) => {
        encodeExportKind(writer, value1);
    });
    writer.writeOption(value.place, (value2) => {
        encodePlaceModifier(writer, value2);
    });
    encodeEnumKind(writer, value.kind);
    writer.writeUnsigned(value.genericParameters.length);
    for (const item4 of value.genericParameters) {
        encodeLocalNodeId(writer, item4);
    }
    writer.writeUnsigned(value.whereClauses.length);
    for (const item5 of value.whereClauses) {
        encodeLocalNodeId(writer, item5);
    }
    writer.writeUnsigned(value.implementsTypes.length);
    for (const item6 of value.implementsTypes) {
        encodeLocalNodeId(writer, item6);
    }
    writer.writeUnsigned(value.fields.length);
    for (const item7 of value.fields) {
        encodeLocalNodeId(writer, item7);
    }
    writer.writeUnsigned(value.members.length);
    for (const item8 of value.members) {
        encodeLocalNodeId(writer, item8);
    }
    writer.writeBool(value.isAmbient);
}

/** Decode one EnumDeclaration. */
export function decodeEnumDeclaration(reader: BinaryReader): EnumDeclaration {
    const name = reader.readOption(() => decodeName(reader));
    const export_ = reader.readOption(() => decodeExportKind(reader));
    const place = reader.readOption(() => decodePlaceModifier(reader));
    const kind = decodeEnumKind(reader);
    const genericParameters = (() => { const length4 = reader.readNumber(); const items4: Array<LocalNodeId> = []; for (let index = 0; index < length4; index += 1) { items4.push(decodeLocalNodeId(reader)); } return items4; })();
    const whereClauses = (() => { const length5 = reader.readNumber(); const items5: Array<LocalNodeId> = []; for (let index = 0; index < length5; index += 1) { items5.push(decodeLocalNodeId(reader)); } return items5; })();
    const implementsTypes = (() => { const length6 = reader.readNumber(); const items6: Array<LocalNodeId> = []; for (let index = 0; index < length6; index += 1) { items6.push(decodeLocalNodeId(reader)); } return items6; })();
    const fields = (() => { const length7 = reader.readNumber(); const items7: Array<LocalNodeId> = []; for (let index = 0; index < length7; index += 1) { items7.push(decodeLocalNodeId(reader)); } return items7; })();
    const members = (() => { const length8 = reader.readNumber(); const items8: Array<LocalNodeId> = []; for (let index = 0; index < length8; index += 1) { items8.push(decodeLocalNodeId(reader)); } return items8; })();
    const isAmbient = reader.readBool();

    return {
        ...(name === undefined ? {} : { name }),
        ...(export_ === undefined ? {} : { export: export_ }),
        ...(place === undefined ? {} : { place }),
        kind,
        genericParameters,
        whereClauses,
        implementsTypes,
        fields,
        members,
        isAmbient,
    };
}

/** Return one JSON value for one EnumDeclaration. */
export function toJsonEnumDeclaration(value: EnumDeclaration): Json {
    return {
        ...(value.name === undefined ? {} : { name: toJsonName(value.name) }),
        ...(value.export === undefined ? {} : { export: toJsonExportKind(value.export) }),
        ...(value.place === undefined ? {} : { place: toJsonPlaceModifier(value.place) }),
        kind: toJsonEnumKind(value.kind),
        genericParameters: value.genericParameters.map((item0) => toJsonLocalNodeId(item0)),
        whereClauses: value.whereClauses.map((item0) => toJsonLocalNodeId(item0)),
        implementsTypes: value.implementsTypes.map((item0) => toJsonLocalNodeId(item0)),
        fields: value.fields.map((item0) => toJsonLocalNodeId(item0)),
        members: value.members.map((item0) => toJsonLocalNodeId(item0)),
        isAmbient: value.isAmbient,
    };
}

/** Return one EnumDeclaration from one JSON value. */
export function fromJsonEnumDeclaration(value: Json): EnumDeclaration {
    const object = jsonObject(value);

    return {
        name: jsonOptional(object, "name", (value) => fromJsonName(value)),
        export: jsonOptional(object, "export", (value) => fromJsonExportKind(value)),
        place: jsonOptional(object, "place", (value) => fromJsonPlaceModifier(value)),
        kind: fromJsonEnumKind(jsonField(object, "kind")),
        genericParameters: jsonArray(jsonField(object, "genericParameters")).map((item0) => fromJsonLocalNodeId(item0)),
        whereClauses: jsonArray(jsonField(object, "whereClauses")).map((item0) => fromJsonLocalNodeId(item0)),
        implementsTypes: jsonArray(jsonField(object, "implementsTypes")).map((item0) => fromJsonLocalNodeId(item0)),
        fields: jsonArray(jsonField(object, "fields")).map((item0) => fromJsonLocalNodeId(item0)),
        members: jsonArray(jsonField(object, "members")).map((item0) => fromJsonLocalNodeId(item0)),
        isAmbient: jsonBool(jsonField(object, "isAmbient")),
    };
}

/** An enum field. */
export type EnumField = {
    /** The name of the enum field. */
    readonly name: Name;
    /** The default value of the enum field. */
    readonly value?: LocalNodeId;
};

export const EnumField = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: EnumField): void {
        encodeEnumField(writer, value);
    },

    /** Decode one EnumField. */
    decode(reader: BinaryReader): EnumField {
        return decodeEnumField(reader);
    },

    /** Return this value as JSON. */
    toJson(value: EnumField): Json {
        return toJsonEnumField(value);
    },

    /** Return one EnumField from one JSON value. */
    fromJson(value: Json): EnumField {
        return fromJsonEnumField(value);
    },
};

/** Encode one EnumField. */
export function encodeEnumField(writer: BinaryWriter, value: EnumField): void {
    encodeName(writer, value.name);
    writer.writeOption(value.value, (value1) => {
        encodeLocalNodeId(writer, value1);
    });
}

/** Decode one EnumField. */
export function decodeEnumField(reader: BinaryReader): EnumField {
    const name = decodeName(reader);
    const value = reader.readOption(() => decodeLocalNodeId(reader));

    return {
        name,
        ...(value === undefined ? {} : { value }),
    };
}

/** Return one JSON value for one EnumField. */
export function toJsonEnumField(value: EnumField): Json {
    return {
        name: toJsonName(value.name),
        ...(value.value === undefined ? {} : { value: toJsonLocalNodeId(value.value) }),
    };
}

/** Return one EnumField from one JSON value. */
export function fromJsonEnumField(value: Json): EnumField {
    const object = jsonObject(value);

    return {
        name: fromJsonName(jsonField(object, "name")),
        value: jsonOptional(object, "value", (value) => fromJsonLocalNodeId(value)),
    };
}

/** The kind of an enum declaration. */
export type EnumKind = "enum" | "const";

export const EnumKind = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: EnumKind): void {
        encodeEnumKind(writer, value);
    },

    /** Decode one EnumKind. */
    decode(reader: BinaryReader): EnumKind {
        return decodeEnumKind(reader);
    },

    /** Return this value as JSON. */
    toJson(value: EnumKind): Json {
        return toJsonEnumKind(value);
    },

    /** Return one EnumKind from one JSON value. */
    fromJson(value: Json): EnumKind {
        return fromJsonEnumKind(value);
    },
};

/** Encode one EnumKind. */
export function encodeEnumKind(writer: BinaryWriter, value: EnumKind): void {
    switch (value) {
        case "enum":
            writer.writeUnsigned(0);
            return;
        case "const":
            writer.writeUnsigned(1);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one EnumKind. */
export function decodeEnumKind(reader: BinaryReader): EnumKind {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "enum";
        case 1:
            return "const";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one EnumKind. */
export function toJsonEnumKind(value: EnumKind): Json {
    return value;
}

/** Return one EnumKind from one JSON value. */
export function fromJsonEnumKind(value: Json): EnumKind {
    const variant = jsonString(value);

    switch (variant) {
        case "enum":
            return "enum";
        case "const":
            return "const";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** An extension declaration. */
export type ExtensionDeclaration = {
    /** The declared name. */
    readonly name?: Name;
    /** The export kind of the declaration. */
    readonly export?: ExportKind;
    /** The generic parameters of the declaration. */
    readonly genericParameters: ReadonlyArray<LocalNodeId>;
    /** The where clauses of the declaration. */
    readonly whereClauses: ReadonlyArray<LocalNodeId>;
    /** The extended target type. */
    readonly targetType: LocalNodeId;
    /** The implemented interfaces. */
    readonly implementsTypes: ReadonlyArray<LocalNodeId>;
    /** The extension members. */
    readonly members: ReadonlyArray<LocalNodeId>;
    /** Whether the declaration is ambient. */
    readonly isAmbient: boolean;
};

export const ExtensionDeclaration = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ExtensionDeclaration): void {
        encodeExtensionDeclaration(writer, value);
    },

    /** Decode one ExtensionDeclaration. */
    decode(reader: BinaryReader): ExtensionDeclaration {
        return decodeExtensionDeclaration(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ExtensionDeclaration): Json {
        return toJsonExtensionDeclaration(value);
    },

    /** Return one ExtensionDeclaration from one JSON value. */
    fromJson(value: Json): ExtensionDeclaration {
        return fromJsonExtensionDeclaration(value);
    },
};

/** Encode one ExtensionDeclaration. */
export function encodeExtensionDeclaration(writer: BinaryWriter, value: ExtensionDeclaration): void {
    writer.writeOption(value.name, (value0) => {
        encodeName(writer, value0);
    });
    writer.writeOption(value.export, (value1) => {
        encodeExportKind(writer, value1);
    });
    writer.writeUnsigned(value.genericParameters.length);
    for (const item2 of value.genericParameters) {
        encodeLocalNodeId(writer, item2);
    }
    writer.writeUnsigned(value.whereClauses.length);
    for (const item3 of value.whereClauses) {
        encodeLocalNodeId(writer, item3);
    }
    encodeLocalNodeId(writer, value.targetType);
    writer.writeUnsigned(value.implementsTypes.length);
    for (const item5 of value.implementsTypes) {
        encodeLocalNodeId(writer, item5);
    }
    writer.writeUnsigned(value.members.length);
    for (const item6 of value.members) {
        encodeLocalNodeId(writer, item6);
    }
    writer.writeBool(value.isAmbient);
}

/** Decode one ExtensionDeclaration. */
export function decodeExtensionDeclaration(reader: BinaryReader): ExtensionDeclaration {
    const name = reader.readOption(() => decodeName(reader));
    const export_ = reader.readOption(() => decodeExportKind(reader));
    const genericParameters = (() => { const length2 = reader.readNumber(); const items2: Array<LocalNodeId> = []; for (let index = 0; index < length2; index += 1) { items2.push(decodeLocalNodeId(reader)); } return items2; })();
    const whereClauses = (() => { const length3 = reader.readNumber(); const items3: Array<LocalNodeId> = []; for (let index = 0; index < length3; index += 1) { items3.push(decodeLocalNodeId(reader)); } return items3; })();
    const targetType = decodeLocalNodeId(reader);
    const implementsTypes = (() => { const length5 = reader.readNumber(); const items5: Array<LocalNodeId> = []; for (let index = 0; index < length5; index += 1) { items5.push(decodeLocalNodeId(reader)); } return items5; })();
    const members = (() => { const length6 = reader.readNumber(); const items6: Array<LocalNodeId> = []; for (let index = 0; index < length6; index += 1) { items6.push(decodeLocalNodeId(reader)); } return items6; })();
    const isAmbient = reader.readBool();

    return {
        ...(name === undefined ? {} : { name }),
        ...(export_ === undefined ? {} : { export: export_ }),
        genericParameters,
        whereClauses,
        targetType,
        implementsTypes,
        members,
        isAmbient,
    };
}

/** Return one JSON value for one ExtensionDeclaration. */
export function toJsonExtensionDeclaration(value: ExtensionDeclaration): Json {
    return {
        ...(value.name === undefined ? {} : { name: toJsonName(value.name) }),
        ...(value.export === undefined ? {} : { export: toJsonExportKind(value.export) }),
        genericParameters: value.genericParameters.map((item0) => toJsonLocalNodeId(item0)),
        whereClauses: value.whereClauses.map((item0) => toJsonLocalNodeId(item0)),
        targetType: toJsonLocalNodeId(value.targetType),
        implementsTypes: value.implementsTypes.map((item0) => toJsonLocalNodeId(item0)),
        members: value.members.map((item0) => toJsonLocalNodeId(item0)),
        isAmbient: value.isAmbient,
    };
}

/** Return one ExtensionDeclaration from one JSON value. */
export function fromJsonExtensionDeclaration(value: Json): ExtensionDeclaration {
    const object = jsonObject(value);

    return {
        name: jsonOptional(object, "name", (value) => fromJsonName(value)),
        export: jsonOptional(object, "export", (value) => fromJsonExportKind(value)),
        genericParameters: jsonArray(jsonField(object, "genericParameters")).map((item0) => fromJsonLocalNodeId(item0)),
        whereClauses: jsonArray(jsonField(object, "whereClauses")).map((item0) => fromJsonLocalNodeId(item0)),
        targetType: fromJsonLocalNodeId(jsonField(object, "targetType")),
        implementsTypes: jsonArray(jsonField(object, "implementsTypes")).map((item0) => fromJsonLocalNodeId(item0)),
        members: jsonArray(jsonField(object, "members")).map((item0) => fromJsonLocalNodeId(item0)),
        isAmbient: jsonBool(jsonField(object, "isAmbient")),
    };
}

/** A function declaration. */
export type FunctionDeclaration = {
    /** The declared name. */
    readonly name?: Name;
    /** The export kind of the declaration. */
    readonly export?: ExportKind;
    /** The function signature. */
    readonly signature: FunctionSignature;
    /** The optional function body. */
    readonly body?: LocalNodeId;
    /** Whether the declaration is ambient. */
    readonly isAmbient: boolean;
};

export const FunctionDeclaration = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: FunctionDeclaration): void {
        encodeFunctionDeclaration(writer, value);
    },

    /** Decode one FunctionDeclaration. */
    decode(reader: BinaryReader): FunctionDeclaration {
        return decodeFunctionDeclaration(reader);
    },

    /** Return this value as JSON. */
    toJson(value: FunctionDeclaration): Json {
        return toJsonFunctionDeclaration(value);
    },

    /** Return one FunctionDeclaration from one JSON value. */
    fromJson(value: Json): FunctionDeclaration {
        return fromJsonFunctionDeclaration(value);
    },
};

/** Encode one FunctionDeclaration. */
export function encodeFunctionDeclaration(writer: BinaryWriter, value: FunctionDeclaration): void {
    writer.writeOption(value.name, (value0) => {
        encodeName(writer, value0);
    });
    writer.writeOption(value.export, (value1) => {
        encodeExportKind(writer, value1);
    });
    encodeFunctionSignature(writer, value.signature);
    writer.writeOption(value.body, (value3) => {
        encodeLocalNodeId(writer, value3);
    });
    writer.writeBool(value.isAmbient);
}

/** Decode one FunctionDeclaration. */
export function decodeFunctionDeclaration(reader: BinaryReader): FunctionDeclaration {
    const name = reader.readOption(() => decodeName(reader));
    const export_ = reader.readOption(() => decodeExportKind(reader));
    const signature = decodeFunctionSignature(reader);
    const body = reader.readOption(() => decodeLocalNodeId(reader));
    const isAmbient = reader.readBool();

    return {
        ...(name === undefined ? {} : { name }),
        ...(export_ === undefined ? {} : { export: export_ }),
        signature,
        ...(body === undefined ? {} : { body }),
        isAmbient,
    };
}

/** Return one JSON value for one FunctionDeclaration. */
export function toJsonFunctionDeclaration(value: FunctionDeclaration): Json {
    return {
        ...(value.name === undefined ? {} : { name: toJsonName(value.name) }),
        ...(value.export === undefined ? {} : { export: toJsonExportKind(value.export) }),
        signature: toJsonFunctionSignature(value.signature),
        ...(value.body === undefined ? {} : { body: toJsonLocalNodeId(value.body) }),
        isAmbient: value.isAmbient,
    };
}

/** Return one FunctionDeclaration from one JSON value. */
export function fromJsonFunctionDeclaration(value: Json): FunctionDeclaration {
    const object = jsonObject(value);

    return {
        name: jsonOptional(object, "name", (value) => fromJsonName(value)),
        export: jsonOptional(object, "export", (value) => fromJsonExportKind(value)),
        signature: fromJsonFunctionSignature(jsonField(object, "signature")),
        body: jsonOptional(object, "body", (value) => fromJsonLocalNodeId(value)),
        isAmbient: jsonBool(jsonField(object, "isAmbient")),
    };
}

/** A global declaration block. */
export type GlobalDeclaration = {
    /** The expressions inside the global body. */
    readonly expressions: ReadonlyArray<LocalNodeId>;
    /** Whether the declaration is ambient. */
    readonly isAmbient: boolean;
};

export const GlobalDeclaration = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: GlobalDeclaration): void {
        encodeGlobalDeclaration(writer, value);
    },

    /** Decode one GlobalDeclaration. */
    decode(reader: BinaryReader): GlobalDeclaration {
        return decodeGlobalDeclaration(reader);
    },

    /** Return this value as JSON. */
    toJson(value: GlobalDeclaration): Json {
        return toJsonGlobalDeclaration(value);
    },

    /** Return one GlobalDeclaration from one JSON value. */
    fromJson(value: Json): GlobalDeclaration {
        return fromJsonGlobalDeclaration(value);
    },
};

/** Encode one GlobalDeclaration. */
export function encodeGlobalDeclaration(writer: BinaryWriter, value: GlobalDeclaration): void {
    writer.writeUnsigned(value.expressions.length);
    for (const item0 of value.expressions) {
        encodeLocalNodeId(writer, item0);
    }
    writer.writeBool(value.isAmbient);
}

/** Decode one GlobalDeclaration. */
export function decodeGlobalDeclaration(reader: BinaryReader): GlobalDeclaration {
    const expressions = (() => { const length0 = reader.readNumber(); const items0: Array<LocalNodeId> = []; for (let index = 0; index < length0; index += 1) { items0.push(decodeLocalNodeId(reader)); } return items0; })();
    const isAmbient = reader.readBool();

    return {
        expressions,
        isAmbient,
    };
}

/** Return one JSON value for one GlobalDeclaration. */
export function toJsonGlobalDeclaration(value: GlobalDeclaration): Json {
    return {
        expressions: value.expressions.map((item0) => toJsonLocalNodeId(item0)),
        isAmbient: value.isAmbient,
    };
}

/** Return one GlobalDeclaration from one JSON value. */
export function fromJsonGlobalDeclaration(value: Json): GlobalDeclaration {
    const object = jsonObject(value);

    return {
        expressions: jsonArray(jsonField(object, "expressions")).map((item0) => fromJsonLocalNodeId(item0)),
        isAmbient: jsonBool(jsonField(object, "isAmbient")),
    };
}

/** An interface declaration. */
export type InterfaceDeclaration = {
    /** The declared name. */
    readonly name?: Name;
    /** The export kind of the declaration. */
    readonly export?: ExportKind;
    /** The explicit placement modifier. */
    readonly place?: PlaceModifier;
    /** The generic parameters of the declaration. */
    readonly genericParameters: ReadonlyArray<LocalNodeId>;
    /** The where clauses of the declaration. */
    readonly whereClauses: ReadonlyArray<LocalNodeId>;
    /** The extended interface types. */
    readonly extendsTypes: ReadonlyArray<LocalNodeId>;
    /** The interface members. */
    readonly members: ReadonlyArray<LocalNodeId>;
    /** Whether the declaration is ambient. */
    readonly isAmbient: boolean;
    /** Whether the interface is nominal. */
    readonly isNominal: boolean;
};

export const InterfaceDeclaration = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: InterfaceDeclaration): void {
        encodeInterfaceDeclaration(writer, value);
    },

    /** Decode one InterfaceDeclaration. */
    decode(reader: BinaryReader): InterfaceDeclaration {
        return decodeInterfaceDeclaration(reader);
    },

    /** Return this value as JSON. */
    toJson(value: InterfaceDeclaration): Json {
        return toJsonInterfaceDeclaration(value);
    },

    /** Return one InterfaceDeclaration from one JSON value. */
    fromJson(value: Json): InterfaceDeclaration {
        return fromJsonInterfaceDeclaration(value);
    },
};

/** Encode one InterfaceDeclaration. */
export function encodeInterfaceDeclaration(writer: BinaryWriter, value: InterfaceDeclaration): void {
    writer.writeOption(value.name, (value0) => {
        encodeName(writer, value0);
    });
    writer.writeOption(value.export, (value1) => {
        encodeExportKind(writer, value1);
    });
    writer.writeOption(value.place, (value2) => {
        encodePlaceModifier(writer, value2);
    });
    writer.writeUnsigned(value.genericParameters.length);
    for (const item3 of value.genericParameters) {
        encodeLocalNodeId(writer, item3);
    }
    writer.writeUnsigned(value.whereClauses.length);
    for (const item4 of value.whereClauses) {
        encodeLocalNodeId(writer, item4);
    }
    writer.writeUnsigned(value.extendsTypes.length);
    for (const item5 of value.extendsTypes) {
        encodeLocalNodeId(writer, item5);
    }
    writer.writeUnsigned(value.members.length);
    for (const item6 of value.members) {
        encodeLocalNodeId(writer, item6);
    }
    writer.writeBool(value.isAmbient);
    writer.writeBool(value.isNominal);
}

/** Decode one InterfaceDeclaration. */
export function decodeInterfaceDeclaration(reader: BinaryReader): InterfaceDeclaration {
    const name = reader.readOption(() => decodeName(reader));
    const export_ = reader.readOption(() => decodeExportKind(reader));
    const place = reader.readOption(() => decodePlaceModifier(reader));
    const genericParameters = (() => { const length3 = reader.readNumber(); const items3: Array<LocalNodeId> = []; for (let index = 0; index < length3; index += 1) { items3.push(decodeLocalNodeId(reader)); } return items3; })();
    const whereClauses = (() => { const length4 = reader.readNumber(); const items4: Array<LocalNodeId> = []; for (let index = 0; index < length4; index += 1) { items4.push(decodeLocalNodeId(reader)); } return items4; })();
    const extendsTypes = (() => { const length5 = reader.readNumber(); const items5: Array<LocalNodeId> = []; for (let index = 0; index < length5; index += 1) { items5.push(decodeLocalNodeId(reader)); } return items5; })();
    const members = (() => { const length6 = reader.readNumber(); const items6: Array<LocalNodeId> = []; for (let index = 0; index < length6; index += 1) { items6.push(decodeLocalNodeId(reader)); } return items6; })();
    const isAmbient = reader.readBool();
    const isNominal = reader.readBool();

    return {
        ...(name === undefined ? {} : { name }),
        ...(export_ === undefined ? {} : { export: export_ }),
        ...(place === undefined ? {} : { place }),
        genericParameters,
        whereClauses,
        extendsTypes,
        members,
        isAmbient,
        isNominal,
    };
}

/** Return one JSON value for one InterfaceDeclaration. */
export function toJsonInterfaceDeclaration(value: InterfaceDeclaration): Json {
    return {
        ...(value.name === undefined ? {} : { name: toJsonName(value.name) }),
        ...(value.export === undefined ? {} : { export: toJsonExportKind(value.export) }),
        ...(value.place === undefined ? {} : { place: toJsonPlaceModifier(value.place) }),
        genericParameters: value.genericParameters.map((item0) => toJsonLocalNodeId(item0)),
        whereClauses: value.whereClauses.map((item0) => toJsonLocalNodeId(item0)),
        extendsTypes: value.extendsTypes.map((item0) => toJsonLocalNodeId(item0)),
        members: value.members.map((item0) => toJsonLocalNodeId(item0)),
        isAmbient: value.isAmbient,
        isNominal: value.isNominal,
    };
}

/** Return one InterfaceDeclaration from one JSON value. */
export function fromJsonInterfaceDeclaration(value: Json): InterfaceDeclaration {
    const object = jsonObject(value);

    return {
        name: jsonOptional(object, "name", (value) => fromJsonName(value)),
        export: jsonOptional(object, "export", (value) => fromJsonExportKind(value)),
        place: jsonOptional(object, "place", (value) => fromJsonPlaceModifier(value)),
        genericParameters: jsonArray(jsonField(object, "genericParameters")).map((item0) => fromJsonLocalNodeId(item0)),
        whereClauses: jsonArray(jsonField(object, "whereClauses")).map((item0) => fromJsonLocalNodeId(item0)),
        extendsTypes: jsonArray(jsonField(object, "extendsTypes")).map((item0) => fromJsonLocalNodeId(item0)),
        members: jsonArray(jsonField(object, "members")).map((item0) => fromJsonLocalNodeId(item0)),
        isAmbient: jsonBool(jsonField(object, "isAmbient")),
        isNominal: jsonBool(jsonField(object, "isNominal")),
    };
}

/** A module declaration block. */
export type ModuleDeclaration = {
    /** The expressions inside the module body. */
    readonly expressions: ReadonlyArray<LocalNodeId>;
};

export const ModuleDeclaration = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ModuleDeclaration): void {
        encodeModuleDeclaration(writer, value);
    },

    /** Decode one ModuleDeclaration. */
    decode(reader: BinaryReader): ModuleDeclaration {
        return decodeModuleDeclaration(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ModuleDeclaration): Json {
        return toJsonModuleDeclaration(value);
    },

    /** Return one ModuleDeclaration from one JSON value. */
    fromJson(value: Json): ModuleDeclaration {
        return fromJsonModuleDeclaration(value);
    },
};

/** Encode one ModuleDeclaration. */
export function encodeModuleDeclaration(writer: BinaryWriter, value: ModuleDeclaration): void {
    writer.writeUnsigned(value.expressions.length);
    for (const item0 of value.expressions) {
        encodeLocalNodeId(writer, item0);
    }
}

/** Decode one ModuleDeclaration. */
export function decodeModuleDeclaration(reader: BinaryReader): ModuleDeclaration {
    const expressions = (() => { const length0 = reader.readNumber(); const items0: Array<LocalNodeId> = []; for (let index = 0; index < length0; index += 1) { items0.push(decodeLocalNodeId(reader)); } return items0; })();

    return {
        expressions,
    };
}

/** Return one JSON value for one ModuleDeclaration. */
export function toJsonModuleDeclaration(value: ModuleDeclaration): Json {
    return {
        expressions: value.expressions.map((item0) => toJsonLocalNodeId(item0)),
    };
}

/** Return one ModuleDeclaration from one JSON value. */
export function fromJsonModuleDeclaration(value: Json): ModuleDeclaration {
    const object = jsonObject(value);

    return {
        expressions: jsonArray(jsonField(object, "expressions")).map((item0) => fromJsonLocalNodeId(item0)),
    };
}

/** Explicit source placement modifier. */
export type PlaceModifier = "local" | "shared";

export const PlaceModifier = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: PlaceModifier): void {
        encodePlaceModifier(writer, value);
    },

    /** Decode one PlaceModifier. */
    decode(reader: BinaryReader): PlaceModifier {
        return decodePlaceModifier(reader);
    },

    /** Return this value as JSON. */
    toJson(value: PlaceModifier): Json {
        return toJsonPlaceModifier(value);
    },

    /** Return one PlaceModifier from one JSON value. */
    fromJson(value: Json): PlaceModifier {
        return fromJsonPlaceModifier(value);
    },
};

/** Encode one PlaceModifier. */
export function encodePlaceModifier(writer: BinaryWriter, value: PlaceModifier): void {
    switch (value) {
        case "local":
            writer.writeUnsigned(0);
            return;
        case "shared":
            writer.writeUnsigned(1);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one PlaceModifier. */
export function decodePlaceModifier(reader: BinaryReader): PlaceModifier {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "local";
        case 1:
            return "shared";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one PlaceModifier. */
export function toJsonPlaceModifier(value: PlaceModifier): Json {
    return value;
}

/** Return one PlaceModifier from one JSON value. */
export function fromJsonPlaceModifier(value: Json): PlaceModifier {
    const variant = jsonString(value);

    switch (variant) {
        case "local":
            return "local";
        case "shared":
            return "shared";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** A struct declaration. */
export type StructDeclaration = {
    /** The declared name. */
    readonly name: Name;
    /** The export kind of the declaration. */
    readonly export?: ExportKind;
    /** The explicit placement modifier. */
    readonly place?: PlaceModifier;
    /** The generic parameters of the declaration. */
    readonly genericParameters: ReadonlyArray<LocalNodeId>;
    /** The where clauses of the declaration. */
    readonly whereClauses: ReadonlyArray<LocalNodeId>;
    /** The implemented interfaces. */
    readonly implementsTypes: ReadonlyArray<LocalNodeId>;
    /** The struct members. */
    readonly members: ReadonlyArray<LocalNodeId>;
    /** Whether the declaration is ambient. */
    readonly isAmbient: boolean;
};

export const StructDeclaration = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: StructDeclaration): void {
        encodeStructDeclaration(writer, value);
    },

    /** Decode one StructDeclaration. */
    decode(reader: BinaryReader): StructDeclaration {
        return decodeStructDeclaration(reader);
    },

    /** Return this value as JSON. */
    toJson(value: StructDeclaration): Json {
        return toJsonStructDeclaration(value);
    },

    /** Return one StructDeclaration from one JSON value. */
    fromJson(value: Json): StructDeclaration {
        return fromJsonStructDeclaration(value);
    },
};

/** Encode one StructDeclaration. */
export function encodeStructDeclaration(writer: BinaryWriter, value: StructDeclaration): void {
    encodeName(writer, value.name);
    writer.writeOption(value.export, (value1) => {
        encodeExportKind(writer, value1);
    });
    writer.writeOption(value.place, (value2) => {
        encodePlaceModifier(writer, value2);
    });
    writer.writeUnsigned(value.genericParameters.length);
    for (const item3 of value.genericParameters) {
        encodeLocalNodeId(writer, item3);
    }
    writer.writeUnsigned(value.whereClauses.length);
    for (const item4 of value.whereClauses) {
        encodeLocalNodeId(writer, item4);
    }
    writer.writeUnsigned(value.implementsTypes.length);
    for (const item5 of value.implementsTypes) {
        encodeLocalNodeId(writer, item5);
    }
    writer.writeUnsigned(value.members.length);
    for (const item6 of value.members) {
        encodeLocalNodeId(writer, item6);
    }
    writer.writeBool(value.isAmbient);
}

/** Decode one StructDeclaration. */
export function decodeStructDeclaration(reader: BinaryReader): StructDeclaration {
    const name = decodeName(reader);
    const export_ = reader.readOption(() => decodeExportKind(reader));
    const place = reader.readOption(() => decodePlaceModifier(reader));
    const genericParameters = (() => { const length3 = reader.readNumber(); const items3: Array<LocalNodeId> = []; for (let index = 0; index < length3; index += 1) { items3.push(decodeLocalNodeId(reader)); } return items3; })();
    const whereClauses = (() => { const length4 = reader.readNumber(); const items4: Array<LocalNodeId> = []; for (let index = 0; index < length4; index += 1) { items4.push(decodeLocalNodeId(reader)); } return items4; })();
    const implementsTypes = (() => { const length5 = reader.readNumber(); const items5: Array<LocalNodeId> = []; for (let index = 0; index < length5; index += 1) { items5.push(decodeLocalNodeId(reader)); } return items5; })();
    const members = (() => { const length6 = reader.readNumber(); const items6: Array<LocalNodeId> = []; for (let index = 0; index < length6; index += 1) { items6.push(decodeLocalNodeId(reader)); } return items6; })();
    const isAmbient = reader.readBool();

    return {
        name,
        ...(export_ === undefined ? {} : { export: export_ }),
        ...(place === undefined ? {} : { place }),
        genericParameters,
        whereClauses,
        implementsTypes,
        members,
        isAmbient,
    };
}

/** Return one JSON value for one StructDeclaration. */
export function toJsonStructDeclaration(value: StructDeclaration): Json {
    return {
        name: toJsonName(value.name),
        ...(value.export === undefined ? {} : { export: toJsonExportKind(value.export) }),
        ...(value.place === undefined ? {} : { place: toJsonPlaceModifier(value.place) }),
        genericParameters: value.genericParameters.map((item0) => toJsonLocalNodeId(item0)),
        whereClauses: value.whereClauses.map((item0) => toJsonLocalNodeId(item0)),
        implementsTypes: value.implementsTypes.map((item0) => toJsonLocalNodeId(item0)),
        members: value.members.map((item0) => toJsonLocalNodeId(item0)),
        isAmbient: value.isAmbient,
    };
}

/** Return one StructDeclaration from one JSON value. */
export function fromJsonStructDeclaration(value: Json): StructDeclaration {
    const object = jsonObject(value);

    return {
        name: fromJsonName(jsonField(object, "name")),
        export: jsonOptional(object, "export", (value) => fromJsonExportKind(value)),
        place: jsonOptional(object, "place", (value) => fromJsonPlaceModifier(value)),
        genericParameters: jsonArray(jsonField(object, "genericParameters")).map((item0) => fromJsonLocalNodeId(item0)),
        whereClauses: jsonArray(jsonField(object, "whereClauses")).map((item0) => fromJsonLocalNodeId(item0)),
        implementsTypes: jsonArray(jsonField(object, "implementsTypes")).map((item0) => fromJsonLocalNodeId(item0)),
        members: jsonArray(jsonField(object, "members")).map((item0) => fromJsonLocalNodeId(item0)),
        isAmbient: jsonBool(jsonField(object, "isAmbient")),
    };
}

/** A type declaration. */
export type TypeDeclaration = {
    /** The declared name. */
    readonly name: Name;
    /** The export kind of the declaration. */
    readonly export?: ExportKind;
    /** The explicit placement modifier. */
    readonly place?: PlaceModifier;
    /** The optional mutability qualifier. */
    readonly mutability?: Mutability;
    /** The generic parameters of the declaration. */
    readonly genericParameters: ReadonlyArray<LocalNodeId>;
    /** The where clauses of the declaration. */
    readonly whereClauses: ReadonlyArray<LocalNodeId>;
    /** The declared type expression. */
    readonly value: LocalNodeId;
    /** Whether the declaration is ambient. */
    readonly isAmbient: boolean;
    /** Whether the declaration is nominal. */
    readonly isNominal: boolean;
};

export const TypeDeclaration = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: TypeDeclaration): void {
        encodeTypeDeclaration(writer, value);
    },

    /** Decode one TypeDeclaration. */
    decode(reader: BinaryReader): TypeDeclaration {
        return decodeTypeDeclaration(reader);
    },

    /** Return this value as JSON. */
    toJson(value: TypeDeclaration): Json {
        return toJsonTypeDeclaration(value);
    },

    /** Return one TypeDeclaration from one JSON value. */
    fromJson(value: Json): TypeDeclaration {
        return fromJsonTypeDeclaration(value);
    },
};

/** Encode one TypeDeclaration. */
export function encodeTypeDeclaration(writer: BinaryWriter, value: TypeDeclaration): void {
    encodeName(writer, value.name);
    writer.writeOption(value.export, (value1) => {
        encodeExportKind(writer, value1);
    });
    writer.writeOption(value.place, (value2) => {
        encodePlaceModifier(writer, value2);
    });
    writer.writeOption(value.mutability, (value3) => {
        encodeMutability(writer, value3);
    });
    writer.writeUnsigned(value.genericParameters.length);
    for (const item4 of value.genericParameters) {
        encodeLocalNodeId(writer, item4);
    }
    writer.writeUnsigned(value.whereClauses.length);
    for (const item5 of value.whereClauses) {
        encodeLocalNodeId(writer, item5);
    }
    encodeLocalNodeId(writer, value.value);
    writer.writeBool(value.isAmbient);
    writer.writeBool(value.isNominal);
}

/** Decode one TypeDeclaration. */
export function decodeTypeDeclaration(reader: BinaryReader): TypeDeclaration {
    const name = decodeName(reader);
    const export_ = reader.readOption(() => decodeExportKind(reader));
    const place = reader.readOption(() => decodePlaceModifier(reader));
    const mutability = reader.readOption(() => decodeMutability(reader));
    const genericParameters = (() => { const length4 = reader.readNumber(); const items4: Array<LocalNodeId> = []; for (let index = 0; index < length4; index += 1) { items4.push(decodeLocalNodeId(reader)); } return items4; })();
    const whereClauses = (() => { const length5 = reader.readNumber(); const items5: Array<LocalNodeId> = []; for (let index = 0; index < length5; index += 1) { items5.push(decodeLocalNodeId(reader)); } return items5; })();
    const value = decodeLocalNodeId(reader);
    const isAmbient = reader.readBool();
    const isNominal = reader.readBool();

    return {
        name,
        ...(export_ === undefined ? {} : { export: export_ }),
        ...(place === undefined ? {} : { place }),
        ...(mutability === undefined ? {} : { mutability }),
        genericParameters,
        whereClauses,
        value,
        isAmbient,
        isNominal,
    };
}

/** Return one JSON value for one TypeDeclaration. */
export function toJsonTypeDeclaration(value: TypeDeclaration): Json {
    return {
        name: toJsonName(value.name),
        ...(value.export === undefined ? {} : { export: toJsonExportKind(value.export) }),
        ...(value.place === undefined ? {} : { place: toJsonPlaceModifier(value.place) }),
        ...(value.mutability === undefined ? {} : { mutability: toJsonMutability(value.mutability) }),
        genericParameters: value.genericParameters.map((item0) => toJsonLocalNodeId(item0)),
        whereClauses: value.whereClauses.map((item0) => toJsonLocalNodeId(item0)),
        value: toJsonLocalNodeId(value.value),
        isAmbient: value.isAmbient,
        isNominal: value.isNominal,
    };
}

/** Return one TypeDeclaration from one JSON value. */
export function fromJsonTypeDeclaration(value: Json): TypeDeclaration {
    const object = jsonObject(value);

    return {
        name: fromJsonName(jsonField(object, "name")),
        export: jsonOptional(object, "export", (value) => fromJsonExportKind(value)),
        place: jsonOptional(object, "place", (value) => fromJsonPlaceModifier(value)),
        mutability: jsonOptional(object, "mutability", (value) => fromJsonMutability(value)),
        genericParameters: jsonArray(jsonField(object, "genericParameters")).map((item0) => fromJsonLocalNodeId(item0)),
        whereClauses: jsonArray(jsonField(object, "whereClauses")).map((item0) => fromJsonLocalNodeId(item0)),
        value: fromJsonLocalNodeId(jsonField(object, "value")),
        isAmbient: jsonBool(jsonField(object, "isAmbient")),
        isNominal: jsonBool(jsonField(object, "isNominal")),
    };
}
