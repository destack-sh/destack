/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, jsonField, jsonObject, jsonOptional, jsonString } from "../../../protocol/serde.js";
import type { Condition } from "./expression.js";
import type { LocalNodeId } from "./node.js";
import { decodeCondition, encodeCondition, fromJsonCondition, toJsonCondition } from "./expression.js";
import { decodeLocalNodeId, encodeLocalNodeId, fromJsonLocalNodeId, toJsonLocalNodeId } from "./node.js";

/** One arm of a match expression. */
export type MatchArm =
    /** An arm with an expression body. */
    | {
          readonly kind: "expression";
          /** The selected pattern. */
          readonly pattern: LocalNodeId;
          /** The optional match guard. */
          readonly guard?: Condition;
          /** The expression body. */
          readonly body: LocalNodeId;
      }
    /** An arm with a block body. */
    | {
          readonly kind: "block";
          /** The selected pattern. */
          readonly pattern: LocalNodeId;
          /** The optional match guard. */
          readonly guard?: Condition;
          /** The block body. */
          readonly body: LocalNodeId;
      }
;

export const MatchArm = {
    /** An arm with an expression body. */
    expression(pattern: LocalNodeId, guard: Condition | undefined, body: LocalNodeId): MatchArm {
        return { kind: "expression", pattern, guard, body };
    },

    /** An arm with a block body. */
    block(pattern: LocalNodeId, guard: Condition | undefined, body: LocalNodeId): MatchArm {
        return { kind: "block", pattern, guard, body };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: MatchArm): void {
        encodeMatchArm(writer, value);
    },

    /** Decode one MatchArm. */
    decode(reader: BinaryReader): MatchArm {
        return decodeMatchArm(reader);
    },

    /** Return this value as JSON. */
    toJson(value: MatchArm): Json {
        return toJsonMatchArm(value);
    },

    /** Return one MatchArm from one JSON value. */
    fromJson(value: Json): MatchArm {
        return fromJsonMatchArm(value);
    },
};

/** Encode one MatchArm. */
export function encodeMatchArm(writer: BinaryWriter, value: MatchArm): void {
    switch (value.kind) {
        case "expression":
            writer.writeUnsigned(0);
            encodeLocalNodeId(writer, value.pattern);
            writer.writeOption(value.guard, (value1) => {
                encodeCondition(writer, value1);
            });
            encodeLocalNodeId(writer, value.body);
            return;
        case "block":
            writer.writeUnsigned(1);
            encodeLocalNodeId(writer, value.pattern);
            writer.writeOption(value.guard, (value1) => {
                encodeCondition(writer, value1);
            });
            encodeLocalNodeId(writer, value.body);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one MatchArm. */
export function decodeMatchArm(reader: BinaryReader): MatchArm {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const pattern = decodeLocalNodeId(reader);
            const guard = reader.readOption(() => decodeCondition(reader));
            const body = decodeLocalNodeId(reader);

            return {
                kind: "expression",
                pattern,
                ...(guard === undefined ? {} : { guard }),
                body,
            };
        }
        case 1: {
            const pattern = decodeLocalNodeId(reader);
            const guard = reader.readOption(() => decodeCondition(reader));
            const body = decodeLocalNodeId(reader);

            return {
                kind: "block",
                pattern,
                ...(guard === undefined ? {} : { guard }),
                body,
            };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one MatchArm. */
export function toJsonMatchArm(value: MatchArm): Json {
    switch (value.kind) {
        case "expression":
            return {
                kind: "expression",
                pattern: toJsonLocalNodeId(value.pattern),
                ...(value.guard === undefined ? {} : { guard: toJsonCondition(value.guard) }),
                body: toJsonLocalNodeId(value.body),
            };
        case "block":
            return {
                kind: "block",
                pattern: toJsonLocalNodeId(value.pattern),
                ...(value.guard === undefined ? {} : { guard: toJsonCondition(value.guard) }),
                body: toJsonLocalNodeId(value.body),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one MatchArm from one JSON value. */
export function fromJsonMatchArm(value: Json): MatchArm {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "expression":
            return {
                kind,
                pattern: fromJsonLocalNodeId(jsonField(object, "pattern")),
                guard: jsonOptional(object, "guard", (value) => fromJsonCondition(value)),
                body: fromJsonLocalNodeId(jsonField(object, "body")),
            };
        case "block":
            return {
                kind,
                pattern: fromJsonLocalNodeId(jsonField(object, "pattern")),
                guard: jsonOptional(object, "guard", (value) => fromJsonCondition(value)),
                body: fromJsonLocalNodeId(jsonField(object, "body")),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** One case of a switch statement. */
export type SwitchCase = {
    /** The case selector. */
    readonly selector: SwitchSelector;
    /** The statement body. */
    readonly body: LocalNodeId;
};

export const SwitchCase = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: SwitchCase): void {
        encodeSwitchCase(writer, value);
    },

    /** Decode one SwitchCase. */
    decode(reader: BinaryReader): SwitchCase {
        return decodeSwitchCase(reader);
    },

    /** Return this value as JSON. */
    toJson(value: SwitchCase): Json {
        return toJsonSwitchCase(value);
    },

    /** Return one SwitchCase from one JSON value. */
    fromJson(value: Json): SwitchCase {
        return fromJsonSwitchCase(value);
    },
};

/** Encode one SwitchCase. */
export function encodeSwitchCase(writer: BinaryWriter, value: SwitchCase): void {
    encodeSwitchSelector(writer, value.selector);
    encodeLocalNodeId(writer, value.body);
}

/** Decode one SwitchCase. */
export function decodeSwitchCase(reader: BinaryReader): SwitchCase {
    const selector = decodeSwitchSelector(reader);
    const body = decodeLocalNodeId(reader);

    return {
        selector,
        body,
    };
}

/** Return one JSON value for one SwitchCase. */
export function toJsonSwitchCase(value: SwitchCase): Json {
    return {
        selector: toJsonSwitchSelector(value.selector),
        body: toJsonLocalNodeId(value.body),
    };
}

/** Return one SwitchCase from one JSON value. */
export function fromJsonSwitchCase(value: Json): SwitchCase {
    const object = jsonObject(value);

    return {
        selector: fromJsonSwitchSelector(jsonField(object, "selector")),
        body: fromJsonLocalNodeId(jsonField(object, "body")),
    };
}

/** One switch case selector. */
export type SwitchSelector =
    /** A case selected by an expression value. */
    | {
          readonly kind: "case";
          readonly case: LocalNodeId;
      }
    /** The default case. */
    | {
          readonly kind: "default";
      }
;

export const SwitchSelector = {
    /** A case selected by an expression value. */
    "case"(case_: LocalNodeId): SwitchSelector {
        return { kind: "case", case: case_ };
    },

    /** The default case. */
    "default"(): SwitchSelector {
        return { kind: "default" };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: SwitchSelector): void {
        encodeSwitchSelector(writer, value);
    },

    /** Decode one SwitchSelector. */
    decode(reader: BinaryReader): SwitchSelector {
        return decodeSwitchSelector(reader);
    },

    /** Return this value as JSON. */
    toJson(value: SwitchSelector): Json {
        return toJsonSwitchSelector(value);
    },

    /** Return one SwitchSelector from one JSON value. */
    fromJson(value: Json): SwitchSelector {
        return fromJsonSwitchSelector(value);
    },
};

/** Encode one SwitchSelector. */
export function encodeSwitchSelector(writer: BinaryWriter, value: SwitchSelector): void {
    switch (value.kind) {
        case "case":
            writer.writeUnsigned(0);
            encodeLocalNodeId(writer, value.case);
            return;
        case "default":
            writer.writeUnsigned(1);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one SwitchSelector. */
export function decodeSwitchSelector(reader: BinaryReader): SwitchSelector {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const case_ = decodeLocalNodeId(reader);

            return { kind: "case", case: case_ };
        }
        case 1: {
            return { kind: "default" };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one SwitchSelector. */
export function toJsonSwitchSelector(value: SwitchSelector): Json {
    switch (value.kind) {
        case "case":
            return {
                kind: "case",
                case: toJsonLocalNodeId(value.case),
            };
        case "default":
            return {
                kind: "default",
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one SwitchSelector from one JSON value. */
export function fromJsonSwitchSelector(value: Json): SwitchSelector {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "case":
            return {
                kind,
                case: fromJsonLocalNodeId(jsonField(object, "case")),
            };
        case "default":
            return {
                kind,
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}
