/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, jsonField, jsonInteger, jsonObject, jsonString } from "../../../protocol/serde.js";
import type { StringId } from "../../core/string.js";
import { decodeStringId, encodeStringId, fromJsonStringId, toJsonStringId } from "../../core/string.js";

/** An authored identifier, string, or integer name. */
export type Name =
    /** An identifier name. */
    | {
          readonly kind: "identifier";
          readonly identifier: StringId;
      }
    /** A quoted string name. */
    | {
          readonly kind: "string";
          readonly string: StringId;
      }
    /** An integer name. */
    | {
          readonly kind: "index";
          readonly index: number;
      }
;

export const Name = {
    /** An identifier name. */
    identifier(identifier: StringId): Name {
        return { kind: "identifier", identifier };
    },

    /** A quoted string name. */
    "string"(string_: StringId): Name {
        return { kind: "string", string: string_ };
    },

    /** An integer name. */
    index(index: number): Name {
        return { kind: "index", index };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: Name): void {
        encodeName(writer, value);
    },

    /** Decode one Name. */
    decode(reader: BinaryReader): Name {
        return decodeName(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Name): Json {
        return toJsonName(value);
    },

    /** Return one Name from one JSON value. */
    fromJson(value: Json): Name {
        return fromJsonName(value);
    },
};

/** Encode one Name. */
export function encodeName(writer: BinaryWriter, value: Name): void {
    switch (value.kind) {
        case "identifier":
            writer.writeUnsigned(0);
            encodeStringId(writer, value.identifier);
            return;
        case "string":
            writer.writeUnsigned(1);
            encodeStringId(writer, value.string);
            return;
        case "index":
            writer.writeUnsigned(2);
            writer.writeUnsigned(value.index);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one Name. */
export function decodeName(reader: BinaryReader): Name {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const identifier = decodeStringId(reader);

            return { kind: "identifier", identifier };
        }
        case 1: {
            const string_ = decodeStringId(reader);

            return { kind: "string", string: string_ };
        }
        case 2: {
            const index = reader.readNumber();

            return { kind: "index", index };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one Name. */
export function toJsonName(value: Name): Json {
    switch (value.kind) {
        case "identifier":
            return {
                kind: "identifier",
                identifier: toJsonStringId(value.identifier),
            };
        case "string":
            return {
                kind: "string",
                string: toJsonStringId(value.string),
            };
        case "index":
            return {
                kind: "index",
                index: value.index,
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one Name from one JSON value. */
export function fromJsonName(value: Json): Name {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "identifier":
            return {
                kind,
                identifier: fromJsonStringId(jsonField(object, "identifier")),
            };
        case "string":
            return {
                kind,
                string: fromJsonStringId(jsonField(object, "string")),
            };
        case "index":
            return {
                kind,
                index: jsonInteger(jsonField(object, "index")),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}
