/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, jsonArray, jsonField, jsonObject, jsonString } from "../../../protocol/serde.js";
import type { Name } from "./key.js";
import type { ScalarLiteral } from "./literal.js";
import { decodeName, encodeName, fromJsonName, toJsonName } from "./key.js";
import { decodeScalarLiteral, encodeScalarLiteral, fromJsonScalarLiteral, toJsonScalarLiteral } from "./literal.js";

/** One import attribute entry. */
export type ImportAttribute = {
    /** The attribute key. */
    readonly key: Name;
    /** The attribute value. */
    readonly value: ImportAttributeValue;
};

export const ImportAttribute = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ImportAttribute): void {
        encodeImportAttribute(writer, value);
    },

    /** Decode one ImportAttribute. */
    decode(reader: BinaryReader): ImportAttribute {
        return decodeImportAttribute(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ImportAttribute): Json {
        return toJsonImportAttribute(value);
    },

    /** Return one ImportAttribute from one JSON value. */
    fromJson(value: Json): ImportAttribute {
        return fromJsonImportAttribute(value);
    },
};

/** Encode one ImportAttribute. */
export function encodeImportAttribute(writer: BinaryWriter, value: ImportAttribute): void {
    encodeName(writer, value.key);
    encodeImportAttributeValue(writer, value.value);
}

/** Decode one ImportAttribute. */
export function decodeImportAttribute(reader: BinaryReader): ImportAttribute {
    const key = decodeName(reader);
    const value = decodeImportAttributeValue(reader);

    return {
        key,
        value,
    };
}

/** Return one JSON value for one ImportAttribute. */
export function toJsonImportAttribute(value: ImportAttribute): Json {
    return {
        key: toJsonName(value.key),
        value: toJsonImportAttributeValue(value.value),
    };
}

/** Return one ImportAttribute from one JSON value. */
export function fromJsonImportAttribute(value: Json): ImportAttribute {
    const object = jsonObject(value);

    return {
        key: fromJsonName(jsonField(object, "key")),
        value: fromJsonImportAttributeValue(jsonField(object, "value")),
    };
}

/** One import attribute clause. */
export type ImportAttributeClause = {
    /** The clause introducer. */
    readonly kind: ImportAttributeClauseKind;
    /** The attribute entries inside the clause body. */
    readonly attributes: ReadonlyArray<ImportAttribute>;
};

export const ImportAttributeClause = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ImportAttributeClause): void {
        encodeImportAttributeClause(writer, value);
    },

    /** Decode one ImportAttributeClause. */
    decode(reader: BinaryReader): ImportAttributeClause {
        return decodeImportAttributeClause(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ImportAttributeClause): Json {
        return toJsonImportAttributeClause(value);
    },

    /** Return one ImportAttributeClause from one JSON value. */
    fromJson(value: Json): ImportAttributeClause {
        return fromJsonImportAttributeClause(value);
    },
};

/** Encode one ImportAttributeClause. */
export function encodeImportAttributeClause(writer: BinaryWriter, value: ImportAttributeClause): void {
    encodeImportAttributeClauseKind(writer, value.kind);
    writer.writeUnsigned(value.attributes.length);
    for (const item1 of value.attributes) {
        encodeImportAttribute(writer, item1);
    }
}

/** Decode one ImportAttributeClause. */
export function decodeImportAttributeClause(reader: BinaryReader): ImportAttributeClause {
    const kind = decodeImportAttributeClauseKind(reader);
    const attributes = (() => { const length1 = reader.readNumber(); const items1: Array<ImportAttribute> = []; for (let index = 0; index < length1; index += 1) { items1.push(decodeImportAttribute(reader)); } return items1; })();

    return {
        kind,
        attributes,
    };
}

/** Return one JSON value for one ImportAttributeClause. */
export function toJsonImportAttributeClause(value: ImportAttributeClause): Json {
    return {
        kind: toJsonImportAttributeClauseKind(value.kind),
        attributes: value.attributes.map((item0) => toJsonImportAttribute(item0)),
    };
}

/** Return one ImportAttributeClause from one JSON value. */
export function fromJsonImportAttributeClause(value: Json): ImportAttributeClause {
    const object = jsonObject(value);

    return {
        kind: fromJsonImportAttributeClauseKind(jsonField(object, "kind")),
        attributes: jsonArray(jsonField(object, "attributes")).map((item0) => fromJsonImportAttribute(item0)),
    };
}

/** The kind of one import attribute clause. */
export type ImportAttributeClauseKind = "with";

export const ImportAttributeClauseKind = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ImportAttributeClauseKind): void {
        encodeImportAttributeClauseKind(writer, value);
    },

    /** Decode one ImportAttributeClauseKind. */
    decode(reader: BinaryReader): ImportAttributeClauseKind {
        return decodeImportAttributeClauseKind(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ImportAttributeClauseKind): Json {
        return toJsonImportAttributeClauseKind(value);
    },

    /** Return one ImportAttributeClauseKind from one JSON value. */
    fromJson(value: Json): ImportAttributeClauseKind {
        return fromJsonImportAttributeClauseKind(value);
    },
};

/** Encode one ImportAttributeClauseKind. */
export function encodeImportAttributeClauseKind(writer: BinaryWriter, value: ImportAttributeClauseKind): void {
    switch (value) {
        case "with":
            writer.writeUnsigned(0);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one ImportAttributeClauseKind. */
export function decodeImportAttributeClauseKind(reader: BinaryReader): ImportAttributeClauseKind {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "with";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one ImportAttributeClauseKind. */
export function toJsonImportAttributeClauseKind(value: ImportAttributeClauseKind): Json {
    return value;
}

/** Return one ImportAttributeClauseKind from one JSON value. */
export function fromJsonImportAttributeClauseKind(value: Json): ImportAttributeClauseKind {
    const variant = jsonString(value);

    switch (variant) {
        case "with":
            return "with";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** One static import attribute value. */
export type ImportAttributeValue =
    /** A scalar literal value. */
    | {
          readonly kind: "scalarLiteral";
          readonly scalar_literal: ScalarLiteral;
      }
    /** An array value. */
    | {
          readonly kind: "array";
          readonly array: ReadonlyArray<ImportAttributeValue>;
      }
    /** An object value. */
    | {
          readonly kind: "object";
          readonly object: ReadonlyArray<ImportAttribute>;
      }
    /** A parser placeholder for invalid syntax. */
    | {
          readonly kind: "error";
      }
;

export const ImportAttributeValue = {
    /** A scalar literal value. */
    scalarLiteral(scalar_literal: ScalarLiteral): ImportAttributeValue {
        return { kind: "scalarLiteral", scalar_literal };
    },

    /** An array value. */
    array(array: ReadonlyArray<ImportAttributeValue>): ImportAttributeValue {
        return { kind: "array", array };
    },

    /** An object value. */
    "object"(object_: ReadonlyArray<ImportAttribute>): ImportAttributeValue {
        return { kind: "object", object: object_ };
    },

    /** A parser placeholder for invalid syntax. */
    error(): ImportAttributeValue {
        return { kind: "error" };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: ImportAttributeValue): void {
        encodeImportAttributeValue(writer, value);
    },

    /** Decode one ImportAttributeValue. */
    decode(reader: BinaryReader): ImportAttributeValue {
        return decodeImportAttributeValue(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ImportAttributeValue): Json {
        return toJsonImportAttributeValue(value);
    },

    /** Return one ImportAttributeValue from one JSON value. */
    fromJson(value: Json): ImportAttributeValue {
        return fromJsonImportAttributeValue(value);
    },
};

/** Encode one ImportAttributeValue. */
export function encodeImportAttributeValue(writer: BinaryWriter, value: ImportAttributeValue): void {
    switch (value.kind) {
        case "scalarLiteral":
            writer.writeUnsigned(0);
            encodeScalarLiteral(writer, value.scalar_literal);
            return;
        case "array":
            writer.writeUnsigned(1);
            writer.writeUnsigned(value.array.length);
            for (const item0 of value.array) {
                encodeImportAttributeValue(writer, item0);
            }
            return;
        case "object":
            writer.writeUnsigned(2);
            writer.writeUnsigned(value.object.length);
            for (const item0 of value.object) {
                encodeImportAttribute(writer, item0);
            }
            return;
        case "error":
            writer.writeUnsigned(3);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one ImportAttributeValue. */
export function decodeImportAttributeValue(reader: BinaryReader): ImportAttributeValue {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const scalar_literal = decodeScalarLiteral(reader);

            return { kind: "scalarLiteral", scalar_literal };
        }
        case 1: {
            const array = (() => { const length0 = reader.readNumber(); const items0: Array<ImportAttributeValue> = []; for (let index = 0; index < length0; index += 1) { items0.push(decodeImportAttributeValue(reader)); } return items0; })();

            return { kind: "array", array };
        }
        case 2: {
            const object_ = (() => { const length0 = reader.readNumber(); const items0: Array<ImportAttribute> = []; for (let index = 0; index < length0; index += 1) { items0.push(decodeImportAttribute(reader)); } return items0; })();

            return { kind: "object", object: object_ };
        }
        case 3: {
            return { kind: "error" };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one ImportAttributeValue. */
export function toJsonImportAttributeValue(value: ImportAttributeValue): Json {
    switch (value.kind) {
        case "scalarLiteral":
            return {
                kind: "scalarLiteral",
                scalar_literal: toJsonScalarLiteral(value.scalar_literal),
            };
        case "array":
            return {
                kind: "array",
                array: value.array.map((item0) => toJsonImportAttributeValue(item0)),
            };
        case "object":
            return {
                kind: "object",
                object: value.object.map((item0) => toJsonImportAttribute(item0)),
            };
        case "error":
            return {
                kind: "error",
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one ImportAttributeValue from one JSON value. */
export function fromJsonImportAttributeValue(value: Json): ImportAttributeValue {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "scalarLiteral":
            return {
                kind,
                scalar_literal: fromJsonScalarLiteral(jsonField(object, "scalar_literal")),
            };
        case "array":
            return {
                kind,
                array: jsonArray(jsonField(object, "array")).map((item0) => fromJsonImportAttributeValue(item0)),
            };
        case "object":
            return {
                kind,
                object: jsonArray(jsonField(object, "object")).map((item0) => fromJsonImportAttribute(item0)),
            };
        case "error":
            return {
                kind,
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}
