/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, jsonArray, jsonField, jsonObject, jsonOptional, jsonString } from "../../../protocol/serde.js";
import type { LocalNodeId } from "./node.js";
import { decodeLocalNodeId, encodeLocalNodeId, fromJsonLocalNodeId, toJsonLocalNodeId } from "./node.js";

/** A Block is a block of statements. */
export type Block = {
    /** The block context. */
    readonly context: BlockContext;
    /** The structural form of the block. */
    readonly form: BlockForm;
    /** The leading expressions whose values are discarded. */
    readonly leadingExpressions: ReadonlyArray<LocalNodeId>;
    /** The optional tail expression whose value becomes the block value. */
    readonly tailExpression?: LocalNodeId;
};

export const Block = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: Block): void {
        encodeBlock(writer, value);
    },

    /** Decode one Block. */
    decode(reader: BinaryReader): Block {
        return decodeBlock(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Block): Json {
        return toJsonBlock(value);
    },

    /** Return one Block from one JSON value. */
    fromJson(value: Json): Block {
        return fromJsonBlock(value);
    },
};

/** Encode one Block. */
export function encodeBlock(writer: BinaryWriter, value: Block): void {
    encodeBlockContext(writer, value.context);
    encodeBlockForm(writer, value.form);
    writer.writeUnsigned(value.leadingExpressions.length);
    for (const item2 of value.leadingExpressions) {
        encodeLocalNodeId(writer, item2);
    }
    writer.writeOption(value.tailExpression, (value3) => {
        encodeLocalNodeId(writer, value3);
    });
}

/** Decode one Block. */
export function decodeBlock(reader: BinaryReader): Block {
    const context = decodeBlockContext(reader);
    const form = decodeBlockForm(reader);
    const leadingExpressions = (() => { const length2 = reader.readNumber(); const items2: Array<LocalNodeId> = []; for (let index = 0; index < length2; index += 1) { items2.push(decodeLocalNodeId(reader)); } return items2; })();
    const tailExpression = reader.readOption(() => decodeLocalNodeId(reader));

    return {
        context,
        form,
        leadingExpressions,
        ...(tailExpression === undefined ? {} : { tailExpression }),
    };
}

/** Return one JSON value for one Block. */
export function toJsonBlock(value: Block): Json {
    return {
        context: toJsonBlockContext(value.context),
        form: toJsonBlockForm(value.form),
        leadingExpressions: value.leadingExpressions.map((item0) => toJsonLocalNodeId(item0)),
        ...(value.tailExpression === undefined ? {} : { tailExpression: toJsonLocalNodeId(value.tailExpression) }),
    };
}

/** Return one Block from one JSON value. */
export function fromJsonBlock(value: Json): Block {
    const object = jsonObject(value);

    return {
        context: fromJsonBlockContext(jsonField(object, "context")),
        form: fromJsonBlockForm(jsonField(object, "form")),
        leadingExpressions: jsonArray(jsonField(object, "leadingExpressions")).map((item0) => fromJsonLocalNodeId(item0)),
        tailExpression: jsonOptional(object, "tailExpression", (value) => fromJsonLocalNodeId(value)),
    };
}

/** How a block is interpreted. */
export type BlockContext = "expression" | "statement";

export const BlockContext = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: BlockContext): void {
        encodeBlockContext(writer, value);
    },

    /** Decode one BlockContext. */
    decode(reader: BinaryReader): BlockContext {
        return decodeBlockContext(reader);
    },

    /** Return this value as JSON. */
    toJson(value: BlockContext): Json {
        return toJsonBlockContext(value);
    },

    /** Return one BlockContext from one JSON value. */
    fromJson(value: Json): BlockContext {
        return fromJsonBlockContext(value);
    },
};

/** Encode one BlockContext. */
export function encodeBlockContext(writer: BinaryWriter, value: BlockContext): void {
    switch (value) {
        case "expression":
            writer.writeUnsigned(0);
            return;
        case "statement":
            writer.writeUnsigned(1);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one BlockContext. */
export function decodeBlockContext(reader: BinaryReader): BlockContext {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "expression";
        case 1:
            return "statement";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one BlockContext. */
export function toJsonBlockContext(value: BlockContext): Json {
    return value;
}

/** Return one BlockContext from one JSON value. */
export function fromJsonBlockContext(value: Json): BlockContext {
    const variant = jsonString(value);

    switch (variant) {
        case "expression":
            return "expression";
        case "statement":
            return "statement";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** The structural form of a block. */
export type BlockForm = "explicit" | "do" | "implicit";

export const BlockForm = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: BlockForm): void {
        encodeBlockForm(writer, value);
    },

    /** Decode one BlockForm. */
    decode(reader: BinaryReader): BlockForm {
        return decodeBlockForm(reader);
    },

    /** Return this value as JSON. */
    toJson(value: BlockForm): Json {
        return toJsonBlockForm(value);
    },

    /** Return one BlockForm from one JSON value. */
    fromJson(value: Json): BlockForm {
        return fromJsonBlockForm(value);
    },
};

/** Encode one BlockForm. */
export function encodeBlockForm(writer: BinaryWriter, value: BlockForm): void {
    switch (value) {
        case "explicit":
            writer.writeUnsigned(0);
            return;
        case "do":
            writer.writeUnsigned(1);
            return;
        case "implicit":
            writer.writeUnsigned(2);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one BlockForm. */
export function decodeBlockForm(reader: BinaryReader): BlockForm {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "explicit";
        case 1:
            return "do";
        case 2:
            return "implicit";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one BlockForm. */
export function toJsonBlockForm(value: BlockForm): Json {
    return value;
}

/** Return one BlockForm from one JSON value. */
export function fromJsonBlockForm(value: Json): BlockForm {
    const variant = jsonString(value);

    switch (variant) {
        case "explicit":
            return "explicit";
        case "do":
            return "do";
        case "implicit":
            return "implicit";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}
