/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, jsonArray, jsonBool, jsonField, jsonObject, jsonOptional, jsonString } from "../../../protocol/serde.js";
import type { StringId } from "../../core/string.js";
import type { Name } from "./key.js";
import type { LocalNodeId } from "./node.js";
import type { Mutability } from "./node.js";
import type { RangeEnd } from "./operator.js";
import { decodeStringId, encodeStringId, fromJsonStringId, toJsonStringId } from "../../core/string.js";
import { decodeName, encodeName, fromJsonName, toJsonName } from "./key.js";
import { decodeLocalNodeId, encodeLocalNodeId, fromJsonLocalNodeId, toJsonLocalNodeId } from "./node.js";
import { decodeMutability, encodeMutability, fromJsonMutability, toJsonMutability } from "./node.js";
import { decodeRangeEnd, encodeRangeEnd, fromJsonRangeEnd, toJsonRangeEnd } from "./operator.js";

/** A Pattern is a pattern to match something and unwrap it. */
export type Pattern =
    /** Wildcard scalar pattern (`_`). */
    | {
          readonly kind: "wildcard";
      }
    /** Must pattern (like `x!`). */
    | {
          readonly kind: "must";
          readonly must: LocalNodeId;
      }
    /** Defaulted pattern like `x = 1`. */
    | {
          readonly kind: "default";
          readonly pattern: LocalNodeId;
          readonly value: LocalNodeId;
      }
    /** Borrow pattern (like `&x`). */
    | {
          readonly kind: "borrowOf";
          readonly mutability?: Mutability;
          readonly right: LocalNodeId;
      }
    /** Move pattern (like `^x`). */
    | {
          readonly kind: "moveOf";
          readonly mutability?: Mutability;
          readonly right: LocalNodeId;
      }
    /** Dereference pattern (like `*x`). */
    | {
          readonly kind: "dereferenceOf";
          readonly right: LocalNodeId;
      }
    /** Binding pattern (basically a PatternField, like `x`, `x: 4`, or `x: int32`). */
    | {
          readonly kind: "binding";
          readonly name: StringId;
          readonly pattern?: LocalNodeId;
      }
    /** Literal value or value-space path pattern. */
    | {
          readonly kind: "expression";
          readonly value: LocalNodeId;
      }
    /** Ordered scalar interval pattern like `0..10` or `..=255`. */
    | {
          readonly kind: "range";
          readonly start?: LocalNodeId;
          readonly end?: LocalNodeId;
          readonly endKind: RangeEnd;
      }
    /** Tuple pattern (like `(x, 0)`). */
    | {
          readonly kind: "tuple";
          readonly fields: ReadonlyArray<LocalNodeId>;
      }
    /** Nominal tuple pattern (like `T(1)`). */
    | {
          readonly kind: "nominalTuple";
          readonly ty: LocalNodeId;
          readonly fields: ReadonlyArray<LocalNodeId>;
      }
    /** Sequence pattern like `[1, 2, x]` or `[1, y, ..]`. */
    | {
          readonly kind: "sequence";
          readonly fields: ReadonlyArray<LocalNodeId>;
      }
    /** Object pattern (like `{ x, y }`). */
    | {
          readonly kind: "object";
          readonly fields: ReadonlyArray<LocalNodeId>;
      }
    /** Nominal object pattern (like `T { x, y }`). */
    | {
          readonly kind: "nominalObject";
          readonly ty: LocalNodeId;
          readonly fields: ReadonlyArray<LocalNodeId>;
      }
    /** Union pattern (like `1 | 2 | 3`). */
    | {
          readonly kind: "union";
          readonly patterns: ReadonlyArray<LocalNodeId>;
      }
;

export const Pattern = {
    /** Wildcard scalar pattern (`_`). */
    wildcard(): Pattern {
        return { kind: "wildcard" };
    },

    /** Must pattern (like `x!`). */
    must(must: LocalNodeId): Pattern {
        return { kind: "must", must };
    },

    /** Defaulted pattern like `x = 1`. */
    "default"(pattern: LocalNodeId, value: LocalNodeId): Pattern {
        return { kind: "default", pattern, value };
    },

    /** Borrow pattern (like `&x`). */
    borrowOf(mutability: Mutability | undefined, right: LocalNodeId): Pattern {
        return { kind: "borrowOf", mutability, right };
    },

    /** Move pattern (like `^x`). */
    moveOf(mutability: Mutability | undefined, right: LocalNodeId): Pattern {
        return { kind: "moveOf", mutability, right };
    },

    /** Dereference pattern (like `*x`). */
    dereferenceOf(right: LocalNodeId): Pattern {
        return { kind: "dereferenceOf", right };
    },

    /** Binding pattern (basically a PatternField, like `x`, `x: 4`, or `x: int32`). */
    binding(name: StringId, pattern: LocalNodeId | undefined): Pattern {
        return { kind: "binding", name, pattern };
    },

    /** Literal value or value-space path pattern. */
    expression(value: LocalNodeId): Pattern {
        return { kind: "expression", value };
    },

    /** Ordered scalar interval pattern like `0..10` or `..=255`. */
    range(start: LocalNodeId | undefined, end: LocalNodeId | undefined, endKind: RangeEnd): Pattern {
        return { kind: "range", start, end, endKind };
    },

    /** Tuple pattern (like `(x, 0)`). */
    tuple(fields: ReadonlyArray<LocalNodeId>): Pattern {
        return { kind: "tuple", fields };
    },

    /** Nominal tuple pattern (like `T(1)`). */
    nominalTuple(ty: LocalNodeId, fields: ReadonlyArray<LocalNodeId>): Pattern {
        return { kind: "nominalTuple", ty, fields };
    },

    /** Sequence pattern like `[1, 2, x]` or `[1, y, ..]`. */
    sequence(fields: ReadonlyArray<LocalNodeId>): Pattern {
        return { kind: "sequence", fields };
    },

    /** Object pattern (like `{ x, y }`). */
    "object"(fields: ReadonlyArray<LocalNodeId>): Pattern {
        return { kind: "object", fields };
    },

    /** Nominal object pattern (like `T { x, y }`). */
    nominalObject(ty: LocalNodeId, fields: ReadonlyArray<LocalNodeId>): Pattern {
        return { kind: "nominalObject", ty, fields };
    },

    /** Union pattern (like `1 | 2 | 3`). */
    union(patterns: ReadonlyArray<LocalNodeId>): Pattern {
        return { kind: "union", patterns };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: Pattern): void {
        encodePattern(writer, value);
    },

    /** Decode one Pattern. */
    decode(reader: BinaryReader): Pattern {
        return decodePattern(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Pattern): Json {
        return toJsonPattern(value);
    },

    /** Return one Pattern from one JSON value. */
    fromJson(value: Json): Pattern {
        return fromJsonPattern(value);
    },
};

/** Encode one Pattern. */
export function encodePattern(writer: BinaryWriter, value: Pattern): void {
    switch (value.kind) {
        case "wildcard":
            writer.writeUnsigned(0);
            return;
        case "must":
            writer.writeUnsigned(1);
            encodeLocalNodeId(writer, value.must);
            return;
        case "default":
            writer.writeUnsigned(2);
            encodeLocalNodeId(writer, value.pattern);
            encodeLocalNodeId(writer, value.value);
            return;
        case "borrowOf":
            writer.writeUnsigned(3);
            writer.writeOption(value.mutability, (value0) => {
                encodeMutability(writer, value0);
            });
            encodeLocalNodeId(writer, value.right);
            return;
        case "moveOf":
            writer.writeUnsigned(4);
            writer.writeOption(value.mutability, (value0) => {
                encodeMutability(writer, value0);
            });
            encodeLocalNodeId(writer, value.right);
            return;
        case "dereferenceOf":
            writer.writeUnsigned(5);
            encodeLocalNodeId(writer, value.right);
            return;
        case "binding":
            writer.writeUnsigned(6);
            encodeStringId(writer, value.name);
            writer.writeOption(value.pattern, (value1) => {
                encodeLocalNodeId(writer, value1);
            });
            return;
        case "expression":
            writer.writeUnsigned(7);
            encodeLocalNodeId(writer, value.value);
            return;
        case "range":
            writer.writeUnsigned(8);
            writer.writeOption(value.start, (value0) => {
                encodeLocalNodeId(writer, value0);
            });
            writer.writeOption(value.end, (value1) => {
                encodeLocalNodeId(writer, value1);
            });
            encodeRangeEnd(writer, value.endKind);
            return;
        case "tuple":
            writer.writeUnsigned(9);
            writer.writeUnsigned(value.fields.length);
            for (const item0 of value.fields) {
                encodeLocalNodeId(writer, item0);
            }
            return;
        case "nominalTuple":
            writer.writeUnsigned(10);
            encodeLocalNodeId(writer, value.ty);
            writer.writeUnsigned(value.fields.length);
            for (const item1 of value.fields) {
                encodeLocalNodeId(writer, item1);
            }
            return;
        case "sequence":
            writer.writeUnsigned(11);
            writer.writeUnsigned(value.fields.length);
            for (const item0 of value.fields) {
                encodeLocalNodeId(writer, item0);
            }
            return;
        case "object":
            writer.writeUnsigned(12);
            writer.writeUnsigned(value.fields.length);
            for (const item0 of value.fields) {
                encodeLocalNodeId(writer, item0);
            }
            return;
        case "nominalObject":
            writer.writeUnsigned(13);
            encodeLocalNodeId(writer, value.ty);
            writer.writeUnsigned(value.fields.length);
            for (const item1 of value.fields) {
                encodeLocalNodeId(writer, item1);
            }
            return;
        case "union":
            writer.writeUnsigned(14);
            writer.writeUnsigned(value.patterns.length);
            for (const item0 of value.patterns) {
                encodeLocalNodeId(writer, item0);
            }
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one Pattern. */
export function decodePattern(reader: BinaryReader): Pattern {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            return { kind: "wildcard" };
        }
        case 1: {
            const must = decodeLocalNodeId(reader);

            return { kind: "must", must };
        }
        case 2: {
            const pattern = decodeLocalNodeId(reader);
            const value = decodeLocalNodeId(reader);

            return {
                kind: "default",
                pattern,
                value,
            };
        }
        case 3: {
            const mutability = reader.readOption(() => decodeMutability(reader));
            const right = decodeLocalNodeId(reader);

            return {
                kind: "borrowOf",
                ...(mutability === undefined ? {} : { mutability }),
                right,
            };
        }
        case 4: {
            const mutability = reader.readOption(() => decodeMutability(reader));
            const right = decodeLocalNodeId(reader);

            return {
                kind: "moveOf",
                ...(mutability === undefined ? {} : { mutability }),
                right,
            };
        }
        case 5: {
            const right = decodeLocalNodeId(reader);

            return {
                kind: "dereferenceOf",
                right,
            };
        }
        case 6: {
            const name = decodeStringId(reader);
            const pattern = reader.readOption(() => decodeLocalNodeId(reader));

            return {
                kind: "binding",
                name,
                ...(pattern === undefined ? {} : { pattern }),
            };
        }
        case 7: {
            const value = decodeLocalNodeId(reader);

            return {
                kind: "expression",
                value,
            };
        }
        case 8: {
            const start = reader.readOption(() => decodeLocalNodeId(reader));
            const end = reader.readOption(() => decodeLocalNodeId(reader));
            const endKind = decodeRangeEnd(reader);

            return {
                kind: "range",
                ...(start === undefined ? {} : { start }),
                ...(end === undefined ? {} : { end }),
                endKind,
            };
        }
        case 9: {
            const fields = (() => { const length0 = reader.readNumber(); const items0: Array<LocalNodeId> = []; for (let index = 0; index < length0; index += 1) { items0.push(decodeLocalNodeId(reader)); } return items0; })();

            return {
                kind: "tuple",
                fields,
            };
        }
        case 10: {
            const ty = decodeLocalNodeId(reader);
            const fields = (() => { const length1 = reader.readNumber(); const items1: Array<LocalNodeId> = []; for (let index = 0; index < length1; index += 1) { items1.push(decodeLocalNodeId(reader)); } return items1; })();

            return {
                kind: "nominalTuple",
                ty,
                fields,
            };
        }
        case 11: {
            const fields = (() => { const length0 = reader.readNumber(); const items0: Array<LocalNodeId> = []; for (let index = 0; index < length0; index += 1) { items0.push(decodeLocalNodeId(reader)); } return items0; })();

            return {
                kind: "sequence",
                fields,
            };
        }
        case 12: {
            const fields = (() => { const length0 = reader.readNumber(); const items0: Array<LocalNodeId> = []; for (let index = 0; index < length0; index += 1) { items0.push(decodeLocalNodeId(reader)); } return items0; })();

            return {
                kind: "object",
                fields,
            };
        }
        case 13: {
            const ty = decodeLocalNodeId(reader);
            const fields = (() => { const length1 = reader.readNumber(); const items1: Array<LocalNodeId> = []; for (let index = 0; index < length1; index += 1) { items1.push(decodeLocalNodeId(reader)); } return items1; })();

            return {
                kind: "nominalObject",
                ty,
                fields,
            };
        }
        case 14: {
            const patterns = (() => { const length0 = reader.readNumber(); const items0: Array<LocalNodeId> = []; for (let index = 0; index < length0; index += 1) { items0.push(decodeLocalNodeId(reader)); } return items0; })();

            return {
                kind: "union",
                patterns,
            };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one Pattern. */
export function toJsonPattern(value: Pattern): Json {
    switch (value.kind) {
        case "wildcard":
            return {
                kind: "wildcard",
            };
        case "must":
            return {
                kind: "must",
                must: toJsonLocalNodeId(value.must),
            };
        case "default":
            return {
                kind: "default",
                pattern: toJsonLocalNodeId(value.pattern),
                value: toJsonLocalNodeId(value.value),
            };
        case "borrowOf":
            return {
                kind: "borrowOf",
                ...(value.mutability === undefined ? {} : { mutability: toJsonMutability(value.mutability) }),
                right: toJsonLocalNodeId(value.right),
            };
        case "moveOf":
            return {
                kind: "moveOf",
                ...(value.mutability === undefined ? {} : { mutability: toJsonMutability(value.mutability) }),
                right: toJsonLocalNodeId(value.right),
            };
        case "dereferenceOf":
            return {
                kind: "dereferenceOf",
                right: toJsonLocalNodeId(value.right),
            };
        case "binding":
            return {
                kind: "binding",
                name: toJsonStringId(value.name),
                ...(value.pattern === undefined ? {} : { pattern: toJsonLocalNodeId(value.pattern) }),
            };
        case "expression":
            return {
                kind: "expression",
                value: toJsonLocalNodeId(value.value),
            };
        case "range":
            return {
                kind: "range",
                ...(value.start === undefined ? {} : { start: toJsonLocalNodeId(value.start) }),
                ...(value.end === undefined ? {} : { end: toJsonLocalNodeId(value.end) }),
                endKind: toJsonRangeEnd(value.endKind),
            };
        case "tuple":
            return {
                kind: "tuple",
                fields: value.fields.map((item0) => toJsonLocalNodeId(item0)),
            };
        case "nominalTuple":
            return {
                kind: "nominalTuple",
                ty: toJsonLocalNodeId(value.ty),
                fields: value.fields.map((item0) => toJsonLocalNodeId(item0)),
            };
        case "sequence":
            return {
                kind: "sequence",
                fields: value.fields.map((item0) => toJsonLocalNodeId(item0)),
            };
        case "object":
            return {
                kind: "object",
                fields: value.fields.map((item0) => toJsonLocalNodeId(item0)),
            };
        case "nominalObject":
            return {
                kind: "nominalObject",
                ty: toJsonLocalNodeId(value.ty),
                fields: value.fields.map((item0) => toJsonLocalNodeId(item0)),
            };
        case "union":
            return {
                kind: "union",
                patterns: value.patterns.map((item0) => toJsonLocalNodeId(item0)),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one Pattern from one JSON value. */
export function fromJsonPattern(value: Json): Pattern {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "wildcard":
            return {
                kind,
            };
        case "must":
            return {
                kind,
                must: fromJsonLocalNodeId(jsonField(object, "must")),
            };
        case "default":
            return {
                kind,
                pattern: fromJsonLocalNodeId(jsonField(object, "pattern")),
                value: fromJsonLocalNodeId(jsonField(object, "value")),
            };
        case "borrowOf":
            return {
                kind,
                mutability: jsonOptional(object, "mutability", (value) => fromJsonMutability(value)),
                right: fromJsonLocalNodeId(jsonField(object, "right")),
            };
        case "moveOf":
            return {
                kind,
                mutability: jsonOptional(object, "mutability", (value) => fromJsonMutability(value)),
                right: fromJsonLocalNodeId(jsonField(object, "right")),
            };
        case "dereferenceOf":
            return {
                kind,
                right: fromJsonLocalNodeId(jsonField(object, "right")),
            };
        case "binding":
            return {
                kind,
                name: fromJsonStringId(jsonField(object, "name")),
                pattern: jsonOptional(object, "pattern", (value) => fromJsonLocalNodeId(value)),
            };
        case "expression":
            return {
                kind,
                value: fromJsonLocalNodeId(jsonField(object, "value")),
            };
        case "range":
            return {
                kind,
                start: jsonOptional(object, "start", (value) => fromJsonLocalNodeId(value)),
                end: jsonOptional(object, "end", (value) => fromJsonLocalNodeId(value)),
                endKind: fromJsonRangeEnd(jsonField(object, "endKind")),
            };
        case "tuple":
            return {
                kind,
                fields: jsonArray(jsonField(object, "fields")).map((item0) => fromJsonLocalNodeId(item0)),
            };
        case "nominalTuple":
            return {
                kind,
                ty: fromJsonLocalNodeId(jsonField(object, "ty")),
                fields: jsonArray(jsonField(object, "fields")).map((item0) => fromJsonLocalNodeId(item0)),
            };
        case "sequence":
            return {
                kind,
                fields: jsonArray(jsonField(object, "fields")).map((item0) => fromJsonLocalNodeId(item0)),
            };
        case "object":
            return {
                kind,
                fields: jsonArray(jsonField(object, "fields")).map((item0) => fromJsonLocalNodeId(item0)),
            };
        case "nominalObject":
            return {
                kind,
                ty: fromJsonLocalNodeId(jsonField(object, "ty")),
                fields: jsonArray(jsonField(object, "fields")).map((item0) => fromJsonLocalNodeId(item0)),
            };
        case "union":
            return {
                kind,
                patterns: jsonArray(jsonField(object, "patterns")).map((item0) => fromJsonLocalNodeId(item0)),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** A PatternField is a field of a variant pattern. */
export type PatternField =
    /** Named field, maybe shorthand and maybe with a nested pattern. */
    | {
          readonly kind: "named";
          readonly name: Name;
          readonly pattern?: LocalNodeId;
          readonly isShorthand: boolean;
      }
    /** Computed field (like `[key]: value`). */
    | {
          readonly kind: "computed";
          readonly key: LocalNodeId;
          readonly pattern: LocalNodeId;
      }
    /** Positional field with a pattern (like `4` or `x = 1`). */
    | {
          readonly kind: "positional";
          readonly pattern: LocalNodeId;
      }
    /** Spread field (like `...x` or `...[a, b]`). */
    | {
          readonly kind: "spread";
          readonly pattern?: LocalNodeId;
      }
    /** Elision in a sequence pattern like `[,a]` or `[,,b]`. */
    | {
          readonly kind: "elision";
      }
;

export const PatternField = {
    /** Named field, maybe shorthand and maybe with a nested pattern. */
    named(name: Name, pattern: LocalNodeId | undefined, isShorthand: boolean): PatternField {
        return { kind: "named", name, pattern, isShorthand };
    },

    /** Computed field (like `[key]: value`). */
    computed(key: LocalNodeId, pattern: LocalNodeId): PatternField {
        return { kind: "computed", key, pattern };
    },

    /** Positional field with a pattern (like `4` or `x = 1`). */
    positional(pattern: LocalNodeId): PatternField {
        return { kind: "positional", pattern };
    },

    /** Spread field (like `...x` or `...[a, b]`). */
    spread(pattern: LocalNodeId | undefined): PatternField {
        return { kind: "spread", pattern };
    },

    /** Elision in a sequence pattern like `[,a]` or `[,,b]`. */
    elision(): PatternField {
        return { kind: "elision" };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: PatternField): void {
        encodePatternField(writer, value);
    },

    /** Decode one PatternField. */
    decode(reader: BinaryReader): PatternField {
        return decodePatternField(reader);
    },

    /** Return this value as JSON. */
    toJson(value: PatternField): Json {
        return toJsonPatternField(value);
    },

    /** Return one PatternField from one JSON value. */
    fromJson(value: Json): PatternField {
        return fromJsonPatternField(value);
    },
};

/** Encode one PatternField. */
export function encodePatternField(writer: BinaryWriter, value: PatternField): void {
    switch (value.kind) {
        case "named":
            writer.writeUnsigned(0);
            encodeName(writer, value.name);
            writer.writeOption(value.pattern, (value1) => {
                encodeLocalNodeId(writer, value1);
            });
            writer.writeBool(value.isShorthand);
            return;
        case "computed":
            writer.writeUnsigned(1);
            encodeLocalNodeId(writer, value.key);
            encodeLocalNodeId(writer, value.pattern);
            return;
        case "positional":
            writer.writeUnsigned(2);
            encodeLocalNodeId(writer, value.pattern);
            return;
        case "spread":
            writer.writeUnsigned(3);
            writer.writeOption(value.pattern, (value0) => {
                encodeLocalNodeId(writer, value0);
            });
            return;
        case "elision":
            writer.writeUnsigned(4);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one PatternField. */
export function decodePatternField(reader: BinaryReader): PatternField {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const name = decodeName(reader);
            const pattern = reader.readOption(() => decodeLocalNodeId(reader));
            const isShorthand = reader.readBool();

            return {
                kind: "named",
                name,
                ...(pattern === undefined ? {} : { pattern }),
                isShorthand,
            };
        }
        case 1: {
            const key = decodeLocalNodeId(reader);
            const pattern = decodeLocalNodeId(reader);

            return {
                kind: "computed",
                key,
                pattern,
            };
        }
        case 2: {
            const pattern = decodeLocalNodeId(reader);

            return {
                kind: "positional",
                pattern,
            };
        }
        case 3: {
            const pattern = reader.readOption(() => decodeLocalNodeId(reader));

            return {
                kind: "spread",
                ...(pattern === undefined ? {} : { pattern }),
            };
        }
        case 4: {
            return { kind: "elision" };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one PatternField. */
export function toJsonPatternField(value: PatternField): Json {
    switch (value.kind) {
        case "named":
            return {
                kind: "named",
                name: toJsonName(value.name),
                ...(value.pattern === undefined ? {} : { pattern: toJsonLocalNodeId(value.pattern) }),
                isShorthand: value.isShorthand,
            };
        case "computed":
            return {
                kind: "computed",
                key: toJsonLocalNodeId(value.key),
                pattern: toJsonLocalNodeId(value.pattern),
            };
        case "positional":
            return {
                kind: "positional",
                pattern: toJsonLocalNodeId(value.pattern),
            };
        case "spread":
            return {
                kind: "spread",
                ...(value.pattern === undefined ? {} : { pattern: toJsonLocalNodeId(value.pattern) }),
            };
        case "elision":
            return {
                kind: "elision",
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one PatternField from one JSON value. */
export function fromJsonPatternField(value: Json): PatternField {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "named":
            return {
                kind,
                name: fromJsonName(jsonField(object, "name")),
                pattern: jsonOptional(object, "pattern", (value) => fromJsonLocalNodeId(value)),
                isShorthand: jsonBool(jsonField(object, "isShorthand")),
            };
        case "computed":
            return {
                kind,
                key: fromJsonLocalNodeId(jsonField(object, "key")),
                pattern: fromJsonLocalNodeId(jsonField(object, "pattern")),
            };
        case "positional":
            return {
                kind,
                pattern: fromJsonLocalNodeId(jsonField(object, "pattern")),
            };
        case "spread":
            return {
                kind,
                pattern: jsonOptional(object, "pattern", (value) => fromJsonLocalNodeId(value)),
            };
        case "elision":
            return {
                kind,
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** An AssignPattern is one assignment left hand side. */
export type AssignPattern =
    /** Writable place target like `x`, `obj.x`, or `obj[key]`. */
    | {
          readonly kind: "place";
          readonly expression: LocalNodeId;
      }
    /** Defaulted destructuring target like `x = 1`. */
    | {
          readonly kind: "default";
          readonly pattern: LocalNodeId;
          readonly value: LocalNodeId;
      }
    /** Sequence destructuring target like `[a, , ...rest]`. */
    | {
          readonly kind: "sequence";
          readonly fields: ReadonlyArray<LocalNodeId>;
      }
    /** Tuple destructuring target like `(a, b)` or `(a,)`. */
    | {
          readonly kind: "tuple";
          readonly fields: ReadonlyArray<LocalNodeId>;
      }
    /** Object destructuring target like `{ x, y: z }`. */
    | {
          readonly kind: "object";
          readonly fields: ReadonlyArray<LocalNodeId>;
      }
;

export const AssignPattern = {
    /** Writable place target like `x`, `obj.x`, or `obj[key]`. */
    place(expression: LocalNodeId): AssignPattern {
        return { kind: "place", expression };
    },

    /** Defaulted destructuring target like `x = 1`. */
    "default"(pattern: LocalNodeId, value: LocalNodeId): AssignPattern {
        return { kind: "default", pattern, value };
    },

    /** Sequence destructuring target like `[a, , ...rest]`. */
    sequence(fields: ReadonlyArray<LocalNodeId>): AssignPattern {
        return { kind: "sequence", fields };
    },

    /** Tuple destructuring target like `(a, b)` or `(a,)`. */
    tuple(fields: ReadonlyArray<LocalNodeId>): AssignPattern {
        return { kind: "tuple", fields };
    },

    /** Object destructuring target like `{ x, y: z }`. */
    "object"(fields: ReadonlyArray<LocalNodeId>): AssignPattern {
        return { kind: "object", fields };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: AssignPattern): void {
        encodeAssignPattern(writer, value);
    },

    /** Decode one AssignPattern. */
    decode(reader: BinaryReader): AssignPattern {
        return decodeAssignPattern(reader);
    },

    /** Return this value as JSON. */
    toJson(value: AssignPattern): Json {
        return toJsonAssignPattern(value);
    },

    /** Return one AssignPattern from one JSON value. */
    fromJson(value: Json): AssignPattern {
        return fromJsonAssignPattern(value);
    },
};

/** Encode one AssignPattern. */
export function encodeAssignPattern(writer: BinaryWriter, value: AssignPattern): void {
    switch (value.kind) {
        case "place":
            writer.writeUnsigned(0);
            encodeLocalNodeId(writer, value.expression);
            return;
        case "default":
            writer.writeUnsigned(1);
            encodeLocalNodeId(writer, value.pattern);
            encodeLocalNodeId(writer, value.value);
            return;
        case "sequence":
            writer.writeUnsigned(2);
            writer.writeUnsigned(value.fields.length);
            for (const item0 of value.fields) {
                encodeLocalNodeId(writer, item0);
            }
            return;
        case "tuple":
            writer.writeUnsigned(3);
            writer.writeUnsigned(value.fields.length);
            for (const item0 of value.fields) {
                encodeLocalNodeId(writer, item0);
            }
            return;
        case "object":
            writer.writeUnsigned(4);
            writer.writeUnsigned(value.fields.length);
            for (const item0 of value.fields) {
                encodeLocalNodeId(writer, item0);
            }
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one AssignPattern. */
export function decodeAssignPattern(reader: BinaryReader): AssignPattern {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const expression = decodeLocalNodeId(reader);

            return {
                kind: "place",
                expression,
            };
        }
        case 1: {
            const pattern = decodeLocalNodeId(reader);
            const value = decodeLocalNodeId(reader);

            return {
                kind: "default",
                pattern,
                value,
            };
        }
        case 2: {
            const fields = (() => { const length0 = reader.readNumber(); const items0: Array<LocalNodeId> = []; for (let index = 0; index < length0; index += 1) { items0.push(decodeLocalNodeId(reader)); } return items0; })();

            return {
                kind: "sequence",
                fields,
            };
        }
        case 3: {
            const fields = (() => { const length0 = reader.readNumber(); const items0: Array<LocalNodeId> = []; for (let index = 0; index < length0; index += 1) { items0.push(decodeLocalNodeId(reader)); } return items0; })();

            return {
                kind: "tuple",
                fields,
            };
        }
        case 4: {
            const fields = (() => { const length0 = reader.readNumber(); const items0: Array<LocalNodeId> = []; for (let index = 0; index < length0; index += 1) { items0.push(decodeLocalNodeId(reader)); } return items0; })();

            return {
                kind: "object",
                fields,
            };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one AssignPattern. */
export function toJsonAssignPattern(value: AssignPattern): Json {
    switch (value.kind) {
        case "place":
            return {
                kind: "place",
                expression: toJsonLocalNodeId(value.expression),
            };
        case "default":
            return {
                kind: "default",
                pattern: toJsonLocalNodeId(value.pattern),
                value: toJsonLocalNodeId(value.value),
            };
        case "sequence":
            return {
                kind: "sequence",
                fields: value.fields.map((item0) => toJsonLocalNodeId(item0)),
            };
        case "tuple":
            return {
                kind: "tuple",
                fields: value.fields.map((item0) => toJsonLocalNodeId(item0)),
            };
        case "object":
            return {
                kind: "object",
                fields: value.fields.map((item0) => toJsonLocalNodeId(item0)),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one AssignPattern from one JSON value. */
export function fromJsonAssignPattern(value: Json): AssignPattern {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "place":
            return {
                kind,
                expression: fromJsonLocalNodeId(jsonField(object, "expression")),
            };
        case "default":
            return {
                kind,
                pattern: fromJsonLocalNodeId(jsonField(object, "pattern")),
                value: fromJsonLocalNodeId(jsonField(object, "value")),
            };
        case "sequence":
            return {
                kind,
                fields: jsonArray(jsonField(object, "fields")).map((item0) => fromJsonLocalNodeId(item0)),
            };
        case "tuple":
            return {
                kind,
                fields: jsonArray(jsonField(object, "fields")).map((item0) => fromJsonLocalNodeId(item0)),
            };
        case "object":
            return {
                kind,
                fields: jsonArray(jsonField(object, "fields")).map((item0) => fromJsonLocalNodeId(item0)),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** An AssignPatternField is one field in a destructuring assignment target. */
export type AssignPatternField =
    /** Named field like `{ x }` or `{ x: y }`. */
    | {
          readonly kind: "named";
          readonly name: Name;
          readonly pattern: LocalNodeId;
          readonly isShorthand: boolean;
      }
    /** Computed field like `{ [key]: value }`. */
    | {
          readonly kind: "computed";
          readonly key: LocalNodeId;
          readonly pattern: LocalNodeId;
      }
    /** Positional field like `[value]`. */
    | {
          readonly kind: "positional";
          readonly pattern: LocalNodeId;
      }
    /** Spread field like `{ ...rest }` or `[...rest]`. */
    | {
          readonly kind: "spread";
          readonly pattern?: LocalNodeId;
      }
    /** Elision like `[, value]`. */
    | {
          readonly kind: "elision";
      }
;

export const AssignPatternField = {
    /** Named field like `{ x }` or `{ x: y }`. */
    named(name: Name, pattern: LocalNodeId, isShorthand: boolean): AssignPatternField {
        return { kind: "named", name, pattern, isShorthand };
    },

    /** Computed field like `{ [key]: value }`. */
    computed(key: LocalNodeId, pattern: LocalNodeId): AssignPatternField {
        return { kind: "computed", key, pattern };
    },

    /** Positional field like `[value]`. */
    positional(pattern: LocalNodeId): AssignPatternField {
        return { kind: "positional", pattern };
    },

    /** Spread field like `{ ...rest }` or `[...rest]`. */
    spread(pattern: LocalNodeId | undefined): AssignPatternField {
        return { kind: "spread", pattern };
    },

    /** Elision like `[, value]`. */
    elision(): AssignPatternField {
        return { kind: "elision" };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: AssignPatternField): void {
        encodeAssignPatternField(writer, value);
    },

    /** Decode one AssignPatternField. */
    decode(reader: BinaryReader): AssignPatternField {
        return decodeAssignPatternField(reader);
    },

    /** Return this value as JSON. */
    toJson(value: AssignPatternField): Json {
        return toJsonAssignPatternField(value);
    },

    /** Return one AssignPatternField from one JSON value. */
    fromJson(value: Json): AssignPatternField {
        return fromJsonAssignPatternField(value);
    },
};

/** Encode one AssignPatternField. */
export function encodeAssignPatternField(writer: BinaryWriter, value: AssignPatternField): void {
    switch (value.kind) {
        case "named":
            writer.writeUnsigned(0);
            encodeName(writer, value.name);
            encodeLocalNodeId(writer, value.pattern);
            writer.writeBool(value.isShorthand);
            return;
        case "computed":
            writer.writeUnsigned(1);
            encodeLocalNodeId(writer, value.key);
            encodeLocalNodeId(writer, value.pattern);
            return;
        case "positional":
            writer.writeUnsigned(2);
            encodeLocalNodeId(writer, value.pattern);
            return;
        case "spread":
            writer.writeUnsigned(3);
            writer.writeOption(value.pattern, (value0) => {
                encodeLocalNodeId(writer, value0);
            });
            return;
        case "elision":
            writer.writeUnsigned(4);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one AssignPatternField. */
export function decodeAssignPatternField(reader: BinaryReader): AssignPatternField {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const name = decodeName(reader);
            const pattern = decodeLocalNodeId(reader);
            const isShorthand = reader.readBool();

            return {
                kind: "named",
                name,
                pattern,
                isShorthand,
            };
        }
        case 1: {
            const key = decodeLocalNodeId(reader);
            const pattern = decodeLocalNodeId(reader);

            return {
                kind: "computed",
                key,
                pattern,
            };
        }
        case 2: {
            const pattern = decodeLocalNodeId(reader);

            return {
                kind: "positional",
                pattern,
            };
        }
        case 3: {
            const pattern = reader.readOption(() => decodeLocalNodeId(reader));

            return {
                kind: "spread",
                ...(pattern === undefined ? {} : { pattern }),
            };
        }
        case 4: {
            return { kind: "elision" };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one AssignPatternField. */
export function toJsonAssignPatternField(value: AssignPatternField): Json {
    switch (value.kind) {
        case "named":
            return {
                kind: "named",
                name: toJsonName(value.name),
                pattern: toJsonLocalNodeId(value.pattern),
                isShorthand: value.isShorthand,
            };
        case "computed":
            return {
                kind: "computed",
                key: toJsonLocalNodeId(value.key),
                pattern: toJsonLocalNodeId(value.pattern),
            };
        case "positional":
            return {
                kind: "positional",
                pattern: toJsonLocalNodeId(value.pattern),
            };
        case "spread":
            return {
                kind: "spread",
                ...(value.pattern === undefined ? {} : { pattern: toJsonLocalNodeId(value.pattern) }),
            };
        case "elision":
            return {
                kind: "elision",
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one AssignPatternField from one JSON value. */
export function fromJsonAssignPatternField(value: Json): AssignPatternField {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "named":
            return {
                kind,
                name: fromJsonName(jsonField(object, "name")),
                pattern: fromJsonLocalNodeId(jsonField(object, "pattern")),
                isShorthand: jsonBool(jsonField(object, "isShorthand")),
            };
        case "computed":
            return {
                kind,
                key: fromJsonLocalNodeId(jsonField(object, "key")),
                pattern: fromJsonLocalNodeId(jsonField(object, "pattern")),
            };
        case "positional":
            return {
                kind,
                pattern: fromJsonLocalNodeId(jsonField(object, "pattern")),
            };
        case "spread":
            return {
                kind,
                pattern: jsonOptional(object, "pattern", (value) => fromJsonLocalNodeId(value)),
            };
        case "elision":
            return {
                kind,
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}
