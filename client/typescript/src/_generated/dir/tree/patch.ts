/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, compareBytes, jsonArray, jsonField, jsonObject, jsonOptional, jsonString, nestedBytes } from "../../../protocol/serde.js";
import type { LocalNodeIdAny } from "./node.js";
import type { Tree } from "./tree.js";
import type { ModuleId } from "../../source/file/model/module.js";
import { decodeLocalNodeIdAny, encodeLocalNodeIdAny, fromJsonLocalNodeIdAny, toJsonLocalNodeIdAny } from "./node.js";
import { decodeTree, encodeTree, fromJsonTree, toJsonTree } from "./tree.js";
import { decodeModuleId, encodeModuleId, fromJsonModuleId, toJsonModuleId } from "../../source/file/model/module.js";

/** A durable overlay over one base DIR tree. */
export type Patch = {
    /** The patch name. */
    readonly name: string;
    /** The owning module id. */
    readonly moduleId: ModuleId;
    /** The tree containing nodes introduced by this patch. */
    readonly tree: Tree;
    /** Replacement roots keyed by the base node they replace. */
    readonly replacementByNode: ReadonlyMap<LocalNodeIdAny, LocalNodeIdAny>;
    /** Node ids deleted by this patch. */
    readonly deletedNodes: ReadonlyArray<LocalNodeIdAny>;
    /** Parent overrides keyed by the visible child node. */
    readonly parentByNode: ReadonlyMap<LocalNodeIdAny, LocalNodeIdAny | undefined>;
};

export const Patch = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: Patch): void {
        encodePatch(writer, value);
    },

    /** Decode one Patch. */
    decode(reader: BinaryReader): Patch {
        return decodePatch(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Patch): Json {
        return toJsonPatch(value);
    },

    /** Return one Patch from one JSON value. */
    fromJson(value: Json): Patch {
        return fromJsonPatch(value);
    },
};

/** Encode one Patch. */
export function encodePatch(writer: BinaryWriter, value: Patch): void {
    writer.writeString(value.name);
    encodeModuleId(writer, value.moduleId);
    encodeTree(writer, value.tree);
    const entries3 = Array.from(value.replacementByNode.entries()).map(([key3, item3]) => {
        const keyBytes = nestedBytes((writer) => {
            encodeLocalNodeIdAny(writer, key3);
        });
        return { key3, item3, keyBytes };
    });
    entries3.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries3.length);
    for (const entry3 of entries3) {
        encodeLocalNodeIdAny(writer, entry3.key3);
        encodeLocalNodeIdAny(writer, entry3.item3);
    }
    writer.writeUnsigned(value.deletedNodes.length);
    for (const item4 of value.deletedNodes) {
        encodeLocalNodeIdAny(writer, item4);
    }
    const entries5 = Array.from(value.parentByNode.entries()).map(([key5, item5]) => {
        const keyBytes = nestedBytes((writer) => {
            encodeLocalNodeIdAny(writer, key5);
        });
        return { key5, item5, keyBytes };
    });
    entries5.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries5.length);
    for (const entry5 of entries5) {
        encodeLocalNodeIdAny(writer, entry5.key5);
        writer.writeOption(entry5.item5, (value6) => {
            encodeLocalNodeIdAny(writer, value6);
        });
    }
}

/** Decode one Patch. */
export function decodePatch(reader: BinaryReader): Patch {
    const name = reader.readString();
    const moduleId = decodeModuleId(reader);
    const tree = decodeTree(reader);
    const replacementByNode = (() => { const length3 = reader.readNumber(); const items3 = new Map<LocalNodeIdAny, LocalNodeIdAny>(); for (let index = 0; index < length3; index += 1) { items3.set(decodeLocalNodeIdAny(reader), decodeLocalNodeIdAny(reader)); } return items3; })();
    const deletedNodes = (() => { const length4 = reader.readNumber(); const items4: Array<LocalNodeIdAny> = []; for (let index = 0; index < length4; index += 1) { items4.push(decodeLocalNodeIdAny(reader)); } return items4; })();
    const parentByNode = (() => { const length5 = reader.readNumber(); const items5 = new Map<LocalNodeIdAny, LocalNodeIdAny | undefined>(); for (let index = 0; index < length5; index += 1) { items5.set(decodeLocalNodeIdAny(reader), reader.readOption(() => decodeLocalNodeIdAny(reader))); } return items5; })();

    return {
        name,
        moduleId,
        tree,
        replacementByNode,
        deletedNodes,
        parentByNode,
    };
}

/** Return one JSON value for one Patch. */
export function toJsonPatch(value: Patch): Json {
    return {
        name: value.name,
        moduleId: toJsonModuleId(value.moduleId),
        tree: toJsonTree(value.tree),
        replacementByNode: Array.from(value.replacementByNode.entries()).map(([key0, item0]) => [toJsonLocalNodeIdAny(key0), toJsonLocalNodeIdAny(item0)] as const),
        deletedNodes: value.deletedNodes.map((item0) => toJsonLocalNodeIdAny(item0)),
        parentByNode: Array.from(value.parentByNode.entries()).map(([key0, item0]) => [toJsonLocalNodeIdAny(key0), item0 === undefined ? null : toJsonLocalNodeIdAny(item0)] as const),
    };
}

/** Return one Patch from one JSON value. */
export function fromJsonPatch(value: Json): Patch {
    const object = jsonObject(value);

    return {
        name: jsonString(jsonField(object, "name")),
        moduleId: fromJsonModuleId(jsonField(object, "moduleId")),
        tree: fromJsonTree(jsonField(object, "tree")),
        replacementByNode: new Map(jsonArray(jsonField(object, "replacementByNode")).map((entry) => { const items = jsonArray(entry); if (items.length !== 2) { throw new SerdeError(`expected JSON map entry length 2: ${items.length}`); } const key0 = items[0]; const item0 = items[1]; return [fromJsonLocalNodeIdAny(key0), fromJsonLocalNodeIdAny(item0)] as const; })),
        deletedNodes: jsonArray(jsonField(object, "deletedNodes")).map((item0) => fromJsonLocalNodeIdAny(item0)),
        parentByNode: new Map(jsonArray(jsonField(object, "parentByNode")).map((entry) => { const items = jsonArray(entry); if (items.length !== 2) { throw new SerdeError(`expected JSON map entry length 2: ${items.length}`); } const key0 = items[0]; const item0 = items[1]; return [fromJsonLocalNodeIdAny(key0), item0 === null ? undefined : fromJsonLocalNodeIdAny(item0)] as const; })),
    };
}
