/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, jsonField, jsonObject, jsonString } from "../../../protocol/serde.js";
import type { LocalNodeId } from "./node.js";
import { decodeLocalNodeId, encodeLocalNodeId, fromJsonLocalNodeId, toJsonLocalNodeId } from "./node.js";

/** A decorator attached to an owner node. */
export type Decorator = {
    /** The decorator expression. */
    readonly expression: LocalNodeId;
    /** The decorator position relative to its owner. */
    readonly position: DecoratorPosition;
};

export const Decorator = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: Decorator): void {
        encodeDecorator(writer, value);
    },

    /** Decode one Decorator. */
    decode(reader: BinaryReader): Decorator {
        return decodeDecorator(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Decorator): Json {
        return toJsonDecorator(value);
    },

    /** Return one Decorator from one JSON value. */
    fromJson(value: Json): Decorator {
        return fromJsonDecorator(value);
    },
};

/** Encode one Decorator. */
export function encodeDecorator(writer: BinaryWriter, value: Decorator): void {
    encodeLocalNodeId(writer, value.expression);
    encodeDecoratorPosition(writer, value.position);
}

/** Decode one Decorator. */
export function decodeDecorator(reader: BinaryReader): Decorator {
    const expression = decodeLocalNodeId(reader);
    const position = decodeDecoratorPosition(reader);

    return {
        expression,
        position,
    };
}

/** Return one JSON value for one Decorator. */
export function toJsonDecorator(value: Decorator): Json {
    return {
        expression: toJsonLocalNodeId(value.expression),
        position: toJsonDecoratorPosition(value.position),
    };
}

/** Return one Decorator from one JSON value. */
export function fromJsonDecorator(value: Json): Decorator {
    const object = jsonObject(value);

    return {
        expression: fromJsonLocalNodeId(jsonField(object, "expression")),
        position: fromJsonDecoratorPosition(jsonField(object, "position")),
    };
}

/** The position of one decorator relative to its owner. */
export type DecoratorPosition = "blockInfix" | "blockPrefix" | "blockPostfix" | "linePrefix" | "linePostfix" | "linePostfixBoundary";

export const DecoratorPosition = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: DecoratorPosition): void {
        encodeDecoratorPosition(writer, value);
    },

    /** Decode one DecoratorPosition. */
    decode(reader: BinaryReader): DecoratorPosition {
        return decodeDecoratorPosition(reader);
    },

    /** Return this value as JSON. */
    toJson(value: DecoratorPosition): Json {
        return toJsonDecoratorPosition(value);
    },

    /** Return one DecoratorPosition from one JSON value. */
    fromJson(value: Json): DecoratorPosition {
        return fromJsonDecoratorPosition(value);
    },
};

/** Encode one DecoratorPosition. */
export function encodeDecoratorPosition(writer: BinaryWriter, value: DecoratorPosition): void {
    switch (value) {
        case "blockInfix":
            writer.writeUnsigned(0);
            return;
        case "blockPrefix":
            writer.writeUnsigned(1);
            return;
        case "blockPostfix":
            writer.writeUnsigned(2);
            return;
        case "linePrefix":
            writer.writeUnsigned(3);
            return;
        case "linePostfix":
            writer.writeUnsigned(4);
            return;
        case "linePostfixBoundary":
            writer.writeUnsigned(5);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one DecoratorPosition. */
export function decodeDecoratorPosition(reader: BinaryReader): DecoratorPosition {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "blockInfix";
        case 1:
            return "blockPrefix";
        case 2:
            return "blockPostfix";
        case 3:
            return "linePrefix";
        case 4:
            return "linePostfix";
        case 5:
            return "linePostfixBoundary";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one DecoratorPosition. */
export function toJsonDecoratorPosition(value: DecoratorPosition): Json {
    return value;
}

/** Return one DecoratorPosition from one JSON value. */
export function fromJsonDecoratorPosition(value: Json): DecoratorPosition {
    const variant = jsonString(value);

    switch (variant) {
        case "blockInfix":
            return "blockInfix";
        case "blockPrefix":
            return "blockPrefix";
        case "blockPostfix":
            return "blockPostfix";
        case "linePrefix":
            return "linePrefix";
        case "linePostfix":
            return "linePostfix";
        case "linePostfixBoundary":
            return "linePostfixBoundary";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}
