/* generated by destack_client_generate, do not edit */

import {
    BinaryReader,
    BinaryWriter,
    Json,
    SerdeError,
    compareBytes,
    jsonArray,
    jsonField,
    jsonInteger,
    jsonObject,
    jsonOptional,
    jsonString,
    nestedBytes,
} from "../../../protocol/serde.js";
import type { GlobalSymbolId } from "../symbol/symbol.js";
import type { TypeLiteral } from "../tree/literal.js";
import type { GlobalNodeIdAny } from "../tree/node.js";
import type { ModuleId } from "../../source/file/model/module.js";
import {
    decodeGlobalSymbolId,
    encodeGlobalSymbolId,
    fromJsonGlobalSymbolId,
    toJsonGlobalSymbolId,
} from "../symbol/symbol.js";
import {
    decodeTypeLiteral,
    encodeTypeLiteral,
    fromJsonTypeLiteral,
    toJsonTypeLiteral,
} from "../tree/literal.js";
import {
    decodeGlobalNodeIdAny,
    encodeGlobalNodeIdAny,
    fromJsonGlobalNodeIdAny,
    toJsonGlobalNodeIdAny,
} from "../tree/node.js";
import {
    decodeModuleId,
    encodeModuleId,
    fromJsonModuleId,
    toJsonModuleId,
} from "../../source/file/model/module.js";

/** How one source reference resolves by name, before types and conditions apply. */
export type Reference =
    /** Resolved to declarations by name: lexical scope or a full namespace path. */
    | {
          readonly kind: "bound";
          readonly bound: ReadonlyArray<GlobalSymbolId>;
      }
    /** Resolved to a namespace: a prefix awaiting a further segment, or a bare namespace value. */
    | {
          readonly kind: "namespace";
          /** The named module. */
          readonly module: ModuleId;
          /** The alias or clause declaring the namespace name, when one names it. */
          readonly declaration?: GlobalNodeIdAny;
      }
    /** A bare name spelling a builtin type literal, with no declaration behind it. */
    | {
          readonly kind: "typeLiteral";
          readonly type_literal: TypeLiteral;
      }
    /** A flat path named through its first segments; `segments[from..]` project from `base`. */
    | {
          readonly kind: "projected";
          /** The exact target named by the leading segments. */
          readonly base: ReferenceTarget;
          /** The segment index where member projection begins. */
          readonly from: number;
      }
    /** No single binding wins; the payload retains every resolved candidate target. */
    | {
          readonly kind: "ambiguous";
          readonly ambiguous: ReadonlyArray<ReferenceTarget>;
      }
    /** No binding by name. */
    | {
          readonly kind: "missing";
      };

export const Reference = {
    /** Resolved to declarations by name: lexical scope or a full namespace path. */
    bound(bound: ReadonlyArray<GlobalSymbolId>): Reference {
        return { kind: "bound", bound };
    },

    /** Resolved to a namespace: a prefix awaiting a further segment, or a bare namespace value. */
    namespace(module_: ModuleId, declaration: GlobalNodeIdAny | undefined): Reference {
        return { kind: "namespace", module: module_, declaration };
    },

    /** A bare name spelling a builtin type literal, with no declaration behind it. */
    typeLiteral(type_literal: TypeLiteral): Reference {
        return { kind: "typeLiteral", type_literal };
    },

    /** A flat path named through its first segments; `segments[from..]` project from `base`. */
    projected(base: ReferenceTarget, from_: number): Reference {
        return { kind: "projected", base, from: from_ };
    },

    /** No single binding wins; the payload retains every resolved candidate target. */
    ambiguous(ambiguous: ReadonlyArray<ReferenceTarget>): Reference {
        return { kind: "ambiguous", ambiguous };
    },

    /** No binding by name. */
    missing(): Reference {
        return { kind: "missing" };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: Reference): void {
        encodeReference(writer, value);
    },

    /** Decode one Reference. */
    decode(reader: BinaryReader): Reference {
        return decodeReference(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Reference): Json {
        return toJsonReference(value);
    },

    /** Return one Reference from one JSON value. */
    fromJson(value: Json): Reference {
        return fromJsonReference(value);
    },
};

/** Encode one Reference. */
export function encodeReference(writer: BinaryWriter, value: Reference): void {
    switch (value.kind) {
        case "bound":
            writer.writeUnsigned(0);
            writer.writeUnsigned(value.bound.length);
            for (const item0 of value.bound) {
                encodeGlobalSymbolId(writer, item0);
            }
            return;
        case "namespace":
            writer.writeUnsigned(1);
            encodeModuleId(writer, value.module);
            writer.writeOption(value.declaration, (value1) => {
                encodeGlobalNodeIdAny(writer, value1);
            });
            return;
        case "typeLiteral":
            writer.writeUnsigned(2);
            encodeTypeLiteral(writer, value.type_literal);
            return;
        case "projected":
            writer.writeUnsigned(3);
            encodeReferenceTarget(writer, value.base);
            writer.writeUnsigned(value.from);
            return;
        case "ambiguous":
            writer.writeUnsigned(4);
            writer.writeUnsigned(value.ambiguous.length);
            for (const item0 of value.ambiguous) {
                encodeReferenceTarget(writer, item0);
            }
            return;
        case "missing":
            writer.writeUnsigned(5);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one Reference. */
export function decodeReference(reader: BinaryReader): Reference {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const bound = (() => {
                const length0 = reader.readNumber();
                const items0: Array<GlobalSymbolId> = [];
                for (let index = 0; index < length0; index += 1) {
                    items0.push(decodeGlobalSymbolId(reader));
                }
                return items0;
            })();

            return { kind: "bound", bound };
        }
        case 1: {
            const module_ = decodeModuleId(reader);
            const declaration = reader.readOption(() => decodeGlobalNodeIdAny(reader));

            return {
                kind: "namespace",
                module: module_,
                ...(declaration === undefined ? {} : { declaration }),
            };
        }
        case 2: {
            const type_literal = decodeTypeLiteral(reader);

            return { kind: "typeLiteral", type_literal };
        }
        case 3: {
            const base = decodeReferenceTarget(reader);
            const from_ = reader.readNumber();

            return {
                kind: "projected",
                base,
                from: from_,
            };
        }
        case 4: {
            const ambiguous = (() => {
                const length0 = reader.readNumber();
                const items0: Array<ReferenceTarget> = [];
                for (let index = 0; index < length0; index += 1) {
                    items0.push(decodeReferenceTarget(reader));
                }
                return items0;
            })();

            return { kind: "ambiguous", ambiguous };
        }
        case 5: {
            return { kind: "missing" };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one Reference. */
export function toJsonReference(value: Reference): Json {
    switch (value.kind) {
        case "bound":
            return {
                kind: "bound",
                bound: value.bound.map((item0) => toJsonGlobalSymbolId(item0)),
            };
        case "namespace":
            return {
                kind: "namespace",
                module: toJsonModuleId(value.module),
                ...(value.declaration === undefined
                    ? {}
                    : { declaration: toJsonGlobalNodeIdAny(value.declaration) }),
            };
        case "typeLiteral":
            return {
                kind: "typeLiteral",
                type_literal: toJsonTypeLiteral(value.type_literal),
            };
        case "projected":
            return {
                kind: "projected",
                base: toJsonReferenceTarget(value.base),
                from: value.from,
            };
        case "ambiguous":
            return {
                kind: "ambiguous",
                ambiguous: value.ambiguous.map((item0) => toJsonReferenceTarget(item0)),
            };
        case "missing":
            return {
                kind: "missing",
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one Reference from one JSON value. */
export function fromJsonReference(value: Json): Reference {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "bound":
            return {
                kind,
                bound: jsonArray(jsonField(object, "bound")).map((item0) =>
                    fromJsonGlobalSymbolId(item0),
                ),
            };
        case "namespace":
            return {
                kind,
                module: fromJsonModuleId(jsonField(object, "module")),
                declaration: jsonOptional(object, "declaration", (value) =>
                    fromJsonGlobalNodeIdAny(value),
                ),
            };
        case "typeLiteral":
            return {
                kind,
                type_literal: fromJsonTypeLiteral(jsonField(object, "type_literal")),
            };
        case "projected":
            return {
                kind,
                base: fromJsonReferenceTarget(jsonField(object, "base")),
                from: jsonInteger(jsonField(object, "from")),
            };
        case "ambiguous":
            return {
                kind,
                ambiguous: jsonArray(jsonField(object, "ambiguous")).map((item0) =>
                    fromJsonReferenceTarget(item0),
                ),
            };
        case "missing":
            return {
                kind,
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** Name resolutions for one module, keyed by the reference node. */
export type ReferenceTable = {
    /** The module id of the reference table. */
    readonly moduleId: ModuleId;
    /** Final targets keyed by their source node. */
    readonly targetByNode: ReadonlyMap<GlobalNodeIdAny, Reference>;
    /** Authored declarations that differ from their final target. */
    readonly declarationByNode: ReadonlyMap<GlobalNodeIdAny, Reference>;
};

export const ReferenceTable = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ReferenceTable): void {
        encodeReferenceTable(writer, value);
    },

    /** Decode one ReferenceTable. */
    decode(reader: BinaryReader): ReferenceTable {
        return decodeReferenceTable(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ReferenceTable): Json {
        return toJsonReferenceTable(value);
    },

    /** Return one ReferenceTable from one JSON value. */
    fromJson(value: Json): ReferenceTable {
        return fromJsonReferenceTable(value);
    },
};

/** Encode one ReferenceTable. */
export function encodeReferenceTable(writer: BinaryWriter, value: ReferenceTable): void {
    encodeModuleId(writer, value.moduleId);
    const entries1 = Array.from(value.targetByNode.entries()).map(([key1, item1]) => {
        const keyBytes = nestedBytes((writer) => {
            encodeGlobalNodeIdAny(writer, key1);
        });
        return { key1, item1, keyBytes };
    });
    entries1.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries1.length);
    for (const entry1 of entries1) {
        encodeGlobalNodeIdAny(writer, entry1.key1);
        encodeReference(writer, entry1.item1);
    }
    const entries2 = Array.from(value.declarationByNode.entries()).map(([key2, item2]) => {
        const keyBytes = nestedBytes((writer) => {
            encodeGlobalNodeIdAny(writer, key2);
        });
        return { key2, item2, keyBytes };
    });
    entries2.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries2.length);
    for (const entry2 of entries2) {
        encodeGlobalNodeIdAny(writer, entry2.key2);
        encodeReference(writer, entry2.item2);
    }
}

/** Decode one ReferenceTable. */
export function decodeReferenceTable(reader: BinaryReader): ReferenceTable {
    const moduleId = decodeModuleId(reader);
    const targetByNode = (() => {
        const length1 = reader.readNumber();
        const items1 = new Map<GlobalNodeIdAny, Reference>();
        for (let index = 0; index < length1; index += 1) {
            items1.set(decodeGlobalNodeIdAny(reader), decodeReference(reader));
        }
        return items1;
    })();
    const declarationByNode = (() => {
        const length2 = reader.readNumber();
        const items2 = new Map<GlobalNodeIdAny, Reference>();
        for (let index = 0; index < length2; index += 1) {
            items2.set(decodeGlobalNodeIdAny(reader), decodeReference(reader));
        }
        return items2;
    })();

    return {
        moduleId,
        targetByNode,
        declarationByNode,
    };
}

/** Return one JSON value for one ReferenceTable. */
export function toJsonReferenceTable(value: ReferenceTable): Json {
    return {
        moduleId: toJsonModuleId(value.moduleId),
        targetByNode: Array.from(value.targetByNode.entries()).map(
            ([key0, item0]) => [toJsonGlobalNodeIdAny(key0), toJsonReference(item0)] as const,
        ),
        declarationByNode: Array.from(value.declarationByNode.entries()).map(
            ([key0, item0]) => [toJsonGlobalNodeIdAny(key0), toJsonReference(item0)] as const,
        ),
    };
}

/** Return one ReferenceTable from one JSON value. */
export function fromJsonReferenceTable(value: Json): ReferenceTable {
    const object = jsonObject(value);

    return {
        moduleId: fromJsonModuleId(jsonField(object, "moduleId")),
        targetByNode: new Map(
            jsonArray(jsonField(object, "targetByNode")).map((entry) => {
                const items = jsonArray(entry);
                if (items.length !== 2) {
                    throw new SerdeError(`expected JSON map entry length 2: ${items.length}`);
                }
                const key0 = items[0];
                const item0 = items[1];
                return [fromJsonGlobalNodeIdAny(key0), fromJsonReference(item0)] as const;
            }),
        ),
        declarationByNode: new Map(
            jsonArray(jsonField(object, "declarationByNode")).map((entry) => {
                const items = jsonArray(entry);
                if (items.length !== 2) {
                    throw new SerdeError(`expected JSON map entry length 2: ${items.length}`);
                }
                const key0 = items[0];
                const item0 = items[1];
                return [fromJsonGlobalNodeIdAny(key0), fromJsonReference(item0)] as const;
            }),
        ),
    };
}

/** One scalar target selected while resolving a source reference. */
export type ReferenceTarget =
    /** One declaration symbol. */
    | {
          readonly kind: "symbol";
          readonly symbol: GlobalSymbolId;
      }
    /** One module namespace object. */
    | {
          readonly kind: "namespace";
          readonly namespace: ModuleId;
      };

export const ReferenceTarget = {
    /** One declaration symbol. */
    symbol(symbol_: GlobalSymbolId): ReferenceTarget {
        return { kind: "symbol", symbol: symbol_ };
    },

    /** One module namespace object. */
    namespace(namespace_: ModuleId): ReferenceTarget {
        return { kind: "namespace", namespace: namespace_ };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: ReferenceTarget): void {
        encodeReferenceTarget(writer, value);
    },

    /** Decode one ReferenceTarget. */
    decode(reader: BinaryReader): ReferenceTarget {
        return decodeReferenceTarget(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ReferenceTarget): Json {
        return toJsonReferenceTarget(value);
    },

    /** Return one ReferenceTarget from one JSON value. */
    fromJson(value: Json): ReferenceTarget {
        return fromJsonReferenceTarget(value);
    },
};

/** Encode one ReferenceTarget. */
export function encodeReferenceTarget(writer: BinaryWriter, value: ReferenceTarget): void {
    switch (value.kind) {
        case "symbol":
            writer.writeUnsigned(0);
            encodeGlobalSymbolId(writer, value.symbol);
            return;
        case "namespace":
            writer.writeUnsigned(1);
            encodeModuleId(writer, value.namespace);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one ReferenceTarget. */
export function decodeReferenceTarget(reader: BinaryReader): ReferenceTarget {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const symbol_ = decodeGlobalSymbolId(reader);

            return { kind: "symbol", symbol: symbol_ };
        }
        case 1: {
            const namespace_ = decodeModuleId(reader);

            return { kind: "namespace", namespace: namespace_ };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one ReferenceTarget. */
export function toJsonReferenceTarget(value: ReferenceTarget): Json {
    switch (value.kind) {
        case "symbol":
            return {
                kind: "symbol",
                symbol: toJsonGlobalSymbolId(value.symbol),
            };
        case "namespace":
            return {
                kind: "namespace",
                namespace: toJsonModuleId(value.namespace),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one ReferenceTarget from one JSON value. */
export function fromJsonReferenceTarget(value: Json): ReferenceTarget {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "symbol":
            return {
                kind,
                symbol: fromJsonGlobalSymbolId(jsonField(object, "symbol")),
            };
        case "namespace":
            return {
                kind,
                namespace: fromJsonModuleId(jsonField(object, "namespace")),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}
