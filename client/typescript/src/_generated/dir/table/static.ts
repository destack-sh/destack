/* generated by destack_client_generate, do not edit */

import {
    BinaryReader,
    BinaryWriter,
    Json,
    SerdeError,
    compareBytes,
    jsonArray,
    jsonField,
    jsonInteger,
    jsonObject,
    jsonString,
    nestedBytes,
} from "../../../protocol/serde.js";
import type { GlobalSymbolId } from "../symbol/symbol.js";
import type { LocalNodeIdAny } from "../tree/node.js";
import type { GlobalStaticId } from "../tree/static.js";
import type { StaticTerm } from "../tree/static.js";
import type { ModuleId } from "../../source/file/model/module.js";
import {
    decodeGlobalSymbolId,
    encodeGlobalSymbolId,
    fromJsonGlobalSymbolId,
    toJsonGlobalSymbolId,
} from "../symbol/symbol.js";
import {
    decodeLocalNodeIdAny,
    encodeLocalNodeIdAny,
    fromJsonLocalNodeIdAny,
    toJsonLocalNodeIdAny,
} from "../tree/node.js";
import {
    decodeGlobalStaticId,
    encodeGlobalStaticId,
    fromJsonGlobalStaticId,
    toJsonGlobalStaticId,
} from "../tree/static.js";
import {
    decodeStaticTerm,
    encodeStaticTerm,
    fromJsonStaticTerm,
    toJsonStaticTerm,
} from "../tree/static.js";
import {
    decodeModuleId,
    encodeModuleId,
    fromJsonModuleId,
    toJsonModuleId,
} from "../../source/file/model/module.js";

/** Source presence decided by one closed static gate. */
export type StaticPresence = "absent" | "present";

export const StaticPresence = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: StaticPresence): void {
        encodeStaticPresence(writer, value);
    },

    /** Decode one StaticPresence. */
    decode(reader: BinaryReader): StaticPresence {
        return decodeStaticPresence(reader);
    },

    /** Return this value as JSON. */
    toJson(value: StaticPresence): Json {
        return toJsonStaticPresence(value);
    },

    /** Return one StaticPresence from one JSON value. */
    fromJson(value: Json): StaticPresence {
        return fromJsonStaticPresence(value);
    },
};

/** Encode one StaticPresence. */
export function encodeStaticPresence(writer: BinaryWriter, value: StaticPresence): void {
    switch (value) {
        case "absent":
            writer.writeUnsigned(0);
            return;
        case "present":
            writer.writeUnsigned(1);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one StaticPresence. */
export function decodeStaticPresence(reader: BinaryReader): StaticPresence {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "absent";
        case 1:
            return "present";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one StaticPresence. */
export function toJsonStaticPresence(value: StaticPresence): Json {
    return value;
}

/** Return one StaticPresence from one JSON value. */
export function fromJsonStaticPresence(value: Json): StaticPresence {
    const variant = jsonString(value);

    switch (variant) {
        case "absent":
            return "absent";
        case "present":
            return "present";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** Static values added by one DIR phase. */
export type StaticSegment = {
    /** The module id of the static segment. */
    readonly moduleId: ModuleId;
    /** The first static id owned by this table segment. */
    readonly firstStaticId: number;
    /** Interned static values. */
    readonly statics: ReadonlyArray<StaticTerm>;
    /** Checked static value keyed by symbol. */
    readonly staticBySymbolId: ReadonlyMap<GlobalSymbolId, GlobalStaticId>;
    /** Gate decisions by decorated node. */
    readonly gates: ReadonlyMap<LocalNodeIdAny, StaticPresence>;
};

export const StaticSegment = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: StaticSegment): void {
        encodeStaticSegment(writer, value);
    },

    /** Decode one StaticSegment. */
    decode(reader: BinaryReader): StaticSegment {
        return decodeStaticSegment(reader);
    },

    /** Return this value as JSON. */
    toJson(value: StaticSegment): Json {
        return toJsonStaticSegment(value);
    },

    /** Return one StaticSegment from one JSON value. */
    fromJson(value: Json): StaticSegment {
        return fromJsonStaticSegment(value);
    },
};

/** Encode one StaticSegment. */
export function encodeStaticSegment(writer: BinaryWriter, value: StaticSegment): void {
    encodeModuleId(writer, value.moduleId);
    writer.writeUnsigned(value.firstStaticId);
    writer.writeUnsigned(value.statics.length);
    for (const item2 of value.statics) {
        encodeStaticTerm(writer, item2);
    }
    const entries3 = Array.from(value.staticBySymbolId.entries()).map(([key3, item3]) => {
        const keyBytes = nestedBytes((writer) => {
            encodeGlobalSymbolId(writer, key3);
        });
        return { key3, item3, keyBytes };
    });
    entries3.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries3.length);
    for (const entry3 of entries3) {
        encodeGlobalSymbolId(writer, entry3.key3);
        encodeGlobalStaticId(writer, entry3.item3);
    }
    const entries4 = Array.from(value.gates.entries()).map(([key4, item4]) => {
        const keyBytes = nestedBytes((writer) => {
            encodeLocalNodeIdAny(writer, key4);
        });
        return { key4, item4, keyBytes };
    });
    entries4.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries4.length);
    for (const entry4 of entries4) {
        encodeLocalNodeIdAny(writer, entry4.key4);
        encodeStaticPresence(writer, entry4.item4);
    }
}

/** Decode one StaticSegment. */
export function decodeStaticSegment(reader: BinaryReader): StaticSegment {
    const moduleId = decodeModuleId(reader);
    const firstStaticId = reader.readNumber();
    const statics = (() => {
        const length2 = reader.readNumber();
        const items2: Array<StaticTerm> = [];
        for (let index = 0; index < length2; index += 1) {
            items2.push(decodeStaticTerm(reader));
        }
        return items2;
    })();
    const staticBySymbolId = (() => {
        const length3 = reader.readNumber();
        const items3 = new Map<GlobalSymbolId, GlobalStaticId>();
        for (let index = 0; index < length3; index += 1) {
            items3.set(decodeGlobalSymbolId(reader), decodeGlobalStaticId(reader));
        }
        return items3;
    })();
    const gates = (() => {
        const length4 = reader.readNumber();
        const items4 = new Map<LocalNodeIdAny, StaticPresence>();
        for (let index = 0; index < length4; index += 1) {
            items4.set(decodeLocalNodeIdAny(reader), decodeStaticPresence(reader));
        }
        return items4;
    })();

    return {
        moduleId,
        firstStaticId,
        statics,
        staticBySymbolId,
        gates,
    };
}

/** Return one JSON value for one StaticSegment. */
export function toJsonStaticSegment(value: StaticSegment): Json {
    return {
        moduleId: toJsonModuleId(value.moduleId),
        firstStaticId: value.firstStaticId,
        statics: value.statics.map((item0) => toJsonStaticTerm(item0)),
        staticBySymbolId: Array.from(value.staticBySymbolId.entries()).map(
            ([key0, item0]) => [toJsonGlobalSymbolId(key0), toJsonGlobalStaticId(item0)] as const,
        ),
        gates: Array.from(value.gates.entries()).map(
            ([key0, item0]) => [toJsonLocalNodeIdAny(key0), toJsonStaticPresence(item0)] as const,
        ),
    };
}

/** Return one StaticSegment from one JSON value. */
export function fromJsonStaticSegment(value: Json): StaticSegment {
    const object = jsonObject(value);

    return {
        moduleId: fromJsonModuleId(jsonField(object, "moduleId")),
        firstStaticId: jsonInteger(jsonField(object, "firstStaticId")),
        statics: jsonArray(jsonField(object, "statics")).map((item0) => fromJsonStaticTerm(item0)),
        staticBySymbolId: new Map(
            jsonArray(jsonField(object, "staticBySymbolId")).map((entry) => {
                const items = jsonArray(entry);
                if (items.length !== 2) {
                    throw new SerdeError(`expected JSON map entry length 2: ${items.length}`);
                }
                const key0 = items[0];
                const item0 = items[1];
                return [fromJsonGlobalSymbolId(key0), fromJsonGlobalStaticId(item0)] as const;
            }),
        ),
        gates: new Map(
            jsonArray(jsonField(object, "gates")).map((entry) => {
                const items = jsonArray(entry);
                if (items.length !== 2) {
                    throw new SerdeError(`expected JSON map entry length 2: ${items.length}`);
                }
                const key0 = items[0];
                const item0 = items[1];
                return [fromJsonLocalNodeIdAny(key0), fromJsonStaticPresence(item0)] as const;
            }),
        ),
    };
}
