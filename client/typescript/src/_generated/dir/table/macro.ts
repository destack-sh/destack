/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, jsonArray, jsonField, jsonObject, jsonOptional, jsonString } from "../../../protocol/serde.js";
import type { GlobalSymbolId } from "../symbol/symbol.js";
import type { GlobalNodeId } from "../tree/node.js";
import type { GlobalNodeIdAny } from "../tree/node.js";
import type { StaticTerm } from "../tree/static.js";
import type { ModuleId } from "../../source/file/model/module.js";
import { decodeGlobalSymbolId, encodeGlobalSymbolId, fromJsonGlobalSymbolId, toJsonGlobalSymbolId } from "../symbol/symbol.js";
import { decodeGlobalNodeId, encodeGlobalNodeId, fromJsonGlobalNodeId, toJsonGlobalNodeId } from "../tree/node.js";
import { decodeGlobalNodeIdAny, encodeGlobalNodeIdAny, fromJsonGlobalNodeIdAny, toJsonGlobalNodeIdAny } from "../tree/node.js";
import { decodeStaticTerm, encodeStaticTerm, fromJsonStaticTerm, toJsonStaticTerm } from "../tree/static.js";
import { decodeModuleId, encodeModuleId, fromJsonModuleId, toJsonModuleId } from "../../source/file/model/module.js";

/** One macro invocation completed during fixed-point expansion. */
export type MacroInvocation = {
    /** The decorated target node. */
    readonly targetNode: GlobalNodeIdAny;
    /** What caused this macro invocation. */
    readonly trigger: MacroTrigger;
    /** The resolved `Macro` implementation. */
    readonly implementation: GlobalSymbolId;
    /** The static macro configuration. */
    readonly config: StaticTerm;
    /** State carried from expansion to materialization. */
    readonly state?: StaticTerm;
};

export const MacroInvocation = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: MacroInvocation): void {
        encodeMacroInvocation(writer, value);
    },

    /** Decode one MacroInvocation. */
    decode(reader: BinaryReader): MacroInvocation {
        return decodeMacroInvocation(reader);
    },

    /** Return this value as JSON. */
    toJson(value: MacroInvocation): Json {
        return toJsonMacroInvocation(value);
    },

    /** Return one MacroInvocation from one JSON value. */
    fromJson(value: Json): MacroInvocation {
        return fromJsonMacroInvocation(value);
    },
};

/** Encode one MacroInvocation. */
export function encodeMacroInvocation(writer: BinaryWriter, value: MacroInvocation): void {
    encodeGlobalNodeIdAny(writer, value.targetNode);
    encodeMacroTrigger(writer, value.trigger);
    encodeGlobalSymbolId(writer, value.implementation);
    encodeStaticTerm(writer, value.config);
    writer.writeOption(value.state, (value4) => {
        encodeStaticTerm(writer, value4);
    });
}

/** Decode one MacroInvocation. */
export function decodeMacroInvocation(reader: BinaryReader): MacroInvocation {
    const targetNode = decodeGlobalNodeIdAny(reader);
    const trigger = decodeMacroTrigger(reader);
    const implementation = decodeGlobalSymbolId(reader);
    const config = decodeStaticTerm(reader);
    const state = reader.readOption(() => decodeStaticTerm(reader));

    return {
        targetNode,
        trigger,
        implementation,
        config,
        ...(state === undefined ? {} : { state }),
    };
}

/** Return one JSON value for one MacroInvocation. */
export function toJsonMacroInvocation(value: MacroInvocation): Json {
    return {
        targetNode: toJsonGlobalNodeIdAny(value.targetNode),
        trigger: toJsonMacroTrigger(value.trigger),
        implementation: toJsonGlobalSymbolId(value.implementation),
        config: toJsonStaticTerm(value.config),
        ...(value.state === undefined ? {} : { state: toJsonStaticTerm(value.state) }),
    };
}

/** Return one MacroInvocation from one JSON value. */
export function fromJsonMacroInvocation(value: Json): MacroInvocation {
    const object = jsonObject(value);

    return {
        targetNode: fromJsonGlobalNodeIdAny(jsonField(object, "targetNode")),
        trigger: fromJsonMacroTrigger(jsonField(object, "trigger")),
        implementation: fromJsonGlobalSymbolId(jsonField(object, "implementation")),
        config: fromJsonStaticTerm(jsonField(object, "config")),
        state: jsonOptional(object, "state", (value) => fromJsonStaticTerm(value)),
    };
}

/** Macro expansion state for one DIR module. */
export type MacroTable = {
    /** The module id of the macro table. */
    readonly moduleId: ModuleId;
    /** Expanded macro invocations. */
    readonly invocations: ReadonlyArray<MacroInvocation>;
};

export const MacroTable = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: MacroTable): void {
        encodeMacroTable(writer, value);
    },

    /** Decode one MacroTable. */
    decode(reader: BinaryReader): MacroTable {
        return decodeMacroTable(reader);
    },

    /** Return this value as JSON. */
    toJson(value: MacroTable): Json {
        return toJsonMacroTable(value);
    },

    /** Return one MacroTable from one JSON value. */
    fromJson(value: Json): MacroTable {
        return fromJsonMacroTable(value);
    },
};

/** Encode one MacroTable. */
export function encodeMacroTable(writer: BinaryWriter, value: MacroTable): void {
    encodeModuleId(writer, value.moduleId);
    writer.writeUnsigned(value.invocations.length);
    for (const item1 of value.invocations) {
        encodeMacroInvocation(writer, item1);
    }
}

/** Decode one MacroTable. */
export function decodeMacroTable(reader: BinaryReader): MacroTable {
    const moduleId = decodeModuleId(reader);
    const invocations = (() => { const length1 = reader.readNumber(); const items1: Array<MacroInvocation> = []; for (let index = 0; index < length1; index += 1) { items1.push(decodeMacroInvocation(reader)); } return items1; })();

    return {
        moduleId,
        invocations,
    };
}

/** Return one JSON value for one MacroTable. */
export function toJsonMacroTable(value: MacroTable): Json {
    return {
        moduleId: toJsonModuleId(value.moduleId),
        invocations: value.invocations.map((item0) => toJsonMacroInvocation(item0)),
    };
}

/** Return one MacroTable from one JSON value. */
export function fromJsonMacroTable(value: Json): MacroTable {
    const object = jsonObject(value);

    return {
        moduleId: fromJsonModuleId(jsonField(object, "moduleId")),
        invocations: jsonArray(jsonField(object, "invocations")).map((item0) => fromJsonMacroInvocation(item0)),
    };
}

/** What caused one macro invocation. */
export type MacroTrigger =
    /** A decorator on the target node. */
    | {
          readonly kind: "decorator";
          readonly decorator: GlobalNodeId;
      }
    /** A configured automatic derivation. */
    | {
          readonly kind: "autoDerive";
      }
;

export const MacroTrigger = {
    /** A decorator on the target node. */
    decorator(decorator: GlobalNodeId): MacroTrigger {
        return { kind: "decorator", decorator };
    },

    /** A configured automatic derivation. */
    autoDerive(): MacroTrigger {
        return { kind: "autoDerive" };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: MacroTrigger): void {
        encodeMacroTrigger(writer, value);
    },

    /** Decode one MacroTrigger. */
    decode(reader: BinaryReader): MacroTrigger {
        return decodeMacroTrigger(reader);
    },

    /** Return this value as JSON. */
    toJson(value: MacroTrigger): Json {
        return toJsonMacroTrigger(value);
    },

    /** Return one MacroTrigger from one JSON value. */
    fromJson(value: Json): MacroTrigger {
        return fromJsonMacroTrigger(value);
    },
};

/** Encode one MacroTrigger. */
export function encodeMacroTrigger(writer: BinaryWriter, value: MacroTrigger): void {
    switch (value.kind) {
        case "decorator":
            writer.writeUnsigned(0);
            encodeGlobalNodeId(writer, value.decorator);
            return;
        case "autoDerive":
            writer.writeUnsigned(1);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one MacroTrigger. */
export function decodeMacroTrigger(reader: BinaryReader): MacroTrigger {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const decorator = decodeGlobalNodeId(reader);

            return { kind: "decorator", decorator };
        }
        case 1: {
            return { kind: "autoDerive" };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one MacroTrigger. */
export function toJsonMacroTrigger(value: MacroTrigger): Json {
    switch (value.kind) {
        case "decorator":
            return {
                kind: "decorator",
                decorator: toJsonGlobalNodeId(value.decorator),
            };
        case "autoDerive":
            return {
                kind: "autoDerive",
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one MacroTrigger from one JSON value. */
export function fromJsonMacroTrigger(value: Json): MacroTrigger {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "decorator":
            return {
                kind,
                decorator: fromJsonGlobalNodeId(jsonField(object, "decorator")),
            };
        case "autoDerive":
            return {
                kind,
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}
