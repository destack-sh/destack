/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, jsonArray, jsonField, jsonObject, jsonString } from "../../../protocol/serde.js";
import type { GlobalSymbolId } from "../symbol/symbol.js";
import type { GlobalNodeIdAny } from "../tree/node.js";
import type { GlobalTypeId } from "../type/type.js";
import type { ModuleId } from "../../source/file/model/module.js";
import { decodeGlobalSymbolId, encodeGlobalSymbolId, fromJsonGlobalSymbolId, toJsonGlobalSymbolId } from "../symbol/symbol.js";
import { decodeGlobalNodeIdAny, encodeGlobalNodeIdAny, fromJsonGlobalNodeIdAny, toJsonGlobalNodeIdAny } from "../tree/node.js";
import { decodeGlobalTypeId, encodeGlobalTypeId, fromJsonGlobalTypeId, toJsonGlobalTypeId } from "../type/type.js";
import { decodeModuleId, encodeModuleId, fromJsonModuleId, toJsonModuleId } from "../../source/file/model/module.js";

/** One checked marker conformance. */
export type AutoConformance = {
    /** The satisfied marker interface. */
    readonly interface: AutoInterface;
    /** The conforming type. */
    readonly target: GlobalTypeId;
};

export const AutoConformance = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: AutoConformance): void {
        encodeAutoConformance(writer, value);
    },

    /** Decode one AutoConformance. */
    decode(reader: BinaryReader): AutoConformance {
        return decodeAutoConformance(reader);
    },

    /** Return this value as JSON. */
    toJson(value: AutoConformance): Json {
        return toJsonAutoConformance(value);
    },

    /** Return one AutoConformance from one JSON value. */
    fromJson(value: Json): AutoConformance {
        return fromJsonAutoConformance(value);
    },
};

/** Encode one AutoConformance. */
export function encodeAutoConformance(writer: BinaryWriter, value: AutoConformance): void {
    encodeAutoInterface(writer, value.interface);
    encodeGlobalTypeId(writer, value.target);
}

/** Decode one AutoConformance. */
export function decodeAutoConformance(reader: BinaryReader): AutoConformance {
    const interface_ = decodeAutoInterface(reader);
    const target = decodeGlobalTypeId(reader);

    return {
        interface: interface_,
        target,
    };
}

/** Return one JSON value for one AutoConformance. */
export function toJsonAutoConformance(value: AutoConformance): Json {
    return {
        interface: toJsonAutoInterface(value.interface),
        target: toJsonGlobalTypeId(value.target),
    };
}

/** Return one AutoConformance from one JSON value. */
export function fromJsonAutoConformance(value: Json): AutoConformance {
    const object = jsonObject(value);

    return {
        interface: fromJsonAutoInterface(jsonField(object, "interface")),
        target: fromJsonGlobalTypeId(jsonField(object, "target")),
    };
}

/** Generated implementation for one auto interface. */
export type AutoDerivedImplementation = {
    /** The source node that requested this implementation. */
    readonly source: GlobalNodeIdAny;
    /** The generated interface. */
    readonly interface: AutoInterface;
    /** The implemented type. */
    readonly target: GlobalTypeId;
    /** The generated members. */
    readonly members: ReadonlyArray<AutoImplementationMember>;
};

export const AutoDerivedImplementation = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: AutoDerivedImplementation): void {
        encodeAutoDerivedImplementation(writer, value);
    },

    /** Decode one AutoDerivedImplementation. */
    decode(reader: BinaryReader): AutoDerivedImplementation {
        return decodeAutoDerivedImplementation(reader);
    },

    /** Return this value as JSON. */
    toJson(value: AutoDerivedImplementation): Json {
        return toJsonAutoDerivedImplementation(value);
    },

    /** Return one AutoDerivedImplementation from one JSON value. */
    fromJson(value: Json): AutoDerivedImplementation {
        return fromJsonAutoDerivedImplementation(value);
    },
};

/** Encode one AutoDerivedImplementation. */
export function encodeAutoDerivedImplementation(writer: BinaryWriter, value: AutoDerivedImplementation): void {
    encodeGlobalNodeIdAny(writer, value.source);
    encodeAutoInterface(writer, value.interface);
    encodeGlobalTypeId(writer, value.target);
    writer.writeUnsigned(value.members.length);
    for (const item3 of value.members) {
        encodeAutoImplementationMember(writer, item3);
    }
}

/** Decode one AutoDerivedImplementation. */
export function decodeAutoDerivedImplementation(reader: BinaryReader): AutoDerivedImplementation {
    const source = decodeGlobalNodeIdAny(reader);
    const interface_ = decodeAutoInterface(reader);
    const target = decodeGlobalTypeId(reader);
    const members = (() => { const length3 = reader.readNumber(); const items3: Array<AutoImplementationMember> = []; for (let index = 0; index < length3; index += 1) { items3.push(decodeAutoImplementationMember(reader)); } return items3; })();

    return {
        source,
        interface: interface_,
        target,
        members,
    };
}

/** Return one JSON value for one AutoDerivedImplementation. */
export function toJsonAutoDerivedImplementation(value: AutoDerivedImplementation): Json {
    return {
        source: toJsonGlobalNodeIdAny(value.source),
        interface: toJsonAutoInterface(value.interface),
        target: toJsonGlobalTypeId(value.target),
        members: value.members.map((item0) => toJsonAutoImplementationMember(item0)),
    };
}

/** Return one AutoDerivedImplementation from one JSON value. */
export function fromJsonAutoDerivedImplementation(value: Json): AutoDerivedImplementation {
    const object = jsonObject(value);

    return {
        source: fromJsonGlobalNodeIdAny(jsonField(object, "source")),
        interface: fromJsonAutoInterface(jsonField(object, "interface")),
        target: fromJsonGlobalTypeId(jsonField(object, "target")),
        members: jsonArray(jsonField(object, "members")).map((item0) => fromJsonAutoImplementationMember(item0)),
    };
}

/** Member generated for one auto-derived implementation. */
export type AutoImplementationMember = {
    /** The source node that owns the generated member. */
    readonly source: GlobalNodeIdAny;
    /** The generated member symbol. */
    readonly symbol: GlobalSymbolId;
    /** The generated member type. */
    readonly ty: GlobalTypeId;
};

export const AutoImplementationMember = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: AutoImplementationMember): void {
        encodeAutoImplementationMember(writer, value);
    },

    /** Decode one AutoImplementationMember. */
    decode(reader: BinaryReader): AutoImplementationMember {
        return decodeAutoImplementationMember(reader);
    },

    /** Return this value as JSON. */
    toJson(value: AutoImplementationMember): Json {
        return toJsonAutoImplementationMember(value);
    },

    /** Return one AutoImplementationMember from one JSON value. */
    fromJson(value: Json): AutoImplementationMember {
        return fromJsonAutoImplementationMember(value);
    },
};

/** Encode one AutoImplementationMember. */
export function encodeAutoImplementationMember(writer: BinaryWriter, value: AutoImplementationMember): void {
    encodeGlobalNodeIdAny(writer, value.source);
    encodeGlobalSymbolId(writer, value.symbol);
    encodeGlobalTypeId(writer, value.ty);
}

/** Decode one AutoImplementationMember. */
export function decodeAutoImplementationMember(reader: BinaryReader): AutoImplementationMember {
    const source = decodeGlobalNodeIdAny(reader);
    const symbol_ = decodeGlobalSymbolId(reader);
    const ty = decodeGlobalTypeId(reader);

    return {
        source,
        symbol: symbol_,
        ty,
    };
}

/** Return one JSON value for one AutoImplementationMember. */
export function toJsonAutoImplementationMember(value: AutoImplementationMember): Json {
    return {
        source: toJsonGlobalNodeIdAny(value.source),
        symbol: toJsonGlobalSymbolId(value.symbol),
        ty: toJsonGlobalTypeId(value.ty),
    };
}

/** Return one AutoImplementationMember from one JSON value. */
export function fromJsonAutoImplementationMember(value: Json): AutoImplementationMember {
    const object = jsonObject(value);

    return {
        source: fromJsonGlobalNodeIdAny(jsonField(object, "source")),
        symbol: fromJsonGlobalSymbolId(jsonField(object, "symbol")),
        ty: fromJsonGlobalTypeId(jsonField(object, "ty")),
    };
}

/** Interface whose implementation can be provided by compiler rules. */
export type AutoInterface = "compare" | "concrete" | "copy" | "clone" | "debug" | "default" | "deserialize" | "dynamicSafe" | "equal" | "float" | "hash" | "integer" | "overwriteStable" | "partialCompare" | "partialEqual" | "serialize" | "sharedSafe" | "unpin" | "zeroable";

export const AutoInterface = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: AutoInterface): void {
        encodeAutoInterface(writer, value);
    },

    /** Decode one AutoInterface. */
    decode(reader: BinaryReader): AutoInterface {
        return decodeAutoInterface(reader);
    },

    /** Return this value as JSON. */
    toJson(value: AutoInterface): Json {
        return toJsonAutoInterface(value);
    },

    /** Return one AutoInterface from one JSON value. */
    fromJson(value: Json): AutoInterface {
        return fromJsonAutoInterface(value);
    },
};

/** Encode one AutoInterface. */
export function encodeAutoInterface(writer: BinaryWriter, value: AutoInterface): void {
    switch (value) {
        case "compare":
            writer.writeUnsigned(0);
            return;
        case "concrete":
            writer.writeUnsigned(1);
            return;
        case "copy":
            writer.writeUnsigned(2);
            return;
        case "clone":
            writer.writeUnsigned(3);
            return;
        case "debug":
            writer.writeUnsigned(4);
            return;
        case "default":
            writer.writeUnsigned(5);
            return;
        case "deserialize":
            writer.writeUnsigned(6);
            return;
        case "dynamicSafe":
            writer.writeUnsigned(7);
            return;
        case "equal":
            writer.writeUnsigned(8);
            return;
        case "float":
            writer.writeUnsigned(9);
            return;
        case "hash":
            writer.writeUnsigned(10);
            return;
        case "integer":
            writer.writeUnsigned(11);
            return;
        case "overwriteStable":
            writer.writeUnsigned(12);
            return;
        case "partialCompare":
            writer.writeUnsigned(13);
            return;
        case "partialEqual":
            writer.writeUnsigned(14);
            return;
        case "serialize":
            writer.writeUnsigned(15);
            return;
        case "sharedSafe":
            writer.writeUnsigned(16);
            return;
        case "unpin":
            writer.writeUnsigned(17);
            return;
        case "zeroable":
            writer.writeUnsigned(18);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one AutoInterface. */
export function decodeAutoInterface(reader: BinaryReader): AutoInterface {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "compare";
        case 1:
            return "concrete";
        case 2:
            return "copy";
        case 3:
            return "clone";
        case 4:
            return "debug";
        case 5:
            return "default";
        case 6:
            return "deserialize";
        case 7:
            return "dynamicSafe";
        case 8:
            return "equal";
        case 9:
            return "float";
        case 10:
            return "hash";
        case 11:
            return "integer";
        case 12:
            return "overwriteStable";
        case 13:
            return "partialCompare";
        case 14:
            return "partialEqual";
        case 15:
            return "serialize";
        case 16:
            return "sharedSafe";
        case 17:
            return "unpin";
        case 18:
            return "zeroable";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one AutoInterface. */
export function toJsonAutoInterface(value: AutoInterface): Json {
    return value;
}

/** Return one AutoInterface from one JSON value. */
export function fromJsonAutoInterface(value: Json): AutoInterface {
    const variant = jsonString(value);

    switch (variant) {
        case "compare":
            return "compare";
        case "concrete":
            return "concrete";
        case "copy":
            return "copy";
        case "clone":
            return "clone";
        case "debug":
            return "debug";
        case "default":
            return "default";
        case "deserialize":
            return "deserialize";
        case "dynamicSafe":
            return "dynamicSafe";
        case "equal":
            return "equal";
        case "float":
            return "float";
        case "hash":
            return "hash";
        case "integer":
            return "integer";
        case "overwriteStable":
            return "overwriteStable";
        case "partialCompare":
            return "partialCompare";
        case "partialEqual":
            return "partialEqual";
        case "serialize":
            return "serialize";
        case "sharedSafe":
            return "sharedSafe";
        case "unpin":
            return "unpin";
        case "zeroable":
            return "zeroable";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** Auto-derived implementations added by one DIR phase. */
export type AutoSegment = {
    /** The module id of the auto segment. */
    readonly moduleId: ModuleId;
    /** Auto-derived implementations in emission order. */
    readonly implementations: ReadonlyArray<AutoDerivedImplementation>;
    /** Checked marker conformances in emission order. */
    readonly conformances: ReadonlyArray<AutoConformance>;
};

export const AutoSegment = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: AutoSegment): void {
        encodeAutoSegment(writer, value);
    },

    /** Decode one AutoSegment. */
    decode(reader: BinaryReader): AutoSegment {
        return decodeAutoSegment(reader);
    },

    /** Return this value as JSON. */
    toJson(value: AutoSegment): Json {
        return toJsonAutoSegment(value);
    },

    /** Return one AutoSegment from one JSON value. */
    fromJson(value: Json): AutoSegment {
        return fromJsonAutoSegment(value);
    },
};

/** Encode one AutoSegment. */
export function encodeAutoSegment(writer: BinaryWriter, value: AutoSegment): void {
    encodeModuleId(writer, value.moduleId);
    writer.writeUnsigned(value.implementations.length);
    for (const item1 of value.implementations) {
        encodeAutoDerivedImplementation(writer, item1);
    }
    writer.writeUnsigned(value.conformances.length);
    for (const item2 of value.conformances) {
        encodeAutoConformance(writer, item2);
    }
}

/** Decode one AutoSegment. */
export function decodeAutoSegment(reader: BinaryReader): AutoSegment {
    const moduleId = decodeModuleId(reader);
    const implementations = (() => { const length1 = reader.readNumber(); const items1: Array<AutoDerivedImplementation> = []; for (let index = 0; index < length1; index += 1) { items1.push(decodeAutoDerivedImplementation(reader)); } return items1; })();
    const conformances = (() => { const length2 = reader.readNumber(); const items2: Array<AutoConformance> = []; for (let index = 0; index < length2; index += 1) { items2.push(decodeAutoConformance(reader)); } return items2; })();

    return {
        moduleId,
        implementations,
        conformances,
    };
}

/** Return one JSON value for one AutoSegment. */
export function toJsonAutoSegment(value: AutoSegment): Json {
    return {
        moduleId: toJsonModuleId(value.moduleId),
        implementations: value.implementations.map((item0) => toJsonAutoDerivedImplementation(item0)),
        conformances: value.conformances.map((item0) => toJsonAutoConformance(item0)),
    };
}

/** Return one AutoSegment from one JSON value. */
export function fromJsonAutoSegment(value: Json): AutoSegment {
    const object = jsonObject(value);

    return {
        moduleId: fromJsonModuleId(jsonField(object, "moduleId")),
        implementations: jsonArray(jsonField(object, "implementations")).map((item0) => fromJsonAutoDerivedImplementation(item0)),
        conformances: jsonArray(jsonField(object, "conformances")).map((item0) => fromJsonAutoConformance(item0)),
    };
}
