/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, compareBytes, jsonArray, jsonBool, jsonField, jsonObject, jsonString, nestedBytes } from "../../../protocol/serde.js";
import type { GlobalNodeIdAny } from "../tree/node.js";
import type { ModuleId } from "../../source/file/model/module.js";
import { decodeGlobalNodeIdAny, encodeGlobalNodeIdAny, fromJsonGlobalNodeIdAny, toJsonGlobalNodeIdAny } from "../tree/node.js";
import { decodeModuleId, encodeModuleId, fromJsonModuleId, toJsonModuleId } from "../../source/file/model/module.js";

/** Elaborated type guard checks. */
export type GuardTable = {
    /** The module id of the guard table. */
    readonly moduleId: ModuleId;
    /** Runtime guard entry by guard node. */
    readonly entryByNode: ReadonlyMap<GlobalNodeIdAny, GuardEntry>;
};

export const GuardTable = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: GuardTable): void {
        encodeGuardTable(writer, value);
    },

    /** Decode one GuardTable. */
    decode(reader: BinaryReader): GuardTable {
        return decodeGuardTable(reader);
    },

    /** Return this value as JSON. */
    toJson(value: GuardTable): Json {
        return toJsonGuardTable(value);
    },

    /** Return one GuardTable from one JSON value. */
    fromJson(value: Json): GuardTable {
        return fromJsonGuardTable(value);
    },
};

/** Encode one GuardTable. */
export function encodeGuardTable(writer: BinaryWriter, value: GuardTable): void {
    encodeModuleId(writer, value.moduleId);
    const entries1 = Array.from(value.entryByNode.entries()).map(([key1, item1]) => {
        const keyBytes = nestedBytes((writer) => {
            encodeGlobalNodeIdAny(writer, key1);
        });
        return { key1, item1, keyBytes };
    });
    entries1.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries1.length);
    for (const entry1 of entries1) {
        encodeGlobalNodeIdAny(writer, entry1.key1);
        encodeGuardEntry(writer, entry1.item1);
    }
}

/** Decode one GuardTable. */
export function decodeGuardTable(reader: BinaryReader): GuardTable {
    const moduleId = decodeModuleId(reader);
    const entryByNode = (() => { const length1 = reader.readNumber(); const items1 = new Map<GlobalNodeIdAny, GuardEntry>(); for (let index = 0; index < length1; index += 1) { items1.set(decodeGlobalNodeIdAny(reader), decodeGuardEntry(reader)); } return items1; })();

    return {
        moduleId,
        entryByNode,
    };
}

/** Return one JSON value for one GuardTable. */
export function toJsonGuardTable(value: GuardTable): Json {
    return {
        moduleId: toJsonModuleId(value.moduleId),
        entryByNode: Array.from(value.entryByNode.entries()).map(([key0, item0]) => [toJsonGlobalNodeIdAny(key0), toJsonGuardEntry(item0)] as const),
    };
}

/** Return one GuardTable from one JSON value. */
export function fromJsonGuardTable(value: Json): GuardTable {
    const object = jsonObject(value);

    return {
        moduleId: fromJsonModuleId(jsonField(object, "moduleId")),
        entryByNode: new Map(jsonArray(jsonField(object, "entryByNode")).map((entry) => { const items = jsonArray(entry); if (items.length !== 2) { throw new SerdeError(`expected JSON map entry length 2: ${items.length}`); } const key0 = items[0]; const item0 = items[1]; return [fromJsonGlobalNodeIdAny(key0), fromJsonGuardEntry(item0)] as const; })),
    };
}

/** Runtime check selected for one elaborated type guard. */
export type GuardEntry =
    /** The runtime check was reduced to a constant. */
    | {
          readonly kind: "constant";
          readonly constant: boolean;
      }
    /** The runtime check uses a union tag. */
    | {
          readonly kind: "unionTag";
      }
    /** The runtime check compares type identities. */
    | {
          readonly kind: "typeDescriptor";
      }
;

export const GuardEntry = {
    /** The runtime check was reduced to a constant. */
    constant(constant: boolean): GuardEntry {
        return { kind: "constant", constant };
    },

    /** The runtime check uses a union tag. */
    unionTag(): GuardEntry {
        return { kind: "unionTag" };
    },

    /** The runtime check compares type identities. */
    typeDescriptor(): GuardEntry {
        return { kind: "typeDescriptor" };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: GuardEntry): void {
        encodeGuardEntry(writer, value);
    },

    /** Decode one GuardEntry. */
    decode(reader: BinaryReader): GuardEntry {
        return decodeGuardEntry(reader);
    },

    /** Return this value as JSON. */
    toJson(value: GuardEntry): Json {
        return toJsonGuardEntry(value);
    },

    /** Return one GuardEntry from one JSON value. */
    fromJson(value: Json): GuardEntry {
        return fromJsonGuardEntry(value);
    },
};

/** Encode one GuardEntry. */
export function encodeGuardEntry(writer: BinaryWriter, value: GuardEntry): void {
    switch (value.kind) {
        case "constant":
            writer.writeUnsigned(0);
            writer.writeBool(value.constant);
            return;
        case "unionTag":
            writer.writeUnsigned(1);
            return;
        case "typeDescriptor":
            writer.writeUnsigned(2);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one GuardEntry. */
export function decodeGuardEntry(reader: BinaryReader): GuardEntry {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const constant = reader.readBool();

            return { kind: "constant", constant };
        }
        case 1: {
            return { kind: "unionTag" };
        }
        case 2: {
            return { kind: "typeDescriptor" };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one GuardEntry. */
export function toJsonGuardEntry(value: GuardEntry): Json {
    switch (value.kind) {
        case "constant":
            return {
                kind: "constant",
                constant: value.constant,
            };
        case "unionTag":
            return {
                kind: "unionTag",
            };
        case "typeDescriptor":
            return {
                kind: "typeDescriptor",
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one GuardEntry from one JSON value. */
export function fromJsonGuardEntry(value: Json): GuardEntry {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "constant":
            return {
                kind,
                constant: jsonBool(jsonField(object, "constant")),
            };
        case "unionTag":
            return {
                kind,
            };
        case "typeDescriptor":
            return {
                kind,
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}
