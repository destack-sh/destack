/* generated by destack_client_generate, do not edit */

import {
    BinaryReader,
    BinaryWriter,
    Json,
    SerdeError,
    compareBytes,
    jsonArray,
    jsonField,
    jsonObject,
    nestedBytes,
} from "../../../protocol/serde.js";
import type { ExportForm } from "../symbol/export.js";
import type { ExportKey } from "../symbol/export.js";
import type { NamedExport } from "../symbol/export.js";
import type { StarExport } from "../symbol/export.js";
import type { LocalSymbolId } from "../symbol/symbol.js";
import type { ModuleId } from "../../source/file/model/module.js";
import {
    decodeExportForm,
    encodeExportForm,
    fromJsonExportForm,
    toJsonExportForm,
} from "../symbol/export.js";
import {
    decodeExportKey,
    encodeExportKey,
    fromJsonExportKey,
    toJsonExportKey,
} from "../symbol/export.js";
import {
    decodeNamedExport,
    encodeNamedExport,
    fromJsonNamedExport,
    toJsonNamedExport,
} from "../symbol/export.js";
import {
    decodeStarExport,
    encodeStarExport,
    fromJsonStarExport,
    toJsonStarExport,
} from "../symbol/export.js";
import {
    decodeLocalSymbolId,
    encodeLocalSymbolId,
    fromJsonLocalSymbolId,
    toJsonLocalSymbolId,
} from "../symbol/symbol.js";
import {
    decodeModuleId,
    encodeModuleId,
    fromJsonModuleId,
    toJsonModuleId,
} from "../../source/file/model/module.js";

/** Resolved module exports for one module. */
export type ExportTable = {
    /** The module id of the export table. */
    readonly moduleId: ModuleId;
    /** Local and indirect exports keyed by exported name. */
    readonly exportByKey: ReadonlyMap<ExportKey, NamedExport>;
    /** Forms of local symbols visible across modules. */
    readonly formBySymbol: ReadonlyMap<LocalSymbolId, ExportForm>;
    /** Star exports declared by the module. */
    readonly starExports: ReadonlyArray<StarExport>;
};

export const ExportTable = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ExportTable): void {
        encodeExportTable(writer, value);
    },

    /** Decode one ExportTable. */
    decode(reader: BinaryReader): ExportTable {
        return decodeExportTable(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ExportTable): Json {
        return toJsonExportTable(value);
    },

    /** Return one ExportTable from one JSON value. */
    fromJson(value: Json): ExportTable {
        return fromJsonExportTable(value);
    },
};

/** Encode one ExportTable. */
export function encodeExportTable(writer: BinaryWriter, value: ExportTable): void {
    encodeModuleId(writer, value.moduleId);
    const entries1 = Array.from(value.exportByKey.entries()).map(([key1, item1]) => {
        const keyBytes = nestedBytes((writer) => {
            encodeExportKey(writer, key1);
        });
        return { key1, item1, keyBytes };
    });
    entries1.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries1.length);
    for (const entry1 of entries1) {
        encodeExportKey(writer, entry1.key1);
        encodeNamedExport(writer, entry1.item1);
    }
    const entries2 = Array.from(value.formBySymbol.entries()).map(([key2, item2]) => {
        const keyBytes = nestedBytes((writer) => {
            encodeLocalSymbolId(writer, key2);
        });
        return { key2, item2, keyBytes };
    });
    entries2.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries2.length);
    for (const entry2 of entries2) {
        encodeLocalSymbolId(writer, entry2.key2);
        encodeExportForm(writer, entry2.item2);
    }
    writer.writeUnsigned(value.starExports.length);
    for (const item3 of value.starExports) {
        encodeStarExport(writer, item3);
    }
}

/** Decode one ExportTable. */
export function decodeExportTable(reader: BinaryReader): ExportTable {
    const moduleId = decodeModuleId(reader);
    const exportByKey = (() => {
        const length1 = reader.readNumber();
        const items1 = new Map<ExportKey, NamedExport>();
        for (let index = 0; index < length1; index += 1) {
            items1.set(decodeExportKey(reader), decodeNamedExport(reader));
        }
        return items1;
    })();
    const formBySymbol = (() => {
        const length2 = reader.readNumber();
        const items2 = new Map<LocalSymbolId, ExportForm>();
        for (let index = 0; index < length2; index += 1) {
            items2.set(decodeLocalSymbolId(reader), decodeExportForm(reader));
        }
        return items2;
    })();
    const starExports = (() => {
        const length3 = reader.readNumber();
        const items3: Array<StarExport> = [];
        for (let index = 0; index < length3; index += 1) {
            items3.push(decodeStarExport(reader));
        }
        return items3;
    })();

    return {
        moduleId,
        exportByKey,
        formBySymbol,
        starExports,
    };
}

/** Return one JSON value for one ExportTable. */
export function toJsonExportTable(value: ExportTable): Json {
    return {
        moduleId: toJsonModuleId(value.moduleId),
        exportByKey: Array.from(value.exportByKey.entries()).map(
            ([key0, item0]) => [toJsonExportKey(key0), toJsonNamedExport(item0)] as const,
        ),
        formBySymbol: Array.from(value.formBySymbol.entries()).map(
            ([key0, item0]) => [toJsonLocalSymbolId(key0), toJsonExportForm(item0)] as const,
        ),
        starExports: value.starExports.map((item0) => toJsonStarExport(item0)),
    };
}

/** Return one ExportTable from one JSON value. */
export function fromJsonExportTable(value: Json): ExportTable {
    const object = jsonObject(value);

    return {
        moduleId: fromJsonModuleId(jsonField(object, "moduleId")),
        exportByKey: new Map(
            jsonArray(jsonField(object, "exportByKey")).map((entry) => {
                const items = jsonArray(entry);
                if (items.length !== 2) {
                    throw new SerdeError(`expected JSON map entry length 2: ${items.length}`);
                }
                const key0 = items[0];
                const item0 = items[1];
                return [fromJsonExportKey(key0), fromJsonNamedExport(item0)] as const;
            }),
        ),
        formBySymbol: new Map(
            jsonArray(jsonField(object, "formBySymbol")).map((entry) => {
                const items = jsonArray(entry);
                if (items.length !== 2) {
                    throw new SerdeError(`expected JSON map entry length 2: ${items.length}`);
                }
                const key0 = items[0];
                const item0 = items[1];
                return [fromJsonLocalSymbolId(key0), fromJsonExportForm(item0)] as const;
            }),
        ),
        starExports: jsonArray(jsonField(object, "starExports")).map((item0) =>
            fromJsonStarExport(item0),
        ),
    };
}
