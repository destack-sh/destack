/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, compareBytes, jsonArray, jsonField, jsonInteger, jsonObject, jsonString, nestedBytes } from "../../../protocol/serde.js";
import type { CastOrigin } from "../tree/cast.js";
import type { GlobalNodeIdAny } from "../tree/node.js";
import type { GenericArgumentBinding } from "../type/generic.js";
import type { GlobalTypeId } from "../type/type.js";
import type { ModuleId } from "../../source/file/model/module.js";
import { decodeCastOrigin, encodeCastOrigin, fromJsonCastOrigin, toJsonCastOrigin } from "../tree/cast.js";
import { decodeGlobalNodeIdAny, encodeGlobalNodeIdAny, fromJsonGlobalNodeIdAny, toJsonGlobalNodeIdAny } from "../tree/node.js";
import { decodeGenericArgumentBinding, encodeGenericArgumentBinding, fromJsonGenericArgumentBinding, toJsonGenericArgumentBinding } from "../type/generic.js";
import { decodeGlobalTypeId, encodeGlobalTypeId, fromJsonGlobalTypeId, toJsonGlobalTypeId } from "../type/type.js";
import { decodeModuleId, encodeModuleId, fromJsonModuleId, toJsonModuleId } from "../../source/file/model/module.js";

/** One checked coercion from a source type to a target type. */
export type Coercion = {
    /** The source type before coercion. */
    readonly source: GlobalTypeId;
    /** The ordered adjustments applied to the source value. */
    readonly adjustments: ReadonlyArray<CoercionAdjustment>;
    /** How the coercion entered DIR. */
    readonly origin: CastOrigin;
};

export const Coercion = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: Coercion): void {
        encodeCoercion(writer, value);
    },

    /** Decode one Coercion. */
    decode(reader: BinaryReader): Coercion {
        return decodeCoercion(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Coercion): Json {
        return toJsonCoercion(value);
    },

    /** Return one Coercion from one JSON value. */
    fromJson(value: Json): Coercion {
        return fromJsonCoercion(value);
    },
};

/** Encode one Coercion. */
export function encodeCoercion(writer: BinaryWriter, value: Coercion): void {
    encodeGlobalTypeId(writer, value.source);
    writer.writeUnsigned(value.adjustments.length);
    for (const item1 of value.adjustments) {
        encodeCoercionAdjustment(writer, item1);
    }
    encodeCastOrigin(writer, value.origin);
}

/** Decode one Coercion. */
export function decodeCoercion(reader: BinaryReader): Coercion {
    const source = decodeGlobalTypeId(reader);
    const adjustments = (() => { const length1 = reader.readNumber(); const items1: Array<CoercionAdjustment> = []; for (let index = 0; index < length1; index += 1) { items1.push(decodeCoercionAdjustment(reader)); } return items1; })();
    const origin = decodeCastOrigin(reader);

    return {
        source,
        adjustments,
        origin,
    };
}

/** Return one JSON value for one Coercion. */
export function toJsonCoercion(value: Coercion): Json {
    return {
        source: toJsonGlobalTypeId(value.source),
        adjustments: value.adjustments.map((item0) => toJsonCoercionAdjustment(item0)),
        origin: toJsonCastOrigin(value.origin),
    };
}

/** Return one Coercion from one JSON value. */
export function fromJsonCoercion(value: Json): Coercion {
    const object = jsonObject(value);

    return {
        source: fromJsonGlobalTypeId(jsonField(object, "source")),
        adjustments: jsonArray(jsonField(object, "adjustments")).map((item0) => fromJsonCoercionAdjustment(item0)),
        origin: fromJsonCastOrigin(jsonField(object, "origin")),
    };
}

/** One adjustment in a checked coercion path. */
export type CoercionAdjustment =
    /** Borrow one value with the target lifetime and access. */
    | {
          readonly kind: "borrow";
          /** The borrowed type. */
          readonly target: GlobalTypeId;
      }
    /** Read one copyable value through a reference. */
    | {
          readonly kind: "read";
          /** The value type after the read. */
          readonly target: GlobalTypeId;
      }
    /** Convert a value whose source or target is a union. */
    | {
          readonly kind: "union";
          /** The type after this adjustment. */
          readonly target: GlobalTypeId;
          /** The selected conversion for each possible source type. */
          readonly cases: ReadonlyArray<CoercionCase>;
      }
    /** Erase the value behind its constraint representation, like an interface or `unknown`. */
    | {
          readonly kind: "erase";
          /** The erased representation type after this adjustment. */
          readonly target: GlobalTypeId;
      }
    /** Convert between scalar representations, like `int32` into `float64`. */
    | {
          readonly kind: "scalar";
          /** The scalar type after this adjustment. */
          readonly target: GlobalTypeId;
      }
    /** Materialize one const scalar at its selected representation, like `42` into `int32`. */
    | {
          readonly kind: "widen";
          /** The scalar type after this adjustment. */
          readonly target: GlobalTypeId;
      }
    /** Convert one tuple value into another tuple type. */
    | {
          readonly kind: "tuple";
          /** The tuple type after this adjustment. */
          readonly target: GlobalTypeId;
      }
    /** Transfer one owned value into managed storage, like `^T` into `T`. */
    | {
          readonly kind: "manage";
          /** The managed type after this adjustment. */
          readonly target: GlobalTypeId;
      }
    /** Change the value representation, like `^T` into `&T` or `T[]` into `[T]`. */
    | {
          readonly kind: "representation";
          /** The representation type after this adjustment. */
          readonly target: GlobalTypeId;
      }
    /** Materialize one generic callable reference at its selected concrete instance. */
    | {
          readonly kind: "instantiate";
          /** The concrete callable type after this adjustment. */
          readonly target: GlobalTypeId;
          /** The generic arguments selecting the instance. */
          readonly arguments: ReadonlyArray<GenericArgumentBinding>;
      }
;

export const CoercionAdjustment = {
    /** Borrow one value with the target lifetime and access. */
    borrow(target: GlobalTypeId): CoercionAdjustment {
        return { kind: "borrow", target };
    },

    /** Read one copyable value through a reference. */
    read(target: GlobalTypeId): CoercionAdjustment {
        return { kind: "read", target };
    },

    /** Convert a value whose source or target is a union. */
    union(target: GlobalTypeId, cases: ReadonlyArray<CoercionCase>): CoercionAdjustment {
        return { kind: "union", target, cases };
    },

    /** Erase the value behind its constraint representation, like an interface or `unknown`. */
    erase(target: GlobalTypeId): CoercionAdjustment {
        return { kind: "erase", target };
    },

    /** Convert between scalar representations, like `int32` into `float64`. */
    scalar(target: GlobalTypeId): CoercionAdjustment {
        return { kind: "scalar", target };
    },

    /** Materialize one const scalar at its selected representation, like `42` into `int32`. */
    widen(target: GlobalTypeId): CoercionAdjustment {
        return { kind: "widen", target };
    },

    /** Convert one tuple value into another tuple type. */
    tuple(target: GlobalTypeId): CoercionAdjustment {
        return { kind: "tuple", target };
    },

    /** Transfer one owned value into managed storage, like `^T` into `T`. */
    manage(target: GlobalTypeId): CoercionAdjustment {
        return { kind: "manage", target };
    },

    /** Change the value representation, like `^T` into `&T` or `T[]` into `[T]`. */
    representation(target: GlobalTypeId): CoercionAdjustment {
        return { kind: "representation", target };
    },

    /** Materialize one generic callable reference at its selected concrete instance. */
    instantiate(target: GlobalTypeId, arguments_: ReadonlyArray<GenericArgumentBinding>): CoercionAdjustment {
        return { kind: "instantiate", target, arguments: arguments_ };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: CoercionAdjustment): void {
        encodeCoercionAdjustment(writer, value);
    },

    /** Decode one CoercionAdjustment. */
    decode(reader: BinaryReader): CoercionAdjustment {
        return decodeCoercionAdjustment(reader);
    },

    /** Return this value as JSON. */
    toJson(value: CoercionAdjustment): Json {
        return toJsonCoercionAdjustment(value);
    },

    /** Return one CoercionAdjustment from one JSON value. */
    fromJson(value: Json): CoercionAdjustment {
        return fromJsonCoercionAdjustment(value);
    },
};

/** Encode one CoercionAdjustment. */
export function encodeCoercionAdjustment(writer: BinaryWriter, value: CoercionAdjustment): void {
    switch (value.kind) {
        case "borrow":
            writer.writeUnsigned(0);
            encodeGlobalTypeId(writer, value.target);
            return;
        case "read":
            writer.writeUnsigned(1);
            encodeGlobalTypeId(writer, value.target);
            return;
        case "union":
            writer.writeUnsigned(2);
            encodeGlobalTypeId(writer, value.target);
            writer.writeUnsigned(value.cases.length);
            for (const item1 of value.cases) {
                encodeCoercionCase(writer, item1);
            }
            return;
        case "erase":
            writer.writeUnsigned(3);
            encodeGlobalTypeId(writer, value.target);
            return;
        case "scalar":
            writer.writeUnsigned(4);
            encodeGlobalTypeId(writer, value.target);
            return;
        case "widen":
            writer.writeUnsigned(5);
            encodeGlobalTypeId(writer, value.target);
            return;
        case "tuple":
            writer.writeUnsigned(6);
            encodeGlobalTypeId(writer, value.target);
            return;
        case "manage":
            writer.writeUnsigned(7);
            encodeGlobalTypeId(writer, value.target);
            return;
        case "representation":
            writer.writeUnsigned(8);
            encodeGlobalTypeId(writer, value.target);
            return;
        case "instantiate":
            writer.writeUnsigned(9);
            encodeGlobalTypeId(writer, value.target);
            writer.writeUnsigned(value.arguments.length);
            for (const item1 of value.arguments) {
                encodeGenericArgumentBinding(writer, item1);
            }
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one CoercionAdjustment. */
export function decodeCoercionAdjustment(reader: BinaryReader): CoercionAdjustment {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const target = decodeGlobalTypeId(reader);

            return {
                kind: "borrow",
                target,
            };
        }
        case 1: {
            const target = decodeGlobalTypeId(reader);

            return {
                kind: "read",
                target,
            };
        }
        case 2: {
            const target = decodeGlobalTypeId(reader);
            const cases = (() => { const length1 = reader.readNumber(); const items1: Array<CoercionCase> = []; for (let index = 0; index < length1; index += 1) { items1.push(decodeCoercionCase(reader)); } return items1; })();

            return {
                kind: "union",
                target,
                cases,
            };
        }
        case 3: {
            const target = decodeGlobalTypeId(reader);

            return {
                kind: "erase",
                target,
            };
        }
        case 4: {
            const target = decodeGlobalTypeId(reader);

            return {
                kind: "scalar",
                target,
            };
        }
        case 5: {
            const target = decodeGlobalTypeId(reader);

            return {
                kind: "widen",
                target,
            };
        }
        case 6: {
            const target = decodeGlobalTypeId(reader);

            return {
                kind: "tuple",
                target,
            };
        }
        case 7: {
            const target = decodeGlobalTypeId(reader);

            return {
                kind: "manage",
                target,
            };
        }
        case 8: {
            const target = decodeGlobalTypeId(reader);

            return {
                kind: "representation",
                target,
            };
        }
        case 9: {
            const target = decodeGlobalTypeId(reader);
            const arguments_ = (() => { const length1 = reader.readNumber(); const items1: Array<GenericArgumentBinding> = []; for (let index = 0; index < length1; index += 1) { items1.push(decodeGenericArgumentBinding(reader)); } return items1; })();

            return {
                kind: "instantiate",
                target,
                arguments: arguments_,
            };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one CoercionAdjustment. */
export function toJsonCoercionAdjustment(value: CoercionAdjustment): Json {
    switch (value.kind) {
        case "borrow":
            return {
                kind: "borrow",
                target: toJsonGlobalTypeId(value.target),
            };
        case "read":
            return {
                kind: "read",
                target: toJsonGlobalTypeId(value.target),
            };
        case "union":
            return {
                kind: "union",
                target: toJsonGlobalTypeId(value.target),
                cases: value.cases.map((item0) => toJsonCoercionCase(item0)),
            };
        case "erase":
            return {
                kind: "erase",
                target: toJsonGlobalTypeId(value.target),
            };
        case "scalar":
            return {
                kind: "scalar",
                target: toJsonGlobalTypeId(value.target),
            };
        case "widen":
            return {
                kind: "widen",
                target: toJsonGlobalTypeId(value.target),
            };
        case "tuple":
            return {
                kind: "tuple",
                target: toJsonGlobalTypeId(value.target),
            };
        case "manage":
            return {
                kind: "manage",
                target: toJsonGlobalTypeId(value.target),
            };
        case "representation":
            return {
                kind: "representation",
                target: toJsonGlobalTypeId(value.target),
            };
        case "instantiate":
            return {
                kind: "instantiate",
                target: toJsonGlobalTypeId(value.target),
                arguments: value.arguments.map((item0) => toJsonGenericArgumentBinding(item0)),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one CoercionAdjustment from one JSON value. */
export function fromJsonCoercionAdjustment(value: Json): CoercionAdjustment {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "borrow":
            return {
                kind,
                target: fromJsonGlobalTypeId(jsonField(object, "target")),
            };
        case "read":
            return {
                kind,
                target: fromJsonGlobalTypeId(jsonField(object, "target")),
            };
        case "union":
            return {
                kind,
                target: fromJsonGlobalTypeId(jsonField(object, "target")),
                cases: jsonArray(jsonField(object, "cases")).map((item0) => fromJsonCoercionCase(item0)),
            };
        case "erase":
            return {
                kind,
                target: fromJsonGlobalTypeId(jsonField(object, "target")),
            };
        case "scalar":
            return {
                kind,
                target: fromJsonGlobalTypeId(jsonField(object, "target")),
            };
        case "widen":
            return {
                kind,
                target: fromJsonGlobalTypeId(jsonField(object, "target")),
            };
        case "tuple":
            return {
                kind,
                target: fromJsonGlobalTypeId(jsonField(object, "target")),
            };
        case "manage":
            return {
                kind,
                target: fromJsonGlobalTypeId(jsonField(object, "target")),
            };
        case "representation":
            return {
                kind,
                target: fromJsonGlobalTypeId(jsonField(object, "target")),
            };
        case "instantiate":
            return {
                kind,
                target: fromJsonGlobalTypeId(jsonField(object, "target")),
                arguments: jsonArray(jsonField(object, "arguments")).map((item0) => fromJsonGenericArgumentBinding(item0)),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** One selected conversion for a possible union source type. */
export type CoercionCase = {
    /** The source type entering this case. */
    readonly source: GlobalTypeId;
    /** The selected target type. */
    readonly target: GlobalTypeId;
    /** The selected arm position in the declared target union. */
    readonly index: number;
    /** The ordered adjustments converting the source to the target. */
    readonly adjustments: ReadonlyArray<CoercionAdjustment>;
};

export const CoercionCase = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: CoercionCase): void {
        encodeCoercionCase(writer, value);
    },

    /** Decode one CoercionCase. */
    decode(reader: BinaryReader): CoercionCase {
        return decodeCoercionCase(reader);
    },

    /** Return this value as JSON. */
    toJson(value: CoercionCase): Json {
        return toJsonCoercionCase(value);
    },

    /** Return one CoercionCase from one JSON value. */
    fromJson(value: Json): CoercionCase {
        return fromJsonCoercionCase(value);
    },
};

/** Encode one CoercionCase. */
export function encodeCoercionCase(writer: BinaryWriter, value: CoercionCase): void {
    encodeGlobalTypeId(writer, value.source);
    encodeGlobalTypeId(writer, value.target);
    writer.writeUnsigned(value.index);
    writer.writeUnsigned(value.adjustments.length);
    for (const item3 of value.adjustments) {
        encodeCoercionAdjustment(writer, item3);
    }
}

/** Decode one CoercionCase. */
export function decodeCoercionCase(reader: BinaryReader): CoercionCase {
    const source = decodeGlobalTypeId(reader);
    const target = decodeGlobalTypeId(reader);
    const index = reader.readNumber();
    const adjustments = (() => { const length3 = reader.readNumber(); const items3: Array<CoercionAdjustment> = []; for (let index = 0; index < length3; index += 1) { items3.push(decodeCoercionAdjustment(reader)); } return items3; })();

    return {
        source,
        target,
        index,
        adjustments,
    };
}

/** Return one JSON value for one CoercionCase. */
export function toJsonCoercionCase(value: CoercionCase): Json {
    return {
        source: toJsonGlobalTypeId(value.source),
        target: toJsonGlobalTypeId(value.target),
        index: value.index,
        adjustments: value.adjustments.map((item0) => toJsonCoercionAdjustment(item0)),
    };
}

/** Return one CoercionCase from one JSON value. */
export function fromJsonCoercionCase(value: Json): CoercionCase {
    const object = jsonObject(value);

    return {
        source: fromJsonGlobalTypeId(jsonField(object, "source")),
        target: fromJsonGlobalTypeId(jsonField(object, "target")),
        index: jsonInteger(jsonField(object, "index")),
        adjustments: jsonArray(jsonField(object, "adjustments")).map((item0) => fromJsonCoercionAdjustment(item0)),
    };
}

/** Coercions added by one DIR phase. */
export type CoercionSegment = {
    /** The module id of the coercion segment. */
    readonly moduleId: ModuleId;
    /** Coercions keyed by their value node. */
    readonly coercions: ReadonlyMap<GlobalNodeIdAny, Coercion>;
};

export const CoercionSegment = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: CoercionSegment): void {
        encodeCoercionSegment(writer, value);
    },

    /** Decode one CoercionSegment. */
    decode(reader: BinaryReader): CoercionSegment {
        return decodeCoercionSegment(reader);
    },

    /** Return this value as JSON. */
    toJson(value: CoercionSegment): Json {
        return toJsonCoercionSegment(value);
    },

    /** Return one CoercionSegment from one JSON value. */
    fromJson(value: Json): CoercionSegment {
        return fromJsonCoercionSegment(value);
    },
};

/** Encode one CoercionSegment. */
export function encodeCoercionSegment(writer: BinaryWriter, value: CoercionSegment): void {
    encodeModuleId(writer, value.moduleId);
    const entries1 = Array.from(value.coercions.entries()).map(([key1, item1]) => {
        const keyBytes = nestedBytes((writer) => {
            encodeGlobalNodeIdAny(writer, key1);
        });
        return { key1, item1, keyBytes };
    });
    entries1.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries1.length);
    for (const entry1 of entries1) {
        encodeGlobalNodeIdAny(writer, entry1.key1);
        encodeCoercion(writer, entry1.item1);
    }
}

/** Decode one CoercionSegment. */
export function decodeCoercionSegment(reader: BinaryReader): CoercionSegment {
    const moduleId = decodeModuleId(reader);
    const coercions = (() => { const length1 = reader.readNumber(); const items1 = new Map<GlobalNodeIdAny, Coercion>(); for (let index = 0; index < length1; index += 1) { items1.set(decodeGlobalNodeIdAny(reader), decodeCoercion(reader)); } return items1; })();

    return {
        moduleId,
        coercions,
    };
}

/** Return one JSON value for one CoercionSegment. */
export function toJsonCoercionSegment(value: CoercionSegment): Json {
    return {
        moduleId: toJsonModuleId(value.moduleId),
        coercions: Array.from(value.coercions.entries()).map(([key0, item0]) => [toJsonGlobalNodeIdAny(key0), toJsonCoercion(item0)] as const),
    };
}

/** Return one CoercionSegment from one JSON value. */
export function fromJsonCoercionSegment(value: Json): CoercionSegment {
    const object = jsonObject(value);

    return {
        moduleId: fromJsonModuleId(jsonField(object, "moduleId")),
        coercions: new Map(jsonArray(jsonField(object, "coercions")).map((entry) => { const items = jsonArray(entry); if (items.length !== 2) { throw new SerdeError(`expected JSON map entry length 2: ${items.length}`); } const key0 = items[0]; const item0 = items[1]; return [fromJsonGlobalNodeIdAny(key0), fromJsonCoercion(item0)] as const; })),
    };
}
