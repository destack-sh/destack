/* generated by destack_client_generate, do not edit */

import {
    BinaryReader,
    BinaryWriter,
    Json,
    SerdeError,
    compareBytes,
    jsonArray,
    jsonField,
    jsonInteger,
    jsonObject,
    jsonOptional,
    nestedBytes,
} from "../../../protocol/serde.js";
import type { GlobalSymbolId } from "../symbol/symbol.js";
import type { LocalSymbolId } from "../symbol/symbol.js";
import type { VarianceModifier } from "../tree/property.js";
import type { Cardinality } from "../type/generic.js";
import type { Instance } from "../type/generic.js";
import type { Instantiation } from "../type/generic.js";
import type { LocalGenericParameterId } from "../type/generic.js";
import type { LocalGenericTemplateId } from "../type/generic.js";
import type { LocalInstanceId } from "../type/generic.js";
import type { GenericParameterBinding } from "../type/generic.js";
import type { GenericTemplate } from "../type/generic.js";
import type { Witness } from "../type/generic.js";
import type { InstanceKey } from "../type/key.js";
import type { GlobalTypeId } from "../type/type.js";
import type { TypeListId } from "../type/type.js";
import type { ModuleId } from "../../source/file/model/module.js";
import {
    decodeGlobalSymbolId,
    encodeGlobalSymbolId,
    fromJsonGlobalSymbolId,
    toJsonGlobalSymbolId,
} from "../symbol/symbol.js";
import {
    decodeLocalSymbolId,
    encodeLocalSymbolId,
    fromJsonLocalSymbolId,
    toJsonLocalSymbolId,
} from "../symbol/symbol.js";
import {
    decodeVarianceModifier,
    encodeVarianceModifier,
    fromJsonVarianceModifier,
    toJsonVarianceModifier,
} from "../tree/property.js";
import {
    decodeCardinality,
    encodeCardinality,
    fromJsonCardinality,
    toJsonCardinality,
} from "../type/generic.js";
import {
    decodeInstance,
    encodeInstance,
    fromJsonInstance,
    toJsonInstance,
} from "../type/generic.js";
import {
    decodeInstantiation,
    encodeInstantiation,
    fromJsonInstantiation,
    toJsonInstantiation,
} from "../type/generic.js";
import {
    decodeLocalGenericParameterId,
    encodeLocalGenericParameterId,
    fromJsonLocalGenericParameterId,
    toJsonLocalGenericParameterId,
} from "../type/generic.js";
import {
    decodeLocalGenericTemplateId,
    encodeLocalGenericTemplateId,
    fromJsonLocalGenericTemplateId,
    toJsonLocalGenericTemplateId,
} from "../type/generic.js";
import {
    decodeLocalInstanceId,
    encodeLocalInstanceId,
    fromJsonLocalInstanceId,
    toJsonLocalInstanceId,
} from "../type/generic.js";
import {
    decodeGenericParameterBinding,
    encodeGenericParameterBinding,
    fromJsonGenericParameterBinding,
    toJsonGenericParameterBinding,
} from "../type/generic.js";
import {
    decodeGenericTemplate,
    encodeGenericTemplate,
    fromJsonGenericTemplate,
    toJsonGenericTemplate,
} from "../type/generic.js";
import { decodeWitness, encodeWitness, fromJsonWitness, toJsonWitness } from "../type/generic.js";
import {
    decodeInstanceKey,
    encodeInstanceKey,
    fromJsonInstanceKey,
    toJsonInstanceKey,
} from "../type/key.js";
import {
    decodeGlobalTypeId,
    encodeGlobalTypeId,
    fromJsonGlobalTypeId,
    toJsonGlobalTypeId,
} from "../type/type.js";
import {
    decodeTypeListId,
    encodeTypeListId,
    fromJsonTypeListId,
    toJsonTypeListId,
} from "../type/type.js";
import {
    decodeModuleId,
    encodeModuleId,
    fromJsonModuleId,
    toJsonModuleId,
} from "../../source/file/model/module.js";

/** Generic templates and parameters added by one DIR phase. */
export type GenericSegment = {
    /** The module id of the generic segment. */
    readonly moduleId: ModuleId;
    /** The first generic template id owned by this table segment. */
    readonly firstTemplateId: number;
    /** The first generic parameter id owned by this table segment. */
    readonly firstParameterId: number;
    /** Generic templates. */
    readonly templates: ReadonlyArray<GenericTemplate>;
    /** Generic template ids keyed by lexical scope id. */
    readonly templatesByScope: ReadonlyArray<LocalGenericTemplateId | undefined>;
    /** Generic parameters. */
    readonly parameters: ReadonlyArray<GenericParameterBinding>;
    /** Variances derived from declared member types. */
    readonly variances: ReadonlyArray<readonly [LocalGenericParameterId, VarianceModifier]>;
    /** Cardinalities derived from declared value positions. */
    readonly cardinalities: ReadonlyArray<readonly [LocalGenericParameterId, Cardinality]>;
    /** The first generic instance id owned by this table segment. */
    readonly firstInstanceId: number;
    /** Generic instances closed by this segment. */
    readonly instances: ReadonlyArray<Instance>;
    /** Instantiations the checked bodies perform, open while they mention parameters. */
    readonly instantiations: ReadonlyArray<Instantiation>;
    /** The interned instance behind each closed application type. */
    readonly applicationInstances: ReadonlyMap<GlobalTypeId, LocalInstanceId>;
    /** The dependents each declaration's signature writes, in signature order. */
    readonly symbolDependents: ReadonlyMap<LocalSymbolId, ReadonlyArray<GlobalTypeId>>;
    /** The allocated instance behind each selection a checked decision wrote. */
    readonly selectionInstances: ReadonlyMap<InstanceKey, LocalInstanceId>;
    /** The witness each closed type answers each interface application with. */
    readonly witnesses: ReadonlyMap<readonly [GlobalTypeId, GlobalTypeId], Witness>;
    /** The bounds each parameter assumes with elided arguments filled. */
    readonly parameterBounds: ReadonlyMap<LocalGenericParameterId, TypeListId>;
    /** The filled where-clause bounds each template assumes for a parameter. */
    readonly assumedBounds: ReadonlyMap<
        readonly [LocalGenericTemplateId, LocalGenericParameterId],
        TypeListId
    >;
    /** Materialized symbol types keyed by instance and symbol. */
    readonly instanceSymbols: ReadonlyMap<readonly [LocalInstanceId, GlobalSymbolId], GlobalTypeId>;
};

export const GenericSegment = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: GenericSegment): void {
        encodeGenericSegment(writer, value);
    },

    /** Decode one GenericSegment. */
    decode(reader: BinaryReader): GenericSegment {
        return decodeGenericSegment(reader);
    },

    /** Return this value as JSON. */
    toJson(value: GenericSegment): Json {
        return toJsonGenericSegment(value);
    },

    /** Return one GenericSegment from one JSON value. */
    fromJson(value: Json): GenericSegment {
        return fromJsonGenericSegment(value);
    },
};

/** Encode one GenericSegment. */
export function encodeGenericSegment(writer: BinaryWriter, value: GenericSegment): void {
    encodeModuleId(writer, value.moduleId);
    writer.writeUnsigned(value.firstTemplateId);
    writer.writeUnsigned(value.firstParameterId);
    writer.writeUnsigned(value.templates.length);
    for (const item3 of value.templates) {
        encodeGenericTemplate(writer, item3);
    }
    writer.writeUnsigned(value.templatesByScope.length);
    for (const item4 of value.templatesByScope) {
        writer.writeOption(item4, (value5) => {
            encodeLocalGenericTemplateId(writer, value5);
        });
    }
    writer.writeUnsigned(value.parameters.length);
    for (const item5 of value.parameters) {
        encodeGenericParameterBinding(writer, item5);
    }
    writer.writeUnsigned(value.variances.length);
    for (const item6 of value.variances) {
        encodeLocalGenericParameterId(writer, item6[0]);
        encodeVarianceModifier(writer, item6[1]);
    }
    writer.writeUnsigned(value.cardinalities.length);
    for (const item7 of value.cardinalities) {
        encodeLocalGenericParameterId(writer, item7[0]);
        encodeCardinality(writer, item7[1]);
    }
    writer.writeUnsigned(value.firstInstanceId);
    writer.writeUnsigned(value.instances.length);
    for (const item9 of value.instances) {
        encodeInstance(writer, item9);
    }
    writer.writeUnsigned(value.instantiations.length);
    for (const item10 of value.instantiations) {
        encodeInstantiation(writer, item10);
    }
    const entries11 = Array.from(value.applicationInstances.entries()).map(([key11, item11]) => {
        const keyBytes = nestedBytes((writer) => {
            encodeGlobalTypeId(writer, key11);
        });
        return { key11, item11, keyBytes };
    });
    entries11.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries11.length);
    for (const entry11 of entries11) {
        encodeGlobalTypeId(writer, entry11.key11);
        encodeLocalInstanceId(writer, entry11.item11);
    }
    const entries12 = Array.from(value.symbolDependents.entries()).map(([key12, item12]) => {
        const keyBytes = nestedBytes((writer) => {
            encodeLocalSymbolId(writer, key12);
        });
        return { key12, item12, keyBytes };
    });
    entries12.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries12.length);
    for (const entry12 of entries12) {
        encodeLocalSymbolId(writer, entry12.key12);
        writer.writeUnsigned(entry12.item12.length);
        for (const item13 of entry12.item12) {
            encodeGlobalTypeId(writer, item13);
        }
    }
    const entries13 = Array.from(value.selectionInstances.entries()).map(([key13, item13]) => {
        const keyBytes = nestedBytes((writer) => {
            encodeInstanceKey(writer, key13);
        });
        return { key13, item13, keyBytes };
    });
    entries13.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries13.length);
    for (const entry13 of entries13) {
        encodeInstanceKey(writer, entry13.key13);
        encodeLocalInstanceId(writer, entry13.item13);
    }
    const entries14 = Array.from(value.witnesses.entries()).map(([key14, item14]) => {
        const keyBytes = nestedBytes((writer) => {
            encodeGlobalTypeId(writer, key14[0]);
            encodeGlobalTypeId(writer, key14[1]);
        });
        return { key14, item14, keyBytes };
    });
    entries14.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries14.length);
    for (const entry14 of entries14) {
        encodeGlobalTypeId(writer, entry14.key14[0]);
        encodeGlobalTypeId(writer, entry14.key14[1]);
        encodeWitness(writer, entry14.item14);
    }
    const entries15 = Array.from(value.parameterBounds.entries()).map(([key15, item15]) => {
        const keyBytes = nestedBytes((writer) => {
            encodeLocalGenericParameterId(writer, key15);
        });
        return { key15, item15, keyBytes };
    });
    entries15.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries15.length);
    for (const entry15 of entries15) {
        encodeLocalGenericParameterId(writer, entry15.key15);
        encodeTypeListId(writer, entry15.item15);
    }
    const entries16 = Array.from(value.assumedBounds.entries()).map(([key16, item16]) => {
        const keyBytes = nestedBytes((writer) => {
            encodeLocalGenericTemplateId(writer, key16[0]);
            encodeLocalGenericParameterId(writer, key16[1]);
        });
        return { key16, item16, keyBytes };
    });
    entries16.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries16.length);
    for (const entry16 of entries16) {
        encodeLocalGenericTemplateId(writer, entry16.key16[0]);
        encodeLocalGenericParameterId(writer, entry16.key16[1]);
        encodeTypeListId(writer, entry16.item16);
    }
    const entries17 = Array.from(value.instanceSymbols.entries()).map(([key17, item17]) => {
        const keyBytes = nestedBytes((writer) => {
            encodeLocalInstanceId(writer, key17[0]);
            encodeGlobalSymbolId(writer, key17[1]);
        });
        return { key17, item17, keyBytes };
    });
    entries17.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries17.length);
    for (const entry17 of entries17) {
        encodeLocalInstanceId(writer, entry17.key17[0]);
        encodeGlobalSymbolId(writer, entry17.key17[1]);
        encodeGlobalTypeId(writer, entry17.item17);
    }
}

/** Decode one GenericSegment. */
export function decodeGenericSegment(reader: BinaryReader): GenericSegment {
    const moduleId = decodeModuleId(reader);
    const firstTemplateId = reader.readNumber();
    const firstParameterId = reader.readNumber();
    const templates = (() => {
        const length3 = reader.readNumber();
        const items3: Array<GenericTemplate> = [];
        for (let index = 0; index < length3; index += 1) {
            items3.push(decodeGenericTemplate(reader));
        }
        return items3;
    })();
    const templatesByScope = (() => {
        const length4 = reader.readNumber();
        const items4: Array<LocalGenericTemplateId | undefined> = [];
        for (let index = 0; index < length4; index += 1) {
            items4.push(reader.readOption(() => decodeLocalGenericTemplateId(reader)));
        }
        return items4;
    })();
    const parameters = (() => {
        const length5 = reader.readNumber();
        const items5: Array<GenericParameterBinding> = [];
        for (let index = 0; index < length5; index += 1) {
            items5.push(decodeGenericParameterBinding(reader));
        }
        return items5;
    })();
    const variances = (() => {
        const length6 = reader.readNumber();
        const items6: Array<readonly [LocalGenericParameterId, VarianceModifier]> = [];
        for (let index = 0; index < length6; index += 1) {
            items6.push([
                decodeLocalGenericParameterId(reader),
                decodeVarianceModifier(reader),
            ] as const);
        }
        return items6;
    })();
    const cardinalities = (() => {
        const length7 = reader.readNumber();
        const items7: Array<readonly [LocalGenericParameterId, Cardinality]> = [];
        for (let index = 0; index < length7; index += 1) {
            items7.push([
                decodeLocalGenericParameterId(reader),
                decodeCardinality(reader),
            ] as const);
        }
        return items7;
    })();
    const firstInstanceId = reader.readNumber();
    const instances = (() => {
        const length9 = reader.readNumber();
        const items9: Array<Instance> = [];
        for (let index = 0; index < length9; index += 1) {
            items9.push(decodeInstance(reader));
        }
        return items9;
    })();
    const instantiations = (() => {
        const length10 = reader.readNumber();
        const items10: Array<Instantiation> = [];
        for (let index = 0; index < length10; index += 1) {
            items10.push(decodeInstantiation(reader));
        }
        return items10;
    })();
    const applicationInstances = (() => {
        const length11 = reader.readNumber();
        const items11 = new Map<GlobalTypeId, LocalInstanceId>();
        for (let index = 0; index < length11; index += 1) {
            items11.set(decodeGlobalTypeId(reader), decodeLocalInstanceId(reader));
        }
        return items11;
    })();
    const symbolDependents = (() => {
        const length12 = reader.readNumber();
        const items12 = new Map<LocalSymbolId, ReadonlyArray<GlobalTypeId>>();
        for (let index = 0; index < length12; index += 1) {
            items12.set(
                decodeLocalSymbolId(reader),
                (() => {
                    const length14 = reader.readNumber();
                    const items14: Array<GlobalTypeId> = [];
                    for (let index = 0; index < length14; index += 1) {
                        items14.push(decodeGlobalTypeId(reader));
                    }
                    return items14;
                })(),
            );
        }
        return items12;
    })();
    const selectionInstances = (() => {
        const length13 = reader.readNumber();
        const items13 = new Map<InstanceKey, LocalInstanceId>();
        for (let index = 0; index < length13; index += 1) {
            items13.set(decodeInstanceKey(reader), decodeLocalInstanceId(reader));
        }
        return items13;
    })();
    const witnesses = (() => {
        const length14 = reader.readNumber();
        const items14 = new Map<readonly [GlobalTypeId, GlobalTypeId], Witness>();
        for (let index = 0; index < length14; index += 1) {
            items14.set(
                [decodeGlobalTypeId(reader), decodeGlobalTypeId(reader)] as const,
                decodeWitness(reader),
            );
        }
        return items14;
    })();
    const parameterBounds = (() => {
        const length15 = reader.readNumber();
        const items15 = new Map<LocalGenericParameterId, TypeListId>();
        for (let index = 0; index < length15; index += 1) {
            items15.set(decodeLocalGenericParameterId(reader), decodeTypeListId(reader));
        }
        return items15;
    })();
    const assumedBounds = (() => {
        const length16 = reader.readNumber();
        const items16 = new Map<
            readonly [LocalGenericTemplateId, LocalGenericParameterId],
            TypeListId
        >();
        for (let index = 0; index < length16; index += 1) {
            items16.set(
                [
                    decodeLocalGenericTemplateId(reader),
                    decodeLocalGenericParameterId(reader),
                ] as const,
                decodeTypeListId(reader),
            );
        }
        return items16;
    })();
    const instanceSymbols = (() => {
        const length17 = reader.readNumber();
        const items17 = new Map<readonly [LocalInstanceId, GlobalSymbolId], GlobalTypeId>();
        for (let index = 0; index < length17; index += 1) {
            items17.set(
                [decodeLocalInstanceId(reader), decodeGlobalSymbolId(reader)] as const,
                decodeGlobalTypeId(reader),
            );
        }
        return items17;
    })();

    return {
        moduleId,
        firstTemplateId,
        firstParameterId,
        templates,
        templatesByScope,
        parameters,
        variances,
        cardinalities,
        firstInstanceId,
        instances,
        instantiations,
        applicationInstances,
        symbolDependents,
        selectionInstances,
        witnesses,
        parameterBounds,
        assumedBounds,
        instanceSymbols,
    };
}

/** Return one JSON value for one GenericSegment. */
export function toJsonGenericSegment(value: GenericSegment): Json {
    return {
        moduleId: toJsonModuleId(value.moduleId),
        firstTemplateId: value.firstTemplateId,
        firstParameterId: value.firstParameterId,
        templates: value.templates.map((item0) => toJsonGenericTemplate(item0)),
        templatesByScope: value.templatesByScope.map((item0) =>
            item0 === undefined ? null : toJsonLocalGenericTemplateId(item0),
        ),
        parameters: value.parameters.map((item0) => toJsonGenericParameterBinding(item0)),
        variances: value.variances.map((item0) => [
            toJsonLocalGenericParameterId(item0[0]),
            toJsonVarianceModifier(item0[1]),
        ]),
        cardinalities: value.cardinalities.map((item0) => [
            toJsonLocalGenericParameterId(item0[0]),
            toJsonCardinality(item0[1]),
        ]),
        firstInstanceId: value.firstInstanceId,
        instances: value.instances.map((item0) => toJsonInstance(item0)),
        instantiations: value.instantiations.map((item0) => toJsonInstantiation(item0)),
        applicationInstances: Array.from(value.applicationInstances.entries()).map(
            ([key0, item0]) => [toJsonGlobalTypeId(key0), toJsonLocalInstanceId(item0)] as const,
        ),
        symbolDependents: Array.from(value.symbolDependents.entries()).map(
            ([key0, item0]) =>
                [
                    toJsonLocalSymbolId(key0),
                    item0.map((item1) => toJsonGlobalTypeId(item1)),
                ] as const,
        ),
        selectionInstances: Array.from(value.selectionInstances.entries()).map(
            ([key0, item0]) => [toJsonInstanceKey(key0), toJsonLocalInstanceId(item0)] as const,
        ),
        witnesses: Array.from(value.witnesses.entries()).map(
            ([key0, item0]) =>
                [
                    [toJsonGlobalTypeId(key0[0]), toJsonGlobalTypeId(key0[1])],
                    toJsonWitness(item0),
                ] as const,
        ),
        parameterBounds: Array.from(value.parameterBounds.entries()).map(
            ([key0, item0]) =>
                [toJsonLocalGenericParameterId(key0), toJsonTypeListId(item0)] as const,
        ),
        assumedBounds: Array.from(value.assumedBounds.entries()).map(
            ([key0, item0]) =>
                [
                    [toJsonLocalGenericTemplateId(key0[0]), toJsonLocalGenericParameterId(key0[1])],
                    toJsonTypeListId(item0),
                ] as const,
        ),
        instanceSymbols: Array.from(value.instanceSymbols.entries()).map(
            ([key0, item0]) =>
                [
                    [toJsonLocalInstanceId(key0[0]), toJsonGlobalSymbolId(key0[1])],
                    toJsonGlobalTypeId(item0),
                ] as const,
        ),
    };
}

/** Return one GenericSegment from one JSON value. */
export function fromJsonGenericSegment(value: Json): GenericSegment {
    const object = jsonObject(value);

    return {
        moduleId: fromJsonModuleId(jsonField(object, "moduleId")),
        firstTemplateId: jsonInteger(jsonField(object, "firstTemplateId")),
        firstParameterId: jsonInteger(jsonField(object, "firstParameterId")),
        templates: jsonArray(jsonField(object, "templates")).map((item0) =>
            fromJsonGenericTemplate(item0),
        ),
        templatesByScope: jsonArray(jsonField(object, "templatesByScope")).map((item0) =>
            item0 === null ? undefined : fromJsonLocalGenericTemplateId(item0),
        ),
        parameters: jsonArray(jsonField(object, "parameters")).map((item0) =>
            fromJsonGenericParameterBinding(item0),
        ),
        variances: jsonArray(jsonField(object, "variances")).map((item0) =>
            (() => {
                const items = jsonArray(item0);
                if (items.length !== 2) {
                    throw new SerdeError(`expected JSON tuple length 2: ${items.length}`);
                }
                return [
                    fromJsonLocalGenericParameterId(items[0]),
                    fromJsonVarianceModifier(items[1]),
                ] as const;
            })(),
        ),
        cardinalities: jsonArray(jsonField(object, "cardinalities")).map((item0) =>
            (() => {
                const items = jsonArray(item0);
                if (items.length !== 2) {
                    throw new SerdeError(`expected JSON tuple length 2: ${items.length}`);
                }
                return [
                    fromJsonLocalGenericParameterId(items[0]),
                    fromJsonCardinality(items[1]),
                ] as const;
            })(),
        ),
        firstInstanceId: jsonInteger(jsonField(object, "firstInstanceId")),
        instances: jsonArray(jsonField(object, "instances")).map((item0) =>
            fromJsonInstance(item0),
        ),
        instantiations: jsonArray(jsonField(object, "instantiations")).map((item0) =>
            fromJsonInstantiation(item0),
        ),
        applicationInstances: new Map(
            jsonArray(jsonField(object, "applicationInstances")).map((entry) => {
                const items = jsonArray(entry);
                if (items.length !== 2) {
                    throw new SerdeError(`expected JSON map entry length 2: ${items.length}`);
                }
                const key0 = items[0];
                const item0 = items[1];
                return [fromJsonGlobalTypeId(key0), fromJsonLocalInstanceId(item0)] as const;
            }),
        ),
        symbolDependents: new Map(
            jsonArray(jsonField(object, "symbolDependents")).map((entry) => {
                const items = jsonArray(entry);
                if (items.length !== 2) {
                    throw new SerdeError(`expected JSON map entry length 2: ${items.length}`);
                }
                const key0 = items[0];
                const item0 = items[1];
                return [
                    fromJsonLocalSymbolId(key0),
                    jsonArray(item0).map((item1) => fromJsonGlobalTypeId(item1)),
                ] as const;
            }),
        ),
        selectionInstances: new Map(
            jsonArray(jsonField(object, "selectionInstances")).map((entry) => {
                const items = jsonArray(entry);
                if (items.length !== 2) {
                    throw new SerdeError(`expected JSON map entry length 2: ${items.length}`);
                }
                const key0 = items[0];
                const item0 = items[1];
                return [fromJsonInstanceKey(key0), fromJsonLocalInstanceId(item0)] as const;
            }),
        ),
        witnesses: new Map(
            jsonArray(jsonField(object, "witnesses")).map((entry) => {
                const items = jsonArray(entry);
                if (items.length !== 2) {
                    throw new SerdeError(`expected JSON map entry length 2: ${items.length}`);
                }
                const key0 = items[0];
                const item0 = items[1];
                return [
                    (() => {
                        const items = jsonArray(key0);
                        if (items.length !== 2) {
                            throw new SerdeError(`expected JSON tuple length 2: ${items.length}`);
                        }
                        return [
                            fromJsonGlobalTypeId(items[0]),
                            fromJsonGlobalTypeId(items[1]),
                        ] as const;
                    })(),
                    fromJsonWitness(item0),
                ] as const;
            }),
        ),
        parameterBounds: new Map(
            jsonArray(jsonField(object, "parameterBounds")).map((entry) => {
                const items = jsonArray(entry);
                if (items.length !== 2) {
                    throw new SerdeError(`expected JSON map entry length 2: ${items.length}`);
                }
                const key0 = items[0];
                const item0 = items[1];
                return [fromJsonLocalGenericParameterId(key0), fromJsonTypeListId(item0)] as const;
            }),
        ),
        assumedBounds: new Map(
            jsonArray(jsonField(object, "assumedBounds")).map((entry) => {
                const items = jsonArray(entry);
                if (items.length !== 2) {
                    throw new SerdeError(`expected JSON map entry length 2: ${items.length}`);
                }
                const key0 = items[0];
                const item0 = items[1];
                return [
                    (() => {
                        const items = jsonArray(key0);
                        if (items.length !== 2) {
                            throw new SerdeError(`expected JSON tuple length 2: ${items.length}`);
                        }
                        return [
                            fromJsonLocalGenericTemplateId(items[0]),
                            fromJsonLocalGenericParameterId(items[1]),
                        ] as const;
                    })(),
                    fromJsonTypeListId(item0),
                ] as const;
            }),
        ),
        instanceSymbols: new Map(
            jsonArray(jsonField(object, "instanceSymbols")).map((entry) => {
                const items = jsonArray(entry);
                if (items.length !== 2) {
                    throw new SerdeError(`expected JSON map entry length 2: ${items.length}`);
                }
                const key0 = items[0];
                const item0 = items[1];
                return [
                    (() => {
                        const items = jsonArray(key0);
                        if (items.length !== 2) {
                            throw new SerdeError(`expected JSON tuple length 2: ${items.length}`);
                        }
                        return [
                            fromJsonLocalInstanceId(items[0]),
                            fromJsonGlobalSymbolId(items[1]),
                        ] as const;
                    })(),
                    fromJsonGlobalTypeId(item0),
                ] as const;
            }),
        ),
    };
}
