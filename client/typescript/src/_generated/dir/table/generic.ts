/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, compareBytes, jsonArray, jsonBool, jsonField, jsonInteger, jsonObject, jsonOptional, nestedBytes } from "../../../protocol/serde.js";
import type { GlobalSymbolId } from "../symbol/symbol.js";
import type { VarianceModifier } from "../tree/property.js";
import type { Cardinality } from "../type/generic.js";
import type { Instance } from "../type/generic.js";
import type { Instantiation } from "../type/generic.js";
import type { LocalGenericParameterId } from "../type/generic.js";
import type { LocalGenericTemplateId } from "../type/generic.js";
import type { LocalInstanceId } from "../type/generic.js";
import type { GenericParameterBinding } from "../type/generic.js";
import type { GenericTemplate } from "../type/generic.js";
import type { InstanceKey } from "../type/key.js";
import type { GlobalTypeId } from "../type/type.js";
import type { ModuleId } from "../../source/file/model/module.js";
import { decodeGlobalSymbolId, encodeGlobalSymbolId, fromJsonGlobalSymbolId, toJsonGlobalSymbolId } from "../symbol/symbol.js";
import { decodeVarianceModifier, encodeVarianceModifier, fromJsonVarianceModifier, toJsonVarianceModifier } from "../tree/property.js";
import { decodeCardinality, encodeCardinality, fromJsonCardinality, toJsonCardinality } from "../type/generic.js";
import { decodeInstance, encodeInstance, fromJsonInstance, toJsonInstance } from "../type/generic.js";
import { decodeInstantiation, encodeInstantiation, fromJsonInstantiation, toJsonInstantiation } from "../type/generic.js";
import { decodeLocalGenericParameterId, encodeLocalGenericParameterId, fromJsonLocalGenericParameterId, toJsonLocalGenericParameterId } from "../type/generic.js";
import { decodeLocalGenericTemplateId, encodeLocalGenericTemplateId, fromJsonLocalGenericTemplateId, toJsonLocalGenericTemplateId } from "../type/generic.js";
import { decodeLocalInstanceId, encodeLocalInstanceId, fromJsonLocalInstanceId, toJsonLocalInstanceId } from "../type/generic.js";
import { decodeGenericParameterBinding, encodeGenericParameterBinding, fromJsonGenericParameterBinding, toJsonGenericParameterBinding } from "../type/generic.js";
import { decodeGenericTemplate, encodeGenericTemplate, fromJsonGenericTemplate, toJsonGenericTemplate } from "../type/generic.js";
import { decodeInstanceKey, encodeInstanceKey, fromJsonInstanceKey, toJsonInstanceKey } from "../type/key.js";
import { decodeGlobalTypeId, encodeGlobalTypeId, fromJsonGlobalTypeId, toJsonGlobalTypeId } from "../type/type.js";
import { decodeModuleId, encodeModuleId, fromJsonModuleId, toJsonModuleId } from "../../source/file/model/module.js";

/** Generic templates and parameters added by one DIR phase. */
export type GenericSegment = {
    /** The module id of the generic segment. */
    readonly moduleId: ModuleId;
    /** The first generic template id owned by this table segment. */
    readonly firstTemplateId: number;
    /** The first generic parameter id owned by this table segment. */
    readonly firstParameterId: number;
    /** Generic templates. */
    readonly templates: ReadonlyArray<GenericTemplate>;
    /** Generic template ids keyed by lexical scope id. */
    readonly templatesByScope: ReadonlyArray<LocalGenericTemplateId | undefined>;
    /** Generic parameters. */
    readonly parameters: ReadonlyArray<GenericParameterBinding>;
    /** Variances derived from declared member types. */
    readonly variances: ReadonlyArray<readonly [LocalGenericParameterId, VarianceModifier]>;
    /** Cardinalities derived from declared value positions. */
    readonly cardinalities: ReadonlyArray<readonly [LocalGenericParameterId, Cardinality]>;
    /** The first generic instance id owned by this table segment. */
    readonly firstInstanceId: number;
    /** Generic instances closed by this segment. */
    readonly instances: ReadonlyArray<Instance>;
    /** Instantiations the checked bodies perform, open while they mention parameters. */
    readonly instantiations: ReadonlyArray<Instantiation>;
    /** Materialized types keyed by instance and template type. */
    readonly instanceTypes: ReadonlyMap<readonly [LocalInstanceId, GlobalTypeId], readonly [GlobalTypeId, boolean]>;
    /** The interned instance behind each closed application type. */
    readonly applicationInstances: ReadonlyMap<GlobalTypeId, LocalInstanceId>;
    /** The region terms each closed application substitutes for its instance's bound regions. */
    readonly applicationRegions: ReadonlyMap<GlobalTypeId, ReadonlyArray<GlobalTypeId>>;
    /** The implementing selection behind each closed interface requirement. */
    readonly dispatchSelections: ReadonlyMap<InstanceKey, InstanceKey>;
    /** Materialized symbol types keyed by instance and symbol. */
    readonly instanceSymbols: ReadonlyMap<readonly [LocalInstanceId, GlobalSymbolId], GlobalTypeId>;
};

export const GenericSegment = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: GenericSegment): void {
        encodeGenericSegment(writer, value);
    },

    /** Decode one GenericSegment. */
    decode(reader: BinaryReader): GenericSegment {
        return decodeGenericSegment(reader);
    },

    /** Return this value as JSON. */
    toJson(value: GenericSegment): Json {
        return toJsonGenericSegment(value);
    },

    /** Return one GenericSegment from one JSON value. */
    fromJson(value: Json): GenericSegment {
        return fromJsonGenericSegment(value);
    },
};

/** Encode one GenericSegment. */
export function encodeGenericSegment(writer: BinaryWriter, value: GenericSegment): void {
    encodeModuleId(writer, value.moduleId);
    writer.writeUnsigned(value.firstTemplateId);
    writer.writeUnsigned(value.firstParameterId);
    writer.writeUnsigned(value.templates.length);
    for (const item3 of value.templates) {
        encodeGenericTemplate(writer, item3);
    }
    writer.writeUnsigned(value.templatesByScope.length);
    for (const item4 of value.templatesByScope) {
        writer.writeOption(item4, (value5) => {
            encodeLocalGenericTemplateId(writer, value5);
        });
    }
    writer.writeUnsigned(value.parameters.length);
    for (const item5 of value.parameters) {
        encodeGenericParameterBinding(writer, item5);
    }
    writer.writeUnsigned(value.variances.length);
    for (const item6 of value.variances) {
        encodeLocalGenericParameterId(writer, item6[0]);
        encodeVarianceModifier(writer, item6[1]);
    }
    writer.writeUnsigned(value.cardinalities.length);
    for (const item7 of value.cardinalities) {
        encodeLocalGenericParameterId(writer, item7[0]);
        encodeCardinality(writer, item7[1]);
    }
    writer.writeUnsigned(value.firstInstanceId);
    writer.writeUnsigned(value.instances.length);
    for (const item9 of value.instances) {
        encodeInstance(writer, item9);
    }
    writer.writeUnsigned(value.instantiations.length);
    for (const item10 of value.instantiations) {
        encodeInstantiation(writer, item10);
    }
    const entries11 = Array.from(value.instanceTypes.entries()).map(([key11, item11]) => {
        const keyBytes = nestedBytes((writer) => {
            encodeLocalInstanceId(writer, key11[0]);
            encodeGlobalTypeId(writer, key11[1]);
        });
        return { key11, item11, keyBytes };
    });
    entries11.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries11.length);
    for (const entry11 of entries11) {
        encodeLocalInstanceId(writer, entry11.key11[0]);
        encodeGlobalTypeId(writer, entry11.key11[1]);
        encodeGlobalTypeId(writer, entry11.item11[0]);
        writer.writeBool(entry11.item11[1]);
    }
    const entries12 = Array.from(value.applicationInstances.entries()).map(([key12, item12]) => {
        const keyBytes = nestedBytes((writer) => {
            encodeGlobalTypeId(writer, key12);
        });
        return { key12, item12, keyBytes };
    });
    entries12.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries12.length);
    for (const entry12 of entries12) {
        encodeGlobalTypeId(writer, entry12.key12);
        encodeLocalInstanceId(writer, entry12.item12);
    }
    const entries13 = Array.from(value.applicationRegions.entries()).map(([key13, item13]) => {
        const keyBytes = nestedBytes((writer) => {
            encodeGlobalTypeId(writer, key13);
        });
        return { key13, item13, keyBytes };
    });
    entries13.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries13.length);
    for (const entry13 of entries13) {
        encodeGlobalTypeId(writer, entry13.key13);
        writer.writeUnsigned(entry13.item13.length);
        for (const item14 of entry13.item13) {
            encodeGlobalTypeId(writer, item14);
        }
    }
    const entries14 = Array.from(value.dispatchSelections.entries()).map(([key14, item14]) => {
        const keyBytes = nestedBytes((writer) => {
            encodeInstanceKey(writer, key14);
        });
        return { key14, item14, keyBytes };
    });
    entries14.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries14.length);
    for (const entry14 of entries14) {
        encodeInstanceKey(writer, entry14.key14);
        encodeInstanceKey(writer, entry14.item14);
    }
    const entries15 = Array.from(value.instanceSymbols.entries()).map(([key15, item15]) => {
        const keyBytes = nestedBytes((writer) => {
            encodeLocalInstanceId(writer, key15[0]);
            encodeGlobalSymbolId(writer, key15[1]);
        });
        return { key15, item15, keyBytes };
    });
    entries15.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries15.length);
    for (const entry15 of entries15) {
        encodeLocalInstanceId(writer, entry15.key15[0]);
        encodeGlobalSymbolId(writer, entry15.key15[1]);
        encodeGlobalTypeId(writer, entry15.item15);
    }
}

/** Decode one GenericSegment. */
export function decodeGenericSegment(reader: BinaryReader): GenericSegment {
    const moduleId = decodeModuleId(reader);
    const firstTemplateId = reader.readNumber();
    const firstParameterId = reader.readNumber();
    const templates = (() => { const length3 = reader.readNumber(); const items3: Array<GenericTemplate> = []; for (let index = 0; index < length3; index += 1) { items3.push(decodeGenericTemplate(reader)); } return items3; })();
    const templatesByScope = (() => { const length4 = reader.readNumber(); const items4: Array<LocalGenericTemplateId | undefined> = []; for (let index = 0; index < length4; index += 1) { items4.push(reader.readOption(() => decodeLocalGenericTemplateId(reader))); } return items4; })();
    const parameters = (() => { const length5 = reader.readNumber(); const items5: Array<GenericParameterBinding> = []; for (let index = 0; index < length5; index += 1) { items5.push(decodeGenericParameterBinding(reader)); } return items5; })();
    const variances = (() => { const length6 = reader.readNumber(); const items6: Array<readonly [LocalGenericParameterId, VarianceModifier]> = []; for (let index = 0; index < length6; index += 1) { items6.push([decodeLocalGenericParameterId(reader), decodeVarianceModifier(reader)] as const); } return items6; })();
    const cardinalities = (() => { const length7 = reader.readNumber(); const items7: Array<readonly [LocalGenericParameterId, Cardinality]> = []; for (let index = 0; index < length7; index += 1) { items7.push([decodeLocalGenericParameterId(reader), decodeCardinality(reader)] as const); } return items7; })();
    const firstInstanceId = reader.readNumber();
    const instances = (() => { const length9 = reader.readNumber(); const items9: Array<Instance> = []; for (let index = 0; index < length9; index += 1) { items9.push(decodeInstance(reader)); } return items9; })();
    const instantiations = (() => { const length10 = reader.readNumber(); const items10: Array<Instantiation> = []; for (let index = 0; index < length10; index += 1) { items10.push(decodeInstantiation(reader)); } return items10; })();
    const instanceTypes = (() => { const length11 = reader.readNumber(); const items11 = new Map<readonly [LocalInstanceId, GlobalTypeId], readonly [GlobalTypeId, boolean]>(); for (let index = 0; index < length11; index += 1) { items11.set([decodeLocalInstanceId(reader), decodeGlobalTypeId(reader)] as const, [decodeGlobalTypeId(reader), reader.readBool()] as const); } return items11; })();
    const applicationInstances = (() => { const length12 = reader.readNumber(); const items12 = new Map<GlobalTypeId, LocalInstanceId>(); for (let index = 0; index < length12; index += 1) { items12.set(decodeGlobalTypeId(reader), decodeLocalInstanceId(reader)); } return items12; })();
    const applicationRegions = (() => { const length13 = reader.readNumber(); const items13 = new Map<GlobalTypeId, ReadonlyArray<GlobalTypeId>>(); for (let index = 0; index < length13; index += 1) { items13.set(decodeGlobalTypeId(reader), (() => { const length15 = reader.readNumber(); const items15: Array<GlobalTypeId> = []; for (let index = 0; index < length15; index += 1) { items15.push(decodeGlobalTypeId(reader)); } return items15; })()); } return items13; })();
    const dispatchSelections = (() => { const length14 = reader.readNumber(); const items14 = new Map<InstanceKey, InstanceKey>(); for (let index = 0; index < length14; index += 1) { items14.set(decodeInstanceKey(reader), decodeInstanceKey(reader)); } return items14; })();
    const instanceSymbols = (() => { const length15 = reader.readNumber(); const items15 = new Map<readonly [LocalInstanceId, GlobalSymbolId], GlobalTypeId>(); for (let index = 0; index < length15; index += 1) { items15.set([decodeLocalInstanceId(reader), decodeGlobalSymbolId(reader)] as const, decodeGlobalTypeId(reader)); } return items15; })();

    return {
        moduleId,
        firstTemplateId,
        firstParameterId,
        templates,
        templatesByScope,
        parameters,
        variances,
        cardinalities,
        firstInstanceId,
        instances,
        instantiations,
        instanceTypes,
        applicationInstances,
        applicationRegions,
        dispatchSelections,
        instanceSymbols,
    };
}

/** Return one JSON value for one GenericSegment. */
export function toJsonGenericSegment(value: GenericSegment): Json {
    return {
        moduleId: toJsonModuleId(value.moduleId),
        firstTemplateId: value.firstTemplateId,
        firstParameterId: value.firstParameterId,
        templates: value.templates.map((item0) => toJsonGenericTemplate(item0)),
        templatesByScope: value.templatesByScope.map((item0) => item0 === undefined ? null : toJsonLocalGenericTemplateId(item0)),
        parameters: value.parameters.map((item0) => toJsonGenericParameterBinding(item0)),
        variances: value.variances.map((item0) => [toJsonLocalGenericParameterId(item0[0]), toJsonVarianceModifier(item0[1])]),
        cardinalities: value.cardinalities.map((item0) => [toJsonLocalGenericParameterId(item0[0]), toJsonCardinality(item0[1])]),
        firstInstanceId: value.firstInstanceId,
        instances: value.instances.map((item0) => toJsonInstance(item0)),
        instantiations: value.instantiations.map((item0) => toJsonInstantiation(item0)),
        instanceTypes: Array.from(value.instanceTypes.entries()).map(([key0, item0]) => [[toJsonLocalInstanceId(key0[0]), toJsonGlobalTypeId(key0[1])], [toJsonGlobalTypeId(item0[0]), item0[1]]] as const),
        applicationInstances: Array.from(value.applicationInstances.entries()).map(([key0, item0]) => [toJsonGlobalTypeId(key0), toJsonLocalInstanceId(item0)] as const),
        applicationRegions: Array.from(value.applicationRegions.entries()).map(([key0, item0]) => [toJsonGlobalTypeId(key0), item0.map((item1) => toJsonGlobalTypeId(item1))] as const),
        dispatchSelections: Array.from(value.dispatchSelections.entries()).map(([key0, item0]) => [toJsonInstanceKey(key0), toJsonInstanceKey(item0)] as const),
        instanceSymbols: Array.from(value.instanceSymbols.entries()).map(([key0, item0]) => [[toJsonLocalInstanceId(key0[0]), toJsonGlobalSymbolId(key0[1])], toJsonGlobalTypeId(item0)] as const),
    };
}

/** Return one GenericSegment from one JSON value. */
export function fromJsonGenericSegment(value: Json): GenericSegment {
    const object = jsonObject(value);

    return {
        moduleId: fromJsonModuleId(jsonField(object, "moduleId")),
        firstTemplateId: jsonInteger(jsonField(object, "firstTemplateId")),
        firstParameterId: jsonInteger(jsonField(object, "firstParameterId")),
        templates: jsonArray(jsonField(object, "templates")).map((item0) => fromJsonGenericTemplate(item0)),
        templatesByScope: jsonArray(jsonField(object, "templatesByScope")).map((item0) => item0 === null ? undefined : fromJsonLocalGenericTemplateId(item0)),
        parameters: jsonArray(jsonField(object, "parameters")).map((item0) => fromJsonGenericParameterBinding(item0)),
        variances: jsonArray(jsonField(object, "variances")).map((item0) => (() => { const items = jsonArray(item0); if (items.length !== 2) { throw new SerdeError(`expected JSON tuple length 2: ${items.length}`); } return [fromJsonLocalGenericParameterId(items[0]), fromJsonVarianceModifier(items[1])] as const; })()),
        cardinalities: jsonArray(jsonField(object, "cardinalities")).map((item0) => (() => { const items = jsonArray(item0); if (items.length !== 2) { throw new SerdeError(`expected JSON tuple length 2: ${items.length}`); } return [fromJsonLocalGenericParameterId(items[0]), fromJsonCardinality(items[1])] as const; })()),
        firstInstanceId: jsonInteger(jsonField(object, "firstInstanceId")),
        instances: jsonArray(jsonField(object, "instances")).map((item0) => fromJsonInstance(item0)),
        instantiations: jsonArray(jsonField(object, "instantiations")).map((item0) => fromJsonInstantiation(item0)),
        instanceTypes: new Map(jsonArray(jsonField(object, "instanceTypes")).map((entry) => { const items = jsonArray(entry); if (items.length !== 2) { throw new SerdeError(`expected JSON map entry length 2: ${items.length}`); } const key0 = items[0]; const item0 = items[1]; return [(() => { const items = jsonArray(key0); if (items.length !== 2) { throw new SerdeError(`expected JSON tuple length 2: ${items.length}`); } return [fromJsonLocalInstanceId(items[0]), fromJsonGlobalTypeId(items[1])] as const; })(), (() => { const items = jsonArray(item0); if (items.length !== 2) { throw new SerdeError(`expected JSON tuple length 2: ${items.length}`); } return [fromJsonGlobalTypeId(items[0]), jsonBool(items[1])] as const; })()] as const; })),
        applicationInstances: new Map(jsonArray(jsonField(object, "applicationInstances")).map((entry) => { const items = jsonArray(entry); if (items.length !== 2) { throw new SerdeError(`expected JSON map entry length 2: ${items.length}`); } const key0 = items[0]; const item0 = items[1]; return [fromJsonGlobalTypeId(key0), fromJsonLocalInstanceId(item0)] as const; })),
        applicationRegions: new Map(jsonArray(jsonField(object, "applicationRegions")).map((entry) => { const items = jsonArray(entry); if (items.length !== 2) { throw new SerdeError(`expected JSON map entry length 2: ${items.length}`); } const key0 = items[0]; const item0 = items[1]; return [fromJsonGlobalTypeId(key0), jsonArray(item0).map((item1) => fromJsonGlobalTypeId(item1))] as const; })),
        dispatchSelections: new Map(jsonArray(jsonField(object, "dispatchSelections")).map((entry) => { const items = jsonArray(entry); if (items.length !== 2) { throw new SerdeError(`expected JSON map entry length 2: ${items.length}`); } const key0 = items[0]; const item0 = items[1]; return [fromJsonInstanceKey(key0), fromJsonInstanceKey(item0)] as const; })),
        instanceSymbols: new Map(jsonArray(jsonField(object, "instanceSymbols")).map((entry) => { const items = jsonArray(entry); if (items.length !== 2) { throw new SerdeError(`expected JSON map entry length 2: ${items.length}`); } const key0 = items[0]; const item0 = items[1]; return [(() => { const items = jsonArray(key0); if (items.length !== 2) { throw new SerdeError(`expected JSON tuple length 2: ${items.length}`); } return [fromJsonLocalInstanceId(items[0]), fromJsonGlobalSymbolId(items[1])] as const; })(), fromJsonGlobalTypeId(item0)] as const; })),
    };
}
