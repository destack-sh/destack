/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, compareBytes, jsonArray, jsonField, jsonObject, jsonString, nestedBytes } from "../../../protocol/serde.js";
import type { GlobalSymbolId } from "../symbol/symbol.js";
import type { GlobalNodeIdAny } from "../tree/node.js";
import type { AccessResolution } from "../type/access.js";
import type { ReceiverDecision } from "../type/receiver.js";
import type { AssignPatternDecision } from "../type/resolution.js";
import type { AssignmentDecision } from "../type/resolution.js";
import type { Call } from "../type/resolution.js";
import type { ConstructDecision } from "../type/resolution.js";
import type { GuardDecision } from "../type/resolution.js";
import type { InstantiationDecision } from "../type/resolution.js";
import type { OperationResolution } from "../type/resolution.js";
import type { PatternDecision } from "../type/resolution.js";
import type { PlaceResolution } from "../type/resolution.js";
import type { TreeDecision } from "../type/resolution.js";
import type { ModuleId } from "../../source/file/model/module.js";
import { decodeGlobalSymbolId, encodeGlobalSymbolId, fromJsonGlobalSymbolId, toJsonGlobalSymbolId } from "../symbol/symbol.js";
import { decodeGlobalNodeIdAny, encodeGlobalNodeIdAny, fromJsonGlobalNodeIdAny, toJsonGlobalNodeIdAny } from "../tree/node.js";
import { decodeAccessResolution, encodeAccessResolution, fromJsonAccessResolution, toJsonAccessResolution } from "../type/access.js";
import { decodeReceiverDecision, encodeReceiverDecision, fromJsonReceiverDecision, toJsonReceiverDecision } from "../type/receiver.js";
import { decodeAssignPatternDecision, encodeAssignPatternDecision, fromJsonAssignPatternDecision, toJsonAssignPatternDecision } from "../type/resolution.js";
import { decodeAssignmentDecision, encodeAssignmentDecision, fromJsonAssignmentDecision, toJsonAssignmentDecision } from "../type/resolution.js";
import { decodeCall, encodeCall, fromJsonCall, toJsonCall } from "../type/resolution.js";
import { decodeConstructDecision, encodeConstructDecision, fromJsonConstructDecision, toJsonConstructDecision } from "../type/resolution.js";
import { decodeGuardDecision, encodeGuardDecision, fromJsonGuardDecision, toJsonGuardDecision } from "../type/resolution.js";
import { decodeInstantiationDecision, encodeInstantiationDecision, fromJsonInstantiationDecision, toJsonInstantiationDecision } from "../type/resolution.js";
import { decodeOperationResolution, encodeOperationResolution, fromJsonOperationResolution, toJsonOperationResolution } from "../type/resolution.js";
import { decodePatternDecision, encodePatternDecision, fromJsonPatternDecision, toJsonPatternDecision } from "../type/resolution.js";
import { decodePlaceResolution, encodePlaceResolution, fromJsonPlaceResolution, toJsonPlaceResolution } from "../type/resolution.js";
import { decodeTreeDecision, encodeTreeDecision, fromJsonTreeDecision, toJsonTreeDecision } from "../type/resolution.js";
import { decodeModuleId, encodeModuleId, fromJsonModuleId, toJsonModuleId } from "../../source/file/model/module.js";

/** The one decision inference made for a DIR node. */
export type Decision =
    /** Resolved explicit generic application. */
    | {
          readonly kind: "instantiation";
          readonly instantiation: InstantiationDecision;
      }
    /** Resolved contextual receiver. */
    | {
          readonly kind: "receiver";
          readonly receiver: ReceiverDecision;
      }
    /** Resolved member access. */
    | {
          readonly kind: "member";
          readonly member: OperationResolution;
      }
    /** Resolved control transfer target. */
    | {
          readonly kind: "label";
          readonly label: GlobalSymbolId;
      }
    /** Resolved operator application. */
    | {
          readonly kind: "operator";
          readonly operator: OperationResolution;
      }
    /** Resolved call. */
    | {
          readonly kind: "call";
          readonly call: OperationResolution;
      }
    /** Resolved subscript access. */
    | {
          readonly kind: "subscript";
          readonly subscript: OperationResolution;
      }
    /** Resolved assignment target. */
    | {
          readonly kind: "assignment";
          readonly assignment: AssignmentDecision;
      }
    /** Resolved runtime predicate expression. */
    | {
          readonly kind: "guard";
          readonly guard: GuardDecision;
      }
    /** Resolved construct expression. */
    | {
          readonly kind: "construct";
          readonly construct: ConstructDecision;
      }
    /** Resolved tree literal. */
    | {
          readonly kind: "tree";
          readonly tree: TreeDecision;
      }
    /** Resolved pattern meaning. */
    | {
          readonly kind: "pattern";
          readonly pattern: PatternDecision;
      }
    /** Resolved assignment pattern meaning. */
    | {
          readonly kind: "assignPattern";
          readonly assign_pattern: AssignPatternDecision;
      }
    /** Rejected node with the one call it reached for. */
    | {
          readonly kind: "attempted";
          readonly attempted: Call;
      }
    /** Rejected node with reported diagnostics. */
    | {
          readonly kind: "rejected";
      }
    /** Poisoned node with an already-reported error. */
    | {
          readonly kind: "poisoned";
      }
;

export const Decision = {
    /** Resolved explicit generic application. */
    instantiation(instantiation: InstantiationDecision): Decision {
        return { kind: "instantiation", instantiation };
    },

    /** Resolved contextual receiver. */
    receiver(receiver: ReceiverDecision): Decision {
        return { kind: "receiver", receiver };
    },

    /** Resolved member access. */
    member(member: OperationResolution): Decision {
        return { kind: "member", member };
    },

    /** Resolved control transfer target. */
    label(label: GlobalSymbolId): Decision {
        return { kind: "label", label };
    },

    /** Resolved operator application. */
    operator(operator: OperationResolution): Decision {
        return { kind: "operator", operator };
    },

    /** Resolved call. */
    call(call: OperationResolution): Decision {
        return { kind: "call", call };
    },

    /** Resolved subscript access. */
    subscript(subscript: OperationResolution): Decision {
        return { kind: "subscript", subscript };
    },

    /** Resolved assignment target. */
    assignment(assignment: AssignmentDecision): Decision {
        return { kind: "assignment", assignment };
    },

    /** Resolved runtime predicate expression. */
    guard(guard: GuardDecision): Decision {
        return { kind: "guard", guard };
    },

    /** Resolved construct expression. */
    construct(construct: ConstructDecision): Decision {
        return { kind: "construct", construct };
    },

    /** Resolved tree literal. */
    tree(tree: TreeDecision): Decision {
        return { kind: "tree", tree };
    },

    /** Resolved pattern meaning. */
    pattern(pattern: PatternDecision): Decision {
        return { kind: "pattern", pattern };
    },

    /** Resolved assignment pattern meaning. */
    assignPattern(assign_pattern: AssignPatternDecision): Decision {
        return { kind: "assignPattern", assign_pattern };
    },

    /** Rejected node with the one call it reached for. */
    attempted(attempted: Call): Decision {
        return { kind: "attempted", attempted };
    },

    /** Rejected node with reported diagnostics. */
    rejected(): Decision {
        return { kind: "rejected" };
    },

    /** Poisoned node with an already-reported error. */
    poisoned(): Decision {
        return { kind: "poisoned" };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: Decision): void {
        encodeDecision(writer, value);
    },

    /** Decode one Decision. */
    decode(reader: BinaryReader): Decision {
        return decodeDecision(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Decision): Json {
        return toJsonDecision(value);
    },

    /** Return one Decision from one JSON value. */
    fromJson(value: Json): Decision {
        return fromJsonDecision(value);
    },
};

/** Encode one Decision. */
export function encodeDecision(writer: BinaryWriter, value: Decision): void {
    switch (value.kind) {
        case "instantiation":
            writer.writeUnsigned(0);
            encodeInstantiationDecision(writer, value.instantiation);
            return;
        case "receiver":
            writer.writeUnsigned(1);
            encodeReceiverDecision(writer, value.receiver);
            return;
        case "member":
            writer.writeUnsigned(2);
            encodeOperationResolution(writer, value.member);
            return;
        case "label":
            writer.writeUnsigned(3);
            encodeGlobalSymbolId(writer, value.label);
            return;
        case "operator":
            writer.writeUnsigned(4);
            encodeOperationResolution(writer, value.operator);
            return;
        case "call":
            writer.writeUnsigned(5);
            encodeOperationResolution(writer, value.call);
            return;
        case "subscript":
            writer.writeUnsigned(6);
            encodeOperationResolution(writer, value.subscript);
            return;
        case "assignment":
            writer.writeUnsigned(7);
            encodeAssignmentDecision(writer, value.assignment);
            return;
        case "guard":
            writer.writeUnsigned(8);
            encodeGuardDecision(writer, value.guard);
            return;
        case "construct":
            writer.writeUnsigned(9);
            encodeConstructDecision(writer, value.construct);
            return;
        case "tree":
            writer.writeUnsigned(10);
            encodeTreeDecision(writer, value.tree);
            return;
        case "pattern":
            writer.writeUnsigned(11);
            encodePatternDecision(writer, value.pattern);
            return;
        case "assignPattern":
            writer.writeUnsigned(12);
            encodeAssignPatternDecision(writer, value.assign_pattern);
            return;
        case "attempted":
            writer.writeUnsigned(13);
            encodeCall(writer, value.attempted);
            return;
        case "rejected":
            writer.writeUnsigned(14);
            return;
        case "poisoned":
            writer.writeUnsigned(15);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one Decision. */
export function decodeDecision(reader: BinaryReader): Decision {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const instantiation = decodeInstantiationDecision(reader);

            return { kind: "instantiation", instantiation };
        }
        case 1: {
            const receiver = decodeReceiverDecision(reader);

            return { kind: "receiver", receiver };
        }
        case 2: {
            const member = decodeOperationResolution(reader);

            return { kind: "member", member };
        }
        case 3: {
            const label = decodeGlobalSymbolId(reader);

            return { kind: "label", label };
        }
        case 4: {
            const operator = decodeOperationResolution(reader);

            return { kind: "operator", operator };
        }
        case 5: {
            const call = decodeOperationResolution(reader);

            return { kind: "call", call };
        }
        case 6: {
            const subscript = decodeOperationResolution(reader);

            return { kind: "subscript", subscript };
        }
        case 7: {
            const assignment = decodeAssignmentDecision(reader);

            return { kind: "assignment", assignment };
        }
        case 8: {
            const guard = decodeGuardDecision(reader);

            return { kind: "guard", guard };
        }
        case 9: {
            const construct = decodeConstructDecision(reader);

            return { kind: "construct", construct };
        }
        case 10: {
            const tree = decodeTreeDecision(reader);

            return { kind: "tree", tree };
        }
        case 11: {
            const pattern = decodePatternDecision(reader);

            return { kind: "pattern", pattern };
        }
        case 12: {
            const assign_pattern = decodeAssignPatternDecision(reader);

            return { kind: "assignPattern", assign_pattern };
        }
        case 13: {
            const attempted = decodeCall(reader);

            return { kind: "attempted", attempted };
        }
        case 14: {
            return { kind: "rejected" };
        }
        case 15: {
            return { kind: "poisoned" };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one Decision. */
export function toJsonDecision(value: Decision): Json {
    switch (value.kind) {
        case "instantiation":
            return {
                kind: "instantiation",
                instantiation: toJsonInstantiationDecision(value.instantiation),
            };
        case "receiver":
            return {
                kind: "receiver",
                receiver: toJsonReceiverDecision(value.receiver),
            };
        case "member":
            return {
                kind: "member",
                member: toJsonOperationResolution(value.member),
            };
        case "label":
            return {
                kind: "label",
                label: toJsonGlobalSymbolId(value.label),
            };
        case "operator":
            return {
                kind: "operator",
                operator: toJsonOperationResolution(value.operator),
            };
        case "call":
            return {
                kind: "call",
                call: toJsonOperationResolution(value.call),
            };
        case "subscript":
            return {
                kind: "subscript",
                subscript: toJsonOperationResolution(value.subscript),
            };
        case "assignment":
            return {
                kind: "assignment",
                assignment: toJsonAssignmentDecision(value.assignment),
            };
        case "guard":
            return {
                kind: "guard",
                guard: toJsonGuardDecision(value.guard),
            };
        case "construct":
            return {
                kind: "construct",
                construct: toJsonConstructDecision(value.construct),
            };
        case "tree":
            return {
                kind: "tree",
                tree: toJsonTreeDecision(value.tree),
            };
        case "pattern":
            return {
                kind: "pattern",
                pattern: toJsonPatternDecision(value.pattern),
            };
        case "assignPattern":
            return {
                kind: "assignPattern",
                assign_pattern: toJsonAssignPatternDecision(value.assign_pattern),
            };
        case "attempted":
            return {
                kind: "attempted",
                attempted: toJsonCall(value.attempted),
            };
        case "rejected":
            return {
                kind: "rejected",
            };
        case "poisoned":
            return {
                kind: "poisoned",
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one Decision from one JSON value. */
export function fromJsonDecision(value: Json): Decision {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "instantiation":
            return {
                kind,
                instantiation: fromJsonInstantiationDecision(jsonField(object, "instantiation")),
            };
        case "receiver":
            return {
                kind,
                receiver: fromJsonReceiverDecision(jsonField(object, "receiver")),
            };
        case "member":
            return {
                kind,
                member: fromJsonOperationResolution(jsonField(object, "member")),
            };
        case "label":
            return {
                kind,
                label: fromJsonGlobalSymbolId(jsonField(object, "label")),
            };
        case "operator":
            return {
                kind,
                operator: fromJsonOperationResolution(jsonField(object, "operator")),
            };
        case "call":
            return {
                kind,
                call: fromJsonOperationResolution(jsonField(object, "call")),
            };
        case "subscript":
            return {
                kind,
                subscript: fromJsonOperationResolution(jsonField(object, "subscript")),
            };
        case "assignment":
            return {
                kind,
                assignment: fromJsonAssignmentDecision(jsonField(object, "assignment")),
            };
        case "guard":
            return {
                kind,
                guard: fromJsonGuardDecision(jsonField(object, "guard")),
            };
        case "construct":
            return {
                kind,
                construct: fromJsonConstructDecision(jsonField(object, "construct")),
            };
        case "tree":
            return {
                kind,
                tree: fromJsonTreeDecision(jsonField(object, "tree")),
            };
        case "pattern":
            return {
                kind,
                pattern: fromJsonPatternDecision(jsonField(object, "pattern")),
            };
        case "assignPattern":
            return {
                kind,
                assign_pattern: fromJsonAssignPatternDecision(jsonField(object, "assign_pattern")),
            };
        case "attempted":
            return {
                kind,
                attempted: fromJsonCall(jsonField(object, "attempted")),
            };
        case "rejected":
            return {
                kind,
            };
        case "poisoned":
            return {
                kind,
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** Inference decisions added by one DIR phase. */
export type DecisionSegment = {
    /** The module id of the decision segment. */
    readonly moduleId: ModuleId;
    /** The one decision made per DIR node. */
    readonly decisions: ReadonlyMap<GlobalNodeIdAny, Decision>;
    /** Decided stable storage accesses keyed by DIR node. */
    readonly accesses: ReadonlyMap<GlobalNodeIdAny, AccessResolution>;
    /** Decided place resolutions keyed by DIR node. */
    readonly places: ReadonlyMap<GlobalNodeIdAny, PlaceResolution>;
};

export const DecisionSegment = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: DecisionSegment): void {
        encodeDecisionSegment(writer, value);
    },

    /** Decode one DecisionSegment. */
    decode(reader: BinaryReader): DecisionSegment {
        return decodeDecisionSegment(reader);
    },

    /** Return this value as JSON. */
    toJson(value: DecisionSegment): Json {
        return toJsonDecisionSegment(value);
    },

    /** Return one DecisionSegment from one JSON value. */
    fromJson(value: Json): DecisionSegment {
        return fromJsonDecisionSegment(value);
    },
};

/** Encode one DecisionSegment. */
export function encodeDecisionSegment(writer: BinaryWriter, value: DecisionSegment): void {
    encodeModuleId(writer, value.moduleId);
    const entries1 = Array.from(value.decisions.entries()).map(([key1, item1]) => {
        const keyBytes = nestedBytes((writer) => {
            encodeGlobalNodeIdAny(writer, key1);
        });
        return { key1, item1, keyBytes };
    });
    entries1.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries1.length);
    for (const entry1 of entries1) {
        encodeGlobalNodeIdAny(writer, entry1.key1);
        encodeDecision(writer, entry1.item1);
    }
    const entries2 = Array.from(value.accesses.entries()).map(([key2, item2]) => {
        const keyBytes = nestedBytes((writer) => {
            encodeGlobalNodeIdAny(writer, key2);
        });
        return { key2, item2, keyBytes };
    });
    entries2.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries2.length);
    for (const entry2 of entries2) {
        encodeGlobalNodeIdAny(writer, entry2.key2);
        encodeAccessResolution(writer, entry2.item2);
    }
    const entries3 = Array.from(value.places.entries()).map(([key3, item3]) => {
        const keyBytes = nestedBytes((writer) => {
            encodeGlobalNodeIdAny(writer, key3);
        });
        return { key3, item3, keyBytes };
    });
    entries3.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries3.length);
    for (const entry3 of entries3) {
        encodeGlobalNodeIdAny(writer, entry3.key3);
        encodePlaceResolution(writer, entry3.item3);
    }
}

/** Decode one DecisionSegment. */
export function decodeDecisionSegment(reader: BinaryReader): DecisionSegment {
    const moduleId = decodeModuleId(reader);
    const decisions = (() => { const length1 = reader.readNumber(); const items1 = new Map<GlobalNodeIdAny, Decision>(); for (let index = 0; index < length1; index += 1) { items1.set(decodeGlobalNodeIdAny(reader), decodeDecision(reader)); } return items1; })();
    const accesses = (() => { const length2 = reader.readNumber(); const items2 = new Map<GlobalNodeIdAny, AccessResolution>(); for (let index = 0; index < length2; index += 1) { items2.set(decodeGlobalNodeIdAny(reader), decodeAccessResolution(reader)); } return items2; })();
    const places = (() => { const length3 = reader.readNumber(); const items3 = new Map<GlobalNodeIdAny, PlaceResolution>(); for (let index = 0; index < length3; index += 1) { items3.set(decodeGlobalNodeIdAny(reader), decodePlaceResolution(reader)); } return items3; })();

    return {
        moduleId,
        decisions,
        accesses,
        places,
    };
}

/** Return one JSON value for one DecisionSegment. */
export function toJsonDecisionSegment(value: DecisionSegment): Json {
    return {
        moduleId: toJsonModuleId(value.moduleId),
        decisions: Array.from(value.decisions.entries()).map(([key0, item0]) => [toJsonGlobalNodeIdAny(key0), toJsonDecision(item0)] as const),
        accesses: Array.from(value.accesses.entries()).map(([key0, item0]) => [toJsonGlobalNodeIdAny(key0), toJsonAccessResolution(item0)] as const),
        places: Array.from(value.places.entries()).map(([key0, item0]) => [toJsonGlobalNodeIdAny(key0), toJsonPlaceResolution(item0)] as const),
    };
}

/** Return one DecisionSegment from one JSON value. */
export function fromJsonDecisionSegment(value: Json): DecisionSegment {
    const object = jsonObject(value);

    return {
        moduleId: fromJsonModuleId(jsonField(object, "moduleId")),
        decisions: new Map(jsonArray(jsonField(object, "decisions")).map((entry) => { const items = jsonArray(entry); if (items.length !== 2) { throw new SerdeError(`expected JSON map entry length 2: ${items.length}`); } const key0 = items[0]; const item0 = items[1]; return [fromJsonGlobalNodeIdAny(key0), fromJsonDecision(item0)] as const; })),
        accesses: new Map(jsonArray(jsonField(object, "accesses")).map((entry) => { const items = jsonArray(entry); if (items.length !== 2) { throw new SerdeError(`expected JSON map entry length 2: ${items.length}`); } const key0 = items[0]; const item0 = items[1]; return [fromJsonGlobalNodeIdAny(key0), fromJsonAccessResolution(item0)] as const; })),
        places: new Map(jsonArray(jsonField(object, "places")).map((entry) => { const items = jsonArray(entry); if (items.length !== 2) { throw new SerdeError(`expected JSON map entry length 2: ${items.length}`); } const key0 = items[0]; const item0 = items[1]; return [fromJsonGlobalNodeIdAny(key0), fromJsonPlaceResolution(item0)] as const; })),
    };
}
