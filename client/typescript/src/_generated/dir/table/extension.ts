/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, compareBytes, jsonArray, jsonField, jsonObject, jsonOptional, nestedBytes } from "../../../protocol/serde.js";
import type { GlobalSymbolId } from "../symbol/symbol.js";
import type { LocalSymbolId } from "../symbol/symbol.js";
import type { ModuleId } from "../../source/file/model/module.js";
import { decodeGlobalSymbolId, encodeGlobalSymbolId, fromJsonGlobalSymbolId, toJsonGlobalSymbolId } from "../symbol/symbol.js";
import { decodeLocalSymbolId, encodeLocalSymbolId, fromJsonLocalSymbolId, toJsonLocalSymbolId } from "../symbol/symbol.js";
import { decodeModuleId, encodeModuleId, fromJsonModuleId, toJsonModuleId } from "../../source/file/model/module.js";

/** One extension's resolved implementation. */
export type ExtensionImplementation = {
    /** The target root declaration, absent for blankets. */
    readonly root?: GlobalSymbolId;
    /** The implemented interfaces. */
    readonly interfaces: ReadonlyArray<GlobalSymbolId>;
};

export const ExtensionImplementation = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ExtensionImplementation): void {
        encodeExtensionImplementation(writer, value);
    },

    /** Decode one ExtensionImplementation. */
    decode(reader: BinaryReader): ExtensionImplementation {
        return decodeExtensionImplementation(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ExtensionImplementation): Json {
        return toJsonExtensionImplementation(value);
    },

    /** Return one ExtensionImplementation from one JSON value. */
    fromJson(value: Json): ExtensionImplementation {
        return fromJsonExtensionImplementation(value);
    },
};

/** Encode one ExtensionImplementation. */
export function encodeExtensionImplementation(writer: BinaryWriter, value: ExtensionImplementation): void {
    writer.writeOption(value.root, (value0) => {
        encodeGlobalSymbolId(writer, value0);
    });
    writer.writeUnsigned(value.interfaces.length);
    for (const item1 of value.interfaces) {
        encodeGlobalSymbolId(writer, item1);
    }
}

/** Decode one ExtensionImplementation. */
export function decodeExtensionImplementation(reader: BinaryReader): ExtensionImplementation {
    const root = reader.readOption(() => decodeGlobalSymbolId(reader));
    const interfaces = (() => { const length1 = reader.readNumber(); const items1: Array<GlobalSymbolId> = []; for (let index = 0; index < length1; index += 1) { items1.push(decodeGlobalSymbolId(reader)); } return items1; })();

    return {
        ...(root === undefined ? {} : { root }),
        interfaces,
    };
}

/** Return one JSON value for one ExtensionImplementation. */
export function toJsonExtensionImplementation(value: ExtensionImplementation): Json {
    return {
        ...(value.root === undefined ? {} : { root: toJsonGlobalSymbolId(value.root) }),
        interfaces: value.interfaces.map((item0) => toJsonGlobalSymbolId(item0)),
    };
}

/** Return one ExtensionImplementation from one JSON value. */
export function fromJsonExtensionImplementation(value: Json): ExtensionImplementation {
    const object = jsonObject(value);

    return {
        root: jsonOptional(object, "root", (value) => fromJsonGlobalSymbolId(value)),
        interfaces: jsonArray(jsonField(object, "interfaces")).map((item0) => fromJsonGlobalSymbolId(item0)),
    };
}

/** Exported extensions resolved to their target root declaration. */
export type ExtensionTable = {
    /** The module id of the extension table. */
    readonly moduleId: ModuleId;
    /** Exported extension symbols keyed to their resolved target root. */
    readonly targetBySymbol: ReadonlyMap<LocalSymbolId, GlobalSymbolId>;
    /** Extension symbols keyed to the interfaces they implement. */
    readonly implementationBySymbol: ReadonlyMap<LocalSymbolId, ExtensionImplementation>;
};

export const ExtensionTable = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ExtensionTable): void {
        encodeExtensionTable(writer, value);
    },

    /** Decode one ExtensionTable. */
    decode(reader: BinaryReader): ExtensionTable {
        return decodeExtensionTable(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ExtensionTable): Json {
        return toJsonExtensionTable(value);
    },

    /** Return one ExtensionTable from one JSON value. */
    fromJson(value: Json): ExtensionTable {
        return fromJsonExtensionTable(value);
    },
};

/** Encode one ExtensionTable. */
export function encodeExtensionTable(writer: BinaryWriter, value: ExtensionTable): void {
    encodeModuleId(writer, value.moduleId);
    const entries1 = Array.from(value.targetBySymbol.entries()).map(([key1, item1]) => {
        const keyBytes = nestedBytes((writer) => {
            encodeLocalSymbolId(writer, key1);
        });
        return { key1, item1, keyBytes };
    });
    entries1.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries1.length);
    for (const entry1 of entries1) {
        encodeLocalSymbolId(writer, entry1.key1);
        encodeGlobalSymbolId(writer, entry1.item1);
    }
    const entries2 = Array.from(value.implementationBySymbol.entries()).map(([key2, item2]) => {
        const keyBytes = nestedBytes((writer) => {
            encodeLocalSymbolId(writer, key2);
        });
        return { key2, item2, keyBytes };
    });
    entries2.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries2.length);
    for (const entry2 of entries2) {
        encodeLocalSymbolId(writer, entry2.key2);
        encodeExtensionImplementation(writer, entry2.item2);
    }
}

/** Decode one ExtensionTable. */
export function decodeExtensionTable(reader: BinaryReader): ExtensionTable {
    const moduleId = decodeModuleId(reader);
    const targetBySymbol = (() => { const length1 = reader.readNumber(); const items1 = new Map<LocalSymbolId, GlobalSymbolId>(); for (let index = 0; index < length1; index += 1) { items1.set(decodeLocalSymbolId(reader), decodeGlobalSymbolId(reader)); } return items1; })();
    const implementationBySymbol = (() => { const length2 = reader.readNumber(); const items2 = new Map<LocalSymbolId, ExtensionImplementation>(); for (let index = 0; index < length2; index += 1) { items2.set(decodeLocalSymbolId(reader), decodeExtensionImplementation(reader)); } return items2; })();

    return {
        moduleId,
        targetBySymbol,
        implementationBySymbol,
    };
}

/** Return one JSON value for one ExtensionTable. */
export function toJsonExtensionTable(value: ExtensionTable): Json {
    return {
        moduleId: toJsonModuleId(value.moduleId),
        targetBySymbol: Array.from(value.targetBySymbol.entries()).map(([key0, item0]) => [toJsonLocalSymbolId(key0), toJsonGlobalSymbolId(item0)] as const),
        implementationBySymbol: Array.from(value.implementationBySymbol.entries()).map(([key0, item0]) => [toJsonLocalSymbolId(key0), toJsonExtensionImplementation(item0)] as const),
    };
}

/** Return one ExtensionTable from one JSON value. */
export function fromJsonExtensionTable(value: Json): ExtensionTable {
    const object = jsonObject(value);

    return {
        moduleId: fromJsonModuleId(jsonField(object, "moduleId")),
        targetBySymbol: new Map(jsonArray(jsonField(object, "targetBySymbol")).map((entry) => { const items = jsonArray(entry); if (items.length !== 2) { throw new SerdeError(`expected JSON map entry length 2: ${items.length}`); } const key0 = items[0]; const item0 = items[1]; return [fromJsonLocalSymbolId(key0), fromJsonGlobalSymbolId(item0)] as const; })),
        implementationBySymbol: new Map(jsonArray(jsonField(object, "implementationBySymbol")).map((entry) => { const items = jsonArray(entry); if (items.length !== 2) { throw new SerdeError(`expected JSON map entry length 2: ${items.length}`); } const key0 = items[0]; const item0 = items[1]; return [fromJsonLocalSymbolId(key0), fromJsonExtensionImplementation(item0)] as const; })),
    };
}
