/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, compareBytes, jsonArray, jsonBigint, jsonField, jsonInteger, jsonObject, jsonOptional, jsonString, nestedBytes } from "../../../protocol/serde.js";
import type { StaticKey } from "../symbol/key.js";
import type { GlobalTypeId } from "../type/type.js";
import type { ModuleId } from "../../source/file/model/module.js";
import { decodeStaticKey, encodeStaticKey, fromJsonStaticKey, toJsonStaticKey } from "../symbol/key.js";
import { decodeGlobalTypeId, encodeGlobalTypeId, fromJsonGlobalTypeId, toJsonGlobalTypeId } from "../type/type.js";
import { decodeModuleId, encodeModuleId, fromJsonModuleId, toJsonModuleId } from "../../source/file/model/module.js";

/** Layouts added by one DIR phase. */
export type LayoutSegment = {
    /** The module id of the layout segment. */
    readonly moduleId: ModuleId;
    /** The first layout id owned by this table segment. */
    readonly firstLayoutId: number;
    /** Concrete layouts. */
    readonly layouts: ReadonlyArray<Layout>;
    /** Layout ids keyed by canonical type id. */
    readonly typeLayouts: ReadonlyMap<GlobalTypeId, LocalLayoutId>;
};

export const LayoutSegment = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: LayoutSegment): void {
        encodeLayoutSegment(writer, value);
    },

    /** Decode one LayoutSegment. */
    decode(reader: BinaryReader): LayoutSegment {
        return decodeLayoutSegment(reader);
    },

    /** Return this value as JSON. */
    toJson(value: LayoutSegment): Json {
        return toJsonLayoutSegment(value);
    },

    /** Return one LayoutSegment from one JSON value. */
    fromJson(value: Json): LayoutSegment {
        return fromJsonLayoutSegment(value);
    },
};

/** Encode one LayoutSegment. */
export function encodeLayoutSegment(writer: BinaryWriter, value: LayoutSegment): void {
    encodeModuleId(writer, value.moduleId);
    writer.writeUnsigned(value.firstLayoutId);
    writer.writeUnsigned(value.layouts.length);
    for (const item2 of value.layouts) {
        encodeLayout(writer, item2);
    }
    const entries3 = Array.from(value.typeLayouts.entries()).map(([key3, item3]) => {
        const keyBytes = nestedBytes((writer) => {
            encodeGlobalTypeId(writer, key3);
        });
        return { key3, item3, keyBytes };
    });
    entries3.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries3.length);
    for (const entry3 of entries3) {
        encodeGlobalTypeId(writer, entry3.key3);
        encodeLocalLayoutId(writer, entry3.item3);
    }
}

/** Decode one LayoutSegment. */
export function decodeLayoutSegment(reader: BinaryReader): LayoutSegment {
    const moduleId = decodeModuleId(reader);
    const firstLayoutId = reader.readNumber();
    const layouts = (() => { const length2 = reader.readNumber(); const items2: Array<Layout> = []; for (let index = 0; index < length2; index += 1) { items2.push(decodeLayout(reader)); } return items2; })();
    const typeLayouts = (() => { const length3 = reader.readNumber(); const items3 = new Map<GlobalTypeId, LocalLayoutId>(); for (let index = 0; index < length3; index += 1) { items3.set(decodeGlobalTypeId(reader), decodeLocalLayoutId(reader)); } return items3; })();

    return {
        moduleId,
        firstLayoutId,
        layouts,
        typeLayouts,
    };
}

/** Return one JSON value for one LayoutSegment. */
export function toJsonLayoutSegment(value: LayoutSegment): Json {
    return {
        moduleId: toJsonModuleId(value.moduleId),
        firstLayoutId: value.firstLayoutId,
        layouts: value.layouts.map((item0) => toJsonLayout(item0)),
        typeLayouts: Array.from(value.typeLayouts.entries()).map(([key0, item0]) => [toJsonGlobalTypeId(key0), toJsonLocalLayoutId(item0)] as const),
    };
}

/** Return one LayoutSegment from one JSON value. */
export function fromJsonLayoutSegment(value: Json): LayoutSegment {
    const object = jsonObject(value);

    return {
        moduleId: fromJsonModuleId(jsonField(object, "moduleId")),
        firstLayoutId: jsonInteger(jsonField(object, "firstLayoutId")),
        layouts: jsonArray(jsonField(object, "layouts")).map((item0) => fromJsonLayout(item0)),
        typeLayouts: new Map(jsonArray(jsonField(object, "typeLayouts")).map((entry) => { const items = jsonArray(entry); if (items.length !== 2) { throw new SerdeError(`expected JSON map entry length 2: ${items.length}`); } const key0 = items[0]; const item0 = items[1]; return [fromJsonGlobalTypeId(key0), fromJsonLocalLayoutId(item0)] as const; })),
    };
}

/** Concrete memory layout for a checked type. */
export type Layout = {
    /** The layout shape. */
    readonly shape: LayoutShape;
    /** The size in bytes. */
    readonly size: number;
    /** The alignment in bytes. */
    readonly alignment: number;
    /** The largest niche of free scalar values, when one exists. */
    readonly niche?: Niche;
};

export const Layout = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: Layout): void {
        encodeLayout(writer, value);
    },

    /** Decode one Layout. */
    decode(reader: BinaryReader): Layout {
        return decodeLayout(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Layout): Json {
        return toJsonLayout(value);
    },

    /** Return one Layout from one JSON value. */
    fromJson(value: Json): Layout {
        return fromJsonLayout(value);
    },
};

/** Encode one Layout. */
export function encodeLayout(writer: BinaryWriter, value: Layout): void {
    encodeLayoutShape(writer, value.shape);
    writer.writeUnsigned(value.size);
    writer.writeUnsigned(value.alignment);
    writer.writeOption(value.niche, (value3) => {
        encodeNiche(writer, value3);
    });
}

/** Decode one Layout. */
export function decodeLayout(reader: BinaryReader): Layout {
    const shape = decodeLayoutShape(reader);
    const size = reader.readNumber();
    const alignment = reader.readNumber();
    const niche = reader.readOption(() => decodeNiche(reader));

    return {
        shape,
        size,
        alignment,
        ...(niche === undefined ? {} : { niche }),
    };
}

/** Return one JSON value for one Layout. */
export function toJsonLayout(value: Layout): Json {
    return {
        shape: toJsonLayoutShape(value.shape),
        size: value.size,
        alignment: value.alignment,
        ...(value.niche === undefined ? {} : { niche: toJsonNiche(value.niche) }),
    };
}

/** Return one Layout from one JSON value. */
export function fromJsonLayout(value: Json): Layout {
    const object = jsonObject(value);

    return {
        shape: fromJsonLayoutShape(jsonField(object, "shape")),
        size: jsonInteger(jsonField(object, "size")),
        alignment: jsonInteger(jsonField(object, "alignment")),
        niche: jsonOptional(object, "niche", (value) => fromJsonNiche(value)),
    };
}

/** Concrete memory layout shape. */
export type LayoutShape =
    /** No runtime storage. */
    | {
          readonly kind: "none";
      }
    /** Builtin scalar storage. */
    | {
          readonly kind: "scalar";
      }
    /** Struct storage. */
    | {
          readonly kind: "struct";
          readonly struct: StructLayout;
      }
    /** Tuple storage. */
    | {
          readonly kind: "tuple";
          readonly tuple: TupleLayout;
      }
    /** Slice header storage. */
    | {
          readonly kind: "slice";
      }
    /** Fixed array storage. */
    | {
          readonly kind: "array";
          readonly array: ElementLayout;
      }
    /** Vector value storage. */
    | {
          readonly kind: "vector";
          readonly vector: ElementLayout;
      }
    /** Tensor handle storage. */
    | {
          readonly kind: "tensor";
          readonly tensor: TensorLayout;
      }
    /** Tensor view descriptor storage. */
    | {
          readonly kind: "tensorView";
          readonly tensor_view: TensorViewLayout;
      }
    /** Variant value storage. */
    | {
          readonly kind: "variant";
          readonly variant: VariantLayout;
      }
    /** Object storage with a dispatch table header. */
    | {
          readonly kind: "object";
          readonly object: ObjectLayout;
      }
    /** Runtime dynamic value storage. */
    | {
          readonly kind: "dynamic";
      }
    /** Runtime function value storage. */
    | {
          readonly kind: "function";
      }
    /** Transparent nominal storage. */
    | {
          readonly kind: "newtype";
          readonly newtype: NewtypeLayout;
      }
    /** Pointer storage slot for an indirectly stored value. */
    | {
          readonly kind: "pointer";
          readonly pointer: PointerLayout;
      }
;

export const LayoutShape = {
    /** No runtime storage. */
    none(): LayoutShape {
        return { kind: "none" };
    },

    /** Builtin scalar storage. */
    scalar(): LayoutShape {
        return { kind: "scalar" };
    },

    /** Struct storage. */
    struct(struct: StructLayout): LayoutShape {
        return { kind: "struct", struct };
    },

    /** Tuple storage. */
    tuple(tuple: TupleLayout): LayoutShape {
        return { kind: "tuple", tuple };
    },

    /** Slice header storage. */
    slice(): LayoutShape {
        return { kind: "slice" };
    },

    /** Fixed array storage. */
    array(array: ElementLayout): LayoutShape {
        return { kind: "array", array };
    },

    /** Vector value storage. */
    vector(vector: ElementLayout): LayoutShape {
        return { kind: "vector", vector };
    },

    /** Tensor handle storage. */
    tensor(tensor: TensorLayout): LayoutShape {
        return { kind: "tensor", tensor };
    },

    /** Tensor view descriptor storage. */
    tensorView(tensor_view: TensorViewLayout): LayoutShape {
        return { kind: "tensorView", tensor_view };
    },

    /** Variant value storage. */
    variant(variant: VariantLayout): LayoutShape {
        return { kind: "variant", variant };
    },

    /** Object storage with a dispatch table header. */
    "object"(object_: ObjectLayout): LayoutShape {
        return { kind: "object", object: object_ };
    },

    /** Runtime dynamic value storage. */
    dynamic(): LayoutShape {
        return { kind: "dynamic" };
    },

    /** Runtime function value storage. */
    "function"(): LayoutShape {
        return { kind: "function" };
    },

    /** Transparent nominal storage. */
    newtype(newtype: NewtypeLayout): LayoutShape {
        return { kind: "newtype", newtype };
    },

    /** Pointer storage slot for an indirectly stored value. */
    pointer(pointer: PointerLayout): LayoutShape {
        return { kind: "pointer", pointer };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: LayoutShape): void {
        encodeLayoutShape(writer, value);
    },

    /** Decode one LayoutShape. */
    decode(reader: BinaryReader): LayoutShape {
        return decodeLayoutShape(reader);
    },

    /** Return this value as JSON. */
    toJson(value: LayoutShape): Json {
        return toJsonLayoutShape(value);
    },

    /** Return one LayoutShape from one JSON value. */
    fromJson(value: Json): LayoutShape {
        return fromJsonLayoutShape(value);
    },
};

/** Encode one LayoutShape. */
export function encodeLayoutShape(writer: BinaryWriter, value: LayoutShape): void {
    switch (value.kind) {
        case "none":
            writer.writeUnsigned(0);
            return;
        case "scalar":
            writer.writeUnsigned(1);
            return;
        case "struct":
            writer.writeUnsigned(2);
            encodeStructLayout(writer, value.struct);
            return;
        case "tuple":
            writer.writeUnsigned(3);
            encodeTupleLayout(writer, value.tuple);
            return;
        case "slice":
            writer.writeUnsigned(4);
            return;
        case "array":
            writer.writeUnsigned(5);
            encodeElementLayout(writer, value.array);
            return;
        case "vector":
            writer.writeUnsigned(6);
            encodeElementLayout(writer, value.vector);
            return;
        case "tensor":
            writer.writeUnsigned(7);
            encodeTensorLayout(writer, value.tensor);
            return;
        case "tensorView":
            writer.writeUnsigned(8);
            encodeTensorViewLayout(writer, value.tensor_view);
            return;
        case "variant":
            writer.writeUnsigned(9);
            encodeVariantLayout(writer, value.variant);
            return;
        case "object":
            writer.writeUnsigned(10);
            encodeObjectLayout(writer, value.object);
            return;
        case "dynamic":
            writer.writeUnsigned(11);
            return;
        case "function":
            writer.writeUnsigned(12);
            return;
        case "newtype":
            writer.writeUnsigned(13);
            encodeNewtypeLayout(writer, value.newtype);
            return;
        case "pointer":
            writer.writeUnsigned(14);
            encodePointerLayout(writer, value.pointer);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one LayoutShape. */
export function decodeLayoutShape(reader: BinaryReader): LayoutShape {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            return { kind: "none" };
        }
        case 1: {
            return { kind: "scalar" };
        }
        case 2: {
            const struct = decodeStructLayout(reader);

            return { kind: "struct", struct };
        }
        case 3: {
            const tuple = decodeTupleLayout(reader);

            return { kind: "tuple", tuple };
        }
        case 4: {
            return { kind: "slice" };
        }
        case 5: {
            const array = decodeElementLayout(reader);

            return { kind: "array", array };
        }
        case 6: {
            const vector = decodeElementLayout(reader);

            return { kind: "vector", vector };
        }
        case 7: {
            const tensor = decodeTensorLayout(reader);

            return { kind: "tensor", tensor };
        }
        case 8: {
            const tensor_view = decodeTensorViewLayout(reader);

            return { kind: "tensorView", tensor_view };
        }
        case 9: {
            const variant = decodeVariantLayout(reader);

            return { kind: "variant", variant };
        }
        case 10: {
            const object_ = decodeObjectLayout(reader);

            return { kind: "object", object: object_ };
        }
        case 11: {
            return { kind: "dynamic" };
        }
        case 12: {
            return { kind: "function" };
        }
        case 13: {
            const newtype = decodeNewtypeLayout(reader);

            return { kind: "newtype", newtype };
        }
        case 14: {
            const pointer = decodePointerLayout(reader);

            return { kind: "pointer", pointer };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one LayoutShape. */
export function toJsonLayoutShape(value: LayoutShape): Json {
    switch (value.kind) {
        case "none":
            return {
                kind: "none",
            };
        case "scalar":
            return {
                kind: "scalar",
            };
        case "struct":
            return {
                kind: "struct",
                struct: toJsonStructLayout(value.struct),
            };
        case "tuple":
            return {
                kind: "tuple",
                tuple: toJsonTupleLayout(value.tuple),
            };
        case "slice":
            return {
                kind: "slice",
            };
        case "array":
            return {
                kind: "array",
                array: toJsonElementLayout(value.array),
            };
        case "vector":
            return {
                kind: "vector",
                vector: toJsonElementLayout(value.vector),
            };
        case "tensor":
            return {
                kind: "tensor",
                tensor: toJsonTensorLayout(value.tensor),
            };
        case "tensorView":
            return {
                kind: "tensorView",
                tensor_view: toJsonTensorViewLayout(value.tensor_view),
            };
        case "variant":
            return {
                kind: "variant",
                variant: toJsonVariantLayout(value.variant),
            };
        case "object":
            return {
                kind: "object",
                object: toJsonObjectLayout(value.object),
            };
        case "dynamic":
            return {
                kind: "dynamic",
            };
        case "function":
            return {
                kind: "function",
            };
        case "newtype":
            return {
                kind: "newtype",
                newtype: toJsonNewtypeLayout(value.newtype),
            };
        case "pointer":
            return {
                kind: "pointer",
                pointer: toJsonPointerLayout(value.pointer),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one LayoutShape from one JSON value. */
export function fromJsonLayoutShape(value: Json): LayoutShape {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "none":
            return {
                kind,
            };
        case "scalar":
            return {
                kind,
            };
        case "struct":
            return {
                kind,
                struct: fromJsonStructLayout(jsonField(object, "struct")),
            };
        case "tuple":
            return {
                kind,
                tuple: fromJsonTupleLayout(jsonField(object, "tuple")),
            };
        case "slice":
            return {
                kind,
            };
        case "array":
            return {
                kind,
                array: fromJsonElementLayout(jsonField(object, "array")),
            };
        case "vector":
            return {
                kind,
                vector: fromJsonElementLayout(jsonField(object, "vector")),
            };
        case "tensor":
            return {
                kind,
                tensor: fromJsonTensorLayout(jsonField(object, "tensor")),
            };
        case "tensorView":
            return {
                kind,
                tensor_view: fromJsonTensorViewLayout(jsonField(object, "tensor_view")),
            };
        case "variant":
            return {
                kind,
                variant: fromJsonVariantLayout(jsonField(object, "variant")),
            };
        case "object":
            return {
                kind,
                object: fromJsonObjectLayout(jsonField(object, "object")),
            };
        case "dynamic":
            return {
                kind,
            };
        case "function":
            return {
                kind,
            };
        case "newtype":
            return {
                kind,
                newtype: fromJsonNewtypeLayout(jsonField(object, "newtype")),
            };
        case "pointer":
            return {
                kind,
                pointer: fromJsonPointerLayout(jsonField(object, "pointer")),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** Concrete layout for a struct. */
export type StructLayout = {
    /** The fields in layout order. */
    readonly fields: ReadonlyArray<LayoutField>;
};

export const StructLayout = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: StructLayout): void {
        encodeStructLayout(writer, value);
    },

    /** Decode one StructLayout. */
    decode(reader: BinaryReader): StructLayout {
        return decodeStructLayout(reader);
    },

    /** Return this value as JSON. */
    toJson(value: StructLayout): Json {
        return toJsonStructLayout(value);
    },

    /** Return one StructLayout from one JSON value. */
    fromJson(value: Json): StructLayout {
        return fromJsonStructLayout(value);
    },
};

/** Encode one StructLayout. */
export function encodeStructLayout(writer: BinaryWriter, value: StructLayout): void {
    writer.writeUnsigned(value.fields.length);
    for (const item0 of value.fields) {
        encodeLayoutField(writer, item0);
    }
}

/** Decode one StructLayout. */
export function decodeStructLayout(reader: BinaryReader): StructLayout {
    const fields = (() => { const length0 = reader.readNumber(); const items0: Array<LayoutField> = []; for (let index = 0; index < length0; index += 1) { items0.push(decodeLayoutField(reader)); } return items0; })();

    return {
        fields,
    };
}

/** Return one JSON value for one StructLayout. */
export function toJsonStructLayout(value: StructLayout): Json {
    return {
        fields: value.fields.map((item0) => toJsonLayoutField(item0)),
    };
}

/** Return one StructLayout from one JSON value. */
export function fromJsonStructLayout(value: Json): StructLayout {
    const object = jsonObject(value);

    return {
        fields: jsonArray(jsonField(object, "fields")).map((item0) => fromJsonLayoutField(item0)),
    };
}

/** Concrete field or tuple-element layout. */
export type LayoutField = {
    /** The field key. */
    readonly key?: StaticKey;
    /** The field type. */
    readonly ty: GlobalTypeId;
    /** The field layout. */
    readonly layout: LocalLayoutId;
    /** The offset in bytes. */
    readonly offset: number;
    /** The size in bytes. */
    readonly size: number;
    /** The alignment in bytes. */
    readonly alignment: number;
};

export const LayoutField = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: LayoutField): void {
        encodeLayoutField(writer, value);
    },

    /** Decode one LayoutField. */
    decode(reader: BinaryReader): LayoutField {
        return decodeLayoutField(reader);
    },

    /** Return this value as JSON. */
    toJson(value: LayoutField): Json {
        return toJsonLayoutField(value);
    },

    /** Return one LayoutField from one JSON value. */
    fromJson(value: Json): LayoutField {
        return fromJsonLayoutField(value);
    },
};

/** Encode one LayoutField. */
export function encodeLayoutField(writer: BinaryWriter, value: LayoutField): void {
    writer.writeOption(value.key, (value0) => {
        encodeStaticKey(writer, value0);
    });
    encodeGlobalTypeId(writer, value.ty);
    encodeLocalLayoutId(writer, value.layout);
    writer.writeUnsigned(value.offset);
    writer.writeUnsigned(value.size);
    writer.writeUnsigned(value.alignment);
}

/** Decode one LayoutField. */
export function decodeLayoutField(reader: BinaryReader): LayoutField {
    const key = reader.readOption(() => decodeStaticKey(reader));
    const ty = decodeGlobalTypeId(reader);
    const layout = decodeLocalLayoutId(reader);
    const offset = reader.readNumber();
    const size = reader.readNumber();
    const alignment = reader.readNumber();

    return {
        ...(key === undefined ? {} : { key }),
        ty,
        layout,
        offset,
        size,
        alignment,
    };
}

/** Return one JSON value for one LayoutField. */
export function toJsonLayoutField(value: LayoutField): Json {
    return {
        ...(value.key === undefined ? {} : { key: toJsonStaticKey(value.key) }),
        ty: toJsonGlobalTypeId(value.ty),
        layout: toJsonLocalLayoutId(value.layout),
        offset: value.offset,
        size: value.size,
        alignment: value.alignment,
    };
}

/** Return one LayoutField from one JSON value. */
export function fromJsonLayoutField(value: Json): LayoutField {
    const object = jsonObject(value);

    return {
        key: jsonOptional(object, "key", (value) => fromJsonStaticKey(value)),
        ty: fromJsonGlobalTypeId(jsonField(object, "ty")),
        layout: fromJsonLocalLayoutId(jsonField(object, "layout")),
        offset: jsonInteger(jsonField(object, "offset")),
        size: jsonInteger(jsonField(object, "size")),
        alignment: jsonInteger(jsonField(object, "alignment")),
    };
}

/** Unique identifier for a concrete layout. */
export type LocalLayoutId = number;

export const LocalLayoutId = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: LocalLayoutId): void {
        encodeLocalLayoutId(writer, value);
    },

    /** Decode one LocalLayoutId. */
    decode(reader: BinaryReader): LocalLayoutId {
        return decodeLocalLayoutId(reader);
    },

    /** Return this value as JSON. */
    toJson(value: LocalLayoutId): Json {
        return toJsonLocalLayoutId(value);
    },

    /** Return one LocalLayoutId from one JSON value. */
    fromJson(value: Json): LocalLayoutId {
        return fromJsonLocalLayoutId(value);
    },
};

/** Encode one LocalLayoutId. */
export function encodeLocalLayoutId(writer: BinaryWriter, value: LocalLayoutId): void {
    writer.writeUnsigned(value);
}

/** Decode one LocalLayoutId. */
export function decodeLocalLayoutId(reader: BinaryReader): LocalLayoutId {
    return reader.readNumber();
}

/** Return one JSON value for one LocalLayoutId. */
export function toJsonLocalLayoutId(value: LocalLayoutId): Json {
    return value;
}

/** Return one LocalLayoutId from one JSON value. */
export function fromJsonLocalLayoutId(value: Json): LocalLayoutId {
    return jsonInteger(value);
}

/** Concrete layout for a tuple. */
export type TupleLayout = {
    /** The tuple elements in layout order. */
    readonly elements: ReadonlyArray<LayoutField>;
};

export const TupleLayout = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: TupleLayout): void {
        encodeTupleLayout(writer, value);
    },

    /** Decode one TupleLayout. */
    decode(reader: BinaryReader): TupleLayout {
        return decodeTupleLayout(reader);
    },

    /** Return this value as JSON. */
    toJson(value: TupleLayout): Json {
        return toJsonTupleLayout(value);
    },

    /** Return one TupleLayout from one JSON value. */
    fromJson(value: Json): TupleLayout {
        return fromJsonTupleLayout(value);
    },
};

/** Encode one TupleLayout. */
export function encodeTupleLayout(writer: BinaryWriter, value: TupleLayout): void {
    writer.writeUnsigned(value.elements.length);
    for (const item0 of value.elements) {
        encodeLayoutField(writer, item0);
    }
}

/** Decode one TupleLayout. */
export function decodeTupleLayout(reader: BinaryReader): TupleLayout {
    const elements = (() => { const length0 = reader.readNumber(); const items0: Array<LayoutField> = []; for (let index = 0; index < length0; index += 1) { items0.push(decodeLayoutField(reader)); } return items0; })();

    return {
        elements,
    };
}

/** Return one JSON value for one TupleLayout. */
export function toJsonTupleLayout(value: TupleLayout): Json {
    return {
        elements: value.elements.map((item0) => toJsonLayoutField(item0)),
    };
}

/** Return one TupleLayout from one JSON value. */
export function fromJsonTupleLayout(value: Json): TupleLayout {
    const object = jsonObject(value);

    return {
        elements: jsonArray(jsonField(object, "elements")).map((item0) => fromJsonLayoutField(item0)),
    };
}

/** Layout for inline indexed element storage. */
export type ElementLayout = {
    /** The stored element type. */
    readonly element: GlobalTypeId;
    /** The byte stride between elements. */
    readonly stride: number;
    /** The fixed element count when known. */
    readonly count: number;
};

export const ElementLayout = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ElementLayout): void {
        encodeElementLayout(writer, value);
    },

    /** Decode one ElementLayout. */
    decode(reader: BinaryReader): ElementLayout {
        return decodeElementLayout(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ElementLayout): Json {
        return toJsonElementLayout(value);
    },

    /** Return one ElementLayout from one JSON value. */
    fromJson(value: Json): ElementLayout {
        return fromJsonElementLayout(value);
    },
};

/** Encode one ElementLayout. */
export function encodeElementLayout(writer: BinaryWriter, value: ElementLayout): void {
    encodeGlobalTypeId(writer, value.element);
    writer.writeUnsigned(value.stride);
    writer.writeUnsigned(value.count);
}

/** Decode one ElementLayout. */
export function decodeElementLayout(reader: BinaryReader): ElementLayout {
    const element = decodeGlobalTypeId(reader);
    const stride = reader.readNumber();
    const count = reader.readNumber();

    return {
        element,
        stride,
        count,
    };
}

/** Return one JSON value for one ElementLayout. */
export function toJsonElementLayout(value: ElementLayout): Json {
    return {
        element: toJsonGlobalTypeId(value.element),
        stride: value.stride,
        count: value.count,
    };
}

/** Return one ElementLayout from one JSON value. */
export function fromJsonElementLayout(value: Json): ElementLayout {
    const object = jsonObject(value);

    return {
        element: fromJsonGlobalTypeId(jsonField(object, "element")),
        stride: jsonInteger(jsonField(object, "stride")),
        count: jsonInteger(jsonField(object, "count")),
    };
}

/** Concrete layout for a tensor handle. */
export type TensorLayout = {
    /** The tensor element type. */
    readonly element: GlobalTypeId;
    /** The tensor storage format. */
    readonly format: TensorFormat;
    /** The tensor placement. */
    readonly sharding: TensorSharding;
    /** The tensor rank. */
    readonly rank: number;
};

export const TensorLayout = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: TensorLayout): void {
        encodeTensorLayout(writer, value);
    },

    /** Decode one TensorLayout. */
    decode(reader: BinaryReader): TensorLayout {
        return decodeTensorLayout(reader);
    },

    /** Return this value as JSON. */
    toJson(value: TensorLayout): Json {
        return toJsonTensorLayout(value);
    },

    /** Return one TensorLayout from one JSON value. */
    fromJson(value: Json): TensorLayout {
        return fromJsonTensorLayout(value);
    },
};

/** Encode one TensorLayout. */
export function encodeTensorLayout(writer: BinaryWriter, value: TensorLayout): void {
    encodeGlobalTypeId(writer, value.element);
    encodeTensorFormat(writer, value.format);
    encodeTensorSharding(writer, value.sharding);
    writer.writeUnsigned(value.rank);
}

/** Decode one TensorLayout. */
export function decodeTensorLayout(reader: BinaryReader): TensorLayout {
    const element = decodeGlobalTypeId(reader);
    const format = decodeTensorFormat(reader);
    const sharding = decodeTensorSharding(reader);
    const rank = reader.readNumber();

    return {
        element,
        format,
        sharding,
        rank,
    };
}

/** Return one JSON value for one TensorLayout. */
export function toJsonTensorLayout(value: TensorLayout): Json {
    return {
        element: toJsonGlobalTypeId(value.element),
        format: toJsonTensorFormat(value.format),
        sharding: toJsonTensorSharding(value.sharding),
        rank: value.rank,
    };
}

/** Return one TensorLayout from one JSON value. */
export function fromJsonTensorLayout(value: Json): TensorLayout {
    const object = jsonObject(value);

    return {
        element: fromJsonGlobalTypeId(jsonField(object, "element")),
        format: fromJsonTensorFormat(jsonField(object, "format")),
        sharding: fromJsonTensorSharding(jsonField(object, "sharding")),
        rank: jsonInteger(jsonField(object, "rank")),
    };
}

/** Format for an owning tensor value. */
export type TensorFormat =
    /** Dense contiguous format. */
    | {
          readonly kind: "dense";
          /** The dimension order. */
          readonly order: TensorDimensionOrder;
      }
;

export const TensorFormat = {
    /** Dense contiguous format. */
    dense(order: TensorDimensionOrder): TensorFormat {
        return { kind: "dense", order };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: TensorFormat): void {
        encodeTensorFormat(writer, value);
    },

    /** Decode one TensorFormat. */
    decode(reader: BinaryReader): TensorFormat {
        return decodeTensorFormat(reader);
    },

    /** Return this value as JSON. */
    toJson(value: TensorFormat): Json {
        return toJsonTensorFormat(value);
    },

    /** Return one TensorFormat from one JSON value. */
    fromJson(value: Json): TensorFormat {
        return fromJsonTensorFormat(value);
    },
};

/** Encode one TensorFormat. */
export function encodeTensorFormat(writer: BinaryWriter, value: TensorFormat): void {
    switch (value.kind) {
        case "dense":
            writer.writeUnsigned(0);
            encodeTensorDimensionOrder(writer, value.order);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one TensorFormat. */
export function decodeTensorFormat(reader: BinaryReader): TensorFormat {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const order = decodeTensorDimensionOrder(reader);

            return {
                kind: "dense",
                order,
            };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one TensorFormat. */
export function toJsonTensorFormat(value: TensorFormat): Json {
    switch (value.kind) {
        case "dense":
            return {
                kind: "dense",
                order: toJsonTensorDimensionOrder(value.order),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one TensorFormat from one JSON value. */
export function fromJsonTensorFormat(value: Json): TensorFormat {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "dense":
            return {
                kind,
                order: fromJsonTensorDimensionOrder(jsonField(object, "order")),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** Dimension order for dense tensor storage. */
export type TensorDimensionOrder = "rowMajor" | "columnMajor";

export const TensorDimensionOrder = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: TensorDimensionOrder): void {
        encodeTensorDimensionOrder(writer, value);
    },

    /** Decode one TensorDimensionOrder. */
    decode(reader: BinaryReader): TensorDimensionOrder {
        return decodeTensorDimensionOrder(reader);
    },

    /** Return this value as JSON. */
    toJson(value: TensorDimensionOrder): Json {
        return toJsonTensorDimensionOrder(value);
    },

    /** Return one TensorDimensionOrder from one JSON value. */
    fromJson(value: Json): TensorDimensionOrder {
        return fromJsonTensorDimensionOrder(value);
    },
};

/** Encode one TensorDimensionOrder. */
export function encodeTensorDimensionOrder(writer: BinaryWriter, value: TensorDimensionOrder): void {
    switch (value) {
        case "rowMajor":
            writer.writeUnsigned(0);
            return;
        case "columnMajor":
            writer.writeUnsigned(1);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one TensorDimensionOrder. */
export function decodeTensorDimensionOrder(reader: BinaryReader): TensorDimensionOrder {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "rowMajor";
        case 1:
            return "columnMajor";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one TensorDimensionOrder. */
export function toJsonTensorDimensionOrder(value: TensorDimensionOrder): Json {
    return value;
}

/** Return one TensorDimensionOrder from one JSON value. */
export function fromJsonTensorDimensionOrder(value: Json): TensorDimensionOrder {
    const variant = jsonString(value);

    switch (variant) {
        case "rowMajor":
            return "rowMajor";
        case "columnMajor":
            return "columnMajor";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** Placement descriptor for tensor storage. */
export type TensorSharding =
    /** Tensor storage is not partitioned across a mesh. */
    | {
          readonly kind: "unsharded";
      }
    /** Tensor storage is mapped across a mesh axis by axis. */
    | {
          readonly kind: "sharding";
          /** The per-axis placement descriptors. */
          readonly axes: ReadonlyArray<TensorShardingAxis>;
      }
;

export const TensorSharding = {
    /** Tensor storage is not partitioned across a mesh. */
    unsharded(): TensorSharding {
        return { kind: "unsharded" };
    },

    /** Tensor storage is mapped across a mesh axis by axis. */
    sharding(axes: ReadonlyArray<TensorShardingAxis>): TensorSharding {
        return { kind: "sharding", axes };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: TensorSharding): void {
        encodeTensorSharding(writer, value);
    },

    /** Decode one TensorSharding. */
    decode(reader: BinaryReader): TensorSharding {
        return decodeTensorSharding(reader);
    },

    /** Return this value as JSON. */
    toJson(value: TensorSharding): Json {
        return toJsonTensorSharding(value);
    },

    /** Return one TensorSharding from one JSON value. */
    fromJson(value: Json): TensorSharding {
        return fromJsonTensorSharding(value);
    },
};

/** Encode one TensorSharding. */
export function encodeTensorSharding(writer: BinaryWriter, value: TensorSharding): void {
    switch (value.kind) {
        case "unsharded":
            writer.writeUnsigned(0);
            return;
        case "sharding":
            writer.writeUnsigned(1);
            writer.writeUnsigned(value.axes.length);
            for (const item0 of value.axes) {
                encodeTensorShardingAxis(writer, item0);
            }
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one TensorSharding. */
export function decodeTensorSharding(reader: BinaryReader): TensorSharding {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            return { kind: "unsharded" };
        }
        case 1: {
            const axes = (() => { const length0 = reader.readNumber(); const items0: Array<TensorShardingAxis> = []; for (let index = 0; index < length0; index += 1) { items0.push(decodeTensorShardingAxis(reader)); } return items0; })();

            return {
                kind: "sharding",
                axes,
            };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one TensorSharding. */
export function toJsonTensorSharding(value: TensorSharding): Json {
    switch (value.kind) {
        case "unsharded":
            return {
                kind: "unsharded",
            };
        case "sharding":
            return {
                kind: "sharding",
                axes: value.axes.map((item0) => toJsonTensorShardingAxis(item0)),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one TensorSharding from one JSON value. */
export function fromJsonTensorSharding(value: Json): TensorSharding {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "unsharded":
            return {
                kind,
            };
        case "sharding":
            return {
                kind,
                axes: jsonArray(jsonField(object, "axes")).map((item0) => fromJsonTensorShardingAxis(item0)),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** Per-axis placement descriptor for a sharded tensor. */
export type TensorShardingAxis =
    /** Split one tensor axis across one mesh axis. */
    | {
          readonly kind: "shard";
          /** The tensor axis being split. */
          readonly axis: number;
      }
    /** Replicate values across one mesh axis. */
    | {
          readonly kind: "replicate";
      }
    /** Store partial results across one mesh axis. */
    | {
          readonly kind: "partial";
          /** The reduction used to combine partial values. */
          readonly reduction: TensorReduction;
      }
;

export const TensorShardingAxis = {
    /** Split one tensor axis across one mesh axis. */
    shard(axis: number): TensorShardingAxis {
        return { kind: "shard", axis };
    },

    /** Replicate values across one mesh axis. */
    replicate(): TensorShardingAxis {
        return { kind: "replicate" };
    },

    /** Store partial results across one mesh axis. */
    partial(reduction: TensorReduction): TensorShardingAxis {
        return { kind: "partial", reduction };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: TensorShardingAxis): void {
        encodeTensorShardingAxis(writer, value);
    },

    /** Decode one TensorShardingAxis. */
    decode(reader: BinaryReader): TensorShardingAxis {
        return decodeTensorShardingAxis(reader);
    },

    /** Return this value as JSON. */
    toJson(value: TensorShardingAxis): Json {
        return toJsonTensorShardingAxis(value);
    },

    /** Return one TensorShardingAxis from one JSON value. */
    fromJson(value: Json): TensorShardingAxis {
        return fromJsonTensorShardingAxis(value);
    },
};

/** Encode one TensorShardingAxis. */
export function encodeTensorShardingAxis(writer: BinaryWriter, value: TensorShardingAxis): void {
    switch (value.kind) {
        case "shard":
            writer.writeUnsigned(0);
            writer.writeSigned(value.axis);
            return;
        case "replicate":
            writer.writeUnsigned(1);
            return;
        case "partial":
            writer.writeUnsigned(2);
            encodeTensorReduction(writer, value.reduction);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one TensorShardingAxis. */
export function decodeTensorShardingAxis(reader: BinaryReader): TensorShardingAxis {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const axis = reader.readSignedNumber();

            return {
                kind: "shard",
                axis,
            };
        }
        case 1: {
            return { kind: "replicate" };
        }
        case 2: {
            const reduction = decodeTensorReduction(reader);

            return {
                kind: "partial",
                reduction,
            };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one TensorShardingAxis. */
export function toJsonTensorShardingAxis(value: TensorShardingAxis): Json {
    switch (value.kind) {
        case "shard":
            return {
                kind: "shard",
                axis: value.axis,
            };
        case "replicate":
            return {
                kind: "replicate",
            };
        case "partial":
            return {
                kind: "partial",
                reduction: toJsonTensorReduction(value.reduction),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one TensorShardingAxis from one JSON value. */
export function fromJsonTensorShardingAxis(value: Json): TensorShardingAxis {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "shard":
            return {
                kind,
                axis: jsonInteger(jsonField(object, "axis")),
            };
        case "replicate":
            return {
                kind,
            };
        case "partial":
            return {
                kind,
                reduction: fromJsonTensorReduction(jsonField(object, "reduction")),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** Reduction used when partial tensor shards are combined. */
export type TensorReduction = "add" | "multiply" | "minimum" | "maximum" | "and" | "or";

export const TensorReduction = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: TensorReduction): void {
        encodeTensorReduction(writer, value);
    },

    /** Decode one TensorReduction. */
    decode(reader: BinaryReader): TensorReduction {
        return decodeTensorReduction(reader);
    },

    /** Return this value as JSON. */
    toJson(value: TensorReduction): Json {
        return toJsonTensorReduction(value);
    },

    /** Return one TensorReduction from one JSON value. */
    fromJson(value: Json): TensorReduction {
        return fromJsonTensorReduction(value);
    },
};

/** Encode one TensorReduction. */
export function encodeTensorReduction(writer: BinaryWriter, value: TensorReduction): void {
    switch (value) {
        case "add":
            writer.writeUnsigned(0);
            return;
        case "multiply":
            writer.writeUnsigned(1);
            return;
        case "minimum":
            writer.writeUnsigned(2);
            return;
        case "maximum":
            writer.writeUnsigned(3);
            return;
        case "and":
            writer.writeUnsigned(4);
            return;
        case "or":
            writer.writeUnsigned(5);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one TensorReduction. */
export function decodeTensorReduction(reader: BinaryReader): TensorReduction {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "add";
        case 1:
            return "multiply";
        case 2:
            return "minimum";
        case 3:
            return "maximum";
        case 4:
            return "and";
        case 5:
            return "or";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one TensorReduction. */
export function toJsonTensorReduction(value: TensorReduction): Json {
    return value;
}

/** Return one TensorReduction from one JSON value. */
export function fromJsonTensorReduction(value: Json): TensorReduction {
    const variant = jsonString(value);

    switch (variant) {
        case "add":
            return "add";
        case "multiply":
            return "multiply";
        case "minimum":
            return "minimum";
        case "maximum":
            return "maximum";
        case "and":
            return "and";
        case "or":
            return "or";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** Concrete layout for a tensor view descriptor. */
export type TensorViewLayout = {
    /** The viewed element type. */
    readonly element: GlobalTypeId;
    /** The tensor view format. */
    readonly format: TensorViewFormat;
    /** The tensor placement. */
    readonly sharding: TensorSharding;
    /** The tensor rank. */
    readonly rank: number;
};

export const TensorViewLayout = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: TensorViewLayout): void {
        encodeTensorViewLayout(writer, value);
    },

    /** Decode one TensorViewLayout. */
    decode(reader: BinaryReader): TensorViewLayout {
        return decodeTensorViewLayout(reader);
    },

    /** Return this value as JSON. */
    toJson(value: TensorViewLayout): Json {
        return toJsonTensorViewLayout(value);
    },

    /** Return one TensorViewLayout from one JSON value. */
    fromJson(value: Json): TensorViewLayout {
        return fromJsonTensorViewLayout(value);
    },
};

/** Encode one TensorViewLayout. */
export function encodeTensorViewLayout(writer: BinaryWriter, value: TensorViewLayout): void {
    encodeGlobalTypeId(writer, value.element);
    encodeTensorViewFormat(writer, value.format);
    encodeTensorSharding(writer, value.sharding);
    writer.writeUnsigned(value.rank);
}

/** Decode one TensorViewLayout. */
export function decodeTensorViewLayout(reader: BinaryReader): TensorViewLayout {
    const element = decodeGlobalTypeId(reader);
    const format = decodeTensorViewFormat(reader);
    const sharding = decodeTensorSharding(reader);
    const rank = reader.readNumber();

    return {
        element,
        format,
        sharding,
        rank,
    };
}

/** Return one JSON value for one TensorViewLayout. */
export function toJsonTensorViewLayout(value: TensorViewLayout): Json {
    return {
        element: toJsonGlobalTypeId(value.element),
        format: toJsonTensorViewFormat(value.format),
        sharding: toJsonTensorSharding(value.sharding),
        rank: value.rank,
    };
}

/** Return one TensorViewLayout from one JSON value. */
export function fromJsonTensorViewLayout(value: Json): TensorViewLayout {
    const object = jsonObject(value);

    return {
        element: fromJsonGlobalTypeId(jsonField(object, "element")),
        format: fromJsonTensorViewFormat(jsonField(object, "format")),
        sharding: fromJsonTensorSharding(jsonField(object, "sharding")),
        rank: jsonInteger(jsonField(object, "rank")),
    };
}

/** Format descriptor for a tensor view. */
export type TensorViewFormat =
    /** Dense contiguous view. */
    | {
          readonly kind: "dense";
          /** The dimension order. */
          readonly order: TensorDimensionOrder;
      }
    /** Explicit strided view. */
    | {
          readonly kind: "strided";
      }
;

export const TensorViewFormat = {
    /** Dense contiguous view. */
    dense(order: TensorDimensionOrder): TensorViewFormat {
        return { kind: "dense", order };
    },

    /** Explicit strided view. */
    strided(): TensorViewFormat {
        return { kind: "strided" };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: TensorViewFormat): void {
        encodeTensorViewFormat(writer, value);
    },

    /** Decode one TensorViewFormat. */
    decode(reader: BinaryReader): TensorViewFormat {
        return decodeTensorViewFormat(reader);
    },

    /** Return this value as JSON. */
    toJson(value: TensorViewFormat): Json {
        return toJsonTensorViewFormat(value);
    },

    /** Return one TensorViewFormat from one JSON value. */
    fromJson(value: Json): TensorViewFormat {
        return fromJsonTensorViewFormat(value);
    },
};

/** Encode one TensorViewFormat. */
export function encodeTensorViewFormat(writer: BinaryWriter, value: TensorViewFormat): void {
    switch (value.kind) {
        case "dense":
            writer.writeUnsigned(0);
            encodeTensorDimensionOrder(writer, value.order);
            return;
        case "strided":
            writer.writeUnsigned(1);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one TensorViewFormat. */
export function decodeTensorViewFormat(reader: BinaryReader): TensorViewFormat {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const order = decodeTensorDimensionOrder(reader);

            return {
                kind: "dense",
                order,
            };
        }
        case 1: {
            return { kind: "strided" };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one TensorViewFormat. */
export function toJsonTensorViewFormat(value: TensorViewFormat): Json {
    switch (value.kind) {
        case "dense":
            return {
                kind: "dense",
                order: toJsonTensorDimensionOrder(value.order),
            };
        case "strided":
            return {
                kind: "strided",
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one TensorViewFormat from one JSON value. */
export function fromJsonTensorViewFormat(value: Json): TensorViewFormat {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "dense":
            return {
                kind,
                order: fromJsonTensorDimensionOrder(jsonField(object, "order")),
            };
        case "strided":
            return {
                kind,
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** Concrete layout for a variant value. */
export type VariantLayout = {
    /** The tag layout. */
    readonly tag: VariantTagLayout;
    /** The variant payload byte offset. */
    readonly payloadOffset?: number;
    /** The variant cases. */
    readonly variants: ReadonlyArray<VariantCaseLayout>;
};

export const VariantLayout = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: VariantLayout): void {
        encodeVariantLayout(writer, value);
    },

    /** Decode one VariantLayout. */
    decode(reader: BinaryReader): VariantLayout {
        return decodeVariantLayout(reader);
    },

    /** Return this value as JSON. */
    toJson(value: VariantLayout): Json {
        return toJsonVariantLayout(value);
    },

    /** Return one VariantLayout from one JSON value. */
    fromJson(value: Json): VariantLayout {
        return fromJsonVariantLayout(value);
    },
};

/** Encode one VariantLayout. */
export function encodeVariantLayout(writer: BinaryWriter, value: VariantLayout): void {
    encodeVariantTagLayout(writer, value.tag);
    writer.writeOption(value.payloadOffset, (value1) => {
        writer.writeUnsigned(value1);
    });
    writer.writeUnsigned(value.variants.length);
    for (const item2 of value.variants) {
        encodeVariantCaseLayout(writer, item2);
    }
}

/** Decode one VariantLayout. */
export function decodeVariantLayout(reader: BinaryReader): VariantLayout {
    const tag = decodeVariantTagLayout(reader);
    const payloadOffset = reader.readOption(() => reader.readNumber());
    const variants = (() => { const length2 = reader.readNumber(); const items2: Array<VariantCaseLayout> = []; for (let index = 0; index < length2; index += 1) { items2.push(decodeVariantCaseLayout(reader)); } return items2; })();

    return {
        tag,
        ...(payloadOffset === undefined ? {} : { payloadOffset }),
        variants,
    };
}

/** Return one JSON value for one VariantLayout. */
export function toJsonVariantLayout(value: VariantLayout): Json {
    return {
        tag: toJsonVariantTagLayout(value.tag),
        ...(value.payloadOffset === undefined ? {} : { payloadOffset: value.payloadOffset }),
        variants: value.variants.map((item0) => toJsonVariantCaseLayout(item0)),
    };
}

/** Return one VariantLayout from one JSON value. */
export function fromJsonVariantLayout(value: Json): VariantLayout {
    const object = jsonObject(value);

    return {
        tag: fromJsonVariantTagLayout(jsonField(object, "tag")),
        payloadOffset: jsonOptional(object, "payloadOffset", (value) => jsonInteger(value)),
        variants: jsonArray(jsonField(object, "variants")).map((item0) => fromJsonVariantCaseLayout(item0)),
    };
}

/** Concrete layout for a variant tag. */
export type VariantTagLayout = {
    /** The tag type when it has been materialized. */
    readonly ty?: GlobalTypeId;
    /** The tag size in bytes. */
    readonly size: number;
    /** The tag alignment in bytes. */
    readonly alignment: number;
};

export const VariantTagLayout = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: VariantTagLayout): void {
        encodeVariantTagLayout(writer, value);
    },

    /** Decode one VariantTagLayout. */
    decode(reader: BinaryReader): VariantTagLayout {
        return decodeVariantTagLayout(reader);
    },

    /** Return this value as JSON. */
    toJson(value: VariantTagLayout): Json {
        return toJsonVariantTagLayout(value);
    },

    /** Return one VariantTagLayout from one JSON value. */
    fromJson(value: Json): VariantTagLayout {
        return fromJsonVariantTagLayout(value);
    },
};

/** Encode one VariantTagLayout. */
export function encodeVariantTagLayout(writer: BinaryWriter, value: VariantTagLayout): void {
    writer.writeOption(value.ty, (value0) => {
        encodeGlobalTypeId(writer, value0);
    });
    writer.writeUnsigned(value.size);
    writer.writeUnsigned(value.alignment);
}

/** Decode one VariantTagLayout. */
export function decodeVariantTagLayout(reader: BinaryReader): VariantTagLayout {
    const ty = reader.readOption(() => decodeGlobalTypeId(reader));
    const size = reader.readNumber();
    const alignment = reader.readNumber();

    return {
        ...(ty === undefined ? {} : { ty }),
        size,
        alignment,
    };
}

/** Return one JSON value for one VariantTagLayout. */
export function toJsonVariantTagLayout(value: VariantTagLayout): Json {
    return {
        ...(value.ty === undefined ? {} : { ty: toJsonGlobalTypeId(value.ty) }),
        size: value.size,
        alignment: value.alignment,
    };
}

/** Return one VariantTagLayout from one JSON value. */
export function fromJsonVariantTagLayout(value: Json): VariantTagLayout {
    const object = jsonObject(value);

    return {
        ty: jsonOptional(object, "ty", (value) => fromJsonGlobalTypeId(value)),
        size: jsonInteger(jsonField(object, "size")),
        alignment: jsonInteger(jsonField(object, "alignment")),
    };
}

/** Concrete layout for one variant case. */
export type VariantCaseLayout = {
    /** The logical case type. */
    readonly ty: GlobalTypeId;
    /** The case layout. */
    readonly layout: LocalLayoutId;
};

export const VariantCaseLayout = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: VariantCaseLayout): void {
        encodeVariantCaseLayout(writer, value);
    },

    /** Decode one VariantCaseLayout. */
    decode(reader: BinaryReader): VariantCaseLayout {
        return decodeVariantCaseLayout(reader);
    },

    /** Return this value as JSON. */
    toJson(value: VariantCaseLayout): Json {
        return toJsonVariantCaseLayout(value);
    },

    /** Return one VariantCaseLayout from one JSON value. */
    fromJson(value: Json): VariantCaseLayout {
        return fromJsonVariantCaseLayout(value);
    },
};

/** Encode one VariantCaseLayout. */
export function encodeVariantCaseLayout(writer: BinaryWriter, value: VariantCaseLayout): void {
    encodeGlobalTypeId(writer, value.ty);
    encodeLocalLayoutId(writer, value.layout);
}

/** Decode one VariantCaseLayout. */
export function decodeVariantCaseLayout(reader: BinaryReader): VariantCaseLayout {
    const ty = decodeGlobalTypeId(reader);
    const layout = decodeLocalLayoutId(reader);

    return {
        ty,
        layout,
    };
}

/** Return one JSON value for one VariantCaseLayout. */
export function toJsonVariantCaseLayout(value: VariantCaseLayout): Json {
    return {
        ty: toJsonGlobalTypeId(value.ty),
        layout: toJsonLocalLayoutId(value.layout),
    };
}

/** Return one VariantCaseLayout from one JSON value. */
export function fromJsonVariantCaseLayout(value: Json): VariantCaseLayout {
    const object = jsonObject(value);

    return {
        ty: fromJsonGlobalTypeId(jsonField(object, "ty")),
        layout: fromJsonLocalLayoutId(jsonField(object, "layout")),
    };
}

/** Concrete layout for an object. */
export type ObjectLayout = {
    /** The fields in layout order. */
    readonly fields: ReadonlyArray<LayoutField>;
};

export const ObjectLayout = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ObjectLayout): void {
        encodeObjectLayout(writer, value);
    },

    /** Decode one ObjectLayout. */
    decode(reader: BinaryReader): ObjectLayout {
        return decodeObjectLayout(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ObjectLayout): Json {
        return toJsonObjectLayout(value);
    },

    /** Return one ObjectLayout from one JSON value. */
    fromJson(value: Json): ObjectLayout {
        return fromJsonObjectLayout(value);
    },
};

/** Encode one ObjectLayout. */
export function encodeObjectLayout(writer: BinaryWriter, value: ObjectLayout): void {
    writer.writeUnsigned(value.fields.length);
    for (const item0 of value.fields) {
        encodeLayoutField(writer, item0);
    }
}

/** Decode one ObjectLayout. */
export function decodeObjectLayout(reader: BinaryReader): ObjectLayout {
    const fields = (() => { const length0 = reader.readNumber(); const items0: Array<LayoutField> = []; for (let index = 0; index < length0; index += 1) { items0.push(decodeLayoutField(reader)); } return items0; })();

    return {
        fields,
    };
}

/** Return one JSON value for one ObjectLayout. */
export function toJsonObjectLayout(value: ObjectLayout): Json {
    return {
        fields: value.fields.map((item0) => toJsonLayoutField(item0)),
    };
}

/** Return one ObjectLayout from one JSON value. */
export function fromJsonObjectLayout(value: Json): ObjectLayout {
    const object = jsonObject(value);

    return {
        fields: jsonArray(jsonField(object, "fields")).map((item0) => fromJsonLayoutField(item0)),
    };
}

/** Concrete layout for a nominal newtype. */
export type NewtypeLayout = {
    /** The backing type. */
    readonly backingType: GlobalTypeId;
    /** The backing type layout. */
    readonly backingLayout: LocalLayoutId;
};

export const NewtypeLayout = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: NewtypeLayout): void {
        encodeNewtypeLayout(writer, value);
    },

    /** Decode one NewtypeLayout. */
    decode(reader: BinaryReader): NewtypeLayout {
        return decodeNewtypeLayout(reader);
    },

    /** Return this value as JSON. */
    toJson(value: NewtypeLayout): Json {
        return toJsonNewtypeLayout(value);
    },

    /** Return one NewtypeLayout from one JSON value. */
    fromJson(value: Json): NewtypeLayout {
        return fromJsonNewtypeLayout(value);
    },
};

/** Encode one NewtypeLayout. */
export function encodeNewtypeLayout(writer: BinaryWriter, value: NewtypeLayout): void {
    encodeGlobalTypeId(writer, value.backingType);
    encodeLocalLayoutId(writer, value.backingLayout);
}

/** Decode one NewtypeLayout. */
export function decodeNewtypeLayout(reader: BinaryReader): NewtypeLayout {
    const backingType = decodeGlobalTypeId(reader);
    const backingLayout = decodeLocalLayoutId(reader);

    return {
        backingType,
        backingLayout,
    };
}

/** Return one JSON value for one NewtypeLayout. */
export function toJsonNewtypeLayout(value: NewtypeLayout): Json {
    return {
        backingType: toJsonGlobalTypeId(value.backingType),
        backingLayout: toJsonLocalLayoutId(value.backingLayout),
    };
}

/** Return one NewtypeLayout from one JSON value. */
export function fromJsonNewtypeLayout(value: Json): NewtypeLayout {
    const object = jsonObject(value);

    return {
        backingType: fromJsonGlobalTypeId(jsonField(object, "backingType")),
        backingLayout: fromJsonLocalLayoutId(jsonField(object, "backingLayout")),
    };
}

/** Concrete layout for a pointer storage slot. */
export type PointerLayout = {
    /** The pointed-to value type. */
    readonly pointee: GlobalTypeId;
};

export const PointerLayout = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: PointerLayout): void {
        encodePointerLayout(writer, value);
    },

    /** Decode one PointerLayout. */
    decode(reader: BinaryReader): PointerLayout {
        return decodePointerLayout(reader);
    },

    /** Return this value as JSON. */
    toJson(value: PointerLayout): Json {
        return toJsonPointerLayout(value);
    },

    /** Return one PointerLayout from one JSON value. */
    fromJson(value: Json): PointerLayout {
        return fromJsonPointerLayout(value);
    },
};

/** Encode one PointerLayout. */
export function encodePointerLayout(writer: BinaryWriter, value: PointerLayout): void {
    encodeGlobalTypeId(writer, value.pointee);
}

/** Decode one PointerLayout. */
export function decodePointerLayout(reader: BinaryReader): PointerLayout {
    const pointee = decodeGlobalTypeId(reader);

    return {
        pointee,
    };
}

/** Return one JSON value for one PointerLayout. */
export function toJsonPointerLayout(value: PointerLayout): Json {
    return {
        pointee: toJsonGlobalTypeId(value.pointee),
    };
}

/** Return one PointerLayout from one JSON value. */
export function fromJsonPointerLayout(value: Json): PointerLayout {
    const object = jsonObject(value);

    return {
        pointee: fromJsonGlobalTypeId(jsonField(object, "pointee")),
    };
}

/** One niche of free values inside a layout. */
export type Niche = {
    /** The byte offset of the niched scalar. */
    readonly offset: number;
    /** The niched scalar width in bytes. */
    readonly width: number;
    /** The first valid value stored by the type. */
    readonly start: bigint;
    /** The last valid value stored by the type. */
    readonly end: bigint;
};

export const Niche = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: Niche): void {
        encodeNiche(writer, value);
    },

    /** Decode one Niche. */
    decode(reader: BinaryReader): Niche {
        return decodeNiche(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Niche): Json {
        return toJsonNiche(value);
    },

    /** Return one Niche from one JSON value. */
    fromJson(value: Json): Niche {
        return fromJsonNiche(value);
    },
};

/** Encode one Niche. */
export function encodeNiche(writer: BinaryWriter, value: Niche): void {
    writer.writeUnsigned(value.offset);
    writer.writeUnsigned(value.width);
    writer.writeUnsigned(value.start);
    writer.writeUnsigned(value.end);
}

/** Decode one Niche. */
export function decodeNiche(reader: BinaryReader): Niche {
    const offset = reader.readNumber();
    const width = reader.readNumber();
    const start = reader.readUnsigned();
    const end = reader.readUnsigned();

    return {
        offset,
        width,
        start,
        end,
    };
}

/** Return one JSON value for one Niche. */
export function toJsonNiche(value: Niche): Json {
    return {
        offset: value.offset,
        width: value.width,
        start: value.start.toString(),
        end: value.end.toString(),
    };
}

/** Return one Niche from one JSON value. */
export function fromJsonNiche(value: Json): Niche {
    const object = jsonObject(value);

    return {
        offset: jsonInteger(jsonField(object, "offset")),
        width: jsonInteger(jsonField(object, "width")),
        start: jsonBigint(jsonField(object, "start")),
        end: jsonBigint(jsonField(object, "end")),
    };
}
