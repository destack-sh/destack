/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, compareBytes, jsonArray, jsonField, jsonInteger, jsonObject, nestedBytes } from "../../../protocol/serde.js";
import type { GlobalNodeIdAny } from "../tree/node.js";
import type { Path } from "../tree/path.js";
import type { AccessResolution } from "../type/access.js";
import type { ReceiverResolution } from "../type/receiver.js";
import type { AssignPatternResolution } from "../type/resolution.js";
import type { AssignmentResolution } from "../type/resolution.js";
import type { ConstructResolution } from "../type/resolution.js";
import type { GuardResolution } from "../type/resolution.js";
import type { InstantiationResolution } from "../type/resolution.js";
import type { LabelResolution } from "../type/resolution.js";
import type { NameResolution } from "../type/resolution.js";
import type { OperationResolution } from "../type/resolution.js";
import type { PatternResolution } from "../type/resolution.js";
import type { PlaceResolution } from "../type/resolution.js";
import type { TreeResolution } from "../type/resolution.js";
import type { ModuleId } from "../../source/file/model/module.js";
import { decodeGlobalNodeIdAny, encodeGlobalNodeIdAny, fromJsonGlobalNodeIdAny, toJsonGlobalNodeIdAny } from "../tree/node.js";
import { decodePath, encodePath, fromJsonPath, toJsonPath } from "../tree/path.js";
import { decodeAccessResolution, encodeAccessResolution, fromJsonAccessResolution, toJsonAccessResolution } from "../type/access.js";
import { decodeReceiverResolution, encodeReceiverResolution, fromJsonReceiverResolution, toJsonReceiverResolution } from "../type/receiver.js";
import { decodeAssignPatternResolution, encodeAssignPatternResolution, fromJsonAssignPatternResolution, toJsonAssignPatternResolution } from "../type/resolution.js";
import { decodeAssignmentResolution, encodeAssignmentResolution, fromJsonAssignmentResolution, toJsonAssignmentResolution } from "../type/resolution.js";
import { decodeConstructResolution, encodeConstructResolution, fromJsonConstructResolution, toJsonConstructResolution } from "../type/resolution.js";
import { decodeGuardResolution, encodeGuardResolution, fromJsonGuardResolution, toJsonGuardResolution } from "../type/resolution.js";
import { decodeInstantiationResolution, encodeInstantiationResolution, fromJsonInstantiationResolution, toJsonInstantiationResolution } from "../type/resolution.js";
import { decodeLabelResolution, encodeLabelResolution, fromJsonLabelResolution, toJsonLabelResolution } from "../type/resolution.js";
import { decodeNameResolution, encodeNameResolution, fromJsonNameResolution, toJsonNameResolution } from "../type/resolution.js";
import { decodeOperationResolution, encodeOperationResolution, fromJsonOperationResolution, toJsonOperationResolution } from "../type/resolution.js";
import { decodePatternResolution, encodePatternResolution, fromJsonPatternResolution, toJsonPatternResolution } from "../type/resolution.js";
import { decodePlaceResolution, encodePlaceResolution, fromJsonPlaceResolution, toJsonPlaceResolution } from "../type/resolution.js";
import { decodeTreeResolution, encodeTreeResolution, fromJsonTreeResolution, toJsonTreeResolution } from "../type/resolution.js";
import { decodeModuleId, encodeModuleId, fromJsonModuleId, toJsonModuleId } from "../../source/file/model/module.js";

/** Resolutions added by one DIR phase. */
export type ResolutionSegment = {
    /** The module id of the resolution segment. */
    readonly moduleId: ModuleId;
    /** Checked lexical or path resolutions keyed by DIR node. */
    readonly names: ReadonlyMap<GlobalNodeIdAny, NameResolution>;
    /** Checked path segment resolutions keyed by DIR node and segment index. */
    readonly paths: ReadonlyMap<readonly [GlobalNodeIdAny, number], NameResolution>;
    /** Checked generic instantiations keyed by DIR node. */
    readonly instantiations: ReadonlyMap<GlobalNodeIdAny, InstantiationResolution>;
    /** Checked label resolutions keyed by DIR node. */
    readonly labels: ReadonlyMap<GlobalNodeIdAny, LabelResolution>;
    /** Checked receiver resolutions keyed by DIR node. */
    readonly receivers: ReadonlyMap<GlobalNodeIdAny, ReceiverResolution>;
    /** Checked stable storage accesses keyed by DIR node. */
    readonly accesses: ReadonlyMap<GlobalNodeIdAny, AccessResolution>;
    /** Checked member resolutions keyed by DIR node. */
    readonly members: ReadonlyMap<GlobalNodeIdAny, OperationResolution>;
    /** Checked operator resolutions keyed by DIR node. */
    readonly operators: ReadonlyMap<GlobalNodeIdAny, OperationResolution>;
    /** Checked call resolutions keyed by DIR node. */
    readonly calls: ReadonlyMap<GlobalNodeIdAny, OperationResolution>;
    /** Checked subscript resolutions keyed by DIR node. */
    readonly subscripts: ReadonlyMap<GlobalNodeIdAny, OperationResolution>;
    /** Checked place resolutions keyed by DIR node. */
    readonly places: ReadonlyMap<GlobalNodeIdAny, PlaceResolution>;
    /** Checked assignment resolutions keyed by DIR node. */
    readonly assignments: ReadonlyMap<GlobalNodeIdAny, AssignmentResolution>;
    /** Checked guard resolutions keyed by DIR node. */
    readonly guards: ReadonlyMap<GlobalNodeIdAny, GuardResolution>;
    /** Checked construct resolutions keyed by DIR node. */
    readonly constructs: ReadonlyMap<GlobalNodeIdAny, ConstructResolution>;
    /** Checked tree literal resolutions keyed by expression node. */
    readonly trees: ReadonlyMap<GlobalNodeIdAny, TreeResolution>;
    /** Checked pattern resolutions keyed by DIR node. */
    readonly patterns: ReadonlyMap<GlobalNodeIdAny, PatternResolution>;
    /** Checked assignment pattern resolutions keyed by DIR node. */
    readonly assigns: ReadonlyMap<GlobalNodeIdAny, AssignPatternResolution>;
    /** Unresolved reference paths keyed by DIR node. */
    readonly unresolved: ReadonlyMap<GlobalNodeIdAny, Path>;
};

export const ResolutionSegment = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ResolutionSegment): void {
        encodeResolutionSegment(writer, value);
    },

    /** Decode one ResolutionSegment. */
    decode(reader: BinaryReader): ResolutionSegment {
        return decodeResolutionSegment(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ResolutionSegment): Json {
        return toJsonResolutionSegment(value);
    },

    /** Return one ResolutionSegment from one JSON value. */
    fromJson(value: Json): ResolutionSegment {
        return fromJsonResolutionSegment(value);
    },
};

/** Encode one ResolutionSegment. */
export function encodeResolutionSegment(writer: BinaryWriter, value: ResolutionSegment): void {
    encodeModuleId(writer, value.moduleId);
    const entries1 = Array.from(value.names.entries()).map(([key1, item1]) => {
        const keyBytes = nestedBytes((writer) => {
            encodeGlobalNodeIdAny(writer, key1);
        });
        return { key1, item1, keyBytes };
    });
    entries1.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries1.length);
    for (const entry1 of entries1) {
        encodeGlobalNodeIdAny(writer, entry1.key1);
        encodeNameResolution(writer, entry1.item1);
    }
    const entries2 = Array.from(value.paths.entries()).map(([key2, item2]) => {
        const keyBytes = nestedBytes((writer) => {
            encodeGlobalNodeIdAny(writer, key2[0]);
            writer.writeUnsigned(key2[1]);
        });
        return { key2, item2, keyBytes };
    });
    entries2.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries2.length);
    for (const entry2 of entries2) {
        encodeGlobalNodeIdAny(writer, entry2.key2[0]);
        writer.writeUnsigned(entry2.key2[1]);
        encodeNameResolution(writer, entry2.item2);
    }
    const entries3 = Array.from(value.instantiations.entries()).map(([key3, item3]) => {
        const keyBytes = nestedBytes((writer) => {
            encodeGlobalNodeIdAny(writer, key3);
        });
        return { key3, item3, keyBytes };
    });
    entries3.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries3.length);
    for (const entry3 of entries3) {
        encodeGlobalNodeIdAny(writer, entry3.key3);
        encodeInstantiationResolution(writer, entry3.item3);
    }
    const entries4 = Array.from(value.labels.entries()).map(([key4, item4]) => {
        const keyBytes = nestedBytes((writer) => {
            encodeGlobalNodeIdAny(writer, key4);
        });
        return { key4, item4, keyBytes };
    });
    entries4.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries4.length);
    for (const entry4 of entries4) {
        encodeGlobalNodeIdAny(writer, entry4.key4);
        encodeLabelResolution(writer, entry4.item4);
    }
    const entries5 = Array.from(value.receivers.entries()).map(([key5, item5]) => {
        const keyBytes = nestedBytes((writer) => {
            encodeGlobalNodeIdAny(writer, key5);
        });
        return { key5, item5, keyBytes };
    });
    entries5.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries5.length);
    for (const entry5 of entries5) {
        encodeGlobalNodeIdAny(writer, entry5.key5);
        encodeReceiverResolution(writer, entry5.item5);
    }
    const entries6 = Array.from(value.accesses.entries()).map(([key6, item6]) => {
        const keyBytes = nestedBytes((writer) => {
            encodeGlobalNodeIdAny(writer, key6);
        });
        return { key6, item6, keyBytes };
    });
    entries6.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries6.length);
    for (const entry6 of entries6) {
        encodeGlobalNodeIdAny(writer, entry6.key6);
        encodeAccessResolution(writer, entry6.item6);
    }
    const entries7 = Array.from(value.members.entries()).map(([key7, item7]) => {
        const keyBytes = nestedBytes((writer) => {
            encodeGlobalNodeIdAny(writer, key7);
        });
        return { key7, item7, keyBytes };
    });
    entries7.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries7.length);
    for (const entry7 of entries7) {
        encodeGlobalNodeIdAny(writer, entry7.key7);
        encodeOperationResolution(writer, entry7.item7);
    }
    const entries8 = Array.from(value.operators.entries()).map(([key8, item8]) => {
        const keyBytes = nestedBytes((writer) => {
            encodeGlobalNodeIdAny(writer, key8);
        });
        return { key8, item8, keyBytes };
    });
    entries8.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries8.length);
    for (const entry8 of entries8) {
        encodeGlobalNodeIdAny(writer, entry8.key8);
        encodeOperationResolution(writer, entry8.item8);
    }
    const entries9 = Array.from(value.calls.entries()).map(([key9, item9]) => {
        const keyBytes = nestedBytes((writer) => {
            encodeGlobalNodeIdAny(writer, key9);
        });
        return { key9, item9, keyBytes };
    });
    entries9.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries9.length);
    for (const entry9 of entries9) {
        encodeGlobalNodeIdAny(writer, entry9.key9);
        encodeOperationResolution(writer, entry9.item9);
    }
    const entries10 = Array.from(value.subscripts.entries()).map(([key10, item10]) => {
        const keyBytes = nestedBytes((writer) => {
            encodeGlobalNodeIdAny(writer, key10);
        });
        return { key10, item10, keyBytes };
    });
    entries10.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries10.length);
    for (const entry10 of entries10) {
        encodeGlobalNodeIdAny(writer, entry10.key10);
        encodeOperationResolution(writer, entry10.item10);
    }
    const entries11 = Array.from(value.places.entries()).map(([key11, item11]) => {
        const keyBytes = nestedBytes((writer) => {
            encodeGlobalNodeIdAny(writer, key11);
        });
        return { key11, item11, keyBytes };
    });
    entries11.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries11.length);
    for (const entry11 of entries11) {
        encodeGlobalNodeIdAny(writer, entry11.key11);
        encodePlaceResolution(writer, entry11.item11);
    }
    const entries12 = Array.from(value.assignments.entries()).map(([key12, item12]) => {
        const keyBytes = nestedBytes((writer) => {
            encodeGlobalNodeIdAny(writer, key12);
        });
        return { key12, item12, keyBytes };
    });
    entries12.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries12.length);
    for (const entry12 of entries12) {
        encodeGlobalNodeIdAny(writer, entry12.key12);
        encodeAssignmentResolution(writer, entry12.item12);
    }
    const entries13 = Array.from(value.guards.entries()).map(([key13, item13]) => {
        const keyBytes = nestedBytes((writer) => {
            encodeGlobalNodeIdAny(writer, key13);
        });
        return { key13, item13, keyBytes };
    });
    entries13.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries13.length);
    for (const entry13 of entries13) {
        encodeGlobalNodeIdAny(writer, entry13.key13);
        encodeGuardResolution(writer, entry13.item13);
    }
    const entries14 = Array.from(value.constructs.entries()).map(([key14, item14]) => {
        const keyBytes = nestedBytes((writer) => {
            encodeGlobalNodeIdAny(writer, key14);
        });
        return { key14, item14, keyBytes };
    });
    entries14.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries14.length);
    for (const entry14 of entries14) {
        encodeGlobalNodeIdAny(writer, entry14.key14);
        encodeConstructResolution(writer, entry14.item14);
    }
    const entries15 = Array.from(value.trees.entries()).map(([key15, item15]) => {
        const keyBytes = nestedBytes((writer) => {
            encodeGlobalNodeIdAny(writer, key15);
        });
        return { key15, item15, keyBytes };
    });
    entries15.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries15.length);
    for (const entry15 of entries15) {
        encodeGlobalNodeIdAny(writer, entry15.key15);
        encodeTreeResolution(writer, entry15.item15);
    }
    const entries16 = Array.from(value.patterns.entries()).map(([key16, item16]) => {
        const keyBytes = nestedBytes((writer) => {
            encodeGlobalNodeIdAny(writer, key16);
        });
        return { key16, item16, keyBytes };
    });
    entries16.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries16.length);
    for (const entry16 of entries16) {
        encodeGlobalNodeIdAny(writer, entry16.key16);
        encodePatternResolution(writer, entry16.item16);
    }
    const entries17 = Array.from(value.assigns.entries()).map(([key17, item17]) => {
        const keyBytes = nestedBytes((writer) => {
            encodeGlobalNodeIdAny(writer, key17);
        });
        return { key17, item17, keyBytes };
    });
    entries17.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries17.length);
    for (const entry17 of entries17) {
        encodeGlobalNodeIdAny(writer, entry17.key17);
        encodeAssignPatternResolution(writer, entry17.item17);
    }
    const entries18 = Array.from(value.unresolved.entries()).map(([key18, item18]) => {
        const keyBytes = nestedBytes((writer) => {
            encodeGlobalNodeIdAny(writer, key18);
        });
        return { key18, item18, keyBytes };
    });
    entries18.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries18.length);
    for (const entry18 of entries18) {
        encodeGlobalNodeIdAny(writer, entry18.key18);
        encodePath(writer, entry18.item18);
    }
}

/** Decode one ResolutionSegment. */
export function decodeResolutionSegment(reader: BinaryReader): ResolutionSegment {
    const moduleId = decodeModuleId(reader);
    const names = (() => { const length1 = reader.readNumber(); const items1 = new Map<GlobalNodeIdAny, NameResolution>(); for (let index = 0; index < length1; index += 1) { items1.set(decodeGlobalNodeIdAny(reader), decodeNameResolution(reader)); } return items1; })();
    const paths = (() => { const length2 = reader.readNumber(); const items2 = new Map<readonly [GlobalNodeIdAny, number], NameResolution>(); for (let index = 0; index < length2; index += 1) { items2.set([decodeGlobalNodeIdAny(reader), reader.readNumber()] as const, decodeNameResolution(reader)); } return items2; })();
    const instantiations = (() => { const length3 = reader.readNumber(); const items3 = new Map<GlobalNodeIdAny, InstantiationResolution>(); for (let index = 0; index < length3; index += 1) { items3.set(decodeGlobalNodeIdAny(reader), decodeInstantiationResolution(reader)); } return items3; })();
    const labels = (() => { const length4 = reader.readNumber(); const items4 = new Map<GlobalNodeIdAny, LabelResolution>(); for (let index = 0; index < length4; index += 1) { items4.set(decodeGlobalNodeIdAny(reader), decodeLabelResolution(reader)); } return items4; })();
    const receivers = (() => { const length5 = reader.readNumber(); const items5 = new Map<GlobalNodeIdAny, ReceiverResolution>(); for (let index = 0; index < length5; index += 1) { items5.set(decodeGlobalNodeIdAny(reader), decodeReceiverResolution(reader)); } return items5; })();
    const accesses = (() => { const length6 = reader.readNumber(); const items6 = new Map<GlobalNodeIdAny, AccessResolution>(); for (let index = 0; index < length6; index += 1) { items6.set(decodeGlobalNodeIdAny(reader), decodeAccessResolution(reader)); } return items6; })();
    const members = (() => { const length7 = reader.readNumber(); const items7 = new Map<GlobalNodeIdAny, OperationResolution>(); for (let index = 0; index < length7; index += 1) { items7.set(decodeGlobalNodeIdAny(reader), decodeOperationResolution(reader)); } return items7; })();
    const operators = (() => { const length8 = reader.readNumber(); const items8 = new Map<GlobalNodeIdAny, OperationResolution>(); for (let index = 0; index < length8; index += 1) { items8.set(decodeGlobalNodeIdAny(reader), decodeOperationResolution(reader)); } return items8; })();
    const calls = (() => { const length9 = reader.readNumber(); const items9 = new Map<GlobalNodeIdAny, OperationResolution>(); for (let index = 0; index < length9; index += 1) { items9.set(decodeGlobalNodeIdAny(reader), decodeOperationResolution(reader)); } return items9; })();
    const subscripts = (() => { const length10 = reader.readNumber(); const items10 = new Map<GlobalNodeIdAny, OperationResolution>(); for (let index = 0; index < length10; index += 1) { items10.set(decodeGlobalNodeIdAny(reader), decodeOperationResolution(reader)); } return items10; })();
    const places = (() => { const length11 = reader.readNumber(); const items11 = new Map<GlobalNodeIdAny, PlaceResolution>(); for (let index = 0; index < length11; index += 1) { items11.set(decodeGlobalNodeIdAny(reader), decodePlaceResolution(reader)); } return items11; })();
    const assignments = (() => { const length12 = reader.readNumber(); const items12 = new Map<GlobalNodeIdAny, AssignmentResolution>(); for (let index = 0; index < length12; index += 1) { items12.set(decodeGlobalNodeIdAny(reader), decodeAssignmentResolution(reader)); } return items12; })();
    const guards = (() => { const length13 = reader.readNumber(); const items13 = new Map<GlobalNodeIdAny, GuardResolution>(); for (let index = 0; index < length13; index += 1) { items13.set(decodeGlobalNodeIdAny(reader), decodeGuardResolution(reader)); } return items13; })();
    const constructs = (() => { const length14 = reader.readNumber(); const items14 = new Map<GlobalNodeIdAny, ConstructResolution>(); for (let index = 0; index < length14; index += 1) { items14.set(decodeGlobalNodeIdAny(reader), decodeConstructResolution(reader)); } return items14; })();
    const trees = (() => { const length15 = reader.readNumber(); const items15 = new Map<GlobalNodeIdAny, TreeResolution>(); for (let index = 0; index < length15; index += 1) { items15.set(decodeGlobalNodeIdAny(reader), decodeTreeResolution(reader)); } return items15; })();
    const patterns = (() => { const length16 = reader.readNumber(); const items16 = new Map<GlobalNodeIdAny, PatternResolution>(); for (let index = 0; index < length16; index += 1) { items16.set(decodeGlobalNodeIdAny(reader), decodePatternResolution(reader)); } return items16; })();
    const assigns = (() => { const length17 = reader.readNumber(); const items17 = new Map<GlobalNodeIdAny, AssignPatternResolution>(); for (let index = 0; index < length17; index += 1) { items17.set(decodeGlobalNodeIdAny(reader), decodeAssignPatternResolution(reader)); } return items17; })();
    const unresolved = (() => { const length18 = reader.readNumber(); const items18 = new Map<GlobalNodeIdAny, Path>(); for (let index = 0; index < length18; index += 1) { items18.set(decodeGlobalNodeIdAny(reader), decodePath(reader)); } return items18; })();

    return {
        moduleId,
        names,
        paths,
        instantiations,
        labels,
        receivers,
        accesses,
        members,
        operators,
        calls,
        subscripts,
        places,
        assignments,
        guards,
        constructs,
        trees,
        patterns,
        assigns,
        unresolved,
    };
}

/** Return one JSON value for one ResolutionSegment. */
export function toJsonResolutionSegment(value: ResolutionSegment): Json {
    return {
        moduleId: toJsonModuleId(value.moduleId),
        names: Array.from(value.names.entries()).map(([key0, item0]) => [toJsonGlobalNodeIdAny(key0), toJsonNameResolution(item0)] as const),
        paths: Array.from(value.paths.entries()).map(([key0, item0]) => [[toJsonGlobalNodeIdAny(key0[0]), key0[1]], toJsonNameResolution(item0)] as const),
        instantiations: Array.from(value.instantiations.entries()).map(([key0, item0]) => [toJsonGlobalNodeIdAny(key0), toJsonInstantiationResolution(item0)] as const),
        labels: Array.from(value.labels.entries()).map(([key0, item0]) => [toJsonGlobalNodeIdAny(key0), toJsonLabelResolution(item0)] as const),
        receivers: Array.from(value.receivers.entries()).map(([key0, item0]) => [toJsonGlobalNodeIdAny(key0), toJsonReceiverResolution(item0)] as const),
        accesses: Array.from(value.accesses.entries()).map(([key0, item0]) => [toJsonGlobalNodeIdAny(key0), toJsonAccessResolution(item0)] as const),
        members: Array.from(value.members.entries()).map(([key0, item0]) => [toJsonGlobalNodeIdAny(key0), toJsonOperationResolution(item0)] as const),
        operators: Array.from(value.operators.entries()).map(([key0, item0]) => [toJsonGlobalNodeIdAny(key0), toJsonOperationResolution(item0)] as const),
        calls: Array.from(value.calls.entries()).map(([key0, item0]) => [toJsonGlobalNodeIdAny(key0), toJsonOperationResolution(item0)] as const),
        subscripts: Array.from(value.subscripts.entries()).map(([key0, item0]) => [toJsonGlobalNodeIdAny(key0), toJsonOperationResolution(item0)] as const),
        places: Array.from(value.places.entries()).map(([key0, item0]) => [toJsonGlobalNodeIdAny(key0), toJsonPlaceResolution(item0)] as const),
        assignments: Array.from(value.assignments.entries()).map(([key0, item0]) => [toJsonGlobalNodeIdAny(key0), toJsonAssignmentResolution(item0)] as const),
        guards: Array.from(value.guards.entries()).map(([key0, item0]) => [toJsonGlobalNodeIdAny(key0), toJsonGuardResolution(item0)] as const),
        constructs: Array.from(value.constructs.entries()).map(([key0, item0]) => [toJsonGlobalNodeIdAny(key0), toJsonConstructResolution(item0)] as const),
        trees: Array.from(value.trees.entries()).map(([key0, item0]) => [toJsonGlobalNodeIdAny(key0), toJsonTreeResolution(item0)] as const),
        patterns: Array.from(value.patterns.entries()).map(([key0, item0]) => [toJsonGlobalNodeIdAny(key0), toJsonPatternResolution(item0)] as const),
        assigns: Array.from(value.assigns.entries()).map(([key0, item0]) => [toJsonGlobalNodeIdAny(key0), toJsonAssignPatternResolution(item0)] as const),
        unresolved: Array.from(value.unresolved.entries()).map(([key0, item0]) => [toJsonGlobalNodeIdAny(key0), toJsonPath(item0)] as const),
    };
}

/** Return one ResolutionSegment from one JSON value. */
export function fromJsonResolutionSegment(value: Json): ResolutionSegment {
    const object = jsonObject(value);

    return {
        moduleId: fromJsonModuleId(jsonField(object, "moduleId")),
        names: new Map(jsonArray(jsonField(object, "names")).map((entry) => { const items = jsonArray(entry); if (items.length !== 2) { throw new SerdeError(`expected JSON map entry length 2: ${items.length}`); } const key0 = items[0]; const item0 = items[1]; return [fromJsonGlobalNodeIdAny(key0), fromJsonNameResolution(item0)] as const; })),
        paths: new Map(jsonArray(jsonField(object, "paths")).map((entry) => { const items = jsonArray(entry); if (items.length !== 2) { throw new SerdeError(`expected JSON map entry length 2: ${items.length}`); } const key0 = items[0]; const item0 = items[1]; return [(() => { const items = jsonArray(key0); if (items.length !== 2) { throw new SerdeError(`expected JSON tuple length 2: ${items.length}`); } return [fromJsonGlobalNodeIdAny(items[0]), jsonInteger(items[1])] as const; })(), fromJsonNameResolution(item0)] as const; })),
        instantiations: new Map(jsonArray(jsonField(object, "instantiations")).map((entry) => { const items = jsonArray(entry); if (items.length !== 2) { throw new SerdeError(`expected JSON map entry length 2: ${items.length}`); } const key0 = items[0]; const item0 = items[1]; return [fromJsonGlobalNodeIdAny(key0), fromJsonInstantiationResolution(item0)] as const; })),
        labels: new Map(jsonArray(jsonField(object, "labels")).map((entry) => { const items = jsonArray(entry); if (items.length !== 2) { throw new SerdeError(`expected JSON map entry length 2: ${items.length}`); } const key0 = items[0]; const item0 = items[1]; return [fromJsonGlobalNodeIdAny(key0), fromJsonLabelResolution(item0)] as const; })),
        receivers: new Map(jsonArray(jsonField(object, "receivers")).map((entry) => { const items = jsonArray(entry); if (items.length !== 2) { throw new SerdeError(`expected JSON map entry length 2: ${items.length}`); } const key0 = items[0]; const item0 = items[1]; return [fromJsonGlobalNodeIdAny(key0), fromJsonReceiverResolution(item0)] as const; })),
        accesses: new Map(jsonArray(jsonField(object, "accesses")).map((entry) => { const items = jsonArray(entry); if (items.length !== 2) { throw new SerdeError(`expected JSON map entry length 2: ${items.length}`); } const key0 = items[0]; const item0 = items[1]; return [fromJsonGlobalNodeIdAny(key0), fromJsonAccessResolution(item0)] as const; })),
        members: new Map(jsonArray(jsonField(object, "members")).map((entry) => { const items = jsonArray(entry); if (items.length !== 2) { throw new SerdeError(`expected JSON map entry length 2: ${items.length}`); } const key0 = items[0]; const item0 = items[1]; return [fromJsonGlobalNodeIdAny(key0), fromJsonOperationResolution(item0)] as const; })),
        operators: new Map(jsonArray(jsonField(object, "operators")).map((entry) => { const items = jsonArray(entry); if (items.length !== 2) { throw new SerdeError(`expected JSON map entry length 2: ${items.length}`); } const key0 = items[0]; const item0 = items[1]; return [fromJsonGlobalNodeIdAny(key0), fromJsonOperationResolution(item0)] as const; })),
        calls: new Map(jsonArray(jsonField(object, "calls")).map((entry) => { const items = jsonArray(entry); if (items.length !== 2) { throw new SerdeError(`expected JSON map entry length 2: ${items.length}`); } const key0 = items[0]; const item0 = items[1]; return [fromJsonGlobalNodeIdAny(key0), fromJsonOperationResolution(item0)] as const; })),
        subscripts: new Map(jsonArray(jsonField(object, "subscripts")).map((entry) => { const items = jsonArray(entry); if (items.length !== 2) { throw new SerdeError(`expected JSON map entry length 2: ${items.length}`); } const key0 = items[0]; const item0 = items[1]; return [fromJsonGlobalNodeIdAny(key0), fromJsonOperationResolution(item0)] as const; })),
        places: new Map(jsonArray(jsonField(object, "places")).map((entry) => { const items = jsonArray(entry); if (items.length !== 2) { throw new SerdeError(`expected JSON map entry length 2: ${items.length}`); } const key0 = items[0]; const item0 = items[1]; return [fromJsonGlobalNodeIdAny(key0), fromJsonPlaceResolution(item0)] as const; })),
        assignments: new Map(jsonArray(jsonField(object, "assignments")).map((entry) => { const items = jsonArray(entry); if (items.length !== 2) { throw new SerdeError(`expected JSON map entry length 2: ${items.length}`); } const key0 = items[0]; const item0 = items[1]; return [fromJsonGlobalNodeIdAny(key0), fromJsonAssignmentResolution(item0)] as const; })),
        guards: new Map(jsonArray(jsonField(object, "guards")).map((entry) => { const items = jsonArray(entry); if (items.length !== 2) { throw new SerdeError(`expected JSON map entry length 2: ${items.length}`); } const key0 = items[0]; const item0 = items[1]; return [fromJsonGlobalNodeIdAny(key0), fromJsonGuardResolution(item0)] as const; })),
        constructs: new Map(jsonArray(jsonField(object, "constructs")).map((entry) => { const items = jsonArray(entry); if (items.length !== 2) { throw new SerdeError(`expected JSON map entry length 2: ${items.length}`); } const key0 = items[0]; const item0 = items[1]; return [fromJsonGlobalNodeIdAny(key0), fromJsonConstructResolution(item0)] as const; })),
        trees: new Map(jsonArray(jsonField(object, "trees")).map((entry) => { const items = jsonArray(entry); if (items.length !== 2) { throw new SerdeError(`expected JSON map entry length 2: ${items.length}`); } const key0 = items[0]; const item0 = items[1]; return [fromJsonGlobalNodeIdAny(key0), fromJsonTreeResolution(item0)] as const; })),
        patterns: new Map(jsonArray(jsonField(object, "patterns")).map((entry) => { const items = jsonArray(entry); if (items.length !== 2) { throw new SerdeError(`expected JSON map entry length 2: ${items.length}`); } const key0 = items[0]; const item0 = items[1]; return [fromJsonGlobalNodeIdAny(key0), fromJsonPatternResolution(item0)] as const; })),
        assigns: new Map(jsonArray(jsonField(object, "assigns")).map((entry) => { const items = jsonArray(entry); if (items.length !== 2) { throw new SerdeError(`expected JSON map entry length 2: ${items.length}`); } const key0 = items[0]; const item0 = items[1]; return [fromJsonGlobalNodeIdAny(key0), fromJsonAssignPatternResolution(item0)] as const; })),
        unresolved: new Map(jsonArray(jsonField(object, "unresolved")).map((entry) => { const items = jsonArray(entry); if (items.length !== 2) { throw new SerdeError(`expected JSON map entry length 2: ${items.length}`); } const key0 = items[0]; const item0 = items[1]; return [fromJsonGlobalNodeIdAny(key0), fromJsonPath(item0)] as const; })),
    };
}
