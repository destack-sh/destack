/* generated by destack_client_generate, do not edit */

import {
    BinaryReader,
    BinaryWriter,
    Json,
    SerdeError,
    compareBytes,
    jsonArray,
    jsonField,
    jsonInteger,
    jsonObject,
    nestedBytes,
} from "../../../protocol/serde.js";
import type { GlobalNodeIdAny } from "../tree/node.js";
import type { Path } from "../tree/path.js";
import type { NameResolution } from "../type/resolution.js";
import type { ModuleId } from "../../source/file/model/module.js";
import {
    decodeGlobalNodeIdAny,
    encodeGlobalNodeIdAny,
    fromJsonGlobalNodeIdAny,
    toJsonGlobalNodeIdAny,
} from "../tree/node.js";
import { decodePath, encodePath, fromJsonPath, toJsonPath } from "../tree/path.js";
import {
    decodeNameResolution,
    encodeNameResolution,
    fromJsonNameResolution,
    toJsonNameResolution,
} from "../type/resolution.js";
import {
    decodeModuleId,
    encodeModuleId,
    fromJsonModuleId,
    toJsonModuleId,
} from "../../source/file/model/module.js";

/** Lexical resolutions added by one DIR phase's walk. */
export type ResolutionSegment = {
    /** The module id of the resolution segment. */
    readonly moduleId: ModuleId;
    /** Resolved lexical names keyed by DIR node. */
    readonly names: ReadonlyMap<GlobalNodeIdAny, NameResolution>;
    /** Resolved path segment names keyed by DIR node and segment index. */
    readonly paths: ReadonlyMap<readonly [GlobalNodeIdAny, number], NameResolution>;
    /** Unresolved reference paths keyed by DIR node. */
    readonly unresolved: ReadonlyMap<GlobalNodeIdAny, Path>;
};

export const ResolutionSegment = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ResolutionSegment): void {
        encodeResolutionSegment(writer, value);
    },

    /** Decode one ResolutionSegment. */
    decode(reader: BinaryReader): ResolutionSegment {
        return decodeResolutionSegment(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ResolutionSegment): Json {
        return toJsonResolutionSegment(value);
    },

    /** Return one ResolutionSegment from one JSON value. */
    fromJson(value: Json): ResolutionSegment {
        return fromJsonResolutionSegment(value);
    },
};

/** Encode one ResolutionSegment. */
export function encodeResolutionSegment(writer: BinaryWriter, value: ResolutionSegment): void {
    encodeModuleId(writer, value.moduleId);
    const entries1 = Array.from(value.names.entries()).map(([key1, item1]) => {
        const keyBytes = nestedBytes((writer) => {
            encodeGlobalNodeIdAny(writer, key1);
        });
        return { key1, item1, keyBytes };
    });
    entries1.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries1.length);
    for (const entry1 of entries1) {
        encodeGlobalNodeIdAny(writer, entry1.key1);
        encodeNameResolution(writer, entry1.item1);
    }
    const entries2 = Array.from(value.paths.entries()).map(([key2, item2]) => {
        const keyBytes = nestedBytes((writer) => {
            encodeGlobalNodeIdAny(writer, key2[0]);
            writer.writeUnsigned(key2[1]);
        });
        return { key2, item2, keyBytes };
    });
    entries2.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries2.length);
    for (const entry2 of entries2) {
        encodeGlobalNodeIdAny(writer, entry2.key2[0]);
        writer.writeUnsigned(entry2.key2[1]);
        encodeNameResolution(writer, entry2.item2);
    }
    const entries3 = Array.from(value.unresolved.entries()).map(([key3, item3]) => {
        const keyBytes = nestedBytes((writer) => {
            encodeGlobalNodeIdAny(writer, key3);
        });
        return { key3, item3, keyBytes };
    });
    entries3.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries3.length);
    for (const entry3 of entries3) {
        encodeGlobalNodeIdAny(writer, entry3.key3);
        encodePath(writer, entry3.item3);
    }
}

/** Decode one ResolutionSegment. */
export function decodeResolutionSegment(reader: BinaryReader): ResolutionSegment {
    const moduleId = decodeModuleId(reader);
    const names = (() => {
        const length1 = reader.readNumber();
        const items1 = new Map<GlobalNodeIdAny, NameResolution>();
        for (let index = 0; index < length1; index += 1) {
            items1.set(decodeGlobalNodeIdAny(reader), decodeNameResolution(reader));
        }
        return items1;
    })();
    const paths = (() => {
        const length2 = reader.readNumber();
        const items2 = new Map<readonly [GlobalNodeIdAny, number], NameResolution>();
        for (let index = 0; index < length2; index += 1) {
            items2.set(
                [decodeGlobalNodeIdAny(reader), reader.readNumber()] as const,
                decodeNameResolution(reader),
            );
        }
        return items2;
    })();
    const unresolved = (() => {
        const length3 = reader.readNumber();
        const items3 = new Map<GlobalNodeIdAny, Path>();
        for (let index = 0; index < length3; index += 1) {
            items3.set(decodeGlobalNodeIdAny(reader), decodePath(reader));
        }
        return items3;
    })();

    return {
        moduleId,
        names,
        paths,
        unresolved,
    };
}

/** Return one JSON value for one ResolutionSegment. */
export function toJsonResolutionSegment(value: ResolutionSegment): Json {
    return {
        moduleId: toJsonModuleId(value.moduleId),
        names: Array.from(value.names.entries()).map(
            ([key0, item0]) => [toJsonGlobalNodeIdAny(key0), toJsonNameResolution(item0)] as const,
        ),
        paths: Array.from(value.paths.entries()).map(
            ([key0, item0]) =>
                [[toJsonGlobalNodeIdAny(key0[0]), key0[1]], toJsonNameResolution(item0)] as const,
        ),
        unresolved: Array.from(value.unresolved.entries()).map(
            ([key0, item0]) => [toJsonGlobalNodeIdAny(key0), toJsonPath(item0)] as const,
        ),
    };
}

/** Return one ResolutionSegment from one JSON value. */
export function fromJsonResolutionSegment(value: Json): ResolutionSegment {
    const object = jsonObject(value);

    return {
        moduleId: fromJsonModuleId(jsonField(object, "moduleId")),
        names: new Map(
            jsonArray(jsonField(object, "names")).map((entry) => {
                const items = jsonArray(entry);
                if (items.length !== 2) {
                    throw new SerdeError(`expected JSON map entry length 2: ${items.length}`);
                }
                const key0 = items[0];
                const item0 = items[1];
                return [fromJsonGlobalNodeIdAny(key0), fromJsonNameResolution(item0)] as const;
            }),
        ),
        paths: new Map(
            jsonArray(jsonField(object, "paths")).map((entry) => {
                const items = jsonArray(entry);
                if (items.length !== 2) {
                    throw new SerdeError(`expected JSON map entry length 2: ${items.length}`);
                }
                const key0 = items[0];
                const item0 = items[1];
                return [
                    (() => {
                        const items = jsonArray(key0);
                        if (items.length !== 2) {
                            throw new SerdeError(`expected JSON tuple length 2: ${items.length}`);
                        }
                        return [fromJsonGlobalNodeIdAny(items[0]), jsonInteger(items[1])] as const;
                    })(),
                    fromJsonNameResolution(item0),
                ] as const;
            }),
        ),
        unresolved: new Map(
            jsonArray(jsonField(object, "unresolved")).map((entry) => {
                const items = jsonArray(entry);
                if (items.length !== 2) {
                    throw new SerdeError(`expected JSON map entry length 2: ${items.length}`);
                }
                const key0 = items[0];
                const item0 = items[1];
                return [fromJsonGlobalNodeIdAny(key0), fromJsonPath(item0)] as const;
            }),
        ),
    };
}
