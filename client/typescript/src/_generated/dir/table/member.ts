/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, compareBytes, jsonArray, jsonBool, jsonField, jsonInteger, jsonObject, jsonOptional, jsonString, nestedBytes } from "../../../protocol/serde.js";
import type { StaticKey } from "../symbol/key.js";
import type { GlobalSymbolId } from "../symbol/symbol.js";
import type { GlobalNodeIdAny } from "../tree/node.js";
import type { GlobalGenericTemplateId } from "../type/generic.js";
import type { GlobalTypeId } from "../type/type.js";
import type { PropertyAccess } from "../type/type.js";
import type { ModuleId } from "../../source/file/model/module.js";
import { decodeStaticKey, encodeStaticKey, fromJsonStaticKey, toJsonStaticKey } from "../symbol/key.js";
import { decodeGlobalSymbolId, encodeGlobalSymbolId, fromJsonGlobalSymbolId, toJsonGlobalSymbolId } from "../symbol/symbol.js";
import { decodeGlobalNodeIdAny, encodeGlobalNodeIdAny, fromJsonGlobalNodeIdAny, toJsonGlobalNodeIdAny } from "../tree/node.js";
import { decodeGlobalGenericTemplateId, encodeGlobalGenericTemplateId, fromJsonGlobalGenericTemplateId, toJsonGlobalGenericTemplateId } from "../type/generic.js";
import { decodeGlobalTypeId, encodeGlobalTypeId, fromJsonGlobalTypeId, toJsonGlobalTypeId } from "../type/type.js";
import { decodePropertyAccess, encodePropertyAccess, fromJsonPropertyAccess, toJsonPropertyAccess } from "../type/type.js";
import { decodeModuleId, encodeModuleId, fromJsonModuleId, toJsonModuleId } from "../../source/file/model/module.js";

/** One member selected by lookup. */
export type MemberBinding = {
    /** The available member key. */
    readonly key: StaticKey;
    /** The available member kind. */
    readonly kind: MemberKind;
    /** The selected read and write types. */
    readonly access: PropertyAccess;
    /** Whether the member may be absent. */
    readonly isOptional: boolean;
    /** The declarations contributing to this binding. */
    readonly declarations: ReadonlyArray<MemberDeclaration>;
};

export const MemberBinding = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: MemberBinding): void {
        encodeMemberBinding(writer, value);
    },

    /** Decode one MemberBinding. */
    decode(reader: BinaryReader): MemberBinding {
        return decodeMemberBinding(reader);
    },

    /** Return this value as JSON. */
    toJson(value: MemberBinding): Json {
        return toJsonMemberBinding(value);
    },

    /** Return one MemberBinding from one JSON value. */
    fromJson(value: Json): MemberBinding {
        return fromJsonMemberBinding(value);
    },
};

/** Encode one MemberBinding. */
export function encodeMemberBinding(writer: BinaryWriter, value: MemberBinding): void {
    encodeStaticKey(writer, value.key);
    encodeMemberKind(writer, value.kind);
    encodePropertyAccess(writer, value.access);
    writer.writeBool(value.isOptional);
    writer.writeUnsigned(value.declarations.length);
    for (const item4 of value.declarations) {
        encodeMemberDeclaration(writer, item4);
    }
}

/** Decode one MemberBinding. */
export function decodeMemberBinding(reader: BinaryReader): MemberBinding {
    const key = decodeStaticKey(reader);
    const kind = decodeMemberKind(reader);
    const access = decodePropertyAccess(reader);
    const isOptional = reader.readBool();
    const declarations = (() => { const length4 = reader.readNumber(); const items4: Array<MemberDeclaration> = []; for (let index = 0; index < length4; index += 1) { items4.push(decodeMemberDeclaration(reader)); } return items4; })();

    return {
        key,
        kind,
        access,
        isOptional,
        declarations,
    };
}

/** Return one JSON value for one MemberBinding. */
export function toJsonMemberBinding(value: MemberBinding): Json {
    return {
        key: toJsonStaticKey(value.key),
        kind: toJsonMemberKind(value.kind),
        access: toJsonPropertyAccess(value.access),
        isOptional: value.isOptional,
        declarations: value.declarations.map((item0) => toJsonMemberDeclaration(item0)),
    };
}

/** Return one MemberBinding from one JSON value. */
export function fromJsonMemberBinding(value: Json): MemberBinding {
    const object = jsonObject(value);

    return {
        key: fromJsonStaticKey(jsonField(object, "key")),
        kind: fromJsonMemberKind(jsonField(object, "kind")),
        access: fromJsonPropertyAccess(jsonField(object, "access")),
        isOptional: jsonBool(jsonField(object, "isOptional")),
        declarations: jsonArray(jsonField(object, "declarations")).map((item0) => fromJsonMemberDeclaration(item0)),
    };
}

/** One declaration contributing to a checked member binding. */
export type MemberDeclaration = {
    /** The selected declaration symbol. */
    readonly symbol: GlobalSymbolId;
    /** The declaration that exposed this member, through heritage. */
    readonly owner: GlobalSymbolId;
    /** The declaration family that exposed this member. */
    readonly origin: MemberOrigin;
    /** How the member behaves at a use site. */
    readonly role: MemberRole;
    /** The substituted callable type, when callable. */
    readonly callableType?: GlobalTypeId;
};

export const MemberDeclaration = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: MemberDeclaration): void {
        encodeMemberDeclaration(writer, value);
    },

    /** Decode one MemberDeclaration. */
    decode(reader: BinaryReader): MemberDeclaration {
        return decodeMemberDeclaration(reader);
    },

    /** Return this value as JSON. */
    toJson(value: MemberDeclaration): Json {
        return toJsonMemberDeclaration(value);
    },

    /** Return one MemberDeclaration from one JSON value. */
    fromJson(value: Json): MemberDeclaration {
        return fromJsonMemberDeclaration(value);
    },
};

/** Encode one MemberDeclaration. */
export function encodeMemberDeclaration(writer: BinaryWriter, value: MemberDeclaration): void {
    encodeGlobalSymbolId(writer, value.symbol);
    encodeGlobalSymbolId(writer, value.owner);
    encodeMemberOrigin(writer, value.origin);
    encodeMemberRole(writer, value.role);
    writer.writeOption(value.callableType, (value4) => {
        encodeGlobalTypeId(writer, value4);
    });
}

/** Decode one MemberDeclaration. */
export function decodeMemberDeclaration(reader: BinaryReader): MemberDeclaration {
    const symbol_ = decodeGlobalSymbolId(reader);
    const owner = decodeGlobalSymbolId(reader);
    const origin = decodeMemberOrigin(reader);
    const role = decodeMemberRole(reader);
    const callableType = reader.readOption(() => decodeGlobalTypeId(reader));

    return {
        symbol: symbol_,
        owner,
        origin,
        role,
        ...(callableType === undefined ? {} : { callableType }),
    };
}

/** Return one JSON value for one MemberDeclaration. */
export function toJsonMemberDeclaration(value: MemberDeclaration): Json {
    return {
        symbol: toJsonGlobalSymbolId(value.symbol),
        owner: toJsonGlobalSymbolId(value.owner),
        origin: toJsonMemberOrigin(value.origin),
        role: toJsonMemberRole(value.role),
        ...(value.callableType === undefined ? {} : { callableType: toJsonGlobalTypeId(value.callableType) }),
    };
}

/** Return one MemberDeclaration from one JSON value. */
export function fromJsonMemberDeclaration(value: Json): MemberDeclaration {
    const object = jsonObject(value);

    return {
        symbol: fromJsonGlobalSymbolId(jsonField(object, "symbol")),
        owner: fromJsonGlobalSymbolId(jsonField(object, "owner")),
        origin: fromJsonMemberOrigin(jsonField(object, "origin")),
        role: fromJsonMemberRole(jsonField(object, "role")),
        callableType: jsonOptional(object, "callableType", (value) => fromJsonGlobalTypeId(value)),
    };
}

/** One member kind. */
export type MemberKind = "field" | "property" | "method" | "constructor" | "callSignature" | "constructSignature" | "indexSignature" | "associatedType" | "associatedConst" | "variant";

export const MemberKind = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: MemberKind): void {
        encodeMemberKind(writer, value);
    },

    /** Decode one MemberKind. */
    decode(reader: BinaryReader): MemberKind {
        return decodeMemberKind(reader);
    },

    /** Return this value as JSON. */
    toJson(value: MemberKind): Json {
        return toJsonMemberKind(value);
    },

    /** Return one MemberKind from one JSON value. */
    fromJson(value: Json): MemberKind {
        return fromJsonMemberKind(value);
    },
};

/** Encode one MemberKind. */
export function encodeMemberKind(writer: BinaryWriter, value: MemberKind): void {
    switch (value) {
        case "field":
            writer.writeUnsigned(0);
            return;
        case "property":
            writer.writeUnsigned(1);
            return;
        case "method":
            writer.writeUnsigned(2);
            return;
        case "constructor":
            writer.writeUnsigned(3);
            return;
        case "callSignature":
            writer.writeUnsigned(4);
            return;
        case "constructSignature":
            writer.writeUnsigned(5);
            return;
        case "indexSignature":
            writer.writeUnsigned(6);
            return;
        case "associatedType":
            writer.writeUnsigned(7);
            return;
        case "associatedConst":
            writer.writeUnsigned(8);
            return;
        case "variant":
            writer.writeUnsigned(9);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one MemberKind. */
export function decodeMemberKind(reader: BinaryReader): MemberKind {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "field";
        case 1:
            return "property";
        case 2:
            return "method";
        case 3:
            return "constructor";
        case 4:
            return "callSignature";
        case 5:
            return "constructSignature";
        case 6:
            return "indexSignature";
        case 7:
            return "associatedType";
        case 8:
            return "associatedConst";
        case 9:
            return "variant";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one MemberKind. */
export function toJsonMemberKind(value: MemberKind): Json {
    return value;
}

/** Return one MemberKind from one JSON value. */
export function fromJsonMemberKind(value: Json): MemberKind {
    const variant = jsonString(value);

    switch (variant) {
        case "field":
            return "field";
        case "property":
            return "property";
        case "method":
            return "method";
        case "constructor":
            return "constructor";
        case "callSignature":
            return "callSignature";
        case "constructSignature":
            return "constructSignature";
        case "indexSignature":
            return "indexSignature";
        case "associatedType":
            return "associatedType";
        case "associatedConst":
            return "associatedConst";
        case "variant":
            return "variant";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** The declaration family one member came from. */
export type MemberOrigin = "declaration" | "rootedExtension" | "blanketExtension";

export const MemberOrigin = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: MemberOrigin): void {
        encodeMemberOrigin(writer, value);
    },

    /** Decode one MemberOrigin. */
    decode(reader: BinaryReader): MemberOrigin {
        return decodeMemberOrigin(reader);
    },

    /** Return this value as JSON. */
    toJson(value: MemberOrigin): Json {
        return toJsonMemberOrigin(value);
    },

    /** Return one MemberOrigin from one JSON value. */
    fromJson(value: Json): MemberOrigin {
        return fromJsonMemberOrigin(value);
    },
};

/** Encode one MemberOrigin. */
export function encodeMemberOrigin(writer: BinaryWriter, value: MemberOrigin): void {
    switch (value) {
        case "declaration":
            writer.writeUnsigned(0);
            return;
        case "rootedExtension":
            writer.writeUnsigned(1);
            return;
        case "blanketExtension":
            writer.writeUnsigned(2);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one MemberOrigin. */
export function decodeMemberOrigin(reader: BinaryReader): MemberOrigin {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "declaration";
        case 1:
            return "rootedExtension";
        case 2:
            return "blanketExtension";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one MemberOrigin. */
export function toJsonMemberOrigin(value: MemberOrigin): Json {
    return value;
}

/** Return one MemberOrigin from one JSON value. */
export function fromJsonMemberOrigin(value: Json): MemberOrigin {
    const variant = jsonString(value);

    switch (variant) {
        case "declaration":
            return "declaration";
        case "rootedExtension":
            return "rootedExtension";
        case "blanketExtension":
            return "blanketExtension";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** How one declaration member behaves at a use site. */
export type MemberRole = "field" | "method" | "getter" | "setter" | "associated" | "variantValue" | "variantConstructor";

export const MemberRole = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: MemberRole): void {
        encodeMemberRole(writer, value);
    },

    /** Decode one MemberRole. */
    decode(reader: BinaryReader): MemberRole {
        return decodeMemberRole(reader);
    },

    /** Return this value as JSON. */
    toJson(value: MemberRole): Json {
        return toJsonMemberRole(value);
    },

    /** Return one MemberRole from one JSON value. */
    fromJson(value: Json): MemberRole {
        return fromJsonMemberRole(value);
    },
};

/** Encode one MemberRole. */
export function encodeMemberRole(writer: BinaryWriter, value: MemberRole): void {
    switch (value) {
        case "field":
            writer.writeUnsigned(0);
            return;
        case "method":
            writer.writeUnsigned(1);
            return;
        case "getter":
            writer.writeUnsigned(2);
            return;
        case "setter":
            writer.writeUnsigned(3);
            return;
        case "associated":
            writer.writeUnsigned(4);
            return;
        case "variantValue":
            writer.writeUnsigned(5);
            return;
        case "variantConstructor":
            writer.writeUnsigned(6);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one MemberRole. */
export function decodeMemberRole(reader: BinaryReader): MemberRole {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "field";
        case 1:
            return "method";
        case 2:
            return "getter";
        case 3:
            return "setter";
        case 4:
            return "associated";
        case 5:
            return "variantValue";
        case 6:
            return "variantConstructor";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one MemberRole. */
export function toJsonMemberRole(value: MemberRole): Json {
    return value;
}

/** Return one MemberRole from one JSON value. */
export function fromJsonMemberRole(value: Json): MemberRole {
    const variant = jsonString(value);

    switch (variant) {
        case "field":
            return "field";
        case "method":
            return "method";
        case "getter":
            return "getter";
        case "setter":
            return "setter";
        case "associated":
            return "associated";
        case "variantValue":
            return "variantValue";
        case "variantConstructor":
            return "variantConstructor";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** Member bindings added by one DIR phase. */
export type MemberSegment = {
    /** The module id of the member segment. */
    readonly moduleId: ModuleId;
    /** Lookup subjects selected at source sites. */
    readonly subjects: ReadonlyMap<MemberSite, MemberSubject>;
    /** Member bindings stored once per lookup subject. */
    readonly bindings: ReadonlyMap<MemberSubject, ReadonlyArray<MemberBinding>>;
};

export const MemberSegment = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: MemberSegment): void {
        encodeMemberSegment(writer, value);
    },

    /** Decode one MemberSegment. */
    decode(reader: BinaryReader): MemberSegment {
        return decodeMemberSegment(reader);
    },

    /** Return this value as JSON. */
    toJson(value: MemberSegment): Json {
        return toJsonMemberSegment(value);
    },

    /** Return one MemberSegment from one JSON value. */
    fromJson(value: Json): MemberSegment {
        return fromJsonMemberSegment(value);
    },
};

/** Encode one MemberSegment. */
export function encodeMemberSegment(writer: BinaryWriter, value: MemberSegment): void {
    encodeModuleId(writer, value.moduleId);
    const entries1 = Array.from(value.subjects.entries()).map(([key1, item1]) => {
        const keyBytes = nestedBytes((writer) => {
            encodeMemberSite(writer, key1);
        });
        return { key1, item1, keyBytes };
    });
    entries1.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries1.length);
    for (const entry1 of entries1) {
        encodeMemberSite(writer, entry1.key1);
        encodeMemberSubject(writer, entry1.item1);
    }
    const entries2 = Array.from(value.bindings.entries()).map(([key2, item2]) => {
        const keyBytes = nestedBytes((writer) => {
            encodeMemberSubject(writer, key2);
        });
        return { key2, item2, keyBytes };
    });
    entries2.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries2.length);
    for (const entry2 of entries2) {
        encodeMemberSubject(writer, entry2.key2);
        writer.writeUnsigned(entry2.item2.length);
        for (const item3 of entry2.item2) {
            encodeMemberBinding(writer, item3);
        }
    }
}

/** Decode one MemberSegment. */
export function decodeMemberSegment(reader: BinaryReader): MemberSegment {
    const moduleId = decodeModuleId(reader);
    const subjects = (() => { const length1 = reader.readNumber(); const items1 = new Map<MemberSite, MemberSubject>(); for (let index = 0; index < length1; index += 1) { items1.set(decodeMemberSite(reader), decodeMemberSubject(reader)); } return items1; })();
    const bindings = (() => { const length2 = reader.readNumber(); const items2 = new Map<MemberSubject, ReadonlyArray<MemberBinding>>(); for (let index = 0; index < length2; index += 1) { items2.set(decodeMemberSubject(reader), (() => { const length4 = reader.readNumber(); const items4: Array<MemberBinding> = []; for (let index = 0; index < length4; index += 1) { items4.push(decodeMemberBinding(reader)); } return items4; })()); } return items2; })();

    return {
        moduleId,
        subjects,
        bindings,
    };
}

/** Return one JSON value for one MemberSegment. */
export function toJsonMemberSegment(value: MemberSegment): Json {
    return {
        moduleId: toJsonModuleId(value.moduleId),
        subjects: Array.from(value.subjects.entries()).map(([key0, item0]) => [toJsonMemberSite(key0), toJsonMemberSubject(item0)] as const),
        bindings: Array.from(value.bindings.entries()).map(([key0, item0]) => [toJsonMemberSubject(key0), item0.map((item1) => toJsonMemberBinding(item1))] as const),
    };
}

/** Return one MemberSegment from one JSON value. */
export function fromJsonMemberSegment(value: Json): MemberSegment {
    const object = jsonObject(value);

    return {
        moduleId: fromJsonModuleId(jsonField(object, "moduleId")),
        subjects: new Map(jsonArray(jsonField(object, "subjects")).map((entry) => { const items = jsonArray(entry); if (items.length !== 2) { throw new SerdeError(`expected JSON map entry length 2: ${items.length}`); } const key0 = items[0]; const item0 = items[1]; return [fromJsonMemberSite(key0), fromJsonMemberSubject(item0)] as const; })),
        bindings: new Map(jsonArray(jsonField(object, "bindings")).map((entry) => { const items = jsonArray(entry); if (items.length !== 2) { throw new SerdeError(`expected JSON map entry length 2: ${items.length}`); } const key0 = items[0]; const item0 = items[1]; return [fromJsonMemberSubject(key0), jsonArray(item0).map((item1) => fromJsonMemberBinding(item1))] as const; })),
    };
}

/** One source member lookup location. */
export type MemberSite =
    /** A member expression or member type node. */
    | {
          readonly kind: "node";
          readonly node: GlobalNodeIdAny;
      }
    /** One segment of a flat reference path. */
    | {
          readonly kind: "path";
          /** The reference path node. */
          readonly node: GlobalNodeIdAny;
          /** The selected path segment. */
          readonly segment: number;
      }
;

export const MemberSite = {
    /** A member expression or member type node. */
    node(node: GlobalNodeIdAny): MemberSite {
        return { kind: "node", node };
    },

    /** One segment of a flat reference path. */
    path(node: GlobalNodeIdAny, segment: number): MemberSite {
        return { kind: "path", node, segment };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: MemberSite): void {
        encodeMemberSite(writer, value);
    },

    /** Decode one MemberSite. */
    decode(reader: BinaryReader): MemberSite {
        return decodeMemberSite(reader);
    },

    /** Return this value as JSON. */
    toJson(value: MemberSite): Json {
        return toJsonMemberSite(value);
    },

    /** Return one MemberSite from one JSON value. */
    fromJson(value: Json): MemberSite {
        return fromJsonMemberSite(value);
    },
};

/** Encode one MemberSite. */
export function encodeMemberSite(writer: BinaryWriter, value: MemberSite): void {
    switch (value.kind) {
        case "node":
            writer.writeUnsigned(0);
            encodeGlobalNodeIdAny(writer, value.node);
            return;
        case "path":
            writer.writeUnsigned(1);
            encodeGlobalNodeIdAny(writer, value.node);
            writer.writeUnsigned(value.segment);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one MemberSite. */
export function decodeMemberSite(reader: BinaryReader): MemberSite {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const node = decodeGlobalNodeIdAny(reader);

            return { kind: "node", node };
        }
        case 1: {
            const node = decodeGlobalNodeIdAny(reader);
            const segment = reader.readNumber();

            return {
                kind: "path",
                node,
                segment,
            };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one MemberSite. */
export function toJsonMemberSite(value: MemberSite): Json {
    switch (value.kind) {
        case "node":
            return {
                kind: "node",
                node: toJsonGlobalNodeIdAny(value.node),
            };
        case "path":
            return {
                kind: "path",
                node: toJsonGlobalNodeIdAny(value.node),
                segment: value.segment,
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one MemberSite from one JSON value. */
export function fromJsonMemberSite(value: Json): MemberSite {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "node":
            return {
                kind,
                node: fromJsonGlobalNodeIdAny(jsonField(object, "node")),
            };
        case "path":
            return {
                kind,
                node: fromJsonGlobalNodeIdAny(jsonField(object, "node")),
                segment: jsonInteger(jsonField(object, "segment")),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** Member namespace selected by member lookup. */
export type MemberSpace = "instance" | "static";

export const MemberSpace = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: MemberSpace): void {
        encodeMemberSpace(writer, value);
    },

    /** Decode one MemberSpace. */
    decode(reader: BinaryReader): MemberSpace {
        return decodeMemberSpace(reader);
    },

    /** Return this value as JSON. */
    toJson(value: MemberSpace): Json {
        return toJsonMemberSpace(value);
    },

    /** Return one MemberSpace from one JSON value. */
    fromJson(value: Json): MemberSpace {
        return fromJsonMemberSpace(value);
    },
};

/** Encode one MemberSpace. */
export function encodeMemberSpace(writer: BinaryWriter, value: MemberSpace): void {
    switch (value) {
        case "instance":
            writer.writeUnsigned(0);
            return;
        case "static":
            writer.writeUnsigned(1);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one MemberSpace. */
export function decodeMemberSpace(reader: BinaryReader): MemberSpace {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "instance";
        case 1:
            return "static";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one MemberSpace. */
export function toJsonMemberSpace(value: MemberSpace): Json {
    return value;
}

/** Return one MemberSpace from one JSON value. */
export function fromJsonMemberSpace(value: Json): MemberSpace {
    const variant = jsonString(value);

    switch (variant) {
        case "instance":
            return "instance";
        case "static":
            return "static";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** The exact input to member lookup. */
export type MemberSubject = {
    /** The non-nullish receiver type. */
    readonly receiver: GlobalTypeId;
    /** The type searched by member lookup. */
    readonly target: GlobalTypeId;
    /** The selected instance or static member space. */
    readonly space: MemberSpace;
    /** The generic template assumptions active at the source site. */
    readonly scope?: GlobalGenericTemplateId;
    /** The type whose member keys are enumerated. */
    readonly keyType: GlobalTypeId;
};

export const MemberSubject = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: MemberSubject): void {
        encodeMemberSubject(writer, value);
    },

    /** Decode one MemberSubject. */
    decode(reader: BinaryReader): MemberSubject {
        return decodeMemberSubject(reader);
    },

    /** Return this value as JSON. */
    toJson(value: MemberSubject): Json {
        return toJsonMemberSubject(value);
    },

    /** Return one MemberSubject from one JSON value. */
    fromJson(value: Json): MemberSubject {
        return fromJsonMemberSubject(value);
    },
};

/** Encode one MemberSubject. */
export function encodeMemberSubject(writer: BinaryWriter, value: MemberSubject): void {
    encodeGlobalTypeId(writer, value.receiver);
    encodeGlobalTypeId(writer, value.target);
    encodeMemberSpace(writer, value.space);
    writer.writeOption(value.scope, (value3) => {
        encodeGlobalGenericTemplateId(writer, value3);
    });
    encodeGlobalTypeId(writer, value.keyType);
}

/** Decode one MemberSubject. */
export function decodeMemberSubject(reader: BinaryReader): MemberSubject {
    const receiver = decodeGlobalTypeId(reader);
    const target = decodeGlobalTypeId(reader);
    const space = decodeMemberSpace(reader);
    const scope = reader.readOption(() => decodeGlobalGenericTemplateId(reader));
    const keyType = decodeGlobalTypeId(reader);

    return {
        receiver,
        target,
        space,
        ...(scope === undefined ? {} : { scope }),
        keyType,
    };
}

/** Return one JSON value for one MemberSubject. */
export function toJsonMemberSubject(value: MemberSubject): Json {
    return {
        receiver: toJsonGlobalTypeId(value.receiver),
        target: toJsonGlobalTypeId(value.target),
        space: toJsonMemberSpace(value.space),
        ...(value.scope === undefined ? {} : { scope: toJsonGlobalGenericTemplateId(value.scope) }),
        keyType: toJsonGlobalTypeId(value.keyType),
    };
}

/** Return one MemberSubject from one JSON value. */
export function fromJsonMemberSubject(value: Json): MemberSubject {
    const object = jsonObject(value);

    return {
        receiver: fromJsonGlobalTypeId(jsonField(object, "receiver")),
        target: fromJsonGlobalTypeId(jsonField(object, "target")),
        space: fromJsonMemberSpace(jsonField(object, "space")),
        scope: jsonOptional(object, "scope", (value) => fromJsonGlobalGenericTemplateId(value)),
        keyType: fromJsonGlobalTypeId(jsonField(object, "keyType")),
    };
}
