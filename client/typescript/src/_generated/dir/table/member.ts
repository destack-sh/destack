/* generated by destack_client_generate, do not edit */

import {
    BinaryReader,
    BinaryWriter,
    Json,
    SerdeError,
    compareBytes,
    jsonArray,
    jsonBool,
    jsonField,
    jsonInteger,
    jsonObject,
    jsonOptional,
    jsonString,
    nestedBytes,
} from "../../../protocol/serde.js";
import type { StaticKey } from "../symbol/key.js";
import type { GlobalSymbolId } from "../symbol/symbol.js";
import type { GlobalNodeIdAny } from "../tree/node.js";
import type { GlobalGenericTemplateId } from "../type/generic.js";
import type { GlobalTypeId } from "../type/type.js";
import type { TypeListId } from "../type/type.js";
import type { PropertyAccess } from "../type/type.js";
import type { ModuleId } from "../../source/file/model/module.js";
import {
    decodeStaticKey,
    encodeStaticKey,
    fromJsonStaticKey,
    toJsonStaticKey,
} from "../symbol/key.js";
import {
    decodeGlobalSymbolId,
    encodeGlobalSymbolId,
    fromJsonGlobalSymbolId,
    toJsonGlobalSymbolId,
} from "../symbol/symbol.js";
import {
    decodeGlobalNodeIdAny,
    encodeGlobalNodeIdAny,
    fromJsonGlobalNodeIdAny,
    toJsonGlobalNodeIdAny,
} from "../tree/node.js";
import {
    decodeGlobalGenericTemplateId,
    encodeGlobalGenericTemplateId,
    fromJsonGlobalGenericTemplateId,
    toJsonGlobalGenericTemplateId,
} from "../type/generic.js";
import {
    decodeGlobalTypeId,
    encodeGlobalTypeId,
    fromJsonGlobalTypeId,
    toJsonGlobalTypeId,
} from "../type/type.js";
import {
    decodeTypeListId,
    encodeTypeListId,
    fromJsonTypeListId,
    toJsonTypeListId,
} from "../type/type.js";
import {
    decodePropertyAccess,
    encodePropertyAccess,
    fromJsonPropertyAccess,
    toJsonPropertyAccess,
} from "../type/type.js";
import {
    decodeModuleId,
    encodeModuleId,
    fromJsonModuleId,
    toJsonModuleId,
} from "../../source/file/model/module.js";

/** One exact member implementation edge. */
export type ImplementationEdge = {
    /** The declared member requirement. */
    readonly declaration: GlobalSymbolId;
    /** The member satisfying the declaration. */
    readonly implementation: GlobalSymbolId;
};

export const ImplementationEdge = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ImplementationEdge): void {
        encodeImplementationEdge(writer, value);
    },

    /** Decode one ImplementationEdge. */
    decode(reader: BinaryReader): ImplementationEdge {
        return decodeImplementationEdge(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ImplementationEdge): Json {
        return toJsonImplementationEdge(value);
    },

    /** Return one ImplementationEdge from one JSON value. */
    fromJson(value: Json): ImplementationEdge {
        return fromJsonImplementationEdge(value);
    },
};

/** Encode one ImplementationEdge. */
export function encodeImplementationEdge(writer: BinaryWriter, value: ImplementationEdge): void {
    encodeGlobalSymbolId(writer, value.declaration);
    encodeGlobalSymbolId(writer, value.implementation);
}

/** Decode one ImplementationEdge. */
export function decodeImplementationEdge(reader: BinaryReader): ImplementationEdge {
    const declaration = decodeGlobalSymbolId(reader);
    const implementation = decodeGlobalSymbolId(reader);

    return {
        declaration,
        implementation,
    };
}

/** Return one JSON value for one ImplementationEdge. */
export function toJsonImplementationEdge(value: ImplementationEdge): Json {
    return {
        declaration: toJsonGlobalSymbolId(value.declaration),
        implementation: toJsonGlobalSymbolId(value.implementation),
    };
}

/** Return one ImplementationEdge from one JSON value. */
export function fromJsonImplementationEdge(value: Json): ImplementationEdge {
    const object = jsonObject(value);

    return {
        declaration: fromJsonGlobalSymbolId(jsonField(object, "declaration")),
        implementation: fromJsonGlobalSymbolId(jsonField(object, "implementation")),
    };
}

/** One member selected by lookup. */
export type MemberBinding = {
    /** The available member key. */
    readonly key: StaticKey;
    /** The available member kind. */
    readonly kind: MemberKind;
    /** The selected read and write types. */
    readonly access: PropertyAccess;
    /** Whether the member may be absent. */
    readonly isOptional: boolean;
    /** The declarations contributing to this binding. */
    readonly declarations: ReadonlyArray<MemberDeclaration>;
};

export const MemberBinding = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: MemberBinding): void {
        encodeMemberBinding(writer, value);
    },

    /** Decode one MemberBinding. */
    decode(reader: BinaryReader): MemberBinding {
        return decodeMemberBinding(reader);
    },

    /** Return this value as JSON. */
    toJson(value: MemberBinding): Json {
        return toJsonMemberBinding(value);
    },

    /** Return one MemberBinding from one JSON value. */
    fromJson(value: Json): MemberBinding {
        return fromJsonMemberBinding(value);
    },
};

/** Encode one MemberBinding. */
export function encodeMemberBinding(writer: BinaryWriter, value: MemberBinding): void {
    encodeStaticKey(writer, value.key);
    encodeMemberKind(writer, value.kind);
    encodePropertyAccess(writer, value.access);
    writer.writeBool(value.isOptional);
    writer.writeUnsigned(value.declarations.length);
    for (const item4 of value.declarations) {
        encodeMemberDeclaration(writer, item4);
    }
}

/** Decode one MemberBinding. */
export function decodeMemberBinding(reader: BinaryReader): MemberBinding {
    const key = decodeStaticKey(reader);
    const kind = decodeMemberKind(reader);
    const access = decodePropertyAccess(reader);
    const isOptional = reader.readBool();
    const declarations = (() => {
        const length4 = reader.readNumber();
        const items4: Array<MemberDeclaration> = [];
        for (let index = 0; index < length4; index += 1) {
            items4.push(decodeMemberDeclaration(reader));
        }
        return items4;
    })();

    return {
        key,
        kind,
        access,
        isOptional,
        declarations,
    };
}

/** Return one JSON value for one MemberBinding. */
export function toJsonMemberBinding(value: MemberBinding): Json {
    return {
        key: toJsonStaticKey(value.key),
        kind: toJsonMemberKind(value.kind),
        access: toJsonPropertyAccess(value.access),
        isOptional: value.isOptional,
        declarations: value.declarations.map((item0) => toJsonMemberDeclaration(item0)),
    };
}

/** Return one MemberBinding from one JSON value. */
export function fromJsonMemberBinding(value: Json): MemberBinding {
    const object = jsonObject(value);

    return {
        key: fromJsonStaticKey(jsonField(object, "key")),
        kind: fromJsonMemberKind(jsonField(object, "kind")),
        access: fromJsonPropertyAccess(jsonField(object, "access")),
        isOptional: jsonBool(jsonField(object, "isOptional")),
        declarations: jsonArray(jsonField(object, "declarations")).map((item0) =>
            fromJsonMemberDeclaration(item0),
        ),
    };
}

/** One member satisfying an interface requirement. */
export type MemberConformance = {
    /** The implementing member symbol. */
    readonly member: GlobalSymbolId;
    /** The required interface member symbol. */
    readonly requirement: GlobalSymbolId;
};

export const MemberConformance = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: MemberConformance): void {
        encodeMemberConformance(writer, value);
    },

    /** Decode one MemberConformance. */
    decode(reader: BinaryReader): MemberConformance {
        return decodeMemberConformance(reader);
    },

    /** Return this value as JSON. */
    toJson(value: MemberConformance): Json {
        return toJsonMemberConformance(value);
    },

    /** Return one MemberConformance from one JSON value. */
    fromJson(value: Json): MemberConformance {
        return fromJsonMemberConformance(value);
    },
};

/** Encode one MemberConformance. */
export function encodeMemberConformance(writer: BinaryWriter, value: MemberConformance): void {
    encodeGlobalSymbolId(writer, value.member);
    encodeGlobalSymbolId(writer, value.requirement);
}

/** Decode one MemberConformance. */
export function decodeMemberConformance(reader: BinaryReader): MemberConformance {
    const member = decodeGlobalSymbolId(reader);
    const requirement = decodeGlobalSymbolId(reader);

    return {
        member,
        requirement,
    };
}

/** Return one JSON value for one MemberConformance. */
export function toJsonMemberConformance(value: MemberConformance): Json {
    return {
        member: toJsonGlobalSymbolId(value.member),
        requirement: toJsonGlobalSymbolId(value.requirement),
    };
}

/** Return one MemberConformance from one JSON value. */
export function fromJsonMemberConformance(value: Json): MemberConformance {
    const object = jsonObject(value);

    return {
        member: fromJsonGlobalSymbolId(jsonField(object, "member")),
        requirement: fromJsonGlobalSymbolId(jsonField(object, "requirement")),
    };
}

/** One declaration contributing to a checked member binding. */
export type MemberDeclaration = {
    /** The selected declaration symbol. */
    readonly symbol: GlobalSymbolId;
    /** The declaration that exposed this member, through heritage. */
    readonly owner: GlobalSymbolId;
    /** The declaration family that exposed this member. */
    readonly origin: MemberOrigin;
    /** How the member behaves at a use site. */
    readonly role: MemberRole;
    /** The substituted callable type, when callable. */
    readonly callableType?: GlobalTypeId;
};

export const MemberDeclaration = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: MemberDeclaration): void {
        encodeMemberDeclaration(writer, value);
    },

    /** Decode one MemberDeclaration. */
    decode(reader: BinaryReader): MemberDeclaration {
        return decodeMemberDeclaration(reader);
    },

    /** Return this value as JSON. */
    toJson(value: MemberDeclaration): Json {
        return toJsonMemberDeclaration(value);
    },

    /** Return one MemberDeclaration from one JSON value. */
    fromJson(value: Json): MemberDeclaration {
        return fromJsonMemberDeclaration(value);
    },
};

/** Encode one MemberDeclaration. */
export function encodeMemberDeclaration(writer: BinaryWriter, value: MemberDeclaration): void {
    encodeGlobalSymbolId(writer, value.symbol);
    encodeGlobalSymbolId(writer, value.owner);
    encodeMemberOrigin(writer, value.origin);
    encodeMemberRole(writer, value.role);
    writer.writeOption(value.callableType, (value4) => {
        encodeGlobalTypeId(writer, value4);
    });
}

/** Decode one MemberDeclaration. */
export function decodeMemberDeclaration(reader: BinaryReader): MemberDeclaration {
    const symbol_ = decodeGlobalSymbolId(reader);
    const owner = decodeGlobalSymbolId(reader);
    const origin = decodeMemberOrigin(reader);
    const role = decodeMemberRole(reader);
    const callableType = reader.readOption(() => decodeGlobalTypeId(reader));

    return {
        symbol: symbol_,
        owner,
        origin,
        role,
        ...(callableType === undefined ? {} : { callableType }),
    };
}

/** Return one JSON value for one MemberDeclaration. */
export function toJsonMemberDeclaration(value: MemberDeclaration): Json {
    return {
        symbol: toJsonGlobalSymbolId(value.symbol),
        owner: toJsonGlobalSymbolId(value.owner),
        origin: toJsonMemberOrigin(value.origin),
        role: toJsonMemberRole(value.role),
        ...(value.callableType === undefined
            ? {}
            : { callableType: toJsonGlobalTypeId(value.callableType) }),
    };
}

/** Return one MemberDeclaration from one JSON value. */
export function fromJsonMemberDeclaration(value: Json): MemberDeclaration {
    const object = jsonObject(value);

    return {
        symbol: fromJsonGlobalSymbolId(jsonField(object, "symbol")),
        owner: fromJsonGlobalSymbolId(jsonField(object, "owner")),
        origin: fromJsonMemberOrigin(jsonField(object, "origin")),
        role: fromJsonMemberRole(jsonField(object, "role")),
        callableType: jsonOptional(object, "callableType", (value) => fromJsonGlobalTypeId(value)),
    };
}

/** One member kind. */
export type MemberKind =
    | "field"
    | "property"
    | "method"
    | "constructor"
    | "callSignature"
    | "constructSignature"
    | "indexSignature"
    | "associatedType"
    | "associatedConst"
    | "variant";

export const MemberKind = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: MemberKind): void {
        encodeMemberKind(writer, value);
    },

    /** Decode one MemberKind. */
    decode(reader: BinaryReader): MemberKind {
        return decodeMemberKind(reader);
    },

    /** Return this value as JSON. */
    toJson(value: MemberKind): Json {
        return toJsonMemberKind(value);
    },

    /** Return one MemberKind from one JSON value. */
    fromJson(value: Json): MemberKind {
        return fromJsonMemberKind(value);
    },
};

/** Encode one MemberKind. */
export function encodeMemberKind(writer: BinaryWriter, value: MemberKind): void {
    switch (value) {
        case "field":
            writer.writeUnsigned(0);
            return;
        case "property":
            writer.writeUnsigned(1);
            return;
        case "method":
            writer.writeUnsigned(2);
            return;
        case "constructor":
            writer.writeUnsigned(3);
            return;
        case "callSignature":
            writer.writeUnsigned(4);
            return;
        case "constructSignature":
            writer.writeUnsigned(5);
            return;
        case "indexSignature":
            writer.writeUnsigned(6);
            return;
        case "associatedType":
            writer.writeUnsigned(7);
            return;
        case "associatedConst":
            writer.writeUnsigned(8);
            return;
        case "variant":
            writer.writeUnsigned(9);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one MemberKind. */
export function decodeMemberKind(reader: BinaryReader): MemberKind {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "field";
        case 1:
            return "property";
        case 2:
            return "method";
        case 3:
            return "constructor";
        case 4:
            return "callSignature";
        case 5:
            return "constructSignature";
        case 6:
            return "indexSignature";
        case 7:
            return "associatedType";
        case 8:
            return "associatedConst";
        case 9:
            return "variant";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one MemberKind. */
export function toJsonMemberKind(value: MemberKind): Json {
    return value;
}

/** Return one MemberKind from one JSON value. */
export function fromJsonMemberKind(value: Json): MemberKind {
    const variant = jsonString(value);

    switch (variant) {
        case "field":
            return "field";
        case "property":
            return "property";
        case "method":
            return "method";
        case "constructor":
            return "constructor";
        case "callSignature":
            return "callSignature";
        case "constructSignature":
            return "constructSignature";
        case "indexSignature":
            return "indexSignature";
        case "associatedType":
            return "associatedType";
        case "associatedConst":
            return "associatedConst";
        case "variant":
            return "variant";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** The declaration family one member came from, ordered by shadowing precedence. */
export type MemberOrigin = "declaration" | "rootedExtension" | "blanketExtension";

export const MemberOrigin = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: MemberOrigin): void {
        encodeMemberOrigin(writer, value);
    },

    /** Decode one MemberOrigin. */
    decode(reader: BinaryReader): MemberOrigin {
        return decodeMemberOrigin(reader);
    },

    /** Return this value as JSON. */
    toJson(value: MemberOrigin): Json {
        return toJsonMemberOrigin(value);
    },

    /** Return one MemberOrigin from one JSON value. */
    fromJson(value: Json): MemberOrigin {
        return fromJsonMemberOrigin(value);
    },
};

/** Encode one MemberOrigin. */
export function encodeMemberOrigin(writer: BinaryWriter, value: MemberOrigin): void {
    switch (value) {
        case "declaration":
            writer.writeUnsigned(0);
            return;
        case "rootedExtension":
            writer.writeUnsigned(1);
            return;
        case "blanketExtension":
            writer.writeUnsigned(2);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one MemberOrigin. */
export function decodeMemberOrigin(reader: BinaryReader): MemberOrigin {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "declaration";
        case 1:
            return "rootedExtension";
        case 2:
            return "blanketExtension";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one MemberOrigin. */
export function toJsonMemberOrigin(value: MemberOrigin): Json {
    return value;
}

/** Return one MemberOrigin from one JSON value. */
export function fromJsonMemberOrigin(value: Json): MemberOrigin {
    const variant = jsonString(value);

    switch (variant) {
        case "declaration":
            return "declaration";
        case "rootedExtension":
            return "rootedExtension";
        case "blanketExtension":
            return "blanketExtension";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** How one declaration member behaves at a use site. */
export type MemberRole = "field" | "method" | "getter" | "setter" | "associated" | "variantValue";

export const MemberRole = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: MemberRole): void {
        encodeMemberRole(writer, value);
    },

    /** Decode one MemberRole. */
    decode(reader: BinaryReader): MemberRole {
        return decodeMemberRole(reader);
    },

    /** Return this value as JSON. */
    toJson(value: MemberRole): Json {
        return toJsonMemberRole(value);
    },

    /** Return one MemberRole from one JSON value. */
    fromJson(value: Json): MemberRole {
        return fromJsonMemberRole(value);
    },
};

/** Encode one MemberRole. */
export function encodeMemberRole(writer: BinaryWriter, value: MemberRole): void {
    switch (value) {
        case "field":
            writer.writeUnsigned(0);
            return;
        case "method":
            writer.writeUnsigned(1);
            return;
        case "getter":
            writer.writeUnsigned(2);
            return;
        case "setter":
            writer.writeUnsigned(3);
            return;
        case "associated":
            writer.writeUnsigned(4);
            return;
        case "variantValue":
            writer.writeUnsigned(5);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one MemberRole. */
export function decodeMemberRole(reader: BinaryReader): MemberRole {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "field";
        case 1:
            return "method";
        case 2:
            return "getter";
        case 3:
            return "setter";
        case 4:
            return "associated";
        case 5:
            return "variantValue";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one MemberRole. */
export function toJsonMemberRole(value: MemberRole): Json {
    return value;
}

/** Return one MemberRole from one JSON value. */
export function fromJsonMemberRole(value: Json): MemberRole {
    const variant = jsonString(value);

    switch (variant) {
        case "field":
            return "field";
        case "method":
            return "method";
        case "getter":
            return "getter";
        case "setter":
            return "setter";
        case "associated":
            return "associated";
        case "variantValue":
            return "variantValue";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** Member bindings added by one DIR phase. */
export type MemberSegment = {
    /** The module id of the member segment. */
    readonly moduleId: ModuleId;
    /** Lookup subjects selected at source sites, each with the member key a keyed lookup names. */
    readonly subjects: ReadonlyMap<MemberSite, readonly [MemberSubject, StaticKey | undefined]>;
    /** Member bindings flattened once per declared owner and space. */
    readonly bindings: ReadonlyMap<
        readonly [GlobalSymbolId, MemberSpace],
        ReadonlyArray<MemberBinding>
    >;
    /** The membership each settled subject selects. */
    readonly memberships: ReadonlyMap<MemberSubject, Membership>;
    /** The members selected to satisfy each `implements` clause, keyed by its source node. */
    readonly conformances: ReadonlyMap<GlobalNodeIdAny, ReadonlyArray<MemberConformance>>;
    /** The constructable backing alternatives derived per newtype, in selection order. */
    readonly newtypeConstructors: ReadonlyMap<GlobalSymbolId, ReadonlyArray<NewtypeConstructor>>;
};

export const MemberSegment = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: MemberSegment): void {
        encodeMemberSegment(writer, value);
    },

    /** Decode one MemberSegment. */
    decode(reader: BinaryReader): MemberSegment {
        return decodeMemberSegment(reader);
    },

    /** Return this value as JSON. */
    toJson(value: MemberSegment): Json {
        return toJsonMemberSegment(value);
    },

    /** Return one MemberSegment from one JSON value. */
    fromJson(value: Json): MemberSegment {
        return fromJsonMemberSegment(value);
    },
};

/** Encode one MemberSegment. */
export function encodeMemberSegment(writer: BinaryWriter, value: MemberSegment): void {
    encodeModuleId(writer, value.moduleId);
    const entries1 = Array.from(value.subjects.entries()).map(([key1, item1]) => {
        const keyBytes = nestedBytes((writer) => {
            encodeMemberSite(writer, key1);
        });
        return { key1, item1, keyBytes };
    });
    entries1.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries1.length);
    for (const entry1 of entries1) {
        encodeMemberSite(writer, entry1.key1);
        encodeMemberSubject(writer, entry1.item1[0]);
        writer.writeOption(entry1.item1[1], (value3) => {
            encodeStaticKey(writer, value3);
        });
    }
    const entries2 = Array.from(value.bindings.entries()).map(([key2, item2]) => {
        const keyBytes = nestedBytes((writer) => {
            encodeGlobalSymbolId(writer, key2[0]);
            encodeMemberSpace(writer, key2[1]);
        });
        return { key2, item2, keyBytes };
    });
    entries2.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries2.length);
    for (const entry2 of entries2) {
        encodeGlobalSymbolId(writer, entry2.key2[0]);
        encodeMemberSpace(writer, entry2.key2[1]);
        writer.writeUnsigned(entry2.item2.length);
        for (const item3 of entry2.item2) {
            encodeMemberBinding(writer, item3);
        }
    }
    const entries3 = Array.from(value.memberships.entries()).map(([key3, item3]) => {
        const keyBytes = nestedBytes((writer) => {
            encodeMemberSubject(writer, key3);
        });
        return { key3, item3, keyBytes };
    });
    entries3.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries3.length);
    for (const entry3 of entries3) {
        encodeMemberSubject(writer, entry3.key3);
        encodeMembership(writer, entry3.item3);
    }
    const entries4 = Array.from(value.conformances.entries()).map(([key4, item4]) => {
        const keyBytes = nestedBytes((writer) => {
            encodeGlobalNodeIdAny(writer, key4);
        });
        return { key4, item4, keyBytes };
    });
    entries4.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries4.length);
    for (const entry4 of entries4) {
        encodeGlobalNodeIdAny(writer, entry4.key4);
        writer.writeUnsigned(entry4.item4.length);
        for (const item5 of entry4.item4) {
            encodeMemberConformance(writer, item5);
        }
    }
    const entries5 = Array.from(value.newtypeConstructors.entries()).map(([key5, item5]) => {
        const keyBytes = nestedBytes((writer) => {
            encodeGlobalSymbolId(writer, key5);
        });
        return { key5, item5, keyBytes };
    });
    entries5.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries5.length);
    for (const entry5 of entries5) {
        encodeGlobalSymbolId(writer, entry5.key5);
        writer.writeUnsigned(entry5.item5.length);
        for (const item6 of entry5.item5) {
            encodeNewtypeConstructor(writer, item6);
        }
    }
}

/** Decode one MemberSegment. */
export function decodeMemberSegment(reader: BinaryReader): MemberSegment {
    const moduleId = decodeModuleId(reader);
    const subjects = (() => {
        const length1 = reader.readNumber();
        const items1 = new Map<MemberSite, readonly [MemberSubject, StaticKey | undefined]>();
        for (let index = 0; index < length1; index += 1) {
            items1.set(decodeMemberSite(reader), [
                decodeMemberSubject(reader),
                reader.readOption(() => decodeStaticKey(reader)),
            ] as const);
        }
        return items1;
    })();
    const bindings = (() => {
        const length2 = reader.readNumber();
        const items2 = new Map<
            readonly [GlobalSymbolId, MemberSpace],
            ReadonlyArray<MemberBinding>
        >();
        for (let index = 0; index < length2; index += 1) {
            items2.set(
                [decodeGlobalSymbolId(reader), decodeMemberSpace(reader)] as const,
                (() => {
                    const length4 = reader.readNumber();
                    const items4: Array<MemberBinding> = [];
                    for (let index = 0; index < length4; index += 1) {
                        items4.push(decodeMemberBinding(reader));
                    }
                    return items4;
                })(),
            );
        }
        return items2;
    })();
    const memberships = (() => {
        const length3 = reader.readNumber();
        const items3 = new Map<MemberSubject, Membership>();
        for (let index = 0; index < length3; index += 1) {
            items3.set(decodeMemberSubject(reader), decodeMembership(reader));
        }
        return items3;
    })();
    const conformances = (() => {
        const length4 = reader.readNumber();
        const items4 = new Map<GlobalNodeIdAny, ReadonlyArray<MemberConformance>>();
        for (let index = 0; index < length4; index += 1) {
            items4.set(
                decodeGlobalNodeIdAny(reader),
                (() => {
                    const length6 = reader.readNumber();
                    const items6: Array<MemberConformance> = [];
                    for (let index = 0; index < length6; index += 1) {
                        items6.push(decodeMemberConformance(reader));
                    }
                    return items6;
                })(),
            );
        }
        return items4;
    })();
    const newtypeConstructors = (() => {
        const length5 = reader.readNumber();
        const items5 = new Map<GlobalSymbolId, ReadonlyArray<NewtypeConstructor>>();
        for (let index = 0; index < length5; index += 1) {
            items5.set(
                decodeGlobalSymbolId(reader),
                (() => {
                    const length7 = reader.readNumber();
                    const items7: Array<NewtypeConstructor> = [];
                    for (let index = 0; index < length7; index += 1) {
                        items7.push(decodeNewtypeConstructor(reader));
                    }
                    return items7;
                })(),
            );
        }
        return items5;
    })();

    return {
        moduleId,
        subjects,
        bindings,
        memberships,
        conformances,
        newtypeConstructors,
    };
}

/** Return one JSON value for one MemberSegment. */
export function toJsonMemberSegment(value: MemberSegment): Json {
    return {
        moduleId: toJsonModuleId(value.moduleId),
        subjects: Array.from(value.subjects.entries()).map(
            ([key0, item0]) =>
                [
                    toJsonMemberSite(key0),
                    [
                        toJsonMemberSubject(item0[0]),
                        item0[1] === undefined ? null : toJsonStaticKey(item0[1]),
                    ],
                ] as const,
        ),
        bindings: Array.from(value.bindings.entries()).map(
            ([key0, item0]) =>
                [
                    [toJsonGlobalSymbolId(key0[0]), toJsonMemberSpace(key0[1])],
                    item0.map((item1) => toJsonMemberBinding(item1)),
                ] as const,
        ),
        memberships: Array.from(value.memberships.entries()).map(
            ([key0, item0]) => [toJsonMemberSubject(key0), toJsonMembership(item0)] as const,
        ),
        conformances: Array.from(value.conformances.entries()).map(
            ([key0, item0]) =>
                [
                    toJsonGlobalNodeIdAny(key0),
                    item0.map((item1) => toJsonMemberConformance(item1)),
                ] as const,
        ),
        newtypeConstructors: Array.from(value.newtypeConstructors.entries()).map(
            ([key0, item0]) =>
                [
                    toJsonGlobalSymbolId(key0),
                    item0.map((item1) => toJsonNewtypeConstructor(item1)),
                ] as const,
        ),
    };
}

/** Return one MemberSegment from one JSON value. */
export function fromJsonMemberSegment(value: Json): MemberSegment {
    const object = jsonObject(value);

    return {
        moduleId: fromJsonModuleId(jsonField(object, "moduleId")),
        subjects: new Map(
            jsonArray(jsonField(object, "subjects")).map((entry) => {
                const items = jsonArray(entry);
                if (items.length !== 2) {
                    throw new SerdeError(`expected JSON map entry length 2: ${items.length}`);
                }
                const key0 = items[0];
                const item0 = items[1];
                return [
                    fromJsonMemberSite(key0),
                    (() => {
                        const items = jsonArray(item0);
                        if (items.length !== 2) {
                            throw new SerdeError(`expected JSON tuple length 2: ${items.length}`);
                        }
                        return [
                            fromJsonMemberSubject(items[0]),
                            items[1] === null ? undefined : fromJsonStaticKey(items[1]),
                        ] as const;
                    })(),
                ] as const;
            }),
        ),
        bindings: new Map(
            jsonArray(jsonField(object, "bindings")).map((entry) => {
                const items = jsonArray(entry);
                if (items.length !== 2) {
                    throw new SerdeError(`expected JSON map entry length 2: ${items.length}`);
                }
                const key0 = items[0];
                const item0 = items[1];
                return [
                    (() => {
                        const items = jsonArray(key0);
                        if (items.length !== 2) {
                            throw new SerdeError(`expected JSON tuple length 2: ${items.length}`);
                        }
                        return [
                            fromJsonGlobalSymbolId(items[0]),
                            fromJsonMemberSpace(items[1]),
                        ] as const;
                    })(),
                    jsonArray(item0).map((item1) => fromJsonMemberBinding(item1)),
                ] as const;
            }),
        ),
        memberships: new Map(
            jsonArray(jsonField(object, "memberships")).map((entry) => {
                const items = jsonArray(entry);
                if (items.length !== 2) {
                    throw new SerdeError(`expected JSON map entry length 2: ${items.length}`);
                }
                const key0 = items[0];
                const item0 = items[1];
                return [fromJsonMemberSubject(key0), fromJsonMembership(item0)] as const;
            }),
        ),
        conformances: new Map(
            jsonArray(jsonField(object, "conformances")).map((entry) => {
                const items = jsonArray(entry);
                if (items.length !== 2) {
                    throw new SerdeError(`expected JSON map entry length 2: ${items.length}`);
                }
                const key0 = items[0];
                const item0 = items[1];
                return [
                    fromJsonGlobalNodeIdAny(key0),
                    jsonArray(item0).map((item1) => fromJsonMemberConformance(item1)),
                ] as const;
            }),
        ),
        newtypeConstructors: new Map(
            jsonArray(jsonField(object, "newtypeConstructors")).map((entry) => {
                const items = jsonArray(entry);
                if (items.length !== 2) {
                    throw new SerdeError(`expected JSON map entry length 2: ${items.length}`);
                }
                const key0 = items[0];
                const item0 = items[1];
                return [
                    fromJsonGlobalSymbolId(key0),
                    jsonArray(item0).map((item1) => fromJsonNewtypeConstructor(item1)),
                ] as const;
            }),
        ),
    };
}

/** One source member lookup location. */
export type MemberSite =
    /** A member expression or member type node. */
    | {
          readonly kind: "node";
          readonly node: GlobalNodeIdAny;
      }
    /** One segment of a flat reference path. */
    | {
          readonly kind: "path";
          /** The reference path node. */
          readonly node: GlobalNodeIdAny;
          /** The selected path segment. */
          readonly segment: number;
      };

export const MemberSite = {
    /** A member expression or member type node. */
    node(node: GlobalNodeIdAny): MemberSite {
        return { kind: "node", node };
    },

    /** One segment of a flat reference path. */
    path(node: GlobalNodeIdAny, segment: number): MemberSite {
        return { kind: "path", node, segment };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: MemberSite): void {
        encodeMemberSite(writer, value);
    },

    /** Decode one MemberSite. */
    decode(reader: BinaryReader): MemberSite {
        return decodeMemberSite(reader);
    },

    /** Return this value as JSON. */
    toJson(value: MemberSite): Json {
        return toJsonMemberSite(value);
    },

    /** Return one MemberSite from one JSON value. */
    fromJson(value: Json): MemberSite {
        return fromJsonMemberSite(value);
    },
};

/** Encode one MemberSite. */
export function encodeMemberSite(writer: BinaryWriter, value: MemberSite): void {
    switch (value.kind) {
        case "node":
            writer.writeUnsigned(0);
            encodeGlobalNodeIdAny(writer, value.node);
            return;
        case "path":
            writer.writeUnsigned(1);
            encodeGlobalNodeIdAny(writer, value.node);
            writer.writeUnsigned(value.segment);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one MemberSite. */
export function decodeMemberSite(reader: BinaryReader): MemberSite {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const node = decodeGlobalNodeIdAny(reader);

            return { kind: "node", node };
        }
        case 1: {
            const node = decodeGlobalNodeIdAny(reader);
            const segment = reader.readNumber();

            return {
                kind: "path",
                node,
                segment,
            };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one MemberSite. */
export function toJsonMemberSite(value: MemberSite): Json {
    switch (value.kind) {
        case "node":
            return {
                kind: "node",
                node: toJsonGlobalNodeIdAny(value.node),
            };
        case "path":
            return {
                kind: "path",
                node: toJsonGlobalNodeIdAny(value.node),
                segment: value.segment,
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one MemberSite from one JSON value. */
export function fromJsonMemberSite(value: Json): MemberSite {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "node":
            return {
                kind,
                node: fromJsonGlobalNodeIdAny(jsonField(object, "node")),
            };
        case "path":
            return {
                kind,
                node: fromJsonGlobalNodeIdAny(jsonField(object, "node")),
                segment: jsonInteger(jsonField(object, "segment")),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** One source contributing declared members to a projected subject membership. */
export type MemberSource = {
    /** The declaring owner whose flattened bindings contribute. */
    readonly owner: GlobalSymbolId;
    /** The type arguments reopening the owner's parameters, in declaration order. */
    readonly arguments: TypeListId;
};

export const MemberSource = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: MemberSource): void {
        encodeMemberSource(writer, value);
    },

    /** Decode one MemberSource. */
    decode(reader: BinaryReader): MemberSource {
        return decodeMemberSource(reader);
    },

    /** Return this value as JSON. */
    toJson(value: MemberSource): Json {
        return toJsonMemberSource(value);
    },

    /** Return one MemberSource from one JSON value. */
    fromJson(value: Json): MemberSource {
        return fromJsonMemberSource(value);
    },
};

/** Encode one MemberSource. */
export function encodeMemberSource(writer: BinaryWriter, value: MemberSource): void {
    encodeGlobalSymbolId(writer, value.owner);
    encodeTypeListId(writer, value.arguments);
}

/** Decode one MemberSource. */
export function decodeMemberSource(reader: BinaryReader): MemberSource {
    const owner = decodeGlobalSymbolId(reader);
    const arguments_ = decodeTypeListId(reader);

    return {
        owner,
        arguments: arguments_,
    };
}

/** Return one JSON value for one MemberSource. */
export function toJsonMemberSource(value: MemberSource): Json {
    return {
        owner: toJsonGlobalSymbolId(value.owner),
        arguments: toJsonTypeListId(value.arguments),
    };
}

/** Return one MemberSource from one JSON value. */
export function fromJsonMemberSource(value: Json): MemberSource {
    const object = jsonObject(value);

    return {
        owner: fromJsonGlobalSymbolId(jsonField(object, "owner")),
        arguments: fromJsonTypeListId(jsonField(object, "arguments")),
    };
}

/** Member namespace selected by member lookup. */
export type MemberSpace = "instance" | "static";

export const MemberSpace = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: MemberSpace): void {
        encodeMemberSpace(writer, value);
    },

    /** Decode one MemberSpace. */
    decode(reader: BinaryReader): MemberSpace {
        return decodeMemberSpace(reader);
    },

    /** Return this value as JSON. */
    toJson(value: MemberSpace): Json {
        return toJsonMemberSpace(value);
    },

    /** Return one MemberSpace from one JSON value. */
    fromJson(value: Json): MemberSpace {
        return fromJsonMemberSpace(value);
    },
};

/** Encode one MemberSpace. */
export function encodeMemberSpace(writer: BinaryWriter, value: MemberSpace): void {
    switch (value) {
        case "instance":
            writer.writeUnsigned(0);
            return;
        case "static":
            writer.writeUnsigned(1);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one MemberSpace. */
export function decodeMemberSpace(reader: BinaryReader): MemberSpace {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "instance";
        case 1:
            return "static";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one MemberSpace. */
export function toJsonMemberSpace(value: MemberSpace): Json {
    return value;
}

/** Return one MemberSpace from one JSON value. */
export function fromJsonMemberSpace(value: Json): MemberSpace {
    const variant = jsonString(value);

    switch (variant) {
        case "instance":
            return "instance";
        case "static":
            return "static";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** The exact input to member lookup. */
export type MemberSubject = {
    /** The use-site receiver type before implicit projections. */
    readonly receiver: GlobalTypeId;
    /** The type searched by member lookup. */
    readonly target: GlobalTypeId;
    /** The selected instance or static member space. */
    readonly space: MemberSpace;
    /** The generic template assumptions active at the source site. */
    readonly scope?: GlobalGenericTemplateId;
    /** The type whose member keys are enumerated. */
    readonly keySource: GlobalTypeId;
};

export const MemberSubject = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: MemberSubject): void {
        encodeMemberSubject(writer, value);
    },

    /** Decode one MemberSubject. */
    decode(reader: BinaryReader): MemberSubject {
        return decodeMemberSubject(reader);
    },

    /** Return this value as JSON. */
    toJson(value: MemberSubject): Json {
        return toJsonMemberSubject(value);
    },

    /** Return one MemberSubject from one JSON value. */
    fromJson(value: Json): MemberSubject {
        return fromJsonMemberSubject(value);
    },
};

/** Encode one MemberSubject. */
export function encodeMemberSubject(writer: BinaryWriter, value: MemberSubject): void {
    encodeGlobalTypeId(writer, value.receiver);
    encodeGlobalTypeId(writer, value.target);
    encodeMemberSpace(writer, value.space);
    writer.writeOption(value.scope, (value3) => {
        encodeGlobalGenericTemplateId(writer, value3);
    });
    encodeGlobalTypeId(writer, value.keySource);
}

/** Decode one MemberSubject. */
export function decodeMemberSubject(reader: BinaryReader): MemberSubject {
    const receiver = decodeGlobalTypeId(reader);
    const target = decodeGlobalTypeId(reader);
    const space = decodeMemberSpace(reader);
    const scope = reader.readOption(() => decodeGlobalGenericTemplateId(reader));
    const keySource = decodeGlobalTypeId(reader);

    return {
        receiver,
        target,
        space,
        ...(scope === undefined ? {} : { scope }),
        keySource,
    };
}

/** Return one JSON value for one MemberSubject. */
export function toJsonMemberSubject(value: MemberSubject): Json {
    return {
        receiver: toJsonGlobalTypeId(value.receiver),
        target: toJsonGlobalTypeId(value.target),
        space: toJsonMemberSpace(value.space),
        ...(value.scope === undefined ? {} : { scope: toJsonGlobalGenericTemplateId(value.scope) }),
        keySource: toJsonGlobalTypeId(value.keySource),
    };
}

/** Return one MemberSubject from one JSON value. */
export function fromJsonMemberSubject(value: Json): MemberSubject {
    const object = jsonObject(value);

    return {
        receiver: fromJsonGlobalTypeId(jsonField(object, "receiver")),
        target: fromJsonGlobalTypeId(jsonField(object, "target")),
        space: fromJsonMemberSpace(jsonField(object, "space")),
        scope: jsonOptional(object, "scope", (value) => fromJsonGlobalGenericTemplateId(value)),
        keySource: fromJsonGlobalTypeId(jsonField(object, "keySource")),
    };
}

/** The membership one settled subject selects. */
export type Membership = {
    /** The receiver the sources' `this` types reopen at. */
    readonly receiver: GlobalTypeId;
    /** The contributing sources, the nominal owner first. */
    readonly sources: ReadonlyArray<MemberSource>;
    /** Structural bindings whose types are already concrete. */
    readonly structural: ReadonlyArray<MemberBinding>;
};

export const Membership = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: Membership): void {
        encodeMembership(writer, value);
    },

    /** Decode one Membership. */
    decode(reader: BinaryReader): Membership {
        return decodeMembership(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Membership): Json {
        return toJsonMembership(value);
    },

    /** Return one Membership from one JSON value. */
    fromJson(value: Json): Membership {
        return fromJsonMembership(value);
    },
};

/** Encode one Membership. */
export function encodeMembership(writer: BinaryWriter, value: Membership): void {
    encodeGlobalTypeId(writer, value.receiver);
    writer.writeUnsigned(value.sources.length);
    for (const item1 of value.sources) {
        encodeMemberSource(writer, item1);
    }
    writer.writeUnsigned(value.structural.length);
    for (const item2 of value.structural) {
        encodeMemberBinding(writer, item2);
    }
}

/** Decode one Membership. */
export function decodeMembership(reader: BinaryReader): Membership {
    const receiver = decodeGlobalTypeId(reader);
    const sources = (() => {
        const length1 = reader.readNumber();
        const items1: Array<MemberSource> = [];
        for (let index = 0; index < length1; index += 1) {
            items1.push(decodeMemberSource(reader));
        }
        return items1;
    })();
    const structural = (() => {
        const length2 = reader.readNumber();
        const items2: Array<MemberBinding> = [];
        for (let index = 0; index < length2; index += 1) {
            items2.push(decodeMemberBinding(reader));
        }
        return items2;
    })();

    return {
        receiver,
        sources,
        structural,
    };
}

/** Return one JSON value for one Membership. */
export function toJsonMembership(value: Membership): Json {
    return {
        receiver: toJsonGlobalTypeId(value.receiver),
        sources: value.sources.map((item0) => toJsonMemberSource(item0)),
        structural: value.structural.map((item0) => toJsonMemberBinding(item0)),
    };
}

/** Return one Membership from one JSON value. */
export function fromJsonMembership(value: Json): Membership {
    const object = jsonObject(value);

    return {
        receiver: fromJsonGlobalTypeId(jsonField(object, "receiver")),
        sources: jsonArray(jsonField(object, "sources")).map((item0) =>
            fromJsonMemberSource(item0),
        ),
        structural: jsonArray(jsonField(object, "structural")).map((item0) =>
            fromJsonMemberBinding(item0),
        ),
    };
}

/** One constructable newtype backing alternative. */
export type NewtypeConstructor = {
    /** The reduced backing alternative selected by this constructor. */
    readonly backing: GlobalTypeId;
    /** The callable constructor type. */
    readonly ty: GlobalTypeId;
};

export const NewtypeConstructor = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: NewtypeConstructor): void {
        encodeNewtypeConstructor(writer, value);
    },

    /** Decode one NewtypeConstructor. */
    decode(reader: BinaryReader): NewtypeConstructor {
        return decodeNewtypeConstructor(reader);
    },

    /** Return this value as JSON. */
    toJson(value: NewtypeConstructor): Json {
        return toJsonNewtypeConstructor(value);
    },

    /** Return one NewtypeConstructor from one JSON value. */
    fromJson(value: Json): NewtypeConstructor {
        return fromJsonNewtypeConstructor(value);
    },
};

/** Encode one NewtypeConstructor. */
export function encodeNewtypeConstructor(writer: BinaryWriter, value: NewtypeConstructor): void {
    encodeGlobalTypeId(writer, value.backing);
    encodeGlobalTypeId(writer, value.ty);
}

/** Decode one NewtypeConstructor. */
export function decodeNewtypeConstructor(reader: BinaryReader): NewtypeConstructor {
    const backing = decodeGlobalTypeId(reader);
    const ty = decodeGlobalTypeId(reader);

    return {
        backing,
        ty,
    };
}

/** Return one JSON value for one NewtypeConstructor. */
export function toJsonNewtypeConstructor(value: NewtypeConstructor): Json {
    return {
        backing: toJsonGlobalTypeId(value.backing),
        ty: toJsonGlobalTypeId(value.ty),
    };
}

/** Return one NewtypeConstructor from one JSON value. */
export function fromJsonNewtypeConstructor(value: Json): NewtypeConstructor {
    const object = jsonObject(value);

    return {
        backing: fromJsonGlobalTypeId(jsonField(object, "backing")),
        ty: fromJsonGlobalTypeId(jsonField(object, "ty")),
    };
}
