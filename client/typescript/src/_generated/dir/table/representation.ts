/* generated by destack_client_generate, do not edit */

import {
    BinaryReader,
    BinaryWriter,
    Json,
    SerdeError,
    compareBytes,
    jsonArray,
    jsonBool,
    jsonField,
    jsonObject,
    nestedBytes,
} from "../../../protocol/serde.js";
import type { GlobalSymbolId } from "../symbol/symbol.js";
import type { GlobalTypeId } from "../type/type.js";
import type { Space } from "../type/type.js";
import type { ModuleId } from "../../source/file/model/module.js";
import {
    decodeGlobalSymbolId,
    encodeGlobalSymbolId,
    fromJsonGlobalSymbolId,
    toJsonGlobalSymbolId,
} from "../symbol/symbol.js";
import {
    decodeGlobalTypeId,
    encodeGlobalTypeId,
    fromJsonGlobalTypeId,
    toJsonGlobalTypeId,
} from "../type/type.js";
import { decodeSpace, encodeSpace, fromJsonSpace, toJsonSpace } from "../type/type.js";
import {
    decodeModuleId,
    encodeModuleId,
    fromJsonModuleId,
    toJsonModuleId,
} from "../../source/file/model/module.js";

/** The layout policies the check pass commits for one module's nominal declarations. */
export type RepresentationSegment = {
    /** The module id of the representation segment. */
    readonly moduleId: ModuleId;
    /** Whether each nominal declaration derives Copy, holding when its stored values do. */
    readonly copyDerivations: ReadonlyMap<GlobalSymbolId, boolean>;
    /** Whether values of each visited type copy. */
    readonly copies: ReadonlyMap<GlobalTypeId, boolean>;
    /** The space each placed nominal declaration's instances live in. */
    readonly spaces: ReadonlyMap<GlobalSymbolId, Space>;
};

export const RepresentationSegment = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: RepresentationSegment): void {
        encodeRepresentationSegment(writer, value);
    },

    /** Decode one RepresentationSegment. */
    decode(reader: BinaryReader): RepresentationSegment {
        return decodeRepresentationSegment(reader);
    },

    /** Return this value as JSON. */
    toJson(value: RepresentationSegment): Json {
        return toJsonRepresentationSegment(value);
    },

    /** Return one RepresentationSegment from one JSON value. */
    fromJson(value: Json): RepresentationSegment {
        return fromJsonRepresentationSegment(value);
    },
};

/** Encode one RepresentationSegment. */
export function encodeRepresentationSegment(
    writer: BinaryWriter,
    value: RepresentationSegment,
): void {
    encodeModuleId(writer, value.moduleId);
    const entries1 = Array.from(value.copyDerivations.entries()).map(([key1, item1]) => {
        const keyBytes = nestedBytes((writer) => {
            encodeGlobalSymbolId(writer, key1);
        });
        return { key1, item1, keyBytes };
    });
    entries1.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries1.length);
    for (const entry1 of entries1) {
        encodeGlobalSymbolId(writer, entry1.key1);
        writer.writeBool(entry1.item1);
    }
    const entries2 = Array.from(value.copies.entries()).map(([key2, item2]) => {
        const keyBytes = nestedBytes((writer) => {
            encodeGlobalTypeId(writer, key2);
        });
        return { key2, item2, keyBytes };
    });
    entries2.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries2.length);
    for (const entry2 of entries2) {
        encodeGlobalTypeId(writer, entry2.key2);
        writer.writeBool(entry2.item2);
    }
    const entries3 = Array.from(value.spaces.entries()).map(([key3, item3]) => {
        const keyBytes = nestedBytes((writer) => {
            encodeGlobalSymbolId(writer, key3);
        });
        return { key3, item3, keyBytes };
    });
    entries3.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries3.length);
    for (const entry3 of entries3) {
        encodeGlobalSymbolId(writer, entry3.key3);
        encodeSpace(writer, entry3.item3);
    }
}

/** Decode one RepresentationSegment. */
export function decodeRepresentationSegment(reader: BinaryReader): RepresentationSegment {
    const moduleId = decodeModuleId(reader);
    const copyDerivations = (() => {
        const length1 = reader.readNumber();
        const items1 = new Map<GlobalSymbolId, boolean>();
        for (let index = 0; index < length1; index += 1) {
            items1.set(decodeGlobalSymbolId(reader), reader.readBool());
        }
        return items1;
    })();
    const copies = (() => {
        const length2 = reader.readNumber();
        const items2 = new Map<GlobalTypeId, boolean>();
        for (let index = 0; index < length2; index += 1) {
            items2.set(decodeGlobalTypeId(reader), reader.readBool());
        }
        return items2;
    })();
    const spaces = (() => {
        const length3 = reader.readNumber();
        const items3 = new Map<GlobalSymbolId, Space>();
        for (let index = 0; index < length3; index += 1) {
            items3.set(decodeGlobalSymbolId(reader), decodeSpace(reader));
        }
        return items3;
    })();

    return {
        moduleId,
        copyDerivations,
        copies,
        spaces,
    };
}

/** Return one JSON value for one RepresentationSegment. */
export function toJsonRepresentationSegment(value: RepresentationSegment): Json {
    return {
        moduleId: toJsonModuleId(value.moduleId),
        copyDerivations: Array.from(value.copyDerivations.entries()).map(
            ([key0, item0]) => [toJsonGlobalSymbolId(key0), item0] as const,
        ),
        copies: Array.from(value.copies.entries()).map(
            ([key0, item0]) => [toJsonGlobalTypeId(key0), item0] as const,
        ),
        spaces: Array.from(value.spaces.entries()).map(
            ([key0, item0]) => [toJsonGlobalSymbolId(key0), toJsonSpace(item0)] as const,
        ),
    };
}

/** Return one RepresentationSegment from one JSON value. */
export function fromJsonRepresentationSegment(value: Json): RepresentationSegment {
    const object = jsonObject(value);

    return {
        moduleId: fromJsonModuleId(jsonField(object, "moduleId")),
        copyDerivations: new Map(
            jsonArray(jsonField(object, "copyDerivations")).map((entry) => {
                const items = jsonArray(entry);
                if (items.length !== 2) {
                    throw new SerdeError(`expected JSON map entry length 2: ${items.length}`);
                }
                const key0 = items[0];
                const item0 = items[1];
                return [fromJsonGlobalSymbolId(key0), jsonBool(item0)] as const;
            }),
        ),
        copies: new Map(
            jsonArray(jsonField(object, "copies")).map((entry) => {
                const items = jsonArray(entry);
                if (items.length !== 2) {
                    throw new SerdeError(`expected JSON map entry length 2: ${items.length}`);
                }
                const key0 = items[0];
                const item0 = items[1];
                return [fromJsonGlobalTypeId(key0), jsonBool(item0)] as const;
            }),
        ),
        spaces: new Map(
            jsonArray(jsonField(object, "spaces")).map((entry) => {
                const items = jsonArray(entry);
                if (items.length !== 2) {
                    throw new SerdeError(`expected JSON map entry length 2: ${items.length}`);
                }
                const key0 = items[0];
                const item0 = items[1];
                return [fromJsonGlobalSymbolId(key0), fromJsonSpace(item0)] as const;
            }),
        ),
    };
}
