/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, compareBytes, jsonArray, jsonField, jsonInteger, jsonObject, jsonString, nestedBytes } from "../../../protocol/serde.js";
import type { LanguageItem } from "../symbol/language.js";
import type { GlobalSymbolId } from "../symbol/symbol.js";
import type { AutoInterface } from "./auto.js";
import type { GlobalNodeId } from "../tree/node.js";
import type { GlobalNodeIdAny } from "../tree/node.js";
import type { LocalNodeId } from "../tree/node.js";
import type { GlobalStaticId } from "../tree/static.js";
import type { ArgumentBinding } from "../type/generic.js";
import type { NewtypeSelection } from "../type/resolution.js";
import type { GlobalTypeId } from "../type/type.js";
import type { ModuleId } from "../../source/file/model/module.js";
import { decodeLanguageItem, encodeLanguageItem, fromJsonLanguageItem, toJsonLanguageItem } from "../symbol/language.js";
import { decodeGlobalSymbolId, encodeGlobalSymbolId, fromJsonGlobalSymbolId, toJsonGlobalSymbolId } from "../symbol/symbol.js";
import { decodeAutoInterface, encodeAutoInterface, fromJsonAutoInterface, toJsonAutoInterface } from "./auto.js";
import { decodeGlobalNodeId, encodeGlobalNodeId, fromJsonGlobalNodeId, toJsonGlobalNodeId } from "../tree/node.js";
import { decodeGlobalNodeIdAny, encodeGlobalNodeIdAny, fromJsonGlobalNodeIdAny, toJsonGlobalNodeIdAny } from "../tree/node.js";
import { decodeLocalNodeId, encodeLocalNodeId, fromJsonLocalNodeId, toJsonLocalNodeId } from "../tree/node.js";
import { decodeGlobalStaticId, encodeGlobalStaticId, fromJsonGlobalStaticId, toJsonGlobalStaticId } from "../tree/static.js";
import { decodeArgumentBinding, encodeArgumentBinding, fromJsonArgumentBinding, toJsonArgumentBinding } from "../type/generic.js";
import { decodeNewtypeSelection, encodeNewtypeSelection, fromJsonNewtypeSelection, toJsonNewtypeSelection } from "../type/resolution.js";
import { decodeGlobalTypeId, encodeGlobalTypeId, fromJsonGlobalTypeId, toJsonGlobalTypeId } from "../type/type.js";
import { decodeModuleId, encodeModuleId, fromJsonModuleId, toJsonModuleId } from "../../source/file/model/module.js";

/** Checked decorator application attached to one owner node. */
export type DecoratorApplication = {
    /** The decorator node. */
    readonly source: GlobalNodeId;
    /** The node decorated by this application. */
    readonly owner: GlobalNodeIdAny;
    /** The decorator target expression. */
    readonly expression: GlobalNodeId;
    /** The checked decorator selection. */
    readonly resolution: DecoratorResolution;
    /** The checked decorator value. */
    readonly value: GlobalStaticId;
};

export const DecoratorApplication = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: DecoratorApplication): void {
        encodeDecoratorApplication(writer, value);
    },

    /** Decode one DecoratorApplication. */
    decode(reader: BinaryReader): DecoratorApplication {
        return decodeDecoratorApplication(reader);
    },

    /** Return this value as JSON. */
    toJson(value: DecoratorApplication): Json {
        return toJsonDecoratorApplication(value);
    },

    /** Return one DecoratorApplication from one JSON value. */
    fromJson(value: Json): DecoratorApplication {
        return fromJsonDecoratorApplication(value);
    },
};

/** Encode one DecoratorApplication. */
export function encodeDecoratorApplication(writer: BinaryWriter, value: DecoratorApplication): void {
    encodeGlobalNodeId(writer, value.source);
    encodeGlobalNodeIdAny(writer, value.owner);
    encodeGlobalNodeId(writer, value.expression);
    encodeDecoratorResolution(writer, value.resolution);
    encodeGlobalStaticId(writer, value.value);
}

/** Decode one DecoratorApplication. */
export function decodeDecoratorApplication(reader: BinaryReader): DecoratorApplication {
    const source = decodeGlobalNodeId(reader);
    const owner = decodeGlobalNodeIdAny(reader);
    const expression = decodeGlobalNodeId(reader);
    const resolution = decodeDecoratorResolution(reader);
    const value = decodeGlobalStaticId(reader);

    return {
        source,
        owner,
        expression,
        resolution,
        value,
    };
}

/** Return one JSON value for one DecoratorApplication. */
export function toJsonDecoratorApplication(value: DecoratorApplication): Json {
    return {
        source: toJsonGlobalNodeId(value.source),
        owner: toJsonGlobalNodeIdAny(value.owner),
        expression: toJsonGlobalNodeId(value.expression),
        resolution: toJsonDecoratorResolution(value.resolution),
        value: toJsonGlobalStaticId(value.value),
    };
}

/** Return one DecoratorApplication from one JSON value. */
export function fromJsonDecoratorApplication(value: Json): DecoratorApplication {
    const object = jsonObject(value);

    return {
        source: fromJsonGlobalNodeId(jsonField(object, "source")),
        owner: fromJsonGlobalNodeIdAny(jsonField(object, "owner")),
        expression: fromJsonGlobalNodeId(jsonField(object, "expression")),
        resolution: fromJsonDecoratorResolution(jsonField(object, "resolution")),
        value: fromJsonGlobalStaticId(jsonField(object, "value")),
    };
}

/** Checked resolution of one decorator application. */
export type DecoratorResolution = {
    /** The resolved decorator declaration. */
    readonly target: DecoratorTarget;
    /** The selection used to construct the decorator value. */
    readonly selection: DecoratorSelection;
    /** The nominal decorator value type. */
    readonly ty: GlobalTypeId;
};

export const DecoratorResolution = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: DecoratorResolution): void {
        encodeDecoratorResolution(writer, value);
    },

    /** Decode one DecoratorResolution. */
    decode(reader: BinaryReader): DecoratorResolution {
        return decodeDecoratorResolution(reader);
    },

    /** Return this value as JSON. */
    toJson(value: DecoratorResolution): Json {
        return toJsonDecoratorResolution(value);
    },

    /** Return one DecoratorResolution from one JSON value. */
    fromJson(value: Json): DecoratorResolution {
        return fromJsonDecoratorResolution(value);
    },
};

/** Encode one DecoratorResolution. */
export function encodeDecoratorResolution(writer: BinaryWriter, value: DecoratorResolution): void {
    encodeDecoratorTarget(writer, value.target);
    encodeDecoratorSelection(writer, value.selection);
    encodeGlobalTypeId(writer, value.ty);
}

/** Decode one DecoratorResolution. */
export function decodeDecoratorResolution(reader: BinaryReader): DecoratorResolution {
    const target = decodeDecoratorTarget(reader);
    const selection = decodeDecoratorSelection(reader);
    const ty = decodeGlobalTypeId(reader);

    return {
        target,
        selection,
        ty,
    };
}

/** Return one JSON value for one DecoratorResolution. */
export function toJsonDecoratorResolution(value: DecoratorResolution): Json {
    return {
        target: toJsonDecoratorTarget(value.target),
        selection: toJsonDecoratorSelection(value.selection),
        ty: toJsonGlobalTypeId(value.ty),
    };
}

/** Return one DecoratorResolution from one JSON value. */
export function fromJsonDecoratorResolution(value: Json): DecoratorResolution {
    const object = jsonObject(value);

    return {
        target: fromJsonDecoratorTarget(jsonField(object, "target")),
        selection: fromJsonDecoratorSelection(jsonField(object, "selection")),
        ty: fromJsonGlobalTypeId(jsonField(object, "ty")),
    };
}

/** Decorator applications added by one DIR phase. */
export type DecoratorSegment = {
    /** The module id of the decorator segment. */
    readonly moduleId: ModuleId;
    /** The first decorator application id owned by this table segment. */
    readonly firstApplicationId: number;
    /** Decorator applications owned by this segment. */
    readonly applications: ReadonlyArray<DecoratorApplication>;
    /** Decorator applications attached to each owner. */
    readonly applicationsByOwner: ReadonlyMap<GlobalNodeIdAny, ReadonlyArray<LocalDecoratorId>>;
    /** Declared decorator uses awaiting selection and evaluation. */
    readonly uses: ReadonlyArray<DecoratorUse>;
};

export const DecoratorSegment = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: DecoratorSegment): void {
        encodeDecoratorSegment(writer, value);
    },

    /** Decode one DecoratorSegment. */
    decode(reader: BinaryReader): DecoratorSegment {
        return decodeDecoratorSegment(reader);
    },

    /** Return this value as JSON. */
    toJson(value: DecoratorSegment): Json {
        return toJsonDecoratorSegment(value);
    },

    /** Return one DecoratorSegment from one JSON value. */
    fromJson(value: Json): DecoratorSegment {
        return fromJsonDecoratorSegment(value);
    },
};

/** Encode one DecoratorSegment. */
export function encodeDecoratorSegment(writer: BinaryWriter, value: DecoratorSegment): void {
    encodeModuleId(writer, value.moduleId);
    writer.writeUnsigned(value.firstApplicationId);
    writer.writeUnsigned(value.applications.length);
    for (const item2 of value.applications) {
        encodeDecoratorApplication(writer, item2);
    }
    const entries3 = Array.from(value.applicationsByOwner.entries()).map(([key3, item3]) => {
        const keyBytes = nestedBytes((writer) => {
            encodeGlobalNodeIdAny(writer, key3);
        });
        return { key3, item3, keyBytes };
    });
    entries3.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries3.length);
    for (const entry3 of entries3) {
        encodeGlobalNodeIdAny(writer, entry3.key3);
        writer.writeUnsigned(entry3.item3.length);
        for (const item4 of entry3.item3) {
            encodeLocalDecoratorId(writer, item4);
        }
    }
    writer.writeUnsigned(value.uses.length);
    for (const item4 of value.uses) {
        encodeDecoratorUse(writer, item4);
    }
}

/** Decode one DecoratorSegment. */
export function decodeDecoratorSegment(reader: BinaryReader): DecoratorSegment {
    const moduleId = decodeModuleId(reader);
    const firstApplicationId = reader.readNumber();
    const applications = (() => { const length2 = reader.readNumber(); const items2: Array<DecoratorApplication> = []; for (let index = 0; index < length2; index += 1) { items2.push(decodeDecoratorApplication(reader)); } return items2; })();
    const applicationsByOwner = (() => { const length3 = reader.readNumber(); const items3 = new Map<GlobalNodeIdAny, ReadonlyArray<LocalDecoratorId>>(); for (let index = 0; index < length3; index += 1) { items3.set(decodeGlobalNodeIdAny(reader), (() => { const length5 = reader.readNumber(); const items5: Array<LocalDecoratorId> = []; for (let index = 0; index < length5; index += 1) { items5.push(decodeLocalDecoratorId(reader)); } return items5; })()); } return items3; })();
    const uses = (() => { const length4 = reader.readNumber(); const items4: Array<DecoratorUse> = []; for (let index = 0; index < length4; index += 1) { items4.push(decodeDecoratorUse(reader)); } return items4; })();

    return {
        moduleId,
        firstApplicationId,
        applications,
        applicationsByOwner,
        uses,
    };
}

/** Return one JSON value for one DecoratorSegment. */
export function toJsonDecoratorSegment(value: DecoratorSegment): Json {
    return {
        moduleId: toJsonModuleId(value.moduleId),
        firstApplicationId: value.firstApplicationId,
        applications: value.applications.map((item0) => toJsonDecoratorApplication(item0)),
        applicationsByOwner: Array.from(value.applicationsByOwner.entries()).map(([key0, item0]) => [toJsonGlobalNodeIdAny(key0), item0.map((item1) => toJsonLocalDecoratorId(item1))] as const),
        uses: value.uses.map((item0) => toJsonDecoratorUse(item0)),
    };
}

/** Return one DecoratorSegment from one JSON value. */
export function fromJsonDecoratorSegment(value: Json): DecoratorSegment {
    const object = jsonObject(value);

    return {
        moduleId: fromJsonModuleId(jsonField(object, "moduleId")),
        firstApplicationId: jsonInteger(jsonField(object, "firstApplicationId")),
        applications: jsonArray(jsonField(object, "applications")).map((item0) => fromJsonDecoratorApplication(item0)),
        applicationsByOwner: new Map(jsonArray(jsonField(object, "applicationsByOwner")).map((entry) => { const items = jsonArray(entry); if (items.length !== 2) { throw new SerdeError(`expected JSON map entry length 2: ${items.length}`); } const key0 = items[0]; const item0 = items[1]; return [fromJsonGlobalNodeIdAny(key0), jsonArray(item0).map((item1) => fromJsonLocalDecoratorId(item1))] as const; })),
        uses: jsonArray(jsonField(object, "uses")).map((item0) => fromJsonDecoratorUse(item0)),
    };
}

/** Selection used to construct one checked decorator value. */
export type DecoratorSelection =
    /** Arguments matched against one newtype backing. */
    | {
          readonly kind: "newtype";
          /** The selected newtype backing. */
          readonly newtype: NewtypeSelection;
          /** The source arguments bound to the selected parameters. */
          readonly arguments: ReadonlyArray<ArgumentBinding>;
      }
    /** Interfaces selected by the compiler-owned derive dispatcher. */
    | {
          readonly kind: "derive";
          /** The selected interfaces in argument order. */
          readonly interfaces: ReadonlyArray<AutoInterface>;
      }
;

export const DecoratorSelection = {
    /** Arguments matched against one newtype backing. */
    newtype(newtype: NewtypeSelection, arguments_: ReadonlyArray<ArgumentBinding>): DecoratorSelection {
        return { kind: "newtype", newtype, arguments: arguments_ };
    },

    /** Interfaces selected by the compiler-owned derive dispatcher. */
    derive(interfaces: ReadonlyArray<AutoInterface>): DecoratorSelection {
        return { kind: "derive", interfaces };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: DecoratorSelection): void {
        encodeDecoratorSelection(writer, value);
    },

    /** Decode one DecoratorSelection. */
    decode(reader: BinaryReader): DecoratorSelection {
        return decodeDecoratorSelection(reader);
    },

    /** Return this value as JSON. */
    toJson(value: DecoratorSelection): Json {
        return toJsonDecoratorSelection(value);
    },

    /** Return one DecoratorSelection from one JSON value. */
    fromJson(value: Json): DecoratorSelection {
        return fromJsonDecoratorSelection(value);
    },
};

/** Encode one DecoratorSelection. */
export function encodeDecoratorSelection(writer: BinaryWriter, value: DecoratorSelection): void {
    switch (value.kind) {
        case "newtype":
            writer.writeUnsigned(0);
            encodeNewtypeSelection(writer, value.newtype);
            writer.writeUnsigned(value.arguments.length);
            for (const item1 of value.arguments) {
                encodeArgumentBinding(writer, item1);
            }
            return;
        case "derive":
            writer.writeUnsigned(1);
            writer.writeUnsigned(value.interfaces.length);
            for (const item0 of value.interfaces) {
                encodeAutoInterface(writer, item0);
            }
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one DecoratorSelection. */
export function decodeDecoratorSelection(reader: BinaryReader): DecoratorSelection {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const newtype = decodeNewtypeSelection(reader);
            const arguments_ = (() => { const length1 = reader.readNumber(); const items1: Array<ArgumentBinding> = []; for (let index = 0; index < length1; index += 1) { items1.push(decodeArgumentBinding(reader)); } return items1; })();

            return {
                kind: "newtype",
                newtype,
                arguments: arguments_,
            };
        }
        case 1: {
            const interfaces = (() => { const length0 = reader.readNumber(); const items0: Array<AutoInterface> = []; for (let index = 0; index < length0; index += 1) { items0.push(decodeAutoInterface(reader)); } return items0; })();

            return {
                kind: "derive",
                interfaces,
            };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one DecoratorSelection. */
export function toJsonDecoratorSelection(value: DecoratorSelection): Json {
    switch (value.kind) {
        case "newtype":
            return {
                kind: "newtype",
                newtype: toJsonNewtypeSelection(value.newtype),
                arguments: value.arguments.map((item0) => toJsonArgumentBinding(item0)),
            };
        case "derive":
            return {
                kind: "derive",
                interfaces: value.interfaces.map((item0) => toJsonAutoInterface(item0)),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one DecoratorSelection from one JSON value. */
export function fromJsonDecoratorSelection(value: Json): DecoratorSelection {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "newtype":
            return {
                kind,
                newtype: fromJsonNewtypeSelection(jsonField(object, "newtype")),
                arguments: jsonArray(jsonField(object, "arguments")).map((item0) => fromJsonArgumentBinding(item0)),
            };
        case "derive":
            return {
                kind,
                interfaces: jsonArray(jsonField(object, "interfaces")).map((item0) => fromJsonAutoInterface(item0)),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** Resolved decorator declaration. */
export type DecoratorTarget =
    /** Toolchain language item decorator. */
    | {
          readonly kind: "languageItem";
          /** The resolved decorator symbol. */
          readonly symbol: GlobalSymbolId;
          /** The well-known decorator identity. */
          readonly item: LanguageItem;
      }
    /** User-defined decorator symbol. */
    | {
          readonly kind: "symbol";
          /** The resolved decorator symbol. */
          readonly symbol: GlobalSymbolId;
      }
;

export const DecoratorTarget = {
    /** Toolchain language item decorator. */
    languageItem(symbol_: GlobalSymbolId, item: LanguageItem): DecoratorTarget {
        return { kind: "languageItem", symbol: symbol_, item };
    },

    /** User-defined decorator symbol. */
    "symbol"(symbol_: GlobalSymbolId): DecoratorTarget {
        return { kind: "symbol", symbol: symbol_ };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: DecoratorTarget): void {
        encodeDecoratorTarget(writer, value);
    },

    /** Decode one DecoratorTarget. */
    decode(reader: BinaryReader): DecoratorTarget {
        return decodeDecoratorTarget(reader);
    },

    /** Return this value as JSON. */
    toJson(value: DecoratorTarget): Json {
        return toJsonDecoratorTarget(value);
    },

    /** Return one DecoratorTarget from one JSON value. */
    fromJson(value: Json): DecoratorTarget {
        return fromJsonDecoratorTarget(value);
    },
};

/** Encode one DecoratorTarget. */
export function encodeDecoratorTarget(writer: BinaryWriter, value: DecoratorTarget): void {
    switch (value.kind) {
        case "languageItem":
            writer.writeUnsigned(0);
            encodeGlobalSymbolId(writer, value.symbol);
            encodeLanguageItem(writer, value.item);
            return;
        case "symbol":
            writer.writeUnsigned(1);
            encodeGlobalSymbolId(writer, value.symbol);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one DecoratorTarget. */
export function decodeDecoratorTarget(reader: BinaryReader): DecoratorTarget {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const symbol_ = decodeGlobalSymbolId(reader);
            const item = decodeLanguageItem(reader);

            return {
                kind: "languageItem",
                symbol: symbol_,
                item,
            };
        }
        case 1: {
            const symbol_ = decodeGlobalSymbolId(reader);

            return {
                kind: "symbol",
                symbol: symbol_,
            };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one DecoratorTarget. */
export function toJsonDecoratorTarget(value: DecoratorTarget): Json {
    switch (value.kind) {
        case "languageItem":
            return {
                kind: "languageItem",
                symbol: toJsonGlobalSymbolId(value.symbol),
                item: toJsonLanguageItem(value.item),
            };
        case "symbol":
            return {
                kind: "symbol",
                symbol: toJsonGlobalSymbolId(value.symbol),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one DecoratorTarget from one JSON value. */
export function fromJsonDecoratorTarget(value: Json): DecoratorTarget {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "languageItem":
            return {
                kind,
                symbol: fromJsonGlobalSymbolId(jsonField(object, "symbol")),
                item: fromJsonLanguageItem(jsonField(object, "item")),
            };
        case "symbol":
            return {
                kind,
                symbol: fromJsonGlobalSymbolId(jsonField(object, "symbol")),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** Declared decorator use awaiting selection and evaluation. */
export type DecoratorUse = {
    /** The decorator node. */
    readonly source: GlobalNodeId;
    /** The node decorated by this use. */
    readonly owner: GlobalNodeIdAny;
    /** The decorator target expression. */
    readonly target: GlobalNodeId;
    /** The resolved decorator symbol. */
    readonly symbol: GlobalSymbolId;
    /** The explicit generic arguments. */
    readonly genericArguments: ReadonlyArray<LocalNodeId>;
    /** The decorator application arguments. */
    readonly arguments: ReadonlyArray<LocalNodeId>;
};

export const DecoratorUse = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: DecoratorUse): void {
        encodeDecoratorUse(writer, value);
    },

    /** Decode one DecoratorUse. */
    decode(reader: BinaryReader): DecoratorUse {
        return decodeDecoratorUse(reader);
    },

    /** Return this value as JSON. */
    toJson(value: DecoratorUse): Json {
        return toJsonDecoratorUse(value);
    },

    /** Return one DecoratorUse from one JSON value. */
    fromJson(value: Json): DecoratorUse {
        return fromJsonDecoratorUse(value);
    },
};

/** Encode one DecoratorUse. */
export function encodeDecoratorUse(writer: BinaryWriter, value: DecoratorUse): void {
    encodeGlobalNodeId(writer, value.source);
    encodeGlobalNodeIdAny(writer, value.owner);
    encodeGlobalNodeId(writer, value.target);
    encodeGlobalSymbolId(writer, value.symbol);
    writer.writeUnsigned(value.genericArguments.length);
    for (const item4 of value.genericArguments) {
        encodeLocalNodeId(writer, item4);
    }
    writer.writeUnsigned(value.arguments.length);
    for (const item5 of value.arguments) {
        encodeLocalNodeId(writer, item5);
    }
}

/** Decode one DecoratorUse. */
export function decodeDecoratorUse(reader: BinaryReader): DecoratorUse {
    const source = decodeGlobalNodeId(reader);
    const owner = decodeGlobalNodeIdAny(reader);
    const target = decodeGlobalNodeId(reader);
    const symbol_ = decodeGlobalSymbolId(reader);
    const genericArguments = (() => { const length4 = reader.readNumber(); const items4: Array<LocalNodeId> = []; for (let index = 0; index < length4; index += 1) { items4.push(decodeLocalNodeId(reader)); } return items4; })();
    const arguments_ = (() => { const length5 = reader.readNumber(); const items5: Array<LocalNodeId> = []; for (let index = 0; index < length5; index += 1) { items5.push(decodeLocalNodeId(reader)); } return items5; })();

    return {
        source,
        owner,
        target,
        symbol: symbol_,
        genericArguments,
        arguments: arguments_,
    };
}

/** Return one JSON value for one DecoratorUse. */
export function toJsonDecoratorUse(value: DecoratorUse): Json {
    return {
        source: toJsonGlobalNodeId(value.source),
        owner: toJsonGlobalNodeIdAny(value.owner),
        target: toJsonGlobalNodeId(value.target),
        symbol: toJsonGlobalSymbolId(value.symbol),
        genericArguments: value.genericArguments.map((item0) => toJsonLocalNodeId(item0)),
        arguments: value.arguments.map((item0) => toJsonLocalNodeId(item0)),
    };
}

/** Return one DecoratorUse from one JSON value. */
export function fromJsonDecoratorUse(value: Json): DecoratorUse {
    const object = jsonObject(value);

    return {
        source: fromJsonGlobalNodeId(jsonField(object, "source")),
        owner: fromJsonGlobalNodeIdAny(jsonField(object, "owner")),
        target: fromJsonGlobalNodeId(jsonField(object, "target")),
        symbol: fromJsonGlobalSymbolId(jsonField(object, "symbol")),
        genericArguments: jsonArray(jsonField(object, "genericArguments")).map((item0) => fromJsonLocalNodeId(item0)),
        arguments: jsonArray(jsonField(object, "arguments")).map((item0) => fromJsonLocalNodeId(item0)),
    };
}

/** Unique identifier for a decorator application. */
export type LocalDecoratorId = number;

export const LocalDecoratorId = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: LocalDecoratorId): void {
        encodeLocalDecoratorId(writer, value);
    },

    /** Decode one LocalDecoratorId. */
    decode(reader: BinaryReader): LocalDecoratorId {
        return decodeLocalDecoratorId(reader);
    },

    /** Return this value as JSON. */
    toJson(value: LocalDecoratorId): Json {
        return toJsonLocalDecoratorId(value);
    },

    /** Return one LocalDecoratorId from one JSON value. */
    fromJson(value: Json): LocalDecoratorId {
        return fromJsonLocalDecoratorId(value);
    },
};

/** Encode one LocalDecoratorId. */
export function encodeLocalDecoratorId(writer: BinaryWriter, value: LocalDecoratorId): void {
    writer.writeUnsigned(value);
}

/** Decode one LocalDecoratorId. */
export function decodeLocalDecoratorId(reader: BinaryReader): LocalDecoratorId {
    return reader.readNumber();
}

/** Return one JSON value for one LocalDecoratorId. */
export function toJsonLocalDecoratorId(value: LocalDecoratorId): Json {
    return value;
}

/** Return one LocalDecoratorId from one JSON value. */
export function fromJsonLocalDecoratorId(value: Json): LocalDecoratorId {
    return jsonInteger(value);
}
