/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, jsonArray, jsonField, jsonInteger, jsonObject } from "../../../protocol/serde.js";
import type { StringId } from "../../core/string.js";
import type { GlobalSymbolId } from "../symbol/symbol.js";
import type { LocalNodeIdAny } from "../tree/node.js";
import type { AccessPath } from "../type/access.js";
import type { ModuleId } from "../../source/file/model/module.js";
import { decodeStringId, encodeStringId, fromJsonStringId, toJsonStringId } from "../../core/string.js";
import { decodeGlobalSymbolId, encodeGlobalSymbolId, fromJsonGlobalSymbolId, toJsonGlobalSymbolId } from "../symbol/symbol.js";
import { decodeLocalNodeIdAny, encodeLocalNodeIdAny, fromJsonLocalNodeIdAny, toJsonLocalNodeIdAny } from "../tree/node.js";
import { decodeAccessPath, encodeAccessPath, fromJsonAccessPath, toJsonAccessPath } from "../type/access.js";
import { decodeModuleId, encodeModuleId, fromJsonModuleId, toJsonModuleId } from "../../source/file/model/module.js";

/** One stable storage access at its occurrence node. */
export type AccessOccurrence = {
    /** The occurrence node. */
    readonly node: LocalNodeIdAny;
    /** The selected storage path. */
    readonly path: AccessPath;
    /** The recorded uses. */
    readonly uses: BindingUse;
};

export const AccessOccurrence = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: AccessOccurrence): void {
        encodeAccessOccurrence(writer, value);
    },

    /** Decode one AccessOccurrence. */
    decode(reader: BinaryReader): AccessOccurrence {
        return decodeAccessOccurrence(reader);
    },

    /** Return this value as JSON. */
    toJson(value: AccessOccurrence): Json {
        return toJsonAccessOccurrence(value);
    },

    /** Return one AccessOccurrence from one JSON value. */
    fromJson(value: Json): AccessOccurrence {
        return fromJsonAccessOccurrence(value);
    },
};

/** Encode one AccessOccurrence. */
export function encodeAccessOccurrence(writer: BinaryWriter, value: AccessOccurrence): void {
    encodeLocalNodeIdAny(writer, value.node);
    encodeAccessPath(writer, value.path);
    encodeBindingUse(writer, value.uses);
}

/** Decode one AccessOccurrence. */
export function decodeAccessOccurrence(reader: BinaryReader): AccessOccurrence {
    const node = decodeLocalNodeIdAny(reader);
    const path = decodeAccessPath(reader);
    const uses = decodeBindingUse(reader);

    return {
        node,
        path,
        uses,
    };
}

/** Return one JSON value for one AccessOccurrence. */
export function toJsonAccessOccurrence(value: AccessOccurrence): Json {
    return {
        node: toJsonLocalNodeIdAny(value.node),
        path: toJsonAccessPath(value.path),
        uses: toJsonBindingUse(value.uses),
    };
}

/** Return one AccessOccurrence from one JSON value. */
export function fromJsonAccessOccurrence(value: Json): AccessOccurrence {
    const object = jsonObject(value);

    return {
        node: fromJsonLocalNodeIdAny(jsonField(object, "node")),
        path: fromJsonAccessPath(jsonField(object, "path")),
        uses: fromJsonBindingUse(jsonField(object, "uses")),
    };
}

/** One binding use at its occurrence node. */
export type BindingOccurrence = {
    /** The occurrence node. */
    readonly node: LocalNodeIdAny;
    /** The used symbol. */
    readonly symbol: GlobalSymbolId;
    /** The recorded uses. */
    readonly uses: BindingUse;
};

export const BindingOccurrence = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: BindingOccurrence): void {
        encodeBindingOccurrence(writer, value);
    },

    /** Decode one BindingOccurrence. */
    decode(reader: BinaryReader): BindingOccurrence {
        return decodeBindingOccurrence(reader);
    },

    /** Return this value as JSON. */
    toJson(value: BindingOccurrence): Json {
        return toJsonBindingOccurrence(value);
    },

    /** Return one BindingOccurrence from one JSON value. */
    fromJson(value: Json): BindingOccurrence {
        return fromJsonBindingOccurrence(value);
    },
};

/** Encode one BindingOccurrence. */
export function encodeBindingOccurrence(writer: BinaryWriter, value: BindingOccurrence): void {
    encodeLocalNodeIdAny(writer, value.node);
    encodeGlobalSymbolId(writer, value.symbol);
    encodeBindingUse(writer, value.uses);
}

/** Decode one BindingOccurrence. */
export function decodeBindingOccurrence(reader: BinaryReader): BindingOccurrence {
    const node = decodeLocalNodeIdAny(reader);
    const symbol_ = decodeGlobalSymbolId(reader);
    const uses = decodeBindingUse(reader);

    return {
        node,
        symbol: symbol_,
        uses,
    };
}

/** Return one JSON value for one BindingOccurrence. */
export function toJsonBindingOccurrence(value: BindingOccurrence): Json {
    return {
        node: toJsonLocalNodeIdAny(value.node),
        symbol: toJsonGlobalSymbolId(value.symbol),
        uses: toJsonBindingUse(value.uses),
    };
}

/** Return one BindingOccurrence from one JSON value. */
export function fromJsonBindingOccurrence(value: Json): BindingOccurrence {
    const object = jsonObject(value);

    return {
        node: fromJsonLocalNodeIdAny(jsonField(object, "node")),
        symbol: fromJsonGlobalSymbolId(jsonField(object, "symbol")),
        uses: fromJsonBindingUse(jsonField(object, "uses")),
    };
}

/** One binding's recorded uses inside a DIR module. */
export type BindingUse = number;

export const BindingUse = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: BindingUse): void {
        encodeBindingUse(writer, value);
    },

    /** Decode one BindingUse. */
    decode(reader: BinaryReader): BindingUse {
        return decodeBindingUse(reader);
    },

    /** Return this value as JSON. */
    toJson(value: BindingUse): Json {
        return toJsonBindingUse(value);
    },

    /** Return one BindingUse from one JSON value. */
    fromJson(value: Json): BindingUse {
        return fromJsonBindingUse(value);
    },
};

/** Encode one BindingUse. */
export function encodeBindingUse(writer: BinaryWriter, value: BindingUse): void {
    writer.writeByte(value);
}

/** Decode one BindingUse. */
export function decodeBindingUse(reader: BinaryReader): BindingUse {
    return reader.readByte();
}

/** Return one JSON value for one BindingUse. */
export function toJsonBindingUse(value: BindingUse): Json {
    return value;
}

/** Return one BindingUse from one JSON value. */
export function fromJsonBindingUse(value: Json): BindingUse {
    return jsonInteger(value);
}

/** Flow conclusions added by one DIR phase. */
export type FlowSegment = {
    /** The module id of the flow segment. */
    readonly moduleId: ModuleId;
    /** Nodes flow proves unreachable. */
    readonly unreachable: ReadonlyArray<LocalNodeIdAny>;
    /** Nodes flow proves never return. */
    readonly diverging: ReadonlyArray<LocalNodeIdAny>;
    /** Proved binding uses. */
    readonly bindingOccurrences: ReadonlyArray<BindingOccurrence>;
    /** Proved stable access uses. */
    readonly accessOccurrences: ReadonlyArray<AccessOccurrence>;
    /** Proved taint domains. */
    readonly taints: ReadonlyArray<TaintOccurrence>;
};

export const FlowSegment = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: FlowSegment): void {
        encodeFlowSegment(writer, value);
    },

    /** Decode one FlowSegment. */
    decode(reader: BinaryReader): FlowSegment {
        return decodeFlowSegment(reader);
    },

    /** Return this value as JSON. */
    toJson(value: FlowSegment): Json {
        return toJsonFlowSegment(value);
    },

    /** Return one FlowSegment from one JSON value. */
    fromJson(value: Json): FlowSegment {
        return fromJsonFlowSegment(value);
    },
};

/** Encode one FlowSegment. */
export function encodeFlowSegment(writer: BinaryWriter, value: FlowSegment): void {
    encodeModuleId(writer, value.moduleId);
    writer.writeUnsigned(value.unreachable.length);
    for (const item1 of value.unreachable) {
        encodeLocalNodeIdAny(writer, item1);
    }
    writer.writeUnsigned(value.diverging.length);
    for (const item2 of value.diverging) {
        encodeLocalNodeIdAny(writer, item2);
    }
    writer.writeUnsigned(value.bindingOccurrences.length);
    for (const item3 of value.bindingOccurrences) {
        encodeBindingOccurrence(writer, item3);
    }
    writer.writeUnsigned(value.accessOccurrences.length);
    for (const item4 of value.accessOccurrences) {
        encodeAccessOccurrence(writer, item4);
    }
    writer.writeUnsigned(value.taints.length);
    for (const item5 of value.taints) {
        encodeTaintOccurrence(writer, item5);
    }
}

/** Decode one FlowSegment. */
export function decodeFlowSegment(reader: BinaryReader): FlowSegment {
    const moduleId = decodeModuleId(reader);
    const unreachable = (() => { const length1 = reader.readNumber(); const items1: Array<LocalNodeIdAny> = []; for (let index = 0; index < length1; index += 1) { items1.push(decodeLocalNodeIdAny(reader)); } return items1; })();
    const diverging = (() => { const length2 = reader.readNumber(); const items2: Array<LocalNodeIdAny> = []; for (let index = 0; index < length2; index += 1) { items2.push(decodeLocalNodeIdAny(reader)); } return items2; })();
    const bindingOccurrences = (() => { const length3 = reader.readNumber(); const items3: Array<BindingOccurrence> = []; for (let index = 0; index < length3; index += 1) { items3.push(decodeBindingOccurrence(reader)); } return items3; })();
    const accessOccurrences = (() => { const length4 = reader.readNumber(); const items4: Array<AccessOccurrence> = []; for (let index = 0; index < length4; index += 1) { items4.push(decodeAccessOccurrence(reader)); } return items4; })();
    const taints = (() => { const length5 = reader.readNumber(); const items5: Array<TaintOccurrence> = []; for (let index = 0; index < length5; index += 1) { items5.push(decodeTaintOccurrence(reader)); } return items5; })();

    return {
        moduleId,
        unreachable,
        diverging,
        bindingOccurrences,
        accessOccurrences,
        taints,
    };
}

/** Return one JSON value for one FlowSegment. */
export function toJsonFlowSegment(value: FlowSegment): Json {
    return {
        moduleId: toJsonModuleId(value.moduleId),
        unreachable: value.unreachable.map((item0) => toJsonLocalNodeIdAny(item0)),
        diverging: value.diverging.map((item0) => toJsonLocalNodeIdAny(item0)),
        bindingOccurrences: value.bindingOccurrences.map((item0) => toJsonBindingOccurrence(item0)),
        accessOccurrences: value.accessOccurrences.map((item0) => toJsonAccessOccurrence(item0)),
        taints: value.taints.map((item0) => toJsonTaintOccurrence(item0)),
    };
}

/** Return one FlowSegment from one JSON value. */
export function fromJsonFlowSegment(value: Json): FlowSegment {
    const object = jsonObject(value);

    return {
        moduleId: fromJsonModuleId(jsonField(object, "moduleId")),
        unreachable: jsonArray(jsonField(object, "unreachable")).map((item0) => fromJsonLocalNodeIdAny(item0)),
        diverging: jsonArray(jsonField(object, "diverging")).map((item0) => fromJsonLocalNodeIdAny(item0)),
        bindingOccurrences: jsonArray(jsonField(object, "bindingOccurrences")).map((item0) => fromJsonBindingOccurrence(item0)),
        accessOccurrences: jsonArray(jsonField(object, "accessOccurrences")).map((item0) => fromJsonAccessOccurrence(item0)),
        taints: jsonArray(jsonField(object, "taints")).map((item0) => fromJsonTaintOccurrence(item0)),
    };
}

/** One taint domain carried at its occurrence node. */
export type TaintOccurrence = {
    /** The occurrence node. */
    readonly node: LocalNodeIdAny;
    /** The carried taint domain. */
    readonly tag: StringId;
};

export const TaintOccurrence = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: TaintOccurrence): void {
        encodeTaintOccurrence(writer, value);
    },

    /** Decode one TaintOccurrence. */
    decode(reader: BinaryReader): TaintOccurrence {
        return decodeTaintOccurrence(reader);
    },

    /** Return this value as JSON. */
    toJson(value: TaintOccurrence): Json {
        return toJsonTaintOccurrence(value);
    },

    /** Return one TaintOccurrence from one JSON value. */
    fromJson(value: Json): TaintOccurrence {
        return fromJsonTaintOccurrence(value);
    },
};

/** Encode one TaintOccurrence. */
export function encodeTaintOccurrence(writer: BinaryWriter, value: TaintOccurrence): void {
    encodeLocalNodeIdAny(writer, value.node);
    encodeStringId(writer, value.tag);
}

/** Decode one TaintOccurrence. */
export function decodeTaintOccurrence(reader: BinaryReader): TaintOccurrence {
    const node = decodeLocalNodeIdAny(reader);
    const tag = decodeStringId(reader);

    return {
        node,
        tag,
    };
}

/** Return one JSON value for one TaintOccurrence. */
export function toJsonTaintOccurrence(value: TaintOccurrence): Json {
    return {
        node: toJsonLocalNodeIdAny(value.node),
        tag: toJsonStringId(value.tag),
    };
}

/** Return one TaintOccurrence from one JSON value. */
export function fromJsonTaintOccurrence(value: Json): TaintOccurrence {
    const object = jsonObject(value);

    return {
        node: fromJsonLocalNodeIdAny(jsonField(object, "node")),
        tag: fromJsonStringId(jsonField(object, "tag")),
    };
}
