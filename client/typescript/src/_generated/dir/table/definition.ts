/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, compareBytes, jsonArray, jsonBigint, jsonBool, jsonField, jsonObject, jsonOptional, jsonString, nestedBytes } from "../../../protocol/serde.js";
import type { StringId } from "../../core/string.js";
import type { StaticKey } from "../symbol/key.js";
import type { GlobalSymbolId } from "../symbol/symbol.js";
import type { AutoInterface } from "./auto.js";
import type { MemberSpace } from "./member.js";
import type { GlobalNodeIdAny } from "../tree/node.js";
import type { FunctionRole } from "../tree/property.js";
import type { MemberSlot } from "../tree/property.js";
import type { MethodAbstraction } from "../tree/property.js";
import type { LocalGenericTemplateId } from "../type/generic.js";
import type { EnumBackingType } from "../type/primitive.js";
import type { EnumVariantValue } from "../type/primitive.js";
import type { IntegerType } from "../type/primitive.js";
import type { PrimitiveType } from "../type/primitive.js";
import type { GlobalTypeId } from "../type/type.js";
import type { Space } from "../type/type.js";
import type { ModuleId } from "../../source/file/model/module.js";
import { decodeStringId, encodeStringId, fromJsonStringId, toJsonStringId } from "../../core/string.js";
import { decodeStaticKey, encodeStaticKey, fromJsonStaticKey, toJsonStaticKey } from "../symbol/key.js";
import { decodeGlobalSymbolId, encodeGlobalSymbolId, fromJsonGlobalSymbolId, toJsonGlobalSymbolId } from "../symbol/symbol.js";
import { decodeAutoInterface, encodeAutoInterface, fromJsonAutoInterface, toJsonAutoInterface } from "./auto.js";
import { decodeMemberSpace, encodeMemberSpace, fromJsonMemberSpace, toJsonMemberSpace } from "./member.js";
import { decodeGlobalNodeIdAny, encodeGlobalNodeIdAny, fromJsonGlobalNodeIdAny, toJsonGlobalNodeIdAny } from "../tree/node.js";
import { decodeFunctionRole, encodeFunctionRole, fromJsonFunctionRole, toJsonFunctionRole } from "../tree/property.js";
import { decodeMemberSlot, encodeMemberSlot, fromJsonMemberSlot, toJsonMemberSlot } from "../tree/property.js";
import { decodeMethodAbstraction, encodeMethodAbstraction, fromJsonMethodAbstraction, toJsonMethodAbstraction } from "../tree/property.js";
import { decodeLocalGenericTemplateId, encodeLocalGenericTemplateId, fromJsonLocalGenericTemplateId, toJsonLocalGenericTemplateId } from "../type/generic.js";
import { decodeEnumBackingType, encodeEnumBackingType, fromJsonEnumBackingType, toJsonEnumBackingType } from "../type/primitive.js";
import { decodeEnumVariantValue, encodeEnumVariantValue, fromJsonEnumVariantValue, toJsonEnumVariantValue } from "../type/primitive.js";
import { decodeIntegerType, encodeIntegerType, fromJsonIntegerType, toJsonIntegerType } from "../type/primitive.js";
import { decodePrimitiveType, encodePrimitiveType, fromJsonPrimitiveType, toJsonPrimitiveType } from "../type/primitive.js";
import { decodeGlobalTypeId, encodeGlobalTypeId, fromJsonGlobalTypeId, toJsonGlobalTypeId } from "../type/type.js";
import { decodeSpace, encodeSpace, fromJsonSpace, toJsonSpace } from "../type/type.js";
import { decodeModuleId, encodeModuleId, fromJsonModuleId, toJsonModuleId } from "../../source/file/model/module.js";

/** One associated constant. */
export type AssociatedConstDefinition = {
    /** The associated const symbol. */
    readonly symbol: GlobalSymbolId;
    /** The source member node. */
    readonly source: GlobalNodeIdAny;
    /** The associated const key. */
    readonly key: StaticKey;
};

export const AssociatedConstDefinition = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: AssociatedConstDefinition): void {
        encodeAssociatedConstDefinition(writer, value);
    },

    /** Decode one AssociatedConstDefinition. */
    decode(reader: BinaryReader): AssociatedConstDefinition {
        return decodeAssociatedConstDefinition(reader);
    },

    /** Return this value as JSON. */
    toJson(value: AssociatedConstDefinition): Json {
        return toJsonAssociatedConstDefinition(value);
    },

    /** Return one AssociatedConstDefinition from one JSON value. */
    fromJson(value: Json): AssociatedConstDefinition {
        return fromJsonAssociatedConstDefinition(value);
    },
};

/** Encode one AssociatedConstDefinition. */
export function encodeAssociatedConstDefinition(writer: BinaryWriter, value: AssociatedConstDefinition): void {
    encodeGlobalSymbolId(writer, value.symbol);
    encodeGlobalNodeIdAny(writer, value.source);
    encodeStaticKey(writer, value.key);
}

/** Decode one AssociatedConstDefinition. */
export function decodeAssociatedConstDefinition(reader: BinaryReader): AssociatedConstDefinition {
    const symbol_ = decodeGlobalSymbolId(reader);
    const source = decodeGlobalNodeIdAny(reader);
    const key = decodeStaticKey(reader);

    return {
        symbol: symbol_,
        source,
        key,
    };
}

/** Return one JSON value for one AssociatedConstDefinition. */
export function toJsonAssociatedConstDefinition(value: AssociatedConstDefinition): Json {
    return {
        symbol: toJsonGlobalSymbolId(value.symbol),
        source: toJsonGlobalNodeIdAny(value.source),
        key: toJsonStaticKey(value.key),
    };
}

/** Return one AssociatedConstDefinition from one JSON value. */
export function fromJsonAssociatedConstDefinition(value: Json): AssociatedConstDefinition {
    const object = jsonObject(value);

    return {
        symbol: fromJsonGlobalSymbolId(jsonField(object, "symbol")),
        source: fromJsonGlobalNodeIdAny(jsonField(object, "source")),
        key: fromJsonStaticKey(jsonField(object, "key")),
    };
}

/** One associated type. */
export type AssociatedTypeDefinition = {
    /** The associated type symbol. */
    readonly symbol: GlobalSymbolId;
    /** The source member node. */
    readonly source: GlobalNodeIdAny;
    /** The associated type key. */
    readonly key: StaticKey;
    /** The upper bound required by this associated type. */
    readonly constraint?: GlobalTypeId;
    /** The concrete associated type value. */
    readonly value?: GlobalTypeId;
};

export const AssociatedTypeDefinition = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: AssociatedTypeDefinition): void {
        encodeAssociatedTypeDefinition(writer, value);
    },

    /** Decode one AssociatedTypeDefinition. */
    decode(reader: BinaryReader): AssociatedTypeDefinition {
        return decodeAssociatedTypeDefinition(reader);
    },

    /** Return this value as JSON. */
    toJson(value: AssociatedTypeDefinition): Json {
        return toJsonAssociatedTypeDefinition(value);
    },

    /** Return one AssociatedTypeDefinition from one JSON value. */
    fromJson(value: Json): AssociatedTypeDefinition {
        return fromJsonAssociatedTypeDefinition(value);
    },
};

/** Encode one AssociatedTypeDefinition. */
export function encodeAssociatedTypeDefinition(writer: BinaryWriter, value: AssociatedTypeDefinition): void {
    encodeGlobalSymbolId(writer, value.symbol);
    encodeGlobalNodeIdAny(writer, value.source);
    encodeStaticKey(writer, value.key);
    writer.writeOption(value.constraint, (value3) => {
        encodeGlobalTypeId(writer, value3);
    });
    writer.writeOption(value.value, (value4) => {
        encodeGlobalTypeId(writer, value4);
    });
}

/** Decode one AssociatedTypeDefinition. */
export function decodeAssociatedTypeDefinition(reader: BinaryReader): AssociatedTypeDefinition {
    const symbol_ = decodeGlobalSymbolId(reader);
    const source = decodeGlobalNodeIdAny(reader);
    const key = decodeStaticKey(reader);
    const constraint = reader.readOption(() => decodeGlobalTypeId(reader));
    const value = reader.readOption(() => decodeGlobalTypeId(reader));

    return {
        symbol: symbol_,
        source,
        key,
        ...(constraint === undefined ? {} : { constraint }),
        ...(value === undefined ? {} : { value }),
    };
}

/** Return one JSON value for one AssociatedTypeDefinition. */
export function toJsonAssociatedTypeDefinition(value: AssociatedTypeDefinition): Json {
    return {
        symbol: toJsonGlobalSymbolId(value.symbol),
        source: toJsonGlobalNodeIdAny(value.source),
        key: toJsonStaticKey(value.key),
        ...(value.constraint === undefined ? {} : { constraint: toJsonGlobalTypeId(value.constraint) }),
        ...(value.value === undefined ? {} : { value: toJsonGlobalTypeId(value.value) }),
    };
}

/** Return one AssociatedTypeDefinition from one JSON value. */
export function fromJsonAssociatedTypeDefinition(value: Json): AssociatedTypeDefinition {
    const object = jsonObject(value);

    return {
        symbol: fromJsonGlobalSymbolId(jsonField(object, "symbol")),
        source: fromJsonGlobalNodeIdAny(jsonField(object, "source")),
        key: fromJsonStaticKey(jsonField(object, "key")),
        constraint: jsonOptional(object, "constraint", (value) => fromJsonGlobalTypeId(value)),
        value: jsonOptional(object, "value", (value) => fromJsonGlobalTypeId(value)),
    };
}

/** Receiver coverage one blanket extension's declared bound decides. */
export type BlanketCoverage =
    /** The blanket covers every receiver type. */
    | {
          readonly kind: "every";
      }
    /** The blanket covers the receivers conforming to one interface. */
    | {
          readonly kind: "interface";
          readonly interface: GlobalSymbolId;
      }
    /** Use sites decide the coverage. */
    | {
          readonly kind: "deferred";
      }
;

export const BlanketCoverage = {
    /** The blanket covers every receiver type. */
    every(): BlanketCoverage {
        return { kind: "every" };
    },

    /** The blanket covers the receivers conforming to one interface. */
    "interface"(interface_: GlobalSymbolId): BlanketCoverage {
        return { kind: "interface", interface: interface_ };
    },

    /** Use sites decide the coverage. */
    deferred(): BlanketCoverage {
        return { kind: "deferred" };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: BlanketCoverage): void {
        encodeBlanketCoverage(writer, value);
    },

    /** Decode one BlanketCoverage. */
    decode(reader: BinaryReader): BlanketCoverage {
        return decodeBlanketCoverage(reader);
    },

    /** Return this value as JSON. */
    toJson(value: BlanketCoverage): Json {
        return toJsonBlanketCoverage(value);
    },

    /** Return one BlanketCoverage from one JSON value. */
    fromJson(value: Json): BlanketCoverage {
        return fromJsonBlanketCoverage(value);
    },
};

/** Encode one BlanketCoverage. */
export function encodeBlanketCoverage(writer: BinaryWriter, value: BlanketCoverage): void {
    switch (value.kind) {
        case "every":
            writer.writeUnsigned(0);
            return;
        case "interface":
            writer.writeUnsigned(1);
            encodeGlobalSymbolId(writer, value.interface);
            return;
        case "deferred":
            writer.writeUnsigned(2);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one BlanketCoverage. */
export function decodeBlanketCoverage(reader: BinaryReader): BlanketCoverage {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            return { kind: "every" };
        }
        case 1: {
            const interface_ = decodeGlobalSymbolId(reader);

            return { kind: "interface", interface: interface_ };
        }
        case 2: {
            return { kind: "deferred" };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one BlanketCoverage. */
export function toJsonBlanketCoverage(value: BlanketCoverage): Json {
    switch (value.kind) {
        case "every":
            return {
                kind: "every",
            };
        case "interface":
            return {
                kind: "interface",
                interface: toJsonGlobalSymbolId(value.interface),
            };
        case "deferred":
            return {
                kind: "deferred",
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one BlanketCoverage from one JSON value. */
export function fromJsonBlanketCoverage(value: Json): BlanketCoverage {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "every":
            return {
                kind,
            };
        case "interface":
            return {
                kind,
                interface: fromJsonGlobalSymbolId(jsonField(object, "interface")),
            };
        case "deferred":
            return {
                kind,
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** Class construct candidate origin. */
export type ClassConstructor =
    /** Constructor explicitly declared by this class. */
    | {
          readonly kind: "declared";
          /** The declared constructor symbol. */
          readonly symbol: GlobalSymbolId;
      }
    /** Default `new T()` candidate for a class with no declared constructor. */
    | {
          readonly kind: "default";
      }
    /** Constructor forwarded to an explicit base class constructor. */
    | {
          readonly kind: "forwardedDeclared";
          /** The base class symbol. */
          readonly base: GlobalSymbolId;
          /** The selected base constructor symbol. */
          readonly symbol: GlobalSymbolId;
      }
    /** Constructor forwarded to a base class default constructor. */
    | {
          readonly kind: "forwardedDefault";
          /** The base class symbol. */
          readonly base: GlobalSymbolId;
      }
;

export const ClassConstructor = {
    /** Constructor explicitly declared by this class. */
    declared(symbol_: GlobalSymbolId): ClassConstructor {
        return { kind: "declared", symbol: symbol_ };
    },

    /** Default `new T()` candidate for a class with no declared constructor. */
    "default"(): ClassConstructor {
        return { kind: "default" };
    },

    /** Constructor forwarded to an explicit base class constructor. */
    forwardedDeclared(base: GlobalSymbolId, symbol_: GlobalSymbolId): ClassConstructor {
        return { kind: "forwardedDeclared", base, symbol: symbol_ };
    },

    /** Constructor forwarded to a base class default constructor. */
    forwardedDefault(base: GlobalSymbolId): ClassConstructor {
        return { kind: "forwardedDefault", base };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: ClassConstructor): void {
        encodeClassConstructor(writer, value);
    },

    /** Decode one ClassConstructor. */
    decode(reader: BinaryReader): ClassConstructor {
        return decodeClassConstructor(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ClassConstructor): Json {
        return toJsonClassConstructor(value);
    },

    /** Return one ClassConstructor from one JSON value. */
    fromJson(value: Json): ClassConstructor {
        return fromJsonClassConstructor(value);
    },
};

/** Encode one ClassConstructor. */
export function encodeClassConstructor(writer: BinaryWriter, value: ClassConstructor): void {
    switch (value.kind) {
        case "declared":
            writer.writeUnsigned(0);
            encodeGlobalSymbolId(writer, value.symbol);
            return;
        case "default":
            writer.writeUnsigned(1);
            return;
        case "forwardedDeclared":
            writer.writeUnsigned(2);
            encodeGlobalSymbolId(writer, value.base);
            encodeGlobalSymbolId(writer, value.symbol);
            return;
        case "forwardedDefault":
            writer.writeUnsigned(3);
            encodeGlobalSymbolId(writer, value.base);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one ClassConstructor. */
export function decodeClassConstructor(reader: BinaryReader): ClassConstructor {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const symbol_ = decodeGlobalSymbolId(reader);

            return {
                kind: "declared",
                symbol: symbol_,
            };
        }
        case 1: {
            return { kind: "default" };
        }
        case 2: {
            const base = decodeGlobalSymbolId(reader);
            const symbol_ = decodeGlobalSymbolId(reader);

            return {
                kind: "forwardedDeclared",
                base,
                symbol: symbol_,
            };
        }
        case 3: {
            const base = decodeGlobalSymbolId(reader);

            return {
                kind: "forwardedDefault",
                base,
            };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one ClassConstructor. */
export function toJsonClassConstructor(value: ClassConstructor): Json {
    switch (value.kind) {
        case "declared":
            return {
                kind: "declared",
                symbol: toJsonGlobalSymbolId(value.symbol),
            };
        case "default":
            return {
                kind: "default",
            };
        case "forwardedDeclared":
            return {
                kind: "forwardedDeclared",
                base: toJsonGlobalSymbolId(value.base),
                symbol: toJsonGlobalSymbolId(value.symbol),
            };
        case "forwardedDefault":
            return {
                kind: "forwardedDefault",
                base: toJsonGlobalSymbolId(value.base),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one ClassConstructor from one JSON value. */
export function fromJsonClassConstructor(value: Json): ClassConstructor {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "declared":
            return {
                kind,
                symbol: fromJsonGlobalSymbolId(jsonField(object, "symbol")),
            };
        case "default":
            return {
                kind,
            };
        case "forwardedDeclared":
            return {
                kind,
                base: fromJsonGlobalSymbolId(jsonField(object, "base")),
                symbol: fromJsonGlobalSymbolId(jsonField(object, "symbol")),
            };
        case "forwardedDefault":
            return {
                kind,
                base: fromJsonGlobalSymbolId(jsonField(object, "base")),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** One class construct candidate. */
export type ClassConstructorDefinition = {
    /** The selected constructor. */
    readonly constructor: ClassConstructor;
    /** The checked constructor signature. */
    readonly ty: GlobalTypeId;
};

export const ClassConstructorDefinition = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ClassConstructorDefinition): void {
        encodeClassConstructorDefinition(writer, value);
    },

    /** Decode one ClassConstructorDefinition. */
    decode(reader: BinaryReader): ClassConstructorDefinition {
        return decodeClassConstructorDefinition(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ClassConstructorDefinition): Json {
        return toJsonClassConstructorDefinition(value);
    },

    /** Return one ClassConstructorDefinition from one JSON value. */
    fromJson(value: Json): ClassConstructorDefinition {
        return fromJsonClassConstructorDefinition(value);
    },
};

/** Encode one ClassConstructorDefinition. */
export function encodeClassConstructorDefinition(writer: BinaryWriter, value: ClassConstructorDefinition): void {
    encodeClassConstructor(writer, value.constructor);
    encodeGlobalTypeId(writer, value.ty);
}

/** Decode one ClassConstructorDefinition. */
export function decodeClassConstructorDefinition(reader: BinaryReader): ClassConstructorDefinition {
    const constructor_ = decodeClassConstructor(reader);
    const ty = decodeGlobalTypeId(reader);

    return {
        constructor: constructor_,
        ty,
    };
}

/** Return one JSON value for one ClassConstructorDefinition. */
export function toJsonClassConstructorDefinition(value: ClassConstructorDefinition): Json {
    return {
        constructor: toJsonClassConstructor(value.constructor),
        ty: toJsonGlobalTypeId(value.ty),
    };
}

/** Return one ClassConstructorDefinition from one JSON value. */
export function fromJsonClassConstructorDefinition(value: Json): ClassConstructorDefinition {
    const object = jsonObject(value);

    return {
        constructor: fromJsonClassConstructor(jsonField(object, "constructor")),
        ty: fromJsonGlobalTypeId(jsonField(object, "ty")),
    };
}

/** Checked declaration data for one nominal class. */
export type ClassDefinition = {
    /** The intrinsic instance space, or relative when absent. */
    readonly space?: Space;
    /** The generic template declared by the class. */
    readonly template?: LocalGenericTemplateId;
    /** The selected runtime representation. */
    readonly representation: Representation;
    /** Whether the class is abstract. */
    readonly isAbstract: boolean;
    /** Whether the class rejects subclasses. */
    readonly isFinal: boolean;
    /** The extended class. */
    readonly extends?: NominalHeritage;
    /** The implemented interfaces. */
    readonly implements: ReadonlyArray<NominalConformance>;
    /** The written derive list replacing the auto set, if any. */
    readonly derives?: ReadonlyArray<AutoInterface>;
    /** The class's direct construct candidates. */
    readonly constructors: ReadonlyArray<ClassConstructorDefinition>;
    /** The members in declaration order. */
    readonly members: ReadonlyArray<DefinitionMember>;
};

export const ClassDefinition = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ClassDefinition): void {
        encodeClassDefinition(writer, value);
    },

    /** Decode one ClassDefinition. */
    decode(reader: BinaryReader): ClassDefinition {
        return decodeClassDefinition(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ClassDefinition): Json {
        return toJsonClassDefinition(value);
    },

    /** Return one ClassDefinition from one JSON value. */
    fromJson(value: Json): ClassDefinition {
        return fromJsonClassDefinition(value);
    },
};

/** Encode one ClassDefinition. */
export function encodeClassDefinition(writer: BinaryWriter, value: ClassDefinition): void {
    writer.writeOption(value.space, (value0) => {
        encodeSpace(writer, value0);
    });
    writer.writeOption(value.template, (value1) => {
        encodeLocalGenericTemplateId(writer, value1);
    });
    encodeRepresentation(writer, value.representation);
    writer.writeBool(value.isAbstract);
    writer.writeBool(value.isFinal);
    writer.writeOption(value.extends, (value5) => {
        encodeNominalHeritage(writer, value5);
    });
    writer.writeUnsigned(value.implements.length);
    for (const item6 of value.implements) {
        encodeNominalConformance(writer, item6);
    }
    writer.writeOption(value.derives, (value7) => {
        writer.writeUnsigned(value7.length);
        for (const item8 of value7) {
            encodeAutoInterface(writer, item8);
        }
    });
    writer.writeUnsigned(value.constructors.length);
    for (const item8 of value.constructors) {
        encodeClassConstructorDefinition(writer, item8);
    }
    writer.writeUnsigned(value.members.length);
    for (const item9 of value.members) {
        encodeDefinitionMember(writer, item9);
    }
}

/** Decode one ClassDefinition. */
export function decodeClassDefinition(reader: BinaryReader): ClassDefinition {
    const space = reader.readOption(() => decodeSpace(reader));
    const template = reader.readOption(() => decodeLocalGenericTemplateId(reader));
    const representation = decodeRepresentation(reader);
    const isAbstract = reader.readBool();
    const isFinal = reader.readBool();
    const extends_ = reader.readOption(() => decodeNominalHeritage(reader));
    const implements_ = (() => { const length6 = reader.readNumber(); const items6: Array<NominalConformance> = []; for (let index = 0; index < length6; index += 1) { items6.push(decodeNominalConformance(reader)); } return items6; })();
    const derives = reader.readOption(() => (() => { const length8 = reader.readNumber(); const items8: Array<AutoInterface> = []; for (let index = 0; index < length8; index += 1) { items8.push(decodeAutoInterface(reader)); } return items8; })());
    const constructors = (() => { const length8 = reader.readNumber(); const items8: Array<ClassConstructorDefinition> = []; for (let index = 0; index < length8; index += 1) { items8.push(decodeClassConstructorDefinition(reader)); } return items8; })();
    const members = (() => { const length9 = reader.readNumber(); const items9: Array<DefinitionMember> = []; for (let index = 0; index < length9; index += 1) { items9.push(decodeDefinitionMember(reader)); } return items9; })();

    return {
        ...(space === undefined ? {} : { space }),
        ...(template === undefined ? {} : { template }),
        representation,
        isAbstract,
        isFinal,
        ...(extends_ === undefined ? {} : { extends: extends_ }),
        implements: implements_,
        ...(derives === undefined ? {} : { derives }),
        constructors,
        members,
    };
}

/** Return one JSON value for one ClassDefinition. */
export function toJsonClassDefinition(value: ClassDefinition): Json {
    return {
        ...(value.space === undefined ? {} : { space: toJsonSpace(value.space) }),
        ...(value.template === undefined ? {} : { template: toJsonLocalGenericTemplateId(value.template) }),
        representation: toJsonRepresentation(value.representation),
        isAbstract: value.isAbstract,
        isFinal: value.isFinal,
        ...(value.extends === undefined ? {} : { extends: toJsonNominalHeritage(value.extends) }),
        implements: value.implements.map((item0) => toJsonNominalConformance(item0)),
        ...(value.derives === undefined ? {} : { derives: value.derives.map((item0) => toJsonAutoInterface(item0)) }),
        constructors: value.constructors.map((item0) => toJsonClassConstructorDefinition(item0)),
        members: value.members.map((item0) => toJsonDefinitionMember(item0)),
    };
}

/** Return one ClassDefinition from one JSON value. */
export function fromJsonClassDefinition(value: Json): ClassDefinition {
    const object = jsonObject(value);

    return {
        space: jsonOptional(object, "space", (value) => fromJsonSpace(value)),
        template: jsonOptional(object, "template", (value) => fromJsonLocalGenericTemplateId(value)),
        representation: fromJsonRepresentation(jsonField(object, "representation")),
        isAbstract: jsonBool(jsonField(object, "isAbstract")),
        isFinal: jsonBool(jsonField(object, "isFinal")),
        extends: jsonOptional(object, "extends", (value) => fromJsonNominalHeritage(value)),
        implements: jsonArray(jsonField(object, "implements")).map((item0) => fromJsonNominalConformance(item0)),
        derives: jsonOptional(object, "derives", (value) => jsonArray(value).map((item0) => fromJsonAutoInterface(item0))),
        constructors: jsonArray(jsonField(object, "constructors")).map((item0) => fromJsonClassConstructorDefinition(item0)),
        members: jsonArray(jsonField(object, "members")).map((item0) => fromJsonDefinitionMember(item0)),
    };
}

/** Checked declaration data for one symbol. */
export type Definition =
    /** Transparent type alias declaration. */
    | {
          readonly kind: "typeAlias";
          readonly type_alias: TypeAliasDefinition;
      }
    /** Struct declaration. */
    | {
          readonly kind: "struct";
          readonly struct: StructDefinition;
      }
    /** Class declaration. */
    | {
          readonly kind: "class";
          readonly class: ClassDefinition;
      }
    /** Nominal interface declaration. */
    | {
          readonly kind: "interface";
          readonly interface: InterfaceDefinition;
      }
    /** Enum declaration. */
    | {
          readonly kind: "enum";
          readonly enum: EnumDefinition;
      }
    /** Newtype declaration. */
    | {
          readonly kind: "newtype";
          readonly newtype: NewtypeDefinition;
      }
    /** Extension declaration. */
    | {
          readonly kind: "extension";
          readonly extension: ExtensionDefinition;
      }
;

export const Definition = {
    /** Transparent type alias declaration. */
    typeAlias(type_alias: TypeAliasDefinition): Definition {
        return { kind: "typeAlias", type_alias };
    },

    /** Struct declaration. */
    struct(struct: StructDefinition): Definition {
        return { kind: "struct", struct };
    },

    /** Class declaration. */
    "class"(class_: ClassDefinition): Definition {
        return { kind: "class", class: class_ };
    },

    /** Nominal interface declaration. */
    "interface"(interface_: InterfaceDefinition): Definition {
        return { kind: "interface", interface: interface_ };
    },

    /** Enum declaration. */
    "enum"(enum_: EnumDefinition): Definition {
        return { kind: "enum", enum: enum_ };
    },

    /** Newtype declaration. */
    newtype(newtype: NewtypeDefinition): Definition {
        return { kind: "newtype", newtype };
    },

    /** Extension declaration. */
    extension(extension: ExtensionDefinition): Definition {
        return { kind: "extension", extension };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: Definition): void {
        encodeDefinition(writer, value);
    },

    /** Decode one Definition. */
    decode(reader: BinaryReader): Definition {
        return decodeDefinition(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Definition): Json {
        return toJsonDefinition(value);
    },

    /** Return one Definition from one JSON value. */
    fromJson(value: Json): Definition {
        return fromJsonDefinition(value);
    },
};

/** Encode one Definition. */
export function encodeDefinition(writer: BinaryWriter, value: Definition): void {
    switch (value.kind) {
        case "typeAlias":
            writer.writeUnsigned(0);
            encodeTypeAliasDefinition(writer, value.type_alias);
            return;
        case "struct":
            writer.writeUnsigned(1);
            encodeStructDefinition(writer, value.struct);
            return;
        case "class":
            writer.writeUnsigned(2);
            encodeClassDefinition(writer, value.class);
            return;
        case "interface":
            writer.writeUnsigned(3);
            encodeInterfaceDefinition(writer, value.interface);
            return;
        case "enum":
            writer.writeUnsigned(4);
            encodeEnumDefinition(writer, value.enum);
            return;
        case "newtype":
            writer.writeUnsigned(5);
            encodeNewtypeDefinition(writer, value.newtype);
            return;
        case "extension":
            writer.writeUnsigned(6);
            encodeExtensionDefinition(writer, value.extension);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one Definition. */
export function decodeDefinition(reader: BinaryReader): Definition {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const type_alias = decodeTypeAliasDefinition(reader);

            return { kind: "typeAlias", type_alias };
        }
        case 1: {
            const struct = decodeStructDefinition(reader);

            return { kind: "struct", struct };
        }
        case 2: {
            const class_ = decodeClassDefinition(reader);

            return { kind: "class", class: class_ };
        }
        case 3: {
            const interface_ = decodeInterfaceDefinition(reader);

            return { kind: "interface", interface: interface_ };
        }
        case 4: {
            const enum_ = decodeEnumDefinition(reader);

            return { kind: "enum", enum: enum_ };
        }
        case 5: {
            const newtype = decodeNewtypeDefinition(reader);

            return { kind: "newtype", newtype };
        }
        case 6: {
            const extension = decodeExtensionDefinition(reader);

            return { kind: "extension", extension };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one Definition. */
export function toJsonDefinition(value: Definition): Json {
    switch (value.kind) {
        case "typeAlias":
            return {
                kind: "typeAlias",
                type_alias: toJsonTypeAliasDefinition(value.type_alias),
            };
        case "struct":
            return {
                kind: "struct",
                struct: toJsonStructDefinition(value.struct),
            };
        case "class":
            return {
                kind: "class",
                class: toJsonClassDefinition(value.class),
            };
        case "interface":
            return {
                kind: "interface",
                interface: toJsonInterfaceDefinition(value.interface),
            };
        case "enum":
            return {
                kind: "enum",
                enum: toJsonEnumDefinition(value.enum),
            };
        case "newtype":
            return {
                kind: "newtype",
                newtype: toJsonNewtypeDefinition(value.newtype),
            };
        case "extension":
            return {
                kind: "extension",
                extension: toJsonExtensionDefinition(value.extension),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one Definition from one JSON value. */
export function fromJsonDefinition(value: Json): Definition {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "typeAlias":
            return {
                kind,
                type_alias: fromJsonTypeAliasDefinition(jsonField(object, "type_alias")),
            };
        case "struct":
            return {
                kind,
                struct: fromJsonStructDefinition(jsonField(object, "struct")),
            };
        case "class":
            return {
                kind,
                class: fromJsonClassDefinition(jsonField(object, "class")),
            };
        case "interface":
            return {
                kind,
                interface: fromJsonInterfaceDefinition(jsonField(object, "interface")),
            };
        case "enum":
            return {
                kind,
                enum: fromJsonEnumDefinition(jsonField(object, "enum")),
            };
        case "newtype":
            return {
                kind,
                newtype: fromJsonNewtypeDefinition(jsonField(object, "newtype")),
            };
        case "extension":
            return {
                kind,
                extension: fromJsonExtensionDefinition(jsonField(object, "extension")),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** One declaration member. */
export type DefinitionMember =
    /** Field member. */
    | {
          readonly kind: "field";
          readonly field: FieldDefinition;
      }
    /** Method member. */
    | {
          readonly kind: "method";
          readonly method: MethodDefinition;
      }
    /** Associated type member. */
    | {
          readonly kind: "associatedType";
          readonly associated_type: AssociatedTypeDefinition;
      }
    /** Associated constant member. */
    | {
          readonly kind: "associatedConst";
          readonly associated_const: AssociatedConstDefinition;
      }
    /** Declared enum variant member. */
    | {
          readonly kind: "enumVariant";
          readonly enum_variant: EnumVariantDefinition;
      }
    /** Structural call signature member. */
    | {
          readonly kind: "callSignature";
          readonly call_signature: SignatureDefinition;
      }
    /** Structural construct signature member. */
    | {
          readonly kind: "constructSignature";
          readonly construct_signature: SignatureDefinition;
      }
    /** Structural index signature member. */
    | {
          readonly kind: "indexSignature";
          readonly index_signature: IndexSignatureDefinition;
      }
;

export const DefinitionMember = {
    /** Field member. */
    field(field: FieldDefinition): DefinitionMember {
        return { kind: "field", field };
    },

    /** Method member. */
    method(method: MethodDefinition): DefinitionMember {
        return { kind: "method", method };
    },

    /** Associated type member. */
    associatedType(associated_type: AssociatedTypeDefinition): DefinitionMember {
        return { kind: "associatedType", associated_type };
    },

    /** Associated constant member. */
    associatedConst(associated_const: AssociatedConstDefinition): DefinitionMember {
        return { kind: "associatedConst", associated_const };
    },

    /** Declared enum variant member. */
    enumVariant(enum_variant: EnumVariantDefinition): DefinitionMember {
        return { kind: "enumVariant", enum_variant };
    },

    /** Structural call signature member. */
    callSignature(call_signature: SignatureDefinition): DefinitionMember {
        return { kind: "callSignature", call_signature };
    },

    /** Structural construct signature member. */
    constructSignature(construct_signature: SignatureDefinition): DefinitionMember {
        return { kind: "constructSignature", construct_signature };
    },

    /** Structural index signature member. */
    indexSignature(index_signature: IndexSignatureDefinition): DefinitionMember {
        return { kind: "indexSignature", index_signature };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: DefinitionMember): void {
        encodeDefinitionMember(writer, value);
    },

    /** Decode one DefinitionMember. */
    decode(reader: BinaryReader): DefinitionMember {
        return decodeDefinitionMember(reader);
    },

    /** Return this value as JSON. */
    toJson(value: DefinitionMember): Json {
        return toJsonDefinitionMember(value);
    },

    /** Return one DefinitionMember from one JSON value. */
    fromJson(value: Json): DefinitionMember {
        return fromJsonDefinitionMember(value);
    },
};

/** Encode one DefinitionMember. */
export function encodeDefinitionMember(writer: BinaryWriter, value: DefinitionMember): void {
    switch (value.kind) {
        case "field":
            writer.writeUnsigned(0);
            encodeFieldDefinition(writer, value.field);
            return;
        case "method":
            writer.writeUnsigned(1);
            encodeMethodDefinition(writer, value.method);
            return;
        case "associatedType":
            writer.writeUnsigned(2);
            encodeAssociatedTypeDefinition(writer, value.associated_type);
            return;
        case "associatedConst":
            writer.writeUnsigned(3);
            encodeAssociatedConstDefinition(writer, value.associated_const);
            return;
        case "enumVariant":
            writer.writeUnsigned(4);
            encodeEnumVariantDefinition(writer, value.enum_variant);
            return;
        case "callSignature":
            writer.writeUnsigned(5);
            encodeSignatureDefinition(writer, value.call_signature);
            return;
        case "constructSignature":
            writer.writeUnsigned(6);
            encodeSignatureDefinition(writer, value.construct_signature);
            return;
        case "indexSignature":
            writer.writeUnsigned(7);
            encodeIndexSignatureDefinition(writer, value.index_signature);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one DefinitionMember. */
export function decodeDefinitionMember(reader: BinaryReader): DefinitionMember {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const field = decodeFieldDefinition(reader);

            return { kind: "field", field };
        }
        case 1: {
            const method = decodeMethodDefinition(reader);

            return { kind: "method", method };
        }
        case 2: {
            const associated_type = decodeAssociatedTypeDefinition(reader);

            return { kind: "associatedType", associated_type };
        }
        case 3: {
            const associated_const = decodeAssociatedConstDefinition(reader);

            return { kind: "associatedConst", associated_const };
        }
        case 4: {
            const enum_variant = decodeEnumVariantDefinition(reader);

            return { kind: "enumVariant", enum_variant };
        }
        case 5: {
            const call_signature = decodeSignatureDefinition(reader);

            return { kind: "callSignature", call_signature };
        }
        case 6: {
            const construct_signature = decodeSignatureDefinition(reader);

            return { kind: "constructSignature", construct_signature };
        }
        case 7: {
            const index_signature = decodeIndexSignatureDefinition(reader);

            return { kind: "indexSignature", index_signature };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one DefinitionMember. */
export function toJsonDefinitionMember(value: DefinitionMember): Json {
    switch (value.kind) {
        case "field":
            return {
                kind: "field",
                field: toJsonFieldDefinition(value.field),
            };
        case "method":
            return {
                kind: "method",
                method: toJsonMethodDefinition(value.method),
            };
        case "associatedType":
            return {
                kind: "associatedType",
                associated_type: toJsonAssociatedTypeDefinition(value.associated_type),
            };
        case "associatedConst":
            return {
                kind: "associatedConst",
                associated_const: toJsonAssociatedConstDefinition(value.associated_const),
            };
        case "enumVariant":
            return {
                kind: "enumVariant",
                enum_variant: toJsonEnumVariantDefinition(value.enum_variant),
            };
        case "callSignature":
            return {
                kind: "callSignature",
                call_signature: toJsonSignatureDefinition(value.call_signature),
            };
        case "constructSignature":
            return {
                kind: "constructSignature",
                construct_signature: toJsonSignatureDefinition(value.construct_signature),
            };
        case "indexSignature":
            return {
                kind: "indexSignature",
                index_signature: toJsonIndexSignatureDefinition(value.index_signature),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one DefinitionMember from one JSON value. */
export function fromJsonDefinitionMember(value: Json): DefinitionMember {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "field":
            return {
                kind,
                field: fromJsonFieldDefinition(jsonField(object, "field")),
            };
        case "method":
            return {
                kind,
                method: fromJsonMethodDefinition(jsonField(object, "method")),
            };
        case "associatedType":
            return {
                kind,
                associated_type: fromJsonAssociatedTypeDefinition(jsonField(object, "associated_type")),
            };
        case "associatedConst":
            return {
                kind,
                associated_const: fromJsonAssociatedConstDefinition(jsonField(object, "associated_const")),
            };
        case "enumVariant":
            return {
                kind,
                enum_variant: fromJsonEnumVariantDefinition(jsonField(object, "enum_variant")),
            };
        case "callSignature":
            return {
                kind,
                call_signature: fromJsonSignatureDefinition(jsonField(object, "call_signature")),
            };
        case "constructSignature":
            return {
                kind,
                construct_signature: fromJsonSignatureDefinition(jsonField(object, "construct_signature")),
            };
        case "indexSignature":
            return {
                kind,
                index_signature: fromJsonIndexSignatureDefinition(jsonField(object, "index_signature")),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** Declaration definitions added by one DIR phase. */
export type DefinitionSegment = {
    /** The module id of the definition segment. */
    readonly moduleId: ModuleId;
    /** Source declaration nodes keyed by declaring symbol. */
    readonly sources: ReadonlyMap<GlobalSymbolId, GlobalNodeIdAny>;
    /** Definitions keyed by declaring symbol. */
    readonly definitions: ReadonlyMap<GlobalSymbolId, Definition>;
    /** Declaring definition symbols keyed by member symbol. */
    readonly definitionsByMember: ReadonlyMap<GlobalSymbolId, GlobalSymbolId>;
    /** Extension symbols by target root. */
    readonly extensionsByRoot: ReadonlyMap<TypeRoot, ReadonlyArray<GlobalSymbolId>>;
    /** Blanket extension symbols. */
    readonly blanketExtensions: ReadonlyArray<GlobalSymbolId>;
};

export const DefinitionSegment = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: DefinitionSegment): void {
        encodeDefinitionSegment(writer, value);
    },

    /** Decode one DefinitionSegment. */
    decode(reader: BinaryReader): DefinitionSegment {
        return decodeDefinitionSegment(reader);
    },

    /** Return this value as JSON. */
    toJson(value: DefinitionSegment): Json {
        return toJsonDefinitionSegment(value);
    },

    /** Return one DefinitionSegment from one JSON value. */
    fromJson(value: Json): DefinitionSegment {
        return fromJsonDefinitionSegment(value);
    },
};

/** Encode one DefinitionSegment. */
export function encodeDefinitionSegment(writer: BinaryWriter, value: DefinitionSegment): void {
    encodeModuleId(writer, value.moduleId);
    const entries1 = Array.from(value.sources.entries()).map(([key1, item1]) => {
        const keyBytes = nestedBytes((writer) => {
            encodeGlobalSymbolId(writer, key1);
        });
        return { key1, item1, keyBytes };
    });
    entries1.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries1.length);
    for (const entry1 of entries1) {
        encodeGlobalSymbolId(writer, entry1.key1);
        encodeGlobalNodeIdAny(writer, entry1.item1);
    }
    const entries2 = Array.from(value.definitions.entries()).map(([key2, item2]) => {
        const keyBytes = nestedBytes((writer) => {
            encodeGlobalSymbolId(writer, key2);
        });
        return { key2, item2, keyBytes };
    });
    entries2.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries2.length);
    for (const entry2 of entries2) {
        encodeGlobalSymbolId(writer, entry2.key2);
        encodeDefinition(writer, entry2.item2);
    }
    const entries3 = Array.from(value.definitionsByMember.entries()).map(([key3, item3]) => {
        const keyBytes = nestedBytes((writer) => {
            encodeGlobalSymbolId(writer, key3);
        });
        return { key3, item3, keyBytes };
    });
    entries3.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries3.length);
    for (const entry3 of entries3) {
        encodeGlobalSymbolId(writer, entry3.key3);
        encodeGlobalSymbolId(writer, entry3.item3);
    }
    const entries4 = Array.from(value.extensionsByRoot.entries()).map(([key4, item4]) => {
        const keyBytes = nestedBytes((writer) => {
            encodeTypeRoot(writer, key4);
        });
        return { key4, item4, keyBytes };
    });
    entries4.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries4.length);
    for (const entry4 of entries4) {
        encodeTypeRoot(writer, entry4.key4);
        writer.writeUnsigned(entry4.item4.length);
        for (const item5 of entry4.item4) {
            encodeGlobalSymbolId(writer, item5);
        }
    }
    writer.writeUnsigned(value.blanketExtensions.length);
    for (const item5 of value.blanketExtensions) {
        encodeGlobalSymbolId(writer, item5);
    }
}

/** Decode one DefinitionSegment. */
export function decodeDefinitionSegment(reader: BinaryReader): DefinitionSegment {
    const moduleId = decodeModuleId(reader);
    const sources = (() => { const length1 = reader.readNumber(); const items1 = new Map<GlobalSymbolId, GlobalNodeIdAny>(); for (let index = 0; index < length1; index += 1) { items1.set(decodeGlobalSymbolId(reader), decodeGlobalNodeIdAny(reader)); } return items1; })();
    const definitions = (() => { const length2 = reader.readNumber(); const items2 = new Map<GlobalSymbolId, Definition>(); for (let index = 0; index < length2; index += 1) { items2.set(decodeGlobalSymbolId(reader), decodeDefinition(reader)); } return items2; })();
    const definitionsByMember = (() => { const length3 = reader.readNumber(); const items3 = new Map<GlobalSymbolId, GlobalSymbolId>(); for (let index = 0; index < length3; index += 1) { items3.set(decodeGlobalSymbolId(reader), decodeGlobalSymbolId(reader)); } return items3; })();
    const extensionsByRoot = (() => { const length4 = reader.readNumber(); const items4 = new Map<TypeRoot, ReadonlyArray<GlobalSymbolId>>(); for (let index = 0; index < length4; index += 1) { items4.set(decodeTypeRoot(reader), (() => { const length6 = reader.readNumber(); const items6: Array<GlobalSymbolId> = []; for (let index = 0; index < length6; index += 1) { items6.push(decodeGlobalSymbolId(reader)); } return items6; })()); } return items4; })();
    const blanketExtensions = (() => { const length5 = reader.readNumber(); const items5: Array<GlobalSymbolId> = []; for (let index = 0; index < length5; index += 1) { items5.push(decodeGlobalSymbolId(reader)); } return items5; })();

    return {
        moduleId,
        sources,
        definitions,
        definitionsByMember,
        extensionsByRoot,
        blanketExtensions,
    };
}

/** Return one JSON value for one DefinitionSegment. */
export function toJsonDefinitionSegment(value: DefinitionSegment): Json {
    return {
        moduleId: toJsonModuleId(value.moduleId),
        sources: Array.from(value.sources.entries()).map(([key0, item0]) => [toJsonGlobalSymbolId(key0), toJsonGlobalNodeIdAny(item0)] as const),
        definitions: Array.from(value.definitions.entries()).map(([key0, item0]) => [toJsonGlobalSymbolId(key0), toJsonDefinition(item0)] as const),
        definitionsByMember: Array.from(value.definitionsByMember.entries()).map(([key0, item0]) => [toJsonGlobalSymbolId(key0), toJsonGlobalSymbolId(item0)] as const),
        extensionsByRoot: Array.from(value.extensionsByRoot.entries()).map(([key0, item0]) => [toJsonTypeRoot(key0), item0.map((item1) => toJsonGlobalSymbolId(item1))] as const),
        blanketExtensions: value.blanketExtensions.map((item0) => toJsonGlobalSymbolId(item0)),
    };
}

/** Return one DefinitionSegment from one JSON value. */
export function fromJsonDefinitionSegment(value: Json): DefinitionSegment {
    const object = jsonObject(value);

    return {
        moduleId: fromJsonModuleId(jsonField(object, "moduleId")),
        sources: new Map(jsonArray(jsonField(object, "sources")).map((entry) => { const items = jsonArray(entry); if (items.length !== 2) { throw new SerdeError(`expected JSON map entry length 2: ${items.length}`); } const key0 = items[0]; const item0 = items[1]; return [fromJsonGlobalSymbolId(key0), fromJsonGlobalNodeIdAny(item0)] as const; })),
        definitions: new Map(jsonArray(jsonField(object, "definitions")).map((entry) => { const items = jsonArray(entry); if (items.length !== 2) { throw new SerdeError(`expected JSON map entry length 2: ${items.length}`); } const key0 = items[0]; const item0 = items[1]; return [fromJsonGlobalSymbolId(key0), fromJsonDefinition(item0)] as const; })),
        definitionsByMember: new Map(jsonArray(jsonField(object, "definitionsByMember")).map((entry) => { const items = jsonArray(entry); if (items.length !== 2) { throw new SerdeError(`expected JSON map entry length 2: ${items.length}`); } const key0 = items[0]; const item0 = items[1]; return [fromJsonGlobalSymbolId(key0), fromJsonGlobalSymbolId(item0)] as const; })),
        extensionsByRoot: new Map(jsonArray(jsonField(object, "extensionsByRoot")).map((entry) => { const items = jsonArray(entry); if (items.length !== 2) { throw new SerdeError(`expected JSON map entry length 2: ${items.length}`); } const key0 = items[0]; const item0 = items[1]; return [fromJsonTypeRoot(key0), jsonArray(item0).map((item1) => fromJsonGlobalSymbolId(item1))] as const; })),
        blanketExtensions: jsonArray(jsonField(object, "blanketExtensions")).map((item0) => fromJsonGlobalSymbolId(item0)),
    };
}

/** Checked declaration data for one nominal enum. */
export type EnumDefinition = {
    /** The intrinsic instance space, or relative when absent. */
    readonly space?: Space;
    /** The generic template declared by the enum. */
    readonly template?: LocalGenericTemplateId;
    /** The selected runtime representation. */
    readonly representation: Representation;
    /** The scalar type backing every enum variant. */
    readonly backing: EnumBackingType;
    /** The implemented interfaces. */
    readonly implements: ReadonlyArray<NominalConformance>;
    /** The written derive list replacing the auto set, if any. */
    readonly derives?: ReadonlyArray<AutoInterface>;
    /** The members in declaration order. */
    readonly members: ReadonlyArray<DefinitionMember>;
};

export const EnumDefinition = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: EnumDefinition): void {
        encodeEnumDefinition(writer, value);
    },

    /** Decode one EnumDefinition. */
    decode(reader: BinaryReader): EnumDefinition {
        return decodeEnumDefinition(reader);
    },

    /** Return this value as JSON. */
    toJson(value: EnumDefinition): Json {
        return toJsonEnumDefinition(value);
    },

    /** Return one EnumDefinition from one JSON value. */
    fromJson(value: Json): EnumDefinition {
        return fromJsonEnumDefinition(value);
    },
};

/** Encode one EnumDefinition. */
export function encodeEnumDefinition(writer: BinaryWriter, value: EnumDefinition): void {
    writer.writeOption(value.space, (value0) => {
        encodeSpace(writer, value0);
    });
    writer.writeOption(value.template, (value1) => {
        encodeLocalGenericTemplateId(writer, value1);
    });
    encodeRepresentation(writer, value.representation);
    encodeEnumBackingType(writer, value.backing);
    writer.writeUnsigned(value.implements.length);
    for (const item4 of value.implements) {
        encodeNominalConformance(writer, item4);
    }
    writer.writeOption(value.derives, (value5) => {
        writer.writeUnsigned(value5.length);
        for (const item6 of value5) {
            encodeAutoInterface(writer, item6);
        }
    });
    writer.writeUnsigned(value.members.length);
    for (const item6 of value.members) {
        encodeDefinitionMember(writer, item6);
    }
}

/** Decode one EnumDefinition. */
export function decodeEnumDefinition(reader: BinaryReader): EnumDefinition {
    const space = reader.readOption(() => decodeSpace(reader));
    const template = reader.readOption(() => decodeLocalGenericTemplateId(reader));
    const representation = decodeRepresentation(reader);
    const backing = decodeEnumBackingType(reader);
    const implements_ = (() => { const length4 = reader.readNumber(); const items4: Array<NominalConformance> = []; for (let index = 0; index < length4; index += 1) { items4.push(decodeNominalConformance(reader)); } return items4; })();
    const derives = reader.readOption(() => (() => { const length6 = reader.readNumber(); const items6: Array<AutoInterface> = []; for (let index = 0; index < length6; index += 1) { items6.push(decodeAutoInterface(reader)); } return items6; })());
    const members = (() => { const length6 = reader.readNumber(); const items6: Array<DefinitionMember> = []; for (let index = 0; index < length6; index += 1) { items6.push(decodeDefinitionMember(reader)); } return items6; })();

    return {
        ...(space === undefined ? {} : { space }),
        ...(template === undefined ? {} : { template }),
        representation,
        backing,
        implements: implements_,
        ...(derives === undefined ? {} : { derives }),
        members,
    };
}

/** Return one JSON value for one EnumDefinition. */
export function toJsonEnumDefinition(value: EnumDefinition): Json {
    return {
        ...(value.space === undefined ? {} : { space: toJsonSpace(value.space) }),
        ...(value.template === undefined ? {} : { template: toJsonLocalGenericTemplateId(value.template) }),
        representation: toJsonRepresentation(value.representation),
        backing: toJsonEnumBackingType(value.backing),
        implements: value.implements.map((item0) => toJsonNominalConformance(item0)),
        ...(value.derives === undefined ? {} : { derives: value.derives.map((item0) => toJsonAutoInterface(item0)) }),
        members: value.members.map((item0) => toJsonDefinitionMember(item0)),
    };
}

/** Return one EnumDefinition from one JSON value. */
export function fromJsonEnumDefinition(value: Json): EnumDefinition {
    const object = jsonObject(value);

    return {
        space: jsonOptional(object, "space", (value) => fromJsonSpace(value)),
        template: jsonOptional(object, "template", (value) => fromJsonLocalGenericTemplateId(value)),
        representation: fromJsonRepresentation(jsonField(object, "representation")),
        backing: fromJsonEnumBackingType(jsonField(object, "backing")),
        implements: jsonArray(jsonField(object, "implements")).map((item0) => fromJsonNominalConformance(item0)),
        derives: jsonOptional(object, "derives", (value) => jsonArray(value).map((item0) => fromJsonAutoInterface(item0))),
        members: jsonArray(jsonField(object, "members")).map((item0) => fromJsonDefinitionMember(item0)),
    };
}

/** One declared enum variant. */
export type EnumVariantDefinition = {
    /** The variant symbol. */
    readonly symbol: GlobalSymbolId;
    /** The source enum field node. */
    readonly source: GlobalNodeIdAny;
    /** The variant key. */
    readonly key: StaticKey;
    /** The resolved scalar value. */
    readonly value: EnumVariantValue;
};

export const EnumVariantDefinition = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: EnumVariantDefinition): void {
        encodeEnumVariantDefinition(writer, value);
    },

    /** Decode one EnumVariantDefinition. */
    decode(reader: BinaryReader): EnumVariantDefinition {
        return decodeEnumVariantDefinition(reader);
    },

    /** Return this value as JSON. */
    toJson(value: EnumVariantDefinition): Json {
        return toJsonEnumVariantDefinition(value);
    },

    /** Return one EnumVariantDefinition from one JSON value. */
    fromJson(value: Json): EnumVariantDefinition {
        return fromJsonEnumVariantDefinition(value);
    },
};

/** Encode one EnumVariantDefinition. */
export function encodeEnumVariantDefinition(writer: BinaryWriter, value: EnumVariantDefinition): void {
    encodeGlobalSymbolId(writer, value.symbol);
    encodeGlobalNodeIdAny(writer, value.source);
    encodeStaticKey(writer, value.key);
    encodeEnumVariantValue(writer, value.value);
}

/** Decode one EnumVariantDefinition. */
export function decodeEnumVariantDefinition(reader: BinaryReader): EnumVariantDefinition {
    const symbol_ = decodeGlobalSymbolId(reader);
    const source = decodeGlobalNodeIdAny(reader);
    const key = decodeStaticKey(reader);
    const value = decodeEnumVariantValue(reader);

    return {
        symbol: symbol_,
        source,
        key,
        value,
    };
}

/** Return one JSON value for one EnumVariantDefinition. */
export function toJsonEnumVariantDefinition(value: EnumVariantDefinition): Json {
    return {
        symbol: toJsonGlobalSymbolId(value.symbol),
        source: toJsonGlobalNodeIdAny(value.source),
        key: toJsonStaticKey(value.key),
        value: toJsonEnumVariantValue(value.value),
    };
}

/** Return one EnumVariantDefinition from one JSON value. */
export function fromJsonEnumVariantDefinition(value: Json): EnumVariantDefinition {
    const object = jsonObject(value);

    return {
        symbol: fromJsonGlobalSymbolId(jsonField(object, "symbol")),
        source: fromJsonGlobalNodeIdAny(jsonField(object, "source")),
        key: fromJsonStaticKey(jsonField(object, "key")),
        value: fromJsonEnumVariantValue(jsonField(object, "value")),
    };
}

/** Checked declaration data for one extension. */
export type ExtensionDefinition = {
    /** The extension declaration's symbol. */
    readonly symbol: GlobalSymbolId;
    /** The extension declaration form. */
    readonly form: ExtensionForm;
    /** The extension's generic template. */
    readonly template?: LocalGenericTemplateId;
    /** The checked receiver target. */
    readonly target: ExtensionTarget;
    /** The implemented interfaces. */
    readonly implements: ReadonlyArray<NominalConformance>;
    /** The members in declaration order. */
    readonly members: ReadonlyArray<DefinitionMember>;
};

export const ExtensionDefinition = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ExtensionDefinition): void {
        encodeExtensionDefinition(writer, value);
    },

    /** Decode one ExtensionDefinition. */
    decode(reader: BinaryReader): ExtensionDefinition {
        return decodeExtensionDefinition(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ExtensionDefinition): Json {
        return toJsonExtensionDefinition(value);
    },

    /** Return one ExtensionDefinition from one JSON value. */
    fromJson(value: Json): ExtensionDefinition {
        return fromJsonExtensionDefinition(value);
    },
};

/** Encode one ExtensionDefinition. */
export function encodeExtensionDefinition(writer: BinaryWriter, value: ExtensionDefinition): void {
    encodeGlobalSymbolId(writer, value.symbol);
    encodeExtensionForm(writer, value.form);
    writer.writeOption(value.template, (value2) => {
        encodeLocalGenericTemplateId(writer, value2);
    });
    encodeExtensionTarget(writer, value.target);
    writer.writeUnsigned(value.implements.length);
    for (const item4 of value.implements) {
        encodeNominalConformance(writer, item4);
    }
    writer.writeUnsigned(value.members.length);
    for (const item5 of value.members) {
        encodeDefinitionMember(writer, item5);
    }
}

/** Decode one ExtensionDefinition. */
export function decodeExtensionDefinition(reader: BinaryReader): ExtensionDefinition {
    const symbol_ = decodeGlobalSymbolId(reader);
    const form = decodeExtensionForm(reader);
    const template = reader.readOption(() => decodeLocalGenericTemplateId(reader));
    const target = decodeExtensionTarget(reader);
    const implements_ = (() => { const length4 = reader.readNumber(); const items4: Array<NominalConformance> = []; for (let index = 0; index < length4; index += 1) { items4.push(decodeNominalConformance(reader)); } return items4; })();
    const members = (() => { const length5 = reader.readNumber(); const items5: Array<DefinitionMember> = []; for (let index = 0; index < length5; index += 1) { items5.push(decodeDefinitionMember(reader)); } return items5; })();

    return {
        symbol: symbol_,
        form,
        ...(template === undefined ? {} : { template }),
        target,
        implements: implements_,
        members,
    };
}

/** Return one JSON value for one ExtensionDefinition. */
export function toJsonExtensionDefinition(value: ExtensionDefinition): Json {
    return {
        symbol: toJsonGlobalSymbolId(value.symbol),
        form: toJsonExtensionForm(value.form),
        ...(value.template === undefined ? {} : { template: toJsonLocalGenericTemplateId(value.template) }),
        target: toJsonExtensionTarget(value.target),
        implements: value.implements.map((item0) => toJsonNominalConformance(item0)),
        members: value.members.map((item0) => toJsonDefinitionMember(item0)),
    };
}

/** Return one ExtensionDefinition from one JSON value. */
export function fromJsonExtensionDefinition(value: Json): ExtensionDefinition {
    const object = jsonObject(value);

    return {
        symbol: fromJsonGlobalSymbolId(jsonField(object, "symbol")),
        form: fromJsonExtensionForm(jsonField(object, "form")),
        template: jsonOptional(object, "template", (value) => fromJsonLocalGenericTemplateId(value)),
        target: fromJsonExtensionTarget(jsonField(object, "target")),
        implements: jsonArray(jsonField(object, "implements")).map((item0) => fromJsonNominalConformance(item0)),
        members: jsonArray(jsonField(object, "members")).map((item0) => fromJsonDefinitionMember(item0)),
    };
}

/** How an extension declaration relates to its target type. */
export type ExtensionForm = "local" | "exported";

export const ExtensionForm = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ExtensionForm): void {
        encodeExtensionForm(writer, value);
    },

    /** Decode one ExtensionForm. */
    decode(reader: BinaryReader): ExtensionForm {
        return decodeExtensionForm(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ExtensionForm): Json {
        return toJsonExtensionForm(value);
    },

    /** Return one ExtensionForm from one JSON value. */
    fromJson(value: Json): ExtensionForm {
        return fromJsonExtensionForm(value);
    },
};

/** Encode one ExtensionForm. */
export function encodeExtensionForm(writer: BinaryWriter, value: ExtensionForm): void {
    switch (value) {
        case "local":
            writer.writeUnsigned(0);
            return;
        case "exported":
            writer.writeUnsigned(1);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one ExtensionForm. */
export function decodeExtensionForm(reader: BinaryReader): ExtensionForm {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "local";
        case 1:
            return "exported";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one ExtensionForm. */
export function toJsonExtensionForm(value: ExtensionForm): Json {
    return value;
}

/** Return one ExtensionForm from one JSON value. */
export function fromJsonExtensionForm(value: Json): ExtensionForm {
    const variant = jsonString(value);

    switch (variant) {
        case "local":
            return "local";
        case "exported":
            return "exported";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** Extension lookup target. */
export type ExtensionTarget =
    /** Extension whose receiver type names one indexable root. */
    | {
          readonly kind: "rooted";
          /** The root used for member lookup and conflicts. */
          readonly root: TypeRoot;
          /** The checked receiver type. */
          readonly ty: GlobalTypeId;
      }
    /** Extension over an open receiver type. */
    | {
          readonly kind: "blanket";
          /** The checked receiver type. */
          readonly ty: GlobalTypeId;
          /** The receiver coverage the declared bound decides. */
          readonly coverage: BlanketCoverage;
      }
;

export const ExtensionTarget = {
    /** Extension whose receiver type names one indexable root. */
    rooted(root: TypeRoot, ty: GlobalTypeId): ExtensionTarget {
        return { kind: "rooted", root, ty };
    },

    /** Extension over an open receiver type. */
    blanket(ty: GlobalTypeId, coverage: BlanketCoverage): ExtensionTarget {
        return { kind: "blanket", ty, coverage };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: ExtensionTarget): void {
        encodeExtensionTarget(writer, value);
    },

    /** Decode one ExtensionTarget. */
    decode(reader: BinaryReader): ExtensionTarget {
        return decodeExtensionTarget(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ExtensionTarget): Json {
        return toJsonExtensionTarget(value);
    },

    /** Return one ExtensionTarget from one JSON value. */
    fromJson(value: Json): ExtensionTarget {
        return fromJsonExtensionTarget(value);
    },
};

/** Encode one ExtensionTarget. */
export function encodeExtensionTarget(writer: BinaryWriter, value: ExtensionTarget): void {
    switch (value.kind) {
        case "rooted":
            writer.writeUnsigned(0);
            encodeTypeRoot(writer, value.root);
            encodeGlobalTypeId(writer, value.ty);
            return;
        case "blanket":
            writer.writeUnsigned(1);
            encodeGlobalTypeId(writer, value.ty);
            encodeBlanketCoverage(writer, value.coverage);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one ExtensionTarget. */
export function decodeExtensionTarget(reader: BinaryReader): ExtensionTarget {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const root = decodeTypeRoot(reader);
            const ty = decodeGlobalTypeId(reader);

            return {
                kind: "rooted",
                root,
                ty,
            };
        }
        case 1: {
            const ty = decodeGlobalTypeId(reader);
            const coverage = decodeBlanketCoverage(reader);

            return {
                kind: "blanket",
                ty,
                coverage,
            };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one ExtensionTarget. */
export function toJsonExtensionTarget(value: ExtensionTarget): Json {
    switch (value.kind) {
        case "rooted":
            return {
                kind: "rooted",
                root: toJsonTypeRoot(value.root),
                ty: toJsonGlobalTypeId(value.ty),
            };
        case "blanket":
            return {
                kind: "blanket",
                ty: toJsonGlobalTypeId(value.ty),
                coverage: toJsonBlanketCoverage(value.coverage),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one ExtensionTarget from one JSON value. */
export function fromJsonExtensionTarget(value: Json): ExtensionTarget {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "rooted":
            return {
                kind,
                root: fromJsonTypeRoot(jsonField(object, "root")),
                ty: fromJsonGlobalTypeId(jsonField(object, "ty")),
            };
        case "blanket":
            return {
                kind,
                ty: fromJsonGlobalTypeId(jsonField(object, "ty")),
                coverage: fromJsonBlanketCoverage(jsonField(object, "coverage")),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** One field member. */
export type FieldDefinition = {
    /** The member space declaring the field. */
    readonly space: MemberSpace;
    /** The field symbol. */
    readonly symbol: GlobalSymbolId;
    /** The source member node. */
    readonly source: GlobalNodeIdAny;
    /** The field key. */
    readonly key: StaticKey;
    /** The field initializer expression, when one is declared. */
    readonly initializer?: GlobalNodeIdAny;
    /** Whether the field is optional on its declaration. */
    readonly isOptional: boolean;
    /** Whether the field rejects writes after initialization. */
    readonly isReadonly: boolean;
    /** Whether subclasses must provide the field. */
    readonly isAbstract: boolean;
    /** Whether the field overrides an inherited member. */
    readonly isOverride: boolean;
    /** The overridden base member, selected while checking. */
    readonly overrides?: GlobalSymbolId;
};

export const FieldDefinition = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: FieldDefinition): void {
        encodeFieldDefinition(writer, value);
    },

    /** Decode one FieldDefinition. */
    decode(reader: BinaryReader): FieldDefinition {
        return decodeFieldDefinition(reader);
    },

    /** Return this value as JSON. */
    toJson(value: FieldDefinition): Json {
        return toJsonFieldDefinition(value);
    },

    /** Return one FieldDefinition from one JSON value. */
    fromJson(value: Json): FieldDefinition {
        return fromJsonFieldDefinition(value);
    },
};

/** Encode one FieldDefinition. */
export function encodeFieldDefinition(writer: BinaryWriter, value: FieldDefinition): void {
    encodeMemberSpace(writer, value.space);
    encodeGlobalSymbolId(writer, value.symbol);
    encodeGlobalNodeIdAny(writer, value.source);
    encodeStaticKey(writer, value.key);
    writer.writeOption(value.initializer, (value4) => {
        encodeGlobalNodeIdAny(writer, value4);
    });
    writer.writeBool(value.isOptional);
    writer.writeBool(value.isReadonly);
    writer.writeBool(value.isAbstract);
    writer.writeBool(value.isOverride);
    writer.writeOption(value.overrides, (value9) => {
        encodeGlobalSymbolId(writer, value9);
    });
}

/** Decode one FieldDefinition. */
export function decodeFieldDefinition(reader: BinaryReader): FieldDefinition {
    const space = decodeMemberSpace(reader);
    const symbol_ = decodeGlobalSymbolId(reader);
    const source = decodeGlobalNodeIdAny(reader);
    const key = decodeStaticKey(reader);
    const initializer = reader.readOption(() => decodeGlobalNodeIdAny(reader));
    const isOptional = reader.readBool();
    const isReadonly = reader.readBool();
    const isAbstract = reader.readBool();
    const isOverride = reader.readBool();
    const overrides = reader.readOption(() => decodeGlobalSymbolId(reader));

    return {
        space,
        symbol: symbol_,
        source,
        key,
        ...(initializer === undefined ? {} : { initializer }),
        isOptional,
        isReadonly,
        isAbstract,
        isOverride,
        ...(overrides === undefined ? {} : { overrides }),
    };
}

/** Return one JSON value for one FieldDefinition. */
export function toJsonFieldDefinition(value: FieldDefinition): Json {
    return {
        space: toJsonMemberSpace(value.space),
        symbol: toJsonGlobalSymbolId(value.symbol),
        source: toJsonGlobalNodeIdAny(value.source),
        key: toJsonStaticKey(value.key),
        ...(value.initializer === undefined ? {} : { initializer: toJsonGlobalNodeIdAny(value.initializer) }),
        isOptional: value.isOptional,
        isReadonly: value.isReadonly,
        isAbstract: value.isAbstract,
        isOverride: value.isOverride,
        ...(value.overrides === undefined ? {} : { overrides: toJsonGlobalSymbolId(value.overrides) }),
    };
}

/** Return one FieldDefinition from one JSON value. */
export function fromJsonFieldDefinition(value: Json): FieldDefinition {
    const object = jsonObject(value);

    return {
        space: fromJsonMemberSpace(jsonField(object, "space")),
        symbol: fromJsonGlobalSymbolId(jsonField(object, "symbol")),
        source: fromJsonGlobalNodeIdAny(jsonField(object, "source")),
        key: fromJsonStaticKey(jsonField(object, "key")),
        initializer: jsonOptional(object, "initializer", (value) => fromJsonGlobalNodeIdAny(value)),
        isOptional: jsonBool(jsonField(object, "isOptional")),
        isReadonly: jsonBool(jsonField(object, "isReadonly")),
        isAbstract: jsonBool(jsonField(object, "isAbstract")),
        isOverride: jsonBool(jsonField(object, "isOverride")),
        overrides: jsonOptional(object, "overrides", (value) => fromJsonGlobalSymbolId(value)),
    };
}

/** One structural index signature member. */
export type IndexSignatureDefinition = {
    /** The source member node. */
    readonly source: GlobalNodeIdAny;
    /** The parameter name like `key`. */
    readonly name: StringId;
    /** The declared key domain. */
    readonly keyType: GlobalTypeId;
    /** The declared value type. */
    readonly valueType: GlobalTypeId;
    /** Whether the index signature may omit a value. */
    readonly isOptional: boolean;
    /** Whether the index signature rejects writes. */
    readonly isReadonly: boolean;
};

export const IndexSignatureDefinition = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: IndexSignatureDefinition): void {
        encodeIndexSignatureDefinition(writer, value);
    },

    /** Decode one IndexSignatureDefinition. */
    decode(reader: BinaryReader): IndexSignatureDefinition {
        return decodeIndexSignatureDefinition(reader);
    },

    /** Return this value as JSON. */
    toJson(value: IndexSignatureDefinition): Json {
        return toJsonIndexSignatureDefinition(value);
    },

    /** Return one IndexSignatureDefinition from one JSON value. */
    fromJson(value: Json): IndexSignatureDefinition {
        return fromJsonIndexSignatureDefinition(value);
    },
};

/** Encode one IndexSignatureDefinition. */
export function encodeIndexSignatureDefinition(writer: BinaryWriter, value: IndexSignatureDefinition): void {
    encodeGlobalNodeIdAny(writer, value.source);
    encodeStringId(writer, value.name);
    encodeGlobalTypeId(writer, value.keyType);
    encodeGlobalTypeId(writer, value.valueType);
    writer.writeBool(value.isOptional);
    writer.writeBool(value.isReadonly);
}

/** Decode one IndexSignatureDefinition. */
export function decodeIndexSignatureDefinition(reader: BinaryReader): IndexSignatureDefinition {
    const source = decodeGlobalNodeIdAny(reader);
    const name = decodeStringId(reader);
    const keyType = decodeGlobalTypeId(reader);
    const valueType = decodeGlobalTypeId(reader);
    const isOptional = reader.readBool();
    const isReadonly = reader.readBool();

    return {
        source,
        name,
        keyType,
        valueType,
        isOptional,
        isReadonly,
    };
}

/** Return one JSON value for one IndexSignatureDefinition. */
export function toJsonIndexSignatureDefinition(value: IndexSignatureDefinition): Json {
    return {
        source: toJsonGlobalNodeIdAny(value.source),
        name: toJsonStringId(value.name),
        keyType: toJsonGlobalTypeId(value.keyType),
        valueType: toJsonGlobalTypeId(value.valueType),
        isOptional: value.isOptional,
        isReadonly: value.isReadonly,
    };
}

/** Return one IndexSignatureDefinition from one JSON value. */
export function fromJsonIndexSignatureDefinition(value: Json): IndexSignatureDefinition {
    const object = jsonObject(value);

    return {
        source: fromJsonGlobalNodeIdAny(jsonField(object, "source")),
        name: fromJsonStringId(jsonField(object, "name")),
        keyType: fromJsonGlobalTypeId(jsonField(object, "keyType")),
        valueType: fromJsonGlobalTypeId(jsonField(object, "valueType")),
        isOptional: jsonBool(jsonField(object, "isOptional")),
        isReadonly: jsonBool(jsonField(object, "isReadonly")),
    };
}

/** Checked declaration data for one nominal interface. */
export type InterfaceDefinition = {
    /** The intrinsic instance space, or relative when absent. */
    readonly space?: Space;
    /** The generic template declared by the interface. */
    readonly template?: LocalGenericTemplateId;
    /** Whether the interface has nominal identity. */
    readonly isNominal: boolean;
    /** The inherited interfaces. */
    readonly extends: ReadonlyArray<NominalHeritage>;
    /** The members in declaration order. */
    readonly members: ReadonlyArray<DefinitionMember>;
};

export const InterfaceDefinition = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: InterfaceDefinition): void {
        encodeInterfaceDefinition(writer, value);
    },

    /** Decode one InterfaceDefinition. */
    decode(reader: BinaryReader): InterfaceDefinition {
        return decodeInterfaceDefinition(reader);
    },

    /** Return this value as JSON. */
    toJson(value: InterfaceDefinition): Json {
        return toJsonInterfaceDefinition(value);
    },

    /** Return one InterfaceDefinition from one JSON value. */
    fromJson(value: Json): InterfaceDefinition {
        return fromJsonInterfaceDefinition(value);
    },
};

/** Encode one InterfaceDefinition. */
export function encodeInterfaceDefinition(writer: BinaryWriter, value: InterfaceDefinition): void {
    writer.writeOption(value.space, (value0) => {
        encodeSpace(writer, value0);
    });
    writer.writeOption(value.template, (value1) => {
        encodeLocalGenericTemplateId(writer, value1);
    });
    writer.writeBool(value.isNominal);
    writer.writeUnsigned(value.extends.length);
    for (const item3 of value.extends) {
        encodeNominalHeritage(writer, item3);
    }
    writer.writeUnsigned(value.members.length);
    for (const item4 of value.members) {
        encodeDefinitionMember(writer, item4);
    }
}

/** Decode one InterfaceDefinition. */
export function decodeInterfaceDefinition(reader: BinaryReader): InterfaceDefinition {
    const space = reader.readOption(() => decodeSpace(reader));
    const template = reader.readOption(() => decodeLocalGenericTemplateId(reader));
    const isNominal = reader.readBool();
    const extends_ = (() => { const length3 = reader.readNumber(); const items3: Array<NominalHeritage> = []; for (let index = 0; index < length3; index += 1) { items3.push(decodeNominalHeritage(reader)); } return items3; })();
    const members = (() => { const length4 = reader.readNumber(); const items4: Array<DefinitionMember> = []; for (let index = 0; index < length4; index += 1) { items4.push(decodeDefinitionMember(reader)); } return items4; })();

    return {
        ...(space === undefined ? {} : { space }),
        ...(template === undefined ? {} : { template }),
        isNominal,
        extends: extends_,
        members,
    };
}

/** Return one JSON value for one InterfaceDefinition. */
export function toJsonInterfaceDefinition(value: InterfaceDefinition): Json {
    return {
        ...(value.space === undefined ? {} : { space: toJsonSpace(value.space) }),
        ...(value.template === undefined ? {} : { template: toJsonLocalGenericTemplateId(value.template) }),
        isNominal: value.isNominal,
        extends: value.extends.map((item0) => toJsonNominalHeritage(item0)),
        members: value.members.map((item0) => toJsonDefinitionMember(item0)),
    };
}

/** Return one InterfaceDefinition from one JSON value. */
export function fromJsonInterfaceDefinition(value: Json): InterfaceDefinition {
    const object = jsonObject(value);

    return {
        space: jsonOptional(object, "space", (value) => fromJsonSpace(value)),
        template: jsonOptional(object, "template", (value) => fromJsonLocalGenericTemplateId(value)),
        isNominal: jsonBool(jsonField(object, "isNominal")),
        extends: jsonArray(jsonField(object, "extends")).map((item0) => fromJsonNominalHeritage(item0)),
        members: jsonArray(jsonField(object, "members")).map((item0) => fromJsonDefinitionMember(item0)),
    };
}

/** One member satisfying an interface requirement. */
export type MemberConformance = {
    /** The implementing member symbol. */
    readonly member: GlobalSymbolId;
    /** The required interface member symbol. */
    readonly requirement: GlobalSymbolId;
};

export const MemberConformance = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: MemberConformance): void {
        encodeMemberConformance(writer, value);
    },

    /** Decode one MemberConformance. */
    decode(reader: BinaryReader): MemberConformance {
        return decodeMemberConformance(reader);
    },

    /** Return this value as JSON. */
    toJson(value: MemberConformance): Json {
        return toJsonMemberConformance(value);
    },

    /** Return one MemberConformance from one JSON value. */
    fromJson(value: Json): MemberConformance {
        return fromJsonMemberConformance(value);
    },
};

/** Encode one MemberConformance. */
export function encodeMemberConformance(writer: BinaryWriter, value: MemberConformance): void {
    encodeGlobalSymbolId(writer, value.member);
    encodeGlobalSymbolId(writer, value.requirement);
}

/** Decode one MemberConformance. */
export function decodeMemberConformance(reader: BinaryReader): MemberConformance {
    const member = decodeGlobalSymbolId(reader);
    const requirement = decodeGlobalSymbolId(reader);

    return {
        member,
        requirement,
    };
}

/** Return one JSON value for one MemberConformance. */
export function toJsonMemberConformance(value: MemberConformance): Json {
    return {
        member: toJsonGlobalSymbolId(value.member),
        requirement: toJsonGlobalSymbolId(value.requirement),
    };
}

/** Return one MemberConformance from one JSON value. */
export function fromJsonMemberConformance(value: Json): MemberConformance {
    const object = jsonObject(value);

    return {
        member: fromJsonGlobalSymbolId(jsonField(object, "member")),
        requirement: fromJsonGlobalSymbolId(jsonField(object, "requirement")),
    };
}

/** One exact member implementation edge. */
export type MemberImplementation = {
    /** The declared member requirement. */
    readonly declaration: GlobalSymbolId;
    /** The member satisfying the declaration. */
    readonly implementation: GlobalSymbolId;
};

export const MemberImplementation = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: MemberImplementation): void {
        encodeMemberImplementation(writer, value);
    },

    /** Decode one MemberImplementation. */
    decode(reader: BinaryReader): MemberImplementation {
        return decodeMemberImplementation(reader);
    },

    /** Return this value as JSON. */
    toJson(value: MemberImplementation): Json {
        return toJsonMemberImplementation(value);
    },

    /** Return one MemberImplementation from one JSON value. */
    fromJson(value: Json): MemberImplementation {
        return fromJsonMemberImplementation(value);
    },
};

/** Encode one MemberImplementation. */
export function encodeMemberImplementation(writer: BinaryWriter, value: MemberImplementation): void {
    encodeGlobalSymbolId(writer, value.declaration);
    encodeGlobalSymbolId(writer, value.implementation);
}

/** Decode one MemberImplementation. */
export function decodeMemberImplementation(reader: BinaryReader): MemberImplementation {
    const declaration = decodeGlobalSymbolId(reader);
    const implementation = decodeGlobalSymbolId(reader);

    return {
        declaration,
        implementation,
    };
}

/** Return one JSON value for one MemberImplementation. */
export function toJsonMemberImplementation(value: MemberImplementation): Json {
    return {
        declaration: toJsonGlobalSymbolId(value.declaration),
        implementation: toJsonGlobalSymbolId(value.implementation),
    };
}

/** Return one MemberImplementation from one JSON value. */
export function fromJsonMemberImplementation(value: Json): MemberImplementation {
    const object = jsonObject(value);

    return {
        declaration: fromJsonGlobalSymbolId(jsonField(object, "declaration")),
        implementation: fromJsonGlobalSymbolId(jsonField(object, "implementation")),
    };
}

/** One method member. */
export type MethodDefinition = {
    /** The member space declaring the method. */
    readonly space: MemberSpace;
    /** The method symbol. */
    readonly symbol: GlobalSymbolId;
    /** The source member node. */
    readonly source: GlobalNodeIdAny;
    /** The nominal member slot. */
    readonly slot: MemberSlot;
    /** The method role. */
    readonly role?: FunctionRole;
    /** The abstraction mode governing overrides. */
    readonly abstraction: MethodAbstraction;
    /** Whether the method overrides an inherited member. */
    readonly isOverride: boolean;
    /** The overridden base member, selected while checking. */
    readonly overrides?: GlobalSymbolId;
    /** How the method receives its implementation. */
    readonly implementation: MethodImplementation;
};

export const MethodDefinition = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: MethodDefinition): void {
        encodeMethodDefinition(writer, value);
    },

    /** Decode one MethodDefinition. */
    decode(reader: BinaryReader): MethodDefinition {
        return decodeMethodDefinition(reader);
    },

    /** Return this value as JSON. */
    toJson(value: MethodDefinition): Json {
        return toJsonMethodDefinition(value);
    },

    /** Return one MethodDefinition from one JSON value. */
    fromJson(value: Json): MethodDefinition {
        return fromJsonMethodDefinition(value);
    },
};

/** Encode one MethodDefinition. */
export function encodeMethodDefinition(writer: BinaryWriter, value: MethodDefinition): void {
    encodeMemberSpace(writer, value.space);
    encodeGlobalSymbolId(writer, value.symbol);
    encodeGlobalNodeIdAny(writer, value.source);
    encodeMemberSlot(writer, value.slot);
    writer.writeOption(value.role, (value4) => {
        encodeFunctionRole(writer, value4);
    });
    encodeMethodAbstraction(writer, value.abstraction);
    writer.writeBool(value.isOverride);
    writer.writeOption(value.overrides, (value7) => {
        encodeGlobalSymbolId(writer, value7);
    });
    encodeMethodImplementation(writer, value.implementation);
}

/** Decode one MethodDefinition. */
export function decodeMethodDefinition(reader: BinaryReader): MethodDefinition {
    const space = decodeMemberSpace(reader);
    const symbol_ = decodeGlobalSymbolId(reader);
    const source = decodeGlobalNodeIdAny(reader);
    const slot = decodeMemberSlot(reader);
    const role = reader.readOption(() => decodeFunctionRole(reader));
    const abstraction = decodeMethodAbstraction(reader);
    const isOverride = reader.readBool();
    const overrides = reader.readOption(() => decodeGlobalSymbolId(reader));
    const implementation = decodeMethodImplementation(reader);

    return {
        space,
        symbol: symbol_,
        source,
        slot,
        ...(role === undefined ? {} : { role }),
        abstraction,
        isOverride,
        ...(overrides === undefined ? {} : { overrides }),
        implementation,
    };
}

/** Return one JSON value for one MethodDefinition. */
export function toJsonMethodDefinition(value: MethodDefinition): Json {
    return {
        space: toJsonMemberSpace(value.space),
        symbol: toJsonGlobalSymbolId(value.symbol),
        source: toJsonGlobalNodeIdAny(value.source),
        slot: toJsonMemberSlot(value.slot),
        ...(value.role === undefined ? {} : { role: toJsonFunctionRole(value.role) }),
        abstraction: toJsonMethodAbstraction(value.abstraction),
        isOverride: value.isOverride,
        ...(value.overrides === undefined ? {} : { overrides: toJsonGlobalSymbolId(value.overrides) }),
        implementation: toJsonMethodImplementation(value.implementation),
    };
}

/** Return one MethodDefinition from one JSON value. */
export function fromJsonMethodDefinition(value: Json): MethodDefinition {
    const object = jsonObject(value);

    return {
        space: fromJsonMemberSpace(jsonField(object, "space")),
        symbol: fromJsonGlobalSymbolId(jsonField(object, "symbol")),
        source: fromJsonGlobalNodeIdAny(jsonField(object, "source")),
        slot: fromJsonMemberSlot(jsonField(object, "slot")),
        role: jsonOptional(object, "role", (value) => fromJsonFunctionRole(value)),
        abstraction: fromJsonMethodAbstraction(jsonField(object, "abstraction")),
        isOverride: jsonBool(jsonField(object, "isOverride")),
        overrides: jsonOptional(object, "overrides", (value) => fromJsonGlobalSymbolId(value)),
        implementation: fromJsonMethodImplementation(jsonField(object, "implementation")),
    };
}

/** How one method receives its implementation. */
export type MethodImplementation = "required" | "body" | "default";

export const MethodImplementation = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: MethodImplementation): void {
        encodeMethodImplementation(writer, value);
    },

    /** Decode one MethodImplementation. */
    decode(reader: BinaryReader): MethodImplementation {
        return decodeMethodImplementation(reader);
    },

    /** Return this value as JSON. */
    toJson(value: MethodImplementation): Json {
        return toJsonMethodImplementation(value);
    },

    /** Return one MethodImplementation from one JSON value. */
    fromJson(value: Json): MethodImplementation {
        return fromJsonMethodImplementation(value);
    },
};

/** Encode one MethodImplementation. */
export function encodeMethodImplementation(writer: BinaryWriter, value: MethodImplementation): void {
    switch (value) {
        case "required":
            writer.writeUnsigned(0);
            return;
        case "body":
            writer.writeUnsigned(1);
            return;
        case "default":
            writer.writeUnsigned(2);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one MethodImplementation. */
export function decodeMethodImplementation(reader: BinaryReader): MethodImplementation {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "required";
        case 1:
            return "body";
        case 2:
            return "default";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one MethodImplementation. */
export function toJsonMethodImplementation(value: MethodImplementation): Json {
    return value;
}

/** Return one MethodImplementation from one JSON value. */
export function fromJsonMethodImplementation(value: Json): MethodImplementation {
    const variant = jsonString(value);

    switch (variant) {
        case "required":
            return "required";
        case "body":
            return "body";
        case "default":
            return "default";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** One constructable newtype backing alternative. */
export type NewtypeConstructor = {
    /** The reduced backing alternative selected by this constructor. */
    readonly backing: GlobalTypeId;
    /** The callable constructor type. */
    readonly ty: GlobalTypeId;
};

export const NewtypeConstructor = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: NewtypeConstructor): void {
        encodeNewtypeConstructor(writer, value);
    },

    /** Decode one NewtypeConstructor. */
    decode(reader: BinaryReader): NewtypeConstructor {
        return decodeNewtypeConstructor(reader);
    },

    /** Return this value as JSON. */
    toJson(value: NewtypeConstructor): Json {
        return toJsonNewtypeConstructor(value);
    },

    /** Return one NewtypeConstructor from one JSON value. */
    fromJson(value: Json): NewtypeConstructor {
        return fromJsonNewtypeConstructor(value);
    },
};

/** Encode one NewtypeConstructor. */
export function encodeNewtypeConstructor(writer: BinaryWriter, value: NewtypeConstructor): void {
    encodeGlobalTypeId(writer, value.backing);
    encodeGlobalTypeId(writer, value.ty);
}

/** Decode one NewtypeConstructor. */
export function decodeNewtypeConstructor(reader: BinaryReader): NewtypeConstructor {
    const backing = decodeGlobalTypeId(reader);
    const ty = decodeGlobalTypeId(reader);

    return {
        backing,
        ty,
    };
}

/** Return one JSON value for one NewtypeConstructor. */
export function toJsonNewtypeConstructor(value: NewtypeConstructor): Json {
    return {
        backing: toJsonGlobalTypeId(value.backing),
        ty: toJsonGlobalTypeId(value.ty),
    };
}

/** Return one NewtypeConstructor from one JSON value. */
export function fromJsonNewtypeConstructor(value: Json): NewtypeConstructor {
    const object = jsonObject(value);

    return {
        backing: fromJsonGlobalTypeId(jsonField(object, "backing")),
        ty: fromJsonGlobalTypeId(jsonField(object, "ty")),
    };
}

/** Checked declaration data for one nominal type alias. */
export type NewtypeDefinition = {
    /** The intrinsic instance space, or relative when absent. */
    readonly space?: Space;
    /** The generic template declared by the newtype. */
    readonly template?: LocalGenericTemplateId;
    /** The selected runtime representation. */
    readonly representation: Representation;
    /** The nominal backing type. */
    readonly backing: GlobalTypeId;
    /** The constructable backing alternatives in selection order. */
    readonly constructors: ReadonlyArray<NewtypeConstructor>;
    /** The written derive list replacing the auto set, if any. */
    readonly derives?: ReadonlyArray<AutoInterface>;
    /** The members in declaration order. */
    readonly members: ReadonlyArray<DefinitionMember>;
};

export const NewtypeDefinition = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: NewtypeDefinition): void {
        encodeNewtypeDefinition(writer, value);
    },

    /** Decode one NewtypeDefinition. */
    decode(reader: BinaryReader): NewtypeDefinition {
        return decodeNewtypeDefinition(reader);
    },

    /** Return this value as JSON. */
    toJson(value: NewtypeDefinition): Json {
        return toJsonNewtypeDefinition(value);
    },

    /** Return one NewtypeDefinition from one JSON value. */
    fromJson(value: Json): NewtypeDefinition {
        return fromJsonNewtypeDefinition(value);
    },
};

/** Encode one NewtypeDefinition. */
export function encodeNewtypeDefinition(writer: BinaryWriter, value: NewtypeDefinition): void {
    writer.writeOption(value.space, (value0) => {
        encodeSpace(writer, value0);
    });
    writer.writeOption(value.template, (value1) => {
        encodeLocalGenericTemplateId(writer, value1);
    });
    encodeRepresentation(writer, value.representation);
    encodeGlobalTypeId(writer, value.backing);
    writer.writeUnsigned(value.constructors.length);
    for (const item4 of value.constructors) {
        encodeNewtypeConstructor(writer, item4);
    }
    writer.writeOption(value.derives, (value5) => {
        writer.writeUnsigned(value5.length);
        for (const item6 of value5) {
            encodeAutoInterface(writer, item6);
        }
    });
    writer.writeUnsigned(value.members.length);
    for (const item6 of value.members) {
        encodeDefinitionMember(writer, item6);
    }
}

/** Decode one NewtypeDefinition. */
export function decodeNewtypeDefinition(reader: BinaryReader): NewtypeDefinition {
    const space = reader.readOption(() => decodeSpace(reader));
    const template = reader.readOption(() => decodeLocalGenericTemplateId(reader));
    const representation = decodeRepresentation(reader);
    const backing = decodeGlobalTypeId(reader);
    const constructors = (() => { const length4 = reader.readNumber(); const items4: Array<NewtypeConstructor> = []; for (let index = 0; index < length4; index += 1) { items4.push(decodeNewtypeConstructor(reader)); } return items4; })();
    const derives = reader.readOption(() => (() => { const length6 = reader.readNumber(); const items6: Array<AutoInterface> = []; for (let index = 0; index < length6; index += 1) { items6.push(decodeAutoInterface(reader)); } return items6; })());
    const members = (() => { const length6 = reader.readNumber(); const items6: Array<DefinitionMember> = []; for (let index = 0; index < length6; index += 1) { items6.push(decodeDefinitionMember(reader)); } return items6; })();

    return {
        ...(space === undefined ? {} : { space }),
        ...(template === undefined ? {} : { template }),
        representation,
        backing,
        constructors,
        ...(derives === undefined ? {} : { derives }),
        members,
    };
}

/** Return one JSON value for one NewtypeDefinition. */
export function toJsonNewtypeDefinition(value: NewtypeDefinition): Json {
    return {
        ...(value.space === undefined ? {} : { space: toJsonSpace(value.space) }),
        ...(value.template === undefined ? {} : { template: toJsonLocalGenericTemplateId(value.template) }),
        representation: toJsonRepresentation(value.representation),
        backing: toJsonGlobalTypeId(value.backing),
        constructors: value.constructors.map((item0) => toJsonNewtypeConstructor(item0)),
        ...(value.derives === undefined ? {} : { derives: value.derives.map((item0) => toJsonAutoInterface(item0)) }),
        members: value.members.map((item0) => toJsonDefinitionMember(item0)),
    };
}

/** Return one NewtypeDefinition from one JSON value. */
export function fromJsonNewtypeDefinition(value: Json): NewtypeDefinition {
    const object = jsonObject(value);

    return {
        space: jsonOptional(object, "space", (value) => fromJsonSpace(value)),
        template: jsonOptional(object, "template", (value) => fromJsonLocalGenericTemplateId(value)),
        representation: fromJsonRepresentation(jsonField(object, "representation")),
        backing: fromJsonGlobalTypeId(jsonField(object, "backing")),
        constructors: jsonArray(jsonField(object, "constructors")).map((item0) => fromJsonNewtypeConstructor(item0)),
        derives: jsonOptional(object, "derives", (value) => jsonArray(value).map((item0) => fromJsonAutoInterface(item0))),
        members: jsonArray(jsonField(object, "members")).map((item0) => fromJsonDefinitionMember(item0)),
    };
}

/** One explicit interface conformance. */
export type NominalConformance = {
    /** The source `implements` node. */
    readonly source: GlobalNodeIdAny;
    /** The applied interface type. */
    readonly interface: GlobalTypeId;
    /** The members selected to satisfy interface requirements. */
    readonly members: ReadonlyArray<MemberConformance>;
};

export const NominalConformance = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: NominalConformance): void {
        encodeNominalConformance(writer, value);
    },

    /** Decode one NominalConformance. */
    decode(reader: BinaryReader): NominalConformance {
        return decodeNominalConformance(reader);
    },

    /** Return this value as JSON. */
    toJson(value: NominalConformance): Json {
        return toJsonNominalConformance(value);
    },

    /** Return one NominalConformance from one JSON value. */
    fromJson(value: Json): NominalConformance {
        return fromJsonNominalConformance(value);
    },
};

/** Encode one NominalConformance. */
export function encodeNominalConformance(writer: BinaryWriter, value: NominalConformance): void {
    encodeGlobalNodeIdAny(writer, value.source);
    encodeGlobalTypeId(writer, value.interface);
    writer.writeUnsigned(value.members.length);
    for (const item2 of value.members) {
        encodeMemberConformance(writer, item2);
    }
}

/** Decode one NominalConformance. */
export function decodeNominalConformance(reader: BinaryReader): NominalConformance {
    const source = decodeGlobalNodeIdAny(reader);
    const interface_ = decodeGlobalTypeId(reader);
    const members = (() => { const length2 = reader.readNumber(); const items2: Array<MemberConformance> = []; for (let index = 0; index < length2; index += 1) { items2.push(decodeMemberConformance(reader)); } return items2; })();

    return {
        source,
        interface: interface_,
        members,
    };
}

/** Return one JSON value for one NominalConformance. */
export function toJsonNominalConformance(value: NominalConformance): Json {
    return {
        source: toJsonGlobalNodeIdAny(value.source),
        interface: toJsonGlobalTypeId(value.interface),
        members: value.members.map((item0) => toJsonMemberConformance(item0)),
    };
}

/** Return one NominalConformance from one JSON value. */
export function fromJsonNominalConformance(value: Json): NominalConformance {
    const object = jsonObject(value);

    return {
        source: fromJsonGlobalNodeIdAny(jsonField(object, "source")),
        interface: fromJsonGlobalTypeId(jsonField(object, "interface")),
        members: jsonArray(jsonField(object, "members")).map((item0) => fromJsonMemberConformance(item0)),
    };
}

/** One nominal heritage. */
export type NominalHeritage = {
    /** The source heritage node. */
    readonly source: GlobalNodeIdAny;
    /** The applied heritage type. */
    readonly ty: GlobalTypeId;
};

export const NominalHeritage = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: NominalHeritage): void {
        encodeNominalHeritage(writer, value);
    },

    /** Decode one NominalHeritage. */
    decode(reader: BinaryReader): NominalHeritage {
        return decodeNominalHeritage(reader);
    },

    /** Return this value as JSON. */
    toJson(value: NominalHeritage): Json {
        return toJsonNominalHeritage(value);
    },

    /** Return one NominalHeritage from one JSON value. */
    fromJson(value: Json): NominalHeritage {
        return fromJsonNominalHeritage(value);
    },
};

/** Encode one NominalHeritage. */
export function encodeNominalHeritage(writer: BinaryWriter, value: NominalHeritage): void {
    encodeGlobalNodeIdAny(writer, value.source);
    encodeGlobalTypeId(writer, value.ty);
}

/** Decode one NominalHeritage. */
export function decodeNominalHeritage(reader: BinaryReader): NominalHeritage {
    const source = decodeGlobalNodeIdAny(reader);
    const ty = decodeGlobalTypeId(reader);

    return {
        source,
        ty,
    };
}

/** Return one JSON value for one NominalHeritage. */
export function toJsonNominalHeritage(value: NominalHeritage): Json {
    return {
        source: toJsonGlobalNodeIdAny(value.source),
        ty: toJsonGlobalTypeId(value.ty),
    };
}

/** Return one NominalHeritage from one JSON value. */
export function fromJsonNominalHeritage(value: Json): NominalHeritage {
    const object = jsonObject(value);

    return {
        source: fromJsonGlobalNodeIdAny(jsonField(object, "source")),
        ty: fromJsonGlobalTypeId(jsonField(object, "ty")),
    };
}

/** Runtime representation selected for one nominal declaration. */
export type Representation = {
    /** The representation family. */
    readonly kind: RepresentationKind;
    /** The required byte alignment. */
    readonly alignment?: bigint;
    /** The maximum field alignment for packed layouts. */
    readonly packing?: bigint;
};

export const Representation = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: Representation): void {
        encodeRepresentation(writer, value);
    },

    /** Decode one Representation. */
    decode(reader: BinaryReader): Representation {
        return decodeRepresentation(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Representation): Json {
        return toJsonRepresentation(value);
    },

    /** Return one Representation from one JSON value. */
    fromJson(value: Json): Representation {
        return fromJsonRepresentation(value);
    },
};

/** Encode one Representation. */
export function encodeRepresentation(writer: BinaryWriter, value: Representation): void {
    encodeRepresentationKind(writer, value.kind);
    writer.writeOption(value.alignment, (value1) => {
        writer.writeUnsigned(value1);
    });
    writer.writeOption(value.packing, (value2) => {
        writer.writeUnsigned(value2);
    });
}

/** Decode one Representation. */
export function decodeRepresentation(reader: BinaryReader): Representation {
    const kind = decodeRepresentationKind(reader);
    const alignment = reader.readOption(() => reader.readUnsigned());
    const packing = reader.readOption(() => reader.readUnsigned());

    return {
        kind,
        ...(alignment === undefined ? {} : { alignment }),
        ...(packing === undefined ? {} : { packing }),
    };
}

/** Return one JSON value for one Representation. */
export function toJsonRepresentation(value: Representation): Json {
    return {
        kind: toJsonRepresentationKind(value.kind),
        ...(value.alignment === undefined ? {} : { alignment: value.alignment.toString() }),
        ...(value.packing === undefined ? {} : { packing: value.packing.toString() }),
    };
}

/** Return one Representation from one JSON value. */
export function fromJsonRepresentation(value: Json): Representation {
    const object = jsonObject(value);

    return {
        kind: fromJsonRepresentationKind(jsonField(object, "kind")),
        alignment: jsonOptional(object, "alignment", (value) => jsonBigint(value)),
        packing: jsonOptional(object, "packing", (value) => jsonBigint(value)),
    };
}

/** Runtime representation family for one nominal declaration. */
export type RepresentationKind =
    /** The native Destack layout. */
    | {
          readonly kind: "destack";
      }
    /** The target C ABI layout. */
    | {
          readonly kind: "c";
      }
    /** The single-field backing layout without a wrapper. */
    | {
          readonly kind: "transparent";
      }
    /** An explicit integer scalar representation. */
    | {
          readonly kind: "integer";
          readonly integer: IntegerType;
      }
;

export const RepresentationKind = {
    /** The native Destack layout. */
    destack(): RepresentationKind {
        return { kind: "destack" };
    },

    /** The target C ABI layout. */
    c(): RepresentationKind {
        return { kind: "c" };
    },

    /** The single-field backing layout without a wrapper. */
    transparent(): RepresentationKind {
        return { kind: "transparent" };
    },

    /** An explicit integer scalar representation. */
    integer(integer: IntegerType): RepresentationKind {
        return { kind: "integer", integer };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: RepresentationKind): void {
        encodeRepresentationKind(writer, value);
    },

    /** Decode one RepresentationKind. */
    decode(reader: BinaryReader): RepresentationKind {
        return decodeRepresentationKind(reader);
    },

    /** Return this value as JSON. */
    toJson(value: RepresentationKind): Json {
        return toJsonRepresentationKind(value);
    },

    /** Return one RepresentationKind from one JSON value. */
    fromJson(value: Json): RepresentationKind {
        return fromJsonRepresentationKind(value);
    },
};

/** Encode one RepresentationKind. */
export function encodeRepresentationKind(writer: BinaryWriter, value: RepresentationKind): void {
    switch (value.kind) {
        case "destack":
            writer.writeUnsigned(0);
            return;
        case "c":
            writer.writeUnsigned(1);
            return;
        case "transparent":
            writer.writeUnsigned(2);
            return;
        case "integer":
            writer.writeUnsigned(3);
            encodeIntegerType(writer, value.integer);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one RepresentationKind. */
export function decodeRepresentationKind(reader: BinaryReader): RepresentationKind {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            return { kind: "destack" };
        }
        case 1: {
            return { kind: "c" };
        }
        case 2: {
            return { kind: "transparent" };
        }
        case 3: {
            const integer = decodeIntegerType(reader);

            return { kind: "integer", integer };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one RepresentationKind. */
export function toJsonRepresentationKind(value: RepresentationKind): Json {
    switch (value.kind) {
        case "destack":
            return {
                kind: "destack",
            };
        case "c":
            return {
                kind: "c",
            };
        case "transparent":
            return {
                kind: "transparent",
            };
        case "integer":
            return {
                kind: "integer",
                integer: toJsonIntegerType(value.integer),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one RepresentationKind from one JSON value. */
export function fromJsonRepresentationKind(value: Json): RepresentationKind {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "destack":
            return {
                kind,
            };
        case "c":
            return {
                kind,
            };
        case "transparent":
            return {
                kind,
            };
        case "integer":
            return {
                kind,
                integer: fromJsonIntegerType(jsonField(object, "integer")),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** One symbol-free signature member. */
export type SignatureDefinition = {
    /** The source member node. */
    readonly source: GlobalNodeIdAny;
    /** The checked signature type. */
    readonly ty: GlobalTypeId;
};

export const SignatureDefinition = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: SignatureDefinition): void {
        encodeSignatureDefinition(writer, value);
    },

    /** Decode one SignatureDefinition. */
    decode(reader: BinaryReader): SignatureDefinition {
        return decodeSignatureDefinition(reader);
    },

    /** Return this value as JSON. */
    toJson(value: SignatureDefinition): Json {
        return toJsonSignatureDefinition(value);
    },

    /** Return one SignatureDefinition from one JSON value. */
    fromJson(value: Json): SignatureDefinition {
        return fromJsonSignatureDefinition(value);
    },
};

/** Encode one SignatureDefinition. */
export function encodeSignatureDefinition(writer: BinaryWriter, value: SignatureDefinition): void {
    encodeGlobalNodeIdAny(writer, value.source);
    encodeGlobalTypeId(writer, value.ty);
}

/** Decode one SignatureDefinition. */
export function decodeSignatureDefinition(reader: BinaryReader): SignatureDefinition {
    const source = decodeGlobalNodeIdAny(reader);
    const ty = decodeGlobalTypeId(reader);

    return {
        source,
        ty,
    };
}

/** Return one JSON value for one SignatureDefinition. */
export function toJsonSignatureDefinition(value: SignatureDefinition): Json {
    return {
        source: toJsonGlobalNodeIdAny(value.source),
        ty: toJsonGlobalTypeId(value.ty),
    };
}

/** Return one SignatureDefinition from one JSON value. */
export function fromJsonSignatureDefinition(value: Json): SignatureDefinition {
    const object = jsonObject(value);

    return {
        source: fromJsonGlobalNodeIdAny(jsonField(object, "source")),
        ty: fromJsonGlobalTypeId(jsonField(object, "ty")),
    };
}

/** Checked declaration data for one nominal struct. */
export type StructDefinition = {
    /** The intrinsic instance space, or relative when absent. */
    readonly space?: Space;
    /** The generic template declared by the struct. */
    readonly template?: LocalGenericTemplateId;
    /** The selected runtime representation. */
    readonly representation: Representation;
    /** The implemented interfaces. */
    readonly implements: ReadonlyArray<NominalConformance>;
    /** The written derive list replacing the auto set, if any. */
    readonly derives?: ReadonlyArray<AutoInterface>;
    /** The members in declaration order. */
    readonly members: ReadonlyArray<DefinitionMember>;
};

export const StructDefinition = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: StructDefinition): void {
        encodeStructDefinition(writer, value);
    },

    /** Decode one StructDefinition. */
    decode(reader: BinaryReader): StructDefinition {
        return decodeStructDefinition(reader);
    },

    /** Return this value as JSON. */
    toJson(value: StructDefinition): Json {
        return toJsonStructDefinition(value);
    },

    /** Return one StructDefinition from one JSON value. */
    fromJson(value: Json): StructDefinition {
        return fromJsonStructDefinition(value);
    },
};

/** Encode one StructDefinition. */
export function encodeStructDefinition(writer: BinaryWriter, value: StructDefinition): void {
    writer.writeOption(value.space, (value0) => {
        encodeSpace(writer, value0);
    });
    writer.writeOption(value.template, (value1) => {
        encodeLocalGenericTemplateId(writer, value1);
    });
    encodeRepresentation(writer, value.representation);
    writer.writeUnsigned(value.implements.length);
    for (const item3 of value.implements) {
        encodeNominalConformance(writer, item3);
    }
    writer.writeOption(value.derives, (value4) => {
        writer.writeUnsigned(value4.length);
        for (const item5 of value4) {
            encodeAutoInterface(writer, item5);
        }
    });
    writer.writeUnsigned(value.members.length);
    for (const item5 of value.members) {
        encodeDefinitionMember(writer, item5);
    }
}

/** Decode one StructDefinition. */
export function decodeStructDefinition(reader: BinaryReader): StructDefinition {
    const space = reader.readOption(() => decodeSpace(reader));
    const template = reader.readOption(() => decodeLocalGenericTemplateId(reader));
    const representation = decodeRepresentation(reader);
    const implements_ = (() => { const length3 = reader.readNumber(); const items3: Array<NominalConformance> = []; for (let index = 0; index < length3; index += 1) { items3.push(decodeNominalConformance(reader)); } return items3; })();
    const derives = reader.readOption(() => (() => { const length5 = reader.readNumber(); const items5: Array<AutoInterface> = []; for (let index = 0; index < length5; index += 1) { items5.push(decodeAutoInterface(reader)); } return items5; })());
    const members = (() => { const length5 = reader.readNumber(); const items5: Array<DefinitionMember> = []; for (let index = 0; index < length5; index += 1) { items5.push(decodeDefinitionMember(reader)); } return items5; })();

    return {
        ...(space === undefined ? {} : { space }),
        ...(template === undefined ? {} : { template }),
        representation,
        implements: implements_,
        ...(derives === undefined ? {} : { derives }),
        members,
    };
}

/** Return one JSON value for one StructDefinition. */
export function toJsonStructDefinition(value: StructDefinition): Json {
    return {
        ...(value.space === undefined ? {} : { space: toJsonSpace(value.space) }),
        ...(value.template === undefined ? {} : { template: toJsonLocalGenericTemplateId(value.template) }),
        representation: toJsonRepresentation(value.representation),
        implements: value.implements.map((item0) => toJsonNominalConformance(item0)),
        ...(value.derives === undefined ? {} : { derives: value.derives.map((item0) => toJsonAutoInterface(item0)) }),
        members: value.members.map((item0) => toJsonDefinitionMember(item0)),
    };
}

/** Return one StructDefinition from one JSON value. */
export function fromJsonStructDefinition(value: Json): StructDefinition {
    const object = jsonObject(value);

    return {
        space: jsonOptional(object, "space", (value) => fromJsonSpace(value)),
        template: jsonOptional(object, "template", (value) => fromJsonLocalGenericTemplateId(value)),
        representation: fromJsonRepresentation(jsonField(object, "representation")),
        implements: jsonArray(jsonField(object, "implements")).map((item0) => fromJsonNominalConformance(item0)),
        derives: jsonOptional(object, "derives", (value) => jsonArray(value).map((item0) => fromJsonAutoInterface(item0))),
        members: jsonArray(jsonField(object, "members")).map((item0) => fromJsonDefinitionMember(item0)),
    };
}

/** Checked declaration data for one transparent type alias. */
export type TypeAliasDefinition = {
    /** The generic template declared by the alias. */
    readonly template?: LocalGenericTemplateId;
    /** The checked alias value. */
    readonly value: GlobalTypeId;
};

export const TypeAliasDefinition = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: TypeAliasDefinition): void {
        encodeTypeAliasDefinition(writer, value);
    },

    /** Decode one TypeAliasDefinition. */
    decode(reader: BinaryReader): TypeAliasDefinition {
        return decodeTypeAliasDefinition(reader);
    },

    /** Return this value as JSON. */
    toJson(value: TypeAliasDefinition): Json {
        return toJsonTypeAliasDefinition(value);
    },

    /** Return one TypeAliasDefinition from one JSON value. */
    fromJson(value: Json): TypeAliasDefinition {
        return fromJsonTypeAliasDefinition(value);
    },
};

/** Encode one TypeAliasDefinition. */
export function encodeTypeAliasDefinition(writer: BinaryWriter, value: TypeAliasDefinition): void {
    writer.writeOption(value.template, (value0) => {
        encodeLocalGenericTemplateId(writer, value0);
    });
    encodeGlobalTypeId(writer, value.value);
}

/** Decode one TypeAliasDefinition. */
export function decodeTypeAliasDefinition(reader: BinaryReader): TypeAliasDefinition {
    const template = reader.readOption(() => decodeLocalGenericTemplateId(reader));
    const value = decodeGlobalTypeId(reader);

    return {
        ...(template === undefined ? {} : { template }),
        value,
    };
}

/** Return one JSON value for one TypeAliasDefinition. */
export function toJsonTypeAliasDefinition(value: TypeAliasDefinition): Json {
    return {
        ...(value.template === undefined ? {} : { template: toJsonLocalGenericTemplateId(value.template) }),
        value: toJsonGlobalTypeId(value.value),
    };
}

/** Return one TypeAliasDefinition from one JSON value. */
export function fromJsonTypeAliasDefinition(value: Json): TypeAliasDefinition {
    const object = jsonObject(value);

    return {
        template: jsonOptional(object, "template", (value) => fromJsonLocalGenericTemplateId(value)),
        value: fromJsonGlobalTypeId(jsonField(object, "value")),
    };
}

/** One indexable receiver root, keying extension lookup and conflicts. */
export type TypeRoot =
    /** A declared nominal root. */
    | {
          readonly kind: "declaration";
          readonly declaration: GlobalSymbolId;
      }
    /** A builtin primitive root. */
    | {
          readonly kind: "primitive";
          readonly primitive: PrimitiveType;
      }
    /** The tuple constructor root. */
    | {
          readonly kind: "tuple";
      }
;

export const TypeRoot = {
    /** A declared nominal root. */
    declaration(declaration: GlobalSymbolId): TypeRoot {
        return { kind: "declaration", declaration };
    },

    /** A builtin primitive root. */
    primitive(primitive: PrimitiveType): TypeRoot {
        return { kind: "primitive", primitive };
    },

    /** The tuple constructor root. */
    tuple(): TypeRoot {
        return { kind: "tuple" };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: TypeRoot): void {
        encodeTypeRoot(writer, value);
    },

    /** Decode one TypeRoot. */
    decode(reader: BinaryReader): TypeRoot {
        return decodeTypeRoot(reader);
    },

    /** Return this value as JSON. */
    toJson(value: TypeRoot): Json {
        return toJsonTypeRoot(value);
    },

    /** Return one TypeRoot from one JSON value. */
    fromJson(value: Json): TypeRoot {
        return fromJsonTypeRoot(value);
    },
};

/** Encode one TypeRoot. */
export function encodeTypeRoot(writer: BinaryWriter, value: TypeRoot): void {
    switch (value.kind) {
        case "declaration":
            writer.writeUnsigned(0);
            encodeGlobalSymbolId(writer, value.declaration);
            return;
        case "primitive":
            writer.writeUnsigned(1);
            encodePrimitiveType(writer, value.primitive);
            return;
        case "tuple":
            writer.writeUnsigned(2);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one TypeRoot. */
export function decodeTypeRoot(reader: BinaryReader): TypeRoot {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const declaration = decodeGlobalSymbolId(reader);

            return { kind: "declaration", declaration };
        }
        case 1: {
            const primitive = decodePrimitiveType(reader);

            return { kind: "primitive", primitive };
        }
        case 2: {
            return { kind: "tuple" };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one TypeRoot. */
export function toJsonTypeRoot(value: TypeRoot): Json {
    switch (value.kind) {
        case "declaration":
            return {
                kind: "declaration",
                declaration: toJsonGlobalSymbolId(value.declaration),
            };
        case "primitive":
            return {
                kind: "primitive",
                primitive: toJsonPrimitiveType(value.primitive),
            };
        case "tuple":
            return {
                kind: "tuple",
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one TypeRoot from one JSON value. */
export function fromJsonTypeRoot(value: Json): TypeRoot {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "declaration":
            return {
                kind,
                declaration: fromJsonGlobalSymbolId(jsonField(object, "declaration")),
            };
        case "primitive":
            return {
                kind,
                primitive: fromJsonPrimitiveType(jsonField(object, "primitive")),
            };
        case "tuple":
            return {
                kind,
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}
