/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, compareBytes, jsonArray, jsonBool, jsonField, jsonObject, jsonOptional, jsonString, nestedBytes } from "../../../protocol/serde.js";
import type { StaticKey } from "../symbol/key.js";
import type { GlobalSymbolId } from "../symbol/symbol.js";
import type { GlobalNodeIdAny } from "../tree/node.js";
import type { FunctionRole } from "../tree/property.js";
import type { MemberSlot } from "../tree/property.js";
import type { MethodAbstraction } from "../tree/property.js";
import type { GlobalStaticId } from "../tree/static.js";
import type { Extension } from "../type/extension.js";
import type { LocalGenericTemplateId } from "../type/generic.js";
import type { GlobalTypeId } from "../type/type.js";
import type { ModuleId } from "../../source/file/model/module.js";
import { decodeStaticKey, encodeStaticKey, fromJsonStaticKey, toJsonStaticKey } from "../symbol/key.js";
import { decodeGlobalSymbolId, encodeGlobalSymbolId, fromJsonGlobalSymbolId, toJsonGlobalSymbolId } from "../symbol/symbol.js";
import { decodeGlobalNodeIdAny, encodeGlobalNodeIdAny, fromJsonGlobalNodeIdAny, toJsonGlobalNodeIdAny } from "../tree/node.js";
import { decodeFunctionRole, encodeFunctionRole, fromJsonFunctionRole, toJsonFunctionRole } from "../tree/property.js";
import { decodeMemberSlot, encodeMemberSlot, fromJsonMemberSlot, toJsonMemberSlot } from "../tree/property.js";
import { decodeMethodAbstraction, encodeMethodAbstraction, fromJsonMethodAbstraction, toJsonMethodAbstraction } from "../tree/property.js";
import { decodeGlobalStaticId, encodeGlobalStaticId, fromJsonGlobalStaticId, toJsonGlobalStaticId } from "../tree/static.js";
import { decodeExtension, encodeExtension, fromJsonExtension, toJsonExtension } from "../type/extension.js";
import { decodeLocalGenericTemplateId, encodeLocalGenericTemplateId, fromJsonLocalGenericTemplateId, toJsonLocalGenericTemplateId } from "../type/generic.js";
import { decodeGlobalTypeId, encodeGlobalTypeId, fromJsonGlobalTypeId, toJsonGlobalTypeId } from "../type/type.js";
import { decodeModuleId, encodeModuleId, fromJsonModuleId, toJsonModuleId } from "../../source/file/model/module.js";

/** Declaration definitions added by one DIR phase. */
export type DefinitionSegment = {
    /** The module id of the definition segment. */
    readonly moduleId: ModuleId;
    /** Source declaration nodes keyed by declaring symbol. */
    readonly sources: ReadonlyMap<GlobalSymbolId, GlobalNodeIdAny>;
    /** Definitions keyed by declaring symbol. */
    readonly definitions: ReadonlyMap<GlobalSymbolId, Definition>;
    /** Extension symbols by target symbol. */
    readonly extensionsByTargetSymbol: ReadonlyMap<GlobalSymbolId, ReadonlyArray<GlobalSymbolId>>;
    /** Blanket extension symbols. */
    readonly blanketExtensions: ReadonlyArray<GlobalSymbolId>;
};

export const DefinitionSegment = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: DefinitionSegment): void {
        encodeDefinitionSegment(writer, value);
    },

    /** Decode one DefinitionSegment. */
    decode(reader: BinaryReader): DefinitionSegment {
        return decodeDefinitionSegment(reader);
    },

    /** Return this value as JSON. */
    toJson(value: DefinitionSegment): Json {
        return toJsonDefinitionSegment(value);
    },

    /** Return one DefinitionSegment from one JSON value. */
    fromJson(value: Json): DefinitionSegment {
        return fromJsonDefinitionSegment(value);
    },
};

/** Encode one DefinitionSegment. */
export function encodeDefinitionSegment(writer: BinaryWriter, value: DefinitionSegment): void {
    encodeModuleId(writer, value.moduleId);
    const entries1 = Array.from(value.sources.entries()).map(([key1, item1]) => {
        const keyBytes = nestedBytes((writer) => {
            encodeGlobalSymbolId(writer, key1);
        });
        return { key1, item1, keyBytes };
    });
    entries1.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries1.length);
    for (const entry1 of entries1) {
        encodeGlobalSymbolId(writer, entry1.key1);
        encodeGlobalNodeIdAny(writer, entry1.item1);
    }
    const entries2 = Array.from(value.definitions.entries()).map(([key2, item2]) => {
        const keyBytes = nestedBytes((writer) => {
            encodeGlobalSymbolId(writer, key2);
        });
        return { key2, item2, keyBytes };
    });
    entries2.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries2.length);
    for (const entry2 of entries2) {
        encodeGlobalSymbolId(writer, entry2.key2);
        encodeDefinition(writer, entry2.item2);
    }
    const entries3 = Array.from(value.extensionsByTargetSymbol.entries()).map(([key3, item3]) => {
        const keyBytes = nestedBytes((writer) => {
            encodeGlobalSymbolId(writer, key3);
        });
        return { key3, item3, keyBytes };
    });
    entries3.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries3.length);
    for (const entry3 of entries3) {
        encodeGlobalSymbolId(writer, entry3.key3);
        writer.writeUnsigned(entry3.item3.length);
        for (const item4 of entry3.item3) {
            encodeGlobalSymbolId(writer, item4);
        }
    }
    writer.writeUnsigned(value.blanketExtensions.length);
    for (const item4 of value.blanketExtensions) {
        encodeGlobalSymbolId(writer, item4);
    }
}

/** Decode one DefinitionSegment. */
export function decodeDefinitionSegment(reader: BinaryReader): DefinitionSegment {
    const moduleId = decodeModuleId(reader);
    const sources = (() => { const length1 = reader.readNumber(); const items1 = new Map<GlobalSymbolId, GlobalNodeIdAny>(); for (let index = 0; index < length1; index += 1) { items1.set(decodeGlobalSymbolId(reader), decodeGlobalNodeIdAny(reader)); } return items1; })();
    const definitions = (() => { const length2 = reader.readNumber(); const items2 = new Map<GlobalSymbolId, Definition>(); for (let index = 0; index < length2; index += 1) { items2.set(decodeGlobalSymbolId(reader), decodeDefinition(reader)); } return items2; })();
    const extensionsByTargetSymbol = (() => { const length3 = reader.readNumber(); const items3 = new Map<GlobalSymbolId, ReadonlyArray<GlobalSymbolId>>(); for (let index = 0; index < length3; index += 1) { items3.set(decodeGlobalSymbolId(reader), (() => { const length5 = reader.readNumber(); const items5: Array<GlobalSymbolId> = []; for (let index = 0; index < length5; index += 1) { items5.push(decodeGlobalSymbolId(reader)); } return items5; })()); } return items3; })();
    const blanketExtensions = (() => { const length4 = reader.readNumber(); const items4: Array<GlobalSymbolId> = []; for (let index = 0; index < length4; index += 1) { items4.push(decodeGlobalSymbolId(reader)); } return items4; })();

    return {
        moduleId,
        sources,
        definitions,
        extensionsByTargetSymbol,
        blanketExtensions,
    };
}

/** Return one JSON value for one DefinitionSegment. */
export function toJsonDefinitionSegment(value: DefinitionSegment): Json {
    return {
        moduleId: toJsonModuleId(value.moduleId),
        sources: Array.from(value.sources.entries()).map(([key0, item0]) => [toJsonGlobalSymbolId(key0), toJsonGlobalNodeIdAny(item0)] as const),
        definitions: Array.from(value.definitions.entries()).map(([key0, item0]) => [toJsonGlobalSymbolId(key0), toJsonDefinition(item0)] as const),
        extensionsByTargetSymbol: Array.from(value.extensionsByTargetSymbol.entries()).map(([key0, item0]) => [toJsonGlobalSymbolId(key0), item0.map((item1) => toJsonGlobalSymbolId(item1))] as const),
        blanketExtensions: value.blanketExtensions.map((item0) => toJsonGlobalSymbolId(item0)),
    };
}

/** Return one DefinitionSegment from one JSON value. */
export function fromJsonDefinitionSegment(value: Json): DefinitionSegment {
    const object = jsonObject(value);

    return {
        moduleId: fromJsonModuleId(jsonField(object, "moduleId")),
        sources: new Map(jsonArray(jsonField(object, "sources")).map((entry) => { const items = jsonArray(entry); if (items.length !== 2) { throw new SerdeError(`expected JSON map entry length 2: ${items.length}`); } const key0 = items[0]; const item0 = items[1]; return [fromJsonGlobalSymbolId(key0), fromJsonGlobalNodeIdAny(item0)] as const; })),
        definitions: new Map(jsonArray(jsonField(object, "definitions")).map((entry) => { const items = jsonArray(entry); if (items.length !== 2) { throw new SerdeError(`expected JSON map entry length 2: ${items.length}`); } const key0 = items[0]; const item0 = items[1]; return [fromJsonGlobalSymbolId(key0), fromJsonDefinition(item0)] as const; })),
        extensionsByTargetSymbol: new Map(jsonArray(jsonField(object, "extensionsByTargetSymbol")).map((entry) => { const items = jsonArray(entry); if (items.length !== 2) { throw new SerdeError(`expected JSON map entry length 2: ${items.length}`); } const key0 = items[0]; const item0 = items[1]; return [fromJsonGlobalSymbolId(key0), jsonArray(item0).map((item1) => fromJsonGlobalSymbolId(item1))] as const; })),
        blanketExtensions: jsonArray(jsonField(object, "blanketExtensions")).map((item0) => fromJsonGlobalSymbolId(item0)),
    };
}

/** Checked declaration data for one symbol. */
export type Definition =
    /** Transparent type alias declaration. */
    | {
          readonly kind: "typeAlias";
          readonly type_alias: TypeAliasDefinition;
      }
    /** Struct declaration. */
    | {
          readonly kind: "struct";
          readonly struct: StructDefinition;
      }
    /** Class declaration. */
    | {
          readonly kind: "class";
          readonly class: ClassDefinition;
      }
    /** Nominal interface declaration. */
    | {
          readonly kind: "interface";
          readonly interface: InterfaceDefinition;
      }
    /** Enum declaration. */
    | {
          readonly kind: "enum";
          readonly enum: EnumDefinition;
      }
    /** Newtype declaration. */
    | {
          readonly kind: "newtype";
          readonly newtype: NewtypeDefinition;
      }
    /** Extension declaration. */
    | {
          readonly kind: "extension";
          readonly extension: Extension;
      }
;

export const Definition = {
    /** Transparent type alias declaration. */
    typeAlias(type_alias: TypeAliasDefinition): Definition {
        return { kind: "typeAlias", type_alias };
    },

    /** Struct declaration. */
    struct(struct: StructDefinition): Definition {
        return { kind: "struct", struct };
    },

    /** Class declaration. */
    "class"(class_: ClassDefinition): Definition {
        return { kind: "class", class: class_ };
    },

    /** Nominal interface declaration. */
    "interface"(interface_: InterfaceDefinition): Definition {
        return { kind: "interface", interface: interface_ };
    },

    /** Enum declaration. */
    "enum"(enum_: EnumDefinition): Definition {
        return { kind: "enum", enum: enum_ };
    },

    /** Newtype declaration. */
    newtype(newtype: NewtypeDefinition): Definition {
        return { kind: "newtype", newtype };
    },

    /** Extension declaration. */
    extension(extension: Extension): Definition {
        return { kind: "extension", extension };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: Definition): void {
        encodeDefinition(writer, value);
    },

    /** Decode one Definition. */
    decode(reader: BinaryReader): Definition {
        return decodeDefinition(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Definition): Json {
        return toJsonDefinition(value);
    },

    /** Return one Definition from one JSON value. */
    fromJson(value: Json): Definition {
        return fromJsonDefinition(value);
    },
};

/** Encode one Definition. */
export function encodeDefinition(writer: BinaryWriter, value: Definition): void {
    switch (value.kind) {
        case "typeAlias":
            writer.writeUnsigned(0);
            encodeTypeAliasDefinition(writer, value.type_alias);
            return;
        case "struct":
            writer.writeUnsigned(1);
            encodeStructDefinition(writer, value.struct);
            return;
        case "class":
            writer.writeUnsigned(2);
            encodeClassDefinition(writer, value.class);
            return;
        case "interface":
            writer.writeUnsigned(3);
            encodeInterfaceDefinition(writer, value.interface);
            return;
        case "enum":
            writer.writeUnsigned(4);
            encodeEnumDefinition(writer, value.enum);
            return;
        case "newtype":
            writer.writeUnsigned(5);
            encodeNewtypeDefinition(writer, value.newtype);
            return;
        case "extension":
            writer.writeUnsigned(6);
            encodeExtension(writer, value.extension);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one Definition. */
export function decodeDefinition(reader: BinaryReader): Definition {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const type_alias = decodeTypeAliasDefinition(reader);

            return { kind: "typeAlias", type_alias };
        }
        case 1: {
            const struct = decodeStructDefinition(reader);

            return { kind: "struct", struct };
        }
        case 2: {
            const class_ = decodeClassDefinition(reader);

            return { kind: "class", class: class_ };
        }
        case 3: {
            const interface_ = decodeInterfaceDefinition(reader);

            return { kind: "interface", interface: interface_ };
        }
        case 4: {
            const enum_ = decodeEnumDefinition(reader);

            return { kind: "enum", enum: enum_ };
        }
        case 5: {
            const newtype = decodeNewtypeDefinition(reader);

            return { kind: "newtype", newtype };
        }
        case 6: {
            const extension = decodeExtension(reader);

            return { kind: "extension", extension };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one Definition. */
export function toJsonDefinition(value: Definition): Json {
    switch (value.kind) {
        case "typeAlias":
            return {
                kind: "typeAlias",
                type_alias: toJsonTypeAliasDefinition(value.type_alias),
            };
        case "struct":
            return {
                kind: "struct",
                struct: toJsonStructDefinition(value.struct),
            };
        case "class":
            return {
                kind: "class",
                class: toJsonClassDefinition(value.class),
            };
        case "interface":
            return {
                kind: "interface",
                interface: toJsonInterfaceDefinition(value.interface),
            };
        case "enum":
            return {
                kind: "enum",
                enum: toJsonEnumDefinition(value.enum),
            };
        case "newtype":
            return {
                kind: "newtype",
                newtype: toJsonNewtypeDefinition(value.newtype),
            };
        case "extension":
            return {
                kind: "extension",
                extension: toJsonExtension(value.extension),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one Definition from one JSON value. */
export function fromJsonDefinition(value: Json): Definition {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "typeAlias":
            return {
                kind,
                type_alias: fromJsonTypeAliasDefinition(jsonField(object, "type_alias")),
            };
        case "struct":
            return {
                kind,
                struct: fromJsonStructDefinition(jsonField(object, "struct")),
            };
        case "class":
            return {
                kind,
                class: fromJsonClassDefinition(jsonField(object, "class")),
            };
        case "interface":
            return {
                kind,
                interface: fromJsonInterfaceDefinition(jsonField(object, "interface")),
            };
        case "enum":
            return {
                kind,
                enum: fromJsonEnumDefinition(jsonField(object, "enum")),
            };
        case "newtype":
            return {
                kind,
                newtype: fromJsonNewtypeDefinition(jsonField(object, "newtype")),
            };
        case "extension":
            return {
                kind,
                extension: fromJsonExtension(jsonField(object, "extension")),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** Checked declaration data for one transparent type alias. */
export type TypeAliasDefinition = {
    /** The generic template declared by the alias. */
    readonly template?: LocalGenericTemplateId;
    /** The checked alias value. */
    readonly value: GlobalTypeId;
};

export const TypeAliasDefinition = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: TypeAliasDefinition): void {
        encodeTypeAliasDefinition(writer, value);
    },

    /** Decode one TypeAliasDefinition. */
    decode(reader: BinaryReader): TypeAliasDefinition {
        return decodeTypeAliasDefinition(reader);
    },

    /** Return this value as JSON. */
    toJson(value: TypeAliasDefinition): Json {
        return toJsonTypeAliasDefinition(value);
    },

    /** Return one TypeAliasDefinition from one JSON value. */
    fromJson(value: Json): TypeAliasDefinition {
        return fromJsonTypeAliasDefinition(value);
    },
};

/** Encode one TypeAliasDefinition. */
export function encodeTypeAliasDefinition(writer: BinaryWriter, value: TypeAliasDefinition): void {
    writer.writeOption(value.template, (value0) => {
        encodeLocalGenericTemplateId(writer, value0);
    });
    encodeGlobalTypeId(writer, value.value);
}

/** Decode one TypeAliasDefinition. */
export function decodeTypeAliasDefinition(reader: BinaryReader): TypeAliasDefinition {
    const template = reader.readOption(() => decodeLocalGenericTemplateId(reader));
    const value = decodeGlobalTypeId(reader);

    return {
        ...(template === undefined ? {} : { template }),
        value,
    };
}

/** Return one JSON value for one TypeAliasDefinition. */
export function toJsonTypeAliasDefinition(value: TypeAliasDefinition): Json {
    return {
        ...(value.template === undefined ? {} : { template: toJsonLocalGenericTemplateId(value.template) }),
        value: toJsonGlobalTypeId(value.value),
    };
}

/** Return one TypeAliasDefinition from one JSON value. */
export function fromJsonTypeAliasDefinition(value: Json): TypeAliasDefinition {
    const object = jsonObject(value);

    return {
        template: jsonOptional(object, "template", (value) => fromJsonLocalGenericTemplateId(value)),
        value: fromJsonGlobalTypeId(jsonField(object, "value")),
    };
}

/** Checked declaration data for one nominal struct. */
export type StructDefinition = {
    /** The generic template declared by the struct. */
    readonly template?: LocalGenericTemplateId;
    /** The implemented interfaces. */
    readonly implements: ReadonlyArray<NominalHeritage>;
    /** The members in declaration order. */
    readonly members: ReadonlyArray<DefinitionMember>;
};

export const StructDefinition = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: StructDefinition): void {
        encodeStructDefinition(writer, value);
    },

    /** Decode one StructDefinition. */
    decode(reader: BinaryReader): StructDefinition {
        return decodeStructDefinition(reader);
    },

    /** Return this value as JSON. */
    toJson(value: StructDefinition): Json {
        return toJsonStructDefinition(value);
    },

    /** Return one StructDefinition from one JSON value. */
    fromJson(value: Json): StructDefinition {
        return fromJsonStructDefinition(value);
    },
};

/** Encode one StructDefinition. */
export function encodeStructDefinition(writer: BinaryWriter, value: StructDefinition): void {
    writer.writeOption(value.template, (value0) => {
        encodeLocalGenericTemplateId(writer, value0);
    });
    writer.writeUnsigned(value.implements.length);
    for (const item1 of value.implements) {
        encodeNominalHeritage(writer, item1);
    }
    writer.writeUnsigned(value.members.length);
    for (const item2 of value.members) {
        encodeDefinitionMember(writer, item2);
    }
}

/** Decode one StructDefinition. */
export function decodeStructDefinition(reader: BinaryReader): StructDefinition {
    const template = reader.readOption(() => decodeLocalGenericTemplateId(reader));
    const implements_ = (() => { const length1 = reader.readNumber(); const items1: Array<NominalHeritage> = []; for (let index = 0; index < length1; index += 1) { items1.push(decodeNominalHeritage(reader)); } return items1; })();
    const members = (() => { const length2 = reader.readNumber(); const items2: Array<DefinitionMember> = []; for (let index = 0; index < length2; index += 1) { items2.push(decodeDefinitionMember(reader)); } return items2; })();

    return {
        ...(template === undefined ? {} : { template }),
        implements: implements_,
        members,
    };
}

/** Return one JSON value for one StructDefinition. */
export function toJsonStructDefinition(value: StructDefinition): Json {
    return {
        ...(value.template === undefined ? {} : { template: toJsonLocalGenericTemplateId(value.template) }),
        implements: value.implements.map((item0) => toJsonNominalHeritage(item0)),
        members: value.members.map((item0) => toJsonDefinitionMember(item0)),
    };
}

/** Return one StructDefinition from one JSON value. */
export function fromJsonStructDefinition(value: Json): StructDefinition {
    const object = jsonObject(value);

    return {
        template: jsonOptional(object, "template", (value) => fromJsonLocalGenericTemplateId(value)),
        implements: jsonArray(jsonField(object, "implements")).map((item0) => fromJsonNominalHeritage(item0)),
        members: jsonArray(jsonField(object, "members")).map((item0) => fromJsonDefinitionMember(item0)),
    };
}

/** One nominal heritage. */
export type NominalHeritage = {
    /** The source heritage node. */
    readonly source: GlobalNodeIdAny;
    /** The heritage nominal symbol. */
    readonly symbol: GlobalSymbolId;
    /** The generic arguments used at the relation site, empty when not applied. */
    readonly arguments: ReadonlyArray<GlobalTypeId>;
};

export const NominalHeritage = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: NominalHeritage): void {
        encodeNominalHeritage(writer, value);
    },

    /** Decode one NominalHeritage. */
    decode(reader: BinaryReader): NominalHeritage {
        return decodeNominalHeritage(reader);
    },

    /** Return this value as JSON. */
    toJson(value: NominalHeritage): Json {
        return toJsonNominalHeritage(value);
    },

    /** Return one NominalHeritage from one JSON value. */
    fromJson(value: Json): NominalHeritage {
        return fromJsonNominalHeritage(value);
    },
};

/** Encode one NominalHeritage. */
export function encodeNominalHeritage(writer: BinaryWriter, value: NominalHeritage): void {
    encodeGlobalNodeIdAny(writer, value.source);
    encodeGlobalSymbolId(writer, value.symbol);
    writer.writeUnsigned(value.arguments.length);
    for (const item2 of value.arguments) {
        encodeGlobalTypeId(writer, item2);
    }
}

/** Decode one NominalHeritage. */
export function decodeNominalHeritage(reader: BinaryReader): NominalHeritage {
    const source = decodeGlobalNodeIdAny(reader);
    const symbol_ = decodeGlobalSymbolId(reader);
    const arguments_ = (() => { const length2 = reader.readNumber(); const items2: Array<GlobalTypeId> = []; for (let index = 0; index < length2; index += 1) { items2.push(decodeGlobalTypeId(reader)); } return items2; })();

    return {
        source,
        symbol: symbol_,
        arguments: arguments_,
    };
}

/** Return one JSON value for one NominalHeritage. */
export function toJsonNominalHeritage(value: NominalHeritage): Json {
    return {
        source: toJsonGlobalNodeIdAny(value.source),
        symbol: toJsonGlobalSymbolId(value.symbol),
        arguments: value.arguments.map((item0) => toJsonGlobalTypeId(item0)),
    };
}

/** Return one NominalHeritage from one JSON value. */
export function fromJsonNominalHeritage(value: Json): NominalHeritage {
    const object = jsonObject(value);

    return {
        source: fromJsonGlobalNodeIdAny(jsonField(object, "source")),
        symbol: fromJsonGlobalSymbolId(jsonField(object, "symbol")),
        arguments: jsonArray(jsonField(object, "arguments")).map((item0) => fromJsonGlobalTypeId(item0)),
    };
}

/** One checked declaration member. */
export type DefinitionMember =
    /** Field member with a checked type. */
    | {
          readonly kind: "field";
          readonly field: FieldDefinition;
      }
    /** Method member with a checked type. */
    | {
          readonly kind: "method";
          readonly method: MethodDefinition;
      }
    /** Associated type member. */
    | {
          readonly kind: "associatedType";
          readonly associated_type: AssociatedTypeDefinition;
      }
    /** Associated constant member. */
    | {
          readonly kind: "associatedConst";
          readonly associated_const: AssociatedConstDefinition;
      }
    /** Enum variant member. */
    | {
          readonly kind: "variant";
          readonly variant: VariantDefinition;
      }
    /** Structural call signature member. */
    | {
          readonly kind: "callSignature";
          readonly call_signature: SignatureDefinition;
      }
    /** Structural construct signature member. */
    | {
          readonly kind: "constructSignature";
          readonly construct_signature: SignatureDefinition;
      }
    /** Structural index signature member. */
    | {
          readonly kind: "indexSignature";
          readonly index_signature: SignatureDefinition;
      }
;

export const DefinitionMember = {
    /** Field member with a checked type. */
    field(field: FieldDefinition): DefinitionMember {
        return { kind: "field", field };
    },

    /** Method member with a checked type. */
    method(method: MethodDefinition): DefinitionMember {
        return { kind: "method", method };
    },

    /** Associated type member. */
    associatedType(associated_type: AssociatedTypeDefinition): DefinitionMember {
        return { kind: "associatedType", associated_type };
    },

    /** Associated constant member. */
    associatedConst(associated_const: AssociatedConstDefinition): DefinitionMember {
        return { kind: "associatedConst", associated_const };
    },

    /** Enum variant member. */
    variant(variant: VariantDefinition): DefinitionMember {
        return { kind: "variant", variant };
    },

    /** Structural call signature member. */
    callSignature(call_signature: SignatureDefinition): DefinitionMember {
        return { kind: "callSignature", call_signature };
    },

    /** Structural construct signature member. */
    constructSignature(construct_signature: SignatureDefinition): DefinitionMember {
        return { kind: "constructSignature", construct_signature };
    },

    /** Structural index signature member. */
    indexSignature(index_signature: SignatureDefinition): DefinitionMember {
        return { kind: "indexSignature", index_signature };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: DefinitionMember): void {
        encodeDefinitionMember(writer, value);
    },

    /** Decode one DefinitionMember. */
    decode(reader: BinaryReader): DefinitionMember {
        return decodeDefinitionMember(reader);
    },

    /** Return this value as JSON. */
    toJson(value: DefinitionMember): Json {
        return toJsonDefinitionMember(value);
    },

    /** Return one DefinitionMember from one JSON value. */
    fromJson(value: Json): DefinitionMember {
        return fromJsonDefinitionMember(value);
    },
};

/** Encode one DefinitionMember. */
export function encodeDefinitionMember(writer: BinaryWriter, value: DefinitionMember): void {
    switch (value.kind) {
        case "field":
            writer.writeUnsigned(0);
            encodeFieldDefinition(writer, value.field);
            return;
        case "method":
            writer.writeUnsigned(1);
            encodeMethodDefinition(writer, value.method);
            return;
        case "associatedType":
            writer.writeUnsigned(2);
            encodeAssociatedTypeDefinition(writer, value.associated_type);
            return;
        case "associatedConst":
            writer.writeUnsigned(3);
            encodeAssociatedConstDefinition(writer, value.associated_const);
            return;
        case "variant":
            writer.writeUnsigned(4);
            encodeVariantDefinition(writer, value.variant);
            return;
        case "callSignature":
            writer.writeUnsigned(5);
            encodeSignatureDefinition(writer, value.call_signature);
            return;
        case "constructSignature":
            writer.writeUnsigned(6);
            encodeSignatureDefinition(writer, value.construct_signature);
            return;
        case "indexSignature":
            writer.writeUnsigned(7);
            encodeSignatureDefinition(writer, value.index_signature);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one DefinitionMember. */
export function decodeDefinitionMember(reader: BinaryReader): DefinitionMember {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const field = decodeFieldDefinition(reader);

            return { kind: "field", field };
        }
        case 1: {
            const method = decodeMethodDefinition(reader);

            return { kind: "method", method };
        }
        case 2: {
            const associated_type = decodeAssociatedTypeDefinition(reader);

            return { kind: "associatedType", associated_type };
        }
        case 3: {
            const associated_const = decodeAssociatedConstDefinition(reader);

            return { kind: "associatedConst", associated_const };
        }
        case 4: {
            const variant = decodeVariantDefinition(reader);

            return { kind: "variant", variant };
        }
        case 5: {
            const call_signature = decodeSignatureDefinition(reader);

            return { kind: "callSignature", call_signature };
        }
        case 6: {
            const construct_signature = decodeSignatureDefinition(reader);

            return { kind: "constructSignature", construct_signature };
        }
        case 7: {
            const index_signature = decodeSignatureDefinition(reader);

            return { kind: "indexSignature", index_signature };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one DefinitionMember. */
export function toJsonDefinitionMember(value: DefinitionMember): Json {
    switch (value.kind) {
        case "field":
            return {
                kind: "field",
                field: toJsonFieldDefinition(value.field),
            };
        case "method":
            return {
                kind: "method",
                method: toJsonMethodDefinition(value.method),
            };
        case "associatedType":
            return {
                kind: "associatedType",
                associated_type: toJsonAssociatedTypeDefinition(value.associated_type),
            };
        case "associatedConst":
            return {
                kind: "associatedConst",
                associated_const: toJsonAssociatedConstDefinition(value.associated_const),
            };
        case "variant":
            return {
                kind: "variant",
                variant: toJsonVariantDefinition(value.variant),
            };
        case "callSignature":
            return {
                kind: "callSignature",
                call_signature: toJsonSignatureDefinition(value.call_signature),
            };
        case "constructSignature":
            return {
                kind: "constructSignature",
                construct_signature: toJsonSignatureDefinition(value.construct_signature),
            };
        case "indexSignature":
            return {
                kind: "indexSignature",
                index_signature: toJsonSignatureDefinition(value.index_signature),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one DefinitionMember from one JSON value. */
export function fromJsonDefinitionMember(value: Json): DefinitionMember {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "field":
            return {
                kind,
                field: fromJsonFieldDefinition(jsonField(object, "field")),
            };
        case "method":
            return {
                kind,
                method: fromJsonMethodDefinition(jsonField(object, "method")),
            };
        case "associatedType":
            return {
                kind,
                associated_type: fromJsonAssociatedTypeDefinition(jsonField(object, "associated_type")),
            };
        case "associatedConst":
            return {
                kind,
                associated_const: fromJsonAssociatedConstDefinition(jsonField(object, "associated_const")),
            };
        case "variant":
            return {
                kind,
                variant: fromJsonVariantDefinition(jsonField(object, "variant")),
            };
        case "callSignature":
            return {
                kind,
                call_signature: fromJsonSignatureDefinition(jsonField(object, "call_signature")),
            };
        case "constructSignature":
            return {
                kind,
                construct_signature: fromJsonSignatureDefinition(jsonField(object, "construct_signature")),
            };
        case "indexSignature":
            return {
                kind,
                index_signature: fromJsonSignatureDefinition(jsonField(object, "index_signature")),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** One checked field member. */
export type FieldDefinition = {
    /** The member space declaring the field. */
    readonly space: MemberSpace;
    /** The field symbol. */
    readonly symbol: GlobalSymbolId;
    /** The source member node. */
    readonly source: GlobalNodeIdAny;
    /** The field key. */
    readonly key: StaticKey;
    /** The checked field type. */
    readonly ty: GlobalTypeId;
    /** Whether subclasses must provide the field. */
    readonly isAbstract: boolean;
    /** Whether the field overrides an inherited member. */
    readonly isOverride: boolean;
    /** The @if availability condition guarding this member, when guarded. */
    readonly condition?: GlobalTypeId;
};

export const FieldDefinition = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: FieldDefinition): void {
        encodeFieldDefinition(writer, value);
    },

    /** Decode one FieldDefinition. */
    decode(reader: BinaryReader): FieldDefinition {
        return decodeFieldDefinition(reader);
    },

    /** Return this value as JSON. */
    toJson(value: FieldDefinition): Json {
        return toJsonFieldDefinition(value);
    },

    /** Return one FieldDefinition from one JSON value. */
    fromJson(value: Json): FieldDefinition {
        return fromJsonFieldDefinition(value);
    },
};

/** Encode one FieldDefinition. */
export function encodeFieldDefinition(writer: BinaryWriter, value: FieldDefinition): void {
    encodeMemberSpace(writer, value.space);
    encodeGlobalSymbolId(writer, value.symbol);
    encodeGlobalNodeIdAny(writer, value.source);
    encodeStaticKey(writer, value.key);
    encodeGlobalTypeId(writer, value.ty);
    writer.writeBool(value.isAbstract);
    writer.writeBool(value.isOverride);
    writer.writeOption(value.condition, (value7) => {
        encodeGlobalTypeId(writer, value7);
    });
}

/** Decode one FieldDefinition. */
export function decodeFieldDefinition(reader: BinaryReader): FieldDefinition {
    const space = decodeMemberSpace(reader);
    const symbol_ = decodeGlobalSymbolId(reader);
    const source = decodeGlobalNodeIdAny(reader);
    const key = decodeStaticKey(reader);
    const ty = decodeGlobalTypeId(reader);
    const isAbstract = reader.readBool();
    const isOverride = reader.readBool();
    const condition = reader.readOption(() => decodeGlobalTypeId(reader));

    return {
        space,
        symbol: symbol_,
        source,
        key,
        ty,
        isAbstract,
        isOverride,
        ...(condition === undefined ? {} : { condition }),
    };
}

/** Return one JSON value for one FieldDefinition. */
export function toJsonFieldDefinition(value: FieldDefinition): Json {
    return {
        space: toJsonMemberSpace(value.space),
        symbol: toJsonGlobalSymbolId(value.symbol),
        source: toJsonGlobalNodeIdAny(value.source),
        key: toJsonStaticKey(value.key),
        ty: toJsonGlobalTypeId(value.ty),
        isAbstract: value.isAbstract,
        isOverride: value.isOverride,
        ...(value.condition === undefined ? {} : { condition: toJsonGlobalTypeId(value.condition) }),
    };
}

/** Return one FieldDefinition from one JSON value. */
export function fromJsonFieldDefinition(value: Json): FieldDefinition {
    const object = jsonObject(value);

    return {
        space: fromJsonMemberSpace(jsonField(object, "space")),
        symbol: fromJsonGlobalSymbolId(jsonField(object, "symbol")),
        source: fromJsonGlobalNodeIdAny(jsonField(object, "source")),
        key: fromJsonStaticKey(jsonField(object, "key")),
        ty: fromJsonGlobalTypeId(jsonField(object, "ty")),
        isAbstract: jsonBool(jsonField(object, "isAbstract")),
        isOverride: jsonBool(jsonField(object, "isOverride")),
        condition: jsonOptional(object, "condition", (value) => fromJsonGlobalTypeId(value)),
    };
}

/** Member namespace selected by member lookup. */
export type MemberSpace = "instance" | "static";

export const MemberSpace = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: MemberSpace): void {
        encodeMemberSpace(writer, value);
    },

    /** Decode one MemberSpace. */
    decode(reader: BinaryReader): MemberSpace {
        return decodeMemberSpace(reader);
    },

    /** Return this value as JSON. */
    toJson(value: MemberSpace): Json {
        return toJsonMemberSpace(value);
    },

    /** Return one MemberSpace from one JSON value. */
    fromJson(value: Json): MemberSpace {
        return fromJsonMemberSpace(value);
    },
};

/** Encode one MemberSpace. */
export function encodeMemberSpace(writer: BinaryWriter, value: MemberSpace): void {
    switch (value) {
        case "instance":
            writer.writeUnsigned(0);
            return;
        case "static":
            writer.writeUnsigned(1);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one MemberSpace. */
export function decodeMemberSpace(reader: BinaryReader): MemberSpace {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "instance";
        case 1:
            return "static";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one MemberSpace. */
export function toJsonMemberSpace(value: MemberSpace): Json {
    return value;
}

/** Return one MemberSpace from one JSON value. */
export function fromJsonMemberSpace(value: Json): MemberSpace {
    const variant = jsonString(value);

    switch (variant) {
        case "instance":
            return "instance";
        case "static":
            return "static";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** One method member. */
export type MethodDefinition = {
    /** The member space declaring the method. */
    readonly space: MemberSpace;
    /** The method symbol. */
    readonly symbol?: GlobalSymbolId;
    /** The source member node. */
    readonly source: GlobalNodeIdAny;
    /** The nominal member slot. */
    readonly slot: MemberSlot;
    /** The method role. */
    readonly role?: FunctionRole;
    /** The checked method type. */
    readonly ty: GlobalTypeId;
    /** The abstraction mode governing overrides. */
    readonly abstraction: MethodAbstraction;
    /** Whether the method overrides an inherited member. */
    readonly isOverride: boolean;
    /** The @if availability condition guarding this member, when guarded. */
    readonly condition?: GlobalTypeId;
};

export const MethodDefinition = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: MethodDefinition): void {
        encodeMethodDefinition(writer, value);
    },

    /** Decode one MethodDefinition. */
    decode(reader: BinaryReader): MethodDefinition {
        return decodeMethodDefinition(reader);
    },

    /** Return this value as JSON. */
    toJson(value: MethodDefinition): Json {
        return toJsonMethodDefinition(value);
    },

    /** Return one MethodDefinition from one JSON value. */
    fromJson(value: Json): MethodDefinition {
        return fromJsonMethodDefinition(value);
    },
};

/** Encode one MethodDefinition. */
export function encodeMethodDefinition(writer: BinaryWriter, value: MethodDefinition): void {
    encodeMemberSpace(writer, value.space);
    writer.writeOption(value.symbol, (value1) => {
        encodeGlobalSymbolId(writer, value1);
    });
    encodeGlobalNodeIdAny(writer, value.source);
    encodeMemberSlot(writer, value.slot);
    writer.writeOption(value.role, (value4) => {
        encodeFunctionRole(writer, value4);
    });
    encodeGlobalTypeId(writer, value.ty);
    encodeMethodAbstraction(writer, value.abstraction);
    writer.writeBool(value.isOverride);
    writer.writeOption(value.condition, (value8) => {
        encodeGlobalTypeId(writer, value8);
    });
}

/** Decode one MethodDefinition. */
export function decodeMethodDefinition(reader: BinaryReader): MethodDefinition {
    const space = decodeMemberSpace(reader);
    const symbol_ = reader.readOption(() => decodeGlobalSymbolId(reader));
    const source = decodeGlobalNodeIdAny(reader);
    const slot = decodeMemberSlot(reader);
    const role = reader.readOption(() => decodeFunctionRole(reader));
    const ty = decodeGlobalTypeId(reader);
    const abstraction = decodeMethodAbstraction(reader);
    const isOverride = reader.readBool();
    const condition = reader.readOption(() => decodeGlobalTypeId(reader));

    return {
        space,
        ...(symbol_ === undefined ? {} : { symbol: symbol_ }),
        source,
        slot,
        ...(role === undefined ? {} : { role }),
        ty,
        abstraction,
        isOverride,
        ...(condition === undefined ? {} : { condition }),
    };
}

/** Return one JSON value for one MethodDefinition. */
export function toJsonMethodDefinition(value: MethodDefinition): Json {
    return {
        space: toJsonMemberSpace(value.space),
        ...(value.symbol === undefined ? {} : { symbol: toJsonGlobalSymbolId(value.symbol) }),
        source: toJsonGlobalNodeIdAny(value.source),
        slot: toJsonMemberSlot(value.slot),
        ...(value.role === undefined ? {} : { role: toJsonFunctionRole(value.role) }),
        ty: toJsonGlobalTypeId(value.ty),
        abstraction: toJsonMethodAbstraction(value.abstraction),
        isOverride: value.isOverride,
        ...(value.condition === undefined ? {} : { condition: toJsonGlobalTypeId(value.condition) }),
    };
}

/** Return one MethodDefinition from one JSON value. */
export function fromJsonMethodDefinition(value: Json): MethodDefinition {
    const object = jsonObject(value);

    return {
        space: fromJsonMemberSpace(jsonField(object, "space")),
        symbol: jsonOptional(object, "symbol", (value) => fromJsonGlobalSymbolId(value)),
        source: fromJsonGlobalNodeIdAny(jsonField(object, "source")),
        slot: fromJsonMemberSlot(jsonField(object, "slot")),
        role: jsonOptional(object, "role", (value) => fromJsonFunctionRole(value)),
        ty: fromJsonGlobalTypeId(jsonField(object, "ty")),
        abstraction: fromJsonMethodAbstraction(jsonField(object, "abstraction")),
        isOverride: jsonBool(jsonField(object, "isOverride")),
        condition: jsonOptional(object, "condition", (value) => fromJsonGlobalTypeId(value)),
    };
}

/** One checked associated type. */
export type AssociatedTypeDefinition = {
    /** The associated type symbol. */
    readonly symbol: GlobalSymbolId;
    /** The source member node. */
    readonly source: GlobalNodeIdAny;
    /** The associated type key. */
    readonly key: StaticKey;
    /** The upper bound required by this associated type. */
    readonly constraint?: GlobalTypeId;
    /** The concrete associated type value. */
    readonly value?: GlobalTypeId;
    /** The @if availability condition guarding this member, when guarded. */
    readonly condition?: GlobalTypeId;
};

export const AssociatedTypeDefinition = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: AssociatedTypeDefinition): void {
        encodeAssociatedTypeDefinition(writer, value);
    },

    /** Decode one AssociatedTypeDefinition. */
    decode(reader: BinaryReader): AssociatedTypeDefinition {
        return decodeAssociatedTypeDefinition(reader);
    },

    /** Return this value as JSON. */
    toJson(value: AssociatedTypeDefinition): Json {
        return toJsonAssociatedTypeDefinition(value);
    },

    /** Return one AssociatedTypeDefinition from one JSON value. */
    fromJson(value: Json): AssociatedTypeDefinition {
        return fromJsonAssociatedTypeDefinition(value);
    },
};

/** Encode one AssociatedTypeDefinition. */
export function encodeAssociatedTypeDefinition(writer: BinaryWriter, value: AssociatedTypeDefinition): void {
    encodeGlobalSymbolId(writer, value.symbol);
    encodeGlobalNodeIdAny(writer, value.source);
    encodeStaticKey(writer, value.key);
    writer.writeOption(value.constraint, (value3) => {
        encodeGlobalTypeId(writer, value3);
    });
    writer.writeOption(value.value, (value4) => {
        encodeGlobalTypeId(writer, value4);
    });
    writer.writeOption(value.condition, (value5) => {
        encodeGlobalTypeId(writer, value5);
    });
}

/** Decode one AssociatedTypeDefinition. */
export function decodeAssociatedTypeDefinition(reader: BinaryReader): AssociatedTypeDefinition {
    const symbol_ = decodeGlobalSymbolId(reader);
    const source = decodeGlobalNodeIdAny(reader);
    const key = decodeStaticKey(reader);
    const constraint = reader.readOption(() => decodeGlobalTypeId(reader));
    const value = reader.readOption(() => decodeGlobalTypeId(reader));
    const condition = reader.readOption(() => decodeGlobalTypeId(reader));

    return {
        symbol: symbol_,
        source,
        key,
        ...(constraint === undefined ? {} : { constraint }),
        ...(value === undefined ? {} : { value }),
        ...(condition === undefined ? {} : { condition }),
    };
}

/** Return one JSON value for one AssociatedTypeDefinition. */
export function toJsonAssociatedTypeDefinition(value: AssociatedTypeDefinition): Json {
    return {
        symbol: toJsonGlobalSymbolId(value.symbol),
        source: toJsonGlobalNodeIdAny(value.source),
        key: toJsonStaticKey(value.key),
        ...(value.constraint === undefined ? {} : { constraint: toJsonGlobalTypeId(value.constraint) }),
        ...(value.value === undefined ? {} : { value: toJsonGlobalTypeId(value.value) }),
        ...(value.condition === undefined ? {} : { condition: toJsonGlobalTypeId(value.condition) }),
    };
}

/** Return one AssociatedTypeDefinition from one JSON value. */
export function fromJsonAssociatedTypeDefinition(value: Json): AssociatedTypeDefinition {
    const object = jsonObject(value);

    return {
        symbol: fromJsonGlobalSymbolId(jsonField(object, "symbol")),
        source: fromJsonGlobalNodeIdAny(jsonField(object, "source")),
        key: fromJsonStaticKey(jsonField(object, "key")),
        constraint: jsonOptional(object, "constraint", (value) => fromJsonGlobalTypeId(value)),
        value: jsonOptional(object, "value", (value) => fromJsonGlobalTypeId(value)),
        condition: jsonOptional(object, "condition", (value) => fromJsonGlobalTypeId(value)),
    };
}

/** One checked associated constant. */
export type AssociatedConstDefinition = {
    /** The associated const symbol. */
    readonly symbol: GlobalSymbolId;
    /** The source member node. */
    readonly source: GlobalNodeIdAny;
    /** The associated const key. */
    readonly key: StaticKey;
    /** The checked static type. */
    readonly ty: GlobalTypeId;
    /** The checked static value. */
    readonly value?: GlobalStaticId;
    /** The @if availability condition guarding this member, when guarded. */
    readonly condition?: GlobalTypeId;
};

export const AssociatedConstDefinition = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: AssociatedConstDefinition): void {
        encodeAssociatedConstDefinition(writer, value);
    },

    /** Decode one AssociatedConstDefinition. */
    decode(reader: BinaryReader): AssociatedConstDefinition {
        return decodeAssociatedConstDefinition(reader);
    },

    /** Return this value as JSON. */
    toJson(value: AssociatedConstDefinition): Json {
        return toJsonAssociatedConstDefinition(value);
    },

    /** Return one AssociatedConstDefinition from one JSON value. */
    fromJson(value: Json): AssociatedConstDefinition {
        return fromJsonAssociatedConstDefinition(value);
    },
};

/** Encode one AssociatedConstDefinition. */
export function encodeAssociatedConstDefinition(writer: BinaryWriter, value: AssociatedConstDefinition): void {
    encodeGlobalSymbolId(writer, value.symbol);
    encodeGlobalNodeIdAny(writer, value.source);
    encodeStaticKey(writer, value.key);
    encodeGlobalTypeId(writer, value.ty);
    writer.writeOption(value.value, (value4) => {
        encodeGlobalStaticId(writer, value4);
    });
    writer.writeOption(value.condition, (value5) => {
        encodeGlobalTypeId(writer, value5);
    });
}

/** Decode one AssociatedConstDefinition. */
export function decodeAssociatedConstDefinition(reader: BinaryReader): AssociatedConstDefinition {
    const symbol_ = decodeGlobalSymbolId(reader);
    const source = decodeGlobalNodeIdAny(reader);
    const key = decodeStaticKey(reader);
    const ty = decodeGlobalTypeId(reader);
    const value = reader.readOption(() => decodeGlobalStaticId(reader));
    const condition = reader.readOption(() => decodeGlobalTypeId(reader));

    return {
        symbol: symbol_,
        source,
        key,
        ty,
        ...(value === undefined ? {} : { value }),
        ...(condition === undefined ? {} : { condition }),
    };
}

/** Return one JSON value for one AssociatedConstDefinition. */
export function toJsonAssociatedConstDefinition(value: AssociatedConstDefinition): Json {
    return {
        symbol: toJsonGlobalSymbolId(value.symbol),
        source: toJsonGlobalNodeIdAny(value.source),
        key: toJsonStaticKey(value.key),
        ty: toJsonGlobalTypeId(value.ty),
        ...(value.value === undefined ? {} : { value: toJsonGlobalStaticId(value.value) }),
        ...(value.condition === undefined ? {} : { condition: toJsonGlobalTypeId(value.condition) }),
    };
}

/** Return one AssociatedConstDefinition from one JSON value. */
export function fromJsonAssociatedConstDefinition(value: Json): AssociatedConstDefinition {
    const object = jsonObject(value);

    return {
        symbol: fromJsonGlobalSymbolId(jsonField(object, "symbol")),
        source: fromJsonGlobalNodeIdAny(jsonField(object, "source")),
        key: fromJsonStaticKey(jsonField(object, "key")),
        ty: fromJsonGlobalTypeId(jsonField(object, "ty")),
        value: jsonOptional(object, "value", (value) => fromJsonGlobalStaticId(value)),
        condition: jsonOptional(object, "condition", (value) => fromJsonGlobalTypeId(value)),
    };
}

/** One checked enum variant. */
export type VariantDefinition = {
    /** The variant symbol. */
    readonly symbol: GlobalSymbolId;
    /** The source enum field node. */
    readonly source: GlobalNodeIdAny;
    /** The variant key. */
    readonly key: StaticKey;
    /** The checked variant value. */
    readonly value?: GlobalStaticId;
    /** The @if availability condition guarding this member, when guarded. */
    readonly condition?: GlobalTypeId;
};

export const VariantDefinition = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: VariantDefinition): void {
        encodeVariantDefinition(writer, value);
    },

    /** Decode one VariantDefinition. */
    decode(reader: BinaryReader): VariantDefinition {
        return decodeVariantDefinition(reader);
    },

    /** Return this value as JSON. */
    toJson(value: VariantDefinition): Json {
        return toJsonVariantDefinition(value);
    },

    /** Return one VariantDefinition from one JSON value. */
    fromJson(value: Json): VariantDefinition {
        return fromJsonVariantDefinition(value);
    },
};

/** Encode one VariantDefinition. */
export function encodeVariantDefinition(writer: BinaryWriter, value: VariantDefinition): void {
    encodeGlobalSymbolId(writer, value.symbol);
    encodeGlobalNodeIdAny(writer, value.source);
    encodeStaticKey(writer, value.key);
    writer.writeOption(value.value, (value3) => {
        encodeGlobalStaticId(writer, value3);
    });
    writer.writeOption(value.condition, (value4) => {
        encodeGlobalTypeId(writer, value4);
    });
}

/** Decode one VariantDefinition. */
export function decodeVariantDefinition(reader: BinaryReader): VariantDefinition {
    const symbol_ = decodeGlobalSymbolId(reader);
    const source = decodeGlobalNodeIdAny(reader);
    const key = decodeStaticKey(reader);
    const value = reader.readOption(() => decodeGlobalStaticId(reader));
    const condition = reader.readOption(() => decodeGlobalTypeId(reader));

    return {
        symbol: symbol_,
        source,
        key,
        ...(value === undefined ? {} : { value }),
        ...(condition === undefined ? {} : { condition }),
    };
}

/** Return one JSON value for one VariantDefinition. */
export function toJsonVariantDefinition(value: VariantDefinition): Json {
    return {
        symbol: toJsonGlobalSymbolId(value.symbol),
        source: toJsonGlobalNodeIdAny(value.source),
        key: toJsonStaticKey(value.key),
        ...(value.value === undefined ? {} : { value: toJsonGlobalStaticId(value.value) }),
        ...(value.condition === undefined ? {} : { condition: toJsonGlobalTypeId(value.condition) }),
    };
}

/** Return one VariantDefinition from one JSON value. */
export function fromJsonVariantDefinition(value: Json): VariantDefinition {
    const object = jsonObject(value);

    return {
        symbol: fromJsonGlobalSymbolId(jsonField(object, "symbol")),
        source: fromJsonGlobalNodeIdAny(jsonField(object, "source")),
        key: fromJsonStaticKey(jsonField(object, "key")),
        value: jsonOptional(object, "value", (value) => fromJsonGlobalStaticId(value)),
        condition: jsonOptional(object, "condition", (value) => fromJsonGlobalTypeId(value)),
    };
}

/** One checked symbol-free signature member. */
export type SignatureDefinition = {
    /** The source member node. */
    readonly source: GlobalNodeIdAny;
    /** The checked signature type. */
    readonly ty: GlobalTypeId;
    /** The @if availability condition guarding this member, when guarded. */
    readonly condition?: GlobalTypeId;
};

export const SignatureDefinition = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: SignatureDefinition): void {
        encodeSignatureDefinition(writer, value);
    },

    /** Decode one SignatureDefinition. */
    decode(reader: BinaryReader): SignatureDefinition {
        return decodeSignatureDefinition(reader);
    },

    /** Return this value as JSON. */
    toJson(value: SignatureDefinition): Json {
        return toJsonSignatureDefinition(value);
    },

    /** Return one SignatureDefinition from one JSON value. */
    fromJson(value: Json): SignatureDefinition {
        return fromJsonSignatureDefinition(value);
    },
};

/** Encode one SignatureDefinition. */
export function encodeSignatureDefinition(writer: BinaryWriter, value: SignatureDefinition): void {
    encodeGlobalNodeIdAny(writer, value.source);
    encodeGlobalTypeId(writer, value.ty);
    writer.writeOption(value.condition, (value2) => {
        encodeGlobalTypeId(writer, value2);
    });
}

/** Decode one SignatureDefinition. */
export function decodeSignatureDefinition(reader: BinaryReader): SignatureDefinition {
    const source = decodeGlobalNodeIdAny(reader);
    const ty = decodeGlobalTypeId(reader);
    const condition = reader.readOption(() => decodeGlobalTypeId(reader));

    return {
        source,
        ty,
        ...(condition === undefined ? {} : { condition }),
    };
}

/** Return one JSON value for one SignatureDefinition. */
export function toJsonSignatureDefinition(value: SignatureDefinition): Json {
    return {
        source: toJsonGlobalNodeIdAny(value.source),
        ty: toJsonGlobalTypeId(value.ty),
        ...(value.condition === undefined ? {} : { condition: toJsonGlobalTypeId(value.condition) }),
    };
}

/** Return one SignatureDefinition from one JSON value. */
export function fromJsonSignatureDefinition(value: Json): SignatureDefinition {
    const object = jsonObject(value);

    return {
        source: fromJsonGlobalNodeIdAny(jsonField(object, "source")),
        ty: fromJsonGlobalTypeId(jsonField(object, "ty")),
        condition: jsonOptional(object, "condition", (value) => fromJsonGlobalTypeId(value)),
    };
}

/** Checked declaration data for one nominal class. */
export type ClassDefinition = {
    /** The generic template declared by the class. */
    readonly template?: LocalGenericTemplateId;
    /** Whether the class is abstract. */
    readonly isAbstract: boolean;
    /** Whether the class rejects subclasses. */
    readonly isFinal: boolean;
    /** The extended class. */
    readonly extends?: NominalHeritage;
    /** The implemented interfaces. */
    readonly implements: ReadonlyArray<NominalHeritage>;
    /** The members in declaration order. */
    readonly members: ReadonlyArray<DefinitionMember>;
};

export const ClassDefinition = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ClassDefinition): void {
        encodeClassDefinition(writer, value);
    },

    /** Decode one ClassDefinition. */
    decode(reader: BinaryReader): ClassDefinition {
        return decodeClassDefinition(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ClassDefinition): Json {
        return toJsonClassDefinition(value);
    },

    /** Return one ClassDefinition from one JSON value. */
    fromJson(value: Json): ClassDefinition {
        return fromJsonClassDefinition(value);
    },
};

/** Encode one ClassDefinition. */
export function encodeClassDefinition(writer: BinaryWriter, value: ClassDefinition): void {
    writer.writeOption(value.template, (value0) => {
        encodeLocalGenericTemplateId(writer, value0);
    });
    writer.writeBool(value.isAbstract);
    writer.writeBool(value.isFinal);
    writer.writeOption(value.extends, (value3) => {
        encodeNominalHeritage(writer, value3);
    });
    writer.writeUnsigned(value.implements.length);
    for (const item4 of value.implements) {
        encodeNominalHeritage(writer, item4);
    }
    writer.writeUnsigned(value.members.length);
    for (const item5 of value.members) {
        encodeDefinitionMember(writer, item5);
    }
}

/** Decode one ClassDefinition. */
export function decodeClassDefinition(reader: BinaryReader): ClassDefinition {
    const template = reader.readOption(() => decodeLocalGenericTemplateId(reader));
    const isAbstract = reader.readBool();
    const isFinal = reader.readBool();
    const extends_ = reader.readOption(() => decodeNominalHeritage(reader));
    const implements_ = (() => { const length4 = reader.readNumber(); const items4: Array<NominalHeritage> = []; for (let index = 0; index < length4; index += 1) { items4.push(decodeNominalHeritage(reader)); } return items4; })();
    const members = (() => { const length5 = reader.readNumber(); const items5: Array<DefinitionMember> = []; for (let index = 0; index < length5; index += 1) { items5.push(decodeDefinitionMember(reader)); } return items5; })();

    return {
        ...(template === undefined ? {} : { template }),
        isAbstract,
        isFinal,
        ...(extends_ === undefined ? {} : { extends: extends_ }),
        implements: implements_,
        members,
    };
}

/** Return one JSON value for one ClassDefinition. */
export function toJsonClassDefinition(value: ClassDefinition): Json {
    return {
        ...(value.template === undefined ? {} : { template: toJsonLocalGenericTemplateId(value.template) }),
        isAbstract: value.isAbstract,
        isFinal: value.isFinal,
        ...(value.extends === undefined ? {} : { extends: toJsonNominalHeritage(value.extends) }),
        implements: value.implements.map((item0) => toJsonNominalHeritage(item0)),
        members: value.members.map((item0) => toJsonDefinitionMember(item0)),
    };
}

/** Return one ClassDefinition from one JSON value. */
export function fromJsonClassDefinition(value: Json): ClassDefinition {
    const object = jsonObject(value);

    return {
        template: jsonOptional(object, "template", (value) => fromJsonLocalGenericTemplateId(value)),
        isAbstract: jsonBool(jsonField(object, "isAbstract")),
        isFinal: jsonBool(jsonField(object, "isFinal")),
        extends: jsonOptional(object, "extends", (value) => fromJsonNominalHeritage(value)),
        implements: jsonArray(jsonField(object, "implements")).map((item0) => fromJsonNominalHeritage(item0)),
        members: jsonArray(jsonField(object, "members")).map((item0) => fromJsonDefinitionMember(item0)),
    };
}

/** Checked declaration data for one nominal interface. */
export type InterfaceDefinition = {
    /** The generic template declared by the interface. */
    readonly template?: LocalGenericTemplateId;
    /** Whether the interface has nominal identity. */
    readonly isNominal: boolean;
    /** The inherited interfaces. */
    readonly extends: ReadonlyArray<NominalHeritage>;
    /** The members in declaration order. */
    readonly members: ReadonlyArray<DefinitionMember>;
};

export const InterfaceDefinition = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: InterfaceDefinition): void {
        encodeInterfaceDefinition(writer, value);
    },

    /** Decode one InterfaceDefinition. */
    decode(reader: BinaryReader): InterfaceDefinition {
        return decodeInterfaceDefinition(reader);
    },

    /** Return this value as JSON. */
    toJson(value: InterfaceDefinition): Json {
        return toJsonInterfaceDefinition(value);
    },

    /** Return one InterfaceDefinition from one JSON value. */
    fromJson(value: Json): InterfaceDefinition {
        return fromJsonInterfaceDefinition(value);
    },
};

/** Encode one InterfaceDefinition. */
export function encodeInterfaceDefinition(writer: BinaryWriter, value: InterfaceDefinition): void {
    writer.writeOption(value.template, (value0) => {
        encodeLocalGenericTemplateId(writer, value0);
    });
    writer.writeBool(value.isNominal);
    writer.writeUnsigned(value.extends.length);
    for (const item2 of value.extends) {
        encodeNominalHeritage(writer, item2);
    }
    writer.writeUnsigned(value.members.length);
    for (const item3 of value.members) {
        encodeDefinitionMember(writer, item3);
    }
}

/** Decode one InterfaceDefinition. */
export function decodeInterfaceDefinition(reader: BinaryReader): InterfaceDefinition {
    const template = reader.readOption(() => decodeLocalGenericTemplateId(reader));
    const isNominal = reader.readBool();
    const extends_ = (() => { const length2 = reader.readNumber(); const items2: Array<NominalHeritage> = []; for (let index = 0; index < length2; index += 1) { items2.push(decodeNominalHeritage(reader)); } return items2; })();
    const members = (() => { const length3 = reader.readNumber(); const items3: Array<DefinitionMember> = []; for (let index = 0; index < length3; index += 1) { items3.push(decodeDefinitionMember(reader)); } return items3; })();

    return {
        ...(template === undefined ? {} : { template }),
        isNominal,
        extends: extends_,
        members,
    };
}

/** Return one JSON value for one InterfaceDefinition. */
export function toJsonInterfaceDefinition(value: InterfaceDefinition): Json {
    return {
        ...(value.template === undefined ? {} : { template: toJsonLocalGenericTemplateId(value.template) }),
        isNominal: value.isNominal,
        extends: value.extends.map((item0) => toJsonNominalHeritage(item0)),
        members: value.members.map((item0) => toJsonDefinitionMember(item0)),
    };
}

/** Return one InterfaceDefinition from one JSON value. */
export function fromJsonInterfaceDefinition(value: Json): InterfaceDefinition {
    const object = jsonObject(value);

    return {
        template: jsonOptional(object, "template", (value) => fromJsonLocalGenericTemplateId(value)),
        isNominal: jsonBool(jsonField(object, "isNominal")),
        extends: jsonArray(jsonField(object, "extends")).map((item0) => fromJsonNominalHeritage(item0)),
        members: jsonArray(jsonField(object, "members")).map((item0) => fromJsonDefinitionMember(item0)),
    };
}

/** Checked declaration data for one nominal enum. */
export type EnumDefinition = {
    /** The generic template declared by the enum. */
    readonly template?: LocalGenericTemplateId;
    /** The implemented interfaces. */
    readonly implements: ReadonlyArray<NominalHeritage>;
    /** The members in declaration order. */
    readonly members: ReadonlyArray<DefinitionMember>;
};

export const EnumDefinition = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: EnumDefinition): void {
        encodeEnumDefinition(writer, value);
    },

    /** Decode one EnumDefinition. */
    decode(reader: BinaryReader): EnumDefinition {
        return decodeEnumDefinition(reader);
    },

    /** Return this value as JSON. */
    toJson(value: EnumDefinition): Json {
        return toJsonEnumDefinition(value);
    },

    /** Return one EnumDefinition from one JSON value. */
    fromJson(value: Json): EnumDefinition {
        return fromJsonEnumDefinition(value);
    },
};

/** Encode one EnumDefinition. */
export function encodeEnumDefinition(writer: BinaryWriter, value: EnumDefinition): void {
    writer.writeOption(value.template, (value0) => {
        encodeLocalGenericTemplateId(writer, value0);
    });
    writer.writeUnsigned(value.implements.length);
    for (const item1 of value.implements) {
        encodeNominalHeritage(writer, item1);
    }
    writer.writeUnsigned(value.members.length);
    for (const item2 of value.members) {
        encodeDefinitionMember(writer, item2);
    }
}

/** Decode one EnumDefinition. */
export function decodeEnumDefinition(reader: BinaryReader): EnumDefinition {
    const template = reader.readOption(() => decodeLocalGenericTemplateId(reader));
    const implements_ = (() => { const length1 = reader.readNumber(); const items1: Array<NominalHeritage> = []; for (let index = 0; index < length1; index += 1) { items1.push(decodeNominalHeritage(reader)); } return items1; })();
    const members = (() => { const length2 = reader.readNumber(); const items2: Array<DefinitionMember> = []; for (let index = 0; index < length2; index += 1) { items2.push(decodeDefinitionMember(reader)); } return items2; })();

    return {
        ...(template === undefined ? {} : { template }),
        implements: implements_,
        members,
    };
}

/** Return one JSON value for one EnumDefinition. */
export function toJsonEnumDefinition(value: EnumDefinition): Json {
    return {
        ...(value.template === undefined ? {} : { template: toJsonLocalGenericTemplateId(value.template) }),
        implements: value.implements.map((item0) => toJsonNominalHeritage(item0)),
        members: value.members.map((item0) => toJsonDefinitionMember(item0)),
    };
}

/** Return one EnumDefinition from one JSON value. */
export function fromJsonEnumDefinition(value: Json): EnumDefinition {
    const object = jsonObject(value);

    return {
        template: jsonOptional(object, "template", (value) => fromJsonLocalGenericTemplateId(value)),
        implements: jsonArray(jsonField(object, "implements")).map((item0) => fromJsonNominalHeritage(item0)),
        members: jsonArray(jsonField(object, "members")).map((item0) => fromJsonDefinitionMember(item0)),
    };
}

/** Checked declaration data for one nominal type alias. */
export type NewtypeDefinition = {
    /** The generic template declared by the newtype. */
    readonly template?: LocalGenericTemplateId;
    /** The nominal backing type. */
    readonly value: GlobalTypeId;
};

export const NewtypeDefinition = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: NewtypeDefinition): void {
        encodeNewtypeDefinition(writer, value);
    },

    /** Decode one NewtypeDefinition. */
    decode(reader: BinaryReader): NewtypeDefinition {
        return decodeNewtypeDefinition(reader);
    },

    /** Return this value as JSON. */
    toJson(value: NewtypeDefinition): Json {
        return toJsonNewtypeDefinition(value);
    },

    /** Return one NewtypeDefinition from one JSON value. */
    fromJson(value: Json): NewtypeDefinition {
        return fromJsonNewtypeDefinition(value);
    },
};

/** Encode one NewtypeDefinition. */
export function encodeNewtypeDefinition(writer: BinaryWriter, value: NewtypeDefinition): void {
    writer.writeOption(value.template, (value0) => {
        encodeLocalGenericTemplateId(writer, value0);
    });
    encodeGlobalTypeId(writer, value.value);
}

/** Decode one NewtypeDefinition. */
export function decodeNewtypeDefinition(reader: BinaryReader): NewtypeDefinition {
    const template = reader.readOption(() => decodeLocalGenericTemplateId(reader));
    const value = decodeGlobalTypeId(reader);

    return {
        ...(template === undefined ? {} : { template }),
        value,
    };
}

/** Return one JSON value for one NewtypeDefinition. */
export function toJsonNewtypeDefinition(value: NewtypeDefinition): Json {
    return {
        ...(value.template === undefined ? {} : { template: toJsonLocalGenericTemplateId(value.template) }),
        value: toJsonGlobalTypeId(value.value),
    };
}

/** Return one NewtypeDefinition from one JSON value. */
export function fromJsonNewtypeDefinition(value: Json): NewtypeDefinition {
    const object = jsonObject(value);

    return {
        template: jsonOptional(object, "template", (value) => fromJsonLocalGenericTemplateId(value)),
        value: fromJsonGlobalTypeId(jsonField(object, "value")),
    };
}
