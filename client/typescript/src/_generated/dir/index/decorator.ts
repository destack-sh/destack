/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, jsonArray, jsonField, jsonObject, jsonOptional, jsonString } from "../../../protocol/serde.js";
import type { Postings } from "./postings.js";
import type { DecoratorTarget } from "../table/decorator.js";
import type { GlobalNodeId } from "../tree/node.js";
import type { GlobalNodeIdAny } from "../tree/node.js";
import { decodePostings, encodePostings, fromJsonPostings, toJsonPostings } from "./postings.js";
import { decodeDecoratorTarget, encodeDecoratorTarget, fromJsonDecoratorTarget, toJsonDecoratorTarget } from "../table/decorator.js";
import { decodeGlobalNodeId, encodeGlobalNodeId, fromJsonGlobalNodeId, toJsonGlobalNodeId } from "../tree/node.js";
import { decodeGlobalNodeIdAny, encodeGlobalNodeIdAny, fromJsonGlobalNodeIdAny, toJsonGlobalNodeIdAny } from "../tree/node.js";

/** One indexed decorator application. */
export type DecoratorEntry = {
    /** The decorator name when syntactically known. */
    readonly name?: string;
    /** The decorator node. */
    readonly decorator: GlobalNodeId;
    /** The decorated owner node. */
    readonly owner: GlobalNodeIdAny;
    /** The resolved decorator declaration. */
    readonly target: DecoratorTarget;
};

export const DecoratorEntry = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: DecoratorEntry): void {
        encodeDecoratorEntry(writer, value);
    },

    /** Decode one DecoratorEntry. */
    decode(reader: BinaryReader): DecoratorEntry {
        return decodeDecoratorEntry(reader);
    },

    /** Return this value as JSON. */
    toJson(value: DecoratorEntry): Json {
        return toJsonDecoratorEntry(value);
    },

    /** Return one DecoratorEntry from one JSON value. */
    fromJson(value: Json): DecoratorEntry {
        return fromJsonDecoratorEntry(value);
    },
};

/** Encode one DecoratorEntry. */
export function encodeDecoratorEntry(writer: BinaryWriter, value: DecoratorEntry): void {
    writer.writeOption(value.name, (value0) => {
        writer.writeString(value0);
    });
    encodeGlobalNodeId(writer, value.decorator);
    encodeGlobalNodeIdAny(writer, value.owner);
    encodeDecoratorTarget(writer, value.target);
}

/** Decode one DecoratorEntry. */
export function decodeDecoratorEntry(reader: BinaryReader): DecoratorEntry {
    const name = reader.readOption(() => reader.readString());
    const decorator = decodeGlobalNodeId(reader);
    const owner = decodeGlobalNodeIdAny(reader);
    const target = decodeDecoratorTarget(reader);

    return {
        ...(name === undefined ? {} : { name }),
        decorator,
        owner,
        target,
    };
}

/** Return one JSON value for one DecoratorEntry. */
export function toJsonDecoratorEntry(value: DecoratorEntry): Json {
    return {
        ...(value.name === undefined ? {} : { name: value.name }),
        decorator: toJsonGlobalNodeId(value.decorator),
        owner: toJsonGlobalNodeIdAny(value.owner),
        target: toJsonDecoratorTarget(value.target),
    };
}

/** Return one DecoratorEntry from one JSON value. */
export function fromJsonDecoratorEntry(value: Json): DecoratorEntry {
    const object = jsonObject(value);

    return {
        name: jsonOptional(object, "name", (value) => jsonString(value)),
        decorator: fromJsonGlobalNodeId(jsonField(object, "decorator")),
        owner: fromJsonGlobalNodeIdAny(jsonField(object, "owner")),
        target: fromJsonDecoratorTarget(jsonField(object, "target")),
    };
}

/** Indexed decorator applications. */
export type DecoratorIndex = {
    /** The decorator applications in stable order. */
    readonly entries: ReadonlyArray<DecoratorEntry>;
};

export const DecoratorIndex = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: DecoratorIndex): void {
        encodeDecoratorIndex(writer, value);
    },

    /** Decode one DecoratorIndex. */
    decode(reader: BinaryReader): DecoratorIndex {
        return decodeDecoratorIndex(reader);
    },

    /** Return this value as JSON. */
    toJson(value: DecoratorIndex): Json {
        return toJsonDecoratorIndex(value);
    },

    /** Return one DecoratorIndex from one JSON value. */
    fromJson(value: Json): DecoratorIndex {
        return fromJsonDecoratorIndex(value);
    },
};

/** Encode one DecoratorIndex. */
export function encodeDecoratorIndex(writer: BinaryWriter, value: DecoratorIndex): void {
    writer.writeUnsigned(value.entries.length);
    for (const item0 of value.entries) {
        encodeDecoratorEntry(writer, item0);
    }
}

/** Decode one DecoratorIndex. */
export function decodeDecoratorIndex(reader: BinaryReader): DecoratorIndex {
    const entries = (() => { const length0 = reader.readNumber(); const items0: Array<DecoratorEntry> = []; for (let index = 0; index < length0; index += 1) { items0.push(decodeDecoratorEntry(reader)); } return items0; })();

    return {
        entries,
    };
}

/** Return one JSON value for one DecoratorIndex. */
export function toJsonDecoratorIndex(value: DecoratorIndex): Json {
    return {
        entries: value.entries.map((item0) => toJsonDecoratorEntry(item0)),
    };
}

/** Return one DecoratorIndex from one JSON value. */
export function fromJsonDecoratorIndex(value: Json): DecoratorIndex {
    const object = jsonObject(value);

    return {
        entries: jsonArray(jsonField(object, "entries")).map((item0) => fromJsonDecoratorEntry(item0)),
    };
}

/** Decorator postings by name. */
export type DecoratorPostings = {
    /** Named decorator postings. */
    readonly names: Postings;
};

export const DecoratorPostings = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: DecoratorPostings): void {
        encodeDecoratorPostings(writer, value);
    },

    /** Decode one DecoratorPostings. */
    decode(reader: BinaryReader): DecoratorPostings {
        return decodeDecoratorPostings(reader);
    },

    /** Return this value as JSON. */
    toJson(value: DecoratorPostings): Json {
        return toJsonDecoratorPostings(value);
    },

    /** Return one DecoratorPostings from one JSON value. */
    fromJson(value: Json): DecoratorPostings {
        return fromJsonDecoratorPostings(value);
    },
};

/** Encode one DecoratorPostings. */
export function encodeDecoratorPostings(writer: BinaryWriter, value: DecoratorPostings): void {
    encodePostings(writer, value.names);
}

/** Decode one DecoratorPostings. */
export function decodeDecoratorPostings(reader: BinaryReader): DecoratorPostings {
    const names = decodePostings(reader);

    return {
        names,
    };
}

/** Return one JSON value for one DecoratorPostings. */
export function toJsonDecoratorPostings(value: DecoratorPostings): Json {
    return {
        names: toJsonPostings(value.names),
    };
}

/** Return one DecoratorPostings from one JSON value. */
export function fromJsonDecoratorPostings(value: Json): DecoratorPostings {
    const object = jsonObject(value);

    return {
        names: fromJsonPostings(jsonField(object, "names")),
    };
}
