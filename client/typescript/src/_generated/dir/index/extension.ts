/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, jsonArray, jsonField, jsonInteger, jsonObject, jsonOptional } from "../../../protocol/serde.js";
import type { Postings } from "./postings.js";
import type { GlobalSymbolId } from "../symbol/symbol.js";
import type { ExtensionForm } from "../table/definition.js";
import type { GlobalNodeIdAny } from "../tree/node.js";
import type { GlobalTypeId } from "../type/type.js";
import type { FileId } from "../../source/file/model/file.js";
import type { Span } from "../../source/file/model/span.js";
import { decodePostings, encodePostings, fromJsonPostings, toJsonPostings } from "./postings.js";
import { decodeGlobalSymbolId, encodeGlobalSymbolId, fromJsonGlobalSymbolId, toJsonGlobalSymbolId } from "../symbol/symbol.js";
import { decodeExtensionForm, encodeExtensionForm, fromJsonExtensionForm, toJsonExtensionForm } from "../table/definition.js";
import { decodeGlobalNodeIdAny, encodeGlobalNodeIdAny, fromJsonGlobalNodeIdAny, toJsonGlobalNodeIdAny } from "../tree/node.js";
import { decodeGlobalTypeId, encodeGlobalTypeId, fromJsonGlobalTypeId, toJsonGlobalTypeId } from "../type/type.js";
import { decodeFileId, encodeFileId, fromJsonFileId, toJsonFileId } from "../../source/file/model/file.js";
import { decodeSpan, encodeSpan, fromJsonSpan, toJsonSpan } from "../../source/file/model/span.js";

/** Indexed checked extensions. */
export type ExtensionIndex = {
    /** Extensions ordered by root symbol. */
    readonly byRoot: ReadonlyArray<ExtensionEntry>;
};

export const ExtensionIndex = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ExtensionIndex): void {
        encodeExtensionIndex(writer, value);
    },

    /** Decode one ExtensionIndex. */
    decode(reader: BinaryReader): ExtensionIndex {
        return decodeExtensionIndex(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ExtensionIndex): Json {
        return toJsonExtensionIndex(value);
    },

    /** Return one ExtensionIndex from one JSON value. */
    fromJson(value: Json): ExtensionIndex {
        return fromJsonExtensionIndex(value);
    },
};

/** Encode one ExtensionIndex. */
export function encodeExtensionIndex(writer: BinaryWriter, value: ExtensionIndex): void {
    writer.writeUnsigned(value.byRoot.length);
    for (const item0 of value.byRoot) {
        encodeExtensionEntry(writer, item0);
    }
}

/** Decode one ExtensionIndex. */
export function decodeExtensionIndex(reader: BinaryReader): ExtensionIndex {
    const byRoot = (() => { const length0 = reader.readNumber(); const items0: Array<ExtensionEntry> = []; for (let index = 0; index < length0; index += 1) { items0.push(decodeExtensionEntry(reader)); } return items0; })();

    return {
        byRoot,
    };
}

/** Return one JSON value for one ExtensionIndex. */
export function toJsonExtensionIndex(value: ExtensionIndex): Json {
    return {
        byRoot: value.byRoot.map((item0) => toJsonExtensionEntry(item0)),
    };
}

/** Return one ExtensionIndex from one JSON value. */
export function fromJsonExtensionIndex(value: Json): ExtensionIndex {
    const object = jsonObject(value);

    return {
        byRoot: jsonArray(jsonField(object, "byRoot")).map((item0) => fromJsonExtensionEntry(item0)),
    };
}

/** One indexed extension. */
export type ExtensionEntry = {
    /** The extension declaration symbol. */
    readonly declaration: GlobalSymbolId;
    /** The source extension declaration node. */
    readonly source: GlobalNodeIdAny;
    /** The source file. */
    readonly file: FileId;
    /** The source range. */
    readonly span: Span;
    /** The canonical lookup root when this is a rooted extension. */
    readonly root?: GlobalSymbolId;
    /** The checked receiver type. */
    readonly ty: GlobalTypeId;
    /** The extension form. */
    readonly form: ExtensionForm;
};

export const ExtensionEntry = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ExtensionEntry): void {
        encodeExtensionEntry(writer, value);
    },

    /** Decode one ExtensionEntry. */
    decode(reader: BinaryReader): ExtensionEntry {
        return decodeExtensionEntry(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ExtensionEntry): Json {
        return toJsonExtensionEntry(value);
    },

    /** Return one ExtensionEntry from one JSON value. */
    fromJson(value: Json): ExtensionEntry {
        return fromJsonExtensionEntry(value);
    },
};

/** Encode one ExtensionEntry. */
export function encodeExtensionEntry(writer: BinaryWriter, value: ExtensionEntry): void {
    encodeGlobalSymbolId(writer, value.declaration);
    encodeGlobalNodeIdAny(writer, value.source);
    encodeFileId(writer, value.file);
    encodeSpan(writer, value.span);
    writer.writeOption(value.root, (value4) => {
        encodeGlobalSymbolId(writer, value4);
    });
    encodeGlobalTypeId(writer, value.ty);
    encodeExtensionForm(writer, value.form);
}

/** Decode one ExtensionEntry. */
export function decodeExtensionEntry(reader: BinaryReader): ExtensionEntry {
    const declaration = decodeGlobalSymbolId(reader);
    const source = decodeGlobalNodeIdAny(reader);
    const file = decodeFileId(reader);
    const span = decodeSpan(reader);
    const root = reader.readOption(() => decodeGlobalSymbolId(reader));
    const ty = decodeGlobalTypeId(reader);
    const form = decodeExtensionForm(reader);

    return {
        declaration,
        source,
        file,
        span,
        ...(root === undefined ? {} : { root }),
        ty,
        form,
    };
}

/** Return one JSON value for one ExtensionEntry. */
export function toJsonExtensionEntry(value: ExtensionEntry): Json {
    return {
        declaration: toJsonGlobalSymbolId(value.declaration),
        source: toJsonGlobalNodeIdAny(value.source),
        file: toJsonFileId(value.file),
        span: toJsonSpan(value.span),
        ...(value.root === undefined ? {} : { root: toJsonGlobalSymbolId(value.root) }),
        ty: toJsonGlobalTypeId(value.ty),
        form: toJsonExtensionForm(value.form),
    };
}

/** Return one ExtensionEntry from one JSON value. */
export function fromJsonExtensionEntry(value: Json): ExtensionEntry {
    const object = jsonObject(value);

    return {
        declaration: fromJsonGlobalSymbolId(jsonField(object, "declaration")),
        source: fromJsonGlobalNodeIdAny(jsonField(object, "source")),
        file: fromJsonFileId(jsonField(object, "file")),
        span: fromJsonSpan(jsonField(object, "span")),
        root: jsonOptional(object, "root", (value) => fromJsonGlobalSymbolId(value)),
        ty: fromJsonGlobalTypeId(jsonField(object, "ty")),
        form: fromJsonExtensionForm(jsonField(object, "form")),
    };
}

/** Extension postings by root symbol. */
export type ExtensionPostings = {
    /** Rooted extension postings. */
    readonly roots: Postings;
    /** Modules that contain blanket extensions. */
    readonly blankets: ReadonlyArray<number>;
};

export const ExtensionPostings = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ExtensionPostings): void {
        encodeExtensionPostings(writer, value);
    },

    /** Decode one ExtensionPostings. */
    decode(reader: BinaryReader): ExtensionPostings {
        return decodeExtensionPostings(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ExtensionPostings): Json {
        return toJsonExtensionPostings(value);
    },

    /** Return one ExtensionPostings from one JSON value. */
    fromJson(value: Json): ExtensionPostings {
        return fromJsonExtensionPostings(value);
    },
};

/** Encode one ExtensionPostings. */
export function encodeExtensionPostings(writer: BinaryWriter, value: ExtensionPostings): void {
    encodePostings(writer, value.roots);
    writer.writeUnsigned(value.blankets.length);
    for (const item1 of value.blankets) {
        writer.writeUnsigned(item1);
    }
}

/** Decode one ExtensionPostings. */
export function decodeExtensionPostings(reader: BinaryReader): ExtensionPostings {
    const roots = decodePostings(reader);
    const blankets = (() => { const length1 = reader.readNumber(); const items1: Array<number> = []; for (let index = 0; index < length1; index += 1) { items1.push(reader.readNumber()); } return items1; })();

    return {
        roots,
        blankets,
    };
}

/** Return one JSON value for one ExtensionPostings. */
export function toJsonExtensionPostings(value: ExtensionPostings): Json {
    return {
        roots: toJsonPostings(value.roots),
        blankets: value.blankets.map((item0) => item0),
    };
}

/** Return one ExtensionPostings from one JSON value. */
export function fromJsonExtensionPostings(value: Json): ExtensionPostings {
    const object = jsonObject(value);

    return {
        roots: fromJsonPostings(jsonField(object, "roots")),
        blankets: jsonArray(jsonField(object, "blankets")).map((item0) => jsonInteger(item0)),
    };
}
