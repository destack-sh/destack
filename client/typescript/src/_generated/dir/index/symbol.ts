/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, jsonArray, jsonField, jsonObject, jsonOptional, jsonString } from "../../../protocol/serde.js";
import type { Postings } from "./postings.js";
import type { GlobalSymbolId } from "../symbol/symbol.js";
import type { SymbolKind } from "../symbol/symbol.js";
import type { SymbolRole } from "../symbol/symbol.js";
import type { MemberKind } from "../table/member.js";
import type { GlobalNodeIdAny } from "../tree/node.js";
import type { Mutability } from "../tree/node.js";
import type { FileId } from "../../source/file/model/file.js";
import type { Span } from "../../source/file/model/span.js";
import { decodePostings, encodePostings, fromJsonPostings, toJsonPostings } from "./postings.js";
import { decodeGlobalSymbolId, encodeGlobalSymbolId, fromJsonGlobalSymbolId, toJsonGlobalSymbolId } from "../symbol/symbol.js";
import { decodeSymbolKind, encodeSymbolKind, fromJsonSymbolKind, toJsonSymbolKind } from "../symbol/symbol.js";
import { decodeSymbolRole, encodeSymbolRole, fromJsonSymbolRole, toJsonSymbolRole } from "../symbol/symbol.js";
import { decodeMemberKind, encodeMemberKind, fromJsonMemberKind, toJsonMemberKind } from "../table/member.js";
import { decodeGlobalNodeIdAny, encodeGlobalNodeIdAny, fromJsonGlobalNodeIdAny, toJsonGlobalNodeIdAny } from "../tree/node.js";
import { decodeMutability, encodeMutability, fromJsonMutability, toJsonMutability } from "../tree/node.js";
import { decodeFileId, encodeFileId, fromJsonFileId, toJsonFileId } from "../../source/file/model/file.js";
import { decodeSpan, encodeSpan, fromJsonSpan, toJsonSpan } from "../../source/file/model/span.js";

/** One indexed declared symbol. */
export type SymbolEntry = {
    /** The display name. */
    readonly name: string;
    /** The symbol kind. */
    readonly kind: SymbolKind;
    /** The declaration member kind when present. */
    readonly memberKind?: MemberKind;
    /** The symbol role. */
    readonly role: SymbolRole;
    /** The symbol id. */
    readonly symbol: GlobalSymbolId;
    /** The source node that declares the symbol. */
    readonly source: GlobalNodeIdAny;
    /** The source file. */
    readonly file: FileId;
    /** The source range. */
    readonly span: Span;
    /** The declaration name range. */
    readonly selection: Span;
    /** The containing declaration display name. */
    readonly container?: string;
    /** The binding mutability when this is a value binding. */
    readonly mutability?: Mutability;
};

export const SymbolEntry = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: SymbolEntry): void {
        encodeSymbolEntry(writer, value);
    },

    /** Decode one SymbolEntry. */
    decode(reader: BinaryReader): SymbolEntry {
        return decodeSymbolEntry(reader);
    },

    /** Return this value as JSON. */
    toJson(value: SymbolEntry): Json {
        return toJsonSymbolEntry(value);
    },

    /** Return one SymbolEntry from one JSON value. */
    fromJson(value: Json): SymbolEntry {
        return fromJsonSymbolEntry(value);
    },
};

/** Encode one SymbolEntry. */
export function encodeSymbolEntry(writer: BinaryWriter, value: SymbolEntry): void {
    writer.writeString(value.name);
    encodeSymbolKind(writer, value.kind);
    writer.writeOption(value.memberKind, (value2) => {
        encodeMemberKind(writer, value2);
    });
    encodeSymbolRole(writer, value.role);
    encodeGlobalSymbolId(writer, value.symbol);
    encodeGlobalNodeIdAny(writer, value.source);
    encodeFileId(writer, value.file);
    encodeSpan(writer, value.span);
    encodeSpan(writer, value.selection);
    writer.writeOption(value.container, (value9) => {
        writer.writeString(value9);
    });
    writer.writeOption(value.mutability, (value10) => {
        encodeMutability(writer, value10);
    });
}

/** Decode one SymbolEntry. */
export function decodeSymbolEntry(reader: BinaryReader): SymbolEntry {
    const name = reader.readString();
    const kind = decodeSymbolKind(reader);
    const memberKind = reader.readOption(() => decodeMemberKind(reader));
    const role = decodeSymbolRole(reader);
    const symbol_ = decodeGlobalSymbolId(reader);
    const source = decodeGlobalNodeIdAny(reader);
    const file = decodeFileId(reader);
    const span = decodeSpan(reader);
    const selection = decodeSpan(reader);
    const container = reader.readOption(() => reader.readString());
    const mutability = reader.readOption(() => decodeMutability(reader));

    return {
        name,
        kind,
        ...(memberKind === undefined ? {} : { memberKind }),
        role,
        symbol: symbol_,
        source,
        file,
        span,
        selection,
        ...(container === undefined ? {} : { container }),
        ...(mutability === undefined ? {} : { mutability }),
    };
}

/** Return one JSON value for one SymbolEntry. */
export function toJsonSymbolEntry(value: SymbolEntry): Json {
    return {
        name: value.name,
        kind: toJsonSymbolKind(value.kind),
        ...(value.memberKind === undefined ? {} : { memberKind: toJsonMemberKind(value.memberKind) }),
        role: toJsonSymbolRole(value.role),
        symbol: toJsonGlobalSymbolId(value.symbol),
        source: toJsonGlobalNodeIdAny(value.source),
        file: toJsonFileId(value.file),
        span: toJsonSpan(value.span),
        selection: toJsonSpan(value.selection),
        ...(value.container === undefined ? {} : { container: value.container }),
        ...(value.mutability === undefined ? {} : { mutability: toJsonMutability(value.mutability) }),
    };
}

/** Return one SymbolEntry from one JSON value. */
export function fromJsonSymbolEntry(value: Json): SymbolEntry {
    const object = jsonObject(value);

    return {
        name: jsonString(jsonField(object, "name")),
        kind: fromJsonSymbolKind(jsonField(object, "kind")),
        memberKind: jsonOptional(object, "memberKind", (value) => fromJsonMemberKind(value)),
        role: fromJsonSymbolRole(jsonField(object, "role")),
        symbol: fromJsonGlobalSymbolId(jsonField(object, "symbol")),
        source: fromJsonGlobalNodeIdAny(jsonField(object, "source")),
        file: fromJsonFileId(jsonField(object, "file")),
        span: fromJsonSpan(jsonField(object, "span")),
        selection: fromJsonSpan(jsonField(object, "selection")),
        container: jsonOptional(object, "container", (value) => jsonString(value)),
        mutability: jsonOptional(object, "mutability", (value) => fromJsonMutability(value)),
    };
}

/** Indexed declared symbols. */
export type SymbolIndex = {
    /** The symbols in stable display order. */
    readonly entries: ReadonlyArray<SymbolEntry>;
};

export const SymbolIndex = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: SymbolIndex): void {
        encodeSymbolIndex(writer, value);
    },

    /** Decode one SymbolIndex. */
    decode(reader: BinaryReader): SymbolIndex {
        return decodeSymbolIndex(reader);
    },

    /** Return this value as JSON. */
    toJson(value: SymbolIndex): Json {
        return toJsonSymbolIndex(value);
    },

    /** Return one SymbolIndex from one JSON value. */
    fromJson(value: Json): SymbolIndex {
        return fromJsonSymbolIndex(value);
    },
};

/** Encode one SymbolIndex. */
export function encodeSymbolIndex(writer: BinaryWriter, value: SymbolIndex): void {
    writer.writeUnsigned(value.entries.length);
    for (const item0 of value.entries) {
        encodeSymbolEntry(writer, item0);
    }
}

/** Decode one SymbolIndex. */
export function decodeSymbolIndex(reader: BinaryReader): SymbolIndex {
    const entries = (() => { const length0 = reader.readNumber(); const items0: Array<SymbolEntry> = []; for (let index = 0; index < length0; index += 1) { items0.push(decodeSymbolEntry(reader)); } return items0; })();

    return {
        entries,
    };
}

/** Return one JSON value for one SymbolIndex. */
export function toJsonSymbolIndex(value: SymbolIndex): Json {
    return {
        entries: value.entries.map((item0) => toJsonSymbolEntry(item0)),
    };
}

/** Return one SymbolIndex from one JSON value. */
export function fromJsonSymbolIndex(value: Json): SymbolIndex {
    const object = jsonObject(value);

    return {
        entries: jsonArray(jsonField(object, "entries")).map((item0) => fromJsonSymbolEntry(item0)),
    };
}

/** Symbol postings by name. */
export type SymbolPostings = {
    /** Symbol name postings. */
    readonly names: Postings;
};

export const SymbolPostings = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: SymbolPostings): void {
        encodeSymbolPostings(writer, value);
    },

    /** Decode one SymbolPostings. */
    decode(reader: BinaryReader): SymbolPostings {
        return decodeSymbolPostings(reader);
    },

    /** Return this value as JSON. */
    toJson(value: SymbolPostings): Json {
        return toJsonSymbolPostings(value);
    },

    /** Return one SymbolPostings from one JSON value. */
    fromJson(value: Json): SymbolPostings {
        return fromJsonSymbolPostings(value);
    },
};

/** Encode one SymbolPostings. */
export function encodeSymbolPostings(writer: BinaryWriter, value: SymbolPostings): void {
    encodePostings(writer, value.names);
}

/** Decode one SymbolPostings. */
export function decodeSymbolPostings(reader: BinaryReader): SymbolPostings {
    const names = decodePostings(reader);

    return {
        names,
    };
}

/** Return one JSON value for one SymbolPostings. */
export function toJsonSymbolPostings(value: SymbolPostings): Json {
    return {
        names: toJsonPostings(value.names),
    };
}

/** Return one SymbolPostings from one JSON value. */
export function fromJsonSymbolPostings(value: Json): SymbolPostings {
    const object = jsonObject(value);

    return {
        names: fromJsonPostings(jsonField(object, "names")),
    };
}
