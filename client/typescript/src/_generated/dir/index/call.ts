/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, jsonArray, jsonField, jsonObject, jsonOptional, jsonString } from "../../../protocol/serde.js";
import type { Postings } from "./postings.js";
import type { GlobalSymbolId } from "../symbol/symbol.js";
import type { GlobalNodeIdAny } from "../tree/node.js";
import type { Span } from "../../source/file/model/span.js";
import { decodePostings, encodePostings, fromJsonPostings, toJsonPostings } from "./postings.js";
import { decodeGlobalSymbolId, encodeGlobalSymbolId, fromJsonGlobalSymbolId, toJsonGlobalSymbolId } from "../symbol/symbol.js";
import { decodeGlobalNodeIdAny, encodeGlobalNodeIdAny, fromJsonGlobalNodeIdAny, toJsonGlobalNodeIdAny } from "../tree/node.js";
import { decodeSpan, encodeSpan, fromJsonSpan, toJsonSpan } from "../../source/file/model/span.js";

/** Call graph index. */
export type CallIndex = {
    /** The calls ordered by callee symbol. */
    readonly byCallee: ReadonlyArray<CallEntry>;
    /** The calls ordered by caller symbol. */
    readonly byCaller: ReadonlyArray<CallEntry>;
};

export const CallIndex = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: CallIndex): void {
        encodeCallIndex(writer, value);
    },

    /** Decode one CallIndex. */
    decode(reader: BinaryReader): CallIndex {
        return decodeCallIndex(reader);
    },

    /** Return this value as JSON. */
    toJson(value: CallIndex): Json {
        return toJsonCallIndex(value);
    },

    /** Return one CallIndex from one JSON value. */
    fromJson(value: Json): CallIndex {
        return fromJsonCallIndex(value);
    },
};

/** Encode one CallIndex. */
export function encodeCallIndex(writer: BinaryWriter, value: CallIndex): void {
    writer.writeUnsigned(value.byCallee.length);
    for (const item0 of value.byCallee) {
        encodeCallEntry(writer, item0);
    }
    writer.writeUnsigned(value.byCaller.length);
    for (const item1 of value.byCaller) {
        encodeCallEntry(writer, item1);
    }
}

/** Decode one CallIndex. */
export function decodeCallIndex(reader: BinaryReader): CallIndex {
    const byCallee = (() => { const length0 = reader.readNumber(); const items0: Array<CallEntry> = []; for (let index = 0; index < length0; index += 1) { items0.push(decodeCallEntry(reader)); } return items0; })();
    const byCaller = (() => { const length1 = reader.readNumber(); const items1: Array<CallEntry> = []; for (let index = 0; index < length1; index += 1) { items1.push(decodeCallEntry(reader)); } return items1; })();

    return {
        byCallee,
        byCaller,
    };
}

/** Return one JSON value for one CallIndex. */
export function toJsonCallIndex(value: CallIndex): Json {
    return {
        byCallee: value.byCallee.map((item0) => toJsonCallEntry(item0)),
        byCaller: value.byCaller.map((item0) => toJsonCallEntry(item0)),
    };
}

/** Return one CallIndex from one JSON value. */
export function fromJsonCallIndex(value: Json): CallIndex {
    const object = jsonObject(value);

    return {
        byCallee: jsonArray(jsonField(object, "byCallee")).map((item0) => fromJsonCallEntry(item0)),
        byCaller: jsonArray(jsonField(object, "byCaller")).map((item0) => fromJsonCallEntry(item0)),
    };
}

/** One call graph edge. */
export type CallEntry = {
    /** The call-like expression node. */
    readonly source: GlobalNodeIdAny;
    /** The expression or type node naming the callee. */
    readonly target: GlobalNodeIdAny;
    /** The kind of call-like operation. */
    readonly kind: CallKind;
    /** The containing function symbol when known. */
    readonly caller?: GlobalSymbolId;
    /** The called function symbol. */
    readonly callee: GlobalSymbolId;
    /** The call source range. */
    readonly span: Span;
};

export const CallEntry = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: CallEntry): void {
        encodeCallEntry(writer, value);
    },

    /** Decode one CallEntry. */
    decode(reader: BinaryReader): CallEntry {
        return decodeCallEntry(reader);
    },

    /** Return this value as JSON. */
    toJson(value: CallEntry): Json {
        return toJsonCallEntry(value);
    },

    /** Return one CallEntry from one JSON value. */
    fromJson(value: Json): CallEntry {
        return fromJsonCallEntry(value);
    },
};

/** Encode one CallEntry. */
export function encodeCallEntry(writer: BinaryWriter, value: CallEntry): void {
    encodeGlobalNodeIdAny(writer, value.source);
    encodeGlobalNodeIdAny(writer, value.target);
    encodeCallKind(writer, value.kind);
    writer.writeOption(value.caller, (value3) => {
        encodeGlobalSymbolId(writer, value3);
    });
    encodeGlobalSymbolId(writer, value.callee);
    encodeSpan(writer, value.span);
}

/** Decode one CallEntry. */
export function decodeCallEntry(reader: BinaryReader): CallEntry {
    const source = decodeGlobalNodeIdAny(reader);
    const target = decodeGlobalNodeIdAny(reader);
    const kind = decodeCallKind(reader);
    const caller = reader.readOption(() => decodeGlobalSymbolId(reader));
    const callee = decodeGlobalSymbolId(reader);
    const span = decodeSpan(reader);

    return {
        source,
        target,
        kind,
        ...(caller === undefined ? {} : { caller }),
        callee,
        span,
    };
}

/** Return one JSON value for one CallEntry. */
export function toJsonCallEntry(value: CallEntry): Json {
    return {
        source: toJsonGlobalNodeIdAny(value.source),
        target: toJsonGlobalNodeIdAny(value.target),
        kind: toJsonCallKind(value.kind),
        ...(value.caller === undefined ? {} : { caller: toJsonGlobalSymbolId(value.caller) }),
        callee: toJsonGlobalSymbolId(value.callee),
        span: toJsonSpan(value.span),
    };
}

/** Return one CallEntry from one JSON value. */
export function fromJsonCallEntry(value: Json): CallEntry {
    const object = jsonObject(value);

    return {
        source: fromJsonGlobalNodeIdAny(jsonField(object, "source")),
        target: fromJsonGlobalNodeIdAny(jsonField(object, "target")),
        kind: fromJsonCallKind(jsonField(object, "kind")),
        caller: jsonOptional(object, "caller", (value) => fromJsonGlobalSymbolId(value)),
        callee: fromJsonGlobalSymbolId(jsonField(object, "callee")),
        span: fromJsonSpan(jsonField(object, "span")),
    };
}

/** Kind of call-like operation. */
export type CallKind = "call" | "construct";

export const CallKind = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: CallKind): void {
        encodeCallKind(writer, value);
    },

    /** Decode one CallKind. */
    decode(reader: BinaryReader): CallKind {
        return decodeCallKind(reader);
    },

    /** Return this value as JSON. */
    toJson(value: CallKind): Json {
        return toJsonCallKind(value);
    },

    /** Return one CallKind from one JSON value. */
    fromJson(value: Json): CallKind {
        return fromJsonCallKind(value);
    },
};

/** Encode one CallKind. */
export function encodeCallKind(writer: BinaryWriter, value: CallKind): void {
    switch (value) {
        case "call":
            writer.writeUnsigned(0);
            return;
        case "construct":
            writer.writeUnsigned(1);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one CallKind. */
export function decodeCallKind(reader: BinaryReader): CallKind {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "call";
        case 1:
            return "construct";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one CallKind. */
export function toJsonCallKind(value: CallKind): Json {
    return value;
}

/** Return one CallKind from one JSON value. */
export function fromJsonCallKind(value: Json): CallKind {
    const variant = jsonString(value);

    switch (variant) {
        case "call":
            return "call";
        case "construct":
            return "construct";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** Call postings by caller and callee symbols. */
export type CallPostings = {
    /** Caller symbol postings. */
    readonly callers: Postings;
    /** Callee symbol postings. */
    readonly callees: Postings;
};

export const CallPostings = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: CallPostings): void {
        encodeCallPostings(writer, value);
    },

    /** Decode one CallPostings. */
    decode(reader: BinaryReader): CallPostings {
        return decodeCallPostings(reader);
    },

    /** Return this value as JSON. */
    toJson(value: CallPostings): Json {
        return toJsonCallPostings(value);
    },

    /** Return one CallPostings from one JSON value. */
    fromJson(value: Json): CallPostings {
        return fromJsonCallPostings(value);
    },
};

/** Encode one CallPostings. */
export function encodeCallPostings(writer: BinaryWriter, value: CallPostings): void {
    encodePostings(writer, value.callers);
    encodePostings(writer, value.callees);
}

/** Decode one CallPostings. */
export function decodeCallPostings(reader: BinaryReader): CallPostings {
    const callers = decodePostings(reader);
    const callees = decodePostings(reader);

    return {
        callers,
        callees,
    };
}

/** Return one JSON value for one CallPostings. */
export function toJsonCallPostings(value: CallPostings): Json {
    return {
        callers: toJsonPostings(value.callers),
        callees: toJsonPostings(value.callees),
    };
}

/** Return one CallPostings from one JSON value. */
export function fromJsonCallPostings(value: Json): CallPostings {
    const object = jsonObject(value);

    return {
        callers: fromJsonPostings(jsonField(object, "callers")),
        callees: fromJsonPostings(jsonField(object, "callees")),
    };
}
