/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, jsonArray, jsonField, jsonObject, jsonString } from "../../../protocol/serde.js";
import type { Postings } from "./postings.js";
import type { GlobalSymbolId } from "../symbol/symbol.js";
import type { GlobalNodeIdAny } from "../tree/node.js";
import type { Span } from "../../source/file/model/span.js";
import { decodePostings, encodePostings, fromJsonPostings, toJsonPostings } from "./postings.js";
import { decodeGlobalSymbolId, encodeGlobalSymbolId, fromJsonGlobalSymbolId, toJsonGlobalSymbolId } from "../symbol/symbol.js";
import { decodeGlobalNodeIdAny, encodeGlobalNodeIdAny, fromJsonGlobalNodeIdAny, toJsonGlobalNodeIdAny } from "../tree/node.js";
import { decodeSpan, encodeSpan, fromJsonSpan, toJsonSpan } from "../../source/file/model/span.js";

/** Reference occurrence index. */
export type ReferenceIndex = {
    /** The references ordered by target symbol. */
    readonly byTarget: ReadonlyArray<ReferenceEntry>;
};

export const ReferenceIndex = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ReferenceIndex): void {
        encodeReferenceIndex(writer, value);
    },

    /** Decode one ReferenceIndex. */
    decode(reader: BinaryReader): ReferenceIndex {
        return decodeReferenceIndex(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ReferenceIndex): Json {
        return toJsonReferenceIndex(value);
    },

    /** Return one ReferenceIndex from one JSON value. */
    fromJson(value: Json): ReferenceIndex {
        return fromJsonReferenceIndex(value);
    },
};

/** Encode one ReferenceIndex. */
export function encodeReferenceIndex(writer: BinaryWriter, value: ReferenceIndex): void {
    writer.writeUnsigned(value.byTarget.length);
    for (const item0 of value.byTarget) {
        encodeReferenceEntry(writer, item0);
    }
}

/** Decode one ReferenceIndex. */
export function decodeReferenceIndex(reader: BinaryReader): ReferenceIndex {
    const byTarget = (() => { const length0 = reader.readNumber(); const items0: Array<ReferenceEntry> = []; for (let index = 0; index < length0; index += 1) { items0.push(decodeReferenceEntry(reader)); } return items0; })();

    return {
        byTarget,
    };
}

/** Return one JSON value for one ReferenceIndex. */
export function toJsonReferenceIndex(value: ReferenceIndex): Json {
    return {
        byTarget: value.byTarget.map((item0) => toJsonReferenceEntry(item0)),
    };
}

/** Return one ReferenceIndex from one JSON value. */
export function fromJsonReferenceIndex(value: Json): ReferenceIndex {
    const object = jsonObject(value);

    return {
        byTarget: jsonArray(jsonField(object, "byTarget")).map((item0) => fromJsonReferenceEntry(item0)),
    };
}

/** One indexed reference occurrence. */
export type ReferenceEntry = {
    /** The referenced symbol. */
    readonly target: GlobalSymbolId;
    /** The source node that references the symbol. */
    readonly source: GlobalNodeIdAny;
    /** The source range. */
    readonly span: Span;
    /** The reference kind. */
    readonly kind: ReferenceKind;
};

export const ReferenceEntry = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ReferenceEntry): void {
        encodeReferenceEntry(writer, value);
    },

    /** Decode one ReferenceEntry. */
    decode(reader: BinaryReader): ReferenceEntry {
        return decodeReferenceEntry(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ReferenceEntry): Json {
        return toJsonReferenceEntry(value);
    },

    /** Return one ReferenceEntry from one JSON value. */
    fromJson(value: Json): ReferenceEntry {
        return fromJsonReferenceEntry(value);
    },
};

/** Encode one ReferenceEntry. */
export function encodeReferenceEntry(writer: BinaryWriter, value: ReferenceEntry): void {
    encodeGlobalSymbolId(writer, value.target);
    encodeGlobalNodeIdAny(writer, value.source);
    encodeSpan(writer, value.span);
    encodeReferenceKind(writer, value.kind);
}

/** Decode one ReferenceEntry. */
export function decodeReferenceEntry(reader: BinaryReader): ReferenceEntry {
    const target = decodeGlobalSymbolId(reader);
    const source = decodeGlobalNodeIdAny(reader);
    const span = decodeSpan(reader);
    const kind = decodeReferenceKind(reader);

    return {
        target,
        source,
        span,
        kind,
    };
}

/** Return one JSON value for one ReferenceEntry. */
export function toJsonReferenceEntry(value: ReferenceEntry): Json {
    return {
        target: toJsonGlobalSymbolId(value.target),
        source: toJsonGlobalNodeIdAny(value.source),
        span: toJsonSpan(value.span),
        kind: toJsonReferenceKind(value.kind),
    };
}

/** Return one ReferenceEntry from one JSON value. */
export function fromJsonReferenceEntry(value: Json): ReferenceEntry {
    const object = jsonObject(value);

    return {
        target: fromJsonGlobalSymbolId(jsonField(object, "target")),
        source: fromJsonGlobalNodeIdAny(jsonField(object, "source")),
        span: fromJsonSpan(jsonField(object, "span")),
        kind: fromJsonReferenceKind(jsonField(object, "kind")),
    };
}

/** Kind of indexed reference occurrence. */
export type ReferenceKind = "name" | "member" | "call" | "construct" | "dependency" | "type" | "storage";

export const ReferenceKind = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ReferenceKind): void {
        encodeReferenceKind(writer, value);
    },

    /** Decode one ReferenceKind. */
    decode(reader: BinaryReader): ReferenceKind {
        return decodeReferenceKind(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ReferenceKind): Json {
        return toJsonReferenceKind(value);
    },

    /** Return one ReferenceKind from one JSON value. */
    fromJson(value: Json): ReferenceKind {
        return fromJsonReferenceKind(value);
    },
};

/** Encode one ReferenceKind. */
export function encodeReferenceKind(writer: BinaryWriter, value: ReferenceKind): void {
    switch (value) {
        case "name":
            writer.writeUnsigned(0);
            return;
        case "member":
            writer.writeUnsigned(1);
            return;
        case "call":
            writer.writeUnsigned(2);
            return;
        case "construct":
            writer.writeUnsigned(3);
            return;
        case "dependency":
            writer.writeUnsigned(4);
            return;
        case "type":
            writer.writeUnsigned(5);
            return;
        case "storage":
            writer.writeUnsigned(6);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one ReferenceKind. */
export function decodeReferenceKind(reader: BinaryReader): ReferenceKind {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "name";
        case 1:
            return "member";
        case 2:
            return "call";
        case 3:
            return "construct";
        case 4:
            return "dependency";
        case 5:
            return "type";
        case 6:
            return "storage";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one ReferenceKind. */
export function toJsonReferenceKind(value: ReferenceKind): Json {
    return value;
}

/** Return one ReferenceKind from one JSON value. */
export function fromJsonReferenceKind(value: Json): ReferenceKind {
    const variant = jsonString(value);

    switch (variant) {
        case "name":
            return "name";
        case "member":
            return "member";
        case "call":
            return "call";
        case "construct":
            return "construct";
        case "dependency":
            return "dependency";
        case "type":
            return "type";
        case "storage":
            return "storage";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** Reference postings by target symbol. */
export type ReferencePostings = {
    /** Reference target postings. */
    readonly targets: Postings;
};

export const ReferencePostings = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ReferencePostings): void {
        encodeReferencePostings(writer, value);
    },

    /** Decode one ReferencePostings. */
    decode(reader: BinaryReader): ReferencePostings {
        return decodeReferencePostings(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ReferencePostings): Json {
        return toJsonReferencePostings(value);
    },

    /** Return one ReferencePostings from one JSON value. */
    fromJson(value: Json): ReferencePostings {
        return fromJsonReferencePostings(value);
    },
};

/** Encode one ReferencePostings. */
export function encodeReferencePostings(writer: BinaryWriter, value: ReferencePostings): void {
    encodePostings(writer, value.targets);
}

/** Decode one ReferencePostings. */
export function decodeReferencePostings(reader: BinaryReader): ReferencePostings {
    const targets = decodePostings(reader);

    return {
        targets,
    };
}

/** Return one JSON value for one ReferencePostings. */
export function toJsonReferencePostings(value: ReferencePostings): Json {
    return {
        targets: toJsonPostings(value.targets),
    };
}

/** Return one ReferencePostings from one JSON value. */
export function fromJsonReferencePostings(value: Json): ReferencePostings {
    const object = jsonObject(value);

    return {
        targets: fromJsonPostings(jsonField(object, "targets")),
    };
}
