/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, jsonArray, jsonField, jsonInteger, jsonObject, jsonOptional, jsonString } from "../../../protocol/serde.js";
import type { Postings } from "./postings.js";
import type { StaticKey } from "../symbol/key.js";
import type { GlobalSymbolId } from "../symbol/symbol.js";
import type { MemberKind } from "../table/member.js";
import type { MemberOrigin } from "../table/member.js";
import type { MemberSpace } from "../table/member.js";
import type { GlobalNodeIdAny } from "../tree/node.js";
import type { GlobalTypeId } from "../type/type.js";
import type { FileId } from "../../source/file/model/file.js";
import type { Span } from "../../source/file/model/span.js";
import { decodePostings, encodePostings, fromJsonPostings, toJsonPostings } from "./postings.js";
import { decodeStaticKey, encodeStaticKey, fromJsonStaticKey, toJsonStaticKey } from "../symbol/key.js";
import { decodeGlobalSymbolId, encodeGlobalSymbolId, fromJsonGlobalSymbolId, toJsonGlobalSymbolId } from "../symbol/symbol.js";
import { decodeMemberKind, encodeMemberKind, fromJsonMemberKind, toJsonMemberKind } from "../table/member.js";
import { decodeMemberOrigin, encodeMemberOrigin, fromJsonMemberOrigin, toJsonMemberOrigin } from "../table/member.js";
import { decodeMemberSpace, encodeMemberSpace, fromJsonMemberSpace, toJsonMemberSpace } from "../table/member.js";
import { decodeGlobalNodeIdAny, encodeGlobalNodeIdAny, fromJsonGlobalNodeIdAny, toJsonGlobalNodeIdAny } from "../tree/node.js";
import { decodeGlobalTypeId, encodeGlobalTypeId, fromJsonGlobalTypeId, toJsonGlobalTypeId } from "../type/type.js";
import { decodeFileId, encodeFileId, fromJsonFileId, toJsonFileId } from "../../source/file/model/file.js";
import { decodeSpan, encodeSpan, fromJsonSpan, toJsonSpan } from "../../source/file/model/span.js";

/** One indexed member. */
export type MemberEntry = {
    /** The member key when the member is keyed. */
    readonly key?: StaticKey;
    /** The member kind. */
    readonly kind: MemberKind;
    /** The symbol this member belongs to or extends. */
    readonly owner?: GlobalSymbolId;
    /** The symbol whose definition declares this member. */
    readonly declaring: GlobalSymbolId;
    /** The member symbol when this member declares one. */
    readonly symbol?: GlobalSymbolId;
    /** The source node that defines the member. */
    readonly source: GlobalNodeIdAny;
    /** The source file. */
    readonly file: FileId;
    /** The source range. */
    readonly span: Span;
    /** The member name range when authored. */
    readonly selection?: Span;
    /** The containing symbol display name. */
    readonly container?: string;
    /** The type of the member when known. */
    readonly ty?: GlobalTypeId;
    /** The member origin. */
    readonly origin: MemberOrigin;
    /** The member space. */
    readonly space: MemberSpace;
};

export const MemberEntry = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: MemberEntry): void {
        encodeMemberEntry(writer, value);
    },

    /** Decode one MemberEntry. */
    decode(reader: BinaryReader): MemberEntry {
        return decodeMemberEntry(reader);
    },

    /** Return this value as JSON. */
    toJson(value: MemberEntry): Json {
        return toJsonMemberEntry(value);
    },

    /** Return one MemberEntry from one JSON value. */
    fromJson(value: Json): MemberEntry {
        return fromJsonMemberEntry(value);
    },
};

/** Encode one MemberEntry. */
export function encodeMemberEntry(writer: BinaryWriter, value: MemberEntry): void {
    writer.writeOption(value.key, (value0) => {
        encodeStaticKey(writer, value0);
    });
    encodeMemberKind(writer, value.kind);
    writer.writeOption(value.owner, (value2) => {
        encodeGlobalSymbolId(writer, value2);
    });
    encodeGlobalSymbolId(writer, value.declaring);
    writer.writeOption(value.symbol, (value4) => {
        encodeGlobalSymbolId(writer, value4);
    });
    encodeGlobalNodeIdAny(writer, value.source);
    encodeFileId(writer, value.file);
    encodeSpan(writer, value.span);
    writer.writeOption(value.selection, (value8) => {
        encodeSpan(writer, value8);
    });
    writer.writeOption(value.container, (value9) => {
        writer.writeString(value9);
    });
    writer.writeOption(value.ty, (value10) => {
        encodeGlobalTypeId(writer, value10);
    });
    encodeMemberOrigin(writer, value.origin);
    encodeMemberSpace(writer, value.space);
}

/** Decode one MemberEntry. */
export function decodeMemberEntry(reader: BinaryReader): MemberEntry {
    const key = reader.readOption(() => decodeStaticKey(reader));
    const kind = decodeMemberKind(reader);
    const owner = reader.readOption(() => decodeGlobalSymbolId(reader));
    const declaring = decodeGlobalSymbolId(reader);
    const symbol_ = reader.readOption(() => decodeGlobalSymbolId(reader));
    const source = decodeGlobalNodeIdAny(reader);
    const file = decodeFileId(reader);
    const span = decodeSpan(reader);
    const selection = reader.readOption(() => decodeSpan(reader));
    const container = reader.readOption(() => reader.readString());
    const ty = reader.readOption(() => decodeGlobalTypeId(reader));
    const origin = decodeMemberOrigin(reader);
    const space = decodeMemberSpace(reader);

    return {
        ...(key === undefined ? {} : { key }),
        kind,
        ...(owner === undefined ? {} : { owner }),
        declaring,
        ...(symbol_ === undefined ? {} : { symbol: symbol_ }),
        source,
        file,
        span,
        ...(selection === undefined ? {} : { selection }),
        ...(container === undefined ? {} : { container }),
        ...(ty === undefined ? {} : { ty }),
        origin,
        space,
    };
}

/** Return one JSON value for one MemberEntry. */
export function toJsonMemberEntry(value: MemberEntry): Json {
    return {
        ...(value.key === undefined ? {} : { key: toJsonStaticKey(value.key) }),
        kind: toJsonMemberKind(value.kind),
        ...(value.owner === undefined ? {} : { owner: toJsonGlobalSymbolId(value.owner) }),
        declaring: toJsonGlobalSymbolId(value.declaring),
        ...(value.symbol === undefined ? {} : { symbol: toJsonGlobalSymbolId(value.symbol) }),
        source: toJsonGlobalNodeIdAny(value.source),
        file: toJsonFileId(value.file),
        span: toJsonSpan(value.span),
        ...(value.selection === undefined ? {} : { selection: toJsonSpan(value.selection) }),
        ...(value.container === undefined ? {} : { container: value.container }),
        ...(value.ty === undefined ? {} : { ty: toJsonGlobalTypeId(value.ty) }),
        origin: toJsonMemberOrigin(value.origin),
        space: toJsonMemberSpace(value.space),
    };
}

/** Return one MemberEntry from one JSON value. */
export function fromJsonMemberEntry(value: Json): MemberEntry {
    const object = jsonObject(value);

    return {
        key: jsonOptional(object, "key", (value) => fromJsonStaticKey(value)),
        kind: fromJsonMemberKind(jsonField(object, "kind")),
        owner: jsonOptional(object, "owner", (value) => fromJsonGlobalSymbolId(value)),
        declaring: fromJsonGlobalSymbolId(jsonField(object, "declaring")),
        symbol: jsonOptional(object, "symbol", (value) => fromJsonGlobalSymbolId(value)),
        source: fromJsonGlobalNodeIdAny(jsonField(object, "source")),
        file: fromJsonFileId(jsonField(object, "file")),
        span: fromJsonSpan(jsonField(object, "span")),
        selection: jsonOptional(object, "selection", (value) => fromJsonSpan(value)),
        container: jsonOptional(object, "container", (value) => jsonString(value)),
        ty: jsonOptional(object, "ty", (value) => fromJsonGlobalTypeId(value)),
        origin: fromJsonMemberOrigin(jsonField(object, "origin")),
        space: fromJsonMemberSpace(jsonField(object, "space")),
    };
}

/** Indexed members. */
export type MemberIndex = {
    /** The members in stable source order. */
    readonly entries: ReadonlyArray<MemberEntry>;
    /** Member indexes ordered by owner symbol. */
    readonly byOwner: ReadonlyArray<number>;
    /** Member indexes ordered by declaring symbol. */
    readonly byDeclaring: ReadonlyArray<number>;
    /** Member indexes ordered by member symbol. */
    readonly bySymbol: ReadonlyArray<number>;
};

export const MemberIndex = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: MemberIndex): void {
        encodeMemberIndex(writer, value);
    },

    /** Decode one MemberIndex. */
    decode(reader: BinaryReader): MemberIndex {
        return decodeMemberIndex(reader);
    },

    /** Return this value as JSON. */
    toJson(value: MemberIndex): Json {
        return toJsonMemberIndex(value);
    },

    /** Return one MemberIndex from one JSON value. */
    fromJson(value: Json): MemberIndex {
        return fromJsonMemberIndex(value);
    },
};

/** Encode one MemberIndex. */
export function encodeMemberIndex(writer: BinaryWriter, value: MemberIndex): void {
    writer.writeUnsigned(value.entries.length);
    for (const item0 of value.entries) {
        encodeMemberEntry(writer, item0);
    }
    writer.writeUnsigned(value.byOwner.length);
    for (const item1 of value.byOwner) {
        writer.writeUnsigned(item1);
    }
    writer.writeUnsigned(value.byDeclaring.length);
    for (const item2 of value.byDeclaring) {
        writer.writeUnsigned(item2);
    }
    writer.writeUnsigned(value.bySymbol.length);
    for (const item3 of value.bySymbol) {
        writer.writeUnsigned(item3);
    }
}

/** Decode one MemberIndex. */
export function decodeMemberIndex(reader: BinaryReader): MemberIndex {
    const entries = (() => { const length0 = reader.readNumber(); const items0: Array<MemberEntry> = []; for (let index = 0; index < length0; index += 1) { items0.push(decodeMemberEntry(reader)); } return items0; })();
    const byOwner = (() => { const length1 = reader.readNumber(); const items1: Array<number> = []; for (let index = 0; index < length1; index += 1) { items1.push(reader.readNumber()); } return items1; })();
    const byDeclaring = (() => { const length2 = reader.readNumber(); const items2: Array<number> = []; for (let index = 0; index < length2; index += 1) { items2.push(reader.readNumber()); } return items2; })();
    const bySymbol = (() => { const length3 = reader.readNumber(); const items3: Array<number> = []; for (let index = 0; index < length3; index += 1) { items3.push(reader.readNumber()); } return items3; })();

    return {
        entries,
        byOwner,
        byDeclaring,
        bySymbol,
    };
}

/** Return one JSON value for one MemberIndex. */
export function toJsonMemberIndex(value: MemberIndex): Json {
    return {
        entries: value.entries.map((item0) => toJsonMemberEntry(item0)),
        byOwner: value.byOwner.map((item0) => item0),
        byDeclaring: value.byDeclaring.map((item0) => item0),
        bySymbol: value.bySymbol.map((item0) => item0),
    };
}

/** Return one MemberIndex from one JSON value. */
export function fromJsonMemberIndex(value: Json): MemberIndex {
    const object = jsonObject(value);

    return {
        entries: jsonArray(jsonField(object, "entries")).map((item0) => fromJsonMemberEntry(item0)),
        byOwner: jsonArray(jsonField(object, "byOwner")).map((item0) => jsonInteger(item0)),
        byDeclaring: jsonArray(jsonField(object, "byDeclaring")).map((item0) => jsonInteger(item0)),
        bySymbol: jsonArray(jsonField(object, "bySymbol")).map((item0) => jsonInteger(item0)),
    };
}

/** Member postings by lookup key. */
export type MemberPostings = {
    /** Member key postings. */
    readonly keys: Postings;
    /** Member owner postings. */
    readonly owners: Postings;
    /** Member declaring symbol postings. */
    readonly declaring: Postings;
};

export const MemberPostings = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: MemberPostings): void {
        encodeMemberPostings(writer, value);
    },

    /** Decode one MemberPostings. */
    decode(reader: BinaryReader): MemberPostings {
        return decodeMemberPostings(reader);
    },

    /** Return this value as JSON. */
    toJson(value: MemberPostings): Json {
        return toJsonMemberPostings(value);
    },

    /** Return one MemberPostings from one JSON value. */
    fromJson(value: Json): MemberPostings {
        return fromJsonMemberPostings(value);
    },
};

/** Encode one MemberPostings. */
export function encodeMemberPostings(writer: BinaryWriter, value: MemberPostings): void {
    encodePostings(writer, value.keys);
    encodePostings(writer, value.owners);
    encodePostings(writer, value.declaring);
}

/** Decode one MemberPostings. */
export function decodeMemberPostings(reader: BinaryReader): MemberPostings {
    const keys = decodePostings(reader);
    const owners = decodePostings(reader);
    const declaring = decodePostings(reader);

    return {
        keys,
        owners,
        declaring,
    };
}

/** Return one JSON value for one MemberPostings. */
export function toJsonMemberPostings(value: MemberPostings): Json {
    return {
        keys: toJsonPostings(value.keys),
        owners: toJsonPostings(value.owners),
        declaring: toJsonPostings(value.declaring),
    };
}

/** Return one MemberPostings from one JSON value. */
export function fromJsonMemberPostings(value: Json): MemberPostings {
    const object = jsonObject(value);

    return {
        keys: fromJsonPostings(jsonField(object, "keys")),
        owners: fromJsonPostings(jsonField(object, "owners")),
        declaring: fromJsonPostings(jsonField(object, "declaring")),
    };
}
