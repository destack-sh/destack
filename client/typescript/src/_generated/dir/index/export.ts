/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, jsonArray, jsonField, jsonObject, jsonOptional, jsonString } from "../../../protocol/serde.js";
import type { Postings } from "./postings.js";
import type { GlobalSymbolId } from "../symbol/symbol.js";
import type { SymbolKind } from "../symbol/symbol.js";
import type { GlobalNodeIdAny } from "../tree/node.js";
import type { FileId } from "../../source/file/model/file.js";
import type { Span } from "../../source/file/model/span.js";
import { decodePostings, encodePostings, fromJsonPostings, toJsonPostings } from "./postings.js";
import { decodeGlobalSymbolId, encodeGlobalSymbolId, fromJsonGlobalSymbolId, toJsonGlobalSymbolId } from "../symbol/symbol.js";
import { decodeSymbolKind, encodeSymbolKind, fromJsonSymbolKind, toJsonSymbolKind } from "../symbol/symbol.js";
import { decodeGlobalNodeIdAny, encodeGlobalNodeIdAny, fromJsonGlobalNodeIdAny, toJsonGlobalNodeIdAny } from "../tree/node.js";
import { decodeFileId, encodeFileId, fromJsonFileId, toJsonFileId } from "../../source/file/model/file.js";
import { decodeSpan, encodeSpan, fromJsonSpan, toJsonSpan } from "../../source/file/model/span.js";

/** Indexed exported symbols. */
export type ExportIndex = {
    /** The exports in stable display order. */
    readonly entries: ReadonlyArray<ExportEntry>;
};

export const ExportIndex = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ExportIndex): void {
        encodeExportIndex(writer, value);
    },

    /** Decode one ExportIndex. */
    decode(reader: BinaryReader): ExportIndex {
        return decodeExportIndex(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ExportIndex): Json {
        return toJsonExportIndex(value);
    },

    /** Return one ExportIndex from one JSON value. */
    fromJson(value: Json): ExportIndex {
        return fromJsonExportIndex(value);
    },
};

/** Encode one ExportIndex. */
export function encodeExportIndex(writer: BinaryWriter, value: ExportIndex): void {
    writer.writeUnsigned(value.entries.length);
    for (const item0 of value.entries) {
        encodeExportEntry(writer, item0);
    }
}

/** Decode one ExportIndex. */
export function decodeExportIndex(reader: BinaryReader): ExportIndex {
    const entries = (() => { const length0 = reader.readNumber(); const items0: Array<ExportEntry> = []; for (let index = 0; index < length0; index += 1) { items0.push(decodeExportEntry(reader)); } return items0; })();

    return {
        entries,
    };
}

/** Return one JSON value for one ExportIndex. */
export function toJsonExportIndex(value: ExportIndex): Json {
    return {
        entries: value.entries.map((item0) => toJsonExportEntry(item0)),
    };
}

/** Return one ExportIndex from one JSON value. */
export function fromJsonExportIndex(value: Json): ExportIndex {
    const object = jsonObject(value);

    return {
        entries: jsonArray(jsonField(object, "entries")).map((item0) => fromJsonExportEntry(item0)),
    };
}

/** One indexed exported symbol. */
export type ExportEntry = {
    /** The exported symbol name. */
    readonly name: string;
    /** The exported symbol kind. */
    readonly kind: SymbolKind;
    /** The resolved exported symbol. */
    readonly symbol: GlobalSymbolId;
    /** The source node that exposes this export. */
    readonly source: GlobalNodeIdAny;
    /** The source file. */
    readonly file: FileId;
    /** The source range. */
    readonly span: Span;
    /** The module path to use in imports. */
    readonly modulePath?: string;
};

export const ExportEntry = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ExportEntry): void {
        encodeExportEntry(writer, value);
    },

    /** Decode one ExportEntry. */
    decode(reader: BinaryReader): ExportEntry {
        return decodeExportEntry(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ExportEntry): Json {
        return toJsonExportEntry(value);
    },

    /** Return one ExportEntry from one JSON value. */
    fromJson(value: Json): ExportEntry {
        return fromJsonExportEntry(value);
    },
};

/** Encode one ExportEntry. */
export function encodeExportEntry(writer: BinaryWriter, value: ExportEntry): void {
    writer.writeString(value.name);
    encodeSymbolKind(writer, value.kind);
    encodeGlobalSymbolId(writer, value.symbol);
    encodeGlobalNodeIdAny(writer, value.source);
    encodeFileId(writer, value.file);
    encodeSpan(writer, value.span);
    writer.writeOption(value.modulePath, (value6) => {
        writer.writeString(value6);
    });
}

/** Decode one ExportEntry. */
export function decodeExportEntry(reader: BinaryReader): ExportEntry {
    const name = reader.readString();
    const kind = decodeSymbolKind(reader);
    const symbol_ = decodeGlobalSymbolId(reader);
    const source = decodeGlobalNodeIdAny(reader);
    const file = decodeFileId(reader);
    const span = decodeSpan(reader);
    const modulePath = reader.readOption(() => reader.readString());

    return {
        name,
        kind,
        symbol: symbol_,
        source,
        file,
        span,
        ...(modulePath === undefined ? {} : { modulePath }),
    };
}

/** Return one JSON value for one ExportEntry. */
export function toJsonExportEntry(value: ExportEntry): Json {
    return {
        name: value.name,
        kind: toJsonSymbolKind(value.kind),
        symbol: toJsonGlobalSymbolId(value.symbol),
        source: toJsonGlobalNodeIdAny(value.source),
        file: toJsonFileId(value.file),
        span: toJsonSpan(value.span),
        ...(value.modulePath === undefined ? {} : { modulePath: value.modulePath }),
    };
}

/** Return one ExportEntry from one JSON value. */
export function fromJsonExportEntry(value: Json): ExportEntry {
    const object = jsonObject(value);

    return {
        name: jsonString(jsonField(object, "name")),
        kind: fromJsonSymbolKind(jsonField(object, "kind")),
        symbol: fromJsonGlobalSymbolId(jsonField(object, "symbol")),
        source: fromJsonGlobalNodeIdAny(jsonField(object, "source")),
        file: fromJsonFileId(jsonField(object, "file")),
        span: fromJsonSpan(jsonField(object, "span")),
        modulePath: jsonOptional(object, "modulePath", (value) => jsonString(value)),
    };
}

/** Export postings by name. */
export type ExportPostings = {
    /** Export name postings. */
    readonly names: Postings;
};

export const ExportPostings = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ExportPostings): void {
        encodeExportPostings(writer, value);
    },

    /** Decode one ExportPostings. */
    decode(reader: BinaryReader): ExportPostings {
        return decodeExportPostings(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ExportPostings): Json {
        return toJsonExportPostings(value);
    },

    /** Return one ExportPostings from one JSON value. */
    fromJson(value: Json): ExportPostings {
        return fromJsonExportPostings(value);
    },
};

/** Encode one ExportPostings. */
export function encodeExportPostings(writer: BinaryWriter, value: ExportPostings): void {
    encodePostings(writer, value.names);
}

/** Decode one ExportPostings. */
export function decodeExportPostings(reader: BinaryReader): ExportPostings {
    const names = decodePostings(reader);

    return {
        names,
    };
}

/** Return one JSON value for one ExportPostings. */
export function toJsonExportPostings(value: ExportPostings): Json {
    return {
        names: toJsonPostings(value.names),
    };
}

/** Return one ExportPostings from one JSON value. */
export function fromJsonExportPostings(value: Json): ExportPostings {
    const object = jsonObject(value);

    return {
        names: fromJsonPostings(jsonField(object, "names")),
    };
}
