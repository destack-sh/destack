/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, jsonArray, jsonField, jsonInteger, jsonObject, jsonString } from "../../../protocol/serde.js";

/** Compact postings from one key to module ordinals. */
export type Postings = {
    /** The sorted posting keys. */
    readonly keys: ReadonlyArray<string>;
    /** Offsets into the module ordinal list. */
    readonly offsets: ReadonlyArray<number>;
    /** Module ordinals by key. */
    readonly modules: ReadonlyArray<number>;
};

export const Postings = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: Postings): void {
        encodePostings(writer, value);
    },

    /** Decode one Postings. */
    decode(reader: BinaryReader): Postings {
        return decodePostings(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Postings): Json {
        return toJsonPostings(value);
    },

    /** Return one Postings from one JSON value. */
    fromJson(value: Json): Postings {
        return fromJsonPostings(value);
    },
};

/** Encode one Postings. */
export function encodePostings(writer: BinaryWriter, value: Postings): void {
    writer.writeUnsigned(value.keys.length);
    for (const item0 of value.keys) {
        writer.writeString(item0);
    }
    writer.writeUnsigned(value.offsets.length);
    for (const item1 of value.offsets) {
        writer.writeUnsigned(item1);
    }
    writer.writeUnsigned(value.modules.length);
    for (const item2 of value.modules) {
        writer.writeUnsigned(item2);
    }
}

/** Decode one Postings. */
export function decodePostings(reader: BinaryReader): Postings {
    const keys = (() => { const length0 = reader.readNumber(); const items0: Array<string> = []; for (let index = 0; index < length0; index += 1) { items0.push(reader.readString()); } return items0; })();
    const offsets = (() => { const length1 = reader.readNumber(); const items1: Array<number> = []; for (let index = 0; index < length1; index += 1) { items1.push(reader.readNumber()); } return items1; })();
    const modules = (() => { const length2 = reader.readNumber(); const items2: Array<number> = []; for (let index = 0; index < length2; index += 1) { items2.push(reader.readNumber()); } return items2; })();

    return {
        keys,
        offsets,
        modules,
    };
}

/** Return one JSON value for one Postings. */
export function toJsonPostings(value: Postings): Json {
    return {
        keys: value.keys.map((item0) => item0),
        offsets: value.offsets.map((item0) => item0),
        modules: value.modules.map((item0) => item0),
    };
}

/** Return one Postings from one JSON value. */
export function fromJsonPostings(value: Json): Postings {
    const object = jsonObject(value);

    return {
        keys: jsonArray(jsonField(object, "keys")).map((item0) => jsonString(item0)),
        offsets: jsonArray(jsonField(object, "offsets")).map((item0) => jsonInteger(item0)),
        modules: jsonArray(jsonField(object, "modules")).map((item0) => jsonInteger(item0)),
    };
}
