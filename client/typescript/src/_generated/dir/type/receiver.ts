/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, jsonArray, jsonField, jsonObject, jsonString } from "../../../protocol/serde.js";
import type { StaticKey } from "../symbol/key.js";
import type { GlobalSymbolId } from "../symbol/symbol.js";
import type { ScalarLiteral } from "../tree/literal.js";
import type { GenericArgumentBinding } from "./generic.js";
import type { Dereference } from "./resolution.js";
import type { VariantCase } from "./resolution.js";
import type { GlobalTypeId } from "./type.js";
import { decodeStaticKey, encodeStaticKey, fromJsonStaticKey, toJsonStaticKey } from "../symbol/key.js";
import { decodeGlobalSymbolId, encodeGlobalSymbolId, fromJsonGlobalSymbolId, toJsonGlobalSymbolId } from "../symbol/symbol.js";
import { decodeScalarLiteral, encodeScalarLiteral, fromJsonScalarLiteral, toJsonScalarLiteral } from "../tree/literal.js";
import { decodeGenericArgumentBinding, encodeGenericArgumentBinding, fromJsonGenericArgumentBinding, toJsonGenericArgumentBinding } from "./generic.js";
import { decodeDereference, encodeDereference, fromJsonDereference, toJsonDereference } from "./resolution.js";
import { decodeVariantCase, encodeVariantCase, fromJsonVariantCase, toJsonVariantCase } from "./resolution.js";
import { decodeGlobalTypeId, encodeGlobalTypeId, fromJsonGlobalTypeId, toJsonGlobalTypeId } from "./type.js";

/** One receiver and its ordered implicit transformations. */
export type AdjustedReceiver = {
    /** The receiver type before adjustment. */
    readonly source: GlobalTypeId;
    /** The ordered receiver adjustments. */
    readonly adjustments: ReadonlyArray<ReceiverAdjustment>;
};

export const AdjustedReceiver = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: AdjustedReceiver): void {
        encodeAdjustedReceiver(writer, value);
    },

    /** Decode one AdjustedReceiver. */
    decode(reader: BinaryReader): AdjustedReceiver {
        return decodeAdjustedReceiver(reader);
    },

    /** Return this value as JSON. */
    toJson(value: AdjustedReceiver): Json {
        return toJsonAdjustedReceiver(value);
    },

    /** Return one AdjustedReceiver from one JSON value. */
    fromJson(value: Json): AdjustedReceiver {
        return fromJsonAdjustedReceiver(value);
    },
};

/** Encode one AdjustedReceiver. */
export function encodeAdjustedReceiver(writer: BinaryWriter, value: AdjustedReceiver): void {
    encodeGlobalTypeId(writer, value.source);
    writer.writeUnsigned(value.adjustments.length);
    for (const item1 of value.adjustments) {
        encodeReceiverAdjustment(writer, item1);
    }
}

/** Decode one AdjustedReceiver. */
export function decodeAdjustedReceiver(reader: BinaryReader): AdjustedReceiver {
    const source = decodeGlobalTypeId(reader);
    const adjustments = (() => { const length1 = reader.readNumber(); const items1: Array<ReceiverAdjustment> = []; for (let index = 0; index < length1; index += 1) { items1.push(decodeReceiverAdjustment(reader)); } return items1; })();

    return {
        source,
        adjustments,
    };
}

/** Return one JSON value for one AdjustedReceiver. */
export function toJsonAdjustedReceiver(value: AdjustedReceiver): Json {
    return {
        source: toJsonGlobalTypeId(value.source),
        adjustments: value.adjustments.map((item0) => toJsonReceiverAdjustment(item0)),
    };
}

/** Return one AdjustedReceiver from one JSON value. */
export function fromJsonAdjustedReceiver(value: Json): AdjustedReceiver {
    const object = jsonObject(value);

    return {
        source: fromJsonGlobalTypeId(jsonField(object, "source")),
        adjustments: jsonArray(jsonField(object, "adjustments")).map((item0) => fromJsonReceiverAdjustment(item0)),
    };
}

/** Erased receiver selected for dynamic dispatch. */
export type DynamicDispatch = {
    /** The erased receiver. */
    readonly receiver: AdjustedReceiver;
    /** The interface constraint declaring the dispatch member. */
    readonly constraint: GlobalTypeId;
};

export const DynamicDispatch = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: DynamicDispatch): void {
        encodeDynamicDispatch(writer, value);
    },

    /** Decode one DynamicDispatch. */
    decode(reader: BinaryReader): DynamicDispatch {
        return decodeDynamicDispatch(reader);
    },

    /** Return this value as JSON. */
    toJson(value: DynamicDispatch): Json {
        return toJsonDynamicDispatch(value);
    },

    /** Return one DynamicDispatch from one JSON value. */
    fromJson(value: Json): DynamicDispatch {
        return fromJsonDynamicDispatch(value);
    },
};

/** Encode one DynamicDispatch. */
export function encodeDynamicDispatch(writer: BinaryWriter, value: DynamicDispatch): void {
    encodeAdjustedReceiver(writer, value.receiver);
    encodeGlobalTypeId(writer, value.constraint);
}

/** Decode one DynamicDispatch. */
export function decodeDynamicDispatch(reader: BinaryReader): DynamicDispatch {
    const receiver = decodeAdjustedReceiver(reader);
    const constraint = decodeGlobalTypeId(reader);

    return {
        receiver,
        constraint,
    };
}

/** Return one JSON value for one DynamicDispatch. */
export function toJsonDynamicDispatch(value: DynamicDispatch): Json {
    return {
        receiver: toJsonAdjustedReceiver(value.receiver),
        constraint: toJsonGlobalTypeId(value.constraint),
    };
}

/** Return one DynamicDispatch from one JSON value. */
export function fromJsonDynamicDispatch(value: Json): DynamicDispatch {
    const object = jsonObject(value);

    return {
        receiver: fromJsonAdjustedReceiver(jsonField(object, "receiver")),
        constraint: fromJsonGlobalTypeId(jsonField(object, "constraint")),
    };
}

/** Receiver selected for one member access. */
export type MemberReceiver =
    /** Member selected directly from the adjusted receiver. */
    | {
          readonly kind: "direct";
          readonly direct: AdjustedReceiver;
      }
    /** Member selected through an erased interface dispatch table. */
    | {
          readonly kind: "dynamic";
          readonly dynamic: DynamicDispatch;
      }
;

export const MemberReceiver = {
    /** Member selected directly from the adjusted receiver. */
    direct(direct: AdjustedReceiver): MemberReceiver {
        return { kind: "direct", direct };
    },

    /** Member selected through an erased interface dispatch table. */
    dynamic(dynamic: DynamicDispatch): MemberReceiver {
        return { kind: "dynamic", dynamic };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: MemberReceiver): void {
        encodeMemberReceiver(writer, value);
    },

    /** Decode one MemberReceiver. */
    decode(reader: BinaryReader): MemberReceiver {
        return decodeMemberReceiver(reader);
    },

    /** Return this value as JSON. */
    toJson(value: MemberReceiver): Json {
        return toJsonMemberReceiver(value);
    },

    /** Return one MemberReceiver from one JSON value. */
    fromJson(value: Json): MemberReceiver {
        return fromJsonMemberReceiver(value);
    },
};

/** Encode one MemberReceiver. */
export function encodeMemberReceiver(writer: BinaryWriter, value: MemberReceiver): void {
    switch (value.kind) {
        case "direct":
            writer.writeUnsigned(0);
            encodeAdjustedReceiver(writer, value.direct);
            return;
        case "dynamic":
            writer.writeUnsigned(1);
            encodeDynamicDispatch(writer, value.dynamic);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one MemberReceiver. */
export function decodeMemberReceiver(reader: BinaryReader): MemberReceiver {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const direct = decodeAdjustedReceiver(reader);

            return { kind: "direct", direct };
        }
        case 1: {
            const dynamic = decodeDynamicDispatch(reader);

            return { kind: "dynamic", dynamic };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one MemberReceiver. */
export function toJsonMemberReceiver(value: MemberReceiver): Json {
    switch (value.kind) {
        case "direct":
            return {
                kind: "direct",
                direct: toJsonAdjustedReceiver(value.direct),
            };
        case "dynamic":
            return {
                kind: "dynamic",
                dynamic: toJsonDynamicDispatch(value.dynamic),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one MemberReceiver from one JSON value. */
export function fromJsonMemberReceiver(value: Json): MemberReceiver {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "direct":
            return {
                kind,
                direct: fromJsonAdjustedReceiver(jsonField(object, "direct")),
            };
        case "dynamic":
            return {
                kind,
                dynamic: fromJsonDynamicDispatch(jsonField(object, "dynamic")),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** One implicit transformation applied before receiver selection. */
export type ReceiverAdjustment =
    /** Borrow the receiver for one method call. */
    | {
          readonly kind: "borrow";
          /** The adjusted receiver type. */
          readonly ty: GlobalTypeId;
      }
    /** Dereference the receiver before lookup. */
    | {
          readonly kind: "dereference";
          readonly dereference: Dereference;
      }
    /** Project the payload of one newtype receiver. */
    | {
          readonly kind: "newtypePayload";
          /** The selected newtype symbol. */
          readonly symbol: GlobalSymbolId;
          /** The selected generic argument bindings. */
          readonly genericArguments: ReadonlyArray<GenericArgumentBinding>;
          /** The adjusted receiver type. */
          readonly ty: GlobalTypeId;
      }
    /** Project the payload of one precise tagged variant. */
    | {
          readonly kind: "variantPayload";
          /** The selected tagged case. */
          readonly case: VariantCase;
          /** The instantiated backing arm. */
          readonly backing: GlobalTypeId;
          /** The selected discriminator field. */
          readonly discriminator: StaticKey;
          /** The discriminant value tested at runtime. */
          readonly discriminant: ScalarLiteral;
          /** The adjusted receiver type. */
          readonly ty: GlobalTypeId;
      }
;

export const ReceiverAdjustment = {
    /** Borrow the receiver for one method call. */
    borrow(ty: GlobalTypeId): ReceiverAdjustment {
        return { kind: "borrow", ty };
    },

    /** Dereference the receiver before lookup. */
    dereference(dereference: Dereference): ReceiverAdjustment {
        return { kind: "dereference", dereference };
    },

    /** Project the payload of one newtype receiver. */
    newtypePayload(symbol_: GlobalSymbolId, genericArguments: ReadonlyArray<GenericArgumentBinding>, ty: GlobalTypeId): ReceiverAdjustment {
        return { kind: "newtypePayload", symbol: symbol_, genericArguments, ty };
    },

    /** Project the payload of one precise tagged variant. */
    variantPayload(case_: VariantCase, backing: GlobalTypeId, discriminator: StaticKey, discriminant: ScalarLiteral, ty: GlobalTypeId): ReceiverAdjustment {
        return { kind: "variantPayload", case: case_, backing, discriminator, discriminant, ty };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: ReceiverAdjustment): void {
        encodeReceiverAdjustment(writer, value);
    },

    /** Decode one ReceiverAdjustment. */
    decode(reader: BinaryReader): ReceiverAdjustment {
        return decodeReceiverAdjustment(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ReceiverAdjustment): Json {
        return toJsonReceiverAdjustment(value);
    },

    /** Return one ReceiverAdjustment from one JSON value. */
    fromJson(value: Json): ReceiverAdjustment {
        return fromJsonReceiverAdjustment(value);
    },
};

/** Encode one ReceiverAdjustment. */
export function encodeReceiverAdjustment(writer: BinaryWriter, value: ReceiverAdjustment): void {
    switch (value.kind) {
        case "borrow":
            writer.writeUnsigned(0);
            encodeGlobalTypeId(writer, value.ty);
            return;
        case "dereference":
            writer.writeUnsigned(1);
            encodeDereference(writer, value.dereference);
            return;
        case "newtypePayload":
            writer.writeUnsigned(2);
            encodeGlobalSymbolId(writer, value.symbol);
            writer.writeUnsigned(value.genericArguments.length);
            for (const item1 of value.genericArguments) {
                encodeGenericArgumentBinding(writer, item1);
            }
            encodeGlobalTypeId(writer, value.ty);
            return;
        case "variantPayload":
            writer.writeUnsigned(3);
            encodeVariantCase(writer, value.case);
            encodeGlobalTypeId(writer, value.backing);
            encodeStaticKey(writer, value.discriminator);
            encodeScalarLiteral(writer, value.discriminant);
            encodeGlobalTypeId(writer, value.ty);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one ReceiverAdjustment. */
export function decodeReceiverAdjustment(reader: BinaryReader): ReceiverAdjustment {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const ty = decodeGlobalTypeId(reader);

            return {
                kind: "borrow",
                ty,
            };
        }
        case 1: {
            const dereference = decodeDereference(reader);

            return { kind: "dereference", dereference };
        }
        case 2: {
            const symbol_ = decodeGlobalSymbolId(reader);
            const genericArguments = (() => { const length1 = reader.readNumber(); const items1: Array<GenericArgumentBinding> = []; for (let index = 0; index < length1; index += 1) { items1.push(decodeGenericArgumentBinding(reader)); } return items1; })();
            const ty = decodeGlobalTypeId(reader);

            return {
                kind: "newtypePayload",
                symbol: symbol_,
                genericArguments,
                ty,
            };
        }
        case 3: {
            const case_ = decodeVariantCase(reader);
            const backing = decodeGlobalTypeId(reader);
            const discriminator = decodeStaticKey(reader);
            const discriminant = decodeScalarLiteral(reader);
            const ty = decodeGlobalTypeId(reader);

            return {
                kind: "variantPayload",
                case: case_,
                backing,
                discriminator,
                discriminant,
                ty,
            };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one ReceiverAdjustment. */
export function toJsonReceiverAdjustment(value: ReceiverAdjustment): Json {
    switch (value.kind) {
        case "borrow":
            return {
                kind: "borrow",
                ty: toJsonGlobalTypeId(value.ty),
            };
        case "dereference":
            return {
                kind: "dereference",
                dereference: toJsonDereference(value.dereference),
            };
        case "newtypePayload":
            return {
                kind: "newtypePayload",
                symbol: toJsonGlobalSymbolId(value.symbol),
                genericArguments: value.genericArguments.map((item0) => toJsonGenericArgumentBinding(item0)),
                ty: toJsonGlobalTypeId(value.ty),
            };
        case "variantPayload":
            return {
                kind: "variantPayload",
                case: toJsonVariantCase(value.case),
                backing: toJsonGlobalTypeId(value.backing),
                discriminator: toJsonStaticKey(value.discriminator),
                discriminant: toJsonScalarLiteral(value.discriminant),
                ty: toJsonGlobalTypeId(value.ty),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one ReceiverAdjustment from one JSON value. */
export function fromJsonReceiverAdjustment(value: Json): ReceiverAdjustment {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "borrow":
            return {
                kind,
                ty: fromJsonGlobalTypeId(jsonField(object, "ty")),
            };
        case "dereference":
            return {
                kind,
                dereference: fromJsonDereference(jsonField(object, "dereference")),
            };
        case "newtypePayload":
            return {
                kind,
                symbol: fromJsonGlobalSymbolId(jsonField(object, "symbol")),
                genericArguments: jsonArray(jsonField(object, "genericArguments")).map((item0) => fromJsonGenericArgumentBinding(item0)),
                ty: fromJsonGlobalTypeId(jsonField(object, "ty")),
            };
        case "variantPayload":
            return {
                kind,
                case: fromJsonVariantCase(jsonField(object, "case")),
                backing: fromJsonGlobalTypeId(jsonField(object, "backing")),
                discriminator: fromJsonStaticKey(jsonField(object, "discriminator")),
                discriminant: fromJsonScalarLiteral(jsonField(object, "discriminant")),
                ty: fromJsonGlobalTypeId(jsonField(object, "ty")),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** Receiver selected by contextual lookup, such as `this` or `super`. */
export type ReceiverDecision = {
    /** The receiver syntax kind. */
    readonly kind: ReceiverKind;
    /** The declaration that introduces the receiver. */
    readonly declaration: GlobalSymbolId;
    /** The receiver type after inference. */
    readonly ty: GlobalTypeId;
};

export const ReceiverDecision = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ReceiverDecision): void {
        encodeReceiverDecision(writer, value);
    },

    /** Decode one ReceiverDecision. */
    decode(reader: BinaryReader): ReceiverDecision {
        return decodeReceiverDecision(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ReceiverDecision): Json {
        return toJsonReceiverDecision(value);
    },

    /** Return one ReceiverDecision from one JSON value. */
    fromJson(value: Json): ReceiverDecision {
        return fromJsonReceiverDecision(value);
    },
};

/** Encode one ReceiverDecision. */
export function encodeReceiverDecision(writer: BinaryWriter, value: ReceiverDecision): void {
    encodeReceiverKind(writer, value.kind);
    encodeGlobalSymbolId(writer, value.declaration);
    encodeGlobalTypeId(writer, value.ty);
}

/** Decode one ReceiverDecision. */
export function decodeReceiverDecision(reader: BinaryReader): ReceiverDecision {
    const kind = decodeReceiverKind(reader);
    const declaration = decodeGlobalSymbolId(reader);
    const ty = decodeGlobalTypeId(reader);

    return {
        kind,
        declaration,
        ty,
    };
}

/** Return one JSON value for one ReceiverDecision. */
export function toJsonReceiverDecision(value: ReceiverDecision): Json {
    return {
        kind: toJsonReceiverKind(value.kind),
        declaration: toJsonGlobalSymbolId(value.declaration),
        ty: toJsonGlobalTypeId(value.ty),
    };
}

/** Return one ReceiverDecision from one JSON value. */
export function fromJsonReceiverDecision(value: Json): ReceiverDecision {
    const object = jsonObject(value);

    return {
        kind: fromJsonReceiverKind(jsonField(object, "kind")),
        declaration: fromJsonGlobalSymbolId(jsonField(object, "declaration")),
        ty: fromJsonGlobalTypeId(jsonField(object, "ty")),
    };
}

/** Receiver syntax resolved by contextual lookup. */
export type ReceiverKind = "this" | "super";

export const ReceiverKind = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ReceiverKind): void {
        encodeReceiverKind(writer, value);
    },

    /** Decode one ReceiverKind. */
    decode(reader: BinaryReader): ReceiverKind {
        return decodeReceiverKind(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ReceiverKind): Json {
        return toJsonReceiverKind(value);
    },

    /** Return one ReceiverKind from one JSON value. */
    fromJson(value: Json): ReceiverKind {
        return fromJsonReceiverKind(value);
    },
};

/** Encode one ReceiverKind. */
export function encodeReceiverKind(writer: BinaryWriter, value: ReceiverKind): void {
    switch (value) {
        case "this":
            writer.writeUnsigned(0);
            return;
        case "super":
            writer.writeUnsigned(1);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one ReceiverKind. */
export function decodeReceiverKind(reader: BinaryReader): ReceiverKind {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "this";
        case 1:
            return "super";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one ReceiverKind. */
export function toJsonReceiverKind(value: ReceiverKind): Json {
    return value;
}

/** Return one ReceiverKind from one JSON value. */
export function fromJsonReceiverKind(value: Json): ReceiverKind {
    const variant = jsonString(value);

    switch (variant) {
        case "this":
            return "this";
        case "super":
            return "super";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}
