/* generated by destack_client_generate, do not edit */

import {
    BinaryReader,
    BinaryWriter,
    Json,
    SerdeError,
    jsonArray,
    jsonField,
    jsonObject,
    jsonString,
} from "../../../protocol/serde.js";
import type { StaticKey } from "../symbol/key.js";
import type { GlobalSymbolId } from "../symbol/symbol.js";
import {
    decodeStaticKey,
    encodeStaticKey,
    fromJsonStaticKey,
    toJsonStaticKey,
} from "../symbol/key.js";
import {
    decodeGlobalSymbolId,
    encodeGlobalSymbolId,
    fromJsonGlobalSymbolId,
    toJsonGlobalSymbolId,
} from "../symbol/symbol.js";

/** Structural path to one value access. */
export type AccessPath = {
    /** The root value. */
    readonly root: AccessRoot;
    /** The keys projected from the root. */
    readonly keys: ReadonlyArray<StaticKey>;
};

export const AccessPath = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: AccessPath): void {
        encodeAccessPath(writer, value);
    },

    /** Decode one AccessPath. */
    decode(reader: BinaryReader): AccessPath {
        return decodeAccessPath(reader);
    },

    /** Return this value as JSON. */
    toJson(value: AccessPath): Json {
        return toJsonAccessPath(value);
    },

    /** Return one AccessPath from one JSON value. */
    fromJson(value: Json): AccessPath {
        return fromJsonAccessPath(value);
    },
};

/** Encode one AccessPath. */
export function encodeAccessPath(writer: BinaryWriter, value: AccessPath): void {
    encodeAccessRoot(writer, value.root);
    writer.writeUnsigned(value.keys.length);
    for (const item1 of value.keys) {
        encodeStaticKey(writer, item1);
    }
}

/** Decode one AccessPath. */
export function decodeAccessPath(reader: BinaryReader): AccessPath {
    const root = decodeAccessRoot(reader);
    const keys = (() => {
        const length1 = reader.readNumber();
        const items1: Array<StaticKey> = [];
        for (let index = 0; index < length1; index += 1) {
            items1.push(decodeStaticKey(reader));
        }
        return items1;
    })();

    return {
        root,
        keys,
    };
}

/** Return one JSON value for one AccessPath. */
export function toJsonAccessPath(value: AccessPath): Json {
    return {
        root: toJsonAccessRoot(value.root),
        keys: value.keys.map((item0) => toJsonStaticKey(item0)),
    };
}

/** Return one AccessPath from one JSON value. */
export function fromJsonAccessPath(value: Json): AccessPath {
    const object = jsonObject(value);

    return {
        root: fromJsonAccessRoot(jsonField(object, "root")),
        keys: jsonArray(jsonField(object, "keys")).map((item0) => fromJsonStaticKey(item0)),
    };
}

/** Stable storage access selected by check. */
export type AccessResolution = {
    /** The selected structural path. */
    readonly path: AccessPath;
};

export const AccessResolution = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: AccessResolution): void {
        encodeAccessResolution(writer, value);
    },

    /** Decode one AccessResolution. */
    decode(reader: BinaryReader): AccessResolution {
        return decodeAccessResolution(reader);
    },

    /** Return this value as JSON. */
    toJson(value: AccessResolution): Json {
        return toJsonAccessResolution(value);
    },

    /** Return one AccessResolution from one JSON value. */
    fromJson(value: Json): AccessResolution {
        return fromJsonAccessResolution(value);
    },
};

/** Encode one AccessResolution. */
export function encodeAccessResolution(writer: BinaryWriter, value: AccessResolution): void {
    encodeAccessPath(writer, value.path);
}

/** Decode one AccessResolution. */
export function decodeAccessResolution(reader: BinaryReader): AccessResolution {
    const path = decodeAccessPath(reader);

    return {
        path,
    };
}

/** Return one JSON value for one AccessResolution. */
export function toJsonAccessResolution(value: AccessResolution): Json {
    return {
        path: toJsonAccessPath(value.path),
    };
}

/** Return one AccessResolution from one JSON value. */
export function fromJsonAccessResolution(value: Json): AccessResolution {
    const object = jsonObject(value);

    return {
        path: fromJsonAccessPath(jsonField(object, "path")),
    };
}

/** Root of one value access path. */
export type AccessRoot =
    /** A source binding symbol. */
    | {
          readonly kind: "symbol";
          readonly symbol: GlobalSymbolId;
      }
    /** The contextual receiver instance, named by `this` and by `super`. */
    | {
          readonly kind: "receiver";
      };

export const AccessRoot = {
    /** A source binding symbol. */
    symbol(symbol_: GlobalSymbolId): AccessRoot {
        return { kind: "symbol", symbol: symbol_ };
    },

    /** The contextual receiver instance, named by `this` and by `super`. */
    receiver(): AccessRoot {
        return { kind: "receiver" };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: AccessRoot): void {
        encodeAccessRoot(writer, value);
    },

    /** Decode one AccessRoot. */
    decode(reader: BinaryReader): AccessRoot {
        return decodeAccessRoot(reader);
    },

    /** Return this value as JSON. */
    toJson(value: AccessRoot): Json {
        return toJsonAccessRoot(value);
    },

    /** Return one AccessRoot from one JSON value. */
    fromJson(value: Json): AccessRoot {
        return fromJsonAccessRoot(value);
    },
};

/** Encode one AccessRoot. */
export function encodeAccessRoot(writer: BinaryWriter, value: AccessRoot): void {
    switch (value.kind) {
        case "symbol":
            writer.writeUnsigned(0);
            encodeGlobalSymbolId(writer, value.symbol);
            return;
        case "receiver":
            writer.writeUnsigned(1);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one AccessRoot. */
export function decodeAccessRoot(reader: BinaryReader): AccessRoot {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const symbol_ = decodeGlobalSymbolId(reader);

            return { kind: "symbol", symbol: symbol_ };
        }
        case 1: {
            return { kind: "receiver" };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one AccessRoot. */
export function toJsonAccessRoot(value: AccessRoot): Json {
    switch (value.kind) {
        case "symbol":
            return {
                kind: "symbol",
                symbol: toJsonGlobalSymbolId(value.symbol),
            };
        case "receiver":
            return {
                kind: "receiver",
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one AccessRoot from one JSON value. */
export function fromJsonAccessRoot(value: Json): AccessRoot {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "symbol":
            return {
                kind,
                symbol: fromJsonGlobalSymbolId(jsonField(object, "symbol")),
            };
        case "receiver":
            return {
                kind,
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}
