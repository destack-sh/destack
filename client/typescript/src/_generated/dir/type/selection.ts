/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, jsonArray, jsonField, jsonObject } from "../../../protocol/serde.js";
import type { GlobalSymbolId } from "../symbol/symbol.js";
import type { GenericArgumentBinding } from "./generic.js";
import { decodeGlobalSymbolId, encodeGlobalSymbolId, fromJsonGlobalSymbolId, toJsonGlobalSymbolId } from "../symbol/symbol.js";
import { decodeGenericArgumentBinding, encodeGenericArgumentBinding, fromJsonGenericArgumentBinding, toJsonGenericArgumentBinding } from "./generic.js";

/** One declaration selected with its generic argument bindings. */
export type Selection = {
    /** The selected declaration. */
    readonly symbol: GlobalSymbolId;
    /** The selected generic argument bindings. */
    readonly arguments: ReadonlyArray<GenericArgumentBinding>;
};

export const Selection = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: Selection): void {
        encodeSelection(writer, value);
    },

    /** Decode one Selection. */
    decode(reader: BinaryReader): Selection {
        return decodeSelection(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Selection): Json {
        return toJsonSelection(value);
    },

    /** Return one Selection from one JSON value. */
    fromJson(value: Json): Selection {
        return fromJsonSelection(value);
    },
};

/** Encode one Selection. */
export function encodeSelection(writer: BinaryWriter, value: Selection): void {
    encodeGlobalSymbolId(writer, value.symbol);
    writer.writeUnsigned(value.arguments.length);
    for (const item1 of value.arguments) {
        encodeGenericArgumentBinding(writer, item1);
    }
}

/** Decode one Selection. */
export function decodeSelection(reader: BinaryReader): Selection {
    const symbol_ = decodeGlobalSymbolId(reader);
    const arguments_ = (() => { const length1 = reader.readNumber(); const items1: Array<GenericArgumentBinding> = []; for (let index = 0; index < length1; index += 1) { items1.push(decodeGenericArgumentBinding(reader)); } return items1; })();

    return {
        symbol: symbol_,
        arguments: arguments_,
    };
}

/** Return one JSON value for one Selection. */
export function toJsonSelection(value: Selection): Json {
    return {
        symbol: toJsonGlobalSymbolId(value.symbol),
        arguments: value.arguments.map((item0) => toJsonGenericArgumentBinding(item0)),
    };
}

/** Return one Selection from one JSON value. */
export function fromJsonSelection(value: Json): Selection {
    const object = jsonObject(value);

    return {
        symbol: fromJsonGlobalSymbolId(jsonField(object, "symbol")),
        arguments: jsonArray(jsonField(object, "arguments")).map((item0) => fromJsonGenericArgumentBinding(item0)),
    };
}
