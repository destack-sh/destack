/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, jsonArray, jsonField, jsonObject, jsonOptional, jsonString } from "../../../protocol/serde.js";
import type { StaticKey } from "../symbol/key.js";
import type { GlobalSymbolId } from "../symbol/symbol.js";
import type { ScalarLiteral } from "../tree/literal.js";
import type { GlobalNodeIdAny } from "../tree/node.js";
import type { GenericArgumentBinding } from "./generic.js";
import type { CallResolution } from "./resolution.js";
import type { MemberResolution } from "./resolution.js";
import type { Access } from "./type.js";
import type { GlobalTypeId } from "./type.js";
import { decodeStaticKey, encodeStaticKey, fromJsonStaticKey, toJsonStaticKey } from "../symbol/key.js";
import { decodeGlobalSymbolId, encodeGlobalSymbolId, fromJsonGlobalSymbolId, toJsonGlobalSymbolId } from "../symbol/symbol.js";
import { decodeScalarLiteral, encodeScalarLiteral, fromJsonScalarLiteral, toJsonScalarLiteral } from "../tree/literal.js";
import { decodeGlobalNodeIdAny, encodeGlobalNodeIdAny, fromJsonGlobalNodeIdAny, toJsonGlobalNodeIdAny } from "../tree/node.js";
import { decodeGenericArgumentBinding, encodeGenericArgumentBinding, fromJsonGenericArgumentBinding, toJsonGenericArgumentBinding } from "./generic.js";
import { decodeCallResolution, encodeCallResolution, fromJsonCallResolution, toJsonCallResolution } from "./resolution.js";
import { decodeMemberResolution, encodeMemberResolution, fromJsonMemberResolution, toJsonMemberResolution } from "./resolution.js";
import { decodeAccess, encodeAccess, fromJsonAccess, toJsonAccess } from "./type.js";
import { decodeGlobalTypeId, encodeGlobalTypeId, fromJsonGlobalTypeId, toJsonGlobalTypeId } from "./type.js";

/** Value projection selected during checking. */
export type Projection =
    /** Extract one static layout field from an aggregate value. */
    | {
          readonly kind: "fieldGet";
          /** The selected field. */
          readonly field: ProjectionField;
          /** The projected value type. */
          readonly ty: GlobalTypeId;
      }
    /** Read one accessor-backed property value. */
    | {
          readonly kind: "propertyGet";
          /** The selected getter member. */
          readonly read: MemberResolution;
          /** The projected value type. */
          readonly ty: GlobalTypeId;
      }
    /** Read one dynamically selected subscript value. */
    | {
          readonly kind: "subscriptGet";
          /** The source node providing the subscript key. */
          readonly index: GlobalNodeIdAny;
          /** The selected subscript operation. */
          readonly read: SubscriptOperation;
          /** The projected value type. */
          readonly ty: GlobalTypeId;
      }
    /** Read one value through a selected call. */
    | {
          readonly kind: "call";
          /** The selected call operation. */
          readonly call: CallResolution;
          /** The returned value type. */
          readonly ty: GlobalTypeId;
      }
    /** Materialize one object rest value from selected fields. */
    | {
          readonly kind: "objectRest";
          /** The selected source field projections. */
          readonly fields: ReadonlyArray<ObjectRestField>;
          /** The materialized rest value type. */
          readonly ty: GlobalTypeId;
      }
    /** Read the runtime length from a slice descriptor. */
    | {
          readonly kind: "sliceLength";
          /** The projected length type. */
          readonly ty: GlobalTypeId;
      }
    /** Read the erased payload from a dynamic value. */
    | {
          readonly kind: "dynamicPayload";
          /** The projected payload type. */
          readonly ty: GlobalTypeId;
      }
    /** Read the concrete type id from a dynamic value. */
    | {
          readonly kind: "dynamicType";
          /** The projected type descriptor type. */
          readonly ty: GlobalTypeId;
      }
    /** Read the active tag from a physical tagged sum value. */
    | {
          readonly kind: "variantTag";
          /** The projected tag type. */
          readonly ty: GlobalTypeId;
      }
    /** Extract the payload selected by one concrete variant tag. */
    | {
          readonly kind: "variantPayload";
          /** The selected tagged case. */
          readonly case: VariantCase;
          /** The selected generic argument bindings for the selected owner. */
          readonly genericArguments: ReadonlyArray<GenericArgumentBinding>;
          /** The discriminant value tested at runtime. */
          readonly discriminant: ScalarLiteral;
          /** The projected payload type. */
          readonly ty: GlobalTypeId;
      }
    /** Unwrap one newtype payload. */
    | {
          readonly kind: "newtypePayload";
          /** The selected newtype symbol. */
          readonly symbol: GlobalSymbolId;
          /** The selected generic argument bindings for the selected newtype. */
          readonly genericArguments: ReadonlyArray<GenericArgumentBinding>;
          /** The projected payload type. */
          readonly ty: GlobalTypeId;
      }
    /** Borrow the input before matching it. */
    | {
          readonly kind: "borrow";
          /** The requested borrow access, if source explicit. */
          readonly access?: Access;
          /** The projected borrow type. */
          readonly ty: GlobalTypeId;
      }
    /** Move the input before matching it. */
    | {
          readonly kind: "move";
          /** The requested move access, if source explicit. */
          readonly access?: Access;
          /** The projected moved type. */
          readonly ty: GlobalTypeId;
      }
    /** Dereference the input before matching it. */
    | {
          readonly kind: "dereference";
          /** The selected dereference operation. */
          readonly read: DereferenceOperation;
          /** The projected pointee type. */
          readonly ty: GlobalTypeId;
      }
    /** Duplicate one copyable value out of a place or view. */
    | {
          readonly kind: "copy";
          /** The duplicated value type. */
          readonly ty: GlobalTypeId;
      }
;

export const Projection = {
    /** Extract one static layout field from an aggregate value. */
    fieldGet(field: ProjectionField, ty: GlobalTypeId): Projection {
        return { kind: "fieldGet", field, ty };
    },

    /** Read one accessor-backed property value. */
    propertyGet(read: MemberResolution, ty: GlobalTypeId): Projection {
        return { kind: "propertyGet", read, ty };
    },

    /** Read one dynamically selected subscript value. */
    subscriptGet(index: GlobalNodeIdAny, read: SubscriptOperation, ty: GlobalTypeId): Projection {
        return { kind: "subscriptGet", index, read, ty };
    },

    /** Read one value through a selected call. */
    call(call: CallResolution, ty: GlobalTypeId): Projection {
        return { kind: "call", call, ty };
    },

    /** Materialize one object rest value from selected fields. */
    objectRest(fields: ReadonlyArray<ObjectRestField>, ty: GlobalTypeId): Projection {
        return { kind: "objectRest", fields, ty };
    },

    /** Read the runtime length from a slice descriptor. */
    sliceLength(ty: GlobalTypeId): Projection {
        return { kind: "sliceLength", ty };
    },

    /** Read the erased payload from a dynamic value. */
    dynamicPayload(ty: GlobalTypeId): Projection {
        return { kind: "dynamicPayload", ty };
    },

    /** Read the concrete type id from a dynamic value. */
    dynamicType(ty: GlobalTypeId): Projection {
        return { kind: "dynamicType", ty };
    },

    /** Read the active tag from a physical tagged sum value. */
    variantTag(ty: GlobalTypeId): Projection {
        return { kind: "variantTag", ty };
    },

    /** Extract the payload selected by one concrete variant tag. */
    variantPayload(case_: VariantCase, genericArguments: ReadonlyArray<GenericArgumentBinding>, discriminant: ScalarLiteral, ty: GlobalTypeId): Projection {
        return { kind: "variantPayload", case: case_, genericArguments, discriminant, ty };
    },

    /** Unwrap one newtype payload. */
    newtypePayload(symbol_: GlobalSymbolId, genericArguments: ReadonlyArray<GenericArgumentBinding>, ty: GlobalTypeId): Projection {
        return { kind: "newtypePayload", symbol: symbol_, genericArguments, ty };
    },

    /** Borrow the input before matching it. */
    borrow(access: Access | undefined, ty: GlobalTypeId): Projection {
        return { kind: "borrow", access, ty };
    },

    /** Move the input before matching it. */
    move(access: Access | undefined, ty: GlobalTypeId): Projection {
        return { kind: "move", access, ty };
    },

    /** Dereference the input before matching it. */
    dereference(read: DereferenceOperation, ty: GlobalTypeId): Projection {
        return { kind: "dereference", read, ty };
    },

    /** Duplicate one copyable value out of a place or view. */
    copy(ty: GlobalTypeId): Projection {
        return { kind: "copy", ty };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: Projection): void {
        encodeProjection(writer, value);
    },

    /** Decode one Projection. */
    decode(reader: BinaryReader): Projection {
        return decodeProjection(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Projection): Json {
        return toJsonProjection(value);
    },

    /** Return one Projection from one JSON value. */
    fromJson(value: Json): Projection {
        return fromJsonProjection(value);
    },
};

/** Encode one Projection. */
export function encodeProjection(writer: BinaryWriter, value: Projection): void {
    switch (value.kind) {
        case "fieldGet":
            writer.writeUnsigned(0);
            encodeProjectionField(writer, value.field);
            encodeGlobalTypeId(writer, value.ty);
            return;
        case "propertyGet":
            writer.writeUnsigned(1);
            encodeMemberResolution(writer, value.read);
            encodeGlobalTypeId(writer, value.ty);
            return;
        case "subscriptGet":
            writer.writeUnsigned(2);
            encodeGlobalNodeIdAny(writer, value.index);
            encodeSubscriptOperation(writer, value.read);
            encodeGlobalTypeId(writer, value.ty);
            return;
        case "call":
            writer.writeUnsigned(3);
            encodeCallResolution(writer, value.call);
            encodeGlobalTypeId(writer, value.ty);
            return;
        case "objectRest":
            writer.writeUnsigned(4);
            writer.writeUnsigned(value.fields.length);
            for (const item0 of value.fields) {
                encodeObjectRestField(writer, item0);
            }
            encodeGlobalTypeId(writer, value.ty);
            return;
        case "sliceLength":
            writer.writeUnsigned(5);
            encodeGlobalTypeId(writer, value.ty);
            return;
        case "dynamicPayload":
            writer.writeUnsigned(6);
            encodeGlobalTypeId(writer, value.ty);
            return;
        case "dynamicType":
            writer.writeUnsigned(7);
            encodeGlobalTypeId(writer, value.ty);
            return;
        case "variantTag":
            writer.writeUnsigned(8);
            encodeGlobalTypeId(writer, value.ty);
            return;
        case "variantPayload":
            writer.writeUnsigned(9);
            encodeVariantCase(writer, value.case);
            writer.writeUnsigned(value.genericArguments.length);
            for (const item1 of value.genericArguments) {
                encodeGenericArgumentBinding(writer, item1);
            }
            encodeScalarLiteral(writer, value.discriminant);
            encodeGlobalTypeId(writer, value.ty);
            return;
        case "newtypePayload":
            writer.writeUnsigned(10);
            encodeGlobalSymbolId(writer, value.symbol);
            writer.writeUnsigned(value.genericArguments.length);
            for (const item1 of value.genericArguments) {
                encodeGenericArgumentBinding(writer, item1);
            }
            encodeGlobalTypeId(writer, value.ty);
            return;
        case "borrow":
            writer.writeUnsigned(11);
            writer.writeOption(value.access, (value0) => {
                encodeAccess(writer, value0);
            });
            encodeGlobalTypeId(writer, value.ty);
            return;
        case "move":
            writer.writeUnsigned(12);
            writer.writeOption(value.access, (value0) => {
                encodeAccess(writer, value0);
            });
            encodeGlobalTypeId(writer, value.ty);
            return;
        case "dereference":
            writer.writeUnsigned(13);
            encodeDereferenceOperation(writer, value.read);
            encodeGlobalTypeId(writer, value.ty);
            return;
        case "copy":
            writer.writeUnsigned(14);
            encodeGlobalTypeId(writer, value.ty);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one Projection. */
export function decodeProjection(reader: BinaryReader): Projection {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const field = decodeProjectionField(reader);
            const ty = decodeGlobalTypeId(reader);

            return {
                kind: "fieldGet",
                field,
                ty,
            };
        }
        case 1: {
            const read = decodeMemberResolution(reader);
            const ty = decodeGlobalTypeId(reader);

            return {
                kind: "propertyGet",
                read,
                ty,
            };
        }
        case 2: {
            const index = decodeGlobalNodeIdAny(reader);
            const read = decodeSubscriptOperation(reader);
            const ty = decodeGlobalTypeId(reader);

            return {
                kind: "subscriptGet",
                index,
                read,
                ty,
            };
        }
        case 3: {
            const call = decodeCallResolution(reader);
            const ty = decodeGlobalTypeId(reader);

            return {
                kind: "call",
                call,
                ty,
            };
        }
        case 4: {
            const fields = (() => { const length0 = reader.readNumber(); const items0: Array<ObjectRestField> = []; for (let index = 0; index < length0; index += 1) { items0.push(decodeObjectRestField(reader)); } return items0; })();
            const ty = decodeGlobalTypeId(reader);

            return {
                kind: "objectRest",
                fields,
                ty,
            };
        }
        case 5: {
            const ty = decodeGlobalTypeId(reader);

            return {
                kind: "sliceLength",
                ty,
            };
        }
        case 6: {
            const ty = decodeGlobalTypeId(reader);

            return {
                kind: "dynamicPayload",
                ty,
            };
        }
        case 7: {
            const ty = decodeGlobalTypeId(reader);

            return {
                kind: "dynamicType",
                ty,
            };
        }
        case 8: {
            const ty = decodeGlobalTypeId(reader);

            return {
                kind: "variantTag",
                ty,
            };
        }
        case 9: {
            const case_ = decodeVariantCase(reader);
            const genericArguments = (() => { const length1 = reader.readNumber(); const items1: Array<GenericArgumentBinding> = []; for (let index = 0; index < length1; index += 1) { items1.push(decodeGenericArgumentBinding(reader)); } return items1; })();
            const discriminant = decodeScalarLiteral(reader);
            const ty = decodeGlobalTypeId(reader);

            return {
                kind: "variantPayload",
                case: case_,
                genericArguments,
                discriminant,
                ty,
            };
        }
        case 10: {
            const symbol_ = decodeGlobalSymbolId(reader);
            const genericArguments = (() => { const length1 = reader.readNumber(); const items1: Array<GenericArgumentBinding> = []; for (let index = 0; index < length1; index += 1) { items1.push(decodeGenericArgumentBinding(reader)); } return items1; })();
            const ty = decodeGlobalTypeId(reader);

            return {
                kind: "newtypePayload",
                symbol: symbol_,
                genericArguments,
                ty,
            };
        }
        case 11: {
            const access = reader.readOption(() => decodeAccess(reader));
            const ty = decodeGlobalTypeId(reader);

            return {
                kind: "borrow",
                ...(access === undefined ? {} : { access }),
                ty,
            };
        }
        case 12: {
            const access = reader.readOption(() => decodeAccess(reader));
            const ty = decodeGlobalTypeId(reader);

            return {
                kind: "move",
                ...(access === undefined ? {} : { access }),
                ty,
            };
        }
        case 13: {
            const read = decodeDereferenceOperation(reader);
            const ty = decodeGlobalTypeId(reader);

            return {
                kind: "dereference",
                read,
                ty,
            };
        }
        case 14: {
            const ty = decodeGlobalTypeId(reader);

            return {
                kind: "copy",
                ty,
            };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one Projection. */
export function toJsonProjection(value: Projection): Json {
    switch (value.kind) {
        case "fieldGet":
            return {
                kind: "fieldGet",
                field: toJsonProjectionField(value.field),
                ty: toJsonGlobalTypeId(value.ty),
            };
        case "propertyGet":
            return {
                kind: "propertyGet",
                read: toJsonMemberResolution(value.read),
                ty: toJsonGlobalTypeId(value.ty),
            };
        case "subscriptGet":
            return {
                kind: "subscriptGet",
                index: toJsonGlobalNodeIdAny(value.index),
                read: toJsonSubscriptOperation(value.read),
                ty: toJsonGlobalTypeId(value.ty),
            };
        case "call":
            return {
                kind: "call",
                call: toJsonCallResolution(value.call),
                ty: toJsonGlobalTypeId(value.ty),
            };
        case "objectRest":
            return {
                kind: "objectRest",
                fields: value.fields.map((item0) => toJsonObjectRestField(item0)),
                ty: toJsonGlobalTypeId(value.ty),
            };
        case "sliceLength":
            return {
                kind: "sliceLength",
                ty: toJsonGlobalTypeId(value.ty),
            };
        case "dynamicPayload":
            return {
                kind: "dynamicPayload",
                ty: toJsonGlobalTypeId(value.ty),
            };
        case "dynamicType":
            return {
                kind: "dynamicType",
                ty: toJsonGlobalTypeId(value.ty),
            };
        case "variantTag":
            return {
                kind: "variantTag",
                ty: toJsonGlobalTypeId(value.ty),
            };
        case "variantPayload":
            return {
                kind: "variantPayload",
                case: toJsonVariantCase(value.case),
                genericArguments: value.genericArguments.map((item0) => toJsonGenericArgumentBinding(item0)),
                discriminant: toJsonScalarLiteral(value.discriminant),
                ty: toJsonGlobalTypeId(value.ty),
            };
        case "newtypePayload":
            return {
                kind: "newtypePayload",
                symbol: toJsonGlobalSymbolId(value.symbol),
                genericArguments: value.genericArguments.map((item0) => toJsonGenericArgumentBinding(item0)),
                ty: toJsonGlobalTypeId(value.ty),
            };
        case "borrow":
            return {
                kind: "borrow",
                ...(value.access === undefined ? {} : { access: toJsonAccess(value.access) }),
                ty: toJsonGlobalTypeId(value.ty),
            };
        case "move":
            return {
                kind: "move",
                ...(value.access === undefined ? {} : { access: toJsonAccess(value.access) }),
                ty: toJsonGlobalTypeId(value.ty),
            };
        case "dereference":
            return {
                kind: "dereference",
                read: toJsonDereferenceOperation(value.read),
                ty: toJsonGlobalTypeId(value.ty),
            };
        case "copy":
            return {
                kind: "copy",
                ty: toJsonGlobalTypeId(value.ty),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one Projection from one JSON value. */
export function fromJsonProjection(value: Json): Projection {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "fieldGet":
            return {
                kind,
                field: fromJsonProjectionField(jsonField(object, "field")),
                ty: fromJsonGlobalTypeId(jsonField(object, "ty")),
            };
        case "propertyGet":
            return {
                kind,
                read: fromJsonMemberResolution(jsonField(object, "read")),
                ty: fromJsonGlobalTypeId(jsonField(object, "ty")),
            };
        case "subscriptGet":
            return {
                kind,
                index: fromJsonGlobalNodeIdAny(jsonField(object, "index")),
                read: fromJsonSubscriptOperation(jsonField(object, "read")),
                ty: fromJsonGlobalTypeId(jsonField(object, "ty")),
            };
        case "call":
            return {
                kind,
                call: fromJsonCallResolution(jsonField(object, "call")),
                ty: fromJsonGlobalTypeId(jsonField(object, "ty")),
            };
        case "objectRest":
            return {
                kind,
                fields: jsonArray(jsonField(object, "fields")).map((item0) => fromJsonObjectRestField(item0)),
                ty: fromJsonGlobalTypeId(jsonField(object, "ty")),
            };
        case "sliceLength":
            return {
                kind,
                ty: fromJsonGlobalTypeId(jsonField(object, "ty")),
            };
        case "dynamicPayload":
            return {
                kind,
                ty: fromJsonGlobalTypeId(jsonField(object, "ty")),
            };
        case "dynamicType":
            return {
                kind,
                ty: fromJsonGlobalTypeId(jsonField(object, "ty")),
            };
        case "variantTag":
            return {
                kind,
                ty: fromJsonGlobalTypeId(jsonField(object, "ty")),
            };
        case "variantPayload":
            return {
                kind,
                case: fromJsonVariantCase(jsonField(object, "case")),
                genericArguments: jsonArray(jsonField(object, "genericArguments")).map((item0) => fromJsonGenericArgumentBinding(item0)),
                discriminant: fromJsonScalarLiteral(jsonField(object, "discriminant")),
                ty: fromJsonGlobalTypeId(jsonField(object, "ty")),
            };
        case "newtypePayload":
            return {
                kind,
                symbol: fromJsonGlobalSymbolId(jsonField(object, "symbol")),
                genericArguments: jsonArray(jsonField(object, "genericArguments")).map((item0) => fromJsonGenericArgumentBinding(item0)),
                ty: fromJsonGlobalTypeId(jsonField(object, "ty")),
            };
        case "borrow":
            return {
                kind,
                access: jsonOptional(object, "access", (value) => fromJsonAccess(value)),
                ty: fromJsonGlobalTypeId(jsonField(object, "ty")),
            };
        case "move":
            return {
                kind,
                access: jsonOptional(object, "access", (value) => fromJsonAccess(value)),
                ty: fromJsonGlobalTypeId(jsonField(object, "ty")),
            };
        case "dereference":
            return {
                kind,
                read: fromJsonDereferenceOperation(jsonField(object, "read")),
                ty: fromJsonGlobalTypeId(jsonField(object, "ty")),
            };
        case "copy":
            return {
                kind,
                ty: fromJsonGlobalTypeId(jsonField(object, "ty")),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** Static field selected by one projection. */
export type ProjectionField =
    /** Structural field key. */
    | {
          readonly kind: "key";
          readonly key: StaticKey;
      }
    /** Declaration-backed nominal stored member. */
    | {
          readonly kind: "member";
          readonly member: GlobalSymbolId;
      }
;

export const ProjectionField = {
    /** Structural field key. */
    key(key: StaticKey): ProjectionField {
        return { kind: "key", key };
    },

    /** Declaration-backed nominal stored member. */
    member(member: GlobalSymbolId): ProjectionField {
        return { kind: "member", member };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: ProjectionField): void {
        encodeProjectionField(writer, value);
    },

    /** Decode one ProjectionField. */
    decode(reader: BinaryReader): ProjectionField {
        return decodeProjectionField(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ProjectionField): Json {
        return toJsonProjectionField(value);
    },

    /** Return one ProjectionField from one JSON value. */
    fromJson(value: Json): ProjectionField {
        return fromJsonProjectionField(value);
    },
};

/** Encode one ProjectionField. */
export function encodeProjectionField(writer: BinaryWriter, value: ProjectionField): void {
    switch (value.kind) {
        case "key":
            writer.writeUnsigned(0);
            encodeStaticKey(writer, value.key);
            return;
        case "member":
            writer.writeUnsigned(1);
            encodeGlobalSymbolId(writer, value.member);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one ProjectionField. */
export function decodeProjectionField(reader: BinaryReader): ProjectionField {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const key = decodeStaticKey(reader);

            return { kind: "key", key };
        }
        case 1: {
            const member = decodeGlobalSymbolId(reader);

            return { kind: "member", member };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one ProjectionField. */
export function toJsonProjectionField(value: ProjectionField): Json {
    switch (value.kind) {
        case "key":
            return {
                kind: "key",
                key: toJsonStaticKey(value.key),
            };
        case "member":
            return {
                kind: "member",
                member: toJsonGlobalSymbolId(value.member),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one ProjectionField from one JSON value. */
export function fromJsonProjectionField(value: Json): ProjectionField {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "key":
            return {
                kind,
                key: fromJsonStaticKey(jsonField(object, "key")),
            };
        case "member":
            return {
                kind,
                member: fromJsonGlobalSymbolId(jsonField(object, "member")),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** Subscript operation selected by one projection or place. */
export type SubscriptOperation =
    /** Structural tuple, field, or index-signature selection. */
    | {
          readonly kind: "member";
          readonly member: MemberResolution;
      }
    /** Protocol-backed subscript call. */
    | {
          readonly kind: "call";
          readonly call: CallResolution;
      }
;

export const SubscriptOperation = {
    /** Structural tuple, field, or index-signature selection. */
    member(member: MemberResolution): SubscriptOperation {
        return { kind: "member", member };
    },

    /** Protocol-backed subscript call. */
    call(call: CallResolution): SubscriptOperation {
        return { kind: "call", call };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: SubscriptOperation): void {
        encodeSubscriptOperation(writer, value);
    },

    /** Decode one SubscriptOperation. */
    decode(reader: BinaryReader): SubscriptOperation {
        return decodeSubscriptOperation(reader);
    },

    /** Return this value as JSON. */
    toJson(value: SubscriptOperation): Json {
        return toJsonSubscriptOperation(value);
    },

    /** Return one SubscriptOperation from one JSON value. */
    fromJson(value: Json): SubscriptOperation {
        return fromJsonSubscriptOperation(value);
    },
};

/** Encode one SubscriptOperation. */
export function encodeSubscriptOperation(writer: BinaryWriter, value: SubscriptOperation): void {
    switch (value.kind) {
        case "member":
            writer.writeUnsigned(0);
            encodeMemberResolution(writer, value.member);
            return;
        case "call":
            writer.writeUnsigned(1);
            encodeCallResolution(writer, value.call);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one SubscriptOperation. */
export function decodeSubscriptOperation(reader: BinaryReader): SubscriptOperation {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const member = decodeMemberResolution(reader);

            return { kind: "member", member };
        }
        case 1: {
            const call = decodeCallResolution(reader);

            return { kind: "call", call };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one SubscriptOperation. */
export function toJsonSubscriptOperation(value: SubscriptOperation): Json {
    switch (value.kind) {
        case "member":
            return {
                kind: "member",
                member: toJsonMemberResolution(value.member),
            };
        case "call":
            return {
                kind: "call",
                call: toJsonCallResolution(value.call),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one SubscriptOperation from one JSON value. */
export function fromJsonSubscriptOperation(value: Json): SubscriptOperation {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "member":
            return {
                kind,
                member: fromJsonMemberResolution(jsonField(object, "member")),
            };
        case "call":
            return {
                kind,
                call: fromJsonCallResolution(jsonField(object, "call")),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** One source field used to materialize an object rest value. */
export type ObjectRestField = {
    /** The materialized field key. */
    readonly key: StaticKey;
    /** The selected source projection. */
    readonly projection: Projection;
};

export const ObjectRestField = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ObjectRestField): void {
        encodeObjectRestField(writer, value);
    },

    /** Decode one ObjectRestField. */
    decode(reader: BinaryReader): ObjectRestField {
        return decodeObjectRestField(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ObjectRestField): Json {
        return toJsonObjectRestField(value);
    },

    /** Return one ObjectRestField from one JSON value. */
    fromJson(value: Json): ObjectRestField {
        return fromJsonObjectRestField(value);
    },
};

/** Encode one ObjectRestField. */
export function encodeObjectRestField(writer: BinaryWriter, value: ObjectRestField): void {
    encodeStaticKey(writer, value.key);
    encodeProjection(writer, value.projection);
}

/** Decode one ObjectRestField. */
export function decodeObjectRestField(reader: BinaryReader): ObjectRestField {
    const key = decodeStaticKey(reader);
    const projection = decodeProjection(reader);

    return {
        key,
        projection,
    };
}

/** Return one JSON value for one ObjectRestField. */
export function toJsonObjectRestField(value: ObjectRestField): Json {
    return {
        key: toJsonStaticKey(value.key),
        projection: toJsonProjection(value.projection),
    };
}

/** Return one ObjectRestField from one JSON value. */
export function fromJsonObjectRestField(value: Json): ObjectRestField {
    const object = jsonObject(value);

    return {
        key: fromJsonStaticKey(jsonField(object, "key")),
        projection: fromJsonProjection(jsonField(object, "projection")),
    };
}

/** One tagged union case selected during checking. */
export type VariantCase = {
    /** The selected variant family symbol. */
    readonly owner: GlobalSymbolId;
    /** The source-level case key. */
    readonly key: StaticKey;
    /** The selected variant declaration. */
    readonly member: GlobalSymbolId;
};

export const VariantCase = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: VariantCase): void {
        encodeVariantCase(writer, value);
    },

    /** Decode one VariantCase. */
    decode(reader: BinaryReader): VariantCase {
        return decodeVariantCase(reader);
    },

    /** Return this value as JSON. */
    toJson(value: VariantCase): Json {
        return toJsonVariantCase(value);
    },

    /** Return one VariantCase from one JSON value. */
    fromJson(value: Json): VariantCase {
        return fromJsonVariantCase(value);
    },
};

/** Encode one VariantCase. */
export function encodeVariantCase(writer: BinaryWriter, value: VariantCase): void {
    encodeGlobalSymbolId(writer, value.owner);
    encodeStaticKey(writer, value.key);
    encodeGlobalSymbolId(writer, value.member);
}

/** Decode one VariantCase. */
export function decodeVariantCase(reader: BinaryReader): VariantCase {
    const owner = decodeGlobalSymbolId(reader);
    const key = decodeStaticKey(reader);
    const member = decodeGlobalSymbolId(reader);

    return {
        owner,
        key,
        member,
    };
}

/** Return one JSON value for one VariantCase. */
export function toJsonVariantCase(value: VariantCase): Json {
    return {
        owner: toJsonGlobalSymbolId(value.owner),
        key: toJsonStaticKey(value.key),
        member: toJsonGlobalSymbolId(value.member),
    };
}

/** Return one VariantCase from one JSON value. */
export function fromJsonVariantCase(value: Json): VariantCase {
    const object = jsonObject(value);

    return {
        owner: fromJsonGlobalSymbolId(jsonField(object, "owner")),
        key: fromJsonStaticKey(jsonField(object, "key")),
        member: fromJsonGlobalSymbolId(jsonField(object, "member")),
    };
}

/** Dereference operation selected by one projection or place. */
export type DereferenceOperation =
    /** Direct dereference of a physical reference or pointer form. */
    | {
          readonly kind: "direct";
      }
    /** Protocol-backed dereference call. */
    | {
          readonly kind: "call";
          readonly call: CallResolution;
      }
;

export const DereferenceOperation = {
    /** Direct dereference of a physical reference or pointer form. */
    direct(): DereferenceOperation {
        return { kind: "direct" };
    },

    /** Protocol-backed dereference call. */
    call(call: CallResolution): DereferenceOperation {
        return { kind: "call", call };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: DereferenceOperation): void {
        encodeDereferenceOperation(writer, value);
    },

    /** Decode one DereferenceOperation. */
    decode(reader: BinaryReader): DereferenceOperation {
        return decodeDereferenceOperation(reader);
    },

    /** Return this value as JSON. */
    toJson(value: DereferenceOperation): Json {
        return toJsonDereferenceOperation(value);
    },

    /** Return one DereferenceOperation from one JSON value. */
    fromJson(value: Json): DereferenceOperation {
        return fromJsonDereferenceOperation(value);
    },
};

/** Encode one DereferenceOperation. */
export function encodeDereferenceOperation(writer: BinaryWriter, value: DereferenceOperation): void {
    switch (value.kind) {
        case "direct":
            writer.writeUnsigned(0);
            return;
        case "call":
            writer.writeUnsigned(1);
            encodeCallResolution(writer, value.call);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one DereferenceOperation. */
export function decodeDereferenceOperation(reader: BinaryReader): DereferenceOperation {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            return { kind: "direct" };
        }
        case 1: {
            const call = decodeCallResolution(reader);

            return { kind: "call", call };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one DereferenceOperation. */
export function toJsonDereferenceOperation(value: DereferenceOperation): Json {
    switch (value.kind) {
        case "direct":
            return {
                kind: "direct",
            };
        case "call":
            return {
                kind: "call",
                call: toJsonCallResolution(value.call),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one DereferenceOperation from one JSON value. */
export function fromJsonDereferenceOperation(value: Json): DereferenceOperation {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "direct":
            return {
                kind,
            };
        case "call":
            return {
                kind,
                call: fromJsonCallResolution(jsonField(object, "call")),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}
