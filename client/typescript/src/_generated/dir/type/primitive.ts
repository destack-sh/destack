/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, jsonBigint, jsonBool, jsonField, jsonInteger, jsonObject, jsonString } from "../../../protocol/serde.js";
import type { StringId } from "../../core/string.js";
import { decodeStringId, encodeStringId, fromJsonStringId, toJsonStringId } from "../../core/string.js";

/** The backing representation of an enum. */
export type EnumBackingType =
    /** Integer-backed enums, like `enum Status { Ready = 0 }`. */
    | {
          readonly kind: "integer";
          readonly integer: IntegerType;
      }
    /** String-backed enums, like `enum Mode { Inline = "inline" }`. */
    | {
          readonly kind: "string";
      }
;

export const EnumBackingType = {
    /** Integer-backed enums, like `enum Status { Ready = 0 }`. */
    integer(integer: IntegerType): EnumBackingType {
        return { kind: "integer", integer };
    },

    /** String-backed enums, like `enum Mode { Inline = "inline" }`. */
    "string"(): EnumBackingType {
        return { kind: "string" };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: EnumBackingType): void {
        encodeEnumBackingType(writer, value);
    },

    /** Decode one EnumBackingType. */
    decode(reader: BinaryReader): EnumBackingType {
        return decodeEnumBackingType(reader);
    },

    /** Return this value as JSON. */
    toJson(value: EnumBackingType): Json {
        return toJsonEnumBackingType(value);
    },

    /** Return one EnumBackingType from one JSON value. */
    fromJson(value: Json): EnumBackingType {
        return fromJsonEnumBackingType(value);
    },
};

/** Encode one EnumBackingType. */
export function encodeEnumBackingType(writer: BinaryWriter, value: EnumBackingType): void {
    switch (value.kind) {
        case "integer":
            writer.writeUnsigned(0);
            encodeIntegerType(writer, value.integer);
            return;
        case "string":
            writer.writeUnsigned(1);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one EnumBackingType. */
export function decodeEnumBackingType(reader: BinaryReader): EnumBackingType {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const integer = decodeIntegerType(reader);

            return { kind: "integer", integer };
        }
        case 1: {
            return { kind: "string" };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one EnumBackingType. */
export function toJsonEnumBackingType(value: EnumBackingType): Json {
    switch (value.kind) {
        case "integer":
            return {
                kind: "integer",
                integer: toJsonIntegerType(value.integer),
            };
        case "string":
            return {
                kind: "string",
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one EnumBackingType from one JSON value. */
export function fromJsonEnumBackingType(value: Json): EnumBackingType {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "integer":
            return {
                kind,
                integer: fromJsonIntegerType(jsonField(object, "integer")),
            };
        case "string":
            return {
                kind,
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** A resolved enum variant value. */
export type EnumVariantValue =
    /** Integer enum value. */
    | {
          readonly kind: "integer";
          readonly integer: bigint;
      }
    /** String enum value. */
    | {
          readonly kind: "string";
          readonly string: StringId;
      }
;

export const EnumVariantValue = {
    /** Integer enum value. */
    integer(integer: bigint): EnumVariantValue {
        return { kind: "integer", integer };
    },

    /** String enum value. */
    "string"(string_: StringId): EnumVariantValue {
        return { kind: "string", string: string_ };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: EnumVariantValue): void {
        encodeEnumVariantValue(writer, value);
    },

    /** Decode one EnumVariantValue. */
    decode(reader: BinaryReader): EnumVariantValue {
        return decodeEnumVariantValue(reader);
    },

    /** Return this value as JSON. */
    toJson(value: EnumVariantValue): Json {
        return toJsonEnumVariantValue(value);
    },

    /** Return one EnumVariantValue from one JSON value. */
    fromJson(value: Json): EnumVariantValue {
        return fromJsonEnumVariantValue(value);
    },
};

/** Encode one EnumVariantValue. */
export function encodeEnumVariantValue(writer: BinaryWriter, value: EnumVariantValue): void {
    switch (value.kind) {
        case "integer":
            writer.writeUnsigned(0);
            writer.writeSigned(value.integer);
            return;
        case "string":
            writer.writeUnsigned(1);
            encodeStringId(writer, value.string);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one EnumVariantValue. */
export function decodeEnumVariantValue(reader: BinaryReader): EnumVariantValue {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const integer = reader.readSigned();

            return { kind: "integer", integer };
        }
        case 1: {
            const string_ = decodeStringId(reader);

            return { kind: "string", string: string_ };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one EnumVariantValue. */
export function toJsonEnumVariantValue(value: EnumVariantValue): Json {
    switch (value.kind) {
        case "integer":
            return {
                kind: "integer",
                integer: value.integer.toString(),
            };
        case "string":
            return {
                kind: "string",
                string: toJsonStringId(value.string),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one EnumVariantValue from one JSON value. */
export function fromJsonEnumVariantValue(value: Json): EnumVariantValue {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "integer":
            return {
                kind,
                integer: jsonBigint(jsonField(object, "integer")),
            };
        case "string":
            return {
                kind,
                string: fromJsonStringId(jsonField(object, "string")),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** A floating-point type. */
export type FloatType = "float16" | "bfloat16" | "float32" | "float64";

export const FloatType = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: FloatType): void {
        encodeFloatType(writer, value);
    },

    /** Decode one FloatType. */
    decode(reader: BinaryReader): FloatType {
        return decodeFloatType(reader);
    },

    /** Return this value as JSON. */
    toJson(value: FloatType): Json {
        return toJsonFloatType(value);
    },

    /** Return one FloatType from one JSON value. */
    fromJson(value: Json): FloatType {
        return fromJsonFloatType(value);
    },
};

/** Encode one FloatType. */
export function encodeFloatType(writer: BinaryWriter, value: FloatType): void {
    switch (value) {
        case "float16":
            writer.writeUnsigned(0);
            return;
        case "bfloat16":
            writer.writeUnsigned(1);
            return;
        case "float32":
            writer.writeUnsigned(2);
            return;
        case "float64":
            writer.writeUnsigned(3);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one FloatType. */
export function decodeFloatType(reader: BinaryReader): FloatType {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "float16";
        case 1:
            return "bfloat16";
        case 2:
            return "float32";
        case 3:
            return "float64";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one FloatType. */
export function toJsonFloatType(value: FloatType): Json {
    return value;
}

/** Return one FloatType from one JSON value. */
export function fromJsonFloatType(value: Json): FloatType {
    const variant = jsonString(value);

    switch (variant) {
        case "float16":
            return "float16";
        case "bfloat16":
            return "bfloat16";
        case "float32":
            return "float32";
        case "float64":
            return "float64";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** An integer type. */
export type IntegerType =
    /** A fixed-width signed or unsigned integer, like `int32` or `uint8`. */
    | {
          readonly kind: "fixed";
          readonly width: number;
          readonly isSigned: boolean;
      }
    /** A pointer-sized signed or unsigned integer, `isize` or `usize`. */
    | {
          readonly kind: "pointer";
          readonly isSigned: boolean;
      }
;

export const IntegerType = {
    /** A fixed-width signed or unsigned integer, like `int32` or `uint8`. */
    fixed(width: number, isSigned: boolean): IntegerType {
        return { kind: "fixed", width, isSigned };
    },

    /** A pointer-sized signed or unsigned integer, `isize` or `usize`. */
    pointer(isSigned: boolean): IntegerType {
        return { kind: "pointer", isSigned };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: IntegerType): void {
        encodeIntegerType(writer, value);
    },

    /** Decode one IntegerType. */
    decode(reader: BinaryReader): IntegerType {
        return decodeIntegerType(reader);
    },

    /** Return this value as JSON. */
    toJson(value: IntegerType): Json {
        return toJsonIntegerType(value);
    },

    /** Return one IntegerType from one JSON value. */
    fromJson(value: Json): IntegerType {
        return fromJsonIntegerType(value);
    },
};

/** Encode one IntegerType. */
export function encodeIntegerType(writer: BinaryWriter, value: IntegerType): void {
    switch (value.kind) {
        case "fixed":
            writer.writeUnsigned(0);
            writer.writeUnsigned(value.width);
            writer.writeBool(value.isSigned);
            return;
        case "pointer":
            writer.writeUnsigned(1);
            writer.writeBool(value.isSigned);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one IntegerType. */
export function decodeIntegerType(reader: BinaryReader): IntegerType {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const width = reader.readNumber();
            const isSigned = reader.readBool();

            return {
                kind: "fixed",
                width,
                isSigned,
            };
        }
        case 1: {
            const isSigned = reader.readBool();

            return {
                kind: "pointer",
                isSigned,
            };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one IntegerType. */
export function toJsonIntegerType(value: IntegerType): Json {
    switch (value.kind) {
        case "fixed":
            return {
                kind: "fixed",
                width: value.width,
                isSigned: value.isSigned,
            };
        case "pointer":
            return {
                kind: "pointer",
                isSigned: value.isSigned,
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one IntegerType from one JSON value. */
export function fromJsonIntegerType(value: Json): IntegerType {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "fixed":
            return {
                kind,
                width: jsonInteger(jsonField(object, "width")),
                isSigned: jsonBool(jsonField(object, "isSigned")),
            };
        case "pointer":
            return {
                kind,
                isSigned: jsonBool(jsonField(object, "isSigned")),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** A primitive type. */
export type PrimitiveType =
    /** Boolean type `boolean`. */
    | {
          readonly kind: "boolean";
      }
    /** Character type `char`. */
    | {
          readonly kind: "character";
      }
    /** String type `string`. */
    | {
          readonly kind: "string";
      }
    /** Bigint type `bigint`. */
    | {
          readonly kind: "bigint";
      }
    /** Integer type, like `int32`, `uint8`, or `usize`. */
    | {
          readonly kind: "integer";
          readonly integer: IntegerType;
      }
    /** Float type, like `float64` or `bfloat16`. */
    | {
          readonly kind: "float";
          readonly float: FloatType;
      }
    /** Symbol type `symbol`. */
    | {
          readonly kind: "symbol";
      }
    /** Unique symbol type `unique symbol`. */
    | {
          readonly kind: "uniqueSymbol";
      }
;

export const PrimitiveType = {
    /** Boolean type `boolean`. */
    "boolean"(): PrimitiveType {
        return { kind: "boolean" };
    },

    /** Character type `char`. */
    character(): PrimitiveType {
        return { kind: "character" };
    },

    /** String type `string`. */
    "string"(): PrimitiveType {
        return { kind: "string" };
    },

    /** Bigint type `bigint`. */
    "bigint"(): PrimitiveType {
        return { kind: "bigint" };
    },

    /** Integer type, like `int32`, `uint8`, or `usize`. */
    integer(integer: IntegerType): PrimitiveType {
        return { kind: "integer", integer };
    },

    /** Float type, like `float64` or `bfloat16`. */
    float(float: FloatType): PrimitiveType {
        return { kind: "float", float };
    },

    /** Symbol type `symbol`. */
    "symbol"(): PrimitiveType {
        return { kind: "symbol" };
    },

    /** Unique symbol type `unique symbol`. */
    uniqueSymbol(): PrimitiveType {
        return { kind: "uniqueSymbol" };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: PrimitiveType): void {
        encodePrimitiveType(writer, value);
    },

    /** Decode one PrimitiveType. */
    decode(reader: BinaryReader): PrimitiveType {
        return decodePrimitiveType(reader);
    },

    /** Return this value as JSON. */
    toJson(value: PrimitiveType): Json {
        return toJsonPrimitiveType(value);
    },

    /** Return one PrimitiveType from one JSON value. */
    fromJson(value: Json): PrimitiveType {
        return fromJsonPrimitiveType(value);
    },
};

/** Encode one PrimitiveType. */
export function encodePrimitiveType(writer: BinaryWriter, value: PrimitiveType): void {
    switch (value.kind) {
        case "boolean":
            writer.writeUnsigned(0);
            return;
        case "character":
            writer.writeUnsigned(1);
            return;
        case "string":
            writer.writeUnsigned(2);
            return;
        case "bigint":
            writer.writeUnsigned(3);
            return;
        case "integer":
            writer.writeUnsigned(4);
            encodeIntegerType(writer, value.integer);
            return;
        case "float":
            writer.writeUnsigned(5);
            encodeFloatType(writer, value.float);
            return;
        case "symbol":
            writer.writeUnsigned(6);
            return;
        case "uniqueSymbol":
            writer.writeUnsigned(7);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one PrimitiveType. */
export function decodePrimitiveType(reader: BinaryReader): PrimitiveType {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            return { kind: "boolean" };
        }
        case 1: {
            return { kind: "character" };
        }
        case 2: {
            return { kind: "string" };
        }
        case 3: {
            return { kind: "bigint" };
        }
        case 4: {
            const integer = decodeIntegerType(reader);

            return { kind: "integer", integer };
        }
        case 5: {
            const float = decodeFloatType(reader);

            return { kind: "float", float };
        }
        case 6: {
            return { kind: "symbol" };
        }
        case 7: {
            return { kind: "uniqueSymbol" };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one PrimitiveType. */
export function toJsonPrimitiveType(value: PrimitiveType): Json {
    switch (value.kind) {
        case "boolean":
            return {
                kind: "boolean",
            };
        case "character":
            return {
                kind: "character",
            };
        case "string":
            return {
                kind: "string",
            };
        case "bigint":
            return {
                kind: "bigint",
            };
        case "integer":
            return {
                kind: "integer",
                integer: toJsonIntegerType(value.integer),
            };
        case "float":
            return {
                kind: "float",
                float: toJsonFloatType(value.float),
            };
        case "symbol":
            return {
                kind: "symbol",
            };
        case "uniqueSymbol":
            return {
                kind: "uniqueSymbol",
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one PrimitiveType from one JSON value. */
export function fromJsonPrimitiveType(value: Json): PrimitiveType {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "boolean":
            return {
                kind,
            };
        case "character":
            return {
                kind,
            };
        case "string":
            return {
                kind,
            };
        case "bigint":
            return {
                kind,
            };
        case "integer":
            return {
                kind,
                integer: fromJsonIntegerType(jsonField(object, "integer")),
            };
        case "float":
            return {
                kind,
                float: fromJsonFloatType(jsonField(object, "float")),
            };
        case "symbol":
            return {
                kind,
            };
        case "uniqueSymbol":
            return {
                kind,
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** One width-less scalar source alias. */
export type ScalarAlias = "int" | "uint" | "float";

export const ScalarAlias = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ScalarAlias): void {
        encodeScalarAlias(writer, value);
    },

    /** Decode one ScalarAlias. */
    decode(reader: BinaryReader): ScalarAlias {
        return decodeScalarAlias(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ScalarAlias): Json {
        return toJsonScalarAlias(value);
    },

    /** Return one ScalarAlias from one JSON value. */
    fromJson(value: Json): ScalarAlias {
        return fromJsonScalarAlias(value);
    },
};

/** Encode one ScalarAlias. */
export function encodeScalarAlias(writer: BinaryWriter, value: ScalarAlias): void {
    switch (value) {
        case "int":
            writer.writeUnsigned(0);
            return;
        case "uint":
            writer.writeUnsigned(1);
            return;
        case "float":
            writer.writeUnsigned(2);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one ScalarAlias. */
export function decodeScalarAlias(reader: BinaryReader): ScalarAlias {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "int";
        case 1:
            return "uint";
        case 2:
            return "float";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one ScalarAlias. */
export function toJsonScalarAlias(value: ScalarAlias): Json {
    return value;
}

/** Return one ScalarAlias from one JSON value. */
export function fromJsonScalarAlias(value: Json): ScalarAlias {
    const variant = jsonString(value);

    switch (variant) {
        case "int":
            return "int";
        case "uint":
            return "uint";
        case "float":
            return "float";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}
