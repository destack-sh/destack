/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, jsonArray, jsonField, jsonObject, jsonOptional } from "../../../protocol/serde.js";
import type { GlobalSymbolId } from "../symbol/symbol.js";
import type { GenericArgumentBinding } from "./generic.js";
import type { GlobalTypeId } from "./type.js";
import { decodeGlobalSymbolId, encodeGlobalSymbolId, fromJsonGlobalSymbolId, toJsonGlobalSymbolId } from "../symbol/symbol.js";
import { decodeGenericArgumentBinding, encodeGenericArgumentBinding, fromJsonGenericArgumentBinding, toJsonGenericArgumentBinding } from "./generic.js";
import { decodeGlobalTypeId, encodeGlobalTypeId, fromJsonGlobalTypeId, toJsonGlobalTypeId } from "./type.js";

/** One declaration selected with its generic argument bindings. */
export type InstanceKey = {
    /** The selected declaration. */
    readonly symbol: GlobalSymbolId;
    /** The receiver closing a this-polymorphic interface member, absent elsewhere. */
    readonly receiver?: GlobalTypeId;
    /** The selected generic argument bindings. */
    readonly arguments: ReadonlyArray<GenericArgumentBinding>;
};

export const InstanceKey = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: InstanceKey): void {
        encodeInstanceKey(writer, value);
    },

    /** Decode one InstanceKey. */
    decode(reader: BinaryReader): InstanceKey {
        return decodeInstanceKey(reader);
    },

    /** Return this value as JSON. */
    toJson(value: InstanceKey): Json {
        return toJsonInstanceKey(value);
    },

    /** Return one InstanceKey from one JSON value. */
    fromJson(value: Json): InstanceKey {
        return fromJsonInstanceKey(value);
    },
};

/** Encode one InstanceKey. */
export function encodeInstanceKey(writer: BinaryWriter, value: InstanceKey): void {
    encodeGlobalSymbolId(writer, value.symbol);
    writer.writeOption(value.receiver, (value1) => {
        encodeGlobalTypeId(writer, value1);
    });
    writer.writeUnsigned(value.arguments.length);
    for (const item2 of value.arguments) {
        encodeGenericArgumentBinding(writer, item2);
    }
}

/** Decode one InstanceKey. */
export function decodeInstanceKey(reader: BinaryReader): InstanceKey {
    const symbol_ = decodeGlobalSymbolId(reader);
    const receiver = reader.readOption(() => decodeGlobalTypeId(reader));
    const arguments_ = (() => { const length2 = reader.readNumber(); const items2: Array<GenericArgumentBinding> = []; for (let index = 0; index < length2; index += 1) { items2.push(decodeGenericArgumentBinding(reader)); } return items2; })();

    return {
        symbol: symbol_,
        ...(receiver === undefined ? {} : { receiver }),
        arguments: arguments_,
    };
}

/** Return one JSON value for one InstanceKey. */
export function toJsonInstanceKey(value: InstanceKey): Json {
    return {
        symbol: toJsonGlobalSymbolId(value.symbol),
        ...(value.receiver === undefined ? {} : { receiver: toJsonGlobalTypeId(value.receiver) }),
        arguments: value.arguments.map((item0) => toJsonGenericArgumentBinding(item0)),
    };
}

/** Return one InstanceKey from one JSON value. */
export function fromJsonInstanceKey(value: Json): InstanceKey {
    const object = jsonObject(value);

    return {
        symbol: fromJsonGlobalSymbolId(jsonField(object, "symbol")),
        receiver: jsonOptional(object, "receiver", (value) => fromJsonGlobalTypeId(value)),
        arguments: jsonArray(jsonField(object, "arguments")).map((item0) => fromJsonGenericArgumentBinding(item0)),
    };
}
