/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, jsonArray, jsonField, jsonObject, jsonOptional, jsonString } from "../../../protocol/serde.js";
import type { StaticKey } from "../symbol/key.js";
import type { ScalarLiteral } from "../tree/literal.js";
import type { RangeEnd } from "../tree/operator.js";
import type { PrimitiveType } from "./primitive.js";
import type { Projection } from "./projection.js";
import type { GlobalTypeId } from "./type.js";
import { decodeStaticKey, encodeStaticKey, fromJsonStaticKey, toJsonStaticKey } from "../symbol/key.js";
import { decodeScalarLiteral, encodeScalarLiteral, fromJsonScalarLiteral, toJsonScalarLiteral } from "../tree/literal.js";
import { decodeRangeEnd, encodeRangeEnd, fromJsonRangeEnd, toJsonRangeEnd } from "../tree/operator.js";
import { decodePrimitiveType, encodePrimitiveType, fromJsonPrimitiveType, toJsonPrimitiveType } from "./primitive.js";
import { decodeProjection, encodeProjection, fromJsonProjection, toJsonProjection } from "./projection.js";
import { decodeGlobalTypeId, encodeGlobalTypeId, fromJsonGlobalTypeId, toJsonGlobalTypeId } from "./type.js";

/** Executable predicate selected during checking. */
export type Predicate = {
    /** The test to execute. */
    readonly test: PredicateTest;
    /** The narrowed value type after the predicate succeeds. */
    readonly narrowed?: GlobalTypeId;
    /** The projected value available after the predicate succeeds. */
    readonly projection?: Projection;
};

export const Predicate = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: Predicate): void {
        encodePredicate(writer, value);
    },

    /** Decode one Predicate. */
    decode(reader: BinaryReader): Predicate {
        return decodePredicate(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Predicate): Json {
        return toJsonPredicate(value);
    },

    /** Return one Predicate from one JSON value. */
    fromJson(value: Json): Predicate {
        return fromJsonPredicate(value);
    },
};

/** Encode one Predicate. */
export function encodePredicate(writer: BinaryWriter, value: Predicate): void {
    encodePredicateTest(writer, value.test);
    writer.writeOption(value.narrowed, (value1) => {
        encodeGlobalTypeId(writer, value1);
    });
    writer.writeOption(value.projection, (value2) => {
        encodeProjection(writer, value2);
    });
}

/** Decode one Predicate. */
export function decodePredicate(reader: BinaryReader): Predicate {
    const test = decodePredicateTest(reader);
    const narrowed = reader.readOption(() => decodeGlobalTypeId(reader));
    const projection = reader.readOption(() => decodeProjection(reader));

    return {
        test,
        ...(narrowed === undefined ? {} : { narrowed }),
        ...(projection === undefined ? {} : { projection }),
    };
}

/** Return one JSON value for one Predicate. */
export function toJsonPredicate(value: Predicate): Json {
    return {
        test: toJsonPredicateTest(value.test),
        ...(value.narrowed === undefined ? {} : { narrowed: toJsonGlobalTypeId(value.narrowed) }),
        ...(value.projection === undefined ? {} : { projection: toJsonProjection(value.projection) }),
    };
}

/** Return one Predicate from one JSON value. */
export function fromJsonPredicate(value: Json): Predicate {
    const object = jsonObject(value);

    return {
        test: fromJsonPredicateTest(jsonField(object, "test")),
        narrowed: jsonOptional(object, "narrowed", (value) => fromJsonGlobalTypeId(value)),
        projection: jsonOptional(object, "projection", (value) => fromJsonProjection(value)),
    };
}

/** Condition applied to one predicate input. */
export type PredicateCondition =
    /** Condition that accepts any projected input. */
    | {
          readonly kind: "always";
      }
    /** Condition that rejects every projected input. */
    | {
          readonly kind: "never";
      }
    /** Scalar literal condition, like `"ready"` or `0`. */
    | {
          readonly kind: "literal";
          readonly literal: ScalarLiteral;
      }
    /** Scalar interval condition, like `0..=255`. */
    | {
          readonly kind: "range";
          readonly range: PredicateRange;
      }
    /** Primitive tag condition, like `string` or `int32`. */
    | {
          readonly kind: "primitive";
          readonly primitive: PrimitiveType;
      }
    /** Exact runtime type descriptor condition. */
    | {
          readonly kind: "type";
          readonly type: GlobalTypeId;
      }
    /** Runtime subtype descriptor condition. */
    | {
          readonly kind: "subtype";
          readonly subtype: GlobalTypeId;
      }
;

export const PredicateCondition = {
    /** Condition that accepts any projected input. */
    always(): PredicateCondition {
        return { kind: "always" };
    },

    /** Condition that rejects every projected input. */
    "never"(): PredicateCondition {
        return { kind: "never" };
    },

    /** Scalar literal condition, like `"ready"` or `0`. */
    literal(literal: ScalarLiteral): PredicateCondition {
        return { kind: "literal", literal };
    },

    /** Scalar interval condition, like `0..=255`. */
    range(range: PredicateRange): PredicateCondition {
        return { kind: "range", range };
    },

    /** Primitive tag condition, like `string` or `int32`. */
    primitive(primitive: PrimitiveType): PredicateCondition {
        return { kind: "primitive", primitive };
    },

    /** Exact runtime type descriptor condition. */
    "type"(type_: GlobalTypeId): PredicateCondition {
        return { kind: "type", type: type_ };
    },

    /** Runtime subtype descriptor condition. */
    subtype(subtype: GlobalTypeId): PredicateCondition {
        return { kind: "subtype", subtype };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: PredicateCondition): void {
        encodePredicateCondition(writer, value);
    },

    /** Decode one PredicateCondition. */
    decode(reader: BinaryReader): PredicateCondition {
        return decodePredicateCondition(reader);
    },

    /** Return this value as JSON. */
    toJson(value: PredicateCondition): Json {
        return toJsonPredicateCondition(value);
    },

    /** Return one PredicateCondition from one JSON value. */
    fromJson(value: Json): PredicateCondition {
        return fromJsonPredicateCondition(value);
    },
};

/** Encode one PredicateCondition. */
export function encodePredicateCondition(writer: BinaryWriter, value: PredicateCondition): void {
    switch (value.kind) {
        case "always":
            writer.writeUnsigned(0);
            return;
        case "never":
            writer.writeUnsigned(1);
            return;
        case "literal":
            writer.writeUnsigned(2);
            encodeScalarLiteral(writer, value.literal);
            return;
        case "range":
            writer.writeUnsigned(3);
            encodePredicateRange(writer, value.range);
            return;
        case "primitive":
            writer.writeUnsigned(4);
            encodePrimitiveType(writer, value.primitive);
            return;
        case "type":
            writer.writeUnsigned(5);
            encodeGlobalTypeId(writer, value.type);
            return;
        case "subtype":
            writer.writeUnsigned(6);
            encodeGlobalTypeId(writer, value.subtype);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one PredicateCondition. */
export function decodePredicateCondition(reader: BinaryReader): PredicateCondition {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            return { kind: "always" };
        }
        case 1: {
            return { kind: "never" };
        }
        case 2: {
            const literal = decodeScalarLiteral(reader);

            return { kind: "literal", literal };
        }
        case 3: {
            const range = decodePredicateRange(reader);

            return { kind: "range", range };
        }
        case 4: {
            const primitive = decodePrimitiveType(reader);

            return { kind: "primitive", primitive };
        }
        case 5: {
            const type_ = decodeGlobalTypeId(reader);

            return { kind: "type", type: type_ };
        }
        case 6: {
            const subtype = decodeGlobalTypeId(reader);

            return { kind: "subtype", subtype };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one PredicateCondition. */
export function toJsonPredicateCondition(value: PredicateCondition): Json {
    switch (value.kind) {
        case "always":
            return {
                kind: "always",
            };
        case "never":
            return {
                kind: "never",
            };
        case "literal":
            return {
                kind: "literal",
                literal: toJsonScalarLiteral(value.literal),
            };
        case "range":
            return {
                kind: "range",
                range: toJsonPredicateRange(value.range),
            };
        case "primitive":
            return {
                kind: "primitive",
                primitive: toJsonPrimitiveType(value.primitive),
            };
        case "type":
            return {
                kind: "type",
                type: toJsonGlobalTypeId(value.type),
            };
        case "subtype":
            return {
                kind: "subtype",
                subtype: toJsonGlobalTypeId(value.subtype),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one PredicateCondition from one JSON value. */
export function fromJsonPredicateCondition(value: Json): PredicateCondition {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "always":
            return {
                kind,
            };
        case "never":
            return {
                kind,
            };
        case "literal":
            return {
                kind,
                literal: fromJsonScalarLiteral(jsonField(object, "literal")),
            };
        case "range":
            return {
                kind,
                range: fromJsonPredicateRange(jsonField(object, "range")),
            };
        case "primitive":
            return {
                kind,
                primitive: fromJsonPrimitiveType(jsonField(object, "primitive")),
            };
        case "type":
            return {
                kind,
                type: fromJsonGlobalTypeId(jsonField(object, "type")),
            };
        case "subtype":
            return {
                kind,
                subtype: fromJsonGlobalTypeId(jsonField(object, "subtype")),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** Key tested by one structural membership predicate. */
export type PredicateKey =
    /** Statically known property key. */
    | {
          readonly kind: "static";
          readonly static: StaticKey;
      }
    /** Runtime-computed property key. */
    | {
          readonly kind: "dynamic";
          readonly dynamic: PredicateOperand;
      }
;

export const PredicateKey = {
    /** Statically known property key. */
    "static"(static_: StaticKey): PredicateKey {
        return { kind: "static", static: static_ };
    },

    /** Runtime-computed property key. */
    dynamic(dynamic: PredicateOperand): PredicateKey {
        return { kind: "dynamic", dynamic };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: PredicateKey): void {
        encodePredicateKey(writer, value);
    },

    /** Decode one PredicateKey. */
    decode(reader: BinaryReader): PredicateKey {
        return decodePredicateKey(reader);
    },

    /** Return this value as JSON. */
    toJson(value: PredicateKey): Json {
        return toJsonPredicateKey(value);
    },

    /** Return one PredicateKey from one JSON value. */
    fromJson(value: Json): PredicateKey {
        return fromJsonPredicateKey(value);
    },
};

/** Encode one PredicateKey. */
export function encodePredicateKey(writer: BinaryWriter, value: PredicateKey): void {
    switch (value.kind) {
        case "static":
            writer.writeUnsigned(0);
            encodeStaticKey(writer, value.static);
            return;
        case "dynamic":
            writer.writeUnsigned(1);
            encodePredicateOperand(writer, value.dynamic);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one PredicateKey. */
export function decodePredicateKey(reader: BinaryReader): PredicateKey {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const static_ = decodeStaticKey(reader);

            return { kind: "static", static: static_ };
        }
        case 1: {
            const dynamic = decodePredicateOperand(reader);

            return { kind: "dynamic", dynamic };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one PredicateKey. */
export function toJsonPredicateKey(value: PredicateKey): Json {
    switch (value.kind) {
        case "static":
            return {
                kind: "static",
                static: toJsonStaticKey(value.static),
            };
        case "dynamic":
            return {
                kind: "dynamic",
                dynamic: toJsonPredicateOperand(value.dynamic),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one PredicateKey from one JSON value. */
export function fromJsonPredicateKey(value: Json): PredicateKey {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "static":
            return {
                kind,
                static: fromJsonStaticKey(jsonField(object, "static")),
            };
        case "dynamic":
            return {
                kind,
                dynamic: fromJsonPredicateOperand(jsonField(object, "dynamic")),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** Structural membership predicate. */
export type PredicateMembershipTest = {
    /** The receiver value. */
    readonly receiver: PredicateOperand;
    /** The tested key. */
    readonly key: PredicateKey;
};

export const PredicateMembershipTest = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: PredicateMembershipTest): void {
        encodePredicateMembershipTest(writer, value);
    },

    /** Decode one PredicateMembershipTest. */
    decode(reader: BinaryReader): PredicateMembershipTest {
        return decodePredicateMembershipTest(reader);
    },

    /** Return this value as JSON. */
    toJson(value: PredicateMembershipTest): Json {
        return toJsonPredicateMembershipTest(value);
    },

    /** Return one PredicateMembershipTest from one JSON value. */
    fromJson(value: Json): PredicateMembershipTest {
        return fromJsonPredicateMembershipTest(value);
    },
};

/** Encode one PredicateMembershipTest. */
export function encodePredicateMembershipTest(writer: BinaryWriter, value: PredicateMembershipTest): void {
    encodePredicateOperand(writer, value.receiver);
    encodePredicateKey(writer, value.key);
}

/** Decode one PredicateMembershipTest. */
export function decodePredicateMembershipTest(reader: BinaryReader): PredicateMembershipTest {
    const receiver = decodePredicateOperand(reader);
    const key = decodePredicateKey(reader);

    return {
        receiver,
        key,
    };
}

/** Return one JSON value for one PredicateMembershipTest. */
export function toJsonPredicateMembershipTest(value: PredicateMembershipTest): Json {
    return {
        receiver: toJsonPredicateOperand(value.receiver),
        key: toJsonPredicateKey(value.key),
    };
}

/** Return one PredicateMembershipTest from one JSON value. */
export function fromJsonPredicateMembershipTest(value: Json): PredicateMembershipTest {
    const object = jsonObject(value);

    return {
        receiver: fromJsonPredicateOperand(jsonField(object, "receiver")),
        key: fromJsonPredicateKey(jsonField(object, "key")),
    };
}

/** Value tested by one executable predicate. */
export type PredicateOperand =
    /** Direct input value. */
    | {
          readonly kind: "direct";
          readonly direct: GlobalTypeId;
      }
    /** Value computed by one projection. */
    | {
          readonly kind: "projected";
          readonly projected: Projection;
      }
;

export const PredicateOperand = {
    /** Direct input value. */
    direct(direct: GlobalTypeId): PredicateOperand {
        return { kind: "direct", direct };
    },

    /** Value computed by one projection. */
    projected(projected: Projection): PredicateOperand {
        return { kind: "projected", projected };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: PredicateOperand): void {
        encodePredicateOperand(writer, value);
    },

    /** Decode one PredicateOperand. */
    decode(reader: BinaryReader): PredicateOperand {
        return decodePredicateOperand(reader);
    },

    /** Return this value as JSON. */
    toJson(value: PredicateOperand): Json {
        return toJsonPredicateOperand(value);
    },

    /** Return one PredicateOperand from one JSON value. */
    fromJson(value: Json): PredicateOperand {
        return fromJsonPredicateOperand(value);
    },
};

/** Encode one PredicateOperand. */
export function encodePredicateOperand(writer: BinaryWriter, value: PredicateOperand): void {
    switch (value.kind) {
        case "direct":
            writer.writeUnsigned(0);
            encodeGlobalTypeId(writer, value.direct);
            return;
        case "projected":
            writer.writeUnsigned(1);
            encodeProjection(writer, value.projected);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one PredicateOperand. */
export function decodePredicateOperand(reader: BinaryReader): PredicateOperand {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const direct = decodeGlobalTypeId(reader);

            return { kind: "direct", direct };
        }
        case 1: {
            const projected = decodeProjection(reader);

            return { kind: "projected", projected };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one PredicateOperand. */
export function toJsonPredicateOperand(value: PredicateOperand): Json {
    switch (value.kind) {
        case "direct":
            return {
                kind: "direct",
                direct: toJsonGlobalTypeId(value.direct),
            };
        case "projected":
            return {
                kind: "projected",
                projected: toJsonProjection(value.projected),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one PredicateOperand from one JSON value. */
export function fromJsonPredicateOperand(value: Json): PredicateOperand {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "direct":
            return {
                kind,
                direct: fromJsonGlobalTypeId(jsonField(object, "direct")),
            };
        case "projected":
            return {
                kind,
                projected: fromJsonProjection(jsonField(object, "projected")),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** Scalar interval condition. */
export type PredicateRange = {
    /** The scalar domain constrained by the range. */
    readonly domain: GlobalTypeId;
    /** The optional committed lower bound. */
    readonly start?: ScalarLiteral;
    /** The optional committed upper bound. */
    readonly end?: ScalarLiteral;
    /** Whether the upper bound is inclusive. */
    readonly endBound: RangeEnd;
};

export const PredicateRange = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: PredicateRange): void {
        encodePredicateRange(writer, value);
    },

    /** Decode one PredicateRange. */
    decode(reader: BinaryReader): PredicateRange {
        return decodePredicateRange(reader);
    },

    /** Return this value as JSON. */
    toJson(value: PredicateRange): Json {
        return toJsonPredicateRange(value);
    },

    /** Return one PredicateRange from one JSON value. */
    fromJson(value: Json): PredicateRange {
        return fromJsonPredicateRange(value);
    },
};

/** Encode one PredicateRange. */
export function encodePredicateRange(writer: BinaryWriter, value: PredicateRange): void {
    encodeGlobalTypeId(writer, value.domain);
    writer.writeOption(value.start, (value1) => {
        encodeScalarLiteral(writer, value1);
    });
    writer.writeOption(value.end, (value2) => {
        encodeScalarLiteral(writer, value2);
    });
    encodeRangeEnd(writer, value.endBound);
}

/** Decode one PredicateRange. */
export function decodePredicateRange(reader: BinaryReader): PredicateRange {
    const domain = decodeGlobalTypeId(reader);
    const start = reader.readOption(() => decodeScalarLiteral(reader));
    const end = reader.readOption(() => decodeScalarLiteral(reader));
    const endBound = decodeRangeEnd(reader);

    return {
        domain,
        ...(start === undefined ? {} : { start }),
        ...(end === undefined ? {} : { end }),
        endBound,
    };
}

/** Return one JSON value for one PredicateRange. */
export function toJsonPredicateRange(value: PredicateRange): Json {
    return {
        domain: toJsonGlobalTypeId(value.domain),
        ...(value.start === undefined ? {} : { start: toJsonScalarLiteral(value.start) }),
        ...(value.end === undefined ? {} : { end: toJsonScalarLiteral(value.end) }),
        endBound: toJsonRangeEnd(value.endBound),
    };
}

/** Return one PredicateRange from one JSON value. */
export function fromJsonPredicateRange(value: Json): PredicateRange {
    const object = jsonObject(value);

    return {
        domain: fromJsonGlobalTypeId(jsonField(object, "domain")),
        start: jsonOptional(object, "start", (value) => fromJsonScalarLiteral(value)),
        end: jsonOptional(object, "end", (value) => fromJsonScalarLiteral(value)),
        endBound: fromJsonRangeEnd(jsonField(object, "endBound")),
    };
}

/** Predicate test selected during checking. */
export type PredicateTest =
    /** Unary test over one input value. */
    | {
          readonly kind: "unary";
          readonly unary: PredicateUnaryTest;
      }
    /** Structural membership test over a receiver and key. */
    | {
          readonly kind: "membership";
          readonly membership: PredicateMembershipTest;
      }
    /** Predicate that accepts when any alternative accepts. */
    | {
          readonly kind: "any";
          readonly any: ReadonlyArray<Predicate>;
      }
;

export const PredicateTest = {
    /** Unary test over one input value. */
    unary(unary: PredicateUnaryTest): PredicateTest {
        return { kind: "unary", unary };
    },

    /** Structural membership test over a receiver and key. */
    membership(membership: PredicateMembershipTest): PredicateTest {
        return { kind: "membership", membership };
    },

    /** Predicate that accepts when any alternative accepts. */
    "any"(any_: ReadonlyArray<Predicate>): PredicateTest {
        return { kind: "any", any: any_ };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: PredicateTest): void {
        encodePredicateTest(writer, value);
    },

    /** Decode one PredicateTest. */
    decode(reader: BinaryReader): PredicateTest {
        return decodePredicateTest(reader);
    },

    /** Return this value as JSON. */
    toJson(value: PredicateTest): Json {
        return toJsonPredicateTest(value);
    },

    /** Return one PredicateTest from one JSON value. */
    fromJson(value: Json): PredicateTest {
        return fromJsonPredicateTest(value);
    },
};

/** Encode one PredicateTest. */
export function encodePredicateTest(writer: BinaryWriter, value: PredicateTest): void {
    switch (value.kind) {
        case "unary":
            writer.writeUnsigned(0);
            encodePredicateUnaryTest(writer, value.unary);
            return;
        case "membership":
            writer.writeUnsigned(1);
            encodePredicateMembershipTest(writer, value.membership);
            return;
        case "any":
            writer.writeUnsigned(2);
            writer.writeUnsigned(value.any.length);
            for (const item0 of value.any) {
                encodePredicate(writer, item0);
            }
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one PredicateTest. */
export function decodePredicateTest(reader: BinaryReader): PredicateTest {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const unary = decodePredicateUnaryTest(reader);

            return { kind: "unary", unary };
        }
        case 1: {
            const membership = decodePredicateMembershipTest(reader);

            return { kind: "membership", membership };
        }
        case 2: {
            const any_ = (() => { const length0 = reader.readNumber(); const items0: Array<Predicate> = []; for (let index = 0; index < length0; index += 1) { items0.push(decodePredicate(reader)); } return items0; })();

            return { kind: "any", any: any_ };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one PredicateTest. */
export function toJsonPredicateTest(value: PredicateTest): Json {
    switch (value.kind) {
        case "unary":
            return {
                kind: "unary",
                unary: toJsonPredicateUnaryTest(value.unary),
            };
        case "membership":
            return {
                kind: "membership",
                membership: toJsonPredicateMembershipTest(value.membership),
            };
        case "any":
            return {
                kind: "any",
                any: value.any.map((item0) => toJsonPredicate(item0)),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one PredicateTest from one JSON value. */
export function fromJsonPredicateTest(value: Json): PredicateTest {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "unary":
            return {
                kind,
                unary: fromJsonPredicateUnaryTest(jsonField(object, "unary")),
            };
        case "membership":
            return {
                kind,
                membership: fromJsonPredicateMembershipTest(jsonField(object, "membership")),
            };
        case "any":
            return {
                kind,
                any: jsonArray(jsonField(object, "any")).map((item0) => fromJsonPredicate(item0)),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** Unary predicate over one input value. */
export type PredicateUnaryTest = {
    /** The value tested by this predicate. */
    readonly input: PredicateOperand;
    /** The condition applied to the projected input. */
    readonly condition: PredicateCondition;
};

export const PredicateUnaryTest = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: PredicateUnaryTest): void {
        encodePredicateUnaryTest(writer, value);
    },

    /** Decode one PredicateUnaryTest. */
    decode(reader: BinaryReader): PredicateUnaryTest {
        return decodePredicateUnaryTest(reader);
    },

    /** Return this value as JSON. */
    toJson(value: PredicateUnaryTest): Json {
        return toJsonPredicateUnaryTest(value);
    },

    /** Return one PredicateUnaryTest from one JSON value. */
    fromJson(value: Json): PredicateUnaryTest {
        return fromJsonPredicateUnaryTest(value);
    },
};

/** Encode one PredicateUnaryTest. */
export function encodePredicateUnaryTest(writer: BinaryWriter, value: PredicateUnaryTest): void {
    encodePredicateOperand(writer, value.input);
    encodePredicateCondition(writer, value.condition);
}

/** Decode one PredicateUnaryTest. */
export function decodePredicateUnaryTest(reader: BinaryReader): PredicateUnaryTest {
    const input = decodePredicateOperand(reader);
    const condition = decodePredicateCondition(reader);

    return {
        input,
        condition,
    };
}

/** Return one JSON value for one PredicateUnaryTest. */
export function toJsonPredicateUnaryTest(value: PredicateUnaryTest): Json {
    return {
        input: toJsonPredicateOperand(value.input),
        condition: toJsonPredicateCondition(value.condition),
    };
}

/** Return one PredicateUnaryTest from one JSON value. */
export function fromJsonPredicateUnaryTest(value: Json): PredicateUnaryTest {
    const object = jsonObject(value);

    return {
        input: fromJsonPredicateOperand(jsonField(object, "input")),
        condition: fromJsonPredicateCondition(jsonField(object, "condition")),
    };
}
