/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, jsonArray, jsonField, jsonObject, jsonString } from "../../protocol/serde.js";
import type { Revision } from "../repository/revision.js";
import { decodeRevision, encodeRevision, fromJsonRevision, toJsonRevision } from "../repository/revision.js";

/** One named workspace revision. */
export type Branch = {
    /** Branch name. */
    readonly name: string;
    /** Current branch revision. */
    readonly revision: Revision;
};

export const Branch = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: Branch): void {
        encodeBranch(writer, value);
    },

    /** Decode one Branch. */
    decode(reader: BinaryReader): Branch {
        return decodeBranch(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Branch): Json {
        return toJsonBranch(value);
    },

    /** Return one Branch from one JSON value. */
    fromJson(value: Json): Branch {
        return fromJsonBranch(value);
    },
};

/** Encode one Branch. */
export function encodeBranch(writer: BinaryWriter, value: Branch): void {
    writer.writeString(value.name);
    encodeRevision(writer, value.revision);
}

/** Decode one Branch. */
export function decodeBranch(reader: BinaryReader): Branch {
    const name = reader.readString();
    const revision = decodeRevision(reader);

    return {
        name,
        revision,
    };
}

/** Return one JSON value for one Branch. */
export function toJsonBranch(value: Branch): Json {
    return {
        name: value.name,
        revision: toJsonRevision(value.revision),
    };
}

/** Return one Branch from one JSON value. */
export function fromJsonBranch(value: Json): Branch {
    const object = jsonObject(value);

    return {
        name: jsonString(jsonField(object, "name")),
        revision: fromJsonRevision(jsonField(object, "revision")),
    };
}

/** Files selected by one branch operation. */
export type FileSelection =
    /** Every changed file. */
    | {
          readonly kind: "all";
      }
    /** Selected paths within the workspace. */
    | {
          readonly kind: "paths";
          readonly paths: ReadonlyArray<string>;
      }
;

export const FileSelection = {
    /** Every changed file. */
    all(): FileSelection {
        return { kind: "all" };
    },

    /** Selected paths within the workspace. */
    paths(paths: ReadonlyArray<string>): FileSelection {
        return { kind: "paths", paths };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: FileSelection): void {
        encodeFileSelection(writer, value);
    },

    /** Decode one FileSelection. */
    decode(reader: BinaryReader): FileSelection {
        return decodeFileSelection(reader);
    },

    /** Return this value as JSON. */
    toJson(value: FileSelection): Json {
        return toJsonFileSelection(value);
    },

    /** Return one FileSelection from one JSON value. */
    fromJson(value: Json): FileSelection {
        return fromJsonFileSelection(value);
    },
};

/** Encode one FileSelection. */
export function encodeFileSelection(writer: BinaryWriter, value: FileSelection): void {
    switch (value.kind) {
        case "all":
            writer.writeUnsigned(0);
            return;
        case "paths":
            writer.writeUnsigned(1);
            writer.writeUnsigned(value.paths.length);
            for (const item0 of value.paths) {
                writer.writeString(item0);
            }
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one FileSelection. */
export function decodeFileSelection(reader: BinaryReader): FileSelection {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            return { kind: "all" };
        }
        case 1: {
            const paths = (() => { const length0 = reader.readNumber(); const items0: Array<string> = []; for (let index = 0; index < length0; index += 1) { items0.push(reader.readString()); } return items0; })();

            return { kind: "paths", paths };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one FileSelection. */
export function toJsonFileSelection(value: FileSelection): Json {
    switch (value.kind) {
        case "all":
            return {
                kind: "all",
            };
        case "paths":
            return {
                kind: "paths",
                paths: value.paths.map((item0) => item0),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one FileSelection from one JSON value. */
export function fromJsonFileSelection(value: Json): FileSelection {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "all":
            return {
                kind,
            };
        case "paths":
            return {
                kind,
                paths: jsonArray(jsonField(object, "paths")).map((item0) => jsonString(item0)),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}
