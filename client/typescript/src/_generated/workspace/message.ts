/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, jsonField, jsonObject, jsonString } from "../../protocol/serde.js";

/** Message payload emitted by one workspace operation. */
export type Message = {
    /** Message severity. */
    readonly kind: MessageKind;
    /** Stable message code. */
    readonly code: string;
    /** Human-readable message. */
    readonly message: string;
};

export const Message = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: Message): void {
        encodeMessage(writer, value);
    },

    /** Decode one Message. */
    decode(reader: BinaryReader): Message {
        return decodeMessage(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Message): Json {
        return toJsonMessage(value);
    },

    /** Return one Message from one JSON value. */
    fromJson(value: Json): Message {
        return fromJsonMessage(value);
    },
};

/** Encode one Message. */
export function encodeMessage(writer: BinaryWriter, value: Message): void {
    encodeMessageKind(writer, value.kind);
    writer.writeString(value.code);
    writer.writeString(value.message);
}

/** Decode one Message. */
export function decodeMessage(reader: BinaryReader): Message {
    const kind = decodeMessageKind(reader);
    const code = reader.readString();
    const message = reader.readString();

    return {
        kind,
        code,
        message,
    };
}

/** Return one JSON value for one Message. */
export function toJsonMessage(value: Message): Json {
    return {
        kind: toJsonMessageKind(value.kind),
        code: value.code,
        message: value.message,
    };
}

/** Return one Message from one JSON value. */
export function fromJsonMessage(value: Json): Message {
    const object = jsonObject(value);

    return {
        kind: fromJsonMessageKind(jsonField(object, "kind")),
        code: jsonString(jsonField(object, "code")),
        message: jsonString(jsonField(object, "message")),
    };
}

/** Message severity for one workspace operation. */
export type MessageKind = "info" | "warning" | "error";

export const MessageKind = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: MessageKind): void {
        encodeMessageKind(writer, value);
    },

    /** Decode one MessageKind. */
    decode(reader: BinaryReader): MessageKind {
        return decodeMessageKind(reader);
    },

    /** Return this value as JSON. */
    toJson(value: MessageKind): Json {
        return toJsonMessageKind(value);
    },

    /** Return one MessageKind from one JSON value. */
    fromJson(value: Json): MessageKind {
        return fromJsonMessageKind(value);
    },
};

/** Encode one MessageKind. */
export function encodeMessageKind(writer: BinaryWriter, value: MessageKind): void {
    switch (value) {
        case "info":
            writer.writeUnsigned(0);
            return;
        case "warning":
            writer.writeUnsigned(1);
            return;
        case "error":
            writer.writeUnsigned(2);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one MessageKind. */
export function decodeMessageKind(reader: BinaryReader): MessageKind {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "info";
        case 1:
            return "warning";
        case 2:
            return "error";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one MessageKind. */
export function toJsonMessageKind(value: MessageKind): Json {
    return value;
}

/** Return one MessageKind from one JSON value. */
export function fromJsonMessageKind(value: Json): MessageKind {
    const variant = jsonString(value);

    switch (variant) {
        case "info":
            return "info";
        case "warning":
            return "warning";
        case "error":
            return "error";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}
