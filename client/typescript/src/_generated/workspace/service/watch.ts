/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, jsonArray, jsonBigint, jsonField, jsonInteger, jsonObject, jsonString } from "../../../protocol/serde.js";

/** Request to watch workspace roots. */
export type WatchRequest = {
    /** Roots to watch. */
    readonly roots: ReadonlyArray<string>;
    /** Maximum event coalescing window in milliseconds. */
    readonly coalesceWindowMilliseconds: bigint;
    /** Maximum event and status count per batch. */
    readonly maxBatchSize: number;
};

export const WatchRequest = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: WatchRequest): void {
        encodeWatchRequest(writer, value);
    },

    /** Decode one WatchRequest. */
    decode(reader: BinaryReader): WatchRequest {
        return decodeWatchRequest(reader);
    },

    /** Return this value as JSON. */
    toJson(value: WatchRequest): Json {
        return toJsonWatchRequest(value);
    },

    /** Return one WatchRequest from one JSON value. */
    fromJson(value: Json): WatchRequest {
        return fromJsonWatchRequest(value);
    },
};

/** Encode one WatchRequest. */
export function encodeWatchRequest(writer: BinaryWriter, value: WatchRequest): void {
    writer.writeUnsigned(value.roots.length);
    for (const item0 of value.roots) {
        writer.writeString(item0);
    }
    writer.writeUnsigned(value.coalesceWindowMilliseconds);
    writer.writeUnsigned(value.maxBatchSize);
}

/** Decode one WatchRequest. */
export function decodeWatchRequest(reader: BinaryReader): WatchRequest {
    const roots = (() => { const length0 = reader.readNumber(); const items0: Array<string> = []; for (let index = 0; index < length0; index += 1) { items0.push(reader.readString()); } return items0; })();
    const coalesceWindowMilliseconds = reader.readUnsigned();
    const maxBatchSize = reader.readNumber();

    return {
        roots,
        coalesceWindowMilliseconds,
        maxBatchSize,
    };
}

/** Return one JSON value for one WatchRequest. */
export function toJsonWatchRequest(value: WatchRequest): Json {
    return {
        roots: value.roots.map((item0) => item0),
        coalesceWindowMilliseconds: value.coalesceWindowMilliseconds.toString(),
        maxBatchSize: value.maxBatchSize,
    };
}

/** Return one WatchRequest from one JSON value. */
export function fromJsonWatchRequest(value: Json): WatchRequest {
    const object = jsonObject(value);

    return {
        roots: jsonArray(jsonField(object, "roots")).map((item0) => jsonString(item0)),
        coalesceWindowMilliseconds: jsonBigint(jsonField(object, "coalesceWindowMilliseconds")),
        maxBatchSize: jsonInteger(jsonField(object, "maxBatchSize")),
    };
}
