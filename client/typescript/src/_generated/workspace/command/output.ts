/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, jsonArray, jsonBool, jsonField, jsonInteger, jsonNull, jsonObject, jsonOptional } from "../../../protocol/serde.js";
import type { TraceSnapshot } from "../../repository/provider/trace.js";
import type { Revision } from "../../repository/revision.js";
import type { Diagnostic } from "../../source/diagnostic/diagnostic.js";
import type { BuildPayload } from "./build.js";
import type { CachePayload } from "./cache.js";
import type { CleanPayload } from "./clean.js";
import type { CommandMessagePayload } from "./common.js";
import type { CommandOutputChunk } from "./common.js";
import type { CommandOutputFile } from "./common.js";
import type { DoctorPayload } from "./doctor.js";
import type { FormatPayload } from "./format.js";
import type { InfoPayload } from "./info.js";
import type { QueryPayload } from "./query.js";
import type { RewritePayload } from "./rewrite.js";
import type { SettingsPayload } from "./settings.js";
import type { TargetsPayload } from "./targets.js";
import type { TaskPayload } from "./task.js";
import type { FileImage } from "../file/image.js";
import type { Message } from "../message.js";
import { decodeTraceSnapshot, encodeTraceSnapshot, fromJsonTraceSnapshot, toJsonTraceSnapshot } from "../../repository/provider/trace.js";
import { decodeRevision, encodeRevision, fromJsonRevision, toJsonRevision } from "../../repository/revision.js";
import { decodeDiagnostic, encodeDiagnostic, fromJsonDiagnostic, toJsonDiagnostic } from "../../source/diagnostic/diagnostic.js";
import { decodeBuildPayload, encodeBuildPayload, fromJsonBuildPayload, toJsonBuildPayload } from "./build.js";
import { decodeCachePayload, encodeCachePayload, fromJsonCachePayload, toJsonCachePayload } from "./cache.js";
import { decodeCleanPayload, encodeCleanPayload, fromJsonCleanPayload, toJsonCleanPayload } from "./clean.js";
import { decodeCommandMessagePayload, encodeCommandMessagePayload, fromJsonCommandMessagePayload, toJsonCommandMessagePayload } from "./common.js";
import { decodeCommandOutputChunk, encodeCommandOutputChunk, fromJsonCommandOutputChunk, toJsonCommandOutputChunk } from "./common.js";
import { decodeCommandOutputFile, encodeCommandOutputFile, fromJsonCommandOutputFile, toJsonCommandOutputFile } from "./common.js";
import { decodeDoctorPayload, encodeDoctorPayload, fromJsonDoctorPayload, toJsonDoctorPayload } from "./doctor.js";
import { decodeFormatPayload, encodeFormatPayload, fromJsonFormatPayload, toJsonFormatPayload } from "./format.js";
import { decodeInfoPayload, encodeInfoPayload, fromJsonInfoPayload, toJsonInfoPayload } from "./info.js";
import { decodeQueryPayload, encodeQueryPayload, fromJsonQueryPayload, toJsonQueryPayload } from "./query.js";
import { decodeRewritePayload, encodeRewritePayload, fromJsonRewritePayload, toJsonRewritePayload } from "./rewrite.js";
import { decodeSettingsPayload, encodeSettingsPayload, fromJsonSettingsPayload, toJsonSettingsPayload } from "./settings.js";
import { decodeTargetsPayload, encodeTargetsPayload, fromJsonTargetsPayload, toJsonTargetsPayload } from "./targets.js";
import { decodeTaskPayload, encodeTaskPayload, fromJsonTaskPayload, toJsonTaskPayload } from "./task.js";
import { decodeFileImage, encodeFileImage, fromJsonFileImage, toJsonFileImage } from "../file/image.js";
import { decodeMessage, encodeMessage, fromJsonMessage, toJsonMessage } from "../message.js";

/** Output produced by one workspace command. */
export type BenchOutput = {
    /** Revision used for this operation. */
    readonly revision: Revision;
    /** Whether the operation succeeded. */
    readonly success: boolean;
    /** Exit code for the operation. */
    readonly exitCode: number;
    /** Diagnostics produced by the operation. */
    readonly diagnostics: ReadonlyArray<Diagnostic>;
    /** File images referenced by diagnostics and command data. */
    readonly files: ReadonlyArray<FileImage>;
    /** Messages produced by operation execution. */
    readonly messages: ReadonlyArray<Message>;
    /** Stream output collected during execution. */
    readonly output: ReadonlyArray<CommandOutputChunk>;
    /** Generated output files. */
    readonly outputs: ReadonlyArray<CommandOutputFile>;
    /** Timing trace when requested by the command. */
    readonly trace?: TraceSnapshot;
    /** Operation payload. */
    readonly data: CommandMessagePayload;
    /** Count of modules involved. */
    readonly moduleCount: number;
    /** Count of profiles involved. */
    readonly profileCount: number;
    /** Count of targets involved. */
    readonly targetCount: number;
};

export const BenchOutput = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: BenchOutput): void {
        encodeBenchOutput(writer, value);
    },

    /** Decode one BenchOutput. */
    decode(reader: BinaryReader): BenchOutput {
        return decodeBenchOutput(reader);
    },

    /** Return this value as JSON. */
    toJson(value: BenchOutput): Json {
        return toJsonBenchOutput(value);
    },

    /** Return one BenchOutput from one JSON value. */
    fromJson(value: Json): BenchOutput {
        return fromJsonBenchOutput(value);
    },
};

/** Encode one BenchOutput. */
export function encodeBenchOutput(writer: BinaryWriter, value: BenchOutput): void {
    encodeRevision(writer, value.revision);
    writer.writeBool(value.success);
    writer.writeSigned(value.exitCode);
    writer.writeUnsigned(value.diagnostics.length);
    for (const item3 of value.diagnostics) {
        encodeDiagnostic(writer, item3);
    }
    writer.writeUnsigned(value.files.length);
    for (const item4 of value.files) {
        encodeFileImage(writer, item4);
    }
    writer.writeUnsigned(value.messages.length);
    for (const item5 of value.messages) {
        encodeMessage(writer, item5);
    }
    writer.writeUnsigned(value.output.length);
    for (const item6 of value.output) {
        encodeCommandOutputChunk(writer, item6);
    }
    writer.writeUnsigned(value.outputs.length);
    for (const item7 of value.outputs) {
        encodeCommandOutputFile(writer, item7);
    }
    writer.writeOption(value.trace, (value8) => {
        encodeTraceSnapshot(writer, value8);
    });
    encodeCommandMessagePayload(writer, value.data);
    writer.writeUnsigned(value.moduleCount);
    writer.writeUnsigned(value.profileCount);
    writer.writeUnsigned(value.targetCount);
}

/** Decode one BenchOutput. */
export function decodeBenchOutput(reader: BinaryReader): BenchOutput {
    const revision = decodeRevision(reader);
    const success = reader.readBool();
    const exitCode = reader.readSignedNumber();
    const diagnostics = (() => { const length3 = reader.readNumber(); const items3: Array<Diagnostic> = []; for (let index = 0; index < length3; index += 1) { items3.push(decodeDiagnostic(reader)); } return items3; })();
    const files = (() => { const length4 = reader.readNumber(); const items4: Array<FileImage> = []; for (let index = 0; index < length4; index += 1) { items4.push(decodeFileImage(reader)); } return items4; })();
    const messages = (() => { const length5 = reader.readNumber(); const items5: Array<Message> = []; for (let index = 0; index < length5; index += 1) { items5.push(decodeMessage(reader)); } return items5; })();
    const output = (() => { const length6 = reader.readNumber(); const items6: Array<CommandOutputChunk> = []; for (let index = 0; index < length6; index += 1) { items6.push(decodeCommandOutputChunk(reader)); } return items6; })();
    const outputs = (() => { const length7 = reader.readNumber(); const items7: Array<CommandOutputFile> = []; for (let index = 0; index < length7; index += 1) { items7.push(decodeCommandOutputFile(reader)); } return items7; })();
    const trace = reader.readOption(() => decodeTraceSnapshot(reader));
    const data = decodeCommandMessagePayload(reader);
    const moduleCount = reader.readNumber();
    const profileCount = reader.readNumber();
    const targetCount = reader.readNumber();

    return {
        revision,
        success,
        exitCode,
        diagnostics,
        files,
        messages,
        output,
        outputs,
        ...(trace === undefined ? {} : { trace }),
        data,
        moduleCount,
        profileCount,
        targetCount,
    };
}

/** Return one JSON value for one BenchOutput. */
export function toJsonBenchOutput(value: BenchOutput): Json {
    return {
        revision: toJsonRevision(value.revision),
        success: value.success,
        exitCode: value.exitCode,
        diagnostics: value.diagnostics.map((item0) => toJsonDiagnostic(item0)),
        files: value.files.map((item0) => toJsonFileImage(item0)),
        messages: value.messages.map((item0) => toJsonMessage(item0)),
        output: value.output.map((item0) => toJsonCommandOutputChunk(item0)),
        outputs: value.outputs.map((item0) => toJsonCommandOutputFile(item0)),
        ...(value.trace === undefined ? {} : { trace: toJsonTraceSnapshot(value.trace) }),
        data: toJsonCommandMessagePayload(value.data),
        moduleCount: value.moduleCount,
        profileCount: value.profileCount,
        targetCount: value.targetCount,
    };
}

/** Return one BenchOutput from one JSON value. */
export function fromJsonBenchOutput(value: Json): BenchOutput {
    const object = jsonObject(value);

    return {
        revision: fromJsonRevision(jsonField(object, "revision")),
        success: jsonBool(jsonField(object, "success")),
        exitCode: jsonInteger(jsonField(object, "exitCode")),
        diagnostics: jsonArray(jsonField(object, "diagnostics")).map((item0) => fromJsonDiagnostic(item0)),
        files: jsonArray(jsonField(object, "files")).map((item0) => fromJsonFileImage(item0)),
        messages: jsonArray(jsonField(object, "messages")).map((item0) => fromJsonMessage(item0)),
        output: jsonArray(jsonField(object, "output")).map((item0) => fromJsonCommandOutputChunk(item0)),
        outputs: jsonArray(jsonField(object, "outputs")).map((item0) => fromJsonCommandOutputFile(item0)),
        trace: jsonOptional(object, "trace", (value) => fromJsonTraceSnapshot(value)),
        data: fromJsonCommandMessagePayload(jsonField(object, "data")),
        moduleCount: jsonInteger(jsonField(object, "moduleCount")),
        profileCount: jsonInteger(jsonField(object, "profileCount")),
        targetCount: jsonInteger(jsonField(object, "targetCount")),
    };
}

/** Output produced by one workspace command. */
export type BuildOutput = {
    /** Revision used for this operation. */
    readonly revision: Revision;
    /** Whether the operation succeeded. */
    readonly success: boolean;
    /** Exit code for the operation. */
    readonly exitCode: number;
    /** Diagnostics produced by the operation. */
    readonly diagnostics: ReadonlyArray<Diagnostic>;
    /** File images referenced by diagnostics and command data. */
    readonly files: ReadonlyArray<FileImage>;
    /** Messages produced by operation execution. */
    readonly messages: ReadonlyArray<Message>;
    /** Stream output collected during execution. */
    readonly output: ReadonlyArray<CommandOutputChunk>;
    /** Generated output files. */
    readonly outputs: ReadonlyArray<CommandOutputFile>;
    /** Timing trace when requested by the command. */
    readonly trace?: TraceSnapshot;
    /** Operation payload. */
    readonly data: BuildPayload;
    /** Count of modules involved. */
    readonly moduleCount: number;
    /** Count of profiles involved. */
    readonly profileCount: number;
    /** Count of targets involved. */
    readonly targetCount: number;
};

export const BuildOutput = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: BuildOutput): void {
        encodeBuildOutput(writer, value);
    },

    /** Decode one BuildOutput. */
    decode(reader: BinaryReader): BuildOutput {
        return decodeBuildOutput(reader);
    },

    /** Return this value as JSON. */
    toJson(value: BuildOutput): Json {
        return toJsonBuildOutput(value);
    },

    /** Return one BuildOutput from one JSON value. */
    fromJson(value: Json): BuildOutput {
        return fromJsonBuildOutput(value);
    },
};

/** Encode one BuildOutput. */
export function encodeBuildOutput(writer: BinaryWriter, value: BuildOutput): void {
    encodeRevision(writer, value.revision);
    writer.writeBool(value.success);
    writer.writeSigned(value.exitCode);
    writer.writeUnsigned(value.diagnostics.length);
    for (const item3 of value.diagnostics) {
        encodeDiagnostic(writer, item3);
    }
    writer.writeUnsigned(value.files.length);
    for (const item4 of value.files) {
        encodeFileImage(writer, item4);
    }
    writer.writeUnsigned(value.messages.length);
    for (const item5 of value.messages) {
        encodeMessage(writer, item5);
    }
    writer.writeUnsigned(value.output.length);
    for (const item6 of value.output) {
        encodeCommandOutputChunk(writer, item6);
    }
    writer.writeUnsigned(value.outputs.length);
    for (const item7 of value.outputs) {
        encodeCommandOutputFile(writer, item7);
    }
    writer.writeOption(value.trace, (value8) => {
        encodeTraceSnapshot(writer, value8);
    });
    encodeBuildPayload(writer, value.data);
    writer.writeUnsigned(value.moduleCount);
    writer.writeUnsigned(value.profileCount);
    writer.writeUnsigned(value.targetCount);
}

/** Decode one BuildOutput. */
export function decodeBuildOutput(reader: BinaryReader): BuildOutput {
    const revision = decodeRevision(reader);
    const success = reader.readBool();
    const exitCode = reader.readSignedNumber();
    const diagnostics = (() => { const length3 = reader.readNumber(); const items3: Array<Diagnostic> = []; for (let index = 0; index < length3; index += 1) { items3.push(decodeDiagnostic(reader)); } return items3; })();
    const files = (() => { const length4 = reader.readNumber(); const items4: Array<FileImage> = []; for (let index = 0; index < length4; index += 1) { items4.push(decodeFileImage(reader)); } return items4; })();
    const messages = (() => { const length5 = reader.readNumber(); const items5: Array<Message> = []; for (let index = 0; index < length5; index += 1) { items5.push(decodeMessage(reader)); } return items5; })();
    const output = (() => { const length6 = reader.readNumber(); const items6: Array<CommandOutputChunk> = []; for (let index = 0; index < length6; index += 1) { items6.push(decodeCommandOutputChunk(reader)); } return items6; })();
    const outputs = (() => { const length7 = reader.readNumber(); const items7: Array<CommandOutputFile> = []; for (let index = 0; index < length7; index += 1) { items7.push(decodeCommandOutputFile(reader)); } return items7; })();
    const trace = reader.readOption(() => decodeTraceSnapshot(reader));
    const data = decodeBuildPayload(reader);
    const moduleCount = reader.readNumber();
    const profileCount = reader.readNumber();
    const targetCount = reader.readNumber();

    return {
        revision,
        success,
        exitCode,
        diagnostics,
        files,
        messages,
        output,
        outputs,
        ...(trace === undefined ? {} : { trace }),
        data,
        moduleCount,
        profileCount,
        targetCount,
    };
}

/** Return one JSON value for one BuildOutput. */
export function toJsonBuildOutput(value: BuildOutput): Json {
    return {
        revision: toJsonRevision(value.revision),
        success: value.success,
        exitCode: value.exitCode,
        diagnostics: value.diagnostics.map((item0) => toJsonDiagnostic(item0)),
        files: value.files.map((item0) => toJsonFileImage(item0)),
        messages: value.messages.map((item0) => toJsonMessage(item0)),
        output: value.output.map((item0) => toJsonCommandOutputChunk(item0)),
        outputs: value.outputs.map((item0) => toJsonCommandOutputFile(item0)),
        ...(value.trace === undefined ? {} : { trace: toJsonTraceSnapshot(value.trace) }),
        data: toJsonBuildPayload(value.data),
        moduleCount: value.moduleCount,
        profileCount: value.profileCount,
        targetCount: value.targetCount,
    };
}

/** Return one BuildOutput from one JSON value. */
export function fromJsonBuildOutput(value: Json): BuildOutput {
    const object = jsonObject(value);

    return {
        revision: fromJsonRevision(jsonField(object, "revision")),
        success: jsonBool(jsonField(object, "success")),
        exitCode: jsonInteger(jsonField(object, "exitCode")),
        diagnostics: jsonArray(jsonField(object, "diagnostics")).map((item0) => fromJsonDiagnostic(item0)),
        files: jsonArray(jsonField(object, "files")).map((item0) => fromJsonFileImage(item0)),
        messages: jsonArray(jsonField(object, "messages")).map((item0) => fromJsonMessage(item0)),
        output: jsonArray(jsonField(object, "output")).map((item0) => fromJsonCommandOutputChunk(item0)),
        outputs: jsonArray(jsonField(object, "outputs")).map((item0) => fromJsonCommandOutputFile(item0)),
        trace: jsonOptional(object, "trace", (value) => fromJsonTraceSnapshot(value)),
        data: fromJsonBuildPayload(jsonField(object, "data")),
        moduleCount: jsonInteger(jsonField(object, "moduleCount")),
        profileCount: jsonInteger(jsonField(object, "profileCount")),
        targetCount: jsonInteger(jsonField(object, "targetCount")),
    };
}

/** Output produced by one workspace command. */
export type CacheOutput = {
    /** Revision used for this operation. */
    readonly revision: Revision;
    /** Whether the operation succeeded. */
    readonly success: boolean;
    /** Exit code for the operation. */
    readonly exitCode: number;
    /** Diagnostics produced by the operation. */
    readonly diagnostics: ReadonlyArray<Diagnostic>;
    /** File images referenced by diagnostics and command data. */
    readonly files: ReadonlyArray<FileImage>;
    /** Messages produced by operation execution. */
    readonly messages: ReadonlyArray<Message>;
    /** Stream output collected during execution. */
    readonly output: ReadonlyArray<CommandOutputChunk>;
    /** Generated output files. */
    readonly outputs: ReadonlyArray<CommandOutputFile>;
    /** Timing trace when requested by the command. */
    readonly trace?: TraceSnapshot;
    /** Operation payload. */
    readonly data: CachePayload;
    /** Count of modules involved. */
    readonly moduleCount: number;
    /** Count of profiles involved. */
    readonly profileCount: number;
    /** Count of targets involved. */
    readonly targetCount: number;
};

export const CacheOutput = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: CacheOutput): void {
        encodeCacheOutput(writer, value);
    },

    /** Decode one CacheOutput. */
    decode(reader: BinaryReader): CacheOutput {
        return decodeCacheOutput(reader);
    },

    /** Return this value as JSON. */
    toJson(value: CacheOutput): Json {
        return toJsonCacheOutput(value);
    },

    /** Return one CacheOutput from one JSON value. */
    fromJson(value: Json): CacheOutput {
        return fromJsonCacheOutput(value);
    },
};

/** Encode one CacheOutput. */
export function encodeCacheOutput(writer: BinaryWriter, value: CacheOutput): void {
    encodeRevision(writer, value.revision);
    writer.writeBool(value.success);
    writer.writeSigned(value.exitCode);
    writer.writeUnsigned(value.diagnostics.length);
    for (const item3 of value.diagnostics) {
        encodeDiagnostic(writer, item3);
    }
    writer.writeUnsigned(value.files.length);
    for (const item4 of value.files) {
        encodeFileImage(writer, item4);
    }
    writer.writeUnsigned(value.messages.length);
    for (const item5 of value.messages) {
        encodeMessage(writer, item5);
    }
    writer.writeUnsigned(value.output.length);
    for (const item6 of value.output) {
        encodeCommandOutputChunk(writer, item6);
    }
    writer.writeUnsigned(value.outputs.length);
    for (const item7 of value.outputs) {
        encodeCommandOutputFile(writer, item7);
    }
    writer.writeOption(value.trace, (value8) => {
        encodeTraceSnapshot(writer, value8);
    });
    encodeCachePayload(writer, value.data);
    writer.writeUnsigned(value.moduleCount);
    writer.writeUnsigned(value.profileCount);
    writer.writeUnsigned(value.targetCount);
}

/** Decode one CacheOutput. */
export function decodeCacheOutput(reader: BinaryReader): CacheOutput {
    const revision = decodeRevision(reader);
    const success = reader.readBool();
    const exitCode = reader.readSignedNumber();
    const diagnostics = (() => { const length3 = reader.readNumber(); const items3: Array<Diagnostic> = []; for (let index = 0; index < length3; index += 1) { items3.push(decodeDiagnostic(reader)); } return items3; })();
    const files = (() => { const length4 = reader.readNumber(); const items4: Array<FileImage> = []; for (let index = 0; index < length4; index += 1) { items4.push(decodeFileImage(reader)); } return items4; })();
    const messages = (() => { const length5 = reader.readNumber(); const items5: Array<Message> = []; for (let index = 0; index < length5; index += 1) { items5.push(decodeMessage(reader)); } return items5; })();
    const output = (() => { const length6 = reader.readNumber(); const items6: Array<CommandOutputChunk> = []; for (let index = 0; index < length6; index += 1) { items6.push(decodeCommandOutputChunk(reader)); } return items6; })();
    const outputs = (() => { const length7 = reader.readNumber(); const items7: Array<CommandOutputFile> = []; for (let index = 0; index < length7; index += 1) { items7.push(decodeCommandOutputFile(reader)); } return items7; })();
    const trace = reader.readOption(() => decodeTraceSnapshot(reader));
    const data = decodeCachePayload(reader);
    const moduleCount = reader.readNumber();
    const profileCount = reader.readNumber();
    const targetCount = reader.readNumber();

    return {
        revision,
        success,
        exitCode,
        diagnostics,
        files,
        messages,
        output,
        outputs,
        ...(trace === undefined ? {} : { trace }),
        data,
        moduleCount,
        profileCount,
        targetCount,
    };
}

/** Return one JSON value for one CacheOutput. */
export function toJsonCacheOutput(value: CacheOutput): Json {
    return {
        revision: toJsonRevision(value.revision),
        success: value.success,
        exitCode: value.exitCode,
        diagnostics: value.diagnostics.map((item0) => toJsonDiagnostic(item0)),
        files: value.files.map((item0) => toJsonFileImage(item0)),
        messages: value.messages.map((item0) => toJsonMessage(item0)),
        output: value.output.map((item0) => toJsonCommandOutputChunk(item0)),
        outputs: value.outputs.map((item0) => toJsonCommandOutputFile(item0)),
        ...(value.trace === undefined ? {} : { trace: toJsonTraceSnapshot(value.trace) }),
        data: toJsonCachePayload(value.data),
        moduleCount: value.moduleCount,
        profileCount: value.profileCount,
        targetCount: value.targetCount,
    };
}

/** Return one CacheOutput from one JSON value. */
export function fromJsonCacheOutput(value: Json): CacheOutput {
    const object = jsonObject(value);

    return {
        revision: fromJsonRevision(jsonField(object, "revision")),
        success: jsonBool(jsonField(object, "success")),
        exitCode: jsonInteger(jsonField(object, "exitCode")),
        diagnostics: jsonArray(jsonField(object, "diagnostics")).map((item0) => fromJsonDiagnostic(item0)),
        files: jsonArray(jsonField(object, "files")).map((item0) => fromJsonFileImage(item0)),
        messages: jsonArray(jsonField(object, "messages")).map((item0) => fromJsonMessage(item0)),
        output: jsonArray(jsonField(object, "output")).map((item0) => fromJsonCommandOutputChunk(item0)),
        outputs: jsonArray(jsonField(object, "outputs")).map((item0) => fromJsonCommandOutputFile(item0)),
        trace: jsonOptional(object, "trace", (value) => fromJsonTraceSnapshot(value)),
        data: fromJsonCachePayload(jsonField(object, "data")),
        moduleCount: jsonInteger(jsonField(object, "moduleCount")),
        profileCount: jsonInteger(jsonField(object, "profileCount")),
        targetCount: jsonInteger(jsonField(object, "targetCount")),
    };
}

/** Output produced by one workspace command. */
export type CheckOutput = {
    /** Revision used for this operation. */
    readonly revision: Revision;
    /** Whether the operation succeeded. */
    readonly success: boolean;
    /** Exit code for the operation. */
    readonly exitCode: number;
    /** Diagnostics produced by the operation. */
    readonly diagnostics: ReadonlyArray<Diagnostic>;
    /** File images referenced by diagnostics and command data. */
    readonly files: ReadonlyArray<FileImage>;
    /** Messages produced by operation execution. */
    readonly messages: ReadonlyArray<Message>;
    /** Stream output collected during execution. */
    readonly output: ReadonlyArray<CommandOutputChunk>;
    /** Generated output files. */
    readonly outputs: ReadonlyArray<CommandOutputFile>;
    /** Timing trace when requested by the command. */
    readonly trace?: TraceSnapshot;
    /** Operation payload. */
    readonly data: null;
    /** Count of modules involved. */
    readonly moduleCount: number;
    /** Count of profiles involved. */
    readonly profileCount: number;
    /** Count of targets involved. */
    readonly targetCount: number;
};

export const CheckOutput = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: CheckOutput): void {
        encodeCheckOutput(writer, value);
    },

    /** Decode one CheckOutput. */
    decode(reader: BinaryReader): CheckOutput {
        return decodeCheckOutput(reader);
    },

    /** Return this value as JSON. */
    toJson(value: CheckOutput): Json {
        return toJsonCheckOutput(value);
    },

    /** Return one CheckOutput from one JSON value. */
    fromJson(value: Json): CheckOutput {
        return fromJsonCheckOutput(value);
    },
};

/** Encode one CheckOutput. */
export function encodeCheckOutput(writer: BinaryWriter, value: CheckOutput): void {
    encodeRevision(writer, value.revision);
    writer.writeBool(value.success);
    writer.writeSigned(value.exitCode);
    writer.writeUnsigned(value.diagnostics.length);
    for (const item3 of value.diagnostics) {
        encodeDiagnostic(writer, item3);
    }
    writer.writeUnsigned(value.files.length);
    for (const item4 of value.files) {
        encodeFileImage(writer, item4);
    }
    writer.writeUnsigned(value.messages.length);
    for (const item5 of value.messages) {
        encodeMessage(writer, item5);
    }
    writer.writeUnsigned(value.output.length);
    for (const item6 of value.output) {
        encodeCommandOutputChunk(writer, item6);
    }
    writer.writeUnsigned(value.outputs.length);
    for (const item7 of value.outputs) {
        encodeCommandOutputFile(writer, item7);
    }
    writer.writeOption(value.trace, (value8) => {
        encodeTraceSnapshot(writer, value8);
    });
    writer.writeUnsigned(value.moduleCount);
    writer.writeUnsigned(value.profileCount);
    writer.writeUnsigned(value.targetCount);
}

/** Decode one CheckOutput. */
export function decodeCheckOutput(reader: BinaryReader): CheckOutput {
    const revision = decodeRevision(reader);
    const success = reader.readBool();
    const exitCode = reader.readSignedNumber();
    const diagnostics = (() => { const length3 = reader.readNumber(); const items3: Array<Diagnostic> = []; for (let index = 0; index < length3; index += 1) { items3.push(decodeDiagnostic(reader)); } return items3; })();
    const files = (() => { const length4 = reader.readNumber(); const items4: Array<FileImage> = []; for (let index = 0; index < length4; index += 1) { items4.push(decodeFileImage(reader)); } return items4; })();
    const messages = (() => { const length5 = reader.readNumber(); const items5: Array<Message> = []; for (let index = 0; index < length5; index += 1) { items5.push(decodeMessage(reader)); } return items5; })();
    const output = (() => { const length6 = reader.readNumber(); const items6: Array<CommandOutputChunk> = []; for (let index = 0; index < length6; index += 1) { items6.push(decodeCommandOutputChunk(reader)); } return items6; })();
    const outputs = (() => { const length7 = reader.readNumber(); const items7: Array<CommandOutputFile> = []; for (let index = 0; index < length7; index += 1) { items7.push(decodeCommandOutputFile(reader)); } return items7; })();
    const trace = reader.readOption(() => decodeTraceSnapshot(reader));
    const data = null;
    const moduleCount = reader.readNumber();
    const profileCount = reader.readNumber();
    const targetCount = reader.readNumber();

    return {
        revision,
        success,
        exitCode,
        diagnostics,
        files,
        messages,
        output,
        outputs,
        ...(trace === undefined ? {} : { trace }),
        data,
        moduleCount,
        profileCount,
        targetCount,
    };
}

/** Return one JSON value for one CheckOutput. */
export function toJsonCheckOutput(value: CheckOutput): Json {
    return {
        revision: toJsonRevision(value.revision),
        success: value.success,
        exitCode: value.exitCode,
        diagnostics: value.diagnostics.map((item0) => toJsonDiagnostic(item0)),
        files: value.files.map((item0) => toJsonFileImage(item0)),
        messages: value.messages.map((item0) => toJsonMessage(item0)),
        output: value.output.map((item0) => toJsonCommandOutputChunk(item0)),
        outputs: value.outputs.map((item0) => toJsonCommandOutputFile(item0)),
        ...(value.trace === undefined ? {} : { trace: toJsonTraceSnapshot(value.trace) }),
        data: null,
        moduleCount: value.moduleCount,
        profileCount: value.profileCount,
        targetCount: value.targetCount,
    };
}

/** Return one CheckOutput from one JSON value. */
export function fromJsonCheckOutput(value: Json): CheckOutput {
    const object = jsonObject(value);

    return {
        revision: fromJsonRevision(jsonField(object, "revision")),
        success: jsonBool(jsonField(object, "success")),
        exitCode: jsonInteger(jsonField(object, "exitCode")),
        diagnostics: jsonArray(jsonField(object, "diagnostics")).map((item0) => fromJsonDiagnostic(item0)),
        files: jsonArray(jsonField(object, "files")).map((item0) => fromJsonFileImage(item0)),
        messages: jsonArray(jsonField(object, "messages")).map((item0) => fromJsonMessage(item0)),
        output: jsonArray(jsonField(object, "output")).map((item0) => fromJsonCommandOutputChunk(item0)),
        outputs: jsonArray(jsonField(object, "outputs")).map((item0) => fromJsonCommandOutputFile(item0)),
        trace: jsonOptional(object, "trace", (value) => fromJsonTraceSnapshot(value)),
        data: jsonNull(jsonField(object, "data")),
        moduleCount: jsonInteger(jsonField(object, "moduleCount")),
        profileCount: jsonInteger(jsonField(object, "profileCount")),
        targetCount: jsonInteger(jsonField(object, "targetCount")),
    };
}

/** Output produced by one workspace command. */
export type CleanOutput = {
    /** Revision used for this operation. */
    readonly revision: Revision;
    /** Whether the operation succeeded. */
    readonly success: boolean;
    /** Exit code for the operation. */
    readonly exitCode: number;
    /** Diagnostics produced by the operation. */
    readonly diagnostics: ReadonlyArray<Diagnostic>;
    /** File images referenced by diagnostics and command data. */
    readonly files: ReadonlyArray<FileImage>;
    /** Messages produced by operation execution. */
    readonly messages: ReadonlyArray<Message>;
    /** Stream output collected during execution. */
    readonly output: ReadonlyArray<CommandOutputChunk>;
    /** Generated output files. */
    readonly outputs: ReadonlyArray<CommandOutputFile>;
    /** Timing trace when requested by the command. */
    readonly trace?: TraceSnapshot;
    /** Operation payload. */
    readonly data: CleanPayload;
    /** Count of modules involved. */
    readonly moduleCount: number;
    /** Count of profiles involved. */
    readonly profileCount: number;
    /** Count of targets involved. */
    readonly targetCount: number;
};

export const CleanOutput = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: CleanOutput): void {
        encodeCleanOutput(writer, value);
    },

    /** Decode one CleanOutput. */
    decode(reader: BinaryReader): CleanOutput {
        return decodeCleanOutput(reader);
    },

    /** Return this value as JSON. */
    toJson(value: CleanOutput): Json {
        return toJsonCleanOutput(value);
    },

    /** Return one CleanOutput from one JSON value. */
    fromJson(value: Json): CleanOutput {
        return fromJsonCleanOutput(value);
    },
};

/** Encode one CleanOutput. */
export function encodeCleanOutput(writer: BinaryWriter, value: CleanOutput): void {
    encodeRevision(writer, value.revision);
    writer.writeBool(value.success);
    writer.writeSigned(value.exitCode);
    writer.writeUnsigned(value.diagnostics.length);
    for (const item3 of value.diagnostics) {
        encodeDiagnostic(writer, item3);
    }
    writer.writeUnsigned(value.files.length);
    for (const item4 of value.files) {
        encodeFileImage(writer, item4);
    }
    writer.writeUnsigned(value.messages.length);
    for (const item5 of value.messages) {
        encodeMessage(writer, item5);
    }
    writer.writeUnsigned(value.output.length);
    for (const item6 of value.output) {
        encodeCommandOutputChunk(writer, item6);
    }
    writer.writeUnsigned(value.outputs.length);
    for (const item7 of value.outputs) {
        encodeCommandOutputFile(writer, item7);
    }
    writer.writeOption(value.trace, (value8) => {
        encodeTraceSnapshot(writer, value8);
    });
    encodeCleanPayload(writer, value.data);
    writer.writeUnsigned(value.moduleCount);
    writer.writeUnsigned(value.profileCount);
    writer.writeUnsigned(value.targetCount);
}

/** Decode one CleanOutput. */
export function decodeCleanOutput(reader: BinaryReader): CleanOutput {
    const revision = decodeRevision(reader);
    const success = reader.readBool();
    const exitCode = reader.readSignedNumber();
    const diagnostics = (() => { const length3 = reader.readNumber(); const items3: Array<Diagnostic> = []; for (let index = 0; index < length3; index += 1) { items3.push(decodeDiagnostic(reader)); } return items3; })();
    const files = (() => { const length4 = reader.readNumber(); const items4: Array<FileImage> = []; for (let index = 0; index < length4; index += 1) { items4.push(decodeFileImage(reader)); } return items4; })();
    const messages = (() => { const length5 = reader.readNumber(); const items5: Array<Message> = []; for (let index = 0; index < length5; index += 1) { items5.push(decodeMessage(reader)); } return items5; })();
    const output = (() => { const length6 = reader.readNumber(); const items6: Array<CommandOutputChunk> = []; for (let index = 0; index < length6; index += 1) { items6.push(decodeCommandOutputChunk(reader)); } return items6; })();
    const outputs = (() => { const length7 = reader.readNumber(); const items7: Array<CommandOutputFile> = []; for (let index = 0; index < length7; index += 1) { items7.push(decodeCommandOutputFile(reader)); } return items7; })();
    const trace = reader.readOption(() => decodeTraceSnapshot(reader));
    const data = decodeCleanPayload(reader);
    const moduleCount = reader.readNumber();
    const profileCount = reader.readNumber();
    const targetCount = reader.readNumber();

    return {
        revision,
        success,
        exitCode,
        diagnostics,
        files,
        messages,
        output,
        outputs,
        ...(trace === undefined ? {} : { trace }),
        data,
        moduleCount,
        profileCount,
        targetCount,
    };
}

/** Return one JSON value for one CleanOutput. */
export function toJsonCleanOutput(value: CleanOutput): Json {
    return {
        revision: toJsonRevision(value.revision),
        success: value.success,
        exitCode: value.exitCode,
        diagnostics: value.diagnostics.map((item0) => toJsonDiagnostic(item0)),
        files: value.files.map((item0) => toJsonFileImage(item0)),
        messages: value.messages.map((item0) => toJsonMessage(item0)),
        output: value.output.map((item0) => toJsonCommandOutputChunk(item0)),
        outputs: value.outputs.map((item0) => toJsonCommandOutputFile(item0)),
        ...(value.trace === undefined ? {} : { trace: toJsonTraceSnapshot(value.trace) }),
        data: toJsonCleanPayload(value.data),
        moduleCount: value.moduleCount,
        profileCount: value.profileCount,
        targetCount: value.targetCount,
    };
}

/** Return one CleanOutput from one JSON value. */
export function fromJsonCleanOutput(value: Json): CleanOutput {
    const object = jsonObject(value);

    return {
        revision: fromJsonRevision(jsonField(object, "revision")),
        success: jsonBool(jsonField(object, "success")),
        exitCode: jsonInteger(jsonField(object, "exitCode")),
        diagnostics: jsonArray(jsonField(object, "diagnostics")).map((item0) => fromJsonDiagnostic(item0)),
        files: jsonArray(jsonField(object, "files")).map((item0) => fromJsonFileImage(item0)),
        messages: jsonArray(jsonField(object, "messages")).map((item0) => fromJsonMessage(item0)),
        output: jsonArray(jsonField(object, "output")).map((item0) => fromJsonCommandOutputChunk(item0)),
        outputs: jsonArray(jsonField(object, "outputs")).map((item0) => fromJsonCommandOutputFile(item0)),
        trace: jsonOptional(object, "trace", (value) => fromJsonTraceSnapshot(value)),
        data: fromJsonCleanPayload(jsonField(object, "data")),
        moduleCount: jsonInteger(jsonField(object, "moduleCount")),
        profileCount: jsonInteger(jsonField(object, "profileCount")),
        targetCount: jsonInteger(jsonField(object, "targetCount")),
    };
}

/** Output produced by one workspace command. */
export type DocOutput = {
    /** Revision used for this operation. */
    readonly revision: Revision;
    /** Whether the operation succeeded. */
    readonly success: boolean;
    /** Exit code for the operation. */
    readonly exitCode: number;
    /** Diagnostics produced by the operation. */
    readonly diagnostics: ReadonlyArray<Diagnostic>;
    /** File images referenced by diagnostics and command data. */
    readonly files: ReadonlyArray<FileImage>;
    /** Messages produced by operation execution. */
    readonly messages: ReadonlyArray<Message>;
    /** Stream output collected during execution. */
    readonly output: ReadonlyArray<CommandOutputChunk>;
    /** Generated output files. */
    readonly outputs: ReadonlyArray<CommandOutputFile>;
    /** Timing trace when requested by the command. */
    readonly trace?: TraceSnapshot;
    /** Operation payload. */
    readonly data: CommandMessagePayload;
    /** Count of modules involved. */
    readonly moduleCount: number;
    /** Count of profiles involved. */
    readonly profileCount: number;
    /** Count of targets involved. */
    readonly targetCount: number;
};

export const DocOutput = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: DocOutput): void {
        encodeDocOutput(writer, value);
    },

    /** Decode one DocOutput. */
    decode(reader: BinaryReader): DocOutput {
        return decodeDocOutput(reader);
    },

    /** Return this value as JSON. */
    toJson(value: DocOutput): Json {
        return toJsonDocOutput(value);
    },

    /** Return one DocOutput from one JSON value. */
    fromJson(value: Json): DocOutput {
        return fromJsonDocOutput(value);
    },
};

/** Encode one DocOutput. */
export function encodeDocOutput(writer: BinaryWriter, value: DocOutput): void {
    encodeRevision(writer, value.revision);
    writer.writeBool(value.success);
    writer.writeSigned(value.exitCode);
    writer.writeUnsigned(value.diagnostics.length);
    for (const item3 of value.diagnostics) {
        encodeDiagnostic(writer, item3);
    }
    writer.writeUnsigned(value.files.length);
    for (const item4 of value.files) {
        encodeFileImage(writer, item4);
    }
    writer.writeUnsigned(value.messages.length);
    for (const item5 of value.messages) {
        encodeMessage(writer, item5);
    }
    writer.writeUnsigned(value.output.length);
    for (const item6 of value.output) {
        encodeCommandOutputChunk(writer, item6);
    }
    writer.writeUnsigned(value.outputs.length);
    for (const item7 of value.outputs) {
        encodeCommandOutputFile(writer, item7);
    }
    writer.writeOption(value.trace, (value8) => {
        encodeTraceSnapshot(writer, value8);
    });
    encodeCommandMessagePayload(writer, value.data);
    writer.writeUnsigned(value.moduleCount);
    writer.writeUnsigned(value.profileCount);
    writer.writeUnsigned(value.targetCount);
}

/** Decode one DocOutput. */
export function decodeDocOutput(reader: BinaryReader): DocOutput {
    const revision = decodeRevision(reader);
    const success = reader.readBool();
    const exitCode = reader.readSignedNumber();
    const diagnostics = (() => { const length3 = reader.readNumber(); const items3: Array<Diagnostic> = []; for (let index = 0; index < length3; index += 1) { items3.push(decodeDiagnostic(reader)); } return items3; })();
    const files = (() => { const length4 = reader.readNumber(); const items4: Array<FileImage> = []; for (let index = 0; index < length4; index += 1) { items4.push(decodeFileImage(reader)); } return items4; })();
    const messages = (() => { const length5 = reader.readNumber(); const items5: Array<Message> = []; for (let index = 0; index < length5; index += 1) { items5.push(decodeMessage(reader)); } return items5; })();
    const output = (() => { const length6 = reader.readNumber(); const items6: Array<CommandOutputChunk> = []; for (let index = 0; index < length6; index += 1) { items6.push(decodeCommandOutputChunk(reader)); } return items6; })();
    const outputs = (() => { const length7 = reader.readNumber(); const items7: Array<CommandOutputFile> = []; for (let index = 0; index < length7; index += 1) { items7.push(decodeCommandOutputFile(reader)); } return items7; })();
    const trace = reader.readOption(() => decodeTraceSnapshot(reader));
    const data = decodeCommandMessagePayload(reader);
    const moduleCount = reader.readNumber();
    const profileCount = reader.readNumber();
    const targetCount = reader.readNumber();

    return {
        revision,
        success,
        exitCode,
        diagnostics,
        files,
        messages,
        output,
        outputs,
        ...(trace === undefined ? {} : { trace }),
        data,
        moduleCount,
        profileCount,
        targetCount,
    };
}

/** Return one JSON value for one DocOutput. */
export function toJsonDocOutput(value: DocOutput): Json {
    return {
        revision: toJsonRevision(value.revision),
        success: value.success,
        exitCode: value.exitCode,
        diagnostics: value.diagnostics.map((item0) => toJsonDiagnostic(item0)),
        files: value.files.map((item0) => toJsonFileImage(item0)),
        messages: value.messages.map((item0) => toJsonMessage(item0)),
        output: value.output.map((item0) => toJsonCommandOutputChunk(item0)),
        outputs: value.outputs.map((item0) => toJsonCommandOutputFile(item0)),
        ...(value.trace === undefined ? {} : { trace: toJsonTraceSnapshot(value.trace) }),
        data: toJsonCommandMessagePayload(value.data),
        moduleCount: value.moduleCount,
        profileCount: value.profileCount,
        targetCount: value.targetCount,
    };
}

/** Return one DocOutput from one JSON value. */
export function fromJsonDocOutput(value: Json): DocOutput {
    const object = jsonObject(value);

    return {
        revision: fromJsonRevision(jsonField(object, "revision")),
        success: jsonBool(jsonField(object, "success")),
        exitCode: jsonInteger(jsonField(object, "exitCode")),
        diagnostics: jsonArray(jsonField(object, "diagnostics")).map((item0) => fromJsonDiagnostic(item0)),
        files: jsonArray(jsonField(object, "files")).map((item0) => fromJsonFileImage(item0)),
        messages: jsonArray(jsonField(object, "messages")).map((item0) => fromJsonMessage(item0)),
        output: jsonArray(jsonField(object, "output")).map((item0) => fromJsonCommandOutputChunk(item0)),
        outputs: jsonArray(jsonField(object, "outputs")).map((item0) => fromJsonCommandOutputFile(item0)),
        trace: jsonOptional(object, "trace", (value) => fromJsonTraceSnapshot(value)),
        data: fromJsonCommandMessagePayload(jsonField(object, "data")),
        moduleCount: jsonInteger(jsonField(object, "moduleCount")),
        profileCount: jsonInteger(jsonField(object, "profileCount")),
        targetCount: jsonInteger(jsonField(object, "targetCount")),
    };
}

/** Output produced by one workspace command. */
export type DoctorOutput = {
    /** Revision used for this operation. */
    readonly revision: Revision;
    /** Whether the operation succeeded. */
    readonly success: boolean;
    /** Exit code for the operation. */
    readonly exitCode: number;
    /** Diagnostics produced by the operation. */
    readonly diagnostics: ReadonlyArray<Diagnostic>;
    /** File images referenced by diagnostics and command data. */
    readonly files: ReadonlyArray<FileImage>;
    /** Messages produced by operation execution. */
    readonly messages: ReadonlyArray<Message>;
    /** Stream output collected during execution. */
    readonly output: ReadonlyArray<CommandOutputChunk>;
    /** Generated output files. */
    readonly outputs: ReadonlyArray<CommandOutputFile>;
    /** Timing trace when requested by the command. */
    readonly trace?: TraceSnapshot;
    /** Operation payload. */
    readonly data: DoctorPayload;
    /** Count of modules involved. */
    readonly moduleCount: number;
    /** Count of profiles involved. */
    readonly profileCount: number;
    /** Count of targets involved. */
    readonly targetCount: number;
};

export const DoctorOutput = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: DoctorOutput): void {
        encodeDoctorOutput(writer, value);
    },

    /** Decode one DoctorOutput. */
    decode(reader: BinaryReader): DoctorOutput {
        return decodeDoctorOutput(reader);
    },

    /** Return this value as JSON. */
    toJson(value: DoctorOutput): Json {
        return toJsonDoctorOutput(value);
    },

    /** Return one DoctorOutput from one JSON value. */
    fromJson(value: Json): DoctorOutput {
        return fromJsonDoctorOutput(value);
    },
};

/** Encode one DoctorOutput. */
export function encodeDoctorOutput(writer: BinaryWriter, value: DoctorOutput): void {
    encodeRevision(writer, value.revision);
    writer.writeBool(value.success);
    writer.writeSigned(value.exitCode);
    writer.writeUnsigned(value.diagnostics.length);
    for (const item3 of value.diagnostics) {
        encodeDiagnostic(writer, item3);
    }
    writer.writeUnsigned(value.files.length);
    for (const item4 of value.files) {
        encodeFileImage(writer, item4);
    }
    writer.writeUnsigned(value.messages.length);
    for (const item5 of value.messages) {
        encodeMessage(writer, item5);
    }
    writer.writeUnsigned(value.output.length);
    for (const item6 of value.output) {
        encodeCommandOutputChunk(writer, item6);
    }
    writer.writeUnsigned(value.outputs.length);
    for (const item7 of value.outputs) {
        encodeCommandOutputFile(writer, item7);
    }
    writer.writeOption(value.trace, (value8) => {
        encodeTraceSnapshot(writer, value8);
    });
    encodeDoctorPayload(writer, value.data);
    writer.writeUnsigned(value.moduleCount);
    writer.writeUnsigned(value.profileCount);
    writer.writeUnsigned(value.targetCount);
}

/** Decode one DoctorOutput. */
export function decodeDoctorOutput(reader: BinaryReader): DoctorOutput {
    const revision = decodeRevision(reader);
    const success = reader.readBool();
    const exitCode = reader.readSignedNumber();
    const diagnostics = (() => { const length3 = reader.readNumber(); const items3: Array<Diagnostic> = []; for (let index = 0; index < length3; index += 1) { items3.push(decodeDiagnostic(reader)); } return items3; })();
    const files = (() => { const length4 = reader.readNumber(); const items4: Array<FileImage> = []; for (let index = 0; index < length4; index += 1) { items4.push(decodeFileImage(reader)); } return items4; })();
    const messages = (() => { const length5 = reader.readNumber(); const items5: Array<Message> = []; for (let index = 0; index < length5; index += 1) { items5.push(decodeMessage(reader)); } return items5; })();
    const output = (() => { const length6 = reader.readNumber(); const items6: Array<CommandOutputChunk> = []; for (let index = 0; index < length6; index += 1) { items6.push(decodeCommandOutputChunk(reader)); } return items6; })();
    const outputs = (() => { const length7 = reader.readNumber(); const items7: Array<CommandOutputFile> = []; for (let index = 0; index < length7; index += 1) { items7.push(decodeCommandOutputFile(reader)); } return items7; })();
    const trace = reader.readOption(() => decodeTraceSnapshot(reader));
    const data = decodeDoctorPayload(reader);
    const moduleCount = reader.readNumber();
    const profileCount = reader.readNumber();
    const targetCount = reader.readNumber();

    return {
        revision,
        success,
        exitCode,
        diagnostics,
        files,
        messages,
        output,
        outputs,
        ...(trace === undefined ? {} : { trace }),
        data,
        moduleCount,
        profileCount,
        targetCount,
    };
}

/** Return one JSON value for one DoctorOutput. */
export function toJsonDoctorOutput(value: DoctorOutput): Json {
    return {
        revision: toJsonRevision(value.revision),
        success: value.success,
        exitCode: value.exitCode,
        diagnostics: value.diagnostics.map((item0) => toJsonDiagnostic(item0)),
        files: value.files.map((item0) => toJsonFileImage(item0)),
        messages: value.messages.map((item0) => toJsonMessage(item0)),
        output: value.output.map((item0) => toJsonCommandOutputChunk(item0)),
        outputs: value.outputs.map((item0) => toJsonCommandOutputFile(item0)),
        ...(value.trace === undefined ? {} : { trace: toJsonTraceSnapshot(value.trace) }),
        data: toJsonDoctorPayload(value.data),
        moduleCount: value.moduleCount,
        profileCount: value.profileCount,
        targetCount: value.targetCount,
    };
}

/** Return one DoctorOutput from one JSON value. */
export function fromJsonDoctorOutput(value: Json): DoctorOutput {
    const object = jsonObject(value);

    return {
        revision: fromJsonRevision(jsonField(object, "revision")),
        success: jsonBool(jsonField(object, "success")),
        exitCode: jsonInteger(jsonField(object, "exitCode")),
        diagnostics: jsonArray(jsonField(object, "diagnostics")).map((item0) => fromJsonDiagnostic(item0)),
        files: jsonArray(jsonField(object, "files")).map((item0) => fromJsonFileImage(item0)),
        messages: jsonArray(jsonField(object, "messages")).map((item0) => fromJsonMessage(item0)),
        output: jsonArray(jsonField(object, "output")).map((item0) => fromJsonCommandOutputChunk(item0)),
        outputs: jsonArray(jsonField(object, "outputs")).map((item0) => fromJsonCommandOutputFile(item0)),
        trace: jsonOptional(object, "trace", (value) => fromJsonTraceSnapshot(value)),
        data: fromJsonDoctorPayload(jsonField(object, "data")),
        moduleCount: jsonInteger(jsonField(object, "moduleCount")),
        profileCount: jsonInteger(jsonField(object, "profileCount")),
        targetCount: jsonInteger(jsonField(object, "targetCount")),
    };
}

/** Output produced by one workspace command. */
export type FormatOutput = {
    /** Revision used for this operation. */
    readonly revision: Revision;
    /** Whether the operation succeeded. */
    readonly success: boolean;
    /** Exit code for the operation. */
    readonly exitCode: number;
    /** Diagnostics produced by the operation. */
    readonly diagnostics: ReadonlyArray<Diagnostic>;
    /** File images referenced by diagnostics and command data. */
    readonly files: ReadonlyArray<FileImage>;
    /** Messages produced by operation execution. */
    readonly messages: ReadonlyArray<Message>;
    /** Stream output collected during execution. */
    readonly output: ReadonlyArray<CommandOutputChunk>;
    /** Generated output files. */
    readonly outputs: ReadonlyArray<CommandOutputFile>;
    /** Timing trace when requested by the command. */
    readonly trace?: TraceSnapshot;
    /** Operation payload. */
    readonly data: FormatPayload;
    /** Count of modules involved. */
    readonly moduleCount: number;
    /** Count of profiles involved. */
    readonly profileCount: number;
    /** Count of targets involved. */
    readonly targetCount: number;
};

export const FormatOutput = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: FormatOutput): void {
        encodeFormatOutput(writer, value);
    },

    /** Decode one FormatOutput. */
    decode(reader: BinaryReader): FormatOutput {
        return decodeFormatOutput(reader);
    },

    /** Return this value as JSON. */
    toJson(value: FormatOutput): Json {
        return toJsonFormatOutput(value);
    },

    /** Return one FormatOutput from one JSON value. */
    fromJson(value: Json): FormatOutput {
        return fromJsonFormatOutput(value);
    },
};

/** Encode one FormatOutput. */
export function encodeFormatOutput(writer: BinaryWriter, value: FormatOutput): void {
    encodeRevision(writer, value.revision);
    writer.writeBool(value.success);
    writer.writeSigned(value.exitCode);
    writer.writeUnsigned(value.diagnostics.length);
    for (const item3 of value.diagnostics) {
        encodeDiagnostic(writer, item3);
    }
    writer.writeUnsigned(value.files.length);
    for (const item4 of value.files) {
        encodeFileImage(writer, item4);
    }
    writer.writeUnsigned(value.messages.length);
    for (const item5 of value.messages) {
        encodeMessage(writer, item5);
    }
    writer.writeUnsigned(value.output.length);
    for (const item6 of value.output) {
        encodeCommandOutputChunk(writer, item6);
    }
    writer.writeUnsigned(value.outputs.length);
    for (const item7 of value.outputs) {
        encodeCommandOutputFile(writer, item7);
    }
    writer.writeOption(value.trace, (value8) => {
        encodeTraceSnapshot(writer, value8);
    });
    encodeFormatPayload(writer, value.data);
    writer.writeUnsigned(value.moduleCount);
    writer.writeUnsigned(value.profileCount);
    writer.writeUnsigned(value.targetCount);
}

/** Decode one FormatOutput. */
export function decodeFormatOutput(reader: BinaryReader): FormatOutput {
    const revision = decodeRevision(reader);
    const success = reader.readBool();
    const exitCode = reader.readSignedNumber();
    const diagnostics = (() => { const length3 = reader.readNumber(); const items3: Array<Diagnostic> = []; for (let index = 0; index < length3; index += 1) { items3.push(decodeDiagnostic(reader)); } return items3; })();
    const files = (() => { const length4 = reader.readNumber(); const items4: Array<FileImage> = []; for (let index = 0; index < length4; index += 1) { items4.push(decodeFileImage(reader)); } return items4; })();
    const messages = (() => { const length5 = reader.readNumber(); const items5: Array<Message> = []; for (let index = 0; index < length5; index += 1) { items5.push(decodeMessage(reader)); } return items5; })();
    const output = (() => { const length6 = reader.readNumber(); const items6: Array<CommandOutputChunk> = []; for (let index = 0; index < length6; index += 1) { items6.push(decodeCommandOutputChunk(reader)); } return items6; })();
    const outputs = (() => { const length7 = reader.readNumber(); const items7: Array<CommandOutputFile> = []; for (let index = 0; index < length7; index += 1) { items7.push(decodeCommandOutputFile(reader)); } return items7; })();
    const trace = reader.readOption(() => decodeTraceSnapshot(reader));
    const data = decodeFormatPayload(reader);
    const moduleCount = reader.readNumber();
    const profileCount = reader.readNumber();
    const targetCount = reader.readNumber();

    return {
        revision,
        success,
        exitCode,
        diagnostics,
        files,
        messages,
        output,
        outputs,
        ...(trace === undefined ? {} : { trace }),
        data,
        moduleCount,
        profileCount,
        targetCount,
    };
}

/** Return one JSON value for one FormatOutput. */
export function toJsonFormatOutput(value: FormatOutput): Json {
    return {
        revision: toJsonRevision(value.revision),
        success: value.success,
        exitCode: value.exitCode,
        diagnostics: value.diagnostics.map((item0) => toJsonDiagnostic(item0)),
        files: value.files.map((item0) => toJsonFileImage(item0)),
        messages: value.messages.map((item0) => toJsonMessage(item0)),
        output: value.output.map((item0) => toJsonCommandOutputChunk(item0)),
        outputs: value.outputs.map((item0) => toJsonCommandOutputFile(item0)),
        ...(value.trace === undefined ? {} : { trace: toJsonTraceSnapshot(value.trace) }),
        data: toJsonFormatPayload(value.data),
        moduleCount: value.moduleCount,
        profileCount: value.profileCount,
        targetCount: value.targetCount,
    };
}

/** Return one FormatOutput from one JSON value. */
export function fromJsonFormatOutput(value: Json): FormatOutput {
    const object = jsonObject(value);

    return {
        revision: fromJsonRevision(jsonField(object, "revision")),
        success: jsonBool(jsonField(object, "success")),
        exitCode: jsonInteger(jsonField(object, "exitCode")),
        diagnostics: jsonArray(jsonField(object, "diagnostics")).map((item0) => fromJsonDiagnostic(item0)),
        files: jsonArray(jsonField(object, "files")).map((item0) => fromJsonFileImage(item0)),
        messages: jsonArray(jsonField(object, "messages")).map((item0) => fromJsonMessage(item0)),
        output: jsonArray(jsonField(object, "output")).map((item0) => fromJsonCommandOutputChunk(item0)),
        outputs: jsonArray(jsonField(object, "outputs")).map((item0) => fromJsonCommandOutputFile(item0)),
        trace: jsonOptional(object, "trace", (value) => fromJsonTraceSnapshot(value)),
        data: fromJsonFormatPayload(jsonField(object, "data")),
        moduleCount: jsonInteger(jsonField(object, "moduleCount")),
        profileCount: jsonInteger(jsonField(object, "profileCount")),
        targetCount: jsonInteger(jsonField(object, "targetCount")),
    };
}

/** Output produced by one workspace command. */
export type InfoOutput = {
    /** Revision used for this operation. */
    readonly revision: Revision;
    /** Whether the operation succeeded. */
    readonly success: boolean;
    /** Exit code for the operation. */
    readonly exitCode: number;
    /** Diagnostics produced by the operation. */
    readonly diagnostics: ReadonlyArray<Diagnostic>;
    /** File images referenced by diagnostics and command data. */
    readonly files: ReadonlyArray<FileImage>;
    /** Messages produced by operation execution. */
    readonly messages: ReadonlyArray<Message>;
    /** Stream output collected during execution. */
    readonly output: ReadonlyArray<CommandOutputChunk>;
    /** Generated output files. */
    readonly outputs: ReadonlyArray<CommandOutputFile>;
    /** Timing trace when requested by the command. */
    readonly trace?: TraceSnapshot;
    /** Operation payload. */
    readonly data: InfoPayload;
    /** Count of modules involved. */
    readonly moduleCount: number;
    /** Count of profiles involved. */
    readonly profileCount: number;
    /** Count of targets involved. */
    readonly targetCount: number;
};

export const InfoOutput = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: InfoOutput): void {
        encodeInfoOutput(writer, value);
    },

    /** Decode one InfoOutput. */
    decode(reader: BinaryReader): InfoOutput {
        return decodeInfoOutput(reader);
    },

    /** Return this value as JSON. */
    toJson(value: InfoOutput): Json {
        return toJsonInfoOutput(value);
    },

    /** Return one InfoOutput from one JSON value. */
    fromJson(value: Json): InfoOutput {
        return fromJsonInfoOutput(value);
    },
};

/** Encode one InfoOutput. */
export function encodeInfoOutput(writer: BinaryWriter, value: InfoOutput): void {
    encodeRevision(writer, value.revision);
    writer.writeBool(value.success);
    writer.writeSigned(value.exitCode);
    writer.writeUnsigned(value.diagnostics.length);
    for (const item3 of value.diagnostics) {
        encodeDiagnostic(writer, item3);
    }
    writer.writeUnsigned(value.files.length);
    for (const item4 of value.files) {
        encodeFileImage(writer, item4);
    }
    writer.writeUnsigned(value.messages.length);
    for (const item5 of value.messages) {
        encodeMessage(writer, item5);
    }
    writer.writeUnsigned(value.output.length);
    for (const item6 of value.output) {
        encodeCommandOutputChunk(writer, item6);
    }
    writer.writeUnsigned(value.outputs.length);
    for (const item7 of value.outputs) {
        encodeCommandOutputFile(writer, item7);
    }
    writer.writeOption(value.trace, (value8) => {
        encodeTraceSnapshot(writer, value8);
    });
    encodeInfoPayload(writer, value.data);
    writer.writeUnsigned(value.moduleCount);
    writer.writeUnsigned(value.profileCount);
    writer.writeUnsigned(value.targetCount);
}

/** Decode one InfoOutput. */
export function decodeInfoOutput(reader: BinaryReader): InfoOutput {
    const revision = decodeRevision(reader);
    const success = reader.readBool();
    const exitCode = reader.readSignedNumber();
    const diagnostics = (() => { const length3 = reader.readNumber(); const items3: Array<Diagnostic> = []; for (let index = 0; index < length3; index += 1) { items3.push(decodeDiagnostic(reader)); } return items3; })();
    const files = (() => { const length4 = reader.readNumber(); const items4: Array<FileImage> = []; for (let index = 0; index < length4; index += 1) { items4.push(decodeFileImage(reader)); } return items4; })();
    const messages = (() => { const length5 = reader.readNumber(); const items5: Array<Message> = []; for (let index = 0; index < length5; index += 1) { items5.push(decodeMessage(reader)); } return items5; })();
    const output = (() => { const length6 = reader.readNumber(); const items6: Array<CommandOutputChunk> = []; for (let index = 0; index < length6; index += 1) { items6.push(decodeCommandOutputChunk(reader)); } return items6; })();
    const outputs = (() => { const length7 = reader.readNumber(); const items7: Array<CommandOutputFile> = []; for (let index = 0; index < length7; index += 1) { items7.push(decodeCommandOutputFile(reader)); } return items7; })();
    const trace = reader.readOption(() => decodeTraceSnapshot(reader));
    const data = decodeInfoPayload(reader);
    const moduleCount = reader.readNumber();
    const profileCount = reader.readNumber();
    const targetCount = reader.readNumber();

    return {
        revision,
        success,
        exitCode,
        diagnostics,
        files,
        messages,
        output,
        outputs,
        ...(trace === undefined ? {} : { trace }),
        data,
        moduleCount,
        profileCount,
        targetCount,
    };
}

/** Return one JSON value for one InfoOutput. */
export function toJsonInfoOutput(value: InfoOutput): Json {
    return {
        revision: toJsonRevision(value.revision),
        success: value.success,
        exitCode: value.exitCode,
        diagnostics: value.diagnostics.map((item0) => toJsonDiagnostic(item0)),
        files: value.files.map((item0) => toJsonFileImage(item0)),
        messages: value.messages.map((item0) => toJsonMessage(item0)),
        output: value.output.map((item0) => toJsonCommandOutputChunk(item0)),
        outputs: value.outputs.map((item0) => toJsonCommandOutputFile(item0)),
        ...(value.trace === undefined ? {} : { trace: toJsonTraceSnapshot(value.trace) }),
        data: toJsonInfoPayload(value.data),
        moduleCount: value.moduleCount,
        profileCount: value.profileCount,
        targetCount: value.targetCount,
    };
}

/** Return one InfoOutput from one JSON value. */
export function fromJsonInfoOutput(value: Json): InfoOutput {
    const object = jsonObject(value);

    return {
        revision: fromJsonRevision(jsonField(object, "revision")),
        success: jsonBool(jsonField(object, "success")),
        exitCode: jsonInteger(jsonField(object, "exitCode")),
        diagnostics: jsonArray(jsonField(object, "diagnostics")).map((item0) => fromJsonDiagnostic(item0)),
        files: jsonArray(jsonField(object, "files")).map((item0) => fromJsonFileImage(item0)),
        messages: jsonArray(jsonField(object, "messages")).map((item0) => fromJsonMessage(item0)),
        output: jsonArray(jsonField(object, "output")).map((item0) => fromJsonCommandOutputChunk(item0)),
        outputs: jsonArray(jsonField(object, "outputs")).map((item0) => fromJsonCommandOutputFile(item0)),
        trace: jsonOptional(object, "trace", (value) => fromJsonTraceSnapshot(value)),
        data: fromJsonInfoPayload(jsonField(object, "data")),
        moduleCount: jsonInteger(jsonField(object, "moduleCount")),
        profileCount: jsonInteger(jsonField(object, "profileCount")),
        targetCount: jsonInteger(jsonField(object, "targetCount")),
    };
}

/** Output produced by one workspace command. */
export type QueryOutput = {
    /** Revision used for this operation. */
    readonly revision: Revision;
    /** Whether the operation succeeded. */
    readonly success: boolean;
    /** Exit code for the operation. */
    readonly exitCode: number;
    /** Diagnostics produced by the operation. */
    readonly diagnostics: ReadonlyArray<Diagnostic>;
    /** File images referenced by diagnostics and command data. */
    readonly files: ReadonlyArray<FileImage>;
    /** Messages produced by operation execution. */
    readonly messages: ReadonlyArray<Message>;
    /** Stream output collected during execution. */
    readonly output: ReadonlyArray<CommandOutputChunk>;
    /** Generated output files. */
    readonly outputs: ReadonlyArray<CommandOutputFile>;
    /** Timing trace when requested by the command. */
    readonly trace?: TraceSnapshot;
    /** Operation payload. */
    readonly data: QueryPayload;
    /** Count of modules involved. */
    readonly moduleCount: number;
    /** Count of profiles involved. */
    readonly profileCount: number;
    /** Count of targets involved. */
    readonly targetCount: number;
};

export const QueryOutput = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: QueryOutput): void {
        encodeQueryOutput(writer, value);
    },

    /** Decode one QueryOutput. */
    decode(reader: BinaryReader): QueryOutput {
        return decodeQueryOutput(reader);
    },

    /** Return this value as JSON. */
    toJson(value: QueryOutput): Json {
        return toJsonQueryOutput(value);
    },

    /** Return one QueryOutput from one JSON value. */
    fromJson(value: Json): QueryOutput {
        return fromJsonQueryOutput(value);
    },
};

/** Encode one QueryOutput. */
export function encodeQueryOutput(writer: BinaryWriter, value: QueryOutput): void {
    encodeRevision(writer, value.revision);
    writer.writeBool(value.success);
    writer.writeSigned(value.exitCode);
    writer.writeUnsigned(value.diagnostics.length);
    for (const item3 of value.diagnostics) {
        encodeDiagnostic(writer, item3);
    }
    writer.writeUnsigned(value.files.length);
    for (const item4 of value.files) {
        encodeFileImage(writer, item4);
    }
    writer.writeUnsigned(value.messages.length);
    for (const item5 of value.messages) {
        encodeMessage(writer, item5);
    }
    writer.writeUnsigned(value.output.length);
    for (const item6 of value.output) {
        encodeCommandOutputChunk(writer, item6);
    }
    writer.writeUnsigned(value.outputs.length);
    for (const item7 of value.outputs) {
        encodeCommandOutputFile(writer, item7);
    }
    writer.writeOption(value.trace, (value8) => {
        encodeTraceSnapshot(writer, value8);
    });
    encodeQueryPayload(writer, value.data);
    writer.writeUnsigned(value.moduleCount);
    writer.writeUnsigned(value.profileCount);
    writer.writeUnsigned(value.targetCount);
}

/** Decode one QueryOutput. */
export function decodeQueryOutput(reader: BinaryReader): QueryOutput {
    const revision = decodeRevision(reader);
    const success = reader.readBool();
    const exitCode = reader.readSignedNumber();
    const diagnostics = (() => { const length3 = reader.readNumber(); const items3: Array<Diagnostic> = []; for (let index = 0; index < length3; index += 1) { items3.push(decodeDiagnostic(reader)); } return items3; })();
    const files = (() => { const length4 = reader.readNumber(); const items4: Array<FileImage> = []; for (let index = 0; index < length4; index += 1) { items4.push(decodeFileImage(reader)); } return items4; })();
    const messages = (() => { const length5 = reader.readNumber(); const items5: Array<Message> = []; for (let index = 0; index < length5; index += 1) { items5.push(decodeMessage(reader)); } return items5; })();
    const output = (() => { const length6 = reader.readNumber(); const items6: Array<CommandOutputChunk> = []; for (let index = 0; index < length6; index += 1) { items6.push(decodeCommandOutputChunk(reader)); } return items6; })();
    const outputs = (() => { const length7 = reader.readNumber(); const items7: Array<CommandOutputFile> = []; for (let index = 0; index < length7; index += 1) { items7.push(decodeCommandOutputFile(reader)); } return items7; })();
    const trace = reader.readOption(() => decodeTraceSnapshot(reader));
    const data = decodeQueryPayload(reader);
    const moduleCount = reader.readNumber();
    const profileCount = reader.readNumber();
    const targetCount = reader.readNumber();

    return {
        revision,
        success,
        exitCode,
        diagnostics,
        files,
        messages,
        output,
        outputs,
        ...(trace === undefined ? {} : { trace }),
        data,
        moduleCount,
        profileCount,
        targetCount,
    };
}

/** Return one JSON value for one QueryOutput. */
export function toJsonQueryOutput(value: QueryOutput): Json {
    return {
        revision: toJsonRevision(value.revision),
        success: value.success,
        exitCode: value.exitCode,
        diagnostics: value.diagnostics.map((item0) => toJsonDiagnostic(item0)),
        files: value.files.map((item0) => toJsonFileImage(item0)),
        messages: value.messages.map((item0) => toJsonMessage(item0)),
        output: value.output.map((item0) => toJsonCommandOutputChunk(item0)),
        outputs: value.outputs.map((item0) => toJsonCommandOutputFile(item0)),
        ...(value.trace === undefined ? {} : { trace: toJsonTraceSnapshot(value.trace) }),
        data: toJsonQueryPayload(value.data),
        moduleCount: value.moduleCount,
        profileCount: value.profileCount,
        targetCount: value.targetCount,
    };
}

/** Return one QueryOutput from one JSON value. */
export function fromJsonQueryOutput(value: Json): QueryOutput {
    const object = jsonObject(value);

    return {
        revision: fromJsonRevision(jsonField(object, "revision")),
        success: jsonBool(jsonField(object, "success")),
        exitCode: jsonInteger(jsonField(object, "exitCode")),
        diagnostics: jsonArray(jsonField(object, "diagnostics")).map((item0) => fromJsonDiagnostic(item0)),
        files: jsonArray(jsonField(object, "files")).map((item0) => fromJsonFileImage(item0)),
        messages: jsonArray(jsonField(object, "messages")).map((item0) => fromJsonMessage(item0)),
        output: jsonArray(jsonField(object, "output")).map((item0) => fromJsonCommandOutputChunk(item0)),
        outputs: jsonArray(jsonField(object, "outputs")).map((item0) => fromJsonCommandOutputFile(item0)),
        trace: jsonOptional(object, "trace", (value) => fromJsonTraceSnapshot(value)),
        data: fromJsonQueryPayload(jsonField(object, "data")),
        moduleCount: jsonInteger(jsonField(object, "moduleCount")),
        profileCount: jsonInteger(jsonField(object, "profileCount")),
        targetCount: jsonInteger(jsonField(object, "targetCount")),
    };
}

/** Output produced by one workspace command. */
export type RewriteOutput = {
    /** Revision used for this operation. */
    readonly revision: Revision;
    /** Whether the operation succeeded. */
    readonly success: boolean;
    /** Exit code for the operation. */
    readonly exitCode: number;
    /** Diagnostics produced by the operation. */
    readonly diagnostics: ReadonlyArray<Diagnostic>;
    /** File images referenced by diagnostics and command data. */
    readonly files: ReadonlyArray<FileImage>;
    /** Messages produced by operation execution. */
    readonly messages: ReadonlyArray<Message>;
    /** Stream output collected during execution. */
    readonly output: ReadonlyArray<CommandOutputChunk>;
    /** Generated output files. */
    readonly outputs: ReadonlyArray<CommandOutputFile>;
    /** Timing trace when requested by the command. */
    readonly trace?: TraceSnapshot;
    /** Operation payload. */
    readonly data: RewritePayload;
    /** Count of modules involved. */
    readonly moduleCount: number;
    /** Count of profiles involved. */
    readonly profileCount: number;
    /** Count of targets involved. */
    readonly targetCount: number;
};

export const RewriteOutput = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: RewriteOutput): void {
        encodeRewriteOutput(writer, value);
    },

    /** Decode one RewriteOutput. */
    decode(reader: BinaryReader): RewriteOutput {
        return decodeRewriteOutput(reader);
    },

    /** Return this value as JSON. */
    toJson(value: RewriteOutput): Json {
        return toJsonRewriteOutput(value);
    },

    /** Return one RewriteOutput from one JSON value. */
    fromJson(value: Json): RewriteOutput {
        return fromJsonRewriteOutput(value);
    },
};

/** Encode one RewriteOutput. */
export function encodeRewriteOutput(writer: BinaryWriter, value: RewriteOutput): void {
    encodeRevision(writer, value.revision);
    writer.writeBool(value.success);
    writer.writeSigned(value.exitCode);
    writer.writeUnsigned(value.diagnostics.length);
    for (const item3 of value.diagnostics) {
        encodeDiagnostic(writer, item3);
    }
    writer.writeUnsigned(value.files.length);
    for (const item4 of value.files) {
        encodeFileImage(writer, item4);
    }
    writer.writeUnsigned(value.messages.length);
    for (const item5 of value.messages) {
        encodeMessage(writer, item5);
    }
    writer.writeUnsigned(value.output.length);
    for (const item6 of value.output) {
        encodeCommandOutputChunk(writer, item6);
    }
    writer.writeUnsigned(value.outputs.length);
    for (const item7 of value.outputs) {
        encodeCommandOutputFile(writer, item7);
    }
    writer.writeOption(value.trace, (value8) => {
        encodeTraceSnapshot(writer, value8);
    });
    encodeRewritePayload(writer, value.data);
    writer.writeUnsigned(value.moduleCount);
    writer.writeUnsigned(value.profileCount);
    writer.writeUnsigned(value.targetCount);
}

/** Decode one RewriteOutput. */
export function decodeRewriteOutput(reader: BinaryReader): RewriteOutput {
    const revision = decodeRevision(reader);
    const success = reader.readBool();
    const exitCode = reader.readSignedNumber();
    const diagnostics = (() => { const length3 = reader.readNumber(); const items3: Array<Diagnostic> = []; for (let index = 0; index < length3; index += 1) { items3.push(decodeDiagnostic(reader)); } return items3; })();
    const files = (() => { const length4 = reader.readNumber(); const items4: Array<FileImage> = []; for (let index = 0; index < length4; index += 1) { items4.push(decodeFileImage(reader)); } return items4; })();
    const messages = (() => { const length5 = reader.readNumber(); const items5: Array<Message> = []; for (let index = 0; index < length5; index += 1) { items5.push(decodeMessage(reader)); } return items5; })();
    const output = (() => { const length6 = reader.readNumber(); const items6: Array<CommandOutputChunk> = []; for (let index = 0; index < length6; index += 1) { items6.push(decodeCommandOutputChunk(reader)); } return items6; })();
    const outputs = (() => { const length7 = reader.readNumber(); const items7: Array<CommandOutputFile> = []; for (let index = 0; index < length7; index += 1) { items7.push(decodeCommandOutputFile(reader)); } return items7; })();
    const trace = reader.readOption(() => decodeTraceSnapshot(reader));
    const data = decodeRewritePayload(reader);
    const moduleCount = reader.readNumber();
    const profileCount = reader.readNumber();
    const targetCount = reader.readNumber();

    return {
        revision,
        success,
        exitCode,
        diagnostics,
        files,
        messages,
        output,
        outputs,
        ...(trace === undefined ? {} : { trace }),
        data,
        moduleCount,
        profileCount,
        targetCount,
    };
}

/** Return one JSON value for one RewriteOutput. */
export function toJsonRewriteOutput(value: RewriteOutput): Json {
    return {
        revision: toJsonRevision(value.revision),
        success: value.success,
        exitCode: value.exitCode,
        diagnostics: value.diagnostics.map((item0) => toJsonDiagnostic(item0)),
        files: value.files.map((item0) => toJsonFileImage(item0)),
        messages: value.messages.map((item0) => toJsonMessage(item0)),
        output: value.output.map((item0) => toJsonCommandOutputChunk(item0)),
        outputs: value.outputs.map((item0) => toJsonCommandOutputFile(item0)),
        ...(value.trace === undefined ? {} : { trace: toJsonTraceSnapshot(value.trace) }),
        data: toJsonRewritePayload(value.data),
        moduleCount: value.moduleCount,
        profileCount: value.profileCount,
        targetCount: value.targetCount,
    };
}

/** Return one RewriteOutput from one JSON value. */
export function fromJsonRewriteOutput(value: Json): RewriteOutput {
    const object = jsonObject(value);

    return {
        revision: fromJsonRevision(jsonField(object, "revision")),
        success: jsonBool(jsonField(object, "success")),
        exitCode: jsonInteger(jsonField(object, "exitCode")),
        diagnostics: jsonArray(jsonField(object, "diagnostics")).map((item0) => fromJsonDiagnostic(item0)),
        files: jsonArray(jsonField(object, "files")).map((item0) => fromJsonFileImage(item0)),
        messages: jsonArray(jsonField(object, "messages")).map((item0) => fromJsonMessage(item0)),
        output: jsonArray(jsonField(object, "output")).map((item0) => fromJsonCommandOutputChunk(item0)),
        outputs: jsonArray(jsonField(object, "outputs")).map((item0) => fromJsonCommandOutputFile(item0)),
        trace: jsonOptional(object, "trace", (value) => fromJsonTraceSnapshot(value)),
        data: fromJsonRewritePayload(jsonField(object, "data")),
        moduleCount: jsonInteger(jsonField(object, "moduleCount")),
        profileCount: jsonInteger(jsonField(object, "profileCount")),
        targetCount: jsonInteger(jsonField(object, "targetCount")),
    };
}

/** Output produced by one workspace command. */
export type SettingsOutput = {
    /** Revision used for this operation. */
    readonly revision: Revision;
    /** Whether the operation succeeded. */
    readonly success: boolean;
    /** Exit code for the operation. */
    readonly exitCode: number;
    /** Diagnostics produced by the operation. */
    readonly diagnostics: ReadonlyArray<Diagnostic>;
    /** File images referenced by diagnostics and command data. */
    readonly files: ReadonlyArray<FileImage>;
    /** Messages produced by operation execution. */
    readonly messages: ReadonlyArray<Message>;
    /** Stream output collected during execution. */
    readonly output: ReadonlyArray<CommandOutputChunk>;
    /** Generated output files. */
    readonly outputs: ReadonlyArray<CommandOutputFile>;
    /** Timing trace when requested by the command. */
    readonly trace?: TraceSnapshot;
    /** Operation payload. */
    readonly data: SettingsPayload;
    /** Count of modules involved. */
    readonly moduleCount: number;
    /** Count of profiles involved. */
    readonly profileCount: number;
    /** Count of targets involved. */
    readonly targetCount: number;
};

export const SettingsOutput = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: SettingsOutput): void {
        encodeSettingsOutput(writer, value);
    },

    /** Decode one SettingsOutput. */
    decode(reader: BinaryReader): SettingsOutput {
        return decodeSettingsOutput(reader);
    },

    /** Return this value as JSON. */
    toJson(value: SettingsOutput): Json {
        return toJsonSettingsOutput(value);
    },

    /** Return one SettingsOutput from one JSON value. */
    fromJson(value: Json): SettingsOutput {
        return fromJsonSettingsOutput(value);
    },
};

/** Encode one SettingsOutput. */
export function encodeSettingsOutput(writer: BinaryWriter, value: SettingsOutput): void {
    encodeRevision(writer, value.revision);
    writer.writeBool(value.success);
    writer.writeSigned(value.exitCode);
    writer.writeUnsigned(value.diagnostics.length);
    for (const item3 of value.diagnostics) {
        encodeDiagnostic(writer, item3);
    }
    writer.writeUnsigned(value.files.length);
    for (const item4 of value.files) {
        encodeFileImage(writer, item4);
    }
    writer.writeUnsigned(value.messages.length);
    for (const item5 of value.messages) {
        encodeMessage(writer, item5);
    }
    writer.writeUnsigned(value.output.length);
    for (const item6 of value.output) {
        encodeCommandOutputChunk(writer, item6);
    }
    writer.writeUnsigned(value.outputs.length);
    for (const item7 of value.outputs) {
        encodeCommandOutputFile(writer, item7);
    }
    writer.writeOption(value.trace, (value8) => {
        encodeTraceSnapshot(writer, value8);
    });
    encodeSettingsPayload(writer, value.data);
    writer.writeUnsigned(value.moduleCount);
    writer.writeUnsigned(value.profileCount);
    writer.writeUnsigned(value.targetCount);
}

/** Decode one SettingsOutput. */
export function decodeSettingsOutput(reader: BinaryReader): SettingsOutput {
    const revision = decodeRevision(reader);
    const success = reader.readBool();
    const exitCode = reader.readSignedNumber();
    const diagnostics = (() => { const length3 = reader.readNumber(); const items3: Array<Diagnostic> = []; for (let index = 0; index < length3; index += 1) { items3.push(decodeDiagnostic(reader)); } return items3; })();
    const files = (() => { const length4 = reader.readNumber(); const items4: Array<FileImage> = []; for (let index = 0; index < length4; index += 1) { items4.push(decodeFileImage(reader)); } return items4; })();
    const messages = (() => { const length5 = reader.readNumber(); const items5: Array<Message> = []; for (let index = 0; index < length5; index += 1) { items5.push(decodeMessage(reader)); } return items5; })();
    const output = (() => { const length6 = reader.readNumber(); const items6: Array<CommandOutputChunk> = []; for (let index = 0; index < length6; index += 1) { items6.push(decodeCommandOutputChunk(reader)); } return items6; })();
    const outputs = (() => { const length7 = reader.readNumber(); const items7: Array<CommandOutputFile> = []; for (let index = 0; index < length7; index += 1) { items7.push(decodeCommandOutputFile(reader)); } return items7; })();
    const trace = reader.readOption(() => decodeTraceSnapshot(reader));
    const data = decodeSettingsPayload(reader);
    const moduleCount = reader.readNumber();
    const profileCount = reader.readNumber();
    const targetCount = reader.readNumber();

    return {
        revision,
        success,
        exitCode,
        diagnostics,
        files,
        messages,
        output,
        outputs,
        ...(trace === undefined ? {} : { trace }),
        data,
        moduleCount,
        profileCount,
        targetCount,
    };
}

/** Return one JSON value for one SettingsOutput. */
export function toJsonSettingsOutput(value: SettingsOutput): Json {
    return {
        revision: toJsonRevision(value.revision),
        success: value.success,
        exitCode: value.exitCode,
        diagnostics: value.diagnostics.map((item0) => toJsonDiagnostic(item0)),
        files: value.files.map((item0) => toJsonFileImage(item0)),
        messages: value.messages.map((item0) => toJsonMessage(item0)),
        output: value.output.map((item0) => toJsonCommandOutputChunk(item0)),
        outputs: value.outputs.map((item0) => toJsonCommandOutputFile(item0)),
        ...(value.trace === undefined ? {} : { trace: toJsonTraceSnapshot(value.trace) }),
        data: toJsonSettingsPayload(value.data),
        moduleCount: value.moduleCount,
        profileCount: value.profileCount,
        targetCount: value.targetCount,
    };
}

/** Return one SettingsOutput from one JSON value. */
export function fromJsonSettingsOutput(value: Json): SettingsOutput {
    const object = jsonObject(value);

    return {
        revision: fromJsonRevision(jsonField(object, "revision")),
        success: jsonBool(jsonField(object, "success")),
        exitCode: jsonInteger(jsonField(object, "exitCode")),
        diagnostics: jsonArray(jsonField(object, "diagnostics")).map((item0) => fromJsonDiagnostic(item0)),
        files: jsonArray(jsonField(object, "files")).map((item0) => fromJsonFileImage(item0)),
        messages: jsonArray(jsonField(object, "messages")).map((item0) => fromJsonMessage(item0)),
        output: jsonArray(jsonField(object, "output")).map((item0) => fromJsonCommandOutputChunk(item0)),
        outputs: jsonArray(jsonField(object, "outputs")).map((item0) => fromJsonCommandOutputFile(item0)),
        trace: jsonOptional(object, "trace", (value) => fromJsonTraceSnapshot(value)),
        data: fromJsonSettingsPayload(jsonField(object, "data")),
        moduleCount: jsonInteger(jsonField(object, "moduleCount")),
        profileCount: jsonInteger(jsonField(object, "profileCount")),
        targetCount: jsonInteger(jsonField(object, "targetCount")),
    };
}

/** Output produced by one workspace command. */
export type TargetsOutput = {
    /** Revision used for this operation. */
    readonly revision: Revision;
    /** Whether the operation succeeded. */
    readonly success: boolean;
    /** Exit code for the operation. */
    readonly exitCode: number;
    /** Diagnostics produced by the operation. */
    readonly diagnostics: ReadonlyArray<Diagnostic>;
    /** File images referenced by diagnostics and command data. */
    readonly files: ReadonlyArray<FileImage>;
    /** Messages produced by operation execution. */
    readonly messages: ReadonlyArray<Message>;
    /** Stream output collected during execution. */
    readonly output: ReadonlyArray<CommandOutputChunk>;
    /** Generated output files. */
    readonly outputs: ReadonlyArray<CommandOutputFile>;
    /** Timing trace when requested by the command. */
    readonly trace?: TraceSnapshot;
    /** Operation payload. */
    readonly data: TargetsPayload;
    /** Count of modules involved. */
    readonly moduleCount: number;
    /** Count of profiles involved. */
    readonly profileCount: number;
    /** Count of targets involved. */
    readonly targetCount: number;
};

export const TargetsOutput = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: TargetsOutput): void {
        encodeTargetsOutput(writer, value);
    },

    /** Decode one TargetsOutput. */
    decode(reader: BinaryReader): TargetsOutput {
        return decodeTargetsOutput(reader);
    },

    /** Return this value as JSON. */
    toJson(value: TargetsOutput): Json {
        return toJsonTargetsOutput(value);
    },

    /** Return one TargetsOutput from one JSON value. */
    fromJson(value: Json): TargetsOutput {
        return fromJsonTargetsOutput(value);
    },
};

/** Encode one TargetsOutput. */
export function encodeTargetsOutput(writer: BinaryWriter, value: TargetsOutput): void {
    encodeRevision(writer, value.revision);
    writer.writeBool(value.success);
    writer.writeSigned(value.exitCode);
    writer.writeUnsigned(value.diagnostics.length);
    for (const item3 of value.diagnostics) {
        encodeDiagnostic(writer, item3);
    }
    writer.writeUnsigned(value.files.length);
    for (const item4 of value.files) {
        encodeFileImage(writer, item4);
    }
    writer.writeUnsigned(value.messages.length);
    for (const item5 of value.messages) {
        encodeMessage(writer, item5);
    }
    writer.writeUnsigned(value.output.length);
    for (const item6 of value.output) {
        encodeCommandOutputChunk(writer, item6);
    }
    writer.writeUnsigned(value.outputs.length);
    for (const item7 of value.outputs) {
        encodeCommandOutputFile(writer, item7);
    }
    writer.writeOption(value.trace, (value8) => {
        encodeTraceSnapshot(writer, value8);
    });
    encodeTargetsPayload(writer, value.data);
    writer.writeUnsigned(value.moduleCount);
    writer.writeUnsigned(value.profileCount);
    writer.writeUnsigned(value.targetCount);
}

/** Decode one TargetsOutput. */
export function decodeTargetsOutput(reader: BinaryReader): TargetsOutput {
    const revision = decodeRevision(reader);
    const success = reader.readBool();
    const exitCode = reader.readSignedNumber();
    const diagnostics = (() => { const length3 = reader.readNumber(); const items3: Array<Diagnostic> = []; for (let index = 0; index < length3; index += 1) { items3.push(decodeDiagnostic(reader)); } return items3; })();
    const files = (() => { const length4 = reader.readNumber(); const items4: Array<FileImage> = []; for (let index = 0; index < length4; index += 1) { items4.push(decodeFileImage(reader)); } return items4; })();
    const messages = (() => { const length5 = reader.readNumber(); const items5: Array<Message> = []; for (let index = 0; index < length5; index += 1) { items5.push(decodeMessage(reader)); } return items5; })();
    const output = (() => { const length6 = reader.readNumber(); const items6: Array<CommandOutputChunk> = []; for (let index = 0; index < length6; index += 1) { items6.push(decodeCommandOutputChunk(reader)); } return items6; })();
    const outputs = (() => { const length7 = reader.readNumber(); const items7: Array<CommandOutputFile> = []; for (let index = 0; index < length7; index += 1) { items7.push(decodeCommandOutputFile(reader)); } return items7; })();
    const trace = reader.readOption(() => decodeTraceSnapshot(reader));
    const data = decodeTargetsPayload(reader);
    const moduleCount = reader.readNumber();
    const profileCount = reader.readNumber();
    const targetCount = reader.readNumber();

    return {
        revision,
        success,
        exitCode,
        diagnostics,
        files,
        messages,
        output,
        outputs,
        ...(trace === undefined ? {} : { trace }),
        data,
        moduleCount,
        profileCount,
        targetCount,
    };
}

/** Return one JSON value for one TargetsOutput. */
export function toJsonTargetsOutput(value: TargetsOutput): Json {
    return {
        revision: toJsonRevision(value.revision),
        success: value.success,
        exitCode: value.exitCode,
        diagnostics: value.diagnostics.map((item0) => toJsonDiagnostic(item0)),
        files: value.files.map((item0) => toJsonFileImage(item0)),
        messages: value.messages.map((item0) => toJsonMessage(item0)),
        output: value.output.map((item0) => toJsonCommandOutputChunk(item0)),
        outputs: value.outputs.map((item0) => toJsonCommandOutputFile(item0)),
        ...(value.trace === undefined ? {} : { trace: toJsonTraceSnapshot(value.trace) }),
        data: toJsonTargetsPayload(value.data),
        moduleCount: value.moduleCount,
        profileCount: value.profileCount,
        targetCount: value.targetCount,
    };
}

/** Return one TargetsOutput from one JSON value. */
export function fromJsonTargetsOutput(value: Json): TargetsOutput {
    const object = jsonObject(value);

    return {
        revision: fromJsonRevision(jsonField(object, "revision")),
        success: jsonBool(jsonField(object, "success")),
        exitCode: jsonInteger(jsonField(object, "exitCode")),
        diagnostics: jsonArray(jsonField(object, "diagnostics")).map((item0) => fromJsonDiagnostic(item0)),
        files: jsonArray(jsonField(object, "files")).map((item0) => fromJsonFileImage(item0)),
        messages: jsonArray(jsonField(object, "messages")).map((item0) => fromJsonMessage(item0)),
        output: jsonArray(jsonField(object, "output")).map((item0) => fromJsonCommandOutputChunk(item0)),
        outputs: jsonArray(jsonField(object, "outputs")).map((item0) => fromJsonCommandOutputFile(item0)),
        trace: jsonOptional(object, "trace", (value) => fromJsonTraceSnapshot(value)),
        data: fromJsonTargetsPayload(jsonField(object, "data")),
        moduleCount: jsonInteger(jsonField(object, "moduleCount")),
        profileCount: jsonInteger(jsonField(object, "profileCount")),
        targetCount: jsonInteger(jsonField(object, "targetCount")),
    };
}

/** Output produced by one workspace command. */
export type TaskOutput = {
    /** Revision used for this operation. */
    readonly revision: Revision;
    /** Whether the operation succeeded. */
    readonly success: boolean;
    /** Exit code for the operation. */
    readonly exitCode: number;
    /** Diagnostics produced by the operation. */
    readonly diagnostics: ReadonlyArray<Diagnostic>;
    /** File images referenced by diagnostics and command data. */
    readonly files: ReadonlyArray<FileImage>;
    /** Messages produced by operation execution. */
    readonly messages: ReadonlyArray<Message>;
    /** Stream output collected during execution. */
    readonly output: ReadonlyArray<CommandOutputChunk>;
    /** Generated output files. */
    readonly outputs: ReadonlyArray<CommandOutputFile>;
    /** Timing trace when requested by the command. */
    readonly trace?: TraceSnapshot;
    /** Operation payload. */
    readonly data: TaskPayload;
    /** Count of modules involved. */
    readonly moduleCount: number;
    /** Count of profiles involved. */
    readonly profileCount: number;
    /** Count of targets involved. */
    readonly targetCount: number;
};

export const TaskOutput = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: TaskOutput): void {
        encodeTaskOutput(writer, value);
    },

    /** Decode one TaskOutput. */
    decode(reader: BinaryReader): TaskOutput {
        return decodeTaskOutput(reader);
    },

    /** Return this value as JSON. */
    toJson(value: TaskOutput): Json {
        return toJsonTaskOutput(value);
    },

    /** Return one TaskOutput from one JSON value. */
    fromJson(value: Json): TaskOutput {
        return fromJsonTaskOutput(value);
    },
};

/** Encode one TaskOutput. */
export function encodeTaskOutput(writer: BinaryWriter, value: TaskOutput): void {
    encodeRevision(writer, value.revision);
    writer.writeBool(value.success);
    writer.writeSigned(value.exitCode);
    writer.writeUnsigned(value.diagnostics.length);
    for (const item3 of value.diagnostics) {
        encodeDiagnostic(writer, item3);
    }
    writer.writeUnsigned(value.files.length);
    for (const item4 of value.files) {
        encodeFileImage(writer, item4);
    }
    writer.writeUnsigned(value.messages.length);
    for (const item5 of value.messages) {
        encodeMessage(writer, item5);
    }
    writer.writeUnsigned(value.output.length);
    for (const item6 of value.output) {
        encodeCommandOutputChunk(writer, item6);
    }
    writer.writeUnsigned(value.outputs.length);
    for (const item7 of value.outputs) {
        encodeCommandOutputFile(writer, item7);
    }
    writer.writeOption(value.trace, (value8) => {
        encodeTraceSnapshot(writer, value8);
    });
    encodeTaskPayload(writer, value.data);
    writer.writeUnsigned(value.moduleCount);
    writer.writeUnsigned(value.profileCount);
    writer.writeUnsigned(value.targetCount);
}

/** Decode one TaskOutput. */
export function decodeTaskOutput(reader: BinaryReader): TaskOutput {
    const revision = decodeRevision(reader);
    const success = reader.readBool();
    const exitCode = reader.readSignedNumber();
    const diagnostics = (() => { const length3 = reader.readNumber(); const items3: Array<Diagnostic> = []; for (let index = 0; index < length3; index += 1) { items3.push(decodeDiagnostic(reader)); } return items3; })();
    const files = (() => { const length4 = reader.readNumber(); const items4: Array<FileImage> = []; for (let index = 0; index < length4; index += 1) { items4.push(decodeFileImage(reader)); } return items4; })();
    const messages = (() => { const length5 = reader.readNumber(); const items5: Array<Message> = []; for (let index = 0; index < length5; index += 1) { items5.push(decodeMessage(reader)); } return items5; })();
    const output = (() => { const length6 = reader.readNumber(); const items6: Array<CommandOutputChunk> = []; for (let index = 0; index < length6; index += 1) { items6.push(decodeCommandOutputChunk(reader)); } return items6; })();
    const outputs = (() => { const length7 = reader.readNumber(); const items7: Array<CommandOutputFile> = []; for (let index = 0; index < length7; index += 1) { items7.push(decodeCommandOutputFile(reader)); } return items7; })();
    const trace = reader.readOption(() => decodeTraceSnapshot(reader));
    const data = decodeTaskPayload(reader);
    const moduleCount = reader.readNumber();
    const profileCount = reader.readNumber();
    const targetCount = reader.readNumber();

    return {
        revision,
        success,
        exitCode,
        diagnostics,
        files,
        messages,
        output,
        outputs,
        ...(trace === undefined ? {} : { trace }),
        data,
        moduleCount,
        profileCount,
        targetCount,
    };
}

/** Return one JSON value for one TaskOutput. */
export function toJsonTaskOutput(value: TaskOutput): Json {
    return {
        revision: toJsonRevision(value.revision),
        success: value.success,
        exitCode: value.exitCode,
        diagnostics: value.diagnostics.map((item0) => toJsonDiagnostic(item0)),
        files: value.files.map((item0) => toJsonFileImage(item0)),
        messages: value.messages.map((item0) => toJsonMessage(item0)),
        output: value.output.map((item0) => toJsonCommandOutputChunk(item0)),
        outputs: value.outputs.map((item0) => toJsonCommandOutputFile(item0)),
        ...(value.trace === undefined ? {} : { trace: toJsonTraceSnapshot(value.trace) }),
        data: toJsonTaskPayload(value.data),
        moduleCount: value.moduleCount,
        profileCount: value.profileCount,
        targetCount: value.targetCount,
    };
}

/** Return one TaskOutput from one JSON value. */
export function fromJsonTaskOutput(value: Json): TaskOutput {
    const object = jsonObject(value);

    return {
        revision: fromJsonRevision(jsonField(object, "revision")),
        success: jsonBool(jsonField(object, "success")),
        exitCode: jsonInteger(jsonField(object, "exitCode")),
        diagnostics: jsonArray(jsonField(object, "diagnostics")).map((item0) => fromJsonDiagnostic(item0)),
        files: jsonArray(jsonField(object, "files")).map((item0) => fromJsonFileImage(item0)),
        messages: jsonArray(jsonField(object, "messages")).map((item0) => fromJsonMessage(item0)),
        output: jsonArray(jsonField(object, "output")).map((item0) => fromJsonCommandOutputChunk(item0)),
        outputs: jsonArray(jsonField(object, "outputs")).map((item0) => fromJsonCommandOutputFile(item0)),
        trace: jsonOptional(object, "trace", (value) => fromJsonTraceSnapshot(value)),
        data: fromJsonTaskPayload(jsonField(object, "data")),
        moduleCount: jsonInteger(jsonField(object, "moduleCount")),
        profileCount: jsonInteger(jsonField(object, "profileCount")),
        targetCount: jsonInteger(jsonField(object, "targetCount")),
    };
}

/** Output produced by one workspace command. */
export type TestOutput = {
    /** Revision used for this operation. */
    readonly revision: Revision;
    /** Whether the operation succeeded. */
    readonly success: boolean;
    /** Exit code for the operation. */
    readonly exitCode: number;
    /** Diagnostics produced by the operation. */
    readonly diagnostics: ReadonlyArray<Diagnostic>;
    /** File images referenced by diagnostics and command data. */
    readonly files: ReadonlyArray<FileImage>;
    /** Messages produced by operation execution. */
    readonly messages: ReadonlyArray<Message>;
    /** Stream output collected during execution. */
    readonly output: ReadonlyArray<CommandOutputChunk>;
    /** Generated output files. */
    readonly outputs: ReadonlyArray<CommandOutputFile>;
    /** Timing trace when requested by the command. */
    readonly trace?: TraceSnapshot;
    /** Operation payload. */
    readonly data: CommandMessagePayload;
    /** Count of modules involved. */
    readonly moduleCount: number;
    /** Count of profiles involved. */
    readonly profileCount: number;
    /** Count of targets involved. */
    readonly targetCount: number;
};

export const TestOutput = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: TestOutput): void {
        encodeTestOutput(writer, value);
    },

    /** Decode one TestOutput. */
    decode(reader: BinaryReader): TestOutput {
        return decodeTestOutput(reader);
    },

    /** Return this value as JSON. */
    toJson(value: TestOutput): Json {
        return toJsonTestOutput(value);
    },

    /** Return one TestOutput from one JSON value. */
    fromJson(value: Json): TestOutput {
        return fromJsonTestOutput(value);
    },
};

/** Encode one TestOutput. */
export function encodeTestOutput(writer: BinaryWriter, value: TestOutput): void {
    encodeRevision(writer, value.revision);
    writer.writeBool(value.success);
    writer.writeSigned(value.exitCode);
    writer.writeUnsigned(value.diagnostics.length);
    for (const item3 of value.diagnostics) {
        encodeDiagnostic(writer, item3);
    }
    writer.writeUnsigned(value.files.length);
    for (const item4 of value.files) {
        encodeFileImage(writer, item4);
    }
    writer.writeUnsigned(value.messages.length);
    for (const item5 of value.messages) {
        encodeMessage(writer, item5);
    }
    writer.writeUnsigned(value.output.length);
    for (const item6 of value.output) {
        encodeCommandOutputChunk(writer, item6);
    }
    writer.writeUnsigned(value.outputs.length);
    for (const item7 of value.outputs) {
        encodeCommandOutputFile(writer, item7);
    }
    writer.writeOption(value.trace, (value8) => {
        encodeTraceSnapshot(writer, value8);
    });
    encodeCommandMessagePayload(writer, value.data);
    writer.writeUnsigned(value.moduleCount);
    writer.writeUnsigned(value.profileCount);
    writer.writeUnsigned(value.targetCount);
}

/** Decode one TestOutput. */
export function decodeTestOutput(reader: BinaryReader): TestOutput {
    const revision = decodeRevision(reader);
    const success = reader.readBool();
    const exitCode = reader.readSignedNumber();
    const diagnostics = (() => { const length3 = reader.readNumber(); const items3: Array<Diagnostic> = []; for (let index = 0; index < length3; index += 1) { items3.push(decodeDiagnostic(reader)); } return items3; })();
    const files = (() => { const length4 = reader.readNumber(); const items4: Array<FileImage> = []; for (let index = 0; index < length4; index += 1) { items4.push(decodeFileImage(reader)); } return items4; })();
    const messages = (() => { const length5 = reader.readNumber(); const items5: Array<Message> = []; for (let index = 0; index < length5; index += 1) { items5.push(decodeMessage(reader)); } return items5; })();
    const output = (() => { const length6 = reader.readNumber(); const items6: Array<CommandOutputChunk> = []; for (let index = 0; index < length6; index += 1) { items6.push(decodeCommandOutputChunk(reader)); } return items6; })();
    const outputs = (() => { const length7 = reader.readNumber(); const items7: Array<CommandOutputFile> = []; for (let index = 0; index < length7; index += 1) { items7.push(decodeCommandOutputFile(reader)); } return items7; })();
    const trace = reader.readOption(() => decodeTraceSnapshot(reader));
    const data = decodeCommandMessagePayload(reader);
    const moduleCount = reader.readNumber();
    const profileCount = reader.readNumber();
    const targetCount = reader.readNumber();

    return {
        revision,
        success,
        exitCode,
        diagnostics,
        files,
        messages,
        output,
        outputs,
        ...(trace === undefined ? {} : { trace }),
        data,
        moduleCount,
        profileCount,
        targetCount,
    };
}

/** Return one JSON value for one TestOutput. */
export function toJsonTestOutput(value: TestOutput): Json {
    return {
        revision: toJsonRevision(value.revision),
        success: value.success,
        exitCode: value.exitCode,
        diagnostics: value.diagnostics.map((item0) => toJsonDiagnostic(item0)),
        files: value.files.map((item0) => toJsonFileImage(item0)),
        messages: value.messages.map((item0) => toJsonMessage(item0)),
        output: value.output.map((item0) => toJsonCommandOutputChunk(item0)),
        outputs: value.outputs.map((item0) => toJsonCommandOutputFile(item0)),
        ...(value.trace === undefined ? {} : { trace: toJsonTraceSnapshot(value.trace) }),
        data: toJsonCommandMessagePayload(value.data),
        moduleCount: value.moduleCount,
        profileCount: value.profileCount,
        targetCount: value.targetCount,
    };
}

/** Return one TestOutput from one JSON value. */
export function fromJsonTestOutput(value: Json): TestOutput {
    const object = jsonObject(value);

    return {
        revision: fromJsonRevision(jsonField(object, "revision")),
        success: jsonBool(jsonField(object, "success")),
        exitCode: jsonInteger(jsonField(object, "exitCode")),
        diagnostics: jsonArray(jsonField(object, "diagnostics")).map((item0) => fromJsonDiagnostic(item0)),
        files: jsonArray(jsonField(object, "files")).map((item0) => fromJsonFileImage(item0)),
        messages: jsonArray(jsonField(object, "messages")).map((item0) => fromJsonMessage(item0)),
        output: jsonArray(jsonField(object, "output")).map((item0) => fromJsonCommandOutputChunk(item0)),
        outputs: jsonArray(jsonField(object, "outputs")).map((item0) => fromJsonCommandOutputFile(item0)),
        trace: jsonOptional(object, "trace", (value) => fromJsonTraceSnapshot(value)),
        data: fromJsonCommandMessagePayload(jsonField(object, "data")),
        moduleCount: jsonInteger(jsonField(object, "moduleCount")),
        profileCount: jsonInteger(jsonField(object, "profileCount")),
        targetCount: jsonInteger(jsonField(object, "targetCount")),
    };
}
