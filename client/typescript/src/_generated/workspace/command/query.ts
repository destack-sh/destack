/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, jsonArray, jsonBool, jsonField, jsonInteger, jsonObject, jsonOptional, jsonString } from "../../../protocol/serde.js";
import type { NodeType } from "../../dir/tree/node.js";
import type { TraceView } from "../../repository/provider/trace.js";
import type { Span } from "../../source/file/model/span.js";
import type { Uri } from "../../source/file/path/uri.js";
import type { CommandEnvVar } from "./common.js";
import type { CommandInput } from "./common.js";
import type { CommandRevision } from "./common.js";
import type { CommandTargetOverrides } from "./common.js";
import type { ManifestOverride } from "./common.js";
import { decodeNodeType, encodeNodeType, fromJsonNodeType, toJsonNodeType } from "../../dir/tree/node.js";
import { decodeTraceView, encodeTraceView, fromJsonTraceView, toJsonTraceView } from "../../repository/provider/trace.js";
import { decodeSpan, encodeSpan, fromJsonSpan, toJsonSpan } from "../../source/file/model/span.js";
import { decodeUri, encodeUri, fromJsonUri, toJsonUri } from "../../source/file/path/uri.js";
import { decodeCommandEnvVar, encodeCommandEnvVar, fromJsonCommandEnvVar, toJsonCommandEnvVar } from "./common.js";
import { decodeCommandInput, encodeCommandInput, fromJsonCommandInput, toJsonCommandInput } from "./common.js";
import { decodeCommandRevision, encodeCommandRevision, fromJsonCommandRevision, toJsonCommandRevision } from "./common.js";
import { decodeCommandTargetOverrides, encodeCommandTargetOverrides, fromJsonCommandTargetOverrides, toJsonCommandTargetOverrides } from "./common.js";
import { decodeManifestOverride, encodeManifestOverride, fromJsonManifestOverride, toJsonManifestOverride } from "./common.js";

/** One named pattern capture. */
export type QueryCapture = {
    /** The metavariable name without leading dollar signs. */
    readonly name: string;
    /** The captured values, empty for a repeated empty match. */
    readonly values: ReadonlyArray<QueryCaptureValue>;
};

export const QueryCapture = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: QueryCapture): void {
        encodeQueryCapture(writer, value);
    },

    /** Decode one QueryCapture. */
    decode(reader: BinaryReader): QueryCapture {
        return decodeQueryCapture(reader);
    },

    /** Return this value as JSON. */
    toJson(value: QueryCapture): Json {
        return toJsonQueryCapture(value);
    },

    /** Return one QueryCapture from one JSON value. */
    fromJson(value: Json): QueryCapture {
        return fromJsonQueryCapture(value);
    },
};

/** Encode one QueryCapture. */
export function encodeQueryCapture(writer: BinaryWriter, value: QueryCapture): void {
    writer.writeString(value.name);
    writer.writeUnsigned(value.values.length);
    for (const item1 of value.values) {
        encodeQueryCaptureValue(writer, item1);
    }
}

/** Decode one QueryCapture. */
export function decodeQueryCapture(reader: BinaryReader): QueryCapture {
    const name = reader.readString();
    const values = (() => { const length1 = reader.readNumber(); const items1: Array<QueryCaptureValue> = []; for (let index = 0; index < length1; index += 1) { items1.push(decodeQueryCaptureValue(reader)); } return items1; })();

    return {
        name,
        values,
    };
}

/** Return one JSON value for one QueryCapture. */
export function toJsonQueryCapture(value: QueryCapture): Json {
    return {
        name: value.name,
        values: value.values.map((item0) => toJsonQueryCaptureValue(item0)),
    };
}

/** Return one QueryCapture from one JSON value. */
export function fromJsonQueryCapture(value: Json): QueryCapture {
    const object = jsonObject(value);

    return {
        name: jsonString(jsonField(object, "name")),
        values: jsonArray(jsonField(object, "values")).map((item0) => fromJsonQueryCaptureValue(item0)),
    };
}

/** One captured source value. */
export type QueryCaptureValue = {
    /** The captured source span. */
    readonly span: Span;
    /** The exact captured source text. */
    readonly text: string;
};

export const QueryCaptureValue = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: QueryCaptureValue): void {
        encodeQueryCaptureValue(writer, value);
    },

    /** Decode one QueryCaptureValue. */
    decode(reader: BinaryReader): QueryCaptureValue {
        return decodeQueryCaptureValue(reader);
    },

    /** Return this value as JSON. */
    toJson(value: QueryCaptureValue): Json {
        return toJsonQueryCaptureValue(value);
    },

    /** Return one QueryCaptureValue from one JSON value. */
    fromJson(value: Json): QueryCaptureValue {
        return fromJsonQueryCaptureValue(value);
    },
};

/** Encode one QueryCaptureValue. */
export function encodeQueryCaptureValue(writer: BinaryWriter, value: QueryCaptureValue): void {
    encodeSpan(writer, value.span);
    writer.writeString(value.text);
}

/** Decode one QueryCaptureValue. */
export function decodeQueryCaptureValue(reader: BinaryReader): QueryCaptureValue {
    const span = decodeSpan(reader);
    const text = reader.readString();

    return {
        span,
        text,
    };
}

/** Return one JSON value for one QueryCaptureValue. */
export function toJsonQueryCaptureValue(value: QueryCaptureValue): Json {
    return {
        span: toJsonSpan(value.span),
        text: value.text,
    };
}

/** Return one QueryCaptureValue from one JSON value. */
export function fromJsonQueryCaptureValue(value: Json): QueryCaptureValue {
    const object = jsonObject(value);

    return {
        span: fromJsonSpan(jsonField(object, "span")),
        text: jsonString(jsonField(object, "text")),
    };
}

/** Request to query source with one structural pattern. */
export type QueryInput = {
    /** Revision selected for this query. */
    readonly revision: CommandRevision;
    /** Input sources for the command. */
    readonly inputs: ReadonlyArray<CommandInput>;
    /** Whether destack.json should resolve inputs when none are provided. */
    readonly configInputs: boolean;
    /** Optional working directory for this command. */
    readonly cwd?: string;
    /** Optional Destack manifest path override. */
    readonly manifest?: string;
    /** Optional target name override. */
    readonly target?: string;
    /** Optional target overrides. */
    readonly targetOverrides?: CommandTargetOverrides;
    /** Optional profile name override. */
    readonly profile?: string;
    /** Optional environment overrides. */
    readonly env: ReadonlyArray<CommandEnvVar>;
    /** Optional manifest overrides. */
    readonly overrides: ReadonlyArray<ManifestOverride>;
    /** Whether the command should watch for changes. */
    readonly watch: boolean;
    /** Whether the command should skip writes. */
    readonly dryRun: boolean;
    /** Trace detail returned for this command. */
    readonly trace?: TraceView;
    /** The authored structural pattern. */
    readonly pattern: string;
    /** The contextual node type, or expression matching by default. */
    readonly kind?: NodeType;
    /** Semantic predicate expressions applied in authored order. */
    readonly predicates: ReadonlyArray<string>;
    /** Whether the response should retain matched source files. */
    readonly includeSources: boolean;
};

export const QueryInput = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: QueryInput): void {
        encodeQueryInput(writer, value);
    },

    /** Decode one QueryInput. */
    decode(reader: BinaryReader): QueryInput {
        return decodeQueryInput(reader);
    },

    /** Return this value as JSON. */
    toJson(value: QueryInput): Json {
        return toJsonQueryInput(value);
    },

    /** Return one QueryInput from one JSON value. */
    fromJson(value: Json): QueryInput {
        return fromJsonQueryInput(value);
    },
};

/** Encode one QueryInput. */
export function encodeQueryInput(writer: BinaryWriter, value: QueryInput): void {
    encodeCommandRevision(writer, value.revision);
    writer.writeUnsigned(value.inputs.length);
    for (const item1 of value.inputs) {
        encodeCommandInput(writer, item1);
    }
    writer.writeBool(value.configInputs);
    writer.writeOption(value.cwd, (value3) => {
        writer.writeString(value3);
    });
    writer.writeOption(value.manifest, (value4) => {
        writer.writeString(value4);
    });
    writer.writeOption(value.target, (value5) => {
        writer.writeString(value5);
    });
    writer.writeOption(value.targetOverrides, (value6) => {
        encodeCommandTargetOverrides(writer, value6);
    });
    writer.writeOption(value.profile, (value7) => {
        writer.writeString(value7);
    });
    writer.writeUnsigned(value.env.length);
    for (const item8 of value.env) {
        encodeCommandEnvVar(writer, item8);
    }
    writer.writeUnsigned(value.overrides.length);
    for (const item9 of value.overrides) {
        encodeManifestOverride(writer, item9);
    }
    writer.writeBool(value.watch);
    writer.writeBool(value.dryRun);
    writer.writeOption(value.trace, (value12) => {
        encodeTraceView(writer, value12);
    });
    writer.writeString(value.pattern);
    writer.writeOption(value.kind, (value14) => {
        encodeNodeType(writer, value14);
    });
    writer.writeUnsigned(value.predicates.length);
    for (const item15 of value.predicates) {
        writer.writeString(item15);
    }
    writer.writeBool(value.includeSources);
}

/** Decode one QueryInput. */
export function decodeQueryInput(reader: BinaryReader): QueryInput {
    const revision = decodeCommandRevision(reader);
    const inputs = (() => { const length1 = reader.readNumber(); const items1: Array<CommandInput> = []; for (let index = 0; index < length1; index += 1) { items1.push(decodeCommandInput(reader)); } return items1; })();
    const configInputs = reader.readBool();
    const cwd = reader.readOption(() => reader.readString());
    const manifest = reader.readOption(() => reader.readString());
    const target = reader.readOption(() => reader.readString());
    const targetOverrides = reader.readOption(() => decodeCommandTargetOverrides(reader));
    const profile = reader.readOption(() => reader.readString());
    const env = (() => { const length8 = reader.readNumber(); const items8: Array<CommandEnvVar> = []; for (let index = 0; index < length8; index += 1) { items8.push(decodeCommandEnvVar(reader)); } return items8; })();
    const overrides = (() => { const length9 = reader.readNumber(); const items9: Array<ManifestOverride> = []; for (let index = 0; index < length9; index += 1) { items9.push(decodeManifestOverride(reader)); } return items9; })();
    const watch = reader.readBool();
    const dryRun = reader.readBool();
    const trace = reader.readOption(() => decodeTraceView(reader));
    const pattern = reader.readString();
    const kind = reader.readOption(() => decodeNodeType(reader));
    const predicates = (() => { const length15 = reader.readNumber(); const items15: Array<string> = []; for (let index = 0; index < length15; index += 1) { items15.push(reader.readString()); } return items15; })();
    const includeSources = reader.readBool();

    return {
        revision,
        inputs,
        configInputs,
        ...(cwd === undefined ? {} : { cwd }),
        ...(manifest === undefined ? {} : { manifest }),
        ...(target === undefined ? {} : { target }),
        ...(targetOverrides === undefined ? {} : { targetOverrides }),
        ...(profile === undefined ? {} : { profile }),
        env,
        overrides,
        watch,
        dryRun,
        ...(trace === undefined ? {} : { trace }),
        pattern,
        ...(kind === undefined ? {} : { kind }),
        predicates,
        includeSources,
    };
}

/** Return one JSON value for one QueryInput. */
export function toJsonQueryInput(value: QueryInput): Json {
    return {
        revision: toJsonCommandRevision(value.revision),
        inputs: value.inputs.map((item0) => toJsonCommandInput(item0)),
        configInputs: value.configInputs,
        ...(value.cwd === undefined ? {} : { cwd: value.cwd }),
        ...(value.manifest === undefined ? {} : { manifest: value.manifest }),
        ...(value.target === undefined ? {} : { target: value.target }),
        ...(value.targetOverrides === undefined ? {} : { targetOverrides: toJsonCommandTargetOverrides(value.targetOverrides) }),
        ...(value.profile === undefined ? {} : { profile: value.profile }),
        env: value.env.map((item0) => toJsonCommandEnvVar(item0)),
        overrides: value.overrides.map((item0) => toJsonManifestOverride(item0)),
        watch: value.watch,
        dryRun: value.dryRun,
        ...(value.trace === undefined ? {} : { trace: toJsonTraceView(value.trace) }),
        pattern: value.pattern,
        ...(value.kind === undefined ? {} : { kind: toJsonNodeType(value.kind) }),
        predicates: value.predicates.map((item0) => item0),
        includeSources: value.includeSources,
    };
}

/** Return one QueryInput from one JSON value. */
export function fromJsonQueryInput(value: Json): QueryInput {
    const object = jsonObject(value);

    return {
        revision: fromJsonCommandRevision(jsonField(object, "revision")),
        inputs: jsonArray(jsonField(object, "inputs")).map((item0) => fromJsonCommandInput(item0)),
        configInputs: jsonBool(jsonField(object, "configInputs")),
        cwd: jsonOptional(object, "cwd", (value) => jsonString(value)),
        manifest: jsonOptional(object, "manifest", (value) => jsonString(value)),
        target: jsonOptional(object, "target", (value) => jsonString(value)),
        targetOverrides: jsonOptional(object, "targetOverrides", (value) => fromJsonCommandTargetOverrides(value)),
        profile: jsonOptional(object, "profile", (value) => jsonString(value)),
        env: jsonArray(jsonField(object, "env")).map((item0) => fromJsonCommandEnvVar(item0)),
        overrides: jsonArray(jsonField(object, "overrides")).map((item0) => fromJsonManifestOverride(item0)),
        watch: jsonBool(jsonField(object, "watch")),
        dryRun: jsonBool(jsonField(object, "dryRun")),
        trace: jsonOptional(object, "trace", (value) => fromJsonTraceView(value)),
        pattern: jsonString(jsonField(object, "pattern")),
        kind: jsonOptional(object, "kind", (value) => fromJsonNodeType(value)),
        predicates: jsonArray(jsonField(object, "predicates")).map((item0) => jsonString(item0)),
        includeSources: jsonBool(jsonField(object, "includeSources")),
    };
}

/** One source node selected by a query. */
export type QueryMatch = {
    /** The source file URI. */
    readonly uri: Uri;
    /** The physical source path when one exists. */
    readonly path?: string;
    /** The complete selected source span. */
    readonly span: Span;
    /** The one-based source line. */
    readonly line: number;
    /** The one-based byte column. */
    readonly column: number;
    /** The exact selected source text. */
    readonly text: string;
    /** Named captures in pattern declaration order. */
    readonly captures: ReadonlyArray<QueryCapture>;
};

export const QueryMatch = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: QueryMatch): void {
        encodeQueryMatch(writer, value);
    },

    /** Decode one QueryMatch. */
    decode(reader: BinaryReader): QueryMatch {
        return decodeQueryMatch(reader);
    },

    /** Return this value as JSON. */
    toJson(value: QueryMatch): Json {
        return toJsonQueryMatch(value);
    },

    /** Return one QueryMatch from one JSON value. */
    fromJson(value: Json): QueryMatch {
        return fromJsonQueryMatch(value);
    },
};

/** Encode one QueryMatch. */
export function encodeQueryMatch(writer: BinaryWriter, value: QueryMatch): void {
    encodeUri(writer, value.uri);
    writer.writeOption(value.path, (value1) => {
        writer.writeString(value1);
    });
    encodeSpan(writer, value.span);
    writer.writeUnsigned(value.line);
    writer.writeUnsigned(value.column);
    writer.writeString(value.text);
    writer.writeUnsigned(value.captures.length);
    for (const item6 of value.captures) {
        encodeQueryCapture(writer, item6);
    }
}

/** Decode one QueryMatch. */
export function decodeQueryMatch(reader: BinaryReader): QueryMatch {
    const uri = decodeUri(reader);
    const path = reader.readOption(() => reader.readString());
    const span = decodeSpan(reader);
    const line = reader.readNumber();
    const column = reader.readNumber();
    const text = reader.readString();
    const captures = (() => { const length6 = reader.readNumber(); const items6: Array<QueryCapture> = []; for (let index = 0; index < length6; index += 1) { items6.push(decodeQueryCapture(reader)); } return items6; })();

    return {
        uri,
        ...(path === undefined ? {} : { path }),
        span,
        line,
        column,
        text,
        captures,
    };
}

/** Return one JSON value for one QueryMatch. */
export function toJsonQueryMatch(value: QueryMatch): Json {
    return {
        uri: toJsonUri(value.uri),
        ...(value.path === undefined ? {} : { path: value.path }),
        span: toJsonSpan(value.span),
        line: value.line,
        column: value.column,
        text: value.text,
        captures: value.captures.map((item0) => toJsonQueryCapture(item0)),
    };
}

/** Return one QueryMatch from one JSON value. */
export function fromJsonQueryMatch(value: Json): QueryMatch {
    const object = jsonObject(value);

    return {
        uri: fromJsonUri(jsonField(object, "uri")),
        path: jsonOptional(object, "path", (value) => jsonString(value)),
        span: fromJsonSpan(jsonField(object, "span")),
        line: jsonInteger(jsonField(object, "line")),
        column: jsonInteger(jsonField(object, "column")),
        text: jsonString(jsonField(object, "text")),
        captures: jsonArray(jsonField(object, "captures")).map((item0) => fromJsonQueryCapture(item0)),
    };
}

/** Payload for query command output. */
export type QueryPayload = {
    /** Named captures declared by the compiled pattern. */
    readonly captures: ReadonlyArray<string>;
    /** Matches in module, file, and source order. */
    readonly matches: ReadonlyArray<QueryMatch>;
};

export const QueryPayload = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: QueryPayload): void {
        encodeQueryPayload(writer, value);
    },

    /** Decode one QueryPayload. */
    decode(reader: BinaryReader): QueryPayload {
        return decodeQueryPayload(reader);
    },

    /** Return this value as JSON. */
    toJson(value: QueryPayload): Json {
        return toJsonQueryPayload(value);
    },

    /** Return one QueryPayload from one JSON value. */
    fromJson(value: Json): QueryPayload {
        return fromJsonQueryPayload(value);
    },
};

/** Encode one QueryPayload. */
export function encodeQueryPayload(writer: BinaryWriter, value: QueryPayload): void {
    writer.writeUnsigned(value.captures.length);
    for (const item0 of value.captures) {
        writer.writeString(item0);
    }
    writer.writeUnsigned(value.matches.length);
    for (const item1 of value.matches) {
        encodeQueryMatch(writer, item1);
    }
}

/** Decode one QueryPayload. */
export function decodeQueryPayload(reader: BinaryReader): QueryPayload {
    const captures = (() => { const length0 = reader.readNumber(); const items0: Array<string> = []; for (let index = 0; index < length0; index += 1) { items0.push(reader.readString()); } return items0; })();
    const matches = (() => { const length1 = reader.readNumber(); const items1: Array<QueryMatch> = []; for (let index = 0; index < length1; index += 1) { items1.push(decodeQueryMatch(reader)); } return items1; })();

    return {
        captures,
        matches,
    };
}

/** Return one JSON value for one QueryPayload. */
export function toJsonQueryPayload(value: QueryPayload): Json {
    return {
        captures: value.captures.map((item0) => item0),
        matches: value.matches.map((item0) => toJsonQueryMatch(item0)),
    };
}

/** Return one QueryPayload from one JSON value. */
export function fromJsonQueryPayload(value: Json): QueryPayload {
    const object = jsonObject(value);

    return {
        captures: jsonArray(jsonField(object, "captures")).map((item0) => jsonString(item0)),
        matches: jsonArray(jsonField(object, "matches")).map((item0) => fromJsonQueryMatch(item0)),
    };
}
