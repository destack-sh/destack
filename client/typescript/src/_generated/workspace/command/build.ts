/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, jsonArray, jsonBool, jsonField, jsonObject, jsonOptional, jsonString } from "../../../protocol/serde.js";
import type { ArtifactReference } from "../../artifact/reference.js";
import type { TraceView } from "../../repository/provider/trace.js";
import type { CommandEnvVar } from "./common.js";
import type { CommandInput } from "./common.js";
import type { CommandRevision } from "./common.js";
import type { CommandTargetOverrides } from "./common.js";
import type { ManifestOverride } from "./common.js";
import { decodeArtifactReference, encodeArtifactReference, fromJsonArtifactReference, toJsonArtifactReference } from "../../artifact/reference.js";
import { decodeTraceView, encodeTraceView, fromJsonTraceView, toJsonTraceView } from "../../repository/provider/trace.js";
import { decodeCommandEnvVar, encodeCommandEnvVar, fromJsonCommandEnvVar, toJsonCommandEnvVar } from "./common.js";
import { decodeCommandInput, encodeCommandInput, fromJsonCommandInput, toJsonCommandInput } from "./common.js";
import { decodeCommandRevision, encodeCommandRevision, fromJsonCommandRevision, toJsonCommandRevision } from "./common.js";
import { decodeCommandTargetOverrides, encodeCommandTargetOverrides, fromJsonCommandTargetOverrides, toJsonCommandTargetOverrides } from "./common.js";
import { decodeManifestOverride, encodeManifestOverride, fromJsonManifestOverride, toJsonManifestOverride } from "./common.js";

/** Request to build target artifacts. */
export type BuildInput = {
    /** Revision selected for this build. */
    readonly revision: CommandRevision;
    /** Input sources for the command. */
    readonly inputs: ReadonlyArray<CommandInput>;
    /** Whether destack.json should resolve inputs when none are provided. */
    readonly configInputs: boolean;
    /** Optional working directory for this command. */
    readonly cwd?: string;
    /** Optional Destack manifest path override. */
    readonly manifest?: string;
    /** Optional target name override. */
    readonly target?: string;
    /** Optional target overrides. */
    readonly targetOverrides?: CommandTargetOverrides;
    /** Optional profile name override. */
    readonly profile?: string;
    /** Optional environment overrides. */
    readonly env: ReadonlyArray<CommandEnvVar>;
    /** Optional manifest overrides. */
    readonly overrides: ReadonlyArray<ManifestOverride>;
    /** Whether the command should watch for changes. */
    readonly watch: boolean;
    /** Whether the command should skip writes. */
    readonly dryRun: boolean;
    /** Trace detail returned for this command. */
    readonly trace?: TraceView;
    /** Product name selected for this build. */
    readonly product?: string;
    /** Build outputs requested by the caller. */
    readonly outputs: BuildOutputs;
};

export const BuildInput = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: BuildInput): void {
        encodeBuildInput(writer, value);
    },

    /** Decode one BuildInput. */
    decode(reader: BinaryReader): BuildInput {
        return decodeBuildInput(reader);
    },

    /** Return this value as JSON. */
    toJson(value: BuildInput): Json {
        return toJsonBuildInput(value);
    },

    /** Return one BuildInput from one JSON value. */
    fromJson(value: Json): BuildInput {
        return fromJsonBuildInput(value);
    },
};

/** Encode one BuildInput. */
export function encodeBuildInput(writer: BinaryWriter, value: BuildInput): void {
    encodeCommandRevision(writer, value.revision);
    writer.writeUnsigned(value.inputs.length);
    for (const item1 of value.inputs) {
        encodeCommandInput(writer, item1);
    }
    writer.writeBool(value.configInputs);
    writer.writeOption(value.cwd, (value3) => {
        writer.writeString(value3);
    });
    writer.writeOption(value.manifest, (value4) => {
        writer.writeString(value4);
    });
    writer.writeOption(value.target, (value5) => {
        writer.writeString(value5);
    });
    writer.writeOption(value.targetOverrides, (value6) => {
        encodeCommandTargetOverrides(writer, value6);
    });
    writer.writeOption(value.profile, (value7) => {
        writer.writeString(value7);
    });
    writer.writeUnsigned(value.env.length);
    for (const item8 of value.env) {
        encodeCommandEnvVar(writer, item8);
    }
    writer.writeUnsigned(value.overrides.length);
    for (const item9 of value.overrides) {
        encodeManifestOverride(writer, item9);
    }
    writer.writeBool(value.watch);
    writer.writeBool(value.dryRun);
    writer.writeOption(value.trace, (value12) => {
        encodeTraceView(writer, value12);
    });
    writer.writeOption(value.product, (value13) => {
        writer.writeString(value13);
    });
    encodeBuildOutputs(writer, value.outputs);
}

/** Decode one BuildInput. */
export function decodeBuildInput(reader: BinaryReader): BuildInput {
    const revision = decodeCommandRevision(reader);
    const inputs = (() => { const length1 = reader.readNumber(); const items1: Array<CommandInput> = []; for (let index = 0; index < length1; index += 1) { items1.push(decodeCommandInput(reader)); } return items1; })();
    const configInputs = reader.readBool();
    const cwd = reader.readOption(() => reader.readString());
    const manifest = reader.readOption(() => reader.readString());
    const target = reader.readOption(() => reader.readString());
    const targetOverrides = reader.readOption(() => decodeCommandTargetOverrides(reader));
    const profile = reader.readOption(() => reader.readString());
    const env = (() => { const length8 = reader.readNumber(); const items8: Array<CommandEnvVar> = []; for (let index = 0; index < length8; index += 1) { items8.push(decodeCommandEnvVar(reader)); } return items8; })();
    const overrides = (() => { const length9 = reader.readNumber(); const items9: Array<ManifestOverride> = []; for (let index = 0; index < length9; index += 1) { items9.push(decodeManifestOverride(reader)); } return items9; })();
    const watch = reader.readBool();
    const dryRun = reader.readBool();
    const trace = reader.readOption(() => decodeTraceView(reader));
    const product = reader.readOption(() => reader.readString());
    const outputs = decodeBuildOutputs(reader);

    return {
        revision,
        inputs,
        configInputs,
        ...(cwd === undefined ? {} : { cwd }),
        ...(manifest === undefined ? {} : { manifest }),
        ...(target === undefined ? {} : { target }),
        ...(targetOverrides === undefined ? {} : { targetOverrides }),
        ...(profile === undefined ? {} : { profile }),
        env,
        overrides,
        watch,
        dryRun,
        ...(trace === undefined ? {} : { trace }),
        ...(product === undefined ? {} : { product }),
        outputs,
    };
}

/** Return one JSON value for one BuildInput. */
export function toJsonBuildInput(value: BuildInput): Json {
    return {
        revision: toJsonCommandRevision(value.revision),
        inputs: value.inputs.map((item0) => toJsonCommandInput(item0)),
        configInputs: value.configInputs,
        ...(value.cwd === undefined ? {} : { cwd: value.cwd }),
        ...(value.manifest === undefined ? {} : { manifest: value.manifest }),
        ...(value.target === undefined ? {} : { target: value.target }),
        ...(value.targetOverrides === undefined ? {} : { targetOverrides: toJsonCommandTargetOverrides(value.targetOverrides) }),
        ...(value.profile === undefined ? {} : { profile: value.profile }),
        env: value.env.map((item0) => toJsonCommandEnvVar(item0)),
        overrides: value.overrides.map((item0) => toJsonManifestOverride(item0)),
        watch: value.watch,
        dryRun: value.dryRun,
        ...(value.trace === undefined ? {} : { trace: toJsonTraceView(value.trace) }),
        ...(value.product === undefined ? {} : { product: value.product }),
        outputs: toJsonBuildOutputs(value.outputs),
    };
}

/** Return one BuildInput from one JSON value. */
export function fromJsonBuildInput(value: Json): BuildInput {
    const object = jsonObject(value);

    return {
        revision: fromJsonCommandRevision(jsonField(object, "revision")),
        inputs: jsonArray(jsonField(object, "inputs")).map((item0) => fromJsonCommandInput(item0)),
        configInputs: jsonBool(jsonField(object, "configInputs")),
        cwd: jsonOptional(object, "cwd", (value) => jsonString(value)),
        manifest: jsonOptional(object, "manifest", (value) => jsonString(value)),
        target: jsonOptional(object, "target", (value) => jsonString(value)),
        targetOverrides: jsonOptional(object, "targetOverrides", (value) => fromJsonCommandTargetOverrides(value)),
        profile: jsonOptional(object, "profile", (value) => jsonString(value)),
        env: jsonArray(jsonField(object, "env")).map((item0) => fromJsonCommandEnvVar(item0)),
        overrides: jsonArray(jsonField(object, "overrides")).map((item0) => fromJsonManifestOverride(item0)),
        watch: jsonBool(jsonField(object, "watch")),
        dryRun: jsonBool(jsonField(object, "dryRun")),
        trace: jsonOptional(object, "trace", (value) => fromJsonTraceView(value)),
        product: jsonOptional(object, "product", (value) => jsonString(value)),
        outputs: fromJsonBuildOutputs(jsonField(object, "outputs")),
    };
}

/** Build output families requested by a caller. */
export type BuildOutputs = {
    /** Return product artifact refs. */
    readonly products: boolean;
    /** Return bundle artifact refs. */
    readonly bundles: boolean;
    /** Return program artifact refs. */
    readonly programs: boolean;
    /** Return per-module asset artifact refs. */
    readonly assets: boolean;
};

export const BuildOutputs = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: BuildOutputs): void {
        encodeBuildOutputs(writer, value);
    },

    /** Decode one BuildOutputs. */
    decode(reader: BinaryReader): BuildOutputs {
        return decodeBuildOutputs(reader);
    },

    /** Return this value as JSON. */
    toJson(value: BuildOutputs): Json {
        return toJsonBuildOutputs(value);
    },

    /** Return one BuildOutputs from one JSON value. */
    fromJson(value: Json): BuildOutputs {
        return fromJsonBuildOutputs(value);
    },
};

/** Encode one BuildOutputs. */
export function encodeBuildOutputs(writer: BinaryWriter, value: BuildOutputs): void {
    writer.writeBool(value.products);
    writer.writeBool(value.bundles);
    writer.writeBool(value.programs);
    writer.writeBool(value.assets);
}

/** Decode one BuildOutputs. */
export function decodeBuildOutputs(reader: BinaryReader): BuildOutputs {
    const products = reader.readBool();
    const bundles = reader.readBool();
    const programs = reader.readBool();
    const assets = reader.readBool();

    return {
        products,
        bundles,
        programs,
        assets,
    };
}

/** Return one JSON value for one BuildOutputs. */
export function toJsonBuildOutputs(value: BuildOutputs): Json {
    return {
        products: value.products,
        bundles: value.bundles,
        programs: value.programs,
        assets: value.assets,
    };
}

/** Return one BuildOutputs from one JSON value. */
export function fromJsonBuildOutputs(value: Json): BuildOutputs {
    const object = jsonObject(value);

    return {
        products: jsonBool(jsonField(object, "products")),
        bundles: jsonBool(jsonField(object, "bundles")),
        programs: jsonBool(jsonField(object, "programs")),
        assets: jsonBool(jsonField(object, "assets")),
    };
}

/** Payload for build command output. */
export type BuildPayload = {
    /** Product artifacts produced by this build. */
    readonly products: ReadonlyArray<ArtifactReference>;
    /** Bundle artifacts produced by this build. */
    readonly bundles: ReadonlyArray<ArtifactReference>;
    /** Program artifacts produced by this build. */
    readonly programs: ReadonlyArray<ArtifactReference>;
    /** Per-module asset artifacts produced by this build. */
    readonly assets: ReadonlyArray<ArtifactReference>;
};

export const BuildPayload = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: BuildPayload): void {
        encodeBuildPayload(writer, value);
    },

    /** Decode one BuildPayload. */
    decode(reader: BinaryReader): BuildPayload {
        return decodeBuildPayload(reader);
    },

    /** Return this value as JSON. */
    toJson(value: BuildPayload): Json {
        return toJsonBuildPayload(value);
    },

    /** Return one BuildPayload from one JSON value. */
    fromJson(value: Json): BuildPayload {
        return fromJsonBuildPayload(value);
    },
};

/** Encode one BuildPayload. */
export function encodeBuildPayload(writer: BinaryWriter, value: BuildPayload): void {
    writer.writeUnsigned(value.products.length);
    for (const item0 of value.products) {
        encodeArtifactReference(writer, item0);
    }
    writer.writeUnsigned(value.bundles.length);
    for (const item1 of value.bundles) {
        encodeArtifactReference(writer, item1);
    }
    writer.writeUnsigned(value.programs.length);
    for (const item2 of value.programs) {
        encodeArtifactReference(writer, item2);
    }
    writer.writeUnsigned(value.assets.length);
    for (const item3 of value.assets) {
        encodeArtifactReference(writer, item3);
    }
}

/** Decode one BuildPayload. */
export function decodeBuildPayload(reader: BinaryReader): BuildPayload {
    const products = (() => { const length0 = reader.readNumber(); const items0: Array<ArtifactReference> = []; for (let index = 0; index < length0; index += 1) { items0.push(decodeArtifactReference(reader)); } return items0; })();
    const bundles = (() => { const length1 = reader.readNumber(); const items1: Array<ArtifactReference> = []; for (let index = 0; index < length1; index += 1) { items1.push(decodeArtifactReference(reader)); } return items1; })();
    const programs = (() => { const length2 = reader.readNumber(); const items2: Array<ArtifactReference> = []; for (let index = 0; index < length2; index += 1) { items2.push(decodeArtifactReference(reader)); } return items2; })();
    const assets = (() => { const length3 = reader.readNumber(); const items3: Array<ArtifactReference> = []; for (let index = 0; index < length3; index += 1) { items3.push(decodeArtifactReference(reader)); } return items3; })();

    return {
        products,
        bundles,
        programs,
        assets,
    };
}

/** Return one JSON value for one BuildPayload. */
export function toJsonBuildPayload(value: BuildPayload): Json {
    return {
        products: value.products.map((item0) => toJsonArtifactReference(item0)),
        bundles: value.bundles.map((item0) => toJsonArtifactReference(item0)),
        programs: value.programs.map((item0) => toJsonArtifactReference(item0)),
        assets: value.assets.map((item0) => toJsonArtifactReference(item0)),
    };
}

/** Return one BuildPayload from one JSON value. */
export function fromJsonBuildPayload(value: Json): BuildPayload {
    const object = jsonObject(value);

    return {
        products: jsonArray(jsonField(object, "products")).map((item0) => fromJsonArtifactReference(item0)),
        bundles: jsonArray(jsonField(object, "bundles")).map((item0) => fromJsonArtifactReference(item0)),
        programs: jsonArray(jsonField(object, "programs")).map((item0) => fromJsonArtifactReference(item0)),
        assets: jsonArray(jsonField(object, "assets")).map((item0) => fromJsonArtifactReference(item0)),
    };
}
