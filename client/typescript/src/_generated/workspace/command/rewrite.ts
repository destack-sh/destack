/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, jsonArray, jsonBool, jsonField, jsonInteger, jsonObject, jsonOptional, jsonString } from "../../../protocol/serde.js";
import type { NodeType } from "../../dir/tree/node.js";
import type { TraceView } from "../../repository/provider/trace.js";
import type { FilePatch } from "../../source/edit/edit.js";
import type { Uri } from "../../source/file/path/uri.js";
import type { CommandEnvVar } from "./common.js";
import type { CommandInput } from "./common.js";
import type { CommandRevision } from "./common.js";
import type { CommandTargetOverrides } from "./common.js";
import type { ManifestOverride } from "./common.js";
import type { Commit } from "../file/update.js";
import { decodeNodeType, encodeNodeType, fromJsonNodeType, toJsonNodeType } from "../../dir/tree/node.js";
import { decodeTraceView, encodeTraceView, fromJsonTraceView, toJsonTraceView } from "../../repository/provider/trace.js";
import { decodeFilePatch, encodeFilePatch, fromJsonFilePatch, toJsonFilePatch } from "../../source/edit/edit.js";
import { decodeUri, encodeUri, fromJsonUri, toJsonUri } from "../../source/file/path/uri.js";
import { decodeCommandEnvVar, encodeCommandEnvVar, fromJsonCommandEnvVar, toJsonCommandEnvVar } from "./common.js";
import { decodeCommandInput, encodeCommandInput, fromJsonCommandInput, toJsonCommandInput } from "./common.js";
import { decodeCommandRevision, encodeCommandRevision, fromJsonCommandRevision, toJsonCommandRevision } from "./common.js";
import { decodeCommandTargetOverrides, encodeCommandTargetOverrides, fromJsonCommandTargetOverrides, toJsonCommandTargetOverrides } from "./common.js";
import { decodeManifestOverride, encodeManifestOverride, fromJsonManifestOverride, toJsonManifestOverride } from "./common.js";
import { decodeCommit, encodeCommit, fromJsonCommit, toJsonCommit } from "../file/update.js";

/** One source file changed by a rewrite. */
export type RewriteChange = {
    /** The source file URI. */
    readonly uri: Uri;
    /** The physical source path when one exists. */
    readonly path?: string;
    /** The exact replacements applied to this source revision. */
    readonly patch: FilePatch;
};

export const RewriteChange = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: RewriteChange): void {
        encodeRewriteChange(writer, value);
    },

    /** Decode one RewriteChange. */
    decode(reader: BinaryReader): RewriteChange {
        return decodeRewriteChange(reader);
    },

    /** Return this value as JSON. */
    toJson(value: RewriteChange): Json {
        return toJsonRewriteChange(value);
    },

    /** Return one RewriteChange from one JSON value. */
    fromJson(value: Json): RewriteChange {
        return fromJsonRewriteChange(value);
    },
};

/** Encode one RewriteChange. */
export function encodeRewriteChange(writer: BinaryWriter, value: RewriteChange): void {
    encodeUri(writer, value.uri);
    writer.writeOption(value.path, (value1) => {
        writer.writeString(value1);
    });
    encodeFilePatch(writer, value.patch);
}

/** Decode one RewriteChange. */
export function decodeRewriteChange(reader: BinaryReader): RewriteChange {
    const uri = decodeUri(reader);
    const path = reader.readOption(() => reader.readString());
    const patch = decodeFilePatch(reader);

    return {
        uri,
        ...(path === undefined ? {} : { path }),
        patch,
    };
}

/** Return one JSON value for one RewriteChange. */
export function toJsonRewriteChange(value: RewriteChange): Json {
    return {
        uri: toJsonUri(value.uri),
        ...(value.path === undefined ? {} : { path: value.path }),
        patch: toJsonFilePatch(value.patch),
    };
}

/** Return one RewriteChange from one JSON value. */
export function fromJsonRewriteChange(value: Json): RewriteChange {
    const object = jsonObject(value);

    return {
        uri: fromJsonUri(jsonField(object, "uri")),
        path: jsonOptional(object, "path", (value) => jsonString(value)),
        patch: fromJsonFilePatch(jsonField(object, "patch")),
    };
}

/** Request to rewrite source with one structural pattern. */
export type RewriteInput = {
    /** Revision selected for this rewrite. */
    readonly revision: CommandRevision;
    /** Input sources for the command. */
    readonly inputs: ReadonlyArray<CommandInput>;
    /** Whether destack.json should resolve inputs when none are provided. */
    readonly configInputs: boolean;
    /** Optional working directory for this command. */
    readonly cwd?: string;
    /** Optional Destack manifest path override. */
    readonly manifest?: string;
    /** Optional target name override. */
    readonly target?: string;
    /** Optional target overrides. */
    readonly targetOverrides?: CommandTargetOverrides;
    /** Optional profile name override. */
    readonly profile?: string;
    /** Optional environment overrides. */
    readonly env: ReadonlyArray<CommandEnvVar>;
    /** Optional manifest overrides. */
    readonly overrides: ReadonlyArray<ManifestOverride>;
    /** Whether the command should watch for changes. */
    readonly watch: boolean;
    /** Whether the command should skip writes. */
    readonly dryRun: boolean;
    /** Trace detail returned for this command. */
    readonly trace?: TraceView;
    /** The authored structural search pattern. */
    readonly pattern: string;
    /** The authored structural replacement. */
    readonly replacement: string;
    /** The contextual node type, or expression rewriting by default. */
    readonly kind?: NodeType;
    /** Semantic predicate expressions applied in authored order. */
    readonly predicates: ReadonlyArray<string>;
    /** Source mutation behavior. */
    readonly mode: RewriteMode;
};

export const RewriteInput = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: RewriteInput): void {
        encodeRewriteInput(writer, value);
    },

    /** Decode one RewriteInput. */
    decode(reader: BinaryReader): RewriteInput {
        return decodeRewriteInput(reader);
    },

    /** Return this value as JSON. */
    toJson(value: RewriteInput): Json {
        return toJsonRewriteInput(value);
    },

    /** Return one RewriteInput from one JSON value. */
    fromJson(value: Json): RewriteInput {
        return fromJsonRewriteInput(value);
    },
};

/** Encode one RewriteInput. */
export function encodeRewriteInput(writer: BinaryWriter, value: RewriteInput): void {
    encodeCommandRevision(writer, value.revision);
    writer.writeUnsigned(value.inputs.length);
    for (const item1 of value.inputs) {
        encodeCommandInput(writer, item1);
    }
    writer.writeBool(value.configInputs);
    writer.writeOption(value.cwd, (value3) => {
        writer.writeString(value3);
    });
    writer.writeOption(value.manifest, (value4) => {
        writer.writeString(value4);
    });
    writer.writeOption(value.target, (value5) => {
        writer.writeString(value5);
    });
    writer.writeOption(value.targetOverrides, (value6) => {
        encodeCommandTargetOverrides(writer, value6);
    });
    writer.writeOption(value.profile, (value7) => {
        writer.writeString(value7);
    });
    writer.writeUnsigned(value.env.length);
    for (const item8 of value.env) {
        encodeCommandEnvVar(writer, item8);
    }
    writer.writeUnsigned(value.overrides.length);
    for (const item9 of value.overrides) {
        encodeManifestOverride(writer, item9);
    }
    writer.writeBool(value.watch);
    writer.writeBool(value.dryRun);
    writer.writeOption(value.trace, (value12) => {
        encodeTraceView(writer, value12);
    });
    writer.writeString(value.pattern);
    writer.writeString(value.replacement);
    writer.writeOption(value.kind, (value15) => {
        encodeNodeType(writer, value15);
    });
    writer.writeUnsigned(value.predicates.length);
    for (const item16 of value.predicates) {
        writer.writeString(item16);
    }
    encodeRewriteMode(writer, value.mode);
}

/** Decode one RewriteInput. */
export function decodeRewriteInput(reader: BinaryReader): RewriteInput {
    const revision = decodeCommandRevision(reader);
    const inputs = (() => { const length1 = reader.readNumber(); const items1: Array<CommandInput> = []; for (let index = 0; index < length1; index += 1) { items1.push(decodeCommandInput(reader)); } return items1; })();
    const configInputs = reader.readBool();
    const cwd = reader.readOption(() => reader.readString());
    const manifest = reader.readOption(() => reader.readString());
    const target = reader.readOption(() => reader.readString());
    const targetOverrides = reader.readOption(() => decodeCommandTargetOverrides(reader));
    const profile = reader.readOption(() => reader.readString());
    const env = (() => { const length8 = reader.readNumber(); const items8: Array<CommandEnvVar> = []; for (let index = 0; index < length8; index += 1) { items8.push(decodeCommandEnvVar(reader)); } return items8; })();
    const overrides = (() => { const length9 = reader.readNumber(); const items9: Array<ManifestOverride> = []; for (let index = 0; index < length9; index += 1) { items9.push(decodeManifestOverride(reader)); } return items9; })();
    const watch = reader.readBool();
    const dryRun = reader.readBool();
    const trace = reader.readOption(() => decodeTraceView(reader));
    const pattern = reader.readString();
    const replacement = reader.readString();
    const kind = reader.readOption(() => decodeNodeType(reader));
    const predicates = (() => { const length16 = reader.readNumber(); const items16: Array<string> = []; for (let index = 0; index < length16; index += 1) { items16.push(reader.readString()); } return items16; })();
    const mode = decodeRewriteMode(reader);

    return {
        revision,
        inputs,
        configInputs,
        ...(cwd === undefined ? {} : { cwd }),
        ...(manifest === undefined ? {} : { manifest }),
        ...(target === undefined ? {} : { target }),
        ...(targetOverrides === undefined ? {} : { targetOverrides }),
        ...(profile === undefined ? {} : { profile }),
        env,
        overrides,
        watch,
        dryRun,
        ...(trace === undefined ? {} : { trace }),
        pattern,
        replacement,
        ...(kind === undefined ? {} : { kind }),
        predicates,
        mode,
    };
}

/** Return one JSON value for one RewriteInput. */
export function toJsonRewriteInput(value: RewriteInput): Json {
    return {
        revision: toJsonCommandRevision(value.revision),
        inputs: value.inputs.map((item0) => toJsonCommandInput(item0)),
        configInputs: value.configInputs,
        ...(value.cwd === undefined ? {} : { cwd: value.cwd }),
        ...(value.manifest === undefined ? {} : { manifest: value.manifest }),
        ...(value.target === undefined ? {} : { target: value.target }),
        ...(value.targetOverrides === undefined ? {} : { targetOverrides: toJsonCommandTargetOverrides(value.targetOverrides) }),
        ...(value.profile === undefined ? {} : { profile: value.profile }),
        env: value.env.map((item0) => toJsonCommandEnvVar(item0)),
        overrides: value.overrides.map((item0) => toJsonManifestOverride(item0)),
        watch: value.watch,
        dryRun: value.dryRun,
        ...(value.trace === undefined ? {} : { trace: toJsonTraceView(value.trace) }),
        pattern: value.pattern,
        replacement: value.replacement,
        ...(value.kind === undefined ? {} : { kind: toJsonNodeType(value.kind) }),
        predicates: value.predicates.map((item0) => item0),
        mode: toJsonRewriteMode(value.mode),
    };
}

/** Return one RewriteInput from one JSON value. */
export function fromJsonRewriteInput(value: Json): RewriteInput {
    const object = jsonObject(value);

    return {
        revision: fromJsonCommandRevision(jsonField(object, "revision")),
        inputs: jsonArray(jsonField(object, "inputs")).map((item0) => fromJsonCommandInput(item0)),
        configInputs: jsonBool(jsonField(object, "configInputs")),
        cwd: jsonOptional(object, "cwd", (value) => jsonString(value)),
        manifest: jsonOptional(object, "manifest", (value) => jsonString(value)),
        target: jsonOptional(object, "target", (value) => jsonString(value)),
        targetOverrides: jsonOptional(object, "targetOverrides", (value) => fromJsonCommandTargetOverrides(value)),
        profile: jsonOptional(object, "profile", (value) => jsonString(value)),
        env: jsonArray(jsonField(object, "env")).map((item0) => fromJsonCommandEnvVar(item0)),
        overrides: jsonArray(jsonField(object, "overrides")).map((item0) => fromJsonManifestOverride(item0)),
        watch: jsonBool(jsonField(object, "watch")),
        dryRun: jsonBool(jsonField(object, "dryRun")),
        trace: jsonOptional(object, "trace", (value) => fromJsonTraceView(value)),
        pattern: jsonString(jsonField(object, "pattern")),
        replacement: jsonString(jsonField(object, "replacement")),
        kind: jsonOptional(object, "kind", (value) => fromJsonNodeType(value)),
        predicates: jsonArray(jsonField(object, "predicates")).map((item0) => jsonString(item0)),
        mode: fromJsonRewriteMode(jsonField(object, "mode")),
    };
}

/** Execution mode for a structural rewrite. */
export type RewriteMode = "diff" | "check" | "write";

export const RewriteMode = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: RewriteMode): void {
        encodeRewriteMode(writer, value);
    },

    /** Decode one RewriteMode. */
    decode(reader: BinaryReader): RewriteMode {
        return decodeRewriteMode(reader);
    },

    /** Return this value as JSON. */
    toJson(value: RewriteMode): Json {
        return toJsonRewriteMode(value);
    },

    /** Return one RewriteMode from one JSON value. */
    fromJson(value: Json): RewriteMode {
        return fromJsonRewriteMode(value);
    },
};

/** Encode one RewriteMode. */
export function encodeRewriteMode(writer: BinaryWriter, value: RewriteMode): void {
    switch (value) {
        case "diff":
            writer.writeUnsigned(0);
            return;
        case "check":
            writer.writeUnsigned(1);
            return;
        case "write":
            writer.writeUnsigned(2);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one RewriteMode. */
export function decodeRewriteMode(reader: BinaryReader): RewriteMode {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "diff";
        case 1:
            return "check";
        case 2:
            return "write";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one RewriteMode. */
export function toJsonRewriteMode(value: RewriteMode): Json {
    return value;
}

/** Return one RewriteMode from one JSON value. */
export function fromJsonRewriteMode(value: Json): RewriteMode {
    const variant = jsonString(value);

    switch (variant) {
        case "diff":
            return "diff";
        case "check":
            return "check";
        case "write":
            return "write";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** Payload for rewrite command output. */
export type RewritePayload = {
    /** Changed files in module and physical file order. */
    readonly changes: ReadonlyArray<RewriteChange>;
    /** The total number of replacements. */
    readonly replacements: number;
    /** The source commit produced by write mode. */
    readonly commit?: Commit;
};

export const RewritePayload = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: RewritePayload): void {
        encodeRewritePayload(writer, value);
    },

    /** Decode one RewritePayload. */
    decode(reader: BinaryReader): RewritePayload {
        return decodeRewritePayload(reader);
    },

    /** Return this value as JSON. */
    toJson(value: RewritePayload): Json {
        return toJsonRewritePayload(value);
    },

    /** Return one RewritePayload from one JSON value. */
    fromJson(value: Json): RewritePayload {
        return fromJsonRewritePayload(value);
    },
};

/** Encode one RewritePayload. */
export function encodeRewritePayload(writer: BinaryWriter, value: RewritePayload): void {
    writer.writeUnsigned(value.changes.length);
    for (const item0 of value.changes) {
        encodeRewriteChange(writer, item0);
    }
    writer.writeUnsigned(value.replacements);
    writer.writeOption(value.commit, (value2) => {
        encodeCommit(writer, value2);
    });
}

/** Decode one RewritePayload. */
export function decodeRewritePayload(reader: BinaryReader): RewritePayload {
    const changes = (() => { const length0 = reader.readNumber(); const items0: Array<RewriteChange> = []; for (let index = 0; index < length0; index += 1) { items0.push(decodeRewriteChange(reader)); } return items0; })();
    const replacements = reader.readNumber();
    const commit = reader.readOption(() => decodeCommit(reader));

    return {
        changes,
        replacements,
        ...(commit === undefined ? {} : { commit }),
    };
}

/** Return one JSON value for one RewritePayload. */
export function toJsonRewritePayload(value: RewritePayload): Json {
    return {
        changes: value.changes.map((item0) => toJsonRewriteChange(item0)),
        replacements: value.replacements,
        ...(value.commit === undefined ? {} : { commit: toJsonCommit(value.commit) }),
    };
}

/** Return one RewritePayload from one JSON value. */
export function fromJsonRewritePayload(value: Json): RewritePayload {
    const object = jsonObject(value);

    return {
        changes: jsonArray(jsonField(object, "changes")).map((item0) => fromJsonRewriteChange(item0)),
        replacements: jsonInteger(jsonField(object, "replacements")),
        commit: jsonOptional(object, "commit", (value) => fromJsonCommit(value)),
    };
}
