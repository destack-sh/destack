/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, jsonField, jsonObject, jsonString } from "../../../protocol/serde.js";

/** Selection for one diagnostic read. */
export type DiagnosticsRequest =
    /** Return diagnostics for this workspace. */
    | {
          readonly kind: "all";
      }
    /** Return diagnostics for one file. */
    | {
          readonly kind: "file";
          readonly file: string;
      }
;

export const DiagnosticsRequest = {
    /** Return diagnostics for this workspace. */
    all(): DiagnosticsRequest {
        return { kind: "all" };
    },

    /** Return diagnostics for one file. */
    file(file: string): DiagnosticsRequest {
        return { kind: "file", file };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: DiagnosticsRequest): void {
        encodeDiagnosticsRequest(writer, value);
    },

    /** Decode one DiagnosticsRequest. */
    decode(reader: BinaryReader): DiagnosticsRequest {
        return decodeDiagnosticsRequest(reader);
    },

    /** Return this value as JSON. */
    toJson(value: DiagnosticsRequest): Json {
        return toJsonDiagnosticsRequest(value);
    },

    /** Return one DiagnosticsRequest from one JSON value. */
    fromJson(value: Json): DiagnosticsRequest {
        return fromJsonDiagnosticsRequest(value);
    },
};

/** Encode one DiagnosticsRequest. */
export function encodeDiagnosticsRequest(writer: BinaryWriter, value: DiagnosticsRequest): void {
    switch (value.kind) {
        case "all":
            writer.writeUnsigned(0);
            return;
        case "file":
            writer.writeUnsigned(1);
            writer.writeString(value.file);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one DiagnosticsRequest. */
export function decodeDiagnosticsRequest(reader: BinaryReader): DiagnosticsRequest {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            return { kind: "all" };
        }
        case 1: {
            const file = reader.readString();

            return { kind: "file", file };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one DiagnosticsRequest. */
export function toJsonDiagnosticsRequest(value: DiagnosticsRequest): Json {
    switch (value.kind) {
        case "all":
            return {
                kind: "all",
            };
        case "file":
            return {
                kind: "file",
                file: value.file,
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one DiagnosticsRequest from one JSON value. */
export function fromJsonDiagnosticsRequest(value: Json): DiagnosticsRequest {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "all":
            return {
                kind,
            };
        case "file":
            return {
                kind,
                file: jsonString(jsonField(object, "file")),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}
