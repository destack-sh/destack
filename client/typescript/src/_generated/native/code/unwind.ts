/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, jsonField, jsonObject, jsonString } from "../../../protocol/serde.js";
import type { SectionSlice } from "../../core/section.js";
import { decodeSectionSlice, encodeSectionSlice, fromJsonSectionSlice, toJsonSectionSlice } from "../../core/section.js";

/** Fully linked target-native unwind tables inside one Program. */
export type Unwind = {
    /** Platform unwind encoding. */
    readonly format: UnwindFormat;
    /** Target unwind sections inside the native load image. */
    readonly sections: SectionSlice;
};

export const Unwind = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: Unwind): void {
        encodeUnwind(writer, value);
    },

    /** Decode one Unwind. */
    decode(reader: BinaryReader): Unwind {
        return decodeUnwind(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Unwind): Json {
        return toJsonUnwind(value);
    },

    /** Return one Unwind from one JSON value. */
    fromJson(value: Json): Unwind {
        return fromJsonUnwind(value);
    },
};

/** Encode one Unwind. */
export function encodeUnwind(writer: BinaryWriter, value: Unwind): void {
    encodeUnwindFormat(writer, value.format);
    encodeSectionSlice(writer, value.sections);
}

/** Decode one Unwind. */
export function decodeUnwind(reader: BinaryReader): Unwind {
    const format = decodeUnwindFormat(reader);
    const sections = decodeSectionSlice(reader);

    return {
        format,
        sections,
    };
}

/** Return one JSON value for one Unwind. */
export function toJsonUnwind(value: Unwind): Json {
    return {
        format: toJsonUnwindFormat(value.format),
        sections: toJsonSectionSlice(value.sections),
    };
}

/** Return one Unwind from one JSON value. */
export function fromJsonUnwind(value: Json): Unwind {
    const object = jsonObject(value);

    return {
        format: fromJsonUnwindFormat(jsonField(object, "format")),
        sections: fromJsonSectionSlice(jsonField(object, "sections")),
    };
}

/** Platform unwind encoding. */
export type UnwindFormat = "dwarf" | "windowsX64" | "windowsArm64";

export const UnwindFormat = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: UnwindFormat): void {
        encodeUnwindFormat(writer, value);
    },

    /** Decode one UnwindFormat. */
    decode(reader: BinaryReader): UnwindFormat {
        return decodeUnwindFormat(reader);
    },

    /** Return this value as JSON. */
    toJson(value: UnwindFormat): Json {
        return toJsonUnwindFormat(value);
    },

    /** Return one UnwindFormat from one JSON value. */
    fromJson(value: Json): UnwindFormat {
        return fromJsonUnwindFormat(value);
    },
};

/** Encode one UnwindFormat. */
export function encodeUnwindFormat(writer: BinaryWriter, value: UnwindFormat): void {
    switch (value) {
        case "dwarf":
            writer.writeUnsigned(0);
            return;
        case "windowsX64":
            writer.writeUnsigned(1);
            return;
        case "windowsArm64":
            writer.writeUnsigned(2);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one UnwindFormat. */
export function decodeUnwindFormat(reader: BinaryReader): UnwindFormat {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "dwarf";
        case 1:
            return "windowsX64";
        case 2:
            return "windowsArm64";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one UnwindFormat. */
export function toJsonUnwindFormat(value: UnwindFormat): Json {
    return value;
}

/** Return one UnwindFormat from one JSON value. */
export function fromJsonUnwindFormat(value: Json): UnwindFormat {
    const variant = jsonString(value);

    switch (variant) {
        case "dwarf":
            return "dwarf";
        case "windowsX64":
            return "windowsX64";
        case "windowsArm64":
            return "windowsArm64";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}
