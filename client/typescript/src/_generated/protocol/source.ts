/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, jsonField, jsonObject } from "../../protocol/serde.js";
import type { Patch } from "../source/edit/edit.js";
import type { FileImage } from "./workspace/file/image.js";
import { decodePatch, encodePatch, fromJsonPatch, toJsonPatch } from "../source/edit/edit.js";
import { decodeFileImage, encodeFileImage, fromJsonFileImage, toJsonFileImage } from "./workspace/file/image.js";

/** Wire representation of one patch over an exact source file. */
export type FileEditPayload = {
    /** The exact source file image interpreted by the patch. */
    readonly file: FileImage;
    /** The source replacement. */
    readonly patch: Patch;
};

export const FileEditPayload = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: FileEditPayload): void {
        encodeFileEditPayload(writer, value);
    },

    /** Decode one FileEditPayload. */
    decode(reader: BinaryReader): FileEditPayload {
        return decodeFileEditPayload(reader);
    },

    /** Return this value as JSON. */
    toJson(value: FileEditPayload): Json {
        return toJsonFileEditPayload(value);
    },

    /** Return one FileEditPayload from one JSON value. */
    fromJson(value: Json): FileEditPayload {
        return fromJsonFileEditPayload(value);
    },
};

/** Encode one FileEditPayload. */
export function encodeFileEditPayload(writer: BinaryWriter, value: FileEditPayload): void {
    encodeFileImage(writer, value.file);
    encodePatch(writer, value.patch);
}

/** Decode one FileEditPayload. */
export function decodeFileEditPayload(reader: BinaryReader): FileEditPayload {
    const file = decodeFileImage(reader);
    const patch = decodePatch(reader);

    return {
        file,
        patch,
    };
}

/** Return one JSON value for one FileEditPayload. */
export function toJsonFileEditPayload(value: FileEditPayload): Json {
    return {
        file: toJsonFileImage(value.file),
        patch: toJsonPatch(value.patch),
    };
}

/** Return one FileEditPayload from one JSON value. */
export function fromJsonFileEditPayload(value: Json): FileEditPayload {
    const object = jsonObject(value);

    return {
        file: fromJsonFileImage(jsonField(object, "file")),
        patch: fromJsonPatch(jsonField(object, "patch")),
    };
}
