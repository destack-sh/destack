/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, jsonArray, jsonBigint, jsonBool, jsonField, jsonObject, jsonString } from "../../protocol/serde.js";
import type { FileOperation } from "./workspace/file/image.js";
import type { Commit } from "./workspace/file/update.js";
import type { SourceUpdate } from "./workspace/file/update.js";
import type { Message } from "./workspace/message.js";
import type { UpdateBatch } from "./workspace/message.js";
import type { DiagnosticBatch } from "./notification.js";
import type { ReloadReason } from "./workspace/root.js";
import { decodeFileOperation, encodeFileOperation, fromJsonFileOperation, toJsonFileOperation } from "./workspace/file/image.js";
import { decodeCommit, encodeCommit, fromJsonCommit, toJsonCommit } from "./workspace/file/update.js";
import { decodeSourceUpdate, encodeSourceUpdate, fromJsonSourceUpdate, toJsonSourceUpdate } from "./workspace/file/update.js";
import { decodeMessage, encodeMessage, fromJsonMessage, toJsonMessage } from "./workspace/message.js";
import { decodeUpdateBatch, encodeUpdateBatch, fromJsonUpdateBatch, toJsonUpdateBatch } from "./workspace/message.js";
import { decodeDiagnosticBatch, encodeDiagnosticBatch, fromJsonDiagnosticBatch, toJsonDiagnosticBatch } from "./notification.js";
import { decodeReloadReason, encodeReloadReason, fromJsonReloadReason, toJsonReloadReason } from "./workspace/root.js";

/** Request to open a root. */
export type OpenRootRequest = {
    /** The root path. */
    readonly root: string;
    /** Root open options. */
    readonly options: RootOpenOptions;
};

export const OpenRootRequest = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: OpenRootRequest): void {
        encodeOpenRootRequest(writer, value);
    },

    /** Decode one OpenRootRequest. */
    decode(reader: BinaryReader): OpenRootRequest {
        return decodeOpenRootRequest(reader);
    },

    /** Return this value as JSON. */
    toJson(value: OpenRootRequest): Json {
        return toJsonOpenRootRequest(value);
    },

    /** Return one OpenRootRequest from one JSON value. */
    fromJson(value: Json): OpenRootRequest {
        return fromJsonOpenRootRequest(value);
    },
};

/** Encode one OpenRootRequest. */
export function encodeOpenRootRequest(writer: BinaryWriter, value: OpenRootRequest): void {
    writer.writeString(value.root);
    encodeRootOpenOptions(writer, value.options);
}

/** Decode one OpenRootRequest. */
export function decodeOpenRootRequest(reader: BinaryReader): OpenRootRequest {
    const root = reader.readString();
    const options = decodeRootOpenOptions(reader);

    return {
        root,
        options,
    };
}

/** Return one JSON value for one OpenRootRequest. */
export function toJsonOpenRootRequest(value: OpenRootRequest): Json {
    return {
        root: value.root,
        options: toJsonRootOpenOptions(value.options),
    };
}

/** Return one OpenRootRequest from one JSON value. */
export function fromJsonOpenRootRequest(value: Json): OpenRootRequest {
    const object = jsonObject(value);

    return {
        root: jsonString(jsonField(object, "root")),
        options: fromJsonRootOpenOptions(jsonField(object, "options")),
    };
}

/** Options for opening a root. */
export type RootOpenOptions = {
    /** Whether to preload root state. */
    readonly loadIndex: boolean;
};

export const RootOpenOptions = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: RootOpenOptions): void {
        encodeRootOpenOptions(writer, value);
    },

    /** Decode one RootOpenOptions. */
    decode(reader: BinaryReader): RootOpenOptions {
        return decodeRootOpenOptions(reader);
    },

    /** Return this value as JSON. */
    toJson(value: RootOpenOptions): Json {
        return toJsonRootOpenOptions(value);
    },

    /** Return one RootOpenOptions from one JSON value. */
    fromJson(value: Json): RootOpenOptions {
        return fromJsonRootOpenOptions(value);
    },
};

/** Encode one RootOpenOptions. */
export function encodeRootOpenOptions(writer: BinaryWriter, value: RootOpenOptions): void {
    writer.writeBool(value.loadIndex);
}

/** Decode one RootOpenOptions. */
export function decodeRootOpenOptions(reader: BinaryReader): RootOpenOptions {
    const loadIndex = reader.readBool();

    return {
        loadIndex,
    };
}

/** Return one JSON value for one RootOpenOptions. */
export function toJsonRootOpenOptions(value: RootOpenOptions): Json {
    return {
        loadIndex: value.loadIndex,
    };
}

/** Return one RootOpenOptions from one JSON value. */
export function fromJsonRootOpenOptions(value: Json): RootOpenOptions {
    const object = jsonObject(value);

    return {
        loadIndex: jsonBool(jsonField(object, "loadIndex")),
    };
}

/** Request to close a root handle. */
export type CloseRootRequest = {
    /** Handle to close. */
    readonly handle: RootId;
};

export const CloseRootRequest = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: CloseRootRequest): void {
        encodeCloseRootRequest(writer, value);
    },

    /** Decode one CloseRootRequest. */
    decode(reader: BinaryReader): CloseRootRequest {
        return decodeCloseRootRequest(reader);
    },

    /** Return this value as JSON. */
    toJson(value: CloseRootRequest): Json {
        return toJsonCloseRootRequest(value);
    },

    /** Return one CloseRootRequest from one JSON value. */
    fromJson(value: Json): CloseRootRequest {
        return fromJsonCloseRootRequest(value);
    },
};

/** Encode one CloseRootRequest. */
export function encodeCloseRootRequest(writer: BinaryWriter, value: CloseRootRequest): void {
    encodeRootId(writer, value.handle);
}

/** Decode one CloseRootRequest. */
export function decodeCloseRootRequest(reader: BinaryReader): CloseRootRequest {
    const handle = decodeRootId(reader);

    return {
        handle,
    };
}

/** Return one JSON value for one CloseRootRequest. */
export function toJsonCloseRootRequest(value: CloseRootRequest): Json {
    return {
        handle: toJsonRootId(value.handle),
    };
}

/** Return one CloseRootRequest from one JSON value. */
export function fromJsonCloseRootRequest(value: Json): CloseRootRequest {
    const object = jsonObject(value);

    return {
        handle: fromJsonRootId(jsonField(object, "handle")),
    };
}

/** Unique identifier for an opened root. */
export type RootId = bigint;

export const RootId = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: RootId): void {
        encodeRootId(writer, value);
    },

    /** Decode one RootId. */
    decode(reader: BinaryReader): RootId {
        return decodeRootId(reader);
    },

    /** Return this value as JSON. */
    toJson(value: RootId): Json {
        return toJsonRootId(value);
    },

    /** Return one RootId from one JSON value. */
    fromJson(value: Json): RootId {
        return fromJsonRootId(value);
    },
};

/** Encode one RootId. */
export function encodeRootId(writer: BinaryWriter, value: RootId): void {
    writer.writeUnsigned(value);
}

/** Decode one RootId. */
export function decodeRootId(reader: BinaryReader): RootId {
    return reader.readUnsigned();
}

/** Return one JSON value for one RootId. */
export function toJsonRootId(value: RootId): Json {
    return value.toString();
}

/** Return one RootId from one JSON value. */
export function fromJsonRootId(value: Json): RootId {
    return jsonBigint(value);
}

/** Request to reload a root. */
export type ReloadRootRequest = {
    /** Root handle. */
    readonly handle: RootId;
    /** Reason for the reload. */
    readonly reason: ReloadReason;
};

export const ReloadRootRequest = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ReloadRootRequest): void {
        encodeReloadRootRequest(writer, value);
    },

    /** Decode one ReloadRootRequest. */
    decode(reader: BinaryReader): ReloadRootRequest {
        return decodeReloadRootRequest(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ReloadRootRequest): Json {
        return toJsonReloadRootRequest(value);
    },

    /** Return one ReloadRootRequest from one JSON value. */
    fromJson(value: Json): ReloadRootRequest {
        return fromJsonReloadRootRequest(value);
    },
};

/** Encode one ReloadRootRequest. */
export function encodeReloadRootRequest(writer: BinaryWriter, value: ReloadRootRequest): void {
    encodeRootId(writer, value.handle);
    encodeReloadReason(writer, value.reason);
}

/** Decode one ReloadRootRequest. */
export function decodeReloadRootRequest(reader: BinaryReader): ReloadRootRequest {
    const handle = decodeRootId(reader);
    const reason = decodeReloadReason(reader);

    return {
        handle,
        reason,
    };
}

/** Return one JSON value for one ReloadRootRequest. */
export function toJsonReloadRootRequest(value: ReloadRootRequest): Json {
    return {
        handle: toJsonRootId(value.handle),
        reason: toJsonReloadReason(value.reason),
    };
}

/** Return one ReloadRootRequest from one JSON value. */
export function fromJsonReloadRootRequest(value: Json): ReloadRootRequest {
    const object = jsonObject(value);

    return {
        handle: fromJsonRootId(jsonField(object, "handle")),
        reason: fromJsonReloadReason(jsonField(object, "reason")),
    };
}

/** Request to apply a file operation. */
export type FileOperationRequest = {
    /** Root handle. */
    readonly handle: RootId;
    /** Operation payload. */
    readonly operation: FileOperation;
};

export const FileOperationRequest = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: FileOperationRequest): void {
        encodeFileOperationRequest(writer, value);
    },

    /** Decode one FileOperationRequest. */
    decode(reader: BinaryReader): FileOperationRequest {
        return decodeFileOperationRequest(reader);
    },

    /** Return this value as JSON. */
    toJson(value: FileOperationRequest): Json {
        return toJsonFileOperationRequest(value);
    },

    /** Return one FileOperationRequest from one JSON value. */
    fromJson(value: Json): FileOperationRequest {
        return fromJsonFileOperationRequest(value);
    },
};

/** Encode one FileOperationRequest. */
export function encodeFileOperationRequest(writer: BinaryWriter, value: FileOperationRequest): void {
    encodeRootId(writer, value.handle);
    encodeFileOperation(writer, value.operation);
}

/** Decode one FileOperationRequest. */
export function decodeFileOperationRequest(reader: BinaryReader): FileOperationRequest {
    const handle = decodeRootId(reader);
    const operation = decodeFileOperation(reader);

    return {
        handle,
        operation,
    };
}

/** Return one JSON value for one FileOperationRequest. */
export function toJsonFileOperationRequest(value: FileOperationRequest): Json {
    return {
        handle: toJsonRootId(value.handle),
        operation: toJsonFileOperation(value.operation),
    };
}

/** Return one FileOperationRequest from one JSON value. */
export function fromJsonFileOperationRequest(value: Json): FileOperationRequest {
    const object = jsonObject(value);

    return {
        handle: fromJsonRootId(jsonField(object, "handle")),
        operation: fromJsonFileOperation(jsonField(object, "operation")),
    };
}

/** Request to apply a source update. */
export type SourceUpdateRequest = {
    /** Root handle. */
    readonly handle: RootId;
    /** Source update payload. */
    readonly update: SourceUpdate;
};

export const SourceUpdateRequest = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: SourceUpdateRequest): void {
        encodeSourceUpdateRequest(writer, value);
    },

    /** Decode one SourceUpdateRequest. */
    decode(reader: BinaryReader): SourceUpdateRequest {
        return decodeSourceUpdateRequest(reader);
    },

    /** Return this value as JSON. */
    toJson(value: SourceUpdateRequest): Json {
        return toJsonSourceUpdateRequest(value);
    },

    /** Return one SourceUpdateRequest from one JSON value. */
    fromJson(value: Json): SourceUpdateRequest {
        return fromJsonSourceUpdateRequest(value);
    },
};

/** Encode one SourceUpdateRequest. */
export function encodeSourceUpdateRequest(writer: BinaryWriter, value: SourceUpdateRequest): void {
    encodeRootId(writer, value.handle);
    encodeSourceUpdate(writer, value.update);
}

/** Decode one SourceUpdateRequest. */
export function decodeSourceUpdateRequest(reader: BinaryReader): SourceUpdateRequest {
    const handle = decodeRootId(reader);
    const update = decodeSourceUpdate(reader);

    return {
        handle,
        update,
    };
}

/** Return one JSON value for one SourceUpdateRequest. */
export function toJsonSourceUpdateRequest(value: SourceUpdateRequest): Json {
    return {
        handle: toJsonRootId(value.handle),
        update: toJsonSourceUpdate(value.update),
    };
}

/** Return one SourceUpdateRequest from one JSON value. */
export function fromJsonSourceUpdateRequest(value: Json): SourceUpdateRequest {
    const object = jsonObject(value);

    return {
        handle: fromJsonRootId(jsonField(object, "handle")),
        update: fromJsonSourceUpdate(jsonField(object, "update")),
    };
}

/** Response to opening a root. */
export type RootOpenedResponse = {
    /** Assigned root handle id. */
    readonly handle: RootId;
    /** Canonical root path opened by the server. */
    readonly root: string;
    /** Diagnostics produced during initialization. */
    readonly diagnostics: ReadonlyArray<DiagnosticBatch>;
    /** Messages produced during initialization. */
    readonly messages: ReadonlyArray<Message>;
};

export const RootOpenedResponse = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: RootOpenedResponse): void {
        encodeRootOpenedResponse(writer, value);
    },

    /** Decode one RootOpenedResponse. */
    decode(reader: BinaryReader): RootOpenedResponse {
        return decodeRootOpenedResponse(reader);
    },

    /** Return this value as JSON. */
    toJson(value: RootOpenedResponse): Json {
        return toJsonRootOpenedResponse(value);
    },

    /** Return one RootOpenedResponse from one JSON value. */
    fromJson(value: Json): RootOpenedResponse {
        return fromJsonRootOpenedResponse(value);
    },
};

/** Encode one RootOpenedResponse. */
export function encodeRootOpenedResponse(writer: BinaryWriter, value: RootOpenedResponse): void {
    encodeRootId(writer, value.handle);
    writer.writeString(value.root);
    writer.writeUnsigned(value.diagnostics.length);
    for (const item2 of value.diagnostics) {
        encodeDiagnosticBatch(writer, item2);
    }
    writer.writeUnsigned(value.messages.length);
    for (const item3 of value.messages) {
        encodeMessage(writer, item3);
    }
}

/** Decode one RootOpenedResponse. */
export function decodeRootOpenedResponse(reader: BinaryReader): RootOpenedResponse {
    const handle = decodeRootId(reader);
    const root = reader.readString();
    const diagnostics = (() => { const length2 = reader.readNumber(); const items2: Array<DiagnosticBatch> = []; for (let index = 0; index < length2; index += 1) { items2.push(decodeDiagnosticBatch(reader)); } return items2; })();
    const messages = (() => { const length3 = reader.readNumber(); const items3: Array<Message> = []; for (let index = 0; index < length3; index += 1) { items3.push(decodeMessage(reader)); } return items3; })();

    return {
        handle,
        root,
        diagnostics,
        messages,
    };
}

/** Return one JSON value for one RootOpenedResponse. */
export function toJsonRootOpenedResponse(value: RootOpenedResponse): Json {
    return {
        handle: toJsonRootId(value.handle),
        root: value.root,
        diagnostics: value.diagnostics.map((item0) => toJsonDiagnosticBatch(item0)),
        messages: value.messages.map((item0) => toJsonMessage(item0)),
    };
}

/** Return one RootOpenedResponse from one JSON value. */
export function fromJsonRootOpenedResponse(value: Json): RootOpenedResponse {
    const object = jsonObject(value);

    return {
        handle: fromJsonRootId(jsonField(object, "handle")),
        root: jsonString(jsonField(object, "root")),
        diagnostics: jsonArray(jsonField(object, "diagnostics")).map((item0) => fromJsonDiagnosticBatch(item0)),
        messages: jsonArray(jsonField(object, "messages")).map((item0) => fromJsonMessage(item0)),
    };
}

/** Response to closing a root. */
export type RootClosedResponse = {
    /** Closed handle id. */
    readonly handle: RootId;
};

export const RootClosedResponse = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: RootClosedResponse): void {
        encodeRootClosedResponse(writer, value);
    },

    /** Decode one RootClosedResponse. */
    decode(reader: BinaryReader): RootClosedResponse {
        return decodeRootClosedResponse(reader);
    },

    /** Return this value as JSON. */
    toJson(value: RootClosedResponse): Json {
        return toJsonRootClosedResponse(value);
    },

    /** Return one RootClosedResponse from one JSON value. */
    fromJson(value: Json): RootClosedResponse {
        return fromJsonRootClosedResponse(value);
    },
};

/** Encode one RootClosedResponse. */
export function encodeRootClosedResponse(writer: BinaryWriter, value: RootClosedResponse): void {
    encodeRootId(writer, value.handle);
}

/** Decode one RootClosedResponse. */
export function decodeRootClosedResponse(reader: BinaryReader): RootClosedResponse {
    const handle = decodeRootId(reader);

    return {
        handle,
    };
}

/** Return one JSON value for one RootClosedResponse. */
export function toJsonRootClosedResponse(value: RootClosedResponse): Json {
    return {
        handle: toJsonRootId(value.handle),
    };
}

/** Return one RootClosedResponse from one JSON value. */
export function fromJsonRootClosedResponse(value: Json): RootClosedResponse {
    const object = jsonObject(value);

    return {
        handle: fromJsonRootId(jsonField(object, "handle")),
    };
}

/** Response to root reloads. */
export type RootReloadResponse = {
    /** Root handle. */
    readonly handle: RootId;
    /** Updates produced during reload. */
    readonly updates: UpdateBatch;
};

export const RootReloadResponse = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: RootReloadResponse): void {
        encodeRootReloadResponse(writer, value);
    },

    /** Decode one RootReloadResponse. */
    decode(reader: BinaryReader): RootReloadResponse {
        return decodeRootReloadResponse(reader);
    },

    /** Return this value as JSON. */
    toJson(value: RootReloadResponse): Json {
        return toJsonRootReloadResponse(value);
    },

    /** Return one RootReloadResponse from one JSON value. */
    fromJson(value: Json): RootReloadResponse {
        return fromJsonRootReloadResponse(value);
    },
};

/** Encode one RootReloadResponse. */
export function encodeRootReloadResponse(writer: BinaryWriter, value: RootReloadResponse): void {
    encodeRootId(writer, value.handle);
    encodeUpdateBatch(writer, value.updates);
}

/** Decode one RootReloadResponse. */
export function decodeRootReloadResponse(reader: BinaryReader): RootReloadResponse {
    const handle = decodeRootId(reader);
    const updates = decodeUpdateBatch(reader);

    return {
        handle,
        updates,
    };
}

/** Return one JSON value for one RootReloadResponse. */
export function toJsonRootReloadResponse(value: RootReloadResponse): Json {
    return {
        handle: toJsonRootId(value.handle),
        updates: toJsonUpdateBatch(value.updates),
    };
}

/** Return one RootReloadResponse from one JSON value. */
export function fromJsonRootReloadResponse(value: Json): RootReloadResponse {
    const object = jsonObject(value);

    return {
        handle: fromJsonRootId(jsonField(object, "handle")),
        updates: fromJsonUpdateBatch(jsonField(object, "updates")),
    };
}

/** Response to a file operation. */
export type FileOperationResponse = {
    /** Root handle. */
    readonly handle: RootId;
    /** Updates produced by the change. */
    readonly updates: UpdateBatch;
};

export const FileOperationResponse = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: FileOperationResponse): void {
        encodeFileOperationResponse(writer, value);
    },

    /** Decode one FileOperationResponse. */
    decode(reader: BinaryReader): FileOperationResponse {
        return decodeFileOperationResponse(reader);
    },

    /** Return this value as JSON. */
    toJson(value: FileOperationResponse): Json {
        return toJsonFileOperationResponse(value);
    },

    /** Return one FileOperationResponse from one JSON value. */
    fromJson(value: Json): FileOperationResponse {
        return fromJsonFileOperationResponse(value);
    },
};

/** Encode one FileOperationResponse. */
export function encodeFileOperationResponse(writer: BinaryWriter, value: FileOperationResponse): void {
    encodeRootId(writer, value.handle);
    encodeUpdateBatch(writer, value.updates);
}

/** Decode one FileOperationResponse. */
export function decodeFileOperationResponse(reader: BinaryReader): FileOperationResponse {
    const handle = decodeRootId(reader);
    const updates = decodeUpdateBatch(reader);

    return {
        handle,
        updates,
    };
}

/** Return one JSON value for one FileOperationResponse. */
export function toJsonFileOperationResponse(value: FileOperationResponse): Json {
    return {
        handle: toJsonRootId(value.handle),
        updates: toJsonUpdateBatch(value.updates),
    };
}

/** Return one FileOperationResponse from one JSON value. */
export function fromJsonFileOperationResponse(value: Json): FileOperationResponse {
    const object = jsonObject(value);

    return {
        handle: fromJsonRootId(jsonField(object, "handle")),
        updates: fromJsonUpdateBatch(jsonField(object, "updates")),
    };
}

/** Response to a source update. */
export type SourceUpdateResponse = {
    /** Root handle. */
    readonly handle: RootId;
    /** Commit produced by the change. */
    readonly commit: Commit;
};

export const SourceUpdateResponse = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: SourceUpdateResponse): void {
        encodeSourceUpdateResponse(writer, value);
    },

    /** Decode one SourceUpdateResponse. */
    decode(reader: BinaryReader): SourceUpdateResponse {
        return decodeSourceUpdateResponse(reader);
    },

    /** Return this value as JSON. */
    toJson(value: SourceUpdateResponse): Json {
        return toJsonSourceUpdateResponse(value);
    },

    /** Return one SourceUpdateResponse from one JSON value. */
    fromJson(value: Json): SourceUpdateResponse {
        return fromJsonSourceUpdateResponse(value);
    },
};

/** Encode one SourceUpdateResponse. */
export function encodeSourceUpdateResponse(writer: BinaryWriter, value: SourceUpdateResponse): void {
    encodeRootId(writer, value.handle);
    encodeCommit(writer, value.commit);
}

/** Decode one SourceUpdateResponse. */
export function decodeSourceUpdateResponse(reader: BinaryReader): SourceUpdateResponse {
    const handle = decodeRootId(reader);
    const commit = decodeCommit(reader);

    return {
        handle,
        commit,
    };
}

/** Return one JSON value for one SourceUpdateResponse. */
export function toJsonSourceUpdateResponse(value: SourceUpdateResponse): Json {
    return {
        handle: toJsonRootId(value.handle),
        commit: toJsonCommit(value.commit),
    };
}

/** Return one SourceUpdateResponse from one JSON value. */
export function fromJsonSourceUpdateResponse(value: Json): SourceUpdateResponse {
    const object = jsonObject(value);

    return {
        handle: fromJsonRootId(jsonField(object, "handle")),
        commit: fromJsonCommit(jsonField(object, "commit")),
    };
}
