/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, jsonArray, jsonBigint, jsonBool, jsonField, jsonObject, jsonString } from "../../../../protocol/serde.js";
import type { ArtifactReference } from "../../../artifact/reference.js";
import type { ContentId } from "../../../source/file/model/file.js";
import { decodeArtifactReference, encodeArtifactReference, fromJsonArtifactReference, toJsonArtifactReference } from "../../../artifact/reference.js";
import { decodeContentId, encodeContentId, fromJsonContentId, toJsonContentId } from "../../../source/file/model/file.js";

/** Request to materialize derived outputs on the workspace host. */
export type ExportRequest = {
    /** Artifact to export. */
    readonly artifact: ArtifactReference;
    /** Output directory on the workspace host. */
    readonly directory: string;
    /** Whether existing files may be overwritten. */
    readonly overwrite: boolean;
};

export const ExportRequest = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ExportRequest): void {
        encodeExportRequest(writer, value);
    },

    /** Decode one ExportRequest. */
    decode(reader: BinaryReader): ExportRequest {
        return decodeExportRequest(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ExportRequest): Json {
        return toJsonExportRequest(value);
    },

    /** Return one ExportRequest from one JSON value. */
    fromJson(value: Json): ExportRequest {
        return fromJsonExportRequest(value);
    },
};

/** Encode one ExportRequest. */
export function encodeExportRequest(writer: BinaryWriter, value: ExportRequest): void {
    encodeArtifactReference(writer, value.artifact);
    writer.writeString(value.directory);
    writer.writeBool(value.overwrite);
}

/** Decode one ExportRequest. */
export function decodeExportRequest(reader: BinaryReader): ExportRequest {
    const artifact = decodeArtifactReference(reader);
    const directory = reader.readString();
    const overwrite = reader.readBool();

    return {
        artifact,
        directory,
        overwrite,
    };
}

/** Return one JSON value for one ExportRequest. */
export function toJsonExportRequest(value: ExportRequest): Json {
    return {
        artifact: toJsonArtifactReference(value.artifact),
        directory: value.directory,
        overwrite: value.overwrite,
    };
}

/** Return one ExportRequest from one JSON value. */
export function fromJsonExportRequest(value: Json): ExportRequest {
    const object = jsonObject(value);

    return {
        artifact: fromJsonArtifactReference(jsonField(object, "artifact")),
        directory: jsonString(jsonField(object, "directory")),
        overwrite: jsonBool(jsonField(object, "overwrite")),
    };
}

/** Result of materializing derived outputs. */
export type ExportResult = {
    /** Files written on the workspace host. */
    readonly files: ReadonlyArray<ExportedFile>;
};

export const ExportResult = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ExportResult): void {
        encodeExportResult(writer, value);
    },

    /** Decode one ExportResult. */
    decode(reader: BinaryReader): ExportResult {
        return decodeExportResult(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ExportResult): Json {
        return toJsonExportResult(value);
    },

    /** Return one ExportResult from one JSON value. */
    fromJson(value: Json): ExportResult {
        return fromJsonExportResult(value);
    },
};

/** Encode one ExportResult. */
export function encodeExportResult(writer: BinaryWriter, value: ExportResult): void {
    writer.writeUnsigned(value.files.length);
    for (const item0 of value.files) {
        encodeExportedFile(writer, item0);
    }
}

/** Decode one ExportResult. */
export function decodeExportResult(reader: BinaryReader): ExportResult {
    const files = (() => { const length0 = reader.readNumber(); const items0: Array<ExportedFile> = []; for (let index = 0; index < length0; index += 1) { items0.push(decodeExportedFile(reader)); } return items0; })();

    return {
        files,
    };
}

/** Return one JSON value for one ExportResult. */
export function toJsonExportResult(value: ExportResult): Json {
    return {
        files: value.files.map((item0) => toJsonExportedFile(item0)),
    };
}

/** Return one ExportResult from one JSON value. */
export function fromJsonExportResult(value: Json): ExportResult {
    const object = jsonObject(value);

    return {
        files: jsonArray(jsonField(object, "files")).map((item0) => fromJsonExportedFile(item0)),
    };
}

/** One file written by export. */
export type ExportedFile = {
    /** Path written on the workspace host. */
    readonly path: string;
    /** Content written to the path. */
    readonly content: ContentId;
    /** Number of bytes written. */
    readonly sizeBytes: bigint;
};

export const ExportedFile = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ExportedFile): void {
        encodeExportedFile(writer, value);
    },

    /** Decode one ExportedFile. */
    decode(reader: BinaryReader): ExportedFile {
        return decodeExportedFile(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ExportedFile): Json {
        return toJsonExportedFile(value);
    },

    /** Return one ExportedFile from one JSON value. */
    fromJson(value: Json): ExportedFile {
        return fromJsonExportedFile(value);
    },
};

/** Encode one ExportedFile. */
export function encodeExportedFile(writer: BinaryWriter, value: ExportedFile): void {
    writer.writeString(value.path);
    encodeContentId(writer, value.content);
    writer.writeUnsigned(value.sizeBytes);
}

/** Decode one ExportedFile. */
export function decodeExportedFile(reader: BinaryReader): ExportedFile {
    const path = reader.readString();
    const content = decodeContentId(reader);
    const sizeBytes = reader.readUnsigned();

    return {
        path,
        content,
        sizeBytes,
    };
}

/** Return one JSON value for one ExportedFile. */
export function toJsonExportedFile(value: ExportedFile): Json {
    return {
        path: value.path,
        content: toJsonContentId(value.content),
        sizeBytes: value.sizeBytes.toString(),
    };
}

/** Return one ExportedFile from one JSON value. */
export function fromJsonExportedFile(value: Json): ExportedFile {
    const object = jsonObject(value);

    return {
        path: jsonString(jsonField(object, "path")),
        content: fromJsonContentId(jsonField(object, "content")),
        sizeBytes: jsonBigint(jsonField(object, "sizeBytes")),
    };
}
