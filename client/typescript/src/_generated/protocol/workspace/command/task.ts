/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, jsonArray, jsonBool, jsonField, jsonInteger, jsonObject, jsonOptional, jsonString } from "../../../../protocol/serde.js";
import type { CommandEnvVar } from "./common.js";
import type { CommandInput } from "./common.js";
import type { CommandRevision } from "./common.js";
import type { CommandTargetOverrides } from "./common.js";
import type { ManifestOverride } from "./common.js";
import { decodeCommandEnvVar, encodeCommandEnvVar, fromJsonCommandEnvVar, toJsonCommandEnvVar } from "./common.js";
import { decodeCommandInput, encodeCommandInput, fromJsonCommandInput, toJsonCommandInput } from "./common.js";
import { decodeCommandRevision, encodeCommandRevision, fromJsonCommandRevision, toJsonCommandRevision } from "./common.js";
import { decodeCommandTargetOverrides, encodeCommandTargetOverrides, fromJsonCommandTargetOverrides, toJsonCommandTargetOverrides } from "./common.js";
import { decodeManifestOverride, encodeManifestOverride, fromJsonManifestOverride, toJsonManifestOverride } from "./common.js";

/** Request to run workspace tasks. */
export type TaskInput = {
    /** Revision selected for this task request. */
    readonly revision: CommandRevision;
    /** Input sources for the command. */
    readonly inputs: ReadonlyArray<CommandInput>;
    /** Whether destack.json should resolve inputs when none are provided. */
    readonly configInputs: boolean;
    /** Optional working directory for this command. */
    readonly cwd?: string;
    /** Optional Destack manifest path override. */
    readonly manifest?: string;
    /** Optional target name override. */
    readonly target?: string;
    /** Optional target overrides. */
    readonly targetOverrides?: CommandTargetOverrides;
    /** Optional profile name override. */
    readonly profile?: string;
    /** Optional environment overrides. */
    readonly env: ReadonlyArray<CommandEnvVar>;
    /** Optional manifest overrides. */
    readonly overrides: ReadonlyArray<ManifestOverride>;
    /** Whether the command should watch for changes. */
    readonly watch: boolean;
    /** Whether the command should skip writes. */
    readonly dryRun: boolean;
    /** Task action to run. */
    readonly action: TaskAction;
    /** Selected projects. */
    readonly projects: ReadonlyArray<string>;
    /** Selected workspace groups. */
    readonly groups: ReadonlyArray<string>;
};

export const TaskInput = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: TaskInput): void {
        encodeTaskInput(writer, value);
    },

    /** Decode one TaskInput. */
    decode(reader: BinaryReader): TaskInput {
        return decodeTaskInput(reader);
    },

    /** Return this value as JSON. */
    toJson(value: TaskInput): Json {
        return toJsonTaskInput(value);
    },

    /** Return one TaskInput from one JSON value. */
    fromJson(value: Json): TaskInput {
        return fromJsonTaskInput(value);
    },
};

/** Encode one TaskInput. */
export function encodeTaskInput(writer: BinaryWriter, value: TaskInput): void {
    encodeCommandRevision(writer, value.revision);
    writer.writeUnsigned(value.inputs.length);
    for (const item1 of value.inputs) {
        encodeCommandInput(writer, item1);
    }
    writer.writeBool(value.configInputs);
    writer.writeOption(value.cwd, (value3) => {
        writer.writeString(value3);
    });
    writer.writeOption(value.manifest, (value4) => {
        writer.writeString(value4);
    });
    writer.writeOption(value.target, (value5) => {
        writer.writeString(value5);
    });
    writer.writeOption(value.targetOverrides, (value6) => {
        encodeCommandTargetOverrides(writer, value6);
    });
    writer.writeOption(value.profile, (value7) => {
        writer.writeString(value7);
    });
    writer.writeUnsigned(value.env.length);
    for (const item8 of value.env) {
        encodeCommandEnvVar(writer, item8);
    }
    writer.writeUnsigned(value.overrides.length);
    for (const item9 of value.overrides) {
        encodeManifestOverride(writer, item9);
    }
    writer.writeBool(value.watch);
    writer.writeBool(value.dryRun);
    encodeTaskAction(writer, value.action);
    writer.writeUnsigned(value.projects.length);
    for (const item13 of value.projects) {
        writer.writeString(item13);
    }
    writer.writeUnsigned(value.groups.length);
    for (const item14 of value.groups) {
        writer.writeString(item14);
    }
}

/** Decode one TaskInput. */
export function decodeTaskInput(reader: BinaryReader): TaskInput {
    const revision = decodeCommandRevision(reader);
    const inputs = (() => { const length1 = reader.readNumber(); const items1: Array<CommandInput> = []; for (let index = 0; index < length1; index += 1) { items1.push(decodeCommandInput(reader)); } return items1; })();
    const configInputs = reader.readBool();
    const cwd = reader.readOption(() => reader.readString());
    const manifest = reader.readOption(() => reader.readString());
    const target = reader.readOption(() => reader.readString());
    const targetOverrides = reader.readOption(() => decodeCommandTargetOverrides(reader));
    const profile = reader.readOption(() => reader.readString());
    const env = (() => { const length8 = reader.readNumber(); const items8: Array<CommandEnvVar> = []; for (let index = 0; index < length8; index += 1) { items8.push(decodeCommandEnvVar(reader)); } return items8; })();
    const overrides = (() => { const length9 = reader.readNumber(); const items9: Array<ManifestOverride> = []; for (let index = 0; index < length9; index += 1) { items9.push(decodeManifestOverride(reader)); } return items9; })();
    const watch = reader.readBool();
    const dryRun = reader.readBool();
    const action = decodeTaskAction(reader);
    const projects = (() => { const length13 = reader.readNumber(); const items13: Array<string> = []; for (let index = 0; index < length13; index += 1) { items13.push(reader.readString()); } return items13; })();
    const groups = (() => { const length14 = reader.readNumber(); const items14: Array<string> = []; for (let index = 0; index < length14; index += 1) { items14.push(reader.readString()); } return items14; })();

    return {
        revision,
        inputs,
        configInputs,
        ...(cwd === undefined ? {} : { cwd }),
        ...(manifest === undefined ? {} : { manifest }),
        ...(target === undefined ? {} : { target }),
        ...(targetOverrides === undefined ? {} : { targetOverrides }),
        ...(profile === undefined ? {} : { profile }),
        env,
        overrides,
        watch,
        dryRun,
        action,
        projects,
        groups,
    };
}

/** Return one JSON value for one TaskInput. */
export function toJsonTaskInput(value: TaskInput): Json {
    return {
        revision: toJsonCommandRevision(value.revision),
        inputs: value.inputs.map((item0) => toJsonCommandInput(item0)),
        configInputs: value.configInputs,
        ...(value.cwd === undefined ? {} : { cwd: value.cwd }),
        ...(value.manifest === undefined ? {} : { manifest: value.manifest }),
        ...(value.target === undefined ? {} : { target: value.target }),
        ...(value.targetOverrides === undefined ? {} : { targetOverrides: toJsonCommandTargetOverrides(value.targetOverrides) }),
        ...(value.profile === undefined ? {} : { profile: value.profile }),
        env: value.env.map((item0) => toJsonCommandEnvVar(item0)),
        overrides: value.overrides.map((item0) => toJsonManifestOverride(item0)),
        watch: value.watch,
        dryRun: value.dryRun,
        action: toJsonTaskAction(value.action),
        projects: value.projects.map((item0) => item0),
        groups: value.groups.map((item0) => item0),
    };
}

/** Return one TaskInput from one JSON value. */
export function fromJsonTaskInput(value: Json): TaskInput {
    const object = jsonObject(value);

    return {
        revision: fromJsonCommandRevision(jsonField(object, "revision")),
        inputs: jsonArray(jsonField(object, "inputs")).map((item0) => fromJsonCommandInput(item0)),
        configInputs: jsonBool(jsonField(object, "configInputs")),
        cwd: jsonOptional(object, "cwd", (value) => jsonString(value)),
        manifest: jsonOptional(object, "manifest", (value) => jsonString(value)),
        target: jsonOptional(object, "target", (value) => jsonString(value)),
        targetOverrides: jsonOptional(object, "targetOverrides", (value) => fromJsonCommandTargetOverrides(value)),
        profile: jsonOptional(object, "profile", (value) => jsonString(value)),
        env: jsonArray(jsonField(object, "env")).map((item0) => fromJsonCommandEnvVar(item0)),
        overrides: jsonArray(jsonField(object, "overrides")).map((item0) => fromJsonManifestOverride(item0)),
        watch: jsonBool(jsonField(object, "watch")),
        dryRun: jsonBool(jsonField(object, "dryRun")),
        action: fromJsonTaskAction(jsonField(object, "action")),
        projects: jsonArray(jsonField(object, "projects")).map((item0) => jsonString(item0)),
        groups: jsonArray(jsonField(object, "groups")).map((item0) => jsonString(item0)),
    };
}

/** Task selection for the task command. */
export type TaskAction =
    /** List available tasks. */
    | {
          readonly kind: "list";
      }
    /** Run a task by name. */
    | {
          readonly kind: "run";
          /** Task name. */
          readonly name: string;
          /** Task arguments. */
          readonly args: ReadonlyArray<string>;
          /** Whether to only print the command. */
          readonly dryRun: boolean;
      }
;

export const TaskAction = {
    /** List available tasks. */
    list(): TaskAction {
        return { kind: "list" };
    },

    /** Run a task by name. */
    run(name: string, args: ReadonlyArray<string>, dryRun: boolean): TaskAction {
        return { kind: "run", name, args, dryRun };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: TaskAction): void {
        encodeTaskAction(writer, value);
    },

    /** Decode one TaskAction. */
    decode(reader: BinaryReader): TaskAction {
        return decodeTaskAction(reader);
    },

    /** Return this value as JSON. */
    toJson(value: TaskAction): Json {
        return toJsonTaskAction(value);
    },

    /** Return one TaskAction from one JSON value. */
    fromJson(value: Json): TaskAction {
        return fromJsonTaskAction(value);
    },
};

/** Encode one TaskAction. */
export function encodeTaskAction(writer: BinaryWriter, value: TaskAction): void {
    switch (value.kind) {
        case "list":
            writer.writeUnsigned(0);
            return;
        case "run":
            writer.writeUnsigned(1);
            writer.writeString(value.name);
            writer.writeUnsigned(value.args.length);
            for (const item1 of value.args) {
                writer.writeString(item1);
            }
            writer.writeBool(value.dryRun);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one TaskAction. */
export function decodeTaskAction(reader: BinaryReader): TaskAction {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            return { kind: "list" };
        }
        case 1: {
            const name = reader.readString();
            const args = (() => { const length1 = reader.readNumber(); const items1: Array<string> = []; for (let index = 0; index < length1; index += 1) { items1.push(reader.readString()); } return items1; })();
            const dryRun = reader.readBool();

            return {
                kind: "run",
                name,
                args,
                dryRun,
            };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one TaskAction. */
export function toJsonTaskAction(value: TaskAction): Json {
    switch (value.kind) {
        case "list":
            return {
                kind: "list",
            };
        case "run":
            return {
                kind: "run",
                name: value.name,
                args: value.args.map((item0) => item0),
                dryRun: value.dryRun,
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one TaskAction from one JSON value. */
export function fromJsonTaskAction(value: Json): TaskAction {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "list":
            return {
                kind,
            };
        case "run":
            return {
                kind,
                name: jsonString(jsonField(object, "name")),
                args: jsonArray(jsonField(object, "args")).map((item0) => jsonString(item0)),
                dryRun: jsonBool(jsonField(object, "dryRun")),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** Payload for task command output. */
export type TaskPayload = {
    /** Task list entries. */
    readonly tasks?: ReadonlyArray<TaskEntry>;
    /** Per-project task execution results. */
    readonly results?: ReadonlyArray<TaskResult>;
    /** Exit code when executed. */
    readonly exitCode?: number;
};

export const TaskPayload = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: TaskPayload): void {
        encodeTaskPayload(writer, value);
    },

    /** Decode one TaskPayload. */
    decode(reader: BinaryReader): TaskPayload {
        return decodeTaskPayload(reader);
    },

    /** Return this value as JSON. */
    toJson(value: TaskPayload): Json {
        return toJsonTaskPayload(value);
    },

    /** Return one TaskPayload from one JSON value. */
    fromJson(value: Json): TaskPayload {
        return fromJsonTaskPayload(value);
    },
};

/** Encode one TaskPayload. */
export function encodeTaskPayload(writer: BinaryWriter, value: TaskPayload): void {
    writer.writeOption(value.tasks, (value0) => {
        writer.writeUnsigned(value0.length);
        for (const item1 of value0) {
            encodeTaskEntry(writer, item1);
        }
    });
    writer.writeOption(value.results, (value1) => {
        writer.writeUnsigned(value1.length);
        for (const item2 of value1) {
            encodeTaskResult(writer, item2);
        }
    });
    writer.writeOption(value.exitCode, (value2) => {
        writer.writeSigned(value2);
    });
}

/** Decode one TaskPayload. */
export function decodeTaskPayload(reader: BinaryReader): TaskPayload {
    const tasks = reader.readOption(() => (() => { const length1 = reader.readNumber(); const items1: Array<TaskEntry> = []; for (let index = 0; index < length1; index += 1) { items1.push(decodeTaskEntry(reader)); } return items1; })());
    const results = reader.readOption(() => (() => { const length2 = reader.readNumber(); const items2: Array<TaskResult> = []; for (let index = 0; index < length2; index += 1) { items2.push(decodeTaskResult(reader)); } return items2; })());
    const exitCode = reader.readOption(() => reader.readSignedNumber());

    return {
        ...(tasks === undefined ? {} : { tasks }),
        ...(results === undefined ? {} : { results }),
        ...(exitCode === undefined ? {} : { exitCode }),
    };
}

/** Return one JSON value for one TaskPayload. */
export function toJsonTaskPayload(value: TaskPayload): Json {
    return {
        ...(value.tasks === undefined ? {} : { tasks: value.tasks.map((item0) => toJsonTaskEntry(item0)) }),
        ...(value.results === undefined ? {} : { results: value.results.map((item0) => toJsonTaskResult(item0)) }),
        ...(value.exitCode === undefined ? {} : { exitCode: value.exitCode }),
    };
}

/** Return one TaskPayload from one JSON value. */
export function fromJsonTaskPayload(value: Json): TaskPayload {
    const object = jsonObject(value);

    return {
        tasks: jsonOptional(object, "tasks", (value) => jsonArray(value).map((item0) => fromJsonTaskEntry(item0))),
        results: jsonOptional(object, "results", (value) => jsonArray(value).map((item0) => fromJsonTaskResult(item0))),
        exitCode: jsonOptional(object, "exitCode", (value) => jsonInteger(value)),
    };
}

/** Task entry for task list output. */
export type TaskEntry = {
    /** Project identifier. */
    readonly project: string;
    /** Task name. */
    readonly name: string;
    /** Task description. */
    readonly description?: string;
    /** Task source. */
    readonly source?: string;
};

export const TaskEntry = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: TaskEntry): void {
        encodeTaskEntry(writer, value);
    },

    /** Decode one TaskEntry. */
    decode(reader: BinaryReader): TaskEntry {
        return decodeTaskEntry(reader);
    },

    /** Return this value as JSON. */
    toJson(value: TaskEntry): Json {
        return toJsonTaskEntry(value);
    },

    /** Return one TaskEntry from one JSON value. */
    fromJson(value: Json): TaskEntry {
        return fromJsonTaskEntry(value);
    },
};

/** Encode one TaskEntry. */
export function encodeTaskEntry(writer: BinaryWriter, value: TaskEntry): void {
    writer.writeString(value.project);
    writer.writeString(value.name);
    writer.writeOption(value.description, (value2) => {
        writer.writeString(value2);
    });
    writer.writeOption(value.source, (value3) => {
        writer.writeString(value3);
    });
}

/** Decode one TaskEntry. */
export function decodeTaskEntry(reader: BinaryReader): TaskEntry {
    const project = reader.readString();
    const name = reader.readString();
    const description = reader.readOption(() => reader.readString());
    const source = reader.readOption(() => reader.readString());

    return {
        project,
        name,
        ...(description === undefined ? {} : { description }),
        ...(source === undefined ? {} : { source }),
    };
}

/** Return one JSON value for one TaskEntry. */
export function toJsonTaskEntry(value: TaskEntry): Json {
    return {
        project: value.project,
        name: value.name,
        ...(value.description === undefined ? {} : { description: value.description }),
        ...(value.source === undefined ? {} : { source: value.source }),
    };
}

/** Return one TaskEntry from one JSON value. */
export function fromJsonTaskEntry(value: Json): TaskEntry {
    const object = jsonObject(value);

    return {
        project: jsonString(jsonField(object, "project")),
        name: jsonString(jsonField(object, "name")),
        description: jsonOptional(object, "description", (value) => jsonString(value)),
        source: jsonOptional(object, "source", (value) => jsonString(value)),
    };
}

/** One per-project task execution result. */
export type TaskResult = {
    /** Project identifier. */
    readonly project: string;
    /** Task name. */
    readonly task: string;
    /** Task command string. */
    readonly command: string;
    /** Task working directory. */
    readonly cwd: string;
    /** Whether this was a dry run. */
    readonly dryRun: boolean;
    /** Task source. */
    readonly source: string;
    /** Exit code when executed. */
    readonly exitCode?: number;
};

export const TaskResult = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: TaskResult): void {
        encodeTaskResult(writer, value);
    },

    /** Decode one TaskResult. */
    decode(reader: BinaryReader): TaskResult {
        return decodeTaskResult(reader);
    },

    /** Return this value as JSON. */
    toJson(value: TaskResult): Json {
        return toJsonTaskResult(value);
    },

    /** Return one TaskResult from one JSON value. */
    fromJson(value: Json): TaskResult {
        return fromJsonTaskResult(value);
    },
};

/** Encode one TaskResult. */
export function encodeTaskResult(writer: BinaryWriter, value: TaskResult): void {
    writer.writeString(value.project);
    writer.writeString(value.task);
    writer.writeString(value.command);
    writer.writeString(value.cwd);
    writer.writeBool(value.dryRun);
    writer.writeString(value.source);
    writer.writeOption(value.exitCode, (value6) => {
        writer.writeSigned(value6);
    });
}

/** Decode one TaskResult. */
export function decodeTaskResult(reader: BinaryReader): TaskResult {
    const project = reader.readString();
    const task = reader.readString();
    const command = reader.readString();
    const cwd = reader.readString();
    const dryRun = reader.readBool();
    const source = reader.readString();
    const exitCode = reader.readOption(() => reader.readSignedNumber());

    return {
        project,
        task,
        command,
        cwd,
        dryRun,
        source,
        ...(exitCode === undefined ? {} : { exitCode }),
    };
}

/** Return one JSON value for one TaskResult. */
export function toJsonTaskResult(value: TaskResult): Json {
    return {
        project: value.project,
        task: value.task,
        command: value.command,
        cwd: value.cwd,
        dryRun: value.dryRun,
        source: value.source,
        ...(value.exitCode === undefined ? {} : { exitCode: value.exitCode }),
    };
}

/** Return one TaskResult from one JSON value. */
export function fromJsonTaskResult(value: Json): TaskResult {
    const object = jsonObject(value);

    return {
        project: jsonString(jsonField(object, "project")),
        task: jsonString(jsonField(object, "task")),
        command: jsonString(jsonField(object, "command")),
        cwd: jsonString(jsonField(object, "cwd")),
        dryRun: jsonBool(jsonField(object, "dryRun")),
        source: jsonString(jsonField(object, "source")),
        exitCode: jsonOptional(object, "exitCode", (value) => jsonInteger(value)),
    };
}
