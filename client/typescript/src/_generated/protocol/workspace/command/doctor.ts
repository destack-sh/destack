/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, jsonArray, jsonBigint, jsonBool, jsonField, jsonInteger, jsonObject, jsonOptional, jsonString } from "../../../../protocol/serde.js";
import type { TraceView } from "../../../repository/provider/trace.js";
import type { CommandEnvVar } from "./common.js";
import type { CommandInput } from "./common.js";
import type { CommandRevision } from "./common.js";
import type { CommandTargetOverrides } from "./common.js";
import type { ManifestOverride } from "./common.js";
import { decodeTraceView, encodeTraceView, fromJsonTraceView, toJsonTraceView } from "../../../repository/provider/trace.js";
import { decodeCommandEnvVar, encodeCommandEnvVar, fromJsonCommandEnvVar, toJsonCommandEnvVar } from "./common.js";
import { decodeCommandInput, encodeCommandInput, fromJsonCommandInput, toJsonCommandInput } from "./common.js";
import { decodeCommandRevision, encodeCommandRevision, fromJsonCommandRevision, toJsonCommandRevision } from "./common.js";
import { decodeCommandTargetOverrides, encodeCommandTargetOverrides, fromJsonCommandTargetOverrides, toJsonCommandTargetOverrides } from "./common.js";
import { decodeManifestOverride, encodeManifestOverride, fromJsonManifestOverride, toJsonManifestOverride } from "./common.js";

/** Request to return workspace health information. */
export type DoctorInput = {
    /** Revision selected for this doctor request. */
    readonly revision: CommandRevision;
    /** Input sources for the command. */
    readonly inputs: ReadonlyArray<CommandInput>;
    /** Whether destack.json should resolve inputs when none are provided. */
    readonly configInputs: boolean;
    /** Optional working directory for this command. */
    readonly cwd?: string;
    /** Optional Destack manifest path override. */
    readonly manifest?: string;
    /** Optional target name override. */
    readonly target?: string;
    /** Optional target overrides. */
    readonly targetOverrides?: CommandTargetOverrides;
    /** Optional profile name override. */
    readonly profile?: string;
    /** Optional environment overrides. */
    readonly env: ReadonlyArray<CommandEnvVar>;
    /** Optional manifest overrides. */
    readonly overrides: ReadonlyArray<ManifestOverride>;
    /** Whether the command should watch for changes. */
    readonly watch: boolean;
    /** Whether the command should skip writes. */
    readonly dryRun: boolean;
    /** Trace detail returned for this command. */
    readonly trace?: TraceView;
    /** Whether to run extended checks. */
    readonly full: boolean;
};

export const DoctorInput = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: DoctorInput): void {
        encodeDoctorInput(writer, value);
    },

    /** Decode one DoctorInput. */
    decode(reader: BinaryReader): DoctorInput {
        return decodeDoctorInput(reader);
    },

    /** Return this value as JSON. */
    toJson(value: DoctorInput): Json {
        return toJsonDoctorInput(value);
    },

    /** Return one DoctorInput from one JSON value. */
    fromJson(value: Json): DoctorInput {
        return fromJsonDoctorInput(value);
    },
};

/** Encode one DoctorInput. */
export function encodeDoctorInput(writer: BinaryWriter, value: DoctorInput): void {
    encodeCommandRevision(writer, value.revision);
    writer.writeUnsigned(value.inputs.length);
    for (const item1 of value.inputs) {
        encodeCommandInput(writer, item1);
    }
    writer.writeBool(value.configInputs);
    writer.writeOption(value.cwd, (value3) => {
        writer.writeString(value3);
    });
    writer.writeOption(value.manifest, (value4) => {
        writer.writeString(value4);
    });
    writer.writeOption(value.target, (value5) => {
        writer.writeString(value5);
    });
    writer.writeOption(value.targetOverrides, (value6) => {
        encodeCommandTargetOverrides(writer, value6);
    });
    writer.writeOption(value.profile, (value7) => {
        writer.writeString(value7);
    });
    writer.writeUnsigned(value.env.length);
    for (const item8 of value.env) {
        encodeCommandEnvVar(writer, item8);
    }
    writer.writeUnsigned(value.overrides.length);
    for (const item9 of value.overrides) {
        encodeManifestOverride(writer, item9);
    }
    writer.writeBool(value.watch);
    writer.writeBool(value.dryRun);
    writer.writeOption(value.trace, (value12) => {
        encodeTraceView(writer, value12);
    });
    writer.writeBool(value.full);
}

/** Decode one DoctorInput. */
export function decodeDoctorInput(reader: BinaryReader): DoctorInput {
    const revision = decodeCommandRevision(reader);
    const inputs = (() => { const length1 = reader.readNumber(); const items1: Array<CommandInput> = []; for (let index = 0; index < length1; index += 1) { items1.push(decodeCommandInput(reader)); } return items1; })();
    const configInputs = reader.readBool();
    const cwd = reader.readOption(() => reader.readString());
    const manifest = reader.readOption(() => reader.readString());
    const target = reader.readOption(() => reader.readString());
    const targetOverrides = reader.readOption(() => decodeCommandTargetOverrides(reader));
    const profile = reader.readOption(() => reader.readString());
    const env = (() => { const length8 = reader.readNumber(); const items8: Array<CommandEnvVar> = []; for (let index = 0; index < length8; index += 1) { items8.push(decodeCommandEnvVar(reader)); } return items8; })();
    const overrides = (() => { const length9 = reader.readNumber(); const items9: Array<ManifestOverride> = []; for (let index = 0; index < length9; index += 1) { items9.push(decodeManifestOverride(reader)); } return items9; })();
    const watch = reader.readBool();
    const dryRun = reader.readBool();
    const trace = reader.readOption(() => decodeTraceView(reader));
    const full = reader.readBool();

    return {
        revision,
        inputs,
        configInputs,
        ...(cwd === undefined ? {} : { cwd }),
        ...(manifest === undefined ? {} : { manifest }),
        ...(target === undefined ? {} : { target }),
        ...(targetOverrides === undefined ? {} : { targetOverrides }),
        ...(profile === undefined ? {} : { profile }),
        env,
        overrides,
        watch,
        dryRun,
        ...(trace === undefined ? {} : { trace }),
        full,
    };
}

/** Return one JSON value for one DoctorInput. */
export function toJsonDoctorInput(value: DoctorInput): Json {
    return {
        revision: toJsonCommandRevision(value.revision),
        inputs: value.inputs.map((item0) => toJsonCommandInput(item0)),
        configInputs: value.configInputs,
        ...(value.cwd === undefined ? {} : { cwd: value.cwd }),
        ...(value.manifest === undefined ? {} : { manifest: value.manifest }),
        ...(value.target === undefined ? {} : { target: value.target }),
        ...(value.targetOverrides === undefined ? {} : { targetOverrides: toJsonCommandTargetOverrides(value.targetOverrides) }),
        ...(value.profile === undefined ? {} : { profile: value.profile }),
        env: value.env.map((item0) => toJsonCommandEnvVar(item0)),
        overrides: value.overrides.map((item0) => toJsonManifestOverride(item0)),
        watch: value.watch,
        dryRun: value.dryRun,
        ...(value.trace === undefined ? {} : { trace: toJsonTraceView(value.trace) }),
        full: value.full,
    };
}

/** Return one DoctorInput from one JSON value. */
export function fromJsonDoctorInput(value: Json): DoctorInput {
    const object = jsonObject(value);

    return {
        revision: fromJsonCommandRevision(jsonField(object, "revision")),
        inputs: jsonArray(jsonField(object, "inputs")).map((item0) => fromJsonCommandInput(item0)),
        configInputs: jsonBool(jsonField(object, "configInputs")),
        cwd: jsonOptional(object, "cwd", (value) => jsonString(value)),
        manifest: jsonOptional(object, "manifest", (value) => jsonString(value)),
        target: jsonOptional(object, "target", (value) => jsonString(value)),
        targetOverrides: jsonOptional(object, "targetOverrides", (value) => fromJsonCommandTargetOverrides(value)),
        profile: jsonOptional(object, "profile", (value) => jsonString(value)),
        env: jsonArray(jsonField(object, "env")).map((item0) => fromJsonCommandEnvVar(item0)),
        overrides: jsonArray(jsonField(object, "overrides")).map((item0) => fromJsonManifestOverride(item0)),
        watch: jsonBool(jsonField(object, "watch")),
        dryRun: jsonBool(jsonField(object, "dryRun")),
        trace: jsonOptional(object, "trace", (value) => fromJsonTraceView(value)),
        full: jsonBool(jsonField(object, "full")),
    };
}

/** Payload for doctor command output. */
export type DoctorPayload = {
    /** CLI version string. */
    readonly cliVersion: string;
    /** Working directory. */
    readonly cwd: string;
    /** Host operating system. */
    readonly os: string;
    /** Host architecture. */
    readonly arch: string;
    /** Worker count configured. */
    readonly workers: bigint;
    /** Detected parallelism. */
    readonly availableParallelism: bigint;
    /** Workspace metadata. */
    readonly workspace: DoctorWorkspace;
    /** Resolved destack.json path. */
    readonly manifest?: string;
    /** Manifest extends entries. */
    readonly extends?: ReadonlyArray<string>;
    /** Default target name. */
    readonly defaultTarget?: string;
    /** Target count. */
    readonly targetCount: number;
    /** Target names when requested. */
    readonly targets?: ReadonlyArray<string>;
    /** Tool checks when requested. */
    readonly tools?: ReadonlyArray<DoctorTool>;
    /** Warning list. */
    readonly warnings?: ReadonlyArray<string>;
};

export const DoctorPayload = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: DoctorPayload): void {
        encodeDoctorPayload(writer, value);
    },

    /** Decode one DoctorPayload. */
    decode(reader: BinaryReader): DoctorPayload {
        return decodeDoctorPayload(reader);
    },

    /** Return this value as JSON. */
    toJson(value: DoctorPayload): Json {
        return toJsonDoctorPayload(value);
    },

    /** Return one DoctorPayload from one JSON value. */
    fromJson(value: Json): DoctorPayload {
        return fromJsonDoctorPayload(value);
    },
};

/** Encode one DoctorPayload. */
export function encodeDoctorPayload(writer: BinaryWriter, value: DoctorPayload): void {
    writer.writeString(value.cliVersion);
    writer.writeString(value.cwd);
    writer.writeString(value.os);
    writer.writeString(value.arch);
    writer.writeUnsigned(value.workers);
    writer.writeUnsigned(value.availableParallelism);
    encodeDoctorWorkspace(writer, value.workspace);
    writer.writeOption(value.manifest, (value7) => {
        writer.writeString(value7);
    });
    writer.writeOption(value.extends, (value8) => {
        writer.writeUnsigned(value8.length);
        for (const item9 of value8) {
            writer.writeString(item9);
        }
    });
    writer.writeOption(value.defaultTarget, (value9) => {
        writer.writeString(value9);
    });
    writer.writeUnsigned(value.targetCount);
    writer.writeOption(value.targets, (value11) => {
        writer.writeUnsigned(value11.length);
        for (const item12 of value11) {
            writer.writeString(item12);
        }
    });
    writer.writeOption(value.tools, (value12) => {
        writer.writeUnsigned(value12.length);
        for (const item13 of value12) {
            encodeDoctorTool(writer, item13);
        }
    });
    writer.writeOption(value.warnings, (value13) => {
        writer.writeUnsigned(value13.length);
        for (const item14 of value13) {
            writer.writeString(item14);
        }
    });
}

/** Decode one DoctorPayload. */
export function decodeDoctorPayload(reader: BinaryReader): DoctorPayload {
    const cliVersion = reader.readString();
    const cwd = reader.readString();
    const os = reader.readString();
    const arch = reader.readString();
    const workers = reader.readUnsigned();
    const availableParallelism = reader.readUnsigned();
    const workspace = decodeDoctorWorkspace(reader);
    const manifest = reader.readOption(() => reader.readString());
    const extends_ = reader.readOption(() => (() => { const length9 = reader.readNumber(); const items9: Array<string> = []; for (let index = 0; index < length9; index += 1) { items9.push(reader.readString()); } return items9; })());
    const defaultTarget = reader.readOption(() => reader.readString());
    const targetCount = reader.readNumber();
    const targets = reader.readOption(() => (() => { const length12 = reader.readNumber(); const items12: Array<string> = []; for (let index = 0; index < length12; index += 1) { items12.push(reader.readString()); } return items12; })());
    const tools = reader.readOption(() => (() => { const length13 = reader.readNumber(); const items13: Array<DoctorTool> = []; for (let index = 0; index < length13; index += 1) { items13.push(decodeDoctorTool(reader)); } return items13; })());
    const warnings = reader.readOption(() => (() => { const length14 = reader.readNumber(); const items14: Array<string> = []; for (let index = 0; index < length14; index += 1) { items14.push(reader.readString()); } return items14; })());

    return {
        cliVersion,
        cwd,
        os,
        arch,
        workers,
        availableParallelism,
        workspace,
        ...(manifest === undefined ? {} : { manifest }),
        ...(extends_ === undefined ? {} : { extends: extends_ }),
        ...(defaultTarget === undefined ? {} : { defaultTarget }),
        targetCount,
        ...(targets === undefined ? {} : { targets }),
        ...(tools === undefined ? {} : { tools }),
        ...(warnings === undefined ? {} : { warnings }),
    };
}

/** Return one JSON value for one DoctorPayload. */
export function toJsonDoctorPayload(value: DoctorPayload): Json {
    return {
        cliVersion: value.cliVersion,
        cwd: value.cwd,
        os: value.os,
        arch: value.arch,
        workers: value.workers.toString(),
        availableParallelism: value.availableParallelism.toString(),
        workspace: toJsonDoctorWorkspace(value.workspace),
        ...(value.manifest === undefined ? {} : { manifest: value.manifest }),
        ...(value.extends === undefined ? {} : { extends: value.extends.map((item0) => item0) }),
        ...(value.defaultTarget === undefined ? {} : { defaultTarget: value.defaultTarget }),
        targetCount: value.targetCount,
        ...(value.targets === undefined ? {} : { targets: value.targets.map((item0) => item0) }),
        ...(value.tools === undefined ? {} : { tools: value.tools.map((item0) => toJsonDoctorTool(item0)) }),
        ...(value.warnings === undefined ? {} : { warnings: value.warnings.map((item0) => item0) }),
    };
}

/** Return one DoctorPayload from one JSON value. */
export function fromJsonDoctorPayload(value: Json): DoctorPayload {
    const object = jsonObject(value);

    return {
        cliVersion: jsonString(jsonField(object, "cliVersion")),
        cwd: jsonString(jsonField(object, "cwd")),
        os: jsonString(jsonField(object, "os")),
        arch: jsonString(jsonField(object, "arch")),
        workers: jsonBigint(jsonField(object, "workers")),
        availableParallelism: jsonBigint(jsonField(object, "availableParallelism")),
        workspace: fromJsonDoctorWorkspace(jsonField(object, "workspace")),
        manifest: jsonOptional(object, "manifest", (value) => jsonString(value)),
        extends: jsonOptional(object, "extends", (value) => jsonArray(value).map((item0) => jsonString(item0))),
        defaultTarget: jsonOptional(object, "defaultTarget", (value) => jsonString(value)),
        targetCount: jsonInteger(jsonField(object, "targetCount")),
        targets: jsonOptional(object, "targets", (value) => jsonArray(value).map((item0) => jsonString(item0))),
        tools: jsonOptional(object, "tools", (value) => jsonArray(value).map((item0) => fromJsonDoctorTool(item0))),
        warnings: jsonOptional(object, "warnings", (value) => jsonArray(value).map((item0) => jsonString(item0))),
    };
}

/** Workspace details for doctor output. */
export type DoctorWorkspace = {
    /** Workspace root path. */
    readonly root: string;
    /** Workspace kind label. */
    readonly kind: string;
    /** Number of packages. */
    readonly packageCount: number;
    /** Package paths when requested. */
    readonly packages?: ReadonlyArray<string>;
};

export const DoctorWorkspace = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: DoctorWorkspace): void {
        encodeDoctorWorkspace(writer, value);
    },

    /** Decode one DoctorWorkspace. */
    decode(reader: BinaryReader): DoctorWorkspace {
        return decodeDoctorWorkspace(reader);
    },

    /** Return this value as JSON. */
    toJson(value: DoctorWorkspace): Json {
        return toJsonDoctorWorkspace(value);
    },

    /** Return one DoctorWorkspace from one JSON value. */
    fromJson(value: Json): DoctorWorkspace {
        return fromJsonDoctorWorkspace(value);
    },
};

/** Encode one DoctorWorkspace. */
export function encodeDoctorWorkspace(writer: BinaryWriter, value: DoctorWorkspace): void {
    writer.writeString(value.root);
    writer.writeString(value.kind);
    writer.writeUnsigned(value.packageCount);
    writer.writeOption(value.packages, (value3) => {
        writer.writeUnsigned(value3.length);
        for (const item4 of value3) {
            writer.writeString(item4);
        }
    });
}

/** Decode one DoctorWorkspace. */
export function decodeDoctorWorkspace(reader: BinaryReader): DoctorWorkspace {
    const root = reader.readString();
    const kind = reader.readString();
    const packageCount = reader.readNumber();
    const packages = reader.readOption(() => (() => { const length4 = reader.readNumber(); const items4: Array<string> = []; for (let index = 0; index < length4; index += 1) { items4.push(reader.readString()); } return items4; })());

    return {
        root,
        kind,
        packageCount,
        ...(packages === undefined ? {} : { packages }),
    };
}

/** Return one JSON value for one DoctorWorkspace. */
export function toJsonDoctorWorkspace(value: DoctorWorkspace): Json {
    return {
        root: value.root,
        kind: value.kind,
        packageCount: value.packageCount,
        ...(value.packages === undefined ? {} : { packages: value.packages.map((item0) => item0) }),
    };
}

/** Return one DoctorWorkspace from one JSON value. */
export function fromJsonDoctorWorkspace(value: Json): DoctorWorkspace {
    const object = jsonObject(value);

    return {
        root: jsonString(jsonField(object, "root")),
        kind: jsonString(jsonField(object, "kind")),
        packageCount: jsonInteger(jsonField(object, "packageCount")),
        packages: jsonOptional(object, "packages", (value) => jsonArray(value).map((item0) => jsonString(item0))),
    };
}

/** Tool probe entry for doctor output. */
export type DoctorTool = {
    /** Tool name. */
    readonly name: string;
    /** Tool version string. */
    readonly version?: string;
    /** Tool probe status. */
    readonly status: DoctorToolStatus;
};

export const DoctorTool = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: DoctorTool): void {
        encodeDoctorTool(writer, value);
    },

    /** Decode one DoctorTool. */
    decode(reader: BinaryReader): DoctorTool {
        return decodeDoctorTool(reader);
    },

    /** Return this value as JSON. */
    toJson(value: DoctorTool): Json {
        return toJsonDoctorTool(value);
    },

    /** Return one DoctorTool from one JSON value. */
    fromJson(value: Json): DoctorTool {
        return fromJsonDoctorTool(value);
    },
};

/** Encode one DoctorTool. */
export function encodeDoctorTool(writer: BinaryWriter, value: DoctorTool): void {
    writer.writeString(value.name);
    writer.writeOption(value.version, (value1) => {
        writer.writeString(value1);
    });
    encodeDoctorToolStatus(writer, value.status);
}

/** Decode one DoctorTool. */
export function decodeDoctorTool(reader: BinaryReader): DoctorTool {
    const name = reader.readString();
    const version = reader.readOption(() => reader.readString());
    const status = decodeDoctorToolStatus(reader);

    return {
        name,
        ...(version === undefined ? {} : { version }),
        status,
    };
}

/** Return one JSON value for one DoctorTool. */
export function toJsonDoctorTool(value: DoctorTool): Json {
    return {
        name: value.name,
        ...(value.version === undefined ? {} : { version: value.version }),
        status: toJsonDoctorToolStatus(value.status),
    };
}

/** Return one DoctorTool from one JSON value. */
export function fromJsonDoctorTool(value: Json): DoctorTool {
    const object = jsonObject(value);

    return {
        name: jsonString(jsonField(object, "name")),
        version: jsonOptional(object, "version", (value) => jsonString(value)),
        status: fromJsonDoctorToolStatus(jsonField(object, "status")),
    };
}

/** Status values for tool detection. */
export type DoctorToolStatus = "available" | "missing" | "error";

export const DoctorToolStatus = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: DoctorToolStatus): void {
        encodeDoctorToolStatus(writer, value);
    },

    /** Decode one DoctorToolStatus. */
    decode(reader: BinaryReader): DoctorToolStatus {
        return decodeDoctorToolStatus(reader);
    },

    /** Return this value as JSON. */
    toJson(value: DoctorToolStatus): Json {
        return toJsonDoctorToolStatus(value);
    },

    /** Return one DoctorToolStatus from one JSON value. */
    fromJson(value: Json): DoctorToolStatus {
        return fromJsonDoctorToolStatus(value);
    },
};

/** Encode one DoctorToolStatus. */
export function encodeDoctorToolStatus(writer: BinaryWriter, value: DoctorToolStatus): void {
    switch (value) {
        case "available":
            writer.writeUnsigned(0);
            return;
        case "missing":
            writer.writeUnsigned(1);
            return;
        case "error":
            writer.writeUnsigned(2);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one DoctorToolStatus. */
export function decodeDoctorToolStatus(reader: BinaryReader): DoctorToolStatus {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "available";
        case 1:
            return "missing";
        case 2:
            return "error";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one DoctorToolStatus. */
export function toJsonDoctorToolStatus(value: DoctorToolStatus): Json {
    return value;
}

/** Return one DoctorToolStatus from one JSON value. */
export function fromJsonDoctorToolStatus(value: Json): DoctorToolStatus {
    const variant = jsonString(value);

    switch (variant) {
        case "available":
            return "available";
        case "missing":
            return "missing";
        case "error":
            return "error";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}
