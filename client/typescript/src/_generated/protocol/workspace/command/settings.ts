/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, jsonArray, jsonBigint, jsonBool, jsonField, jsonInteger, jsonObject, jsonOptional, jsonString } from "../../../../protocol/serde.js";
import type { TraceView } from "../../../repository/provider/trace.js";
import type { CommandEnvVar } from "./common.js";
import type { CommandInput } from "./common.js";
import type { CommandRevision } from "./common.js";
import type { CommandTargetOverrides } from "./common.js";
import type { ManifestOverride } from "./common.js";
import { decodeTraceView, encodeTraceView, fromJsonTraceView, toJsonTraceView } from "../../../repository/provider/trace.js";
import { decodeCommandEnvVar, encodeCommandEnvVar, fromJsonCommandEnvVar, toJsonCommandEnvVar } from "./common.js";
import { decodeCommandInput, encodeCommandInput, fromJsonCommandInput, toJsonCommandInput } from "./common.js";
import { decodeCommandRevision, encodeCommandRevision, fromJsonCommandRevision, toJsonCommandRevision } from "./common.js";
import { decodeCommandTargetOverrides, encodeCommandTargetOverrides, fromJsonCommandTargetOverrides, toJsonCommandTargetOverrides } from "./common.js";
import { decodeManifestOverride, encodeManifestOverride, fromJsonManifestOverride, toJsonManifestOverride } from "./common.js";

/** Request to return resolved settings. */
export type SettingsInput = {
    /** Revision selected for this settings request. */
    readonly revision: CommandRevision;
    /** Input sources for the command. */
    readonly inputs: ReadonlyArray<CommandInput>;
    /** Whether destack.json should resolve inputs when none are provided. */
    readonly configInputs: boolean;
    /** Optional working directory for this command. */
    readonly cwd?: string;
    /** Optional Destack manifest path override. */
    readonly manifest?: string;
    /** Optional target name override. */
    readonly target?: string;
    /** Optional target overrides. */
    readonly targetOverrides?: CommandTargetOverrides;
    /** Optional profile name override. */
    readonly profile?: string;
    /** Optional environment overrides. */
    readonly env: ReadonlyArray<CommandEnvVar>;
    /** Optional manifest overrides. */
    readonly overrides: ReadonlyArray<ManifestOverride>;
    /** Whether the command should watch for changes. */
    readonly watch: boolean;
    /** Whether the command should skip writes. */
    readonly dryRun: boolean;
    /** Trace detail returned for this command. */
    readonly trace?: TraceView;
};

export const SettingsInput = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: SettingsInput): void {
        encodeSettingsInput(writer, value);
    },

    /** Decode one SettingsInput. */
    decode(reader: BinaryReader): SettingsInput {
        return decodeSettingsInput(reader);
    },

    /** Return this value as JSON. */
    toJson(value: SettingsInput): Json {
        return toJsonSettingsInput(value);
    },

    /** Return one SettingsInput from one JSON value. */
    fromJson(value: Json): SettingsInput {
        return fromJsonSettingsInput(value);
    },
};

/** Encode one SettingsInput. */
export function encodeSettingsInput(writer: BinaryWriter, value: SettingsInput): void {
    encodeCommandRevision(writer, value.revision);
    writer.writeUnsigned(value.inputs.length);
    for (const item1 of value.inputs) {
        encodeCommandInput(writer, item1);
    }
    writer.writeBool(value.configInputs);
    writer.writeOption(value.cwd, (value3) => {
        writer.writeString(value3);
    });
    writer.writeOption(value.manifest, (value4) => {
        writer.writeString(value4);
    });
    writer.writeOption(value.target, (value5) => {
        writer.writeString(value5);
    });
    writer.writeOption(value.targetOverrides, (value6) => {
        encodeCommandTargetOverrides(writer, value6);
    });
    writer.writeOption(value.profile, (value7) => {
        writer.writeString(value7);
    });
    writer.writeUnsigned(value.env.length);
    for (const item8 of value.env) {
        encodeCommandEnvVar(writer, item8);
    }
    writer.writeUnsigned(value.overrides.length);
    for (const item9 of value.overrides) {
        encodeManifestOverride(writer, item9);
    }
    writer.writeBool(value.watch);
    writer.writeBool(value.dryRun);
    writer.writeOption(value.trace, (value12) => {
        encodeTraceView(writer, value12);
    });
}

/** Decode one SettingsInput. */
export function decodeSettingsInput(reader: BinaryReader): SettingsInput {
    const revision = decodeCommandRevision(reader);
    const inputs = (() => { const length1 = reader.readNumber(); const items1: Array<CommandInput> = []; for (let index = 0; index < length1; index += 1) { items1.push(decodeCommandInput(reader)); } return items1; })();
    const configInputs = reader.readBool();
    const cwd = reader.readOption(() => reader.readString());
    const manifest = reader.readOption(() => reader.readString());
    const target = reader.readOption(() => reader.readString());
    const targetOverrides = reader.readOption(() => decodeCommandTargetOverrides(reader));
    const profile = reader.readOption(() => reader.readString());
    const env = (() => { const length8 = reader.readNumber(); const items8: Array<CommandEnvVar> = []; for (let index = 0; index < length8; index += 1) { items8.push(decodeCommandEnvVar(reader)); } return items8; })();
    const overrides = (() => { const length9 = reader.readNumber(); const items9: Array<ManifestOverride> = []; for (let index = 0; index < length9; index += 1) { items9.push(decodeManifestOverride(reader)); } return items9; })();
    const watch = reader.readBool();
    const dryRun = reader.readBool();
    const trace = reader.readOption(() => decodeTraceView(reader));

    return {
        revision,
        inputs,
        configInputs,
        ...(cwd === undefined ? {} : { cwd }),
        ...(manifest === undefined ? {} : { manifest }),
        ...(target === undefined ? {} : { target }),
        ...(targetOverrides === undefined ? {} : { targetOverrides }),
        ...(profile === undefined ? {} : { profile }),
        env,
        overrides,
        watch,
        dryRun,
        ...(trace === undefined ? {} : { trace }),
    };
}

/** Return one JSON value for one SettingsInput. */
export function toJsonSettingsInput(value: SettingsInput): Json {
    return {
        revision: toJsonCommandRevision(value.revision),
        inputs: value.inputs.map((item0) => toJsonCommandInput(item0)),
        configInputs: value.configInputs,
        ...(value.cwd === undefined ? {} : { cwd: value.cwd }),
        ...(value.manifest === undefined ? {} : { manifest: value.manifest }),
        ...(value.target === undefined ? {} : { target: value.target }),
        ...(value.targetOverrides === undefined ? {} : { targetOverrides: toJsonCommandTargetOverrides(value.targetOverrides) }),
        ...(value.profile === undefined ? {} : { profile: value.profile }),
        env: value.env.map((item0) => toJsonCommandEnvVar(item0)),
        overrides: value.overrides.map((item0) => toJsonManifestOverride(item0)),
        watch: value.watch,
        dryRun: value.dryRun,
        ...(value.trace === undefined ? {} : { trace: toJsonTraceView(value.trace) }),
    };
}

/** Return one SettingsInput from one JSON value. */
export function fromJsonSettingsInput(value: Json): SettingsInput {
    const object = jsonObject(value);

    return {
        revision: fromJsonCommandRevision(jsonField(object, "revision")),
        inputs: jsonArray(jsonField(object, "inputs")).map((item0) => fromJsonCommandInput(item0)),
        configInputs: jsonBool(jsonField(object, "configInputs")),
        cwd: jsonOptional(object, "cwd", (value) => jsonString(value)),
        manifest: jsonOptional(object, "manifest", (value) => jsonString(value)),
        target: jsonOptional(object, "target", (value) => jsonString(value)),
        targetOverrides: jsonOptional(object, "targetOverrides", (value) => fromJsonCommandTargetOverrides(value)),
        profile: jsonOptional(object, "profile", (value) => jsonString(value)),
        env: jsonArray(jsonField(object, "env")).map((item0) => fromJsonCommandEnvVar(item0)),
        overrides: jsonArray(jsonField(object, "overrides")).map((item0) => fromJsonManifestOverride(item0)),
        watch: jsonBool(jsonField(object, "watch")),
        dryRun: jsonBool(jsonField(object, "dryRun")),
        trace: jsonOptional(object, "trace", (value) => fromJsonTraceView(value)),
    };
}

/** Payload for settings command output. */
export type SettingsPayload = {
    /** Machine-local Destack home. */
    readonly home: string;
    /** Package directory. */
    readonly packages: string;
    /** Maximum package directory size in bytes before pruning is requested. */
    readonly packageMaximumBytes?: bigint;
    /** Workspace-local cache and session directory. */
    readonly workspaceCache: string;
    /** Maximum cache size in bytes before pruning is requested. */
    readonly cacheMaximumBytes?: bigint;
    /** Workspace-owned vendor directory. */
    readonly vendor: string;
    /** Default registry name. */
    readonly registry?: string;
    /** Known registries. */
    readonly registries: ReadonlyArray<SettingsRegistry>;
    /** Network settings for package and update commands. */
    readonly network: SettingsNetwork;
};

export const SettingsPayload = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: SettingsPayload): void {
        encodeSettingsPayload(writer, value);
    },

    /** Decode one SettingsPayload. */
    decode(reader: BinaryReader): SettingsPayload {
        return decodeSettingsPayload(reader);
    },

    /** Return this value as JSON. */
    toJson(value: SettingsPayload): Json {
        return toJsonSettingsPayload(value);
    },

    /** Return one SettingsPayload from one JSON value. */
    fromJson(value: Json): SettingsPayload {
        return fromJsonSettingsPayload(value);
    },
};

/** Encode one SettingsPayload. */
export function encodeSettingsPayload(writer: BinaryWriter, value: SettingsPayload): void {
    writer.writeString(value.home);
    writer.writeString(value.packages);
    writer.writeOption(value.packageMaximumBytes, (value2) => {
        writer.writeUnsigned(value2);
    });
    writer.writeString(value.workspaceCache);
    writer.writeOption(value.cacheMaximumBytes, (value4) => {
        writer.writeUnsigned(value4);
    });
    writer.writeString(value.vendor);
    writer.writeOption(value.registry, (value6) => {
        writer.writeString(value6);
    });
    writer.writeUnsigned(value.registries.length);
    for (const item7 of value.registries) {
        encodeSettingsRegistry(writer, item7);
    }
    encodeSettingsNetwork(writer, value.network);
}

/** Decode one SettingsPayload. */
export function decodeSettingsPayload(reader: BinaryReader): SettingsPayload {
    const home = reader.readString();
    const packages = reader.readString();
    const packageMaximumBytes = reader.readOption(() => reader.readUnsigned());
    const workspaceCache = reader.readString();
    const cacheMaximumBytes = reader.readOption(() => reader.readUnsigned());
    const vendor = reader.readString();
    const registry = reader.readOption(() => reader.readString());
    const registries = (() => { const length7 = reader.readNumber(); const items7: Array<SettingsRegistry> = []; for (let index = 0; index < length7; index += 1) { items7.push(decodeSettingsRegistry(reader)); } return items7; })();
    const network = decodeSettingsNetwork(reader);

    return {
        home,
        packages,
        ...(packageMaximumBytes === undefined ? {} : { packageMaximumBytes }),
        workspaceCache,
        ...(cacheMaximumBytes === undefined ? {} : { cacheMaximumBytes }),
        vendor,
        ...(registry === undefined ? {} : { registry }),
        registries,
        network,
    };
}

/** Return one JSON value for one SettingsPayload. */
export function toJsonSettingsPayload(value: SettingsPayload): Json {
    return {
        home: value.home,
        packages: value.packages,
        ...(value.packageMaximumBytes === undefined ? {} : { packageMaximumBytes: value.packageMaximumBytes.toString() }),
        workspaceCache: value.workspaceCache,
        ...(value.cacheMaximumBytes === undefined ? {} : { cacheMaximumBytes: value.cacheMaximumBytes.toString() }),
        vendor: value.vendor,
        ...(value.registry === undefined ? {} : { registry: value.registry }),
        registries: value.registries.map((item0) => toJsonSettingsRegistry(item0)),
        network: toJsonSettingsNetwork(value.network),
    };
}

/** Return one SettingsPayload from one JSON value. */
export function fromJsonSettingsPayload(value: Json): SettingsPayload {
    const object = jsonObject(value);

    return {
        home: jsonString(jsonField(object, "home")),
        packages: jsonString(jsonField(object, "packages")),
        packageMaximumBytes: jsonOptional(object, "packageMaximumBytes", (value) => jsonBigint(value)),
        workspaceCache: jsonString(jsonField(object, "workspaceCache")),
        cacheMaximumBytes: jsonOptional(object, "cacheMaximumBytes", (value) => jsonBigint(value)),
        vendor: jsonString(jsonField(object, "vendor")),
        registry: jsonOptional(object, "registry", (value) => jsonString(value)),
        registries: jsonArray(jsonField(object, "registries")).map((item0) => fromJsonSettingsRegistry(item0)),
        network: fromJsonSettingsNetwork(jsonField(object, "network")),
    };
}

/** Registry settings for command output. */
export type SettingsRegistry = {
    /** Registry name. */
    readonly name: string;
    /** Registry URL. */
    readonly url: string;
    /** Redacted authentication shape. */
    readonly authentication: SettingsRegistryAuthentication;
};

export const SettingsRegistry = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: SettingsRegistry): void {
        encodeSettingsRegistry(writer, value);
    },

    /** Decode one SettingsRegistry. */
    decode(reader: BinaryReader): SettingsRegistry {
        return decodeSettingsRegistry(reader);
    },

    /** Return this value as JSON. */
    toJson(value: SettingsRegistry): Json {
        return toJsonSettingsRegistry(value);
    },

    /** Return one SettingsRegistry from one JSON value. */
    fromJson(value: Json): SettingsRegistry {
        return fromJsonSettingsRegistry(value);
    },
};

/** Encode one SettingsRegistry. */
export function encodeSettingsRegistry(writer: BinaryWriter, value: SettingsRegistry): void {
    writer.writeString(value.name);
    writer.writeString(value.url);
    encodeSettingsRegistryAuthentication(writer, value.authentication);
}

/** Decode one SettingsRegistry. */
export function decodeSettingsRegistry(reader: BinaryReader): SettingsRegistry {
    const name = reader.readString();
    const url = reader.readString();
    const authentication = decodeSettingsRegistryAuthentication(reader);

    return {
        name,
        url,
        authentication,
    };
}

/** Return one JSON value for one SettingsRegistry. */
export function toJsonSettingsRegistry(value: SettingsRegistry): Json {
    return {
        name: value.name,
        url: value.url,
        authentication: toJsonSettingsRegistryAuthentication(value.authentication),
    };
}

/** Return one SettingsRegistry from one JSON value. */
export function fromJsonSettingsRegistry(value: Json): SettingsRegistry {
    const object = jsonObject(value);

    return {
        name: jsonString(jsonField(object, "name")),
        url: jsonString(jsonField(object, "url")),
        authentication: fromJsonSettingsRegistryAuthentication(jsonField(object, "authentication")),
    };
}

/** Redacted registry authentication shape. */
export type SettingsRegistryAuthentication =
    /** No registry authentication. */
    | {
          readonly kind: "none";
      }
    /** Inline token authentication. */
    | {
          readonly kind: "token";
      }
    /** Token read from one environment variable. */
    | {
          readonly kind: "tokenFromEnvironment";
          /** Environment variable name. */
          readonly variable: string;
      }
    /** Credentials printed by one process command. */
    | {
          readonly kind: "command";
          /** Program to run. */
          readonly program: string;
      }
;

export const SettingsRegistryAuthentication = {
    /** No registry authentication. */
    none(): SettingsRegistryAuthentication {
        return { kind: "none" };
    },

    /** Inline token authentication. */
    token(): SettingsRegistryAuthentication {
        return { kind: "token" };
    },

    /** Token read from one environment variable. */
    tokenFromEnvironment(variable: string): SettingsRegistryAuthentication {
        return { kind: "tokenFromEnvironment", variable };
    },

    /** Credentials printed by one process command. */
    command(program: string): SettingsRegistryAuthentication {
        return { kind: "command", program };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: SettingsRegistryAuthentication): void {
        encodeSettingsRegistryAuthentication(writer, value);
    },

    /** Decode one SettingsRegistryAuthentication. */
    decode(reader: BinaryReader): SettingsRegistryAuthentication {
        return decodeSettingsRegistryAuthentication(reader);
    },

    /** Return this value as JSON. */
    toJson(value: SettingsRegistryAuthentication): Json {
        return toJsonSettingsRegistryAuthentication(value);
    },

    /** Return one SettingsRegistryAuthentication from one JSON value. */
    fromJson(value: Json): SettingsRegistryAuthentication {
        return fromJsonSettingsRegistryAuthentication(value);
    },
};

/** Encode one SettingsRegistryAuthentication. */
export function encodeSettingsRegistryAuthentication(writer: BinaryWriter, value: SettingsRegistryAuthentication): void {
    switch (value.kind) {
        case "none":
            writer.writeUnsigned(0);
            return;
        case "token":
            writer.writeUnsigned(1);
            return;
        case "tokenFromEnvironment":
            writer.writeUnsigned(2);
            writer.writeString(value.variable);
            return;
        case "command":
            writer.writeUnsigned(3);
            writer.writeString(value.program);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one SettingsRegistryAuthentication. */
export function decodeSettingsRegistryAuthentication(reader: BinaryReader): SettingsRegistryAuthentication {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            return { kind: "none" };
        }
        case 1: {
            return { kind: "token" };
        }
        case 2: {
            const variable = reader.readString();

            return {
                kind: "tokenFromEnvironment",
                variable,
            };
        }
        case 3: {
            const program = reader.readString();

            return {
                kind: "command",
                program,
            };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one SettingsRegistryAuthentication. */
export function toJsonSettingsRegistryAuthentication(value: SettingsRegistryAuthentication): Json {
    switch (value.kind) {
        case "none":
            return {
                kind: "none",
            };
        case "token":
            return {
                kind: "token",
            };
        case "tokenFromEnvironment":
            return {
                kind: "tokenFromEnvironment",
                variable: value.variable,
            };
        case "command":
            return {
                kind: "command",
                program: value.program,
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one SettingsRegistryAuthentication from one JSON value. */
export function fromJsonSettingsRegistryAuthentication(value: Json): SettingsRegistryAuthentication {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "none":
            return {
                kind,
            };
        case "token":
            return {
                kind,
            };
        case "tokenFromEnvironment":
            return {
                kind,
                variable: jsonString(jsonField(object, "variable")),
            };
        case "command":
            return {
                kind,
                program: jsonString(jsonField(object, "program")),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** Network settings for command output. */
export type SettingsNetwork = {
    /** Whether network access should be disabled by default. */
    readonly offline: boolean;
    /** Whether a proxy is configured. */
    readonly hasProxy: boolean;
    /** Request timeout in milliseconds. */
    readonly timeoutMilliseconds?: bigint;
    /** Number of retries for transient network failures. */
    readonly retryCount?: number;
    /** Maximum concurrent network requests. */
    readonly concurrency?: number;
};

export const SettingsNetwork = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: SettingsNetwork): void {
        encodeSettingsNetwork(writer, value);
    },

    /** Decode one SettingsNetwork. */
    decode(reader: BinaryReader): SettingsNetwork {
        return decodeSettingsNetwork(reader);
    },

    /** Return this value as JSON. */
    toJson(value: SettingsNetwork): Json {
        return toJsonSettingsNetwork(value);
    },

    /** Return one SettingsNetwork from one JSON value. */
    fromJson(value: Json): SettingsNetwork {
        return fromJsonSettingsNetwork(value);
    },
};

/** Encode one SettingsNetwork. */
export function encodeSettingsNetwork(writer: BinaryWriter, value: SettingsNetwork): void {
    writer.writeBool(value.offline);
    writer.writeBool(value.hasProxy);
    writer.writeOption(value.timeoutMilliseconds, (value2) => {
        writer.writeUnsigned(value2);
    });
    writer.writeOption(value.retryCount, (value3) => {
        writer.writeUnsigned(value3);
    });
    writer.writeOption(value.concurrency, (value4) => {
        writer.writeUnsigned(value4);
    });
}

/** Decode one SettingsNetwork. */
export function decodeSettingsNetwork(reader: BinaryReader): SettingsNetwork {
    const offline = reader.readBool();
    const hasProxy = reader.readBool();
    const timeoutMilliseconds = reader.readOption(() => reader.readUnsigned());
    const retryCount = reader.readOption(() => reader.readNumber());
    const concurrency = reader.readOption(() => reader.readNumber());

    return {
        offline,
        hasProxy,
        ...(timeoutMilliseconds === undefined ? {} : { timeoutMilliseconds }),
        ...(retryCount === undefined ? {} : { retryCount }),
        ...(concurrency === undefined ? {} : { concurrency }),
    };
}

/** Return one JSON value for one SettingsNetwork. */
export function toJsonSettingsNetwork(value: SettingsNetwork): Json {
    return {
        offline: value.offline,
        hasProxy: value.hasProxy,
        ...(value.timeoutMilliseconds === undefined ? {} : { timeoutMilliseconds: value.timeoutMilliseconds.toString() }),
        ...(value.retryCount === undefined ? {} : { retryCount: value.retryCount }),
        ...(value.concurrency === undefined ? {} : { concurrency: value.concurrency }),
    };
}

/** Return one SettingsNetwork from one JSON value. */
export function fromJsonSettingsNetwork(value: Json): SettingsNetwork {
    const object = jsonObject(value);

    return {
        offline: jsonBool(jsonField(object, "offline")),
        hasProxy: jsonBool(jsonField(object, "hasProxy")),
        timeoutMilliseconds: jsonOptional(object, "timeoutMilliseconds", (value) => jsonBigint(value)),
        retryCount: jsonOptional(object, "retryCount", (value) => jsonInteger(value)),
        concurrency: jsonOptional(object, "concurrency", (value) => jsonInteger(value)),
    };
}
