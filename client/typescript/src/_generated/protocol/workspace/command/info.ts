/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, jsonArray, jsonBool, jsonField, jsonObject, jsonOptional, jsonString } from "../../../../protocol/serde.js";
import type { CommandEnvVar } from "./common.js";
import type { CommandInput } from "./common.js";
import type { CommandRevision } from "./common.js";
import type { CommandTargetOverrides } from "./common.js";
import type { ManifestOverride } from "./common.js";
import type { TargetEntry } from "./targets.js";
import { decodeCommandEnvVar, encodeCommandEnvVar, fromJsonCommandEnvVar, toJsonCommandEnvVar } from "./common.js";
import { decodeCommandInput, encodeCommandInput, fromJsonCommandInput, toJsonCommandInput } from "./common.js";
import { decodeCommandRevision, encodeCommandRevision, fromJsonCommandRevision, toJsonCommandRevision } from "./common.js";
import { decodeCommandTargetOverrides, encodeCommandTargetOverrides, fromJsonCommandTargetOverrides, toJsonCommandTargetOverrides } from "./common.js";
import { decodeManifestOverride, encodeManifestOverride, fromJsonManifestOverride, toJsonManifestOverride } from "./common.js";
import { decodeTargetEntry, encodeTargetEntry, fromJsonTargetEntry, toJsonTargetEntry } from "./targets.js";

/** Request to return workspace information. */
export type InfoInput = {
    /** Revision selected for this info request. */
    readonly revision: CommandRevision;
    /** Input sources for the command. */
    readonly inputs: ReadonlyArray<CommandInput>;
    /** Whether destack.json should resolve inputs when none are provided. */
    readonly configInputs: boolean;
    /** Optional working directory for this command. */
    readonly cwd?: string;
    /** Optional Destack manifest path override. */
    readonly manifest?: string;
    /** Optional target name override. */
    readonly target?: string;
    /** Optional target overrides. */
    readonly targetOverrides?: CommandTargetOverrides;
    /** Optional profile name override. */
    readonly profile?: string;
    /** Optional environment overrides. */
    readonly env: ReadonlyArray<CommandEnvVar>;
    /** Optional manifest overrides. */
    readonly overrides: ReadonlyArray<ManifestOverride>;
    /** Whether the command should watch for changes. */
    readonly watch: boolean;
    /** Whether the command should skip writes. */
    readonly dryRun: boolean;
    /** Whether to include all workspace packages. */
    readonly all: boolean;
};

export const InfoInput = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: InfoInput): void {
        encodeInfoInput(writer, value);
    },

    /** Decode one InfoInput. */
    decode(reader: BinaryReader): InfoInput {
        return decodeInfoInput(reader);
    },

    /** Return this value as JSON. */
    toJson(value: InfoInput): Json {
        return toJsonInfoInput(value);
    },

    /** Return one InfoInput from one JSON value. */
    fromJson(value: Json): InfoInput {
        return fromJsonInfoInput(value);
    },
};

/** Encode one InfoInput. */
export function encodeInfoInput(writer: BinaryWriter, value: InfoInput): void {
    encodeCommandRevision(writer, value.revision);
    writer.writeUnsigned(value.inputs.length);
    for (const item1 of value.inputs) {
        encodeCommandInput(writer, item1);
    }
    writer.writeBool(value.configInputs);
    writer.writeOption(value.cwd, (value3) => {
        writer.writeString(value3);
    });
    writer.writeOption(value.manifest, (value4) => {
        writer.writeString(value4);
    });
    writer.writeOption(value.target, (value5) => {
        writer.writeString(value5);
    });
    writer.writeOption(value.targetOverrides, (value6) => {
        encodeCommandTargetOverrides(writer, value6);
    });
    writer.writeOption(value.profile, (value7) => {
        writer.writeString(value7);
    });
    writer.writeUnsigned(value.env.length);
    for (const item8 of value.env) {
        encodeCommandEnvVar(writer, item8);
    }
    writer.writeUnsigned(value.overrides.length);
    for (const item9 of value.overrides) {
        encodeManifestOverride(writer, item9);
    }
    writer.writeBool(value.watch);
    writer.writeBool(value.dryRun);
    writer.writeBool(value.all);
}

/** Decode one InfoInput. */
export function decodeInfoInput(reader: BinaryReader): InfoInput {
    const revision = decodeCommandRevision(reader);
    const inputs = (() => { const length1 = reader.readNumber(); const items1: Array<CommandInput> = []; for (let index = 0; index < length1; index += 1) { items1.push(decodeCommandInput(reader)); } return items1; })();
    const configInputs = reader.readBool();
    const cwd = reader.readOption(() => reader.readString());
    const manifest = reader.readOption(() => reader.readString());
    const target = reader.readOption(() => reader.readString());
    const targetOverrides = reader.readOption(() => decodeCommandTargetOverrides(reader));
    const profile = reader.readOption(() => reader.readString());
    const env = (() => { const length8 = reader.readNumber(); const items8: Array<CommandEnvVar> = []; for (let index = 0; index < length8; index += 1) { items8.push(decodeCommandEnvVar(reader)); } return items8; })();
    const overrides = (() => { const length9 = reader.readNumber(); const items9: Array<ManifestOverride> = []; for (let index = 0; index < length9; index += 1) { items9.push(decodeManifestOverride(reader)); } return items9; })();
    const watch = reader.readBool();
    const dryRun = reader.readBool();
    const all = reader.readBool();

    return {
        revision,
        inputs,
        configInputs,
        ...(cwd === undefined ? {} : { cwd }),
        ...(manifest === undefined ? {} : { manifest }),
        ...(target === undefined ? {} : { target }),
        ...(targetOverrides === undefined ? {} : { targetOverrides }),
        ...(profile === undefined ? {} : { profile }),
        env,
        overrides,
        watch,
        dryRun,
        all,
    };
}

/** Return one JSON value for one InfoInput. */
export function toJsonInfoInput(value: InfoInput): Json {
    return {
        revision: toJsonCommandRevision(value.revision),
        inputs: value.inputs.map((item0) => toJsonCommandInput(item0)),
        configInputs: value.configInputs,
        ...(value.cwd === undefined ? {} : { cwd: value.cwd }),
        ...(value.manifest === undefined ? {} : { manifest: value.manifest }),
        ...(value.target === undefined ? {} : { target: value.target }),
        ...(value.targetOverrides === undefined ? {} : { targetOverrides: toJsonCommandTargetOverrides(value.targetOverrides) }),
        ...(value.profile === undefined ? {} : { profile: value.profile }),
        env: value.env.map((item0) => toJsonCommandEnvVar(item0)),
        overrides: value.overrides.map((item0) => toJsonManifestOverride(item0)),
        watch: value.watch,
        dryRun: value.dryRun,
        all: value.all,
    };
}

/** Return one InfoInput from one JSON value. */
export function fromJsonInfoInput(value: Json): InfoInput {
    const object = jsonObject(value);

    return {
        revision: fromJsonCommandRevision(jsonField(object, "revision")),
        inputs: jsonArray(jsonField(object, "inputs")).map((item0) => fromJsonCommandInput(item0)),
        configInputs: jsonBool(jsonField(object, "configInputs")),
        cwd: jsonOptional(object, "cwd", (value) => jsonString(value)),
        manifest: jsonOptional(object, "manifest", (value) => jsonString(value)),
        target: jsonOptional(object, "target", (value) => jsonString(value)),
        targetOverrides: jsonOptional(object, "targetOverrides", (value) => fromJsonCommandTargetOverrides(value)),
        profile: jsonOptional(object, "profile", (value) => jsonString(value)),
        env: jsonArray(jsonField(object, "env")).map((item0) => fromJsonCommandEnvVar(item0)),
        overrides: jsonArray(jsonField(object, "overrides")).map((item0) => fromJsonManifestOverride(item0)),
        watch: jsonBool(jsonField(object, "watch")),
        dryRun: jsonBool(jsonField(object, "dryRun")),
        all: jsonBool(jsonField(object, "all")),
    };
}

/** Payload for info command output. */
export type InfoPayload = {
    /** Workspace metadata. */
    readonly workspace: InfoWorkspace;
    /** Resolved destack.json path. */
    readonly manifest?: string;
    /** Targets for the active package. */
    readonly targets?: ReadonlyArray<TargetEntry>;
    /** Targets for all workspace packages. */
    readonly workspaceTargets?: ReadonlyArray<TargetEntry>;
};

export const InfoPayload = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: InfoPayload): void {
        encodeInfoPayload(writer, value);
    },

    /** Decode one InfoPayload. */
    decode(reader: BinaryReader): InfoPayload {
        return decodeInfoPayload(reader);
    },

    /** Return this value as JSON. */
    toJson(value: InfoPayload): Json {
        return toJsonInfoPayload(value);
    },

    /** Return one InfoPayload from one JSON value. */
    fromJson(value: Json): InfoPayload {
        return fromJsonInfoPayload(value);
    },
};

/** Encode one InfoPayload. */
export function encodeInfoPayload(writer: BinaryWriter, value: InfoPayload): void {
    encodeInfoWorkspace(writer, value.workspace);
    writer.writeOption(value.manifest, (value1) => {
        writer.writeString(value1);
    });
    writer.writeOption(value.targets, (value2) => {
        writer.writeUnsigned(value2.length);
        for (const item3 of value2) {
            encodeTargetEntry(writer, item3);
        }
    });
    writer.writeOption(value.workspaceTargets, (value3) => {
        writer.writeUnsigned(value3.length);
        for (const item4 of value3) {
            encodeTargetEntry(writer, item4);
        }
    });
}

/** Decode one InfoPayload. */
export function decodeInfoPayload(reader: BinaryReader): InfoPayload {
    const workspace = decodeInfoWorkspace(reader);
    const manifest = reader.readOption(() => reader.readString());
    const targets = reader.readOption(() => (() => { const length3 = reader.readNumber(); const items3: Array<TargetEntry> = []; for (let index = 0; index < length3; index += 1) { items3.push(decodeTargetEntry(reader)); } return items3; })());
    const workspaceTargets = reader.readOption(() => (() => { const length4 = reader.readNumber(); const items4: Array<TargetEntry> = []; for (let index = 0; index < length4; index += 1) { items4.push(decodeTargetEntry(reader)); } return items4; })());

    return {
        workspace,
        ...(manifest === undefined ? {} : { manifest }),
        ...(targets === undefined ? {} : { targets }),
        ...(workspaceTargets === undefined ? {} : { workspaceTargets }),
    };
}

/** Return one JSON value for one InfoPayload. */
export function toJsonInfoPayload(value: InfoPayload): Json {
    return {
        workspace: toJsonInfoWorkspace(value.workspace),
        ...(value.manifest === undefined ? {} : { manifest: value.manifest }),
        ...(value.targets === undefined ? {} : { targets: value.targets.map((item0) => toJsonTargetEntry(item0)) }),
        ...(value.workspaceTargets === undefined ? {} : { workspaceTargets: value.workspaceTargets.map((item0) => toJsonTargetEntry(item0)) }),
    };
}

/** Return one InfoPayload from one JSON value. */
export function fromJsonInfoPayload(value: Json): InfoPayload {
    const object = jsonObject(value);

    return {
        workspace: fromJsonInfoWorkspace(jsonField(object, "workspace")),
        manifest: jsonOptional(object, "manifest", (value) => jsonString(value)),
        targets: jsonOptional(object, "targets", (value) => jsonArray(value).map((item0) => fromJsonTargetEntry(item0))),
        workspaceTargets: jsonOptional(object, "workspaceTargets", (value) => jsonArray(value).map((item0) => fromJsonTargetEntry(item0))),
    };
}

/** Workspace info for info command output. */
export type InfoWorkspace = {
    /** Workspace root path. */
    readonly root: string;
    /** Workspace kind label. */
    readonly kind: string;
    /** Workspace package paths. */
    readonly packages: ReadonlyArray<string>;
};

export const InfoWorkspace = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: InfoWorkspace): void {
        encodeInfoWorkspace(writer, value);
    },

    /** Decode one InfoWorkspace. */
    decode(reader: BinaryReader): InfoWorkspace {
        return decodeInfoWorkspace(reader);
    },

    /** Return this value as JSON. */
    toJson(value: InfoWorkspace): Json {
        return toJsonInfoWorkspace(value);
    },

    /** Return one InfoWorkspace from one JSON value. */
    fromJson(value: Json): InfoWorkspace {
        return fromJsonInfoWorkspace(value);
    },
};

/** Encode one InfoWorkspace. */
export function encodeInfoWorkspace(writer: BinaryWriter, value: InfoWorkspace): void {
    writer.writeString(value.root);
    writer.writeString(value.kind);
    writer.writeUnsigned(value.packages.length);
    for (const item2 of value.packages) {
        writer.writeString(item2);
    }
}

/** Decode one InfoWorkspace. */
export function decodeInfoWorkspace(reader: BinaryReader): InfoWorkspace {
    const root = reader.readString();
    const kind = reader.readString();
    const packages = (() => { const length2 = reader.readNumber(); const items2: Array<string> = []; for (let index = 0; index < length2; index += 1) { items2.push(reader.readString()); } return items2; })();

    return {
        root,
        kind,
        packages,
    };
}

/** Return one JSON value for one InfoWorkspace. */
export function toJsonInfoWorkspace(value: InfoWorkspace): Json {
    return {
        root: value.root,
        kind: value.kind,
        packages: value.packages.map((item0) => item0),
    };
}

/** Return one InfoWorkspace from one JSON value. */
export function fromJsonInfoWorkspace(value: Json): InfoWorkspace {
    const object = jsonObject(value);

    return {
        root: jsonString(jsonField(object, "root")),
        kind: jsonString(jsonField(object, "kind")),
        packages: jsonArray(jsonField(object, "packages")).map((item0) => jsonString(item0)),
    };
}
