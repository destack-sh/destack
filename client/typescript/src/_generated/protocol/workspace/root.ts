/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, jsonString } from "../../../protocol/serde.js";

/** Reason for reloading host source state. */
export type ReloadReason = "manual" | "overflow" | "watch";

export const ReloadReason = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ReloadReason): void {
        encodeReloadReason(writer, value);
    },

    /** Decode one ReloadReason. */
    decode(reader: BinaryReader): ReloadReason {
        return decodeReloadReason(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ReloadReason): Json {
        return toJsonReloadReason(value);
    },

    /** Return one ReloadReason from one JSON value. */
    fromJson(value: Json): ReloadReason {
        return fromJsonReloadReason(value);
    },
};

/** Encode one ReloadReason. */
export function encodeReloadReason(writer: BinaryWriter, value: ReloadReason): void {
    switch (value) {
        case "manual":
            writer.writeUnsigned(0);
            return;
        case "overflow":
            writer.writeUnsigned(1);
            return;
        case "watch":
            writer.writeUnsigned(2);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one ReloadReason. */
export function decodeReloadReason(reader: BinaryReader): ReloadReason {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "manual";
        case 1:
            return "overflow";
        case 2:
            return "watch";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one ReloadReason. */
export function toJsonReloadReason(value: ReloadReason): Json {
    return value;
}

/** Return one ReloadReason from one JSON value. */
export function fromJsonReloadReason(value: Json): ReloadReason {
    const variant = jsonString(value);

    switch (variant) {
        case "manual":
            return "manual";
        case "overflow":
            return "overflow";
        case "watch":
            return "watch";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}
