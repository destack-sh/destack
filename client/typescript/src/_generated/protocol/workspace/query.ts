/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, jsonField, jsonObject, jsonString } from "../../../protocol/serde.js";
import type { QueryRequest } from "../../query/protocol/message.js";
import type { QueryResponse } from "../../query/protocol/message.js";
import type { Revision } from "../../repository/revision.js";
import { decodeQueryRequest, encodeQueryRequest, fromJsonQueryRequest, toJsonQueryRequest } from "../../query/protocol/message.js";
import { decodeQueryResponse, encodeQueryResponse, fromJsonQueryResponse, toJsonQueryResponse } from "../../query/protocol/message.js";
import { decodeRevision, encodeRevision, fromJsonRevision, toJsonRevision } from "../../repository/revision.js";

/** Revision selection policy for one query. */
export type RevisionPolicy =
    /** Use the ref's latest revision when execution starts. */
    | {
          readonly kind: "latest";
      }
    /** Use one exact immutable revision. */
    | {
          readonly kind: "exact";
          readonly exact: Revision;
      }
    /** Use one revision only if the ref still points at it. */
    | {
          readonly kind: "current";
          readonly current: Revision;
      }
;

export const RevisionPolicy = {
    /** Use the ref's latest revision when execution starts. */
    latest(): RevisionPolicy {
        return { kind: "latest" };
    },

    /** Use one exact immutable revision. */
    exact(exact: Revision): RevisionPolicy {
        return { kind: "exact", exact };
    },

    /** Use one revision only if the ref still points at it. */
    current(current: Revision): RevisionPolicy {
        return { kind: "current", current };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: RevisionPolicy): void {
        encodeRevisionPolicy(writer, value);
    },

    /** Decode one RevisionPolicy. */
    decode(reader: BinaryReader): RevisionPolicy {
        return decodeRevisionPolicy(reader);
    },

    /** Return this value as JSON. */
    toJson(value: RevisionPolicy): Json {
        return toJsonRevisionPolicy(value);
    },

    /** Return one RevisionPolicy from one JSON value. */
    fromJson(value: Json): RevisionPolicy {
        return fromJsonRevisionPolicy(value);
    },
};

/** Encode one RevisionPolicy. */
export function encodeRevisionPolicy(writer: BinaryWriter, value: RevisionPolicy): void {
    switch (value.kind) {
        case "latest":
            writer.writeUnsigned(0);
            return;
        case "exact":
            writer.writeUnsigned(1);
            encodeRevision(writer, value.exact);
            return;
        case "current":
            writer.writeUnsigned(2);
            encodeRevision(writer, value.current);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one RevisionPolicy. */
export function decodeRevisionPolicy(reader: BinaryReader): RevisionPolicy {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            return { kind: "latest" };
        }
        case 1: {
            const exact = decodeRevision(reader);

            return { kind: "exact", exact };
        }
        case 2: {
            const current = decodeRevision(reader);

            return { kind: "current", current };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one RevisionPolicy. */
export function toJsonRevisionPolicy(value: RevisionPolicy): Json {
    switch (value.kind) {
        case "latest":
            return {
                kind: "latest",
            };
        case "exact":
            return {
                kind: "exact",
                exact: toJsonRevision(value.exact),
            };
        case "current":
            return {
                kind: "current",
                current: toJsonRevision(value.current),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one RevisionPolicy from one JSON value. */
export function fromJsonRevisionPolicy(value: Json): RevisionPolicy {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "latest":
            return {
                kind,
            };
        case "exact":
            return {
                kind,
                exact: fromJsonRevision(jsonField(object, "exact")),
            };
        case "current":
            return {
                kind,
                current: fromJsonRevision(jsonField(object, "current")),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** Request to run one semantic query. */
export type RunQueryRequest = {
    /** Revision selection for this query. */
    readonly revision: RevisionPolicy;
    /** The query request. */
    readonly request: QueryRequest;
};

export const RunQueryRequest = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: RunQueryRequest): void {
        encodeRunQueryRequest(writer, value);
    },

    /** Decode one RunQueryRequest. */
    decode(reader: BinaryReader): RunQueryRequest {
        return decodeRunQueryRequest(reader);
    },

    /** Return this value as JSON. */
    toJson(value: RunQueryRequest): Json {
        return toJsonRunQueryRequest(value);
    },

    /** Return one RunQueryRequest from one JSON value. */
    fromJson(value: Json): RunQueryRequest {
        return fromJsonRunQueryRequest(value);
    },
};

/** Encode one RunQueryRequest. */
export function encodeRunQueryRequest(writer: BinaryWriter, value: RunQueryRequest): void {
    encodeRevisionPolicy(writer, value.revision);
    encodeQueryRequest(writer, value.request);
}

/** Decode one RunQueryRequest. */
export function decodeRunQueryRequest(reader: BinaryReader): RunQueryRequest {
    const revision = decodeRevisionPolicy(reader);
    const request = decodeQueryRequest(reader);

    return {
        revision,
        request,
    };
}

/** Return one JSON value for one RunQueryRequest. */
export function toJsonRunQueryRequest(value: RunQueryRequest): Json {
    return {
        revision: toJsonRevisionPolicy(value.revision),
        request: toJsonQueryRequest(value.request),
    };
}

/** Return one RunQueryRequest from one JSON value. */
export function fromJsonRunQueryRequest(value: Json): RunQueryRequest {
    const object = jsonObject(value);

    return {
        revision: fromJsonRevisionPolicy(jsonField(object, "revision")),
        request: fromJsonQueryRequest(jsonField(object, "request")),
    };
}

/** Response from one semantic query. */
export type RunQueryResponse = {
    /** The revision used for query execution. */
    readonly revision: Revision;
    /** The query response. */
    readonly response: QueryResponse;
};

export const RunQueryResponse = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: RunQueryResponse): void {
        encodeRunQueryResponse(writer, value);
    },

    /** Decode one RunQueryResponse. */
    decode(reader: BinaryReader): RunQueryResponse {
        return decodeRunQueryResponse(reader);
    },

    /** Return this value as JSON. */
    toJson(value: RunQueryResponse): Json {
        return toJsonRunQueryResponse(value);
    },

    /** Return one RunQueryResponse from one JSON value. */
    fromJson(value: Json): RunQueryResponse {
        return fromJsonRunQueryResponse(value);
    },
};

/** Encode one RunQueryResponse. */
export function encodeRunQueryResponse(writer: BinaryWriter, value: RunQueryResponse): void {
    encodeRevision(writer, value.revision);
    encodeQueryResponse(writer, value.response);
}

/** Decode one RunQueryResponse. */
export function decodeRunQueryResponse(reader: BinaryReader): RunQueryResponse {
    const revision = decodeRevision(reader);
    const response = decodeQueryResponse(reader);

    return {
        revision,
        response,
    };
}

/** Return one JSON value for one RunQueryResponse. */
export function toJsonRunQueryResponse(value: RunQueryResponse): Json {
    return {
        revision: toJsonRevision(value.revision),
        response: toJsonQueryResponse(value.response),
    };
}

/** Return one RunQueryResponse from one JSON value. */
export function fromJsonRunQueryResponse(value: Json): RunQueryResponse {
    const object = jsonObject(value);

    return {
        revision: fromJsonRevision(jsonField(object, "revision")),
        response: fromJsonQueryResponse(jsonField(object, "response")),
    };
}
