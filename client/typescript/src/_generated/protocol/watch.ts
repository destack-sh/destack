/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, jsonArray, jsonBigint, jsonBool, jsonField, jsonInteger, jsonObject, jsonOptional, jsonString } from "../../protocol/serde.js";
import type { UpdateBatch } from "./workspace/message.js";
import type { RootId } from "./root.js";
import type { ReloadReason } from "./workspace/root.js";
import { decodeUpdateBatch, encodeUpdateBatch, fromJsonUpdateBatch, toJsonUpdateBatch } from "./workspace/message.js";
import { decodeRootId, encodeRootId, fromJsonRootId, toJsonRootId } from "./root.js";
import { decodeReloadReason, encodeReloadReason, fromJsonReloadReason, toJsonReloadReason } from "./workspace/root.js";

/** Request to start watching roots through a handle. */
export type WatchStartRequest = {
    /** Root handle. */
    readonly handle: RootId;
    /** Roots watched through this handle. */
    readonly roots: ReadonlyArray<string>;
    /** Watch start options. */
    readonly options: WatchStartOptions;
};

export const WatchStartRequest = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: WatchStartRequest): void {
        encodeWatchStartRequest(writer, value);
    },

    /** Decode one WatchStartRequest. */
    decode(reader: BinaryReader): WatchStartRequest {
        return decodeWatchStartRequest(reader);
    },

    /** Return this value as JSON. */
    toJson(value: WatchStartRequest): Json {
        return toJsonWatchStartRequest(value);
    },

    /** Return one WatchStartRequest from one JSON value. */
    fromJson(value: Json): WatchStartRequest {
        return fromJsonWatchStartRequest(value);
    },
};

/** Encode one WatchStartRequest. */
export function encodeWatchStartRequest(writer: BinaryWriter, value: WatchStartRequest): void {
    encodeRootId(writer, value.handle);
    writer.writeUnsigned(value.roots.length);
    for (const item1 of value.roots) {
        writer.writeString(item1);
    }
    encodeWatchStartOptions(writer, value.options);
}

/** Decode one WatchStartRequest. */
export function decodeWatchStartRequest(reader: BinaryReader): WatchStartRequest {
    const handle = decodeRootId(reader);
    const roots = (() => { const length1 = reader.readNumber(); const items1: Array<string> = []; for (let index = 0; index < length1; index += 1) { items1.push(reader.readString()); } return items1; })();
    const options = decodeWatchStartOptions(reader);

    return {
        handle,
        roots,
        options,
    };
}

/** Return one JSON value for one WatchStartRequest. */
export function toJsonWatchStartRequest(value: WatchStartRequest): Json {
    return {
        handle: toJsonRootId(value.handle),
        roots: value.roots.map((item0) => item0),
        options: toJsonWatchStartOptions(value.options),
    };
}

/** Return one WatchStartRequest from one JSON value. */
export function fromJsonWatchStartRequest(value: Json): WatchStartRequest {
    const object = jsonObject(value);

    return {
        handle: fromJsonRootId(jsonField(object, "handle")),
        roots: jsonArray(jsonField(object, "roots")).map((item0) => jsonString(item0)),
        options: fromJsonWatchStartOptions(jsonField(object, "options")),
    };
}

/** Options for starting a workspace watch. */
export type WatchStartOptions = {
    /** Maximum time to coalesce events in milliseconds. */
    readonly coalesceWindowMs: bigint;
    /** Maximum event and status count per batch. */
    readonly maxBatchSize: number;
};

export const WatchStartOptions = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: WatchStartOptions): void {
        encodeWatchStartOptions(writer, value);
    },

    /** Decode one WatchStartOptions. */
    decode(reader: BinaryReader): WatchStartOptions {
        return decodeWatchStartOptions(reader);
    },

    /** Return this value as JSON. */
    toJson(value: WatchStartOptions): Json {
        return toJsonWatchStartOptions(value);
    },

    /** Return one WatchStartOptions from one JSON value. */
    fromJson(value: Json): WatchStartOptions {
        return fromJsonWatchStartOptions(value);
    },
};

/** Encode one WatchStartOptions. */
export function encodeWatchStartOptions(writer: BinaryWriter, value: WatchStartOptions): void {
    writer.writeUnsigned(value.coalesceWindowMs);
    writer.writeUnsigned(value.maxBatchSize);
}

/** Decode one WatchStartOptions. */
export function decodeWatchStartOptions(reader: BinaryReader): WatchStartOptions {
    const coalesceWindowMs = reader.readUnsigned();
    const maxBatchSize = reader.readNumber();

    return {
        coalesceWindowMs,
        maxBatchSize,
    };
}

/** Return one JSON value for one WatchStartOptions. */
export function toJsonWatchStartOptions(value: WatchStartOptions): Json {
    return {
        coalesceWindowMs: value.coalesceWindowMs.toString(),
        maxBatchSize: value.maxBatchSize,
    };
}

/** Return one WatchStartOptions from one JSON value. */
export function fromJsonWatchStartOptions(value: Json): WatchStartOptions {
    const object = jsonObject(value);

    return {
        coalesceWindowMs: jsonBigint(jsonField(object, "coalesceWindowMs")),
        maxBatchSize: jsonInteger(jsonField(object, "maxBatchSize")),
    };
}

/** Request to receive and apply the next watch batch. */
export type WatchNextRequest = {
    /** Root handle. */
    readonly handle: RootId;
};

export const WatchNextRequest = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: WatchNextRequest): void {
        encodeWatchNextRequest(writer, value);
    },

    /** Decode one WatchNextRequest. */
    decode(reader: BinaryReader): WatchNextRequest {
        return decodeWatchNextRequest(reader);
    },

    /** Return this value as JSON. */
    toJson(value: WatchNextRequest): Json {
        return toJsonWatchNextRequest(value);
    },

    /** Return one WatchNextRequest from one JSON value. */
    fromJson(value: Json): WatchNextRequest {
        return fromJsonWatchNextRequest(value);
    },
};

/** Encode one WatchNextRequest. */
export function encodeWatchNextRequest(writer: BinaryWriter, value: WatchNextRequest): void {
    encodeRootId(writer, value.handle);
}

/** Decode one WatchNextRequest. */
export function decodeWatchNextRequest(reader: BinaryReader): WatchNextRequest {
    const handle = decodeRootId(reader);

    return {
        handle,
    };
}

/** Return one JSON value for one WatchNextRequest. */
export function toJsonWatchNextRequest(value: WatchNextRequest): Json {
    return {
        handle: toJsonRootId(value.handle),
    };
}

/** Return one WatchNextRequest from one JSON value. */
export function fromJsonWatchNextRequest(value: Json): WatchNextRequest {
    const object = jsonObject(value);

    return {
        handle: fromJsonRootId(jsonField(object, "handle")),
    };
}

/** Request to stop watching roots through a handle. */
export type WatchStopRequest = {
    /** Root handle. */
    readonly handle: RootId;
};

export const WatchStopRequest = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: WatchStopRequest): void {
        encodeWatchStopRequest(writer, value);
    },

    /** Decode one WatchStopRequest. */
    decode(reader: BinaryReader): WatchStopRequest {
        return decodeWatchStopRequest(reader);
    },

    /** Return this value as JSON. */
    toJson(value: WatchStopRequest): Json {
        return toJsonWatchStopRequest(value);
    },

    /** Return one WatchStopRequest from one JSON value. */
    fromJson(value: Json): WatchStopRequest {
        return fromJsonWatchStopRequest(value);
    },
};

/** Encode one WatchStopRequest. */
export function encodeWatchStopRequest(writer: BinaryWriter, value: WatchStopRequest): void {
    encodeRootId(writer, value.handle);
}

/** Decode one WatchStopRequest. */
export function decodeWatchStopRequest(reader: BinaryReader): WatchStopRequest {
    const handle = decodeRootId(reader);

    return {
        handle,
    };
}

/** Return one JSON value for one WatchStopRequest. */
export function toJsonWatchStopRequest(value: WatchStopRequest): Json {
    return {
        handle: toJsonRootId(value.handle),
    };
}

/** Return one WatchStopRequest from one JSON value. */
export function fromJsonWatchStopRequest(value: Json): WatchStopRequest {
    const object = jsonObject(value);

    return {
        handle: fromJsonRootId(jsonField(object, "handle")),
    };
}

/** Response for watch start requests. */
export type WatchStartedResponse = {
    /** Root handle. */
    readonly handle: RootId;
};

export const WatchStartedResponse = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: WatchStartedResponse): void {
        encodeWatchStartedResponse(writer, value);
    },

    /** Decode one WatchStartedResponse. */
    decode(reader: BinaryReader): WatchStartedResponse {
        return decodeWatchStartedResponse(reader);
    },

    /** Return this value as JSON. */
    toJson(value: WatchStartedResponse): Json {
        return toJsonWatchStartedResponse(value);
    },

    /** Return one WatchStartedResponse from one JSON value. */
    fromJson(value: Json): WatchStartedResponse {
        return fromJsonWatchStartedResponse(value);
    },
};

/** Encode one WatchStartedResponse. */
export function encodeWatchStartedResponse(writer: BinaryWriter, value: WatchStartedResponse): void {
    encodeRootId(writer, value.handle);
}

/** Decode one WatchStartedResponse. */
export function decodeWatchStartedResponse(reader: BinaryReader): WatchStartedResponse {
    const handle = decodeRootId(reader);

    return {
        handle,
    };
}

/** Return one JSON value for one WatchStartedResponse. */
export function toJsonWatchStartedResponse(value: WatchStartedResponse): Json {
    return {
        handle: toJsonRootId(value.handle),
    };
}

/** Return one WatchStartedResponse from one JSON value. */
export function fromJsonWatchStartedResponse(value: Json): WatchStartedResponse {
    const object = jsonObject(value);

    return {
        handle: fromJsonRootId(jsonField(object, "handle")),
    };
}

/** Response for watch batch processing. */
export type WatchBatchResponse = {
    /** Root handle. */
    readonly handle: RootId;
    /** Watch batch received from the workspace watcher. */
    readonly batch?: WatchBatch;
    /** Updates produced by the batch. */
    readonly updates: UpdateBatch;
};

export const WatchBatchResponse = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: WatchBatchResponse): void {
        encodeWatchBatchResponse(writer, value);
    },

    /** Decode one WatchBatchResponse. */
    decode(reader: BinaryReader): WatchBatchResponse {
        return decodeWatchBatchResponse(reader);
    },

    /** Return this value as JSON. */
    toJson(value: WatchBatchResponse): Json {
        return toJsonWatchBatchResponse(value);
    },

    /** Return one WatchBatchResponse from one JSON value. */
    fromJson(value: Json): WatchBatchResponse {
        return fromJsonWatchBatchResponse(value);
    },
};

/** Encode one WatchBatchResponse. */
export function encodeWatchBatchResponse(writer: BinaryWriter, value: WatchBatchResponse): void {
    encodeRootId(writer, value.handle);
    writer.writeOption(value.batch, (value1) => {
        encodeWatchBatch(writer, value1);
    });
    encodeUpdateBatch(writer, value.updates);
}

/** Decode one WatchBatchResponse. */
export function decodeWatchBatchResponse(reader: BinaryReader): WatchBatchResponse {
    const handle = decodeRootId(reader);
    const batch = reader.readOption(() => decodeWatchBatch(reader));
    const updates = decodeUpdateBatch(reader);

    return {
        handle,
        ...(batch === undefined ? {} : { batch }),
        updates,
    };
}

/** Return one JSON value for one WatchBatchResponse. */
export function toJsonWatchBatchResponse(value: WatchBatchResponse): Json {
    return {
        handle: toJsonRootId(value.handle),
        ...(value.batch === undefined ? {} : { batch: toJsonWatchBatch(value.batch) }),
        updates: toJsonUpdateBatch(value.updates),
    };
}

/** Return one WatchBatchResponse from one JSON value. */
export function fromJsonWatchBatchResponse(value: Json): WatchBatchResponse {
    const object = jsonObject(value);

    return {
        handle: fromJsonRootId(jsonField(object, "handle")),
        batch: jsonOptional(object, "batch", (value) => fromJsonWatchBatch(value)),
        updates: fromJsonUpdateBatch(jsonField(object, "updates")),
    };
}

/** Watch batch payload used in the protocol. */
export type WatchBatch = {
    /** List of file events in the batch. */
    readonly events: ReadonlyArray<WatchEvent>;
    /** Status updates emitted by the watcher. */
    readonly status: ReadonlyArray<WatchStatus>;
    /** Whether overflow occurred. */
    readonly overflowed: boolean;
    /** Batch start timestamp in relative nanoseconds. */
    readonly startedAtNs: bigint;
    /** Batch end timestamp in relative nanoseconds. */
    readonly endedAtNs: bigint;
};

export const WatchBatch = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: WatchBatch): void {
        encodeWatchBatch(writer, value);
    },

    /** Decode one WatchBatch. */
    decode(reader: BinaryReader): WatchBatch {
        return decodeWatchBatch(reader);
    },

    /** Return this value as JSON. */
    toJson(value: WatchBatch): Json {
        return toJsonWatchBatch(value);
    },

    /** Return one WatchBatch from one JSON value. */
    fromJson(value: Json): WatchBatch {
        return fromJsonWatchBatch(value);
    },
};

/** Encode one WatchBatch. */
export function encodeWatchBatch(writer: BinaryWriter, value: WatchBatch): void {
    writer.writeUnsigned(value.events.length);
    for (const item0 of value.events) {
        encodeWatchEvent(writer, item0);
    }
    writer.writeUnsigned(value.status.length);
    for (const item1 of value.status) {
        encodeWatchStatus(writer, item1);
    }
    writer.writeBool(value.overflowed);
    writer.writeUnsigned(value.startedAtNs);
    writer.writeUnsigned(value.endedAtNs);
}

/** Decode one WatchBatch. */
export function decodeWatchBatch(reader: BinaryReader): WatchBatch {
    const events = (() => { const length0 = reader.readNumber(); const items0: Array<WatchEvent> = []; for (let index = 0; index < length0; index += 1) { items0.push(decodeWatchEvent(reader)); } return items0; })();
    const status = (() => { const length1 = reader.readNumber(); const items1: Array<WatchStatus> = []; for (let index = 0; index < length1; index += 1) { items1.push(decodeWatchStatus(reader)); } return items1; })();
    const overflowed = reader.readBool();
    const startedAtNs = reader.readUnsigned();
    const endedAtNs = reader.readUnsigned();

    return {
        events,
        status,
        overflowed,
        startedAtNs,
        endedAtNs,
    };
}

/** Return one JSON value for one WatchBatch. */
export function toJsonWatchBatch(value: WatchBatch): Json {
    return {
        events: value.events.map((item0) => toJsonWatchEvent(item0)),
        status: value.status.map((item0) => toJsonWatchStatus(item0)),
        overflowed: value.overflowed,
        startedAtNs: value.startedAtNs.toString(),
        endedAtNs: value.endedAtNs.toString(),
    };
}

/** Return one WatchBatch from one JSON value. */
export function fromJsonWatchBatch(value: Json): WatchBatch {
    const object = jsonObject(value);

    return {
        events: jsonArray(jsonField(object, "events")).map((item0) => fromJsonWatchEvent(item0)),
        status: jsonArray(jsonField(object, "status")).map((item0) => fromJsonWatchStatus(item0)),
        overflowed: jsonBool(jsonField(object, "overflowed")),
        startedAtNs: jsonBigint(jsonField(object, "startedAtNs")),
        endedAtNs: jsonBigint(jsonField(object, "endedAtNs")),
    };
}

/** Watch event payload. */
export type WatchEvent = {
    /** Event path. */
    readonly path: string;
    /** Optional previous path for renames. */
    readonly previousPath?: string;
    /** Event kind. */
    readonly kind: WatchEventKind;
};

export const WatchEvent = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: WatchEvent): void {
        encodeWatchEvent(writer, value);
    },

    /** Decode one WatchEvent. */
    decode(reader: BinaryReader): WatchEvent {
        return decodeWatchEvent(reader);
    },

    /** Return this value as JSON. */
    toJson(value: WatchEvent): Json {
        return toJsonWatchEvent(value);
    },

    /** Return one WatchEvent from one JSON value. */
    fromJson(value: Json): WatchEvent {
        return fromJsonWatchEvent(value);
    },
};

/** Encode one WatchEvent. */
export function encodeWatchEvent(writer: BinaryWriter, value: WatchEvent): void {
    writer.writeString(value.path);
    writer.writeOption(value.previousPath, (value1) => {
        writer.writeString(value1);
    });
    encodeWatchEventKind(writer, value.kind);
}

/** Decode one WatchEvent. */
export function decodeWatchEvent(reader: BinaryReader): WatchEvent {
    const path = reader.readString();
    const previousPath = reader.readOption(() => reader.readString());
    const kind = decodeWatchEventKind(reader);

    return {
        path,
        ...(previousPath === undefined ? {} : { previousPath }),
        kind,
    };
}

/** Return one JSON value for one WatchEvent. */
export function toJsonWatchEvent(value: WatchEvent): Json {
    return {
        path: value.path,
        ...(value.previousPath === undefined ? {} : { previousPath: value.previousPath }),
        kind: toJsonWatchEventKind(value.kind),
    };
}

/** Return one WatchEvent from one JSON value. */
export function fromJsonWatchEvent(value: Json): WatchEvent {
    const object = jsonObject(value);

    return {
        path: jsonString(jsonField(object, "path")),
        previousPath: jsonOptional(object, "previousPath", (value) => jsonString(value)),
        kind: fromJsonWatchEventKind(jsonField(object, "kind")),
    };
}

/** Watch event kind. */
export type WatchEventKind = "created" | "modified" | "deleted" | "renamed" | "overflow";

export const WatchEventKind = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: WatchEventKind): void {
        encodeWatchEventKind(writer, value);
    },

    /** Decode one WatchEventKind. */
    decode(reader: BinaryReader): WatchEventKind {
        return decodeWatchEventKind(reader);
    },

    /** Return this value as JSON. */
    toJson(value: WatchEventKind): Json {
        return toJsonWatchEventKind(value);
    },

    /** Return one WatchEventKind from one JSON value. */
    fromJson(value: Json): WatchEventKind {
        return fromJsonWatchEventKind(value);
    },
};

/** Encode one WatchEventKind. */
export function encodeWatchEventKind(writer: BinaryWriter, value: WatchEventKind): void {
    switch (value) {
        case "created":
            writer.writeUnsigned(0);
            return;
        case "modified":
            writer.writeUnsigned(1);
            return;
        case "deleted":
            writer.writeUnsigned(2);
            return;
        case "renamed":
            writer.writeUnsigned(3);
            return;
        case "overflow":
            writer.writeUnsigned(4);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one WatchEventKind. */
export function decodeWatchEventKind(reader: BinaryReader): WatchEventKind {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "created";
        case 1:
            return "modified";
        case 2:
            return "deleted";
        case 3:
            return "renamed";
        case 4:
            return "overflow";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one WatchEventKind. */
export function toJsonWatchEventKind(value: WatchEventKind): Json {
    return value;
}

/** Return one WatchEventKind from one JSON value. */
export function fromJsonWatchEventKind(value: Json): WatchEventKind {
    const variant = jsonString(value);

    switch (variant) {
        case "created":
            return "created";
        case "modified":
            return "modified";
        case "deleted":
            return "deleted";
        case "renamed":
            return "renamed";
        case "overflow":
            return "overflow";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** Watch status update. */
export type WatchStatus =
    /** Watcher is ready. */
    | {
          readonly kind: "ready";
          readonly roots: ReadonlyArray<string>;
      }
    /** Watcher requests a filesystem reload. */
    | {
          readonly kind: "reloadRequested";
          /** Watch roots for the reload. */
          readonly roots: ReadonlyArray<string>;
          /** Reload reason. */
          readonly reason: ReloadReason;
      }
    /** Watcher encountered an error. */
    | {
          readonly kind: "error";
          readonly message: string;
      }
    /** Watcher stopped. */
    | {
          readonly kind: "stopped";
      }
;

export const WatchStatus = {
    /** Watcher is ready. */
    ready(roots: ReadonlyArray<string>): WatchStatus {
        return { kind: "ready", roots };
    },

    /** Watcher requests a filesystem reload. */
    reloadRequested(roots: ReadonlyArray<string>, reason: ReloadReason): WatchStatus {
        return { kind: "reloadRequested", roots, reason };
    },

    /** Watcher encountered an error. */
    error(message: string): WatchStatus {
        return { kind: "error", message };
    },

    /** Watcher stopped. */
    stopped(): WatchStatus {
        return { kind: "stopped" };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: WatchStatus): void {
        encodeWatchStatus(writer, value);
    },

    /** Decode one WatchStatus. */
    decode(reader: BinaryReader): WatchStatus {
        return decodeWatchStatus(reader);
    },

    /** Return this value as JSON. */
    toJson(value: WatchStatus): Json {
        return toJsonWatchStatus(value);
    },

    /** Return one WatchStatus from one JSON value. */
    fromJson(value: Json): WatchStatus {
        return fromJsonWatchStatus(value);
    },
};

/** Encode one WatchStatus. */
export function encodeWatchStatus(writer: BinaryWriter, value: WatchStatus): void {
    switch (value.kind) {
        case "ready":
            writer.writeUnsigned(0);
            writer.writeUnsigned(value.roots.length);
            for (const item0 of value.roots) {
                writer.writeString(item0);
            }
            return;
        case "reloadRequested":
            writer.writeUnsigned(1);
            writer.writeUnsigned(value.roots.length);
            for (const item0 of value.roots) {
                writer.writeString(item0);
            }
            encodeReloadReason(writer, value.reason);
            return;
        case "error":
            writer.writeUnsigned(2);
            writer.writeString(value.message);
            return;
        case "stopped":
            writer.writeUnsigned(3);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one WatchStatus. */
export function decodeWatchStatus(reader: BinaryReader): WatchStatus {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const roots = (() => { const length0 = reader.readNumber(); const items0: Array<string> = []; for (let index = 0; index < length0; index += 1) { items0.push(reader.readString()); } return items0; })();

            return {
                kind: "ready",
                roots,
            };
        }
        case 1: {
            const roots = (() => { const length0 = reader.readNumber(); const items0: Array<string> = []; for (let index = 0; index < length0; index += 1) { items0.push(reader.readString()); } return items0; })();
            const reason = decodeReloadReason(reader);

            return {
                kind: "reloadRequested",
                roots,
                reason,
            };
        }
        case 2: {
            const message = reader.readString();

            return {
                kind: "error",
                message,
            };
        }
        case 3: {
            return { kind: "stopped" };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one WatchStatus. */
export function toJsonWatchStatus(value: WatchStatus): Json {
    switch (value.kind) {
        case "ready":
            return {
                kind: "ready",
                roots: value.roots.map((item0) => item0),
            };
        case "reloadRequested":
            return {
                kind: "reloadRequested",
                roots: value.roots.map((item0) => item0),
                reason: toJsonReloadReason(value.reason),
            };
        case "error":
            return {
                kind: "error",
                message: value.message,
            };
        case "stopped":
            return {
                kind: "stopped",
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one WatchStatus from one JSON value. */
export function fromJsonWatchStatus(value: Json): WatchStatus {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "ready":
            return {
                kind,
                roots: jsonArray(jsonField(object, "roots")).map((item0) => jsonString(item0)),
            };
        case "reloadRequested":
            return {
                kind,
                roots: jsonArray(jsonField(object, "roots")).map((item0) => jsonString(item0)),
                reason: fromJsonReloadReason(jsonField(object, "reason")),
            };
        case "error":
            return {
                kind,
                message: jsonString(jsonField(object, "message")),
            };
        case "stopped":
            return {
                kind,
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** Response for watch stop requests. */
export type WatchStoppedResponse = {
    /** Root handle. */
    readonly handle: RootId;
};

export const WatchStoppedResponse = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: WatchStoppedResponse): void {
        encodeWatchStoppedResponse(writer, value);
    },

    /** Decode one WatchStoppedResponse. */
    decode(reader: BinaryReader): WatchStoppedResponse {
        return decodeWatchStoppedResponse(reader);
    },

    /** Return this value as JSON. */
    toJson(value: WatchStoppedResponse): Json {
        return toJsonWatchStoppedResponse(value);
    },

    /** Return one WatchStoppedResponse from one JSON value. */
    fromJson(value: Json): WatchStoppedResponse {
        return fromJsonWatchStoppedResponse(value);
    },
};

/** Encode one WatchStoppedResponse. */
export function encodeWatchStoppedResponse(writer: BinaryWriter, value: WatchStoppedResponse): void {
    encodeRootId(writer, value.handle);
}

/** Decode one WatchStoppedResponse. */
export function decodeWatchStoppedResponse(reader: BinaryReader): WatchStoppedResponse {
    const handle = decodeRootId(reader);

    return {
        handle,
    };
}

/** Return one JSON value for one WatchStoppedResponse. */
export function toJsonWatchStoppedResponse(value: WatchStoppedResponse): Json {
    return {
        handle: toJsonRootId(value.handle),
    };
}

/** Return one WatchStoppedResponse from one JSON value. */
export function fromJsonWatchStoppedResponse(value: Json): WatchStoppedResponse {
    const object = jsonObject(value);

    return {
        handle: fromJsonRootId(jsonField(object, "handle")),
    };
}
