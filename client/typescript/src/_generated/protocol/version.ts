/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, jsonField, jsonInteger, jsonObject } from "../../protocol/serde.js";

/** Protocol version range used for negotiation. */
export type ProtocolRange = {
    /** Minimum supported version. */
    readonly min: ProtocolVersion;
    /** Maximum supported version. */
    readonly max: ProtocolVersion;
};

export const ProtocolRange = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ProtocolRange): void {
        encodeProtocolRange(writer, value);
    },

    /** Decode one ProtocolRange. */
    decode(reader: BinaryReader): ProtocolRange {
        return decodeProtocolRange(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ProtocolRange): Json {
        return toJsonProtocolRange(value);
    },

    /** Return one ProtocolRange from one JSON value. */
    fromJson(value: Json): ProtocolRange {
        return fromJsonProtocolRange(value);
    },
};

/** Encode one ProtocolRange. */
export function encodeProtocolRange(writer: BinaryWriter, value: ProtocolRange): void {
    encodeProtocolVersion(writer, value.min);
    encodeProtocolVersion(writer, value.max);
}

/** Decode one ProtocolRange. */
export function decodeProtocolRange(reader: BinaryReader): ProtocolRange {
    const min = decodeProtocolVersion(reader);
    const max = decodeProtocolVersion(reader);

    return {
        min,
        max,
    };
}

/** Return one JSON value for one ProtocolRange. */
export function toJsonProtocolRange(value: ProtocolRange): Json {
    return {
        min: toJsonProtocolVersion(value.min),
        max: toJsonProtocolVersion(value.max),
    };
}

/** Return one ProtocolRange from one JSON value. */
export function fromJsonProtocolRange(value: Json): ProtocolRange {
    const object = jsonObject(value);

    return {
        min: fromJsonProtocolVersion(jsonField(object, "min")),
        max: fromJsonProtocolVersion(jsonField(object, "max")),
    };
}

/** Version identifier for the workspace protocol. */
export type ProtocolVersion = {
    /** Major version for breaking changes. */
    readonly major: number;
    /** Minor version for backward compatible changes. */
    readonly minor: number;
    /** Patch version for fixes and clarifications. */
    readonly patch: number;
};

export const ProtocolVersion = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ProtocolVersion): void {
        encodeProtocolVersion(writer, value);
    },

    /** Decode one ProtocolVersion. */
    decode(reader: BinaryReader): ProtocolVersion {
        return decodeProtocolVersion(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ProtocolVersion): Json {
        return toJsonProtocolVersion(value);
    },

    /** Return one ProtocolVersion from one JSON value. */
    fromJson(value: Json): ProtocolVersion {
        return fromJsonProtocolVersion(value);
    },
};

/** Encode one ProtocolVersion. */
export function encodeProtocolVersion(writer: BinaryWriter, value: ProtocolVersion): void {
    writer.writeUnsigned(value.major);
    writer.writeUnsigned(value.minor);
    writer.writeUnsigned(value.patch);
}

/** Decode one ProtocolVersion. */
export function decodeProtocolVersion(reader: BinaryReader): ProtocolVersion {
    const major = reader.readNumber();
    const minor = reader.readNumber();
    const patch = reader.readNumber();

    return {
        major,
        minor,
        patch,
    };
}

/** Return one JSON value for one ProtocolVersion. */
export function toJsonProtocolVersion(value: ProtocolVersion): Json {
    return {
        major: value.major,
        minor: value.minor,
        patch: value.patch,
    };
}

/** Return one ProtocolVersion from one JSON value. */
export function fromJsonProtocolVersion(value: Json): ProtocolVersion {
    const object = jsonObject(value);

    return {
        major: jsonInteger(jsonField(object, "major")),
        minor: jsonInteger(jsonField(object, "minor")),
        patch: jsonInteger(jsonField(object, "patch")),
    };
}
