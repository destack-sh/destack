/* generated by destack_client_generate, do not edit */

import type { ClientDescriptor, ProtocolLimits } from "./handshake.js";
import type { ProtocolRange, ProtocolVersion } from "./version.js";

/** Current workspace protocol version. */
export const protocolVersion: ProtocolVersion = {
    major: 2,
    minor: 0,
    patch: 0,
};

/** Minimum compatible workspace protocol version. */
export const minProtocolVersion: ProtocolVersion = {
    major: 2,
    minor: 0,
    patch: 0,
};

/** Supported protocol range for TypeScript clients. */
export const protocolRange: ProtocolRange = {
    min: minProtocolVersion,
    max: protocolVersion,
};

/** Default protocol limits for TypeScript clients. */
export const protocolLimits: ProtocolLimits = {
    maxFrameBytes: 16777216n,
    maxPayloadBytes: 1048576n,
};

/** Default protocol client descriptor for TypeScript clients. */
export const clientDescriptor: ClientDescriptor = {
    name: "destack-typescript",
    version: "0.55.4",
};
