/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, jsonArray, jsonField, jsonObject, jsonOptional, jsonString } from "../../../protocol/serde.js";
import type { GlobalNodeIdAny } from "../../dir/tree/node.js";
import type { ModuleId } from "../../source/file/model/module.js";
import { decodeGlobalNodeIdAny, encodeGlobalNodeIdAny, fromJsonGlobalNodeIdAny, toJsonGlobalNodeIdAny } from "../../dir/tree/node.js";
import { decodeModuleId, encodeModuleId, fromJsonModuleId, toJsonModuleId } from "../../source/file/model/module.js";

/** Searchable annotation and decorator index. */
export type AnnotationIndex = {
    /** The annotation entries in stable order. */
    readonly entries: ReadonlyArray<AnnotationEntry>;
};

export const AnnotationIndex = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: AnnotationIndex): void {
        encodeAnnotationIndex(writer, value);
    },

    /** Decode one AnnotationIndex. */
    decode(reader: BinaryReader): AnnotationIndex {
        return decodeAnnotationIndex(reader);
    },

    /** Return this value as JSON. */
    toJson(value: AnnotationIndex): Json {
        return toJsonAnnotationIndex(value);
    },

    /** Return one AnnotationIndex from one JSON value. */
    fromJson(value: Json): AnnotationIndex {
        return fromJsonAnnotationIndex(value);
    },
};

/** Encode one AnnotationIndex. */
export function encodeAnnotationIndex(writer: BinaryWriter, value: AnnotationIndex): void {
    writer.writeUnsigned(value.entries.length);
    for (const item0 of value.entries) {
        encodeAnnotationEntry(writer, item0);
    }
}

/** Decode one AnnotationIndex. */
export function decodeAnnotationIndex(reader: BinaryReader): AnnotationIndex {
    const entries = (() => { const length0 = reader.readNumber(); const items0: Array<AnnotationEntry> = []; for (let index = 0; index < length0; index += 1) { items0.push(decodeAnnotationEntry(reader)); } return items0; })();

    return {
        entries,
    };
}

/** Return one JSON value for one AnnotationIndex. */
export function toJsonAnnotationIndex(value: AnnotationIndex): Json {
    return {
        entries: value.entries.map((item0) => toJsonAnnotationEntry(item0)),
    };
}

/** Return one AnnotationIndex from one JSON value. */
export function fromJsonAnnotationIndex(value: Json): AnnotationIndex {
    const object = jsonObject(value);

    return {
        entries: jsonArray(jsonField(object, "entries")).map((item0) => fromJsonAnnotationEntry(item0)),
    };
}

/** Searchable annotation or decorator entry. */
export type AnnotationEntry = {
    /** The annotation name when syntactically known. */
    readonly name?: string;
    /** The module that owns the annotation. */
    readonly moduleId: ModuleId;
    /** The decorator node. */
    readonly decoratorId: GlobalNodeIdAny;
    /** The annotated target node. */
    readonly targetId: GlobalNodeIdAny;
};

export const AnnotationEntry = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: AnnotationEntry): void {
        encodeAnnotationEntry(writer, value);
    },

    /** Decode one AnnotationEntry. */
    decode(reader: BinaryReader): AnnotationEntry {
        return decodeAnnotationEntry(reader);
    },

    /** Return this value as JSON. */
    toJson(value: AnnotationEntry): Json {
        return toJsonAnnotationEntry(value);
    },

    /** Return one AnnotationEntry from one JSON value. */
    fromJson(value: Json): AnnotationEntry {
        return fromJsonAnnotationEntry(value);
    },
};

/** Encode one AnnotationEntry. */
export function encodeAnnotationEntry(writer: BinaryWriter, value: AnnotationEntry): void {
    writer.writeOption(value.name, (value0) => {
        writer.writeString(value0);
    });
    encodeModuleId(writer, value.moduleId);
    encodeGlobalNodeIdAny(writer, value.decoratorId);
    encodeGlobalNodeIdAny(writer, value.targetId);
}

/** Decode one AnnotationEntry. */
export function decodeAnnotationEntry(reader: BinaryReader): AnnotationEntry {
    const name = reader.readOption(() => reader.readString());
    const moduleId = decodeModuleId(reader);
    const decoratorId = decodeGlobalNodeIdAny(reader);
    const targetId = decodeGlobalNodeIdAny(reader);

    return {
        ...(name === undefined ? {} : { name }),
        moduleId,
        decoratorId,
        targetId,
    };
}

/** Return one JSON value for one AnnotationEntry. */
export function toJsonAnnotationEntry(value: AnnotationEntry): Json {
    return {
        ...(value.name === undefined ? {} : { name: value.name }),
        moduleId: toJsonModuleId(value.moduleId),
        decoratorId: toJsonGlobalNodeIdAny(value.decoratorId),
        targetId: toJsonGlobalNodeIdAny(value.targetId),
    };
}

/** Return one AnnotationEntry from one JSON value. */
export function fromJsonAnnotationEntry(value: Json): AnnotationEntry {
    const object = jsonObject(value);

    return {
        name: jsonOptional(object, "name", (value) => jsonString(value)),
        moduleId: fromJsonModuleId(jsonField(object, "moduleId")),
        decoratorId: fromJsonGlobalNodeIdAny(jsonField(object, "decoratorId")),
        targetId: fromJsonGlobalNodeIdAny(jsonField(object, "targetId")),
    };
}
