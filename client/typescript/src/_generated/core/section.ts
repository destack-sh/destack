/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, jsonArray, jsonBigint, jsonField, jsonInteger, jsonObject } from "../../protocol/serde.js";

/** Section ids and byte ranges for one image. */
export type SectionDirectory = {
    /** Loadable image segments. */
    readonly segments: SegmentTable;
    /** Logical image sections. */
    readonly sections: SectionTable;
    /** Segment holding immutable entry tables. */
    readonly tableSegment: SegmentId;
    /** Number of initialized bytes in the table segment. */
    readonly tableByteLen: bigint;
};

export const SectionDirectory = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: SectionDirectory): void {
        encodeSectionDirectory(writer, value);
    },

    /** Decode one SectionDirectory. */
    decode(reader: BinaryReader): SectionDirectory {
        return decodeSectionDirectory(reader);
    },

    /** Return this value as JSON. */
    toJson(value: SectionDirectory): Json {
        return toJsonSectionDirectory(value);
    },

    /** Return one SectionDirectory from one JSON value. */
    fromJson(value: Json): SectionDirectory {
        return fromJsonSectionDirectory(value);
    },
};

/** Encode one SectionDirectory. */
export function encodeSectionDirectory(writer: BinaryWriter, value: SectionDirectory): void {
    encodeSegmentTable(writer, value.segments);
    encodeSectionTable(writer, value.sections);
    encodeSegmentId(writer, value.tableSegment);
    writer.writeUnsigned(value.tableByteLen);
}

/** Decode one SectionDirectory. */
export function decodeSectionDirectory(reader: BinaryReader): SectionDirectory {
    const segments = decodeSegmentTable(reader);
    const sections = decodeSectionTable(reader);
    const tableSegment = decodeSegmentId(reader);
    const tableByteLen = reader.readUnsigned();

    return {
        segments,
        sections,
        tableSegment,
        tableByteLen,
    };
}

/** Return one JSON value for one SectionDirectory. */
export function toJsonSectionDirectory(value: SectionDirectory): Json {
    return {
        segments: toJsonSegmentTable(value.segments),
        sections: toJsonSectionTable(value.sections),
        tableSegment: toJsonSegmentId(value.tableSegment),
        tableByteLen: value.tableByteLen.toString(),
    };
}

/** Return one SectionDirectory from one JSON value. */
export function fromJsonSectionDirectory(value: Json): SectionDirectory {
    const object = jsonObject(value);

    return {
        segments: fromJsonSegmentTable(jsonField(object, "segments")),
        sections: fromJsonSectionTable(jsonField(object, "sections")),
        tableSegment: fromJsonSegmentId(jsonField(object, "tableSegment")),
        tableByteLen: jsonBigint(jsonField(object, "tableByteLen")),
    };
}

/** Loadable segment table inside one image. */
export type SegmentTable = {
    /** Segments indexed by dense segment id. */
    readonly segments: ReadonlyArray<Segment>;
};

export const SegmentTable = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: SegmentTable): void {
        encodeSegmentTable(writer, value);
    },

    /** Decode one SegmentTable. */
    decode(reader: BinaryReader): SegmentTable {
        return decodeSegmentTable(reader);
    },

    /** Return this value as JSON. */
    toJson(value: SegmentTable): Json {
        return toJsonSegmentTable(value);
    },

    /** Return one SegmentTable from one JSON value. */
    fromJson(value: Json): SegmentTable {
        return fromJsonSegmentTable(value);
    },
};

/** Encode one SegmentTable. */
export function encodeSegmentTable(writer: BinaryWriter, value: SegmentTable): void {
    writer.writeUnsigned(value.segments.length);
    for (const item0 of value.segments) {
        encodeSegment(writer, item0);
    }
}

/** Decode one SegmentTable. */
export function decodeSegmentTable(reader: BinaryReader): SegmentTable {
    const segments = (() => { const length0 = reader.readNumber(); const items0: Array<Segment> = []; for (let index = 0; index < length0; index += 1) { items0.push(decodeSegment(reader)); } return items0; })();

    return {
        segments,
    };
}

/** Return one JSON value for one SegmentTable. */
export function toJsonSegmentTable(value: SegmentTable): Json {
    return {
        segments: value.segments.map((item0) => toJsonSegment(item0)),
    };
}

/** Return one SegmentTable from one JSON value. */
export function fromJsonSegmentTable(value: Json): SegmentTable {
    const object = jsonObject(value);

    return {
        segments: jsonArray(jsonField(object, "segments")).map((item0) => fromJsonSegment(item0)),
    };
}

/** Loadable byte segment inside one image. */
export type Segment = {
    /** Byte offset from the image base. */
    readonly offset: bigint;
    /** Byte length stored in the image. */
    readonly byteLen: bigint;
    /** Byte length reserved after loading. */
    readonly memoryLen: bigint;
    /** Required byte alignment. */
    readonly alignment: number;
    /** Segment memory access policy. */
    readonly access: SegmentAccess;
};

export const Segment = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: Segment): void {
        encodeSegment(writer, value);
    },

    /** Decode one Segment. */
    decode(reader: BinaryReader): Segment {
        return decodeSegment(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Segment): Json {
        return toJsonSegment(value);
    },

    /** Return one Segment from one JSON value. */
    fromJson(value: Json): Segment {
        return fromJsonSegment(value);
    },
};

/** Encode one Segment. */
export function encodeSegment(writer: BinaryWriter, value: Segment): void {
    writer.writeUnsigned(value.offset);
    writer.writeUnsigned(value.byteLen);
    writer.writeUnsigned(value.memoryLen);
    writer.writeUnsigned(value.alignment);
    encodeSegmentAccess(writer, value.access);
}

/** Decode one Segment. */
export function decodeSegment(reader: BinaryReader): Segment {
    const offset = reader.readUnsigned();
    const byteLen = reader.readUnsigned();
    const memoryLen = reader.readUnsigned();
    const alignment = reader.readNumber();
    const access = decodeSegmentAccess(reader);

    return {
        offset,
        byteLen,
        memoryLen,
        alignment,
        access,
    };
}

/** Return one JSON value for one Segment. */
export function toJsonSegment(value: Segment): Json {
    return {
        offset: value.offset.toString(),
        byteLen: value.byteLen.toString(),
        memoryLen: value.memoryLen.toString(),
        alignment: value.alignment,
        access: toJsonSegmentAccess(value.access),
    };
}

/** Return one Segment from one JSON value. */
export function fromJsonSegment(value: Json): Segment {
    const object = jsonObject(value);

    return {
        offset: jsonBigint(jsonField(object, "offset")),
        byteLen: jsonBigint(jsonField(object, "byteLen")),
        memoryLen: jsonBigint(jsonField(object, "memoryLen")),
        alignment: jsonInteger(jsonField(object, "alignment")),
        access: fromJsonSegmentAccess(jsonField(object, "access")),
    };
}

/** Memory access policy for one segment. */
export type SegmentAccess = {
    /** Packed access flags. */
    readonly bits: number;
};

export const SegmentAccess = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: SegmentAccess): void {
        encodeSegmentAccess(writer, value);
    },

    /** Decode one SegmentAccess. */
    decode(reader: BinaryReader): SegmentAccess {
        return decodeSegmentAccess(reader);
    },

    /** Return this value as JSON. */
    toJson(value: SegmentAccess): Json {
        return toJsonSegmentAccess(value);
    },

    /** Return one SegmentAccess from one JSON value. */
    fromJson(value: Json): SegmentAccess {
        return fromJsonSegmentAccess(value);
    },
};

/** Encode one SegmentAccess. */
export function encodeSegmentAccess(writer: BinaryWriter, value: SegmentAccess): void {
    writer.writeUnsigned(value.bits);
}

/** Decode one SegmentAccess. */
export function decodeSegmentAccess(reader: BinaryReader): SegmentAccess {
    const bits = reader.readNumber();

    return {
        bits,
    };
}

/** Return one JSON value for one SegmentAccess. */
export function toJsonSegmentAccess(value: SegmentAccess): Json {
    return {
        bits: value.bits,
    };
}

/** Return one SegmentAccess from one JSON value. */
export function fromJsonSegmentAccess(value: Json): SegmentAccess {
    const object = jsonObject(value);

    return {
        bits: jsonInteger(jsonField(object, "bits")),
    };
}

/** Logical section table inside one image. */
export type SectionTable = {
    /** Sections indexed by dense section id. */
    readonly sections: ReadonlyArray<Section>;
};

export const SectionTable = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: SectionTable): void {
        encodeSectionTable(writer, value);
    },

    /** Decode one SectionTable. */
    decode(reader: BinaryReader): SectionTable {
        return decodeSectionTable(reader);
    },

    /** Return this value as JSON. */
    toJson(value: SectionTable): Json {
        return toJsonSectionTable(value);
    },

    /** Return one SectionTable from one JSON value. */
    fromJson(value: Json): SectionTable {
        return fromJsonSectionTable(value);
    },
};

/** Encode one SectionTable. */
export function encodeSectionTable(writer: BinaryWriter, value: SectionTable): void {
    writer.writeUnsigned(value.sections.length);
    for (const item0 of value.sections) {
        encodeSection(writer, item0);
    }
}

/** Decode one SectionTable. */
export function decodeSectionTable(reader: BinaryReader): SectionTable {
    const sections = (() => { const length0 = reader.readNumber(); const items0: Array<Section> = []; for (let index = 0; index < length0; index += 1) { items0.push(decodeSection(reader)); } return items0; })();

    return {
        sections,
    };
}

/** Return one JSON value for one SectionTable. */
export function toJsonSectionTable(value: SectionTable): Json {
    return {
        sections: value.sections.map((item0) => toJsonSection(item0)),
    };
}

/** Return one SectionTable from one JSON value. */
export function fromJsonSectionTable(value: Json): SectionTable {
    const object = jsonObject(value);

    return {
        sections: jsonArray(jsonField(object, "sections")).map((item0) => fromJsonSection(item0)),
    };
}

/** Logical byte section inside one image. */
export type Section = {
    /** Segment containing this section. */
    readonly segment: SegmentId;
    /** Byte offset from the containing segment base. */
    readonly byteOffset: number;
    /** Section byte length inside the containing segment. */
    readonly byteLen: number;
    /** Required byte alignment. */
    readonly alignment: number;
};

export const Section = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: Section): void {
        encodeSection(writer, value);
    },

    /** Decode one Section. */
    decode(reader: BinaryReader): Section {
        return decodeSection(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Section): Json {
        return toJsonSection(value);
    },

    /** Return one Section from one JSON value. */
    fromJson(value: Json): Section {
        return fromJsonSection(value);
    },
};

/** Encode one Section. */
export function encodeSection(writer: BinaryWriter, value: Section): void {
    encodeSegmentId(writer, value.segment);
    writer.writeUnsigned(value.byteOffset);
    writer.writeUnsigned(value.byteLen);
    writer.writeUnsigned(value.alignment);
}

/** Decode one Section. */
export function decodeSection(reader: BinaryReader): Section {
    const segment = decodeSegmentId(reader);
    const byteOffset = reader.readNumber();
    const byteLen = reader.readNumber();
    const alignment = reader.readNumber();

    return {
        segment,
        byteOffset,
        byteLen,
        alignment,
    };
}

/** Return one JSON value for one Section. */
export function toJsonSection(value: Section): Json {
    return {
        segment: toJsonSegmentId(value.segment),
        byteOffset: value.byteOffset,
        byteLen: value.byteLen,
        alignment: value.alignment,
    };
}

/** Return one Section from one JSON value. */
export function fromJsonSection(value: Json): Section {
    const object = jsonObject(value);

    return {
        segment: fromJsonSegmentId(jsonField(object, "segment")),
        byteOffset: jsonInteger(jsonField(object, "byteOffset")),
        byteLen: jsonInteger(jsonField(object, "byteLen")),
        alignment: jsonInteger(jsonField(object, "alignment")),
    };
}

/** Dense image segment id. */
export type SegmentId = number;

export const SegmentId = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: SegmentId): void {
        encodeSegmentId(writer, value);
    },

    /** Decode one SegmentId. */
    decode(reader: BinaryReader): SegmentId {
        return decodeSegmentId(reader);
    },

    /** Return this value as JSON. */
    toJson(value: SegmentId): Json {
        return toJsonSegmentId(value);
    },

    /** Return one SegmentId from one JSON value. */
    fromJson(value: Json): SegmentId {
        return fromJsonSegmentId(value);
    },
};

/** Encode one SegmentId. */
export function encodeSegmentId(writer: BinaryWriter, value: SegmentId): void {
    writer.writeUnsigned(value);
}

/** Decode one SegmentId. */
export function decodeSegmentId(reader: BinaryReader): SegmentId {
    return reader.readNumber();
}

/** Return one JSON value for one SegmentId. */
export function toJsonSegmentId(value: SegmentId): Json {
    return value;
}

/** Return one SegmentId from one JSON value. */
export function fromJsonSegmentId(value: Json): SegmentId {
    return jsonInteger(value);
}

/** Typed entry slice stored in one image section. */
export type SectionSlice = {
    /** Section containing the typed entries. */
    readonly section: SectionId;
    /** Byte offset from the containing section base. */
    readonly byteOffset: number;
    /** Number of typed entries in the slice. */
    readonly len: number;
};

export const SectionSlice = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: SectionSlice): void {
        encodeSectionSlice(writer, value);
    },

    /** Decode one SectionSlice. */
    decode(reader: BinaryReader): SectionSlice {
        return decodeSectionSlice(reader);
    },

    /** Return this value as JSON. */
    toJson(value: SectionSlice): Json {
        return toJsonSectionSlice(value);
    },

    /** Return one SectionSlice from one JSON value. */
    fromJson(value: Json): SectionSlice {
        return fromJsonSectionSlice(value);
    },
};

/** Encode one SectionSlice. */
export function encodeSectionSlice(writer: BinaryWriter, value: SectionSlice): void {
    encodeSectionId(writer, value.section);
    writer.writeUnsigned(value.byteOffset);
    writer.writeUnsigned(value.len);
}

/** Decode one SectionSlice. */
export function decodeSectionSlice(reader: BinaryReader): SectionSlice {
    const section = decodeSectionId(reader);
    const byteOffset = reader.readNumber();
    const len = reader.readNumber();

    return {
        section,
        byteOffset,
        len,
    };
}

/** Return one JSON value for one SectionSlice. */
export function toJsonSectionSlice(value: SectionSlice): Json {
    return {
        section: toJsonSectionId(value.section),
        byteOffset: value.byteOffset,
        len: value.len,
    };
}

/** Return one SectionSlice from one JSON value. */
export function fromJsonSectionSlice(value: Json): SectionSlice {
    const object = jsonObject(value);

    return {
        section: fromJsonSectionId(jsonField(object, "section")),
        byteOffset: jsonInteger(jsonField(object, "byteOffset")),
        len: jsonInteger(jsonField(object, "len")),
    };
}

/** Dense image section id. */
export type SectionId = number;

export const SectionId = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: SectionId): void {
        encodeSectionId(writer, value);
    },

    /** Decode one SectionId. */
    decode(reader: BinaryReader): SectionId {
        return decodeSectionId(reader);
    },

    /** Return this value as JSON. */
    toJson(value: SectionId): Json {
        return toJsonSectionId(value);
    },

    /** Return one SectionId from one JSON value. */
    fromJson(value: Json): SectionId {
        return fromJsonSectionId(value);
    },
};

/** Encode one SectionId. */
export function encodeSectionId(writer: BinaryWriter, value: SectionId): void {
    writer.writeUnsigned(value);
}

/** Decode one SectionId. */
export function decodeSectionId(reader: BinaryReader): SectionId {
    return reader.readNumber();
}

/** Return one JSON value for one SectionId. */
export function toJsonSectionId(value: SectionId): Json {
    return value;
}

/** Return one SectionId from one JSON value. */
export function fromJsonSectionId(value: Json): SectionId {
    return jsonInteger(value);
}
