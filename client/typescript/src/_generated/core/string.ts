/* generated by destack_client_generate, do not edit */

import {
    BinaryReader,
    BinaryWriter,
    Json,
    SerdeError,
    jsonArray,
    jsonBigint,
    jsonField,
    jsonObject,
    jsonString,
} from "../../protocol/serde.js";

/** Stable content identity for one interned string. */
export type StringId = bigint;

export const StringId = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: StringId): void {
        encodeStringId(writer, value);
    },

    /** Decode one StringId. */
    decode(reader: BinaryReader): StringId {
        return decodeStringId(reader);
    },

    /** Return this value as JSON. */
    toJson(value: StringId): Json {
        return toJsonStringId(value);
    },

    /** Return one StringId from one JSON value. */
    fromJson(value: Json): StringId {
        return fromJsonStringId(value);
    },
};

/** Encode one StringId. */
export function encodeStringId(writer: BinaryWriter, value: StringId): void {
    writer.writeUnsigned(value);
}

/** Decode one StringId. */
export function decodeStringId(reader: BinaryReader): StringId {
    return reader.readUnsigned();
}

/** Return one JSON value for one StringId. */
export function toJsonStringId(value: StringId): Json {
    return value.toString();
}

/** Return one StringId from one JSON value. */
export function fromJsonStringId(value: Json): StringId {
    return jsonBigint(value);
}

/** Serialized string pool representation. */
export type StringPoolData = {
    /** Stored strings sorted by stable id. */
    readonly strings: ReadonlyArray<readonly [StringId, string]>;
};

export const StringPoolData = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: StringPoolData): void {
        encodeStringPoolData(writer, value);
    },

    /** Decode one StringPoolData. */
    decode(reader: BinaryReader): StringPoolData {
        return decodeStringPoolData(reader);
    },

    /** Return this value as JSON. */
    toJson(value: StringPoolData): Json {
        return toJsonStringPoolData(value);
    },

    /** Return one StringPoolData from one JSON value. */
    fromJson(value: Json): StringPoolData {
        return fromJsonStringPoolData(value);
    },
};

/** Encode one StringPoolData. */
export function encodeStringPoolData(writer: BinaryWriter, value: StringPoolData): void {
    writer.writeUnsigned(value.strings.length);
    for (const item0 of value.strings) {
        encodeStringId(writer, item0[0]);
        writer.writeString(item0[1]);
    }
}

/** Decode one StringPoolData. */
export function decodeStringPoolData(reader: BinaryReader): StringPoolData {
    const strings = (() => {
        const length0 = reader.readNumber();
        const items0: Array<readonly [StringId, string]> = [];
        for (let index = 0; index < length0; index += 1) {
            items0.push([decodeStringId(reader), reader.readString()] as const);
        }
        return items0;
    })();

    return {
        strings,
    };
}

/** Return one JSON value for one StringPoolData. */
export function toJsonStringPoolData(value: StringPoolData): Json {
    return {
        strings: value.strings.map((item0) => [toJsonStringId(item0[0]), item0[1]]),
    };
}

/** Return one StringPoolData from one JSON value. */
export function fromJsonStringPoolData(value: Json): StringPoolData {
    const object = jsonObject(value);

    return {
        strings: jsonArray(jsonField(object, "strings")).map((item0) =>
            (() => {
                const items = jsonArray(item0);
                if (items.length !== 2) {
                    throw new SerdeError(`expected JSON tuple length 2: ${items.length}`);
                }
                return [fromJsonStringId(items[0]), jsonString(items[1])] as const;
            })(),
        ),
    };
}
