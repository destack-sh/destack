/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, bytesFromJson, bytesToJson, jsonBigint, jsonField, jsonObject } from "../../protocol/serde.js";

/** One exact immutable byte sequence. */
export type Blob = {
    /** The exact byte identity. */
    readonly id: BlobId;
    /** The byte length. */
    readonly byteLen: bigint;
};

export const Blob = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: Blob): void {
        encodeBlob(writer, value);
    },

    /** Decode one Blob. */
    decode(reader: BinaryReader): Blob {
        return decodeBlob(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Blob): Json {
        return toJsonBlob(value);
    },

    /** Return one Blob from one JSON value. */
    fromJson(value: Json): Blob {
        return fromJsonBlob(value);
    },
};

/** Encode one Blob. */
export function encodeBlob(writer: BinaryWriter, value: Blob): void {
    encodeBlobId(writer, value.id);
    writer.writeUnsigned(value.byteLen);
}

/** Decode one Blob. */
export function decodeBlob(reader: BinaryReader): Blob {
    const id = decodeBlobId(reader);
    const byteLen = reader.readUnsigned();

    return {
        id,
        byteLen,
    };
}

/** Return one JSON value for one Blob. */
export function toJsonBlob(value: Blob): Json {
    return {
        id: toJsonBlobId(value.id),
        byteLen: value.byteLen.toString(),
    };
}

/** Return one Blob from one JSON value. */
export function fromJsonBlob(value: Json): Blob {
    const object = jsonObject(value);

    return {
        id: fromJsonBlobId(jsonField(object, "id")),
        byteLen: jsonBigint(jsonField(object, "byteLen")),
    };
}

/** The identity of one exact immutable byte sequence. */
export type BlobId = Uint8Array | readonly number[];

export const BlobId = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: BlobId): void {
        encodeBlobId(writer, value);
    },

    /** Decode one BlobId. */
    decode(reader: BinaryReader): BlobId {
        return decodeBlobId(reader);
    },

    /** Return this value as JSON. */
    toJson(value: BlobId): Json {
        return toJsonBlobId(value);
    },

    /** Return one BlobId from one JSON value. */
    fromJson(value: Json): BlobId {
        return fromJsonBlobId(value);
    },
};

/** Encode one BlobId. */
export function encodeBlobId(writer: BinaryWriter, value: BlobId): void {
    writer.writeBytes(value);
}

/** Decode one BlobId. */
export function decodeBlobId(reader: BinaryReader): BlobId {
    return reader.readBytes(32);
}

/** Return one JSON value for one BlobId. */
export function toJsonBlobId(value: BlobId): Json {
    return bytesToJson(value);
}

/** Return one BlobId from one JSON value. */
export function fromJsonBlobId(value: Json): BlobId {
    return bytesFromJson(value);
}
