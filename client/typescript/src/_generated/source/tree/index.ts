/* generated by destack_client_generate, do not edit */

import {
    BinaryReader,
    BinaryWriter,
    Json,
    SerdeError,
    jsonArray,
    jsonField,
    jsonInteger,
    jsonObject,
    jsonOptional,
    jsonString,
} from "../../../protocol/serde.js";
import type { Span } from "../file/model/span.js";
import { decodeSpan, encodeSpan, fromJsonSpan, toJsonSpan } from "../file/model/span.js";

/** A source boundary owned by one node. */
export type NodeSpanBoundary = "leading" | "leadingOperator" | "trailing";

export const NodeSpanBoundary = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: NodeSpanBoundary): void {
        encodeNodeSpanBoundary(writer, value);
    },

    /** Decode one NodeSpanBoundary. */
    decode(reader: BinaryReader): NodeSpanBoundary {
        return decodeNodeSpanBoundary(reader);
    },

    /** Return this value as JSON. */
    toJson(value: NodeSpanBoundary): Json {
        return toJsonNodeSpanBoundary(value);
    },

    /** Return one NodeSpanBoundary from one JSON value. */
    fromJson(value: Json): NodeSpanBoundary {
        return fromJsonNodeSpanBoundary(value);
    },
};

/** Encode one NodeSpanBoundary. */
export function encodeNodeSpanBoundary(writer: BinaryWriter, value: NodeSpanBoundary): void {
    switch (value) {
        case "leading":
            writer.writeUnsigned(0);
            return;
        case "leadingOperator":
            writer.writeUnsigned(1);
            return;
        case "trailing":
            writer.writeUnsigned(2);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one NodeSpanBoundary. */
export function decodeNodeSpanBoundary(reader: BinaryReader): NodeSpanBoundary {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "leading";
        case 1:
            return "leadingOperator";
        case 2:
            return "trailing";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one NodeSpanBoundary. */
export function toJsonNodeSpanBoundary(value: NodeSpanBoundary): Json {
    return value;
}

/** Return one NodeSpanBoundary from one JSON value. */
export function fromJsonNodeSpanBoundary(value: Json): NodeSpanBoundary {
    const variant = jsonString(value);

    switch (variant) {
        case "leading":
            return "leading";
        case "leadingOperator":
            return "leadingOperator";
        case "trailing":
            return "trailing";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** One typed node span keyed by source node id and span kind. */
export type NodeSpanKey = {
    /** The source node id that owns this node span. */
    readonly sourceId: number;
    /** The span kind within that source node. */
    readonly spanType: NodeSpanType;
};

export const NodeSpanKey = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: NodeSpanKey): void {
        encodeNodeSpanKey(writer, value);
    },

    /** Decode one NodeSpanKey. */
    decode(reader: BinaryReader): NodeSpanKey {
        return decodeNodeSpanKey(reader);
    },

    /** Return this value as JSON. */
    toJson(value: NodeSpanKey): Json {
        return toJsonNodeSpanKey(value);
    },

    /** Return one NodeSpanKey from one JSON value. */
    fromJson(value: Json): NodeSpanKey {
        return fromJsonNodeSpanKey(value);
    },
};

/** Encode one NodeSpanKey. */
export function encodeNodeSpanKey(writer: BinaryWriter, value: NodeSpanKey): void {
    writer.writeUnsigned(value.sourceId);
    encodeNodeSpanType(writer, value.spanType);
}

/** Decode one NodeSpanKey. */
export function decodeNodeSpanKey(reader: BinaryReader): NodeSpanKey {
    const sourceId = reader.readNumber();
    const spanType = decodeNodeSpanType(reader);

    return {
        sourceId,
        spanType,
    };
}

/** Return one JSON value for one NodeSpanKey. */
export function toJsonNodeSpanKey(value: NodeSpanKey): Json {
    return {
        sourceId: value.sourceId,
        spanType: toJsonNodeSpanType(value.spanType),
    };
}

/** Return one NodeSpanKey from one JSON value. */
export function fromJsonNodeSpanKey(value: Json): NodeSpanKey {
    const object = jsonObject(value);

    return {
        sourceId: jsonInteger(jsonField(object, "sourceId")),
        spanType: fromJsonNodeSpanType(jsonField(object, "spanType")),
    };
}

/** An indexed source list within one node. */
export type NodeSpanList = "segment" | "entry";

export const NodeSpanList = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: NodeSpanList): void {
        encodeNodeSpanList(writer, value);
    },

    /** Decode one NodeSpanList. */
    decode(reader: BinaryReader): NodeSpanList {
        return decodeNodeSpanList(reader);
    },

    /** Return this value as JSON. */
    toJson(value: NodeSpanList): Json {
        return toJsonNodeSpanList(value);
    },

    /** Return one NodeSpanList from one JSON value. */
    fromJson(value: Json): NodeSpanList {
        return fromJsonNodeSpanList(value);
    },
};

/** Encode one NodeSpanList. */
export function encodeNodeSpanList(writer: BinaryWriter, value: NodeSpanList): void {
    switch (value) {
        case "segment":
            writer.writeUnsigned(0);
            return;
        case "entry":
            writer.writeUnsigned(1);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one NodeSpanList. */
export function decodeNodeSpanList(reader: BinaryReader): NodeSpanList {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "segment";
        case 1:
            return "entry";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one NodeSpanList. */
export function toJsonNodeSpanList(value: NodeSpanList): Json {
    return value;
}

/** Return one NodeSpanList from one JSON value. */
export function fromJsonNodeSpanList(value: Json): NodeSpanList {
    const variant = jsonString(value);

    switch (variant) {
        case "segment":
            return "segment";
        case "entry":
            return "entry";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** A named source region within one node. */
export type NodeSpanRegion =
    | "keyword"
    | "bindingKeyword"
    | "opening"
    | "genericParameters"
    | "parameters"
    | "arguments"
    | "body"
    | "else"
    | "alternate"
    | "guard"
    | "attributes"
    | "type"
    | "parentheses"
    | "treeContainer";

export const NodeSpanRegion = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: NodeSpanRegion): void {
        encodeNodeSpanRegion(writer, value);
    },

    /** Decode one NodeSpanRegion. */
    decode(reader: BinaryReader): NodeSpanRegion {
        return decodeNodeSpanRegion(reader);
    },

    /** Return this value as JSON. */
    toJson(value: NodeSpanRegion): Json {
        return toJsonNodeSpanRegion(value);
    },

    /** Return one NodeSpanRegion from one JSON value. */
    fromJson(value: Json): NodeSpanRegion {
        return fromJsonNodeSpanRegion(value);
    },
};

/** Encode one NodeSpanRegion. */
export function encodeNodeSpanRegion(writer: BinaryWriter, value: NodeSpanRegion): void {
    switch (value) {
        case "keyword":
            writer.writeUnsigned(0);
            return;
        case "bindingKeyword":
            writer.writeUnsigned(1);
            return;
        case "opening":
            writer.writeUnsigned(2);
            return;
        case "genericParameters":
            writer.writeUnsigned(3);
            return;
        case "parameters":
            writer.writeUnsigned(4);
            return;
        case "arguments":
            writer.writeUnsigned(5);
            return;
        case "body":
            writer.writeUnsigned(6);
            return;
        case "else":
            writer.writeUnsigned(7);
            return;
        case "alternate":
            writer.writeUnsigned(8);
            return;
        case "guard":
            writer.writeUnsigned(9);
            return;
        case "attributes":
            writer.writeUnsigned(10);
            return;
        case "type":
            writer.writeUnsigned(11);
            return;
        case "parentheses":
            writer.writeUnsigned(12);
            return;
        case "treeContainer":
            writer.writeUnsigned(13);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one NodeSpanRegion. */
export function decodeNodeSpanRegion(reader: BinaryReader): NodeSpanRegion {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "keyword";
        case 1:
            return "bindingKeyword";
        case 2:
            return "opening";
        case 3:
            return "genericParameters";
        case 4:
            return "parameters";
        case 5:
            return "arguments";
        case 6:
            return "body";
        case 7:
            return "else";
        case 8:
            return "alternate";
        case 9:
            return "guard";
        case 10:
            return "attributes";
        case 11:
            return "type";
        case 12:
            return "parentheses";
        case 13:
            return "treeContainer";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one NodeSpanRegion. */
export function toJsonNodeSpanRegion(value: NodeSpanRegion): Json {
    return value;
}

/** Return one NodeSpanRegion from one JSON value. */
export function fromJsonNodeSpanRegion(value: Json): NodeSpanRegion {
    const variant = jsonString(value);

    switch (variant) {
        case "keyword":
            return "keyword";
        case "bindingKeyword":
            return "bindingKeyword";
        case "opening":
            return "opening";
        case "genericParameters":
            return "genericParameters";
        case "parameters":
            return "parameters";
        case "arguments":
            return "arguments";
        case "body":
            return "body";
        case "else":
            return "else";
        case "alternate":
            return "alternate";
        case "guard":
            return "guard";
        case "attributes":
            return "attributes";
        case "type":
            return "type";
        case "parentheses":
            return "parentheses";
        case "treeContainer":
            return "treeContainer";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** The type of span for a node. */
export type NodeSpanType =
    /** The enclosing span of a node. */
    | {
          readonly kind: "enclosing";
      }
    /** The main span of a node (usually its identifier). */
    | {
          readonly kind: "main";
      }
    /** The head span of a node. */
    | {
          readonly kind: "head";
      }
    /** One boundary span owned by a node. */
    | {
          readonly kind: "boundary";
          readonly boundary: NodeSpanBoundary;
      }
    /** One named region span within a node. */
    | {
          readonly kind: "region";
          readonly region: NodeSpanRegion;
      }
    /** One indexed item span within a node. */
    | {
          readonly kind: "listItem";
          readonly list_item: readonly [NodeSpanList, number];
      };

export const NodeSpanType = {
    /** The enclosing span of a node. */
    enclosing(): NodeSpanType {
        return { kind: "enclosing" };
    },

    /** The main span of a node (usually its identifier). */
    main(): NodeSpanType {
        return { kind: "main" };
    },

    /** The head span of a node. */
    head(): NodeSpanType {
        return { kind: "head" };
    },

    /** One boundary span owned by a node. */
    boundary(boundary: NodeSpanBoundary): NodeSpanType {
        return { kind: "boundary", boundary };
    },

    /** One named region span within a node. */
    region(region: NodeSpanRegion): NodeSpanType {
        return { kind: "region", region };
    },

    /** One indexed item span within a node. */
    listItem(list_item: readonly [NodeSpanList, number]): NodeSpanType {
        return { kind: "listItem", list_item };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: NodeSpanType): void {
        encodeNodeSpanType(writer, value);
    },

    /** Decode one NodeSpanType. */
    decode(reader: BinaryReader): NodeSpanType {
        return decodeNodeSpanType(reader);
    },

    /** Return this value as JSON. */
    toJson(value: NodeSpanType): Json {
        return toJsonNodeSpanType(value);
    },

    /** Return one NodeSpanType from one JSON value. */
    fromJson(value: Json): NodeSpanType {
        return fromJsonNodeSpanType(value);
    },
};

/** Encode one NodeSpanType. */
export function encodeNodeSpanType(writer: BinaryWriter, value: NodeSpanType): void {
    switch (value.kind) {
        case "enclosing":
            writer.writeUnsigned(0);
            return;
        case "main":
            writer.writeUnsigned(1);
            return;
        case "head":
            writer.writeUnsigned(2);
            return;
        case "boundary":
            writer.writeUnsigned(3);
            encodeNodeSpanBoundary(writer, value.boundary);
            return;
        case "region":
            writer.writeUnsigned(4);
            encodeNodeSpanRegion(writer, value.region);
            return;
        case "listItem":
            writer.writeUnsigned(5);
            encodeNodeSpanList(writer, value.list_item[0]);
            writer.writeUnsigned(value.list_item[1]);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one NodeSpanType. */
export function decodeNodeSpanType(reader: BinaryReader): NodeSpanType {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            return { kind: "enclosing" };
        }
        case 1: {
            return { kind: "main" };
        }
        case 2: {
            return { kind: "head" };
        }
        case 3: {
            const boundary = decodeNodeSpanBoundary(reader);

            return { kind: "boundary", boundary };
        }
        case 4: {
            const region = decodeNodeSpanRegion(reader);

            return { kind: "region", region };
        }
        case 5: {
            const list_item = [decodeNodeSpanList(reader), reader.readNumber()] as const;

            return { kind: "listItem", list_item };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one NodeSpanType. */
export function toJsonNodeSpanType(value: NodeSpanType): Json {
    switch (value.kind) {
        case "enclosing":
            return {
                kind: "enclosing",
            };
        case "main":
            return {
                kind: "main",
            };
        case "head":
            return {
                kind: "head",
            };
        case "boundary":
            return {
                kind: "boundary",
                boundary: toJsonNodeSpanBoundary(value.boundary),
            };
        case "region":
            return {
                kind: "region",
                region: toJsonNodeSpanRegion(value.region),
            };
        case "listItem":
            return {
                kind: "listItem",
                list_item: [toJsonNodeSpanList(value.list_item[0]), value.list_item[1]],
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one NodeSpanType from one JSON value. */
export function fromJsonNodeSpanType(value: Json): NodeSpanType {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "enclosing":
            return {
                kind,
            };
        case "main":
            return {
                kind,
            };
        case "head":
            return {
                kind,
            };
        case "boundary":
            return {
                kind,
                boundary: fromJsonNodeSpanBoundary(jsonField(object, "boundary")),
            };
        case "region":
            return {
                kind,
                region: fromJsonNodeSpanRegion(jsonField(object, "region")),
            };
        case "listItem":
            return {
                kind,
                list_item: (() => {
                    const items = jsonArray(jsonField(object, "list_item"));
                    if (items.length !== 2) {
                        throw new SerdeError(`expected JSON tuple length 2: ${items.length}`);
                    }
                    return [fromJsonNodeSpanList(items[0]), jsonInteger(items[1])] as const;
                })(),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** Serialized source index shape. */
export type SourceIndexArchive = {
    readonly enclosingSpans: ReadonlyArray<Span>;
    readonly mainSpans: ReadonlyArray<Span | undefined>;
    readonly typeSpans: ReadonlyArray<Span | undefined>;
    readonly sideSpans: ReadonlyArray<readonly [NodeSpanKey, Span]>;
};

export const SourceIndexArchive = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: SourceIndexArchive): void {
        encodeSourceIndexArchive(writer, value);
    },

    /** Decode one SourceIndexArchive. */
    decode(reader: BinaryReader): SourceIndexArchive {
        return decodeSourceIndexArchive(reader);
    },

    /** Return this value as JSON. */
    toJson(value: SourceIndexArchive): Json {
        return toJsonSourceIndexArchive(value);
    },

    /** Return one SourceIndexArchive from one JSON value. */
    fromJson(value: Json): SourceIndexArchive {
        return fromJsonSourceIndexArchive(value);
    },
};

/** Encode one SourceIndexArchive. */
export function encodeSourceIndexArchive(writer: BinaryWriter, value: SourceIndexArchive): void {
    writer.writeUnsigned(value.enclosingSpans.length);
    for (const item0 of value.enclosingSpans) {
        encodeSpan(writer, item0);
    }
    writer.writeUnsigned(value.mainSpans.length);
    for (const item1 of value.mainSpans) {
        writer.writeOption(item1, (value2) => {
            encodeSpan(writer, value2);
        });
    }
    writer.writeUnsigned(value.typeSpans.length);
    for (const item2 of value.typeSpans) {
        writer.writeOption(item2, (value3) => {
            encodeSpan(writer, value3);
        });
    }
    writer.writeUnsigned(value.sideSpans.length);
    for (const item3 of value.sideSpans) {
        encodeNodeSpanKey(writer, item3[0]);
        encodeSpan(writer, item3[1]);
    }
}

/** Decode one SourceIndexArchive. */
export function decodeSourceIndexArchive(reader: BinaryReader): SourceIndexArchive {
    const enclosingSpans = (() => {
        const length0 = reader.readNumber();
        const items0: Array<Span> = [];
        for (let index = 0; index < length0; index += 1) {
            items0.push(decodeSpan(reader));
        }
        return items0;
    })();
    const mainSpans = (() => {
        const length1 = reader.readNumber();
        const items1: Array<Span | undefined> = [];
        for (let index = 0; index < length1; index += 1) {
            items1.push(reader.readOption(() => decodeSpan(reader)));
        }
        return items1;
    })();
    const typeSpans = (() => {
        const length2 = reader.readNumber();
        const items2: Array<Span | undefined> = [];
        for (let index = 0; index < length2; index += 1) {
            items2.push(reader.readOption(() => decodeSpan(reader)));
        }
        return items2;
    })();
    const sideSpans = (() => {
        const length3 = reader.readNumber();
        const items3: Array<readonly [NodeSpanKey, Span]> = [];
        for (let index = 0; index < length3; index += 1) {
            items3.push([decodeNodeSpanKey(reader), decodeSpan(reader)] as const);
        }
        return items3;
    })();

    return {
        enclosingSpans,
        mainSpans,
        typeSpans,
        sideSpans,
    };
}

/** Return one JSON value for one SourceIndexArchive. */
export function toJsonSourceIndexArchive(value: SourceIndexArchive): Json {
    return {
        enclosingSpans: value.enclosingSpans.map((item0) => toJsonSpan(item0)),
        mainSpans: value.mainSpans.map((item0) => (item0 === undefined ? null : toJsonSpan(item0))),
        typeSpans: value.typeSpans.map((item0) => (item0 === undefined ? null : toJsonSpan(item0))),
        sideSpans: value.sideSpans.map((item0) => [
            toJsonNodeSpanKey(item0[0]),
            toJsonSpan(item0[1]),
        ]),
    };
}

/** Return one SourceIndexArchive from one JSON value. */
export function fromJsonSourceIndexArchive(value: Json): SourceIndexArchive {
    const object = jsonObject(value);

    return {
        enclosingSpans: jsonArray(jsonField(object, "enclosingSpans")).map((item0) =>
            fromJsonSpan(item0),
        ),
        mainSpans: jsonArray(jsonField(object, "mainSpans")).map((item0) =>
            item0 === null ? undefined : fromJsonSpan(item0),
        ),
        typeSpans: jsonArray(jsonField(object, "typeSpans")).map((item0) =>
            item0 === null ? undefined : fromJsonSpan(item0),
        ),
        sideSpans: jsonArray(jsonField(object, "sideSpans")).map((item0) =>
            (() => {
                const items = jsonArray(item0);
                if (items.length !== 2) {
                    throw new SerdeError(`expected JSON tuple length 2: ${items.length}`);
                }
                return [fromJsonNodeSpanKey(items[0]), fromJsonSpan(items[1])] as const;
            })(),
        ),
    };
}
