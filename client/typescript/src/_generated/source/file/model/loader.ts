/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, jsonString } from "../../../../protocol/serde.js";

/** How source content is interpreted as a module. */
export type Loader = "destack" | "json" | "toml" | "yaml" | "text" | "binary" | "file" | "base64";

export const Loader = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: Loader): void {
        encodeLoader(writer, value);
    },

    /** Decode one Loader. */
    decode(reader: BinaryReader): Loader {
        return decodeLoader(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Loader): Json {
        return toJsonLoader(value);
    },

    /** Return one Loader from one JSON value. */
    fromJson(value: Json): Loader {
        return fromJsonLoader(value);
    },
};

/** Encode one Loader. */
export function encodeLoader(writer: BinaryWriter, value: Loader): void {
    switch (value) {
        case "destack":
            writer.writeUnsigned(0);
            return;
        case "json":
            writer.writeUnsigned(1);
            return;
        case "toml":
            writer.writeUnsigned(2);
            return;
        case "yaml":
            writer.writeUnsigned(3);
            return;
        case "text":
            writer.writeUnsigned(4);
            return;
        case "binary":
            writer.writeUnsigned(5);
            return;
        case "file":
            writer.writeUnsigned(6);
            return;
        case "base64":
            writer.writeUnsigned(7);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one Loader. */
export function decodeLoader(reader: BinaryReader): Loader {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "destack";
        case 1:
            return "json";
        case 2:
            return "toml";
        case 3:
            return "yaml";
        case 4:
            return "text";
        case 5:
            return "binary";
        case 6:
            return "file";
        case 7:
            return "base64";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one Loader. */
export function toJsonLoader(value: Loader): Json {
    return value;
}

/** Return one Loader from one JSON value. */
export function fromJsonLoader(value: Json): Loader {
    const variant = jsonString(value);

    switch (variant) {
        case "destack":
            return "destack";
        case "json":
            return "json";
        case "toml":
            return "toml";
        case "yaml":
            return "yaml";
        case "text":
            return "text";
        case "binary":
            return "binary";
        case "file":
            return "file";
        case "base64":
            return "base64";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}
