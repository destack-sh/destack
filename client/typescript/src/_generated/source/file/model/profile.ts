/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, jsonBigint } from "../../../../protocol/serde.js";

/** Unique identifier for profiles. */
export type ProfileId = bigint;

export const ProfileId = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ProfileId): void {
        encodeProfileId(writer, value);
    },

    /** Decode one ProfileId. */
    decode(reader: BinaryReader): ProfileId {
        return decodeProfileId(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ProfileId): Json {
        return toJsonProfileId(value);
    },

    /** Return one ProfileId from one JSON value. */
    fromJson(value: Json): ProfileId {
        return fromJsonProfileId(value);
    },
};

/** Encode one ProfileId. */
export function encodeProfileId(writer: BinaryWriter, value: ProfileId): void {
    writer.writeUnsigned(value);
}

/** Decode one ProfileId. */
export function decodeProfileId(reader: BinaryReader): ProfileId {
    return reader.readUnsigned();
}

/** Return one JSON value for one ProfileId. */
export function toJsonProfileId(value: ProfileId): Json {
    return value.toString();
}

/** Return one ProfileId from one JSON value. */
export function fromJsonProfileId(value: Json): ProfileId {
    return jsonBigint(value);
}
