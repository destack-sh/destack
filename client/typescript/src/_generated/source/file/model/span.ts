/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, jsonField, jsonInteger, jsonObject } from "../../../../protocol/serde.js";
import type { FileId } from "./file.js";
import { decodeFileId, encodeFileId, fromJsonFileId, toJsonFileId } from "./file.js";

/** A source range in bytes (in some File). */
export type Span = {
    /** The file that the Span belongs to. */
    readonly file: FileId;
    /** The start position of the Span in bytes (absolute, inclusive). */
    readonly start: number;
    /** The end position of the Span in bytes (absolute, exclusive). */
    readonly end: number;
};

export const Span = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: Span): void {
        encodeSpan(writer, value);
    },

    /** Decode one Span. */
    decode(reader: BinaryReader): Span {
        return decodeSpan(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Span): Json {
        return toJsonSpan(value);
    },

    /** Return one Span from one JSON value. */
    fromJson(value: Json): Span {
        return fromJsonSpan(value);
    },
};

/** Encode one Span. */
export function encodeSpan(writer: BinaryWriter, value: Span): void {
    encodeFileId(writer, value.file);
    writer.writeUnsigned(value.start);
    writer.writeUnsigned(value.end);
}

/** Decode one Span. */
export function decodeSpan(reader: BinaryReader): Span {
    const file = decodeFileId(reader);
    const start = reader.readNumber();
    const end = reader.readNumber();

    return {
        file,
        start,
        end,
    };
}

/** Return one JSON value for one Span. */
export function toJsonSpan(value: Span): Json {
    return {
        file: toJsonFileId(value.file),
        start: value.start,
        end: value.end,
    };
}

/** Return one Span from one JSON value. */
export function fromJsonSpan(value: Json): Span {
    const object = jsonObject(value);

    return {
        file: fromJsonFileId(jsonField(object, "file")),
        start: jsonInteger(jsonField(object, "start")),
        end: jsonInteger(jsonField(object, "end")),
    };
}
