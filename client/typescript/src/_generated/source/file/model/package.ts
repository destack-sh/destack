/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, jsonBigint } from "../../../../protocol/serde.js";

/** Unique identifier for one source package. */
export type PackageId = bigint;

export const PackageId = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: PackageId): void {
        encodePackageId(writer, value);
    },

    /** Decode one PackageId. */
    decode(reader: BinaryReader): PackageId {
        return decodePackageId(reader);
    },

    /** Return this value as JSON. */
    toJson(value: PackageId): Json {
        return toJsonPackageId(value);
    },

    /** Return one PackageId from one JSON value. */
    fromJson(value: Json): PackageId {
        return fromJsonPackageId(value);
    },
};

/** Encode one PackageId. */
export function encodePackageId(writer: BinaryWriter, value: PackageId): void {
    writer.writeUnsigned(value);
}

/** Decode one PackageId. */
export function decodePackageId(reader: BinaryReader): PackageId {
    return reader.readUnsigned();
}

/** Return one JSON value for one PackageId. */
export function toJsonPackageId(value: PackageId): Json {
    return value.toString();
}

/** Return one PackageId from one JSON value. */
export function fromJsonPackageId(value: Json): PackageId {
    return jsonBigint(value);
}
