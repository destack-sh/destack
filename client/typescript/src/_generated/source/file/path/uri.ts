/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, jsonField, jsonObject, jsonString } from "../../../../protocol/serde.js";

/** A generic URI. */
export type Uri = string;

export const Uri = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: Uri): void {
        encodeUri(writer, value);
    },

    /** Decode one Uri. */
    decode(reader: BinaryReader): Uri {
        return decodeUri(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Uri): Json {
        return toJsonUri(value);
    },

    /** Return one Uri from one JSON value. */
    fromJson(value: Json): Uri {
        return fromJsonUri(value);
    },
};

/** Encode one Uri. */
export function encodeUri(writer: BinaryWriter, value: Uri): void {
    writer.writeString(value);
}

/** Decode one Uri. */
export function decodeUri(reader: BinaryReader): Uri {
    return reader.readString();
}

/** Return one JSON value for one Uri. */
export function toJsonUri(value: Uri): Json {
    return value;
}

/** Return one Uri from one JSON value. */
export function fromJsonUri(value: Json): Uri {
    return jsonString(value);
}
