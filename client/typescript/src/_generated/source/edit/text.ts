/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, jsonField, jsonInteger, jsonObject, jsonOptional, jsonString } from "../../../protocol/serde.js";

/** One textual edit in an open text buffer. */
export type TextChange = {
    /** Optional range to replace, absent for full replacement. */
    readonly range?: TextRange;
    /** Replacement text. */
    readonly text: string;
};

export const TextChange = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: TextChange): void {
        encodeTextChange(writer, value);
    },

    /** Decode one TextChange. */
    decode(reader: BinaryReader): TextChange {
        return decodeTextChange(reader);
    },

    /** Return this value as JSON. */
    toJson(value: TextChange): Json {
        return toJsonTextChange(value);
    },

    /** Return one TextChange from one JSON value. */
    fromJson(value: Json): TextChange {
        return fromJsonTextChange(value);
    },
};

/** Encode one TextChange. */
export function encodeTextChange(writer: BinaryWriter, value: TextChange): void {
    writer.writeOption(value.range, (value0) => {
        encodeTextRange(writer, value0);
    });
    writer.writeString(value.text);
}

/** Decode one TextChange. */
export function decodeTextChange(reader: BinaryReader): TextChange {
    const range = reader.readOption(() => decodeTextRange(reader));
    const text = reader.readString();

    return {
        ...(range === undefined ? {} : { range }),
        text,
    };
}

/** Return one JSON value for one TextChange. */
export function toJsonTextChange(value: TextChange): Json {
    return {
        ...(value.range === undefined ? {} : { range: toJsonTextRange(value.range) }),
        text: value.text,
    };
}

/** Return one TextChange from one JSON value. */
export function fromJsonTextChange(value: Json): TextChange {
    const object = jsonObject(value);

    return {
        range: jsonOptional(object, "range", (value) => fromJsonTextRange(value)),
        text: jsonString(jsonField(object, "text")),
    };
}

/** One text range expressed in UTF-16 positions. */
export type TextRange = {
    /** Start position. */
    readonly start: TextPosition;
    /** End position. */
    readonly end: TextPosition;
};

export const TextRange = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: TextRange): void {
        encodeTextRange(writer, value);
    },

    /** Decode one TextRange. */
    decode(reader: BinaryReader): TextRange {
        return decodeTextRange(reader);
    },

    /** Return this value as JSON. */
    toJson(value: TextRange): Json {
        return toJsonTextRange(value);
    },

    /** Return one TextRange from one JSON value. */
    fromJson(value: Json): TextRange {
        return fromJsonTextRange(value);
    },
};

/** Encode one TextRange. */
export function encodeTextRange(writer: BinaryWriter, value: TextRange): void {
    encodeTextPosition(writer, value.start);
    encodeTextPosition(writer, value.end);
}

/** Decode one TextRange. */
export function decodeTextRange(reader: BinaryReader): TextRange {
    const start = decodeTextPosition(reader);
    const end = decodeTextPosition(reader);

    return {
        start,
        end,
    };
}

/** Return one JSON value for one TextRange. */
export function toJsonTextRange(value: TextRange): Json {
    return {
        start: toJsonTextPosition(value.start),
        end: toJsonTextPosition(value.end),
    };
}

/** Return one TextRange from one JSON value. */
export function fromJsonTextRange(value: Json): TextRange {
    const object = jsonObject(value);

    return {
        start: fromJsonTextPosition(jsonField(object, "start")),
        end: fromJsonTextPosition(jsonField(object, "end")),
    };
}

/** One zero-based UTF-16 text position. */
export type TextPosition = {
    /** Zero-based line number. */
    readonly line: number;
    /** Zero-based UTF-16 column. */
    readonly character: number;
};

export const TextPosition = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: TextPosition): void {
        encodeTextPosition(writer, value);
    },

    /** Decode one TextPosition. */
    decode(reader: BinaryReader): TextPosition {
        return decodeTextPosition(reader);
    },

    /** Return this value as JSON. */
    toJson(value: TextPosition): Json {
        return toJsonTextPosition(value);
    },

    /** Return one TextPosition from one JSON value. */
    fromJson(value: Json): TextPosition {
        return fromJsonTextPosition(value);
    },
};

/** Encode one TextPosition. */
export function encodeTextPosition(writer: BinaryWriter, value: TextPosition): void {
    writer.writeUnsigned(value.line);
    writer.writeUnsigned(value.character);
}

/** Decode one TextPosition. */
export function decodeTextPosition(reader: BinaryReader): TextPosition {
    const line = reader.readNumber();
    const character = reader.readNumber();

    return {
        line,
        character,
    };
}

/** Return one JSON value for one TextPosition. */
export function toJsonTextPosition(value: TextPosition): Json {
    return {
        line: value.line,
        character: value.character,
    };
}

/** Return one TextPosition from one JSON value. */
export function fromJsonTextPosition(value: Json): TextPosition {
    const object = jsonObject(value);

    return {
        line: jsonInteger(jsonField(object, "line")),
        character: jsonInteger(jsonField(object, "character")),
    };
}
