/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, jsonArray, jsonField, jsonObject, jsonString } from "../../../protocol/serde.js";
import type { FileId } from "../file/model/file.js";
import type { Span } from "../file/model/span.js";
import { decodeFileId, encodeFileId, fromJsonFileId, toJsonFileId } from "../file/model/file.js";
import { decodeSpan, encodeSpan, fromJsonSpan, toJsonSpan } from "../file/model/span.js";

/** A single patch: replace a span with new text. */
export type Patch = {
    /** The span to replace. */
    readonly span: Span;
    /** The replacement text (empty for deletion). */
    readonly newText: string;
};

export const Patch = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: Patch): void {
        encodePatch(writer, value);
    },

    /** Decode one Patch. */
    decode(reader: BinaryReader): Patch {
        return decodePatch(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Patch): Json {
        return toJsonPatch(value);
    },

    /** Return one Patch from one JSON value. */
    fromJson(value: Json): Patch {
        return fromJsonPatch(value);
    },
};

/** Encode one Patch. */
export function encodePatch(writer: BinaryWriter, value: Patch): void {
    encodeSpan(writer, value.span);
    writer.writeString(value.newText);
}

/** Decode one Patch. */
export function decodePatch(reader: BinaryReader): Patch {
    const span = decodeSpan(reader);
    const newText = reader.readString();

    return {
        span,
        newText,
    };
}

/** Return one JSON value for one Patch. */
export function toJsonPatch(value: Patch): Json {
    return {
        span: toJsonSpan(value.span),
        newText: value.newText,
    };
}

/** Return one Patch from one JSON value. */
export function fromJsonPatch(value: Json): Patch {
    const object = jsonObject(value);

    return {
        span: fromJsonSpan(jsonField(object, "span")),
        newText: jsonString(jsonField(object, "newText")),
    };
}

/** Patches across multiple files. */
export type PatchSet = {
    /** Per-file patches. */
    readonly files: ReadonlyArray<FilePatch>;
};

export const PatchSet = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: PatchSet): void {
        encodePatchSet(writer, value);
    },

    /** Decode one PatchSet. */
    decode(reader: BinaryReader): PatchSet {
        return decodePatchSet(reader);
    },

    /** Return this value as JSON. */
    toJson(value: PatchSet): Json {
        return toJsonPatchSet(value);
    },

    /** Return one PatchSet from one JSON value. */
    fromJson(value: Json): PatchSet {
        return fromJsonPatchSet(value);
    },
};

/** Encode one PatchSet. */
export function encodePatchSet(writer: BinaryWriter, value: PatchSet): void {
    writer.writeUnsigned(value.files.length);
    for (const item0 of value.files) {
        encodeFilePatch(writer, item0);
    }
}

/** Decode one PatchSet. */
export function decodePatchSet(reader: BinaryReader): PatchSet {
    const files = (() => { const length0 = reader.readNumber(); const items0: Array<FilePatch> = []; for (let index = 0; index < length0; index += 1) { items0.push(decodeFilePatch(reader)); } return items0; })();

    return {
        files,
    };
}

/** Return one JSON value for one PatchSet. */
export function toJsonPatchSet(value: PatchSet): Json {
    return {
        files: value.files.map((item0) => toJsonFilePatch(item0)),
    };
}

/** Return one PatchSet from one JSON value. */
export function fromJsonPatchSet(value: Json): PatchSet {
    const object = jsonObject(value);

    return {
        files: jsonArray(jsonField(object, "files")).map((item0) => fromJsonFilePatch(item0)),
    };
}

/** Patches for a single file. */
export type FilePatch = {
    /** The file to patch. */
    readonly file: FileId;
    /** The patches to apply (should be non-overlapping). */
    readonly patches: ReadonlyArray<Patch>;
};

export const FilePatch = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: FilePatch): void {
        encodeFilePatch(writer, value);
    },

    /** Decode one FilePatch. */
    decode(reader: BinaryReader): FilePatch {
        return decodeFilePatch(reader);
    },

    /** Return this value as JSON. */
    toJson(value: FilePatch): Json {
        return toJsonFilePatch(value);
    },

    /** Return one FilePatch from one JSON value. */
    fromJson(value: Json): FilePatch {
        return fromJsonFilePatch(value);
    },
};

/** Encode one FilePatch. */
export function encodeFilePatch(writer: BinaryWriter, value: FilePatch): void {
    encodeFileId(writer, value.file);
    writer.writeUnsigned(value.patches.length);
    for (const item1 of value.patches) {
        encodePatch(writer, item1);
    }
}

/** Decode one FilePatch. */
export function decodeFilePatch(reader: BinaryReader): FilePatch {
    const file = decodeFileId(reader);
    const patches = (() => { const length1 = reader.readNumber(); const items1: Array<Patch> = []; for (let index = 0; index < length1; index += 1) { items1.push(decodePatch(reader)); } return items1; })();

    return {
        file,
        patches,
    };
}

/** Return one JSON value for one FilePatch. */
export function toJsonFilePatch(value: FilePatch): Json {
    return {
        file: toJsonFileId(value.file),
        patches: value.patches.map((item0) => toJsonPatch(item0)),
    };
}

/** Return one FilePatch from one JSON value. */
export function fromJsonFilePatch(value: Json): FilePatch {
    const object = jsonObject(value);

    return {
        file: fromJsonFileId(jsonField(object, "file")),
        patches: jsonArray(jsonField(object, "patches")).map((item0) => fromJsonPatch(item0)),
    };
}
