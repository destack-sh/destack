/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, jsonArray, jsonField, jsonObject } from "../../../protocol/serde.js";
import type { Diagnostic } from "./diagnostic.js";
import { decodeDiagnostic, encodeDiagnostic, fromJsonDiagnostic, toJsonDiagnostic } from "./diagnostic.js";

/** A collection of diagnostics. */
export type DiagnosticCollection = {
    /** The diagnostics. */
    readonly diagnostics: ReadonlyArray<Diagnostic>;
};

export const DiagnosticCollection = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: DiagnosticCollection): void {
        encodeDiagnosticCollection(writer, value);
    },

    /** Decode one DiagnosticCollection. */
    decode(reader: BinaryReader): DiagnosticCollection {
        return decodeDiagnosticCollection(reader);
    },

    /** Return this value as JSON. */
    toJson(value: DiagnosticCollection): Json {
        return toJsonDiagnosticCollection(value);
    },

    /** Return one DiagnosticCollection from one JSON value. */
    fromJson(value: Json): DiagnosticCollection {
        return fromJsonDiagnosticCollection(value);
    },
};

/** Encode one DiagnosticCollection. */
export function encodeDiagnosticCollection(writer: BinaryWriter, value: DiagnosticCollection): void {
    writer.writeUnsigned(value.diagnostics.length);
    for (const item0 of value.diagnostics) {
        encodeDiagnostic(writer, item0);
    }
}

/** Decode one DiagnosticCollection. */
export function decodeDiagnosticCollection(reader: BinaryReader): DiagnosticCollection {
    const diagnostics = (() => { const length0 = reader.readNumber(); const items0: Array<Diagnostic> = []; for (let index = 0; index < length0; index += 1) { items0.push(decodeDiagnostic(reader)); } return items0; })();

    return {
        diagnostics,
    };
}

/** Return one JSON value for one DiagnosticCollection. */
export function toJsonDiagnosticCollection(value: DiagnosticCollection): Json {
    return {
        diagnostics: value.diagnostics.map((item0) => toJsonDiagnostic(item0)),
    };
}

/** Return one DiagnosticCollection from one JSON value. */
export function fromJsonDiagnosticCollection(value: Json): DiagnosticCollection {
    const object = jsonObject(value);

    return {
        diagnostics: jsonArray(jsonField(object, "diagnostics")).map((item0) => fromJsonDiagnostic(item0)),
    };
}
