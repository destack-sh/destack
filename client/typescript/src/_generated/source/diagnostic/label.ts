/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, jsonField, jsonObject, jsonOptional, jsonString } from "../../../protocol/serde.js";
import type { ContentId } from "../file/model/file.js";
import type { FileId } from "../file/model/file.js";
import type { Span } from "../file/model/span.js";
import { decodeContentId, encodeContentId, fromJsonContentId, toJsonContentId } from "../file/model/file.js";
import { decodeFileId, encodeFileId, fromJsonFileId, toJsonFileId } from "../file/model/file.js";
import { decodeSpan, encodeSpan, fromJsonSpan, toJsonSpan } from "../file/model/span.js";

/** One concrete source label in a diagnostic. */
export type DiagnosticLabel = {
    /** The exact content the target was recorded against. */
    readonly content: ContentId;
    /** The targeted source location. */
    readonly target: DiagnosticTarget;
    /** The optional label shown on the target. */
    readonly message?: string;
};

export const DiagnosticLabel = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: DiagnosticLabel): void {
        encodeDiagnosticLabel(writer, value);
    },

    /** Decode one DiagnosticLabel. */
    decode(reader: BinaryReader): DiagnosticLabel {
        return decodeDiagnosticLabel(reader);
    },

    /** Return this value as JSON. */
    toJson(value: DiagnosticLabel): Json {
        return toJsonDiagnosticLabel(value);
    },

    /** Return one DiagnosticLabel from one JSON value. */
    fromJson(value: Json): DiagnosticLabel {
        return fromJsonDiagnosticLabel(value);
    },
};

/** Encode one DiagnosticLabel. */
export function encodeDiagnosticLabel(writer: BinaryWriter, value: DiagnosticLabel): void {
    encodeContentId(writer, value.content);
    encodeDiagnosticTarget(writer, value.target);
    writer.writeOption(value.message, (value2) => {
        writer.writeString(value2);
    });
}

/** Decode one DiagnosticLabel. */
export function decodeDiagnosticLabel(reader: BinaryReader): DiagnosticLabel {
    const content = decodeContentId(reader);
    const target = decodeDiagnosticTarget(reader);
    const message = reader.readOption(() => reader.readString());

    return {
        content,
        target,
        ...(message === undefined ? {} : { message }),
    };
}

/** Return one JSON value for one DiagnosticLabel. */
export function toJsonDiagnosticLabel(value: DiagnosticLabel): Json {
    return {
        content: toJsonContentId(value.content),
        target: toJsonDiagnosticTarget(value.target),
        ...(value.message === undefined ? {} : { message: value.message }),
    };
}

/** Return one DiagnosticLabel from one JSON value. */
export function fromJsonDiagnosticLabel(value: Json): DiagnosticLabel {
    const object = jsonObject(value);

    return {
        content: fromJsonContentId(jsonField(object, "content")),
        target: fromJsonDiagnosticTarget(jsonField(object, "target")),
        message: jsonOptional(object, "message", (value) => jsonString(value)),
    };
}

/** One resolved diagnostic location. */
export type DiagnosticTarget =
    /** An exact source span. */
    | {
          readonly kind: "span";
          readonly span: Span;
      }
    /** A whole source file. */
    | {
          readonly kind: "file";
          readonly file: FileId;
      }
;

export const DiagnosticTarget = {
    /** An exact source span. */
    span(span: Span): DiagnosticTarget {
        return { kind: "span", span };
    },

    /** A whole source file. */
    file(file: FileId): DiagnosticTarget {
        return { kind: "file", file };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: DiagnosticTarget): void {
        encodeDiagnosticTarget(writer, value);
    },

    /** Decode one DiagnosticTarget. */
    decode(reader: BinaryReader): DiagnosticTarget {
        return decodeDiagnosticTarget(reader);
    },

    /** Return this value as JSON. */
    toJson(value: DiagnosticTarget): Json {
        return toJsonDiagnosticTarget(value);
    },

    /** Return one DiagnosticTarget from one JSON value. */
    fromJson(value: Json): DiagnosticTarget {
        return fromJsonDiagnosticTarget(value);
    },
};

/** Encode one DiagnosticTarget. */
export function encodeDiagnosticTarget(writer: BinaryWriter, value: DiagnosticTarget): void {
    switch (value.kind) {
        case "span":
            writer.writeUnsigned(0);
            encodeSpan(writer, value.span);
            return;
        case "file":
            writer.writeUnsigned(1);
            encodeFileId(writer, value.file);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one DiagnosticTarget. */
export function decodeDiagnosticTarget(reader: BinaryReader): DiagnosticTarget {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const span = decodeSpan(reader);

            return { kind: "span", span };
        }
        case 1: {
            const file = decodeFileId(reader);

            return { kind: "file", file };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one DiagnosticTarget. */
export function toJsonDiagnosticTarget(value: DiagnosticTarget): Json {
    switch (value.kind) {
        case "span":
            return {
                kind: "span",
                span: toJsonSpan(value.span),
            };
        case "file":
            return {
                kind: "file",
                file: toJsonFileId(value.file),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one DiagnosticTarget from one JSON value. */
export function fromJsonDiagnosticTarget(value: Json): DiagnosticTarget {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "span":
            return {
                kind,
                span: fromJsonSpan(jsonField(object, "span")),
            };
        case "file":
            return {
                kind,
                file: fromJsonFileId(jsonField(object, "file")),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** Extra context for understanding a diagnostic. */
export type DiagnosticNote = {
    /** The note message. */
    readonly message: string;
};

export const DiagnosticNote = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: DiagnosticNote): void {
        encodeDiagnosticNote(writer, value);
    },

    /** Decode one DiagnosticNote. */
    decode(reader: BinaryReader): DiagnosticNote {
        return decodeDiagnosticNote(reader);
    },

    /** Return this value as JSON. */
    toJson(value: DiagnosticNote): Json {
        return toJsonDiagnosticNote(value);
    },

    /** Return one DiagnosticNote from one JSON value. */
    fromJson(value: Json): DiagnosticNote {
        return fromJsonDiagnosticNote(value);
    },
};

/** Encode one DiagnosticNote. */
export function encodeDiagnosticNote(writer: BinaryWriter, value: DiagnosticNote): void {
    writer.writeString(value.message);
}

/** Decode one DiagnosticNote. */
export function decodeDiagnosticNote(reader: BinaryReader): DiagnosticNote {
    const message = reader.readString();

    return {
        message,
    };
}

/** Return one JSON value for one DiagnosticNote. */
export function toJsonDiagnosticNote(value: DiagnosticNote): Json {
    return {
        message: value.message,
    };
}

/** Return one DiagnosticNote from one JSON value. */
export function fromJsonDiagnosticNote(value: Json): DiagnosticNote {
    const object = jsonObject(value);

    return {
        message: jsonString(jsonField(object, "message")),
    };
}

/** Guidance for fixing or avoiding a diagnostic. */
export type DiagnosticHelp = {
    /** The help message. */
    readonly message: string;
};

export const DiagnosticHelp = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: DiagnosticHelp): void {
        encodeDiagnosticHelp(writer, value);
    },

    /** Decode one DiagnosticHelp. */
    decode(reader: BinaryReader): DiagnosticHelp {
        return decodeDiagnosticHelp(reader);
    },

    /** Return this value as JSON. */
    toJson(value: DiagnosticHelp): Json {
        return toJsonDiagnosticHelp(value);
    },

    /** Return one DiagnosticHelp from one JSON value. */
    fromJson(value: Json): DiagnosticHelp {
        return fromJsonDiagnosticHelp(value);
    },
};

/** Encode one DiagnosticHelp. */
export function encodeDiagnosticHelp(writer: BinaryWriter, value: DiagnosticHelp): void {
    writer.writeString(value.message);
}

/** Decode one DiagnosticHelp. */
export function decodeDiagnosticHelp(reader: BinaryReader): DiagnosticHelp {
    const message = reader.readString();

    return {
        message,
    };
}

/** Return one JSON value for one DiagnosticHelp. */
export function toJsonDiagnosticHelp(value: DiagnosticHelp): Json {
    return {
        message: value.message,
    };
}

/** Return one DiagnosticHelp from one JSON value. */
export function fromJsonDiagnosticHelp(value: Json): DiagnosticHelp {
    const object = jsonObject(value);

    return {
        message: jsonString(jsonField(object, "message")),
    };
}
