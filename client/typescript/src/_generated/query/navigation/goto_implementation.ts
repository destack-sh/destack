/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, jsonArray, jsonField, jsonObject } from "../../../protocol/serde.js";
import type { NavigationTarget } from "./target.js";
import type { QueryPosition } from "../protocol/target.js";
import { decodeNavigationTarget, encodeNavigationTarget, fromJsonNavigationTarget, toJsonNavigationTarget } from "./target.js";
import { decodeQueryPosition, encodeQueryPosition, fromJsonQueryPosition, toJsonQueryPosition } from "../protocol/target.js";

/** A goto implementation request. */
export type GotoImplementationRequest = {
    /** The queried position. */
    readonly position: QueryPosition;
};

export const GotoImplementationRequest = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: GotoImplementationRequest): void {
        encodeGotoImplementationRequest(writer, value);
    },

    /** Decode one GotoImplementationRequest. */
    decode(reader: BinaryReader): GotoImplementationRequest {
        return decodeGotoImplementationRequest(reader);
    },

    /** Return this value as JSON. */
    toJson(value: GotoImplementationRequest): Json {
        return toJsonGotoImplementationRequest(value);
    },

    /** Return one GotoImplementationRequest from one JSON value. */
    fromJson(value: Json): GotoImplementationRequest {
        return fromJsonGotoImplementationRequest(value);
    },
};

/** Encode one GotoImplementationRequest. */
export function encodeGotoImplementationRequest(writer: BinaryWriter, value: GotoImplementationRequest): void {
    encodeQueryPosition(writer, value.position);
}

/** Decode one GotoImplementationRequest. */
export function decodeGotoImplementationRequest(reader: BinaryReader): GotoImplementationRequest {
    const position = decodeQueryPosition(reader);

    return {
        position,
    };
}

/** Return one JSON value for one GotoImplementationRequest. */
export function toJsonGotoImplementationRequest(value: GotoImplementationRequest): Json {
    return {
        position: toJsonQueryPosition(value.position),
    };
}

/** Return one GotoImplementationRequest from one JSON value. */
export function fromJsonGotoImplementationRequest(value: Json): GotoImplementationRequest {
    const object = jsonObject(value);

    return {
        position: fromJsonQueryPosition(jsonField(object, "position")),
    };
}

/** A goto implementation response. */
export type GotoImplementationResponse = {
    /** Implementation targets. */
    readonly targets: ReadonlyArray<NavigationTarget>;
};

export const GotoImplementationResponse = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: GotoImplementationResponse): void {
        encodeGotoImplementationResponse(writer, value);
    },

    /** Decode one GotoImplementationResponse. */
    decode(reader: BinaryReader): GotoImplementationResponse {
        return decodeGotoImplementationResponse(reader);
    },

    /** Return this value as JSON. */
    toJson(value: GotoImplementationResponse): Json {
        return toJsonGotoImplementationResponse(value);
    },

    /** Return one GotoImplementationResponse from one JSON value. */
    fromJson(value: Json): GotoImplementationResponse {
        return fromJsonGotoImplementationResponse(value);
    },
};

/** Encode one GotoImplementationResponse. */
export function encodeGotoImplementationResponse(writer: BinaryWriter, value: GotoImplementationResponse): void {
    writer.writeUnsigned(value.targets.length);
    for (const item0 of value.targets) {
        encodeNavigationTarget(writer, item0);
    }
}

/** Decode one GotoImplementationResponse. */
export function decodeGotoImplementationResponse(reader: BinaryReader): GotoImplementationResponse {
    const targets = (() => { const length0 = reader.readNumber(); const items0: Array<NavigationTarget> = []; for (let index = 0; index < length0; index += 1) { items0.push(decodeNavigationTarget(reader)); } return items0; })();

    return {
        targets,
    };
}

/** Return one JSON value for one GotoImplementationResponse. */
export function toJsonGotoImplementationResponse(value: GotoImplementationResponse): Json {
    return {
        targets: value.targets.map((item0) => toJsonNavigationTarget(item0)),
    };
}

/** Return one GotoImplementationResponse from one JSON value. */
export function fromJsonGotoImplementationResponse(value: Json): GotoImplementationResponse {
    const object = jsonObject(value);

    return {
        targets: jsonArray(jsonField(object, "targets")).map((item0) => fromJsonNavigationTarget(item0)),
    };
}
