/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, jsonArray, jsonField, jsonObject, jsonOptional, jsonString } from "../../../protocol/serde.js";
import type { Target } from "../protocol/target.js";
import type { Module } from "../protocol/target.js";
import type { ProfileId } from "../../source/file/model/profile.js";
import { decodeTarget, encodeTarget, fromJsonTarget, toJsonTarget } from "../protocol/target.js";
import { decodeModule, encodeModule, fromJsonModule, toJsonModule } from "../protocol/target.js";
import { decodeProfileId, encodeProfileId, fromJsonProfileId, toJsonProfileId } from "../../source/file/model/profile.js";

/** Request payload for decorator queries. */
export type DecoratorsRequest = {
    /** The query scope. */
    readonly scope: DecoratorScope;
    /** The decorator name filter. */
    readonly name?: string;
};

export const DecoratorsRequest = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: DecoratorsRequest): void {
        encodeDecoratorsRequest(writer, value);
    },

    /** Decode one DecoratorsRequest. */
    decode(reader: BinaryReader): DecoratorsRequest {
        return decodeDecoratorsRequest(reader);
    },

    /** Return this value as JSON. */
    toJson(value: DecoratorsRequest): Json {
        return toJsonDecoratorsRequest(value);
    },

    /** Return one DecoratorsRequest from one JSON value. */
    fromJson(value: Json): DecoratorsRequest {
        return fromJsonDecoratorsRequest(value);
    },
};

/** Encode one DecoratorsRequest. */
export function encodeDecoratorsRequest(writer: BinaryWriter, value: DecoratorsRequest): void {
    encodeDecoratorScope(writer, value.scope);
    writer.writeOption(value.name, (value1) => {
        writer.writeString(value1);
    });
}

/** Decode one DecoratorsRequest. */
export function decodeDecoratorsRequest(reader: BinaryReader): DecoratorsRequest {
    const scope = decodeDecoratorScope(reader);
    const name = reader.readOption(() => reader.readString());

    return {
        scope,
        ...(name === undefined ? {} : { name }),
    };
}

/** Return one JSON value for one DecoratorsRequest. */
export function toJsonDecoratorsRequest(value: DecoratorsRequest): Json {
    return {
        scope: toJsonDecoratorScope(value.scope),
        ...(value.name === undefined ? {} : { name: value.name }),
    };
}

/** Return one DecoratorsRequest from one JSON value. */
export function fromJsonDecoratorsRequest(value: Json): DecoratorsRequest {
    const object = jsonObject(value);

    return {
        scope: fromJsonDecoratorScope(jsonField(object, "scope")),
        name: jsonOptional(object, "name", (value) => jsonString(value)),
    };
}

/** Scope for decorator queries. */
export type DecoratorScope =
    /** One module. */
    | {
          readonly kind: "module";
          readonly module: Module;
      }
    /** Program profiles. */
    | {
          readonly kind: "program";
          /** The profiles to search. */
          readonly profileIds: ReadonlyArray<ProfileId>;
      }
;

export const DecoratorScope = {
    /** One module. */
    "module"(module_: Module): DecoratorScope {
        return { kind: "module", module: module_ };
    },

    /** Program profiles. */
    program(profileIds: ReadonlyArray<ProfileId>): DecoratorScope {
        return { kind: "program", profileIds };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: DecoratorScope): void {
        encodeDecoratorScope(writer, value);
    },

    /** Decode one DecoratorScope. */
    decode(reader: BinaryReader): DecoratorScope {
        return decodeDecoratorScope(reader);
    },

    /** Return this value as JSON. */
    toJson(value: DecoratorScope): Json {
        return toJsonDecoratorScope(value);
    },

    /** Return one DecoratorScope from one JSON value. */
    fromJson(value: Json): DecoratorScope {
        return fromJsonDecoratorScope(value);
    },
};

/** Encode one DecoratorScope. */
export function encodeDecoratorScope(writer: BinaryWriter, value: DecoratorScope): void {
    switch (value.kind) {
        case "module":
            writer.writeUnsigned(0);
            encodeModule(writer, value.module);
            return;
        case "program":
            writer.writeUnsigned(1);
            writer.writeUnsigned(value.profileIds.length);
            for (const item0 of value.profileIds) {
                encodeProfileId(writer, item0);
            }
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one DecoratorScope. */
export function decodeDecoratorScope(reader: BinaryReader): DecoratorScope {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const module_ = decodeModule(reader);

            return { kind: "module", module: module_ };
        }
        case 1: {
            const profileIds = (() => { const length0 = reader.readNumber(); const items0: Array<ProfileId> = []; for (let index = 0; index < length0; index += 1) { items0.push(decodeProfileId(reader)); } return items0; })();

            return {
                kind: "program",
                profileIds,
            };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one DecoratorScope. */
export function toJsonDecoratorScope(value: DecoratorScope): Json {
    switch (value.kind) {
        case "module":
            return {
                kind: "module",
                module: toJsonModule(value.module),
            };
        case "program":
            return {
                kind: "program",
                profileIds: value.profileIds.map((item0) => toJsonProfileId(item0)),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one DecoratorScope from one JSON value. */
export function fromJsonDecoratorScope(value: Json): DecoratorScope {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "module":
            return {
                kind,
                module: fromJsonModule(jsonField(object, "module")),
            };
        case "program":
            return {
                kind,
                profileIds: jsonArray(jsonField(object, "profileIds")).map((item0) => fromJsonProfileId(item0)),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** Response payload for decorator queries. */
export type DecoratorsResponse = {
    /** Matching decorators. */
    readonly decorators: ReadonlyArray<DecoratorItem>;
};

export const DecoratorsResponse = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: DecoratorsResponse): void {
        encodeDecoratorsResponse(writer, value);
    },

    /** Decode one DecoratorsResponse. */
    decode(reader: BinaryReader): DecoratorsResponse {
        return decodeDecoratorsResponse(reader);
    },

    /** Return this value as JSON. */
    toJson(value: DecoratorsResponse): Json {
        return toJsonDecoratorsResponse(value);
    },

    /** Return one DecoratorsResponse from one JSON value. */
    fromJson(value: Json): DecoratorsResponse {
        return fromJsonDecoratorsResponse(value);
    },
};

/** Encode one DecoratorsResponse. */
export function encodeDecoratorsResponse(writer: BinaryWriter, value: DecoratorsResponse): void {
    writer.writeUnsigned(value.decorators.length);
    for (const item0 of value.decorators) {
        encodeDecoratorItem(writer, item0);
    }
}

/** Decode one DecoratorsResponse. */
export function decodeDecoratorsResponse(reader: BinaryReader): DecoratorsResponse {
    const decorators = (() => { const length0 = reader.readNumber(); const items0: Array<DecoratorItem> = []; for (let index = 0; index < length0; index += 1) { items0.push(decodeDecoratorItem(reader)); } return items0; })();

    return {
        decorators,
    };
}

/** Return one JSON value for one DecoratorsResponse. */
export function toJsonDecoratorsResponse(value: DecoratorsResponse): Json {
    return {
        decorators: value.decorators.map((item0) => toJsonDecoratorItem(item0)),
    };
}

/** Return one DecoratorsResponse from one JSON value. */
export function fromJsonDecoratorsResponse(value: Json): DecoratorsResponse {
    const object = jsonObject(value);

    return {
        decorators: jsonArray(jsonField(object, "decorators")).map((item0) => fromJsonDecoratorItem(item0)),
    };
}

/** One decorator query item. */
export type DecoratorItem = {
    /** The decorator name when syntactically known. */
    readonly name?: string;
    /** The decorator expression target. */
    readonly decorator: Target;
    /** The decorated target. */
    readonly target: Target;
    /** The resolved decorator role. */
    readonly role: DecoratorRole;
};

export const DecoratorItem = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: DecoratorItem): void {
        encodeDecoratorItem(writer, value);
    },

    /** Decode one DecoratorItem. */
    decode(reader: BinaryReader): DecoratorItem {
        return decodeDecoratorItem(reader);
    },

    /** Return this value as JSON. */
    toJson(value: DecoratorItem): Json {
        return toJsonDecoratorItem(value);
    },

    /** Return one DecoratorItem from one JSON value. */
    fromJson(value: Json): DecoratorItem {
        return fromJsonDecoratorItem(value);
    },
};

/** Encode one DecoratorItem. */
export function encodeDecoratorItem(writer: BinaryWriter, value: DecoratorItem): void {
    writer.writeOption(value.name, (value0) => {
        writer.writeString(value0);
    });
    encodeTarget(writer, value.decorator);
    encodeTarget(writer, value.target);
    encodeDecoratorRole(writer, value.role);
}

/** Decode one DecoratorItem. */
export function decodeDecoratorItem(reader: BinaryReader): DecoratorItem {
    const name = reader.readOption(() => reader.readString());
    const decorator = decodeTarget(reader);
    const target = decodeTarget(reader);
    const role = decodeDecoratorRole(reader);

    return {
        ...(name === undefined ? {} : { name }),
        decorator,
        target,
        role,
    };
}

/** Return one JSON value for one DecoratorItem. */
export function toJsonDecoratorItem(value: DecoratorItem): Json {
    return {
        ...(value.name === undefined ? {} : { name: value.name }),
        decorator: toJsonTarget(value.decorator),
        target: toJsonTarget(value.target),
        role: toJsonDecoratorRole(value.role),
    };
}

/** Return one DecoratorItem from one JSON value. */
export function fromJsonDecoratorItem(value: Json): DecoratorItem {
    const object = jsonObject(value);

    return {
        name: jsonOptional(object, "name", (value) => jsonString(value)),
        decorator: fromJsonTarget(jsonField(object, "decorator")),
        target: fromJsonTarget(jsonField(object, "target")),
        role: fromJsonDecoratorRole(jsonField(object, "role")),
    };
}

/** Role of one decorator expression. */
export type DecoratorRole = "languageItem" | "symbol" | "unresolved";

export const DecoratorRole = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: DecoratorRole): void {
        encodeDecoratorRole(writer, value);
    },

    /** Decode one DecoratorRole. */
    decode(reader: BinaryReader): DecoratorRole {
        return decodeDecoratorRole(reader);
    },

    /** Return this value as JSON. */
    toJson(value: DecoratorRole): Json {
        return toJsonDecoratorRole(value);
    },

    /** Return one DecoratorRole from one JSON value. */
    fromJson(value: Json): DecoratorRole {
        return fromJsonDecoratorRole(value);
    },
};

/** Encode one DecoratorRole. */
export function encodeDecoratorRole(writer: BinaryWriter, value: DecoratorRole): void {
    switch (value) {
        case "languageItem":
            writer.writeUnsigned(0);
            return;
        case "symbol":
            writer.writeUnsigned(1);
            return;
        case "unresolved":
            writer.writeUnsigned(2);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one DecoratorRole. */
export function decodeDecoratorRole(reader: BinaryReader): DecoratorRole {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "languageItem";
        case 1:
            return "symbol";
        case 2:
            return "unresolved";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one DecoratorRole. */
export function toJsonDecoratorRole(value: DecoratorRole): Json {
    return value;
}

/** Return one DecoratorRole from one JSON value. */
export function fromJsonDecoratorRole(value: Json): DecoratorRole {
    const variant = jsonString(value);

    switch (variant) {
        case "languageItem":
            return "languageItem";
        case "symbol":
            return "symbol";
        case "unresolved":
            return "unresolved";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}
