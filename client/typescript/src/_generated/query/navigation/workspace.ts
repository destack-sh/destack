/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, jsonArray, jsonField, jsonInteger, jsonObject, jsonOptional, jsonString } from "../../../protocol/serde.js";
import type { SymbolKind } from "../../dir/symbol/symbol.js";
import type { Target } from "../protocol/target.js";
import type { ProfileId } from "../../source/file/model/profile.js";
import { decodeSymbolKind, encodeSymbolKind, fromJsonSymbolKind, toJsonSymbolKind } from "../../dir/symbol/symbol.js";
import { decodeTarget, encodeTarget, fromJsonTarget, toJsonTarget } from "../protocol/target.js";
import { decodeProfileId, encodeProfileId, fromJsonProfileId, toJsonProfileId } from "../../source/file/model/profile.js";

/** Request symbol search for a query string. */
export type SymbolSearchRequest = {
    /** The profiles to search. */
    readonly profileIds: ReadonlyArray<ProfileId>;
    /** The search query string. */
    readonly query: string;
    /** The maximum number of results. */
    readonly maxResults: number;
};

export const SymbolSearchRequest = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: SymbolSearchRequest): void {
        encodeSymbolSearchRequest(writer, value);
    },

    /** Decode one SymbolSearchRequest. */
    decode(reader: BinaryReader): SymbolSearchRequest {
        return decodeSymbolSearchRequest(reader);
    },

    /** Return this value as JSON. */
    toJson(value: SymbolSearchRequest): Json {
        return toJsonSymbolSearchRequest(value);
    },

    /** Return one SymbolSearchRequest from one JSON value. */
    fromJson(value: Json): SymbolSearchRequest {
        return fromJsonSymbolSearchRequest(value);
    },
};

/** Encode one SymbolSearchRequest. */
export function encodeSymbolSearchRequest(writer: BinaryWriter, value: SymbolSearchRequest): void {
    writer.writeUnsigned(value.profileIds.length);
    for (const item0 of value.profileIds) {
        encodeProfileId(writer, item0);
    }
    writer.writeString(value.query);
    writer.writeUnsigned(value.maxResults);
}

/** Decode one SymbolSearchRequest. */
export function decodeSymbolSearchRequest(reader: BinaryReader): SymbolSearchRequest {
    const profileIds = (() => { const length0 = reader.readNumber(); const items0: Array<ProfileId> = []; for (let index = 0; index < length0; index += 1) { items0.push(decodeProfileId(reader)); } return items0; })();
    const query = reader.readString();
    const maxResults = reader.readNumber();

    return {
        profileIds,
        query,
        maxResults,
    };
}

/** Return one JSON value for one SymbolSearchRequest. */
export function toJsonSymbolSearchRequest(value: SymbolSearchRequest): Json {
    return {
        profileIds: value.profileIds.map((item0) => toJsonProfileId(item0)),
        query: value.query,
        maxResults: value.maxResults,
    };
}

/** Return one SymbolSearchRequest from one JSON value. */
export function fromJsonSymbolSearchRequest(value: Json): SymbolSearchRequest {
    const object = jsonObject(value);

    return {
        profileIds: jsonArray(jsonField(object, "profileIds")).map((item0) => fromJsonProfileId(item0)),
        query: jsonString(jsonField(object, "query")),
        maxResults: jsonInteger(jsonField(object, "maxResults")),
    };
}

/** Response payload for symbol search queries. */
export type SymbolSearchResponse = {
    /** The matching symbols. */
    readonly symbols: ReadonlyArray<SymbolMatch>;
};

export const SymbolSearchResponse = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: SymbolSearchResponse): void {
        encodeSymbolSearchResponse(writer, value);
    },

    /** Decode one SymbolSearchResponse. */
    decode(reader: BinaryReader): SymbolSearchResponse {
        return decodeSymbolSearchResponse(reader);
    },

    /** Return this value as JSON. */
    toJson(value: SymbolSearchResponse): Json {
        return toJsonSymbolSearchResponse(value);
    },

    /** Return one SymbolSearchResponse from one JSON value. */
    fromJson(value: Json): SymbolSearchResponse {
        return fromJsonSymbolSearchResponse(value);
    },
};

/** Encode one SymbolSearchResponse. */
export function encodeSymbolSearchResponse(writer: BinaryWriter, value: SymbolSearchResponse): void {
    writer.writeUnsigned(value.symbols.length);
    for (const item0 of value.symbols) {
        encodeSymbolMatch(writer, item0);
    }
}

/** Decode one SymbolSearchResponse. */
export function decodeSymbolSearchResponse(reader: BinaryReader): SymbolSearchResponse {
    const symbols = (() => { const length0 = reader.readNumber(); const items0: Array<SymbolMatch> = []; for (let index = 0; index < length0; index += 1) { items0.push(decodeSymbolMatch(reader)); } return items0; })();

    return {
        symbols,
    };
}

/** Return one JSON value for one SymbolSearchResponse. */
export function toJsonSymbolSearchResponse(value: SymbolSearchResponse): Json {
    return {
        symbols: value.symbols.map((item0) => toJsonSymbolMatch(item0)),
    };
}

/** Return one SymbolSearchResponse from one JSON value. */
export function fromJsonSymbolSearchResponse(value: Json): SymbolSearchResponse {
    const object = jsonObject(value);

    return {
        symbols: jsonArray(jsonField(object, "symbols")).map((item0) => fromJsonSymbolMatch(item0)),
    };
}

/** One symbol search result. */
export type SymbolMatch = {
    /** The symbol's name. */
    readonly name: string;
    /** The kind of symbol. */
    readonly kind: SymbolKind;
    /** The symbol source target. */
    readonly target: Target;
    /** Container name (e.g., class name for methods). */
    readonly container?: string;
};

export const SymbolMatch = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: SymbolMatch): void {
        encodeSymbolMatch(writer, value);
    },

    /** Decode one SymbolMatch. */
    decode(reader: BinaryReader): SymbolMatch {
        return decodeSymbolMatch(reader);
    },

    /** Return this value as JSON. */
    toJson(value: SymbolMatch): Json {
        return toJsonSymbolMatch(value);
    },

    /** Return one SymbolMatch from one JSON value. */
    fromJson(value: Json): SymbolMatch {
        return fromJsonSymbolMatch(value);
    },
};

/** Encode one SymbolMatch. */
export function encodeSymbolMatch(writer: BinaryWriter, value: SymbolMatch): void {
    writer.writeString(value.name);
    encodeSymbolKind(writer, value.kind);
    encodeTarget(writer, value.target);
    writer.writeOption(value.container, (value3) => {
        writer.writeString(value3);
    });
}

/** Decode one SymbolMatch. */
export function decodeSymbolMatch(reader: BinaryReader): SymbolMatch {
    const name = reader.readString();
    const kind = decodeSymbolKind(reader);
    const target = decodeTarget(reader);
    const container = reader.readOption(() => reader.readString());

    return {
        name,
        kind,
        target,
        ...(container === undefined ? {} : { container }),
    };
}

/** Return one JSON value for one SymbolMatch. */
export function toJsonSymbolMatch(value: SymbolMatch): Json {
    return {
        name: value.name,
        kind: toJsonSymbolKind(value.kind),
        target: toJsonTarget(value.target),
        ...(value.container === undefined ? {} : { container: value.container }),
    };
}

/** Return one SymbolMatch from one JSON value. */
export function fromJsonSymbolMatch(value: Json): SymbolMatch {
    const object = jsonObject(value);

    return {
        name: jsonString(jsonField(object, "name")),
        kind: fromJsonSymbolKind(jsonField(object, "kind")),
        target: fromJsonTarget(jsonField(object, "target")),
        container: jsonOptional(object, "container", (value) => jsonString(value)),
    };
}
