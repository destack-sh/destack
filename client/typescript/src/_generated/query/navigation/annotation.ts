/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, jsonArray, jsonField, jsonObject, jsonOptional, jsonString } from "../../../protocol/serde.js";
import type { QueryModule } from "../core/target.js";
import type { QueryTarget } from "../core/target.js";
import type { ProfileId } from "../../source/file/model/profile.js";
import { decodeQueryModule, encodeQueryModule, fromJsonQueryModule, toJsonQueryModule } from "../core/target.js";
import { decodeQueryTarget, encodeQueryTarget, fromJsonQueryTarget, toJsonQueryTarget } from "../core/target.js";
import { decodeProfileId, encodeProfileId, fromJsonProfileId, toJsonProfileId } from "../../source/file/model/profile.js";

/** Request payload for annotation queries. */
export type AnnotationsRequest = {
    /** The query scope. */
    readonly scope: AnnotationScope;
    /** The annotation name filter. */
    readonly name?: string;
};

export const AnnotationsRequest = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: AnnotationsRequest): void {
        encodeAnnotationsRequest(writer, value);
    },

    /** Decode one AnnotationsRequest. */
    decode(reader: BinaryReader): AnnotationsRequest {
        return decodeAnnotationsRequest(reader);
    },

    /** Return this value as JSON. */
    toJson(value: AnnotationsRequest): Json {
        return toJsonAnnotationsRequest(value);
    },

    /** Return one AnnotationsRequest from one JSON value. */
    fromJson(value: Json): AnnotationsRequest {
        return fromJsonAnnotationsRequest(value);
    },
};

/** Encode one AnnotationsRequest. */
export function encodeAnnotationsRequest(writer: BinaryWriter, value: AnnotationsRequest): void {
    encodeAnnotationScope(writer, value.scope);
    writer.writeOption(value.name, (value1) => {
        writer.writeString(value1);
    });
}

/** Decode one AnnotationsRequest. */
export function decodeAnnotationsRequest(reader: BinaryReader): AnnotationsRequest {
    const scope = decodeAnnotationScope(reader);
    const name = reader.readOption(() => reader.readString());

    return {
        scope,
        ...(name === undefined ? {} : { name }),
    };
}

/** Return one JSON value for one AnnotationsRequest. */
export function toJsonAnnotationsRequest(value: AnnotationsRequest): Json {
    return {
        scope: toJsonAnnotationScope(value.scope),
        ...(value.name === undefined ? {} : { name: value.name }),
    };
}

/** Return one AnnotationsRequest from one JSON value. */
export function fromJsonAnnotationsRequest(value: Json): AnnotationsRequest {
    const object = jsonObject(value);

    return {
        scope: fromJsonAnnotationScope(jsonField(object, "scope")),
        name: jsonOptional(object, "name", (value) => jsonString(value)),
    };
}

/** Scope for annotation queries. */
export type AnnotationScope =
    /** One module. */
    | {
          readonly kind: "module";
          readonly module: QueryModule;
      }
    /** Workspace profiles. */
    | {
          readonly kind: "workspace";
          /** The profiles to search. */
          readonly profileIds: ReadonlyArray<ProfileId>;
      }
;

export const AnnotationScope = {
    /** One module. */
    "module"(module_: QueryModule): AnnotationScope {
        return { kind: "module", module: module_ };
    },

    /** Workspace profiles. */
    workspace(profileIds: ReadonlyArray<ProfileId>): AnnotationScope {
        return { kind: "workspace", profileIds };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: AnnotationScope): void {
        encodeAnnotationScope(writer, value);
    },

    /** Decode one AnnotationScope. */
    decode(reader: BinaryReader): AnnotationScope {
        return decodeAnnotationScope(reader);
    },

    /** Return this value as JSON. */
    toJson(value: AnnotationScope): Json {
        return toJsonAnnotationScope(value);
    },

    /** Return one AnnotationScope from one JSON value. */
    fromJson(value: Json): AnnotationScope {
        return fromJsonAnnotationScope(value);
    },
};

/** Encode one AnnotationScope. */
export function encodeAnnotationScope(writer: BinaryWriter, value: AnnotationScope): void {
    switch (value.kind) {
        case "module":
            writer.writeUnsigned(0);
            encodeQueryModule(writer, value.module);
            return;
        case "workspace":
            writer.writeUnsigned(1);
            writer.writeUnsigned(value.profileIds.length);
            for (const item0 of value.profileIds) {
                encodeProfileId(writer, item0);
            }
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one AnnotationScope. */
export function decodeAnnotationScope(reader: BinaryReader): AnnotationScope {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const module_ = decodeQueryModule(reader);

            return { kind: "module", module: module_ };
        }
        case 1: {
            const profileIds = (() => { const length0 = reader.readNumber(); const items0: Array<ProfileId> = []; for (let index = 0; index < length0; index += 1) { items0.push(decodeProfileId(reader)); } return items0; })();

            return {
                kind: "workspace",
                profileIds,
            };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one AnnotationScope. */
export function toJsonAnnotationScope(value: AnnotationScope): Json {
    switch (value.kind) {
        case "module":
            return {
                kind: "module",
                module: toJsonQueryModule(value.module),
            };
        case "workspace":
            return {
                kind: "workspace",
                profileIds: value.profileIds.map((item0) => toJsonProfileId(item0)),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one AnnotationScope from one JSON value. */
export function fromJsonAnnotationScope(value: Json): AnnotationScope {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "module":
            return {
                kind,
                module: fromJsonQueryModule(jsonField(object, "module")),
            };
        case "workspace":
            return {
                kind,
                profileIds: jsonArray(jsonField(object, "profileIds")).map((item0) => fromJsonProfileId(item0)),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** Response payload for annotation queries. */
export type AnnotationsResponse = {
    /** Matching annotations. */
    readonly annotations: ReadonlyArray<AnnotationItem>;
};

export const AnnotationsResponse = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: AnnotationsResponse): void {
        encodeAnnotationsResponse(writer, value);
    },

    /** Decode one AnnotationsResponse. */
    decode(reader: BinaryReader): AnnotationsResponse {
        return decodeAnnotationsResponse(reader);
    },

    /** Return this value as JSON. */
    toJson(value: AnnotationsResponse): Json {
        return toJsonAnnotationsResponse(value);
    },

    /** Return one AnnotationsResponse from one JSON value. */
    fromJson(value: Json): AnnotationsResponse {
        return fromJsonAnnotationsResponse(value);
    },
};

/** Encode one AnnotationsResponse. */
export function encodeAnnotationsResponse(writer: BinaryWriter, value: AnnotationsResponse): void {
    writer.writeUnsigned(value.annotations.length);
    for (const item0 of value.annotations) {
        encodeAnnotationItem(writer, item0);
    }
}

/** Decode one AnnotationsResponse. */
export function decodeAnnotationsResponse(reader: BinaryReader): AnnotationsResponse {
    const annotations = (() => { const length0 = reader.readNumber(); const items0: Array<AnnotationItem> = []; for (let index = 0; index < length0; index += 1) { items0.push(decodeAnnotationItem(reader)); } return items0; })();

    return {
        annotations,
    };
}

/** Return one JSON value for one AnnotationsResponse. */
export function toJsonAnnotationsResponse(value: AnnotationsResponse): Json {
    return {
        annotations: value.annotations.map((item0) => toJsonAnnotationItem(item0)),
    };
}

/** Return one AnnotationsResponse from one JSON value. */
export function fromJsonAnnotationsResponse(value: Json): AnnotationsResponse {
    const object = jsonObject(value);

    return {
        annotations: jsonArray(jsonField(object, "annotations")).map((item0) => fromJsonAnnotationItem(item0)),
    };
}

/** One annotation query item. */
export type AnnotationItem = {
    /** The annotation name when syntactically known. */
    readonly name?: string;
    /** The decorator expression target. */
    readonly decorator: QueryTarget;
    /** The annotated target. */
    readonly target: QueryTarget;
    /** The resolved annotation role. */
    readonly role: AnnotationRole;
};

export const AnnotationItem = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: AnnotationItem): void {
        encodeAnnotationItem(writer, value);
    },

    /** Decode one AnnotationItem. */
    decode(reader: BinaryReader): AnnotationItem {
        return decodeAnnotationItem(reader);
    },

    /** Return this value as JSON. */
    toJson(value: AnnotationItem): Json {
        return toJsonAnnotationItem(value);
    },

    /** Return one AnnotationItem from one JSON value. */
    fromJson(value: Json): AnnotationItem {
        return fromJsonAnnotationItem(value);
    },
};

/** Encode one AnnotationItem. */
export function encodeAnnotationItem(writer: BinaryWriter, value: AnnotationItem): void {
    writer.writeOption(value.name, (value0) => {
        writer.writeString(value0);
    });
    encodeQueryTarget(writer, value.decorator);
    encodeQueryTarget(writer, value.target);
    encodeAnnotationRole(writer, value.role);
}

/** Decode one AnnotationItem. */
export function decodeAnnotationItem(reader: BinaryReader): AnnotationItem {
    const name = reader.readOption(() => reader.readString());
    const decorator = decodeQueryTarget(reader);
    const target = decodeQueryTarget(reader);
    const role = decodeAnnotationRole(reader);

    return {
        ...(name === undefined ? {} : { name }),
        decorator,
        target,
        role,
    };
}

/** Return one JSON value for one AnnotationItem. */
export function toJsonAnnotationItem(value: AnnotationItem): Json {
    return {
        ...(value.name === undefined ? {} : { name: value.name }),
        decorator: toJsonQueryTarget(value.decorator),
        target: toJsonQueryTarget(value.target),
        role: toJsonAnnotationRole(value.role),
    };
}

/** Return one AnnotationItem from one JSON value. */
export function fromJsonAnnotationItem(value: Json): AnnotationItem {
    const object = jsonObject(value);

    return {
        name: jsonOptional(object, "name", (value) => jsonString(value)),
        decorator: fromJsonQueryTarget(jsonField(object, "decorator")),
        target: fromJsonQueryTarget(jsonField(object, "target")),
        role: fromJsonAnnotationRole(jsonField(object, "role")),
    };
}

/** Role of one annotation expression. */
export type AnnotationRole = "annotation" | "decorator" | "unknown";

export const AnnotationRole = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: AnnotationRole): void {
        encodeAnnotationRole(writer, value);
    },

    /** Decode one AnnotationRole. */
    decode(reader: BinaryReader): AnnotationRole {
        return decodeAnnotationRole(reader);
    },

    /** Return this value as JSON. */
    toJson(value: AnnotationRole): Json {
        return toJsonAnnotationRole(value);
    },

    /** Return one AnnotationRole from one JSON value. */
    fromJson(value: Json): AnnotationRole {
        return fromJsonAnnotationRole(value);
    },
};

/** Encode one AnnotationRole. */
export function encodeAnnotationRole(writer: BinaryWriter, value: AnnotationRole): void {
    switch (value) {
        case "annotation":
            writer.writeUnsigned(0);
            return;
        case "decorator":
            writer.writeUnsigned(1);
            return;
        case "unknown":
            writer.writeUnsigned(2);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one AnnotationRole. */
export function decodeAnnotationRole(reader: BinaryReader): AnnotationRole {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "annotation";
        case 1:
            return "decorator";
        case 2:
            return "unknown";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one AnnotationRole. */
export function toJsonAnnotationRole(value: AnnotationRole): Json {
    return value;
}

/** Return one AnnotationRole from one JSON value. */
export function fromJsonAnnotationRole(value: Json): AnnotationRole {
    const variant = jsonString(value);

    switch (variant) {
        case "annotation":
            return "annotation";
        case "decorator":
            return "decorator";
        case "unknown":
            return "unknown";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}
