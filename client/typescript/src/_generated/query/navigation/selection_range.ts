/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, jsonArray, jsonField, jsonInteger, jsonObject, jsonOptional } from "../../../protocol/serde.js";
import type { QueryModule } from "../core/target.js";
import type { Span } from "../../source/file/model/span.js";
import { decodeQueryModule, encodeQueryModule, fromJsonQueryModule, toJsonQueryModule } from "../core/target.js";
import { decodeSpan, encodeSpan, fromJsonSpan, toJsonSpan } from "../../source/file/model/span.js";

/** Request selection ranges for positions in a document. */
export type SelectionRangesRequest = {
    /** The queried module. */
    readonly module: QueryModule;
    /** The byte offsets in the document. */
    readonly offsets: ReadonlyArray<number>;
};

export const SelectionRangesRequest = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: SelectionRangesRequest): void {
        encodeSelectionRangesRequest(writer, value);
    },

    /** Decode one SelectionRangesRequest. */
    decode(reader: BinaryReader): SelectionRangesRequest {
        return decodeSelectionRangesRequest(reader);
    },

    /** Return this value as JSON. */
    toJson(value: SelectionRangesRequest): Json {
        return toJsonSelectionRangesRequest(value);
    },

    /** Return one SelectionRangesRequest from one JSON value. */
    fromJson(value: Json): SelectionRangesRequest {
        return fromJsonSelectionRangesRequest(value);
    },
};

/** Encode one SelectionRangesRequest. */
export function encodeSelectionRangesRequest(writer: BinaryWriter, value: SelectionRangesRequest): void {
    encodeQueryModule(writer, value.module);
    writer.writeUnsigned(value.offsets.length);
    for (const item1 of value.offsets) {
        writer.writeUnsigned(item1);
    }
}

/** Decode one SelectionRangesRequest. */
export function decodeSelectionRangesRequest(reader: BinaryReader): SelectionRangesRequest {
    const module_ = decodeQueryModule(reader);
    const offsets = (() => { const length1 = reader.readNumber(); const items1: Array<number> = []; for (let index = 0; index < length1; index += 1) { items1.push(reader.readNumber()); } return items1; })();

    return {
        module: module_,
        offsets,
    };
}

/** Return one JSON value for one SelectionRangesRequest. */
export function toJsonSelectionRangesRequest(value: SelectionRangesRequest): Json {
    return {
        module: toJsonQueryModule(value.module),
        offsets: value.offsets.map((item0) => item0),
    };
}

/** Return one SelectionRangesRequest from one JSON value. */
export function fromJsonSelectionRangesRequest(value: Json): SelectionRangesRequest {
    const object = jsonObject(value);

    return {
        module: fromJsonQueryModule(jsonField(object, "module")),
        offsets: jsonArray(jsonField(object, "offsets")).map((item0) => jsonInteger(item0)),
    };
}

/** Response payload for selection ranges queries. */
export type SelectionRangesResponse = {
    /** Selection ranges. */
    readonly ranges: ReadonlyArray<SelectionRange>;
};

export const SelectionRangesResponse = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: SelectionRangesResponse): void {
        encodeSelectionRangesResponse(writer, value);
    },

    /** Decode one SelectionRangesResponse. */
    decode(reader: BinaryReader): SelectionRangesResponse {
        return decodeSelectionRangesResponse(reader);
    },

    /** Return this value as JSON. */
    toJson(value: SelectionRangesResponse): Json {
        return toJsonSelectionRangesResponse(value);
    },

    /** Return one SelectionRangesResponse from one JSON value. */
    fromJson(value: Json): SelectionRangesResponse {
        return fromJsonSelectionRangesResponse(value);
    },
};

/** Encode one SelectionRangesResponse. */
export function encodeSelectionRangesResponse(writer: BinaryWriter, value: SelectionRangesResponse): void {
    writer.writeUnsigned(value.ranges.length);
    for (const item0 of value.ranges) {
        encodeSelectionRange(writer, item0);
    }
}

/** Decode one SelectionRangesResponse. */
export function decodeSelectionRangesResponse(reader: BinaryReader): SelectionRangesResponse {
    const ranges = (() => { const length0 = reader.readNumber(); const items0: Array<SelectionRange> = []; for (let index = 0; index < length0; index += 1) { items0.push(decodeSelectionRange(reader)); } return items0; })();

    return {
        ranges,
    };
}

/** Return one JSON value for one SelectionRangesResponse. */
export function toJsonSelectionRangesResponse(value: SelectionRangesResponse): Json {
    return {
        ranges: value.ranges.map((item0) => toJsonSelectionRange(item0)),
    };
}

/** Return one SelectionRangesResponse from one JSON value. */
export function fromJsonSelectionRangesResponse(value: Json): SelectionRangesResponse {
    const object = jsonObject(value);

    return {
        ranges: jsonArray(jsonField(object, "ranges")).map((item0) => fromJsonSelectionRange(item0)),
    };
}

/** A selection range with parent. */
export type SelectionRange = {
    /** The range of this selection. */
    readonly range: Span;
    /** The parent selection range (for expand selection). */
    readonly parent?: SelectionRange;
};

export const SelectionRange = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: SelectionRange): void {
        encodeSelectionRange(writer, value);
    },

    /** Decode one SelectionRange. */
    decode(reader: BinaryReader): SelectionRange {
        return decodeSelectionRange(reader);
    },

    /** Return this value as JSON. */
    toJson(value: SelectionRange): Json {
        return toJsonSelectionRange(value);
    },

    /** Return one SelectionRange from one JSON value. */
    fromJson(value: Json): SelectionRange {
        return fromJsonSelectionRange(value);
    },
};

/** Encode one SelectionRange. */
export function encodeSelectionRange(writer: BinaryWriter, value: SelectionRange): void {
    encodeSpan(writer, value.range);
    writer.writeOption(value.parent, (value1) => {
        encodeSelectionRange(writer, value1);
    });
}

/** Decode one SelectionRange. */
export function decodeSelectionRange(reader: BinaryReader): SelectionRange {
    const range = decodeSpan(reader);
    const parent = reader.readOption(() => decodeSelectionRange(reader));

    return {
        range,
        ...(parent === undefined ? {} : { parent }),
    };
}

/** Return one JSON value for one SelectionRange. */
export function toJsonSelectionRange(value: SelectionRange): Json {
    return {
        range: toJsonSpan(value.range),
        ...(value.parent === undefined ? {} : { parent: toJsonSelectionRange(value.parent) }),
    };
}

/** Return one SelectionRange from one JSON value. */
export function fromJsonSelectionRange(value: Json): SelectionRange {
    const object = jsonObject(value);

    return {
        range: fromJsonSpan(jsonField(object, "range")),
        parent: jsonOptional(object, "parent", (value) => fromJsonSelectionRange(value)),
    };
}
