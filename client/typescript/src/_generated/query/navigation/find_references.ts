/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, jsonArray, jsonBool, jsonField, jsonObject } from "../../../protocol/serde.js";
import type { GlobalSymbolId } from "../../dir/symbol/symbol.js";
import type { Target } from "../protocol/target.js";
import type { QueryPosition } from "../protocol/target.js";
import { decodeGlobalSymbolId, encodeGlobalSymbolId, fromJsonGlobalSymbolId, toJsonGlobalSymbolId } from "../../dir/symbol/symbol.js";
import { decodeTarget, encodeTarget, fromJsonTarget, toJsonTarget } from "../protocol/target.js";
import { decodeQueryPosition, encodeQueryPosition, fromJsonQueryPosition, toJsonQueryPosition } from "../protocol/target.js";

/** A find references request. */
export type FindReferencesRequest = {
    /** The queried position. */
    readonly position: QueryPosition;
    /** Whether to include the declaration in results. */
    readonly includeDeclaration: boolean;
};

export const FindReferencesRequest = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: FindReferencesRequest): void {
        encodeFindReferencesRequest(writer, value);
    },

    /** Decode one FindReferencesRequest. */
    decode(reader: BinaryReader): FindReferencesRequest {
        return decodeFindReferencesRequest(reader);
    },

    /** Return this value as JSON. */
    toJson(value: FindReferencesRequest): Json {
        return toJsonFindReferencesRequest(value);
    },

    /** Return one FindReferencesRequest from one JSON value. */
    fromJson(value: Json): FindReferencesRequest {
        return fromJsonFindReferencesRequest(value);
    },
};

/** Encode one FindReferencesRequest. */
export function encodeFindReferencesRequest(writer: BinaryWriter, value: FindReferencesRequest): void {
    encodeQueryPosition(writer, value.position);
    writer.writeBool(value.includeDeclaration);
}

/** Decode one FindReferencesRequest. */
export function decodeFindReferencesRequest(reader: BinaryReader): FindReferencesRequest {
    const position = decodeQueryPosition(reader);
    const includeDeclaration = reader.readBool();

    return {
        position,
        includeDeclaration,
    };
}

/** Return one JSON value for one FindReferencesRequest. */
export function toJsonFindReferencesRequest(value: FindReferencesRequest): Json {
    return {
        position: toJsonQueryPosition(value.position),
        includeDeclaration: value.includeDeclaration,
    };
}

/** Return one FindReferencesRequest from one JSON value. */
export function fromJsonFindReferencesRequest(value: Json): FindReferencesRequest {
    const object = jsonObject(value);

    return {
        position: fromJsonQueryPosition(jsonField(object, "position")),
        includeDeclaration: jsonBool(jsonField(object, "includeDeclaration")),
    };
}

/** A find references response. */
export type FindReferencesResponse = {
    /** Reference occurrences. */
    readonly references: ReadonlyArray<ReferenceOccurrence>;
};

export const FindReferencesResponse = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: FindReferencesResponse): void {
        encodeFindReferencesResponse(writer, value);
    },

    /** Decode one FindReferencesResponse. */
    decode(reader: BinaryReader): FindReferencesResponse {
        return decodeFindReferencesResponse(reader);
    },

    /** Return this value as JSON. */
    toJson(value: FindReferencesResponse): Json {
        return toJsonFindReferencesResponse(value);
    },

    /** Return one FindReferencesResponse from one JSON value. */
    fromJson(value: Json): FindReferencesResponse {
        return fromJsonFindReferencesResponse(value);
    },
};

/** Encode one FindReferencesResponse. */
export function encodeFindReferencesResponse(writer: BinaryWriter, value: FindReferencesResponse): void {
    writer.writeUnsigned(value.references.length);
    for (const item0 of value.references) {
        encodeReferenceOccurrence(writer, item0);
    }
}

/** Decode one FindReferencesResponse. */
export function decodeFindReferencesResponse(reader: BinaryReader): FindReferencesResponse {
    const references = (() => { const length0 = reader.readNumber(); const items0: Array<ReferenceOccurrence> = []; for (let index = 0; index < length0; index += 1) { items0.push(decodeReferenceOccurrence(reader)); } return items0; })();

    return {
        references,
    };
}

/** Return one JSON value for one FindReferencesResponse. */
export function toJsonFindReferencesResponse(value: FindReferencesResponse): Json {
    return {
        references: value.references.map((item0) => toJsonReferenceOccurrence(item0)),
    };
}

/** Return one FindReferencesResponse from one JSON value. */
export function fromJsonFindReferencesResponse(value: Json): FindReferencesResponse {
    const object = jsonObject(value);

    return {
        references: jsonArray(jsonField(object, "references")).map((item0) => fromJsonReferenceOccurrence(item0)),
    };
}

/** One symbol reference occurrence. */
export type ReferenceOccurrence = {
    /** The reference source. */
    readonly target: Target;
    /** The referenced symbols. */
    readonly symbols: ReadonlyArray<GlobalSymbolId>;
};

export const ReferenceOccurrence = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ReferenceOccurrence): void {
        encodeReferenceOccurrence(writer, value);
    },

    /** Decode one ReferenceOccurrence. */
    decode(reader: BinaryReader): ReferenceOccurrence {
        return decodeReferenceOccurrence(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ReferenceOccurrence): Json {
        return toJsonReferenceOccurrence(value);
    },

    /** Return one ReferenceOccurrence from one JSON value. */
    fromJson(value: Json): ReferenceOccurrence {
        return fromJsonReferenceOccurrence(value);
    },
};

/** Encode one ReferenceOccurrence. */
export function encodeReferenceOccurrence(writer: BinaryWriter, value: ReferenceOccurrence): void {
    encodeTarget(writer, value.target);
    writer.writeUnsigned(value.symbols.length);
    for (const item1 of value.symbols) {
        encodeGlobalSymbolId(writer, item1);
    }
}

/** Decode one ReferenceOccurrence. */
export function decodeReferenceOccurrence(reader: BinaryReader): ReferenceOccurrence {
    const target = decodeTarget(reader);
    const symbols = (() => { const length1 = reader.readNumber(); const items1: Array<GlobalSymbolId> = []; for (let index = 0; index < length1; index += 1) { items1.push(decodeGlobalSymbolId(reader)); } return items1; })();

    return {
        target,
        symbols,
    };
}

/** Return one JSON value for one ReferenceOccurrence. */
export function toJsonReferenceOccurrence(value: ReferenceOccurrence): Json {
    return {
        target: toJsonTarget(value.target),
        symbols: value.symbols.map((item0) => toJsonGlobalSymbolId(item0)),
    };
}

/** Return one ReferenceOccurrence from one JSON value. */
export function fromJsonReferenceOccurrence(value: Json): ReferenceOccurrence {
    const object = jsonObject(value);

    return {
        target: fromJsonTarget(jsonField(object, "target")),
        symbols: jsonArray(jsonField(object, "symbols")).map((item0) => fromJsonGlobalSymbolId(item0)),
    };
}
