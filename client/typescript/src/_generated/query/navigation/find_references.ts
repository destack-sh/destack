/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, jsonArray, jsonBool, jsonField, jsonObject, jsonString } from "../../../protocol/serde.js";
import type { QueryPosition } from "../core/target.js";
import type { QueryTarget } from "../core/target.js";
import { decodeQueryPosition, encodeQueryPosition, fromJsonQueryPosition, toJsonQueryPosition } from "../core/target.js";
import { decodeQueryTarget, encodeQueryTarget, fromJsonQueryTarget, toJsonQueryTarget } from "../core/target.js";

/** Request find references at a cursor position. */
export type FindReferencesRequest = {
    /** The queried position. */
    readonly position: QueryPosition;
    /** Whether to include the declaration in results. */
    readonly includeDeclaration: boolean;
};

export const FindReferencesRequest = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: FindReferencesRequest): void {
        encodeFindReferencesRequest(writer, value);
    },

    /** Decode one FindReferencesRequest. */
    decode(reader: BinaryReader): FindReferencesRequest {
        return decodeFindReferencesRequest(reader);
    },

    /** Return this value as JSON. */
    toJson(value: FindReferencesRequest): Json {
        return toJsonFindReferencesRequest(value);
    },

    /** Return one FindReferencesRequest from one JSON value. */
    fromJson(value: Json): FindReferencesRequest {
        return fromJsonFindReferencesRequest(value);
    },
};

/** Encode one FindReferencesRequest. */
export function encodeFindReferencesRequest(writer: BinaryWriter, value: FindReferencesRequest): void {
    encodeQueryPosition(writer, value.position);
    writer.writeBool(value.includeDeclaration);
}

/** Decode one FindReferencesRequest. */
export function decodeFindReferencesRequest(reader: BinaryReader): FindReferencesRequest {
    const position = decodeQueryPosition(reader);
    const includeDeclaration = reader.readBool();

    return {
        position,
        includeDeclaration,
    };
}

/** Return one JSON value for one FindReferencesRequest. */
export function toJsonFindReferencesRequest(value: FindReferencesRequest): Json {
    return {
        position: toJsonQueryPosition(value.position),
        includeDeclaration: value.includeDeclaration,
    };
}

/** Return one FindReferencesRequest from one JSON value. */
export function fromJsonFindReferencesRequest(value: Json): FindReferencesRequest {
    const object = jsonObject(value);

    return {
        position: fromJsonQueryPosition(jsonField(object, "position")),
        includeDeclaration: jsonBool(jsonField(object, "includeDeclaration")),
    };
}

/** Response payload for find references queries. */
export type FindReferencesResponse = {
    /** Reference occurrences. */
    readonly references: ReadonlyArray<Reference>;
};

export const FindReferencesResponse = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: FindReferencesResponse): void {
        encodeFindReferencesResponse(writer, value);
    },

    /** Decode one FindReferencesResponse. */
    decode(reader: BinaryReader): FindReferencesResponse {
        return decodeFindReferencesResponse(reader);
    },

    /** Return this value as JSON. */
    toJson(value: FindReferencesResponse): Json {
        return toJsonFindReferencesResponse(value);
    },

    /** Return one FindReferencesResponse from one JSON value. */
    fromJson(value: Json): FindReferencesResponse {
        return fromJsonFindReferencesResponse(value);
    },
};

/** Encode one FindReferencesResponse. */
export function encodeFindReferencesResponse(writer: BinaryWriter, value: FindReferencesResponse): void {
    writer.writeUnsigned(value.references.length);
    for (const item0 of value.references) {
        encodeReference(writer, item0);
    }
}

/** Decode one FindReferencesResponse. */
export function decodeFindReferencesResponse(reader: BinaryReader): FindReferencesResponse {
    const references = (() => { const length0 = reader.readNumber(); const items0: Array<Reference> = []; for (let index = 0; index < length0; index += 1) { items0.push(decodeReference(reader)); } return items0; })();

    return {
        references,
    };
}

/** Return one JSON value for one FindReferencesResponse. */
export function toJsonFindReferencesResponse(value: FindReferencesResponse): Json {
    return {
        references: value.references.map((item0) => toJsonReference(item0)),
    };
}

/** Return one FindReferencesResponse from one JSON value. */
export function fromJsonFindReferencesResponse(value: Json): FindReferencesResponse {
    const object = jsonObject(value);

    return {
        references: jsonArray(jsonField(object, "references")).map((item0) => fromJsonReference(item0)),
    };
}

/** One symbol reference occurrence. */
export type Reference = {
    /** The referenced source target. */
    readonly target: QueryTarget;
    /** The reference role. */
    readonly role: ReferenceRole;
};

export const Reference = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: Reference): void {
        encodeReference(writer, value);
    },

    /** Decode one Reference. */
    decode(reader: BinaryReader): Reference {
        return decodeReference(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Reference): Json {
        return toJsonReference(value);
    },

    /** Return one Reference from one JSON value. */
    fromJson(value: Json): Reference {
        return fromJsonReference(value);
    },
};

/** Encode one Reference. */
export function encodeReference(writer: BinaryWriter, value: Reference): void {
    encodeQueryTarget(writer, value.target);
    encodeReferenceRole(writer, value.role);
}

/** Decode one Reference. */
export function decodeReference(reader: BinaryReader): Reference {
    const target = decodeQueryTarget(reader);
    const role = decodeReferenceRole(reader);

    return {
        target,
        role,
    };
}

/** Return one JSON value for one Reference. */
export function toJsonReference(value: Reference): Json {
    return {
        target: toJsonQueryTarget(value.target),
        role: toJsonReferenceRole(value.role),
    };
}

/** Return one Reference from one JSON value. */
export function fromJsonReference(value: Json): Reference {
    const object = jsonObject(value);

    return {
        target: fromJsonQueryTarget(jsonField(object, "target")),
        role: fromJsonReferenceRole(jsonField(object, "role")),
    };
}

/** Role of one reference occurrence. */
export type ReferenceRole = "declaration" | "read" | "write" | "type" | "import" | "export";

export const ReferenceRole = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ReferenceRole): void {
        encodeReferenceRole(writer, value);
    },

    /** Decode one ReferenceRole. */
    decode(reader: BinaryReader): ReferenceRole {
        return decodeReferenceRole(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ReferenceRole): Json {
        return toJsonReferenceRole(value);
    },

    /** Return one ReferenceRole from one JSON value. */
    fromJson(value: Json): ReferenceRole {
        return fromJsonReferenceRole(value);
    },
};

/** Encode one ReferenceRole. */
export function encodeReferenceRole(writer: BinaryWriter, value: ReferenceRole): void {
    switch (value) {
        case "declaration":
            writer.writeUnsigned(0);
            return;
        case "read":
            writer.writeUnsigned(1);
            return;
        case "write":
            writer.writeUnsigned(2);
            return;
        case "type":
            writer.writeUnsigned(3);
            return;
        case "import":
            writer.writeUnsigned(4);
            return;
        case "export":
            writer.writeUnsigned(5);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one ReferenceRole. */
export function decodeReferenceRole(reader: BinaryReader): ReferenceRole {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "declaration";
        case 1:
            return "read";
        case 2:
            return "write";
        case 3:
            return "type";
        case 4:
            return "import";
        case 5:
            return "export";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one ReferenceRole. */
export function toJsonReferenceRole(value: ReferenceRole): Json {
    return value;
}

/** Return one ReferenceRole from one JSON value. */
export function fromJsonReferenceRole(value: Json): ReferenceRole {
    const variant = jsonString(value);

    switch (variant) {
        case "declaration":
            return "declaration";
        case "read":
            return "read";
        case "write":
            return "write";
        case "type":
            return "type";
        case "import":
            return "import";
        case "export":
            return "export";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}
