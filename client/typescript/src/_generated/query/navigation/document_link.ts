/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, jsonArray, jsonField, jsonInteger, jsonObject, jsonOptional, jsonString } from "../../../protocol/serde.js";
import type { QueryModule } from "../core/target.js";
import type { Span } from "../../source/file/model/span.js";
import { decodeQueryModule, encodeQueryModule, fromJsonQueryModule, toJsonQueryModule } from "../core/target.js";
import { decodeSpan, encodeSpan, fromJsonSpan, toJsonSpan } from "../../source/file/model/span.js";

/** Request document links for a document. */
export type DocumentLinksRequest = {
    /** The queried module. */
    readonly module: QueryModule;
};

export const DocumentLinksRequest = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: DocumentLinksRequest): void {
        encodeDocumentLinksRequest(writer, value);
    },

    /** Decode one DocumentLinksRequest. */
    decode(reader: BinaryReader): DocumentLinksRequest {
        return decodeDocumentLinksRequest(reader);
    },

    /** Return this value as JSON. */
    toJson(value: DocumentLinksRequest): Json {
        return toJsonDocumentLinksRequest(value);
    },

    /** Return one DocumentLinksRequest from one JSON value. */
    fromJson(value: Json): DocumentLinksRequest {
        return fromJsonDocumentLinksRequest(value);
    },
};

/** Encode one DocumentLinksRequest. */
export function encodeDocumentLinksRequest(writer: BinaryWriter, value: DocumentLinksRequest): void {
    encodeQueryModule(writer, value.module);
}

/** Decode one DocumentLinksRequest. */
export function decodeDocumentLinksRequest(reader: BinaryReader): DocumentLinksRequest {
    const module_ = decodeQueryModule(reader);

    return {
        module: module_,
    };
}

/** Return one JSON value for one DocumentLinksRequest. */
export function toJsonDocumentLinksRequest(value: DocumentLinksRequest): Json {
    return {
        module: toJsonQueryModule(value.module),
    };
}

/** Return one DocumentLinksRequest from one JSON value. */
export function fromJsonDocumentLinksRequest(value: Json): DocumentLinksRequest {
    const object = jsonObject(value);

    return {
        module: fromJsonQueryModule(jsonField(object, "module")),
    };
}

/** Response payload for document links queries. */
export type DocumentLinksResponse = {
    /** Document links. */
    readonly links: ReadonlyArray<DocumentLink>;
};

export const DocumentLinksResponse = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: DocumentLinksResponse): void {
        encodeDocumentLinksResponse(writer, value);
    },

    /** Decode one DocumentLinksResponse. */
    decode(reader: BinaryReader): DocumentLinksResponse {
        return decodeDocumentLinksResponse(reader);
    },

    /** Return this value as JSON. */
    toJson(value: DocumentLinksResponse): Json {
        return toJsonDocumentLinksResponse(value);
    },

    /** Return one DocumentLinksResponse from one JSON value. */
    fromJson(value: Json): DocumentLinksResponse {
        return fromJsonDocumentLinksResponse(value);
    },
};

/** Encode one DocumentLinksResponse. */
export function encodeDocumentLinksResponse(writer: BinaryWriter, value: DocumentLinksResponse): void {
    writer.writeUnsigned(value.links.length);
    for (const item0 of value.links) {
        encodeDocumentLink(writer, item0);
    }
}

/** Decode one DocumentLinksResponse. */
export function decodeDocumentLinksResponse(reader: BinaryReader): DocumentLinksResponse {
    const links = (() => { const length0 = reader.readNumber(); const items0: Array<DocumentLink> = []; for (let index = 0; index < length0; index += 1) { items0.push(decodeDocumentLink(reader)); } return items0; })();

    return {
        links,
    };
}

/** Return one JSON value for one DocumentLinksResponse. */
export function toJsonDocumentLinksResponse(value: DocumentLinksResponse): Json {
    return {
        links: value.links.map((item0) => toJsonDocumentLink(item0)),
    };
}

/** Return one DocumentLinksResponse from one JSON value. */
export function fromJsonDocumentLinksResponse(value: Json): DocumentLinksResponse {
    const object = jsonObject(value);

    return {
        links: jsonArray(jsonField(object, "links")).map((item0) => fromJsonDocumentLink(item0)),
    };
}

/** A clickable link in a document. */
export type DocumentLink = {
    /** The range of the link in the document. */
    readonly range: Span;
    /** The target of the link. */
    readonly target: DocumentLinkTarget;
    /** Tooltip text (shown on hover). */
    readonly tooltip?: string;
};

export const DocumentLink = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: DocumentLink): void {
        encodeDocumentLink(writer, value);
    },

    /** Decode one DocumentLink. */
    decode(reader: BinaryReader): DocumentLink {
        return decodeDocumentLink(reader);
    },

    /** Return this value as JSON. */
    toJson(value: DocumentLink): Json {
        return toJsonDocumentLink(value);
    },

    /** Return one DocumentLink from one JSON value. */
    fromJson(value: Json): DocumentLink {
        return fromJsonDocumentLink(value);
    },
};

/** Encode one DocumentLink. */
export function encodeDocumentLink(writer: BinaryWriter, value: DocumentLink): void {
    encodeSpan(writer, value.range);
    encodeDocumentLinkTarget(writer, value.target);
    writer.writeOption(value.tooltip, (value2) => {
        writer.writeString(value2);
    });
}

/** Decode one DocumentLink. */
export function decodeDocumentLink(reader: BinaryReader): DocumentLink {
    const range = decodeSpan(reader);
    const target = decodeDocumentLinkTarget(reader);
    const tooltip = reader.readOption(() => reader.readString());

    return {
        range,
        target,
        ...(tooltip === undefined ? {} : { tooltip }),
    };
}

/** Return one JSON value for one DocumentLink. */
export function toJsonDocumentLink(value: DocumentLink): Json {
    return {
        range: toJsonSpan(value.range),
        target: toJsonDocumentLinkTarget(value.target),
        ...(value.tooltip === undefined ? {} : { tooltip: value.tooltip }),
    };
}

/** Return one DocumentLink from one JSON value. */
export function fromJsonDocumentLink(value: Json): DocumentLink {
    const object = jsonObject(value);

    return {
        range: fromJsonSpan(jsonField(object, "range")),
        target: fromJsonDocumentLinkTarget(jsonField(object, "target")),
        tooltip: jsonOptional(object, "tooltip", (value) => jsonString(value)),
    };
}

/** The target of a document link. */
export type DocumentLinkTarget =
    /** Link to a file (resolved import). */
    | {
          readonly kind: "file";
          /** The file path. */
          readonly path: string;
      }
    /** Link to a URL. */
    | {
          readonly kind: "url";
          /** The URL. */
          readonly url: string;
      }
    /** Link to a position in a file. */
    | {
          readonly kind: "position";
          /** The file path. */
          readonly path: string;
          /** Line number (0-indexed). */
          readonly line: number;
          /** Column number (0-indexed). */
          readonly column: number;
      }
;

export const DocumentLinkTarget = {
    /** Link to a file (resolved import). */
    file(path: string): DocumentLinkTarget {
        return { kind: "file", path };
    },

    /** Link to a URL. */
    url(url: string): DocumentLinkTarget {
        return { kind: "url", url };
    },

    /** Link to a position in a file. */
    position(path: string, line: number, column: number): DocumentLinkTarget {
        return { kind: "position", path, line, column };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: DocumentLinkTarget): void {
        encodeDocumentLinkTarget(writer, value);
    },

    /** Decode one DocumentLinkTarget. */
    decode(reader: BinaryReader): DocumentLinkTarget {
        return decodeDocumentLinkTarget(reader);
    },

    /** Return this value as JSON. */
    toJson(value: DocumentLinkTarget): Json {
        return toJsonDocumentLinkTarget(value);
    },

    /** Return one DocumentLinkTarget from one JSON value. */
    fromJson(value: Json): DocumentLinkTarget {
        return fromJsonDocumentLinkTarget(value);
    },
};

/** Encode one DocumentLinkTarget. */
export function encodeDocumentLinkTarget(writer: BinaryWriter, value: DocumentLinkTarget): void {
    switch (value.kind) {
        case "file":
            writer.writeUnsigned(0);
            writer.writeString(value.path);
            return;
        case "url":
            writer.writeUnsigned(1);
            writer.writeString(value.url);
            return;
        case "position":
            writer.writeUnsigned(2);
            writer.writeString(value.path);
            writer.writeUnsigned(value.line);
            writer.writeUnsigned(value.column);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one DocumentLinkTarget. */
export function decodeDocumentLinkTarget(reader: BinaryReader): DocumentLinkTarget {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const path = reader.readString();

            return {
                kind: "file",
                path,
            };
        }
        case 1: {
            const url = reader.readString();

            return {
                kind: "url",
                url,
            };
        }
        case 2: {
            const path = reader.readString();
            const line = reader.readNumber();
            const column = reader.readNumber();

            return {
                kind: "position",
                path,
                line,
                column,
            };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one DocumentLinkTarget. */
export function toJsonDocumentLinkTarget(value: DocumentLinkTarget): Json {
    switch (value.kind) {
        case "file":
            return {
                kind: "file",
                path: value.path,
            };
        case "url":
            return {
                kind: "url",
                url: value.url,
            };
        case "position":
            return {
                kind: "position",
                path: value.path,
                line: value.line,
                column: value.column,
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one DocumentLinkTarget from one JSON value. */
export function fromJsonDocumentLinkTarget(value: Json): DocumentLinkTarget {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "file":
            return {
                kind,
                path: jsonString(jsonField(object, "path")),
            };
        case "url":
            return {
                kind,
                url: jsonString(jsonField(object, "url")),
            };
        case "position":
            return {
                kind,
                path: jsonString(jsonField(object, "path")),
                line: jsonInteger(jsonField(object, "line")),
                column: jsonInteger(jsonField(object, "column")),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}
