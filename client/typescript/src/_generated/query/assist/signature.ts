/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, jsonArray, jsonField, jsonInteger, jsonObject, jsonOptional, jsonString } from "../../../protocol/serde.js";
import type { QueryPosition } from "../core/target.js";
import { decodeQueryPosition, encodeQueryPosition, fromJsonQueryPosition, toJsonQueryPosition } from "../core/target.js";

/** Request signature help at a cursor position. */
export type SignatureHelpRequest = {
    /** The queried position. */
    readonly position: QueryPosition;
};

export const SignatureHelpRequest = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: SignatureHelpRequest): void {
        encodeSignatureHelpRequest(writer, value);
    },

    /** Decode one SignatureHelpRequest. */
    decode(reader: BinaryReader): SignatureHelpRequest {
        return decodeSignatureHelpRequest(reader);
    },

    /** Return this value as JSON. */
    toJson(value: SignatureHelpRequest): Json {
        return toJsonSignatureHelpRequest(value);
    },

    /** Return one SignatureHelpRequest from one JSON value. */
    fromJson(value: Json): SignatureHelpRequest {
        return fromJsonSignatureHelpRequest(value);
    },
};

/** Encode one SignatureHelpRequest. */
export function encodeSignatureHelpRequest(writer: BinaryWriter, value: SignatureHelpRequest): void {
    encodeQueryPosition(writer, value.position);
}

/** Decode one SignatureHelpRequest. */
export function decodeSignatureHelpRequest(reader: BinaryReader): SignatureHelpRequest {
    const position = decodeQueryPosition(reader);

    return {
        position,
    };
}

/** Return one JSON value for one SignatureHelpRequest. */
export function toJsonSignatureHelpRequest(value: SignatureHelpRequest): Json {
    return {
        position: toJsonQueryPosition(value.position),
    };
}

/** Return one SignatureHelpRequest from one JSON value. */
export function fromJsonSignatureHelpRequest(value: Json): SignatureHelpRequest {
    const object = jsonObject(value);

    return {
        position: fromJsonQueryPosition(jsonField(object, "position")),
    };
}

/** Response payload for signature help queries. */
export type SignatureHelpResponse = {
    /** Signature help data, if available. */
    readonly help?: SignatureHelp;
};

export const SignatureHelpResponse = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: SignatureHelpResponse): void {
        encodeSignatureHelpResponse(writer, value);
    },

    /** Decode one SignatureHelpResponse. */
    decode(reader: BinaryReader): SignatureHelpResponse {
        return decodeSignatureHelpResponse(reader);
    },

    /** Return this value as JSON. */
    toJson(value: SignatureHelpResponse): Json {
        return toJsonSignatureHelpResponse(value);
    },

    /** Return one SignatureHelpResponse from one JSON value. */
    fromJson(value: Json): SignatureHelpResponse {
        return fromJsonSignatureHelpResponse(value);
    },
};

/** Encode one SignatureHelpResponse. */
export function encodeSignatureHelpResponse(writer: BinaryWriter, value: SignatureHelpResponse): void {
    writer.writeOption(value.help, (value0) => {
        encodeSignatureHelp(writer, value0);
    });
}

/** Decode one SignatureHelpResponse. */
export function decodeSignatureHelpResponse(reader: BinaryReader): SignatureHelpResponse {
    const help = reader.readOption(() => decodeSignatureHelp(reader));

    return {
        ...(help === undefined ? {} : { help }),
    };
}

/** Return one JSON value for one SignatureHelpResponse. */
export function toJsonSignatureHelpResponse(value: SignatureHelpResponse): Json {
    return {
        ...(value.help === undefined ? {} : { help: toJsonSignatureHelp(value.help) }),
    };
}

/** Return one SignatureHelpResponse from one JSON value. */
export function fromJsonSignatureHelpResponse(value: Json): SignatureHelpResponse {
    const object = jsonObject(value);

    return {
        help: jsonOptional(object, "help", (value) => fromJsonSignatureHelp(value)),
    };
}

/** Signature help result. */
export type SignatureHelp = {
    /** Available signatures. */
    readonly signatures: ReadonlyArray<SignatureItem>;
    /** The active signature (index into signatures). */
    readonly activeSignature: number;
    /** The active parameter (index into parameters). */
    readonly activeParameter: number;
};

export const SignatureHelp = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: SignatureHelp): void {
        encodeSignatureHelp(writer, value);
    },

    /** Decode one SignatureHelp. */
    decode(reader: BinaryReader): SignatureHelp {
        return decodeSignatureHelp(reader);
    },

    /** Return this value as JSON. */
    toJson(value: SignatureHelp): Json {
        return toJsonSignatureHelp(value);
    },

    /** Return one SignatureHelp from one JSON value. */
    fromJson(value: Json): SignatureHelp {
        return fromJsonSignatureHelp(value);
    },
};

/** Encode one SignatureHelp. */
export function encodeSignatureHelp(writer: BinaryWriter, value: SignatureHelp): void {
    writer.writeUnsigned(value.signatures.length);
    for (const item0 of value.signatures) {
        encodeSignatureItem(writer, item0);
    }
    writer.writeUnsigned(value.activeSignature);
    writer.writeUnsigned(value.activeParameter);
}

/** Decode one SignatureHelp. */
export function decodeSignatureHelp(reader: BinaryReader): SignatureHelp {
    const signatures = (() => { const length0 = reader.readNumber(); const items0: Array<SignatureItem> = []; for (let index = 0; index < length0; index += 1) { items0.push(decodeSignatureItem(reader)); } return items0; })();
    const activeSignature = reader.readNumber();
    const activeParameter = reader.readNumber();

    return {
        signatures,
        activeSignature,
        activeParameter,
    };
}

/** Return one JSON value for one SignatureHelp. */
export function toJsonSignatureHelp(value: SignatureHelp): Json {
    return {
        signatures: value.signatures.map((item0) => toJsonSignatureItem(item0)),
        activeSignature: value.activeSignature,
        activeParameter: value.activeParameter,
    };
}

/** Return one SignatureHelp from one JSON value. */
export function fromJsonSignatureHelp(value: Json): SignatureHelp {
    const object = jsonObject(value);

    return {
        signatures: jsonArray(jsonField(object, "signatures")).map((item0) => fromJsonSignatureItem(item0)),
        activeSignature: jsonInteger(jsonField(object, "activeSignature")),
        activeParameter: jsonInteger(jsonField(object, "activeParameter")),
    };
}

/** A single signature (for overloaded functions, there may be multiple). */
export type SignatureItem = {
    /** The full signature label. */
    readonly label: string;
    /** Documentation for the signature. */
    readonly documentation?: string;
    /** Parameters in this signature. */
    readonly parameters: ReadonlyArray<SignatureParameter>;
};

export const SignatureItem = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: SignatureItem): void {
        encodeSignatureItem(writer, value);
    },

    /** Decode one SignatureItem. */
    decode(reader: BinaryReader): SignatureItem {
        return decodeSignatureItem(reader);
    },

    /** Return this value as JSON. */
    toJson(value: SignatureItem): Json {
        return toJsonSignatureItem(value);
    },

    /** Return one SignatureItem from one JSON value. */
    fromJson(value: Json): SignatureItem {
        return fromJsonSignatureItem(value);
    },
};

/** Encode one SignatureItem. */
export function encodeSignatureItem(writer: BinaryWriter, value: SignatureItem): void {
    writer.writeString(value.label);
    writer.writeOption(value.documentation, (value1) => {
        writer.writeString(value1);
    });
    writer.writeUnsigned(value.parameters.length);
    for (const item2 of value.parameters) {
        encodeSignatureParameter(writer, item2);
    }
}

/** Decode one SignatureItem. */
export function decodeSignatureItem(reader: BinaryReader): SignatureItem {
    const label = reader.readString();
    const documentation = reader.readOption(() => reader.readString());
    const parameters = (() => { const length2 = reader.readNumber(); const items2: Array<SignatureParameter> = []; for (let index = 0; index < length2; index += 1) { items2.push(decodeSignatureParameter(reader)); } return items2; })();

    return {
        label,
        ...(documentation === undefined ? {} : { documentation }),
        parameters,
    };
}

/** Return one JSON value for one SignatureItem. */
export function toJsonSignatureItem(value: SignatureItem): Json {
    return {
        label: value.label,
        ...(value.documentation === undefined ? {} : { documentation: value.documentation }),
        parameters: value.parameters.map((item0) => toJsonSignatureParameter(item0)),
    };
}

/** Return one SignatureItem from one JSON value. */
export function fromJsonSignatureItem(value: Json): SignatureItem {
    const object = jsonObject(value);

    return {
        label: jsonString(jsonField(object, "label")),
        documentation: jsonOptional(object, "documentation", (value) => jsonString(value)),
        parameters: jsonArray(jsonField(object, "parameters")).map((item0) => fromJsonSignatureParameter(item0)),
    };
}

/** A parameter in a signature. */
export type SignatureParameter = {
    /** The parameter label (e.g., "name: string"). */
    readonly label: string;
    /** Documentation for this parameter. */
    readonly documentation?: string;
};

export const SignatureParameter = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: SignatureParameter): void {
        encodeSignatureParameter(writer, value);
    },

    /** Decode one SignatureParameter. */
    decode(reader: BinaryReader): SignatureParameter {
        return decodeSignatureParameter(reader);
    },

    /** Return this value as JSON. */
    toJson(value: SignatureParameter): Json {
        return toJsonSignatureParameter(value);
    },

    /** Return one SignatureParameter from one JSON value. */
    fromJson(value: Json): SignatureParameter {
        return fromJsonSignatureParameter(value);
    },
};

/** Encode one SignatureParameter. */
export function encodeSignatureParameter(writer: BinaryWriter, value: SignatureParameter): void {
    writer.writeString(value.label);
    writer.writeOption(value.documentation, (value1) => {
        writer.writeString(value1);
    });
}

/** Decode one SignatureParameter. */
export function decodeSignatureParameter(reader: BinaryReader): SignatureParameter {
    const label = reader.readString();
    const documentation = reader.readOption(() => reader.readString());

    return {
        label,
        ...(documentation === undefined ? {} : { documentation }),
    };
}

/** Return one JSON value for one SignatureParameter. */
export function toJsonSignatureParameter(value: SignatureParameter): Json {
    return {
        label: value.label,
        ...(value.documentation === undefined ? {} : { documentation: value.documentation }),
    };
}

/** Return one SignatureParameter from one JSON value. */
export function fromJsonSignatureParameter(value: Json): SignatureParameter {
    const object = jsonObject(value);

    return {
        label: jsonString(jsonField(object, "label")),
        documentation: jsonOptional(object, "documentation", (value) => jsonString(value)),
    };
}
