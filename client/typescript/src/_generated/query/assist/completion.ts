/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, jsonArray, jsonBool, jsonField, jsonInteger, jsonObject, jsonOptional, jsonString } from "../../../protocol/serde.js";
import type { QueryPosition } from "../protocol/target.js";
import type { Patch } from "../../source/edit/edit.js";
import type { Span } from "../../source/file/model/span.js";
import { decodeQueryPosition, encodeQueryPosition, fromJsonQueryPosition, toJsonQueryPosition } from "../protocol/target.js";
import { decodePatch, encodePatch, fromJsonPatch, toJsonPatch } from "../../source/edit/edit.js";
import { decodeSpan, encodeSpan, fromJsonSpan, toJsonSpan } from "../../source/file/model/span.js";

/** Request completion items at a cursor position. */
export type CompletionRequest = {
    /** The queried position. */
    readonly position: QueryPosition;
    /** The trigger that initiated completion. */
    readonly trigger: CompletionTrigger;
    /** Whether to include auto import completions. */
    readonly includeAutoImports: boolean;
};

export const CompletionRequest = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: CompletionRequest): void {
        encodeCompletionRequest(writer, value);
    },

    /** Decode one CompletionRequest. */
    decode(reader: BinaryReader): CompletionRequest {
        return decodeCompletionRequest(reader);
    },

    /** Return this value as JSON. */
    toJson(value: CompletionRequest): Json {
        return toJsonCompletionRequest(value);
    },

    /** Return one CompletionRequest from one JSON value. */
    fromJson(value: Json): CompletionRequest {
        return fromJsonCompletionRequest(value);
    },
};

/** Encode one CompletionRequest. */
export function encodeCompletionRequest(writer: BinaryWriter, value: CompletionRequest): void {
    encodeQueryPosition(writer, value.position);
    encodeCompletionTrigger(writer, value.trigger);
    writer.writeBool(value.includeAutoImports);
}

/** Decode one CompletionRequest. */
export function decodeCompletionRequest(reader: BinaryReader): CompletionRequest {
    const position = decodeQueryPosition(reader);
    const trigger = decodeCompletionTrigger(reader);
    const includeAutoImports = reader.readBool();

    return {
        position,
        trigger,
        includeAutoImports,
    };
}

/** Return one JSON value for one CompletionRequest. */
export function toJsonCompletionRequest(value: CompletionRequest): Json {
    return {
        position: toJsonQueryPosition(value.position),
        trigger: toJsonCompletionTrigger(value.trigger),
        includeAutoImports: value.includeAutoImports,
    };
}

/** Return one CompletionRequest from one JSON value. */
export function fromJsonCompletionRequest(value: Json): CompletionRequest {
    const object = jsonObject(value);

    return {
        position: fromJsonQueryPosition(jsonField(object, "position")),
        trigger: fromJsonCompletionTrigger(jsonField(object, "trigger")),
        includeAutoImports: jsonBool(jsonField(object, "includeAutoImports")),
    };
}

/** Trigger character that caused the completion. */
export type CompletionTrigger =
    /** Invoked manually or automatically. */
    | {
          readonly kind: "invoked";
      }
    /** Triggered by one character, for example `.`. */
    | {
          readonly kind: "character";
          readonly character: string;
      }
    /** Retriggered for incomplete results. */
    | {
          readonly kind: "incomplete";
      }
;

export const CompletionTrigger = {
    /** Invoked manually or automatically. */
    invoked(): CompletionTrigger {
        return { kind: "invoked" };
    },

    /** Triggered by one character, for example `.`. */
    character(character: string): CompletionTrigger {
        return { kind: "character", character };
    },

    /** Retriggered for incomplete results. */
    incomplete(): CompletionTrigger {
        return { kind: "incomplete" };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: CompletionTrigger): void {
        encodeCompletionTrigger(writer, value);
    },

    /** Decode one CompletionTrigger. */
    decode(reader: BinaryReader): CompletionTrigger {
        return decodeCompletionTrigger(reader);
    },

    /** Return this value as JSON. */
    toJson(value: CompletionTrigger): Json {
        return toJsonCompletionTrigger(value);
    },

    /** Return one CompletionTrigger from one JSON value. */
    fromJson(value: Json): CompletionTrigger {
        return fromJsonCompletionTrigger(value);
    },
};

/** Encode one CompletionTrigger. */
export function encodeCompletionTrigger(writer: BinaryWriter, value: CompletionTrigger): void {
    switch (value.kind) {
        case "invoked":
            writer.writeUnsigned(0);
            return;
        case "character":
            writer.writeUnsigned(1);
            writer.writeChar(value.character);
            return;
        case "incomplete":
            writer.writeUnsigned(2);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one CompletionTrigger. */
export function decodeCompletionTrigger(reader: BinaryReader): CompletionTrigger {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            return { kind: "invoked" };
        }
        case 1: {
            const character = reader.readChar();

            return { kind: "character", character };
        }
        case 2: {
            return { kind: "incomplete" };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one CompletionTrigger. */
export function toJsonCompletionTrigger(value: CompletionTrigger): Json {
    switch (value.kind) {
        case "invoked":
            return {
                kind: "invoked",
            };
        case "character":
            return {
                kind: "character",
                character: value.character,
            };
        case "incomplete":
            return {
                kind: "incomplete",
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one CompletionTrigger from one JSON value. */
export function fromJsonCompletionTrigger(value: Json): CompletionTrigger {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "invoked":
            return {
                kind,
            };
        case "character":
            return {
                kind,
                character: jsonString(jsonField(object, "character")),
            };
        case "incomplete":
            return {
                kind,
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** Response payload for completion queries. */
export type CompletionResponse = {
    /** Completion items. */
    readonly items: ReadonlyArray<CompletionItem>;
    /** Whether another request may produce more items. */
    readonly isIncomplete: boolean;
};

export const CompletionResponse = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: CompletionResponse): void {
        encodeCompletionResponse(writer, value);
    },

    /** Decode one CompletionResponse. */
    decode(reader: BinaryReader): CompletionResponse {
        return decodeCompletionResponse(reader);
    },

    /** Return this value as JSON. */
    toJson(value: CompletionResponse): Json {
        return toJsonCompletionResponse(value);
    },

    /** Return one CompletionResponse from one JSON value. */
    fromJson(value: Json): CompletionResponse {
        return fromJsonCompletionResponse(value);
    },
};

/** Encode one CompletionResponse. */
export function encodeCompletionResponse(writer: BinaryWriter, value: CompletionResponse): void {
    writer.writeUnsigned(value.items.length);
    for (const item0 of value.items) {
        encodeCompletionItem(writer, item0);
    }
    writer.writeBool(value.isIncomplete);
}

/** Decode one CompletionResponse. */
export function decodeCompletionResponse(reader: BinaryReader): CompletionResponse {
    const items = (() => { const length0 = reader.readNumber(); const items0: Array<CompletionItem> = []; for (let index = 0; index < length0; index += 1) { items0.push(decodeCompletionItem(reader)); } return items0; })();
    const isIncomplete = reader.readBool();

    return {
        items,
        isIncomplete,
    };
}

/** Return one JSON value for one CompletionResponse. */
export function toJsonCompletionResponse(value: CompletionResponse): Json {
    return {
        items: value.items.map((item0) => toJsonCompletionItem(item0)),
        isIncomplete: value.isIncomplete,
    };
}

/** Return one CompletionResponse from one JSON value. */
export function fromJsonCompletionResponse(value: Json): CompletionResponse {
    const object = jsonObject(value);

    return {
        items: jsonArray(jsonField(object, "items")).map((item0) => fromJsonCompletionItem(item0)),
        isIncomplete: jsonBool(jsonField(object, "isIncomplete")),
    };
}

/** A completion item. */
export type CompletionItem = {
    /** The label shown in the completion list. */
    readonly label: string;
    /** The kind of completion. */
    readonly kind: CompletionItemKind;
    /** Detail shown alongside the label. */
    readonly detail?: string;
    /** Documentation for the item. */
    readonly documentation?: string;
    /** The primary source edit. */
    readonly edit: CompletionEdit;
    /** Whether to preselect this item. */
    readonly preselect: boolean;
    /** Whether this item is deprecated. */
    readonly isDeprecated: boolean;
    /** Additional text edits to apply, for example auto imports. */
    readonly additionalEdits: ReadonlyArray<Patch>;
    /** Whether this completion inserts one auto import. */
    readonly isAutoImport: boolean;
    /** Matched character positions in the label. */
    readonly matchPositions: ReadonlyArray<number>;
};

export const CompletionItem = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: CompletionItem): void {
        encodeCompletionItem(writer, value);
    },

    /** Decode one CompletionItem. */
    decode(reader: BinaryReader): CompletionItem {
        return decodeCompletionItem(reader);
    },

    /** Return this value as JSON. */
    toJson(value: CompletionItem): Json {
        return toJsonCompletionItem(value);
    },

    /** Return one CompletionItem from one JSON value. */
    fromJson(value: Json): CompletionItem {
        return fromJsonCompletionItem(value);
    },
};

/** Encode one CompletionItem. */
export function encodeCompletionItem(writer: BinaryWriter, value: CompletionItem): void {
    writer.writeString(value.label);
    encodeCompletionItemKind(writer, value.kind);
    writer.writeOption(value.detail, (value2) => {
        writer.writeString(value2);
    });
    writer.writeOption(value.documentation, (value3) => {
        writer.writeString(value3);
    });
    encodeCompletionEdit(writer, value.edit);
    writer.writeBool(value.preselect);
    writer.writeBool(value.isDeprecated);
    writer.writeUnsigned(value.additionalEdits.length);
    for (const item7 of value.additionalEdits) {
        encodePatch(writer, item7);
    }
    writer.writeBool(value.isAutoImport);
    writer.writeUnsigned(value.matchPositions.length);
    for (const item9 of value.matchPositions) {
        writer.writeUnsigned(item9);
    }
}

/** Decode one CompletionItem. */
export function decodeCompletionItem(reader: BinaryReader): CompletionItem {
    const label = reader.readString();
    const kind = decodeCompletionItemKind(reader);
    const detail = reader.readOption(() => reader.readString());
    const documentation = reader.readOption(() => reader.readString());
    const edit = decodeCompletionEdit(reader);
    const preselect = reader.readBool();
    const isDeprecated = reader.readBool();
    const additionalEdits = (() => { const length7 = reader.readNumber(); const items7: Array<Patch> = []; for (let index = 0; index < length7; index += 1) { items7.push(decodePatch(reader)); } return items7; })();
    const isAutoImport = reader.readBool();
    const matchPositions = (() => { const length9 = reader.readNumber(); const items9: Array<number> = []; for (let index = 0; index < length9; index += 1) { items9.push(reader.readNumber()); } return items9; })();

    return {
        label,
        kind,
        ...(detail === undefined ? {} : { detail }),
        ...(documentation === undefined ? {} : { documentation }),
        edit,
        preselect,
        isDeprecated,
        additionalEdits,
        isAutoImport,
        matchPositions,
    };
}

/** Return one JSON value for one CompletionItem. */
export function toJsonCompletionItem(value: CompletionItem): Json {
    return {
        label: value.label,
        kind: toJsonCompletionItemKind(value.kind),
        ...(value.detail === undefined ? {} : { detail: value.detail }),
        ...(value.documentation === undefined ? {} : { documentation: value.documentation }),
        edit: toJsonCompletionEdit(value.edit),
        preselect: value.preselect,
        isDeprecated: value.isDeprecated,
        additionalEdits: value.additionalEdits.map((item0) => toJsonPatch(item0)),
        isAutoImport: value.isAutoImport,
        matchPositions: value.matchPositions.map((item0) => item0),
    };
}

/** Return one CompletionItem from one JSON value. */
export function fromJsonCompletionItem(value: Json): CompletionItem {
    const object = jsonObject(value);

    return {
        label: jsonString(jsonField(object, "label")),
        kind: fromJsonCompletionItemKind(jsonField(object, "kind")),
        detail: jsonOptional(object, "detail", (value) => jsonString(value)),
        documentation: jsonOptional(object, "documentation", (value) => jsonString(value)),
        edit: fromJsonCompletionEdit(jsonField(object, "edit")),
        preselect: jsonBool(jsonField(object, "preselect")),
        isDeprecated: jsonBool(jsonField(object, "isDeprecated")),
        additionalEdits: jsonArray(jsonField(object, "additionalEdits")).map((item0) => fromJsonPatch(item0)),
        isAutoImport: jsonBool(jsonField(object, "isAutoImport")),
        matchPositions: jsonArray(jsonField(object, "matchPositions")).map((item0) => jsonInteger(item0)),
    };
}

/** Kind of completion item. */
export type CompletionItemKind = "associatedConst" | "associatedType" | "method" | "function" | "constructor" | "field" | "variable" | "class" | "interface" | "newtypeInterface" | "newtype" | "typeAlias" | "extension" | "module" | "property" | "value" | "enum" | "keyword" | "file" | "reference" | "label" | "folder" | "enumMember" | "constant" | "struct" | "typeParameter" | "valueParameter" | "builtinType";

export const CompletionItemKind = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: CompletionItemKind): void {
        encodeCompletionItemKind(writer, value);
    },

    /** Decode one CompletionItemKind. */
    decode(reader: BinaryReader): CompletionItemKind {
        return decodeCompletionItemKind(reader);
    },

    /** Return this value as JSON. */
    toJson(value: CompletionItemKind): Json {
        return toJsonCompletionItemKind(value);
    },

    /** Return one CompletionItemKind from one JSON value. */
    fromJson(value: Json): CompletionItemKind {
        return fromJsonCompletionItemKind(value);
    },
};

/** Encode one CompletionItemKind. */
export function encodeCompletionItemKind(writer: BinaryWriter, value: CompletionItemKind): void {
    switch (value) {
        case "associatedConst":
            writer.writeUnsigned(0);
            return;
        case "associatedType":
            writer.writeUnsigned(1);
            return;
        case "method":
            writer.writeUnsigned(2);
            return;
        case "function":
            writer.writeUnsigned(3);
            return;
        case "constructor":
            writer.writeUnsigned(4);
            return;
        case "field":
            writer.writeUnsigned(5);
            return;
        case "variable":
            writer.writeUnsigned(6);
            return;
        case "class":
            writer.writeUnsigned(7);
            return;
        case "interface":
            writer.writeUnsigned(8);
            return;
        case "newtypeInterface":
            writer.writeUnsigned(9);
            return;
        case "newtype":
            writer.writeUnsigned(10);
            return;
        case "typeAlias":
            writer.writeUnsigned(11);
            return;
        case "extension":
            writer.writeUnsigned(12);
            return;
        case "module":
            writer.writeUnsigned(13);
            return;
        case "property":
            writer.writeUnsigned(14);
            return;
        case "value":
            writer.writeUnsigned(15);
            return;
        case "enum":
            writer.writeUnsigned(16);
            return;
        case "keyword":
            writer.writeUnsigned(17);
            return;
        case "file":
            writer.writeUnsigned(18);
            return;
        case "reference":
            writer.writeUnsigned(19);
            return;
        case "label":
            writer.writeUnsigned(20);
            return;
        case "folder":
            writer.writeUnsigned(21);
            return;
        case "enumMember":
            writer.writeUnsigned(22);
            return;
        case "constant":
            writer.writeUnsigned(23);
            return;
        case "struct":
            writer.writeUnsigned(24);
            return;
        case "typeParameter":
            writer.writeUnsigned(25);
            return;
        case "valueParameter":
            writer.writeUnsigned(26);
            return;
        case "builtinType":
            writer.writeUnsigned(27);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one CompletionItemKind. */
export function decodeCompletionItemKind(reader: BinaryReader): CompletionItemKind {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "associatedConst";
        case 1:
            return "associatedType";
        case 2:
            return "method";
        case 3:
            return "function";
        case 4:
            return "constructor";
        case 5:
            return "field";
        case 6:
            return "variable";
        case 7:
            return "class";
        case 8:
            return "interface";
        case 9:
            return "newtypeInterface";
        case 10:
            return "newtype";
        case 11:
            return "typeAlias";
        case 12:
            return "extension";
        case 13:
            return "module";
        case 14:
            return "property";
        case 15:
            return "value";
        case 16:
            return "enum";
        case 17:
            return "keyword";
        case 18:
            return "file";
        case 19:
            return "reference";
        case 20:
            return "label";
        case 21:
            return "folder";
        case 22:
            return "enumMember";
        case 23:
            return "constant";
        case 24:
            return "struct";
        case 25:
            return "typeParameter";
        case 26:
            return "valueParameter";
        case 27:
            return "builtinType";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one CompletionItemKind. */
export function toJsonCompletionItemKind(value: CompletionItemKind): Json {
    return value;
}

/** Return one CompletionItemKind from one JSON value. */
export function fromJsonCompletionItemKind(value: Json): CompletionItemKind {
    const variant = jsonString(value);

    switch (variant) {
        case "associatedConst":
            return "associatedConst";
        case "associatedType":
            return "associatedType";
        case "method":
            return "method";
        case "function":
            return "function";
        case "constructor":
            return "constructor";
        case "field":
            return "field";
        case "variable":
            return "variable";
        case "class":
            return "class";
        case "interface":
            return "interface";
        case "newtypeInterface":
            return "newtypeInterface";
        case "newtype":
            return "newtype";
        case "typeAlias":
            return "typeAlias";
        case "extension":
            return "extension";
        case "module":
            return "module";
        case "property":
            return "property";
        case "value":
            return "value";
        case "enum":
            return "enum";
        case "keyword":
            return "keyword";
        case "file":
            return "file";
        case "reference":
            return "reference";
        case "label":
            return "label";
        case "folder":
            return "folder";
        case "enumMember":
            return "enumMember";
        case "constant":
            return "constant";
        case "struct":
            return "struct";
        case "typeParameter":
            return "typeParameter";
        case "valueParameter":
            return "valueParameter";
        case "builtinType":
            return "builtinType";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** The primary source edit applied by one completion item. */
export type CompletionEdit = {
    /** The source range replaced by the completion. */
    readonly span: Span;
    /** The inserted text or snippet. */
    readonly newText: string;
    /** Whether the inserted text is a snippet. */
    readonly isSnippet: boolean;
};

export const CompletionEdit = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: CompletionEdit): void {
        encodeCompletionEdit(writer, value);
    },

    /** Decode one CompletionEdit. */
    decode(reader: BinaryReader): CompletionEdit {
        return decodeCompletionEdit(reader);
    },

    /** Return this value as JSON. */
    toJson(value: CompletionEdit): Json {
        return toJsonCompletionEdit(value);
    },

    /** Return one CompletionEdit from one JSON value. */
    fromJson(value: Json): CompletionEdit {
        return fromJsonCompletionEdit(value);
    },
};

/** Encode one CompletionEdit. */
export function encodeCompletionEdit(writer: BinaryWriter, value: CompletionEdit): void {
    encodeSpan(writer, value.span);
    writer.writeString(value.newText);
    writer.writeBool(value.isSnippet);
}

/** Decode one CompletionEdit. */
export function decodeCompletionEdit(reader: BinaryReader): CompletionEdit {
    const span = decodeSpan(reader);
    const newText = reader.readString();
    const isSnippet = reader.readBool();

    return {
        span,
        newText,
        isSnippet,
    };
}

/** Return one JSON value for one CompletionEdit. */
export function toJsonCompletionEdit(value: CompletionEdit): Json {
    return {
        span: toJsonSpan(value.span),
        newText: value.newText,
        isSnippet: value.isSnippet,
    };
}

/** Return one CompletionEdit from one JSON value. */
export function fromJsonCompletionEdit(value: Json): CompletionEdit {
    const object = jsonObject(value);

    return {
        span: fromJsonSpan(jsonField(object, "span")),
        newText: jsonString(jsonField(object, "newText")),
        isSnippet: jsonBool(jsonField(object, "isSnippet")),
    };
}
