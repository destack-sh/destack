/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, jsonArray, jsonBool, jsonField, jsonInteger, jsonObject, jsonOptional, jsonString } from "../../../protocol/serde.js";
import type { GlobalSymbolId } from "../../dir/symbol/symbol.js";
import type { Module } from "../protocol/target.js";
import type { QueryPosition } from "../protocol/target.js";
import type { Patch } from "../../source/edit/edit.js";
import type { FileId } from "../../source/file/model/file.js";
import type { Span } from "../../source/file/model/span.js";
import { decodeGlobalSymbolId, encodeGlobalSymbolId, fromJsonGlobalSymbolId, toJsonGlobalSymbolId } from "../../dir/symbol/symbol.js";
import { decodeModule, encodeModule, fromJsonModule, toJsonModule } from "../protocol/target.js";
import { decodeQueryPosition, encodeQueryPosition, fromJsonQueryPosition, toJsonQueryPosition } from "../protocol/target.js";
import { decodePatch, encodePatch, fromJsonPatch, toJsonPatch } from "../../source/edit/edit.js";
import { decodeFileId, encodeFileId, fromJsonFileId, toJsonFileId } from "../../source/file/model/file.js";
import { decodeSpan, encodeSpan, fromJsonSpan, toJsonSpan } from "../../source/file/model/span.js";

/** When expanded completion fields are computed. */
export type CompletionDetailsMode = "eager" | "deferred";

export const CompletionDetailsMode = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: CompletionDetailsMode): void {
        encodeCompletionDetailsMode(writer, value);
    },

    /** Decode one CompletionDetailsMode. */
    decode(reader: BinaryReader): CompletionDetailsMode {
        return decodeCompletionDetailsMode(reader);
    },

    /** Return this value as JSON. */
    toJson(value: CompletionDetailsMode): Json {
        return toJsonCompletionDetailsMode(value);
    },

    /** Return one CompletionDetailsMode from one JSON value. */
    fromJson(value: Json): CompletionDetailsMode {
        return fromJsonCompletionDetailsMode(value);
    },
};

/** Encode one CompletionDetailsMode. */
export function encodeCompletionDetailsMode(writer: BinaryWriter, value: CompletionDetailsMode): void {
    switch (value) {
        case "eager":
            writer.writeUnsigned(0);
            return;
        case "deferred":
            writer.writeUnsigned(1);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one CompletionDetailsMode. */
export function decodeCompletionDetailsMode(reader: BinaryReader): CompletionDetailsMode {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "eager";
        case 1:
            return "deferred";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one CompletionDetailsMode. */
export function toJsonCompletionDetailsMode(value: CompletionDetailsMode): Json {
    return value;
}

/** Return one CompletionDetailsMode from one JSON value. */
export function fromJsonCompletionDetailsMode(value: Json): CompletionDetailsMode {
    const variant = jsonString(value);

    switch (variant) {
        case "eager":
            return "eager";
        case "deferred":
            return "deferred";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** Request for the expanded fields of one completion entry. */
export type CompletionDetailsRequest = {
    /** The module that produced the entry. */
    readonly module: Module;
    /** The source file that produced the entry. */
    readonly fileId: FileId;
    /** The selected declaration when one exists. */
    readonly symbol?: GlobalSymbolId;
    /** Additional source edits applied with the selected entry. */
    readonly additionalEdits: ReadonlyArray<Patch>;
};

export const CompletionDetailsRequest = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: CompletionDetailsRequest): void {
        encodeCompletionDetailsRequest(writer, value);
    },

    /** Decode one CompletionDetailsRequest. */
    decode(reader: BinaryReader): CompletionDetailsRequest {
        return decodeCompletionDetailsRequest(reader);
    },

    /** Return this value as JSON. */
    toJson(value: CompletionDetailsRequest): Json {
        return toJsonCompletionDetailsRequest(value);
    },

    /** Return one CompletionDetailsRequest from one JSON value. */
    fromJson(value: Json): CompletionDetailsRequest {
        return fromJsonCompletionDetailsRequest(value);
    },
};

/** Encode one CompletionDetailsRequest. */
export function encodeCompletionDetailsRequest(writer: BinaryWriter, value: CompletionDetailsRequest): void {
    encodeModule(writer, value.module);
    encodeFileId(writer, value.fileId);
    writer.writeOption(value.symbol, (value2) => {
        encodeGlobalSymbolId(writer, value2);
    });
    writer.writeUnsigned(value.additionalEdits.length);
    for (const item3 of value.additionalEdits) {
        encodePatch(writer, item3);
    }
}

/** Decode one CompletionDetailsRequest. */
export function decodeCompletionDetailsRequest(reader: BinaryReader): CompletionDetailsRequest {
    const module_ = decodeModule(reader);
    const fileId = decodeFileId(reader);
    const symbol_ = reader.readOption(() => decodeGlobalSymbolId(reader));
    const additionalEdits = (() => { const length3 = reader.readNumber(); const items3: Array<Patch> = []; for (let index = 0; index < length3; index += 1) { items3.push(decodePatch(reader)); } return items3; })();

    return {
        module: module_,
        fileId,
        ...(symbol_ === undefined ? {} : { symbol: symbol_ }),
        additionalEdits,
    };
}

/** Return one JSON value for one CompletionDetailsRequest. */
export function toJsonCompletionDetailsRequest(value: CompletionDetailsRequest): Json {
    return {
        module: toJsonModule(value.module),
        fileId: toJsonFileId(value.fileId),
        ...(value.symbol === undefined ? {} : { symbol: toJsonGlobalSymbolId(value.symbol) }),
        additionalEdits: value.additionalEdits.map((item0) => toJsonPatch(item0)),
    };
}

/** Return one CompletionDetailsRequest from one JSON value. */
export function fromJsonCompletionDetailsRequest(value: Json): CompletionDetailsRequest {
    const object = jsonObject(value);

    return {
        module: fromJsonModule(jsonField(object, "module")),
        fileId: fromJsonFileId(jsonField(object, "fileId")),
        symbol: jsonOptional(object, "symbol", (value) => fromJsonGlobalSymbolId(value)),
        additionalEdits: jsonArray(jsonField(object, "additionalEdits")).map((item0) => fromJsonPatch(item0)),
    };
}

/** Expanded fields for one selected completion entry. */
export type CompletionDetailsResponse = {
    /** The declaration shown for the selected entry. */
    readonly declaration?: string;
    /** Documentation for the selected entry. */
    readonly documentation?: string;
    /** Additional source edits applied with the selected entry. */
    readonly additionalEdits: ReadonlyArray<Patch>;
};

export const CompletionDetailsResponse = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: CompletionDetailsResponse): void {
        encodeCompletionDetailsResponse(writer, value);
    },

    /** Decode one CompletionDetailsResponse. */
    decode(reader: BinaryReader): CompletionDetailsResponse {
        return decodeCompletionDetailsResponse(reader);
    },

    /** Return this value as JSON. */
    toJson(value: CompletionDetailsResponse): Json {
        return toJsonCompletionDetailsResponse(value);
    },

    /** Return one CompletionDetailsResponse from one JSON value. */
    fromJson(value: Json): CompletionDetailsResponse {
        return fromJsonCompletionDetailsResponse(value);
    },
};

/** Encode one CompletionDetailsResponse. */
export function encodeCompletionDetailsResponse(writer: BinaryWriter, value: CompletionDetailsResponse): void {
    writer.writeOption(value.declaration, (value0) => {
        writer.writeString(value0);
    });
    writer.writeOption(value.documentation, (value1) => {
        writer.writeString(value1);
    });
    writer.writeUnsigned(value.additionalEdits.length);
    for (const item2 of value.additionalEdits) {
        encodePatch(writer, item2);
    }
}

/** Decode one CompletionDetailsResponse. */
export function decodeCompletionDetailsResponse(reader: BinaryReader): CompletionDetailsResponse {
    const declaration = reader.readOption(() => reader.readString());
    const documentation = reader.readOption(() => reader.readString());
    const additionalEdits = (() => { const length2 = reader.readNumber(); const items2: Array<Patch> = []; for (let index = 0; index < length2; index += 1) { items2.push(decodePatch(reader)); } return items2; })();

    return {
        ...(declaration === undefined ? {} : { declaration }),
        ...(documentation === undefined ? {} : { documentation }),
        additionalEdits,
    };
}

/** Return one JSON value for one CompletionDetailsResponse. */
export function toJsonCompletionDetailsResponse(value: CompletionDetailsResponse): Json {
    return {
        ...(value.declaration === undefined ? {} : { declaration: value.declaration }),
        ...(value.documentation === undefined ? {} : { documentation: value.documentation }),
        additionalEdits: value.additionalEdits.map((item0) => toJsonPatch(item0)),
    };
}

/** Return one CompletionDetailsResponse from one JSON value. */
export function fromJsonCompletionDetailsResponse(value: Json): CompletionDetailsResponse {
    const object = jsonObject(value);

    return {
        declaration: jsonOptional(object, "declaration", (value) => jsonString(value)),
        documentation: jsonOptional(object, "documentation", (value) => jsonString(value)),
        additionalEdits: jsonArray(jsonField(object, "additionalEdits")).map((item0) => fromJsonPatch(item0)),
    };
}

/** The primary source edit applied by one completion item. */
export type CompletionEdit = {
    /** The source range replaced by the completion. */
    readonly span: Span;
    /** The inserted text or snippet. */
    readonly newText: string;
    /** Whether the inserted text is a snippet. */
    readonly isSnippet: boolean;
};

export const CompletionEdit = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: CompletionEdit): void {
        encodeCompletionEdit(writer, value);
    },

    /** Decode one CompletionEdit. */
    decode(reader: BinaryReader): CompletionEdit {
        return decodeCompletionEdit(reader);
    },

    /** Return this value as JSON. */
    toJson(value: CompletionEdit): Json {
        return toJsonCompletionEdit(value);
    },

    /** Return one CompletionEdit from one JSON value. */
    fromJson(value: Json): CompletionEdit {
        return fromJsonCompletionEdit(value);
    },
};

/** Encode one CompletionEdit. */
export function encodeCompletionEdit(writer: BinaryWriter, value: CompletionEdit): void {
    encodeSpan(writer, value.span);
    writer.writeString(value.newText);
    writer.writeBool(value.isSnippet);
}

/** Decode one CompletionEdit. */
export function decodeCompletionEdit(reader: BinaryReader): CompletionEdit {
    const span = decodeSpan(reader);
    const newText = reader.readString();
    const isSnippet = reader.readBool();

    return {
        span,
        newText,
        isSnippet,
    };
}

/** Return one JSON value for one CompletionEdit. */
export function toJsonCompletionEdit(value: CompletionEdit): Json {
    return {
        span: toJsonSpan(value.span),
        newText: value.newText,
        isSnippet: value.isSnippet,
    };
}

/** Return one CompletionEdit from one JSON value. */
export function fromJsonCompletionEdit(value: Json): CompletionEdit {
    const object = jsonObject(value);

    return {
        span: fromJsonSpan(jsonField(object, "span")),
        newText: jsonString(jsonField(object, "newText")),
        isSnippet: jsonBool(jsonField(object, "isSnippet")),
    };
}

/** One entry in a completion list. */
export type CompletionEntry = {
    /** The label shown in the completion list. */
    readonly label: string;
    /** The kind of completion. */
    readonly kind: CompletionItemKind;
    /** The exact text shown directly after the label. */
    readonly labelSuffix?: string;
    /** The declaration owner or import source. */
    readonly description?: string;
    /** The primary source edit. */
    readonly edit: CompletionEdit;
    /** Whether to preselect this item. */
    readonly preselect: boolean;
    /** Whether this item is deprecated. */
    readonly isDeprecated: boolean;
    /** Whether this completion inserts one auto import. */
    readonly isAutoImport: boolean;
    /** Matched character positions in the label. */
    readonly matchPositions: ReadonlyArray<number>;
    /** Expanded fields or the request that computes them. */
    readonly details?: CompletionEntryDetails;
};

export const CompletionEntry = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: CompletionEntry): void {
        encodeCompletionEntry(writer, value);
    },

    /** Decode one CompletionEntry. */
    decode(reader: BinaryReader): CompletionEntry {
        return decodeCompletionEntry(reader);
    },

    /** Return this value as JSON. */
    toJson(value: CompletionEntry): Json {
        return toJsonCompletionEntry(value);
    },

    /** Return one CompletionEntry from one JSON value. */
    fromJson(value: Json): CompletionEntry {
        return fromJsonCompletionEntry(value);
    },
};

/** Encode one CompletionEntry. */
export function encodeCompletionEntry(writer: BinaryWriter, value: CompletionEntry): void {
    writer.writeString(value.label);
    encodeCompletionItemKind(writer, value.kind);
    writer.writeOption(value.labelSuffix, (value2) => {
        writer.writeString(value2);
    });
    writer.writeOption(value.description, (value3) => {
        writer.writeString(value3);
    });
    encodeCompletionEdit(writer, value.edit);
    writer.writeBool(value.preselect);
    writer.writeBool(value.isDeprecated);
    writer.writeBool(value.isAutoImport);
    writer.writeUnsigned(value.matchPositions.length);
    for (const item8 of value.matchPositions) {
        writer.writeUnsigned(item8);
    }
    writer.writeOption(value.details, (value9) => {
        encodeCompletionEntryDetails(writer, value9);
    });
}

/** Decode one CompletionEntry. */
export function decodeCompletionEntry(reader: BinaryReader): CompletionEntry {
    const label = reader.readString();
    const kind = decodeCompletionItemKind(reader);
    const labelSuffix = reader.readOption(() => reader.readString());
    const description = reader.readOption(() => reader.readString());
    const edit = decodeCompletionEdit(reader);
    const preselect = reader.readBool();
    const isDeprecated = reader.readBool();
    const isAutoImport = reader.readBool();
    const matchPositions = (() => { const length8 = reader.readNumber(); const items8: Array<number> = []; for (let index = 0; index < length8; index += 1) { items8.push(reader.readNumber()); } return items8; })();
    const details = reader.readOption(() => decodeCompletionEntryDetails(reader));

    return {
        label,
        kind,
        ...(labelSuffix === undefined ? {} : { labelSuffix }),
        ...(description === undefined ? {} : { description }),
        edit,
        preselect,
        isDeprecated,
        isAutoImport,
        matchPositions,
        ...(details === undefined ? {} : { details }),
    };
}

/** Return one JSON value for one CompletionEntry. */
export function toJsonCompletionEntry(value: CompletionEntry): Json {
    return {
        label: value.label,
        kind: toJsonCompletionItemKind(value.kind),
        ...(value.labelSuffix === undefined ? {} : { labelSuffix: value.labelSuffix }),
        ...(value.description === undefined ? {} : { description: value.description }),
        edit: toJsonCompletionEdit(value.edit),
        preselect: value.preselect,
        isDeprecated: value.isDeprecated,
        isAutoImport: value.isAutoImport,
        matchPositions: value.matchPositions.map((item0) => item0),
        ...(value.details === undefined ? {} : { details: toJsonCompletionEntryDetails(value.details) }),
    };
}

/** Return one CompletionEntry from one JSON value. */
export function fromJsonCompletionEntry(value: Json): CompletionEntry {
    const object = jsonObject(value);

    return {
        label: jsonString(jsonField(object, "label")),
        kind: fromJsonCompletionItemKind(jsonField(object, "kind")),
        labelSuffix: jsonOptional(object, "labelSuffix", (value) => jsonString(value)),
        description: jsonOptional(object, "description", (value) => jsonString(value)),
        edit: fromJsonCompletionEdit(jsonField(object, "edit")),
        preselect: jsonBool(jsonField(object, "preselect")),
        isDeprecated: jsonBool(jsonField(object, "isDeprecated")),
        isAutoImport: jsonBool(jsonField(object, "isAutoImport")),
        matchPositions: jsonArray(jsonField(object, "matchPositions")).map((item0) => jsonInteger(item0)),
        details: jsonOptional(object, "details", (value) => fromJsonCompletionEntryDetails(value)),
    };
}

/** Expanded fields retained by one completion entry. */
export type CompletionEntryDetails =
    /** A request retained until the entry is selected. */
    | {
          readonly kind: "deferred";
          readonly deferred: CompletionDetailsRequest;
      }
    /** Fields computed with the completion list. */
    | {
          readonly kind: "eager";
          readonly eager: CompletionDetailsResponse;
      }
;

export const CompletionEntryDetails = {
    /** A request retained until the entry is selected. */
    deferred(deferred: CompletionDetailsRequest): CompletionEntryDetails {
        return { kind: "deferred", deferred };
    },

    /** Fields computed with the completion list. */
    eager(eager: CompletionDetailsResponse): CompletionEntryDetails {
        return { kind: "eager", eager };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: CompletionEntryDetails): void {
        encodeCompletionEntryDetails(writer, value);
    },

    /** Decode one CompletionEntryDetails. */
    decode(reader: BinaryReader): CompletionEntryDetails {
        return decodeCompletionEntryDetails(reader);
    },

    /** Return this value as JSON. */
    toJson(value: CompletionEntryDetails): Json {
        return toJsonCompletionEntryDetails(value);
    },

    /** Return one CompletionEntryDetails from one JSON value. */
    fromJson(value: Json): CompletionEntryDetails {
        return fromJsonCompletionEntryDetails(value);
    },
};

/** Encode one CompletionEntryDetails. */
export function encodeCompletionEntryDetails(writer: BinaryWriter, value: CompletionEntryDetails): void {
    switch (value.kind) {
        case "deferred":
            writer.writeUnsigned(0);
            encodeCompletionDetailsRequest(writer, value.deferred);
            return;
        case "eager":
            writer.writeUnsigned(1);
            encodeCompletionDetailsResponse(writer, value.eager);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one CompletionEntryDetails. */
export function decodeCompletionEntryDetails(reader: BinaryReader): CompletionEntryDetails {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const deferred = decodeCompletionDetailsRequest(reader);

            return { kind: "deferred", deferred };
        }
        case 1: {
            const eager = decodeCompletionDetailsResponse(reader);

            return { kind: "eager", eager };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one CompletionEntryDetails. */
export function toJsonCompletionEntryDetails(value: CompletionEntryDetails): Json {
    switch (value.kind) {
        case "deferred":
            return {
                kind: "deferred",
                deferred: toJsonCompletionDetailsRequest(value.deferred),
            };
        case "eager":
            return {
                kind: "eager",
                eager: toJsonCompletionDetailsResponse(value.eager),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one CompletionEntryDetails from one JSON value. */
export function fromJsonCompletionEntryDetails(value: Json): CompletionEntryDetails {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "deferred":
            return {
                kind,
                deferred: fromJsonCompletionDetailsRequest(jsonField(object, "deferred")),
            };
        case "eager":
            return {
                kind,
                eager: fromJsonCompletionDetailsResponse(jsonField(object, "eager")),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** Kind of completion item. */
export type CompletionItemKind = "associatedConst" | "associatedType" | "method" | "function" | "constructor" | "field" | "variable" | "class" | "interface" | "newtypeInterface" | "newtype" | "typeAlias" | "extension" | "module" | "property" | "value" | "enum" | "keyword" | "file" | "reference" | "label" | "folder" | "enumMember" | "constant" | "struct" | "typeParameter" | "valueParameter" | "builtinType";

export const CompletionItemKind = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: CompletionItemKind): void {
        encodeCompletionItemKind(writer, value);
    },

    /** Decode one CompletionItemKind. */
    decode(reader: BinaryReader): CompletionItemKind {
        return decodeCompletionItemKind(reader);
    },

    /** Return this value as JSON. */
    toJson(value: CompletionItemKind): Json {
        return toJsonCompletionItemKind(value);
    },

    /** Return one CompletionItemKind from one JSON value. */
    fromJson(value: Json): CompletionItemKind {
        return fromJsonCompletionItemKind(value);
    },
};

/** Encode one CompletionItemKind. */
export function encodeCompletionItemKind(writer: BinaryWriter, value: CompletionItemKind): void {
    switch (value) {
        case "associatedConst":
            writer.writeUnsigned(0);
            return;
        case "associatedType":
            writer.writeUnsigned(1);
            return;
        case "method":
            writer.writeUnsigned(2);
            return;
        case "function":
            writer.writeUnsigned(3);
            return;
        case "constructor":
            writer.writeUnsigned(4);
            return;
        case "field":
            writer.writeUnsigned(5);
            return;
        case "variable":
            writer.writeUnsigned(6);
            return;
        case "class":
            writer.writeUnsigned(7);
            return;
        case "interface":
            writer.writeUnsigned(8);
            return;
        case "newtypeInterface":
            writer.writeUnsigned(9);
            return;
        case "newtype":
            writer.writeUnsigned(10);
            return;
        case "typeAlias":
            writer.writeUnsigned(11);
            return;
        case "extension":
            writer.writeUnsigned(12);
            return;
        case "module":
            writer.writeUnsigned(13);
            return;
        case "property":
            writer.writeUnsigned(14);
            return;
        case "value":
            writer.writeUnsigned(15);
            return;
        case "enum":
            writer.writeUnsigned(16);
            return;
        case "keyword":
            writer.writeUnsigned(17);
            return;
        case "file":
            writer.writeUnsigned(18);
            return;
        case "reference":
            writer.writeUnsigned(19);
            return;
        case "label":
            writer.writeUnsigned(20);
            return;
        case "folder":
            writer.writeUnsigned(21);
            return;
        case "enumMember":
            writer.writeUnsigned(22);
            return;
        case "constant":
            writer.writeUnsigned(23);
            return;
        case "struct":
            writer.writeUnsigned(24);
            return;
        case "typeParameter":
            writer.writeUnsigned(25);
            return;
        case "valueParameter":
            writer.writeUnsigned(26);
            return;
        case "builtinType":
            writer.writeUnsigned(27);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one CompletionItemKind. */
export function decodeCompletionItemKind(reader: BinaryReader): CompletionItemKind {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "associatedConst";
        case 1:
            return "associatedType";
        case 2:
            return "method";
        case 3:
            return "function";
        case 4:
            return "constructor";
        case 5:
            return "field";
        case 6:
            return "variable";
        case 7:
            return "class";
        case 8:
            return "interface";
        case 9:
            return "newtypeInterface";
        case 10:
            return "newtype";
        case 11:
            return "typeAlias";
        case 12:
            return "extension";
        case 13:
            return "module";
        case 14:
            return "property";
        case 15:
            return "value";
        case 16:
            return "enum";
        case 17:
            return "keyword";
        case 18:
            return "file";
        case 19:
            return "reference";
        case 20:
            return "label";
        case 21:
            return "folder";
        case 22:
            return "enumMember";
        case 23:
            return "constant";
        case 24:
            return "struct";
        case 25:
            return "typeParameter";
        case 26:
            return "valueParameter";
        case 27:
            return "builtinType";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one CompletionItemKind. */
export function toJsonCompletionItemKind(value: CompletionItemKind): Json {
    return value;
}

/** Return one CompletionItemKind from one JSON value. */
export function fromJsonCompletionItemKind(value: Json): CompletionItemKind {
    const variant = jsonString(value);

    switch (variant) {
        case "associatedConst":
            return "associatedConst";
        case "associatedType":
            return "associatedType";
        case "method":
            return "method";
        case "function":
            return "function";
        case "constructor":
            return "constructor";
        case "field":
            return "field";
        case "variable":
            return "variable";
        case "class":
            return "class";
        case "interface":
            return "interface";
        case "newtypeInterface":
            return "newtypeInterface";
        case "newtype":
            return "newtype";
        case "typeAlias":
            return "typeAlias";
        case "extension":
            return "extension";
        case "module":
            return "module";
        case "property":
            return "property";
        case "value":
            return "value";
        case "enum":
            return "enum";
        case "keyword":
            return "keyword";
        case "file":
            return "file";
        case "reference":
            return "reference";
        case "label":
            return "label";
        case "folder":
            return "folder";
        case "enumMember":
            return "enumMember";
        case "constant":
            return "constant";
        case "struct":
            return "struct";
        case "typeParameter":
            return "typeParameter";
        case "valueParameter":
            return "valueParameter";
        case "builtinType":
            return "builtinType";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** A completion request. */
export type CompletionRequest = {
    /** The queried position. */
    readonly position: QueryPosition;
    /** The trigger that initiated completion. */
    readonly trigger: CompletionTrigger;
    /** Whether to include auto import completions. */
    readonly includeAutoImports: boolean;
    /** When expanded fields are computed. */
    readonly details: CompletionDetailsMode;
};

export const CompletionRequest = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: CompletionRequest): void {
        encodeCompletionRequest(writer, value);
    },

    /** Decode one CompletionRequest. */
    decode(reader: BinaryReader): CompletionRequest {
        return decodeCompletionRequest(reader);
    },

    /** Return this value as JSON. */
    toJson(value: CompletionRequest): Json {
        return toJsonCompletionRequest(value);
    },

    /** Return one CompletionRequest from one JSON value. */
    fromJson(value: Json): CompletionRequest {
        return fromJsonCompletionRequest(value);
    },
};

/** Encode one CompletionRequest. */
export function encodeCompletionRequest(writer: BinaryWriter, value: CompletionRequest): void {
    encodeQueryPosition(writer, value.position);
    encodeCompletionTrigger(writer, value.trigger);
    writer.writeBool(value.includeAutoImports);
    encodeCompletionDetailsMode(writer, value.details);
}

/** Decode one CompletionRequest. */
export function decodeCompletionRequest(reader: BinaryReader): CompletionRequest {
    const position = decodeQueryPosition(reader);
    const trigger = decodeCompletionTrigger(reader);
    const includeAutoImports = reader.readBool();
    const details = decodeCompletionDetailsMode(reader);

    return {
        position,
        trigger,
        includeAutoImports,
        details,
    };
}

/** Return one JSON value for one CompletionRequest. */
export function toJsonCompletionRequest(value: CompletionRequest): Json {
    return {
        position: toJsonQueryPosition(value.position),
        trigger: toJsonCompletionTrigger(value.trigger),
        includeAutoImports: value.includeAutoImports,
        details: toJsonCompletionDetailsMode(value.details),
    };
}

/** Return one CompletionRequest from one JSON value. */
export function fromJsonCompletionRequest(value: Json): CompletionRequest {
    const object = jsonObject(value);

    return {
        position: fromJsonQueryPosition(jsonField(object, "position")),
        trigger: fromJsonCompletionTrigger(jsonField(object, "trigger")),
        includeAutoImports: jsonBool(jsonField(object, "includeAutoImports")),
        details: fromJsonCompletionDetailsMode(jsonField(object, "details")),
    };
}

/** A completion response. */
export type CompletionResponse = {
    /** Completion entries. */
    readonly entries: ReadonlyArray<CompletionEntry>;
    /** Whether another request may produce more items. */
    readonly isIncomplete: boolean;
};

export const CompletionResponse = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: CompletionResponse): void {
        encodeCompletionResponse(writer, value);
    },

    /** Decode one CompletionResponse. */
    decode(reader: BinaryReader): CompletionResponse {
        return decodeCompletionResponse(reader);
    },

    /** Return this value as JSON. */
    toJson(value: CompletionResponse): Json {
        return toJsonCompletionResponse(value);
    },

    /** Return one CompletionResponse from one JSON value. */
    fromJson(value: Json): CompletionResponse {
        return fromJsonCompletionResponse(value);
    },
};

/** Encode one CompletionResponse. */
export function encodeCompletionResponse(writer: BinaryWriter, value: CompletionResponse): void {
    writer.writeUnsigned(value.entries.length);
    for (const item0 of value.entries) {
        encodeCompletionEntry(writer, item0);
    }
    writer.writeBool(value.isIncomplete);
}

/** Decode one CompletionResponse. */
export function decodeCompletionResponse(reader: BinaryReader): CompletionResponse {
    const entries = (() => { const length0 = reader.readNumber(); const items0: Array<CompletionEntry> = []; for (let index = 0; index < length0; index += 1) { items0.push(decodeCompletionEntry(reader)); } return items0; })();
    const isIncomplete = reader.readBool();

    return {
        entries,
        isIncomplete,
    };
}

/** Return one JSON value for one CompletionResponse. */
export function toJsonCompletionResponse(value: CompletionResponse): Json {
    return {
        entries: value.entries.map((item0) => toJsonCompletionEntry(item0)),
        isIncomplete: value.isIncomplete,
    };
}

/** Return one CompletionResponse from one JSON value. */
export function fromJsonCompletionResponse(value: Json): CompletionResponse {
    const object = jsonObject(value);

    return {
        entries: jsonArray(jsonField(object, "entries")).map((item0) => fromJsonCompletionEntry(item0)),
        isIncomplete: jsonBool(jsonField(object, "isIncomplete")),
    };
}

/** Trigger character that caused the completion. */
export type CompletionTrigger =
    /** Invoked manually or automatically. */
    | {
          readonly kind: "invoked";
      }
    /** Triggered by one character, for example `.`. */
    | {
          readonly kind: "character";
          readonly character: string;
      }
    /** Retriggered for incomplete results. */
    | {
          readonly kind: "incomplete";
      }
;

export const CompletionTrigger = {
    /** Invoked manually or automatically. */
    invoked(): CompletionTrigger {
        return { kind: "invoked" };
    },

    /** Triggered by one character, for example `.`. */
    character(character: string): CompletionTrigger {
        return { kind: "character", character };
    },

    /** Retriggered for incomplete results. */
    incomplete(): CompletionTrigger {
        return { kind: "incomplete" };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: CompletionTrigger): void {
        encodeCompletionTrigger(writer, value);
    },

    /** Decode one CompletionTrigger. */
    decode(reader: BinaryReader): CompletionTrigger {
        return decodeCompletionTrigger(reader);
    },

    /** Return this value as JSON. */
    toJson(value: CompletionTrigger): Json {
        return toJsonCompletionTrigger(value);
    },

    /** Return one CompletionTrigger from one JSON value. */
    fromJson(value: Json): CompletionTrigger {
        return fromJsonCompletionTrigger(value);
    },
};

/** Encode one CompletionTrigger. */
export function encodeCompletionTrigger(writer: BinaryWriter, value: CompletionTrigger): void {
    switch (value.kind) {
        case "invoked":
            writer.writeUnsigned(0);
            return;
        case "character":
            writer.writeUnsigned(1);
            writer.writeChar(value.character);
            return;
        case "incomplete":
            writer.writeUnsigned(2);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one CompletionTrigger. */
export function decodeCompletionTrigger(reader: BinaryReader): CompletionTrigger {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            return { kind: "invoked" };
        }
        case 1: {
            const character = reader.readChar();

            return { kind: "character", character };
        }
        case 2: {
            return { kind: "incomplete" };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one CompletionTrigger. */
export function toJsonCompletionTrigger(value: CompletionTrigger): Json {
    switch (value.kind) {
        case "invoked":
            return {
                kind: "invoked",
            };
        case "character":
            return {
                kind: "character",
                character: value.character,
            };
        case "incomplete":
            return {
                kind: "incomplete",
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one CompletionTrigger from one JSON value. */
export function fromJsonCompletionTrigger(value: Json): CompletionTrigger {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "invoked":
            return {
                kind,
            };
        case "character":
            return {
                kind,
                character: jsonString(jsonField(object, "character")),
            };
        case "incomplete":
            return {
                kind,
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}
