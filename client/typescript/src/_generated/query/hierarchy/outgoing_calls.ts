/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, jsonArray, jsonField, jsonObject } from "../../../protocol/serde.js";
import type { CallItem } from "./call_item.js";
import type { Span } from "../../source/file/model/span.js";
import { decodeCallItem, encodeCallItem, fromJsonCallItem, toJsonCallItem } from "./call_item.js";
import { decodeSpan, encodeSpan, fromJsonSpan, toJsonSpan } from "../../source/file/model/span.js";

/** Request outgoing calls. */
export type OutgoingCallsRequest = {
    /** The call item to expand. */
    readonly item: CallItem;
};

export const OutgoingCallsRequest = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: OutgoingCallsRequest): void {
        encodeOutgoingCallsRequest(writer, value);
    },

    /** Decode one OutgoingCallsRequest. */
    decode(reader: BinaryReader): OutgoingCallsRequest {
        return decodeOutgoingCallsRequest(reader);
    },

    /** Return this value as JSON. */
    toJson(value: OutgoingCallsRequest): Json {
        return toJsonOutgoingCallsRequest(value);
    },

    /** Return one OutgoingCallsRequest from one JSON value. */
    fromJson(value: Json): OutgoingCallsRequest {
        return fromJsonOutgoingCallsRequest(value);
    },
};

/** Encode one OutgoingCallsRequest. */
export function encodeOutgoingCallsRequest(writer: BinaryWriter, value: OutgoingCallsRequest): void {
    encodeCallItem(writer, value.item);
}

/** Decode one OutgoingCallsRequest. */
export function decodeOutgoingCallsRequest(reader: BinaryReader): OutgoingCallsRequest {
    const item = decodeCallItem(reader);

    return {
        item,
    };
}

/** Return one JSON value for one OutgoingCallsRequest. */
export function toJsonOutgoingCallsRequest(value: OutgoingCallsRequest): Json {
    return {
        item: toJsonCallItem(value.item),
    };
}

/** Return one OutgoingCallsRequest from one JSON value. */
export function fromJsonOutgoingCallsRequest(value: Json): OutgoingCallsRequest {
    const object = jsonObject(value);

    return {
        item: fromJsonCallItem(jsonField(object, "item")),
    };
}

/** Response payload for outgoing call queries. */
export type OutgoingCallsResponse = {
    /** Outgoing calls. */
    readonly calls: ReadonlyArray<OutgoingCall>;
};

export const OutgoingCallsResponse = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: OutgoingCallsResponse): void {
        encodeOutgoingCallsResponse(writer, value);
    },

    /** Decode one OutgoingCallsResponse. */
    decode(reader: BinaryReader): OutgoingCallsResponse {
        return decodeOutgoingCallsResponse(reader);
    },

    /** Return this value as JSON. */
    toJson(value: OutgoingCallsResponse): Json {
        return toJsonOutgoingCallsResponse(value);
    },

    /** Return one OutgoingCallsResponse from one JSON value. */
    fromJson(value: Json): OutgoingCallsResponse {
        return fromJsonOutgoingCallsResponse(value);
    },
};

/** Encode one OutgoingCallsResponse. */
export function encodeOutgoingCallsResponse(writer: BinaryWriter, value: OutgoingCallsResponse): void {
    writer.writeUnsigned(value.calls.length);
    for (const item0 of value.calls) {
        encodeOutgoingCall(writer, item0);
    }
}

/** Decode one OutgoingCallsResponse. */
export function decodeOutgoingCallsResponse(reader: BinaryReader): OutgoingCallsResponse {
    const calls = (() => { const length0 = reader.readNumber(); const items0: Array<OutgoingCall> = []; for (let index = 0; index < length0; index += 1) { items0.push(decodeOutgoingCall(reader)); } return items0; })();

    return {
        calls,
    };
}

/** Return one JSON value for one OutgoingCallsResponse. */
export function toJsonOutgoingCallsResponse(value: OutgoingCallsResponse): Json {
    return {
        calls: value.calls.map((item0) => toJsonOutgoingCall(item0)),
    };
}

/** Return one OutgoingCallsResponse from one JSON value. */
export function fromJsonOutgoingCallsResponse(value: Json): OutgoingCallsResponse {
    const object = jsonObject(value);

    return {
        calls: jsonArray(jsonField(object, "calls")).map((item0) => fromJsonOutgoingCall(item0)),
    };
}

/** One outgoing call. */
export type OutgoingCall = {
    /** The item being called. */
    readonly to: CallItem;
    /** The call expression ranges in the source item. */
    readonly fromRanges: ReadonlyArray<Span>;
};

export const OutgoingCall = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: OutgoingCall): void {
        encodeOutgoingCall(writer, value);
    },

    /** Decode one OutgoingCall. */
    decode(reader: BinaryReader): OutgoingCall {
        return decodeOutgoingCall(reader);
    },

    /** Return this value as JSON. */
    toJson(value: OutgoingCall): Json {
        return toJsonOutgoingCall(value);
    },

    /** Return one OutgoingCall from one JSON value. */
    fromJson(value: Json): OutgoingCall {
        return fromJsonOutgoingCall(value);
    },
};

/** Encode one OutgoingCall. */
export function encodeOutgoingCall(writer: BinaryWriter, value: OutgoingCall): void {
    encodeCallItem(writer, value.to);
    writer.writeUnsigned(value.fromRanges.length);
    for (const item1 of value.fromRanges) {
        encodeSpan(writer, item1);
    }
}

/** Decode one OutgoingCall. */
export function decodeOutgoingCall(reader: BinaryReader): OutgoingCall {
    const to = decodeCallItem(reader);
    const fromRanges = (() => { const length1 = reader.readNumber(); const items1: Array<Span> = []; for (let index = 0; index < length1; index += 1) { items1.push(decodeSpan(reader)); } return items1; })();

    return {
        to,
        fromRanges,
    };
}

/** Return one JSON value for one OutgoingCall. */
export function toJsonOutgoingCall(value: OutgoingCall): Json {
    return {
        to: toJsonCallItem(value.to),
        fromRanges: value.fromRanges.map((item0) => toJsonSpan(item0)),
    };
}

/** Return one OutgoingCall from one JSON value. */
export function fromJsonOutgoingCall(value: Json): OutgoingCall {
    const object = jsonObject(value);

    return {
        to: fromJsonCallItem(jsonField(object, "to")),
        fromRanges: jsonArray(jsonField(object, "fromRanges")).map((item0) => fromJsonSpan(item0)),
    };
}
