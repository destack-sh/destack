/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, jsonArray, jsonField, jsonObject } from "../../../protocol/serde.js";
import type { CallItem } from "./call_item.js";
import type { Span } from "../../source/file/model/span.js";
import { decodeCallItem, encodeCallItem, fromJsonCallItem, toJsonCallItem } from "./call_item.js";
import { decodeSpan, encodeSpan, fromJsonSpan, toJsonSpan } from "../../source/file/model/span.js";

/** One incoming call. */
export type IncomingCall = {
    /** The item that contains the call sites. */
    readonly from: CallItem;
    /** The call expression ranges within `from`. */
    readonly fromRanges: ReadonlyArray<Span>;
};

export const IncomingCall = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: IncomingCall): void {
        encodeIncomingCall(writer, value);
    },

    /** Decode one IncomingCall. */
    decode(reader: BinaryReader): IncomingCall {
        return decodeIncomingCall(reader);
    },

    /** Return this value as JSON. */
    toJson(value: IncomingCall): Json {
        return toJsonIncomingCall(value);
    },

    /** Return one IncomingCall from one JSON value. */
    fromJson(value: Json): IncomingCall {
        return fromJsonIncomingCall(value);
    },
};

/** Encode one IncomingCall. */
export function encodeIncomingCall(writer: BinaryWriter, value: IncomingCall): void {
    encodeCallItem(writer, value.from);
    writer.writeUnsigned(value.fromRanges.length);
    for (const item1 of value.fromRanges) {
        encodeSpan(writer, item1);
    }
}

/** Decode one IncomingCall. */
export function decodeIncomingCall(reader: BinaryReader): IncomingCall {
    const from_ = decodeCallItem(reader);
    const fromRanges = (() => { const length1 = reader.readNumber(); const items1: Array<Span> = []; for (let index = 0; index < length1; index += 1) { items1.push(decodeSpan(reader)); } return items1; })();

    return {
        from: from_,
        fromRanges,
    };
}

/** Return one JSON value for one IncomingCall. */
export function toJsonIncomingCall(value: IncomingCall): Json {
    return {
        from: toJsonCallItem(value.from),
        fromRanges: value.fromRanges.map((item0) => toJsonSpan(item0)),
    };
}

/** Return one IncomingCall from one JSON value. */
export function fromJsonIncomingCall(value: Json): IncomingCall {
    const object = jsonObject(value);

    return {
        from: fromJsonCallItem(jsonField(object, "from")),
        fromRanges: jsonArray(jsonField(object, "fromRanges")).map((item0) => fromJsonSpan(item0)),
    };
}

/** An incoming calls request. */
export type IncomingCallsRequest = {
    /** The call item to expand. */
    readonly item: CallItem;
};

export const IncomingCallsRequest = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: IncomingCallsRequest): void {
        encodeIncomingCallsRequest(writer, value);
    },

    /** Decode one IncomingCallsRequest. */
    decode(reader: BinaryReader): IncomingCallsRequest {
        return decodeIncomingCallsRequest(reader);
    },

    /** Return this value as JSON. */
    toJson(value: IncomingCallsRequest): Json {
        return toJsonIncomingCallsRequest(value);
    },

    /** Return one IncomingCallsRequest from one JSON value. */
    fromJson(value: Json): IncomingCallsRequest {
        return fromJsonIncomingCallsRequest(value);
    },
};

/** Encode one IncomingCallsRequest. */
export function encodeIncomingCallsRequest(writer: BinaryWriter, value: IncomingCallsRequest): void {
    encodeCallItem(writer, value.item);
}

/** Decode one IncomingCallsRequest. */
export function decodeIncomingCallsRequest(reader: BinaryReader): IncomingCallsRequest {
    const item = decodeCallItem(reader);

    return {
        item,
    };
}

/** Return one JSON value for one IncomingCallsRequest. */
export function toJsonIncomingCallsRequest(value: IncomingCallsRequest): Json {
    return {
        item: toJsonCallItem(value.item),
    };
}

/** Return one IncomingCallsRequest from one JSON value. */
export function fromJsonIncomingCallsRequest(value: Json): IncomingCallsRequest {
    const object = jsonObject(value);

    return {
        item: fromJsonCallItem(jsonField(object, "item")),
    };
}

/** An incoming calls response. */
export type IncomingCallsResponse = {
    /** Incoming calls. */
    readonly calls: ReadonlyArray<IncomingCall>;
};

export const IncomingCallsResponse = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: IncomingCallsResponse): void {
        encodeIncomingCallsResponse(writer, value);
    },

    /** Decode one IncomingCallsResponse. */
    decode(reader: BinaryReader): IncomingCallsResponse {
        return decodeIncomingCallsResponse(reader);
    },

    /** Return this value as JSON. */
    toJson(value: IncomingCallsResponse): Json {
        return toJsonIncomingCallsResponse(value);
    },

    /** Return one IncomingCallsResponse from one JSON value. */
    fromJson(value: Json): IncomingCallsResponse {
        return fromJsonIncomingCallsResponse(value);
    },
};

/** Encode one IncomingCallsResponse. */
export function encodeIncomingCallsResponse(writer: BinaryWriter, value: IncomingCallsResponse): void {
    writer.writeUnsigned(value.calls.length);
    for (const item0 of value.calls) {
        encodeIncomingCall(writer, item0);
    }
}

/** Decode one IncomingCallsResponse. */
export function decodeIncomingCallsResponse(reader: BinaryReader): IncomingCallsResponse {
    const calls = (() => { const length0 = reader.readNumber(); const items0: Array<IncomingCall> = []; for (let index = 0; index < length0; index += 1) { items0.push(decodeIncomingCall(reader)); } return items0; })();

    return {
        calls,
    };
}

/** Return one JSON value for one IncomingCallsResponse. */
export function toJsonIncomingCallsResponse(value: IncomingCallsResponse): Json {
    return {
        calls: value.calls.map((item0) => toJsonIncomingCall(item0)),
    };
}

/** Return one IncomingCallsResponse from one JSON value. */
export function fromJsonIncomingCallsResponse(value: Json): IncomingCallsResponse {
    const object = jsonObject(value);

    return {
        calls: jsonArray(jsonField(object, "calls")).map((item0) => fromJsonIncomingCall(item0)),
    };
}
