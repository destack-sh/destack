/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, jsonField, jsonObject, jsonOptional, jsonString } from "../../../protocol/serde.js";
import type { GlobalSymbolId } from "../../dir/symbol/symbol.js";
import type { Target } from "../protocol/target.js";
import type { QueryPosition } from "../protocol/target.js";
import { decodeGlobalSymbolId, encodeGlobalSymbolId, fromJsonGlobalSymbolId, toJsonGlobalSymbolId } from "../../dir/symbol/symbol.js";
import { decodeTarget, encodeTarget, fromJsonTarget, toJsonTarget } from "../protocol/target.js";
import { decodeQueryPosition, encodeQueryPosition, fromJsonQueryPosition, toJsonQueryPosition } from "../protocol/target.js";

/** Request the call item at a cursor position. */
export type CallItemRequest = {
    /** The queried position. */
    readonly position: QueryPosition;
};

export const CallItemRequest = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: CallItemRequest): void {
        encodeCallItemRequest(writer, value);
    },

    /** Decode one CallItemRequest. */
    decode(reader: BinaryReader): CallItemRequest {
        return decodeCallItemRequest(reader);
    },

    /** Return this value as JSON. */
    toJson(value: CallItemRequest): Json {
        return toJsonCallItemRequest(value);
    },

    /** Return one CallItemRequest from one JSON value. */
    fromJson(value: Json): CallItemRequest {
        return fromJsonCallItemRequest(value);
    },
};

/** Encode one CallItemRequest. */
export function encodeCallItemRequest(writer: BinaryWriter, value: CallItemRequest): void {
    encodeQueryPosition(writer, value.position);
}

/** Decode one CallItemRequest. */
export function decodeCallItemRequest(reader: BinaryReader): CallItemRequest {
    const position = decodeQueryPosition(reader);

    return {
        position,
    };
}

/** Return one JSON value for one CallItemRequest. */
export function toJsonCallItemRequest(value: CallItemRequest): Json {
    return {
        position: toJsonQueryPosition(value.position),
    };
}

/** Return one CallItemRequest from one JSON value. */
export function fromJsonCallItemRequest(value: Json): CallItemRequest {
    const object = jsonObject(value);

    return {
        position: fromJsonQueryPosition(jsonField(object, "position")),
    };
}

/** One callable item. */
export type CallItem = {
    /** The item name. */
    readonly name: string;
    /** The callable kind. */
    readonly kind: CallItemKind;
    /** The rendered signature when available. */
    readonly detail?: string;
    /** The target source. */
    readonly target: Target;
    /** The resolved callable symbol. */
    readonly symbolId: GlobalSymbolId;
};

export const CallItem = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: CallItem): void {
        encodeCallItem(writer, value);
    },

    /** Decode one CallItem. */
    decode(reader: BinaryReader): CallItem {
        return decodeCallItem(reader);
    },

    /** Return this value as JSON. */
    toJson(value: CallItem): Json {
        return toJsonCallItem(value);
    },

    /** Return one CallItem from one JSON value. */
    fromJson(value: Json): CallItem {
        return fromJsonCallItem(value);
    },
};

/** Encode one CallItem. */
export function encodeCallItem(writer: BinaryWriter, value: CallItem): void {
    writer.writeString(value.name);
    encodeCallItemKind(writer, value.kind);
    writer.writeOption(value.detail, (value2) => {
        writer.writeString(value2);
    });
    encodeTarget(writer, value.target);
    encodeGlobalSymbolId(writer, value.symbolId);
}

/** Decode one CallItem. */
export function decodeCallItem(reader: BinaryReader): CallItem {
    const name = reader.readString();
    const kind = decodeCallItemKind(reader);
    const detail = reader.readOption(() => reader.readString());
    const target = decodeTarget(reader);
    const symbolId = decodeGlobalSymbolId(reader);

    return {
        name,
        kind,
        ...(detail === undefined ? {} : { detail }),
        target,
        symbolId,
    };
}

/** Return one JSON value for one CallItem. */
export function toJsonCallItem(value: CallItem): Json {
    return {
        name: value.name,
        kind: toJsonCallItemKind(value.kind),
        ...(value.detail === undefined ? {} : { detail: value.detail }),
        target: toJsonTarget(value.target),
        symbolId: toJsonGlobalSymbolId(value.symbolId),
    };
}

/** Return one CallItem from one JSON value. */
export function fromJsonCallItem(value: Json): CallItem {
    const object = jsonObject(value);

    return {
        name: jsonString(jsonField(object, "name")),
        kind: fromJsonCallItemKind(jsonField(object, "kind")),
        detail: jsonOptional(object, "detail", (value) => jsonString(value)),
        target: fromJsonTarget(jsonField(object, "target")),
        symbolId: fromJsonGlobalSymbolId(jsonField(object, "symbolId")),
    };
}

/** Kind of callable item. */
export type CallItemKind = "function" | "method" | "constructor";

export const CallItemKind = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: CallItemKind): void {
        encodeCallItemKind(writer, value);
    },

    /** Decode one CallItemKind. */
    decode(reader: BinaryReader): CallItemKind {
        return decodeCallItemKind(reader);
    },

    /** Return this value as JSON. */
    toJson(value: CallItemKind): Json {
        return toJsonCallItemKind(value);
    },

    /** Return one CallItemKind from one JSON value. */
    fromJson(value: Json): CallItemKind {
        return fromJsonCallItemKind(value);
    },
};

/** Encode one CallItemKind. */
export function encodeCallItemKind(writer: BinaryWriter, value: CallItemKind): void {
    switch (value) {
        case "function":
            writer.writeUnsigned(0);
            return;
        case "method":
            writer.writeUnsigned(1);
            return;
        case "constructor":
            writer.writeUnsigned(2);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one CallItemKind. */
export function decodeCallItemKind(reader: BinaryReader): CallItemKind {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "function";
        case 1:
            return "method";
        case 2:
            return "constructor";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one CallItemKind. */
export function toJsonCallItemKind(value: CallItemKind): Json {
    return value;
}

/** Return one CallItemKind from one JSON value. */
export function fromJsonCallItemKind(value: Json): CallItemKind {
    const variant = jsonString(value);

    switch (variant) {
        case "function":
            return "function";
        case "method":
            return "method";
        case "constructor":
            return "constructor";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** Response payload for call item queries. */
export type CallItemResponse = {
    /** Call item, if available. */
    readonly item?: CallItem;
};

export const CallItemResponse = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: CallItemResponse): void {
        encodeCallItemResponse(writer, value);
    },

    /** Decode one CallItemResponse. */
    decode(reader: BinaryReader): CallItemResponse {
        return decodeCallItemResponse(reader);
    },

    /** Return this value as JSON. */
    toJson(value: CallItemResponse): Json {
        return toJsonCallItemResponse(value);
    },

    /** Return one CallItemResponse from one JSON value. */
    fromJson(value: Json): CallItemResponse {
        return fromJsonCallItemResponse(value);
    },
};

/** Encode one CallItemResponse. */
export function encodeCallItemResponse(writer: BinaryWriter, value: CallItemResponse): void {
    writer.writeOption(value.item, (value0) => {
        encodeCallItem(writer, value0);
    });
}

/** Decode one CallItemResponse. */
export function decodeCallItemResponse(reader: BinaryReader): CallItemResponse {
    const item = reader.readOption(() => decodeCallItem(reader));

    return {
        ...(item === undefined ? {} : { item }),
    };
}

/** Return one JSON value for one CallItemResponse. */
export function toJsonCallItemResponse(value: CallItemResponse): Json {
    return {
        ...(value.item === undefined ? {} : { item: toJsonCallItem(value.item) }),
    };
}

/** Return one CallItemResponse from one JSON value. */
export function fromJsonCallItemResponse(value: Json): CallItemResponse {
    const object = jsonObject(value);

    return {
        item: jsonOptional(object, "item", (value) => fromJsonCallItem(value)),
    };
}
