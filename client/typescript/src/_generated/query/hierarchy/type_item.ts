/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, jsonField, jsonObject, jsonOptional, jsonString } from "../../../protocol/serde.js";
import type { GlobalSymbolId } from "../../dir/symbol/symbol.js";
import type { SymbolKind } from "../document/outline.js";
import type { Target } from "../protocol/target.js";
import type { QueryPosition } from "../protocol/target.js";
import { decodeGlobalSymbolId, encodeGlobalSymbolId, fromJsonGlobalSymbolId, toJsonGlobalSymbolId } from "../../dir/symbol/symbol.js";
import { decodeSymbolKind, encodeSymbolKind, fromJsonSymbolKind, toJsonSymbolKind } from "../document/outline.js";
import { decodeTarget, encodeTarget, fromJsonTarget, toJsonTarget } from "../protocol/target.js";
import { decodeQueryPosition, encodeQueryPosition, fromJsonQueryPosition, toJsonQueryPosition } from "../protocol/target.js";

/** An item in the type hierarchy. */
export type TypeItem = {
    /** The name of the type. */
    readonly name: string;
    /** The kind of type. */
    readonly kind: SymbolKind;
    /** The rendered generic parameters. */
    readonly detail?: string;
    /** The target source. */
    readonly target: Target;
    /** The resolved type symbol. */
    readonly symbolId: GlobalSymbolId;
};

export const TypeItem = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: TypeItem): void {
        encodeTypeItem(writer, value);
    },

    /** Decode one TypeItem. */
    decode(reader: BinaryReader): TypeItem {
        return decodeTypeItem(reader);
    },

    /** Return this value as JSON. */
    toJson(value: TypeItem): Json {
        return toJsonTypeItem(value);
    },

    /** Return one TypeItem from one JSON value. */
    fromJson(value: Json): TypeItem {
        return fromJsonTypeItem(value);
    },
};

/** Encode one TypeItem. */
export function encodeTypeItem(writer: BinaryWriter, value: TypeItem): void {
    writer.writeString(value.name);
    encodeSymbolKind(writer, value.kind);
    writer.writeOption(value.detail, (value2) => {
        writer.writeString(value2);
    });
    encodeTarget(writer, value.target);
    encodeGlobalSymbolId(writer, value.symbolId);
}

/** Decode one TypeItem. */
export function decodeTypeItem(reader: BinaryReader): TypeItem {
    const name = reader.readString();
    const kind = decodeSymbolKind(reader);
    const detail = reader.readOption(() => reader.readString());
    const target = decodeTarget(reader);
    const symbolId = decodeGlobalSymbolId(reader);

    return {
        name,
        kind,
        ...(detail === undefined ? {} : { detail }),
        target,
        symbolId,
    };
}

/** Return one JSON value for one TypeItem. */
export function toJsonTypeItem(value: TypeItem): Json {
    return {
        name: value.name,
        kind: toJsonSymbolKind(value.kind),
        ...(value.detail === undefined ? {} : { detail: value.detail }),
        target: toJsonTarget(value.target),
        symbolId: toJsonGlobalSymbolId(value.symbolId),
    };
}

/** Return one TypeItem from one JSON value. */
export function fromJsonTypeItem(value: Json): TypeItem {
    const object = jsonObject(value);

    return {
        name: jsonString(jsonField(object, "name")),
        kind: fromJsonSymbolKind(jsonField(object, "kind")),
        detail: jsonOptional(object, "detail", (value) => jsonString(value)),
        target: fromJsonTarget(jsonField(object, "target")),
        symbolId: fromJsonGlobalSymbolId(jsonField(object, "symbolId")),
    };
}

/** Request the type item at a cursor position. */
export type TypeItemRequest = {
    /** The queried position. */
    readonly position: QueryPosition;
};

export const TypeItemRequest = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: TypeItemRequest): void {
        encodeTypeItemRequest(writer, value);
    },

    /** Decode one TypeItemRequest. */
    decode(reader: BinaryReader): TypeItemRequest {
        return decodeTypeItemRequest(reader);
    },

    /** Return this value as JSON. */
    toJson(value: TypeItemRequest): Json {
        return toJsonTypeItemRequest(value);
    },

    /** Return one TypeItemRequest from one JSON value. */
    fromJson(value: Json): TypeItemRequest {
        return fromJsonTypeItemRequest(value);
    },
};

/** Encode one TypeItemRequest. */
export function encodeTypeItemRequest(writer: BinaryWriter, value: TypeItemRequest): void {
    encodeQueryPosition(writer, value.position);
}

/** Decode one TypeItemRequest. */
export function decodeTypeItemRequest(reader: BinaryReader): TypeItemRequest {
    const position = decodeQueryPosition(reader);

    return {
        position,
    };
}

/** Return one JSON value for one TypeItemRequest. */
export function toJsonTypeItemRequest(value: TypeItemRequest): Json {
    return {
        position: toJsonQueryPosition(value.position),
    };
}

/** Return one TypeItemRequest from one JSON value. */
export function fromJsonTypeItemRequest(value: Json): TypeItemRequest {
    const object = jsonObject(value);

    return {
        position: fromJsonQueryPosition(jsonField(object, "position")),
    };
}

/** Response payload for type item queries. */
export type TypeItemResponse = {
    /** Type hierarchy item, if available. */
    readonly item?: TypeItem;
};

export const TypeItemResponse = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: TypeItemResponse): void {
        encodeTypeItemResponse(writer, value);
    },

    /** Decode one TypeItemResponse. */
    decode(reader: BinaryReader): TypeItemResponse {
        return decodeTypeItemResponse(reader);
    },

    /** Return this value as JSON. */
    toJson(value: TypeItemResponse): Json {
        return toJsonTypeItemResponse(value);
    },

    /** Return one TypeItemResponse from one JSON value. */
    fromJson(value: Json): TypeItemResponse {
        return fromJsonTypeItemResponse(value);
    },
};

/** Encode one TypeItemResponse. */
export function encodeTypeItemResponse(writer: BinaryWriter, value: TypeItemResponse): void {
    writer.writeOption(value.item, (value0) => {
        encodeTypeItem(writer, value0);
    });
}

/** Decode one TypeItemResponse. */
export function decodeTypeItemResponse(reader: BinaryReader): TypeItemResponse {
    const item = reader.readOption(() => decodeTypeItem(reader));

    return {
        ...(item === undefined ? {} : { item }),
    };
}

/** Return one JSON value for one TypeItemResponse. */
export function toJsonTypeItemResponse(value: TypeItemResponse): Json {
    return {
        ...(value.item === undefined ? {} : { item: toJsonTypeItem(value.item) }),
    };
}

/** Return one TypeItemResponse from one JSON value. */
export function fromJsonTypeItemResponse(value: Json): TypeItemResponse {
    const object = jsonObject(value);

    return {
        item: jsonOptional(object, "item", (value) => fromJsonTypeItem(value)),
    };
}
