/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, jsonArray, jsonField, jsonObject, jsonOptional, jsonString } from "../../../protocol/serde.js";
import type { PatchSet } from "../../source/edit/edit.js";
import { decodePatchSet, encodePatchSet, fromJsonPatchSet, toJsonPatchSet } from "../../source/edit/edit.js";

/** One source file or directory rename. */
export type FileRename = {
    /** The old path before the rename. */
    readonly oldPath: string;
    /** The new path after the rename. */
    readonly newPath: string;
};

export const FileRename = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: FileRename): void {
        encodeFileRename(writer, value);
    },

    /** Decode one FileRename. */
    decode(reader: BinaryReader): FileRename {
        return decodeFileRename(reader);
    },

    /** Return this value as JSON. */
    toJson(value: FileRename): Json {
        return toJsonFileRename(value);
    },

    /** Return one FileRename from one JSON value. */
    fromJson(value: Json): FileRename {
        return fromJsonFileRename(value);
    },
};

/** Encode one FileRename. */
export function encodeFileRename(writer: BinaryWriter, value: FileRename): void {
    writer.writeString(value.oldPath);
    writer.writeString(value.newPath);
}

/** Decode one FileRename. */
export function decodeFileRename(reader: BinaryReader): FileRename {
    const oldPath = reader.readString();
    const newPath = reader.readString();

    return {
        oldPath,
        newPath,
    };
}

/** Return one JSON value for one FileRename. */
export function toJsonFileRename(value: FileRename): Json {
    return {
        oldPath: value.oldPath,
        newPath: value.newPath,
    };
}

/** Return one FileRename from one JSON value. */
export function fromJsonFileRename(value: Json): FileRename {
    const object = jsonObject(value);

    return {
        oldPath: jsonString(jsonField(object, "oldPath")),
        newPath: jsonString(jsonField(object, "newPath")),
    };
}

/** Request payload for file rename edits. */
export type RenameFilesRequest = {
    /** The file rename entries to apply. */
    readonly renames: ReadonlyArray<FileRename>;
};

export const RenameFilesRequest = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: RenameFilesRequest): void {
        encodeRenameFilesRequest(writer, value);
    },

    /** Decode one RenameFilesRequest. */
    decode(reader: BinaryReader): RenameFilesRequest {
        return decodeRenameFilesRequest(reader);
    },

    /** Return this value as JSON. */
    toJson(value: RenameFilesRequest): Json {
        return toJsonRenameFilesRequest(value);
    },

    /** Return one RenameFilesRequest from one JSON value. */
    fromJson(value: Json): RenameFilesRequest {
        return fromJsonRenameFilesRequest(value);
    },
};

/** Encode one RenameFilesRequest. */
export function encodeRenameFilesRequest(writer: BinaryWriter, value: RenameFilesRequest): void {
    writer.writeUnsigned(value.renames.length);
    for (const item0 of value.renames) {
        encodeFileRename(writer, item0);
    }
}

/** Decode one RenameFilesRequest. */
export function decodeRenameFilesRequest(reader: BinaryReader): RenameFilesRequest {
    const renames = (() => { const length0 = reader.readNumber(); const items0: Array<FileRename> = []; for (let index = 0; index < length0; index += 1) { items0.push(decodeFileRename(reader)); } return items0; })();

    return {
        renames,
    };
}

/** Return one JSON value for one RenameFilesRequest. */
export function toJsonRenameFilesRequest(value: RenameFilesRequest): Json {
    return {
        renames: value.renames.map((item0) => toJsonFileRename(item0)),
    };
}

/** Return one RenameFilesRequest from one JSON value. */
export function fromJsonRenameFilesRequest(value: Json): RenameFilesRequest {
    const object = jsonObject(value);

    return {
        renames: jsonArray(jsonField(object, "renames")).map((item0) => fromJsonFileRename(item0)),
    };
}

/** Response payload for file rename queries. */
export type RenameFilesResponse = {
    /** File rename edit, if available. */
    readonly edit?: PatchSet;
};

export const RenameFilesResponse = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: RenameFilesResponse): void {
        encodeRenameFilesResponse(writer, value);
    },

    /** Decode one RenameFilesResponse. */
    decode(reader: BinaryReader): RenameFilesResponse {
        return decodeRenameFilesResponse(reader);
    },

    /** Return this value as JSON. */
    toJson(value: RenameFilesResponse): Json {
        return toJsonRenameFilesResponse(value);
    },

    /** Return one RenameFilesResponse from one JSON value. */
    fromJson(value: Json): RenameFilesResponse {
        return fromJsonRenameFilesResponse(value);
    },
};

/** Encode one RenameFilesResponse. */
export function encodeRenameFilesResponse(writer: BinaryWriter, value: RenameFilesResponse): void {
    writer.writeOption(value.edit, (value0) => {
        encodePatchSet(writer, value0);
    });
}

/** Decode one RenameFilesResponse. */
export function decodeRenameFilesResponse(reader: BinaryReader): RenameFilesResponse {
    const edit = reader.readOption(() => decodePatchSet(reader));

    return {
        ...(edit === undefined ? {} : { edit }),
    };
}

/** Return one JSON value for one RenameFilesResponse. */
export function toJsonRenameFilesResponse(value: RenameFilesResponse): Json {
    return {
        ...(value.edit === undefined ? {} : { edit: toJsonPatchSet(value.edit) }),
    };
}

/** Return one RenameFilesResponse from one JSON value. */
export function fromJsonRenameFilesResponse(value: Json): RenameFilesResponse {
    const object = jsonObject(value);

    return {
        edit: jsonOptional(object, "edit", (value) => fromJsonPatchSet(value)),
    };
}
