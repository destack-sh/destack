/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, jsonArray, jsonField, jsonObject, jsonOptional, jsonString } from "../../../protocol/serde.js";
import type { GlobalSymbolId } from "../../dir/symbol/symbol.js";
import type { Target } from "../protocol/target.js";
import type { QueryPosition } from "../protocol/target.js";
import { decodeGlobalSymbolId, encodeGlobalSymbolId, fromJsonGlobalSymbolId, toJsonGlobalSymbolId } from "../../dir/symbol/symbol.js";
import { decodeTarget, encodeTarget, fromJsonTarget, toJsonTarget } from "../protocol/target.js";
import { decodeQueryPosition, encodeQueryPosition, fromJsonQueryPosition, toJsonQueryPosition } from "../protocol/target.js";

/** Target of a rename query. */
export type RenameTarget = {
    /** The rename source. */
    readonly target: Target;
    /** The resolved rename symbols. */
    readonly symbols: ReadonlyArray<GlobalSymbolId>;
    /** The current name. */
    readonly placeholder: string;
};

export const RenameTarget = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: RenameTarget): void {
        encodeRenameTarget(writer, value);
    },

    /** Decode one RenameTarget. */
    decode(reader: BinaryReader): RenameTarget {
        return decodeRenameTarget(reader);
    },

    /** Return this value as JSON. */
    toJson(value: RenameTarget): Json {
        return toJsonRenameTarget(value);
    },

    /** Return one RenameTarget from one JSON value. */
    fromJson(value: Json): RenameTarget {
        return fromJsonRenameTarget(value);
    },
};

/** Encode one RenameTarget. */
export function encodeRenameTarget(writer: BinaryWriter, value: RenameTarget): void {
    encodeTarget(writer, value.target);
    writer.writeUnsigned(value.symbols.length);
    for (const item1 of value.symbols) {
        encodeGlobalSymbolId(writer, item1);
    }
    writer.writeString(value.placeholder);
}

/** Decode one RenameTarget. */
export function decodeRenameTarget(reader: BinaryReader): RenameTarget {
    const target = decodeTarget(reader);
    const symbols = (() => { const length1 = reader.readNumber(); const items1: Array<GlobalSymbolId> = []; for (let index = 0; index < length1; index += 1) { items1.push(decodeGlobalSymbolId(reader)); } return items1; })();
    const placeholder = reader.readString();

    return {
        target,
        symbols,
        placeholder,
    };
}

/** Return one JSON value for one RenameTarget. */
export function toJsonRenameTarget(value: RenameTarget): Json {
    return {
        target: toJsonTarget(value.target),
        symbols: value.symbols.map((item0) => toJsonGlobalSymbolId(item0)),
        placeholder: value.placeholder,
    };
}

/** Return one RenameTarget from one JSON value. */
export function fromJsonRenameTarget(value: Json): RenameTarget {
    const object = jsonObject(value);

    return {
        target: fromJsonTarget(jsonField(object, "target")),
        symbols: jsonArray(jsonField(object, "symbols")).map((item0) => fromJsonGlobalSymbolId(item0)),
        placeholder: jsonString(jsonField(object, "placeholder")),
    };
}

/** A rename target request. */
export type RenameTargetRequest = {
    /** The queried position. */
    readonly position: QueryPosition;
};

export const RenameTargetRequest = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: RenameTargetRequest): void {
        encodeRenameTargetRequest(writer, value);
    },

    /** Decode one RenameTargetRequest. */
    decode(reader: BinaryReader): RenameTargetRequest {
        return decodeRenameTargetRequest(reader);
    },

    /** Return this value as JSON. */
    toJson(value: RenameTargetRequest): Json {
        return toJsonRenameTargetRequest(value);
    },

    /** Return one RenameTargetRequest from one JSON value. */
    fromJson(value: Json): RenameTargetRequest {
        return fromJsonRenameTargetRequest(value);
    },
};

/** Encode one RenameTargetRequest. */
export function encodeRenameTargetRequest(writer: BinaryWriter, value: RenameTargetRequest): void {
    encodeQueryPosition(writer, value.position);
}

/** Decode one RenameTargetRequest. */
export function decodeRenameTargetRequest(reader: BinaryReader): RenameTargetRequest {
    const position = decodeQueryPosition(reader);

    return {
        position,
    };
}

/** Return one JSON value for one RenameTargetRequest. */
export function toJsonRenameTargetRequest(value: RenameTargetRequest): Json {
    return {
        position: toJsonQueryPosition(value.position),
    };
}

/** Return one RenameTargetRequest from one JSON value. */
export function fromJsonRenameTargetRequest(value: Json): RenameTargetRequest {
    const object = jsonObject(value);

    return {
        position: fromJsonQueryPosition(jsonField(object, "position")),
    };
}

/** A rename target response. */
export type RenameTargetResponse = {
    /** Rename target, if available. */
    readonly target?: RenameTarget;
};

export const RenameTargetResponse = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: RenameTargetResponse): void {
        encodeRenameTargetResponse(writer, value);
    },

    /** Decode one RenameTargetResponse. */
    decode(reader: BinaryReader): RenameTargetResponse {
        return decodeRenameTargetResponse(reader);
    },

    /** Return this value as JSON. */
    toJson(value: RenameTargetResponse): Json {
        return toJsonRenameTargetResponse(value);
    },

    /** Return one RenameTargetResponse from one JSON value. */
    fromJson(value: Json): RenameTargetResponse {
        return fromJsonRenameTargetResponse(value);
    },
};

/** Encode one RenameTargetResponse. */
export function encodeRenameTargetResponse(writer: BinaryWriter, value: RenameTargetResponse): void {
    writer.writeOption(value.target, (value0) => {
        encodeRenameTarget(writer, value0);
    });
}

/** Decode one RenameTargetResponse. */
export function decodeRenameTargetResponse(reader: BinaryReader): RenameTargetResponse {
    const target = reader.readOption(() => decodeRenameTarget(reader));

    return {
        ...(target === undefined ? {} : { target }),
    };
}

/** Return one JSON value for one RenameTargetResponse. */
export function toJsonRenameTargetResponse(value: RenameTargetResponse): Json {
    return {
        ...(value.target === undefined ? {} : { target: toJsonRenameTarget(value.target) }),
    };
}

/** Return one RenameTargetResponse from one JSON value. */
export function fromJsonRenameTargetResponse(value: Json): RenameTargetResponse {
    const object = jsonObject(value);

    return {
        target: jsonOptional(object, "target", (value) => fromJsonRenameTarget(value)),
    };
}
