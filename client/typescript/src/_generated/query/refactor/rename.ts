/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, jsonField, jsonObject, jsonOptional, jsonString } from "../../../protocol/serde.js";
import type { QueryPosition } from "../protocol/target.js";
import type { PatchSet } from "../../source/edit/edit.js";
import { decodeQueryPosition, encodeQueryPosition, fromJsonQueryPosition, toJsonQueryPosition } from "../protocol/target.js";
import { decodePatchSet, encodePatchSet, fromJsonPatchSet, toJsonPatchSet } from "../../source/edit/edit.js";

/** A rename request. */
export type RenameRequest = {
    /** The queried position. */
    readonly position: QueryPosition;
    /** The new name for the symbol. */
    readonly newName: string;
};

export const RenameRequest = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: RenameRequest): void {
        encodeRenameRequest(writer, value);
    },

    /** Decode one RenameRequest. */
    decode(reader: BinaryReader): RenameRequest {
        return decodeRenameRequest(reader);
    },

    /** Return this value as JSON. */
    toJson(value: RenameRequest): Json {
        return toJsonRenameRequest(value);
    },

    /** Return one RenameRequest from one JSON value. */
    fromJson(value: Json): RenameRequest {
        return fromJsonRenameRequest(value);
    },
};

/** Encode one RenameRequest. */
export function encodeRenameRequest(writer: BinaryWriter, value: RenameRequest): void {
    encodeQueryPosition(writer, value.position);
    writer.writeString(value.newName);
}

/** Decode one RenameRequest. */
export function decodeRenameRequest(reader: BinaryReader): RenameRequest {
    const position = decodeQueryPosition(reader);
    const newName = reader.readString();

    return {
        position,
        newName,
    };
}

/** Return one JSON value for one RenameRequest. */
export function toJsonRenameRequest(value: RenameRequest): Json {
    return {
        position: toJsonQueryPosition(value.position),
        newName: value.newName,
    };
}

/** Return one RenameRequest from one JSON value. */
export function fromJsonRenameRequest(value: Json): RenameRequest {
    const object = jsonObject(value);

    return {
        position: fromJsonQueryPosition(jsonField(object, "position")),
        newName: jsonString(jsonField(object, "newName")),
    };
}

/** A rename response. */
export type RenameResponse = {
    /** Rename edit, if available. */
    readonly edit?: PatchSet;
};

export const RenameResponse = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: RenameResponse): void {
        encodeRenameResponse(writer, value);
    },

    /** Decode one RenameResponse. */
    decode(reader: BinaryReader): RenameResponse {
        return decodeRenameResponse(reader);
    },

    /** Return this value as JSON. */
    toJson(value: RenameResponse): Json {
        return toJsonRenameResponse(value);
    },

    /** Return one RenameResponse from one JSON value. */
    fromJson(value: Json): RenameResponse {
        return fromJsonRenameResponse(value);
    },
};

/** Encode one RenameResponse. */
export function encodeRenameResponse(writer: BinaryWriter, value: RenameResponse): void {
    writer.writeOption(value.edit, (value0) => {
        encodePatchSet(writer, value0);
    });
}

/** Decode one RenameResponse. */
export function decodeRenameResponse(reader: BinaryReader): RenameResponse {
    const edit = reader.readOption(() => decodePatchSet(reader));

    return {
        ...(edit === undefined ? {} : { edit }),
    };
}

/** Return one JSON value for one RenameResponse. */
export function toJsonRenameResponse(value: RenameResponse): Json {
    return {
        ...(value.edit === undefined ? {} : { edit: toJsonPatchSet(value.edit) }),
    };
}

/** Return one RenameResponse from one JSON value. */
export function fromJsonRenameResponse(value: Json): RenameResponse {
    const object = jsonObject(value);

    return {
        edit: jsonOptional(object, "edit", (value) => fromJsonPatchSet(value)),
    };
}
