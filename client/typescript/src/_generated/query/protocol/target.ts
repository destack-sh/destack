/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, jsonField, jsonInteger, jsonObject, jsonOptional, jsonString } from "../../../protocol/serde.js";
import type { GlobalSymbolId } from "../../dir/symbol/symbol.js";
import type { GlobalNodeIdAny } from "../../dir/tree/node.js";
import type { FileId } from "../../source/file/model/file.js";
import type { ModuleId } from "../../source/file/model/module.js";
import type { ProfileId } from "../../source/file/model/profile.js";
import type { Span } from "../../source/file/model/span.js";
import { decodeGlobalSymbolId, encodeGlobalSymbolId, fromJsonGlobalSymbolId, toJsonGlobalSymbolId } from "../../dir/symbol/symbol.js";
import { decodeGlobalNodeIdAny, encodeGlobalNodeIdAny, fromJsonGlobalNodeIdAny, toJsonGlobalNodeIdAny } from "../../dir/tree/node.js";
import { decodeFileId, encodeFileId, fromJsonFileId, toJsonFileId } from "../../source/file/model/file.js";
import { decodeModuleId, encodeModuleId, fromJsonModuleId, toJsonModuleId } from "../../source/file/model/module.js";
import { decodeProfileId, encodeProfileId, fromJsonProfileId, toJsonProfileId } from "../../source/file/model/profile.js";
import { decodeSpan, encodeSpan, fromJsonSpan, toJsonSpan } from "../../source/file/model/span.js";

/** One module in one query profile. */
export type Module = {
    /** The queried module. */
    readonly moduleId: ModuleId;
    /** The queried profile. */
    readonly profileId: ProfileId;
};

export const Module = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: Module): void {
        encodeModule(writer, value);
    },

    /** Decode one Module. */
    decode(reader: BinaryReader): Module {
        return decodeModule(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Module): Json {
        return toJsonModule(value);
    },

    /** Return one Module from one JSON value. */
    fromJson(value: Json): Module {
        return fromJsonModule(value);
    },
};

/** Encode one Module. */
export function encodeModule(writer: BinaryWriter, value: Module): void {
    encodeModuleId(writer, value.moduleId);
    encodeProfileId(writer, value.profileId);
}

/** Decode one Module. */
export function decodeModule(reader: BinaryReader): Module {
    const moduleId = decodeModuleId(reader);
    const profileId = decodeProfileId(reader);

    return {
        moduleId,
        profileId,
    };
}

/** Return one JSON value for one Module. */
export function toJsonModule(value: Module): Json {
    return {
        moduleId: toJsonModuleId(value.moduleId),
        profileId: toJsonProfileId(value.profileId),
    };
}

/** Return one Module from one JSON value. */
export function fromJsonModule(value: Json): Module {
    const object = jsonObject(value);

    return {
        moduleId: fromJsonModuleId(jsonField(object, "moduleId")),
        profileId: fromJsonProfileId(jsonField(object, "profileId")),
    };
}

/** One byte position in a module source file. */
export type Position = {
    /** The queried module profile. */
    readonly module: Module;
    /** The source file. */
    readonly fileId: FileId;
    /** The byte offset in the source file. */
    readonly offset: number;
};

export const Position = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: Position): void {
        encodePosition(writer, value);
    },

    /** Decode one Position. */
    decode(reader: BinaryReader): Position {
        return decodePosition(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Position): Json {
        return toJsonPosition(value);
    },

    /** Return one Position from one JSON value. */
    fromJson(value: Json): Position {
        return fromJsonPosition(value);
    },
};

/** Encode one Position. */
export function encodePosition(writer: BinaryWriter, value: Position): void {
    encodeModule(writer, value.module);
    encodeFileId(writer, value.fileId);
    writer.writeUnsigned(value.offset);
}

/** Decode one Position. */
export function decodePosition(reader: BinaryReader): Position {
    const module_ = decodeModule(reader);
    const fileId = decodeFileId(reader);
    const offset = reader.readNumber();

    return {
        module: module_,
        fileId,
        offset,
    };
}

/** Return one JSON value for one Position. */
export function toJsonPosition(value: Position): Json {
    return {
        module: toJsonModule(value.module),
        fileId: toJsonFileId(value.fileId),
        offset: value.offset,
    };
}

/** Return one Position from one JSON value. */
export function fromJsonPosition(value: Json): Position {
    const object = jsonObject(value);

    return {
        module: fromJsonModule(jsonField(object, "module")),
        fileId: fromJsonFileId(jsonField(object, "fileId")),
        offset: jsonInteger(jsonField(object, "offset")),
    };
}

/** One source range in a module. */
export type Range = {
    /** The queried module profile. */
    readonly module: Module;
    /** The source range. */
    readonly span: Span;
};

export const Range = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: Range): void {
        encodeRange(writer, value);
    },

    /** Decode one Range. */
    decode(reader: BinaryReader): Range {
        return decodeRange(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Range): Json {
        return toJsonRange(value);
    },

    /** Return one Range from one JSON value. */
    fromJson(value: Json): Range {
        return fromJsonRange(value);
    },
};

/** Encode one Range. */
export function encodeRange(writer: BinaryWriter, value: Range): void {
    encodeModule(writer, value.module);
    encodeSpan(writer, value.span);
}

/** Decode one Range. */
export function decodeRange(reader: BinaryReader): Range {
    const module_ = decodeModule(reader);
    const span = decodeSpan(reader);

    return {
        module: module_,
        span,
    };
}

/** Return one JSON value for one Range. */
export function toJsonRange(value: Range): Json {
    return {
        module: toJsonModule(value.module),
        span: toJsonSpan(value.span),
    };
}

/** Return one Range from one JSON value. */
export function fromJsonRange(value: Json): Range {
    const object = jsonObject(value);

    return {
        module: fromJsonModule(jsonField(object, "module")),
        span: fromJsonSpan(jsonField(object, "span")),
    };
}

/** One source-backed target. */
export type Target = {
    /** The target module profile. */
    readonly module: Module;
    /** The full source range. */
    readonly span: Span;
    /** The primary selection range. */
    readonly selectionSpan?: Span;
    /** The target symbol when known. */
    readonly symbolId?: GlobalSymbolId;
    /** The target node when known. */
    readonly nodeId?: GlobalNodeIdAny;
};

export const Target = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: Target): void {
        encodeTarget(writer, value);
    },

    /** Decode one Target. */
    decode(reader: BinaryReader): Target {
        return decodeTarget(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Target): Json {
        return toJsonTarget(value);
    },

    /** Return one Target from one JSON value. */
    fromJson(value: Json): Target {
        return fromJsonTarget(value);
    },
};

/** Encode one Target. */
export function encodeTarget(writer: BinaryWriter, value: Target): void {
    encodeModule(writer, value.module);
    encodeSpan(writer, value.span);
    writer.writeOption(value.selectionSpan, (value2) => {
        encodeSpan(writer, value2);
    });
    writer.writeOption(value.symbolId, (value3) => {
        encodeGlobalSymbolId(writer, value3);
    });
    writer.writeOption(value.nodeId, (value4) => {
        encodeGlobalNodeIdAny(writer, value4);
    });
}

/** Decode one Target. */
export function decodeTarget(reader: BinaryReader): Target {
    const module_ = decodeModule(reader);
    const span = decodeSpan(reader);
    const selectionSpan = reader.readOption(() => decodeSpan(reader));
    const symbolId = reader.readOption(() => decodeGlobalSymbolId(reader));
    const nodeId = reader.readOption(() => decodeGlobalNodeIdAny(reader));

    return {
        module: module_,
        span,
        ...(selectionSpan === undefined ? {} : { selectionSpan }),
        ...(symbolId === undefined ? {} : { symbolId }),
        ...(nodeId === undefined ? {} : { nodeId }),
    };
}

/** Return one JSON value for one Target. */
export function toJsonTarget(value: Target): Json {
    return {
        module: toJsonModule(value.module),
        span: toJsonSpan(value.span),
        ...(value.selectionSpan === undefined ? {} : { selectionSpan: toJsonSpan(value.selectionSpan) }),
        ...(value.symbolId === undefined ? {} : { symbolId: toJsonGlobalSymbolId(value.symbolId) }),
        ...(value.nodeId === undefined ? {} : { nodeId: toJsonGlobalNodeIdAny(value.nodeId) }),
    };
}

/** Return one Target from one JSON value. */
export function fromJsonTarget(value: Json): Target {
    const object = jsonObject(value);

    return {
        module: fromJsonModule(jsonField(object, "module")),
        span: fromJsonSpan(jsonField(object, "span")),
        selectionSpan: jsonOptional(object, "selectionSpan", (value) => fromJsonSpan(value)),
        symbolId: jsonOptional(object, "symbolId", (value) => fromJsonGlobalSymbolId(value)),
        nodeId: jsonOptional(object, "nodeId", (value) => fromJsonGlobalNodeIdAny(value)),
    };
}

/** Text in display formats understood by clients. */
export type Text = {
    /** Plain text. */
    readonly plain?: string;
    /** Markdown text. */
    readonly markdown?: string;
};

export const Text = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: Text): void {
        encodeText(writer, value);
    },

    /** Decode one Text. */
    decode(reader: BinaryReader): Text {
        return decodeText(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Text): Json {
        return toJsonText(value);
    },

    /** Return one Text from one JSON value. */
    fromJson(value: Json): Text {
        return fromJsonText(value);
    },
};

/** Encode one Text. */
export function encodeText(writer: BinaryWriter, value: Text): void {
    writer.writeOption(value.plain, (value0) => {
        writer.writeString(value0);
    });
    writer.writeOption(value.markdown, (value1) => {
        writer.writeString(value1);
    });
}

/** Decode one Text. */
export function decodeText(reader: BinaryReader): Text {
    const plain = reader.readOption(() => reader.readString());
    const markdown = reader.readOption(() => reader.readString());

    return {
        ...(plain === undefined ? {} : { plain }),
        ...(markdown === undefined ? {} : { markdown }),
    };
}

/** Return one JSON value for one Text. */
export function toJsonText(value: Text): Json {
    return {
        ...(value.plain === undefined ? {} : { plain: value.plain }),
        ...(value.markdown === undefined ? {} : { markdown: value.markdown }),
    };
}

/** Return one Text from one JSON value. */
export function fromJsonText(value: Json): Text {
    const object = jsonObject(value);

    return {
        plain: jsonOptional(object, "plain", (value) => jsonString(value)),
        markdown: jsonOptional(object, "markdown", (value) => jsonString(value)),
    };
}
