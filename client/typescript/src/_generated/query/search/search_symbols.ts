/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, jsonArray, jsonField, jsonInteger, jsonObject, jsonOptional, jsonString } from "../../../protocol/serde.js";
import type { GlobalSymbolId } from "../../dir/symbol/symbol.js";
import type { SymbolKind } from "../document/outline.js";
import type { Target } from "../protocol/target.js";
import { decodeGlobalSymbolId, encodeGlobalSymbolId, fromJsonGlobalSymbolId, toJsonGlobalSymbolId } from "../../dir/symbol/symbol.js";
import { decodeSymbolKind, encodeSymbolKind, fromJsonSymbolKind, toJsonSymbolKind } from "../document/outline.js";
import { decodeTarget, encodeTarget, fromJsonTarget, toJsonTarget } from "../protocol/target.js";

/** One symbol search result. */
export type SearchSymbol = {
    /** The symbol's name. */
    readonly name: string;
    /** The kind of symbol. */
    readonly kind: SymbolKind;
    /** The exact symbol identity. */
    readonly symbolId: GlobalSymbolId;
    /** The symbol source target. */
    readonly target: Target;
    /** The containing declaration name. */
    readonly container?: string;
};

export const SearchSymbol = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: SearchSymbol): void {
        encodeSearchSymbol(writer, value);
    },

    /** Decode one SearchSymbol. */
    decode(reader: BinaryReader): SearchSymbol {
        return decodeSearchSymbol(reader);
    },

    /** Return this value as JSON. */
    toJson(value: SearchSymbol): Json {
        return toJsonSearchSymbol(value);
    },

    /** Return one SearchSymbol from one JSON value. */
    fromJson(value: Json): SearchSymbol {
        return fromJsonSearchSymbol(value);
    },
};

/** Encode one SearchSymbol. */
export function encodeSearchSymbol(writer: BinaryWriter, value: SearchSymbol): void {
    writer.writeString(value.name);
    encodeSymbolKind(writer, value.kind);
    encodeGlobalSymbolId(writer, value.symbolId);
    encodeTarget(writer, value.target);
    writer.writeOption(value.container, (value4) => {
        writer.writeString(value4);
    });
}

/** Decode one SearchSymbol. */
export function decodeSearchSymbol(reader: BinaryReader): SearchSymbol {
    const name = reader.readString();
    const kind = decodeSymbolKind(reader);
    const symbolId = decodeGlobalSymbolId(reader);
    const target = decodeTarget(reader);
    const container = reader.readOption(() => reader.readString());

    return {
        name,
        kind,
        symbolId,
        target,
        ...(container === undefined ? {} : { container }),
    };
}

/** Return one JSON value for one SearchSymbol. */
export function toJsonSearchSymbol(value: SearchSymbol): Json {
    return {
        name: value.name,
        kind: toJsonSymbolKind(value.kind),
        symbolId: toJsonGlobalSymbolId(value.symbolId),
        target: toJsonTarget(value.target),
        ...(value.container === undefined ? {} : { container: value.container }),
    };
}

/** Return one SearchSymbol from one JSON value. */
export function fromJsonSearchSymbol(value: Json): SearchSymbol {
    const object = jsonObject(value);

    return {
        name: jsonString(jsonField(object, "name")),
        kind: fromJsonSymbolKind(jsonField(object, "kind")),
        symbolId: fromJsonGlobalSymbolId(jsonField(object, "symbolId")),
        target: fromJsonTarget(jsonField(object, "target")),
        container: jsonOptional(object, "container", (value) => jsonString(value)),
    };
}

/** A symbol search request. */
export type SearchSymbolsRequest = {
    /** The search query string. */
    readonly query: string;
    /** The maximum number of results. */
    readonly maxResults: number;
};

export const SearchSymbolsRequest = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: SearchSymbolsRequest): void {
        encodeSearchSymbolsRequest(writer, value);
    },

    /** Decode one SearchSymbolsRequest. */
    decode(reader: BinaryReader): SearchSymbolsRequest {
        return decodeSearchSymbolsRequest(reader);
    },

    /** Return this value as JSON. */
    toJson(value: SearchSymbolsRequest): Json {
        return toJsonSearchSymbolsRequest(value);
    },

    /** Return one SearchSymbolsRequest from one JSON value. */
    fromJson(value: Json): SearchSymbolsRequest {
        return fromJsonSearchSymbolsRequest(value);
    },
};

/** Encode one SearchSymbolsRequest. */
export function encodeSearchSymbolsRequest(writer: BinaryWriter, value: SearchSymbolsRequest): void {
    writer.writeString(value.query);
    writer.writeUnsigned(value.maxResults);
}

/** Decode one SearchSymbolsRequest. */
export function decodeSearchSymbolsRequest(reader: BinaryReader): SearchSymbolsRequest {
    const query = reader.readString();
    const maxResults = reader.readNumber();

    return {
        query,
        maxResults,
    };
}

/** Return one JSON value for one SearchSymbolsRequest. */
export function toJsonSearchSymbolsRequest(value: SearchSymbolsRequest): Json {
    return {
        query: value.query,
        maxResults: value.maxResults,
    };
}

/** Return one SearchSymbolsRequest from one JSON value. */
export function fromJsonSearchSymbolsRequest(value: Json): SearchSymbolsRequest {
    const object = jsonObject(value);

    return {
        query: jsonString(jsonField(object, "query")),
        maxResults: jsonInteger(jsonField(object, "maxResults")),
    };
}

/** A symbol search response. */
export type SearchSymbolsResponse = {
    /** The matching symbols. */
    readonly symbols: ReadonlyArray<SearchSymbol>;
};

export const SearchSymbolsResponse = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: SearchSymbolsResponse): void {
        encodeSearchSymbolsResponse(writer, value);
    },

    /** Decode one SearchSymbolsResponse. */
    decode(reader: BinaryReader): SearchSymbolsResponse {
        return decodeSearchSymbolsResponse(reader);
    },

    /** Return this value as JSON. */
    toJson(value: SearchSymbolsResponse): Json {
        return toJsonSearchSymbolsResponse(value);
    },

    /** Return one SearchSymbolsResponse from one JSON value. */
    fromJson(value: Json): SearchSymbolsResponse {
        return fromJsonSearchSymbolsResponse(value);
    },
};

/** Encode one SearchSymbolsResponse. */
export function encodeSearchSymbolsResponse(writer: BinaryWriter, value: SearchSymbolsResponse): void {
    writer.writeUnsigned(value.symbols.length);
    for (const item0 of value.symbols) {
        encodeSearchSymbol(writer, item0);
    }
}

/** Decode one SearchSymbolsResponse. */
export function decodeSearchSymbolsResponse(reader: BinaryReader): SearchSymbolsResponse {
    const symbols = (() => { const length0 = reader.readNumber(); const items0: Array<SearchSymbol> = []; for (let index = 0; index < length0; index += 1) { items0.push(decodeSearchSymbol(reader)); } return items0; })();

    return {
        symbols,
    };
}

/** Return one JSON value for one SearchSymbolsResponse. */
export function toJsonSearchSymbolsResponse(value: SearchSymbolsResponse): Json {
    return {
        symbols: value.symbols.map((item0) => toJsonSearchSymbol(item0)),
    };
}

/** Return one SearchSymbolsResponse from one JSON value. */
export function fromJsonSearchSymbolsResponse(value: Json): SearchSymbolsResponse {
    const object = jsonObject(value);

    return {
        symbols: jsonArray(jsonField(object, "symbols")).map((item0) => fromJsonSearchSymbol(item0)),
    };
}
