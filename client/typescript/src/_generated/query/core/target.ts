/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, jsonField, jsonInteger, jsonObject, jsonOptional, jsonString } from "../../../protocol/serde.js";
import type { GlobalSymbolId } from "../../dir/symbol/symbol.js";
import type { GlobalNodeIdAny } from "../../dir/tree/node.js";
import type { FileId } from "../../source/file/model/file.js";
import type { ModuleId } from "../../source/file/model/module.js";
import type { ProfileId } from "../../source/file/model/profile.js";
import type { Span } from "../../source/file/model/span.js";
import { decodeGlobalSymbolId, encodeGlobalSymbolId, fromJsonGlobalSymbolId, toJsonGlobalSymbolId } from "../../dir/symbol/symbol.js";
import { decodeGlobalNodeIdAny, encodeGlobalNodeIdAny, fromJsonGlobalNodeIdAny, toJsonGlobalNodeIdAny } from "../../dir/tree/node.js";
import { decodeFileId, encodeFileId, fromJsonFileId, toJsonFileId } from "../../source/file/model/file.js";
import { decodeModuleId, encodeModuleId, fromJsonModuleId, toJsonModuleId } from "../../source/file/model/module.js";
import { decodeProfileId, encodeProfileId, fromJsonProfileId, toJsonProfileId } from "../../source/file/model/profile.js";
import { decodeSpan, encodeSpan, fromJsonSpan, toJsonSpan } from "../../source/file/model/span.js";

/** One module in one query profile. */
export type QueryModule = {
    /** The queried module. */
    readonly moduleId: ModuleId;
    /** The queried profile. */
    readonly profileId: ProfileId;
};

export const QueryModule = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: QueryModule): void {
        encodeQueryModule(writer, value);
    },

    /** Decode one QueryModule. */
    decode(reader: BinaryReader): QueryModule {
        return decodeQueryModule(reader);
    },

    /** Return this value as JSON. */
    toJson(value: QueryModule): Json {
        return toJsonQueryModule(value);
    },

    /** Return one QueryModule from one JSON value. */
    fromJson(value: Json): QueryModule {
        return fromJsonQueryModule(value);
    },
};

/** Encode one QueryModule. */
export function encodeQueryModule(writer: BinaryWriter, value: QueryModule): void {
    encodeModuleId(writer, value.moduleId);
    encodeProfileId(writer, value.profileId);
}

/** Decode one QueryModule. */
export function decodeQueryModule(reader: BinaryReader): QueryModule {
    const moduleId = decodeModuleId(reader);
    const profileId = decodeProfileId(reader);

    return {
        moduleId,
        profileId,
    };
}

/** Return one JSON value for one QueryModule. */
export function toJsonQueryModule(value: QueryModule): Json {
    return {
        moduleId: toJsonModuleId(value.moduleId),
        profileId: toJsonProfileId(value.profileId),
    };
}

/** Return one QueryModule from one JSON value. */
export function fromJsonQueryModule(value: Json): QueryModule {
    const object = jsonObject(value);

    return {
        moduleId: fromJsonModuleId(jsonField(object, "moduleId")),
        profileId: fromJsonProfileId(jsonField(object, "profileId")),
    };
}

/** One byte position in a module source file. */
export type QueryPosition = {
    /** The queried module profile. */
    readonly module: QueryModule;
    /** The source file. */
    readonly fileId: FileId;
    /** The byte offset in the source file. */
    readonly offset: number;
};

export const QueryPosition = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: QueryPosition): void {
        encodeQueryPosition(writer, value);
    },

    /** Decode one QueryPosition. */
    decode(reader: BinaryReader): QueryPosition {
        return decodeQueryPosition(reader);
    },

    /** Return this value as JSON. */
    toJson(value: QueryPosition): Json {
        return toJsonQueryPosition(value);
    },

    /** Return one QueryPosition from one JSON value. */
    fromJson(value: Json): QueryPosition {
        return fromJsonQueryPosition(value);
    },
};

/** Encode one QueryPosition. */
export function encodeQueryPosition(writer: BinaryWriter, value: QueryPosition): void {
    encodeQueryModule(writer, value.module);
    encodeFileId(writer, value.fileId);
    writer.writeUnsigned(value.offset);
}

/** Decode one QueryPosition. */
export function decodeQueryPosition(reader: BinaryReader): QueryPosition {
    const module_ = decodeQueryModule(reader);
    const fileId = decodeFileId(reader);
    const offset = reader.readNumber();

    return {
        module: module_,
        fileId,
        offset,
    };
}

/** Return one JSON value for one QueryPosition. */
export function toJsonQueryPosition(value: QueryPosition): Json {
    return {
        module: toJsonQueryModule(value.module),
        fileId: toJsonFileId(value.fileId),
        offset: value.offset,
    };
}

/** Return one QueryPosition from one JSON value. */
export function fromJsonQueryPosition(value: Json): QueryPosition {
    const object = jsonObject(value);

    return {
        module: fromJsonQueryModule(jsonField(object, "module")),
        fileId: fromJsonFileId(jsonField(object, "fileId")),
        offset: jsonInteger(jsonField(object, "offset")),
    };
}

/** One source range in a module. */
export type QueryRange = {
    /** The queried module profile. */
    readonly module: QueryModule;
    /** The source range. */
    readonly span: Span;
};

export const QueryRange = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: QueryRange): void {
        encodeQueryRange(writer, value);
    },

    /** Decode one QueryRange. */
    decode(reader: BinaryReader): QueryRange {
        return decodeQueryRange(reader);
    },

    /** Return this value as JSON. */
    toJson(value: QueryRange): Json {
        return toJsonQueryRange(value);
    },

    /** Return one QueryRange from one JSON value. */
    fromJson(value: Json): QueryRange {
        return fromJsonQueryRange(value);
    },
};

/** Encode one QueryRange. */
export function encodeQueryRange(writer: BinaryWriter, value: QueryRange): void {
    encodeQueryModule(writer, value.module);
    encodeSpan(writer, value.span);
}

/** Decode one QueryRange. */
export function decodeQueryRange(reader: BinaryReader): QueryRange {
    const module_ = decodeQueryModule(reader);
    const span = decodeSpan(reader);

    return {
        module: module_,
        span,
    };
}

/** Return one JSON value for one QueryRange. */
export function toJsonQueryRange(value: QueryRange): Json {
    return {
        module: toJsonQueryModule(value.module),
        span: toJsonSpan(value.span),
    };
}

/** Return one QueryRange from one JSON value. */
export function fromJsonQueryRange(value: Json): QueryRange {
    const object = jsonObject(value);

    return {
        module: fromJsonQueryModule(jsonField(object, "module")),
        span: fromJsonSpan(jsonField(object, "span")),
    };
}

/** One source-backed query target. */
export type QueryTarget = {
    /** The target module profile. */
    readonly module: QueryModule;
    /** The full source range. */
    readonly span: Span;
    /** The primary selection range. */
    readonly selectionSpan?: Span;
    /** The target symbol when known. */
    readonly symbolId?: GlobalSymbolId;
    /** The target node when known. */
    readonly nodeId?: GlobalNodeIdAny;
};

export const QueryTarget = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: QueryTarget): void {
        encodeQueryTarget(writer, value);
    },

    /** Decode one QueryTarget. */
    decode(reader: BinaryReader): QueryTarget {
        return decodeQueryTarget(reader);
    },

    /** Return this value as JSON. */
    toJson(value: QueryTarget): Json {
        return toJsonQueryTarget(value);
    },

    /** Return one QueryTarget from one JSON value. */
    fromJson(value: Json): QueryTarget {
        return fromJsonQueryTarget(value);
    },
};

/** Encode one QueryTarget. */
export function encodeQueryTarget(writer: BinaryWriter, value: QueryTarget): void {
    encodeQueryModule(writer, value.module);
    encodeSpan(writer, value.span);
    writer.writeOption(value.selectionSpan, (value2) => {
        encodeSpan(writer, value2);
    });
    writer.writeOption(value.symbolId, (value3) => {
        encodeGlobalSymbolId(writer, value3);
    });
    writer.writeOption(value.nodeId, (value4) => {
        encodeGlobalNodeIdAny(writer, value4);
    });
}

/** Decode one QueryTarget. */
export function decodeQueryTarget(reader: BinaryReader): QueryTarget {
    const module_ = decodeQueryModule(reader);
    const span = decodeSpan(reader);
    const selectionSpan = reader.readOption(() => decodeSpan(reader));
    const symbolId = reader.readOption(() => decodeGlobalSymbolId(reader));
    const nodeId = reader.readOption(() => decodeGlobalNodeIdAny(reader));

    return {
        module: module_,
        span,
        ...(selectionSpan === undefined ? {} : { selectionSpan }),
        ...(symbolId === undefined ? {} : { symbolId }),
        ...(nodeId === undefined ? {} : { nodeId }),
    };
}

/** Return one JSON value for one QueryTarget. */
export function toJsonQueryTarget(value: QueryTarget): Json {
    return {
        module: toJsonQueryModule(value.module),
        span: toJsonSpan(value.span),
        ...(value.selectionSpan === undefined ? {} : { selectionSpan: toJsonSpan(value.selectionSpan) }),
        ...(value.symbolId === undefined ? {} : { symbolId: toJsonGlobalSymbolId(value.symbolId) }),
        ...(value.nodeId === undefined ? {} : { nodeId: toJsonGlobalNodeIdAny(value.nodeId) }),
    };
}

/** Return one QueryTarget from one JSON value. */
export function fromJsonQueryTarget(value: Json): QueryTarget {
    const object = jsonObject(value);

    return {
        module: fromJsonQueryModule(jsonField(object, "module")),
        span: fromJsonSpan(jsonField(object, "span")),
        selectionSpan: jsonOptional(object, "selectionSpan", (value) => fromJsonSpan(value)),
        symbolId: jsonOptional(object, "symbolId", (value) => fromJsonGlobalSymbolId(value)),
        nodeId: jsonOptional(object, "nodeId", (value) => fromJsonGlobalNodeIdAny(value)),
    };
}

/** Query text in display formats understood by clients. */
export type QueryText = {
    /** Plain text. */
    readonly plain?: string;
    /** Markdown text. */
    readonly markdown?: string;
};

export const QueryText = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: QueryText): void {
        encodeQueryText(writer, value);
    },

    /** Decode one QueryText. */
    decode(reader: BinaryReader): QueryText {
        return decodeQueryText(reader);
    },

    /** Return this value as JSON. */
    toJson(value: QueryText): Json {
        return toJsonQueryText(value);
    },

    /** Return one QueryText from one JSON value. */
    fromJson(value: Json): QueryText {
        return fromJsonQueryText(value);
    },
};

/** Encode one QueryText. */
export function encodeQueryText(writer: BinaryWriter, value: QueryText): void {
    writer.writeOption(value.plain, (value0) => {
        writer.writeString(value0);
    });
    writer.writeOption(value.markdown, (value1) => {
        writer.writeString(value1);
    });
}

/** Decode one QueryText. */
export function decodeQueryText(reader: BinaryReader): QueryText {
    const plain = reader.readOption(() => reader.readString());
    const markdown = reader.readOption(() => reader.readString());

    return {
        ...(plain === undefined ? {} : { plain }),
        ...(markdown === undefined ? {} : { markdown }),
    };
}

/** Return one JSON value for one QueryText. */
export function toJsonQueryText(value: QueryText): Json {
    return {
        ...(value.plain === undefined ? {} : { plain: value.plain }),
        ...(value.markdown === undefined ? {} : { markdown: value.markdown }),
    };
}

/** Return one QueryText from one JSON value. */
export function fromJsonQueryText(value: Json): QueryText {
    const object = jsonObject(value);

    return {
        plain: jsonOptional(object, "plain", (value) => jsonString(value)),
        markdown: jsonOptional(object, "markdown", (value) => jsonString(value)),
    };
}
