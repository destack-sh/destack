/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, jsonArray, jsonField, jsonInteger, jsonObject, jsonOptional, jsonString } from "../../../protocol/serde.js";
import type { Module } from "../protocol/target.js";
import type { FileId } from "../../source/file/model/file.js";
import { decodeModule, encodeModule, fromJsonModule, toJsonModule } from "../protocol/target.js";
import { decodeFileId, encodeFileId, fromJsonFileId, toJsonFileId } from "../../source/file/model/file.js";

/** A foldable range in source code. */
export type FoldingRange = {
    /** Start line. */
    readonly startLine: number;
    /** End line. */
    readonly endLine: number;
    /** Optional start character. */
    readonly startCharacter?: number;
    /** Optional end character. */
    readonly endCharacter?: number;
    /** The kind of folding range. */
    readonly kind?: FoldingRangeKind;
    /** Text to show when collapsed. */
    readonly collapsedText?: string;
};

export const FoldingRange = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: FoldingRange): void {
        encodeFoldingRange(writer, value);
    },

    /** Decode one FoldingRange. */
    decode(reader: BinaryReader): FoldingRange {
        return decodeFoldingRange(reader);
    },

    /** Return this value as JSON. */
    toJson(value: FoldingRange): Json {
        return toJsonFoldingRange(value);
    },

    /** Return one FoldingRange from one JSON value. */
    fromJson(value: Json): FoldingRange {
        return fromJsonFoldingRange(value);
    },
};

/** Encode one FoldingRange. */
export function encodeFoldingRange(writer: BinaryWriter, value: FoldingRange): void {
    writer.writeUnsigned(value.startLine);
    writer.writeUnsigned(value.endLine);
    writer.writeOption(value.startCharacter, (value2) => {
        writer.writeUnsigned(value2);
    });
    writer.writeOption(value.endCharacter, (value3) => {
        writer.writeUnsigned(value3);
    });
    writer.writeOption(value.kind, (value4) => {
        encodeFoldingRangeKind(writer, value4);
    });
    writer.writeOption(value.collapsedText, (value5) => {
        writer.writeString(value5);
    });
}

/** Decode one FoldingRange. */
export function decodeFoldingRange(reader: BinaryReader): FoldingRange {
    const startLine = reader.readNumber();
    const endLine = reader.readNumber();
    const startCharacter = reader.readOption(() => reader.readNumber());
    const endCharacter = reader.readOption(() => reader.readNumber());
    const kind = reader.readOption(() => decodeFoldingRangeKind(reader));
    const collapsedText = reader.readOption(() => reader.readString());

    return {
        startLine,
        endLine,
        ...(startCharacter === undefined ? {} : { startCharacter }),
        ...(endCharacter === undefined ? {} : { endCharacter }),
        ...(kind === undefined ? {} : { kind }),
        ...(collapsedText === undefined ? {} : { collapsedText }),
    };
}

/** Return one JSON value for one FoldingRange. */
export function toJsonFoldingRange(value: FoldingRange): Json {
    return {
        startLine: value.startLine,
        endLine: value.endLine,
        ...(value.startCharacter === undefined ? {} : { startCharacter: value.startCharacter }),
        ...(value.endCharacter === undefined ? {} : { endCharacter: value.endCharacter }),
        ...(value.kind === undefined ? {} : { kind: toJsonFoldingRangeKind(value.kind) }),
        ...(value.collapsedText === undefined ? {} : { collapsedText: value.collapsedText }),
    };
}

/** Return one FoldingRange from one JSON value. */
export function fromJsonFoldingRange(value: Json): FoldingRange {
    const object = jsonObject(value);

    return {
        startLine: jsonInteger(jsonField(object, "startLine")),
        endLine: jsonInteger(jsonField(object, "endLine")),
        startCharacter: jsonOptional(object, "startCharacter", (value) => jsonInteger(value)),
        endCharacter: jsonOptional(object, "endCharacter", (value) => jsonInteger(value)),
        kind: jsonOptional(object, "kind", (value) => fromJsonFoldingRangeKind(value)),
        collapsedText: jsonOptional(object, "collapsedText", (value) => jsonString(value)),
    };
}

/** Kind of folding range. */
export type FoldingRangeKind = "comment" | "imports" | "region";

export const FoldingRangeKind = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: FoldingRangeKind): void {
        encodeFoldingRangeKind(writer, value);
    },

    /** Decode one FoldingRangeKind. */
    decode(reader: BinaryReader): FoldingRangeKind {
        return decodeFoldingRangeKind(reader);
    },

    /** Return this value as JSON. */
    toJson(value: FoldingRangeKind): Json {
        return toJsonFoldingRangeKind(value);
    },

    /** Return one FoldingRangeKind from one JSON value. */
    fromJson(value: Json): FoldingRangeKind {
        return fromJsonFoldingRangeKind(value);
    },
};

/** Encode one FoldingRangeKind. */
export function encodeFoldingRangeKind(writer: BinaryWriter, value: FoldingRangeKind): void {
    switch (value) {
        case "comment":
            writer.writeUnsigned(0);
            return;
        case "imports":
            writer.writeUnsigned(1);
            return;
        case "region":
            writer.writeUnsigned(2);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one FoldingRangeKind. */
export function decodeFoldingRangeKind(reader: BinaryReader): FoldingRangeKind {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "comment";
        case 1:
            return "imports";
        case 2:
            return "region";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one FoldingRangeKind. */
export function toJsonFoldingRangeKind(value: FoldingRangeKind): Json {
    return value;
}

/** Return one FoldingRangeKind from one JSON value. */
export function fromJsonFoldingRangeKind(value: Json): FoldingRangeKind {
    const variant = jsonString(value);

    switch (variant) {
        case "comment":
            return "comment";
        case "imports":
            return "imports";
        case "region":
            return "region";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** A folding ranges request. */
export type FoldingRangesRequest = {
    /** The queried module profile. */
    readonly module: Module;
    /** The queried source file. */
    readonly fileId: FileId;
};

export const FoldingRangesRequest = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: FoldingRangesRequest): void {
        encodeFoldingRangesRequest(writer, value);
    },

    /** Decode one FoldingRangesRequest. */
    decode(reader: BinaryReader): FoldingRangesRequest {
        return decodeFoldingRangesRequest(reader);
    },

    /** Return this value as JSON. */
    toJson(value: FoldingRangesRequest): Json {
        return toJsonFoldingRangesRequest(value);
    },

    /** Return one FoldingRangesRequest from one JSON value. */
    fromJson(value: Json): FoldingRangesRequest {
        return fromJsonFoldingRangesRequest(value);
    },
};

/** Encode one FoldingRangesRequest. */
export function encodeFoldingRangesRequest(writer: BinaryWriter, value: FoldingRangesRequest): void {
    encodeModule(writer, value.module);
    encodeFileId(writer, value.fileId);
}

/** Decode one FoldingRangesRequest. */
export function decodeFoldingRangesRequest(reader: BinaryReader): FoldingRangesRequest {
    const module_ = decodeModule(reader);
    const fileId = decodeFileId(reader);

    return {
        module: module_,
        fileId,
    };
}

/** Return one JSON value for one FoldingRangesRequest. */
export function toJsonFoldingRangesRequest(value: FoldingRangesRequest): Json {
    return {
        module: toJsonModule(value.module),
        fileId: toJsonFileId(value.fileId),
    };
}

/** Return one FoldingRangesRequest from one JSON value. */
export function fromJsonFoldingRangesRequest(value: Json): FoldingRangesRequest {
    const object = jsonObject(value);

    return {
        module: fromJsonModule(jsonField(object, "module")),
        fileId: fromJsonFileId(jsonField(object, "fileId")),
    };
}

/** A folding ranges response. */
export type FoldingRangesResponse = {
    /** Folding ranges. */
    readonly ranges: ReadonlyArray<FoldingRange>;
};

export const FoldingRangesResponse = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: FoldingRangesResponse): void {
        encodeFoldingRangesResponse(writer, value);
    },

    /** Decode one FoldingRangesResponse. */
    decode(reader: BinaryReader): FoldingRangesResponse {
        return decodeFoldingRangesResponse(reader);
    },

    /** Return this value as JSON. */
    toJson(value: FoldingRangesResponse): Json {
        return toJsonFoldingRangesResponse(value);
    },

    /** Return one FoldingRangesResponse from one JSON value. */
    fromJson(value: Json): FoldingRangesResponse {
        return fromJsonFoldingRangesResponse(value);
    },
};

/** Encode one FoldingRangesResponse. */
export function encodeFoldingRangesResponse(writer: BinaryWriter, value: FoldingRangesResponse): void {
    writer.writeUnsigned(value.ranges.length);
    for (const item0 of value.ranges) {
        encodeFoldingRange(writer, item0);
    }
}

/** Decode one FoldingRangesResponse. */
export function decodeFoldingRangesResponse(reader: BinaryReader): FoldingRangesResponse {
    const ranges = (() => { const length0 = reader.readNumber(); const items0: Array<FoldingRange> = []; for (let index = 0; index < length0; index += 1) { items0.push(decodeFoldingRange(reader)); } return items0; })();

    return {
        ranges,
    };
}

/** Return one JSON value for one FoldingRangesResponse. */
export function toJsonFoldingRangesResponse(value: FoldingRangesResponse): Json {
    return {
        ranges: value.ranges.map((item0) => toJsonFoldingRange(item0)),
    };
}

/** Return one FoldingRangesResponse from one JSON value. */
export function fromJsonFoldingRangesResponse(value: Json): FoldingRangesResponse {
    const object = jsonObject(value);

    return {
        ranges: jsonArray(jsonField(object, "ranges")).map((item0) => fromJsonFoldingRange(item0)),
    };
}
