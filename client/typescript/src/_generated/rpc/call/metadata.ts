/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, bytesFromJson, bytesToJson, jsonArray, jsonField, jsonObject, jsonString } from "../../../protocol/serde.js";

/** RPC metadata attached to one request or response. */
export type Metadata = {
    /** Ordered metadata entries, including repeated names. */
    readonly entries: ReadonlyArray<readonly [string, Uint8Array | readonly number[]]>;
};

export const Metadata = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: Metadata): void {
        encodeMetadata(writer, value);
    },

    /** Decode one Metadata. */
    decode(reader: BinaryReader): Metadata {
        return decodeMetadata(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Metadata): Json {
        return toJsonMetadata(value);
    },

    /** Return one Metadata from one JSON value. */
    fromJson(value: Json): Metadata {
        return fromJsonMetadata(value);
    },
};

/** Encode one Metadata. */
export function encodeMetadata(writer: BinaryWriter, value: Metadata): void {
    writer.writeUnsigned(value.entries.length);
    for (const item0 of value.entries) {
        writer.writeString(item0[0]);
        writer.writeByteSlice(item0[1]);
    }
}

/** Decode one Metadata. */
export function decodeMetadata(reader: BinaryReader): Metadata {
    const entries = (() => { const length0 = reader.readNumber(); const items0: Array<readonly [string, Uint8Array | readonly number[]]> = []; for (let index = 0; index < length0; index += 1) { items0.push([reader.readString(), reader.readByteSlice()] as const); } return items0; })();

    return {
        entries,
    };
}

/** Return one JSON value for one Metadata. */
export function toJsonMetadata(value: Metadata): Json {
    return {
        entries: value.entries.map((item0) => [item0[0], bytesToJson(item0[1])]),
    };
}

/** Return one Metadata from one JSON value. */
export function fromJsonMetadata(value: Json): Metadata {
    const object = jsonObject(value);

    return {
        entries: jsonArray(jsonField(object, "entries")).map((item0) => (() => { const items = jsonArray(item0); if (items.length !== 2) { throw new SerdeError(`expected JSON tuple length 2: ${items.length}`); } return [jsonString(items[0]), bytesFromJson(items[1])] as const; })()),
    };
}
