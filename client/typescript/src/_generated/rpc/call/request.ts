/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, jsonField, jsonObject } from "../../../protocol/serde.js";
import type { Metadata } from "./metadata.js";
import type { Payload } from "../protocol/payload.js";
import { decodeMetadata, encodeMetadata, fromJsonMetadata, toJsonMetadata } from "./metadata.js";
import { decodePayload, encodePayload, fromJsonPayload, toJsonPayload } from "../protocol/payload.js";

/** One RPC request and its call options. */
export type Request = {
    /** Request metadata. */
    readonly metadata: Metadata;
    /** Request value. */
    readonly value: Payload;
};

export const Request = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: Request): void {
        encodeRequest(writer, value);
    },

    /** Decode one Request. */
    decode(reader: BinaryReader): Request {
        return decodeRequest(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Request): Json {
        return toJsonRequest(value);
    },

    /** Return one Request from one JSON value. */
    fromJson(value: Json): Request {
        return fromJsonRequest(value);
    },
};

/** Encode one Request. */
export function encodeRequest(writer: BinaryWriter, value: Request): void {
    encodeMetadata(writer, value.metadata);
    encodePayload(writer, value.value);
}

/** Decode one Request. */
export function decodeRequest(reader: BinaryReader): Request {
    const metadata = decodeMetadata(reader);
    const value = decodePayload(reader);

    return {
        metadata,
        value,
    };
}

/** Return one JSON value for one Request. */
export function toJsonRequest(value: Request): Json {
    return {
        metadata: toJsonMetadata(value.metadata),
        value: toJsonPayload(value.value),
    };
}

/** Return one Request from one JSON value. */
export function fromJsonRequest(value: Json): Request {
    const object = jsonObject(value);

    return {
        metadata: fromJsonMetadata(jsonField(object, "metadata")),
        value: fromJsonPayload(jsonField(object, "value")),
    };
}
