/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, jsonBigint } from "../../../protocol/serde.js";

/** Caller-scoped RPC call identifier. */
export type CallId = bigint;

export const CallId = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: CallId): void {
        encodeCallId(writer, value);
    },

    /** Decode one CallId. */
    decode(reader: BinaryReader): CallId {
        return decodeCallId(reader);
    },

    /** Return this value as JSON. */
    toJson(value: CallId): Json {
        return toJsonCallId(value);
    },

    /** Return one CallId from one JSON value. */
    fromJson(value: Json): CallId {
        return fromJsonCallId(value);
    },
};

/** Encode one CallId. */
export function encodeCallId(writer: BinaryWriter, value: CallId): void {
    writer.writeUnsigned(value);
}

/** Decode one CallId. */
export function decodeCallId(reader: BinaryReader): CallId {
    return reader.readUnsigned();
}

/** Return one JSON value for one CallId. */
export function toJsonCallId(value: CallId): Json {
    return value.toString();
}

/** Return one CallId from one JSON value. */
export function fromJsonCallId(value: Json): CallId {
    return jsonBigint(value);
}
