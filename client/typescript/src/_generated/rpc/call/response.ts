/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, jsonField, jsonObject } from "../../../protocol/serde.js";
import type { Metadata } from "./metadata.js";
import type { Payload } from "../protocol/payload.js";
import { decodeMetadata, encodeMetadata, fromJsonMetadata, toJsonMetadata } from "./metadata.js";
import { decodePayload, encodePayload, fromJsonPayload, toJsonPayload } from "../protocol/payload.js";

/** One successful RPC response and its metadata. */
export type Response = {
    /** Response metadata. */
    readonly metadata: Metadata;
    /** Response value. */
    readonly value: Payload;
};

export const Response = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: Response): void {
        encodeResponse(writer, value);
    },

    /** Decode one Response. */
    decode(reader: BinaryReader): Response {
        return decodeResponse(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Response): Json {
        return toJsonResponse(value);
    },

    /** Return one Response from one JSON value. */
    fromJson(value: Json): Response {
        return fromJsonResponse(value);
    },
};

/** Encode one Response. */
export function encodeResponse(writer: BinaryWriter, value: Response): void {
    encodeMetadata(writer, value.metadata);
    encodePayload(writer, value.value);
}

/** Decode one Response. */
export function decodeResponse(reader: BinaryReader): Response {
    const metadata = decodeMetadata(reader);
    const value = decodePayload(reader);

    return {
        metadata,
        value,
    };
}

/** Return one JSON value for one Response. */
export function toJsonResponse(value: Response): Json {
    return {
        metadata: toJsonMetadata(value.metadata),
        value: toJsonPayload(value.value),
    };
}

/** Return one Response from one JSON value. */
export function fromJsonResponse(value: Json): Response {
    const object = jsonObject(value);

    return {
        metadata: fromJsonMetadata(jsonField(object, "metadata")),
        value: fromJsonPayload(jsonField(object, "value")),
    };
}
