/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, jsonField, jsonInteger, jsonObject, jsonString } from "../../../protocol/serde.js";
import type { CallId } from "../call/id.js";
import type { Request } from "../call/request.js";
import type { Response } from "../call/response.js";
import type { Payload } from "./payload.js";
import type { PayloadChunk } from "./payload.js";
import type { Status } from "./status.js";
import type { MethodId } from "../service/method.js";
import type { ServiceId } from "../service/service.js";
import { decodeCallId, encodeCallId, fromJsonCallId, toJsonCallId } from "../call/id.js";
import { decodeRequest, encodeRequest, fromJsonRequest, toJsonRequest } from "../call/request.js";
import { decodeResponse, encodeResponse, fromJsonResponse, toJsonResponse } from "../call/response.js";
import { decodePayload, encodePayload, fromJsonPayload, toJsonPayload } from "./payload.js";
import { decodePayloadChunk, encodePayloadChunk, fromJsonPayloadChunk, toJsonPayloadChunk } from "./payload.js";
import { decodeStatus, encodeStatus, fromJsonStatus, toJsonStatus } from "./status.js";
import { decodeMethodId, encodeMethodId, fromJsonMethodId, toJsonMethodId } from "../service/method.js";
import { decodeServiceId, encodeServiceId, fromJsonServiceId, toJsonServiceId } from "../service/service.js";

/** Message opening one RPC call. */
export type CallStart = {
    /** Caller-scoped call identifier. */
    readonly call: CallId;
    /** Called service. */
    readonly service: ServiceId;
    /** Called method. */
    readonly method: MethodId;
    /** Encoded request. */
    readonly request: Request;
};

export const CallStart = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: CallStart): void {
        encodeCallStart(writer, value);
    },

    /** Decode one CallStart. */
    decode(reader: BinaryReader): CallStart {
        return decodeCallStart(reader);
    },

    /** Return this value as JSON. */
    toJson(value: CallStart): Json {
        return toJsonCallStart(value);
    },

    /** Return one CallStart from one JSON value. */
    fromJson(value: Json): CallStart {
        return fromJsonCallStart(value);
    },
};

/** Encode one CallStart. */
export function encodeCallStart(writer: BinaryWriter, value: CallStart): void {
    encodeCallId(writer, value.call);
    encodeServiceId(writer, value.service);
    encodeMethodId(writer, value.method);
    encodeRequest(writer, value.request);
}

/** Decode one CallStart. */
export function decodeCallStart(reader: BinaryReader): CallStart {
    const call = decodeCallId(reader);
    const service = decodeServiceId(reader);
    const method = decodeMethodId(reader);
    const request = decodeRequest(reader);

    return {
        call,
        service,
        method,
        request,
    };
}

/** Return one JSON value for one CallStart. */
export function toJsonCallStart(value: CallStart): Json {
    return {
        call: toJsonCallId(value.call),
        service: toJsonServiceId(value.service),
        method: toJsonMethodId(value.method),
        request: toJsonRequest(value.request),
    };
}

/** Return one CallStart from one JSON value. */
export function fromJsonCallStart(value: Json): CallStart {
    const object = jsonObject(value);

    return {
        call: fromJsonCallId(jsonField(object, "call")),
        service: fromJsonServiceId(jsonField(object, "service")),
        method: fromJsonMethodId(jsonField(object, "method")),
        request: fromJsonRequest(jsonField(object, "request")),
    };
}

/** Message carrying one stream item. */
export type StreamItem = {
    /** Owning call. */
    readonly call: CallId;
    /** Encoded stream item. */
    readonly payload: Payload;
};

export const StreamItem = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: StreamItem): void {
        encodeStreamItem(writer, value);
    },

    /** Decode one StreamItem. */
    decode(reader: BinaryReader): StreamItem {
        return decodeStreamItem(reader);
    },

    /** Return this value as JSON. */
    toJson(value: StreamItem): Json {
        return toJsonStreamItem(value);
    },

    /** Return one StreamItem from one JSON value. */
    fromJson(value: Json): StreamItem {
        return fromJsonStreamItem(value);
    },
};

/** Encode one StreamItem. */
export function encodeStreamItem(writer: BinaryWriter, value: StreamItem): void {
    encodeCallId(writer, value.call);
    encodePayload(writer, value.payload);
}

/** Decode one StreamItem. */
export function decodeStreamItem(reader: BinaryReader): StreamItem {
    const call = decodeCallId(reader);
    const payload = decodePayload(reader);

    return {
        call,
        payload,
    };
}

/** Return one JSON value for one StreamItem. */
export function toJsonStreamItem(value: StreamItem): Json {
    return {
        call: toJsonCallId(value.call),
        payload: toJsonPayload(value.payload),
    };
}

/** Return one StreamItem from one JSON value. */
export function fromJsonStreamItem(value: Json): StreamItem {
    const object = jsonObject(value);

    return {
        call: fromJsonCallId(jsonField(object, "call")),
        payload: fromJsonPayload(jsonField(object, "payload")),
    };
}

/** Message closing the caller-to-callee stream. */
export type StreamClose = {
    /** Owning call. */
    readonly call: CallId;
};

export const StreamClose = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: StreamClose): void {
        encodeStreamClose(writer, value);
    },

    /** Decode one StreamClose. */
    decode(reader: BinaryReader): StreamClose {
        return decodeStreamClose(reader);
    },

    /** Return this value as JSON. */
    toJson(value: StreamClose): Json {
        return toJsonStreamClose(value);
    },

    /** Return one StreamClose from one JSON value. */
    fromJson(value: Json): StreamClose {
        return fromJsonStreamClose(value);
    },
};

/** Encode one StreamClose. */
export function encodeStreamClose(writer: BinaryWriter, value: StreamClose): void {
    encodeCallId(writer, value.call);
}

/** Decode one StreamClose. */
export function decodeStreamClose(reader: BinaryReader): StreamClose {
    const call = decodeCallId(reader);

    return {
        call,
    };
}

/** Return one JSON value for one StreamClose. */
export function toJsonStreamClose(value: StreamClose): Json {
    return {
        call: toJsonCallId(value.call),
    };
}

/** Return one StreamClose from one JSON value. */
export function fromJsonStreamClose(value: Json): StreamClose {
    const object = jsonObject(value);

    return {
        call: fromJsonCallId(jsonField(object, "call")),
    };
}

/** Message increasing one stream's send window. */
export type WindowUpdate = {
    /** Owning call. */
    readonly call: CallId;
    /** Additional permitted items. */
    readonly items: number;
};

export const WindowUpdate = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: WindowUpdate): void {
        encodeWindowUpdate(writer, value);
    },

    /** Decode one WindowUpdate. */
    decode(reader: BinaryReader): WindowUpdate {
        return decodeWindowUpdate(reader);
    },

    /** Return this value as JSON. */
    toJson(value: WindowUpdate): Json {
        return toJsonWindowUpdate(value);
    },

    /** Return one WindowUpdate from one JSON value. */
    fromJson(value: Json): WindowUpdate {
        return fromJsonWindowUpdate(value);
    },
};

/** Encode one WindowUpdate. */
export function encodeWindowUpdate(writer: BinaryWriter, value: WindowUpdate): void {
    encodeCallId(writer, value.call);
    writer.writeUnsigned(value.items);
}

/** Decode one WindowUpdate. */
export function decodeWindowUpdate(reader: BinaryReader): WindowUpdate {
    const call = decodeCallId(reader);
    const items = reader.readNumber();

    return {
        call,
        items,
    };
}

/** Return one JSON value for one WindowUpdate. */
export function toJsonWindowUpdate(value: WindowUpdate): Json {
    return {
        call: toJsonCallId(value.call),
        items: value.items,
    };
}

/** Return one WindowUpdate from one JSON value. */
export function fromJsonWindowUpdate(value: Json): WindowUpdate {
    const object = jsonObject(value);

    return {
        call: fromJsonCallId(jsonField(object, "call")),
        items: jsonInteger(jsonField(object, "items")),
    };
}

/** Terminal completion of one RPC call. */
export type Completion =
    /** Complete one call successfully. */
    | {
          readonly kind: "response";
          /** Completed call. */
          readonly call: CallId;
          /** Successful response. */
          readonly response: Response;
      }
    /** Complete one call unsuccessfully. */
    | {
          readonly kind: "status";
          /** Completed call. */
          readonly call: CallId;
          /** Terminal failure. */
          readonly status: Status;
      }
;

export const Completion = {
    /** Complete one call successfully. */
    response(call: CallId, response: Response): Completion {
        return { kind: "response", call, response };
    },

    /** Complete one call unsuccessfully. */
    status(call: CallId, status: Status): Completion {
        return { kind: "status", call, status };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: Completion): void {
        encodeCompletion(writer, value);
    },

    /** Decode one Completion. */
    decode(reader: BinaryReader): Completion {
        return decodeCompletion(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Completion): Json {
        return toJsonCompletion(value);
    },

    /** Return one Completion from one JSON value. */
    fromJson(value: Json): Completion {
        return fromJsonCompletion(value);
    },
};

/** Encode one Completion. */
export function encodeCompletion(writer: BinaryWriter, value: Completion): void {
    switch (value.kind) {
        case "response":
            writer.writeUnsigned(0);
            encodeCallId(writer, value.call);
            encodeResponse(writer, value.response);
            return;
        case "status":
            writer.writeUnsigned(1);
            encodeCallId(writer, value.call);
            encodeStatus(writer, value.status);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one Completion. */
export function decodeCompletion(reader: BinaryReader): Completion {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const call = decodeCallId(reader);
            const response = decodeResponse(reader);

            return {
                kind: "response",
                call,
                response,
            };
        }
        case 1: {
            const call = decodeCallId(reader);
            const status = decodeStatus(reader);

            return {
                kind: "status",
                call,
                status,
            };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one Completion. */
export function toJsonCompletion(value: Completion): Json {
    switch (value.kind) {
        case "response":
            return {
                kind: "response",
                call: toJsonCallId(value.call),
                response: toJsonResponse(value.response),
            };
        case "status":
            return {
                kind: "status",
                call: toJsonCallId(value.call),
                status: toJsonStatus(value.status),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one Completion from one JSON value. */
export function fromJsonCompletion(value: Json): Completion {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "response":
            return {
                kind,
                call: fromJsonCallId(jsonField(object, "call")),
                response: fromJsonResponse(jsonField(object, "response")),
            };
        case "status":
            return {
                kind,
                call: fromJsonCallId(jsonField(object, "call")),
                status: fromJsonStatus(jsonField(object, "status")),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** Message requesting cancellation of one call. */
export type Cancel = {
    /** Canceled call. */
    readonly call: CallId;
};

export const Cancel = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: Cancel): void {
        encodeCancel(writer, value);
    },

    /** Decode one Cancel. */
    decode(reader: BinaryReader): Cancel {
        return decodeCancel(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Cancel): Json {
        return toJsonCancel(value);
    },

    /** Return one Cancel from one JSON value. */
    fromJson(value: Json): Cancel {
        return fromJsonCancel(value);
    },
};

/** Encode one Cancel. */
export function encodeCancel(writer: BinaryWriter, value: Cancel): void {
    encodeCallId(writer, value.call);
}

/** Decode one Cancel. */
export function decodeCancel(reader: BinaryReader): Cancel {
    const call = decodeCallId(reader);

    return {
        call,
    };
}

/** Return one JSON value for one Cancel. */
export function toJsonCancel(value: Cancel): Json {
    return {
        call: toJsonCallId(value.call),
    };
}

/** Return one Cancel from one JSON value. */
export function fromJsonCancel(value: Json): Cancel {
    const object = jsonObject(value);

    return {
        call: fromJsonCallId(jsonField(object, "call")),
    };
}

/** One encoded RPC message. */
export type Message =
    /** Open one call. */
    | {
          readonly kind: "start";
          readonly start: CallStart;
      }
    /** Transfer one stream item. */
    | {
          readonly kind: "item";
          readonly item: StreamItem;
      }
    /** Close the caller-to-callee stream. */
    | {
          readonly kind: "close";
          readonly close: StreamClose;
      }
    /** Increase one stream's send window. */
    | {
          readonly kind: "window";
          readonly window: WindowUpdate;
      }
    /** Complete one call. */
    | {
          readonly kind: "complete";
          readonly complete: Completion;
      }
    /** Request cancellation of one call. */
    | {
          readonly kind: "cancel";
          readonly cancel: Cancel;
      }
    /** Transfer contiguous bytes for one deferred payload. */
    | {
          readonly kind: "chunk";
          readonly chunk: PayloadChunk;
      }
;

export const Message = {
    /** Open one call. */
    start(start: CallStart): Message {
        return { kind: "start", start };
    },

    /** Transfer one stream item. */
    item(item: StreamItem): Message {
        return { kind: "item", item };
    },

    /** Close the caller-to-callee stream. */
    close(close: StreamClose): Message {
        return { kind: "close", close };
    },

    /** Increase one stream's send window. */
    window(window: WindowUpdate): Message {
        return { kind: "window", window };
    },

    /** Complete one call. */
    complete(complete: Completion): Message {
        return { kind: "complete", complete };
    },

    /** Request cancellation of one call. */
    cancel(cancel: Cancel): Message {
        return { kind: "cancel", cancel };
    },

    /** Transfer contiguous bytes for one deferred payload. */
    chunk(chunk: PayloadChunk): Message {
        return { kind: "chunk", chunk };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: Message): void {
        encodeMessage(writer, value);
    },

    /** Decode one Message. */
    decode(reader: BinaryReader): Message {
        return decodeMessage(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Message): Json {
        return toJsonMessage(value);
    },

    /** Return one Message from one JSON value. */
    fromJson(value: Json): Message {
        return fromJsonMessage(value);
    },
};

/** Encode one Message. */
export function encodeMessage(writer: BinaryWriter, value: Message): void {
    switch (value.kind) {
        case "start":
            writer.writeUnsigned(0);
            encodeCallStart(writer, value.start);
            return;
        case "item":
            writer.writeUnsigned(1);
            encodeStreamItem(writer, value.item);
            return;
        case "close":
            writer.writeUnsigned(2);
            encodeStreamClose(writer, value.close);
            return;
        case "window":
            writer.writeUnsigned(3);
            encodeWindowUpdate(writer, value.window);
            return;
        case "complete":
            writer.writeUnsigned(4);
            encodeCompletion(writer, value.complete);
            return;
        case "cancel":
            writer.writeUnsigned(5);
            encodeCancel(writer, value.cancel);
            return;
        case "chunk":
            writer.writeUnsigned(6);
            encodePayloadChunk(writer, value.chunk);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one Message. */
export function decodeMessage(reader: BinaryReader): Message {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const start = decodeCallStart(reader);

            return { kind: "start", start };
        }
        case 1: {
            const item = decodeStreamItem(reader);

            return { kind: "item", item };
        }
        case 2: {
            const close = decodeStreamClose(reader);

            return { kind: "close", close };
        }
        case 3: {
            const window = decodeWindowUpdate(reader);

            return { kind: "window", window };
        }
        case 4: {
            const complete = decodeCompletion(reader);

            return { kind: "complete", complete };
        }
        case 5: {
            const cancel = decodeCancel(reader);

            return { kind: "cancel", cancel };
        }
        case 6: {
            const chunk = decodePayloadChunk(reader);

            return { kind: "chunk", chunk };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one Message. */
export function toJsonMessage(value: Message): Json {
    switch (value.kind) {
        case "start":
            return {
                kind: "start",
                start: toJsonCallStart(value.start),
            };
        case "item":
            return {
                kind: "item",
                item: toJsonStreamItem(value.item),
            };
        case "close":
            return {
                kind: "close",
                close: toJsonStreamClose(value.close),
            };
        case "window":
            return {
                kind: "window",
                window: toJsonWindowUpdate(value.window),
            };
        case "complete":
            return {
                kind: "complete",
                complete: toJsonCompletion(value.complete),
            };
        case "cancel":
            return {
                kind: "cancel",
                cancel: toJsonCancel(value.cancel),
            };
        case "chunk":
            return {
                kind: "chunk",
                chunk: toJsonPayloadChunk(value.chunk),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one Message from one JSON value. */
export function fromJsonMessage(value: Json): Message {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "start":
            return {
                kind,
                start: fromJsonCallStart(jsonField(object, "start")),
            };
        case "item":
            return {
                kind,
                item: fromJsonStreamItem(jsonField(object, "item")),
            };
        case "close":
            return {
                kind,
                close: fromJsonStreamClose(jsonField(object, "close")),
            };
        case "window":
            return {
                kind,
                window: fromJsonWindowUpdate(jsonField(object, "window")),
            };
        case "complete":
            return {
                kind,
                complete: fromJsonCompletion(jsonField(object, "complete")),
            };
        case "cancel":
            return {
                kind,
                cancel: fromJsonCancel(jsonField(object, "cancel")),
            };
        case "chunk":
            return {
                kind,
                chunk: fromJsonPayloadChunk(jsonField(object, "chunk")),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}
