/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, bytesFromJson, bytesToJson, jsonBigint, jsonField, jsonObject, jsonString } from "../../../protocol/serde.js";

/** One encoded call value. */
export type Payload =
    /** Bytes carried by their owning message. */
    | {
          readonly kind: "inline";
          readonly inline: Uint8Array | readonly number[];
      }
    /** Bytes carried by immediately following chunk messages. */
    | {
          readonly kind: "deferred";
          /** Direction-local payload identifier. */
          readonly id: PayloadId;
          /** Exact encoded byte length. */
          readonly byteLen: bigint;
      }
;

export const Payload = {
    /** Bytes carried by their owning message. */
    inline(inline: Uint8Array | readonly number[]): Payload {
        return { kind: "inline", inline };
    },

    /** Bytes carried by immediately following chunk messages. */
    deferred(id: PayloadId, byteLen: bigint): Payload {
        return { kind: "deferred", id, byteLen };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: Payload): void {
        encodePayload(writer, value);
    },

    /** Decode one Payload. */
    decode(reader: BinaryReader): Payload {
        return decodePayload(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Payload): Json {
        return toJsonPayload(value);
    },

    /** Return one Payload from one JSON value. */
    fromJson(value: Json): Payload {
        return fromJsonPayload(value);
    },
};

/** Encode one Payload. */
export function encodePayload(writer: BinaryWriter, value: Payload): void {
    switch (value.kind) {
        case "inline":
            writer.writeUnsigned(0);
            writer.writeByteSlice(value.inline);
            return;
        case "deferred":
            writer.writeUnsigned(1);
            encodePayloadId(writer, value.id);
            writer.writeUnsigned(value.byteLen);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one Payload. */
export function decodePayload(reader: BinaryReader): Payload {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const inline = reader.readByteSlice();

            return { kind: "inline", inline };
        }
        case 1: {
            const id = decodePayloadId(reader);
            const byteLen = reader.readUnsigned();

            return {
                kind: "deferred",
                id,
                byteLen,
            };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one Payload. */
export function toJsonPayload(value: Payload): Json {
    switch (value.kind) {
        case "inline":
            return {
                kind: "inline",
                inline: bytesToJson(value.inline),
            };
        case "deferred":
            return {
                kind: "deferred",
                id: toJsonPayloadId(value.id),
                byteLen: value.byteLen.toString(),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one Payload from one JSON value. */
export function fromJsonPayload(value: Json): Payload {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "inline":
            return {
                kind,
                inline: bytesFromJson(jsonField(object, "inline")),
            };
        case "deferred":
            return {
                kind,
                id: fromJsonPayloadId(jsonField(object, "id")),
                byteLen: jsonBigint(jsonField(object, "byteLen")),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** Direction-local deferred payload identifier. */
export type PayloadId = bigint;

export const PayloadId = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: PayloadId): void {
        encodePayloadId(writer, value);
    },

    /** Decode one PayloadId. */
    decode(reader: BinaryReader): PayloadId {
        return decodePayloadId(reader);
    },

    /** Return this value as JSON. */
    toJson(value: PayloadId): Json {
        return toJsonPayloadId(value);
    },

    /** Return one PayloadId from one JSON value. */
    fromJson(value: Json): PayloadId {
        return fromJsonPayloadId(value);
    },
};

/** Encode one PayloadId. */
export function encodePayloadId(writer: BinaryWriter, value: PayloadId): void {
    writer.writeUnsigned(value);
}

/** Decode one PayloadId. */
export function decodePayloadId(reader: BinaryReader): PayloadId {
    return reader.readUnsigned();
}

/** Return one JSON value for one PayloadId. */
export function toJsonPayloadId(value: PayloadId): Json {
    return value.toString();
}

/** Return one PayloadId from one JSON value. */
export function fromJsonPayloadId(value: Json): PayloadId {
    return jsonBigint(value);
}

/** One contiguous deferred payload chunk. */
export type PayloadChunk = {
    /** Deferred payload receiving these bytes. */
    readonly payload: PayloadId;
    /** Next contiguous encoded bytes. */
    readonly bytes: Uint8Array | readonly number[];
};

export const PayloadChunk = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: PayloadChunk): void {
        encodePayloadChunk(writer, value);
    },

    /** Decode one PayloadChunk. */
    decode(reader: BinaryReader): PayloadChunk {
        return decodePayloadChunk(reader);
    },

    /** Return this value as JSON. */
    toJson(value: PayloadChunk): Json {
        return toJsonPayloadChunk(value);
    },

    /** Return one PayloadChunk from one JSON value. */
    fromJson(value: Json): PayloadChunk {
        return fromJsonPayloadChunk(value);
    },
};

/** Encode one PayloadChunk. */
export function encodePayloadChunk(writer: BinaryWriter, value: PayloadChunk): void {
    encodePayloadId(writer, value.payload);
    writer.writeByteSlice(value.bytes);
}

/** Decode one PayloadChunk. */
export function decodePayloadChunk(reader: BinaryReader): PayloadChunk {
    const payload = decodePayloadId(reader);
    const bytes = reader.readByteSlice();

    return {
        payload,
        bytes,
    };
}

/** Return one JSON value for one PayloadChunk. */
export function toJsonPayloadChunk(value: PayloadChunk): Json {
    return {
        payload: toJsonPayloadId(value.payload),
        bytes: bytesToJson(value.bytes),
    };
}

/** Return one PayloadChunk from one JSON value. */
export function fromJsonPayloadChunk(value: Json): PayloadChunk {
    const object = jsonObject(value);

    return {
        payload: fromJsonPayloadId(jsonField(object, "payload")),
        bytes: bytesFromJson(jsonField(object, "bytes")),
    };
}
