/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, jsonBigint, jsonField, jsonObject } from "../../../protocol/serde.js";

/** Stable method identifier within one RPC service. */
export type MethodId = bigint;

export const MethodId = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: MethodId): void {
        encodeMethodId(writer, value);
    },

    /** Decode one MethodId. */
    decode(reader: BinaryReader): MethodId {
        return decodeMethodId(reader);
    },

    /** Return this value as JSON. */
    toJson(value: MethodId): Json {
        return toJsonMethodId(value);
    },

    /** Return one MethodId from one JSON value. */
    fromJson(value: Json): MethodId {
        return fromJsonMethodId(value);
    },
};

/** Encode one MethodId. */
export function encodeMethodId(writer: BinaryWriter, value: MethodId): void {
    writer.writeUnsigned(value);
}

/** Decode one MethodId. */
export function decodeMethodId(reader: BinaryReader): MethodId {
    return reader.readUnsigned();
}

/** Return one JSON value for one MethodId. */
export function toJsonMethodId(value: MethodId): Json {
    return value.toString();
}

/** Return one MethodId from one JSON value. */
export function fromJsonMethodId(value: Json): MethodId {
    return jsonBigint(value);
}

/** Stable canonical RPC method schema fingerprint. */
export type MethodFingerprint = bigint;

export const MethodFingerprint = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: MethodFingerprint): void {
        encodeMethodFingerprint(writer, value);
    },

    /** Decode one MethodFingerprint. */
    decode(reader: BinaryReader): MethodFingerprint {
        return decodeMethodFingerprint(reader);
    },

    /** Return this value as JSON. */
    toJson(value: MethodFingerprint): Json {
        return toJsonMethodFingerprint(value);
    },

    /** Return one MethodFingerprint from one JSON value. */
    fromJson(value: Json): MethodFingerprint {
        return fromJsonMethodFingerprint(value);
    },
};

/** Encode one MethodFingerprint. */
export function encodeMethodFingerprint(writer: BinaryWriter, value: MethodFingerprint): void {
    writer.writeUnsigned(value);
}

/** Decode one MethodFingerprint. */
export function decodeMethodFingerprint(reader: BinaryReader): MethodFingerprint {
    return reader.readUnsigned();
}

/** Return one JSON value for one MethodFingerprint. */
export function toJsonMethodFingerprint(value: MethodFingerprint): Json {
    return value.toString();
}

/** Return one MethodFingerprint from one JSON value. */
export function fromJsonMethodFingerprint(value: Json): MethodFingerprint {
    return jsonBigint(value);
}
