/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, jsonBool, jsonField, jsonObject, jsonOptional, jsonString } from "../../protocol/serde.js";
import type { SourceMap } from "./map.js";
import type { Module } from "../js/tree/script.js";
import { decodeSourceMap, encodeSourceMap, fromJsonSourceMap, toJsonSourceMap } from "./map.js";
import { decodeModule, encodeModule, fromJsonModule, toJsonModule } from "../js/tree/script.js";

/** One structured target language. */
export type ScriptLanguage = "javaScript" | "typeScript";

export const ScriptLanguage = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ScriptLanguage): void {
        encodeScriptLanguage(writer, value);
    },

    /** Decode one ScriptLanguage. */
    decode(reader: BinaryReader): ScriptLanguage {
        return decodeScriptLanguage(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ScriptLanguage): Json {
        return toJsonScriptLanguage(value);
    },

    /** Return one ScriptLanguage from one JSON value. */
    fromJson(value: Json): ScriptLanguage {
        return fromJsonScriptLanguage(value);
    },
};

/** Encode one ScriptLanguage. */
export function encodeScriptLanguage(writer: BinaryWriter, value: ScriptLanguage): void {
    switch (value) {
        case "javaScript":
            writer.writeUnsigned(0);
            return;
        case "typeScript":
            writer.writeUnsigned(1);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one ScriptLanguage. */
export function decodeScriptLanguage(reader: BinaryReader): ScriptLanguage {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "javaScript";
        case 1:
            return "typeScript";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one ScriptLanguage. */
export function toJsonScriptLanguage(value: ScriptLanguage): Json {
    return value;
}

/** Return one ScriptLanguage from one JSON value. */
export function fromJsonScriptLanguage(value: Json): ScriptLanguage {
    const variant = jsonString(value);

    switch (variant) {
        case "javaScript":
            return "javaScript";
        case "typeScript":
            return "typeScript";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** One structured script body. */
export type ScriptBody =
    /** ECMAScript-family module IR. */
    | {
          readonly kind: "ecmaScript";
          readonly ecma_script: Module;
      }
;

export const ScriptBody = {
    /** ECMAScript-family module IR. */
    ecmaScript(ecma_script: Module): ScriptBody {
        return { kind: "ecmaScript", ecma_script };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: ScriptBody): void {
        encodeScriptBody(writer, value);
    },

    /** Decode one ScriptBody. */
    decode(reader: BinaryReader): ScriptBody {
        return decodeScriptBody(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ScriptBody): Json {
        return toJsonScriptBody(value);
    },

    /** Return one ScriptBody from one JSON value. */
    fromJson(value: Json): ScriptBody {
        return fromJsonScriptBody(value);
    },
};

/** Encode one ScriptBody. */
export function encodeScriptBody(writer: BinaryWriter, value: ScriptBody): void {
    switch (value.kind) {
        case "ecmaScript":
            writer.writeUnsigned(0);
            encodeModule(writer, value.ecma_script);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one ScriptBody. */
export function decodeScriptBody(reader: BinaryReader): ScriptBody {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const ecma_script = decodeModule(reader);

            return { kind: "ecmaScript", ecma_script };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one ScriptBody. */
export function toJsonScriptBody(value: ScriptBody): Json {
    switch (value.kind) {
        case "ecmaScript":
            return {
                kind: "ecmaScript",
                ecma_script: toJsonModule(value.ecma_script),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one ScriptBody from one JSON value. */
export function fromJsonScriptBody(value: Json): ScriptBody {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "ecmaScript":
            return {
                kind,
                ecma_script: fromJsonModule(jsonField(object, "ecma_script")),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** One structured script linker input for a target. */
export type Script = {
    /** The target language of this script. */
    readonly language: ScriptLanguage;
    /** The structured script body. */
    readonly body: ScriptBody;
    /** The source map when one exists. */
    readonly map?: SourceMap;
    /** Whether this script has top level side effects. */
    readonly hasTopLevelSideEffects: boolean;
};

export const Script = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: Script): void {
        encodeScript(writer, value);
    },

    /** Decode one Script. */
    decode(reader: BinaryReader): Script {
        return decodeScript(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Script): Json {
        return toJsonScript(value);
    },

    /** Return one Script from one JSON value. */
    fromJson(value: Json): Script {
        return fromJsonScript(value);
    },
};

/** Encode one Script. */
export function encodeScript(writer: BinaryWriter, value: Script): void {
    encodeScriptLanguage(writer, value.language);
    encodeScriptBody(writer, value.body);
    writer.writeOption(value.map, (value2) => {
        encodeSourceMap(writer, value2);
    });
    writer.writeBool(value.hasTopLevelSideEffects);
}

/** Decode one Script. */
export function decodeScript(reader: BinaryReader): Script {
    const language = decodeScriptLanguage(reader);
    const body = decodeScriptBody(reader);
    const map = reader.readOption(() => decodeSourceMap(reader));
    const hasTopLevelSideEffects = reader.readBool();

    return {
        language,
        body,
        ...(map === undefined ? {} : { map }),
        hasTopLevelSideEffects,
    };
}

/** Return one JSON value for one Script. */
export function toJsonScript(value: Script): Json {
    return {
        language: toJsonScriptLanguage(value.language),
        body: toJsonScriptBody(value.body),
        ...(value.map === undefined ? {} : { map: toJsonSourceMap(value.map) }),
        hasTopLevelSideEffects: value.hasTopLevelSideEffects,
    };
}

/** Return one Script from one JSON value. */
export function fromJsonScript(value: Json): Script {
    const object = jsonObject(value);

    return {
        language: fromJsonScriptLanguage(jsonField(object, "language")),
        body: fromJsonScriptBody(jsonField(object, "body")),
        map: jsonOptional(object, "map", (value) => fromJsonSourceMap(value)),
        hasTopLevelSideEffects: jsonBool(jsonField(object, "hasTopLevelSideEffects")),
    };
}
