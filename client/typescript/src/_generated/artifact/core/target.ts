/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, jsonField, jsonObject, jsonString } from "../../../protocol/serde.js";

/** Build distribution profile. */
export type BuildProfile = "full" | "minimal" | "freestanding";

export const BuildProfile = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: BuildProfile): void {
        encodeBuildProfile(writer, value);
    },

    /** Decode one BuildProfile. */
    decode(reader: BinaryReader): BuildProfile {
        return decodeBuildProfile(reader);
    },

    /** Return this value as JSON. */
    toJson(value: BuildProfile): Json {
        return toJsonBuildProfile(value);
    },

    /** Return one BuildProfile from one JSON value. */
    fromJson(value: Json): BuildProfile {
        return fromJsonBuildProfile(value);
    },
};

/** Encode one BuildProfile. */
export function encodeBuildProfile(writer: BinaryWriter, value: BuildProfile): void {
    switch (value) {
        case "full":
            writer.writeUnsigned(0);
            return;
        case "minimal":
            writer.writeUnsigned(1);
            return;
        case "freestanding":
            writer.writeUnsigned(2);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one BuildProfile. */
export function decodeBuildProfile(reader: BinaryReader): BuildProfile {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "full";
        case 1:
            return "minimal";
        case 2:
            return "freestanding";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one BuildProfile. */
export function toJsonBuildProfile(value: BuildProfile): Json {
    return value;
}

/** Return one BuildProfile from one JSON value. */
export function fromJsonBuildProfile(value: Json): BuildProfile {
    const variant = jsonString(value);

    switch (variant) {
        case "full":
            return "full";
        case "minimal":
            return "minimal";
        case "freestanding":
            return "freestanding";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** Build payload linkage. */
export type BuildLinkage = "portable" | "static" | "dynamic";

export const BuildLinkage = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: BuildLinkage): void {
        encodeBuildLinkage(writer, value);
    },

    /** Decode one BuildLinkage. */
    decode(reader: BinaryReader): BuildLinkage {
        return decodeBuildLinkage(reader);
    },

    /** Return this value as JSON. */
    toJson(value: BuildLinkage): Json {
        return toJsonBuildLinkage(value);
    },

    /** Return one BuildLinkage from one JSON value. */
    fromJson(value: Json): BuildLinkage {
        return fromJsonBuildLinkage(value);
    },
};

/** Encode one BuildLinkage. */
export function encodeBuildLinkage(writer: BinaryWriter, value: BuildLinkage): void {
    switch (value) {
        case "portable":
            writer.writeUnsigned(0);
            return;
        case "static":
            writer.writeUnsigned(1);
            return;
        case "dynamic":
            writer.writeUnsigned(2);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one BuildLinkage. */
export function decodeBuildLinkage(reader: BinaryReader): BuildLinkage {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "portable";
        case 1:
            return "static";
        case 2:
            return "dynamic";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one BuildLinkage. */
export function toJsonBuildLinkage(value: BuildLinkage): Json {
    return value;
}

/** Return one BuildLinkage from one JSON value. */
export function fromJsonBuildLinkage(value: Json): BuildLinkage {
    const variant = jsonString(value);

    switch (variant) {
        case "portable":
            return "portable";
        case "static":
            return "static";
        case "dynamic":
            return "dynamic";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** Semantic runtime contract for compiled code. */
export type Runtime = "destack" | "js";

export const Runtime = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: Runtime): void {
        encodeRuntime(writer, value);
    },

    /** Decode one Runtime. */
    decode(reader: BinaryReader): Runtime {
        return decodeRuntime(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Runtime): Json {
        return toJsonRuntime(value);
    },

    /** Return one Runtime from one JSON value. */
    fromJson(value: Json): Runtime {
        return fromJsonRuntime(value);
    },
};

/** Encode one Runtime. */
export function encodeRuntime(writer: BinaryWriter, value: Runtime): void {
    switch (value) {
        case "destack":
            writer.writeUnsigned(0);
            return;
        case "js":
            writer.writeUnsigned(1);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one Runtime. */
export function decodeRuntime(reader: BinaryReader): Runtime {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "destack";
        case 1:
            return "js";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one Runtime. */
export function toJsonRuntime(value: Runtime): Json {
    return value;
}

/** Return one Runtime from one JSON value. */
export function fromJsonRuntime(value: Json): Runtime {
    const variant = jsonString(value);

    switch (variant) {
        case "destack":
            return "destack";
        case "js":
            return "js";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** Host environment that provides target imports and ambient effects. */
export type Host = "native" | "browser" | "wasi" | "emscripten" | "freestanding";

export const Host = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: Host): void {
        encodeHost(writer, value);
    },

    /** Decode one Host. */
    decode(reader: BinaryReader): Host {
        return decodeHost(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Host): Json {
        return toJsonHost(value);
    },

    /** Return one Host from one JSON value. */
    fromJson(value: Json): Host {
        return fromJsonHost(value);
    },
};

/** Encode one Host. */
export function encodeHost(writer: BinaryWriter, value: Host): void {
    switch (value) {
        case "native":
            writer.writeUnsigned(0);
            return;
        case "browser":
            writer.writeUnsigned(1);
            return;
        case "wasi":
            writer.writeUnsigned(2);
            return;
        case "emscripten":
            writer.writeUnsigned(3);
            return;
        case "freestanding":
            writer.writeUnsigned(4);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one Host. */
export function decodeHost(reader: BinaryReader): Host {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "native";
        case 1:
            return "browser";
        case 2:
            return "wasi";
        case 3:
            return "emscripten";
        case 4:
            return "freestanding";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one Host. */
export function toJsonHost(value: Host): Json {
    return value;
}

/** Return one Host from one JSON value. */
export function fromJsonHost(value: Json): Host {
    const variant = jsonString(value);

    switch (variant) {
        case "native":
            return "native";
        case "browser":
            return "browser";
        case "wasi":
            return "wasi";
        case "emscripten":
            return "emscripten";
        case "freestanding":
            return "freestanding";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** Operating system component of a target. */
export type Platform = "unknown" | "windows" | "macOS" | "linux" | "freeBsd" | "openBsd" | "netBsd" | "dragonFly" | "solaris" | "illumos" | "haiku" | "fuchsia" | "redox" | "hermit" | "none";

export const Platform = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: Platform): void {
        encodePlatform(writer, value);
    },

    /** Decode one Platform. */
    decode(reader: BinaryReader): Platform {
        return decodePlatform(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Platform): Json {
        return toJsonPlatform(value);
    },

    /** Return one Platform from one JSON value. */
    fromJson(value: Json): Platform {
        return fromJsonPlatform(value);
    },
};

/** Encode one Platform. */
export function encodePlatform(writer: BinaryWriter, value: Platform): void {
    switch (value) {
        case "unknown":
            writer.writeUnsigned(0);
            return;
        case "windows":
            writer.writeUnsigned(1);
            return;
        case "macOS":
            writer.writeUnsigned(2);
            return;
        case "linux":
            writer.writeUnsigned(3);
            return;
        case "freeBsd":
            writer.writeUnsigned(4);
            return;
        case "openBsd":
            writer.writeUnsigned(5);
            return;
        case "netBsd":
            writer.writeUnsigned(6);
            return;
        case "dragonFly":
            writer.writeUnsigned(7);
            return;
        case "solaris":
            writer.writeUnsigned(8);
            return;
        case "illumos":
            writer.writeUnsigned(9);
            return;
        case "haiku":
            writer.writeUnsigned(10);
            return;
        case "fuchsia":
            writer.writeUnsigned(11);
            return;
        case "redox":
            writer.writeUnsigned(12);
            return;
        case "hermit":
            writer.writeUnsigned(13);
            return;
        case "none":
            writer.writeUnsigned(14);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one Platform. */
export function decodePlatform(reader: BinaryReader): Platform {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "unknown";
        case 1:
            return "windows";
        case 2:
            return "macOS";
        case 3:
            return "linux";
        case 4:
            return "freeBsd";
        case 5:
            return "openBsd";
        case 6:
            return "netBsd";
        case 7:
            return "dragonFly";
        case 8:
            return "solaris";
        case 9:
            return "illumos";
        case 10:
            return "haiku";
        case 11:
            return "fuchsia";
        case 12:
            return "redox";
        case 13:
            return "hermit";
        case 14:
            return "none";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one Platform. */
export function toJsonPlatform(value: Platform): Json {
    return value;
}

/** Return one Platform from one JSON value. */
export function fromJsonPlatform(value: Json): Platform {
    const variant = jsonString(value);

    switch (variant) {
        case "unknown":
            return "unknown";
        case "windows":
            return "windows";
        case "macOS":
            return "macOS";
        case "linux":
            return "linux";
        case "freeBsd":
            return "freeBsd";
        case "openBsd":
            return "openBsd";
        case "netBsd":
            return "netBsd";
        case "dragonFly":
            return "dragonFly";
        case "solaris":
            return "solaris";
        case "illumos":
            return "illumos";
        case "haiku":
            return "haiku";
        case "fuchsia":
            return "fuchsia";
        case "redox":
            return "redox";
        case "hermit":
            return "hermit";
        case "none":
            return "none";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** CPU architecture for native targets. */
export type TargetArch =
    /** x86_64. */
    | {
          readonly kind: "x8664";
      }
    /** x86. */
    | {
          readonly kind: "x86";
      }
    /** AArch64. */
    | {
          readonly kind: "aarch64";
      }
    /** ARMv7. */
    | {
          readonly kind: "armv7";
      }
    /** ARMv6. */
    | {
          readonly kind: "armv6";
      }
    /** RISC-V 64-bit. */
    | {
          readonly kind: "riscv64";
      }
    /** RISC-V 32-bit. */
    | {
          readonly kind: "riscv32";
      }
    /** PowerPC 64-bit. */
    | {
          readonly kind: "powerPc64";
      }
    /** PowerPC 64-bit little-endian. */
    | {
          readonly kind: "powerPc64le";
      }
    /** s390x. */
    | {
          readonly kind: "s390x";
      }
    /** MIPS64. */
    | {
          readonly kind: "mips64";
      }
    /** MIPS64 little-endian. */
    | {
          readonly kind: "mips64el";
      }
    /** LoongArch64. */
    | {
          readonly kind: "loongArch64";
      }
    /** WebAssembly 32-bit. */
    | {
          readonly kind: "wasm32";
      }
    /** WebAssembly 64-bit. */
    | {
          readonly kind: "wasm64";
      }
    /** Other architecture. */
    | {
          readonly kind: "other";
          readonly other: string;
      }
;

export const TargetArch = {
    /** x86_64. */
    x8664(): TargetArch {
        return { kind: "x8664" };
    },

    /** x86. */
    x86(): TargetArch {
        return { kind: "x86" };
    },

    /** AArch64. */
    aarch64(): TargetArch {
        return { kind: "aarch64" };
    },

    /** ARMv7. */
    armv7(): TargetArch {
        return { kind: "armv7" };
    },

    /** ARMv6. */
    armv6(): TargetArch {
        return { kind: "armv6" };
    },

    /** RISC-V 64-bit. */
    riscv64(): TargetArch {
        return { kind: "riscv64" };
    },

    /** RISC-V 32-bit. */
    riscv32(): TargetArch {
        return { kind: "riscv32" };
    },

    /** PowerPC 64-bit. */
    powerPc64(): TargetArch {
        return { kind: "powerPc64" };
    },

    /** PowerPC 64-bit little-endian. */
    powerPc64le(): TargetArch {
        return { kind: "powerPc64le" };
    },

    /** s390x. */
    s390x(): TargetArch {
        return { kind: "s390x" };
    },

    /** MIPS64. */
    mips64(): TargetArch {
        return { kind: "mips64" };
    },

    /** MIPS64 little-endian. */
    mips64el(): TargetArch {
        return { kind: "mips64el" };
    },

    /** LoongArch64. */
    loongArch64(): TargetArch {
        return { kind: "loongArch64" };
    },

    /** WebAssembly 32-bit. */
    wasm32(): TargetArch {
        return { kind: "wasm32" };
    },

    /** WebAssembly 64-bit. */
    wasm64(): TargetArch {
        return { kind: "wasm64" };
    },

    /** Other architecture. */
    other(other: string): TargetArch {
        return { kind: "other", other };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: TargetArch): void {
        encodeTargetArch(writer, value);
    },

    /** Decode one TargetArch. */
    decode(reader: BinaryReader): TargetArch {
        return decodeTargetArch(reader);
    },

    /** Return this value as JSON. */
    toJson(value: TargetArch): Json {
        return toJsonTargetArch(value);
    },

    /** Return one TargetArch from one JSON value. */
    fromJson(value: Json): TargetArch {
        return fromJsonTargetArch(value);
    },
};

/** Encode one TargetArch. */
export function encodeTargetArch(writer: BinaryWriter, value: TargetArch): void {
    switch (value.kind) {
        case "x8664":
            writer.writeUnsigned(0);
            return;
        case "x86":
            writer.writeUnsigned(1);
            return;
        case "aarch64":
            writer.writeUnsigned(2);
            return;
        case "armv7":
            writer.writeUnsigned(3);
            return;
        case "armv6":
            writer.writeUnsigned(4);
            return;
        case "riscv64":
            writer.writeUnsigned(5);
            return;
        case "riscv32":
            writer.writeUnsigned(6);
            return;
        case "powerPc64":
            writer.writeUnsigned(7);
            return;
        case "powerPc64le":
            writer.writeUnsigned(8);
            return;
        case "s390x":
            writer.writeUnsigned(9);
            return;
        case "mips64":
            writer.writeUnsigned(10);
            return;
        case "mips64el":
            writer.writeUnsigned(11);
            return;
        case "loongArch64":
            writer.writeUnsigned(12);
            return;
        case "wasm32":
            writer.writeUnsigned(13);
            return;
        case "wasm64":
            writer.writeUnsigned(14);
            return;
        case "other":
            writer.writeUnsigned(15);
            writer.writeString(value.other);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one TargetArch. */
export function decodeTargetArch(reader: BinaryReader): TargetArch {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            return { kind: "x8664" };
        }
        case 1: {
            return { kind: "x86" };
        }
        case 2: {
            return { kind: "aarch64" };
        }
        case 3: {
            return { kind: "armv7" };
        }
        case 4: {
            return { kind: "armv6" };
        }
        case 5: {
            return { kind: "riscv64" };
        }
        case 6: {
            return { kind: "riscv32" };
        }
        case 7: {
            return { kind: "powerPc64" };
        }
        case 8: {
            return { kind: "powerPc64le" };
        }
        case 9: {
            return { kind: "s390x" };
        }
        case 10: {
            return { kind: "mips64" };
        }
        case 11: {
            return { kind: "mips64el" };
        }
        case 12: {
            return { kind: "loongArch64" };
        }
        case 13: {
            return { kind: "wasm32" };
        }
        case 14: {
            return { kind: "wasm64" };
        }
        case 15: {
            const other = reader.readString();

            return { kind: "other", other };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one TargetArch. */
export function toJsonTargetArch(value: TargetArch): Json {
    switch (value.kind) {
        case "x8664":
            return {
                kind: "x8664",
            };
        case "x86":
            return {
                kind: "x86",
            };
        case "aarch64":
            return {
                kind: "aarch64",
            };
        case "armv7":
            return {
                kind: "armv7",
            };
        case "armv6":
            return {
                kind: "armv6",
            };
        case "riscv64":
            return {
                kind: "riscv64",
            };
        case "riscv32":
            return {
                kind: "riscv32",
            };
        case "powerPc64":
            return {
                kind: "powerPc64",
            };
        case "powerPc64le":
            return {
                kind: "powerPc64le",
            };
        case "s390x":
            return {
                kind: "s390x",
            };
        case "mips64":
            return {
                kind: "mips64",
            };
        case "mips64el":
            return {
                kind: "mips64el",
            };
        case "loongArch64":
            return {
                kind: "loongArch64",
            };
        case "wasm32":
            return {
                kind: "wasm32",
            };
        case "wasm64":
            return {
                kind: "wasm64",
            };
        case "other":
            return {
                kind: "other",
                other: value.other,
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one TargetArch from one JSON value. */
export function fromJsonTargetArch(value: Json): TargetArch {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "x8664":
            return {
                kind,
            };
        case "x86":
            return {
                kind,
            };
        case "aarch64":
            return {
                kind,
            };
        case "armv7":
            return {
                kind,
            };
        case "armv6":
            return {
                kind,
            };
        case "riscv64":
            return {
                kind,
            };
        case "riscv32":
            return {
                kind,
            };
        case "powerPc64":
            return {
                kind,
            };
        case "powerPc64le":
            return {
                kind,
            };
        case "s390x":
            return {
                kind,
            };
        case "mips64":
            return {
                kind,
            };
        case "mips64el":
            return {
                kind,
            };
        case "loongArch64":
            return {
                kind,
            };
        case "wasm32":
            return {
                kind,
            };
        case "wasm64":
            return {
                kind,
            };
        case "other":
            return {
                kind,
                other: jsonString(jsonField(object, "other")),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** Target vendor for native targets. */
export type TargetVendor =
    /** Unknown vendor. */
    | {
          readonly kind: "unknown";
      }
    /** Apple. */
    | {
          readonly kind: "apple";
      }
    /** PC. */
    | {
          readonly kind: "pc";
      }
    /** IBM. */
    | {
          readonly kind: "ibm";
      }
    /** Nintendo. */
    | {
          readonly kind: "nintendo";
      }
    /** Other vendor. */
    | {
          readonly kind: "other";
          readonly other: string;
      }
;

export const TargetVendor = {
    /** Unknown vendor. */
    "unknown"(): TargetVendor {
        return { kind: "unknown" };
    },

    /** Apple. */
    apple(): TargetVendor {
        return { kind: "apple" };
    },

    /** PC. */
    pc(): TargetVendor {
        return { kind: "pc" };
    },

    /** IBM. */
    ibm(): TargetVendor {
        return { kind: "ibm" };
    },

    /** Nintendo. */
    nintendo(): TargetVendor {
        return { kind: "nintendo" };
    },

    /** Other vendor. */
    other(other: string): TargetVendor {
        return { kind: "other", other };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: TargetVendor): void {
        encodeTargetVendor(writer, value);
    },

    /** Decode one TargetVendor. */
    decode(reader: BinaryReader): TargetVendor {
        return decodeTargetVendor(reader);
    },

    /** Return this value as JSON. */
    toJson(value: TargetVendor): Json {
        return toJsonTargetVendor(value);
    },

    /** Return one TargetVendor from one JSON value. */
    fromJson(value: Json): TargetVendor {
        return fromJsonTargetVendor(value);
    },
};

/** Encode one TargetVendor. */
export function encodeTargetVendor(writer: BinaryWriter, value: TargetVendor): void {
    switch (value.kind) {
        case "unknown":
            writer.writeUnsigned(0);
            return;
        case "apple":
            writer.writeUnsigned(1);
            return;
        case "pc":
            writer.writeUnsigned(2);
            return;
        case "ibm":
            writer.writeUnsigned(3);
            return;
        case "nintendo":
            writer.writeUnsigned(4);
            return;
        case "other":
            writer.writeUnsigned(5);
            writer.writeString(value.other);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one TargetVendor. */
export function decodeTargetVendor(reader: BinaryReader): TargetVendor {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            return { kind: "unknown" };
        }
        case 1: {
            return { kind: "apple" };
        }
        case 2: {
            return { kind: "pc" };
        }
        case 3: {
            return { kind: "ibm" };
        }
        case 4: {
            return { kind: "nintendo" };
        }
        case 5: {
            const other = reader.readString();

            return { kind: "other", other };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one TargetVendor. */
export function toJsonTargetVendor(value: TargetVendor): Json {
    switch (value.kind) {
        case "unknown":
            return {
                kind: "unknown",
            };
        case "apple":
            return {
                kind: "apple",
            };
        case "pc":
            return {
                kind: "pc",
            };
        case "ibm":
            return {
                kind: "ibm",
            };
        case "nintendo":
            return {
                kind: "nintendo",
            };
        case "other":
            return {
                kind: "other",
                other: value.other,
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one TargetVendor from one JSON value. */
export function fromJsonTargetVendor(value: Json): TargetVendor {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "unknown":
            return {
                kind,
            };
        case "apple":
            return {
                kind,
            };
        case "pc":
            return {
                kind,
            };
        case "ibm":
            return {
                kind,
            };
        case "nintendo":
            return {
                kind,
            };
        case "other":
            return {
                kind,
                other: jsonString(jsonField(object, "other")),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** Target ABI flavor. */
export type TargetAbi =
    /** GNU environment. */
    | {
          readonly kind: "gnu";
      }
    /** Musl environment. */
    | {
          readonly kind: "musl";
      }
    /** MSVC environment. */
    | {
          readonly kind: "msvc";
      }
    /** GNU + LLVM environment. */
    | {
          readonly kind: "gnuLlvm";
      }
    /** EABI. */
    | {
          readonly kind: "eabi";
      }
    /** EABI with hard-float. */
    | {
          readonly kind: "eabihf";
      }
    /** Musl + EABI. */
    | {
          readonly kind: "muslEabi";
      }
    /** Musl + EABI hard-float. */
    | {
          readonly kind: "muslEabihf";
      }
    /** Other environment. */
    | {
          readonly kind: "other";
          readonly other: string;
      }
;

export const TargetAbi = {
    /** GNU environment. */
    gnu(): TargetAbi {
        return { kind: "gnu" };
    },

    /** Musl environment. */
    musl(): TargetAbi {
        return { kind: "musl" };
    },

    /** MSVC environment. */
    msvc(): TargetAbi {
        return { kind: "msvc" };
    },

    /** GNU + LLVM environment. */
    gnuLlvm(): TargetAbi {
        return { kind: "gnuLlvm" };
    },

    /** EABI. */
    eabi(): TargetAbi {
        return { kind: "eabi" };
    },

    /** EABI with hard-float. */
    eabihf(): TargetAbi {
        return { kind: "eabihf" };
    },

    /** Musl + EABI. */
    muslEabi(): TargetAbi {
        return { kind: "muslEabi" };
    },

    /** Musl + EABI hard-float. */
    muslEabihf(): TargetAbi {
        return { kind: "muslEabihf" };
    },

    /** Other environment. */
    other(other: string): TargetAbi {
        return { kind: "other", other };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: TargetAbi): void {
        encodeTargetAbi(writer, value);
    },

    /** Decode one TargetAbi. */
    decode(reader: BinaryReader): TargetAbi {
        return decodeTargetAbi(reader);
    },

    /** Return this value as JSON. */
    toJson(value: TargetAbi): Json {
        return toJsonTargetAbi(value);
    },

    /** Return one TargetAbi from one JSON value. */
    fromJson(value: Json): TargetAbi {
        return fromJsonTargetAbi(value);
    },
};

/** Encode one TargetAbi. */
export function encodeTargetAbi(writer: BinaryWriter, value: TargetAbi): void {
    switch (value.kind) {
        case "gnu":
            writer.writeUnsigned(0);
            return;
        case "musl":
            writer.writeUnsigned(1);
            return;
        case "msvc":
            writer.writeUnsigned(2);
            return;
        case "gnuLlvm":
            writer.writeUnsigned(3);
            return;
        case "eabi":
            writer.writeUnsigned(4);
            return;
        case "eabihf":
            writer.writeUnsigned(5);
            return;
        case "muslEabi":
            writer.writeUnsigned(6);
            return;
        case "muslEabihf":
            writer.writeUnsigned(7);
            return;
        case "other":
            writer.writeUnsigned(8);
            writer.writeString(value.other);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one TargetAbi. */
export function decodeTargetAbi(reader: BinaryReader): TargetAbi {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            return { kind: "gnu" };
        }
        case 1: {
            return { kind: "musl" };
        }
        case 2: {
            return { kind: "msvc" };
        }
        case 3: {
            return { kind: "gnuLlvm" };
        }
        case 4: {
            return { kind: "eabi" };
        }
        case 5: {
            return { kind: "eabihf" };
        }
        case 6: {
            return { kind: "muslEabi" };
        }
        case 7: {
            return { kind: "muslEabihf" };
        }
        case 8: {
            const other = reader.readString();

            return { kind: "other", other };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one TargetAbi. */
export function toJsonTargetAbi(value: TargetAbi): Json {
    switch (value.kind) {
        case "gnu":
            return {
                kind: "gnu",
            };
        case "musl":
            return {
                kind: "musl",
            };
        case "msvc":
            return {
                kind: "msvc",
            };
        case "gnuLlvm":
            return {
                kind: "gnuLlvm",
            };
        case "eabi":
            return {
                kind: "eabi",
            };
        case "eabihf":
            return {
                kind: "eabihf",
            };
        case "muslEabi":
            return {
                kind: "muslEabi",
            };
        case "muslEabihf":
            return {
                kind: "muslEabihf",
            };
        case "other":
            return {
                kind: "other",
                other: value.other,
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one TargetAbi from one JSON value. */
export function fromJsonTargetAbi(value: Json): TargetAbi {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "gnu":
            return {
                kind,
            };
        case "musl":
            return {
                kind,
            };
        case "msvc":
            return {
                kind,
            };
        case "gnuLlvm":
            return {
                kind,
            };
        case "eabi":
            return {
                kind,
            };
        case "eabihf":
            return {
                kind,
            };
        case "muslEabi":
            return {
                kind,
            };
        case "muslEabihf":
            return {
                kind,
            };
        case "other":
            return {
                kind,
                other: jsonString(jsonField(object, "other")),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}
