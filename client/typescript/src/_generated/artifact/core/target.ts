/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, jsonString } from "../../../protocol/serde.js";

/** Build payload linkage. */
export type BuildLinkage = "portable" | "static" | "dynamic";

export const BuildLinkage = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: BuildLinkage): void {
        encodeBuildLinkage(writer, value);
    },

    /** Decode one BuildLinkage. */
    decode(reader: BinaryReader): BuildLinkage {
        return decodeBuildLinkage(reader);
    },

    /** Return this value as JSON. */
    toJson(value: BuildLinkage): Json {
        return toJsonBuildLinkage(value);
    },

    /** Return one BuildLinkage from one JSON value. */
    fromJson(value: Json): BuildLinkage {
        return fromJsonBuildLinkage(value);
    },
};

/** Encode one BuildLinkage. */
export function encodeBuildLinkage(writer: BinaryWriter, value: BuildLinkage): void {
    switch (value) {
        case "portable":
            writer.writeUnsigned(0);
            return;
        case "static":
            writer.writeUnsigned(1);
            return;
        case "dynamic":
            writer.writeUnsigned(2);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one BuildLinkage. */
export function decodeBuildLinkage(reader: BinaryReader): BuildLinkage {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "portable";
        case 1:
            return "static";
        case 2:
            return "dynamic";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one BuildLinkage. */
export function toJsonBuildLinkage(value: BuildLinkage): Json {
    return value;
}

/** Return one BuildLinkage from one JSON value. */
export function fromJsonBuildLinkage(value: Json): BuildLinkage {
    const variant = jsonString(value);

    switch (variant) {
        case "portable":
            return "portable";
        case "static":
            return "static";
        case "dynamic":
            return "dynamic";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** Build distribution profile. */
export type BuildProfile = "full" | "minimal" | "freestanding";

export const BuildProfile = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: BuildProfile): void {
        encodeBuildProfile(writer, value);
    },

    /** Decode one BuildProfile. */
    decode(reader: BinaryReader): BuildProfile {
        return decodeBuildProfile(reader);
    },

    /** Return this value as JSON. */
    toJson(value: BuildProfile): Json {
        return toJsonBuildProfile(value);
    },

    /** Return one BuildProfile from one JSON value. */
    fromJson(value: Json): BuildProfile {
        return fromJsonBuildProfile(value);
    },
};

/** Encode one BuildProfile. */
export function encodeBuildProfile(writer: BinaryWriter, value: BuildProfile): void {
    switch (value) {
        case "full":
            writer.writeUnsigned(0);
            return;
        case "minimal":
            writer.writeUnsigned(1);
            return;
        case "freestanding":
            writer.writeUnsigned(2);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one BuildProfile. */
export function decodeBuildProfile(reader: BinaryReader): BuildProfile {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "full";
        case 1:
            return "minimal";
        case 2:
            return "freestanding";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one BuildProfile. */
export function toJsonBuildProfile(value: BuildProfile): Json {
    return value;
}

/** Return one BuildProfile from one JSON value. */
export function fromJsonBuildProfile(value: Json): BuildProfile {
    const variant = jsonString(value);

    switch (variant) {
        case "full":
            return "full";
        case "minimal":
            return "minimal";
        case "freestanding":
            return "freestanding";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** Host environment that provides target imports and ambient effects. */
export type Host = "native" | "browser" | "wasi" | "emscripten" | "freestanding";

export const Host = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: Host): void {
        encodeHost(writer, value);
    },

    /** Decode one Host. */
    decode(reader: BinaryReader): Host {
        return decodeHost(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Host): Json {
        return toJsonHost(value);
    },

    /** Return one Host from one JSON value. */
    fromJson(value: Json): Host {
        return fromJsonHost(value);
    },
};

/** Encode one Host. */
export function encodeHost(writer: BinaryWriter, value: Host): void {
    switch (value) {
        case "native":
            writer.writeUnsigned(0);
            return;
        case "browser":
            writer.writeUnsigned(1);
            return;
        case "wasi":
            writer.writeUnsigned(2);
            return;
        case "emscripten":
            writer.writeUnsigned(3);
            return;
        case "freestanding":
            writer.writeUnsigned(4);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one Host. */
export function decodeHost(reader: BinaryReader): Host {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "native";
        case 1:
            return "browser";
        case 2:
            return "wasi";
        case 3:
            return "emscripten";
        case 4:
            return "freestanding";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one Host. */
export function toJsonHost(value: Host): Json {
    return value;
}

/** Return one Host from one JSON value. */
export function fromJsonHost(value: Json): Host {
    const variant = jsonString(value);

    switch (variant) {
        case "native":
            return "native";
        case "browser":
            return "browser";
        case "wasi":
            return "wasi";
        case "emscripten":
            return "emscripten";
        case "freestanding":
            return "freestanding";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** Operating system component of a target. */
export type Platform = "unknown" | "windows" | "macOS" | "linux" | "freeBsd" | "openBsd" | "netBsd" | "dragonFly" | "solaris" | "illumos" | "haiku" | "fuchsia" | "redox" | "hermit" | "none";

export const Platform = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: Platform): void {
        encodePlatform(writer, value);
    },

    /** Decode one Platform. */
    decode(reader: BinaryReader): Platform {
        return decodePlatform(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Platform): Json {
        return toJsonPlatform(value);
    },

    /** Return one Platform from one JSON value. */
    fromJson(value: Json): Platform {
        return fromJsonPlatform(value);
    },
};

/** Encode one Platform. */
export function encodePlatform(writer: BinaryWriter, value: Platform): void {
    switch (value) {
        case "unknown":
            writer.writeUnsigned(0);
            return;
        case "windows":
            writer.writeUnsigned(1);
            return;
        case "macOS":
            writer.writeUnsigned(2);
            return;
        case "linux":
            writer.writeUnsigned(3);
            return;
        case "freeBsd":
            writer.writeUnsigned(4);
            return;
        case "openBsd":
            writer.writeUnsigned(5);
            return;
        case "netBsd":
            writer.writeUnsigned(6);
            return;
        case "dragonFly":
            writer.writeUnsigned(7);
            return;
        case "solaris":
            writer.writeUnsigned(8);
            return;
        case "illumos":
            writer.writeUnsigned(9);
            return;
        case "haiku":
            writer.writeUnsigned(10);
            return;
        case "fuchsia":
            writer.writeUnsigned(11);
            return;
        case "redox":
            writer.writeUnsigned(12);
            return;
        case "hermit":
            writer.writeUnsigned(13);
            return;
        case "none":
            writer.writeUnsigned(14);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one Platform. */
export function decodePlatform(reader: BinaryReader): Platform {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "unknown";
        case 1:
            return "windows";
        case 2:
            return "macOS";
        case 3:
            return "linux";
        case 4:
            return "freeBsd";
        case 5:
            return "openBsd";
        case 6:
            return "netBsd";
        case 7:
            return "dragonFly";
        case 8:
            return "solaris";
        case 9:
            return "illumos";
        case 10:
            return "haiku";
        case 11:
            return "fuchsia";
        case 12:
            return "redox";
        case 13:
            return "hermit";
        case 14:
            return "none";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one Platform. */
export function toJsonPlatform(value: Platform): Json {
    return value;
}

/** Return one Platform from one JSON value. */
export function fromJsonPlatform(value: Json): Platform {
    const variant = jsonString(value);

    switch (variant) {
        case "unknown":
            return "unknown";
        case "windows":
            return "windows";
        case "macOS":
            return "macOS";
        case "linux":
            return "linux";
        case "freeBsd":
            return "freeBsd";
        case "openBsd":
            return "openBsd";
        case "netBsd":
            return "netBsd";
        case "dragonFly":
            return "dragonFly";
        case "solaris":
            return "solaris";
        case "illumos":
            return "illumos";
        case "haiku":
            return "haiku";
        case "fuchsia":
            return "fuchsia";
        case "redox":
            return "redox";
        case "hermit":
            return "hermit";
        case "none":
            return "none";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** Semantic runtime contract for compiled code. */
export type Runtime = "destack" | "js";

export const Runtime = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: Runtime): void {
        encodeRuntime(writer, value);
    },

    /** Decode one Runtime. */
    decode(reader: BinaryReader): Runtime {
        return decodeRuntime(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Runtime): Json {
        return toJsonRuntime(value);
    },

    /** Return one Runtime from one JSON value. */
    fromJson(value: Json): Runtime {
        return fromJsonRuntime(value);
    },
};

/** Encode one Runtime. */
export function encodeRuntime(writer: BinaryWriter, value: Runtime): void {
    switch (value) {
        case "destack":
            writer.writeUnsigned(0);
            return;
        case "js":
            writer.writeUnsigned(1);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one Runtime. */
export function decodeRuntime(reader: BinaryReader): Runtime {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "destack";
        case 1:
            return "js";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one Runtime. */
export function toJsonRuntime(value: Runtime): Json {
    return value;
}

/** Return one Runtime from one JSON value. */
export function fromJsonRuntime(value: Json): Runtime {
    const variant = jsonString(value);

    switch (variant) {
        case "destack":
            return "destack";
        case "js":
            return "js";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}
