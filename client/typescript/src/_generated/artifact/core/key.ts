/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, jsonField, jsonObject, jsonString } from "../../../protocol/serde.js";
import type { IndexKind } from "../index.js";
import type { ModuleId } from "../../source/file/model/module.js";
import type { PackageId } from "../../source/file/model/package.js";
import type { ProductId } from "../../source/file/model/product.js";
import type { ProfileId } from "../../source/file/model/profile.js";
import type { TargetId } from "../../source/file/model/target.js";
import { decodeIndexKind, encodeIndexKind, fromJsonIndexKind, toJsonIndexKind } from "../index.js";
import { decodeModuleId, encodeModuleId, fromJsonModuleId, toJsonModuleId } from "../../source/file/model/module.js";
import { decodePackageId, encodePackageId, fromJsonPackageId, toJsonPackageId } from "../../source/file/model/package.js";
import { decodeProductId, encodeProductId, fromJsonProductId, toJsonProductId } from "../../source/file/model/product.js";
import { decodeProfileId, encodeProfileId, fromJsonProfileId, toJsonProfileId } from "../../source/file/model/profile.js";
import { decodeTargetId, encodeTargetId, fromJsonTargetId, toJsonTargetId } from "../../source/file/model/target.js";

/** One artifact kind and its owner. */
export type ArtifactKey =
    /** Toolchain build payload for one target. */
    | {
          readonly kind: "build";
          readonly target: TargetId;
      }
    /** Parsed module DIR. */
    | {
          readonly kind: "dirParsed";
          readonly module: ModuleId;
      }
    /** Parsed non-code module data. */
    | {
          readonly kind: "data";
          readonly module: ModuleId;
      }
    /** Bound DIR. */
    | {
          readonly kind: "dirBound";
          readonly module: ModuleId;
          readonly profile: ProfileId;
      }
    /** Bound implicit environment for one profile. */
    | {
          readonly kind: "environmentBound";
          readonly profile: ProfileId;
      }
    /** Import graph over the modules of one profile. */
    | {
          readonly kind: "moduleGraph";
          readonly profile: ProfileId;
      }
    /** Imported DIR. */
    | {
          readonly kind: "dirImported";
          readonly module: ModuleId;
          readonly profile: ProfileId;
      }
    /** Expanded DIR. */
    | {
          readonly kind: "dirExpanded";
          readonly module: ModuleId;
          readonly profile: ProfileId;
      }
    /** Exported DIR. */
    | {
          readonly kind: "dirExported";
          readonly module: ModuleId;
          readonly profile: ProfileId;
      }
    /** Resolved DIR imports. */
    | {
          readonly kind: "dirResolved";
          readonly module: ModuleId;
          readonly profile: ProfileId;
      }
    /** Declared DIR module. */
    | {
          readonly kind: "dirDeclared";
          readonly module: ModuleId;
          readonly profile: ProfileId;
      }
    /** Declared implicit environment for one profile. */
    | {
          readonly kind: "environmentDeclared";
          readonly profile: ProfileId;
      }
    /** Elaborated DIR module. */
    | {
          readonly kind: "dirElaborated";
          readonly module: ModuleId;
          readonly profile: ProfileId;
      }
    /** Checked DIR module. */
    | {
          readonly kind: "dirChecked";
          readonly module: ModuleId;
          readonly profile: ProfileId;
      }
    /** Materialized DIR. */
    | {
          readonly kind: "dirMaterialized";
          readonly module: ModuleId;
          readonly profile: ProfileId;
      }
    /** Lowered MIR before optimization. */
    | {
          readonly kind: "mirLowered";
          readonly module: ModuleId;
          readonly profile: ProfileId;
          readonly target: TargetId;
      }
    /** Verified MIR after required semantic verification. */
    | {
          readonly kind: "mirVerified";
          readonly module: ModuleId;
          readonly profile: ProfileId;
          readonly target: TargetId;
      }
    /** MIR after required elaboration. */
    | {
          readonly kind: "mirElaborated";
          readonly module: ModuleId;
          readonly profile: ProfileId;
          readonly target: TargetId;
      }
    /** Per-module link summary for whole-program analysis. */
    | {
          readonly kind: "mirAnalyzed";
          readonly module: ModuleId;
          readonly profile: ProfileId;
          readonly target: TargetId;
      }
    /** Optimized MIR. */
    | {
          readonly kind: "mirOptimized";
          readonly module: ModuleId;
          readonly profile: ProfileId;
          readonly target: TargetId;
      }
    /** Whole-program analysis for one profile and target. */
    | {
          readonly kind: "programAnalysis";
          readonly profile: ProfileId;
          readonly target: TargetId;
      }
    /** One index for a module profile. */
    | {
          readonly kind: "moduleIndex";
          readonly module: ModuleId;
          readonly profile: ProfileId;
          readonly kindValue: IndexKind;
      }
    /** One index for a program profile. */
    | {
          readonly kind: "programIndex";
          readonly profile: ProfileId;
          readonly kindValue: IndexKind;
      }
    /** Completed lint analysis for one module in one target. */
    | {
          readonly kind: "moduleLinted";
          readonly module: ModuleId;
          readonly profile: ProfileId;
          readonly target: TargetId;
      }
    /** Completed lint analysis for one target program. */
    | {
          readonly kind: "programLinted";
          readonly profile: ProfileId;
          readonly target: TargetId;
      }
    /** One structured linker input for one target. */
    | {
          readonly kind: "script";
          readonly module: ModuleId;
          readonly target: TargetId;
      }
    /** One optimized module object for one target. */
    | {
          readonly kind: "object";
          readonly module: ModuleId;
          readonly target: TargetId;
      }
    /** One opaque linker input for one target. */
    | {
          readonly kind: "asset";
          readonly module: ModuleId;
          readonly target: TargetId;
      }
    /** Linked file graph for one package target. */
    | {
          readonly kind: "bundle";
          readonly package: PackageId;
          readonly target: TargetId;
      }
    /** Program for one package target. */
    | {
          readonly kind: "program";
          readonly package: PackageId;
          readonly target: TargetId;
      }
    /** Linked product assembled from configured target artifacts. */
    | {
          readonly kind: "product";
          readonly package: PackageId;
          readonly product: ProductId;
      }
;

export const ArtifactKey = {
    /** Toolchain build payload for one target. */
    build(target: TargetId): ArtifactKey {
        return { kind: "build", target };
    },

    /** Parsed module DIR. */
    dirParsed(module_: ModuleId): ArtifactKey {
        return { kind: "dirParsed", module: module_ };
    },

    /** Parsed non-code module data. */
    data(module_: ModuleId): ArtifactKey {
        return { kind: "data", module: module_ };
    },

    /** Bound DIR. */
    dirBound(module_: ModuleId, profile: ProfileId): ArtifactKey {
        return { kind: "dirBound", module: module_, profile };
    },

    /** Bound implicit environment for one profile. */
    environmentBound(profile: ProfileId): ArtifactKey {
        return { kind: "environmentBound", profile };
    },

    /** Import graph over the modules of one profile. */
    moduleGraph(profile: ProfileId): ArtifactKey {
        return { kind: "moduleGraph", profile };
    },

    /** Imported DIR. */
    dirImported(module_: ModuleId, profile: ProfileId): ArtifactKey {
        return { kind: "dirImported", module: module_, profile };
    },

    /** Expanded DIR. */
    dirExpanded(module_: ModuleId, profile: ProfileId): ArtifactKey {
        return { kind: "dirExpanded", module: module_, profile };
    },

    /** Exported DIR. */
    dirExported(module_: ModuleId, profile: ProfileId): ArtifactKey {
        return { kind: "dirExported", module: module_, profile };
    },

    /** Resolved DIR imports. */
    dirResolved(module_: ModuleId, profile: ProfileId): ArtifactKey {
        return { kind: "dirResolved", module: module_, profile };
    },

    /** Declared DIR module. */
    dirDeclared(module_: ModuleId, profile: ProfileId): ArtifactKey {
        return { kind: "dirDeclared", module: module_, profile };
    },

    /** Declared implicit environment for one profile. */
    environmentDeclared(profile: ProfileId): ArtifactKey {
        return { kind: "environmentDeclared", profile };
    },

    /** Elaborated DIR module. */
    dirElaborated(module_: ModuleId, profile: ProfileId): ArtifactKey {
        return { kind: "dirElaborated", module: module_, profile };
    },

    /** Checked DIR module. */
    dirChecked(module_: ModuleId, profile: ProfileId): ArtifactKey {
        return { kind: "dirChecked", module: module_, profile };
    },

    /** Materialized DIR. */
    dirMaterialized(module_: ModuleId, profile: ProfileId): ArtifactKey {
        return { kind: "dirMaterialized", module: module_, profile };
    },

    /** Lowered MIR before optimization. */
    mirLowered(module_: ModuleId, profile: ProfileId, target: TargetId): ArtifactKey {
        return { kind: "mirLowered", module: module_, profile, target };
    },

    /** Verified MIR after required semantic verification. */
    mirVerified(module_: ModuleId, profile: ProfileId, target: TargetId): ArtifactKey {
        return { kind: "mirVerified", module: module_, profile, target };
    },

    /** MIR after required elaboration. */
    mirElaborated(module_: ModuleId, profile: ProfileId, target: TargetId): ArtifactKey {
        return { kind: "mirElaborated", module: module_, profile, target };
    },

    /** Per-module link summary for whole-program analysis. */
    mirAnalyzed(module_: ModuleId, profile: ProfileId, target: TargetId): ArtifactKey {
        return { kind: "mirAnalyzed", module: module_, profile, target };
    },

    /** Optimized MIR. */
    mirOptimized(module_: ModuleId, profile: ProfileId, target: TargetId): ArtifactKey {
        return { kind: "mirOptimized", module: module_, profile, target };
    },

    /** Whole-program analysis for one profile and target. */
    programAnalysis(profile: ProfileId, target: TargetId): ArtifactKey {
        return { kind: "programAnalysis", profile, target };
    },

    /** One index for a module profile. */
    moduleIndex(module_: ModuleId, profile: ProfileId, kind: IndexKind): ArtifactKey {
        return { kind: "moduleIndex", module: module_, profile, kindValue: kind };
    },

    /** One index for a program profile. */
    programIndex(profile: ProfileId, kind: IndexKind): ArtifactKey {
        return { kind: "programIndex", profile, kindValue: kind };
    },

    /** Completed lint analysis for one module in one target. */
    moduleLinted(module_: ModuleId, profile: ProfileId, target: TargetId): ArtifactKey {
        return { kind: "moduleLinted", module: module_, profile, target };
    },

    /** Completed lint analysis for one target program. */
    programLinted(profile: ProfileId, target: TargetId): ArtifactKey {
        return { kind: "programLinted", profile, target };
    },

    /** One structured linker input for one target. */
    script(module_: ModuleId, target: TargetId): ArtifactKey {
        return { kind: "script", module: module_, target };
    },

    /** One optimized module object for one target. */
    "object"(module_: ModuleId, target: TargetId): ArtifactKey {
        return { kind: "object", module: module_, target };
    },

    /** One opaque linker input for one target. */
    asset(module_: ModuleId, target: TargetId): ArtifactKey {
        return { kind: "asset", module: module_, target };
    },

    /** Linked file graph for one package target. */
    bundle(package_: PackageId, target: TargetId): ArtifactKey {
        return { kind: "bundle", package: package_, target };
    },

    /** Program for one package target. */
    program(package_: PackageId, target: TargetId): ArtifactKey {
        return { kind: "program", package: package_, target };
    },

    /** Linked product assembled from configured target artifacts. */
    product(package_: PackageId, product: ProductId): ArtifactKey {
        return { kind: "product", package: package_, product };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: ArtifactKey): void {
        encodeArtifactKey(writer, value);
    },

    /** Decode one ArtifactKey. */
    decode(reader: BinaryReader): ArtifactKey {
        return decodeArtifactKey(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ArtifactKey): Json {
        return toJsonArtifactKey(value);
    },

    /** Return one ArtifactKey from one JSON value. */
    fromJson(value: Json): ArtifactKey {
        return fromJsonArtifactKey(value);
    },
};

/** Encode one ArtifactKey. */
export function encodeArtifactKey(writer: BinaryWriter, value: ArtifactKey): void {
    switch (value.kind) {
        case "build":
            writer.writeUnsigned(0);
            encodeTargetId(writer, value.target);
            return;
        case "dirParsed":
            writer.writeUnsigned(1);
            encodeModuleId(writer, value.module);
            return;
        case "data":
            writer.writeUnsigned(2);
            encodeModuleId(writer, value.module);
            return;
        case "dirBound":
            writer.writeUnsigned(3);
            encodeModuleId(writer, value.module);
            encodeProfileId(writer, value.profile);
            return;
        case "environmentBound":
            writer.writeUnsigned(4);
            encodeProfileId(writer, value.profile);
            return;
        case "moduleGraph":
            writer.writeUnsigned(5);
            encodeProfileId(writer, value.profile);
            return;
        case "dirImported":
            writer.writeUnsigned(6);
            encodeModuleId(writer, value.module);
            encodeProfileId(writer, value.profile);
            return;
        case "dirExpanded":
            writer.writeUnsigned(7);
            encodeModuleId(writer, value.module);
            encodeProfileId(writer, value.profile);
            return;
        case "dirExported":
            writer.writeUnsigned(8);
            encodeModuleId(writer, value.module);
            encodeProfileId(writer, value.profile);
            return;
        case "dirResolved":
            writer.writeUnsigned(9);
            encodeModuleId(writer, value.module);
            encodeProfileId(writer, value.profile);
            return;
        case "dirDeclared":
            writer.writeUnsigned(10);
            encodeModuleId(writer, value.module);
            encodeProfileId(writer, value.profile);
            return;
        case "environmentDeclared":
            writer.writeUnsigned(11);
            encodeProfileId(writer, value.profile);
            return;
        case "dirElaborated":
            writer.writeUnsigned(12);
            encodeModuleId(writer, value.module);
            encodeProfileId(writer, value.profile);
            return;
        case "dirChecked":
            writer.writeUnsigned(13);
            encodeModuleId(writer, value.module);
            encodeProfileId(writer, value.profile);
            return;
        case "dirMaterialized":
            writer.writeUnsigned(14);
            encodeModuleId(writer, value.module);
            encodeProfileId(writer, value.profile);
            return;
        case "mirLowered":
            writer.writeUnsigned(15);
            encodeModuleId(writer, value.module);
            encodeProfileId(writer, value.profile);
            encodeTargetId(writer, value.target);
            return;
        case "mirVerified":
            writer.writeUnsigned(16);
            encodeModuleId(writer, value.module);
            encodeProfileId(writer, value.profile);
            encodeTargetId(writer, value.target);
            return;
        case "mirElaborated":
            writer.writeUnsigned(17);
            encodeModuleId(writer, value.module);
            encodeProfileId(writer, value.profile);
            encodeTargetId(writer, value.target);
            return;
        case "mirAnalyzed":
            writer.writeUnsigned(18);
            encodeModuleId(writer, value.module);
            encodeProfileId(writer, value.profile);
            encodeTargetId(writer, value.target);
            return;
        case "mirOptimized":
            writer.writeUnsigned(19);
            encodeModuleId(writer, value.module);
            encodeProfileId(writer, value.profile);
            encodeTargetId(writer, value.target);
            return;
        case "programAnalysis":
            writer.writeUnsigned(20);
            encodeProfileId(writer, value.profile);
            encodeTargetId(writer, value.target);
            return;
        case "moduleIndex":
            writer.writeUnsigned(21);
            encodeModuleId(writer, value.module);
            encodeProfileId(writer, value.profile);
            encodeIndexKind(writer, value.kindValue);
            return;
        case "programIndex":
            writer.writeUnsigned(22);
            encodeProfileId(writer, value.profile);
            encodeIndexKind(writer, value.kindValue);
            return;
        case "moduleLinted":
            writer.writeUnsigned(23);
            encodeModuleId(writer, value.module);
            encodeProfileId(writer, value.profile);
            encodeTargetId(writer, value.target);
            return;
        case "programLinted":
            writer.writeUnsigned(24);
            encodeProfileId(writer, value.profile);
            encodeTargetId(writer, value.target);
            return;
        case "script":
            writer.writeUnsigned(25);
            encodeModuleId(writer, value.module);
            encodeTargetId(writer, value.target);
            return;
        case "object":
            writer.writeUnsigned(26);
            encodeModuleId(writer, value.module);
            encodeTargetId(writer, value.target);
            return;
        case "asset":
            writer.writeUnsigned(27);
            encodeModuleId(writer, value.module);
            encodeTargetId(writer, value.target);
            return;
        case "bundle":
            writer.writeUnsigned(28);
            encodePackageId(writer, value.package);
            encodeTargetId(writer, value.target);
            return;
        case "program":
            writer.writeUnsigned(29);
            encodePackageId(writer, value.package);
            encodeTargetId(writer, value.target);
            return;
        case "product":
            writer.writeUnsigned(30);
            encodePackageId(writer, value.package);
            encodeProductId(writer, value.product);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one ArtifactKey. */
export function decodeArtifactKey(reader: BinaryReader): ArtifactKey {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const target = decodeTargetId(reader);

            return {
                kind: "build",
                target,
            };
        }
        case 1: {
            const module_ = decodeModuleId(reader);

            return {
                kind: "dirParsed",
                module: module_,
            };
        }
        case 2: {
            const module_ = decodeModuleId(reader);

            return {
                kind: "data",
                module: module_,
            };
        }
        case 3: {
            const module_ = decodeModuleId(reader);
            const profile = decodeProfileId(reader);

            return {
                kind: "dirBound",
                module: module_,
                profile,
            };
        }
        case 4: {
            const profile = decodeProfileId(reader);

            return {
                kind: "environmentBound",
                profile,
            };
        }
        case 5: {
            const profile = decodeProfileId(reader);

            return {
                kind: "moduleGraph",
                profile,
            };
        }
        case 6: {
            const module_ = decodeModuleId(reader);
            const profile = decodeProfileId(reader);

            return {
                kind: "dirImported",
                module: module_,
                profile,
            };
        }
        case 7: {
            const module_ = decodeModuleId(reader);
            const profile = decodeProfileId(reader);

            return {
                kind: "dirExpanded",
                module: module_,
                profile,
            };
        }
        case 8: {
            const module_ = decodeModuleId(reader);
            const profile = decodeProfileId(reader);

            return {
                kind: "dirExported",
                module: module_,
                profile,
            };
        }
        case 9: {
            const module_ = decodeModuleId(reader);
            const profile = decodeProfileId(reader);

            return {
                kind: "dirResolved",
                module: module_,
                profile,
            };
        }
        case 10: {
            const module_ = decodeModuleId(reader);
            const profile = decodeProfileId(reader);

            return {
                kind: "dirDeclared",
                module: module_,
                profile,
            };
        }
        case 11: {
            const profile = decodeProfileId(reader);

            return {
                kind: "environmentDeclared",
                profile,
            };
        }
        case 12: {
            const module_ = decodeModuleId(reader);
            const profile = decodeProfileId(reader);

            return {
                kind: "dirElaborated",
                module: module_,
                profile,
            };
        }
        case 13: {
            const module_ = decodeModuleId(reader);
            const profile = decodeProfileId(reader);

            return {
                kind: "dirChecked",
                module: module_,
                profile,
            };
        }
        case 14: {
            const module_ = decodeModuleId(reader);
            const profile = decodeProfileId(reader);

            return {
                kind: "dirMaterialized",
                module: module_,
                profile,
            };
        }
        case 15: {
            const module_ = decodeModuleId(reader);
            const profile = decodeProfileId(reader);
            const target = decodeTargetId(reader);

            return {
                kind: "mirLowered",
                module: module_,
                profile,
                target,
            };
        }
        case 16: {
            const module_ = decodeModuleId(reader);
            const profile = decodeProfileId(reader);
            const target = decodeTargetId(reader);

            return {
                kind: "mirVerified",
                module: module_,
                profile,
                target,
            };
        }
        case 17: {
            const module_ = decodeModuleId(reader);
            const profile = decodeProfileId(reader);
            const target = decodeTargetId(reader);

            return {
                kind: "mirElaborated",
                module: module_,
                profile,
                target,
            };
        }
        case 18: {
            const module_ = decodeModuleId(reader);
            const profile = decodeProfileId(reader);
            const target = decodeTargetId(reader);

            return {
                kind: "mirAnalyzed",
                module: module_,
                profile,
                target,
            };
        }
        case 19: {
            const module_ = decodeModuleId(reader);
            const profile = decodeProfileId(reader);
            const target = decodeTargetId(reader);

            return {
                kind: "mirOptimized",
                module: module_,
                profile,
                target,
            };
        }
        case 20: {
            const profile = decodeProfileId(reader);
            const target = decodeTargetId(reader);

            return {
                kind: "programAnalysis",
                profile,
                target,
            };
        }
        case 21: {
            const module_ = decodeModuleId(reader);
            const profile = decodeProfileId(reader);
            const kindValue = decodeIndexKind(reader);

            return {
                kind: "moduleIndex",
                module: module_,
                profile,
                kindValue,
            };
        }
        case 22: {
            const profile = decodeProfileId(reader);
            const kindValue = decodeIndexKind(reader);

            return {
                kind: "programIndex",
                profile,
                kindValue,
            };
        }
        case 23: {
            const module_ = decodeModuleId(reader);
            const profile = decodeProfileId(reader);
            const target = decodeTargetId(reader);

            return {
                kind: "moduleLinted",
                module: module_,
                profile,
                target,
            };
        }
        case 24: {
            const profile = decodeProfileId(reader);
            const target = decodeTargetId(reader);

            return {
                kind: "programLinted",
                profile,
                target,
            };
        }
        case 25: {
            const module_ = decodeModuleId(reader);
            const target = decodeTargetId(reader);

            return {
                kind: "script",
                module: module_,
                target,
            };
        }
        case 26: {
            const module_ = decodeModuleId(reader);
            const target = decodeTargetId(reader);

            return {
                kind: "object",
                module: module_,
                target,
            };
        }
        case 27: {
            const module_ = decodeModuleId(reader);
            const target = decodeTargetId(reader);

            return {
                kind: "asset",
                module: module_,
                target,
            };
        }
        case 28: {
            const package_ = decodePackageId(reader);
            const target = decodeTargetId(reader);

            return {
                kind: "bundle",
                package: package_,
                target,
            };
        }
        case 29: {
            const package_ = decodePackageId(reader);
            const target = decodeTargetId(reader);

            return {
                kind: "program",
                package: package_,
                target,
            };
        }
        case 30: {
            const package_ = decodePackageId(reader);
            const product = decodeProductId(reader);

            return {
                kind: "product",
                package: package_,
                product,
            };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one ArtifactKey. */
export function toJsonArtifactKey(value: ArtifactKey): Json {
    switch (value.kind) {
        case "build":
            return {
                kind: "build",
                target: toJsonTargetId(value.target),
            };
        case "dirParsed":
            return {
                kind: "dirParsed",
                module: toJsonModuleId(value.module),
            };
        case "data":
            return {
                kind: "data",
                module: toJsonModuleId(value.module),
            };
        case "dirBound":
            return {
                kind: "dirBound",
                module: toJsonModuleId(value.module),
                profile: toJsonProfileId(value.profile),
            };
        case "environmentBound":
            return {
                kind: "environmentBound",
                profile: toJsonProfileId(value.profile),
            };
        case "moduleGraph":
            return {
                kind: "moduleGraph",
                profile: toJsonProfileId(value.profile),
            };
        case "dirImported":
            return {
                kind: "dirImported",
                module: toJsonModuleId(value.module),
                profile: toJsonProfileId(value.profile),
            };
        case "dirExpanded":
            return {
                kind: "dirExpanded",
                module: toJsonModuleId(value.module),
                profile: toJsonProfileId(value.profile),
            };
        case "dirExported":
            return {
                kind: "dirExported",
                module: toJsonModuleId(value.module),
                profile: toJsonProfileId(value.profile),
            };
        case "dirResolved":
            return {
                kind: "dirResolved",
                module: toJsonModuleId(value.module),
                profile: toJsonProfileId(value.profile),
            };
        case "dirDeclared":
            return {
                kind: "dirDeclared",
                module: toJsonModuleId(value.module),
                profile: toJsonProfileId(value.profile),
            };
        case "environmentDeclared":
            return {
                kind: "environmentDeclared",
                profile: toJsonProfileId(value.profile),
            };
        case "dirElaborated":
            return {
                kind: "dirElaborated",
                module: toJsonModuleId(value.module),
                profile: toJsonProfileId(value.profile),
            };
        case "dirChecked":
            return {
                kind: "dirChecked",
                module: toJsonModuleId(value.module),
                profile: toJsonProfileId(value.profile),
            };
        case "dirMaterialized":
            return {
                kind: "dirMaterialized",
                module: toJsonModuleId(value.module),
                profile: toJsonProfileId(value.profile),
            };
        case "mirLowered":
            return {
                kind: "mirLowered",
                module: toJsonModuleId(value.module),
                profile: toJsonProfileId(value.profile),
                target: toJsonTargetId(value.target),
            };
        case "mirVerified":
            return {
                kind: "mirVerified",
                module: toJsonModuleId(value.module),
                profile: toJsonProfileId(value.profile),
                target: toJsonTargetId(value.target),
            };
        case "mirElaborated":
            return {
                kind: "mirElaborated",
                module: toJsonModuleId(value.module),
                profile: toJsonProfileId(value.profile),
                target: toJsonTargetId(value.target),
            };
        case "mirAnalyzed":
            return {
                kind: "mirAnalyzed",
                module: toJsonModuleId(value.module),
                profile: toJsonProfileId(value.profile),
                target: toJsonTargetId(value.target),
            };
        case "mirOptimized":
            return {
                kind: "mirOptimized",
                module: toJsonModuleId(value.module),
                profile: toJsonProfileId(value.profile),
                target: toJsonTargetId(value.target),
            };
        case "programAnalysis":
            return {
                kind: "programAnalysis",
                profile: toJsonProfileId(value.profile),
                target: toJsonTargetId(value.target),
            };
        case "moduleIndex":
            return {
                kind: "moduleIndex",
                module: toJsonModuleId(value.module),
                profile: toJsonProfileId(value.profile),
                kindValue: toJsonIndexKind(value.kindValue),
            };
        case "programIndex":
            return {
                kind: "programIndex",
                profile: toJsonProfileId(value.profile),
                kindValue: toJsonIndexKind(value.kindValue),
            };
        case "moduleLinted":
            return {
                kind: "moduleLinted",
                module: toJsonModuleId(value.module),
                profile: toJsonProfileId(value.profile),
                target: toJsonTargetId(value.target),
            };
        case "programLinted":
            return {
                kind: "programLinted",
                profile: toJsonProfileId(value.profile),
                target: toJsonTargetId(value.target),
            };
        case "script":
            return {
                kind: "script",
                module: toJsonModuleId(value.module),
                target: toJsonTargetId(value.target),
            };
        case "object":
            return {
                kind: "object",
                module: toJsonModuleId(value.module),
                target: toJsonTargetId(value.target),
            };
        case "asset":
            return {
                kind: "asset",
                module: toJsonModuleId(value.module),
                target: toJsonTargetId(value.target),
            };
        case "bundle":
            return {
                kind: "bundle",
                package: toJsonPackageId(value.package),
                target: toJsonTargetId(value.target),
            };
        case "program":
            return {
                kind: "program",
                package: toJsonPackageId(value.package),
                target: toJsonTargetId(value.target),
            };
        case "product":
            return {
                kind: "product",
                package: toJsonPackageId(value.package),
                product: toJsonProductId(value.product),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one ArtifactKey from one JSON value. */
export function fromJsonArtifactKey(value: Json): ArtifactKey {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "build":
            return {
                kind,
                target: fromJsonTargetId(jsonField(object, "target")),
            };
        case "dirParsed":
            return {
                kind,
                module: fromJsonModuleId(jsonField(object, "module")),
            };
        case "data":
            return {
                kind,
                module: fromJsonModuleId(jsonField(object, "module")),
            };
        case "dirBound":
            return {
                kind,
                module: fromJsonModuleId(jsonField(object, "module")),
                profile: fromJsonProfileId(jsonField(object, "profile")),
            };
        case "environmentBound":
            return {
                kind,
                profile: fromJsonProfileId(jsonField(object, "profile")),
            };
        case "moduleGraph":
            return {
                kind,
                profile: fromJsonProfileId(jsonField(object, "profile")),
            };
        case "dirImported":
            return {
                kind,
                module: fromJsonModuleId(jsonField(object, "module")),
                profile: fromJsonProfileId(jsonField(object, "profile")),
            };
        case "dirExpanded":
            return {
                kind,
                module: fromJsonModuleId(jsonField(object, "module")),
                profile: fromJsonProfileId(jsonField(object, "profile")),
            };
        case "dirExported":
            return {
                kind,
                module: fromJsonModuleId(jsonField(object, "module")),
                profile: fromJsonProfileId(jsonField(object, "profile")),
            };
        case "dirResolved":
            return {
                kind,
                module: fromJsonModuleId(jsonField(object, "module")),
                profile: fromJsonProfileId(jsonField(object, "profile")),
            };
        case "dirDeclared":
            return {
                kind,
                module: fromJsonModuleId(jsonField(object, "module")),
                profile: fromJsonProfileId(jsonField(object, "profile")),
            };
        case "environmentDeclared":
            return {
                kind,
                profile: fromJsonProfileId(jsonField(object, "profile")),
            };
        case "dirElaborated":
            return {
                kind,
                module: fromJsonModuleId(jsonField(object, "module")),
                profile: fromJsonProfileId(jsonField(object, "profile")),
            };
        case "dirChecked":
            return {
                kind,
                module: fromJsonModuleId(jsonField(object, "module")),
                profile: fromJsonProfileId(jsonField(object, "profile")),
            };
        case "dirMaterialized":
            return {
                kind,
                module: fromJsonModuleId(jsonField(object, "module")),
                profile: fromJsonProfileId(jsonField(object, "profile")),
            };
        case "mirLowered":
            return {
                kind,
                module: fromJsonModuleId(jsonField(object, "module")),
                profile: fromJsonProfileId(jsonField(object, "profile")),
                target: fromJsonTargetId(jsonField(object, "target")),
            };
        case "mirVerified":
            return {
                kind,
                module: fromJsonModuleId(jsonField(object, "module")),
                profile: fromJsonProfileId(jsonField(object, "profile")),
                target: fromJsonTargetId(jsonField(object, "target")),
            };
        case "mirElaborated":
            return {
                kind,
                module: fromJsonModuleId(jsonField(object, "module")),
                profile: fromJsonProfileId(jsonField(object, "profile")),
                target: fromJsonTargetId(jsonField(object, "target")),
            };
        case "mirAnalyzed":
            return {
                kind,
                module: fromJsonModuleId(jsonField(object, "module")),
                profile: fromJsonProfileId(jsonField(object, "profile")),
                target: fromJsonTargetId(jsonField(object, "target")),
            };
        case "mirOptimized":
            return {
                kind,
                module: fromJsonModuleId(jsonField(object, "module")),
                profile: fromJsonProfileId(jsonField(object, "profile")),
                target: fromJsonTargetId(jsonField(object, "target")),
            };
        case "programAnalysis":
            return {
                kind,
                profile: fromJsonProfileId(jsonField(object, "profile")),
                target: fromJsonTargetId(jsonField(object, "target")),
            };
        case "moduleIndex":
            return {
                kind,
                module: fromJsonModuleId(jsonField(object, "module")),
                profile: fromJsonProfileId(jsonField(object, "profile")),
                kindValue: fromJsonIndexKind(jsonField(object, "kindValue")),
            };
        case "programIndex":
            return {
                kind,
                profile: fromJsonProfileId(jsonField(object, "profile")),
                kindValue: fromJsonIndexKind(jsonField(object, "kindValue")),
            };
        case "moduleLinted":
            return {
                kind,
                module: fromJsonModuleId(jsonField(object, "module")),
                profile: fromJsonProfileId(jsonField(object, "profile")),
                target: fromJsonTargetId(jsonField(object, "target")),
            };
        case "programLinted":
            return {
                kind,
                profile: fromJsonProfileId(jsonField(object, "profile")),
                target: fromJsonTargetId(jsonField(object, "target")),
            };
        case "script":
            return {
                kind,
                module: fromJsonModuleId(jsonField(object, "module")),
                target: fromJsonTargetId(jsonField(object, "target")),
            };
        case "object":
            return {
                kind,
                module: fromJsonModuleId(jsonField(object, "module")),
                target: fromJsonTargetId(jsonField(object, "target")),
            };
        case "asset":
            return {
                kind,
                module: fromJsonModuleId(jsonField(object, "module")),
                target: fromJsonTargetId(jsonField(object, "target")),
            };
        case "bundle":
            return {
                kind,
                package: fromJsonPackageId(jsonField(object, "package")),
                target: fromJsonTargetId(jsonField(object, "target")),
            };
        case "program":
            return {
                kind,
                package: fromJsonPackageId(jsonField(object, "package")),
                target: fromJsonTargetId(jsonField(object, "target")),
            };
        case "product":
            return {
                kind,
                package: fromJsonPackageId(jsonField(object, "package")),
                product: fromJsonProductId(jsonField(object, "product")),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}
