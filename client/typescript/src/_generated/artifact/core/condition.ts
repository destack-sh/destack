/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, jsonArray, jsonField, jsonObject, jsonOptional, jsonString } from "../../../protocol/serde.js";
import type { Host } from "./target.js";
import type { Platform } from "./target.js";
import type { Runtime } from "./target.js";
import { decodeHost, encodeHost, fromJsonHost, toJsonHost } from "./target.js";
import { decodePlatform, encodePlatform, fromJsonPlatform, toJsonPlatform } from "./target.js";
import { decodeRuntime, encodeRuntime, fromJsonRuntime, toJsonRuntime } from "./target.js";

/** Active source graph and runtime selection conditions. */
export type ConditionSet = {
    /** Active source graph modes. */
    readonly modes: ReadonlyArray<string>;
    /** Active source graph roles. */
    readonly roles: ReadonlyArray<string>;
    /** Active optional features. */
    readonly features: ReadonlyArray<string>;
    /** Active source graph tags. */
    readonly tags: ReadonlyArray<string>;
    /** Active build target. */
    readonly target?: string;
    /** Active product. */
    readonly product?: string;
    /** Active package release stage. */
    readonly stage?: string;
    /** Active target platform. */
    readonly platform?: Platform;
    /** Active host environment. */
    readonly host?: Host;
    /** Active runtime. */
    readonly runtime?: Runtime;
};

export const ConditionSet = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ConditionSet): void {
        encodeConditionSet(writer, value);
    },

    /** Decode one ConditionSet. */
    decode(reader: BinaryReader): ConditionSet {
        return decodeConditionSet(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ConditionSet): Json {
        return toJsonConditionSet(value);
    },

    /** Return one ConditionSet from one JSON value. */
    fromJson(value: Json): ConditionSet {
        return fromJsonConditionSet(value);
    },
};

/** Encode one ConditionSet. */
export function encodeConditionSet(writer: BinaryWriter, value: ConditionSet): void {
    writer.writeUnsigned(value.modes.length);
    for (const item0 of value.modes) {
        writer.writeString(item0);
    }
    writer.writeUnsigned(value.roles.length);
    for (const item1 of value.roles) {
        writer.writeString(item1);
    }
    writer.writeUnsigned(value.features.length);
    for (const item2 of value.features) {
        writer.writeString(item2);
    }
    writer.writeUnsigned(value.tags.length);
    for (const item3 of value.tags) {
        writer.writeString(item3);
    }
    writer.writeOption(value.target, (value4) => {
        writer.writeString(value4);
    });
    writer.writeOption(value.product, (value5) => {
        writer.writeString(value5);
    });
    writer.writeOption(value.stage, (value6) => {
        writer.writeString(value6);
    });
    writer.writeOption(value.platform, (value7) => {
        encodePlatform(writer, value7);
    });
    writer.writeOption(value.host, (value8) => {
        encodeHost(writer, value8);
    });
    writer.writeOption(value.runtime, (value9) => {
        encodeRuntime(writer, value9);
    });
}

/** Decode one ConditionSet. */
export function decodeConditionSet(reader: BinaryReader): ConditionSet {
    const modes = (() => { const length0 = reader.readNumber(); const items0: Array<string> = []; for (let index = 0; index < length0; index += 1) { items0.push(reader.readString()); } return items0; })();
    const roles = (() => { const length1 = reader.readNumber(); const items1: Array<string> = []; for (let index = 0; index < length1; index += 1) { items1.push(reader.readString()); } return items1; })();
    const features = (() => { const length2 = reader.readNumber(); const items2: Array<string> = []; for (let index = 0; index < length2; index += 1) { items2.push(reader.readString()); } return items2; })();
    const tags = (() => { const length3 = reader.readNumber(); const items3: Array<string> = []; for (let index = 0; index < length3; index += 1) { items3.push(reader.readString()); } return items3; })();
    const target = reader.readOption(() => reader.readString());
    const product = reader.readOption(() => reader.readString());
    const stage = reader.readOption(() => reader.readString());
    const platform = reader.readOption(() => decodePlatform(reader));
    const host = reader.readOption(() => decodeHost(reader));
    const runtime = reader.readOption(() => decodeRuntime(reader));

    return {
        modes,
        roles,
        features,
        tags,
        ...(target === undefined ? {} : { target }),
        ...(product === undefined ? {} : { product }),
        ...(stage === undefined ? {} : { stage }),
        ...(platform === undefined ? {} : { platform }),
        ...(host === undefined ? {} : { host }),
        ...(runtime === undefined ? {} : { runtime }),
    };
}

/** Return one JSON value for one ConditionSet. */
export function toJsonConditionSet(value: ConditionSet): Json {
    return {
        modes: value.modes.map((item0) => item0),
        roles: value.roles.map((item0) => item0),
        features: value.features.map((item0) => item0),
        tags: value.tags.map((item0) => item0),
        ...(value.target === undefined ? {} : { target: value.target }),
        ...(value.product === undefined ? {} : { product: value.product }),
        ...(value.stage === undefined ? {} : { stage: value.stage }),
        ...(value.platform === undefined ? {} : { platform: toJsonPlatform(value.platform) }),
        ...(value.host === undefined ? {} : { host: toJsonHost(value.host) }),
        ...(value.runtime === undefined ? {} : { runtime: toJsonRuntime(value.runtime) }),
    };
}

/** Return one ConditionSet from one JSON value. */
export function fromJsonConditionSet(value: Json): ConditionSet {
    const object = jsonObject(value);

    return {
        modes: jsonArray(jsonField(object, "modes")).map((item0) => jsonString(item0)),
        roles: jsonArray(jsonField(object, "roles")).map((item0) => jsonString(item0)),
        features: jsonArray(jsonField(object, "features")).map((item0) => jsonString(item0)),
        tags: jsonArray(jsonField(object, "tags")).map((item0) => jsonString(item0)),
        target: jsonOptional(object, "target", (value) => jsonString(value)),
        product: jsonOptional(object, "product", (value) => jsonString(value)),
        stage: jsonOptional(object, "stage", (value) => jsonString(value)),
        platform: jsonOptional(object, "platform", (value) => fromJsonPlatform(value)),
        host: jsonOptional(object, "host", (value) => fromJsonHost(value)),
        runtime: jsonOptional(object, "runtime", (value) => fromJsonRuntime(value)),
    };
}
