/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, jsonArray, jsonField, jsonInteger, jsonObject, jsonOptional } from "../../protocol/serde.js";
import type { GlobalSymbolId } from "../dir/symbol/symbol.js";
import type { ModuleId } from "../source/file/model/module.js";
import type { ProfileId } from "../source/file/model/profile.js";
import { decodeGlobalSymbolId, encodeGlobalSymbolId, fromJsonGlobalSymbolId, toJsonGlobalSymbolId } from "../dir/symbol/symbol.js";
import { decodeModuleId, encodeModuleId, fromJsonModuleId, toJsonModuleId } from "../source/file/model/module.js";
import { decodeProfileId, encodeProfileId, fromJsonProfileId, toJsonProfileId } from "../source/file/model/profile.js";

/** One exported extension of a target declared in its own package. */
export type Implementation = {
    /** The implemented interface. */
    readonly interface: GlobalSymbolId;
    /** The implementing extension symbol. */
    readonly symbol: GlobalSymbolId;
    /** The implementing target root, absent for blankets. */
    readonly root?: GlobalSymbolId;
};

export const Implementation = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: Implementation): void {
        encodeImplementation(writer, value);
    },

    /** Decode one Implementation. */
    decode(reader: BinaryReader): Implementation {
        return decodeImplementation(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Implementation): Json {
        return toJsonImplementation(value);
    },

    /** Return one Implementation from one JSON value. */
    fromJson(value: Json): Implementation {
        return fromJsonImplementation(value);
    },
};

/** Encode one Implementation. */
export function encodeImplementation(writer: BinaryWriter, value: Implementation): void {
    encodeGlobalSymbolId(writer, value.interface);
    encodeGlobalSymbolId(writer, value.symbol);
    writer.writeOption(value.root, (value2) => {
        encodeGlobalSymbolId(writer, value2);
    });
}

/** Decode one Implementation. */
export function decodeImplementation(reader: BinaryReader): Implementation {
    const interface_ = decodeGlobalSymbolId(reader);
    const symbol_ = decodeGlobalSymbolId(reader);
    const root = reader.readOption(() => decodeGlobalSymbolId(reader));

    return {
        interface: interface_,
        symbol: symbol_,
        ...(root === undefined ? {} : { root }),
    };
}

/** Return one JSON value for one Implementation. */
export function toJsonImplementation(value: Implementation): Json {
    return {
        interface: toJsonGlobalSymbolId(value.interface),
        symbol: toJsonGlobalSymbolId(value.symbol),
        ...(value.root === undefined ? {} : { root: toJsonGlobalSymbolId(value.root) }),
    };
}

/** Return one Implementation from one JSON value. */
export function fromJsonImplementation(value: Json): Implementation {
    const object = jsonObject(value);

    return {
        interface: fromJsonGlobalSymbolId(jsonField(object, "interface")),
        symbol: fromJsonGlobalSymbolId(jsonField(object, "symbol")),
        root: jsonOptional(object, "root", (value) => fromJsonGlobalSymbolId(value)),
    };
}

/** Dense per-module edge targets in compressed row storage. */
export type ModuleEdges = {
    /** Per-module target start offsets into `targets`. */
    readonly offsets: ReadonlyArray<number>;
    /** Edge targets as dense module indexes. */
    readonly targets: ReadonlyArray<number>;
};

export const ModuleEdges = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ModuleEdges): void {
        encodeModuleEdges(writer, value);
    },

    /** Decode one ModuleEdges. */
    decode(reader: BinaryReader): ModuleEdges {
        return decodeModuleEdges(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ModuleEdges): Json {
        return toJsonModuleEdges(value);
    },

    /** Return one ModuleEdges from one JSON value. */
    fromJson(value: Json): ModuleEdges {
        return fromJsonModuleEdges(value);
    },
};

/** Encode one ModuleEdges. */
export function encodeModuleEdges(writer: BinaryWriter, value: ModuleEdges): void {
    writer.writeUnsigned(value.offsets.length);
    for (const item0 of value.offsets) {
        writer.writeUnsigned(item0);
    }
    writer.writeUnsigned(value.targets.length);
    for (const item1 of value.targets) {
        writer.writeUnsigned(item1);
    }
}

/** Decode one ModuleEdges. */
export function decodeModuleEdges(reader: BinaryReader): ModuleEdges {
    const offsets = (() => { const length0 = reader.readNumber(); const items0: Array<number> = []; for (let index = 0; index < length0; index += 1) { items0.push(reader.readNumber()); } return items0; })();
    const targets = (() => { const length1 = reader.readNumber(); const items1: Array<number> = []; for (let index = 0; index < length1; index += 1) { items1.push(reader.readNumber()); } return items1; })();

    return {
        offsets,
        targets,
    };
}

/** Return one JSON value for one ModuleEdges. */
export function toJsonModuleEdges(value: ModuleEdges): Json {
    return {
        offsets: value.offsets.map((item0) => item0),
        targets: value.targets.map((item0) => item0),
    };
}

/** Return one ModuleEdges from one JSON value. */
export function fromJsonModuleEdges(value: Json): ModuleEdges {
    const object = jsonObject(value);

    return {
        offsets: jsonArray(jsonField(object, "offsets")).map((item0) => jsonInteger(item0)),
        targets: jsonArray(jsonField(object, "targets")).map((item0) => jsonInteger(item0)),
    };
}

/** Import graph over the modules of one profile. */
export type ModuleGraph = {
    /** The profile this graph belongs to. */
    readonly profile: ProfileId;
    /** Modules sorted by stable id. */
    readonly modules: ReadonlyArray<ModuleId>;
    /** Import edges over the module universe. */
    readonly edges: ModuleEdges;
    /** All interface implementations declared across the graph's modules, sorted by interface. */
    readonly implementations: ReadonlyArray<Implementation>;
};

export const ModuleGraph = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ModuleGraph): void {
        encodeModuleGraph(writer, value);
    },

    /** Decode one ModuleGraph. */
    decode(reader: BinaryReader): ModuleGraph {
        return decodeModuleGraph(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ModuleGraph): Json {
        return toJsonModuleGraph(value);
    },

    /** Return one ModuleGraph from one JSON value. */
    fromJson(value: Json): ModuleGraph {
        return fromJsonModuleGraph(value);
    },
};

/** Encode one ModuleGraph. */
export function encodeModuleGraph(writer: BinaryWriter, value: ModuleGraph): void {
    encodeProfileId(writer, value.profile);
    writer.writeUnsigned(value.modules.length);
    for (const item1 of value.modules) {
        encodeModuleId(writer, item1);
    }
    encodeModuleEdges(writer, value.edges);
    writer.writeUnsigned(value.implementations.length);
    for (const item3 of value.implementations) {
        encodeImplementation(writer, item3);
    }
}

/** Decode one ModuleGraph. */
export function decodeModuleGraph(reader: BinaryReader): ModuleGraph {
    const profile = decodeProfileId(reader);
    const modules = (() => { const length1 = reader.readNumber(); const items1: Array<ModuleId> = []; for (let index = 0; index < length1; index += 1) { items1.push(decodeModuleId(reader)); } return items1; })();
    const edges = decodeModuleEdges(reader);
    const implementations = (() => { const length3 = reader.readNumber(); const items3: Array<Implementation> = []; for (let index = 0; index < length3; index += 1) { items3.push(decodeImplementation(reader)); } return items3; })();

    return {
        profile,
        modules,
        edges,
        implementations,
    };
}

/** Return one JSON value for one ModuleGraph. */
export function toJsonModuleGraph(value: ModuleGraph): Json {
    return {
        profile: toJsonProfileId(value.profile),
        modules: value.modules.map((item0) => toJsonModuleId(item0)),
        edges: toJsonModuleEdges(value.edges),
        implementations: value.implementations.map((item0) => toJsonImplementation(item0)),
    };
}

/** Return one ModuleGraph from one JSON value. */
export function fromJsonModuleGraph(value: Json): ModuleGraph {
    const object = jsonObject(value);

    return {
        profile: fromJsonProfileId(jsonField(object, "profile")),
        modules: jsonArray(jsonField(object, "modules")).map((item0) => fromJsonModuleId(item0)),
        edges: fromJsonModuleEdges(jsonField(object, "edges")),
        implementations: jsonArray(jsonField(object, "implementations")).map((item0) => fromJsonImplementation(item0)),
    };
}
