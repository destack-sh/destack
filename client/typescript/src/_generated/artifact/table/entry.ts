/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, compareBytes, jsonArray, jsonField, jsonObject, jsonString, nestedBytes } from "../../../protocol/serde.js";
import type { Content } from "../../source/file/model/file.js";
import { decodeContent, encodeContent, fromJsonContent, toJsonContent } from "../../source/file/model/file.js";

/** One named artifact sidecar. */
export type ArtifactSidecar = {
    /** The sidecar name. */
    readonly name: string;
    /** The stable labels describing this sidecar. */
    readonly labels: Readonly<Record<string, string>>;
    /** The sidecar content. */
    readonly content: Content;
};

export const ArtifactSidecar = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ArtifactSidecar): void {
        encodeArtifactSidecar(writer, value);
    },

    /** Decode one ArtifactSidecar. */
    decode(reader: BinaryReader): ArtifactSidecar {
        return decodeArtifactSidecar(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ArtifactSidecar): Json {
        return toJsonArtifactSidecar(value);
    },

    /** Return one ArtifactSidecar from one JSON value. */
    fromJson(value: Json): ArtifactSidecar {
        return fromJsonArtifactSidecar(value);
    },
};

/** Encode one ArtifactSidecar. */
export function encodeArtifactSidecar(writer: BinaryWriter, value: ArtifactSidecar): void {
    writer.writeString(value.name);
    const entries1 = Object.entries(value.labels).map(([key1, item1]) => {
        const keyBytes = nestedBytes((writer) => {
            writer.writeString(key1);
        });
        return { key1, item1, keyBytes };
    });
    entries1.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries1.length);
    for (const entry1 of entries1) {
        writer.writeString(entry1.key1);
        writer.writeString(entry1.item1);
    }
    encodeContent(writer, value.content);
}

/** Decode one ArtifactSidecar. */
export function decodeArtifactSidecar(reader: BinaryReader): ArtifactSidecar {
    const name = reader.readString();
    const labels = (() => { const length1 = reader.readNumber(); const items1: Record<string, string> = {}; for (let index = 0; index < length1; index += 1) { const key = reader.readString(); items1[key] = reader.readString(); } return items1; })();
    const content = decodeContent(reader);

    return {
        name,
        labels,
        content,
    };
}

/** Return one JSON value for one ArtifactSidecar. */
export function toJsonArtifactSidecar(value: ArtifactSidecar): Json {
    return {
        name: value.name,
        labels: Object.fromEntries(Object.entries(value.labels).map(([key0, item0]) => [key0, item0] as const)),
        content: toJsonContent(value.content),
    };
}

/** Return one ArtifactSidecar from one JSON value. */
export function fromJsonArtifactSidecar(value: Json): ArtifactSidecar {
    const object = jsonObject(value);

    return {
        name: jsonString(jsonField(object, "name")),
        labels: Object.fromEntries(Object.entries(jsonObject(jsonField(object, "labels"))).map(([key0, item0]) => [key0, jsonString(item0)] as const)),
        content: fromJsonContent(jsonField(object, "content")),
    };
}
