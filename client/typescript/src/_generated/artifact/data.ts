/* generated by destack_client_generate, do not edit */

import {
    BinaryReader,
    BinaryWriter,
    Json,
    SerdeError,
    jsonField,
    jsonObject,
    jsonString,
} from "../../protocol/serde.js";
import type { Value } from "../serde/value.js";
import { decodeValue, encodeValue, fromJsonValue, toJsonValue } from "../serde/value.js";

/** One parsed non-code module payload. */
export type Data =
    /** One parsed JSON-like module value. */
    {
        readonly kind: "json";
        readonly json: Value;
    };

export const Data = {
    /** One parsed JSON-like module value. */
    json(json: Value): Data {
        return { kind: "json", json };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: Data): void {
        encodeData(writer, value);
    },

    /** Decode one Data. */
    decode(reader: BinaryReader): Data {
        return decodeData(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Data): Json {
        return toJsonData(value);
    },

    /** Return one Data from one JSON value. */
    fromJson(value: Json): Data {
        return fromJsonData(value);
    },
};

/** Encode one Data. */
export function encodeData(writer: BinaryWriter, value: Data): void {
    switch (value.kind) {
        case "json":
            writer.writeUnsigned(0);
            encodeValue(writer, value.json);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one Data. */
export function decodeData(reader: BinaryReader): Data {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const json = decodeValue(reader);

            return { kind: "json", json };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one Data. */
export function toJsonData(value: Data): Json {
    switch (value.kind) {
        case "json":
            return {
                kind: "json",
                json: toJsonValue(value.json),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one Data from one JSON value. */
export function fromJsonData(value: Json): Data {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "json":
            return {
                kind,
                json: fromJsonValue(jsonField(object, "json")),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}
