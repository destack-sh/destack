/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, jsonField, jsonObject, jsonString } from "../../protocol/serde.js";
import type { Asset } from "./asset.js";
import type { Build } from "./build.js";
import type { Bundle } from "./bundle.js";
import type { ComponentGraph } from "./component.js";
import type { Data } from "./data.js";
import type { DirBound } from "./dir.js";
import type { DirChecked } from "./dir.js";
import type { DirCheckedComponent } from "./dir.js";
import type { DirElaborated } from "./dir.js";
import type { DirExpanded } from "./dir.js";
import type { DirExported } from "./dir.js";
import type { DirImported } from "./dir.js";
import type { DirMaterialized } from "./dir.js";
import type { DirParsed } from "./dir.js";
import type { DirResolved } from "./dir.js";
import type { GlobalEnvironment } from "./environment.js";
import type { ModuleLinted } from "./lint.js";
import type { PackageLinted } from "./lint.js";
import type { WorkspaceLinted } from "./lint.js";
import type { MirAnalyzed } from "./mir.js";
import type { MirLowered } from "./mir.js";
import type { MirOptimized } from "./mir.js";
import type { ProgramAnalysis } from "./mir.js";
import type { MirVerified } from "./mir.js";
import type { Object } from "./object.js";
import type { PackageIndex } from "./package.js";
import type { Product } from "./product.js";
import type { ModuleQueryIndex } from "./query.js";
import type { WorkspaceQueryIndex } from "./query.js";
import type { Script } from "./script.js";
import type { Program } from "../program/program.js";
import { decodeAsset, encodeAsset, fromJsonAsset, toJsonAsset } from "./asset.js";
import { decodeBuild, encodeBuild, fromJsonBuild, toJsonBuild } from "./build.js";
import { decodeBundle, encodeBundle, fromJsonBundle, toJsonBundle } from "./bundle.js";
import { decodeComponentGraph, encodeComponentGraph, fromJsonComponentGraph, toJsonComponentGraph } from "./component.js";
import { decodeData, encodeData, fromJsonData, toJsonData } from "./data.js";
import { decodeDirBound, encodeDirBound, fromJsonDirBound, toJsonDirBound } from "./dir.js";
import { decodeDirChecked, encodeDirChecked, fromJsonDirChecked, toJsonDirChecked } from "./dir.js";
import { decodeDirCheckedComponent, encodeDirCheckedComponent, fromJsonDirCheckedComponent, toJsonDirCheckedComponent } from "./dir.js";
import { decodeDirElaborated, encodeDirElaborated, fromJsonDirElaborated, toJsonDirElaborated } from "./dir.js";
import { decodeDirExpanded, encodeDirExpanded, fromJsonDirExpanded, toJsonDirExpanded } from "./dir.js";
import { decodeDirExported, encodeDirExported, fromJsonDirExported, toJsonDirExported } from "./dir.js";
import { decodeDirImported, encodeDirImported, fromJsonDirImported, toJsonDirImported } from "./dir.js";
import { decodeDirMaterialized, encodeDirMaterialized, fromJsonDirMaterialized, toJsonDirMaterialized } from "./dir.js";
import { decodeDirParsed, encodeDirParsed, fromJsonDirParsed, toJsonDirParsed } from "./dir.js";
import { decodeDirResolved, encodeDirResolved, fromJsonDirResolved, toJsonDirResolved } from "./dir.js";
import { decodeGlobalEnvironment, encodeGlobalEnvironment, fromJsonGlobalEnvironment, toJsonGlobalEnvironment } from "./environment.js";
import { decodeModuleLinted, encodeModuleLinted, fromJsonModuleLinted, toJsonModuleLinted } from "./lint.js";
import { decodePackageLinted, encodePackageLinted, fromJsonPackageLinted, toJsonPackageLinted } from "./lint.js";
import { decodeWorkspaceLinted, encodeWorkspaceLinted, fromJsonWorkspaceLinted, toJsonWorkspaceLinted } from "./lint.js";
import { decodeMirAnalyzed, encodeMirAnalyzed, fromJsonMirAnalyzed, toJsonMirAnalyzed } from "./mir.js";
import { decodeMirLowered, encodeMirLowered, fromJsonMirLowered, toJsonMirLowered } from "./mir.js";
import { decodeMirOptimized, encodeMirOptimized, fromJsonMirOptimized, toJsonMirOptimized } from "./mir.js";
import { decodeProgramAnalysis, encodeProgramAnalysis, fromJsonProgramAnalysis, toJsonProgramAnalysis } from "./mir.js";
import { decodeMirVerified, encodeMirVerified, fromJsonMirVerified, toJsonMirVerified } from "./mir.js";
import { decodeObject, encodeObject, fromJsonObject, toJsonObject } from "./object.js";
import { decodePackageIndex, encodePackageIndex, fromJsonPackageIndex, toJsonPackageIndex } from "./package.js";
import { decodeProduct, encodeProduct, fromJsonProduct, toJsonProduct } from "./product.js";
import { decodeModuleQueryIndex, encodeModuleQueryIndex, fromJsonModuleQueryIndex, toJsonModuleQueryIndex } from "./query.js";
import { decodeWorkspaceQueryIndex, encodeWorkspaceQueryIndex, fromJsonWorkspaceQueryIndex, toJsonWorkspaceQueryIndex } from "./query.js";
import { decodeScript, encodeScript, fromJsonScript, toJsonScript } from "./script.js";
import { decodeProgram, encodeProgram, fromJsonProgram, toJsonProgram } from "../program/program.js";

/** One typed artifact payload. */
export type ArtifactPayload =
    /** Parsed module DIR. */
    | {
          readonly kind: "dirParsed";
          readonly dir_parsed: DirParsed;
      }
    /** Parsed non-code module data. */
    | {
          readonly kind: "data";
          readonly data: Data;
      }
    /** Explicit global environment for one profile. */
    | {
          readonly kind: "globalEnvironment";
          readonly global_environment: GlobalEnvironment;
      }
    /** Active dependency index for one profile. */
    | {
          readonly kind: "packageIndex";
          readonly package_index: PackageIndex;
      }
    /** Component partition for one profile. */
    | {
          readonly kind: "componentGraph";
          readonly component_graph: ComponentGraph;
      }
    /** Whole-program analysis for one profile and target. */
    | {
          readonly kind: "programAnalysis";
          readonly program_analysis: ProgramAnalysis;
      }
    /** Bound DIR. */
    | {
          readonly kind: "dirBound";
          readonly dir_bound: DirBound;
      }
    /** Imported DIR. */
    | {
          readonly kind: "dirImported";
          readonly dir_imported: DirImported;
      }
    /** Expanded DIR. */
    | {
          readonly kind: "dirExpanded";
          readonly dir_expanded: DirExpanded;
      }
    /** Exported DIR. */
    | {
          readonly kind: "dirExported";
          readonly dir_exported: DirExported;
      }
    /** Resolved DIR imports. */
    | {
          readonly kind: "dirResolved";
          readonly dir_resolved: DirResolved;
      }
    /** Checked DIR component. */
    | {
          readonly kind: "dirCheckedComponent";
          readonly dir_checked_component: DirCheckedComponent;
      }
    /** Checked DIR facade. */
    | {
          readonly kind: "dirChecked";
          readonly dir_checked: DirChecked;
      }
    /** Materialized DIR. */
    | {
          readonly kind: "dirMaterialized";
          readonly dir_materialized: DirMaterialized;
      }
    /** Elaborated DIR. */
    | {
          readonly kind: "dirElaborated";
          readonly dir_elaborated: DirElaborated;
      }
    /** Lowered MIR before optimization. */
    | {
          readonly kind: "mirLowered";
          readonly mir_lowered: MirLowered;
      }
    /** Verified MIR marker after required semantic verification. */
    | {
          readonly kind: "mirVerified";
          readonly mir_verified: MirVerified;
      }
    /** Per-module link summary for whole-program analysis. */
    | {
          readonly kind: "mirAnalyzed";
          readonly mir_analyzed: MirAnalyzed;
      }
    /** Optimized MIR. */
    | {
          readonly kind: "mirOptimized";
          readonly mir_optimized: MirOptimized;
      }
    /** Query index for one module profile. */
    | {
          readonly kind: "moduleQueryIndex";
          readonly module_query_index: ModuleQueryIndex;
      }
    /** Query index for one workspace profile. */
    | {
          readonly kind: "workspaceQueryIndex";
          readonly workspace_query_index: WorkspaceQueryIndex;
      }
    /** One structured linker input for one target. */
    | {
          readonly kind: "script";
          readonly script: Script;
      }
    /** One compiled-code linker input for one target. */
    | {
          readonly kind: "object";
          readonly object: Object;
      }
    /** One opaque linker input for one target. */
    | {
          readonly kind: "asset";
          readonly asset: Asset;
      }
    /** Target-built toolchain payload. */
    | {
          readonly kind: "build";
          readonly build: Build;
      }
    /** Linked file graph for one package target. */
    | {
          readonly kind: "bundle";
          readonly bundle: Bundle;
      }
    /** Program for one package target. */
    | {
          readonly kind: "program";
          readonly program: Program;
      }
    /** Linked product assembled from configured target artifacts. */
    | {
          readonly kind: "product";
          readonly product: Product;
      }
    /** Realized lint diagnostics for one module profile. */
    | {
          readonly kind: "moduleLinted";
          readonly module_linted: ModuleLinted;
      }
    /** Realized lint diagnostics for one package. */
    | {
          readonly kind: "packageLinted";
          readonly package_linted: PackageLinted;
      }
    /** Realized lint diagnostics for the workspace. */
    | {
          readonly kind: "workspaceLinted";
          readonly workspace_linted: WorkspaceLinted;
      }
;

export const ArtifactPayload = {
    /** Parsed module DIR. */
    dirParsed(dir_parsed: DirParsed): ArtifactPayload {
        return { kind: "dirParsed", dir_parsed };
    },

    /** Parsed non-code module data. */
    data(data: Data): ArtifactPayload {
        return { kind: "data", data };
    },

    /** Explicit global environment for one profile. */
    globalEnvironment(global_environment: GlobalEnvironment): ArtifactPayload {
        return { kind: "globalEnvironment", global_environment };
    },

    /** Active dependency index for one profile. */
    packageIndex(package_index: PackageIndex): ArtifactPayload {
        return { kind: "packageIndex", package_index };
    },

    /** Component partition for one profile. */
    componentGraph(component_graph: ComponentGraph): ArtifactPayload {
        return { kind: "componentGraph", component_graph };
    },

    /** Whole-program analysis for one profile and target. */
    programAnalysis(program_analysis: ProgramAnalysis): ArtifactPayload {
        return { kind: "programAnalysis", program_analysis };
    },

    /** Bound DIR. */
    dirBound(dir_bound: DirBound): ArtifactPayload {
        return { kind: "dirBound", dir_bound };
    },

    /** Imported DIR. */
    dirImported(dir_imported: DirImported): ArtifactPayload {
        return { kind: "dirImported", dir_imported };
    },

    /** Expanded DIR. */
    dirExpanded(dir_expanded: DirExpanded): ArtifactPayload {
        return { kind: "dirExpanded", dir_expanded };
    },

    /** Exported DIR. */
    dirExported(dir_exported: DirExported): ArtifactPayload {
        return { kind: "dirExported", dir_exported };
    },

    /** Resolved DIR imports. */
    dirResolved(dir_resolved: DirResolved): ArtifactPayload {
        return { kind: "dirResolved", dir_resolved };
    },

    /** Checked DIR component. */
    dirCheckedComponent(dir_checked_component: DirCheckedComponent): ArtifactPayload {
        return { kind: "dirCheckedComponent", dir_checked_component };
    },

    /** Checked DIR facade. */
    dirChecked(dir_checked: DirChecked): ArtifactPayload {
        return { kind: "dirChecked", dir_checked };
    },

    /** Materialized DIR. */
    dirMaterialized(dir_materialized: DirMaterialized): ArtifactPayload {
        return { kind: "dirMaterialized", dir_materialized };
    },

    /** Elaborated DIR. */
    dirElaborated(dir_elaborated: DirElaborated): ArtifactPayload {
        return { kind: "dirElaborated", dir_elaborated };
    },

    /** Lowered MIR before optimization. */
    mirLowered(mir_lowered: MirLowered): ArtifactPayload {
        return { kind: "mirLowered", mir_lowered };
    },

    /** Verified MIR marker after required semantic verification. */
    mirVerified(mir_verified: MirVerified): ArtifactPayload {
        return { kind: "mirVerified", mir_verified };
    },

    /** Per-module link summary for whole-program analysis. */
    mirAnalyzed(mir_analyzed: MirAnalyzed): ArtifactPayload {
        return { kind: "mirAnalyzed", mir_analyzed };
    },

    /** Optimized MIR. */
    mirOptimized(mir_optimized: MirOptimized): ArtifactPayload {
        return { kind: "mirOptimized", mir_optimized };
    },

    /** Query index for one module profile. */
    moduleQueryIndex(module_query_index: ModuleQueryIndex): ArtifactPayload {
        return { kind: "moduleQueryIndex", module_query_index };
    },

    /** Query index for one workspace profile. */
    workspaceQueryIndex(workspace_query_index: WorkspaceQueryIndex): ArtifactPayload {
        return { kind: "workspaceQueryIndex", workspace_query_index };
    },

    /** One structured linker input for one target. */
    script(script: Script): ArtifactPayload {
        return { kind: "script", script };
    },

    /** One compiled-code linker input for one target. */
    "object"(object_: Object): ArtifactPayload {
        return { kind: "object", object: object_ };
    },

    /** One opaque linker input for one target. */
    asset(asset: Asset): ArtifactPayload {
        return { kind: "asset", asset };
    },

    /** Target-built toolchain payload. */
    build(build: Build): ArtifactPayload {
        return { kind: "build", build };
    },

    /** Linked file graph for one package target. */
    bundle(bundle: Bundle): ArtifactPayload {
        return { kind: "bundle", bundle };
    },

    /** Program for one package target. */
    program(program: Program): ArtifactPayload {
        return { kind: "program", program };
    },

    /** Linked product assembled from configured target artifacts. */
    product(product: Product): ArtifactPayload {
        return { kind: "product", product };
    },

    /** Realized lint diagnostics for one module profile. */
    moduleLinted(module_linted: ModuleLinted): ArtifactPayload {
        return { kind: "moduleLinted", module_linted };
    },

    /** Realized lint diagnostics for one package. */
    packageLinted(package_linted: PackageLinted): ArtifactPayload {
        return { kind: "packageLinted", package_linted };
    },

    /** Realized lint diagnostics for the workspace. */
    workspaceLinted(workspace_linted: WorkspaceLinted): ArtifactPayload {
        return { kind: "workspaceLinted", workspace_linted };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: ArtifactPayload): void {
        encodeArtifactPayload(writer, value);
    },

    /** Decode one ArtifactPayload. */
    decode(reader: BinaryReader): ArtifactPayload {
        return decodeArtifactPayload(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ArtifactPayload): Json {
        return toJsonArtifactPayload(value);
    },

    /** Return one ArtifactPayload from one JSON value. */
    fromJson(value: Json): ArtifactPayload {
        return fromJsonArtifactPayload(value);
    },
};

/** Encode one ArtifactPayload. */
export function encodeArtifactPayload(writer: BinaryWriter, value: ArtifactPayload): void {
    switch (value.kind) {
        case "dirParsed":
            writer.writeUnsigned(0);
            encodeDirParsed(writer, value.dir_parsed);
            return;
        case "data":
            writer.writeUnsigned(1);
            encodeData(writer, value.data);
            return;
        case "globalEnvironment":
            writer.writeUnsigned(2);
            encodeGlobalEnvironment(writer, value.global_environment);
            return;
        case "packageIndex":
            writer.writeUnsigned(3);
            encodePackageIndex(writer, value.package_index);
            return;
        case "componentGraph":
            writer.writeUnsigned(4);
            encodeComponentGraph(writer, value.component_graph);
            return;
        case "programAnalysis":
            writer.writeUnsigned(5);
            encodeProgramAnalysis(writer, value.program_analysis);
            return;
        case "dirBound":
            writer.writeUnsigned(6);
            encodeDirBound(writer, value.dir_bound);
            return;
        case "dirImported":
            writer.writeUnsigned(7);
            encodeDirImported(writer, value.dir_imported);
            return;
        case "dirExpanded":
            writer.writeUnsigned(8);
            encodeDirExpanded(writer, value.dir_expanded);
            return;
        case "dirExported":
            writer.writeUnsigned(9);
            encodeDirExported(writer, value.dir_exported);
            return;
        case "dirResolved":
            writer.writeUnsigned(10);
            encodeDirResolved(writer, value.dir_resolved);
            return;
        case "dirCheckedComponent":
            writer.writeUnsigned(11);
            encodeDirCheckedComponent(writer, value.dir_checked_component);
            return;
        case "dirChecked":
            writer.writeUnsigned(12);
            encodeDirChecked(writer, value.dir_checked);
            return;
        case "dirMaterialized":
            writer.writeUnsigned(13);
            encodeDirMaterialized(writer, value.dir_materialized);
            return;
        case "dirElaborated":
            writer.writeUnsigned(14);
            encodeDirElaborated(writer, value.dir_elaborated);
            return;
        case "mirLowered":
            writer.writeUnsigned(15);
            encodeMirLowered(writer, value.mir_lowered);
            return;
        case "mirVerified":
            writer.writeUnsigned(16);
            encodeMirVerified(writer, value.mir_verified);
            return;
        case "mirAnalyzed":
            writer.writeUnsigned(17);
            encodeMirAnalyzed(writer, value.mir_analyzed);
            return;
        case "mirOptimized":
            writer.writeUnsigned(18);
            encodeMirOptimized(writer, value.mir_optimized);
            return;
        case "moduleQueryIndex":
            writer.writeUnsigned(19);
            encodeModuleQueryIndex(writer, value.module_query_index);
            return;
        case "workspaceQueryIndex":
            writer.writeUnsigned(20);
            encodeWorkspaceQueryIndex(writer, value.workspace_query_index);
            return;
        case "script":
            writer.writeUnsigned(21);
            encodeScript(writer, value.script);
            return;
        case "object":
            writer.writeUnsigned(22);
            encodeObject(writer, value.object);
            return;
        case "asset":
            writer.writeUnsigned(23);
            encodeAsset(writer, value.asset);
            return;
        case "build":
            writer.writeUnsigned(24);
            encodeBuild(writer, value.build);
            return;
        case "bundle":
            writer.writeUnsigned(25);
            encodeBundle(writer, value.bundle);
            return;
        case "program":
            writer.writeUnsigned(26);
            encodeProgram(writer, value.program);
            return;
        case "product":
            writer.writeUnsigned(27);
            encodeProduct(writer, value.product);
            return;
        case "moduleLinted":
            writer.writeUnsigned(28);
            encodeModuleLinted(writer, value.module_linted);
            return;
        case "packageLinted":
            writer.writeUnsigned(29);
            encodePackageLinted(writer, value.package_linted);
            return;
        case "workspaceLinted":
            writer.writeUnsigned(30);
            encodeWorkspaceLinted(writer, value.workspace_linted);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one ArtifactPayload. */
export function decodeArtifactPayload(reader: BinaryReader): ArtifactPayload {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const dir_parsed = decodeDirParsed(reader);

            return { kind: "dirParsed", dir_parsed };
        }
        case 1: {
            const data = decodeData(reader);

            return { kind: "data", data };
        }
        case 2: {
            const global_environment = decodeGlobalEnvironment(reader);

            return { kind: "globalEnvironment", global_environment };
        }
        case 3: {
            const package_index = decodePackageIndex(reader);

            return { kind: "packageIndex", package_index };
        }
        case 4: {
            const component_graph = decodeComponentGraph(reader);

            return { kind: "componentGraph", component_graph };
        }
        case 5: {
            const program_analysis = decodeProgramAnalysis(reader);

            return { kind: "programAnalysis", program_analysis };
        }
        case 6: {
            const dir_bound = decodeDirBound(reader);

            return { kind: "dirBound", dir_bound };
        }
        case 7: {
            const dir_imported = decodeDirImported(reader);

            return { kind: "dirImported", dir_imported };
        }
        case 8: {
            const dir_expanded = decodeDirExpanded(reader);

            return { kind: "dirExpanded", dir_expanded };
        }
        case 9: {
            const dir_exported = decodeDirExported(reader);

            return { kind: "dirExported", dir_exported };
        }
        case 10: {
            const dir_resolved = decodeDirResolved(reader);

            return { kind: "dirResolved", dir_resolved };
        }
        case 11: {
            const dir_checked_component = decodeDirCheckedComponent(reader);

            return { kind: "dirCheckedComponent", dir_checked_component };
        }
        case 12: {
            const dir_checked = decodeDirChecked(reader);

            return { kind: "dirChecked", dir_checked };
        }
        case 13: {
            const dir_materialized = decodeDirMaterialized(reader);

            return { kind: "dirMaterialized", dir_materialized };
        }
        case 14: {
            const dir_elaborated = decodeDirElaborated(reader);

            return { kind: "dirElaborated", dir_elaborated };
        }
        case 15: {
            const mir_lowered = decodeMirLowered(reader);

            return { kind: "mirLowered", mir_lowered };
        }
        case 16: {
            const mir_verified = decodeMirVerified(reader);

            return { kind: "mirVerified", mir_verified };
        }
        case 17: {
            const mir_analyzed = decodeMirAnalyzed(reader);

            return { kind: "mirAnalyzed", mir_analyzed };
        }
        case 18: {
            const mir_optimized = decodeMirOptimized(reader);

            return { kind: "mirOptimized", mir_optimized };
        }
        case 19: {
            const module_query_index = decodeModuleQueryIndex(reader);

            return { kind: "moduleQueryIndex", module_query_index };
        }
        case 20: {
            const workspace_query_index = decodeWorkspaceQueryIndex(reader);

            return { kind: "workspaceQueryIndex", workspace_query_index };
        }
        case 21: {
            const script = decodeScript(reader);

            return { kind: "script", script };
        }
        case 22: {
            const object_ = decodeObject(reader);

            return { kind: "object", object: object_ };
        }
        case 23: {
            const asset = decodeAsset(reader);

            return { kind: "asset", asset };
        }
        case 24: {
            const build = decodeBuild(reader);

            return { kind: "build", build };
        }
        case 25: {
            const bundle = decodeBundle(reader);

            return { kind: "bundle", bundle };
        }
        case 26: {
            const program = decodeProgram(reader);

            return { kind: "program", program };
        }
        case 27: {
            const product = decodeProduct(reader);

            return { kind: "product", product };
        }
        case 28: {
            const module_linted = decodeModuleLinted(reader);

            return { kind: "moduleLinted", module_linted };
        }
        case 29: {
            const package_linted = decodePackageLinted(reader);

            return { kind: "packageLinted", package_linted };
        }
        case 30: {
            const workspace_linted = decodeWorkspaceLinted(reader);

            return { kind: "workspaceLinted", workspace_linted };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one ArtifactPayload. */
export function toJsonArtifactPayload(value: ArtifactPayload): Json {
    switch (value.kind) {
        case "dirParsed":
            return {
                kind: "dirParsed",
                dir_parsed: toJsonDirParsed(value.dir_parsed),
            };
        case "data":
            return {
                kind: "data",
                data: toJsonData(value.data),
            };
        case "globalEnvironment":
            return {
                kind: "globalEnvironment",
                global_environment: toJsonGlobalEnvironment(value.global_environment),
            };
        case "packageIndex":
            return {
                kind: "packageIndex",
                package_index: toJsonPackageIndex(value.package_index),
            };
        case "componentGraph":
            return {
                kind: "componentGraph",
                component_graph: toJsonComponentGraph(value.component_graph),
            };
        case "programAnalysis":
            return {
                kind: "programAnalysis",
                program_analysis: toJsonProgramAnalysis(value.program_analysis),
            };
        case "dirBound":
            return {
                kind: "dirBound",
                dir_bound: toJsonDirBound(value.dir_bound),
            };
        case "dirImported":
            return {
                kind: "dirImported",
                dir_imported: toJsonDirImported(value.dir_imported),
            };
        case "dirExpanded":
            return {
                kind: "dirExpanded",
                dir_expanded: toJsonDirExpanded(value.dir_expanded),
            };
        case "dirExported":
            return {
                kind: "dirExported",
                dir_exported: toJsonDirExported(value.dir_exported),
            };
        case "dirResolved":
            return {
                kind: "dirResolved",
                dir_resolved: toJsonDirResolved(value.dir_resolved),
            };
        case "dirCheckedComponent":
            return {
                kind: "dirCheckedComponent",
                dir_checked_component: toJsonDirCheckedComponent(value.dir_checked_component),
            };
        case "dirChecked":
            return {
                kind: "dirChecked",
                dir_checked: toJsonDirChecked(value.dir_checked),
            };
        case "dirMaterialized":
            return {
                kind: "dirMaterialized",
                dir_materialized: toJsonDirMaterialized(value.dir_materialized),
            };
        case "dirElaborated":
            return {
                kind: "dirElaborated",
                dir_elaborated: toJsonDirElaborated(value.dir_elaborated),
            };
        case "mirLowered":
            return {
                kind: "mirLowered",
                mir_lowered: toJsonMirLowered(value.mir_lowered),
            };
        case "mirVerified":
            return {
                kind: "mirVerified",
                mir_verified: toJsonMirVerified(value.mir_verified),
            };
        case "mirAnalyzed":
            return {
                kind: "mirAnalyzed",
                mir_analyzed: toJsonMirAnalyzed(value.mir_analyzed),
            };
        case "mirOptimized":
            return {
                kind: "mirOptimized",
                mir_optimized: toJsonMirOptimized(value.mir_optimized),
            };
        case "moduleQueryIndex":
            return {
                kind: "moduleQueryIndex",
                module_query_index: toJsonModuleQueryIndex(value.module_query_index),
            };
        case "workspaceQueryIndex":
            return {
                kind: "workspaceQueryIndex",
                workspace_query_index: toJsonWorkspaceQueryIndex(value.workspace_query_index),
            };
        case "script":
            return {
                kind: "script",
                script: toJsonScript(value.script),
            };
        case "object":
            return {
                kind: "object",
                object: toJsonObject(value.object),
            };
        case "asset":
            return {
                kind: "asset",
                asset: toJsonAsset(value.asset),
            };
        case "build":
            return {
                kind: "build",
                build: toJsonBuild(value.build),
            };
        case "bundle":
            return {
                kind: "bundle",
                bundle: toJsonBundle(value.bundle),
            };
        case "program":
            return {
                kind: "program",
                program: toJsonProgram(value.program),
            };
        case "product":
            return {
                kind: "product",
                product: toJsonProduct(value.product),
            };
        case "moduleLinted":
            return {
                kind: "moduleLinted",
                module_linted: toJsonModuleLinted(value.module_linted),
            };
        case "packageLinted":
            return {
                kind: "packageLinted",
                package_linted: toJsonPackageLinted(value.package_linted),
            };
        case "workspaceLinted":
            return {
                kind: "workspaceLinted",
                workspace_linted: toJsonWorkspaceLinted(value.workspace_linted),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one ArtifactPayload from one JSON value. */
export function fromJsonArtifactPayload(value: Json): ArtifactPayload {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "dirParsed":
            return {
                kind,
                dir_parsed: fromJsonDirParsed(jsonField(object, "dir_parsed")),
            };
        case "data":
            return {
                kind,
                data: fromJsonData(jsonField(object, "data")),
            };
        case "globalEnvironment":
            return {
                kind,
                global_environment: fromJsonGlobalEnvironment(jsonField(object, "global_environment")),
            };
        case "packageIndex":
            return {
                kind,
                package_index: fromJsonPackageIndex(jsonField(object, "package_index")),
            };
        case "componentGraph":
            return {
                kind,
                component_graph: fromJsonComponentGraph(jsonField(object, "component_graph")),
            };
        case "programAnalysis":
            return {
                kind,
                program_analysis: fromJsonProgramAnalysis(jsonField(object, "program_analysis")),
            };
        case "dirBound":
            return {
                kind,
                dir_bound: fromJsonDirBound(jsonField(object, "dir_bound")),
            };
        case "dirImported":
            return {
                kind,
                dir_imported: fromJsonDirImported(jsonField(object, "dir_imported")),
            };
        case "dirExpanded":
            return {
                kind,
                dir_expanded: fromJsonDirExpanded(jsonField(object, "dir_expanded")),
            };
        case "dirExported":
            return {
                kind,
                dir_exported: fromJsonDirExported(jsonField(object, "dir_exported")),
            };
        case "dirResolved":
            return {
                kind,
                dir_resolved: fromJsonDirResolved(jsonField(object, "dir_resolved")),
            };
        case "dirCheckedComponent":
            return {
                kind,
                dir_checked_component: fromJsonDirCheckedComponent(jsonField(object, "dir_checked_component")),
            };
        case "dirChecked":
            return {
                kind,
                dir_checked: fromJsonDirChecked(jsonField(object, "dir_checked")),
            };
        case "dirMaterialized":
            return {
                kind,
                dir_materialized: fromJsonDirMaterialized(jsonField(object, "dir_materialized")),
            };
        case "dirElaborated":
            return {
                kind,
                dir_elaborated: fromJsonDirElaborated(jsonField(object, "dir_elaborated")),
            };
        case "mirLowered":
            return {
                kind,
                mir_lowered: fromJsonMirLowered(jsonField(object, "mir_lowered")),
            };
        case "mirVerified":
            return {
                kind,
                mir_verified: fromJsonMirVerified(jsonField(object, "mir_verified")),
            };
        case "mirAnalyzed":
            return {
                kind,
                mir_analyzed: fromJsonMirAnalyzed(jsonField(object, "mir_analyzed")),
            };
        case "mirOptimized":
            return {
                kind,
                mir_optimized: fromJsonMirOptimized(jsonField(object, "mir_optimized")),
            };
        case "moduleQueryIndex":
            return {
                kind,
                module_query_index: fromJsonModuleQueryIndex(jsonField(object, "module_query_index")),
            };
        case "workspaceQueryIndex":
            return {
                kind,
                workspace_query_index: fromJsonWorkspaceQueryIndex(jsonField(object, "workspace_query_index")),
            };
        case "script":
            return {
                kind,
                script: fromJsonScript(jsonField(object, "script")),
            };
        case "object":
            return {
                kind,
                object: fromJsonObject(jsonField(object, "object")),
            };
        case "asset":
            return {
                kind,
                asset: fromJsonAsset(jsonField(object, "asset")),
            };
        case "build":
            return {
                kind,
                build: fromJsonBuild(jsonField(object, "build")),
            };
        case "bundle":
            return {
                kind,
                bundle: fromJsonBundle(jsonField(object, "bundle")),
            };
        case "program":
            return {
                kind,
                program: fromJsonProgram(jsonField(object, "program")),
            };
        case "product":
            return {
                kind,
                product: fromJsonProduct(jsonField(object, "product")),
            };
        case "moduleLinted":
            return {
                kind,
                module_linted: fromJsonModuleLinted(jsonField(object, "module_linted")),
            };
        case "packageLinted":
            return {
                kind,
                package_linted: fromJsonPackageLinted(jsonField(object, "package_linted")),
            };
        case "workspaceLinted":
            return {
                kind,
                workspace_linted: fromJsonWorkspaceLinted(jsonField(object, "workspace_linted")),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}
