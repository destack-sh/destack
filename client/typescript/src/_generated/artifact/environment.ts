/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, compareBytes, jsonArray, jsonField, jsonObject, jsonOptional, nestedBytes } from "../../protocol/serde.js";
import type { ExportResolution } from "../dir/symbol/export.js";
import type { StaticKey } from "../dir/symbol/key.js";
import type { LanguageItem } from "../dir/symbol/language.js";
import type { GlobalSymbolId } from "../dir/symbol/symbol.js";
import type { TypeRoot } from "../dir/table/definition.js";
import type { ModuleId } from "../source/file/model/module.js";
import { decodeExportResolution, encodeExportResolution, fromJsonExportResolution, toJsonExportResolution } from "../dir/symbol/export.js";
import { decodeStaticKey, encodeStaticKey, fromJsonStaticKey, toJsonStaticKey } from "../dir/symbol/key.js";
import { decodeLanguageItem, encodeLanguageItem, fromJsonLanguageItem, toJsonLanguageItem } from "../dir/symbol/language.js";
import { decodeGlobalSymbolId, encodeGlobalSymbolId, fromJsonGlobalSymbolId, toJsonGlobalSymbolId } from "../dir/symbol/symbol.js";
import { decodeTypeRoot, encodeTypeRoot, fromJsonTypeRoot, toJsonTypeRoot } from "../dir/table/definition.js";
import { decodeModuleId, encodeModuleId, fromJsonModuleId, toJsonModuleId } from "../source/file/model/module.js";

/** Bound-stage aggregate over the implicit modules. */
export type EnvironmentBound = {
    /** Compiler-known language environment. */
    readonly language: LanguageEnvironment;
    /** Explicit global modules in load order. */
    readonly globals: ReadonlyArray<ModuleId>;
    /** Resolved global bindings by key across the global modules. */
    readonly globalResolutionsByKey: ReadonlyMap<StaticKey, ReadonlyArray<ExportResolution>>;
    /** The default tree builder selected by the profile. */
    readonly tree?: GlobalSymbolId;
};

export const EnvironmentBound = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: EnvironmentBound): void {
        encodeEnvironmentBound(writer, value);
    },

    /** Decode one EnvironmentBound. */
    decode(reader: BinaryReader): EnvironmentBound {
        return decodeEnvironmentBound(reader);
    },

    /** Return this value as JSON. */
    toJson(value: EnvironmentBound): Json {
        return toJsonEnvironmentBound(value);
    },

    /** Return one EnvironmentBound from one JSON value. */
    fromJson(value: Json): EnvironmentBound {
        return fromJsonEnvironmentBound(value);
    },
};

/** Encode one EnvironmentBound. */
export function encodeEnvironmentBound(writer: BinaryWriter, value: EnvironmentBound): void {
    encodeLanguageEnvironment(writer, value.language);
    writer.writeUnsigned(value.globals.length);
    for (const item1 of value.globals) {
        encodeModuleId(writer, item1);
    }
    const entries2 = Array.from(value.globalResolutionsByKey.entries()).map(([key2, item2]) => {
        const keyBytes = nestedBytes((writer) => {
            encodeStaticKey(writer, key2);
        });
        return { key2, item2, keyBytes };
    });
    entries2.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries2.length);
    for (const entry2 of entries2) {
        encodeStaticKey(writer, entry2.key2);
        writer.writeUnsigned(entry2.item2.length);
        for (const item3 of entry2.item2) {
            encodeExportResolution(writer, item3);
        }
    }
    writer.writeOption(value.tree, (value3) => {
        encodeGlobalSymbolId(writer, value3);
    });
}

/** Decode one EnvironmentBound. */
export function decodeEnvironmentBound(reader: BinaryReader): EnvironmentBound {
    const language = decodeLanguageEnvironment(reader);
    const globals = (() => { const length1 = reader.readNumber(); const items1: Array<ModuleId> = []; for (let index = 0; index < length1; index += 1) { items1.push(decodeModuleId(reader)); } return items1; })();
    const globalResolutionsByKey = (() => { const length2 = reader.readNumber(); const items2 = new Map<StaticKey, ReadonlyArray<ExportResolution>>(); for (let index = 0; index < length2; index += 1) { items2.set(decodeStaticKey(reader), (() => { const length4 = reader.readNumber(); const items4: Array<ExportResolution> = []; for (let index = 0; index < length4; index += 1) { items4.push(decodeExportResolution(reader)); } return items4; })()); } return items2; })();
    const tree = reader.readOption(() => decodeGlobalSymbolId(reader));

    return {
        language,
        globals,
        globalResolutionsByKey,
        ...(tree === undefined ? {} : { tree }),
    };
}

/** Return one JSON value for one EnvironmentBound. */
export function toJsonEnvironmentBound(value: EnvironmentBound): Json {
    return {
        language: toJsonLanguageEnvironment(value.language),
        globals: value.globals.map((item0) => toJsonModuleId(item0)),
        globalResolutionsByKey: Array.from(value.globalResolutionsByKey.entries()).map(([key0, item0]) => [toJsonStaticKey(key0), item0.map((item1) => toJsonExportResolution(item1))] as const),
        ...(value.tree === undefined ? {} : { tree: toJsonGlobalSymbolId(value.tree) }),
    };
}

/** Return one EnvironmentBound from one JSON value. */
export function fromJsonEnvironmentBound(value: Json): EnvironmentBound {
    const object = jsonObject(value);

    return {
        language: fromJsonLanguageEnvironment(jsonField(object, "language")),
        globals: jsonArray(jsonField(object, "globals")).map((item0) => fromJsonModuleId(item0)),
        globalResolutionsByKey: new Map(jsonArray(jsonField(object, "globalResolutionsByKey")).map((entry) => { const items = jsonArray(entry); if (items.length !== 2) { throw new SerdeError(`expected JSON map entry length 2: ${items.length}`); } const key0 = items[0]; const item0 = items[1]; return [fromJsonStaticKey(key0), jsonArray(item0).map((item1) => fromJsonExportResolution(item1))] as const; })),
        tree: jsonOptional(object, "tree", (value) => fromJsonGlobalSymbolId(value)),
    };
}

/** Declared-stage aggregate over the implicit modules. */
export type EnvironmentDeclared = {
    /** Extension declarations keyed by their resolved target root. */
    readonly extensionsByRoot: ReadonlyMap<TypeRoot, ReadonlyArray<GlobalSymbolId>>;
    /** Blanket extension declarations over open parameter targets. */
    readonly blanketExtensions: ReadonlyArray<GlobalSymbolId>;
    /** Implementing declarations keyed by their implemented interface, with their target roots. */
    readonly implementationsByInterface: ReadonlyMap<GlobalSymbolId, ReadonlyArray<readonly [GlobalSymbolId, TypeRoot | undefined]>>;
};

export const EnvironmentDeclared = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: EnvironmentDeclared): void {
        encodeEnvironmentDeclared(writer, value);
    },

    /** Decode one EnvironmentDeclared. */
    decode(reader: BinaryReader): EnvironmentDeclared {
        return decodeEnvironmentDeclared(reader);
    },

    /** Return this value as JSON. */
    toJson(value: EnvironmentDeclared): Json {
        return toJsonEnvironmentDeclared(value);
    },

    /** Return one EnvironmentDeclared from one JSON value. */
    fromJson(value: Json): EnvironmentDeclared {
        return fromJsonEnvironmentDeclared(value);
    },
};

/** Encode one EnvironmentDeclared. */
export function encodeEnvironmentDeclared(writer: BinaryWriter, value: EnvironmentDeclared): void {
    const entries0 = Array.from(value.extensionsByRoot.entries()).map(([key0, item0]) => {
        const keyBytes = nestedBytes((writer) => {
            encodeTypeRoot(writer, key0);
        });
        return { key0, item0, keyBytes };
    });
    entries0.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries0.length);
    for (const entry0 of entries0) {
        encodeTypeRoot(writer, entry0.key0);
        writer.writeUnsigned(entry0.item0.length);
        for (const item1 of entry0.item0) {
            encodeGlobalSymbolId(writer, item1);
        }
    }
    writer.writeUnsigned(value.blanketExtensions.length);
    for (const item1 of value.blanketExtensions) {
        encodeGlobalSymbolId(writer, item1);
    }
    const entries2 = Array.from(value.implementationsByInterface.entries()).map(([key2, item2]) => {
        const keyBytes = nestedBytes((writer) => {
            encodeGlobalSymbolId(writer, key2);
        });
        return { key2, item2, keyBytes };
    });
    entries2.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries2.length);
    for (const entry2 of entries2) {
        encodeGlobalSymbolId(writer, entry2.key2);
        writer.writeUnsigned(entry2.item2.length);
        for (const item3 of entry2.item2) {
            encodeGlobalSymbolId(writer, item3[0]);
            writer.writeOption(item3[1], (value5) => {
                encodeTypeRoot(writer, value5);
            });
        }
    }
}

/** Decode one EnvironmentDeclared. */
export function decodeEnvironmentDeclared(reader: BinaryReader): EnvironmentDeclared {
    const extensionsByRoot = (() => { const length0 = reader.readNumber(); const items0 = new Map<TypeRoot, ReadonlyArray<GlobalSymbolId>>(); for (let index = 0; index < length0; index += 1) { items0.set(decodeTypeRoot(reader), (() => { const length2 = reader.readNumber(); const items2: Array<GlobalSymbolId> = []; for (let index = 0; index < length2; index += 1) { items2.push(decodeGlobalSymbolId(reader)); } return items2; })()); } return items0; })();
    const blanketExtensions = (() => { const length1 = reader.readNumber(); const items1: Array<GlobalSymbolId> = []; for (let index = 0; index < length1; index += 1) { items1.push(decodeGlobalSymbolId(reader)); } return items1; })();
    const implementationsByInterface = (() => { const length2 = reader.readNumber(); const items2 = new Map<GlobalSymbolId, ReadonlyArray<readonly [GlobalSymbolId, TypeRoot | undefined]>>(); for (let index = 0; index < length2; index += 1) { items2.set(decodeGlobalSymbolId(reader), (() => { const length4 = reader.readNumber(); const items4: Array<readonly [GlobalSymbolId, TypeRoot | undefined]> = []; for (let index = 0; index < length4; index += 1) { items4.push([decodeGlobalSymbolId(reader), reader.readOption(() => decodeTypeRoot(reader))] as const); } return items4; })()); } return items2; })();

    return {
        extensionsByRoot,
        blanketExtensions,
        implementationsByInterface,
    };
}

/** Return one JSON value for one EnvironmentDeclared. */
export function toJsonEnvironmentDeclared(value: EnvironmentDeclared): Json {
    return {
        extensionsByRoot: Array.from(value.extensionsByRoot.entries()).map(([key0, item0]) => [toJsonTypeRoot(key0), item0.map((item1) => toJsonGlobalSymbolId(item1))] as const),
        blanketExtensions: value.blanketExtensions.map((item0) => toJsonGlobalSymbolId(item0)),
        implementationsByInterface: Array.from(value.implementationsByInterface.entries()).map(([key0, item0]) => [toJsonGlobalSymbolId(key0), item0.map((item1) => [toJsonGlobalSymbolId(item1[0]), item1[1] === undefined ? null : toJsonTypeRoot(item1[1])])] as const),
    };
}

/** Return one EnvironmentDeclared from one JSON value. */
export function fromJsonEnvironmentDeclared(value: Json): EnvironmentDeclared {
    const object = jsonObject(value);

    return {
        extensionsByRoot: new Map(jsonArray(jsonField(object, "extensionsByRoot")).map((entry) => { const items = jsonArray(entry); if (items.length !== 2) { throw new SerdeError(`expected JSON map entry length 2: ${items.length}`); } const key0 = items[0]; const item0 = items[1]; return [fromJsonTypeRoot(key0), jsonArray(item0).map((item1) => fromJsonGlobalSymbolId(item1))] as const; })),
        blanketExtensions: jsonArray(jsonField(object, "blanketExtensions")).map((item0) => fromJsonGlobalSymbolId(item0)),
        implementationsByInterface: new Map(jsonArray(jsonField(object, "implementationsByInterface")).map((entry) => { const items = jsonArray(entry); if (items.length !== 2) { throw new SerdeError(`expected JSON map entry length 2: ${items.length}`); } const key0 = items[0]; const item0 = items[1]; return [fromJsonGlobalSymbolId(key0), jsonArray(item0).map((item1) => (() => { const items = jsonArray(item1); if (items.length !== 2) { throw new SerdeError(`expected JSON tuple length 2: ${items.length}`); } return [fromJsonGlobalSymbolId(items[0]), items[1] === null ? undefined : fromJsonTypeRoot(items[1])] as const; })())] as const; })),
    };
}

/** Compiler-known language environment for one profile. */
export type LanguageEnvironment = {
    /** Language item symbols by item id. */
    readonly symbolByItem: ReadonlyMap<LanguageItem, GlobalSymbolId>;
    /** Language items by symbol id. */
    readonly itemsBySymbol: ReadonlyMap<GlobalSymbolId, LanguageItem>;
};

export const LanguageEnvironment = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: LanguageEnvironment): void {
        encodeLanguageEnvironment(writer, value);
    },

    /** Decode one LanguageEnvironment. */
    decode(reader: BinaryReader): LanguageEnvironment {
        return decodeLanguageEnvironment(reader);
    },

    /** Return this value as JSON. */
    toJson(value: LanguageEnvironment): Json {
        return toJsonLanguageEnvironment(value);
    },

    /** Return one LanguageEnvironment from one JSON value. */
    fromJson(value: Json): LanguageEnvironment {
        return fromJsonLanguageEnvironment(value);
    },
};

/** Encode one LanguageEnvironment. */
export function encodeLanguageEnvironment(writer: BinaryWriter, value: LanguageEnvironment): void {
    const entries0 = Array.from(value.symbolByItem.entries()).map(([key0, item0]) => {
        const keyBytes = nestedBytes((writer) => {
            encodeLanguageItem(writer, key0);
        });
        return { key0, item0, keyBytes };
    });
    entries0.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries0.length);
    for (const entry0 of entries0) {
        encodeLanguageItem(writer, entry0.key0);
        encodeGlobalSymbolId(writer, entry0.item0);
    }
    const entries1 = Array.from(value.itemsBySymbol.entries()).map(([key1, item1]) => {
        const keyBytes = nestedBytes((writer) => {
            encodeGlobalSymbolId(writer, key1);
        });
        return { key1, item1, keyBytes };
    });
    entries1.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries1.length);
    for (const entry1 of entries1) {
        encodeGlobalSymbolId(writer, entry1.key1);
        encodeLanguageItem(writer, entry1.item1);
    }
}

/** Decode one LanguageEnvironment. */
export function decodeLanguageEnvironment(reader: BinaryReader): LanguageEnvironment {
    const symbolByItem = (() => { const length0 = reader.readNumber(); const items0 = new Map<LanguageItem, GlobalSymbolId>(); for (let index = 0; index < length0; index += 1) { items0.set(decodeLanguageItem(reader), decodeGlobalSymbolId(reader)); } return items0; })();
    const itemsBySymbol = (() => { const length1 = reader.readNumber(); const items1 = new Map<GlobalSymbolId, LanguageItem>(); for (let index = 0; index < length1; index += 1) { items1.set(decodeGlobalSymbolId(reader), decodeLanguageItem(reader)); } return items1; })();

    return {
        symbolByItem,
        itemsBySymbol,
    };
}

/** Return one JSON value for one LanguageEnvironment. */
export function toJsonLanguageEnvironment(value: LanguageEnvironment): Json {
    return {
        symbolByItem: Array.from(value.symbolByItem.entries()).map(([key0, item0]) => [toJsonLanguageItem(key0), toJsonGlobalSymbolId(item0)] as const),
        itemsBySymbol: Array.from(value.itemsBySymbol.entries()).map(([key0, item0]) => [toJsonGlobalSymbolId(key0), toJsonLanguageItem(item0)] as const),
    };
}

/** Return one LanguageEnvironment from one JSON value. */
export function fromJsonLanguageEnvironment(value: Json): LanguageEnvironment {
    const object = jsonObject(value);

    return {
        symbolByItem: new Map(jsonArray(jsonField(object, "symbolByItem")).map((entry) => { const items = jsonArray(entry); if (items.length !== 2) { throw new SerdeError(`expected JSON map entry length 2: ${items.length}`); } const key0 = items[0]; const item0 = items[1]; return [fromJsonLanguageItem(key0), fromJsonGlobalSymbolId(item0)] as const; })),
        itemsBySymbol: new Map(jsonArray(jsonField(object, "itemsBySymbol")).map((entry) => { const items = jsonArray(entry); if (items.length !== 2) { throw new SerdeError(`expected JSON map entry length 2: ${items.length}`); } const key0 = items[0]; const item0 = items[1]; return [fromJsonGlobalSymbolId(key0), fromJsonLanguageItem(item0)] as const; })),
    };
}
