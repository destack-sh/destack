/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, compareBytes, jsonArray, jsonBool, jsonField, jsonObject, jsonString, nestedBytes } from "../../../../protocol/serde.js";

/** Linter configuration. */
export type LinterOptions = {
    /** Whether linting is enabled. */
    readonly enabled: boolean;
    /** Explicit levels keyed by rule id or diagnostic code. */
    readonly rules: Readonly<Record<string, LintLevel>>;
};

export const LinterOptions = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: LinterOptions): void {
        encodeLinterOptions(writer, value);
    },

    /** Decode one LinterOptions. */
    decode(reader: BinaryReader): LinterOptions {
        return decodeLinterOptions(reader);
    },

    /** Return this value as JSON. */
    toJson(value: LinterOptions): Json {
        return toJsonLinterOptions(value);
    },

    /** Return one LinterOptions from one JSON value. */
    fromJson(value: Json): LinterOptions {
        return fromJsonLinterOptions(value);
    },
};

/** Encode one LinterOptions. */
export function encodeLinterOptions(writer: BinaryWriter, value: LinterOptions): void {
    writer.writeBool(value.enabled);
    const entries1 = Object.entries(value.rules).map(([key1, item1]) => {
        const keyBytes = nestedBytes((writer) => {
            writer.writeString(key1);
        });
        return { key1, item1, keyBytes };
    });
    entries1.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries1.length);
    for (const entry1 of entries1) {
        writer.writeString(entry1.key1);
        encodeLintLevel(writer, entry1.item1);
    }
}

/** Decode one LinterOptions. */
export function decodeLinterOptions(reader: BinaryReader): LinterOptions {
    const enabled = reader.readBool();
    const rules = (() => { const length1 = reader.readNumber(); const items1: Record<string, LintLevel> = {}; for (let index = 0; index < length1; index += 1) { const key = reader.readString(); items1[key] = decodeLintLevel(reader); } return items1; })();

    return {
        enabled,
        rules,
    };
}

/** Return one JSON value for one LinterOptions. */
export function toJsonLinterOptions(value: LinterOptions): Json {
    return {
        enabled: value.enabled,
        rules: Object.fromEntries(Object.entries(value.rules).map(([key0, item0]) => [key0, toJsonLintLevel(item0)] as const)),
    };
}

/** Return one LinterOptions from one JSON value. */
export function fromJsonLinterOptions(value: Json): LinterOptions {
    const object = jsonObject(value);

    return {
        enabled: jsonBool(jsonField(object, "enabled")),
        rules: Object.fromEntries(Object.entries(jsonObject(jsonField(object, "rules"))).map(([key0, item0]) => [key0, fromJsonLintLevel(item0)] as const)),
    };
}

/** One configured lint level. */
export type LintLevel = "off" | "warning" | "error";

export const LintLevel = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: LintLevel): void {
        encodeLintLevel(writer, value);
    },

    /** Decode one LintLevel. */
    decode(reader: BinaryReader): LintLevel {
        return decodeLintLevel(reader);
    },

    /** Return this value as JSON. */
    toJson(value: LintLevel): Json {
        return toJsonLintLevel(value);
    },

    /** Return one LintLevel from one JSON value. */
    fromJson(value: Json): LintLevel {
        return fromJsonLintLevel(value);
    },
};

/** Encode one LintLevel. */
export function encodeLintLevel(writer: BinaryWriter, value: LintLevel): void {
    switch (value) {
        case "off":
            writer.writeUnsigned(0);
            return;
        case "warning":
            writer.writeUnsigned(1);
            return;
        case "error":
            writer.writeUnsigned(2);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one LintLevel. */
export function decodeLintLevel(reader: BinaryReader): LintLevel {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "off";
        case 1:
            return "warning";
        case 2:
            return "error";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one LintLevel. */
export function toJsonLintLevel(value: LintLevel): Json {
    return value;
}

/** Return one LintLevel from one JSON value. */
export function fromJsonLintLevel(value: Json): LintLevel {
    const variant = jsonString(value);

    switch (variant) {
        case "off":
            return "off";
        case "warning":
            return "warning";
        case "error":
            return "error";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}
