/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, jsonField, jsonObject } from "../../../../protocol/serde.js";
import type { UnicodeRegexpRequireFlag } from "./core.js";
import { decodeUnicodeRegexpRequireFlag, encodeUnicodeRegexpRequireFlag, fromJsonUnicodeRegexpRequireFlag, toJsonUnicodeRegexpRequireFlag } from "./core.js";

/** Performance-category linter options. */
export type LinterPerformanceOptions = {
    /** Required Unicode regex flag for `require-unicode-regexp`. */
    readonly requireUnicodeRegexpRequireFlag: UnicodeRegexpRequireFlag;
};

export const LinterPerformanceOptions = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: LinterPerformanceOptions): void {
        encodeLinterPerformanceOptions(writer, value);
    },

    /** Decode one LinterPerformanceOptions. */
    decode(reader: BinaryReader): LinterPerformanceOptions {
        return decodeLinterPerformanceOptions(reader);
    },

    /** Return this value as JSON. */
    toJson(value: LinterPerformanceOptions): Json {
        return toJsonLinterPerformanceOptions(value);
    },

    /** Return one LinterPerformanceOptions from one JSON value. */
    fromJson(value: Json): LinterPerformanceOptions {
        return fromJsonLinterPerformanceOptions(value);
    },
};

/** Encode one LinterPerformanceOptions. */
export function encodeLinterPerformanceOptions(writer: BinaryWriter, value: LinterPerformanceOptions): void {
    encodeUnicodeRegexpRequireFlag(writer, value.requireUnicodeRegexpRequireFlag);
}

/** Decode one LinterPerformanceOptions. */
export function decodeLinterPerformanceOptions(reader: BinaryReader): LinterPerformanceOptions {
    const requireUnicodeRegexpRequireFlag = decodeUnicodeRegexpRequireFlag(reader);

    return {
        requireUnicodeRegexpRequireFlag,
    };
}

/** Return one JSON value for one LinterPerformanceOptions. */
export function toJsonLinterPerformanceOptions(value: LinterPerformanceOptions): Json {
    return {
        requireUnicodeRegexpRequireFlag: toJsonUnicodeRegexpRequireFlag(value.requireUnicodeRegexpRequireFlag),
    };
}

/** Return one LinterPerformanceOptions from one JSON value. */
export function fromJsonLinterPerformanceOptions(value: Json): LinterPerformanceOptions {
    const object = jsonObject(value);

    return {
        requireUnicodeRegexpRequireFlag: fromJsonUnicodeRegexpRequireFlag(jsonField(object, "requireUnicodeRegexpRequireFlag")),
    };
}
