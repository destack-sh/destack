/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, jsonObject } from "../../../../protocol/serde.js";

/** Suspicious-category linter options. */
export type LinterSuspiciousOptions = {
};

export const LinterSuspiciousOptions = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: LinterSuspiciousOptions): void {
        encodeLinterSuspiciousOptions(writer, value);
    },

    /** Decode one LinterSuspiciousOptions. */
    decode(reader: BinaryReader): LinterSuspiciousOptions {
        return decodeLinterSuspiciousOptions(reader);
    },

    /** Return this value as JSON. */
    toJson(value: LinterSuspiciousOptions): Json {
        return toJsonLinterSuspiciousOptions(value);
    },

    /** Return one LinterSuspiciousOptions from one JSON value. */
    fromJson(value: Json): LinterSuspiciousOptions {
        return fromJsonLinterSuspiciousOptions(value);
    },
};

/** Encode one LinterSuspiciousOptions. */
export function encodeLinterSuspiciousOptions(writer: BinaryWriter, value: LinterSuspiciousOptions): void {
}

/** Decode one LinterSuspiciousOptions. */
export function decodeLinterSuspiciousOptions(reader: BinaryReader): LinterSuspiciousOptions {
    return {
    };
}

/** Return one JSON value for one LinterSuspiciousOptions. */
export function toJsonLinterSuspiciousOptions(value: LinterSuspiciousOptions): Json {
    return {
    };
}

/** Return one LinterSuspiciousOptions from one JSON value. */
export function fromJsonLinterSuspiciousOptions(value: Json): LinterSuspiciousOptions {
    const object = jsonObject(value);
    return {
    };
}
