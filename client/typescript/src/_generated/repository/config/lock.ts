/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, compareBytes, jsonArray, jsonBool, jsonField, jsonInteger, jsonObject, jsonOptional, jsonString, nestedBytes } from "../../../protocol/serde.js";
import type { Dependency } from "./dependency.js";
import { decodeDependency, encodeDependency, fromJsonDependency, toJsonDependency } from "./dependency.js";

/** Locked package source and dependency record. */
export type PackageLock = {
    /** Package name read from the locked manifest. */
    readonly name: string;
    /** Package version read from the locked manifest. */
    readonly version?: string;
    /** Resolved source locator. */
    readonly source: SourceLock;
    /** Hash of the package source tree. */
    readonly sourceHash: string;
    /** Hash of the package manifest. */
    readonly manifestHash: string;
    /** Locked dependency declarations. */
    readonly dependencies: Readonly<Record<string, Dependency>>;
    /** Whether this package is an editable source. */
    readonly editable: boolean;
};

export const PackageLock = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: PackageLock): void {
        encodePackageLock(writer, value);
    },

    /** Decode one PackageLock. */
    decode(reader: BinaryReader): PackageLock {
        return decodePackageLock(reader);
    },

    /** Return this value as JSON. */
    toJson(value: PackageLock): Json {
        return toJsonPackageLock(value);
    },

    /** Return one PackageLock from one JSON value. */
    fromJson(value: Json): PackageLock {
        return fromJsonPackageLock(value);
    },
};

/** Encode one PackageLock. */
export function encodePackageLock(writer: BinaryWriter, value: PackageLock): void {
    writer.writeString(value.name);
    writer.writeOption(value.version, (value1) => {
        writer.writeString(value1);
    });
    encodeSourceLock(writer, value.source);
    writer.writeString(value.sourceHash);
    writer.writeString(value.manifestHash);
    const entries5 = Object.entries(value.dependencies).map(([key5, item5]) => {
        const keyBytes = nestedBytes((writer) => {
            writer.writeString(key5);
        });
        return { key5, item5, keyBytes };
    });
    entries5.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries5.length);
    for (const entry5 of entries5) {
        writer.writeString(entry5.key5);
        encodeDependency(writer, entry5.item5);
    }
    writer.writeBool(value.editable);
}

/** Decode one PackageLock. */
export function decodePackageLock(reader: BinaryReader): PackageLock {
    const name = reader.readString();
    const version = reader.readOption(() => reader.readString());
    const source = decodeSourceLock(reader);
    const sourceHash = reader.readString();
    const manifestHash = reader.readString();
    const dependencies = (() => { const length5 = reader.readNumber(); const items5: Record<string, Dependency> = {}; for (let index = 0; index < length5; index += 1) { const key = reader.readString(); items5[key] = decodeDependency(reader); } return items5; })();
    const editable = reader.readBool();

    return {
        name,
        ...(version === undefined ? {} : { version }),
        source,
        sourceHash,
        manifestHash,
        dependencies,
        editable,
    };
}

/** Return one JSON value for one PackageLock. */
export function toJsonPackageLock(value: PackageLock): Json {
    return {
        name: value.name,
        ...(value.version === undefined ? {} : { version: value.version }),
        source: toJsonSourceLock(value.source),
        sourceHash: value.sourceHash,
        manifestHash: value.manifestHash,
        dependencies: Object.fromEntries(Object.entries(value.dependencies).map(([key0, item0]) => [key0, toJsonDependency(item0)] as const)),
        editable: value.editable,
    };
}

/** Return one PackageLock from one JSON value. */
export function fromJsonPackageLock(value: Json): PackageLock {
    const object = jsonObject(value);

    return {
        name: jsonString(jsonField(object, "name")),
        version: jsonOptional(object, "version", (value) => jsonString(value)),
        source: fromJsonSourceLock(jsonField(object, "source")),
        sourceHash: jsonString(jsonField(object, "sourceHash")),
        manifestHash: jsonString(jsonField(object, "manifestHash")),
        dependencies: Object.fromEntries(Object.entries(jsonObject(jsonField(object, "dependencies"))).map(([key0, item0]) => [key0, fromJsonDependency(item0)] as const)),
        editable: jsonBool(jsonField(object, "editable")),
    };
}

/** Locked source locator. */
export type SourceLock =
    /** Package resolved from a workspace member. */
    | {
          readonly kind: "workspace";
          /** Workspace relative package path. */
          readonly path: string;
      }
    /** Package resolved from a registry. */
    | {
          readonly kind: "registry";
          /** Registry name. */
          readonly registry?: string;
          /** Exact package version. */
          readonly version: string;
      }
    /** Package resolved from a local path. */
    | {
          readonly kind: "path";
          /** Package path. */
          readonly path: string;
      }
    /** Package resolved from one Git repository. */
    | {
          readonly kind: "git";
          /** Repository URL. */
          readonly url: string;
          /** Exact commit revision. */
          readonly rev: string;
          /** Repository subdirectory containing the package. */
          readonly path?: string;
      }
;

export const SourceLock = {
    /** Package resolved from a workspace member. */
    workspace(path: string): SourceLock {
        return { kind: "workspace", path };
    },

    /** Package resolved from a registry. */
    registry(registry: string | undefined, version: string): SourceLock {
        return { kind: "registry", registry, version };
    },

    /** Package resolved from a local path. */
    path(path: string): SourceLock {
        return { kind: "path", path };
    },

    /** Package resolved from one Git repository. */
    git(url: string, rev: string, path: string | undefined): SourceLock {
        return { kind: "git", url, rev, path };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: SourceLock): void {
        encodeSourceLock(writer, value);
    },

    /** Decode one SourceLock. */
    decode(reader: BinaryReader): SourceLock {
        return decodeSourceLock(reader);
    },

    /** Return this value as JSON. */
    toJson(value: SourceLock): Json {
        return toJsonSourceLock(value);
    },

    /** Return one SourceLock from one JSON value. */
    fromJson(value: Json): SourceLock {
        return fromJsonSourceLock(value);
    },
};

/** Encode one SourceLock. */
export function encodeSourceLock(writer: BinaryWriter, value: SourceLock): void {
    switch (value.kind) {
        case "workspace":
            writer.writeUnsigned(0);
            writer.writeString(value.path);
            return;
        case "registry":
            writer.writeUnsigned(1);
            writer.writeOption(value.registry, (value0) => {
                writer.writeString(value0);
            });
            writer.writeString(value.version);
            return;
        case "path":
            writer.writeUnsigned(2);
            writer.writeString(value.path);
            return;
        case "git":
            writer.writeUnsigned(3);
            writer.writeString(value.url);
            writer.writeString(value.rev);
            writer.writeOption(value.path, (value2) => {
                writer.writeString(value2);
            });
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one SourceLock. */
export function decodeSourceLock(reader: BinaryReader): SourceLock {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const path = reader.readString();

            return {
                kind: "workspace",
                path,
            };
        }
        case 1: {
            const registry = reader.readOption(() => reader.readString());
            const version = reader.readString();

            return {
                kind: "registry",
                ...(registry === undefined ? {} : { registry }),
                version,
            };
        }
        case 2: {
            const path = reader.readString();

            return {
                kind: "path",
                path,
            };
        }
        case 3: {
            const url = reader.readString();
            const rev = reader.readString();
            const path = reader.readOption(() => reader.readString());

            return {
                kind: "git",
                url,
                rev,
                ...(path === undefined ? {} : { path }),
            };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one SourceLock. */
export function toJsonSourceLock(value: SourceLock): Json {
    switch (value.kind) {
        case "workspace":
            return {
                kind: "workspace",
                path: value.path,
            };
        case "registry":
            return {
                kind: "registry",
                ...(value.registry === undefined ? {} : { registry: value.registry }),
                version: value.version,
            };
        case "path":
            return {
                kind: "path",
                path: value.path,
            };
        case "git":
            return {
                kind: "git",
                url: value.url,
                rev: value.rev,
                ...(value.path === undefined ? {} : { path: value.path }),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one SourceLock from one JSON value. */
export function fromJsonSourceLock(value: Json): SourceLock {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "workspace":
            return {
                kind,
                path: jsonString(jsonField(object, "path")),
            };
        case "registry":
            return {
                kind,
                registry: jsonOptional(object, "registry", (value) => jsonString(value)),
                version: jsonString(jsonField(object, "version")),
            };
        case "path":
            return {
                kind,
                path: jsonString(jsonField(object, "path")),
            };
        case "git":
            return {
                kind,
                url: jsonString(jsonField(object, "url")),
                rev: jsonString(jsonField(object, "rev")),
                path: jsonOptional(object, "path", (value) => jsonString(value)),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** Destack lock document. */
export type DestackLock = {
    /** Lock document format version. */
    readonly version: number;
    /** Root package name. */
    readonly root?: string;
    /** Locked packages keyed by package import name. */
    readonly packages: Readonly<Record<string, PackageLock>>;
};

export const DestackLock = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: DestackLock): void {
        encodeDestackLock(writer, value);
    },

    /** Decode one DestackLock. */
    decode(reader: BinaryReader): DestackLock {
        return decodeDestackLock(reader);
    },

    /** Return this value as JSON. */
    toJson(value: DestackLock): Json {
        return toJsonDestackLock(value);
    },

    /** Return one DestackLock from one JSON value. */
    fromJson(value: Json): DestackLock {
        return fromJsonDestackLock(value);
    },
};

/** Encode one DestackLock. */
export function encodeDestackLock(writer: BinaryWriter, value: DestackLock): void {
    writer.writeUnsigned(value.version);
    writer.writeOption(value.root, (value1) => {
        writer.writeString(value1);
    });
    const entries2 = Object.entries(value.packages).map(([key2, item2]) => {
        const keyBytes = nestedBytes((writer) => {
            writer.writeString(key2);
        });
        return { key2, item2, keyBytes };
    });
    entries2.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries2.length);
    for (const entry2 of entries2) {
        writer.writeString(entry2.key2);
        encodePackageLock(writer, entry2.item2);
    }
}

/** Decode one DestackLock. */
export function decodeDestackLock(reader: BinaryReader): DestackLock {
    const version = reader.readNumber();
    const root = reader.readOption(() => reader.readString());
    const packages = (() => { const length2 = reader.readNumber(); const items2: Record<string, PackageLock> = {}; for (let index = 0; index < length2; index += 1) { const key = reader.readString(); items2[key] = decodePackageLock(reader); } return items2; })();

    return {
        version,
        ...(root === undefined ? {} : { root }),
        packages,
    };
}

/** Return one JSON value for one DestackLock. */
export function toJsonDestackLock(value: DestackLock): Json {
    return {
        version: value.version,
        ...(value.root === undefined ? {} : { root: value.root }),
        packages: Object.fromEntries(Object.entries(value.packages).map(([key0, item0]) => [key0, toJsonPackageLock(item0)] as const)),
    };
}

/** Return one DestackLock from one JSON value. */
export function fromJsonDestackLock(value: Json): DestackLock {
    const object = jsonObject(value);

    return {
        version: jsonInteger(jsonField(object, "version")),
        root: jsonOptional(object, "root", (value) => jsonString(value)),
        packages: Object.fromEntries(Object.entries(jsonObject(jsonField(object, "packages"))).map(([key0, item0]) => [key0, fromJsonPackageLock(item0)] as const)),
    };
}
