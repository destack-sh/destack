/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, compareBytes, jsonArray, jsonField, jsonObject, jsonOptional, jsonString, nestedBytes } from "../../../protocol/serde.js";
import type { Dependency } from "./dependency.js";
import { decodeDependency, encodeDependency, fromJsonDependency, toJsonDependency } from "./dependency.js";

/** Source-level reference to one active condition predicate. */
export type ConditionRef =
    /** Named condition alias or `axis:name` reference. */
    | {
          readonly kind: "name";
          readonly name: string;
      }
    /** Inline condition gate. */
    | {
          readonly kind: "predicate";
          readonly predicate: ConditionPredicate;
      }
;

export const ConditionRef = {
    /** Named condition alias or `axis:name` reference. */
    name(name: string): ConditionRef {
        return { kind: "name", name };
    },

    /** Inline condition gate. */
    predicate(predicate: ConditionPredicate): ConditionRef {
        return { kind: "predicate", predicate };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: ConditionRef): void {
        encodeConditionRef(writer, value);
    },

    /** Decode one ConditionRef. */
    decode(reader: BinaryReader): ConditionRef {
        return decodeConditionRef(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ConditionRef): Json {
        return toJsonConditionRef(value);
    },

    /** Return one ConditionRef from one JSON value. */
    fromJson(value: Json): ConditionRef {
        return fromJsonConditionRef(value);
    },
};

/** Encode one ConditionRef. */
export function encodeConditionRef(writer: BinaryWriter, value: ConditionRef): void {
    switch (value.kind) {
        case "name":
            writer.writeUnsigned(0);
            writer.writeString(value.name);
            return;
        case "predicate":
            writer.writeUnsigned(1);
            encodeConditionPredicate(writer, value.predicate);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one ConditionRef. */
export function decodeConditionRef(reader: BinaryReader): ConditionRef {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const name = reader.readString();

            return { kind: "name", name };
        }
        case 1: {
            const predicate = decodeConditionPredicate(reader);

            return { kind: "predicate", predicate };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one ConditionRef. */
export function toJsonConditionRef(value: ConditionRef): Json {
    switch (value.kind) {
        case "name":
            return {
                kind: "name",
                name: value.name,
            };
        case "predicate":
            return {
                kind: "predicate",
                predicate: toJsonConditionPredicate(value.predicate),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one ConditionRef from one JSON value. */
export function fromJsonConditionRef(value: Json): ConditionRef {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "name":
            return {
                kind,
                name: jsonString(jsonField(object, "name")),
            };
        case "predicate":
            return {
                kind,
                predicate: fromJsonConditionPredicate(jsonField(object, "predicate")),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** Declared condition predicate before named references are resolved. */
export type ConditionPredicate = {
    /** Active mode selector. */
    readonly mode?: ConditionSelector;
    /** Active role selector. */
    readonly role?: ConditionSelector;
    /** Active feature selector. */
    readonly feature?: ConditionSelector;
    /** Active tag selector. */
    readonly tag?: ConditionSelector;
    /** Active build target selector. */
    readonly target?: ConditionSelector;
    /** Active product selector. */
    readonly product?: ConditionSelector;
    /** Active package release stage selector. */
    readonly stage?: ConditionSelector;
    /** Active target platform selector. */
    readonly platform?: ConditionSelector;
    /** Active host environment selector. */
    readonly host?: ConditionSelector;
    /** Active runtime selector. */
    readonly runtime?: ConditionSelector;
    /** Predicates that must all match. */
    readonly all?: ReadonlyArray<ConditionRef>;
    /** Predicates where at least one must match. */
    readonly any?: ReadonlyArray<ConditionRef>;
    /** Predicate that must not match. */
    readonly not?: ConditionRef;
};

export const ConditionPredicate = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ConditionPredicate): void {
        encodeConditionPredicate(writer, value);
    },

    /** Decode one ConditionPredicate. */
    decode(reader: BinaryReader): ConditionPredicate {
        return decodeConditionPredicate(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ConditionPredicate): Json {
        return toJsonConditionPredicate(value);
    },

    /** Return one ConditionPredicate from one JSON value. */
    fromJson(value: Json): ConditionPredicate {
        return fromJsonConditionPredicate(value);
    },
};

/** Encode one ConditionPredicate. */
export function encodeConditionPredicate(writer: BinaryWriter, value: ConditionPredicate): void {
    writer.writeOption(value.mode, (value0) => {
        encodeConditionSelector(writer, value0);
    });
    writer.writeOption(value.role, (value1) => {
        encodeConditionSelector(writer, value1);
    });
    writer.writeOption(value.feature, (value2) => {
        encodeConditionSelector(writer, value2);
    });
    writer.writeOption(value.tag, (value3) => {
        encodeConditionSelector(writer, value3);
    });
    writer.writeOption(value.target, (value4) => {
        encodeConditionSelector(writer, value4);
    });
    writer.writeOption(value.product, (value5) => {
        encodeConditionSelector(writer, value5);
    });
    writer.writeOption(value.stage, (value6) => {
        encodeConditionSelector(writer, value6);
    });
    writer.writeOption(value.platform, (value7) => {
        encodeConditionSelector(writer, value7);
    });
    writer.writeOption(value.host, (value8) => {
        encodeConditionSelector(writer, value8);
    });
    writer.writeOption(value.runtime, (value9) => {
        encodeConditionSelector(writer, value9);
    });
    writer.writeOption(value.all, (value10) => {
        writer.writeUnsigned(value10.length);
        for (const item11 of value10) {
            encodeConditionRef(writer, item11);
        }
    });
    writer.writeOption(value.any, (value11) => {
        writer.writeUnsigned(value11.length);
        for (const item12 of value11) {
            encodeConditionRef(writer, item12);
        }
    });
    writer.writeOption(value.not, (value12) => {
        encodeConditionRef(writer, value12);
    });
}

/** Decode one ConditionPredicate. */
export function decodeConditionPredicate(reader: BinaryReader): ConditionPredicate {
    const mode = reader.readOption(() => decodeConditionSelector(reader));
    const role = reader.readOption(() => decodeConditionSelector(reader));
    const feature = reader.readOption(() => decodeConditionSelector(reader));
    const tag = reader.readOption(() => decodeConditionSelector(reader));
    const target = reader.readOption(() => decodeConditionSelector(reader));
    const product = reader.readOption(() => decodeConditionSelector(reader));
    const stage = reader.readOption(() => decodeConditionSelector(reader));
    const platform = reader.readOption(() => decodeConditionSelector(reader));
    const host = reader.readOption(() => decodeConditionSelector(reader));
    const runtime = reader.readOption(() => decodeConditionSelector(reader));
    const all = reader.readOption(() => (() => { const length11 = reader.readNumber(); const items11: Array<ConditionRef> = []; for (let index = 0; index < length11; index += 1) { items11.push(decodeConditionRef(reader)); } return items11; })());
    const any_ = reader.readOption(() => (() => { const length12 = reader.readNumber(); const items12: Array<ConditionRef> = []; for (let index = 0; index < length12; index += 1) { items12.push(decodeConditionRef(reader)); } return items12; })());
    const not = reader.readOption(() => decodeConditionRef(reader));

    return {
        ...(mode === undefined ? {} : { mode }),
        ...(role === undefined ? {} : { role }),
        ...(feature === undefined ? {} : { feature }),
        ...(tag === undefined ? {} : { tag }),
        ...(target === undefined ? {} : { target }),
        ...(product === undefined ? {} : { product }),
        ...(stage === undefined ? {} : { stage }),
        ...(platform === undefined ? {} : { platform }),
        ...(host === undefined ? {} : { host }),
        ...(runtime === undefined ? {} : { runtime }),
        ...(all === undefined ? {} : { all }),
        ...(any_ === undefined ? {} : { any: any_ }),
        ...(not === undefined ? {} : { not }),
    };
}

/** Return one JSON value for one ConditionPredicate. */
export function toJsonConditionPredicate(value: ConditionPredicate): Json {
    return {
        ...(value.mode === undefined ? {} : { mode: toJsonConditionSelector(value.mode) }),
        ...(value.role === undefined ? {} : { role: toJsonConditionSelector(value.role) }),
        ...(value.feature === undefined ? {} : { feature: toJsonConditionSelector(value.feature) }),
        ...(value.tag === undefined ? {} : { tag: toJsonConditionSelector(value.tag) }),
        ...(value.target === undefined ? {} : { target: toJsonConditionSelector(value.target) }),
        ...(value.product === undefined ? {} : { product: toJsonConditionSelector(value.product) }),
        ...(value.stage === undefined ? {} : { stage: toJsonConditionSelector(value.stage) }),
        ...(value.platform === undefined ? {} : { platform: toJsonConditionSelector(value.platform) }),
        ...(value.host === undefined ? {} : { host: toJsonConditionSelector(value.host) }),
        ...(value.runtime === undefined ? {} : { runtime: toJsonConditionSelector(value.runtime) }),
        ...(value.all === undefined ? {} : { all: value.all.map((item0) => toJsonConditionRef(item0)) }),
        ...(value.any === undefined ? {} : { any: value.any.map((item0) => toJsonConditionRef(item0)) }),
        ...(value.not === undefined ? {} : { not: toJsonConditionRef(value.not) }),
    };
}

/** Return one ConditionPredicate from one JSON value. */
export function fromJsonConditionPredicate(value: Json): ConditionPredicate {
    const object = jsonObject(value);

    return {
        mode: jsonOptional(object, "mode", (value) => fromJsonConditionSelector(value)),
        role: jsonOptional(object, "role", (value) => fromJsonConditionSelector(value)),
        feature: jsonOptional(object, "feature", (value) => fromJsonConditionSelector(value)),
        tag: jsonOptional(object, "tag", (value) => fromJsonConditionSelector(value)),
        target: jsonOptional(object, "target", (value) => fromJsonConditionSelector(value)),
        product: jsonOptional(object, "product", (value) => fromJsonConditionSelector(value)),
        stage: jsonOptional(object, "stage", (value) => fromJsonConditionSelector(value)),
        platform: jsonOptional(object, "platform", (value) => fromJsonConditionSelector(value)),
        host: jsonOptional(object, "host", (value) => fromJsonConditionSelector(value)),
        runtime: jsonOptional(object, "runtime", (value) => fromJsonConditionSelector(value)),
        all: jsonOptional(object, "all", (value) => jsonArray(value).map((item0) => fromJsonConditionRef(item0))),
        any: jsonOptional(object, "any", (value) => jsonArray(value).map((item0) => fromJsonConditionRef(item0))),
        not: jsonOptional(object, "not", (value) => fromJsonConditionRef(value)),
    };
}

/** Selector over one active condition axis. */
export type ConditionSelector = {
    /** Condition names or glob patterns. */
    readonly patterns: ReadonlyArray<string>;
};

export const ConditionSelector = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ConditionSelector): void {
        encodeConditionSelector(writer, value);
    },

    /** Decode one ConditionSelector. */
    decode(reader: BinaryReader): ConditionSelector {
        return decodeConditionSelector(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ConditionSelector): Json {
        return toJsonConditionSelector(value);
    },

    /** Return one ConditionSelector from one JSON value. */
    fromJson(value: Json): ConditionSelector {
        return fromJsonConditionSelector(value);
    },
};

/** Encode one ConditionSelector. */
export function encodeConditionSelector(writer: BinaryWriter, value: ConditionSelector): void {
    writer.writeUnsigned(value.patterns.length);
    for (const item0 of value.patterns) {
        writer.writeString(item0);
    }
}

/** Decode one ConditionSelector. */
export function decodeConditionSelector(reader: BinaryReader): ConditionSelector {
    const patterns = (() => { const length0 = reader.readNumber(); const items0: Array<string> = []; for (let index = 0; index < length0; index += 1) { items0.push(reader.readString()); } return items0; })();

    return {
        patterns,
    };
}

/** Return one JSON value for one ConditionSelector. */
export function toJsonConditionSelector(value: ConditionSelector): Json {
    return {
        patterns: value.patterns.map((item0) => item0),
    };
}

/** Return one ConditionSelector from one JSON value. */
export function fromJsonConditionSelector(value: Json): ConditionSelector {
    const object = jsonObject(value);

    return {
        patterns: jsonArray(jsonField(object, "patterns")).map((item0) => jsonString(item0)),
    };
}

/** Named condition declarations from `destack.json`. */
export type ConditionCatalog = {
    /** Named source graph modes. */
    readonly modes: Readonly<Record<string, Condition>>;
    /** Named source graph roles. */
    readonly roles: Readonly<Record<string, Condition>>;
    /** Named optional source graph features. */
    readonly features: Readonly<Record<string, Condition>>;
    /** Named source graph tags. */
    readonly tags: Readonly<Record<string, Condition>>;
    /** Named condition aliases. */
    readonly aliases: Readonly<Record<string, ConditionRef>>;
};

export const ConditionCatalog = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ConditionCatalog): void {
        encodeConditionCatalog(writer, value);
    },

    /** Decode one ConditionCatalog. */
    decode(reader: BinaryReader): ConditionCatalog {
        return decodeConditionCatalog(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ConditionCatalog): Json {
        return toJsonConditionCatalog(value);
    },

    /** Return one ConditionCatalog from one JSON value. */
    fromJson(value: Json): ConditionCatalog {
        return fromJsonConditionCatalog(value);
    },
};

/** Encode one ConditionCatalog. */
export function encodeConditionCatalog(writer: BinaryWriter, value: ConditionCatalog): void {
    const entries0 = Object.entries(value.modes).map(([key0, item0]) => {
        const keyBytes = nestedBytes((writer) => {
            writer.writeString(key0);
        });
        return { key0, item0, keyBytes };
    });
    entries0.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries0.length);
    for (const entry0 of entries0) {
        writer.writeString(entry0.key0);
        encodeCondition(writer, entry0.item0);
    }
    const entries1 = Object.entries(value.roles).map(([key1, item1]) => {
        const keyBytes = nestedBytes((writer) => {
            writer.writeString(key1);
        });
        return { key1, item1, keyBytes };
    });
    entries1.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries1.length);
    for (const entry1 of entries1) {
        writer.writeString(entry1.key1);
        encodeCondition(writer, entry1.item1);
    }
    const entries2 = Object.entries(value.features).map(([key2, item2]) => {
        const keyBytes = nestedBytes((writer) => {
            writer.writeString(key2);
        });
        return { key2, item2, keyBytes };
    });
    entries2.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries2.length);
    for (const entry2 of entries2) {
        writer.writeString(entry2.key2);
        encodeCondition(writer, entry2.item2);
    }
    const entries3 = Object.entries(value.tags).map(([key3, item3]) => {
        const keyBytes = nestedBytes((writer) => {
            writer.writeString(key3);
        });
        return { key3, item3, keyBytes };
    });
    entries3.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries3.length);
    for (const entry3 of entries3) {
        writer.writeString(entry3.key3);
        encodeCondition(writer, entry3.item3);
    }
    const entries4 = Object.entries(value.aliases).map(([key4, item4]) => {
        const keyBytes = nestedBytes((writer) => {
            writer.writeString(key4);
        });
        return { key4, item4, keyBytes };
    });
    entries4.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries4.length);
    for (const entry4 of entries4) {
        writer.writeString(entry4.key4);
        encodeConditionRef(writer, entry4.item4);
    }
}

/** Decode one ConditionCatalog. */
export function decodeConditionCatalog(reader: BinaryReader): ConditionCatalog {
    const modes = (() => { const length0 = reader.readNumber(); const items0: Record<string, Condition> = {}; for (let index = 0; index < length0; index += 1) { const key = reader.readString(); items0[key] = decodeCondition(reader); } return items0; })();
    const roles = (() => { const length1 = reader.readNumber(); const items1: Record<string, Condition> = {}; for (let index = 0; index < length1; index += 1) { const key = reader.readString(); items1[key] = decodeCondition(reader); } return items1; })();
    const features = (() => { const length2 = reader.readNumber(); const items2: Record<string, Condition> = {}; for (let index = 0; index < length2; index += 1) { const key = reader.readString(); items2[key] = decodeCondition(reader); } return items2; })();
    const tags = (() => { const length3 = reader.readNumber(); const items3: Record<string, Condition> = {}; for (let index = 0; index < length3; index += 1) { const key = reader.readString(); items3[key] = decodeCondition(reader); } return items3; })();
    const aliases = (() => { const length4 = reader.readNumber(); const items4: Record<string, ConditionRef> = {}; for (let index = 0; index < length4; index += 1) { const key = reader.readString(); items4[key] = decodeConditionRef(reader); } return items4; })();

    return {
        modes,
        roles,
        features,
        tags,
        aliases,
    };
}

/** Return one JSON value for one ConditionCatalog. */
export function toJsonConditionCatalog(value: ConditionCatalog): Json {
    return {
        modes: Object.fromEntries(Object.entries(value.modes).map(([key0, item0]) => [key0, toJsonCondition(item0)] as const)),
        roles: Object.fromEntries(Object.entries(value.roles).map(([key0, item0]) => [key0, toJsonCondition(item0)] as const)),
        features: Object.fromEntries(Object.entries(value.features).map(([key0, item0]) => [key0, toJsonCondition(item0)] as const)),
        tags: Object.fromEntries(Object.entries(value.tags).map(([key0, item0]) => [key0, toJsonCondition(item0)] as const)),
        aliases: Object.fromEntries(Object.entries(value.aliases).map(([key0, item0]) => [key0, toJsonConditionRef(item0)] as const)),
    };
}

/** Return one ConditionCatalog from one JSON value. */
export function fromJsonConditionCatalog(value: Json): ConditionCatalog {
    const object = jsonObject(value);

    return {
        modes: Object.fromEntries(Object.entries(jsonObject(jsonField(object, "modes"))).map(([key0, item0]) => [key0, fromJsonCondition(item0)] as const)),
        roles: Object.fromEntries(Object.entries(jsonObject(jsonField(object, "roles"))).map(([key0, item0]) => [key0, fromJsonCondition(item0)] as const)),
        features: Object.fromEntries(Object.entries(jsonObject(jsonField(object, "features"))).map(([key0, item0]) => [key0, fromJsonCondition(item0)] as const)),
        tags: Object.fromEntries(Object.entries(jsonObject(jsonField(object, "tags"))).map(([key0, item0]) => [key0, fromJsonCondition(item0)] as const)),
        aliases: Object.fromEntries(Object.entries(jsonObject(jsonField(object, "aliases"))).map(([key0, item0]) => [key0, fromJsonConditionRef(item0)] as const)),
    };
}

/** Named source graph condition. */
export type Condition = {
    /** Human-readable condition description. */
    readonly description?: string;
    /** Condition labels. */
    readonly labels: Readonly<Record<string, string>>;
    /** Condition names included before this condition. */
    readonly extends: ReadonlyArray<string>;
    /** Dependencies enabled by this condition. */
    readonly dependencies: Readonly<Record<string, Dependency>>;
};

export const Condition = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: Condition): void {
        encodeCondition(writer, value);
    },

    /** Decode one Condition. */
    decode(reader: BinaryReader): Condition {
        return decodeCondition(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Condition): Json {
        return toJsonCondition(value);
    },

    /** Return one Condition from one JSON value. */
    fromJson(value: Json): Condition {
        return fromJsonCondition(value);
    },
};

/** Encode one Condition. */
export function encodeCondition(writer: BinaryWriter, value: Condition): void {
    writer.writeOption(value.description, (value0) => {
        writer.writeString(value0);
    });
    const entries1 = Object.entries(value.labels).map(([key1, item1]) => {
        const keyBytes = nestedBytes((writer) => {
            writer.writeString(key1);
        });
        return { key1, item1, keyBytes };
    });
    entries1.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries1.length);
    for (const entry1 of entries1) {
        writer.writeString(entry1.key1);
        writer.writeString(entry1.item1);
    }
    writer.writeUnsigned(value.extends.length);
    for (const item2 of value.extends) {
        writer.writeString(item2);
    }
    const entries3 = Object.entries(value.dependencies).map(([key3, item3]) => {
        const keyBytes = nestedBytes((writer) => {
            writer.writeString(key3);
        });
        return { key3, item3, keyBytes };
    });
    entries3.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries3.length);
    for (const entry3 of entries3) {
        writer.writeString(entry3.key3);
        encodeDependency(writer, entry3.item3);
    }
}

/** Decode one Condition. */
export function decodeCondition(reader: BinaryReader): Condition {
    const description = reader.readOption(() => reader.readString());
    const labels = (() => { const length1 = reader.readNumber(); const items1: Record<string, string> = {}; for (let index = 0; index < length1; index += 1) { const key = reader.readString(); items1[key] = reader.readString(); } return items1; })();
    const extends_ = (() => { const length2 = reader.readNumber(); const items2: Array<string> = []; for (let index = 0; index < length2; index += 1) { items2.push(reader.readString()); } return items2; })();
    const dependencies = (() => { const length3 = reader.readNumber(); const items3: Record<string, Dependency> = {}; for (let index = 0; index < length3; index += 1) { const key = reader.readString(); items3[key] = decodeDependency(reader); } return items3; })();

    return {
        ...(description === undefined ? {} : { description }),
        labels,
        extends: extends_,
        dependencies,
    };
}

/** Return one JSON value for one Condition. */
export function toJsonCondition(value: Condition): Json {
    return {
        ...(value.description === undefined ? {} : { description: value.description }),
        labels: Object.fromEntries(Object.entries(value.labels).map(([key0, item0]) => [key0, item0] as const)),
        extends: value.extends.map((item0) => item0),
        dependencies: Object.fromEntries(Object.entries(value.dependencies).map(([key0, item0]) => [key0, toJsonDependency(item0)] as const)),
    };
}

/** Return one Condition from one JSON value. */
export function fromJsonCondition(value: Json): Condition {
    const object = jsonObject(value);

    return {
        description: jsonOptional(object, "description", (value) => jsonString(value)),
        labels: Object.fromEntries(Object.entries(jsonObject(jsonField(object, "labels"))).map(([key0, item0]) => [key0, jsonString(item0)] as const)),
        extends: jsonArray(jsonField(object, "extends")).map((item0) => jsonString(item0)),
        dependencies: Object.fromEntries(Object.entries(jsonObject(jsonField(object, "dependencies"))).map(([key0, item0]) => [key0, fromJsonDependency(item0)] as const)),
    };
}
