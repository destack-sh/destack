/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, jsonArray, jsonField, jsonObject, jsonOptional, jsonString } from "../../../../protocol/serde.js";
import type { Stage } from "../stage.js";
import { decodeStage, encodeStage, fromJsonStage, toJsonStage } from "../stage.js";

/** Target contribution to the active source graph condition set. */
export type TargetConditionSet = {
    /** Explicit profile name for this target. */
    readonly profile?: string;
    /** Release stage for this target. */
    readonly stage?: Stage;
    /** Active source graph modes for this target. */
    readonly modes: ReadonlyArray<string>;
    /** Active source graph roles for this target. */
    readonly roles: ReadonlyArray<string>;
    /** Active optional features for this target. */
    readonly features: ReadonlyArray<string>;
    /** Active source graph tags for this target. */
    readonly tags: ReadonlyArray<string>;
};

export const TargetConditionSet = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: TargetConditionSet): void {
        encodeTargetConditionSet(writer, value);
    },

    /** Decode one TargetConditionSet. */
    decode(reader: BinaryReader): TargetConditionSet {
        return decodeTargetConditionSet(reader);
    },

    /** Return this value as JSON. */
    toJson(value: TargetConditionSet): Json {
        return toJsonTargetConditionSet(value);
    },

    /** Return one TargetConditionSet from one JSON value. */
    fromJson(value: Json): TargetConditionSet {
        return fromJsonTargetConditionSet(value);
    },
};

/** Encode one TargetConditionSet. */
export function encodeTargetConditionSet(writer: BinaryWriter, value: TargetConditionSet): void {
    writer.writeOption(value.profile, (value0) => {
        writer.writeString(value0);
    });
    writer.writeOption(value.stage, (value1) => {
        encodeStage(writer, value1);
    });
    writer.writeUnsigned(value.modes.length);
    for (const item2 of value.modes) {
        writer.writeString(item2);
    }
    writer.writeUnsigned(value.roles.length);
    for (const item3 of value.roles) {
        writer.writeString(item3);
    }
    writer.writeUnsigned(value.features.length);
    for (const item4 of value.features) {
        writer.writeString(item4);
    }
    writer.writeUnsigned(value.tags.length);
    for (const item5 of value.tags) {
        writer.writeString(item5);
    }
}

/** Decode one TargetConditionSet. */
export function decodeTargetConditionSet(reader: BinaryReader): TargetConditionSet {
    const profile = reader.readOption(() => reader.readString());
    const stage = reader.readOption(() => decodeStage(reader));
    const modes = (() => { const length2 = reader.readNumber(); const items2: Array<string> = []; for (let index = 0; index < length2; index += 1) { items2.push(reader.readString()); } return items2; })();
    const roles = (() => { const length3 = reader.readNumber(); const items3: Array<string> = []; for (let index = 0; index < length3; index += 1) { items3.push(reader.readString()); } return items3; })();
    const features = (() => { const length4 = reader.readNumber(); const items4: Array<string> = []; for (let index = 0; index < length4; index += 1) { items4.push(reader.readString()); } return items4; })();
    const tags = (() => { const length5 = reader.readNumber(); const items5: Array<string> = []; for (let index = 0; index < length5; index += 1) { items5.push(reader.readString()); } return items5; })();

    return {
        ...(profile === undefined ? {} : { profile }),
        ...(stage === undefined ? {} : { stage }),
        modes,
        roles,
        features,
        tags,
    };
}

/** Return one JSON value for one TargetConditionSet. */
export function toJsonTargetConditionSet(value: TargetConditionSet): Json {
    return {
        ...(value.profile === undefined ? {} : { profile: value.profile }),
        ...(value.stage === undefined ? {} : { stage: toJsonStage(value.stage) }),
        modes: value.modes.map((item0) => item0),
        roles: value.roles.map((item0) => item0),
        features: value.features.map((item0) => item0),
        tags: value.tags.map((item0) => item0),
    };
}

/** Return one TargetConditionSet from one JSON value. */
export function fromJsonTargetConditionSet(value: Json): TargetConditionSet {
    const object = jsonObject(value);

    return {
        profile: jsonOptional(object, "profile", (value) => jsonString(value)),
        stage: jsonOptional(object, "stage", (value) => fromJsonStage(value)),
        modes: jsonArray(jsonField(object, "modes")).map((item0) => jsonString(item0)),
        roles: jsonArray(jsonField(object, "roles")).map((item0) => jsonString(item0)),
        features: jsonArray(jsonField(object, "features")).map((item0) => jsonString(item0)),
        tags: jsonArray(jsonField(object, "tags")).map((item0) => jsonString(item0)),
    };
}
