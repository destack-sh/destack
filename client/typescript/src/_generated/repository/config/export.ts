/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, jsonField, jsonObject, jsonOptional, jsonString } from "../../../protocol/serde.js";
import type { ConditionRef } from "./condition.js";
import { decodeConditionRef, encodeConditionRef, fromJsonConditionRef, toJsonConditionRef } from "./condition.js";

/** Public package material declaration. */
export type Export = {
    /** Exported material kind. */
    readonly kind: ExportKind;
    /** Package relative material path. */
    readonly path: string;
    /** Condition predicate required for this export. */
    readonly when?: ConditionRef;
};

export const Export = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: Export): void {
        encodeExport(writer, value);
    },

    /** Decode one Export. */
    decode(reader: BinaryReader): Export {
        return decodeExport(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Export): Json {
        return toJsonExport(value);
    },

    /** Return one Export from one JSON value. */
    fromJson(value: Json): Export {
        return fromJsonExport(value);
    },
};

/** Encode one Export. */
export function encodeExport(writer: BinaryWriter, value: Export): void {
    encodeExportKind(writer, value.kind);
    writer.writeString(value.path);
    writer.writeOption(value.when, (value2) => {
        encodeConditionRef(writer, value2);
    });
}

/** Decode one Export. */
export function decodeExport(reader: BinaryReader): Export {
    const kind = decodeExportKind(reader);
    const path = reader.readString();
    const when = reader.readOption(() => decodeConditionRef(reader));

    return {
        kind,
        path,
        ...(when === undefined ? {} : { when }),
    };
}

/** Return one JSON value for one Export. */
export function toJsonExport(value: Export): Json {
    return {
        kind: toJsonExportKind(value.kind),
        path: value.path,
        ...(value.when === undefined ? {} : { when: toJsonConditionRef(value.when) }),
    };
}

/** Return one Export from one JSON value. */
export function fromJsonExport(value: Json): Export {
    const object = jsonObject(value);

    return {
        kind: fromJsonExportKind(jsonField(object, "kind")),
        path: jsonString(jsonField(object, "path")),
        when: jsonOptional(object, "when", (value) => fromJsonConditionRef(value)),
    };
}

/** Public package material kind. */
export type ExportKind = "module" | "asset" | "template" | "reflect" | "simulation" | "service" | "app";

export const ExportKind = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ExportKind): void {
        encodeExportKind(writer, value);
    },

    /** Decode one ExportKind. */
    decode(reader: BinaryReader): ExportKind {
        return decodeExportKind(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ExportKind): Json {
        return toJsonExportKind(value);
    },

    /** Return one ExportKind from one JSON value. */
    fromJson(value: Json): ExportKind {
        return fromJsonExportKind(value);
    },
};

/** Encode one ExportKind. */
export function encodeExportKind(writer: BinaryWriter, value: ExportKind): void {
    switch (value) {
        case "module":
            writer.writeUnsigned(0);
            return;
        case "asset":
            writer.writeUnsigned(1);
            return;
        case "template":
            writer.writeUnsigned(2);
            return;
        case "reflect":
            writer.writeUnsigned(3);
            return;
        case "simulation":
            writer.writeUnsigned(4);
            return;
        case "service":
            writer.writeUnsigned(5);
            return;
        case "app":
            writer.writeUnsigned(6);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one ExportKind. */
export function decodeExportKind(reader: BinaryReader): ExportKind {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "module";
        case 1:
            return "asset";
        case 2:
            return "template";
        case 3:
            return "reflect";
        case 4:
            return "simulation";
        case 5:
            return "service";
        case 6:
            return "app";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one ExportKind. */
export function toJsonExportKind(value: ExportKind): Json {
    return value;
}

/** Return one ExportKind from one JSON value. */
export function fromJsonExportKind(value: Json): ExportKind {
    const variant = jsonString(value);

    switch (variant) {
        case "module":
            return "module";
        case "asset":
            return "asset";
        case "template":
            return "template";
        case "reflect":
            return "reflect";
        case "simulation":
            return "simulation";
        case "service":
            return "service";
        case "app":
            return "app";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}
