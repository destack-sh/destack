/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, compareBytes, jsonArray, jsonBool, jsonField, jsonObject, jsonOptional, jsonString, nestedBytes } from "../../../protocol/serde.js";
import type { CompilerOptions } from "./compiler.js";
import type { ConditionCatalog } from "./condition.js";
import type { Dependency } from "./dependency.js";
import type { ConditionalDependencies } from "./dependency.js";
import type { PackagePatch } from "./dependency.js";
import type { Export } from "./export.js";
import type { FormatterOptions } from "./formatter.js";
import type { LinterOptions } from "./linter/core.js";
import type { Policy } from "./policy.js";
import type { Product } from "./product/product.js";
import type { ProfileOptions } from "./profile.js";
import type { RuntimeOptions } from "./runtime/runtime.js";
import type { Stage } from "./stage.js";
import type { Target } from "./target/target.js";
import type { Task } from "./task.js";
import type { Topology } from "./topology.js";
import type { Vendor } from "./vendor.js";
import { decodeCompilerOptions, encodeCompilerOptions, fromJsonCompilerOptions, toJsonCompilerOptions } from "./compiler.js";
import { decodeConditionCatalog, encodeConditionCatalog, fromJsonConditionCatalog, toJsonConditionCatalog } from "./condition.js";
import { decodeDependency, encodeDependency, fromJsonDependency, toJsonDependency } from "./dependency.js";
import { decodeConditionalDependencies, encodeConditionalDependencies, fromJsonConditionalDependencies, toJsonConditionalDependencies } from "./dependency.js";
import { decodePackagePatch, encodePackagePatch, fromJsonPackagePatch, toJsonPackagePatch } from "./dependency.js";
import { decodeExport, encodeExport, fromJsonExport, toJsonExport } from "./export.js";
import { decodeFormatterOptions, encodeFormatterOptions, fromJsonFormatterOptions, toJsonFormatterOptions } from "./formatter.js";
import { decodeLinterOptions, encodeLinterOptions, fromJsonLinterOptions, toJsonLinterOptions } from "./linter/core.js";
import { decodePolicy, encodePolicy, fromJsonPolicy, toJsonPolicy } from "./policy.js";
import { decodeProduct, encodeProduct, fromJsonProduct, toJsonProduct } from "./product/product.js";
import { decodeProfileOptions, encodeProfileOptions, fromJsonProfileOptions, toJsonProfileOptions } from "./profile.js";
import { decodeRuntimeOptions, encodeRuntimeOptions, fromJsonRuntimeOptions, toJsonRuntimeOptions } from "./runtime/runtime.js";
import { decodeStage, encodeStage, fromJsonStage, toJsonStage } from "./stage.js";
import { decodeTarget, encodeTarget, fromJsonTarget, toJsonTarget } from "./target/target.js";
import { decodeTask, encodeTask, fromJsonTask, toJsonTask } from "./task.js";
import { decodeTopology, encodeTopology, fromJsonTopology, toJsonTopology } from "./topology.js";
import { decodeVendor, encodeVendor, fromJsonVendor, toJsonVendor } from "./vendor.js";

/** Workspace package layout. */
export type WorkspaceLayout = {
    /** Package root glob patterns. */
    readonly packages?: ReadonlyArray<string>;
    /** Named groups of package paths. */
    readonly groups?: Readonly<Record<string, ReadonlyArray<string>>>;
};

export const WorkspaceLayout = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: WorkspaceLayout): void {
        encodeWorkspaceLayout(writer, value);
    },

    /** Decode one WorkspaceLayout. */
    decode(reader: BinaryReader): WorkspaceLayout {
        return decodeWorkspaceLayout(reader);
    },

    /** Return this value as JSON. */
    toJson(value: WorkspaceLayout): Json {
        return toJsonWorkspaceLayout(value);
    },

    /** Return one WorkspaceLayout from one JSON value. */
    fromJson(value: Json): WorkspaceLayout {
        return fromJsonWorkspaceLayout(value);
    },
};

/** Encode one WorkspaceLayout. */
export function encodeWorkspaceLayout(writer: BinaryWriter, value: WorkspaceLayout): void {
    writer.writeOption(value.packages, (value0) => {
        writer.writeUnsigned(value0.length);
        for (const item1 of value0) {
            writer.writeString(item1);
        }
    });
    writer.writeOption(value.groups, (value1) => {
        const entries2 = Object.entries(value1).map(([key2, item2]) => {
            const keyBytes = nestedBytes((writer) => {
                writer.writeString(key2);
            });
            return { key2, item2, keyBytes };
        });
        entries2.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
        writer.writeUnsigned(entries2.length);
        for (const entry2 of entries2) {
            writer.writeString(entry2.key2);
            writer.writeUnsigned(entry2.item2.length);
            for (const item3 of entry2.item2) {
                writer.writeString(item3);
            }
        }
    });
}

/** Decode one WorkspaceLayout. */
export function decodeWorkspaceLayout(reader: BinaryReader): WorkspaceLayout {
    const packages = reader.readOption(() => (() => { const length1 = reader.readNumber(); const items1: Array<string> = []; for (let index = 0; index < length1; index += 1) { items1.push(reader.readString()); } return items1; })());
    const groups = reader.readOption(() => (() => { const length2 = reader.readNumber(); const items2: Record<string, ReadonlyArray<string>> = {}; for (let index = 0; index < length2; index += 1) { const key = reader.readString(); items2[key] = (() => { const length4 = reader.readNumber(); const items4: Array<string> = []; for (let index = 0; index < length4; index += 1) { items4.push(reader.readString()); } return items4; })(); } return items2; })());

    return {
        ...(packages === undefined ? {} : { packages }),
        ...(groups === undefined ? {} : { groups }),
    };
}

/** Return one JSON value for one WorkspaceLayout. */
export function toJsonWorkspaceLayout(value: WorkspaceLayout): Json {
    return {
        ...(value.packages === undefined ? {} : { packages: value.packages.map((item0) => item0) }),
        ...(value.groups === undefined ? {} : { groups: Object.fromEntries(Object.entries(value.groups).map(([key0, item0]) => [key0, item0.map((item1) => item1)] as const)) }),
    };
}

/** Return one WorkspaceLayout from one JSON value. */
export function fromJsonWorkspaceLayout(value: Json): WorkspaceLayout {
    const object = jsonObject(value);

    return {
        packages: jsonOptional(object, "packages", (value) => jsonArray(value).map((item0) => jsonString(item0))),
        groups: jsonOptional(object, "groups", (value) => Object.fromEntries(Object.entries(jsonObject(value)).map(([key0, item0]) => [key0, jsonArray(item0).map((item1) => jsonString(item1))] as const))),
    };
}

/** Destack configuration document. */
export type Destack = {
    /** Package name. */
    readonly name?: string;
    /** Package version. */
    readonly version?: string;
    /** Package release stage. */
    readonly stage?: Stage;
    /** Whether the package is private. */
    readonly private?: boolean;
    /** Package description. */
    readonly description?: string;
    /** Package license identifier. */
    readonly license?: string;
    /** Package repository metadata. */
    readonly repository?: unknown;
    /** Package homepage. */
    readonly homepage?: string;
    /** Package keywords. */
    readonly keywords: ReadonlyArray<string>;
    /** Repository wide workspace package and group configuration. */
    readonly workspace?: WorkspaceLayout;
    /** Config path inherited before this config. */
    readonly extends?: string;
    /** Specific files to include in the project. */
    readonly files: ReadonlyArray<string>;
    /** Glob patterns for files to include. */
    readonly include: ReadonlyArray<string>;
    /** Glob patterns for files to exclude. */
    readonly exclude: ReadonlyArray<string>;
    /** Public package exports. */
    readonly exports: Readonly<Record<string, Export>>;
    /** Package dependencies. */
    readonly dependencies: Readonly<Record<string, Dependency>>;
    /** Dependencies enabled by condition predicates. */
    readonly conditionalDependencies: ReadonlyArray<ConditionalDependencies>;
    /** Package dependency overrides. */
    readonly overrides: Readonly<Record<string, Dependency>>;
    /** Package patch files. */
    readonly patches: Readonly<Record<string, PackagePatch>>;
    /** Vendored dependency resolution declaration. */
    readonly vendor: Vendor;
    /** Package topology definition. */
    readonly topology: Topology;
    /** Compiler configuration. */
    readonly compiler: CompilerOptions;
    /** Package policy declarations and rules. */
    readonly policy: Policy;
    /** Runtime configuration. */
    readonly runtime: RuntimeOptions;
    /** Formatter configuration. */
    readonly formatter: FormatterOptions;
    /** Linter configuration. */
    readonly linter: LinterOptions;
    /** Build targets. */
    readonly targets: Readonly<Record<string, Target>>;
    /** Deliverable products. */
    readonly products: Readonly<Record<string, Product>>;
    /** Named profiles for semantic configuration. */
    readonly profiles: Readonly<Record<string, ProfileOptions>>;
    /** Named source graph conditions. */
    readonly conditions: ConditionCatalog;
    /** Named toolchain and shell tasks. */
    readonly tasks: Readonly<Record<string, Task>>;
    /** Default target for the package. */
    readonly defaultTarget?: string;
    /** Default product for the package. */
    readonly defaultProduct?: string;
};

export const Destack = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: Destack): void {
        encodeDestack(writer, value);
    },

    /** Decode one Destack. */
    decode(reader: BinaryReader): Destack {
        return decodeDestack(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Destack): Json {
        return toJsonDestack(value);
    },

    /** Return one Destack from one JSON value. */
    fromJson(value: Json): Destack {
        return fromJsonDestack(value);
    },
};

/** Encode one Destack. */
export function encodeDestack(writer: BinaryWriter, value: Destack): void {
    writer.writeOption(value.name, (value0) => {
        writer.writeString(value0);
    });
    writer.writeOption(value.version, (value1) => {
        writer.writeString(value1);
    });
    writer.writeOption(value.stage, (value2) => {
        encodeStage(writer, value2);
    });
    writer.writeOption(value.private, (value3) => {
        writer.writeBool(value3);
    });
    writer.writeOption(value.description, (value4) => {
        writer.writeString(value4);
    });
    writer.writeOption(value.license, (value5) => {
        writer.writeString(value5);
    });
    writer.writeOption(value.repository, (value6) => {
        writer.writeJson(value6);
    });
    writer.writeOption(value.homepage, (value7) => {
        writer.writeString(value7);
    });
    writer.writeUnsigned(value.keywords.length);
    for (const item8 of value.keywords) {
        writer.writeString(item8);
    }
    writer.writeOption(value.workspace, (value9) => {
        encodeWorkspaceLayout(writer, value9);
    });
    writer.writeOption(value.extends, (value10) => {
        writer.writeString(value10);
    });
    writer.writeUnsigned(value.files.length);
    for (const item11 of value.files) {
        writer.writeString(item11);
    }
    writer.writeUnsigned(value.include.length);
    for (const item12 of value.include) {
        writer.writeString(item12);
    }
    writer.writeUnsigned(value.exclude.length);
    for (const item13 of value.exclude) {
        writer.writeString(item13);
    }
    const entries14 = Object.entries(value.exports).map(([key14, item14]) => {
        const keyBytes = nestedBytes((writer) => {
            writer.writeString(key14);
        });
        return { key14, item14, keyBytes };
    });
    entries14.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries14.length);
    for (const entry14 of entries14) {
        writer.writeString(entry14.key14);
        encodeExport(writer, entry14.item14);
    }
    const entries15 = Object.entries(value.dependencies).map(([key15, item15]) => {
        const keyBytes = nestedBytes((writer) => {
            writer.writeString(key15);
        });
        return { key15, item15, keyBytes };
    });
    entries15.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries15.length);
    for (const entry15 of entries15) {
        writer.writeString(entry15.key15);
        encodeDependency(writer, entry15.item15);
    }
    writer.writeUnsigned(value.conditionalDependencies.length);
    for (const item16 of value.conditionalDependencies) {
        encodeConditionalDependencies(writer, item16);
    }
    const entries17 = Object.entries(value.overrides).map(([key17, item17]) => {
        const keyBytes = nestedBytes((writer) => {
            writer.writeString(key17);
        });
        return { key17, item17, keyBytes };
    });
    entries17.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries17.length);
    for (const entry17 of entries17) {
        writer.writeString(entry17.key17);
        encodeDependency(writer, entry17.item17);
    }
    const entries18 = Object.entries(value.patches).map(([key18, item18]) => {
        const keyBytes = nestedBytes((writer) => {
            writer.writeString(key18);
        });
        return { key18, item18, keyBytes };
    });
    entries18.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries18.length);
    for (const entry18 of entries18) {
        writer.writeString(entry18.key18);
        encodePackagePatch(writer, entry18.item18);
    }
    encodeVendor(writer, value.vendor);
    encodeTopology(writer, value.topology);
    encodeCompilerOptions(writer, value.compiler);
    encodePolicy(writer, value.policy);
    encodeRuntimeOptions(writer, value.runtime);
    encodeFormatterOptions(writer, value.formatter);
    encodeLinterOptions(writer, value.linter);
    const entries26 = Object.entries(value.targets).map(([key26, item26]) => {
        const keyBytes = nestedBytes((writer) => {
            writer.writeString(key26);
        });
        return { key26, item26, keyBytes };
    });
    entries26.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries26.length);
    for (const entry26 of entries26) {
        writer.writeString(entry26.key26);
        encodeTarget(writer, entry26.item26);
    }
    const entries27 = Object.entries(value.products).map(([key27, item27]) => {
        const keyBytes = nestedBytes((writer) => {
            writer.writeString(key27);
        });
        return { key27, item27, keyBytes };
    });
    entries27.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries27.length);
    for (const entry27 of entries27) {
        writer.writeString(entry27.key27);
        encodeProduct(writer, entry27.item27);
    }
    const entries28 = Object.entries(value.profiles).map(([key28, item28]) => {
        const keyBytes = nestedBytes((writer) => {
            writer.writeString(key28);
        });
        return { key28, item28, keyBytes };
    });
    entries28.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries28.length);
    for (const entry28 of entries28) {
        writer.writeString(entry28.key28);
        encodeProfileOptions(writer, entry28.item28);
    }
    encodeConditionCatalog(writer, value.conditions);
    const entries30 = Object.entries(value.tasks).map(([key30, item30]) => {
        const keyBytes = nestedBytes((writer) => {
            writer.writeString(key30);
        });
        return { key30, item30, keyBytes };
    });
    entries30.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries30.length);
    for (const entry30 of entries30) {
        writer.writeString(entry30.key30);
        encodeTask(writer, entry30.item30);
    }
    writer.writeOption(value.defaultTarget, (value31) => {
        writer.writeString(value31);
    });
    writer.writeOption(value.defaultProduct, (value32) => {
        writer.writeString(value32);
    });
}

/** Decode one Destack. */
export function decodeDestack(reader: BinaryReader): Destack {
    const name = reader.readOption(() => reader.readString());
    const version = reader.readOption(() => reader.readString());
    const stage = reader.readOption(() => decodeStage(reader));
    const private_ = reader.readOption(() => reader.readBool());
    const description = reader.readOption(() => reader.readString());
    const license = reader.readOption(() => reader.readString());
    const repository = reader.readOption(() => reader.readJson());
    const homepage = reader.readOption(() => reader.readString());
    const keywords = (() => { const length8 = reader.readNumber(); const items8: Array<string> = []; for (let index = 0; index < length8; index += 1) { items8.push(reader.readString()); } return items8; })();
    const workspace = reader.readOption(() => decodeWorkspaceLayout(reader));
    const extends_ = reader.readOption(() => reader.readString());
    const files = (() => { const length11 = reader.readNumber(); const items11: Array<string> = []; for (let index = 0; index < length11; index += 1) { items11.push(reader.readString()); } return items11; })();
    const include = (() => { const length12 = reader.readNumber(); const items12: Array<string> = []; for (let index = 0; index < length12; index += 1) { items12.push(reader.readString()); } return items12; })();
    const exclude = (() => { const length13 = reader.readNumber(); const items13: Array<string> = []; for (let index = 0; index < length13; index += 1) { items13.push(reader.readString()); } return items13; })();
    const exports = (() => { const length14 = reader.readNumber(); const items14: Record<string, Export> = {}; for (let index = 0; index < length14; index += 1) { const key = reader.readString(); items14[key] = decodeExport(reader); } return items14; })();
    const dependencies = (() => { const length15 = reader.readNumber(); const items15: Record<string, Dependency> = {}; for (let index = 0; index < length15; index += 1) { const key = reader.readString(); items15[key] = decodeDependency(reader); } return items15; })();
    const conditionalDependencies = (() => { const length16 = reader.readNumber(); const items16: Array<ConditionalDependencies> = []; for (let index = 0; index < length16; index += 1) { items16.push(decodeConditionalDependencies(reader)); } return items16; })();
    const overrides = (() => { const length17 = reader.readNumber(); const items17: Record<string, Dependency> = {}; for (let index = 0; index < length17; index += 1) { const key = reader.readString(); items17[key] = decodeDependency(reader); } return items17; })();
    const patches = (() => { const length18 = reader.readNumber(); const items18: Record<string, PackagePatch> = {}; for (let index = 0; index < length18; index += 1) { const key = reader.readString(); items18[key] = decodePackagePatch(reader); } return items18; })();
    const vendor = decodeVendor(reader);
    const topology = decodeTopology(reader);
    const compiler = decodeCompilerOptions(reader);
    const policy = decodePolicy(reader);
    const runtime = decodeRuntimeOptions(reader);
    const formatter = decodeFormatterOptions(reader);
    const linter = decodeLinterOptions(reader);
    const targets = (() => { const length26 = reader.readNumber(); const items26: Record<string, Target> = {}; for (let index = 0; index < length26; index += 1) { const key = reader.readString(); items26[key] = decodeTarget(reader); } return items26; })();
    const products = (() => { const length27 = reader.readNumber(); const items27: Record<string, Product> = {}; for (let index = 0; index < length27; index += 1) { const key = reader.readString(); items27[key] = decodeProduct(reader); } return items27; })();
    const profiles = (() => { const length28 = reader.readNumber(); const items28: Record<string, ProfileOptions> = {}; for (let index = 0; index < length28; index += 1) { const key = reader.readString(); items28[key] = decodeProfileOptions(reader); } return items28; })();
    const conditions = decodeConditionCatalog(reader);
    const tasks = (() => { const length30 = reader.readNumber(); const items30: Record<string, Task> = {}; for (let index = 0; index < length30; index += 1) { const key = reader.readString(); items30[key] = decodeTask(reader); } return items30; })();
    const defaultTarget = reader.readOption(() => reader.readString());
    const defaultProduct = reader.readOption(() => reader.readString());

    return {
        ...(name === undefined ? {} : { name }),
        ...(version === undefined ? {} : { version }),
        ...(stage === undefined ? {} : { stage }),
        ...(private_ === undefined ? {} : { private: private_ }),
        ...(description === undefined ? {} : { description }),
        ...(license === undefined ? {} : { license }),
        ...(repository === undefined ? {} : { repository }),
        ...(homepage === undefined ? {} : { homepage }),
        keywords,
        ...(workspace === undefined ? {} : { workspace }),
        ...(extends_ === undefined ? {} : { extends: extends_ }),
        files,
        include,
        exclude,
        exports,
        dependencies,
        conditionalDependencies,
        overrides,
        patches,
        vendor,
        topology,
        compiler,
        policy,
        runtime,
        formatter,
        linter,
        targets,
        products,
        profiles,
        conditions,
        tasks,
        ...(defaultTarget === undefined ? {} : { defaultTarget }),
        ...(defaultProduct === undefined ? {} : { defaultProduct }),
    };
}

/** Return one JSON value for one Destack. */
export function toJsonDestack(value: Destack): Json {
    return {
        ...(value.name === undefined ? {} : { name: value.name }),
        ...(value.version === undefined ? {} : { version: value.version }),
        ...(value.stage === undefined ? {} : { stage: toJsonStage(value.stage) }),
        ...(value.private === undefined ? {} : { private: value.private }),
        ...(value.description === undefined ? {} : { description: value.description }),
        ...(value.license === undefined ? {} : { license: value.license }),
        ...(value.repository === undefined ? {} : { repository: value.repository as Json }),
        ...(value.homepage === undefined ? {} : { homepage: value.homepage }),
        keywords: value.keywords.map((item0) => item0),
        ...(value.workspace === undefined ? {} : { workspace: toJsonWorkspaceLayout(value.workspace) }),
        ...(value.extends === undefined ? {} : { extends: value.extends }),
        files: value.files.map((item0) => item0),
        include: value.include.map((item0) => item0),
        exclude: value.exclude.map((item0) => item0),
        exports: Object.fromEntries(Object.entries(value.exports).map(([key0, item0]) => [key0, toJsonExport(item0)] as const)),
        dependencies: Object.fromEntries(Object.entries(value.dependencies).map(([key0, item0]) => [key0, toJsonDependency(item0)] as const)),
        conditionalDependencies: value.conditionalDependencies.map((item0) => toJsonConditionalDependencies(item0)),
        overrides: Object.fromEntries(Object.entries(value.overrides).map(([key0, item0]) => [key0, toJsonDependency(item0)] as const)),
        patches: Object.fromEntries(Object.entries(value.patches).map(([key0, item0]) => [key0, toJsonPackagePatch(item0)] as const)),
        vendor: toJsonVendor(value.vendor),
        topology: toJsonTopology(value.topology),
        compiler: toJsonCompilerOptions(value.compiler),
        policy: toJsonPolicy(value.policy),
        runtime: toJsonRuntimeOptions(value.runtime),
        formatter: toJsonFormatterOptions(value.formatter),
        linter: toJsonLinterOptions(value.linter),
        targets: Object.fromEntries(Object.entries(value.targets).map(([key0, item0]) => [key0, toJsonTarget(item0)] as const)),
        products: Object.fromEntries(Object.entries(value.products).map(([key0, item0]) => [key0, toJsonProduct(item0)] as const)),
        profiles: Object.fromEntries(Object.entries(value.profiles).map(([key0, item0]) => [key0, toJsonProfileOptions(item0)] as const)),
        conditions: toJsonConditionCatalog(value.conditions),
        tasks: Object.fromEntries(Object.entries(value.tasks).map(([key0, item0]) => [key0, toJsonTask(item0)] as const)),
        ...(value.defaultTarget === undefined ? {} : { defaultTarget: value.defaultTarget }),
        ...(value.defaultProduct === undefined ? {} : { defaultProduct: value.defaultProduct }),
    };
}

/** Return one Destack from one JSON value. */
export function fromJsonDestack(value: Json): Destack {
    const object = jsonObject(value);

    return {
        name: jsonOptional(object, "name", (value) => jsonString(value)),
        version: jsonOptional(object, "version", (value) => jsonString(value)),
        stage: jsonOptional(object, "stage", (value) => fromJsonStage(value)),
        private: jsonOptional(object, "private", (value) => jsonBool(value)),
        description: jsonOptional(object, "description", (value) => jsonString(value)),
        license: jsonOptional(object, "license", (value) => jsonString(value)),
        repository: jsonOptional(object, "repository", (value) => value),
        homepage: jsonOptional(object, "homepage", (value) => jsonString(value)),
        keywords: jsonArray(jsonField(object, "keywords")).map((item0) => jsonString(item0)),
        workspace: jsonOptional(object, "workspace", (value) => fromJsonWorkspaceLayout(value)),
        extends: jsonOptional(object, "extends", (value) => jsonString(value)),
        files: jsonArray(jsonField(object, "files")).map((item0) => jsonString(item0)),
        include: jsonArray(jsonField(object, "include")).map((item0) => jsonString(item0)),
        exclude: jsonArray(jsonField(object, "exclude")).map((item0) => jsonString(item0)),
        exports: Object.fromEntries(Object.entries(jsonObject(jsonField(object, "exports"))).map(([key0, item0]) => [key0, fromJsonExport(item0)] as const)),
        dependencies: Object.fromEntries(Object.entries(jsonObject(jsonField(object, "dependencies"))).map(([key0, item0]) => [key0, fromJsonDependency(item0)] as const)),
        conditionalDependencies: jsonArray(jsonField(object, "conditionalDependencies")).map((item0) => fromJsonConditionalDependencies(item0)),
        overrides: Object.fromEntries(Object.entries(jsonObject(jsonField(object, "overrides"))).map(([key0, item0]) => [key0, fromJsonDependency(item0)] as const)),
        patches: Object.fromEntries(Object.entries(jsonObject(jsonField(object, "patches"))).map(([key0, item0]) => [key0, fromJsonPackagePatch(item0)] as const)),
        vendor: fromJsonVendor(jsonField(object, "vendor")),
        topology: fromJsonTopology(jsonField(object, "topology")),
        compiler: fromJsonCompilerOptions(jsonField(object, "compiler")),
        policy: fromJsonPolicy(jsonField(object, "policy")),
        runtime: fromJsonRuntimeOptions(jsonField(object, "runtime")),
        formatter: fromJsonFormatterOptions(jsonField(object, "formatter")),
        linter: fromJsonLinterOptions(jsonField(object, "linter")),
        targets: Object.fromEntries(Object.entries(jsonObject(jsonField(object, "targets"))).map(([key0, item0]) => [key0, fromJsonTarget(item0)] as const)),
        products: Object.fromEntries(Object.entries(jsonObject(jsonField(object, "products"))).map(([key0, item0]) => [key0, fromJsonProduct(item0)] as const)),
        profiles: Object.fromEntries(Object.entries(jsonObject(jsonField(object, "profiles"))).map(([key0, item0]) => [key0, fromJsonProfileOptions(item0)] as const)),
        conditions: fromJsonConditionCatalog(jsonField(object, "conditions")),
        tasks: Object.fromEntries(Object.entries(jsonObject(jsonField(object, "tasks"))).map(([key0, item0]) => [key0, fromJsonTask(item0)] as const)),
        defaultTarget: jsonOptional(object, "defaultTarget", (value) => jsonString(value)),
        defaultProduct: jsonOptional(object, "defaultProduct", (value) => jsonString(value)),
    };
}
