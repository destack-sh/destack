/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, jsonBigint, jsonField, jsonInteger, jsonObject, jsonOptional } from "../../../../protocol/serde.js";

/** Runtime heap configuration. */
export type HeapOptions = {
    /** Soft memory limit inherited by local and shared heap collectors. */
    readonly memoryLimitBytes?: bigint;
    /** The proportional heap growth target percentage. */
    readonly growthPercent: number;
    /** Worker-local heap policy. */
    readonly local: LocalHeapOptions;
    /** Runtime-shared heap policy. */
    readonly shared: SharedHeapOptions;
};

export const HeapOptions = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: HeapOptions): void {
        encodeHeapOptions(writer, value);
    },

    /** Decode one HeapOptions. */
    decode(reader: BinaryReader): HeapOptions {
        return decodeHeapOptions(reader);
    },

    /** Return this value as JSON. */
    toJson(value: HeapOptions): Json {
        return toJsonHeapOptions(value);
    },

    /** Return one HeapOptions from one JSON value. */
    fromJson(value: Json): HeapOptions {
        return fromJsonHeapOptions(value);
    },
};

/** Encode one HeapOptions. */
export function encodeHeapOptions(writer: BinaryWriter, value: HeapOptions): void {
    writer.writeOption(value.memoryLimitBytes, (value0) => {
        writer.writeUnsigned(value0);
    });
    writer.writeUnsigned(value.growthPercent);
    encodeLocalHeapOptions(writer, value.local);
    encodeSharedHeapOptions(writer, value.shared);
}

/** Decode one HeapOptions. */
export function decodeHeapOptions(reader: BinaryReader): HeapOptions {
    const memoryLimitBytes = reader.readOption(() => reader.readUnsigned());
    const growthPercent = reader.readNumber();
    const local = decodeLocalHeapOptions(reader);
    const shared = decodeSharedHeapOptions(reader);

    return {
        ...(memoryLimitBytes === undefined ? {} : { memoryLimitBytes }),
        growthPercent,
        local,
        shared,
    };
}

/** Return one JSON value for one HeapOptions. */
export function toJsonHeapOptions(value: HeapOptions): Json {
    return {
        ...(value.memoryLimitBytes === undefined ? {} : { memoryLimitBytes: value.memoryLimitBytes.toString() }),
        growthPercent: value.growthPercent,
        local: toJsonLocalHeapOptions(value.local),
        shared: toJsonSharedHeapOptions(value.shared),
    };
}

/** Return one HeapOptions from one JSON value. */
export function fromJsonHeapOptions(value: Json): HeapOptions {
    const object = jsonObject(value);

    return {
        memoryLimitBytes: jsonOptional(object, "memoryLimitBytes", (value) => jsonBigint(value)),
        growthPercent: jsonInteger(jsonField(object, "growthPercent")),
        local: fromJsonLocalHeapOptions(jsonField(object, "local")),
        shared: fromJsonSharedHeapOptions(jsonField(object, "shared")),
    };
}

/** Runtime local-heap policy. */
export type LocalHeapOptions = {
    /** The byte width for worker-local young space. */
    readonly youngSizeBytes: number;
    /** Minimum heap bytes before normal growth pacing applies. */
    readonly minBytes?: bigint;
    /** Hard limit for total retained local heap bytes. */
    readonly maxBytes?: bigint;
};

export const LocalHeapOptions = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: LocalHeapOptions): void {
        encodeLocalHeapOptions(writer, value);
    },

    /** Decode one LocalHeapOptions. */
    decode(reader: BinaryReader): LocalHeapOptions {
        return decodeLocalHeapOptions(reader);
    },

    /** Return this value as JSON. */
    toJson(value: LocalHeapOptions): Json {
        return toJsonLocalHeapOptions(value);
    },

    /** Return one LocalHeapOptions from one JSON value. */
    fromJson(value: Json): LocalHeapOptions {
        return fromJsonLocalHeapOptions(value);
    },
};

/** Encode one LocalHeapOptions. */
export function encodeLocalHeapOptions(writer: BinaryWriter, value: LocalHeapOptions): void {
    writer.writeUnsigned(value.youngSizeBytes);
    writer.writeOption(value.minBytes, (value1) => {
        writer.writeUnsigned(value1);
    });
    writer.writeOption(value.maxBytes, (value2) => {
        writer.writeUnsigned(value2);
    });
}

/** Decode one LocalHeapOptions. */
export function decodeLocalHeapOptions(reader: BinaryReader): LocalHeapOptions {
    const youngSizeBytes = reader.readNumber();
    const minBytes = reader.readOption(() => reader.readUnsigned());
    const maxBytes = reader.readOption(() => reader.readUnsigned());

    return {
        youngSizeBytes,
        ...(minBytes === undefined ? {} : { minBytes }),
        ...(maxBytes === undefined ? {} : { maxBytes }),
    };
}

/** Return one JSON value for one LocalHeapOptions. */
export function toJsonLocalHeapOptions(value: LocalHeapOptions): Json {
    return {
        youngSizeBytes: value.youngSizeBytes,
        ...(value.minBytes === undefined ? {} : { minBytes: value.minBytes.toString() }),
        ...(value.maxBytes === undefined ? {} : { maxBytes: value.maxBytes.toString() }),
    };
}

/** Return one LocalHeapOptions from one JSON value. */
export function fromJsonLocalHeapOptions(value: Json): LocalHeapOptions {
    const object = jsonObject(value);

    return {
        youngSizeBytes: jsonInteger(jsonField(object, "youngSizeBytes")),
        minBytes: jsonOptional(object, "minBytes", (value) => jsonBigint(value)),
        maxBytes: jsonOptional(object, "maxBytes", (value) => jsonBigint(value)),
    };
}

/** Runtime shared-heap policy. */
export type SharedHeapOptions = {
    /** Minimum heap bytes before normal growth pacing applies. */
    readonly minBytes?: bigint;
    /** Hard limit for total retained shared heap bytes. */
    readonly maxBytes?: bigint;
};

export const SharedHeapOptions = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: SharedHeapOptions): void {
        encodeSharedHeapOptions(writer, value);
    },

    /** Decode one SharedHeapOptions. */
    decode(reader: BinaryReader): SharedHeapOptions {
        return decodeSharedHeapOptions(reader);
    },

    /** Return this value as JSON. */
    toJson(value: SharedHeapOptions): Json {
        return toJsonSharedHeapOptions(value);
    },

    /** Return one SharedHeapOptions from one JSON value. */
    fromJson(value: Json): SharedHeapOptions {
        return fromJsonSharedHeapOptions(value);
    },
};

/** Encode one SharedHeapOptions. */
export function encodeSharedHeapOptions(writer: BinaryWriter, value: SharedHeapOptions): void {
    writer.writeOption(value.minBytes, (value0) => {
        writer.writeUnsigned(value0);
    });
    writer.writeOption(value.maxBytes, (value1) => {
        writer.writeUnsigned(value1);
    });
}

/** Decode one SharedHeapOptions. */
export function decodeSharedHeapOptions(reader: BinaryReader): SharedHeapOptions {
    const minBytes = reader.readOption(() => reader.readUnsigned());
    const maxBytes = reader.readOption(() => reader.readUnsigned());

    return {
        ...(minBytes === undefined ? {} : { minBytes }),
        ...(maxBytes === undefined ? {} : { maxBytes }),
    };
}

/** Return one JSON value for one SharedHeapOptions. */
export function toJsonSharedHeapOptions(value: SharedHeapOptions): Json {
    return {
        ...(value.minBytes === undefined ? {} : { minBytes: value.minBytes.toString() }),
        ...(value.maxBytes === undefined ? {} : { maxBytes: value.maxBytes.toString() }),
    };
}

/** Return one SharedHeapOptions from one JSON value. */
export function fromJsonSharedHeapOptions(value: Json): SharedHeapOptions {
    const object = jsonObject(value);

    return {
        minBytes: jsonOptional(object, "minBytes", (value) => jsonBigint(value)),
        maxBytes: jsonOptional(object, "maxBytes", (value) => jsonBigint(value)),
    };
}
