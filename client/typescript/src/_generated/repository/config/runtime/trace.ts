/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, jsonBigint, jsonField, jsonObject, jsonOptional, jsonString } from "../../../../protocol/serde.js";
import type { ReplayPayloadMode } from "./execution.js";
import { decodeReplayPayloadMode, encodeReplayPayloadMode, fromJsonReplayPayloadMode, toJsonReplayPayloadMode } from "./execution.js";

/** Runtime trace configuration. */
export type TraceOptions = {
    /** The configured restore contract. */
    readonly restore: RestoreMode;
    /** Base path for trace logs. */
    readonly path?: string;
    /** Template for auto-generated log file names. */
    readonly template?: string;
    /** Chunk size in megabytes for log rotation. */
    readonly chunkSizeMb?: bigint;
    /** Trace payload selection for recorded binding calls. */
    readonly payload: ReplayPayloadMode;
};

export const TraceOptions = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: TraceOptions): void {
        encodeTraceOptions(writer, value);
    },

    /** Decode one TraceOptions. */
    decode(reader: BinaryReader): TraceOptions {
        return decodeTraceOptions(reader);
    },

    /** Return this value as JSON. */
    toJson(value: TraceOptions): Json {
        return toJsonTraceOptions(value);
    },

    /** Return one TraceOptions from one JSON value. */
    fromJson(value: Json): TraceOptions {
        return fromJsonTraceOptions(value);
    },
};

/** Encode one TraceOptions. */
export function encodeTraceOptions(writer: BinaryWriter, value: TraceOptions): void {
    encodeRestoreMode(writer, value.restore);
    writer.writeOption(value.path, (value1) => {
        writer.writeString(value1);
    });
    writer.writeOption(value.template, (value2) => {
        writer.writeString(value2);
    });
    writer.writeOption(value.chunkSizeMb, (value3) => {
        writer.writeUnsigned(value3);
    });
    encodeReplayPayloadMode(writer, value.payload);
}

/** Decode one TraceOptions. */
export function decodeTraceOptions(reader: BinaryReader): TraceOptions {
    const restore = decodeRestoreMode(reader);
    const path = reader.readOption(() => reader.readString());
    const template = reader.readOption(() => reader.readString());
    const chunkSizeMb = reader.readOption(() => reader.readUnsigned());
    const payload = decodeReplayPayloadMode(reader);

    return {
        restore,
        ...(path === undefined ? {} : { path }),
        ...(template === undefined ? {} : { template }),
        ...(chunkSizeMb === undefined ? {} : { chunkSizeMb }),
        payload,
    };
}

/** Return one JSON value for one TraceOptions. */
export function toJsonTraceOptions(value: TraceOptions): Json {
    return {
        restore: toJsonRestoreMode(value.restore),
        ...(value.path === undefined ? {} : { path: value.path }),
        ...(value.template === undefined ? {} : { template: value.template }),
        ...(value.chunkSizeMb === undefined ? {} : { chunkSizeMb: value.chunkSizeMb.toString() }),
        payload: toJsonReplayPayloadMode(value.payload),
    };
}

/** Return one TraceOptions from one JSON value. */
export function fromJsonTraceOptions(value: Json): TraceOptions {
    const object = jsonObject(value);

    return {
        restore: fromJsonRestoreMode(jsonField(object, "restore")),
        path: jsonOptional(object, "path", (value) => jsonString(value)),
        template: jsonOptional(object, "template", (value) => jsonString(value)),
        chunkSizeMb: jsonOptional(object, "chunkSizeMb", (value) => jsonBigint(value)),
        payload: fromJsonReplayPayloadMode(jsonField(object, "payload")),
    };
}

/** Restore contract for one world checkpoint or replay boundary. */
export type RestoreMode = "world" | "image";

export const RestoreMode = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: RestoreMode): void {
        encodeRestoreMode(writer, value);
    },

    /** Decode one RestoreMode. */
    decode(reader: BinaryReader): RestoreMode {
        return decodeRestoreMode(reader);
    },

    /** Return this value as JSON. */
    toJson(value: RestoreMode): Json {
        return toJsonRestoreMode(value);
    },

    /** Return one RestoreMode from one JSON value. */
    fromJson(value: Json): RestoreMode {
        return fromJsonRestoreMode(value);
    },
};

/** Encode one RestoreMode. */
export function encodeRestoreMode(writer: BinaryWriter, value: RestoreMode): void {
    switch (value) {
        case "world":
            writer.writeUnsigned(0);
            return;
        case "image":
            writer.writeUnsigned(1);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one RestoreMode. */
export function decodeRestoreMode(reader: BinaryReader): RestoreMode {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "world";
        case 1:
            return "image";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one RestoreMode. */
export function toJsonRestoreMode(value: RestoreMode): Json {
    return value;
}

/** Return one RestoreMode from one JSON value. */
export function fromJsonRestoreMode(value: Json): RestoreMode {
    const variant = jsonString(value);

    switch (variant) {
        case "world":
            return "world";
        case "image":
            return "image";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}
