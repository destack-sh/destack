/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, jsonBigint, jsonField, jsonObject, jsonOptional } from "../../../../protocol/serde.js";

/** World clock configuration. */
export type ClockOptions = {
    /** World wall-clock epoch in nanoseconds. */
    readonly epochNanos?: bigint;
};

export const ClockOptions = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ClockOptions): void {
        encodeClockOptions(writer, value);
    },

    /** Decode one ClockOptions. */
    decode(reader: BinaryReader): ClockOptions {
        return decodeClockOptions(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ClockOptions): Json {
        return toJsonClockOptions(value);
    },

    /** Return one ClockOptions from one JSON value. */
    fromJson(value: Json): ClockOptions {
        return fromJsonClockOptions(value);
    },
};

/** Encode one ClockOptions. */
export function encodeClockOptions(writer: BinaryWriter, value: ClockOptions): void {
    writer.writeOption(value.epochNanos, (value0) => {
        writer.writeUnsigned(value0);
    });
}

/** Decode one ClockOptions. */
export function decodeClockOptions(reader: BinaryReader): ClockOptions {
    const epochNanos = reader.readOption(() => reader.readUnsigned());

    return {
        ...(epochNanos === undefined ? {} : { epochNanos }),
    };
}

/** Return one JSON value for one ClockOptions. */
export function toJsonClockOptions(value: ClockOptions): Json {
    return {
        ...(value.epochNanos === undefined ? {} : { epochNanos: value.epochNanos.toString() }),
    };
}

/** Return one ClockOptions from one JSON value. */
export function fromJsonClockOptions(value: Json): ClockOptions {
    const object = jsonObject(value);

    return {
        epochNanos: jsonOptional(object, "epochNanos", (value) => jsonBigint(value)),
    };
}
