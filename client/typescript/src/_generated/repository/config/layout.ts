/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, jsonField, jsonObject, jsonOptional, jsonString } from "../../../protocol/serde.js";

/** Resolved Destack storage layout for one invocation. */
export type DestackLayout = {
    /** Machine-local Destack home. */
    readonly home: string;
    /** Machine-local package directory. */
    readonly packages: string;
    /** Workspace-local cache and session directory. */
    readonly workspaceCache: string;
    /** Workspace-owned vendor directory. */
    readonly vendor: string;
};

export const DestackLayout = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: DestackLayout): void {
        encodeDestackLayout(writer, value);
    },

    /** Decode one DestackLayout. */
    decode(reader: BinaryReader): DestackLayout {
        return decodeDestackLayout(reader);
    },

    /** Return this value as JSON. */
    toJson(value: DestackLayout): Json {
        return toJsonDestackLayout(value);
    },

    /** Return one DestackLayout from one JSON value. */
    fromJson(value: Json): DestackLayout {
        return fromJsonDestackLayout(value);
    },
};

/** Encode one DestackLayout. */
export function encodeDestackLayout(writer: BinaryWriter, value: DestackLayout): void {
    writer.writeString(value.home);
    writer.writeString(value.packages);
    writer.writeString(value.workspaceCache);
    writer.writeString(value.vendor);
}

/** Decode one DestackLayout. */
export function decodeDestackLayout(reader: BinaryReader): DestackLayout {
    const home = reader.readString();
    const packages = reader.readString();
    const workspaceCache = reader.readString();
    const vendor = reader.readString();

    return {
        home,
        packages,
        workspaceCache,
        vendor,
    };
}

/** Return one JSON value for one DestackLayout. */
export function toJsonDestackLayout(value: DestackLayout): Json {
    return {
        home: value.home,
        packages: value.packages,
        workspaceCache: value.workspaceCache,
        vendor: value.vendor,
    };
}

/** Return one DestackLayout from one JSON value. */
export function fromJsonDestackLayout(value: Json): DestackLayout {
    const object = jsonObject(value);

    return {
        home: jsonString(jsonField(object, "home")),
        packages: jsonString(jsonField(object, "packages")),
        workspaceCache: jsonString(jsonField(object, "workspaceCache")),
        vendor: jsonString(jsonField(object, "vendor")),
    };
}

/** Invocation-level layout overrides. */
export type DestackLayoutOverride = {
    /** Home directory override. */
    readonly home?: string;
    /** Package directory override. */
    readonly packages?: string;
    /** Workspace cache override. */
    readonly workspaceCache?: string;
};

export const DestackLayoutOverride = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: DestackLayoutOverride): void {
        encodeDestackLayoutOverride(writer, value);
    },

    /** Decode one DestackLayoutOverride. */
    decode(reader: BinaryReader): DestackLayoutOverride {
        return decodeDestackLayoutOverride(reader);
    },

    /** Return this value as JSON. */
    toJson(value: DestackLayoutOverride): Json {
        return toJsonDestackLayoutOverride(value);
    },

    /** Return one DestackLayoutOverride from one JSON value. */
    fromJson(value: Json): DestackLayoutOverride {
        return fromJsonDestackLayoutOverride(value);
    },
};

/** Encode one DestackLayoutOverride. */
export function encodeDestackLayoutOverride(writer: BinaryWriter, value: DestackLayoutOverride): void {
    writer.writeOption(value.home, (value0) => {
        writer.writeString(value0);
    });
    writer.writeOption(value.packages, (value1) => {
        writer.writeString(value1);
    });
    writer.writeOption(value.workspaceCache, (value2) => {
        writer.writeString(value2);
    });
}

/** Decode one DestackLayoutOverride. */
export function decodeDestackLayoutOverride(reader: BinaryReader): DestackLayoutOverride {
    const home = reader.readOption(() => reader.readString());
    const packages = reader.readOption(() => reader.readString());
    const workspaceCache = reader.readOption(() => reader.readString());

    return {
        ...(home === undefined ? {} : { home }),
        ...(packages === undefined ? {} : { packages }),
        ...(workspaceCache === undefined ? {} : { workspaceCache }),
    };
}

/** Return one JSON value for one DestackLayoutOverride. */
export function toJsonDestackLayoutOverride(value: DestackLayoutOverride): Json {
    return {
        ...(value.home === undefined ? {} : { home: value.home }),
        ...(value.packages === undefined ? {} : { packages: value.packages }),
        ...(value.workspaceCache === undefined ? {} : { workspaceCache: value.workspaceCache }),
    };
}

/** Return one DestackLayoutOverride from one JSON value. */
export function fromJsonDestackLayoutOverride(value: Json): DestackLayoutOverride {
    const object = jsonObject(value);

    return {
        home: jsonOptional(object, "home", (value) => jsonString(value)),
        packages: jsonOptional(object, "packages", (value) => jsonString(value)),
        workspaceCache: jsonOptional(object, "workspaceCache", (value) => jsonString(value)),
    };
}
