/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, compareBytes, jsonArray, jsonBigint, jsonBool, jsonField, jsonInteger, jsonObject, jsonOptional, jsonString, nestedBytes } from "../../../protocol/serde.js";

/** Package directory settings. */
export type PackageSettings = {
    /** Package directory. */
    readonly path?: string;
    /** Maximum package directory size in bytes before pruning is requested. */
    readonly maximumBytes?: bigint;
};

export const PackageSettings = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: PackageSettings): void {
        encodePackageSettings(writer, value);
    },

    /** Decode one PackageSettings. */
    decode(reader: BinaryReader): PackageSettings {
        return decodePackageSettings(reader);
    },

    /** Return this value as JSON. */
    toJson(value: PackageSettings): Json {
        return toJsonPackageSettings(value);
    },

    /** Return one PackageSettings from one JSON value. */
    fromJson(value: Json): PackageSettings {
        return fromJsonPackageSettings(value);
    },
};

/** Encode one PackageSettings. */
export function encodePackageSettings(writer: BinaryWriter, value: PackageSettings): void {
    writer.writeOption(value.path, (value0) => {
        writer.writeString(value0);
    });
    writer.writeOption(value.maximumBytes, (value1) => {
        writer.writeUnsigned(value1);
    });
}

/** Decode one PackageSettings. */
export function decodePackageSettings(reader: BinaryReader): PackageSettings {
    const path = reader.readOption(() => reader.readString());
    const maximumBytes = reader.readOption(() => reader.readUnsigned());

    return {
        ...(path === undefined ? {} : { path }),
        ...(maximumBytes === undefined ? {} : { maximumBytes }),
    };
}

/** Return one JSON value for one PackageSettings. */
export function toJsonPackageSettings(value: PackageSettings): Json {
    return {
        ...(value.path === undefined ? {} : { path: value.path }),
        ...(value.maximumBytes === undefined ? {} : { maximumBytes: value.maximumBytes.toString() }),
    };
}

/** Return one PackageSettings from one JSON value. */
export function fromJsonPackageSettings(value: Json): PackageSettings {
    const object = jsonObject(value);

    return {
        path: jsonOptional(object, "path", (value) => jsonString(value)),
        maximumBytes: jsonOptional(object, "maximumBytes", (value) => jsonBigint(value)),
    };
}

/** Workspace cache settings. */
export type CacheSettings = {
    /** Cache directory. */
    readonly path?: string;
    /** Maximum cache size in bytes before pruning is requested. */
    readonly maximumBytes?: bigint;
};

export const CacheSettings = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: CacheSettings): void {
        encodeCacheSettings(writer, value);
    },

    /** Decode one CacheSettings. */
    decode(reader: BinaryReader): CacheSettings {
        return decodeCacheSettings(reader);
    },

    /** Return this value as JSON. */
    toJson(value: CacheSettings): Json {
        return toJsonCacheSettings(value);
    },

    /** Return one CacheSettings from one JSON value. */
    fromJson(value: Json): CacheSettings {
        return fromJsonCacheSettings(value);
    },
};

/** Encode one CacheSettings. */
export function encodeCacheSettings(writer: BinaryWriter, value: CacheSettings): void {
    writer.writeOption(value.path, (value0) => {
        writer.writeString(value0);
    });
    writer.writeOption(value.maximumBytes, (value1) => {
        writer.writeUnsigned(value1);
    });
}

/** Decode one CacheSettings. */
export function decodeCacheSettings(reader: BinaryReader): CacheSettings {
    const path = reader.readOption(() => reader.readString());
    const maximumBytes = reader.readOption(() => reader.readUnsigned());

    return {
        ...(path === undefined ? {} : { path }),
        ...(maximumBytes === undefined ? {} : { maximumBytes }),
    };
}

/** Return one JSON value for one CacheSettings. */
export function toJsonCacheSettings(value: CacheSettings): Json {
    return {
        ...(value.path === undefined ? {} : { path: value.path }),
        ...(value.maximumBytes === undefined ? {} : { maximumBytes: value.maximumBytes.toString() }),
    };
}

/** Return one CacheSettings from one JSON value. */
export function fromJsonCacheSettings(value: Json): CacheSettings {
    const object = jsonObject(value);

    return {
        path: jsonOptional(object, "path", (value) => jsonString(value)),
        maximumBytes: jsonOptional(object, "maximumBytes", (value) => jsonBigint(value)),
    };
}

/** Registry settings. */
export type RegistrySettings = {
    /** Registry URL. */
    readonly url: string;
    /** Authentication used for this registry. */
    readonly authentication: RegistryAuthentication;
};

export const RegistrySettings = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: RegistrySettings): void {
        encodeRegistrySettings(writer, value);
    },

    /** Decode one RegistrySettings. */
    decode(reader: BinaryReader): RegistrySettings {
        return decodeRegistrySettings(reader);
    },

    /** Return this value as JSON. */
    toJson(value: RegistrySettings): Json {
        return toJsonRegistrySettings(value);
    },

    /** Return one RegistrySettings from one JSON value. */
    fromJson(value: Json): RegistrySettings {
        return fromJsonRegistrySettings(value);
    },
};

/** Encode one RegistrySettings. */
export function encodeRegistrySettings(writer: BinaryWriter, value: RegistrySettings): void {
    writer.writeString(value.url);
    encodeRegistryAuthentication(writer, value.authentication);
}

/** Decode one RegistrySettings. */
export function decodeRegistrySettings(reader: BinaryReader): RegistrySettings {
    const url = reader.readString();
    const authentication = decodeRegistryAuthentication(reader);

    return {
        url,
        authentication,
    };
}

/** Return one JSON value for one RegistrySettings. */
export function toJsonRegistrySettings(value: RegistrySettings): Json {
    return {
        url: value.url,
        authentication: toJsonRegistryAuthentication(value.authentication),
    };
}

/** Return one RegistrySettings from one JSON value. */
export function fromJsonRegistrySettings(value: Json): RegistrySettings {
    const object = jsonObject(value);

    return {
        url: jsonString(jsonField(object, "url")),
        authentication: fromJsonRegistryAuthentication(jsonField(object, "authentication")),
    };
}

/** Registry authentication settings. */
export type RegistryAuthentication =
    /** No registry authentication. */
    | {
          readonly kind: "none";
      }
    /** Inline token stored in the machine-local settings file. */
    | {
          readonly kind: "token";
          /** Registry token. */
          readonly token: string;
      }
    /** Token read from one environment variable. */
    | {
          readonly kind: "tokenFromEnvironment";
          /** Environment variable name. */
          readonly variable: string;
      }
    /** Token or credential material printed by one command. */
    | {
          readonly kind: "command";
          /** Program to run. */
          readonly program: string;
          /** Program arguments. */
          readonly args: ReadonlyArray<string>;
      }
;

export const RegistryAuthentication = {
    /** No registry authentication. */
    none(): RegistryAuthentication {
        return { kind: "none" };
    },

    /** Inline token stored in the machine-local settings file. */
    token(token: string): RegistryAuthentication {
        return { kind: "token", token };
    },

    /** Token read from one environment variable. */
    tokenFromEnvironment(variable: string): RegistryAuthentication {
        return { kind: "tokenFromEnvironment", variable };
    },

    /** Token or credential material printed by one command. */
    command(program: string, args: ReadonlyArray<string>): RegistryAuthentication {
        return { kind: "command", program, args };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: RegistryAuthentication): void {
        encodeRegistryAuthentication(writer, value);
    },

    /** Decode one RegistryAuthentication. */
    decode(reader: BinaryReader): RegistryAuthentication {
        return decodeRegistryAuthentication(reader);
    },

    /** Return this value as JSON. */
    toJson(value: RegistryAuthentication): Json {
        return toJsonRegistryAuthentication(value);
    },

    /** Return one RegistryAuthentication from one JSON value. */
    fromJson(value: Json): RegistryAuthentication {
        return fromJsonRegistryAuthentication(value);
    },
};

/** Encode one RegistryAuthentication. */
export function encodeRegistryAuthentication(writer: BinaryWriter, value: RegistryAuthentication): void {
    switch (value.kind) {
        case "none":
            writer.writeUnsigned(0);
            return;
        case "token":
            writer.writeUnsigned(1);
            writer.writeString(value.token);
            return;
        case "tokenFromEnvironment":
            writer.writeUnsigned(2);
            writer.writeString(value.variable);
            return;
        case "command":
            writer.writeUnsigned(3);
            writer.writeString(value.program);
            writer.writeUnsigned(value.args.length);
            for (const item1 of value.args) {
                writer.writeString(item1);
            }
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one RegistryAuthentication. */
export function decodeRegistryAuthentication(reader: BinaryReader): RegistryAuthentication {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            return { kind: "none" };
        }
        case 1: {
            const token = reader.readString();

            return {
                kind: "token",
                token,
            };
        }
        case 2: {
            const variable = reader.readString();

            return {
                kind: "tokenFromEnvironment",
                variable,
            };
        }
        case 3: {
            const program = reader.readString();
            const args = (() => { const length1 = reader.readNumber(); const items1: Array<string> = []; for (let index = 0; index < length1; index += 1) { items1.push(reader.readString()); } return items1; })();

            return {
                kind: "command",
                program,
                args,
            };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one RegistryAuthentication. */
export function toJsonRegistryAuthentication(value: RegistryAuthentication): Json {
    switch (value.kind) {
        case "none":
            return {
                kind: "none",
            };
        case "token":
            return {
                kind: "token",
                token: value.token,
            };
        case "tokenFromEnvironment":
            return {
                kind: "tokenFromEnvironment",
                variable: value.variable,
            };
        case "command":
            return {
                kind: "command",
                program: value.program,
                args: value.args.map((item0) => item0),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one RegistryAuthentication from one JSON value. */
export function fromJsonRegistryAuthentication(value: Json): RegistryAuthentication {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "none":
            return {
                kind,
            };
        case "token":
            return {
                kind,
                token: jsonString(jsonField(object, "token")),
            };
        case "tokenFromEnvironment":
            return {
                kind,
                variable: jsonString(jsonField(object, "variable")),
            };
        case "command":
            return {
                kind,
                program: jsonString(jsonField(object, "program")),
                args: jsonArray(jsonField(object, "args")).map((item0) => jsonString(item0)),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** Network settings for package and update commands. */
export type NetworkSettings = {
    /** Whether network access should be disabled by default. */
    readonly offline: boolean;
    /** Optional HTTP proxy URL. */
    readonly proxy?: string;
    /** Request timeout in milliseconds. */
    readonly timeoutMilliseconds?: bigint;
    /** Number of retries for transient network failures. */
    readonly retryCount?: number;
    /** Maximum concurrent network requests. */
    readonly concurrency?: number;
};

export const NetworkSettings = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: NetworkSettings): void {
        encodeNetworkSettings(writer, value);
    },

    /** Decode one NetworkSettings. */
    decode(reader: BinaryReader): NetworkSettings {
        return decodeNetworkSettings(reader);
    },

    /** Return this value as JSON. */
    toJson(value: NetworkSettings): Json {
        return toJsonNetworkSettings(value);
    },

    /** Return one NetworkSettings from one JSON value. */
    fromJson(value: Json): NetworkSettings {
        return fromJsonNetworkSettings(value);
    },
};

/** Encode one NetworkSettings. */
export function encodeNetworkSettings(writer: BinaryWriter, value: NetworkSettings): void {
    writer.writeBool(value.offline);
    writer.writeOption(value.proxy, (value1) => {
        writer.writeString(value1);
    });
    writer.writeOption(value.timeoutMilliseconds, (value2) => {
        writer.writeUnsigned(value2);
    });
    writer.writeOption(value.retryCount, (value3) => {
        writer.writeUnsigned(value3);
    });
    writer.writeOption(value.concurrency, (value4) => {
        writer.writeUnsigned(value4);
    });
}

/** Decode one NetworkSettings. */
export function decodeNetworkSettings(reader: BinaryReader): NetworkSettings {
    const offline = reader.readBool();
    const proxy = reader.readOption(() => reader.readString());
    const timeoutMilliseconds = reader.readOption(() => reader.readUnsigned());
    const retryCount = reader.readOption(() => reader.readNumber());
    const concurrency = reader.readOption(() => reader.readNumber());

    return {
        offline,
        ...(proxy === undefined ? {} : { proxy }),
        ...(timeoutMilliseconds === undefined ? {} : { timeoutMilliseconds }),
        ...(retryCount === undefined ? {} : { retryCount }),
        ...(concurrency === undefined ? {} : { concurrency }),
    };
}

/** Return one JSON value for one NetworkSettings. */
export function toJsonNetworkSettings(value: NetworkSettings): Json {
    return {
        offline: value.offline,
        ...(value.proxy === undefined ? {} : { proxy: value.proxy }),
        ...(value.timeoutMilliseconds === undefined ? {} : { timeoutMilliseconds: value.timeoutMilliseconds.toString() }),
        ...(value.retryCount === undefined ? {} : { retryCount: value.retryCount }),
        ...(value.concurrency === undefined ? {} : { concurrency: value.concurrency }),
    };
}

/** Return one NetworkSettings from one JSON value. */
export function fromJsonNetworkSettings(value: Json): NetworkSettings {
    const object = jsonObject(value);

    return {
        offline: jsonBool(jsonField(object, "offline")),
        proxy: jsonOptional(object, "proxy", (value) => jsonString(value)),
        timeoutMilliseconds: jsonOptional(object, "timeoutMilliseconds", (value) => jsonBigint(value)),
        retryCount: jsonOptional(object, "retryCount", (value) => jsonInteger(value)),
        concurrency: jsonOptional(object, "concurrency", (value) => jsonInteger(value)),
    };
}

/** Machine-local Destack settings. */
export type Settings = {
    /** Package directory settings. */
    readonly packages: PackageSettings;
    /** Workspace cache settings. */
    readonly cache: CacheSettings;
    /** Registry settings keyed by registry name. */
    readonly registries: Readonly<Record<string, RegistrySettings>>;
    /** Default registry name. */
    readonly registry?: string;
    /** Network settings for package and update commands. */
    readonly network: NetworkSettings;
};

export const Settings = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: Settings): void {
        encodeSettings(writer, value);
    },

    /** Decode one Settings. */
    decode(reader: BinaryReader): Settings {
        return decodeSettings(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Settings): Json {
        return toJsonSettings(value);
    },

    /** Return one Settings from one JSON value. */
    fromJson(value: Json): Settings {
        return fromJsonSettings(value);
    },
};

/** Encode one Settings. */
export function encodeSettings(writer: BinaryWriter, value: Settings): void {
    encodePackageSettings(writer, value.packages);
    encodeCacheSettings(writer, value.cache);
    const entries2 = Object.entries(value.registries).map(([key2, item2]) => {
        const keyBytes = nestedBytes((writer) => {
            writer.writeString(key2);
        });
        return { key2, item2, keyBytes };
    });
    entries2.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries2.length);
    for (const entry2 of entries2) {
        writer.writeString(entry2.key2);
        encodeRegistrySettings(writer, entry2.item2);
    }
    writer.writeOption(value.registry, (value3) => {
        writer.writeString(value3);
    });
    encodeNetworkSettings(writer, value.network);
}

/** Decode one Settings. */
export function decodeSettings(reader: BinaryReader): Settings {
    const packages = decodePackageSettings(reader);
    const cache = decodeCacheSettings(reader);
    const registries = (() => { const length2 = reader.readNumber(); const items2: Record<string, RegistrySettings> = {}; for (let index = 0; index < length2; index += 1) { const key = reader.readString(); items2[key] = decodeRegistrySettings(reader); } return items2; })();
    const registry = reader.readOption(() => reader.readString());
    const network = decodeNetworkSettings(reader);

    return {
        packages,
        cache,
        registries,
        ...(registry === undefined ? {} : { registry }),
        network,
    };
}

/** Return one JSON value for one Settings. */
export function toJsonSettings(value: Settings): Json {
    return {
        packages: toJsonPackageSettings(value.packages),
        cache: toJsonCacheSettings(value.cache),
        registries: Object.fromEntries(Object.entries(value.registries).map(([key0, item0]) => [key0, toJsonRegistrySettings(item0)] as const)),
        ...(value.registry === undefined ? {} : { registry: value.registry }),
        network: toJsonNetworkSettings(value.network),
    };
}

/** Return one Settings from one JSON value. */
export function fromJsonSettings(value: Json): Settings {
    const object = jsonObject(value);

    return {
        packages: fromJsonPackageSettings(jsonField(object, "packages")),
        cache: fromJsonCacheSettings(jsonField(object, "cache")),
        registries: Object.fromEntries(Object.entries(jsonObject(jsonField(object, "registries"))).map(([key0, item0]) => [key0, fromJsonRegistrySettings(item0)] as const)),
        registry: jsonOptional(object, "registry", (value) => jsonString(value)),
        network: fromJsonNetworkSettings(jsonField(object, "network")),
    };
}
