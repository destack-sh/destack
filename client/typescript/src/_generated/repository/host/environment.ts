/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, compareBytes, jsonArray, jsonField, jsonObject, jsonOptional, jsonString, nestedBytes } from "../../../protocol/serde.js";

/** Requested source graph and profile selection. */
export type ConditionSelection = {
    /** Requested build target name. */
    readonly target?: string;
    /** Requested product name. */
    readonly product?: string;
    /** Requested profile name. */
    readonly profile?: string;
    /** Requested source graph modes. */
    readonly modes: ReadonlyArray<string>;
    /** Requested source graph roles. */
    readonly roles: ReadonlyArray<string>;
    /** Requested source graph features. */
    readonly features: ReadonlyArray<string>;
    /** Requested source graph tags. */
    readonly tags: ReadonlyArray<string>;
};

export const ConditionSelection = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ConditionSelection): void {
        encodeConditionSelection(writer, value);
    },

    /** Decode one ConditionSelection. */
    decode(reader: BinaryReader): ConditionSelection {
        return decodeConditionSelection(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ConditionSelection): Json {
        return toJsonConditionSelection(value);
    },

    /** Return one ConditionSelection from one JSON value. */
    fromJson(value: Json): ConditionSelection {
        return fromJsonConditionSelection(value);
    },
};

/** Encode one ConditionSelection. */
export function encodeConditionSelection(writer: BinaryWriter, value: ConditionSelection): void {
    writer.writeOption(value.target, (value0) => {
        writer.writeString(value0);
    });
    writer.writeOption(value.product, (value1) => {
        writer.writeString(value1);
    });
    writer.writeOption(value.profile, (value2) => {
        writer.writeString(value2);
    });
    writer.writeUnsigned(value.modes.length);
    for (const item3 of value.modes) {
        writer.writeString(item3);
    }
    writer.writeUnsigned(value.roles.length);
    for (const item4 of value.roles) {
        writer.writeString(item4);
    }
    writer.writeUnsigned(value.features.length);
    for (const item5 of value.features) {
        writer.writeString(item5);
    }
    writer.writeUnsigned(value.tags.length);
    for (const item6 of value.tags) {
        writer.writeString(item6);
    }
}

/** Decode one ConditionSelection. */
export function decodeConditionSelection(reader: BinaryReader): ConditionSelection {
    const target = reader.readOption(() => reader.readString());
    const product = reader.readOption(() => reader.readString());
    const profile = reader.readOption(() => reader.readString());
    const modes = (() => { const length3 = reader.readNumber(); const items3: Array<string> = []; for (let index = 0; index < length3; index += 1) { items3.push(reader.readString()); } return items3; })();
    const roles = (() => { const length4 = reader.readNumber(); const items4: Array<string> = []; for (let index = 0; index < length4; index += 1) { items4.push(reader.readString()); } return items4; })();
    const features = (() => { const length5 = reader.readNumber(); const items5: Array<string> = []; for (let index = 0; index < length5; index += 1) { items5.push(reader.readString()); } return items5; })();
    const tags = (() => { const length6 = reader.readNumber(); const items6: Array<string> = []; for (let index = 0; index < length6; index += 1) { items6.push(reader.readString()); } return items6; })();

    return {
        ...(target === undefined ? {} : { target }),
        ...(product === undefined ? {} : { product }),
        ...(profile === undefined ? {} : { profile }),
        modes,
        roles,
        features,
        tags,
    };
}

/** Return one JSON value for one ConditionSelection. */
export function toJsonConditionSelection(value: ConditionSelection): Json {
    return {
        ...(value.target === undefined ? {} : { target: value.target }),
        ...(value.product === undefined ? {} : { product: value.product }),
        ...(value.profile === undefined ? {} : { profile: value.profile }),
        modes: value.modes.map((item0) => item0),
        roles: value.roles.map((item0) => item0),
        features: value.features.map((item0) => item0),
        tags: value.tags.map((item0) => item0),
    };
}

/** Return one ConditionSelection from one JSON value. */
export function fromJsonConditionSelection(value: Json): ConditionSelection {
    const object = jsonObject(value);

    return {
        target: jsonOptional(object, "target", (value) => jsonString(value)),
        product: jsonOptional(object, "product", (value) => jsonString(value)),
        profile: jsonOptional(object, "profile", (value) => jsonString(value)),
        modes: jsonArray(jsonField(object, "modes")).map((item0) => jsonString(item0)),
        roles: jsonArray(jsonField(object, "roles")).map((item0) => jsonString(item0)),
        features: jsonArray(jsonField(object, "features")).map((item0) => jsonString(item0)),
        tags: jsonArray(jsonField(object, "tags")).map((item0) => jsonString(item0)),
    };
}

/** Virtual ambient state for one session operation. */
export type Environment = {
    /** Working directory visible to this operation. */
    readonly cwd?: string;
    /** Argument vector visible to this operation. */
    readonly args: ReadonlyArray<string>;
    /** Captured environment variables visible to this operation. */
    readonly env: Readonly<Record<string, string>>;
    /** Requested condition selection. */
    readonly selection: ConditionSelection;
};

export const Environment = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: Environment): void {
        encodeEnvironment(writer, value);
    },

    /** Decode one Environment. */
    decode(reader: BinaryReader): Environment {
        return decodeEnvironment(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Environment): Json {
        return toJsonEnvironment(value);
    },

    /** Return one Environment from one JSON value. */
    fromJson(value: Json): Environment {
        return fromJsonEnvironment(value);
    },
};

/** Encode one Environment. */
export function encodeEnvironment(writer: BinaryWriter, value: Environment): void {
    writer.writeOption(value.cwd, (value0) => {
        writer.writeString(value0);
    });
    writer.writeUnsigned(value.args.length);
    for (const item1 of value.args) {
        writer.writeString(item1);
    }
    const entries2 = Object.entries(value.env).map(([key2, item2]) => {
        const keyBytes = nestedBytes((writer) => {
            writer.writeString(key2);
        });
        return { key2, item2, keyBytes };
    });
    entries2.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries2.length);
    for (const entry2 of entries2) {
        writer.writeString(entry2.key2);
        writer.writeString(entry2.item2);
    }
    encodeConditionSelection(writer, value.selection);
}

/** Decode one Environment. */
export function decodeEnvironment(reader: BinaryReader): Environment {
    const cwd = reader.readOption(() => reader.readString());
    const args = (() => { const length1 = reader.readNumber(); const items1: Array<string> = []; for (let index = 0; index < length1; index += 1) { items1.push(reader.readString()); } return items1; })();
    const env = (() => { const length2 = reader.readNumber(); const items2: Record<string, string> = {}; for (let index = 0; index < length2; index += 1) { const key = reader.readString(); items2[key] = reader.readString(); } return items2; })();
    const selection = decodeConditionSelection(reader);

    return {
        ...(cwd === undefined ? {} : { cwd }),
        args,
        env,
        selection,
    };
}

/** Return one JSON value for one Environment. */
export function toJsonEnvironment(value: Environment): Json {
    return {
        ...(value.cwd === undefined ? {} : { cwd: value.cwd }),
        args: value.args.map((item0) => item0),
        env: Object.fromEntries(Object.entries(value.env).map(([key0, item0]) => [key0, item0] as const)),
        selection: toJsonConditionSelection(value.selection),
    };
}

/** Return one Environment from one JSON value. */
export function fromJsonEnvironment(value: Json): Environment {
    const object = jsonObject(value);

    return {
        cwd: jsonOptional(object, "cwd", (value) => jsonString(value)),
        args: jsonArray(jsonField(object, "args")).map((item0) => jsonString(item0)),
        env: Object.fromEntries(Object.entries(jsonObject(jsonField(object, "env"))).map(([key0, item0]) => [key0, jsonString(item0)] as const)),
        selection: fromJsonConditionSelection(jsonField(object, "selection")),
    };
}
