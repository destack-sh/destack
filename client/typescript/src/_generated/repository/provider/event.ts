/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, jsonArray, jsonBigint, jsonBool, jsonField, jsonObject, jsonString } from "../../../protocol/serde.js";

/** One named trace event with ordered fields. */
export type TraceEvent = {
    /** The stable event name. */
    readonly name: string;
    /** The event fields in display order. */
    readonly fields: ReadonlyArray<TraceEventField>;
};

export const TraceEvent = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: TraceEvent): void {
        encodeTraceEvent(writer, value);
    },

    /** Decode one TraceEvent. */
    decode(reader: BinaryReader): TraceEvent {
        return decodeTraceEvent(reader);
    },

    /** Return this value as JSON. */
    toJson(value: TraceEvent): Json {
        return toJsonTraceEvent(value);
    },

    /** Return one TraceEvent from one JSON value. */
    fromJson(value: Json): TraceEvent {
        return fromJsonTraceEvent(value);
    },
};

/** Encode one TraceEvent. */
export function encodeTraceEvent(writer: BinaryWriter, value: TraceEvent): void {
    writer.writeString(value.name);
    writer.writeUnsigned(value.fields.length);
    for (const item1 of value.fields) {
        encodeTraceEventField(writer, item1);
    }
}

/** Decode one TraceEvent. */
export function decodeTraceEvent(reader: BinaryReader): TraceEvent {
    const name = reader.readString();
    const fields = (() => { const length1 = reader.readNumber(); const items1: Array<TraceEventField> = []; for (let index = 0; index < length1; index += 1) { items1.push(decodeTraceEventField(reader)); } return items1; })();

    return {
        name,
        fields,
    };
}

/** Return one JSON value for one TraceEvent. */
export function toJsonTraceEvent(value: TraceEvent): Json {
    return {
        name: value.name,
        fields: value.fields.map((item0) => toJsonTraceEventField(item0)),
    };
}

/** Return one TraceEvent from one JSON value. */
export function fromJsonTraceEvent(value: Json): TraceEvent {
    const object = jsonObject(value);

    return {
        name: jsonString(jsonField(object, "name")),
        fields: jsonArray(jsonField(object, "fields")).map((item0) => fromJsonTraceEventField(item0)),
    };
}

/** One ordered trace event field. */
export type TraceEventField = {
    /** The field key. */
    readonly key: string;
    /** The field value. */
    readonly value: TraceEventValue;
};

export const TraceEventField = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: TraceEventField): void {
        encodeTraceEventField(writer, value);
    },

    /** Decode one TraceEventField. */
    decode(reader: BinaryReader): TraceEventField {
        return decodeTraceEventField(reader);
    },

    /** Return this value as JSON. */
    toJson(value: TraceEventField): Json {
        return toJsonTraceEventField(value);
    },

    /** Return one TraceEventField from one JSON value. */
    fromJson(value: Json): TraceEventField {
        return fromJsonTraceEventField(value);
    },
};

/** Encode one TraceEventField. */
export function encodeTraceEventField(writer: BinaryWriter, value: TraceEventField): void {
    writer.writeString(value.key);
    encodeTraceEventValue(writer, value.value);
}

/** Decode one TraceEventField. */
export function decodeTraceEventField(reader: BinaryReader): TraceEventField {
    const key = reader.readString();
    const value = decodeTraceEventValue(reader);

    return {
        key,
        value,
    };
}

/** Return one JSON value for one TraceEventField. */
export function toJsonTraceEventField(value: TraceEventField): Json {
    return {
        key: value.key,
        value: toJsonTraceEventValue(value.value),
    };
}

/** Return one TraceEventField from one JSON value. */
export function fromJsonTraceEventField(value: Json): TraceEventField {
    const object = jsonObject(value);

    return {
        key: jsonString(jsonField(object, "key")),
        value: fromJsonTraceEventValue(jsonField(object, "value")),
    };
}

/** One trace event field value. */
export type TraceEventValue =
    /** Boolean field value. */
    | {
          readonly kind: "bool";
          readonly bool: boolean;
      }
    /** Integer field value. */
    | {
          readonly kind: "integer";
          readonly integer: bigint;
      }
    /** Text field value. */
    | {
          readonly kind: "text";
          readonly text: string;
      }
;

export const TraceEventValue = {
    /** Boolean field value. */
    bool(bool: boolean): TraceEventValue {
        return { kind: "bool", bool };
    },

    /** Integer field value. */
    integer(integer: bigint): TraceEventValue {
        return { kind: "integer", integer };
    },

    /** Text field value. */
    text(text: string): TraceEventValue {
        return { kind: "text", text };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: TraceEventValue): void {
        encodeTraceEventValue(writer, value);
    },

    /** Decode one TraceEventValue. */
    decode(reader: BinaryReader): TraceEventValue {
        return decodeTraceEventValue(reader);
    },

    /** Return this value as JSON. */
    toJson(value: TraceEventValue): Json {
        return toJsonTraceEventValue(value);
    },

    /** Return one TraceEventValue from one JSON value. */
    fromJson(value: Json): TraceEventValue {
        return fromJsonTraceEventValue(value);
    },
};

/** Encode one TraceEventValue. */
export function encodeTraceEventValue(writer: BinaryWriter, value: TraceEventValue): void {
    switch (value.kind) {
        case "bool":
            writer.writeUnsigned(0);
            writer.writeBool(value.bool);
            return;
        case "integer":
            writer.writeUnsigned(1);
            writer.writeUnsigned(value.integer);
            return;
        case "text":
            writer.writeUnsigned(2);
            writer.writeString(value.text);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one TraceEventValue. */
export function decodeTraceEventValue(reader: BinaryReader): TraceEventValue {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const bool = reader.readBool();

            return { kind: "bool", bool };
        }
        case 1: {
            const integer = reader.readUnsigned();

            return { kind: "integer", integer };
        }
        case 2: {
            const text = reader.readString();

            return { kind: "text", text };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one TraceEventValue. */
export function toJsonTraceEventValue(value: TraceEventValue): Json {
    switch (value.kind) {
        case "bool":
            return {
                kind: "bool",
                bool: value.bool,
            };
        case "integer":
            return {
                kind: "integer",
                integer: value.integer.toString(),
            };
        case "text":
            return {
                kind: "text",
                text: value.text,
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one TraceEventValue from one JSON value. */
export function fromJsonTraceEventValue(value: Json): TraceEventValue {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "bool":
            return {
                kind,
                bool: jsonBool(jsonField(object, "bool")),
            };
        case "integer":
            return {
                kind,
                integer: jsonBigint(jsonField(object, "integer")),
            };
        case "text":
            return {
                kind,
                text: jsonString(jsonField(object, "text")),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}
