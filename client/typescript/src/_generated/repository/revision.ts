/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, bytesFromJson, bytesToJson, jsonField, jsonObject } from "../../protocol/serde.js";

/** Content identity for one repository source and environment state. */
export type Revision = {
    readonly value: Uint8Array | readonly number[];
};

export const Revision = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: Revision): void {
        encodeRevision(writer, value);
    },

    /** Decode one Revision. */
    decode(reader: BinaryReader): Revision {
        return decodeRevision(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Revision): Json {
        return toJsonRevision(value);
    },

    /** Return one Revision from one JSON value. */
    fromJson(value: Json): Revision {
        return fromJsonRevision(value);
    },
};

/** Encode one Revision. */
export function encodeRevision(writer: BinaryWriter, value: Revision): void {
    writer.writeBytes(value.value);
}

/** Decode one Revision. */
export function decodeRevision(reader: BinaryReader): Revision {
    const value = reader.readBytes(32);

    return {
        value,
    };
}

/** Return one JSON value for one Revision. */
export function toJsonRevision(value: Revision): Json {
    return {
        value: bytesToJson(value.value),
    };
}

/** Return one Revision from one JSON value. */
export function fromJsonRevision(value: Json): Revision {
    const object = jsonObject(value);

    return {
        value: bytesFromJson(jsonField(object, "value")),
    };
}
