/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, jsonField, jsonObject } from "../../../../protocol/serde.js";
import type { Blob } from "../../../core/blob.js";
import { decodeBlob, encodeBlob, fromJsonBlob, toJsonBlob } from "../../../core/blob.js";

/** Serialized World image stored as one Blob. */
export type Snapshot = {
    /** Encoded snapshot payload. */
    readonly blob: Blob;
};

export const Snapshot = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: Snapshot): void {
        encodeSnapshot(writer, value);
    },

    /** Decode one Snapshot. */
    decode(reader: BinaryReader): Snapshot {
        return decodeSnapshot(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Snapshot): Json {
        return toJsonSnapshot(value);
    },

    /** Return one Snapshot from one JSON value. */
    fromJson(value: Json): Snapshot {
        return fromJsonSnapshot(value);
    },
};

/** Encode one Snapshot. */
export function encodeSnapshot(writer: BinaryWriter, value: Snapshot): void {
    encodeBlob(writer, value.blob);
}

/** Decode one Snapshot. */
export function decodeSnapshot(reader: BinaryReader): Snapshot {
    const blob = decodeBlob(reader);

    return {
        blob,
    };
}

/** Return one JSON value for one Snapshot. */
export function toJsonSnapshot(value: Snapshot): Json {
    return {
        blob: toJsonBlob(value.blob),
    };
}

/** Return one Snapshot from one JSON value. */
export function fromJsonSnapshot(value: Json): Snapshot {
    const object = jsonObject(value);

    return {
        blob: fromJsonBlob(jsonField(object, "blob")),
    };
}
