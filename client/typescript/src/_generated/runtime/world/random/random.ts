/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, jsonString } from "../../../../protocol/serde.js";

/** Effective runtime randomness source. */
export type RandomSource = "host" | "deterministic";

export const RandomSource = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: RandomSource): void {
        encodeRandomSource(writer, value);
    },

    /** Decode one RandomSource. */
    decode(reader: BinaryReader): RandomSource {
        return decodeRandomSource(reader);
    },

    /** Return this value as JSON. */
    toJson(value: RandomSource): Json {
        return toJsonRandomSource(value);
    },

    /** Return one RandomSource from one JSON value. */
    fromJson(value: Json): RandomSource {
        return fromJsonRandomSource(value);
    },
};

/** Encode one RandomSource. */
export function encodeRandomSource(writer: BinaryWriter, value: RandomSource): void {
    switch (value) {
        case "host":
            writer.writeUnsigned(0);
            return;
        case "deterministic":
            writer.writeUnsigned(1);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one RandomSource. */
export function decodeRandomSource(reader: BinaryReader): RandomSource {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "host";
        case 1:
            return "deterministic";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one RandomSource. */
export function toJsonRandomSource(value: RandomSource): Json {
    return value;
}

/** Return one RandomSource from one JSON value. */
export function fromJsonRandomSource(value: Json): RandomSource {
    const variant = jsonString(value);

    switch (variant) {
        case "host":
            return "host";
        case "deterministic":
            return "deterministic";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}
