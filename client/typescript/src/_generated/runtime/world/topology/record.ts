/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, compareBytes, jsonArray, jsonField, jsonObject, jsonString, nestedBytes } from "../../../../protocol/serde.js";
import type { EdgeId } from "./identity.js";
import type { EdgeKind } from "./identity.js";
import type { EntityId } from "./identity.js";
import type { EntityKind } from "./identity.js";
import { decodeEdgeId, encodeEdgeId, fromJsonEdgeId, toJsonEdgeId } from "./identity.js";
import { decodeEdgeKind, encodeEdgeKind, fromJsonEdgeKind, toJsonEdgeKind } from "./identity.js";
import { decodeEntityId, encodeEntityId, fromJsonEntityId, toJsonEntityId } from "./identity.js";
import { decodeEntityKind, encodeEntityKind, fromJsonEntityKind, toJsonEntityKind } from "./identity.js";

/** Topology edge payload. */
export type Edge = {
    /** Stable edge identifier. */
    readonly id: EdgeId;
    /** Stable edge kind identifier. */
    readonly kind: EdgeKind;
    /** Stable edge name. */
    readonly name: string;
    /** Source entity identifier. */
    readonly from: EntityId;
    /** Destination entity identifier. */
    readonly to: EntityId;
    /** Edge labels. */
    readonly labels: Readonly<Record<string, string>>;
};

export const Edge = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: Edge): void {
        encodeEdge(writer, value);
    },

    /** Decode one Edge. */
    decode(reader: BinaryReader): Edge {
        return decodeEdge(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Edge): Json {
        return toJsonEdge(value);
    },

    /** Return one Edge from one JSON value. */
    fromJson(value: Json): Edge {
        return fromJsonEdge(value);
    },
};

/** Encode one Edge. */
export function encodeEdge(writer: BinaryWriter, value: Edge): void {
    encodeEdgeId(writer, value.id);
    encodeEdgeKind(writer, value.kind);
    writer.writeString(value.name);
    encodeEntityId(writer, value.from);
    encodeEntityId(writer, value.to);
    const entries5 = Object.entries(value.labels).map(([key5, item5]) => {
        const keyBytes = nestedBytes((writer) => {
            writer.writeString(key5);
        });
        return { key5, item5, keyBytes };
    });
    entries5.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries5.length);
    for (const entry5 of entries5) {
        writer.writeString(entry5.key5);
        writer.writeString(entry5.item5);
    }
}

/** Decode one Edge. */
export function decodeEdge(reader: BinaryReader): Edge {
    const id = decodeEdgeId(reader);
    const kind = decodeEdgeKind(reader);
    const name = reader.readString();
    const from_ = decodeEntityId(reader);
    const to = decodeEntityId(reader);
    const labels = (() => { const length5 = reader.readNumber(); const items5: Record<string, string> = {}; for (let index = 0; index < length5; index += 1) { const key = reader.readString(); items5[key] = reader.readString(); } return items5; })();

    return {
        id,
        kind,
        name,
        from: from_,
        to,
        labels,
    };
}

/** Return one JSON value for one Edge. */
export function toJsonEdge(value: Edge): Json {
    return {
        id: toJsonEdgeId(value.id),
        kind: toJsonEdgeKind(value.kind),
        name: value.name,
        from: toJsonEntityId(value.from),
        to: toJsonEntityId(value.to),
        labels: Object.fromEntries(Object.entries(value.labels).map(([key0, item0]) => [key0, item0] as const)),
    };
}

/** Return one Edge from one JSON value. */
export function fromJsonEdge(value: Json): Edge {
    const object = jsonObject(value);

    return {
        id: fromJsonEdgeId(jsonField(object, "id")),
        kind: fromJsonEdgeKind(jsonField(object, "kind")),
        name: jsonString(jsonField(object, "name")),
        from: fromJsonEntityId(jsonField(object, "from")),
        to: fromJsonEntityId(jsonField(object, "to")),
        labels: Object.fromEntries(Object.entries(jsonObject(jsonField(object, "labels"))).map(([key0, item0]) => [key0, jsonString(item0)] as const)),
    };
}

/** Topology edge-kind registration payload. */
export type EdgeDefinition = {
    /** Stable kind identifier. */
    readonly kind: EdgeKind;
    /** Kind labels for selector matching. */
    readonly labels: Readonly<Record<string, string>>;
};

export const EdgeDefinition = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: EdgeDefinition): void {
        encodeEdgeDefinition(writer, value);
    },

    /** Decode one EdgeDefinition. */
    decode(reader: BinaryReader): EdgeDefinition {
        return decodeEdgeDefinition(reader);
    },

    /** Return this value as JSON. */
    toJson(value: EdgeDefinition): Json {
        return toJsonEdgeDefinition(value);
    },

    /** Return one EdgeDefinition from one JSON value. */
    fromJson(value: Json): EdgeDefinition {
        return fromJsonEdgeDefinition(value);
    },
};

/** Encode one EdgeDefinition. */
export function encodeEdgeDefinition(writer: BinaryWriter, value: EdgeDefinition): void {
    encodeEdgeKind(writer, value.kind);
    const entries1 = Object.entries(value.labels).map(([key1, item1]) => {
        const keyBytes = nestedBytes((writer) => {
            writer.writeString(key1);
        });
        return { key1, item1, keyBytes };
    });
    entries1.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries1.length);
    for (const entry1 of entries1) {
        writer.writeString(entry1.key1);
        writer.writeString(entry1.item1);
    }
}

/** Decode one EdgeDefinition. */
export function decodeEdgeDefinition(reader: BinaryReader): EdgeDefinition {
    const kind = decodeEdgeKind(reader);
    const labels = (() => { const length1 = reader.readNumber(); const items1: Record<string, string> = {}; for (let index = 0; index < length1; index += 1) { const key = reader.readString(); items1[key] = reader.readString(); } return items1; })();

    return {
        kind,
        labels,
    };
}

/** Return one JSON value for one EdgeDefinition. */
export function toJsonEdgeDefinition(value: EdgeDefinition): Json {
    return {
        kind: toJsonEdgeKind(value.kind),
        labels: Object.fromEntries(Object.entries(value.labels).map(([key0, item0]) => [key0, item0] as const)),
    };
}

/** Return one EdgeDefinition from one JSON value. */
export function fromJsonEdgeDefinition(value: Json): EdgeDefinition {
    const object = jsonObject(value);

    return {
        kind: fromJsonEdgeKind(jsonField(object, "kind")),
        labels: Object.fromEntries(Object.entries(jsonObject(jsonField(object, "labels"))).map(([key0, item0]) => [key0, jsonString(item0)] as const)),
    };
}

/** Topology entity payload. */
export type Entity = {
    /** Stable entity identifier. */
    readonly id: EntityId;
    /** Stable entity kind identifier. */
    readonly kind: EntityKind;
    /** Stable entity name. */
    readonly name: string;
    /** Entity labels. */
    readonly labels: Readonly<Record<string, string>>;
};

export const Entity = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: Entity): void {
        encodeEntity(writer, value);
    },

    /** Decode one Entity. */
    decode(reader: BinaryReader): Entity {
        return decodeEntity(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Entity): Json {
        return toJsonEntity(value);
    },

    /** Return one Entity from one JSON value. */
    fromJson(value: Json): Entity {
        return fromJsonEntity(value);
    },
};

/** Encode one Entity. */
export function encodeEntity(writer: BinaryWriter, value: Entity): void {
    encodeEntityId(writer, value.id);
    encodeEntityKind(writer, value.kind);
    writer.writeString(value.name);
    const entries3 = Object.entries(value.labels).map(([key3, item3]) => {
        const keyBytes = nestedBytes((writer) => {
            writer.writeString(key3);
        });
        return { key3, item3, keyBytes };
    });
    entries3.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries3.length);
    for (const entry3 of entries3) {
        writer.writeString(entry3.key3);
        writer.writeString(entry3.item3);
    }
}

/** Decode one Entity. */
export function decodeEntity(reader: BinaryReader): Entity {
    const id = decodeEntityId(reader);
    const kind = decodeEntityKind(reader);
    const name = reader.readString();
    const labels = (() => { const length3 = reader.readNumber(); const items3: Record<string, string> = {}; for (let index = 0; index < length3; index += 1) { const key = reader.readString(); items3[key] = reader.readString(); } return items3; })();

    return {
        id,
        kind,
        name,
        labels,
    };
}

/** Return one JSON value for one Entity. */
export function toJsonEntity(value: Entity): Json {
    return {
        id: toJsonEntityId(value.id),
        kind: toJsonEntityKind(value.kind),
        name: value.name,
        labels: Object.fromEntries(Object.entries(value.labels).map(([key0, item0]) => [key0, item0] as const)),
    };
}

/** Return one Entity from one JSON value. */
export function fromJsonEntity(value: Json): Entity {
    const object = jsonObject(value);

    return {
        id: fromJsonEntityId(jsonField(object, "id")),
        kind: fromJsonEntityKind(jsonField(object, "kind")),
        name: jsonString(jsonField(object, "name")),
        labels: Object.fromEntries(Object.entries(jsonObject(jsonField(object, "labels"))).map(([key0, item0]) => [key0, jsonString(item0)] as const)),
    };
}

/** Topology entity-kind registration payload. */
export type EntityDefinition = {
    /** Stable kind identifier. */
    readonly kind: EntityKind;
    /** Kind labels for selector matching. */
    readonly labels: Readonly<Record<string, string>>;
};

export const EntityDefinition = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: EntityDefinition): void {
        encodeEntityDefinition(writer, value);
    },

    /** Decode one EntityDefinition. */
    decode(reader: BinaryReader): EntityDefinition {
        return decodeEntityDefinition(reader);
    },

    /** Return this value as JSON. */
    toJson(value: EntityDefinition): Json {
        return toJsonEntityDefinition(value);
    },

    /** Return one EntityDefinition from one JSON value. */
    fromJson(value: Json): EntityDefinition {
        return fromJsonEntityDefinition(value);
    },
};

/** Encode one EntityDefinition. */
export function encodeEntityDefinition(writer: BinaryWriter, value: EntityDefinition): void {
    encodeEntityKind(writer, value.kind);
    const entries1 = Object.entries(value.labels).map(([key1, item1]) => {
        const keyBytes = nestedBytes((writer) => {
            writer.writeString(key1);
        });
        return { key1, item1, keyBytes };
    });
    entries1.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries1.length);
    for (const entry1 of entries1) {
        writer.writeString(entry1.key1);
        writer.writeString(entry1.item1);
    }
}

/** Decode one EntityDefinition. */
export function decodeEntityDefinition(reader: BinaryReader): EntityDefinition {
    const kind = decodeEntityKind(reader);
    const labels = (() => { const length1 = reader.readNumber(); const items1: Record<string, string> = {}; for (let index = 0; index < length1; index += 1) { const key = reader.readString(); items1[key] = reader.readString(); } return items1; })();

    return {
        kind,
        labels,
    };
}

/** Return one JSON value for one EntityDefinition. */
export function toJsonEntityDefinition(value: EntityDefinition): Json {
    return {
        kind: toJsonEntityKind(value.kind),
        labels: Object.fromEntries(Object.entries(value.labels).map(([key0, item0]) => [key0, item0] as const)),
    };
}

/** Return one EntityDefinition from one JSON value. */
export function fromJsonEntityDefinition(value: Json): EntityDefinition {
    const object = jsonObject(value);

    return {
        kind: fromJsonEntityKind(jsonField(object, "kind")),
        labels: Object.fromEntries(Object.entries(jsonObject(jsonField(object, "labels"))).map(([key0, item0]) => [key0, jsonString(item0)] as const)),
    };
}
