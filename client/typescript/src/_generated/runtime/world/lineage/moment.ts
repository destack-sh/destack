/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, jsonBigint, jsonField, jsonObject } from "../../../../protocol/serde.js";
import type { BranchId } from "./branch.js";
import { decodeBranchId, encodeBranchId, fromJsonBranchId, toJsonBranchId } from "./branch.js";

/** Precise lineage coordinate over one branch and one moment sequence. */
export type Moment = {
    /** The branch that owns this lineage coordinate. */
    readonly branchId: BranchId;
    /** The moment sequence reached at this coordinate. */
    readonly sequence: MomentSequence;
};

export const Moment = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: Moment): void {
        encodeMoment(writer, value);
    },

    /** Decode one Moment. */
    decode(reader: BinaryReader): Moment {
        return decodeMoment(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Moment): Json {
        return toJsonMoment(value);
    },

    /** Return one Moment from one JSON value. */
    fromJson(value: Json): Moment {
        return fromJsonMoment(value);
    },
};

/** Encode one Moment. */
export function encodeMoment(writer: BinaryWriter, value: Moment): void {
    encodeBranchId(writer, value.branchId);
    encodeMomentSequence(writer, value.sequence);
}

/** Decode one Moment. */
export function decodeMoment(reader: BinaryReader): Moment {
    const branchId = decodeBranchId(reader);
    const sequence = decodeMomentSequence(reader);

    return {
        branchId,
        sequence,
    };
}

/** Return one JSON value for one Moment. */
export function toJsonMoment(value: Moment): Json {
    return {
        branchId: toJsonBranchId(value.branchId),
        sequence: toJsonMomentSequence(value.sequence),
    };
}

/** Return one Moment from one JSON value. */
export function fromJsonMoment(value: Json): Moment {
    const object = jsonObject(value);

    return {
        branchId: fromJsonBranchId(jsonField(object, "branchId")),
        sequence: fromJsonMomentSequence(jsonField(object, "sequence")),
    };
}

/** Sequence number for moments within one branch. */
export type MomentSequence = bigint;

export const MomentSequence = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: MomentSequence): void {
        encodeMomentSequence(writer, value);
    },

    /** Decode one MomentSequence. */
    decode(reader: BinaryReader): MomentSequence {
        return decodeMomentSequence(reader);
    },

    /** Return this value as JSON. */
    toJson(value: MomentSequence): Json {
        return toJsonMomentSequence(value);
    },

    /** Return one MomentSequence from one JSON value. */
    fromJson(value: Json): MomentSequence {
        return fromJsonMomentSequence(value);
    },
};

/** Encode one MomentSequence. */
export function encodeMomentSequence(writer: BinaryWriter, value: MomentSequence): void {
    writer.writeUnsigned(value);
}

/** Decode one MomentSequence. */
export function decodeMomentSequence(reader: BinaryReader): MomentSequence {
    return reader.readUnsigned();
}

/** Return one JSON value for one MomentSequence. */
export function toJsonMomentSequence(value: MomentSequence): Json {
    return value.toString();
}

/** Return one MomentSequence from one JSON value. */
export function fromJsonMomentSequence(value: Json): MomentSequence {
    return jsonBigint(value);
}
