/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, compareBytes, jsonArray, jsonBigint, jsonField, jsonObject, jsonOptional, jsonString, nestedBytes } from "../../../../protocol/serde.js";
import type { Moment } from "./moment.js";
import { decodeMoment, encodeMoment, fromJsonMoment, toJsonMoment } from "./moment.js";

/** One retained World image. */
export type Image = {
    /** The retained image identifier. */
    readonly id: ImageId;
    /** The committed Moment captured by this image. */
    readonly moment: Moment;
    /** The image name, when explicitly named. */
    readonly name?: string;
    /** The image labels. */
    readonly labels: Readonly<Record<string, string>>;
};

export const Image = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: Image): void {
        encodeImage(writer, value);
    },

    /** Decode one Image. */
    decode(reader: BinaryReader): Image {
        return decodeImage(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Image): Json {
        return toJsonImage(value);
    },

    /** Return one Image from one JSON value. */
    fromJson(value: Json): Image {
        return fromJsonImage(value);
    },
};

/** Encode one Image. */
export function encodeImage(writer: BinaryWriter, value: Image): void {
    encodeImageId(writer, value.id);
    encodeMoment(writer, value.moment);
    writer.writeOption(value.name, (value2) => {
        writer.writeString(value2);
    });
    const entries3 = Object.entries(value.labels).map(([key3, item3]) => {
        const keyBytes = nestedBytes((writer) => {
            writer.writeString(key3);
        });
        return { key3, item3, keyBytes };
    });
    entries3.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries3.length);
    for (const entry3 of entries3) {
        writer.writeString(entry3.key3);
        writer.writeString(entry3.item3);
    }
}

/** Decode one Image. */
export function decodeImage(reader: BinaryReader): Image {
    const id = decodeImageId(reader);
    const moment = decodeMoment(reader);
    const name = reader.readOption(() => reader.readString());
    const labels = (() => { const length3 = reader.readNumber(); const items3: Record<string, string> = {}; for (let index = 0; index < length3; index += 1) { const key = reader.readString(); items3[key] = reader.readString(); } return items3; })();

    return {
        id,
        moment,
        ...(name === undefined ? {} : { name }),
        labels,
    };
}

/** Return one JSON value for one Image. */
export function toJsonImage(value: Image): Json {
    return {
        id: toJsonImageId(value.id),
        moment: toJsonMoment(value.moment),
        ...(value.name === undefined ? {} : { name: value.name }),
        labels: Object.fromEntries(Object.entries(value.labels).map(([key0, item0]) => [key0, item0] as const)),
    };
}

/** Return one Image from one JSON value. */
export function fromJsonImage(value: Json): Image {
    const object = jsonObject(value);

    return {
        id: fromJsonImageId(jsonField(object, "id")),
        moment: fromJsonMoment(jsonField(object, "moment")),
        name: jsonOptional(object, "name", (value) => jsonString(value)),
        labels: Object.fromEntries(Object.entries(jsonObject(jsonField(object, "labels"))).map(([key0, item0]) => [key0, jsonString(item0)] as const)),
    };
}

/** Identifier for one revision state image. */
export type ImageId = bigint;

export const ImageId = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ImageId): void {
        encodeImageId(writer, value);
    },

    /** Decode one ImageId. */
    decode(reader: BinaryReader): ImageId {
        return decodeImageId(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ImageId): Json {
        return toJsonImageId(value);
    },

    /** Return one ImageId from one JSON value. */
    fromJson(value: Json): ImageId {
        return fromJsonImageId(value);
    },
};

/** Encode one ImageId. */
export function encodeImageId(writer: BinaryWriter, value: ImageId): void {
    writer.writeUnsigned(value);
}

/** Decode one ImageId. */
export function decodeImageId(reader: BinaryReader): ImageId {
    return reader.readUnsigned();
}

/** Return one JSON value for one ImageId. */
export function toJsonImageId(value: ImageId): Json {
    return value.toString();
}

/** Return one ImageId from one JSON value. */
export function fromJsonImageId(value: Json): ImageId {
    return jsonBigint(value);
}
