/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, compareBytes, jsonArray, jsonBigint, jsonField, jsonObject, jsonString, nestedBytes } from "../../../../protocol/serde.js";
import type { Moment } from "./moment.js";
import type { RevisionId } from "./revision.js";
import { decodeMoment, encodeMoment, fromJsonMoment, toJsonMoment } from "./moment.js";
import { decodeRevisionId, encodeRevisionId, fromJsonRevisionId, toJsonRevisionId } from "./revision.js";

/** Branch identifier for one world lineage. */
export type BranchId = bigint;

export const BranchId = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: BranchId): void {
        encodeBranchId(writer, value);
    },

    /** Decode one BranchId. */
    decode(reader: BinaryReader): BranchId {
        return decodeBranchId(reader);
    },

    /** Return this value as JSON. */
    toJson(value: BranchId): Json {
        return toJsonBranchId(value);
    },

    /** Return one BranchId from one JSON value. */
    fromJson(value: Json): BranchId {
        return fromJsonBranchId(value);
    },
};

/** Encode one BranchId. */
export function encodeBranchId(writer: BinaryWriter, value: BranchId): void {
    writer.writeUnsigned(value);
}

/** Decode one BranchId. */
export function decodeBranchId(reader: BinaryReader): BranchId {
    return reader.readUnsigned();
}

/** Return one JSON value for one BranchId. */
export function toJsonBranchId(value: BranchId): Json {
    return value.toString();
}

/** Return one BranchId from one JSON value. */
export function fromJsonBranchId(value: Json): BranchId {
    return jsonBigint(value);
}

/** Branch metadata for one world lineage. */
export type Branch = {
    /** The branch identifier. */
    readonly id: BranchId;
    /** The current head revision for this branch. */
    readonly headRevisionId: RevisionId;
    /** The branch origin in the world lineage. */
    readonly origin: BranchOrigin;
    /** The branch name. */
    readonly name: string;
    /** The branch labels. */
    readonly labels: Readonly<Record<string, string>>;
};

export const Branch = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: Branch): void {
        encodeBranch(writer, value);
    },

    /** Decode one Branch. */
    decode(reader: BinaryReader): Branch {
        return decodeBranch(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Branch): Json {
        return toJsonBranch(value);
    },

    /** Return one Branch from one JSON value. */
    fromJson(value: Json): Branch {
        return fromJsonBranch(value);
    },
};

/** Encode one Branch. */
export function encodeBranch(writer: BinaryWriter, value: Branch): void {
    encodeBranchId(writer, value.id);
    encodeRevisionId(writer, value.headRevisionId);
    encodeBranchOrigin(writer, value.origin);
    writer.writeString(value.name);
    const entries4 = Object.entries(value.labels).map(([key4, item4]) => {
        const keyBytes = nestedBytes((writer) => {
            writer.writeString(key4);
        });
        return { key4, item4, keyBytes };
    });
    entries4.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries4.length);
    for (const entry4 of entries4) {
        writer.writeString(entry4.key4);
        writer.writeString(entry4.item4);
    }
}

/** Decode one Branch. */
export function decodeBranch(reader: BinaryReader): Branch {
    const id = decodeBranchId(reader);
    const headRevisionId = decodeRevisionId(reader);
    const origin = decodeBranchOrigin(reader);
    const name = reader.readString();
    const labels = (() => { const length4 = reader.readNumber(); const items4: Record<string, string> = {}; for (let index = 0; index < length4; index += 1) { const key = reader.readString(); items4[key] = reader.readString(); } return items4; })();

    return {
        id,
        headRevisionId,
        origin,
        name,
        labels,
    };
}

/** Return one JSON value for one Branch. */
export function toJsonBranch(value: Branch): Json {
    return {
        id: toJsonBranchId(value.id),
        headRevisionId: toJsonRevisionId(value.headRevisionId),
        origin: toJsonBranchOrigin(value.origin),
        name: value.name,
        labels: Object.fromEntries(Object.entries(value.labels).map(([key0, item0]) => [key0, item0] as const)),
    };
}

/** Return one Branch from one JSON value. */
export function fromJsonBranch(value: Json): Branch {
    const object = jsonObject(value);

    return {
        id: fromJsonBranchId(jsonField(object, "id")),
        headRevisionId: fromJsonRevisionId(jsonField(object, "headRevisionId")),
        origin: fromJsonBranchOrigin(jsonField(object, "origin")),
        name: jsonString(jsonField(object, "name")),
        labels: Object.fromEntries(Object.entries(jsonObject(jsonField(object, "labels"))).map(([key0, item0]) => [key0, jsonString(item0)] as const)),
    };
}

/** Branch origin in one world lineage. */
export type BranchOrigin =
    /** The root branch for one new world lineage. */
    | {
          readonly kind: "root";
      }
    /** One child branch forked from one parent moment. */
    | {
          readonly kind: "fork";
          /** The moment where this branch forked. */
          readonly parent: Moment;
      }
;

export const BranchOrigin = {
    /** The root branch for one new world lineage. */
    root(): BranchOrigin {
        return { kind: "root" };
    },

    /** One child branch forked from one parent moment. */
    fork(parent: Moment): BranchOrigin {
        return { kind: "fork", parent };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: BranchOrigin): void {
        encodeBranchOrigin(writer, value);
    },

    /** Decode one BranchOrigin. */
    decode(reader: BinaryReader): BranchOrigin {
        return decodeBranchOrigin(reader);
    },

    /** Return this value as JSON. */
    toJson(value: BranchOrigin): Json {
        return toJsonBranchOrigin(value);
    },

    /** Return one BranchOrigin from one JSON value. */
    fromJson(value: Json): BranchOrigin {
        return fromJsonBranchOrigin(value);
    },
};

/** Encode one BranchOrigin. */
export function encodeBranchOrigin(writer: BinaryWriter, value: BranchOrigin): void {
    switch (value.kind) {
        case "root":
            writer.writeUnsigned(0);
            return;
        case "fork":
            writer.writeUnsigned(1);
            encodeMoment(writer, value.parent);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one BranchOrigin. */
export function decodeBranchOrigin(reader: BinaryReader): BranchOrigin {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            return { kind: "root" };
        }
        case 1: {
            const parent = decodeMoment(reader);

            return {
                kind: "fork",
                parent,
            };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one BranchOrigin. */
export function toJsonBranchOrigin(value: BranchOrigin): Json {
    switch (value.kind) {
        case "root":
            return {
                kind: "root",
            };
        case "fork":
            return {
                kind: "fork",
                parent: toJsonMoment(value.parent),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one BranchOrigin from one JSON value. */
export function fromJsonBranchOrigin(value: Json): BranchOrigin {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "root":
            return {
                kind,
            };
        case "fork":
            return {
                kind,
                parent: fromJsonMoment(jsonField(object, "parent")),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}
