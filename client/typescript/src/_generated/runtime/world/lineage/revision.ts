/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, jsonBigint } from "../../../../protocol/serde.js";

/** Revision identifier for one world lineage. */
export type RevisionId = bigint;

export const RevisionId = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: RevisionId): void {
        encodeRevisionId(writer, value);
    },

    /** Decode one RevisionId. */
    decode(reader: BinaryReader): RevisionId {
        return decodeRevisionId(reader);
    },

    /** Return this value as JSON. */
    toJson(value: RevisionId): Json {
        return toJsonRevisionId(value);
    },

    /** Return one RevisionId from one JSON value. */
    fromJson(value: Json): RevisionId {
        return fromJsonRevisionId(value);
    },
};

/** Encode one RevisionId. */
export function encodeRevisionId(writer: BinaryWriter, value: RevisionId): void {
    writer.writeUnsigned(value);
}

/** Decode one RevisionId. */
export function decodeRevisionId(reader: BinaryReader): RevisionId {
    return reader.readUnsigned();
}

/** Return one JSON value for one RevisionId. */
export function toJsonRevisionId(value: RevisionId): Json {
    return value.toString();
}

/** Return one RevisionId from one JSON value. */
export function fromJsonRevisionId(value: Json): RevisionId {
    return jsonBigint(value);
}
