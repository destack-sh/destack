/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, jsonBigint } from "../../../../protocol/serde.js";

/** Nanosecond duration on the shared World timeline. */
export type Nanos = bigint;

export const Nanos = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: Nanos): void {
        encodeNanos(writer, value);
    },

    /** Decode one Nanos. */
    decode(reader: BinaryReader): Nanos {
        return decodeNanos(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Nanos): Json {
        return toJsonNanos(value);
    },

    /** Return one Nanos from one JSON value. */
    fromJson(value: Json): Nanos {
        return fromJsonNanos(value);
    },
};

/** Encode one Nanos. */
export function encodeNanos(writer: BinaryWriter, value: Nanos): void {
    writer.writeUnsigned(value);
}

/** Decode one Nanos. */
export function decodeNanos(reader: BinaryReader): Nanos {
    return reader.readUnsigned();
}

/** Return one JSON value for one Nanos. */
export function toJsonNanos(value: Nanos): Json {
    return value.toString();
}

/** Return one Nanos from one JSON value. */
export function fromJsonNanos(value: Json): Nanos {
    return jsonBigint(value);
}
