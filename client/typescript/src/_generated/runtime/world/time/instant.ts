/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json } from "../../../../protocol/serde.js";
import type { Nanos } from "./nanos.js";
import { decodeNanos, encodeNanos, fromJsonNanos, toJsonNanos } from "./nanos.js";

/** Absolute instant on the shared world timeline. */
export type Instant = Nanos;

export const Instant = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: Instant): void {
        encodeInstant(writer, value);
    },

    /** Decode one Instant. */
    decode(reader: BinaryReader): Instant {
        return decodeInstant(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Instant): Json {
        return toJsonInstant(value);
    },

    /** Return one Instant from one JSON value. */
    fromJson(value: Json): Instant {
        return fromJsonInstant(value);
    },
};

/** Encode one Instant. */
export function encodeInstant(writer: BinaryWriter, value: Instant): void {
    encodeNanos(writer, value);
}

/** Decode one Instant. */
export function decodeInstant(reader: BinaryReader): Instant {
    return decodeNanos(reader);
}

/** Return one JSON value for one Instant. */
export function toJsonInstant(value: Instant): Json {
    return toJsonNanos(value);
}

/** Return one Instant from one JSON value. */
export function fromJsonInstant(value: Json): Instant {
    return fromJsonNanos(value);
}
