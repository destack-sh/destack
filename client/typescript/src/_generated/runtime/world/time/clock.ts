/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, jsonString } from "../../../../protocol/serde.js";

/** Effective runtime clock source. */
export type ClockSource = "host" | "runtime";

export const ClockSource = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ClockSource): void {
        encodeClockSource(writer, value);
    },

    /** Decode one ClockSource. */
    decode(reader: BinaryReader): ClockSource {
        return decodeClockSource(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ClockSource): Json {
        return toJsonClockSource(value);
    },

    /** Return one ClockSource from one JSON value. */
    fromJson(value: Json): ClockSource {
        return fromJsonClockSource(value);
    },
};

/** Encode one ClockSource. */
export function encodeClockSource(writer: BinaryWriter, value: ClockSource): void {
    switch (value) {
        case "host":
            writer.writeUnsigned(0);
            return;
        case "runtime":
            writer.writeUnsigned(1);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one ClockSource. */
export function decodeClockSource(reader: BinaryReader): ClockSource {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "host";
        case 1:
            return "runtime";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one ClockSource. */
export function toJsonClockSource(value: ClockSource): Json {
    return value;
}

/** Return one ClockSource from one JSON value. */
export function fromJsonClockSource(value: Json): ClockSource {
    const variant = jsonString(value);

    switch (variant) {
        case "host":
            return "host";
        case "runtime":
            return "runtime";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}
