/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, jsonBigint } from "../../../protocol/serde.js";

/** Stable identifier for one world-managed worker. */
export type WorkerId = bigint;

export const WorkerId = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: WorkerId): void {
        encodeWorkerId(writer, value);
    },

    /** Decode one WorkerId. */
    decode(reader: BinaryReader): WorkerId {
        return decodeWorkerId(reader);
    },

    /** Return this value as JSON. */
    toJson(value: WorkerId): Json {
        return toJsonWorkerId(value);
    },

    /** Return one WorkerId from one JSON value. */
    fromJson(value: Json): WorkerId {
        return fromJsonWorkerId(value);
    },
};

/** Encode one WorkerId. */
export function encodeWorkerId(writer: BinaryWriter, value: WorkerId): void {
    writer.writeUnsigned(value);
}

/** Decode one WorkerId. */
export function decodeWorkerId(reader: BinaryReader): WorkerId {
    return reader.readUnsigned();
}

/** Return one JSON value for one WorkerId. */
export function toJsonWorkerId(value: WorkerId): Json {
    return value.toString();
}

/** Return one WorkerId from one JSON value. */
export function fromJsonWorkerId(value: Json): WorkerId {
    return jsonBigint(value);
}
