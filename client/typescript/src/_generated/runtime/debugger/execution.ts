/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, jsonField, jsonObject, jsonString } from "../../../protocol/serde.js";
import type { FiberId } from "../../program/fiber.js";
import type { RuntimeId } from "../runtime.js";
import type { WorkerId } from "../worker/worker.js";
import { decodeFiberId, encodeFiberId, fromJsonFiberId, toJsonFiberId } from "../../program/fiber.js";
import { decodeRuntimeId, encodeRuntimeId, fromJsonRuntimeId, toJsonRuntimeId } from "../runtime.js";
import { decodeWorkerId, encodeWorkerId, fromJsonWorkerId, toJsonWorkerId } from "../worker/worker.js";

/** World-local execution selected for debugger control. */
export type Execution =
    /** Every Runtime in the World. */
    | {
          readonly kind: "world";
      }
    /** One Runtime. */
    | {
          readonly kind: "runtime";
          /** Selected Runtime. */
          readonly runtimeId: RuntimeId;
      }
    /** One Worker. */
    | {
          readonly kind: "worker";
          /** Selected Runtime. */
          readonly runtimeId: RuntimeId;
          /** Selected Worker. */
          readonly workerId: WorkerId;
      }
    /** One Fiber. */
    | {
          readonly kind: "fiber";
          /** Selected Runtime. */
          readonly runtimeId: RuntimeId;
          /** Selected Worker. */
          readonly workerId: WorkerId;
          /** Selected Fiber. */
          readonly fiberId: FiberId;
      }
;

export const Execution = {
    /** Every Runtime in the World. */
    world(): Execution {
        return { kind: "world" };
    },

    /** One Runtime. */
    runtime(runtimeId: RuntimeId): Execution {
        return { kind: "runtime", runtimeId };
    },

    /** One Worker. */
    worker(runtimeId: RuntimeId, workerId: WorkerId): Execution {
        return { kind: "worker", runtimeId, workerId };
    },

    /** One Fiber. */
    fiber(runtimeId: RuntimeId, workerId: WorkerId, fiberId: FiberId): Execution {
        return { kind: "fiber", runtimeId, workerId, fiberId };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: Execution): void {
        encodeExecution(writer, value);
    },

    /** Decode one Execution. */
    decode(reader: BinaryReader): Execution {
        return decodeExecution(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Execution): Json {
        return toJsonExecution(value);
    },

    /** Return one Execution from one JSON value. */
    fromJson(value: Json): Execution {
        return fromJsonExecution(value);
    },
};

/** Encode one Execution. */
export function encodeExecution(writer: BinaryWriter, value: Execution): void {
    switch (value.kind) {
        case "world":
            writer.writeUnsigned(0);
            return;
        case "runtime":
            writer.writeUnsigned(1);
            encodeRuntimeId(writer, value.runtimeId);
            return;
        case "worker":
            writer.writeUnsigned(2);
            encodeRuntimeId(writer, value.runtimeId);
            encodeWorkerId(writer, value.workerId);
            return;
        case "fiber":
            writer.writeUnsigned(3);
            encodeRuntimeId(writer, value.runtimeId);
            encodeWorkerId(writer, value.workerId);
            encodeFiberId(writer, value.fiberId);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one Execution. */
export function decodeExecution(reader: BinaryReader): Execution {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            return { kind: "world" };
        }
        case 1: {
            const runtimeId = decodeRuntimeId(reader);

            return {
                kind: "runtime",
                runtimeId,
            };
        }
        case 2: {
            const runtimeId = decodeRuntimeId(reader);
            const workerId = decodeWorkerId(reader);

            return {
                kind: "worker",
                runtimeId,
                workerId,
            };
        }
        case 3: {
            const runtimeId = decodeRuntimeId(reader);
            const workerId = decodeWorkerId(reader);
            const fiberId = decodeFiberId(reader);

            return {
                kind: "fiber",
                runtimeId,
                workerId,
                fiberId,
            };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one Execution. */
export function toJsonExecution(value: Execution): Json {
    switch (value.kind) {
        case "world":
            return {
                kind: "world",
            };
        case "runtime":
            return {
                kind: "runtime",
                runtimeId: toJsonRuntimeId(value.runtimeId),
            };
        case "worker":
            return {
                kind: "worker",
                runtimeId: toJsonRuntimeId(value.runtimeId),
                workerId: toJsonWorkerId(value.workerId),
            };
        case "fiber":
            return {
                kind: "fiber",
                runtimeId: toJsonRuntimeId(value.runtimeId),
                workerId: toJsonWorkerId(value.workerId),
                fiberId: toJsonFiberId(value.fiberId),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one Execution from one JSON value. */
export function fromJsonExecution(value: Json): Execution {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "world":
            return {
                kind,
            };
        case "runtime":
            return {
                kind,
                runtimeId: fromJsonRuntimeId(jsonField(object, "runtimeId")),
            };
        case "worker":
            return {
                kind,
                runtimeId: fromJsonRuntimeId(jsonField(object, "runtimeId")),
                workerId: fromJsonWorkerId(jsonField(object, "workerId")),
            };
        case "fiber":
            return {
                kind,
                runtimeId: fromJsonRuntimeId(jsonField(object, "runtimeId")),
                workerId: fromJsonWorkerId(jsonField(object, "workerId")),
                fiberId: fromJsonFiberId(jsonField(object, "fiberId")),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** Debugger step granularity. */
export type Step = "instruction" | "into" | "over" | "out";

export const Step = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: Step): void {
        encodeStep(writer, value);
    },

    /** Decode one Step. */
    decode(reader: BinaryReader): Step {
        return decodeStep(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Step): Json {
        return toJsonStep(value);
    },

    /** Return one Step from one JSON value. */
    fromJson(value: Json): Step {
        return fromJsonStep(value);
    },
};

/** Encode one Step. */
export function encodeStep(writer: BinaryWriter, value: Step): void {
    switch (value) {
        case "instruction":
            writer.writeUnsigned(0);
            return;
        case "into":
            writer.writeUnsigned(1);
            return;
        case "over":
            writer.writeUnsigned(2);
            return;
        case "out":
            writer.writeUnsigned(3);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one Step. */
export function decodeStep(reader: BinaryReader): Step {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "instruction";
        case 1:
            return "into";
        case 2:
            return "over";
        case 3:
            return "out";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one Step. */
export function toJsonStep(value: Step): Json {
    return value;
}

/** Return one Step from one JSON value. */
export function fromJsonStep(value: Json): Step {
    const variant = jsonString(value);

    switch (variant) {
        case "instruction":
            return "instruction";
        case "into":
            return "into";
        case "over":
            return "over";
        case "out":
            return "out";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}
