/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, jsonField, jsonObject, jsonString } from "../../../../protocol/serde.js";

/** Runtime-visible host ingress payload. */
export type HostEvent =
    /** Host lifecycle transition event. */
    | {
          readonly kind: "lifecycle";
          readonly lifecycle: LifecycleEvent;
      }
    /** Host memory pressure event. */
    | {
          readonly kind: "memoryPressure";
          readonly memory_pressure: MemoryPressureEvent;
      }
    /** Host thermal state event. */
    | {
          readonly kind: "thermalState";
          readonly thermal_state: ThermalEvent;
      }
    /** Host power mode event. */
    | {
          readonly kind: "powerMode";
          readonly power_mode: PowerModeEvent;
      }
    /** Host wall clock change event. */
    | {
          readonly kind: "wallClock";
          readonly wall_clock: WallClockEvent;
      }
;

export const HostEvent = {
    /** Host lifecycle transition event. */
    lifecycle(lifecycle: LifecycleEvent): HostEvent {
        return { kind: "lifecycle", lifecycle };
    },

    /** Host memory pressure event. */
    memoryPressure(memory_pressure: MemoryPressureEvent): HostEvent {
        return { kind: "memoryPressure", memory_pressure };
    },

    /** Host thermal state event. */
    thermalState(thermal_state: ThermalEvent): HostEvent {
        return { kind: "thermalState", thermal_state };
    },

    /** Host power mode event. */
    powerMode(power_mode: PowerModeEvent): HostEvent {
        return { kind: "powerMode", power_mode };
    },

    /** Host wall clock change event. */
    wallClock(wall_clock: WallClockEvent): HostEvent {
        return { kind: "wallClock", wall_clock };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: HostEvent): void {
        encodeHostEvent(writer, value);
    },

    /** Decode one HostEvent. */
    decode(reader: BinaryReader): HostEvent {
        return decodeHostEvent(reader);
    },

    /** Return this value as JSON. */
    toJson(value: HostEvent): Json {
        return toJsonHostEvent(value);
    },

    /** Return one HostEvent from one JSON value. */
    fromJson(value: Json): HostEvent {
        return fromJsonHostEvent(value);
    },
};

/** Encode one HostEvent. */
export function encodeHostEvent(writer: BinaryWriter, value: HostEvent): void {
    switch (value.kind) {
        case "lifecycle":
            writer.writeUnsigned(0);
            encodeLifecycleEvent(writer, value.lifecycle);
            return;
        case "memoryPressure":
            writer.writeUnsigned(1);
            encodeMemoryPressureEvent(writer, value.memory_pressure);
            return;
        case "thermalState":
            writer.writeUnsigned(2);
            encodeThermalEvent(writer, value.thermal_state);
            return;
        case "powerMode":
            writer.writeUnsigned(3);
            encodePowerModeEvent(writer, value.power_mode);
            return;
        case "wallClock":
            writer.writeUnsigned(4);
            encodeWallClockEvent(writer, value.wall_clock);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one HostEvent. */
export function decodeHostEvent(reader: BinaryReader): HostEvent {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const lifecycle = decodeLifecycleEvent(reader);

            return { kind: "lifecycle", lifecycle };
        }
        case 1: {
            const memory_pressure = decodeMemoryPressureEvent(reader);

            return { kind: "memoryPressure", memory_pressure };
        }
        case 2: {
            const thermal_state = decodeThermalEvent(reader);

            return { kind: "thermalState", thermal_state };
        }
        case 3: {
            const power_mode = decodePowerModeEvent(reader);

            return { kind: "powerMode", power_mode };
        }
        case 4: {
            const wall_clock = decodeWallClockEvent(reader);

            return { kind: "wallClock", wall_clock };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one HostEvent. */
export function toJsonHostEvent(value: HostEvent): Json {
    switch (value.kind) {
        case "lifecycle":
            return {
                kind: "lifecycle",
                lifecycle: toJsonLifecycleEvent(value.lifecycle),
            };
        case "memoryPressure":
            return {
                kind: "memoryPressure",
                memory_pressure: toJsonMemoryPressureEvent(value.memory_pressure),
            };
        case "thermalState":
            return {
                kind: "thermalState",
                thermal_state: toJsonThermalEvent(value.thermal_state),
            };
        case "powerMode":
            return {
                kind: "powerMode",
                power_mode: toJsonPowerModeEvent(value.power_mode),
            };
        case "wallClock":
            return {
                kind: "wallClock",
                wall_clock: toJsonWallClockEvent(value.wall_clock),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one HostEvent from one JSON value. */
export function fromJsonHostEvent(value: Json): HostEvent {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "lifecycle":
            return {
                kind,
                lifecycle: fromJsonLifecycleEvent(jsonField(object, "lifecycle")),
            };
        case "memoryPressure":
            return {
                kind,
                memory_pressure: fromJsonMemoryPressureEvent(jsonField(object, "memory_pressure")),
            };
        case "thermalState":
            return {
                kind,
                thermal_state: fromJsonThermalEvent(jsonField(object, "thermal_state")),
            };
        case "powerMode":
            return {
                kind,
                power_mode: fromJsonPowerModeEvent(jsonField(object, "power_mode")),
            };
        case "wallClock":
            return {
                kind,
                wall_clock: fromJsonWallClockEvent(jsonField(object, "wall_clock")),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** Host lifecycle transition. */
export type LifecycleEvent = {
    /** Lifecycle source. */
    readonly sourceKind: LifecycleSourceKind;
    /** Lifecycle state. */
    readonly state: LifecycleState;
};

export const LifecycleEvent = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: LifecycleEvent): void {
        encodeLifecycleEvent(writer, value);
    },

    /** Decode one LifecycleEvent. */
    decode(reader: BinaryReader): LifecycleEvent {
        return decodeLifecycleEvent(reader);
    },

    /** Return this value as JSON. */
    toJson(value: LifecycleEvent): Json {
        return toJsonLifecycleEvent(value);
    },

    /** Return one LifecycleEvent from one JSON value. */
    fromJson(value: Json): LifecycleEvent {
        return fromJsonLifecycleEvent(value);
    },
};

/** Encode one LifecycleEvent. */
export function encodeLifecycleEvent(writer: BinaryWriter, value: LifecycleEvent): void {
    encodeLifecycleSourceKind(writer, value.sourceKind);
    encodeLifecycleState(writer, value.state);
}

/** Decode one LifecycleEvent. */
export function decodeLifecycleEvent(reader: BinaryReader): LifecycleEvent {
    const sourceKind = decodeLifecycleSourceKind(reader);
    const state = decodeLifecycleState(reader);

    return {
        sourceKind,
        state,
    };
}

/** Return one JSON value for one LifecycleEvent. */
export function toJsonLifecycleEvent(value: LifecycleEvent): Json {
    return {
        sourceKind: toJsonLifecycleSourceKind(value.sourceKind),
        state: toJsonLifecycleState(value.state),
    };
}

/** Return one LifecycleEvent from one JSON value. */
export function fromJsonLifecycleEvent(value: Json): LifecycleEvent {
    const object = jsonObject(value);

    return {
        sourceKind: fromJsonLifecycleSourceKind(jsonField(object, "sourceKind")),
        state: fromJsonLifecycleState(jsonField(object, "state")),
    };
}

/** Host lifecycle source attachment. */
export type LifecycleSourceKind = "application" | "worker" | "window";

export const LifecycleSourceKind = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: LifecycleSourceKind): void {
        encodeLifecycleSourceKind(writer, value);
    },

    /** Decode one LifecycleSourceKind. */
    decode(reader: BinaryReader): LifecycleSourceKind {
        return decodeLifecycleSourceKind(reader);
    },

    /** Return this value as JSON. */
    toJson(value: LifecycleSourceKind): Json {
        return toJsonLifecycleSourceKind(value);
    },

    /** Return one LifecycleSourceKind from one JSON value. */
    fromJson(value: Json): LifecycleSourceKind {
        return fromJsonLifecycleSourceKind(value);
    },
};

/** Encode one LifecycleSourceKind. */
export function encodeLifecycleSourceKind(writer: BinaryWriter, value: LifecycleSourceKind): void {
    switch (value) {
        case "application":
            writer.writeUnsigned(0);
            return;
        case "worker":
            writer.writeUnsigned(1);
            return;
        case "window":
            writer.writeUnsigned(2);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one LifecycleSourceKind. */
export function decodeLifecycleSourceKind(reader: BinaryReader): LifecycleSourceKind {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "application";
        case 1:
            return "worker";
        case 2:
            return "window";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one LifecycleSourceKind. */
export function toJsonLifecycleSourceKind(value: LifecycleSourceKind): Json {
    return value;
}

/** Return one LifecycleSourceKind from one JSON value. */
export function fromJsonLifecycleSourceKind(value: Json): LifecycleSourceKind {
    const variant = jsonString(value);

    switch (variant) {
        case "application":
            return "application";
        case "worker":
            return "worker";
        case "window":
            return "window";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** Host lifecycle state. */
export type LifecycleState = "initializing" | "running" | "paused" | "stopped" | "destroyed";

export const LifecycleState = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: LifecycleState): void {
        encodeLifecycleState(writer, value);
    },

    /** Decode one LifecycleState. */
    decode(reader: BinaryReader): LifecycleState {
        return decodeLifecycleState(reader);
    },

    /** Return this value as JSON. */
    toJson(value: LifecycleState): Json {
        return toJsonLifecycleState(value);
    },

    /** Return one LifecycleState from one JSON value. */
    fromJson(value: Json): LifecycleState {
        return fromJsonLifecycleState(value);
    },
};

/** Encode one LifecycleState. */
export function encodeLifecycleState(writer: BinaryWriter, value: LifecycleState): void {
    switch (value) {
        case "initializing":
            writer.writeUnsigned(0);
            return;
        case "running":
            writer.writeUnsigned(1);
            return;
        case "paused":
            writer.writeUnsigned(2);
            return;
        case "stopped":
            writer.writeUnsigned(3);
            return;
        case "destroyed":
            writer.writeUnsigned(4);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one LifecycleState. */
export function decodeLifecycleState(reader: BinaryReader): LifecycleState {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "initializing";
        case 1:
            return "running";
        case 2:
            return "paused";
        case 3:
            return "stopped";
        case 4:
            return "destroyed";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one LifecycleState. */
export function toJsonLifecycleState(value: LifecycleState): Json {
    return value;
}

/** Return one LifecycleState from one JSON value. */
export function fromJsonLifecycleState(value: Json): LifecycleState {
    const variant = jsonString(value);

    switch (variant) {
        case "initializing":
            return "initializing";
        case "running":
            return "running";
        case "paused":
            return "paused";
        case "stopped":
            return "stopped";
        case "destroyed":
            return "destroyed";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** Host memory pressure event. */
export type MemoryPressureEvent = {
    /** Memory pressure level. */
    readonly level: MemoryPressureLevel;
};

export const MemoryPressureEvent = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: MemoryPressureEvent): void {
        encodeMemoryPressureEvent(writer, value);
    },

    /** Decode one MemoryPressureEvent. */
    decode(reader: BinaryReader): MemoryPressureEvent {
        return decodeMemoryPressureEvent(reader);
    },

    /** Return this value as JSON. */
    toJson(value: MemoryPressureEvent): Json {
        return toJsonMemoryPressureEvent(value);
    },

    /** Return one MemoryPressureEvent from one JSON value. */
    fromJson(value: Json): MemoryPressureEvent {
        return fromJsonMemoryPressureEvent(value);
    },
};

/** Encode one MemoryPressureEvent. */
export function encodeMemoryPressureEvent(writer: BinaryWriter, value: MemoryPressureEvent): void {
    encodeMemoryPressureLevel(writer, value.level);
}

/** Decode one MemoryPressureEvent. */
export function decodeMemoryPressureEvent(reader: BinaryReader): MemoryPressureEvent {
    const level = decodeMemoryPressureLevel(reader);

    return {
        level,
    };
}

/** Return one JSON value for one MemoryPressureEvent. */
export function toJsonMemoryPressureEvent(value: MemoryPressureEvent): Json {
    return {
        level: toJsonMemoryPressureLevel(value.level),
    };
}

/** Return one MemoryPressureEvent from one JSON value. */
export function fromJsonMemoryPressureEvent(value: Json): MemoryPressureEvent {
    const object = jsonObject(value);

    return {
        level: fromJsonMemoryPressureLevel(jsonField(object, "level")),
    };
}

/** Host memory pressure state. */
export type MemoryPressureLevel = "normal" | "warning" | "critical";

export const MemoryPressureLevel = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: MemoryPressureLevel): void {
        encodeMemoryPressureLevel(writer, value);
    },

    /** Decode one MemoryPressureLevel. */
    decode(reader: BinaryReader): MemoryPressureLevel {
        return decodeMemoryPressureLevel(reader);
    },

    /** Return this value as JSON. */
    toJson(value: MemoryPressureLevel): Json {
        return toJsonMemoryPressureLevel(value);
    },

    /** Return one MemoryPressureLevel from one JSON value. */
    fromJson(value: Json): MemoryPressureLevel {
        return fromJsonMemoryPressureLevel(value);
    },
};

/** Encode one MemoryPressureLevel. */
export function encodeMemoryPressureLevel(writer: BinaryWriter, value: MemoryPressureLevel): void {
    switch (value) {
        case "normal":
            writer.writeUnsigned(0);
            return;
        case "warning":
            writer.writeUnsigned(1);
            return;
        case "critical":
            writer.writeUnsigned(2);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one MemoryPressureLevel. */
export function decodeMemoryPressureLevel(reader: BinaryReader): MemoryPressureLevel {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "normal";
        case 1:
            return "warning";
        case 2:
            return "critical";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one MemoryPressureLevel. */
export function toJsonMemoryPressureLevel(value: MemoryPressureLevel): Json {
    return value;
}

/** Return one MemoryPressureLevel from one JSON value. */
export function fromJsonMemoryPressureLevel(value: Json): MemoryPressureLevel {
    const variant = jsonString(value);

    switch (variant) {
        case "normal":
            return "normal";
        case "warning":
            return "warning";
        case "critical":
            return "critical";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** Host power mode state. */
export type PowerMode = "normal" | "lowPower";

export const PowerMode = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: PowerMode): void {
        encodePowerMode(writer, value);
    },

    /** Decode one PowerMode. */
    decode(reader: BinaryReader): PowerMode {
        return decodePowerMode(reader);
    },

    /** Return this value as JSON. */
    toJson(value: PowerMode): Json {
        return toJsonPowerMode(value);
    },

    /** Return one PowerMode from one JSON value. */
    fromJson(value: Json): PowerMode {
        return fromJsonPowerMode(value);
    },
};

/** Encode one PowerMode. */
export function encodePowerMode(writer: BinaryWriter, value: PowerMode): void {
    switch (value) {
        case "normal":
            writer.writeUnsigned(0);
            return;
        case "lowPower":
            writer.writeUnsigned(1);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one PowerMode. */
export function decodePowerMode(reader: BinaryReader): PowerMode {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "normal";
        case 1:
            return "lowPower";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one PowerMode. */
export function toJsonPowerMode(value: PowerMode): Json {
    return value;
}

/** Return one PowerMode from one JSON value. */
export function fromJsonPowerMode(value: Json): PowerMode {
    const variant = jsonString(value);

    switch (variant) {
        case "normal":
            return "normal";
        case "lowPower":
            return "lowPower";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** Host power mode event. */
export type PowerModeEvent = {
    /** Power mode. */
    readonly mode: PowerMode;
};

export const PowerModeEvent = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: PowerModeEvent): void {
        encodePowerModeEvent(writer, value);
    },

    /** Decode one PowerModeEvent. */
    decode(reader: BinaryReader): PowerModeEvent {
        return decodePowerModeEvent(reader);
    },

    /** Return this value as JSON. */
    toJson(value: PowerModeEvent): Json {
        return toJsonPowerModeEvent(value);
    },

    /** Return one PowerModeEvent from one JSON value. */
    fromJson(value: Json): PowerModeEvent {
        return fromJsonPowerModeEvent(value);
    },
};

/** Encode one PowerModeEvent. */
export function encodePowerModeEvent(writer: BinaryWriter, value: PowerModeEvent): void {
    encodePowerMode(writer, value.mode);
}

/** Decode one PowerModeEvent. */
export function decodePowerModeEvent(reader: BinaryReader): PowerModeEvent {
    const mode = decodePowerMode(reader);

    return {
        mode,
    };
}

/** Return one JSON value for one PowerModeEvent. */
export function toJsonPowerModeEvent(value: PowerModeEvent): Json {
    return {
        mode: toJsonPowerMode(value.mode),
    };
}

/** Return one PowerModeEvent from one JSON value. */
export function fromJsonPowerModeEvent(value: Json): PowerModeEvent {
    const object = jsonObject(value);

    return {
        mode: fromJsonPowerMode(jsonField(object, "mode")),
    };
}

/** Host thermal state event. */
export type ThermalEvent = {
    /** Thermal state. */
    readonly state: ThermalState;
};

export const ThermalEvent = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ThermalEvent): void {
        encodeThermalEvent(writer, value);
    },

    /** Decode one ThermalEvent. */
    decode(reader: BinaryReader): ThermalEvent {
        return decodeThermalEvent(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ThermalEvent): Json {
        return toJsonThermalEvent(value);
    },

    /** Return one ThermalEvent from one JSON value. */
    fromJson(value: Json): ThermalEvent {
        return fromJsonThermalEvent(value);
    },
};

/** Encode one ThermalEvent. */
export function encodeThermalEvent(writer: BinaryWriter, value: ThermalEvent): void {
    encodeThermalState(writer, value.state);
}

/** Decode one ThermalEvent. */
export function decodeThermalEvent(reader: BinaryReader): ThermalEvent {
    const state = decodeThermalState(reader);

    return {
        state,
    };
}

/** Return one JSON value for one ThermalEvent. */
export function toJsonThermalEvent(value: ThermalEvent): Json {
    return {
        state: toJsonThermalState(value.state),
    };
}

/** Return one ThermalEvent from one JSON value. */
export function fromJsonThermalEvent(value: Json): ThermalEvent {
    const object = jsonObject(value);

    return {
        state: fromJsonThermalState(jsonField(object, "state")),
    };
}

/** Host thermal state. */
export type ThermalState = "nominal" | "fair" | "serious" | "critical";

export const ThermalState = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ThermalState): void {
        encodeThermalState(writer, value);
    },

    /** Decode one ThermalState. */
    decode(reader: BinaryReader): ThermalState {
        return decodeThermalState(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ThermalState): Json {
        return toJsonThermalState(value);
    },

    /** Return one ThermalState from one JSON value. */
    fromJson(value: Json): ThermalState {
        return fromJsonThermalState(value);
    },
};

/** Encode one ThermalState. */
export function encodeThermalState(writer: BinaryWriter, value: ThermalState): void {
    switch (value) {
        case "nominal":
            writer.writeUnsigned(0);
            return;
        case "fair":
            writer.writeUnsigned(1);
            return;
        case "serious":
            writer.writeUnsigned(2);
            return;
        case "critical":
            writer.writeUnsigned(3);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one ThermalState. */
export function decodeThermalState(reader: BinaryReader): ThermalState {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "nominal";
        case 1:
            return "fair";
        case 2:
            return "serious";
        case 3:
            return "critical";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one ThermalState. */
export function toJsonThermalState(value: ThermalState): Json {
    return value;
}

/** Return one ThermalState from one JSON value. */
export function fromJsonThermalState(value: Json): ThermalState {
    const variant = jsonString(value);

    switch (variant) {
        case "nominal":
            return "nominal";
        case "fair":
            return "fair";
        case "serious":
            return "serious";
        case "critical":
            return "critical";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** Host wall clock change event. */
export type WallClockEvent = {
};

export const WallClockEvent = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: WallClockEvent): void {
        encodeWallClockEvent(writer, value);
    },

    /** Decode one WallClockEvent. */
    decode(reader: BinaryReader): WallClockEvent {
        return decodeWallClockEvent(reader);
    },

    /** Return this value as JSON. */
    toJson(value: WallClockEvent): Json {
        return toJsonWallClockEvent(value);
    },

    /** Return one WallClockEvent from one JSON value. */
    fromJson(value: Json): WallClockEvent {
        return fromJsonWallClockEvent(value);
    },
};

/** Encode one WallClockEvent. */
export function encodeWallClockEvent(writer: BinaryWriter, value: WallClockEvent): void {
}

/** Decode one WallClockEvent. */
export function decodeWallClockEvent(reader: BinaryReader): WallClockEvent {
    return {
    };
}

/** Return one JSON value for one WallClockEvent. */
export function toJsonWallClockEvent(value: WallClockEvent): Json {
    return {
    };
}

/** Return one WallClockEvent from one JSON value. */
export function fromJsonWallClockEvent(value: Json): WallClockEvent {
    const object = jsonObject(value);
    return {
    };
}
