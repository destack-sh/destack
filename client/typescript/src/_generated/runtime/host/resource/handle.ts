/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, jsonBigint, jsonField, jsonObject } from "../../../../protocol/serde.js";
import type { WorkerId } from "../../worker/worker.js";
import { decodeWorkerId, encodeWorkerId, fromJsonWorkerId, toJsonWorkerId } from "../../worker/worker.js";

/** The global identifier for one runtime-managed resource. */
export type ResourceId = {
    /** Worker that owns this resource. */
    readonly workerId: WorkerId;
    /** Worker-local resource identifier. */
    readonly localId: bigint;
};

export const ResourceId = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ResourceId): void {
        encodeResourceId(writer, value);
    },

    /** Decode one ResourceId. */
    decode(reader: BinaryReader): ResourceId {
        return decodeResourceId(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ResourceId): Json {
        return toJsonResourceId(value);
    },

    /** Return one ResourceId from one JSON value. */
    fromJson(value: Json): ResourceId {
        return fromJsonResourceId(value);
    },
};

/** Encode one ResourceId. */
export function encodeResourceId(writer: BinaryWriter, value: ResourceId): void {
    encodeWorkerId(writer, value.workerId);
    writer.writeUnsigned(value.localId);
}

/** Decode one ResourceId. */
export function decodeResourceId(reader: BinaryReader): ResourceId {
    const workerId = decodeWorkerId(reader);
    const localId = reader.readUnsigned();

    return {
        workerId,
        localId,
    };
}

/** Return one JSON value for one ResourceId. */
export function toJsonResourceId(value: ResourceId): Json {
    return {
        workerId: toJsonWorkerId(value.workerId),
        localId: value.localId.toString(),
    };
}

/** Return one ResourceId from one JSON value. */
export function fromJsonResourceId(value: Json): ResourceId {
    const object = jsonObject(value);

    return {
        workerId: fromJsonWorkerId(jsonField(object, "workerId")),
        localId: jsonBigint(jsonField(object, "localId")),
    };
}
