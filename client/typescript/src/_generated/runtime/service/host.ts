/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, jsonBigint, jsonField, jsonObject, jsonOptional } from "../../../protocol/serde.js";
import type { HostEvent } from "../host/event/event.js";
import type { WorldId } from "./world.js";
import type { RandomSource } from "../world/random/random.js";
import type { ClockSource } from "../world/time/clock.js";
import type { Instant } from "../world/time/instant.js";
import { decodeHostEvent, encodeHostEvent, fromJsonHostEvent, toJsonHostEvent } from "../host/event/event.js";
import { decodeWorldId, encodeWorldId, fromJsonWorldId, toJsonWorldId } from "./world.js";
import { decodeRandomSource, encodeRandomSource, fromJsonRandomSource, toJsonRandomSource } from "../world/random/random.js";
import { decodeClockSource, encodeClockSource, fromJsonClockSource, toJsonClockSource } from "../world/time/clock.js";
import { decodeInstant, encodeInstant, fromJsonInstant, toJsonInstant } from "../world/time/instant.js";

/** Request to advance one runtime-controlled Clock. */
export type AdvanceClockRequest = {
    /** World whose Clock advances. */
    readonly worldId: WorldId;
    /** Absolute wall-clock deadline to reach. */
    readonly deadline: Instant;
};

export const AdvanceClockRequest = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: AdvanceClockRequest): void {
        encodeAdvanceClockRequest(writer, value);
    },

    /** Decode one AdvanceClockRequest. */
    decode(reader: BinaryReader): AdvanceClockRequest {
        return decodeAdvanceClockRequest(reader);
    },

    /** Return this value as JSON. */
    toJson(value: AdvanceClockRequest): Json {
        return toJsonAdvanceClockRequest(value);
    },

    /** Return one AdvanceClockRequest from one JSON value. */
    fromJson(value: Json): AdvanceClockRequest {
        return fromJsonAdvanceClockRequest(value);
    },
};

/** Encode one AdvanceClockRequest. */
export function encodeAdvanceClockRequest(writer: BinaryWriter, value: AdvanceClockRequest): void {
    encodeWorldId(writer, value.worldId);
    encodeInstant(writer, value.deadline);
}

/** Decode one AdvanceClockRequest. */
export function decodeAdvanceClockRequest(reader: BinaryReader): AdvanceClockRequest {
    const worldId = decodeWorldId(reader);
    const deadline = decodeInstant(reader);

    return {
        worldId,
        deadline,
    };
}

/** Return one JSON value for one AdvanceClockRequest. */
export function toJsonAdvanceClockRequest(value: AdvanceClockRequest): Json {
    return {
        worldId: toJsonWorldId(value.worldId),
        deadline: toJsonInstant(value.deadline),
    };
}

/** Return one AdvanceClockRequest from one JSON value. */
export function fromJsonAdvanceClockRequest(value: Json): AdvanceClockRequest {
    const object = jsonObject(value);

    return {
        worldId: fromJsonWorldId(jsonField(object, "worldId")),
        deadline: fromJsonInstant(jsonField(object, "deadline")),
    };
}

/** Effective World clock reading. */
export type Clock = {
    /** Effective clock source. */
    readonly source: ClockSource;
    /** Current wall-clock time. */
    readonly wall: Instant;
    /** Current monotonic time. */
    readonly monotonic: Instant;
};

export const Clock = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: Clock): void {
        encodeClock(writer, value);
    },

    /** Decode one Clock. */
    decode(reader: BinaryReader): Clock {
        return decodeClock(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Clock): Json {
        return toJsonClock(value);
    },

    /** Return one Clock from one JSON value. */
    fromJson(value: Json): Clock {
        return fromJsonClock(value);
    },
};

/** Encode one Clock. */
export function encodeClock(writer: BinaryWriter, value: Clock): void {
    encodeClockSource(writer, value.source);
    encodeInstant(writer, value.wall);
    encodeInstant(writer, value.monotonic);
}

/** Decode one Clock. */
export function decodeClock(reader: BinaryReader): Clock {
    const source = decodeClockSource(reader);
    const wall = decodeInstant(reader);
    const monotonic = decodeInstant(reader);

    return {
        source,
        wall,
        monotonic,
    };
}

/** Return one JSON value for one Clock. */
export function toJsonClock(value: Clock): Json {
    return {
        source: toJsonClockSource(value.source),
        wall: toJsonInstant(value.wall),
        monotonic: toJsonInstant(value.monotonic),
    };
}

/** Return one Clock from one JSON value. */
export function fromJsonClock(value: Json): Clock {
    const object = jsonObject(value);

    return {
        source: fromJsonClockSource(jsonField(object, "source")),
        wall: fromJsonInstant(jsonField(object, "wall")),
        monotonic: fromJsonInstant(jsonField(object, "monotonic")),
    };
}

/** Effective World randomness state. */
export type Random = {
    /** Effective randomness source. */
    readonly source: RandomSource;
    /** Deterministic root seed, or absent for host randomness. */
    readonly seed?: bigint;
};

export const Random = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: Random): void {
        encodeRandom(writer, value);
    },

    /** Decode one Random. */
    decode(reader: BinaryReader): Random {
        return decodeRandom(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Random): Json {
        return toJsonRandom(value);
    },

    /** Return one Random from one JSON value. */
    fromJson(value: Json): Random {
        return fromJsonRandom(value);
    },
};

/** Encode one Random. */
export function encodeRandom(writer: BinaryWriter, value: Random): void {
    encodeRandomSource(writer, value.source);
    writer.writeOption(value.seed, (value1) => {
        writer.writeUnsigned(value1);
    });
}

/** Decode one Random. */
export function decodeRandom(reader: BinaryReader): Random {
    const source = decodeRandomSource(reader);
    const seed = reader.readOption(() => reader.readUnsigned());

    return {
        source,
        ...(seed === undefined ? {} : { seed }),
    };
}

/** Return one JSON value for one Random. */
export function toJsonRandom(value: Random): Json {
    return {
        source: toJsonRandomSource(value.source),
        ...(value.seed === undefined ? {} : { seed: value.seed.toString() }),
    };
}

/** Return one Random from one JSON value. */
export function fromJsonRandom(value: Json): Random {
    const object = jsonObject(value);

    return {
        source: fromJsonRandomSource(jsonField(object, "source")),
        seed: jsonOptional(object, "seed", (value) => jsonBigint(value)),
    };
}

/** Request to read one World's effective Clock. */
export type ReadClockRequest = {
    /** World to inspect. */
    readonly worldId: WorldId;
};

export const ReadClockRequest = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ReadClockRequest): void {
        encodeReadClockRequest(writer, value);
    },

    /** Decode one ReadClockRequest. */
    decode(reader: BinaryReader): ReadClockRequest {
        return decodeReadClockRequest(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ReadClockRequest): Json {
        return toJsonReadClockRequest(value);
    },

    /** Return one ReadClockRequest from one JSON value. */
    fromJson(value: Json): ReadClockRequest {
        return fromJsonReadClockRequest(value);
    },
};

/** Encode one ReadClockRequest. */
export function encodeReadClockRequest(writer: BinaryWriter, value: ReadClockRequest): void {
    encodeWorldId(writer, value.worldId);
}

/** Decode one ReadClockRequest. */
export function decodeReadClockRequest(reader: BinaryReader): ReadClockRequest {
    const worldId = decodeWorldId(reader);

    return {
        worldId,
    };
}

/** Return one JSON value for one ReadClockRequest. */
export function toJsonReadClockRequest(value: ReadClockRequest): Json {
    return {
        worldId: toJsonWorldId(value.worldId),
    };
}

/** Return one ReadClockRequest from one JSON value. */
export function fromJsonReadClockRequest(value: Json): ReadClockRequest {
    const object = jsonObject(value);

    return {
        worldId: fromJsonWorldId(jsonField(object, "worldId")),
    };
}

/** Request to read one World's effective Random source. */
export type ReadRandomRequest = {
    /** World to inspect. */
    readonly worldId: WorldId;
};

export const ReadRandomRequest = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ReadRandomRequest): void {
        encodeReadRandomRequest(writer, value);
    },

    /** Decode one ReadRandomRequest. */
    decode(reader: BinaryReader): ReadRandomRequest {
        return decodeReadRandomRequest(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ReadRandomRequest): Json {
        return toJsonReadRandomRequest(value);
    },

    /** Return one ReadRandomRequest from one JSON value. */
    fromJson(value: Json): ReadRandomRequest {
        return fromJsonReadRandomRequest(value);
    },
};

/** Encode one ReadRandomRequest. */
export function encodeReadRandomRequest(writer: BinaryWriter, value: ReadRandomRequest): void {
    encodeWorldId(writer, value.worldId);
}

/** Decode one ReadRandomRequest. */
export function decodeReadRandomRequest(reader: BinaryReader): ReadRandomRequest {
    const worldId = decodeWorldId(reader);

    return {
        worldId,
    };
}

/** Return one JSON value for one ReadRandomRequest. */
export function toJsonReadRandomRequest(value: ReadRandomRequest): Json {
    return {
        worldId: toJsonWorldId(value.worldId),
    };
}

/** Return one ReadRandomRequest from one JSON value. */
export function fromJsonReadRandomRequest(value: Json): ReadRandomRequest {
    const object = jsonObject(value);

    return {
        worldId: fromJsonWorldId(jsonField(object, "worldId")),
    };
}

/** Request to reseed one deterministic Random source. */
export type ReseedRandomRequest = {
    /** World whose Random source changes. */
    readonly worldId: WorldId;
    /** Replacement deterministic root seed. */
    readonly seed: bigint;
};

export const ReseedRandomRequest = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ReseedRandomRequest): void {
        encodeReseedRandomRequest(writer, value);
    },

    /** Decode one ReseedRandomRequest. */
    decode(reader: BinaryReader): ReseedRandomRequest {
        return decodeReseedRandomRequest(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ReseedRandomRequest): Json {
        return toJsonReseedRandomRequest(value);
    },

    /** Return one ReseedRandomRequest from one JSON value. */
    fromJson(value: Json): ReseedRandomRequest {
        return fromJsonReseedRandomRequest(value);
    },
};

/** Encode one ReseedRandomRequest. */
export function encodeReseedRandomRequest(writer: BinaryWriter, value: ReseedRandomRequest): void {
    encodeWorldId(writer, value.worldId);
    writer.writeUnsigned(value.seed);
}

/** Decode one ReseedRandomRequest. */
export function decodeReseedRandomRequest(reader: BinaryReader): ReseedRandomRequest {
    const worldId = decodeWorldId(reader);
    const seed = reader.readUnsigned();

    return {
        worldId,
        seed,
    };
}

/** Return one JSON value for one ReseedRandomRequest. */
export function toJsonReseedRandomRequest(value: ReseedRandomRequest): Json {
    return {
        worldId: toJsonWorldId(value.worldId),
        seed: value.seed.toString(),
    };
}

/** Return one ReseedRandomRequest from one JSON value. */
export function fromJsonReseedRandomRequest(value: Json): ReseedRandomRequest {
    const object = jsonObject(value);

    return {
        worldId: fromJsonWorldId(jsonField(object, "worldId")),
        seed: jsonBigint(jsonField(object, "seed")),
    };
}

/** Request to send one typed host Event into a World. */
export type SendEventRequest = {
    /** World receiving the Event. */
    readonly worldId: WorldId;
    /** Host Event to enqueue. */
    readonly event: HostEvent;
};

export const SendEventRequest = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: SendEventRequest): void {
        encodeSendEventRequest(writer, value);
    },

    /** Decode one SendEventRequest. */
    decode(reader: BinaryReader): SendEventRequest {
        return decodeSendEventRequest(reader);
    },

    /** Return this value as JSON. */
    toJson(value: SendEventRequest): Json {
        return toJsonSendEventRequest(value);
    },

    /** Return one SendEventRequest from one JSON value. */
    fromJson(value: Json): SendEventRequest {
        return fromJsonSendEventRequest(value);
    },
};

/** Encode one SendEventRequest. */
export function encodeSendEventRequest(writer: BinaryWriter, value: SendEventRequest): void {
    encodeWorldId(writer, value.worldId);
    encodeHostEvent(writer, value.event);
}

/** Decode one SendEventRequest. */
export function decodeSendEventRequest(reader: BinaryReader): SendEventRequest {
    const worldId = decodeWorldId(reader);
    const event = decodeHostEvent(reader);

    return {
        worldId,
        event,
    };
}

/** Return one JSON value for one SendEventRequest. */
export function toJsonSendEventRequest(value: SendEventRequest): Json {
    return {
        worldId: toJsonWorldId(value.worldId),
        event: toJsonHostEvent(value.event),
    };
}

/** Return one SendEventRequest from one JSON value. */
export function fromJsonSendEventRequest(value: Json): SendEventRequest {
    const object = jsonObject(value);

    return {
        worldId: fromJsonWorldId(jsonField(object, "worldId")),
        event: fromJsonHostEvent(jsonField(object, "event")),
    };
}

/** Request to replace one runtime-controlled Clock. */
export type SetClockRequest = {
    /** World whose Clock changes. */
    readonly worldId: WorldId;
    /** Replacement wall-clock time. */
    readonly wall: Instant;
    /** Replacement monotonic time. */
    readonly monotonic: Instant;
};

export const SetClockRequest = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: SetClockRequest): void {
        encodeSetClockRequest(writer, value);
    },

    /** Decode one SetClockRequest. */
    decode(reader: BinaryReader): SetClockRequest {
        return decodeSetClockRequest(reader);
    },

    /** Return this value as JSON. */
    toJson(value: SetClockRequest): Json {
        return toJsonSetClockRequest(value);
    },

    /** Return one SetClockRequest from one JSON value. */
    fromJson(value: Json): SetClockRequest {
        return fromJsonSetClockRequest(value);
    },
};

/** Encode one SetClockRequest. */
export function encodeSetClockRequest(writer: BinaryWriter, value: SetClockRequest): void {
    encodeWorldId(writer, value.worldId);
    encodeInstant(writer, value.wall);
    encodeInstant(writer, value.monotonic);
}

/** Decode one SetClockRequest. */
export function decodeSetClockRequest(reader: BinaryReader): SetClockRequest {
    const worldId = decodeWorldId(reader);
    const wall = decodeInstant(reader);
    const monotonic = decodeInstant(reader);

    return {
        worldId,
        wall,
        monotonic,
    };
}

/** Return one JSON value for one SetClockRequest. */
export function toJsonSetClockRequest(value: SetClockRequest): Json {
    return {
        worldId: toJsonWorldId(value.worldId),
        wall: toJsonInstant(value.wall),
        monotonic: toJsonInstant(value.monotonic),
    };
}

/** Return one SetClockRequest from one JSON value. */
export function fromJsonSetClockRequest(value: Json): SetClockRequest {
    const object = jsonObject(value);

    return {
        worldId: fromJsonWorldId(jsonField(object, "worldId")),
        wall: fromJsonInstant(jsonField(object, "wall")),
        monotonic: fromJsonInstant(jsonField(object, "monotonic")),
    };
}
