/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, jsonBigint, jsonField, jsonObject, jsonOptional, jsonString } from "../../../protocol/serde.js";
import type { MemoryRange } from "../../memory/address/map.js";
import type { BreakpointId } from "../../program/outcome.js";
import type { WatchpointId } from "../../program/watch.js";
import type { Breakpoint } from "../debugger/breakpoint.js";
import type { PointFilter } from "../debugger/breakpoint.js";
import type { EvaluationLimits } from "../debugger/evaluation.js";
import type { EvaluationMode } from "../debugger/evaluation.js";
import type { EvaluationOutcome } from "../debugger/evaluation.js";
import type { EvaluationTarget } from "../debugger/evaluation.js";
import type { Execution } from "../debugger/execution.js";
import type { Step } from "../debugger/execution.js";
import type { FrameId } from "../debugger/frame.js";
import type { AllocationId } from "../debugger/heap.js";
import type { RootId } from "../debugger/heap.js";
import type { Probe } from "../debugger/probe.js";
import type { ProbeAction } from "../debugger/probe.js";
import type { ProbeFilter } from "../debugger/probe.js";
import type { ProbeId } from "../debugger/probe.js";
import type { Watchpoint } from "../debugger/watchpoint.js";
import type { MemoryFilter } from "../debugger/watchpoint.js";
import type { RuntimeId } from "../runtime.js";
import type { WorldId } from "./world.js";
import type { WorkerId } from "../worker/worker.js";
import type { Moment } from "../world/lineage/moment.js";
import { decodeMemoryRange, encodeMemoryRange, fromJsonMemoryRange, toJsonMemoryRange } from "../../memory/address/map.js";
import { decodeBreakpointId, encodeBreakpointId, fromJsonBreakpointId, toJsonBreakpointId } from "../../program/outcome.js";
import { decodeWatchpointId, encodeWatchpointId, fromJsonWatchpointId, toJsonWatchpointId } from "../../program/watch.js";
import { decodeBreakpoint, encodeBreakpoint, fromJsonBreakpoint, toJsonBreakpoint } from "../debugger/breakpoint.js";
import { decodePointFilter, encodePointFilter, fromJsonPointFilter, toJsonPointFilter } from "../debugger/breakpoint.js";
import { decodeEvaluationLimits, encodeEvaluationLimits, fromJsonEvaluationLimits, toJsonEvaluationLimits } from "../debugger/evaluation.js";
import { decodeEvaluationMode, encodeEvaluationMode, fromJsonEvaluationMode, toJsonEvaluationMode } from "../debugger/evaluation.js";
import { decodeEvaluationOutcome, encodeEvaluationOutcome, fromJsonEvaluationOutcome, toJsonEvaluationOutcome } from "../debugger/evaluation.js";
import { decodeEvaluationTarget, encodeEvaluationTarget, fromJsonEvaluationTarget, toJsonEvaluationTarget } from "../debugger/evaluation.js";
import { decodeExecution, encodeExecution, fromJsonExecution, toJsonExecution } from "../debugger/execution.js";
import { decodeStep, encodeStep, fromJsonStep, toJsonStep } from "../debugger/execution.js";
import { decodeFrameId, encodeFrameId, fromJsonFrameId, toJsonFrameId } from "../debugger/frame.js";
import { decodeAllocationId, encodeAllocationId, fromJsonAllocationId, toJsonAllocationId } from "../debugger/heap.js";
import { decodeRootId, encodeRootId, fromJsonRootId, toJsonRootId } from "../debugger/heap.js";
import { decodeProbe, encodeProbe, fromJsonProbe, toJsonProbe } from "../debugger/probe.js";
import { decodeProbeAction, encodeProbeAction, fromJsonProbeAction, toJsonProbeAction } from "../debugger/probe.js";
import { decodeProbeFilter, encodeProbeFilter, fromJsonProbeFilter, toJsonProbeFilter } from "../debugger/probe.js";
import { decodeProbeId, encodeProbeId, fromJsonProbeId, toJsonProbeId } from "../debugger/probe.js";
import { decodeWatchpoint, encodeWatchpoint, fromJsonWatchpoint, toJsonWatchpoint } from "../debugger/watchpoint.js";
import { decodeMemoryFilter, encodeMemoryFilter, fromJsonMemoryFilter, toJsonMemoryFilter } from "../debugger/watchpoint.js";
import { decodeRuntimeId, encodeRuntimeId, fromJsonRuntimeId, toJsonRuntimeId } from "../runtime.js";
import { decodeWorldId, encodeWorldId, fromJsonWorldId, toJsonWorldId } from "./world.js";
import { decodeWorkerId, encodeWorkerId, fromJsonWorkerId, toJsonWorkerId } from "../worker/worker.js";
import { decodeMoment, encodeMoment, fromJsonMoment, toJsonMoment } from "../world/lineage/moment.js";

/** Request to add one Breakpoint. */
export type AddBreakpointRequest = {
    /** World receiving the Breakpoint. */
    readonly worldId: WorldId;
    /** Executable Program point selection. */
    readonly filter: PointFilter;
};

export const AddBreakpointRequest = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: AddBreakpointRequest): void {
        encodeAddBreakpointRequest(writer, value);
    },

    /** Decode one AddBreakpointRequest. */
    decode(reader: BinaryReader): AddBreakpointRequest {
        return decodeAddBreakpointRequest(reader);
    },

    /** Return this value as JSON. */
    toJson(value: AddBreakpointRequest): Json {
        return toJsonAddBreakpointRequest(value);
    },

    /** Return one AddBreakpointRequest from one JSON value. */
    fromJson(value: Json): AddBreakpointRequest {
        return fromJsonAddBreakpointRequest(value);
    },
};

/** Encode one AddBreakpointRequest. */
export function encodeAddBreakpointRequest(writer: BinaryWriter, value: AddBreakpointRequest): void {
    encodeWorldId(writer, value.worldId);
    encodePointFilter(writer, value.filter);
}

/** Decode one AddBreakpointRequest. */
export function decodeAddBreakpointRequest(reader: BinaryReader): AddBreakpointRequest {
    const worldId = decodeWorldId(reader);
    const filter = decodePointFilter(reader);

    return {
        worldId,
        filter,
    };
}

/** Return one JSON value for one AddBreakpointRequest. */
export function toJsonAddBreakpointRequest(value: AddBreakpointRequest): Json {
    return {
        worldId: toJsonWorldId(value.worldId),
        filter: toJsonPointFilter(value.filter),
    };
}

/** Return one AddBreakpointRequest from one JSON value. */
export function fromJsonAddBreakpointRequest(value: Json): AddBreakpointRequest {
    const object = jsonObject(value);

    return {
        worldId: fromJsonWorldId(jsonField(object, "worldId")),
        filter: fromJsonPointFilter(jsonField(object, "filter")),
    };
}

/** Request to add one Probe. */
export type AddProbeRequest = {
    /** World receiving the Probe. */
    readonly worldId: WorldId;
    /** Runtime activity selection. */
    readonly filter: ProbeFilter;
    /** Action performed for each match. */
    readonly action: ProbeAction;
};

export const AddProbeRequest = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: AddProbeRequest): void {
        encodeAddProbeRequest(writer, value);
    },

    /** Decode one AddProbeRequest. */
    decode(reader: BinaryReader): AddProbeRequest {
        return decodeAddProbeRequest(reader);
    },

    /** Return this value as JSON. */
    toJson(value: AddProbeRequest): Json {
        return toJsonAddProbeRequest(value);
    },

    /** Return one AddProbeRequest from one JSON value. */
    fromJson(value: Json): AddProbeRequest {
        return fromJsonAddProbeRequest(value);
    },
};

/** Encode one AddProbeRequest. */
export function encodeAddProbeRequest(writer: BinaryWriter, value: AddProbeRequest): void {
    encodeWorldId(writer, value.worldId);
    encodeProbeFilter(writer, value.filter);
    encodeProbeAction(writer, value.action);
}

/** Decode one AddProbeRequest. */
export function decodeAddProbeRequest(reader: BinaryReader): AddProbeRequest {
    const worldId = decodeWorldId(reader);
    const filter = decodeProbeFilter(reader);
    const action = decodeProbeAction(reader);

    return {
        worldId,
        filter,
        action,
    };
}

/** Return one JSON value for one AddProbeRequest. */
export function toJsonAddProbeRequest(value: AddProbeRequest): Json {
    return {
        worldId: toJsonWorldId(value.worldId),
        filter: toJsonProbeFilter(value.filter),
        action: toJsonProbeAction(value.action),
    };
}

/** Return one AddProbeRequest from one JSON value. */
export function fromJsonAddProbeRequest(value: Json): AddProbeRequest {
    const object = jsonObject(value);

    return {
        worldId: fromJsonWorldId(jsonField(object, "worldId")),
        filter: fromJsonProbeFilter(jsonField(object, "filter")),
        action: fromJsonProbeAction(jsonField(object, "action")),
    };
}

/** Request to add one Watchpoint. */
export type AddWatchpointRequest = {
    /** World receiving the Watchpoint. */
    readonly worldId: WorldId;
    /** Memory access selection. */
    readonly filter: MemoryFilter;
};

export const AddWatchpointRequest = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: AddWatchpointRequest): void {
        encodeAddWatchpointRequest(writer, value);
    },

    /** Decode one AddWatchpointRequest. */
    decode(reader: BinaryReader): AddWatchpointRequest {
        return decodeAddWatchpointRequest(reader);
    },

    /** Return this value as JSON. */
    toJson(value: AddWatchpointRequest): Json {
        return toJsonAddWatchpointRequest(value);
    },

    /** Return one AddWatchpointRequest from one JSON value. */
    fromJson(value: Json): AddWatchpointRequest {
        return fromJsonAddWatchpointRequest(value);
    },
};

/** Encode one AddWatchpointRequest. */
export function encodeAddWatchpointRequest(writer: BinaryWriter, value: AddWatchpointRequest): void {
    encodeWorldId(writer, value.worldId);
    encodeMemoryFilter(writer, value.filter);
}

/** Decode one AddWatchpointRequest. */
export function decodeAddWatchpointRequest(reader: BinaryReader): AddWatchpointRequest {
    const worldId = decodeWorldId(reader);
    const filter = decodeMemoryFilter(reader);

    return {
        worldId,
        filter,
    };
}

/** Return one JSON value for one AddWatchpointRequest. */
export function toJsonAddWatchpointRequest(value: AddWatchpointRequest): Json {
    return {
        worldId: toJsonWorldId(value.worldId),
        filter: toJsonMemoryFilter(value.filter),
    };
}

/** Return one AddWatchpointRequest from one JSON value. */
export function fromJsonAddWatchpointRequest(value: Json): AddWatchpointRequest {
    const object = jsonObject(value);

    return {
        worldId: fromJsonWorldId(jsonField(object, "worldId")),
        filter: fromJsonMemoryFilter(jsonField(object, "filter")),
    };
}

/** Request to evaluate one TS++ expression. */
export type EvaluateRequest = {
    /** World containing the selected context. */
    readonly worldId: WorldId;
    /** Committed Moment to inspect, or the current World when absent. */
    readonly moment?: Moment;
    /** Evaluation context. */
    readonly target: EvaluationTarget;
    /** TS++ source expression. */
    readonly expression: string;
    /** Mutation behavior. */
    readonly mode: EvaluationMode;
    /** Evaluation resource limits. */
    readonly limits: EvaluationLimits;
};

export const EvaluateRequest = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: EvaluateRequest): void {
        encodeEvaluateRequest(writer, value);
    },

    /** Decode one EvaluateRequest. */
    decode(reader: BinaryReader): EvaluateRequest {
        return decodeEvaluateRequest(reader);
    },

    /** Return this value as JSON. */
    toJson(value: EvaluateRequest): Json {
        return toJsonEvaluateRequest(value);
    },

    /** Return one EvaluateRequest from one JSON value. */
    fromJson(value: Json): EvaluateRequest {
        return fromJsonEvaluateRequest(value);
    },
};

/** Encode one EvaluateRequest. */
export function encodeEvaluateRequest(writer: BinaryWriter, value: EvaluateRequest): void {
    encodeWorldId(writer, value.worldId);
    writer.writeOption(value.moment, (value1) => {
        encodeMoment(writer, value1);
    });
    encodeEvaluationTarget(writer, value.target);
    writer.writeString(value.expression);
    encodeEvaluationMode(writer, value.mode);
    encodeEvaluationLimits(writer, value.limits);
}

/** Decode one EvaluateRequest. */
export function decodeEvaluateRequest(reader: BinaryReader): EvaluateRequest {
    const worldId = decodeWorldId(reader);
    const moment = reader.readOption(() => decodeMoment(reader));
    const target = decodeEvaluationTarget(reader);
    const expression = reader.readString();
    const mode = decodeEvaluationMode(reader);
    const limits = decodeEvaluationLimits(reader);

    return {
        worldId,
        ...(moment === undefined ? {} : { moment }),
        target,
        expression,
        mode,
        limits,
    };
}

/** Return one JSON value for one EvaluateRequest. */
export function toJsonEvaluateRequest(value: EvaluateRequest): Json {
    return {
        worldId: toJsonWorldId(value.worldId),
        ...(value.moment === undefined ? {} : { moment: toJsonMoment(value.moment) }),
        target: toJsonEvaluationTarget(value.target),
        expression: value.expression,
        mode: toJsonEvaluationMode(value.mode),
        limits: toJsonEvaluationLimits(value.limits),
    };
}

/** Return one EvaluateRequest from one JSON value. */
export function fromJsonEvaluateRequest(value: Json): EvaluateRequest {
    const object = jsonObject(value);

    return {
        worldId: fromJsonWorldId(jsonField(object, "worldId")),
        moment: jsonOptional(object, "moment", (value) => fromJsonMoment(value)),
        target: fromJsonEvaluationTarget(jsonField(object, "target")),
        expression: jsonString(jsonField(object, "expression")),
        mode: fromJsonEvaluationMode(jsonField(object, "mode")),
        limits: fromJsonEvaluationLimits(jsonField(object, "limits")),
    };
}

/** Result of one hosted expression evaluation. */
export type Evaluation = {
    /** World that performed the evaluation. */
    readonly worldId: WorldId;
    /** Moment containing the evaluation outcome. */
    readonly moment: Moment;
    /** Evaluation outcome. */
    readonly outcome: EvaluationOutcome;
};

export const Evaluation = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: Evaluation): void {
        encodeEvaluation(writer, value);
    },

    /** Decode one Evaluation. */
    decode(reader: BinaryReader): Evaluation {
        return decodeEvaluation(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Evaluation): Json {
        return toJsonEvaluation(value);
    },

    /** Return one Evaluation from one JSON value. */
    fromJson(value: Json): Evaluation {
        return fromJsonEvaluation(value);
    },
};

/** Encode one Evaluation. */
export function encodeEvaluation(writer: BinaryWriter, value: Evaluation): void {
    encodeWorldId(writer, value.worldId);
    encodeMoment(writer, value.moment);
    encodeEvaluationOutcome(writer, value.outcome);
}

/** Decode one Evaluation. */
export function decodeEvaluation(reader: BinaryReader): Evaluation {
    const worldId = decodeWorldId(reader);
    const moment = decodeMoment(reader);
    const outcome = decodeEvaluationOutcome(reader);

    return {
        worldId,
        moment,
        outcome,
    };
}

/** Return one JSON value for one Evaluation. */
export function toJsonEvaluation(value: Evaluation): Json {
    return {
        worldId: toJsonWorldId(value.worldId),
        moment: toJsonMoment(value.moment),
        outcome: toJsonEvaluationOutcome(value.outcome),
    };
}

/** Return one Evaluation from one JSON value. */
export function fromJsonEvaluation(value: Json): Evaluation {
    const object = jsonObject(value);

    return {
        worldId: fromJsonWorldId(jsonField(object, "worldId")),
        moment: fromJsonMoment(jsonField(object, "moment")),
        outcome: fromJsonEvaluationOutcome(jsonField(object, "outcome")),
    };
}

/** Request to list managed heap Allocations. */
export type ListAllocationsRequest = {
    /** World to inspect. */
    readonly worldId: WorldId;
    /** Exact committed Moment to inspect. */
    readonly moment: Moment;
    /** Allocation preceding the requested stream. */
    readonly after?: AllocationId;
};

export const ListAllocationsRequest = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ListAllocationsRequest): void {
        encodeListAllocationsRequest(writer, value);
    },

    /** Decode one ListAllocationsRequest. */
    decode(reader: BinaryReader): ListAllocationsRequest {
        return decodeListAllocationsRequest(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ListAllocationsRequest): Json {
        return toJsonListAllocationsRequest(value);
    },

    /** Return one ListAllocationsRequest from one JSON value. */
    fromJson(value: Json): ListAllocationsRequest {
        return fromJsonListAllocationsRequest(value);
    },
};

/** Encode one ListAllocationsRequest. */
export function encodeListAllocationsRequest(writer: BinaryWriter, value: ListAllocationsRequest): void {
    encodeWorldId(writer, value.worldId);
    encodeMoment(writer, value.moment);
    writer.writeOption(value.after, (value2) => {
        encodeAllocationId(writer, value2);
    });
}

/** Decode one ListAllocationsRequest. */
export function decodeListAllocationsRequest(reader: BinaryReader): ListAllocationsRequest {
    const worldId = decodeWorldId(reader);
    const moment = decodeMoment(reader);
    const after = reader.readOption(() => decodeAllocationId(reader));

    return {
        worldId,
        moment,
        ...(after === undefined ? {} : { after }),
    };
}

/** Return one JSON value for one ListAllocationsRequest. */
export function toJsonListAllocationsRequest(value: ListAllocationsRequest): Json {
    return {
        worldId: toJsonWorldId(value.worldId),
        moment: toJsonMoment(value.moment),
        ...(value.after === undefined ? {} : { after: toJsonAllocationId(value.after) }),
    };
}

/** Return one ListAllocationsRequest from one JSON value. */
export function fromJsonListAllocationsRequest(value: Json): ListAllocationsRequest {
    const object = jsonObject(value);

    return {
        worldId: fromJsonWorldId(jsonField(object, "worldId")),
        moment: fromJsonMoment(jsonField(object, "moment")),
        after: jsonOptional(object, "after", (value) => fromJsonAllocationId(value)),
    };
}

/** Request to list the Breakpoints in one World. */
export type ListBreakpointsRequest = {
    /** World to inspect. */
    readonly worldId: WorldId;
    /** Committed Moment to inspect, or the current World when absent. */
    readonly moment?: Moment;
};

export const ListBreakpointsRequest = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ListBreakpointsRequest): void {
        encodeListBreakpointsRequest(writer, value);
    },

    /** Decode one ListBreakpointsRequest. */
    decode(reader: BinaryReader): ListBreakpointsRequest {
        return decodeListBreakpointsRequest(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ListBreakpointsRequest): Json {
        return toJsonListBreakpointsRequest(value);
    },

    /** Return one ListBreakpointsRequest from one JSON value. */
    fromJson(value: Json): ListBreakpointsRequest {
        return fromJsonListBreakpointsRequest(value);
    },
};

/** Encode one ListBreakpointsRequest. */
export function encodeListBreakpointsRequest(writer: BinaryWriter, value: ListBreakpointsRequest): void {
    encodeWorldId(writer, value.worldId);
    writer.writeOption(value.moment, (value1) => {
        encodeMoment(writer, value1);
    });
}

/** Decode one ListBreakpointsRequest. */
export function decodeListBreakpointsRequest(reader: BinaryReader): ListBreakpointsRequest {
    const worldId = decodeWorldId(reader);
    const moment = reader.readOption(() => decodeMoment(reader));

    return {
        worldId,
        ...(moment === undefined ? {} : { moment }),
    };
}

/** Return one JSON value for one ListBreakpointsRequest. */
export function toJsonListBreakpointsRequest(value: ListBreakpointsRequest): Json {
    return {
        worldId: toJsonWorldId(value.worldId),
        ...(value.moment === undefined ? {} : { moment: toJsonMoment(value.moment) }),
    };
}

/** Return one ListBreakpointsRequest from one JSON value. */
export function fromJsonListBreakpointsRequest(value: Json): ListBreakpointsRequest {
    const object = jsonObject(value);

    return {
        worldId: fromJsonWorldId(jsonField(object, "worldId")),
        moment: jsonOptional(object, "moment", (value) => fromJsonMoment(value)),
    };
}

/** Request to list the Probes in one World. */
export type ListProbesRequest = {
    /** World to inspect. */
    readonly worldId: WorldId;
    /** Committed Moment to inspect, or the current World when absent. */
    readonly moment?: Moment;
};

export const ListProbesRequest = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ListProbesRequest): void {
        encodeListProbesRequest(writer, value);
    },

    /** Decode one ListProbesRequest. */
    decode(reader: BinaryReader): ListProbesRequest {
        return decodeListProbesRequest(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ListProbesRequest): Json {
        return toJsonListProbesRequest(value);
    },

    /** Return one ListProbesRequest from one JSON value. */
    fromJson(value: Json): ListProbesRequest {
        return fromJsonListProbesRequest(value);
    },
};

/** Encode one ListProbesRequest. */
export function encodeListProbesRequest(writer: BinaryWriter, value: ListProbesRequest): void {
    encodeWorldId(writer, value.worldId);
    writer.writeOption(value.moment, (value1) => {
        encodeMoment(writer, value1);
    });
}

/** Decode one ListProbesRequest. */
export function decodeListProbesRequest(reader: BinaryReader): ListProbesRequest {
    const worldId = decodeWorldId(reader);
    const moment = reader.readOption(() => decodeMoment(reader));

    return {
        worldId,
        ...(moment === undefined ? {} : { moment }),
    };
}

/** Return one JSON value for one ListProbesRequest. */
export function toJsonListProbesRequest(value: ListProbesRequest): Json {
    return {
        worldId: toJsonWorldId(value.worldId),
        ...(value.moment === undefined ? {} : { moment: toJsonMoment(value.moment) }),
    };
}

/** Return one ListProbesRequest from one JSON value. */
export function fromJsonListProbesRequest(value: Json): ListProbesRequest {
    const object = jsonObject(value);

    return {
        worldId: fromJsonWorldId(jsonField(object, "worldId")),
        moment: jsonOptional(object, "moment", (value) => fromJsonMoment(value)),
    };
}

/** Request to list outgoing References from one Allocation. */
export type ListReferencesRequest = {
    /** World to inspect. */
    readonly worldId: WorldId;
    /** Exact committed Moment to inspect. */
    readonly moment: Moment;
    /** Source Allocation. */
    readonly allocationId: AllocationId;
    /** Reference byte offset preceding the requested stream. */
    readonly after?: bigint;
};

export const ListReferencesRequest = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ListReferencesRequest): void {
        encodeListReferencesRequest(writer, value);
    },

    /** Decode one ListReferencesRequest. */
    decode(reader: BinaryReader): ListReferencesRequest {
        return decodeListReferencesRequest(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ListReferencesRequest): Json {
        return toJsonListReferencesRequest(value);
    },

    /** Return one ListReferencesRequest from one JSON value. */
    fromJson(value: Json): ListReferencesRequest {
        return fromJsonListReferencesRequest(value);
    },
};

/** Encode one ListReferencesRequest. */
export function encodeListReferencesRequest(writer: BinaryWriter, value: ListReferencesRequest): void {
    encodeWorldId(writer, value.worldId);
    encodeMoment(writer, value.moment);
    encodeAllocationId(writer, value.allocationId);
    writer.writeOption(value.after, (value3) => {
        writer.writeUnsigned(value3);
    });
}

/** Decode one ListReferencesRequest. */
export function decodeListReferencesRequest(reader: BinaryReader): ListReferencesRequest {
    const worldId = decodeWorldId(reader);
    const moment = decodeMoment(reader);
    const allocationId = decodeAllocationId(reader);
    const after = reader.readOption(() => reader.readUnsigned());

    return {
        worldId,
        moment,
        allocationId,
        ...(after === undefined ? {} : { after }),
    };
}

/** Return one JSON value for one ListReferencesRequest. */
export function toJsonListReferencesRequest(value: ListReferencesRequest): Json {
    return {
        worldId: toJsonWorldId(value.worldId),
        moment: toJsonMoment(value.moment),
        allocationId: toJsonAllocationId(value.allocationId),
        ...(value.after === undefined ? {} : { after: value.after.toString() }),
    };
}

/** Return one ListReferencesRequest from one JSON value. */
export function fromJsonListReferencesRequest(value: Json): ListReferencesRequest {
    const object = jsonObject(value);

    return {
        worldId: fromJsonWorldId(jsonField(object, "worldId")),
        moment: fromJsonMoment(jsonField(object, "moment")),
        allocationId: fromJsonAllocationId(jsonField(object, "allocationId")),
        after: jsonOptional(object, "after", (value) => jsonBigint(value)),
    };
}

/** Request to list heap Roots. */
export type ListRootsRequest = {
    /** World to inspect. */
    readonly worldId: WorldId;
    /** Exact committed Moment to inspect. */
    readonly moment: Moment;
    /** Root preceding the requested stream. */
    readonly after?: RootId;
};

export const ListRootsRequest = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ListRootsRequest): void {
        encodeListRootsRequest(writer, value);
    },

    /** Decode one ListRootsRequest. */
    decode(reader: BinaryReader): ListRootsRequest {
        return decodeListRootsRequest(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ListRootsRequest): Json {
        return toJsonListRootsRequest(value);
    },

    /** Return one ListRootsRequest from one JSON value. */
    fromJson(value: Json): ListRootsRequest {
        return fromJsonListRootsRequest(value);
    },
};

/** Encode one ListRootsRequest. */
export function encodeListRootsRequest(writer: BinaryWriter, value: ListRootsRequest): void {
    encodeWorldId(writer, value.worldId);
    encodeMoment(writer, value.moment);
    writer.writeOption(value.after, (value2) => {
        encodeRootId(writer, value2);
    });
}

/** Decode one ListRootsRequest. */
export function decodeListRootsRequest(reader: BinaryReader): ListRootsRequest {
    const worldId = decodeWorldId(reader);
    const moment = decodeMoment(reader);
    const after = reader.readOption(() => decodeRootId(reader));

    return {
        worldId,
        moment,
        ...(after === undefined ? {} : { after }),
    };
}

/** Return one JSON value for one ListRootsRequest. */
export function toJsonListRootsRequest(value: ListRootsRequest): Json {
    return {
        worldId: toJsonWorldId(value.worldId),
        moment: toJsonMoment(value.moment),
        ...(value.after === undefined ? {} : { after: toJsonRootId(value.after) }),
    };
}

/** Return one ListRootsRequest from one JSON value. */
export function fromJsonListRootsRequest(value: Json): ListRootsRequest {
    const object = jsonObject(value);

    return {
        worldId: fromJsonWorldId(jsonField(object, "worldId")),
        moment: fromJsonMoment(jsonField(object, "moment")),
        after: jsonOptional(object, "after", (value) => fromJsonRootId(value)),
    };
}

/** Request to list the Watchpoints in one World. */
export type ListWatchpointsRequest = {
    /** World to inspect. */
    readonly worldId: WorldId;
    /** Committed Moment to inspect, or the current World when absent. */
    readonly moment?: Moment;
};

export const ListWatchpointsRequest = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ListWatchpointsRequest): void {
        encodeListWatchpointsRequest(writer, value);
    },

    /** Decode one ListWatchpointsRequest. */
    decode(reader: BinaryReader): ListWatchpointsRequest {
        return decodeListWatchpointsRequest(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ListWatchpointsRequest): Json {
        return toJsonListWatchpointsRequest(value);
    },

    /** Return one ListWatchpointsRequest from one JSON value. */
    fromJson(value: Json): ListWatchpointsRequest {
        return fromJsonListWatchpointsRequest(value);
    },
};

/** Encode one ListWatchpointsRequest. */
export function encodeListWatchpointsRequest(writer: BinaryWriter, value: ListWatchpointsRequest): void {
    encodeWorldId(writer, value.worldId);
    writer.writeOption(value.moment, (value1) => {
        encodeMoment(writer, value1);
    });
}

/** Decode one ListWatchpointsRequest. */
export function decodeListWatchpointsRequest(reader: BinaryReader): ListWatchpointsRequest {
    const worldId = decodeWorldId(reader);
    const moment = reader.readOption(() => decodeMoment(reader));

    return {
        worldId,
        ...(moment === undefined ? {} : { moment }),
    };
}

/** Return one JSON value for one ListWatchpointsRequest. */
export function toJsonListWatchpointsRequest(value: ListWatchpointsRequest): Json {
    return {
        worldId: toJsonWorldId(value.worldId),
        ...(value.moment === undefined ? {} : { moment: toJsonMoment(value.moment) }),
    };
}

/** Return one ListWatchpointsRequest from one JSON value. */
export function fromJsonListWatchpointsRequest(value: Json): ListWatchpointsRequest {
    const object = jsonObject(value);

    return {
        worldId: fromJsonWorldId(jsonField(object, "worldId")),
        moment: jsonOptional(object, "moment", (value) => fromJsonMoment(value)),
    };
}

/** Request to pause selected World execution. */
export type PauseRequest = {
    /** World containing the selected execution. */
    readonly worldId: WorldId;
    /** Execution to pause. */
    readonly execution: Execution;
};

export const PauseRequest = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: PauseRequest): void {
        encodePauseRequest(writer, value);
    },

    /** Decode one PauseRequest. */
    decode(reader: BinaryReader): PauseRequest {
        return decodePauseRequest(reader);
    },

    /** Return this value as JSON. */
    toJson(value: PauseRequest): Json {
        return toJsonPauseRequest(value);
    },

    /** Return one PauseRequest from one JSON value. */
    fromJson(value: Json): PauseRequest {
        return fromJsonPauseRequest(value);
    },
};

/** Encode one PauseRequest. */
export function encodePauseRequest(writer: BinaryWriter, value: PauseRequest): void {
    encodeWorldId(writer, value.worldId);
    encodeExecution(writer, value.execution);
}

/** Decode one PauseRequest. */
export function decodePauseRequest(reader: BinaryReader): PauseRequest {
    const worldId = decodeWorldId(reader);
    const execution = decodeExecution(reader);

    return {
        worldId,
        execution,
    };
}

/** Return one JSON value for one PauseRequest. */
export function toJsonPauseRequest(value: PauseRequest): Json {
    return {
        worldId: toJsonWorldId(value.worldId),
        execution: toJsonExecution(value.execution),
    };
}

/** Return one PauseRequest from one JSON value. */
export function fromJsonPauseRequest(value: Json): PauseRequest {
    const object = jsonObject(value);

    return {
        worldId: fromJsonWorldId(jsonField(object, "worldId")),
        execution: fromJsonExecution(jsonField(object, "execution")),
    };
}

/** Request to read captured Frames from one World. */
export type ReadFramesRequest = {
    /** World to inspect. */
    readonly worldId: WorldId;
    /** Committed Moment to inspect, or the current World when absent. */
    readonly moment?: Moment;
    /** Worker to inspect, or every Worker when absent. */
    readonly workerId?: WorkerId;
};

export const ReadFramesRequest = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ReadFramesRequest): void {
        encodeReadFramesRequest(writer, value);
    },

    /** Decode one ReadFramesRequest. */
    decode(reader: BinaryReader): ReadFramesRequest {
        return decodeReadFramesRequest(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ReadFramesRequest): Json {
        return toJsonReadFramesRequest(value);
    },

    /** Return one ReadFramesRequest from one JSON value. */
    fromJson(value: Json): ReadFramesRequest {
        return fromJsonReadFramesRequest(value);
    },
};

/** Encode one ReadFramesRequest. */
export function encodeReadFramesRequest(writer: BinaryWriter, value: ReadFramesRequest): void {
    encodeWorldId(writer, value.worldId);
    writer.writeOption(value.moment, (value1) => {
        encodeMoment(writer, value1);
    });
    writer.writeOption(value.workerId, (value2) => {
        encodeWorkerId(writer, value2);
    });
}

/** Decode one ReadFramesRequest. */
export function decodeReadFramesRequest(reader: BinaryReader): ReadFramesRequest {
    const worldId = decodeWorldId(reader);
    const moment = reader.readOption(() => decodeMoment(reader));
    const workerId = reader.readOption(() => decodeWorkerId(reader));

    return {
        worldId,
        ...(moment === undefined ? {} : { moment }),
        ...(workerId === undefined ? {} : { workerId }),
    };
}

/** Return one JSON value for one ReadFramesRequest. */
export function toJsonReadFramesRequest(value: ReadFramesRequest): Json {
    return {
        worldId: toJsonWorldId(value.worldId),
        ...(value.moment === undefined ? {} : { moment: toJsonMoment(value.moment) }),
        ...(value.workerId === undefined ? {} : { workerId: toJsonWorkerId(value.workerId) }),
    };
}

/** Return one ReadFramesRequest from one JSON value. */
export function fromJsonReadFramesRequest(value: Json): ReadFramesRequest {
    const object = jsonObject(value);

    return {
        worldId: fromJsonWorldId(jsonField(object, "worldId")),
        moment: jsonOptional(object, "moment", (value) => fromJsonMoment(value)),
        workerId: jsonOptional(object, "workerId", (value) => fromJsonWorkerId(value)),
    };
}

/** Request to read one World's mapped memory Regions. */
export type ReadMemoryMapRequest = {
    /** World to inspect. */
    readonly worldId: WorldId;
    /** Committed Moment to inspect, or the current World when absent. */
    readonly moment?: Moment;
};

export const ReadMemoryMapRequest = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ReadMemoryMapRequest): void {
        encodeReadMemoryMapRequest(writer, value);
    },

    /** Decode one ReadMemoryMapRequest. */
    decode(reader: BinaryReader): ReadMemoryMapRequest {
        return decodeReadMemoryMapRequest(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ReadMemoryMapRequest): Json {
        return toJsonReadMemoryMapRequest(value);
    },

    /** Return one ReadMemoryMapRequest from one JSON value. */
    fromJson(value: Json): ReadMemoryMapRequest {
        return fromJsonReadMemoryMapRequest(value);
    },
};

/** Encode one ReadMemoryMapRequest. */
export function encodeReadMemoryMapRequest(writer: BinaryWriter, value: ReadMemoryMapRequest): void {
    encodeWorldId(writer, value.worldId);
    writer.writeOption(value.moment, (value1) => {
        encodeMoment(writer, value1);
    });
}

/** Decode one ReadMemoryMapRequest. */
export function decodeReadMemoryMapRequest(reader: BinaryReader): ReadMemoryMapRequest {
    const worldId = decodeWorldId(reader);
    const moment = reader.readOption(() => decodeMoment(reader));

    return {
        worldId,
        ...(moment === undefined ? {} : { moment }),
    };
}

/** Return one JSON value for one ReadMemoryMapRequest. */
export function toJsonReadMemoryMapRequest(value: ReadMemoryMapRequest): Json {
    return {
        worldId: toJsonWorldId(value.worldId),
        ...(value.moment === undefined ? {} : { moment: toJsonMoment(value.moment) }),
    };
}

/** Return one ReadMemoryMapRequest from one JSON value. */
export function fromJsonReadMemoryMapRequest(value: Json): ReadMemoryMapRequest {
    const object = jsonObject(value);

    return {
        worldId: fromJsonWorldId(jsonField(object, "worldId")),
        moment: jsonOptional(object, "moment", (value) => fromJsonMoment(value)),
    };
}

/** Request to read one exact World memory range. */
export type ReadMemoryRequest = {
    /** World to inspect. */
    readonly worldId: WorldId;
    /** Exact committed Moment to inspect. */
    readonly moment: Moment;
    /** Exact logical memory range to read. */
    readonly range: MemoryRange;
};

export const ReadMemoryRequest = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ReadMemoryRequest): void {
        encodeReadMemoryRequest(writer, value);
    },

    /** Decode one ReadMemoryRequest. */
    decode(reader: BinaryReader): ReadMemoryRequest {
        return decodeReadMemoryRequest(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ReadMemoryRequest): Json {
        return toJsonReadMemoryRequest(value);
    },

    /** Return one ReadMemoryRequest from one JSON value. */
    fromJson(value: Json): ReadMemoryRequest {
        return fromJsonReadMemoryRequest(value);
    },
};

/** Encode one ReadMemoryRequest. */
export function encodeReadMemoryRequest(writer: BinaryWriter, value: ReadMemoryRequest): void {
    encodeWorldId(writer, value.worldId);
    encodeMoment(writer, value.moment);
    encodeMemoryRange(writer, value.range);
}

/** Decode one ReadMemoryRequest. */
export function decodeReadMemoryRequest(reader: BinaryReader): ReadMemoryRequest {
    const worldId = decodeWorldId(reader);
    const moment = decodeMoment(reader);
    const range = decodeMemoryRange(reader);

    return {
        worldId,
        moment,
        range,
    };
}

/** Return one JSON value for one ReadMemoryRequest. */
export function toJsonReadMemoryRequest(value: ReadMemoryRequest): Json {
    return {
        worldId: toJsonWorldId(value.worldId),
        moment: toJsonMoment(value.moment),
        range: toJsonMemoryRange(value.range),
    };
}

/** Return one ReadMemoryRequest from one JSON value. */
export function fromJsonReadMemoryRequest(value: Json): ReadMemoryRequest {
    const object = jsonObject(value);

    return {
        worldId: fromJsonWorldId(jsonField(object, "worldId")),
        moment: fromJsonMoment(jsonField(object, "moment")),
        range: fromJsonMemoryRange(jsonField(object, "range")),
    };
}

/** Request to remove one Breakpoint. */
export type RemoveBreakpointRequest = {
    /** World containing the Breakpoint. */
    readonly worldId: WorldId;
    /** Breakpoint to remove. */
    readonly breakpointId: BreakpointId;
};

export const RemoveBreakpointRequest = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: RemoveBreakpointRequest): void {
        encodeRemoveBreakpointRequest(writer, value);
    },

    /** Decode one RemoveBreakpointRequest. */
    decode(reader: BinaryReader): RemoveBreakpointRequest {
        return decodeRemoveBreakpointRequest(reader);
    },

    /** Return this value as JSON. */
    toJson(value: RemoveBreakpointRequest): Json {
        return toJsonRemoveBreakpointRequest(value);
    },

    /** Return one RemoveBreakpointRequest from one JSON value. */
    fromJson(value: Json): RemoveBreakpointRequest {
        return fromJsonRemoveBreakpointRequest(value);
    },
};

/** Encode one RemoveBreakpointRequest. */
export function encodeRemoveBreakpointRequest(writer: BinaryWriter, value: RemoveBreakpointRequest): void {
    encodeWorldId(writer, value.worldId);
    encodeBreakpointId(writer, value.breakpointId);
}

/** Decode one RemoveBreakpointRequest. */
export function decodeRemoveBreakpointRequest(reader: BinaryReader): RemoveBreakpointRequest {
    const worldId = decodeWorldId(reader);
    const breakpointId = decodeBreakpointId(reader);

    return {
        worldId,
        breakpointId,
    };
}

/** Return one JSON value for one RemoveBreakpointRequest. */
export function toJsonRemoveBreakpointRequest(value: RemoveBreakpointRequest): Json {
    return {
        worldId: toJsonWorldId(value.worldId),
        breakpointId: toJsonBreakpointId(value.breakpointId),
    };
}

/** Return one RemoveBreakpointRequest from one JSON value. */
export function fromJsonRemoveBreakpointRequest(value: Json): RemoveBreakpointRequest {
    const object = jsonObject(value);

    return {
        worldId: fromJsonWorldId(jsonField(object, "worldId")),
        breakpointId: fromJsonBreakpointId(jsonField(object, "breakpointId")),
    };
}

/** Request to remove one Probe. */
export type RemoveProbeRequest = {
    /** World containing the Probe. */
    readonly worldId: WorldId;
    /** Probe to remove. */
    readonly probeId: ProbeId;
};

export const RemoveProbeRequest = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: RemoveProbeRequest): void {
        encodeRemoveProbeRequest(writer, value);
    },

    /** Decode one RemoveProbeRequest. */
    decode(reader: BinaryReader): RemoveProbeRequest {
        return decodeRemoveProbeRequest(reader);
    },

    /** Return this value as JSON. */
    toJson(value: RemoveProbeRequest): Json {
        return toJsonRemoveProbeRequest(value);
    },

    /** Return one RemoveProbeRequest from one JSON value. */
    fromJson(value: Json): RemoveProbeRequest {
        return fromJsonRemoveProbeRequest(value);
    },
};

/** Encode one RemoveProbeRequest. */
export function encodeRemoveProbeRequest(writer: BinaryWriter, value: RemoveProbeRequest): void {
    encodeWorldId(writer, value.worldId);
    encodeProbeId(writer, value.probeId);
}

/** Decode one RemoveProbeRequest. */
export function decodeRemoveProbeRequest(reader: BinaryReader): RemoveProbeRequest {
    const worldId = decodeWorldId(reader);
    const probeId = decodeProbeId(reader);

    return {
        worldId,
        probeId,
    };
}

/** Return one JSON value for one RemoveProbeRequest. */
export function toJsonRemoveProbeRequest(value: RemoveProbeRequest): Json {
    return {
        worldId: toJsonWorldId(value.worldId),
        probeId: toJsonProbeId(value.probeId),
    };
}

/** Return one RemoveProbeRequest from one JSON value. */
export function fromJsonRemoveProbeRequest(value: Json): RemoveProbeRequest {
    const object = jsonObject(value);

    return {
        worldId: fromJsonWorldId(jsonField(object, "worldId")),
        probeId: fromJsonProbeId(jsonField(object, "probeId")),
    };
}

/** Request to remove one Watchpoint. */
export type RemoveWatchpointRequest = {
    /** World containing the Watchpoint. */
    readonly worldId: WorldId;
    /** Watchpoint to remove. */
    readonly watchpointId: WatchpointId;
};

export const RemoveWatchpointRequest = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: RemoveWatchpointRequest): void {
        encodeRemoveWatchpointRequest(writer, value);
    },

    /** Decode one RemoveWatchpointRequest. */
    decode(reader: BinaryReader): RemoveWatchpointRequest {
        return decodeRemoveWatchpointRequest(reader);
    },

    /** Return this value as JSON. */
    toJson(value: RemoveWatchpointRequest): Json {
        return toJsonRemoveWatchpointRequest(value);
    },

    /** Return one RemoveWatchpointRequest from one JSON value. */
    fromJson(value: Json): RemoveWatchpointRequest {
        return fromJsonRemoveWatchpointRequest(value);
    },
};

/** Encode one RemoveWatchpointRequest. */
export function encodeRemoveWatchpointRequest(writer: BinaryWriter, value: RemoveWatchpointRequest): void {
    encodeWorldId(writer, value.worldId);
    encodeWatchpointId(writer, value.watchpointId);
}

/** Decode one RemoveWatchpointRequest. */
export function decodeRemoveWatchpointRequest(reader: BinaryReader): RemoveWatchpointRequest {
    const worldId = decodeWorldId(reader);
    const watchpointId = decodeWatchpointId(reader);

    return {
        worldId,
        watchpointId,
    };
}

/** Return one JSON value for one RemoveWatchpointRequest. */
export function toJsonRemoveWatchpointRequest(value: RemoveWatchpointRequest): Json {
    return {
        worldId: toJsonWorldId(value.worldId),
        watchpointId: toJsonWatchpointId(value.watchpointId),
    };
}

/** Return one RemoveWatchpointRequest from one JSON value. */
export function fromJsonRemoveWatchpointRequest(value: Json): RemoveWatchpointRequest {
    const object = jsonObject(value);

    return {
        worldId: fromJsonWorldId(jsonField(object, "worldId")),
        watchpointId: fromJsonWatchpointId(jsonField(object, "watchpointId")),
    };
}

/** Request to resume one debugger-stopped Worker. */
export type ResumeRequest = {
    /** World containing the stopped Worker. */
    readonly worldId: WorldId;
    /** Runtime containing the stopped Worker. */
    readonly runtimeId: RuntimeId;
    /** Worker to resume. */
    readonly workerId: WorkerId;
};

export const ResumeRequest = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ResumeRequest): void {
        encodeResumeRequest(writer, value);
    },

    /** Decode one ResumeRequest. */
    decode(reader: BinaryReader): ResumeRequest {
        return decodeResumeRequest(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ResumeRequest): Json {
        return toJsonResumeRequest(value);
    },

    /** Return one ResumeRequest from one JSON value. */
    fromJson(value: Json): ResumeRequest {
        return fromJsonResumeRequest(value);
    },
};

/** Encode one ResumeRequest. */
export function encodeResumeRequest(writer: BinaryWriter, value: ResumeRequest): void {
    encodeWorldId(writer, value.worldId);
    encodeRuntimeId(writer, value.runtimeId);
    encodeWorkerId(writer, value.workerId);
}

/** Decode one ResumeRequest. */
export function decodeResumeRequest(reader: BinaryReader): ResumeRequest {
    const worldId = decodeWorldId(reader);
    const runtimeId = decodeRuntimeId(reader);
    const workerId = decodeWorkerId(reader);

    return {
        worldId,
        runtimeId,
        workerId,
    };
}

/** Return one JSON value for one ResumeRequest. */
export function toJsonResumeRequest(value: ResumeRequest): Json {
    return {
        worldId: toJsonWorldId(value.worldId),
        runtimeId: toJsonRuntimeId(value.runtimeId),
        workerId: toJsonWorkerId(value.workerId),
    };
}

/** Return one ResumeRequest from one JSON value. */
export function fromJsonResumeRequest(value: Json): ResumeRequest {
    const object = jsonObject(value);

    return {
        worldId: fromJsonWorldId(jsonField(object, "worldId")),
        runtimeId: fromJsonRuntimeId(jsonField(object, "runtimeId")),
        workerId: fromJsonWorkerId(jsonField(object, "workerId")),
    };
}

/** Request to step one debugger-stopped Frame. */
export type StepRequest = {
    /** World containing the stopped Frame. */
    readonly worldId: WorldId;
    /** Frame to step. */
    readonly frameId: FrameId;
    /** Step granularity. */
    readonly step: Step;
};

export const StepRequest = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: StepRequest): void {
        encodeStepRequest(writer, value);
    },

    /** Decode one StepRequest. */
    decode(reader: BinaryReader): StepRequest {
        return decodeStepRequest(reader);
    },

    /** Return this value as JSON. */
    toJson(value: StepRequest): Json {
        return toJsonStepRequest(value);
    },

    /** Return one StepRequest from one JSON value. */
    fromJson(value: Json): StepRequest {
        return fromJsonStepRequest(value);
    },
};

/** Encode one StepRequest. */
export function encodeStepRequest(writer: BinaryWriter, value: StepRequest): void {
    encodeWorldId(writer, value.worldId);
    encodeFrameId(writer, value.frameId);
    encodeStep(writer, value.step);
}

/** Decode one StepRequest. */
export function decodeStepRequest(reader: BinaryReader): StepRequest {
    const worldId = decodeWorldId(reader);
    const frameId = decodeFrameId(reader);
    const step = decodeStep(reader);

    return {
        worldId,
        frameId,
        step,
    };
}

/** Return one JSON value for one StepRequest. */
export function toJsonStepRequest(value: StepRequest): Json {
    return {
        worldId: toJsonWorldId(value.worldId),
        frameId: toJsonFrameId(value.frameId),
        step: toJsonStep(value.step),
    };
}

/** Return one StepRequest from one JSON value. */
export function fromJsonStepRequest(value: Json): StepRequest {
    const object = jsonObject(value);

    return {
        worldId: fromJsonWorldId(jsonField(object, "worldId")),
        frameId: fromJsonFrameId(jsonField(object, "frameId")),
        step: fromJsonStep(jsonField(object, "step")),
    };
}

/** Request to update one Breakpoint. */
export type UpdateBreakpointRequest = {
    /** World containing the Breakpoint. */
    readonly worldId: WorldId;
    /** Complete replacement Breakpoint. */
    readonly breakpoint: Breakpoint;
};

export const UpdateBreakpointRequest = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: UpdateBreakpointRequest): void {
        encodeUpdateBreakpointRequest(writer, value);
    },

    /** Decode one UpdateBreakpointRequest. */
    decode(reader: BinaryReader): UpdateBreakpointRequest {
        return decodeUpdateBreakpointRequest(reader);
    },

    /** Return this value as JSON. */
    toJson(value: UpdateBreakpointRequest): Json {
        return toJsonUpdateBreakpointRequest(value);
    },

    /** Return one UpdateBreakpointRequest from one JSON value. */
    fromJson(value: Json): UpdateBreakpointRequest {
        return fromJsonUpdateBreakpointRequest(value);
    },
};

/** Encode one UpdateBreakpointRequest. */
export function encodeUpdateBreakpointRequest(writer: BinaryWriter, value: UpdateBreakpointRequest): void {
    encodeWorldId(writer, value.worldId);
    encodeBreakpoint(writer, value.breakpoint);
}

/** Decode one UpdateBreakpointRequest. */
export function decodeUpdateBreakpointRequest(reader: BinaryReader): UpdateBreakpointRequest {
    const worldId = decodeWorldId(reader);
    const breakpoint = decodeBreakpoint(reader);

    return {
        worldId,
        breakpoint,
    };
}

/** Return one JSON value for one UpdateBreakpointRequest. */
export function toJsonUpdateBreakpointRequest(value: UpdateBreakpointRequest): Json {
    return {
        worldId: toJsonWorldId(value.worldId),
        breakpoint: toJsonBreakpoint(value.breakpoint),
    };
}

/** Return one UpdateBreakpointRequest from one JSON value. */
export function fromJsonUpdateBreakpointRequest(value: Json): UpdateBreakpointRequest {
    const object = jsonObject(value);

    return {
        worldId: fromJsonWorldId(jsonField(object, "worldId")),
        breakpoint: fromJsonBreakpoint(jsonField(object, "breakpoint")),
    };
}

/** Request to update one Probe. */
export type UpdateProbeRequest = {
    /** World containing the Probe. */
    readonly worldId: WorldId;
    /** Complete replacement Probe. */
    readonly probe: Probe;
};

export const UpdateProbeRequest = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: UpdateProbeRequest): void {
        encodeUpdateProbeRequest(writer, value);
    },

    /** Decode one UpdateProbeRequest. */
    decode(reader: BinaryReader): UpdateProbeRequest {
        return decodeUpdateProbeRequest(reader);
    },

    /** Return this value as JSON. */
    toJson(value: UpdateProbeRequest): Json {
        return toJsonUpdateProbeRequest(value);
    },

    /** Return one UpdateProbeRequest from one JSON value. */
    fromJson(value: Json): UpdateProbeRequest {
        return fromJsonUpdateProbeRequest(value);
    },
};

/** Encode one UpdateProbeRequest. */
export function encodeUpdateProbeRequest(writer: BinaryWriter, value: UpdateProbeRequest): void {
    encodeWorldId(writer, value.worldId);
    encodeProbe(writer, value.probe);
}

/** Decode one UpdateProbeRequest. */
export function decodeUpdateProbeRequest(reader: BinaryReader): UpdateProbeRequest {
    const worldId = decodeWorldId(reader);
    const probe = decodeProbe(reader);

    return {
        worldId,
        probe,
    };
}

/** Return one JSON value for one UpdateProbeRequest. */
export function toJsonUpdateProbeRequest(value: UpdateProbeRequest): Json {
    return {
        worldId: toJsonWorldId(value.worldId),
        probe: toJsonProbe(value.probe),
    };
}

/** Return one UpdateProbeRequest from one JSON value. */
export function fromJsonUpdateProbeRequest(value: Json): UpdateProbeRequest {
    const object = jsonObject(value);

    return {
        worldId: fromJsonWorldId(jsonField(object, "worldId")),
        probe: fromJsonProbe(jsonField(object, "probe")),
    };
}

/** Request to update one Watchpoint. */
export type UpdateWatchpointRequest = {
    /** World containing the Watchpoint. */
    readonly worldId: WorldId;
    /** Complete replacement Watchpoint. */
    readonly watchpoint: Watchpoint;
};

export const UpdateWatchpointRequest = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: UpdateWatchpointRequest): void {
        encodeUpdateWatchpointRequest(writer, value);
    },

    /** Decode one UpdateWatchpointRequest. */
    decode(reader: BinaryReader): UpdateWatchpointRequest {
        return decodeUpdateWatchpointRequest(reader);
    },

    /** Return this value as JSON. */
    toJson(value: UpdateWatchpointRequest): Json {
        return toJsonUpdateWatchpointRequest(value);
    },

    /** Return one UpdateWatchpointRequest from one JSON value. */
    fromJson(value: Json): UpdateWatchpointRequest {
        return fromJsonUpdateWatchpointRequest(value);
    },
};

/** Encode one UpdateWatchpointRequest. */
export function encodeUpdateWatchpointRequest(writer: BinaryWriter, value: UpdateWatchpointRequest): void {
    encodeWorldId(writer, value.worldId);
    encodeWatchpoint(writer, value.watchpoint);
}

/** Decode one UpdateWatchpointRequest. */
export function decodeUpdateWatchpointRequest(reader: BinaryReader): UpdateWatchpointRequest {
    const worldId = decodeWorldId(reader);
    const watchpoint = decodeWatchpoint(reader);

    return {
        worldId,
        watchpoint,
    };
}

/** Return one JSON value for one UpdateWatchpointRequest. */
export function toJsonUpdateWatchpointRequest(value: UpdateWatchpointRequest): Json {
    return {
        worldId: toJsonWorldId(value.worldId),
        watchpoint: toJsonWatchpoint(value.watchpoint),
    };
}

/** Return one UpdateWatchpointRequest from one JSON value. */
export function fromJsonUpdateWatchpointRequest(value: Json): UpdateWatchpointRequest {
    const object = jsonObject(value);

    return {
        worldId: fromJsonWorldId(jsonField(object, "worldId")),
        watchpoint: fromJsonWatchpoint(jsonField(object, "watchpoint")),
    };
}
