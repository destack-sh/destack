/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, jsonBigint } from "../../protocol/serde.js";

/** Stable identifier for one Runtime in one World. */
export type RuntimeId = bigint;

export const RuntimeId = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: RuntimeId): void {
        encodeRuntimeId(writer, value);
    },

    /** Decode one RuntimeId. */
    decode(reader: BinaryReader): RuntimeId {
        return decodeRuntimeId(reader);
    },

    /** Return this value as JSON. */
    toJson(value: RuntimeId): Json {
        return toJsonRuntimeId(value);
    },

    /** Return one RuntimeId from one JSON value. */
    fromJson(value: Json): RuntimeId {
        return fromJsonRuntimeId(value);
    },
};

/** Encode one RuntimeId. */
export function encodeRuntimeId(writer: BinaryWriter, value: RuntimeId): void {
    writer.writeUnsigned(value);
}

/** Decode one RuntimeId. */
export function decodeRuntimeId(reader: BinaryReader): RuntimeId {
    return reader.readUnsigned();
}

/** Return one JSON value for one RuntimeId. */
export function toJsonRuntimeId(value: RuntimeId): Json {
    return value.toString();
}

/** Return one RuntimeId from one JSON value. */
export function fromJsonRuntimeId(value: Json): RuntimeId {
    return jsonBigint(value);
}
