/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, bytesFromJson, bytesToJson, jsonField, jsonObject, jsonOptional } from "../protocol/serde.js";
import type { TraceTable } from "./heap/core/trace.js";
import type { TargetLayout } from "./mir/table/target.js";
import type { BindingTable } from "./program/binding.js";
import type { DispatchTable } from "./program/dispatch.js";
import type { DropTable } from "./program/drop.js";
import type { FrameTable } from "./program/frame.js";
import type { FunctionTable } from "./program/function.js";
import type { ProgramInfo } from "./program/info.js";
import type { LayoutTable } from "./program/layout.js";
import type { GlobalTable } from "./program/memory/region.js";
import type { StaticImage } from "./program/memory/space.js";
import type { SiteTable } from "./program/site.js";
import type { StringTable } from "./program/string.js";
import type { TypeTable } from "./program/type.js";
import * as bytecodeTreeCode from "./bytecode/tree/code.js";
import * as nativeCodeCode from "./native/code/code.js";
import * as webassemblyCodeCode from "./webassembly/code/code.js";
import { decodeTraceTable, encodeTraceTable, fromJsonTraceTable, toJsonTraceTable } from "./heap/core/trace.js";
import { decodeTargetLayout, encodeTargetLayout, fromJsonTargetLayout, toJsonTargetLayout } from "./mir/table/target.js";
import { decodeBindingTable, encodeBindingTable, fromJsonBindingTable, toJsonBindingTable } from "./program/binding.js";
import { decodeDispatchTable, encodeDispatchTable, fromJsonDispatchTable, toJsonDispatchTable } from "./program/dispatch.js";
import { decodeDropTable, encodeDropTable, fromJsonDropTable, toJsonDropTable } from "./program/drop.js";
import { decodeFrameTable, encodeFrameTable, fromJsonFrameTable, toJsonFrameTable } from "./program/frame.js";
import { decodeFunctionTable, encodeFunctionTable, fromJsonFunctionTable, toJsonFunctionTable } from "./program/function.js";
import { decodeProgramInfo, encodeProgramInfo, fromJsonProgramInfo, toJsonProgramInfo } from "./program/info.js";
import { decodeLayoutTable, encodeLayoutTable, fromJsonLayoutTable, toJsonLayoutTable } from "./program/layout.js";
import { decodeGlobalTable, encodeGlobalTable, fromJsonGlobalTable, toJsonGlobalTable } from "./program/memory/region.js";
import { decodeStaticImage, encodeStaticImage, fromJsonStaticImage, toJsonStaticImage } from "./program/memory/space.js";
import { decodeSiteTable, encodeSiteTable, fromJsonSiteTable, toJsonSiteTable } from "./program/site.js";
import { decodeStringTable, encodeStringTable, fromJsonStringTable, toJsonStringTable } from "./program/string.js";
import { decodeTypeTable, encodeTypeTable, fromJsonTypeTable, toJsonTypeTable } from "./program/type.js";

/** Linked program. */
export type Program = {
    /** Target ABI layout used by program layouts and pointer-sized integer types. */
    readonly targetLayout: TargetLayout;
    /** Program string table. */
    readonly strings: StringTable;
    /** Runtime type table. */
    readonly types: TypeTable;
    /** Destructors keyed by drop id. */
    readonly drops: DropTable;
    /** Runtime layouts keyed by layout id. */
    readonly layouts: LayoutTable;
    /** Canonical frame states and layouts. */
    readonly frames: FrameTable;
    /** Program function table. */
    readonly functions: FunctionTable;
    /** Runtime binding declarations. */
    readonly bindings: BindingTable;
    /** Runtime dispatch table. */
    readonly dispatch: DispatchTable;
    /** Program sites used by debugging, probes, and observations. */
    readonly sites: SiteTable;
    /** Canonical trace table used by heap tables. */
    readonly traces: TraceTable;
    /** Program globals keyed by dense global id. */
    readonly globals: GlobalTable;
    /** Optional source reflection table. */
    readonly info?: ProgramInfo;
    /** Immutable constant storage owned by this program. */
    readonly constants: StaticImage;
    /** Initial shared static storage for each runtime. */
    readonly sharedStatics: StaticImage;
    /** Initial local static storage for each worker. */
    readonly localStatics: StaticImage;
    /** Bytecode used for interpretation and deoptimization when included. */
    readonly bytecode: bytecodeTreeCode.Code;
    /** Native code when generated for this program. */
    readonly native?: nativeCodeCode.Code;
    /** WebAssembly code when generated for this program. */
    readonly wasm?: webassemblyCodeCode.Code;
    /** Program section storage. */
    readonly storage: Uint8Array | readonly number[];
};

export const Program = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: Program): void {
        encodeProgram(writer, value);
    },

    /** Decode one Program. */
    decode(reader: BinaryReader): Program {
        return decodeProgram(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Program): Json {
        return toJsonProgram(value);
    },

    /** Return one Program from one JSON value. */
    fromJson(value: Json): Program {
        return fromJsonProgram(value);
    },
};

/** Encode one Program. */
export function encodeProgram(writer: BinaryWriter, value: Program): void {
    encodeTargetLayout(writer, value.targetLayout);
    encodeStringTable(writer, value.strings);
    encodeTypeTable(writer, value.types);
    encodeDropTable(writer, value.drops);
    encodeLayoutTable(writer, value.layouts);
    encodeFrameTable(writer, value.frames);
    encodeFunctionTable(writer, value.functions);
    encodeBindingTable(writer, value.bindings);
    encodeDispatchTable(writer, value.dispatch);
    encodeSiteTable(writer, value.sites);
    encodeTraceTable(writer, value.traces);
    encodeGlobalTable(writer, value.globals);
    writer.writeOption(value.info, (value12) => {
        encodeProgramInfo(writer, value12);
    });
    encodeStaticImage(writer, value.constants);
    encodeStaticImage(writer, value.sharedStatics);
    encodeStaticImage(writer, value.localStatics);
    bytecodeTreeCode.encodeCode(writer, value.bytecode);
    writer.writeOption(value.native, (value17) => {
        nativeCodeCode.encodeCode(writer, value17);
    });
    writer.writeOption(value.wasm, (value18) => {
        webassemblyCodeCode.encodeCode(writer, value18);
    });
    writer.writeByteSlice(value.storage);
}

/** Decode one Program. */
export function decodeProgram(reader: BinaryReader): Program {
    const targetLayout = decodeTargetLayout(reader);
    const strings = decodeStringTable(reader);
    const types = decodeTypeTable(reader);
    const drops = decodeDropTable(reader);
    const layouts = decodeLayoutTable(reader);
    const frames = decodeFrameTable(reader);
    const functions = decodeFunctionTable(reader);
    const bindings = decodeBindingTable(reader);
    const dispatch = decodeDispatchTable(reader);
    const sites = decodeSiteTable(reader);
    const traces = decodeTraceTable(reader);
    const globals = decodeGlobalTable(reader);
    const info = reader.readOption(() => decodeProgramInfo(reader));
    const constants = decodeStaticImage(reader);
    const sharedStatics = decodeStaticImage(reader);
    const localStatics = decodeStaticImage(reader);
    const bytecode = bytecodeTreeCode.decodeCode(reader);
    const native = reader.readOption(() => nativeCodeCode.decodeCode(reader));
    const wasm = reader.readOption(() => webassemblyCodeCode.decodeCode(reader));
    const storage = reader.readByteSlice();

    return {
        targetLayout,
        strings,
        types,
        drops,
        layouts,
        frames,
        functions,
        bindings,
        dispatch,
        sites,
        traces,
        globals,
        ...(info === undefined ? {} : { info }),
        constants,
        sharedStatics,
        localStatics,
        bytecode,
        ...(native === undefined ? {} : { native }),
        ...(wasm === undefined ? {} : { wasm }),
        storage,
    };
}

/** Return one JSON value for one Program. */
export function toJsonProgram(value: Program): Json {
    return {
        targetLayout: toJsonTargetLayout(value.targetLayout),
        strings: toJsonStringTable(value.strings),
        types: toJsonTypeTable(value.types),
        drops: toJsonDropTable(value.drops),
        layouts: toJsonLayoutTable(value.layouts),
        frames: toJsonFrameTable(value.frames),
        functions: toJsonFunctionTable(value.functions),
        bindings: toJsonBindingTable(value.bindings),
        dispatch: toJsonDispatchTable(value.dispatch),
        sites: toJsonSiteTable(value.sites),
        traces: toJsonTraceTable(value.traces),
        globals: toJsonGlobalTable(value.globals),
        ...(value.info === undefined ? {} : { info: toJsonProgramInfo(value.info) }),
        constants: toJsonStaticImage(value.constants),
        sharedStatics: toJsonStaticImage(value.sharedStatics),
        localStatics: toJsonStaticImage(value.localStatics),
        bytecode: bytecodeTreeCode.toJsonCode(value.bytecode),
        ...(value.native === undefined ? {} : { native: nativeCodeCode.toJsonCode(value.native) }),
        ...(value.wasm === undefined ? {} : { wasm: webassemblyCodeCode.toJsonCode(value.wasm) }),
        storage: bytesToJson(value.storage),
    };
}

/** Return one Program from one JSON value. */
export function fromJsonProgram(value: Json): Program {
    const object = jsonObject(value);

    return {
        targetLayout: fromJsonTargetLayout(jsonField(object, "targetLayout")),
        strings: fromJsonStringTable(jsonField(object, "strings")),
        types: fromJsonTypeTable(jsonField(object, "types")),
        drops: fromJsonDropTable(jsonField(object, "drops")),
        layouts: fromJsonLayoutTable(jsonField(object, "layouts")),
        frames: fromJsonFrameTable(jsonField(object, "frames")),
        functions: fromJsonFunctionTable(jsonField(object, "functions")),
        bindings: fromJsonBindingTable(jsonField(object, "bindings")),
        dispatch: fromJsonDispatchTable(jsonField(object, "dispatch")),
        sites: fromJsonSiteTable(jsonField(object, "sites")),
        traces: fromJsonTraceTable(jsonField(object, "traces")),
        globals: fromJsonGlobalTable(jsonField(object, "globals")),
        info: jsonOptional(object, "info", (value) => fromJsonProgramInfo(value)),
        constants: fromJsonStaticImage(jsonField(object, "constants")),
        sharedStatics: fromJsonStaticImage(jsonField(object, "sharedStatics")),
        localStatics: fromJsonStaticImage(jsonField(object, "localStatics")),
        bytecode: bytecodeTreeCode.fromJsonCode(jsonField(object, "bytecode")),
        native: jsonOptional(object, "native", (value) => nativeCodeCode.fromJsonCode(value)),
        wasm: jsonOptional(object, "wasm", (value) => webassemblyCodeCode.fromJsonCode(value)),
        storage: bytesFromJson(jsonField(object, "storage")),
    };
}
