/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, jsonBigint, jsonBool, jsonField, jsonInteger, jsonObject, jsonString } from "../../../protocol/serde.js";

/** Collector advanced by one GC result. */
export type GcCollector = "local" | "shared";

export const GcCollector = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: GcCollector): void {
        encodeGcCollector(writer, value);
    },

    /** Decode one GcCollector. */
    decode(reader: BinaryReader): GcCollector {
        return decodeGcCollector(reader);
    },

    /** Return this value as JSON. */
    toJson(value: GcCollector): Json {
        return toJsonGcCollector(value);
    },

    /** Return one GcCollector from one JSON value. */
    fromJson(value: Json): GcCollector {
        return fromJsonGcCollector(value);
    },
};

/** Encode one GcCollector. */
export function encodeGcCollector(writer: BinaryWriter, value: GcCollector): void {
    switch (value) {
        case "local":
            writer.writeUnsigned(0);
            return;
        case "shared":
            writer.writeUnsigned(1);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one GcCollector. */
export function decodeGcCollector(reader: BinaryReader): GcCollector {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "local";
        case 1:
            return "shared";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one GcCollector. */
export function toJsonGcCollector(value: GcCollector): Json {
    return value;
}

/** Return one GcCollector from one JSON value. */
export function fromJsonGcCollector(value: Json): GcCollector {
    const variant = jsonString(value);

    switch (variant) {
        case "local":
            return "local";
        case "shared":
            return "shared";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** Completed garbage collection cycle. */
export type GcCycle = {
    /** Collector that completed the cycle. */
    readonly collector: GcCollector;
    /** Whether this cycle also started in the final increment. */
    readonly isStart: boolean;
    /** Final collector phase that completed the cycle. */
    readonly phase: GcPhase;
    /** Requested work budget in bytes for the final increment. */
    readonly budgetBytes: number;
    /** Actual charged work in bytes for the final increment. */
    readonly workBytes: number;
    /** Statistics for the completed cycle. */
    readonly stats: GcStats;
};

export const GcCycle = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: GcCycle): void {
        encodeGcCycle(writer, value);
    },

    /** Decode one GcCycle. */
    decode(reader: BinaryReader): GcCycle {
        return decodeGcCycle(reader);
    },

    /** Return this value as JSON. */
    toJson(value: GcCycle): Json {
        return toJsonGcCycle(value);
    },

    /** Return one GcCycle from one JSON value. */
    fromJson(value: Json): GcCycle {
        return fromJsonGcCycle(value);
    },
};

/** Encode one GcCycle. */
export function encodeGcCycle(writer: BinaryWriter, value: GcCycle): void {
    encodeGcCollector(writer, value.collector);
    writer.writeBool(value.isStart);
    encodeGcPhase(writer, value.phase);
    writer.writeUnsigned(value.budgetBytes);
    writer.writeUnsigned(value.workBytes);
    encodeGcStats(writer, value.stats);
}

/** Decode one GcCycle. */
export function decodeGcCycle(reader: BinaryReader): GcCycle {
    const collector = decodeGcCollector(reader);
    const isStart = reader.readBool();
    const phase = decodeGcPhase(reader);
    const budgetBytes = reader.readNumber();
    const workBytes = reader.readNumber();
    const stats = decodeGcStats(reader);

    return {
        collector,
        isStart,
        phase,
        budgetBytes,
        workBytes,
        stats,
    };
}

/** Return one JSON value for one GcCycle. */
export function toJsonGcCycle(value: GcCycle): Json {
    return {
        collector: toJsonGcCollector(value.collector),
        isStart: value.isStart,
        phase: toJsonGcPhase(value.phase),
        budgetBytes: value.budgetBytes,
        workBytes: value.workBytes,
        stats: toJsonGcStats(value.stats),
    };
}

/** Return one GcCycle from one JSON value. */
export function fromJsonGcCycle(value: Json): GcCycle {
    const object = jsonObject(value);

    return {
        collector: fromJsonGcCollector(jsonField(object, "collector")),
        isStart: jsonBool(jsonField(object, "isStart")),
        phase: fromJsonGcPhase(jsonField(object, "phase")),
        budgetBytes: jsonInteger(jsonField(object, "budgetBytes")),
        workBytes: jsonInteger(jsonField(object, "workBytes")),
        stats: fromJsonGcStats(jsonField(object, "stats")),
    };
}

/** Phase advanced by one GC result. */
export type GcPhase = "idle" | "publishRoots" | "scanEdges" | "mark" | "drop" | "sweep";

export const GcPhase = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: GcPhase): void {
        encodeGcPhase(writer, value);
    },

    /** Decode one GcPhase. */
    decode(reader: BinaryReader): GcPhase {
        return decodeGcPhase(reader);
    },

    /** Return this value as JSON. */
    toJson(value: GcPhase): Json {
        return toJsonGcPhase(value);
    },

    /** Return one GcPhase from one JSON value. */
    fromJson(value: Json): GcPhase {
        return fromJsonGcPhase(value);
    },
};

/** Encode one GcPhase. */
export function encodeGcPhase(writer: BinaryWriter, value: GcPhase): void {
    switch (value) {
        case "idle":
            writer.writeUnsigned(0);
            return;
        case "publishRoots":
            writer.writeUnsigned(1);
            return;
        case "scanEdges":
            writer.writeUnsigned(2);
            return;
        case "mark":
            writer.writeUnsigned(3);
            return;
        case "drop":
            writer.writeUnsigned(4);
            return;
        case "sweep":
            writer.writeUnsigned(5);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one GcPhase. */
export function decodeGcPhase(reader: BinaryReader): GcPhase {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "idle";
        case 1:
            return "publishRoots";
        case 2:
            return "scanEdges";
        case 3:
            return "mark";
        case 4:
            return "drop";
        case 5:
            return "sweep";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one GcPhase. */
export function toJsonGcPhase(value: GcPhase): Json {
    return value;
}

/** Return one GcPhase from one JSON value. */
export function fromJsonGcPhase(value: Json): GcPhase {
    const variant = jsonString(value);

    switch (variant) {
        case "idle":
            return "idle";
        case "publishRoots":
            return "publishRoots";
        case "scanEdges":
            return "scanEdges";
        case "mark":
            return "mark";
        case "drop":
            return "drop";
        case "sweep":
            return "sweep";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** Garbage collection cycle statistics. */
export type GcStats = {
    /** Number of blocks freed by the collection. */
    readonly freedAllocations: number;
    /** Number of live blocks after the collection. */
    readonly liveAllocations: number;
    /** Number of bytes freed by the collection. */
    readonly freedBytes: bigint;
    /** Number of live allocated bytes after the collection. */
    readonly allocatedBytes: bigint;
    /** Total retained heap bytes after the collection. */
    readonly retainedBytes: bigint;
};

export const GcStats = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: GcStats): void {
        encodeGcStats(writer, value);
    },

    /** Decode one GcStats. */
    decode(reader: BinaryReader): GcStats {
        return decodeGcStats(reader);
    },

    /** Return this value as JSON. */
    toJson(value: GcStats): Json {
        return toJsonGcStats(value);
    },

    /** Return one GcStats from one JSON value. */
    fromJson(value: Json): GcStats {
        return fromJsonGcStats(value);
    },
};

/** Encode one GcStats. */
export function encodeGcStats(writer: BinaryWriter, value: GcStats): void {
    writer.writeUnsigned(value.freedAllocations);
    writer.writeUnsigned(value.liveAllocations);
    writer.writeUnsigned(value.freedBytes);
    writer.writeUnsigned(value.allocatedBytes);
    writer.writeUnsigned(value.retainedBytes);
}

/** Decode one GcStats. */
export function decodeGcStats(reader: BinaryReader): GcStats {
    const freedAllocations = reader.readNumber();
    const liveAllocations = reader.readNumber();
    const freedBytes = reader.readUnsigned();
    const allocatedBytes = reader.readUnsigned();
    const retainedBytes = reader.readUnsigned();

    return {
        freedAllocations,
        liveAllocations,
        freedBytes,
        allocatedBytes,
        retainedBytes,
    };
}

/** Return one JSON value for one GcStats. */
export function toJsonGcStats(value: GcStats): Json {
    return {
        freedAllocations: value.freedAllocations,
        liveAllocations: value.liveAllocations,
        freedBytes: value.freedBytes.toString(),
        allocatedBytes: value.allocatedBytes.toString(),
        retainedBytes: value.retainedBytes.toString(),
    };
}

/** Return one GcStats from one JSON value. */
export function fromJsonGcStats(value: Json): GcStats {
    const object = jsonObject(value);

    return {
        freedAllocations: jsonInteger(jsonField(object, "freedAllocations")),
        liveAllocations: jsonInteger(jsonField(object, "liveAllocations")),
        freedBytes: jsonBigint(jsonField(object, "freedBytes")),
        allocatedBytes: jsonBigint(jsonField(object, "allocatedBytes")),
        retainedBytes: jsonBigint(jsonField(object, "retainedBytes")),
    };
}

/** Work completed by one bounded collector increment. */
export type GcStep = {
    /** Collector that performed the work. */
    readonly collector: GcCollector;
    /** Whether this step also started the cycle. */
    readonly isStart: boolean;
    /** Collector phase that performed the work. */
    readonly phase: GcPhase;
    /** Requested work budget in bytes. */
    readonly budgetBytes: number;
    /** Actual charged work in bytes. */
    readonly workBytes: number;
};

export const GcStep = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: GcStep): void {
        encodeGcStep(writer, value);
    },

    /** Decode one GcStep. */
    decode(reader: BinaryReader): GcStep {
        return decodeGcStep(reader);
    },

    /** Return this value as JSON. */
    toJson(value: GcStep): Json {
        return toJsonGcStep(value);
    },

    /** Return one GcStep from one JSON value. */
    fromJson(value: Json): GcStep {
        return fromJsonGcStep(value);
    },
};

/** Encode one GcStep. */
export function encodeGcStep(writer: BinaryWriter, value: GcStep): void {
    encodeGcCollector(writer, value.collector);
    writer.writeBool(value.isStart);
    encodeGcPhase(writer, value.phase);
    writer.writeUnsigned(value.budgetBytes);
    writer.writeUnsigned(value.workBytes);
}

/** Decode one GcStep. */
export function decodeGcStep(reader: BinaryReader): GcStep {
    const collector = decodeGcCollector(reader);
    const isStart = reader.readBool();
    const phase = decodeGcPhase(reader);
    const budgetBytes = reader.readNumber();
    const workBytes = reader.readNumber();

    return {
        collector,
        isStart,
        phase,
        budgetBytes,
        workBytes,
    };
}

/** Return one JSON value for one GcStep. */
export function toJsonGcStep(value: GcStep): Json {
    return {
        collector: toJsonGcCollector(value.collector),
        isStart: value.isStart,
        phase: toJsonGcPhase(value.phase),
        budgetBytes: value.budgetBytes,
        workBytes: value.workBytes,
    };
}

/** Return one GcStep from one JSON value. */
export function fromJsonGcStep(value: Json): GcStep {
    const object = jsonObject(value);

    return {
        collector: fromJsonGcCollector(jsonField(object, "collector")),
        isStart: jsonBool(jsonField(object, "isStart")),
        phase: fromJsonGcPhase(jsonField(object, "phase")),
        budgetBytes: jsonInteger(jsonField(object, "budgetBytes")),
        workBytes: jsonInteger(jsonField(object, "workBytes")),
    };
}
