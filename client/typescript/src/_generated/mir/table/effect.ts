/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, jsonArray, jsonBool, jsonField, jsonObject, jsonString } from "../../../protocol/serde.js";
import type { Point } from "../tree/call.js";
import type { StorageSet } from "../tree/memory.js";
import type { LocalNodeId } from "../tree/node.js";
import { decodePoint, encodePoint, fromJsonPoint, toJsonPoint } from "../tree/call.js";
import { decodeStorageSet, encodeStorageSet, fromJsonStorageSet, toJsonStorageSet } from "../tree/memory.js";
import { decodeLocalNodeId, encodeLocalNodeId, fromJsonLocalNodeId, toJsonLocalNodeId } from "../tree/node.js";

/** Access mode for a bodyless call pointer argument. */
export type ArgumentAccess = "none" | "read" | "write" | "readWrite";

export const ArgumentAccess = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ArgumentAccess): void {
        encodeArgumentAccess(writer, value);
    },

    /** Decode one ArgumentAccess. */
    decode(reader: BinaryReader): ArgumentAccess {
        return decodeArgumentAccess(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ArgumentAccess): Json {
        return toJsonArgumentAccess(value);
    },

    /** Return one ArgumentAccess from one JSON value. */
    fromJson(value: Json): ArgumentAccess {
        return fromJsonArgumentAccess(value);
    },
};

/** Encode one ArgumentAccess. */
export function encodeArgumentAccess(writer: BinaryWriter, value: ArgumentAccess): void {
    switch (value) {
        case "none":
            writer.writeUnsigned(0);
            return;
        case "read":
            writer.writeUnsigned(1);
            return;
        case "write":
            writer.writeUnsigned(2);
            return;
        case "readWrite":
            writer.writeUnsigned(3);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one ArgumentAccess. */
export function decodeArgumentAccess(reader: BinaryReader): ArgumentAccess {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "none";
        case 1:
            return "read";
        case 2:
            return "write";
        case 3:
            return "readWrite";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one ArgumentAccess. */
export function toJsonArgumentAccess(value: ArgumentAccess): Json {
    return value;
}

/** Return one ArgumentAccess from one JSON value. */
export function fromJsonArgumentAccess(value: Json): ArgumentAccess {
    const variant = jsonString(value);

    switch (variant) {
        case "none":
            return "none";
        case "read":
            return "read";
        case "write":
            return "write";
        case "readWrite":
            return "readWrite";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** Escape behavior for a bodyless call argument. */
export type ArgumentEscape = "none" | "return" | "escape";

export const ArgumentEscape = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ArgumentEscape): void {
        encodeArgumentEscape(writer, value);
    },

    /** Decode one ArgumentEscape. */
    decode(reader: BinaryReader): ArgumentEscape {
        return decodeArgumentEscape(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ArgumentEscape): Json {
        return toJsonArgumentEscape(value);
    },

    /** Return one ArgumentEscape from one JSON value. */
    fromJson(value: Json): ArgumentEscape {
        return fromJsonArgumentEscape(value);
    },
};

/** Encode one ArgumentEscape. */
export function encodeArgumentEscape(writer: BinaryWriter, value: ArgumentEscape): void {
    switch (value) {
        case "none":
            writer.writeUnsigned(0);
            return;
        case "return":
            writer.writeUnsigned(1);
            return;
        case "escape":
            writer.writeUnsigned(2);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one ArgumentEscape. */
export function decodeArgumentEscape(reader: BinaryReader): ArgumentEscape {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "none";
        case 1:
            return "return";
        case 2:
            return "escape";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one ArgumentEscape. */
export function toJsonArgumentEscape(value: ArgumentEscape): Json {
    return value;
}

/** Return one ArgumentEscape from one JSON value. */
export function fromJsonArgumentEscape(value: Json): ArgumentEscape {
    const variant = jsonString(value);

    switch (variant) {
        case "none":
            return "none";
        case "return":
            return "return";
        case "escape":
            return "escape";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** Behavior of one argument passed to a bodyless call. */
export type CallArgumentEffect = {
    /** Access mode for this argument. */
    readonly access: ArgumentAccess;
    /** Escape behavior for this argument. */
    readonly escape: ArgumentEscape;
};

export const CallArgumentEffect = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: CallArgumentEffect): void {
        encodeCallArgumentEffect(writer, value);
    },

    /** Decode one CallArgumentEffect. */
    decode(reader: BinaryReader): CallArgumentEffect {
        return decodeCallArgumentEffect(reader);
    },

    /** Return this value as JSON. */
    toJson(value: CallArgumentEffect): Json {
        return toJsonCallArgumentEffect(value);
    },

    /** Return one CallArgumentEffect from one JSON value. */
    fromJson(value: Json): CallArgumentEffect {
        return fromJsonCallArgumentEffect(value);
    },
};

/** Encode one CallArgumentEffect. */
export function encodeCallArgumentEffect(writer: BinaryWriter, value: CallArgumentEffect): void {
    encodeArgumentAccess(writer, value.access);
    encodeArgumentEscape(writer, value.escape);
}

/** Decode one CallArgumentEffect. */
export function decodeCallArgumentEffect(reader: BinaryReader): CallArgumentEffect {
    const access = decodeArgumentAccess(reader);
    const escape = decodeArgumentEscape(reader);

    return {
        access,
        escape,
    };
}

/** Return one JSON value for one CallArgumentEffect. */
export function toJsonCallArgumentEffect(value: CallArgumentEffect): Json {
    return {
        access: toJsonArgumentAccess(value.access),
        escape: toJsonArgumentEscape(value.escape),
    };
}

/** Return one CallArgumentEffect from one JSON value. */
export function fromJsonCallArgumentEffect(value: Json): CallArgumentEffect {
    const object = jsonObject(value);

    return {
        access: fromJsonArgumentAccess(jsonField(object, "access")),
        escape: fromJsonArgumentEscape(jsonField(object, "escape")),
    };
}

/** Effects for one callsite. */
export type CallEffect = {
    /** Memory touched by this call. */
    readonly memory: MemoryEffect;
    /** Behavioral effects of this call. */
    readonly behavior: FunctionBehavior;
    /** Argument memory behavior when known. */
    readonly arguments: ReadonlyArray<CallArgumentEffect>;
};

export const CallEffect = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: CallEffect): void {
        encodeCallEffect(writer, value);
    },

    /** Decode one CallEffect. */
    decode(reader: BinaryReader): CallEffect {
        return decodeCallEffect(reader);
    },

    /** Return this value as JSON. */
    toJson(value: CallEffect): Json {
        return toJsonCallEffect(value);
    },

    /** Return one CallEffect from one JSON value. */
    fromJson(value: Json): CallEffect {
        return fromJsonCallEffect(value);
    },
};

/** Encode one CallEffect. */
export function encodeCallEffect(writer: BinaryWriter, value: CallEffect): void {
    encodeMemoryEffect(writer, value.memory);
    encodeFunctionBehavior(writer, value.behavior);
    writer.writeUnsigned(value.arguments.length);
    for (const item2 of value.arguments) {
        encodeCallArgumentEffect(writer, item2);
    }
}

/** Decode one CallEffect. */
export function decodeCallEffect(reader: BinaryReader): CallEffect {
    const memory = decodeMemoryEffect(reader);
    const behavior = decodeFunctionBehavior(reader);
    const arguments_ = (() => { const length2 = reader.readNumber(); const items2: Array<CallArgumentEffect> = []; for (let index = 0; index < length2; index += 1) { items2.push(decodeCallArgumentEffect(reader)); } return items2; })();

    return {
        memory,
        behavior,
        arguments: arguments_,
    };
}

/** Return one JSON value for one CallEffect. */
export function toJsonCallEffect(value: CallEffect): Json {
    return {
        memory: toJsonMemoryEffect(value.memory),
        behavior: toJsonFunctionBehavior(value.behavior),
        arguments: value.arguments.map((item0) => toJsonCallArgumentEffect(item0)),
    };
}

/** Return one CallEffect from one JSON value. */
export function fromJsonCallEffect(value: Json): CallEffect {
    const object = jsonObject(value);

    return {
        memory: fromJsonMemoryEffect(jsonField(object, "memory")),
        behavior: fromJsonFunctionBehavior(jsonField(object, "behavior")),
        arguments: jsonArray(jsonField(object, "arguments")).map((item0) => fromJsonCallArgumentEffect(item0)),
    };
}

/** Determinism for a call or function. */
export type Determinism = "deterministic" | "nonDeterministic";

export const Determinism = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: Determinism): void {
        encodeDeterminism(writer, value);
    },

    /** Decode one Determinism. */
    decode(reader: BinaryReader): Determinism {
        return decodeDeterminism(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Determinism): Json {
        return toJsonDeterminism(value);
    },

    /** Return one Determinism from one JSON value. */
    fromJson(value: Json): Determinism {
        return fromJsonDeterminism(value);
    },
};

/** Encode one Determinism. */
export function encodeDeterminism(writer: BinaryWriter, value: Determinism): void {
    switch (value) {
        case "deterministic":
            writer.writeUnsigned(0);
            return;
        case "nonDeterministic":
            writer.writeUnsigned(1);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one Determinism. */
export function decodeDeterminism(reader: BinaryReader): Determinism {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "deterministic";
        case 1:
            return "nonDeterministic";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one Determinism. */
export function toJsonDeterminism(value: Determinism): Json {
    return value;
}

/** Return one Determinism from one JSON value. */
export function fromJsonDeterminism(value: Json): Determinism {
    const variant = jsonString(value);

    switch (variant) {
        case "deterministic":
            return "deterministic";
        case "nonDeterministic":
            return "nonDeterministic";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** Function and call effect tables for one MIR module. */
export type EffectTable = {
    /** Function effects sorted by function id. */
    readonly functions: ReadonlyArray<readonly [LocalNodeId, FunctionEffect]>;
    /** Call effects sorted by callsite. */
    readonly calls: ReadonlyArray<readonly [Point, CallEffect]>;
};

export const EffectTable = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: EffectTable): void {
        encodeEffectTable(writer, value);
    },

    /** Decode one EffectTable. */
    decode(reader: BinaryReader): EffectTable {
        return decodeEffectTable(reader);
    },

    /** Return this value as JSON. */
    toJson(value: EffectTable): Json {
        return toJsonEffectTable(value);
    },

    /** Return one EffectTable from one JSON value. */
    fromJson(value: Json): EffectTable {
        return fromJsonEffectTable(value);
    },
};

/** Encode one EffectTable. */
export function encodeEffectTable(writer: BinaryWriter, value: EffectTable): void {
    writer.writeUnsigned(value.functions.length);
    for (const item0 of value.functions) {
        encodeLocalNodeId(writer, item0[0]);
        encodeFunctionEffect(writer, item0[1]);
    }
    writer.writeUnsigned(value.calls.length);
    for (const item1 of value.calls) {
        encodePoint(writer, item1[0]);
        encodeCallEffect(writer, item1[1]);
    }
}

/** Decode one EffectTable. */
export function decodeEffectTable(reader: BinaryReader): EffectTable {
    const functions = (() => { const length0 = reader.readNumber(); const items0: Array<readonly [LocalNodeId, FunctionEffect]> = []; for (let index = 0; index < length0; index += 1) { items0.push([decodeLocalNodeId(reader), decodeFunctionEffect(reader)] as const); } return items0; })();
    const calls = (() => { const length1 = reader.readNumber(); const items1: Array<readonly [Point, CallEffect]> = []; for (let index = 0; index < length1; index += 1) { items1.push([decodePoint(reader), decodeCallEffect(reader)] as const); } return items1; })();

    return {
        functions,
        calls,
    };
}

/** Return one JSON value for one EffectTable. */
export function toJsonEffectTable(value: EffectTable): Json {
    return {
        functions: value.functions.map((item0) => [toJsonLocalNodeId(item0[0]), toJsonFunctionEffect(item0[1])]),
        calls: value.calls.map((item0) => [toJsonPoint(item0[0]), toJsonCallEffect(item0[1])]),
    };
}

/** Return one EffectTable from one JSON value. */
export function fromJsonEffectTable(value: Json): EffectTable {
    const object = jsonObject(value);

    return {
        functions: jsonArray(jsonField(object, "functions")).map((item0) => (() => { const items = jsonArray(item0); if (items.length !== 2) { throw new SerdeError(`expected JSON tuple length 2: ${items.length}`); } return [fromJsonLocalNodeId(items[0]), fromJsonFunctionEffect(items[1])] as const; })()),
        calls: jsonArray(jsonField(object, "calls")).map((item0) => (() => { const items = jsonArray(item0); if (items.length !== 2) { throw new SerdeError(`expected JSON tuple length 2: ${items.length}`); } return [fromJsonPoint(items[0]), fromJsonCallEffect(items[1])] as const; })()),
    };
}

/** Behavioral effects for calls and functions. */
export type FunctionBehavior = {
    /** Determinism for this operation. */
    readonly determinism: Determinism;
    /** Panic behavior for this operation. */
    readonly panic: PanicBehavior;
    /** Return behavior for this operation. */
    readonly returnBehavior: ReturnBehavior;
    /** Parking behavior for this operation. */
    readonly park: ParkBehavior;
    /** Whether optimization must preserve each execution of this operation. */
    readonly mustPreserveExecution: boolean;
    /** Whether this operation may allocate storage. */
    readonly allocates: boolean;
    /** Whether this operation may free storage. */
    readonly frees: boolean;
};

export const FunctionBehavior = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: FunctionBehavior): void {
        encodeFunctionBehavior(writer, value);
    },

    /** Decode one FunctionBehavior. */
    decode(reader: BinaryReader): FunctionBehavior {
        return decodeFunctionBehavior(reader);
    },

    /** Return this value as JSON. */
    toJson(value: FunctionBehavior): Json {
        return toJsonFunctionBehavior(value);
    },

    /** Return one FunctionBehavior from one JSON value. */
    fromJson(value: Json): FunctionBehavior {
        return fromJsonFunctionBehavior(value);
    },
};

/** Encode one FunctionBehavior. */
export function encodeFunctionBehavior(writer: BinaryWriter, value: FunctionBehavior): void {
    encodeDeterminism(writer, value.determinism);
    encodePanicBehavior(writer, value.panic);
    encodeReturnBehavior(writer, value.returnBehavior);
    encodeParkBehavior(writer, value.park);
    writer.writeBool(value.mustPreserveExecution);
    writer.writeBool(value.allocates);
    writer.writeBool(value.frees);
}

/** Decode one FunctionBehavior. */
export function decodeFunctionBehavior(reader: BinaryReader): FunctionBehavior {
    const determinism = decodeDeterminism(reader);
    const panic = decodePanicBehavior(reader);
    const returnBehavior = decodeReturnBehavior(reader);
    const park = decodeParkBehavior(reader);
    const mustPreserveExecution = reader.readBool();
    const allocates = reader.readBool();
    const frees = reader.readBool();

    return {
        determinism,
        panic,
        returnBehavior,
        park,
        mustPreserveExecution,
        allocates,
        frees,
    };
}

/** Return one JSON value for one FunctionBehavior. */
export function toJsonFunctionBehavior(value: FunctionBehavior): Json {
    return {
        determinism: toJsonDeterminism(value.determinism),
        panic: toJsonPanicBehavior(value.panic),
        returnBehavior: toJsonReturnBehavior(value.returnBehavior),
        park: toJsonParkBehavior(value.park),
        mustPreserveExecution: value.mustPreserveExecution,
        allocates: value.allocates,
        frees: value.frees,
    };
}

/** Return one FunctionBehavior from one JSON value. */
export function fromJsonFunctionBehavior(value: Json): FunctionBehavior {
    const object = jsonObject(value);

    return {
        determinism: fromJsonDeterminism(jsonField(object, "determinism")),
        panic: fromJsonPanicBehavior(jsonField(object, "panic")),
        returnBehavior: fromJsonReturnBehavior(jsonField(object, "returnBehavior")),
        park: fromJsonParkBehavior(jsonField(object, "park")),
        mustPreserveExecution: jsonBool(jsonField(object, "mustPreserveExecution")),
        allocates: jsonBool(jsonField(object, "allocates")),
        frees: jsonBool(jsonField(object, "frees")),
    };
}

/** Effects for one function body or declaration. */
export type FunctionEffect = {
    /** Memory touched by this function. */
    readonly memory: MemoryEffect;
    /** Behavioral effects of this function. */
    readonly behavior: FunctionBehavior;
};

export const FunctionEffect = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: FunctionEffect): void {
        encodeFunctionEffect(writer, value);
    },

    /** Decode one FunctionEffect. */
    decode(reader: BinaryReader): FunctionEffect {
        return decodeFunctionEffect(reader);
    },

    /** Return this value as JSON. */
    toJson(value: FunctionEffect): Json {
        return toJsonFunctionEffect(value);
    },

    /** Return one FunctionEffect from one JSON value. */
    fromJson(value: Json): FunctionEffect {
        return fromJsonFunctionEffect(value);
    },
};

/** Encode one FunctionEffect. */
export function encodeFunctionEffect(writer: BinaryWriter, value: FunctionEffect): void {
    encodeMemoryEffect(writer, value.memory);
    encodeFunctionBehavior(writer, value.behavior);
}

/** Decode one FunctionEffect. */
export function decodeFunctionEffect(reader: BinaryReader): FunctionEffect {
    const memory = decodeMemoryEffect(reader);
    const behavior = decodeFunctionBehavior(reader);

    return {
        memory,
        behavior,
    };
}

/** Return one JSON value for one FunctionEffect. */
export function toJsonFunctionEffect(value: FunctionEffect): Json {
    return {
        memory: toJsonMemoryEffect(value.memory),
        behavior: toJsonFunctionBehavior(value.behavior),
    };
}

/** Return one FunctionEffect from one JSON value. */
export function fromJsonFunctionEffect(value: Json): FunctionEffect {
    const object = jsonObject(value);

    return {
        memory: fromJsonMemoryEffect(jsonField(object, "memory")),
        behavior: fromJsonFunctionBehavior(jsonField(object, "behavior")),
    };
}

/** Memory access effect for a call or operation. */
export type MemoryEffect = {
    /** Storage regions this operation may read. */
    readonly read: StorageSet;
    /** Storage regions this operation may write. */
    readonly write: StorageSet;
};

export const MemoryEffect = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: MemoryEffect): void {
        encodeMemoryEffect(writer, value);
    },

    /** Decode one MemoryEffect. */
    decode(reader: BinaryReader): MemoryEffect {
        return decodeMemoryEffect(reader);
    },

    /** Return this value as JSON. */
    toJson(value: MemoryEffect): Json {
        return toJsonMemoryEffect(value);
    },

    /** Return one MemoryEffect from one JSON value. */
    fromJson(value: Json): MemoryEffect {
        return fromJsonMemoryEffect(value);
    },
};

/** Encode one MemoryEffect. */
export function encodeMemoryEffect(writer: BinaryWriter, value: MemoryEffect): void {
    encodeStorageSet(writer, value.read);
    encodeStorageSet(writer, value.write);
}

/** Decode one MemoryEffect. */
export function decodeMemoryEffect(reader: BinaryReader): MemoryEffect {
    const read = decodeStorageSet(reader);
    const write = decodeStorageSet(reader);

    return {
        read,
        write,
    };
}

/** Return one JSON value for one MemoryEffect. */
export function toJsonMemoryEffect(value: MemoryEffect): Json {
    return {
        read: toJsonStorageSet(value.read),
        write: toJsonStorageSet(value.write),
    };
}

/** Return one MemoryEffect from one JSON value. */
export function fromJsonMemoryEffect(value: Json): MemoryEffect {
    const object = jsonObject(value);

    return {
        read: fromJsonStorageSet(jsonField(object, "read")),
        write: fromJsonStorageSet(jsonField(object, "write")),
    };
}

/** Panic behavior for a call or function. */
export type PanicBehavior = "cannotPanic" | "mayPanic";

export const PanicBehavior = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: PanicBehavior): void {
        encodePanicBehavior(writer, value);
    },

    /** Decode one PanicBehavior. */
    decode(reader: BinaryReader): PanicBehavior {
        return decodePanicBehavior(reader);
    },

    /** Return this value as JSON. */
    toJson(value: PanicBehavior): Json {
        return toJsonPanicBehavior(value);
    },

    /** Return one PanicBehavior from one JSON value. */
    fromJson(value: Json): PanicBehavior {
        return fromJsonPanicBehavior(value);
    },
};

/** Encode one PanicBehavior. */
export function encodePanicBehavior(writer: BinaryWriter, value: PanicBehavior): void {
    switch (value) {
        case "cannotPanic":
            writer.writeUnsigned(0);
            return;
        case "mayPanic":
            writer.writeUnsigned(1);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one PanicBehavior. */
export function decodePanicBehavior(reader: BinaryReader): PanicBehavior {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "cannotPanic";
        case 1:
            return "mayPanic";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one PanicBehavior. */
export function toJsonPanicBehavior(value: PanicBehavior): Json {
    return value;
}

/** Return one PanicBehavior from one JSON value. */
export function fromJsonPanicBehavior(value: Json): PanicBehavior {
    const variant = jsonString(value);

    switch (variant) {
        case "cannotPanic":
            return "cannotPanic";
        case "mayPanic":
            return "mayPanic";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** Parking behavior for a call or function. */
export type ParkBehavior = "cannotPark" | "mayPark";

export const ParkBehavior = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ParkBehavior): void {
        encodeParkBehavior(writer, value);
    },

    /** Decode one ParkBehavior. */
    decode(reader: BinaryReader): ParkBehavior {
        return decodeParkBehavior(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ParkBehavior): Json {
        return toJsonParkBehavior(value);
    },

    /** Return one ParkBehavior from one JSON value. */
    fromJson(value: Json): ParkBehavior {
        return fromJsonParkBehavior(value);
    },
};

/** Encode one ParkBehavior. */
export function encodeParkBehavior(writer: BinaryWriter, value: ParkBehavior): void {
    switch (value) {
        case "cannotPark":
            writer.writeUnsigned(0);
            return;
        case "mayPark":
            writer.writeUnsigned(1);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one ParkBehavior. */
export function decodeParkBehavior(reader: BinaryReader): ParkBehavior {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "cannotPark";
        case 1:
            return "mayPark";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one ParkBehavior. */
export function toJsonParkBehavior(value: ParkBehavior): Json {
    return value;
}

/** Return one ParkBehavior from one JSON value. */
export function fromJsonParkBehavior(value: Json): ParkBehavior {
    const variant = jsonString(value);

    switch (variant) {
        case "cannotPark":
            return "cannotPark";
        case "mayPark":
            return "mayPark";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** Return behavior for a call or function. */
export type ReturnBehavior = "mayReturn" | "noReturn" | "willReturn";

export const ReturnBehavior = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ReturnBehavior): void {
        encodeReturnBehavior(writer, value);
    },

    /** Decode one ReturnBehavior. */
    decode(reader: BinaryReader): ReturnBehavior {
        return decodeReturnBehavior(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ReturnBehavior): Json {
        return toJsonReturnBehavior(value);
    },

    /** Return one ReturnBehavior from one JSON value. */
    fromJson(value: Json): ReturnBehavior {
        return fromJsonReturnBehavior(value);
    },
};

/** Encode one ReturnBehavior. */
export function encodeReturnBehavior(writer: BinaryWriter, value: ReturnBehavior): void {
    switch (value) {
        case "mayReturn":
            writer.writeUnsigned(0);
            return;
        case "noReturn":
            writer.writeUnsigned(1);
            return;
        case "willReturn":
            writer.writeUnsigned(2);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one ReturnBehavior. */
export function decodeReturnBehavior(reader: BinaryReader): ReturnBehavior {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "mayReturn";
        case 1:
            return "noReturn";
        case 2:
            return "willReturn";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one ReturnBehavior. */
export function toJsonReturnBehavior(value: ReturnBehavior): Json {
    return value;
}

/** Return one ReturnBehavior from one JSON value. */
export function fromJsonReturnBehavior(value: Json): ReturnBehavior {
    const variant = jsonString(value);

    switch (variant) {
        case "mayReturn":
            return "mayReturn";
        case "noReturn":
            return "noReturn";
        case "willReturn":
            return "willReturn";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}
