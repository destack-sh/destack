/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, compareBytes, jsonArray, jsonBigint, jsonField, jsonInteger, jsonObject, jsonString, nestedBytes } from "../../../protocol/serde.js";
import type { CallSite } from "../tree/call.js";
import type { Edge } from "../tree/edge.js";
import type { LocalNodeId } from "../tree/node.js";
import type { Value } from "../tree/value.js";
import { decodeCallSite, encodeCallSite, fromJsonCallSite, toJsonCallSite } from "../tree/call.js";
import { decodeEdge, encodeEdge, fromJsonEdge, toJsonEdge } from "../tree/edge.js";
import { decodeLocalNodeId, encodeLocalNodeId, fromJsonLocalNodeId, toJsonLocalNodeId } from "../tree/node.js";
import { decodeValue, encodeValue, fromJsonValue, toJsonValue } from "../tree/value.js";

/** Identifier for one emitted profile counter, positional within a function. */
export type CounterId = number;

export const CounterId = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: CounterId): void {
        encodeCounterId(writer, value);
    },

    /** Decode one CounterId. */
    decode(reader: BinaryReader): CounterId {
        return decodeCounterId(reader);
    },

    /** Return this value as JSON. */
    toJson(value: CounterId): Json {
        return toJsonCounterId(value);
    },

    /** Return one CounterId from one JSON value. */
    fromJson(value: Json): CounterId {
        return fromJsonCounterId(value);
    },
};

/** Encode one CounterId. */
export function encodeCounterId(writer: BinaryWriter, value: CounterId): void {
    writer.writeUnsigned(value);
}

/** Decode one CounterId. */
export function decodeCounterId(reader: BinaryReader): CounterId {
    return reader.readNumber();
}

/** Return one JSON value for one CounterId. */
export function toJsonCounterId(value: CounterId): Json {
    return value;
}

/** Return one CounterId from one JSON value. */
export function fromJsonCounterId(value: Json): CounterId {
    return jsonInteger(value);
}

/** Static profile counter table for one MIR module. */
export type ProfileTable = {
    /** Per-function profile counter tables. */
    readonly functions: ReadonlyMap<LocalNodeId, FunctionProfileTable>;
};

export const ProfileTable = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ProfileTable): void {
        encodeProfileTable(writer, value);
    },

    /** Decode one ProfileTable. */
    decode(reader: BinaryReader): ProfileTable {
        return decodeProfileTable(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ProfileTable): Json {
        return toJsonProfileTable(value);
    },

    /** Return one ProfileTable from one JSON value. */
    fromJson(value: Json): ProfileTable {
        return fromJsonProfileTable(value);
    },
};

/** Encode one ProfileTable. */
export function encodeProfileTable(writer: BinaryWriter, value: ProfileTable): void {
    const entries0 = Array.from(value.functions.entries()).map(([key0, item0]) => {
        const keyBytes = nestedBytes((writer) => {
            encodeLocalNodeId(writer, key0);
        });
        return { key0, item0, keyBytes };
    });
    entries0.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries0.length);
    for (const entry0 of entries0) {
        encodeLocalNodeId(writer, entry0.key0);
        encodeFunctionProfileTable(writer, entry0.item0);
    }
}

/** Decode one ProfileTable. */
export function decodeProfileTable(reader: BinaryReader): ProfileTable {
    const functions = (() => { const length0 = reader.readNumber(); const items0 = new Map<LocalNodeId, FunctionProfileTable>(); for (let index = 0; index < length0; index += 1) { items0.set(decodeLocalNodeId(reader), decodeFunctionProfileTable(reader)); } return items0; })();

    return {
        functions,
    };
}

/** Return one JSON value for one ProfileTable. */
export function toJsonProfileTable(value: ProfileTable): Json {
    return {
        functions: Array.from(value.functions.entries()).map(([key0, item0]) => [toJsonLocalNodeId(key0), toJsonFunctionProfileTable(item0)] as const),
    };
}

/** Return one ProfileTable from one JSON value. */
export function fromJsonProfileTable(value: Json): ProfileTable {
    const object = jsonObject(value);

    return {
        functions: new Map(jsonArray(jsonField(object, "functions")).map((entry) => { const items = jsonArray(entry); if (items.length !== 2) { throw new SerdeError(`expected JSON map entry length 2: ${items.length}`); } const key0 = items[0]; const item0 = items[1]; return [fromJsonLocalNodeId(key0), fromJsonFunctionProfileTable(item0)] as const; })),
    };
}

/** Static profile counter table for one function. */
export type FunctionProfileTable = {
    /** Control-flow hash guarding against stale profile application. */
    readonly hash: FunctionHash;
    /** Profile points indexed by counter id. */
    readonly points: ReadonlyArray<ProfilePoint>;
};

export const FunctionProfileTable = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: FunctionProfileTable): void {
        encodeFunctionProfileTable(writer, value);
    },

    /** Decode one FunctionProfileTable. */
    decode(reader: BinaryReader): FunctionProfileTable {
        return decodeFunctionProfileTable(reader);
    },

    /** Return this value as JSON. */
    toJson(value: FunctionProfileTable): Json {
        return toJsonFunctionProfileTable(value);
    },

    /** Return one FunctionProfileTable from one JSON value. */
    fromJson(value: Json): FunctionProfileTable {
        return fromJsonFunctionProfileTable(value);
    },
};

/** Encode one FunctionProfileTable. */
export function encodeFunctionProfileTable(writer: BinaryWriter, value: FunctionProfileTable): void {
    encodeFunctionHash(writer, value.hash);
    writer.writeUnsigned(value.points.length);
    for (const item1 of value.points) {
        encodeProfilePoint(writer, item1);
    }
}

/** Decode one FunctionProfileTable. */
export function decodeFunctionProfileTable(reader: BinaryReader): FunctionProfileTable {
    const hash = decodeFunctionHash(reader);
    const points = (() => { const length1 = reader.readNumber(); const items1: Array<ProfilePoint> = []; for (let index = 0; index < length1; index += 1) { items1.push(decodeProfilePoint(reader)); } return items1; })();

    return {
        hash,
        points,
    };
}

/** Return one JSON value for one FunctionProfileTable. */
export function toJsonFunctionProfileTable(value: FunctionProfileTable): Json {
    return {
        hash: toJsonFunctionHash(value.hash),
        points: value.points.map((item0) => toJsonProfilePoint(item0)),
    };
}

/** Return one FunctionProfileTable from one JSON value. */
export function fromJsonFunctionProfileTable(value: Json): FunctionProfileTable {
    const object = jsonObject(value);

    return {
        hash: fromJsonFunctionHash(jsonField(object, "hash")),
        points: jsonArray(jsonField(object, "points")).map((item0) => fromJsonProfilePoint(item0)),
    };
}

/** Structural hash of a function's profiled control flow, for stale detection. */
export type FunctionHash = bigint;

export const FunctionHash = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: FunctionHash): void {
        encodeFunctionHash(writer, value);
    },

    /** Decode one FunctionHash. */
    decode(reader: BinaryReader): FunctionHash {
        return decodeFunctionHash(reader);
    },

    /** Return this value as JSON. */
    toJson(value: FunctionHash): Json {
        return toJsonFunctionHash(value);
    },

    /** Return one FunctionHash from one JSON value. */
    fromJson(value: Json): FunctionHash {
        return fromJsonFunctionHash(value);
    },
};

/** Encode one FunctionHash. */
export function encodeFunctionHash(writer: BinaryWriter, value: FunctionHash): void {
    writer.writeUnsigned(value);
}

/** Decode one FunctionHash. */
export function decodeFunctionHash(reader: BinaryReader): FunctionHash {
    return reader.readUnsigned();
}

/** Return one JSON value for one FunctionHash. */
export function toJsonFunctionHash(value: FunctionHash): Json {
    return value.toString();
}

/** Return one FunctionHash from one JSON value. */
export function fromJsonFunctionHash(value: Json): FunctionHash {
    return jsonBigint(value);
}

/** Semantic meaning of one profile counter. */
export type ProfilePoint =
    /** Function entry execution count. */
    | {
          readonly kind: "entry";
      }
    /** Control-flow edge count. */
    | {
          readonly kind: "edge";
          readonly edge: Edge;
      }
    /** Value distribution for one SSA value. */
    | {
          readonly kind: "value";
          readonly value: Value;
      }
    /** Observed call target distribution for one callsite. */
    | {
          readonly kind: "callTarget";
          readonly call_target: CallSite;
      }
    /** Observed receiver type distribution for one callsite. */
    | {
          readonly kind: "receiverType";
          readonly receiver_type: CallSite;
      }
    /** Observed allocation behavior for one allocation instruction. */
    | {
          readonly kind: "allocation";
          readonly allocation: LocalNodeId;
      }
    /** Suspension behavior for one instruction. */
    | {
          readonly kind: "suspension";
          readonly suspension: LocalNodeId;
      }
;

export const ProfilePoint = {
    /** Function entry execution count. */
    entry(): ProfilePoint {
        return { kind: "entry" };
    },

    /** Control-flow edge count. */
    edge(edge: Edge): ProfilePoint {
        return { kind: "edge", edge };
    },

    /** Value distribution for one SSA value. */
    value(value: Value): ProfilePoint {
        return { kind: "value", value };
    },

    /** Observed call target distribution for one callsite. */
    callTarget(call_target: CallSite): ProfilePoint {
        return { kind: "callTarget", call_target };
    },

    /** Observed receiver type distribution for one callsite. */
    receiverType(receiver_type: CallSite): ProfilePoint {
        return { kind: "receiverType", receiver_type };
    },

    /** Observed allocation behavior for one allocation instruction. */
    allocation(allocation: LocalNodeId): ProfilePoint {
        return { kind: "allocation", allocation };
    },

    /** Suspension behavior for one instruction. */
    suspension(suspension: LocalNodeId): ProfilePoint {
        return { kind: "suspension", suspension };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: ProfilePoint): void {
        encodeProfilePoint(writer, value);
    },

    /** Decode one ProfilePoint. */
    decode(reader: BinaryReader): ProfilePoint {
        return decodeProfilePoint(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ProfilePoint): Json {
        return toJsonProfilePoint(value);
    },

    /** Return one ProfilePoint from one JSON value. */
    fromJson(value: Json): ProfilePoint {
        return fromJsonProfilePoint(value);
    },
};

/** Encode one ProfilePoint. */
export function encodeProfilePoint(writer: BinaryWriter, value: ProfilePoint): void {
    switch (value.kind) {
        case "entry":
            writer.writeUnsigned(0);
            return;
        case "edge":
            writer.writeUnsigned(1);
            encodeEdge(writer, value.edge);
            return;
        case "value":
            writer.writeUnsigned(2);
            encodeValue(writer, value.value);
            return;
        case "callTarget":
            writer.writeUnsigned(3);
            encodeCallSite(writer, value.call_target);
            return;
        case "receiverType":
            writer.writeUnsigned(4);
            encodeCallSite(writer, value.receiver_type);
            return;
        case "allocation":
            writer.writeUnsigned(5);
            encodeLocalNodeId(writer, value.allocation);
            return;
        case "suspension":
            writer.writeUnsigned(6);
            encodeLocalNodeId(writer, value.suspension);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one ProfilePoint. */
export function decodeProfilePoint(reader: BinaryReader): ProfilePoint {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            return { kind: "entry" };
        }
        case 1: {
            const edge = decodeEdge(reader);

            return { kind: "edge", edge };
        }
        case 2: {
            const value = decodeValue(reader);

            return { kind: "value", value };
        }
        case 3: {
            const call_target = decodeCallSite(reader);

            return { kind: "callTarget", call_target };
        }
        case 4: {
            const receiver_type = decodeCallSite(reader);

            return { kind: "receiverType", receiver_type };
        }
        case 5: {
            const allocation = decodeLocalNodeId(reader);

            return { kind: "allocation", allocation };
        }
        case 6: {
            const suspension = decodeLocalNodeId(reader);

            return { kind: "suspension", suspension };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one ProfilePoint. */
export function toJsonProfilePoint(value: ProfilePoint): Json {
    switch (value.kind) {
        case "entry":
            return {
                kind: "entry",
            };
        case "edge":
            return {
                kind: "edge",
                edge: toJsonEdge(value.edge),
            };
        case "value":
            return {
                kind: "value",
                value: toJsonValue(value.value),
            };
        case "callTarget":
            return {
                kind: "callTarget",
                call_target: toJsonCallSite(value.call_target),
            };
        case "receiverType":
            return {
                kind: "receiverType",
                receiver_type: toJsonCallSite(value.receiver_type),
            };
        case "allocation":
            return {
                kind: "allocation",
                allocation: toJsonLocalNodeId(value.allocation),
            };
        case "suspension":
            return {
                kind: "suspension",
                suspension: toJsonLocalNodeId(value.suspension),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one ProfilePoint from one JSON value. */
export function fromJsonProfilePoint(value: Json): ProfilePoint {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "entry":
            return {
                kind,
            };
        case "edge":
            return {
                kind,
                edge: fromJsonEdge(jsonField(object, "edge")),
            };
        case "value":
            return {
                kind,
                value: fromJsonValue(jsonField(object, "value")),
            };
        case "callTarget":
            return {
                kind,
                call_target: fromJsonCallSite(jsonField(object, "call_target")),
            };
        case "receiverType":
            return {
                kind,
                receiver_type: fromJsonCallSite(jsonField(object, "receiver_type")),
            };
        case "allocation":
            return {
                kind,
                allocation: fromJsonLocalNodeId(jsonField(object, "allocation")),
            };
        case "suspension":
            return {
                kind,
                suspension: fromJsonLocalNodeId(jsonField(object, "suspension")),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}
