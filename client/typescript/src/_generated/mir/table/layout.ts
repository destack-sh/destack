/* generated by destack_client_generate, do not edit */

import {
    BinaryReader,
    BinaryWriter,
    Json,
    SerdeError,
    compareBytes,
    jsonArray,
    jsonBigint,
    jsonField,
    jsonInteger,
    jsonObject,
    jsonOptional,
    jsonString,
    nestedBytes,
} from "../../../protocol/serde.js";
import type { StringId } from "../../core/string.js";
import type { TraceMap } from "./trace.js";
import type { LocalNodeId } from "../tree/node.js";
import type { FloatType } from "../tree/type.js";
import {
    decodeStringId,
    encodeStringId,
    fromJsonStringId,
    toJsonStringId,
} from "../../core/string.js";
import { decodeTraceMap, encodeTraceMap, fromJsonTraceMap, toJsonTraceMap } from "./trace.js";
import {
    decodeLocalNodeId,
    encodeLocalNodeId,
    fromJsonLocalNodeId,
    toJsonLocalNodeId,
} from "../tree/node.js";
import {
    decodeFloatType,
    encodeFloatType,
    fromJsonFloatType,
    toJsonFloatType,
} from "../tree/type.js";

/** Target-independent logical variant discriminant bits. */
export type Discriminant = {
    /** Low 64 bits. */
    readonly low: bigint;
    /** High 64 bits. */
    readonly high: bigint;
};

export const Discriminant = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: Discriminant): void {
        encodeDiscriminant(writer, value);
    },

    /** Decode one Discriminant. */
    decode(reader: BinaryReader): Discriminant {
        return decodeDiscriminant(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Discriminant): Json {
        return toJsonDiscriminant(value);
    },

    /** Return one Discriminant from one JSON value. */
    fromJson(value: Json): Discriminant {
        return fromJsonDiscriminant(value);
    },
};

/** Encode one Discriminant. */
export function encodeDiscriminant(writer: BinaryWriter, value: Discriminant): void {
    writer.writeUnsigned(value.low);
    writer.writeUnsigned(value.high);
}

/** Decode one Discriminant. */
export function decodeDiscriminant(reader: BinaryReader): Discriminant {
    const low = reader.readUnsigned();
    const high = reader.readUnsigned();

    return {
        low,
        high,
    };
}

/** Return one JSON value for one Discriminant. */
export function toJsonDiscriminant(value: Discriminant): Json {
    return {
        low: value.low.toString(),
        high: value.high.toString(),
    };
}

/** Return one Discriminant from one JSON value. */
export function fromJsonDiscriminant(value: Json): Discriminant {
    const object = jsonObject(value);

    return {
        low: jsonBigint(jsonField(object, "low")),
        high: jsonBigint(jsonField(object, "high")),
    };
}

/** Physical scalar field carrying a variant discriminant. */
export type DiscriminantField = {
    /** The byte offset from the variant base. */
    readonly offset: number;
    /** The scalar storage width in bytes. */
    readonly byteLen: number;
    /** The first discriminant bit inside the scalar. */
    readonly bitOffset: number;
    /** The discriminant width in bits. */
    readonly bitLen: number;
};

export const DiscriminantField = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: DiscriminantField): void {
        encodeDiscriminantField(writer, value);
    },

    /** Decode one DiscriminantField. */
    decode(reader: BinaryReader): DiscriminantField {
        return decodeDiscriminantField(reader);
    },

    /** Return this value as JSON. */
    toJson(value: DiscriminantField): Json {
        return toJsonDiscriminantField(value);
    },

    /** Return one DiscriminantField from one JSON value. */
    fromJson(value: Json): DiscriminantField {
        return fromJsonDiscriminantField(value);
    },
};

/** Encode one DiscriminantField. */
export function encodeDiscriminantField(writer: BinaryWriter, value: DiscriminantField): void {
    writer.writeUnsigned(value.offset);
    writer.writeByte(value.byteLen);
    writer.writeByte(value.bitOffset);
    writer.writeByte(value.bitLen);
}

/** Decode one DiscriminantField. */
export function decodeDiscriminantField(reader: BinaryReader): DiscriminantField {
    const offset = reader.readNumber();
    const byteLen = reader.readByte();
    const bitOffset = reader.readByte();
    const bitLen = reader.readByte();

    return {
        offset,
        byteLen,
        bitOffset,
        bitLen,
    };
}

/** Return one JSON value for one DiscriminantField. */
export function toJsonDiscriminantField(value: DiscriminantField): Json {
    return {
        offset: value.offset,
        byteLen: value.byteLen,
        bitOffset: value.bitOffset,
        bitLen: value.bitLen,
    };
}

/** Return one DiscriminantField from one JSON value. */
export function fromJsonDiscriminantField(value: Json): DiscriminantField {
    const object = jsonObject(value);

    return {
        offset: jsonInteger(jsonField(object, "offset")),
        byteLen: jsonInteger(jsonField(object, "byteLen")),
        bitOffset: jsonInteger(jsonField(object, "bitOffset")),
        bitLen: jsonInteger(jsonField(object, "bitLen")),
    };
}

/** Layout for inline indexed element storage. */
export type ElementLayout = {
    /** The stored element type. */
    readonly element: LocalNodeId;
    /** The byte stride between elements. */
    readonly stride: number;
    /** The fixed element count. */
    readonly count: number;
};

export const ElementLayout = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ElementLayout): void {
        encodeElementLayout(writer, value);
    },

    /** Decode one ElementLayout. */
    decode(reader: BinaryReader): ElementLayout {
        return decodeElementLayout(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ElementLayout): Json {
        return toJsonElementLayout(value);
    },

    /** Return one ElementLayout from one JSON value. */
    fromJson(value: Json): ElementLayout {
        return fromJsonElementLayout(value);
    },
};

/** Encode one ElementLayout. */
export function encodeElementLayout(writer: BinaryWriter, value: ElementLayout): void {
    encodeLocalNodeId(writer, value.element);
    writer.writeUnsigned(value.stride);
    writer.writeUnsigned(value.count);
}

/** Decode one ElementLayout. */
export function decodeElementLayout(reader: BinaryReader): ElementLayout {
    const element = decodeLocalNodeId(reader);
    const stride = reader.readNumber();
    const count = reader.readNumber();

    return {
        element,
        stride,
        count,
    };
}

/** Return one JSON value for one ElementLayout. */
export function toJsonElementLayout(value: ElementLayout): Json {
    return {
        element: toJsonLocalNodeId(value.element),
        stride: value.stride,
        count: value.count,
    };
}

/** Return one ElementLayout from one JSON value. */
export function fromJsonElementLayout(value: Json): ElementLayout {
    const object = jsonObject(value);

    return {
        element: fromJsonLocalNodeId(jsonField(object, "element")),
        stride: jsonInteger(jsonField(object, "stride")),
        count: jsonInteger(jsonField(object, "count")),
    };
}

/** Concrete memory layout for one MIR type. */
export type Layout = {
    /** The layout shape. */
    readonly shape: LayoutShape;
    /** The physical value representation. */
    readonly representation: Representation;
    /** The largest scalar field containing invalid values. */
    readonly niche?: ScalarField;
    /** Total size in bytes, including trailing padding. */
    readonly size: number;
    /** Alignment requirement in bytes. */
    readonly alignment: number;
    /** Managed-reference trace map for this layout. */
    readonly traceMap: TraceMap;
};

export const Layout = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: Layout): void {
        encodeLayout(writer, value);
    },

    /** Decode one Layout. */
    decode(reader: BinaryReader): Layout {
        return decodeLayout(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Layout): Json {
        return toJsonLayout(value);
    },

    /** Return one Layout from one JSON value. */
    fromJson(value: Json): Layout {
        return fromJsonLayout(value);
    },
};

/** Encode one Layout. */
export function encodeLayout(writer: BinaryWriter, value: Layout): void {
    encodeLayoutShape(writer, value.shape);
    encodeRepresentation(writer, value.representation);
    writer.writeOption(value.niche, (value2) => {
        encodeScalarField(writer, value2);
    });
    writer.writeUnsigned(value.size);
    writer.writeUnsigned(value.alignment);
    encodeTraceMap(writer, value.traceMap);
}

/** Decode one Layout. */
export function decodeLayout(reader: BinaryReader): Layout {
    const shape = decodeLayoutShape(reader);
    const representation = decodeRepresentation(reader);
    const niche = reader.readOption(() => decodeScalarField(reader));
    const size = reader.readNumber();
    const alignment = reader.readNumber();
    const traceMap = decodeTraceMap(reader);

    return {
        shape,
        representation,
        ...(niche === undefined ? {} : { niche }),
        size,
        alignment,
        traceMap,
    };
}

/** Return one JSON value for one Layout. */
export function toJsonLayout(value: Layout): Json {
    return {
        shape: toJsonLayoutShape(value.shape),
        representation: toJsonRepresentation(value.representation),
        ...(value.niche === undefined ? {} : { niche: toJsonScalarField(value.niche) }),
        size: value.size,
        alignment: value.alignment,
        traceMap: toJsonTraceMap(value.traceMap),
    };
}

/** Return one Layout from one JSON value. */
export function fromJsonLayout(value: Json): Layout {
    const object = jsonObject(value);

    return {
        shape: fromJsonLayoutShape(jsonField(object, "shape")),
        representation: fromJsonRepresentation(jsonField(object, "representation")),
        niche: jsonOptional(object, "niche", (value) => fromJsonScalarField(value)),
        size: jsonInteger(jsonField(object, "size")),
        alignment: jsonInteger(jsonField(object, "alignment")),
        traceMap: fromJsonTraceMap(jsonField(object, "traceMap")),
    };
}

/** Memory layout for a single field. */
export type LayoutField = {
    /** Field name for lookup and debugging. */
    readonly name?: StringId;
    /** MIR type of the field. */
    readonly ty: LocalNodeId;
    /** Byte offset from the start of the aggregate. */
    readonly offset: number;
    /** Size of the field in bytes. */
    readonly size: number;
    /** Alignment requirement of the field in bytes. */
    readonly alignment: number;
    /** Logical field index before physical layout ordering. */
    readonly sourceIndex: number;
};

export const LayoutField = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: LayoutField): void {
        encodeLayoutField(writer, value);
    },

    /** Decode one LayoutField. */
    decode(reader: BinaryReader): LayoutField {
        return decodeLayoutField(reader);
    },

    /** Return this value as JSON. */
    toJson(value: LayoutField): Json {
        return toJsonLayoutField(value);
    },

    /** Return one LayoutField from one JSON value. */
    fromJson(value: Json): LayoutField {
        return fromJsonLayoutField(value);
    },
};

/** Encode one LayoutField. */
export function encodeLayoutField(writer: BinaryWriter, value: LayoutField): void {
    writer.writeOption(value.name, (value0) => {
        encodeStringId(writer, value0);
    });
    encodeLocalNodeId(writer, value.ty);
    writer.writeUnsigned(value.offset);
    writer.writeUnsigned(value.size);
    writer.writeUnsigned(value.alignment);
    writer.writeUnsigned(value.sourceIndex);
}

/** Decode one LayoutField. */
export function decodeLayoutField(reader: BinaryReader): LayoutField {
    const name = reader.readOption(() => decodeStringId(reader));
    const ty = decodeLocalNodeId(reader);
    const offset = reader.readNumber();
    const size = reader.readNumber();
    const alignment = reader.readNumber();
    const sourceIndex = reader.readNumber();

    return {
        ...(name === undefined ? {} : { name }),
        ty,
        offset,
        size,
        alignment,
        sourceIndex,
    };
}

/** Return one JSON value for one LayoutField. */
export function toJsonLayoutField(value: LayoutField): Json {
    return {
        ...(value.name === undefined ? {} : { name: toJsonStringId(value.name) }),
        ty: toJsonLocalNodeId(value.ty),
        offset: value.offset,
        size: value.size,
        alignment: value.alignment,
        sourceIndex: value.sourceIndex,
    };
}

/** Return one LayoutField from one JSON value. */
export function fromJsonLayoutField(value: Json): LayoutField {
    const object = jsonObject(value);

    return {
        name: jsonOptional(object, "name", (value) => fromJsonStringId(value)),
        ty: fromJsonLocalNodeId(jsonField(object, "ty")),
        offset: jsonInteger(jsonField(object, "offset")),
        size: jsonInteger(jsonField(object, "size")),
        alignment: jsonInteger(jsonField(object, "alignment")),
        sourceIndex: jsonInteger(jsonField(object, "sourceIndex")),
    };
}

/** Opaque identifier for a concrete memory layout. */
export type LayoutId = number;

export const LayoutId = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: LayoutId): void {
        encodeLayoutId(writer, value);
    },

    /** Decode one LayoutId. */
    decode(reader: BinaryReader): LayoutId {
        return decodeLayoutId(reader);
    },

    /** Return this value as JSON. */
    toJson(value: LayoutId): Json {
        return toJsonLayoutId(value);
    },

    /** Return one LayoutId from one JSON value. */
    fromJson(value: Json): LayoutId {
        return fromJsonLayoutId(value);
    },
};

/** Encode one LayoutId. */
export function encodeLayoutId(writer: BinaryWriter, value: LayoutId): void {
    writer.writeUnsigned(value);
}

/** Decode one LayoutId. */
export function decodeLayoutId(reader: BinaryReader): LayoutId {
    return reader.readNumber();
}

/** Return one JSON value for one LayoutId. */
export function toJsonLayoutId(value: LayoutId): Json {
    return value;
}

/** Return one LayoutId from one JSON value. */
export function fromJsonLayoutId(value: Json): LayoutId {
    return jsonInteger(value);
}

/** Concrete memory layout shape. */
export type LayoutShape =
    /** No runtime storage. */
    | {
          readonly kind: "none";
      }
    /** Builtin scalar storage. */
    | {
          readonly kind: "scalar";
      }
    /** Struct storage. */
    | {
          readonly kind: "struct";
          readonly struct: StructLayout;
      }
    /** Tuple storage. */
    | {
          readonly kind: "tuple";
          readonly tuple: TupleLayout;
      }
    /** Slice header storage. */
    | {
          readonly kind: "slice";
      }
    /** Fixed array storage. */
    | {
          readonly kind: "array";
          readonly array: ElementLayout;
      }
    /** Vector value storage. */
    | {
          readonly kind: "vector";
          readonly vector: ElementLayout;
      }
    /** Variant value storage. */
    | {
          readonly kind: "variant";
          readonly variant: VariantLayout;
      }
    /** Object field storage with optional virtual dispatch. */
    | {
          readonly kind: "object";
          readonly object: ObjectLayout;
      }
    /** Runtime dynamic value layout. */
    | {
          readonly kind: "dynamic";
      }
    /** Runtime function value storage. */
    | {
          readonly kind: "function";
      }
    /** Transparent nominal storage. */
    | {
          readonly kind: "newtype";
          readonly newtype: NewtypeLayout;
      };

export const LayoutShape = {
    /** No runtime storage. */
    none(): LayoutShape {
        return { kind: "none" };
    },

    /** Builtin scalar storage. */
    scalar(): LayoutShape {
        return { kind: "scalar" };
    },

    /** Struct storage. */
    struct(struct: StructLayout): LayoutShape {
        return { kind: "struct", struct };
    },

    /** Tuple storage. */
    tuple(tuple: TupleLayout): LayoutShape {
        return { kind: "tuple", tuple };
    },

    /** Slice header storage. */
    slice(): LayoutShape {
        return { kind: "slice" };
    },

    /** Fixed array storage. */
    array(array: ElementLayout): LayoutShape {
        return { kind: "array", array };
    },

    /** Vector value storage. */
    vector(vector: ElementLayout): LayoutShape {
        return { kind: "vector", vector };
    },

    /** Variant value storage. */
    variant(variant: VariantLayout): LayoutShape {
        return { kind: "variant", variant };
    },

    /** Object field storage with optional virtual dispatch. */
    object(object_: ObjectLayout): LayoutShape {
        return { kind: "object", object: object_ };
    },

    /** Runtime dynamic value layout. */
    dynamic(): LayoutShape {
        return { kind: "dynamic" };
    },

    /** Runtime function value storage. */
    function(): LayoutShape {
        return { kind: "function" };
    },

    /** Transparent nominal storage. */
    newtype(newtype: NewtypeLayout): LayoutShape {
        return { kind: "newtype", newtype };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: LayoutShape): void {
        encodeLayoutShape(writer, value);
    },

    /** Decode one LayoutShape. */
    decode(reader: BinaryReader): LayoutShape {
        return decodeLayoutShape(reader);
    },

    /** Return this value as JSON. */
    toJson(value: LayoutShape): Json {
        return toJsonLayoutShape(value);
    },

    /** Return one LayoutShape from one JSON value. */
    fromJson(value: Json): LayoutShape {
        return fromJsonLayoutShape(value);
    },
};

/** Encode one LayoutShape. */
export function encodeLayoutShape(writer: BinaryWriter, value: LayoutShape): void {
    switch (value.kind) {
        case "none":
            writer.writeUnsigned(0);
            return;
        case "scalar":
            writer.writeUnsigned(1);
            return;
        case "struct":
            writer.writeUnsigned(2);
            encodeStructLayout(writer, value.struct);
            return;
        case "tuple":
            writer.writeUnsigned(3);
            encodeTupleLayout(writer, value.tuple);
            return;
        case "slice":
            writer.writeUnsigned(4);
            return;
        case "array":
            writer.writeUnsigned(5);
            encodeElementLayout(writer, value.array);
            return;
        case "vector":
            writer.writeUnsigned(6);
            encodeElementLayout(writer, value.vector);
            return;
        case "variant":
            writer.writeUnsigned(7);
            encodeVariantLayout(writer, value.variant);
            return;
        case "object":
            writer.writeUnsigned(8);
            encodeObjectLayout(writer, value.object);
            return;
        case "dynamic":
            writer.writeUnsigned(9);
            return;
        case "function":
            writer.writeUnsigned(10);
            return;
        case "newtype":
            writer.writeUnsigned(11);
            encodeNewtypeLayout(writer, value.newtype);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one LayoutShape. */
export function decodeLayoutShape(reader: BinaryReader): LayoutShape {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            return { kind: "none" };
        }
        case 1: {
            return { kind: "scalar" };
        }
        case 2: {
            const struct = decodeStructLayout(reader);

            return { kind: "struct", struct };
        }
        case 3: {
            const tuple = decodeTupleLayout(reader);

            return { kind: "tuple", tuple };
        }
        case 4: {
            return { kind: "slice" };
        }
        case 5: {
            const array = decodeElementLayout(reader);

            return { kind: "array", array };
        }
        case 6: {
            const vector = decodeElementLayout(reader);

            return { kind: "vector", vector };
        }
        case 7: {
            const variant = decodeVariantLayout(reader);

            return { kind: "variant", variant };
        }
        case 8: {
            const object_ = decodeObjectLayout(reader);

            return { kind: "object", object: object_ };
        }
        case 9: {
            return { kind: "dynamic" };
        }
        case 10: {
            return { kind: "function" };
        }
        case 11: {
            const newtype = decodeNewtypeLayout(reader);

            return { kind: "newtype", newtype };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one LayoutShape. */
export function toJsonLayoutShape(value: LayoutShape): Json {
    switch (value.kind) {
        case "none":
            return {
                kind: "none",
            };
        case "scalar":
            return {
                kind: "scalar",
            };
        case "struct":
            return {
                kind: "struct",
                struct: toJsonStructLayout(value.struct),
            };
        case "tuple":
            return {
                kind: "tuple",
                tuple: toJsonTupleLayout(value.tuple),
            };
        case "slice":
            return {
                kind: "slice",
            };
        case "array":
            return {
                kind: "array",
                array: toJsonElementLayout(value.array),
            };
        case "vector":
            return {
                kind: "vector",
                vector: toJsonElementLayout(value.vector),
            };
        case "variant":
            return {
                kind: "variant",
                variant: toJsonVariantLayout(value.variant),
            };
        case "object":
            return {
                kind: "object",
                object: toJsonObjectLayout(value.object),
            };
        case "dynamic":
            return {
                kind: "dynamic",
            };
        case "function":
            return {
                kind: "function",
            };
        case "newtype":
            return {
                kind: "newtype",
                newtype: toJsonNewtypeLayout(value.newtype),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one LayoutShape from one JSON value. */
export function fromJsonLayoutShape(value: Json): LayoutShape {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "none":
            return {
                kind,
            };
        case "scalar":
            return {
                kind,
            };
        case "struct":
            return {
                kind,
                struct: fromJsonStructLayout(jsonField(object, "struct")),
            };
        case "tuple":
            return {
                kind,
                tuple: fromJsonTupleLayout(jsonField(object, "tuple")),
            };
        case "slice":
            return {
                kind,
            };
        case "array":
            return {
                kind,
                array: fromJsonElementLayout(jsonField(object, "array")),
            };
        case "vector":
            return {
                kind,
                vector: fromJsonElementLayout(jsonField(object, "vector")),
            };
        case "variant":
            return {
                kind,
                variant: fromJsonVariantLayout(jsonField(object, "variant")),
            };
        case "object":
            return {
                kind,
                object: fromJsonObjectLayout(jsonField(object, "object")),
            };
        case "dynamic":
            return {
                kind,
            };
        case "function":
            return {
                kind,
            };
        case "newtype":
            return {
                kind,
                newtype: fromJsonNewtypeLayout(jsonField(object, "newtype")),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** Canonical layout table for one MIR module. */
export type LayoutTable = {
    /** Layout entries indexed by LayoutId. */
    readonly entries: ReadonlyArray<Layout>;
    /** Layout ids keyed by type id. */
    readonly types: ReadonlyMap<LocalNodeId, LayoutId>;
};

export const LayoutTable = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: LayoutTable): void {
        encodeLayoutTable(writer, value);
    },

    /** Decode one LayoutTable. */
    decode(reader: BinaryReader): LayoutTable {
        return decodeLayoutTable(reader);
    },

    /** Return this value as JSON. */
    toJson(value: LayoutTable): Json {
        return toJsonLayoutTable(value);
    },

    /** Return one LayoutTable from one JSON value. */
    fromJson(value: Json): LayoutTable {
        return fromJsonLayoutTable(value);
    },
};

/** Encode one LayoutTable. */
export function encodeLayoutTable(writer: BinaryWriter, value: LayoutTable): void {
    writer.writeUnsigned(value.entries.length);
    for (const item0 of value.entries) {
        encodeLayout(writer, item0);
    }
    const entries1 = Array.from(value.types.entries()).map(([key1, item1]) => {
        const keyBytes = nestedBytes((writer) => {
            encodeLocalNodeId(writer, key1);
        });
        return { key1, item1, keyBytes };
    });
    entries1.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries1.length);
    for (const entry1 of entries1) {
        encodeLocalNodeId(writer, entry1.key1);
        encodeLayoutId(writer, entry1.item1);
    }
}

/** Decode one LayoutTable. */
export function decodeLayoutTable(reader: BinaryReader): LayoutTable {
    const entries = (() => {
        const length0 = reader.readNumber();
        const items0: Array<Layout> = [];
        for (let index = 0; index < length0; index += 1) {
            items0.push(decodeLayout(reader));
        }
        return items0;
    })();
    const types = (() => {
        const length1 = reader.readNumber();
        const items1 = new Map<LocalNodeId, LayoutId>();
        for (let index = 0; index < length1; index += 1) {
            items1.set(decodeLocalNodeId(reader), decodeLayoutId(reader));
        }
        return items1;
    })();

    return {
        entries,
        types,
    };
}

/** Return one JSON value for one LayoutTable. */
export function toJsonLayoutTable(value: LayoutTable): Json {
    return {
        entries: value.entries.map((item0) => toJsonLayout(item0)),
        types: Array.from(value.types.entries()).map(
            ([key0, item0]) => [toJsonLocalNodeId(key0), toJsonLayoutId(item0)] as const,
        ),
    };
}

/** Return one LayoutTable from one JSON value. */
export function fromJsonLayoutTable(value: Json): LayoutTable {
    const object = jsonObject(value);

    return {
        entries: jsonArray(jsonField(object, "entries")).map((item0) => fromJsonLayout(item0)),
        types: new Map(
            jsonArray(jsonField(object, "types")).map((entry) => {
                const items = jsonArray(entry);
                if (items.length !== 2) {
                    throw new SerdeError(`expected JSON map entry length 2: ${items.length}`);
                }
                const key0 = items[0];
                const item0 = items[1];
                return [fromJsonLocalNodeId(key0), fromJsonLayoutId(item0)] as const;
            }),
        ),
    };
}

/** Concrete layout for a nominal newtype. */
export type NewtypeLayout = {
    /** The backing type. */
    readonly backingType: LocalNodeId;
    /** The backing type layout. */
    readonly backingLayout: LayoutId;
};

export const NewtypeLayout = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: NewtypeLayout): void {
        encodeNewtypeLayout(writer, value);
    },

    /** Decode one NewtypeLayout. */
    decode(reader: BinaryReader): NewtypeLayout {
        return decodeNewtypeLayout(reader);
    },

    /** Return this value as JSON. */
    toJson(value: NewtypeLayout): Json {
        return toJsonNewtypeLayout(value);
    },

    /** Return one NewtypeLayout from one JSON value. */
    fromJson(value: Json): NewtypeLayout {
        return fromJsonNewtypeLayout(value);
    },
};

/** Encode one NewtypeLayout. */
export function encodeNewtypeLayout(writer: BinaryWriter, value: NewtypeLayout): void {
    encodeLocalNodeId(writer, value.backingType);
    encodeLayoutId(writer, value.backingLayout);
}

/** Decode one NewtypeLayout. */
export function decodeNewtypeLayout(reader: BinaryReader): NewtypeLayout {
    const backingType = decodeLocalNodeId(reader);
    const backingLayout = decodeLayoutId(reader);

    return {
        backingType,
        backingLayout,
    };
}

/** Return one JSON value for one NewtypeLayout. */
export function toJsonNewtypeLayout(value: NewtypeLayout): Json {
    return {
        backingType: toJsonLocalNodeId(value.backingType),
        backingLayout: toJsonLayoutId(value.backingLayout),
    };
}

/** Return one NewtypeLayout from one JSON value. */
export function fromJsonNewtypeLayout(value: Json): NewtypeLayout {
    const object = jsonObject(value);

    return {
        backingType: fromJsonLocalNodeId(jsonField(object, "backingType")),
        backingLayout: fromJsonLayoutId(jsonField(object, "backingLayout")),
    };
}

/** Concrete layout for an object. */
export type ObjectLayout = {
    /** Byte offset of the virtual table id when present. */
    readonly dispatchOffset?: number;
    /** The fields in layout order. */
    readonly fields: ReadonlyArray<LayoutField>;
};

export const ObjectLayout = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ObjectLayout): void {
        encodeObjectLayout(writer, value);
    },

    /** Decode one ObjectLayout. */
    decode(reader: BinaryReader): ObjectLayout {
        return decodeObjectLayout(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ObjectLayout): Json {
        return toJsonObjectLayout(value);
    },

    /** Return one ObjectLayout from one JSON value. */
    fromJson(value: Json): ObjectLayout {
        return fromJsonObjectLayout(value);
    },
};

/** Encode one ObjectLayout. */
export function encodeObjectLayout(writer: BinaryWriter, value: ObjectLayout): void {
    writer.writeOption(value.dispatchOffset, (value0) => {
        writer.writeUnsigned(value0);
    });
    writer.writeUnsigned(value.fields.length);
    for (const item1 of value.fields) {
        encodeLayoutField(writer, item1);
    }
}

/** Decode one ObjectLayout. */
export function decodeObjectLayout(reader: BinaryReader): ObjectLayout {
    const dispatchOffset = reader.readOption(() => reader.readNumber());
    const fields = (() => {
        const length1 = reader.readNumber();
        const items1: Array<LayoutField> = [];
        for (let index = 0; index < length1; index += 1) {
            items1.push(decodeLayoutField(reader));
        }
        return items1;
    })();

    return {
        ...(dispatchOffset === undefined ? {} : { dispatchOffset }),
        fields,
    };
}

/** Return one JSON value for one ObjectLayout. */
export function toJsonObjectLayout(value: ObjectLayout): Json {
    return {
        ...(value.dispatchOffset === undefined ? {} : { dispatchOffset: value.dispatchOffset }),
        fields: value.fields.map((item0) => toJsonLayoutField(item0)),
    };
}

/** Return one ObjectLayout from one JSON value. */
export function fromJsonObjectLayout(value: Json): ObjectLayout {
    const object = jsonObject(value);

    return {
        dispatchOffset: jsonOptional(object, "dispatchOffset", (value) => jsonInteger(value)),
        fields: jsonArray(jsonField(object, "fields")).map((item0) => fromJsonLayoutField(item0)),
    };
}

/** One scalar primitive understood by physical backends. */
export type Primitive =
    /** An integer bit string. */
    | {
          readonly kind: "integer";
          readonly width: number;
      }
    /** A floating point value. */
    | {
          readonly kind: "float";
          readonly float: FloatType;
      }
    /** A process or program pointer. */
    | {
          readonly kind: "pointer";
          readonly width: number;
      };

export const Primitive = {
    /** An integer bit string. */
    integer(width: number): Primitive {
        return { kind: "integer", width };
    },

    /** A floating point value. */
    float(float: FloatType): Primitive {
        return { kind: "float", float };
    },

    /** A process or program pointer. */
    pointer(width: number): Primitive {
        return { kind: "pointer", width };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: Primitive): void {
        encodePrimitive(writer, value);
    },

    /** Decode one Primitive. */
    decode(reader: BinaryReader): Primitive {
        return decodePrimitive(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Primitive): Json {
        return toJsonPrimitive(value);
    },

    /** Return one Primitive from one JSON value. */
    fromJson(value: Json): Primitive {
        return fromJsonPrimitive(value);
    },
};

/** Encode one Primitive. */
export function encodePrimitive(writer: BinaryWriter, value: Primitive): void {
    switch (value.kind) {
        case "integer":
            writer.writeUnsigned(0);
            writer.writeUnsigned(value.width);
            return;
        case "float":
            writer.writeUnsigned(1);
            encodeFloatType(writer, value.float);
            return;
        case "pointer":
            writer.writeUnsigned(2);
            writer.writeUnsigned(value.width);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one Primitive. */
export function decodePrimitive(reader: BinaryReader): Primitive {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const width = reader.readNumber();

            return {
                kind: "integer",
                width,
            };
        }
        case 1: {
            const float = decodeFloatType(reader);

            return { kind: "float", float };
        }
        case 2: {
            const width = reader.readNumber();

            return {
                kind: "pointer",
                width,
            };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one Primitive. */
export function toJsonPrimitive(value: Primitive): Json {
    switch (value.kind) {
        case "integer":
            return {
                kind: "integer",
                width: value.width,
            };
        case "float":
            return {
                kind: "float",
                float: toJsonFloatType(value.float),
            };
        case "pointer":
            return {
                kind: "pointer",
                width: value.width,
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one Primitive from one JSON value. */
export function fromJsonPrimitive(value: Json): Primitive {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "integer":
            return {
                kind,
                width: jsonInteger(jsonField(object, "width")),
            };
        case "float":
            return {
                kind,
                float: fromJsonFloatType(jsonField(object, "float")),
            };
        case "pointer":
            return {
                kind,
                width: jsonInteger(jsonField(object, "width")),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** Physical representation of one MIR value. */
export type Representation =
    /** One scalar register value. */
    | {
          readonly kind: "scalar";
          readonly scalar: Scalar;
      }
    /** Two scalar register values in canonical byte order. */
    | {
          readonly kind: "scalarPair";
          readonly scalar_pair: readonly [ScalarField, ScalarField];
      }
    /** One fixed vector value eligible for target legalization. */
    | {
          readonly kind: "vector";
          readonly vector: Vector;
      }
    /** Canonical bytes addressed in memory. */
    | {
          readonly kind: "memory";
      };

export const Representation = {
    /** One scalar register value. */
    scalar(scalar: Scalar): Representation {
        return { kind: "scalar", scalar };
    },

    /** Two scalar register values in canonical byte order. */
    scalarPair(scalar_pair: readonly [ScalarField, ScalarField]): Representation {
        return { kind: "scalarPair", scalar_pair };
    },

    /** One fixed vector value eligible for target legalization. */
    vector(vector: Vector): Representation {
        return { kind: "vector", vector };
    },

    /** Canonical bytes addressed in memory. */
    memory(): Representation {
        return { kind: "memory" };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: Representation): void {
        encodeRepresentation(writer, value);
    },

    /** Decode one Representation. */
    decode(reader: BinaryReader): Representation {
        return decodeRepresentation(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Representation): Json {
        return toJsonRepresentation(value);
    },

    /** Return one Representation from one JSON value. */
    fromJson(value: Json): Representation {
        return fromJsonRepresentation(value);
    },
};

/** Encode one Representation. */
export function encodeRepresentation(writer: BinaryWriter, value: Representation): void {
    switch (value.kind) {
        case "scalar":
            writer.writeUnsigned(0);
            encodeScalar(writer, value.scalar);
            return;
        case "scalarPair":
            writer.writeUnsigned(1);
            for (const item0 of value.scalar_pair) {
                encodeScalarField(writer, item0);
            }
            return;
        case "vector":
            writer.writeUnsigned(2);
            encodeVector(writer, value.vector);
            return;
        case "memory":
            writer.writeUnsigned(3);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one Representation. */
export function decodeRepresentation(reader: BinaryReader): Representation {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const scalar = decodeScalar(reader);

            return { kind: "scalar", scalar };
        }
        case 1: {
            const scalar_pair = [decodeScalarField(reader), decodeScalarField(reader)] as const;

            return { kind: "scalarPair", scalar_pair };
        }
        case 2: {
            const vector = decodeVector(reader);

            return { kind: "vector", vector };
        }
        case 3: {
            return { kind: "memory" };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one Representation. */
export function toJsonRepresentation(value: Representation): Json {
    switch (value.kind) {
        case "scalar":
            return {
                kind: "scalar",
                scalar: toJsonScalar(value.scalar),
            };
        case "scalarPair":
            return {
                kind: "scalarPair",
                scalar_pair: value.scalar_pair.map((item0) => toJsonScalarField(item0)),
            };
        case "vector":
            return {
                kind: "vector",
                vector: toJsonVector(value.vector),
            };
        case "memory":
            return {
                kind: "memory",
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one Representation from one JSON value. */
export function fromJsonRepresentation(value: Json): Representation {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "scalar":
            return {
                kind,
                scalar: fromJsonScalar(jsonField(object, "scalar")),
            };
        case "scalarPair":
            return {
                kind,
                scalar_pair: (() => {
                    const items = jsonArray(jsonField(object, "scalar_pair"));
                    if (items.length !== 2) {
                        throw new SerdeError(`expected JSON array length 2: ${items.length}`);
                    }
                    return [fromJsonScalarField(items[0]), fromJsonScalarField(items[1])] as const;
                })(),
            };
        case "vector":
            return {
                kind,
                vector: fromJsonVector(jsonField(object, "vector")),
            };
        case "memory":
            return {
                kind,
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** One scalar machine value and its valid bit range. */
export type Scalar = {
    /** The scalar machine primitive. */
    readonly primitive: Primitive;
    /** The scalar values admitted by the type. */
    readonly validity: Validity;
};

export const Scalar = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: Scalar): void {
        encodeScalar(writer, value);
    },

    /** Decode one Scalar. */
    decode(reader: BinaryReader): Scalar {
        return decodeScalar(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Scalar): Json {
        return toJsonScalar(value);
    },

    /** Return one Scalar from one JSON value. */
    fromJson(value: Json): Scalar {
        return fromJsonScalar(value);
    },
};

/** Encode one Scalar. */
export function encodeScalar(writer: BinaryWriter, value: Scalar): void {
    encodePrimitive(writer, value.primitive);
    encodeValidity(writer, value.validity);
}

/** Decode one Scalar. */
export function decodeScalar(reader: BinaryReader): Scalar {
    const primitive = decodePrimitive(reader);
    const validity = decodeValidity(reader);

    return {
        primitive,
        validity,
    };
}

/** Return one JSON value for one Scalar. */
export function toJsonScalar(value: Scalar): Json {
    return {
        primitive: toJsonPrimitive(value.primitive),
        validity: toJsonValidity(value.validity),
    };
}

/** Return one Scalar from one JSON value. */
export function fromJsonScalar(value: Json): Scalar {
    const object = jsonObject(value);

    return {
        primitive: fromJsonPrimitive(jsonField(object, "primitive")),
        validity: fromJsonValidity(jsonField(object, "validity")),
    };
}

/** One scalar field inside a pair representation. */
export type ScalarField = {
    /** The scalar field representation. */
    readonly scalar: Scalar;
    /** The canonical byte offset. */
    readonly offset: number;
};

export const ScalarField = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ScalarField): void {
        encodeScalarField(writer, value);
    },

    /** Decode one ScalarField. */
    decode(reader: BinaryReader): ScalarField {
        return decodeScalarField(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ScalarField): Json {
        return toJsonScalarField(value);
    },

    /** Return one ScalarField from one JSON value. */
    fromJson(value: Json): ScalarField {
        return fromJsonScalarField(value);
    },
};

/** Encode one ScalarField. */
export function encodeScalarField(writer: BinaryWriter, value: ScalarField): void {
    encodeScalar(writer, value.scalar);
    writer.writeUnsigned(value.offset);
}

/** Decode one ScalarField. */
export function decodeScalarField(reader: BinaryReader): ScalarField {
    const scalar = decodeScalar(reader);
    const offset = reader.readNumber();

    return {
        scalar,
        offset,
    };
}

/** Return one JSON value for one ScalarField. */
export function toJsonScalarField(value: ScalarField): Json {
    return {
        scalar: toJsonScalar(value.scalar),
        offset: value.offset,
    };
}

/** Return one ScalarField from one JSON value. */
export function fromJsonScalarField(value: Json): ScalarField {
    const object = jsonObject(value);

    return {
        scalar: fromJsonScalar(jsonField(object, "scalar")),
        offset: jsonInteger(jsonField(object, "offset")),
    };
}

/** Concrete layout for a struct. */
export type StructLayout = {
    /** The fields in source order. */
    readonly fields: ReadonlyArray<LayoutField>;
};

export const StructLayout = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: StructLayout): void {
        encodeStructLayout(writer, value);
    },

    /** Decode one StructLayout. */
    decode(reader: BinaryReader): StructLayout {
        return decodeStructLayout(reader);
    },

    /** Return this value as JSON. */
    toJson(value: StructLayout): Json {
        return toJsonStructLayout(value);
    },

    /** Return one StructLayout from one JSON value. */
    fromJson(value: Json): StructLayout {
        return fromJsonStructLayout(value);
    },
};

/** Encode one StructLayout. */
export function encodeStructLayout(writer: BinaryWriter, value: StructLayout): void {
    writer.writeUnsigned(value.fields.length);
    for (const item0 of value.fields) {
        encodeLayoutField(writer, item0);
    }
}

/** Decode one StructLayout. */
export function decodeStructLayout(reader: BinaryReader): StructLayout {
    const fields = (() => {
        const length0 = reader.readNumber();
        const items0: Array<LayoutField> = [];
        for (let index = 0; index < length0; index += 1) {
            items0.push(decodeLayoutField(reader));
        }
        return items0;
    })();

    return {
        fields,
    };
}

/** Return one JSON value for one StructLayout. */
export function toJsonStructLayout(value: StructLayout): Json {
    return {
        fields: value.fields.map((item0) => toJsonLayoutField(item0)),
    };
}

/** Return one StructLayout from one JSON value. */
export function fromJsonStructLayout(value: Json): StructLayout {
    const object = jsonObject(value);

    return {
        fields: jsonArray(jsonField(object, "fields")).map((item0) => fromJsonLayoutField(item0)),
    };
}

/** Concrete layout for a tuple. */
export type TupleLayout = {
    /** The tuple elements in source order. */
    readonly elements: ReadonlyArray<LayoutField>;
};

export const TupleLayout = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: TupleLayout): void {
        encodeTupleLayout(writer, value);
    },

    /** Decode one TupleLayout. */
    decode(reader: BinaryReader): TupleLayout {
        return decodeTupleLayout(reader);
    },

    /** Return this value as JSON. */
    toJson(value: TupleLayout): Json {
        return toJsonTupleLayout(value);
    },

    /** Return one TupleLayout from one JSON value. */
    fromJson(value: Json): TupleLayout {
        return fromJsonTupleLayout(value);
    },
};

/** Encode one TupleLayout. */
export function encodeTupleLayout(writer: BinaryWriter, value: TupleLayout): void {
    writer.writeUnsigned(value.elements.length);
    for (const item0 of value.elements) {
        encodeLayoutField(writer, item0);
    }
}

/** Decode one TupleLayout. */
export function decodeTupleLayout(reader: BinaryReader): TupleLayout {
    const elements = (() => {
        const length0 = reader.readNumber();
        const items0: Array<LayoutField> = [];
        for (let index = 0; index < length0; index += 1) {
            items0.push(decodeLayoutField(reader));
        }
        return items0;
    })();

    return {
        elements,
    };
}

/** Return one JSON value for one TupleLayout. */
export function toJsonTupleLayout(value: TupleLayout): Json {
    return {
        elements: value.elements.map((item0) => toJsonLayoutField(item0)),
    };
}

/** Return one TupleLayout from one JSON value. */
export function fromJsonTupleLayout(value: Json): TupleLayout {
    const object = jsonObject(value);

    return {
        elements: jsonArray(jsonField(object, "elements")).map((item0) =>
            fromJsonLayoutField(item0),
        ),
    };
}

/** One inclusive wrapping range of valid scalar bits. */
export type Validity = {
    /** The first valid scalar value. */
    readonly start: Discriminant;
    /** The last valid scalar value. */
    readonly end: Discriminant;
};

export const Validity = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: Validity): void {
        encodeValidity(writer, value);
    },

    /** Decode one Validity. */
    decode(reader: BinaryReader): Validity {
        return decodeValidity(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Validity): Json {
        return toJsonValidity(value);
    },

    /** Return one Validity from one JSON value. */
    fromJson(value: Json): Validity {
        return fromJsonValidity(value);
    },
};

/** Encode one Validity. */
export function encodeValidity(writer: BinaryWriter, value: Validity): void {
    encodeDiscriminant(writer, value.start);
    encodeDiscriminant(writer, value.end);
}

/** Decode one Validity. */
export function decodeValidity(reader: BinaryReader): Validity {
    const start = decodeDiscriminant(reader);
    const end = decodeDiscriminant(reader);

    return {
        start,
        end,
    };
}

/** Return one JSON value for one Validity. */
export function toJsonValidity(value: Validity): Json {
    return {
        start: toJsonDiscriminant(value.start),
        end: toJsonDiscriminant(value.end),
    };
}

/** Return one Validity from one JSON value. */
export function fromJsonValidity(value: Json): Validity {
    const object = jsonObject(value);

    return {
        start: fromJsonDiscriminant(jsonField(object, "start")),
        end: fromJsonDiscriminant(jsonField(object, "end")),
    };
}

/** Concrete layout for one variant case. */
export type VariantCaseLayout = {
    /** The logical discriminant bits. */
    readonly discriminant: Discriminant;
    /** The logical case type. */
    readonly ty: LocalNodeId;
    /** The payload byte offset from the variant base. */
    readonly payloadOffset: number;
};

export const VariantCaseLayout = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: VariantCaseLayout): void {
        encodeVariantCaseLayout(writer, value);
    },

    /** Decode one VariantCaseLayout. */
    decode(reader: BinaryReader): VariantCaseLayout {
        return decodeVariantCaseLayout(reader);
    },

    /** Return this value as JSON. */
    toJson(value: VariantCaseLayout): Json {
        return toJsonVariantCaseLayout(value);
    },

    /** Return one VariantCaseLayout from one JSON value. */
    fromJson(value: Json): VariantCaseLayout {
        return fromJsonVariantCaseLayout(value);
    },
};

/** Encode one VariantCaseLayout. */
export function encodeVariantCaseLayout(writer: BinaryWriter, value: VariantCaseLayout): void {
    encodeDiscriminant(writer, value.discriminant);
    encodeLocalNodeId(writer, value.ty);
    writer.writeUnsigned(value.payloadOffset);
}

/** Decode one VariantCaseLayout. */
export function decodeVariantCaseLayout(reader: BinaryReader): VariantCaseLayout {
    const discriminant = decodeDiscriminant(reader);
    const ty = decodeLocalNodeId(reader);
    const payloadOffset = reader.readNumber();

    return {
        discriminant,
        ty,
        payloadOffset,
    };
}

/** Return one JSON value for one VariantCaseLayout. */
export function toJsonVariantCaseLayout(value: VariantCaseLayout): Json {
    return {
        discriminant: toJsonDiscriminant(value.discriminant),
        ty: toJsonLocalNodeId(value.ty),
        payloadOffset: value.payloadOffset,
    };
}

/** Return one VariantCaseLayout from one JSON value. */
export function fromJsonVariantCaseLayout(value: Json): VariantCaseLayout {
    const object = jsonObject(value);

    return {
        discriminant: fromJsonDiscriminant(jsonField(object, "discriminant")),
        ty: fromJsonLocalNodeId(jsonField(object, "ty")),
        payloadOffset: jsonInteger(jsonField(object, "payloadOffset")),
    };
}

/** Physical encoding for one variant discriminant. */
export type VariantEncoding =
    /** Store the logical discriminant directly. */
    | {
          readonly kind: "direct";
          /** The physical discriminant field. */
          readonly field: DiscriminantField;
      }
    /** Encode selected cases in invalid values of one payload field. */
    | {
          readonly kind: "niche";
          /** The payload field carrying the niche. */
          readonly field: DiscriminantField;
          /** The case represented by every value outside the niche range. */
          readonly untaggedCase: number;
          /** The first physical niche value. */
          readonly nicheStart: Discriminant;
      };

export const VariantEncoding = {
    /** Store the logical discriminant directly. */
    direct(field: DiscriminantField): VariantEncoding {
        return { kind: "direct", field };
    },

    /** Encode selected cases in invalid values of one payload field. */
    niche(
        field: DiscriminantField,
        untaggedCase: number,
        nicheStart: Discriminant,
    ): VariantEncoding {
        return { kind: "niche", field, untaggedCase, nicheStart };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: VariantEncoding): void {
        encodeVariantEncoding(writer, value);
    },

    /** Decode one VariantEncoding. */
    decode(reader: BinaryReader): VariantEncoding {
        return decodeVariantEncoding(reader);
    },

    /** Return this value as JSON. */
    toJson(value: VariantEncoding): Json {
        return toJsonVariantEncoding(value);
    },

    /** Return one VariantEncoding from one JSON value. */
    fromJson(value: Json): VariantEncoding {
        return fromJsonVariantEncoding(value);
    },
};

/** Encode one VariantEncoding. */
export function encodeVariantEncoding(writer: BinaryWriter, value: VariantEncoding): void {
    switch (value.kind) {
        case "direct":
            writer.writeUnsigned(0);
            encodeDiscriminantField(writer, value.field);
            return;
        case "niche":
            writer.writeUnsigned(1);
            encodeDiscriminantField(writer, value.field);
            writer.writeUnsigned(value.untaggedCase);
            encodeDiscriminant(writer, value.nicheStart);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one VariantEncoding. */
export function decodeVariantEncoding(reader: BinaryReader): VariantEncoding {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const field = decodeDiscriminantField(reader);

            return {
                kind: "direct",
                field,
            };
        }
        case 1: {
            const field = decodeDiscriminantField(reader);
            const untaggedCase = reader.readNumber();
            const nicheStart = decodeDiscriminant(reader);

            return {
                kind: "niche",
                field,
                untaggedCase,
                nicheStart,
            };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one VariantEncoding. */
export function toJsonVariantEncoding(value: VariantEncoding): Json {
    switch (value.kind) {
        case "direct":
            return {
                kind: "direct",
                field: toJsonDiscriminantField(value.field),
            };
        case "niche":
            return {
                kind: "niche",
                field: toJsonDiscriminantField(value.field),
                untaggedCase: value.untaggedCase,
                nicheStart: toJsonDiscriminant(value.nicheStart),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one VariantEncoding from one JSON value. */
export function fromJsonVariantEncoding(value: Json): VariantEncoding {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "direct":
            return {
                kind,
                field: fromJsonDiscriminantField(jsonField(object, "field")),
            };
        case "niche":
            return {
                kind,
                field: fromJsonDiscriminantField(jsonField(object, "field")),
                untaggedCase: jsonInteger(jsonField(object, "untaggedCase")),
                nicheStart: fromJsonDiscriminant(jsonField(object, "nicheStart")),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** Concrete layout for a variant value. */
export type VariantLayout = {
    /** The logical discriminant type. */
    readonly discriminant: LocalNodeId;
    /** The physical discriminant encoding. */
    readonly encoding: VariantEncoding;
    /** The variant cases. */
    readonly cases: ReadonlyArray<VariantCaseLayout>;
};

export const VariantLayout = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: VariantLayout): void {
        encodeVariantLayout(writer, value);
    },

    /** Decode one VariantLayout. */
    decode(reader: BinaryReader): VariantLayout {
        return decodeVariantLayout(reader);
    },

    /** Return this value as JSON. */
    toJson(value: VariantLayout): Json {
        return toJsonVariantLayout(value);
    },

    /** Return one VariantLayout from one JSON value. */
    fromJson(value: Json): VariantLayout {
        return fromJsonVariantLayout(value);
    },
};

/** Encode one VariantLayout. */
export function encodeVariantLayout(writer: BinaryWriter, value: VariantLayout): void {
    encodeLocalNodeId(writer, value.discriminant);
    encodeVariantEncoding(writer, value.encoding);
    writer.writeUnsigned(value.cases.length);
    for (const item2 of value.cases) {
        encodeVariantCaseLayout(writer, item2);
    }
}

/** Decode one VariantLayout. */
export function decodeVariantLayout(reader: BinaryReader): VariantLayout {
    const discriminant = decodeLocalNodeId(reader);
    const encoding = decodeVariantEncoding(reader);
    const cases = (() => {
        const length2 = reader.readNumber();
        const items2: Array<VariantCaseLayout> = [];
        for (let index = 0; index < length2; index += 1) {
            items2.push(decodeVariantCaseLayout(reader));
        }
        return items2;
    })();

    return {
        discriminant,
        encoding,
        cases,
    };
}

/** Return one JSON value for one VariantLayout. */
export function toJsonVariantLayout(value: VariantLayout): Json {
    return {
        discriminant: toJsonLocalNodeId(value.discriminant),
        encoding: toJsonVariantEncoding(value.encoding),
        cases: value.cases.map((item0) => toJsonVariantCaseLayout(item0)),
    };
}

/** Return one VariantLayout from one JSON value. */
export function fromJsonVariantLayout(value: Json): VariantLayout {
    const object = jsonObject(value);

    return {
        discriminant: fromJsonLocalNodeId(jsonField(object, "discriminant")),
        encoding: fromJsonVariantEncoding(jsonField(object, "encoding")),
        cases: jsonArray(jsonField(object, "cases")).map((item0) =>
            fromJsonVariantCaseLayout(item0),
        ),
    };
}

/** One fixed vector representation. */
export type Vector = {
    /** The scalar lane representation. */
    readonly element: Scalar;
    /** The fixed lane count. */
    readonly lanes: number;
};

export const Vector = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: Vector): void {
        encodeVector(writer, value);
    },

    /** Decode one Vector. */
    decode(reader: BinaryReader): Vector {
        return decodeVector(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Vector): Json {
        return toJsonVector(value);
    },

    /** Return one Vector from one JSON value. */
    fromJson(value: Json): Vector {
        return fromJsonVector(value);
    },
};

/** Encode one Vector. */
export function encodeVector(writer: BinaryWriter, value: Vector): void {
    encodeScalar(writer, value.element);
    writer.writeUnsigned(value.lanes);
}

/** Decode one Vector. */
export function decodeVector(reader: BinaryReader): Vector {
    const element = decodeScalar(reader);
    const lanes = reader.readNumber();

    return {
        element,
        lanes,
    };
}

/** Return one JSON value for one Vector. */
export function toJsonVector(value: Vector): Json {
    return {
        element: toJsonScalar(value.element),
        lanes: value.lanes,
    };
}

/** Return one Vector from one JSON value. */
export function fromJsonVector(value: Json): Vector {
    const object = jsonObject(value);

    return {
        element: fromJsonScalar(jsonField(object, "element")),
        lanes: jsonInteger(jsonField(object, "lanes")),
    };
}
