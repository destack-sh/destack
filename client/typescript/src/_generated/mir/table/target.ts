/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, jsonField, jsonInteger, jsonObject, jsonString } from "../../../protocol/serde.js";

/** ABI layout facts for one target. */
export type TargetLayout = {
    /** Target byte order. */
    readonly endian: Endian;
    /** Target pointer layout. */
    readonly pointer: PointerLayout;
    /** Stack alignment in bytes. */
    readonly stackAlignmentBytes: number;
};

export const TargetLayout = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: TargetLayout): void {
        encodeTargetLayout(writer, value);
    },

    /** Decode one TargetLayout. */
    decode(reader: BinaryReader): TargetLayout {
        return decodeTargetLayout(reader);
    },

    /** Return this value as JSON. */
    toJson(value: TargetLayout): Json {
        return toJsonTargetLayout(value);
    },

    /** Return one TargetLayout from one JSON value. */
    fromJson(value: Json): TargetLayout {
        return fromJsonTargetLayout(value);
    },
};

/** Encode one TargetLayout. */
export function encodeTargetLayout(writer: BinaryWriter, value: TargetLayout): void {
    encodeEndian(writer, value.endian);
    encodePointerLayout(writer, value.pointer);
    writer.writeUnsigned(value.stackAlignmentBytes);
}

/** Decode one TargetLayout. */
export function decodeTargetLayout(reader: BinaryReader): TargetLayout {
    const endian = decodeEndian(reader);
    const pointer = decodePointerLayout(reader);
    const stackAlignmentBytes = reader.readNumber();

    return {
        endian,
        pointer,
        stackAlignmentBytes,
    };
}

/** Return one JSON value for one TargetLayout. */
export function toJsonTargetLayout(value: TargetLayout): Json {
    return {
        endian: toJsonEndian(value.endian),
        pointer: toJsonPointerLayout(value.pointer),
        stackAlignmentBytes: value.stackAlignmentBytes,
    };
}

/** Return one TargetLayout from one JSON value. */
export function fromJsonTargetLayout(value: Json): TargetLayout {
    const object = jsonObject(value);

    return {
        endian: fromJsonEndian(jsonField(object, "endian")),
        pointer: fromJsonPointerLayout(jsonField(object, "pointer")),
        stackAlignmentBytes: jsonInteger(jsonField(object, "stackAlignmentBytes")),
    };
}

/** Byte order for target scalar memory operations. */
export type Endian = "little" | "big";

export const Endian = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: Endian): void {
        encodeEndian(writer, value);
    },

    /** Decode one Endian. */
    decode(reader: BinaryReader): Endian {
        return decodeEndian(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Endian): Json {
        return toJsonEndian(value);
    },

    /** Return one Endian from one JSON value. */
    fromJson(value: Json): Endian {
        return fromJsonEndian(value);
    },
};

/** Encode one Endian. */
export function encodeEndian(writer: BinaryWriter, value: Endian): void {
    switch (value) {
        case "little":
            writer.writeUnsigned(0);
            return;
        case "big":
            writer.writeUnsigned(1);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one Endian. */
export function decodeEndian(reader: BinaryReader): Endian {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "little";
        case 1:
            return "big";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one Endian. */
export function toJsonEndian(value: Endian): Json {
    return value;
}

/** Return one Endian from one JSON value. */
export function fromJsonEndian(value: Json): Endian {
    const variant = jsonString(value);

    switch (variant) {
        case "little":
            return "little";
        case "big":
            return "big";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** Pointer representation on the target. */
export type PointerLayout = {
    /** Pointer size in bytes. */
    readonly sizeBytes: number;
    /** Pointer alignment in bytes. */
    readonly alignmentBytes: number;
};

export const PointerLayout = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: PointerLayout): void {
        encodePointerLayout(writer, value);
    },

    /** Decode one PointerLayout. */
    decode(reader: BinaryReader): PointerLayout {
        return decodePointerLayout(reader);
    },

    /** Return this value as JSON. */
    toJson(value: PointerLayout): Json {
        return toJsonPointerLayout(value);
    },

    /** Return one PointerLayout from one JSON value. */
    fromJson(value: Json): PointerLayout {
        return fromJsonPointerLayout(value);
    },
};

/** Encode one PointerLayout. */
export function encodePointerLayout(writer: BinaryWriter, value: PointerLayout): void {
    writer.writeByte(value.sizeBytes);
    writer.writeByte(value.alignmentBytes);
}

/** Decode one PointerLayout. */
export function decodePointerLayout(reader: BinaryReader): PointerLayout {
    const sizeBytes = reader.readByte();
    const alignmentBytes = reader.readByte();

    return {
        sizeBytes,
        alignmentBytes,
    };
}

/** Return one JSON value for one PointerLayout. */
export function toJsonPointerLayout(value: PointerLayout): Json {
    return {
        sizeBytes: value.sizeBytes,
        alignmentBytes: value.alignmentBytes,
    };
}

/** Return one PointerLayout from one JSON value. */
export function fromJsonPointerLayout(value: Json): PointerLayout {
    const object = jsonObject(value);

    return {
        sizeBytes: jsonInteger(jsonField(object, "sizeBytes")),
        alignmentBytes: jsonInteger(jsonField(object, "alignmentBytes")),
    };
}
