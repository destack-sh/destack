/* generated by destack_client_generate, do not edit */

import {
    BinaryReader,
    BinaryWriter,
    Json,
    SerdeError,
    compareBytes,
    jsonArray,
    jsonField,
    jsonObject,
    nestedBytes,
} from "../../../protocol/serde.js";
import type { LocalNodeId } from "../tree/node.js";
import type { Storage } from "../tree/type.js";
import {
    decodeLocalNodeId,
    encodeLocalNodeId,
    fromJsonLocalNodeId,
    toJsonLocalNodeId,
} from "../tree/node.js";
import { decodeStorage, encodeStorage, fromJsonStorage, toJsonStorage } from "../tree/type.js";

/** Drop table for one MIR module. */
export type DropTable = {
    /** Generated destructors keyed by type and storage. */
    readonly destructors: ReadonlyMap<readonly [LocalNodeId, Storage], LocalNodeId>;
    /** User-authored drop hooks keyed by type. */
    readonly hooks: ReadonlyMap<LocalNodeId, LocalNodeId>;
};

export const DropTable = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: DropTable): void {
        encodeDropTable(writer, value);
    },

    /** Decode one DropTable. */
    decode(reader: BinaryReader): DropTable {
        return decodeDropTable(reader);
    },

    /** Return this value as JSON. */
    toJson(value: DropTable): Json {
        return toJsonDropTable(value);
    },

    /** Return one DropTable from one JSON value. */
    fromJson(value: Json): DropTable {
        return fromJsonDropTable(value);
    },
};

/** Encode one DropTable. */
export function encodeDropTable(writer: BinaryWriter, value: DropTable): void {
    const entries0 = Array.from(value.destructors.entries()).map(([key0, item0]) => {
        const keyBytes = nestedBytes((writer) => {
            encodeLocalNodeId(writer, key0[0]);
            encodeStorage(writer, key0[1]);
        });
        return { key0, item0, keyBytes };
    });
    entries0.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries0.length);
    for (const entry0 of entries0) {
        encodeLocalNodeId(writer, entry0.key0[0]);
        encodeStorage(writer, entry0.key0[1]);
        encodeLocalNodeId(writer, entry0.item0);
    }
    const entries1 = Array.from(value.hooks.entries()).map(([key1, item1]) => {
        const keyBytes = nestedBytes((writer) => {
            encodeLocalNodeId(writer, key1);
        });
        return { key1, item1, keyBytes };
    });
    entries1.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries1.length);
    for (const entry1 of entries1) {
        encodeLocalNodeId(writer, entry1.key1);
        encodeLocalNodeId(writer, entry1.item1);
    }
}

/** Decode one DropTable. */
export function decodeDropTable(reader: BinaryReader): DropTable {
    const destructors = (() => {
        const length0 = reader.readNumber();
        const items0 = new Map<readonly [LocalNodeId, Storage], LocalNodeId>();
        for (let index = 0; index < length0; index += 1) {
            items0.set(
                [decodeLocalNodeId(reader), decodeStorage(reader)] as const,
                decodeLocalNodeId(reader),
            );
        }
        return items0;
    })();
    const hooks = (() => {
        const length1 = reader.readNumber();
        const items1 = new Map<LocalNodeId, LocalNodeId>();
        for (let index = 0; index < length1; index += 1) {
            items1.set(decodeLocalNodeId(reader), decodeLocalNodeId(reader));
        }
        return items1;
    })();

    return {
        destructors,
        hooks,
    };
}

/** Return one JSON value for one DropTable. */
export function toJsonDropTable(value: DropTable): Json {
    return {
        destructors: Array.from(value.destructors.entries()).map(
            ([key0, item0]) =>
                [
                    [toJsonLocalNodeId(key0[0]), toJsonStorage(key0[1])],
                    toJsonLocalNodeId(item0),
                ] as const,
        ),
        hooks: Array.from(value.hooks.entries()).map(
            ([key0, item0]) => [toJsonLocalNodeId(key0), toJsonLocalNodeId(item0)] as const,
        ),
    };
}

/** Return one DropTable from one JSON value. */
export function fromJsonDropTable(value: Json): DropTable {
    const object = jsonObject(value);

    return {
        destructors: new Map(
            jsonArray(jsonField(object, "destructors")).map((entry) => {
                const items = jsonArray(entry);
                if (items.length !== 2) {
                    throw new SerdeError(`expected JSON map entry length 2: ${items.length}`);
                }
                const key0 = items[0];
                const item0 = items[1];
                return [
                    (() => {
                        const items = jsonArray(key0);
                        if (items.length !== 2) {
                            throw new SerdeError(`expected JSON tuple length 2: ${items.length}`);
                        }
                        return [fromJsonLocalNodeId(items[0]), fromJsonStorage(items[1])] as const;
                    })(),
                    fromJsonLocalNodeId(item0),
                ] as const;
            }),
        ),
        hooks: new Map(
            jsonArray(jsonField(object, "hooks")).map((entry) => {
                const items = jsonArray(entry);
                if (items.length !== 2) {
                    throw new SerdeError(`expected JSON map entry length 2: ${items.length}`);
                }
                const key0 = items[0];
                const item0 = items[1];
                return [fromJsonLocalNodeId(key0), fromJsonLocalNodeId(item0)] as const;
            }),
        ),
    };
}
