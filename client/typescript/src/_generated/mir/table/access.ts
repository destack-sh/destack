/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, compareBytes, jsonArray, jsonBigint, jsonField, jsonInteger, jsonObject, jsonOptional, jsonString, nestedBytes } from "../../../protocol/serde.js";
import type { AtomicAccess } from "../tree/memory.js";
import type { LocalNodeId } from "../tree/node.js";
import type { Value } from "../tree/value.js";
import { decodeAtomicAccess, encodeAtomicAccess, fromJsonAtomicAccess, toJsonAtomicAccess } from "../tree/memory.js";
import { decodeLocalNodeId, encodeLocalNodeId, fromJsonLocalNodeId, toJsonLocalNodeId } from "../tree/node.js";
import { decodeValue, encodeValue, fromJsonValue, toJsonValue } from "../tree/value.js";

/** Table of explicit memory accesses. */
export type AccessTable = {
    /** Memory accesses keyed by instruction id. */
    readonly accesses: ReadonlyMap<LocalNodeId, ReadonlyArray<MemoryAccess>>;
};

export const AccessTable = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: AccessTable): void {
        encodeAccessTable(writer, value);
    },

    /** Decode one AccessTable. */
    decode(reader: BinaryReader): AccessTable {
        return decodeAccessTable(reader);
    },

    /** Return this value as JSON. */
    toJson(value: AccessTable): Json {
        return toJsonAccessTable(value);
    },

    /** Return one AccessTable from one JSON value. */
    fromJson(value: Json): AccessTable {
        return fromJsonAccessTable(value);
    },
};

/** Encode one AccessTable. */
export function encodeAccessTable(writer: BinaryWriter, value: AccessTable): void {
    const entries0 = Array.from(value.accesses.entries()).map(([key0, item0]) => {
        const keyBytes = nestedBytes((writer) => {
            encodeLocalNodeId(writer, key0);
        });
        return { key0, item0, keyBytes };
    });
    entries0.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries0.length);
    for (const entry0 of entries0) {
        encodeLocalNodeId(writer, entry0.key0);
        writer.writeUnsigned(entry0.item0.length);
        for (const item1 of entry0.item0) {
            encodeMemoryAccess(writer, item1);
        }
    }
}

/** Decode one AccessTable. */
export function decodeAccessTable(reader: BinaryReader): AccessTable {
    const accesses = (() => { const length0 = reader.readNumber(); const items0 = new Map<LocalNodeId, ReadonlyArray<MemoryAccess>>(); for (let index = 0; index < length0; index += 1) { items0.set(decodeLocalNodeId(reader), (() => { const length2 = reader.readNumber(); const items2: Array<MemoryAccess> = []; for (let index = 0; index < length2; index += 1) { items2.push(decodeMemoryAccess(reader)); } return items2; })()); } return items0; })();

    return {
        accesses,
    };
}

/** Return one JSON value for one AccessTable. */
export function toJsonAccessTable(value: AccessTable): Json {
    return {
        accesses: Array.from(value.accesses.entries()).map(([key0, item0]) => [toJsonLocalNodeId(key0), item0.map((item1) => toJsonMemoryAccess(item1))] as const),
    };
}

/** Return one AccessTable from one JSON value. */
export function fromJsonAccessTable(value: Json): AccessTable {
    const object = jsonObject(value);

    return {
        accesses: new Map(jsonArray(jsonField(object, "accesses")).map((entry) => { const items = jsonArray(entry); if (items.length !== 2) { throw new SerdeError(`expected JSON map entry length 2: ${items.length}`); } const key0 = items[0]; const item0 = items[1]; return [fromJsonLocalNodeId(key0), jsonArray(item0).map((item1) => fromJsonMemoryAccess(item1))] as const; })),
    };
}

/** Explicit memory access attached to one instruction. */
export type MemoryAccess = {
    /** The operation performed. */
    readonly operation: MemoryOperation;
    /** The access target. */
    readonly target: MemoryTarget;
    /** The number of bytes accessed when known. */
    readonly byteLen?: bigint;
    /** Alignment in bytes, when known. */
    readonly alignmentBytes?: number;
    /** The ordering constraints on this access. */
    readonly order: MemoryAccessOrder;
};

export const MemoryAccess = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: MemoryAccess): void {
        encodeMemoryAccess(writer, value);
    },

    /** Decode one MemoryAccess. */
    decode(reader: BinaryReader): MemoryAccess {
        return decodeMemoryAccess(reader);
    },

    /** Return this value as JSON. */
    toJson(value: MemoryAccess): Json {
        return toJsonMemoryAccess(value);
    },

    /** Return one MemoryAccess from one JSON value. */
    fromJson(value: Json): MemoryAccess {
        return fromJsonMemoryAccess(value);
    },
};

/** Encode one MemoryAccess. */
export function encodeMemoryAccess(writer: BinaryWriter, value: MemoryAccess): void {
    encodeMemoryOperation(writer, value.operation);
    encodeMemoryTarget(writer, value.target);
    writer.writeOption(value.byteLen, (value2) => {
        writer.writeUnsigned(value2);
    });
    writer.writeOption(value.alignmentBytes, (value3) => {
        writer.writeUnsigned(value3);
    });
    encodeMemoryAccessOrder(writer, value.order);
}

/** Decode one MemoryAccess. */
export function decodeMemoryAccess(reader: BinaryReader): MemoryAccess {
    const operation = decodeMemoryOperation(reader);
    const target = decodeMemoryTarget(reader);
    const byteLen = reader.readOption(() => reader.readUnsigned());
    const alignmentBytes = reader.readOption(() => reader.readNumber());
    const order = decodeMemoryAccessOrder(reader);

    return {
        operation,
        target,
        ...(byteLen === undefined ? {} : { byteLen }),
        ...(alignmentBytes === undefined ? {} : { alignmentBytes }),
        order,
    };
}

/** Return one JSON value for one MemoryAccess. */
export function toJsonMemoryAccess(value: MemoryAccess): Json {
    return {
        operation: toJsonMemoryOperation(value.operation),
        target: toJsonMemoryTarget(value.target),
        ...(value.byteLen === undefined ? {} : { byteLen: value.byteLen.toString() }),
        ...(value.alignmentBytes === undefined ? {} : { alignmentBytes: value.alignmentBytes }),
        order: toJsonMemoryAccessOrder(value.order),
    };
}

/** Return one MemoryAccess from one JSON value. */
export function fromJsonMemoryAccess(value: Json): MemoryAccess {
    const object = jsonObject(value);

    return {
        operation: fromJsonMemoryOperation(jsonField(object, "operation")),
        target: fromJsonMemoryTarget(jsonField(object, "target")),
        byteLen: jsonOptional(object, "byteLen", (value) => jsonBigint(value)),
        alignmentBytes: jsonOptional(object, "alignmentBytes", (value) => jsonInteger(value)),
        order: fromJsonMemoryAccessOrder(jsonField(object, "order")),
    };
}

/** Ordering constraints for one memory access. */
export type MemoryAccessOrder =
    /** Ordinary memory access. */
    | {
          readonly kind: "plain";
      }
    /** Externally observable memory access. */
    | {
          readonly kind: "volatile";
      }
    /** Atomic memory access. */
    | {
          readonly kind: "atomic";
          readonly atomic: AtomicAccess;
      }
;

export const MemoryAccessOrder = {
    /** Ordinary memory access. */
    plain(): MemoryAccessOrder {
        return { kind: "plain" };
    },

    /** Externally observable memory access. */
    volatile(): MemoryAccessOrder {
        return { kind: "volatile" };
    },

    /** Atomic memory access. */
    atomic(atomic: AtomicAccess): MemoryAccessOrder {
        return { kind: "atomic", atomic };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: MemoryAccessOrder): void {
        encodeMemoryAccessOrder(writer, value);
    },

    /** Decode one MemoryAccessOrder. */
    decode(reader: BinaryReader): MemoryAccessOrder {
        return decodeMemoryAccessOrder(reader);
    },

    /** Return this value as JSON. */
    toJson(value: MemoryAccessOrder): Json {
        return toJsonMemoryAccessOrder(value);
    },

    /** Return one MemoryAccessOrder from one JSON value. */
    fromJson(value: Json): MemoryAccessOrder {
        return fromJsonMemoryAccessOrder(value);
    },
};

/** Encode one MemoryAccessOrder. */
export function encodeMemoryAccessOrder(writer: BinaryWriter, value: MemoryAccessOrder): void {
    switch (value.kind) {
        case "plain":
            writer.writeUnsigned(0);
            return;
        case "volatile":
            writer.writeUnsigned(1);
            return;
        case "atomic":
            writer.writeUnsigned(2);
            encodeAtomicAccess(writer, value.atomic);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one MemoryAccessOrder. */
export function decodeMemoryAccessOrder(reader: BinaryReader): MemoryAccessOrder {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            return { kind: "plain" };
        }
        case 1: {
            return { kind: "volatile" };
        }
        case 2: {
            const atomic = decodeAtomicAccess(reader);

            return { kind: "atomic", atomic };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one MemoryAccessOrder. */
export function toJsonMemoryAccessOrder(value: MemoryAccessOrder): Json {
    switch (value.kind) {
        case "plain":
            return {
                kind: "plain",
            };
        case "volatile":
            return {
                kind: "volatile",
            };
        case "atomic":
            return {
                kind: "atomic",
                atomic: toJsonAtomicAccess(value.atomic),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one MemoryAccessOrder from one JSON value. */
export function fromJsonMemoryAccessOrder(value: Json): MemoryAccessOrder {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "plain":
            return {
                kind,
            };
        case "volatile":
            return {
                kind,
            };
        case "atomic":
            return {
                kind,
                atomic: fromJsonAtomicAccess(jsonField(object, "atomic")),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** The operation performed by a memory access. */
export type MemoryOperation = "read" | "write" | "readWrite";

export const MemoryOperation = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: MemoryOperation): void {
        encodeMemoryOperation(writer, value);
    },

    /** Decode one MemoryOperation. */
    decode(reader: BinaryReader): MemoryOperation {
        return decodeMemoryOperation(reader);
    },

    /** Return this value as JSON. */
    toJson(value: MemoryOperation): Json {
        return toJsonMemoryOperation(value);
    },

    /** Return one MemoryOperation from one JSON value. */
    fromJson(value: Json): MemoryOperation {
        return fromJsonMemoryOperation(value);
    },
};

/** Encode one MemoryOperation. */
export function encodeMemoryOperation(writer: BinaryWriter, value: MemoryOperation): void {
    switch (value) {
        case "read":
            writer.writeUnsigned(0);
            return;
        case "write":
            writer.writeUnsigned(1);
            return;
        case "readWrite":
            writer.writeUnsigned(2);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one MemoryOperation. */
export function decodeMemoryOperation(reader: BinaryReader): MemoryOperation {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "read";
        case 1:
            return "write";
        case 2:
            return "readWrite";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one MemoryOperation. */
export function toJsonMemoryOperation(value: MemoryOperation): Json {
    return value;
}

/** Return one MemoryOperation from one JSON value. */
export function fromJsonMemoryOperation(value: Json): MemoryOperation {
    const variant = jsonString(value);

    switch (variant) {
        case "read":
            return "read";
        case "write":
            return "write";
        case "readWrite":
            return "readWrite";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** Target of one memory access. */
export type MemoryTarget =
    /** Access through an address-bearing value. */
    | {
          readonly kind: "address";
          readonly address: Value;
      }
    /** Access through a local slot. */
    | {
          readonly kind: "local";
          readonly local: LocalNodeId;
      }
    /** Access through a global. */
    | {
          readonly kind: "global";
          readonly global: LocalNodeId;
      }
;

export const MemoryTarget = {
    /** Access through an address-bearing value. */
    address(address: Value): MemoryTarget {
        return { kind: "address", address };
    },

    /** Access through a local slot. */
    local(local: LocalNodeId): MemoryTarget {
        return { kind: "local", local };
    },

    /** Access through a global. */
    global(global: LocalNodeId): MemoryTarget {
        return { kind: "global", global };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: MemoryTarget): void {
        encodeMemoryTarget(writer, value);
    },

    /** Decode one MemoryTarget. */
    decode(reader: BinaryReader): MemoryTarget {
        return decodeMemoryTarget(reader);
    },

    /** Return this value as JSON. */
    toJson(value: MemoryTarget): Json {
        return toJsonMemoryTarget(value);
    },

    /** Return one MemoryTarget from one JSON value. */
    fromJson(value: Json): MemoryTarget {
        return fromJsonMemoryTarget(value);
    },
};

/** Encode one MemoryTarget. */
export function encodeMemoryTarget(writer: BinaryWriter, value: MemoryTarget): void {
    switch (value.kind) {
        case "address":
            writer.writeUnsigned(0);
            encodeValue(writer, value.address);
            return;
        case "local":
            writer.writeUnsigned(1);
            encodeLocalNodeId(writer, value.local);
            return;
        case "global":
            writer.writeUnsigned(2);
            encodeLocalNodeId(writer, value.global);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one MemoryTarget. */
export function decodeMemoryTarget(reader: BinaryReader): MemoryTarget {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const address = decodeValue(reader);

            return { kind: "address", address };
        }
        case 1: {
            const local = decodeLocalNodeId(reader);

            return { kind: "local", local };
        }
        case 2: {
            const global = decodeLocalNodeId(reader);

            return { kind: "global", global };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one MemoryTarget. */
export function toJsonMemoryTarget(value: MemoryTarget): Json {
    switch (value.kind) {
        case "address":
            return {
                kind: "address",
                address: toJsonValue(value.address),
            };
        case "local":
            return {
                kind: "local",
                local: toJsonLocalNodeId(value.local),
            };
        case "global":
            return {
                kind: "global",
                global: toJsonLocalNodeId(value.global),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one MemoryTarget from one JSON value. */
export function fromJsonMemoryTarget(value: Json): MemoryTarget {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "address":
            return {
                kind,
                address: fromJsonValue(jsonField(object, "address")),
            };
        case "local":
            return {
                kind,
                local: fromJsonLocalNodeId(jsonField(object, "local")),
            };
        case "global":
            return {
                kind,
                global: fromJsonLocalNodeId(jsonField(object, "global")),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}
