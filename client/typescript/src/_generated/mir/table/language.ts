/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, compareBytes, jsonArray, jsonField, jsonObject, nestedBytes } from "../../../protocol/serde.js";
import type { StringId } from "../../core/string.js";
import type { LocalNodeIdAny } from "../tree/node.js";
import type { StaticKey } from "../tree/static.js";
import { decodeStringId, encodeStringId, fromJsonStringId, toJsonStringId } from "../../core/string.js";
import { decodeLocalNodeIdAny, encodeLocalNodeIdAny, fromJsonLocalNodeIdAny, toJsonLocalNodeIdAny } from "../tree/node.js";
import { decodeStaticKey, encodeStaticKey, fromJsonStaticKey, toJsonStaticKey } from "../tree/static.js";

/** One keyed member of a canonical language item. */
export type LanguageMember = {
    /** The stable key of the canonical owner. */
    readonly owner: StringId;
    /** The member key. */
    readonly key: StaticKey;
};

export const LanguageMember = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: LanguageMember): void {
        encodeLanguageMember(writer, value);
    },

    /** Decode one LanguageMember. */
    decode(reader: BinaryReader): LanguageMember {
        return decodeLanguageMember(reader);
    },

    /** Return this value as JSON. */
    toJson(value: LanguageMember): Json {
        return toJsonLanguageMember(value);
    },

    /** Return one LanguageMember from one JSON value. */
    fromJson(value: Json): LanguageMember {
        return fromJsonLanguageMember(value);
    },
};

/** Encode one LanguageMember. */
export function encodeLanguageMember(writer: BinaryWriter, value: LanguageMember): void {
    encodeStringId(writer, value.owner);
    encodeStaticKey(writer, value.key);
}

/** Decode one LanguageMember. */
export function decodeLanguageMember(reader: BinaryReader): LanguageMember {
    const owner = decodeStringId(reader);
    const key = decodeStaticKey(reader);

    return {
        owner,
        key,
    };
}

/** Return one JSON value for one LanguageMember. */
export function toJsonLanguageMember(value: LanguageMember): Json {
    return {
        owner: toJsonStringId(value.owner),
        key: toJsonStaticKey(value.key),
    };
}

/** Return one LanguageMember from one JSON value. */
export function fromJsonLanguageMember(value: Json): LanguageMember {
    const object = jsonObject(value);

    return {
        owner: fromJsonStringId(jsonField(object, "owner")),
        key: fromJsonStaticKey(jsonField(object, "key")),
    };
}

/** Canonical language identities retained by one MIR module. */
export type LanguageTable = {
    /** Canonical items keyed by their MIR declarations. */
    readonly items: ReadonlyMap<LocalNodeIdAny, StringId>;
    /** Canonical members declared by MIR declarations. */
    readonly declaredMembers: ReadonlyMap<LocalNodeIdAny, LanguageMember>;
    /** Canonical members implemented by MIR declarations. */
    readonly implementedMembers: ReadonlyArray<readonly [LocalNodeIdAny, LanguageMember]>;
};

export const LanguageTable = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: LanguageTable): void {
        encodeLanguageTable(writer, value);
    },

    /** Decode one LanguageTable. */
    decode(reader: BinaryReader): LanguageTable {
        return decodeLanguageTable(reader);
    },

    /** Return this value as JSON. */
    toJson(value: LanguageTable): Json {
        return toJsonLanguageTable(value);
    },

    /** Return one LanguageTable from one JSON value. */
    fromJson(value: Json): LanguageTable {
        return fromJsonLanguageTable(value);
    },
};

/** Encode one LanguageTable. */
export function encodeLanguageTable(writer: BinaryWriter, value: LanguageTable): void {
    const entries0 = Array.from(value.items.entries()).map(([key0, item0]) => {
        const keyBytes = nestedBytes((writer) => {
            encodeLocalNodeIdAny(writer, key0);
        });
        return { key0, item0, keyBytes };
    });
    entries0.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries0.length);
    for (const entry0 of entries0) {
        encodeLocalNodeIdAny(writer, entry0.key0);
        encodeStringId(writer, entry0.item0);
    }
    const entries1 = Array.from(value.declaredMembers.entries()).map(([key1, item1]) => {
        const keyBytes = nestedBytes((writer) => {
            encodeLocalNodeIdAny(writer, key1);
        });
        return { key1, item1, keyBytes };
    });
    entries1.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries1.length);
    for (const entry1 of entries1) {
        encodeLocalNodeIdAny(writer, entry1.key1);
        encodeLanguageMember(writer, entry1.item1);
    }
    writer.writeUnsigned(value.implementedMembers.length);
    for (const item2 of value.implementedMembers) {
        encodeLocalNodeIdAny(writer, item2[0]);
        encodeLanguageMember(writer, item2[1]);
    }
}

/** Decode one LanguageTable. */
export function decodeLanguageTable(reader: BinaryReader): LanguageTable {
    const items = (() => { const length0 = reader.readNumber(); const items0 = new Map<LocalNodeIdAny, StringId>(); for (let index = 0; index < length0; index += 1) { items0.set(decodeLocalNodeIdAny(reader), decodeStringId(reader)); } return items0; })();
    const declaredMembers = (() => { const length1 = reader.readNumber(); const items1 = new Map<LocalNodeIdAny, LanguageMember>(); for (let index = 0; index < length1; index += 1) { items1.set(decodeLocalNodeIdAny(reader), decodeLanguageMember(reader)); } return items1; })();
    const implementedMembers = (() => { const length2 = reader.readNumber(); const items2: Array<readonly [LocalNodeIdAny, LanguageMember]> = []; for (let index = 0; index < length2; index += 1) { items2.push([decodeLocalNodeIdAny(reader), decodeLanguageMember(reader)] as const); } return items2; })();

    return {
        items,
        declaredMembers,
        implementedMembers,
    };
}

/** Return one JSON value for one LanguageTable. */
export function toJsonLanguageTable(value: LanguageTable): Json {
    return {
        items: Array.from(value.items.entries()).map(([key0, item0]) => [toJsonLocalNodeIdAny(key0), toJsonStringId(item0)] as const),
        declaredMembers: Array.from(value.declaredMembers.entries()).map(([key0, item0]) => [toJsonLocalNodeIdAny(key0), toJsonLanguageMember(item0)] as const),
        implementedMembers: value.implementedMembers.map((item0) => [toJsonLocalNodeIdAny(item0[0]), toJsonLanguageMember(item0[1])]),
    };
}

/** Return one LanguageTable from one JSON value. */
export function fromJsonLanguageTable(value: Json): LanguageTable {
    const object = jsonObject(value);

    return {
        items: new Map(jsonArray(jsonField(object, "items")).map((entry) => { const items = jsonArray(entry); if (items.length !== 2) { throw new SerdeError(`expected JSON map entry length 2: ${items.length}`); } const key0 = items[0]; const item0 = items[1]; return [fromJsonLocalNodeIdAny(key0), fromJsonStringId(item0)] as const; })),
        declaredMembers: new Map(jsonArray(jsonField(object, "declaredMembers")).map((entry) => { const items = jsonArray(entry); if (items.length !== 2) { throw new SerdeError(`expected JSON map entry length 2: ${items.length}`); } const key0 = items[0]; const item0 = items[1]; return [fromJsonLocalNodeIdAny(key0), fromJsonLanguageMember(item0)] as const; })),
        implementedMembers: jsonArray(jsonField(object, "implementedMembers")).map((item0) => (() => { const items = jsonArray(item0); if (items.length !== 2) { throw new SerdeError(`expected JSON tuple length 2: ${items.length}`); } return [fromJsonLocalNodeIdAny(items[0]), fromJsonLanguageMember(items[1])] as const; })()),
    };
}
