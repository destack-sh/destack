/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, compareBytes, jsonArray, jsonBool, jsonField, jsonObject, jsonOptional, nestedBytes } from "../../../protocol/serde.js";
import type { LocalNodeId } from "../tree/node.js";
import { decodeLocalNodeId, encodeLocalNodeId, fromJsonLocalNodeId, toJsonLocalNodeId } from "../tree/node.js";

/** Lineage tables for nominal types. */
export type TypeLineage = {
    /** Optional parent type for class inheritance. */
    readonly parent?: LocalNodeId;
    /** Interfaces implemented by this type. */
    readonly interfaces: ReadonlyArray<LocalNodeId>;
    /** True when the type is sealed to external extension. */
    readonly isSealed: boolean;
    /** True when the type is final and cannot be subclassed. */
    readonly isFinal: boolean;
    /** True when the type is abstract and cannot be instantiated. */
    readonly isAbstract: boolean;
    /** True when the type represents an interface. */
    readonly isInterface: boolean;
};

export const TypeLineage = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: TypeLineage): void {
        encodeTypeLineage(writer, value);
    },

    /** Decode one TypeLineage. */
    decode(reader: BinaryReader): TypeLineage {
        return decodeTypeLineage(reader);
    },

    /** Return this value as JSON. */
    toJson(value: TypeLineage): Json {
        return toJsonTypeLineage(value);
    },

    /** Return one TypeLineage from one JSON value. */
    fromJson(value: Json): TypeLineage {
        return fromJsonTypeLineage(value);
    },
};

/** Encode one TypeLineage. */
export function encodeTypeLineage(writer: BinaryWriter, value: TypeLineage): void {
    writer.writeOption(value.parent, (value0) => {
        encodeLocalNodeId(writer, value0);
    });
    writer.writeUnsigned(value.interfaces.length);
    for (const item1 of value.interfaces) {
        encodeLocalNodeId(writer, item1);
    }
    writer.writeBool(value.isSealed);
    writer.writeBool(value.isFinal);
    writer.writeBool(value.isAbstract);
    writer.writeBool(value.isInterface);
}

/** Decode one TypeLineage. */
export function decodeTypeLineage(reader: BinaryReader): TypeLineage {
    const parent = reader.readOption(() => decodeLocalNodeId(reader));
    const interfaces = (() => { const length1 = reader.readNumber(); const items1: Array<LocalNodeId> = []; for (let index = 0; index < length1; index += 1) { items1.push(decodeLocalNodeId(reader)); } return items1; })();
    const isSealed = reader.readBool();
    const isFinal = reader.readBool();
    const isAbstract = reader.readBool();
    const isInterface = reader.readBool();

    return {
        ...(parent === undefined ? {} : { parent }),
        interfaces,
        isSealed,
        isFinal,
        isAbstract,
        isInterface,
    };
}

/** Return one JSON value for one TypeLineage. */
export function toJsonTypeLineage(value: TypeLineage): Json {
    return {
        ...(value.parent === undefined ? {} : { parent: toJsonLocalNodeId(value.parent) }),
        interfaces: value.interfaces.map((item0) => toJsonLocalNodeId(item0)),
        isSealed: value.isSealed,
        isFinal: value.isFinal,
        isAbstract: value.isAbstract,
        isInterface: value.isInterface,
    };
}

/** Return one TypeLineage from one JSON value. */
export function fromJsonTypeLineage(value: Json): TypeLineage {
    const object = jsonObject(value);

    return {
        parent: jsonOptional(object, "parent", (value) => fromJsonLocalNodeId(value)),
        interfaces: jsonArray(jsonField(object, "interfaces")).map((item0) => fromJsonLocalNodeId(item0)),
        isSealed: jsonBool(jsonField(object, "isSealed")),
        isFinal: jsonBool(jsonField(object, "isFinal")),
        isAbstract: jsonBool(jsonField(object, "isAbstract")),
        isInterface: jsonBool(jsonField(object, "isInterface")),
    };
}

/** Canonical type table for one MIR module. */
export type TypeTable = {
    /** Nominal lineage keyed by type id. */
    readonly lineageByType: ReadonlyMap<LocalNodeId, TypeLineage>;
};

export const TypeTable = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: TypeTable): void {
        encodeTypeTable(writer, value);
    },

    /** Decode one TypeTable. */
    decode(reader: BinaryReader): TypeTable {
        return decodeTypeTable(reader);
    },

    /** Return this value as JSON. */
    toJson(value: TypeTable): Json {
        return toJsonTypeTable(value);
    },

    /** Return one TypeTable from one JSON value. */
    fromJson(value: Json): TypeTable {
        return fromJsonTypeTable(value);
    },
};

/** Encode one TypeTable. */
export function encodeTypeTable(writer: BinaryWriter, value: TypeTable): void {
    const entries0 = Array.from(value.lineageByType.entries()).map(([key0, item0]) => {
        const keyBytes = nestedBytes((writer) => {
            encodeLocalNodeId(writer, key0);
        });
        return { key0, item0, keyBytes };
    });
    entries0.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries0.length);
    for (const entry0 of entries0) {
        encodeLocalNodeId(writer, entry0.key0);
        encodeTypeLineage(writer, entry0.item0);
    }
}

/** Decode one TypeTable. */
export function decodeTypeTable(reader: BinaryReader): TypeTable {
    const lineageByType = (() => { const length0 = reader.readNumber(); const items0 = new Map<LocalNodeId, TypeLineage>(); for (let index = 0; index < length0; index += 1) { items0.set(decodeLocalNodeId(reader), decodeTypeLineage(reader)); } return items0; })();

    return {
        lineageByType,
    };
}

/** Return one JSON value for one TypeTable. */
export function toJsonTypeTable(value: TypeTable): Json {
    return {
        lineageByType: Array.from(value.lineageByType.entries()).map(([key0, item0]) => [toJsonLocalNodeId(key0), toJsonTypeLineage(item0)] as const),
    };
}

/** Return one TypeTable from one JSON value. */
export function fromJsonTypeTable(value: Json): TypeTable {
    const object = jsonObject(value);

    return {
        lineageByType: new Map(jsonArray(jsonField(object, "lineageByType")).map((entry) => { const items = jsonArray(entry); if (items.length !== 2) { throw new SerdeError(`expected JSON map entry length 2: ${items.length}`); } const key0 = items[0]; const item0 = items[1]; return [fromJsonLocalNodeId(key0), fromJsonTypeLineage(item0)] as const; })),
    };
}
