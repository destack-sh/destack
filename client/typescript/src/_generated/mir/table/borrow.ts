/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, jsonArray, jsonField, jsonInteger, jsonObject } from "../../../protocol/serde.js";
import type { PlaceOrigin } from "../analyses/place.js";
import { decodePlaceOrigin, encodePlaceOrigin, fromJsonPlaceOrigin, toJsonPlaceOrigin } from "../analyses/place.js";

/** One carrier keeping a move-only owner alive. */
export type BorrowDependency = {
    /** The value or storage carrying the borrow. */
    readonly carrier: PlaceOrigin;
    /** The borrowed move-only owner. */
    readonly owner: PlaceOrigin;
};

export const BorrowDependency = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: BorrowDependency): void {
        encodeBorrowDependency(writer, value);
    },

    /** Decode one BorrowDependency. */
    decode(reader: BinaryReader): BorrowDependency {
        return decodeBorrowDependency(reader);
    },

    /** Return this value as JSON. */
    toJson(value: BorrowDependency): Json {
        return toJsonBorrowDependency(value);
    },

    /** Return one BorrowDependency from one JSON value. */
    fromJson(value: Json): BorrowDependency {
        return fromJsonBorrowDependency(value);
    },
};

/** Encode one BorrowDependency. */
export function encodeBorrowDependency(writer: BinaryWriter, value: BorrowDependency): void {
    encodePlaceOrigin(writer, value.carrier);
    encodePlaceOrigin(writer, value.owner);
}

/** Decode one BorrowDependency. */
export function decodeBorrowDependency(reader: BinaryReader): BorrowDependency {
    const carrier = decodePlaceOrigin(reader);
    const owner = decodePlaceOrigin(reader);

    return {
        carrier,
        owner,
    };
}

/** Return one JSON value for one BorrowDependency. */
export function toJsonBorrowDependency(value: BorrowDependency): Json {
    return {
        carrier: toJsonPlaceOrigin(value.carrier),
        owner: toJsonPlaceOrigin(value.owner),
    };
}

/** Return one BorrowDependency from one JSON value. */
export function fromJsonBorrowDependency(value: Json): BorrowDependency {
    const object = jsonObject(value);

    return {
        carrier: fromJsonPlaceOrigin(jsonField(object, "carrier")),
        owner: fromJsonPlaceOrigin(jsonField(object, "owner")),
    };
}

/** Dependency range for one MIR program point. */
export type BorrowPoint = {
    /** The local MIR node identifier. */
    readonly node: number;
    /** The dependencies live at this point. */
    readonly range: BorrowRange;
};

export const BorrowPoint = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: BorrowPoint): void {
        encodeBorrowPoint(writer, value);
    },

    /** Decode one BorrowPoint. */
    decode(reader: BinaryReader): BorrowPoint {
        return decodeBorrowPoint(reader);
    },

    /** Return this value as JSON. */
    toJson(value: BorrowPoint): Json {
        return toJsonBorrowPoint(value);
    },

    /** Return one BorrowPoint from one JSON value. */
    fromJson(value: Json): BorrowPoint {
        return fromJsonBorrowPoint(value);
    },
};

/** Encode one BorrowPoint. */
export function encodeBorrowPoint(writer: BinaryWriter, value: BorrowPoint): void {
    writer.writeUnsigned(value.node);
    encodeBorrowRange(writer, value.range);
}

/** Decode one BorrowPoint. */
export function decodeBorrowPoint(reader: BinaryReader): BorrowPoint {
    const node = reader.readNumber();
    const range = decodeBorrowRange(reader);

    return {
        node,
        range,
    };
}

/** Return one JSON value for one BorrowPoint. */
export function toJsonBorrowPoint(value: BorrowPoint): Json {
    return {
        node: value.node,
        range: toJsonBorrowRange(value.range),
    };
}

/** Return one BorrowPoint from one JSON value. */
export function fromJsonBorrowPoint(value: Json): BorrowPoint {
    const object = jsonObject(value);

    return {
        node: jsonInteger(jsonField(object, "node")),
        range: fromJsonBorrowRange(jsonField(object, "range")),
    };
}

/** One range in flattened borrow dependency storage. */
export type BorrowRange = {
    /** The first dependency index. */
    readonly start: number;
    /** The number of dependencies. */
    readonly length: number;
};

export const BorrowRange = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: BorrowRange): void {
        encodeBorrowRange(writer, value);
    },

    /** Decode one BorrowRange. */
    decode(reader: BinaryReader): BorrowRange {
        return decodeBorrowRange(reader);
    },

    /** Return this value as JSON. */
    toJson(value: BorrowRange): Json {
        return toJsonBorrowRange(value);
    },

    /** Return one BorrowRange from one JSON value. */
    fromJson(value: Json): BorrowRange {
        return fromJsonBorrowRange(value);
    },
};

/** Encode one BorrowRange. */
export function encodeBorrowRange(writer: BinaryWriter, value: BorrowRange): void {
    writer.writeUnsigned(value.start);
    writer.writeUnsigned(value.length);
}

/** Decode one BorrowRange. */
export function decodeBorrowRange(reader: BinaryReader): BorrowRange {
    const start = reader.readNumber();
    const length = reader.readNumber();

    return {
        start,
        length,
    };
}

/** Return one JSON value for one BorrowRange. */
export function toJsonBorrowRange(value: BorrowRange): Json {
    return {
        start: value.start,
        length: value.length,
    };
}

/** Return one BorrowRange from one JSON value. */
export function fromJsonBorrowRange(value: Json): BorrowRange {
    const object = jsonObject(value);

    return {
        start: jsonInteger(jsonField(object, "start")),
        length: jsonInteger(jsonField(object, "length")),
    };
}

/** Borrow dependencies at verified MIR program points. */
export type BorrowTable = {
    /** Flattened borrow dependencies. */
    readonly dependencies: ReadonlyArray<BorrowDependency>;
    /** Dependencies live at block entries. */
    readonly blocks: ReadonlyArray<BorrowPoint>;
    /** Dependencies live after instructions. */
    readonly instructions: ReadonlyArray<BorrowPoint>;
};

export const BorrowTable = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: BorrowTable): void {
        encodeBorrowTable(writer, value);
    },

    /** Decode one BorrowTable. */
    decode(reader: BinaryReader): BorrowTable {
        return decodeBorrowTable(reader);
    },

    /** Return this value as JSON. */
    toJson(value: BorrowTable): Json {
        return toJsonBorrowTable(value);
    },

    /** Return one BorrowTable from one JSON value. */
    fromJson(value: Json): BorrowTable {
        return fromJsonBorrowTable(value);
    },
};

/** Encode one BorrowTable. */
export function encodeBorrowTable(writer: BinaryWriter, value: BorrowTable): void {
    writer.writeUnsigned(value.dependencies.length);
    for (const item0 of value.dependencies) {
        encodeBorrowDependency(writer, item0);
    }
    writer.writeUnsigned(value.blocks.length);
    for (const item1 of value.blocks) {
        encodeBorrowPoint(writer, item1);
    }
    writer.writeUnsigned(value.instructions.length);
    for (const item2 of value.instructions) {
        encodeBorrowPoint(writer, item2);
    }
}

/** Decode one BorrowTable. */
export function decodeBorrowTable(reader: BinaryReader): BorrowTable {
    const dependencies = (() => { const length0 = reader.readNumber(); const items0: Array<BorrowDependency> = []; for (let index = 0; index < length0; index += 1) { items0.push(decodeBorrowDependency(reader)); } return items0; })();
    const blocks = (() => { const length1 = reader.readNumber(); const items1: Array<BorrowPoint> = []; for (let index = 0; index < length1; index += 1) { items1.push(decodeBorrowPoint(reader)); } return items1; })();
    const instructions = (() => { const length2 = reader.readNumber(); const items2: Array<BorrowPoint> = []; for (let index = 0; index < length2; index += 1) { items2.push(decodeBorrowPoint(reader)); } return items2; })();

    return {
        dependencies,
        blocks,
        instructions,
    };
}

/** Return one JSON value for one BorrowTable. */
export function toJsonBorrowTable(value: BorrowTable): Json {
    return {
        dependencies: value.dependencies.map((item0) => toJsonBorrowDependency(item0)),
        blocks: value.blocks.map((item0) => toJsonBorrowPoint(item0)),
        instructions: value.instructions.map((item0) => toJsonBorrowPoint(item0)),
    };
}

/** Return one BorrowTable from one JSON value. */
export function fromJsonBorrowTable(value: Json): BorrowTable {
    const object = jsonObject(value);

    return {
        dependencies: jsonArray(jsonField(object, "dependencies")).map((item0) => fromJsonBorrowDependency(item0)),
        blocks: jsonArray(jsonField(object, "blocks")).map((item0) => fromJsonBorrowPoint(item0)),
        instructions: jsonArray(jsonField(object, "instructions")).map((item0) => fromJsonBorrowPoint(item0)),
    };
}
