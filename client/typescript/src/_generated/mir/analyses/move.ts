/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, jsonInteger } from "../../../protocol/serde.js";

/** Dense identifier for one move path. */
export type MovePathId = number;

export const MovePathId = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: MovePathId): void {
        encodeMovePathId(writer, value);
    },

    /** Decode one MovePathId. */
    decode(reader: BinaryReader): MovePathId {
        return decodeMovePathId(reader);
    },

    /** Return this value as JSON. */
    toJson(value: MovePathId): Json {
        return toJsonMovePathId(value);
    },

    /** Return one MovePathId from one JSON value. */
    fromJson(value: Json): MovePathId {
        return fromJsonMovePathId(value);
    },
};

/** Encode one MovePathId. */
export function encodeMovePathId(writer: BinaryWriter, value: MovePathId): void {
    writer.writeUnsigned(value);
}

/** Decode one MovePathId. */
export function decodeMovePathId(reader: BinaryReader): MovePathId {
    return reader.readNumber();
}

/** Return one JSON value for one MovePathId. */
export function toJsonMovePathId(value: MovePathId): Json {
    return value;
}

/** Return one MovePathId from one JSON value. */
export function fromJsonMovePathId(value: Json): MovePathId {
    return jsonInteger(value);
}
