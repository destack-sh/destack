/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, compareBytes, jsonArray, jsonBool, jsonField, jsonInteger, jsonObject, jsonString, nestedBytes } from "../../../protocol/serde.js";
import type { BitSet } from "../../core/bitset.js";
import type { FunctionBehavior } from "../table/effect.js";
import type { MemoryEffect } from "../table/effect.js";
import type { Linkage } from "../tree/global.js";
import type { Symbol } from "../tree/symbol.js";
import { decodeBitSet, encodeBitSet, fromJsonBitSet, toJsonBitSet } from "../../core/bitset.js";
import { decodeFunctionBehavior, encodeFunctionBehavior, fromJsonFunctionBehavior, toJsonFunctionBehavior } from "../table/effect.js";
import { decodeMemoryEffect, encodeMemoryEffect, fromJsonMemoryEffect, toJsonMemoryEffect } from "../table/effect.js";
import { decodeLinkage, encodeLinkage, fromJsonLinkage, toJsonLinkage } from "../tree/global.js";
import { decodeSymbol, encodeSymbol, fromJsonSymbol, toJsonSymbol } from "../tree/symbol.js";

/** Strongly connected components of the whole-program call graph, by dense symbol id. */
export type CallComponentGraph = {
    /** The component id of each symbol, by dense id. */
    readonly component: ReadonlyArray<number>;
    /** Whether each component contains a cycle, by component id. */
    readonly recursive: BitSet;
};

export const CallComponentGraph = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: CallComponentGraph): void {
        encodeCallComponentGraph(writer, value);
    },

    /** Decode one CallComponentGraph. */
    decode(reader: BinaryReader): CallComponentGraph {
        return decodeCallComponentGraph(reader);
    },

    /** Return this value as JSON. */
    toJson(value: CallComponentGraph): Json {
        return toJsonCallComponentGraph(value);
    },

    /** Return one CallComponentGraph from one JSON value. */
    fromJson(value: Json): CallComponentGraph {
        return fromJsonCallComponentGraph(value);
    },
};

/** Encode one CallComponentGraph. */
export function encodeCallComponentGraph(writer: BinaryWriter, value: CallComponentGraph): void {
    writer.writeUnsigned(value.component.length);
    for (const item0 of value.component) {
        writer.writeUnsigned(item0);
    }
    encodeBitSet(writer, value.recursive);
}

/** Decode one CallComponentGraph. */
export function decodeCallComponentGraph(reader: BinaryReader): CallComponentGraph {
    const component = (() => { const length0 = reader.readNumber(); const items0: Array<number> = []; for (let index = 0; index < length0; index += 1) { items0.push(reader.readNumber()); } return items0; })();
    const recursive = decodeBitSet(reader);

    return {
        component,
        recursive,
    };
}

/** Return one JSON value for one CallComponentGraph. */
export function toJsonCallComponentGraph(value: CallComponentGraph): Json {
    return {
        component: value.component.map((item0) => item0),
        recursive: toJsonBitSet(value.recursive),
    };
}

/** Return one CallComponentGraph from one JSON value. */
export function fromJsonCallComponentGraph(value: Json): CallComponentGraph {
    const object = jsonObject(value);

    return {
        component: jsonArray(jsonField(object, "component")).map((item0) => jsonInteger(item0)),
        recursive: fromJsonBitSet(jsonField(object, "recursive")),
    };
}

/** One outgoing reference from a symbol to another symbol. */
export type LinkEdge = {
    /** The referenced symbol. */
    readonly target: Symbol;
    /** How the target is referenced. */
    readonly kind: LinkEdgeKind;
};

export const LinkEdge = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: LinkEdge): void {
        encodeLinkEdge(writer, value);
    },

    /** Decode one LinkEdge. */
    decode(reader: BinaryReader): LinkEdge {
        return decodeLinkEdge(reader);
    },

    /** Return this value as JSON. */
    toJson(value: LinkEdge): Json {
        return toJsonLinkEdge(value);
    },

    /** Return one LinkEdge from one JSON value. */
    fromJson(value: Json): LinkEdge {
        return fromJsonLinkEdge(value);
    },
};

/** Encode one LinkEdge. */
export function encodeLinkEdge(writer: BinaryWriter, value: LinkEdge): void {
    encodeSymbol(writer, value.target);
    encodeLinkEdgeKind(writer, value.kind);
}

/** Decode one LinkEdge. */
export function decodeLinkEdge(reader: BinaryReader): LinkEdge {
    const target = decodeSymbol(reader);
    const kind = decodeLinkEdgeKind(reader);

    return {
        target,
        kind,
    };
}

/** Return one JSON value for one LinkEdge. */
export function toJsonLinkEdge(value: LinkEdge): Json {
    return {
        target: toJsonSymbol(value.target),
        kind: toJsonLinkEdgeKind(value.kind),
    };
}

/** Return one LinkEdge from one JSON value. */
export function fromJsonLinkEdge(value: Json): LinkEdge {
    const object = jsonObject(value);

    return {
        target: fromJsonSymbol(jsonField(object, "target")),
        kind: fromJsonLinkEdgeKind(jsonField(object, "kind")),
    };
}

/** How one symbol references another. */
export type LinkEdgeKind = "call" | "address" | "continuation";

export const LinkEdgeKind = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: LinkEdgeKind): void {
        encodeLinkEdgeKind(writer, value);
    },

    /** Decode one LinkEdgeKind. */
    decode(reader: BinaryReader): LinkEdgeKind {
        return decodeLinkEdgeKind(reader);
    },

    /** Return this value as JSON. */
    toJson(value: LinkEdgeKind): Json {
        return toJsonLinkEdgeKind(value);
    },

    /** Return one LinkEdgeKind from one JSON value. */
    fromJson(value: Json): LinkEdgeKind {
        return fromJsonLinkEdgeKind(value);
    },
};

/** Encode one LinkEdgeKind. */
export function encodeLinkEdgeKind(writer: BinaryWriter, value: LinkEdgeKind): void {
    switch (value) {
        case "call":
            writer.writeUnsigned(0);
            return;
        case "address":
            writer.writeUnsigned(1);
            return;
        case "continuation":
            writer.writeUnsigned(2);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one LinkEdgeKind. */
export function decodeLinkEdgeKind(reader: BinaryReader): LinkEdgeKind {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "call";
        case 1:
            return "address";
        case 2:
            return "continuation";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one LinkEdgeKind. */
export function toJsonLinkEdgeKind(value: LinkEdgeKind): Json {
    return value;
}

/** Return one LinkEdgeKind from one JSON value. */
export function fromJsonLinkEdgeKind(value: Json): LinkEdgeKind {
    const variant = jsonString(value);

    switch (variant) {
        case "call":
            return "call";
        case "address":
            return "address";
        case "continuation":
            return "continuation";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** Symbol-scoped reference graph for a module's linkable surface. */
export type LinkGraph = {
    /** Defined symbols, by identity. */
    readonly nodes: ReadonlyMap<Symbol, LinkNode>;
    /** Outgoing references, by source symbol. */
    readonly edges: ReadonlyMap<Symbol, ReadonlyArray<LinkEdge>>;
};

export const LinkGraph = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: LinkGraph): void {
        encodeLinkGraph(writer, value);
    },

    /** Decode one LinkGraph. */
    decode(reader: BinaryReader): LinkGraph {
        return decodeLinkGraph(reader);
    },

    /** Return this value as JSON. */
    toJson(value: LinkGraph): Json {
        return toJsonLinkGraph(value);
    },

    /** Return one LinkGraph from one JSON value. */
    fromJson(value: Json): LinkGraph {
        return fromJsonLinkGraph(value);
    },
};

/** Encode one LinkGraph. */
export function encodeLinkGraph(writer: BinaryWriter, value: LinkGraph): void {
    const entries0 = Array.from(value.nodes.entries()).map(([key0, item0]) => {
        const keyBytes = nestedBytes((writer) => {
            encodeSymbol(writer, key0);
        });
        return { key0, item0, keyBytes };
    });
    entries0.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries0.length);
    for (const entry0 of entries0) {
        encodeSymbol(writer, entry0.key0);
        encodeLinkNode(writer, entry0.item0);
    }
    const entries1 = Array.from(value.edges.entries()).map(([key1, item1]) => {
        const keyBytes = nestedBytes((writer) => {
            encodeSymbol(writer, key1);
        });
        return { key1, item1, keyBytes };
    });
    entries1.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries1.length);
    for (const entry1 of entries1) {
        encodeSymbol(writer, entry1.key1);
        writer.writeUnsigned(entry1.item1.length);
        for (const item2 of entry1.item1) {
            encodeLinkEdge(writer, item2);
        }
    }
}

/** Decode one LinkGraph. */
export function decodeLinkGraph(reader: BinaryReader): LinkGraph {
    const nodes = (() => { const length0 = reader.readNumber(); const items0 = new Map<Symbol, LinkNode>(); for (let index = 0; index < length0; index += 1) { items0.set(decodeSymbol(reader), decodeLinkNode(reader)); } return items0; })();
    const edges = (() => { const length1 = reader.readNumber(); const items1 = new Map<Symbol, ReadonlyArray<LinkEdge>>(); for (let index = 0; index < length1; index += 1) { items1.set(decodeSymbol(reader), (() => { const length3 = reader.readNumber(); const items3: Array<LinkEdge> = []; for (let index = 0; index < length3; index += 1) { items3.push(decodeLinkEdge(reader)); } return items3; })()); } return items1; })();

    return {
        nodes,
        edges,
    };
}

/** Return one JSON value for one LinkGraph. */
export function toJsonLinkGraph(value: LinkGraph): Json {
    return {
        nodes: Array.from(value.nodes.entries()).map(([key0, item0]) => [toJsonSymbol(key0), toJsonLinkNode(item0)] as const),
        edges: Array.from(value.edges.entries()).map(([key0, item0]) => [toJsonSymbol(key0), item0.map((item1) => toJsonLinkEdge(item1))] as const),
    };
}

/** Return one LinkGraph from one JSON value. */
export function fromJsonLinkGraph(value: Json): LinkGraph {
    const object = jsonObject(value);

    return {
        nodes: new Map(jsonArray(jsonField(object, "nodes")).map((entry) => { const items = jsonArray(entry); if (items.length !== 2) { throw new SerdeError(`expected JSON map entry length 2: ${items.length}`); } const key0 = items[0]; const item0 = items[1]; return [fromJsonSymbol(key0), fromJsonLinkNode(item0)] as const; })),
        edges: new Map(jsonArray(jsonField(object, "edges")).map((entry) => { const items = jsonArray(entry); if (items.length !== 2) { throw new SerdeError(`expected JSON map entry length 2: ${items.length}`); } const key0 = items[0]; const item0 = items[1]; return [fromJsonSymbol(key0), jsonArray(item0).map((item1) => fromJsonLinkEdge(item1))] as const; })),
    };
}

/** One defined symbol in the link graph. */
export type LinkNode =
    /** A defined function. */
    | {
          readonly kind: "function";
          /** Visibility and definition location. */
          readonly linkage: Linkage;
          /** Memory effect. */
          readonly memory: MemoryEffect;
          /** Behavioral effects (unwind, determinism, allocation, and so on). */
          readonly behavior: FunctionBehavior;
          /** Estimated inline cost. */
          readonly inlineCost: number;
          /** True when the function makes indirect or virtual calls. */
          readonly indirect: boolean;
      }
    /** A defined global. */
    | {
          readonly kind: "global";
          /** Visibility and definition location. */
          readonly linkage: Linkage;
      }
;

export const LinkNode = {
    /** A defined function. */
    "function"(linkage: Linkage, memory: MemoryEffect, behavior: FunctionBehavior, inlineCost: number, indirect: boolean): LinkNode {
        return { kind: "function", linkage, memory, behavior, inlineCost, indirect };
    },

    /** A defined global. */
    global(linkage: Linkage): LinkNode {
        return { kind: "global", linkage };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: LinkNode): void {
        encodeLinkNode(writer, value);
    },

    /** Decode one LinkNode. */
    decode(reader: BinaryReader): LinkNode {
        return decodeLinkNode(reader);
    },

    /** Return this value as JSON. */
    toJson(value: LinkNode): Json {
        return toJsonLinkNode(value);
    },

    /** Return one LinkNode from one JSON value. */
    fromJson(value: Json): LinkNode {
        return fromJsonLinkNode(value);
    },
};

/** Encode one LinkNode. */
export function encodeLinkNode(writer: BinaryWriter, value: LinkNode): void {
    switch (value.kind) {
        case "function":
            writer.writeUnsigned(0);
            encodeLinkage(writer, value.linkage);
            encodeMemoryEffect(writer, value.memory);
            encodeFunctionBehavior(writer, value.behavior);
            writer.writeUnsigned(value.inlineCost);
            writer.writeBool(value.indirect);
            return;
        case "global":
            writer.writeUnsigned(1);
            encodeLinkage(writer, value.linkage);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one LinkNode. */
export function decodeLinkNode(reader: BinaryReader): LinkNode {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const linkage = decodeLinkage(reader);
            const memory = decodeMemoryEffect(reader);
            const behavior = decodeFunctionBehavior(reader);
            const inlineCost = reader.readNumber();
            const indirect = reader.readBool();

            return {
                kind: "function",
                linkage,
                memory,
                behavior,
                inlineCost,
                indirect,
            };
        }
        case 1: {
            const linkage = decodeLinkage(reader);

            return {
                kind: "global",
                linkage,
            };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one LinkNode. */
export function toJsonLinkNode(value: LinkNode): Json {
    switch (value.kind) {
        case "function":
            return {
                kind: "function",
                linkage: toJsonLinkage(value.linkage),
                memory: toJsonMemoryEffect(value.memory),
                behavior: toJsonFunctionBehavior(value.behavior),
                inlineCost: value.inlineCost,
                indirect: value.indirect,
            };
        case "global":
            return {
                kind: "global",
                linkage: toJsonLinkage(value.linkage),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one LinkNode from one JSON value. */
export function fromJsonLinkNode(value: Json): LinkNode {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "function":
            return {
                kind,
                linkage: fromJsonLinkage(jsonField(object, "linkage")),
                memory: fromJsonMemoryEffect(jsonField(object, "memory")),
                behavior: fromJsonFunctionBehavior(jsonField(object, "behavior")),
                inlineCost: jsonInteger(jsonField(object, "inlineCost")),
                indirect: jsonBool(jsonField(object, "indirect")),
            };
        case "global":
            return {
                kind,
                linkage: fromJsonLinkage(jsonField(object, "linkage")),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}
