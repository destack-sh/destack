/* generated by destack_client_generate, do not edit */

import {
    BinaryReader,
    BinaryWriter,
    Json,
    SerdeError,
    jsonArray,
    jsonBool,
    jsonField,
    jsonInteger,
    jsonObject,
    jsonString,
} from "../../../../protocol/serde.js";
import type { FunctionBehavior } from "../../table/effect.js";
import type { MemoryEffect } from "../../table/effect.js";
import type { Linkage } from "../../tree/global.js";
import type { Symbol } from "../../tree/symbol.js";
import {
    decodeFunctionBehavior,
    encodeFunctionBehavior,
    fromJsonFunctionBehavior,
    toJsonFunctionBehavior,
} from "../../table/effect.js";
import {
    decodeMemoryEffect,
    encodeMemoryEffect,
    fromJsonMemoryEffect,
    toJsonMemoryEffect,
} from "../../table/effect.js";
import { decodeLinkage, encodeLinkage, fromJsonLinkage, toJsonLinkage } from "../../tree/global.js";
import { decodeSymbol, encodeSymbol, fromJsonSymbol, toJsonSymbol } from "../../tree/symbol.js";

/** One outgoing reference from a symbol to another symbol. */
export type LinkEdge = {
    /** The referenced symbol. */
    readonly target: Symbol;
    /** How the target is referenced. */
    readonly kind: LinkEdgeKind;
};

export const LinkEdge = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: LinkEdge): void {
        encodeLinkEdge(writer, value);
    },

    /** Decode one LinkEdge. */
    decode(reader: BinaryReader): LinkEdge {
        return decodeLinkEdge(reader);
    },

    /** Return this value as JSON. */
    toJson(value: LinkEdge): Json {
        return toJsonLinkEdge(value);
    },

    /** Return one LinkEdge from one JSON value. */
    fromJson(value: Json): LinkEdge {
        return fromJsonLinkEdge(value);
    },
};

/** Encode one LinkEdge. */
export function encodeLinkEdge(writer: BinaryWriter, value: LinkEdge): void {
    encodeSymbol(writer, value.target);
    encodeLinkEdgeKind(writer, value.kind);
}

/** Decode one LinkEdge. */
export function decodeLinkEdge(reader: BinaryReader): LinkEdge {
    const target = decodeSymbol(reader);
    const kind = decodeLinkEdgeKind(reader);

    return {
        target,
        kind,
    };
}

/** Return one JSON value for one LinkEdge. */
export function toJsonLinkEdge(value: LinkEdge): Json {
    return {
        target: toJsonSymbol(value.target),
        kind: toJsonLinkEdgeKind(value.kind),
    };
}

/** Return one LinkEdge from one JSON value. */
export function fromJsonLinkEdge(value: Json): LinkEdge {
    const object = jsonObject(value);

    return {
        target: fromJsonSymbol(jsonField(object, "target")),
        kind: fromJsonLinkEdgeKind(jsonField(object, "kind")),
    };
}

/** How one symbol references another. */
export type LinkEdgeKind = "call" | "address" | "continuation";

export const LinkEdgeKind = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: LinkEdgeKind): void {
        encodeLinkEdgeKind(writer, value);
    },

    /** Decode one LinkEdgeKind. */
    decode(reader: BinaryReader): LinkEdgeKind {
        return decodeLinkEdgeKind(reader);
    },

    /** Return this value as JSON. */
    toJson(value: LinkEdgeKind): Json {
        return toJsonLinkEdgeKind(value);
    },

    /** Return one LinkEdgeKind from one JSON value. */
    fromJson(value: Json): LinkEdgeKind {
        return fromJsonLinkEdgeKind(value);
    },
};

/** Encode one LinkEdgeKind. */
export function encodeLinkEdgeKind(writer: BinaryWriter, value: LinkEdgeKind): void {
    switch (value) {
        case "call":
            writer.writeUnsigned(0);
            return;
        case "address":
            writer.writeUnsigned(1);
            return;
        case "continuation":
            writer.writeUnsigned(2);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one LinkEdgeKind. */
export function decodeLinkEdgeKind(reader: BinaryReader): LinkEdgeKind {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "call";
        case 1:
            return "address";
        case 2:
            return "continuation";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one LinkEdgeKind. */
export function toJsonLinkEdgeKind(value: LinkEdgeKind): Json {
    return value;
}

/** Return one LinkEdgeKind from one JSON value. */
export function fromJsonLinkEdgeKind(value: Json): LinkEdgeKind {
    const variant = jsonString(value);

    switch (variant) {
        case "call":
            return "call";
        case "address":
            return "address";
        case "continuation":
            return "continuation";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** One defined symbol in the link graph. */
export type LinkNode =
    /** A defined function. */
    | {
          readonly kind: "function";
          /** Visibility and definition location. */
          readonly linkage: Linkage;
          /** Memory effect. */
          readonly memory: MemoryEffect;
          /** Behavioral effects (unwind, determinism, allocation, and so on). */
          readonly behavior: FunctionBehavior;
          /** Estimated inline cost. */
          readonly inlineCost: number;
          /** Whether the function contains calls without a closed target. */
          readonly hasOpenCalls: boolean;
      }
    /** A defined global. */
    | {
          readonly kind: "global";
          /** Visibility and definition location. */
          readonly linkage: Linkage;
      };

export const LinkNode = {
    /** A defined function. */
    function(
        linkage: Linkage,
        memory: MemoryEffect,
        behavior: FunctionBehavior,
        inlineCost: number,
        hasOpenCalls: boolean,
    ): LinkNode {
        return { kind: "function", linkage, memory, behavior, inlineCost, hasOpenCalls };
    },

    /** A defined global. */
    global(linkage: Linkage): LinkNode {
        return { kind: "global", linkage };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: LinkNode): void {
        encodeLinkNode(writer, value);
    },

    /** Decode one LinkNode. */
    decode(reader: BinaryReader): LinkNode {
        return decodeLinkNode(reader);
    },

    /** Return this value as JSON. */
    toJson(value: LinkNode): Json {
        return toJsonLinkNode(value);
    },

    /** Return one LinkNode from one JSON value. */
    fromJson(value: Json): LinkNode {
        return fromJsonLinkNode(value);
    },
};

/** Encode one LinkNode. */
export function encodeLinkNode(writer: BinaryWriter, value: LinkNode): void {
    switch (value.kind) {
        case "function":
            writer.writeUnsigned(0);
            encodeLinkage(writer, value.linkage);
            encodeMemoryEffect(writer, value.memory);
            encodeFunctionBehavior(writer, value.behavior);
            writer.writeUnsigned(value.inlineCost);
            writer.writeBool(value.hasOpenCalls);
            return;
        case "global":
            writer.writeUnsigned(1);
            encodeLinkage(writer, value.linkage);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one LinkNode. */
export function decodeLinkNode(reader: BinaryReader): LinkNode {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const linkage = decodeLinkage(reader);
            const memory = decodeMemoryEffect(reader);
            const behavior = decodeFunctionBehavior(reader);
            const inlineCost = reader.readNumber();
            const hasOpenCalls = reader.readBool();

            return {
                kind: "function",
                linkage,
                memory,
                behavior,
                inlineCost,
                hasOpenCalls,
            };
        }
        case 1: {
            const linkage = decodeLinkage(reader);

            return {
                kind: "global",
                linkage,
            };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one LinkNode. */
export function toJsonLinkNode(value: LinkNode): Json {
    switch (value.kind) {
        case "function":
            return {
                kind: "function",
                linkage: toJsonLinkage(value.linkage),
                memory: toJsonMemoryEffect(value.memory),
                behavior: toJsonFunctionBehavior(value.behavior),
                inlineCost: value.inlineCost,
                hasOpenCalls: value.hasOpenCalls,
            };
        case "global":
            return {
                kind: "global",
                linkage: toJsonLinkage(value.linkage),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one LinkNode from one JSON value. */
export function fromJsonLinkNode(value: Json): LinkNode {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "function":
            return {
                kind,
                linkage: fromJsonLinkage(jsonField(object, "linkage")),
                memory: fromJsonMemoryEffect(jsonField(object, "memory")),
                behavior: fromJsonFunctionBehavior(jsonField(object, "behavior")),
                inlineCost: jsonInteger(jsonField(object, "inlineCost")),
                hasOpenCalls: jsonBool(jsonField(object, "hasOpenCalls")),
            };
        case "global":
            return {
                kind,
                linkage: fromJsonLinkage(jsonField(object, "linkage")),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** Symbol references for one module. */
export type LinkTable = {
    /** Defined symbols and nodes sorted by identity. */
    readonly nodes: ReadonlyArray<readonly [Symbol, LinkNode]>;
    /** First outgoing edge offset for each node and the final edge count. */
    readonly offsets: ReadonlyArray<number>;
    /** Outgoing references grouped by source node. */
    readonly edges: ReadonlyArray<LinkEdge>;
};

export const LinkTable = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: LinkTable): void {
        encodeLinkTable(writer, value);
    },

    /** Decode one LinkTable. */
    decode(reader: BinaryReader): LinkTable {
        return decodeLinkTable(reader);
    },

    /** Return this value as JSON. */
    toJson(value: LinkTable): Json {
        return toJsonLinkTable(value);
    },

    /** Return one LinkTable from one JSON value. */
    fromJson(value: Json): LinkTable {
        return fromJsonLinkTable(value);
    },
};

/** Encode one LinkTable. */
export function encodeLinkTable(writer: BinaryWriter, value: LinkTable): void {
    writer.writeUnsigned(value.nodes.length);
    for (const item0 of value.nodes) {
        encodeSymbol(writer, item0[0]);
        encodeLinkNode(writer, item0[1]);
    }
    writer.writeUnsigned(value.offsets.length);
    for (const item1 of value.offsets) {
        writer.writeUnsigned(item1);
    }
    writer.writeUnsigned(value.edges.length);
    for (const item2 of value.edges) {
        encodeLinkEdge(writer, item2);
    }
}

/** Decode one LinkTable. */
export function decodeLinkTable(reader: BinaryReader): LinkTable {
    const nodes = (() => {
        const length0 = reader.readNumber();
        const items0: Array<readonly [Symbol, LinkNode]> = [];
        for (let index = 0; index < length0; index += 1) {
            items0.push([decodeSymbol(reader), decodeLinkNode(reader)] as const);
        }
        return items0;
    })();
    const offsets = (() => {
        const length1 = reader.readNumber();
        const items1: Array<number> = [];
        for (let index = 0; index < length1; index += 1) {
            items1.push(reader.readNumber());
        }
        return items1;
    })();
    const edges = (() => {
        const length2 = reader.readNumber();
        const items2: Array<LinkEdge> = [];
        for (let index = 0; index < length2; index += 1) {
            items2.push(decodeLinkEdge(reader));
        }
        return items2;
    })();

    return {
        nodes,
        offsets,
        edges,
    };
}

/** Return one JSON value for one LinkTable. */
export function toJsonLinkTable(value: LinkTable): Json {
    return {
        nodes: value.nodes.map((item0) => [toJsonSymbol(item0[0]), toJsonLinkNode(item0[1])]),
        offsets: value.offsets.map((item0) => item0),
        edges: value.edges.map((item0) => toJsonLinkEdge(item0)),
    };
}

/** Return one LinkTable from one JSON value. */
export function fromJsonLinkTable(value: Json): LinkTable {
    const object = jsonObject(value);

    return {
        nodes: jsonArray(jsonField(object, "nodes")).map((item0) =>
            (() => {
                const items = jsonArray(item0);
                if (items.length !== 2) {
                    throw new SerdeError(`expected JSON tuple length 2: ${items.length}`);
                }
                return [fromJsonSymbol(items[0]), fromJsonLinkNode(items[1])] as const;
            })(),
        ),
        offsets: jsonArray(jsonField(object, "offsets")).map((item0) => jsonInteger(item0)),
        edges: jsonArray(jsonField(object, "edges")).map((item0) => fromJsonLinkEdge(item0)),
    };
}
