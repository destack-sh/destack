/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, jsonField, jsonObject, jsonString } from "../../../protocol/serde.js";
import type { LocalNodeId } from "../tree/node.js";
import type { Value } from "../tree/value.js";
import { decodeLocalNodeId, encodeLocalNodeId, fromJsonLocalNodeId, toJsonLocalNodeId } from "../tree/node.js";
import { decodeValue, encodeValue, fromJsonValue, toJsonValue } from "../tree/value.js";

/** Root storage for one analyzed MIR place. */
export type PlaceOrigin =
    /** A function-local stack slot. */
    | {
          readonly kind: "local";
          readonly local: LocalNodeId;
      }
    /** A module global. */
    | {
          readonly kind: "global";
          readonly global: LocalNodeId;
      }
    /** Storage rooted in an SSA value. */
    | {
          readonly kind: "value";
          readonly value: Value;
      }
;

export const PlaceOrigin = {
    /** A function-local stack slot. */
    local(local: LocalNodeId): PlaceOrigin {
        return { kind: "local", local };
    },

    /** A module global. */
    global(global: LocalNodeId): PlaceOrigin {
        return { kind: "global", global };
    },

    /** Storage rooted in an SSA value. */
    value(value: Value): PlaceOrigin {
        return { kind: "value", value };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: PlaceOrigin): void {
        encodePlaceOrigin(writer, value);
    },

    /** Decode one PlaceOrigin. */
    decode(reader: BinaryReader): PlaceOrigin {
        return decodePlaceOrigin(reader);
    },

    /** Return this value as JSON. */
    toJson(value: PlaceOrigin): Json {
        return toJsonPlaceOrigin(value);
    },

    /** Return one PlaceOrigin from one JSON value. */
    fromJson(value: Json): PlaceOrigin {
        return fromJsonPlaceOrigin(value);
    },
};

/** Encode one PlaceOrigin. */
export function encodePlaceOrigin(writer: BinaryWriter, value: PlaceOrigin): void {
    switch (value.kind) {
        case "local":
            writer.writeUnsigned(0);
            encodeLocalNodeId(writer, value.local);
            return;
        case "global":
            writer.writeUnsigned(1);
            encodeLocalNodeId(writer, value.global);
            return;
        case "value":
            writer.writeUnsigned(2);
            encodeValue(writer, value.value);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one PlaceOrigin. */
export function decodePlaceOrigin(reader: BinaryReader): PlaceOrigin {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const local = decodeLocalNodeId(reader);

            return { kind: "local", local };
        }
        case 1: {
            const global = decodeLocalNodeId(reader);

            return { kind: "global", global };
        }
        case 2: {
            const value = decodeValue(reader);

            return { kind: "value", value };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one PlaceOrigin. */
export function toJsonPlaceOrigin(value: PlaceOrigin): Json {
    switch (value.kind) {
        case "local":
            return {
                kind: "local",
                local: toJsonLocalNodeId(value.local),
            };
        case "global":
            return {
                kind: "global",
                global: toJsonLocalNodeId(value.global),
            };
        case "value":
            return {
                kind: "value",
                value: toJsonValue(value.value),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one PlaceOrigin from one JSON value. */
export function fromJsonPlaceOrigin(value: Json): PlaceOrigin {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "local":
            return {
                kind,
                local: fromJsonLocalNodeId(jsonField(object, "local")),
            };
        case "global":
            return {
                kind,
                global: fromJsonLocalNodeId(jsonField(object, "global")),
            };
        case "value":
            return {
                kind,
                value: fromJsonValue(jsonField(object, "value")),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}
