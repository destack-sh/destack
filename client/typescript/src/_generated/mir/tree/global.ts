/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, bytesFromJson, bytesToJson, jsonArray, jsonField, jsonObject, jsonOptional, jsonString } from "../../../protocol/serde.js";
import type { StringId } from "../../core/string.js";
import type { Constant } from "./constant.js";
import type { LocalNodeId } from "./node.js";
import type { Symbol } from "./symbol.js";
import type { Mutability } from "./type.js";
import type { Space } from "./type.js";
import { decodeStringId, encodeStringId, fromJsonStringId, toJsonStringId } from "../../core/string.js";
import { decodeConstant, encodeConstant, fromJsonConstant, toJsonConstant } from "./constant.js";
import { decodeLocalNodeId, encodeLocalNodeId, fromJsonLocalNodeId, toJsonLocalNodeId } from "./node.js";
import { decodeSymbol, encodeSymbol, fromJsonSymbol, toJsonSymbol } from "./symbol.js";
import { decodeMutability, encodeMutability, fromJsonMutability, toJsonMutability } from "./type.js";
import { decodeSpace, encodeSpace, fromJsonSpace, toJsonSpace } from "./type.js";

/** Symbol linkage (visibility and definition location). */
export type Linkage = "local" | "export" | "import";

export const Linkage = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: Linkage): void {
        encodeLinkage(writer, value);
    },

    /** Decode one Linkage. */
    decode(reader: BinaryReader): Linkage {
        return decodeLinkage(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Linkage): Json {
        return toJsonLinkage(value);
    },

    /** Return one Linkage from one JSON value. */
    fromJson(value: Json): Linkage {
        return fromJsonLinkage(value);
    },
};

/** Encode one Linkage. */
export function encodeLinkage(writer: BinaryWriter, value: Linkage): void {
    switch (value) {
        case "local":
            writer.writeUnsigned(0);
            return;
        case "export":
            writer.writeUnsigned(1);
            return;
        case "import":
            writer.writeUnsigned(2);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one Linkage. */
export function decodeLinkage(reader: BinaryReader): Linkage {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "local";
        case 1:
            return "export";
        case 2:
            return "import";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one Linkage. */
export function toJsonLinkage(value: Linkage): Json {
    return value;
}

/** Return one Linkage from one JSON value. */
export function fromJsonLinkage(value: Json): Linkage {
    const variant = jsonString(value);

    switch (variant) {
        case "local":
            return "local";
        case "export":
            return "export";
        case "import":
            return "import";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** Global data definition (module-level variable or constant). */
export type Global = {
    /** Name for linking and debugging. */
    readonly name: StringId;
    /** The global's persistent mangled symbol: its linkable identity. */
    readonly symbol: Symbol;
    /** The type of the global. */
    readonly ty: LocalNodeId;
    /** Whether this global is mutable. */
    readonly mutability: Mutability;
    /** The space that owns this global storage. */
    readonly space: Space;
    /** Linkage (local, export, or import). */
    readonly linkage: Linkage;
    /** Initial value. None for imported globals. */
    readonly initializer?: GlobalInitializer;
};

export const Global = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: Global): void {
        encodeGlobal(writer, value);
    },

    /** Decode one Global. */
    decode(reader: BinaryReader): Global {
        return decodeGlobal(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Global): Json {
        return toJsonGlobal(value);
    },

    /** Return one Global from one JSON value. */
    fromJson(value: Json): Global {
        return fromJsonGlobal(value);
    },
};

/** Encode one Global. */
export function encodeGlobal(writer: BinaryWriter, value: Global): void {
    encodeStringId(writer, value.name);
    encodeSymbol(writer, value.symbol);
    encodeLocalNodeId(writer, value.ty);
    encodeMutability(writer, value.mutability);
    encodeSpace(writer, value.space);
    encodeLinkage(writer, value.linkage);
    writer.writeOption(value.initializer, (value6) => {
        encodeGlobalInitializer(writer, value6);
    });
}

/** Decode one Global. */
export function decodeGlobal(reader: BinaryReader): Global {
    const name = decodeStringId(reader);
    const symbol_ = decodeSymbol(reader);
    const ty = decodeLocalNodeId(reader);
    const mutability = decodeMutability(reader);
    const space = decodeSpace(reader);
    const linkage = decodeLinkage(reader);
    const initializer = reader.readOption(() => decodeGlobalInitializer(reader));

    return {
        name,
        symbol: symbol_,
        ty,
        mutability,
        space,
        linkage,
        ...(initializer === undefined ? {} : { initializer }),
    };
}

/** Return one JSON value for one Global. */
export function toJsonGlobal(value: Global): Json {
    return {
        name: toJsonStringId(value.name),
        symbol: toJsonSymbol(value.symbol),
        ty: toJsonLocalNodeId(value.ty),
        mutability: toJsonMutability(value.mutability),
        space: toJsonSpace(value.space),
        linkage: toJsonLinkage(value.linkage),
        ...(value.initializer === undefined ? {} : { initializer: toJsonGlobalInitializer(value.initializer) }),
    };
}

/** Return one Global from one JSON value. */
export function fromJsonGlobal(value: Json): Global {
    const object = jsonObject(value);

    return {
        name: fromJsonStringId(jsonField(object, "name")),
        symbol: fromJsonSymbol(jsonField(object, "symbol")),
        ty: fromJsonLocalNodeId(jsonField(object, "ty")),
        mutability: fromJsonMutability(jsonField(object, "mutability")),
        space: fromJsonSpace(jsonField(object, "space")),
        linkage: fromJsonLinkage(jsonField(object, "linkage")),
        initializer: jsonOptional(object, "initializer", (value) => fromJsonGlobalInitializer(value)),
    };
}

/** Initializer for global data. */
export type GlobalInitializer =
    /** Zero-initialized (all bytes zero). */
    | {
          readonly kind: "zero";
      }
    /** Scalar constant (bool, int, float). */
    | {
          readonly kind: "scalar";
          readonly scalar: Constant;
      }
    /** Address of one function inside the program. */
    | {
          readonly kind: "functionAddress";
          readonly function_address: LocalNodeId;
      }
    /** Raw bytes (blobs). */
    | {
          readonly kind: "bytes";
          readonly bytes: Uint8Array | readonly number[];
      }
    /** Aggregate (array/struct fields). */
    | {
          readonly kind: "aggregate";
          readonly aggregate: ReadonlyArray<GlobalInitializer>;
      }
;

export const GlobalInitializer = {
    /** Zero-initialized (all bytes zero). */
    zero(): GlobalInitializer {
        return { kind: "zero" };
    },

    /** Scalar constant (bool, int, float). */
    scalar(scalar: Constant): GlobalInitializer {
        return { kind: "scalar", scalar };
    },

    /** Address of one function inside the program. */
    functionAddress(function_address: LocalNodeId): GlobalInitializer {
        return { kind: "functionAddress", function_address };
    },

    /** Raw bytes (blobs). */
    bytes(bytes: Uint8Array | readonly number[]): GlobalInitializer {
        return { kind: "bytes", bytes };
    },

    /** Aggregate (array/struct fields). */
    aggregate(aggregate: ReadonlyArray<GlobalInitializer>): GlobalInitializer {
        return { kind: "aggregate", aggregate };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: GlobalInitializer): void {
        encodeGlobalInitializer(writer, value);
    },

    /** Decode one GlobalInitializer. */
    decode(reader: BinaryReader): GlobalInitializer {
        return decodeGlobalInitializer(reader);
    },

    /** Return this value as JSON. */
    toJson(value: GlobalInitializer): Json {
        return toJsonGlobalInitializer(value);
    },

    /** Return one GlobalInitializer from one JSON value. */
    fromJson(value: Json): GlobalInitializer {
        return fromJsonGlobalInitializer(value);
    },
};

/** Encode one GlobalInitializer. */
export function encodeGlobalInitializer(writer: BinaryWriter, value: GlobalInitializer): void {
    switch (value.kind) {
        case "zero":
            writer.writeUnsigned(0);
            return;
        case "scalar":
            writer.writeUnsigned(1);
            encodeConstant(writer, value.scalar);
            return;
        case "functionAddress":
            writer.writeUnsigned(2);
            encodeLocalNodeId(writer, value.function_address);
            return;
        case "bytes":
            writer.writeUnsigned(3);
            writer.writeByteSlice(value.bytes);
            return;
        case "aggregate":
            writer.writeUnsigned(4);
            writer.writeUnsigned(value.aggregate.length);
            for (const item0 of value.aggregate) {
                encodeGlobalInitializer(writer, item0);
            }
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one GlobalInitializer. */
export function decodeGlobalInitializer(reader: BinaryReader): GlobalInitializer {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            return { kind: "zero" };
        }
        case 1: {
            const scalar = decodeConstant(reader);

            return { kind: "scalar", scalar };
        }
        case 2: {
            const function_address = decodeLocalNodeId(reader);

            return { kind: "functionAddress", function_address };
        }
        case 3: {
            const bytes = reader.readByteSlice();

            return { kind: "bytes", bytes };
        }
        case 4: {
            const aggregate = (() => { const length0 = reader.readNumber(); const items0: Array<GlobalInitializer> = []; for (let index = 0; index < length0; index += 1) { items0.push(decodeGlobalInitializer(reader)); } return items0; })();

            return { kind: "aggregate", aggregate };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one GlobalInitializer. */
export function toJsonGlobalInitializer(value: GlobalInitializer): Json {
    switch (value.kind) {
        case "zero":
            return {
                kind: "zero",
            };
        case "scalar":
            return {
                kind: "scalar",
                scalar: toJsonConstant(value.scalar),
            };
        case "functionAddress":
            return {
                kind: "functionAddress",
                function_address: toJsonLocalNodeId(value.function_address),
            };
        case "bytes":
            return {
                kind: "bytes",
                bytes: bytesToJson(value.bytes),
            };
        case "aggregate":
            return {
                kind: "aggregate",
                aggregate: value.aggregate.map((item0) => toJsonGlobalInitializer(item0)),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one GlobalInitializer from one JSON value. */
export function fromJsonGlobalInitializer(value: Json): GlobalInitializer {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "zero":
            return {
                kind,
            };
        case "scalar":
            return {
                kind,
                scalar: fromJsonConstant(jsonField(object, "scalar")),
            };
        case "functionAddress":
            return {
                kind,
                function_address: fromJsonLocalNodeId(jsonField(object, "function_address")),
            };
        case "bytes":
            return {
                kind,
                bytes: bytesFromJson(jsonField(object, "bytes")),
            };
        case "aggregate":
            return {
                kind,
                aggregate: jsonArray(jsonField(object, "aggregate")).map((item0) => fromJsonGlobalInitializer(item0)),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}
