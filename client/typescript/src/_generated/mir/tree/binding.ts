/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, jsonArray, jsonBool, jsonField, jsonObject, jsonString } from "../../../protocol/serde.js";
import type { StringId } from "../../core/string.js";
import { decodeStringId, encodeStringId, fromJsonStringId, toJsonStringId } from "../../core/string.js";

/** One runtime binding declaration. */
export type Binding = {
    /** Stable binding name. */
    readonly name: StringId;
    /** Binding implementation owner. */
    readonly provider: BindingProvider;
    /** Observable effect class. */
    readonly effect: BindingEffect;
    /** Replay behavior. */
    readonly replay: BindingReplay;
    /** Required execution context. */
    readonly affinity: BindingAffinity;
    /** Whether calls through this binding park the calling fiber. */
    readonly isPark: boolean;
    /** Required runtime actions. */
    readonly requires: ReadonlyArray<StringId>;
    /** Supported platforms. */
    readonly platforms: ReadonlyArray<StringId>;
    /** Supported platform families. */
    readonly families: ReadonlyArray<StringId>;
    /** Supported hosts. */
    readonly hosts: ReadonlyArray<StringId>;
};

export const Binding = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: Binding): void {
        encodeBinding(writer, value);
    },

    /** Decode one Binding. */
    decode(reader: BinaryReader): Binding {
        return decodeBinding(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Binding): Json {
        return toJsonBinding(value);
    },

    /** Return one Binding from one JSON value. */
    fromJson(value: Json): Binding {
        return fromJsonBinding(value);
    },
};

/** Encode one Binding. */
export function encodeBinding(writer: BinaryWriter, value: Binding): void {
    encodeStringId(writer, value.name);
    encodeBindingProvider(writer, value.provider);
    encodeBindingEffect(writer, value.effect);
    encodeBindingReplay(writer, value.replay);
    encodeBindingAffinity(writer, value.affinity);
    writer.writeBool(value.isPark);
    writer.writeUnsigned(value.requires.length);
    for (const item6 of value.requires) {
        encodeStringId(writer, item6);
    }
    writer.writeUnsigned(value.platforms.length);
    for (const item7 of value.platforms) {
        encodeStringId(writer, item7);
    }
    writer.writeUnsigned(value.families.length);
    for (const item8 of value.families) {
        encodeStringId(writer, item8);
    }
    writer.writeUnsigned(value.hosts.length);
    for (const item9 of value.hosts) {
        encodeStringId(writer, item9);
    }
}

/** Decode one Binding. */
export function decodeBinding(reader: BinaryReader): Binding {
    const name = decodeStringId(reader);
    const provider = decodeBindingProvider(reader);
    const effect = decodeBindingEffect(reader);
    const replay = decodeBindingReplay(reader);
    const affinity = decodeBindingAffinity(reader);
    const isPark = reader.readBool();
    const requires = (() => { const length6 = reader.readNumber(); const items6: Array<StringId> = []; for (let index = 0; index < length6; index += 1) { items6.push(decodeStringId(reader)); } return items6; })();
    const platforms = (() => { const length7 = reader.readNumber(); const items7: Array<StringId> = []; for (let index = 0; index < length7; index += 1) { items7.push(decodeStringId(reader)); } return items7; })();
    const families = (() => { const length8 = reader.readNumber(); const items8: Array<StringId> = []; for (let index = 0; index < length8; index += 1) { items8.push(decodeStringId(reader)); } return items8; })();
    const hosts = (() => { const length9 = reader.readNumber(); const items9: Array<StringId> = []; for (let index = 0; index < length9; index += 1) { items9.push(decodeStringId(reader)); } return items9; })();

    return {
        name,
        provider,
        effect,
        replay,
        affinity,
        isPark,
        requires,
        platforms,
        families,
        hosts,
    };
}

/** Return one JSON value for one Binding. */
export function toJsonBinding(value: Binding): Json {
    return {
        name: toJsonStringId(value.name),
        provider: toJsonBindingProvider(value.provider),
        effect: toJsonBindingEffect(value.effect),
        replay: toJsonBindingReplay(value.replay),
        affinity: toJsonBindingAffinity(value.affinity),
        isPark: value.isPark,
        requires: value.requires.map((item0) => toJsonStringId(item0)),
        platforms: value.platforms.map((item0) => toJsonStringId(item0)),
        families: value.families.map((item0) => toJsonStringId(item0)),
        hosts: value.hosts.map((item0) => toJsonStringId(item0)),
    };
}

/** Return one Binding from one JSON value. */
export function fromJsonBinding(value: Json): Binding {
    const object = jsonObject(value);

    return {
        name: fromJsonStringId(jsonField(object, "name")),
        provider: fromJsonBindingProvider(jsonField(object, "provider")),
        effect: fromJsonBindingEffect(jsonField(object, "effect")),
        replay: fromJsonBindingReplay(jsonField(object, "replay")),
        affinity: fromJsonBindingAffinity(jsonField(object, "affinity")),
        isPark: jsonBool(jsonField(object, "isPark")),
        requires: jsonArray(jsonField(object, "requires")).map((item0) => fromJsonStringId(item0)),
        platforms: jsonArray(jsonField(object, "platforms")).map((item0) => fromJsonStringId(item0)),
        families: jsonArray(jsonField(object, "families")).map((item0) => fromJsonStringId(item0)),
        hosts: jsonArray(jsonField(object, "hosts")).map((item0) => fromJsonStringId(item0)),
    };
}

/** Execution context required by one binding. */
export type BindingAffinity = "none" | "worker" | "main";

export const BindingAffinity = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: BindingAffinity): void {
        encodeBindingAffinity(writer, value);
    },

    /** Decode one BindingAffinity. */
    decode(reader: BinaryReader): BindingAffinity {
        return decodeBindingAffinity(reader);
    },

    /** Return this value as JSON. */
    toJson(value: BindingAffinity): Json {
        return toJsonBindingAffinity(value);
    },

    /** Return one BindingAffinity from one JSON value. */
    fromJson(value: Json): BindingAffinity {
        return fromJsonBindingAffinity(value);
    },
};

/** Encode one BindingAffinity. */
export function encodeBindingAffinity(writer: BinaryWriter, value: BindingAffinity): void {
    switch (value) {
        case "none":
            writer.writeUnsigned(0);
            return;
        case "worker":
            writer.writeUnsigned(1);
            return;
        case "main":
            writer.writeUnsigned(2);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one BindingAffinity. */
export function decodeBindingAffinity(reader: BinaryReader): BindingAffinity {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "none";
        case 1:
            return "worker";
        case 2:
            return "main";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one BindingAffinity. */
export function toJsonBindingAffinity(value: BindingAffinity): Json {
    return value;
}

/** Return one BindingAffinity from one JSON value. */
export function fromJsonBindingAffinity(value: Json): BindingAffinity {
    const variant = jsonString(value);

    switch (variant) {
        case "none":
            return "none";
        case "worker":
            return "worker";
        case "main":
            return "main";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** Observable effect class for one binding. */
export type BindingEffect = "pure" | "deterministic" | "external";

export const BindingEffect = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: BindingEffect): void {
        encodeBindingEffect(writer, value);
    },

    /** Decode one BindingEffect. */
    decode(reader: BinaryReader): BindingEffect {
        return decodeBindingEffect(reader);
    },

    /** Return this value as JSON. */
    toJson(value: BindingEffect): Json {
        return toJsonBindingEffect(value);
    },

    /** Return one BindingEffect from one JSON value. */
    fromJson(value: Json): BindingEffect {
        return fromJsonBindingEffect(value);
    },
};

/** Encode one BindingEffect. */
export function encodeBindingEffect(writer: BinaryWriter, value: BindingEffect): void {
    switch (value) {
        case "pure":
            writer.writeUnsigned(0);
            return;
        case "deterministic":
            writer.writeUnsigned(1);
            return;
        case "external":
            writer.writeUnsigned(2);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one BindingEffect. */
export function decodeBindingEffect(reader: BinaryReader): BindingEffect {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "pure";
        case 1:
            return "deterministic";
        case 2:
            return "external";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one BindingEffect. */
export function toJsonBindingEffect(value: BindingEffect): Json {
    return value;
}

/** Return one BindingEffect from one JSON value. */
export function fromJsonBindingEffect(value: Json): BindingEffect {
    const variant = jsonString(value);

    switch (variant) {
        case "pure":
            return "pure";
        case "deterministic":
            return "deterministic";
        case "external":
            return "external";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** Owner that implements one binding. */
export type BindingProvider = "host" | "runtime";

export const BindingProvider = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: BindingProvider): void {
        encodeBindingProvider(writer, value);
    },

    /** Decode one BindingProvider. */
    decode(reader: BinaryReader): BindingProvider {
        return decodeBindingProvider(reader);
    },

    /** Return this value as JSON. */
    toJson(value: BindingProvider): Json {
        return toJsonBindingProvider(value);
    },

    /** Return one BindingProvider from one JSON value. */
    fromJson(value: Json): BindingProvider {
        return fromJsonBindingProvider(value);
    },
};

/** Encode one BindingProvider. */
export function encodeBindingProvider(writer: BinaryWriter, value: BindingProvider): void {
    switch (value) {
        case "host":
            writer.writeUnsigned(0);
            return;
        case "runtime":
            writer.writeUnsigned(1);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one BindingProvider. */
export function decodeBindingProvider(reader: BinaryReader): BindingProvider {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "host";
        case 1:
            return "runtime";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one BindingProvider. */
export function toJsonBindingProvider(value: BindingProvider): Json {
    return value;
}

/** Return one BindingProvider from one JSON value. */
export function fromJsonBindingProvider(value: Json): BindingProvider {
    const variant = jsonString(value);

    switch (variant) {
        case "host":
            return "host";
        case "runtime":
            return "runtime";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** Replay behavior for one binding. */
export type BindingReplay = "recordable" | "forbidden";

export const BindingReplay = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: BindingReplay): void {
        encodeBindingReplay(writer, value);
    },

    /** Decode one BindingReplay. */
    decode(reader: BinaryReader): BindingReplay {
        return decodeBindingReplay(reader);
    },

    /** Return this value as JSON. */
    toJson(value: BindingReplay): Json {
        return toJsonBindingReplay(value);
    },

    /** Return one BindingReplay from one JSON value. */
    fromJson(value: Json): BindingReplay {
        return fromJsonBindingReplay(value);
    },
};

/** Encode one BindingReplay. */
export function encodeBindingReplay(writer: BinaryWriter, value: BindingReplay): void {
    switch (value) {
        case "recordable":
            writer.writeUnsigned(0);
            return;
        case "forbidden":
            writer.writeUnsigned(1);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one BindingReplay. */
export function decodeBindingReplay(reader: BinaryReader): BindingReplay {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "recordable";
        case 1:
            return "forbidden";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one BindingReplay. */
export function toJsonBindingReplay(value: BindingReplay): Json {
    return value;
}

/** Return one BindingReplay from one JSON value. */
export function fromJsonBindingReplay(value: Json): BindingReplay {
    const variant = jsonString(value);

    switch (variant) {
        case "recordable":
            return "recordable";
        case "forbidden":
            return "forbidden";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}
