/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, jsonField, jsonInteger, jsonObject, jsonString } from "../../../protocol/serde.js";

/** Compact reference to an index list stored in the MIR tree. */
export type IndexSlice = {
    /** Start index in the index buffer. */
    readonly start: number;
    /** Number of indices in the slice. */
    readonly count: number;
};

export const IndexSlice = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: IndexSlice): void {
        encodeIndexSlice(writer, value);
    },

    /** Decode one IndexSlice. */
    decode(reader: BinaryReader): IndexSlice {
        return decodeIndexSlice(reader);
    },

    /** Return this value as JSON. */
    toJson(value: IndexSlice): Json {
        return toJsonIndexSlice(value);
    },

    /** Return one IndexSlice from one JSON value. */
    fromJson(value: Json): IndexSlice {
        return fromJsonIndexSlice(value);
    },
};

/** Encode one IndexSlice. */
export function encodeIndexSlice(writer: BinaryWriter, value: IndexSlice): void {
    writer.writeUnsigned(value.start);
    writer.writeUnsigned(value.count);
}

/** Decode one IndexSlice. */
export function decodeIndexSlice(reader: BinaryReader): IndexSlice {
    const start = reader.readNumber();
    const count = reader.readNumber();

    return {
        start,
        count,
    };
}

/** Return one JSON value for one IndexSlice. */
export function toJsonIndexSlice(value: IndexSlice): Json {
    return {
        start: value.start,
        count: value.count,
    };
}

/** Return one IndexSlice from one JSON value. */
export function fromJsonIndexSlice(value: Json): IndexSlice {
    const object = jsonObject(value);

    return {
        start: jsonInteger(jsonField(object, "start")),
        count: jsonInteger(jsonField(object, "count")),
    };
}

/** Compact identity for one tensor immediate stored in the MIR tree. */
export type TensorImmediateId = number;

export const TensorImmediateId = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: TensorImmediateId): void {
        encodeTensorImmediateId(writer, value);
    },

    /** Decode one TensorImmediateId. */
    decode(reader: BinaryReader): TensorImmediateId {
        return decodeTensorImmediateId(reader);
    },

    /** Return this value as JSON. */
    toJson(value: TensorImmediateId): Json {
        return toJsonTensorImmediateId(value);
    },

    /** Return one TensorImmediateId from one JSON value. */
    fromJson(value: Json): TensorImmediateId {
        return fromJsonTensorImmediateId(value);
    },
};

/** Encode one TensorImmediateId. */
export function encodeTensorImmediateId(writer: BinaryWriter, value: TensorImmediateId): void {
    writer.writeUnsigned(value);
}

/** Decode one TensorImmediateId. */
export function decodeTensorImmediateId(reader: BinaryReader): TensorImmediateId {
    return reader.readNumber();
}

/** Return one JSON value for one TensorImmediateId. */
export function toJsonTensorImmediateId(value: TensorImmediateId): Json {
    return value;
}

/** Return one TensorImmediateId from one JSON value. */
export function fromJsonTensorImmediateId(value: Json): TensorImmediateId {
    return jsonInteger(value);
}

/** Static tensor instruction immediate. */
export type TensorImmediate =
    /** Dimension numbers for a tensor dot product. */
    | {
          readonly kind: "dot";
          /** Batch dimensions on the left operand. */
          readonly lhsBatch: IndexSlice;
          /** Batch dimensions on the right operand. */
          readonly rhsBatch: IndexSlice;
          /** Contracting dimensions on the left operand. */
          readonly lhsContracting: IndexSlice;
          /** Contracting dimensions on the right operand. */
          readonly rhsContracting: IndexSlice;
      }
    /** Dimension numbers and window parameters for tensor convolution. */
    | {
          readonly kind: "convolution";
          /** Batch dimension for the input tensor. */
          readonly inputBatch: number;
          /** Feature dimension for the input tensor. */
          readonly inputFeature: number;
          /** Spatial dimensions for the input tensor. */
          readonly inputSpatial: IndexSlice;
          /** Input feature dimension for the kernel tensor. */
          readonly kernelInputFeature: number;
          /** Output feature dimension for the kernel tensor. */
          readonly kernelOutputFeature: number;
          /** Spatial dimensions for the kernel tensor. */
          readonly kernelSpatial: IndexSlice;
          /** Batch dimension for the output tensor. */
          readonly outputBatch: number;
          /** Feature dimension for the output tensor. */
          readonly outputFeature: number;
          /** Spatial dimensions for the output tensor. */
          readonly outputSpatial: IndexSlice;
          /** Stride for each spatial dimension. */
          readonly strides: ExtentSlice;
          /** Padding at the low end for each spatial dimension. */
          readonly paddingLow: ExtentSlice;
          /** Padding at the high end for each spatial dimension. */
          readonly paddingHigh: ExtentSlice;
          /** Input dilation for each spatial dimension. */
          readonly lhsDilation: ExtentSlice;
          /** Kernel dilation for each spatial dimension. */
          readonly rhsDilation: ExtentSlice;
          /** Whether each spatial dimension is reversed. */
          readonly windowReversal: FlagSlice;
          /** Number of feature groups. */
          readonly featureGroupCount: number;
          /** Number of batch groups. */
          readonly batchGroupCount: number;
      }
    /** Dimension numbers for tensor gather. */
    | {
          readonly kind: "gather";
          /** Offset dimensions in the output. */
          readonly offsetDims: IndexSlice;
          /** Collapsed slice dimensions in the operand. */
          readonly collapsedSliceDims: IndexSlice;
          /** Mapping from index components to operand dimensions. */
          readonly startIndexMap: IndexSlice;
          /** Index vector dimension in the indices tensor. */
          readonly indexVectorDim: number;
          /** Slice sizes for each operand dimension. */
          readonly sliceSizes: IndexSlice;
      }
    /** Dimension numbers for tensor scatter. */
    | {
          readonly kind: "scatter";
          /** Dimensions of the update window in the updates tensor. */
          readonly updateWindowDims: IndexSlice;
          /** Dimensions inserted into the operand shape. */
          readonly insertedWindowDims: IndexSlice;
          /** Mapping from scatter indices to operand dimensions. */
          readonly scatterDimsToOperandDims: IndexSlice;
          /** Index vector dimension in the indices tensor. */
          readonly indexVectorDim: number;
      }
;

export const TensorImmediate = {
    /** Dimension numbers for a tensor dot product. */
    dot(lhsBatch: IndexSlice, rhsBatch: IndexSlice, lhsContracting: IndexSlice, rhsContracting: IndexSlice): TensorImmediate {
        return { kind: "dot", lhsBatch, rhsBatch, lhsContracting, rhsContracting };
    },

    /** Dimension numbers and window parameters for tensor convolution. */
    convolution(inputBatch: number, inputFeature: number, inputSpatial: IndexSlice, kernelInputFeature: number, kernelOutputFeature: number, kernelSpatial: IndexSlice, outputBatch: number, outputFeature: number, outputSpatial: IndexSlice, strides: ExtentSlice, paddingLow: ExtentSlice, paddingHigh: ExtentSlice, lhsDilation: ExtentSlice, rhsDilation: ExtentSlice, windowReversal: FlagSlice, featureGroupCount: number, batchGroupCount: number): TensorImmediate {
        return { kind: "convolution", inputBatch, inputFeature, inputSpatial, kernelInputFeature, kernelOutputFeature, kernelSpatial, outputBatch, outputFeature, outputSpatial, strides, paddingLow, paddingHigh, lhsDilation, rhsDilation, windowReversal, featureGroupCount, batchGroupCount };
    },

    /** Dimension numbers for tensor gather. */
    gather(offsetDims: IndexSlice, collapsedSliceDims: IndexSlice, startIndexMap: IndexSlice, indexVectorDim: number, sliceSizes: IndexSlice): TensorImmediate {
        return { kind: "gather", offsetDims, collapsedSliceDims, startIndexMap, indexVectorDim, sliceSizes };
    },

    /** Dimension numbers for tensor scatter. */
    scatter(updateWindowDims: IndexSlice, insertedWindowDims: IndexSlice, scatterDimsToOperandDims: IndexSlice, indexVectorDim: number): TensorImmediate {
        return { kind: "scatter", updateWindowDims, insertedWindowDims, scatterDimsToOperandDims, indexVectorDim };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: TensorImmediate): void {
        encodeTensorImmediate(writer, value);
    },

    /** Decode one TensorImmediate. */
    decode(reader: BinaryReader): TensorImmediate {
        return decodeTensorImmediate(reader);
    },

    /** Return this value as JSON. */
    toJson(value: TensorImmediate): Json {
        return toJsonTensorImmediate(value);
    },

    /** Return one TensorImmediate from one JSON value. */
    fromJson(value: Json): TensorImmediate {
        return fromJsonTensorImmediate(value);
    },
};

/** Encode one TensorImmediate. */
export function encodeTensorImmediate(writer: BinaryWriter, value: TensorImmediate): void {
    switch (value.kind) {
        case "dot":
            writer.writeUnsigned(0);
            encodeIndexSlice(writer, value.lhsBatch);
            encodeIndexSlice(writer, value.rhsBatch);
            encodeIndexSlice(writer, value.lhsContracting);
            encodeIndexSlice(writer, value.rhsContracting);
            return;
        case "convolution":
            writer.writeUnsigned(1);
            writer.writeUnsigned(value.inputBatch);
            writer.writeUnsigned(value.inputFeature);
            encodeIndexSlice(writer, value.inputSpatial);
            writer.writeUnsigned(value.kernelInputFeature);
            writer.writeUnsigned(value.kernelOutputFeature);
            encodeIndexSlice(writer, value.kernelSpatial);
            writer.writeUnsigned(value.outputBatch);
            writer.writeUnsigned(value.outputFeature);
            encodeIndexSlice(writer, value.outputSpatial);
            encodeExtentSlice(writer, value.strides);
            encodeExtentSlice(writer, value.paddingLow);
            encodeExtentSlice(writer, value.paddingHigh);
            encodeExtentSlice(writer, value.lhsDilation);
            encodeExtentSlice(writer, value.rhsDilation);
            encodeFlagSlice(writer, value.windowReversal);
            writer.writeUnsigned(value.featureGroupCount);
            writer.writeUnsigned(value.batchGroupCount);
            return;
        case "gather":
            writer.writeUnsigned(2);
            encodeIndexSlice(writer, value.offsetDims);
            encodeIndexSlice(writer, value.collapsedSliceDims);
            encodeIndexSlice(writer, value.startIndexMap);
            writer.writeUnsigned(value.indexVectorDim);
            encodeIndexSlice(writer, value.sliceSizes);
            return;
        case "scatter":
            writer.writeUnsigned(3);
            encodeIndexSlice(writer, value.updateWindowDims);
            encodeIndexSlice(writer, value.insertedWindowDims);
            encodeIndexSlice(writer, value.scatterDimsToOperandDims);
            writer.writeUnsigned(value.indexVectorDim);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one TensorImmediate. */
export function decodeTensorImmediate(reader: BinaryReader): TensorImmediate {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const lhsBatch = decodeIndexSlice(reader);
            const rhsBatch = decodeIndexSlice(reader);
            const lhsContracting = decodeIndexSlice(reader);
            const rhsContracting = decodeIndexSlice(reader);

            return {
                kind: "dot",
                lhsBatch,
                rhsBatch,
                lhsContracting,
                rhsContracting,
            };
        }
        case 1: {
            const inputBatch = reader.readNumber();
            const inputFeature = reader.readNumber();
            const inputSpatial = decodeIndexSlice(reader);
            const kernelInputFeature = reader.readNumber();
            const kernelOutputFeature = reader.readNumber();
            const kernelSpatial = decodeIndexSlice(reader);
            const outputBatch = reader.readNumber();
            const outputFeature = reader.readNumber();
            const outputSpatial = decodeIndexSlice(reader);
            const strides = decodeExtentSlice(reader);
            const paddingLow = decodeExtentSlice(reader);
            const paddingHigh = decodeExtentSlice(reader);
            const lhsDilation = decodeExtentSlice(reader);
            const rhsDilation = decodeExtentSlice(reader);
            const windowReversal = decodeFlagSlice(reader);
            const featureGroupCount = reader.readNumber();
            const batchGroupCount = reader.readNumber();

            return {
                kind: "convolution",
                inputBatch,
                inputFeature,
                inputSpatial,
                kernelInputFeature,
                kernelOutputFeature,
                kernelSpatial,
                outputBatch,
                outputFeature,
                outputSpatial,
                strides,
                paddingLow,
                paddingHigh,
                lhsDilation,
                rhsDilation,
                windowReversal,
                featureGroupCount,
                batchGroupCount,
            };
        }
        case 2: {
            const offsetDims = decodeIndexSlice(reader);
            const collapsedSliceDims = decodeIndexSlice(reader);
            const startIndexMap = decodeIndexSlice(reader);
            const indexVectorDim = reader.readNumber();
            const sliceSizes = decodeIndexSlice(reader);

            return {
                kind: "gather",
                offsetDims,
                collapsedSliceDims,
                startIndexMap,
                indexVectorDim,
                sliceSizes,
            };
        }
        case 3: {
            const updateWindowDims = decodeIndexSlice(reader);
            const insertedWindowDims = decodeIndexSlice(reader);
            const scatterDimsToOperandDims = decodeIndexSlice(reader);
            const indexVectorDim = reader.readNumber();

            return {
                kind: "scatter",
                updateWindowDims,
                insertedWindowDims,
                scatterDimsToOperandDims,
                indexVectorDim,
            };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one TensorImmediate. */
export function toJsonTensorImmediate(value: TensorImmediate): Json {
    switch (value.kind) {
        case "dot":
            return {
                kind: "dot",
                lhsBatch: toJsonIndexSlice(value.lhsBatch),
                rhsBatch: toJsonIndexSlice(value.rhsBatch),
                lhsContracting: toJsonIndexSlice(value.lhsContracting),
                rhsContracting: toJsonIndexSlice(value.rhsContracting),
            };
        case "convolution":
            return {
                kind: "convolution",
                inputBatch: value.inputBatch,
                inputFeature: value.inputFeature,
                inputSpatial: toJsonIndexSlice(value.inputSpatial),
                kernelInputFeature: value.kernelInputFeature,
                kernelOutputFeature: value.kernelOutputFeature,
                kernelSpatial: toJsonIndexSlice(value.kernelSpatial),
                outputBatch: value.outputBatch,
                outputFeature: value.outputFeature,
                outputSpatial: toJsonIndexSlice(value.outputSpatial),
                strides: toJsonExtentSlice(value.strides),
                paddingLow: toJsonExtentSlice(value.paddingLow),
                paddingHigh: toJsonExtentSlice(value.paddingHigh),
                lhsDilation: toJsonExtentSlice(value.lhsDilation),
                rhsDilation: toJsonExtentSlice(value.rhsDilation),
                windowReversal: toJsonFlagSlice(value.windowReversal),
                featureGroupCount: value.featureGroupCount,
                batchGroupCount: value.batchGroupCount,
            };
        case "gather":
            return {
                kind: "gather",
                offsetDims: toJsonIndexSlice(value.offsetDims),
                collapsedSliceDims: toJsonIndexSlice(value.collapsedSliceDims),
                startIndexMap: toJsonIndexSlice(value.startIndexMap),
                indexVectorDim: value.indexVectorDim,
                sliceSizes: toJsonIndexSlice(value.sliceSizes),
            };
        case "scatter":
            return {
                kind: "scatter",
                updateWindowDims: toJsonIndexSlice(value.updateWindowDims),
                insertedWindowDims: toJsonIndexSlice(value.insertedWindowDims),
                scatterDimsToOperandDims: toJsonIndexSlice(value.scatterDimsToOperandDims),
                indexVectorDim: value.indexVectorDim,
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one TensorImmediate from one JSON value. */
export function fromJsonTensorImmediate(value: Json): TensorImmediate {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "dot":
            return {
                kind,
                lhsBatch: fromJsonIndexSlice(jsonField(object, "lhsBatch")),
                rhsBatch: fromJsonIndexSlice(jsonField(object, "rhsBatch")),
                lhsContracting: fromJsonIndexSlice(jsonField(object, "lhsContracting")),
                rhsContracting: fromJsonIndexSlice(jsonField(object, "rhsContracting")),
            };
        case "convolution":
            return {
                kind,
                inputBatch: jsonInteger(jsonField(object, "inputBatch")),
                inputFeature: jsonInteger(jsonField(object, "inputFeature")),
                inputSpatial: fromJsonIndexSlice(jsonField(object, "inputSpatial")),
                kernelInputFeature: jsonInteger(jsonField(object, "kernelInputFeature")),
                kernelOutputFeature: jsonInteger(jsonField(object, "kernelOutputFeature")),
                kernelSpatial: fromJsonIndexSlice(jsonField(object, "kernelSpatial")),
                outputBatch: jsonInteger(jsonField(object, "outputBatch")),
                outputFeature: jsonInteger(jsonField(object, "outputFeature")),
                outputSpatial: fromJsonIndexSlice(jsonField(object, "outputSpatial")),
                strides: fromJsonExtentSlice(jsonField(object, "strides")),
                paddingLow: fromJsonExtentSlice(jsonField(object, "paddingLow")),
                paddingHigh: fromJsonExtentSlice(jsonField(object, "paddingHigh")),
                lhsDilation: fromJsonExtentSlice(jsonField(object, "lhsDilation")),
                rhsDilation: fromJsonExtentSlice(jsonField(object, "rhsDilation")),
                windowReversal: fromJsonFlagSlice(jsonField(object, "windowReversal")),
                featureGroupCount: jsonInteger(jsonField(object, "featureGroupCount")),
                batchGroupCount: jsonInteger(jsonField(object, "batchGroupCount")),
            };
        case "gather":
            return {
                kind,
                offsetDims: fromJsonIndexSlice(jsonField(object, "offsetDims")),
                collapsedSliceDims: fromJsonIndexSlice(jsonField(object, "collapsedSliceDims")),
                startIndexMap: fromJsonIndexSlice(jsonField(object, "startIndexMap")),
                indexVectorDim: jsonInteger(jsonField(object, "indexVectorDim")),
                sliceSizes: fromJsonIndexSlice(jsonField(object, "sliceSizes")),
            };
        case "scatter":
            return {
                kind,
                updateWindowDims: fromJsonIndexSlice(jsonField(object, "updateWindowDims")),
                insertedWindowDims: fromJsonIndexSlice(jsonField(object, "insertedWindowDims")),
                scatterDimsToOperandDims: fromJsonIndexSlice(jsonField(object, "scatterDimsToOperandDims")),
                indexVectorDim: jsonInteger(jsonField(object, "indexVectorDim")),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** Compact reference to an extent list stored in the MIR tree. */
export type ExtentSlice = {
    /** Start index in the extent buffer. */
    readonly start: number;
    /** Number of extents in the slice. */
    readonly count: number;
};

export const ExtentSlice = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ExtentSlice): void {
        encodeExtentSlice(writer, value);
    },

    /** Decode one ExtentSlice. */
    decode(reader: BinaryReader): ExtentSlice {
        return decodeExtentSlice(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ExtentSlice): Json {
        return toJsonExtentSlice(value);
    },

    /** Return one ExtentSlice from one JSON value. */
    fromJson(value: Json): ExtentSlice {
        return fromJsonExtentSlice(value);
    },
};

/** Encode one ExtentSlice. */
export function encodeExtentSlice(writer: BinaryWriter, value: ExtentSlice): void {
    writer.writeUnsigned(value.start);
    writer.writeUnsigned(value.count);
}

/** Decode one ExtentSlice. */
export function decodeExtentSlice(reader: BinaryReader): ExtentSlice {
    const start = reader.readNumber();
    const count = reader.readNumber();

    return {
        start,
        count,
    };
}

/** Return one JSON value for one ExtentSlice. */
export function toJsonExtentSlice(value: ExtentSlice): Json {
    return {
        start: value.start,
        count: value.count,
    };
}

/** Return one ExtentSlice from one JSON value. */
export function fromJsonExtentSlice(value: Json): ExtentSlice {
    const object = jsonObject(value);

    return {
        start: jsonInteger(jsonField(object, "start")),
        count: jsonInteger(jsonField(object, "count")),
    };
}

/** Compact reference to a flag list stored in the MIR tree. */
export type FlagSlice = {
    /** Start index in the flag buffer. */
    readonly start: number;
    /** Number of flags in the slice. */
    readonly count: number;
};

export const FlagSlice = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: FlagSlice): void {
        encodeFlagSlice(writer, value);
    },

    /** Decode one FlagSlice. */
    decode(reader: BinaryReader): FlagSlice {
        return decodeFlagSlice(reader);
    },

    /** Return this value as JSON. */
    toJson(value: FlagSlice): Json {
        return toJsonFlagSlice(value);
    },

    /** Return one FlagSlice from one JSON value. */
    fromJson(value: Json): FlagSlice {
        return fromJsonFlagSlice(value);
    },
};

/** Encode one FlagSlice. */
export function encodeFlagSlice(writer: BinaryWriter, value: FlagSlice): void {
    writer.writeUnsigned(value.start);
    writer.writeUnsigned(value.count);
}

/** Decode one FlagSlice. */
export function decodeFlagSlice(reader: BinaryReader): FlagSlice {
    const start = reader.readNumber();
    const count = reader.readNumber();

    return {
        start,
        count,
    };
}

/** Return one JSON value for one FlagSlice. */
export function toJsonFlagSlice(value: FlagSlice): Json {
    return {
        start: value.start,
        count: value.count,
    };
}

/** Return one FlagSlice from one JSON value. */
export function fromJsonFlagSlice(value: Json): FlagSlice {
    const object = jsonObject(value);

    return {
        start: jsonInteger(jsonField(object, "start")),
        count: jsonInteger(jsonField(object, "count")),
    };
}
