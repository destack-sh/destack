/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, jsonBigint, jsonField, jsonObject, jsonOptional, jsonString } from "../../../protocol/serde.js";
import type { LocalNodeId } from "./node.js";
import type { Symbol } from "./symbol.js";
import type { Type } from "./type.js";
import { decodeLocalNodeId, encodeLocalNodeId, fromJsonLocalNodeId, toJsonLocalNodeId } from "./node.js";
import { decodeSymbol, encodeSymbol, fromJsonSymbol, toJsonSymbol } from "./symbol.js";
import { decodeType, encodeType, fromJsonType, toJsonType } from "./type.js";

/** One stored MIR type. */
export type TypeEntry =
    /** A structural type interned by equality. */
    | {
          readonly kind: "structural";
          /** The MIR type. */
          readonly ty: Type;
      }
    /** An identified type reserved for its recursive definition. */
    | {
          readonly kind: "reserved";
          /** The persistent identity of the type. */
          readonly symbol: Symbol;
      }
    /** A completely defined identified type. */
    | {
          readonly kind: "identified";
          /** The MIR type. */
          readonly ty: Type;
          /** The persistent identity of the type. */
          readonly symbol: Symbol;
          /** The source declaration of the type when present. */
          readonly declaration?: LocalNodeId;
      }
;

export const TypeEntry = {
    /** A structural type interned by equality. */
    structural(ty: Type): TypeEntry {
        return { kind: "structural", ty };
    },

    /** An identified type reserved for its recursive definition. */
    reserved(symbol_: Symbol): TypeEntry {
        return { kind: "reserved", symbol: symbol_ };
    },

    /** A completely defined identified type. */
    identified(ty: Type, symbol_: Symbol, declaration: LocalNodeId | undefined): TypeEntry {
        return { kind: "identified", ty, symbol: symbol_, declaration };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: TypeEntry): void {
        encodeTypeEntry(writer, value);
    },

    /** Decode one TypeEntry. */
    decode(reader: BinaryReader): TypeEntry {
        return decodeTypeEntry(reader);
    },

    /** Return this value as JSON. */
    toJson(value: TypeEntry): Json {
        return toJsonTypeEntry(value);
    },

    /** Return one TypeEntry from one JSON value. */
    fromJson(value: Json): TypeEntry {
        return fromJsonTypeEntry(value);
    },
};

/** Encode one TypeEntry. */
export function encodeTypeEntry(writer: BinaryWriter, value: TypeEntry): void {
    switch (value.kind) {
        case "structural":
            writer.writeUnsigned(0);
            encodeType(writer, value.ty);
            return;
        case "reserved":
            writer.writeUnsigned(1);
            encodeSymbol(writer, value.symbol);
            return;
        case "identified":
            writer.writeUnsigned(2);
            encodeType(writer, value.ty);
            encodeSymbol(writer, value.symbol);
            writer.writeOption(value.declaration, (value2) => {
                encodeLocalNodeId(writer, value2);
            });
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one TypeEntry. */
export function decodeTypeEntry(reader: BinaryReader): TypeEntry {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const ty = decodeType(reader);

            return {
                kind: "structural",
                ty,
            };
        }
        case 1: {
            const symbol_ = decodeSymbol(reader);

            return {
                kind: "reserved",
                symbol: symbol_,
            };
        }
        case 2: {
            const ty = decodeType(reader);
            const symbol_ = decodeSymbol(reader);
            const declaration = reader.readOption(() => decodeLocalNodeId(reader));

            return {
                kind: "identified",
                ty,
                symbol: symbol_,
                ...(declaration === undefined ? {} : { declaration }),
            };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one TypeEntry. */
export function toJsonTypeEntry(value: TypeEntry): Json {
    switch (value.kind) {
        case "structural":
            return {
                kind: "structural",
                ty: toJsonType(value.ty),
            };
        case "reserved":
            return {
                kind: "reserved",
                symbol: toJsonSymbol(value.symbol),
            };
        case "identified":
            return {
                kind: "identified",
                ty: toJsonType(value.ty),
                symbol: toJsonSymbol(value.symbol),
                ...(value.declaration === undefined ? {} : { declaration: toJsonLocalNodeId(value.declaration) }),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one TypeEntry from one JSON value. */
export function fromJsonTypeEntry(value: Json): TypeEntry {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "structural":
            return {
                kind,
                ty: fromJsonType(jsonField(object, "ty")),
            };
        case "reserved":
            return {
                kind,
                symbol: fromJsonSymbol(jsonField(object, "symbol")),
            };
        case "identified":
            return {
                kind,
                ty: fromJsonType(jsonField(object, "ty")),
                symbol: fromJsonSymbol(jsonField(object, "symbol")),
                declaration: jsonOptional(object, "declaration", (value) => fromJsonLocalNodeId(value)),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** One canonical MIR type index key. */
export type TypeIndexKey =
    /** The structural lookup hash of an anonymous type. */
    | {
          readonly kind: "structural";
          readonly structural: bigint;
      }
    /** The persistent symbol of an identified type. */
    | {
          readonly kind: "identified";
          readonly identified: Symbol;
      }
;

export const TypeIndexKey = {
    /** The structural lookup hash of an anonymous type. */
    structural(structural: bigint): TypeIndexKey {
        return { kind: "structural", structural };
    },

    /** The persistent symbol of an identified type. */
    identified(identified: Symbol): TypeIndexKey {
        return { kind: "identified", identified };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: TypeIndexKey): void {
        encodeTypeIndexKey(writer, value);
    },

    /** Decode one TypeIndexKey. */
    decode(reader: BinaryReader): TypeIndexKey {
        return decodeTypeIndexKey(reader);
    },

    /** Return this value as JSON. */
    toJson(value: TypeIndexKey): Json {
        return toJsonTypeIndexKey(value);
    },

    /** Return one TypeIndexKey from one JSON value. */
    fromJson(value: Json): TypeIndexKey {
        return fromJsonTypeIndexKey(value);
    },
};

/** Encode one TypeIndexKey. */
export function encodeTypeIndexKey(writer: BinaryWriter, value: TypeIndexKey): void {
    switch (value.kind) {
        case "structural":
            writer.writeUnsigned(0);
            writer.writeUnsigned(value.structural);
            return;
        case "identified":
            writer.writeUnsigned(1);
            encodeSymbol(writer, value.identified);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one TypeIndexKey. */
export function decodeTypeIndexKey(reader: BinaryReader): TypeIndexKey {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const structural = reader.readUnsigned();

            return { kind: "structural", structural };
        }
        case 1: {
            const identified = decodeSymbol(reader);

            return { kind: "identified", identified };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one TypeIndexKey. */
export function toJsonTypeIndexKey(value: TypeIndexKey): Json {
    switch (value.kind) {
        case "structural":
            return {
                kind: "structural",
                structural: value.structural.toString(),
            };
        case "identified":
            return {
                kind: "identified",
                identified: toJsonSymbol(value.identified),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one TypeIndexKey from one JSON value. */
export function fromJsonTypeIndexKey(value: Json): TypeIndexKey {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "structural":
            return {
                kind,
                structural: jsonBigint(jsonField(object, "structural")),
            };
        case "identified":
            return {
                kind,
                identified: fromJsonSymbol(jsonField(object, "identified")),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}
