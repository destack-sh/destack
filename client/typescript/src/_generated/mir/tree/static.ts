/* generated by destack_client_generate, do not edit */

import {
    BinaryReader,
    BinaryWriter,
    Json,
    SerdeError,
    jsonArray,
    jsonBigint,
    jsonBool,
    jsonField,
    jsonInteger,
    jsonObject,
    jsonOptional,
    jsonString,
} from "../../../protocol/serde.js";
import type { StringId } from "../../core/string.js";
import type { LocalNodeId } from "./node.js";
import type { Space } from "./type.js";
import {
    decodeStringId,
    encodeStringId,
    fromJsonStringId,
    toJsonStringId,
} from "../../core/string.js";
import {
    decodeLocalNodeId,
    encodeLocalNodeId,
    fromJsonLocalNodeId,
    toJsonLocalNodeId,
} from "./node.js";
import { decodeSpace, encodeSpace, fromJsonSpace, toJsonSpace } from "./type.js";

/** One compile-time value retained in MIR. */
export type Static =
    /** The value one template parameter names. */
    | {
          readonly kind: "parameter";
          readonly parameter: number;
      }
    /** The null value. */
    | {
          readonly kind: "null";
      }
    /** The undefined value. */
    | {
          readonly kind: "undefined";
      }
    /** A boolean value. */
    | {
          readonly kind: "boolean";
          readonly boolean: boolean;
      }
    /** A signed integer value. */
    | {
          readonly kind: "integer";
          readonly integer: bigint;
      }
    /** A bigint value. */
    | {
          readonly kind: "bigint";
          readonly bigint: bigint;
      }
    /** A 64-bit floating-point value stored as exact bits. */
    | {
          readonly kind: "float";
          readonly float: bigint;
      }
    /** A Unicode scalar value. */
    | {
          readonly kind: "character";
          readonly character: string;
      }
    /** An interned string value. */
    | {
          readonly kind: "string";
          readonly string: StringId;
      }
    /** An interned regular expression value. */
    | {
          readonly kind: "regex";
          /** The regular expression source. */
          readonly content: StringId;
          /** The regular expression flags when present. */
          readonly flags?: StringId;
      }
    /** A type reflected as a compile-time value. */
    | {
          readonly kind: "type";
          readonly type: LocalNodeId;
      }
    /** A storage space bound as a compile-time value. */
    | {
          readonly kind: "space";
          readonly space: Space;
      }
    /** A homogeneous array value. */
    | {
          readonly kind: "array";
          readonly array: ReadonlyArray<StaticId>;
      }
    /** A compact repeated fixed-array value. */
    | {
          readonly kind: "fixedArray";
          /** The repeated element value. */
          readonly value: StaticId;
          /** The array length. */
          readonly length: bigint;
      }
    /** A heterogeneous tuple value. */
    | {
          readonly kind: "tuple";
          readonly tuple: ReadonlyArray<StaticId>;
      }
    /** A nominal newtype value. */
    | {
          readonly kind: "newtype";
          /** The concrete newtype. */
          readonly ty: LocalNodeId;
          /** The wrapped value. */
          readonly value: StaticId;
      }
    /** A structural object value. */
    | {
          readonly kind: "object";
          readonly object: ReadonlyArray<StaticField>;
      }
    /** A nominal struct value. */
    | {
          readonly kind: "struct";
          /** The concrete struct type. */
          readonly ty: LocalNodeId;
          /** The struct fields. */
          readonly fields: ReadonlyArray<StaticField>;
      };

export const Static = {
    /** The value one template parameter names. */
    parameter(parameter: number): Static {
        return { kind: "parameter", parameter };
    },

    /** The null value. */
    null(): Static {
        return { kind: "null" };
    },

    /** The undefined value. */
    undefined(): Static {
        return { kind: "undefined" };
    },

    /** A boolean value. */
    boolean(boolean_: boolean): Static {
        return { kind: "boolean", boolean: boolean_ };
    },

    /** A signed integer value. */
    integer(integer: bigint): Static {
        return { kind: "integer", integer };
    },

    /** A bigint value. */
    bigint(bigint_: bigint): Static {
        return { kind: "bigint", bigint: bigint_ };
    },

    /** A 64-bit floating-point value stored as exact bits. */
    float(float: bigint): Static {
        return { kind: "float", float };
    },

    /** A Unicode scalar value. */
    character(character: string): Static {
        return { kind: "character", character };
    },

    /** An interned string value. */
    string(string_: StringId): Static {
        return { kind: "string", string: string_ };
    },

    /** An interned regular expression value. */
    regex(content: StringId, flags: StringId | undefined): Static {
        return { kind: "regex", content, flags };
    },

    /** A type reflected as a compile-time value. */
    type(type_: LocalNodeId): Static {
        return { kind: "type", type: type_ };
    },

    /** A storage space bound as a compile-time value. */
    space(space: Space): Static {
        return { kind: "space", space };
    },

    /** A homogeneous array value. */
    array(array: ReadonlyArray<StaticId>): Static {
        return { kind: "array", array };
    },

    /** A compact repeated fixed-array value. */
    fixedArray(value: StaticId, length: bigint): Static {
        return { kind: "fixedArray", value, length };
    },

    /** A heterogeneous tuple value. */
    tuple(tuple: ReadonlyArray<StaticId>): Static {
        return { kind: "tuple", tuple };
    },

    /** A nominal newtype value. */
    newtype(ty: LocalNodeId, value: StaticId): Static {
        return { kind: "newtype", ty, value };
    },

    /** A structural object value. */
    object(object_: ReadonlyArray<StaticField>): Static {
        return { kind: "object", object: object_ };
    },

    /** A nominal struct value. */
    struct(ty: LocalNodeId, fields: ReadonlyArray<StaticField>): Static {
        return { kind: "struct", ty, fields };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: Static): void {
        encodeStatic(writer, value);
    },

    /** Decode one Static. */
    decode(reader: BinaryReader): Static {
        return decodeStatic(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Static): Json {
        return toJsonStatic(value);
    },

    /** Return one Static from one JSON value. */
    fromJson(value: Json): Static {
        return fromJsonStatic(value);
    },
};

/** Encode one Static. */
export function encodeStatic(writer: BinaryWriter, value: Static): void {
    switch (value.kind) {
        case "parameter":
            writer.writeUnsigned(0);
            writer.writeUnsigned(value.parameter);
            return;
        case "null":
            writer.writeUnsigned(1);
            return;
        case "undefined":
            writer.writeUnsigned(2);
            return;
        case "boolean":
            writer.writeUnsigned(3);
            writer.writeBool(value.boolean);
            return;
        case "integer":
            writer.writeUnsigned(4);
            writer.writeSigned(value.integer);
            return;
        case "bigint":
            writer.writeUnsigned(5);
            writer.writeSigned(value.bigint);
            return;
        case "float":
            writer.writeUnsigned(6);
            writer.writeUnsigned(value.float);
            return;
        case "character":
            writer.writeUnsigned(7);
            writer.writeChar(value.character);
            return;
        case "string":
            writer.writeUnsigned(8);
            encodeStringId(writer, value.string);
            return;
        case "regex":
            writer.writeUnsigned(9);
            encodeStringId(writer, value.content);
            writer.writeOption(value.flags, (value1) => {
                encodeStringId(writer, value1);
            });
            return;
        case "type":
            writer.writeUnsigned(10);
            encodeLocalNodeId(writer, value.type);
            return;
        case "space":
            writer.writeUnsigned(11);
            encodeSpace(writer, value.space);
            return;
        case "array":
            writer.writeUnsigned(12);
            writer.writeUnsigned(value.array.length);
            for (const item0 of value.array) {
                encodeStaticId(writer, item0);
            }
            return;
        case "fixedArray":
            writer.writeUnsigned(13);
            encodeStaticId(writer, value.value);
            writer.writeUnsigned(value.length);
            return;
        case "tuple":
            writer.writeUnsigned(14);
            writer.writeUnsigned(value.tuple.length);
            for (const item0 of value.tuple) {
                encodeStaticId(writer, item0);
            }
            return;
        case "newtype":
            writer.writeUnsigned(15);
            encodeLocalNodeId(writer, value.ty);
            encodeStaticId(writer, value.value);
            return;
        case "object":
            writer.writeUnsigned(16);
            writer.writeUnsigned(value.object.length);
            for (const item0 of value.object) {
                encodeStaticField(writer, item0);
            }
            return;
        case "struct":
            writer.writeUnsigned(17);
            encodeLocalNodeId(writer, value.ty);
            writer.writeUnsigned(value.fields.length);
            for (const item1 of value.fields) {
                encodeStaticField(writer, item1);
            }
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one Static. */
export function decodeStatic(reader: BinaryReader): Static {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const parameter = reader.readNumber();

            return { kind: "parameter", parameter };
        }
        case 1: {
            return { kind: "null" };
        }
        case 2: {
            return { kind: "undefined" };
        }
        case 3: {
            const boolean_ = reader.readBool();

            return { kind: "boolean", boolean: boolean_ };
        }
        case 4: {
            const integer = reader.readSigned();

            return { kind: "integer", integer };
        }
        case 5: {
            const bigint_ = reader.readSigned();

            return { kind: "bigint", bigint: bigint_ };
        }
        case 6: {
            const float = reader.readUnsigned();

            return { kind: "float", float };
        }
        case 7: {
            const character = reader.readChar();

            return { kind: "character", character };
        }
        case 8: {
            const string_ = decodeStringId(reader);

            return { kind: "string", string: string_ };
        }
        case 9: {
            const content = decodeStringId(reader);
            const flags = reader.readOption(() => decodeStringId(reader));

            return {
                kind: "regex",
                content,
                ...(flags === undefined ? {} : { flags }),
            };
        }
        case 10: {
            const type_ = decodeLocalNodeId(reader);

            return { kind: "type", type: type_ };
        }
        case 11: {
            const space = decodeSpace(reader);

            return { kind: "space", space };
        }
        case 12: {
            const array = (() => {
                const length0 = reader.readNumber();
                const items0: Array<StaticId> = [];
                for (let index = 0; index < length0; index += 1) {
                    items0.push(decodeStaticId(reader));
                }
                return items0;
            })();

            return { kind: "array", array };
        }
        case 13: {
            const value = decodeStaticId(reader);
            const length = reader.readUnsigned();

            return {
                kind: "fixedArray",
                value,
                length,
            };
        }
        case 14: {
            const tuple = (() => {
                const length0 = reader.readNumber();
                const items0: Array<StaticId> = [];
                for (let index = 0; index < length0; index += 1) {
                    items0.push(decodeStaticId(reader));
                }
                return items0;
            })();

            return { kind: "tuple", tuple };
        }
        case 15: {
            const ty = decodeLocalNodeId(reader);
            const value = decodeStaticId(reader);

            return {
                kind: "newtype",
                ty,
                value,
            };
        }
        case 16: {
            const object_ = (() => {
                const length0 = reader.readNumber();
                const items0: Array<StaticField> = [];
                for (let index = 0; index < length0; index += 1) {
                    items0.push(decodeStaticField(reader));
                }
                return items0;
            })();

            return { kind: "object", object: object_ };
        }
        case 17: {
            const ty = decodeLocalNodeId(reader);
            const fields = (() => {
                const length1 = reader.readNumber();
                const items1: Array<StaticField> = [];
                for (let index = 0; index < length1; index += 1) {
                    items1.push(decodeStaticField(reader));
                }
                return items1;
            })();

            return {
                kind: "struct",
                ty,
                fields,
            };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one Static. */
export function toJsonStatic(value: Static): Json {
    switch (value.kind) {
        case "parameter":
            return {
                kind: "parameter",
                parameter: value.parameter,
            };
        case "null":
            return {
                kind: "null",
            };
        case "undefined":
            return {
                kind: "undefined",
            };
        case "boolean":
            return {
                kind: "boolean",
                boolean: value.boolean,
            };
        case "integer":
            return {
                kind: "integer",
                integer: value.integer.toString(),
            };
        case "bigint":
            return {
                kind: "bigint",
                bigint: value.bigint.toString(),
            };
        case "float":
            return {
                kind: "float",
                float: value.float.toString(),
            };
        case "character":
            return {
                kind: "character",
                character: value.character,
            };
        case "string":
            return {
                kind: "string",
                string: toJsonStringId(value.string),
            };
        case "regex":
            return {
                kind: "regex",
                content: toJsonStringId(value.content),
                ...(value.flags === undefined ? {} : { flags: toJsonStringId(value.flags) }),
            };
        case "type":
            return {
                kind: "type",
                type: toJsonLocalNodeId(value.type),
            };
        case "space":
            return {
                kind: "space",
                space: toJsonSpace(value.space),
            };
        case "array":
            return {
                kind: "array",
                array: value.array.map((item0) => toJsonStaticId(item0)),
            };
        case "fixedArray":
            return {
                kind: "fixedArray",
                value: toJsonStaticId(value.value),
                length: value.length.toString(),
            };
        case "tuple":
            return {
                kind: "tuple",
                tuple: value.tuple.map((item0) => toJsonStaticId(item0)),
            };
        case "newtype":
            return {
                kind: "newtype",
                ty: toJsonLocalNodeId(value.ty),
                value: toJsonStaticId(value.value),
            };
        case "object":
            return {
                kind: "object",
                object: value.object.map((item0) => toJsonStaticField(item0)),
            };
        case "struct":
            return {
                kind: "struct",
                ty: toJsonLocalNodeId(value.ty),
                fields: value.fields.map((item0) => toJsonStaticField(item0)),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one Static from one JSON value. */
export function fromJsonStatic(value: Json): Static {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "parameter":
            return {
                kind,
                parameter: jsonInteger(jsonField(object, "parameter")),
            };
        case "null":
            return {
                kind,
            };
        case "undefined":
            return {
                kind,
            };
        case "boolean":
            return {
                kind,
                boolean: jsonBool(jsonField(object, "boolean")),
            };
        case "integer":
            return {
                kind,
                integer: jsonBigint(jsonField(object, "integer")),
            };
        case "bigint":
            return {
                kind,
                bigint: jsonBigint(jsonField(object, "bigint")),
            };
        case "float":
            return {
                kind,
                float: jsonBigint(jsonField(object, "float")),
            };
        case "character":
            return {
                kind,
                character: jsonString(jsonField(object, "character")),
            };
        case "string":
            return {
                kind,
                string: fromJsonStringId(jsonField(object, "string")),
            };
        case "regex":
            return {
                kind,
                content: fromJsonStringId(jsonField(object, "content")),
                flags: jsonOptional(object, "flags", (value) => fromJsonStringId(value)),
            };
        case "type":
            return {
                kind,
                type: fromJsonLocalNodeId(jsonField(object, "type")),
            };
        case "space":
            return {
                kind,
                space: fromJsonSpace(jsonField(object, "space")),
            };
        case "array":
            return {
                kind,
                array: jsonArray(jsonField(object, "array")).map((item0) =>
                    fromJsonStaticId(item0),
                ),
            };
        case "fixedArray":
            return {
                kind,
                value: fromJsonStaticId(jsonField(object, "value")),
                length: jsonBigint(jsonField(object, "length")),
            };
        case "tuple":
            return {
                kind,
                tuple: jsonArray(jsonField(object, "tuple")).map((item0) =>
                    fromJsonStaticId(item0),
                ),
            };
        case "newtype":
            return {
                kind,
                ty: fromJsonLocalNodeId(jsonField(object, "ty")),
                value: fromJsonStaticId(jsonField(object, "value")),
            };
        case "object":
            return {
                kind,
                object: jsonArray(jsonField(object, "object")).map((item0) =>
                    fromJsonStaticField(item0),
                ),
            };
        case "struct":
            return {
                kind,
                ty: fromJsonLocalNodeId(jsonField(object, "ty")),
                fields: jsonArray(jsonField(object, "fields")).map((item0) =>
                    fromJsonStaticField(item0),
                ),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** One field in a compile-time object or struct value. */
export type StaticField = {
    /** The field key. */
    readonly key: StaticKey;
    /** The field value. */
    readonly value: StaticId;
};

export const StaticField = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: StaticField): void {
        encodeStaticField(writer, value);
    },

    /** Decode one StaticField. */
    decode(reader: BinaryReader): StaticField {
        return decodeStaticField(reader);
    },

    /** Return this value as JSON. */
    toJson(value: StaticField): Json {
        return toJsonStaticField(value);
    },

    /** Return one StaticField from one JSON value. */
    fromJson(value: Json): StaticField {
        return fromJsonStaticField(value);
    },
};

/** Encode one StaticField. */
export function encodeStaticField(writer: BinaryWriter, value: StaticField): void {
    encodeStaticKey(writer, value.key);
    encodeStaticId(writer, value.value);
}

/** Decode one StaticField. */
export function decodeStaticField(reader: BinaryReader): StaticField {
    const key = decodeStaticKey(reader);
    const value = decodeStaticId(reader);

    return {
        key,
        value,
    };
}

/** Return one JSON value for one StaticField. */
export function toJsonStaticField(value: StaticField): Json {
    return {
        key: toJsonStaticKey(value.key),
        value: toJsonStaticId(value.value),
    };
}

/** Return one StaticField from one JSON value. */
export function fromJsonStaticField(value: Json): StaticField {
    const object = jsonObject(value);

    return {
        key: fromJsonStaticKey(jsonField(object, "key")),
        value: fromJsonStaticId(jsonField(object, "value")),
    };
}

/** Compact identity of one interned compile-time value. */
export type StaticId = number;

export const StaticId = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: StaticId): void {
        encodeStaticId(writer, value);
    },

    /** Decode one StaticId. */
    decode(reader: BinaryReader): StaticId {
        return decodeStaticId(reader);
    },

    /** Return this value as JSON. */
    toJson(value: StaticId): Json {
        return toJsonStaticId(value);
    },

    /** Return one StaticId from one JSON value. */
    fromJson(value: Json): StaticId {
        return fromJsonStaticId(value);
    },
};

/** Encode one StaticId. */
export function encodeStaticId(writer: BinaryWriter, value: StaticId): void {
    writer.writeUnsigned(value);
}

/** Decode one StaticId. */
export function decodeStaticId(reader: BinaryReader): StaticId {
    return reader.readNumber();
}

/** Return one JSON value for one StaticId. */
export function toJsonStaticId(value: StaticId): Json {
    return value;
}

/** Return one StaticId from one JSON value. */
export function fromJsonStaticId(value: Json): StaticId {
    return jsonInteger(value);
}

/** One compile-time property key. */
export type StaticKey =
    /** An interned name key. */
    | {
          readonly kind: "name";
          readonly name: StringId;
      }
    /** A positional index key. */
    | {
          readonly kind: "index";
          readonly index: bigint;
      };

export const StaticKey = {
    /** An interned name key. */
    name(name: StringId): StaticKey {
        return { kind: "name", name };
    },

    /** A positional index key. */
    index(index: bigint): StaticKey {
        return { kind: "index", index };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: StaticKey): void {
        encodeStaticKey(writer, value);
    },

    /** Decode one StaticKey. */
    decode(reader: BinaryReader): StaticKey {
        return decodeStaticKey(reader);
    },

    /** Return this value as JSON. */
    toJson(value: StaticKey): Json {
        return toJsonStaticKey(value);
    },

    /** Return one StaticKey from one JSON value. */
    fromJson(value: Json): StaticKey {
        return fromJsonStaticKey(value);
    },
};

/** Encode one StaticKey. */
export function encodeStaticKey(writer: BinaryWriter, value: StaticKey): void {
    switch (value.kind) {
        case "name":
            writer.writeUnsigned(0);
            encodeStringId(writer, value.name);
            return;
        case "index":
            writer.writeUnsigned(1);
            writer.writeUnsigned(value.index);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one StaticKey. */
export function decodeStaticKey(reader: BinaryReader): StaticKey {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const name = decodeStringId(reader);

            return { kind: "name", name };
        }
        case 1: {
            const index = reader.readUnsigned();

            return { kind: "index", index };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one StaticKey. */
export function toJsonStaticKey(value: StaticKey): Json {
    switch (value.kind) {
        case "name":
            return {
                kind: "name",
                name: toJsonStringId(value.name),
            };
        case "index":
            return {
                kind: "index",
                index: value.index.toString(),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one StaticKey from one JSON value. */
export function fromJsonStaticKey(value: Json): StaticKey {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "name":
            return {
                kind,
                name: fromJsonStringId(jsonField(object, "name")),
            };
        case "index":
            return {
                kind,
                index: jsonBigint(jsonField(object, "index")),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}
