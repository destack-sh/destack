/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, jsonBigint, jsonField, jsonObject, jsonString } from "../../../protocol/serde.js";
import type { LocalNodeId } from "./node.js";
import { decodeLocalNodeId, encodeLocalNodeId, fromJsonLocalNodeId, toJsonLocalNodeId } from "./node.js";

/** Control-flow edge selected by a terminator successor. */
export type Edge = {
    /** The source block. */
    readonly source: LocalNodeId;
    /** The successor field selected from the source terminator. */
    readonly successor: Successor;
    /** The target block. */
    readonly target: LocalNodeId;
};

export const Edge = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: Edge): void {
        encodeEdge(writer, value);
    },

    /** Decode one Edge. */
    decode(reader: BinaryReader): Edge {
        return decodeEdge(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Edge): Json {
        return toJsonEdge(value);
    },

    /** Return one Edge from one JSON value. */
    fromJson(value: Json): Edge {
        return fromJsonEdge(value);
    },
};

/** Encode one Edge. */
export function encodeEdge(writer: BinaryWriter, value: Edge): void {
    encodeLocalNodeId(writer, value.source);
    encodeSuccessor(writer, value.successor);
    encodeLocalNodeId(writer, value.target);
}

/** Decode one Edge. */
export function decodeEdge(reader: BinaryReader): Edge {
    const source = decodeLocalNodeId(reader);
    const successor = decodeSuccessor(reader);
    const target = decodeLocalNodeId(reader);

    return {
        source,
        successor,
        target,
    };
}

/** Return one JSON value for one Edge. */
export function toJsonEdge(value: Edge): Json {
    return {
        source: toJsonLocalNodeId(value.source),
        successor: toJsonSuccessor(value.successor),
        target: toJsonLocalNodeId(value.target),
    };
}

/** Return one Edge from one JSON value. */
export function fromJsonEdge(value: Json): Edge {
    const object = jsonObject(value);

    return {
        source: fromJsonLocalNodeId(jsonField(object, "source")),
        successor: fromJsonSuccessor(jsonField(object, "successor")),
        target: fromJsonLocalNodeId(jsonField(object, "target")),
    };
}

/** Successor selected by one MIR terminator. */
export type Successor =
    /** The target of an unconditional jump. */
    | {
          readonly kind: "jump";
      }
    /** The return continuation of a call terminator. */
    | {
          readonly kind: "callReturn";
      }
    /** The unwind continuation of a call terminator. */
    | {
          readonly kind: "callUnwind";
      }
    /** The then target of a branch terminator. */
    | {
          readonly kind: "branchThen";
      }
    /** The else target of a branch terminator. */
    | {
          readonly kind: "branchElse";
      }
    /** The success target of a check terminator. */
    | {
          readonly kind: "checkSuccess";
      }
    /** The failure target of a check terminator. */
    | {
          readonly kind: "checkFailure";
      }
    /** The success target of a fallible terminator. */
    | {
          readonly kind: "trySuccess";
      }
    /** The failure target of a fallible terminator. */
    | {
          readonly kind: "tryFailure";
      }
    /** One switch case target. */
    | {
          readonly kind: "switchCase";
          /** The matched case value. */
          readonly value: bigint;
      }
    /** The default target of a switch terminator. */
    | {
          readonly kind: "switchDefault";
      }
    /** The resume target of a yield terminator. */
    | {
          readonly kind: "yieldResume";
      }
    /** The unwind target of a yield terminator. */
    | {
          readonly kind: "yieldUnwind";
      }
;

export const Successor = {
    /** The target of an unconditional jump. */
    jump(): Successor {
        return { kind: "jump" };
    },

    /** The return continuation of a call terminator. */
    callReturn(): Successor {
        return { kind: "callReturn" };
    },

    /** The unwind continuation of a call terminator. */
    callUnwind(): Successor {
        return { kind: "callUnwind" };
    },

    /** The then target of a branch terminator. */
    branchThen(): Successor {
        return { kind: "branchThen" };
    },

    /** The else target of a branch terminator. */
    branchElse(): Successor {
        return { kind: "branchElse" };
    },

    /** The success target of a check terminator. */
    checkSuccess(): Successor {
        return { kind: "checkSuccess" };
    },

    /** The failure target of a check terminator. */
    checkFailure(): Successor {
        return { kind: "checkFailure" };
    },

    /** The success target of a fallible terminator. */
    trySuccess(): Successor {
        return { kind: "trySuccess" };
    },

    /** The failure target of a fallible terminator. */
    tryFailure(): Successor {
        return { kind: "tryFailure" };
    },

    /** One switch case target. */
    switchCase(value: bigint): Successor {
        return { kind: "switchCase", value };
    },

    /** The default target of a switch terminator. */
    switchDefault(): Successor {
        return { kind: "switchDefault" };
    },

    /** The resume target of a yield terminator. */
    yieldResume(): Successor {
        return { kind: "yieldResume" };
    },

    /** The unwind target of a yield terminator. */
    yieldUnwind(): Successor {
        return { kind: "yieldUnwind" };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: Successor): void {
        encodeSuccessor(writer, value);
    },

    /** Decode one Successor. */
    decode(reader: BinaryReader): Successor {
        return decodeSuccessor(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Successor): Json {
        return toJsonSuccessor(value);
    },

    /** Return one Successor from one JSON value. */
    fromJson(value: Json): Successor {
        return fromJsonSuccessor(value);
    },
};

/** Encode one Successor. */
export function encodeSuccessor(writer: BinaryWriter, value: Successor): void {
    switch (value.kind) {
        case "jump":
            writer.writeUnsigned(0);
            return;
        case "callReturn":
            writer.writeUnsigned(1);
            return;
        case "callUnwind":
            writer.writeUnsigned(2);
            return;
        case "branchThen":
            writer.writeUnsigned(3);
            return;
        case "branchElse":
            writer.writeUnsigned(4);
            return;
        case "checkSuccess":
            writer.writeUnsigned(5);
            return;
        case "checkFailure":
            writer.writeUnsigned(6);
            return;
        case "trySuccess":
            writer.writeUnsigned(7);
            return;
        case "tryFailure":
            writer.writeUnsigned(8);
            return;
        case "switchCase":
            writer.writeUnsigned(9);
            writer.writeSigned(value.value);
            return;
        case "switchDefault":
            writer.writeUnsigned(10);
            return;
        case "yieldResume":
            writer.writeUnsigned(11);
            return;
        case "yieldUnwind":
            writer.writeUnsigned(12);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one Successor. */
export function decodeSuccessor(reader: BinaryReader): Successor {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            return { kind: "jump" };
        }
        case 1: {
            return { kind: "callReturn" };
        }
        case 2: {
            return { kind: "callUnwind" };
        }
        case 3: {
            return { kind: "branchThen" };
        }
        case 4: {
            return { kind: "branchElse" };
        }
        case 5: {
            return { kind: "checkSuccess" };
        }
        case 6: {
            return { kind: "checkFailure" };
        }
        case 7: {
            return { kind: "trySuccess" };
        }
        case 8: {
            return { kind: "tryFailure" };
        }
        case 9: {
            const value = reader.readSigned();

            return {
                kind: "switchCase",
                value,
            };
        }
        case 10: {
            return { kind: "switchDefault" };
        }
        case 11: {
            return { kind: "yieldResume" };
        }
        case 12: {
            return { kind: "yieldUnwind" };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one Successor. */
export function toJsonSuccessor(value: Successor): Json {
    switch (value.kind) {
        case "jump":
            return {
                kind: "jump",
            };
        case "callReturn":
            return {
                kind: "callReturn",
            };
        case "callUnwind":
            return {
                kind: "callUnwind",
            };
        case "branchThen":
            return {
                kind: "branchThen",
            };
        case "branchElse":
            return {
                kind: "branchElse",
            };
        case "checkSuccess":
            return {
                kind: "checkSuccess",
            };
        case "checkFailure":
            return {
                kind: "checkFailure",
            };
        case "trySuccess":
            return {
                kind: "trySuccess",
            };
        case "tryFailure":
            return {
                kind: "tryFailure",
            };
        case "switchCase":
            return {
                kind: "switchCase",
                value: value.value.toString(),
            };
        case "switchDefault":
            return {
                kind: "switchDefault",
            };
        case "yieldResume":
            return {
                kind: "yieldResume",
            };
        case "yieldUnwind":
            return {
                kind: "yieldUnwind",
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one Successor from one JSON value. */
export function fromJsonSuccessor(value: Json): Successor {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "jump":
            return {
                kind,
            };
        case "callReturn":
            return {
                kind,
            };
        case "callUnwind":
            return {
                kind,
            };
        case "branchThen":
            return {
                kind,
            };
        case "branchElse":
            return {
                kind,
            };
        case "checkSuccess":
            return {
                kind,
            };
        case "checkFailure":
            return {
                kind,
            };
        case "trySuccess":
            return {
                kind,
            };
        case "tryFailure":
            return {
                kind,
            };
        case "switchCase":
            return {
                kind,
                value: jsonBigint(jsonField(object, "value")),
            };
        case "switchDefault":
            return {
                kind,
            };
        case "yieldResume":
            return {
                kind,
            };
        case "yieldUnwind":
            return {
                kind,
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}
