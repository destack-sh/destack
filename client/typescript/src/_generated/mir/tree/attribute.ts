/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, jsonArray, jsonBigint, jsonBool, jsonField, jsonObject, jsonString } from "../../../protocol/serde.js";
import type { StringId } from "../../core/string.js";
import type { LocalNodeId } from "./node.js";
import { decodeStringId, encodeStringId, fromJsonStringId, toJsonStringId } from "../../core/string.js";
import { decodeLocalNodeId, encodeLocalNodeId, fromJsonLocalNodeId, toJsonLocalNodeId } from "./node.js";

import { FloatValueImpl } from "../../../_impl/mir/tree/attribute.js";

/** A tables attribute attached to a MIR node. */
export type Attribute = {
    /** The attribute name. */
    readonly name: AttributeIdentifier;
    /** The attribute arguments. */
    readonly args: AttributeArgs;
};

export const Attribute = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: Attribute): void {
        encodeAttribute(writer, value);
    },

    /** Decode one Attribute. */
    decode(reader: BinaryReader): Attribute {
        return decodeAttribute(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Attribute): Json {
        return toJsonAttribute(value);
    },

    /** Return one Attribute from one JSON value. */
    fromJson(value: Json): Attribute {
        return fromJsonAttribute(value);
    },
};

/** Encode one Attribute. */
export function encodeAttribute(writer: BinaryWriter, value: Attribute): void {
    encodeAttributeIdentifier(writer, value.name);
    encodeAttributeArgs(writer, value.args);
}

/** Decode one Attribute. */
export function decodeAttribute(reader: BinaryReader): Attribute {
    const name = decodeAttributeIdentifier(reader);
    const args = decodeAttributeArgs(reader);

    return {
        name,
        args,
    };
}

/** Return one JSON value for one Attribute. */
export function toJsonAttribute(value: Attribute): Json {
    return {
        name: toJsonAttributeIdentifier(value.name),
        args: toJsonAttributeArgs(value.args),
    };
}

/** Return one Attribute from one JSON value. */
export function fromJsonAttribute(value: Json): Attribute {
    const object = jsonObject(value);

    return {
        name: fromJsonAttributeIdentifier(jsonField(object, "name")),
        args: fromJsonAttributeArgs(jsonField(object, "args")),
    };
}

/** One identifier inside attribute syntax. */
export type AttributeIdentifier =
    /** One concrete identifier. */
    | {
          readonly kind: "identifier";
          readonly identifier: StringId;
      }
    /** One required identifier that was omitted. */
    | {
          readonly kind: "missing";
      }
    /** One malformed identifier fragment. */
    | {
          readonly kind: "error";
      }
;

export const AttributeIdentifier = {
    /** One concrete identifier. */
    identifier(identifier: StringId): AttributeIdentifier {
        return { kind: "identifier", identifier };
    },

    /** One required identifier that was omitted. */
    missing(): AttributeIdentifier {
        return { kind: "missing" };
    },

    /** One malformed identifier fragment. */
    error(): AttributeIdentifier {
        return { kind: "error" };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: AttributeIdentifier): void {
        encodeAttributeIdentifier(writer, value);
    },

    /** Decode one AttributeIdentifier. */
    decode(reader: BinaryReader): AttributeIdentifier {
        return decodeAttributeIdentifier(reader);
    },

    /** Return this value as JSON. */
    toJson(value: AttributeIdentifier): Json {
        return toJsonAttributeIdentifier(value);
    },

    /** Return one AttributeIdentifier from one JSON value. */
    fromJson(value: Json): AttributeIdentifier {
        return fromJsonAttributeIdentifier(value);
    },
};

/** Encode one AttributeIdentifier. */
export function encodeAttributeIdentifier(writer: BinaryWriter, value: AttributeIdentifier): void {
    switch (value.kind) {
        case "identifier":
            writer.writeUnsigned(0);
            encodeStringId(writer, value.identifier);
            return;
        case "missing":
            writer.writeUnsigned(1);
            return;
        case "error":
            writer.writeUnsigned(2);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one AttributeIdentifier. */
export function decodeAttributeIdentifier(reader: BinaryReader): AttributeIdentifier {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const identifier = decodeStringId(reader);

            return { kind: "identifier", identifier };
        }
        case 1: {
            return { kind: "missing" };
        }
        case 2: {
            return { kind: "error" };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one AttributeIdentifier. */
export function toJsonAttributeIdentifier(value: AttributeIdentifier): Json {
    switch (value.kind) {
        case "identifier":
            return {
                kind: "identifier",
                identifier: toJsonStringId(value.identifier),
            };
        case "missing":
            return {
                kind: "missing",
            };
        case "error":
            return {
                kind: "error",
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one AttributeIdentifier from one JSON value. */
export function fromJsonAttributeIdentifier(value: Json): AttributeIdentifier {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "identifier":
            return {
                kind,
                identifier: fromJsonStringId(jsonField(object, "identifier")),
            };
        case "missing":
            return {
                kind,
            };
        case "error":
            return {
                kind,
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** Arguments for a MIR attribute. */
export type AttributeArgs =
    /** No arguments were provided. */
    | {
          readonly kind: "none";
      }
    /** A single unnamed value was provided. */
    | {
          readonly kind: "value";
          readonly value: AttributeValue;
      }
    /** Multiple unnamed values were provided. */
    | {
          readonly kind: "values";
          readonly values: ReadonlyArray<AttributeValue>;
      }
    /** Named arguments were provided as key-value pairs. */
    | {
          readonly kind: "keyValues";
          readonly key_values: ReadonlyArray<AttributeKeyValue>;
      }
;

export const AttributeArgs = {
    /** No arguments were provided. */
    none(): AttributeArgs {
        return { kind: "none" };
    },

    /** A single unnamed value was provided. */
    value(value: AttributeValue): AttributeArgs {
        return { kind: "value", value };
    },

    /** Multiple unnamed values were provided. */
    values(values: ReadonlyArray<AttributeValue>): AttributeArgs {
        return { kind: "values", values };
    },

    /** Named arguments were provided as key-value pairs. */
    keyValues(key_values: ReadonlyArray<AttributeKeyValue>): AttributeArgs {
        return { kind: "keyValues", key_values };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: AttributeArgs): void {
        encodeAttributeArgs(writer, value);
    },

    /** Decode one AttributeArgs. */
    decode(reader: BinaryReader): AttributeArgs {
        return decodeAttributeArgs(reader);
    },

    /** Return this value as JSON. */
    toJson(value: AttributeArgs): Json {
        return toJsonAttributeArgs(value);
    },

    /** Return one AttributeArgs from one JSON value. */
    fromJson(value: Json): AttributeArgs {
        return fromJsonAttributeArgs(value);
    },
};

/** Encode one AttributeArgs. */
export function encodeAttributeArgs(writer: BinaryWriter, value: AttributeArgs): void {
    switch (value.kind) {
        case "none":
            writer.writeUnsigned(0);
            return;
        case "value":
            writer.writeUnsigned(1);
            encodeAttributeValue(writer, value.value);
            return;
        case "values":
            writer.writeUnsigned(2);
            writer.writeUnsigned(value.values.length);
            for (const item0 of value.values) {
                encodeAttributeValue(writer, item0);
            }
            return;
        case "keyValues":
            writer.writeUnsigned(3);
            writer.writeUnsigned(value.key_values.length);
            for (const item0 of value.key_values) {
                encodeAttributeKeyValue(writer, item0);
            }
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one AttributeArgs. */
export function decodeAttributeArgs(reader: BinaryReader): AttributeArgs {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            return { kind: "none" };
        }
        case 1: {
            const value = decodeAttributeValue(reader);

            return { kind: "value", value };
        }
        case 2: {
            const values = (() => { const length0 = reader.readNumber(); const items0: Array<AttributeValue> = []; for (let index = 0; index < length0; index += 1) { items0.push(decodeAttributeValue(reader)); } return items0; })();

            return { kind: "values", values };
        }
        case 3: {
            const key_values = (() => { const length0 = reader.readNumber(); const items0: Array<AttributeKeyValue> = []; for (let index = 0; index < length0; index += 1) { items0.push(decodeAttributeKeyValue(reader)); } return items0; })();

            return { kind: "keyValues", key_values };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one AttributeArgs. */
export function toJsonAttributeArgs(value: AttributeArgs): Json {
    switch (value.kind) {
        case "none":
            return {
                kind: "none",
            };
        case "value":
            return {
                kind: "value",
                value: toJsonAttributeValue(value.value),
            };
        case "values":
            return {
                kind: "values",
                values: value.values.map((item0) => toJsonAttributeValue(item0)),
            };
        case "keyValues":
            return {
                kind: "keyValues",
                key_values: value.key_values.map((item0) => toJsonAttributeKeyValue(item0)),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one AttributeArgs from one JSON value. */
export function fromJsonAttributeArgs(value: Json): AttributeArgs {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "none":
            return {
                kind,
            };
        case "value":
            return {
                kind,
                value: fromJsonAttributeValue(jsonField(object, "value")),
            };
        case "values":
            return {
                kind,
                values: jsonArray(jsonField(object, "values")).map((item0) => fromJsonAttributeValue(item0)),
            };
        case "keyValues":
            return {
                kind,
                key_values: jsonArray(jsonField(object, "key_values")).map((item0) => fromJsonAttributeKeyValue(item0)),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** A value inside an attribute argument list. */
export type AttributeValue =
    /** An identifier value. */
    | {
          readonly kind: "identifier";
          readonly identifier: AttributeIdentifier;
      }
    /** A type value. */
    | {
          readonly kind: "type";
          readonly type: LocalNodeId;
      }
    /** An integer literal. */
    | {
          readonly kind: "integer";
          readonly integer: bigint;
      }
    /** A floating point literal. */
    | {
          readonly kind: "float";
          readonly float: FloatValue;
      }
    /** A boolean literal. */
    | {
          readonly kind: "boolean";
          readonly boolean: boolean;
      }
    /** A string literal. */
    | {
          readonly kind: "string";
          readonly string: StringId;
      }
    /** A list of values. */
    | {
          readonly kind: "list";
          readonly list: ReadonlyArray<AttributeValue>;
      }
    /** One required value that was omitted. */
    | {
          readonly kind: "missing";
      }
    /** One malformed value fragment. */
    | {
          readonly kind: "error";
      }
;

export const AttributeValue = {
    /** An identifier value. */
    identifier(identifier: AttributeIdentifier): AttributeValue {
        return { kind: "identifier", identifier };
    },

    /** A type value. */
    "type"(type_: LocalNodeId): AttributeValue {
        return { kind: "type", type: type_ };
    },

    /** An integer literal. */
    integer(integer: bigint): AttributeValue {
        return { kind: "integer", integer };
    },

    /** A floating point literal. */
    float(float: FloatValue): AttributeValue {
        return { kind: "float", float };
    },

    /** A boolean literal. */
    "boolean"(boolean_: boolean): AttributeValue {
        return { kind: "boolean", boolean: boolean_ };
    },

    /** A string literal. */
    "string"(string_: StringId): AttributeValue {
        return { kind: "string", string: string_ };
    },

    /** A list of values. */
    list(list: ReadonlyArray<AttributeValue>): AttributeValue {
        return { kind: "list", list };
    },

    /** One required value that was omitted. */
    missing(): AttributeValue {
        return { kind: "missing" };
    },

    /** One malformed value fragment. */
    error(): AttributeValue {
        return { kind: "error" };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: AttributeValue): void {
        encodeAttributeValue(writer, value);
    },

    /** Decode one AttributeValue. */
    decode(reader: BinaryReader): AttributeValue {
        return decodeAttributeValue(reader);
    },

    /** Return this value as JSON. */
    toJson(value: AttributeValue): Json {
        return toJsonAttributeValue(value);
    },

    /** Return one AttributeValue from one JSON value. */
    fromJson(value: Json): AttributeValue {
        return fromJsonAttributeValue(value);
    },
};

/** Encode one AttributeValue. */
export function encodeAttributeValue(writer: BinaryWriter, value: AttributeValue): void {
    switch (value.kind) {
        case "identifier":
            writer.writeUnsigned(0);
            encodeAttributeIdentifier(writer, value.identifier);
            return;
        case "type":
            writer.writeUnsigned(1);
            encodeLocalNodeId(writer, value.type);
            return;
        case "integer":
            writer.writeUnsigned(2);
            writer.writeSigned(value.integer);
            return;
        case "float":
            writer.writeUnsigned(3);
            encodeFloatValue(writer, value.float);
            return;
        case "boolean":
            writer.writeUnsigned(4);
            writer.writeBool(value.boolean);
            return;
        case "string":
            writer.writeUnsigned(5);
            encodeStringId(writer, value.string);
            return;
        case "list":
            writer.writeUnsigned(6);
            writer.writeUnsigned(value.list.length);
            for (const item0 of value.list) {
                encodeAttributeValue(writer, item0);
            }
            return;
        case "missing":
            writer.writeUnsigned(7);
            return;
        case "error":
            writer.writeUnsigned(8);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one AttributeValue. */
export function decodeAttributeValue(reader: BinaryReader): AttributeValue {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const identifier = decodeAttributeIdentifier(reader);

            return { kind: "identifier", identifier };
        }
        case 1: {
            const type_ = decodeLocalNodeId(reader);

            return { kind: "type", type: type_ };
        }
        case 2: {
            const integer = reader.readSigned();

            return { kind: "integer", integer };
        }
        case 3: {
            const float = decodeFloatValue(reader);

            return { kind: "float", float };
        }
        case 4: {
            const boolean_ = reader.readBool();

            return { kind: "boolean", boolean: boolean_ };
        }
        case 5: {
            const string_ = decodeStringId(reader);

            return { kind: "string", string: string_ };
        }
        case 6: {
            const list = (() => { const length0 = reader.readNumber(); const items0: Array<AttributeValue> = []; for (let index = 0; index < length0; index += 1) { items0.push(decodeAttributeValue(reader)); } return items0; })();

            return { kind: "list", list };
        }
        case 7: {
            return { kind: "missing" };
        }
        case 8: {
            return { kind: "error" };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one AttributeValue. */
export function toJsonAttributeValue(value: AttributeValue): Json {
    switch (value.kind) {
        case "identifier":
            return {
                kind: "identifier",
                identifier: toJsonAttributeIdentifier(value.identifier),
            };
        case "type":
            return {
                kind: "type",
                type: toJsonLocalNodeId(value.type),
            };
        case "integer":
            return {
                kind: "integer",
                integer: value.integer.toString(),
            };
        case "float":
            return {
                kind: "float",
                float: toJsonFloatValue(value.float),
            };
        case "boolean":
            return {
                kind: "boolean",
                boolean: value.boolean,
            };
        case "string":
            return {
                kind: "string",
                string: toJsonStringId(value.string),
            };
        case "list":
            return {
                kind: "list",
                list: value.list.map((item0) => toJsonAttributeValue(item0)),
            };
        case "missing":
            return {
                kind: "missing",
            };
        case "error":
            return {
                kind: "error",
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one AttributeValue from one JSON value. */
export function fromJsonAttributeValue(value: Json): AttributeValue {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "identifier":
            return {
                kind,
                identifier: fromJsonAttributeIdentifier(jsonField(object, "identifier")),
            };
        case "type":
            return {
                kind,
                type: fromJsonLocalNodeId(jsonField(object, "type")),
            };
        case "integer":
            return {
                kind,
                integer: jsonBigint(jsonField(object, "integer")),
            };
        case "float":
            return {
                kind,
                float: fromJsonFloatValue(jsonField(object, "float")),
            };
        case "boolean":
            return {
                kind,
                boolean: jsonBool(jsonField(object, "boolean")),
            };
        case "string":
            return {
                kind,
                string: fromJsonStringId(jsonField(object, "string")),
            };
        case "list":
            return {
                kind,
                list: jsonArray(jsonField(object, "list")).map((item0) => fromJsonAttributeValue(item0)),
            };
        case "missing":
            return {
                kind,
            };
        case "error":
            return {
                kind,
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** A floating point literal stored by bit pattern. */
export type FloatValue = {
    /** The IEEE-754 bits for the value. */
    readonly bits: bigint;
};

export const FloatValue = {
    ...FloatValueImpl,

    /** Encode this value. */
    encode(writer: BinaryWriter, value: FloatValue): void {
        encodeFloatValue(writer, value);
    },

    /** Decode one FloatValue. */
    decode(reader: BinaryReader): FloatValue {
        return decodeFloatValue(reader);
    },

    /** Return this value as JSON. */
    toJson(value: FloatValue): Json {
        return toJsonFloatValue(value);
    },

    /** Return one FloatValue from one JSON value. */
    fromJson(value: Json): FloatValue {
        return fromJsonFloatValue(value);
    },
};

/** Encode one FloatValue. */
export function encodeFloatValue(writer: BinaryWriter, value: FloatValue): void {
    writer.writeUnsigned(value.bits);
}

/** Decode one FloatValue. */
export function decodeFloatValue(reader: BinaryReader): FloatValue {
    const bits = reader.readUnsigned();

    return {
        bits,
    };
}

/** Return one JSON value for one FloatValue. */
export function toJsonFloatValue(value: FloatValue): Json {
    return {
        bits: value.bits.toString(),
    };
}

/** Return one FloatValue from one JSON value. */
export function fromJsonFloatValue(value: Json): FloatValue {
    const object = jsonObject(value);

    return {
        bits: jsonBigint(jsonField(object, "bits")),
    };
}

/** A key-value pair within an attribute argument list. */
export type AttributeKeyValue = {
    /** The argument name. */
    readonly key: AttributeIdentifier;
    /** The argument value. */
    readonly value: AttributeValue;
};

export const AttributeKeyValue = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: AttributeKeyValue): void {
        encodeAttributeKeyValue(writer, value);
    },

    /** Decode one AttributeKeyValue. */
    decode(reader: BinaryReader): AttributeKeyValue {
        return decodeAttributeKeyValue(reader);
    },

    /** Return this value as JSON. */
    toJson(value: AttributeKeyValue): Json {
        return toJsonAttributeKeyValue(value);
    },

    /** Return one AttributeKeyValue from one JSON value. */
    fromJson(value: Json): AttributeKeyValue {
        return fromJsonAttributeKeyValue(value);
    },
};

/** Encode one AttributeKeyValue. */
export function encodeAttributeKeyValue(writer: BinaryWriter, value: AttributeKeyValue): void {
    encodeAttributeIdentifier(writer, value.key);
    encodeAttributeValue(writer, value.value);
}

/** Decode one AttributeKeyValue. */
export function decodeAttributeKeyValue(reader: BinaryReader): AttributeKeyValue {
    const key = decodeAttributeIdentifier(reader);
    const value = decodeAttributeValue(reader);

    return {
        key,
        value,
    };
}

/** Return one JSON value for one AttributeKeyValue. */
export function toJsonAttributeKeyValue(value: AttributeKeyValue): Json {
    return {
        key: toJsonAttributeIdentifier(value.key),
        value: toJsonAttributeValue(value.value),
    };
}

/** Return one AttributeKeyValue from one JSON value. */
export function fromJsonAttributeKeyValue(value: Json): AttributeKeyValue {
    const object = jsonObject(value);

    return {
        key: fromJsonAttributeIdentifier(jsonField(object, "key")),
        value: fromJsonAttributeValue(jsonField(object, "value")),
    };
}
