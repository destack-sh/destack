/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, jsonField, jsonObject, jsonString } from "../../../protocol/serde.js";
import type { DispatchSlot } from "../table/dispatch.js";
import type { LocalNodeId } from "./node.js";
import type { Value } from "./value.js";
import type { ValueSlice } from "./value.js";
import { decodeDispatchSlot, encodeDispatchSlot, fromJsonDispatchSlot, toJsonDispatchSlot } from "../table/dispatch.js";
import { decodeLocalNodeId, encodeLocalNodeId, fromJsonLocalNodeId, toJsonLocalNodeId } from "./node.js";
import { decodeValue, encodeValue, fromJsonValue, toJsonValue } from "./value.js";
import { decodeValueSlice, encodeValueSlice, fromJsonValueSlice, toJsonValueSlice } from "./value.js";

/** One call operation shared by instructions and terminators. */
export type Call = {
    /** The callable target. */
    readonly callee: Callee;
    /** The call arguments. */
    readonly arguments: ValueSlice;
    /** The signature type for the callee. */
    readonly signature: LocalNodeId;
};

export const Call = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: Call): void {
        encodeCall(writer, value);
    },

    /** Decode one Call. */
    decode(reader: BinaryReader): Call {
        return decodeCall(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Call): Json {
        return toJsonCall(value);
    },

    /** Return one Call from one JSON value. */
    fromJson(value: Json): Call {
        return fromJsonCall(value);
    },
};

/** Encode one Call. */
export function encodeCall(writer: BinaryWriter, value: Call): void {
    encodeCallee(writer, value.callee);
    encodeValueSlice(writer, value.arguments);
    encodeLocalNodeId(writer, value.signature);
}

/** Decode one Call. */
export function decodeCall(reader: BinaryReader): Call {
    const callee = decodeCallee(reader);
    const arguments_ = decodeValueSlice(reader);
    const signature = decodeLocalNodeId(reader);

    return {
        callee,
        arguments: arguments_,
        signature,
    };
}

/** Return one JSON value for one Call. */
export function toJsonCall(value: Call): Json {
    return {
        callee: toJsonCallee(value.callee),
        arguments: toJsonValueSlice(value.arguments),
        signature: toJsonLocalNodeId(value.signature),
    };
}

/** Return one Call from one JSON value. */
export function fromJsonCall(value: Json): Call {
    const object = jsonObject(value);

    return {
        callee: fromJsonCallee(jsonField(object, "callee")),
        arguments: fromJsonValueSlice(jsonField(object, "arguments")),
        signature: fromJsonLocalNodeId(jsonField(object, "signature")),
    };
}

/** Dispatch performed by one callee. */
export type CallDispatch =
    /** Direct function call. */
    | {
          readonly kind: "direct";
      }
    /** Indirect call through a function value or pointer. */
    | {
          readonly kind: "indirect";
      }
    /** Virtual call through an object dispatch slot. */
    | {
          readonly kind: "virtual";
          /** The dispatch slot for the method. */
          readonly slot: DispatchSlot;
      }
    /** Dynamic call through an erased dispatch table slot. */
    | {
          readonly kind: "dynamic";
          /** The dispatch slot for the method. */
          readonly slot: DispatchSlot;
      }
;

export const CallDispatch = {
    /** Direct function call. */
    direct(): CallDispatch {
        return { kind: "direct" };
    },

    /** Indirect call through a function value or pointer. */
    indirect(): CallDispatch {
        return { kind: "indirect" };
    },

    /** Virtual call through an object dispatch slot. */
    virtual(slot: DispatchSlot): CallDispatch {
        return { kind: "virtual", slot };
    },

    /** Dynamic call through an erased dispatch table slot. */
    dynamic(slot: DispatchSlot): CallDispatch {
        return { kind: "dynamic", slot };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: CallDispatch): void {
        encodeCallDispatch(writer, value);
    },

    /** Decode one CallDispatch. */
    decode(reader: BinaryReader): CallDispatch {
        return decodeCallDispatch(reader);
    },

    /** Return this value as JSON. */
    toJson(value: CallDispatch): Json {
        return toJsonCallDispatch(value);
    },

    /** Return one CallDispatch from one JSON value. */
    fromJson(value: Json): CallDispatch {
        return fromJsonCallDispatch(value);
    },
};

/** Encode one CallDispatch. */
export function encodeCallDispatch(writer: BinaryWriter, value: CallDispatch): void {
    switch (value.kind) {
        case "direct":
            writer.writeUnsigned(0);
            return;
        case "indirect":
            writer.writeUnsigned(1);
            return;
        case "virtual":
            writer.writeUnsigned(2);
            encodeDispatchSlot(writer, value.slot);
            return;
        case "dynamic":
            writer.writeUnsigned(3);
            encodeDispatchSlot(writer, value.slot);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one CallDispatch. */
export function decodeCallDispatch(reader: BinaryReader): CallDispatch {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            return { kind: "direct" };
        }
        case 1: {
            return { kind: "indirect" };
        }
        case 2: {
            const slot = decodeDispatchSlot(reader);

            return {
                kind: "virtual",
                slot,
            };
        }
        case 3: {
            const slot = decodeDispatchSlot(reader);

            return {
                kind: "dynamic",
                slot,
            };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one CallDispatch. */
export function toJsonCallDispatch(value: CallDispatch): Json {
    switch (value.kind) {
        case "direct":
            return {
                kind: "direct",
            };
        case "indirect":
            return {
                kind: "indirect",
            };
        case "virtual":
            return {
                kind: "virtual",
                slot: toJsonDispatchSlot(value.slot),
            };
        case "dynamic":
            return {
                kind: "dynamic",
                slot: toJsonDispatchSlot(value.slot),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one CallDispatch from one JSON value. */
export function fromJsonCallDispatch(value: Json): CallDispatch {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "direct":
            return {
                kind,
            };
        case "indirect":
            return {
                kind,
            };
        case "virtual":
            return {
                kind,
                slot: fromJsonDispatchSlot(jsonField(object, "slot")),
            };
        case "dynamic":
            return {
                kind,
                slot: fromJsonDispatchSlot(jsonField(object, "slot")),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** Stable identifier for one callsite inside a function body. */
export type CallSite =
    /** Callsite stored as an instruction. */
    | {
          readonly kind: "instruction";
          readonly instruction: LocalNodeId;
      }
    /** Callsite stored as a block terminator. */
    | {
          readonly kind: "terminator";
          readonly terminator: LocalNodeId;
      }
;

export const CallSite = {
    /** Callsite stored as an instruction. */
    instruction(instruction: LocalNodeId): CallSite {
        return { kind: "instruction", instruction };
    },

    /** Callsite stored as a block terminator. */
    terminator(terminator: LocalNodeId): CallSite {
        return { kind: "terminator", terminator };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: CallSite): void {
        encodeCallSite(writer, value);
    },

    /** Decode one CallSite. */
    decode(reader: BinaryReader): CallSite {
        return decodeCallSite(reader);
    },

    /** Return this value as JSON. */
    toJson(value: CallSite): Json {
        return toJsonCallSite(value);
    },

    /** Return one CallSite from one JSON value. */
    fromJson(value: Json): CallSite {
        return fromJsonCallSite(value);
    },
};

/** Encode one CallSite. */
export function encodeCallSite(writer: BinaryWriter, value: CallSite): void {
    switch (value.kind) {
        case "instruction":
            writer.writeUnsigned(0);
            encodeLocalNodeId(writer, value.instruction);
            return;
        case "terminator":
            writer.writeUnsigned(1);
            encodeLocalNodeId(writer, value.terminator);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one CallSite. */
export function decodeCallSite(reader: BinaryReader): CallSite {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const instruction = decodeLocalNodeId(reader);

            return { kind: "instruction", instruction };
        }
        case 1: {
            const terminator = decodeLocalNodeId(reader);

            return { kind: "terminator", terminator };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one CallSite. */
export function toJsonCallSite(value: CallSite): Json {
    switch (value.kind) {
        case "instruction":
            return {
                kind: "instruction",
                instruction: toJsonLocalNodeId(value.instruction),
            };
        case "terminator":
            return {
                kind: "terminator",
                terminator: toJsonLocalNodeId(value.terminator),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one CallSite from one JSON value. */
export function fromJsonCallSite(value: Json): CallSite {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "instruction":
            return {
                kind,
                instruction: fromJsonLocalNodeId(jsonField(object, "instruction")),
            };
        case "terminator":
            return {
                kind,
                terminator: fromJsonLocalNodeId(jsonField(object, "terminator")),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** Callable target for one call. */
export type Callee =
    /** Direct function target. */
    | {
          readonly kind: "direct";
          /** The function to call. */
          readonly function: LocalNodeId;
      }
    /** Indirect function value or pointer target. */
    | {
          readonly kind: "indirect";
          /** The function pointer or function value to call. */
          readonly value: Value;
      }
    /** Virtual method target. */
    | {
          readonly kind: "virtual";
          /** The receiver value used for dispatch. */
          readonly receiver: Value;
          /** The class declaring the dispatch slot. */
          readonly class: LocalNodeId;
          /** The virtual dispatch slot. */
          readonly slot: DispatchSlot;
      }
    /** Dynamic constraint target. */
    | {
          readonly kind: "dynamic";
          /** The receiver value used for dispatch. */
          readonly receiver: Value;
          /** The constraint declaring the dispatch slot. */
          readonly constraint: LocalNodeId;
          /** The dynamic dispatch slot. */
          readonly slot: DispatchSlot;
      }
;

export const Callee = {
    /** Direct function target. */
    direct(function_: LocalNodeId): Callee {
        return { kind: "direct", function: function_ };
    },

    /** Indirect function value or pointer target. */
    indirect(value: Value): Callee {
        return { kind: "indirect", value };
    },

    /** Virtual method target. */
    virtual(receiver: Value, class_: LocalNodeId, slot: DispatchSlot): Callee {
        return { kind: "virtual", receiver, class: class_, slot };
    },

    /** Dynamic constraint target. */
    dynamic(receiver: Value, constraint: LocalNodeId, slot: DispatchSlot): Callee {
        return { kind: "dynamic", receiver, constraint, slot };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: Callee): void {
        encodeCallee(writer, value);
    },

    /** Decode one Callee. */
    decode(reader: BinaryReader): Callee {
        return decodeCallee(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Callee): Json {
        return toJsonCallee(value);
    },

    /** Return one Callee from one JSON value. */
    fromJson(value: Json): Callee {
        return fromJsonCallee(value);
    },
};

/** Encode one Callee. */
export function encodeCallee(writer: BinaryWriter, value: Callee): void {
    switch (value.kind) {
        case "direct":
            writer.writeUnsigned(0);
            encodeLocalNodeId(writer, value.function);
            return;
        case "indirect":
            writer.writeUnsigned(1);
            encodeValue(writer, value.value);
            return;
        case "virtual":
            writer.writeUnsigned(2);
            encodeValue(writer, value.receiver);
            encodeLocalNodeId(writer, value.class);
            encodeDispatchSlot(writer, value.slot);
            return;
        case "dynamic":
            writer.writeUnsigned(3);
            encodeValue(writer, value.receiver);
            encodeLocalNodeId(writer, value.constraint);
            encodeDispatchSlot(writer, value.slot);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one Callee. */
export function decodeCallee(reader: BinaryReader): Callee {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const function_ = decodeLocalNodeId(reader);

            return {
                kind: "direct",
                function: function_,
            };
        }
        case 1: {
            const value = decodeValue(reader);

            return {
                kind: "indirect",
                value,
            };
        }
        case 2: {
            const receiver = decodeValue(reader);
            const class_ = decodeLocalNodeId(reader);
            const slot = decodeDispatchSlot(reader);

            return {
                kind: "virtual",
                receiver,
                class: class_,
                slot,
            };
        }
        case 3: {
            const receiver = decodeValue(reader);
            const constraint = decodeLocalNodeId(reader);
            const slot = decodeDispatchSlot(reader);

            return {
                kind: "dynamic",
                receiver,
                constraint,
                slot,
            };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one Callee. */
export function toJsonCallee(value: Callee): Json {
    switch (value.kind) {
        case "direct":
            return {
                kind: "direct",
                function: toJsonLocalNodeId(value.function),
            };
        case "indirect":
            return {
                kind: "indirect",
                value: toJsonValue(value.value),
            };
        case "virtual":
            return {
                kind: "virtual",
                receiver: toJsonValue(value.receiver),
                class: toJsonLocalNodeId(value.class),
                slot: toJsonDispatchSlot(value.slot),
            };
        case "dynamic":
            return {
                kind: "dynamic",
                receiver: toJsonValue(value.receiver),
                constraint: toJsonLocalNodeId(value.constraint),
                slot: toJsonDispatchSlot(value.slot),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one Callee from one JSON value. */
export function fromJsonCallee(value: Json): Callee {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "direct":
            return {
                kind,
                function: fromJsonLocalNodeId(jsonField(object, "function")),
            };
        case "indirect":
            return {
                kind,
                value: fromJsonValue(jsonField(object, "value")),
            };
        case "virtual":
            return {
                kind,
                receiver: fromJsonValue(jsonField(object, "receiver")),
                class: fromJsonLocalNodeId(jsonField(object, "class")),
                slot: fromJsonDispatchSlot(jsonField(object, "slot")),
            };
        case "dynamic":
            return {
                kind,
                receiver: fromJsonValue(jsonField(object, "receiver")),
                constraint: fromJsonLocalNodeId(jsonField(object, "constraint")),
                slot: fromJsonDispatchSlot(jsonField(object, "slot")),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}
