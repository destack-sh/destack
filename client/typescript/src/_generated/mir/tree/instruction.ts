/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, jsonBool, jsonField, jsonInteger, jsonObject, jsonOptional, jsonString } from "../../../protocol/serde.js";
import type { CounterId } from "../table/profile.js";
import type { SamplerId } from "../table/profile.js";
import type { Call } from "./call.js";
import type { Constant } from "./constant.js";
import type { ConvertMode } from "./convert.js";
import type { IndexSlice } from "./immediate.js";
import type { TensorImmediateId } from "./immediate.js";
import type { Intrinsic } from "./intrinsic.js";
import type { AtomicAccess } from "./memory.js";
import type { AtomicRmwOperator } from "./memory.js";
import type { CompareExchangeAccess } from "./memory.js";
import type { FenceAccess } from "./memory.js";
import type { LocalNodeId } from "./node.js";
import type { BinaryOperator } from "./operator.js";
import type { UnaryOperator } from "./operator.js";
import type { TensorIndexReduceOperator } from "./tensor.js";
import type { TensorIndexTieBreak } from "./tensor.js";
import type { TensorReduceOperator } from "./tensor.js";
import type { TensorScatterMode } from "./tensor.js";
import type { Value } from "./value.js";
import type { ValueSlice } from "./value.js";
import type { VectorReduceOperator } from "./vector.js";
import { decodeCounterId, encodeCounterId, fromJsonCounterId, toJsonCounterId } from "../table/profile.js";
import { decodeSamplerId, encodeSamplerId, fromJsonSamplerId, toJsonSamplerId } from "../table/profile.js";
import { decodeCall, encodeCall, fromJsonCall, toJsonCall } from "./call.js";
import { decodeConstant, encodeConstant, fromJsonConstant, toJsonConstant } from "./constant.js";
import { decodeConvertMode, encodeConvertMode, fromJsonConvertMode, toJsonConvertMode } from "./convert.js";
import { decodeIndexSlice, encodeIndexSlice, fromJsonIndexSlice, toJsonIndexSlice } from "./immediate.js";
import { decodeTensorImmediateId, encodeTensorImmediateId, fromJsonTensorImmediateId, toJsonTensorImmediateId } from "./immediate.js";
import { decodeIntrinsic, encodeIntrinsic, fromJsonIntrinsic, toJsonIntrinsic } from "./intrinsic.js";
import { decodeAtomicAccess, encodeAtomicAccess, fromJsonAtomicAccess, toJsonAtomicAccess } from "./memory.js";
import { decodeAtomicRmwOperator, encodeAtomicRmwOperator, fromJsonAtomicRmwOperator, toJsonAtomicRmwOperator } from "./memory.js";
import { decodeCompareExchangeAccess, encodeCompareExchangeAccess, fromJsonCompareExchangeAccess, toJsonCompareExchangeAccess } from "./memory.js";
import { decodeFenceAccess, encodeFenceAccess, fromJsonFenceAccess, toJsonFenceAccess } from "./memory.js";
import { decodeLocalNodeId, encodeLocalNodeId, fromJsonLocalNodeId, toJsonLocalNodeId } from "./node.js";
import { decodeBinaryOperator, encodeBinaryOperator, fromJsonBinaryOperator, toJsonBinaryOperator } from "./operator.js";
import { decodeUnaryOperator, encodeUnaryOperator, fromJsonUnaryOperator, toJsonUnaryOperator } from "./operator.js";
import { decodeTensorIndexReduceOperator, encodeTensorIndexReduceOperator, fromJsonTensorIndexReduceOperator, toJsonTensorIndexReduceOperator } from "./tensor.js";
import { decodeTensorIndexTieBreak, encodeTensorIndexTieBreak, fromJsonTensorIndexTieBreak, toJsonTensorIndexTieBreak } from "./tensor.js";
import { decodeTensorReduceOperator, encodeTensorReduceOperator, fromJsonTensorReduceOperator, toJsonTensorReduceOperator } from "./tensor.js";
import { decodeTensorScatterMode, encodeTensorScatterMode, fromJsonTensorScatterMode, toJsonTensorScatterMode } from "./tensor.js";
import { decodeValue, encodeValue, fromJsonValue, toJsonValue } from "./value.js";
import { decodeValueSlice, encodeValueSlice, fromJsonValueSlice, toJsonValueSlice } from "./value.js";
import { decodeVectorReduceOperator, encodeVectorReduceOperator, fromJsonVectorReduceOperator, toJsonVectorReduceOperator } from "./vector.js";

/** Kind of type cast. */
export type CastOperator = "bitcast" | "truncate" | "saturate" | "zeroExtend" | "signExtend" | "floatToSignedInt" | "floatToUnsignedInt" | "floatToSignedIntSaturating" | "floatToUnsignedIntSaturating" | "signedIntToFloat" | "unsignedIntToFloat" | "floatTruncate" | "floatExtend" | "floatConvert" | "pointerToInt" | "intToPointer";

export const CastOperator = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: CastOperator): void {
        encodeCastOperator(writer, value);
    },

    /** Decode one CastOperator. */
    decode(reader: BinaryReader): CastOperator {
        return decodeCastOperator(reader);
    },

    /** Return this value as JSON. */
    toJson(value: CastOperator): Json {
        return toJsonCastOperator(value);
    },

    /** Return one CastOperator from one JSON value. */
    fromJson(value: Json): CastOperator {
        return fromJsonCastOperator(value);
    },
};

/** Encode one CastOperator. */
export function encodeCastOperator(writer: BinaryWriter, value: CastOperator): void {
    switch (value) {
        case "bitcast":
            writer.writeUnsigned(0);
            return;
        case "truncate":
            writer.writeUnsigned(1);
            return;
        case "saturate":
            writer.writeUnsigned(2);
            return;
        case "zeroExtend":
            writer.writeUnsigned(3);
            return;
        case "signExtend":
            writer.writeUnsigned(4);
            return;
        case "floatToSignedInt":
            writer.writeUnsigned(5);
            return;
        case "floatToUnsignedInt":
            writer.writeUnsigned(6);
            return;
        case "floatToSignedIntSaturating":
            writer.writeUnsigned(7);
            return;
        case "floatToUnsignedIntSaturating":
            writer.writeUnsigned(8);
            return;
        case "signedIntToFloat":
            writer.writeUnsigned(9);
            return;
        case "unsignedIntToFloat":
            writer.writeUnsigned(10);
            return;
        case "floatTruncate":
            writer.writeUnsigned(11);
            return;
        case "floatExtend":
            writer.writeUnsigned(12);
            return;
        case "floatConvert":
            writer.writeUnsigned(13);
            return;
        case "pointerToInt":
            writer.writeUnsigned(14);
            return;
        case "intToPointer":
            writer.writeUnsigned(15);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one CastOperator. */
export function decodeCastOperator(reader: BinaryReader): CastOperator {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "bitcast";
        case 1:
            return "truncate";
        case 2:
            return "saturate";
        case 3:
            return "zeroExtend";
        case 4:
            return "signExtend";
        case 5:
            return "floatToSignedInt";
        case 6:
            return "floatToUnsignedInt";
        case 7:
            return "floatToSignedIntSaturating";
        case 8:
            return "floatToUnsignedIntSaturating";
        case 9:
            return "signedIntToFloat";
        case 10:
            return "unsignedIntToFloat";
        case 11:
            return "floatTruncate";
        case 12:
            return "floatExtend";
        case 13:
            return "floatConvert";
        case 14:
            return "pointerToInt";
        case 15:
            return "intToPointer";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one CastOperator. */
export function toJsonCastOperator(value: CastOperator): Json {
    return value;
}

/** Return one CastOperator from one JSON value. */
export function fromJsonCastOperator(value: Json): CastOperator {
    const variant = jsonString(value);

    switch (variant) {
        case "bitcast":
            return "bitcast";
        case "truncate":
            return "truncate";
        case "saturate":
            return "saturate";
        case "zeroExtend":
            return "zeroExtend";
        case "signExtend":
            return "signExtend";
        case "floatToSignedInt":
            return "floatToSignedInt";
        case "floatToUnsignedInt":
            return "floatToUnsignedInt";
        case "floatToSignedIntSaturating":
            return "floatToSignedIntSaturating";
        case "floatToUnsignedIntSaturating":
            return "floatToUnsignedIntSaturating";
        case "signedIntToFloat":
            return "signedIntToFloat";
        case "unsignedIntToFloat":
            return "unsignedIntToFloat";
        case "floatTruncate":
            return "floatTruncate";
        case "floatExtend":
            return "floatExtend";
        case "floatConvert":
            return "floatConvert";
        case "pointerToInt":
            return "pointerToInt";
        case "intToPointer":
            return "intToPointer";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** One MIR instruction. */
export type Instruction =
    /** Recovered invalid instruction syntax. */
    | {
          readonly kind: "error";
      }
    /** Load a constant value. */
    | {
          readonly kind: "const";
          /** The SSA value to define. */
          readonly destination: Value;
          /** The constant value to load. */
          readonly value: Constant;
      }
    /** Binary operation (e.g., add, subtract, compare). */
    | {
          readonly kind: "binary";
          /** The SSA value to define with the result. */
          readonly destination: Value;
          /** The binary operator to apply. */
          readonly operator: BinaryOperator;
          /** The left-hand operand. */
          readonly left: Value;
          /** The right-hand operand. */
          readonly right: Value;
      }
    /** Unary operation (e.g., negate, not). */
    | {
          readonly kind: "unary";
          /** The SSA value to define with the result. */
          readonly destination: Value;
          /** The unary operator to apply. */
          readonly operator: UnaryOperator;
          /** The operand. */
          readonly argument: Value;
      }
    /** Cast between types (bitcast, truncate, extend, etc.). */
    | {
          readonly kind: "cast";
          /** The SSA value to define with the converted result. */
          readonly destination: Value;
          /** The cast operator to perform. */
          readonly operator: CastOperator;
          /** The value to cast. */
          readonly argument: Value;
          /** The target type to cast to. */
          readonly toType: LocalNodeId;
      }
    /** Select a value based on a boolean condition. */
    | {
          readonly kind: "select";
          /** The SSA value to define with the selected result. */
          readonly destination: Value;
          /** The boolean condition (must be bool type). */
          readonly condition: Value;
          /** The value returned if condition is true. */
          readonly thenValue: Value;
          /** The value returned if condition is false. */
          readonly elseValue: Value;
      }
    /** Load from a local variable (stack slot). */
    | {
          readonly kind: "localGet";
          /** The SSA value to define with the loaded value. */
          readonly destination: Value;
          /** The local variable to load from. */
          readonly local: LocalNodeId;
      }
    /** Get a reference to a local variable. */
    | {
          readonly kind: "localAddr";
          /** The SSA value to define with the local address. */
          readonly destination: Value;
          /** The local variable to take the address of. */
          readonly local: LocalNodeId;
          /** The result reference type. */
          readonly resultType: LocalNodeId;
      }
    /** Store to a local variable (stack slot). */
    | {
          readonly kind: "localSet";
          /** The local variable to store to. */
          readonly local: LocalNodeId;
          /** The value to store. */
          readonly value: Value;
      }
    /** Get a reference to a mutable global variable. */
    | {
          readonly kind: "globalAddr";
          /** The SSA value to define with the reference. */
          readonly destination: Value;
          /** The global variable to get the address of. */
          readonly global: LocalNodeId;
          /** The result reference type. */
          readonly resultType: LocalNodeId;
      }
    /** Get a function pointer for a function (function.address). */
    | {
          readonly kind: "functionAddr";
          /** The SSA value to define with the function pointer. */
          readonly destination: Value;
          /** The function to take the address of. */
          readonly function: LocalNodeId;
      }
    /** Bind one environment to a function and produce a function value (function.bind). */
    | {
          readonly kind: "functionBind";
          /** The SSA value to define with the function value. */
          readonly destination: Value;
          /** The function to pair with the environment. */
          readonly function: LocalNodeId;
          /** The environment value to capture. */
          readonly environment: Value;
      }
    /** Project the environment from one function value (function.environment). */
    | {
          readonly kind: "functionEnvironment";
          /** The SSA value to define with the environment. */
          readonly destination: Value;
          /** The function value to project. */
          readonly function: Value;
      }
    /** Load the hidden environment for the current function (function.environment.current). */
    | {
          readonly kind: "functionEnvironmentCurrent";
          /** The SSA value to define with the hidden environment pointer. */
          readonly destination: Value;
      }
    /** Load the current execution context. */
    | {
          readonly kind: "contextCurrent";
          /** The SSA value to define with the current context. */
          readonly destination: Value;
      }
    /** Replace the current execution context and return its previous value. */
    | {
          readonly kind: "contextReplace";
          /** The SSA value to define with the previous context. */
          readonly destination: Value;
          /** The new current context. */
          readonly context: Value;
      }
    /** Extend one execution context with a variable value. */
    | {
          readonly kind: "contextBind";
          /** The SSA value to define with the extended context. */
          readonly destination: Value;
          /** The context to extend. */
          readonly context: Value;
          /** The context variable identity. */
          readonly variable: Value;
          /** The value to bind. */
          readonly value: Value;
          /** The physical context node type. */
          readonly nodeType: LocalNodeId;
          /** The result context type. */
          readonly resultType: LocalNodeId;
      }
    /** Load one variable value from an execution context. */
    | {
          readonly kind: "contextGet";
          /** The SSA value to define with the selected value. */
          readonly destination: Value;
          /** The context to search. */
          readonly context: Value;
          /** The context variable identity. */
          readonly variable: Value;
          /** The value returned when the variable is unbound. */
          readonly default: Value;
          /** The physical context node type. */
          readonly nodeType: LocalNodeId;
          /** The result value type. */
          readonly resultType: LocalNodeId;
      }
    /** Load from a pointer (dereference). */
    | {
          readonly kind: "load";
          /** The SSA value to define with the loaded value. */
          readonly destination: Value;
          /** The pointer to load from. */
          readonly pointer: Value;
          /** The loaded value type. */
          readonly resultType: LocalNodeId;
      }
    /** Store to a pointer (write through pointer). */
    | {
          readonly kind: "store";
          /** The pointer to store to. */
          readonly pointer: Value;
          /** The value to store. */
          readonly value: Value;
      }
    /** Construct an aggregate from values in logical slot order. */
    | {
          readonly kind: "aggregate";
          /** The SSA value to define with the constructed aggregate. */
          readonly destination: Value;
          /** The aggregate values stored in the tree's value buffer. */
          readonly values: ValueSlice;
      }
    /** Extract one structural field from an aggregate value. */
    | {
          readonly kind: "fieldGet";
          /** The SSA value to define with the extracted field. */
          readonly destination: Value;
          /** The aggregate value to extract from. */
          readonly aggregate: Value;
          /** The zero-based logical field. */
          readonly field: number;
      }
    /** Replace one structural field in an aggregate value. */
    | {
          readonly kind: "fieldSet";
          /** The SSA value to define with the new aggregate. */
          readonly destination: Value;
          /** The original aggregate value. */
          readonly aggregate: Value;
          /** The zero-based logical field to update. */
          readonly field: number;
          /** The value to insert at the field. */
          readonly value: Value;
      }
    /** Get the address of one structural field in an addressable aggregate. */
    | {
          readonly kind: "fieldAddr";
          /** The SSA value to define with the field address. */
          readonly destination: Value;
          /** The aggregate base to project from. */
          readonly aggregate: Value;
          /** The zero-based logical field. */
          readonly field: number;
          /** The result type of the address. */
          readonly resultType: LocalNodeId;
      }
    /** Extract one statically selected fixed-array element. */
    | {
          readonly kind: "elementGet";
          /** The SSA value to define with the extracted element. */
          readonly destination: Value;
          /** The fixed-array aggregate to extract from. */
          readonly aggregate: Value;
          /** The zero-based element index. */
          readonly index: number;
      }
    /** Insert one value into a statically selected fixed-array element. */
    | {
          readonly kind: "elementSet";
          /** The SSA value to define with the updated fixed array. */
          readonly destination: Value;
          /** The original fixed-array aggregate. */
          readonly aggregate: Value;
          /** The zero-based element index. */
          readonly index: number;
          /** The value to insert. */
          readonly value: Value;
      }
    /** Get the address of an element from an addressable indexed value (element.address). */
    | {
          readonly kind: "elementAddr";
          /** The SSA value to define with the element address. */
          readonly destination: Value;
          /** The indexed base to project from. */
          readonly base: Value;
          /** The index of the element (runtime value). */
          readonly index: Value;
          /** The result type of the address. */
          readonly resultType: LocalNodeId;
      }
    /** Construct a variant value from one case payload. */
    | {
          readonly kind: "variantNew";
          /** The SSA value to define with the constructed variant. */
          readonly destination: Value;
          /** The zero-based case index. */
          readonly case: number;
          /** The case payload, or None for payloadless cases. */
          readonly payload?: Value;
          /** The result variant type. */
          readonly resultType: LocalNodeId;
      }
    /** Read the discriminant of a variant value. */
    | {
          readonly kind: "variantTag";
          /** The SSA value to define with the discriminant. */
          readonly destination: Value;
          /** The variant value whose discriminant is read. */
          readonly variant: Value;
      }
    /** Read the discriminant of a stored variant. */
    | {
          readonly kind: "variantTagLoad";
          /** The SSA value to define with the discriminant. */
          readonly destination: Value;
          /** The variant address to read from. */
          readonly variant: Value;
      }
    /** Extract the payload of one statically selected variant case. */
    | {
          readonly kind: "variantPayload";
          /** The SSA value to define with the extracted payload. */
          readonly destination: Value;
          /** The variant value to extract from. */
          readonly variant: Value;
          /** The zero-based case index. */
          readonly case: number;
      }
    /** Get the address of one statically selected variant payload. */
    | {
          readonly kind: "variantPayloadAddr";
          /** The SSA value to define with the payload address. */
          readonly destination: Value;
          /** The variant address to project from. */
          readonly variant: Value;
          /** The zero-based case index. */
          readonly case: number;
          /** The result type of the address. */
          readonly resultType: LocalNodeId;
      }
    /** Form a non-owning slice view over a contiguous source region. */
    | {
          readonly kind: "sliceView";
          /** The SSA value to define with the slice view. */
          readonly destination: Value;
          /** The source slice value. */
          readonly source: Value;
          /** The start index inside the source slice. */
          readonly start: Value;
          /** The number of elements in the result. */
          readonly length: Value;
          /** The result slice type. */
          readonly resultType: LocalNodeId;
      }
    /** Read the runtime length from a slice descriptor. */
    | {
          readonly kind: "sliceLength";
          /** The SSA value to define with the length. */
          readonly destination: Value;
          /** The slice value whose length is read. */
          readonly slice: Value;
      }
    /** Bind a typed reference payload to its concrete dispatch implementation. */
    | {
          readonly kind: "dynamicBind";
          /** The SSA value to define with the dynamic value. */
          readonly destination: Value;
          /** The typed payload reference. */
          readonly payload: Value;
          /** The concrete payload type paired with the destination constraint. */
          readonly concrete: LocalNodeId;
      }
    /** Read the erased payload from a dynamic value. */
    | {
          readonly kind: "dynamicPayload";
          /** The SSA value to define with the payload. */
          readonly destination: Value;
          /** The dynamic value whose payload is read. */
          readonly dynamic: Value;
          /** The result type of the payload value. */
          readonly resultType: LocalNodeId;
      }
    /** Read the concrete type id from a dynamic value's dispatch table. */
    | {
          readonly kind: "dynamicType";
          /** The SSA value to define with the type id. */
          readonly destination: Value;
          /** The dynamic value whose concrete type is read. */
          readonly dynamic: Value;
      }
    /** Find one named entry through a dynamic value's concrete table. */
    | {
          readonly kind: "dynamicFind";
          /** The SSA value to define with the optional entry value. */
          readonly destination: Value;
          /** The dynamic value whose entries are searched. */
          readonly dynamic: Value;
          /** The string key value naming the entry. */
          readonly key: Value;
          /** The result type carrying the found value or undefined. */
          readonly resultType: LocalNodeId;
      }
    /** Broadcast a scalar to all vector lanes. */
    | {
          readonly kind: "vectorSplat";
          /** The SSA value to define with the vector result. */
          readonly destination: Value;
          /** The scalar value to broadcast. */
          readonly value: Value;
      }
    /** Extract a lane from a vector. */
    | {
          readonly kind: "vectorExtract";
          /** The SSA value to define with the extracted lane. */
          readonly destination: Value;
          /** The vector value to extract from. */
          readonly vector: Value;
          /** The lane index to extract. */
          readonly index: Value;
      }
    /** Insert a lane into a vector. */
    | {
          readonly kind: "vectorInsert";
          /** The SSA value to define with the updated vector. */
          readonly destination: Value;
          /** The original vector value. */
          readonly vector: Value;
          /** The lane index to update. */
          readonly index: Value;
          /** The lane value to insert. */
          readonly value: Value;
      }
    /** Shuffle vector lanes using a constant mask. */
    | {
          readonly kind: "vectorShuffle";
          /** The SSA value to define with the shuffled result. */
          readonly destination: Value;
          /** The left vector operand. */
          readonly left: Value;
          /** The right vector operand. */
          readonly right: Value;
          /** The shuffle mask indices. */
          readonly mask: IndexSlice;
      }
    /** Select vector lanes based on a boolean mask. */
    | {
          readonly kind: "vectorSelect";
          /** The SSA value to define with the selected result. */
          readonly destination: Value;
          /** The boolean mask vector. */
          readonly mask: Value;
          /** The value returned if the mask lane is true. */
          readonly thenValue: Value;
          /** The value returned if the mask lane is false. */
          readonly elseValue: Value;
      }
    /** Reduce a vector to a scalar. */
    | {
          readonly kind: "vectorReduce";
          /** The SSA value to define with the reduced result. */
          readonly destination: Value;
          /** The reduction operator to apply. */
          readonly operator: VectorReduceOperator;
          /** The vector value to reduce. */
          readonly vector: Value;
      }
    /** Compare two vectors elementwise. */
    | {
          readonly kind: "vectorCompare";
          /** The SSA value to define with the comparison result. */
          readonly destination: Value;
          /** The comparison operator to apply. */
          readonly operator: BinaryOperator;
          /** The left vector operand. */
          readonly left: Value;
          /** The right vector operand. */
          readonly right: Value;
      }
    /** Convert vector element types with an explicit mode. */
    | {
          readonly kind: "vectorConvert";
          /** The SSA value to define with the converted vector. */
          readonly destination: Value;
          /** The conversion mode to apply. */
          readonly mode: ConvertMode;
          /** The vector value to convert. */
          readonly vector: Value;
      }
    /** Broadcast a scalar to all tensor elements. */
    | {
          readonly kind: "tensorSplat";
          /** The SSA value to define with the tensor result. */
          readonly destination: Value;
          /** The scalar value to broadcast. */
          readonly value: Value;
      }
    /** Load a tensor element from a tensor reference. */
    | {
          readonly kind: "tensorLoad";
          /** The SSA value to define with the loaded element. */
          readonly destination: Value;
          /** The tensor reference to load from. */
          readonly view: Value;
          /** The index values (stored in Tree's argument buffer). */
          readonly indices: ValueSlice;
      }
    /** Extract a tensor element from a tensor value. */
    | {
          readonly kind: "tensorExtract";
          /** The SSA value to define with the extracted element. */
          readonly destination: Value;
          /** The tensor value to extract from. */
          readonly tensor: Value;
          /** The index values (stored in Tree's argument buffer). */
          readonly indices: ValueSlice;
      }
    /** Store a tensor element into a tensor reference. */
    | {
          readonly kind: "tensorStore";
          /** The tensor reference to store into. */
          readonly view: Value;
          /** The index values (stored in Tree's argument buffer). */
          readonly indices: ValueSlice;
          /** The value to store. */
          readonly value: Value;
      }
    /** Fill a tensor reference with a scalar value. */
    | {
          readonly kind: "tensorFill";
          /** The tensor reference to fill. */
          readonly view: Value;
          /** The scalar value to write. */
          readonly value: Value;
      }
    /** Copy elements from a source tensor reference into a destination tensor reference. */
    | {
          readonly kind: "tensorCopy";
          /** The destination tensor reference. */
          readonly target: Value;
          /** The source tensor reference. */
          readonly source: Value;
      }
    /** Reshape a tensor value into a new shape. */
    | {
          readonly kind: "tensorReshape";
          /** The SSA value to define with the reshaped tensor. */
          readonly destination: Value;
          /** The tensor value to reshape. */
          readonly tensor: Value;
          /** The shape values (stored in Tree's argument buffer). */
          readonly shape: ValueSlice;
      }
    /** Broadcast a tensor into a larger shape. */
    | {
          readonly kind: "tensorBroadcast";
          /** The SSA value to define with the broadcasted tensor. */
          readonly destination: Value;
          /** The tensor value to broadcast. */
          readonly tensor: Value;
          /** The operand dimensions mapped into the result. */
          readonly dimensions: IndexSlice;
      }
    /** Permute tensor dimensions. */
    | {
          readonly kind: "tensorTranspose";
          /** The SSA value to define with the transposed tensor. */
          readonly destination: Value;
          /** The tensor value to transpose. */
          readonly tensor: Value;
          /** The permutation of dimensions. */
          readonly permutation: IndexSlice;
      }
    /** Refine a tensor type without changing its contents. */
    | {
          readonly kind: "tensorCast";
          /** The SSA value to define with the cast tensor. */
          readonly destination: Value;
          /** The tensor value to cast. */
          readonly tensor: Value;
      }
    /** Create a view into a tensor reference. */
    | {
          readonly kind: "tensorView";
          /** The SSA value to define with the view result. */
          readonly destination: Value;
          /** The tensor reference to view. */
          readonly view: Value;
          /** The view arguments (offsets, sizes, strides) stored in Tree's argument buffer. */
          readonly arguments: ValueSlice;
          /** The number of offset values. */
          readonly offsetsCount: number;
          /** The number of size values. */
          readonly sizesCount: number;
          /** The number of stride values. */
          readonly stridesCount: number;
      }
    /** Slice a tensor by offsets, sizes, and strides. */
    | {
          readonly kind: "tensorSlice";
          /** The SSA value to define with the sliced tensor. */
          readonly destination: Value;
          /** The tensor value to slice. */
          readonly tensor: Value;
          /** The slice arguments (offsets, sizes, strides) stored in Tree's argument buffer. */
          readonly arguments: ValueSlice;
          /** The number of offset values. */
          readonly offsetsCount: number;
          /** The number of size values. */
          readonly sizesCount: number;
          /** The number of stride values. */
          readonly stridesCount: number;
      }
    /** Pad a tensor with low, high, and interior padding. */
    | {
          readonly kind: "tensorPad";
          /** The SSA value to define with the padded tensor. */
          readonly destination: Value;
          /** The tensor value to pad. */
          readonly tensor: Value;
          /** The padding arguments (low, high, interior) stored in Tree's argument buffer. */
          readonly arguments: ValueSlice;
          /** The number of low padding values. */
          readonly lowCount: number;
          /** The number of high padding values. */
          readonly highCount: number;
          /** The number of interior padding values. */
          readonly interiorCount: number;
          /** The scalar padding value. */
          readonly value: Value;
      }
    /** Concatenate tensors along a dimension. */
    | {
          readonly kind: "tensorConcat";
          /** The SSA value to define with the concatenated tensor. */
          readonly destination: Value;
          /** The tensor operands stored in Tree's argument buffer. */
          readonly tensors: ValueSlice;
          /** The concatenation axis. */
          readonly axis: number;
      }
    /** Compare two tensors elementwise. */
    | {
          readonly kind: "tensorCompare";
          /** The SSA value to define with the comparison result. */
          readonly destination: Value;
          /** The comparison operator to apply. */
          readonly operator: BinaryOperator;
          /** The left tensor operand. */
          readonly left: Value;
          /** The right tensor operand. */
          readonly right: Value;
      }
    /** Select tensor elements based on a boolean mask. */
    | {
          readonly kind: "tensorSelect";
          /** The SSA value to define with the selected tensor. */
          readonly destination: Value;
          /** The boolean mask tensor. */
          readonly mask: Value;
          /** The tensor returned if the mask element is true. */
          readonly thenValue: Value;
          /** The tensor returned if the mask element is false. */
          readonly elseValue: Value;
      }
    /** Reduce a tensor along axes with a fixed operator. */
    | {
          readonly kind: "tensorReduce";
          /** The SSA value to define with the reduced tensor. */
          readonly destination: Value;
          /** The reduction operator to apply. */
          readonly operator: TensorReduceOperator;
          /** The tensor value to reduce. */
          readonly tensor: Value;
          /** The initial value for the reduction. */
          readonly initial: Value;
          /** The axes to reduce. */
          readonly axes: IndexSlice;
      }
    /** Reduce a tensor along one axis and return selected source indices. */
    | {
          readonly kind: "tensorIndexReduce";
          /** The SSA value to define with the index tensor. */
          readonly destination: Value;
          /** The index reduction operator to apply. */
          readonly operator: TensorIndexReduceOperator;
          /** The tensor value to reduce. */
          readonly tensor: Value;
          /** The axis to reduce. */
          readonly axis: number;
          /** The behavior for equal selected values. */
          readonly tieBreak: TensorIndexTieBreak;
      }
    /** Dot product of two tensors. */
    | {
          readonly kind: "tensorDot";
          /** The SSA value to define with the dot result. */
          readonly destination: Value;
          /** The left operand. */
          readonly left: Value;
          /** The right operand. */
          readonly right: Value;
          /** The dot dimension numbers. */
          readonly immediate: TensorImmediateId;
      }
    /** Convolution between an input tensor and a kernel tensor. */
    | {
          readonly kind: "tensorConvolution";
          /** The SSA value to define with the convolution result. */
          readonly destination: Value;
          /** The input tensor. */
          readonly input: Value;
          /** The kernel tensor. */
          readonly kernel: Value;
          /** The convolution dimension numbers. */
          readonly immediate: TensorImmediateId;
      }
    /** Gather slices from a tensor based on indices. */
    | {
          readonly kind: "tensorGather";
          /** The SSA value to define with the gathered tensor. */
          readonly destination: Value;
          /** The operand tensor. */
          readonly operand: Value;
          /** The indices tensor. */
          readonly indices: Value;
          /** The gather dimension numbers. */
          readonly immediate: TensorImmediateId;
      }
    /** Scatter updates into a tensor based on indices. */
    | {
          readonly kind: "tensorScatter";
          /** The SSA value to define with the scatter result. */
          readonly destination: Value;
          /** The operand tensor. */
          readonly operand: Value;
          /** The indices tensor. */
          readonly indices: Value;
          /** The updates tensor. */
          readonly updates: Value;
          /** The scatter dimension numbers. */
          readonly immediate: TensorImmediateId;
          /** The scatter update mode. */
          readonly mode: TensorScatterMode;
      }
    /** Convert a tensor element type. */
    | {
          readonly kind: "tensorConvert";
          /** The SSA value to define with the converted tensor. */
          readonly destination: Value;
          /** The conversion mode to apply. */
          readonly mode: ConvertMode;
          /** The tensor value to convert. */
          readonly tensor: Value;
      }
    /** Call one callable target without a local unwind continuation. */
    | {
          readonly kind: "call";
          /** The SSA value to define with the return value, if any. */
          readonly destination?: Value;
          /** The call operation. */
          readonly call: Call;
      }
    /** Drop one value. */
    | {
          readonly kind: "drop";
          /** The value to drop. */
          readonly value: Value;
      }
    /** Allocate zeroed typed heap storage (`new.zeroed`). */
    | {
          readonly kind: "newZeroed";
          /** The SSA value to define with the allocated reference. */
          readonly destination: Value;
          /** The type stored in the allocation. */
          readonly storageType: LocalNodeId;
          /** The result type of the allocation. */
          readonly resultType: LocalNodeId;
      }
    /** Allocate uninitialized typed heap storage (`new.uninit`). */
    | {
          readonly kind: "newUninit";
          /** The SSA value to define with the initialization token. */
          readonly destination: Value;
          /** The type stored in the allocation. */
          readonly storageType: LocalNodeId;
          /** The result type of the allocation. */
          readonly resultType: LocalNodeId;
      }
    /** Complete initialization of one allocation (`new.complete`). */
    | {
          readonly kind: "newComplete";
          /** The SSA value to define with the completed allocation. */
          readonly destination: Value;
          /** The initialization token to complete. */
          readonly value: Value;
          /** The result type of the completed value. */
          readonly resultType: LocalNodeId;
      }
    /** Allocate zeroed typed repeated heap storage (`new.slice.zeroed`). */
    | {
          readonly kind: "newSliceZeroed";
          /** The SSA value to define with the allocated slice. */
          readonly destination: Value;
          /** The element type of the repeated storage. */
          readonly element: LocalNodeId;
          /** The number of elements (runtime value). */
          readonly length: Value;
          /** The result type of the allocation. */
          readonly resultType: LocalNodeId;
      }
    /** Allocate uninitialized typed repeated heap storage (`new.slice.uninit`). */
    | {
          readonly kind: "newSliceUninit";
          /** The SSA value to define with the initialization token. */
          readonly destination: Value;
          /** The element type of the repeated storage. */
          readonly element: LocalNodeId;
          /** The number of elements (runtime value). */
          readonly length: Value;
          /** The result type of the allocation. */
          readonly resultType: LocalNodeId;
      }
    /** Release one unique carrier's backing heap allocation (`free`). */
    | {
          readonly kind: "free";
          /** The unique heap carrier whose backing allocation is released. */
          readonly value: Value;
      }
    /** Stabilize one heap value against movement (`pin`). */
    | {
          readonly kind: "pin";
          /** The SSA value to define with the pinned reference. */
          readonly destination: Value;
          /** The heap value to pin. */
          readonly value: Value;
          /** The result type of the pinned reference. */
          readonly resultType: LocalNodeId;
      }
    /** Release one heap pin (`unpin`). */
    | {
          readonly kind: "unpin";
          /** The heap value to unpin. */
          readonly value: Value;
      }
    /** Record a managed reference write for the collector. */
    | {
          readonly kind: "barrierWrite";
          /** The managed object whose reference range changed. */
          readonly object: Value;
          /** The byte offset of the changed reference range. */
          readonly offset: Value;
          /** The changed byte length. */
          readonly byteLen: Value;
      }
    /** Load from memory atomically. */
    | {
          readonly kind: "atomicLoad";
          /** The SSA value to define with the loaded result. */
          readonly destination: Value;
          /** The pointer to load from. */
          readonly pointer: Value;
          /** The loaded value type. */
          readonly resultType: LocalNodeId;
          /** The atomic access. */
          readonly access: AtomicAccess;
      }
    /** Store to memory atomically. */
    | {
          readonly kind: "atomicStore";
          /** The pointer to store to. */
          readonly pointer: Value;
          /** The value to store. */
          readonly value: Value;
          /** The atomic access. */
          readonly access: AtomicAccess;
      }
    /** Compare exchange one memory location atomically. */
    | {
          readonly kind: "atomicCompareExchange";
          /** The SSA value to define with the old value and success flag. */
          readonly destination: Value;
          /** The pointer to update. */
          readonly pointer: Value;
          /** The expected current value. */
          readonly expected: Value;
          /** The replacement value. */
          readonly newValue: Value;
          /** Whether the compare exchange is weak. */
          readonly isWeak: boolean;
          /** The compare exchange access. */
          readonly access: CompareExchangeAccess;
      }
    /** Apply one atomic read modify write operation. */
    | {
          readonly kind: "atomicRmw";
          /** The SSA value to define with the old value. */
          readonly destination: Value;
          /** The read modify write operator. */
          readonly operator: AtomicRmwOperator;
          /** The pointer to update. */
          readonly pointer: Value;
          /** The value argument for the operator. */
          readonly value: Value;
          /** The atomic access. */
          readonly access: AtomicAccess;
      }
    /** Publish one memory fence. */
    | {
          readonly kind: "atomicFence";
          /** The fence access. */
          readonly access: FenceAccess;
      }
    /** Assume a condition is true (UB if false). */
    | {
          readonly kind: "assume";
          /** The condition to assume. */
          readonly condition: Value;
      }
    /** Increment one profile counter. */
    | {
          readonly kind: "profileIncrement";
          /** The counter to increment. */
          readonly counter: CounterId;
      }
    /** Sample one runtime value. */
    | {
          readonly kind: "profileSample";
          /** The sampler receiving the value. */
          readonly sampler: SamplerId;
          /** The sampled MIR value. */
          readonly value: Value;
      }
    /** Cooperate with runtime work at one explicit statepoint. */
    | {
          readonly kind: "poll";
      }
    /** Debugger breakpoint. */
    | {
          readonly kind: "breakpoint";
      }
    /** Call a machine intrinsic. */
    | {
          readonly kind: "intrinsic";
          /** The SSA value to define with the result, if any. */
          readonly destination?: Value;
          /** The intrinsic to call. */
          readonly intrinsic: Intrinsic;
          /** The arguments to pass. */
          readonly arguments: ValueSlice;
      }
;

export const Instruction = {
    /** Recovered invalid instruction syntax. */
    error(): Instruction {
        return { kind: "error" };
    },

    /** Load a constant value. */
    "const"(destination: Value, value: Constant): Instruction {
        return { kind: "const", destination, value };
    },

    /** Binary operation (e.g., add, subtract, compare). */
    binary(destination: Value, operator: BinaryOperator, left: Value, right: Value): Instruction {
        return { kind: "binary", destination, operator, left, right };
    },

    /** Unary operation (e.g., negate, not). */
    unary(destination: Value, operator: UnaryOperator, argument: Value): Instruction {
        return { kind: "unary", destination, operator, argument };
    },

    /** Cast between types (bitcast, truncate, extend, etc.). */
    cast(destination: Value, operator: CastOperator, argument: Value, toType: LocalNodeId): Instruction {
        return { kind: "cast", destination, operator, argument, toType };
    },

    /** Select a value based on a boolean condition. */
    select(destination: Value, condition: Value, thenValue: Value, elseValue: Value): Instruction {
        return { kind: "select", destination, condition, thenValue, elseValue };
    },

    /** Load from a local variable (stack slot). */
    localGet(destination: Value, local: LocalNodeId): Instruction {
        return { kind: "localGet", destination, local };
    },

    /** Get a reference to a local variable. */
    localAddr(destination: Value, local: LocalNodeId, resultType: LocalNodeId): Instruction {
        return { kind: "localAddr", destination, local, resultType };
    },

    /** Store to a local variable (stack slot). */
    localSet(local: LocalNodeId, value: Value): Instruction {
        return { kind: "localSet", local, value };
    },

    /** Get a reference to a mutable global variable. */
    globalAddr(destination: Value, global: LocalNodeId, resultType: LocalNodeId): Instruction {
        return { kind: "globalAddr", destination, global, resultType };
    },

    /** Get a function pointer for a function (function.address). */
    functionAddr(destination: Value, function_: LocalNodeId): Instruction {
        return { kind: "functionAddr", destination, function: function_ };
    },

    /** Bind one environment to a function and produce a function value (function.bind). */
    functionBind(destination: Value, function_: LocalNodeId, environment: Value): Instruction {
        return { kind: "functionBind", destination, function: function_, environment };
    },

    /** Project the environment from one function value (function.environment). */
    functionEnvironment(destination: Value, function_: Value): Instruction {
        return { kind: "functionEnvironment", destination, function: function_ };
    },

    /** Load the hidden environment for the current function (function.environment.current). */
    functionEnvironmentCurrent(destination: Value): Instruction {
        return { kind: "functionEnvironmentCurrent", destination };
    },

    /** Load the current execution context. */
    contextCurrent(destination: Value): Instruction {
        return { kind: "contextCurrent", destination };
    },

    /** Replace the current execution context and return its previous value. */
    contextReplace(destination: Value, context: Value): Instruction {
        return { kind: "contextReplace", destination, context };
    },

    /** Extend one execution context with a variable value. */
    contextBind(destination: Value, context: Value, variable: Value, value: Value, nodeType: LocalNodeId, resultType: LocalNodeId): Instruction {
        return { kind: "contextBind", destination, context, variable, value, nodeType, resultType };
    },

    /** Load one variable value from an execution context. */
    contextGet(destination: Value, context: Value, variable: Value, default_: Value, nodeType: LocalNodeId, resultType: LocalNodeId): Instruction {
        return { kind: "contextGet", destination, context, variable, default: default_, nodeType, resultType };
    },

    /** Load from a pointer (dereference). */
    load(destination: Value, pointer: Value, resultType: LocalNodeId): Instruction {
        return { kind: "load", destination, pointer, resultType };
    },

    /** Store to a pointer (write through pointer). */
    store(pointer: Value, value: Value): Instruction {
        return { kind: "store", pointer, value };
    },

    /** Construct an aggregate from values in logical slot order. */
    aggregate(destination: Value, values: ValueSlice): Instruction {
        return { kind: "aggregate", destination, values };
    },

    /** Extract one structural field from an aggregate value. */
    fieldGet(destination: Value, aggregate: Value, field: number): Instruction {
        return { kind: "fieldGet", destination, aggregate, field };
    },

    /** Replace one structural field in an aggregate value. */
    fieldSet(destination: Value, aggregate: Value, field: number, value: Value): Instruction {
        return { kind: "fieldSet", destination, aggregate, field, value };
    },

    /** Get the address of one structural field in an addressable aggregate. */
    fieldAddr(destination: Value, aggregate: Value, field: number, resultType: LocalNodeId): Instruction {
        return { kind: "fieldAddr", destination, aggregate, field, resultType };
    },

    /** Extract one statically selected fixed-array element. */
    elementGet(destination: Value, aggregate: Value, index: number): Instruction {
        return { kind: "elementGet", destination, aggregate, index };
    },

    /** Insert one value into a statically selected fixed-array element. */
    elementSet(destination: Value, aggregate: Value, index: number, value: Value): Instruction {
        return { kind: "elementSet", destination, aggregate, index, value };
    },

    /** Get the address of an element from an addressable indexed value (element.address). */
    elementAddr(destination: Value, base: Value, index: Value, resultType: LocalNodeId): Instruction {
        return { kind: "elementAddr", destination, base, index, resultType };
    },

    /** Construct a variant value from one case payload. */
    variantNew(destination: Value, case_: number, payload: Value | undefined, resultType: LocalNodeId): Instruction {
        return { kind: "variantNew", destination, case: case_, payload, resultType };
    },

    /** Read the discriminant of a variant value. */
    variantTag(destination: Value, variant: Value): Instruction {
        return { kind: "variantTag", destination, variant };
    },

    /** Read the discriminant of a stored variant. */
    variantTagLoad(destination: Value, variant: Value): Instruction {
        return { kind: "variantTagLoad", destination, variant };
    },

    /** Extract the payload of one statically selected variant case. */
    variantPayload(destination: Value, variant: Value, case_: number): Instruction {
        return { kind: "variantPayload", destination, variant, case: case_ };
    },

    /** Get the address of one statically selected variant payload. */
    variantPayloadAddr(destination: Value, variant: Value, case_: number, resultType: LocalNodeId): Instruction {
        return { kind: "variantPayloadAddr", destination, variant, case: case_, resultType };
    },

    /** Form a non-owning slice view over a contiguous source region. */
    sliceView(destination: Value, source: Value, start: Value, length: Value, resultType: LocalNodeId): Instruction {
        return { kind: "sliceView", destination, source, start, length, resultType };
    },

    /** Read the runtime length from a slice descriptor. */
    sliceLength(destination: Value, slice: Value): Instruction {
        return { kind: "sliceLength", destination, slice };
    },

    /** Bind a typed reference payload to its concrete dispatch implementation. */
    dynamicBind(destination: Value, payload: Value, concrete: LocalNodeId): Instruction {
        return { kind: "dynamicBind", destination, payload, concrete };
    },

    /** Read the erased payload from a dynamic value. */
    dynamicPayload(destination: Value, dynamic: Value, resultType: LocalNodeId): Instruction {
        return { kind: "dynamicPayload", destination, dynamic, resultType };
    },

    /** Read the concrete type id from a dynamic value's dispatch table. */
    dynamicType(destination: Value, dynamic: Value): Instruction {
        return { kind: "dynamicType", destination, dynamic };
    },

    /** Find one named entry through a dynamic value's concrete table. */
    dynamicFind(destination: Value, dynamic: Value, key: Value, resultType: LocalNodeId): Instruction {
        return { kind: "dynamicFind", destination, dynamic, key, resultType };
    },

    /** Broadcast a scalar to all vector lanes. */
    vectorSplat(destination: Value, value: Value): Instruction {
        return { kind: "vectorSplat", destination, value };
    },

    /** Extract a lane from a vector. */
    vectorExtract(destination: Value, vector: Value, index: Value): Instruction {
        return { kind: "vectorExtract", destination, vector, index };
    },

    /** Insert a lane into a vector. */
    vectorInsert(destination: Value, vector: Value, index: Value, value: Value): Instruction {
        return { kind: "vectorInsert", destination, vector, index, value };
    },

    /** Shuffle vector lanes using a constant mask. */
    vectorShuffle(destination: Value, left: Value, right: Value, mask: IndexSlice): Instruction {
        return { kind: "vectorShuffle", destination, left, right, mask };
    },

    /** Select vector lanes based on a boolean mask. */
    vectorSelect(destination: Value, mask: Value, thenValue: Value, elseValue: Value): Instruction {
        return { kind: "vectorSelect", destination, mask, thenValue, elseValue };
    },

    /** Reduce a vector to a scalar. */
    vectorReduce(destination: Value, operator: VectorReduceOperator, vector: Value): Instruction {
        return { kind: "vectorReduce", destination, operator, vector };
    },

    /** Compare two vectors elementwise. */
    vectorCompare(destination: Value, operator: BinaryOperator, left: Value, right: Value): Instruction {
        return { kind: "vectorCompare", destination, operator, left, right };
    },

    /** Convert vector element types with an explicit mode. */
    vectorConvert(destination: Value, mode: ConvertMode, vector: Value): Instruction {
        return { kind: "vectorConvert", destination, mode, vector };
    },

    /** Broadcast a scalar to all tensor elements. */
    tensorSplat(destination: Value, value: Value): Instruction {
        return { kind: "tensorSplat", destination, value };
    },

    /** Load a tensor element from a tensor reference. */
    tensorLoad(destination: Value, view: Value, indices: ValueSlice): Instruction {
        return { kind: "tensorLoad", destination, view, indices };
    },

    /** Extract a tensor element from a tensor value. */
    tensorExtract(destination: Value, tensor: Value, indices: ValueSlice): Instruction {
        return { kind: "tensorExtract", destination, tensor, indices };
    },

    /** Store a tensor element into a tensor reference. */
    tensorStore(view: Value, indices: ValueSlice, value: Value): Instruction {
        return { kind: "tensorStore", view, indices, value };
    },

    /** Fill a tensor reference with a scalar value. */
    tensorFill(view: Value, value: Value): Instruction {
        return { kind: "tensorFill", view, value };
    },

    /** Copy elements from a source tensor reference into a destination tensor reference. */
    tensorCopy(target: Value, source: Value): Instruction {
        return { kind: "tensorCopy", target, source };
    },

    /** Reshape a tensor value into a new shape. */
    tensorReshape(destination: Value, tensor: Value, shape: ValueSlice): Instruction {
        return { kind: "tensorReshape", destination, tensor, shape };
    },

    /** Broadcast a tensor into a larger shape. */
    tensorBroadcast(destination: Value, tensor: Value, dimensions: IndexSlice): Instruction {
        return { kind: "tensorBroadcast", destination, tensor, dimensions };
    },

    /** Permute tensor dimensions. */
    tensorTranspose(destination: Value, tensor: Value, permutation: IndexSlice): Instruction {
        return { kind: "tensorTranspose", destination, tensor, permutation };
    },

    /** Refine a tensor type without changing its contents. */
    tensorCast(destination: Value, tensor: Value): Instruction {
        return { kind: "tensorCast", destination, tensor };
    },

    /** Create a view into a tensor reference. */
    tensorView(destination: Value, view: Value, arguments_: ValueSlice, offsetsCount: number, sizesCount: number, stridesCount: number): Instruction {
        return { kind: "tensorView", destination, view, arguments: arguments_, offsetsCount, sizesCount, stridesCount };
    },

    /** Slice a tensor by offsets, sizes, and strides. */
    tensorSlice(destination: Value, tensor: Value, arguments_: ValueSlice, offsetsCount: number, sizesCount: number, stridesCount: number): Instruction {
        return { kind: "tensorSlice", destination, tensor, arguments: arguments_, offsetsCount, sizesCount, stridesCount };
    },

    /** Pad a tensor with low, high, and interior padding. */
    tensorPad(destination: Value, tensor: Value, arguments_: ValueSlice, lowCount: number, highCount: number, interiorCount: number, value: Value): Instruction {
        return { kind: "tensorPad", destination, tensor, arguments: arguments_, lowCount, highCount, interiorCount, value };
    },

    /** Concatenate tensors along a dimension. */
    tensorConcat(destination: Value, tensors: ValueSlice, axis: number): Instruction {
        return { kind: "tensorConcat", destination, tensors, axis };
    },

    /** Compare two tensors elementwise. */
    tensorCompare(destination: Value, operator: BinaryOperator, left: Value, right: Value): Instruction {
        return { kind: "tensorCompare", destination, operator, left, right };
    },

    /** Select tensor elements based on a boolean mask. */
    tensorSelect(destination: Value, mask: Value, thenValue: Value, elseValue: Value): Instruction {
        return { kind: "tensorSelect", destination, mask, thenValue, elseValue };
    },

    /** Reduce a tensor along axes with a fixed operator. */
    tensorReduce(destination: Value, operator: TensorReduceOperator, tensor: Value, initial: Value, axes: IndexSlice): Instruction {
        return { kind: "tensorReduce", destination, operator, tensor, initial, axes };
    },

    /** Reduce a tensor along one axis and return selected source indices. */
    tensorIndexReduce(destination: Value, operator: TensorIndexReduceOperator, tensor: Value, axis: number, tieBreak: TensorIndexTieBreak): Instruction {
        return { kind: "tensorIndexReduce", destination, operator, tensor, axis, tieBreak };
    },

    /** Dot product of two tensors. */
    tensorDot(destination: Value, left: Value, right: Value, immediate: TensorImmediateId): Instruction {
        return { kind: "tensorDot", destination, left, right, immediate };
    },

    /** Convolution between an input tensor and a kernel tensor. */
    tensorConvolution(destination: Value, input: Value, kernel: Value, immediate: TensorImmediateId): Instruction {
        return { kind: "tensorConvolution", destination, input, kernel, immediate };
    },

    /** Gather slices from a tensor based on indices. */
    tensorGather(destination: Value, operand: Value, indices: Value, immediate: TensorImmediateId): Instruction {
        return { kind: "tensorGather", destination, operand, indices, immediate };
    },

    /** Scatter updates into a tensor based on indices. */
    tensorScatter(destination: Value, operand: Value, indices: Value, updates: Value, immediate: TensorImmediateId, mode: TensorScatterMode): Instruction {
        return { kind: "tensorScatter", destination, operand, indices, updates, immediate, mode };
    },

    /** Convert a tensor element type. */
    tensorConvert(destination: Value, mode: ConvertMode, tensor: Value): Instruction {
        return { kind: "tensorConvert", destination, mode, tensor };
    },

    /** Call one callable target without a local unwind continuation. */
    call(destination: Value | undefined, call: Call): Instruction {
        return { kind: "call", destination, call };
    },

    /** Drop one value. */
    drop(value: Value): Instruction {
        return { kind: "drop", value };
    },

    /** Allocate zeroed typed heap storage (`new.zeroed`). */
    newZeroed(destination: Value, storageType: LocalNodeId, resultType: LocalNodeId): Instruction {
        return { kind: "newZeroed", destination, storageType, resultType };
    },

    /** Allocate uninitialized typed heap storage (`new.uninit`). */
    newUninit(destination: Value, storageType: LocalNodeId, resultType: LocalNodeId): Instruction {
        return { kind: "newUninit", destination, storageType, resultType };
    },

    /** Complete initialization of one allocation (`new.complete`). */
    newComplete(destination: Value, value: Value, resultType: LocalNodeId): Instruction {
        return { kind: "newComplete", destination, value, resultType };
    },

    /** Allocate zeroed typed repeated heap storage (`new.slice.zeroed`). */
    newSliceZeroed(destination: Value, element: LocalNodeId, length: Value, resultType: LocalNodeId): Instruction {
        return { kind: "newSliceZeroed", destination, element, length, resultType };
    },

    /** Allocate uninitialized typed repeated heap storage (`new.slice.uninit`). */
    newSliceUninit(destination: Value, element: LocalNodeId, length: Value, resultType: LocalNodeId): Instruction {
        return { kind: "newSliceUninit", destination, element, length, resultType };
    },

    /** Release one unique carrier's backing heap allocation (`free`). */
    free(value: Value): Instruction {
        return { kind: "free", value };
    },

    /** Stabilize one heap value against movement (`pin`). */
    pin(destination: Value, value: Value, resultType: LocalNodeId): Instruction {
        return { kind: "pin", destination, value, resultType };
    },

    /** Release one heap pin (`unpin`). */
    unpin(value: Value): Instruction {
        return { kind: "unpin", value };
    },

    /** Record a managed reference write for the collector. */
    barrierWrite(object_: Value, offset: Value, byteLen: Value): Instruction {
        return { kind: "barrierWrite", object: object_, offset, byteLen };
    },

    /** Load from memory atomically. */
    atomicLoad(destination: Value, pointer: Value, resultType: LocalNodeId, access: AtomicAccess): Instruction {
        return { kind: "atomicLoad", destination, pointer, resultType, access };
    },

    /** Store to memory atomically. */
    atomicStore(pointer: Value, value: Value, access: AtomicAccess): Instruction {
        return { kind: "atomicStore", pointer, value, access };
    },

    /** Compare exchange one memory location atomically. */
    atomicCompareExchange(destination: Value, pointer: Value, expected: Value, newValue: Value, isWeak: boolean, access: CompareExchangeAccess): Instruction {
        return { kind: "atomicCompareExchange", destination, pointer, expected, newValue, isWeak, access };
    },

    /** Apply one atomic read modify write operation. */
    atomicRmw(destination: Value, operator: AtomicRmwOperator, pointer: Value, value: Value, access: AtomicAccess): Instruction {
        return { kind: "atomicRmw", destination, operator, pointer, value, access };
    },

    /** Publish one memory fence. */
    atomicFence(access: FenceAccess): Instruction {
        return { kind: "atomicFence", access };
    },

    /** Assume a condition is true (UB if false). */
    assume(condition: Value): Instruction {
        return { kind: "assume", condition };
    },

    /** Increment one profile counter. */
    profileIncrement(counter: CounterId): Instruction {
        return { kind: "profileIncrement", counter };
    },

    /** Sample one runtime value. */
    profileSample(sampler: SamplerId, value: Value): Instruction {
        return { kind: "profileSample", sampler, value };
    },

    /** Cooperate with runtime work at one explicit statepoint. */
    poll(): Instruction {
        return { kind: "poll" };
    },

    /** Debugger breakpoint. */
    breakpoint(): Instruction {
        return { kind: "breakpoint" };
    },

    /** Call a machine intrinsic. */
    intrinsic(destination: Value | undefined, intrinsic: Intrinsic, arguments_: ValueSlice): Instruction {
        return { kind: "intrinsic", destination, intrinsic, arguments: arguments_ };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: Instruction): void {
        encodeInstruction(writer, value);
    },

    /** Decode one Instruction. */
    decode(reader: BinaryReader): Instruction {
        return decodeInstruction(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Instruction): Json {
        return toJsonInstruction(value);
    },

    /** Return one Instruction from one JSON value. */
    fromJson(value: Json): Instruction {
        return fromJsonInstruction(value);
    },
};

/** Encode one Instruction. */
export function encodeInstruction(writer: BinaryWriter, value: Instruction): void {
    switch (value.kind) {
        case "error":
            writer.writeUnsigned(0);
            return;
        case "const":
            writer.writeUnsigned(1);
            encodeValue(writer, value.destination);
            encodeConstant(writer, value.value);
            return;
        case "binary":
            writer.writeUnsigned(2);
            encodeValue(writer, value.destination);
            encodeBinaryOperator(writer, value.operator);
            encodeValue(writer, value.left);
            encodeValue(writer, value.right);
            return;
        case "unary":
            writer.writeUnsigned(3);
            encodeValue(writer, value.destination);
            encodeUnaryOperator(writer, value.operator);
            encodeValue(writer, value.argument);
            return;
        case "cast":
            writer.writeUnsigned(4);
            encodeValue(writer, value.destination);
            encodeCastOperator(writer, value.operator);
            encodeValue(writer, value.argument);
            encodeLocalNodeId(writer, value.toType);
            return;
        case "select":
            writer.writeUnsigned(5);
            encodeValue(writer, value.destination);
            encodeValue(writer, value.condition);
            encodeValue(writer, value.thenValue);
            encodeValue(writer, value.elseValue);
            return;
        case "localGet":
            writer.writeUnsigned(6);
            encodeValue(writer, value.destination);
            encodeLocalNodeId(writer, value.local);
            return;
        case "localAddr":
            writer.writeUnsigned(7);
            encodeValue(writer, value.destination);
            encodeLocalNodeId(writer, value.local);
            encodeLocalNodeId(writer, value.resultType);
            return;
        case "localSet":
            writer.writeUnsigned(8);
            encodeLocalNodeId(writer, value.local);
            encodeValue(writer, value.value);
            return;
        case "globalAddr":
            writer.writeUnsigned(9);
            encodeValue(writer, value.destination);
            encodeLocalNodeId(writer, value.global);
            encodeLocalNodeId(writer, value.resultType);
            return;
        case "functionAddr":
            writer.writeUnsigned(10);
            encodeValue(writer, value.destination);
            encodeLocalNodeId(writer, value.function);
            return;
        case "functionBind":
            writer.writeUnsigned(11);
            encodeValue(writer, value.destination);
            encodeLocalNodeId(writer, value.function);
            encodeValue(writer, value.environment);
            return;
        case "functionEnvironment":
            writer.writeUnsigned(12);
            encodeValue(writer, value.destination);
            encodeValue(writer, value.function);
            return;
        case "functionEnvironmentCurrent":
            writer.writeUnsigned(13);
            encodeValue(writer, value.destination);
            return;
        case "contextCurrent":
            writer.writeUnsigned(14);
            encodeValue(writer, value.destination);
            return;
        case "contextReplace":
            writer.writeUnsigned(15);
            encodeValue(writer, value.destination);
            encodeValue(writer, value.context);
            return;
        case "contextBind":
            writer.writeUnsigned(16);
            encodeValue(writer, value.destination);
            encodeValue(writer, value.context);
            encodeValue(writer, value.variable);
            encodeValue(writer, value.value);
            encodeLocalNodeId(writer, value.nodeType);
            encodeLocalNodeId(writer, value.resultType);
            return;
        case "contextGet":
            writer.writeUnsigned(17);
            encodeValue(writer, value.destination);
            encodeValue(writer, value.context);
            encodeValue(writer, value.variable);
            encodeValue(writer, value.default);
            encodeLocalNodeId(writer, value.nodeType);
            encodeLocalNodeId(writer, value.resultType);
            return;
        case "load":
            writer.writeUnsigned(18);
            encodeValue(writer, value.destination);
            encodeValue(writer, value.pointer);
            encodeLocalNodeId(writer, value.resultType);
            return;
        case "store":
            writer.writeUnsigned(19);
            encodeValue(writer, value.pointer);
            encodeValue(writer, value.value);
            return;
        case "aggregate":
            writer.writeUnsigned(20);
            encodeValue(writer, value.destination);
            encodeValueSlice(writer, value.values);
            return;
        case "fieldGet":
            writer.writeUnsigned(21);
            encodeValue(writer, value.destination);
            encodeValue(writer, value.aggregate);
            writer.writeUnsigned(value.field);
            return;
        case "fieldSet":
            writer.writeUnsigned(22);
            encodeValue(writer, value.destination);
            encodeValue(writer, value.aggregate);
            writer.writeUnsigned(value.field);
            encodeValue(writer, value.value);
            return;
        case "fieldAddr":
            writer.writeUnsigned(23);
            encodeValue(writer, value.destination);
            encodeValue(writer, value.aggregate);
            writer.writeUnsigned(value.field);
            encodeLocalNodeId(writer, value.resultType);
            return;
        case "elementGet":
            writer.writeUnsigned(24);
            encodeValue(writer, value.destination);
            encodeValue(writer, value.aggregate);
            writer.writeUnsigned(value.index);
            return;
        case "elementSet":
            writer.writeUnsigned(25);
            encodeValue(writer, value.destination);
            encodeValue(writer, value.aggregate);
            writer.writeUnsigned(value.index);
            encodeValue(writer, value.value);
            return;
        case "elementAddr":
            writer.writeUnsigned(26);
            encodeValue(writer, value.destination);
            encodeValue(writer, value.base);
            encodeValue(writer, value.index);
            encodeLocalNodeId(writer, value.resultType);
            return;
        case "variantNew":
            writer.writeUnsigned(27);
            encodeValue(writer, value.destination);
            writer.writeUnsigned(value.case);
            writer.writeOption(value.payload, (value2) => {
                encodeValue(writer, value2);
            });
            encodeLocalNodeId(writer, value.resultType);
            return;
        case "variantTag":
            writer.writeUnsigned(28);
            encodeValue(writer, value.destination);
            encodeValue(writer, value.variant);
            return;
        case "variantTagLoad":
            writer.writeUnsigned(29);
            encodeValue(writer, value.destination);
            encodeValue(writer, value.variant);
            return;
        case "variantPayload":
            writer.writeUnsigned(30);
            encodeValue(writer, value.destination);
            encodeValue(writer, value.variant);
            writer.writeUnsigned(value.case);
            return;
        case "variantPayloadAddr":
            writer.writeUnsigned(31);
            encodeValue(writer, value.destination);
            encodeValue(writer, value.variant);
            writer.writeUnsigned(value.case);
            encodeLocalNodeId(writer, value.resultType);
            return;
        case "sliceView":
            writer.writeUnsigned(32);
            encodeValue(writer, value.destination);
            encodeValue(writer, value.source);
            encodeValue(writer, value.start);
            encodeValue(writer, value.length);
            encodeLocalNodeId(writer, value.resultType);
            return;
        case "sliceLength":
            writer.writeUnsigned(33);
            encodeValue(writer, value.destination);
            encodeValue(writer, value.slice);
            return;
        case "dynamicBind":
            writer.writeUnsigned(34);
            encodeValue(writer, value.destination);
            encodeValue(writer, value.payload);
            encodeLocalNodeId(writer, value.concrete);
            return;
        case "dynamicPayload":
            writer.writeUnsigned(35);
            encodeValue(writer, value.destination);
            encodeValue(writer, value.dynamic);
            encodeLocalNodeId(writer, value.resultType);
            return;
        case "dynamicType":
            writer.writeUnsigned(36);
            encodeValue(writer, value.destination);
            encodeValue(writer, value.dynamic);
            return;
        case "dynamicFind":
            writer.writeUnsigned(37);
            encodeValue(writer, value.destination);
            encodeValue(writer, value.dynamic);
            encodeValue(writer, value.key);
            encodeLocalNodeId(writer, value.resultType);
            return;
        case "vectorSplat":
            writer.writeUnsigned(38);
            encodeValue(writer, value.destination);
            encodeValue(writer, value.value);
            return;
        case "vectorExtract":
            writer.writeUnsigned(39);
            encodeValue(writer, value.destination);
            encodeValue(writer, value.vector);
            encodeValue(writer, value.index);
            return;
        case "vectorInsert":
            writer.writeUnsigned(40);
            encodeValue(writer, value.destination);
            encodeValue(writer, value.vector);
            encodeValue(writer, value.index);
            encodeValue(writer, value.value);
            return;
        case "vectorShuffle":
            writer.writeUnsigned(41);
            encodeValue(writer, value.destination);
            encodeValue(writer, value.left);
            encodeValue(writer, value.right);
            encodeIndexSlice(writer, value.mask);
            return;
        case "vectorSelect":
            writer.writeUnsigned(42);
            encodeValue(writer, value.destination);
            encodeValue(writer, value.mask);
            encodeValue(writer, value.thenValue);
            encodeValue(writer, value.elseValue);
            return;
        case "vectorReduce":
            writer.writeUnsigned(43);
            encodeValue(writer, value.destination);
            encodeVectorReduceOperator(writer, value.operator);
            encodeValue(writer, value.vector);
            return;
        case "vectorCompare":
            writer.writeUnsigned(44);
            encodeValue(writer, value.destination);
            encodeBinaryOperator(writer, value.operator);
            encodeValue(writer, value.left);
            encodeValue(writer, value.right);
            return;
        case "vectorConvert":
            writer.writeUnsigned(45);
            encodeValue(writer, value.destination);
            encodeConvertMode(writer, value.mode);
            encodeValue(writer, value.vector);
            return;
        case "tensorSplat":
            writer.writeUnsigned(46);
            encodeValue(writer, value.destination);
            encodeValue(writer, value.value);
            return;
        case "tensorLoad":
            writer.writeUnsigned(47);
            encodeValue(writer, value.destination);
            encodeValue(writer, value.view);
            encodeValueSlice(writer, value.indices);
            return;
        case "tensorExtract":
            writer.writeUnsigned(48);
            encodeValue(writer, value.destination);
            encodeValue(writer, value.tensor);
            encodeValueSlice(writer, value.indices);
            return;
        case "tensorStore":
            writer.writeUnsigned(49);
            encodeValue(writer, value.view);
            encodeValueSlice(writer, value.indices);
            encodeValue(writer, value.value);
            return;
        case "tensorFill":
            writer.writeUnsigned(50);
            encodeValue(writer, value.view);
            encodeValue(writer, value.value);
            return;
        case "tensorCopy":
            writer.writeUnsigned(51);
            encodeValue(writer, value.target);
            encodeValue(writer, value.source);
            return;
        case "tensorReshape":
            writer.writeUnsigned(52);
            encodeValue(writer, value.destination);
            encodeValue(writer, value.tensor);
            encodeValueSlice(writer, value.shape);
            return;
        case "tensorBroadcast":
            writer.writeUnsigned(53);
            encodeValue(writer, value.destination);
            encodeValue(writer, value.tensor);
            encodeIndexSlice(writer, value.dimensions);
            return;
        case "tensorTranspose":
            writer.writeUnsigned(54);
            encodeValue(writer, value.destination);
            encodeValue(writer, value.tensor);
            encodeIndexSlice(writer, value.permutation);
            return;
        case "tensorCast":
            writer.writeUnsigned(55);
            encodeValue(writer, value.destination);
            encodeValue(writer, value.tensor);
            return;
        case "tensorView":
            writer.writeUnsigned(56);
            encodeValue(writer, value.destination);
            encodeValue(writer, value.view);
            encodeValueSlice(writer, value.arguments);
            writer.writeUnsigned(value.offsetsCount);
            writer.writeUnsigned(value.sizesCount);
            writer.writeUnsigned(value.stridesCount);
            return;
        case "tensorSlice":
            writer.writeUnsigned(57);
            encodeValue(writer, value.destination);
            encodeValue(writer, value.tensor);
            encodeValueSlice(writer, value.arguments);
            writer.writeUnsigned(value.offsetsCount);
            writer.writeUnsigned(value.sizesCount);
            writer.writeUnsigned(value.stridesCount);
            return;
        case "tensorPad":
            writer.writeUnsigned(58);
            encodeValue(writer, value.destination);
            encodeValue(writer, value.tensor);
            encodeValueSlice(writer, value.arguments);
            writer.writeUnsigned(value.lowCount);
            writer.writeUnsigned(value.highCount);
            writer.writeUnsigned(value.interiorCount);
            encodeValue(writer, value.value);
            return;
        case "tensorConcat":
            writer.writeUnsigned(59);
            encodeValue(writer, value.destination);
            encodeValueSlice(writer, value.tensors);
            writer.writeUnsigned(value.axis);
            return;
        case "tensorCompare":
            writer.writeUnsigned(60);
            encodeValue(writer, value.destination);
            encodeBinaryOperator(writer, value.operator);
            encodeValue(writer, value.left);
            encodeValue(writer, value.right);
            return;
        case "tensorSelect":
            writer.writeUnsigned(61);
            encodeValue(writer, value.destination);
            encodeValue(writer, value.mask);
            encodeValue(writer, value.thenValue);
            encodeValue(writer, value.elseValue);
            return;
        case "tensorReduce":
            writer.writeUnsigned(62);
            encodeValue(writer, value.destination);
            encodeTensorReduceOperator(writer, value.operator);
            encodeValue(writer, value.tensor);
            encodeValue(writer, value.initial);
            encodeIndexSlice(writer, value.axes);
            return;
        case "tensorIndexReduce":
            writer.writeUnsigned(63);
            encodeValue(writer, value.destination);
            encodeTensorIndexReduceOperator(writer, value.operator);
            encodeValue(writer, value.tensor);
            writer.writeUnsigned(value.axis);
            encodeTensorIndexTieBreak(writer, value.tieBreak);
            return;
        case "tensorDot":
            writer.writeUnsigned(64);
            encodeValue(writer, value.destination);
            encodeValue(writer, value.left);
            encodeValue(writer, value.right);
            encodeTensorImmediateId(writer, value.immediate);
            return;
        case "tensorConvolution":
            writer.writeUnsigned(65);
            encodeValue(writer, value.destination);
            encodeValue(writer, value.input);
            encodeValue(writer, value.kernel);
            encodeTensorImmediateId(writer, value.immediate);
            return;
        case "tensorGather":
            writer.writeUnsigned(66);
            encodeValue(writer, value.destination);
            encodeValue(writer, value.operand);
            encodeValue(writer, value.indices);
            encodeTensorImmediateId(writer, value.immediate);
            return;
        case "tensorScatter":
            writer.writeUnsigned(67);
            encodeValue(writer, value.destination);
            encodeValue(writer, value.operand);
            encodeValue(writer, value.indices);
            encodeValue(writer, value.updates);
            encodeTensorImmediateId(writer, value.immediate);
            encodeTensorScatterMode(writer, value.mode);
            return;
        case "tensorConvert":
            writer.writeUnsigned(68);
            encodeValue(writer, value.destination);
            encodeConvertMode(writer, value.mode);
            encodeValue(writer, value.tensor);
            return;
        case "call":
            writer.writeUnsigned(69);
            writer.writeOption(value.destination, (value0) => {
                encodeValue(writer, value0);
            });
            encodeCall(writer, value.call);
            return;
        case "drop":
            writer.writeUnsigned(70);
            encodeValue(writer, value.value);
            return;
        case "newZeroed":
            writer.writeUnsigned(71);
            encodeValue(writer, value.destination);
            encodeLocalNodeId(writer, value.storageType);
            encodeLocalNodeId(writer, value.resultType);
            return;
        case "newUninit":
            writer.writeUnsigned(72);
            encodeValue(writer, value.destination);
            encodeLocalNodeId(writer, value.storageType);
            encodeLocalNodeId(writer, value.resultType);
            return;
        case "newComplete":
            writer.writeUnsigned(73);
            encodeValue(writer, value.destination);
            encodeValue(writer, value.value);
            encodeLocalNodeId(writer, value.resultType);
            return;
        case "newSliceZeroed":
            writer.writeUnsigned(74);
            encodeValue(writer, value.destination);
            encodeLocalNodeId(writer, value.element);
            encodeValue(writer, value.length);
            encodeLocalNodeId(writer, value.resultType);
            return;
        case "newSliceUninit":
            writer.writeUnsigned(75);
            encodeValue(writer, value.destination);
            encodeLocalNodeId(writer, value.element);
            encodeValue(writer, value.length);
            encodeLocalNodeId(writer, value.resultType);
            return;
        case "free":
            writer.writeUnsigned(76);
            encodeValue(writer, value.value);
            return;
        case "pin":
            writer.writeUnsigned(77);
            encodeValue(writer, value.destination);
            encodeValue(writer, value.value);
            encodeLocalNodeId(writer, value.resultType);
            return;
        case "unpin":
            writer.writeUnsigned(78);
            encodeValue(writer, value.value);
            return;
        case "barrierWrite":
            writer.writeUnsigned(79);
            encodeValue(writer, value.object);
            encodeValue(writer, value.offset);
            encodeValue(writer, value.byteLen);
            return;
        case "atomicLoad":
            writer.writeUnsigned(80);
            encodeValue(writer, value.destination);
            encodeValue(writer, value.pointer);
            encodeLocalNodeId(writer, value.resultType);
            encodeAtomicAccess(writer, value.access);
            return;
        case "atomicStore":
            writer.writeUnsigned(81);
            encodeValue(writer, value.pointer);
            encodeValue(writer, value.value);
            encodeAtomicAccess(writer, value.access);
            return;
        case "atomicCompareExchange":
            writer.writeUnsigned(82);
            encodeValue(writer, value.destination);
            encodeValue(writer, value.pointer);
            encodeValue(writer, value.expected);
            encodeValue(writer, value.newValue);
            writer.writeBool(value.isWeak);
            encodeCompareExchangeAccess(writer, value.access);
            return;
        case "atomicRmw":
            writer.writeUnsigned(83);
            encodeValue(writer, value.destination);
            encodeAtomicRmwOperator(writer, value.operator);
            encodeValue(writer, value.pointer);
            encodeValue(writer, value.value);
            encodeAtomicAccess(writer, value.access);
            return;
        case "atomicFence":
            writer.writeUnsigned(84);
            encodeFenceAccess(writer, value.access);
            return;
        case "assume":
            writer.writeUnsigned(85);
            encodeValue(writer, value.condition);
            return;
        case "profileIncrement":
            writer.writeUnsigned(86);
            encodeCounterId(writer, value.counter);
            return;
        case "profileSample":
            writer.writeUnsigned(87);
            encodeSamplerId(writer, value.sampler);
            encodeValue(writer, value.value);
            return;
        case "poll":
            writer.writeUnsigned(88);
            return;
        case "breakpoint":
            writer.writeUnsigned(89);
            return;
        case "intrinsic":
            writer.writeUnsigned(90);
            writer.writeOption(value.destination, (value0) => {
                encodeValue(writer, value0);
            });
            encodeIntrinsic(writer, value.intrinsic);
            encodeValueSlice(writer, value.arguments);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one Instruction. */
export function decodeInstruction(reader: BinaryReader): Instruction {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            return { kind: "error" };
        }
        case 1: {
            const destination = decodeValue(reader);
            const value = decodeConstant(reader);

            return {
                kind: "const",
                destination,
                value,
            };
        }
        case 2: {
            const destination = decodeValue(reader);
            const operator = decodeBinaryOperator(reader);
            const left = decodeValue(reader);
            const right = decodeValue(reader);

            return {
                kind: "binary",
                destination,
                operator,
                left,
                right,
            };
        }
        case 3: {
            const destination = decodeValue(reader);
            const operator = decodeUnaryOperator(reader);
            const argument = decodeValue(reader);

            return {
                kind: "unary",
                destination,
                operator,
                argument,
            };
        }
        case 4: {
            const destination = decodeValue(reader);
            const operator = decodeCastOperator(reader);
            const argument = decodeValue(reader);
            const toType = decodeLocalNodeId(reader);

            return {
                kind: "cast",
                destination,
                operator,
                argument,
                toType,
            };
        }
        case 5: {
            const destination = decodeValue(reader);
            const condition = decodeValue(reader);
            const thenValue = decodeValue(reader);
            const elseValue = decodeValue(reader);

            return {
                kind: "select",
                destination,
                condition,
                thenValue,
                elseValue,
            };
        }
        case 6: {
            const destination = decodeValue(reader);
            const local = decodeLocalNodeId(reader);

            return {
                kind: "localGet",
                destination,
                local,
            };
        }
        case 7: {
            const destination = decodeValue(reader);
            const local = decodeLocalNodeId(reader);
            const resultType = decodeLocalNodeId(reader);

            return {
                kind: "localAddr",
                destination,
                local,
                resultType,
            };
        }
        case 8: {
            const local = decodeLocalNodeId(reader);
            const value = decodeValue(reader);

            return {
                kind: "localSet",
                local,
                value,
            };
        }
        case 9: {
            const destination = decodeValue(reader);
            const global = decodeLocalNodeId(reader);
            const resultType = decodeLocalNodeId(reader);

            return {
                kind: "globalAddr",
                destination,
                global,
                resultType,
            };
        }
        case 10: {
            const destination = decodeValue(reader);
            const function_ = decodeLocalNodeId(reader);

            return {
                kind: "functionAddr",
                destination,
                function: function_,
            };
        }
        case 11: {
            const destination = decodeValue(reader);
            const function_ = decodeLocalNodeId(reader);
            const environment = decodeValue(reader);

            return {
                kind: "functionBind",
                destination,
                function: function_,
                environment,
            };
        }
        case 12: {
            const destination = decodeValue(reader);
            const function_ = decodeValue(reader);

            return {
                kind: "functionEnvironment",
                destination,
                function: function_,
            };
        }
        case 13: {
            const destination = decodeValue(reader);

            return {
                kind: "functionEnvironmentCurrent",
                destination,
            };
        }
        case 14: {
            const destination = decodeValue(reader);

            return {
                kind: "contextCurrent",
                destination,
            };
        }
        case 15: {
            const destination = decodeValue(reader);
            const context = decodeValue(reader);

            return {
                kind: "contextReplace",
                destination,
                context,
            };
        }
        case 16: {
            const destination = decodeValue(reader);
            const context = decodeValue(reader);
            const variable = decodeValue(reader);
            const value = decodeValue(reader);
            const nodeType = decodeLocalNodeId(reader);
            const resultType = decodeLocalNodeId(reader);

            return {
                kind: "contextBind",
                destination,
                context,
                variable,
                value,
                nodeType,
                resultType,
            };
        }
        case 17: {
            const destination = decodeValue(reader);
            const context = decodeValue(reader);
            const variable = decodeValue(reader);
            const default_ = decodeValue(reader);
            const nodeType = decodeLocalNodeId(reader);
            const resultType = decodeLocalNodeId(reader);

            return {
                kind: "contextGet",
                destination,
                context,
                variable,
                default: default_,
                nodeType,
                resultType,
            };
        }
        case 18: {
            const destination = decodeValue(reader);
            const pointer = decodeValue(reader);
            const resultType = decodeLocalNodeId(reader);

            return {
                kind: "load",
                destination,
                pointer,
                resultType,
            };
        }
        case 19: {
            const pointer = decodeValue(reader);
            const value = decodeValue(reader);

            return {
                kind: "store",
                pointer,
                value,
            };
        }
        case 20: {
            const destination = decodeValue(reader);
            const values = decodeValueSlice(reader);

            return {
                kind: "aggregate",
                destination,
                values,
            };
        }
        case 21: {
            const destination = decodeValue(reader);
            const aggregate = decodeValue(reader);
            const field = reader.readNumber();

            return {
                kind: "fieldGet",
                destination,
                aggregate,
                field,
            };
        }
        case 22: {
            const destination = decodeValue(reader);
            const aggregate = decodeValue(reader);
            const field = reader.readNumber();
            const value = decodeValue(reader);

            return {
                kind: "fieldSet",
                destination,
                aggregate,
                field,
                value,
            };
        }
        case 23: {
            const destination = decodeValue(reader);
            const aggregate = decodeValue(reader);
            const field = reader.readNumber();
            const resultType = decodeLocalNodeId(reader);

            return {
                kind: "fieldAddr",
                destination,
                aggregate,
                field,
                resultType,
            };
        }
        case 24: {
            const destination = decodeValue(reader);
            const aggregate = decodeValue(reader);
            const index = reader.readNumber();

            return {
                kind: "elementGet",
                destination,
                aggregate,
                index,
            };
        }
        case 25: {
            const destination = decodeValue(reader);
            const aggregate = decodeValue(reader);
            const index = reader.readNumber();
            const value = decodeValue(reader);

            return {
                kind: "elementSet",
                destination,
                aggregate,
                index,
                value,
            };
        }
        case 26: {
            const destination = decodeValue(reader);
            const base = decodeValue(reader);
            const index = decodeValue(reader);
            const resultType = decodeLocalNodeId(reader);

            return {
                kind: "elementAddr",
                destination,
                base,
                index,
                resultType,
            };
        }
        case 27: {
            const destination = decodeValue(reader);
            const case_ = reader.readNumber();
            const payload = reader.readOption(() => decodeValue(reader));
            const resultType = decodeLocalNodeId(reader);

            return {
                kind: "variantNew",
                destination,
                case: case_,
                ...(payload === undefined ? {} : { payload }),
                resultType,
            };
        }
        case 28: {
            const destination = decodeValue(reader);
            const variant = decodeValue(reader);

            return {
                kind: "variantTag",
                destination,
                variant,
            };
        }
        case 29: {
            const destination = decodeValue(reader);
            const variant = decodeValue(reader);

            return {
                kind: "variantTagLoad",
                destination,
                variant,
            };
        }
        case 30: {
            const destination = decodeValue(reader);
            const variant = decodeValue(reader);
            const case_ = reader.readNumber();

            return {
                kind: "variantPayload",
                destination,
                variant,
                case: case_,
            };
        }
        case 31: {
            const destination = decodeValue(reader);
            const variant = decodeValue(reader);
            const case_ = reader.readNumber();
            const resultType = decodeLocalNodeId(reader);

            return {
                kind: "variantPayloadAddr",
                destination,
                variant,
                case: case_,
                resultType,
            };
        }
        case 32: {
            const destination = decodeValue(reader);
            const source = decodeValue(reader);
            const start = decodeValue(reader);
            const length = decodeValue(reader);
            const resultType = decodeLocalNodeId(reader);

            return {
                kind: "sliceView",
                destination,
                source,
                start,
                length,
                resultType,
            };
        }
        case 33: {
            const destination = decodeValue(reader);
            const slice = decodeValue(reader);

            return {
                kind: "sliceLength",
                destination,
                slice,
            };
        }
        case 34: {
            const destination = decodeValue(reader);
            const payload = decodeValue(reader);
            const concrete = decodeLocalNodeId(reader);

            return {
                kind: "dynamicBind",
                destination,
                payload,
                concrete,
            };
        }
        case 35: {
            const destination = decodeValue(reader);
            const dynamic = decodeValue(reader);
            const resultType = decodeLocalNodeId(reader);

            return {
                kind: "dynamicPayload",
                destination,
                dynamic,
                resultType,
            };
        }
        case 36: {
            const destination = decodeValue(reader);
            const dynamic = decodeValue(reader);

            return {
                kind: "dynamicType",
                destination,
                dynamic,
            };
        }
        case 37: {
            const destination = decodeValue(reader);
            const dynamic = decodeValue(reader);
            const key = decodeValue(reader);
            const resultType = decodeLocalNodeId(reader);

            return {
                kind: "dynamicFind",
                destination,
                dynamic,
                key,
                resultType,
            };
        }
        case 38: {
            const destination = decodeValue(reader);
            const value = decodeValue(reader);

            return {
                kind: "vectorSplat",
                destination,
                value,
            };
        }
        case 39: {
            const destination = decodeValue(reader);
            const vector = decodeValue(reader);
            const index = decodeValue(reader);

            return {
                kind: "vectorExtract",
                destination,
                vector,
                index,
            };
        }
        case 40: {
            const destination = decodeValue(reader);
            const vector = decodeValue(reader);
            const index = decodeValue(reader);
            const value = decodeValue(reader);

            return {
                kind: "vectorInsert",
                destination,
                vector,
                index,
                value,
            };
        }
        case 41: {
            const destination = decodeValue(reader);
            const left = decodeValue(reader);
            const right = decodeValue(reader);
            const mask = decodeIndexSlice(reader);

            return {
                kind: "vectorShuffle",
                destination,
                left,
                right,
                mask,
            };
        }
        case 42: {
            const destination = decodeValue(reader);
            const mask = decodeValue(reader);
            const thenValue = decodeValue(reader);
            const elseValue = decodeValue(reader);

            return {
                kind: "vectorSelect",
                destination,
                mask,
                thenValue,
                elseValue,
            };
        }
        case 43: {
            const destination = decodeValue(reader);
            const operator = decodeVectorReduceOperator(reader);
            const vector = decodeValue(reader);

            return {
                kind: "vectorReduce",
                destination,
                operator,
                vector,
            };
        }
        case 44: {
            const destination = decodeValue(reader);
            const operator = decodeBinaryOperator(reader);
            const left = decodeValue(reader);
            const right = decodeValue(reader);

            return {
                kind: "vectorCompare",
                destination,
                operator,
                left,
                right,
            };
        }
        case 45: {
            const destination = decodeValue(reader);
            const mode = decodeConvertMode(reader);
            const vector = decodeValue(reader);

            return {
                kind: "vectorConvert",
                destination,
                mode,
                vector,
            };
        }
        case 46: {
            const destination = decodeValue(reader);
            const value = decodeValue(reader);

            return {
                kind: "tensorSplat",
                destination,
                value,
            };
        }
        case 47: {
            const destination = decodeValue(reader);
            const view = decodeValue(reader);
            const indices = decodeValueSlice(reader);

            return {
                kind: "tensorLoad",
                destination,
                view,
                indices,
            };
        }
        case 48: {
            const destination = decodeValue(reader);
            const tensor = decodeValue(reader);
            const indices = decodeValueSlice(reader);

            return {
                kind: "tensorExtract",
                destination,
                tensor,
                indices,
            };
        }
        case 49: {
            const view = decodeValue(reader);
            const indices = decodeValueSlice(reader);
            const value = decodeValue(reader);

            return {
                kind: "tensorStore",
                view,
                indices,
                value,
            };
        }
        case 50: {
            const view = decodeValue(reader);
            const value = decodeValue(reader);

            return {
                kind: "tensorFill",
                view,
                value,
            };
        }
        case 51: {
            const target = decodeValue(reader);
            const source = decodeValue(reader);

            return {
                kind: "tensorCopy",
                target,
                source,
            };
        }
        case 52: {
            const destination = decodeValue(reader);
            const tensor = decodeValue(reader);
            const shape = decodeValueSlice(reader);

            return {
                kind: "tensorReshape",
                destination,
                tensor,
                shape,
            };
        }
        case 53: {
            const destination = decodeValue(reader);
            const tensor = decodeValue(reader);
            const dimensions = decodeIndexSlice(reader);

            return {
                kind: "tensorBroadcast",
                destination,
                tensor,
                dimensions,
            };
        }
        case 54: {
            const destination = decodeValue(reader);
            const tensor = decodeValue(reader);
            const permutation = decodeIndexSlice(reader);

            return {
                kind: "tensorTranspose",
                destination,
                tensor,
                permutation,
            };
        }
        case 55: {
            const destination = decodeValue(reader);
            const tensor = decodeValue(reader);

            return {
                kind: "tensorCast",
                destination,
                tensor,
            };
        }
        case 56: {
            const destination = decodeValue(reader);
            const view = decodeValue(reader);
            const arguments_ = decodeValueSlice(reader);
            const offsetsCount = reader.readNumber();
            const sizesCount = reader.readNumber();
            const stridesCount = reader.readNumber();

            return {
                kind: "tensorView",
                destination,
                view,
                arguments: arguments_,
                offsetsCount,
                sizesCount,
                stridesCount,
            };
        }
        case 57: {
            const destination = decodeValue(reader);
            const tensor = decodeValue(reader);
            const arguments_ = decodeValueSlice(reader);
            const offsetsCount = reader.readNumber();
            const sizesCount = reader.readNumber();
            const stridesCount = reader.readNumber();

            return {
                kind: "tensorSlice",
                destination,
                tensor,
                arguments: arguments_,
                offsetsCount,
                sizesCount,
                stridesCount,
            };
        }
        case 58: {
            const destination = decodeValue(reader);
            const tensor = decodeValue(reader);
            const arguments_ = decodeValueSlice(reader);
            const lowCount = reader.readNumber();
            const highCount = reader.readNumber();
            const interiorCount = reader.readNumber();
            const value = decodeValue(reader);

            return {
                kind: "tensorPad",
                destination,
                tensor,
                arguments: arguments_,
                lowCount,
                highCount,
                interiorCount,
                value,
            };
        }
        case 59: {
            const destination = decodeValue(reader);
            const tensors = decodeValueSlice(reader);
            const axis = reader.readNumber();

            return {
                kind: "tensorConcat",
                destination,
                tensors,
                axis,
            };
        }
        case 60: {
            const destination = decodeValue(reader);
            const operator = decodeBinaryOperator(reader);
            const left = decodeValue(reader);
            const right = decodeValue(reader);

            return {
                kind: "tensorCompare",
                destination,
                operator,
                left,
                right,
            };
        }
        case 61: {
            const destination = decodeValue(reader);
            const mask = decodeValue(reader);
            const thenValue = decodeValue(reader);
            const elseValue = decodeValue(reader);

            return {
                kind: "tensorSelect",
                destination,
                mask,
                thenValue,
                elseValue,
            };
        }
        case 62: {
            const destination = decodeValue(reader);
            const operator = decodeTensorReduceOperator(reader);
            const tensor = decodeValue(reader);
            const initial = decodeValue(reader);
            const axes = decodeIndexSlice(reader);

            return {
                kind: "tensorReduce",
                destination,
                operator,
                tensor,
                initial,
                axes,
            };
        }
        case 63: {
            const destination = decodeValue(reader);
            const operator = decodeTensorIndexReduceOperator(reader);
            const tensor = decodeValue(reader);
            const axis = reader.readNumber();
            const tieBreak = decodeTensorIndexTieBreak(reader);

            return {
                kind: "tensorIndexReduce",
                destination,
                operator,
                tensor,
                axis,
                tieBreak,
            };
        }
        case 64: {
            const destination = decodeValue(reader);
            const left = decodeValue(reader);
            const right = decodeValue(reader);
            const immediate = decodeTensorImmediateId(reader);

            return {
                kind: "tensorDot",
                destination,
                left,
                right,
                immediate,
            };
        }
        case 65: {
            const destination = decodeValue(reader);
            const input = decodeValue(reader);
            const kernel = decodeValue(reader);
            const immediate = decodeTensorImmediateId(reader);

            return {
                kind: "tensorConvolution",
                destination,
                input,
                kernel,
                immediate,
            };
        }
        case 66: {
            const destination = decodeValue(reader);
            const operand = decodeValue(reader);
            const indices = decodeValue(reader);
            const immediate = decodeTensorImmediateId(reader);

            return {
                kind: "tensorGather",
                destination,
                operand,
                indices,
                immediate,
            };
        }
        case 67: {
            const destination = decodeValue(reader);
            const operand = decodeValue(reader);
            const indices = decodeValue(reader);
            const updates = decodeValue(reader);
            const immediate = decodeTensorImmediateId(reader);
            const mode = decodeTensorScatterMode(reader);

            return {
                kind: "tensorScatter",
                destination,
                operand,
                indices,
                updates,
                immediate,
                mode,
            };
        }
        case 68: {
            const destination = decodeValue(reader);
            const mode = decodeConvertMode(reader);
            const tensor = decodeValue(reader);

            return {
                kind: "tensorConvert",
                destination,
                mode,
                tensor,
            };
        }
        case 69: {
            const destination = reader.readOption(() => decodeValue(reader));
            const call = decodeCall(reader);

            return {
                kind: "call",
                ...(destination === undefined ? {} : { destination }),
                call,
            };
        }
        case 70: {
            const value = decodeValue(reader);

            return {
                kind: "drop",
                value,
            };
        }
        case 71: {
            const destination = decodeValue(reader);
            const storageType = decodeLocalNodeId(reader);
            const resultType = decodeLocalNodeId(reader);

            return {
                kind: "newZeroed",
                destination,
                storageType,
                resultType,
            };
        }
        case 72: {
            const destination = decodeValue(reader);
            const storageType = decodeLocalNodeId(reader);
            const resultType = decodeLocalNodeId(reader);

            return {
                kind: "newUninit",
                destination,
                storageType,
                resultType,
            };
        }
        case 73: {
            const destination = decodeValue(reader);
            const value = decodeValue(reader);
            const resultType = decodeLocalNodeId(reader);

            return {
                kind: "newComplete",
                destination,
                value,
                resultType,
            };
        }
        case 74: {
            const destination = decodeValue(reader);
            const element = decodeLocalNodeId(reader);
            const length = decodeValue(reader);
            const resultType = decodeLocalNodeId(reader);

            return {
                kind: "newSliceZeroed",
                destination,
                element,
                length,
                resultType,
            };
        }
        case 75: {
            const destination = decodeValue(reader);
            const element = decodeLocalNodeId(reader);
            const length = decodeValue(reader);
            const resultType = decodeLocalNodeId(reader);

            return {
                kind: "newSliceUninit",
                destination,
                element,
                length,
                resultType,
            };
        }
        case 76: {
            const value = decodeValue(reader);

            return {
                kind: "free",
                value,
            };
        }
        case 77: {
            const destination = decodeValue(reader);
            const value = decodeValue(reader);
            const resultType = decodeLocalNodeId(reader);

            return {
                kind: "pin",
                destination,
                value,
                resultType,
            };
        }
        case 78: {
            const value = decodeValue(reader);

            return {
                kind: "unpin",
                value,
            };
        }
        case 79: {
            const object_ = decodeValue(reader);
            const offset = decodeValue(reader);
            const byteLen = decodeValue(reader);

            return {
                kind: "barrierWrite",
                object: object_,
                offset,
                byteLen,
            };
        }
        case 80: {
            const destination = decodeValue(reader);
            const pointer = decodeValue(reader);
            const resultType = decodeLocalNodeId(reader);
            const access = decodeAtomicAccess(reader);

            return {
                kind: "atomicLoad",
                destination,
                pointer,
                resultType,
                access,
            };
        }
        case 81: {
            const pointer = decodeValue(reader);
            const value = decodeValue(reader);
            const access = decodeAtomicAccess(reader);

            return {
                kind: "atomicStore",
                pointer,
                value,
                access,
            };
        }
        case 82: {
            const destination = decodeValue(reader);
            const pointer = decodeValue(reader);
            const expected = decodeValue(reader);
            const newValue = decodeValue(reader);
            const isWeak = reader.readBool();
            const access = decodeCompareExchangeAccess(reader);

            return {
                kind: "atomicCompareExchange",
                destination,
                pointer,
                expected,
                newValue,
                isWeak,
                access,
            };
        }
        case 83: {
            const destination = decodeValue(reader);
            const operator = decodeAtomicRmwOperator(reader);
            const pointer = decodeValue(reader);
            const value = decodeValue(reader);
            const access = decodeAtomicAccess(reader);

            return {
                kind: "atomicRmw",
                destination,
                operator,
                pointer,
                value,
                access,
            };
        }
        case 84: {
            const access = decodeFenceAccess(reader);

            return {
                kind: "atomicFence",
                access,
            };
        }
        case 85: {
            const condition = decodeValue(reader);

            return {
                kind: "assume",
                condition,
            };
        }
        case 86: {
            const counter = decodeCounterId(reader);

            return {
                kind: "profileIncrement",
                counter,
            };
        }
        case 87: {
            const sampler = decodeSamplerId(reader);
            const value = decodeValue(reader);

            return {
                kind: "profileSample",
                sampler,
                value,
            };
        }
        case 88: {
            return { kind: "poll" };
        }
        case 89: {
            return { kind: "breakpoint" };
        }
        case 90: {
            const destination = reader.readOption(() => decodeValue(reader));
            const intrinsic = decodeIntrinsic(reader);
            const arguments_ = decodeValueSlice(reader);

            return {
                kind: "intrinsic",
                ...(destination === undefined ? {} : { destination }),
                intrinsic,
                arguments: arguments_,
            };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one Instruction. */
export function toJsonInstruction(value: Instruction): Json {
    switch (value.kind) {
        case "error":
            return {
                kind: "error",
            };
        case "const":
            return {
                kind: "const",
                destination: toJsonValue(value.destination),
                value: toJsonConstant(value.value),
            };
        case "binary":
            return {
                kind: "binary",
                destination: toJsonValue(value.destination),
                operator: toJsonBinaryOperator(value.operator),
                left: toJsonValue(value.left),
                right: toJsonValue(value.right),
            };
        case "unary":
            return {
                kind: "unary",
                destination: toJsonValue(value.destination),
                operator: toJsonUnaryOperator(value.operator),
                argument: toJsonValue(value.argument),
            };
        case "cast":
            return {
                kind: "cast",
                destination: toJsonValue(value.destination),
                operator: toJsonCastOperator(value.operator),
                argument: toJsonValue(value.argument),
                toType: toJsonLocalNodeId(value.toType),
            };
        case "select":
            return {
                kind: "select",
                destination: toJsonValue(value.destination),
                condition: toJsonValue(value.condition),
                thenValue: toJsonValue(value.thenValue),
                elseValue: toJsonValue(value.elseValue),
            };
        case "localGet":
            return {
                kind: "localGet",
                destination: toJsonValue(value.destination),
                local: toJsonLocalNodeId(value.local),
            };
        case "localAddr":
            return {
                kind: "localAddr",
                destination: toJsonValue(value.destination),
                local: toJsonLocalNodeId(value.local),
                resultType: toJsonLocalNodeId(value.resultType),
            };
        case "localSet":
            return {
                kind: "localSet",
                local: toJsonLocalNodeId(value.local),
                value: toJsonValue(value.value),
            };
        case "globalAddr":
            return {
                kind: "globalAddr",
                destination: toJsonValue(value.destination),
                global: toJsonLocalNodeId(value.global),
                resultType: toJsonLocalNodeId(value.resultType),
            };
        case "functionAddr":
            return {
                kind: "functionAddr",
                destination: toJsonValue(value.destination),
                function: toJsonLocalNodeId(value.function),
            };
        case "functionBind":
            return {
                kind: "functionBind",
                destination: toJsonValue(value.destination),
                function: toJsonLocalNodeId(value.function),
                environment: toJsonValue(value.environment),
            };
        case "functionEnvironment":
            return {
                kind: "functionEnvironment",
                destination: toJsonValue(value.destination),
                function: toJsonValue(value.function),
            };
        case "functionEnvironmentCurrent":
            return {
                kind: "functionEnvironmentCurrent",
                destination: toJsonValue(value.destination),
            };
        case "contextCurrent":
            return {
                kind: "contextCurrent",
                destination: toJsonValue(value.destination),
            };
        case "contextReplace":
            return {
                kind: "contextReplace",
                destination: toJsonValue(value.destination),
                context: toJsonValue(value.context),
            };
        case "contextBind":
            return {
                kind: "contextBind",
                destination: toJsonValue(value.destination),
                context: toJsonValue(value.context),
                variable: toJsonValue(value.variable),
                value: toJsonValue(value.value),
                nodeType: toJsonLocalNodeId(value.nodeType),
                resultType: toJsonLocalNodeId(value.resultType),
            };
        case "contextGet":
            return {
                kind: "contextGet",
                destination: toJsonValue(value.destination),
                context: toJsonValue(value.context),
                variable: toJsonValue(value.variable),
                default: toJsonValue(value.default),
                nodeType: toJsonLocalNodeId(value.nodeType),
                resultType: toJsonLocalNodeId(value.resultType),
            };
        case "load":
            return {
                kind: "load",
                destination: toJsonValue(value.destination),
                pointer: toJsonValue(value.pointer),
                resultType: toJsonLocalNodeId(value.resultType),
            };
        case "store":
            return {
                kind: "store",
                pointer: toJsonValue(value.pointer),
                value: toJsonValue(value.value),
            };
        case "aggregate":
            return {
                kind: "aggregate",
                destination: toJsonValue(value.destination),
                values: toJsonValueSlice(value.values),
            };
        case "fieldGet":
            return {
                kind: "fieldGet",
                destination: toJsonValue(value.destination),
                aggregate: toJsonValue(value.aggregate),
                field: value.field,
            };
        case "fieldSet":
            return {
                kind: "fieldSet",
                destination: toJsonValue(value.destination),
                aggregate: toJsonValue(value.aggregate),
                field: value.field,
                value: toJsonValue(value.value),
            };
        case "fieldAddr":
            return {
                kind: "fieldAddr",
                destination: toJsonValue(value.destination),
                aggregate: toJsonValue(value.aggregate),
                field: value.field,
                resultType: toJsonLocalNodeId(value.resultType),
            };
        case "elementGet":
            return {
                kind: "elementGet",
                destination: toJsonValue(value.destination),
                aggregate: toJsonValue(value.aggregate),
                index: value.index,
            };
        case "elementSet":
            return {
                kind: "elementSet",
                destination: toJsonValue(value.destination),
                aggregate: toJsonValue(value.aggregate),
                index: value.index,
                value: toJsonValue(value.value),
            };
        case "elementAddr":
            return {
                kind: "elementAddr",
                destination: toJsonValue(value.destination),
                base: toJsonValue(value.base),
                index: toJsonValue(value.index),
                resultType: toJsonLocalNodeId(value.resultType),
            };
        case "variantNew":
            return {
                kind: "variantNew",
                destination: toJsonValue(value.destination),
                case: value.case,
                ...(value.payload === undefined ? {} : { payload: toJsonValue(value.payload) }),
                resultType: toJsonLocalNodeId(value.resultType),
            };
        case "variantTag":
            return {
                kind: "variantTag",
                destination: toJsonValue(value.destination),
                variant: toJsonValue(value.variant),
            };
        case "variantTagLoad":
            return {
                kind: "variantTagLoad",
                destination: toJsonValue(value.destination),
                variant: toJsonValue(value.variant),
            };
        case "variantPayload":
            return {
                kind: "variantPayload",
                destination: toJsonValue(value.destination),
                variant: toJsonValue(value.variant),
                case: value.case,
            };
        case "variantPayloadAddr":
            return {
                kind: "variantPayloadAddr",
                destination: toJsonValue(value.destination),
                variant: toJsonValue(value.variant),
                case: value.case,
                resultType: toJsonLocalNodeId(value.resultType),
            };
        case "sliceView":
            return {
                kind: "sliceView",
                destination: toJsonValue(value.destination),
                source: toJsonValue(value.source),
                start: toJsonValue(value.start),
                length: toJsonValue(value.length),
                resultType: toJsonLocalNodeId(value.resultType),
            };
        case "sliceLength":
            return {
                kind: "sliceLength",
                destination: toJsonValue(value.destination),
                slice: toJsonValue(value.slice),
            };
        case "dynamicBind":
            return {
                kind: "dynamicBind",
                destination: toJsonValue(value.destination),
                payload: toJsonValue(value.payload),
                concrete: toJsonLocalNodeId(value.concrete),
            };
        case "dynamicPayload":
            return {
                kind: "dynamicPayload",
                destination: toJsonValue(value.destination),
                dynamic: toJsonValue(value.dynamic),
                resultType: toJsonLocalNodeId(value.resultType),
            };
        case "dynamicType":
            return {
                kind: "dynamicType",
                destination: toJsonValue(value.destination),
                dynamic: toJsonValue(value.dynamic),
            };
        case "dynamicFind":
            return {
                kind: "dynamicFind",
                destination: toJsonValue(value.destination),
                dynamic: toJsonValue(value.dynamic),
                key: toJsonValue(value.key),
                resultType: toJsonLocalNodeId(value.resultType),
            };
        case "vectorSplat":
            return {
                kind: "vectorSplat",
                destination: toJsonValue(value.destination),
                value: toJsonValue(value.value),
            };
        case "vectorExtract":
            return {
                kind: "vectorExtract",
                destination: toJsonValue(value.destination),
                vector: toJsonValue(value.vector),
                index: toJsonValue(value.index),
            };
        case "vectorInsert":
            return {
                kind: "vectorInsert",
                destination: toJsonValue(value.destination),
                vector: toJsonValue(value.vector),
                index: toJsonValue(value.index),
                value: toJsonValue(value.value),
            };
        case "vectorShuffle":
            return {
                kind: "vectorShuffle",
                destination: toJsonValue(value.destination),
                left: toJsonValue(value.left),
                right: toJsonValue(value.right),
                mask: toJsonIndexSlice(value.mask),
            };
        case "vectorSelect":
            return {
                kind: "vectorSelect",
                destination: toJsonValue(value.destination),
                mask: toJsonValue(value.mask),
                thenValue: toJsonValue(value.thenValue),
                elseValue: toJsonValue(value.elseValue),
            };
        case "vectorReduce":
            return {
                kind: "vectorReduce",
                destination: toJsonValue(value.destination),
                operator: toJsonVectorReduceOperator(value.operator),
                vector: toJsonValue(value.vector),
            };
        case "vectorCompare":
            return {
                kind: "vectorCompare",
                destination: toJsonValue(value.destination),
                operator: toJsonBinaryOperator(value.operator),
                left: toJsonValue(value.left),
                right: toJsonValue(value.right),
            };
        case "vectorConvert":
            return {
                kind: "vectorConvert",
                destination: toJsonValue(value.destination),
                mode: toJsonConvertMode(value.mode),
                vector: toJsonValue(value.vector),
            };
        case "tensorSplat":
            return {
                kind: "tensorSplat",
                destination: toJsonValue(value.destination),
                value: toJsonValue(value.value),
            };
        case "tensorLoad":
            return {
                kind: "tensorLoad",
                destination: toJsonValue(value.destination),
                view: toJsonValue(value.view),
                indices: toJsonValueSlice(value.indices),
            };
        case "tensorExtract":
            return {
                kind: "tensorExtract",
                destination: toJsonValue(value.destination),
                tensor: toJsonValue(value.tensor),
                indices: toJsonValueSlice(value.indices),
            };
        case "tensorStore":
            return {
                kind: "tensorStore",
                view: toJsonValue(value.view),
                indices: toJsonValueSlice(value.indices),
                value: toJsonValue(value.value),
            };
        case "tensorFill":
            return {
                kind: "tensorFill",
                view: toJsonValue(value.view),
                value: toJsonValue(value.value),
            };
        case "tensorCopy":
            return {
                kind: "tensorCopy",
                target: toJsonValue(value.target),
                source: toJsonValue(value.source),
            };
        case "tensorReshape":
            return {
                kind: "tensorReshape",
                destination: toJsonValue(value.destination),
                tensor: toJsonValue(value.tensor),
                shape: toJsonValueSlice(value.shape),
            };
        case "tensorBroadcast":
            return {
                kind: "tensorBroadcast",
                destination: toJsonValue(value.destination),
                tensor: toJsonValue(value.tensor),
                dimensions: toJsonIndexSlice(value.dimensions),
            };
        case "tensorTranspose":
            return {
                kind: "tensorTranspose",
                destination: toJsonValue(value.destination),
                tensor: toJsonValue(value.tensor),
                permutation: toJsonIndexSlice(value.permutation),
            };
        case "tensorCast":
            return {
                kind: "tensorCast",
                destination: toJsonValue(value.destination),
                tensor: toJsonValue(value.tensor),
            };
        case "tensorView":
            return {
                kind: "tensorView",
                destination: toJsonValue(value.destination),
                view: toJsonValue(value.view),
                arguments: toJsonValueSlice(value.arguments),
                offsetsCount: value.offsetsCount,
                sizesCount: value.sizesCount,
                stridesCount: value.stridesCount,
            };
        case "tensorSlice":
            return {
                kind: "tensorSlice",
                destination: toJsonValue(value.destination),
                tensor: toJsonValue(value.tensor),
                arguments: toJsonValueSlice(value.arguments),
                offsetsCount: value.offsetsCount,
                sizesCount: value.sizesCount,
                stridesCount: value.stridesCount,
            };
        case "tensorPad":
            return {
                kind: "tensorPad",
                destination: toJsonValue(value.destination),
                tensor: toJsonValue(value.tensor),
                arguments: toJsonValueSlice(value.arguments),
                lowCount: value.lowCount,
                highCount: value.highCount,
                interiorCount: value.interiorCount,
                value: toJsonValue(value.value),
            };
        case "tensorConcat":
            return {
                kind: "tensorConcat",
                destination: toJsonValue(value.destination),
                tensors: toJsonValueSlice(value.tensors),
                axis: value.axis,
            };
        case "tensorCompare":
            return {
                kind: "tensorCompare",
                destination: toJsonValue(value.destination),
                operator: toJsonBinaryOperator(value.operator),
                left: toJsonValue(value.left),
                right: toJsonValue(value.right),
            };
        case "tensorSelect":
            return {
                kind: "tensorSelect",
                destination: toJsonValue(value.destination),
                mask: toJsonValue(value.mask),
                thenValue: toJsonValue(value.thenValue),
                elseValue: toJsonValue(value.elseValue),
            };
        case "tensorReduce":
            return {
                kind: "tensorReduce",
                destination: toJsonValue(value.destination),
                operator: toJsonTensorReduceOperator(value.operator),
                tensor: toJsonValue(value.tensor),
                initial: toJsonValue(value.initial),
                axes: toJsonIndexSlice(value.axes),
            };
        case "tensorIndexReduce":
            return {
                kind: "tensorIndexReduce",
                destination: toJsonValue(value.destination),
                operator: toJsonTensorIndexReduceOperator(value.operator),
                tensor: toJsonValue(value.tensor),
                axis: value.axis,
                tieBreak: toJsonTensorIndexTieBreak(value.tieBreak),
            };
        case "tensorDot":
            return {
                kind: "tensorDot",
                destination: toJsonValue(value.destination),
                left: toJsonValue(value.left),
                right: toJsonValue(value.right),
                immediate: toJsonTensorImmediateId(value.immediate),
            };
        case "tensorConvolution":
            return {
                kind: "tensorConvolution",
                destination: toJsonValue(value.destination),
                input: toJsonValue(value.input),
                kernel: toJsonValue(value.kernel),
                immediate: toJsonTensorImmediateId(value.immediate),
            };
        case "tensorGather":
            return {
                kind: "tensorGather",
                destination: toJsonValue(value.destination),
                operand: toJsonValue(value.operand),
                indices: toJsonValue(value.indices),
                immediate: toJsonTensorImmediateId(value.immediate),
            };
        case "tensorScatter":
            return {
                kind: "tensorScatter",
                destination: toJsonValue(value.destination),
                operand: toJsonValue(value.operand),
                indices: toJsonValue(value.indices),
                updates: toJsonValue(value.updates),
                immediate: toJsonTensorImmediateId(value.immediate),
                mode: toJsonTensorScatterMode(value.mode),
            };
        case "tensorConvert":
            return {
                kind: "tensorConvert",
                destination: toJsonValue(value.destination),
                mode: toJsonConvertMode(value.mode),
                tensor: toJsonValue(value.tensor),
            };
        case "call":
            return {
                kind: "call",
                ...(value.destination === undefined ? {} : { destination: toJsonValue(value.destination) }),
                call: toJsonCall(value.call),
            };
        case "drop":
            return {
                kind: "drop",
                value: toJsonValue(value.value),
            };
        case "newZeroed":
            return {
                kind: "newZeroed",
                destination: toJsonValue(value.destination),
                storageType: toJsonLocalNodeId(value.storageType),
                resultType: toJsonLocalNodeId(value.resultType),
            };
        case "newUninit":
            return {
                kind: "newUninit",
                destination: toJsonValue(value.destination),
                storageType: toJsonLocalNodeId(value.storageType),
                resultType: toJsonLocalNodeId(value.resultType),
            };
        case "newComplete":
            return {
                kind: "newComplete",
                destination: toJsonValue(value.destination),
                value: toJsonValue(value.value),
                resultType: toJsonLocalNodeId(value.resultType),
            };
        case "newSliceZeroed":
            return {
                kind: "newSliceZeroed",
                destination: toJsonValue(value.destination),
                element: toJsonLocalNodeId(value.element),
                length: toJsonValue(value.length),
                resultType: toJsonLocalNodeId(value.resultType),
            };
        case "newSliceUninit":
            return {
                kind: "newSliceUninit",
                destination: toJsonValue(value.destination),
                element: toJsonLocalNodeId(value.element),
                length: toJsonValue(value.length),
                resultType: toJsonLocalNodeId(value.resultType),
            };
        case "free":
            return {
                kind: "free",
                value: toJsonValue(value.value),
            };
        case "pin":
            return {
                kind: "pin",
                destination: toJsonValue(value.destination),
                value: toJsonValue(value.value),
                resultType: toJsonLocalNodeId(value.resultType),
            };
        case "unpin":
            return {
                kind: "unpin",
                value: toJsonValue(value.value),
            };
        case "barrierWrite":
            return {
                kind: "barrierWrite",
                object: toJsonValue(value.object),
                offset: toJsonValue(value.offset),
                byteLen: toJsonValue(value.byteLen),
            };
        case "atomicLoad":
            return {
                kind: "atomicLoad",
                destination: toJsonValue(value.destination),
                pointer: toJsonValue(value.pointer),
                resultType: toJsonLocalNodeId(value.resultType),
                access: toJsonAtomicAccess(value.access),
            };
        case "atomicStore":
            return {
                kind: "atomicStore",
                pointer: toJsonValue(value.pointer),
                value: toJsonValue(value.value),
                access: toJsonAtomicAccess(value.access),
            };
        case "atomicCompareExchange":
            return {
                kind: "atomicCompareExchange",
                destination: toJsonValue(value.destination),
                pointer: toJsonValue(value.pointer),
                expected: toJsonValue(value.expected),
                newValue: toJsonValue(value.newValue),
                isWeak: value.isWeak,
                access: toJsonCompareExchangeAccess(value.access),
            };
        case "atomicRmw":
            return {
                kind: "atomicRmw",
                destination: toJsonValue(value.destination),
                operator: toJsonAtomicRmwOperator(value.operator),
                pointer: toJsonValue(value.pointer),
                value: toJsonValue(value.value),
                access: toJsonAtomicAccess(value.access),
            };
        case "atomicFence":
            return {
                kind: "atomicFence",
                access: toJsonFenceAccess(value.access),
            };
        case "assume":
            return {
                kind: "assume",
                condition: toJsonValue(value.condition),
            };
        case "profileIncrement":
            return {
                kind: "profileIncrement",
                counter: toJsonCounterId(value.counter),
            };
        case "profileSample":
            return {
                kind: "profileSample",
                sampler: toJsonSamplerId(value.sampler),
                value: toJsonValue(value.value),
            };
        case "poll":
            return {
                kind: "poll",
            };
        case "breakpoint":
            return {
                kind: "breakpoint",
            };
        case "intrinsic":
            return {
                kind: "intrinsic",
                ...(value.destination === undefined ? {} : { destination: toJsonValue(value.destination) }),
                intrinsic: toJsonIntrinsic(value.intrinsic),
                arguments: toJsonValueSlice(value.arguments),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one Instruction from one JSON value. */
export function fromJsonInstruction(value: Json): Instruction {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "error":
            return {
                kind,
            };
        case "const":
            return {
                kind,
                destination: fromJsonValue(jsonField(object, "destination")),
                value: fromJsonConstant(jsonField(object, "value")),
            };
        case "binary":
            return {
                kind,
                destination: fromJsonValue(jsonField(object, "destination")),
                operator: fromJsonBinaryOperator(jsonField(object, "operator")),
                left: fromJsonValue(jsonField(object, "left")),
                right: fromJsonValue(jsonField(object, "right")),
            };
        case "unary":
            return {
                kind,
                destination: fromJsonValue(jsonField(object, "destination")),
                operator: fromJsonUnaryOperator(jsonField(object, "operator")),
                argument: fromJsonValue(jsonField(object, "argument")),
            };
        case "cast":
            return {
                kind,
                destination: fromJsonValue(jsonField(object, "destination")),
                operator: fromJsonCastOperator(jsonField(object, "operator")),
                argument: fromJsonValue(jsonField(object, "argument")),
                toType: fromJsonLocalNodeId(jsonField(object, "toType")),
            };
        case "select":
            return {
                kind,
                destination: fromJsonValue(jsonField(object, "destination")),
                condition: fromJsonValue(jsonField(object, "condition")),
                thenValue: fromJsonValue(jsonField(object, "thenValue")),
                elseValue: fromJsonValue(jsonField(object, "elseValue")),
            };
        case "localGet":
            return {
                kind,
                destination: fromJsonValue(jsonField(object, "destination")),
                local: fromJsonLocalNodeId(jsonField(object, "local")),
            };
        case "localAddr":
            return {
                kind,
                destination: fromJsonValue(jsonField(object, "destination")),
                local: fromJsonLocalNodeId(jsonField(object, "local")),
                resultType: fromJsonLocalNodeId(jsonField(object, "resultType")),
            };
        case "localSet":
            return {
                kind,
                local: fromJsonLocalNodeId(jsonField(object, "local")),
                value: fromJsonValue(jsonField(object, "value")),
            };
        case "globalAddr":
            return {
                kind,
                destination: fromJsonValue(jsonField(object, "destination")),
                global: fromJsonLocalNodeId(jsonField(object, "global")),
                resultType: fromJsonLocalNodeId(jsonField(object, "resultType")),
            };
        case "functionAddr":
            return {
                kind,
                destination: fromJsonValue(jsonField(object, "destination")),
                function: fromJsonLocalNodeId(jsonField(object, "function")),
            };
        case "functionBind":
            return {
                kind,
                destination: fromJsonValue(jsonField(object, "destination")),
                function: fromJsonLocalNodeId(jsonField(object, "function")),
                environment: fromJsonValue(jsonField(object, "environment")),
            };
        case "functionEnvironment":
            return {
                kind,
                destination: fromJsonValue(jsonField(object, "destination")),
                function: fromJsonValue(jsonField(object, "function")),
            };
        case "functionEnvironmentCurrent":
            return {
                kind,
                destination: fromJsonValue(jsonField(object, "destination")),
            };
        case "contextCurrent":
            return {
                kind,
                destination: fromJsonValue(jsonField(object, "destination")),
            };
        case "contextReplace":
            return {
                kind,
                destination: fromJsonValue(jsonField(object, "destination")),
                context: fromJsonValue(jsonField(object, "context")),
            };
        case "contextBind":
            return {
                kind,
                destination: fromJsonValue(jsonField(object, "destination")),
                context: fromJsonValue(jsonField(object, "context")),
                variable: fromJsonValue(jsonField(object, "variable")),
                value: fromJsonValue(jsonField(object, "value")),
                nodeType: fromJsonLocalNodeId(jsonField(object, "nodeType")),
                resultType: fromJsonLocalNodeId(jsonField(object, "resultType")),
            };
        case "contextGet":
            return {
                kind,
                destination: fromJsonValue(jsonField(object, "destination")),
                context: fromJsonValue(jsonField(object, "context")),
                variable: fromJsonValue(jsonField(object, "variable")),
                default: fromJsonValue(jsonField(object, "default")),
                nodeType: fromJsonLocalNodeId(jsonField(object, "nodeType")),
                resultType: fromJsonLocalNodeId(jsonField(object, "resultType")),
            };
        case "load":
            return {
                kind,
                destination: fromJsonValue(jsonField(object, "destination")),
                pointer: fromJsonValue(jsonField(object, "pointer")),
                resultType: fromJsonLocalNodeId(jsonField(object, "resultType")),
            };
        case "store":
            return {
                kind,
                pointer: fromJsonValue(jsonField(object, "pointer")),
                value: fromJsonValue(jsonField(object, "value")),
            };
        case "aggregate":
            return {
                kind,
                destination: fromJsonValue(jsonField(object, "destination")),
                values: fromJsonValueSlice(jsonField(object, "values")),
            };
        case "fieldGet":
            return {
                kind,
                destination: fromJsonValue(jsonField(object, "destination")),
                aggregate: fromJsonValue(jsonField(object, "aggregate")),
                field: jsonInteger(jsonField(object, "field")),
            };
        case "fieldSet":
            return {
                kind,
                destination: fromJsonValue(jsonField(object, "destination")),
                aggregate: fromJsonValue(jsonField(object, "aggregate")),
                field: jsonInteger(jsonField(object, "field")),
                value: fromJsonValue(jsonField(object, "value")),
            };
        case "fieldAddr":
            return {
                kind,
                destination: fromJsonValue(jsonField(object, "destination")),
                aggregate: fromJsonValue(jsonField(object, "aggregate")),
                field: jsonInteger(jsonField(object, "field")),
                resultType: fromJsonLocalNodeId(jsonField(object, "resultType")),
            };
        case "elementGet":
            return {
                kind,
                destination: fromJsonValue(jsonField(object, "destination")),
                aggregate: fromJsonValue(jsonField(object, "aggregate")),
                index: jsonInteger(jsonField(object, "index")),
            };
        case "elementSet":
            return {
                kind,
                destination: fromJsonValue(jsonField(object, "destination")),
                aggregate: fromJsonValue(jsonField(object, "aggregate")),
                index: jsonInteger(jsonField(object, "index")),
                value: fromJsonValue(jsonField(object, "value")),
            };
        case "elementAddr":
            return {
                kind,
                destination: fromJsonValue(jsonField(object, "destination")),
                base: fromJsonValue(jsonField(object, "base")),
                index: fromJsonValue(jsonField(object, "index")),
                resultType: fromJsonLocalNodeId(jsonField(object, "resultType")),
            };
        case "variantNew":
            return {
                kind,
                destination: fromJsonValue(jsonField(object, "destination")),
                case: jsonInteger(jsonField(object, "case")),
                payload: jsonOptional(object, "payload", (value) => fromJsonValue(value)),
                resultType: fromJsonLocalNodeId(jsonField(object, "resultType")),
            };
        case "variantTag":
            return {
                kind,
                destination: fromJsonValue(jsonField(object, "destination")),
                variant: fromJsonValue(jsonField(object, "variant")),
            };
        case "variantTagLoad":
            return {
                kind,
                destination: fromJsonValue(jsonField(object, "destination")),
                variant: fromJsonValue(jsonField(object, "variant")),
            };
        case "variantPayload":
            return {
                kind,
                destination: fromJsonValue(jsonField(object, "destination")),
                variant: fromJsonValue(jsonField(object, "variant")),
                case: jsonInteger(jsonField(object, "case")),
            };
        case "variantPayloadAddr":
            return {
                kind,
                destination: fromJsonValue(jsonField(object, "destination")),
                variant: fromJsonValue(jsonField(object, "variant")),
                case: jsonInteger(jsonField(object, "case")),
                resultType: fromJsonLocalNodeId(jsonField(object, "resultType")),
            };
        case "sliceView":
            return {
                kind,
                destination: fromJsonValue(jsonField(object, "destination")),
                source: fromJsonValue(jsonField(object, "source")),
                start: fromJsonValue(jsonField(object, "start")),
                length: fromJsonValue(jsonField(object, "length")),
                resultType: fromJsonLocalNodeId(jsonField(object, "resultType")),
            };
        case "sliceLength":
            return {
                kind,
                destination: fromJsonValue(jsonField(object, "destination")),
                slice: fromJsonValue(jsonField(object, "slice")),
            };
        case "dynamicBind":
            return {
                kind,
                destination: fromJsonValue(jsonField(object, "destination")),
                payload: fromJsonValue(jsonField(object, "payload")),
                concrete: fromJsonLocalNodeId(jsonField(object, "concrete")),
            };
        case "dynamicPayload":
            return {
                kind,
                destination: fromJsonValue(jsonField(object, "destination")),
                dynamic: fromJsonValue(jsonField(object, "dynamic")),
                resultType: fromJsonLocalNodeId(jsonField(object, "resultType")),
            };
        case "dynamicType":
            return {
                kind,
                destination: fromJsonValue(jsonField(object, "destination")),
                dynamic: fromJsonValue(jsonField(object, "dynamic")),
            };
        case "dynamicFind":
            return {
                kind,
                destination: fromJsonValue(jsonField(object, "destination")),
                dynamic: fromJsonValue(jsonField(object, "dynamic")),
                key: fromJsonValue(jsonField(object, "key")),
                resultType: fromJsonLocalNodeId(jsonField(object, "resultType")),
            };
        case "vectorSplat":
            return {
                kind,
                destination: fromJsonValue(jsonField(object, "destination")),
                value: fromJsonValue(jsonField(object, "value")),
            };
        case "vectorExtract":
            return {
                kind,
                destination: fromJsonValue(jsonField(object, "destination")),
                vector: fromJsonValue(jsonField(object, "vector")),
                index: fromJsonValue(jsonField(object, "index")),
            };
        case "vectorInsert":
            return {
                kind,
                destination: fromJsonValue(jsonField(object, "destination")),
                vector: fromJsonValue(jsonField(object, "vector")),
                index: fromJsonValue(jsonField(object, "index")),
                value: fromJsonValue(jsonField(object, "value")),
            };
        case "vectorShuffle":
            return {
                kind,
                destination: fromJsonValue(jsonField(object, "destination")),
                left: fromJsonValue(jsonField(object, "left")),
                right: fromJsonValue(jsonField(object, "right")),
                mask: fromJsonIndexSlice(jsonField(object, "mask")),
            };
        case "vectorSelect":
            return {
                kind,
                destination: fromJsonValue(jsonField(object, "destination")),
                mask: fromJsonValue(jsonField(object, "mask")),
                thenValue: fromJsonValue(jsonField(object, "thenValue")),
                elseValue: fromJsonValue(jsonField(object, "elseValue")),
            };
        case "vectorReduce":
            return {
                kind,
                destination: fromJsonValue(jsonField(object, "destination")),
                operator: fromJsonVectorReduceOperator(jsonField(object, "operator")),
                vector: fromJsonValue(jsonField(object, "vector")),
            };
        case "vectorCompare":
            return {
                kind,
                destination: fromJsonValue(jsonField(object, "destination")),
                operator: fromJsonBinaryOperator(jsonField(object, "operator")),
                left: fromJsonValue(jsonField(object, "left")),
                right: fromJsonValue(jsonField(object, "right")),
            };
        case "vectorConvert":
            return {
                kind,
                destination: fromJsonValue(jsonField(object, "destination")),
                mode: fromJsonConvertMode(jsonField(object, "mode")),
                vector: fromJsonValue(jsonField(object, "vector")),
            };
        case "tensorSplat":
            return {
                kind,
                destination: fromJsonValue(jsonField(object, "destination")),
                value: fromJsonValue(jsonField(object, "value")),
            };
        case "tensorLoad":
            return {
                kind,
                destination: fromJsonValue(jsonField(object, "destination")),
                view: fromJsonValue(jsonField(object, "view")),
                indices: fromJsonValueSlice(jsonField(object, "indices")),
            };
        case "tensorExtract":
            return {
                kind,
                destination: fromJsonValue(jsonField(object, "destination")),
                tensor: fromJsonValue(jsonField(object, "tensor")),
                indices: fromJsonValueSlice(jsonField(object, "indices")),
            };
        case "tensorStore":
            return {
                kind,
                view: fromJsonValue(jsonField(object, "view")),
                indices: fromJsonValueSlice(jsonField(object, "indices")),
                value: fromJsonValue(jsonField(object, "value")),
            };
        case "tensorFill":
            return {
                kind,
                view: fromJsonValue(jsonField(object, "view")),
                value: fromJsonValue(jsonField(object, "value")),
            };
        case "tensorCopy":
            return {
                kind,
                target: fromJsonValue(jsonField(object, "target")),
                source: fromJsonValue(jsonField(object, "source")),
            };
        case "tensorReshape":
            return {
                kind,
                destination: fromJsonValue(jsonField(object, "destination")),
                tensor: fromJsonValue(jsonField(object, "tensor")),
                shape: fromJsonValueSlice(jsonField(object, "shape")),
            };
        case "tensorBroadcast":
            return {
                kind,
                destination: fromJsonValue(jsonField(object, "destination")),
                tensor: fromJsonValue(jsonField(object, "tensor")),
                dimensions: fromJsonIndexSlice(jsonField(object, "dimensions")),
            };
        case "tensorTranspose":
            return {
                kind,
                destination: fromJsonValue(jsonField(object, "destination")),
                tensor: fromJsonValue(jsonField(object, "tensor")),
                permutation: fromJsonIndexSlice(jsonField(object, "permutation")),
            };
        case "tensorCast":
            return {
                kind,
                destination: fromJsonValue(jsonField(object, "destination")),
                tensor: fromJsonValue(jsonField(object, "tensor")),
            };
        case "tensorView":
            return {
                kind,
                destination: fromJsonValue(jsonField(object, "destination")),
                view: fromJsonValue(jsonField(object, "view")),
                arguments: fromJsonValueSlice(jsonField(object, "arguments")),
                offsetsCount: jsonInteger(jsonField(object, "offsetsCount")),
                sizesCount: jsonInteger(jsonField(object, "sizesCount")),
                stridesCount: jsonInteger(jsonField(object, "stridesCount")),
            };
        case "tensorSlice":
            return {
                kind,
                destination: fromJsonValue(jsonField(object, "destination")),
                tensor: fromJsonValue(jsonField(object, "tensor")),
                arguments: fromJsonValueSlice(jsonField(object, "arguments")),
                offsetsCount: jsonInteger(jsonField(object, "offsetsCount")),
                sizesCount: jsonInteger(jsonField(object, "sizesCount")),
                stridesCount: jsonInteger(jsonField(object, "stridesCount")),
            };
        case "tensorPad":
            return {
                kind,
                destination: fromJsonValue(jsonField(object, "destination")),
                tensor: fromJsonValue(jsonField(object, "tensor")),
                arguments: fromJsonValueSlice(jsonField(object, "arguments")),
                lowCount: jsonInteger(jsonField(object, "lowCount")),
                highCount: jsonInteger(jsonField(object, "highCount")),
                interiorCount: jsonInteger(jsonField(object, "interiorCount")),
                value: fromJsonValue(jsonField(object, "value")),
            };
        case "tensorConcat":
            return {
                kind,
                destination: fromJsonValue(jsonField(object, "destination")),
                tensors: fromJsonValueSlice(jsonField(object, "tensors")),
                axis: jsonInteger(jsonField(object, "axis")),
            };
        case "tensorCompare":
            return {
                kind,
                destination: fromJsonValue(jsonField(object, "destination")),
                operator: fromJsonBinaryOperator(jsonField(object, "operator")),
                left: fromJsonValue(jsonField(object, "left")),
                right: fromJsonValue(jsonField(object, "right")),
            };
        case "tensorSelect":
            return {
                kind,
                destination: fromJsonValue(jsonField(object, "destination")),
                mask: fromJsonValue(jsonField(object, "mask")),
                thenValue: fromJsonValue(jsonField(object, "thenValue")),
                elseValue: fromJsonValue(jsonField(object, "elseValue")),
            };
        case "tensorReduce":
            return {
                kind,
                destination: fromJsonValue(jsonField(object, "destination")),
                operator: fromJsonTensorReduceOperator(jsonField(object, "operator")),
                tensor: fromJsonValue(jsonField(object, "tensor")),
                initial: fromJsonValue(jsonField(object, "initial")),
                axes: fromJsonIndexSlice(jsonField(object, "axes")),
            };
        case "tensorIndexReduce":
            return {
                kind,
                destination: fromJsonValue(jsonField(object, "destination")),
                operator: fromJsonTensorIndexReduceOperator(jsonField(object, "operator")),
                tensor: fromJsonValue(jsonField(object, "tensor")),
                axis: jsonInteger(jsonField(object, "axis")),
                tieBreak: fromJsonTensorIndexTieBreak(jsonField(object, "tieBreak")),
            };
        case "tensorDot":
            return {
                kind,
                destination: fromJsonValue(jsonField(object, "destination")),
                left: fromJsonValue(jsonField(object, "left")),
                right: fromJsonValue(jsonField(object, "right")),
                immediate: fromJsonTensorImmediateId(jsonField(object, "immediate")),
            };
        case "tensorConvolution":
            return {
                kind,
                destination: fromJsonValue(jsonField(object, "destination")),
                input: fromJsonValue(jsonField(object, "input")),
                kernel: fromJsonValue(jsonField(object, "kernel")),
                immediate: fromJsonTensorImmediateId(jsonField(object, "immediate")),
            };
        case "tensorGather":
            return {
                kind,
                destination: fromJsonValue(jsonField(object, "destination")),
                operand: fromJsonValue(jsonField(object, "operand")),
                indices: fromJsonValue(jsonField(object, "indices")),
                immediate: fromJsonTensorImmediateId(jsonField(object, "immediate")),
            };
        case "tensorScatter":
            return {
                kind,
                destination: fromJsonValue(jsonField(object, "destination")),
                operand: fromJsonValue(jsonField(object, "operand")),
                indices: fromJsonValue(jsonField(object, "indices")),
                updates: fromJsonValue(jsonField(object, "updates")),
                immediate: fromJsonTensorImmediateId(jsonField(object, "immediate")),
                mode: fromJsonTensorScatterMode(jsonField(object, "mode")),
            };
        case "tensorConvert":
            return {
                kind,
                destination: fromJsonValue(jsonField(object, "destination")),
                mode: fromJsonConvertMode(jsonField(object, "mode")),
                tensor: fromJsonValue(jsonField(object, "tensor")),
            };
        case "call":
            return {
                kind,
                destination: jsonOptional(object, "destination", (value) => fromJsonValue(value)),
                call: fromJsonCall(jsonField(object, "call")),
            };
        case "drop":
            return {
                kind,
                value: fromJsonValue(jsonField(object, "value")),
            };
        case "newZeroed":
            return {
                kind,
                destination: fromJsonValue(jsonField(object, "destination")),
                storageType: fromJsonLocalNodeId(jsonField(object, "storageType")),
                resultType: fromJsonLocalNodeId(jsonField(object, "resultType")),
            };
        case "newUninit":
            return {
                kind,
                destination: fromJsonValue(jsonField(object, "destination")),
                storageType: fromJsonLocalNodeId(jsonField(object, "storageType")),
                resultType: fromJsonLocalNodeId(jsonField(object, "resultType")),
            };
        case "newComplete":
            return {
                kind,
                destination: fromJsonValue(jsonField(object, "destination")),
                value: fromJsonValue(jsonField(object, "value")),
                resultType: fromJsonLocalNodeId(jsonField(object, "resultType")),
            };
        case "newSliceZeroed":
            return {
                kind,
                destination: fromJsonValue(jsonField(object, "destination")),
                element: fromJsonLocalNodeId(jsonField(object, "element")),
                length: fromJsonValue(jsonField(object, "length")),
                resultType: fromJsonLocalNodeId(jsonField(object, "resultType")),
            };
        case "newSliceUninit":
            return {
                kind,
                destination: fromJsonValue(jsonField(object, "destination")),
                element: fromJsonLocalNodeId(jsonField(object, "element")),
                length: fromJsonValue(jsonField(object, "length")),
                resultType: fromJsonLocalNodeId(jsonField(object, "resultType")),
            };
        case "free":
            return {
                kind,
                value: fromJsonValue(jsonField(object, "value")),
            };
        case "pin":
            return {
                kind,
                destination: fromJsonValue(jsonField(object, "destination")),
                value: fromJsonValue(jsonField(object, "value")),
                resultType: fromJsonLocalNodeId(jsonField(object, "resultType")),
            };
        case "unpin":
            return {
                kind,
                value: fromJsonValue(jsonField(object, "value")),
            };
        case "barrierWrite":
            return {
                kind,
                object: fromJsonValue(jsonField(object, "object")),
                offset: fromJsonValue(jsonField(object, "offset")),
                byteLen: fromJsonValue(jsonField(object, "byteLen")),
            };
        case "atomicLoad":
            return {
                kind,
                destination: fromJsonValue(jsonField(object, "destination")),
                pointer: fromJsonValue(jsonField(object, "pointer")),
                resultType: fromJsonLocalNodeId(jsonField(object, "resultType")),
                access: fromJsonAtomicAccess(jsonField(object, "access")),
            };
        case "atomicStore":
            return {
                kind,
                pointer: fromJsonValue(jsonField(object, "pointer")),
                value: fromJsonValue(jsonField(object, "value")),
                access: fromJsonAtomicAccess(jsonField(object, "access")),
            };
        case "atomicCompareExchange":
            return {
                kind,
                destination: fromJsonValue(jsonField(object, "destination")),
                pointer: fromJsonValue(jsonField(object, "pointer")),
                expected: fromJsonValue(jsonField(object, "expected")),
                newValue: fromJsonValue(jsonField(object, "newValue")),
                isWeak: jsonBool(jsonField(object, "isWeak")),
                access: fromJsonCompareExchangeAccess(jsonField(object, "access")),
            };
        case "atomicRmw":
            return {
                kind,
                destination: fromJsonValue(jsonField(object, "destination")),
                operator: fromJsonAtomicRmwOperator(jsonField(object, "operator")),
                pointer: fromJsonValue(jsonField(object, "pointer")),
                value: fromJsonValue(jsonField(object, "value")),
                access: fromJsonAtomicAccess(jsonField(object, "access")),
            };
        case "atomicFence":
            return {
                kind,
                access: fromJsonFenceAccess(jsonField(object, "access")),
            };
        case "assume":
            return {
                kind,
                condition: fromJsonValue(jsonField(object, "condition")),
            };
        case "profileIncrement":
            return {
                kind,
                counter: fromJsonCounterId(jsonField(object, "counter")),
            };
        case "profileSample":
            return {
                kind,
                sampler: fromJsonSamplerId(jsonField(object, "sampler")),
                value: fromJsonValue(jsonField(object, "value")),
            };
        case "poll":
            return {
                kind,
            };
        case "breakpoint":
            return {
                kind,
            };
        case "intrinsic":
            return {
                kind,
                destination: jsonOptional(object, "destination", (value) => fromJsonValue(value)),
                intrinsic: fromJsonIntrinsic(jsonField(object, "intrinsic")),
                arguments: fromJsonValueSlice(jsonField(object, "arguments")),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}
