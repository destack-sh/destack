/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, jsonField, jsonObject } from "../../../protocol/serde.js";
import type { LocalNodeId } from "./node.js";
import type { Mutability } from "./type.js";
import { decodeLocalNodeId, encodeLocalNodeId, fromJsonLocalNodeId, toJsonLocalNodeId } from "./node.js";
import { decodeMutability, encodeMutability, fromJsonMutability, toJsonMutability } from "./type.js";

/** Local variable (stack slot) in a function. */
export type Local = {
    /** The type of the value stored in this slot. */
    readonly ty: LocalNodeId;
    /** Whether this local can be mutated after initialization. */
    readonly mutability: Mutability;
};

export const Local = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: Local): void {
        encodeLocal(writer, value);
    },

    /** Decode one Local. */
    decode(reader: BinaryReader): Local {
        return decodeLocal(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Local): Json {
        return toJsonLocal(value);
    },

    /** Return one Local from one JSON value. */
    fromJson(value: Json): Local {
        return fromJsonLocal(value);
    },
};

/** Encode one Local. */
export function encodeLocal(writer: BinaryWriter, value: Local): void {
    encodeLocalNodeId(writer, value.ty);
    encodeMutability(writer, value.mutability);
}

/** Decode one Local. */
export function decodeLocal(reader: BinaryReader): Local {
    const ty = decodeLocalNodeId(reader);
    const mutability = decodeMutability(reader);

    return {
        ty,
        mutability,
    };
}

/** Return one JSON value for one Local. */
export function toJsonLocal(value: Local): Json {
    return {
        ty: toJsonLocalNodeId(value.ty),
        mutability: toJsonMutability(value.mutability),
    };
}

/** Return one Local from one JSON value. */
export function fromJsonLocal(value: Json): Local {
    const object = jsonObject(value);

    return {
        ty: fromJsonLocalNodeId(jsonField(object, "ty")),
        mutability: fromJsonMutability(jsonField(object, "mutability")),
    };
}
