/* generated by destack_client_generate, do not edit */

import {
    BinaryReader,
    BinaryWriter,
    Json,
    SerdeError,
    jsonArray,
    jsonBigint,
    jsonBool,
    jsonField,
    jsonInteger,
    jsonObject,
    jsonOptional,
    jsonString,
} from "../../../protocol/serde.js";
import type { StringId } from "../../core/string.js";
import type { Constant } from "./constant.js";
import type { Lifetime } from "./lifetime.js";
import type { LifetimeParameter } from "./lifetime.js";
import type { LocalNodeId } from "./node.js";
import type { SignatureParameter } from "./parameter.js";
import type { StaticId } from "./static.js";
import {
    decodeStringId,
    encodeStringId,
    fromJsonStringId,
    toJsonStringId,
} from "../../core/string.js";
import { decodeConstant, encodeConstant, fromJsonConstant, toJsonConstant } from "./constant.js";
import { decodeLifetime, encodeLifetime, fromJsonLifetime, toJsonLifetime } from "./lifetime.js";
import {
    decodeLifetimeParameter,
    encodeLifetimeParameter,
    fromJsonLifetimeParameter,
    toJsonLifetimeParameter,
} from "./lifetime.js";
import {
    decodeLocalNodeId,
    encodeLocalNodeId,
    fromJsonLocalNodeId,
    toJsonLocalNodeId,
} from "./node.js";
import {
    decodeSignatureParameter,
    encodeSignatureParameter,
    fromJsonSignatureParameter,
    toJsonSignatureParameter,
} from "./parameter.js";
import { decodeStaticId, encodeStaticId, fromJsonStaticId, toJsonStaticId } from "./static.js";

/** Access exposed by a reference-like value. */
export type Access =
    /** Readonly access. */
    | {
          readonly kind: "readonly";
      }
    /** Mutable access, exclusive on owned storage by the borrow check and aliased through handles. */
    | {
          readonly kind: "mutable";
      }
    /** The access one template parameter names. */
    | {
          readonly kind: "parameter";
          readonly parameter: number;
      };

export const Access = {
    /** Readonly access. */
    readonly(): Access {
        return { kind: "readonly" };
    },

    /** Mutable access, exclusive on owned storage by the borrow check and aliased through handles. */
    mutable(): Access {
        return { kind: "mutable" };
    },

    /** The access one template parameter names. */
    parameter(parameter: number): Access {
        return { kind: "parameter", parameter };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: Access): void {
        encodeAccess(writer, value);
    },

    /** Decode one Access. */
    decode(reader: BinaryReader): Access {
        return decodeAccess(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Access): Json {
        return toJsonAccess(value);
    },

    /** Return one Access from one JSON value. */
    fromJson(value: Json): Access {
        return fromJsonAccess(value);
    },
};

/** Encode one Access. */
export function encodeAccess(writer: BinaryWriter, value: Access): void {
    switch (value.kind) {
        case "readonly":
            writer.writeUnsigned(0);
            return;
        case "mutable":
            writer.writeUnsigned(1);
            return;
        case "parameter":
            writer.writeUnsigned(2);
            writer.writeUnsigned(value.parameter);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one Access. */
export function decodeAccess(reader: BinaryReader): Access {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            return { kind: "readonly" };
        }
        case 1: {
            return { kind: "mutable" };
        }
        case 2: {
            const parameter = reader.readNumber();

            return { kind: "parameter", parameter };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one Access. */
export function toJsonAccess(value: Access): Json {
    switch (value.kind) {
        case "readonly":
            return {
                kind: "readonly",
            };
        case "mutable":
            return {
                kind: "mutable",
            };
        case "parameter":
            return {
                kind: "parameter",
                parameter: value.parameter,
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one Access from one JSON value. */
export function fromJsonAccess(value: Json): Access {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "readonly":
            return {
                kind,
            };
        case "mutable":
            return {
                kind,
            };
        case "parameter":
            return {
                kind,
                parameter: jsonInteger(jsonField(object, "parameter")),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** Copyability of a type. */
export type Copy = "yes" | "no";

export const Copy = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: Copy): void {
        encodeCopy(writer, value);
    },

    /** Decode one Copy. */
    decode(reader: BinaryReader): Copy {
        return decodeCopy(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Copy): Json {
        return toJsonCopy(value);
    },

    /** Return one Copy from one JSON value. */
    fromJson(value: Json): Copy {
        return fromJsonCopy(value);
    },
};

/** Encode one Copy. */
export function encodeCopy(writer: BinaryWriter, value: Copy): void {
    switch (value) {
        case "yes":
            writer.writeUnsigned(0);
            return;
        case "no":
            writer.writeUnsigned(1);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one Copy. */
export function decodeCopy(reader: BinaryReader): Copy {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "yes";
        case 1:
            return "no";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one Copy. */
export function toJsonCopy(value: Copy): Json {
    return value;
}

/** Return one Copy from one JSON value. */
export function fromJsonCopy(value: Json): Copy {
    const variant = jsonString(value);

    switch (variant) {
        case "yes":
            return "yes";
        case "no":
            return "no";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** A field in a struct type. */
export type Field = {
    /** The optional field name. */
    readonly name?: StringId;
    /** Type of the field. */
    readonly ty: LocalNodeId;
};

export const Field = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: Field): void {
        encodeField(writer, value);
    },

    /** Decode one Field. */
    decode(reader: BinaryReader): Field {
        return decodeField(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Field): Json {
        return toJsonField(value);
    },

    /** Return one Field from one JSON value. */
    fromJson(value: Json): Field {
        return fromJsonField(value);
    },
};

/** Encode one Field. */
export function encodeField(writer: BinaryWriter, value: Field): void {
    writer.writeOption(value.name, (value0) => {
        encodeStringId(writer, value0);
    });
    encodeLocalNodeId(writer, value.ty);
}

/** Decode one Field. */
export function decodeField(reader: BinaryReader): Field {
    const name = reader.readOption(() => decodeStringId(reader));
    const ty = decodeLocalNodeId(reader);

    return {
        ...(name === undefined ? {} : { name }),
        ty,
    };
}

/** Return one JSON value for one Field. */
export function toJsonField(value: Field): Json {
    return {
        ...(value.name === undefined ? {} : { name: toJsonStringId(value.name) }),
        ty: toJsonLocalNodeId(value.ty),
    };
}

/** Return one Field from one JSON value. */
export function fromJsonField(value: Json): Field {
    const object = jsonObject(value);

    return {
        name: jsonOptional(object, "name", (value) => fromJsonStringId(value)),
        ty: fromJsonLocalNodeId(jsonField(object, "ty")),
    };
}

/** A concrete MIR floating-point type. */
export type FloatType = "float32" | "float64";

export const FloatType = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: FloatType): void {
        encodeFloatType(writer, value);
    },

    /** Decode one FloatType. */
    decode(reader: BinaryReader): FloatType {
        return decodeFloatType(reader);
    },

    /** Return this value as JSON. */
    toJson(value: FloatType): Json {
        return toJsonFloatType(value);
    },

    /** Return one FloatType from one JSON value. */
    fromJson(value: Json): FloatType {
        return fromJsonFloatType(value);
    },
};

/** Encode one FloatType. */
export function encodeFloatType(writer: BinaryWriter, value: FloatType): void {
    switch (value) {
        case "float32":
            writer.writeUnsigned(0);
            return;
        case "float64":
            writer.writeUnsigned(1);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one FloatType. */
export function decodeFloatType(reader: BinaryReader): FloatType {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "float32";
        case 1:
            return "float64";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one FloatType. */
export function toJsonFloatType(value: FloatType): Json {
    return value;
}

/** Return one FloatType from one JSON value. */
export function fromJsonFloatType(value: Json): FloatType {
    const variant = jsonString(value);

    switch (variant) {
        case "float32":
            return "float32";
        case "float64":
            return "float64";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** One argument applied to a generic parameter. */
export type GenericArgument =
    /** A type. */
    | {
          readonly kind: "type";
          readonly type: LocalNodeId;
      }
    /** A region: the extent a borrow lives within and the space it points into. */
    | {
          readonly kind: "region";
          /** The extent. */
          readonly lifetime: Lifetime;
          /** The space. */
          readonly space: Space;
      }
    /** A memory space. */
    | {
          readonly kind: "space";
          readonly space: Space;
      }
    /** A reference access. */
    | {
          readonly kind: "access";
          readonly access: Access;
      }
    /** A value. */
    | {
          readonly kind: "value";
          readonly value: StaticId;
      };

export const GenericArgument = {
    /** A type. */
    type(type_: LocalNodeId): GenericArgument {
        return { kind: "type", type: type_ };
    },

    /** A region: the extent a borrow lives within and the space it points into. */
    region(lifetime: Lifetime, space: Space): GenericArgument {
        return { kind: "region", lifetime, space };
    },

    /** A memory space. */
    space(space: Space): GenericArgument {
        return { kind: "space", space };
    },

    /** A reference access. */
    access(access: Access): GenericArgument {
        return { kind: "access", access };
    },

    /** A value. */
    value(value: StaticId): GenericArgument {
        return { kind: "value", value };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: GenericArgument): void {
        encodeGenericArgument(writer, value);
    },

    /** Decode one GenericArgument. */
    decode(reader: BinaryReader): GenericArgument {
        return decodeGenericArgument(reader);
    },

    /** Return this value as JSON. */
    toJson(value: GenericArgument): Json {
        return toJsonGenericArgument(value);
    },

    /** Return one GenericArgument from one JSON value. */
    fromJson(value: Json): GenericArgument {
        return fromJsonGenericArgument(value);
    },
};

/** Encode one GenericArgument. */
export function encodeGenericArgument(writer: BinaryWriter, value: GenericArgument): void {
    switch (value.kind) {
        case "type":
            writer.writeUnsigned(0);
            encodeLocalNodeId(writer, value.type);
            return;
        case "region":
            writer.writeUnsigned(1);
            encodeLifetime(writer, value.lifetime);
            encodeSpace(writer, value.space);
            return;
        case "space":
            writer.writeUnsigned(2);
            encodeSpace(writer, value.space);
            return;
        case "access":
            writer.writeUnsigned(3);
            encodeAccess(writer, value.access);
            return;
        case "value":
            writer.writeUnsigned(4);
            encodeStaticId(writer, value.value);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one GenericArgument. */
export function decodeGenericArgument(reader: BinaryReader): GenericArgument {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const type_ = decodeLocalNodeId(reader);

            return { kind: "type", type: type_ };
        }
        case 1: {
            const lifetime = decodeLifetime(reader);
            const space = decodeSpace(reader);

            return {
                kind: "region",
                lifetime,
                space,
            };
        }
        case 2: {
            const space = decodeSpace(reader);

            return { kind: "space", space };
        }
        case 3: {
            const access = decodeAccess(reader);

            return { kind: "access", access };
        }
        case 4: {
            const value = decodeStaticId(reader);

            return { kind: "value", value };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one GenericArgument. */
export function toJsonGenericArgument(value: GenericArgument): Json {
    switch (value.kind) {
        case "type":
            return {
                kind: "type",
                type: toJsonLocalNodeId(value.type),
            };
        case "region":
            return {
                kind: "region",
                lifetime: toJsonLifetime(value.lifetime),
                space: toJsonSpace(value.space),
            };
        case "space":
            return {
                kind: "space",
                space: toJsonSpace(value.space),
            };
        case "access":
            return {
                kind: "access",
                access: toJsonAccess(value.access),
            };
        case "value":
            return {
                kind: "value",
                value: toJsonStaticId(value.value),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one GenericArgument from one JSON value. */
export function fromJsonGenericArgument(value: Json): GenericArgument {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "type":
            return {
                kind,
                type: fromJsonLocalNodeId(jsonField(object, "type")),
            };
        case "region":
            return {
                kind,
                lifetime: fromJsonLifetime(jsonField(object, "lifetime")),
                space: fromJsonSpace(jsonField(object, "space")),
            };
        case "space":
            return {
                kind,
                space: fromJsonSpace(jsonField(object, "space")),
            };
        case "access":
            return {
                kind,
                access: fromJsonAccess(jsonField(object, "access")),
            };
        case "value":
            return {
                kind,
                value: fromJsonStaticId(jsonField(object, "value")),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** One generic parameter a function or type declaration takes. */
export type GenericParameter = {
    /** The declared name. */
    readonly name: StringId;
    /** The values the parameter ranges over. */
    readonly domain: GenericParameterDomain;
};

export const GenericParameter = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: GenericParameter): void {
        encodeGenericParameter(writer, value);
    },

    /** Decode one GenericParameter. */
    decode(reader: BinaryReader): GenericParameter {
        return decodeGenericParameter(reader);
    },

    /** Return this value as JSON. */
    toJson(value: GenericParameter): Json {
        return toJsonGenericParameter(value);
    },

    /** Return one GenericParameter from one JSON value. */
    fromJson(value: Json): GenericParameter {
        return fromJsonGenericParameter(value);
    },
};

/** Encode one GenericParameter. */
export function encodeGenericParameter(writer: BinaryWriter, value: GenericParameter): void {
    encodeStringId(writer, value.name);
    encodeGenericParameterDomain(writer, value.domain);
}

/** Decode one GenericParameter. */
export function decodeGenericParameter(reader: BinaryReader): GenericParameter {
    const name = decodeStringId(reader);
    const domain = decodeGenericParameterDomain(reader);

    return {
        name,
        domain,
    };
}

/** Return one JSON value for one GenericParameter. */
export function toJsonGenericParameter(value: GenericParameter): Json {
    return {
        name: toJsonStringId(value.name),
        domain: toJsonGenericParameterDomain(value.domain),
    };
}

/** Return one GenericParameter from one JSON value. */
export function fromJsonGenericParameter(value: Json): GenericParameter {
    const object = jsonObject(value);

    return {
        name: fromJsonStringId(jsonField(object, "name")),
        domain: fromJsonGenericParameterDomain(jsonField(object, "domain")),
    };
}

/** The values one generic parameter ranges over. */
export type GenericParameterDomain =
    /** The types satisfying every bound. */
    | {
          readonly kind: "type";
          /** The applied interfaces the parameter satisfies. */
          readonly bounds: ReadonlyArray<LocalNodeId>;
      }
    /** The regions, an extent with its space. */
    | {
          readonly kind: "region";
          /** The region parameters this one outlives, by generic index. */
          readonly outlives: ReadonlyArray<number>;
      }
    /** The memory spaces. */
    | {
          readonly kind: "space";
      }
    /** The reference accesses. */
    | {
          readonly kind: "access";
      }
    /** The values of one type. */
    | {
          readonly kind: "value";
          /** The value type. */
          readonly ty: LocalNodeId;
      };

export const GenericParameterDomain = {
    /** The types satisfying every bound. */
    type(bounds: ReadonlyArray<LocalNodeId>): GenericParameterDomain {
        return { kind: "type", bounds };
    },

    /** The regions, an extent with its space. */
    region(outlives: ReadonlyArray<number>): GenericParameterDomain {
        return { kind: "region", outlives };
    },

    /** The memory spaces. */
    space(): GenericParameterDomain {
        return { kind: "space" };
    },

    /** The reference accesses. */
    access(): GenericParameterDomain {
        return { kind: "access" };
    },

    /** The values of one type. */
    value(ty: LocalNodeId): GenericParameterDomain {
        return { kind: "value", ty };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: GenericParameterDomain): void {
        encodeGenericParameterDomain(writer, value);
    },

    /** Decode one GenericParameterDomain. */
    decode(reader: BinaryReader): GenericParameterDomain {
        return decodeGenericParameterDomain(reader);
    },

    /** Return this value as JSON. */
    toJson(value: GenericParameterDomain): Json {
        return toJsonGenericParameterDomain(value);
    },

    /** Return one GenericParameterDomain from one JSON value. */
    fromJson(value: Json): GenericParameterDomain {
        return fromJsonGenericParameterDomain(value);
    },
};

/** Encode one GenericParameterDomain. */
export function encodeGenericParameterDomain(
    writer: BinaryWriter,
    value: GenericParameterDomain,
): void {
    switch (value.kind) {
        case "type":
            writer.writeUnsigned(0);
            writer.writeUnsigned(value.bounds.length);
            for (const item0 of value.bounds) {
                encodeLocalNodeId(writer, item0);
            }
            return;
        case "region":
            writer.writeUnsigned(1);
            writer.writeUnsigned(value.outlives.length);
            for (const item0 of value.outlives) {
                writer.writeUnsigned(item0);
            }
            return;
        case "space":
            writer.writeUnsigned(2);
            return;
        case "access":
            writer.writeUnsigned(3);
            return;
        case "value":
            writer.writeUnsigned(4);
            encodeLocalNodeId(writer, value.ty);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one GenericParameterDomain. */
export function decodeGenericParameterDomain(reader: BinaryReader): GenericParameterDomain {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const bounds = (() => {
                const length0 = reader.readNumber();
                const items0: Array<LocalNodeId> = [];
                for (let index = 0; index < length0; index += 1) {
                    items0.push(decodeLocalNodeId(reader));
                }
                return items0;
            })();

            return {
                kind: "type",
                bounds,
            };
        }
        case 1: {
            const outlives = (() => {
                const length0 = reader.readNumber();
                const items0: Array<number> = [];
                for (let index = 0; index < length0; index += 1) {
                    items0.push(reader.readNumber());
                }
                return items0;
            })();

            return {
                kind: "region",
                outlives,
            };
        }
        case 2: {
            return { kind: "space" };
        }
        case 3: {
            return { kind: "access" };
        }
        case 4: {
            const ty = decodeLocalNodeId(reader);

            return {
                kind: "value",
                ty,
            };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one GenericParameterDomain. */
export function toJsonGenericParameterDomain(value: GenericParameterDomain): Json {
    switch (value.kind) {
        case "type":
            return {
                kind: "type",
                bounds: value.bounds.map((item0) => toJsonLocalNodeId(item0)),
            };
        case "region":
            return {
                kind: "region",
                outlives: value.outlives.map((item0) => item0),
            };
        case "space":
            return {
                kind: "space",
            };
        case "access":
            return {
                kind: "access",
            };
        case "value":
            return {
                kind: "value",
                ty: toJsonLocalNodeId(value.ty),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one GenericParameterDomain from one JSON value. */
export function fromJsonGenericParameterDomain(value: Json): GenericParameterDomain {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "type":
            return {
                kind,
                bounds: jsonArray(jsonField(object, "bounds")).map((item0) =>
                    fromJsonLocalNodeId(item0),
                ),
            };
        case "region":
            return {
                kind,
                outlives: jsonArray(jsonField(object, "outlives")).map((item0) =>
                    jsonInteger(item0),
                ),
            };
        case "space":
            return {
                kind,
            };
        case "access":
            return {
                kind,
            };
        case "value":
            return {
                kind,
                ty: fromJsonLocalNodeId(jsonField(object, "ty")),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** Permitted invocation count for a callable value. */
export type Multiplicity = "repeatable" | "once";

export const Multiplicity = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: Multiplicity): void {
        encodeMultiplicity(writer, value);
    },

    /** Decode one Multiplicity. */
    decode(reader: BinaryReader): Multiplicity {
        return decodeMultiplicity(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Multiplicity): Json {
        return toJsonMultiplicity(value);
    },

    /** Return one Multiplicity from one JSON value. */
    fromJson(value: Json): Multiplicity {
        return fromJsonMultiplicity(value);
    },
};

/** Encode one Multiplicity. */
export function encodeMultiplicity(writer: BinaryWriter, value: Multiplicity): void {
    switch (value) {
        case "repeatable":
            writer.writeUnsigned(0);
            return;
        case "once":
            writer.writeUnsigned(1);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one Multiplicity. */
export function decodeMultiplicity(reader: BinaryReader): Multiplicity {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "repeatable";
        case 1:
            return "once";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one Multiplicity. */
export function toJsonMultiplicity(value: Multiplicity): Json {
    return value;
}

/** Return one Multiplicity from one JSON value. */
export function fromJsonMultiplicity(value: Json): Multiplicity {
    const variant = jsonString(value);

    switch (variant) {
        case "repeatable":
            return "repeatable";
        case "once":
            return "once";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** Mutability of a storage binding. */
export type Mutability = "immutable" | "mutable";

export const Mutability = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: Mutability): void {
        encodeMutability(writer, value);
    },

    /** Decode one Mutability. */
    decode(reader: BinaryReader): Mutability {
        return decodeMutability(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Mutability): Json {
        return toJsonMutability(value);
    },

    /** Return one Mutability from one JSON value. */
    fromJson(value: Json): Mutability {
        return fromJsonMutability(value);
    },
};

/** Encode one Mutability. */
export function encodeMutability(writer: BinaryWriter, value: Mutability): void {
    switch (value) {
        case "immutable":
            writer.writeUnsigned(0);
            return;
        case "mutable":
            writer.writeUnsigned(1);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one Mutability. */
export function decodeMutability(reader: BinaryReader): Mutability {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "immutable";
        case 1:
            return "mutable";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one Mutability. */
export function toJsonMutability(value: Mutability): Json {
    return value;
}

/** Return one Mutability from one JSON value. */
export function fromJsonMutability(value: Json): Mutability {
    const variant = jsonString(value);

    switch (variant) {
        case "immutable":
            return "immutable";
        case "mutable":
            return "mutable";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** Kind of reference in MIR. */
export type ReferenceKind = "managed" | "unique" | "borrowed";

export const ReferenceKind = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ReferenceKind): void {
        encodeReferenceKind(writer, value);
    },

    /** Decode one ReferenceKind. */
    decode(reader: BinaryReader): ReferenceKind {
        return decodeReferenceKind(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ReferenceKind): Json {
        return toJsonReferenceKind(value);
    },

    /** Return one ReferenceKind from one JSON value. */
    fromJson(value: Json): ReferenceKind {
        return fromJsonReferenceKind(value);
    },
};

/** Encode one ReferenceKind. */
export function encodeReferenceKind(writer: BinaryWriter, value: ReferenceKind): void {
    switch (value) {
        case "managed":
            writer.writeUnsigned(0);
            return;
        case "unique":
            writer.writeUnsigned(1);
            return;
        case "borrowed":
            writer.writeUnsigned(2);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one ReferenceKind. */
export function decodeReferenceKind(reader: BinaryReader): ReferenceKind {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "managed";
        case 1:
            return "unique";
        case 2:
            return "borrowed";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one ReferenceKind. */
export function toJsonReferenceKind(value: ReferenceKind): Json {
    return value;
}

/** Return one ReferenceKind from one JSON value. */
export function fromJsonReferenceKind(value: Json): ReferenceKind {
    const variant = jsonString(value);

    switch (variant) {
        case "managed":
            return "managed";
        case "unique":
            return "unique";
        case "borrowed":
            return "borrowed";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** Runtime ownership domain. */
export type Space =
    /** Worker-local storage. */
    | {
          readonly kind: "local";
      }
    /** Runtime-shared storage. */
    | {
          readonly kind: "shared";
      }
    /** Immutable link-written storage, reachable from every space. */
    | {
          readonly kind: "constant";
      }
    /** The space one template parameter names. */
    | {
          readonly kind: "parameter";
          readonly parameter: number;
      };

export const Space = {
    /** Worker-local storage. */
    local(): Space {
        return { kind: "local" };
    },

    /** Runtime-shared storage. */
    shared(): Space {
        return { kind: "shared" };
    },

    /** Immutable link-written storage, reachable from every space. */
    constant(): Space {
        return { kind: "constant" };
    },

    /** The space one template parameter names. */
    parameter(parameter: number): Space {
        return { kind: "parameter", parameter };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: Space): void {
        encodeSpace(writer, value);
    },

    /** Decode one Space. */
    decode(reader: BinaryReader): Space {
        return decodeSpace(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Space): Json {
        return toJsonSpace(value);
    },

    /** Return one Space from one JSON value. */
    fromJson(value: Json): Space {
        return fromJsonSpace(value);
    },
};

/** Encode one Space. */
export function encodeSpace(writer: BinaryWriter, value: Space): void {
    switch (value.kind) {
        case "local":
            writer.writeUnsigned(0);
            return;
        case "shared":
            writer.writeUnsigned(1);
            return;
        case "constant":
            writer.writeUnsigned(2);
            return;
        case "parameter":
            writer.writeUnsigned(3);
            writer.writeUnsigned(value.parameter);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one Space. */
export function decodeSpace(reader: BinaryReader): Space {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            return { kind: "local" };
        }
        case 1: {
            return { kind: "shared" };
        }
        case 2: {
            return { kind: "constant" };
        }
        case 3: {
            const parameter = reader.readNumber();

            return { kind: "parameter", parameter };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one Space. */
export function toJsonSpace(value: Space): Json {
    switch (value.kind) {
        case "local":
            return {
                kind: "local",
            };
        case "shared":
            return {
                kind: "shared",
            };
        case "constant":
            return {
                kind: "constant",
            };
        case "parameter":
            return {
                kind: "parameter",
                parameter: value.parameter,
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one Space from one JSON value. */
export function fromJsonSpace(value: Json): Space {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "local":
            return {
                kind,
            };
        case "shared":
            return {
                kind,
            };
        case "constant":
            return {
                kind,
            };
        case "parameter":
            return {
                kind,
                parameter: jsonInteger(jsonField(object, "parameter")),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** Backing storage addressed by one reference-like value. */
export type Storage =
    /** Frame storage inside the current activation. */
    | {
          readonly kind: "frame";
      }
    /** Heap storage in one space. */
    | {
          readonly kind: "heap";
          readonly heap: Space;
      }
    /** Static storage written once in one space. */
    | {
          readonly kind: "static";
          readonly static: Space;
      };

export const Storage = {
    /** Frame storage inside the current activation. */
    frame(): Storage {
        return { kind: "frame" };
    },

    /** Heap storage in one space. */
    heap(heap: Space): Storage {
        return { kind: "heap", heap };
    },

    /** Static storage written once in one space. */
    static(static_: Space): Storage {
        return { kind: "static", static: static_ };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: Storage): void {
        encodeStorage(writer, value);
    },

    /** Decode one Storage. */
    decode(reader: BinaryReader): Storage {
        return decodeStorage(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Storage): Json {
        return toJsonStorage(value);
    },

    /** Return one Storage from one JSON value. */
    fromJson(value: Json): Storage {
        return fromJsonStorage(value);
    },
};

/** Encode one Storage. */
export function encodeStorage(writer: BinaryWriter, value: Storage): void {
    switch (value.kind) {
        case "frame":
            writer.writeUnsigned(0);
            return;
        case "heap":
            writer.writeUnsigned(1);
            encodeSpace(writer, value.heap);
            return;
        case "static":
            writer.writeUnsigned(2);
            encodeSpace(writer, value.static);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one Storage. */
export function decodeStorage(reader: BinaryReader): Storage {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            return { kind: "frame" };
        }
        case 1: {
            const heap = decodeSpace(reader);

            return { kind: "heap", heap };
        }
        case 2: {
            const static_ = decodeSpace(reader);

            return { kind: "static", static: static_ };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one Storage. */
export function toJsonStorage(value: Storage): Json {
    switch (value.kind) {
        case "frame":
            return {
                kind: "frame",
            };
        case "heap":
            return {
                kind: "heap",
                heap: toJsonSpace(value.heap),
            };
        case "static":
            return {
                kind: "static",
                static: toJsonSpace(value.static),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one Storage from one JSON value. */
export function fromJsonStorage(value: Json): Storage {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "frame":
            return {
                kind,
            };
        case "heap":
            return {
                kind,
                heap: fromJsonSpace(jsonField(object, "heap")),
            };
        case "static":
            return {
                kind,
                static: fromJsonSpace(jsonField(object, "static")),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** One logical MIR type. */
export type Type =
    /** Invalid type produced while recovering malformed MIR text. */
    | {
          readonly kind: "error";
      }
    /** Uninhabited type for execution that cannot complete normally. */
    | {
          readonly kind: "never";
      }
    /** Void / unit type (no value). */
    | {
          readonly kind: "void";
      }
    /** The null singleton: zero-sized, distinct from void. */
    | {
          readonly kind: "null";
      }
    /** Boolean (1 bit logical, typically 1 byte). */
    | {
          readonly kind: "boolean";
      }
    /** Unicode scalar value. */
    | {
          readonly kind: "character";
      }
    /** Integer with explicit width and signedness. */
    | {
          readonly kind: "int";
          readonly width: number;
          readonly isSigned: boolean;
      }
    /** Pointer-sized signed integer. */
    | {
          readonly kind: "isize";
      }
    /** Pointer-sized unsigned integer. */
    | {
          readonly kind: "usize";
      }
    /** Floating point with concrete representation. */
    | {
          readonly kind: "float";
          readonly float: FloatType;
      }
    /** Compact 32-bit runtime type identity token. */
    | {
          readonly kind: "typeId";
      }
    /** One parameter of the enclosing template. */
    | {
          readonly kind: "parameter";
          /** The parameter index in template order. */
          readonly index: number;
      }
    /** Atomic storage cell for one value type. */
    | {
          readonly kind: "atomic";
          /** The stored value type. */
          readonly value: LocalNodeId;
      }
    /** Runtime-erased value satisfying one dynamic constraint. */
    | {
          readonly kind: "dynamic";
          /** The reference kind of the erased payload. */
          readonly kindValue: ReferenceKind;
          /** Lifetime roots for a borrowed erased payload. */
          readonly lifetime: Lifetime;
          /** The lowered dynamic constraint type. */
          readonly constraint: LocalNodeId;
          /** The backing storage of the erased payload. */
          readonly storage: Storage;
          /** The access exposed through the erased payload. */
          readonly access: Access;
      }
    /** Reference with explicit kind and access. */
    | {
          readonly kind: "reference";
          /** The reference kind. */
          readonly kindValue: ReferenceKind;
          /** Lifetime roots for borrowed references. */
          readonly lifetime: Lifetime;
          /** The backing storage for this reference. */
          readonly storage: Storage;
          /** The access exposed through this reference. */
          readonly access: Access;
          /** The referenced type. */
          readonly pointee: LocalNodeId;
      }
    /** Process-local machine pointer. */
    | {
          readonly kind: "pointer";
          /** The pointed-to type. */
          readonly pointee: LocalNodeId;
          /** The access exposed through this pointer. */
          readonly access: Access;
      }
    /** Slice into memory, a repeated element with explicit kind and access. */
    | {
          readonly kind: "slice";
          /** The reference kind of the slice base. */
          readonly kindValue: ReferenceKind;
          /** Lifetime roots for borrowed slices. */
          readonly lifetime: Lifetime;
          /** The element type of the slice. */
          readonly element: LocalNodeId;
          /** The backing storage of the slice base. */
          readonly storage: Storage;
          /** The element access exposed by the slice. */
          readonly access: Access;
      }
    /** Linear token for one possibly uninitialized storage. */
    | {
          readonly kind: "uninit";
          /** The value under construction. */
          readonly value: LocalNodeId;
      }
    /** Owned storage whose automatic drop is suppressed. */
    | {
          readonly kind: "manuallyDrop";
          /** The wrapped value. */
          readonly value: LocalNodeId;
      }
    /** Fixed array: `[T; N]`. */
    | {
          readonly kind: "fixedArray";
          /** The element type of the array. */
          readonly element: LocalNodeId;
          /** The number of elements in the array. */
          readonly length: StaticId;
      }
    /** Tuple: `(T1, T2, ...)`. */
    | {
          readonly kind: "tuple";
          /** The element types of the tuple. */
          readonly elements: ReadonlyArray<LocalNodeId>;
      }
    /** Struct. */
    | {
          readonly kind: "struct";
          /** The fields of the struct. */
          readonly fields: ReadonlyArray<LocalNodeId>;
          /** Copy of this struct type. */
          readonly copy: Copy;
      }
    /** Nominal newtype over one wrapped type. */
    | {
          readonly kind: "newtype";
          /** The wrapped type. */
          readonly inner: LocalNodeId;
          /** Copy of this newtype. */
          readonly copy: Copy;
      }
    /** Sum value with one logical discriminant and case payloads. */
    | {
          readonly kind: "variant";
          /** The logical discriminant type. */
          readonly discriminant: LocalNodeId;
          /** The cases keyed by discriminant value. */
          readonly cases: ReadonlyArray<VariantCase>;
          /** Copy of this variant type. */
          readonly copy: Copy;
      }
    /** Fixed-width vector value. */
    | {
          readonly kind: "vector";
          /** The element type. */
          readonly element: LocalNodeId;
          /** The number of lanes. */
          readonly lanes: StaticId;
      }
    /** Bare function signature. */
    | {
          readonly kind: "functionSignature";
          /** Lifetime parameters in signature-local slot order. */
          readonly lifetimes: ReadonlyArray<LifetimeParameter>;
          /** The parameters of the function. */
          readonly parameters: ReadonlyArray<SignatureParameter>;
          /** The result type of the function. */
          readonly result: LocalNodeId;
      }
    /** Function value type. */
    | {
          readonly kind: "function";
          /** The bare function signature. */
          readonly signature: LocalNodeId;
          /** The permitted number of invocations. */
          readonly multiplicity: Multiplicity;
          /** The reference kind of the captured environment. */
          readonly kindValue: ReferenceKind;
          /** Lifetime roots for a borrowed captured environment. */
          readonly lifetime: Lifetime;
          /** The backing storage of the captured environment. */
          readonly storage: Storage;
          /** The access exposed through the captured environment. */
          readonly access: Access;
      }
    /** Function pointer type. */
    | {
          readonly kind: "functionPointer";
          /** The bare function signature. */
          readonly signature: LocalNodeId;
      }
    /** Type use with applied generic arguments. */
    | {
          readonly kind: "application";
          /** The type being applied. */
          readonly base: LocalNodeId;
          /** The applied generic arguments, in template order. */
          readonly arguments: ReadonlyArray<GenericArgument>;
      };

export const Type = {
    /** Invalid type produced while recovering malformed MIR text. */
    error(): Type {
        return { kind: "error" };
    },

    /** Uninhabited type for execution that cannot complete normally. */
    never(): Type {
        return { kind: "never" };
    },

    /** Void / unit type (no value). */
    void(): Type {
        return { kind: "void" };
    },

    /** The null singleton: zero-sized, distinct from void. */
    null(): Type {
        return { kind: "null" };
    },

    /** Boolean (1 bit logical, typically 1 byte). */
    boolean(): Type {
        return { kind: "boolean" };
    },

    /** Unicode scalar value. */
    character(): Type {
        return { kind: "character" };
    },

    /** Integer with explicit width and signedness. */
    int(width: number, isSigned: boolean): Type {
        return { kind: "int", width, isSigned };
    },

    /** Pointer-sized signed integer. */
    isize(): Type {
        return { kind: "isize" };
    },

    /** Pointer-sized unsigned integer. */
    usize(): Type {
        return { kind: "usize" };
    },

    /** Floating point with concrete representation. */
    float(float: FloatType): Type {
        return { kind: "float", float };
    },

    /** Compact 32-bit runtime type identity token. */
    typeId(): Type {
        return { kind: "typeId" };
    },

    /** One parameter of the enclosing template. */
    parameter(index: number): Type {
        return { kind: "parameter", index };
    },

    /** Atomic storage cell for one value type. */
    atomic(value: LocalNodeId): Type {
        return { kind: "atomic", value };
    },

    /** Runtime-erased value satisfying one dynamic constraint. */
    dynamic(
        kind: ReferenceKind,
        lifetime: Lifetime,
        constraint: LocalNodeId,
        storage: Storage,
        access: Access,
    ): Type {
        return { kind: "dynamic", kindValue: kind, lifetime, constraint, storage, access };
    },

    /** Reference with explicit kind and access. */
    reference(
        kind: ReferenceKind,
        lifetime: Lifetime,
        storage: Storage,
        access: Access,
        pointee: LocalNodeId,
    ): Type {
        return { kind: "reference", kindValue: kind, lifetime, storage, access, pointee };
    },

    /** Process-local machine pointer. */
    pointer(pointee: LocalNodeId, access: Access): Type {
        return { kind: "pointer", pointee, access };
    },

    /** Slice into memory, a repeated element with explicit kind and access. */
    slice(
        kind: ReferenceKind,
        lifetime: Lifetime,
        element: LocalNodeId,
        storage: Storage,
        access: Access,
    ): Type {
        return { kind: "slice", kindValue: kind, lifetime, element, storage, access };
    },

    /** Linear token for one possibly uninitialized storage. */
    uninit(value: LocalNodeId): Type {
        return { kind: "uninit", value };
    },

    /** Owned storage whose automatic drop is suppressed. */
    manuallyDrop(value: LocalNodeId): Type {
        return { kind: "manuallyDrop", value };
    },

    /** Fixed array: `[T; N]`. */
    fixedArray(element: LocalNodeId, length: StaticId): Type {
        return { kind: "fixedArray", element, length };
    },

    /** Tuple: `(T1, T2, ...)`. */
    tuple(elements: ReadonlyArray<LocalNodeId>): Type {
        return { kind: "tuple", elements };
    },

    /** Struct. */
    struct(fields: ReadonlyArray<LocalNodeId>, copy: Copy): Type {
        return { kind: "struct", fields, copy };
    },

    /** Nominal newtype over one wrapped type. */
    newtype(inner: LocalNodeId, copy: Copy): Type {
        return { kind: "newtype", inner, copy };
    },

    /** Sum value with one logical discriminant and case payloads. */
    variant(discriminant: LocalNodeId, cases: ReadonlyArray<VariantCase>, copy: Copy): Type {
        return { kind: "variant", discriminant, cases, copy };
    },

    /** Fixed-width vector value. */
    vector(element: LocalNodeId, lanes: StaticId): Type {
        return { kind: "vector", element, lanes };
    },

    /** Bare function signature. */
    functionSignature(
        lifetimes: ReadonlyArray<LifetimeParameter>,
        parameters: ReadonlyArray<SignatureParameter>,
        result: LocalNodeId,
    ): Type {
        return { kind: "functionSignature", lifetimes, parameters, result };
    },

    /** Function value type. */
    function(
        signature: LocalNodeId,
        multiplicity: Multiplicity,
        kind: ReferenceKind,
        lifetime: Lifetime,
        storage: Storage,
        access: Access,
    ): Type {
        return {
            kind: "function",
            signature,
            multiplicity,
            kindValue: kind,
            lifetime,
            storage,
            access,
        };
    },

    /** Function pointer type. */
    functionPointer(signature: LocalNodeId): Type {
        return { kind: "functionPointer", signature };
    },

    /** Type use with applied generic arguments. */
    application(base: LocalNodeId, arguments_: ReadonlyArray<GenericArgument>): Type {
        return { kind: "application", base, arguments: arguments_ };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: Type): void {
        encodeType(writer, value);
    },

    /** Decode one Type. */
    decode(reader: BinaryReader): Type {
        return decodeType(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Type): Json {
        return toJsonType(value);
    },

    /** Return one Type from one JSON value. */
    fromJson(value: Json): Type {
        return fromJsonType(value);
    },
};

/** Encode one Type. */
export function encodeType(writer: BinaryWriter, value: Type): void {
    switch (value.kind) {
        case "error":
            writer.writeUnsigned(0);
            return;
        case "never":
            writer.writeUnsigned(1);
            return;
        case "void":
            writer.writeUnsigned(2);
            return;
        case "null":
            writer.writeUnsigned(3);
            return;
        case "boolean":
            writer.writeUnsigned(4);
            return;
        case "character":
            writer.writeUnsigned(5);
            return;
        case "int":
            writer.writeUnsigned(6);
            writer.writeUnsigned(value.width);
            writer.writeBool(value.isSigned);
            return;
        case "isize":
            writer.writeUnsigned(7);
            return;
        case "usize":
            writer.writeUnsigned(8);
            return;
        case "float":
            writer.writeUnsigned(9);
            encodeFloatType(writer, value.float);
            return;
        case "typeId":
            writer.writeUnsigned(10);
            return;
        case "parameter":
            writer.writeUnsigned(11);
            writer.writeUnsigned(value.index);
            return;
        case "atomic":
            writer.writeUnsigned(12);
            encodeLocalNodeId(writer, value.value);
            return;
        case "dynamic":
            writer.writeUnsigned(13);
            encodeReferenceKind(writer, value.kindValue);
            encodeLifetime(writer, value.lifetime);
            encodeLocalNodeId(writer, value.constraint);
            encodeStorage(writer, value.storage);
            encodeAccess(writer, value.access);
            return;
        case "reference":
            writer.writeUnsigned(14);
            encodeReferenceKind(writer, value.kindValue);
            encodeLifetime(writer, value.lifetime);
            encodeStorage(writer, value.storage);
            encodeAccess(writer, value.access);
            encodeLocalNodeId(writer, value.pointee);
            return;
        case "pointer":
            writer.writeUnsigned(15);
            encodeLocalNodeId(writer, value.pointee);
            encodeAccess(writer, value.access);
            return;
        case "slice":
            writer.writeUnsigned(16);
            encodeReferenceKind(writer, value.kindValue);
            encodeLifetime(writer, value.lifetime);
            encodeLocalNodeId(writer, value.element);
            encodeStorage(writer, value.storage);
            encodeAccess(writer, value.access);
            return;
        case "uninit":
            writer.writeUnsigned(17);
            encodeLocalNodeId(writer, value.value);
            return;
        case "manuallyDrop":
            writer.writeUnsigned(18);
            encodeLocalNodeId(writer, value.value);
            return;
        case "fixedArray":
            writer.writeUnsigned(19);
            encodeLocalNodeId(writer, value.element);
            encodeStaticId(writer, value.length);
            return;
        case "tuple":
            writer.writeUnsigned(20);
            writer.writeUnsigned(value.elements.length);
            for (const item0 of value.elements) {
                encodeLocalNodeId(writer, item0);
            }
            return;
        case "struct":
            writer.writeUnsigned(21);
            writer.writeUnsigned(value.fields.length);
            for (const item0 of value.fields) {
                encodeLocalNodeId(writer, item0);
            }
            encodeCopy(writer, value.copy);
            return;
        case "newtype":
            writer.writeUnsigned(22);
            encodeLocalNodeId(writer, value.inner);
            encodeCopy(writer, value.copy);
            return;
        case "variant":
            writer.writeUnsigned(23);
            encodeLocalNodeId(writer, value.discriminant);
            writer.writeUnsigned(value.cases.length);
            for (const item1 of value.cases) {
                encodeVariantCase(writer, item1);
            }
            encodeCopy(writer, value.copy);
            return;
        case "vector":
            writer.writeUnsigned(24);
            encodeLocalNodeId(writer, value.element);
            encodeStaticId(writer, value.lanes);
            return;
        case "functionSignature":
            writer.writeUnsigned(25);
            writer.writeUnsigned(value.lifetimes.length);
            for (const item0 of value.lifetimes) {
                encodeLifetimeParameter(writer, item0);
            }
            writer.writeUnsigned(value.parameters.length);
            for (const item1 of value.parameters) {
                encodeSignatureParameter(writer, item1);
            }
            encodeLocalNodeId(writer, value.result);
            return;
        case "function":
            writer.writeUnsigned(26);
            encodeLocalNodeId(writer, value.signature);
            encodeMultiplicity(writer, value.multiplicity);
            encodeReferenceKind(writer, value.kindValue);
            encodeLifetime(writer, value.lifetime);
            encodeStorage(writer, value.storage);
            encodeAccess(writer, value.access);
            return;
        case "functionPointer":
            writer.writeUnsigned(27);
            encodeLocalNodeId(writer, value.signature);
            return;
        case "application":
            writer.writeUnsigned(28);
            encodeLocalNodeId(writer, value.base);
            writer.writeUnsigned(value.arguments.length);
            for (const item1 of value.arguments) {
                encodeGenericArgument(writer, item1);
            }
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one Type. */
export function decodeType(reader: BinaryReader): Type {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            return { kind: "error" };
        }
        case 1: {
            return { kind: "never" };
        }
        case 2: {
            return { kind: "void" };
        }
        case 3: {
            return { kind: "null" };
        }
        case 4: {
            return { kind: "boolean" };
        }
        case 5: {
            return { kind: "character" };
        }
        case 6: {
            const width = reader.readNumber();
            const isSigned = reader.readBool();

            return {
                kind: "int",
                width,
                isSigned,
            };
        }
        case 7: {
            return { kind: "isize" };
        }
        case 8: {
            return { kind: "usize" };
        }
        case 9: {
            const float = decodeFloatType(reader);

            return { kind: "float", float };
        }
        case 10: {
            return { kind: "typeId" };
        }
        case 11: {
            const index = reader.readNumber();

            return {
                kind: "parameter",
                index,
            };
        }
        case 12: {
            const value = decodeLocalNodeId(reader);

            return {
                kind: "atomic",
                value,
            };
        }
        case 13: {
            const kindValue = decodeReferenceKind(reader);
            const lifetime = decodeLifetime(reader);
            const constraint = decodeLocalNodeId(reader);
            const storage = decodeStorage(reader);
            const access = decodeAccess(reader);

            return {
                kind: "dynamic",
                kindValue,
                lifetime,
                constraint,
                storage,
                access,
            };
        }
        case 14: {
            const kindValue = decodeReferenceKind(reader);
            const lifetime = decodeLifetime(reader);
            const storage = decodeStorage(reader);
            const access = decodeAccess(reader);
            const pointee = decodeLocalNodeId(reader);

            return {
                kind: "reference",
                kindValue,
                lifetime,
                storage,
                access,
                pointee,
            };
        }
        case 15: {
            const pointee = decodeLocalNodeId(reader);
            const access = decodeAccess(reader);

            return {
                kind: "pointer",
                pointee,
                access,
            };
        }
        case 16: {
            const kindValue = decodeReferenceKind(reader);
            const lifetime = decodeLifetime(reader);
            const element = decodeLocalNodeId(reader);
            const storage = decodeStorage(reader);
            const access = decodeAccess(reader);

            return {
                kind: "slice",
                kindValue,
                lifetime,
                element,
                storage,
                access,
            };
        }
        case 17: {
            const value = decodeLocalNodeId(reader);

            return {
                kind: "uninit",
                value,
            };
        }
        case 18: {
            const value = decodeLocalNodeId(reader);

            return {
                kind: "manuallyDrop",
                value,
            };
        }
        case 19: {
            const element = decodeLocalNodeId(reader);
            const length = decodeStaticId(reader);

            return {
                kind: "fixedArray",
                element,
                length,
            };
        }
        case 20: {
            const elements = (() => {
                const length0 = reader.readNumber();
                const items0: Array<LocalNodeId> = [];
                for (let index = 0; index < length0; index += 1) {
                    items0.push(decodeLocalNodeId(reader));
                }
                return items0;
            })();

            return {
                kind: "tuple",
                elements,
            };
        }
        case 21: {
            const fields = (() => {
                const length0 = reader.readNumber();
                const items0: Array<LocalNodeId> = [];
                for (let index = 0; index < length0; index += 1) {
                    items0.push(decodeLocalNodeId(reader));
                }
                return items0;
            })();
            const copy = decodeCopy(reader);

            return {
                kind: "struct",
                fields,
                copy,
            };
        }
        case 22: {
            const inner = decodeLocalNodeId(reader);
            const copy = decodeCopy(reader);

            return {
                kind: "newtype",
                inner,
                copy,
            };
        }
        case 23: {
            const discriminant = decodeLocalNodeId(reader);
            const cases = (() => {
                const length1 = reader.readNumber();
                const items1: Array<VariantCase> = [];
                for (let index = 0; index < length1; index += 1) {
                    items1.push(decodeVariantCase(reader));
                }
                return items1;
            })();
            const copy = decodeCopy(reader);

            return {
                kind: "variant",
                discriminant,
                cases,
                copy,
            };
        }
        case 24: {
            const element = decodeLocalNodeId(reader);
            const lanes = decodeStaticId(reader);

            return {
                kind: "vector",
                element,
                lanes,
            };
        }
        case 25: {
            const lifetimes = (() => {
                const length0 = reader.readNumber();
                const items0: Array<LifetimeParameter> = [];
                for (let index = 0; index < length0; index += 1) {
                    items0.push(decodeLifetimeParameter(reader));
                }
                return items0;
            })();
            const parameters = (() => {
                const length1 = reader.readNumber();
                const items1: Array<SignatureParameter> = [];
                for (let index = 0; index < length1; index += 1) {
                    items1.push(decodeSignatureParameter(reader));
                }
                return items1;
            })();
            const result = decodeLocalNodeId(reader);

            return {
                kind: "functionSignature",
                lifetimes,
                parameters,
                result,
            };
        }
        case 26: {
            const signature = decodeLocalNodeId(reader);
            const multiplicity = decodeMultiplicity(reader);
            const kindValue = decodeReferenceKind(reader);
            const lifetime = decodeLifetime(reader);
            const storage = decodeStorage(reader);
            const access = decodeAccess(reader);

            return {
                kind: "function",
                signature,
                multiplicity,
                kindValue,
                lifetime,
                storage,
                access,
            };
        }
        case 27: {
            const signature = decodeLocalNodeId(reader);

            return {
                kind: "functionPointer",
                signature,
            };
        }
        case 28: {
            const base = decodeLocalNodeId(reader);
            const arguments_ = (() => {
                const length1 = reader.readNumber();
                const items1: Array<GenericArgument> = [];
                for (let index = 0; index < length1; index += 1) {
                    items1.push(decodeGenericArgument(reader));
                }
                return items1;
            })();

            return {
                kind: "application",
                base,
                arguments: arguments_,
            };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one Type. */
export function toJsonType(value: Type): Json {
    switch (value.kind) {
        case "error":
            return {
                kind: "error",
            };
        case "never":
            return {
                kind: "never",
            };
        case "void":
            return {
                kind: "void",
            };
        case "null":
            return {
                kind: "null",
            };
        case "boolean":
            return {
                kind: "boolean",
            };
        case "character":
            return {
                kind: "character",
            };
        case "int":
            return {
                kind: "int",
                width: value.width,
                isSigned: value.isSigned,
            };
        case "isize":
            return {
                kind: "isize",
            };
        case "usize":
            return {
                kind: "usize",
            };
        case "float":
            return {
                kind: "float",
                float: toJsonFloatType(value.float),
            };
        case "typeId":
            return {
                kind: "typeId",
            };
        case "parameter":
            return {
                kind: "parameter",
                index: value.index,
            };
        case "atomic":
            return {
                kind: "atomic",
                value: toJsonLocalNodeId(value.value),
            };
        case "dynamic":
            return {
                kind: "dynamic",
                kindValue: toJsonReferenceKind(value.kindValue),
                lifetime: toJsonLifetime(value.lifetime),
                constraint: toJsonLocalNodeId(value.constraint),
                storage: toJsonStorage(value.storage),
                access: toJsonAccess(value.access),
            };
        case "reference":
            return {
                kind: "reference",
                kindValue: toJsonReferenceKind(value.kindValue),
                lifetime: toJsonLifetime(value.lifetime),
                storage: toJsonStorage(value.storage),
                access: toJsonAccess(value.access),
                pointee: toJsonLocalNodeId(value.pointee),
            };
        case "pointer":
            return {
                kind: "pointer",
                pointee: toJsonLocalNodeId(value.pointee),
                access: toJsonAccess(value.access),
            };
        case "slice":
            return {
                kind: "slice",
                kindValue: toJsonReferenceKind(value.kindValue),
                lifetime: toJsonLifetime(value.lifetime),
                element: toJsonLocalNodeId(value.element),
                storage: toJsonStorage(value.storage),
                access: toJsonAccess(value.access),
            };
        case "uninit":
            return {
                kind: "uninit",
                value: toJsonLocalNodeId(value.value),
            };
        case "manuallyDrop":
            return {
                kind: "manuallyDrop",
                value: toJsonLocalNodeId(value.value),
            };
        case "fixedArray":
            return {
                kind: "fixedArray",
                element: toJsonLocalNodeId(value.element),
                length: toJsonStaticId(value.length),
            };
        case "tuple":
            return {
                kind: "tuple",
                elements: value.elements.map((item0) => toJsonLocalNodeId(item0)),
            };
        case "struct":
            return {
                kind: "struct",
                fields: value.fields.map((item0) => toJsonLocalNodeId(item0)),
                copy: toJsonCopy(value.copy),
            };
        case "newtype":
            return {
                kind: "newtype",
                inner: toJsonLocalNodeId(value.inner),
                copy: toJsonCopy(value.copy),
            };
        case "variant":
            return {
                kind: "variant",
                discriminant: toJsonLocalNodeId(value.discriminant),
                cases: value.cases.map((item0) => toJsonVariantCase(item0)),
                copy: toJsonCopy(value.copy),
            };
        case "vector":
            return {
                kind: "vector",
                element: toJsonLocalNodeId(value.element),
                lanes: toJsonStaticId(value.lanes),
            };
        case "functionSignature":
            return {
                kind: "functionSignature",
                lifetimes: value.lifetimes.map((item0) => toJsonLifetimeParameter(item0)),
                parameters: value.parameters.map((item0) => toJsonSignatureParameter(item0)),
                result: toJsonLocalNodeId(value.result),
            };
        case "function":
            return {
                kind: "function",
                signature: toJsonLocalNodeId(value.signature),
                multiplicity: toJsonMultiplicity(value.multiplicity),
                kindValue: toJsonReferenceKind(value.kindValue),
                lifetime: toJsonLifetime(value.lifetime),
                storage: toJsonStorage(value.storage),
                access: toJsonAccess(value.access),
            };
        case "functionPointer":
            return {
                kind: "functionPointer",
                signature: toJsonLocalNodeId(value.signature),
            };
        case "application":
            return {
                kind: "application",
                base: toJsonLocalNodeId(value.base),
                arguments: value.arguments.map((item0) => toJsonGenericArgument(item0)),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one Type from one JSON value. */
export function fromJsonType(value: Json): Type {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "error":
            return {
                kind,
            };
        case "never":
            return {
                kind,
            };
        case "void":
            return {
                kind,
            };
        case "null":
            return {
                kind,
            };
        case "boolean":
            return {
                kind,
            };
        case "character":
            return {
                kind,
            };
        case "int":
            return {
                kind,
                width: jsonInteger(jsonField(object, "width")),
                isSigned: jsonBool(jsonField(object, "isSigned")),
            };
        case "isize":
            return {
                kind,
            };
        case "usize":
            return {
                kind,
            };
        case "float":
            return {
                kind,
                float: fromJsonFloatType(jsonField(object, "float")),
            };
        case "typeId":
            return {
                kind,
            };
        case "parameter":
            return {
                kind,
                index: jsonInteger(jsonField(object, "index")),
            };
        case "atomic":
            return {
                kind,
                value: fromJsonLocalNodeId(jsonField(object, "value")),
            };
        case "dynamic":
            return {
                kind,
                kindValue: fromJsonReferenceKind(jsonField(object, "kindValue")),
                lifetime: fromJsonLifetime(jsonField(object, "lifetime")),
                constraint: fromJsonLocalNodeId(jsonField(object, "constraint")),
                storage: fromJsonStorage(jsonField(object, "storage")),
                access: fromJsonAccess(jsonField(object, "access")),
            };
        case "reference":
            return {
                kind,
                kindValue: fromJsonReferenceKind(jsonField(object, "kindValue")),
                lifetime: fromJsonLifetime(jsonField(object, "lifetime")),
                storage: fromJsonStorage(jsonField(object, "storage")),
                access: fromJsonAccess(jsonField(object, "access")),
                pointee: fromJsonLocalNodeId(jsonField(object, "pointee")),
            };
        case "pointer":
            return {
                kind,
                pointee: fromJsonLocalNodeId(jsonField(object, "pointee")),
                access: fromJsonAccess(jsonField(object, "access")),
            };
        case "slice":
            return {
                kind,
                kindValue: fromJsonReferenceKind(jsonField(object, "kindValue")),
                lifetime: fromJsonLifetime(jsonField(object, "lifetime")),
                element: fromJsonLocalNodeId(jsonField(object, "element")),
                storage: fromJsonStorage(jsonField(object, "storage")),
                access: fromJsonAccess(jsonField(object, "access")),
            };
        case "uninit":
            return {
                kind,
                value: fromJsonLocalNodeId(jsonField(object, "value")),
            };
        case "manuallyDrop":
            return {
                kind,
                value: fromJsonLocalNodeId(jsonField(object, "value")),
            };
        case "fixedArray":
            return {
                kind,
                element: fromJsonLocalNodeId(jsonField(object, "element")),
                length: fromJsonStaticId(jsonField(object, "length")),
            };
        case "tuple":
            return {
                kind,
                elements: jsonArray(jsonField(object, "elements")).map((item0) =>
                    fromJsonLocalNodeId(item0),
                ),
            };
        case "struct":
            return {
                kind,
                fields: jsonArray(jsonField(object, "fields")).map((item0) =>
                    fromJsonLocalNodeId(item0),
                ),
                copy: fromJsonCopy(jsonField(object, "copy")),
            };
        case "newtype":
            return {
                kind,
                inner: fromJsonLocalNodeId(jsonField(object, "inner")),
                copy: fromJsonCopy(jsonField(object, "copy")),
            };
        case "variant":
            return {
                kind,
                discriminant: fromJsonLocalNodeId(jsonField(object, "discriminant")),
                cases: jsonArray(jsonField(object, "cases")).map((item0) =>
                    fromJsonVariantCase(item0),
                ),
                copy: fromJsonCopy(jsonField(object, "copy")),
            };
        case "vector":
            return {
                kind,
                element: fromJsonLocalNodeId(jsonField(object, "element")),
                lanes: fromJsonStaticId(jsonField(object, "lanes")),
            };
        case "functionSignature":
            return {
                kind,
                lifetimes: jsonArray(jsonField(object, "lifetimes")).map((item0) =>
                    fromJsonLifetimeParameter(item0),
                ),
                parameters: jsonArray(jsonField(object, "parameters")).map((item0) =>
                    fromJsonSignatureParameter(item0),
                ),
                result: fromJsonLocalNodeId(jsonField(object, "result")),
            };
        case "function":
            return {
                kind,
                signature: fromJsonLocalNodeId(jsonField(object, "signature")),
                multiplicity: fromJsonMultiplicity(jsonField(object, "multiplicity")),
                kindValue: fromJsonReferenceKind(jsonField(object, "kindValue")),
                lifetime: fromJsonLifetime(jsonField(object, "lifetime")),
                storage: fromJsonStorage(jsonField(object, "storage")),
                access: fromJsonAccess(jsonField(object, "access")),
            };
        case "functionPointer":
            return {
                kind,
                signature: fromJsonLocalNodeId(jsonField(object, "signature")),
            };
        case "application":
            return {
                kind,
                base: fromJsonLocalNodeId(jsonField(object, "base")),
                arguments: jsonArray(jsonField(object, "arguments")).map((item0) =>
                    fromJsonGenericArgument(item0),
                ),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** A named MIR type declaration. */
export type TypeDeclaration = {
    /** The declaration name. */
    readonly name: StringId;
    /** The generic parameters a template takes, regions among them. */
    readonly generics: ReadonlyArray<GenericParameter>;
    /** The identified type. */
    readonly ty: LocalNodeId;
    /** The directly inherited and implemented types. */
    readonly heritage: TypeHeritage;
};

export const TypeDeclaration = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: TypeDeclaration): void {
        encodeTypeDeclaration(writer, value);
    },

    /** Decode one TypeDeclaration. */
    decode(reader: BinaryReader): TypeDeclaration {
        return decodeTypeDeclaration(reader);
    },

    /** Return this value as JSON. */
    toJson(value: TypeDeclaration): Json {
        return toJsonTypeDeclaration(value);
    },

    /** Return one TypeDeclaration from one JSON value. */
    fromJson(value: Json): TypeDeclaration {
        return fromJsonTypeDeclaration(value);
    },
};

/** Encode one TypeDeclaration. */
export function encodeTypeDeclaration(writer: BinaryWriter, value: TypeDeclaration): void {
    encodeStringId(writer, value.name);
    writer.writeUnsigned(value.generics.length);
    for (const item1 of value.generics) {
        encodeGenericParameter(writer, item1);
    }
    encodeLocalNodeId(writer, value.ty);
    encodeTypeHeritage(writer, value.heritage);
}

/** Decode one TypeDeclaration. */
export function decodeTypeDeclaration(reader: BinaryReader): TypeDeclaration {
    const name = decodeStringId(reader);
    const generics = (() => {
        const length1 = reader.readNumber();
        const items1: Array<GenericParameter> = [];
        for (let index = 0; index < length1; index += 1) {
            items1.push(decodeGenericParameter(reader));
        }
        return items1;
    })();
    const ty = decodeLocalNodeId(reader);
    const heritage = decodeTypeHeritage(reader);

    return {
        name,
        generics,
        ty,
        heritage,
    };
}

/** Return one JSON value for one TypeDeclaration. */
export function toJsonTypeDeclaration(value: TypeDeclaration): Json {
    return {
        name: toJsonStringId(value.name),
        generics: value.generics.map((item0) => toJsonGenericParameter(item0)),
        ty: toJsonLocalNodeId(value.ty),
        heritage: toJsonTypeHeritage(value.heritage),
    };
}

/** Return one TypeDeclaration from one JSON value. */
export function fromJsonTypeDeclaration(value: Json): TypeDeclaration {
    const object = jsonObject(value);

    return {
        name: fromJsonStringId(jsonField(object, "name")),
        generics: jsonArray(jsonField(object, "generics")).map((item0) =>
            fromJsonGenericParameter(item0),
        ),
        ty: fromJsonLocalNodeId(jsonField(object, "ty")),
        heritage: fromJsonTypeHeritage(jsonField(object, "heritage")),
    };
}

/** Stable canonical fingerprint of one MIR type. */
export type TypeFingerprint = bigint;

export const TypeFingerprint = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: TypeFingerprint): void {
        encodeTypeFingerprint(writer, value);
    },

    /** Decode one TypeFingerprint. */
    decode(reader: BinaryReader): TypeFingerprint {
        return decodeTypeFingerprint(reader);
    },

    /** Return this value as JSON. */
    toJson(value: TypeFingerprint): Json {
        return toJsonTypeFingerprint(value);
    },

    /** Return one TypeFingerprint from one JSON value. */
    fromJson(value: Json): TypeFingerprint {
        return fromJsonTypeFingerprint(value);
    },
};

/** Encode one TypeFingerprint. */
export function encodeTypeFingerprint(writer: BinaryWriter, value: TypeFingerprint): void {
    writer.writeUnsigned(value);
}

/** Decode one TypeFingerprint. */
export function decodeTypeFingerprint(reader: BinaryReader): TypeFingerprint {
    return reader.readUnsigned();
}

/** Return one JSON value for one TypeFingerprint. */
export function toJsonTypeFingerprint(value: TypeFingerprint): Json {
    return value.toString();
}

/** Return one TypeFingerprint from one JSON value. */
export function fromJsonTypeFingerprint(value: Json): TypeFingerprint {
    return jsonBigint(value);
}

/** Direct heritage for one nominal type. */
export type TypeHeritage = {
    /** Directly inherited types. */
    readonly extends: ReadonlyArray<LocalNodeId>;
    /** Directly implemented interfaces. */
    readonly implements: ReadonlyArray<LocalNodeId>;
};

export const TypeHeritage = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: TypeHeritage): void {
        encodeTypeHeritage(writer, value);
    },

    /** Decode one TypeHeritage. */
    decode(reader: BinaryReader): TypeHeritage {
        return decodeTypeHeritage(reader);
    },

    /** Return this value as JSON. */
    toJson(value: TypeHeritage): Json {
        return toJsonTypeHeritage(value);
    },

    /** Return one TypeHeritage from one JSON value. */
    fromJson(value: Json): TypeHeritage {
        return fromJsonTypeHeritage(value);
    },
};

/** Encode one TypeHeritage. */
export function encodeTypeHeritage(writer: BinaryWriter, value: TypeHeritage): void {
    writer.writeUnsigned(value.extends.length);
    for (const item0 of value.extends) {
        encodeLocalNodeId(writer, item0);
    }
    writer.writeUnsigned(value.implements.length);
    for (const item1 of value.implements) {
        encodeLocalNodeId(writer, item1);
    }
}

/** Decode one TypeHeritage. */
export function decodeTypeHeritage(reader: BinaryReader): TypeHeritage {
    const extends_ = (() => {
        const length0 = reader.readNumber();
        const items0: Array<LocalNodeId> = [];
        for (let index = 0; index < length0; index += 1) {
            items0.push(decodeLocalNodeId(reader));
        }
        return items0;
    })();
    const implements_ = (() => {
        const length1 = reader.readNumber();
        const items1: Array<LocalNodeId> = [];
        for (let index = 0; index < length1; index += 1) {
            items1.push(decodeLocalNodeId(reader));
        }
        return items1;
    })();

    return {
        extends: extends_,
        implements: implements_,
    };
}

/** Return one JSON value for one TypeHeritage. */
export function toJsonTypeHeritage(value: TypeHeritage): Json {
    return {
        extends: value.extends.map((item0) => toJsonLocalNodeId(item0)),
        implements: value.implements.map((item0) => toJsonLocalNodeId(item0)),
    };
}

/** Return one TypeHeritage from one JSON value. */
export function fromJsonTypeHeritage(value: Json): TypeHeritage {
    const object = jsonObject(value);

    return {
        extends: jsonArray(jsonField(object, "extends")).map((item0) => fromJsonLocalNodeId(item0)),
        implements: jsonArray(jsonField(object, "implements")).map((item0) =>
            fromJsonLocalNodeId(item0),
        ),
    };
}

/** One sum case. */
export type VariantCase = {
    /** The discriminant constant selecting this case. */
    readonly discriminant: Constant;
    /** The stored payload type, the unique box of a boxed case. */
    readonly ty: LocalNodeId;
    /** Whether the case stores its payload behind a unique box it owns. */
    readonly isBoxed: boolean;
};

export const VariantCase = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: VariantCase): void {
        encodeVariantCase(writer, value);
    },

    /** Decode one VariantCase. */
    decode(reader: BinaryReader): VariantCase {
        return decodeVariantCase(reader);
    },

    /** Return this value as JSON. */
    toJson(value: VariantCase): Json {
        return toJsonVariantCase(value);
    },

    /** Return one VariantCase from one JSON value. */
    fromJson(value: Json): VariantCase {
        return fromJsonVariantCase(value);
    },
};

/** Encode one VariantCase. */
export function encodeVariantCase(writer: BinaryWriter, value: VariantCase): void {
    encodeConstant(writer, value.discriminant);
    encodeLocalNodeId(writer, value.ty);
    writer.writeBool(value.isBoxed);
}

/** Decode one VariantCase. */
export function decodeVariantCase(reader: BinaryReader): VariantCase {
    const discriminant = decodeConstant(reader);
    const ty = decodeLocalNodeId(reader);
    const isBoxed = reader.readBool();

    return {
        discriminant,
        ty,
        isBoxed,
    };
}

/** Return one JSON value for one VariantCase. */
export function toJsonVariantCase(value: VariantCase): Json {
    return {
        discriminant: toJsonConstant(value.discriminant),
        ty: toJsonLocalNodeId(value.ty),
        isBoxed: value.isBoxed,
    };
}

/** Return one VariantCase from one JSON value. */
export function fromJsonVariantCase(value: Json): VariantCase {
    const object = jsonObject(value);

    return {
        discriminant: fromJsonConstant(jsonField(object, "discriminant")),
        ty: fromJsonLocalNodeId(jsonField(object, "ty")),
        isBoxed: jsonBool(jsonField(object, "isBoxed")),
    };
}
