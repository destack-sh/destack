/* generated by destack_client_generate, do not edit */

import {
    BinaryReader,
    BinaryWriter,
    Json,
    SerdeError,
    jsonField,
    jsonInteger,
    jsonObject,
    jsonString,
} from "../../../protocol/serde.js";

/** Atomic ordering and scope for one memory operation. */
export type AtomicAccess = {
    /** The memory ordering. */
    readonly ordering: MemoryOrdering;
    /** The execution scope. */
    readonly scope: ExecutionScope;
};

export const AtomicAccess = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: AtomicAccess): void {
        encodeAtomicAccess(writer, value);
    },

    /** Decode one AtomicAccess. */
    decode(reader: BinaryReader): AtomicAccess {
        return decodeAtomicAccess(reader);
    },

    /** Return this value as JSON. */
    toJson(value: AtomicAccess): Json {
        return toJsonAtomicAccess(value);
    },

    /** Return one AtomicAccess from one JSON value. */
    fromJson(value: Json): AtomicAccess {
        return fromJsonAtomicAccess(value);
    },
};

/** Encode one AtomicAccess. */
export function encodeAtomicAccess(writer: BinaryWriter, value: AtomicAccess): void {
    encodeMemoryOrdering(writer, value.ordering);
    encodeExecutionScope(writer, value.scope);
}

/** Decode one AtomicAccess. */
export function decodeAtomicAccess(reader: BinaryReader): AtomicAccess {
    const ordering = decodeMemoryOrdering(reader);
    const scope = decodeExecutionScope(reader);

    return {
        ordering,
        scope,
    };
}

/** Return one JSON value for one AtomicAccess. */
export function toJsonAtomicAccess(value: AtomicAccess): Json {
    return {
        ordering: toJsonMemoryOrdering(value.ordering),
        scope: toJsonExecutionScope(value.scope),
    };
}

/** Return one AtomicAccess from one JSON value. */
export function fromJsonAtomicAccess(value: Json): AtomicAccess {
    const object = jsonObject(value);

    return {
        ordering: fromJsonMemoryOrdering(jsonField(object, "ordering")),
        scope: fromJsonExecutionScope(jsonField(object, "scope")),
    };
}

/** Read-modify-write operator for atomic memory operations. */
export type AtomicRmwOperator =
    | "exchange"
    | "add"
    | "subtract"
    | "and"
    | "or"
    | "xor"
    | "min"
    | "max";

export const AtomicRmwOperator = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: AtomicRmwOperator): void {
        encodeAtomicRmwOperator(writer, value);
    },

    /** Decode one AtomicRmwOperator. */
    decode(reader: BinaryReader): AtomicRmwOperator {
        return decodeAtomicRmwOperator(reader);
    },

    /** Return this value as JSON. */
    toJson(value: AtomicRmwOperator): Json {
        return toJsonAtomicRmwOperator(value);
    },

    /** Return one AtomicRmwOperator from one JSON value. */
    fromJson(value: Json): AtomicRmwOperator {
        return fromJsonAtomicRmwOperator(value);
    },
};

/** Encode one AtomicRmwOperator. */
export function encodeAtomicRmwOperator(writer: BinaryWriter, value: AtomicRmwOperator): void {
    switch (value) {
        case "exchange":
            writer.writeUnsigned(0);
            return;
        case "add":
            writer.writeUnsigned(1);
            return;
        case "subtract":
            writer.writeUnsigned(2);
            return;
        case "and":
            writer.writeUnsigned(3);
            return;
        case "or":
            writer.writeUnsigned(4);
            return;
        case "xor":
            writer.writeUnsigned(5);
            return;
        case "min":
            writer.writeUnsigned(6);
            return;
        case "max":
            writer.writeUnsigned(7);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one AtomicRmwOperator. */
export function decodeAtomicRmwOperator(reader: BinaryReader): AtomicRmwOperator {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "exchange";
        case 1:
            return "add";
        case 2:
            return "subtract";
        case 3:
            return "and";
        case 4:
            return "or";
        case 5:
            return "xor";
        case 6:
            return "min";
        case 7:
            return "max";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one AtomicRmwOperator. */
export function toJsonAtomicRmwOperator(value: AtomicRmwOperator): Json {
    return value;
}

/** Return one AtomicRmwOperator from one JSON value. */
export function fromJsonAtomicRmwOperator(value: Json): AtomicRmwOperator {
    const variant = jsonString(value);

    switch (variant) {
        case "exchange":
            return "exchange";
        case "add":
            return "add";
        case "subtract":
            return "subtract";
        case "and":
            return "and";
        case "or":
            return "or";
        case "xor":
            return "xor";
        case "min":
            return "min";
        case "max":
            return "max";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** Access for one compare exchange operation. */
export type CompareExchangeAccess = {
    /** Access used when the comparison succeeds. */
    readonly success: AtomicAccess;
    /** Ordering used by the failed comparison load. */
    readonly failureOrdering: MemoryOrdering;
};

export const CompareExchangeAccess = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: CompareExchangeAccess): void {
        encodeCompareExchangeAccess(writer, value);
    },

    /** Decode one CompareExchangeAccess. */
    decode(reader: BinaryReader): CompareExchangeAccess {
        return decodeCompareExchangeAccess(reader);
    },

    /** Return this value as JSON. */
    toJson(value: CompareExchangeAccess): Json {
        return toJsonCompareExchangeAccess(value);
    },

    /** Return one CompareExchangeAccess from one JSON value. */
    fromJson(value: Json): CompareExchangeAccess {
        return fromJsonCompareExchangeAccess(value);
    },
};

/** Encode one CompareExchangeAccess. */
export function encodeCompareExchangeAccess(
    writer: BinaryWriter,
    value: CompareExchangeAccess,
): void {
    encodeAtomicAccess(writer, value.success);
    encodeMemoryOrdering(writer, value.failureOrdering);
}

/** Decode one CompareExchangeAccess. */
export function decodeCompareExchangeAccess(reader: BinaryReader): CompareExchangeAccess {
    const success = decodeAtomicAccess(reader);
    const failureOrdering = decodeMemoryOrdering(reader);

    return {
        success,
        failureOrdering,
    };
}

/** Return one JSON value for one CompareExchangeAccess. */
export function toJsonCompareExchangeAccess(value: CompareExchangeAccess): Json {
    return {
        success: toJsonAtomicAccess(value.success),
        failureOrdering: toJsonMemoryOrdering(value.failureOrdering),
    };
}

/** Return one CompareExchangeAccess from one JSON value. */
export function fromJsonCompareExchangeAccess(value: Json): CompareExchangeAccess {
    const object = jsonObject(value);

    return {
        success: fromJsonAtomicAccess(jsonField(object, "success")),
        failureOrdering: fromJsonMemoryOrdering(jsonField(object, "failureOrdering")),
    };
}

/** Execution scope for atomic operations and fences. */
export type ExecutionScope = "invocation" | "subgroup" | "workgroup" | "device" | "system";

export const ExecutionScope = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ExecutionScope): void {
        encodeExecutionScope(writer, value);
    },

    /** Decode one ExecutionScope. */
    decode(reader: BinaryReader): ExecutionScope {
        return decodeExecutionScope(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ExecutionScope): Json {
        return toJsonExecutionScope(value);
    },

    /** Return one ExecutionScope from one JSON value. */
    fromJson(value: Json): ExecutionScope {
        return fromJsonExecutionScope(value);
    },
};

/** Encode one ExecutionScope. */
export function encodeExecutionScope(writer: BinaryWriter, value: ExecutionScope): void {
    switch (value) {
        case "invocation":
            writer.writeUnsigned(0);
            return;
        case "subgroup":
            writer.writeUnsigned(1);
            return;
        case "workgroup":
            writer.writeUnsigned(2);
            return;
        case "device":
            writer.writeUnsigned(3);
            return;
        case "system":
            writer.writeUnsigned(4);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one ExecutionScope. */
export function decodeExecutionScope(reader: BinaryReader): ExecutionScope {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "invocation";
        case 1:
            return "subgroup";
        case 2:
            return "workgroup";
        case 3:
            return "device";
        case 4:
            return "system";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one ExecutionScope. */
export function toJsonExecutionScope(value: ExecutionScope): Json {
    return value;
}

/** Return one ExecutionScope from one JSON value. */
export function fromJsonExecutionScope(value: Json): ExecutionScope {
    const variant = jsonString(value);

    switch (variant) {
        case "invocation":
            return "invocation";
        case "subgroup":
            return "subgroup";
        case "workgroup":
            return "workgroup";
        case "device":
            return "device";
        case "system":
            return "system";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** Fence ordering, scope, and storage. */
export type FenceAccess = {
    /** The memory ordering. */
    readonly ordering: MemoryOrdering;
    /** The execution scope. */
    readonly scope: ExecutionScope;
    /** The ordered storage regions. */
    readonly storage: StorageSet;
};

export const FenceAccess = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: FenceAccess): void {
        encodeFenceAccess(writer, value);
    },

    /** Decode one FenceAccess. */
    decode(reader: BinaryReader): FenceAccess {
        return decodeFenceAccess(reader);
    },

    /** Return this value as JSON. */
    toJson(value: FenceAccess): Json {
        return toJsonFenceAccess(value);
    },

    /** Return one FenceAccess from one JSON value. */
    fromJson(value: Json): FenceAccess {
        return fromJsonFenceAccess(value);
    },
};

/** Encode one FenceAccess. */
export function encodeFenceAccess(writer: BinaryWriter, value: FenceAccess): void {
    encodeMemoryOrdering(writer, value.ordering);
    encodeExecutionScope(writer, value.scope);
    encodeStorageSet(writer, value.storage);
}

/** Decode one FenceAccess. */
export function decodeFenceAccess(reader: BinaryReader): FenceAccess {
    const ordering = decodeMemoryOrdering(reader);
    const scope = decodeExecutionScope(reader);
    const storage = decodeStorageSet(reader);

    return {
        ordering,
        scope,
        storage,
    };
}

/** Return one JSON value for one FenceAccess. */
export function toJsonFenceAccess(value: FenceAccess): Json {
    return {
        ordering: toJsonMemoryOrdering(value.ordering),
        scope: toJsonExecutionScope(value.scope),
        storage: toJsonStorageSet(value.storage),
    };
}

/** Return one FenceAccess from one JSON value. */
export function fromJsonFenceAccess(value: Json): FenceAccess {
    const object = jsonObject(value);

    return {
        ordering: fromJsonMemoryOrdering(jsonField(object, "ordering")),
        scope: fromJsonExecutionScope(jsonField(object, "scope")),
        storage: fromJsonStorageSet(jsonField(object, "storage")),
    };
}

/** Memory ordering for atomic operations. */
export type MemoryOrdering =
    | "relaxed"
    | "acquire"
    | "release"
    | "acquireRelease"
    | "sequentiallyConsistent";

export const MemoryOrdering = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: MemoryOrdering): void {
        encodeMemoryOrdering(writer, value);
    },

    /** Decode one MemoryOrdering. */
    decode(reader: BinaryReader): MemoryOrdering {
        return decodeMemoryOrdering(reader);
    },

    /** Return this value as JSON. */
    toJson(value: MemoryOrdering): Json {
        return toJsonMemoryOrdering(value);
    },

    /** Return one MemoryOrdering from one JSON value. */
    fromJson(value: Json): MemoryOrdering {
        return fromJsonMemoryOrdering(value);
    },
};

/** Encode one MemoryOrdering. */
export function encodeMemoryOrdering(writer: BinaryWriter, value: MemoryOrdering): void {
    switch (value) {
        case "relaxed":
            writer.writeUnsigned(0);
            return;
        case "acquire":
            writer.writeUnsigned(1);
            return;
        case "release":
            writer.writeUnsigned(2);
            return;
        case "acquireRelease":
            writer.writeUnsigned(3);
            return;
        case "sequentiallyConsistent":
            writer.writeUnsigned(4);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one MemoryOrdering. */
export function decodeMemoryOrdering(reader: BinaryReader): MemoryOrdering {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "relaxed";
        case 1:
            return "acquire";
        case 2:
            return "release";
        case 3:
            return "acquireRelease";
        case 4:
            return "sequentiallyConsistent";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one MemoryOrdering. */
export function toJsonMemoryOrdering(value: MemoryOrdering): Json {
    return value;
}

/** Return one MemoryOrdering from one JSON value. */
export function fromJsonMemoryOrdering(value: Json): MemoryOrdering {
    const variant = jsonString(value);

    switch (variant) {
        case "relaxed":
            return "relaxed";
        case "acquire":
            return "acquire";
        case "release":
            return "release";
        case "acquireRelease":
            return "acquireRelease";
        case "sequentiallyConsistent":
            return "sequentiallyConsistent";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** Set of backing storage regions that an operation may access. */
export type StorageSet = number;

export const StorageSet = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: StorageSet): void {
        encodeStorageSet(writer, value);
    },

    /** Decode one StorageSet. */
    decode(reader: BinaryReader): StorageSet {
        return decodeStorageSet(reader);
    },

    /** Return this value as JSON. */
    toJson(value: StorageSet): Json {
        return toJsonStorageSet(value);
    },

    /** Return one StorageSet from one JSON value. */
    fromJson(value: Json): StorageSet {
        return fromJsonStorageSet(value);
    },
};

/** Encode one StorageSet. */
export function encodeStorageSet(writer: BinaryWriter, value: StorageSet): void {
    writer.writeUnsigned(value);
}

/** Decode one StorageSet. */
export function decodeStorageSet(reader: BinaryReader): StorageSet {
    return reader.readNumber();
}

/** Return one JSON value for one StorageSet. */
export function toJsonStorageSet(value: StorageSet): Json {
    return value;
}

/** Return one StorageSet from one JSON value. */
export function fromJsonStorageSet(value: Json): StorageSet {
    return jsonInteger(value);
}
