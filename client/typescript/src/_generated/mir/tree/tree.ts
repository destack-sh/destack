/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, bytesFromJson, bytesToJson, compareBytes, jsonArray, jsonBigint, jsonField, jsonInteger, jsonObject, jsonOptional, jsonString, nestedBytes } from "../../../protocol/serde.js";
import type { Token } from "../source/token.js";
import type { Attribute } from "./attribute.js";
import type { Block } from "./block.js";
import type { Function } from "./function.js";
import type { Global } from "./global.js";
import type { TensorImmediate } from "./immediate.js";
import type { Instruction } from "./instruction.js";
import type { LifetimeParameter } from "./lifetime.js";
import type { Local } from "./local.js";
import type { LocalNodeId } from "./node.js";
import type { OriginTable } from "./origin.js";
import type { Terminator } from "./terminator.js";
import type { SwitchCase } from "./terminator.js";
import type { FieldSpan } from "./trivia.js";
import type { FunctionHeaderSpans } from "./trivia.js";
import type { TypeDeclarationSpans } from "./trivia.js";
import type { TypedValueSpan } from "./trivia.js";
import type { Type } from "./type.js";
import type { TypeAlias } from "./type.js";
import type { Field } from "./type.js";
import type { Value } from "./value.js";
import type { Span } from "../../source/file/model/span.js";
import type { SourceIndexData } from "../../source/tree/index.js";
import { decodeToken, encodeToken, fromJsonToken, toJsonToken } from "../source/token.js";
import { decodeAttribute, encodeAttribute, fromJsonAttribute, toJsonAttribute } from "./attribute.js";
import { decodeBlock, encodeBlock, fromJsonBlock, toJsonBlock } from "./block.js";
import { decodeFunction, encodeFunction, fromJsonFunction, toJsonFunction } from "./function.js";
import { decodeGlobal, encodeGlobal, fromJsonGlobal, toJsonGlobal } from "./global.js";
import { decodeTensorImmediate, encodeTensorImmediate, fromJsonTensorImmediate, toJsonTensorImmediate } from "./immediate.js";
import { decodeInstruction, encodeInstruction, fromJsonInstruction, toJsonInstruction } from "./instruction.js";
import { decodeLifetimeParameter, encodeLifetimeParameter, fromJsonLifetimeParameter, toJsonLifetimeParameter } from "./lifetime.js";
import { decodeLocal, encodeLocal, fromJsonLocal, toJsonLocal } from "./local.js";
import { decodeLocalNodeId, encodeLocalNodeId, fromJsonLocalNodeId, toJsonLocalNodeId } from "./node.js";
import { decodeOriginTable, encodeOriginTable, fromJsonOriginTable, toJsonOriginTable } from "./origin.js";
import { decodeTerminator, encodeTerminator, fromJsonTerminator, toJsonTerminator } from "./terminator.js";
import { decodeSwitchCase, encodeSwitchCase, fromJsonSwitchCase, toJsonSwitchCase } from "./terminator.js";
import { decodeFieldSpan, encodeFieldSpan, fromJsonFieldSpan, toJsonFieldSpan } from "./trivia.js";
import { decodeFunctionHeaderSpans, encodeFunctionHeaderSpans, fromJsonFunctionHeaderSpans, toJsonFunctionHeaderSpans } from "./trivia.js";
import { decodeTypeDeclarationSpans, encodeTypeDeclarationSpans, fromJsonTypeDeclarationSpans, toJsonTypeDeclarationSpans } from "./trivia.js";
import { decodeTypedValueSpan, encodeTypedValueSpan, fromJsonTypedValueSpan, toJsonTypedValueSpan } from "./trivia.js";
import { decodeType, encodeType, fromJsonType, toJsonType } from "./type.js";
import { decodeTypeAlias, encodeTypeAlias, fromJsonTypeAlias, toJsonTypeAlias } from "./type.js";
import { decodeField, encodeField, fromJsonField, toJsonField } from "./type.js";
import { decodeValue, encodeValue, fromJsonValue, toJsonValue } from "./value.js";
import { decodeSpan, encodeSpan, fromJsonSpan, toJsonSpan } from "../../source/file/model/span.js";
import { decodeSourceIndexData, encodeSourceIndexData, fromJsonSourceIndexData, toJsonSourceIndexData } from "../../source/tree/index.js";

/** Dense index entry for one MIR node id. */
export type NodeIndexEntry = {
    /** The packed local id and node type. */
    readonly packed: number;
};

export const NodeIndexEntry = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: NodeIndexEntry): void {
        encodeNodeIndexEntry(writer, value);
    },

    /** Decode one NodeIndexEntry. */
    decode(reader: BinaryReader): NodeIndexEntry {
        return decodeNodeIndexEntry(reader);
    },

    /** Return this value as JSON. */
    toJson(value: NodeIndexEntry): Json {
        return toJsonNodeIndexEntry(value);
    },

    /** Return one NodeIndexEntry from one JSON value. */
    fromJson(value: Json): NodeIndexEntry {
        return fromJsonNodeIndexEntry(value);
    },
};

/** Encode one NodeIndexEntry. */
export function encodeNodeIndexEntry(writer: BinaryWriter, value: NodeIndexEntry): void {
    writer.writeUnsigned(value.packed);
}

/** Decode one NodeIndexEntry. */
export function decodeNodeIndexEntry(reader: BinaryReader): NodeIndexEntry {
    const packed = reader.readNumber();

    return {
        packed,
    };
}

/** Return one JSON value for one NodeIndexEntry. */
export function toJsonNodeIndexEntry(value: NodeIndexEntry): Json {
    return {
        packed: value.packed,
    };
}

/** Return one NodeIndexEntry from one JSON value. */
export function fromJsonNodeIndexEntry(value: Json): NodeIndexEntry {
    const object = jsonObject(value);

    return {
        packed: jsonInteger(jsonField(object, "packed")),
    };
}

/** MIR tree for a single unit. */
export type Tree = {
    /** The first global node id stored in this tree. */
    readonly firstGlobalId: number;
    /** The next global node id to allocate. */
    readonly nextGlobalId: number;
    /** Dense local id and node type by node id. */
    readonly nodeIndexByNodeId: ReadonlyArray<NodeIndexEntry>;
    /** Maps global node id → attached attributes. */
    readonly attributesByNodeId: ReadonlyMap<number, ReadonlyArray<Attribute>>;
    /** DIR source id keyed by MIR node id. */
    readonly sourceIdByNodeId: ReadonlyArray<number | undefined>;
    /** How each pass-created node came to be. */
    readonly originByNodeId: OriginTable;
    /** Source ranges and anchors for parsed MIR node ownership. */
    readonly sourceIndex: SourceIndexData;
    /** The parsed MIR source text. */
    readonly sourceText?: string;
    /** The full parsed token stream. */
    readonly tokens: ReadonlyArray<Token>;
    /** Leading comment spans keyed by global node id. */
    readonly leadingCommentSpansByNodeId: ReadonlyArray<Span | undefined>;
    /** Parsed attribute spans keyed by global node id. */
    readonly attributeSpansByNodeId: ReadonlyMap<number, ReadonlyArray<Span>>;
    /** Parsed declaration keyword spans keyed by global node id. */
    readonly keywordSpansByNodeId: ReadonlyMap<number, Span>;
    /** Parsed function parameter spans keyed by global node id. */
    readonly functionParameterSpansByNodeId: ReadonlyMap<number, ReadonlyArray<TypedValueSpan>>;
    /** Parsed function header spans keyed by global node id. */
    readonly functionHeaderSpansByNodeId: ReadonlyMap<number, FunctionHeaderSpans>;
    /** Parsed type field spans keyed by global node id. */
    readonly typeFieldSpansByNodeId: ReadonlyMap<number, ReadonlyArray<FieldSpan>>;
    /** Parsed type declaration spans keyed by global node id. */
    readonly typeDeclarationSpansByNodeId: ReadonlyMap<number, TypeDeclarationSpans>;
    readonly functions: ReadonlyArray<Function>;
    readonly blocks: ReadonlyArray<Block>;
    readonly instructions: ReadonlyArray<Instruction>;
    readonly terminators: ReadonlyArray<Terminator>;
    readonly locals: ReadonlyArray<Local>;
    readonly types: ReadonlyArray<Type>;
    readonly typeAliases: ReadonlyArray<TypeAlias>;
    readonly fields: ReadonlyArray<Field>;
    readonly globals: ReadonlyArray<Global>;
    /** Lifetime parameters keyed by type node. */
    readonly lifetimesByType: ReadonlyMap<LocalNodeId, ReadonlyArray<LifetimeParameter>>;
    /** Flat buffer of MIR values. */
    readonly values: ReadonlyArray<Value>;
    /** Flat buffer of instruction indices. */
    readonly indices: ReadonlyArray<number>;
    /** Flat buffer of instruction extents. */
    readonly extents: ReadonlyArray<bigint>;
    /** Flat buffer of instruction flags. */
    readonly flags: Uint8Array | readonly number[];
    /** Flat buffer of switch cases. */
    readonly switchCases: ReadonlyArray<SwitchCase>;
    /** Structured tensor immediates. */
    readonly tensorImmediates: ReadonlyArray<TensorImmediate>;
};

export const Tree = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: Tree): void {
        encodeTree(writer, value);
    },

    /** Decode one Tree. */
    decode(reader: BinaryReader): Tree {
        return decodeTree(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Tree): Json {
        return toJsonTree(value);
    },

    /** Return one Tree from one JSON value. */
    fromJson(value: Json): Tree {
        return fromJsonTree(value);
    },
};

/** Encode one Tree. */
export function encodeTree(writer: BinaryWriter, value: Tree): void {
    writer.writeUnsigned(value.firstGlobalId);
    writer.writeUnsigned(value.nextGlobalId);
    writer.writeUnsigned(value.nodeIndexByNodeId.length);
    for (const item2 of value.nodeIndexByNodeId) {
        encodeNodeIndexEntry(writer, item2);
    }
    const entries3 = Array.from(value.attributesByNodeId.entries()).map(([key3, item3]) => {
        const keyBytes = nestedBytes((writer) => {
            writer.writeUnsigned(key3);
        });
        return { key3, item3, keyBytes };
    });
    entries3.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries3.length);
    for (const entry3 of entries3) {
        writer.writeUnsigned(entry3.key3);
        writer.writeUnsigned(entry3.item3.length);
        for (const item4 of entry3.item3) {
            encodeAttribute(writer, item4);
        }
    }
    writer.writeUnsigned(value.sourceIdByNodeId.length);
    for (const item4 of value.sourceIdByNodeId) {
        writer.writeOption(item4, (value5) => {
            writer.writeUnsigned(value5);
        });
    }
    encodeOriginTable(writer, value.originByNodeId);
    encodeSourceIndexData(writer, value.sourceIndex);
    writer.writeOption(value.sourceText, (value7) => {
        writer.writeString(value7);
    });
    writer.writeUnsigned(value.tokens.length);
    for (const item8 of value.tokens) {
        encodeToken(writer, item8);
    }
    writer.writeUnsigned(value.leadingCommentSpansByNodeId.length);
    for (const item9 of value.leadingCommentSpansByNodeId) {
        writer.writeOption(item9, (value10) => {
            encodeSpan(writer, value10);
        });
    }
    const entries10 = Array.from(value.attributeSpansByNodeId.entries()).map(([key10, item10]) => {
        const keyBytes = nestedBytes((writer) => {
            writer.writeUnsigned(key10);
        });
        return { key10, item10, keyBytes };
    });
    entries10.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries10.length);
    for (const entry10 of entries10) {
        writer.writeUnsigned(entry10.key10);
        writer.writeUnsigned(entry10.item10.length);
        for (const item11 of entry10.item10) {
            encodeSpan(writer, item11);
        }
    }
    const entries11 = Array.from(value.keywordSpansByNodeId.entries()).map(([key11, item11]) => {
        const keyBytes = nestedBytes((writer) => {
            writer.writeUnsigned(key11);
        });
        return { key11, item11, keyBytes };
    });
    entries11.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries11.length);
    for (const entry11 of entries11) {
        writer.writeUnsigned(entry11.key11);
        encodeSpan(writer, entry11.item11);
    }
    const entries12 = Array.from(value.functionParameterSpansByNodeId.entries()).map(([key12, item12]) => {
        const keyBytes = nestedBytes((writer) => {
            writer.writeUnsigned(key12);
        });
        return { key12, item12, keyBytes };
    });
    entries12.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries12.length);
    for (const entry12 of entries12) {
        writer.writeUnsigned(entry12.key12);
        writer.writeUnsigned(entry12.item12.length);
        for (const item13 of entry12.item12) {
            encodeTypedValueSpan(writer, item13);
        }
    }
    const entries13 = Array.from(value.functionHeaderSpansByNodeId.entries()).map(([key13, item13]) => {
        const keyBytes = nestedBytes((writer) => {
            writer.writeUnsigned(key13);
        });
        return { key13, item13, keyBytes };
    });
    entries13.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries13.length);
    for (const entry13 of entries13) {
        writer.writeUnsigned(entry13.key13);
        encodeFunctionHeaderSpans(writer, entry13.item13);
    }
    const entries14 = Array.from(value.typeFieldSpansByNodeId.entries()).map(([key14, item14]) => {
        const keyBytes = nestedBytes((writer) => {
            writer.writeUnsigned(key14);
        });
        return { key14, item14, keyBytes };
    });
    entries14.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries14.length);
    for (const entry14 of entries14) {
        writer.writeUnsigned(entry14.key14);
        writer.writeUnsigned(entry14.item14.length);
        for (const item15 of entry14.item14) {
            encodeFieldSpan(writer, item15);
        }
    }
    const entries15 = Array.from(value.typeDeclarationSpansByNodeId.entries()).map(([key15, item15]) => {
        const keyBytes = nestedBytes((writer) => {
            writer.writeUnsigned(key15);
        });
        return { key15, item15, keyBytes };
    });
    entries15.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries15.length);
    for (const entry15 of entries15) {
        writer.writeUnsigned(entry15.key15);
        encodeTypeDeclarationSpans(writer, entry15.item15);
    }
    writer.writeUnsigned(value.functions.length);
    for (const item16 of value.functions) {
        encodeFunction(writer, item16);
    }
    writer.writeUnsigned(value.blocks.length);
    for (const item17 of value.blocks) {
        encodeBlock(writer, item17);
    }
    writer.writeUnsigned(value.instructions.length);
    for (const item18 of value.instructions) {
        encodeInstruction(writer, item18);
    }
    writer.writeUnsigned(value.terminators.length);
    for (const item19 of value.terminators) {
        encodeTerminator(writer, item19);
    }
    writer.writeUnsigned(value.locals.length);
    for (const item20 of value.locals) {
        encodeLocal(writer, item20);
    }
    writer.writeUnsigned(value.types.length);
    for (const item21 of value.types) {
        encodeType(writer, item21);
    }
    writer.writeUnsigned(value.typeAliases.length);
    for (const item22 of value.typeAliases) {
        encodeTypeAlias(writer, item22);
    }
    writer.writeUnsigned(value.fields.length);
    for (const item23 of value.fields) {
        encodeField(writer, item23);
    }
    writer.writeUnsigned(value.globals.length);
    for (const item24 of value.globals) {
        encodeGlobal(writer, item24);
    }
    const entries25 = Array.from(value.lifetimesByType.entries()).map(([key25, item25]) => {
        const keyBytes = nestedBytes((writer) => {
            encodeLocalNodeId(writer, key25);
        });
        return { key25, item25, keyBytes };
    });
    entries25.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries25.length);
    for (const entry25 of entries25) {
        encodeLocalNodeId(writer, entry25.key25);
        writer.writeUnsigned(entry25.item25.length);
        for (const item26 of entry25.item25) {
            encodeLifetimeParameter(writer, item26);
        }
    }
    writer.writeUnsigned(value.values.length);
    for (const item26 of value.values) {
        encodeValue(writer, item26);
    }
    writer.writeUnsigned(value.indices.length);
    for (const item27 of value.indices) {
        writer.writeUnsigned(item27);
    }
    writer.writeUnsigned(value.extents.length);
    for (const item28 of value.extents) {
        writer.writeUnsigned(item28);
    }
    writer.writeByteSlice(value.flags);
    writer.writeUnsigned(value.switchCases.length);
    for (const item30 of value.switchCases) {
        encodeSwitchCase(writer, item30);
    }
    writer.writeUnsigned(value.tensorImmediates.length);
    for (const item31 of value.tensorImmediates) {
        encodeTensorImmediate(writer, item31);
    }
}

/** Decode one Tree. */
export function decodeTree(reader: BinaryReader): Tree {
    const firstGlobalId = reader.readNumber();
    const nextGlobalId = reader.readNumber();
    const nodeIndexByNodeId = (() => { const length2 = reader.readNumber(); const items2: Array<NodeIndexEntry> = []; for (let index = 0; index < length2; index += 1) { items2.push(decodeNodeIndexEntry(reader)); } return items2; })();
    const attributesByNodeId = (() => { const length3 = reader.readNumber(); const items3 = new Map<number, ReadonlyArray<Attribute>>(); for (let index = 0; index < length3; index += 1) { items3.set(reader.readNumber(), (() => { const length5 = reader.readNumber(); const items5: Array<Attribute> = []; for (let index = 0; index < length5; index += 1) { items5.push(decodeAttribute(reader)); } return items5; })()); } return items3; })();
    const sourceIdByNodeId = (() => { const length4 = reader.readNumber(); const items4: Array<number | undefined> = []; for (let index = 0; index < length4; index += 1) { items4.push(reader.readOption(() => reader.readNumber())); } return items4; })();
    const originByNodeId = decodeOriginTable(reader);
    const sourceIndex = decodeSourceIndexData(reader);
    const sourceText = reader.readOption(() => reader.readString());
    const tokens = (() => { const length8 = reader.readNumber(); const items8: Array<Token> = []; for (let index = 0; index < length8; index += 1) { items8.push(decodeToken(reader)); } return items8; })();
    const leadingCommentSpansByNodeId = (() => { const length9 = reader.readNumber(); const items9: Array<Span | undefined> = []; for (let index = 0; index < length9; index += 1) { items9.push(reader.readOption(() => decodeSpan(reader))); } return items9; })();
    const attributeSpansByNodeId = (() => { const length10 = reader.readNumber(); const items10 = new Map<number, ReadonlyArray<Span>>(); for (let index = 0; index < length10; index += 1) { items10.set(reader.readNumber(), (() => { const length12 = reader.readNumber(); const items12: Array<Span> = []; for (let index = 0; index < length12; index += 1) { items12.push(decodeSpan(reader)); } return items12; })()); } return items10; })();
    const keywordSpansByNodeId = (() => { const length11 = reader.readNumber(); const items11 = new Map<number, Span>(); for (let index = 0; index < length11; index += 1) { items11.set(reader.readNumber(), decodeSpan(reader)); } return items11; })();
    const functionParameterSpansByNodeId = (() => { const length12 = reader.readNumber(); const items12 = new Map<number, ReadonlyArray<TypedValueSpan>>(); for (let index = 0; index < length12; index += 1) { items12.set(reader.readNumber(), (() => { const length14 = reader.readNumber(); const items14: Array<TypedValueSpan> = []; for (let index = 0; index < length14; index += 1) { items14.push(decodeTypedValueSpan(reader)); } return items14; })()); } return items12; })();
    const functionHeaderSpansByNodeId = (() => { const length13 = reader.readNumber(); const items13 = new Map<number, FunctionHeaderSpans>(); for (let index = 0; index < length13; index += 1) { items13.set(reader.readNumber(), decodeFunctionHeaderSpans(reader)); } return items13; })();
    const typeFieldSpansByNodeId = (() => { const length14 = reader.readNumber(); const items14 = new Map<number, ReadonlyArray<FieldSpan>>(); for (let index = 0; index < length14; index += 1) { items14.set(reader.readNumber(), (() => { const length16 = reader.readNumber(); const items16: Array<FieldSpan> = []; for (let index = 0; index < length16; index += 1) { items16.push(decodeFieldSpan(reader)); } return items16; })()); } return items14; })();
    const typeDeclarationSpansByNodeId = (() => { const length15 = reader.readNumber(); const items15 = new Map<number, TypeDeclarationSpans>(); for (let index = 0; index < length15; index += 1) { items15.set(reader.readNumber(), decodeTypeDeclarationSpans(reader)); } return items15; })();
    const functions = (() => { const length16 = reader.readNumber(); const items16: Array<Function> = []; for (let index = 0; index < length16; index += 1) { items16.push(decodeFunction(reader)); } return items16; })();
    const blocks = (() => { const length17 = reader.readNumber(); const items17: Array<Block> = []; for (let index = 0; index < length17; index += 1) { items17.push(decodeBlock(reader)); } return items17; })();
    const instructions = (() => { const length18 = reader.readNumber(); const items18: Array<Instruction> = []; for (let index = 0; index < length18; index += 1) { items18.push(decodeInstruction(reader)); } return items18; })();
    const terminators = (() => { const length19 = reader.readNumber(); const items19: Array<Terminator> = []; for (let index = 0; index < length19; index += 1) { items19.push(decodeTerminator(reader)); } return items19; })();
    const locals = (() => { const length20 = reader.readNumber(); const items20: Array<Local> = []; for (let index = 0; index < length20; index += 1) { items20.push(decodeLocal(reader)); } return items20; })();
    const types = (() => { const length21 = reader.readNumber(); const items21: Array<Type> = []; for (let index = 0; index < length21; index += 1) { items21.push(decodeType(reader)); } return items21; })();
    const typeAliases = (() => { const length22 = reader.readNumber(); const items22: Array<TypeAlias> = []; for (let index = 0; index < length22; index += 1) { items22.push(decodeTypeAlias(reader)); } return items22; })();
    const fields = (() => { const length23 = reader.readNumber(); const items23: Array<Field> = []; for (let index = 0; index < length23; index += 1) { items23.push(decodeField(reader)); } return items23; })();
    const globals = (() => { const length24 = reader.readNumber(); const items24: Array<Global> = []; for (let index = 0; index < length24; index += 1) { items24.push(decodeGlobal(reader)); } return items24; })();
    const lifetimesByType = (() => { const length25 = reader.readNumber(); const items25 = new Map<LocalNodeId, ReadonlyArray<LifetimeParameter>>(); for (let index = 0; index < length25; index += 1) { items25.set(decodeLocalNodeId(reader), (() => { const length27 = reader.readNumber(); const items27: Array<LifetimeParameter> = []; for (let index = 0; index < length27; index += 1) { items27.push(decodeLifetimeParameter(reader)); } return items27; })()); } return items25; })();
    const values = (() => { const length26 = reader.readNumber(); const items26: Array<Value> = []; for (let index = 0; index < length26; index += 1) { items26.push(decodeValue(reader)); } return items26; })();
    const indices = (() => { const length27 = reader.readNumber(); const items27: Array<number> = []; for (let index = 0; index < length27; index += 1) { items27.push(reader.readNumber()); } return items27; })();
    const extents = (() => { const length28 = reader.readNumber(); const items28: Array<bigint> = []; for (let index = 0; index < length28; index += 1) { items28.push(reader.readUnsigned()); } return items28; })();
    const flags = reader.readByteSlice();
    const switchCases = (() => { const length30 = reader.readNumber(); const items30: Array<SwitchCase> = []; for (let index = 0; index < length30; index += 1) { items30.push(decodeSwitchCase(reader)); } return items30; })();
    const tensorImmediates = (() => { const length31 = reader.readNumber(); const items31: Array<TensorImmediate> = []; for (let index = 0; index < length31; index += 1) { items31.push(decodeTensorImmediate(reader)); } return items31; })();

    return {
        firstGlobalId,
        nextGlobalId,
        nodeIndexByNodeId,
        attributesByNodeId,
        sourceIdByNodeId,
        originByNodeId,
        sourceIndex,
        ...(sourceText === undefined ? {} : { sourceText }),
        tokens,
        leadingCommentSpansByNodeId,
        attributeSpansByNodeId,
        keywordSpansByNodeId,
        functionParameterSpansByNodeId,
        functionHeaderSpansByNodeId,
        typeFieldSpansByNodeId,
        typeDeclarationSpansByNodeId,
        functions,
        blocks,
        instructions,
        terminators,
        locals,
        types,
        typeAliases,
        fields,
        globals,
        lifetimesByType,
        values,
        indices,
        extents,
        flags,
        switchCases,
        tensorImmediates,
    };
}

/** Return one JSON value for one Tree. */
export function toJsonTree(value: Tree): Json {
    return {
        firstGlobalId: value.firstGlobalId,
        nextGlobalId: value.nextGlobalId,
        nodeIndexByNodeId: value.nodeIndexByNodeId.map((item0) => toJsonNodeIndexEntry(item0)),
        attributesByNodeId: Array.from(value.attributesByNodeId.entries()).map(([key0, item0]) => [key0, item0.map((item1) => toJsonAttribute(item1))] as const),
        sourceIdByNodeId: value.sourceIdByNodeId.map((item0) => item0 === undefined ? null : item0),
        originByNodeId: toJsonOriginTable(value.originByNodeId),
        sourceIndex: toJsonSourceIndexData(value.sourceIndex),
        ...(value.sourceText === undefined ? {} : { sourceText: value.sourceText }),
        tokens: value.tokens.map((item0) => toJsonToken(item0)),
        leadingCommentSpansByNodeId: value.leadingCommentSpansByNodeId.map((item0) => item0 === undefined ? null : toJsonSpan(item0)),
        attributeSpansByNodeId: Array.from(value.attributeSpansByNodeId.entries()).map(([key0, item0]) => [key0, item0.map((item1) => toJsonSpan(item1))] as const),
        keywordSpansByNodeId: Array.from(value.keywordSpansByNodeId.entries()).map(([key0, item0]) => [key0, toJsonSpan(item0)] as const),
        functionParameterSpansByNodeId: Array.from(value.functionParameterSpansByNodeId.entries()).map(([key0, item0]) => [key0, item0.map((item1) => toJsonTypedValueSpan(item1))] as const),
        functionHeaderSpansByNodeId: Array.from(value.functionHeaderSpansByNodeId.entries()).map(([key0, item0]) => [key0, toJsonFunctionHeaderSpans(item0)] as const),
        typeFieldSpansByNodeId: Array.from(value.typeFieldSpansByNodeId.entries()).map(([key0, item0]) => [key0, item0.map((item1) => toJsonFieldSpan(item1))] as const),
        typeDeclarationSpansByNodeId: Array.from(value.typeDeclarationSpansByNodeId.entries()).map(([key0, item0]) => [key0, toJsonTypeDeclarationSpans(item0)] as const),
        functions: value.functions.map((item0) => toJsonFunction(item0)),
        blocks: value.blocks.map((item0) => toJsonBlock(item0)),
        instructions: value.instructions.map((item0) => toJsonInstruction(item0)),
        terminators: value.terminators.map((item0) => toJsonTerminator(item0)),
        locals: value.locals.map((item0) => toJsonLocal(item0)),
        types: value.types.map((item0) => toJsonType(item0)),
        typeAliases: value.typeAliases.map((item0) => toJsonTypeAlias(item0)),
        fields: value.fields.map((item0) => toJsonField(item0)),
        globals: value.globals.map((item0) => toJsonGlobal(item0)),
        lifetimesByType: Array.from(value.lifetimesByType.entries()).map(([key0, item0]) => [toJsonLocalNodeId(key0), item0.map((item1) => toJsonLifetimeParameter(item1))] as const),
        values: value.values.map((item0) => toJsonValue(item0)),
        indices: value.indices.map((item0) => item0),
        extents: value.extents.map((item0) => item0.toString()),
        flags: bytesToJson(value.flags),
        switchCases: value.switchCases.map((item0) => toJsonSwitchCase(item0)),
        tensorImmediates: value.tensorImmediates.map((item0) => toJsonTensorImmediate(item0)),
    };
}

/** Return one Tree from one JSON value. */
export function fromJsonTree(value: Json): Tree {
    const object = jsonObject(value);

    return {
        firstGlobalId: jsonInteger(jsonField(object, "firstGlobalId")),
        nextGlobalId: jsonInteger(jsonField(object, "nextGlobalId")),
        nodeIndexByNodeId: jsonArray(jsonField(object, "nodeIndexByNodeId")).map((item0) => fromJsonNodeIndexEntry(item0)),
        attributesByNodeId: new Map(jsonArray(jsonField(object, "attributesByNodeId")).map((entry) => { const items = jsonArray(entry); if (items.length !== 2) { throw new SerdeError(`expected JSON map entry length 2: ${items.length}`); } const key0 = items[0]; const item0 = items[1]; return [jsonInteger(key0), jsonArray(item0).map((item1) => fromJsonAttribute(item1))] as const; })),
        sourceIdByNodeId: jsonArray(jsonField(object, "sourceIdByNodeId")).map((item0) => item0 === null ? undefined : jsonInteger(item0)),
        originByNodeId: fromJsonOriginTable(jsonField(object, "originByNodeId")),
        sourceIndex: fromJsonSourceIndexData(jsonField(object, "sourceIndex")),
        sourceText: jsonOptional(object, "sourceText", (value) => jsonString(value)),
        tokens: jsonArray(jsonField(object, "tokens")).map((item0) => fromJsonToken(item0)),
        leadingCommentSpansByNodeId: jsonArray(jsonField(object, "leadingCommentSpansByNodeId")).map((item0) => item0 === null ? undefined : fromJsonSpan(item0)),
        attributeSpansByNodeId: new Map(jsonArray(jsonField(object, "attributeSpansByNodeId")).map((entry) => { const items = jsonArray(entry); if (items.length !== 2) { throw new SerdeError(`expected JSON map entry length 2: ${items.length}`); } const key0 = items[0]; const item0 = items[1]; return [jsonInteger(key0), jsonArray(item0).map((item1) => fromJsonSpan(item1))] as const; })),
        keywordSpansByNodeId: new Map(jsonArray(jsonField(object, "keywordSpansByNodeId")).map((entry) => { const items = jsonArray(entry); if (items.length !== 2) { throw new SerdeError(`expected JSON map entry length 2: ${items.length}`); } const key0 = items[0]; const item0 = items[1]; return [jsonInteger(key0), fromJsonSpan(item0)] as const; })),
        functionParameterSpansByNodeId: new Map(jsonArray(jsonField(object, "functionParameterSpansByNodeId")).map((entry) => { const items = jsonArray(entry); if (items.length !== 2) { throw new SerdeError(`expected JSON map entry length 2: ${items.length}`); } const key0 = items[0]; const item0 = items[1]; return [jsonInteger(key0), jsonArray(item0).map((item1) => fromJsonTypedValueSpan(item1))] as const; })),
        functionHeaderSpansByNodeId: new Map(jsonArray(jsonField(object, "functionHeaderSpansByNodeId")).map((entry) => { const items = jsonArray(entry); if (items.length !== 2) { throw new SerdeError(`expected JSON map entry length 2: ${items.length}`); } const key0 = items[0]; const item0 = items[1]; return [jsonInteger(key0), fromJsonFunctionHeaderSpans(item0)] as const; })),
        typeFieldSpansByNodeId: new Map(jsonArray(jsonField(object, "typeFieldSpansByNodeId")).map((entry) => { const items = jsonArray(entry); if (items.length !== 2) { throw new SerdeError(`expected JSON map entry length 2: ${items.length}`); } const key0 = items[0]; const item0 = items[1]; return [jsonInteger(key0), jsonArray(item0).map((item1) => fromJsonFieldSpan(item1))] as const; })),
        typeDeclarationSpansByNodeId: new Map(jsonArray(jsonField(object, "typeDeclarationSpansByNodeId")).map((entry) => { const items = jsonArray(entry); if (items.length !== 2) { throw new SerdeError(`expected JSON map entry length 2: ${items.length}`); } const key0 = items[0]; const item0 = items[1]; return [jsonInteger(key0), fromJsonTypeDeclarationSpans(item0)] as const; })),
        functions: jsonArray(jsonField(object, "functions")).map((item0) => fromJsonFunction(item0)),
        blocks: jsonArray(jsonField(object, "blocks")).map((item0) => fromJsonBlock(item0)),
        instructions: jsonArray(jsonField(object, "instructions")).map((item0) => fromJsonInstruction(item0)),
        terminators: jsonArray(jsonField(object, "terminators")).map((item0) => fromJsonTerminator(item0)),
        locals: jsonArray(jsonField(object, "locals")).map((item0) => fromJsonLocal(item0)),
        types: jsonArray(jsonField(object, "types")).map((item0) => fromJsonType(item0)),
        typeAliases: jsonArray(jsonField(object, "typeAliases")).map((item0) => fromJsonTypeAlias(item0)),
        fields: jsonArray(jsonField(object, "fields")).map((item0) => fromJsonField(item0)),
        globals: jsonArray(jsonField(object, "globals")).map((item0) => fromJsonGlobal(item0)),
        lifetimesByType: new Map(jsonArray(jsonField(object, "lifetimesByType")).map((entry) => { const items = jsonArray(entry); if (items.length !== 2) { throw new SerdeError(`expected JSON map entry length 2: ${items.length}`); } const key0 = items[0]; const item0 = items[1]; return [fromJsonLocalNodeId(key0), jsonArray(item0).map((item1) => fromJsonLifetimeParameter(item1))] as const; })),
        values: jsonArray(jsonField(object, "values")).map((item0) => fromJsonValue(item0)),
        indices: jsonArray(jsonField(object, "indices")).map((item0) => jsonInteger(item0)),
        extents: jsonArray(jsonField(object, "extents")).map((item0) => jsonBigint(item0)),
        flags: bytesFromJson(jsonField(object, "flags")),
        switchCases: jsonArray(jsonField(object, "switchCases")).map((item0) => fromJsonSwitchCase(item0)),
        tensorImmediates: jsonArray(jsonField(object, "tensorImmediates")).map((item0) => fromJsonTensorImmediate(item0)),
    };
}
