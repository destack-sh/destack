/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, jsonField, jsonInteger, jsonObject } from "../../../protocol/serde.js";

/** SSA value (virtual register). */
export type Value = number;

export const Value = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: Value): void {
        encodeValue(writer, value);
    },

    /** Decode one Value. */
    decode(reader: BinaryReader): Value {
        return decodeValue(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Value): Json {
        return toJsonValue(value);
    },

    /** Return one Value from one JSON value. */
    fromJson(value: Json): Value {
        return fromJsonValue(value);
    },
};

/** Encode one Value. */
export function encodeValue(writer: BinaryWriter, value: Value): void {
    writer.writeUnsigned(value);
}

/** Decode one Value. */
export function decodeValue(reader: BinaryReader): Value {
    return reader.readNumber();
}

/** Return one JSON value for one Value. */
export function toJsonValue(value: Value): Json {
    return value;
}

/** Return one Value from one JSON value. */
export function fromJsonValue(value: Json): Value {
    return jsonInteger(value);
}

/** Compact reference to a value list stored in the MIR tree. */
export type ValueSlice = {
    /** Start index in the value buffer. */
    readonly start: number;
    /** Number of values in the slice. */
    readonly count: number;
};

export const ValueSlice = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ValueSlice): void {
        encodeValueSlice(writer, value);
    },

    /** Decode one ValueSlice. */
    decode(reader: BinaryReader): ValueSlice {
        return decodeValueSlice(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ValueSlice): Json {
        return toJsonValueSlice(value);
    },

    /** Return one ValueSlice from one JSON value. */
    fromJson(value: Json): ValueSlice {
        return fromJsonValueSlice(value);
    },
};

/** Encode one ValueSlice. */
export function encodeValueSlice(writer: BinaryWriter, value: ValueSlice): void {
    writer.writeUnsigned(value.start);
    writer.writeUnsigned(value.count);
}

/** Decode one ValueSlice. */
export function decodeValueSlice(reader: BinaryReader): ValueSlice {
    const start = reader.readNumber();
    const count = reader.readNumber();

    return {
        start,
        count,
    };
}

/** Return one JSON value for one ValueSlice. */
export function toJsonValueSlice(value: ValueSlice): Json {
    return {
        start: value.start,
        count: value.count,
    };
}

/** Return one ValueSlice from one JSON value. */
export function fromJsonValueSlice(value: Json): ValueSlice {
    const object = jsonObject(value);

    return {
        start: jsonInteger(jsonField(object, "start")),
        count: jsonInteger(jsonField(object, "count")),
    };
}
