/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, jsonArray, jsonField, jsonInteger, jsonObject, jsonOptional, jsonString } from "../../../protocol/serde.js";
import type { StringId } from "../../core/string.js";
import { decodeStringId, encodeStringId, fromJsonStringId, toJsonStringId } from "../../core/string.js";

/** The boundary lifetime for an escaping borrowed value. */
export type Lifetime = {
    /** Terms the borrowed value may depend on. */
    readonly terms: ReadonlyArray<LifetimeTerm>;
};

export const Lifetime = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: Lifetime): void {
        encodeLifetime(writer, value);
    },

    /** Decode one Lifetime. */
    decode(reader: BinaryReader): Lifetime {
        return decodeLifetime(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Lifetime): Json {
        return toJsonLifetime(value);
    },

    /** Return one Lifetime from one JSON value. */
    fromJson(value: Json): Lifetime {
        return fromJsonLifetime(value);
    },
};

/** Encode one Lifetime. */
export function encodeLifetime(writer: BinaryWriter, value: Lifetime): void {
    writer.writeUnsigned(value.terms.length);
    for (const item0 of value.terms) {
        encodeLifetimeTerm(writer, item0);
    }
}

/** Decode one Lifetime. */
export function decodeLifetime(reader: BinaryReader): Lifetime {
    const terms = (() => { const length0 = reader.readNumber(); const items0: Array<LifetimeTerm> = []; for (let index = 0; index < length0; index += 1) { items0.push(decodeLifetimeTerm(reader)); } return items0; })();

    return {
        terms,
    };
}

/** Return one JSON value for one Lifetime. */
export function toJsonLifetime(value: Lifetime): Json {
    return {
        terms: value.terms.map((item0) => toJsonLifetimeTerm(item0)),
    };
}

/** Return one Lifetime from one JSON value. */
export function fromJsonLifetime(value: Json): Lifetime {
    const object = jsonObject(value);

    return {
        terms: jsonArray(jsonField(object, "terms")).map((item0) => fromJsonLifetimeTerm(item0)),
    };
}

/** One declared lifetime parameter. */
export type LifetimeParameter = {
    /** The source or generated parameter name. */
    readonly name?: StringId;
    /** The declared slots this parameter outlives. */
    readonly outlives: ReadonlyArray<LifetimeSlot>;
};

export const LifetimeParameter = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: LifetimeParameter): void {
        encodeLifetimeParameter(writer, value);
    },

    /** Decode one LifetimeParameter. */
    decode(reader: BinaryReader): LifetimeParameter {
        return decodeLifetimeParameter(reader);
    },

    /** Return this value as JSON. */
    toJson(value: LifetimeParameter): Json {
        return toJsonLifetimeParameter(value);
    },

    /** Return one LifetimeParameter from one JSON value. */
    fromJson(value: Json): LifetimeParameter {
        return fromJsonLifetimeParameter(value);
    },
};

/** Encode one LifetimeParameter. */
export function encodeLifetimeParameter(writer: BinaryWriter, value: LifetimeParameter): void {
    writer.writeOption(value.name, (value0) => {
        encodeStringId(writer, value0);
    });
    writer.writeUnsigned(value.outlives.length);
    for (const item1 of value.outlives) {
        encodeLifetimeSlot(writer, item1);
    }
}

/** Decode one LifetimeParameter. */
export function decodeLifetimeParameter(reader: BinaryReader): LifetimeParameter {
    const name = reader.readOption(() => decodeStringId(reader));
    const outlives = (() => { const length1 = reader.readNumber(); const items1: Array<LifetimeSlot> = []; for (let index = 0; index < length1; index += 1) { items1.push(decodeLifetimeSlot(reader)); } return items1; })();

    return {
        ...(name === undefined ? {} : { name }),
        outlives,
    };
}

/** Return one JSON value for one LifetimeParameter. */
export function toJsonLifetimeParameter(value: LifetimeParameter): Json {
    return {
        ...(value.name === undefined ? {} : { name: toJsonStringId(value.name) }),
        outlives: value.outlives.map((item0) => toJsonLifetimeSlot(item0)),
    };
}

/** Return one LifetimeParameter from one JSON value. */
export function fromJsonLifetimeParameter(value: Json): LifetimeParameter {
    const object = jsonObject(value);

    return {
        name: jsonOptional(object, "name", (value) => fromJsonStringId(value)),
        outlives: jsonArray(jsonField(object, "outlives")).map((item0) => fromJsonLifetimeSlot(item0)),
    };
}

/** One lifetime slot in a MIR lifetime environment. */
export type LifetimeSlot = number;

export const LifetimeSlot = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: LifetimeSlot): void {
        encodeLifetimeSlot(writer, value);
    },

    /** Decode one LifetimeSlot. */
    decode(reader: BinaryReader): LifetimeSlot {
        return decodeLifetimeSlot(reader);
    },

    /** Return this value as JSON. */
    toJson(value: LifetimeSlot): Json {
        return toJsonLifetimeSlot(value);
    },

    /** Return one LifetimeSlot from one JSON value. */
    fromJson(value: Json): LifetimeSlot {
        return fromJsonLifetimeSlot(value);
    },
};

/** Encode one LifetimeSlot. */
export function encodeLifetimeSlot(writer: BinaryWriter, value: LifetimeSlot): void {
    writer.writeUnsigned(value);
}

/** Decode one LifetimeSlot. */
export function decodeLifetimeSlot(reader: BinaryReader): LifetimeSlot {
    return reader.readNumber();
}

/** Return one JSON value for one LifetimeSlot. */
export function toJsonLifetimeSlot(value: LifetimeSlot): Json {
    return value;
}

/** Return one LifetimeSlot from one JSON value. */
export function fromJsonLifetimeSlot(value: Json): LifetimeSlot {
    return jsonInteger(value);
}

/** One term in a MIR lifetime. */
export type LifetimeTerm =
    /** Global or static storage. */
    | {
          readonly kind: "static";
      }
    /** Storage owned by the current activation. */
    | {
          readonly kind: "frame";
      }
    /** A lifetime slot in the current lifetime environment. */
    | {
          readonly kind: "slot";
          readonly slot: LifetimeSlot;
      }
;

export const LifetimeTerm = {
    /** Global or static storage. */
    "static"(): LifetimeTerm {
        return { kind: "static" };
    },

    /** Storage owned by the current activation. */
    frame(): LifetimeTerm {
        return { kind: "frame" };
    },

    /** A lifetime slot in the current lifetime environment. */
    slot(slot: LifetimeSlot): LifetimeTerm {
        return { kind: "slot", slot };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: LifetimeTerm): void {
        encodeLifetimeTerm(writer, value);
    },

    /** Decode one LifetimeTerm. */
    decode(reader: BinaryReader): LifetimeTerm {
        return decodeLifetimeTerm(reader);
    },

    /** Return this value as JSON. */
    toJson(value: LifetimeTerm): Json {
        return toJsonLifetimeTerm(value);
    },

    /** Return one LifetimeTerm from one JSON value. */
    fromJson(value: Json): LifetimeTerm {
        return fromJsonLifetimeTerm(value);
    },
};

/** Encode one LifetimeTerm. */
export function encodeLifetimeTerm(writer: BinaryWriter, value: LifetimeTerm): void {
    switch (value.kind) {
        case "static":
            writer.writeUnsigned(0);
            return;
        case "frame":
            writer.writeUnsigned(1);
            return;
        case "slot":
            writer.writeUnsigned(2);
            encodeLifetimeSlot(writer, value.slot);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one LifetimeTerm. */
export function decodeLifetimeTerm(reader: BinaryReader): LifetimeTerm {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            return { kind: "static" };
        }
        case 1: {
            return { kind: "frame" };
        }
        case 2: {
            const slot = decodeLifetimeSlot(reader);

            return { kind: "slot", slot };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one LifetimeTerm. */
export function toJsonLifetimeTerm(value: LifetimeTerm): Json {
    switch (value.kind) {
        case "static":
            return {
                kind: "static",
            };
        case "frame":
            return {
                kind: "frame",
            };
        case "slot":
            return {
                kind: "slot",
                slot: toJsonLifetimeSlot(value.slot),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one LifetimeTerm from one JSON value. */
export function fromJsonLifetimeTerm(value: Json): LifetimeTerm {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "static":
            return {
                kind,
            };
        case "frame":
            return {
                kind,
            };
        case "slot":
            return {
                kind,
                slot: fromJsonLifetimeSlot(jsonField(object, "slot")),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}
