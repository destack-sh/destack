/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, jsonField, jsonObject } from "../../../protocol/serde.js";
import type { LocalNodeId } from "./node.js";
import type { Value } from "./value.js";
import { decodeLocalNodeId, encodeLocalNodeId, fromJsonLocalNodeId, toJsonLocalNodeId } from "./node.js";
import { decodeValue, encodeValue, fromJsonValue, toJsonValue } from "./value.js";

/** One block parameter. */
export type BlockParameter = {
    /** The SSA value. */
    readonly value: Value;
    /** The parameter type. */
    readonly ty: LocalNodeId;
};

export const BlockParameter = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: BlockParameter): void {
        encodeBlockParameter(writer, value);
    },

    /** Decode one BlockParameter. */
    decode(reader: BinaryReader): BlockParameter {
        return decodeBlockParameter(reader);
    },

    /** Return this value as JSON. */
    toJson(value: BlockParameter): Json {
        return toJsonBlockParameter(value);
    },

    /** Return one BlockParameter from one JSON value. */
    fromJson(value: Json): BlockParameter {
        return fromJsonBlockParameter(value);
    },
};

/** Encode one BlockParameter. */
export function encodeBlockParameter(writer: BinaryWriter, value: BlockParameter): void {
    encodeValue(writer, value.value);
    encodeLocalNodeId(writer, value.ty);
}

/** Decode one BlockParameter. */
export function decodeBlockParameter(reader: BinaryReader): BlockParameter {
    const value = decodeValue(reader);
    const ty = decodeLocalNodeId(reader);

    return {
        value,
        ty,
    };
}

/** Return one JSON value for one BlockParameter. */
export function toJsonBlockParameter(value: BlockParameter): Json {
    return {
        value: toJsonValue(value.value),
        ty: toJsonLocalNodeId(value.ty),
    };
}

/** Return one BlockParameter from one JSON value. */
export function fromJsonBlockParameter(value: Json): BlockParameter {
    const object = jsonObject(value);

    return {
        value: fromJsonValue(jsonField(object, "value")),
        ty: fromJsonLocalNodeId(jsonField(object, "ty")),
    };
}

/** One function entry parameter. */
export type FunctionParameter = {
    /** The SSA value. */
    readonly value: Value;
    /** The parameter type. */
    readonly ty: LocalNodeId;
};

export const FunctionParameter = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: FunctionParameter): void {
        encodeFunctionParameter(writer, value);
    },

    /** Decode one FunctionParameter. */
    decode(reader: BinaryReader): FunctionParameter {
        return decodeFunctionParameter(reader);
    },

    /** Return this value as JSON. */
    toJson(value: FunctionParameter): Json {
        return toJsonFunctionParameter(value);
    },

    /** Return one FunctionParameter from one JSON value. */
    fromJson(value: Json): FunctionParameter {
        return fromJsonFunctionParameter(value);
    },
};

/** Encode one FunctionParameter. */
export function encodeFunctionParameter(writer: BinaryWriter, value: FunctionParameter): void {
    encodeValue(writer, value.value);
    encodeLocalNodeId(writer, value.ty);
}

/** Decode one FunctionParameter. */
export function decodeFunctionParameter(reader: BinaryReader): FunctionParameter {
    const value = decodeValue(reader);
    const ty = decodeLocalNodeId(reader);

    return {
        value,
        ty,
    };
}

/** Return one JSON value for one FunctionParameter. */
export function toJsonFunctionParameter(value: FunctionParameter): Json {
    return {
        value: toJsonValue(value.value),
        ty: toJsonLocalNodeId(value.ty),
    };
}

/** Return one FunctionParameter from one JSON value. */
export function fromJsonFunctionParameter(value: Json): FunctionParameter {
    const object = jsonObject(value);

    return {
        value: fromJsonValue(jsonField(object, "value")),
        ty: fromJsonLocalNodeId(jsonField(object, "ty")),
    };
}

/** One callable signature parameter. */
export type SignatureParameter = {
    /** The parameter type. */
    readonly ty: LocalNodeId;
};

export const SignatureParameter = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: SignatureParameter): void {
        encodeSignatureParameter(writer, value);
    },

    /** Decode one SignatureParameter. */
    decode(reader: BinaryReader): SignatureParameter {
        return decodeSignatureParameter(reader);
    },

    /** Return this value as JSON. */
    toJson(value: SignatureParameter): Json {
        return toJsonSignatureParameter(value);
    },

    /** Return one SignatureParameter from one JSON value. */
    fromJson(value: Json): SignatureParameter {
        return fromJsonSignatureParameter(value);
    },
};

/** Encode one SignatureParameter. */
export function encodeSignatureParameter(writer: BinaryWriter, value: SignatureParameter): void {
    encodeLocalNodeId(writer, value.ty);
}

/** Decode one SignatureParameter. */
export function decodeSignatureParameter(reader: BinaryReader): SignatureParameter {
    const ty = decodeLocalNodeId(reader);

    return {
        ty,
    };
}

/** Return one JSON value for one SignatureParameter. */
export function toJsonSignatureParameter(value: SignatureParameter): Json {
    return {
        ty: toJsonLocalNodeId(value.ty),
    };
}

/** Return one SignatureParameter from one JSON value. */
export function fromJsonSignatureParameter(value: Json): SignatureParameter {
    const object = jsonObject(value);

    return {
        ty: fromJsonLocalNodeId(jsonField(object, "ty")),
    };
}
