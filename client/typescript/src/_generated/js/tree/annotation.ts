/* generated by destack_client_generate, do not edit */

import {
    BinaryReader,
    BinaryWriter,
    Json,
    SerdeError,
    jsonField,
    jsonObject,
    jsonString,
} from "../../../protocol/serde.js";
import type { StringId } from "../../core/string.js";
import {
    decodeStringId,
    encodeStringId,
    fromJsonStringId,
    toJsonStringId,
} from "../../core/string.js";

/** Annotation to a JS node. */
export type Annotation =
    /** Comment annotation (like `//` or `/*`). */
    {
        readonly kind: "comment";
        readonly position: AnnotationPosition;
        readonly string: StringId;
    };

export const Annotation = {
    /** Comment annotation (like `//` or `/*`). */
    comment(position: AnnotationPosition, string_: StringId): Annotation {
        return { kind: "comment", position, string: string_ };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: Annotation): void {
        encodeAnnotation(writer, value);
    },

    /** Decode one Annotation. */
    decode(reader: BinaryReader): Annotation {
        return decodeAnnotation(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Annotation): Json {
        return toJsonAnnotation(value);
    },

    /** Return one Annotation from one JSON value. */
    fromJson(value: Json): Annotation {
        return fromJsonAnnotation(value);
    },
};

/** Encode one Annotation. */
export function encodeAnnotation(writer: BinaryWriter, value: Annotation): void {
    switch (value.kind) {
        case "comment":
            writer.writeUnsigned(0);
            encodeAnnotationPosition(writer, value.position);
            encodeStringId(writer, value.string);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one Annotation. */
export function decodeAnnotation(reader: BinaryReader): Annotation {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const position = decodeAnnotationPosition(reader);
            const string_ = decodeStringId(reader);

            return {
                kind: "comment",
                position,
                string: string_,
            };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one Annotation. */
export function toJsonAnnotation(value: Annotation): Json {
    switch (value.kind) {
        case "comment":
            return {
                kind: "comment",
                position: toJsonAnnotationPosition(value.position),
                string: toJsonStringId(value.string),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one Annotation from one JSON value. */
export function fromJsonAnnotation(value: Json): Annotation {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "comment":
            return {
                kind,
                position: fromJsonAnnotationPosition(jsonField(object, "position")),
                string: fromJsonStringId(jsonField(object, "string")),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** The position of a JS annotation. */
export type AnnotationPosition = "prefix" | "infix" | "postfix";

export const AnnotationPosition = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: AnnotationPosition): void {
        encodeAnnotationPosition(writer, value);
    },

    /** Decode one AnnotationPosition. */
    decode(reader: BinaryReader): AnnotationPosition {
        return decodeAnnotationPosition(reader);
    },

    /** Return this value as JSON. */
    toJson(value: AnnotationPosition): Json {
        return toJsonAnnotationPosition(value);
    },

    /** Return one AnnotationPosition from one JSON value. */
    fromJson(value: Json): AnnotationPosition {
        return fromJsonAnnotationPosition(value);
    },
};

/** Encode one AnnotationPosition. */
export function encodeAnnotationPosition(writer: BinaryWriter, value: AnnotationPosition): void {
    switch (value) {
        case "prefix":
            writer.writeUnsigned(0);
            return;
        case "infix":
            writer.writeUnsigned(1);
            return;
        case "postfix":
            writer.writeUnsigned(2);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one AnnotationPosition. */
export function decodeAnnotationPosition(reader: BinaryReader): AnnotationPosition {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "prefix";
        case 1:
            return "infix";
        case 2:
            return "postfix";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one AnnotationPosition. */
export function toJsonAnnotationPosition(value: AnnotationPosition): Json {
    return value;
}

/** Return one AnnotationPosition from one JSON value. */
export function fromJsonAnnotationPosition(value: Json): AnnotationPosition {
    const variant = jsonString(value);

    switch (variant) {
        case "prefix":
            return "prefix";
        case "infix":
            return "infix";
        case "postfix":
            return "postfix";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}
