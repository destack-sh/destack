/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, jsonArray, jsonField, jsonObject, jsonOptional, jsonString } from "../../../protocol/serde.js";
import type { ExportKind } from "./dependency.js";
import type { FunctionSignature } from "./function.js";
import type { Name } from "./key.js";
import type { LocalNodeId } from "./node.js";
import { decodeExportKind, encodeExportKind, fromJsonExportKind, toJsonExportKind } from "./dependency.js";
import { decodeFunctionSignature, encodeFunctionSignature, fromJsonFunctionSignature, toJsonFunctionSignature } from "./function.js";
import { decodeName, encodeName, fromJsonName, toJsonName } from "./key.js";
import { decodeLocalNodeId, encodeLocalNodeId, fromJsonLocalNodeId, toJsonLocalNodeId } from "./node.js";

/** A class declaration. */
export type ClassDeclaration = {
    /** The declared name. */
    readonly name?: Name;
    /** The export binding of the declaration. */
    readonly export?: ExportKind;
    /** The optional extended class expression. */
    readonly extendsExpression?: LocalNodeId;
    /** The class members. */
    readonly members: ReadonlyArray<LocalNodeId>;
};

export const ClassDeclaration = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ClassDeclaration): void {
        encodeClassDeclaration(writer, value);
    },

    /** Decode one ClassDeclaration. */
    decode(reader: BinaryReader): ClassDeclaration {
        return decodeClassDeclaration(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ClassDeclaration): Json {
        return toJsonClassDeclaration(value);
    },

    /** Return one ClassDeclaration from one JSON value. */
    fromJson(value: Json): ClassDeclaration {
        return fromJsonClassDeclaration(value);
    },
};

/** Encode one ClassDeclaration. */
export function encodeClassDeclaration(writer: BinaryWriter, value: ClassDeclaration): void {
    writer.writeOption(value.name, (value0) => {
        encodeName(writer, value0);
    });
    writer.writeOption(value.export, (value1) => {
        encodeExportKind(writer, value1);
    });
    writer.writeOption(value.extendsExpression, (value2) => {
        encodeLocalNodeId(writer, value2);
    });
    writer.writeUnsigned(value.members.length);
    for (const item3 of value.members) {
        encodeLocalNodeId(writer, item3);
    }
}

/** Decode one ClassDeclaration. */
export function decodeClassDeclaration(reader: BinaryReader): ClassDeclaration {
    const name = reader.readOption(() => decodeName(reader));
    const export_ = reader.readOption(() => decodeExportKind(reader));
    const extendsExpression = reader.readOption(() => decodeLocalNodeId(reader));
    const members = (() => { const length3 = reader.readNumber(); const items3: Array<LocalNodeId> = []; for (let index = 0; index < length3; index += 1) { items3.push(decodeLocalNodeId(reader)); } return items3; })();

    return {
        ...(name === undefined ? {} : { name }),
        ...(export_ === undefined ? {} : { export: export_ }),
        ...(extendsExpression === undefined ? {} : { extendsExpression }),
        members,
    };
}

/** Return one JSON value for one ClassDeclaration. */
export function toJsonClassDeclaration(value: ClassDeclaration): Json {
    return {
        ...(value.name === undefined ? {} : { name: toJsonName(value.name) }),
        ...(value.export === undefined ? {} : { export: toJsonExportKind(value.export) }),
        ...(value.extendsExpression === undefined ? {} : { extendsExpression: toJsonLocalNodeId(value.extendsExpression) }),
        members: value.members.map((item0) => toJsonLocalNodeId(item0)),
    };
}

/** Return one ClassDeclaration from one JSON value. */
export function fromJsonClassDeclaration(value: Json): ClassDeclaration {
    const object = jsonObject(value);

    return {
        name: jsonOptional(object, "name", (value) => fromJsonName(value)),
        export: jsonOptional(object, "export", (value) => fromJsonExportKind(value)),
        extendsExpression: jsonOptional(object, "extendsExpression", (value) => fromJsonLocalNodeId(value)),
        members: jsonArray(jsonField(object, "members")).map((item0) => fromJsonLocalNodeId(item0)),
    };
}

/** A declaration item. */
export type Declaration =
    /** Class declaration. */
    | {
          readonly kind: "class";
          readonly class: ClassDeclaration;
      }
    /** Function declaration. */
    | {
          readonly kind: "function";
          readonly function: FunctionDeclaration;
      }
;

export const Declaration = {
    /** Class declaration. */
    "class"(class_: ClassDeclaration): Declaration {
        return { kind: "class", class: class_ };
    },

    /** Function declaration. */
    "function"(function_: FunctionDeclaration): Declaration {
        return { kind: "function", function: function_ };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: Declaration): void {
        encodeDeclaration(writer, value);
    },

    /** Decode one Declaration. */
    decode(reader: BinaryReader): Declaration {
        return decodeDeclaration(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Declaration): Json {
        return toJsonDeclaration(value);
    },

    /** Return one Declaration from one JSON value. */
    fromJson(value: Json): Declaration {
        return fromJsonDeclaration(value);
    },
};

/** Encode one Declaration. */
export function encodeDeclaration(writer: BinaryWriter, value: Declaration): void {
    switch (value.kind) {
        case "class":
            writer.writeUnsigned(0);
            encodeClassDeclaration(writer, value.class);
            return;
        case "function":
            writer.writeUnsigned(1);
            encodeFunctionDeclaration(writer, value.function);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one Declaration. */
export function decodeDeclaration(reader: BinaryReader): Declaration {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const class_ = decodeClassDeclaration(reader);

            return { kind: "class", class: class_ };
        }
        case 1: {
            const function_ = decodeFunctionDeclaration(reader);

            return { kind: "function", function: function_ };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one Declaration. */
export function toJsonDeclaration(value: Declaration): Json {
    switch (value.kind) {
        case "class":
            return {
                kind: "class",
                class: toJsonClassDeclaration(value.class),
            };
        case "function":
            return {
                kind: "function",
                function: toJsonFunctionDeclaration(value.function),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one Declaration from one JSON value. */
export function fromJsonDeclaration(value: Json): Declaration {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "class":
            return {
                kind,
                class: fromJsonClassDeclaration(jsonField(object, "class")),
            };
        case "function":
            return {
                kind,
                function: fromJsonFunctionDeclaration(jsonField(object, "function")),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** A function declaration. */
export type FunctionDeclaration = {
    /** The declared name. */
    readonly name?: Name;
    /** The export binding of the declaration. */
    readonly export?: ExportKind;
    /** The function signature. */
    readonly signature: FunctionSignature;
    /** The function body. */
    readonly body: LocalNodeId;
};

export const FunctionDeclaration = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: FunctionDeclaration): void {
        encodeFunctionDeclaration(writer, value);
    },

    /** Decode one FunctionDeclaration. */
    decode(reader: BinaryReader): FunctionDeclaration {
        return decodeFunctionDeclaration(reader);
    },

    /** Return this value as JSON. */
    toJson(value: FunctionDeclaration): Json {
        return toJsonFunctionDeclaration(value);
    },

    /** Return one FunctionDeclaration from one JSON value. */
    fromJson(value: Json): FunctionDeclaration {
        return fromJsonFunctionDeclaration(value);
    },
};

/** Encode one FunctionDeclaration. */
export function encodeFunctionDeclaration(writer: BinaryWriter, value: FunctionDeclaration): void {
    writer.writeOption(value.name, (value0) => {
        encodeName(writer, value0);
    });
    writer.writeOption(value.export, (value1) => {
        encodeExportKind(writer, value1);
    });
    encodeFunctionSignature(writer, value.signature);
    encodeLocalNodeId(writer, value.body);
}

/** Decode one FunctionDeclaration. */
export function decodeFunctionDeclaration(reader: BinaryReader): FunctionDeclaration {
    const name = reader.readOption(() => decodeName(reader));
    const export_ = reader.readOption(() => decodeExportKind(reader));
    const signature = decodeFunctionSignature(reader);
    const body = decodeLocalNodeId(reader);

    return {
        ...(name === undefined ? {} : { name }),
        ...(export_ === undefined ? {} : { export: export_ }),
        signature,
        body,
    };
}

/** Return one JSON value for one FunctionDeclaration. */
export function toJsonFunctionDeclaration(value: FunctionDeclaration): Json {
    return {
        ...(value.name === undefined ? {} : { name: toJsonName(value.name) }),
        ...(value.export === undefined ? {} : { export: toJsonExportKind(value.export) }),
        signature: toJsonFunctionSignature(value.signature),
        body: toJsonLocalNodeId(value.body),
    };
}

/** Return one FunctionDeclaration from one JSON value. */
export function fromJsonFunctionDeclaration(value: Json): FunctionDeclaration {
    const object = jsonObject(value);

    return {
        name: jsonOptional(object, "name", (value) => fromJsonName(value)),
        export: jsonOptional(object, "export", (value) => fromJsonExportKind(value)),
        signature: fromJsonFunctionSignature(jsonField(object, "signature")),
        body: fromJsonLocalNodeId(jsonField(object, "body")),
    };
}
