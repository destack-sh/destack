/* generated by destack_client_generate, do not edit */

import {
    BinaryReader,
    BinaryWriter,
    Json,
    SerdeError,
    jsonArray,
    jsonBool,
    jsonField,
    jsonObject,
    jsonOptional,
    jsonString,
} from "../../../protocol/serde.js";
import type { StringId } from "../../core/string.js";
import type { Asynchrony } from "./node.js";
import type { LocalNodeId } from "./node.js";
import type { Mutability } from "./node.js";
import type { AssignOperator } from "./operator.js";
import type { ModuleId } from "../../source/file/model/module.js";
import {
    decodeStringId,
    encodeStringId,
    fromJsonStringId,
    toJsonStringId,
} from "../../core/string.js";
import {
    decodeAsynchrony,
    encodeAsynchrony,
    fromJsonAsynchrony,
    toJsonAsynchrony,
} from "./node.js";
import {
    decodeLocalNodeId,
    encodeLocalNodeId,
    fromJsonLocalNodeId,
    toJsonLocalNodeId,
} from "./node.js";
import {
    decodeMutability,
    encodeMutability,
    fromJsonMutability,
    toJsonMutability,
} from "./node.js";
import {
    decodeAssignOperator,
    encodeAssignOperator,
    fromJsonAssignOperator,
    toJsonAssignOperator,
} from "./operator.js";
import {
    decodeModuleId,
    encodeModuleId,
    fromJsonModuleId,
    toJsonModuleId,
} from "../../source/file/model/module.js";

/** The binding keyword used by a for each binding. */
export type BindingKeyword = "var" | "let" | "const";

export const BindingKeyword = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: BindingKeyword): void {
        encodeBindingKeyword(writer, value);
    },

    /** Decode one BindingKeyword. */
    decode(reader: BinaryReader): BindingKeyword {
        return decodeBindingKeyword(reader);
    },

    /** Return this value as JSON. */
    toJson(value: BindingKeyword): Json {
        return toJsonBindingKeyword(value);
    },

    /** Return one BindingKeyword from one JSON value. */
    fromJson(value: Json): BindingKeyword {
        return fromJsonBindingKeyword(value);
    },
};

/** Encode one BindingKeyword. */
export function encodeBindingKeyword(writer: BinaryWriter, value: BindingKeyword): void {
    switch (value) {
        case "var":
            writer.writeUnsigned(0);
            return;
        case "let":
            writer.writeUnsigned(1);
            return;
        case "const":
            writer.writeUnsigned(2);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one BindingKeyword. */
export function decodeBindingKeyword(reader: BinaryReader): BindingKeyword {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "var";
        case 1:
            return "let";
        case 2:
            return "const";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one BindingKeyword. */
export function toJsonBindingKeyword(value: BindingKeyword): Json {
    return value;
}

/** Return one BindingKeyword from one JSON value. */
export function fromJsonBindingKeyword(value: Json): BindingKeyword {
    const variant = jsonString(value);

    switch (variant) {
        case "var":
            return "var";
        case "let":
            return "let";
        case "const":
            return "const";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** One dependency attribute clause. */
export type DependencyAttributeClause = {
    /** The clause introducer. */
    readonly kind: DependencyAttributeClauseKind;
    /** The attribute entries inside the clause body. */
    readonly properties: ReadonlyArray<LocalNodeId>;
};

export const DependencyAttributeClause = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: DependencyAttributeClause): void {
        encodeDependencyAttributeClause(writer, value);
    },

    /** Decode one DependencyAttributeClause. */
    decode(reader: BinaryReader): DependencyAttributeClause {
        return decodeDependencyAttributeClause(reader);
    },

    /** Return this value as JSON. */
    toJson(value: DependencyAttributeClause): Json {
        return toJsonDependencyAttributeClause(value);
    },

    /** Return one DependencyAttributeClause from one JSON value. */
    fromJson(value: Json): DependencyAttributeClause {
        return fromJsonDependencyAttributeClause(value);
    },
};

/** Encode one DependencyAttributeClause. */
export function encodeDependencyAttributeClause(
    writer: BinaryWriter,
    value: DependencyAttributeClause,
): void {
    encodeDependencyAttributeClauseKind(writer, value.kind);
    writer.writeUnsigned(value.properties.length);
    for (const item1 of value.properties) {
        encodeLocalNodeId(writer, item1);
    }
}

/** Decode one DependencyAttributeClause. */
export function decodeDependencyAttributeClause(reader: BinaryReader): DependencyAttributeClause {
    const kind = decodeDependencyAttributeClauseKind(reader);
    const properties = (() => {
        const length1 = reader.readNumber();
        const items1: Array<LocalNodeId> = [];
        for (let index = 0; index < length1; index += 1) {
            items1.push(decodeLocalNodeId(reader));
        }
        return items1;
    })();

    return {
        kind,
        properties,
    };
}

/** Return one JSON value for one DependencyAttributeClause. */
export function toJsonDependencyAttributeClause(value: DependencyAttributeClause): Json {
    return {
        kind: toJsonDependencyAttributeClauseKind(value.kind),
        properties: value.properties.map((item0) => toJsonLocalNodeId(item0)),
    };
}

/** Return one DependencyAttributeClause from one JSON value. */
export function fromJsonDependencyAttributeClause(value: Json): DependencyAttributeClause {
    const object = jsonObject(value);

    return {
        kind: fromJsonDependencyAttributeClauseKind(jsonField(object, "kind")),
        properties: jsonArray(jsonField(object, "properties")).map((item0) =>
            fromJsonLocalNodeId(item0),
        ),
    };
}

/** The kind of one dependency attribute clause. */
export type DependencyAttributeClauseKind = "with" | "assert";

export const DependencyAttributeClauseKind = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: DependencyAttributeClauseKind): void {
        encodeDependencyAttributeClauseKind(writer, value);
    },

    /** Decode one DependencyAttributeClauseKind. */
    decode(reader: BinaryReader): DependencyAttributeClauseKind {
        return decodeDependencyAttributeClauseKind(reader);
    },

    /** Return this value as JSON. */
    toJson(value: DependencyAttributeClauseKind): Json {
        return toJsonDependencyAttributeClauseKind(value);
    },

    /** Return one DependencyAttributeClauseKind from one JSON value. */
    fromJson(value: Json): DependencyAttributeClauseKind {
        return fromJsonDependencyAttributeClauseKind(value);
    },
};

/** Encode one DependencyAttributeClauseKind. */
export function encodeDependencyAttributeClauseKind(
    writer: BinaryWriter,
    value: DependencyAttributeClauseKind,
): void {
    switch (value) {
        case "with":
            writer.writeUnsigned(0);
            return;
        case "assert":
            writer.writeUnsigned(1);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one DependencyAttributeClauseKind. */
export function decodeDependencyAttributeClauseKind(
    reader: BinaryReader,
): DependencyAttributeClauseKind {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "with";
        case 1:
            return "assert";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one DependencyAttributeClauseKind. */
export function toJsonDependencyAttributeClauseKind(value: DependencyAttributeClauseKind): Json {
    return value;
}

/** Return one DependencyAttributeClauseKind from one JSON value. */
export function fromJsonDependencyAttributeClauseKind(value: Json): DependencyAttributeClauseKind {
    const variant = jsonString(value);

    switch (variant) {
        case "with":
            return "with";
        case "assert":
            return "assert";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** The initializer of one for statement. */
export type ForInitialization =
    /** One expression initializer. */
    | {
          readonly kind: "expression";
          readonly expression: LocalNodeId;
      }
    /** One declaration initializer. */
    | {
          readonly kind: "declaration";
          readonly keyword: BindingKeyword;
          readonly declarators: ReadonlyArray<LocalNodeId>;
      };

export const ForInitialization = {
    /** One expression initializer. */
    expression(expression: LocalNodeId): ForInitialization {
        return { kind: "expression", expression };
    },

    /** One declaration initializer. */
    declaration(
        keyword: BindingKeyword,
        declarators: ReadonlyArray<LocalNodeId>,
    ): ForInitialization {
        return { kind: "declaration", keyword, declarators };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: ForInitialization): void {
        encodeForInitialization(writer, value);
    },

    /** Decode one ForInitialization. */
    decode(reader: BinaryReader): ForInitialization {
        return decodeForInitialization(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ForInitialization): Json {
        return toJsonForInitialization(value);
    },

    /** Return one ForInitialization from one JSON value. */
    fromJson(value: Json): ForInitialization {
        return fromJsonForInitialization(value);
    },
};

/** Encode one ForInitialization. */
export function encodeForInitialization(writer: BinaryWriter, value: ForInitialization): void {
    switch (value.kind) {
        case "expression":
            writer.writeUnsigned(0);
            encodeLocalNodeId(writer, value.expression);
            return;
        case "declaration":
            writer.writeUnsigned(1);
            encodeBindingKeyword(writer, value.keyword);
            writer.writeUnsigned(value.declarators.length);
            for (const item1 of value.declarators) {
                encodeLocalNodeId(writer, item1);
            }
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one ForInitialization. */
export function decodeForInitialization(reader: BinaryReader): ForInitialization {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const expression = decodeLocalNodeId(reader);

            return { kind: "expression", expression };
        }
        case 1: {
            const keyword = decodeBindingKeyword(reader);
            const declarators = (() => {
                const length1 = reader.readNumber();
                const items1: Array<LocalNodeId> = [];
                for (let index = 0; index < length1; index += 1) {
                    items1.push(decodeLocalNodeId(reader));
                }
                return items1;
            })();

            return {
                kind: "declaration",
                keyword,
                declarators,
            };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one ForInitialization. */
export function toJsonForInitialization(value: ForInitialization): Json {
    switch (value.kind) {
        case "expression":
            return {
                kind: "expression",
                expression: toJsonLocalNodeId(value.expression),
            };
        case "declaration":
            return {
                kind: "declaration",
                keyword: toJsonBindingKeyword(value.keyword),
                declarators: value.declarators.map((item0) => toJsonLocalNodeId(item0)),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one ForInitialization from one JSON value. */
export function fromJsonForInitialization(value: Json): ForInitialization {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "expression":
            return {
                kind,
                expression: fromJsonLocalNodeId(jsonField(object, "expression")),
            };
        case "declaration":
            return {
                kind,
                keyword: fromJsonBindingKeyword(jsonField(object, "keyword")),
                declarators: jsonArray(jsonField(object, "declarators")).map((item0) =>
                    fromJsonLocalNodeId(item0),
                ),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** A JavaScript statement in some container or block. */
export type Statement =
    /** Import items. */
    | {
          readonly kind: "import";
          readonly target: StringId;
          readonly targetModule?: ModuleId;
          readonly items?: ReadonlyArray<LocalNodeId>;
          readonly attributes?: DependencyAttributeClause;
      }
    /** Export items. */
    | {
          readonly kind: "export";
          readonly target?: StringId;
          readonly targetModule?: ModuleId;
          readonly items: ReadonlyArray<LocalNodeId>;
          readonly attributes?: DependencyAttributeClause;
      }
    /** Default export. */
    | {
          readonly kind: "exportDefault";
          readonly value: LocalNodeId;
      }
    /** Declaration statement. */
    | {
          readonly kind: "declaration";
          readonly declaration: LocalNodeId;
      }
    /** Block of statements. */
    | {
          readonly kind: "block";
          readonly block: LocalNodeId;
      }
    /** Labelled statement (like `label: stmt`). */
    | {
          readonly kind: "labelled";
          readonly label: StringId;
          readonly body: LocalNodeId;
      }
    /** Let binding. */
    | {
          readonly kind: "let";
          readonly isExported: boolean;
          readonly mutability: Mutability;
          readonly declarators: ReadonlyArray<LocalNodeId>;
      }
    /** Var binding. */
    | {
          readonly kind: "var";
          readonly isExported: boolean;
          readonly declarators: ReadonlyArray<LocalNodeId>;
      }
    /** Using binding. */
    | {
          readonly kind: "using";
          readonly asynchrony: Asynchrony;
          readonly isExported: boolean;
          readonly declarators: ReadonlyArray<LocalNodeId>;
      }
    /** Assignment operation. */
    | {
          readonly kind: "assign";
          readonly left: LocalNodeId;
          readonly operator: AssignOperator;
          readonly right: LocalNodeId;
      }
    /** Expression statement. */
    | {
          readonly kind: "expression";
          readonly expression: LocalNodeId;
      }
    /** If statement. */
    | {
          readonly kind: "if";
          readonly condition: LocalNodeId;
          readonly thenBlock: LocalNodeId;
          readonly elseBlock?: LocalNodeId;
      }
    /** While statement. */
    | {
          readonly kind: "while";
          readonly condition: LocalNodeId;
          readonly body: LocalNodeId;
      }
    /** Do while statement. */
    | {
          readonly kind: "doWhile";
          readonly body: LocalNodeId;
          readonly condition: LocalNodeId;
      }
    /** For statement. */
    | {
          readonly kind: "for";
          readonly initialization?: ForInitialization;
          readonly condition?: LocalNodeId;
          readonly increment?: LocalNodeId;
          readonly body: LocalNodeId;
      }
    /** For of statement. */
    | {
          readonly kind: "forOf";
          readonly asynchrony: Asynchrony;
          readonly keyword?: BindingKeyword;
          readonly pattern: LocalNodeId;
          readonly iterator: LocalNodeId;
          readonly body: LocalNodeId;
      }
    /** Switch statement. */
    | {
          readonly kind: "switch";
          readonly value: LocalNodeId;
          readonly cases: ReadonlyArray<LocalNodeId>;
      }
    /** Try statement. */
    | {
          readonly kind: "try";
          readonly tryBlock: LocalNodeId;
          readonly catchClause?: LocalNodeId;
          readonly finallyBlock?: LocalNodeId;
      }
    /** Throw statement. */
    | {
          readonly kind: "throw";
          readonly value: LocalNodeId;
      }
    /** Continue statement. */
    | {
          readonly kind: "continue";
          readonly label?: StringId;
      }
    /** Break statement. */
    | {
          readonly kind: "break";
          readonly label?: StringId;
      }
    /** Return statement. */
    | {
          readonly kind: "return";
          readonly value?: LocalNodeId;
      }
    /** Debugger statement. */
    | {
          readonly kind: "debugger";
      };

export const Statement = {
    /** Import items. */
    import(
        target: StringId,
        targetModule: ModuleId | undefined,
        items: ReadonlyArray<LocalNodeId> | undefined,
        attributes: DependencyAttributeClause | undefined,
    ): Statement {
        return { kind: "import", target, targetModule, items, attributes };
    },

    /** Export items. */
    export(
        target: StringId | undefined,
        targetModule: ModuleId | undefined,
        items: ReadonlyArray<LocalNodeId>,
        attributes: DependencyAttributeClause | undefined,
    ): Statement {
        return { kind: "export", target, targetModule, items, attributes };
    },

    /** Default export. */
    exportDefault(value: LocalNodeId): Statement {
        return { kind: "exportDefault", value };
    },

    /** Declaration statement. */
    declaration(declaration: LocalNodeId): Statement {
        return { kind: "declaration", declaration };
    },

    /** Block of statements. */
    block(block: LocalNodeId): Statement {
        return { kind: "block", block };
    },

    /** Labelled statement (like `label: stmt`). */
    labelled(label: StringId, body: LocalNodeId): Statement {
        return { kind: "labelled", label, body };
    },

    /** Let binding. */
    let(
        isExported: boolean,
        mutability: Mutability,
        declarators: ReadonlyArray<LocalNodeId>,
    ): Statement {
        return { kind: "let", isExported, mutability, declarators };
    },

    /** Var binding. */
    var(isExported: boolean, declarators: ReadonlyArray<LocalNodeId>): Statement {
        return { kind: "var", isExported, declarators };
    },

    /** Using binding. */
    using(
        asynchrony: Asynchrony,
        isExported: boolean,
        declarators: ReadonlyArray<LocalNodeId>,
    ): Statement {
        return { kind: "using", asynchrony, isExported, declarators };
    },

    /** Assignment operation. */
    assign(left: LocalNodeId, operator: AssignOperator, right: LocalNodeId): Statement {
        return { kind: "assign", left, operator, right };
    },

    /** Expression statement. */
    expression(expression: LocalNodeId): Statement {
        return { kind: "expression", expression };
    },

    /** If statement. */
    if(
        condition: LocalNodeId,
        thenBlock: LocalNodeId,
        elseBlock: LocalNodeId | undefined,
    ): Statement {
        return { kind: "if", condition, thenBlock, elseBlock };
    },

    /** While statement. */
    while(condition: LocalNodeId, body: LocalNodeId): Statement {
        return { kind: "while", condition, body };
    },

    /** Do while statement. */
    doWhile(body: LocalNodeId, condition: LocalNodeId): Statement {
        return { kind: "doWhile", body, condition };
    },

    /** For statement. */
    for(
        initialization: ForInitialization | undefined,
        condition: LocalNodeId | undefined,
        increment: LocalNodeId | undefined,
        body: LocalNodeId,
    ): Statement {
        return { kind: "for", initialization, condition, increment, body };
    },

    /** For of statement. */
    forOf(
        asynchrony: Asynchrony,
        keyword: BindingKeyword | undefined,
        pattern: LocalNodeId,
        iterator: LocalNodeId,
        body: LocalNodeId,
    ): Statement {
        return { kind: "forOf", asynchrony, keyword, pattern, iterator, body };
    },

    /** Switch statement. */
    switch(value: LocalNodeId, cases: ReadonlyArray<LocalNodeId>): Statement {
        return { kind: "switch", value, cases };
    },

    /** Try statement. */
    try(
        tryBlock: LocalNodeId,
        catchClause: LocalNodeId | undefined,
        finallyBlock: LocalNodeId | undefined,
    ): Statement {
        return { kind: "try", tryBlock, catchClause, finallyBlock };
    },

    /** Throw statement. */
    throw(value: LocalNodeId): Statement {
        return { kind: "throw", value };
    },

    /** Continue statement. */
    continue(label: StringId | undefined): Statement {
        return { kind: "continue", label };
    },

    /** Break statement. */
    break(label: StringId | undefined): Statement {
        return { kind: "break", label };
    },

    /** Return statement. */
    return(value: LocalNodeId | undefined): Statement {
        return { kind: "return", value };
    },

    /** Debugger statement. */
    debugger(): Statement {
        return { kind: "debugger" };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: Statement): void {
        encodeStatement(writer, value);
    },

    /** Decode one Statement. */
    decode(reader: BinaryReader): Statement {
        return decodeStatement(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Statement): Json {
        return toJsonStatement(value);
    },

    /** Return one Statement from one JSON value. */
    fromJson(value: Json): Statement {
        return fromJsonStatement(value);
    },
};

/** Encode one Statement. */
export function encodeStatement(writer: BinaryWriter, value: Statement): void {
    switch (value.kind) {
        case "import":
            writer.writeUnsigned(0);
            encodeStringId(writer, value.target);
            writer.writeOption(value.targetModule, (value1) => {
                encodeModuleId(writer, value1);
            });
            writer.writeOption(value.items, (value2) => {
                writer.writeUnsigned(value2.length);
                for (const item3 of value2) {
                    encodeLocalNodeId(writer, item3);
                }
            });
            writer.writeOption(value.attributes, (value3) => {
                encodeDependencyAttributeClause(writer, value3);
            });
            return;
        case "export":
            writer.writeUnsigned(1);
            writer.writeOption(value.target, (value0) => {
                encodeStringId(writer, value0);
            });
            writer.writeOption(value.targetModule, (value1) => {
                encodeModuleId(writer, value1);
            });
            writer.writeUnsigned(value.items.length);
            for (const item2 of value.items) {
                encodeLocalNodeId(writer, item2);
            }
            writer.writeOption(value.attributes, (value3) => {
                encodeDependencyAttributeClause(writer, value3);
            });
            return;
        case "exportDefault":
            writer.writeUnsigned(2);
            encodeLocalNodeId(writer, value.value);
            return;
        case "declaration":
            writer.writeUnsigned(3);
            encodeLocalNodeId(writer, value.declaration);
            return;
        case "block":
            writer.writeUnsigned(4);
            encodeLocalNodeId(writer, value.block);
            return;
        case "labelled":
            writer.writeUnsigned(5);
            encodeStringId(writer, value.label);
            encodeLocalNodeId(writer, value.body);
            return;
        case "let":
            writer.writeUnsigned(6);
            writer.writeBool(value.isExported);
            encodeMutability(writer, value.mutability);
            writer.writeUnsigned(value.declarators.length);
            for (const item2 of value.declarators) {
                encodeLocalNodeId(writer, item2);
            }
            return;
        case "var":
            writer.writeUnsigned(7);
            writer.writeBool(value.isExported);
            writer.writeUnsigned(value.declarators.length);
            for (const item1 of value.declarators) {
                encodeLocalNodeId(writer, item1);
            }
            return;
        case "using":
            writer.writeUnsigned(8);
            encodeAsynchrony(writer, value.asynchrony);
            writer.writeBool(value.isExported);
            writer.writeUnsigned(value.declarators.length);
            for (const item2 of value.declarators) {
                encodeLocalNodeId(writer, item2);
            }
            return;
        case "assign":
            writer.writeUnsigned(9);
            encodeLocalNodeId(writer, value.left);
            encodeAssignOperator(writer, value.operator);
            encodeLocalNodeId(writer, value.right);
            return;
        case "expression":
            writer.writeUnsigned(10);
            encodeLocalNodeId(writer, value.expression);
            return;
        case "if":
            writer.writeUnsigned(11);
            encodeLocalNodeId(writer, value.condition);
            encodeLocalNodeId(writer, value.thenBlock);
            writer.writeOption(value.elseBlock, (value2) => {
                encodeLocalNodeId(writer, value2);
            });
            return;
        case "while":
            writer.writeUnsigned(12);
            encodeLocalNodeId(writer, value.condition);
            encodeLocalNodeId(writer, value.body);
            return;
        case "doWhile":
            writer.writeUnsigned(13);
            encodeLocalNodeId(writer, value.body);
            encodeLocalNodeId(writer, value.condition);
            return;
        case "for":
            writer.writeUnsigned(14);
            writer.writeOption(value.initialization, (value0) => {
                encodeForInitialization(writer, value0);
            });
            writer.writeOption(value.condition, (value1) => {
                encodeLocalNodeId(writer, value1);
            });
            writer.writeOption(value.increment, (value2) => {
                encodeLocalNodeId(writer, value2);
            });
            encodeLocalNodeId(writer, value.body);
            return;
        case "forOf":
            writer.writeUnsigned(15);
            encodeAsynchrony(writer, value.asynchrony);
            writer.writeOption(value.keyword, (value1) => {
                encodeBindingKeyword(writer, value1);
            });
            encodeLocalNodeId(writer, value.pattern);
            encodeLocalNodeId(writer, value.iterator);
            encodeLocalNodeId(writer, value.body);
            return;
        case "switch":
            writer.writeUnsigned(16);
            encodeLocalNodeId(writer, value.value);
            writer.writeUnsigned(value.cases.length);
            for (const item1 of value.cases) {
                encodeLocalNodeId(writer, item1);
            }
            return;
        case "try":
            writer.writeUnsigned(17);
            encodeLocalNodeId(writer, value.tryBlock);
            writer.writeOption(value.catchClause, (value1) => {
                encodeLocalNodeId(writer, value1);
            });
            writer.writeOption(value.finallyBlock, (value2) => {
                encodeLocalNodeId(writer, value2);
            });
            return;
        case "throw":
            writer.writeUnsigned(18);
            encodeLocalNodeId(writer, value.value);
            return;
        case "continue":
            writer.writeUnsigned(19);
            writer.writeOption(value.label, (value0) => {
                encodeStringId(writer, value0);
            });
            return;
        case "break":
            writer.writeUnsigned(20);
            writer.writeOption(value.label, (value0) => {
                encodeStringId(writer, value0);
            });
            return;
        case "return":
            writer.writeUnsigned(21);
            writer.writeOption(value.value, (value0) => {
                encodeLocalNodeId(writer, value0);
            });
            return;
        case "debugger":
            writer.writeUnsigned(22);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one Statement. */
export function decodeStatement(reader: BinaryReader): Statement {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const target = decodeStringId(reader);
            const targetModule = reader.readOption(() => decodeModuleId(reader));
            const items = reader.readOption(() =>
                (() => {
                    const length3 = reader.readNumber();
                    const items3: Array<LocalNodeId> = [];
                    for (let index = 0; index < length3; index += 1) {
                        items3.push(decodeLocalNodeId(reader));
                    }
                    return items3;
                })(),
            );
            const attributes = reader.readOption(() => decodeDependencyAttributeClause(reader));

            return {
                kind: "import",
                target,
                ...(targetModule === undefined ? {} : { targetModule }),
                ...(items === undefined ? {} : { items }),
                ...(attributes === undefined ? {} : { attributes }),
            };
        }
        case 1: {
            const target = reader.readOption(() => decodeStringId(reader));
            const targetModule = reader.readOption(() => decodeModuleId(reader));
            const items = (() => {
                const length2 = reader.readNumber();
                const items2: Array<LocalNodeId> = [];
                for (let index = 0; index < length2; index += 1) {
                    items2.push(decodeLocalNodeId(reader));
                }
                return items2;
            })();
            const attributes = reader.readOption(() => decodeDependencyAttributeClause(reader));

            return {
                kind: "export",
                ...(target === undefined ? {} : { target }),
                ...(targetModule === undefined ? {} : { targetModule }),
                items,
                ...(attributes === undefined ? {} : { attributes }),
            };
        }
        case 2: {
            const value = decodeLocalNodeId(reader);

            return {
                kind: "exportDefault",
                value,
            };
        }
        case 3: {
            const declaration = decodeLocalNodeId(reader);

            return {
                kind: "declaration",
                declaration,
            };
        }
        case 4: {
            const block = decodeLocalNodeId(reader);

            return {
                kind: "block",
                block,
            };
        }
        case 5: {
            const label = decodeStringId(reader);
            const body = decodeLocalNodeId(reader);

            return {
                kind: "labelled",
                label,
                body,
            };
        }
        case 6: {
            const isExported = reader.readBool();
            const mutability = decodeMutability(reader);
            const declarators = (() => {
                const length2 = reader.readNumber();
                const items2: Array<LocalNodeId> = [];
                for (let index = 0; index < length2; index += 1) {
                    items2.push(decodeLocalNodeId(reader));
                }
                return items2;
            })();

            return {
                kind: "let",
                isExported,
                mutability,
                declarators,
            };
        }
        case 7: {
            const isExported = reader.readBool();
            const declarators = (() => {
                const length1 = reader.readNumber();
                const items1: Array<LocalNodeId> = [];
                for (let index = 0; index < length1; index += 1) {
                    items1.push(decodeLocalNodeId(reader));
                }
                return items1;
            })();

            return {
                kind: "var",
                isExported,
                declarators,
            };
        }
        case 8: {
            const asynchrony = decodeAsynchrony(reader);
            const isExported = reader.readBool();
            const declarators = (() => {
                const length2 = reader.readNumber();
                const items2: Array<LocalNodeId> = [];
                for (let index = 0; index < length2; index += 1) {
                    items2.push(decodeLocalNodeId(reader));
                }
                return items2;
            })();

            return {
                kind: "using",
                asynchrony,
                isExported,
                declarators,
            };
        }
        case 9: {
            const left = decodeLocalNodeId(reader);
            const operator = decodeAssignOperator(reader);
            const right = decodeLocalNodeId(reader);

            return {
                kind: "assign",
                left,
                operator,
                right,
            };
        }
        case 10: {
            const expression = decodeLocalNodeId(reader);

            return {
                kind: "expression",
                expression,
            };
        }
        case 11: {
            const condition = decodeLocalNodeId(reader);
            const thenBlock = decodeLocalNodeId(reader);
            const elseBlock = reader.readOption(() => decodeLocalNodeId(reader));

            return {
                kind: "if",
                condition,
                thenBlock,
                ...(elseBlock === undefined ? {} : { elseBlock }),
            };
        }
        case 12: {
            const condition = decodeLocalNodeId(reader);
            const body = decodeLocalNodeId(reader);

            return {
                kind: "while",
                condition,
                body,
            };
        }
        case 13: {
            const body = decodeLocalNodeId(reader);
            const condition = decodeLocalNodeId(reader);

            return {
                kind: "doWhile",
                body,
                condition,
            };
        }
        case 14: {
            const initialization = reader.readOption(() => decodeForInitialization(reader));
            const condition = reader.readOption(() => decodeLocalNodeId(reader));
            const increment = reader.readOption(() => decodeLocalNodeId(reader));
            const body = decodeLocalNodeId(reader);

            return {
                kind: "for",
                ...(initialization === undefined ? {} : { initialization }),
                ...(condition === undefined ? {} : { condition }),
                ...(increment === undefined ? {} : { increment }),
                body,
            };
        }
        case 15: {
            const asynchrony = decodeAsynchrony(reader);
            const keyword = reader.readOption(() => decodeBindingKeyword(reader));
            const pattern = decodeLocalNodeId(reader);
            const iterator = decodeLocalNodeId(reader);
            const body = decodeLocalNodeId(reader);

            return {
                kind: "forOf",
                asynchrony,
                ...(keyword === undefined ? {} : { keyword }),
                pattern,
                iterator,
                body,
            };
        }
        case 16: {
            const value = decodeLocalNodeId(reader);
            const cases = (() => {
                const length1 = reader.readNumber();
                const items1: Array<LocalNodeId> = [];
                for (let index = 0; index < length1; index += 1) {
                    items1.push(decodeLocalNodeId(reader));
                }
                return items1;
            })();

            return {
                kind: "switch",
                value,
                cases,
            };
        }
        case 17: {
            const tryBlock = decodeLocalNodeId(reader);
            const catchClause = reader.readOption(() => decodeLocalNodeId(reader));
            const finallyBlock = reader.readOption(() => decodeLocalNodeId(reader));

            return {
                kind: "try",
                tryBlock,
                ...(catchClause === undefined ? {} : { catchClause }),
                ...(finallyBlock === undefined ? {} : { finallyBlock }),
            };
        }
        case 18: {
            const value = decodeLocalNodeId(reader);

            return {
                kind: "throw",
                value,
            };
        }
        case 19: {
            const label = reader.readOption(() => decodeStringId(reader));

            return {
                kind: "continue",
                ...(label === undefined ? {} : { label }),
            };
        }
        case 20: {
            const label = reader.readOption(() => decodeStringId(reader));

            return {
                kind: "break",
                ...(label === undefined ? {} : { label }),
            };
        }
        case 21: {
            const value = reader.readOption(() => decodeLocalNodeId(reader));

            return {
                kind: "return",
                ...(value === undefined ? {} : { value }),
            };
        }
        case 22: {
            return { kind: "debugger" };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one Statement. */
export function toJsonStatement(value: Statement): Json {
    switch (value.kind) {
        case "import":
            return {
                kind: "import",
                target: toJsonStringId(value.target),
                ...(value.targetModule === undefined
                    ? {}
                    : { targetModule: toJsonModuleId(value.targetModule) }),
                ...(value.items === undefined
                    ? {}
                    : { items: value.items.map((item0) => toJsonLocalNodeId(item0)) }),
                ...(value.attributes === undefined
                    ? {}
                    : { attributes: toJsonDependencyAttributeClause(value.attributes) }),
            };
        case "export":
            return {
                kind: "export",
                ...(value.target === undefined ? {} : { target: toJsonStringId(value.target) }),
                ...(value.targetModule === undefined
                    ? {}
                    : { targetModule: toJsonModuleId(value.targetModule) }),
                items: value.items.map((item0) => toJsonLocalNodeId(item0)),
                ...(value.attributes === undefined
                    ? {}
                    : { attributes: toJsonDependencyAttributeClause(value.attributes) }),
            };
        case "exportDefault":
            return {
                kind: "exportDefault",
                value: toJsonLocalNodeId(value.value),
            };
        case "declaration":
            return {
                kind: "declaration",
                declaration: toJsonLocalNodeId(value.declaration),
            };
        case "block":
            return {
                kind: "block",
                block: toJsonLocalNodeId(value.block),
            };
        case "labelled":
            return {
                kind: "labelled",
                label: toJsonStringId(value.label),
                body: toJsonLocalNodeId(value.body),
            };
        case "let":
            return {
                kind: "let",
                isExported: value.isExported,
                mutability: toJsonMutability(value.mutability),
                declarators: value.declarators.map((item0) => toJsonLocalNodeId(item0)),
            };
        case "var":
            return {
                kind: "var",
                isExported: value.isExported,
                declarators: value.declarators.map((item0) => toJsonLocalNodeId(item0)),
            };
        case "using":
            return {
                kind: "using",
                asynchrony: toJsonAsynchrony(value.asynchrony),
                isExported: value.isExported,
                declarators: value.declarators.map((item0) => toJsonLocalNodeId(item0)),
            };
        case "assign":
            return {
                kind: "assign",
                left: toJsonLocalNodeId(value.left),
                operator: toJsonAssignOperator(value.operator),
                right: toJsonLocalNodeId(value.right),
            };
        case "expression":
            return {
                kind: "expression",
                expression: toJsonLocalNodeId(value.expression),
            };
        case "if":
            return {
                kind: "if",
                condition: toJsonLocalNodeId(value.condition),
                thenBlock: toJsonLocalNodeId(value.thenBlock),
                ...(value.elseBlock === undefined
                    ? {}
                    : { elseBlock: toJsonLocalNodeId(value.elseBlock) }),
            };
        case "while":
            return {
                kind: "while",
                condition: toJsonLocalNodeId(value.condition),
                body: toJsonLocalNodeId(value.body),
            };
        case "doWhile":
            return {
                kind: "doWhile",
                body: toJsonLocalNodeId(value.body),
                condition: toJsonLocalNodeId(value.condition),
            };
        case "for":
            return {
                kind: "for",
                ...(value.initialization === undefined
                    ? {}
                    : { initialization: toJsonForInitialization(value.initialization) }),
                ...(value.condition === undefined
                    ? {}
                    : { condition: toJsonLocalNodeId(value.condition) }),
                ...(value.increment === undefined
                    ? {}
                    : { increment: toJsonLocalNodeId(value.increment) }),
                body: toJsonLocalNodeId(value.body),
            };
        case "forOf":
            return {
                kind: "forOf",
                asynchrony: toJsonAsynchrony(value.asynchrony),
                ...(value.keyword === undefined
                    ? {}
                    : { keyword: toJsonBindingKeyword(value.keyword) }),
                pattern: toJsonLocalNodeId(value.pattern),
                iterator: toJsonLocalNodeId(value.iterator),
                body: toJsonLocalNodeId(value.body),
            };
        case "switch":
            return {
                kind: "switch",
                value: toJsonLocalNodeId(value.value),
                cases: value.cases.map((item0) => toJsonLocalNodeId(item0)),
            };
        case "try":
            return {
                kind: "try",
                tryBlock: toJsonLocalNodeId(value.tryBlock),
                ...(value.catchClause === undefined
                    ? {}
                    : { catchClause: toJsonLocalNodeId(value.catchClause) }),
                ...(value.finallyBlock === undefined
                    ? {}
                    : { finallyBlock: toJsonLocalNodeId(value.finallyBlock) }),
            };
        case "throw":
            return {
                kind: "throw",
                value: toJsonLocalNodeId(value.value),
            };
        case "continue":
            return {
                kind: "continue",
                ...(value.label === undefined ? {} : { label: toJsonStringId(value.label) }),
            };
        case "break":
            return {
                kind: "break",
                ...(value.label === undefined ? {} : { label: toJsonStringId(value.label) }),
            };
        case "return":
            return {
                kind: "return",
                ...(value.value === undefined ? {} : { value: toJsonLocalNodeId(value.value) }),
            };
        case "debugger":
            return {
                kind: "debugger",
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one Statement from one JSON value. */
export function fromJsonStatement(value: Json): Statement {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "import":
            return {
                kind,
                target: fromJsonStringId(jsonField(object, "target")),
                targetModule: jsonOptional(object, "targetModule", (value) =>
                    fromJsonModuleId(value),
                ),
                items: jsonOptional(object, "items", (value) =>
                    jsonArray(value).map((item0) => fromJsonLocalNodeId(item0)),
                ),
                attributes: jsonOptional(object, "attributes", (value) =>
                    fromJsonDependencyAttributeClause(value),
                ),
            };
        case "export":
            return {
                kind,
                target: jsonOptional(object, "target", (value) => fromJsonStringId(value)),
                targetModule: jsonOptional(object, "targetModule", (value) =>
                    fromJsonModuleId(value),
                ),
                items: jsonArray(jsonField(object, "items")).map((item0) =>
                    fromJsonLocalNodeId(item0),
                ),
                attributes: jsonOptional(object, "attributes", (value) =>
                    fromJsonDependencyAttributeClause(value),
                ),
            };
        case "exportDefault":
            return {
                kind,
                value: fromJsonLocalNodeId(jsonField(object, "value")),
            };
        case "declaration":
            return {
                kind,
                declaration: fromJsonLocalNodeId(jsonField(object, "declaration")),
            };
        case "block":
            return {
                kind,
                block: fromJsonLocalNodeId(jsonField(object, "block")),
            };
        case "labelled":
            return {
                kind,
                label: fromJsonStringId(jsonField(object, "label")),
                body: fromJsonLocalNodeId(jsonField(object, "body")),
            };
        case "let":
            return {
                kind,
                isExported: jsonBool(jsonField(object, "isExported")),
                mutability: fromJsonMutability(jsonField(object, "mutability")),
                declarators: jsonArray(jsonField(object, "declarators")).map((item0) =>
                    fromJsonLocalNodeId(item0),
                ),
            };
        case "var":
            return {
                kind,
                isExported: jsonBool(jsonField(object, "isExported")),
                declarators: jsonArray(jsonField(object, "declarators")).map((item0) =>
                    fromJsonLocalNodeId(item0),
                ),
            };
        case "using":
            return {
                kind,
                asynchrony: fromJsonAsynchrony(jsonField(object, "asynchrony")),
                isExported: jsonBool(jsonField(object, "isExported")),
                declarators: jsonArray(jsonField(object, "declarators")).map((item0) =>
                    fromJsonLocalNodeId(item0),
                ),
            };
        case "assign":
            return {
                kind,
                left: fromJsonLocalNodeId(jsonField(object, "left")),
                operator: fromJsonAssignOperator(jsonField(object, "operator")),
                right: fromJsonLocalNodeId(jsonField(object, "right")),
            };
        case "expression":
            return {
                kind,
                expression: fromJsonLocalNodeId(jsonField(object, "expression")),
            };
        case "if":
            return {
                kind,
                condition: fromJsonLocalNodeId(jsonField(object, "condition")),
                thenBlock: fromJsonLocalNodeId(jsonField(object, "thenBlock")),
                elseBlock: jsonOptional(object, "elseBlock", (value) => fromJsonLocalNodeId(value)),
            };
        case "while":
            return {
                kind,
                condition: fromJsonLocalNodeId(jsonField(object, "condition")),
                body: fromJsonLocalNodeId(jsonField(object, "body")),
            };
        case "doWhile":
            return {
                kind,
                body: fromJsonLocalNodeId(jsonField(object, "body")),
                condition: fromJsonLocalNodeId(jsonField(object, "condition")),
            };
        case "for":
            return {
                kind,
                initialization: jsonOptional(object, "initialization", (value) =>
                    fromJsonForInitialization(value),
                ),
                condition: jsonOptional(object, "condition", (value) => fromJsonLocalNodeId(value)),
                increment: jsonOptional(object, "increment", (value) => fromJsonLocalNodeId(value)),
                body: fromJsonLocalNodeId(jsonField(object, "body")),
            };
        case "forOf":
            return {
                kind,
                asynchrony: fromJsonAsynchrony(jsonField(object, "asynchrony")),
                keyword: jsonOptional(object, "keyword", (value) => fromJsonBindingKeyword(value)),
                pattern: fromJsonLocalNodeId(jsonField(object, "pattern")),
                iterator: fromJsonLocalNodeId(jsonField(object, "iterator")),
                body: fromJsonLocalNodeId(jsonField(object, "body")),
            };
        case "switch":
            return {
                kind,
                value: fromJsonLocalNodeId(jsonField(object, "value")),
                cases: jsonArray(jsonField(object, "cases")).map((item0) =>
                    fromJsonLocalNodeId(item0),
                ),
            };
        case "try":
            return {
                kind,
                tryBlock: fromJsonLocalNodeId(jsonField(object, "tryBlock")),
                catchClause: jsonOptional(object, "catchClause", (value) =>
                    fromJsonLocalNodeId(value),
                ),
                finallyBlock: jsonOptional(object, "finallyBlock", (value) =>
                    fromJsonLocalNodeId(value),
                ),
            };
        case "throw":
            return {
                kind,
                value: fromJsonLocalNodeId(jsonField(object, "value")),
            };
        case "continue":
            return {
                kind,
                label: jsonOptional(object, "label", (value) => fromJsonStringId(value)),
            };
        case "break":
            return {
                kind,
                label: jsonOptional(object, "label", (value) => fromJsonStringId(value)),
            };
        case "return":
            return {
                kind,
                value: jsonOptional(object, "value", (value) => fromJsonLocalNodeId(value)),
            };
        case "debugger":
            return {
                kind,
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}
