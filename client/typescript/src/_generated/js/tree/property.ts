/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, jsonArray, jsonBool, jsonField, jsonObject, jsonOptional, jsonString } from "../../../protocol/serde.js";
import type { MemberModifier } from "./argument.js";
import type { FunctionRole } from "./function.js";
import type { FunctionSignature } from "./function.js";
import type { Key } from "./key.js";
import type { LocalNodeId } from "./node.js";
import { decodeMemberModifier, encodeMemberModifier, fromJsonMemberModifier, toJsonMemberModifier } from "./argument.js";
import { decodeFunctionRole, encodeFunctionRole, fromJsonFunctionRole, toJsonFunctionRole } from "./function.js";
import { decodeFunctionSignature, encodeFunctionSignature, fromJsonFunctionSignature, toJsonFunctionSignature } from "./function.js";
import { decodeKey, encodeKey, fromJsonKey, toJsonKey } from "./key.js";
import { decodeLocalNodeId, encodeLocalNodeId, fromJsonLocalNodeId, toJsonLocalNodeId } from "./node.js";

/** One class member. */
export type Member =
    /** Named field such as `x = value`. */
    | {
          readonly kind: "field";
          readonly modifiers: MemberModifier;
          readonly key: Key;
          readonly default?: LocalNodeId;
      }
    /** Named method. */
    | {
          readonly kind: "method";
          readonly modifiers: MemberModifier;
          readonly key: Key;
          readonly role?: FunctionRole;
          readonly signature: FunctionSignature;
          readonly body: LocalNodeId;
      }
    /** Constructor method. */
    | {
          readonly kind: "constructor";
          readonly parameters: ReadonlyArray<LocalNodeId>;
          readonly body: LocalNodeId;
      }
    /** Static initialization block (like `static { ... }`). */
    | {
          readonly kind: "staticBlock";
          readonly body: LocalNodeId;
      }
;

export const Member = {
    /** Named field such as `x = value`. */
    field(modifiers: MemberModifier, key: Key, default_: LocalNodeId | undefined): Member {
        return { kind: "field", modifiers, key, default: default_ };
    },

    /** Named method. */
    method(modifiers: MemberModifier, key: Key, role: FunctionRole | undefined, signature: FunctionSignature, body: LocalNodeId): Member {
        return { kind: "method", modifiers, key, role, signature, body };
    },

    /** Constructor method. */
    "constructor"(parameters: ReadonlyArray<LocalNodeId>, body: LocalNodeId): Member {
        return { kind: "constructor", parameters, body };
    },

    /** Static initialization block (like `static { ... }`). */
    staticBlock(body: LocalNodeId): Member {
        return { kind: "staticBlock", body };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: Member): void {
        encodeMember(writer, value);
    },

    /** Decode one Member. */
    decode(reader: BinaryReader): Member {
        return decodeMember(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Member): Json {
        return toJsonMember(value);
    },

    /** Return one Member from one JSON value. */
    fromJson(value: Json): Member {
        return fromJsonMember(value);
    },
};

/** Encode one Member. */
export function encodeMember(writer: BinaryWriter, value: Member): void {
    switch (value.kind) {
        case "field":
            writer.writeUnsigned(0);
            encodeMemberModifier(writer, value.modifiers);
            encodeKey(writer, value.key);
            writer.writeOption(value.default, (value2) => {
                encodeLocalNodeId(writer, value2);
            });
            return;
        case "method":
            writer.writeUnsigned(1);
            encodeMemberModifier(writer, value.modifiers);
            encodeKey(writer, value.key);
            writer.writeOption(value.role, (value2) => {
                encodeFunctionRole(writer, value2);
            });
            encodeFunctionSignature(writer, value.signature);
            encodeLocalNodeId(writer, value.body);
            return;
        case "constructor":
            writer.writeUnsigned(2);
            writer.writeUnsigned(value.parameters.length);
            for (const item0 of value.parameters) {
                encodeLocalNodeId(writer, item0);
            }
            encodeLocalNodeId(writer, value.body);
            return;
        case "staticBlock":
            writer.writeUnsigned(3);
            encodeLocalNodeId(writer, value.body);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one Member. */
export function decodeMember(reader: BinaryReader): Member {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const modifiers = decodeMemberModifier(reader);
            const key = decodeKey(reader);
            const default_ = reader.readOption(() => decodeLocalNodeId(reader));

            return {
                kind: "field",
                modifiers,
                key,
                ...(default_ === undefined ? {} : { default: default_ }),
            };
        }
        case 1: {
            const modifiers = decodeMemberModifier(reader);
            const key = decodeKey(reader);
            const role = reader.readOption(() => decodeFunctionRole(reader));
            const signature = decodeFunctionSignature(reader);
            const body = decodeLocalNodeId(reader);

            return {
                kind: "method",
                modifiers,
                key,
                ...(role === undefined ? {} : { role }),
                signature,
                body,
            };
        }
        case 2: {
            const parameters = (() => { const length0 = reader.readNumber(); const items0: Array<LocalNodeId> = []; for (let index = 0; index < length0; index += 1) { items0.push(decodeLocalNodeId(reader)); } return items0; })();
            const body = decodeLocalNodeId(reader);

            return {
                kind: "constructor",
                parameters,
                body,
            };
        }
        case 3: {
            const body = decodeLocalNodeId(reader);

            return {
                kind: "staticBlock",
                body,
            };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one Member. */
export function toJsonMember(value: Member): Json {
    switch (value.kind) {
        case "field":
            return {
                kind: "field",
                modifiers: toJsonMemberModifier(value.modifiers),
                key: toJsonKey(value.key),
                ...(value.default === undefined ? {} : { default: toJsonLocalNodeId(value.default) }),
            };
        case "method":
            return {
                kind: "method",
                modifiers: toJsonMemberModifier(value.modifiers),
                key: toJsonKey(value.key),
                ...(value.role === undefined ? {} : { role: toJsonFunctionRole(value.role) }),
                signature: toJsonFunctionSignature(value.signature),
                body: toJsonLocalNodeId(value.body),
            };
        case "constructor":
            return {
                kind: "constructor",
                parameters: value.parameters.map((item0) => toJsonLocalNodeId(item0)),
                body: toJsonLocalNodeId(value.body),
            };
        case "staticBlock":
            return {
                kind: "staticBlock",
                body: toJsonLocalNodeId(value.body),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one Member from one JSON value. */
export function fromJsonMember(value: Json): Member {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "field":
            return {
                kind,
                modifiers: fromJsonMemberModifier(jsonField(object, "modifiers")),
                key: fromJsonKey(jsonField(object, "key")),
                default: jsonOptional(object, "default", (value) => fromJsonLocalNodeId(value)),
            };
        case "method":
            return {
                kind,
                modifiers: fromJsonMemberModifier(jsonField(object, "modifiers")),
                key: fromJsonKey(jsonField(object, "key")),
                role: jsonOptional(object, "role", (value) => fromJsonFunctionRole(value)),
                signature: fromJsonFunctionSignature(jsonField(object, "signature")),
                body: fromJsonLocalNodeId(jsonField(object, "body")),
            };
        case "constructor":
            return {
                kind,
                parameters: jsonArray(jsonField(object, "parameters")).map((item0) => fromJsonLocalNodeId(item0)),
                body: fromJsonLocalNodeId(jsonField(object, "body")),
            };
        case "staticBlock":
            return {
                kind,
                body: fromJsonLocalNodeId(jsonField(object, "body")),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** A Property is a property of an object literal (may be a field, method, or spread). */
export type Property =
    /** Named field such as `x: value`. */
    | {
          readonly kind: "field";
          readonly key: Key;
          readonly value: LocalNodeId;
          readonly isShorthand: boolean;
      }
    /** Named method such as `foo()`. */
    | {
          readonly kind: "method";
          readonly key: Key;
          readonly role?: FunctionRole;
          readonly signature: FunctionSignature;
          readonly body: LocalNodeId;
      }
    /** Spread property (like `...a`). */
    | {
          readonly kind: "spread";
          readonly value: LocalNodeId;
      }
;

export const Property = {
    /** Named field such as `x: value`. */
    field(key: Key, value: LocalNodeId, isShorthand: boolean): Property {
        return { kind: "field", key, value, isShorthand };
    },

    /** Named method such as `foo()`. */
    method(key: Key, role: FunctionRole | undefined, signature: FunctionSignature, body: LocalNodeId): Property {
        return { kind: "method", key, role, signature, body };
    },

    /** Spread property (like `...a`). */
    spread(value: LocalNodeId): Property {
        return { kind: "spread", value };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: Property): void {
        encodeProperty(writer, value);
    },

    /** Decode one Property. */
    decode(reader: BinaryReader): Property {
        return decodeProperty(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Property): Json {
        return toJsonProperty(value);
    },

    /** Return one Property from one JSON value. */
    fromJson(value: Json): Property {
        return fromJsonProperty(value);
    },
};

/** Encode one Property. */
export function encodeProperty(writer: BinaryWriter, value: Property): void {
    switch (value.kind) {
        case "field":
            writer.writeUnsigned(0);
            encodeKey(writer, value.key);
            encodeLocalNodeId(writer, value.value);
            writer.writeBool(value.isShorthand);
            return;
        case "method":
            writer.writeUnsigned(1);
            encodeKey(writer, value.key);
            writer.writeOption(value.role, (value1) => {
                encodeFunctionRole(writer, value1);
            });
            encodeFunctionSignature(writer, value.signature);
            encodeLocalNodeId(writer, value.body);
            return;
        case "spread":
            writer.writeUnsigned(2);
            encodeLocalNodeId(writer, value.value);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one Property. */
export function decodeProperty(reader: BinaryReader): Property {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const key = decodeKey(reader);
            const value = decodeLocalNodeId(reader);
            const isShorthand = reader.readBool();

            return {
                kind: "field",
                key,
                value,
                isShorthand,
            };
        }
        case 1: {
            const key = decodeKey(reader);
            const role = reader.readOption(() => decodeFunctionRole(reader));
            const signature = decodeFunctionSignature(reader);
            const body = decodeLocalNodeId(reader);

            return {
                kind: "method",
                key,
                ...(role === undefined ? {} : { role }),
                signature,
                body,
            };
        }
        case 2: {
            const value = decodeLocalNodeId(reader);

            return {
                kind: "spread",
                value,
            };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one Property. */
export function toJsonProperty(value: Property): Json {
    switch (value.kind) {
        case "field":
            return {
                kind: "field",
                key: toJsonKey(value.key),
                value: toJsonLocalNodeId(value.value),
                isShorthand: value.isShorthand,
            };
        case "method":
            return {
                kind: "method",
                key: toJsonKey(value.key),
                ...(value.role === undefined ? {} : { role: toJsonFunctionRole(value.role) }),
                signature: toJsonFunctionSignature(value.signature),
                body: toJsonLocalNodeId(value.body),
            };
        case "spread":
            return {
                kind: "spread",
                value: toJsonLocalNodeId(value.value),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one Property from one JSON value. */
export function fromJsonProperty(value: Json): Property {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "field":
            return {
                kind,
                key: fromJsonKey(jsonField(object, "key")),
                value: fromJsonLocalNodeId(jsonField(object, "value")),
                isShorthand: jsonBool(jsonField(object, "isShorthand")),
            };
        case "method":
            return {
                kind,
                key: fromJsonKey(jsonField(object, "key")),
                role: jsonOptional(object, "role", (value) => fromJsonFunctionRole(value)),
                signature: fromJsonFunctionSignature(jsonField(object, "signature")),
                body: fromJsonLocalNodeId(jsonField(object, "body")),
            };
        case "spread":
            return {
                kind,
                value: fromJsonLocalNodeId(jsonField(object, "value")),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}
