/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, jsonField, jsonObject, jsonOptional } from "../../../protocol/serde.js";
import type { LocalNodeId } from "./node.js";
import { decodeLocalNodeId, encodeLocalNodeId, fromJsonLocalNodeId, toJsonLocalNodeId } from "./node.js";

/** A Declarator is an individual variable declaration within a let/const/var statement. */
export type Declarator = {
    /** The pattern to bind (can be a simple identifier or destructuring pattern). */
    readonly pattern: LocalNodeId;
    /** Optional type annotation. */
    readonly ty?: LocalNodeId;
    /** Optional value expression. */
    readonly value?: LocalNodeId;
};

export const Declarator = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: Declarator): void {
        encodeDeclarator(writer, value);
    },

    /** Decode one Declarator. */
    decode(reader: BinaryReader): Declarator {
        return decodeDeclarator(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Declarator): Json {
        return toJsonDeclarator(value);
    },

    /** Return one Declarator from one JSON value. */
    fromJson(value: Json): Declarator {
        return fromJsonDeclarator(value);
    },
};

/** Encode one Declarator. */
export function encodeDeclarator(writer: BinaryWriter, value: Declarator): void {
    encodeLocalNodeId(writer, value.pattern);
    writer.writeOption(value.ty, (value1) => {
        encodeLocalNodeId(writer, value1);
    });
    writer.writeOption(value.value, (value2) => {
        encodeLocalNodeId(writer, value2);
    });
}

/** Decode one Declarator. */
export function decodeDeclarator(reader: BinaryReader): Declarator {
    const pattern = decodeLocalNodeId(reader);
    const ty = reader.readOption(() => decodeLocalNodeId(reader));
    const value = reader.readOption(() => decodeLocalNodeId(reader));

    return {
        pattern,
        ...(ty === undefined ? {} : { ty }),
        ...(value === undefined ? {} : { value }),
    };
}

/** Return one JSON value for one Declarator. */
export function toJsonDeclarator(value: Declarator): Json {
    return {
        pattern: toJsonLocalNodeId(value.pattern),
        ...(value.ty === undefined ? {} : { ty: toJsonLocalNodeId(value.ty) }),
        ...(value.value === undefined ? {} : { value: toJsonLocalNodeId(value.value) }),
    };
}

/** Return one Declarator from one JSON value. */
export function fromJsonDeclarator(value: Json): Declarator {
    const object = jsonObject(value);

    return {
        pattern: fromJsonLocalNodeId(jsonField(object, "pattern")),
        ty: jsonOptional(object, "ty", (value) => fromJsonLocalNodeId(value)),
        value: jsonOptional(object, "value", (value) => fromJsonLocalNodeId(value)),
    };
}
