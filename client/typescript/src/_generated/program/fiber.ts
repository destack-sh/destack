/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, jsonBigint } from "../../protocol/serde.js";

/** Runtime identity for one fiber. */
export type FiberId = bigint;

export const FiberId = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: FiberId): void {
        encodeFiberId(writer, value);
    },

    /** Decode one FiberId. */
    decode(reader: BinaryReader): FiberId {
        return decodeFiberId(reader);
    },

    /** Return this value as JSON. */
    toJson(value: FiberId): Json {
        return toJsonFiberId(value);
    },

    /** Return one FiberId from one JSON value. */
    fromJson(value: Json): FiberId {
        return fromJsonFiberId(value);
    },
};

/** Encode one FiberId. */
export function encodeFiberId(writer: BinaryWriter, value: FiberId): void {
    writer.writeUnsigned(value);
}

/** Decode one FiberId. */
export function decodeFiberId(reader: BinaryReader): FiberId {
    return reader.readUnsigned();
}

/** Return one JSON value for one FiberId. */
export function toJsonFiberId(value: FiberId): Json {
    return value.toString();
}

/** Return one FiberId from one JSON value. */
export function fromJsonFiberId(value: Json): FiberId {
    return jsonBigint(value);
}
