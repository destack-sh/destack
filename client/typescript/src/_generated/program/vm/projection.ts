/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, jsonBigint, jsonField, jsonInteger, jsonObject, jsonOptional } from "../../../protocol/serde.js";
import type { LocalNodeId } from "../../mir/tree/node.js";
import type { CellLayout } from "./value.js";
import { decodeLocalNodeId, encodeLocalNodeId, fromJsonLocalNodeId, toJsonLocalNodeId } from "../../mir/tree/node.js";
import { decodeCellLayout, encodeCellLayout, fromJsonCellLayout, toJsonCellLayout } from "./value.js";

/** Compiled projection from a base address to one value. */
export type Projection = {
    /** The projected value type. */
    readonly valueType: LocalNodeId;
    /** The fixed byte offset from the base address. */
    readonly byteOffset: number;
    /** The byte stride for indexed projections. */
    readonly byteStride: number;
    /** The number of addressable elements when statically known. */
    readonly length: bigint;
    /** The byte width of the projected value. */
    readonly byteLen: number;
    /** The cell representation for scalar projections. */
    readonly cellLayout?: CellLayout;
};

export const Projection = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: Projection): void {
        encodeProjection(writer, value);
    },

    /** Decode one Projection. */
    decode(reader: BinaryReader): Projection {
        return decodeProjection(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Projection): Json {
        return toJsonProjection(value);
    },

    /** Return one Projection from one JSON value. */
    fromJson(value: Json): Projection {
        return fromJsonProjection(value);
    },
};

/** Encode one Projection. */
export function encodeProjection(writer: BinaryWriter, value: Projection): void {
    encodeLocalNodeId(writer, value.valueType);
    writer.writeUnsigned(value.byteOffset);
    writer.writeUnsigned(value.byteStride);
    writer.writeUnsigned(value.length);
    writer.writeUnsigned(value.byteLen);
    writer.writeOption(value.cellLayout, (value5) => {
        encodeCellLayout(writer, value5);
    });
}

/** Decode one Projection. */
export function decodeProjection(reader: BinaryReader): Projection {
    const valueType = decodeLocalNodeId(reader);
    const byteOffset = reader.readNumber();
    const byteStride = reader.readNumber();
    const length = reader.readUnsigned();
    const byteLen = reader.readNumber();
    const cellLayout = reader.readOption(() => decodeCellLayout(reader));

    return {
        valueType,
        byteOffset,
        byteStride,
        length,
        byteLen,
        ...(cellLayout === undefined ? {} : { cellLayout }),
    };
}

/** Return one JSON value for one Projection. */
export function toJsonProjection(value: Projection): Json {
    return {
        valueType: toJsonLocalNodeId(value.valueType),
        byteOffset: value.byteOffset,
        byteStride: value.byteStride,
        length: value.length.toString(),
        byteLen: value.byteLen,
        ...(value.cellLayout === undefined ? {} : { cellLayout: toJsonCellLayout(value.cellLayout) }),
    };
}

/** Return one Projection from one JSON value. */
export function fromJsonProjection(value: Json): Projection {
    const object = jsonObject(value);

    return {
        valueType: fromJsonLocalNodeId(jsonField(object, "valueType")),
        byteOffset: jsonInteger(jsonField(object, "byteOffset")),
        byteStride: jsonInteger(jsonField(object, "byteStride")),
        length: jsonBigint(jsonField(object, "length")),
        byteLen: jsonInteger(jsonField(object, "byteLen")),
        cellLayout: jsonOptional(object, "cellLayout", (value) => fromJsonCellLayout(value)),
    };
}

/** Compiled projection data for one slice descriptor. */
export type SliceProjection = {
    /** The slice data projection. */
    readonly data: SlotProjection;
    /** The slice length projection. */
    readonly length: SlotProjection;
    /** The backing element projection. */
    readonly element: Projection;
};

export const SliceProjection = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: SliceProjection): void {
        encodeSliceProjection(writer, value);
    },

    /** Decode one SliceProjection. */
    decode(reader: BinaryReader): SliceProjection {
        return decodeSliceProjection(reader);
    },

    /** Return this value as JSON. */
    toJson(value: SliceProjection): Json {
        return toJsonSliceProjection(value);
    },

    /** Return one SliceProjection from one JSON value. */
    fromJson(value: Json): SliceProjection {
        return fromJsonSliceProjection(value);
    },
};

/** Encode one SliceProjection. */
export function encodeSliceProjection(writer: BinaryWriter, value: SliceProjection): void {
    encodeSlotProjection(writer, value.data);
    encodeSlotProjection(writer, value.length);
    encodeProjection(writer, value.element);
}

/** Decode one SliceProjection. */
export function decodeSliceProjection(reader: BinaryReader): SliceProjection {
    const data = decodeSlotProjection(reader);
    const length = decodeSlotProjection(reader);
    const element = decodeProjection(reader);

    return {
        data,
        length,
        element,
    };
}

/** Return one JSON value for one SliceProjection. */
export function toJsonSliceProjection(value: SliceProjection): Json {
    return {
        data: toJsonSlotProjection(value.data),
        length: toJsonSlotProjection(value.length),
        element: toJsonProjection(value.element),
    };
}

/** Return one SliceProjection from one JSON value. */
export function fromJsonSliceProjection(value: Json): SliceProjection {
    const object = jsonObject(value);

    return {
        data: fromJsonSlotProjection(jsonField(object, "data")),
        length: fromJsonSlotProjection(jsonField(object, "length")),
        element: fromJsonProjection(jsonField(object, "element")),
    };
}

/** Compiled projection from a base address to one physical cell slot. */
export type SlotProjection = {
    /** The fixed byte offset from the base address. */
    readonly byteOffset: number;
    /** The byte width of the slot payload. */
    readonly byteLen: number;
    /** The cell representation for this slot. */
    readonly cellLayout: CellLayout;
};

export const SlotProjection = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: SlotProjection): void {
        encodeSlotProjection(writer, value);
    },

    /** Decode one SlotProjection. */
    decode(reader: BinaryReader): SlotProjection {
        return decodeSlotProjection(reader);
    },

    /** Return this value as JSON. */
    toJson(value: SlotProjection): Json {
        return toJsonSlotProjection(value);
    },

    /** Return one SlotProjection from one JSON value. */
    fromJson(value: Json): SlotProjection {
        return fromJsonSlotProjection(value);
    },
};

/** Encode one SlotProjection. */
export function encodeSlotProjection(writer: BinaryWriter, value: SlotProjection): void {
    writer.writeUnsigned(value.byteOffset);
    writer.writeUnsigned(value.byteLen);
    encodeCellLayout(writer, value.cellLayout);
}

/** Decode one SlotProjection. */
export function decodeSlotProjection(reader: BinaryReader): SlotProjection {
    const byteOffset = reader.readNumber();
    const byteLen = reader.readNumber();
    const cellLayout = decodeCellLayout(reader);

    return {
        byteOffset,
        byteLen,
        cellLayout,
    };
}

/** Return one JSON value for one SlotProjection. */
export function toJsonSlotProjection(value: SlotProjection): Json {
    return {
        byteOffset: value.byteOffset,
        byteLen: value.byteLen,
        cellLayout: toJsonCellLayout(value.cellLayout),
    };
}

/** Return one SlotProjection from one JSON value. */
export function fromJsonSlotProjection(value: Json): SlotProjection {
    const object = jsonObject(value);

    return {
        byteOffset: jsonInteger(jsonField(object, "byteOffset")),
        byteLen: jsonInteger(jsonField(object, "byteLen")),
        cellLayout: fromJsonCellLayout(jsonField(object, "cellLayout")),
    };
}
