/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, jsonField, jsonObject } from "../../../../protocol/serde.js";
import type { SectionSlice } from "../../../core/section.js";
import { decodeSectionSlice, encodeSectionSlice, fromJsonSectionSlice, toJsonSectionSlice } from "../../../core/section.js";

/** Immutable side table referenced by compact side records. */
export type SideTable = {
    /** Pooled side records. */
    readonly record: SideRecordTable;
    /** Pooled allocation plans. */
    readonly allocationPlan: SectionSlice;
    /** Pooled small allocation plans. */
    readonly smallAllocationPlan: SectionSlice;
    /** Pooled constants. */
    readonly constant: SectionSlice;
    /** Flattened constant bytes. */
    readonly constantBytes: SectionSlice;
    /** Pooled address projections. */
    readonly projection: SectionSlice;
    /** Pooled slice projections. */
    readonly sliceProjection: SectionSlice;
    /** Pooled check constraints. */
    readonly check: SectionSlice;
    /** Pooled switch case tables. */
    readonly switchCases: SectionSlice;
    /** Flattened switch case entries. */
    readonly switchCaseEntries: SectionSlice;
    /** Pooled dense switch tables. */
    readonly switchTable: SectionSlice;
    /** Pooled control edges. */
    readonly edge: SectionSlice;
    /** Pooled u32 slices. */
    readonly u32Ranges: SectionSlice;
    /** Flattened u32 entries. */
    readonly u32Entries: SectionSlice;
    /** Flattened tensor u64 entries. */
    readonly tensorU64Entries: SectionSlice;
    /** Flattened tensor flag entries. */
    readonly tensorFlagEntries: SectionSlice;
    /** Pooled tensor dot descriptors. */
    readonly tensorDot: SectionSlice;
    /** Pooled tensor convolution dimension descriptors. */
    readonly tensorConvolution: SectionSlice;
    /** Pooled tensor convolution window descriptors. */
    readonly tensorWindow: SectionSlice;
    /** Pooled tensor gather descriptors. */
    readonly tensorGather: SectionSlice;
    /** Pooled tensor scatter descriptors. */
    readonly tensorScatter: SectionSlice;
    /** Pooled tensor layouts. */
    readonly tensorLayout: SectionSlice;
    /** Pooled callable signatures. */
    readonly signature: SectionSlice;
    /** Flattened callable signature parameters. */
    readonly signatureParameters: SectionSlice;
};

export const SideTable = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: SideTable): void {
        encodeSideTable(writer, value);
    },

    /** Decode one SideTable. */
    decode(reader: BinaryReader): SideTable {
        return decodeSideTable(reader);
    },

    /** Return this value as JSON. */
    toJson(value: SideTable): Json {
        return toJsonSideTable(value);
    },

    /** Return one SideTable from one JSON value. */
    fromJson(value: Json): SideTable {
        return fromJsonSideTable(value);
    },
};

/** Encode one SideTable. */
export function encodeSideTable(writer: BinaryWriter, value: SideTable): void {
    encodeSideRecordTable(writer, value.record);
    encodeSectionSlice(writer, value.allocationPlan);
    encodeSectionSlice(writer, value.smallAllocationPlan);
    encodeSectionSlice(writer, value.constant);
    encodeSectionSlice(writer, value.constantBytes);
    encodeSectionSlice(writer, value.projection);
    encodeSectionSlice(writer, value.sliceProjection);
    encodeSectionSlice(writer, value.check);
    encodeSectionSlice(writer, value.switchCases);
    encodeSectionSlice(writer, value.switchCaseEntries);
    encodeSectionSlice(writer, value.switchTable);
    encodeSectionSlice(writer, value.edge);
    encodeSectionSlice(writer, value.u32Ranges);
    encodeSectionSlice(writer, value.u32Entries);
    encodeSectionSlice(writer, value.tensorU64Entries);
    encodeSectionSlice(writer, value.tensorFlagEntries);
    encodeSectionSlice(writer, value.tensorDot);
    encodeSectionSlice(writer, value.tensorConvolution);
    encodeSectionSlice(writer, value.tensorWindow);
    encodeSectionSlice(writer, value.tensorGather);
    encodeSectionSlice(writer, value.tensorScatter);
    encodeSectionSlice(writer, value.tensorLayout);
    encodeSectionSlice(writer, value.signature);
    encodeSectionSlice(writer, value.signatureParameters);
}

/** Decode one SideTable. */
export function decodeSideTable(reader: BinaryReader): SideTable {
    const record = decodeSideRecordTable(reader);
    const allocationPlan = decodeSectionSlice(reader);
    const smallAllocationPlan = decodeSectionSlice(reader);
    const constant = decodeSectionSlice(reader);
    const constantBytes = decodeSectionSlice(reader);
    const projection = decodeSectionSlice(reader);
    const sliceProjection = decodeSectionSlice(reader);
    const check = decodeSectionSlice(reader);
    const switchCases = decodeSectionSlice(reader);
    const switchCaseEntries = decodeSectionSlice(reader);
    const switchTable = decodeSectionSlice(reader);
    const edge = decodeSectionSlice(reader);
    const u32Ranges = decodeSectionSlice(reader);
    const u32Entries = decodeSectionSlice(reader);
    const tensorU64Entries = decodeSectionSlice(reader);
    const tensorFlagEntries = decodeSectionSlice(reader);
    const tensorDot = decodeSectionSlice(reader);
    const tensorConvolution = decodeSectionSlice(reader);
    const tensorWindow = decodeSectionSlice(reader);
    const tensorGather = decodeSectionSlice(reader);
    const tensorScatter = decodeSectionSlice(reader);
    const tensorLayout = decodeSectionSlice(reader);
    const signature = decodeSectionSlice(reader);
    const signatureParameters = decodeSectionSlice(reader);

    return {
        record,
        allocationPlan,
        smallAllocationPlan,
        constant,
        constantBytes,
        projection,
        sliceProjection,
        check,
        switchCases,
        switchCaseEntries,
        switchTable,
        edge,
        u32Ranges,
        u32Entries,
        tensorU64Entries,
        tensorFlagEntries,
        tensorDot,
        tensorConvolution,
        tensorWindow,
        tensorGather,
        tensorScatter,
        tensorLayout,
        signature,
        signatureParameters,
    };
}

/** Return one JSON value for one SideTable. */
export function toJsonSideTable(value: SideTable): Json {
    return {
        record: toJsonSideRecordTable(value.record),
        allocationPlan: toJsonSectionSlice(value.allocationPlan),
        smallAllocationPlan: toJsonSectionSlice(value.smallAllocationPlan),
        constant: toJsonSectionSlice(value.constant),
        constantBytes: toJsonSectionSlice(value.constantBytes),
        projection: toJsonSectionSlice(value.projection),
        sliceProjection: toJsonSectionSlice(value.sliceProjection),
        check: toJsonSectionSlice(value.check),
        switchCases: toJsonSectionSlice(value.switchCases),
        switchCaseEntries: toJsonSectionSlice(value.switchCaseEntries),
        switchTable: toJsonSectionSlice(value.switchTable),
        edge: toJsonSectionSlice(value.edge),
        u32Ranges: toJsonSectionSlice(value.u32Ranges),
        u32Entries: toJsonSectionSlice(value.u32Entries),
        tensorU64Entries: toJsonSectionSlice(value.tensorU64Entries),
        tensorFlagEntries: toJsonSectionSlice(value.tensorFlagEntries),
        tensorDot: toJsonSectionSlice(value.tensorDot),
        tensorConvolution: toJsonSectionSlice(value.tensorConvolution),
        tensorWindow: toJsonSectionSlice(value.tensorWindow),
        tensorGather: toJsonSectionSlice(value.tensorGather),
        tensorScatter: toJsonSectionSlice(value.tensorScatter),
        tensorLayout: toJsonSectionSlice(value.tensorLayout),
        signature: toJsonSectionSlice(value.signature),
        signatureParameters: toJsonSectionSlice(value.signatureParameters),
    };
}

/** Return one SideTable from one JSON value. */
export function fromJsonSideTable(value: Json): SideTable {
    const object = jsonObject(value);

    return {
        record: fromJsonSideRecordTable(jsonField(object, "record")),
        allocationPlan: fromJsonSectionSlice(jsonField(object, "allocationPlan")),
        smallAllocationPlan: fromJsonSectionSlice(jsonField(object, "smallAllocationPlan")),
        constant: fromJsonSectionSlice(jsonField(object, "constant")),
        constantBytes: fromJsonSectionSlice(jsonField(object, "constantBytes")),
        projection: fromJsonSectionSlice(jsonField(object, "projection")),
        sliceProjection: fromJsonSectionSlice(jsonField(object, "sliceProjection")),
        check: fromJsonSectionSlice(jsonField(object, "check")),
        switchCases: fromJsonSectionSlice(jsonField(object, "switchCases")),
        switchCaseEntries: fromJsonSectionSlice(jsonField(object, "switchCaseEntries")),
        switchTable: fromJsonSectionSlice(jsonField(object, "switchTable")),
        edge: fromJsonSectionSlice(jsonField(object, "edge")),
        u32Ranges: fromJsonSectionSlice(jsonField(object, "u32Ranges")),
        u32Entries: fromJsonSectionSlice(jsonField(object, "u32Entries")),
        tensorU64Entries: fromJsonSectionSlice(jsonField(object, "tensorU64Entries")),
        tensorFlagEntries: fromJsonSectionSlice(jsonField(object, "tensorFlagEntries")),
        tensorDot: fromJsonSectionSlice(jsonField(object, "tensorDot")),
        tensorConvolution: fromJsonSectionSlice(jsonField(object, "tensorConvolution")),
        tensorWindow: fromJsonSectionSlice(jsonField(object, "tensorWindow")),
        tensorGather: fromJsonSectionSlice(jsonField(object, "tensorGather")),
        tensorScatter: fromJsonSectionSlice(jsonField(object, "tensorScatter")),
        tensorLayout: fromJsonSectionSlice(jsonField(object, "tensorLayout")),
        signature: fromJsonSectionSlice(jsonField(object, "signature")),
        signatureParameters: fromJsonSectionSlice(jsonField(object, "signatureParameters")),
    };
}

/** Immutable side records referenced by instruction ids. */
export type SideRecordTable = {
    readonly aggregateSelect: SectionSlice;
    readonly allocationBranch: SectionSlice;
    readonly sliceAllocationBranch: SectionSlice;
    readonly atomicCompareExchange: SectionSlice;
    readonly vectorSplat: SectionSlice;
    readonly vectorExtract: SectionSlice;
    readonly vectorBinary: SectionSlice;
    readonly vectorUnary: SectionSlice;
    readonly vectorInsert: SectionSlice;
    readonly vectorShuffle: SectionSlice;
    readonly vectorSelect: SectionSlice;
    readonly vectorReduce: SectionSlice;
    readonly vectorConvert: SectionSlice;
    readonly functionBind: SectionSlice;
    readonly call: SectionSlice;
    readonly callBranch: SectionSlice;
    readonly callVirtual: SectionSlice;
    readonly callVirtualBranch: SectionSlice;
    readonly callDynamic: SectionSlice;
    readonly callDynamicBranch: SectionSlice;
    readonly indirectCall: SectionSlice;
    readonly indirectCallBranch: SectionSlice;
    readonly tensorLoad: SectionSlice;
    readonly tensorExtract: SectionSlice;
    readonly tensorBinary: SectionSlice;
    readonly tensorContiguousBinary: SectionSlice;
    readonly tensorUnary: SectionSlice;
    readonly tensorContiguousUnary: SectionSlice;
    readonly tensorStore: SectionSlice;
    readonly tensorFill: SectionSlice;
    readonly tensorCopy: SectionSlice;
    readonly tensorViewCast: SectionSlice;
    readonly tensorReshape: SectionSlice;
    readonly tensorBroadcast: SectionSlice;
    readonly tensorTranspose: SectionSlice;
    readonly tensorSlice: SectionSlice;
    readonly tensorPad: SectionSlice;
    readonly tensorConcat: SectionSlice;
    readonly tensorReduce: SectionSlice;
    readonly tensorIndexReduce: SectionSlice;
    readonly tensorDot: SectionSlice;
    readonly tensorConvolution: SectionSlice;
    readonly tensorGather: SectionSlice;
    readonly tensorScatter: SectionSlice;
    readonly tensorSelect: SectionSlice;
    readonly tensorConvert: SectionSlice;
    readonly tensorView: SectionSlice;
    readonly intrinsic: SectionSlice;
    readonly tailCall: SectionSlice;
    readonly tailCallVirtual: SectionSlice;
    readonly tailCallDynamic: SectionSlice;
    readonly indirectTailCall: SectionSlice;
};

export const SideRecordTable = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: SideRecordTable): void {
        encodeSideRecordTable(writer, value);
    },

    /** Decode one SideRecordTable. */
    decode(reader: BinaryReader): SideRecordTable {
        return decodeSideRecordTable(reader);
    },

    /** Return this value as JSON. */
    toJson(value: SideRecordTable): Json {
        return toJsonSideRecordTable(value);
    },

    /** Return one SideRecordTable from one JSON value. */
    fromJson(value: Json): SideRecordTable {
        return fromJsonSideRecordTable(value);
    },
};

/** Encode one SideRecordTable. */
export function encodeSideRecordTable(writer: BinaryWriter, value: SideRecordTable): void {
    encodeSectionSlice(writer, value.aggregateSelect);
    encodeSectionSlice(writer, value.allocationBranch);
    encodeSectionSlice(writer, value.sliceAllocationBranch);
    encodeSectionSlice(writer, value.atomicCompareExchange);
    encodeSectionSlice(writer, value.vectorSplat);
    encodeSectionSlice(writer, value.vectorExtract);
    encodeSectionSlice(writer, value.vectorBinary);
    encodeSectionSlice(writer, value.vectorUnary);
    encodeSectionSlice(writer, value.vectorInsert);
    encodeSectionSlice(writer, value.vectorShuffle);
    encodeSectionSlice(writer, value.vectorSelect);
    encodeSectionSlice(writer, value.vectorReduce);
    encodeSectionSlice(writer, value.vectorConvert);
    encodeSectionSlice(writer, value.functionBind);
    encodeSectionSlice(writer, value.call);
    encodeSectionSlice(writer, value.callBranch);
    encodeSectionSlice(writer, value.callVirtual);
    encodeSectionSlice(writer, value.callVirtualBranch);
    encodeSectionSlice(writer, value.callDynamic);
    encodeSectionSlice(writer, value.callDynamicBranch);
    encodeSectionSlice(writer, value.indirectCall);
    encodeSectionSlice(writer, value.indirectCallBranch);
    encodeSectionSlice(writer, value.tensorLoad);
    encodeSectionSlice(writer, value.tensorExtract);
    encodeSectionSlice(writer, value.tensorBinary);
    encodeSectionSlice(writer, value.tensorContiguousBinary);
    encodeSectionSlice(writer, value.tensorUnary);
    encodeSectionSlice(writer, value.tensorContiguousUnary);
    encodeSectionSlice(writer, value.tensorStore);
    encodeSectionSlice(writer, value.tensorFill);
    encodeSectionSlice(writer, value.tensorCopy);
    encodeSectionSlice(writer, value.tensorViewCast);
    encodeSectionSlice(writer, value.tensorReshape);
    encodeSectionSlice(writer, value.tensorBroadcast);
    encodeSectionSlice(writer, value.tensorTranspose);
    encodeSectionSlice(writer, value.tensorSlice);
    encodeSectionSlice(writer, value.tensorPad);
    encodeSectionSlice(writer, value.tensorConcat);
    encodeSectionSlice(writer, value.tensorReduce);
    encodeSectionSlice(writer, value.tensorIndexReduce);
    encodeSectionSlice(writer, value.tensorDot);
    encodeSectionSlice(writer, value.tensorConvolution);
    encodeSectionSlice(writer, value.tensorGather);
    encodeSectionSlice(writer, value.tensorScatter);
    encodeSectionSlice(writer, value.tensorSelect);
    encodeSectionSlice(writer, value.tensorConvert);
    encodeSectionSlice(writer, value.tensorView);
    encodeSectionSlice(writer, value.intrinsic);
    encodeSectionSlice(writer, value.tailCall);
    encodeSectionSlice(writer, value.tailCallVirtual);
    encodeSectionSlice(writer, value.tailCallDynamic);
    encodeSectionSlice(writer, value.indirectTailCall);
}

/** Decode one SideRecordTable. */
export function decodeSideRecordTable(reader: BinaryReader): SideRecordTable {
    const aggregateSelect = decodeSectionSlice(reader);
    const allocationBranch = decodeSectionSlice(reader);
    const sliceAllocationBranch = decodeSectionSlice(reader);
    const atomicCompareExchange = decodeSectionSlice(reader);
    const vectorSplat = decodeSectionSlice(reader);
    const vectorExtract = decodeSectionSlice(reader);
    const vectorBinary = decodeSectionSlice(reader);
    const vectorUnary = decodeSectionSlice(reader);
    const vectorInsert = decodeSectionSlice(reader);
    const vectorShuffle = decodeSectionSlice(reader);
    const vectorSelect = decodeSectionSlice(reader);
    const vectorReduce = decodeSectionSlice(reader);
    const vectorConvert = decodeSectionSlice(reader);
    const functionBind = decodeSectionSlice(reader);
    const call = decodeSectionSlice(reader);
    const callBranch = decodeSectionSlice(reader);
    const callVirtual = decodeSectionSlice(reader);
    const callVirtualBranch = decodeSectionSlice(reader);
    const callDynamic = decodeSectionSlice(reader);
    const callDynamicBranch = decodeSectionSlice(reader);
    const indirectCall = decodeSectionSlice(reader);
    const indirectCallBranch = decodeSectionSlice(reader);
    const tensorLoad = decodeSectionSlice(reader);
    const tensorExtract = decodeSectionSlice(reader);
    const tensorBinary = decodeSectionSlice(reader);
    const tensorContiguousBinary = decodeSectionSlice(reader);
    const tensorUnary = decodeSectionSlice(reader);
    const tensorContiguousUnary = decodeSectionSlice(reader);
    const tensorStore = decodeSectionSlice(reader);
    const tensorFill = decodeSectionSlice(reader);
    const tensorCopy = decodeSectionSlice(reader);
    const tensorViewCast = decodeSectionSlice(reader);
    const tensorReshape = decodeSectionSlice(reader);
    const tensorBroadcast = decodeSectionSlice(reader);
    const tensorTranspose = decodeSectionSlice(reader);
    const tensorSlice = decodeSectionSlice(reader);
    const tensorPad = decodeSectionSlice(reader);
    const tensorConcat = decodeSectionSlice(reader);
    const tensorReduce = decodeSectionSlice(reader);
    const tensorIndexReduce = decodeSectionSlice(reader);
    const tensorDot = decodeSectionSlice(reader);
    const tensorConvolution = decodeSectionSlice(reader);
    const tensorGather = decodeSectionSlice(reader);
    const tensorScatter = decodeSectionSlice(reader);
    const tensorSelect = decodeSectionSlice(reader);
    const tensorConvert = decodeSectionSlice(reader);
    const tensorView = decodeSectionSlice(reader);
    const intrinsic = decodeSectionSlice(reader);
    const tailCall = decodeSectionSlice(reader);
    const tailCallVirtual = decodeSectionSlice(reader);
    const tailCallDynamic = decodeSectionSlice(reader);
    const indirectTailCall = decodeSectionSlice(reader);

    return {
        aggregateSelect,
        allocationBranch,
        sliceAllocationBranch,
        atomicCompareExchange,
        vectorSplat,
        vectorExtract,
        vectorBinary,
        vectorUnary,
        vectorInsert,
        vectorShuffle,
        vectorSelect,
        vectorReduce,
        vectorConvert,
        functionBind,
        call,
        callBranch,
        callVirtual,
        callVirtualBranch,
        callDynamic,
        callDynamicBranch,
        indirectCall,
        indirectCallBranch,
        tensorLoad,
        tensorExtract,
        tensorBinary,
        tensorContiguousBinary,
        tensorUnary,
        tensorContiguousUnary,
        tensorStore,
        tensorFill,
        tensorCopy,
        tensorViewCast,
        tensorReshape,
        tensorBroadcast,
        tensorTranspose,
        tensorSlice,
        tensorPad,
        tensorConcat,
        tensorReduce,
        tensorIndexReduce,
        tensorDot,
        tensorConvolution,
        tensorGather,
        tensorScatter,
        tensorSelect,
        tensorConvert,
        tensorView,
        intrinsic,
        tailCall,
        tailCallVirtual,
        tailCallDynamic,
        indirectTailCall,
    };
}

/** Return one JSON value for one SideRecordTable. */
export function toJsonSideRecordTable(value: SideRecordTable): Json {
    return {
        aggregateSelect: toJsonSectionSlice(value.aggregateSelect),
        allocationBranch: toJsonSectionSlice(value.allocationBranch),
        sliceAllocationBranch: toJsonSectionSlice(value.sliceAllocationBranch),
        atomicCompareExchange: toJsonSectionSlice(value.atomicCompareExchange),
        vectorSplat: toJsonSectionSlice(value.vectorSplat),
        vectorExtract: toJsonSectionSlice(value.vectorExtract),
        vectorBinary: toJsonSectionSlice(value.vectorBinary),
        vectorUnary: toJsonSectionSlice(value.vectorUnary),
        vectorInsert: toJsonSectionSlice(value.vectorInsert),
        vectorShuffle: toJsonSectionSlice(value.vectorShuffle),
        vectorSelect: toJsonSectionSlice(value.vectorSelect),
        vectorReduce: toJsonSectionSlice(value.vectorReduce),
        vectorConvert: toJsonSectionSlice(value.vectorConvert),
        functionBind: toJsonSectionSlice(value.functionBind),
        call: toJsonSectionSlice(value.call),
        callBranch: toJsonSectionSlice(value.callBranch),
        callVirtual: toJsonSectionSlice(value.callVirtual),
        callVirtualBranch: toJsonSectionSlice(value.callVirtualBranch),
        callDynamic: toJsonSectionSlice(value.callDynamic),
        callDynamicBranch: toJsonSectionSlice(value.callDynamicBranch),
        indirectCall: toJsonSectionSlice(value.indirectCall),
        indirectCallBranch: toJsonSectionSlice(value.indirectCallBranch),
        tensorLoad: toJsonSectionSlice(value.tensorLoad),
        tensorExtract: toJsonSectionSlice(value.tensorExtract),
        tensorBinary: toJsonSectionSlice(value.tensorBinary),
        tensorContiguousBinary: toJsonSectionSlice(value.tensorContiguousBinary),
        tensorUnary: toJsonSectionSlice(value.tensorUnary),
        tensorContiguousUnary: toJsonSectionSlice(value.tensorContiguousUnary),
        tensorStore: toJsonSectionSlice(value.tensorStore),
        tensorFill: toJsonSectionSlice(value.tensorFill),
        tensorCopy: toJsonSectionSlice(value.tensorCopy),
        tensorViewCast: toJsonSectionSlice(value.tensorViewCast),
        tensorReshape: toJsonSectionSlice(value.tensorReshape),
        tensorBroadcast: toJsonSectionSlice(value.tensorBroadcast),
        tensorTranspose: toJsonSectionSlice(value.tensorTranspose),
        tensorSlice: toJsonSectionSlice(value.tensorSlice),
        tensorPad: toJsonSectionSlice(value.tensorPad),
        tensorConcat: toJsonSectionSlice(value.tensorConcat),
        tensorReduce: toJsonSectionSlice(value.tensorReduce),
        tensorIndexReduce: toJsonSectionSlice(value.tensorIndexReduce),
        tensorDot: toJsonSectionSlice(value.tensorDot),
        tensorConvolution: toJsonSectionSlice(value.tensorConvolution),
        tensorGather: toJsonSectionSlice(value.tensorGather),
        tensorScatter: toJsonSectionSlice(value.tensorScatter),
        tensorSelect: toJsonSectionSlice(value.tensorSelect),
        tensorConvert: toJsonSectionSlice(value.tensorConvert),
        tensorView: toJsonSectionSlice(value.tensorView),
        intrinsic: toJsonSectionSlice(value.intrinsic),
        tailCall: toJsonSectionSlice(value.tailCall),
        tailCallVirtual: toJsonSectionSlice(value.tailCallVirtual),
        tailCallDynamic: toJsonSectionSlice(value.tailCallDynamic),
        indirectTailCall: toJsonSectionSlice(value.indirectTailCall),
    };
}

/** Return one SideRecordTable from one JSON value. */
export function fromJsonSideRecordTable(value: Json): SideRecordTable {
    const object = jsonObject(value);

    return {
        aggregateSelect: fromJsonSectionSlice(jsonField(object, "aggregateSelect")),
        allocationBranch: fromJsonSectionSlice(jsonField(object, "allocationBranch")),
        sliceAllocationBranch: fromJsonSectionSlice(jsonField(object, "sliceAllocationBranch")),
        atomicCompareExchange: fromJsonSectionSlice(jsonField(object, "atomicCompareExchange")),
        vectorSplat: fromJsonSectionSlice(jsonField(object, "vectorSplat")),
        vectorExtract: fromJsonSectionSlice(jsonField(object, "vectorExtract")),
        vectorBinary: fromJsonSectionSlice(jsonField(object, "vectorBinary")),
        vectorUnary: fromJsonSectionSlice(jsonField(object, "vectorUnary")),
        vectorInsert: fromJsonSectionSlice(jsonField(object, "vectorInsert")),
        vectorShuffle: fromJsonSectionSlice(jsonField(object, "vectorShuffle")),
        vectorSelect: fromJsonSectionSlice(jsonField(object, "vectorSelect")),
        vectorReduce: fromJsonSectionSlice(jsonField(object, "vectorReduce")),
        vectorConvert: fromJsonSectionSlice(jsonField(object, "vectorConvert")),
        functionBind: fromJsonSectionSlice(jsonField(object, "functionBind")),
        call: fromJsonSectionSlice(jsonField(object, "call")),
        callBranch: fromJsonSectionSlice(jsonField(object, "callBranch")),
        callVirtual: fromJsonSectionSlice(jsonField(object, "callVirtual")),
        callVirtualBranch: fromJsonSectionSlice(jsonField(object, "callVirtualBranch")),
        callDynamic: fromJsonSectionSlice(jsonField(object, "callDynamic")),
        callDynamicBranch: fromJsonSectionSlice(jsonField(object, "callDynamicBranch")),
        indirectCall: fromJsonSectionSlice(jsonField(object, "indirectCall")),
        indirectCallBranch: fromJsonSectionSlice(jsonField(object, "indirectCallBranch")),
        tensorLoad: fromJsonSectionSlice(jsonField(object, "tensorLoad")),
        tensorExtract: fromJsonSectionSlice(jsonField(object, "tensorExtract")),
        tensorBinary: fromJsonSectionSlice(jsonField(object, "tensorBinary")),
        tensorContiguousBinary: fromJsonSectionSlice(jsonField(object, "tensorContiguousBinary")),
        tensorUnary: fromJsonSectionSlice(jsonField(object, "tensorUnary")),
        tensorContiguousUnary: fromJsonSectionSlice(jsonField(object, "tensorContiguousUnary")),
        tensorStore: fromJsonSectionSlice(jsonField(object, "tensorStore")),
        tensorFill: fromJsonSectionSlice(jsonField(object, "tensorFill")),
        tensorCopy: fromJsonSectionSlice(jsonField(object, "tensorCopy")),
        tensorViewCast: fromJsonSectionSlice(jsonField(object, "tensorViewCast")),
        tensorReshape: fromJsonSectionSlice(jsonField(object, "tensorReshape")),
        tensorBroadcast: fromJsonSectionSlice(jsonField(object, "tensorBroadcast")),
        tensorTranspose: fromJsonSectionSlice(jsonField(object, "tensorTranspose")),
        tensorSlice: fromJsonSectionSlice(jsonField(object, "tensorSlice")),
        tensorPad: fromJsonSectionSlice(jsonField(object, "tensorPad")),
        tensorConcat: fromJsonSectionSlice(jsonField(object, "tensorConcat")),
        tensorReduce: fromJsonSectionSlice(jsonField(object, "tensorReduce")),
        tensorIndexReduce: fromJsonSectionSlice(jsonField(object, "tensorIndexReduce")),
        tensorDot: fromJsonSectionSlice(jsonField(object, "tensorDot")),
        tensorConvolution: fromJsonSectionSlice(jsonField(object, "tensorConvolution")),
        tensorGather: fromJsonSectionSlice(jsonField(object, "tensorGather")),
        tensorScatter: fromJsonSectionSlice(jsonField(object, "tensorScatter")),
        tensorSelect: fromJsonSectionSlice(jsonField(object, "tensorSelect")),
        tensorConvert: fromJsonSectionSlice(jsonField(object, "tensorConvert")),
        tensorView: fromJsonSectionSlice(jsonField(object, "tensorView")),
        intrinsic: fromJsonSectionSlice(jsonField(object, "intrinsic")),
        tailCall: fromJsonSectionSlice(jsonField(object, "tailCall")),
        tailCallVirtual: fromJsonSectionSlice(jsonField(object, "tailCallVirtual")),
        tailCallDynamic: fromJsonSectionSlice(jsonField(object, "tailCallDynamic")),
        indirectTailCall: fromJsonSectionSlice(jsonField(object, "indirectTailCall")),
    };
}
