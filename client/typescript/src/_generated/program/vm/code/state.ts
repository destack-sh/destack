/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, jsonField, jsonObject } from "../../../../protocol/serde.js";
import type { SectionSlice } from "../../../core/section.js";
import { decodeSectionSlice, encodeSectionSlice, fromJsonSectionSlice, toJsonSectionSlice } from "../../../core/section.js";

/** VM resume states keyed by execution frame state. */
export type ResumeTable = {
    /** Resume states by dense frame state id. */
    readonly states: SectionSlice;
    /** Resume state ids sorted by lowered program point. */
    readonly points: SectionSlice;
    /** Flattened frame entry bindings. */
    readonly bindings: SectionSlice;
};

export const ResumeTable = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ResumeTable): void {
        encodeResumeTable(writer, value);
    },

    /** Decode one ResumeTable. */
    decode(reader: BinaryReader): ResumeTable {
        return decodeResumeTable(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ResumeTable): Json {
        return toJsonResumeTable(value);
    },

    /** Return one ResumeTable from one JSON value. */
    fromJson(value: Json): ResumeTable {
        return fromJsonResumeTable(value);
    },
};

/** Encode one ResumeTable. */
export function encodeResumeTable(writer: BinaryWriter, value: ResumeTable): void {
    encodeSectionSlice(writer, value.states);
    encodeSectionSlice(writer, value.points);
    encodeSectionSlice(writer, value.bindings);
}

/** Decode one ResumeTable. */
export function decodeResumeTable(reader: BinaryReader): ResumeTable {
    const states = decodeSectionSlice(reader);
    const points = decodeSectionSlice(reader);
    const bindings = decodeSectionSlice(reader);

    return {
        states,
        points,
        bindings,
    };
}

/** Return one JSON value for one ResumeTable. */
export function toJsonResumeTable(value: ResumeTable): Json {
    return {
        states: toJsonSectionSlice(value.states),
        points: toJsonSectionSlice(value.points),
        bindings: toJsonSectionSlice(value.bindings),
    };
}

/** Return one ResumeTable from one JSON value. */
export function fromJsonResumeTable(value: Json): ResumeTable {
    const object = jsonObject(value);

    return {
        states: fromJsonSectionSlice(jsonField(object, "states")),
        points: fromJsonSectionSlice(jsonField(object, "points")),
        bindings: fromJsonSectionSlice(jsonField(object, "bindings")),
    };
}
