/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, jsonField, jsonInteger, jsonObject } from "../../../protocol/serde.js";
import type { TraceId } from "../../mir/metadata/trace.js";
import type { AllocationSiteId } from "./table.js";
import type { Edge } from "./table.js";
import type { SliceProjectionId } from "./table.js";
import * as heapCoreLayout from "../../heap/core/layout.js";
import { decodeTraceId, encodeTraceId, fromJsonTraceId, toJsonTraceId } from "../../mir/metadata/trace.js";
import { decodeAllocationSiteId, encodeAllocationSiteId, fromJsonAllocationSiteId, toJsonAllocationSiteId } from "./table.js";
import { decodeEdge, encodeEdge, fromJsonEdge, toJsonEdge } from "./table.js";
import { decodeSliceProjectionId, encodeSliceProjectionId, fromJsonSliceProjectionId, toJsonSliceProjectionId } from "./table.js";

/** Branching allocation consumed by fallible heap allocation instructions. */
export type AllocationBranch = {
    /** The destination frame offset. */
    readonly destination: number;
    /** The allocation site. */
    readonly allocation: AllocationSiteId;
    /** The success edge. */
    readonly success: Edge;
    /** The failure edge. */
    readonly failure: Edge;
};

export const AllocationBranch = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: AllocationBranch): void {
        encodeAllocationBranch(writer, value);
    },

    /** Decode one AllocationBranch. */
    decode(reader: BinaryReader): AllocationBranch {
        return decodeAllocationBranch(reader);
    },

    /** Return this value as JSON. */
    toJson(value: AllocationBranch): Json {
        return toJsonAllocationBranch(value);
    },

    /** Return one AllocationBranch from one JSON value. */
    fromJson(value: Json): AllocationBranch {
        return fromJsonAllocationBranch(value);
    },
};

/** Encode one AllocationBranch. */
export function encodeAllocationBranch(writer: BinaryWriter, value: AllocationBranch): void {
    writer.writeUnsigned(value.destination);
    encodeAllocationSiteId(writer, value.allocation);
    encodeEdge(writer, value.success);
    encodeEdge(writer, value.failure);
}

/** Decode one AllocationBranch. */
export function decodeAllocationBranch(reader: BinaryReader): AllocationBranch {
    const destination = reader.readNumber();
    const allocation = decodeAllocationSiteId(reader);
    const success = decodeEdge(reader);
    const failure = decodeEdge(reader);

    return {
        destination,
        allocation,
        success,
        failure,
    };
}

/** Return one JSON value for one AllocationBranch. */
export function toJsonAllocationBranch(value: AllocationBranch): Json {
    return {
        destination: value.destination,
        allocation: toJsonAllocationSiteId(value.allocation),
        success: toJsonEdge(value.success),
        failure: toJsonEdge(value.failure),
    };
}

/** Return one AllocationBranch from one JSON value. */
export function fromJsonAllocationBranch(value: Json): AllocationBranch {
    const object = jsonObject(value);

    return {
        destination: jsonInteger(jsonField(object, "destination")),
        allocation: fromJsonAllocationSiteId(jsonField(object, "allocation")),
        success: fromJsonEdge(jsonField(object, "success")),
        failure: fromJsonEdge(jsonField(object, "failure")),
    };
}

/** Branching slice allocation consumed by fallible slice allocation instructions. */
export type SliceAllocationBranch = {
    /** The destination frame offset. */
    readonly destination: number;
    /** The length cell frame offset. */
    readonly length: number;
    /** The backing element allocation site. */
    readonly element: AllocationSiteId;
    /** The slice descriptor projection. */
    readonly access: SliceProjectionId;
    /** The success edge. */
    readonly success: Edge;
    /** The failure edge. */
    readonly failure: Edge;
};

export const SliceAllocationBranch = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: SliceAllocationBranch): void {
        encodeSliceAllocationBranch(writer, value);
    },

    /** Decode one SliceAllocationBranch. */
    decode(reader: BinaryReader): SliceAllocationBranch {
        return decodeSliceAllocationBranch(reader);
    },

    /** Return this value as JSON. */
    toJson(value: SliceAllocationBranch): Json {
        return toJsonSliceAllocationBranch(value);
    },

    /** Return one SliceAllocationBranch from one JSON value. */
    fromJson(value: Json): SliceAllocationBranch {
        return fromJsonSliceAllocationBranch(value);
    },
};

/** Encode one SliceAllocationBranch. */
export function encodeSliceAllocationBranch(writer: BinaryWriter, value: SliceAllocationBranch): void {
    writer.writeUnsigned(value.destination);
    writer.writeUnsigned(value.length);
    encodeAllocationSiteId(writer, value.element);
    encodeSliceProjectionId(writer, value.access);
    encodeEdge(writer, value.success);
    encodeEdge(writer, value.failure);
}

/** Decode one SliceAllocationBranch. */
export function decodeSliceAllocationBranch(reader: BinaryReader): SliceAllocationBranch {
    const destination = reader.readNumber();
    const length = reader.readNumber();
    const element = decodeAllocationSiteId(reader);
    const access = decodeSliceProjectionId(reader);
    const success = decodeEdge(reader);
    const failure = decodeEdge(reader);

    return {
        destination,
        length,
        element,
        access,
        success,
        failure,
    };
}

/** Return one JSON value for one SliceAllocationBranch. */
export function toJsonSliceAllocationBranch(value: SliceAllocationBranch): Json {
    return {
        destination: value.destination,
        length: value.length,
        element: toJsonAllocationSiteId(value.element),
        access: toJsonSliceProjectionId(value.access),
        success: toJsonEdge(value.success),
        failure: toJsonEdge(value.failure),
    };
}

/** Return one SliceAllocationBranch from one JSON value. */
export function fromJsonSliceAllocationBranch(value: Json): SliceAllocationBranch {
    const object = jsonObject(value);

    return {
        destination: jsonInteger(jsonField(object, "destination")),
        length: jsonInteger(jsonField(object, "length")),
        element: fromJsonAllocationSiteId(jsonField(object, "element")),
        access: fromJsonSliceProjectionId(jsonField(object, "access")),
        success: fromJsonEdge(jsonField(object, "success")),
        failure: fromJsonEdge(jsonField(object, "failure")),
    };
}

/** The allocation site consumed by heap allocation instructions. */
export type AllocationSite = {
    /** The heap-ready allocation site. */
    readonly heap: heapCoreLayout.AllocationSite;
    /** The exact heap trace map id. */
    readonly traceMap: TraceId;
};

export const AllocationSite = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: AllocationSite): void {
        encodeAllocationSite(writer, value);
    },

    /** Decode one AllocationSite. */
    decode(reader: BinaryReader): AllocationSite {
        return decodeAllocationSite(reader);
    },

    /** Return this value as JSON. */
    toJson(value: AllocationSite): Json {
        return toJsonAllocationSite(value);
    },

    /** Return one AllocationSite from one JSON value. */
    fromJson(value: Json): AllocationSite {
        return fromJsonAllocationSite(value);
    },
};

/** Encode one AllocationSite. */
export function encodeAllocationSite(writer: BinaryWriter, value: AllocationSite): void {
    heapCoreLayout.encodeAllocationSite(writer, value.heap);
    encodeTraceId(writer, value.traceMap);
}

/** Decode one AllocationSite. */
export function decodeAllocationSite(reader: BinaryReader): AllocationSite {
    const heap = heapCoreLayout.decodeAllocationSite(reader);
    const traceMap = decodeTraceId(reader);

    return {
        heap,
        traceMap,
    };
}

/** Return one JSON value for one AllocationSite. */
export function toJsonAllocationSite(value: AllocationSite): Json {
    return {
        heap: heapCoreLayout.toJsonAllocationSite(value.heap),
        traceMap: toJsonTraceId(value.traceMap),
    };
}

/** Return one AllocationSite from one JSON value. */
export function fromJsonAllocationSite(value: Json): AllocationSite {
    const object = jsonObject(value);

    return {
        heap: heapCoreLayout.fromJsonAllocationSite(jsonField(object, "heap")),
        traceMap: fromJsonTraceId(jsonField(object, "traceMap")),
    };
}

/** The small allocation site consumed by small heap allocation instructions. */
export type SmallAllocationSite = {
    /** The full heap-ready allocation site for cold allocation. */
    readonly heap: heapCoreLayout.AllocationSite;
    /** The heap-ready small allocation site for cursor reservation. */
    readonly small: heapCoreLayout.SmallAllocationSite;
    /** The exact heap trace map id. */
    readonly traceMap: TraceId;
};

export const SmallAllocationSite = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: SmallAllocationSite): void {
        encodeSmallAllocationSite(writer, value);
    },

    /** Decode one SmallAllocationSite. */
    decode(reader: BinaryReader): SmallAllocationSite {
        return decodeSmallAllocationSite(reader);
    },

    /** Return this value as JSON. */
    toJson(value: SmallAllocationSite): Json {
        return toJsonSmallAllocationSite(value);
    },

    /** Return one SmallAllocationSite from one JSON value. */
    fromJson(value: Json): SmallAllocationSite {
        return fromJsonSmallAllocationSite(value);
    },
};

/** Encode one SmallAllocationSite. */
export function encodeSmallAllocationSite(writer: BinaryWriter, value: SmallAllocationSite): void {
    heapCoreLayout.encodeAllocationSite(writer, value.heap);
    heapCoreLayout.encodeSmallAllocationSite(writer, value.small);
    encodeTraceId(writer, value.traceMap);
}

/** Decode one SmallAllocationSite. */
export function decodeSmallAllocationSite(reader: BinaryReader): SmallAllocationSite {
    const heap = heapCoreLayout.decodeAllocationSite(reader);
    const small = heapCoreLayout.decodeSmallAllocationSite(reader);
    const traceMap = decodeTraceId(reader);

    return {
        heap,
        small,
        traceMap,
    };
}

/** Return one JSON value for one SmallAllocationSite. */
export function toJsonSmallAllocationSite(value: SmallAllocationSite): Json {
    return {
        heap: heapCoreLayout.toJsonAllocationSite(value.heap),
        small: heapCoreLayout.toJsonSmallAllocationSite(value.small),
        traceMap: toJsonTraceId(value.traceMap),
    };
}

/** Return one SmallAllocationSite from one JSON value. */
export function fromJsonSmallAllocationSite(value: Json): SmallAllocationSite {
    const object = jsonObject(value);

    return {
        heap: heapCoreLayout.fromJsonAllocationSite(jsonField(object, "heap")),
        small: heapCoreLayout.fromJsonSmallAllocationSite(jsonField(object, "small")),
        traceMap: fromJsonTraceId(jsonField(object, "traceMap")),
    };
}
