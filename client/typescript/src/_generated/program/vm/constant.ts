/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, bytesFromJson, bytesToJson, jsonField, jsonObject, jsonString } from "../../../protocol/serde.js";

/** Constant value stored in lowered instructions. */
export type ConstValue =
    /** Constant payload for an aggregate value. */
    | {
          readonly kind: "aggregate";
          readonly aggregate: Uint8Array | readonly number[];
      }
;

export const ConstValue = {
    /** Constant payload for an aggregate value. */
    aggregate(aggregate: Uint8Array | readonly number[]): ConstValue {
        return { kind: "aggregate", aggregate };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: ConstValue): void {
        encodeConstValue(writer, value);
    },

    /** Decode one ConstValue. */
    decode(reader: BinaryReader): ConstValue {
        return decodeConstValue(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ConstValue): Json {
        return toJsonConstValue(value);
    },

    /** Return one ConstValue from one JSON value. */
    fromJson(value: Json): ConstValue {
        return fromJsonConstValue(value);
    },
};

/** Encode one ConstValue. */
export function encodeConstValue(writer: BinaryWriter, value: ConstValue): void {
    switch (value.kind) {
        case "aggregate":
            writer.writeUnsigned(0);
            writer.writeByteSlice(value.aggregate);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one ConstValue. */
export function decodeConstValue(reader: BinaryReader): ConstValue {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const aggregate = reader.readByteSlice();

            return { kind: "aggregate", aggregate };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one ConstValue. */
export function toJsonConstValue(value: ConstValue): Json {
    switch (value.kind) {
        case "aggregate":
            return {
                kind: "aggregate",
                aggregate: bytesToJson(value.aggregate),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one ConstValue from one JSON value. */
export function fromJsonConstValue(value: Json): ConstValue {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "aggregate":
            return {
                kind,
                aggregate: bytesFromJson(jsonField(object, "aggregate")),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}
