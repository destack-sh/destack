/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, jsonArray, jsonField, jsonInteger, jsonObject, jsonOptional, jsonString } from "../../protocol/serde.js";
import type { StringId } from "../core/string.js";
import type { FunctionId } from "./function.js";
import type { TypeId } from "./type.js";
import { decodeStringId, encodeStringId, fromJsonStringId, toJsonStringId } from "../core/string.js";
import { decodeFunctionId, encodeFunctionId, fromJsonFunctionId, toJsonFunctionId } from "./function.js";
import { decodeTypeId, encodeTypeId, fromJsonTypeId, toJsonTypeId } from "./type.js";

/** Executable dispatch table carried by one durable program. */
export type DispatchTable = {
    /** Virtual dispatch tables keyed by dense table index. */
    readonly virtualTables: ReadonlyArray<VirtualTable>;
    /** Dynamic dispatch tables keyed by dense table index. */
    readonly dynamicTables: ReadonlyArray<DynamicTable>;
    /** Dynamic table shapes keyed by constraint type. */
    readonly dynamicShapes: ReadonlyArray<DynamicShape>;
};

export const DispatchTable = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: DispatchTable): void {
        encodeDispatchTable(writer, value);
    },

    /** Decode one DispatchTable. */
    decode(reader: BinaryReader): DispatchTable {
        return decodeDispatchTable(reader);
    },

    /** Return this value as JSON. */
    toJson(value: DispatchTable): Json {
        return toJsonDispatchTable(value);
    },

    /** Return one DispatchTable from one JSON value. */
    fromJson(value: Json): DispatchTable {
        return fromJsonDispatchTable(value);
    },
};

/** Encode one DispatchTable. */
export function encodeDispatchTable(writer: BinaryWriter, value: DispatchTable): void {
    writer.writeUnsigned(value.virtualTables.length);
    for (const item0 of value.virtualTables) {
        encodeVirtualTable(writer, item0);
    }
    writer.writeUnsigned(value.dynamicTables.length);
    for (const item1 of value.dynamicTables) {
        encodeDynamicTable(writer, item1);
    }
    writer.writeUnsigned(value.dynamicShapes.length);
    for (const item2 of value.dynamicShapes) {
        encodeDynamicShape(writer, item2);
    }
}

/** Decode one DispatchTable. */
export function decodeDispatchTable(reader: BinaryReader): DispatchTable {
    const virtualTables = (() => { const length0 = reader.readNumber(); const items0: Array<VirtualTable> = []; for (let index = 0; index < length0; index += 1) { items0.push(decodeVirtualTable(reader)); } return items0; })();
    const dynamicTables = (() => { const length1 = reader.readNumber(); const items1: Array<DynamicTable> = []; for (let index = 0; index < length1; index += 1) { items1.push(decodeDynamicTable(reader)); } return items1; })();
    const dynamicShapes = (() => { const length2 = reader.readNumber(); const items2: Array<DynamicShape> = []; for (let index = 0; index < length2; index += 1) { items2.push(decodeDynamicShape(reader)); } return items2; })();

    return {
        virtualTables,
        dynamicTables,
        dynamicShapes,
    };
}

/** Return one JSON value for one DispatchTable. */
export function toJsonDispatchTable(value: DispatchTable): Json {
    return {
        virtualTables: value.virtualTables.map((item0) => toJsonVirtualTable(item0)),
        dynamicTables: value.dynamicTables.map((item0) => toJsonDynamicTable(item0)),
        dynamicShapes: value.dynamicShapes.map((item0) => toJsonDynamicShape(item0)),
    };
}

/** Return one DispatchTable from one JSON value. */
export function fromJsonDispatchTable(value: Json): DispatchTable {
    const object = jsonObject(value);

    return {
        virtualTables: jsonArray(jsonField(object, "virtualTables")).map((item0) => fromJsonVirtualTable(item0)),
        dynamicTables: jsonArray(jsonField(object, "dynamicTables")).map((item0) => fromJsonDynamicTable(item0)),
        dynamicShapes: jsonArray(jsonField(object, "dynamicShapes")).map((item0) => fromJsonDynamicShape(item0)),
    };
}

/** Executable virtual dispatch table for one concrete type. */
export type VirtualTable = {
    /** Concrete type owning this table. */
    readonly ty: TypeId;
    /** Drop glue function when one exists. */
    readonly destructor?: FunctionId;
    /** Method implementations in runtime slot order. */
    readonly methods: ReadonlyArray<FunctionId>;
};

export const VirtualTable = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: VirtualTable): void {
        encodeVirtualTable(writer, value);
    },

    /** Decode one VirtualTable. */
    decode(reader: BinaryReader): VirtualTable {
        return decodeVirtualTable(reader);
    },

    /** Return this value as JSON. */
    toJson(value: VirtualTable): Json {
        return toJsonVirtualTable(value);
    },

    /** Return one VirtualTable from one JSON value. */
    fromJson(value: Json): VirtualTable {
        return fromJsonVirtualTable(value);
    },
};

/** Encode one VirtualTable. */
export function encodeVirtualTable(writer: BinaryWriter, value: VirtualTable): void {
    encodeTypeId(writer, value.ty);
    writer.writeOption(value.destructor, (value1) => {
        encodeFunctionId(writer, value1);
    });
    writer.writeUnsigned(value.methods.length);
    for (const item2 of value.methods) {
        encodeFunctionId(writer, item2);
    }
}

/** Decode one VirtualTable. */
export function decodeVirtualTable(reader: BinaryReader): VirtualTable {
    const ty = decodeTypeId(reader);
    const destructor = reader.readOption(() => decodeFunctionId(reader));
    const methods = (() => { const length2 = reader.readNumber(); const items2: Array<FunctionId> = []; for (let index = 0; index < length2; index += 1) { items2.push(decodeFunctionId(reader)); } return items2; })();

    return {
        ty,
        ...(destructor === undefined ? {} : { destructor }),
        methods,
    };
}

/** Return one JSON value for one VirtualTable. */
export function toJsonVirtualTable(value: VirtualTable): Json {
    return {
        ty: toJsonTypeId(value.ty),
        ...(value.destructor === undefined ? {} : { destructor: toJsonFunctionId(value.destructor) }),
        methods: value.methods.map((item0) => toJsonFunctionId(item0)),
    };
}

/** Return one VirtualTable from one JSON value. */
export function fromJsonVirtualTable(value: Json): VirtualTable {
    const object = jsonObject(value);

    return {
        ty: fromJsonTypeId(jsonField(object, "ty")),
        destructor: jsonOptional(object, "destructor", (value) => fromJsonFunctionId(value)),
        methods: jsonArray(jsonField(object, "methods")).map((item0) => fromJsonFunctionId(item0)),
    };
}

/** Executable dynamic dispatch table for one concrete implementation. */
export type DynamicTable = {
    /** Concrete type providing the implementation. */
    readonly concrete: TypeId;
    /** Dynamic constraint type being implemented. */
    readonly constraint: TypeId;
    /** Entries in runtime slot order. */
    readonly entries: ReadonlyArray<DynamicEntry>;
};

export const DynamicTable = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: DynamicTable): void {
        encodeDynamicTable(writer, value);
    },

    /** Decode one DynamicTable. */
    decode(reader: BinaryReader): DynamicTable {
        return decodeDynamicTable(reader);
    },

    /** Return this value as JSON. */
    toJson(value: DynamicTable): Json {
        return toJsonDynamicTable(value);
    },

    /** Return one DynamicTable from one JSON value. */
    fromJson(value: Json): DynamicTable {
        return fromJsonDynamicTable(value);
    },
};

/** Encode one DynamicTable. */
export function encodeDynamicTable(writer: BinaryWriter, value: DynamicTable): void {
    encodeTypeId(writer, value.concrete);
    encodeTypeId(writer, value.constraint);
    writer.writeUnsigned(value.entries.length);
    for (const item2 of value.entries) {
        encodeDynamicEntry(writer, item2);
    }
}

/** Decode one DynamicTable. */
export function decodeDynamicTable(reader: BinaryReader): DynamicTable {
    const concrete = decodeTypeId(reader);
    const constraint = decodeTypeId(reader);
    const entries = (() => { const length2 = reader.readNumber(); const items2: Array<DynamicEntry> = []; for (let index = 0; index < length2; index += 1) { items2.push(decodeDynamicEntry(reader)); } return items2; })();

    return {
        concrete,
        constraint,
        entries,
    };
}

/** Return one JSON value for one DynamicTable. */
export function toJsonDynamicTable(value: DynamicTable): Json {
    return {
        concrete: toJsonTypeId(value.concrete),
        constraint: toJsonTypeId(value.constraint),
        entries: value.entries.map((item0) => toJsonDynamicEntry(item0)),
    };
}

/** Return one DynamicTable from one JSON value. */
export function fromJsonDynamicTable(value: Json): DynamicTable {
    const object = jsonObject(value);

    return {
        concrete: fromJsonTypeId(jsonField(object, "concrete")),
        constraint: fromJsonTypeId(jsonField(object, "constraint")),
        entries: jsonArray(jsonField(object, "entries")).map((item0) => fromJsonDynamicEntry(item0)),
    };
}

/** Executable dynamic dispatch table entry. */
export type DynamicEntry =
    /** Slot containing a field byte offset. */
    | {
          readonly kind: "fieldOffset";
          /** Field offset in bytes. */
          readonly offset: number;
      }
    /** Slot containing a function implementation. */
    | {
          readonly kind: "function";
          /** Concrete function implementation. */
          readonly function: FunctionId;
      }
;

export const DynamicEntry = {
    /** Slot containing a field byte offset. */
    fieldOffset(offset: number): DynamicEntry {
        return { kind: "fieldOffset", offset };
    },

    /** Slot containing a function implementation. */
    "function"(function_: FunctionId): DynamicEntry {
        return { kind: "function", function: function_ };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: DynamicEntry): void {
        encodeDynamicEntry(writer, value);
    },

    /** Decode one DynamicEntry. */
    decode(reader: BinaryReader): DynamicEntry {
        return decodeDynamicEntry(reader);
    },

    /** Return this value as JSON. */
    toJson(value: DynamicEntry): Json {
        return toJsonDynamicEntry(value);
    },

    /** Return one DynamicEntry from one JSON value. */
    fromJson(value: Json): DynamicEntry {
        return fromJsonDynamicEntry(value);
    },
};

/** Encode one DynamicEntry. */
export function encodeDynamicEntry(writer: BinaryWriter, value: DynamicEntry): void {
    switch (value.kind) {
        case "fieldOffset":
            writer.writeUnsigned(0);
            writer.writeUnsigned(value.offset);
            return;
        case "function":
            writer.writeUnsigned(1);
            encodeFunctionId(writer, value.function);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one DynamicEntry. */
export function decodeDynamicEntry(reader: BinaryReader): DynamicEntry {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const offset = reader.readNumber();

            return {
                kind: "fieldOffset",
                offset,
            };
        }
        case 1: {
            const function_ = decodeFunctionId(reader);

            return {
                kind: "function",
                function: function_,
            };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one DynamicEntry. */
export function toJsonDynamicEntry(value: DynamicEntry): Json {
    switch (value.kind) {
        case "fieldOffset":
            return {
                kind: "fieldOffset",
                offset: value.offset,
            };
        case "function":
            return {
                kind: "function",
                function: toJsonFunctionId(value.function),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one DynamicEntry from one JSON value. */
export function fromJsonDynamicEntry(value: Json): DynamicEntry {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "fieldOffset":
            return {
                kind,
                offset: jsonInteger(jsonField(object, "offset")),
            };
        case "function":
            return {
                kind,
                function: fromJsonFunctionId(jsonField(object, "function")),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** Executable dynamic table shape for one constraint type. */
export type DynamicShape = {
    /** Dynamic constraint type owning this shape. */
    readonly constraint: TypeId;
    /** Slots in declaration order. */
    readonly slots: ReadonlyArray<DynamicSlot>;
};

export const DynamicShape = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: DynamicShape): void {
        encodeDynamicShape(writer, value);
    },

    /** Decode one DynamicShape. */
    decode(reader: BinaryReader): DynamicShape {
        return decodeDynamicShape(reader);
    },

    /** Return this value as JSON. */
    toJson(value: DynamicShape): Json {
        return toJsonDynamicShape(value);
    },

    /** Return one DynamicShape from one JSON value. */
    fromJson(value: Json): DynamicShape {
        return fromJsonDynamicShape(value);
    },
};

/** Encode one DynamicShape. */
export function encodeDynamicShape(writer: BinaryWriter, value: DynamicShape): void {
    encodeTypeId(writer, value.constraint);
    writer.writeUnsigned(value.slots.length);
    for (const item1 of value.slots) {
        encodeDynamicSlot(writer, item1);
    }
}

/** Decode one DynamicShape. */
export function decodeDynamicShape(reader: BinaryReader): DynamicShape {
    const constraint = decodeTypeId(reader);
    const slots = (() => { const length1 = reader.readNumber(); const items1: Array<DynamicSlot> = []; for (let index = 0; index < length1; index += 1) { items1.push(decodeDynamicSlot(reader)); } return items1; })();

    return {
        constraint,
        slots,
    };
}

/** Return one JSON value for one DynamicShape. */
export function toJsonDynamicShape(value: DynamicShape): Json {
    return {
        constraint: toJsonTypeId(value.constraint),
        slots: value.slots.map((item0) => toJsonDynamicSlot(item0)),
    };
}

/** Return one DynamicShape from one JSON value. */
export function fromJsonDynamicShape(value: Json): DynamicShape {
    const object = jsonObject(value);

    return {
        constraint: fromJsonTypeId(jsonField(object, "constraint")),
        slots: jsonArray(jsonField(object, "slots")).map((item0) => fromJsonDynamicSlot(item0)),
    };
}

/** Executable dynamic slot descriptor. */
export type DynamicSlot =
    /** Field slot. */
    | {
          readonly kind: "field";
          /** Field name. */
          readonly name: StringId;
      }
    /** Function slot. */
    | {
          readonly kind: "function";
          /** Function name, absent for call signatures. */
          readonly name?: StringId;
          /** Function signature. */
          readonly signature: TypeId;
      }
;

export const DynamicSlot = {
    /** Field slot. */
    field(name: StringId): DynamicSlot {
        return { kind: "field", name };
    },

    /** Function slot. */
    "function"(name: StringId | undefined, signature: TypeId): DynamicSlot {
        return { kind: "function", name, signature };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: DynamicSlot): void {
        encodeDynamicSlot(writer, value);
    },

    /** Decode one DynamicSlot. */
    decode(reader: BinaryReader): DynamicSlot {
        return decodeDynamicSlot(reader);
    },

    /** Return this value as JSON. */
    toJson(value: DynamicSlot): Json {
        return toJsonDynamicSlot(value);
    },

    /** Return one DynamicSlot from one JSON value. */
    fromJson(value: Json): DynamicSlot {
        return fromJsonDynamicSlot(value);
    },
};

/** Encode one DynamicSlot. */
export function encodeDynamicSlot(writer: BinaryWriter, value: DynamicSlot): void {
    switch (value.kind) {
        case "field":
            writer.writeUnsigned(0);
            encodeStringId(writer, value.name);
            return;
        case "function":
            writer.writeUnsigned(1);
            writer.writeOption(value.name, (value0) => {
                encodeStringId(writer, value0);
            });
            encodeTypeId(writer, value.signature);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one DynamicSlot. */
export function decodeDynamicSlot(reader: BinaryReader): DynamicSlot {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const name = decodeStringId(reader);

            return {
                kind: "field",
                name,
            };
        }
        case 1: {
            const name = reader.readOption(() => decodeStringId(reader));
            const signature = decodeTypeId(reader);

            return {
                kind: "function",
                ...(name === undefined ? {} : { name }),
                signature,
            };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one DynamicSlot. */
export function toJsonDynamicSlot(value: DynamicSlot): Json {
    switch (value.kind) {
        case "field":
            return {
                kind: "field",
                name: toJsonStringId(value.name),
            };
        case "function":
            return {
                kind: "function",
                ...(value.name === undefined ? {} : { name: toJsonStringId(value.name) }),
                signature: toJsonTypeId(value.signature),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one DynamicSlot from one JSON value. */
export function fromJsonDynamicSlot(value: Json): DynamicSlot {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "field":
            return {
                kind,
                name: fromJsonStringId(jsonField(object, "name")),
            };
        case "function":
            return {
                kind,
                name: jsonOptional(object, "name", (value) => fromJsonStringId(value)),
                signature: fromJsonTypeId(jsonField(object, "signature")),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}
