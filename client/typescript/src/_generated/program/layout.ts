/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, jsonField, jsonObject } from "../../protocol/serde.js";
import type { SectionSlice } from "../core/section.js";
import { decodeSectionSlice, encodeSectionSlice, fromJsonSectionSlice, toJsonSectionSlice } from "../core/section.js";

/** Shared layout table for runtime values. */
export type LayoutTable = {
    /** Layout entries indexed by LayoutId. */
    readonly layouts: SectionSlice;
    /** Flattened field layout entries. */
    readonly fields: SectionSlice;
    /** Flattened variant case layout entries. */
    readonly variants: SectionSlice;
    /** Flattened signature parameter type entries. */
    readonly parameters: SectionSlice;
    /** Flattened tensor sharding axis entries. */
    readonly tensorAxes: SectionSlice;
};

export const LayoutTable = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: LayoutTable): void {
        encodeLayoutTable(writer, value);
    },

    /** Decode one LayoutTable. */
    decode(reader: BinaryReader): LayoutTable {
        return decodeLayoutTable(reader);
    },

    /** Return this value as JSON. */
    toJson(value: LayoutTable): Json {
        return toJsonLayoutTable(value);
    },

    /** Return one LayoutTable from one JSON value. */
    fromJson(value: Json): LayoutTable {
        return fromJsonLayoutTable(value);
    },
};

/** Encode one LayoutTable. */
export function encodeLayoutTable(writer: BinaryWriter, value: LayoutTable): void {
    encodeSectionSlice(writer, value.layouts);
    encodeSectionSlice(writer, value.fields);
    encodeSectionSlice(writer, value.variants);
    encodeSectionSlice(writer, value.parameters);
    encodeSectionSlice(writer, value.tensorAxes);
}

/** Decode one LayoutTable. */
export function decodeLayoutTable(reader: BinaryReader): LayoutTable {
    const layouts = decodeSectionSlice(reader);
    const fields = decodeSectionSlice(reader);
    const variants = decodeSectionSlice(reader);
    const parameters = decodeSectionSlice(reader);
    const tensorAxes = decodeSectionSlice(reader);

    return {
        layouts,
        fields,
        variants,
        parameters,
        tensorAxes,
    };
}

/** Return one JSON value for one LayoutTable. */
export function toJsonLayoutTable(value: LayoutTable): Json {
    return {
        layouts: toJsonSectionSlice(value.layouts),
        fields: toJsonSectionSlice(value.fields),
        variants: toJsonSectionSlice(value.variants),
        parameters: toJsonSectionSlice(value.parameters),
        tensorAxes: toJsonSectionSlice(value.tensorAxes),
    };
}

/** Return one LayoutTable from one JSON value. */
export function fromJsonLayoutTable(value: Json): LayoutTable {
    const object = jsonObject(value);

    return {
        layouts: fromJsonSectionSlice(jsonField(object, "layouts")),
        fields: fromJsonSectionSlice(jsonField(object, "fields")),
        variants: fromJsonSectionSlice(jsonField(object, "variants")),
        parameters: fromJsonSectionSlice(jsonField(object, "parameters")),
        tensorAxes: fromJsonSectionSlice(jsonField(object, "tensorAxes")),
    };
}
