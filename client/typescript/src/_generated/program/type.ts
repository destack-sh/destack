/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, jsonField, jsonObject } from "../../protocol/serde.js";
import type { SectionSlice } from "../core/section.js";
import { decodeSectionSlice, encodeSectionSlice, fromJsonSectionSlice, toJsonSectionSlice } from "../core/section.js";

/** Runtime type table carried by one durable program. */
export type TypeTable = {
    /** Stable fingerprints keyed by program type id. */
    readonly fingerprints: SectionSlice;
    /** Dense runtime type descriptors keyed by program type id. */
    readonly descriptors: SectionSlice;
    /** Flattened runtime supertype ids. */
    readonly supertypes: SectionSlice;
};

export const TypeTable = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: TypeTable): void {
        encodeTypeTable(writer, value);
    },

    /** Decode one TypeTable. */
    decode(reader: BinaryReader): TypeTable {
        return decodeTypeTable(reader);
    },

    /** Return this value as JSON. */
    toJson(value: TypeTable): Json {
        return toJsonTypeTable(value);
    },

    /** Return one TypeTable from one JSON value. */
    fromJson(value: Json): TypeTable {
        return fromJsonTypeTable(value);
    },
};

/** Encode one TypeTable. */
export function encodeTypeTable(writer: BinaryWriter, value: TypeTable): void {
    encodeSectionSlice(writer, value.fingerprints);
    encodeSectionSlice(writer, value.descriptors);
    encodeSectionSlice(writer, value.supertypes);
}

/** Decode one TypeTable. */
export function decodeTypeTable(reader: BinaryReader): TypeTable {
    const fingerprints = decodeSectionSlice(reader);
    const descriptors = decodeSectionSlice(reader);
    const supertypes = decodeSectionSlice(reader);

    return {
        fingerprints,
        descriptors,
        supertypes,
    };
}

/** Return one JSON value for one TypeTable. */
export function toJsonTypeTable(value: TypeTable): Json {
    return {
        fingerprints: toJsonSectionSlice(value.fingerprints),
        descriptors: toJsonSectionSlice(value.descriptors),
        supertypes: toJsonSectionSlice(value.supertypes),
    };
}

/** Return one TypeTable from one JSON value. */
export function fromJsonTypeTable(value: Json): TypeTable {
    const object = jsonObject(value);

    return {
        fingerprints: fromJsonSectionSlice(jsonField(object, "fingerprints")),
        descriptors: fromJsonSectionSlice(jsonField(object, "descriptors")),
        supertypes: fromJsonSectionSlice(jsonField(object, "supertypes")),
    };
}
