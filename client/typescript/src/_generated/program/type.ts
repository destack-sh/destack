/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, compareBytes, jsonArray, jsonField, jsonInteger, jsonObject, jsonOptional, nestedBytes } from "../../protocol/serde.js";
import type { LayoutId } from "../mir/metadata/layout.js";
import type { LocalNodeId } from "../mir/tree/node.js";
import type { Type } from "../mir/tree/type.js";
import { decodeLayoutId, encodeLayoutId, fromJsonLayoutId, toJsonLayoutId } from "../mir/metadata/layout.js";
import { decodeLocalNodeId, encodeLocalNodeId, fromJsonLocalNodeId, toJsonLocalNodeId } from "../mir/tree/node.js";
import { decodeType, encodeType, fromJsonType, toJsonType } from "../mir/tree/type.js";

/** Runtime type metadata carried by one durable program. */
export type TypeTable = {
    /** Dense runtime type records keyed by program type id. */
    readonly types: ReadonlyArray<Type | undefined>;
    /** MIR type id by program type id. */
    readonly mirTypes: ReadonlyArray<LocalNodeId | undefined>;
    /** Program type id by MIR type id. */
    readonly typeIds: ReadonlyMap<LocalNodeId, TypeId>;
    /** Dense runtime layout ids keyed by program type id. */
    readonly layoutByType: ReadonlyArray<LayoutId | undefined>;
};

export const TypeTable = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: TypeTable): void {
        encodeTypeTable(writer, value);
    },

    /** Decode one TypeTable. */
    decode(reader: BinaryReader): TypeTable {
        return decodeTypeTable(reader);
    },

    /** Return this value as JSON. */
    toJson(value: TypeTable): Json {
        return toJsonTypeTable(value);
    },

    /** Return one TypeTable from one JSON value. */
    fromJson(value: Json): TypeTable {
        return fromJsonTypeTable(value);
    },
};

/** Encode one TypeTable. */
export function encodeTypeTable(writer: BinaryWriter, value: TypeTable): void {
    writer.writeUnsigned(value.types.length);
    for (const item0 of value.types) {
        writer.writeOption(item0, (value1) => {
            encodeType(writer, value1);
        });
    }
    writer.writeUnsigned(value.mirTypes.length);
    for (const item1 of value.mirTypes) {
        writer.writeOption(item1, (value2) => {
            encodeLocalNodeId(writer, value2);
        });
    }
    const entries2 = Array.from(value.typeIds.entries()).map(([key2, item2]) => {
        const keyBytes = nestedBytes((writer) => {
            encodeLocalNodeId(writer, key2);
        });
        return { key2, item2, keyBytes };
    });
    entries2.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries2.length);
    for (const entry2 of entries2) {
        encodeLocalNodeId(writer, entry2.key2);
        encodeTypeId(writer, entry2.item2);
    }
    writer.writeUnsigned(value.layoutByType.length);
    for (const item3 of value.layoutByType) {
        writer.writeOption(item3, (value4) => {
            encodeLayoutId(writer, value4);
        });
    }
}

/** Decode one TypeTable. */
export function decodeTypeTable(reader: BinaryReader): TypeTable {
    const types = (() => { const length0 = reader.readNumber(); const items0: Array<Type | undefined> = []; for (let index = 0; index < length0; index += 1) { items0.push(reader.readOption(() => decodeType(reader))); } return items0; })();
    const mirTypes = (() => { const length1 = reader.readNumber(); const items1: Array<LocalNodeId | undefined> = []; for (let index = 0; index < length1; index += 1) { items1.push(reader.readOption(() => decodeLocalNodeId(reader))); } return items1; })();
    const typeIds = (() => { const length2 = reader.readNumber(); const items2 = new Map<LocalNodeId, TypeId>(); for (let index = 0; index < length2; index += 1) { items2.set(decodeLocalNodeId(reader), decodeTypeId(reader)); } return items2; })();
    const layoutByType = (() => { const length3 = reader.readNumber(); const items3: Array<LayoutId | undefined> = []; for (let index = 0; index < length3; index += 1) { items3.push(reader.readOption(() => decodeLayoutId(reader))); } return items3; })();

    return {
        types,
        mirTypes,
        typeIds,
        layoutByType,
    };
}

/** Return one JSON value for one TypeTable. */
export function toJsonTypeTable(value: TypeTable): Json {
    return {
        types: value.types.map((item0) => item0 === undefined ? null : toJsonType(item0)),
        mirTypes: value.mirTypes.map((item0) => item0 === undefined ? null : toJsonLocalNodeId(item0)),
        typeIds: Array.from(value.typeIds.entries()).map(([key0, item0]) => [toJsonLocalNodeId(key0), toJsonTypeId(item0)] as const),
        layoutByType: value.layoutByType.map((item0) => item0 === undefined ? null : toJsonLayoutId(item0)),
    };
}

/** Return one TypeTable from one JSON value. */
export function fromJsonTypeTable(value: Json): TypeTable {
    const object = jsonObject(value);

    return {
        types: jsonArray(jsonField(object, "types")).map((item0) => item0 === null ? undefined : fromJsonType(item0)),
        mirTypes: jsonArray(jsonField(object, "mirTypes")).map((item0) => item0 === null ? undefined : fromJsonLocalNodeId(item0)),
        typeIds: new Map(jsonArray(jsonField(object, "typeIds")).map((entry) => { const items = jsonArray(entry); if (items.length !== 2) { throw new SerdeError(`expected JSON map entry length 2: ${items.length}`); } const key0 = items[0]; const item0 = items[1]; return [fromJsonLocalNodeId(key0), fromJsonTypeId(item0)] as const; })),
        layoutByType: jsonArray(jsonField(object, "layoutByType")).map((item0) => item0 === null ? undefined : fromJsonLayoutId(item0)),
    };
}

/** Durable runtime type id inside one program. */
export type TypeId = number;

export const TypeId = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: TypeId): void {
        encodeTypeId(writer, value);
    },

    /** Decode one TypeId. */
    decode(reader: BinaryReader): TypeId {
        return decodeTypeId(reader);
    },

    /** Return this value as JSON. */
    toJson(value: TypeId): Json {
        return toJsonTypeId(value);
    },

    /** Return one TypeId from one JSON value. */
    fromJson(value: Json): TypeId {
        return fromJsonTypeId(value);
    },
};

/** Encode one TypeId. */
export function encodeTypeId(writer: BinaryWriter, value: TypeId): void {
    writer.writeUnsigned(value);
}

/** Decode one TypeId. */
export function decodeTypeId(reader: BinaryReader): TypeId {
    return reader.readNumber();
}

/** Return one JSON value for one TypeId. */
export function toJsonTypeId(value: TypeId): Json {
    return value;
}

/** Return one TypeId from one JSON value. */
export function fromJsonTypeId(value: Json): TypeId {
    return jsonInteger(value);
}
