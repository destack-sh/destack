/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, jsonField, jsonObject } from "../../protocol/serde.js";
import type { SectionSlice } from "../core/section.js";
import { decodeSectionSlice, encodeSectionSlice, fromJsonSectionSlice, toJsonSectionSlice } from "../core/section.js";

/** Program sites used by debugging, probes, and observations. */
export type SiteTable = {
    /** Allocation sites sorted by program point. */
    readonly allocations: SectionSlice;
    /** Addressable memory operation sites sorted by program point. */
    readonly memory: SectionSlice;
    /** Function call operation sites sorted by program point. */
    readonly calls: SectionSlice;
    /** Control-flow edge sites sorted by source and target point. */
    readonly edges: SectionSlice;
    /** Explicit counter sites sorted by program point. */
    readonly counters: SectionSlice;
    /** Explicit sample sites sorted by program point. */
    readonly samples: SectionSlice;
};

export const SiteTable = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: SiteTable): void {
        encodeSiteTable(writer, value);
    },

    /** Decode one SiteTable. */
    decode(reader: BinaryReader): SiteTable {
        return decodeSiteTable(reader);
    },

    /** Return this value as JSON. */
    toJson(value: SiteTable): Json {
        return toJsonSiteTable(value);
    },

    /** Return one SiteTable from one JSON value. */
    fromJson(value: Json): SiteTable {
        return fromJsonSiteTable(value);
    },
};

/** Encode one SiteTable. */
export function encodeSiteTable(writer: BinaryWriter, value: SiteTable): void {
    encodeSectionSlice(writer, value.allocations);
    encodeSectionSlice(writer, value.memory);
    encodeSectionSlice(writer, value.calls);
    encodeSectionSlice(writer, value.edges);
    encodeSectionSlice(writer, value.counters);
    encodeSectionSlice(writer, value.samples);
}

/** Decode one SiteTable. */
export function decodeSiteTable(reader: BinaryReader): SiteTable {
    const allocations = decodeSectionSlice(reader);
    const memory = decodeSectionSlice(reader);
    const calls = decodeSectionSlice(reader);
    const edges = decodeSectionSlice(reader);
    const counters = decodeSectionSlice(reader);
    const samples = decodeSectionSlice(reader);

    return {
        allocations,
        memory,
        calls,
        edges,
        counters,
        samples,
    };
}

/** Return one JSON value for one SiteTable. */
export function toJsonSiteTable(value: SiteTable): Json {
    return {
        allocations: toJsonSectionSlice(value.allocations),
        memory: toJsonSectionSlice(value.memory),
        calls: toJsonSectionSlice(value.calls),
        edges: toJsonSectionSlice(value.edges),
        counters: toJsonSectionSlice(value.counters),
        samples: toJsonSectionSlice(value.samples),
    };
}

/** Return one SiteTable from one JSON value. */
export function fromJsonSiteTable(value: Json): SiteTable {
    const object = jsonObject(value);

    return {
        allocations: fromJsonSectionSlice(jsonField(object, "allocations")),
        memory: fromJsonSectionSlice(jsonField(object, "memory")),
        calls: fromJsonSectionSlice(jsonField(object, "calls")),
        edges: fromJsonSectionSlice(jsonField(object, "edges")),
        counters: fromJsonSectionSlice(jsonField(object, "counters")),
        samples: fromJsonSectionSlice(jsonField(object, "samples")),
    };
}
