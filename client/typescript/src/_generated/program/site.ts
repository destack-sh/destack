/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, jsonField, jsonInteger, jsonObject, jsonOptional, jsonString } from "../../protocol/serde.js";
import type { Space } from "../mir/tree/type.js";
import type { Storage } from "../mir/tree/type.js";
import type { VirtualTableId } from "./dispatch.js";
import type { FunctionId } from "./function.js";
import type { SignatureId } from "./function.js";
import type { LayoutId } from "./layout.js";
import type { ProgramPoint } from "./point.js";
import type { TypeId } from "./type.js";
import { decodeSpace, encodeSpace, fromJsonSpace, toJsonSpace } from "../mir/tree/type.js";
import { decodeStorage, encodeStorage, fromJsonStorage, toJsonStorage } from "../mir/tree/type.js";
import { decodeVirtualTableId, encodeVirtualTableId, fromJsonVirtualTableId, toJsonVirtualTableId } from "./dispatch.js";
import { decodeFunctionId, encodeFunctionId, fromJsonFunctionId, toJsonFunctionId } from "./function.js";
import { decodeSignatureId, encodeSignatureId, fromJsonSignatureId, toJsonSignatureId } from "./function.js";
import { decodeLayoutId, encodeLayoutId, fromJsonLayoutId, toJsonLayoutId } from "./layout.js";
import { decodeProgramPoint, encodeProgramPoint, fromJsonProgramPoint, toJsonProgramPoint } from "./point.js";
import { decodeTypeId, encodeTypeId, fromJsonTypeId, toJsonTypeId } from "./type.js";

/** Dense allocation site identifier within one program. */
export type AllocationSiteId = number;

export const AllocationSiteId = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: AllocationSiteId): void {
        encodeAllocationSiteId(writer, value);
    },

    /** Decode one AllocationSiteId. */
    decode(reader: BinaryReader): AllocationSiteId {
        return decodeAllocationSiteId(reader);
    },

    /** Return this value as JSON. */
    toJson(value: AllocationSiteId): Json {
        return toJsonAllocationSiteId(value);
    },

    /** Return one AllocationSiteId from one JSON value. */
    fromJson(value: Json): AllocationSiteId {
        return fromJsonAllocationSiteId(value);
    },
};

/** Encode one AllocationSiteId. */
export function encodeAllocationSiteId(writer: BinaryWriter, value: AllocationSiteId): void {
    writer.writeUnsigned(value);
}

/** Decode one AllocationSiteId. */
export function decodeAllocationSiteId(reader: BinaryReader): AllocationSiteId {
    return reader.readNumber();
}

/** Return one JSON value for one AllocationSiteId. */
export function toJsonAllocationSiteId(value: AllocationSiteId): Json {
    return value;
}

/** Return one AllocationSiteId from one JSON value. */
export function fromJsonAllocationSiteId(value: Json): AllocationSiteId {
    return jsonInteger(value);
}

/** Memory access operation. */
export type MemoryAccess = "read" | "write" | "readWrite";

export const MemoryAccess = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: MemoryAccess): void {
        encodeMemoryAccess(writer, value);
    },

    /** Decode one MemoryAccess. */
    decode(reader: BinaryReader): MemoryAccess {
        return decodeMemoryAccess(reader);
    },

    /** Return this value as JSON. */
    toJson(value: MemoryAccess): Json {
        return toJsonMemoryAccess(value);
    },

    /** Return one MemoryAccess from one JSON value. */
    fromJson(value: Json): MemoryAccess {
        return fromJsonMemoryAccess(value);
    },
};

/** Encode one MemoryAccess. */
export function encodeMemoryAccess(writer: BinaryWriter, value: MemoryAccess): void {
    switch (value) {
        case "read":
            writer.writeUnsigned(0);
            return;
        case "write":
            writer.writeUnsigned(1);
            return;
        case "readWrite":
            writer.writeUnsigned(2);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one MemoryAccess. */
export function decodeMemoryAccess(reader: BinaryReader): MemoryAccess {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "read";
        case 1:
            return "write";
        case 2:
            return "readWrite";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one MemoryAccess. */
export function toJsonMemoryAccess(value: MemoryAccess): Json {
    return value;
}

/** Return one MemoryAccess from one JSON value. */
export function fromJsonMemoryAccess(value: Json): MemoryAccess {
    const variant = jsonString(value);

    switch (variant) {
        case "read":
            return "read";
        case "write":
            return "write";
        case "readWrite":
            return "readWrite";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** One allocation site. */
export type AllocationSite = {
    /** The program point that performs the allocation. */
    readonly point: ProgramPoint;
    /** The storage space receiving the allocated storage. */
    readonly space: Space;
    /** The type produced by the allocation expression. */
    readonly resultType: TypeId;
    /** The type used for the allocated storage. */
    readonly storageType: TypeId;
    /** The physical layout used for the allocated storage. */
    readonly layout: LayoutId;
    /** The virtual table written into the allocation when present. */
    readonly virtualTable?: VirtualTableId;
};

export const AllocationSite = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: AllocationSite): void {
        encodeAllocationSite(writer, value);
    },

    /** Decode one AllocationSite. */
    decode(reader: BinaryReader): AllocationSite {
        return decodeAllocationSite(reader);
    },

    /** Return this value as JSON. */
    toJson(value: AllocationSite): Json {
        return toJsonAllocationSite(value);
    },

    /** Return one AllocationSite from one JSON value. */
    fromJson(value: Json): AllocationSite {
        return fromJsonAllocationSite(value);
    },
};

/** Encode one AllocationSite. */
export function encodeAllocationSite(writer: BinaryWriter, value: AllocationSite): void {
    encodeProgramPoint(writer, value.point);
    encodeSpace(writer, value.space);
    encodeTypeId(writer, value.resultType);
    encodeTypeId(writer, value.storageType);
    encodeLayoutId(writer, value.layout);
    writer.writeOption(value.virtualTable, (value5) => {
        encodeVirtualTableId(writer, value5);
    });
}

/** Decode one AllocationSite. */
export function decodeAllocationSite(reader: BinaryReader): AllocationSite {
    const point = decodeProgramPoint(reader);
    const space = decodeSpace(reader);
    const resultType = decodeTypeId(reader);
    const storageType = decodeTypeId(reader);
    const layout = decodeLayoutId(reader);
    const virtualTable = reader.readOption(() => decodeVirtualTableId(reader));

    return {
        point,
        space,
        resultType,
        storageType,
        layout,
        ...(virtualTable === undefined ? {} : { virtualTable }),
    };
}

/** Return one JSON value for one AllocationSite. */
export function toJsonAllocationSite(value: AllocationSite): Json {
    return {
        point: toJsonProgramPoint(value.point),
        space: toJsonSpace(value.space),
        resultType: toJsonTypeId(value.resultType),
        storageType: toJsonTypeId(value.storageType),
        layout: toJsonLayoutId(value.layout),
        ...(value.virtualTable === undefined ? {} : { virtualTable: toJsonVirtualTableId(value.virtualTable) }),
    };
}

/** Return one AllocationSite from one JSON value. */
export function fromJsonAllocationSite(value: Json): AllocationSite {
    const object = jsonObject(value);

    return {
        point: fromJsonProgramPoint(jsonField(object, "point")),
        space: fromJsonSpace(jsonField(object, "space")),
        resultType: fromJsonTypeId(jsonField(object, "resultType")),
        storageType: fromJsonTypeId(jsonField(object, "storageType")),
        layout: fromJsonLayoutId(jsonField(object, "layout")),
        virtualTable: jsonOptional(object, "virtualTable", (value) => fromJsonVirtualTableId(value)),
    };
}

/** Call dispatch mechanism. */
export type CallDispatch = "direct" | "virtual" | "dynamic" | "indirect";

export const CallDispatch = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: CallDispatch): void {
        encodeCallDispatch(writer, value);
    },

    /** Decode one CallDispatch. */
    decode(reader: BinaryReader): CallDispatch {
        return decodeCallDispatch(reader);
    },

    /** Return this value as JSON. */
    toJson(value: CallDispatch): Json {
        return toJsonCallDispatch(value);
    },

    /** Return one CallDispatch from one JSON value. */
    fromJson(value: Json): CallDispatch {
        return fromJsonCallDispatch(value);
    },
};

/** Encode one CallDispatch. */
export function encodeCallDispatch(writer: BinaryWriter, value: CallDispatch): void {
    switch (value) {
        case "direct":
            writer.writeUnsigned(0);
            return;
        case "virtual":
            writer.writeUnsigned(1);
            return;
        case "dynamic":
            writer.writeUnsigned(2);
            return;
        case "indirect":
            writer.writeUnsigned(3);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one CallDispatch. */
export function decodeCallDispatch(reader: BinaryReader): CallDispatch {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "direct";
        case 1:
            return "virtual";
        case 2:
            return "dynamic";
        case 3:
            return "indirect";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one CallDispatch. */
export function toJsonCallDispatch(value: CallDispatch): Json {
    return value;
}

/** Return one CallDispatch from one JSON value. */
export function fromJsonCallDispatch(value: Json): CallDispatch {
    const variant = jsonString(value);

    switch (variant) {
        case "direct":
            return "direct";
        case "virtual":
            return "virtual";
        case "dynamic":
            return "dynamic";
        case "indirect":
            return "indirect";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** Call return mode. */
export type CallMode = "return" | "tail";

export const CallMode = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: CallMode): void {
        encodeCallMode(writer, value);
    },

    /** Decode one CallMode. */
    decode(reader: BinaryReader): CallMode {
        return decodeCallMode(reader);
    },

    /** Return this value as JSON. */
    toJson(value: CallMode): Json {
        return toJsonCallMode(value);
    },

    /** Return one CallMode from one JSON value. */
    fromJson(value: Json): CallMode {
        return fromJsonCallMode(value);
    },
};

/** Encode one CallMode. */
export function encodeCallMode(writer: BinaryWriter, value: CallMode): void {
    switch (value) {
        case "return":
            writer.writeUnsigned(0);
            return;
        case "tail":
            writer.writeUnsigned(1);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one CallMode. */
export function decodeCallMode(reader: BinaryReader): CallMode {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "return";
        case 1:
            return "tail";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one CallMode. */
export function toJsonCallMode(value: CallMode): Json {
    return value;
}

/** Return one CallMode from one JSON value. */
export function fromJsonCallMode(value: Json): CallMode {
    const variant = jsonString(value);

    switch (variant) {
        case "return":
            return "return";
        case "tail":
            return "tail";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** Function call operation at one program point. */
export type CallSite = {
    /** The program point that performs the call. */
    readonly point: ProgramPoint;
    /** The program point entered after normal completion when the call returns. */
    readonly resume?: ProgramPoint;
    /** The program point entered during panic unwinding when present. */
    readonly unwind?: ProgramPoint;
    /** Whether the call returns to the caller frame. */
    readonly mode: CallMode;
    /** The dispatch mechanism used by the call. */
    readonly dispatch: CallDispatch;
    /** The receiver storage space for virtual and dynamic calls. */
    readonly space?: Space;
    /** The resolved callee for direct calls. */
    readonly target?: FunctionId;
    /** The type that defines the virtual or dynamic dispatch slot. */
    readonly dispatchType?: TypeId;
    /** The callable signature. */
    readonly signature: SignatureId;
    /** The dispatch slot for virtual and dynamic calls. */
    readonly slot?: number;
};

export const CallSite = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: CallSite): void {
        encodeCallSite(writer, value);
    },

    /** Decode one CallSite. */
    decode(reader: BinaryReader): CallSite {
        return decodeCallSite(reader);
    },

    /** Return this value as JSON. */
    toJson(value: CallSite): Json {
        return toJsonCallSite(value);
    },

    /** Return one CallSite from one JSON value. */
    fromJson(value: Json): CallSite {
        return fromJsonCallSite(value);
    },
};

/** Encode one CallSite. */
export function encodeCallSite(writer: BinaryWriter, value: CallSite): void {
    encodeProgramPoint(writer, value.point);
    writer.writeOption(value.resume, (value1) => {
        encodeProgramPoint(writer, value1);
    });
    writer.writeOption(value.unwind, (value2) => {
        encodeProgramPoint(writer, value2);
    });
    encodeCallMode(writer, value.mode);
    encodeCallDispatch(writer, value.dispatch);
    writer.writeOption(value.space, (value5) => {
        encodeSpace(writer, value5);
    });
    writer.writeOption(value.target, (value6) => {
        encodeFunctionId(writer, value6);
    });
    writer.writeOption(value.dispatchType, (value7) => {
        encodeTypeId(writer, value7);
    });
    encodeSignatureId(writer, value.signature);
    writer.writeOption(value.slot, (value9) => {
        writer.writeUnsigned(value9);
    });
}

/** Decode one CallSite. */
export function decodeCallSite(reader: BinaryReader): CallSite {
    const point = decodeProgramPoint(reader);
    const resume = reader.readOption(() => decodeProgramPoint(reader));
    const unwind = reader.readOption(() => decodeProgramPoint(reader));
    const mode = decodeCallMode(reader);
    const dispatch = decodeCallDispatch(reader);
    const space = reader.readOption(() => decodeSpace(reader));
    const target = reader.readOption(() => decodeFunctionId(reader));
    const dispatchType = reader.readOption(() => decodeTypeId(reader));
    const signature = decodeSignatureId(reader);
    const slot = reader.readOption(() => reader.readNumber());

    return {
        point,
        ...(resume === undefined ? {} : { resume }),
        ...(unwind === undefined ? {} : { unwind }),
        mode,
        dispatch,
        ...(space === undefined ? {} : { space }),
        ...(target === undefined ? {} : { target }),
        ...(dispatchType === undefined ? {} : { dispatchType }),
        signature,
        ...(slot === undefined ? {} : { slot }),
    };
}

/** Return one JSON value for one CallSite. */
export function toJsonCallSite(value: CallSite): Json {
    return {
        point: toJsonProgramPoint(value.point),
        ...(value.resume === undefined ? {} : { resume: toJsonProgramPoint(value.resume) }),
        ...(value.unwind === undefined ? {} : { unwind: toJsonProgramPoint(value.unwind) }),
        mode: toJsonCallMode(value.mode),
        dispatch: toJsonCallDispatch(value.dispatch),
        ...(value.space === undefined ? {} : { space: toJsonSpace(value.space) }),
        ...(value.target === undefined ? {} : { target: toJsonFunctionId(value.target) }),
        ...(value.dispatchType === undefined ? {} : { dispatchType: toJsonTypeId(value.dispatchType) }),
        signature: toJsonSignatureId(value.signature),
        ...(value.slot === undefined ? {} : { slot: value.slot }),
    };
}

/** Return one CallSite from one JSON value. */
export function fromJsonCallSite(value: Json): CallSite {
    const object = jsonObject(value);

    return {
        point: fromJsonProgramPoint(jsonField(object, "point")),
        resume: jsonOptional(object, "resume", (value) => fromJsonProgramPoint(value)),
        unwind: jsonOptional(object, "unwind", (value) => fromJsonProgramPoint(value)),
        mode: fromJsonCallMode(jsonField(object, "mode")),
        dispatch: fromJsonCallDispatch(jsonField(object, "dispatch")),
        space: jsonOptional(object, "space", (value) => fromJsonSpace(value)),
        target: jsonOptional(object, "target", (value) => fromJsonFunctionId(value)),
        dispatchType: jsonOptional(object, "dispatchType", (value) => fromJsonTypeId(value)),
        signature: fromJsonSignatureId(jsonField(object, "signature")),
        slot: jsonOptional(object, "slot", (value) => jsonInteger(value)),
    };
}

/** Control-flow edge between program points. */
export type EdgeSite = {
    /** The program point producing the transfer. */
    readonly source: ProgramPoint;
    /** The program point entered after the transfer. */
    readonly target: ProgramPoint;
};

export const EdgeSite = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: EdgeSite): void {
        encodeEdgeSite(writer, value);
    },

    /** Decode one EdgeSite. */
    decode(reader: BinaryReader): EdgeSite {
        return decodeEdgeSite(reader);
    },

    /** Return this value as JSON. */
    toJson(value: EdgeSite): Json {
        return toJsonEdgeSite(value);
    },

    /** Return one EdgeSite from one JSON value. */
    fromJson(value: Json): EdgeSite {
        return fromJsonEdgeSite(value);
    },
};

/** Encode one EdgeSite. */
export function encodeEdgeSite(writer: BinaryWriter, value: EdgeSite): void {
    encodeProgramPoint(writer, value.source);
    encodeProgramPoint(writer, value.target);
}

/** Decode one EdgeSite. */
export function decodeEdgeSite(reader: BinaryReader): EdgeSite {
    const source = decodeProgramPoint(reader);
    const target = decodeProgramPoint(reader);

    return {
        source,
        target,
    };
}

/** Return one JSON value for one EdgeSite. */
export function toJsonEdgeSite(value: EdgeSite): Json {
    return {
        source: toJsonProgramPoint(value.source),
        target: toJsonProgramPoint(value.target),
    };
}

/** Return one EdgeSite from one JSON value. */
export function fromJsonEdgeSite(value: Json): EdgeSite {
    const object = jsonObject(value);

    return {
        source: fromJsonProgramPoint(jsonField(object, "source")),
        target: fromJsonProgramPoint(jsonField(object, "target")),
    };
}

/** Addressable memory operation at one program point. */
export type MemorySite = {
    /** The program point that performs the access. */
    readonly point: ProgramPoint;
    /** The memory operation performed at the site. */
    readonly access: MemoryAccess;
    /** The addressed relative storage, or none for an absolute pointer. */
    readonly storage?: Storage;
    /** The loaded or stored value type. */
    readonly valueType: TypeId;
};

export const MemorySite = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: MemorySite): void {
        encodeMemorySite(writer, value);
    },

    /** Decode one MemorySite. */
    decode(reader: BinaryReader): MemorySite {
        return decodeMemorySite(reader);
    },

    /** Return this value as JSON. */
    toJson(value: MemorySite): Json {
        return toJsonMemorySite(value);
    },

    /** Return one MemorySite from one JSON value. */
    fromJson(value: Json): MemorySite {
        return fromJsonMemorySite(value);
    },
};

/** Encode one MemorySite. */
export function encodeMemorySite(writer: BinaryWriter, value: MemorySite): void {
    encodeProgramPoint(writer, value.point);
    encodeMemoryAccess(writer, value.access);
    writer.writeOption(value.storage, (value2) => {
        encodeStorage(writer, value2);
    });
    encodeTypeId(writer, value.valueType);
}

/** Decode one MemorySite. */
export function decodeMemorySite(reader: BinaryReader): MemorySite {
    const point = decodeProgramPoint(reader);
    const access = decodeMemoryAccess(reader);
    const storage = reader.readOption(() => decodeStorage(reader));
    const valueType = decodeTypeId(reader);

    return {
        point,
        access,
        ...(storage === undefined ? {} : { storage }),
        valueType,
    };
}

/** Return one JSON value for one MemorySite. */
export function toJsonMemorySite(value: MemorySite): Json {
    return {
        point: toJsonProgramPoint(value.point),
        access: toJsonMemoryAccess(value.access),
        ...(value.storage === undefined ? {} : { storage: toJsonStorage(value.storage) }),
        valueType: toJsonTypeId(value.valueType),
    };
}

/** Return one MemorySite from one JSON value. */
export function fromJsonMemorySite(value: Json): MemorySite {
    const object = jsonObject(value);

    return {
        point: fromJsonProgramPoint(jsonField(object, "point")),
        access: fromJsonMemoryAccess(jsonField(object, "access")),
        storage: jsonOptional(object, "storage", (value) => fromJsonStorage(value)),
        valueType: fromJsonTypeId(jsonField(object, "valueType")),
    };
}
