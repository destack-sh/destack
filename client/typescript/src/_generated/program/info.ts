/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, jsonArray, jsonBigint, jsonBool, jsonField, jsonInteger, jsonObject, jsonOptional, jsonString } from "../../protocol/serde.js";
import type { StringId } from "../core/string.js";
import type { Lifetime } from "../mir/tree/lifetime.js";
import type { Access } from "../mir/tree/type.js";
import type { FloatType } from "../mir/tree/type.js";
import type { Space } from "../mir/tree/type.js";
import type { EntryPoint } from "./entry.js";
import type { FunctionId } from "./function.js";
import type { LayoutId } from "./layout.js";
import type { TypeId } from "./type.js";
import type { ModuleId } from "../source/file/model/module.js";
import { decodeStringId, encodeStringId, fromJsonStringId, toJsonStringId } from "../core/string.js";
import { decodeLifetime, encodeLifetime, fromJsonLifetime, toJsonLifetime } from "../mir/tree/lifetime.js";
import { decodeAccess, encodeAccess, fromJsonAccess, toJsonAccess } from "../mir/tree/type.js";
import { decodeFloatType, encodeFloatType, fromJsonFloatType, toJsonFloatType } from "../mir/tree/type.js";
import { decodeSpace, encodeSpace, fromJsonSpace, toJsonSpace } from "../mir/tree/type.js";
import { decodeEntryPoint, encodeEntryPoint, fromJsonEntryPoint, toJsonEntryPoint } from "./entry.js";
import { decodeFunctionId, encodeFunctionId, fromJsonFunctionId, toJsonFunctionId } from "./function.js";
import { decodeLayoutId, encodeLayoutId, fromJsonLayoutId, toJsonLayoutId } from "./layout.js";
import { decodeTypeId, encodeTypeId, fromJsonTypeId, toJsonTypeId } from "./type.js";
import { decodeModuleId, encodeModuleId, fromJsonModuleId, toJsonModuleId } from "../source/file/model/module.js";

/** Cold reflected view of one executable program. */
export type ProgramInfo = {
    /** Reflected modules in this program. */
    readonly modules: ReadonlyArray<ModuleInfo>;
    /** Reflected types keyed by program type id. */
    readonly types: ReadonlyArray<TypeInfo | undefined>;
    /** Reflected functions keyed by program function id. */
    readonly functions: ReadonlyArray<FunctionInfo | undefined>;
    /** Reflected frame layouts keyed by frame layout id. */
    readonly frames: ReadonlyArray<FrameInfo>;
    /** Reflected globals keyed by program global id. */
    readonly globals: ReadonlyArray<GlobalInfo>;
    /** Reflected entry points. */
    readonly entries: ReadonlyArray<EntryInfo>;
};

export const ProgramInfo = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ProgramInfo): void {
        encodeProgramInfo(writer, value);
    },

    /** Decode one ProgramInfo. */
    decode(reader: BinaryReader): ProgramInfo {
        return decodeProgramInfo(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ProgramInfo): Json {
        return toJsonProgramInfo(value);
    },

    /** Return one ProgramInfo from one JSON value. */
    fromJson(value: Json): ProgramInfo {
        return fromJsonProgramInfo(value);
    },
};

/** Encode one ProgramInfo. */
export function encodeProgramInfo(writer: BinaryWriter, value: ProgramInfo): void {
    writer.writeUnsigned(value.modules.length);
    for (const item0 of value.modules) {
        encodeModuleInfo(writer, item0);
    }
    writer.writeUnsigned(value.types.length);
    for (const item1 of value.types) {
        writer.writeOption(item1, (value2) => {
            encodeTypeInfo(writer, value2);
        });
    }
    writer.writeUnsigned(value.functions.length);
    for (const item2 of value.functions) {
        writer.writeOption(item2, (value3) => {
            encodeFunctionInfo(writer, value3);
        });
    }
    writer.writeUnsigned(value.frames.length);
    for (const item3 of value.frames) {
        encodeFrameInfo(writer, item3);
    }
    writer.writeUnsigned(value.globals.length);
    for (const item4 of value.globals) {
        encodeGlobalInfo(writer, item4);
    }
    writer.writeUnsigned(value.entries.length);
    for (const item5 of value.entries) {
        encodeEntryInfo(writer, item5);
    }
}

/** Decode one ProgramInfo. */
export function decodeProgramInfo(reader: BinaryReader): ProgramInfo {
    const modules = (() => { const length0 = reader.readNumber(); const items0: Array<ModuleInfo> = []; for (let index = 0; index < length0; index += 1) { items0.push(decodeModuleInfo(reader)); } return items0; })();
    const types = (() => { const length1 = reader.readNumber(); const items1: Array<TypeInfo | undefined> = []; for (let index = 0; index < length1; index += 1) { items1.push(reader.readOption(() => decodeTypeInfo(reader))); } return items1; })();
    const functions = (() => { const length2 = reader.readNumber(); const items2: Array<FunctionInfo | undefined> = []; for (let index = 0; index < length2; index += 1) { items2.push(reader.readOption(() => decodeFunctionInfo(reader))); } return items2; })();
    const frames = (() => { const length3 = reader.readNumber(); const items3: Array<FrameInfo> = []; for (let index = 0; index < length3; index += 1) { items3.push(decodeFrameInfo(reader)); } return items3; })();
    const globals = (() => { const length4 = reader.readNumber(); const items4: Array<GlobalInfo> = []; for (let index = 0; index < length4; index += 1) { items4.push(decodeGlobalInfo(reader)); } return items4; })();
    const entries = (() => { const length5 = reader.readNumber(); const items5: Array<EntryInfo> = []; for (let index = 0; index < length5; index += 1) { items5.push(decodeEntryInfo(reader)); } return items5; })();

    return {
        modules,
        types,
        functions,
        frames,
        globals,
        entries,
    };
}

/** Return one JSON value for one ProgramInfo. */
export function toJsonProgramInfo(value: ProgramInfo): Json {
    return {
        modules: value.modules.map((item0) => toJsonModuleInfo(item0)),
        types: value.types.map((item0) => item0 === undefined ? null : toJsonTypeInfo(item0)),
        functions: value.functions.map((item0) => item0 === undefined ? null : toJsonFunctionInfo(item0)),
        frames: value.frames.map((item0) => toJsonFrameInfo(item0)),
        globals: value.globals.map((item0) => toJsonGlobalInfo(item0)),
        entries: value.entries.map((item0) => toJsonEntryInfo(item0)),
    };
}

/** Return one ProgramInfo from one JSON value. */
export function fromJsonProgramInfo(value: Json): ProgramInfo {
    const object = jsonObject(value);

    return {
        modules: jsonArray(jsonField(object, "modules")).map((item0) => fromJsonModuleInfo(item0)),
        types: jsonArray(jsonField(object, "types")).map((item0) => item0 === null ? undefined : fromJsonTypeInfo(item0)),
        functions: jsonArray(jsonField(object, "functions")).map((item0) => item0 === null ? undefined : fromJsonFunctionInfo(item0)),
        frames: jsonArray(jsonField(object, "frames")).map((item0) => fromJsonFrameInfo(item0)),
        globals: jsonArray(jsonField(object, "globals")).map((item0) => fromJsonGlobalInfo(item0)),
        entries: jsonArray(jsonField(object, "entries")).map((item0) => fromJsonEntryInfo(item0)),
    };
}

/** Reflected module in one executable program. */
export type ModuleInfo = {
    /** Source module id when one exists. */
    readonly id?: ModuleId;
    /** Module display name when one exists. */
    readonly name?: StringId;
};

export const ModuleInfo = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ModuleInfo): void {
        encodeModuleInfo(writer, value);
    },

    /** Decode one ModuleInfo. */
    decode(reader: BinaryReader): ModuleInfo {
        return decodeModuleInfo(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ModuleInfo): Json {
        return toJsonModuleInfo(value);
    },

    /** Return one ModuleInfo from one JSON value. */
    fromJson(value: Json): ModuleInfo {
        return fromJsonModuleInfo(value);
    },
};

/** Encode one ModuleInfo. */
export function encodeModuleInfo(writer: BinaryWriter, value: ModuleInfo): void {
    writer.writeOption(value.id, (value0) => {
        encodeModuleId(writer, value0);
    });
    writer.writeOption(value.name, (value1) => {
        encodeStringId(writer, value1);
    });
}

/** Decode one ModuleInfo. */
export function decodeModuleInfo(reader: BinaryReader): ModuleInfo {
    const id = reader.readOption(() => decodeModuleId(reader));
    const name = reader.readOption(() => decodeStringId(reader));

    return {
        ...(id === undefined ? {} : { id }),
        ...(name === undefined ? {} : { name }),
    };
}

/** Return one JSON value for one ModuleInfo. */
export function toJsonModuleInfo(value: ModuleInfo): Json {
    return {
        ...(value.id === undefined ? {} : { id: toJsonModuleId(value.id) }),
        ...(value.name === undefined ? {} : { name: toJsonStringId(value.name) }),
    };
}

/** Return one ModuleInfo from one JSON value. */
export function fromJsonModuleInfo(value: Json): ModuleInfo {
    const object = jsonObject(value);

    return {
        id: jsonOptional(object, "id", (value) => fromJsonModuleId(value)),
        name: jsonOptional(object, "name", (value) => fromJsonStringId(value)),
    };
}

/** Reflected type in one executable program. */
export type TypeInfo = {
    /** Type display name when one exists. */
    readonly name?: StringId;
    /** Source module containing the type when one exists. */
    readonly module?: ModuleId;
    /** Transparent executable representation. */
    readonly repr: TypeId;
    /** Concrete executable layout when one exists. */
    readonly layout?: LayoutId;
    /** Normalized reflected program type. */
    readonly ty: ProgramType;
};

export const TypeInfo = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: TypeInfo): void {
        encodeTypeInfo(writer, value);
    },

    /** Decode one TypeInfo. */
    decode(reader: BinaryReader): TypeInfo {
        return decodeTypeInfo(reader);
    },

    /** Return this value as JSON. */
    toJson(value: TypeInfo): Json {
        return toJsonTypeInfo(value);
    },

    /** Return one TypeInfo from one JSON value. */
    fromJson(value: Json): TypeInfo {
        return fromJsonTypeInfo(value);
    },
};

/** Encode one TypeInfo. */
export function encodeTypeInfo(writer: BinaryWriter, value: TypeInfo): void {
    writer.writeOption(value.name, (value0) => {
        encodeStringId(writer, value0);
    });
    writer.writeOption(value.module, (value1) => {
        encodeModuleId(writer, value1);
    });
    encodeTypeId(writer, value.repr);
    writer.writeOption(value.layout, (value3) => {
        encodeLayoutId(writer, value3);
    });
    encodeProgramType(writer, value.ty);
}

/** Decode one TypeInfo. */
export function decodeTypeInfo(reader: BinaryReader): TypeInfo {
    const name = reader.readOption(() => decodeStringId(reader));
    const module_ = reader.readOption(() => decodeModuleId(reader));
    const repr = decodeTypeId(reader);
    const layout = reader.readOption(() => decodeLayoutId(reader));
    const ty = decodeProgramType(reader);

    return {
        ...(name === undefined ? {} : { name }),
        ...(module_ === undefined ? {} : { module: module_ }),
        repr,
        ...(layout === undefined ? {} : { layout }),
        ty,
    };
}

/** Return one JSON value for one TypeInfo. */
export function toJsonTypeInfo(value: TypeInfo): Json {
    return {
        ...(value.name === undefined ? {} : { name: toJsonStringId(value.name) }),
        ...(value.module === undefined ? {} : { module: toJsonModuleId(value.module) }),
        repr: toJsonTypeId(value.repr),
        ...(value.layout === undefined ? {} : { layout: toJsonLayoutId(value.layout) }),
        ty: toJsonProgramType(value.ty),
    };
}

/** Return one TypeInfo from one JSON value. */
export function fromJsonTypeInfo(value: Json): TypeInfo {
    const object = jsonObject(value);

    return {
        name: jsonOptional(object, "name", (value) => fromJsonStringId(value)),
        module: jsonOptional(object, "module", (value) => fromJsonModuleId(value)),
        repr: fromJsonTypeId(jsonField(object, "repr")),
        layout: jsonOptional(object, "layout", (value) => fromJsonLayoutId(value)),
        ty: fromJsonProgramType(jsonField(object, "ty")),
    };
}

/** Normalized program type visible through program reflection. */
export type ProgramType =
    /** Never type. */
    | {
          readonly kind: "never";
      }
    /** Unknown type. */
    | {
          readonly kind: "unknown";
      }
    /** Void type. */
    | {
          readonly kind: "void";
      }
    /** Null singleton type. */
    | {
          readonly kind: "null";
      }
    /** Undefined singleton type. */
    | {
          readonly kind: "undefined";
      }
    /** Object constraint type. */
    | {
          readonly kind: "object";
      }
    /** Primitive type. */
    | {
          readonly kind: "primitive";
          readonly primitive: PrimitiveInfo;
      }
    /** Scalar literal type. */
    | {
          readonly kind: "literal";
          readonly literal: LiteralInfo;
      }
    /** Normalized memory singleton type. */
    | {
          readonly kind: "memory";
          readonly memory: MemoryInfo;
      }
    /** Declaration reference with applied type arguments. */
    | {
          readonly kind: "reference";
          readonly reference: GenericInfo;
      }
    /** Member type selected from an owner type. */
    | {
          readonly kind: "member";
          readonly member: MemberInfo;
      }
    /** Canonical ownership or access form. */
    | {
          readonly kind: "form";
          readonly form: FormInfo;
      }
    /** Explicit erased dynamic value. */
    | {
          readonly kind: "dynamic";
          readonly dynamic: DynamicInfo;
      }
    /** Homogeneous dynamic-length array. */
    | {
          readonly kind: "array";
          readonly array: ArrayInfo;
      }
    /** Fixed-length array. */
    | {
          readonly kind: "fixedArray";
          readonly fixed_array: FixedArrayInfo;
      }
    /** Compact scalar interval. */
    | {
          readonly kind: "range";
          readonly range: RangeInfo;
      }
    /** Runtime-length homogeneous view. */
    | {
          readonly kind: "slice";
          readonly slice: SliceInfo;
      }
    /** Tuple value. */
    | {
          readonly kind: "tuple";
          readonly tuple: TupleInfo;
      }
    /** Structural object shape. */
    | {
          readonly kind: "shape";
          readonly shape: ShapeInfo;
      }
    /** Callable signature. */
    | {
          readonly kind: "functionSignature";
          readonly function_signature: FunctionSignatureInfo;
      }
    /** Fat callable value with captured environment. */
    | {
          readonly kind: "function";
          readonly function: FunctionTypeInfo;
      }
    /** Thin callable value. */
    | {
          readonly kind: "functionPointer";
          readonly function_pointer: FunctionPointerInfo;
      }
    /** Union type. */
    | {
          readonly kind: "union";
          readonly union: UnionInfo;
      }
    /** Intersection type. */
    | {
          readonly kind: "intersection";
          readonly intersection: IntersectionInfo;
      }
    /** Transparent nominal type. */
    | {
          readonly kind: "newtype";
          readonly newtype: NewtypeInfo;
      }
    /** Struct declaration type. */
    | {
          readonly kind: "struct";
          readonly struct: StructInfo;
      }
    /** Class declaration type. */
    | {
          readonly kind: "class";
          readonly class: ClassInfo;
      }
    /** Interface declaration type. */
    | {
          readonly kind: "interface";
          readonly interface: InterfaceInfo;
      }
    /** Variant declaration type. */
    | {
          readonly kind: "variant";
          readonly variant: VariantInfo;
      }
;

export const ProgramType = {
    /** Never type. */
    "never"(): ProgramType {
        return { kind: "never" };
    },

    /** Unknown type. */
    "unknown"(): ProgramType {
        return { kind: "unknown" };
    },

    /** Void type. */
    "void"(): ProgramType {
        return { kind: "void" };
    },

    /** Null singleton type. */
    "null"(): ProgramType {
        return { kind: "null" };
    },

    /** Undefined singleton type. */
    "undefined"(): ProgramType {
        return { kind: "undefined" };
    },

    /** Object constraint type. */
    "object"(): ProgramType {
        return { kind: "object" };
    },

    /** Primitive type. */
    primitive(primitive: PrimitiveInfo): ProgramType {
        return { kind: "primitive", primitive };
    },

    /** Scalar literal type. */
    literal(literal: LiteralInfo): ProgramType {
        return { kind: "literal", literal };
    },

    /** Normalized memory singleton type. */
    memory(memory: MemoryInfo): ProgramType {
        return { kind: "memory", memory };
    },

    /** Declaration reference with applied type arguments. */
    reference(reference: GenericInfo): ProgramType {
        return { kind: "reference", reference };
    },

    /** Member type selected from an owner type. */
    member(member: MemberInfo): ProgramType {
        return { kind: "member", member };
    },

    /** Canonical ownership or access form. */
    form(form: FormInfo): ProgramType {
        return { kind: "form", form };
    },

    /** Explicit erased dynamic value. */
    dynamic(dynamic: DynamicInfo): ProgramType {
        return { kind: "dynamic", dynamic };
    },

    /** Homogeneous dynamic-length array. */
    array(array: ArrayInfo): ProgramType {
        return { kind: "array", array };
    },

    /** Fixed-length array. */
    fixedArray(fixed_array: FixedArrayInfo): ProgramType {
        return { kind: "fixedArray", fixed_array };
    },

    /** Compact scalar interval. */
    range(range: RangeInfo): ProgramType {
        return { kind: "range", range };
    },

    /** Runtime-length homogeneous view. */
    slice(slice: SliceInfo): ProgramType {
        return { kind: "slice", slice };
    },

    /** Tuple value. */
    tuple(tuple: TupleInfo): ProgramType {
        return { kind: "tuple", tuple };
    },

    /** Structural object shape. */
    shape(shape: ShapeInfo): ProgramType {
        return { kind: "shape", shape };
    },

    /** Callable signature. */
    functionSignature(function_signature: FunctionSignatureInfo): ProgramType {
        return { kind: "functionSignature", function_signature };
    },

    /** Fat callable value with captured environment. */
    "function"(function_: FunctionTypeInfo): ProgramType {
        return { kind: "function", function: function_ };
    },

    /** Thin callable value. */
    functionPointer(function_pointer: FunctionPointerInfo): ProgramType {
        return { kind: "functionPointer", function_pointer };
    },

    /** Union type. */
    union(union: UnionInfo): ProgramType {
        return { kind: "union", union };
    },

    /** Intersection type. */
    intersection(intersection: IntersectionInfo): ProgramType {
        return { kind: "intersection", intersection };
    },

    /** Transparent nominal type. */
    newtype(newtype: NewtypeInfo): ProgramType {
        return { kind: "newtype", newtype };
    },

    /** Struct declaration type. */
    struct(struct: StructInfo): ProgramType {
        return { kind: "struct", struct };
    },

    /** Class declaration type. */
    "class"(class_: ClassInfo): ProgramType {
        return { kind: "class", class: class_ };
    },

    /** Interface declaration type. */
    "interface"(interface_: InterfaceInfo): ProgramType {
        return { kind: "interface", interface: interface_ };
    },

    /** Variant declaration type. */
    variant(variant: VariantInfo): ProgramType {
        return { kind: "variant", variant };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: ProgramType): void {
        encodeProgramType(writer, value);
    },

    /** Decode one ProgramType. */
    decode(reader: BinaryReader): ProgramType {
        return decodeProgramType(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ProgramType): Json {
        return toJsonProgramType(value);
    },

    /** Return one ProgramType from one JSON value. */
    fromJson(value: Json): ProgramType {
        return fromJsonProgramType(value);
    },
};

/** Encode one ProgramType. */
export function encodeProgramType(writer: BinaryWriter, value: ProgramType): void {
    switch (value.kind) {
        case "never":
            writer.writeUnsigned(0);
            return;
        case "unknown":
            writer.writeUnsigned(1);
            return;
        case "void":
            writer.writeUnsigned(2);
            return;
        case "null":
            writer.writeUnsigned(3);
            return;
        case "undefined":
            writer.writeUnsigned(4);
            return;
        case "object":
            writer.writeUnsigned(5);
            return;
        case "primitive":
            writer.writeUnsigned(6);
            encodePrimitiveInfo(writer, value.primitive);
            return;
        case "literal":
            writer.writeUnsigned(7);
            encodeLiteralInfo(writer, value.literal);
            return;
        case "memory":
            writer.writeUnsigned(8);
            encodeMemoryInfo(writer, value.memory);
            return;
        case "reference":
            writer.writeUnsigned(9);
            encodeGenericInfo(writer, value.reference);
            return;
        case "member":
            writer.writeUnsigned(10);
            encodeMemberInfo(writer, value.member);
            return;
        case "form":
            writer.writeUnsigned(11);
            encodeFormInfo(writer, value.form);
            return;
        case "dynamic":
            writer.writeUnsigned(12);
            encodeDynamicInfo(writer, value.dynamic);
            return;
        case "array":
            writer.writeUnsigned(13);
            encodeArrayInfo(writer, value.array);
            return;
        case "fixedArray":
            writer.writeUnsigned(14);
            encodeFixedArrayInfo(writer, value.fixed_array);
            return;
        case "range":
            writer.writeUnsigned(15);
            encodeRangeInfo(writer, value.range);
            return;
        case "slice":
            writer.writeUnsigned(16);
            encodeSliceInfo(writer, value.slice);
            return;
        case "tuple":
            writer.writeUnsigned(17);
            encodeTupleInfo(writer, value.tuple);
            return;
        case "shape":
            writer.writeUnsigned(18);
            encodeShapeInfo(writer, value.shape);
            return;
        case "functionSignature":
            writer.writeUnsigned(19);
            encodeFunctionSignatureInfo(writer, value.function_signature);
            return;
        case "function":
            writer.writeUnsigned(20);
            encodeFunctionTypeInfo(writer, value.function);
            return;
        case "functionPointer":
            writer.writeUnsigned(21);
            encodeFunctionPointerInfo(writer, value.function_pointer);
            return;
        case "union":
            writer.writeUnsigned(22);
            encodeUnionInfo(writer, value.union);
            return;
        case "intersection":
            writer.writeUnsigned(23);
            encodeIntersectionInfo(writer, value.intersection);
            return;
        case "newtype":
            writer.writeUnsigned(24);
            encodeNewtypeInfo(writer, value.newtype);
            return;
        case "struct":
            writer.writeUnsigned(25);
            encodeStructInfo(writer, value.struct);
            return;
        case "class":
            writer.writeUnsigned(26);
            encodeClassInfo(writer, value.class);
            return;
        case "interface":
            writer.writeUnsigned(27);
            encodeInterfaceInfo(writer, value.interface);
            return;
        case "variant":
            writer.writeUnsigned(28);
            encodeVariantInfo(writer, value.variant);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one ProgramType. */
export function decodeProgramType(reader: BinaryReader): ProgramType {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            return { kind: "never" };
        }
        case 1: {
            return { kind: "unknown" };
        }
        case 2: {
            return { kind: "void" };
        }
        case 3: {
            return { kind: "null" };
        }
        case 4: {
            return { kind: "undefined" };
        }
        case 5: {
            return { kind: "object" };
        }
        case 6: {
            const primitive = decodePrimitiveInfo(reader);

            return { kind: "primitive", primitive };
        }
        case 7: {
            const literal = decodeLiteralInfo(reader);

            return { kind: "literal", literal };
        }
        case 8: {
            const memory = decodeMemoryInfo(reader);

            return { kind: "memory", memory };
        }
        case 9: {
            const reference = decodeGenericInfo(reader);

            return { kind: "reference", reference };
        }
        case 10: {
            const member = decodeMemberInfo(reader);

            return { kind: "member", member };
        }
        case 11: {
            const form = decodeFormInfo(reader);

            return { kind: "form", form };
        }
        case 12: {
            const dynamic = decodeDynamicInfo(reader);

            return { kind: "dynamic", dynamic };
        }
        case 13: {
            const array = decodeArrayInfo(reader);

            return { kind: "array", array };
        }
        case 14: {
            const fixed_array = decodeFixedArrayInfo(reader);

            return { kind: "fixedArray", fixed_array };
        }
        case 15: {
            const range = decodeRangeInfo(reader);

            return { kind: "range", range };
        }
        case 16: {
            const slice = decodeSliceInfo(reader);

            return { kind: "slice", slice };
        }
        case 17: {
            const tuple = decodeTupleInfo(reader);

            return { kind: "tuple", tuple };
        }
        case 18: {
            const shape = decodeShapeInfo(reader);

            return { kind: "shape", shape };
        }
        case 19: {
            const function_signature = decodeFunctionSignatureInfo(reader);

            return { kind: "functionSignature", function_signature };
        }
        case 20: {
            const function_ = decodeFunctionTypeInfo(reader);

            return { kind: "function", function: function_ };
        }
        case 21: {
            const function_pointer = decodeFunctionPointerInfo(reader);

            return { kind: "functionPointer", function_pointer };
        }
        case 22: {
            const union = decodeUnionInfo(reader);

            return { kind: "union", union };
        }
        case 23: {
            const intersection = decodeIntersectionInfo(reader);

            return { kind: "intersection", intersection };
        }
        case 24: {
            const newtype = decodeNewtypeInfo(reader);

            return { kind: "newtype", newtype };
        }
        case 25: {
            const struct = decodeStructInfo(reader);

            return { kind: "struct", struct };
        }
        case 26: {
            const class_ = decodeClassInfo(reader);

            return { kind: "class", class: class_ };
        }
        case 27: {
            const interface_ = decodeInterfaceInfo(reader);

            return { kind: "interface", interface: interface_ };
        }
        case 28: {
            const variant = decodeVariantInfo(reader);

            return { kind: "variant", variant };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one ProgramType. */
export function toJsonProgramType(value: ProgramType): Json {
    switch (value.kind) {
        case "never":
            return {
                kind: "never",
            };
        case "unknown":
            return {
                kind: "unknown",
            };
        case "void":
            return {
                kind: "void",
            };
        case "null":
            return {
                kind: "null",
            };
        case "undefined":
            return {
                kind: "undefined",
            };
        case "object":
            return {
                kind: "object",
            };
        case "primitive":
            return {
                kind: "primitive",
                primitive: toJsonPrimitiveInfo(value.primitive),
            };
        case "literal":
            return {
                kind: "literal",
                literal: toJsonLiteralInfo(value.literal),
            };
        case "memory":
            return {
                kind: "memory",
                memory: toJsonMemoryInfo(value.memory),
            };
        case "reference":
            return {
                kind: "reference",
                reference: toJsonGenericInfo(value.reference),
            };
        case "member":
            return {
                kind: "member",
                member: toJsonMemberInfo(value.member),
            };
        case "form":
            return {
                kind: "form",
                form: toJsonFormInfo(value.form),
            };
        case "dynamic":
            return {
                kind: "dynamic",
                dynamic: toJsonDynamicInfo(value.dynamic),
            };
        case "array":
            return {
                kind: "array",
                array: toJsonArrayInfo(value.array),
            };
        case "fixedArray":
            return {
                kind: "fixedArray",
                fixed_array: toJsonFixedArrayInfo(value.fixed_array),
            };
        case "range":
            return {
                kind: "range",
                range: toJsonRangeInfo(value.range),
            };
        case "slice":
            return {
                kind: "slice",
                slice: toJsonSliceInfo(value.slice),
            };
        case "tuple":
            return {
                kind: "tuple",
                tuple: toJsonTupleInfo(value.tuple),
            };
        case "shape":
            return {
                kind: "shape",
                shape: toJsonShapeInfo(value.shape),
            };
        case "functionSignature":
            return {
                kind: "functionSignature",
                function_signature: toJsonFunctionSignatureInfo(value.function_signature),
            };
        case "function":
            return {
                kind: "function",
                function: toJsonFunctionTypeInfo(value.function),
            };
        case "functionPointer":
            return {
                kind: "functionPointer",
                function_pointer: toJsonFunctionPointerInfo(value.function_pointer),
            };
        case "union":
            return {
                kind: "union",
                union: toJsonUnionInfo(value.union),
            };
        case "intersection":
            return {
                kind: "intersection",
                intersection: toJsonIntersectionInfo(value.intersection),
            };
        case "newtype":
            return {
                kind: "newtype",
                newtype: toJsonNewtypeInfo(value.newtype),
            };
        case "struct":
            return {
                kind: "struct",
                struct: toJsonStructInfo(value.struct),
            };
        case "class":
            return {
                kind: "class",
                class: toJsonClassInfo(value.class),
            };
        case "interface":
            return {
                kind: "interface",
                interface: toJsonInterfaceInfo(value.interface),
            };
        case "variant":
            return {
                kind: "variant",
                variant: toJsonVariantInfo(value.variant),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one ProgramType from one JSON value. */
export function fromJsonProgramType(value: Json): ProgramType {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "never":
            return {
                kind,
            };
        case "unknown":
            return {
                kind,
            };
        case "void":
            return {
                kind,
            };
        case "null":
            return {
                kind,
            };
        case "undefined":
            return {
                kind,
            };
        case "object":
            return {
                kind,
            };
        case "primitive":
            return {
                kind,
                primitive: fromJsonPrimitiveInfo(jsonField(object, "primitive")),
            };
        case "literal":
            return {
                kind,
                literal: fromJsonLiteralInfo(jsonField(object, "literal")),
            };
        case "memory":
            return {
                kind,
                memory: fromJsonMemoryInfo(jsonField(object, "memory")),
            };
        case "reference":
            return {
                kind,
                reference: fromJsonGenericInfo(jsonField(object, "reference")),
            };
        case "member":
            return {
                kind,
                member: fromJsonMemberInfo(jsonField(object, "member")),
            };
        case "form":
            return {
                kind,
                form: fromJsonFormInfo(jsonField(object, "form")),
            };
        case "dynamic":
            return {
                kind,
                dynamic: fromJsonDynamicInfo(jsonField(object, "dynamic")),
            };
        case "array":
            return {
                kind,
                array: fromJsonArrayInfo(jsonField(object, "array")),
            };
        case "fixedArray":
            return {
                kind,
                fixed_array: fromJsonFixedArrayInfo(jsonField(object, "fixed_array")),
            };
        case "range":
            return {
                kind,
                range: fromJsonRangeInfo(jsonField(object, "range")),
            };
        case "slice":
            return {
                kind,
                slice: fromJsonSliceInfo(jsonField(object, "slice")),
            };
        case "tuple":
            return {
                kind,
                tuple: fromJsonTupleInfo(jsonField(object, "tuple")),
            };
        case "shape":
            return {
                kind,
                shape: fromJsonShapeInfo(jsonField(object, "shape")),
            };
        case "functionSignature":
            return {
                kind,
                function_signature: fromJsonFunctionSignatureInfo(jsonField(object, "function_signature")),
            };
        case "function":
            return {
                kind,
                function: fromJsonFunctionTypeInfo(jsonField(object, "function")),
            };
        case "functionPointer":
            return {
                kind,
                function_pointer: fromJsonFunctionPointerInfo(jsonField(object, "function_pointer")),
            };
        case "union":
            return {
                kind,
                union: fromJsonUnionInfo(jsonField(object, "union")),
            };
        case "intersection":
            return {
                kind,
                intersection: fromJsonIntersectionInfo(jsonField(object, "intersection")),
            };
        case "newtype":
            return {
                kind,
                newtype: fromJsonNewtypeInfo(jsonField(object, "newtype")),
            };
        case "struct":
            return {
                kind,
                struct: fromJsonStructInfo(jsonField(object, "struct")),
            };
        case "class":
            return {
                kind,
                class: fromJsonClassInfo(jsonField(object, "class")),
            };
        case "interface":
            return {
                kind,
                interface: fromJsonInterfaceInfo(jsonField(object, "interface")),
            };
        case "variant":
            return {
                kind,
                variant: fromJsonVariantInfo(jsonField(object, "variant")),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** Reflected primitive program type. */
export type PrimitiveInfo =
    /** Boolean primitive. */
    | {
          readonly kind: "boolean";
      }
    /** Unicode scalar value primitive. */
    | {
          readonly kind: "character";
      }
    /** String primitive. */
    | {
          readonly kind: "string";
      }
    /** Bigint primitive. */
    | {
          readonly kind: "bigint";
      }
    /** Number primitive. */
    | {
          readonly kind: "number";
      }
    /** Integer primitive. */
    | {
          readonly kind: "int";
          /** Integer bit width. */
          readonly width: number;
          /** Whether the integer is signed. */
          readonly isSigned: boolean;
      }
    /** Floating-point primitive. */
    | {
          readonly kind: "float";
          /** Float format. */
          readonly format: FloatType;
      }
    /** Symbol primitive. */
    | {
          readonly kind: "symbol";
      }
    /** Unique symbol primitive. */
    | {
          readonly kind: "uniqueSymbol";
      }
;

export const PrimitiveInfo = {
    /** Boolean primitive. */
    "boolean"(): PrimitiveInfo {
        return { kind: "boolean" };
    },

    /** Unicode scalar value primitive. */
    character(): PrimitiveInfo {
        return { kind: "character" };
    },

    /** String primitive. */
    "string"(): PrimitiveInfo {
        return { kind: "string" };
    },

    /** Bigint primitive. */
    "bigint"(): PrimitiveInfo {
        return { kind: "bigint" };
    },

    /** Number primitive. */
    "number"(): PrimitiveInfo {
        return { kind: "number" };
    },

    /** Integer primitive. */
    int(width: number, isSigned: boolean): PrimitiveInfo {
        return { kind: "int", width, isSigned };
    },

    /** Floating-point primitive. */
    float(format: FloatType): PrimitiveInfo {
        return { kind: "float", format };
    },

    /** Symbol primitive. */
    "symbol"(): PrimitiveInfo {
        return { kind: "symbol" };
    },

    /** Unique symbol primitive. */
    uniqueSymbol(): PrimitiveInfo {
        return { kind: "uniqueSymbol" };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: PrimitiveInfo): void {
        encodePrimitiveInfo(writer, value);
    },

    /** Decode one PrimitiveInfo. */
    decode(reader: BinaryReader): PrimitiveInfo {
        return decodePrimitiveInfo(reader);
    },

    /** Return this value as JSON. */
    toJson(value: PrimitiveInfo): Json {
        return toJsonPrimitiveInfo(value);
    },

    /** Return one PrimitiveInfo from one JSON value. */
    fromJson(value: Json): PrimitiveInfo {
        return fromJsonPrimitiveInfo(value);
    },
};

/** Encode one PrimitiveInfo. */
export function encodePrimitiveInfo(writer: BinaryWriter, value: PrimitiveInfo): void {
    switch (value.kind) {
        case "boolean":
            writer.writeUnsigned(0);
            return;
        case "character":
            writer.writeUnsigned(1);
            return;
        case "string":
            writer.writeUnsigned(2);
            return;
        case "bigint":
            writer.writeUnsigned(3);
            return;
        case "number":
            writer.writeUnsigned(4);
            return;
        case "int":
            writer.writeUnsigned(5);
            writer.writeUnsigned(value.width);
            writer.writeBool(value.isSigned);
            return;
        case "float":
            writer.writeUnsigned(6);
            encodeFloatType(writer, value.format);
            return;
        case "symbol":
            writer.writeUnsigned(7);
            return;
        case "uniqueSymbol":
            writer.writeUnsigned(8);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one PrimitiveInfo. */
export function decodePrimitiveInfo(reader: BinaryReader): PrimitiveInfo {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            return { kind: "boolean" };
        }
        case 1: {
            return { kind: "character" };
        }
        case 2: {
            return { kind: "string" };
        }
        case 3: {
            return { kind: "bigint" };
        }
        case 4: {
            return { kind: "number" };
        }
        case 5: {
            const width = reader.readNumber();
            const isSigned = reader.readBool();

            return {
                kind: "int",
                width,
                isSigned,
            };
        }
        case 6: {
            const format = decodeFloatType(reader);

            return {
                kind: "float",
                format,
            };
        }
        case 7: {
            return { kind: "symbol" };
        }
        case 8: {
            return { kind: "uniqueSymbol" };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one PrimitiveInfo. */
export function toJsonPrimitiveInfo(value: PrimitiveInfo): Json {
    switch (value.kind) {
        case "boolean":
            return {
                kind: "boolean",
            };
        case "character":
            return {
                kind: "character",
            };
        case "string":
            return {
                kind: "string",
            };
        case "bigint":
            return {
                kind: "bigint",
            };
        case "number":
            return {
                kind: "number",
            };
        case "int":
            return {
                kind: "int",
                width: value.width,
                isSigned: value.isSigned,
            };
        case "float":
            return {
                kind: "float",
                format: toJsonFloatType(value.format),
            };
        case "symbol":
            return {
                kind: "symbol",
            };
        case "uniqueSymbol":
            return {
                kind: "uniqueSymbol",
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one PrimitiveInfo from one JSON value. */
export function fromJsonPrimitiveInfo(value: Json): PrimitiveInfo {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "boolean":
            return {
                kind,
            };
        case "character":
            return {
                kind,
            };
        case "string":
            return {
                kind,
            };
        case "bigint":
            return {
                kind,
            };
        case "number":
            return {
                kind,
            };
        case "int":
            return {
                kind,
                width: jsonInteger(jsonField(object, "width")),
                isSigned: jsonBool(jsonField(object, "isSigned")),
            };
        case "float":
            return {
                kind,
                format: fromJsonFloatType(jsonField(object, "format")),
            };
        case "symbol":
            return {
                kind,
            };
        case "uniqueSymbol":
            return {
                kind,
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** Reflected scalar literal program type. */
export type LiteralInfo =
    /** Boolean literal. */
    | {
          readonly kind: "boolean";
          readonly boolean: boolean;
      }
    /** Character literal. */
    | {
          readonly kind: "character";
          readonly character: string;
      }
    /** String literal. */
    | {
          readonly kind: "string";
          readonly string: StringId;
      }
    /** Integer literal. */
    | {
          readonly kind: "integer";
          readonly integer: bigint;
      }
    /** Floating-point literal bits. */
    | {
          readonly kind: "float";
          /** Float format. */
          readonly format: FloatType;
          /** Float bits. */
          readonly bits: bigint;
      }
    /** Bigint literal. */
    | {
          readonly kind: "bigint";
          readonly bigint: bigint;
      }
;

export const LiteralInfo = {
    /** Boolean literal. */
    "boolean"(boolean_: boolean): LiteralInfo {
        return { kind: "boolean", boolean: boolean_ };
    },

    /** Character literal. */
    character(character: string): LiteralInfo {
        return { kind: "character", character };
    },

    /** String literal. */
    "string"(string_: StringId): LiteralInfo {
        return { kind: "string", string: string_ };
    },

    /** Integer literal. */
    integer(integer: bigint): LiteralInfo {
        return { kind: "integer", integer };
    },

    /** Floating-point literal bits. */
    float(format: FloatType, bits: bigint): LiteralInfo {
        return { kind: "float", format, bits };
    },

    /** Bigint literal. */
    "bigint"(bigint_: bigint): LiteralInfo {
        return { kind: "bigint", bigint: bigint_ };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: LiteralInfo): void {
        encodeLiteralInfo(writer, value);
    },

    /** Decode one LiteralInfo. */
    decode(reader: BinaryReader): LiteralInfo {
        return decodeLiteralInfo(reader);
    },

    /** Return this value as JSON. */
    toJson(value: LiteralInfo): Json {
        return toJsonLiteralInfo(value);
    },

    /** Return one LiteralInfo from one JSON value. */
    fromJson(value: Json): LiteralInfo {
        return fromJsonLiteralInfo(value);
    },
};

/** Encode one LiteralInfo. */
export function encodeLiteralInfo(writer: BinaryWriter, value: LiteralInfo): void {
    switch (value.kind) {
        case "boolean":
            writer.writeUnsigned(0);
            writer.writeBool(value.boolean);
            return;
        case "character":
            writer.writeUnsigned(1);
            writer.writeChar(value.character);
            return;
        case "string":
            writer.writeUnsigned(2);
            encodeStringId(writer, value.string);
            return;
        case "integer":
            writer.writeUnsigned(3);
            writer.writeSigned(value.integer);
            return;
        case "float":
            writer.writeUnsigned(4);
            encodeFloatType(writer, value.format);
            writer.writeUnsigned(value.bits);
            return;
        case "bigint":
            writer.writeUnsigned(5);
            writer.writeSigned(value.bigint);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one LiteralInfo. */
export function decodeLiteralInfo(reader: BinaryReader): LiteralInfo {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const boolean_ = reader.readBool();

            return { kind: "boolean", boolean: boolean_ };
        }
        case 1: {
            const character = reader.readChar();

            return { kind: "character", character };
        }
        case 2: {
            const string_ = decodeStringId(reader);

            return { kind: "string", string: string_ };
        }
        case 3: {
            const integer = reader.readSigned();

            return { kind: "integer", integer };
        }
        case 4: {
            const format = decodeFloatType(reader);
            const bits = reader.readUnsigned();

            return {
                kind: "float",
                format,
                bits,
            };
        }
        case 5: {
            const bigint_ = reader.readSigned();

            return { kind: "bigint", bigint: bigint_ };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one LiteralInfo. */
export function toJsonLiteralInfo(value: LiteralInfo): Json {
    switch (value.kind) {
        case "boolean":
            return {
                kind: "boolean",
                boolean: value.boolean,
            };
        case "character":
            return {
                kind: "character",
                character: value.character,
            };
        case "string":
            return {
                kind: "string",
                string: toJsonStringId(value.string),
            };
        case "integer":
            return {
                kind: "integer",
                integer: value.integer.toString(),
            };
        case "float":
            return {
                kind: "float",
                format: toJsonFloatType(value.format),
                bits: value.bits.toString(),
            };
        case "bigint":
            return {
                kind: "bigint",
                bigint: value.bigint.toString(),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one LiteralInfo from one JSON value. */
export function fromJsonLiteralInfo(value: Json): LiteralInfo {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "boolean":
            return {
                kind,
                boolean: jsonBool(jsonField(object, "boolean")),
            };
        case "character":
            return {
                kind,
                character: jsonString(jsonField(object, "character")),
            };
        case "string":
            return {
                kind,
                string: fromJsonStringId(jsonField(object, "string")),
            };
        case "integer":
            return {
                kind,
                integer: jsonBigint(jsonField(object, "integer")),
            };
        case "float":
            return {
                kind,
                format: fromJsonFloatType(jsonField(object, "format")),
                bits: jsonBigint(jsonField(object, "bits")),
            };
        case "bigint":
            return {
                kind,
                bigint: jsonBigint(jsonField(object, "bigint")),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** Reflected memory singleton program type. */
export type MemoryInfo =
    /** Reference access singleton. */
    | {
          readonly kind: "access";
          readonly access: Access;
      }
    /** Concrete storage space singleton. */
    | {
          readonly kind: "space";
          readonly space: Space;
      }
    /** Lifetime singleton. */
    | {
          readonly kind: "lifetime";
          readonly lifetime: Lifetime;
      }
;

export const MemoryInfo = {
    /** Reference access singleton. */
    access(access: Access): MemoryInfo {
        return { kind: "access", access };
    },

    /** Concrete storage space singleton. */
    space(space: Space): MemoryInfo {
        return { kind: "space", space };
    },

    /** Lifetime singleton. */
    lifetime(lifetime: Lifetime): MemoryInfo {
        return { kind: "lifetime", lifetime };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: MemoryInfo): void {
        encodeMemoryInfo(writer, value);
    },

    /** Decode one MemoryInfo. */
    decode(reader: BinaryReader): MemoryInfo {
        return decodeMemoryInfo(reader);
    },

    /** Return this value as JSON. */
    toJson(value: MemoryInfo): Json {
        return toJsonMemoryInfo(value);
    },

    /** Return one MemoryInfo from one JSON value. */
    fromJson(value: Json): MemoryInfo {
        return fromJsonMemoryInfo(value);
    },
};

/** Encode one MemoryInfo. */
export function encodeMemoryInfo(writer: BinaryWriter, value: MemoryInfo): void {
    switch (value.kind) {
        case "access":
            writer.writeUnsigned(0);
            encodeAccess(writer, value.access);
            return;
        case "space":
            writer.writeUnsigned(1);
            encodeSpace(writer, value.space);
            return;
        case "lifetime":
            writer.writeUnsigned(2);
            encodeLifetime(writer, value.lifetime);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one MemoryInfo. */
export function decodeMemoryInfo(reader: BinaryReader): MemoryInfo {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const access = decodeAccess(reader);

            return { kind: "access", access };
        }
        case 1: {
            const space = decodeSpace(reader);

            return { kind: "space", space };
        }
        case 2: {
            const lifetime = decodeLifetime(reader);

            return { kind: "lifetime", lifetime };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one MemoryInfo. */
export function toJsonMemoryInfo(value: MemoryInfo): Json {
    switch (value.kind) {
        case "access":
            return {
                kind: "access",
                access: toJsonAccess(value.access),
            };
        case "space":
            return {
                kind: "space",
                space: toJsonSpace(value.space),
            };
        case "lifetime":
            return {
                kind: "lifetime",
                lifetime: toJsonLifetime(value.lifetime),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one MemoryInfo from one JSON value. */
export function fromJsonMemoryInfo(value: Json): MemoryInfo {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "access":
            return {
                kind,
                access: fromJsonAccess(jsonField(object, "access")),
            };
        case "space":
            return {
                kind,
                space: fromJsonSpace(jsonField(object, "space")),
            };
        case "lifetime":
            return {
                kind,
                lifetime: fromJsonLifetime(jsonField(object, "lifetime")),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** Reflected generic declaration application. */
export type GenericInfo = {
    /** Declaration name when one exists. */
    readonly name?: StringId;
    /** Complete type arguments. */
    readonly arguments: ReadonlyArray<TypeId>;
};

export const GenericInfo = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: GenericInfo): void {
        encodeGenericInfo(writer, value);
    },

    /** Decode one GenericInfo. */
    decode(reader: BinaryReader): GenericInfo {
        return decodeGenericInfo(reader);
    },

    /** Return this value as JSON. */
    toJson(value: GenericInfo): Json {
        return toJsonGenericInfo(value);
    },

    /** Return one GenericInfo from one JSON value. */
    fromJson(value: Json): GenericInfo {
        return fromJsonGenericInfo(value);
    },
};

/** Encode one GenericInfo. */
export function encodeGenericInfo(writer: BinaryWriter, value: GenericInfo): void {
    writer.writeOption(value.name, (value0) => {
        encodeStringId(writer, value0);
    });
    writer.writeUnsigned(value.arguments.length);
    for (const item1 of value.arguments) {
        encodeTypeId(writer, item1);
    }
}

/** Decode one GenericInfo. */
export function decodeGenericInfo(reader: BinaryReader): GenericInfo {
    const name = reader.readOption(() => decodeStringId(reader));
    const arguments_ = (() => { const length1 = reader.readNumber(); const items1: Array<TypeId> = []; for (let index = 0; index < length1; index += 1) { items1.push(decodeTypeId(reader)); } return items1; })();

    return {
        ...(name === undefined ? {} : { name }),
        arguments: arguments_,
    };
}

/** Return one JSON value for one GenericInfo. */
export function toJsonGenericInfo(value: GenericInfo): Json {
    return {
        ...(value.name === undefined ? {} : { name: toJsonStringId(value.name) }),
        arguments: value.arguments.map((item0) => toJsonTypeId(item0)),
    };
}

/** Return one GenericInfo from one JSON value. */
export function fromJsonGenericInfo(value: Json): GenericInfo {
    const object = jsonObject(value);

    return {
        name: jsonOptional(object, "name", (value) => fromJsonStringId(value)),
        arguments: jsonArray(jsonField(object, "arguments")).map((item0) => fromJsonTypeId(item0)),
    };
}

/** Reflected member type selection. */
export type MemberInfo = {
    /** Owner type. */
    readonly owner: TypeId;
    /** Selected member name when one exists. */
    readonly name?: StringId;
    /** Complete type arguments. */
    readonly arguments: ReadonlyArray<TypeId>;
};

export const MemberInfo = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: MemberInfo): void {
        encodeMemberInfo(writer, value);
    },

    /** Decode one MemberInfo. */
    decode(reader: BinaryReader): MemberInfo {
        return decodeMemberInfo(reader);
    },

    /** Return this value as JSON. */
    toJson(value: MemberInfo): Json {
        return toJsonMemberInfo(value);
    },

    /** Return one MemberInfo from one JSON value. */
    fromJson(value: Json): MemberInfo {
        return fromJsonMemberInfo(value);
    },
};

/** Encode one MemberInfo. */
export function encodeMemberInfo(writer: BinaryWriter, value: MemberInfo): void {
    encodeTypeId(writer, value.owner);
    writer.writeOption(value.name, (value1) => {
        encodeStringId(writer, value1);
    });
    writer.writeUnsigned(value.arguments.length);
    for (const item2 of value.arguments) {
        encodeTypeId(writer, item2);
    }
}

/** Decode one MemberInfo. */
export function decodeMemberInfo(reader: BinaryReader): MemberInfo {
    const owner = decodeTypeId(reader);
    const name = reader.readOption(() => decodeStringId(reader));
    const arguments_ = (() => { const length2 = reader.readNumber(); const items2: Array<TypeId> = []; for (let index = 0; index < length2; index += 1) { items2.push(decodeTypeId(reader)); } return items2; })();

    return {
        owner,
        ...(name === undefined ? {} : { name }),
        arguments: arguments_,
    };
}

/** Return one JSON value for one MemberInfo. */
export function toJsonMemberInfo(value: MemberInfo): Json {
    return {
        owner: toJsonTypeId(value.owner),
        ...(value.name === undefined ? {} : { name: toJsonStringId(value.name) }),
        arguments: value.arguments.map((item0) => toJsonTypeId(item0)),
    };
}

/** Return one MemberInfo from one JSON value. */
export function fromJsonMemberInfo(value: Json): MemberInfo {
    const object = jsonObject(value);

    return {
        owner: fromJsonTypeId(jsonField(object, "owner")),
        name: jsonOptional(object, "name", (value) => fromJsonStringId(value)),
        arguments: jsonArray(jsonField(object, "arguments")).map((item0) => fromJsonTypeId(item0)),
    };
}

/** Reflected ownership or access form. */
export type FormInfo = {
    /** Form constructor. */
    readonly form: Form;
    /** Carried value type. */
    readonly value: TypeId;
};

export const FormInfo = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: FormInfo): void {
        encodeFormInfo(writer, value);
    },

    /** Decode one FormInfo. */
    decode(reader: BinaryReader): FormInfo {
        return decodeFormInfo(reader);
    },

    /** Return this value as JSON. */
    toJson(value: FormInfo): Json {
        return toJsonFormInfo(value);
    },

    /** Return one FormInfo from one JSON value. */
    fromJson(value: Json): FormInfo {
        return fromJsonFormInfo(value);
    },
};

/** Encode one FormInfo. */
export function encodeFormInfo(writer: BinaryWriter, value: FormInfo): void {
    encodeForm(writer, value.form);
    encodeTypeId(writer, value.value);
}

/** Decode one FormInfo. */
export function decodeFormInfo(reader: BinaryReader): FormInfo {
    const form = decodeForm(reader);
    const value = decodeTypeId(reader);

    return {
        form,
        value,
    };
}

/** Return one JSON value for one FormInfo. */
export function toJsonFormInfo(value: FormInfo): Json {
    return {
        form: toJsonForm(value.form),
        value: toJsonTypeId(value.value),
    };
}

/** Return one FormInfo from one JSON value. */
export function fromJsonFormInfo(value: Json): FormInfo {
    const object = jsonObject(value);

    return {
        form: fromJsonForm(jsonField(object, "form")),
        value: fromJsonTypeId(jsonField(object, "value")),
    };
}

/** Reflected ownership or access form constructor. */
export type Form =
    /** Managed value. */
    | {
          readonly kind: "managed";
      }
    /** Owned value. */
    | {
          readonly kind: "owned";
      }
    /** Borrowed value. */
    | {
          readonly kind: "borrowed";
          /** Borrow lifetime type. */
          readonly lifetime: TypeId;
          /** Borrow access type. */
          readonly access: TypeId;
      }
    /** Raw pointer value. */
    | {
          readonly kind: "raw";
      }
    /** Placed value. */
    | {
          readonly kind: "placed";
          /** Placement type. */
          readonly place: TypeId;
      }
    /** Readonly view. */
    | {
          readonly kind: "readonly";
      }
;

export const Form = {
    /** Managed value. */
    managed(): Form {
        return { kind: "managed" };
    },

    /** Owned value. */
    owned(): Form {
        return { kind: "owned" };
    },

    /** Borrowed value. */
    borrowed(lifetime: TypeId, access: TypeId): Form {
        return { kind: "borrowed", lifetime, access };
    },

    /** Raw pointer value. */
    raw(): Form {
        return { kind: "raw" };
    },

    /** Placed value. */
    placed(place: TypeId): Form {
        return { kind: "placed", place };
    },

    /** Readonly view. */
    "readonly"(): Form {
        return { kind: "readonly" };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: Form): void {
        encodeForm(writer, value);
    },

    /** Decode one Form. */
    decode(reader: BinaryReader): Form {
        return decodeForm(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Form): Json {
        return toJsonForm(value);
    },

    /** Return one Form from one JSON value. */
    fromJson(value: Json): Form {
        return fromJsonForm(value);
    },
};

/** Encode one Form. */
export function encodeForm(writer: BinaryWriter, value: Form): void {
    switch (value.kind) {
        case "managed":
            writer.writeUnsigned(0);
            return;
        case "owned":
            writer.writeUnsigned(1);
            return;
        case "borrowed":
            writer.writeUnsigned(2);
            encodeTypeId(writer, value.lifetime);
            encodeTypeId(writer, value.access);
            return;
        case "raw":
            writer.writeUnsigned(3);
            return;
        case "placed":
            writer.writeUnsigned(4);
            encodeTypeId(writer, value.place);
            return;
        case "readonly":
            writer.writeUnsigned(5);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one Form. */
export function decodeForm(reader: BinaryReader): Form {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            return { kind: "managed" };
        }
        case 1: {
            return { kind: "owned" };
        }
        case 2: {
            const lifetime = decodeTypeId(reader);
            const access = decodeTypeId(reader);

            return {
                kind: "borrowed",
                lifetime,
                access,
            };
        }
        case 3: {
            return { kind: "raw" };
        }
        case 4: {
            const place = decodeTypeId(reader);

            return {
                kind: "placed",
                place,
            };
        }
        case 5: {
            return { kind: "readonly" };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one Form. */
export function toJsonForm(value: Form): Json {
    switch (value.kind) {
        case "managed":
            return {
                kind: "managed",
            };
        case "owned":
            return {
                kind: "owned",
            };
        case "borrowed":
            return {
                kind: "borrowed",
                lifetime: toJsonTypeId(value.lifetime),
                access: toJsonTypeId(value.access),
            };
        case "raw":
            return {
                kind: "raw",
            };
        case "placed":
            return {
                kind: "placed",
                place: toJsonTypeId(value.place),
            };
        case "readonly":
            return {
                kind: "readonly",
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one Form from one JSON value. */
export function fromJsonForm(value: Json): Form {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "managed":
            return {
                kind,
            };
        case "owned":
            return {
                kind,
            };
        case "borrowed":
            return {
                kind,
                lifetime: fromJsonTypeId(jsonField(object, "lifetime")),
                access: fromJsonTypeId(jsonField(object, "access")),
            };
        case "raw":
            return {
                kind,
            };
        case "placed":
            return {
                kind,
                place: fromJsonTypeId(jsonField(object, "place")),
            };
        case "readonly":
            return {
                kind,
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** Reflected erased dynamic value type. */
export type DynamicInfo = {
    /** Erased constraint type. */
    readonly constraint: TypeId;
};

export const DynamicInfo = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: DynamicInfo): void {
        encodeDynamicInfo(writer, value);
    },

    /** Decode one DynamicInfo. */
    decode(reader: BinaryReader): DynamicInfo {
        return decodeDynamicInfo(reader);
    },

    /** Return this value as JSON. */
    toJson(value: DynamicInfo): Json {
        return toJsonDynamicInfo(value);
    },

    /** Return one DynamicInfo from one JSON value. */
    fromJson(value: Json): DynamicInfo {
        return fromJsonDynamicInfo(value);
    },
};

/** Encode one DynamicInfo. */
export function encodeDynamicInfo(writer: BinaryWriter, value: DynamicInfo): void {
    encodeTypeId(writer, value.constraint);
}

/** Decode one DynamicInfo. */
export function decodeDynamicInfo(reader: BinaryReader): DynamicInfo {
    const constraint = decodeTypeId(reader);

    return {
        constraint,
    };
}

/** Return one JSON value for one DynamicInfo. */
export function toJsonDynamicInfo(value: DynamicInfo): Json {
    return {
        constraint: toJsonTypeId(value.constraint),
    };
}

/** Return one DynamicInfo from one JSON value. */
export function fromJsonDynamicInfo(value: Json): DynamicInfo {
    const object = jsonObject(value);

    return {
        constraint: fromJsonTypeId(jsonField(object, "constraint")),
    };
}

/** Reflected homogeneous array type. */
export type ArrayInfo = {
    /** Element type. */
    readonly element: TypeId;
};

export const ArrayInfo = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ArrayInfo): void {
        encodeArrayInfo(writer, value);
    },

    /** Decode one ArrayInfo. */
    decode(reader: BinaryReader): ArrayInfo {
        return decodeArrayInfo(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ArrayInfo): Json {
        return toJsonArrayInfo(value);
    },

    /** Return one ArrayInfo from one JSON value. */
    fromJson(value: Json): ArrayInfo {
        return fromJsonArrayInfo(value);
    },
};

/** Encode one ArrayInfo. */
export function encodeArrayInfo(writer: BinaryWriter, value: ArrayInfo): void {
    encodeTypeId(writer, value.element);
}

/** Decode one ArrayInfo. */
export function decodeArrayInfo(reader: BinaryReader): ArrayInfo {
    const element = decodeTypeId(reader);

    return {
        element,
    };
}

/** Return one JSON value for one ArrayInfo. */
export function toJsonArrayInfo(value: ArrayInfo): Json {
    return {
        element: toJsonTypeId(value.element),
    };
}

/** Return one ArrayInfo from one JSON value. */
export function fromJsonArrayInfo(value: Json): ArrayInfo {
    const object = jsonObject(value);

    return {
        element: fromJsonTypeId(jsonField(object, "element")),
    };
}

/** Reflected fixed-length array type. */
export type FixedArrayInfo = {
    /** Element type. */
    readonly element: TypeId;
    /** Static count type. */
    readonly count: TypeId;
};

export const FixedArrayInfo = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: FixedArrayInfo): void {
        encodeFixedArrayInfo(writer, value);
    },

    /** Decode one FixedArrayInfo. */
    decode(reader: BinaryReader): FixedArrayInfo {
        return decodeFixedArrayInfo(reader);
    },

    /** Return this value as JSON. */
    toJson(value: FixedArrayInfo): Json {
        return toJsonFixedArrayInfo(value);
    },

    /** Return one FixedArrayInfo from one JSON value. */
    fromJson(value: Json): FixedArrayInfo {
        return fromJsonFixedArrayInfo(value);
    },
};

/** Encode one FixedArrayInfo. */
export function encodeFixedArrayInfo(writer: BinaryWriter, value: FixedArrayInfo): void {
    encodeTypeId(writer, value.element);
    encodeTypeId(writer, value.count);
}

/** Decode one FixedArrayInfo. */
export function decodeFixedArrayInfo(reader: BinaryReader): FixedArrayInfo {
    const element = decodeTypeId(reader);
    const count = decodeTypeId(reader);

    return {
        element,
        count,
    };
}

/** Return one JSON value for one FixedArrayInfo. */
export function toJsonFixedArrayInfo(value: FixedArrayInfo): Json {
    return {
        element: toJsonTypeId(value.element),
        count: toJsonTypeId(value.count),
    };
}

/** Return one FixedArrayInfo from one JSON value. */
export function fromJsonFixedArrayInfo(value: Json): FixedArrayInfo {
    const object = jsonObject(value);

    return {
        element: fromJsonTypeId(jsonField(object, "element")),
        count: fromJsonTypeId(jsonField(object, "count")),
    };
}

/** Reflected scalar interval type. */
export type RangeInfo = {
    /** Inclusive lower bound. */
    readonly start?: LiteralInfo;
    /** Upper bound. */
    readonly end?: LiteralInfo;
    /** Whether the upper bound is included. */
    readonly isInclusive: boolean;
};

export const RangeInfo = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: RangeInfo): void {
        encodeRangeInfo(writer, value);
    },

    /** Decode one RangeInfo. */
    decode(reader: BinaryReader): RangeInfo {
        return decodeRangeInfo(reader);
    },

    /** Return this value as JSON. */
    toJson(value: RangeInfo): Json {
        return toJsonRangeInfo(value);
    },

    /** Return one RangeInfo from one JSON value. */
    fromJson(value: Json): RangeInfo {
        return fromJsonRangeInfo(value);
    },
};

/** Encode one RangeInfo. */
export function encodeRangeInfo(writer: BinaryWriter, value: RangeInfo): void {
    writer.writeOption(value.start, (value0) => {
        encodeLiteralInfo(writer, value0);
    });
    writer.writeOption(value.end, (value1) => {
        encodeLiteralInfo(writer, value1);
    });
    writer.writeBool(value.isInclusive);
}

/** Decode one RangeInfo. */
export function decodeRangeInfo(reader: BinaryReader): RangeInfo {
    const start = reader.readOption(() => decodeLiteralInfo(reader));
    const end = reader.readOption(() => decodeLiteralInfo(reader));
    const isInclusive = reader.readBool();

    return {
        ...(start === undefined ? {} : { start }),
        ...(end === undefined ? {} : { end }),
        isInclusive,
    };
}

/** Return one JSON value for one RangeInfo. */
export function toJsonRangeInfo(value: RangeInfo): Json {
    return {
        ...(value.start === undefined ? {} : { start: toJsonLiteralInfo(value.start) }),
        ...(value.end === undefined ? {} : { end: toJsonLiteralInfo(value.end) }),
        isInclusive: value.isInclusive,
    };
}

/** Return one RangeInfo from one JSON value. */
export function fromJsonRangeInfo(value: Json): RangeInfo {
    const object = jsonObject(value);

    return {
        start: jsonOptional(object, "start", (value) => fromJsonLiteralInfo(value)),
        end: jsonOptional(object, "end", (value) => fromJsonLiteralInfo(value)),
        isInclusive: jsonBool(jsonField(object, "isInclusive")),
    };
}

/** Reflected runtime-length homogeneous view type. */
export type SliceInfo = {
    /** Element type. */
    readonly element: TypeId;
};

export const SliceInfo = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: SliceInfo): void {
        encodeSliceInfo(writer, value);
    },

    /** Decode one SliceInfo. */
    decode(reader: BinaryReader): SliceInfo {
        return decodeSliceInfo(reader);
    },

    /** Return this value as JSON. */
    toJson(value: SliceInfo): Json {
        return toJsonSliceInfo(value);
    },

    /** Return one SliceInfo from one JSON value. */
    fromJson(value: Json): SliceInfo {
        return fromJsonSliceInfo(value);
    },
};

/** Encode one SliceInfo. */
export function encodeSliceInfo(writer: BinaryWriter, value: SliceInfo): void {
    encodeTypeId(writer, value.element);
}

/** Decode one SliceInfo. */
export function decodeSliceInfo(reader: BinaryReader): SliceInfo {
    const element = decodeTypeId(reader);

    return {
        element,
    };
}

/** Return one JSON value for one SliceInfo. */
export function toJsonSliceInfo(value: SliceInfo): Json {
    return {
        element: toJsonTypeId(value.element),
    };
}

/** Return one SliceInfo from one JSON value. */
export function fromJsonSliceInfo(value: Json): SliceInfo {
    const object = jsonObject(value);

    return {
        element: fromJsonTypeId(jsonField(object, "element")),
    };
}

/** Reflected tuple type. */
export type TupleInfo = {
    /** Tuple elements. */
    readonly elements: ReadonlyArray<TypeElementInfo>;
};

export const TupleInfo = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: TupleInfo): void {
        encodeTupleInfo(writer, value);
    },

    /** Decode one TupleInfo. */
    decode(reader: BinaryReader): TupleInfo {
        return decodeTupleInfo(reader);
    },

    /** Return this value as JSON. */
    toJson(value: TupleInfo): Json {
        return toJsonTupleInfo(value);
    },

    /** Return one TupleInfo from one JSON value. */
    fromJson(value: Json): TupleInfo {
        return fromJsonTupleInfo(value);
    },
};

/** Encode one TupleInfo. */
export function encodeTupleInfo(writer: BinaryWriter, value: TupleInfo): void {
    writer.writeUnsigned(value.elements.length);
    for (const item0 of value.elements) {
        encodeTypeElementInfo(writer, item0);
    }
}

/** Decode one TupleInfo. */
export function decodeTupleInfo(reader: BinaryReader): TupleInfo {
    const elements = (() => { const length0 = reader.readNumber(); const items0: Array<TypeElementInfo> = []; for (let index = 0; index < length0; index += 1) { items0.push(decodeTypeElementInfo(reader)); } return items0; })();

    return {
        elements,
    };
}

/** Return one JSON value for one TupleInfo. */
export function toJsonTupleInfo(value: TupleInfo): Json {
    return {
        elements: value.elements.map((item0) => toJsonTypeElementInfo(item0)),
    };
}

/** Return one TupleInfo from one JSON value. */
export function fromJsonTupleInfo(value: Json): TupleInfo {
    const object = jsonObject(value);

    return {
        elements: jsonArray(jsonField(object, "elements")).map((item0) => fromJsonTypeElementInfo(item0)),
    };
}

/** Reflected tuple element. */
export type TypeElementInfo = {
    /** Element label when one exists. */
    readonly label?: StringId;
    /** Element type. */
    readonly ty: TypeId;
    /** Whether the element is optional. */
    readonly isOptional: boolean;
    /** Whether the element is readonly. */
    readonly isReadonly: boolean;
    /** Whether the element captures remaining tuple elements. */
    readonly isRest: boolean;
};

export const TypeElementInfo = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: TypeElementInfo): void {
        encodeTypeElementInfo(writer, value);
    },

    /** Decode one TypeElementInfo. */
    decode(reader: BinaryReader): TypeElementInfo {
        return decodeTypeElementInfo(reader);
    },

    /** Return this value as JSON. */
    toJson(value: TypeElementInfo): Json {
        return toJsonTypeElementInfo(value);
    },

    /** Return one TypeElementInfo from one JSON value. */
    fromJson(value: Json): TypeElementInfo {
        return fromJsonTypeElementInfo(value);
    },
};

/** Encode one TypeElementInfo. */
export function encodeTypeElementInfo(writer: BinaryWriter, value: TypeElementInfo): void {
    writer.writeOption(value.label, (value0) => {
        encodeStringId(writer, value0);
    });
    encodeTypeId(writer, value.ty);
    writer.writeBool(value.isOptional);
    writer.writeBool(value.isReadonly);
    writer.writeBool(value.isRest);
}

/** Decode one TypeElementInfo. */
export function decodeTypeElementInfo(reader: BinaryReader): TypeElementInfo {
    const label = reader.readOption(() => decodeStringId(reader));
    const ty = decodeTypeId(reader);
    const isOptional = reader.readBool();
    const isReadonly = reader.readBool();
    const isRest = reader.readBool();

    return {
        ...(label === undefined ? {} : { label }),
        ty,
        isOptional,
        isReadonly,
        isRest,
    };
}

/** Return one JSON value for one TypeElementInfo. */
export function toJsonTypeElementInfo(value: TypeElementInfo): Json {
    return {
        ...(value.label === undefined ? {} : { label: toJsonStringId(value.label) }),
        ty: toJsonTypeId(value.ty),
        isOptional: value.isOptional,
        isReadonly: value.isReadonly,
        isRest: value.isRest,
    };
}

/** Return one TypeElementInfo from one JSON value. */
export function fromJsonTypeElementInfo(value: Json): TypeElementInfo {
    const object = jsonObject(value);

    return {
        label: jsonOptional(object, "label", (value) => fromJsonStringId(value)),
        ty: fromJsonTypeId(jsonField(object, "ty")),
        isOptional: jsonBool(jsonField(object, "isOptional")),
        isReadonly: jsonBool(jsonField(object, "isReadonly")),
        isRest: jsonBool(jsonField(object, "isRest")),
    };
}

/** Reflected structural object shape type. */
export type ShapeInfo = {
    /** Shape fields. */
    readonly fields: ReadonlyArray<TypeFieldInfo>;
    /** Call signature types. */
    readonly callSignatures: ReadonlyArray<TypeId>;
    /** Construct signature types. */
    readonly constructSignatures: ReadonlyArray<TypeId>;
    /** Index signatures. */
    readonly indexSignatures: ReadonlyArray<TypeIndexSignatureInfo>;
};

export const ShapeInfo = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ShapeInfo): void {
        encodeShapeInfo(writer, value);
    },

    /** Decode one ShapeInfo. */
    decode(reader: BinaryReader): ShapeInfo {
        return decodeShapeInfo(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ShapeInfo): Json {
        return toJsonShapeInfo(value);
    },

    /** Return one ShapeInfo from one JSON value. */
    fromJson(value: Json): ShapeInfo {
        return fromJsonShapeInfo(value);
    },
};

/** Encode one ShapeInfo. */
export function encodeShapeInfo(writer: BinaryWriter, value: ShapeInfo): void {
    writer.writeUnsigned(value.fields.length);
    for (const item0 of value.fields) {
        encodeTypeFieldInfo(writer, item0);
    }
    writer.writeUnsigned(value.callSignatures.length);
    for (const item1 of value.callSignatures) {
        encodeTypeId(writer, item1);
    }
    writer.writeUnsigned(value.constructSignatures.length);
    for (const item2 of value.constructSignatures) {
        encodeTypeId(writer, item2);
    }
    writer.writeUnsigned(value.indexSignatures.length);
    for (const item3 of value.indexSignatures) {
        encodeTypeIndexSignatureInfo(writer, item3);
    }
}

/** Decode one ShapeInfo. */
export function decodeShapeInfo(reader: BinaryReader): ShapeInfo {
    const fields = (() => { const length0 = reader.readNumber(); const items0: Array<TypeFieldInfo> = []; for (let index = 0; index < length0; index += 1) { items0.push(decodeTypeFieldInfo(reader)); } return items0; })();
    const callSignatures = (() => { const length1 = reader.readNumber(); const items1: Array<TypeId> = []; for (let index = 0; index < length1; index += 1) { items1.push(decodeTypeId(reader)); } return items1; })();
    const constructSignatures = (() => { const length2 = reader.readNumber(); const items2: Array<TypeId> = []; for (let index = 0; index < length2; index += 1) { items2.push(decodeTypeId(reader)); } return items2; })();
    const indexSignatures = (() => { const length3 = reader.readNumber(); const items3: Array<TypeIndexSignatureInfo> = []; for (let index = 0; index < length3; index += 1) { items3.push(decodeTypeIndexSignatureInfo(reader)); } return items3; })();

    return {
        fields,
        callSignatures,
        constructSignatures,
        indexSignatures,
    };
}

/** Return one JSON value for one ShapeInfo. */
export function toJsonShapeInfo(value: ShapeInfo): Json {
    return {
        fields: value.fields.map((item0) => toJsonTypeFieldInfo(item0)),
        callSignatures: value.callSignatures.map((item0) => toJsonTypeId(item0)),
        constructSignatures: value.constructSignatures.map((item0) => toJsonTypeId(item0)),
        indexSignatures: value.indexSignatures.map((item0) => toJsonTypeIndexSignatureInfo(item0)),
    };
}

/** Return one ShapeInfo from one JSON value. */
export function fromJsonShapeInfo(value: Json): ShapeInfo {
    const object = jsonObject(value);

    return {
        fields: jsonArray(jsonField(object, "fields")).map((item0) => fromJsonTypeFieldInfo(item0)),
        callSignatures: jsonArray(jsonField(object, "callSignatures")).map((item0) => fromJsonTypeId(item0)),
        constructSignatures: jsonArray(jsonField(object, "constructSignatures")).map((item0) => fromJsonTypeId(item0)),
        indexSignatures: jsonArray(jsonField(object, "indexSignatures")).map((item0) => fromJsonTypeIndexSignatureInfo(item0)),
    };
}

/** Reflected object-like type field. */
export type TypeFieldInfo = {
    /** Field name when one exists. */
    readonly name?: StringId;
    /** Field type. */
    readonly ty: TypeId;
    /** Whether the field is optional. */
    readonly isOptional: boolean;
    /** Whether the field is readonly. */
    readonly isReadonly: boolean;
};

export const TypeFieldInfo = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: TypeFieldInfo): void {
        encodeTypeFieldInfo(writer, value);
    },

    /** Decode one TypeFieldInfo. */
    decode(reader: BinaryReader): TypeFieldInfo {
        return decodeTypeFieldInfo(reader);
    },

    /** Return this value as JSON. */
    toJson(value: TypeFieldInfo): Json {
        return toJsonTypeFieldInfo(value);
    },

    /** Return one TypeFieldInfo from one JSON value. */
    fromJson(value: Json): TypeFieldInfo {
        return fromJsonTypeFieldInfo(value);
    },
};

/** Encode one TypeFieldInfo. */
export function encodeTypeFieldInfo(writer: BinaryWriter, value: TypeFieldInfo): void {
    writer.writeOption(value.name, (value0) => {
        encodeStringId(writer, value0);
    });
    encodeTypeId(writer, value.ty);
    writer.writeBool(value.isOptional);
    writer.writeBool(value.isReadonly);
}

/** Decode one TypeFieldInfo. */
export function decodeTypeFieldInfo(reader: BinaryReader): TypeFieldInfo {
    const name = reader.readOption(() => decodeStringId(reader));
    const ty = decodeTypeId(reader);
    const isOptional = reader.readBool();
    const isReadonly = reader.readBool();

    return {
        ...(name === undefined ? {} : { name }),
        ty,
        isOptional,
        isReadonly,
    };
}

/** Return one JSON value for one TypeFieldInfo. */
export function toJsonTypeFieldInfo(value: TypeFieldInfo): Json {
    return {
        ...(value.name === undefined ? {} : { name: toJsonStringId(value.name) }),
        ty: toJsonTypeId(value.ty),
        isOptional: value.isOptional,
        isReadonly: value.isReadonly,
    };
}

/** Return one TypeFieldInfo from one JSON value. */
export function fromJsonTypeFieldInfo(value: Json): TypeFieldInfo {
    const object = jsonObject(value);

    return {
        name: jsonOptional(object, "name", (value) => fromJsonStringId(value)),
        ty: fromJsonTypeId(jsonField(object, "ty")),
        isOptional: jsonBool(jsonField(object, "isOptional")),
        isReadonly: jsonBool(jsonField(object, "isReadonly")),
    };
}

/** Reflected index signature in an object-like type. */
export type TypeIndexSignatureInfo = {
    /** Key type. */
    readonly keyType: TypeId;
    /** Value type. */
    readonly valueType: TypeId;
    /** Whether the index signature is optional. */
    readonly isOptional: boolean;
    /** Whether the index signature is readonly. */
    readonly isReadonly: boolean;
};

export const TypeIndexSignatureInfo = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: TypeIndexSignatureInfo): void {
        encodeTypeIndexSignatureInfo(writer, value);
    },

    /** Decode one TypeIndexSignatureInfo. */
    decode(reader: BinaryReader): TypeIndexSignatureInfo {
        return decodeTypeIndexSignatureInfo(reader);
    },

    /** Return this value as JSON. */
    toJson(value: TypeIndexSignatureInfo): Json {
        return toJsonTypeIndexSignatureInfo(value);
    },

    /** Return one TypeIndexSignatureInfo from one JSON value. */
    fromJson(value: Json): TypeIndexSignatureInfo {
        return fromJsonTypeIndexSignatureInfo(value);
    },
};

/** Encode one TypeIndexSignatureInfo. */
export function encodeTypeIndexSignatureInfo(writer: BinaryWriter, value: TypeIndexSignatureInfo): void {
    encodeTypeId(writer, value.keyType);
    encodeTypeId(writer, value.valueType);
    writer.writeBool(value.isOptional);
    writer.writeBool(value.isReadonly);
}

/** Decode one TypeIndexSignatureInfo. */
export function decodeTypeIndexSignatureInfo(reader: BinaryReader): TypeIndexSignatureInfo {
    const keyType = decodeTypeId(reader);
    const valueType = decodeTypeId(reader);
    const isOptional = reader.readBool();
    const isReadonly = reader.readBool();

    return {
        keyType,
        valueType,
        isOptional,
        isReadonly,
    };
}

/** Return one JSON value for one TypeIndexSignatureInfo. */
export function toJsonTypeIndexSignatureInfo(value: TypeIndexSignatureInfo): Json {
    return {
        keyType: toJsonTypeId(value.keyType),
        valueType: toJsonTypeId(value.valueType),
        isOptional: value.isOptional,
        isReadonly: value.isReadonly,
    };
}

/** Return one TypeIndexSignatureInfo from one JSON value. */
export function fromJsonTypeIndexSignatureInfo(value: Json): TypeIndexSignatureInfo {
    const object = jsonObject(value);

    return {
        keyType: fromJsonTypeId(jsonField(object, "keyType")),
        valueType: fromJsonTypeId(jsonField(object, "valueType")),
        isOptional: jsonBool(jsonField(object, "isOptional")),
        isReadonly: jsonBool(jsonField(object, "isReadonly")),
    };
}

/** Reflected function signature type. */
export type FunctionSignatureInfo = {
    /** Optional receiver type. */
    readonly thisParameter?: TypeId;
    /** Runtime parameters. */
    readonly parameters: ReadonlyArray<FunctionParameterInfo>;
    /** Optional return type. */
    readonly returnType?: TypeId;
    /** Whether this signature is async. */
    readonly isAsync: boolean;
    /** Whether this signature is a generator. */
    readonly isGenerator: boolean;
};

export const FunctionSignatureInfo = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: FunctionSignatureInfo): void {
        encodeFunctionSignatureInfo(writer, value);
    },

    /** Decode one FunctionSignatureInfo. */
    decode(reader: BinaryReader): FunctionSignatureInfo {
        return decodeFunctionSignatureInfo(reader);
    },

    /** Return this value as JSON. */
    toJson(value: FunctionSignatureInfo): Json {
        return toJsonFunctionSignatureInfo(value);
    },

    /** Return one FunctionSignatureInfo from one JSON value. */
    fromJson(value: Json): FunctionSignatureInfo {
        return fromJsonFunctionSignatureInfo(value);
    },
};

/** Encode one FunctionSignatureInfo. */
export function encodeFunctionSignatureInfo(writer: BinaryWriter, value: FunctionSignatureInfo): void {
    writer.writeOption(value.thisParameter, (value0) => {
        encodeTypeId(writer, value0);
    });
    writer.writeUnsigned(value.parameters.length);
    for (const item1 of value.parameters) {
        encodeFunctionParameterInfo(writer, item1);
    }
    writer.writeOption(value.returnType, (value2) => {
        encodeTypeId(writer, value2);
    });
    writer.writeBool(value.isAsync);
    writer.writeBool(value.isGenerator);
}

/** Decode one FunctionSignatureInfo. */
export function decodeFunctionSignatureInfo(reader: BinaryReader): FunctionSignatureInfo {
    const thisParameter = reader.readOption(() => decodeTypeId(reader));
    const parameters = (() => { const length1 = reader.readNumber(); const items1: Array<FunctionParameterInfo> = []; for (let index = 0; index < length1; index += 1) { items1.push(decodeFunctionParameterInfo(reader)); } return items1; })();
    const returnType = reader.readOption(() => decodeTypeId(reader));
    const isAsync = reader.readBool();
    const isGenerator = reader.readBool();

    return {
        ...(thisParameter === undefined ? {} : { thisParameter }),
        parameters,
        ...(returnType === undefined ? {} : { returnType }),
        isAsync,
        isGenerator,
    };
}

/** Return one JSON value for one FunctionSignatureInfo. */
export function toJsonFunctionSignatureInfo(value: FunctionSignatureInfo): Json {
    return {
        ...(value.thisParameter === undefined ? {} : { thisParameter: toJsonTypeId(value.thisParameter) }),
        parameters: value.parameters.map((item0) => toJsonFunctionParameterInfo(item0)),
        ...(value.returnType === undefined ? {} : { returnType: toJsonTypeId(value.returnType) }),
        isAsync: value.isAsync,
        isGenerator: value.isGenerator,
    };
}

/** Return one FunctionSignatureInfo from one JSON value. */
export function fromJsonFunctionSignatureInfo(value: Json): FunctionSignatureInfo {
    const object = jsonObject(value);

    return {
        thisParameter: jsonOptional(object, "thisParameter", (value) => fromJsonTypeId(value)),
        parameters: jsonArray(jsonField(object, "parameters")).map((item0) => fromJsonFunctionParameterInfo(item0)),
        returnType: jsonOptional(object, "returnType", (value) => fromJsonTypeId(value)),
        isAsync: jsonBool(jsonField(object, "isAsync")),
        isGenerator: jsonBool(jsonField(object, "isGenerator")),
    };
}

/** Reflected runtime parameter in a function type. */
export type FunctionParameterInfo = {
    /** Parameter type. */
    readonly ty: TypeId;
    /** Whether the parameter may be omitted. */
    readonly isOptional: boolean;
    /** Whether the parameter captures remaining arguments. */
    readonly isRest: boolean;
};

export const FunctionParameterInfo = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: FunctionParameterInfo): void {
        encodeFunctionParameterInfo(writer, value);
    },

    /** Decode one FunctionParameterInfo. */
    decode(reader: BinaryReader): FunctionParameterInfo {
        return decodeFunctionParameterInfo(reader);
    },

    /** Return this value as JSON. */
    toJson(value: FunctionParameterInfo): Json {
        return toJsonFunctionParameterInfo(value);
    },

    /** Return one FunctionParameterInfo from one JSON value. */
    fromJson(value: Json): FunctionParameterInfo {
        return fromJsonFunctionParameterInfo(value);
    },
};

/** Encode one FunctionParameterInfo. */
export function encodeFunctionParameterInfo(writer: BinaryWriter, value: FunctionParameterInfo): void {
    encodeTypeId(writer, value.ty);
    writer.writeBool(value.isOptional);
    writer.writeBool(value.isRest);
}

/** Decode one FunctionParameterInfo. */
export function decodeFunctionParameterInfo(reader: BinaryReader): FunctionParameterInfo {
    const ty = decodeTypeId(reader);
    const isOptional = reader.readBool();
    const isRest = reader.readBool();

    return {
        ty,
        isOptional,
        isRest,
    };
}

/** Return one JSON value for one FunctionParameterInfo. */
export function toJsonFunctionParameterInfo(value: FunctionParameterInfo): Json {
    return {
        ty: toJsonTypeId(value.ty),
        isOptional: value.isOptional,
        isRest: value.isRest,
    };
}

/** Return one FunctionParameterInfo from one JSON value. */
export function fromJsonFunctionParameterInfo(value: Json): FunctionParameterInfo {
    const object = jsonObject(value);

    return {
        ty: fromJsonTypeId(jsonField(object, "ty")),
        isOptional: jsonBool(jsonField(object, "isOptional")),
        isRest: jsonBool(jsonField(object, "isRest")),
    };
}

/** Reflected fat callable value type. */
export type FunctionTypeInfo = {
    /** Function signature type. */
    readonly signature: TypeId;
    /** Captured environment type. */
    readonly environment: TypeId;
};

export const FunctionTypeInfo = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: FunctionTypeInfo): void {
        encodeFunctionTypeInfo(writer, value);
    },

    /** Decode one FunctionTypeInfo. */
    decode(reader: BinaryReader): FunctionTypeInfo {
        return decodeFunctionTypeInfo(reader);
    },

    /** Return this value as JSON. */
    toJson(value: FunctionTypeInfo): Json {
        return toJsonFunctionTypeInfo(value);
    },

    /** Return one FunctionTypeInfo from one JSON value. */
    fromJson(value: Json): FunctionTypeInfo {
        return fromJsonFunctionTypeInfo(value);
    },
};

/** Encode one FunctionTypeInfo. */
export function encodeFunctionTypeInfo(writer: BinaryWriter, value: FunctionTypeInfo): void {
    encodeTypeId(writer, value.signature);
    encodeTypeId(writer, value.environment);
}

/** Decode one FunctionTypeInfo. */
export function decodeFunctionTypeInfo(reader: BinaryReader): FunctionTypeInfo {
    const signature = decodeTypeId(reader);
    const environment = decodeTypeId(reader);

    return {
        signature,
        environment,
    };
}

/** Return one JSON value for one FunctionTypeInfo. */
export function toJsonFunctionTypeInfo(value: FunctionTypeInfo): Json {
    return {
        signature: toJsonTypeId(value.signature),
        environment: toJsonTypeId(value.environment),
    };
}

/** Return one FunctionTypeInfo from one JSON value. */
export function fromJsonFunctionTypeInfo(value: Json): FunctionTypeInfo {
    const object = jsonObject(value);

    return {
        signature: fromJsonTypeId(jsonField(object, "signature")),
        environment: fromJsonTypeId(jsonField(object, "environment")),
    };
}

/** Reflected thin callable value type. */
export type FunctionPointerInfo = {
    /** Function signature type. */
    readonly signature: TypeId;
};

export const FunctionPointerInfo = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: FunctionPointerInfo): void {
        encodeFunctionPointerInfo(writer, value);
    },

    /** Decode one FunctionPointerInfo. */
    decode(reader: BinaryReader): FunctionPointerInfo {
        return decodeFunctionPointerInfo(reader);
    },

    /** Return this value as JSON. */
    toJson(value: FunctionPointerInfo): Json {
        return toJsonFunctionPointerInfo(value);
    },

    /** Return one FunctionPointerInfo from one JSON value. */
    fromJson(value: Json): FunctionPointerInfo {
        return fromJsonFunctionPointerInfo(value);
    },
};

/** Encode one FunctionPointerInfo. */
export function encodeFunctionPointerInfo(writer: BinaryWriter, value: FunctionPointerInfo): void {
    encodeTypeId(writer, value.signature);
}

/** Decode one FunctionPointerInfo. */
export function decodeFunctionPointerInfo(reader: BinaryReader): FunctionPointerInfo {
    const signature = decodeTypeId(reader);

    return {
        signature,
    };
}

/** Return one JSON value for one FunctionPointerInfo. */
export function toJsonFunctionPointerInfo(value: FunctionPointerInfo): Json {
    return {
        signature: toJsonTypeId(value.signature),
    };
}

/** Return one FunctionPointerInfo from one JSON value. */
export function fromJsonFunctionPointerInfo(value: Json): FunctionPointerInfo {
    const object = jsonObject(value);

    return {
        signature: fromJsonTypeId(jsonField(object, "signature")),
    };
}

/** Reflected union type. */
export type UnionInfo = {
    /** Union elements. */
    readonly elements: ReadonlyArray<TypeId>;
};

export const UnionInfo = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: UnionInfo): void {
        encodeUnionInfo(writer, value);
    },

    /** Decode one UnionInfo. */
    decode(reader: BinaryReader): UnionInfo {
        return decodeUnionInfo(reader);
    },

    /** Return this value as JSON. */
    toJson(value: UnionInfo): Json {
        return toJsonUnionInfo(value);
    },

    /** Return one UnionInfo from one JSON value. */
    fromJson(value: Json): UnionInfo {
        return fromJsonUnionInfo(value);
    },
};

/** Encode one UnionInfo. */
export function encodeUnionInfo(writer: BinaryWriter, value: UnionInfo): void {
    writer.writeUnsigned(value.elements.length);
    for (const item0 of value.elements) {
        encodeTypeId(writer, item0);
    }
}

/** Decode one UnionInfo. */
export function decodeUnionInfo(reader: BinaryReader): UnionInfo {
    const elements = (() => { const length0 = reader.readNumber(); const items0: Array<TypeId> = []; for (let index = 0; index < length0; index += 1) { items0.push(decodeTypeId(reader)); } return items0; })();

    return {
        elements,
    };
}

/** Return one JSON value for one UnionInfo. */
export function toJsonUnionInfo(value: UnionInfo): Json {
    return {
        elements: value.elements.map((item0) => toJsonTypeId(item0)),
    };
}

/** Return one UnionInfo from one JSON value. */
export function fromJsonUnionInfo(value: Json): UnionInfo {
    const object = jsonObject(value);

    return {
        elements: jsonArray(jsonField(object, "elements")).map((item0) => fromJsonTypeId(item0)),
    };
}

/** Reflected intersection type. */
export type IntersectionInfo = {
    /** Intersection elements. */
    readonly elements: ReadonlyArray<TypeId>;
};

export const IntersectionInfo = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: IntersectionInfo): void {
        encodeIntersectionInfo(writer, value);
    },

    /** Decode one IntersectionInfo. */
    decode(reader: BinaryReader): IntersectionInfo {
        return decodeIntersectionInfo(reader);
    },

    /** Return this value as JSON. */
    toJson(value: IntersectionInfo): Json {
        return toJsonIntersectionInfo(value);
    },

    /** Return one IntersectionInfo from one JSON value. */
    fromJson(value: Json): IntersectionInfo {
        return fromJsonIntersectionInfo(value);
    },
};

/** Encode one IntersectionInfo. */
export function encodeIntersectionInfo(writer: BinaryWriter, value: IntersectionInfo): void {
    writer.writeUnsigned(value.elements.length);
    for (const item0 of value.elements) {
        encodeTypeId(writer, item0);
    }
}

/** Decode one IntersectionInfo. */
export function decodeIntersectionInfo(reader: BinaryReader): IntersectionInfo {
    const elements = (() => { const length0 = reader.readNumber(); const items0: Array<TypeId> = []; for (let index = 0; index < length0; index += 1) { items0.push(decodeTypeId(reader)); } return items0; })();

    return {
        elements,
    };
}

/** Return one JSON value for one IntersectionInfo. */
export function toJsonIntersectionInfo(value: IntersectionInfo): Json {
    return {
        elements: value.elements.map((item0) => toJsonTypeId(item0)),
    };
}

/** Return one IntersectionInfo from one JSON value. */
export function fromJsonIntersectionInfo(value: Json): IntersectionInfo {
    const object = jsonObject(value);

    return {
        elements: jsonArray(jsonField(object, "elements")).map((item0) => fromJsonTypeId(item0)),
    };
}

/** Reflected transparent nominal type. */
export type NewtypeInfo = {
    /** Backing type. */
    readonly backing: TypeId;
};

export const NewtypeInfo = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: NewtypeInfo): void {
        encodeNewtypeInfo(writer, value);
    },

    /** Decode one NewtypeInfo. */
    decode(reader: BinaryReader): NewtypeInfo {
        return decodeNewtypeInfo(reader);
    },

    /** Return this value as JSON. */
    toJson(value: NewtypeInfo): Json {
        return toJsonNewtypeInfo(value);
    },

    /** Return one NewtypeInfo from one JSON value. */
    fromJson(value: Json): NewtypeInfo {
        return fromJsonNewtypeInfo(value);
    },
};

/** Encode one NewtypeInfo. */
export function encodeNewtypeInfo(writer: BinaryWriter, value: NewtypeInfo): void {
    encodeTypeId(writer, value.backing);
}

/** Decode one NewtypeInfo. */
export function decodeNewtypeInfo(reader: BinaryReader): NewtypeInfo {
    const backing = decodeTypeId(reader);

    return {
        backing,
    };
}

/** Return one JSON value for one NewtypeInfo. */
export function toJsonNewtypeInfo(value: NewtypeInfo): Json {
    return {
        backing: toJsonTypeId(value.backing),
    };
}

/** Return one NewtypeInfo from one JSON value. */
export function fromJsonNewtypeInfo(value: Json): NewtypeInfo {
    const object = jsonObject(value);

    return {
        backing: fromJsonTypeId(jsonField(object, "backing")),
    };
}

/** Reflected struct declaration type. */
export type StructInfo = {
    /** Struct fields. */
    readonly fields: ReadonlyArray<TypeFieldInfo>;
};

export const StructInfo = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: StructInfo): void {
        encodeStructInfo(writer, value);
    },

    /** Decode one StructInfo. */
    decode(reader: BinaryReader): StructInfo {
        return decodeStructInfo(reader);
    },

    /** Return this value as JSON. */
    toJson(value: StructInfo): Json {
        return toJsonStructInfo(value);
    },

    /** Return one StructInfo from one JSON value. */
    fromJson(value: Json): StructInfo {
        return fromJsonStructInfo(value);
    },
};

/** Encode one StructInfo. */
export function encodeStructInfo(writer: BinaryWriter, value: StructInfo): void {
    writer.writeUnsigned(value.fields.length);
    for (const item0 of value.fields) {
        encodeTypeFieldInfo(writer, item0);
    }
}

/** Decode one StructInfo. */
export function decodeStructInfo(reader: BinaryReader): StructInfo {
    const fields = (() => { const length0 = reader.readNumber(); const items0: Array<TypeFieldInfo> = []; for (let index = 0; index < length0; index += 1) { items0.push(decodeTypeFieldInfo(reader)); } return items0; })();

    return {
        fields,
    };
}

/** Return one JSON value for one StructInfo. */
export function toJsonStructInfo(value: StructInfo): Json {
    return {
        fields: value.fields.map((item0) => toJsonTypeFieldInfo(item0)),
    };
}

/** Return one StructInfo from one JSON value. */
export function fromJsonStructInfo(value: Json): StructInfo {
    const object = jsonObject(value);

    return {
        fields: jsonArray(jsonField(object, "fields")).map((item0) => fromJsonTypeFieldInfo(item0)),
    };
}

/** Reflected class declaration type. */
export type ClassInfo = {
    /** Class fields. */
    readonly fields: ReadonlyArray<TypeFieldInfo>;
    /** Class members. */
    readonly members: ReadonlyArray<MemberInfo>;
};

export const ClassInfo = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ClassInfo): void {
        encodeClassInfo(writer, value);
    },

    /** Decode one ClassInfo. */
    decode(reader: BinaryReader): ClassInfo {
        return decodeClassInfo(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ClassInfo): Json {
        return toJsonClassInfo(value);
    },

    /** Return one ClassInfo from one JSON value. */
    fromJson(value: Json): ClassInfo {
        return fromJsonClassInfo(value);
    },
};

/** Encode one ClassInfo. */
export function encodeClassInfo(writer: BinaryWriter, value: ClassInfo): void {
    writer.writeUnsigned(value.fields.length);
    for (const item0 of value.fields) {
        encodeTypeFieldInfo(writer, item0);
    }
    writer.writeUnsigned(value.members.length);
    for (const item1 of value.members) {
        encodeMemberInfo(writer, item1);
    }
}

/** Decode one ClassInfo. */
export function decodeClassInfo(reader: BinaryReader): ClassInfo {
    const fields = (() => { const length0 = reader.readNumber(); const items0: Array<TypeFieldInfo> = []; for (let index = 0; index < length0; index += 1) { items0.push(decodeTypeFieldInfo(reader)); } return items0; })();
    const members = (() => { const length1 = reader.readNumber(); const items1: Array<MemberInfo> = []; for (let index = 0; index < length1; index += 1) { items1.push(decodeMemberInfo(reader)); } return items1; })();

    return {
        fields,
        members,
    };
}

/** Return one JSON value for one ClassInfo. */
export function toJsonClassInfo(value: ClassInfo): Json {
    return {
        fields: value.fields.map((item0) => toJsonTypeFieldInfo(item0)),
        members: value.members.map((item0) => toJsonMemberInfo(item0)),
    };
}

/** Return one ClassInfo from one JSON value. */
export function fromJsonClassInfo(value: Json): ClassInfo {
    const object = jsonObject(value);

    return {
        fields: jsonArray(jsonField(object, "fields")).map((item0) => fromJsonTypeFieldInfo(item0)),
        members: jsonArray(jsonField(object, "members")).map((item0) => fromJsonMemberInfo(item0)),
    };
}

/** Reflected interface declaration type. */
export type InterfaceInfo = {
    /** Whether the interface is nominal. */
    readonly isNominal: boolean;
    /** Interface members. */
    readonly members: ReadonlyArray<MemberInfo>;
};

export const InterfaceInfo = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: InterfaceInfo): void {
        encodeInterfaceInfo(writer, value);
    },

    /** Decode one InterfaceInfo. */
    decode(reader: BinaryReader): InterfaceInfo {
        return decodeInterfaceInfo(reader);
    },

    /** Return this value as JSON. */
    toJson(value: InterfaceInfo): Json {
        return toJsonInterfaceInfo(value);
    },

    /** Return one InterfaceInfo from one JSON value. */
    fromJson(value: Json): InterfaceInfo {
        return fromJsonInterfaceInfo(value);
    },
};

/** Encode one InterfaceInfo. */
export function encodeInterfaceInfo(writer: BinaryWriter, value: InterfaceInfo): void {
    writer.writeBool(value.isNominal);
    writer.writeUnsigned(value.members.length);
    for (const item1 of value.members) {
        encodeMemberInfo(writer, item1);
    }
}

/** Decode one InterfaceInfo. */
export function decodeInterfaceInfo(reader: BinaryReader): InterfaceInfo {
    const isNominal = reader.readBool();
    const members = (() => { const length1 = reader.readNumber(); const items1: Array<MemberInfo> = []; for (let index = 0; index < length1; index += 1) { items1.push(decodeMemberInfo(reader)); } return items1; })();

    return {
        isNominal,
        members,
    };
}

/** Return one JSON value for one InterfaceInfo. */
export function toJsonInterfaceInfo(value: InterfaceInfo): Json {
    return {
        isNominal: value.isNominal,
        members: value.members.map((item0) => toJsonMemberInfo(item0)),
    };
}

/** Return one InterfaceInfo from one JSON value. */
export function fromJsonInterfaceInfo(value: Json): InterfaceInfo {
    const object = jsonObject(value);

    return {
        isNominal: jsonBool(jsonField(object, "isNominal")),
        members: jsonArray(jsonField(object, "members")).map((item0) => fromJsonMemberInfo(item0)),
    };
}

/** Reflected variant declaration type. */
export type VariantInfo = {
    /** Variant cases. */
    readonly cases: ReadonlyArray<VariantCaseInfo>;
};

export const VariantInfo = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: VariantInfo): void {
        encodeVariantInfo(writer, value);
    },

    /** Decode one VariantInfo. */
    decode(reader: BinaryReader): VariantInfo {
        return decodeVariantInfo(reader);
    },

    /** Return this value as JSON. */
    toJson(value: VariantInfo): Json {
        return toJsonVariantInfo(value);
    },

    /** Return one VariantInfo from one JSON value. */
    fromJson(value: Json): VariantInfo {
        return fromJsonVariantInfo(value);
    },
};

/** Encode one VariantInfo. */
export function encodeVariantInfo(writer: BinaryWriter, value: VariantInfo): void {
    writer.writeUnsigned(value.cases.length);
    for (const item0 of value.cases) {
        encodeVariantCaseInfo(writer, item0);
    }
}

/** Decode one VariantInfo. */
export function decodeVariantInfo(reader: BinaryReader): VariantInfo {
    const cases = (() => { const length0 = reader.readNumber(); const items0: Array<VariantCaseInfo> = []; for (let index = 0; index < length0; index += 1) { items0.push(decodeVariantCaseInfo(reader)); } return items0; })();

    return {
        cases,
    };
}

/** Return one JSON value for one VariantInfo. */
export function toJsonVariantInfo(value: VariantInfo): Json {
    return {
        cases: value.cases.map((item0) => toJsonVariantCaseInfo(item0)),
    };
}

/** Return one VariantInfo from one JSON value. */
export function fromJsonVariantInfo(value: Json): VariantInfo {
    const object = jsonObject(value);

    return {
        cases: jsonArray(jsonField(object, "cases")).map((item0) => fromJsonVariantCaseInfo(item0)),
    };
}

/** Reflected variant case. */
export type VariantCaseInfo = {
    /** Case name when one exists. */
    readonly name?: StringId;
    /** Case payload type. */
    readonly ty: TypeId;
};

export const VariantCaseInfo = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: VariantCaseInfo): void {
        encodeVariantCaseInfo(writer, value);
    },

    /** Decode one VariantCaseInfo. */
    decode(reader: BinaryReader): VariantCaseInfo {
        return decodeVariantCaseInfo(reader);
    },

    /** Return this value as JSON. */
    toJson(value: VariantCaseInfo): Json {
        return toJsonVariantCaseInfo(value);
    },

    /** Return one VariantCaseInfo from one JSON value. */
    fromJson(value: Json): VariantCaseInfo {
        return fromJsonVariantCaseInfo(value);
    },
};

/** Encode one VariantCaseInfo. */
export function encodeVariantCaseInfo(writer: BinaryWriter, value: VariantCaseInfo): void {
    writer.writeOption(value.name, (value0) => {
        encodeStringId(writer, value0);
    });
    encodeTypeId(writer, value.ty);
}

/** Decode one VariantCaseInfo. */
export function decodeVariantCaseInfo(reader: BinaryReader): VariantCaseInfo {
    const name = reader.readOption(() => decodeStringId(reader));
    const ty = decodeTypeId(reader);

    return {
        ...(name === undefined ? {} : { name }),
        ty,
    };
}

/** Return one JSON value for one VariantCaseInfo. */
export function toJsonVariantCaseInfo(value: VariantCaseInfo): Json {
    return {
        ...(value.name === undefined ? {} : { name: toJsonStringId(value.name) }),
        ty: toJsonTypeId(value.ty),
    };
}

/** Return one VariantCaseInfo from one JSON value. */
export function fromJsonVariantCaseInfo(value: Json): VariantCaseInfo {
    const object = jsonObject(value);

    return {
        name: jsonOptional(object, "name", (value) => fromJsonStringId(value)),
        ty: fromJsonTypeId(jsonField(object, "ty")),
    };
}

/** Reflected executable function. */
export type FunctionInfo = {
    /** Function display name when one exists. */
    readonly name?: StringId;
    /** Source module containing the function when one exists. */
    readonly module?: ModuleId;
    /** Function signature type. */
    readonly signature: TypeId;
    /** Captured environment type when one exists. */
    readonly environment?: TypeId;
};

export const FunctionInfo = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: FunctionInfo): void {
        encodeFunctionInfo(writer, value);
    },

    /** Decode one FunctionInfo. */
    decode(reader: BinaryReader): FunctionInfo {
        return decodeFunctionInfo(reader);
    },

    /** Return this value as JSON. */
    toJson(value: FunctionInfo): Json {
        return toJsonFunctionInfo(value);
    },

    /** Return one FunctionInfo from one JSON value. */
    fromJson(value: Json): FunctionInfo {
        return fromJsonFunctionInfo(value);
    },
};

/** Encode one FunctionInfo. */
export function encodeFunctionInfo(writer: BinaryWriter, value: FunctionInfo): void {
    writer.writeOption(value.name, (value0) => {
        encodeStringId(writer, value0);
    });
    writer.writeOption(value.module, (value1) => {
        encodeModuleId(writer, value1);
    });
    encodeTypeId(writer, value.signature);
    writer.writeOption(value.environment, (value3) => {
        encodeTypeId(writer, value3);
    });
}

/** Decode one FunctionInfo. */
export function decodeFunctionInfo(reader: BinaryReader): FunctionInfo {
    const name = reader.readOption(() => decodeStringId(reader));
    const module_ = reader.readOption(() => decodeModuleId(reader));
    const signature = decodeTypeId(reader);
    const environment = reader.readOption(() => decodeTypeId(reader));

    return {
        ...(name === undefined ? {} : { name }),
        ...(module_ === undefined ? {} : { module: module_ }),
        signature,
        ...(environment === undefined ? {} : { environment }),
    };
}

/** Return one JSON value for one FunctionInfo. */
export function toJsonFunctionInfo(value: FunctionInfo): Json {
    return {
        ...(value.name === undefined ? {} : { name: toJsonStringId(value.name) }),
        ...(value.module === undefined ? {} : { module: toJsonModuleId(value.module) }),
        signature: toJsonTypeId(value.signature),
        ...(value.environment === undefined ? {} : { environment: toJsonTypeId(value.environment) }),
    };
}

/** Return one FunctionInfo from one JSON value. */
export function fromJsonFunctionInfo(value: Json): FunctionInfo {
    const object = jsonObject(value);

    return {
        name: jsonOptional(object, "name", (value) => fromJsonStringId(value)),
        module: jsonOptional(object, "module", (value) => fromJsonModuleId(value)),
        signature: fromJsonTypeId(jsonField(object, "signature")),
        environment: jsonOptional(object, "environment", (value) => fromJsonTypeId(value)),
    };
}

/** Reflected executable frame layout. */
export type FrameInfo = {
    /** Function owning this frame. */
    readonly function: FunctionId;
    /** Frame slots. */
    readonly slots: ReadonlyArray<FrameSlotInfo>;
};

export const FrameInfo = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: FrameInfo): void {
        encodeFrameInfo(writer, value);
    },

    /** Decode one FrameInfo. */
    decode(reader: BinaryReader): FrameInfo {
        return decodeFrameInfo(reader);
    },

    /** Return this value as JSON. */
    toJson(value: FrameInfo): Json {
        return toJsonFrameInfo(value);
    },

    /** Return one FrameInfo from one JSON value. */
    fromJson(value: Json): FrameInfo {
        return fromJsonFrameInfo(value);
    },
};

/** Encode one FrameInfo. */
export function encodeFrameInfo(writer: BinaryWriter, value: FrameInfo): void {
    encodeFunctionId(writer, value.function);
    writer.writeUnsigned(value.slots.length);
    for (const item1 of value.slots) {
        encodeFrameSlotInfo(writer, item1);
    }
}

/** Decode one FrameInfo. */
export function decodeFrameInfo(reader: BinaryReader): FrameInfo {
    const function_ = decodeFunctionId(reader);
    const slots = (() => { const length1 = reader.readNumber(); const items1: Array<FrameSlotInfo> = []; for (let index = 0; index < length1; index += 1) { items1.push(decodeFrameSlotInfo(reader)); } return items1; })();

    return {
        function: function_,
        slots,
    };
}

/** Return one JSON value for one FrameInfo. */
export function toJsonFrameInfo(value: FrameInfo): Json {
    return {
        function: toJsonFunctionId(value.function),
        slots: value.slots.map((item0) => toJsonFrameSlotInfo(item0)),
    };
}

/** Return one FrameInfo from one JSON value. */
export function fromJsonFrameInfo(value: Json): FrameInfo {
    const object = jsonObject(value);

    return {
        function: fromJsonFunctionId(jsonField(object, "function")),
        slots: jsonArray(jsonField(object, "slots")).map((item0) => fromJsonFrameSlotInfo(item0)),
    };
}

/** Reflected executable frame slot. */
export type FrameSlotInfo = {
    /** Slot type. */
    readonly ty: TypeId;
    /** Slot byte offset. */
    readonly offset: number;
    /** Slot byte length. */
    readonly byteLen: number;
};

export const FrameSlotInfo = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: FrameSlotInfo): void {
        encodeFrameSlotInfo(writer, value);
    },

    /** Decode one FrameSlotInfo. */
    decode(reader: BinaryReader): FrameSlotInfo {
        return decodeFrameSlotInfo(reader);
    },

    /** Return this value as JSON. */
    toJson(value: FrameSlotInfo): Json {
        return toJsonFrameSlotInfo(value);
    },

    /** Return one FrameSlotInfo from one JSON value. */
    fromJson(value: Json): FrameSlotInfo {
        return fromJsonFrameSlotInfo(value);
    },
};

/** Encode one FrameSlotInfo. */
export function encodeFrameSlotInfo(writer: BinaryWriter, value: FrameSlotInfo): void {
    encodeTypeId(writer, value.ty);
    writer.writeUnsigned(value.offset);
    writer.writeUnsigned(value.byteLen);
}

/** Decode one FrameSlotInfo. */
export function decodeFrameSlotInfo(reader: BinaryReader): FrameSlotInfo {
    const ty = decodeTypeId(reader);
    const offset = reader.readNumber();
    const byteLen = reader.readNumber();

    return {
        ty,
        offset,
        byteLen,
    };
}

/** Return one JSON value for one FrameSlotInfo. */
export function toJsonFrameSlotInfo(value: FrameSlotInfo): Json {
    return {
        ty: toJsonTypeId(value.ty),
        offset: value.offset,
        byteLen: value.byteLen,
    };
}

/** Return one FrameSlotInfo from one JSON value. */
export function fromJsonFrameSlotInfo(value: Json): FrameSlotInfo {
    const object = jsonObject(value);

    return {
        ty: fromJsonTypeId(jsonField(object, "ty")),
        offset: jsonInteger(jsonField(object, "offset")),
        byteLen: jsonInteger(jsonField(object, "byteLen")),
    };
}

/** Reflected global in one executable program. */
export type GlobalInfo = {
    /** Global display name when one exists. */
    readonly name?: StringId;
    /** Global type. */
    readonly ty: TypeId;
};

export const GlobalInfo = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: GlobalInfo): void {
        encodeGlobalInfo(writer, value);
    },

    /** Decode one GlobalInfo. */
    decode(reader: BinaryReader): GlobalInfo {
        return decodeGlobalInfo(reader);
    },

    /** Return this value as JSON. */
    toJson(value: GlobalInfo): Json {
        return toJsonGlobalInfo(value);
    },

    /** Return one GlobalInfo from one JSON value. */
    fromJson(value: Json): GlobalInfo {
        return fromJsonGlobalInfo(value);
    },
};

/** Encode one GlobalInfo. */
export function encodeGlobalInfo(writer: BinaryWriter, value: GlobalInfo): void {
    writer.writeOption(value.name, (value0) => {
        encodeStringId(writer, value0);
    });
    encodeTypeId(writer, value.ty);
}

/** Decode one GlobalInfo. */
export function decodeGlobalInfo(reader: BinaryReader): GlobalInfo {
    const name = reader.readOption(() => decodeStringId(reader));
    const ty = decodeTypeId(reader);

    return {
        ...(name === undefined ? {} : { name }),
        ty,
    };
}

/** Return one JSON value for one GlobalInfo. */
export function toJsonGlobalInfo(value: GlobalInfo): Json {
    return {
        ...(value.name === undefined ? {} : { name: toJsonStringId(value.name) }),
        ty: toJsonTypeId(value.ty),
    };
}

/** Return one GlobalInfo from one JSON value. */
export function fromJsonGlobalInfo(value: Json): GlobalInfo {
    const object = jsonObject(value);

    return {
        name: jsonOptional(object, "name", (value) => fromJsonStringId(value)),
        ty: fromJsonTypeId(jsonField(object, "ty")),
    };
}

/** Reflected entry point. */
export type EntryInfo = {
    /** Entry point. */
    readonly entry: EntryPoint;
};

export const EntryInfo = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: EntryInfo): void {
        encodeEntryInfo(writer, value);
    },

    /** Decode one EntryInfo. */
    decode(reader: BinaryReader): EntryInfo {
        return decodeEntryInfo(reader);
    },

    /** Return this value as JSON. */
    toJson(value: EntryInfo): Json {
        return toJsonEntryInfo(value);
    },

    /** Return one EntryInfo from one JSON value. */
    fromJson(value: Json): EntryInfo {
        return fromJsonEntryInfo(value);
    },
};

/** Encode one EntryInfo. */
export function encodeEntryInfo(writer: BinaryWriter, value: EntryInfo): void {
    encodeEntryPoint(writer, value.entry);
}

/** Decode one EntryInfo. */
export function decodeEntryInfo(reader: BinaryReader): EntryInfo {
    const entry = decodeEntryPoint(reader);

    return {
        entry,
    };
}

/** Return one JSON value for one EntryInfo. */
export function toJsonEntryInfo(value: EntryInfo): Json {
    return {
        entry: toJsonEntryPoint(value.entry),
    };
}

/** Return one EntryInfo from one JSON value. */
export function fromJsonEntryInfo(value: Json): EntryInfo {
    const object = jsonObject(value);

    return {
        entry: fromJsonEntryPoint(jsonField(object, "entry")),
    };
}
