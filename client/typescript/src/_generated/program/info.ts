/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, jsonBigint, jsonField, jsonObject } from "../../protocol/serde.js";

/** Section-safe source module id. */
export type ModuleId = {
    /** Stable source package id. */
    readonly package: bigint;
    /** Stable source module key. */
    readonly module: bigint;
};

export const ModuleId = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ModuleId): void {
        encodeModuleId(writer, value);
    },

    /** Decode one ModuleId. */
    decode(reader: BinaryReader): ModuleId {
        return decodeModuleId(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ModuleId): Json {
        return toJsonModuleId(value);
    },

    /** Return one ModuleId from one JSON value. */
    fromJson(value: Json): ModuleId {
        return fromJsonModuleId(value);
    },
};

/** Encode one ModuleId. */
export function encodeModuleId(writer: BinaryWriter, value: ModuleId): void {
    writer.writeUnsigned(value.package);
    writer.writeUnsigned(value.module);
}

/** Decode one ModuleId. */
export function decodeModuleId(reader: BinaryReader): ModuleId {
    const package_ = reader.readUnsigned();
    const module_ = reader.readUnsigned();

    return {
        package: package_,
        module: module_,
    };
}

/** Return one JSON value for one ModuleId. */
export function toJsonModuleId(value: ModuleId): Json {
    return {
        package: value.package.toString(),
        module: value.module.toString(),
    };
}

/** Return one ModuleId from one JSON value. */
export function fromJsonModuleId(value: Json): ModuleId {
    const object = jsonObject(value);

    return {
        package: jsonBigint(jsonField(object, "package")),
        module: jsonBigint(jsonField(object, "module")),
    };
}
