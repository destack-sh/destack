/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, jsonBigint, jsonField, jsonObject, jsonString } from "../../protocol/serde.js";
import type { GlobalId } from "./memory/region.js";
import type { GlobalLocation } from "./memory/region.js";
import type { ProgramPoint } from "./point.js";
import type { TypeId } from "./type.js";
import { decodeGlobalId, encodeGlobalId, fromJsonGlobalId, toJsonGlobalId } from "./memory/region.js";
import { decodeGlobalLocation, encodeGlobalLocation, fromJsonGlobalLocation, toJsonGlobalLocation } from "./memory/region.js";
import { decodeProgramPoint, encodeProgramPoint, fromJsonProgramPoint, toJsonProgramPoint } from "./point.js";
import { decodeTypeId, encodeTypeId, fromJsonTypeId, toJsonTypeId } from "./type.js";

/** Contiguous byte range inside one memory owner. */
export type ByteRange = {
    /** Owner-relative byte start. */
    readonly start: bigint;
    /** Byte length. */
    readonly byteLen: bigint;
};

export const ByteRange = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ByteRange): void {
        encodeByteRange(writer, value);
    },

    /** Decode one ByteRange. */
    decode(reader: BinaryReader): ByteRange {
        return decodeByteRange(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ByteRange): Json {
        return toJsonByteRange(value);
    },

    /** Return one ByteRange from one JSON value. */
    fromJson(value: Json): ByteRange {
        return fromJsonByteRange(value);
    },
};

/** Encode one ByteRange. */
export function encodeByteRange(writer: BinaryWriter, value: ByteRange): void {
    writer.writeUnsigned(value.start);
    writer.writeUnsigned(value.byteLen);
}

/** Decode one ByteRange. */
export function decodeByteRange(reader: BinaryReader): ByteRange {
    const start = reader.readUnsigned();
    const byteLen = reader.readUnsigned();

    return {
        start,
        byteLen,
    };
}

/** Return one JSON value for one ByteRange. */
export function toJsonByteRange(value: ByteRange): Json {
    return {
        start: value.start.toString(),
        byteLen: value.byteLen.toString(),
    };
}

/** Return one ByteRange from one JSON value. */
export function fromJsonByteRange(value: Json): ByteRange {
    const object = jsonObject(value);

    return {
        start: jsonBigint(jsonField(object, "start")),
        byteLen: jsonBigint(jsonField(object, "byteLen")),
    };
}

/** Contiguous byte range inside one program global. */
export type GlobalRange = {
    /** Addressed program global. */
    readonly global: GlobalId;
    /** Static storage location containing the global. */
    readonly location: GlobalLocation;
    /** Byte range inside the global. */
    readonly range: ByteRange;
};

export const GlobalRange = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: GlobalRange): void {
        encodeGlobalRange(writer, value);
    },

    /** Decode one GlobalRange. */
    decode(reader: BinaryReader): GlobalRange {
        return decodeGlobalRange(reader);
    },

    /** Return this value as JSON. */
    toJson(value: GlobalRange): Json {
        return toJsonGlobalRange(value);
    },

    /** Return one GlobalRange from one JSON value. */
    fromJson(value: Json): GlobalRange {
        return fromJsonGlobalRange(value);
    },
};

/** Encode one GlobalRange. */
export function encodeGlobalRange(writer: BinaryWriter, value: GlobalRange): void {
    encodeGlobalId(writer, value.global);
    encodeGlobalLocation(writer, value.location);
    encodeByteRange(writer, value.range);
}

/** Decode one GlobalRange. */
export function decodeGlobalRange(reader: BinaryReader): GlobalRange {
    const global = decodeGlobalId(reader);
    const location = decodeGlobalLocation(reader);
    const range = decodeByteRange(reader);

    return {
        global,
        location,
        range,
    };
}

/** Return one JSON value for one GlobalRange. */
export function toJsonGlobalRange(value: GlobalRange): Json {
    return {
        global: toJsonGlobalId(value.global),
        location: toJsonGlobalLocation(value.location),
        range: toJsonByteRange(value.range),
    };
}

/** Return one GlobalRange from one JSON value. */
export function fromJsonGlobalRange(value: Json): GlobalRange {
    const object = jsonObject(value);

    return {
        global: fromJsonGlobalId(jsonField(object, "global")),
        location: fromJsonGlobalLocation(jsonField(object, "location")),
        range: fromJsonByteRange(jsonField(object, "range")),
    };
}

/** Executed memory byte range. */
export type MemoryRange =
    /** Local heap storage range. */
    | {
          readonly kind: "localHeap";
          readonly local_heap: ByteRange;
      }
    /** Shared heap storage range. */
    | {
          readonly kind: "sharedHeap";
          readonly shared_heap: ByteRange;
      }
    /** Native address range. */
    | {
          readonly kind: "address";
          readonly address: ByteRange;
      }
    /** Frame value range. */
    | {
          readonly kind: "frame";
          readonly frame: ByteRange;
      }
    /** Program global range. */
    | {
          readonly kind: "global";
          readonly global: GlobalRange;
      }
;

export const MemoryRange = {
    /** Local heap storage range. */
    localHeap(local_heap: ByteRange): MemoryRange {
        return { kind: "localHeap", local_heap };
    },

    /** Shared heap storage range. */
    sharedHeap(shared_heap: ByteRange): MemoryRange {
        return { kind: "sharedHeap", shared_heap };
    },

    /** Native address range. */
    address(address: ByteRange): MemoryRange {
        return { kind: "address", address };
    },

    /** Frame value range. */
    frame(frame: ByteRange): MemoryRange {
        return { kind: "frame", frame };
    },

    /** Program global range. */
    global(global: GlobalRange): MemoryRange {
        return { kind: "global", global };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: MemoryRange): void {
        encodeMemoryRange(writer, value);
    },

    /** Decode one MemoryRange. */
    decode(reader: BinaryReader): MemoryRange {
        return decodeMemoryRange(reader);
    },

    /** Return this value as JSON. */
    toJson(value: MemoryRange): Json {
        return toJsonMemoryRange(value);
    },

    /** Return one MemoryRange from one JSON value. */
    fromJson(value: Json): MemoryRange {
        return fromJsonMemoryRange(value);
    },
};

/** Encode one MemoryRange. */
export function encodeMemoryRange(writer: BinaryWriter, value: MemoryRange): void {
    switch (value.kind) {
        case "localHeap":
            writer.writeUnsigned(0);
            encodeByteRange(writer, value.local_heap);
            return;
        case "sharedHeap":
            writer.writeUnsigned(1);
            encodeByteRange(writer, value.shared_heap);
            return;
        case "address":
            writer.writeUnsigned(2);
            encodeByteRange(writer, value.address);
            return;
        case "frame":
            writer.writeUnsigned(3);
            encodeByteRange(writer, value.frame);
            return;
        case "global":
            writer.writeUnsigned(4);
            encodeGlobalRange(writer, value.global);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one MemoryRange. */
export function decodeMemoryRange(reader: BinaryReader): MemoryRange {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const local_heap = decodeByteRange(reader);

            return { kind: "localHeap", local_heap };
        }
        case 1: {
            const shared_heap = decodeByteRange(reader);

            return { kind: "sharedHeap", shared_heap };
        }
        case 2: {
            const address = decodeByteRange(reader);

            return { kind: "address", address };
        }
        case 3: {
            const frame = decodeByteRange(reader);

            return { kind: "frame", frame };
        }
        case 4: {
            const global = decodeGlobalRange(reader);

            return { kind: "global", global };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one MemoryRange. */
export function toJsonMemoryRange(value: MemoryRange): Json {
    switch (value.kind) {
        case "localHeap":
            return {
                kind: "localHeap",
                local_heap: toJsonByteRange(value.local_heap),
            };
        case "sharedHeap":
            return {
                kind: "sharedHeap",
                shared_heap: toJsonByteRange(value.shared_heap),
            };
        case "address":
            return {
                kind: "address",
                address: toJsonByteRange(value.address),
            };
        case "frame":
            return {
                kind: "frame",
                frame: toJsonByteRange(value.frame),
            };
        case "global":
            return {
                kind: "global",
                global: toJsonGlobalRange(value.global),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one MemoryRange from one JSON value. */
export function fromJsonMemoryRange(value: Json): MemoryRange {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "localHeap":
            return {
                kind,
                local_heap: fromJsonByteRange(jsonField(object, "local_heap")),
            };
        case "sharedHeap":
            return {
                kind,
                shared_heap: fromJsonByteRange(jsonField(object, "shared_heap")),
            };
        case "address":
            return {
                kind,
                address: fromJsonByteRange(jsonField(object, "address")),
            };
        case "frame":
            return {
                kind,
                frame: fromJsonByteRange(jsonField(object, "frame")),
            };
        case "global":
            return {
                kind,
                global: fromJsonGlobalRange(jsonField(object, "global")),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** Memory target selected by one watchpoint or probe. */
export type MemoryTarget =
    /** Any memory target. */
    | {
          readonly kind: "any";
      }
    /** Program memory operation point. */
    | {
          readonly kind: "point";
          readonly point: ProgramPoint;
      }
    /** Program type. */
    | {
          readonly kind: "type";
          readonly type: TypeId;
      }
    /** Executed memory byte range. */
    | {
          readonly kind: "range";
          readonly range: MemoryRange;
      }
;

export const MemoryTarget = {
    /** Any memory target. */
    "any"(): MemoryTarget {
        return { kind: "any" };
    },

    /** Program memory operation point. */
    point(point: ProgramPoint): MemoryTarget {
        return { kind: "point", point };
    },

    /** Program type. */
    "type"(type_: TypeId): MemoryTarget {
        return { kind: "type", type: type_ };
    },

    /** Executed memory byte range. */
    range(range: MemoryRange): MemoryTarget {
        return { kind: "range", range };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: MemoryTarget): void {
        encodeMemoryTarget(writer, value);
    },

    /** Decode one MemoryTarget. */
    decode(reader: BinaryReader): MemoryTarget {
        return decodeMemoryTarget(reader);
    },

    /** Return this value as JSON. */
    toJson(value: MemoryTarget): Json {
        return toJsonMemoryTarget(value);
    },

    /** Return one MemoryTarget from one JSON value. */
    fromJson(value: Json): MemoryTarget {
        return fromJsonMemoryTarget(value);
    },
};

/** Encode one MemoryTarget. */
export function encodeMemoryTarget(writer: BinaryWriter, value: MemoryTarget): void {
    switch (value.kind) {
        case "any":
            writer.writeUnsigned(0);
            return;
        case "point":
            writer.writeUnsigned(1);
            encodeProgramPoint(writer, value.point);
            return;
        case "type":
            writer.writeUnsigned(2);
            encodeTypeId(writer, value.type);
            return;
        case "range":
            writer.writeUnsigned(3);
            encodeMemoryRange(writer, value.range);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one MemoryTarget. */
export function decodeMemoryTarget(reader: BinaryReader): MemoryTarget {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            return { kind: "any" };
        }
        case 1: {
            const point = decodeProgramPoint(reader);

            return { kind: "point", point };
        }
        case 2: {
            const type_ = decodeTypeId(reader);

            return { kind: "type", type: type_ };
        }
        case 3: {
            const range = decodeMemoryRange(reader);

            return { kind: "range", range };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one MemoryTarget. */
export function toJsonMemoryTarget(value: MemoryTarget): Json {
    switch (value.kind) {
        case "any":
            return {
                kind: "any",
            };
        case "point":
            return {
                kind: "point",
                point: toJsonProgramPoint(value.point),
            };
        case "type":
            return {
                kind: "type",
                type: toJsonTypeId(value.type),
            };
        case "range":
            return {
                kind: "range",
                range: toJsonMemoryRange(value.range),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one MemoryTarget from one JSON value. */
export function fromJsonMemoryTarget(value: Json): MemoryTarget {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "any":
            return {
                kind,
            };
        case "point":
            return {
                kind,
                point: fromJsonProgramPoint(jsonField(object, "point")),
            };
        case "type":
            return {
                kind,
                type: fromJsonTypeId(jsonField(object, "type")),
            };
        case "range":
            return {
                kind,
                range: fromJsonMemoryRange(jsonField(object, "range")),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** Runtime watchpoint identifier. */
export type WatchpointId = bigint;

export const WatchpointId = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: WatchpointId): void {
        encodeWatchpointId(writer, value);
    },

    /** Decode one WatchpointId. */
    decode(reader: BinaryReader): WatchpointId {
        return decodeWatchpointId(reader);
    },

    /** Return this value as JSON. */
    toJson(value: WatchpointId): Json {
        return toJsonWatchpointId(value);
    },

    /** Return one WatchpointId from one JSON value. */
    fromJson(value: Json): WatchpointId {
        return fromJsonWatchpointId(value);
    },
};

/** Encode one WatchpointId. */
export function encodeWatchpointId(writer: BinaryWriter, value: WatchpointId): void {
    writer.writeUnsigned(value);
}

/** Decode one WatchpointId. */
export function decodeWatchpointId(reader: BinaryReader): WatchpointId {
    return reader.readUnsigned();
}

/** Return one JSON value for one WatchpointId. */
export function toJsonWatchpointId(value: WatchpointId): Json {
    return value.toString();
}

/** Return one WatchpointId from one JSON value. */
export function fromJsonWatchpointId(value: Json): WatchpointId {
    return jsonBigint(value);
}
