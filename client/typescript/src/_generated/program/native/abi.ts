/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, jsonField, jsonInteger, jsonObject, jsonString } from "../../../protocol/serde.js";

/** Native ABI required by one native code payload. */
export type Abi = {
    /** Destack native ABI version. */
    readonly version: number;
    /** Target triple or equivalent target identity. */
    readonly target: string;
    /** Native pointer byte width expected by this code. */
    readonly pointerBytes: number;
};

export const Abi = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: Abi): void {
        encodeAbi(writer, value);
    },

    /** Decode one Abi. */
    decode(reader: BinaryReader): Abi {
        return decodeAbi(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Abi): Json {
        return toJsonAbi(value);
    },

    /** Return one Abi from one JSON value. */
    fromJson(value: Json): Abi {
        return fromJsonAbi(value);
    },
};

/** Encode one Abi. */
export function encodeAbi(writer: BinaryWriter, value: Abi): void {
    writer.writeUnsigned(value.version);
    writer.writeString(value.target);
    writer.writeByte(value.pointerBytes);
}

/** Decode one Abi. */
export function decodeAbi(reader: BinaryReader): Abi {
    const version = reader.readNumber();
    const target = reader.readString();
    const pointerBytes = reader.readByte();

    return {
        version,
        target,
        pointerBytes,
    };
}

/** Return one JSON value for one Abi. */
export function toJsonAbi(value: Abi): Json {
    return {
        version: value.version,
        target: value.target,
        pointerBytes: value.pointerBytes,
    };
}

/** Return one Abi from one JSON value. */
export function fromJsonAbi(value: Json): Abi {
    const object = jsonObject(value);

    return {
        version: jsonInteger(jsonField(object, "version")),
        target: jsonString(jsonField(object, "target")),
        pointerBytes: jsonInteger(jsonField(object, "pointerBytes")),
    };
}
