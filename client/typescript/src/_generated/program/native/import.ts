/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, jsonArray, jsonField, jsonObject, jsonString } from "../../../protocol/serde.js";

/** Native imports required by one native code payload. */
export type ImportTable = {
    /** Native imports in linker order. */
    readonly import: ReadonlyArray<Import>;
};

export const ImportTable = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ImportTable): void {
        encodeImportTable(writer, value);
    },

    /** Decode one ImportTable. */
    decode(reader: BinaryReader): ImportTable {
        return decodeImportTable(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ImportTable): Json {
        return toJsonImportTable(value);
    },

    /** Return one ImportTable from one JSON value. */
    fromJson(value: Json): ImportTable {
        return fromJsonImportTable(value);
    },
};

/** Encode one ImportTable. */
export function encodeImportTable(writer: BinaryWriter, value: ImportTable): void {
    writer.writeUnsigned(value.import.length);
    for (const item0 of value.import) {
        encodeImport(writer, item0);
    }
}

/** Decode one ImportTable. */
export function decodeImportTable(reader: BinaryReader): ImportTable {
    const import_ = (() => { const length0 = reader.readNumber(); const items0: Array<Import> = []; for (let index = 0; index < length0; index += 1) { items0.push(decodeImport(reader)); } return items0; })();

    return {
        import: import_,
    };
}

/** Return one JSON value for one ImportTable. */
export function toJsonImportTable(value: ImportTable): Json {
    return {
        import: value.import.map((item0) => toJsonImport(item0)),
    };
}

/** Return one ImportTable from one JSON value. */
export function fromJsonImportTable(value: Json): ImportTable {
    const object = jsonObject(value);

    return {
        import: jsonArray(jsonField(object, "import")).map((item0) => fromJsonImport(item0)),
    };
}

/** One native import required by generated native code. */
export type Import =
    /** Fixed Destack runtime binding. */
    | {
          readonly kind: "runtime";
          readonly runtime: RuntimeBinding;
      }
    /** External linker-visible symbol. */
    | {
          readonly kind: "symbol";
          readonly symbol: SymbolImport;
      }
;

export const Import = {
    /** Fixed Destack runtime binding. */
    runtime(runtime: RuntimeBinding): Import {
        return { kind: "runtime", runtime };
    },

    /** External linker-visible symbol. */
    "symbol"(symbol_: SymbolImport): Import {
        return { kind: "symbol", symbol: symbol_ };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: Import): void {
        encodeImport(writer, value);
    },

    /** Decode one Import. */
    decode(reader: BinaryReader): Import {
        return decodeImport(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Import): Json {
        return toJsonImport(value);
    },

    /** Return one Import from one JSON value. */
    fromJson(value: Json): Import {
        return fromJsonImport(value);
    },
};

/** Encode one Import. */
export function encodeImport(writer: BinaryWriter, value: Import): void {
    switch (value.kind) {
        case "runtime":
            writer.writeUnsigned(0);
            encodeRuntimeBinding(writer, value.runtime);
            return;
        case "symbol":
            writer.writeUnsigned(1);
            encodeSymbolImport(writer, value.symbol);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one Import. */
export function decodeImport(reader: BinaryReader): Import {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const runtime = decodeRuntimeBinding(reader);

            return { kind: "runtime", runtime };
        }
        case 1: {
            const symbol_ = decodeSymbolImport(reader);

            return { kind: "symbol", symbol: symbol_ };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one Import. */
export function toJsonImport(value: Import): Json {
    switch (value.kind) {
        case "runtime":
            return {
                kind: "runtime",
                runtime: toJsonRuntimeBinding(value.runtime),
            };
        case "symbol":
            return {
                kind: "symbol",
                symbol: toJsonSymbolImport(value.symbol),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one Import from one JSON value. */
export function fromJsonImport(value: Json): Import {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "runtime":
            return {
                kind,
                runtime: fromJsonRuntimeBinding(jsonField(object, "runtime")),
            };
        case "symbol":
            return {
                kind,
                symbol: fromJsonSymbolImport(jsonField(object, "symbol")),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** Fixed Destack runtime ABI binding imported by generated native code. */
export type RuntimeBinding = "new" | "newSlice" | "free" | "pin" | "unpin" | "writeBarrier" | "safepoint" | "yield" | "deopt" | "trap" | "panic" | "unwindResume" | "contextCurrent" | "contextPush" | "contextPop" | "contextGet" | "contextRequire" | "contextFamily" | "bindingCall";

export const RuntimeBinding = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: RuntimeBinding): void {
        encodeRuntimeBinding(writer, value);
    },

    /** Decode one RuntimeBinding. */
    decode(reader: BinaryReader): RuntimeBinding {
        return decodeRuntimeBinding(reader);
    },

    /** Return this value as JSON. */
    toJson(value: RuntimeBinding): Json {
        return toJsonRuntimeBinding(value);
    },

    /** Return one RuntimeBinding from one JSON value. */
    fromJson(value: Json): RuntimeBinding {
        return fromJsonRuntimeBinding(value);
    },
};

/** Encode one RuntimeBinding. */
export function encodeRuntimeBinding(writer: BinaryWriter, value: RuntimeBinding): void {
    switch (value) {
        case "new":
            writer.writeUnsigned(0);
            return;
        case "newSlice":
            writer.writeUnsigned(1);
            return;
        case "free":
            writer.writeUnsigned(2);
            return;
        case "pin":
            writer.writeUnsigned(3);
            return;
        case "unpin":
            writer.writeUnsigned(4);
            return;
        case "writeBarrier":
            writer.writeUnsigned(5);
            return;
        case "safepoint":
            writer.writeUnsigned(6);
            return;
        case "yield":
            writer.writeUnsigned(7);
            return;
        case "deopt":
            writer.writeUnsigned(8);
            return;
        case "trap":
            writer.writeUnsigned(9);
            return;
        case "panic":
            writer.writeUnsigned(10);
            return;
        case "unwindResume":
            writer.writeUnsigned(11);
            return;
        case "contextCurrent":
            writer.writeUnsigned(12);
            return;
        case "contextPush":
            writer.writeUnsigned(13);
            return;
        case "contextPop":
            writer.writeUnsigned(14);
            return;
        case "contextGet":
            writer.writeUnsigned(15);
            return;
        case "contextRequire":
            writer.writeUnsigned(16);
            return;
        case "contextFamily":
            writer.writeUnsigned(17);
            return;
        case "bindingCall":
            writer.writeUnsigned(18);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one RuntimeBinding. */
export function decodeRuntimeBinding(reader: BinaryReader): RuntimeBinding {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "new";
        case 1:
            return "newSlice";
        case 2:
            return "free";
        case 3:
            return "pin";
        case 4:
            return "unpin";
        case 5:
            return "writeBarrier";
        case 6:
            return "safepoint";
        case 7:
            return "yield";
        case 8:
            return "deopt";
        case 9:
            return "trap";
        case 10:
            return "panic";
        case 11:
            return "unwindResume";
        case 12:
            return "contextCurrent";
        case 13:
            return "contextPush";
        case 14:
            return "contextPop";
        case 15:
            return "contextGet";
        case 16:
            return "contextRequire";
        case 17:
            return "contextFamily";
        case 18:
            return "bindingCall";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one RuntimeBinding. */
export function toJsonRuntimeBinding(value: RuntimeBinding): Json {
    return value;
}

/** Return one RuntimeBinding from one JSON value. */
export function fromJsonRuntimeBinding(value: Json): RuntimeBinding {
    const variant = jsonString(value);

    switch (variant) {
        case "new":
            return "new";
        case "newSlice":
            return "newSlice";
        case "free":
            return "free";
        case "pin":
            return "pin";
        case "unpin":
            return "unpin";
        case "writeBarrier":
            return "writeBarrier";
        case "safepoint":
            return "safepoint";
        case "yield":
            return "yield";
        case "deopt":
            return "deopt";
        case "trap":
            return "trap";
        case "panic":
            return "panic";
        case "unwindResume":
            return "unwindResume";
        case "contextCurrent":
            return "contextCurrent";
        case "contextPush":
            return "contextPush";
        case "contextPop":
            return "contextPop";
        case "contextGet":
            return "contextGet";
        case "contextRequire":
            return "contextRequire";
        case "contextFamily":
            return "contextFamily";
        case "bindingCall":
            return "bindingCall";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** External linker-visible symbol import. */
export type SymbolImport = {
    /** The imported native symbol. */
    readonly symbol: string;
};

export const SymbolImport = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: SymbolImport): void {
        encodeSymbolImport(writer, value);
    },

    /** Decode one SymbolImport. */
    decode(reader: BinaryReader): SymbolImport {
        return decodeSymbolImport(reader);
    },

    /** Return this value as JSON. */
    toJson(value: SymbolImport): Json {
        return toJsonSymbolImport(value);
    },

    /** Return one SymbolImport from one JSON value. */
    fromJson(value: Json): SymbolImport {
        return fromJsonSymbolImport(value);
    },
};

/** Encode one SymbolImport. */
export function encodeSymbolImport(writer: BinaryWriter, value: SymbolImport): void {
    writer.writeString(value.symbol);
}

/** Decode one SymbolImport. */
export function decodeSymbolImport(reader: BinaryReader): SymbolImport {
    const symbol_ = reader.readString();

    return {
        symbol: symbol_,
    };
}

/** Return one JSON value for one SymbolImport. */
export function toJsonSymbolImport(value: SymbolImport): Json {
    return {
        symbol: value.symbol,
    };
}

/** Return one SymbolImport from one JSON value. */
export function fromJsonSymbolImport(value: Json): SymbolImport {
    const object = jsonObject(value);

    return {
        symbol: jsonString(jsonField(object, "symbol")),
    };
}
