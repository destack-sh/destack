/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, jsonField, jsonObject, jsonOptional, jsonString } from "../../../../protocol/serde.js";
import type { StringId } from "../../../core/string.js";
import type { ContentId } from "../../../source/file/model/file.js";
import { decodeStringId, encodeStringId, fromJsonStringId, toJsonStringId } from "../../../core/string.js";
import { decodeContentId, encodeContentId, fromJsonContentId, toJsonContentId } from "../../../source/file/model/file.js";

/** Loadable native library image. */
export type Library = {
    /** The native library location. */
    readonly source: LibrarySource;
    /** Native unwind tables bytes. */
    readonly unwind?: ContentId;
};

export const Library = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: Library): void {
        encodeLibrary(writer, value);
    },

    /** Decode one Library. */
    decode(reader: BinaryReader): Library {
        return decodeLibrary(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Library): Json {
        return toJsonLibrary(value);
    },

    /** Return one Library from one JSON value. */
    fromJson(value: Json): Library {
        return fromJsonLibrary(value);
    },
};

/** Encode one Library. */
export function encodeLibrary(writer: BinaryWriter, value: Library): void {
    encodeLibrarySource(writer, value.source);
    writer.writeOption(value.unwind, (value1) => {
        encodeContentId(writer, value1);
    });
}

/** Decode one Library. */
export function decodeLibrary(reader: BinaryReader): Library {
    const source = decodeLibrarySource(reader);
    const unwind = reader.readOption(() => decodeContentId(reader));

    return {
        source,
        ...(unwind === undefined ? {} : { unwind }),
    };
}

/** Return one JSON value for one Library. */
export function toJsonLibrary(value: Library): Json {
    return {
        source: toJsonLibrarySource(value.source),
        ...(value.unwind === undefined ? {} : { unwind: toJsonContentId(value.unwind) }),
    };
}

/** Return one Library from one JSON value. */
export function fromJsonLibrary(value: Json): Library {
    const object = jsonObject(value);

    return {
        source: fromJsonLibrarySource(jsonField(object, "source")),
        unwind: jsonOptional(object, "unwind", (value) => fromJsonContentId(value)),
    };
}

/** Native library source. */
export type LibrarySource =
    /** Library is loaded from a process or platform search path. */
    | {
          readonly kind: "name";
          readonly name: StringId;
      }
    /** Library is packaged as a program artifact. */
    | {
          readonly kind: "artifact";
          readonly artifact: ContentId;
      }
;

export const LibrarySource = {
    /** Library is loaded from a process or platform search path. */
    name(name: StringId): LibrarySource {
        return { kind: "name", name };
    },

    /** Library is packaged as a program artifact. */
    artifact(artifact: ContentId): LibrarySource {
        return { kind: "artifact", artifact };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: LibrarySource): void {
        encodeLibrarySource(writer, value);
    },

    /** Decode one LibrarySource. */
    decode(reader: BinaryReader): LibrarySource {
        return decodeLibrarySource(reader);
    },

    /** Return this value as JSON. */
    toJson(value: LibrarySource): Json {
        return toJsonLibrarySource(value);
    },

    /** Return one LibrarySource from one JSON value. */
    fromJson(value: Json): LibrarySource {
        return fromJsonLibrarySource(value);
    },
};

/** Encode one LibrarySource. */
export function encodeLibrarySource(writer: BinaryWriter, value: LibrarySource): void {
    switch (value.kind) {
        case "name":
            writer.writeUnsigned(0);
            encodeStringId(writer, value.name);
            return;
        case "artifact":
            writer.writeUnsigned(1);
            encodeContentId(writer, value.artifact);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one LibrarySource. */
export function decodeLibrarySource(reader: BinaryReader): LibrarySource {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const name = decodeStringId(reader);

            return { kind: "name", name };
        }
        case 1: {
            const artifact = decodeContentId(reader);

            return { kind: "artifact", artifact };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one LibrarySource. */
export function toJsonLibrarySource(value: LibrarySource): Json {
    switch (value.kind) {
        case "name":
            return {
                kind: "name",
                name: toJsonStringId(value.name),
            };
        case "artifact":
            return {
                kind: "artifact",
                artifact: toJsonContentId(value.artifact),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one LibrarySource from one JSON value. */
export function fromJsonLibrarySource(value: Json): LibrarySource {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "name":
            return {
                kind,
                name: fromJsonStringId(jsonField(object, "name")),
            };
        case "artifact":
            return {
                kind,
                artifact: fromJsonContentId(jsonField(object, "artifact")),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}
