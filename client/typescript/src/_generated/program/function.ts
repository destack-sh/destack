/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, compareBytes, jsonArray, jsonField, jsonInteger, jsonObject, jsonOptional, jsonString, nestedBytes } from "../../protocol/serde.js";
import type { TypeId } from "./type.js";
import { decodeTypeId, encodeTypeId, fromJsonTypeId, toJsonTypeId } from "./type.js";

/** Runtime function metadata carried by one durable program. */
export type FunctionTable = {
    /** Dense function records keyed by program function id. */
    readonly functions: ReadonlyArray<Function | undefined>;
    /** Function ids keyed by exported source name. */
    readonly functionByName: Readonly<Record<string, FunctionId>>;
};

export const FunctionTable = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: FunctionTable): void {
        encodeFunctionTable(writer, value);
    },

    /** Decode one FunctionTable. */
    decode(reader: BinaryReader): FunctionTable {
        return decodeFunctionTable(reader);
    },

    /** Return this value as JSON. */
    toJson(value: FunctionTable): Json {
        return toJsonFunctionTable(value);
    },

    /** Return one FunctionTable from one JSON value. */
    fromJson(value: Json): FunctionTable {
        return fromJsonFunctionTable(value);
    },
};

/** Encode one FunctionTable. */
export function encodeFunctionTable(writer: BinaryWriter, value: FunctionTable): void {
    writer.writeUnsigned(value.functions.length);
    for (const item0 of value.functions) {
        writer.writeOption(item0, (value1) => {
            encodeFunction(writer, value1);
        });
    }
    const entries1 = Object.entries(value.functionByName).map(([key1, item1]) => {
        const keyBytes = nestedBytes((writer) => {
            writer.writeString(key1);
        });
        return { key1, item1, keyBytes };
    });
    entries1.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries1.length);
    for (const entry1 of entries1) {
        writer.writeString(entry1.key1);
        encodeFunctionId(writer, entry1.item1);
    }
}

/** Decode one FunctionTable. */
export function decodeFunctionTable(reader: BinaryReader): FunctionTable {
    const functions = (() => { const length0 = reader.readNumber(); const items0: Array<Function | undefined> = []; for (let index = 0; index < length0; index += 1) { items0.push(reader.readOption(() => decodeFunction(reader))); } return items0; })();
    const functionByName = (() => { const length1 = reader.readNumber(); const items1: Record<string, FunctionId> = {}; for (let index = 0; index < length1; index += 1) { const key = reader.readString(); items1[key] = decodeFunctionId(reader); } return items1; })();

    return {
        functions,
        functionByName,
    };
}

/** Return one JSON value for one FunctionTable. */
export function toJsonFunctionTable(value: FunctionTable): Json {
    return {
        functions: value.functions.map((item0) => item0 === undefined ? null : toJsonFunction(item0)),
        functionByName: Object.fromEntries(Object.entries(value.functionByName).map(([key0, item0]) => [key0, toJsonFunctionId(item0)] as const)),
    };
}

/** Return one FunctionTable from one JSON value. */
export function fromJsonFunctionTable(value: Json): FunctionTable {
    const object = jsonObject(value);

    return {
        functions: jsonArray(jsonField(object, "functions")).map((item0) => item0 === null ? undefined : fromJsonFunction(item0)),
        functionByName: Object.fromEntries(Object.entries(jsonObject(jsonField(object, "functionByName"))).map(([key0, item0]) => [key0, fromJsonFunctionId(item0)] as const)),
    };
}

/** Runtime function metadata. */
export type Function = {
    /** The source-facing function name. */
    readonly name: string;
    /** Function parameter types. */
    readonly parameters: ReadonlyArray<TypeId>;
    /** Function return type. */
    readonly returnType: TypeId;
    /** Captured closure environment type when one exists. */
    readonly environment?: TypeId;
};

export const Function = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: Function): void {
        encodeFunction(writer, value);
    },

    /** Decode one Function. */
    decode(reader: BinaryReader): Function {
        return decodeFunction(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Function): Json {
        return toJsonFunction(value);
    },

    /** Return one Function from one JSON value. */
    fromJson(value: Json): Function {
        return fromJsonFunction(value);
    },
};

/** Encode one Function. */
export function encodeFunction(writer: BinaryWriter, value: Function): void {
    writer.writeString(value.name);
    writer.writeUnsigned(value.parameters.length);
    for (const item1 of value.parameters) {
        encodeTypeId(writer, item1);
    }
    encodeTypeId(writer, value.returnType);
    writer.writeOption(value.environment, (value3) => {
        encodeTypeId(writer, value3);
    });
}

/** Decode one Function. */
export function decodeFunction(reader: BinaryReader): Function {
    const name = reader.readString();
    const parameters = (() => { const length1 = reader.readNumber(); const items1: Array<TypeId> = []; for (let index = 0; index < length1; index += 1) { items1.push(decodeTypeId(reader)); } return items1; })();
    const returnType = decodeTypeId(reader);
    const environment = reader.readOption(() => decodeTypeId(reader));

    return {
        name,
        parameters,
        returnType,
        ...(environment === undefined ? {} : { environment }),
    };
}

/** Return one JSON value for one Function. */
export function toJsonFunction(value: Function): Json {
    return {
        name: value.name,
        parameters: value.parameters.map((item0) => toJsonTypeId(item0)),
        returnType: toJsonTypeId(value.returnType),
        ...(value.environment === undefined ? {} : { environment: toJsonTypeId(value.environment) }),
    };
}

/** Return one Function from one JSON value. */
export function fromJsonFunction(value: Json): Function {
    const object = jsonObject(value);

    return {
        name: jsonString(jsonField(object, "name")),
        parameters: jsonArray(jsonField(object, "parameters")).map((item0) => fromJsonTypeId(item0)),
        returnType: fromJsonTypeId(jsonField(object, "returnType")),
        environment: jsonOptional(object, "environment", (value) => fromJsonTypeId(value)),
    };
}

/** Durable runtime function id inside one program. */
export type FunctionId = number;

export const FunctionId = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: FunctionId): void {
        encodeFunctionId(writer, value);
    },

    /** Decode one FunctionId. */
    decode(reader: BinaryReader): FunctionId {
        return decodeFunctionId(reader);
    },

    /** Return this value as JSON. */
    toJson(value: FunctionId): Json {
        return toJsonFunctionId(value);
    },

    /** Return one FunctionId from one JSON value. */
    fromJson(value: Json): FunctionId {
        return fromJsonFunctionId(value);
    },
};

/** Encode one FunctionId. */
export function encodeFunctionId(writer: BinaryWriter, value: FunctionId): void {
    writer.writeUnsigned(value);
}

/** Decode one FunctionId. */
export function decodeFunctionId(reader: BinaryReader): FunctionId {
    return reader.readNumber();
}

/** Return one JSON value for one FunctionId. */
export function toJsonFunctionId(value: FunctionId): Json {
    return value;
}

/** Return one FunctionId from one JSON value. */
export function fromJsonFunctionId(value: Json): FunctionId {
    return jsonInteger(value);
}
