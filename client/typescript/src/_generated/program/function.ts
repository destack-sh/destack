/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, jsonInteger } from "../../protocol/serde.js";

/** Durable runtime function id inside one program. */
export type FunctionId = number;

export const FunctionId = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: FunctionId): void {
        encodeFunctionId(writer, value);
    },

    /** Decode one FunctionId. */
    decode(reader: BinaryReader): FunctionId {
        return decodeFunctionId(reader);
    },

    /** Return this value as JSON. */
    toJson(value: FunctionId): Json {
        return toJsonFunctionId(value);
    },

    /** Return one FunctionId from one JSON value. */
    fromJson(value: Json): FunctionId {
        return fromJsonFunctionId(value);
    },
};

/** Encode one FunctionId. */
export function encodeFunctionId(writer: BinaryWriter, value: FunctionId): void {
    writer.writeUnsigned(value);
}

/** Decode one FunctionId. */
export function decodeFunctionId(reader: BinaryReader): FunctionId {
    return reader.readNumber();
}

/** Return one JSON value for one FunctionId. */
export function toJsonFunctionId(value: FunctionId): Json {
    return value;
}

/** Return one FunctionId from one JSON value. */
export function fromJsonFunctionId(value: Json): FunctionId {
    return jsonInteger(value);
}

/** Durable id for one callable signature. */
export type SignatureId = number;

export const SignatureId = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: SignatureId): void {
        encodeSignatureId(writer, value);
    },

    /** Decode one SignatureId. */
    decode(reader: BinaryReader): SignatureId {
        return decodeSignatureId(reader);
    },

    /** Return this value as JSON. */
    toJson(value: SignatureId): Json {
        return toJsonSignatureId(value);
    },

    /** Return one SignatureId from one JSON value. */
    fromJson(value: Json): SignatureId {
        return fromJsonSignatureId(value);
    },
};

/** Encode one SignatureId. */
export function encodeSignatureId(writer: BinaryWriter, value: SignatureId): void {
    writer.writeUnsigned(value);
}

/** Decode one SignatureId. */
export function decodeSignatureId(reader: BinaryReader): SignatureId {
    return reader.readNumber();
}

/** Return one JSON value for one SignatureId. */
export function toJsonSignatureId(value: SignatureId): Json {
    return value;
}

/** Return one SignatureId from one JSON value. */
export function fromJsonSignatureId(value: Json): SignatureId {
    return jsonInteger(value);
}
