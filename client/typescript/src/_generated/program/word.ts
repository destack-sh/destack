/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, jsonBigint } from "../../protocol/serde.js";

/** One 64-bit program execution word. */
export type Word = bigint;

export const Word = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: Word): void {
        encodeWord(writer, value);
    },

    /** Decode one Word. */
    decode(reader: BinaryReader): Word {
        return decodeWord(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Word): Json {
        return toJsonWord(value);
    },

    /** Return one Word from one JSON value. */
    fromJson(value: Json): Word {
        return fromJsonWord(value);
    },
};

/** Encode one Word. */
export function encodeWord(writer: BinaryWriter, value: Word): void {
    writer.writeUnsigned(value);
}

/** Decode one Word. */
export function decodeWord(reader: BinaryReader): Word {
    return reader.readUnsigned();
}

/** Return one JSON value for one Word. */
export function toJsonWord(value: Word): Json {
    return value.toString();
}

/** Return one Word from one JSON value. */
export function fromJsonWord(value: Json): Word {
    return jsonBigint(value);
}
