/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, jsonField, jsonObject } from "../../protocol/serde.js";
import type { SectionSlice } from "../core/section.js";
import { decodeSectionSlice, encodeSectionSlice, fromJsonSectionSlice, toJsonSectionSlice } from "../core/section.js";

/** Section-backed program string table. */
export type StringTable = {
    /** String entries sorted by stable string id. */
    readonly entries: SectionSlice;
    /** Concatenated UTF-8 string bytes. */
    readonly bytes: SectionSlice;
};

export const StringTable = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: StringTable): void {
        encodeStringTable(writer, value);
    },

    /** Decode one StringTable. */
    decode(reader: BinaryReader): StringTable {
        return decodeStringTable(reader);
    },

    /** Return this value as JSON. */
    toJson(value: StringTable): Json {
        return toJsonStringTable(value);
    },

    /** Return one StringTable from one JSON value. */
    fromJson(value: Json): StringTable {
        return fromJsonStringTable(value);
    },
};

/** Encode one StringTable. */
export function encodeStringTable(writer: BinaryWriter, value: StringTable): void {
    encodeSectionSlice(writer, value.entries);
    encodeSectionSlice(writer, value.bytes);
}

/** Decode one StringTable. */
export function decodeStringTable(reader: BinaryReader): StringTable {
    const entries = decodeSectionSlice(reader);
    const bytes = decodeSectionSlice(reader);

    return {
        entries,
        bytes,
    };
}

/** Return one JSON value for one StringTable. */
export function toJsonStringTable(value: StringTable): Json {
    return {
        entries: toJsonSectionSlice(value.entries),
        bytes: toJsonSectionSlice(value.bytes),
    };
}

/** Return one StringTable from one JSON value. */
export function fromJsonStringTable(value: Json): StringTable {
    const object = jsonObject(value);

    return {
        entries: fromJsonSectionSlice(jsonField(object, "entries")),
        bytes: fromJsonSectionSlice(jsonField(object, "bytes")),
    };
}
