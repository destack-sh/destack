/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, jsonArray, jsonField, jsonObject, jsonString } from "../../../protocol/serde.js";
import type { LocalNodeId } from "../../mir/tree/node.js";
import type { Value } from "../../mir/tree/value.js";
import type { FramePoint } from "./point.js";
import { decodeLocalNodeId, encodeLocalNodeId, fromJsonLocalNodeId, toJsonLocalNodeId } from "../../mir/tree/node.js";
import { decodeValue, encodeValue, fromJsonValue, toJsonValue } from "../../mir/tree/value.js";
import { decodeFramePoint, encodeFramePoint, fromJsonFramePoint, toJsonFramePoint } from "./point.js";

/** One MIR place retained by an engine frame. */
export type FramePlace =
    /** The hidden callable environment. */
    | {
          readonly kind: "environment";
      }
    /** One addressable MIR local. */
    | {
          readonly kind: "local";
          readonly local: LocalNodeId;
      }
    /** One MIR SSA value. */
    | {
          readonly kind: "value";
          readonly value: Value;
      }
;

export const FramePlace = {
    /** The hidden callable environment. */
    environment(): FramePlace {
        return { kind: "environment" };
    },

    /** One addressable MIR local. */
    local(local: LocalNodeId): FramePlace {
        return { kind: "local", local };
    },

    /** One MIR SSA value. */
    value(value: Value): FramePlace {
        return { kind: "value", value };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: FramePlace): void {
        encodeFramePlace(writer, value);
    },

    /** Decode one FramePlace. */
    decode(reader: BinaryReader): FramePlace {
        return decodeFramePlace(reader);
    },

    /** Return this value as JSON. */
    toJson(value: FramePlace): Json {
        return toJsonFramePlace(value);
    },

    /** Return one FramePlace from one JSON value. */
    fromJson(value: Json): FramePlace {
        return fromJsonFramePlace(value);
    },
};

/** Encode one FramePlace. */
export function encodeFramePlace(writer: BinaryWriter, value: FramePlace): void {
    switch (value.kind) {
        case "environment":
            writer.writeUnsigned(0);
            return;
        case "local":
            writer.writeUnsigned(1);
            encodeLocalNodeId(writer, value.local);
            return;
        case "value":
            writer.writeUnsigned(2);
            encodeValue(writer, value.value);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one FramePlace. */
export function decodeFramePlace(reader: BinaryReader): FramePlace {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            return { kind: "environment" };
        }
        case 1: {
            const local = decodeLocalNodeId(reader);

            return { kind: "local", local };
        }
        case 2: {
            const value = decodeValue(reader);

            return { kind: "value", value };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one FramePlace. */
export function toJsonFramePlace(value: FramePlace): Json {
    switch (value.kind) {
        case "environment":
            return {
                kind: "environment",
            };
        case "local":
            return {
                kind: "local",
                local: toJsonLocalNodeId(value.local),
            };
        case "value":
            return {
                kind: "value",
                value: toJsonValue(value.value),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one FramePlace from one JSON value. */
export function fromJsonFramePlace(value: Json): FramePlace {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "environment":
            return {
                kind,
            };
        case "local":
            return {
                kind,
                local: fromJsonLocalNodeId(jsonField(object, "local")),
            };
        case "value":
            return {
                kind,
                value: fromJsonValue(jsonField(object, "value")),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** One live logical value inside a frame state. */
export type FrameSlot = {
    /** The logical place containing the value. */
    readonly place: FramePlace;
    /** The object-local value type. */
    readonly ty: LocalNodeId;
};

export const FrameSlot = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: FrameSlot): void {
        encodeFrameSlot(writer, value);
    },

    /** Decode one FrameSlot. */
    decode(reader: BinaryReader): FrameSlot {
        return decodeFrameSlot(reader);
    },

    /** Return this value as JSON. */
    toJson(value: FrameSlot): Json {
        return toJsonFrameSlot(value);
    },

    /** Return one FrameSlot from one JSON value. */
    fromJson(value: Json): FrameSlot {
        return fromJsonFrameSlot(value);
    },
};

/** Encode one FrameSlot. */
export function encodeFrameSlot(writer: BinaryWriter, value: FrameSlot): void {
    encodeFramePlace(writer, value.place);
    encodeLocalNodeId(writer, value.ty);
}

/** Decode one FrameSlot. */
export function decodeFrameSlot(reader: BinaryReader): FrameSlot {
    const place = decodeFramePlace(reader);
    const ty = decodeLocalNodeId(reader);

    return {
        place,
        ty,
    };
}

/** Return one JSON value for one FrameSlot. */
export function toJsonFrameSlot(value: FrameSlot): Json {
    return {
        place: toJsonFramePlace(value.place),
        ty: toJsonLocalNodeId(value.ty),
    };
}

/** Return one FrameSlot from one JSON value. */
export function fromJsonFrameSlot(value: Json): FrameSlot {
    const object = jsonObject(value);

    return {
        place: fromJsonFramePlace(jsonField(object, "place")),
        ty: fromJsonLocalNodeId(jsonField(object, "ty")),
    };
}

/** One logical frame state in acquisition order. */
export type FrameState = {
    /** The logical coordinate represented by this frame state. */
    readonly point: FramePoint;
    /** The live logical frame slots in acquisition order. */
    readonly slots: ReadonlyArray<FrameSlot>;
};

export const FrameState = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: FrameState): void {
        encodeFrameState(writer, value);
    },

    /** Decode one FrameState. */
    decode(reader: BinaryReader): FrameState {
        return decodeFrameState(reader);
    },

    /** Return this value as JSON. */
    toJson(value: FrameState): Json {
        return toJsonFrameState(value);
    },

    /** Return one FrameState from one JSON value. */
    fromJson(value: Json): FrameState {
        return fromJsonFrameState(value);
    },
};

/** Encode one FrameState. */
export function encodeFrameState(writer: BinaryWriter, value: FrameState): void {
    encodeFramePoint(writer, value.point);
    writer.writeUnsigned(value.slots.length);
    for (const item1 of value.slots) {
        encodeFrameSlot(writer, item1);
    }
}

/** Decode one FrameState. */
export function decodeFrameState(reader: BinaryReader): FrameState {
    const point = decodeFramePoint(reader);
    const slots = (() => { const length1 = reader.readNumber(); const items1: Array<FrameSlot> = []; for (let index = 0; index < length1; index += 1) { items1.push(decodeFrameSlot(reader)); } return items1; })();

    return {
        point,
        slots,
    };
}

/** Return one JSON value for one FrameState. */
export function toJsonFrameState(value: FrameState): Json {
    return {
        point: toJsonFramePoint(value.point),
        slots: value.slots.map((item0) => toJsonFrameSlot(item0)),
    };
}

/** Return one FrameState from one JSON value. */
export function fromJsonFrameState(value: Json): FrameState {
    const object = jsonObject(value);

    return {
        point: fromJsonFramePoint(jsonField(object, "point")),
        slots: jsonArray(jsonField(object, "slots")).map((item0) => fromJsonFrameSlot(item0)),
    };
}
