/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, jsonField, jsonInteger, jsonObject, jsonString } from "../../../protocol/serde.js";
import type { LocalNodeId } from "../../mir/tree/node.js";
import { decodeLocalNodeId, encodeLocalNodeId, fromJsonLocalNodeId, toJsonLocalNodeId } from "../../mir/tree/node.js";

/** One logical frame coordinate in an emitted object. */
export type FramePoint =
    /** State before a coroutine executes its first operation. */
    | {
          readonly kind: "entry";
          /** The object-local coroutine function. */
          readonly function: LocalNodeId;
      }
    /** State at one executable operation. */
    | {
          readonly kind: "operation";
          readonly operation: Point;
      }
;

export const FramePoint = {
    /** State before a coroutine executes its first operation. */
    entry(function_: LocalNodeId): FramePoint {
        return { kind: "entry", function: function_ };
    },

    /** State at one executable operation. */
    operation(operation: Point): FramePoint {
        return { kind: "operation", operation };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: FramePoint): void {
        encodeFramePoint(writer, value);
    },

    /** Decode one FramePoint. */
    decode(reader: BinaryReader): FramePoint {
        return decodeFramePoint(reader);
    },

    /** Return this value as JSON. */
    toJson(value: FramePoint): Json {
        return toJsonFramePoint(value);
    },

    /** Return one FramePoint from one JSON value. */
    fromJson(value: Json): FramePoint {
        return fromJsonFramePoint(value);
    },
};

/** Encode one FramePoint. */
export function encodeFramePoint(writer: BinaryWriter, value: FramePoint): void {
    switch (value.kind) {
        case "entry":
            writer.writeUnsigned(0);
            encodeLocalNodeId(writer, value.function);
            return;
        case "operation":
            writer.writeUnsigned(1);
            encodePoint(writer, value.operation);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one FramePoint. */
export function decodeFramePoint(reader: BinaryReader): FramePoint {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const function_ = decodeLocalNodeId(reader);

            return {
                kind: "entry",
                function: function_,
            };
        }
        case 1: {
            const operation = decodePoint(reader);

            return { kind: "operation", operation };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one FramePoint. */
export function toJsonFramePoint(value: FramePoint): Json {
    switch (value.kind) {
        case "entry":
            return {
                kind: "entry",
                function: toJsonLocalNodeId(value.function),
            };
        case "operation":
            return {
                kind: "operation",
                operation: toJsonPoint(value.operation),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one FramePoint from one JSON value. */
export function fromJsonFramePoint(value: Json): FramePoint {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "entry":
            return {
                kind,
                function: fromJsonLocalNodeId(jsonField(object, "function")),
            };
        case "operation":
            return {
                kind,
                operation: fromJsonPoint(jsonField(object, "operation")),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** One logical operation in an emitted object. */
export type Point = {
    /** The object-local function. */
    readonly function: LocalNodeId;
    /** The operation index inside the function. */
    readonly operation: number;
};

export const Point = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: Point): void {
        encodePoint(writer, value);
    },

    /** Decode one Point. */
    decode(reader: BinaryReader): Point {
        return decodePoint(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Point): Json {
        return toJsonPoint(value);
    },

    /** Return one Point from one JSON value. */
    fromJson(value: Json): Point {
        return fromJsonPoint(value);
    },
};

/** Encode one Point. */
export function encodePoint(writer: BinaryWriter, value: Point): void {
    encodeLocalNodeId(writer, value.function);
    writer.writeUnsigned(value.operation);
}

/** Decode one Point. */
export function decodePoint(reader: BinaryReader): Point {
    const function_ = decodeLocalNodeId(reader);
    const operation = reader.readNumber();

    return {
        function: function_,
        operation,
    };
}

/** Return one JSON value for one Point. */
export function toJsonPoint(value: Point): Json {
    return {
        function: toJsonLocalNodeId(value.function),
        operation: value.operation,
    };
}

/** Return one Point from one JSON value. */
export function fromJsonPoint(value: Json): Point {
    const object = jsonObject(value);

    return {
        function: fromJsonLocalNodeId(jsonField(object, "function")),
        operation: jsonInteger(jsonField(object, "operation")),
    };
}
