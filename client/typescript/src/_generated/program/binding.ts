/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, jsonField, jsonObject } from "../../protocol/serde.js";
import type { SectionSlice } from "../core/section.js";
import { decodeSectionSlice, encodeSectionSlice, fromJsonSectionSlice, toJsonSectionSlice } from "../core/section.js";

/** Runtime binding declarations carried by one Program. */
export type BindingTable = {
    /** Binding declarations in function order. */
    readonly bindings: SectionSlice;
    /** Binding entry indices in stable id order. */
    readonly idIndex: SectionSlice;
    /** Flattened binding string lists. */
    readonly strings: SectionSlice;
};

export const BindingTable = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: BindingTable): void {
        encodeBindingTable(writer, value);
    },

    /** Decode one BindingTable. */
    decode(reader: BinaryReader): BindingTable {
        return decodeBindingTable(reader);
    },

    /** Return this value as JSON. */
    toJson(value: BindingTable): Json {
        return toJsonBindingTable(value);
    },

    /** Return one BindingTable from one JSON value. */
    fromJson(value: Json): BindingTable {
        return fromJsonBindingTable(value);
    },
};

/** Encode one BindingTable. */
export function encodeBindingTable(writer: BinaryWriter, value: BindingTable): void {
    encodeSectionSlice(writer, value.bindings);
    encodeSectionSlice(writer, value.idIndex);
    encodeSectionSlice(writer, value.strings);
}

/** Decode one BindingTable. */
export function decodeBindingTable(reader: BinaryReader): BindingTable {
    const bindings = decodeSectionSlice(reader);
    const idIndex = decodeSectionSlice(reader);
    const strings = decodeSectionSlice(reader);

    return {
        bindings,
        idIndex,
        strings,
    };
}

/** Return one JSON value for one BindingTable. */
export function toJsonBindingTable(value: BindingTable): Json {
    return {
        bindings: toJsonSectionSlice(value.bindings),
        idIndex: toJsonSectionSlice(value.idIndex),
        strings: toJsonSectionSlice(value.strings),
    };
}

/** Return one BindingTable from one JSON value. */
export function fromJsonBindingTable(value: Json): BindingTable {
    const object = jsonObject(value);

    return {
        bindings: fromJsonSectionSlice(jsonField(object, "bindings")),
        idIndex: fromJsonSectionSlice(jsonField(object, "idIndex")),
        strings: fromJsonSectionSlice(jsonField(object, "strings")),
    };
}
