/* generated by destack_client_generate, do not edit */

import {
    BinaryReader,
    BinaryWriter,
    Json,
    SerdeError,
    jsonField,
    jsonObject,
    jsonOptional,
    jsonString,
} from "../../protocol/serde.js";
import type { BindingId } from "./binding.js";
import type { FunctionId } from "./function.js";
import type { ProgramPoint } from "./point.js";
import type { AllocationSite } from "./site.js";
import type { CallSite } from "./site.js";
import type { EdgeSite } from "./site.js";
import type { MemoryAccess } from "./site.js";
import type { MemorySite } from "./site.js";
import type { TypeId } from "./type.js";
import type { MemoryRange } from "./watch.js";
import { decodeBindingId, encodeBindingId, fromJsonBindingId, toJsonBindingId } from "./binding.js";
import {
    decodeFunctionId,
    encodeFunctionId,
    fromJsonFunctionId,
    toJsonFunctionId,
} from "./function.js";
import {
    decodeProgramPoint,
    encodeProgramPoint,
    fromJsonProgramPoint,
    toJsonProgramPoint,
} from "./point.js";
import {
    decodeAllocationSite,
    encodeAllocationSite,
    fromJsonAllocationSite,
    toJsonAllocationSite,
} from "./site.js";
import { decodeCallSite, encodeCallSite, fromJsonCallSite, toJsonCallSite } from "./site.js";
import { decodeEdgeSite, encodeEdgeSite, fromJsonEdgeSite, toJsonEdgeSite } from "./site.js";
import {
    decodeMemoryAccess,
    encodeMemoryAccess,
    fromJsonMemoryAccess,
    toJsonMemoryAccess,
} from "./site.js";
import {
    decodeMemorySite,
    encodeMemorySite,
    fromJsonMemorySite,
    toJsonMemorySite,
} from "./site.js";
import { decodeTypeId, encodeTypeId, fromJsonTypeId, toJsonTypeId } from "./type.js";
import {
    decodeMemoryRange,
    encodeMemoryRange,
    fromJsonMemoryRange,
    toJsonMemoryRange,
} from "./watch.js";

/** One runtime binding event. */
export type BindingEvent = "enter" | "exit";

export const BindingEvent = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: BindingEvent): void {
        encodeBindingEvent(writer, value);
    },

    /** Decode one BindingEvent. */
    decode(reader: BinaryReader): BindingEvent {
        return decodeBindingEvent(reader);
    },

    /** Return this value as JSON. */
    toJson(value: BindingEvent): Json {
        return toJsonBindingEvent(value);
    },

    /** Return one BindingEvent from one JSON value. */
    fromJson(value: Json): BindingEvent {
        return fromJsonBindingEvent(value);
    },
};

/** Encode one BindingEvent. */
export function encodeBindingEvent(writer: BinaryWriter, value: BindingEvent): void {
    switch (value) {
        case "enter":
            writer.writeUnsigned(0);
            return;
        case "exit":
            writer.writeUnsigned(1);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one BindingEvent. */
export function decodeBindingEvent(reader: BinaryReader): BindingEvent {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "enter";
        case 1:
            return "exit";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one BindingEvent. */
export function toJsonBindingEvent(value: BindingEvent): Json {
    return value;
}

/** Return one BindingEvent from one JSON value. */
export function fromJsonBindingEvent(value: Json): BindingEvent {
    const variant = jsonString(value);

    switch (variant) {
        case "enter":
            return "enter";
        case "exit":
            return "exit";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** One frame event. */
export type FrameEvent = "enter" | "exit";

export const FrameEvent = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: FrameEvent): void {
        encodeFrameEvent(writer, value);
    },

    /** Decode one FrameEvent. */
    decode(reader: BinaryReader): FrameEvent {
        return decodeFrameEvent(reader);
    },

    /** Return this value as JSON. */
    toJson(value: FrameEvent): Json {
        return toJsonFrameEvent(value);
    },

    /** Return one FrameEvent from one JSON value. */
    fromJson(value: Json): FrameEvent {
        return fromJsonFrameEvent(value);
    },
};

/** Encode one FrameEvent. */
export function encodeFrameEvent(writer: BinaryWriter, value: FrameEvent): void {
    switch (value) {
        case "enter":
            writer.writeUnsigned(0);
            return;
        case "exit":
            writer.writeUnsigned(1);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one FrameEvent. */
export function decodeFrameEvent(reader: BinaryReader): FrameEvent {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "enter";
        case 1:
            return "exit";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one FrameEvent. */
export function toJsonFrameEvent(value: FrameEvent): Json {
    return value;
}

/** Return one FrameEvent from one JSON value. */
export function fromJsonFrameEvent(value: Json): FrameEvent {
    const variant = jsonString(value);

    switch (variant) {
        case "enter":
            return "enter";
        case "exit":
            return "exit";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** One observable Program execution event. */
export type Event =
    /** One Program point reached execution. */
    | {
          readonly kind: "point";
          /** The reached Program point. */
          readonly point: ProgramPoint;
      }
    /** One control-flow edge was taken. */
    | {
          readonly kind: "edge";
          /** The executed control-flow edge. */
          readonly site: EdgeSite;
      }
    /** One function call began. */
    | {
          readonly kind: "call";
          /** The executed call site. */
          readonly site: CallSite;
          /** The resolved callee. */
          readonly function: FunctionId;
      }
    /** One frame entered or left execution. */
    | {
          readonly kind: "frame";
          /** The frame event. */
          readonly event: FrameEvent;
          /** The affected function. */
          readonly function: FunctionId;
      }
    /** One allocation completed. */
    | {
          readonly kind: "allocation";
          /** The executed allocation site. */
          readonly site: AllocationSite;
          /** The allocated memory range. */
          readonly range: MemoryRange;
      }
    /** One memory access completed. */
    | {
          readonly kind: "memory";
          /** The executed memory site. */
          readonly site: MemorySite;
          /** The executed memory access. */
          readonly access: MemoryAccess;
          /** The accessed memory range when materialized. */
          readonly range?: MemoryRange;
      }
    /** One runtime binding call entered or exited. */
    | {
          readonly kind: "binding";
          /** The binding event. */
          readonly event: BindingEvent;
          /** The called binding. */
          readonly bindingId: BindingId;
      }
    /** One language panic began unwinding. */
    | {
          readonly kind: "panic";
          /** The Program point that raised the panic. */
          readonly point: ProgramPoint;
          /** The carried value type when present. */
          readonly ty?: TypeId;
      };

export const Event = {
    /** One Program point reached execution. */
    point(point: ProgramPoint): Event {
        return { kind: "point", point };
    },

    /** One control-flow edge was taken. */
    edge(site: EdgeSite): Event {
        return { kind: "edge", site };
    },

    /** One function call began. */
    call(site: CallSite, function_: FunctionId): Event {
        return { kind: "call", site, function: function_ };
    },

    /** One frame entered or left execution. */
    frame(event: FrameEvent, function_: FunctionId): Event {
        return { kind: "frame", event, function: function_ };
    },

    /** One allocation completed. */
    allocation(site: AllocationSite, range: MemoryRange): Event {
        return { kind: "allocation", site, range };
    },

    /** One memory access completed. */
    memory(site: MemorySite, access: MemoryAccess, range: MemoryRange | undefined): Event {
        return { kind: "memory", site, access, range };
    },

    /** One runtime binding call entered or exited. */
    binding(event: BindingEvent, bindingId: BindingId): Event {
        return { kind: "binding", event, bindingId };
    },

    /** One language panic began unwinding. */
    panic(point: ProgramPoint, ty: TypeId | undefined): Event {
        return { kind: "panic", point, ty };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: Event): void {
        encodeEvent(writer, value);
    },

    /** Decode one Event. */
    decode(reader: BinaryReader): Event {
        return decodeEvent(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Event): Json {
        return toJsonEvent(value);
    },

    /** Return one Event from one JSON value. */
    fromJson(value: Json): Event {
        return fromJsonEvent(value);
    },
};

/** Encode one Event. */
export function encodeEvent(writer: BinaryWriter, value: Event): void {
    switch (value.kind) {
        case "point":
            writer.writeUnsigned(0);
            encodeProgramPoint(writer, value.point);
            return;
        case "edge":
            writer.writeUnsigned(1);
            encodeEdgeSite(writer, value.site);
            return;
        case "call":
            writer.writeUnsigned(2);
            encodeCallSite(writer, value.site);
            encodeFunctionId(writer, value.function);
            return;
        case "frame":
            writer.writeUnsigned(3);
            encodeFrameEvent(writer, value.event);
            encodeFunctionId(writer, value.function);
            return;
        case "allocation":
            writer.writeUnsigned(4);
            encodeAllocationSite(writer, value.site);
            encodeMemoryRange(writer, value.range);
            return;
        case "memory":
            writer.writeUnsigned(5);
            encodeMemorySite(writer, value.site);
            encodeMemoryAccess(writer, value.access);
            writer.writeOption(value.range, (value2) => {
                encodeMemoryRange(writer, value2);
            });
            return;
        case "binding":
            writer.writeUnsigned(6);
            encodeBindingEvent(writer, value.event);
            encodeBindingId(writer, value.bindingId);
            return;
        case "panic":
            writer.writeUnsigned(7);
            encodeProgramPoint(writer, value.point);
            writer.writeOption(value.ty, (value1) => {
                encodeTypeId(writer, value1);
            });
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one Event. */
export function decodeEvent(reader: BinaryReader): Event {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const point = decodeProgramPoint(reader);

            return {
                kind: "point",
                point,
            };
        }
        case 1: {
            const site = decodeEdgeSite(reader);

            return {
                kind: "edge",
                site,
            };
        }
        case 2: {
            const site = decodeCallSite(reader);
            const function_ = decodeFunctionId(reader);

            return {
                kind: "call",
                site,
                function: function_,
            };
        }
        case 3: {
            const event = decodeFrameEvent(reader);
            const function_ = decodeFunctionId(reader);

            return {
                kind: "frame",
                event,
                function: function_,
            };
        }
        case 4: {
            const site = decodeAllocationSite(reader);
            const range = decodeMemoryRange(reader);

            return {
                kind: "allocation",
                site,
                range,
            };
        }
        case 5: {
            const site = decodeMemorySite(reader);
            const access = decodeMemoryAccess(reader);
            const range = reader.readOption(() => decodeMemoryRange(reader));

            return {
                kind: "memory",
                site,
                access,
                ...(range === undefined ? {} : { range }),
            };
        }
        case 6: {
            const event = decodeBindingEvent(reader);
            const bindingId = decodeBindingId(reader);

            return {
                kind: "binding",
                event,
                bindingId,
            };
        }
        case 7: {
            const point = decodeProgramPoint(reader);
            const ty = reader.readOption(() => decodeTypeId(reader));

            return {
                kind: "panic",
                point,
                ...(ty === undefined ? {} : { ty }),
            };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one Event. */
export function toJsonEvent(value: Event): Json {
    switch (value.kind) {
        case "point":
            return {
                kind: "point",
                point: toJsonProgramPoint(value.point),
            };
        case "edge":
            return {
                kind: "edge",
                site: toJsonEdgeSite(value.site),
            };
        case "call":
            return {
                kind: "call",
                site: toJsonCallSite(value.site),
                function: toJsonFunctionId(value.function),
            };
        case "frame":
            return {
                kind: "frame",
                event: toJsonFrameEvent(value.event),
                function: toJsonFunctionId(value.function),
            };
        case "allocation":
            return {
                kind: "allocation",
                site: toJsonAllocationSite(value.site),
                range: toJsonMemoryRange(value.range),
            };
        case "memory":
            return {
                kind: "memory",
                site: toJsonMemorySite(value.site),
                access: toJsonMemoryAccess(value.access),
                ...(value.range === undefined ? {} : { range: toJsonMemoryRange(value.range) }),
            };
        case "binding":
            return {
                kind: "binding",
                event: toJsonBindingEvent(value.event),
                bindingId: toJsonBindingId(value.bindingId),
            };
        case "panic":
            return {
                kind: "panic",
                point: toJsonProgramPoint(value.point),
                ...(value.ty === undefined ? {} : { ty: toJsonTypeId(value.ty) }),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one Event from one JSON value. */
export function fromJsonEvent(value: Json): Event {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "point":
            return {
                kind,
                point: fromJsonProgramPoint(jsonField(object, "point")),
            };
        case "edge":
            return {
                kind,
                site: fromJsonEdgeSite(jsonField(object, "site")),
            };
        case "call":
            return {
                kind,
                site: fromJsonCallSite(jsonField(object, "site")),
                function: fromJsonFunctionId(jsonField(object, "function")),
            };
        case "frame":
            return {
                kind,
                event: fromJsonFrameEvent(jsonField(object, "event")),
                function: fromJsonFunctionId(jsonField(object, "function")),
            };
        case "allocation":
            return {
                kind,
                site: fromJsonAllocationSite(jsonField(object, "site")),
                range: fromJsonMemoryRange(jsonField(object, "range")),
            };
        case "memory":
            return {
                kind,
                site: fromJsonMemorySite(jsonField(object, "site")),
                access: fromJsonMemoryAccess(jsonField(object, "access")),
                range: jsonOptional(object, "range", (value) => fromJsonMemoryRange(value)),
            };
        case "binding":
            return {
                kind,
                event: fromJsonBindingEvent(jsonField(object, "event")),
                bindingId: fromJsonBindingId(jsonField(object, "bindingId")),
            };
        case "panic":
            return {
                kind,
                point: fromJsonProgramPoint(jsonField(object, "point")),
                ty: jsonOptional(object, "ty", (value) => fromJsonTypeId(value)),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}
