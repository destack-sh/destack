/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, jsonField, jsonObject } from "../../protocol/serde.js";
import type { SectionSlice } from "../core/section.js";
import { decodeSectionSlice, encodeSectionSlice, fromJsonSectionSlice, toJsonSectionSlice } from "../core/section.js";

/** Program frame states and their canonical layouts. */
export type FrameTable = {
    /** Frame states keyed by dense frame state id. */
    readonly states: SectionSlice;
    /** Canonical frame layouts keyed by dense frame layout id. */
    readonly layouts: SectionSlice;
    /** Flattened frame slots. */
    readonly slots: SectionSlice;
};

export const FrameTable = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: FrameTable): void {
        encodeFrameTable(writer, value);
    },

    /** Decode one FrameTable. */
    decode(reader: BinaryReader): FrameTable {
        return decodeFrameTable(reader);
    },

    /** Return this value as JSON. */
    toJson(value: FrameTable): Json {
        return toJsonFrameTable(value);
    },

    /** Return one FrameTable from one JSON value. */
    fromJson(value: Json): FrameTable {
        return fromJsonFrameTable(value);
    },
};

/** Encode one FrameTable. */
export function encodeFrameTable(writer: BinaryWriter, value: FrameTable): void {
    encodeSectionSlice(writer, value.states);
    encodeSectionSlice(writer, value.layouts);
    encodeSectionSlice(writer, value.slots);
}

/** Decode one FrameTable. */
export function decodeFrameTable(reader: BinaryReader): FrameTable {
    const states = decodeSectionSlice(reader);
    const layouts = decodeSectionSlice(reader);
    const slots = decodeSectionSlice(reader);

    return {
        states,
        layouts,
        slots,
    };
}

/** Return one JSON value for one FrameTable. */
export function toJsonFrameTable(value: FrameTable): Json {
    return {
        states: toJsonSectionSlice(value.states),
        layouts: toJsonSectionSlice(value.layouts),
        slots: toJsonSectionSlice(value.slots),
    };
}

/** Return one FrameTable from one JSON value. */
export function fromJsonFrameTable(value: Json): FrameTable {
    const object = jsonObject(value);

    return {
        states: fromJsonSectionSlice(jsonField(object, "states")),
        layouts: fromJsonSectionSlice(jsonField(object, "layouts")),
        slots: fromJsonSectionSlice(jsonField(object, "slots")),
    };
}
