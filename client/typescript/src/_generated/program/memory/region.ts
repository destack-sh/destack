/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, jsonBool, jsonField, jsonInteger, jsonObject } from "../../../protocol/serde.js";
import type { TypeId } from "../type.js";
import { decodeTypeId, encodeTypeId, fromJsonTypeId, toJsonTypeId } from "../type.js";

/** One typed region inside static memory. */
export type StaticRegion = {
    /** The region id. */
    readonly id: StaticId;
    /** The region byte offset. */
    readonly offset: number;
    /** The region byte length. */
    readonly byteLen: number;
    /** The region value type. */
    readonly ty: TypeId;
    /** Whether this region allows stores. */
    readonly isMutable: boolean;
};

export const StaticRegion = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: StaticRegion): void {
        encodeStaticRegion(writer, value);
    },

    /** Decode one StaticRegion. */
    decode(reader: BinaryReader): StaticRegion {
        return decodeStaticRegion(reader);
    },

    /** Return this value as JSON. */
    toJson(value: StaticRegion): Json {
        return toJsonStaticRegion(value);
    },

    /** Return one StaticRegion from one JSON value. */
    fromJson(value: Json): StaticRegion {
        return fromJsonStaticRegion(value);
    },
};

/** Encode one StaticRegion. */
export function encodeStaticRegion(writer: BinaryWriter, value: StaticRegion): void {
    encodeStaticId(writer, value.id);
    writer.writeUnsigned(value.offset);
    writer.writeUnsigned(value.byteLen);
    encodeTypeId(writer, value.ty);
    writer.writeBool(value.isMutable);
}

/** Decode one StaticRegion. */
export function decodeStaticRegion(reader: BinaryReader): StaticRegion {
    const id = decodeStaticId(reader);
    const offset = reader.readNumber();
    const byteLen = reader.readNumber();
    const ty = decodeTypeId(reader);
    const isMutable = reader.readBool();

    return {
        id,
        offset,
        byteLen,
        ty,
        isMutable,
    };
}

/** Return one JSON value for one StaticRegion. */
export function toJsonStaticRegion(value: StaticRegion): Json {
    return {
        id: toJsonStaticId(value.id),
        offset: value.offset,
        byteLen: value.byteLen,
        ty: toJsonTypeId(value.ty),
        isMutable: value.isMutable,
    };
}

/** Return one StaticRegion from one JSON value. */
export function fromJsonStaticRegion(value: Json): StaticRegion {
    const object = jsonObject(value);

    return {
        id: fromJsonStaticId(jsonField(object, "id")),
        offset: jsonInteger(jsonField(object, "offset")),
        byteLen: jsonInteger(jsonField(object, "byteLen")),
        ty: fromJsonTypeId(jsonField(object, "ty")),
        isMutable: jsonBool(jsonField(object, "isMutable")),
    };
}

/** One static region. */
export type StaticId = number;

export const StaticId = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: StaticId): void {
        encodeStaticId(writer, value);
    },

    /** Decode one StaticId. */
    decode(reader: BinaryReader): StaticId {
        return decodeStaticId(reader);
    },

    /** Return this value as JSON. */
    toJson(value: StaticId): Json {
        return toJsonStaticId(value);
    },

    /** Return one StaticId from one JSON value. */
    fromJson(value: Json): StaticId {
        return fromJsonStaticId(value);
    },
};

/** Encode one StaticId. */
export function encodeStaticId(writer: BinaryWriter, value: StaticId): void {
    writer.writeUnsigned(value);
}

/** Decode one StaticId. */
export function decodeStaticId(reader: BinaryReader): StaticId {
    return reader.readNumber();
}

/** Return one JSON value for one StaticId. */
export function toJsonStaticId(value: StaticId): Json {
    return value;
}

/** Return one StaticId from one JSON value. */
export function fromJsonStaticId(value: Json): StaticId {
    return jsonInteger(value);
}
