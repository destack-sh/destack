/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, jsonField, jsonInteger, jsonObject, jsonOptional, jsonString } from "../../../protocol/serde.js";

/** One stable source location. */
export type SourceReference = {
    /** The stable logical source path when filesystem backed. */
    readonly path?: string;
    /** The one-based source line. */
    readonly line: number;
    /** The one-based byte column. */
    readonly column: number;
    /** The one-based final source line. */
    readonly endLine: number;
    /** The one-based final byte column. */
    readonly endColumn: number;
};

export const SourceReference = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: SourceReference): void {
        encodeSourceReference(writer, value);
    },

    /** Decode one SourceReference. */
    decode(reader: BinaryReader): SourceReference {
        return decodeSourceReference(reader);
    },

    /** Return this value as JSON. */
    toJson(value: SourceReference): Json {
        return toJsonSourceReference(value);
    },

    /** Return one SourceReference from one JSON value. */
    fromJson(value: Json): SourceReference {
        return fromJsonSourceReference(value);
    },
};

/** Encode one SourceReference. */
export function encodeSourceReference(writer: BinaryWriter, value: SourceReference): void {
    writer.writeOption(value.path, (value0) => {
        writer.writeString(value0);
    });
    writer.writeUnsigned(value.line);
    writer.writeUnsigned(value.column);
    writer.writeUnsigned(value.endLine);
    writer.writeUnsigned(value.endColumn);
}

/** Decode one SourceReference. */
export function decodeSourceReference(reader: BinaryReader): SourceReference {
    const path = reader.readOption(() => reader.readString());
    const line = reader.readNumber();
    const column = reader.readNumber();
    const endLine = reader.readNumber();
    const endColumn = reader.readNumber();

    return {
        ...(path === undefined ? {} : { path }),
        line,
        column,
        endLine,
        endColumn,
    };
}

/** Return one JSON value for one SourceReference. */
export function toJsonSourceReference(value: SourceReference): Json {
    return {
        ...(value.path === undefined ? {} : { path: value.path }),
        line: value.line,
        column: value.column,
        endLine: value.endLine,
        endColumn: value.endColumn,
    };
}

/** Return one SourceReference from one JSON value. */
export function fromJsonSourceReference(value: Json): SourceReference {
    const object = jsonObject(value);

    return {
        path: jsonOptional(object, "path", (value) => jsonString(value)),
        line: jsonInteger(jsonField(object, "line")),
        column: jsonInteger(jsonField(object, "column")),
        endLine: jsonInteger(jsonField(object, "endLine")),
        endColumn: jsonInteger(jsonField(object, "endColumn")),
    };
}
