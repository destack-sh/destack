/* generated by destack_client_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, jsonField, jsonInteger, jsonObject, jsonOptional, jsonString } from "../../../protocol/serde.js";

/** One checked declaration signature. */
export type Signature = {
    /** The formatted UTF-8 text. */
    readonly text: string;
    /** The declared name interval when one exists. */
    readonly name?: TextRange;
};

export const Signature = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: Signature): void {
        encodeSignature(writer, value);
    },

    /** Decode one Signature. */
    decode(reader: BinaryReader): Signature {
        return decodeSignature(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Signature): Json {
        return toJsonSignature(value);
    },

    /** Return one Signature from one JSON value. */
    fromJson(value: Json): Signature {
        return fromJsonSignature(value);
    },
};

/** Encode one Signature. */
export function encodeSignature(writer: BinaryWriter, value: Signature): void {
    writer.writeString(value.text);
    writer.writeOption(value.name, (value1) => {
        encodeTextRange(writer, value1);
    });
}

/** Decode one Signature. */
export function decodeSignature(reader: BinaryReader): Signature {
    const text = reader.readString();
    const name = reader.readOption(() => decodeTextRange(reader));

    return {
        text,
        ...(name === undefined ? {} : { name }),
    };
}

/** Return one JSON value for one Signature. */
export function toJsonSignature(value: Signature): Json {
    return {
        text: value.text,
        ...(value.name === undefined ? {} : { name: toJsonTextRange(value.name) }),
    };
}

/** Return one Signature from one JSON value. */
export function fromJsonSignature(value: Json): Signature {
    const object = jsonObject(value);

    return {
        text: jsonString(jsonField(object, "text")),
        name: jsonOptional(object, "name", (value) => fromJsonTextRange(value)),
    };
}

/** One UTF-8 byte interval inside text. */
export type TextRange = {
    /** The inclusive byte offset. */
    readonly start: number;
    /** The exclusive byte offset. */
    readonly end: number;
};

export const TextRange = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: TextRange): void {
        encodeTextRange(writer, value);
    },

    /** Decode one TextRange. */
    decode(reader: BinaryReader): TextRange {
        return decodeTextRange(reader);
    },

    /** Return this value as JSON. */
    toJson(value: TextRange): Json {
        return toJsonTextRange(value);
    },

    /** Return one TextRange from one JSON value. */
    fromJson(value: Json): TextRange {
        return fromJsonTextRange(value);
    },
};

/** Encode one TextRange. */
export function encodeTextRange(writer: BinaryWriter, value: TextRange): void {
    writer.writeUnsigned(value.start);
    writer.writeUnsigned(value.end);
}

/** Decode one TextRange. */
export function decodeTextRange(reader: BinaryReader): TextRange {
    const start = reader.readNumber();
    const end = reader.readNumber();

    return {
        start,
        end,
    };
}

/** Return one JSON value for one TextRange. */
export function toJsonTextRange(value: TextRange): Json {
    return {
        start: value.start,
        end: value.end,
    };
}

/** Return one TextRange from one JSON value. */
export function fromJsonTextRange(value: Json): TextRange {
    const object = jsonObject(value);

    return {
        start: jsonInteger(jsonField(object, "start")),
        end: jsonInteger(jsonField(object, "end")),
    };
}
