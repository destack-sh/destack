set shell := ["bash", "-cu"]

# all platform crates
packages := "-p destack_cli -p destack_lsp -p destack_vscode -p destack_daemon"

_default:
    @just --list --unsorted

# check
check:
    cargo clippy --release {{ packages }}

# build
build:
    cargo build --release {{ packages }}

# test
test:
    cargo test --release {{ packages }}
    cd vscode && bun run test:grammar

# format
fmt:
    cargo fmt {{ packages }}
