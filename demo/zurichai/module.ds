use dyst

struct MeetupSeries: Entity {
    name: string
}

struct Meetup: Entity {
    fullName: string

    {
        function get(self) { return @format("{self.name} {self.surname}") }
        function set(self, value: string) { self.name = value }
    }
}