// SYNTAX TEST "source.ds"

// expressions / const / expressions and blocks
const size = const sizeOf<User>();
//           ^^^^^ storage.modifier.ds

// expressions / const / static if annotations
@if(import.meta.runtime == "js")
// <~-- entity.name.function.decorator.ds
function runtimeName(): string {
    "js"
}

function make<const N: uint>(value: [uint8; N]): Buffer<N> {
//            ^^^^^ storage.modifier.ds
    Buffer.from(value)
}
