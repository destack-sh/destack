// SYNTAX TEST "source.ds"

// expressions / ranges / values
const closed = start..=end;
//                  ^^^ keyword.operator.range.ds

const full = ..;
//           ^^ keyword.operator.range.ds

const head = ..end;
//           ^^ keyword.operator.range.ds

const inclusiveHead = ..=end;
//                    ^^^ keyword.operator.range.ds

const tail = start..;
//                ^^ keyword.operator.range.ds

// expressions / ranges / subscripts
const window = values[1..4];
//                     ^^ keyword.operator.range.ds

const suffix = values[2..];

// expressions / ranges / patterns
match (digit) {
// <- keyword.control.conditional.ds
    ..0 => "negative",
    0..=9 => "decimal",
    10.. => "large",
    _ => "other",
}
