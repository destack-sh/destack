// SYNTAX TEST "source.ds"

// expressions / using / sync / using binding introduces a resource name
class File implements Dispose {
// <- keyword.declaration.type.ds
    dispose(): void {}
}

using file = new File();
file satisfies File;

// expressions / using / sync / using expression yields void
class File implements Dispose {
    dispose(): void {}
}

let result: void = using file = new File();
result satisfies void;

// expressions / using / sync / using accepts nullish resources
using missing = null;
using absent = undefined;

// expressions / using / sync / using rejects non disposable values
using value = 1;

// expressions / using / sync / using rejects async-only resources
class Connection implements AsyncDispose {
    asyncDispose(): Promise<void> {
        Promise.resolve()
    }
}

using connection = new Connection();
