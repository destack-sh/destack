// SYNTAX TEST "source.ds"

// expressions / literals / character / character literal yields character type
const value = 'a';
value satisfies char;

// expressions / literals / character / character literal does not widen to string
const value: char = 'a';
const text: string = value;

// expressions / literals / character / character literals are not assignable to integers
const value: int32 = 'a';

// expressions / literals / character / character literals work with character unions
const value: char | string = 'a';
value satisfies char | string;
