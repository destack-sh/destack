// SYNTAX TEST "source.ds"

// expressions / literals / number / integer literal
const x: number = 42;
// <- keyword.declaration.type.ds

// expressions / literals / number / negative integer
const x: number = -42;

// expressions / literals / number / zero
const x: number = 0;

// expressions / literals / number / hex literal
const x: number = 0xff;

// expressions / literals / number / octal literal
const x: number = 0o17;

// expressions / literals / number / binary literal
const x: number = 0b1010;

// expressions / literals / number / float literal
const x: number = 3.14;

// expressions / literals / number / negative float
const x: number = -3.14;

// expressions / literals / number / integer literals stay exact
const x = 123;
x satisfies 123;
x satisfies number;

// expressions / literals / number / float literals stay exact
const x = 3.14;
x satisfies 3.14;
x satisfies number;

// expressions / literals / number / number does not satisfy string
const x = 123;
x satisfies string;

// expressions / literals / number / number toFixed resolves
const value = 12;
const fixed = value.toFixed(2);
fixed satisfies string;
