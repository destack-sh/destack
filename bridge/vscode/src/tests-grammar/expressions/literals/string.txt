// SYNTAX TEST "source.ds"

// expressions / literals / string / string literal
const x: string = "hello";
// <- keyword.declaration.type.ds

// expressions / literals / string / empty string
const x: string = "";

// expressions / literals / string / string with spaces
const x: string = "hello world";

// expressions / literals / string / string literals stay exact
const x = "hello";
x satisfies "hello";
x satisfies string;

// expressions / literals / string / empty string literals stay exact
const x = "";
x satisfies "";
x satisfies string;

// expressions / literals / string / string does not satisfy number
const x = "hello";
x satisfies number;

// expressions / literals / string / string length resolves
const value = "hello";
value.length satisfies int32;

// expressions / literals / string / string toUpperCase resolves
const value = "hello";
const text = value.toUpperCase();
text satisfies string;
