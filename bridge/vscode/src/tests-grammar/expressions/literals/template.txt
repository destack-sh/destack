// SYNTAX TEST "source.ds"

// expressions / literals / template / template literal yields string
let name = "Ada";
// <- keyword.declaration.type.ds
let greeting = `hello ${name}`;

// expressions / literals / template / template literal is assignable to string
let greeting: string = `hello`;

// expressions / literals / template / template literal is not assignable to number
let value: number = `hello`;

// expressions / literals / template / tagged template uses tag return type
declare function tag(strings: TemplateStringsArray, value: int32): boolean;

let result: boolean = tag`value=${1}`;

// expressions / literals / template / tagged template enforces argument types
declare function tag(strings: TemplateStringsArray, value: string): string;

let result = tag`value=${1}`;

// expressions / literals / template / tagged template return type is not widened
declare function tag(strings: TemplateStringsArray): int32;

let result: string = tag`value`;
