// SYNTAX TEST "source.ds"

// expressions / values / block / if blocks yield branch values
const enabled = true;
// <- keyword.declaration.type.ds
const value = if (enabled) {
    1
} else {
    2
};

value satisfies int32;

// expressions / values / block / blocks without tail expressions yield void
const enabled = true;
const value: int32 = if (enabled) {
    let item = 1;
} else {
    let item = 2;
};

// expressions / values / block / function bodies return tail expressions
function add(left: int32, right: int32): int32 {
    left + right
}

add(1, 2) satisfies int32;
