// SYNTAX TEST "source.ds"

// expressions / functions / implicit / parameters require types
function handle(value) {}
// <- keyword.declaration.type.ds

// expressions / functions / implicit / parameter defaults infer parameter types
function handle(value = 1) {
    value satisfies number;
}

// expressions / functions / implicit / uninitialized bindings require annotations
let pending;
