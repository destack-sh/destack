// SYNTAX TEST "source.ds"

// expressions / functions / returns / block bodies reject missing returns
function example(value: number): number {
// <- keyword.declaration.type.ds
    if (value > 0) {
        return value;
    }
}

// expressions / functions / returns / tail expressions satisfy return types
function example(value: number): number {
    if (value > 0) {
        return value;
    }
    value + 1
}
