// SYNTAX TEST "source.ds"

// expressions / functions / parameters / positional arguments map to parameters
function sum(a: number, b: number): number {
// <- keyword.declaration.type.ds
    return a + b;
}

const value = sum(1, 2);
value satisfies number;

// expressions / functions / parameters / defaulted parameters are optional
function greet(name: string = "hi"): string {
    return name;
}

greet() satisfies string;
greet("hello") satisfies string;

// expressions / functions / parameters / defaulted parameter types are enforced
function repeat(value: string = "hi"): string {
    return value;
}

repeat("ok");
repeat(1);

// expressions / functions / parameters / tuple rest parameters infer element types
function pair<T, U>(...values: (T, U)): (T, U) {
    return (values[0], values[1]);
}

const result = pair(1, "hi");
result satisfies (int, string);

// expressions / functions / parameters / function parameters cannot be parameter properties
function build(public value: number) {}

// expressions / functions / parameters / optional pattern parameters are rejected
interface Payload {
    value: string;
}

function handle({ value }?: Payload) {
    let _ = value;
}
