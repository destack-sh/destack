// SYNTAX TEST "source.ds"

// types / extensions / unions / inherent extensions resolve on unions
export struct Cat {
// <- keyword.declaration.import.ds
    name: string;
}
export struct Dog {
    name: string;
}

extension of Cat {
    speak(): string {
        "meow"
    }
}

extension of Dog {
    speak(): string {
        "woof"
    }
}

// types / extensions / unions / inherent extensions resolve on unions
import { Cat, Dog } from "./types.ds";

declare function getPet(): Cat | Dog;

const sound = getPet().speak();
sound satisfies string;

// types / extensions / unions / named extensions require imports on unions
export struct Cat {
    name: string;
}
export struct Dog {
    name: string;
}

// types / extensions / unions / named extensions require imports on unions
import { Cat, Dog } from "./types.ds";

export extension CatTalk of Cat {
    speak(): string {
        "meow"
    }
}

export extension DogTalk of Dog {
    speak(): string {
        "woof"
    }
}

// types / extensions / unions / named extensions require imports on unions
import { Cat, Dog } from "./types.ds";

declare function getPet(): Cat | Dog;

getPet().speak();

// types / extensions / unions / named extension imports resolve unions
export struct Cat {
    name: string;
}
export struct Dog {
    name: string;
}

// types / extensions / unions / named extension imports resolve unions
import { Cat, Dog } from "./types.ds";

export extension CatTalk of Cat {
    speak(): string {
        "meow"
    }
}

export extension DogTalk of Dog {
    speak(): string {
        "woof"
    }
}

// types / extensions / unions / named extension imports resolve unions
import { CatTalk, DogTalk } from "./extensions.ds";
import { Cat, Dog } from "./types.ds";

declare function getPet(): Cat | Dog;

const sound = getPet().speak();
sound satisfies string;

// types / extensions / unions / re-exported named extensions resolve unions
export struct Cat {
    name: string;
}
export struct Dog {
    name: string;
}

// types / extensions / unions / re-exported named extensions resolve unions
import { Cat, Dog } from "./types.ds";

export extension CatTalk of Cat {
    speak(): string {
        "meow"
    }
}

export extension DogTalk of Dog {
    speak(): string {
        "woof"
    }
}

// types / extensions / unions / re-exported named extensions resolve unions
export { CatTalk, DogTalk } from "./extensions.ds";

// types / extensions / unions / re-exported named extensions resolve unions
import { CatTalk, DogTalk } from "./barrel.ds";
import { Cat, Dog } from "./types.ds";

declare function getPet(): Cat | Dog;

const sound = getPet().speak();
sound satisfies string;

// types / extensions / unions / namespace imports do not activate named extensions
export struct Cat {
    name: string;
}
export struct Dog {
    name: string;
}

// types / extensions / unions / namespace imports do not activate named extensions
import { Cat, Dog } from "./types.ds";

export extension CatTalk of Cat {
    speak(): string {
        "meow"
    }
}

export extension DogTalk of Dog {
    speak(): string {
        "woof"
    }
}

// types / extensions / unions / namespace imports do not activate named extensions
import * as ext from "./extensions.ds";
import { Cat, Dog } from "./types.ds";

declare function getPet(): Cat | Dog;

getPet().speak();

// types / extensions / unions / union calls require every extension
export struct Cat {
    name: string;
}
export struct Dog {
    name: string;
}

// types / extensions / unions / union calls require every extension
import { Cat, Dog } from "./types.ds";

export extension CatTalk of Cat {
    speak(): string {
        "meow"
    }
}

export extension DogTalk of Dog {
    speak(): string {
        "woof"
    }
}

// types / extensions / unions / union calls require every extension
import { CatTalk } from "./extensions.ds";
import { Cat, Dog } from "./types.ds";

declare function getPet(): Cat | Dog;

getPet().speak();

// types / extensions / unions / renamed named extensions resolve unions
export struct Cat {
    name: string;
}
export struct Dog {
    name: string;
}

// types / extensions / unions / renamed named extensions resolve unions
import { Cat, Dog } from "./types.ds";

export extension CatTalk of Cat {
    speak(): string {
        "meow"
    }
}

export extension DogTalk of Dog {
    speak(): string {
        "woof"
    }
}

// types / extensions / unions / renamed named extensions resolve unions
import { CatTalk as CatSpeak, DogTalk as DogSpeak } from "./extensions.ds";
import { Cat, Dog } from "./types.ds";

declare function getPet(): Cat | Dog;

const sound = getPet().speak();
sound satisfies string;
