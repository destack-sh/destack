// SYNTAX TEST "source.ds"

// types / type operators / intrinsic / intrinsic types cannot be indexed
type Bad = intrinsic["foo"];
// <- keyword.declaration.type.ds

// types / type operators / intrinsic / intrinsic types cannot appear in value annotations
const value: intrinsic = "x";

// types / type operators / intrinsic / intrinsic types cannot be used in conditional operators
type Select<T> = intrinsic extends T ? true : false;

// types / type operators / intrinsic / intrinsic types cannot be passed as generic arguments
type Box<T> = T;
type Bad = Box<intrinsic>;

// types / type operators / intrinsic / intrinsic types cannot appear in union members
type Bad = intrinsic | string;
