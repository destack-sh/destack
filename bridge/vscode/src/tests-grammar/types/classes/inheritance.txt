// SYNTAX TEST "source.ds"

// types / classes / inheritance / children assign to parents
class Animal {
    name: string = "";
}

class Dog extends Animal {
    breed: string = "";
}

declare function getDog(): Dog;

const animal: Animal = getDog();

// types / classes / inheritance / final classes construct normally
final class Session {
    id: string = "";
}

const session = new Session();
session satisfies Session;

// types / classes / inheritance / final classes reject inheritance
final class Session {
    id: string = "";
}

class DerivedSession extends Session {}

// types / classes / inheritance / parents do not assign to children
class Animal {
    name: string = "";
}

class Dog extends Animal {
    breed: string = "";
}

declare function getAnimal(): Animal;

const dog: Dog = getAnimal();

// types / classes / inheritance / children satisfy parents
class Animal {
    name: string = "";
}

class Dog extends Animal {
    breed: string = "";
}

declare function getDog(): Dog;

getDog() satisfies Animal;

// types / classes / inheritance / parents do not satisfy children
class Animal {
    name: string = "";
}

class Dog extends Animal {
    breed: string = "";
}

declare function getAnimal(): Animal;

getAnimal() satisfies Dog;

// types / classes / inheritance / grandchildren assign to grandparents
class Animal {
    name: string = "";
}

class Dog extends Animal {
    breed: string = "";
}

class Labrador extends Dog {
    color: string = "";
}

declare function getLabrador(): Labrador;

const animal: Animal = getLabrador();

// types / classes / inheritance / grandchildren assign to parents
class Animal {
    name: string = "";
}

class Dog extends Animal {
    breed: string = "";
}

class Labrador extends Dog {
    color: string = "";
}

declare function getLabrador(): Labrador;

const dog: Dog = getLabrador();

// types / classes / inheritance / grandparents do not assign to grandchildren
class Animal {
    name: string = "";
}

class Dog extends Animal {
    breed: string = "";
}

class Labrador extends Dog {
    color: string = "";
}

declare function getAnimal(): Animal;

const labrador: Labrador = getAnimal();

// types / classes / inheritance / children pass to parent parameters
class Animal {
    name: string = "";
}

class Dog extends Animal {
    breed: string = "";
}

function acceptAnimal(a: Animal): void {}

declare function getDog(): Dog;

acceptAnimal(getDog());

// types / classes / inheritance / child parameters reject parents
class Animal {
    name: string = "";
}

class Dog extends Animal {
    breed: string = "";
}

function acceptDog(d: Dog): void {}

declare function getAnimal(): Animal;

acceptDog(getAnimal());

// types / classes / inheritance / siblings do not assign to each other
class Animal {
    name: string = "";
}

class Dog extends Animal {
    breed: string = "";
}

class Cat extends Animal {
    whiskers: number = 0;
}

declare function getDog(): Dog;

const cat: Cat = getDog();
