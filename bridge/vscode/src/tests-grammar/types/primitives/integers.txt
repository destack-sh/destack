// SYNTAX TEST "source.ds"

// types / primitives / integers / integer literals satisfy fitting widths
7 satisfies uint3;
7 satisfies int4;

// types / primitives / integers / integer literals reject narrow widths
7 satisfies uint2;

// types / primitives / integers / intN accepts in-range literals
let small: int3 = 3;

// types / primitives / integers / intN rejects out-of-range literals
let tooLarge: int3 = 4;

// types / primitives / integers / uintN accepts in-range literals
let small: uint3 = 7;

// types / primitives / integers / uintN rejects out-of-range literals
let tooLarge: uint3 = 8;

// types / primitives / integers / pointer-sized integers accept literals
let signed: isize = 0;
let unsigned: usize = 1;
