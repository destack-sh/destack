// SYNTAX TEST "source.ds"

// types / primitives / nullish / null has its own type
const value: null = null;
// <- keyword.declaration.type.ds
value satisfies null;

// types / primitives / nullish / null rejects non-nullish targets
const value: string = null;

// types / primitives / nullish / undefined has its own type
const value: undefined = undefined;
value satisfies undefined;

// types / primitives / nullish / undefined rejects non-nullish targets
const value: string = undefined;

// types / primitives / nullish / void marks no useful return value
function log(message: string): void {
    print(message);
}
