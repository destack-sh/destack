// SYNTAX TEST "source.ds"

// types / primitives / floats / float32 accepts literals
let value: float32 = 1.5;
// <- keyword.declaration.type.ds

// types / primitives / floats / float32 widens to float64
let small: float32 = 1.5;
let wide: float64 = small;

// types / primitives / floats / float64 does not narrow to float32
let wide: float64 = 1.5;
let narrow: float32 = wide;

// types / primitives / floats / floats do not implicitly convert to integers
let value: int32 = 1.5;

// types / primitives / floats / float arithmetic stays float-typed
let value: float32 = 1.5 + 2.5;
value satisfies float32;
