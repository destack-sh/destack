// SYNTAX TEST "source.ds"

// types / arrays / slices / bracket slice syntax names slice types
declare const values: [int32];
// <- keyword.declaration.type.ds
values satisfies Slice<int32>;

// types / arrays / slices / slice alias matches bracket syntax
declare const values: Slice<int32>;
values satisfies [int32];

// types / arrays / slices / borrowed slices keep the slice view shape
declare const values: [int32];

const borrow: &[int32] = &values;
borrow satisfies &[int32];

// types / arrays / slices / slice indexing yields element types
declare const values: [int32];

const first = values[0];
first satisfies int32;

// types / arrays / slices / slices are not fixed arrays
declare const values: [int32];
const fixed: [int32; 2] = values;
