// SYNTAX TEST "source.ds"

// types / utilities / uncapitalize / uncapitalize converts first character
type Value = Uncapitalize<"Hello">;
// <- keyword.declaration.type.ds

const ok: Value = "hello";

// types / utilities / uncapitalize / uncapitalize distributes over unions
type Value = Uncapitalize<"Yes" | "No">;

const ok: Value = "yes";
const ok2: Value = "no";

// types / utilities / uncapitalize / uncapitalize rejects original casing
type Value = Uncapitalize<"Hello">;

const bad: Value = "Hello";
