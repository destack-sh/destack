// SYNTAX TEST "source.ds"

// types / utilities / omit this / OmitThisParameter removes explicit this
type Fn = OmitThisParameter<(this: { id: string }, value: number) => string>;
// <- keyword.declaration.type.ds

const fn: Fn = (value) => String(value);
fn(1) satisfies string;

// types / utilities / omit this / OmitThisParameter keeps argument types
type Fn = OmitThisParameter<(this: { id: string }, value: number) => string>;

const fn: Fn = (value) => String(value);
fn("bad");

// types / utilities / omit this / OmitThisParameter keeps functions without this
type Fn = OmitThisParameter<(value: number) => string>;

const fn: Fn = (value) => String(value);
fn(1) satisfies string;
