// SYNTAX TEST "source.ds"

// types / utilities / nonnullable / NonNullable removes nullish
type MaybeName = string | null | undefined;
// <- keyword.declaration.type.ds
type Name = NonNullable<MaybeName>;

const ok: Name = "Ada";
ok satisfies Name;

// types / utilities / nonnullable / NonNullable rejects nullish values
type MaybeName = string | null | undefined;
type Name = NonNullable<MaybeName>;

const bad: Name = null;

// types / utilities / nonnullable / NonNullable with never yields never
type NeverValue = NonNullable<never>;

let bad: NeverValue = "no";
