// SYNTAX TEST "source.ds"

// types / utilities / extract / extract keeps matching members
type Letters = "a" | "b" | "c";
type OnlyAorB = Extract<Letters, "a" | "b">;

const ok: OnlyAorB = "a";
ok satisfies OnlyAorB;
const ok2: OnlyAorB = "b";
ok2 satisfies OnlyAorB;

// types / utilities / extract / extract rejects non members
type Letters = "a" | "b" | "c";
type OnlyAorB = Extract<Letters, "a" | "b">;

const bad: OnlyAorB = "c";

// types / utilities / extract / extract with never yields never
type NeverLetters = Extract<never, "a">;

let bad: NeverLetters = "a";
