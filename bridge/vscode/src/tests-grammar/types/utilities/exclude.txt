// SYNTAX TEST "source.ds"

// types / utilities / exclude / exclude distributes over unions
type Letters = "a" | "b" | "c";
type Only = Exclude<Letters, "b">;

const ok: Only = "a";
ok satisfies Only;

// types / utilities / exclude / exclude rejects removed members
type Letters = "a" | "b" | "c";
type Only = Exclude<Letters, "b">;

const bad: Only = "b";

// types / utilities / exclude / exclude with never yields never
type NeverLetters = Exclude<never, "b">;

let bad: NeverLetters = "b";
