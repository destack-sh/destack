// SYNTAX TEST "source.ds"

// types / utilities / return type / ReturnType extracts return values
type Value = ReturnType<() => string>;
// <- keyword.declaration.type.ds

const ok: Value = "ready";
ok satisfies string;

// types / utilities / return type / ReturnType rejects wrong values
type Value = ReturnType<() => string>;

const bad: Value = 1;

// types / utilities / return type / ReturnType keeps unions
type Value = ReturnType<() => "a" | "b">;

const ok: Value = "a";
const ok2: Value = "b";
