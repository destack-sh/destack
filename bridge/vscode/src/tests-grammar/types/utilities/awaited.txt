// SYNTAX TEST "source.ds"

// types / utilities / awaited / awaited keeps non promises
type Value = Awaited<string>;
// <- keyword.declaration.type.ds

const ok: Value = "ready";
ok satisfies string;

// types / utilities / awaited / awaited unwraps promise-like values
type Value = Awaited<PromiseLike<string>>;

const ok: Value = "ready";
ok satisfies string;

// types / utilities / awaited / awaited unwraps nested promise-like values
type Value = Awaited<PromiseLike<PromiseLike<string>>>;

const ok: Value = "ready";
ok satisfies string;

// types / utilities / awaited / awaited rejects unresolved promise values
type Value = Awaited<PromiseLike<string>>;

const bad: Value = Promise.resolve("ready");

// types / utilities / awaited / awaited preserves nullish values
type Value = Awaited<null | undefined>;

const ok: Value = null;
const ok2: Value = undefined;
