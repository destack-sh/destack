// SYNTAX TEST "source.ds"

// types / utilities / capitalize / capitalize converts first character
type Value = Capitalize<"hello">;

const ok: Value = "Hello";

// types / utilities / capitalize / capitalize distributes over unions
type Value = Capitalize<"yes" | "no">;

const ok: Value = "Yes";
const ok2: Value = "No";

// types / utilities / capitalize / capitalize rejects original casing
type Value = Capitalize<"hello">;

const bad: Value = "hello";
