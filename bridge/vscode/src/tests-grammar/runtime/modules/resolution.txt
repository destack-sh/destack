// SYNTAX TEST "source.ds"

// runtime / modules / resolution / resolves ds module without extension
import { value } from "./mod";
// <- keyword.declaration.import.ds

value satisfies int32;

// runtime / modules / resolution / resolves ds module without extension
export const value: int32 = 1;

// runtime / modules / resolution / resolves inferred module without extension
import { value } from "./mod";

value satisfies number;

// runtime / modules / resolution / resolves inferred module without extension
export const value = 1;

// runtime / modules / resolution / resolves declaration modules without extension
import type { User } from "./types";

type Alias = User;
const value: Alias = { name: "Ada" };
value.name satisfies string;

// runtime / modules / resolution / resolves declaration modules without extension
export interface User {
    name: string;
}

// runtime / modules / resolution / resolves declaration value exports
import { version } from "./types";

version satisfies string;

// runtime / modules / resolution / resolves declaration value exports
export const version: string;

// runtime / modules / resolution / resolves directory index module
import { value } from "./dir";

value satisfies string;

// runtime / modules / resolution / resolves directory index module
export const value: string = "ok";
