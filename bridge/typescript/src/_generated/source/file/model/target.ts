/* generated by destack_bridge_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, jsonBigint, jsonField, jsonObject } from "../../../../protocol/serde.js";
import type { PackageId } from "./package.js";
import { decodePackageId, encodePackageId, fromJsonPackageId, toJsonPackageId } from "./package.js";

/** Stable key for one target within a package. */
export type TargetKey = bigint;

export const TargetKey = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: TargetKey): void {
        encodeTargetKey(writer, value);
    },

    /** Decode one TargetKey. */
    decode(reader: BinaryReader): TargetKey {
        return decodeTargetKey(reader);
    },

    /** Return this value as JSON. */
    toJson(value: TargetKey): Json {
        return toJsonTargetKey(value);
    },

    /** Return one TargetKey from one JSON value. */
    fromJson(value: Json): TargetKey {
        return fromJsonTargetKey(value);
    },
};

/** Encode one TargetKey. */
export function encodeTargetKey(writer: BinaryWriter, value: TargetKey): void {
    writer.writeUnsigned(value);
}

/** Decode one TargetKey. */
export function decodeTargetKey(reader: BinaryReader): TargetKey {
    return reader.readUnsigned();
}

/** Return one JSON value for one TargetKey. */
export function toJsonTargetKey(value: TargetKey): Json {
    return value.toString();
}

/** Return one TargetKey from one JSON value. */
export function fromJsonTargetKey(value: Json): TargetKey {
    return jsonBigint(value);
}

/** Unique identifier for a build target within a package. */
export type TargetId = {
    /** The owning package id. */
    readonly packageId: PackageId;
    /** The stable key for this target within its package. */
    readonly targetKey: TargetKey;
};

export const TargetId = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: TargetId): void {
        encodeTargetId(writer, value);
    },

    /** Decode one TargetId. */
    decode(reader: BinaryReader): TargetId {
        return decodeTargetId(reader);
    },

    /** Return this value as JSON. */
    toJson(value: TargetId): Json {
        return toJsonTargetId(value);
    },

    /** Return one TargetId from one JSON value. */
    fromJson(value: Json): TargetId {
        return fromJsonTargetId(value);
    },
};

/** Encode one TargetId. */
export function encodeTargetId(writer: BinaryWriter, value: TargetId): void {
    encodePackageId(writer, value.packageId);
    encodeTargetKey(writer, value.targetKey);
}

/** Decode one TargetId. */
export function decodeTargetId(reader: BinaryReader): TargetId {
    const packageId = decodePackageId(reader);
    const targetKey = decodeTargetKey(reader);

    return {
        packageId,
        targetKey,
    };
}

/** Return one JSON value for one TargetId. */
export function toJsonTargetId(value: TargetId): Json {
    return {
        packageId: toJsonPackageId(value.packageId),
        targetKey: toJsonTargetKey(value.targetKey),
    };
}

/** Return one TargetId from one JSON value. */
export function fromJsonTargetId(value: Json): TargetId {
    const object = jsonObject(value);

    return {
        packageId: fromJsonPackageId(jsonField(object, "packageId")),
        targetKey: fromJsonTargetKey(jsonField(object, "targetKey")),
    };
}
