/* generated by destack_bridge_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, jsonString } from "../../../../protocol/serde.js";

/** The type of line ending to apply to the printed input. */
export type LineEnding = "lineFeed" | "carriageReturnLineFeed" | "carriageReturn";

export const LineEnding = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: LineEnding): void {
        encodeLineEnding(writer, value);
    },

    /** Decode one LineEnding. */
    decode(reader: BinaryReader): LineEnding {
        return decodeLineEnding(reader);
    },

    /** Return this value as JSON. */
    toJson(value: LineEnding): Json {
        return toJsonLineEnding(value);
    },

    /** Return one LineEnding from one JSON value. */
    fromJson(value: Json): LineEnding {
        return fromJsonLineEnding(value);
    },
};

/** Encode one LineEnding. */
export function encodeLineEnding(writer: BinaryWriter, value: LineEnding): void {
    switch (value) {
        case "lineFeed":
            writer.writeUnsigned(0);
            return;
        case "carriageReturnLineFeed":
            writer.writeUnsigned(1);
            return;
        case "carriageReturn":
            writer.writeUnsigned(2);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one LineEnding. */
export function decodeLineEnding(reader: BinaryReader): LineEnding {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "lineFeed";
        case 1:
            return "carriageReturnLineFeed";
        case 2:
            return "carriageReturn";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one LineEnding. */
export function toJsonLineEnding(value: LineEnding): Json {
    return value;
}

/** Return one LineEnding from one JSON value. */
export function fromJsonLineEnding(value: Json): LineEnding {
    const variant = jsonString(value);

    switch (variant) {
        case "lineFeed":
            return "lineFeed";
        case "carriageReturnLineFeed":
            return "carriageReturnLineFeed";
        case "carriageReturn":
            return "carriageReturn";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** The indent style. */
export type IndentStyle = "tab" | "space";

export const IndentStyle = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: IndentStyle): void {
        encodeIndentStyle(writer, value);
    },

    /** Decode one IndentStyle. */
    decode(reader: BinaryReader): IndentStyle {
        return decodeIndentStyle(reader);
    },

    /** Return this value as JSON. */
    toJson(value: IndentStyle): Json {
        return toJsonIndentStyle(value);
    },

    /** Return one IndentStyle from one JSON value. */
    fromJson(value: Json): IndentStyle {
        return fromJsonIndentStyle(value);
    },
};

/** Encode one IndentStyle. */
export function encodeIndentStyle(writer: BinaryWriter, value: IndentStyle): void {
    switch (value) {
        case "tab":
            writer.writeUnsigned(0);
            return;
        case "space":
            writer.writeUnsigned(1);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one IndentStyle. */
export function decodeIndentStyle(reader: BinaryReader): IndentStyle {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "tab";
        case 1:
            return "space";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one IndentStyle. */
export function toJsonIndentStyle(value: IndentStyle): Json {
    return value;
}

/** Return one IndentStyle from one JSON value. */
export function fromJsonIndentStyle(value: Json): IndentStyle {
    const variant = jsonString(value);

    switch (variant) {
        case "tab":
            return "tab";
        case "space":
            return "space";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}
