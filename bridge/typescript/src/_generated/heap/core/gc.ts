/* generated by destack_bridge_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, jsonBigint, jsonField, jsonInteger, jsonObject, jsonOptional } from "../../../protocol/serde.js";

/** Collector configuration for one heap. */
export type GcOptions = {
    /** The proportional heap growth target after one cycle. */
    readonly growthPercent: number;
    /** The heap trigger as a percentage of the current goal. */
    readonly triggerPercent: number;
    /** Optional soft memory limit in bytes. */
    readonly softLimitBytes?: bigint;
    /** Optional minimum heap floor in bytes. */
    readonly minimumHeapBytes?: bigint;
    /** Minimum collector work for one safepoint. */
    readonly minimumWorkBytes: number;
};

export const GcOptions = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: GcOptions): void {
        encodeGcOptions(writer, value);
    },

    /** Decode one GcOptions. */
    decode(reader: BinaryReader): GcOptions {
        return decodeGcOptions(reader);
    },

    /** Return this value as JSON. */
    toJson(value: GcOptions): Json {
        return toJsonGcOptions(value);
    },

    /** Return one GcOptions from one JSON value. */
    fromJson(value: Json): GcOptions {
        return fromJsonGcOptions(value);
    },
};

/** Encode one GcOptions. */
export function encodeGcOptions(writer: BinaryWriter, value: GcOptions): void {
    writer.writeUnsigned(value.growthPercent);
    writer.writeUnsigned(value.triggerPercent);
    writer.writeOption(value.softLimitBytes, (value2) => {
        writer.writeUnsigned(value2);
    });
    writer.writeOption(value.minimumHeapBytes, (value3) => {
        writer.writeUnsigned(value3);
    });
    writer.writeUnsigned(value.minimumWorkBytes);
}

/** Decode one GcOptions. */
export function decodeGcOptions(reader: BinaryReader): GcOptions {
    const growthPercent = reader.readNumber();
    const triggerPercent = reader.readNumber();
    const softLimitBytes = reader.readOption(() => reader.readUnsigned());
    const minimumHeapBytes = reader.readOption(() => reader.readUnsigned());
    const minimumWorkBytes = reader.readNumber();

    return {
        growthPercent,
        triggerPercent,
        ...(softLimitBytes === undefined ? {} : { softLimitBytes }),
        ...(minimumHeapBytes === undefined ? {} : { minimumHeapBytes }),
        minimumWorkBytes,
    };
}

/** Return one JSON value for one GcOptions. */
export function toJsonGcOptions(value: GcOptions): Json {
    return {
        growthPercent: value.growthPercent,
        triggerPercent: value.triggerPercent,
        ...(value.softLimitBytes === undefined ? {} : { softLimitBytes: value.softLimitBytes.toString() }),
        ...(value.minimumHeapBytes === undefined ? {} : { minimumHeapBytes: value.minimumHeapBytes.toString() }),
        minimumWorkBytes: value.minimumWorkBytes,
    };
}

/** Return one GcOptions from one JSON value. */
export function fromJsonGcOptions(value: Json): GcOptions {
    const object = jsonObject(value);

    return {
        growthPercent: jsonInteger(jsonField(object, "growthPercent")),
        triggerPercent: jsonInteger(jsonField(object, "triggerPercent")),
        softLimitBytes: jsonOptional(object, "softLimitBytes", (value) => jsonBigint(value)),
        minimumHeapBytes: jsonOptional(object, "minimumHeapBytes", (value) => jsonBigint(value)),
        minimumWorkBytes: jsonInteger(jsonField(object, "minimumWorkBytes")),
    };
}
