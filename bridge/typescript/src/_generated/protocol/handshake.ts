/* generated by destack_bridge_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, jsonBigint, jsonField, jsonObject, jsonOptional, jsonString } from "../../protocol/serde.js";
import type { ProtocolRange } from "./version.js";
import type { ProtocolVersion } from "./version.js";
import { decodeProtocolRange, encodeProtocolRange, fromJsonProtocolRange, toJsonProtocolRange } from "./version.js";
import { decodeProtocolVersion, encodeProtocolVersion, fromJsonProtocolVersion, toJsonProtocolVersion } from "./version.js";

/** Handshake request payload for protocol negotiation. */
export type HandshakeRequest = {
    /** Supported protocol range on the client. */
    readonly protocol: ProtocolRange;
    /** Client descriptor. */
    readonly client: ClientDescriptor;
    /** Client protocol limits. */
    readonly limits: ProtocolLimits;
};

export const HandshakeRequest = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: HandshakeRequest): void {
        encodeHandshakeRequest(writer, value);
    },

    /** Decode one HandshakeRequest. */
    decode(reader: BinaryReader): HandshakeRequest {
        return decodeHandshakeRequest(reader);
    },

    /** Return this value as JSON. */
    toJson(value: HandshakeRequest): Json {
        return toJsonHandshakeRequest(value);
    },

    /** Return one HandshakeRequest from one JSON value. */
    fromJson(value: Json): HandshakeRequest {
        return fromJsonHandshakeRequest(value);
    },
};

/** Encode one HandshakeRequest. */
export function encodeHandshakeRequest(writer: BinaryWriter, value: HandshakeRequest): void {
    encodeProtocolRange(writer, value.protocol);
    encodeClientDescriptor(writer, value.client);
    encodeProtocolLimits(writer, value.limits);
}

/** Decode one HandshakeRequest. */
export function decodeHandshakeRequest(reader: BinaryReader): HandshakeRequest {
    const protocol = decodeProtocolRange(reader);
    const client = decodeClientDescriptor(reader);
    const limits = decodeProtocolLimits(reader);

    return {
        protocol,
        client,
        limits,
    };
}

/** Return one JSON value for one HandshakeRequest. */
export function toJsonHandshakeRequest(value: HandshakeRequest): Json {
    return {
        protocol: toJsonProtocolRange(value.protocol),
        client: toJsonClientDescriptor(value.client),
        limits: toJsonProtocolLimits(value.limits),
    };
}

/** Return one HandshakeRequest from one JSON value. */
export function fromJsonHandshakeRequest(value: Json): HandshakeRequest {
    const object = jsonObject(value);

    return {
        protocol: fromJsonProtocolRange(jsonField(object, "protocol")),
        client: fromJsonClientDescriptor(jsonField(object, "client")),
        limits: fromJsonProtocolLimits(jsonField(object, "limits")),
    };
}

/** Client descriptor sent during handshake negotiation. */
export type ClientDescriptor = {
    /** Client name (cli, lsp, editor, etc.). */
    readonly name: string;
    /** Client version string. */
    readonly version: string;
    /** Optional build identifier. */
    readonly build?: string;
};

export const ClientDescriptor = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ClientDescriptor): void {
        encodeClientDescriptor(writer, value);
    },

    /** Decode one ClientDescriptor. */
    decode(reader: BinaryReader): ClientDescriptor {
        return decodeClientDescriptor(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ClientDescriptor): Json {
        return toJsonClientDescriptor(value);
    },

    /** Return one ClientDescriptor from one JSON value. */
    fromJson(value: Json): ClientDescriptor {
        return fromJsonClientDescriptor(value);
    },
};

/** Encode one ClientDescriptor. */
export function encodeClientDescriptor(writer: BinaryWriter, value: ClientDescriptor): void {
    writer.writeString(value.name);
    writer.writeString(value.version);
    writer.writeOption(value.build, (value2) => {
        writer.writeString(value2);
    });
}

/** Decode one ClientDescriptor. */
export function decodeClientDescriptor(reader: BinaryReader): ClientDescriptor {
    const name = reader.readString();
    const version = reader.readString();
    const build = reader.readOption(() => reader.readString());

    return {
        name,
        version,
        ...(build === undefined ? {} : { build }),
    };
}

/** Return one JSON value for one ClientDescriptor. */
export function toJsonClientDescriptor(value: ClientDescriptor): Json {
    return {
        name: value.name,
        version: value.version,
        ...(value.build === undefined ? {} : { build: value.build }),
    };
}

/** Return one ClientDescriptor from one JSON value. */
export function fromJsonClientDescriptor(value: Json): ClientDescriptor {
    const object = jsonObject(value);

    return {
        name: jsonString(jsonField(object, "name")),
        version: jsonString(jsonField(object, "version")),
        build: jsonOptional(object, "build", (value) => jsonString(value)),
    };
}

/** Negotiated protocol limits. */
export type ProtocolLimits = {
    /** Maximum frame size in bytes. */
    readonly maxFrameBytes: bigint;
    /** Maximum payload size in bytes. */
    readonly maxPayloadBytes: bigint;
};

export const ProtocolLimits = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ProtocolLimits): void {
        encodeProtocolLimits(writer, value);
    },

    /** Decode one ProtocolLimits. */
    decode(reader: BinaryReader): ProtocolLimits {
        return decodeProtocolLimits(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ProtocolLimits): Json {
        return toJsonProtocolLimits(value);
    },

    /** Return one ProtocolLimits from one JSON value. */
    fromJson(value: Json): ProtocolLimits {
        return fromJsonProtocolLimits(value);
    },
};

/** Encode one ProtocolLimits. */
export function encodeProtocolLimits(writer: BinaryWriter, value: ProtocolLimits): void {
    writer.writeUnsigned(value.maxFrameBytes);
    writer.writeUnsigned(value.maxPayloadBytes);
}

/** Decode one ProtocolLimits. */
export function decodeProtocolLimits(reader: BinaryReader): ProtocolLimits {
    const maxFrameBytes = reader.readUnsigned();
    const maxPayloadBytes = reader.readUnsigned();

    return {
        maxFrameBytes,
        maxPayloadBytes,
    };
}

/** Return one JSON value for one ProtocolLimits. */
export function toJsonProtocolLimits(value: ProtocolLimits): Json {
    return {
        maxFrameBytes: value.maxFrameBytes.toString(),
        maxPayloadBytes: value.maxPayloadBytes.toString(),
    };
}

/** Return one ProtocolLimits from one JSON value. */
export function fromJsonProtocolLimits(value: Json): ProtocolLimits {
    const object = jsonObject(value);

    return {
        maxFrameBytes: jsonBigint(jsonField(object, "maxFrameBytes")),
        maxPayloadBytes: jsonBigint(jsonField(object, "maxPayloadBytes")),
    };
}

/** Handshake response payload for protocol negotiation. */
export type HandshakeResponse = {
    /** Selected protocol version. */
    readonly protocol: ProtocolVersion;
    /** Server descriptor. */
    readonly server: ServerDescriptor;
    /** Negotiated protocol limits. */
    readonly limits: ProtocolLimits;
};

export const HandshakeResponse = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: HandshakeResponse): void {
        encodeHandshakeResponse(writer, value);
    },

    /** Decode one HandshakeResponse. */
    decode(reader: BinaryReader): HandshakeResponse {
        return decodeHandshakeResponse(reader);
    },

    /** Return this value as JSON. */
    toJson(value: HandshakeResponse): Json {
        return toJsonHandshakeResponse(value);
    },

    /** Return one HandshakeResponse from one JSON value. */
    fromJson(value: Json): HandshakeResponse {
        return fromJsonHandshakeResponse(value);
    },
};

/** Encode one HandshakeResponse. */
export function encodeHandshakeResponse(writer: BinaryWriter, value: HandshakeResponse): void {
    encodeProtocolVersion(writer, value.protocol);
    encodeServerDescriptor(writer, value.server);
    encodeProtocolLimits(writer, value.limits);
}

/** Decode one HandshakeResponse. */
export function decodeHandshakeResponse(reader: BinaryReader): HandshakeResponse {
    const protocol = decodeProtocolVersion(reader);
    const server = decodeServerDescriptor(reader);
    const limits = decodeProtocolLimits(reader);

    return {
        protocol,
        server,
        limits,
    };
}

/** Return one JSON value for one HandshakeResponse. */
export function toJsonHandshakeResponse(value: HandshakeResponse): Json {
    return {
        protocol: toJsonProtocolVersion(value.protocol),
        server: toJsonServerDescriptor(value.server),
        limits: toJsonProtocolLimits(value.limits),
    };
}

/** Return one HandshakeResponse from one JSON value. */
export function fromJsonHandshakeResponse(value: Json): HandshakeResponse {
    const object = jsonObject(value);

    return {
        protocol: fromJsonProtocolVersion(jsonField(object, "protocol")),
        server: fromJsonServerDescriptor(jsonField(object, "server")),
        limits: fromJsonProtocolLimits(jsonField(object, "limits")),
    };
}

/** Server descriptor sent during handshake negotiation. */
export type ServerDescriptor = {
    /** Server name. */
    readonly name: string;
    /** Server version string. */
    readonly version: string;
    /** Optional build identifier. */
    readonly build?: string;
};

export const ServerDescriptor = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ServerDescriptor): void {
        encodeServerDescriptor(writer, value);
    },

    /** Decode one ServerDescriptor. */
    decode(reader: BinaryReader): ServerDescriptor {
        return decodeServerDescriptor(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ServerDescriptor): Json {
        return toJsonServerDescriptor(value);
    },

    /** Return one ServerDescriptor from one JSON value. */
    fromJson(value: Json): ServerDescriptor {
        return fromJsonServerDescriptor(value);
    },
};

/** Encode one ServerDescriptor. */
export function encodeServerDescriptor(writer: BinaryWriter, value: ServerDescriptor): void {
    writer.writeString(value.name);
    writer.writeString(value.version);
    writer.writeOption(value.build, (value2) => {
        writer.writeString(value2);
    });
}

/** Decode one ServerDescriptor. */
export function decodeServerDescriptor(reader: BinaryReader): ServerDescriptor {
    const name = reader.readString();
    const version = reader.readString();
    const build = reader.readOption(() => reader.readString());

    return {
        name,
        version,
        ...(build === undefined ? {} : { build }),
    };
}

/** Return one JSON value for one ServerDescriptor. */
export function toJsonServerDescriptor(value: ServerDescriptor): Json {
    return {
        name: value.name,
        version: value.version,
        ...(value.build === undefined ? {} : { build: value.build }),
    };
}

/** Return one ServerDescriptor from one JSON value. */
export function fromJsonServerDescriptor(value: Json): ServerDescriptor {
    const object = jsonObject(value);

    return {
        name: jsonString(jsonField(object, "name")),
        version: jsonString(jsonField(object, "version")),
        build: jsonOptional(object, "build", (value) => jsonString(value)),
    };
}
