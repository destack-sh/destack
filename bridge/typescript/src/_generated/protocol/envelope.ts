/* generated by destack_bridge_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, jsonBigint, jsonField, jsonInteger, jsonObject, jsonOptional, jsonString } from "../../protocol/serde.js";
import type { WorkspaceNotification } from "./notification.js";
import type { WorkspaceRequest } from "./request.js";
import type { WorkspaceResponse } from "./response.js";
import { decodeWorkspaceNotification, encodeWorkspaceNotification, fromJsonWorkspaceNotification, toJsonWorkspaceNotification } from "./notification.js";
import { decodeWorkspaceRequest, encodeWorkspaceRequest, fromJsonWorkspaceRequest, toJsonWorkspaceRequest } from "./request.js";
import { decodeWorkspaceResponse, encodeWorkspaceResponse, fromJsonWorkspaceResponse, toJsonWorkspaceResponse } from "./response.js";

/** Request envelope with identifier and payload. */
export type ProtocolRequest = {
    /** Unique request id. */
    readonly id: RequestId;
    /** Request options. */
    readonly options: RequestOptions;
    /** Request payload. */
    readonly payload: WorkspaceRequest;
};

export const ProtocolRequest = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ProtocolRequest): void {
        encodeProtocolRequest(writer, value);
    },

    /** Decode one ProtocolRequest. */
    decode(reader: BinaryReader): ProtocolRequest {
        return decodeProtocolRequest(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ProtocolRequest): Json {
        return toJsonProtocolRequest(value);
    },

    /** Return one ProtocolRequest from one JSON value. */
    fromJson(value: Json): ProtocolRequest {
        return fromJsonProtocolRequest(value);
    },
};

/** Encode one ProtocolRequest. */
export function encodeProtocolRequest(writer: BinaryWriter, value: ProtocolRequest): void {
    encodeRequestId(writer, value.id);
    encodeRequestOptions(writer, value.options);
    encodeWorkspaceRequest(writer, value.payload);
}

/** Decode one ProtocolRequest. */
export function decodeProtocolRequest(reader: BinaryReader): ProtocolRequest {
    const id = decodeRequestId(reader);
    const options = decodeRequestOptions(reader);
    const payload = decodeWorkspaceRequest(reader);

    return {
        id,
        options,
        payload,
    };
}

/** Return one JSON value for one ProtocolRequest. */
export function toJsonProtocolRequest(value: ProtocolRequest): Json {
    return {
        id: toJsonRequestId(value.id),
        options: toJsonRequestOptions(value.options),
        payload: toJsonWorkspaceRequest(value.payload),
    };
}

/** Return one ProtocolRequest from one JSON value. */
export function fromJsonProtocolRequest(value: Json): ProtocolRequest {
    const object = jsonObject(value);

    return {
        id: fromJsonRequestId(jsonField(object, "id")),
        options: fromJsonRequestOptions(jsonField(object, "options")),
        payload: fromJsonWorkspaceRequest(jsonField(object, "payload")),
    };
}

/** Unique identifier for protocol requests. */
export type RequestId = bigint;

export const RequestId = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: RequestId): void {
        encodeRequestId(writer, value);
    },

    /** Decode one RequestId. */
    decode(reader: BinaryReader): RequestId {
        return decodeRequestId(reader);
    },

    /** Return this value as JSON. */
    toJson(value: RequestId): Json {
        return toJsonRequestId(value);
    },

    /** Return one RequestId from one JSON value. */
    fromJson(value: Json): RequestId {
        return fromJsonRequestId(value);
    },
};

/** Encode one RequestId. */
export function encodeRequestId(writer: BinaryWriter, value: RequestId): void {
    writer.writeUnsigned(value);
}

/** Decode one RequestId. */
export function decodeRequestId(reader: BinaryReader): RequestId {
    return reader.readUnsigned();
}

/** Return one JSON value for one RequestId. */
export function toJsonRequestId(value: RequestId): Json {
    return value.toString();
}

/** Return one RequestId from one JSON value. */
export function fromJsonRequestId(value: Json): RequestId {
    return jsonBigint(value);
}

/** Request options for protocol calls. */
export type RequestOptions = {
    /** Optional timeout in milliseconds. */
    readonly timeoutMs?: bigint;
    /** Optional priority, lower is higher priority. */
    readonly priority?: number;
    /** Optional trace id for correlation. */
    readonly traceId?: string;
};

export const RequestOptions = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: RequestOptions): void {
        encodeRequestOptions(writer, value);
    },

    /** Decode one RequestOptions. */
    decode(reader: BinaryReader): RequestOptions {
        return decodeRequestOptions(reader);
    },

    /** Return this value as JSON. */
    toJson(value: RequestOptions): Json {
        return toJsonRequestOptions(value);
    },

    /** Return one RequestOptions from one JSON value. */
    fromJson(value: Json): RequestOptions {
        return fromJsonRequestOptions(value);
    },
};

/** Encode one RequestOptions. */
export function encodeRequestOptions(writer: BinaryWriter, value: RequestOptions): void {
    writer.writeOption(value.timeoutMs, (value0) => {
        writer.writeUnsigned(value0);
    });
    writer.writeOption(value.priority, (value1) => {
        writer.writeByte(value1);
    });
    writer.writeOption(value.traceId, (value2) => {
        writer.writeString(value2);
    });
}

/** Decode one RequestOptions. */
export function decodeRequestOptions(reader: BinaryReader): RequestOptions {
    const timeoutMs = reader.readOption(() => reader.readUnsigned());
    const priority = reader.readOption(() => reader.readByte());
    const traceId = reader.readOption(() => reader.readString());

    return {
        ...(timeoutMs === undefined ? {} : { timeoutMs }),
        ...(priority === undefined ? {} : { priority }),
        ...(traceId === undefined ? {} : { traceId }),
    };
}

/** Return one JSON value for one RequestOptions. */
export function toJsonRequestOptions(value: RequestOptions): Json {
    return {
        ...(value.timeoutMs === undefined ? {} : { timeoutMs: value.timeoutMs.toString() }),
        ...(value.priority === undefined ? {} : { priority: value.priority }),
        ...(value.traceId === undefined ? {} : { traceId: value.traceId }),
    };
}

/** Return one RequestOptions from one JSON value. */
export function fromJsonRequestOptions(value: Json): RequestOptions {
    const object = jsonObject(value);

    return {
        timeoutMs: jsonOptional(object, "timeoutMs", (value) => jsonBigint(value)),
        priority: jsonOptional(object, "priority", (value) => jsonInteger(value)),
        traceId: jsonOptional(object, "traceId", (value) => jsonString(value)),
    };
}

/** Response envelope with identifier and payload. */
export type ProtocolResponse = {
    /** Request id being answered. */
    readonly id: RequestId;
    /** Response payload. */
    readonly payload: WorkspaceResponse;
};

export const ProtocolResponse = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ProtocolResponse): void {
        encodeProtocolResponse(writer, value);
    },

    /** Decode one ProtocolResponse. */
    decode(reader: BinaryReader): ProtocolResponse {
        return decodeProtocolResponse(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ProtocolResponse): Json {
        return toJsonProtocolResponse(value);
    },

    /** Return one ProtocolResponse from one JSON value. */
    fromJson(value: Json): ProtocolResponse {
        return fromJsonProtocolResponse(value);
    },
};

/** Encode one ProtocolResponse. */
export function encodeProtocolResponse(writer: BinaryWriter, value: ProtocolResponse): void {
    encodeRequestId(writer, value.id);
    encodeWorkspaceResponse(writer, value.payload);
}

/** Decode one ProtocolResponse. */
export function decodeProtocolResponse(reader: BinaryReader): ProtocolResponse {
    const id = decodeRequestId(reader);
    const payload = decodeWorkspaceResponse(reader);

    return {
        id,
        payload,
    };
}

/** Return one JSON value for one ProtocolResponse. */
export function toJsonProtocolResponse(value: ProtocolResponse): Json {
    return {
        id: toJsonRequestId(value.id),
        payload: toJsonWorkspaceResponse(value.payload),
    };
}

/** Return one ProtocolResponse from one JSON value. */
export function fromJsonProtocolResponse(value: Json): ProtocolResponse {
    const object = jsonObject(value);

    return {
        id: fromJsonRequestId(jsonField(object, "id")),
        payload: fromJsonWorkspaceResponse(jsonField(object, "payload")),
    };
}

/** Notification envelope sent without an explicit response. */
export type ProtocolNotification = {
    /** Notification payload. */
    readonly payload: WorkspaceNotification;
};

export const ProtocolNotification = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ProtocolNotification): void {
        encodeProtocolNotification(writer, value);
    },

    /** Decode one ProtocolNotification. */
    decode(reader: BinaryReader): ProtocolNotification {
        return decodeProtocolNotification(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ProtocolNotification): Json {
        return toJsonProtocolNotification(value);
    },

    /** Return one ProtocolNotification from one JSON value. */
    fromJson(value: Json): ProtocolNotification {
        return fromJsonProtocolNotification(value);
    },
};

/** Encode one ProtocolNotification. */
export function encodeProtocolNotification(writer: BinaryWriter, value: ProtocolNotification): void {
    encodeWorkspaceNotification(writer, value.payload);
}

/** Decode one ProtocolNotification. */
export function decodeProtocolNotification(reader: BinaryReader): ProtocolNotification {
    const payload = decodeWorkspaceNotification(reader);

    return {
        payload,
    };
}

/** Return one JSON value for one ProtocolNotification. */
export function toJsonProtocolNotification(value: ProtocolNotification): Json {
    return {
        payload: toJsonWorkspaceNotification(value.payload),
    };
}

/** Return one ProtocolNotification from one JSON value. */
export function fromJsonProtocolNotification(value: Json): ProtocolNotification {
    const object = jsonObject(value);

    return {
        payload: fromJsonWorkspaceNotification(jsonField(object, "payload")),
    };
}

/** Protocol message envelope. */
export type ProtocolMessage =
    /** Request message sent from a client to a server. */
    | {
          readonly kind: "request";
          readonly request: ProtocolRequest;
      }
    /** Response message sent from a server to a client. */
    | {
          readonly kind: "response";
          readonly response: ProtocolResponse;
      }
    /** Notification sent without an explicit response. */
    | {
          readonly kind: "notification";
          readonly notification: ProtocolNotification;
      }
;

export const ProtocolMessage = {
    /** Request message sent from a client to a server. */
    request(request: ProtocolRequest): ProtocolMessage {
        return { kind: "request", request };
    },

    /** Response message sent from a server to a client. */
    response(response: ProtocolResponse): ProtocolMessage {
        return { kind: "response", response };
    },

    /** Notification sent without an explicit response. */
    notification(notification: ProtocolNotification): ProtocolMessage {
        return { kind: "notification", notification };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: ProtocolMessage): void {
        encodeProtocolMessage(writer, value);
    },

    /** Decode one ProtocolMessage. */
    decode(reader: BinaryReader): ProtocolMessage {
        return decodeProtocolMessage(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ProtocolMessage): Json {
        return toJsonProtocolMessage(value);
    },

    /** Return one ProtocolMessage from one JSON value. */
    fromJson(value: Json): ProtocolMessage {
        return fromJsonProtocolMessage(value);
    },
};

/** Encode one ProtocolMessage. */
export function encodeProtocolMessage(writer: BinaryWriter, value: ProtocolMessage): void {
    switch (value.kind) {
        case "request":
            writer.writeUnsigned(0);
            encodeProtocolRequest(writer, value.request);
            return;
        case "response":
            writer.writeUnsigned(1);
            encodeProtocolResponse(writer, value.response);
            return;
        case "notification":
            writer.writeUnsigned(2);
            encodeProtocolNotification(writer, value.notification);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one ProtocolMessage. */
export function decodeProtocolMessage(reader: BinaryReader): ProtocolMessage {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const request = decodeProtocolRequest(reader);

            return { kind: "request", request };
        }
        case 1: {
            const response = decodeProtocolResponse(reader);

            return { kind: "response", response };
        }
        case 2: {
            const notification = decodeProtocolNotification(reader);

            return { kind: "notification", notification };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one ProtocolMessage. */
export function toJsonProtocolMessage(value: ProtocolMessage): Json {
    switch (value.kind) {
        case "request":
            return {
                kind: "request",
                request: toJsonProtocolRequest(value.request),
            };
        case "response":
            return {
                kind: "response",
                response: toJsonProtocolResponse(value.response),
            };
        case "notification":
            return {
                kind: "notification",
                notification: toJsonProtocolNotification(value.notification),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one ProtocolMessage from one JSON value. */
export function fromJsonProtocolMessage(value: Json): ProtocolMessage {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "request":
            return {
                kind,
                request: fromJsonProtocolRequest(jsonField(object, "request")),
            };
        case "response":
            return {
                kind,
                response: fromJsonProtocolResponse(jsonField(object, "response")),
            };
        case "notification":
            return {
                kind,
                notification: fromJsonProtocolNotification(jsonField(object, "notification")),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}
