/* generated by destack_bridge_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, jsonArray, jsonBool, jsonField, jsonInteger, jsonObject, jsonOptional, jsonString } from "../../../../protocol/serde.js";
import type { ContentId } from "../../../source/file/model/file.js";
import type { FileType } from "../../../source/file/model/type.js";
import type { CommandEnvVar } from "./common.js";
import type { CommandInput } from "./common.js";
import type { CommandRevision } from "./common.js";
import type { CommandTargetOverrides } from "./common.js";
import type { ManifestOverride } from "./common.js";
import { decodeContentId, encodeContentId, fromJsonContentId, toJsonContentId } from "../../../source/file/model/file.js";
import { decodeFileType, encodeFileType, fromJsonFileType, toJsonFileType } from "../../../source/file/model/type.js";
import { decodeCommandEnvVar, encodeCommandEnvVar, fromJsonCommandEnvVar, toJsonCommandEnvVar } from "./common.js";
import { decodeCommandInput, encodeCommandInput, fromJsonCommandInput, toJsonCommandInput } from "./common.js";
import { decodeCommandRevision, encodeCommandRevision, fromJsonCommandRevision, toJsonCommandRevision } from "./common.js";
import { decodeCommandTargetOverrides, encodeCommandTargetOverrides, fromJsonCommandTargetOverrides, toJsonCommandTargetOverrides } from "./common.js";
import { decodeManifestOverride, encodeManifestOverride, fromJsonManifestOverride, toJsonManifestOverride } from "./common.js";

/** Request to format source files or content. */
export type FormatInput = {
    /** Revision selected for this format request. */
    readonly revision: CommandRevision;
    /** Input sources for the command. */
    readonly inputs: ReadonlyArray<CommandInput>;
    /** Whether destack.json should resolve inputs when none are provided. */
    readonly configInputs: boolean;
    /** Optional working directory for this command. */
    readonly cwd?: string;
    /** Optional Destack manifest path override. */
    readonly manifest?: string;
    /** Optional target name override. */
    readonly target?: string;
    /** Optional target overrides. */
    readonly targetOverrides?: CommandTargetOverrides;
    /** Optional profile name override. */
    readonly profile?: string;
    /** Optional environment overrides. */
    readonly env: ReadonlyArray<CommandEnvVar>;
    /** Optional manifest overrides. */
    readonly overrides: ReadonlyArray<ManifestOverride>;
    /** Whether the command should watch for changes. */
    readonly watch: boolean;
    /** Whether the command should skip writes. */
    readonly dryRun: boolean;
    /** Formatting source. */
    readonly source: FormatSource;
    /** Formatting mode. */
    readonly mode: FormatMode;
};

export const FormatInput = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: FormatInput): void {
        encodeFormatInput(writer, value);
    },

    /** Decode one FormatInput. */
    decode(reader: BinaryReader): FormatInput {
        return decodeFormatInput(reader);
    },

    /** Return this value as JSON. */
    toJson(value: FormatInput): Json {
        return toJsonFormatInput(value);
    },

    /** Return one FormatInput from one JSON value. */
    fromJson(value: Json): FormatInput {
        return fromJsonFormatInput(value);
    },
};

/** Encode one FormatInput. */
export function encodeFormatInput(writer: BinaryWriter, value: FormatInput): void {
    encodeCommandRevision(writer, value.revision);
    writer.writeUnsigned(value.inputs.length);
    for (const item1 of value.inputs) {
        encodeCommandInput(writer, item1);
    }
    writer.writeBool(value.configInputs);
    writer.writeOption(value.cwd, (value3) => {
        writer.writeString(value3);
    });
    writer.writeOption(value.manifest, (value4) => {
        writer.writeString(value4);
    });
    writer.writeOption(value.target, (value5) => {
        writer.writeString(value5);
    });
    writer.writeOption(value.targetOverrides, (value6) => {
        encodeCommandTargetOverrides(writer, value6);
    });
    writer.writeOption(value.profile, (value7) => {
        writer.writeString(value7);
    });
    writer.writeUnsigned(value.env.length);
    for (const item8 of value.env) {
        encodeCommandEnvVar(writer, item8);
    }
    writer.writeUnsigned(value.overrides.length);
    for (const item9 of value.overrides) {
        encodeManifestOverride(writer, item9);
    }
    writer.writeBool(value.watch);
    writer.writeBool(value.dryRun);
    encodeFormatSource(writer, value.source);
    encodeFormatMode(writer, value.mode);
}

/** Decode one FormatInput. */
export function decodeFormatInput(reader: BinaryReader): FormatInput {
    const revision = decodeCommandRevision(reader);
    const inputs = (() => { const length1 = reader.readNumber(); const items1: Array<CommandInput> = []; for (let index = 0; index < length1; index += 1) { items1.push(decodeCommandInput(reader)); } return items1; })();
    const configInputs = reader.readBool();
    const cwd = reader.readOption(() => reader.readString());
    const manifest = reader.readOption(() => reader.readString());
    const target = reader.readOption(() => reader.readString());
    const targetOverrides = reader.readOption(() => decodeCommandTargetOverrides(reader));
    const profile = reader.readOption(() => reader.readString());
    const env = (() => { const length8 = reader.readNumber(); const items8: Array<CommandEnvVar> = []; for (let index = 0; index < length8; index += 1) { items8.push(decodeCommandEnvVar(reader)); } return items8; })();
    const overrides = (() => { const length9 = reader.readNumber(); const items9: Array<ManifestOverride> = []; for (let index = 0; index < length9; index += 1) { items9.push(decodeManifestOverride(reader)); } return items9; })();
    const watch = reader.readBool();
    const dryRun = reader.readBool();
    const source = decodeFormatSource(reader);
    const mode = decodeFormatMode(reader);

    return {
        revision,
        inputs,
        configInputs,
        ...(cwd === undefined ? {} : { cwd }),
        ...(manifest === undefined ? {} : { manifest }),
        ...(target === undefined ? {} : { target }),
        ...(targetOverrides === undefined ? {} : { targetOverrides }),
        ...(profile === undefined ? {} : { profile }),
        env,
        overrides,
        watch,
        dryRun,
        source,
        mode,
    };
}

/** Return one JSON value for one FormatInput. */
export function toJsonFormatInput(value: FormatInput): Json {
    return {
        revision: toJsonCommandRevision(value.revision),
        inputs: value.inputs.map((item0) => toJsonCommandInput(item0)),
        configInputs: value.configInputs,
        ...(value.cwd === undefined ? {} : { cwd: value.cwd }),
        ...(value.manifest === undefined ? {} : { manifest: value.manifest }),
        ...(value.target === undefined ? {} : { target: value.target }),
        ...(value.targetOverrides === undefined ? {} : { targetOverrides: toJsonCommandTargetOverrides(value.targetOverrides) }),
        ...(value.profile === undefined ? {} : { profile: value.profile }),
        env: value.env.map((item0) => toJsonCommandEnvVar(item0)),
        overrides: value.overrides.map((item0) => toJsonManifestOverride(item0)),
        watch: value.watch,
        dryRun: value.dryRun,
        source: toJsonFormatSource(value.source),
        mode: toJsonFormatMode(value.mode),
    };
}

/** Return one FormatInput from one JSON value. */
export function fromJsonFormatInput(value: Json): FormatInput {
    const object = jsonObject(value);

    return {
        revision: fromJsonCommandRevision(jsonField(object, "revision")),
        inputs: jsonArray(jsonField(object, "inputs")).map((item0) => fromJsonCommandInput(item0)),
        configInputs: jsonBool(jsonField(object, "configInputs")),
        cwd: jsonOptional(object, "cwd", (value) => jsonString(value)),
        manifest: jsonOptional(object, "manifest", (value) => jsonString(value)),
        target: jsonOptional(object, "target", (value) => jsonString(value)),
        targetOverrides: jsonOptional(object, "targetOverrides", (value) => fromJsonCommandTargetOverrides(value)),
        profile: jsonOptional(object, "profile", (value) => jsonString(value)),
        env: jsonArray(jsonField(object, "env")).map((item0) => fromJsonCommandEnvVar(item0)),
        overrides: jsonArray(jsonField(object, "overrides")).map((item0) => fromJsonManifestOverride(item0)),
        watch: jsonBool(jsonField(object, "watch")),
        dryRun: jsonBool(jsonField(object, "dryRun")),
        source: fromJsonFormatSource(jsonField(object, "source")),
        mode: fromJsonFormatMode(jsonField(object, "mode")),
    };
}

/** Formatting source. */
export type FormatSource =
    /** Format files or directories. */
    | {
          readonly kind: "files";
          readonly files: ReadonlyArray<string>;
      }
    /** Format one open file. */
    | {
          readonly kind: "openFile";
          readonly open_file: string;
      }
    /** Format explicit content. */
    | {
          readonly kind: "content";
          /** Input label. */
          readonly name: string;
          /** Input file type. */
          readonly fileType: FileType;
          /** Content to format. */
          readonly content: ContentId;
      }
;

export const FormatSource = {
    /** Format files or directories. */
    files(files: ReadonlyArray<string>): FormatSource {
        return { kind: "files", files };
    },

    /** Format one open file. */
    openFile(open_file: string): FormatSource {
        return { kind: "openFile", open_file };
    },

    /** Format explicit content. */
    content(name: string, fileType: FileType, content: ContentId): FormatSource {
        return { kind: "content", name, fileType, content };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: FormatSource): void {
        encodeFormatSource(writer, value);
    },

    /** Decode one FormatSource. */
    decode(reader: BinaryReader): FormatSource {
        return decodeFormatSource(reader);
    },

    /** Return this value as JSON. */
    toJson(value: FormatSource): Json {
        return toJsonFormatSource(value);
    },

    /** Return one FormatSource from one JSON value. */
    fromJson(value: Json): FormatSource {
        return fromJsonFormatSource(value);
    },
};

/** Encode one FormatSource. */
export function encodeFormatSource(writer: BinaryWriter, value: FormatSource): void {
    switch (value.kind) {
        case "files":
            writer.writeUnsigned(0);
            writer.writeUnsigned(value.files.length);
            for (const item0 of value.files) {
                writer.writeString(item0);
            }
            return;
        case "openFile":
            writer.writeUnsigned(1);
            writer.writeString(value.open_file);
            return;
        case "content":
            writer.writeUnsigned(2);
            writer.writeString(value.name);
            encodeFileType(writer, value.fileType);
            encodeContentId(writer, value.content);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one FormatSource. */
export function decodeFormatSource(reader: BinaryReader): FormatSource {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const files = (() => { const length0 = reader.readNumber(); const items0: Array<string> = []; for (let index = 0; index < length0; index += 1) { items0.push(reader.readString()); } return items0; })();

            return { kind: "files", files };
        }
        case 1: {
            const open_file = reader.readString();

            return { kind: "openFile", open_file };
        }
        case 2: {
            const name = reader.readString();
            const fileType = decodeFileType(reader);
            const content = decodeContentId(reader);

            return {
                kind: "content",
                name,
                fileType,
                content,
            };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one FormatSource. */
export function toJsonFormatSource(value: FormatSource): Json {
    switch (value.kind) {
        case "files":
            return {
                kind: "files",
                files: value.files.map((item0) => item0),
            };
        case "openFile":
            return {
                kind: "openFile",
                open_file: value.open_file,
            };
        case "content":
            return {
                kind: "content",
                name: value.name,
                fileType: toJsonFileType(value.fileType),
                content: toJsonContentId(value.content),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one FormatSource from one JSON value. */
export function fromJsonFormatSource(value: Json): FormatSource {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "files":
            return {
                kind,
                files: jsonArray(jsonField(object, "files")).map((item0) => jsonString(item0)),
            };
        case "openFile":
            return {
                kind,
                open_file: jsonString(jsonField(object, "open_file")),
            };
        case "content":
            return {
                kind,
                name: jsonString(jsonField(object, "name")),
                fileType: fromJsonFileType(jsonField(object, "fileType")),
                content: fromJsonContentId(jsonField(object, "content")),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** Formatting mode. */
export type FormatMode = "preview" | "check" | "write";

export const FormatMode = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: FormatMode): void {
        encodeFormatMode(writer, value);
    },

    /** Decode one FormatMode. */
    decode(reader: BinaryReader): FormatMode {
        return decodeFormatMode(reader);
    },

    /** Return this value as JSON. */
    toJson(value: FormatMode): Json {
        return toJsonFormatMode(value);
    },

    /** Return one FormatMode from one JSON value. */
    fromJson(value: Json): FormatMode {
        return fromJsonFormatMode(value);
    },
};

/** Encode one FormatMode. */
export function encodeFormatMode(writer: BinaryWriter, value: FormatMode): void {
    switch (value) {
        case "preview":
            writer.writeUnsigned(0);
            return;
        case "check":
            writer.writeUnsigned(1);
            return;
        case "write":
            writer.writeUnsigned(2);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one FormatMode. */
export function decodeFormatMode(reader: BinaryReader): FormatMode {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "preview";
        case 1:
            return "check";
        case 2:
            return "write";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one FormatMode. */
export function toJsonFormatMode(value: FormatMode): Json {
    return value;
}

/** Return one FormatMode from one JSON value. */
export function fromJsonFormatMode(value: Json): FormatMode {
    const variant = jsonString(value);

    switch (variant) {
        case "preview":
            return "preview";
        case "check":
            return "check";
        case "write":
            return "write";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** Payload for format command output. */
export type FormatPayload = {
    /** The number of files inspected. */
    readonly files: number;
    /** The number of files that would change. */
    readonly changed: number;
    /** Paths for files that changed or would change in check mode. */
    readonly changedFiles: ReadonlyArray<string>;
    /** The number of errors encountered. */
    readonly errors: number;
    /** Paths for files that failed formatting. */
    readonly errorFiles: ReadonlyArray<string>;
    /** Whether this was a check-only run. */
    readonly check: boolean;
    /** Formatted output for eval mode. */
    readonly formatted?: string;
};

export const FormatPayload = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: FormatPayload): void {
        encodeFormatPayload(writer, value);
    },

    /** Decode one FormatPayload. */
    decode(reader: BinaryReader): FormatPayload {
        return decodeFormatPayload(reader);
    },

    /** Return this value as JSON. */
    toJson(value: FormatPayload): Json {
        return toJsonFormatPayload(value);
    },

    /** Return one FormatPayload from one JSON value. */
    fromJson(value: Json): FormatPayload {
        return fromJsonFormatPayload(value);
    },
};

/** Encode one FormatPayload. */
export function encodeFormatPayload(writer: BinaryWriter, value: FormatPayload): void {
    writer.writeUnsigned(value.files);
    writer.writeUnsigned(value.changed);
    writer.writeUnsigned(value.changedFiles.length);
    for (const item2 of value.changedFiles) {
        writer.writeString(item2);
    }
    writer.writeUnsigned(value.errors);
    writer.writeUnsigned(value.errorFiles.length);
    for (const item4 of value.errorFiles) {
        writer.writeString(item4);
    }
    writer.writeBool(value.check);
    writer.writeOption(value.formatted, (value6) => {
        writer.writeString(value6);
    });
}

/** Decode one FormatPayload. */
export function decodeFormatPayload(reader: BinaryReader): FormatPayload {
    const files = reader.readNumber();
    const changed = reader.readNumber();
    const changedFiles = (() => { const length2 = reader.readNumber(); const items2: Array<string> = []; for (let index = 0; index < length2; index += 1) { items2.push(reader.readString()); } return items2; })();
    const errors = reader.readNumber();
    const errorFiles = (() => { const length4 = reader.readNumber(); const items4: Array<string> = []; for (let index = 0; index < length4; index += 1) { items4.push(reader.readString()); } return items4; })();
    const check = reader.readBool();
    const formatted = reader.readOption(() => reader.readString());

    return {
        files,
        changed,
        changedFiles,
        errors,
        errorFiles,
        check,
        ...(formatted === undefined ? {} : { formatted }),
    };
}

/** Return one JSON value for one FormatPayload. */
export function toJsonFormatPayload(value: FormatPayload): Json {
    return {
        files: value.files,
        changed: value.changed,
        changedFiles: value.changedFiles.map((item0) => item0),
        errors: value.errors,
        errorFiles: value.errorFiles.map((item0) => item0),
        check: value.check,
        ...(value.formatted === undefined ? {} : { formatted: value.formatted }),
    };
}

/** Return one FormatPayload from one JSON value. */
export function fromJsonFormatPayload(value: Json): FormatPayload {
    const object = jsonObject(value);

    return {
        files: jsonInteger(jsonField(object, "files")),
        changed: jsonInteger(jsonField(object, "changed")),
        changedFiles: jsonArray(jsonField(object, "changedFiles")).map((item0) => jsonString(item0)),
        errors: jsonInteger(jsonField(object, "errors")),
        errorFiles: jsonArray(jsonField(object, "errorFiles")).map((item0) => jsonString(item0)),
        check: jsonBool(jsonField(object, "check")),
        formatted: jsonOptional(object, "formatted", (value) => jsonString(value)),
    };
}
