/* generated by destack_bridge_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, jsonField, jsonObject, jsonString } from "../../protocol/serde.js";
import type { ArtifactReference } from "../artifact/reference.js";
import type { Content } from "../source/file/model/file.js";
import type { ContentId } from "../source/file/model/file.js";
import type { ExportRequest } from "./workspace/artifact/export.js";
import type { BenchInput } from "./workspace/command/bench.js";
import type { BuildInput } from "./workspace/command/build.js";
import type { CacheInput } from "./workspace/command/cache.js";
import type { CheckInput } from "./workspace/command/check.js";
import type { LintInput } from "./workspace/command/check.js";
import type { CleanInput } from "./workspace/command/clean.js";
import type { DocInput } from "./workspace/command/doc.js";
import type { DoctorInput } from "./workspace/command/doctor.js";
import type { FormatInput } from "./workspace/command/format.js";
import type { InfoInput } from "./workspace/command/info.js";
import type { RunInput } from "./workspace/command/run.js";
import type { SettingsInput } from "./workspace/command/settings.js";
import type { TargetsInput } from "./workspace/command/targets.js";
import type { TaskInput } from "./workspace/command/task.js";
import type { TestInput } from "./workspace/command/test.js";
import type { HandshakeRequest } from "./handshake.js";
import type { RequestId } from "./envelope.js";
import type { WorkspaceQuery } from "./query.js";
import type { CloseRootRequest } from "./root.js";
import type { FileOperationRequest } from "./root.js";
import type { RootId } from "./root.js";
import type { OpenRootRequest } from "./root.js";
import type { ReloadRootRequest } from "./root.js";
import type { SourceUpdateRequest } from "./root.js";
import type { WatchNextRequest } from "./watch.js";
import type { WatchStartRequest } from "./watch.js";
import type { WatchStopRequest } from "./watch.js";
import { decodeArtifactReference, encodeArtifactReference, fromJsonArtifactReference, toJsonArtifactReference } from "../artifact/reference.js";
import { decodeContent, encodeContent, fromJsonContent, toJsonContent } from "../source/file/model/file.js";
import { decodeContentId, encodeContentId, fromJsonContentId, toJsonContentId } from "../source/file/model/file.js";
import { decodeExportRequest, encodeExportRequest, fromJsonExportRequest, toJsonExportRequest } from "./workspace/artifact/export.js";
import { decodeBenchInput, encodeBenchInput, fromJsonBenchInput, toJsonBenchInput } from "./workspace/command/bench.js";
import { decodeBuildInput, encodeBuildInput, fromJsonBuildInput, toJsonBuildInput } from "./workspace/command/build.js";
import { decodeCacheInput, encodeCacheInput, fromJsonCacheInput, toJsonCacheInput } from "./workspace/command/cache.js";
import { decodeCheckInput, encodeCheckInput, fromJsonCheckInput, toJsonCheckInput } from "./workspace/command/check.js";
import { decodeLintInput, encodeLintInput, fromJsonLintInput, toJsonLintInput } from "./workspace/command/check.js";
import { decodeCleanInput, encodeCleanInput, fromJsonCleanInput, toJsonCleanInput } from "./workspace/command/clean.js";
import { decodeDocInput, encodeDocInput, fromJsonDocInput, toJsonDocInput } from "./workspace/command/doc.js";
import { decodeDoctorInput, encodeDoctorInput, fromJsonDoctorInput, toJsonDoctorInput } from "./workspace/command/doctor.js";
import { decodeFormatInput, encodeFormatInput, fromJsonFormatInput, toJsonFormatInput } from "./workspace/command/format.js";
import { decodeInfoInput, encodeInfoInput, fromJsonInfoInput, toJsonInfoInput } from "./workspace/command/info.js";
import { decodeRunInput, encodeRunInput, fromJsonRunInput, toJsonRunInput } from "./workspace/command/run.js";
import { decodeSettingsInput, encodeSettingsInput, fromJsonSettingsInput, toJsonSettingsInput } from "./workspace/command/settings.js";
import { decodeTargetsInput, encodeTargetsInput, fromJsonTargetsInput, toJsonTargetsInput } from "./workspace/command/targets.js";
import { decodeTaskInput, encodeTaskInput, fromJsonTaskInput, toJsonTaskInput } from "./workspace/command/task.js";
import { decodeTestInput, encodeTestInput, fromJsonTestInput, toJsonTestInput } from "./workspace/command/test.js";
import { decodeHandshakeRequest, encodeHandshakeRequest, fromJsonHandshakeRequest, toJsonHandshakeRequest } from "./handshake.js";
import { decodeRequestId, encodeRequestId, fromJsonRequestId, toJsonRequestId } from "./envelope.js";
import { decodeWorkspaceQuery, encodeWorkspaceQuery, fromJsonWorkspaceQuery, toJsonWorkspaceQuery } from "./query.js";
import { decodeCloseRootRequest, encodeCloseRootRequest, fromJsonCloseRootRequest, toJsonCloseRootRequest } from "./root.js";
import { decodeFileOperationRequest, encodeFileOperationRequest, fromJsonFileOperationRequest, toJsonFileOperationRequest } from "./root.js";
import { decodeRootId, encodeRootId, fromJsonRootId, toJsonRootId } from "./root.js";
import { decodeOpenRootRequest, encodeOpenRootRequest, fromJsonOpenRootRequest, toJsonOpenRootRequest } from "./root.js";
import { decodeReloadRootRequest, encodeReloadRootRequest, fromJsonReloadRootRequest, toJsonReloadRootRequest } from "./root.js";
import { decodeSourceUpdateRequest, encodeSourceUpdateRequest, fromJsonSourceUpdateRequest, toJsonSourceUpdateRequest } from "./root.js";
import { decodeWatchNextRequest, encodeWatchNextRequest, fromJsonWatchNextRequest, toJsonWatchNextRequest } from "./watch.js";
import { decodeWatchStartRequest, encodeWatchStartRequest, fromJsonWatchStartRequest, toJsonWatchStartRequest } from "./watch.js";
import { decodeWatchStopRequest, encodeWatchStopRequest, fromJsonWatchStopRequest, toJsonWatchStopRequest } from "./watch.js";

/** Requests accepted by the workspace protocol. */
export type WorkspaceRequest =
    /** Negotiate protocol version and capabilities. */
    | {
          readonly kind: "handshake";
          readonly handshake: HandshakeRequest;
      }
    /** Check server liveness. */
    | {
          readonly kind: "ping";
      }
    /** Cancel an in flight request. */
    | {
          readonly kind: "cancel";
          readonly id: RequestId;
      }
    /** Request server shutdown. */
    | {
          readonly kind: "shutdown";
      }
    /** Open or register a root. */
    | {
          readonly kind: "openRoot";
          readonly open_root: OpenRootRequest;
      }
    /** Close a root handle. */
    | {
          readonly kind: "closeRoot";
          readonly close_root: CloseRootRequest;
      }
    /** Reload a root. */
    | {
          readonly kind: "reloadRoot";
          readonly reload_root: ReloadRootRequest;
      }
    /** Apply a file operation to a root. */
    | {
          readonly kind: "applyFileOperation";
          readonly apply_file_operation: FileOperationRequest;
      }
    /** Apply a source update to a root. */
    | {
          readonly kind: "applySourceUpdate";
          readonly apply_source_update: SourceUpdateRequest;
      }
    /** Start watching a root. */
    | {
          readonly kind: "startWatch";
          readonly start_watch: WatchStartRequest;
      }
    /** Receive and apply the next watch batch. */
    | {
          readonly kind: "nextWatchBatch";
          readonly next_watch_batch: WatchNextRequest;
      }
    /** Stop watching a root. */
    | {
          readonly kind: "stopWatch";
          readonly stop_watch: WatchStopRequest;
      }
    /** Check source state. */
    | {
          readonly kind: "check";
          /** Root handle. */
          readonly handle: RootId;
          /** Check input. */
          readonly input: CheckInput;
      }
    /** Lint source state. */
    | {
          readonly kind: "lint";
          /** Root handle. */
          readonly handle: RootId;
          /** Lint input. */
          readonly input: LintInput;
      }
    /** Format source files or content. */
    | {
          readonly kind: "format";
          /** Root handle. */
          readonly handle: RootId;
          /** Format input. */
          readonly input: FormatInput;
      }
    /** Build target artifacts. */
    | {
          readonly kind: "build";
          /** Root handle. */
          readonly handle: RootId;
          /** Build input. */
          readonly input: BuildInput;
      }
    /** Run a workspace target. */
    | {
          readonly kind: "run";
          /** Root handle. */
          readonly handle: RootId;
          /** Run input. */
          readonly input: RunInput;
      }
    /** Run workspace tests. */
    | {
          readonly kind: "test";
          /** Root handle. */
          readonly handle: RootId;
          /** Test input. */
          readonly input: TestInput;
      }
    /** Generate documentation. */
    | {
          readonly kind: "doc";
          /** Root handle. */
          readonly handle: RootId;
          /** Documentation input. */
          readonly input: DocInput;
      }
    /** Run benchmarks. */
    | {
          readonly kind: "bench";
          /** Root handle. */
          readonly handle: RootId;
          /** Benchmark input. */
          readonly input: BenchInput;
      }
    /** Return workspace information. */
    | {
          readonly kind: "info";
          /** Root handle. */
          readonly handle: RootId;
          /** Information input. */
          readonly input: InfoInput;
      }
    /** Return configured targets. */
    | {
          readonly kind: "targets";
          /** Root handle. */
          readonly handle: RootId;
          /** Targets input. */
          readonly input: TargetsInput;
      }
    /** Return cache locations. */
    | {
          readonly kind: "cache";
          /** Root handle. */
          readonly handle: RootId;
          /** Cache input. */
          readonly input: CacheInput;
      }
    /** Return resolved settings. */
    | {
          readonly kind: "settings";
          /** Root handle. */
          readonly handle: RootId;
          /** Settings input. */
          readonly input: SettingsInput;
      }
    /** Return workspace health information. */
    | {
          readonly kind: "doctor";
          /** Root handle. */
          readonly handle: RootId;
          /** Doctor input. */
          readonly input: DoctorInput;
      }
    /** Run workspace tasks. */
    | {
          readonly kind: "task";
          /** Root handle. */
          readonly handle: RootId;
          /** Task input. */
          readonly input: TaskInput;
      }
    /** Clean generated state. */
    | {
          readonly kind: "clean";
          /** Root handle. */
          readonly handle: RootId;
          /** Clean input. */
          readonly input: CleanInput;
      }
    /** Return one artifact payload. */
    | {
          readonly kind: "artifact";
          /** Root handle. */
          readonly handle: RootId;
          /** Artifact reference. */
          readonly artifact: ArtifactReference;
      }
    /** Store one content payload. */
    | {
          readonly kind: "store";
          /** Root handle. */
          readonly handle: RootId;
          /** Content payload. */
          readonly content: Content;
      }
    /** Load one content payload. */
    | {
          readonly kind: "load";
          /** Root handle. */
          readonly handle: RootId;
          /** Content id. */
          readonly content: ContentId;
      }
    /** Materialize derived outputs. */
    | {
          readonly kind: "export";
          /** Root handle. */
          readonly handle: RootId;
          /** Export request. */
          readonly request: ExportRequest;
      }
    /** Execute a query. */
    | {
          readonly kind: "query";
          readonly query: WorkspaceQuery;
      }
;

export const WorkspaceRequest = {
    /** Negotiate protocol version and capabilities. */
    handshake(handshake: HandshakeRequest): WorkspaceRequest {
        return { kind: "handshake", handshake };
    },

    /** Check server liveness. */
    ping(): WorkspaceRequest {
        return { kind: "ping" };
    },

    /** Cancel an in flight request. */
    cancel(id: RequestId): WorkspaceRequest {
        return { kind: "cancel", id };
    },

    /** Request server shutdown. */
    shutdown(): WorkspaceRequest {
        return { kind: "shutdown" };
    },

    /** Open or register a root. */
    openRoot(open_root: OpenRootRequest): WorkspaceRequest {
        return { kind: "openRoot", open_root };
    },

    /** Close a root handle. */
    closeRoot(close_root: CloseRootRequest): WorkspaceRequest {
        return { kind: "closeRoot", close_root };
    },

    /** Reload a root. */
    reloadRoot(reload_root: ReloadRootRequest): WorkspaceRequest {
        return { kind: "reloadRoot", reload_root };
    },

    /** Apply a file operation to a root. */
    applyFileOperation(apply_file_operation: FileOperationRequest): WorkspaceRequest {
        return { kind: "applyFileOperation", apply_file_operation };
    },

    /** Apply a source update to a root. */
    applySourceUpdate(apply_source_update: SourceUpdateRequest): WorkspaceRequest {
        return { kind: "applySourceUpdate", apply_source_update };
    },

    /** Start watching a root. */
    startWatch(start_watch: WatchStartRequest): WorkspaceRequest {
        return { kind: "startWatch", start_watch };
    },

    /** Receive and apply the next watch batch. */
    nextWatchBatch(next_watch_batch: WatchNextRequest): WorkspaceRequest {
        return { kind: "nextWatchBatch", next_watch_batch };
    },

    /** Stop watching a root. */
    stopWatch(stop_watch: WatchStopRequest): WorkspaceRequest {
        return { kind: "stopWatch", stop_watch };
    },

    /** Check source state. */
    check(handle: RootId, input: CheckInput): WorkspaceRequest {
        return { kind: "check", handle, input };
    },

    /** Lint source state. */
    lint(handle: RootId, input: LintInput): WorkspaceRequest {
        return { kind: "lint", handle, input };
    },

    /** Format source files or content. */
    format(handle: RootId, input: FormatInput): WorkspaceRequest {
        return { kind: "format", handle, input };
    },

    /** Build target artifacts. */
    build(handle: RootId, input: BuildInput): WorkspaceRequest {
        return { kind: "build", handle, input };
    },

    /** Run a workspace target. */
    run(handle: RootId, input: RunInput): WorkspaceRequest {
        return { kind: "run", handle, input };
    },

    /** Run workspace tests. */
    test(handle: RootId, input: TestInput): WorkspaceRequest {
        return { kind: "test", handle, input };
    },

    /** Generate documentation. */
    doc(handle: RootId, input: DocInput): WorkspaceRequest {
        return { kind: "doc", handle, input };
    },

    /** Run benchmarks. */
    bench(handle: RootId, input: BenchInput): WorkspaceRequest {
        return { kind: "bench", handle, input };
    },

    /** Return workspace information. */
    info(handle: RootId, input: InfoInput): WorkspaceRequest {
        return { kind: "info", handle, input };
    },

    /** Return configured targets. */
    targets(handle: RootId, input: TargetsInput): WorkspaceRequest {
        return { kind: "targets", handle, input };
    },

    /** Return cache locations. */
    cache(handle: RootId, input: CacheInput): WorkspaceRequest {
        return { kind: "cache", handle, input };
    },

    /** Return resolved settings. */
    settings(handle: RootId, input: SettingsInput): WorkspaceRequest {
        return { kind: "settings", handle, input };
    },

    /** Return workspace health information. */
    doctor(handle: RootId, input: DoctorInput): WorkspaceRequest {
        return { kind: "doctor", handle, input };
    },

    /** Run workspace tasks. */
    task(handle: RootId, input: TaskInput): WorkspaceRequest {
        return { kind: "task", handle, input };
    },

    /** Clean generated state. */
    clean(handle: RootId, input: CleanInput): WorkspaceRequest {
        return { kind: "clean", handle, input };
    },

    /** Return one artifact payload. */
    artifact(handle: RootId, artifact: ArtifactReference): WorkspaceRequest {
        return { kind: "artifact", handle, artifact };
    },

    /** Store one content payload. */
    store(handle: RootId, content: Content): WorkspaceRequest {
        return { kind: "store", handle, content };
    },

    /** Load one content payload. */
    load(handle: RootId, content: ContentId): WorkspaceRequest {
        return { kind: "load", handle, content };
    },

    /** Materialize derived outputs. */
    "export"(handle: RootId, request: ExportRequest): WorkspaceRequest {
        return { kind: "export", handle, request };
    },

    /** Execute a query. */
    query(query: WorkspaceQuery): WorkspaceRequest {
        return { kind: "query", query };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: WorkspaceRequest): void {
        encodeWorkspaceRequest(writer, value);
    },

    /** Decode one WorkspaceRequest. */
    decode(reader: BinaryReader): WorkspaceRequest {
        return decodeWorkspaceRequest(reader);
    },

    /** Return this value as JSON. */
    toJson(value: WorkspaceRequest): Json {
        return toJsonWorkspaceRequest(value);
    },

    /** Return one WorkspaceRequest from one JSON value. */
    fromJson(value: Json): WorkspaceRequest {
        return fromJsonWorkspaceRequest(value);
    },
};

/** Encode one WorkspaceRequest. */
export function encodeWorkspaceRequest(writer: BinaryWriter, value: WorkspaceRequest): void {
    switch (value.kind) {
        case "handshake":
            writer.writeUnsigned(0);
            encodeHandshakeRequest(writer, value.handshake);
            return;
        case "ping":
            writer.writeUnsigned(1);
            return;
        case "cancel":
            writer.writeUnsigned(2);
            encodeRequestId(writer, value.id);
            return;
        case "shutdown":
            writer.writeUnsigned(3);
            return;
        case "openRoot":
            writer.writeUnsigned(4);
            encodeOpenRootRequest(writer, value.open_root);
            return;
        case "closeRoot":
            writer.writeUnsigned(5);
            encodeCloseRootRequest(writer, value.close_root);
            return;
        case "reloadRoot":
            writer.writeUnsigned(6);
            encodeReloadRootRequest(writer, value.reload_root);
            return;
        case "applyFileOperation":
            writer.writeUnsigned(7);
            encodeFileOperationRequest(writer, value.apply_file_operation);
            return;
        case "applySourceUpdate":
            writer.writeUnsigned(8);
            encodeSourceUpdateRequest(writer, value.apply_source_update);
            return;
        case "startWatch":
            writer.writeUnsigned(9);
            encodeWatchStartRequest(writer, value.start_watch);
            return;
        case "nextWatchBatch":
            writer.writeUnsigned(10);
            encodeWatchNextRequest(writer, value.next_watch_batch);
            return;
        case "stopWatch":
            writer.writeUnsigned(11);
            encodeWatchStopRequest(writer, value.stop_watch);
            return;
        case "check":
            writer.writeUnsigned(12);
            encodeRootId(writer, value.handle);
            encodeCheckInput(writer, value.input);
            return;
        case "lint":
            writer.writeUnsigned(13);
            encodeRootId(writer, value.handle);
            encodeLintInput(writer, value.input);
            return;
        case "format":
            writer.writeUnsigned(14);
            encodeRootId(writer, value.handle);
            encodeFormatInput(writer, value.input);
            return;
        case "build":
            writer.writeUnsigned(15);
            encodeRootId(writer, value.handle);
            encodeBuildInput(writer, value.input);
            return;
        case "run":
            writer.writeUnsigned(16);
            encodeRootId(writer, value.handle);
            encodeRunInput(writer, value.input);
            return;
        case "test":
            writer.writeUnsigned(17);
            encodeRootId(writer, value.handle);
            encodeTestInput(writer, value.input);
            return;
        case "doc":
            writer.writeUnsigned(18);
            encodeRootId(writer, value.handle);
            encodeDocInput(writer, value.input);
            return;
        case "bench":
            writer.writeUnsigned(19);
            encodeRootId(writer, value.handle);
            encodeBenchInput(writer, value.input);
            return;
        case "info":
            writer.writeUnsigned(20);
            encodeRootId(writer, value.handle);
            encodeInfoInput(writer, value.input);
            return;
        case "targets":
            writer.writeUnsigned(21);
            encodeRootId(writer, value.handle);
            encodeTargetsInput(writer, value.input);
            return;
        case "cache":
            writer.writeUnsigned(22);
            encodeRootId(writer, value.handle);
            encodeCacheInput(writer, value.input);
            return;
        case "settings":
            writer.writeUnsigned(23);
            encodeRootId(writer, value.handle);
            encodeSettingsInput(writer, value.input);
            return;
        case "doctor":
            writer.writeUnsigned(24);
            encodeRootId(writer, value.handle);
            encodeDoctorInput(writer, value.input);
            return;
        case "task":
            writer.writeUnsigned(25);
            encodeRootId(writer, value.handle);
            encodeTaskInput(writer, value.input);
            return;
        case "clean":
            writer.writeUnsigned(26);
            encodeRootId(writer, value.handle);
            encodeCleanInput(writer, value.input);
            return;
        case "artifact":
            writer.writeUnsigned(27);
            encodeRootId(writer, value.handle);
            encodeArtifactReference(writer, value.artifact);
            return;
        case "store":
            writer.writeUnsigned(28);
            encodeRootId(writer, value.handle);
            encodeContent(writer, value.content);
            return;
        case "load":
            writer.writeUnsigned(29);
            encodeRootId(writer, value.handle);
            encodeContentId(writer, value.content);
            return;
        case "export":
            writer.writeUnsigned(30);
            encodeRootId(writer, value.handle);
            encodeExportRequest(writer, value.request);
            return;
        case "query":
            writer.writeUnsigned(31);
            encodeWorkspaceQuery(writer, value.query);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one WorkspaceRequest. */
export function decodeWorkspaceRequest(reader: BinaryReader): WorkspaceRequest {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const handshake = decodeHandshakeRequest(reader);

            return { kind: "handshake", handshake };
        }
        case 1: {
            return { kind: "ping" };
        }
        case 2: {
            const id = decodeRequestId(reader);

            return {
                kind: "cancel",
                id,
            };
        }
        case 3: {
            return { kind: "shutdown" };
        }
        case 4: {
            const open_root = decodeOpenRootRequest(reader);

            return { kind: "openRoot", open_root };
        }
        case 5: {
            const close_root = decodeCloseRootRequest(reader);

            return { kind: "closeRoot", close_root };
        }
        case 6: {
            const reload_root = decodeReloadRootRequest(reader);

            return { kind: "reloadRoot", reload_root };
        }
        case 7: {
            const apply_file_operation = decodeFileOperationRequest(reader);

            return { kind: "applyFileOperation", apply_file_operation };
        }
        case 8: {
            const apply_source_update = decodeSourceUpdateRequest(reader);

            return { kind: "applySourceUpdate", apply_source_update };
        }
        case 9: {
            const start_watch = decodeWatchStartRequest(reader);

            return { kind: "startWatch", start_watch };
        }
        case 10: {
            const next_watch_batch = decodeWatchNextRequest(reader);

            return { kind: "nextWatchBatch", next_watch_batch };
        }
        case 11: {
            const stop_watch = decodeWatchStopRequest(reader);

            return { kind: "stopWatch", stop_watch };
        }
        case 12: {
            const handle = decodeRootId(reader);
            const input = decodeCheckInput(reader);

            return {
                kind: "check",
                handle,
                input,
            };
        }
        case 13: {
            const handle = decodeRootId(reader);
            const input = decodeLintInput(reader);

            return {
                kind: "lint",
                handle,
                input,
            };
        }
        case 14: {
            const handle = decodeRootId(reader);
            const input = decodeFormatInput(reader);

            return {
                kind: "format",
                handle,
                input,
            };
        }
        case 15: {
            const handle = decodeRootId(reader);
            const input = decodeBuildInput(reader);

            return {
                kind: "build",
                handle,
                input,
            };
        }
        case 16: {
            const handle = decodeRootId(reader);
            const input = decodeRunInput(reader);

            return {
                kind: "run",
                handle,
                input,
            };
        }
        case 17: {
            const handle = decodeRootId(reader);
            const input = decodeTestInput(reader);

            return {
                kind: "test",
                handle,
                input,
            };
        }
        case 18: {
            const handle = decodeRootId(reader);
            const input = decodeDocInput(reader);

            return {
                kind: "doc",
                handle,
                input,
            };
        }
        case 19: {
            const handle = decodeRootId(reader);
            const input = decodeBenchInput(reader);

            return {
                kind: "bench",
                handle,
                input,
            };
        }
        case 20: {
            const handle = decodeRootId(reader);
            const input = decodeInfoInput(reader);

            return {
                kind: "info",
                handle,
                input,
            };
        }
        case 21: {
            const handle = decodeRootId(reader);
            const input = decodeTargetsInput(reader);

            return {
                kind: "targets",
                handle,
                input,
            };
        }
        case 22: {
            const handle = decodeRootId(reader);
            const input = decodeCacheInput(reader);

            return {
                kind: "cache",
                handle,
                input,
            };
        }
        case 23: {
            const handle = decodeRootId(reader);
            const input = decodeSettingsInput(reader);

            return {
                kind: "settings",
                handle,
                input,
            };
        }
        case 24: {
            const handle = decodeRootId(reader);
            const input = decodeDoctorInput(reader);

            return {
                kind: "doctor",
                handle,
                input,
            };
        }
        case 25: {
            const handle = decodeRootId(reader);
            const input = decodeTaskInput(reader);

            return {
                kind: "task",
                handle,
                input,
            };
        }
        case 26: {
            const handle = decodeRootId(reader);
            const input = decodeCleanInput(reader);

            return {
                kind: "clean",
                handle,
                input,
            };
        }
        case 27: {
            const handle = decodeRootId(reader);
            const artifact = decodeArtifactReference(reader);

            return {
                kind: "artifact",
                handle,
                artifact,
            };
        }
        case 28: {
            const handle = decodeRootId(reader);
            const content = decodeContent(reader);

            return {
                kind: "store",
                handle,
                content,
            };
        }
        case 29: {
            const handle = decodeRootId(reader);
            const content = decodeContentId(reader);

            return {
                kind: "load",
                handle,
                content,
            };
        }
        case 30: {
            const handle = decodeRootId(reader);
            const request = decodeExportRequest(reader);

            return {
                kind: "export",
                handle,
                request,
            };
        }
        case 31: {
            const query = decodeWorkspaceQuery(reader);

            return { kind: "query", query };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one WorkspaceRequest. */
export function toJsonWorkspaceRequest(value: WorkspaceRequest): Json {
    switch (value.kind) {
        case "handshake":
            return {
                kind: "handshake",
                handshake: toJsonHandshakeRequest(value.handshake),
            };
        case "ping":
            return {
                kind: "ping",
            };
        case "cancel":
            return {
                kind: "cancel",
                id: toJsonRequestId(value.id),
            };
        case "shutdown":
            return {
                kind: "shutdown",
            };
        case "openRoot":
            return {
                kind: "openRoot",
                open_root: toJsonOpenRootRequest(value.open_root),
            };
        case "closeRoot":
            return {
                kind: "closeRoot",
                close_root: toJsonCloseRootRequest(value.close_root),
            };
        case "reloadRoot":
            return {
                kind: "reloadRoot",
                reload_root: toJsonReloadRootRequest(value.reload_root),
            };
        case "applyFileOperation":
            return {
                kind: "applyFileOperation",
                apply_file_operation: toJsonFileOperationRequest(value.apply_file_operation),
            };
        case "applySourceUpdate":
            return {
                kind: "applySourceUpdate",
                apply_source_update: toJsonSourceUpdateRequest(value.apply_source_update),
            };
        case "startWatch":
            return {
                kind: "startWatch",
                start_watch: toJsonWatchStartRequest(value.start_watch),
            };
        case "nextWatchBatch":
            return {
                kind: "nextWatchBatch",
                next_watch_batch: toJsonWatchNextRequest(value.next_watch_batch),
            };
        case "stopWatch":
            return {
                kind: "stopWatch",
                stop_watch: toJsonWatchStopRequest(value.stop_watch),
            };
        case "check":
            return {
                kind: "check",
                handle: toJsonRootId(value.handle),
                input: toJsonCheckInput(value.input),
            };
        case "lint":
            return {
                kind: "lint",
                handle: toJsonRootId(value.handle),
                input: toJsonLintInput(value.input),
            };
        case "format":
            return {
                kind: "format",
                handle: toJsonRootId(value.handle),
                input: toJsonFormatInput(value.input),
            };
        case "build":
            return {
                kind: "build",
                handle: toJsonRootId(value.handle),
                input: toJsonBuildInput(value.input),
            };
        case "run":
            return {
                kind: "run",
                handle: toJsonRootId(value.handle),
                input: toJsonRunInput(value.input),
            };
        case "test":
            return {
                kind: "test",
                handle: toJsonRootId(value.handle),
                input: toJsonTestInput(value.input),
            };
        case "doc":
            return {
                kind: "doc",
                handle: toJsonRootId(value.handle),
                input: toJsonDocInput(value.input),
            };
        case "bench":
            return {
                kind: "bench",
                handle: toJsonRootId(value.handle),
                input: toJsonBenchInput(value.input),
            };
        case "info":
            return {
                kind: "info",
                handle: toJsonRootId(value.handle),
                input: toJsonInfoInput(value.input),
            };
        case "targets":
            return {
                kind: "targets",
                handle: toJsonRootId(value.handle),
                input: toJsonTargetsInput(value.input),
            };
        case "cache":
            return {
                kind: "cache",
                handle: toJsonRootId(value.handle),
                input: toJsonCacheInput(value.input),
            };
        case "settings":
            return {
                kind: "settings",
                handle: toJsonRootId(value.handle),
                input: toJsonSettingsInput(value.input),
            };
        case "doctor":
            return {
                kind: "doctor",
                handle: toJsonRootId(value.handle),
                input: toJsonDoctorInput(value.input),
            };
        case "task":
            return {
                kind: "task",
                handle: toJsonRootId(value.handle),
                input: toJsonTaskInput(value.input),
            };
        case "clean":
            return {
                kind: "clean",
                handle: toJsonRootId(value.handle),
                input: toJsonCleanInput(value.input),
            };
        case "artifact":
            return {
                kind: "artifact",
                handle: toJsonRootId(value.handle),
                artifact: toJsonArtifactReference(value.artifact),
            };
        case "store":
            return {
                kind: "store",
                handle: toJsonRootId(value.handle),
                content: toJsonContent(value.content),
            };
        case "load":
            return {
                kind: "load",
                handle: toJsonRootId(value.handle),
                content: toJsonContentId(value.content),
            };
        case "export":
            return {
                kind: "export",
                handle: toJsonRootId(value.handle),
                request: toJsonExportRequest(value.request),
            };
        case "query":
            return {
                kind: "query",
                query: toJsonWorkspaceQuery(value.query),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one WorkspaceRequest from one JSON value. */
export function fromJsonWorkspaceRequest(value: Json): WorkspaceRequest {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "handshake":
            return {
                kind,
                handshake: fromJsonHandshakeRequest(jsonField(object, "handshake")),
            };
        case "ping":
            return {
                kind,
            };
        case "cancel":
            return {
                kind,
                id: fromJsonRequestId(jsonField(object, "id")),
            };
        case "shutdown":
            return {
                kind,
            };
        case "openRoot":
            return {
                kind,
                open_root: fromJsonOpenRootRequest(jsonField(object, "open_root")),
            };
        case "closeRoot":
            return {
                kind,
                close_root: fromJsonCloseRootRequest(jsonField(object, "close_root")),
            };
        case "reloadRoot":
            return {
                kind,
                reload_root: fromJsonReloadRootRequest(jsonField(object, "reload_root")),
            };
        case "applyFileOperation":
            return {
                kind,
                apply_file_operation: fromJsonFileOperationRequest(jsonField(object, "apply_file_operation")),
            };
        case "applySourceUpdate":
            return {
                kind,
                apply_source_update: fromJsonSourceUpdateRequest(jsonField(object, "apply_source_update")),
            };
        case "startWatch":
            return {
                kind,
                start_watch: fromJsonWatchStartRequest(jsonField(object, "start_watch")),
            };
        case "nextWatchBatch":
            return {
                kind,
                next_watch_batch: fromJsonWatchNextRequest(jsonField(object, "next_watch_batch")),
            };
        case "stopWatch":
            return {
                kind,
                stop_watch: fromJsonWatchStopRequest(jsonField(object, "stop_watch")),
            };
        case "check":
            return {
                kind,
                handle: fromJsonRootId(jsonField(object, "handle")),
                input: fromJsonCheckInput(jsonField(object, "input")),
            };
        case "lint":
            return {
                kind,
                handle: fromJsonRootId(jsonField(object, "handle")),
                input: fromJsonLintInput(jsonField(object, "input")),
            };
        case "format":
            return {
                kind,
                handle: fromJsonRootId(jsonField(object, "handle")),
                input: fromJsonFormatInput(jsonField(object, "input")),
            };
        case "build":
            return {
                kind,
                handle: fromJsonRootId(jsonField(object, "handle")),
                input: fromJsonBuildInput(jsonField(object, "input")),
            };
        case "run":
            return {
                kind,
                handle: fromJsonRootId(jsonField(object, "handle")),
                input: fromJsonRunInput(jsonField(object, "input")),
            };
        case "test":
            return {
                kind,
                handle: fromJsonRootId(jsonField(object, "handle")),
                input: fromJsonTestInput(jsonField(object, "input")),
            };
        case "doc":
            return {
                kind,
                handle: fromJsonRootId(jsonField(object, "handle")),
                input: fromJsonDocInput(jsonField(object, "input")),
            };
        case "bench":
            return {
                kind,
                handle: fromJsonRootId(jsonField(object, "handle")),
                input: fromJsonBenchInput(jsonField(object, "input")),
            };
        case "info":
            return {
                kind,
                handle: fromJsonRootId(jsonField(object, "handle")),
                input: fromJsonInfoInput(jsonField(object, "input")),
            };
        case "targets":
            return {
                kind,
                handle: fromJsonRootId(jsonField(object, "handle")),
                input: fromJsonTargetsInput(jsonField(object, "input")),
            };
        case "cache":
            return {
                kind,
                handle: fromJsonRootId(jsonField(object, "handle")),
                input: fromJsonCacheInput(jsonField(object, "input")),
            };
        case "settings":
            return {
                kind,
                handle: fromJsonRootId(jsonField(object, "handle")),
                input: fromJsonSettingsInput(jsonField(object, "input")),
            };
        case "doctor":
            return {
                kind,
                handle: fromJsonRootId(jsonField(object, "handle")),
                input: fromJsonDoctorInput(jsonField(object, "input")),
            };
        case "task":
            return {
                kind,
                handle: fromJsonRootId(jsonField(object, "handle")),
                input: fromJsonTaskInput(jsonField(object, "input")),
            };
        case "clean":
            return {
                kind,
                handle: fromJsonRootId(jsonField(object, "handle")),
                input: fromJsonCleanInput(jsonField(object, "input")),
            };
        case "artifact":
            return {
                kind,
                handle: fromJsonRootId(jsonField(object, "handle")),
                artifact: fromJsonArtifactReference(jsonField(object, "artifact")),
            };
        case "store":
            return {
                kind,
                handle: fromJsonRootId(jsonField(object, "handle")),
                content: fromJsonContent(jsonField(object, "content")),
            };
        case "load":
            return {
                kind,
                handle: fromJsonRootId(jsonField(object, "handle")),
                content: fromJsonContentId(jsonField(object, "content")),
            };
        case "export":
            return {
                kind,
                handle: fromJsonRootId(jsonField(object, "handle")),
                request: fromJsonExportRequest(jsonField(object, "request")),
            };
        case "query":
            return {
                kind,
                query: fromJsonWorkspaceQuery(jsonField(object, "query")),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}
