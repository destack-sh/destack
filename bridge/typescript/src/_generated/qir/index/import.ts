/* generated by destack_bridge_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, jsonArray, jsonField, jsonObject, jsonOptional, jsonString } from "../../../protocol/serde.js";
import type { SymbolKind } from "../../dir/symbol/symbol.js";
import type { LocalSymbolId } from "../../dir/symbol/symbol.js";
import type { SymbolSpace } from "../../dir/symbol/symbol.js";
import type { ModuleId } from "../../source/file/model/module.js";
import { decodeSymbolKind, encodeSymbolKind, fromJsonSymbolKind, toJsonSymbolKind } from "../../dir/symbol/symbol.js";
import { decodeLocalSymbolId, encodeLocalSymbolId, fromJsonLocalSymbolId, toJsonLocalSymbolId } from "../../dir/symbol/symbol.js";
import { decodeSymbolSpace, encodeSymbolSpace, fromJsonSymbolSpace, toJsonSymbolSpace } from "../../dir/symbol/symbol.js";
import { decodeModuleId, encodeModuleId, fromJsonModuleId, toJsonModuleId } from "../../source/file/model/module.js";

/** Importable export index. */
export type ImportIndex = {
    /** The import entries in stable display order. */
    readonly entries: ReadonlyArray<ImportEntry>;
};

export const ImportIndex = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ImportIndex): void {
        encodeImportIndex(writer, value);
    },

    /** Decode one ImportIndex. */
    decode(reader: BinaryReader): ImportIndex {
        return decodeImportIndex(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ImportIndex): Json {
        return toJsonImportIndex(value);
    },

    /** Return one ImportIndex from one JSON value. */
    fromJson(value: Json): ImportIndex {
        return fromJsonImportIndex(value);
    },
};

/** Encode one ImportIndex. */
export function encodeImportIndex(writer: BinaryWriter, value: ImportIndex): void {
    writer.writeUnsigned(value.entries.length);
    for (const item0 of value.entries) {
        encodeImportEntry(writer, item0);
    }
}

/** Decode one ImportIndex. */
export function decodeImportIndex(reader: BinaryReader): ImportIndex {
    const entries = (() => { const length0 = reader.readNumber(); const items0: Array<ImportEntry> = []; for (let index = 0; index < length0; index += 1) { items0.push(decodeImportEntry(reader)); } return items0; })();

    return {
        entries,
    };
}

/** Return one JSON value for one ImportIndex. */
export function toJsonImportIndex(value: ImportIndex): Json {
    return {
        entries: value.entries.map((item0) => toJsonImportEntry(item0)),
    };
}

/** Return one ImportIndex from one JSON value. */
export function fromJsonImportIndex(value: Json): ImportIndex {
    const object = jsonObject(value);

    return {
        entries: jsonArray(jsonField(object, "entries")).map((item0) => fromJsonImportEntry(item0)),
    };
}

/** Importable exported symbol entry. */
export type ImportEntry = {
    /** The exported symbol name. */
    readonly name: string;
    /** The exported symbol kind. */
    readonly kind: SymbolKind;
    /** The exported symbol space. */
    readonly space: SymbolSpace;
    /** The module that exports this symbol. */
    readonly moduleId: ModuleId;
    /** The local exported symbol id. */
    readonly localId: LocalSymbolId;
    /** The module path to use in imports. */
    readonly modulePath?: string;
};

export const ImportEntry = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ImportEntry): void {
        encodeImportEntry(writer, value);
    },

    /** Decode one ImportEntry. */
    decode(reader: BinaryReader): ImportEntry {
        return decodeImportEntry(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ImportEntry): Json {
        return toJsonImportEntry(value);
    },

    /** Return one ImportEntry from one JSON value. */
    fromJson(value: Json): ImportEntry {
        return fromJsonImportEntry(value);
    },
};

/** Encode one ImportEntry. */
export function encodeImportEntry(writer: BinaryWriter, value: ImportEntry): void {
    writer.writeString(value.name);
    encodeSymbolKind(writer, value.kind);
    encodeSymbolSpace(writer, value.space);
    encodeModuleId(writer, value.moduleId);
    encodeLocalSymbolId(writer, value.localId);
    writer.writeOption(value.modulePath, (value5) => {
        writer.writeString(value5);
    });
}

/** Decode one ImportEntry. */
export function decodeImportEntry(reader: BinaryReader): ImportEntry {
    const name = reader.readString();
    const kind = decodeSymbolKind(reader);
    const space = decodeSymbolSpace(reader);
    const moduleId = decodeModuleId(reader);
    const localId = decodeLocalSymbolId(reader);
    const modulePath = reader.readOption(() => reader.readString());

    return {
        name,
        kind,
        space,
        moduleId,
        localId,
        ...(modulePath === undefined ? {} : { modulePath }),
    };
}

/** Return one JSON value for one ImportEntry. */
export function toJsonImportEntry(value: ImportEntry): Json {
    return {
        name: value.name,
        kind: toJsonSymbolKind(value.kind),
        space: toJsonSymbolSpace(value.space),
        moduleId: toJsonModuleId(value.moduleId),
        localId: toJsonLocalSymbolId(value.localId),
        ...(value.modulePath === undefined ? {} : { modulePath: value.modulePath }),
    };
}

/** Return one ImportEntry from one JSON value. */
export function fromJsonImportEntry(value: Json): ImportEntry {
    const object = jsonObject(value);

    return {
        name: jsonString(jsonField(object, "name")),
        kind: fromJsonSymbolKind(jsonField(object, "kind")),
        space: fromJsonSymbolSpace(jsonField(object, "space")),
        moduleId: fromJsonModuleId(jsonField(object, "moduleId")),
        localId: fromJsonLocalSymbolId(jsonField(object, "localId")),
        modulePath: jsonOptional(object, "modulePath", (value) => jsonString(value)),
    };
}
