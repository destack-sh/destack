/* generated by destack_bridge_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, jsonArray, jsonBool, jsonField, jsonInteger, jsonObject, jsonOptional, jsonString } from "../../../protocol/serde.js";
import type { GlobalSymbolId } from "../../dir/symbol/symbol.js";
import type { GlobalNodeIdAny } from "../../dir/tree/node.js";
import type { GlobalTypeId } from "../../dir/type/type.js";
import type { Name } from "./name.js";
import type { FileId } from "../../source/file/model/file.js";
import type { ModuleId } from "../../source/file/model/module.js";
import type { Span } from "../../source/file/model/span.js";
import { decodeGlobalSymbolId, encodeGlobalSymbolId, fromJsonGlobalSymbolId, toJsonGlobalSymbolId } from "../../dir/symbol/symbol.js";
import { decodeGlobalNodeIdAny, encodeGlobalNodeIdAny, fromJsonGlobalNodeIdAny, toJsonGlobalNodeIdAny } from "../../dir/tree/node.js";
import { decodeGlobalTypeId, encodeGlobalTypeId, fromJsonGlobalTypeId, toJsonGlobalTypeId } from "../../dir/type/type.js";
import { decodeName, encodeName, fromJsonName, toJsonName } from "./name.js";
import { decodeFileId, encodeFileId, fromJsonFileId, toJsonFileId } from "../../source/file/model/file.js";
import { decodeModuleId, encodeModuleId, fromJsonModuleId, toJsonModuleId } from "../../source/file/model/module.js";
import { decodeSpan, encodeSpan, fromJsonSpan, toJsonSpan } from "../../source/file/model/span.js";

/** Searchable source member index. */
export type MemberIndex = {
    /** The member entries in stable source order. */
    readonly entries: ReadonlyArray<MemberEntry>;
    /** Member entry indexes ordered by name. */
    readonly byName: ReadonlyArray<number>;
    /** Member entry indexes ordered by owner symbol. */
    readonly byOwner: ReadonlyArray<number>;
    /** Member entry indexes ordered by declaring symbol. */
    readonly byDeclaring: ReadonlyArray<number>;
    /** Member entry indexes ordered by member symbol. */
    readonly bySymbol: ReadonlyArray<number>;
};

export const MemberIndex = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: MemberIndex): void {
        encodeMemberIndex(writer, value);
    },

    /** Decode one MemberIndex. */
    decode(reader: BinaryReader): MemberIndex {
        return decodeMemberIndex(reader);
    },

    /** Return this value as JSON. */
    toJson(value: MemberIndex): Json {
        return toJsonMemberIndex(value);
    },

    /** Return one MemberIndex from one JSON value. */
    fromJson(value: Json): MemberIndex {
        return fromJsonMemberIndex(value);
    },
};

/** Encode one MemberIndex. */
export function encodeMemberIndex(writer: BinaryWriter, value: MemberIndex): void {
    writer.writeUnsigned(value.entries.length);
    for (const item0 of value.entries) {
        encodeMemberEntry(writer, item0);
    }
    writer.writeUnsigned(value.byName.length);
    for (const item1 of value.byName) {
        writer.writeUnsigned(item1);
    }
    writer.writeUnsigned(value.byOwner.length);
    for (const item2 of value.byOwner) {
        writer.writeUnsigned(item2);
    }
    writer.writeUnsigned(value.byDeclaring.length);
    for (const item3 of value.byDeclaring) {
        writer.writeUnsigned(item3);
    }
    writer.writeUnsigned(value.bySymbol.length);
    for (const item4 of value.bySymbol) {
        writer.writeUnsigned(item4);
    }
}

/** Decode one MemberIndex. */
export function decodeMemberIndex(reader: BinaryReader): MemberIndex {
    const entries = (() => { const length0 = reader.readNumber(); const items0: Array<MemberEntry> = []; for (let index = 0; index < length0; index += 1) { items0.push(decodeMemberEntry(reader)); } return items0; })();
    const byName = (() => { const length1 = reader.readNumber(); const items1: Array<number> = []; for (let index = 0; index < length1; index += 1) { items1.push(reader.readNumber()); } return items1; })();
    const byOwner = (() => { const length2 = reader.readNumber(); const items2: Array<number> = []; for (let index = 0; index < length2; index += 1) { items2.push(reader.readNumber()); } return items2; })();
    const byDeclaring = (() => { const length3 = reader.readNumber(); const items3: Array<number> = []; for (let index = 0; index < length3; index += 1) { items3.push(reader.readNumber()); } return items3; })();
    const bySymbol = (() => { const length4 = reader.readNumber(); const items4: Array<number> = []; for (let index = 0; index < length4; index += 1) { items4.push(reader.readNumber()); } return items4; })();

    return {
        entries,
        byName,
        byOwner,
        byDeclaring,
        bySymbol,
    };
}

/** Return one JSON value for one MemberIndex. */
export function toJsonMemberIndex(value: MemberIndex): Json {
    return {
        entries: value.entries.map((item0) => toJsonMemberEntry(item0)),
        byName: value.byName.map((item0) => item0),
        byOwner: value.byOwner.map((item0) => item0),
        byDeclaring: value.byDeclaring.map((item0) => item0),
        bySymbol: value.bySymbol.map((item0) => item0),
    };
}

/** Return one MemberIndex from one JSON value. */
export function fromJsonMemberIndex(value: Json): MemberIndex {
    const object = jsonObject(value);

    return {
        entries: jsonArray(jsonField(object, "entries")).map((item0) => fromJsonMemberEntry(item0)),
        byName: jsonArray(jsonField(object, "byName")).map((item0) => jsonInteger(item0)),
        byOwner: jsonArray(jsonField(object, "byOwner")).map((item0) => jsonInteger(item0)),
        byDeclaring: jsonArray(jsonField(object, "byDeclaring")).map((item0) => jsonInteger(item0)),
        bySymbol: jsonArray(jsonField(object, "bySymbol")).map((item0) => jsonInteger(item0)),
    };
}

/** Searchable source member entry. */
export type MemberEntry = {
    /** The member name. */
    readonly name: Name;
    /** The member kind. */
    readonly kind: MemberKind;
    /** The owner symbol. */
    readonly ownerSymbol: GlobalSymbolId;
    /** The symbol whose declaration contains this member. */
    readonly declaringSymbol: GlobalSymbolId;
    /** The member symbol. */
    readonly memberSymbol: GlobalSymbolId;
    /** The source node that declares the member. */
    readonly sourceNode: GlobalNodeIdAny;
    /** The module containing the member declaration. */
    readonly moduleId: ModuleId;
    /** The source file. */
    readonly fileId: FileId;
    /** The source range. */
    readonly range: Span;
    /** The containing symbol display name. */
    readonly containerName?: string;
    /** The checked type of the member when known. */
    readonly declaredType?: GlobalTypeId;
    /** The member source family. */
    readonly source: MemberSource;
    /** Whether this member belongs to the static surface. */
    readonly isStatic: boolean;
};

export const MemberEntry = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: MemberEntry): void {
        encodeMemberEntry(writer, value);
    },

    /** Decode one MemberEntry. */
    decode(reader: BinaryReader): MemberEntry {
        return decodeMemberEntry(reader);
    },

    /** Return this value as JSON. */
    toJson(value: MemberEntry): Json {
        return toJsonMemberEntry(value);
    },

    /** Return one MemberEntry from one JSON value. */
    fromJson(value: Json): MemberEntry {
        return fromJsonMemberEntry(value);
    },
};

/** Encode one MemberEntry. */
export function encodeMemberEntry(writer: BinaryWriter, value: MemberEntry): void {
    encodeName(writer, value.name);
    encodeMemberKind(writer, value.kind);
    encodeGlobalSymbolId(writer, value.ownerSymbol);
    encodeGlobalSymbolId(writer, value.declaringSymbol);
    encodeGlobalSymbolId(writer, value.memberSymbol);
    encodeGlobalNodeIdAny(writer, value.sourceNode);
    encodeModuleId(writer, value.moduleId);
    encodeFileId(writer, value.fileId);
    encodeSpan(writer, value.range);
    writer.writeOption(value.containerName, (value9) => {
        writer.writeString(value9);
    });
    writer.writeOption(value.declaredType, (value10) => {
        encodeGlobalTypeId(writer, value10);
    });
    encodeMemberSource(writer, value.source);
    writer.writeBool(value.isStatic);
}

/** Decode one MemberEntry. */
export function decodeMemberEntry(reader: BinaryReader): MemberEntry {
    const name = decodeName(reader);
    const kind = decodeMemberKind(reader);
    const ownerSymbol = decodeGlobalSymbolId(reader);
    const declaringSymbol = decodeGlobalSymbolId(reader);
    const memberSymbol = decodeGlobalSymbolId(reader);
    const sourceNode = decodeGlobalNodeIdAny(reader);
    const moduleId = decodeModuleId(reader);
    const fileId = decodeFileId(reader);
    const range = decodeSpan(reader);
    const containerName = reader.readOption(() => reader.readString());
    const declaredType = reader.readOption(() => decodeGlobalTypeId(reader));
    const source = decodeMemberSource(reader);
    const isStatic = reader.readBool();

    return {
        name,
        kind,
        ownerSymbol,
        declaringSymbol,
        memberSymbol,
        sourceNode,
        moduleId,
        fileId,
        range,
        ...(containerName === undefined ? {} : { containerName }),
        ...(declaredType === undefined ? {} : { declaredType }),
        source,
        isStatic,
    };
}

/** Return one JSON value for one MemberEntry. */
export function toJsonMemberEntry(value: MemberEntry): Json {
    return {
        name: toJsonName(value.name),
        kind: toJsonMemberKind(value.kind),
        ownerSymbol: toJsonGlobalSymbolId(value.ownerSymbol),
        declaringSymbol: toJsonGlobalSymbolId(value.declaringSymbol),
        memberSymbol: toJsonGlobalSymbolId(value.memberSymbol),
        sourceNode: toJsonGlobalNodeIdAny(value.sourceNode),
        moduleId: toJsonModuleId(value.moduleId),
        fileId: toJsonFileId(value.fileId),
        range: toJsonSpan(value.range),
        ...(value.containerName === undefined ? {} : { containerName: value.containerName }),
        ...(value.declaredType === undefined ? {} : { declaredType: toJsonGlobalTypeId(value.declaredType) }),
        source: toJsonMemberSource(value.source),
        isStatic: value.isStatic,
    };
}

/** Return one MemberEntry from one JSON value. */
export function fromJsonMemberEntry(value: Json): MemberEntry {
    const object = jsonObject(value);

    return {
        name: fromJsonName(jsonField(object, "name")),
        kind: fromJsonMemberKind(jsonField(object, "kind")),
        ownerSymbol: fromJsonGlobalSymbolId(jsonField(object, "ownerSymbol")),
        declaringSymbol: fromJsonGlobalSymbolId(jsonField(object, "declaringSymbol")),
        memberSymbol: fromJsonGlobalSymbolId(jsonField(object, "memberSymbol")),
        sourceNode: fromJsonGlobalNodeIdAny(jsonField(object, "sourceNode")),
        moduleId: fromJsonModuleId(jsonField(object, "moduleId")),
        fileId: fromJsonFileId(jsonField(object, "fileId")),
        range: fromJsonSpan(jsonField(object, "range")),
        containerName: jsonOptional(object, "containerName", (value) => jsonString(value)),
        declaredType: jsonOptional(object, "declaredType", (value) => fromJsonGlobalTypeId(value)),
        source: fromJsonMemberSource(jsonField(object, "source")),
        isStatic: jsonBool(jsonField(object, "isStatic")),
    };
}

/** Searchable source member kind. */
export type MemberKind = "field" | "method" | "associatedType" | "associatedConst" | "variant";

export const MemberKind = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: MemberKind): void {
        encodeMemberKind(writer, value);
    },

    /** Decode one MemberKind. */
    decode(reader: BinaryReader): MemberKind {
        return decodeMemberKind(reader);
    },

    /** Return this value as JSON. */
    toJson(value: MemberKind): Json {
        return toJsonMemberKind(value);
    },

    /** Return one MemberKind from one JSON value. */
    fromJson(value: Json): MemberKind {
        return fromJsonMemberKind(value);
    },
};

/** Encode one MemberKind. */
export function encodeMemberKind(writer: BinaryWriter, value: MemberKind): void {
    switch (value) {
        case "field":
            writer.writeUnsigned(0);
            return;
        case "method":
            writer.writeUnsigned(1);
            return;
        case "associatedType":
            writer.writeUnsigned(2);
            return;
        case "associatedConst":
            writer.writeUnsigned(3);
            return;
        case "variant":
            writer.writeUnsigned(4);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one MemberKind. */
export function decodeMemberKind(reader: BinaryReader): MemberKind {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "field";
        case 1:
            return "method";
        case 2:
            return "associatedType";
        case 3:
            return "associatedConst";
        case 4:
            return "variant";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one MemberKind. */
export function toJsonMemberKind(value: MemberKind): Json {
    return value;
}

/** Return one MemberKind from one JSON value. */
export function fromJsonMemberKind(value: Json): MemberKind {
    const variant = jsonString(value);

    switch (variant) {
        case "field":
            return "field";
        case "method":
            return "method";
        case "associatedType":
            return "associatedType";
        case "associatedConst":
            return "associatedConst";
        case "variant":
            return "variant";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** Searchable source member source family. */
export type MemberSource = "declaration" | "typeMember" | "enumVariant" | "extension";

export const MemberSource = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: MemberSource): void {
        encodeMemberSource(writer, value);
    },

    /** Decode one MemberSource. */
    decode(reader: BinaryReader): MemberSource {
        return decodeMemberSource(reader);
    },

    /** Return this value as JSON. */
    toJson(value: MemberSource): Json {
        return toJsonMemberSource(value);
    },

    /** Return one MemberSource from one JSON value. */
    fromJson(value: Json): MemberSource {
        return fromJsonMemberSource(value);
    },
};

/** Encode one MemberSource. */
export function encodeMemberSource(writer: BinaryWriter, value: MemberSource): void {
    switch (value) {
        case "declaration":
            writer.writeUnsigned(0);
            return;
        case "typeMember":
            writer.writeUnsigned(1);
            return;
        case "enumVariant":
            writer.writeUnsigned(2);
            return;
        case "extension":
            writer.writeUnsigned(3);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one MemberSource. */
export function decodeMemberSource(reader: BinaryReader): MemberSource {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "declaration";
        case 1:
            return "typeMember";
        case 2:
            return "enumVariant";
        case 3:
            return "extension";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one MemberSource. */
export function toJsonMemberSource(value: MemberSource): Json {
    return value;
}

/** Return one MemberSource from one JSON value. */
export function fromJsonMemberSource(value: Json): MemberSource {
    const variant = jsonString(value);

    switch (variant) {
        case "declaration":
            return "declaration";
        case "typeMember":
            return "typeMember";
        case "enumVariant":
            return "enumVariant";
        case "extension":
            return "extension";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}
