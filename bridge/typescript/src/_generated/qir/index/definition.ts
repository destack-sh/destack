/* generated by destack_bridge_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, jsonArray, jsonField, jsonObject, jsonString } from "../../../protocol/serde.js";
import type { GlobalSymbolId } from "../../dir/symbol/symbol.js";
import { decodeGlobalSymbolId, encodeGlobalSymbolId, fromJsonGlobalSymbolId, toJsonGlobalSymbolId } from "../../dir/symbol/symbol.js";

/** Query index entries read from checked definitions. */
export type DefinitionIndex = {
    /** Nominal relation entries ordered by target symbol. */
    readonly relationsByTarget: ReadonlyArray<NominalEntry>;
    /** Extension entries ordered by target symbol. */
    readonly extensionsByTarget: ReadonlyArray<ExtensionEntry>;
};

export const DefinitionIndex = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: DefinitionIndex): void {
        encodeDefinitionIndex(writer, value);
    },

    /** Decode one DefinitionIndex. */
    decode(reader: BinaryReader): DefinitionIndex {
        return decodeDefinitionIndex(reader);
    },

    /** Return this value as JSON. */
    toJson(value: DefinitionIndex): Json {
        return toJsonDefinitionIndex(value);
    },

    /** Return one DefinitionIndex from one JSON value. */
    fromJson(value: Json): DefinitionIndex {
        return fromJsonDefinitionIndex(value);
    },
};

/** Encode one DefinitionIndex. */
export function encodeDefinitionIndex(writer: BinaryWriter, value: DefinitionIndex): void {
    writer.writeUnsigned(value.relationsByTarget.length);
    for (const item0 of value.relationsByTarget) {
        encodeNominalEntry(writer, item0);
    }
    writer.writeUnsigned(value.extensionsByTarget.length);
    for (const item1 of value.extensionsByTarget) {
        encodeExtensionEntry(writer, item1);
    }
}

/** Decode one DefinitionIndex. */
export function decodeDefinitionIndex(reader: BinaryReader): DefinitionIndex {
    const relationsByTarget = (() => { const length0 = reader.readNumber(); const items0: Array<NominalEntry> = []; for (let index = 0; index < length0; index += 1) { items0.push(decodeNominalEntry(reader)); } return items0; })();
    const extensionsByTarget = (() => { const length1 = reader.readNumber(); const items1: Array<ExtensionEntry> = []; for (let index = 0; index < length1; index += 1) { items1.push(decodeExtensionEntry(reader)); } return items1; })();

    return {
        relationsByTarget,
        extensionsByTarget,
    };
}

/** Return one JSON value for one DefinitionIndex. */
export function toJsonDefinitionIndex(value: DefinitionIndex): Json {
    return {
        relationsByTarget: value.relationsByTarget.map((item0) => toJsonNominalEntry(item0)),
        extensionsByTarget: value.extensionsByTarget.map((item0) => toJsonExtensionEntry(item0)),
    };
}

/** Return one DefinitionIndex from one JSON value. */
export function fromJsonDefinitionIndex(value: Json): DefinitionIndex {
    const object = jsonObject(value);

    return {
        relationsByTarget: jsonArray(jsonField(object, "relationsByTarget")).map((item0) => fromJsonNominalEntry(item0)),
        extensionsByTarget: jsonArray(jsonField(object, "extensionsByTarget")).map((item0) => fromJsonExtensionEntry(item0)),
    };
}

/** Nominal relation entry. */
export type NominalEntry = {
    /** The source nominal symbol. */
    readonly sourceSymbol: GlobalSymbolId;
    /** The target nominal symbol. */
    readonly targetSymbol: GlobalSymbolId;
    /** The relation kind. */
    readonly relation: NominalRelation;
};

export const NominalEntry = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: NominalEntry): void {
        encodeNominalEntry(writer, value);
    },

    /** Decode one NominalEntry. */
    decode(reader: BinaryReader): NominalEntry {
        return decodeNominalEntry(reader);
    },

    /** Return this value as JSON. */
    toJson(value: NominalEntry): Json {
        return toJsonNominalEntry(value);
    },

    /** Return one NominalEntry from one JSON value. */
    fromJson(value: Json): NominalEntry {
        return fromJsonNominalEntry(value);
    },
};

/** Encode one NominalEntry. */
export function encodeNominalEntry(writer: BinaryWriter, value: NominalEntry): void {
    encodeGlobalSymbolId(writer, value.sourceSymbol);
    encodeGlobalSymbolId(writer, value.targetSymbol);
    encodeNominalRelation(writer, value.relation);
}

/** Decode one NominalEntry. */
export function decodeNominalEntry(reader: BinaryReader): NominalEntry {
    const sourceSymbol = decodeGlobalSymbolId(reader);
    const targetSymbol = decodeGlobalSymbolId(reader);
    const relation = decodeNominalRelation(reader);

    return {
        sourceSymbol,
        targetSymbol,
        relation,
    };
}

/** Return one JSON value for one NominalEntry. */
export function toJsonNominalEntry(value: NominalEntry): Json {
    return {
        sourceSymbol: toJsonGlobalSymbolId(value.sourceSymbol),
        targetSymbol: toJsonGlobalSymbolId(value.targetSymbol),
        relation: toJsonNominalRelation(value.relation),
    };
}

/** Return one NominalEntry from one JSON value. */
export function fromJsonNominalEntry(value: Json): NominalEntry {
    const object = jsonObject(value);

    return {
        sourceSymbol: fromJsonGlobalSymbolId(jsonField(object, "sourceSymbol")),
        targetSymbol: fromJsonGlobalSymbolId(jsonField(object, "targetSymbol")),
        relation: fromJsonNominalRelation(jsonField(object, "relation")),
    };
}

/** Nominal relation kind. */
export type NominalRelation = "extends" | "implements";

export const NominalRelation = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: NominalRelation): void {
        encodeNominalRelation(writer, value);
    },

    /** Decode one NominalRelation. */
    decode(reader: BinaryReader): NominalRelation {
        return decodeNominalRelation(reader);
    },

    /** Return this value as JSON. */
    toJson(value: NominalRelation): Json {
        return toJsonNominalRelation(value);
    },

    /** Return one NominalRelation from one JSON value. */
    fromJson(value: Json): NominalRelation {
        return fromJsonNominalRelation(value);
    },
};

/** Encode one NominalRelation. */
export function encodeNominalRelation(writer: BinaryWriter, value: NominalRelation): void {
    switch (value) {
        case "extends":
            writer.writeUnsigned(0);
            return;
        case "implements":
            writer.writeUnsigned(1);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one NominalRelation. */
export function decodeNominalRelation(reader: BinaryReader): NominalRelation {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "extends";
        case 1:
            return "implements";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one NominalRelation. */
export function toJsonNominalRelation(value: NominalRelation): Json {
    return value;
}

/** Return one NominalRelation from one JSON value. */
export function fromJsonNominalRelation(value: Json): NominalRelation {
    const variant = jsonString(value);

    switch (variant) {
        case "extends":
            return "extends";
        case "implements":
            return "implements";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** Extension declaration entry. */
export type ExtensionEntry = {
    /** The extension declaration symbol. */
    readonly extensionSymbol: GlobalSymbolId;
    /** The canonical target symbol. */
    readonly targetSymbol: GlobalSymbolId;
};

export const ExtensionEntry = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ExtensionEntry): void {
        encodeExtensionEntry(writer, value);
    },

    /** Decode one ExtensionEntry. */
    decode(reader: BinaryReader): ExtensionEntry {
        return decodeExtensionEntry(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ExtensionEntry): Json {
        return toJsonExtensionEntry(value);
    },

    /** Return one ExtensionEntry from one JSON value. */
    fromJson(value: Json): ExtensionEntry {
        return fromJsonExtensionEntry(value);
    },
};

/** Encode one ExtensionEntry. */
export function encodeExtensionEntry(writer: BinaryWriter, value: ExtensionEntry): void {
    encodeGlobalSymbolId(writer, value.extensionSymbol);
    encodeGlobalSymbolId(writer, value.targetSymbol);
}

/** Decode one ExtensionEntry. */
export function decodeExtensionEntry(reader: BinaryReader): ExtensionEntry {
    const extensionSymbol = decodeGlobalSymbolId(reader);
    const targetSymbol = decodeGlobalSymbolId(reader);

    return {
        extensionSymbol,
        targetSymbol,
    };
}

/** Return one JSON value for one ExtensionEntry. */
export function toJsonExtensionEntry(value: ExtensionEntry): Json {
    return {
        extensionSymbol: toJsonGlobalSymbolId(value.extensionSymbol),
        targetSymbol: toJsonGlobalSymbolId(value.targetSymbol),
    };
}

/** Return one ExtensionEntry from one JSON value. */
export function fromJsonExtensionEntry(value: Json): ExtensionEntry {
    const object = jsonObject(value);

    return {
        extensionSymbol: fromJsonGlobalSymbolId(jsonField(object, "extensionSymbol")),
        targetSymbol: fromJsonGlobalSymbolId(jsonField(object, "targetSymbol")),
    };
}
