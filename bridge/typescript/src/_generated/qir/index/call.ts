/* generated by destack_bridge_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, jsonArray, jsonField, jsonObject, jsonOptional } from "../../../protocol/serde.js";
import type { GlobalSymbolId } from "../../dir/symbol/symbol.js";
import type { ModuleId } from "../../source/file/model/module.js";
import type { Span } from "../../source/file/model/span.js";
import { decodeGlobalSymbolId, encodeGlobalSymbolId, fromJsonGlobalSymbolId, toJsonGlobalSymbolId } from "../../dir/symbol/symbol.js";
import { decodeModuleId, encodeModuleId, fromJsonModuleId, toJsonModuleId } from "../../source/file/model/module.js";
import { decodeSpan, encodeSpan, fromJsonSpan, toJsonSpan } from "../../source/file/model/span.js";

/** Call graph index. */
export type CallIndex = {
    /** The call entries ordered by callee symbol. */
    readonly byCallee: ReadonlyArray<CallEntry>;
    /** The call entries ordered by caller symbol. */
    readonly byCaller: ReadonlyArray<CallEntry>;
};

export const CallIndex = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: CallIndex): void {
        encodeCallIndex(writer, value);
    },

    /** Decode one CallIndex. */
    decode(reader: BinaryReader): CallIndex {
        return decodeCallIndex(reader);
    },

    /** Return this value as JSON. */
    toJson(value: CallIndex): Json {
        return toJsonCallIndex(value);
    },

    /** Return one CallIndex from one JSON value. */
    fromJson(value: Json): CallIndex {
        return fromJsonCallIndex(value);
    },
};

/** Encode one CallIndex. */
export function encodeCallIndex(writer: BinaryWriter, value: CallIndex): void {
    writer.writeUnsigned(value.byCallee.length);
    for (const item0 of value.byCallee) {
        encodeCallEntry(writer, item0);
    }
    writer.writeUnsigned(value.byCaller.length);
    for (const item1 of value.byCaller) {
        encodeCallEntry(writer, item1);
    }
}

/** Decode one CallIndex. */
export function decodeCallIndex(reader: BinaryReader): CallIndex {
    const byCallee = (() => { const length0 = reader.readNumber(); const items0: Array<CallEntry> = []; for (let index = 0; index < length0; index += 1) { items0.push(decodeCallEntry(reader)); } return items0; })();
    const byCaller = (() => { const length1 = reader.readNumber(); const items1: Array<CallEntry> = []; for (let index = 0; index < length1; index += 1) { items1.push(decodeCallEntry(reader)); } return items1; })();

    return {
        byCallee,
        byCaller,
    };
}

/** Return one JSON value for one CallIndex. */
export function toJsonCallIndex(value: CallIndex): Json {
    return {
        byCallee: value.byCallee.map((item0) => toJsonCallEntry(item0)),
        byCaller: value.byCaller.map((item0) => toJsonCallEntry(item0)),
    };
}

/** Return one CallIndex from one JSON value. */
export function fromJsonCallIndex(value: Json): CallIndex {
    const object = jsonObject(value);

    return {
        byCallee: jsonArray(jsonField(object, "byCallee")).map((item0) => fromJsonCallEntry(item0)),
        byCaller: jsonArray(jsonField(object, "byCaller")).map((item0) => fromJsonCallEntry(item0)),
    };
}

/** Call graph edge entry. */
export type CallEntry = {
    /** The module containing the call. */
    readonly moduleId: ModuleId;
    /** The containing function symbol when known. */
    readonly callerSymbol?: GlobalSymbolId;
    /** The called function symbol. */
    readonly calleeSymbol: GlobalSymbolId;
    /** The call source range. */
    readonly span: Span;
};

export const CallEntry = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: CallEntry): void {
        encodeCallEntry(writer, value);
    },

    /** Decode one CallEntry. */
    decode(reader: BinaryReader): CallEntry {
        return decodeCallEntry(reader);
    },

    /** Return this value as JSON. */
    toJson(value: CallEntry): Json {
        return toJsonCallEntry(value);
    },

    /** Return one CallEntry from one JSON value. */
    fromJson(value: Json): CallEntry {
        return fromJsonCallEntry(value);
    },
};

/** Encode one CallEntry. */
export function encodeCallEntry(writer: BinaryWriter, value: CallEntry): void {
    encodeModuleId(writer, value.moduleId);
    writer.writeOption(value.callerSymbol, (value1) => {
        encodeGlobalSymbolId(writer, value1);
    });
    encodeGlobalSymbolId(writer, value.calleeSymbol);
    encodeSpan(writer, value.span);
}

/** Decode one CallEntry. */
export function decodeCallEntry(reader: BinaryReader): CallEntry {
    const moduleId = decodeModuleId(reader);
    const callerSymbol = reader.readOption(() => decodeGlobalSymbolId(reader));
    const calleeSymbol = decodeGlobalSymbolId(reader);
    const span = decodeSpan(reader);

    return {
        moduleId,
        ...(callerSymbol === undefined ? {} : { callerSymbol }),
        calleeSymbol,
        span,
    };
}

/** Return one JSON value for one CallEntry. */
export function toJsonCallEntry(value: CallEntry): Json {
    return {
        moduleId: toJsonModuleId(value.moduleId),
        ...(value.callerSymbol === undefined ? {} : { callerSymbol: toJsonGlobalSymbolId(value.callerSymbol) }),
        calleeSymbol: toJsonGlobalSymbolId(value.calleeSymbol),
        span: toJsonSpan(value.span),
    };
}

/** Return one CallEntry from one JSON value. */
export function fromJsonCallEntry(value: Json): CallEntry {
    const object = jsonObject(value);

    return {
        moduleId: fromJsonModuleId(jsonField(object, "moduleId")),
        callerSymbol: jsonOptional(object, "callerSymbol", (value) => fromJsonGlobalSymbolId(value)),
        calleeSymbol: fromJsonGlobalSymbolId(jsonField(object, "calleeSymbol")),
        span: fromJsonSpan(jsonField(object, "span")),
    };
}
