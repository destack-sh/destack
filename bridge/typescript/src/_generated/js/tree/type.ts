/* generated by destack_bridge_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, jsonArray, jsonBool, jsonField, jsonObject, jsonOptional, jsonString } from "../../../protocol/serde.js";
import type { StringId } from "../../core/string.js";
import type { BindingModifier } from "./argument.js";
import type { FunctionSignature } from "./function.js";
import type { Key } from "./key.js";
import type { ScalarLiteral } from "./literal.js";
import type { LocalNodeId } from "./node.js";
import type { Path } from "./path.js";
import { decodeStringId, encodeStringId, fromJsonStringId, toJsonStringId } from "../../core/string.js";
import { decodeBindingModifier, encodeBindingModifier, fromJsonBindingModifier, toJsonBindingModifier } from "./argument.js";
import { decodeFunctionSignature, encodeFunctionSignature, fromJsonFunctionSignature, toJsonFunctionSignature } from "./function.js";
import { decodeKey, encodeKey, fromJsonKey, toJsonKey } from "./key.js";
import { decodeScalarLiteral, encodeScalarLiteral, fromJsonScalarLiteral, toJsonScalarLiteral } from "./literal.js";
import { decodeLocalNodeId, encodeLocalNodeId, fromJsonLocalNodeId, toJsonLocalNodeId } from "./node.js";
import { decodePath, encodePath, fromJsonPath, toJsonPath } from "./path.js";

/** A TypeExpression is a TypeScript type expression. */
export type TypeExpression =
    /** Scalar type literal. */
    | {
          readonly kind: "scalar";
          readonly scalar: TypeLiteral;
      }
    /** This type. */
    | {
          readonly kind: "this";
      }
    /** Path to something. */
    | {
          readonly kind: "path";
          readonly path: Path;
          readonly genericArguments: ReadonlyArray<LocalNodeId>;
      }
    /** `readonly T`. */
    | {
          readonly kind: "readonly";
          readonly targetType: LocalNodeId;
      }
    /** `keyof T`. */
    | {
          readonly kind: "keyOf";
          readonly targetType: LocalNodeId;
      }
    /** `T!`. */
    | {
          readonly kind: "must";
          readonly targetType: LocalNodeId;
      }
    /** `!T`. */
    | {
          readonly kind: "not";
          readonly targetType: LocalNodeId;
      }
    /** `T extends U`. */
    | {
          readonly kind: "extends";
          readonly left: LocalNodeId;
          readonly right: LocalNodeId;
      }
    /** `T implements U`. */
    | {
          readonly kind: "implements";
          readonly left: LocalNodeId;
          readonly right: LocalNodeId;
      }
    /** Conditional type. */
    | {
          readonly kind: "conditional";
          readonly left: LocalNodeId;
          readonly right: LocalNodeId;
          readonly thenType: LocalNodeId;
          readonly elseType: LocalNodeId;
      }
    /** Mapped type. */
    | {
          readonly kind: "mapped";
          readonly parameter: TypeMappedParameter;
          readonly modifiers: TypeMappedModifiers;
          readonly value?: LocalNodeId;
      }
    /** Index access type. */
    | {
          readonly kind: "index";
          readonly left: LocalNodeId;
          readonly index: LocalNodeId;
      }
    /** Template literal type. */
    | {
          readonly kind: "templateLiteral";
          readonly template_literal: TypeTemplateLiteral;
      }
    /** Import type. */
    | {
          readonly kind: "import";
          readonly target: StringId;
          readonly qualifier?: Path;
          readonly genericArguments: ReadonlyArray<LocalNodeId>;
      }
    /** Infer type binding. */
    | {
          readonly kind: "infer";
          readonly name: StringId;
          readonly constraint?: LocalNodeId;
      }
    /** Array type `T[]`. */
    | {
          readonly kind: "array";
          readonly element: LocalNodeId;
      }
    /** Tuple type `[T1, T2, ...]`. */
    | {
          readonly kind: "tuple";
          readonly elements: ReadonlyArray<LocalNodeId>;
      }
    /** Object type `{ a: T1, b: T2, ... }`. */
    | {
          readonly kind: "object";
          readonly members: ReadonlyArray<LocalNodeId>;
      }
    /** Union type `A | B | C`. */
    | {
          readonly kind: "union";
          readonly elements: ReadonlyArray<LocalNodeId>;
      }
    /** Intersection type `A & B & C`. */
    | {
          readonly kind: "intersection";
          readonly elements: ReadonlyArray<LocalNodeId>;
      }
    /** Function type declaration. */
    | {
          readonly kind: "functionTypeDeclaration";
          readonly function_type_declaration: FunctionTypeDeclaration;
      }
    /** Constructor type declaration. */
    | {
          readonly kind: "constructorTypeDeclaration";
          readonly constructor_type_declaration: ConstructorTypeDeclaration;
      }
    /** Error type that could not be resolved. */
    | {
          readonly kind: "error";
      }
;

export const TypeExpression = {
    /** Scalar type literal. */
    scalar(scalar: TypeLiteral): TypeExpression {
        return { kind: "scalar", scalar };
    },

    /** This type. */
    "this"(): TypeExpression {
        return { kind: "this" };
    },

    /** Path to something. */
    path(path: Path, genericArguments: ReadonlyArray<LocalNodeId>): TypeExpression {
        return { kind: "path", path, genericArguments };
    },

    /** `readonly T`. */
    "readonly"(targetType: LocalNodeId): TypeExpression {
        return { kind: "readonly", targetType };
    },

    /** `keyof T`. */
    keyOf(targetType: LocalNodeId): TypeExpression {
        return { kind: "keyOf", targetType };
    },

    /** `T!`. */
    must(targetType: LocalNodeId): TypeExpression {
        return { kind: "must", targetType };
    },

    /** `!T`. */
    not(targetType: LocalNodeId): TypeExpression {
        return { kind: "not", targetType };
    },

    /** `T extends U`. */
    "extends"(left: LocalNodeId, right: LocalNodeId): TypeExpression {
        return { kind: "extends", left, right };
    },

    /** `T implements U`. */
    "implements"(left: LocalNodeId, right: LocalNodeId): TypeExpression {
        return { kind: "implements", left, right };
    },

    /** Conditional type. */
    conditional(left: LocalNodeId, right: LocalNodeId, thenType: LocalNodeId, elseType: LocalNodeId): TypeExpression {
        return { kind: "conditional", left, right, thenType, elseType };
    },

    /** Mapped type. */
    mapped(parameter: TypeMappedParameter, modifiers: TypeMappedModifiers, value: LocalNodeId | undefined): TypeExpression {
        return { kind: "mapped", parameter, modifiers, value };
    },

    /** Index access type. */
    index(left: LocalNodeId, index: LocalNodeId): TypeExpression {
        return { kind: "index", left, index };
    },

    /** Template literal type. */
    templateLiteral(template_literal: TypeTemplateLiteral): TypeExpression {
        return { kind: "templateLiteral", template_literal };
    },

    /** Import type. */
    "import"(target: StringId, qualifier: Path | undefined, genericArguments: ReadonlyArray<LocalNodeId>): TypeExpression {
        return { kind: "import", target, qualifier, genericArguments };
    },

    /** Infer type binding. */
    "infer"(name: StringId, constraint: LocalNodeId | undefined): TypeExpression {
        return { kind: "infer", name, constraint };
    },

    /** Array type `T[]`. */
    array(element: LocalNodeId): TypeExpression {
        return { kind: "array", element };
    },

    /** Tuple type `[T1, T2, ...]`. */
    tuple(elements: ReadonlyArray<LocalNodeId>): TypeExpression {
        return { kind: "tuple", elements };
    },

    /** Object type `{ a: T1, b: T2, ... }`. */
    "object"(members: ReadonlyArray<LocalNodeId>): TypeExpression {
        return { kind: "object", members };
    },

    /** Union type `A | B | C`. */
    union(elements: ReadonlyArray<LocalNodeId>): TypeExpression {
        return { kind: "union", elements };
    },

    /** Intersection type `A & B & C`. */
    intersection(elements: ReadonlyArray<LocalNodeId>): TypeExpression {
        return { kind: "intersection", elements };
    },

    /** Function type declaration. */
    functionTypeDeclaration(function_type_declaration: FunctionTypeDeclaration): TypeExpression {
        return { kind: "functionTypeDeclaration", function_type_declaration };
    },

    /** Constructor type declaration. */
    constructorTypeDeclaration(constructor_type_declaration: ConstructorTypeDeclaration): TypeExpression {
        return { kind: "constructorTypeDeclaration", constructor_type_declaration };
    },

    /** Error type that could not be resolved. */
    error(): TypeExpression {
        return { kind: "error" };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: TypeExpression): void {
        encodeTypeExpression(writer, value);
    },

    /** Decode one TypeExpression. */
    decode(reader: BinaryReader): TypeExpression {
        return decodeTypeExpression(reader);
    },

    /** Return this value as JSON. */
    toJson(value: TypeExpression): Json {
        return toJsonTypeExpression(value);
    },

    /** Return one TypeExpression from one JSON value. */
    fromJson(value: Json): TypeExpression {
        return fromJsonTypeExpression(value);
    },
};

/** Encode one TypeExpression. */
export function encodeTypeExpression(writer: BinaryWriter, value: TypeExpression): void {
    switch (value.kind) {
        case "scalar":
            writer.writeUnsigned(0);
            encodeTypeLiteral(writer, value.scalar);
            return;
        case "this":
            writer.writeUnsigned(1);
            return;
        case "path":
            writer.writeUnsigned(2);
            encodePath(writer, value.path);
            writer.writeUnsigned(value.genericArguments.length);
            for (const item1 of value.genericArguments) {
                encodeLocalNodeId(writer, item1);
            }
            return;
        case "readonly":
            writer.writeUnsigned(3);
            encodeLocalNodeId(writer, value.targetType);
            return;
        case "keyOf":
            writer.writeUnsigned(4);
            encodeLocalNodeId(writer, value.targetType);
            return;
        case "must":
            writer.writeUnsigned(5);
            encodeLocalNodeId(writer, value.targetType);
            return;
        case "not":
            writer.writeUnsigned(6);
            encodeLocalNodeId(writer, value.targetType);
            return;
        case "extends":
            writer.writeUnsigned(7);
            encodeLocalNodeId(writer, value.left);
            encodeLocalNodeId(writer, value.right);
            return;
        case "implements":
            writer.writeUnsigned(8);
            encodeLocalNodeId(writer, value.left);
            encodeLocalNodeId(writer, value.right);
            return;
        case "conditional":
            writer.writeUnsigned(9);
            encodeLocalNodeId(writer, value.left);
            encodeLocalNodeId(writer, value.right);
            encodeLocalNodeId(writer, value.thenType);
            encodeLocalNodeId(writer, value.elseType);
            return;
        case "mapped":
            writer.writeUnsigned(10);
            encodeTypeMappedParameter(writer, value.parameter);
            encodeTypeMappedModifiers(writer, value.modifiers);
            writer.writeOption(value.value, (value2) => {
                encodeLocalNodeId(writer, value2);
            });
            return;
        case "index":
            writer.writeUnsigned(11);
            encodeLocalNodeId(writer, value.left);
            encodeLocalNodeId(writer, value.index);
            return;
        case "templateLiteral":
            writer.writeUnsigned(12);
            encodeTypeTemplateLiteral(writer, value.template_literal);
            return;
        case "import":
            writer.writeUnsigned(13);
            encodeStringId(writer, value.target);
            writer.writeOption(value.qualifier, (value1) => {
                encodePath(writer, value1);
            });
            writer.writeUnsigned(value.genericArguments.length);
            for (const item2 of value.genericArguments) {
                encodeLocalNodeId(writer, item2);
            }
            return;
        case "infer":
            writer.writeUnsigned(14);
            encodeStringId(writer, value.name);
            writer.writeOption(value.constraint, (value1) => {
                encodeLocalNodeId(writer, value1);
            });
            return;
        case "array":
            writer.writeUnsigned(15);
            encodeLocalNodeId(writer, value.element);
            return;
        case "tuple":
            writer.writeUnsigned(16);
            writer.writeUnsigned(value.elements.length);
            for (const item0 of value.elements) {
                encodeLocalNodeId(writer, item0);
            }
            return;
        case "object":
            writer.writeUnsigned(17);
            writer.writeUnsigned(value.members.length);
            for (const item0 of value.members) {
                encodeLocalNodeId(writer, item0);
            }
            return;
        case "union":
            writer.writeUnsigned(18);
            writer.writeUnsigned(value.elements.length);
            for (const item0 of value.elements) {
                encodeLocalNodeId(writer, item0);
            }
            return;
        case "intersection":
            writer.writeUnsigned(19);
            writer.writeUnsigned(value.elements.length);
            for (const item0 of value.elements) {
                encodeLocalNodeId(writer, item0);
            }
            return;
        case "functionTypeDeclaration":
            writer.writeUnsigned(20);
            encodeFunctionTypeDeclaration(writer, value.function_type_declaration);
            return;
        case "constructorTypeDeclaration":
            writer.writeUnsigned(21);
            encodeConstructorTypeDeclaration(writer, value.constructor_type_declaration);
            return;
        case "error":
            writer.writeUnsigned(22);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one TypeExpression. */
export function decodeTypeExpression(reader: BinaryReader): TypeExpression {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const scalar = decodeTypeLiteral(reader);

            return { kind: "scalar", scalar };
        }
        case 1: {
            return { kind: "this" };
        }
        case 2: {
            const path = decodePath(reader);
            const genericArguments = (() => { const length1 = reader.readNumber(); const items1: Array<LocalNodeId> = []; for (let index = 0; index < length1; index += 1) { items1.push(decodeLocalNodeId(reader)); } return items1; })();

            return {
                kind: "path",
                path,
                genericArguments,
            };
        }
        case 3: {
            const targetType = decodeLocalNodeId(reader);

            return {
                kind: "readonly",
                targetType,
            };
        }
        case 4: {
            const targetType = decodeLocalNodeId(reader);

            return {
                kind: "keyOf",
                targetType,
            };
        }
        case 5: {
            const targetType = decodeLocalNodeId(reader);

            return {
                kind: "must",
                targetType,
            };
        }
        case 6: {
            const targetType = decodeLocalNodeId(reader);

            return {
                kind: "not",
                targetType,
            };
        }
        case 7: {
            const left = decodeLocalNodeId(reader);
            const right = decodeLocalNodeId(reader);

            return {
                kind: "extends",
                left,
                right,
            };
        }
        case 8: {
            const left = decodeLocalNodeId(reader);
            const right = decodeLocalNodeId(reader);

            return {
                kind: "implements",
                left,
                right,
            };
        }
        case 9: {
            const left = decodeLocalNodeId(reader);
            const right = decodeLocalNodeId(reader);
            const thenType = decodeLocalNodeId(reader);
            const elseType = decodeLocalNodeId(reader);

            return {
                kind: "conditional",
                left,
                right,
                thenType,
                elseType,
            };
        }
        case 10: {
            const parameter = decodeTypeMappedParameter(reader);
            const modifiers = decodeTypeMappedModifiers(reader);
            const value = reader.readOption(() => decodeLocalNodeId(reader));

            return {
                kind: "mapped",
                parameter,
                modifiers,
                ...(value === undefined ? {} : { value }),
            };
        }
        case 11: {
            const left = decodeLocalNodeId(reader);
            const index = decodeLocalNodeId(reader);

            return {
                kind: "index",
                left,
                index,
            };
        }
        case 12: {
            const template_literal = decodeTypeTemplateLiteral(reader);

            return { kind: "templateLiteral", template_literal };
        }
        case 13: {
            const target = decodeStringId(reader);
            const qualifier = reader.readOption(() => decodePath(reader));
            const genericArguments = (() => { const length2 = reader.readNumber(); const items2: Array<LocalNodeId> = []; for (let index = 0; index < length2; index += 1) { items2.push(decodeLocalNodeId(reader)); } return items2; })();

            return {
                kind: "import",
                target,
                ...(qualifier === undefined ? {} : { qualifier }),
                genericArguments,
            };
        }
        case 14: {
            const name = decodeStringId(reader);
            const constraint = reader.readOption(() => decodeLocalNodeId(reader));

            return {
                kind: "infer",
                name,
                ...(constraint === undefined ? {} : { constraint }),
            };
        }
        case 15: {
            const element = decodeLocalNodeId(reader);

            return {
                kind: "array",
                element,
            };
        }
        case 16: {
            const elements = (() => { const length0 = reader.readNumber(); const items0: Array<LocalNodeId> = []; for (let index = 0; index < length0; index += 1) { items0.push(decodeLocalNodeId(reader)); } return items0; })();

            return {
                kind: "tuple",
                elements,
            };
        }
        case 17: {
            const members = (() => { const length0 = reader.readNumber(); const items0: Array<LocalNodeId> = []; for (let index = 0; index < length0; index += 1) { items0.push(decodeLocalNodeId(reader)); } return items0; })();

            return {
                kind: "object",
                members,
            };
        }
        case 18: {
            const elements = (() => { const length0 = reader.readNumber(); const items0: Array<LocalNodeId> = []; for (let index = 0; index < length0; index += 1) { items0.push(decodeLocalNodeId(reader)); } return items0; })();

            return {
                kind: "union",
                elements,
            };
        }
        case 19: {
            const elements = (() => { const length0 = reader.readNumber(); const items0: Array<LocalNodeId> = []; for (let index = 0; index < length0; index += 1) { items0.push(decodeLocalNodeId(reader)); } return items0; })();

            return {
                kind: "intersection",
                elements,
            };
        }
        case 20: {
            const function_type_declaration = decodeFunctionTypeDeclaration(reader);

            return { kind: "functionTypeDeclaration", function_type_declaration };
        }
        case 21: {
            const constructor_type_declaration = decodeConstructorTypeDeclaration(reader);

            return { kind: "constructorTypeDeclaration", constructor_type_declaration };
        }
        case 22: {
            return { kind: "error" };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one TypeExpression. */
export function toJsonTypeExpression(value: TypeExpression): Json {
    switch (value.kind) {
        case "scalar":
            return {
                kind: "scalar",
                scalar: toJsonTypeLiteral(value.scalar),
            };
        case "this":
            return {
                kind: "this",
            };
        case "path":
            return {
                kind: "path",
                path: toJsonPath(value.path),
                genericArguments: value.genericArguments.map((item0) => toJsonLocalNodeId(item0)),
            };
        case "readonly":
            return {
                kind: "readonly",
                targetType: toJsonLocalNodeId(value.targetType),
            };
        case "keyOf":
            return {
                kind: "keyOf",
                targetType: toJsonLocalNodeId(value.targetType),
            };
        case "must":
            return {
                kind: "must",
                targetType: toJsonLocalNodeId(value.targetType),
            };
        case "not":
            return {
                kind: "not",
                targetType: toJsonLocalNodeId(value.targetType),
            };
        case "extends":
            return {
                kind: "extends",
                left: toJsonLocalNodeId(value.left),
                right: toJsonLocalNodeId(value.right),
            };
        case "implements":
            return {
                kind: "implements",
                left: toJsonLocalNodeId(value.left),
                right: toJsonLocalNodeId(value.right),
            };
        case "conditional":
            return {
                kind: "conditional",
                left: toJsonLocalNodeId(value.left),
                right: toJsonLocalNodeId(value.right),
                thenType: toJsonLocalNodeId(value.thenType),
                elseType: toJsonLocalNodeId(value.elseType),
            };
        case "mapped":
            return {
                kind: "mapped",
                parameter: toJsonTypeMappedParameter(value.parameter),
                modifiers: toJsonTypeMappedModifiers(value.modifiers),
                ...(value.value === undefined ? {} : { value: toJsonLocalNodeId(value.value) }),
            };
        case "index":
            return {
                kind: "index",
                left: toJsonLocalNodeId(value.left),
                index: toJsonLocalNodeId(value.index),
            };
        case "templateLiteral":
            return {
                kind: "templateLiteral",
                template_literal: toJsonTypeTemplateLiteral(value.template_literal),
            };
        case "import":
            return {
                kind: "import",
                target: toJsonStringId(value.target),
                ...(value.qualifier === undefined ? {} : { qualifier: toJsonPath(value.qualifier) }),
                genericArguments: value.genericArguments.map((item0) => toJsonLocalNodeId(item0)),
            };
        case "infer":
            return {
                kind: "infer",
                name: toJsonStringId(value.name),
                ...(value.constraint === undefined ? {} : { constraint: toJsonLocalNodeId(value.constraint) }),
            };
        case "array":
            return {
                kind: "array",
                element: toJsonLocalNodeId(value.element),
            };
        case "tuple":
            return {
                kind: "tuple",
                elements: value.elements.map((item0) => toJsonLocalNodeId(item0)),
            };
        case "object":
            return {
                kind: "object",
                members: value.members.map((item0) => toJsonLocalNodeId(item0)),
            };
        case "union":
            return {
                kind: "union",
                elements: value.elements.map((item0) => toJsonLocalNodeId(item0)),
            };
        case "intersection":
            return {
                kind: "intersection",
                elements: value.elements.map((item0) => toJsonLocalNodeId(item0)),
            };
        case "functionTypeDeclaration":
            return {
                kind: "functionTypeDeclaration",
                function_type_declaration: toJsonFunctionTypeDeclaration(value.function_type_declaration),
            };
        case "constructorTypeDeclaration":
            return {
                kind: "constructorTypeDeclaration",
                constructor_type_declaration: toJsonConstructorTypeDeclaration(value.constructor_type_declaration),
            };
        case "error":
            return {
                kind: "error",
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one TypeExpression from one JSON value. */
export function fromJsonTypeExpression(value: Json): TypeExpression {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "scalar":
            return {
                kind,
                scalar: fromJsonTypeLiteral(jsonField(object, "scalar")),
            };
        case "this":
            return {
                kind,
            };
        case "path":
            return {
                kind,
                path: fromJsonPath(jsonField(object, "path")),
                genericArguments: jsonArray(jsonField(object, "genericArguments")).map((item0) => fromJsonLocalNodeId(item0)),
            };
        case "readonly":
            return {
                kind,
                targetType: fromJsonLocalNodeId(jsonField(object, "targetType")),
            };
        case "keyOf":
            return {
                kind,
                targetType: fromJsonLocalNodeId(jsonField(object, "targetType")),
            };
        case "must":
            return {
                kind,
                targetType: fromJsonLocalNodeId(jsonField(object, "targetType")),
            };
        case "not":
            return {
                kind,
                targetType: fromJsonLocalNodeId(jsonField(object, "targetType")),
            };
        case "extends":
            return {
                kind,
                left: fromJsonLocalNodeId(jsonField(object, "left")),
                right: fromJsonLocalNodeId(jsonField(object, "right")),
            };
        case "implements":
            return {
                kind,
                left: fromJsonLocalNodeId(jsonField(object, "left")),
                right: fromJsonLocalNodeId(jsonField(object, "right")),
            };
        case "conditional":
            return {
                kind,
                left: fromJsonLocalNodeId(jsonField(object, "left")),
                right: fromJsonLocalNodeId(jsonField(object, "right")),
                thenType: fromJsonLocalNodeId(jsonField(object, "thenType")),
                elseType: fromJsonLocalNodeId(jsonField(object, "elseType")),
            };
        case "mapped":
            return {
                kind,
                parameter: fromJsonTypeMappedParameter(jsonField(object, "parameter")),
                modifiers: fromJsonTypeMappedModifiers(jsonField(object, "modifiers")),
                value: jsonOptional(object, "value", (value) => fromJsonLocalNodeId(value)),
            };
        case "index":
            return {
                kind,
                left: fromJsonLocalNodeId(jsonField(object, "left")),
                index: fromJsonLocalNodeId(jsonField(object, "index")),
            };
        case "templateLiteral":
            return {
                kind,
                template_literal: fromJsonTypeTemplateLiteral(jsonField(object, "template_literal")),
            };
        case "import":
            return {
                kind,
                target: fromJsonStringId(jsonField(object, "target")),
                qualifier: jsonOptional(object, "qualifier", (value) => fromJsonPath(value)),
                genericArguments: jsonArray(jsonField(object, "genericArguments")).map((item0) => fromJsonLocalNodeId(item0)),
            };
        case "infer":
            return {
                kind,
                name: fromJsonStringId(jsonField(object, "name")),
                constraint: jsonOptional(object, "constraint", (value) => fromJsonLocalNodeId(value)),
            };
        case "array":
            return {
                kind,
                element: fromJsonLocalNodeId(jsonField(object, "element")),
            };
        case "tuple":
            return {
                kind,
                elements: jsonArray(jsonField(object, "elements")).map((item0) => fromJsonLocalNodeId(item0)),
            };
        case "object":
            return {
                kind,
                members: jsonArray(jsonField(object, "members")).map((item0) => fromJsonLocalNodeId(item0)),
            };
        case "union":
            return {
                kind,
                elements: jsonArray(jsonField(object, "elements")).map((item0) => fromJsonLocalNodeId(item0)),
            };
        case "intersection":
            return {
                kind,
                elements: jsonArray(jsonField(object, "elements")).map((item0) => fromJsonLocalNodeId(item0)),
            };
        case "functionTypeDeclaration":
            return {
                kind,
                function_type_declaration: fromJsonFunctionTypeDeclaration(jsonField(object, "function_type_declaration")),
            };
        case "constructorTypeDeclaration":
            return {
                kind,
                constructor_type_declaration: fromJsonConstructorTypeDeclaration(jsonField(object, "constructor_type_declaration")),
            };
        case "error":
            return {
                kind,
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** A TypeLiteral is a scalar type. */
export type TypeLiteral =
    /** Never type `never`. */
    | {
          readonly kind: "never";
      }
    /** Any type `any`. */
    | {
          readonly kind: "any";
      }
    /** Undefined type and value. */
    | {
          readonly kind: "undefined";
      }
    /** Unknown type. */
    | {
          readonly kind: "unknown";
      }
    /** Object type (any non-primitive). */
    | {
          readonly kind: "object";
      }
    /** Void type. */
    | {
          readonly kind: "void";
      }
    /** Null type and value. */
    | {
          readonly kind: "null";
      }
    /** Primitive type. */
    | {
          readonly kind: "primitive";
          readonly primitive: PrimitiveType;
      }
    /** Scalar literal. */
    | {
          readonly kind: "scalarLiteral";
          readonly scalar_literal: ScalarLiteral;
      }
;

export const TypeLiteral = {
    /** Never type `never`. */
    "never"(): TypeLiteral {
        return { kind: "never" };
    },

    /** Any type `any`. */
    "any"(): TypeLiteral {
        return { kind: "any" };
    },

    /** Undefined type and value. */
    "undefined"(): TypeLiteral {
        return { kind: "undefined" };
    },

    /** Unknown type. */
    "unknown"(): TypeLiteral {
        return { kind: "unknown" };
    },

    /** Object type (any non-primitive). */
    "object"(): TypeLiteral {
        return { kind: "object" };
    },

    /** Void type. */
    "void"(): TypeLiteral {
        return { kind: "void" };
    },

    /** Null type and value. */
    "null"(): TypeLiteral {
        return { kind: "null" };
    },

    /** Primitive type. */
    primitive(primitive: PrimitiveType): TypeLiteral {
        return { kind: "primitive", primitive };
    },

    /** Scalar literal. */
    scalarLiteral(scalar_literal: ScalarLiteral): TypeLiteral {
        return { kind: "scalarLiteral", scalar_literal };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: TypeLiteral): void {
        encodeTypeLiteral(writer, value);
    },

    /** Decode one TypeLiteral. */
    decode(reader: BinaryReader): TypeLiteral {
        return decodeTypeLiteral(reader);
    },

    /** Return this value as JSON. */
    toJson(value: TypeLiteral): Json {
        return toJsonTypeLiteral(value);
    },

    /** Return one TypeLiteral from one JSON value. */
    fromJson(value: Json): TypeLiteral {
        return fromJsonTypeLiteral(value);
    },
};

/** Encode one TypeLiteral. */
export function encodeTypeLiteral(writer: BinaryWriter, value: TypeLiteral): void {
    switch (value.kind) {
        case "never":
            writer.writeUnsigned(0);
            return;
        case "any":
            writer.writeUnsigned(1);
            return;
        case "undefined":
            writer.writeUnsigned(2);
            return;
        case "unknown":
            writer.writeUnsigned(3);
            return;
        case "object":
            writer.writeUnsigned(4);
            return;
        case "void":
            writer.writeUnsigned(5);
            return;
        case "null":
            writer.writeUnsigned(6);
            return;
        case "primitive":
            writer.writeUnsigned(7);
            encodePrimitiveType(writer, value.primitive);
            return;
        case "scalarLiteral":
            writer.writeUnsigned(8);
            encodeScalarLiteral(writer, value.scalar_literal);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one TypeLiteral. */
export function decodeTypeLiteral(reader: BinaryReader): TypeLiteral {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            return { kind: "never" };
        }
        case 1: {
            return { kind: "any" };
        }
        case 2: {
            return { kind: "undefined" };
        }
        case 3: {
            return { kind: "unknown" };
        }
        case 4: {
            return { kind: "object" };
        }
        case 5: {
            return { kind: "void" };
        }
        case 6: {
            return { kind: "null" };
        }
        case 7: {
            const primitive = decodePrimitiveType(reader);

            return { kind: "primitive", primitive };
        }
        case 8: {
            const scalar_literal = decodeScalarLiteral(reader);

            return { kind: "scalarLiteral", scalar_literal };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one TypeLiteral. */
export function toJsonTypeLiteral(value: TypeLiteral): Json {
    switch (value.kind) {
        case "never":
            return {
                kind: "never",
            };
        case "any":
            return {
                kind: "any",
            };
        case "undefined":
            return {
                kind: "undefined",
            };
        case "unknown":
            return {
                kind: "unknown",
            };
        case "object":
            return {
                kind: "object",
            };
        case "void":
            return {
                kind: "void",
            };
        case "null":
            return {
                kind: "null",
            };
        case "primitive":
            return {
                kind: "primitive",
                primitive: toJsonPrimitiveType(value.primitive),
            };
        case "scalarLiteral":
            return {
                kind: "scalarLiteral",
                scalar_literal: toJsonScalarLiteral(value.scalar_literal),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one TypeLiteral from one JSON value. */
export function fromJsonTypeLiteral(value: Json): TypeLiteral {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "never":
            return {
                kind,
            };
        case "any":
            return {
                kind,
            };
        case "undefined":
            return {
                kind,
            };
        case "unknown":
            return {
                kind,
            };
        case "object":
            return {
                kind,
            };
        case "void":
            return {
                kind,
            };
        case "null":
            return {
                kind,
            };
        case "primitive":
            return {
                kind,
                primitive: fromJsonPrimitiveType(jsonField(object, "primitive")),
            };
        case "scalarLiteral":
            return {
                kind,
                scalar_literal: fromJsonScalarLiteral(jsonField(object, "scalar_literal")),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** A PrimitiveType is a primitive type node. */
export type PrimitiveType = "boolean" | "string" | "bigint" | "number" | "symbol" | "uniqueSymbol";

export const PrimitiveType = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: PrimitiveType): void {
        encodePrimitiveType(writer, value);
    },

    /** Decode one PrimitiveType. */
    decode(reader: BinaryReader): PrimitiveType {
        return decodePrimitiveType(reader);
    },

    /** Return this value as JSON. */
    toJson(value: PrimitiveType): Json {
        return toJsonPrimitiveType(value);
    },

    /** Return one PrimitiveType from one JSON value. */
    fromJson(value: Json): PrimitiveType {
        return fromJsonPrimitiveType(value);
    },
};

/** Encode one PrimitiveType. */
export function encodePrimitiveType(writer: BinaryWriter, value: PrimitiveType): void {
    switch (value) {
        case "boolean":
            writer.writeUnsigned(0);
            return;
        case "string":
            writer.writeUnsigned(1);
            return;
        case "bigint":
            writer.writeUnsigned(2);
            return;
        case "number":
            writer.writeUnsigned(3);
            return;
        case "symbol":
            writer.writeUnsigned(4);
            return;
        case "uniqueSymbol":
            writer.writeUnsigned(5);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one PrimitiveType. */
export function decodePrimitiveType(reader: BinaryReader): PrimitiveType {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "boolean";
        case 1:
            return "string";
        case 2:
            return "bigint";
        case 3:
            return "number";
        case 4:
            return "symbol";
        case 5:
            return "uniqueSymbol";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one PrimitiveType. */
export function toJsonPrimitiveType(value: PrimitiveType): Json {
    return value;
}

/** Return one PrimitiveType from one JSON value. */
export function fromJsonPrimitiveType(value: Json): PrimitiveType {
    const variant = jsonString(value);

    switch (variant) {
        case "boolean":
            return "boolean";
        case "string":
            return "string";
        case "bigint":
            return "bigint";
        case "number":
            return "number";
        case "symbol":
            return "symbol";
        case "uniqueSymbol":
            return "uniqueSymbol";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** One mapped type parameter. */
export type TypeMappedParameter = {
    /** The parameter name. */
    readonly name: StringId;
    /** The source type iterated by `in`. */
    readonly sourceType: LocalNodeId;
    /** The optional key remap. */
    readonly keyRemap?: LocalNodeId;
};

export const TypeMappedParameter = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: TypeMappedParameter): void {
        encodeTypeMappedParameter(writer, value);
    },

    /** Decode one TypeMappedParameter. */
    decode(reader: BinaryReader): TypeMappedParameter {
        return decodeTypeMappedParameter(reader);
    },

    /** Return this value as JSON. */
    toJson(value: TypeMappedParameter): Json {
        return toJsonTypeMappedParameter(value);
    },

    /** Return one TypeMappedParameter from one JSON value. */
    fromJson(value: Json): TypeMappedParameter {
        return fromJsonTypeMappedParameter(value);
    },
};

/** Encode one TypeMappedParameter. */
export function encodeTypeMappedParameter(writer: BinaryWriter, value: TypeMappedParameter): void {
    encodeStringId(writer, value.name);
    encodeLocalNodeId(writer, value.sourceType);
    writer.writeOption(value.keyRemap, (value2) => {
        encodeLocalNodeId(writer, value2);
    });
}

/** Decode one TypeMappedParameter. */
export function decodeTypeMappedParameter(reader: BinaryReader): TypeMappedParameter {
    const name = decodeStringId(reader);
    const sourceType = decodeLocalNodeId(reader);
    const keyRemap = reader.readOption(() => decodeLocalNodeId(reader));

    return {
        name,
        sourceType,
        ...(keyRemap === undefined ? {} : { keyRemap }),
    };
}

/** Return one JSON value for one TypeMappedParameter. */
export function toJsonTypeMappedParameter(value: TypeMappedParameter): Json {
    return {
        name: toJsonStringId(value.name),
        sourceType: toJsonLocalNodeId(value.sourceType),
        ...(value.keyRemap === undefined ? {} : { keyRemap: toJsonLocalNodeId(value.keyRemap) }),
    };
}

/** Return one TypeMappedParameter from one JSON value. */
export function fromJsonTypeMappedParameter(value: Json): TypeMappedParameter {
    const object = jsonObject(value);

    return {
        name: fromJsonStringId(jsonField(object, "name")),
        sourceType: fromJsonLocalNodeId(jsonField(object, "sourceType")),
        keyRemap: jsonOptional(object, "keyRemap", (value) => fromJsonLocalNodeId(value)),
    };
}

/** One mapped type modifier set. */
export type TypeMappedModifiers = {
    /** The readonly modifier. */
    readonly readonly: MappedTypeModifier;
    /** The optional modifier. */
    readonly optional: MappedTypeModifier;
};

export const TypeMappedModifiers = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: TypeMappedModifiers): void {
        encodeTypeMappedModifiers(writer, value);
    },

    /** Decode one TypeMappedModifiers. */
    decode(reader: BinaryReader): TypeMappedModifiers {
        return decodeTypeMappedModifiers(reader);
    },

    /** Return this value as JSON. */
    toJson(value: TypeMappedModifiers): Json {
        return toJsonTypeMappedModifiers(value);
    },

    /** Return one TypeMappedModifiers from one JSON value. */
    fromJson(value: Json): TypeMappedModifiers {
        return fromJsonTypeMappedModifiers(value);
    },
};

/** Encode one TypeMappedModifiers. */
export function encodeTypeMappedModifiers(writer: BinaryWriter, value: TypeMappedModifiers): void {
    encodeMappedTypeModifier(writer, value.readonly);
    encodeMappedTypeModifier(writer, value.optional);
}

/** Decode one TypeMappedModifiers. */
export function decodeTypeMappedModifiers(reader: BinaryReader): TypeMappedModifiers {
    const readonly_ = decodeMappedTypeModifier(reader);
    const optional = decodeMappedTypeModifier(reader);

    return {
        readonly: readonly_,
        optional,
    };
}

/** Return one JSON value for one TypeMappedModifiers. */
export function toJsonTypeMappedModifiers(value: TypeMappedModifiers): Json {
    return {
        readonly: toJsonMappedTypeModifier(value.readonly),
        optional: toJsonMappedTypeModifier(value.optional),
    };
}

/** Return one TypeMappedModifiers from one JSON value. */
export function fromJsonTypeMappedModifiers(value: Json): TypeMappedModifiers {
    const object = jsonObject(value);

    return {
        readonly: fromJsonMappedTypeModifier(jsonField(object, "readonly")),
        optional: fromJsonMappedTypeModifier(jsonField(object, "optional")),
    };
}

/** One mapped type modifier. */
export type MappedTypeModifier = "present" | "add" | "remove" | "none";

export const MappedTypeModifier = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: MappedTypeModifier): void {
        encodeMappedTypeModifier(writer, value);
    },

    /** Decode one MappedTypeModifier. */
    decode(reader: BinaryReader): MappedTypeModifier {
        return decodeMappedTypeModifier(reader);
    },

    /** Return this value as JSON. */
    toJson(value: MappedTypeModifier): Json {
        return toJsonMappedTypeModifier(value);
    },

    /** Return one MappedTypeModifier from one JSON value. */
    fromJson(value: Json): MappedTypeModifier {
        return fromJsonMappedTypeModifier(value);
    },
};

/** Encode one MappedTypeModifier. */
export function encodeMappedTypeModifier(writer: BinaryWriter, value: MappedTypeModifier): void {
    switch (value) {
        case "present":
            writer.writeUnsigned(0);
            return;
        case "add":
            writer.writeUnsigned(1);
            return;
        case "remove":
            writer.writeUnsigned(2);
            return;
        case "none":
            writer.writeUnsigned(3);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one MappedTypeModifier. */
export function decodeMappedTypeModifier(reader: BinaryReader): MappedTypeModifier {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "present";
        case 1:
            return "add";
        case 2:
            return "remove";
        case 3:
            return "none";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one MappedTypeModifier. */
export function toJsonMappedTypeModifier(value: MappedTypeModifier): Json {
    return value;
}

/** Return one MappedTypeModifier from one JSON value. */
export function fromJsonMappedTypeModifier(value: Json): MappedTypeModifier {
    const variant = jsonString(value);

    switch (variant) {
        case "present":
            return "present";
        case "add":
            return "add";
        case "remove":
            return "remove";
        case "none":
            return "none";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** One type template literal. */
export type TypeTemplateLiteral = {
    /** The raw template strings. */
    readonly strings: ReadonlyArray<StringId>;
    /** The interpolated type spans. */
    readonly spans: ReadonlyArray<LocalNodeId>;
};

export const TypeTemplateLiteral = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: TypeTemplateLiteral): void {
        encodeTypeTemplateLiteral(writer, value);
    },

    /** Decode one TypeTemplateLiteral. */
    decode(reader: BinaryReader): TypeTemplateLiteral {
        return decodeTypeTemplateLiteral(reader);
    },

    /** Return this value as JSON. */
    toJson(value: TypeTemplateLiteral): Json {
        return toJsonTypeTemplateLiteral(value);
    },

    /** Return one TypeTemplateLiteral from one JSON value. */
    fromJson(value: Json): TypeTemplateLiteral {
        return fromJsonTypeTemplateLiteral(value);
    },
};

/** Encode one TypeTemplateLiteral. */
export function encodeTypeTemplateLiteral(writer: BinaryWriter, value: TypeTemplateLiteral): void {
    writer.writeUnsigned(value.strings.length);
    for (const item0 of value.strings) {
        encodeStringId(writer, item0);
    }
    writer.writeUnsigned(value.spans.length);
    for (const item1 of value.spans) {
        encodeLocalNodeId(writer, item1);
    }
}

/** Decode one TypeTemplateLiteral. */
export function decodeTypeTemplateLiteral(reader: BinaryReader): TypeTemplateLiteral {
    const strings = (() => { const length0 = reader.readNumber(); const items0: Array<StringId> = []; for (let index = 0; index < length0; index += 1) { items0.push(decodeStringId(reader)); } return items0; })();
    const spans = (() => { const length1 = reader.readNumber(); const items1: Array<LocalNodeId> = []; for (let index = 0; index < length1; index += 1) { items1.push(decodeLocalNodeId(reader)); } return items1; })();

    return {
        strings,
        spans,
    };
}

/** Return one JSON value for one TypeTemplateLiteral. */
export function toJsonTypeTemplateLiteral(value: TypeTemplateLiteral): Json {
    return {
        strings: value.strings.map((item0) => toJsonStringId(item0)),
        spans: value.spans.map((item0) => toJsonLocalNodeId(item0)),
    };
}

/** Return one TypeTemplateLiteral from one JSON value. */
export function fromJsonTypeTemplateLiteral(value: Json): TypeTemplateLiteral {
    const object = jsonObject(value);

    return {
        strings: jsonArray(jsonField(object, "strings")).map((item0) => fromJsonStringId(item0)),
        spans: jsonArray(jsonField(object, "spans")).map((item0) => fromJsonLocalNodeId(item0)),
    };
}

/** One function type declaration in type space. */
export type FunctionTypeDeclaration = {
    /** The generic parameters of the function type. */
    readonly genericParameters: ReadonlyArray<LocalNodeId>;
    /** The optional `this` parameter. */
    readonly thisParameter?: LocalNodeId;
    /** The parameters of the function type. */
    readonly parameters: ReadonlyArray<LocalNodeId>;
    /** The return type of the function type. */
    readonly returnType?: LocalNodeId;
};

export const FunctionTypeDeclaration = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: FunctionTypeDeclaration): void {
        encodeFunctionTypeDeclaration(writer, value);
    },

    /** Decode one FunctionTypeDeclaration. */
    decode(reader: BinaryReader): FunctionTypeDeclaration {
        return decodeFunctionTypeDeclaration(reader);
    },

    /** Return this value as JSON. */
    toJson(value: FunctionTypeDeclaration): Json {
        return toJsonFunctionTypeDeclaration(value);
    },

    /** Return one FunctionTypeDeclaration from one JSON value. */
    fromJson(value: Json): FunctionTypeDeclaration {
        return fromJsonFunctionTypeDeclaration(value);
    },
};

/** Encode one FunctionTypeDeclaration. */
export function encodeFunctionTypeDeclaration(writer: BinaryWriter, value: FunctionTypeDeclaration): void {
    writer.writeUnsigned(value.genericParameters.length);
    for (const item0 of value.genericParameters) {
        encodeLocalNodeId(writer, item0);
    }
    writer.writeOption(value.thisParameter, (value1) => {
        encodeLocalNodeId(writer, value1);
    });
    writer.writeUnsigned(value.parameters.length);
    for (const item2 of value.parameters) {
        encodeLocalNodeId(writer, item2);
    }
    writer.writeOption(value.returnType, (value3) => {
        encodeLocalNodeId(writer, value3);
    });
}

/** Decode one FunctionTypeDeclaration. */
export function decodeFunctionTypeDeclaration(reader: BinaryReader): FunctionTypeDeclaration {
    const genericParameters = (() => { const length0 = reader.readNumber(); const items0: Array<LocalNodeId> = []; for (let index = 0; index < length0; index += 1) { items0.push(decodeLocalNodeId(reader)); } return items0; })();
    const thisParameter = reader.readOption(() => decodeLocalNodeId(reader));
    const parameters = (() => { const length2 = reader.readNumber(); const items2: Array<LocalNodeId> = []; for (let index = 0; index < length2; index += 1) { items2.push(decodeLocalNodeId(reader)); } return items2; })();
    const returnType = reader.readOption(() => decodeLocalNodeId(reader));

    return {
        genericParameters,
        ...(thisParameter === undefined ? {} : { thisParameter }),
        parameters,
        ...(returnType === undefined ? {} : { returnType }),
    };
}

/** Return one JSON value for one FunctionTypeDeclaration. */
export function toJsonFunctionTypeDeclaration(value: FunctionTypeDeclaration): Json {
    return {
        genericParameters: value.genericParameters.map((item0) => toJsonLocalNodeId(item0)),
        ...(value.thisParameter === undefined ? {} : { thisParameter: toJsonLocalNodeId(value.thisParameter) }),
        parameters: value.parameters.map((item0) => toJsonLocalNodeId(item0)),
        ...(value.returnType === undefined ? {} : { returnType: toJsonLocalNodeId(value.returnType) }),
    };
}

/** Return one FunctionTypeDeclaration from one JSON value. */
export function fromJsonFunctionTypeDeclaration(value: Json): FunctionTypeDeclaration {
    const object = jsonObject(value);

    return {
        genericParameters: jsonArray(jsonField(object, "genericParameters")).map((item0) => fromJsonLocalNodeId(item0)),
        thisParameter: jsonOptional(object, "thisParameter", (value) => fromJsonLocalNodeId(value)),
        parameters: jsonArray(jsonField(object, "parameters")).map((item0) => fromJsonLocalNodeId(item0)),
        returnType: jsonOptional(object, "returnType", (value) => fromJsonLocalNodeId(value)),
    };
}

/** One constructor type declaration in type space. */
export type ConstructorTypeDeclaration = {
    /** Whether the constructor type is abstract. */
    readonly isAbstract: boolean;
    /** The generic parameters of the constructor type. */
    readonly genericParameters: ReadonlyArray<LocalNodeId>;
    /** The parameters of the constructor type. */
    readonly parameters: ReadonlyArray<LocalNodeId>;
    /** The return type of the constructor type. */
    readonly returnType?: LocalNodeId;
};

export const ConstructorTypeDeclaration = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ConstructorTypeDeclaration): void {
        encodeConstructorTypeDeclaration(writer, value);
    },

    /** Decode one ConstructorTypeDeclaration. */
    decode(reader: BinaryReader): ConstructorTypeDeclaration {
        return decodeConstructorTypeDeclaration(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ConstructorTypeDeclaration): Json {
        return toJsonConstructorTypeDeclaration(value);
    },

    /** Return one ConstructorTypeDeclaration from one JSON value. */
    fromJson(value: Json): ConstructorTypeDeclaration {
        return fromJsonConstructorTypeDeclaration(value);
    },
};

/** Encode one ConstructorTypeDeclaration. */
export function encodeConstructorTypeDeclaration(writer: BinaryWriter, value: ConstructorTypeDeclaration): void {
    writer.writeBool(value.isAbstract);
    writer.writeUnsigned(value.genericParameters.length);
    for (const item1 of value.genericParameters) {
        encodeLocalNodeId(writer, item1);
    }
    writer.writeUnsigned(value.parameters.length);
    for (const item2 of value.parameters) {
        encodeLocalNodeId(writer, item2);
    }
    writer.writeOption(value.returnType, (value3) => {
        encodeLocalNodeId(writer, value3);
    });
}

/** Decode one ConstructorTypeDeclaration. */
export function decodeConstructorTypeDeclaration(reader: BinaryReader): ConstructorTypeDeclaration {
    const isAbstract = reader.readBool();
    const genericParameters = (() => { const length1 = reader.readNumber(); const items1: Array<LocalNodeId> = []; for (let index = 0; index < length1; index += 1) { items1.push(decodeLocalNodeId(reader)); } return items1; })();
    const parameters = (() => { const length2 = reader.readNumber(); const items2: Array<LocalNodeId> = []; for (let index = 0; index < length2; index += 1) { items2.push(decodeLocalNodeId(reader)); } return items2; })();
    const returnType = reader.readOption(() => decodeLocalNodeId(reader));

    return {
        isAbstract,
        genericParameters,
        parameters,
        ...(returnType === undefined ? {} : { returnType }),
    };
}

/** Return one JSON value for one ConstructorTypeDeclaration. */
export function toJsonConstructorTypeDeclaration(value: ConstructorTypeDeclaration): Json {
    return {
        isAbstract: value.isAbstract,
        genericParameters: value.genericParameters.map((item0) => toJsonLocalNodeId(item0)),
        parameters: value.parameters.map((item0) => toJsonLocalNodeId(item0)),
        ...(value.returnType === undefined ? {} : { returnType: toJsonLocalNodeId(value.returnType) }),
    };
}

/** Return one ConstructorTypeDeclaration from one JSON value. */
export function fromJsonConstructorTypeDeclaration(value: Json): ConstructorTypeDeclaration {
    const object = jsonObject(value);

    return {
        isAbstract: jsonBool(jsonField(object, "isAbstract")),
        genericParameters: jsonArray(jsonField(object, "genericParameters")).map((item0) => fromJsonLocalNodeId(item0)),
        parameters: jsonArray(jsonField(object, "parameters")).map((item0) => fromJsonLocalNodeId(item0)),
        returnType: jsonOptional(object, "returnType", (value) => fromJsonLocalNodeId(value)),
    };
}

/** One tuple type element. */
export type TupleElement = {
    /** The optional element label. */
    readonly label?: StringId;
    /** The element type. */
    readonly ty: LocalNodeId;
    /** Whether the element is optional. */
    readonly isOptional: boolean;
    /** Whether the element is readonly. */
    readonly isReadonly: boolean;
    /** Whether the element is a rest element. */
    readonly isRest: boolean;
};

export const TupleElement = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: TupleElement): void {
        encodeTupleElement(writer, value);
    },

    /** Decode one TupleElement. */
    decode(reader: BinaryReader): TupleElement {
        return decodeTupleElement(reader);
    },

    /** Return this value as JSON. */
    toJson(value: TupleElement): Json {
        return toJsonTupleElement(value);
    },

    /** Return one TupleElement from one JSON value. */
    fromJson(value: Json): TupleElement {
        return fromJsonTupleElement(value);
    },
};

/** Encode one TupleElement. */
export function encodeTupleElement(writer: BinaryWriter, value: TupleElement): void {
    writer.writeOption(value.label, (value0) => {
        encodeStringId(writer, value0);
    });
    encodeLocalNodeId(writer, value.ty);
    writer.writeBool(value.isOptional);
    writer.writeBool(value.isReadonly);
    writer.writeBool(value.isRest);
}

/** Decode one TupleElement. */
export function decodeTupleElement(reader: BinaryReader): TupleElement {
    const label = reader.readOption(() => decodeStringId(reader));
    const ty = decodeLocalNodeId(reader);
    const isOptional = reader.readBool();
    const isReadonly = reader.readBool();
    const isRest = reader.readBool();

    return {
        ...(label === undefined ? {} : { label }),
        ty,
        isOptional,
        isReadonly,
        isRest,
    };
}

/** Return one JSON value for one TupleElement. */
export function toJsonTupleElement(value: TupleElement): Json {
    return {
        ...(value.label === undefined ? {} : { label: toJsonStringId(value.label) }),
        ty: toJsonLocalNodeId(value.ty),
        isOptional: value.isOptional,
        isReadonly: value.isReadonly,
        isRest: value.isRest,
    };
}

/** Return one TupleElement from one JSON value. */
export function fromJsonTupleElement(value: Json): TupleElement {
    const object = jsonObject(value);

    return {
        label: jsonOptional(object, "label", (value) => fromJsonStringId(value)),
        ty: fromJsonLocalNodeId(jsonField(object, "ty")),
        isOptional: jsonBool(jsonField(object, "isOptional")),
        isReadonly: jsonBool(jsonField(object, "isReadonly")),
        isRest: jsonBool(jsonField(object, "isRest")),
    };
}

/** The type of an attribute (like a property or field). */
export type TypeMember =
    /** Named field (like `a: T`). */
    | {
          readonly kind: "field";
          readonly modifiers?: BindingModifier;
          readonly key: Key;
          readonly ty: LocalNodeId;
      }
    /** Named method (like `foo(): T`). */
    | {
          readonly kind: "method";
          readonly modifiers?: BindingModifier;
          readonly key: Key;
          readonly signature: FunctionSignature;
      }
    /** Call signature (like `<T>(value: T): U`). */
    | {
          readonly kind: "callSignature";
          readonly modifiers?: BindingModifier;
          readonly signature: FunctionTypeDeclaration;
      }
    /** Construct signature (like `new <T>(value: T): U`). */
    | {
          readonly kind: "constructSignature";
          readonly modifiers?: BindingModifier;
          readonly signature: ConstructorTypeDeclaration;
      }
    /** Index signature (like `[key: string]: T`). */
    | {
          readonly kind: "indexSignature";
          readonly modifiers?: BindingModifier;
          readonly name: StringId;
          readonly keyType: LocalNodeId;
          readonly valueType: LocalNodeId;
      }
;

export const TypeMember = {
    /** Named field (like `a: T`). */
    field(modifiers: BindingModifier | undefined, key: Key, ty: LocalNodeId): TypeMember {
        return { kind: "field", modifiers, key, ty };
    },

    /** Named method (like `foo(): T`). */
    method(modifiers: BindingModifier | undefined, key: Key, signature: FunctionSignature): TypeMember {
        return { kind: "method", modifiers, key, signature };
    },

    /** Call signature (like `<T>(value: T): U`). */
    callSignature(modifiers: BindingModifier | undefined, signature: FunctionTypeDeclaration): TypeMember {
        return { kind: "callSignature", modifiers, signature };
    },

    /** Construct signature (like `new <T>(value: T): U`). */
    constructSignature(modifiers: BindingModifier | undefined, signature: ConstructorTypeDeclaration): TypeMember {
        return { kind: "constructSignature", modifiers, signature };
    },

    /** Index signature (like `[key: string]: T`). */
    indexSignature(modifiers: BindingModifier | undefined, name: StringId, keyType: LocalNodeId, valueType: LocalNodeId): TypeMember {
        return { kind: "indexSignature", modifiers, name, keyType, valueType };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: TypeMember): void {
        encodeTypeMember(writer, value);
    },

    /** Decode one TypeMember. */
    decode(reader: BinaryReader): TypeMember {
        return decodeTypeMember(reader);
    },

    /** Return this value as JSON. */
    toJson(value: TypeMember): Json {
        return toJsonTypeMember(value);
    },

    /** Return one TypeMember from one JSON value. */
    fromJson(value: Json): TypeMember {
        return fromJsonTypeMember(value);
    },
};

/** Encode one TypeMember. */
export function encodeTypeMember(writer: BinaryWriter, value: TypeMember): void {
    switch (value.kind) {
        case "field":
            writer.writeUnsigned(0);
            writer.writeOption(value.modifiers, (value0) => {
                encodeBindingModifier(writer, value0);
            });
            encodeKey(writer, value.key);
            encodeLocalNodeId(writer, value.ty);
            return;
        case "method":
            writer.writeUnsigned(1);
            writer.writeOption(value.modifiers, (value0) => {
                encodeBindingModifier(writer, value0);
            });
            encodeKey(writer, value.key);
            encodeFunctionSignature(writer, value.signature);
            return;
        case "callSignature":
            writer.writeUnsigned(2);
            writer.writeOption(value.modifiers, (value0) => {
                encodeBindingModifier(writer, value0);
            });
            encodeFunctionTypeDeclaration(writer, value.signature);
            return;
        case "constructSignature":
            writer.writeUnsigned(3);
            writer.writeOption(value.modifiers, (value0) => {
                encodeBindingModifier(writer, value0);
            });
            encodeConstructorTypeDeclaration(writer, value.signature);
            return;
        case "indexSignature":
            writer.writeUnsigned(4);
            writer.writeOption(value.modifiers, (value0) => {
                encodeBindingModifier(writer, value0);
            });
            encodeStringId(writer, value.name);
            encodeLocalNodeId(writer, value.keyType);
            encodeLocalNodeId(writer, value.valueType);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one TypeMember. */
export function decodeTypeMember(reader: BinaryReader): TypeMember {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const modifiers = reader.readOption(() => decodeBindingModifier(reader));
            const key = decodeKey(reader);
            const ty = decodeLocalNodeId(reader);

            return {
                kind: "field",
                ...(modifiers === undefined ? {} : { modifiers }),
                key,
                ty,
            };
        }
        case 1: {
            const modifiers = reader.readOption(() => decodeBindingModifier(reader));
            const key = decodeKey(reader);
            const signature = decodeFunctionSignature(reader);

            return {
                kind: "method",
                ...(modifiers === undefined ? {} : { modifiers }),
                key,
                signature,
            };
        }
        case 2: {
            const modifiers = reader.readOption(() => decodeBindingModifier(reader));
            const signature = decodeFunctionTypeDeclaration(reader);

            return {
                kind: "callSignature",
                ...(modifiers === undefined ? {} : { modifiers }),
                signature,
            };
        }
        case 3: {
            const modifiers = reader.readOption(() => decodeBindingModifier(reader));
            const signature = decodeConstructorTypeDeclaration(reader);

            return {
                kind: "constructSignature",
                ...(modifiers === undefined ? {} : { modifiers }),
                signature,
            };
        }
        case 4: {
            const modifiers = reader.readOption(() => decodeBindingModifier(reader));
            const name = decodeStringId(reader);
            const keyType = decodeLocalNodeId(reader);
            const valueType = decodeLocalNodeId(reader);

            return {
                kind: "indexSignature",
                ...(modifiers === undefined ? {} : { modifiers }),
                name,
                keyType,
                valueType,
            };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one TypeMember. */
export function toJsonTypeMember(value: TypeMember): Json {
    switch (value.kind) {
        case "field":
            return {
                kind: "field",
                ...(value.modifiers === undefined ? {} : { modifiers: toJsonBindingModifier(value.modifiers) }),
                key: toJsonKey(value.key),
                ty: toJsonLocalNodeId(value.ty),
            };
        case "method":
            return {
                kind: "method",
                ...(value.modifiers === undefined ? {} : { modifiers: toJsonBindingModifier(value.modifiers) }),
                key: toJsonKey(value.key),
                signature: toJsonFunctionSignature(value.signature),
            };
        case "callSignature":
            return {
                kind: "callSignature",
                ...(value.modifiers === undefined ? {} : { modifiers: toJsonBindingModifier(value.modifiers) }),
                signature: toJsonFunctionTypeDeclaration(value.signature),
            };
        case "constructSignature":
            return {
                kind: "constructSignature",
                ...(value.modifiers === undefined ? {} : { modifiers: toJsonBindingModifier(value.modifiers) }),
                signature: toJsonConstructorTypeDeclaration(value.signature),
            };
        case "indexSignature":
            return {
                kind: "indexSignature",
                ...(value.modifiers === undefined ? {} : { modifiers: toJsonBindingModifier(value.modifiers) }),
                name: toJsonStringId(value.name),
                keyType: toJsonLocalNodeId(value.keyType),
                valueType: toJsonLocalNodeId(value.valueType),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one TypeMember from one JSON value. */
export function fromJsonTypeMember(value: Json): TypeMember {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "field":
            return {
                kind,
                modifiers: jsonOptional(object, "modifiers", (value) => fromJsonBindingModifier(value)),
                key: fromJsonKey(jsonField(object, "key")),
                ty: fromJsonLocalNodeId(jsonField(object, "ty")),
            };
        case "method":
            return {
                kind,
                modifiers: jsonOptional(object, "modifiers", (value) => fromJsonBindingModifier(value)),
                key: fromJsonKey(jsonField(object, "key")),
                signature: fromJsonFunctionSignature(jsonField(object, "signature")),
            };
        case "callSignature":
            return {
                kind,
                modifiers: jsonOptional(object, "modifiers", (value) => fromJsonBindingModifier(value)),
                signature: fromJsonFunctionTypeDeclaration(jsonField(object, "signature")),
            };
        case "constructSignature":
            return {
                kind,
                modifiers: jsonOptional(object, "modifiers", (value) => fromJsonBindingModifier(value)),
                signature: fromJsonConstructorTypeDeclaration(jsonField(object, "signature")),
            };
        case "indexSignature":
            return {
                kind,
                modifiers: jsonOptional(object, "modifiers", (value) => fromJsonBindingModifier(value)),
                name: fromJsonStringId(jsonField(object, "name")),
                keyType: fromJsonLocalNodeId(jsonField(object, "keyType")),
                valueType: fromJsonLocalNodeId(jsonField(object, "valueType")),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}
