/* generated by destack_bridge_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, jsonArray, jsonField, jsonObject, jsonOptional } from "../../../protocol/serde.js";
import type { LocalNodeId } from "./node.js";
import { decodeLocalNodeId, encodeLocalNodeId, fromJsonLocalNodeId, toJsonLocalNodeId } from "./node.js";

/** Block of statements. */
export type Block = {
    /** The statements in the block. */
    readonly statements: ReadonlyArray<LocalNodeId>;
};

export const Block = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: Block): void {
        encodeBlock(writer, value);
    },

    /** Decode one Block. */
    decode(reader: BinaryReader): Block {
        return decodeBlock(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Block): Json {
        return toJsonBlock(value);
    },

    /** Return one Block from one JSON value. */
    fromJson(value: Json): Block {
        return fromJsonBlock(value);
    },
};

/** Encode one Block. */
export function encodeBlock(writer: BinaryWriter, value: Block): void {
    writer.writeUnsigned(value.statements.length);
    for (const item0 of value.statements) {
        encodeLocalNodeId(writer, item0);
    }
}

/** Decode one Block. */
export function decodeBlock(reader: BinaryReader): Block {
    const statements = (() => { const length0 = reader.readNumber(); const items0: Array<LocalNodeId> = []; for (let index = 0; index < length0; index += 1) { items0.push(decodeLocalNodeId(reader)); } return items0; })();

    return {
        statements,
    };
}

/** Return one JSON value for one Block. */
export function toJsonBlock(value: Block): Json {
    return {
        statements: value.statements.map((item0) => toJsonLocalNodeId(item0)),
    };
}

/** Return one Block from one JSON value. */
export function fromJsonBlock(value: Json): Block {
    const object = jsonObject(value);

    return {
        statements: jsonArray(jsonField(object, "statements")).map((item0) => fromJsonLocalNodeId(item0)),
    };
}

/** A catch clause. */
export type CatchClause = {
    /** The optional catch pattern. */
    readonly pattern?: LocalNodeId;
    /** The catch body. */
    readonly body: LocalNodeId;
};

export const CatchClause = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: CatchClause): void {
        encodeCatchClause(writer, value);
    },

    /** Decode one CatchClause. */
    decode(reader: BinaryReader): CatchClause {
        return decodeCatchClause(reader);
    },

    /** Return this value as JSON. */
    toJson(value: CatchClause): Json {
        return toJsonCatchClause(value);
    },

    /** Return one CatchClause from one JSON value. */
    fromJson(value: Json): CatchClause {
        return fromJsonCatchClause(value);
    },
};

/** Encode one CatchClause. */
export function encodeCatchClause(writer: BinaryWriter, value: CatchClause): void {
    writer.writeOption(value.pattern, (value0) => {
        encodeLocalNodeId(writer, value0);
    });
    encodeLocalNodeId(writer, value.body);
}

/** Decode one CatchClause. */
export function decodeCatchClause(reader: BinaryReader): CatchClause {
    const pattern = reader.readOption(() => decodeLocalNodeId(reader));
    const body = decodeLocalNodeId(reader);

    return {
        ...(pattern === undefined ? {} : { pattern }),
        body,
    };
}

/** Return one JSON value for one CatchClause. */
export function toJsonCatchClause(value: CatchClause): Json {
    return {
        ...(value.pattern === undefined ? {} : { pattern: toJsonLocalNodeId(value.pattern) }),
        body: toJsonLocalNodeId(value.body),
    };
}

/** Return one CatchClause from one JSON value. */
export function fromJsonCatchClause(value: Json): CatchClause {
    const object = jsonObject(value);

    return {
        pattern: jsonOptional(object, "pattern", (value) => fromJsonLocalNodeId(value)),
        body: fromJsonLocalNodeId(jsonField(object, "body")),
    };
}

/** A switch case. */
export type SwitchCase = {
    /** The optional case selector. */
    readonly value?: LocalNodeId;
    /** The body of the case. */
    readonly body: LocalNodeId;
};

export const SwitchCase = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: SwitchCase): void {
        encodeSwitchCase(writer, value);
    },

    /** Decode one SwitchCase. */
    decode(reader: BinaryReader): SwitchCase {
        return decodeSwitchCase(reader);
    },

    /** Return this value as JSON. */
    toJson(value: SwitchCase): Json {
        return toJsonSwitchCase(value);
    },

    /** Return one SwitchCase from one JSON value. */
    fromJson(value: Json): SwitchCase {
        return fromJsonSwitchCase(value);
    },
};

/** Encode one SwitchCase. */
export function encodeSwitchCase(writer: BinaryWriter, value: SwitchCase): void {
    writer.writeOption(value.value, (value0) => {
        encodeLocalNodeId(writer, value0);
    });
    encodeLocalNodeId(writer, value.body);
}

/** Decode one SwitchCase. */
export function decodeSwitchCase(reader: BinaryReader): SwitchCase {
    const value = reader.readOption(() => decodeLocalNodeId(reader));
    const body = decodeLocalNodeId(reader);

    return {
        ...(value === undefined ? {} : { value }),
        body,
    };
}

/** Return one JSON value for one SwitchCase. */
export function toJsonSwitchCase(value: SwitchCase): Json {
    return {
        ...(value.value === undefined ? {} : { value: toJsonLocalNodeId(value.value) }),
        body: toJsonLocalNodeId(value.body),
    };
}

/** Return one SwitchCase from one JSON value. */
export function fromJsonSwitchCase(value: Json): SwitchCase {
    const object = jsonObject(value);

    return {
        value: jsonOptional(object, "value", (value) => fromJsonLocalNodeId(value)),
        body: fromJsonLocalNodeId(jsonField(object, "body")),
    };
}
