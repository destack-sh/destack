/* generated by destack_bridge_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, jsonArray, jsonField, jsonObject } from "../../../protocol/serde.js";
import type { StringId } from "../../core/string.js";
import { decodeStringId, encodeStringId, fromJsonStringId, toJsonStringId } from "../../core/string.js";

/** A Path is a sequence of segments. */
export type Path = {
    /** The segments of the path. */
    readonly segments: ReadonlyArray<StringId>;
};

export const Path = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: Path): void {
        encodePath(writer, value);
    },

    /** Decode one Path. */
    decode(reader: BinaryReader): Path {
        return decodePath(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Path): Json {
        return toJsonPath(value);
    },

    /** Return one Path from one JSON value. */
    fromJson(value: Json): Path {
        return fromJsonPath(value);
    },
};

/** Encode one Path. */
export function encodePath(writer: BinaryWriter, value: Path): void {
    writer.writeUnsigned(value.segments.length);
    for (const item0 of value.segments) {
        encodeStringId(writer, item0);
    }
}

/** Decode one Path. */
export function decodePath(reader: BinaryReader): Path {
    const segments = (() => { const length0 = reader.readNumber(); const items0: Array<StringId> = []; for (let index = 0; index < length0; index += 1) { items0.push(decodeStringId(reader)); } return items0; })();

    return {
        segments,
    };
}

/** Return one JSON value for one Path. */
export function toJsonPath(value: Path): Json {
    return {
        segments: value.segments.map((item0) => toJsonStringId(item0)),
    };
}

/** Return one Path from one JSON value. */
export function fromJsonPath(value: Json): Path {
    const object = jsonObject(value);

    return {
        segments: jsonArray(jsonField(object, "segments")).map((item0) => fromJsonStringId(item0)),
    };
}
