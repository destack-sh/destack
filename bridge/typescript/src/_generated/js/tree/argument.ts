/* generated by destack_bridge_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, jsonBool, jsonField, jsonObject, jsonOptional, jsonString } from "../../../protocol/serde.js";
import type { StringId } from "../../core/string.js";
import type { LocalNodeId } from "./node.js";
import type { Mutability } from "./node.js";
import type { Visibility } from "./node.js";
import { decodeStringId, encodeStringId, fromJsonStringId, toJsonStringId } from "../../core/string.js";
import { decodeLocalNodeId, encodeLocalNodeId, fromJsonLocalNodeId, toJsonLocalNodeId } from "./node.js";
import { decodeMutability, encodeMutability, fromJsonMutability, toJsonMutability } from "./node.js";
import { decodeVisibility, encodeVisibility, fromJsonVisibility, toJsonVisibility } from "./node.js";

/** The modifiers of a field-like item. */
export type BindingModifier = {
    /** The kind of the binding. */
    readonly kind?: BindingKind;
    /** The variance of a type parameter. */
    readonly variance?: VarianceModifier;
    /** The scope of the binding. */
    readonly anchor?: BindingAnchor;
    /** The mutability of the field. */
    readonly mutability?: Mutability;
    /** The visibility of the field. */
    readonly visibility?: Visibility;
    /** The operator to apply to the binding. */
    readonly operator?: BindingOperator;
    /** Whether the binding uses a definite assignment assertion. */
    readonly definite: boolean;
    /** The accessor kind of the binding. */
    readonly accessor?: AccessorKind;
};

export const BindingModifier = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: BindingModifier): void {
        encodeBindingModifier(writer, value);
    },

    /** Decode one BindingModifier. */
    decode(reader: BinaryReader): BindingModifier {
        return decodeBindingModifier(reader);
    },

    /** Return this value as JSON. */
    toJson(value: BindingModifier): Json {
        return toJsonBindingModifier(value);
    },

    /** Return one BindingModifier from one JSON value. */
    fromJson(value: Json): BindingModifier {
        return fromJsonBindingModifier(value);
    },
};

/** Encode one BindingModifier. */
export function encodeBindingModifier(writer: BinaryWriter, value: BindingModifier): void {
    writer.writeOption(value.kind, (value0) => {
        encodeBindingKind(writer, value0);
    });
    writer.writeOption(value.variance, (value1) => {
        encodeVarianceModifier(writer, value1);
    });
    writer.writeOption(value.anchor, (value2) => {
        encodeBindingAnchor(writer, value2);
    });
    writer.writeOption(value.mutability, (value3) => {
        encodeMutability(writer, value3);
    });
    writer.writeOption(value.visibility, (value4) => {
        encodeVisibility(writer, value4);
    });
    writer.writeOption(value.operator, (value5) => {
        encodeBindingOperator(writer, value5);
    });
    writer.writeBool(value.definite);
    writer.writeOption(value.accessor, (value7) => {
        encodeAccessorKind(writer, value7);
    });
}

/** Decode one BindingModifier. */
export function decodeBindingModifier(reader: BinaryReader): BindingModifier {
    const kind = reader.readOption(() => decodeBindingKind(reader));
    const variance = reader.readOption(() => decodeVarianceModifier(reader));
    const anchor = reader.readOption(() => decodeBindingAnchor(reader));
    const mutability = reader.readOption(() => decodeMutability(reader));
    const visibility = reader.readOption(() => decodeVisibility(reader));
    const operator = reader.readOption(() => decodeBindingOperator(reader));
    const definite = reader.readBool();
    const accessor = reader.readOption(() => decodeAccessorKind(reader));

    return {
        ...(kind === undefined ? {} : { kind }),
        ...(variance === undefined ? {} : { variance }),
        ...(anchor === undefined ? {} : { anchor }),
        ...(mutability === undefined ? {} : { mutability }),
        ...(visibility === undefined ? {} : { visibility }),
        ...(operator === undefined ? {} : { operator }),
        definite,
        ...(accessor === undefined ? {} : { accessor }),
    };
}

/** Return one JSON value for one BindingModifier. */
export function toJsonBindingModifier(value: BindingModifier): Json {
    return {
        ...(value.kind === undefined ? {} : { kind: toJsonBindingKind(value.kind) }),
        ...(value.variance === undefined ? {} : { variance: toJsonVarianceModifier(value.variance) }),
        ...(value.anchor === undefined ? {} : { anchor: toJsonBindingAnchor(value.anchor) }),
        ...(value.mutability === undefined ? {} : { mutability: toJsonMutability(value.mutability) }),
        ...(value.visibility === undefined ? {} : { visibility: toJsonVisibility(value.visibility) }),
        ...(value.operator === undefined ? {} : { operator: toJsonBindingOperator(value.operator) }),
        definite: value.definite,
        ...(value.accessor === undefined ? {} : { accessor: toJsonAccessorKind(value.accessor) }),
    };
}

/** Return one BindingModifier from one JSON value. */
export function fromJsonBindingModifier(value: Json): BindingModifier {
    const object = jsonObject(value);

    return {
        kind: jsonOptional(object, "kind", (value) => fromJsonBindingKind(value)),
        variance: jsonOptional(object, "variance", (value) => fromJsonVarianceModifier(value)),
        anchor: jsonOptional(object, "anchor", (value) => fromJsonBindingAnchor(value)),
        mutability: jsonOptional(object, "mutability", (value) => fromJsonMutability(value)),
        visibility: jsonOptional(object, "visibility", (value) => fromJsonVisibility(value)),
        operator: jsonOptional(object, "operator", (value) => fromJsonBindingOperator(value)),
        definite: jsonBool(jsonField(object, "definite")),
        accessor: jsonOptional(object, "accessor", (value) => fromJsonAccessorKind(value)),
    };
}

/** The type of a binding. */
export type BindingKind = "must" | "maybe";

export const BindingKind = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: BindingKind): void {
        encodeBindingKind(writer, value);
    },

    /** Decode one BindingKind. */
    decode(reader: BinaryReader): BindingKind {
        return decodeBindingKind(reader);
    },

    /** Return this value as JSON. */
    toJson(value: BindingKind): Json {
        return toJsonBindingKind(value);
    },

    /** Return one BindingKind from one JSON value. */
    fromJson(value: Json): BindingKind {
        return fromJsonBindingKind(value);
    },
};

/** Encode one BindingKind. */
export function encodeBindingKind(writer: BinaryWriter, value: BindingKind): void {
    switch (value) {
        case "must":
            writer.writeUnsigned(0);
            return;
        case "maybe":
            writer.writeUnsigned(1);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one BindingKind. */
export function decodeBindingKind(reader: BinaryReader): BindingKind {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "must";
        case 1:
            return "maybe";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one BindingKind. */
export function toJsonBindingKind(value: BindingKind): Json {
    return value;
}

/** Return one BindingKind from one JSON value. */
export function fromJsonBindingKind(value: Json): BindingKind {
    const variant = jsonString(value);

    switch (variant) {
        case "must":
            return "must";
        case "maybe":
            return "maybe";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** Variance annotation for type parameters. */
export type VarianceModifier = "in" | "out" | "inOut";

export const VarianceModifier = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: VarianceModifier): void {
        encodeVarianceModifier(writer, value);
    },

    /** Decode one VarianceModifier. */
    decode(reader: BinaryReader): VarianceModifier {
        return decodeVarianceModifier(reader);
    },

    /** Return this value as JSON. */
    toJson(value: VarianceModifier): Json {
        return toJsonVarianceModifier(value);
    },

    /** Return one VarianceModifier from one JSON value. */
    fromJson(value: Json): VarianceModifier {
        return fromJsonVarianceModifier(value);
    },
};

/** Encode one VarianceModifier. */
export function encodeVarianceModifier(writer: BinaryWriter, value: VarianceModifier): void {
    switch (value) {
        case "in":
            writer.writeUnsigned(0);
            return;
        case "out":
            writer.writeUnsigned(1);
            return;
        case "inOut":
            writer.writeUnsigned(2);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one VarianceModifier. */
export function decodeVarianceModifier(reader: BinaryReader): VarianceModifier {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "in";
        case 1:
            return "out";
        case 2:
            return "inOut";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one VarianceModifier. */
export function toJsonVarianceModifier(value: VarianceModifier): Json {
    return value;
}

/** Return one VarianceModifier from one JSON value. */
export function fromJsonVarianceModifier(value: Json): VarianceModifier {
    const variant = jsonString(value);

    switch (variant) {
        case "in":
            return "in";
        case "out":
            return "out";
        case "inOut":
            return "inOut";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** The scope of a binding (dynamic or static). */
export type BindingAnchor = "instance" | "static";

export const BindingAnchor = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: BindingAnchor): void {
        encodeBindingAnchor(writer, value);
    },

    /** Decode one BindingAnchor. */
    decode(reader: BinaryReader): BindingAnchor {
        return decodeBindingAnchor(reader);
    },

    /** Return this value as JSON. */
    toJson(value: BindingAnchor): Json {
        return toJsonBindingAnchor(value);
    },

    /** Return one BindingAnchor from one JSON value. */
    fromJson(value: Json): BindingAnchor {
        return fromJsonBindingAnchor(value);
    },
};

/** Encode one BindingAnchor. */
export function encodeBindingAnchor(writer: BinaryWriter, value: BindingAnchor): void {
    switch (value) {
        case "instance":
            writer.writeUnsigned(0);
            return;
        case "static":
            writer.writeUnsigned(1);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one BindingAnchor. */
export function decodeBindingAnchor(reader: BinaryReader): BindingAnchor {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "instance";
        case 1:
            return "static";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one BindingAnchor. */
export function toJsonBindingAnchor(value: BindingAnchor): Json {
    return value;
}

/** Return one BindingAnchor from one JSON value. */
export function fromJsonBindingAnchor(value: Json): BindingAnchor {
    const variant = jsonString(value);

    switch (variant) {
        case "instance":
            return "instance";
        case "static":
            return "static";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** The operator to apply to the binding. */
export type BindingOperator = "asConst";

export const BindingOperator = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: BindingOperator): void {
        encodeBindingOperator(writer, value);
    },

    /** Decode one BindingOperator. */
    decode(reader: BinaryReader): BindingOperator {
        return decodeBindingOperator(reader);
    },

    /** Return this value as JSON. */
    toJson(value: BindingOperator): Json {
        return toJsonBindingOperator(value);
    },

    /** Return one BindingOperator from one JSON value. */
    fromJson(value: Json): BindingOperator {
        return fromJsonBindingOperator(value);
    },
};

/** Encode one BindingOperator. */
export function encodeBindingOperator(writer: BinaryWriter, value: BindingOperator): void {
    switch (value) {
        case "asConst":
            writer.writeUnsigned(0);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one BindingOperator. */
export function decodeBindingOperator(reader: BinaryReader): BindingOperator {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "asConst";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one BindingOperator. */
export function toJsonBindingOperator(value: BindingOperator): Json {
    return value;
}

/** Return one BindingOperator from one JSON value. */
export function fromJsonBindingOperator(value: Json): BindingOperator {
    const variant = jsonString(value);

    switch (variant) {
        case "asConst":
            return "asConst";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** The accessor kind of a binding. */
export type AccessorKind = "accessor";

export const AccessorKind = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: AccessorKind): void {
        encodeAccessorKind(writer, value);
    },

    /** Decode one AccessorKind. */
    decode(reader: BinaryReader): AccessorKind {
        return decodeAccessorKind(reader);
    },

    /** Return this value as JSON. */
    toJson(value: AccessorKind): Json {
        return toJsonAccessorKind(value);
    },

    /** Return one AccessorKind from one JSON value. */
    fromJson(value: Json): AccessorKind {
        return fromJsonAccessorKind(value);
    },
};

/** Encode one AccessorKind. */
export function encodeAccessorKind(writer: BinaryWriter, value: AccessorKind): void {
    switch (value) {
        case "accessor":
            writer.writeUnsigned(0);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one AccessorKind. */
export function decodeAccessorKind(reader: BinaryReader): AccessorKind {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "accessor";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one AccessorKind. */
export function toJsonAccessorKind(value: AccessorKind): Json {
    return value;
}

/** Return one AccessorKind from one JSON value. */
export function fromJsonAccessorKind(value: Json): AccessorKind {
    const variant = jsonString(value);

    switch (variant) {
        case "accessor":
            return "accessor";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** One generic parameter. */
export type GenericParameter =
    /** Type parameter. */
    | {
          readonly kind: "type";
          readonly modifiers?: BindingModifier;
          readonly name: StringId;
          readonly constraint?: LocalNodeId;
          readonly default?: LocalNodeId;
      }
;

export const GenericParameter = {
    /** Type parameter. */
    "type"(modifiers: BindingModifier | undefined, name: StringId, constraint: LocalNodeId | undefined, default_: LocalNodeId | undefined): GenericParameter {
        return { kind: "type", modifiers, name, constraint, default: default_ };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: GenericParameter): void {
        encodeGenericParameter(writer, value);
    },

    /** Decode one GenericParameter. */
    decode(reader: BinaryReader): GenericParameter {
        return decodeGenericParameter(reader);
    },

    /** Return this value as JSON. */
    toJson(value: GenericParameter): Json {
        return toJsonGenericParameter(value);
    },

    /** Return one GenericParameter from one JSON value. */
    fromJson(value: Json): GenericParameter {
        return fromJsonGenericParameter(value);
    },
};

/** Encode one GenericParameter. */
export function encodeGenericParameter(writer: BinaryWriter, value: GenericParameter): void {
    switch (value.kind) {
        case "type":
            writer.writeUnsigned(0);
            writer.writeOption(value.modifiers, (value0) => {
                encodeBindingModifier(writer, value0);
            });
            encodeStringId(writer, value.name);
            writer.writeOption(value.constraint, (value2) => {
                encodeLocalNodeId(writer, value2);
            });
            writer.writeOption(value.default, (value3) => {
                encodeLocalNodeId(writer, value3);
            });
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one GenericParameter. */
export function decodeGenericParameter(reader: BinaryReader): GenericParameter {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const modifiers = reader.readOption(() => decodeBindingModifier(reader));
            const name = decodeStringId(reader);
            const constraint = reader.readOption(() => decodeLocalNodeId(reader));
            const default_ = reader.readOption(() => decodeLocalNodeId(reader));

            return {
                kind: "type",
                ...(modifiers === undefined ? {} : { modifiers }),
                name,
                ...(constraint === undefined ? {} : { constraint }),
                ...(default_ === undefined ? {} : { default: default_ }),
            };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one GenericParameter. */
export function toJsonGenericParameter(value: GenericParameter): Json {
    switch (value.kind) {
        case "type":
            return {
                kind: "type",
                ...(value.modifiers === undefined ? {} : { modifiers: toJsonBindingModifier(value.modifiers) }),
                name: toJsonStringId(value.name),
                ...(value.constraint === undefined ? {} : { constraint: toJsonLocalNodeId(value.constraint) }),
                ...(value.default === undefined ? {} : { default: toJsonLocalNodeId(value.default) }),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one GenericParameter from one JSON value. */
export function fromJsonGenericParameter(value: Json): GenericParameter {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "type":
            return {
                kind,
                modifiers: jsonOptional(object, "modifiers", (value) => fromJsonBindingModifier(value)),
                name: fromJsonStringId(jsonField(object, "name")),
                constraint: jsonOptional(object, "constraint", (value) => fromJsonLocalNodeId(value)),
                default: jsonOptional(object, "default", (value) => fromJsonLocalNodeId(value)),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** Named or positional parameter to some construct. */
export type Parameter =
    /** Named parameter (like `x: int32` or `Validate: boolean = true`). */
    | {
          readonly kind: "named";
          readonly modifiers?: BindingModifier;
          readonly name: StringId;
          readonly ty?: LocalNodeId;
          readonly default?: LocalNodeId;
      }
    /** Pattern parameter (like `_` or `{ x }` or `{ x, ..rest }: MyType = Foo`). */
    | {
          readonly kind: "pattern";
          readonly modifiers?: BindingModifier;
          readonly pattern: LocalNodeId;
          readonly ty?: LocalNodeId;
          readonly default?: LocalNodeId;
      }
    /** Variadic parameter with a named binding (like `...args: int32[]`). */
    | {
          readonly kind: "variadicNamed";
          readonly modifiers?: BindingModifier;
          readonly name: StringId;
          readonly ty?: LocalNodeId;
      }
    /** Variadic parameter with a pattern binding (like `...[a, b]`). */
    | {
          readonly kind: "variadicPattern";
          readonly modifiers?: BindingModifier;
          readonly pattern: LocalNodeId;
          readonly ty?: LocalNodeId;
      }
;

export const Parameter = {
    /** Named parameter (like `x: int32` or `Validate: boolean = true`). */
    named(modifiers: BindingModifier | undefined, name: StringId, ty: LocalNodeId | undefined, default_: LocalNodeId | undefined): Parameter {
        return { kind: "named", modifiers, name, ty, default: default_ };
    },

    /** Pattern parameter (like `_` or `{ x }` or `{ x, ..rest }: MyType = Foo`). */
    pattern(modifiers: BindingModifier | undefined, pattern: LocalNodeId, ty: LocalNodeId | undefined, default_: LocalNodeId | undefined): Parameter {
        return { kind: "pattern", modifiers, pattern, ty, default: default_ };
    },

    /** Variadic parameter with a named binding (like `...args: int32[]`). */
    variadicNamed(modifiers: BindingModifier | undefined, name: StringId, ty: LocalNodeId | undefined): Parameter {
        return { kind: "variadicNamed", modifiers, name, ty };
    },

    /** Variadic parameter with a pattern binding (like `...[a, b]`). */
    variadicPattern(modifiers: BindingModifier | undefined, pattern: LocalNodeId, ty: LocalNodeId | undefined): Parameter {
        return { kind: "variadicPattern", modifiers, pattern, ty };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: Parameter): void {
        encodeParameter(writer, value);
    },

    /** Decode one Parameter. */
    decode(reader: BinaryReader): Parameter {
        return decodeParameter(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Parameter): Json {
        return toJsonParameter(value);
    },

    /** Return one Parameter from one JSON value. */
    fromJson(value: Json): Parameter {
        return fromJsonParameter(value);
    },
};

/** Encode one Parameter. */
export function encodeParameter(writer: BinaryWriter, value: Parameter): void {
    switch (value.kind) {
        case "named":
            writer.writeUnsigned(0);
            writer.writeOption(value.modifiers, (value0) => {
                encodeBindingModifier(writer, value0);
            });
            encodeStringId(writer, value.name);
            writer.writeOption(value.ty, (value2) => {
                encodeLocalNodeId(writer, value2);
            });
            writer.writeOption(value.default, (value3) => {
                encodeLocalNodeId(writer, value3);
            });
            return;
        case "pattern":
            writer.writeUnsigned(1);
            writer.writeOption(value.modifiers, (value0) => {
                encodeBindingModifier(writer, value0);
            });
            encodeLocalNodeId(writer, value.pattern);
            writer.writeOption(value.ty, (value2) => {
                encodeLocalNodeId(writer, value2);
            });
            writer.writeOption(value.default, (value3) => {
                encodeLocalNodeId(writer, value3);
            });
            return;
        case "variadicNamed":
            writer.writeUnsigned(2);
            writer.writeOption(value.modifiers, (value0) => {
                encodeBindingModifier(writer, value0);
            });
            encodeStringId(writer, value.name);
            writer.writeOption(value.ty, (value2) => {
                encodeLocalNodeId(writer, value2);
            });
            return;
        case "variadicPattern":
            writer.writeUnsigned(3);
            writer.writeOption(value.modifiers, (value0) => {
                encodeBindingModifier(writer, value0);
            });
            encodeLocalNodeId(writer, value.pattern);
            writer.writeOption(value.ty, (value2) => {
                encodeLocalNodeId(writer, value2);
            });
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one Parameter. */
export function decodeParameter(reader: BinaryReader): Parameter {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const modifiers = reader.readOption(() => decodeBindingModifier(reader));
            const name = decodeStringId(reader);
            const ty = reader.readOption(() => decodeLocalNodeId(reader));
            const default_ = reader.readOption(() => decodeLocalNodeId(reader));

            return {
                kind: "named",
                ...(modifiers === undefined ? {} : { modifiers }),
                name,
                ...(ty === undefined ? {} : { ty }),
                ...(default_ === undefined ? {} : { default: default_ }),
            };
        }
        case 1: {
            const modifiers = reader.readOption(() => decodeBindingModifier(reader));
            const pattern = decodeLocalNodeId(reader);
            const ty = reader.readOption(() => decodeLocalNodeId(reader));
            const default_ = reader.readOption(() => decodeLocalNodeId(reader));

            return {
                kind: "pattern",
                ...(modifiers === undefined ? {} : { modifiers }),
                pattern,
                ...(ty === undefined ? {} : { ty }),
                ...(default_ === undefined ? {} : { default: default_ }),
            };
        }
        case 2: {
            const modifiers = reader.readOption(() => decodeBindingModifier(reader));
            const name = decodeStringId(reader);
            const ty = reader.readOption(() => decodeLocalNodeId(reader));

            return {
                kind: "variadicNamed",
                ...(modifiers === undefined ? {} : { modifiers }),
                name,
                ...(ty === undefined ? {} : { ty }),
            };
        }
        case 3: {
            const modifiers = reader.readOption(() => decodeBindingModifier(reader));
            const pattern = decodeLocalNodeId(reader);
            const ty = reader.readOption(() => decodeLocalNodeId(reader));

            return {
                kind: "variadicPattern",
                ...(modifiers === undefined ? {} : { modifiers }),
                pattern,
                ...(ty === undefined ? {} : { ty }),
            };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one Parameter. */
export function toJsonParameter(value: Parameter): Json {
    switch (value.kind) {
        case "named":
            return {
                kind: "named",
                ...(value.modifiers === undefined ? {} : { modifiers: toJsonBindingModifier(value.modifiers) }),
                name: toJsonStringId(value.name),
                ...(value.ty === undefined ? {} : { ty: toJsonLocalNodeId(value.ty) }),
                ...(value.default === undefined ? {} : { default: toJsonLocalNodeId(value.default) }),
            };
        case "pattern":
            return {
                kind: "pattern",
                ...(value.modifiers === undefined ? {} : { modifiers: toJsonBindingModifier(value.modifiers) }),
                pattern: toJsonLocalNodeId(value.pattern),
                ...(value.ty === undefined ? {} : { ty: toJsonLocalNodeId(value.ty) }),
                ...(value.default === undefined ? {} : { default: toJsonLocalNodeId(value.default) }),
            };
        case "variadicNamed":
            return {
                kind: "variadicNamed",
                ...(value.modifiers === undefined ? {} : { modifiers: toJsonBindingModifier(value.modifiers) }),
                name: toJsonStringId(value.name),
                ...(value.ty === undefined ? {} : { ty: toJsonLocalNodeId(value.ty) }),
            };
        case "variadicPattern":
            return {
                kind: "variadicPattern",
                ...(value.modifiers === undefined ? {} : { modifiers: toJsonBindingModifier(value.modifiers) }),
                pattern: toJsonLocalNodeId(value.pattern),
                ...(value.ty === undefined ? {} : { ty: toJsonLocalNodeId(value.ty) }),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one Parameter from one JSON value. */
export function fromJsonParameter(value: Json): Parameter {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "named":
            return {
                kind,
                modifiers: jsonOptional(object, "modifiers", (value) => fromJsonBindingModifier(value)),
                name: fromJsonStringId(jsonField(object, "name")),
                ty: jsonOptional(object, "ty", (value) => fromJsonLocalNodeId(value)),
                default: jsonOptional(object, "default", (value) => fromJsonLocalNodeId(value)),
            };
        case "pattern":
            return {
                kind,
                modifiers: jsonOptional(object, "modifiers", (value) => fromJsonBindingModifier(value)),
                pattern: fromJsonLocalNodeId(jsonField(object, "pattern")),
                ty: jsonOptional(object, "ty", (value) => fromJsonLocalNodeId(value)),
                default: jsonOptional(object, "default", (value) => fromJsonLocalNodeId(value)),
            };
        case "variadicNamed":
            return {
                kind,
                modifiers: jsonOptional(object, "modifiers", (value) => fromJsonBindingModifier(value)),
                name: fromJsonStringId(jsonField(object, "name")),
                ty: jsonOptional(object, "ty", (value) => fromJsonLocalNodeId(value)),
            };
        case "variadicPattern":
            return {
                kind,
                modifiers: jsonOptional(object, "modifiers", (value) => fromJsonBindingModifier(value)),
                pattern: fromJsonLocalNodeId(jsonField(object, "pattern")),
                ty: jsonOptional(object, "ty", (value) => fromJsonLocalNodeId(value)),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** Positional argument to some construct. */
export type Argument =
    /** Positional argument (like `1` or `foo()`). */
    | {
          readonly kind: "positional";
          readonly value: LocalNodeId;
      }
    /** Spread argument (like `...args`). */
    | {
          readonly kind: "spread";
          readonly value: LocalNodeId;
      }
;

export const Argument = {
    /** Positional argument (like `1` or `foo()`). */
    positional(value: LocalNodeId): Argument {
        return { kind: "positional", value };
    },

    /** Spread argument (like `...args`). */
    spread(value: LocalNodeId): Argument {
        return { kind: "spread", value };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: Argument): void {
        encodeArgument(writer, value);
    },

    /** Decode one Argument. */
    decode(reader: BinaryReader): Argument {
        return decodeArgument(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Argument): Json {
        return toJsonArgument(value);
    },

    /** Return one Argument from one JSON value. */
    fromJson(value: Json): Argument {
        return fromJsonArgument(value);
    },
};

/** Encode one Argument. */
export function encodeArgument(writer: BinaryWriter, value: Argument): void {
    switch (value.kind) {
        case "positional":
            writer.writeUnsigned(0);
            encodeLocalNodeId(writer, value.value);
            return;
        case "spread":
            writer.writeUnsigned(1);
            encodeLocalNodeId(writer, value.value);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one Argument. */
export function decodeArgument(reader: BinaryReader): Argument {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const value = decodeLocalNodeId(reader);

            return {
                kind: "positional",
                value,
            };
        }
        case 1: {
            const value = decodeLocalNodeId(reader);

            return {
                kind: "spread",
                value,
            };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one Argument. */
export function toJsonArgument(value: Argument): Json {
    switch (value.kind) {
        case "positional":
            return {
                kind: "positional",
                value: toJsonLocalNodeId(value.value),
            };
        case "spread":
            return {
                kind: "spread",
                value: toJsonLocalNodeId(value.value),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one Argument from one JSON value. */
export function fromJsonArgument(value: Json): Argument {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "positional":
            return {
                kind,
                value: fromJsonLocalNodeId(jsonField(object, "value")),
            };
        case "spread":
            return {
                kind,
                value: fromJsonLocalNodeId(jsonField(object, "value")),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}
