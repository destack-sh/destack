/* generated by destack_bridge_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, jsonArray, jsonBigint, jsonBool, jsonField, jsonNumber, jsonObject, jsonOptional, jsonString } from "../../../protocol/serde.js";
import type { StringId } from "../../core/string.js";
import type { LocalNodeId } from "./node.js";
import type { Path } from "./path.js";
import { decodeStringId, encodeStringId, fromJsonStringId, toJsonStringId } from "../../core/string.js";
import { decodeLocalNodeId, encodeLocalNodeId, fromJsonLocalNodeId, toJsonLocalNodeId } from "./node.js";
import { decodePath, encodePath, fromJsonPath, toJsonPath } from "./path.js";

/** A ScalarLiteral is literal scalar value. */
export type ScalarLiteral =
    /** Null value. */
    | {
          readonly kind: "null";
      }
    /** Undefined value. */
    | {
          readonly kind: "undefined";
      }
    /** Boolean value. */
    | {
          readonly kind: "boolean";
          readonly boolean: boolean;
      }
    /** Number value. */
    | {
          readonly kind: "number";
          readonly number: number;
      }
    /** Bigint value. */
    | {
          readonly kind: "bigint";
          readonly bigint: bigint;
      }
    /** String value. */
    | {
          readonly kind: "string";
          readonly string: StringId;
      }
    /** Regex string value. */
    | {
          readonly kind: "regexString";
          readonly content: StringId;
          readonly flags?: StringId;
      }
;

export const ScalarLiteral = {
    /** Null value. */
    "null"(): ScalarLiteral {
        return { kind: "null" };
    },

    /** Undefined value. */
    "undefined"(): ScalarLiteral {
        return { kind: "undefined" };
    },

    /** Boolean value. */
    "boolean"(boolean_: boolean): ScalarLiteral {
        return { kind: "boolean", boolean: boolean_ };
    },

    /** Number value. */
    "number"(number_: number): ScalarLiteral {
        return { kind: "number", number: number_ };
    },

    /** Bigint value. */
    "bigint"(bigint_: bigint): ScalarLiteral {
        return { kind: "bigint", bigint: bigint_ };
    },

    /** String value. */
    "string"(string_: StringId): ScalarLiteral {
        return { kind: "string", string: string_ };
    },

    /** Regex string value. */
    regexString(content: StringId, flags: StringId | undefined): ScalarLiteral {
        return { kind: "regexString", content, flags };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: ScalarLiteral): void {
        encodeScalarLiteral(writer, value);
    },

    /** Decode one ScalarLiteral. */
    decode(reader: BinaryReader): ScalarLiteral {
        return decodeScalarLiteral(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ScalarLiteral): Json {
        return toJsonScalarLiteral(value);
    },

    /** Return one ScalarLiteral from one JSON value. */
    fromJson(value: Json): ScalarLiteral {
        return fromJsonScalarLiteral(value);
    },
};

/** Encode one ScalarLiteral. */
export function encodeScalarLiteral(writer: BinaryWriter, value: ScalarLiteral): void {
    switch (value.kind) {
        case "null":
            writer.writeUnsigned(0);
            return;
        case "undefined":
            writer.writeUnsigned(1);
            return;
        case "boolean":
            writer.writeUnsigned(2);
            writer.writeBool(value.boolean);
            return;
        case "number":
            writer.writeUnsigned(3);
            writer.writeF64(value.number);
            return;
        case "bigint":
            writer.writeUnsigned(4);
            writer.writeSigned(value.bigint);
            return;
        case "string":
            writer.writeUnsigned(5);
            encodeStringId(writer, value.string);
            return;
        case "regexString":
            writer.writeUnsigned(6);
            encodeStringId(writer, value.content);
            writer.writeOption(value.flags, (value1) => {
                encodeStringId(writer, value1);
            });
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one ScalarLiteral. */
export function decodeScalarLiteral(reader: BinaryReader): ScalarLiteral {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            return { kind: "null" };
        }
        case 1: {
            return { kind: "undefined" };
        }
        case 2: {
            const boolean_ = reader.readBool();

            return { kind: "boolean", boolean: boolean_ };
        }
        case 3: {
            const number_ = reader.readF64();

            return { kind: "number", number: number_ };
        }
        case 4: {
            const bigint_ = reader.readSigned();

            return { kind: "bigint", bigint: bigint_ };
        }
        case 5: {
            const string_ = decodeStringId(reader);

            return { kind: "string", string: string_ };
        }
        case 6: {
            const content = decodeStringId(reader);
            const flags = reader.readOption(() => decodeStringId(reader));

            return {
                kind: "regexString",
                content,
                ...(flags === undefined ? {} : { flags }),
            };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one ScalarLiteral. */
export function toJsonScalarLiteral(value: ScalarLiteral): Json {
    switch (value.kind) {
        case "null":
            return {
                kind: "null",
            };
        case "undefined":
            return {
                kind: "undefined",
            };
        case "boolean":
            return {
                kind: "boolean",
                boolean: value.boolean,
            };
        case "number":
            return {
                kind: "number",
                number: value.number,
            };
        case "bigint":
            return {
                kind: "bigint",
                bigint: value.bigint.toString(),
            };
        case "string":
            return {
                kind: "string",
                string: toJsonStringId(value.string),
            };
        case "regexString":
            return {
                kind: "regexString",
                content: toJsonStringId(value.content),
                ...(value.flags === undefined ? {} : { flags: toJsonStringId(value.flags) }),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one ScalarLiteral from one JSON value. */
export function fromJsonScalarLiteral(value: Json): ScalarLiteral {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "null":
            return {
                kind,
            };
        case "undefined":
            return {
                kind,
            };
        case "boolean":
            return {
                kind,
                boolean: jsonBool(jsonField(object, "boolean")),
            };
        case "number":
            return {
                kind,
                number: jsonNumber(jsonField(object, "number")),
            };
        case "bigint":
            return {
                kind,
                bigint: jsonBigint(jsonField(object, "bigint")),
            };
        case "string":
            return {
                kind,
                string: fromJsonStringId(jsonField(object, "string")),
            };
        case "regexString":
            return {
                kind,
                content: fromJsonStringId(jsonField(object, "content")),
                flags: jsonOptional(object, "flags", (value) => fromJsonStringId(value)),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** A TemplateLiteral is literal template value. */
export type TemplateLiteral =
    /** Template string value. */
    | {
          readonly kind: "string";
          readonly template: StringId;
      }
    /** Tagged template literal value. */
    | {
          readonly kind: "taggedString";
          readonly tag: Path;
          readonly template: StringId;
      }
    /** Interpolated template literal value. */
    | {
          readonly kind: "interpolatedString";
          readonly template: ReadonlyArray<StringId>;
          readonly expressions: ReadonlyArray<LocalNodeId>;
      }
    /** Tagged interpolated template literal value. */
    | {
          readonly kind: "taggedInterpolatedString";
          readonly tag: Path;
          readonly template: ReadonlyArray<StringId>;
          readonly expressions: ReadonlyArray<LocalNodeId>;
      }
;

export const TemplateLiteral = {
    /** Template string value. */
    "string"(template: StringId): TemplateLiteral {
        return { kind: "string", template };
    },

    /** Tagged template literal value. */
    taggedString(tag: Path, template: StringId): TemplateLiteral {
        return { kind: "taggedString", tag, template };
    },

    /** Interpolated template literal value. */
    interpolatedString(template: ReadonlyArray<StringId>, expressions: ReadonlyArray<LocalNodeId>): TemplateLiteral {
        return { kind: "interpolatedString", template, expressions };
    },

    /** Tagged interpolated template literal value. */
    taggedInterpolatedString(tag: Path, template: ReadonlyArray<StringId>, expressions: ReadonlyArray<LocalNodeId>): TemplateLiteral {
        return { kind: "taggedInterpolatedString", tag, template, expressions };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: TemplateLiteral): void {
        encodeTemplateLiteral(writer, value);
    },

    /** Decode one TemplateLiteral. */
    decode(reader: BinaryReader): TemplateLiteral {
        return decodeTemplateLiteral(reader);
    },

    /** Return this value as JSON. */
    toJson(value: TemplateLiteral): Json {
        return toJsonTemplateLiteral(value);
    },

    /** Return one TemplateLiteral from one JSON value. */
    fromJson(value: Json): TemplateLiteral {
        return fromJsonTemplateLiteral(value);
    },
};

/** Encode one TemplateLiteral. */
export function encodeTemplateLiteral(writer: BinaryWriter, value: TemplateLiteral): void {
    switch (value.kind) {
        case "string":
            writer.writeUnsigned(0);
            encodeStringId(writer, value.template);
            return;
        case "taggedString":
            writer.writeUnsigned(1);
            encodePath(writer, value.tag);
            encodeStringId(writer, value.template);
            return;
        case "interpolatedString":
            writer.writeUnsigned(2);
            writer.writeUnsigned(value.template.length);
            for (const item0 of value.template) {
                encodeStringId(writer, item0);
            }
            writer.writeUnsigned(value.expressions.length);
            for (const item1 of value.expressions) {
                encodeLocalNodeId(writer, item1);
            }
            return;
        case "taggedInterpolatedString":
            writer.writeUnsigned(3);
            encodePath(writer, value.tag);
            writer.writeUnsigned(value.template.length);
            for (const item1 of value.template) {
                encodeStringId(writer, item1);
            }
            writer.writeUnsigned(value.expressions.length);
            for (const item2 of value.expressions) {
                encodeLocalNodeId(writer, item2);
            }
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one TemplateLiteral. */
export function decodeTemplateLiteral(reader: BinaryReader): TemplateLiteral {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const template = decodeStringId(reader);

            return {
                kind: "string",
                template,
            };
        }
        case 1: {
            const tag = decodePath(reader);
            const template = decodeStringId(reader);

            return {
                kind: "taggedString",
                tag,
                template,
            };
        }
        case 2: {
            const template = (() => { const length0 = reader.readNumber(); const items0: Array<StringId> = []; for (let index = 0; index < length0; index += 1) { items0.push(decodeStringId(reader)); } return items0; })();
            const expressions = (() => { const length1 = reader.readNumber(); const items1: Array<LocalNodeId> = []; for (let index = 0; index < length1; index += 1) { items1.push(decodeLocalNodeId(reader)); } return items1; })();

            return {
                kind: "interpolatedString",
                template,
                expressions,
            };
        }
        case 3: {
            const tag = decodePath(reader);
            const template = (() => { const length1 = reader.readNumber(); const items1: Array<StringId> = []; for (let index = 0; index < length1; index += 1) { items1.push(decodeStringId(reader)); } return items1; })();
            const expressions = (() => { const length2 = reader.readNumber(); const items2: Array<LocalNodeId> = []; for (let index = 0; index < length2; index += 1) { items2.push(decodeLocalNodeId(reader)); } return items2; })();

            return {
                kind: "taggedInterpolatedString",
                tag,
                template,
                expressions,
            };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one TemplateLiteral. */
export function toJsonTemplateLiteral(value: TemplateLiteral): Json {
    switch (value.kind) {
        case "string":
            return {
                kind: "string",
                template: toJsonStringId(value.template),
            };
        case "taggedString":
            return {
                kind: "taggedString",
                tag: toJsonPath(value.tag),
                template: toJsonStringId(value.template),
            };
        case "interpolatedString":
            return {
                kind: "interpolatedString",
                template: value.template.map((item0) => toJsonStringId(item0)),
                expressions: value.expressions.map((item0) => toJsonLocalNodeId(item0)),
            };
        case "taggedInterpolatedString":
            return {
                kind: "taggedInterpolatedString",
                tag: toJsonPath(value.tag),
                template: value.template.map((item0) => toJsonStringId(item0)),
                expressions: value.expressions.map((item0) => toJsonLocalNodeId(item0)),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one TemplateLiteral from one JSON value. */
export function fromJsonTemplateLiteral(value: Json): TemplateLiteral {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "string":
            return {
                kind,
                template: fromJsonStringId(jsonField(object, "template")),
            };
        case "taggedString":
            return {
                kind,
                tag: fromJsonPath(jsonField(object, "tag")),
                template: fromJsonStringId(jsonField(object, "template")),
            };
        case "interpolatedString":
            return {
                kind,
                template: jsonArray(jsonField(object, "template")).map((item0) => fromJsonStringId(item0)),
                expressions: jsonArray(jsonField(object, "expressions")).map((item0) => fromJsonLocalNodeId(item0)),
            };
        case "taggedInterpolatedString":
            return {
                kind,
                tag: fromJsonPath(jsonField(object, "tag")),
                template: jsonArray(jsonField(object, "template")).map((item0) => fromJsonStringId(item0)),
                expressions: jsonArray(jsonField(object, "expressions")).map((item0) => fromJsonLocalNodeId(item0)),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}
