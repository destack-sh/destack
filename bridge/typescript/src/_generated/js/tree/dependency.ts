/* generated by destack_bridge_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, jsonField, jsonObject, jsonOptional, jsonString } from "../../../protocol/serde.js";
import type { StringId } from "../../core/string.js";
import type { Name } from "./key.js";
import type { LocalNodeId } from "./node.js";
import { decodeStringId, encodeStringId, fromJsonStringId, toJsonStringId } from "../../core/string.js";
import { decodeName, encodeName, fromJsonName, toJsonName } from "./key.js";
import { decodeLocalNodeId, encodeLocalNodeId, fromJsonLocalNodeId, toJsonLocalNodeId } from "./node.js";

/** The source form of one dependency item. */
export type DependencyForm = "type" | "plain";

export const DependencyForm = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: DependencyForm): void {
        encodeDependencyForm(writer, value);
    },

    /** Decode one DependencyForm. */
    decode(reader: BinaryReader): DependencyForm {
        return decodeDependencyForm(reader);
    },

    /** Return this value as JSON. */
    toJson(value: DependencyForm): Json {
        return toJsonDependencyForm(value);
    },

    /** Return one DependencyForm from one JSON value. */
    fromJson(value: Json): DependencyForm {
        return fromJsonDependencyForm(value);
    },
};

/** Encode one DependencyForm. */
export function encodeDependencyForm(writer: BinaryWriter, value: DependencyForm): void {
    switch (value) {
        case "type":
            writer.writeUnsigned(0);
            return;
        case "plain":
            writer.writeUnsigned(1);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one DependencyForm. */
export function decodeDependencyForm(reader: BinaryReader): DependencyForm {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "type";
        case 1:
            return "plain";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one DependencyForm. */
export function toJsonDependencyForm(value: DependencyForm): Json {
    return value;
}

/** Return one DependencyForm from one JSON value. */
export function fromJsonDependencyForm(value: Json): DependencyForm {
    const variant = jsonString(value);

    switch (variant) {
        case "type":
            return "type";
        case "plain":
            return "plain";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** How one dependency item binds into the local module or export surface. */
export type DependencyBinding = "named" | "default" | "namespace";

export const DependencyBinding = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: DependencyBinding): void {
        encodeDependencyBinding(writer, value);
    },

    /** Decode one DependencyBinding. */
    decode(reader: BinaryReader): DependencyBinding {
        return decodeDependencyBinding(reader);
    },

    /** Return this value as JSON. */
    toJson(value: DependencyBinding): Json {
        return toJsonDependencyBinding(value);
    },

    /** Return one DependencyBinding from one JSON value. */
    fromJson(value: Json): DependencyBinding {
        return fromJsonDependencyBinding(value);
    },
};

/** Encode one DependencyBinding. */
export function encodeDependencyBinding(writer: BinaryWriter, value: DependencyBinding): void {
    switch (value) {
        case "named":
            writer.writeUnsigned(0);
            return;
        case "default":
            writer.writeUnsigned(1);
            return;
        case "namespace":
            writer.writeUnsigned(2);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one DependencyBinding. */
export function decodeDependencyBinding(reader: BinaryReader): DependencyBinding {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "named";
        case 1:
            return "default";
        case 2:
            return "namespace";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one DependencyBinding. */
export function toJsonDependencyBinding(value: DependencyBinding): Json {
    return value;
}

/** Return one DependencyBinding from one JSON value. */
export function fromJsonDependencyBinding(value: Json): DependencyBinding {
    const variant = jsonString(value);

    switch (variant) {
        case "named":
            return "named";
        case "default":
            return "default";
        case "namespace":
            return "namespace";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** One dependency binding in an import or export clause. */
export type DependencyItem = {
    /** How the item binds. */
    readonly binding: DependencyBinding;
    /** The source form of the item, when specified. */
    readonly form?: DependencyForm;
    /** The name of the item (like `foo` in `foo as bar`). */
    readonly name?: Name;
    /** The alias to use for the item (like `bar` in `foo as bar`). */
    readonly alias?: StringId;
    /** The value of the item (for `export = foo` style exports). */
    readonly value?: LocalNodeId;
};

export const DependencyItem = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: DependencyItem): void {
        encodeDependencyItem(writer, value);
    },

    /** Decode one DependencyItem. */
    decode(reader: BinaryReader): DependencyItem {
        return decodeDependencyItem(reader);
    },

    /** Return this value as JSON. */
    toJson(value: DependencyItem): Json {
        return toJsonDependencyItem(value);
    },

    /** Return one DependencyItem from one JSON value. */
    fromJson(value: Json): DependencyItem {
        return fromJsonDependencyItem(value);
    },
};

/** Encode one DependencyItem. */
export function encodeDependencyItem(writer: BinaryWriter, value: DependencyItem): void {
    encodeDependencyBinding(writer, value.binding);
    writer.writeOption(value.form, (value1) => {
        encodeDependencyForm(writer, value1);
    });
    writer.writeOption(value.name, (value2) => {
        encodeName(writer, value2);
    });
    writer.writeOption(value.alias, (value3) => {
        encodeStringId(writer, value3);
    });
    writer.writeOption(value.value, (value4) => {
        encodeLocalNodeId(writer, value4);
    });
}

/** Decode one DependencyItem. */
export function decodeDependencyItem(reader: BinaryReader): DependencyItem {
    const binding = decodeDependencyBinding(reader);
    const form = reader.readOption(() => decodeDependencyForm(reader));
    const name = reader.readOption(() => decodeName(reader));
    const alias = reader.readOption(() => decodeStringId(reader));
    const value = reader.readOption(() => decodeLocalNodeId(reader));

    return {
        binding,
        ...(form === undefined ? {} : { form }),
        ...(name === undefined ? {} : { name }),
        ...(alias === undefined ? {} : { alias }),
        ...(value === undefined ? {} : { value }),
    };
}

/** Return one JSON value for one DependencyItem. */
export function toJsonDependencyItem(value: DependencyItem): Json {
    return {
        binding: toJsonDependencyBinding(value.binding),
        ...(value.form === undefined ? {} : { form: toJsonDependencyForm(value.form) }),
        ...(value.name === undefined ? {} : { name: toJsonName(value.name) }),
        ...(value.alias === undefined ? {} : { alias: toJsonStringId(value.alias) }),
        ...(value.value === undefined ? {} : { value: toJsonLocalNodeId(value.value) }),
    };
}

/** Return one DependencyItem from one JSON value. */
export function fromJsonDependencyItem(value: Json): DependencyItem {
    const object = jsonObject(value);

    return {
        binding: fromJsonDependencyBinding(jsonField(object, "binding")),
        form: jsonOptional(object, "form", (value) => fromJsonDependencyForm(value)),
        name: jsonOptional(object, "name", (value) => fromJsonName(value)),
        alias: jsonOptional(object, "alias", (value) => fromJsonStringId(value)),
        value: jsonOptional(object, "value", (value) => fromJsonLocalNodeId(value)),
    };
}
