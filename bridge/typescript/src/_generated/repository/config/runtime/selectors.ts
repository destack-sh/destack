/* generated by destack_bridge_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, compareBytes, jsonArray, jsonField, jsonObject, jsonOptional, jsonString, nestedBytes } from "../../../../protocol/serde.js";

/** Runtime identity selector for worker and runtime scopes. */
export type RuntimeIdentitySelector = {
    /** Name selector for one runtime or one worker. */
    readonly name?: string;
    /** Label selector for one runtime or one worker. */
    readonly labels?: RuntimeLabelSelector;
};

export const RuntimeIdentitySelector = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: RuntimeIdentitySelector): void {
        encodeRuntimeIdentitySelector(writer, value);
    },

    /** Decode one RuntimeIdentitySelector. */
    decode(reader: BinaryReader): RuntimeIdentitySelector {
        return decodeRuntimeIdentitySelector(reader);
    },

    /** Return this value as JSON. */
    toJson(value: RuntimeIdentitySelector): Json {
        return toJsonRuntimeIdentitySelector(value);
    },

    /** Return one RuntimeIdentitySelector from one JSON value. */
    fromJson(value: Json): RuntimeIdentitySelector {
        return fromJsonRuntimeIdentitySelector(value);
    },
};

/** Encode one RuntimeIdentitySelector. */
export function encodeRuntimeIdentitySelector(writer: BinaryWriter, value: RuntimeIdentitySelector): void {
    writer.writeOption(value.name, (value0) => {
        writer.writeString(value0);
    });
    writer.writeOption(value.labels, (value1) => {
        encodeRuntimeLabelSelector(writer, value1);
    });
}

/** Decode one RuntimeIdentitySelector. */
export function decodeRuntimeIdentitySelector(reader: BinaryReader): RuntimeIdentitySelector {
    const name = reader.readOption(() => reader.readString());
    const labels = reader.readOption(() => decodeRuntimeLabelSelector(reader));

    return {
        ...(name === undefined ? {} : { name }),
        ...(labels === undefined ? {} : { labels }),
    };
}

/** Return one JSON value for one RuntimeIdentitySelector. */
export function toJsonRuntimeIdentitySelector(value: RuntimeIdentitySelector): Json {
    return {
        ...(value.name === undefined ? {} : { name: value.name }),
        ...(value.labels === undefined ? {} : { labels: toJsonRuntimeLabelSelector(value.labels) }),
    };
}

/** Return one RuntimeIdentitySelector from one JSON value. */
export function fromJsonRuntimeIdentitySelector(value: Json): RuntimeIdentitySelector {
    const object = jsonObject(value);

    return {
        name: jsonOptional(object, "name", (value) => jsonString(value)),
        labels: jsonOptional(object, "labels", (value) => fromJsonRuntimeLabelSelector(value)),
    };
}

/** Kubernetes-style label selector for runtime identity. */
export type RuntimeLabelSelector = {
    /** Exact-match labels that must all be present. */
    readonly matchLabels: Readonly<Record<string, string>>;
    /** Additional set-based label requirements. */
    readonly matchExpressions: ReadonlyArray<RuntimeLabelRequirement>;
};

export const RuntimeLabelSelector = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: RuntimeLabelSelector): void {
        encodeRuntimeLabelSelector(writer, value);
    },

    /** Decode one RuntimeLabelSelector. */
    decode(reader: BinaryReader): RuntimeLabelSelector {
        return decodeRuntimeLabelSelector(reader);
    },

    /** Return this value as JSON. */
    toJson(value: RuntimeLabelSelector): Json {
        return toJsonRuntimeLabelSelector(value);
    },

    /** Return one RuntimeLabelSelector from one JSON value. */
    fromJson(value: Json): RuntimeLabelSelector {
        return fromJsonRuntimeLabelSelector(value);
    },
};

/** Encode one RuntimeLabelSelector. */
export function encodeRuntimeLabelSelector(writer: BinaryWriter, value: RuntimeLabelSelector): void {
    const entries0 = Object.entries(value.matchLabels).map(([key0, item0]) => {
        const keyBytes = nestedBytes((writer) => {
            writer.writeString(key0);
        });
        return { key0, item0, keyBytes };
    });
    entries0.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries0.length);
    for (const entry0 of entries0) {
        writer.writeString(entry0.key0);
        writer.writeString(entry0.item0);
    }
    writer.writeUnsigned(value.matchExpressions.length);
    for (const item1 of value.matchExpressions) {
        encodeRuntimeLabelRequirement(writer, item1);
    }
}

/** Decode one RuntimeLabelSelector. */
export function decodeRuntimeLabelSelector(reader: BinaryReader): RuntimeLabelSelector {
    const matchLabels = (() => { const length0 = reader.readNumber(); const items0: Record<string, string> = {}; for (let index = 0; index < length0; index += 1) { const key = reader.readString(); items0[key] = reader.readString(); } return items0; })();
    const matchExpressions = (() => { const length1 = reader.readNumber(); const items1: Array<RuntimeLabelRequirement> = []; for (let index = 0; index < length1; index += 1) { items1.push(decodeRuntimeLabelRequirement(reader)); } return items1; })();

    return {
        matchLabels,
        matchExpressions,
    };
}

/** Return one JSON value for one RuntimeLabelSelector. */
export function toJsonRuntimeLabelSelector(value: RuntimeLabelSelector): Json {
    return {
        matchLabels: Object.fromEntries(Object.entries(value.matchLabels).map(([key0, item0]) => [key0, item0] as const)),
        matchExpressions: value.matchExpressions.map((item0) => toJsonRuntimeLabelRequirement(item0)),
    };
}

/** Return one RuntimeLabelSelector from one JSON value. */
export function fromJsonRuntimeLabelSelector(value: Json): RuntimeLabelSelector {
    const object = jsonObject(value);

    return {
        matchLabels: Object.fromEntries(Object.entries(jsonObject(jsonField(object, "matchLabels"))).map(([key0, item0]) => [key0, jsonString(item0)] as const)),
        matchExpressions: jsonArray(jsonField(object, "matchExpressions")).map((item0) => fromJsonRuntimeLabelRequirement(item0)),
    };
}

/** One label requirement clause for runtime identity selectors. */
export type RuntimeLabelRequirement = {
    /** Label key to evaluate. */
    readonly key: string;
    /** Label requirement operator. */
    readonly operator: RuntimeLabelOperator;
    /** Label values for set-based operators. */
    readonly values: ReadonlyArray<string>;
};

export const RuntimeLabelRequirement = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: RuntimeLabelRequirement): void {
        encodeRuntimeLabelRequirement(writer, value);
    },

    /** Decode one RuntimeLabelRequirement. */
    decode(reader: BinaryReader): RuntimeLabelRequirement {
        return decodeRuntimeLabelRequirement(reader);
    },

    /** Return this value as JSON. */
    toJson(value: RuntimeLabelRequirement): Json {
        return toJsonRuntimeLabelRequirement(value);
    },

    /** Return one RuntimeLabelRequirement from one JSON value. */
    fromJson(value: Json): RuntimeLabelRequirement {
        return fromJsonRuntimeLabelRequirement(value);
    },
};

/** Encode one RuntimeLabelRequirement. */
export function encodeRuntimeLabelRequirement(writer: BinaryWriter, value: RuntimeLabelRequirement): void {
    writer.writeString(value.key);
    encodeRuntimeLabelOperator(writer, value.operator);
    writer.writeUnsigned(value.values.length);
    for (const item2 of value.values) {
        writer.writeString(item2);
    }
}

/** Decode one RuntimeLabelRequirement. */
export function decodeRuntimeLabelRequirement(reader: BinaryReader): RuntimeLabelRequirement {
    const key = reader.readString();
    const operator = decodeRuntimeLabelOperator(reader);
    const values = (() => { const length2 = reader.readNumber(); const items2: Array<string> = []; for (let index = 0; index < length2; index += 1) { items2.push(reader.readString()); } return items2; })();

    return {
        key,
        operator,
        values,
    };
}

/** Return one JSON value for one RuntimeLabelRequirement. */
export function toJsonRuntimeLabelRequirement(value: RuntimeLabelRequirement): Json {
    return {
        key: value.key,
        operator: toJsonRuntimeLabelOperator(value.operator),
        values: value.values.map((item0) => item0),
    };
}

/** Return one RuntimeLabelRequirement from one JSON value. */
export function fromJsonRuntimeLabelRequirement(value: Json): RuntimeLabelRequirement {
    const object = jsonObject(value);

    return {
        key: jsonString(jsonField(object, "key")),
        operator: fromJsonRuntimeLabelOperator(jsonField(object, "operator")),
        values: jsonArray(jsonField(object, "values")).map((item0) => jsonString(item0)),
    };
}

/** Label selection operator for runtime identity selectors. */
export type RuntimeLabelOperator = "in" | "notIn" | "exists" | "doesNotExist";

export const RuntimeLabelOperator = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: RuntimeLabelOperator): void {
        encodeRuntimeLabelOperator(writer, value);
    },

    /** Decode one RuntimeLabelOperator. */
    decode(reader: BinaryReader): RuntimeLabelOperator {
        return decodeRuntimeLabelOperator(reader);
    },

    /** Return this value as JSON. */
    toJson(value: RuntimeLabelOperator): Json {
        return toJsonRuntimeLabelOperator(value);
    },

    /** Return one RuntimeLabelOperator from one JSON value. */
    fromJson(value: Json): RuntimeLabelOperator {
        return fromJsonRuntimeLabelOperator(value);
    },
};

/** Encode one RuntimeLabelOperator. */
export function encodeRuntimeLabelOperator(writer: BinaryWriter, value: RuntimeLabelOperator): void {
    switch (value) {
        case "in":
            writer.writeUnsigned(0);
            return;
        case "notIn":
            writer.writeUnsigned(1);
            return;
        case "exists":
            writer.writeUnsigned(2);
            return;
        case "doesNotExist":
            writer.writeUnsigned(3);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one RuntimeLabelOperator. */
export function decodeRuntimeLabelOperator(reader: BinaryReader): RuntimeLabelOperator {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "in";
        case 1:
            return "notIn";
        case 2:
            return "exists";
        case 3:
            return "doesNotExist";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one RuntimeLabelOperator. */
export function toJsonRuntimeLabelOperator(value: RuntimeLabelOperator): Json {
    return value;
}

/** Return one RuntimeLabelOperator from one JSON value. */
export function fromJsonRuntimeLabelOperator(value: Json): RuntimeLabelOperator {
    const variant = jsonString(value);

    switch (variant) {
        case "in":
            return "in";
        case "notIn":
            return "notIn";
        case "exists":
            return "exists";
        case "doesNotExist":
            return "doesNotExist";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}
