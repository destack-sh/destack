/* generated by destack_bridge_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, jsonBigint, jsonField, jsonObject, jsonOptional } from "../../../../protocol/serde.js";

/** Runtime randomness configuration. */
export type RandomOptions = {
    /** Seed for deterministic randomness streams. */
    readonly seed?: bigint;
};

export const RandomOptions = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: RandomOptions): void {
        encodeRandomOptions(writer, value);
    },

    /** Decode one RandomOptions. */
    decode(reader: BinaryReader): RandomOptions {
        return decodeRandomOptions(reader);
    },

    /** Return this value as JSON. */
    toJson(value: RandomOptions): Json {
        return toJsonRandomOptions(value);
    },

    /** Return one RandomOptions from one JSON value. */
    fromJson(value: Json): RandomOptions {
        return fromJsonRandomOptions(value);
    },
};

/** Encode one RandomOptions. */
export function encodeRandomOptions(writer: BinaryWriter, value: RandomOptions): void {
    writer.writeOption(value.seed, (value0) => {
        writer.writeUnsigned(value0);
    });
}

/** Decode one RandomOptions. */
export function decodeRandomOptions(reader: BinaryReader): RandomOptions {
    const seed = reader.readOption(() => reader.readUnsigned());

    return {
        ...(seed === undefined ? {} : { seed }),
    };
}

/** Return one JSON value for one RandomOptions. */
export function toJsonRandomOptions(value: RandomOptions): Json {
    return {
        ...(value.seed === undefined ? {} : { seed: value.seed.toString() }),
    };
}

/** Return one RandomOptions from one JSON value. */
export function fromJsonRandomOptions(value: Json): RandomOptions {
    const object = jsonObject(value);

    return {
        seed: jsonOptional(object, "seed", (value) => jsonBigint(value)),
    };
}
