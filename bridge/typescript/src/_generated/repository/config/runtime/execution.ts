/* generated by destack_bridge_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, jsonString } from "../../../../protocol/serde.js";

/** Execution mode for runtime scheduling and replay. */
export type ExecutionMode = "strict" | "fast" | "record" | "replay";

export const ExecutionMode = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ExecutionMode): void {
        encodeExecutionMode(writer, value);
    },

    /** Decode one ExecutionMode. */
    decode(reader: BinaryReader): ExecutionMode {
        return decodeExecutionMode(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ExecutionMode): Json {
        return toJsonExecutionMode(value);
    },

    /** Return one ExecutionMode from one JSON value. */
    fromJson(value: Json): ExecutionMode {
        return fromJsonExecutionMode(value);
    },
};

/** Encode one ExecutionMode. */
export function encodeExecutionMode(writer: BinaryWriter, value: ExecutionMode): void {
    switch (value) {
        case "strict":
            writer.writeUnsigned(0);
            return;
        case "fast":
            writer.writeUnsigned(1);
            return;
        case "record":
            writer.writeUnsigned(2);
            return;
        case "replay":
            writer.writeUnsigned(3);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one ExecutionMode. */
export function decodeExecutionMode(reader: BinaryReader): ExecutionMode {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "strict";
        case 1:
            return "fast";
        case 2:
            return "record";
        case 3:
            return "replay";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one ExecutionMode. */
export function toJsonExecutionMode(value: ExecutionMode): Json {
    return value;
}

/** Return one ExecutionMode from one JSON value. */
export function fromJsonExecutionMode(value: Json): ExecutionMode {
    const variant = jsonString(value);

    switch (variant) {
        case "strict":
            return "strict";
        case "fast":
            return "fast";
        case "record":
            return "record";
        case "replay":
            return "replay";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** Replay payload selection for record/replay. */
export type ReplayPayloadMode = "resultsOnly" | "argumentsAndResults";

export const ReplayPayloadMode = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ReplayPayloadMode): void {
        encodeReplayPayloadMode(writer, value);
    },

    /** Decode one ReplayPayloadMode. */
    decode(reader: BinaryReader): ReplayPayloadMode {
        return decodeReplayPayloadMode(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ReplayPayloadMode): Json {
        return toJsonReplayPayloadMode(value);
    },

    /** Return one ReplayPayloadMode from one JSON value. */
    fromJson(value: Json): ReplayPayloadMode {
        return fromJsonReplayPayloadMode(value);
    },
};

/** Encode one ReplayPayloadMode. */
export function encodeReplayPayloadMode(writer: BinaryWriter, value: ReplayPayloadMode): void {
    switch (value) {
        case "resultsOnly":
            writer.writeUnsigned(0);
            return;
        case "argumentsAndResults":
            writer.writeUnsigned(1);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one ReplayPayloadMode. */
export function decodeReplayPayloadMode(reader: BinaryReader): ReplayPayloadMode {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "resultsOnly";
        case 1:
            return "argumentsAndResults";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one ReplayPayloadMode. */
export function toJsonReplayPayloadMode(value: ReplayPayloadMode): Json {
    return value;
}

/** Return one ReplayPayloadMode from one JSON value. */
export function fromJsonReplayPayloadMode(value: Json): ReplayPayloadMode {
    const variant = jsonString(value);

    switch (variant) {
        case "resultsOnly":
            return "resultsOnly";
        case "argumentsAndResults":
            return "argumentsAndResults";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}
