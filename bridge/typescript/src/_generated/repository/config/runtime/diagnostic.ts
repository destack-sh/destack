/* generated by destack_bridge_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, jsonBigint, jsonField, jsonObject, jsonOptional, jsonString } from "../../../../protocol/serde.js";

/** Runtime diagnostics configuration. */
export type RuntimeDiagnosticOptions = {
    /** Minimum diagnostic level recorded by the runtime. */
    readonly level: RuntimeDiagnosticLevel;
    /** Maximum number of diagnostic entries retained in the runtime ring buffer. */
    readonly capacity?: bigint;
};

export const RuntimeDiagnosticOptions = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: RuntimeDiagnosticOptions): void {
        encodeRuntimeDiagnosticOptions(writer, value);
    },

    /** Decode one RuntimeDiagnosticOptions. */
    decode(reader: BinaryReader): RuntimeDiagnosticOptions {
        return decodeRuntimeDiagnosticOptions(reader);
    },

    /** Return this value as JSON. */
    toJson(value: RuntimeDiagnosticOptions): Json {
        return toJsonRuntimeDiagnosticOptions(value);
    },

    /** Return one RuntimeDiagnosticOptions from one JSON value. */
    fromJson(value: Json): RuntimeDiagnosticOptions {
        return fromJsonRuntimeDiagnosticOptions(value);
    },
};

/** Encode one RuntimeDiagnosticOptions. */
export function encodeRuntimeDiagnosticOptions(writer: BinaryWriter, value: RuntimeDiagnosticOptions): void {
    encodeRuntimeDiagnosticLevel(writer, value.level);
    writer.writeOption(value.capacity, (value1) => {
        writer.writeUnsigned(value1);
    });
}

/** Decode one RuntimeDiagnosticOptions. */
export function decodeRuntimeDiagnosticOptions(reader: BinaryReader): RuntimeDiagnosticOptions {
    const level = decodeRuntimeDiagnosticLevel(reader);
    const capacity = reader.readOption(() => reader.readUnsigned());

    return {
        level,
        ...(capacity === undefined ? {} : { capacity }),
    };
}

/** Return one JSON value for one RuntimeDiagnosticOptions. */
export function toJsonRuntimeDiagnosticOptions(value: RuntimeDiagnosticOptions): Json {
    return {
        level: toJsonRuntimeDiagnosticLevel(value.level),
        ...(value.capacity === undefined ? {} : { capacity: value.capacity.toString() }),
    };
}

/** Return one RuntimeDiagnosticOptions from one JSON value. */
export function fromJsonRuntimeDiagnosticOptions(value: Json): RuntimeDiagnosticOptions {
    const object = jsonObject(value);

    return {
        level: fromJsonRuntimeDiagnosticLevel(jsonField(object, "level")),
        capacity: jsonOptional(object, "capacity", (value) => jsonBigint(value)),
    };
}

/** Runtime diagnostic verbosity. */
export type RuntimeDiagnosticLevel = "off" | "error" | "warn" | "info" | "debug" | "trace";

export const RuntimeDiagnosticLevel = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: RuntimeDiagnosticLevel): void {
        encodeRuntimeDiagnosticLevel(writer, value);
    },

    /** Decode one RuntimeDiagnosticLevel. */
    decode(reader: BinaryReader): RuntimeDiagnosticLevel {
        return decodeRuntimeDiagnosticLevel(reader);
    },

    /** Return this value as JSON. */
    toJson(value: RuntimeDiagnosticLevel): Json {
        return toJsonRuntimeDiagnosticLevel(value);
    },

    /** Return one RuntimeDiagnosticLevel from one JSON value. */
    fromJson(value: Json): RuntimeDiagnosticLevel {
        return fromJsonRuntimeDiagnosticLevel(value);
    },
};

/** Encode one RuntimeDiagnosticLevel. */
export function encodeRuntimeDiagnosticLevel(writer: BinaryWriter, value: RuntimeDiagnosticLevel): void {
    switch (value) {
        case "off":
            writer.writeUnsigned(0);
            return;
        case "error":
            writer.writeUnsigned(1);
            return;
        case "warn":
            writer.writeUnsigned(2);
            return;
        case "info":
            writer.writeUnsigned(3);
            return;
        case "debug":
            writer.writeUnsigned(4);
            return;
        case "trace":
            writer.writeUnsigned(5);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one RuntimeDiagnosticLevel. */
export function decodeRuntimeDiagnosticLevel(reader: BinaryReader): RuntimeDiagnosticLevel {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "off";
        case 1:
            return "error";
        case 2:
            return "warn";
        case 3:
            return "info";
        case 4:
            return "debug";
        case 5:
            return "trace";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one RuntimeDiagnosticLevel. */
export function toJsonRuntimeDiagnosticLevel(value: RuntimeDiagnosticLevel): Json {
    return value;
}

/** Return one RuntimeDiagnosticLevel from one JSON value. */
export function fromJsonRuntimeDiagnosticLevel(value: Json): RuntimeDiagnosticLevel {
    const variant = jsonString(value);

    switch (variant) {
        case "off":
            return "off";
        case "error":
            return "error";
        case "warn":
            return "warn";
        case "info":
            return "info";
        case "debug":
            return "debug";
        case "trace":
            return "trace";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}
