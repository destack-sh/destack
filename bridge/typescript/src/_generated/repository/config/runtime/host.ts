/* generated by destack_bridge_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, jsonArray, jsonBool, jsonField, jsonObject, jsonOptional, jsonString } from "../../../../protocol/serde.js";

/** Runtime host module defaults. */
export type HostOptions = {
    /** Host poller defaults. */
    readonly poller: HostPollerOptions;
    /** Filesystem host defaults. */
    readonly fs: HostFsOptions;
    /** Network host defaults. */
    readonly net: HostNetOptions;
    /** Process host defaults. */
    readonly process: HostProcessOptions;
    /** Audio host defaults. */
    readonly audio: HostAudioOptions;
    /** Display host defaults. */
    readonly display: HostDisplayOptions;
    /** Input host defaults. */
    readonly input: HostInputOptions;
    /** GPU host defaults. */
    readonly gpu: HostGpuOptions;
    /** TLS host defaults. */
    readonly tls: HostTlsOptions;
    /** OS host defaults. */
    readonly os: HostOsOptions;
};

export const HostOptions = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: HostOptions): void {
        encodeHostOptions(writer, value);
    },

    /** Decode one HostOptions. */
    decode(reader: BinaryReader): HostOptions {
        return decodeHostOptions(reader);
    },

    /** Return this value as JSON. */
    toJson(value: HostOptions): Json {
        return toJsonHostOptions(value);
    },

    /** Return one HostOptions from one JSON value. */
    fromJson(value: Json): HostOptions {
        return fromJsonHostOptions(value);
    },
};

/** Encode one HostOptions. */
export function encodeHostOptions(writer: BinaryWriter, value: HostOptions): void {
    encodeHostPollerOptions(writer, value.poller);
    encodeHostFsOptions(writer, value.fs);
    encodeHostNetOptions(writer, value.net);
    encodeHostProcessOptions(writer, value.process);
    encodeHostAudioOptions(writer, value.audio);
    encodeHostDisplayOptions(writer, value.display);
    encodeHostInputOptions(writer, value.input);
    encodeHostGpuOptions(writer, value.gpu);
    encodeHostTlsOptions(writer, value.tls);
    encodeHostOsOptions(writer, value.os);
}

/** Decode one HostOptions. */
export function decodeHostOptions(reader: BinaryReader): HostOptions {
    const poller = decodeHostPollerOptions(reader);
    const fs = decodeHostFsOptions(reader);
    const net = decodeHostNetOptions(reader);
    const process = decodeHostProcessOptions(reader);
    const audio = decodeHostAudioOptions(reader);
    const display = decodeHostDisplayOptions(reader);
    const input = decodeHostInputOptions(reader);
    const gpu = decodeHostGpuOptions(reader);
    const tls = decodeHostTlsOptions(reader);
    const os = decodeHostOsOptions(reader);

    return {
        poller,
        fs,
        net,
        process,
        audio,
        display,
        input,
        gpu,
        tls,
        os,
    };
}

/** Return one JSON value for one HostOptions. */
export function toJsonHostOptions(value: HostOptions): Json {
    return {
        poller: toJsonHostPollerOptions(value.poller),
        fs: toJsonHostFsOptions(value.fs),
        net: toJsonHostNetOptions(value.net),
        process: toJsonHostProcessOptions(value.process),
        audio: toJsonHostAudioOptions(value.audio),
        display: toJsonHostDisplayOptions(value.display),
        input: toJsonHostInputOptions(value.input),
        gpu: toJsonHostGpuOptions(value.gpu),
        tls: toJsonHostTlsOptions(value.tls),
        os: toJsonHostOsOptions(value.os),
    };
}

/** Return one HostOptions from one JSON value. */
export function fromJsonHostOptions(value: Json): HostOptions {
    const object = jsonObject(value);

    return {
        poller: fromJsonHostPollerOptions(jsonField(object, "poller")),
        fs: fromJsonHostFsOptions(jsonField(object, "fs")),
        net: fromJsonHostNetOptions(jsonField(object, "net")),
        process: fromJsonHostProcessOptions(jsonField(object, "process")),
        audio: fromJsonHostAudioOptions(jsonField(object, "audio")),
        display: fromJsonHostDisplayOptions(jsonField(object, "display")),
        input: fromJsonHostInputOptions(jsonField(object, "input")),
        gpu: fromJsonHostGpuOptions(jsonField(object, "gpu")),
        tls: fromJsonHostTlsOptions(jsonField(object, "tls")),
        os: fromJsonHostOsOptions(jsonField(object, "os")),
    };
}

/** Runtime host-poller configuration. */
export type HostPollerOptions = {
    /** Host poller backend selection. */
    readonly backend: PollerBackend;
};

export const HostPollerOptions = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: HostPollerOptions): void {
        encodeHostPollerOptions(writer, value);
    },

    /** Decode one HostPollerOptions. */
    decode(reader: BinaryReader): HostPollerOptions {
        return decodeHostPollerOptions(reader);
    },

    /** Return this value as JSON. */
    toJson(value: HostPollerOptions): Json {
        return toJsonHostPollerOptions(value);
    },

    /** Return one HostPollerOptions from one JSON value. */
    fromJson(value: Json): HostPollerOptions {
        return fromJsonHostPollerOptions(value);
    },
};

/** Encode one HostPollerOptions. */
export function encodeHostPollerOptions(writer: BinaryWriter, value: HostPollerOptions): void {
    encodePollerBackend(writer, value.backend);
}

/** Decode one HostPollerOptions. */
export function decodeHostPollerOptions(reader: BinaryReader): HostPollerOptions {
    const backend = decodePollerBackend(reader);

    return {
        backend,
    };
}

/** Return one JSON value for one HostPollerOptions. */
export function toJsonHostPollerOptions(value: HostPollerOptions): Json {
    return {
        backend: toJsonPollerBackend(value.backend),
    };
}

/** Return one HostPollerOptions from one JSON value. */
export function fromJsonHostPollerOptions(value: Json): HostPollerOptions {
    const object = jsonObject(value);

    return {
        backend: fromJsonPollerBackend(jsonField(object, "backend")),
    };
}

/** Host poller backend selection. */
export type PollerBackend = "auto" | "ioUring" | "epoll" | "kqueue" | "poll" | "windows";

export const PollerBackend = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: PollerBackend): void {
        encodePollerBackend(writer, value);
    },

    /** Decode one PollerBackend. */
    decode(reader: BinaryReader): PollerBackend {
        return decodePollerBackend(reader);
    },

    /** Return this value as JSON. */
    toJson(value: PollerBackend): Json {
        return toJsonPollerBackend(value);
    },

    /** Return one PollerBackend from one JSON value. */
    fromJson(value: Json): PollerBackend {
        return fromJsonPollerBackend(value);
    },
};

/** Encode one PollerBackend. */
export function encodePollerBackend(writer: BinaryWriter, value: PollerBackend): void {
    switch (value) {
        case "auto":
            writer.writeUnsigned(0);
            return;
        case "ioUring":
            writer.writeUnsigned(1);
            return;
        case "epoll":
            writer.writeUnsigned(2);
            return;
        case "kqueue":
            writer.writeUnsigned(3);
            return;
        case "poll":
            writer.writeUnsigned(4);
            return;
        case "windows":
            writer.writeUnsigned(5);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one PollerBackend. */
export function decodePollerBackend(reader: BinaryReader): PollerBackend {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "auto";
        case 1:
            return "ioUring";
        case 2:
            return "epoll";
        case 3:
            return "kqueue";
        case 4:
            return "poll";
        case 5:
            return "windows";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one PollerBackend. */
export function toJsonPollerBackend(value: PollerBackend): Json {
    return value;
}

/** Return one PollerBackend from one JSON value. */
export function fromJsonPollerBackend(value: Json): PollerBackend {
    const variant = jsonString(value);

    switch (variant) {
        case "auto":
            return "auto";
        case "ioUring":
            return "ioUring";
        case "epoll":
            return "epoll";
        case "kqueue":
            return "kqueue";
        case "poll":
            return "poll";
        case "windows":
            return "windows";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** Filesystem host defaults. */
export type HostFsOptions = {
    /** Optional temporary directory override. */
    readonly temporaryDirectory?: string;
    /** Optional cache directory override. */
    readonly cacheDirectory?: string;
};

export const HostFsOptions = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: HostFsOptions): void {
        encodeHostFsOptions(writer, value);
    },

    /** Decode one HostFsOptions. */
    decode(reader: BinaryReader): HostFsOptions {
        return decodeHostFsOptions(reader);
    },

    /** Return this value as JSON. */
    toJson(value: HostFsOptions): Json {
        return toJsonHostFsOptions(value);
    },

    /** Return one HostFsOptions from one JSON value. */
    fromJson(value: Json): HostFsOptions {
        return fromJsonHostFsOptions(value);
    },
};

/** Encode one HostFsOptions. */
export function encodeHostFsOptions(writer: BinaryWriter, value: HostFsOptions): void {
    writer.writeOption(value.temporaryDirectory, (value0) => {
        writer.writeString(value0);
    });
    writer.writeOption(value.cacheDirectory, (value1) => {
        writer.writeString(value1);
    });
}

/** Decode one HostFsOptions. */
export function decodeHostFsOptions(reader: BinaryReader): HostFsOptions {
    const temporaryDirectory = reader.readOption(() => reader.readString());
    const cacheDirectory = reader.readOption(() => reader.readString());

    return {
        ...(temporaryDirectory === undefined ? {} : { temporaryDirectory }),
        ...(cacheDirectory === undefined ? {} : { cacheDirectory }),
    };
}

/** Return one JSON value for one HostFsOptions. */
export function toJsonHostFsOptions(value: HostFsOptions): Json {
    return {
        ...(value.temporaryDirectory === undefined ? {} : { temporaryDirectory: value.temporaryDirectory }),
        ...(value.cacheDirectory === undefined ? {} : { cacheDirectory: value.cacheDirectory }),
    };
}

/** Return one HostFsOptions from one JSON value. */
export function fromJsonHostFsOptions(value: Json): HostFsOptions {
    const object = jsonObject(value);

    return {
        temporaryDirectory: jsonOptional(object, "temporaryDirectory", (value) => jsonString(value)),
        cacheDirectory: jsonOptional(object, "cacheDirectory", (value) => jsonString(value)),
    };
}

/** Network host defaults. */
export type HostNetOptions = {
    /** Optional DNS server override list. */
    readonly dnsServers: ReadonlyArray<string>;
    /** Optional proxy URL override. */
    readonly proxyUrl?: string;
    /** Optional default egress interface binding. */
    readonly bindInterface?: string;
};

export const HostNetOptions = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: HostNetOptions): void {
        encodeHostNetOptions(writer, value);
    },

    /** Decode one HostNetOptions. */
    decode(reader: BinaryReader): HostNetOptions {
        return decodeHostNetOptions(reader);
    },

    /** Return this value as JSON. */
    toJson(value: HostNetOptions): Json {
        return toJsonHostNetOptions(value);
    },

    /** Return one HostNetOptions from one JSON value. */
    fromJson(value: Json): HostNetOptions {
        return fromJsonHostNetOptions(value);
    },
};

/** Encode one HostNetOptions. */
export function encodeHostNetOptions(writer: BinaryWriter, value: HostNetOptions): void {
    writer.writeUnsigned(value.dnsServers.length);
    for (const item0 of value.dnsServers) {
        writer.writeString(item0);
    }
    writer.writeOption(value.proxyUrl, (value1) => {
        writer.writeString(value1);
    });
    writer.writeOption(value.bindInterface, (value2) => {
        writer.writeString(value2);
    });
}

/** Decode one HostNetOptions. */
export function decodeHostNetOptions(reader: BinaryReader): HostNetOptions {
    const dnsServers = (() => { const length0 = reader.readNumber(); const items0: Array<string> = []; for (let index = 0; index < length0; index += 1) { items0.push(reader.readString()); } return items0; })();
    const proxyUrl = reader.readOption(() => reader.readString());
    const bindInterface = reader.readOption(() => reader.readString());

    return {
        dnsServers,
        ...(proxyUrl === undefined ? {} : { proxyUrl }),
        ...(bindInterface === undefined ? {} : { bindInterface }),
    };
}

/** Return one JSON value for one HostNetOptions. */
export function toJsonHostNetOptions(value: HostNetOptions): Json {
    return {
        dnsServers: value.dnsServers.map((item0) => item0),
        ...(value.proxyUrl === undefined ? {} : { proxyUrl: value.proxyUrl }),
        ...(value.bindInterface === undefined ? {} : { bindInterface: value.bindInterface }),
    };
}

/** Return one HostNetOptions from one JSON value. */
export function fromJsonHostNetOptions(value: Json): HostNetOptions {
    const object = jsonObject(value);

    return {
        dnsServers: jsonArray(jsonField(object, "dnsServers")).map((item0) => jsonString(item0)),
        proxyUrl: jsonOptional(object, "proxyUrl", (value) => jsonString(value)),
        bindInterface: jsonOptional(object, "bindInterface", (value) => jsonString(value)),
    };
}

/** Process host defaults. */
export type HostProcessOptions = {
    /** Optional default working directory for spawned child processes. */
    readonly defaultWorkingDirectory?: string;
    /** Whether child processes inherit environment variables by default. */
    readonly inheritEnvironment?: boolean;
    /** Optional allow-list for inherited environment variables. */
    readonly environmentAllowlist: ReadonlyArray<string>;
};

export const HostProcessOptions = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: HostProcessOptions): void {
        encodeHostProcessOptions(writer, value);
    },

    /** Decode one HostProcessOptions. */
    decode(reader: BinaryReader): HostProcessOptions {
        return decodeHostProcessOptions(reader);
    },

    /** Return this value as JSON. */
    toJson(value: HostProcessOptions): Json {
        return toJsonHostProcessOptions(value);
    },

    /** Return one HostProcessOptions from one JSON value. */
    fromJson(value: Json): HostProcessOptions {
        return fromJsonHostProcessOptions(value);
    },
};

/** Encode one HostProcessOptions. */
export function encodeHostProcessOptions(writer: BinaryWriter, value: HostProcessOptions): void {
    writer.writeOption(value.defaultWorkingDirectory, (value0) => {
        writer.writeString(value0);
    });
    writer.writeOption(value.inheritEnvironment, (value1) => {
        writer.writeBool(value1);
    });
    writer.writeUnsigned(value.environmentAllowlist.length);
    for (const item2 of value.environmentAllowlist) {
        writer.writeString(item2);
    }
}

/** Decode one HostProcessOptions. */
export function decodeHostProcessOptions(reader: BinaryReader): HostProcessOptions {
    const defaultWorkingDirectory = reader.readOption(() => reader.readString());
    const inheritEnvironment = reader.readOption(() => reader.readBool());
    const environmentAllowlist = (() => { const length2 = reader.readNumber(); const items2: Array<string> = []; for (let index = 0; index < length2; index += 1) { items2.push(reader.readString()); } return items2; })();

    return {
        ...(defaultWorkingDirectory === undefined ? {} : { defaultWorkingDirectory }),
        ...(inheritEnvironment === undefined ? {} : { inheritEnvironment }),
        environmentAllowlist,
    };
}

/** Return one JSON value for one HostProcessOptions. */
export function toJsonHostProcessOptions(value: HostProcessOptions): Json {
    return {
        ...(value.defaultWorkingDirectory === undefined ? {} : { defaultWorkingDirectory: value.defaultWorkingDirectory }),
        ...(value.inheritEnvironment === undefined ? {} : { inheritEnvironment: value.inheritEnvironment }),
        environmentAllowlist: value.environmentAllowlist.map((item0) => item0),
    };
}

/** Return one HostProcessOptions from one JSON value. */
export function fromJsonHostProcessOptions(value: Json): HostProcessOptions {
    const object = jsonObject(value);

    return {
        defaultWorkingDirectory: jsonOptional(object, "defaultWorkingDirectory", (value) => jsonString(value)),
        inheritEnvironment: jsonOptional(object, "inheritEnvironment", (value) => jsonBool(value)),
        environmentAllowlist: jsonArray(jsonField(object, "environmentAllowlist")).map((item0) => jsonString(item0)),
    };
}

/** Audio host defaults. */
export type HostAudioOptions = {
    /** Optional preferred audio backend name. */
    readonly backend?: string;
    /** Optional preferred output device identifier. */
    readonly outputDevice?: string;
    /** Optional preferred input device identifier. */
    readonly inputDevice?: string;
};

export const HostAudioOptions = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: HostAudioOptions): void {
        encodeHostAudioOptions(writer, value);
    },

    /** Decode one HostAudioOptions. */
    decode(reader: BinaryReader): HostAudioOptions {
        return decodeHostAudioOptions(reader);
    },

    /** Return this value as JSON. */
    toJson(value: HostAudioOptions): Json {
        return toJsonHostAudioOptions(value);
    },

    /** Return one HostAudioOptions from one JSON value. */
    fromJson(value: Json): HostAudioOptions {
        return fromJsonHostAudioOptions(value);
    },
};

/** Encode one HostAudioOptions. */
export function encodeHostAudioOptions(writer: BinaryWriter, value: HostAudioOptions): void {
    writer.writeOption(value.backend, (value0) => {
        writer.writeString(value0);
    });
    writer.writeOption(value.outputDevice, (value1) => {
        writer.writeString(value1);
    });
    writer.writeOption(value.inputDevice, (value2) => {
        writer.writeString(value2);
    });
}

/** Decode one HostAudioOptions. */
export function decodeHostAudioOptions(reader: BinaryReader): HostAudioOptions {
    const backend = reader.readOption(() => reader.readString());
    const outputDevice = reader.readOption(() => reader.readString());
    const inputDevice = reader.readOption(() => reader.readString());

    return {
        ...(backend === undefined ? {} : { backend }),
        ...(outputDevice === undefined ? {} : { outputDevice }),
        ...(inputDevice === undefined ? {} : { inputDevice }),
    };
}

/** Return one JSON value for one HostAudioOptions. */
export function toJsonHostAudioOptions(value: HostAudioOptions): Json {
    return {
        ...(value.backend === undefined ? {} : { backend: value.backend }),
        ...(value.outputDevice === undefined ? {} : { outputDevice: value.outputDevice }),
        ...(value.inputDevice === undefined ? {} : { inputDevice: value.inputDevice }),
    };
}

/** Return one HostAudioOptions from one JSON value. */
export function fromJsonHostAudioOptions(value: Json): HostAudioOptions {
    const object = jsonObject(value);

    return {
        backend: jsonOptional(object, "backend", (value) => jsonString(value)),
        outputDevice: jsonOptional(object, "outputDevice", (value) => jsonString(value)),
        inputDevice: jsonOptional(object, "inputDevice", (value) => jsonString(value)),
    };
}

/** Display host defaults. */
export type HostDisplayOptions = {
    /** Optional preferred display backend name. */
    readonly backend?: string;
};

export const HostDisplayOptions = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: HostDisplayOptions): void {
        encodeHostDisplayOptions(writer, value);
    },

    /** Decode one HostDisplayOptions. */
    decode(reader: BinaryReader): HostDisplayOptions {
        return decodeHostDisplayOptions(reader);
    },

    /** Return this value as JSON. */
    toJson(value: HostDisplayOptions): Json {
        return toJsonHostDisplayOptions(value);
    },

    /** Return one HostDisplayOptions from one JSON value. */
    fromJson(value: Json): HostDisplayOptions {
        return fromJsonHostDisplayOptions(value);
    },
};

/** Encode one HostDisplayOptions. */
export function encodeHostDisplayOptions(writer: BinaryWriter, value: HostDisplayOptions): void {
    writer.writeOption(value.backend, (value0) => {
        writer.writeString(value0);
    });
}

/** Decode one HostDisplayOptions. */
export function decodeHostDisplayOptions(reader: BinaryReader): HostDisplayOptions {
    const backend = reader.readOption(() => reader.readString());

    return {
        ...(backend === undefined ? {} : { backend }),
    };
}

/** Return one JSON value for one HostDisplayOptions. */
export function toJsonHostDisplayOptions(value: HostDisplayOptions): Json {
    return {
        ...(value.backend === undefined ? {} : { backend: value.backend }),
    };
}

/** Return one HostDisplayOptions from one JSON value. */
export function fromJsonHostDisplayOptions(value: Json): HostDisplayOptions {
    const object = jsonObject(value);

    return {
        backend: jsonOptional(object, "backend", (value) => jsonString(value)),
    };
}

/** Input host defaults. */
export type HostInputOptions = {
    /** Optional preferred input backend name. */
    readonly backend?: string;
};

export const HostInputOptions = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: HostInputOptions): void {
        encodeHostInputOptions(writer, value);
    },

    /** Decode one HostInputOptions. */
    decode(reader: BinaryReader): HostInputOptions {
        return decodeHostInputOptions(reader);
    },

    /** Return this value as JSON. */
    toJson(value: HostInputOptions): Json {
        return toJsonHostInputOptions(value);
    },

    /** Return one HostInputOptions from one JSON value. */
    fromJson(value: Json): HostInputOptions {
        return fromJsonHostInputOptions(value);
    },
};

/** Encode one HostInputOptions. */
export function encodeHostInputOptions(writer: BinaryWriter, value: HostInputOptions): void {
    writer.writeOption(value.backend, (value0) => {
        writer.writeString(value0);
    });
}

/** Decode one HostInputOptions. */
export function decodeHostInputOptions(reader: BinaryReader): HostInputOptions {
    const backend = reader.readOption(() => reader.readString());

    return {
        ...(backend === undefined ? {} : { backend }),
    };
}

/** Return one JSON value for one HostInputOptions. */
export function toJsonHostInputOptions(value: HostInputOptions): Json {
    return {
        ...(value.backend === undefined ? {} : { backend: value.backend }),
    };
}

/** Return one HostInputOptions from one JSON value. */
export function fromJsonHostInputOptions(value: Json): HostInputOptions {
    const object = jsonObject(value);

    return {
        backend: jsonOptional(object, "backend", (value) => jsonString(value)),
    };
}

/** GPU host defaults. */
export type HostGpuOptions = {
    /** Optional preferred GPU backend name. */
    readonly backend?: string;
    /** Optional preferred adapter name filter. */
    readonly adapterName?: string;
};

export const HostGpuOptions = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: HostGpuOptions): void {
        encodeHostGpuOptions(writer, value);
    },

    /** Decode one HostGpuOptions. */
    decode(reader: BinaryReader): HostGpuOptions {
        return decodeHostGpuOptions(reader);
    },

    /** Return this value as JSON. */
    toJson(value: HostGpuOptions): Json {
        return toJsonHostGpuOptions(value);
    },

    /** Return one HostGpuOptions from one JSON value. */
    fromJson(value: Json): HostGpuOptions {
        return fromJsonHostGpuOptions(value);
    },
};

/** Encode one HostGpuOptions. */
export function encodeHostGpuOptions(writer: BinaryWriter, value: HostGpuOptions): void {
    writer.writeOption(value.backend, (value0) => {
        writer.writeString(value0);
    });
    writer.writeOption(value.adapterName, (value1) => {
        writer.writeString(value1);
    });
}

/** Decode one HostGpuOptions. */
export function decodeHostGpuOptions(reader: BinaryReader): HostGpuOptions {
    const backend = reader.readOption(() => reader.readString());
    const adapterName = reader.readOption(() => reader.readString());

    return {
        ...(backend === undefined ? {} : { backend }),
        ...(adapterName === undefined ? {} : { adapterName }),
    };
}

/** Return one JSON value for one HostGpuOptions. */
export function toJsonHostGpuOptions(value: HostGpuOptions): Json {
    return {
        ...(value.backend === undefined ? {} : { backend: value.backend }),
        ...(value.adapterName === undefined ? {} : { adapterName: value.adapterName }),
    };
}

/** Return one HostGpuOptions from one JSON value. */
export function fromJsonHostGpuOptions(value: Json): HostGpuOptions {
    const object = jsonObject(value);

    return {
        backend: jsonOptional(object, "backend", (value) => jsonString(value)),
        adapterName: jsonOptional(object, "adapterName", (value) => jsonString(value)),
    };
}

/** TLS host defaults. */
export type HostTlsOptions = {
    /** Optional trust store path override. */
    readonly trustStorePath?: string;
    /** Override list for Unix system certificate bundle files. */
    readonly systemCertificateFiles: ReadonlyArray<string>;
    /** Override list for Unix system certificate directories. */
    readonly systemCertificateDirectories: ReadonlyArray<string>;
};

export const HostTlsOptions = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: HostTlsOptions): void {
        encodeHostTlsOptions(writer, value);
    },

    /** Decode one HostTlsOptions. */
    decode(reader: BinaryReader): HostTlsOptions {
        return decodeHostTlsOptions(reader);
    },

    /** Return this value as JSON. */
    toJson(value: HostTlsOptions): Json {
        return toJsonHostTlsOptions(value);
    },

    /** Return one HostTlsOptions from one JSON value. */
    fromJson(value: Json): HostTlsOptions {
        return fromJsonHostTlsOptions(value);
    },
};

/** Encode one HostTlsOptions. */
export function encodeHostTlsOptions(writer: BinaryWriter, value: HostTlsOptions): void {
    writer.writeOption(value.trustStorePath, (value0) => {
        writer.writeString(value0);
    });
    writer.writeUnsigned(value.systemCertificateFiles.length);
    for (const item1 of value.systemCertificateFiles) {
        writer.writeString(item1);
    }
    writer.writeUnsigned(value.systemCertificateDirectories.length);
    for (const item2 of value.systemCertificateDirectories) {
        writer.writeString(item2);
    }
}

/** Decode one HostTlsOptions. */
export function decodeHostTlsOptions(reader: BinaryReader): HostTlsOptions {
    const trustStorePath = reader.readOption(() => reader.readString());
    const systemCertificateFiles = (() => { const length1 = reader.readNumber(); const items1: Array<string> = []; for (let index = 0; index < length1; index += 1) { items1.push(reader.readString()); } return items1; })();
    const systemCertificateDirectories = (() => { const length2 = reader.readNumber(); const items2: Array<string> = []; for (let index = 0; index < length2; index += 1) { items2.push(reader.readString()); } return items2; })();

    return {
        ...(trustStorePath === undefined ? {} : { trustStorePath }),
        systemCertificateFiles,
        systemCertificateDirectories,
    };
}

/** Return one JSON value for one HostTlsOptions. */
export function toJsonHostTlsOptions(value: HostTlsOptions): Json {
    return {
        ...(value.trustStorePath === undefined ? {} : { trustStorePath: value.trustStorePath }),
        systemCertificateFiles: value.systemCertificateFiles.map((item0) => item0),
        systemCertificateDirectories: value.systemCertificateDirectories.map((item0) => item0),
    };
}

/** Return one HostTlsOptions from one JSON value. */
export function fromJsonHostTlsOptions(value: Json): HostTlsOptions {
    const object = jsonObject(value);

    return {
        trustStorePath: jsonOptional(object, "trustStorePath", (value) => jsonString(value)),
        systemCertificateFiles: jsonArray(jsonField(object, "systemCertificateFiles")).map((item0) => jsonString(item0)),
        systemCertificateDirectories: jsonArray(jsonField(object, "systemCertificateDirectories")).map((item0) => jsonString(item0)),
    };
}

/** OS host defaults. */
export type HostOsOptions = {
    /** Optional default locale override. */
    readonly defaultLocale?: string;
    /** Optional application data directory override. */
    readonly dataDirectory?: string;
    /** Optional application state directory override. */
    readonly stateDirectory?: string;
};

export const HostOsOptions = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: HostOsOptions): void {
        encodeHostOsOptions(writer, value);
    },

    /** Decode one HostOsOptions. */
    decode(reader: BinaryReader): HostOsOptions {
        return decodeHostOsOptions(reader);
    },

    /** Return this value as JSON. */
    toJson(value: HostOsOptions): Json {
        return toJsonHostOsOptions(value);
    },

    /** Return one HostOsOptions from one JSON value. */
    fromJson(value: Json): HostOsOptions {
        return fromJsonHostOsOptions(value);
    },
};

/** Encode one HostOsOptions. */
export function encodeHostOsOptions(writer: BinaryWriter, value: HostOsOptions): void {
    writer.writeOption(value.defaultLocale, (value0) => {
        writer.writeString(value0);
    });
    writer.writeOption(value.dataDirectory, (value1) => {
        writer.writeString(value1);
    });
    writer.writeOption(value.stateDirectory, (value2) => {
        writer.writeString(value2);
    });
}

/** Decode one HostOsOptions. */
export function decodeHostOsOptions(reader: BinaryReader): HostOsOptions {
    const defaultLocale = reader.readOption(() => reader.readString());
    const dataDirectory = reader.readOption(() => reader.readString());
    const stateDirectory = reader.readOption(() => reader.readString());

    return {
        ...(defaultLocale === undefined ? {} : { defaultLocale }),
        ...(dataDirectory === undefined ? {} : { dataDirectory }),
        ...(stateDirectory === undefined ? {} : { stateDirectory }),
    };
}

/** Return one JSON value for one HostOsOptions. */
export function toJsonHostOsOptions(value: HostOsOptions): Json {
    return {
        ...(value.defaultLocale === undefined ? {} : { defaultLocale: value.defaultLocale }),
        ...(value.dataDirectory === undefined ? {} : { dataDirectory: value.dataDirectory }),
        ...(value.stateDirectory === undefined ? {} : { stateDirectory: value.stateDirectory }),
    };
}

/** Return one HostOsOptions from one JSON value. */
export function fromJsonHostOsOptions(value: Json): HostOsOptions {
    const object = jsonObject(value);

    return {
        defaultLocale: jsonOptional(object, "defaultLocale", (value) => jsonString(value)),
        dataDirectory: jsonOptional(object, "dataDirectory", (value) => jsonString(value)),
        stateDirectory: jsonOptional(object, "stateDirectory", (value) => jsonString(value)),
    };
}
