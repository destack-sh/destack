/* generated by destack_bridge_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, jsonArray, jsonField, jsonObject, jsonOptional, jsonString } from "../../../protocol/serde.js";
import type { Host } from "../../artifact/core/target.js";
import type { Platform } from "../../artifact/core/target.js";
import type { Derive } from "./compiler.js";
import type { CompilerRestrictions } from "./compiler.js";
import type { Stage } from "./stage.js";
import { decodeHost, encodeHost, fromJsonHost, toJsonHost } from "../../artifact/core/target.js";
import { decodePlatform, encodePlatform, fromJsonPlatform, toJsonPlatform } from "../../artifact/core/target.js";
import { decodeDerive, encodeDerive, fromJsonDerive, toJsonDerive } from "./compiler.js";
import { decodeCompilerRestrictions, encodeCompilerRestrictions, fromJsonCompilerRestrictions, toJsonCompilerRestrictions } from "./compiler.js";
import { decodeStage, encodeStage, fromJsonStage, toJsonStage } from "./stage.js";

/** Normalized profile options. */
export type ProfileOptions = {
    /** Release stage for this profile. */
    readonly stage?: Stage;
    /** Target platform / operating system for this profile. */
    readonly platform?: Platform;
    /** Target host environment for this profile. */
    readonly host?: Host;
    /** Comptime environment whitelist. */
    readonly comptimeEnv?: ReadonlyArray<string>;
    /** Active source graph modes for this profile. */
    readonly modes: ReadonlyArray<string>;
    /** Active source graph roles for this profile. */
    readonly roles: ReadonlyArray<string>;
    /** Active source graph features for this profile. */
    readonly features: ReadonlyArray<string>;
    /** Active source graph tags for this profile. */
    readonly tags: ReadonlyArray<string>;
    /** Default tree tag builder provider. */
    readonly tree?: string;
    /** Global provider modules for this profile. */
    readonly globals: ReadonlyArray<string>;
    /** Well-known derives automatically considered in this profile. */
    readonly derive: ReadonlyArray<Derive>;
    /** Static semantic restrictions for this profile. */
    readonly restrictions: CompilerRestrictions;
};

export const ProfileOptions = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ProfileOptions): void {
        encodeProfileOptions(writer, value);
    },

    /** Decode one ProfileOptions. */
    decode(reader: BinaryReader): ProfileOptions {
        return decodeProfileOptions(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ProfileOptions): Json {
        return toJsonProfileOptions(value);
    },

    /** Return one ProfileOptions from one JSON value. */
    fromJson(value: Json): ProfileOptions {
        return fromJsonProfileOptions(value);
    },
};

/** Encode one ProfileOptions. */
export function encodeProfileOptions(writer: BinaryWriter, value: ProfileOptions): void {
    writer.writeOption(value.stage, (value0) => {
        encodeStage(writer, value0);
    });
    writer.writeOption(value.platform, (value1) => {
        encodePlatform(writer, value1);
    });
    writer.writeOption(value.host, (value2) => {
        encodeHost(writer, value2);
    });
    writer.writeOption(value.comptimeEnv, (value3) => {
        writer.writeUnsigned(value3.length);
        for (const item4 of value3) {
            writer.writeString(item4);
        }
    });
    writer.writeUnsigned(value.modes.length);
    for (const item4 of value.modes) {
        writer.writeString(item4);
    }
    writer.writeUnsigned(value.roles.length);
    for (const item5 of value.roles) {
        writer.writeString(item5);
    }
    writer.writeUnsigned(value.features.length);
    for (const item6 of value.features) {
        writer.writeString(item6);
    }
    writer.writeUnsigned(value.tags.length);
    for (const item7 of value.tags) {
        writer.writeString(item7);
    }
    writer.writeOption(value.tree, (value8) => {
        writer.writeString(value8);
    });
    writer.writeUnsigned(value.globals.length);
    for (const item9 of value.globals) {
        writer.writeString(item9);
    }
    writer.writeUnsigned(value.derive.length);
    for (const item10 of value.derive) {
        encodeDerive(writer, item10);
    }
    encodeCompilerRestrictions(writer, value.restrictions);
}

/** Decode one ProfileOptions. */
export function decodeProfileOptions(reader: BinaryReader): ProfileOptions {
    const stage = reader.readOption(() => decodeStage(reader));
    const platform = reader.readOption(() => decodePlatform(reader));
    const host = reader.readOption(() => decodeHost(reader));
    const comptimeEnv = reader.readOption(() => (() => { const length4 = reader.readNumber(); const items4: Array<string> = []; for (let index = 0; index < length4; index += 1) { items4.push(reader.readString()); } return items4; })());
    const modes = (() => { const length4 = reader.readNumber(); const items4: Array<string> = []; for (let index = 0; index < length4; index += 1) { items4.push(reader.readString()); } return items4; })();
    const roles = (() => { const length5 = reader.readNumber(); const items5: Array<string> = []; for (let index = 0; index < length5; index += 1) { items5.push(reader.readString()); } return items5; })();
    const features = (() => { const length6 = reader.readNumber(); const items6: Array<string> = []; for (let index = 0; index < length6; index += 1) { items6.push(reader.readString()); } return items6; })();
    const tags = (() => { const length7 = reader.readNumber(); const items7: Array<string> = []; for (let index = 0; index < length7; index += 1) { items7.push(reader.readString()); } return items7; })();
    const tree = reader.readOption(() => reader.readString());
    const globals = (() => { const length9 = reader.readNumber(); const items9: Array<string> = []; for (let index = 0; index < length9; index += 1) { items9.push(reader.readString()); } return items9; })();
    const derive = (() => { const length10 = reader.readNumber(); const items10: Array<Derive> = []; for (let index = 0; index < length10; index += 1) { items10.push(decodeDerive(reader)); } return items10; })();
    const restrictions = decodeCompilerRestrictions(reader);

    return {
        ...(stage === undefined ? {} : { stage }),
        ...(platform === undefined ? {} : { platform }),
        ...(host === undefined ? {} : { host }),
        ...(comptimeEnv === undefined ? {} : { comptimeEnv }),
        modes,
        roles,
        features,
        tags,
        ...(tree === undefined ? {} : { tree }),
        globals,
        derive,
        restrictions,
    };
}

/** Return one JSON value for one ProfileOptions. */
export function toJsonProfileOptions(value: ProfileOptions): Json {
    return {
        ...(value.stage === undefined ? {} : { stage: toJsonStage(value.stage) }),
        ...(value.platform === undefined ? {} : { platform: toJsonPlatform(value.platform) }),
        ...(value.host === undefined ? {} : { host: toJsonHost(value.host) }),
        ...(value.comptimeEnv === undefined ? {} : { comptimeEnv: value.comptimeEnv.map((item0) => item0) }),
        modes: value.modes.map((item0) => item0),
        roles: value.roles.map((item0) => item0),
        features: value.features.map((item0) => item0),
        tags: value.tags.map((item0) => item0),
        ...(value.tree === undefined ? {} : { tree: value.tree }),
        globals: value.globals.map((item0) => item0),
        derive: value.derive.map((item0) => toJsonDerive(item0)),
        restrictions: toJsonCompilerRestrictions(value.restrictions),
    };
}

/** Return one ProfileOptions from one JSON value. */
export function fromJsonProfileOptions(value: Json): ProfileOptions {
    const object = jsonObject(value);

    return {
        stage: jsonOptional(object, "stage", (value) => fromJsonStage(value)),
        platform: jsonOptional(object, "platform", (value) => fromJsonPlatform(value)),
        host: jsonOptional(object, "host", (value) => fromJsonHost(value)),
        comptimeEnv: jsonOptional(object, "comptimeEnv", (value) => jsonArray(value).map((item0) => jsonString(item0))),
        modes: jsonArray(jsonField(object, "modes")).map((item0) => jsonString(item0)),
        roles: jsonArray(jsonField(object, "roles")).map((item0) => jsonString(item0)),
        features: jsonArray(jsonField(object, "features")).map((item0) => jsonString(item0)),
        tags: jsonArray(jsonField(object, "tags")).map((item0) => jsonString(item0)),
        tree: jsonOptional(object, "tree", (value) => jsonString(value)),
        globals: jsonArray(jsonField(object, "globals")).map((item0) => jsonString(item0)),
        derive: jsonArray(jsonField(object, "derive")).map((item0) => fromJsonDerive(item0)),
        restrictions: fromJsonCompilerRestrictions(jsonField(object, "restrictions")),
    };
}
