/* generated by destack_bridge_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, jsonArray, jsonBool, jsonField, jsonObject, jsonString } from "../../../protocol/serde.js";

/** Vendored dependency resolution options. */
export type Vendor = {
    /** Vendored dependency resolution mode. */
    readonly mode: VendorMode;
    /** Project-owned vendor directory. */
    readonly path: string;
    /** Package patterns included in the vendor mirror. */
    readonly include: ReadonlyArray<string>;
    /** Package patterns excluded from the vendor mirror. */
    readonly exclude: ReadonlyArray<string>;
    /** Whether vendored packages must match the lock. */
    readonly verify: boolean;
};

export const Vendor = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: Vendor): void {
        encodeVendor(writer, value);
    },

    /** Decode one Vendor. */
    decode(reader: BinaryReader): Vendor {
        return decodeVendor(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Vendor): Json {
        return toJsonVendor(value);
    },

    /** Return one Vendor from one JSON value. */
    fromJson(value: Json): Vendor {
        return fromJsonVendor(value);
    },
};

/** Encode one Vendor. */
export function encodeVendor(writer: BinaryWriter, value: Vendor): void {
    encodeVendorMode(writer, value.mode);
    writer.writeString(value.path);
    writer.writeUnsigned(value.include.length);
    for (const item2 of value.include) {
        writer.writeString(item2);
    }
    writer.writeUnsigned(value.exclude.length);
    for (const item3 of value.exclude) {
        writer.writeString(item3);
    }
    writer.writeBool(value.verify);
}

/** Decode one Vendor. */
export function decodeVendor(reader: BinaryReader): Vendor {
    const mode = decodeVendorMode(reader);
    const path = reader.readString();
    const include = (() => { const length2 = reader.readNumber(); const items2: Array<string> = []; for (let index = 0; index < length2; index += 1) { items2.push(reader.readString()); } return items2; })();
    const exclude = (() => { const length3 = reader.readNumber(); const items3: Array<string> = []; for (let index = 0; index < length3; index += 1) { items3.push(reader.readString()); } return items3; })();
    const verify = reader.readBool();

    return {
        mode,
        path,
        include,
        exclude,
        verify,
    };
}

/** Return one JSON value for one Vendor. */
export function toJsonVendor(value: Vendor): Json {
    return {
        mode: toJsonVendorMode(value.mode),
        path: value.path,
        include: value.include.map((item0) => item0),
        exclude: value.exclude.map((item0) => item0),
        verify: value.verify,
    };
}

/** Return one Vendor from one JSON value. */
export function fromJsonVendor(value: Json): Vendor {
    const object = jsonObject(value);

    return {
        mode: fromJsonVendorMode(jsonField(object, "mode")),
        path: jsonString(jsonField(object, "path")),
        include: jsonArray(jsonField(object, "include")).map((item0) => jsonString(item0)),
        exclude: jsonArray(jsonField(object, "exclude")).map((item0) => jsonString(item0)),
        verify: jsonBool(jsonField(object, "verify")),
    };
}

/** Vendored dependency resolution mode. */
export type VendorMode = "auto" | "prefer" | "require" | "ignore";

export const VendorMode = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: VendorMode): void {
        encodeVendorMode(writer, value);
    },

    /** Decode one VendorMode. */
    decode(reader: BinaryReader): VendorMode {
        return decodeVendorMode(reader);
    },

    /** Return this value as JSON. */
    toJson(value: VendorMode): Json {
        return toJsonVendorMode(value);
    },

    /** Return one VendorMode from one JSON value. */
    fromJson(value: Json): VendorMode {
        return fromJsonVendorMode(value);
    },
};

/** Encode one VendorMode. */
export function encodeVendorMode(writer: BinaryWriter, value: VendorMode): void {
    switch (value) {
        case "auto":
            writer.writeUnsigned(0);
            return;
        case "prefer":
            writer.writeUnsigned(1);
            return;
        case "require":
            writer.writeUnsigned(2);
            return;
        case "ignore":
            writer.writeUnsigned(3);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one VendorMode. */
export function decodeVendorMode(reader: BinaryReader): VendorMode {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "auto";
        case 1:
            return "prefer";
        case 2:
            return "require";
        case 3:
            return "ignore";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one VendorMode. */
export function toJsonVendorMode(value: VendorMode): Json {
    return value;
}

/** Return one VendorMode from one JSON value. */
export function fromJsonVendorMode(value: Json): VendorMode {
    const variant = jsonString(value);

    switch (variant) {
        case "auto":
            return "auto";
        case "prefer":
            return "prefer";
        case "require":
            return "require";
        case "ignore":
            return "ignore";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}
