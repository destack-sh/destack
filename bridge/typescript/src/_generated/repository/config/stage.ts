/* generated by destack_bridge_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, jsonString } from "../../../protocol/serde.js";

/** Package release stage. */
export type Stage = "experimental" | "alpha" | "beta" | "stable";

export const Stage = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: Stage): void {
        encodeStage(writer, value);
    },

    /** Decode one Stage. */
    decode(reader: BinaryReader): Stage {
        return decodeStage(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Stage): Json {
        return toJsonStage(value);
    },

    /** Return one Stage from one JSON value. */
    fromJson(value: Json): Stage {
        return fromJsonStage(value);
    },
};

/** Encode one Stage. */
export function encodeStage(writer: BinaryWriter, value: Stage): void {
    switch (value) {
        case "experimental":
            writer.writeUnsigned(0);
            return;
        case "alpha":
            writer.writeUnsigned(1);
            return;
        case "beta":
            writer.writeUnsigned(2);
            return;
        case "stable":
            writer.writeUnsigned(3);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one Stage. */
export function decodeStage(reader: BinaryReader): Stage {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "experimental";
        case 1:
            return "alpha";
        case 2:
            return "beta";
        case 3:
            return "stable";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one Stage. */
export function toJsonStage(value: Stage): Json {
    return value;
}

/** Return one Stage from one JSON value. */
export function fromJsonStage(value: Json): Stage {
    const variant = jsonString(value);

    switch (variant) {
        case "experimental":
            return "experimental";
        case "alpha":
            return "alpha";
        case "beta":
            return "beta";
        case "stable":
            return "stable";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}
