/* generated by destack_bridge_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, compareBytes, jsonArray, jsonField, jsonObject, jsonOptional, jsonString, nestedBytes } from "../../../protocol/serde.js";

/** Package topology definition. */
export type Topology = {
    /** Declared topology entities. */
    readonly entities: Readonly<Record<string, TopologyEntity>>;
    /** Declared topology edges. */
    readonly edges: Readonly<Record<string, TopologyEdge>>;
};

export const Topology = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: Topology): void {
        encodeTopology(writer, value);
    },

    /** Decode one Topology. */
    decode(reader: BinaryReader): Topology {
        return decodeTopology(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Topology): Json {
        return toJsonTopology(value);
    },

    /** Return one Topology from one JSON value. */
    fromJson(value: Json): Topology {
        return fromJsonTopology(value);
    },
};

/** Encode one Topology. */
export function encodeTopology(writer: BinaryWriter, value: Topology): void {
    const entries0 = Object.entries(value.entities).map(([key0, item0]) => {
        const keyBytes = nestedBytes((writer) => {
            writer.writeString(key0);
        });
        return { key0, item0, keyBytes };
    });
    entries0.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries0.length);
    for (const entry0 of entries0) {
        writer.writeString(entry0.key0);
        encodeTopologyEntity(writer, entry0.item0);
    }
    const entries1 = Object.entries(value.edges).map(([key1, item1]) => {
        const keyBytes = nestedBytes((writer) => {
            writer.writeString(key1);
        });
        return { key1, item1, keyBytes };
    });
    entries1.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries1.length);
    for (const entry1 of entries1) {
        writer.writeString(entry1.key1);
        encodeTopologyEdge(writer, entry1.item1);
    }
}

/** Decode one Topology. */
export function decodeTopology(reader: BinaryReader): Topology {
    const entities = (() => { const length0 = reader.readNumber(); const items0: Record<string, TopologyEntity> = {}; for (let index = 0; index < length0; index += 1) { const key = reader.readString(); items0[key] = decodeTopologyEntity(reader); } return items0; })();
    const edges = (() => { const length1 = reader.readNumber(); const items1: Record<string, TopologyEdge> = {}; for (let index = 0; index < length1; index += 1) { const key = reader.readString(); items1[key] = decodeTopologyEdge(reader); } return items1; })();

    return {
        entities,
        edges,
    };
}

/** Return one JSON value for one Topology. */
export function toJsonTopology(value: Topology): Json {
    return {
        entities: Object.fromEntries(Object.entries(value.entities).map(([key0, item0]) => [key0, toJsonTopologyEntity(item0)] as const)),
        edges: Object.fromEntries(Object.entries(value.edges).map(([key0, item0]) => [key0, toJsonTopologyEdge(item0)] as const)),
    };
}

/** Return one Topology from one JSON value. */
export function fromJsonTopology(value: Json): Topology {
    const object = jsonObject(value);

    return {
        entities: Object.fromEntries(Object.entries(jsonObject(jsonField(object, "entities"))).map(([key0, item0]) => [key0, fromJsonTopologyEntity(item0)] as const)),
        edges: Object.fromEntries(Object.entries(jsonObject(jsonField(object, "edges"))).map(([key0, item0]) => [key0, fromJsonTopologyEdge(item0)] as const)),
    };
}

/** One declared topology entity. */
export type TopologyEntity = {
    /** Stable entity kind identifier. */
    readonly kind: string;
    /** Stable entity name. */
    readonly name?: string;
    /** Entity labels. */
    readonly labels: Readonly<Record<string, string>>;
};

export const TopologyEntity = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: TopologyEntity): void {
        encodeTopologyEntity(writer, value);
    },

    /** Decode one TopologyEntity. */
    decode(reader: BinaryReader): TopologyEntity {
        return decodeTopologyEntity(reader);
    },

    /** Return this value as JSON. */
    toJson(value: TopologyEntity): Json {
        return toJsonTopologyEntity(value);
    },

    /** Return one TopologyEntity from one JSON value. */
    fromJson(value: Json): TopologyEntity {
        return fromJsonTopologyEntity(value);
    },
};

/** Encode one TopologyEntity. */
export function encodeTopologyEntity(writer: BinaryWriter, value: TopologyEntity): void {
    writer.writeString(value.kind);
    writer.writeOption(value.name, (value1) => {
        writer.writeString(value1);
    });
    const entries2 = Object.entries(value.labels).map(([key2, item2]) => {
        const keyBytes = nestedBytes((writer) => {
            writer.writeString(key2);
        });
        return { key2, item2, keyBytes };
    });
    entries2.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries2.length);
    for (const entry2 of entries2) {
        writer.writeString(entry2.key2);
        writer.writeString(entry2.item2);
    }
}

/** Decode one TopologyEntity. */
export function decodeTopologyEntity(reader: BinaryReader): TopologyEntity {
    const kind = reader.readString();
    const name = reader.readOption(() => reader.readString());
    const labels = (() => { const length2 = reader.readNumber(); const items2: Record<string, string> = {}; for (let index = 0; index < length2; index += 1) { const key = reader.readString(); items2[key] = reader.readString(); } return items2; })();

    return {
        kind,
        ...(name === undefined ? {} : { name }),
        labels,
    };
}

/** Return one JSON value for one TopologyEntity. */
export function toJsonTopologyEntity(value: TopologyEntity): Json {
    return {
        kind: value.kind,
        ...(value.name === undefined ? {} : { name: value.name }),
        labels: Object.fromEntries(Object.entries(value.labels).map(([key0, item0]) => [key0, item0] as const)),
    };
}

/** Return one TopologyEntity from one JSON value. */
export function fromJsonTopologyEntity(value: Json): TopologyEntity {
    const object = jsonObject(value);

    return {
        kind: jsonString(jsonField(object, "kind")),
        name: jsonOptional(object, "name", (value) => jsonString(value)),
        labels: Object.fromEntries(Object.entries(jsonObject(jsonField(object, "labels"))).map(([key0, item0]) => [key0, jsonString(item0)] as const)),
    };
}

/** One declared topology edge. */
export type TopologyEdge = {
    /** Stable edge kind identifier. */
    readonly kind: string;
    /** Stable edge name. */
    readonly name?: string;
    /** Source node name. */
    readonly from: string;
    /** Target node name. */
    readonly to: string;
    /** Edge labels. */
    readonly labels: Readonly<Record<string, string>>;
};

export const TopologyEdge = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: TopologyEdge): void {
        encodeTopologyEdge(writer, value);
    },

    /** Decode one TopologyEdge. */
    decode(reader: BinaryReader): TopologyEdge {
        return decodeTopologyEdge(reader);
    },

    /** Return this value as JSON. */
    toJson(value: TopologyEdge): Json {
        return toJsonTopologyEdge(value);
    },

    /** Return one TopologyEdge from one JSON value. */
    fromJson(value: Json): TopologyEdge {
        return fromJsonTopologyEdge(value);
    },
};

/** Encode one TopologyEdge. */
export function encodeTopologyEdge(writer: BinaryWriter, value: TopologyEdge): void {
    writer.writeString(value.kind);
    writer.writeOption(value.name, (value1) => {
        writer.writeString(value1);
    });
    writer.writeString(value.from);
    writer.writeString(value.to);
    const entries4 = Object.entries(value.labels).map(([key4, item4]) => {
        const keyBytes = nestedBytes((writer) => {
            writer.writeString(key4);
        });
        return { key4, item4, keyBytes };
    });
    entries4.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries4.length);
    for (const entry4 of entries4) {
        writer.writeString(entry4.key4);
        writer.writeString(entry4.item4);
    }
}

/** Decode one TopologyEdge. */
export function decodeTopologyEdge(reader: BinaryReader): TopologyEdge {
    const kind = reader.readString();
    const name = reader.readOption(() => reader.readString());
    const from_ = reader.readString();
    const to = reader.readString();
    const labels = (() => { const length4 = reader.readNumber(); const items4: Record<string, string> = {}; for (let index = 0; index < length4; index += 1) { const key = reader.readString(); items4[key] = reader.readString(); } return items4; })();

    return {
        kind,
        ...(name === undefined ? {} : { name }),
        from: from_,
        to,
        labels,
    };
}

/** Return one JSON value for one TopologyEdge. */
export function toJsonTopologyEdge(value: TopologyEdge): Json {
    return {
        kind: value.kind,
        ...(value.name === undefined ? {} : { name: value.name }),
        from: value.from,
        to: value.to,
        labels: Object.fromEntries(Object.entries(value.labels).map(([key0, item0]) => [key0, item0] as const)),
    };
}

/** Return one TopologyEdge from one JSON value. */
export function fromJsonTopologyEdge(value: Json): TopologyEdge {
    const object = jsonObject(value);

    return {
        kind: jsonString(jsonField(object, "kind")),
        name: jsonOptional(object, "name", (value) => jsonString(value)),
        from: jsonString(jsonField(object, "from")),
        to: jsonString(jsonField(object, "to")),
        labels: Object.fromEntries(Object.entries(jsonObject(jsonField(object, "labels"))).map(([key0, item0]) => [key0, jsonString(item0)] as const)),
    };
}
