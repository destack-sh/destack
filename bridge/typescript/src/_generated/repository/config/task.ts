/* generated by destack_bridge_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, compareBytes, jsonArray, jsonField, jsonObject, jsonOptional, jsonString, nestedBytes } from "../../../protocol/serde.js";

/** Named toolchain or shell invocation. */
export type Task = {
    /** Destack command name to run. */
    readonly run?: string;
    /** Explicit shell command to execute. */
    readonly exec?: string;
    /** Build target selected for this task. */
    readonly target?: string;
    /** Product selected for this task. */
    readonly product?: string;
    /** Profile selected for this task. */
    readonly profile?: string;
    /** Active source graph modes added by this task. */
    readonly modes: ReadonlyArray<string>;
    /** Active source graph roles added by this task. */
    readonly roles: ReadonlyArray<string>;
    /** Active source graph features added by this task. */
    readonly features: ReadonlyArray<string>;
    /** Active source graph tags added by this task. */
    readonly tags: ReadonlyArray<string>;
    /** Environment variables passed to this task. */
    readonly env: Readonly<Record<string, string>>;
    /** Structured command arguments. */
    readonly arguments: Readonly<Record<string, unknown>>;
    /** Tasks that must complete before this task. */
    readonly dependsOn: ReadonlyArray<string>;
};

export const Task = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: Task): void {
        encodeTask(writer, value);
    },

    /** Decode one Task. */
    decode(reader: BinaryReader): Task {
        return decodeTask(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Task): Json {
        return toJsonTask(value);
    },

    /** Return one Task from one JSON value. */
    fromJson(value: Json): Task {
        return fromJsonTask(value);
    },
};

/** Encode one Task. */
export function encodeTask(writer: BinaryWriter, value: Task): void {
    writer.writeOption(value.run, (value0) => {
        writer.writeString(value0);
    });
    writer.writeOption(value.exec, (value1) => {
        writer.writeString(value1);
    });
    writer.writeOption(value.target, (value2) => {
        writer.writeString(value2);
    });
    writer.writeOption(value.product, (value3) => {
        writer.writeString(value3);
    });
    writer.writeOption(value.profile, (value4) => {
        writer.writeString(value4);
    });
    writer.writeUnsigned(value.modes.length);
    for (const item5 of value.modes) {
        writer.writeString(item5);
    }
    writer.writeUnsigned(value.roles.length);
    for (const item6 of value.roles) {
        writer.writeString(item6);
    }
    writer.writeUnsigned(value.features.length);
    for (const item7 of value.features) {
        writer.writeString(item7);
    }
    writer.writeUnsigned(value.tags.length);
    for (const item8 of value.tags) {
        writer.writeString(item8);
    }
    const entries9 = Object.entries(value.env).map(([key9, item9]) => {
        const keyBytes = nestedBytes((writer) => {
            writer.writeString(key9);
        });
        return { key9, item9, keyBytes };
    });
    entries9.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries9.length);
    for (const entry9 of entries9) {
        writer.writeString(entry9.key9);
        writer.writeString(entry9.item9);
    }
    const entries10 = Object.entries(value.arguments).map(([key10, item10]) => {
        const keyBytes = nestedBytes((writer) => {
            writer.writeString(key10);
        });
        return { key10, item10, keyBytes };
    });
    entries10.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries10.length);
    for (const entry10 of entries10) {
        writer.writeString(entry10.key10);
        writer.writeJson(entry10.item10);
    }
    writer.writeUnsigned(value.dependsOn.length);
    for (const item11 of value.dependsOn) {
        writer.writeString(item11);
    }
}

/** Decode one Task. */
export function decodeTask(reader: BinaryReader): Task {
    const run = reader.readOption(() => reader.readString());
    const exec = reader.readOption(() => reader.readString());
    const target = reader.readOption(() => reader.readString());
    const product = reader.readOption(() => reader.readString());
    const profile = reader.readOption(() => reader.readString());
    const modes = (() => { const length5 = reader.readNumber(); const items5: Array<string> = []; for (let index = 0; index < length5; index += 1) { items5.push(reader.readString()); } return items5; })();
    const roles = (() => { const length6 = reader.readNumber(); const items6: Array<string> = []; for (let index = 0; index < length6; index += 1) { items6.push(reader.readString()); } return items6; })();
    const features = (() => { const length7 = reader.readNumber(); const items7: Array<string> = []; for (let index = 0; index < length7; index += 1) { items7.push(reader.readString()); } return items7; })();
    const tags = (() => { const length8 = reader.readNumber(); const items8: Array<string> = []; for (let index = 0; index < length8; index += 1) { items8.push(reader.readString()); } return items8; })();
    const env = (() => { const length9 = reader.readNumber(); const items9: Record<string, string> = {}; for (let index = 0; index < length9; index += 1) { const key = reader.readString(); items9[key] = reader.readString(); } return items9; })();
    const arguments_ = (() => { const length10 = reader.readNumber(); const items10: Record<string, unknown> = {}; for (let index = 0; index < length10; index += 1) { const key = reader.readString(); items10[key] = reader.readJson(); } return items10; })();
    const dependsOn = (() => { const length11 = reader.readNumber(); const items11: Array<string> = []; for (let index = 0; index < length11; index += 1) { items11.push(reader.readString()); } return items11; })();

    return {
        ...(run === undefined ? {} : { run }),
        ...(exec === undefined ? {} : { exec }),
        ...(target === undefined ? {} : { target }),
        ...(product === undefined ? {} : { product }),
        ...(profile === undefined ? {} : { profile }),
        modes,
        roles,
        features,
        tags,
        env,
        arguments: arguments_,
        dependsOn,
    };
}

/** Return one JSON value for one Task. */
export function toJsonTask(value: Task): Json {
    return {
        ...(value.run === undefined ? {} : { run: value.run }),
        ...(value.exec === undefined ? {} : { exec: value.exec }),
        ...(value.target === undefined ? {} : { target: value.target }),
        ...(value.product === undefined ? {} : { product: value.product }),
        ...(value.profile === undefined ? {} : { profile: value.profile }),
        modes: value.modes.map((item0) => item0),
        roles: value.roles.map((item0) => item0),
        features: value.features.map((item0) => item0),
        tags: value.tags.map((item0) => item0),
        env: Object.fromEntries(Object.entries(value.env).map(([key0, item0]) => [key0, item0] as const)),
        arguments: Object.fromEntries(Object.entries(value.arguments).map(([key0, item0]) => [key0, item0 as Json] as const)),
        dependsOn: value.dependsOn.map((item0) => item0),
    };
}

/** Return one Task from one JSON value. */
export function fromJsonTask(value: Json): Task {
    const object = jsonObject(value);

    return {
        run: jsonOptional(object, "run", (value) => jsonString(value)),
        exec: jsonOptional(object, "exec", (value) => jsonString(value)),
        target: jsonOptional(object, "target", (value) => jsonString(value)),
        product: jsonOptional(object, "product", (value) => jsonString(value)),
        profile: jsonOptional(object, "profile", (value) => jsonString(value)),
        modes: jsonArray(jsonField(object, "modes")).map((item0) => jsonString(item0)),
        roles: jsonArray(jsonField(object, "roles")).map((item0) => jsonString(item0)),
        features: jsonArray(jsonField(object, "features")).map((item0) => jsonString(item0)),
        tags: jsonArray(jsonField(object, "tags")).map((item0) => jsonString(item0)),
        env: Object.fromEntries(Object.entries(jsonObject(jsonField(object, "env"))).map(([key0, item0]) => [key0, jsonString(item0)] as const)),
        arguments: Object.fromEntries(Object.entries(jsonObject(jsonField(object, "arguments"))).map(([key0, item0]) => [key0, item0] as const)),
        dependsOn: jsonArray(jsonField(object, "dependsOn")).map((item0) => jsonString(item0)),
    };
}
