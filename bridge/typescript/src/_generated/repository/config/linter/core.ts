/* generated by destack_bridge_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, compareBytes, jsonArray, jsonBool, jsonField, jsonObject, jsonString, nestedBytes } from "../../../../protocol/serde.js";
import type { LinterComplexityOptions } from "./complexity.js";
import type { LinterCorrectnessOptions } from "./correctness.js";
import type { LinterPerformanceOptions } from "./performance.js";
import type { LinterRestrictionOptions } from "./restriction.js";
import type { LinterSecurityOptions } from "./security.js";
import type { LinterStyleOptions } from "./style.js";
import type { LinterSuspiciousOptions } from "./suspicious.js";
import { decodeLinterComplexityOptions, encodeLinterComplexityOptions, fromJsonLinterComplexityOptions, toJsonLinterComplexityOptions } from "./complexity.js";
import { decodeLinterCorrectnessOptions, encodeLinterCorrectnessOptions, fromJsonLinterCorrectnessOptions, toJsonLinterCorrectnessOptions } from "./correctness.js";
import { decodeLinterPerformanceOptions, encodeLinterPerformanceOptions, fromJsonLinterPerformanceOptions, toJsonLinterPerformanceOptions } from "./performance.js";
import { decodeLinterRestrictionOptions, encodeLinterRestrictionOptions, fromJsonLinterRestrictionOptions, toJsonLinterRestrictionOptions } from "./restriction.js";
import { decodeLinterSecurityOptions, encodeLinterSecurityOptions, fromJsonLinterSecurityOptions, toJsonLinterSecurityOptions } from "./security.js";
import { decodeLinterStyleOptions, encodeLinterStyleOptions, fromJsonLinterStyleOptions, toJsonLinterStyleOptions } from "./style.js";
import { decodeLinterSuspiciousOptions, encodeLinterSuspiciousOptions, fromJsonLinterSuspiciousOptions, toJsonLinterSuspiciousOptions } from "./suspicious.js";

/** Linter options. */
export type LinterOptions = {
    /** Whether linting is enabled. */
    readonly enabled: boolean;
    /** Base preset (none, recommended, all). */
    readonly preset: LintPreset;
    /** Category-level severity overrides. */
    readonly categories: ReadonlyMap<LintCategory, LintSeverity>;
    /** Individual rule severity overrides. */
    readonly overrides: Readonly<Record<string, LintSeverity>>;
    /** Include declaration files when evaluating declaration-gated rules. */
    readonly includeDeclarationFiles: boolean;
    /** Correctness-category options. */
    readonly correctness: LinterCorrectnessOptions;
    /** Suspicious-category options. */
    readonly suspicious: LinterSuspiciousOptions;
    /** Performance-category options. */
    readonly performance: LinterPerformanceOptions;
    /** Style-category options. */
    readonly style: LinterStyleOptions;
    /** Security-category options. */
    readonly security: LinterSecurityOptions;
    /** Complexity-category options. */
    readonly complexity: LinterComplexityOptions;
    /** Restriction-category options. */
    readonly restriction: LinterRestrictionOptions;
};

export const LinterOptions = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: LinterOptions): void {
        encodeLinterOptions(writer, value);
    },

    /** Decode one LinterOptions. */
    decode(reader: BinaryReader): LinterOptions {
        return decodeLinterOptions(reader);
    },

    /** Return this value as JSON. */
    toJson(value: LinterOptions): Json {
        return toJsonLinterOptions(value);
    },

    /** Return one LinterOptions from one JSON value. */
    fromJson(value: Json): LinterOptions {
        return fromJsonLinterOptions(value);
    },
};

/** Encode one LinterOptions. */
export function encodeLinterOptions(writer: BinaryWriter, value: LinterOptions): void {
    writer.writeBool(value.enabled);
    encodeLintPreset(writer, value.preset);
    const entries2 = Array.from(value.categories.entries()).map(([key2, item2]) => {
        const keyBytes = nestedBytes((writer) => {
            encodeLintCategory(writer, key2);
        });
        return { key2, item2, keyBytes };
    });
    entries2.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries2.length);
    for (const entry2 of entries2) {
        encodeLintCategory(writer, entry2.key2);
        encodeLintSeverity(writer, entry2.item2);
    }
    const entries3 = Object.entries(value.overrides).map(([key3, item3]) => {
        const keyBytes = nestedBytes((writer) => {
            writer.writeString(key3);
        });
        return { key3, item3, keyBytes };
    });
    entries3.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries3.length);
    for (const entry3 of entries3) {
        writer.writeString(entry3.key3);
        encodeLintSeverity(writer, entry3.item3);
    }
    writer.writeBool(value.includeDeclarationFiles);
    encodeLinterCorrectnessOptions(writer, value.correctness);
    encodeLinterSuspiciousOptions(writer, value.suspicious);
    encodeLinterPerformanceOptions(writer, value.performance);
    encodeLinterStyleOptions(writer, value.style);
    encodeLinterSecurityOptions(writer, value.security);
    encodeLinterComplexityOptions(writer, value.complexity);
    encodeLinterRestrictionOptions(writer, value.restriction);
}

/** Decode one LinterOptions. */
export function decodeLinterOptions(reader: BinaryReader): LinterOptions {
    const enabled = reader.readBool();
    const preset = decodeLintPreset(reader);
    const categories = (() => { const length2 = reader.readNumber(); const items2 = new Map<LintCategory, LintSeverity>(); for (let index = 0; index < length2; index += 1) { items2.set(decodeLintCategory(reader), decodeLintSeverity(reader)); } return items2; })();
    const overrides = (() => { const length3 = reader.readNumber(); const items3: Record<string, LintSeverity> = {}; for (let index = 0; index < length3; index += 1) { const key = reader.readString(); items3[key] = decodeLintSeverity(reader); } return items3; })();
    const includeDeclarationFiles = reader.readBool();
    const correctness = decodeLinterCorrectnessOptions(reader);
    const suspicious = decodeLinterSuspiciousOptions(reader);
    const performance = decodeLinterPerformanceOptions(reader);
    const style = decodeLinterStyleOptions(reader);
    const security = decodeLinterSecurityOptions(reader);
    const complexity = decodeLinterComplexityOptions(reader);
    const restriction = decodeLinterRestrictionOptions(reader);

    return {
        enabled,
        preset,
        categories,
        overrides,
        includeDeclarationFiles,
        correctness,
        suspicious,
        performance,
        style,
        security,
        complexity,
        restriction,
    };
}

/** Return one JSON value for one LinterOptions. */
export function toJsonLinterOptions(value: LinterOptions): Json {
    return {
        enabled: value.enabled,
        preset: toJsonLintPreset(value.preset),
        categories: Array.from(value.categories.entries()).map(([key0, item0]) => [toJsonLintCategory(key0), toJsonLintSeverity(item0)] as const),
        overrides: Object.fromEntries(Object.entries(value.overrides).map(([key0, item0]) => [key0, toJsonLintSeverity(item0)] as const)),
        includeDeclarationFiles: value.includeDeclarationFiles,
        correctness: toJsonLinterCorrectnessOptions(value.correctness),
        suspicious: toJsonLinterSuspiciousOptions(value.suspicious),
        performance: toJsonLinterPerformanceOptions(value.performance),
        style: toJsonLinterStyleOptions(value.style),
        security: toJsonLinterSecurityOptions(value.security),
        complexity: toJsonLinterComplexityOptions(value.complexity),
        restriction: toJsonLinterRestrictionOptions(value.restriction),
    };
}

/** Return one LinterOptions from one JSON value. */
export function fromJsonLinterOptions(value: Json): LinterOptions {
    const object = jsonObject(value);

    return {
        enabled: jsonBool(jsonField(object, "enabled")),
        preset: fromJsonLintPreset(jsonField(object, "preset")),
        categories: new Map(jsonArray(jsonField(object, "categories")).map((entry) => { const items = jsonArray(entry); if (items.length !== 2) { throw new SerdeError(`expected JSON map entry length 2: ${items.length}`); } const key0 = items[0]; const item0 = items[1]; return [fromJsonLintCategory(key0), fromJsonLintSeverity(item0)] as const; })),
        overrides: Object.fromEntries(Object.entries(jsonObject(jsonField(object, "overrides"))).map(([key0, item0]) => [key0, fromJsonLintSeverity(item0)] as const)),
        includeDeclarationFiles: jsonBool(jsonField(object, "includeDeclarationFiles")),
        correctness: fromJsonLinterCorrectnessOptions(jsonField(object, "correctness")),
        suspicious: fromJsonLinterSuspiciousOptions(jsonField(object, "suspicious")),
        performance: fromJsonLinterPerformanceOptions(jsonField(object, "performance")),
        style: fromJsonLinterStyleOptions(jsonField(object, "style")),
        security: fromJsonLinterSecurityOptions(jsonField(object, "security")),
        complexity: fromJsonLinterComplexityOptions(jsonField(object, "complexity")),
        restriction: fromJsonLinterRestrictionOptions(jsonField(object, "restriction")),
    };
}

/** Lint rule preset. */
export type LintPreset = "none" | "recommended" | "strict" | "all";

export const LintPreset = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: LintPreset): void {
        encodeLintPreset(writer, value);
    },

    /** Decode one LintPreset. */
    decode(reader: BinaryReader): LintPreset {
        return decodeLintPreset(reader);
    },

    /** Return this value as JSON. */
    toJson(value: LintPreset): Json {
        return toJsonLintPreset(value);
    },

    /** Return one LintPreset from one JSON value. */
    fromJson(value: Json): LintPreset {
        return fromJsonLintPreset(value);
    },
};

/** Encode one LintPreset. */
export function encodeLintPreset(writer: BinaryWriter, value: LintPreset): void {
    switch (value) {
        case "none":
            writer.writeUnsigned(0);
            return;
        case "recommended":
            writer.writeUnsigned(1);
            return;
        case "strict":
            writer.writeUnsigned(2);
            return;
        case "all":
            writer.writeUnsigned(3);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one LintPreset. */
export function decodeLintPreset(reader: BinaryReader): LintPreset {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "none";
        case 1:
            return "recommended";
        case 2:
            return "strict";
        case 3:
            return "all";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one LintPreset. */
export function toJsonLintPreset(value: LintPreset): Json {
    return value;
}

/** Return one LintPreset from one JSON value. */
export function fromJsonLintPreset(value: Json): LintPreset {
    const variant = jsonString(value);

    switch (variant) {
        case "none":
            return "none";
        case "recommended":
            return "recommended";
        case "strict":
            return "strict";
        case "all":
            return "all";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** Lint rule categories. */
export type LintCategory = "correctness" | "suspicious" | "performance" | "style" | "security" | "complexity" | "restriction";

export const LintCategory = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: LintCategory): void {
        encodeLintCategory(writer, value);
    },

    /** Decode one LintCategory. */
    decode(reader: BinaryReader): LintCategory {
        return decodeLintCategory(reader);
    },

    /** Return this value as JSON. */
    toJson(value: LintCategory): Json {
        return toJsonLintCategory(value);
    },

    /** Return one LintCategory from one JSON value. */
    fromJson(value: Json): LintCategory {
        return fromJsonLintCategory(value);
    },
};

/** Encode one LintCategory. */
export function encodeLintCategory(writer: BinaryWriter, value: LintCategory): void {
    switch (value) {
        case "correctness":
            writer.writeUnsigned(0);
            return;
        case "suspicious":
            writer.writeUnsigned(1);
            return;
        case "performance":
            writer.writeUnsigned(2);
            return;
        case "style":
            writer.writeUnsigned(3);
            return;
        case "security":
            writer.writeUnsigned(4);
            return;
        case "complexity":
            writer.writeUnsigned(5);
            return;
        case "restriction":
            writer.writeUnsigned(6);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one LintCategory. */
export function decodeLintCategory(reader: BinaryReader): LintCategory {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "correctness";
        case 1:
            return "suspicious";
        case 2:
            return "performance";
        case 3:
            return "style";
        case 4:
            return "security";
        case 5:
            return "complexity";
        case 6:
            return "restriction";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one LintCategory. */
export function toJsonLintCategory(value: LintCategory): Json {
    return value;
}

/** Return one LintCategory from one JSON value. */
export function fromJsonLintCategory(value: Json): LintCategory {
    const variant = jsonString(value);

    switch (variant) {
        case "correctness":
            return "correctness";
        case "suspicious":
            return "suspicious";
        case "performance":
            return "performance";
        case "style":
            return "style";
        case "security":
            return "security";
        case "complexity":
            return "complexity";
        case "restriction":
            return "restriction";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** Rule severity level. */
export type LintSeverity = "off" | "note" | "warning" | "error";

export const LintSeverity = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: LintSeverity): void {
        encodeLintSeverity(writer, value);
    },

    /** Decode one LintSeverity. */
    decode(reader: BinaryReader): LintSeverity {
        return decodeLintSeverity(reader);
    },

    /** Return this value as JSON. */
    toJson(value: LintSeverity): Json {
        return toJsonLintSeverity(value);
    },

    /** Return one LintSeverity from one JSON value. */
    fromJson(value: Json): LintSeverity {
        return fromJsonLintSeverity(value);
    },
};

/** Encode one LintSeverity. */
export function encodeLintSeverity(writer: BinaryWriter, value: LintSeverity): void {
    switch (value) {
        case "off":
            writer.writeUnsigned(0);
            return;
        case "note":
            writer.writeUnsigned(1);
            return;
        case "warning":
            writer.writeUnsigned(2);
            return;
        case "error":
            writer.writeUnsigned(3);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one LintSeverity. */
export function decodeLintSeverity(reader: BinaryReader): LintSeverity {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "off";
        case 1:
            return "note";
        case 2:
            return "warning";
        case 3:
            return "error";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one LintSeverity. */
export function toJsonLintSeverity(value: LintSeverity): Json {
    return value;
}

/** Return one LintSeverity from one JSON value. */
export function fromJsonLintSeverity(value: Json): LintSeverity {
    const variant = jsonString(value);

    switch (variant) {
        case "off":
            return "off";
        case "note":
            return "note";
        case "warning":
            return "warning";
        case "error":
            return "error";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** Condition assignment policy for `no-cond-assign`. */
export type ConditionAssignmentMode = "exceptParens" | "always";

export const ConditionAssignmentMode = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ConditionAssignmentMode): void {
        encodeConditionAssignmentMode(writer, value);
    },

    /** Decode one ConditionAssignmentMode. */
    decode(reader: BinaryReader): ConditionAssignmentMode {
        return decodeConditionAssignmentMode(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ConditionAssignmentMode): Json {
        return toJsonConditionAssignmentMode(value);
    },

    /** Return one ConditionAssignmentMode from one JSON value. */
    fromJson(value: Json): ConditionAssignmentMode {
        return fromJsonConditionAssignmentMode(value);
    },
};

/** Encode one ConditionAssignmentMode. */
export function encodeConditionAssignmentMode(writer: BinaryWriter, value: ConditionAssignmentMode): void {
    switch (value) {
        case "exceptParens":
            writer.writeUnsigned(0);
            return;
        case "always":
            writer.writeUnsigned(1);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one ConditionAssignmentMode. */
export function decodeConditionAssignmentMode(reader: BinaryReader): ConditionAssignmentMode {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "exceptParens";
        case 1:
            return "always";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one ConditionAssignmentMode. */
export function toJsonConditionAssignmentMode(value: ConditionAssignmentMode): Json {
    return value;
}

/** Return one ConditionAssignmentMode from one JSON value. */
export function fromJsonConditionAssignmentMode(value: Json): ConditionAssignmentMode {
    const variant = jsonString(value);

    switch (variant) {
        case "exceptParens":
            return "exceptParens";
        case "always":
            return "always";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** Empty function kinds that `no-empty-function` may allow. */
export type EmptyFunctionKind = "functions" | "arrowFunctions" | "generatorFunctions" | "methods" | "generatorMethods" | "getters" | "setters" | "constructors" | "asyncFunctions" | "asyncMethods" | "overrideMethods";

export const EmptyFunctionKind = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: EmptyFunctionKind): void {
        encodeEmptyFunctionKind(writer, value);
    },

    /** Decode one EmptyFunctionKind. */
    decode(reader: BinaryReader): EmptyFunctionKind {
        return decodeEmptyFunctionKind(reader);
    },

    /** Return this value as JSON. */
    toJson(value: EmptyFunctionKind): Json {
        return toJsonEmptyFunctionKind(value);
    },

    /** Return one EmptyFunctionKind from one JSON value. */
    fromJson(value: Json): EmptyFunctionKind {
        return fromJsonEmptyFunctionKind(value);
    },
};

/** Encode one EmptyFunctionKind. */
export function encodeEmptyFunctionKind(writer: BinaryWriter, value: EmptyFunctionKind): void {
    switch (value) {
        case "functions":
            writer.writeUnsigned(0);
            return;
        case "arrowFunctions":
            writer.writeUnsigned(1);
            return;
        case "generatorFunctions":
            writer.writeUnsigned(2);
            return;
        case "methods":
            writer.writeUnsigned(3);
            return;
        case "generatorMethods":
            writer.writeUnsigned(4);
            return;
        case "getters":
            writer.writeUnsigned(5);
            return;
        case "setters":
            writer.writeUnsigned(6);
            return;
        case "constructors":
            writer.writeUnsigned(7);
            return;
        case "asyncFunctions":
            writer.writeUnsigned(8);
            return;
        case "asyncMethods":
            writer.writeUnsigned(9);
            return;
        case "overrideMethods":
            writer.writeUnsigned(10);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one EmptyFunctionKind. */
export function decodeEmptyFunctionKind(reader: BinaryReader): EmptyFunctionKind {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "functions";
        case 1:
            return "arrowFunctions";
        case 2:
            return "generatorFunctions";
        case 3:
            return "methods";
        case 4:
            return "generatorMethods";
        case 5:
            return "getters";
        case 6:
            return "setters";
        case 7:
            return "constructors";
        case 8:
            return "asyncFunctions";
        case 9:
            return "asyncMethods";
        case 10:
            return "overrideMethods";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one EmptyFunctionKind. */
export function toJsonEmptyFunctionKind(value: EmptyFunctionKind): Json {
    return value;
}

/** Return one EmptyFunctionKind from one JSON value. */
export function fromJsonEmptyFunctionKind(value: Json): EmptyFunctionKind {
    const variant = jsonString(value);

    switch (variant) {
        case "functions":
            return "functions";
        case "arrowFunctions":
            return "arrowFunctions";
        case "generatorFunctions":
            return "generatorFunctions";
        case "methods":
            return "methods";
        case "generatorMethods":
            return "generatorMethods";
        case "getters":
            return "getters";
        case "setters":
            return "setters";
        case "constructors":
            return "constructors";
        case "asyncFunctions":
            return "asyncFunctions";
        case "asyncMethods":
            return "asyncMethods";
        case "overrideMethods":
            return "overrideMethods";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** Return-await mode for the `return-await` rule. */
export type ReturnAwaitMode = "inTryCatch" | "errorHandlingCorrectnessOnly" | "always" | "never";

export const ReturnAwaitMode = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ReturnAwaitMode): void {
        encodeReturnAwaitMode(writer, value);
    },

    /** Decode one ReturnAwaitMode. */
    decode(reader: BinaryReader): ReturnAwaitMode {
        return decodeReturnAwaitMode(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ReturnAwaitMode): Json {
        return toJsonReturnAwaitMode(value);
    },

    /** Return one ReturnAwaitMode from one JSON value. */
    fromJson(value: Json): ReturnAwaitMode {
        return fromJsonReturnAwaitMode(value);
    },
};

/** Encode one ReturnAwaitMode. */
export function encodeReturnAwaitMode(writer: BinaryWriter, value: ReturnAwaitMode): void {
    switch (value) {
        case "inTryCatch":
            writer.writeUnsigned(0);
            return;
        case "errorHandlingCorrectnessOnly":
            writer.writeUnsigned(1);
            return;
        case "always":
            writer.writeUnsigned(2);
            return;
        case "never":
            writer.writeUnsigned(3);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one ReturnAwaitMode. */
export function decodeReturnAwaitMode(reader: BinaryReader): ReturnAwaitMode {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "inTryCatch";
        case 1:
            return "errorHandlingCorrectnessOnly";
        case 2:
            return "always";
        case 3:
            return "never";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one ReturnAwaitMode. */
export function toJsonReturnAwaitMode(value: ReturnAwaitMode): Json {
    return value;
}

/** Return one ReturnAwaitMode from one JSON value. */
export function fromJsonReturnAwaitMode(value: Json): ReturnAwaitMode {
    const variant = jsonString(value);

    switch (variant) {
        case "inTryCatch":
            return "inTryCatch";
        case "errorHandlingCorrectnessOnly":
            return "errorHandlingCorrectnessOnly";
        case "always":
            return "always";
        case "never":
            return "never";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** Required Unicode regex flag for `require-unicode-regexp`. */
export type UnicodeRegexpRequireFlag = "u" | "v";

export const UnicodeRegexpRequireFlag = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: UnicodeRegexpRequireFlag): void {
        encodeUnicodeRegexpRequireFlag(writer, value);
    },

    /** Decode one UnicodeRegexpRequireFlag. */
    decode(reader: BinaryReader): UnicodeRegexpRequireFlag {
        return decodeUnicodeRegexpRequireFlag(reader);
    },

    /** Return this value as JSON. */
    toJson(value: UnicodeRegexpRequireFlag): Json {
        return toJsonUnicodeRegexpRequireFlag(value);
    },

    /** Return one UnicodeRegexpRequireFlag from one JSON value. */
    fromJson(value: Json): UnicodeRegexpRequireFlag {
        return fromJsonUnicodeRegexpRequireFlag(value);
    },
};

/** Encode one UnicodeRegexpRequireFlag. */
export function encodeUnicodeRegexpRequireFlag(writer: BinaryWriter, value: UnicodeRegexpRequireFlag): void {
    switch (value) {
        case "u":
            writer.writeUnsigned(0);
            return;
        case "v":
            writer.writeUnsigned(1);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one UnicodeRegexpRequireFlag. */
export function decodeUnicodeRegexpRequireFlag(reader: BinaryReader): UnicodeRegexpRequireFlag {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "u";
        case 1:
            return "v";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one UnicodeRegexpRequireFlag. */
export function toJsonUnicodeRegexpRequireFlag(value: UnicodeRegexpRequireFlag): Json {
    return value;
}

/** Return one UnicodeRegexpRequireFlag from one JSON value. */
export function fromJsonUnicodeRegexpRequireFlag(value: Json): UnicodeRegexpRequireFlag {
    const variant = jsonString(value);

    switch (variant) {
        case "u":
            return "u";
        case "v":
            return "v";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** Preferred array type syntax for the `array-type` rule. */
export type ArrayTypeStyle = "array" | "generic";

export const ArrayTypeStyle = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ArrayTypeStyle): void {
        encodeArrayTypeStyle(writer, value);
    },

    /** Decode one ArrayTypeStyle. */
    decode(reader: BinaryReader): ArrayTypeStyle {
        return decodeArrayTypeStyle(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ArrayTypeStyle): Json {
        return toJsonArrayTypeStyle(value);
    },

    /** Return one ArrayTypeStyle from one JSON value. */
    fromJson(value: Json): ArrayTypeStyle {
        return fromJsonArrayTypeStyle(value);
    },
};

/** Encode one ArrayTypeStyle. */
export function encodeArrayTypeStyle(writer: BinaryWriter, value: ArrayTypeStyle): void {
    switch (value) {
        case "array":
            writer.writeUnsigned(0);
            return;
        case "generic":
            writer.writeUnsigned(1);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one ArrayTypeStyle. */
export function decodeArrayTypeStyle(reader: BinaryReader): ArrayTypeStyle {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "array";
        case 1:
            return "generic";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one ArrayTypeStyle. */
export function toJsonArrayTypeStyle(value: ArrayTypeStyle): Json {
    return value;
}

/** Return one ArrayTypeStyle from one JSON value. */
export function fromJsonArrayTypeStyle(value: Json): ArrayTypeStyle {
    const variant = jsonString(value);

    switch (variant) {
        case "array":
            return "array";
        case "generic":
            return "generic";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** Filename case style for the `filename-case` rule. */
export type FilenameCase = "kebab" | "snake" | "camel" | "pascal";

export const FilenameCase = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: FilenameCase): void {
        encodeFilenameCase(writer, value);
    },

    /** Decode one FilenameCase. */
    decode(reader: BinaryReader): FilenameCase {
        return decodeFilenameCase(reader);
    },

    /** Return this value as JSON. */
    toJson(value: FilenameCase): Json {
        return toJsonFilenameCase(value);
    },

    /** Return one FilenameCase from one JSON value. */
    fromJson(value: Json): FilenameCase {
        return fromJsonFilenameCase(value);
    },
};

/** Encode one FilenameCase. */
export function encodeFilenameCase(writer: BinaryWriter, value: FilenameCase): void {
    switch (value) {
        case "kebab":
            writer.writeUnsigned(0);
            return;
        case "snake":
            writer.writeUnsigned(1);
            return;
        case "camel":
            writer.writeUnsigned(2);
            return;
        case "pascal":
            writer.writeUnsigned(3);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one FilenameCase. */
export function decodeFilenameCase(reader: BinaryReader): FilenameCase {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "kebab";
        case 1:
            return "snake";
        case 2:
            return "camel";
        case 3:
            return "pascal";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one FilenameCase. */
export function toJsonFilenameCase(value: FilenameCase): Json {
    return value;
}

/** Return one FilenameCase from one JSON value. */
export function fromJsonFilenameCase(value: Json): FilenameCase {
    const variant = jsonString(value);

    switch (variant) {
        case "kebab":
            return "kebab";
        case "snake":
            return "snake";
        case "camel":
            return "camel";
        case "pascal":
            return "pascal";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** Ordering policy for `grouped-accessor-pairs`. */
export type GroupedAccessorPairsOrder = "anyOrder" | "getBeforeSet" | "setBeforeGet";

export const GroupedAccessorPairsOrder = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: GroupedAccessorPairsOrder): void {
        encodeGroupedAccessorPairsOrder(writer, value);
    },

    /** Decode one GroupedAccessorPairsOrder. */
    decode(reader: BinaryReader): GroupedAccessorPairsOrder {
        return decodeGroupedAccessorPairsOrder(reader);
    },

    /** Return this value as JSON. */
    toJson(value: GroupedAccessorPairsOrder): Json {
        return toJsonGroupedAccessorPairsOrder(value);
    },

    /** Return one GroupedAccessorPairsOrder from one JSON value. */
    fromJson(value: Json): GroupedAccessorPairsOrder {
        return fromJsonGroupedAccessorPairsOrder(value);
    },
};

/** Encode one GroupedAccessorPairsOrder. */
export function encodeGroupedAccessorPairsOrder(writer: BinaryWriter, value: GroupedAccessorPairsOrder): void {
    switch (value) {
        case "anyOrder":
            writer.writeUnsigned(0);
            return;
        case "getBeforeSet":
            writer.writeUnsigned(1);
            return;
        case "setBeforeGet":
            writer.writeUnsigned(2);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one GroupedAccessorPairsOrder. */
export function decodeGroupedAccessorPairsOrder(reader: BinaryReader): GroupedAccessorPairsOrder {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "anyOrder";
        case 1:
            return "getBeforeSet";
        case 2:
            return "setBeforeGet";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one GroupedAccessorPairsOrder. */
export function toJsonGroupedAccessorPairsOrder(value: GroupedAccessorPairsOrder): Json {
    return value;
}

/** Return one GroupedAccessorPairsOrder from one JSON value. */
export function fromJsonGroupedAccessorPairsOrder(value: Json): GroupedAccessorPairsOrder {
    const variant = jsonString(value);

    switch (variant) {
        case "anyOrder":
            return "anyOrder";
        case "getBeforeSet":
            return "getBeforeSet";
        case "setBeforeGet":
            return "setBeforeGet";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** Enforcement mode for `operator-assignment`. */
export type OperatorAssignmentMode = "always" | "never";

export const OperatorAssignmentMode = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: OperatorAssignmentMode): void {
        encodeOperatorAssignmentMode(writer, value);
    },

    /** Decode one OperatorAssignmentMode. */
    decode(reader: BinaryReader): OperatorAssignmentMode {
        return decodeOperatorAssignmentMode(reader);
    },

    /** Return this value as JSON. */
    toJson(value: OperatorAssignmentMode): Json {
        return toJsonOperatorAssignmentMode(value);
    },

    /** Return one OperatorAssignmentMode from one JSON value. */
    fromJson(value: Json): OperatorAssignmentMode {
        return fromJsonOperatorAssignmentMode(value);
    },
};

/** Encode one OperatorAssignmentMode. */
export function encodeOperatorAssignmentMode(writer: BinaryWriter, value: OperatorAssignmentMode): void {
    switch (value) {
        case "always":
            writer.writeUnsigned(0);
            return;
        case "never":
            writer.writeUnsigned(1);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one OperatorAssignmentMode. */
export function decodeOperatorAssignmentMode(reader: BinaryReader): OperatorAssignmentMode {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "always";
        case 1:
            return "never";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one OperatorAssignmentMode. */
export function toJsonOperatorAssignmentMode(value: OperatorAssignmentMode): Json {
    return value;
}

/** Return one OperatorAssignmentMode from one JSON value. */
export function fromJsonOperatorAssignmentMode(value: Json): OperatorAssignmentMode {
    const variant = jsonString(value);

    switch (variant) {
        case "always":
            return "always";
        case "never":
            return "never";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** Enforcement mode for `object-shorthand`. */
export type ObjectShorthandMode = "always" | "methods" | "properties" | "never" | "consistent" | "consistentAsNeeded";

export const ObjectShorthandMode = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ObjectShorthandMode): void {
        encodeObjectShorthandMode(writer, value);
    },

    /** Decode one ObjectShorthandMode. */
    decode(reader: BinaryReader): ObjectShorthandMode {
        return decodeObjectShorthandMode(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ObjectShorthandMode): Json {
        return toJsonObjectShorthandMode(value);
    },

    /** Return one ObjectShorthandMode from one JSON value. */
    fromJson(value: Json): ObjectShorthandMode {
        return fromJsonObjectShorthandMode(value);
    },
};

/** Encode one ObjectShorthandMode. */
export function encodeObjectShorthandMode(writer: BinaryWriter, value: ObjectShorthandMode): void {
    switch (value) {
        case "always":
            writer.writeUnsigned(0);
            return;
        case "methods":
            writer.writeUnsigned(1);
            return;
        case "properties":
            writer.writeUnsigned(2);
            return;
        case "never":
            writer.writeUnsigned(3);
            return;
        case "consistent":
            writer.writeUnsigned(4);
            return;
        case "consistentAsNeeded":
            writer.writeUnsigned(5);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one ObjectShorthandMode. */
export function decodeObjectShorthandMode(reader: BinaryReader): ObjectShorthandMode {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "always";
        case 1:
            return "methods";
        case 2:
            return "properties";
        case 3:
            return "never";
        case 4:
            return "consistent";
        case 5:
            return "consistentAsNeeded";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one ObjectShorthandMode. */
export function toJsonObjectShorthandMode(value: ObjectShorthandMode): Json {
    return value;
}

/** Return one ObjectShorthandMode from one JSON value. */
export function fromJsonObjectShorthandMode(value: Json): ObjectShorthandMode {
    const variant = jsonString(value);

    switch (variant) {
        case "always":
            return "always";
        case "methods":
            return "methods";
        case "properties":
            return "properties";
        case "never":
            return "never";
        case "consistent":
            return "consistent";
        case "consistentAsNeeded":
            return "consistentAsNeeded";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** Destructuring policy for `prefer-const`. */
export type PreferConstDestructuring = "any" | "all";

export const PreferConstDestructuring = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: PreferConstDestructuring): void {
        encodePreferConstDestructuring(writer, value);
    },

    /** Decode one PreferConstDestructuring. */
    decode(reader: BinaryReader): PreferConstDestructuring {
        return decodePreferConstDestructuring(reader);
    },

    /** Return this value as JSON. */
    toJson(value: PreferConstDestructuring): Json {
        return toJsonPreferConstDestructuring(value);
    },

    /** Return one PreferConstDestructuring from one JSON value. */
    fromJson(value: Json): PreferConstDestructuring {
        return fromJsonPreferConstDestructuring(value);
    },
};

/** Encode one PreferConstDestructuring. */
export function encodePreferConstDestructuring(writer: BinaryWriter, value: PreferConstDestructuring): void {
    switch (value) {
        case "any":
            writer.writeUnsigned(0);
            return;
        case "all":
            writer.writeUnsigned(1);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one PreferConstDestructuring. */
export function decodePreferConstDestructuring(reader: BinaryReader): PreferConstDestructuring {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "any";
        case 1:
            return "all";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one PreferConstDestructuring. */
export function toJsonPreferConstDestructuring(value: PreferConstDestructuring): Json {
    return value;
}

/** Return one PreferConstDestructuring from one JSON value. */
export function fromJsonPreferConstDestructuring(value: Json): PreferConstDestructuring {
    const variant = jsonString(value);

    switch (variant) {
        case "any":
            return "any";
        case "all":
            return "all";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** Enforcement mode for `yoda`. */
export type YodaMode = "always" | "never";

export const YodaMode = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: YodaMode): void {
        encodeYodaMode(writer, value);
    },

    /** Decode one YodaMode. */
    decode(reader: BinaryReader): YodaMode {
        return decodeYodaMode(reader);
    },

    /** Return this value as JSON. */
    toJson(value: YodaMode): Json {
        return toJsonYodaMode(value);
    },

    /** Return one YodaMode from one JSON value. */
    fromJson(value: Json): YodaMode {
        return fromJsonYodaMode(value);
    },
};

/** Encode one YodaMode. */
export function encodeYodaMode(writer: BinaryWriter, value: YodaMode): void {
    switch (value) {
        case "always":
            writer.writeUnsigned(0);
            return;
        case "never":
            writer.writeUnsigned(1);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one YodaMode. */
export function decodeYodaMode(reader: BinaryReader): YodaMode {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "always";
        case 1:
            return "never";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one YodaMode. */
export function toJsonYodaMode(value: YodaMode): Json {
    return value;
}

/** Return one YodaMode from one JSON value. */
export function fromJsonYodaMode(value: Json): YodaMode {
    const variant = jsonString(value);

    switch (variant) {
        case "always":
            return "always";
        case "never":
            return "never";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** Member syntax groups for `sort-imports`. */
export type SortImportsMemberSyntax = "none" | "all" | "multiple" | "single";

export const SortImportsMemberSyntax = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: SortImportsMemberSyntax): void {
        encodeSortImportsMemberSyntax(writer, value);
    },

    /** Decode one SortImportsMemberSyntax. */
    decode(reader: BinaryReader): SortImportsMemberSyntax {
        return decodeSortImportsMemberSyntax(reader);
    },

    /** Return this value as JSON. */
    toJson(value: SortImportsMemberSyntax): Json {
        return toJsonSortImportsMemberSyntax(value);
    },

    /** Return one SortImportsMemberSyntax from one JSON value. */
    fromJson(value: Json): SortImportsMemberSyntax {
        return fromJsonSortImportsMemberSyntax(value);
    },
};

/** Encode one SortImportsMemberSyntax. */
export function encodeSortImportsMemberSyntax(writer: BinaryWriter, value: SortImportsMemberSyntax): void {
    switch (value) {
        case "none":
            writer.writeUnsigned(0);
            return;
        case "all":
            writer.writeUnsigned(1);
            return;
        case "multiple":
            writer.writeUnsigned(2);
            return;
        case "single":
            writer.writeUnsigned(3);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one SortImportsMemberSyntax. */
export function decodeSortImportsMemberSyntax(reader: BinaryReader): SortImportsMemberSyntax {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "none";
        case 1:
            return "all";
        case 2:
            return "multiple";
        case 3:
            return "single";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one SortImportsMemberSyntax. */
export function toJsonSortImportsMemberSyntax(value: SortImportsMemberSyntax): Json {
    return value;
}

/** Return one SortImportsMemberSyntax from one JSON value. */
export function fromJsonSortImportsMemberSyntax(value: Json): SortImportsMemberSyntax {
    const variant = jsonString(value);

    switch (variant) {
        case "none":
            return "none";
        case "all":
            return "all";
        case "multiple":
            return "multiple";
        case "single":
            return "single";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** Switch counting variant for `cyclomatic-complexity`. */
export type CyclomaticComplexityVariant = "classic" | "modified";

export const CyclomaticComplexityVariant = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: CyclomaticComplexityVariant): void {
        encodeCyclomaticComplexityVariant(writer, value);
    },

    /** Decode one CyclomaticComplexityVariant. */
    decode(reader: BinaryReader): CyclomaticComplexityVariant {
        return decodeCyclomaticComplexityVariant(reader);
    },

    /** Return this value as JSON. */
    toJson(value: CyclomaticComplexityVariant): Json {
        return toJsonCyclomaticComplexityVariant(value);
    },

    /** Return one CyclomaticComplexityVariant from one JSON value. */
    fromJson(value: Json): CyclomaticComplexityVariant {
        return fromJsonCyclomaticComplexityVariant(value);
    },
};

/** Encode one CyclomaticComplexityVariant. */
export function encodeCyclomaticComplexityVariant(writer: BinaryWriter, value: CyclomaticComplexityVariant): void {
    switch (value) {
        case "classic":
            writer.writeUnsigned(0);
            return;
        case "modified":
            writer.writeUnsigned(1);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one CyclomaticComplexityVariant. */
export function decodeCyclomaticComplexityVariant(reader: BinaryReader): CyclomaticComplexityVariant {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "classic";
        case 1:
            return "modified";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one CyclomaticComplexityVariant. */
export function toJsonCyclomaticComplexityVariant(value: CyclomaticComplexityVariant): Json {
    return value;
}

/** Return one CyclomaticComplexityVariant from one JSON value. */
export function fromJsonCyclomaticComplexityVariant(value: Json): CyclomaticComplexityVariant {
    const variant = jsonString(value);

    switch (variant) {
        case "classic":
            return "classic";
        case "modified":
            return "modified";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** `this` parameter counting policy for `max-params`. */
export type MaxParamsCountThis = "never" | "exceptVoid" | "always";

export const MaxParamsCountThis = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: MaxParamsCountThis): void {
        encodeMaxParamsCountThis(writer, value);
    },

    /** Decode one MaxParamsCountThis. */
    decode(reader: BinaryReader): MaxParamsCountThis {
        return decodeMaxParamsCountThis(reader);
    },

    /** Return this value as JSON. */
    toJson(value: MaxParamsCountThis): Json {
        return toJsonMaxParamsCountThis(value);
    },

    /** Return one MaxParamsCountThis from one JSON value. */
    fromJson(value: Json): MaxParamsCountThis {
        return fromJsonMaxParamsCountThis(value);
    },
};

/** Encode one MaxParamsCountThis. */
export function encodeMaxParamsCountThis(writer: BinaryWriter, value: MaxParamsCountThis): void {
    switch (value) {
        case "never":
            writer.writeUnsigned(0);
            return;
        case "exceptVoid":
            writer.writeUnsigned(1);
            return;
        case "always":
            writer.writeUnsigned(2);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one MaxParamsCountThis. */
export function decodeMaxParamsCountThis(reader: BinaryReader): MaxParamsCountThis {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "never";
        case 1:
            return "exceptVoid";
        case 2:
            return "always";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one MaxParamsCountThis. */
export function toJsonMaxParamsCountThis(value: MaxParamsCountThis): Json {
    return value;
}

/** Return one MaxParamsCountThis from one JSON value. */
export function fromJsonMaxParamsCountThis(value: Json): MaxParamsCountThis {
    const variant = jsonString(value);

    switch (variant) {
        case "never":
            return "never";
        case "exceptVoid":
            return "exceptVoid";
        case "always":
            return "always";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** Bitwise operators configurable for the `no-bitwise` rule. */
export type BitwiseOperator = "and" | "xor" | "or" | "not" | "shiftLeft" | "shiftRight" | "unsignedShiftRight" | "andAssign" | "xorAssign" | "orAssign" | "shiftLeftAssign" | "shiftRightAssign" | "unsignedShiftRightAssign";

export const BitwiseOperator = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: BitwiseOperator): void {
        encodeBitwiseOperator(writer, value);
    },

    /** Decode one BitwiseOperator. */
    decode(reader: BinaryReader): BitwiseOperator {
        return decodeBitwiseOperator(reader);
    },

    /** Return this value as JSON. */
    toJson(value: BitwiseOperator): Json {
        return toJsonBitwiseOperator(value);
    },

    /** Return one BitwiseOperator from one JSON value. */
    fromJson(value: Json): BitwiseOperator {
        return fromJsonBitwiseOperator(value);
    },
};

/** Encode one BitwiseOperator. */
export function encodeBitwiseOperator(writer: BinaryWriter, value: BitwiseOperator): void {
    switch (value) {
        case "and":
            writer.writeUnsigned(0);
            return;
        case "xor":
            writer.writeUnsigned(1);
            return;
        case "or":
            writer.writeUnsigned(2);
            return;
        case "not":
            writer.writeUnsigned(3);
            return;
        case "shiftLeft":
            writer.writeUnsigned(4);
            return;
        case "shiftRight":
            writer.writeUnsigned(5);
            return;
        case "unsignedShiftRight":
            writer.writeUnsigned(6);
            return;
        case "andAssign":
            writer.writeUnsigned(7);
            return;
        case "xorAssign":
            writer.writeUnsigned(8);
            return;
        case "orAssign":
            writer.writeUnsigned(9);
            return;
        case "shiftLeftAssign":
            writer.writeUnsigned(10);
            return;
        case "shiftRightAssign":
            writer.writeUnsigned(11);
            return;
        case "unsignedShiftRightAssign":
            writer.writeUnsigned(12);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one BitwiseOperator. */
export function decodeBitwiseOperator(reader: BinaryReader): BitwiseOperator {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "and";
        case 1:
            return "xor";
        case 2:
            return "or";
        case 3:
            return "not";
        case 4:
            return "shiftLeft";
        case 5:
            return "shiftRight";
        case 6:
            return "unsignedShiftRight";
        case 7:
            return "andAssign";
        case 8:
            return "xorAssign";
        case 9:
            return "orAssign";
        case 10:
            return "shiftLeftAssign";
        case 11:
            return "shiftRightAssign";
        case 12:
            return "unsignedShiftRightAssign";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one BitwiseOperator. */
export function toJsonBitwiseOperator(value: BitwiseOperator): Json {
    return value;
}

/** Return one BitwiseOperator from one JSON value. */
export function fromJsonBitwiseOperator(value: Json): BitwiseOperator {
    const variant = jsonString(value);

    switch (variant) {
        case "and":
            return "and";
        case "xor":
            return "xor";
        case "or":
            return "or";
        case "not":
            return "not";
        case "shiftLeft":
            return "shiftLeft";
        case "shiftRight":
            return "shiftRight";
        case "unsignedShiftRight":
            return "unsignedShiftRight";
        case "andAssign":
            return "andAssign";
        case "xorAssign":
            return "xorAssign";
        case "orAssign":
            return "orAssign";
        case "shiftLeftAssign":
            return "shiftLeftAssign";
        case "shiftRightAssign":
            return "shiftRightAssign";
        case "unsignedShiftRightAssign":
            return "unsignedShiftRightAssign";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** Warning comment term matching location for `no-warning-comments`. */
export type WarningCommentLocation = "start" | "anywhere";

export const WarningCommentLocation = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: WarningCommentLocation): void {
        encodeWarningCommentLocation(writer, value);
    },

    /** Decode one WarningCommentLocation. */
    decode(reader: BinaryReader): WarningCommentLocation {
        return decodeWarningCommentLocation(reader);
    },

    /** Return this value as JSON. */
    toJson(value: WarningCommentLocation): Json {
        return toJsonWarningCommentLocation(value);
    },

    /** Return one WarningCommentLocation from one JSON value. */
    fromJson(value: Json): WarningCommentLocation {
        return fromJsonWarningCommentLocation(value);
    },
};

/** Encode one WarningCommentLocation. */
export function encodeWarningCommentLocation(writer: BinaryWriter, value: WarningCommentLocation): void {
    switch (value) {
        case "start":
            writer.writeUnsigned(0);
            return;
        case "anywhere":
            writer.writeUnsigned(1);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one WarningCommentLocation. */
export function decodeWarningCommentLocation(reader: BinaryReader): WarningCommentLocation {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "start";
        case 1:
            return "anywhere";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one WarningCommentLocation. */
export function toJsonWarningCommentLocation(value: WarningCommentLocation): Json {
    return value;
}

/** Return one WarningCommentLocation from one JSON value. */
export function fromJsonWarningCommentLocation(value: Json): WarningCommentLocation {
    const variant = jsonString(value);

    switch (variant) {
        case "start":
            return "start";
        case "anywhere":
            return "anywhere";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}
