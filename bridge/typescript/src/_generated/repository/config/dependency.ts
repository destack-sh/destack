/* generated by destack_bridge_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, compareBytes, jsonArray, jsonField, jsonObject, jsonOptional, jsonString, nestedBytes } from "../../../protocol/serde.js";
import type { ConditionRef } from "./condition.js";
import { decodeConditionRef, encodeConditionRef, fromJsonConditionRef, toJsonConditionRef } from "./condition.js";

/** Package dependency declaration. */
export type Dependency =
    /** Package resolved from the current workspace. */
    | {
          readonly kind: "workspace";
      }
    /** Package resolved from the configured package registry. */
    | {
          readonly kind: "registry";
          /** Registry name. */
          readonly registry?: string;
          /** Exact package version. */
          readonly version: string;
      }
    /** Package resolved from one local package path. */
    | {
          readonly kind: "path";
          /** Package path. */
          readonly path: string;
      }
    /** Package resolved from one Git repository. */
    | {
          readonly kind: "git";
          /** Repository URL. */
          readonly url: string;
          /** Repository subdirectory containing the package. */
          readonly path?: string;
          /** Exact commit revision. */
          readonly rev?: string;
          /** Git tag selector. */
          readonly tag?: string;
          /** Git branch selector. */
          readonly branch?: string;
      }
;

export const Dependency = {
    /** Package resolved from the current workspace. */
    workspace(): Dependency {
        return { kind: "workspace" };
    },

    /** Package resolved from the configured package registry. */
    registry(registry: string | undefined, version: string): Dependency {
        return { kind: "registry", registry, version };
    },

    /** Package resolved from one local package path. */
    path(path: string): Dependency {
        return { kind: "path", path };
    },

    /** Package resolved from one Git repository. */
    git(url: string, path: string | undefined, rev: string | undefined, tag: string | undefined, branch: string | undefined): Dependency {
        return { kind: "git", url, path, rev, tag, branch };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: Dependency): void {
        encodeDependency(writer, value);
    },

    /** Decode one Dependency. */
    decode(reader: BinaryReader): Dependency {
        return decodeDependency(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Dependency): Json {
        return toJsonDependency(value);
    },

    /** Return one Dependency from one JSON value. */
    fromJson(value: Json): Dependency {
        return fromJsonDependency(value);
    },
};

/** Encode one Dependency. */
export function encodeDependency(writer: BinaryWriter, value: Dependency): void {
    switch (value.kind) {
        case "workspace":
            writer.writeUnsigned(0);
            return;
        case "registry":
            writer.writeUnsigned(1);
            writer.writeOption(value.registry, (value0) => {
                writer.writeString(value0);
            });
            writer.writeString(value.version);
            return;
        case "path":
            writer.writeUnsigned(2);
            writer.writeString(value.path);
            return;
        case "git":
            writer.writeUnsigned(3);
            writer.writeString(value.url);
            writer.writeOption(value.path, (value1) => {
                writer.writeString(value1);
            });
            writer.writeOption(value.rev, (value2) => {
                writer.writeString(value2);
            });
            writer.writeOption(value.tag, (value3) => {
                writer.writeString(value3);
            });
            writer.writeOption(value.branch, (value4) => {
                writer.writeString(value4);
            });
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one Dependency. */
export function decodeDependency(reader: BinaryReader): Dependency {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            return { kind: "workspace" };
        }
        case 1: {
            const registry = reader.readOption(() => reader.readString());
            const version = reader.readString();

            return {
                kind: "registry",
                ...(registry === undefined ? {} : { registry }),
                version,
            };
        }
        case 2: {
            const path = reader.readString();

            return {
                kind: "path",
                path,
            };
        }
        case 3: {
            const url = reader.readString();
            const path = reader.readOption(() => reader.readString());
            const rev = reader.readOption(() => reader.readString());
            const tag = reader.readOption(() => reader.readString());
            const branch = reader.readOption(() => reader.readString());

            return {
                kind: "git",
                url,
                ...(path === undefined ? {} : { path }),
                ...(rev === undefined ? {} : { rev }),
                ...(tag === undefined ? {} : { tag }),
                ...(branch === undefined ? {} : { branch }),
            };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one Dependency. */
export function toJsonDependency(value: Dependency): Json {
    switch (value.kind) {
        case "workspace":
            return {
                kind: "workspace",
            };
        case "registry":
            return {
                kind: "registry",
                ...(value.registry === undefined ? {} : { registry: value.registry }),
                version: value.version,
            };
        case "path":
            return {
                kind: "path",
                path: value.path,
            };
        case "git":
            return {
                kind: "git",
                url: value.url,
                ...(value.path === undefined ? {} : { path: value.path }),
                ...(value.rev === undefined ? {} : { rev: value.rev }),
                ...(value.tag === undefined ? {} : { tag: value.tag }),
                ...(value.branch === undefined ? {} : { branch: value.branch }),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one Dependency from one JSON value. */
export function fromJsonDependency(value: Json): Dependency {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "workspace":
            return {
                kind,
            };
        case "registry":
            return {
                kind,
                registry: jsonOptional(object, "registry", (value) => jsonString(value)),
                version: jsonString(jsonField(object, "version")),
            };
        case "path":
            return {
                kind,
                path: jsonString(jsonField(object, "path")),
            };
        case "git":
            return {
                kind,
                url: jsonString(jsonField(object, "url")),
                path: jsonOptional(object, "path", (value) => jsonString(value)),
                rev: jsonOptional(object, "rev", (value) => jsonString(value)),
                tag: jsonOptional(object, "tag", (value) => jsonString(value)),
                branch: jsonOptional(object, "branch", (value) => jsonString(value)),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** Dependency declarations guarded by one active condition predicate. */
export type ConditionalDependencies = {
    /** Condition predicate enabling these dependencies. */
    readonly when: ConditionRef;
    /** Dependency declarations enabled when the predicate matches. */
    readonly dependencies: Readonly<Record<string, Dependency>>;
};

export const ConditionalDependencies = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ConditionalDependencies): void {
        encodeConditionalDependencies(writer, value);
    },

    /** Decode one ConditionalDependencies. */
    decode(reader: BinaryReader): ConditionalDependencies {
        return decodeConditionalDependencies(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ConditionalDependencies): Json {
        return toJsonConditionalDependencies(value);
    },

    /** Return one ConditionalDependencies from one JSON value. */
    fromJson(value: Json): ConditionalDependencies {
        return fromJsonConditionalDependencies(value);
    },
};

/** Encode one ConditionalDependencies. */
export function encodeConditionalDependencies(writer: BinaryWriter, value: ConditionalDependencies): void {
    encodeConditionRef(writer, value.when);
    const entries1 = Object.entries(value.dependencies).map(([key1, item1]) => {
        const keyBytes = nestedBytes((writer) => {
            writer.writeString(key1);
        });
        return { key1, item1, keyBytes };
    });
    entries1.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries1.length);
    for (const entry1 of entries1) {
        writer.writeString(entry1.key1);
        encodeDependency(writer, entry1.item1);
    }
}

/** Decode one ConditionalDependencies. */
export function decodeConditionalDependencies(reader: BinaryReader): ConditionalDependencies {
    const when = decodeConditionRef(reader);
    const dependencies = (() => { const length1 = reader.readNumber(); const items1: Record<string, Dependency> = {}; for (let index = 0; index < length1; index += 1) { const key = reader.readString(); items1[key] = decodeDependency(reader); } return items1; })();

    return {
        when,
        dependencies,
    };
}

/** Return one JSON value for one ConditionalDependencies. */
export function toJsonConditionalDependencies(value: ConditionalDependencies): Json {
    return {
        when: toJsonConditionRef(value.when),
        dependencies: Object.fromEntries(Object.entries(value.dependencies).map(([key0, item0]) => [key0, toJsonDependency(item0)] as const)),
    };
}

/** Return one ConditionalDependencies from one JSON value. */
export function fromJsonConditionalDependencies(value: Json): ConditionalDependencies {
    const object = jsonObject(value);

    return {
        when: fromJsonConditionRef(jsonField(object, "when")),
        dependencies: Object.fromEntries(Object.entries(jsonObject(jsonField(object, "dependencies"))).map(([key0, item0]) => [key0, fromJsonDependency(item0)] as const)),
    };
}

/** Patch file applied to one resolved package. */
export type PackagePatch = {
    /** Patch file path. */
    readonly path: string;
};

export const PackagePatch = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: PackagePatch): void {
        encodePackagePatch(writer, value);
    },

    /** Decode one PackagePatch. */
    decode(reader: BinaryReader): PackagePatch {
        return decodePackagePatch(reader);
    },

    /** Return this value as JSON. */
    toJson(value: PackagePatch): Json {
        return toJsonPackagePatch(value);
    },

    /** Return one PackagePatch from one JSON value. */
    fromJson(value: Json): PackagePatch {
        return fromJsonPackagePatch(value);
    },
};

/** Encode one PackagePatch. */
export function encodePackagePatch(writer: BinaryWriter, value: PackagePatch): void {
    writer.writeString(value.path);
}

/** Decode one PackagePatch. */
export function decodePackagePatch(reader: BinaryReader): PackagePatch {
    const path = reader.readString();

    return {
        path,
    };
}

/** Return one JSON value for one PackagePatch. */
export function toJsonPackagePatch(value: PackagePatch): Json {
    return {
        path: value.path,
    };
}

/** Return one PackagePatch from one JSON value. */
export function fromJsonPackagePatch(value: Json): PackagePatch {
    const object = jsonObject(value);

    return {
        path: jsonString(jsonField(object, "path")),
    };
}
