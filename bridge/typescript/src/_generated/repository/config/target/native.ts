/* generated by destack_bridge_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, jsonArray, jsonField, jsonObject, jsonOptional, jsonString } from "../../../../protocol/serde.js";
import type { TargetAbi } from "../../../artifact/core/target.js";
import type { TargetArch } from "../../../artifact/core/target.js";
import type { TargetVendor } from "../../../artifact/core/target.js";
import { decodeTargetAbi, encodeTargetAbi, fromJsonTargetAbi, toJsonTargetAbi } from "../../../artifact/core/target.js";
import { decodeTargetArch, encodeTargetArch, fromJsonTargetArch, toJsonTargetArch } from "../../../artifact/core/target.js";
import { decodeTargetVendor, encodeTargetVendor, fromJsonTargetVendor, toJsonTargetVendor } from "../../../artifact/core/target.js";

/** Native target configuration. */
export type TargetNativeOptions = {
    /** Native output shape. */
    readonly output: NativeOutputKind;
    /** Target architecture for native codegen. */
    readonly arch?: TargetArch;
    /** Target vendor for native codegen. */
    readonly vendor?: TargetVendor;
    /** Target ABI for native codegen. */
    readonly abi?: TargetAbi;
    /** CPU name for native codegen. */
    readonly cpu?: string;
    /** CPU feature flags for native codegen. */
    readonly cpuFeatures: ReadonlyArray<string>;
    /** Sysroot path for native toolchains. */
    readonly sysroot?: string;
    /** C runtime linkage policy. */
    readonly crt: CrtLinkage;
    /** Native linker configuration. */
    readonly link: TargetLinkOptions;
};

export const TargetNativeOptions = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: TargetNativeOptions): void {
        encodeTargetNativeOptions(writer, value);
    },

    /** Decode one TargetNativeOptions. */
    decode(reader: BinaryReader): TargetNativeOptions {
        return decodeTargetNativeOptions(reader);
    },

    /** Return this value as JSON. */
    toJson(value: TargetNativeOptions): Json {
        return toJsonTargetNativeOptions(value);
    },

    /** Return one TargetNativeOptions from one JSON value. */
    fromJson(value: Json): TargetNativeOptions {
        return fromJsonTargetNativeOptions(value);
    },
};

/** Encode one TargetNativeOptions. */
export function encodeTargetNativeOptions(writer: BinaryWriter, value: TargetNativeOptions): void {
    encodeNativeOutputKind(writer, value.output);
    writer.writeOption(value.arch, (value1) => {
        encodeTargetArch(writer, value1);
    });
    writer.writeOption(value.vendor, (value2) => {
        encodeTargetVendor(writer, value2);
    });
    writer.writeOption(value.abi, (value3) => {
        encodeTargetAbi(writer, value3);
    });
    writer.writeOption(value.cpu, (value4) => {
        writer.writeString(value4);
    });
    writer.writeUnsigned(value.cpuFeatures.length);
    for (const item5 of value.cpuFeatures) {
        writer.writeString(item5);
    }
    writer.writeOption(value.sysroot, (value6) => {
        writer.writeString(value6);
    });
    encodeCrtLinkage(writer, value.crt);
    encodeTargetLinkOptions(writer, value.link);
}

/** Decode one TargetNativeOptions. */
export function decodeTargetNativeOptions(reader: BinaryReader): TargetNativeOptions {
    const output = decodeNativeOutputKind(reader);
    const arch = reader.readOption(() => decodeTargetArch(reader));
    const vendor = reader.readOption(() => decodeTargetVendor(reader));
    const abi = reader.readOption(() => decodeTargetAbi(reader));
    const cpu = reader.readOption(() => reader.readString());
    const cpuFeatures = (() => { const length5 = reader.readNumber(); const items5: Array<string> = []; for (let index = 0; index < length5; index += 1) { items5.push(reader.readString()); } return items5; })();
    const sysroot = reader.readOption(() => reader.readString());
    const crt = decodeCrtLinkage(reader);
    const link = decodeTargetLinkOptions(reader);

    return {
        output,
        ...(arch === undefined ? {} : { arch }),
        ...(vendor === undefined ? {} : { vendor }),
        ...(abi === undefined ? {} : { abi }),
        ...(cpu === undefined ? {} : { cpu }),
        cpuFeatures,
        ...(sysroot === undefined ? {} : { sysroot }),
        crt,
        link,
    };
}

/** Return one JSON value for one TargetNativeOptions. */
export function toJsonTargetNativeOptions(value: TargetNativeOptions): Json {
    return {
        output: toJsonNativeOutputKind(value.output),
        ...(value.arch === undefined ? {} : { arch: toJsonTargetArch(value.arch) }),
        ...(value.vendor === undefined ? {} : { vendor: toJsonTargetVendor(value.vendor) }),
        ...(value.abi === undefined ? {} : { abi: toJsonTargetAbi(value.abi) }),
        ...(value.cpu === undefined ? {} : { cpu: value.cpu }),
        cpuFeatures: value.cpuFeatures.map((item0) => item0),
        ...(value.sysroot === undefined ? {} : { sysroot: value.sysroot }),
        crt: toJsonCrtLinkage(value.crt),
        link: toJsonTargetLinkOptions(value.link),
    };
}

/** Return one TargetNativeOptions from one JSON value. */
export function fromJsonTargetNativeOptions(value: Json): TargetNativeOptions {
    const object = jsonObject(value);

    return {
        output: fromJsonNativeOutputKind(jsonField(object, "output")),
        arch: jsonOptional(object, "arch", (value) => fromJsonTargetArch(value)),
        vendor: jsonOptional(object, "vendor", (value) => fromJsonTargetVendor(value)),
        abi: jsonOptional(object, "abi", (value) => fromJsonTargetAbi(value)),
        cpu: jsonOptional(object, "cpu", (value) => jsonString(value)),
        cpuFeatures: jsonArray(jsonField(object, "cpuFeatures")).map((item0) => jsonString(item0)),
        sysroot: jsonOptional(object, "sysroot", (value) => jsonString(value)),
        crt: fromJsonCrtLinkage(jsonField(object, "crt")),
        link: fromJsonTargetLinkOptions(jsonField(object, "link")),
    };
}

/** Native output kind for one target. */
export type NativeOutputKind = "executable" | "staticLibrary" | "sharedLibrary";

export const NativeOutputKind = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: NativeOutputKind): void {
        encodeNativeOutputKind(writer, value);
    },

    /** Decode one NativeOutputKind. */
    decode(reader: BinaryReader): NativeOutputKind {
        return decodeNativeOutputKind(reader);
    },

    /** Return this value as JSON. */
    toJson(value: NativeOutputKind): Json {
        return toJsonNativeOutputKind(value);
    },

    /** Return one NativeOutputKind from one JSON value. */
    fromJson(value: Json): NativeOutputKind {
        return fromJsonNativeOutputKind(value);
    },
};

/** Encode one NativeOutputKind. */
export function encodeNativeOutputKind(writer: BinaryWriter, value: NativeOutputKind): void {
    switch (value) {
        case "executable":
            writer.writeUnsigned(0);
            return;
        case "staticLibrary":
            writer.writeUnsigned(1);
            return;
        case "sharedLibrary":
            writer.writeUnsigned(2);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one NativeOutputKind. */
export function decodeNativeOutputKind(reader: BinaryReader): NativeOutputKind {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "executable";
        case 1:
            return "staticLibrary";
        case 2:
            return "sharedLibrary";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one NativeOutputKind. */
export function toJsonNativeOutputKind(value: NativeOutputKind): Json {
    return value;
}

/** Return one NativeOutputKind from one JSON value. */
export function fromJsonNativeOutputKind(value: Json): NativeOutputKind {
    const variant = jsonString(value);

    switch (variant) {
        case "executable":
            return "executable";
        case "staticLibrary":
            return "staticLibrary";
        case "sharedLibrary":
            return "sharedLibrary";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** C runtime linkage policy for one native target. */
export type CrtLinkage = "default" | "dynamic" | "static";

export const CrtLinkage = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: CrtLinkage): void {
        encodeCrtLinkage(writer, value);
    },

    /** Decode one CrtLinkage. */
    decode(reader: BinaryReader): CrtLinkage {
        return decodeCrtLinkage(reader);
    },

    /** Return this value as JSON. */
    toJson(value: CrtLinkage): Json {
        return toJsonCrtLinkage(value);
    },

    /** Return one CrtLinkage from one JSON value. */
    fromJson(value: Json): CrtLinkage {
        return fromJsonCrtLinkage(value);
    },
};

/** Encode one CrtLinkage. */
export function encodeCrtLinkage(writer: BinaryWriter, value: CrtLinkage): void {
    switch (value) {
        case "default":
            writer.writeUnsigned(0);
            return;
        case "dynamic":
            writer.writeUnsigned(1);
            return;
        case "static":
            writer.writeUnsigned(2);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one CrtLinkage. */
export function decodeCrtLinkage(reader: BinaryReader): CrtLinkage {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "default";
        case 1:
            return "dynamic";
        case 2:
            return "static";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one CrtLinkage. */
export function toJsonCrtLinkage(value: CrtLinkage): Json {
    return value;
}

/** Return one CrtLinkage from one JSON value. */
export function fromJsonCrtLinkage(value: Json): CrtLinkage {
    const variant = jsonString(value);

    switch (variant) {
        case "default":
            return "default";
        case "dynamic":
            return "dynamic";
        case "static":
            return "static";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** Target native linker configuration. */
export type TargetLinkOptions = {
    /** Explicit linker executable. */
    readonly linker?: string;
    /** Extra linker arguments. */
    readonly args: ReadonlyArray<string>;
    /** Additional library search paths. */
    readonly libraryPaths: ReadonlyArray<string>;
    /** Additional libraries to link. */
    readonly libraries: ReadonlyArray<string>;
    /** Additional framework search paths. */
    readonly frameworkPaths: ReadonlyArray<string>;
    /** Additional frameworks to link. */
    readonly frameworks: ReadonlyArray<string>;
    /** Runtime dynamic library search paths. */
    readonly runtimeLibraryPaths: ReadonlyArray<string>;
    /** Symbol visibility policy. */
    readonly symbolVisibility: SymbolVisibility;
    /** Version script for exported symbols. */
    readonly versionScript?: string;
    /** Linker script for the final link. */
    readonly linkerScript?: string;
    /** Position independent code policy. */
    readonly positionIndependent: PositionIndependentMode;
    /** Shared object soname. */
    readonly soname?: string;
    /** Darwin install name. */
    readonly installName?: string;
};

export const TargetLinkOptions = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: TargetLinkOptions): void {
        encodeTargetLinkOptions(writer, value);
    },

    /** Decode one TargetLinkOptions. */
    decode(reader: BinaryReader): TargetLinkOptions {
        return decodeTargetLinkOptions(reader);
    },

    /** Return this value as JSON. */
    toJson(value: TargetLinkOptions): Json {
        return toJsonTargetLinkOptions(value);
    },

    /** Return one TargetLinkOptions from one JSON value. */
    fromJson(value: Json): TargetLinkOptions {
        return fromJsonTargetLinkOptions(value);
    },
};

/** Encode one TargetLinkOptions. */
export function encodeTargetLinkOptions(writer: BinaryWriter, value: TargetLinkOptions): void {
    writer.writeOption(value.linker, (value0) => {
        writer.writeString(value0);
    });
    writer.writeUnsigned(value.args.length);
    for (const item1 of value.args) {
        writer.writeString(item1);
    }
    writer.writeUnsigned(value.libraryPaths.length);
    for (const item2 of value.libraryPaths) {
        writer.writeString(item2);
    }
    writer.writeUnsigned(value.libraries.length);
    for (const item3 of value.libraries) {
        writer.writeString(item3);
    }
    writer.writeUnsigned(value.frameworkPaths.length);
    for (const item4 of value.frameworkPaths) {
        writer.writeString(item4);
    }
    writer.writeUnsigned(value.frameworks.length);
    for (const item5 of value.frameworks) {
        writer.writeString(item5);
    }
    writer.writeUnsigned(value.runtimeLibraryPaths.length);
    for (const item6 of value.runtimeLibraryPaths) {
        writer.writeString(item6);
    }
    encodeSymbolVisibility(writer, value.symbolVisibility);
    writer.writeOption(value.versionScript, (value8) => {
        writer.writeString(value8);
    });
    writer.writeOption(value.linkerScript, (value9) => {
        writer.writeString(value9);
    });
    encodePositionIndependentMode(writer, value.positionIndependent);
    writer.writeOption(value.soname, (value11) => {
        writer.writeString(value11);
    });
    writer.writeOption(value.installName, (value12) => {
        writer.writeString(value12);
    });
}

/** Decode one TargetLinkOptions. */
export function decodeTargetLinkOptions(reader: BinaryReader): TargetLinkOptions {
    const linker = reader.readOption(() => reader.readString());
    const args = (() => { const length1 = reader.readNumber(); const items1: Array<string> = []; for (let index = 0; index < length1; index += 1) { items1.push(reader.readString()); } return items1; })();
    const libraryPaths = (() => { const length2 = reader.readNumber(); const items2: Array<string> = []; for (let index = 0; index < length2; index += 1) { items2.push(reader.readString()); } return items2; })();
    const libraries = (() => { const length3 = reader.readNumber(); const items3: Array<string> = []; for (let index = 0; index < length3; index += 1) { items3.push(reader.readString()); } return items3; })();
    const frameworkPaths = (() => { const length4 = reader.readNumber(); const items4: Array<string> = []; for (let index = 0; index < length4; index += 1) { items4.push(reader.readString()); } return items4; })();
    const frameworks = (() => { const length5 = reader.readNumber(); const items5: Array<string> = []; for (let index = 0; index < length5; index += 1) { items5.push(reader.readString()); } return items5; })();
    const runtimeLibraryPaths = (() => { const length6 = reader.readNumber(); const items6: Array<string> = []; for (let index = 0; index < length6; index += 1) { items6.push(reader.readString()); } return items6; })();
    const symbolVisibility = decodeSymbolVisibility(reader);
    const versionScript = reader.readOption(() => reader.readString());
    const linkerScript = reader.readOption(() => reader.readString());
    const positionIndependent = decodePositionIndependentMode(reader);
    const soname = reader.readOption(() => reader.readString());
    const installName = reader.readOption(() => reader.readString());

    return {
        ...(linker === undefined ? {} : { linker }),
        args,
        libraryPaths,
        libraries,
        frameworkPaths,
        frameworks,
        runtimeLibraryPaths,
        symbolVisibility,
        ...(versionScript === undefined ? {} : { versionScript }),
        ...(linkerScript === undefined ? {} : { linkerScript }),
        positionIndependent,
        ...(soname === undefined ? {} : { soname }),
        ...(installName === undefined ? {} : { installName }),
    };
}

/** Return one JSON value for one TargetLinkOptions. */
export function toJsonTargetLinkOptions(value: TargetLinkOptions): Json {
    return {
        ...(value.linker === undefined ? {} : { linker: value.linker }),
        args: value.args.map((item0) => item0),
        libraryPaths: value.libraryPaths.map((item0) => item0),
        libraries: value.libraries.map((item0) => item0),
        frameworkPaths: value.frameworkPaths.map((item0) => item0),
        frameworks: value.frameworks.map((item0) => item0),
        runtimeLibraryPaths: value.runtimeLibraryPaths.map((item0) => item0),
        symbolVisibility: toJsonSymbolVisibility(value.symbolVisibility),
        ...(value.versionScript === undefined ? {} : { versionScript: value.versionScript }),
        ...(value.linkerScript === undefined ? {} : { linkerScript: value.linkerScript }),
        positionIndependent: toJsonPositionIndependentMode(value.positionIndependent),
        ...(value.soname === undefined ? {} : { soname: value.soname }),
        ...(value.installName === undefined ? {} : { installName: value.installName }),
    };
}

/** Return one TargetLinkOptions from one JSON value. */
export function fromJsonTargetLinkOptions(value: Json): TargetLinkOptions {
    const object = jsonObject(value);

    return {
        linker: jsonOptional(object, "linker", (value) => jsonString(value)),
        args: jsonArray(jsonField(object, "args")).map((item0) => jsonString(item0)),
        libraryPaths: jsonArray(jsonField(object, "libraryPaths")).map((item0) => jsonString(item0)),
        libraries: jsonArray(jsonField(object, "libraries")).map((item0) => jsonString(item0)),
        frameworkPaths: jsonArray(jsonField(object, "frameworkPaths")).map((item0) => jsonString(item0)),
        frameworks: jsonArray(jsonField(object, "frameworks")).map((item0) => jsonString(item0)),
        runtimeLibraryPaths: jsonArray(jsonField(object, "runtimeLibraryPaths")).map((item0) => jsonString(item0)),
        symbolVisibility: fromJsonSymbolVisibility(jsonField(object, "symbolVisibility")),
        versionScript: jsonOptional(object, "versionScript", (value) => jsonString(value)),
        linkerScript: jsonOptional(object, "linkerScript", (value) => jsonString(value)),
        positionIndependent: fromJsonPositionIndependentMode(jsonField(object, "positionIndependent")),
        soname: jsonOptional(object, "soname", (value) => jsonString(value)),
        installName: jsonOptional(object, "installName", (value) => jsonString(value)),
    };
}

/** Symbol visibility policy for one native target. */
export type SymbolVisibility = "default" | "hidden" | "protected";

export const SymbolVisibility = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: SymbolVisibility): void {
        encodeSymbolVisibility(writer, value);
    },

    /** Decode one SymbolVisibility. */
    decode(reader: BinaryReader): SymbolVisibility {
        return decodeSymbolVisibility(reader);
    },

    /** Return this value as JSON. */
    toJson(value: SymbolVisibility): Json {
        return toJsonSymbolVisibility(value);
    },

    /** Return one SymbolVisibility from one JSON value. */
    fromJson(value: Json): SymbolVisibility {
        return fromJsonSymbolVisibility(value);
    },
};

/** Encode one SymbolVisibility. */
export function encodeSymbolVisibility(writer: BinaryWriter, value: SymbolVisibility): void {
    switch (value) {
        case "default":
            writer.writeUnsigned(0);
            return;
        case "hidden":
            writer.writeUnsigned(1);
            return;
        case "protected":
            writer.writeUnsigned(2);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one SymbolVisibility. */
export function decodeSymbolVisibility(reader: BinaryReader): SymbolVisibility {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "default";
        case 1:
            return "hidden";
        case 2:
            return "protected";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one SymbolVisibility. */
export function toJsonSymbolVisibility(value: SymbolVisibility): Json {
    return value;
}

/** Return one SymbolVisibility from one JSON value. */
export function fromJsonSymbolVisibility(value: Json): SymbolVisibility {
    const variant = jsonString(value);

    switch (variant) {
        case "default":
            return "default";
        case "hidden":
            return "hidden";
        case "protected":
            return "protected";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** Position independent code policy for one native target. */
export type PositionIndependentMode = "default" | "disabled" | "pie" | "staticPie";

export const PositionIndependentMode = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: PositionIndependentMode): void {
        encodePositionIndependentMode(writer, value);
    },

    /** Decode one PositionIndependentMode. */
    decode(reader: BinaryReader): PositionIndependentMode {
        return decodePositionIndependentMode(reader);
    },

    /** Return this value as JSON. */
    toJson(value: PositionIndependentMode): Json {
        return toJsonPositionIndependentMode(value);
    },

    /** Return one PositionIndependentMode from one JSON value. */
    fromJson(value: Json): PositionIndependentMode {
        return fromJsonPositionIndependentMode(value);
    },
};

/** Encode one PositionIndependentMode. */
export function encodePositionIndependentMode(writer: BinaryWriter, value: PositionIndependentMode): void {
    switch (value) {
        case "default":
            writer.writeUnsigned(0);
            return;
        case "disabled":
            writer.writeUnsigned(1);
            return;
        case "pie":
            writer.writeUnsigned(2);
            return;
        case "staticPie":
            writer.writeUnsigned(3);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one PositionIndependentMode. */
export function decodePositionIndependentMode(reader: BinaryReader): PositionIndependentMode {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "default";
        case 1:
            return "disabled";
        case 2:
            return "pie";
        case 3:
            return "staticPie";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one PositionIndependentMode. */
export function toJsonPositionIndependentMode(value: PositionIndependentMode): Json {
    return value;
}

/** Return one PositionIndependentMode from one JSON value. */
export function fromJsonPositionIndependentMode(value: Json): PositionIndependentMode {
    const variant = jsonString(value);

    switch (variant) {
        case "default":
            return "default";
        case "disabled":
            return "disabled";
        case "pie":
            return "pie";
        case "staticPie":
            return "staticPie";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}
