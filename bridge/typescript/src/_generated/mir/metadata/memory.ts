/* generated by destack_bridge_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, compareBytes, jsonArray, jsonBigint, jsonBool, jsonField, jsonInteger, jsonObject, jsonOptional, jsonString, nestedBytes } from "../../../protocol/serde.js";
import type { MemoryFlags } from "../tree/memory.js";
import type { MemoryOrdering } from "../tree/memory.js";
import type { MemoryScope } from "../tree/memory.js";
import type { SyncScope } from "../tree/memory.js";
import type { LocalNodeId } from "../tree/node.js";
import type { Space } from "../tree/type.js";
import type { Value } from "../tree/value.js";
import { decodeMemoryFlags, encodeMemoryFlags, fromJsonMemoryFlags, toJsonMemoryFlags } from "../tree/memory.js";
import { decodeMemoryOrdering, encodeMemoryOrdering, fromJsonMemoryOrdering, toJsonMemoryOrdering } from "../tree/memory.js";
import { decodeMemoryScope, encodeMemoryScope, fromJsonMemoryScope, toJsonMemoryScope } from "../tree/memory.js";
import { decodeSyncScope, encodeSyncScope, fromJsonSyncScope, toJsonSyncScope } from "../tree/memory.js";
import { decodeLocalNodeId, encodeLocalNodeId, fromJsonLocalNodeId, toJsonLocalNodeId } from "../tree/node.js";
import { decodeSpace, encodeSpace, fromJsonSpace, toJsonSpace } from "../tree/type.js";
import { decodeValue, encodeValue, fromJsonValue, toJsonValue } from "../tree/value.js";

import { MemoryMetadataImpl } from "../../../_impl/mir/metadata/memory.js";

/** Allocation size information for functions returning newly allocated memory. */
export type AllocationSize = {
    /** The parameter index containing the element size in bytes. */
    readonly strideIndex: number;
    /** The parameter index containing the element count, if any. */
    readonly elementCountIndex?: number;
};

export const AllocationSize = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: AllocationSize): void {
        encodeAllocationSize(writer, value);
    },

    /** Decode one AllocationSize. */
    decode(reader: BinaryReader): AllocationSize {
        return decodeAllocationSize(reader);
    },

    /** Return this value as JSON. */
    toJson(value: AllocationSize): Json {
        return toJsonAllocationSize(value);
    },

    /** Return one AllocationSize from one JSON value. */
    fromJson(value: Json): AllocationSize {
        return fromJsonAllocationSize(value);
    },
};

/** Encode one AllocationSize. */
export function encodeAllocationSize(writer: BinaryWriter, value: AllocationSize): void {
    writer.writeUnsigned(value.strideIndex);
    writer.writeOption(value.elementCountIndex, (value1) => {
        writer.writeUnsigned(value1);
    });
}

/** Decode one AllocationSize. */
export function decodeAllocationSize(reader: BinaryReader): AllocationSize {
    const strideIndex = reader.readNumber();
    const elementCountIndex = reader.readOption(() => reader.readNumber());

    return {
        strideIndex,
        ...(elementCountIndex === undefined ? {} : { elementCountIndex }),
    };
}

/** Return one JSON value for one AllocationSize. */
export function toJsonAllocationSize(value: AllocationSize): Json {
    return {
        strideIndex: value.strideIndex,
        ...(value.elementCountIndex === undefined ? {} : { elementCountIndex: value.elementCountIndex }),
    };
}

/** Return one AllocationSize from one JSON value. */
export function fromJsonAllocationSize(value: Json): AllocationSize {
    const object = jsonObject(value);

    return {
        strideIndex: jsonInteger(jsonField(object, "strideIndex")),
        elementCountIndex: jsonOptional(object, "elementCountIndex", (value) => jsonInteger(value)),
    };
}

/** Memory behavior for one call argument. */
export type CallArgumentEffect = {
    /** Access mode for this argument. */
    readonly access: ArgumentAccess;
    /** Escape behavior for this argument. */
    readonly escape: ArgumentEscape;
};

export const CallArgumentEffect = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: CallArgumentEffect): void {
        encodeCallArgumentEffect(writer, value);
    },

    /** Decode one CallArgumentEffect. */
    decode(reader: BinaryReader): CallArgumentEffect {
        return decodeCallArgumentEffect(reader);
    },

    /** Return this value as JSON. */
    toJson(value: CallArgumentEffect): Json {
        return toJsonCallArgumentEffect(value);
    },

    /** Return one CallArgumentEffect from one JSON value. */
    fromJson(value: Json): CallArgumentEffect {
        return fromJsonCallArgumentEffect(value);
    },
};

/** Encode one CallArgumentEffect. */
export function encodeCallArgumentEffect(writer: BinaryWriter, value: CallArgumentEffect): void {
    encodeArgumentAccess(writer, value.access);
    encodeArgumentEscape(writer, value.escape);
}

/** Decode one CallArgumentEffect. */
export function decodeCallArgumentEffect(reader: BinaryReader): CallArgumentEffect {
    const access = decodeArgumentAccess(reader);
    const escape = decodeArgumentEscape(reader);

    return {
        access,
        escape,
    };
}

/** Return one JSON value for one CallArgumentEffect. */
export function toJsonCallArgumentEffect(value: CallArgumentEffect): Json {
    return {
        access: toJsonArgumentAccess(value.access),
        escape: toJsonArgumentEscape(value.escape),
    };
}

/** Return one CallArgumentEffect from one JSON value. */
export function fromJsonCallArgumentEffect(value: Json): CallArgumentEffect {
    const object = jsonObject(value);

    return {
        access: fromJsonArgumentAccess(jsonField(object, "access")),
        escape: fromJsonArgumentEscape(jsonField(object, "escape")),
    };
}

/** Access mode for a pointer argument. */
export type ArgumentAccess = "none" | "read" | "write" | "readWrite";

export const ArgumentAccess = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ArgumentAccess): void {
        encodeArgumentAccess(writer, value);
    },

    /** Decode one ArgumentAccess. */
    decode(reader: BinaryReader): ArgumentAccess {
        return decodeArgumentAccess(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ArgumentAccess): Json {
        return toJsonArgumentAccess(value);
    },

    /** Return one ArgumentAccess from one JSON value. */
    fromJson(value: Json): ArgumentAccess {
        return fromJsonArgumentAccess(value);
    },
};

/** Encode one ArgumentAccess. */
export function encodeArgumentAccess(writer: BinaryWriter, value: ArgumentAccess): void {
    switch (value) {
        case "none":
            writer.writeUnsigned(0);
            return;
        case "read":
            writer.writeUnsigned(1);
            return;
        case "write":
            writer.writeUnsigned(2);
            return;
        case "readWrite":
            writer.writeUnsigned(3);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one ArgumentAccess. */
export function decodeArgumentAccess(reader: BinaryReader): ArgumentAccess {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "none";
        case 1:
            return "read";
        case 2:
            return "write";
        case 3:
            return "readWrite";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one ArgumentAccess. */
export function toJsonArgumentAccess(value: ArgumentAccess): Json {
    return value;
}

/** Return one ArgumentAccess from one JSON value. */
export function fromJsonArgumentAccess(value: Json): ArgumentAccess {
    const variant = jsonString(value);

    switch (variant) {
        case "none":
            return "none";
        case "read":
            return "read";
        case "write":
            return "write";
        case "readWrite":
            return "readWrite";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** Escape behavior for a call argument. */
export type ArgumentEscape = "none" | "return" | "escape";

export const ArgumentEscape = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ArgumentEscape): void {
        encodeArgumentEscape(writer, value);
    },

    /** Decode one ArgumentEscape. */
    decode(reader: BinaryReader): ArgumentEscape {
        return decodeArgumentEscape(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ArgumentEscape): Json {
        return toJsonArgumentEscape(value);
    },

    /** Return one ArgumentEscape from one JSON value. */
    fromJson(value: Json): ArgumentEscape {
        return fromJsonArgumentEscape(value);
    },
};

/** Encode one ArgumentEscape. */
export function encodeArgumentEscape(writer: BinaryWriter, value: ArgumentEscape): void {
    switch (value) {
        case "none":
            writer.writeUnsigned(0);
            return;
        case "return":
            writer.writeUnsigned(1);
            return;
        case "escape":
            writer.writeUnsigned(2);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one ArgumentEscape. */
export function decodeArgumentEscape(reader: BinaryReader): ArgumentEscape {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "none";
        case 1:
            return "return";
        case 2:
            return "escape";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one ArgumentEscape. */
export function toJsonArgumentEscape(value: ArgumentEscape): Json {
    return value;
}

/** Return one ArgumentEscape from one JSON value. */
export function fromJsonArgumentEscape(value: Json): ArgumentEscape {
    const variant = jsonString(value);

    switch (variant) {
        case "none":
            return "none";
        case "return":
            return "return";
        case "escape":
            return "escape";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** Table of memory metadata entries. */
export type MemoryMetadata = {
    /** Memory access metadata keyed by instruction id. */
    readonly memoryAccessesByInstructionId: ReadonlyMap<LocalNodeId, ReadonlyArray<MemoryAccessMetadata>>;
};

export const MemoryMetadata = {
    ...MemoryMetadataImpl,

    /** Encode this value. */
    encode(writer: BinaryWriter, value: MemoryMetadata): void {
        encodeMemoryMetadata(writer, value);
    },

    /** Decode one MemoryMetadata. */
    decode(reader: BinaryReader): MemoryMetadata {
        return decodeMemoryMetadata(reader);
    },

    /** Return this value as JSON. */
    toJson(value: MemoryMetadata): Json {
        return toJsonMemoryMetadata(value);
    },

    /** Return one MemoryMetadata from one JSON value. */
    fromJson(value: Json): MemoryMetadata {
        return fromJsonMemoryMetadata(value);
    },
};

/** Encode one MemoryMetadata. */
export function encodeMemoryMetadata(writer: BinaryWriter, value: MemoryMetadata): void {
    const entries0 = Array.from(value.memoryAccessesByInstructionId.entries()).map(([key0, item0]) => {
        const keyBytes = nestedBytes((writer) => {
            encodeLocalNodeId(writer, key0);
        });
        return { key0, item0, keyBytes };
    });
    entries0.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries0.length);
    for (const entry0 of entries0) {
        encodeLocalNodeId(writer, entry0.key0);
        writer.writeUnsigned(entry0.item0.length);
        for (const item1 of entry0.item0) {
            encodeMemoryAccessMetadata(writer, item1);
        }
    }
}

/** Decode one MemoryMetadata. */
export function decodeMemoryMetadata(reader: BinaryReader): MemoryMetadata {
    const memoryAccessesByInstructionId = (() => { const length0 = reader.readNumber(); const items0 = new Map<LocalNodeId, ReadonlyArray<MemoryAccessMetadata>>(); for (let index = 0; index < length0; index += 1) { items0.set(decodeLocalNodeId(reader), (() => { const length2 = reader.readNumber(); const items2: Array<MemoryAccessMetadata> = []; for (let index = 0; index < length2; index += 1) { items2.push(decodeMemoryAccessMetadata(reader)); } return items2; })()); } return items0; })();

    return {
        memoryAccessesByInstructionId,
    };
}

/** Return one JSON value for one MemoryMetadata. */
export function toJsonMemoryMetadata(value: MemoryMetadata): Json {
    return {
        memoryAccessesByInstructionId: Array.from(value.memoryAccessesByInstructionId.entries()).map(([key0, item0]) => [toJsonLocalNodeId(key0), item0.map((item1) => toJsonMemoryAccessMetadata(item1))] as const),
    };
}

/** Return one MemoryMetadata from one JSON value. */
export function fromJsonMemoryMetadata(value: Json): MemoryMetadata {
    const object = jsonObject(value);

    return {
        memoryAccessesByInstructionId: new Map(jsonArray(jsonField(object, "memoryAccessesByInstructionId")).map((entry) => { const items = jsonArray(entry); if (items.length !== 2) { throw new SerdeError(`expected JSON map entry length 2: ${items.length}`); } const key0 = items[0]; const item0 = items[1]; return [fromJsonLocalNodeId(key0), jsonArray(item0).map((item1) => fromJsonMemoryAccessMetadata(item1))] as const; })),
    };
}

/** Metadata describing a single memory access in an instruction. */
export type MemoryAccessMetadata = {
    /** The kind of access performed. */
    readonly kind: MemoryAccessKind;
    /** The memory target for the access. */
    readonly target: MemoryAccessTarget;
    /** The number of bytes accessed when known. */
    readonly size?: bigint;
    /** Alignment in bytes, when known. */
    readonly alignment?: number;
    /** Whether the access is volatile. */
    readonly isVolatile: boolean;
    /** Whether repeated loads observe the same value. */
    readonly isLoadInvariant: boolean;
    /** Memory ordering for atomic accesses. */
    readonly ordering?: MemoryOrdering;
    /** Synchronization scope for atomic accesses and fences. */
    readonly scope?: SyncScope;
    /** Memory visibility scope for fences. */
    readonly memoryScope?: MemoryScope;
    /** Memory flags for fences. */
    readonly flags?: MemoryFlags;
    /** Space override for the access. */
    readonly space?: Space;
};

export const MemoryAccessMetadata = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: MemoryAccessMetadata): void {
        encodeMemoryAccessMetadata(writer, value);
    },

    /** Decode one MemoryAccessMetadata. */
    decode(reader: BinaryReader): MemoryAccessMetadata {
        return decodeMemoryAccessMetadata(reader);
    },

    /** Return this value as JSON. */
    toJson(value: MemoryAccessMetadata): Json {
        return toJsonMemoryAccessMetadata(value);
    },

    /** Return one MemoryAccessMetadata from one JSON value. */
    fromJson(value: Json): MemoryAccessMetadata {
        return fromJsonMemoryAccessMetadata(value);
    },
};

/** Encode one MemoryAccessMetadata. */
export function encodeMemoryAccessMetadata(writer: BinaryWriter, value: MemoryAccessMetadata): void {
    encodeMemoryAccessKind(writer, value.kind);
    encodeMemoryAccessTarget(writer, value.target);
    writer.writeOption(value.size, (value2) => {
        writer.writeUnsigned(value2);
    });
    writer.writeOption(value.alignment, (value3) => {
        writer.writeUnsigned(value3);
    });
    writer.writeBool(value.isVolatile);
    writer.writeBool(value.isLoadInvariant);
    writer.writeOption(value.ordering, (value6) => {
        encodeMemoryOrdering(writer, value6);
    });
    writer.writeOption(value.scope, (value7) => {
        encodeSyncScope(writer, value7);
    });
    writer.writeOption(value.memoryScope, (value8) => {
        encodeMemoryScope(writer, value8);
    });
    writer.writeOption(value.flags, (value9) => {
        encodeMemoryFlags(writer, value9);
    });
    writer.writeOption(value.space, (value10) => {
        encodeSpace(writer, value10);
    });
}

/** Decode one MemoryAccessMetadata. */
export function decodeMemoryAccessMetadata(reader: BinaryReader): MemoryAccessMetadata {
    const kind = decodeMemoryAccessKind(reader);
    const target = decodeMemoryAccessTarget(reader);
    const size = reader.readOption(() => reader.readUnsigned());
    const alignment = reader.readOption(() => reader.readNumber());
    const isVolatile = reader.readBool();
    const isLoadInvariant = reader.readBool();
    const ordering = reader.readOption(() => decodeMemoryOrdering(reader));
    const scope = reader.readOption(() => decodeSyncScope(reader));
    const memoryScope = reader.readOption(() => decodeMemoryScope(reader));
    const flags = reader.readOption(() => decodeMemoryFlags(reader));
    const space = reader.readOption(() => decodeSpace(reader));

    return {
        kind,
        target,
        ...(size === undefined ? {} : { size }),
        ...(alignment === undefined ? {} : { alignment }),
        isVolatile,
        isLoadInvariant,
        ...(ordering === undefined ? {} : { ordering }),
        ...(scope === undefined ? {} : { scope }),
        ...(memoryScope === undefined ? {} : { memoryScope }),
        ...(flags === undefined ? {} : { flags }),
        ...(space === undefined ? {} : { space }),
    };
}

/** Return one JSON value for one MemoryAccessMetadata. */
export function toJsonMemoryAccessMetadata(value: MemoryAccessMetadata): Json {
    return {
        kind: toJsonMemoryAccessKind(value.kind),
        target: toJsonMemoryAccessTarget(value.target),
        ...(value.size === undefined ? {} : { size: value.size.toString() }),
        ...(value.alignment === undefined ? {} : { alignment: value.alignment }),
        isVolatile: value.isVolatile,
        isLoadInvariant: value.isLoadInvariant,
        ...(value.ordering === undefined ? {} : { ordering: toJsonMemoryOrdering(value.ordering) }),
        ...(value.scope === undefined ? {} : { scope: toJsonSyncScope(value.scope) }),
        ...(value.memoryScope === undefined ? {} : { memoryScope: toJsonMemoryScope(value.memoryScope) }),
        ...(value.flags === undefined ? {} : { flags: toJsonMemoryFlags(value.flags) }),
        ...(value.space === undefined ? {} : { space: toJsonSpace(value.space) }),
    };
}

/** Return one MemoryAccessMetadata from one JSON value. */
export function fromJsonMemoryAccessMetadata(value: Json): MemoryAccessMetadata {
    const object = jsonObject(value);

    return {
        kind: fromJsonMemoryAccessKind(jsonField(object, "kind")),
        target: fromJsonMemoryAccessTarget(jsonField(object, "target")),
        size: jsonOptional(object, "size", (value) => jsonBigint(value)),
        alignment: jsonOptional(object, "alignment", (value) => jsonInteger(value)),
        isVolatile: jsonBool(jsonField(object, "isVolatile")),
        isLoadInvariant: jsonBool(jsonField(object, "isLoadInvariant")),
        ordering: jsonOptional(object, "ordering", (value) => fromJsonMemoryOrdering(value)),
        scope: jsonOptional(object, "scope", (value) => fromJsonSyncScope(value)),
        memoryScope: jsonOptional(object, "memoryScope", (value) => fromJsonMemoryScope(value)),
        flags: jsonOptional(object, "flags", (value) => fromJsonMemoryFlags(value)),
        space: jsonOptional(object, "space", (value) => fromJsonSpace(value)),
    };
}

/** The kind of memory access represented by metadata. */
export type MemoryAccessKind = "read" | "write" | "readWrite" | "readModifyWrite" | "fence" | "prefetchRead" | "prefetchWrite";

export const MemoryAccessKind = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: MemoryAccessKind): void {
        encodeMemoryAccessKind(writer, value);
    },

    /** Decode one MemoryAccessKind. */
    decode(reader: BinaryReader): MemoryAccessKind {
        return decodeMemoryAccessKind(reader);
    },

    /** Return this value as JSON. */
    toJson(value: MemoryAccessKind): Json {
        return toJsonMemoryAccessKind(value);
    },

    /** Return one MemoryAccessKind from one JSON value. */
    fromJson(value: Json): MemoryAccessKind {
        return fromJsonMemoryAccessKind(value);
    },
};

/** Encode one MemoryAccessKind. */
export function encodeMemoryAccessKind(writer: BinaryWriter, value: MemoryAccessKind): void {
    switch (value) {
        case "read":
            writer.writeUnsigned(0);
            return;
        case "write":
            writer.writeUnsigned(1);
            return;
        case "readWrite":
            writer.writeUnsigned(2);
            return;
        case "readModifyWrite":
            writer.writeUnsigned(3);
            return;
        case "fence":
            writer.writeUnsigned(4);
            return;
        case "prefetchRead":
            writer.writeUnsigned(5);
            return;
        case "prefetchWrite":
            writer.writeUnsigned(6);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one MemoryAccessKind. */
export function decodeMemoryAccessKind(reader: BinaryReader): MemoryAccessKind {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "read";
        case 1:
            return "write";
        case 2:
            return "readWrite";
        case 3:
            return "readModifyWrite";
        case 4:
            return "fence";
        case 5:
            return "prefetchRead";
        case 6:
            return "prefetchWrite";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one MemoryAccessKind. */
export function toJsonMemoryAccessKind(value: MemoryAccessKind): Json {
    return value;
}

/** Return one MemoryAccessKind from one JSON value. */
export function fromJsonMemoryAccessKind(value: Json): MemoryAccessKind {
    const variant = jsonString(value);

    switch (variant) {
        case "read":
            return "read";
        case "write":
            return "write";
        case "readWrite":
            return "readWrite";
        case "readModifyWrite":
            return "readModifyWrite";
        case "fence":
            return "fence";
        case "prefetchRead":
            return "prefetchRead";
        case "prefetchWrite":
            return "prefetchWrite";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** Target of a memory access. */
export type MemoryAccessTarget =
    /** Access through a pointer value. */
    | {
          readonly kind: "pointer";
          readonly pointer: Value;
      }
    /** Access through a local slot. */
    | {
          readonly kind: "local";
          readonly local: LocalNodeId;
      }
    /** Access through a global. */
    | {
          readonly kind: "global";
          readonly global: LocalNodeId;
      }
    /** Access with unknown target. */
    | {
          readonly kind: "unknown";
      }
;

export const MemoryAccessTarget = {
    /** Access through a pointer value. */
    pointer(pointer: Value): MemoryAccessTarget {
        return { kind: "pointer", pointer };
    },

    /** Access through a local slot. */
    local(local: LocalNodeId): MemoryAccessTarget {
        return { kind: "local", local };
    },

    /** Access through a global. */
    global(global: LocalNodeId): MemoryAccessTarget {
        return { kind: "global", global };
    },

    /** Access with unknown target. */
    "unknown"(): MemoryAccessTarget {
        return { kind: "unknown" };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: MemoryAccessTarget): void {
        encodeMemoryAccessTarget(writer, value);
    },

    /** Decode one MemoryAccessTarget. */
    decode(reader: BinaryReader): MemoryAccessTarget {
        return decodeMemoryAccessTarget(reader);
    },

    /** Return this value as JSON. */
    toJson(value: MemoryAccessTarget): Json {
        return toJsonMemoryAccessTarget(value);
    },

    /** Return one MemoryAccessTarget from one JSON value. */
    fromJson(value: Json): MemoryAccessTarget {
        return fromJsonMemoryAccessTarget(value);
    },
};

/** Encode one MemoryAccessTarget. */
export function encodeMemoryAccessTarget(writer: BinaryWriter, value: MemoryAccessTarget): void {
    switch (value.kind) {
        case "pointer":
            writer.writeUnsigned(0);
            encodeValue(writer, value.pointer);
            return;
        case "local":
            writer.writeUnsigned(1);
            encodeLocalNodeId(writer, value.local);
            return;
        case "global":
            writer.writeUnsigned(2);
            encodeLocalNodeId(writer, value.global);
            return;
        case "unknown":
            writer.writeUnsigned(3);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one MemoryAccessTarget. */
export function decodeMemoryAccessTarget(reader: BinaryReader): MemoryAccessTarget {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const pointer = decodeValue(reader);

            return { kind: "pointer", pointer };
        }
        case 1: {
            const local = decodeLocalNodeId(reader);

            return { kind: "local", local };
        }
        case 2: {
            const global = decodeLocalNodeId(reader);

            return { kind: "global", global };
        }
        case 3: {
            return { kind: "unknown" };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one MemoryAccessTarget. */
export function toJsonMemoryAccessTarget(value: MemoryAccessTarget): Json {
    switch (value.kind) {
        case "pointer":
            return {
                kind: "pointer",
                pointer: toJsonValue(value.pointer),
            };
        case "local":
            return {
                kind: "local",
                local: toJsonLocalNodeId(value.local),
            };
        case "global":
            return {
                kind: "global",
                global: toJsonLocalNodeId(value.global),
            };
        case "unknown":
            return {
                kind: "unknown",
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one MemoryAccessTarget from one JSON value. */
export function fromJsonMemoryAccessTarget(value: Json): MemoryAccessTarget {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "pointer":
            return {
                kind,
                pointer: fromJsonValue(jsonField(object, "pointer")),
            };
        case "local":
            return {
                kind,
                local: fromJsonLocalNodeId(jsonField(object, "local")),
            };
        case "global":
            return {
                kind,
                global: fromJsonLocalNodeId(jsonField(object, "global")),
            };
        case "unknown":
            return {
                kind,
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}
