/* generated by destack_bridge_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, compareBytes, jsonArray, jsonField, jsonObject, jsonOptional, jsonString, nestedBytes } from "../../../protocol/serde.js";
import type { FunctionBehavior } from "./effect.js";
import type { MemoryEffect } from "./effect.js";
import type { AllocationSize } from "./memory.js";
import type { CallArgumentEffect } from "./memory.js";
import type { LocalNodeId } from "../tree/node.js";
import { decodeFunctionBehavior, encodeFunctionBehavior, fromJsonFunctionBehavior, toJsonFunctionBehavior } from "./effect.js";
import { decodeMemoryEffect, encodeMemoryEffect, fromJsonMemoryEffect, toJsonMemoryEffect } from "./effect.js";
import { decodeAllocationSize, encodeAllocationSize, fromJsonAllocationSize, toJsonAllocationSize } from "./memory.js";
import { decodeCallArgumentEffect, encodeCallArgumentEffect, fromJsonCallArgumentEffect, toJsonCallArgumentEffect } from "./memory.js";
import { decodeLocalNodeId, encodeLocalNodeId, fromJsonLocalNodeId, toJsonLocalNodeId } from "../tree/node.js";

import { FunctionMetadataTableImpl } from "../../../_impl/mir/metadata/function.js";

/** Function and call metadata derived from semantic MIR. */
export type FunctionMetadataTable = {
    /** Metadata keyed by function id. */
    readonly functions: ReadonlyMap<LocalNodeId, FunctionMetadata>;
    /** Metadata keyed by callsite. */
    readonly calls: ReadonlyMap<CallSite, CallMetadata>;
};

export const FunctionMetadataTable = {
    ...FunctionMetadataTableImpl,

    /** Encode this value. */
    encode(writer: BinaryWriter, value: FunctionMetadataTable): void {
        encodeFunctionMetadataTable(writer, value);
    },

    /** Decode one FunctionMetadataTable. */
    decode(reader: BinaryReader): FunctionMetadataTable {
        return decodeFunctionMetadataTable(reader);
    },

    /** Return this value as JSON. */
    toJson(value: FunctionMetadataTable): Json {
        return toJsonFunctionMetadataTable(value);
    },

    /** Return one FunctionMetadataTable from one JSON value. */
    fromJson(value: Json): FunctionMetadataTable {
        return fromJsonFunctionMetadataTable(value);
    },
};

/** Encode one FunctionMetadataTable. */
export function encodeFunctionMetadataTable(writer: BinaryWriter, value: FunctionMetadataTable): void {
    const entries0 = Array.from(value.functions.entries()).map(([key0, item0]) => {
        const keyBytes = nestedBytes((writer) => {
            encodeLocalNodeId(writer, key0);
        });
        return { key0, item0, keyBytes };
    });
    entries0.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries0.length);
    for (const entry0 of entries0) {
        encodeLocalNodeId(writer, entry0.key0);
        encodeFunctionMetadata(writer, entry0.item0);
    }
    const entries1 = Array.from(value.calls.entries()).map(([key1, item1]) => {
        const keyBytes = nestedBytes((writer) => {
            encodeCallSite(writer, key1);
        });
        return { key1, item1, keyBytes };
    });
    entries1.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries1.length);
    for (const entry1 of entries1) {
        encodeCallSite(writer, entry1.key1);
        encodeCallMetadata(writer, entry1.item1);
    }
}

/** Decode one FunctionMetadataTable. */
export function decodeFunctionMetadataTable(reader: BinaryReader): FunctionMetadataTable {
    const functions = (() => { const length0 = reader.readNumber(); const items0 = new Map<LocalNodeId, FunctionMetadata>(); for (let index = 0; index < length0; index += 1) { items0.set(decodeLocalNodeId(reader), decodeFunctionMetadata(reader)); } return items0; })();
    const calls = (() => { const length1 = reader.readNumber(); const items1 = new Map<CallSite, CallMetadata>(); for (let index = 0; index < length1; index += 1) { items1.set(decodeCallSite(reader), decodeCallMetadata(reader)); } return items1; })();

    return {
        functions,
        calls,
    };
}

/** Return one JSON value for one FunctionMetadataTable. */
export function toJsonFunctionMetadataTable(value: FunctionMetadataTable): Json {
    return {
        functions: Array.from(value.functions.entries()).map(([key0, item0]) => [toJsonLocalNodeId(key0), toJsonFunctionMetadata(item0)] as const),
        calls: Array.from(value.calls.entries()).map(([key0, item0]) => [toJsonCallSite(key0), toJsonCallMetadata(item0)] as const),
    };
}

/** Return one FunctionMetadataTable from one JSON value. */
export function fromJsonFunctionMetadataTable(value: Json): FunctionMetadataTable {
    const object = jsonObject(value);

    return {
        functions: new Map(jsonArray(jsonField(object, "functions")).map((entry) => { const items = jsonArray(entry); if (items.length !== 2) { throw new SerdeError(`expected JSON map entry length 2: ${items.length}`); } const key0 = items[0]; const item0 = items[1]; return [fromJsonLocalNodeId(key0), fromJsonFunctionMetadata(item0)] as const; })),
        calls: new Map(jsonArray(jsonField(object, "calls")).map((entry) => { const items = jsonArray(entry); if (items.length !== 2) { throw new SerdeError(`expected JSON map entry length 2: ${items.length}`); } const key0 = items[0]; const item0 = items[1]; return [fromJsonCallSite(key0), fromJsonCallMetadata(item0)] as const; })),
    };
}

/** Metadata for one function body or declaration. */
export type FunctionMetadata = {
    /** Memory touched by this function. */
    readonly memory: MemoryEffect;
    /** Behavioral effects of this function. */
    readonly behavior: FunctionBehavior;
    /** Allocation result size relation when known. */
    readonly allocationSize?: AllocationSize;
};

export const FunctionMetadata = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: FunctionMetadata): void {
        encodeFunctionMetadata(writer, value);
    },

    /** Decode one FunctionMetadata. */
    decode(reader: BinaryReader): FunctionMetadata {
        return decodeFunctionMetadata(reader);
    },

    /** Return this value as JSON. */
    toJson(value: FunctionMetadata): Json {
        return toJsonFunctionMetadata(value);
    },

    /** Return one FunctionMetadata from one JSON value. */
    fromJson(value: Json): FunctionMetadata {
        return fromJsonFunctionMetadata(value);
    },
};

/** Encode one FunctionMetadata. */
export function encodeFunctionMetadata(writer: BinaryWriter, value: FunctionMetadata): void {
    encodeMemoryEffect(writer, value.memory);
    encodeFunctionBehavior(writer, value.behavior);
    writer.writeOption(value.allocationSize, (value2) => {
        encodeAllocationSize(writer, value2);
    });
}

/** Decode one FunctionMetadata. */
export function decodeFunctionMetadata(reader: BinaryReader): FunctionMetadata {
    const memory = decodeMemoryEffect(reader);
    const behavior = decodeFunctionBehavior(reader);
    const allocationSize = reader.readOption(() => decodeAllocationSize(reader));

    return {
        memory,
        behavior,
        ...(allocationSize === undefined ? {} : { allocationSize }),
    };
}

/** Return one JSON value for one FunctionMetadata. */
export function toJsonFunctionMetadata(value: FunctionMetadata): Json {
    return {
        memory: toJsonMemoryEffect(value.memory),
        behavior: toJsonFunctionBehavior(value.behavior),
        ...(value.allocationSize === undefined ? {} : { allocationSize: toJsonAllocationSize(value.allocationSize) }),
    };
}

/** Return one FunctionMetadata from one JSON value. */
export function fromJsonFunctionMetadata(value: Json): FunctionMetadata {
    const object = jsonObject(value);

    return {
        memory: fromJsonMemoryEffect(jsonField(object, "memory")),
        behavior: fromJsonFunctionBehavior(jsonField(object, "behavior")),
        allocationSize: jsonOptional(object, "allocationSize", (value) => fromJsonAllocationSize(value)),
    };
}

/** Stable identifier for one callsite inside a function body. */
export type CallSite =
    /** Callsite stored as an instruction. */
    | {
          readonly kind: "instruction";
          readonly instruction: LocalNodeId;
      }
    /** Callsite stored as a block terminator. */
    | {
          readonly kind: "terminator";
          readonly terminator: LocalNodeId;
      }
;

export const CallSite = {
    /** Callsite stored as an instruction. */
    instruction(instruction: LocalNodeId): CallSite {
        return { kind: "instruction", instruction };
    },

    /** Callsite stored as a block terminator. */
    terminator(terminator: LocalNodeId): CallSite {
        return { kind: "terminator", terminator };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: CallSite): void {
        encodeCallSite(writer, value);
    },

    /** Decode one CallSite. */
    decode(reader: BinaryReader): CallSite {
        return decodeCallSite(reader);
    },

    /** Return this value as JSON. */
    toJson(value: CallSite): Json {
        return toJsonCallSite(value);
    },

    /** Return one CallSite from one JSON value. */
    fromJson(value: Json): CallSite {
        return fromJsonCallSite(value);
    },
};

/** Encode one CallSite. */
export function encodeCallSite(writer: BinaryWriter, value: CallSite): void {
    switch (value.kind) {
        case "instruction":
            writer.writeUnsigned(0);
            encodeLocalNodeId(writer, value.instruction);
            return;
        case "terminator":
            writer.writeUnsigned(1);
            encodeLocalNodeId(writer, value.terminator);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one CallSite. */
export function decodeCallSite(reader: BinaryReader): CallSite {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const instruction = decodeLocalNodeId(reader);

            return { kind: "instruction", instruction };
        }
        case 1: {
            const terminator = decodeLocalNodeId(reader);

            return { kind: "terminator", terminator };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one CallSite. */
export function toJsonCallSite(value: CallSite): Json {
    switch (value.kind) {
        case "instruction":
            return {
                kind: "instruction",
                instruction: toJsonLocalNodeId(value.instruction),
            };
        case "terminator":
            return {
                kind: "terminator",
                terminator: toJsonLocalNodeId(value.terminator),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one CallSite from one JSON value. */
export function fromJsonCallSite(value: Json): CallSite {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "instruction":
            return {
                kind,
                instruction: fromJsonLocalNodeId(jsonField(object, "instruction")),
            };
        case "terminator":
            return {
                kind,
                terminator: fromJsonLocalNodeId(jsonField(object, "terminator")),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** Metadata for one callsite. */
export type CallMetadata = {
    /** Memory touched by this call. */
    readonly memory: MemoryEffect;
    /** Behavioral effects of this call. */
    readonly behavior: FunctionBehavior;
    /** Allocation result size relation when known. */
    readonly allocationSize?: AllocationSize;
    /** Resolved direct target when dispatch analysis proves one. */
    readonly target?: LocalNodeId;
    /** Argument memory behavior when known. */
    readonly arguments: ReadonlyArray<CallArgumentEffect>;
};

export const CallMetadata = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: CallMetadata): void {
        encodeCallMetadata(writer, value);
    },

    /** Decode one CallMetadata. */
    decode(reader: BinaryReader): CallMetadata {
        return decodeCallMetadata(reader);
    },

    /** Return this value as JSON. */
    toJson(value: CallMetadata): Json {
        return toJsonCallMetadata(value);
    },

    /** Return one CallMetadata from one JSON value. */
    fromJson(value: Json): CallMetadata {
        return fromJsonCallMetadata(value);
    },
};

/** Encode one CallMetadata. */
export function encodeCallMetadata(writer: BinaryWriter, value: CallMetadata): void {
    encodeMemoryEffect(writer, value.memory);
    encodeFunctionBehavior(writer, value.behavior);
    writer.writeOption(value.allocationSize, (value2) => {
        encodeAllocationSize(writer, value2);
    });
    writer.writeOption(value.target, (value3) => {
        encodeLocalNodeId(writer, value3);
    });
    writer.writeUnsigned(value.arguments.length);
    for (const item4 of value.arguments) {
        encodeCallArgumentEffect(writer, item4);
    }
}

/** Decode one CallMetadata. */
export function decodeCallMetadata(reader: BinaryReader): CallMetadata {
    const memory = decodeMemoryEffect(reader);
    const behavior = decodeFunctionBehavior(reader);
    const allocationSize = reader.readOption(() => decodeAllocationSize(reader));
    const target = reader.readOption(() => decodeLocalNodeId(reader));
    const arguments_ = (() => { const length4 = reader.readNumber(); const items4: Array<CallArgumentEffect> = []; for (let index = 0; index < length4; index += 1) { items4.push(decodeCallArgumentEffect(reader)); } return items4; })();

    return {
        memory,
        behavior,
        ...(allocationSize === undefined ? {} : { allocationSize }),
        ...(target === undefined ? {} : { target }),
        arguments: arguments_,
    };
}

/** Return one JSON value for one CallMetadata. */
export function toJsonCallMetadata(value: CallMetadata): Json {
    return {
        memory: toJsonMemoryEffect(value.memory),
        behavior: toJsonFunctionBehavior(value.behavior),
        ...(value.allocationSize === undefined ? {} : { allocationSize: toJsonAllocationSize(value.allocationSize) }),
        ...(value.target === undefined ? {} : { target: toJsonLocalNodeId(value.target) }),
        arguments: value.arguments.map((item0) => toJsonCallArgumentEffect(item0)),
    };
}

/** Return one CallMetadata from one JSON value. */
export function fromJsonCallMetadata(value: Json): CallMetadata {
    const object = jsonObject(value);

    return {
        memory: fromJsonMemoryEffect(jsonField(object, "memory")),
        behavior: fromJsonFunctionBehavior(jsonField(object, "behavior")),
        allocationSize: jsonOptional(object, "allocationSize", (value) => fromJsonAllocationSize(value)),
        target: jsonOptional(object, "target", (value) => fromJsonLocalNodeId(value)),
        arguments: jsonArray(jsonField(object, "arguments")).map((item0) => fromJsonCallArgumentEffect(item0)),
    };
}
