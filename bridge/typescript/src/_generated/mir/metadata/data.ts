/* generated by destack_bridge_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, jsonField, jsonInteger, jsonObject } from "../../../protocol/serde.js";

import { DataLayoutImpl } from "../../../_impl/mir/metadata/data.js";

/** Target data layout for one MIR module. */
export type DataLayout = {
    /** Pointer size in bytes. */
    readonly pointerBytes: number;
};

export const DataLayout = {
    ...DataLayoutImpl,

    /** Encode this value. */
    encode(writer: BinaryWriter, value: DataLayout): void {
        encodeDataLayout(writer, value);
    },

    /** Decode one DataLayout. */
    decode(reader: BinaryReader): DataLayout {
        return decodeDataLayout(reader);
    },

    /** Return this value as JSON. */
    toJson(value: DataLayout): Json {
        return toJsonDataLayout(value);
    },

    /** Return one DataLayout from one JSON value. */
    fromJson(value: Json): DataLayout {
        return fromJsonDataLayout(value);
    },
};

/** Encode one DataLayout. */
export function encodeDataLayout(writer: BinaryWriter, value: DataLayout): void {
    writer.writeByte(value.pointerBytes);
}

/** Decode one DataLayout. */
export function decodeDataLayout(reader: BinaryReader): DataLayout {
    const pointerBytes = reader.readByte();

    return {
        pointerBytes,
    };
}

/** Return one JSON value for one DataLayout. */
export function toJsonDataLayout(value: DataLayout): Json {
    return {
        pointerBytes: value.pointerBytes,
    };
}

/** Return one DataLayout from one JSON value. */
export function fromJsonDataLayout(value: Json): DataLayout {
    const object = jsonObject(value);

    return {
        pointerBytes: jsonInteger(jsonField(object, "pointerBytes")),
    };
}
