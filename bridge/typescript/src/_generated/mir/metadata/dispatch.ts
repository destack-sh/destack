/* generated by destack_bridge_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, compareBytes, jsonArray, jsonField, jsonInteger, jsonObject, jsonOptional, jsonString, nestedBytes } from "../../../protocol/serde.js";
import type { StringId } from "../../core/string.js";
import type { LocalNodeId } from "../tree/node.js";
import { decodeStringId, encodeStringId, fromJsonStringId, toJsonStringId } from "../../core/string.js";
import { decodeLocalNodeId, encodeLocalNodeId, fromJsonLocalNodeId, toJsonLocalNodeId } from "../tree/node.js";

import { DispatchSlotImpl } from "../../../_impl/mir/metadata/dispatch.js";
import { DispatchMetadataImpl } from "../../../_impl/mir/metadata/dispatch.js";

/** Slot index inside a dispatch table. */
export type DispatchSlot = number;

export const DispatchSlot = {
    ...DispatchSlotImpl,

    /** Encode this value. */
    encode(writer: BinaryWriter, value: DispatchSlot): void {
        encodeDispatchSlot(writer, value);
    },

    /** Decode one DispatchSlot. */
    decode(reader: BinaryReader): DispatchSlot {
        return decodeDispatchSlot(reader);
    },

    /** Return this value as JSON. */
    toJson(value: DispatchSlot): Json {
        return toJsonDispatchSlot(value);
    },

    /** Return one DispatchSlot from one JSON value. */
    fromJson(value: Json): DispatchSlot {
        return fromJsonDispatchSlot(value);
    },
};

/** Encode one DispatchSlot. */
export function encodeDispatchSlot(writer: BinaryWriter, value: DispatchSlot): void {
    writer.writeUnsigned(value);
}

/** Decode one DispatchSlot. */
export function decodeDispatchSlot(reader: BinaryReader): DispatchSlot {
    return reader.readNumber();
}

/** Return one JSON value for one DispatchSlot. */
export function toJsonDispatchSlot(value: DispatchSlot): Json {
    return value;
}

/** Return one DispatchSlot from one JSON value. */
export function fromJsonDispatchSlot(value: Json): DispatchSlot {
    return jsonInteger(value);
}

/** Canonical dispatch metadata for one MIR module. */
export type DispatchMetadata = {
    /** Class dispatch tables. */
    readonly vtables: ReadonlyArray<Vtable>;
    /** Dynamic dispatch tables. */
    readonly dynamicTables: ReadonlyArray<DynamicTable>;
    /** Dynamic slot layouts keyed by constraint type id. */
    readonly dynamicShapes: ReadonlyMap<LocalNodeId, DynamicShape>;
};

export const DispatchMetadata = {
    ...DispatchMetadataImpl,

    /** Encode this value. */
    encode(writer: BinaryWriter, value: DispatchMetadata): void {
        encodeDispatchMetadata(writer, value);
    },

    /** Decode one DispatchMetadata. */
    decode(reader: BinaryReader): DispatchMetadata {
        return decodeDispatchMetadata(reader);
    },

    /** Return this value as JSON. */
    toJson(value: DispatchMetadata): Json {
        return toJsonDispatchMetadata(value);
    },

    /** Return one DispatchMetadata from one JSON value. */
    fromJson(value: Json): DispatchMetadata {
        return fromJsonDispatchMetadata(value);
    },
};

/** Encode one DispatchMetadata. */
export function encodeDispatchMetadata(writer: BinaryWriter, value: DispatchMetadata): void {
    writer.writeUnsigned(value.vtables.length);
    for (const item0 of value.vtables) {
        encodeVtable(writer, item0);
    }
    writer.writeUnsigned(value.dynamicTables.length);
    for (const item1 of value.dynamicTables) {
        encodeDynamicTable(writer, item1);
    }
    const entries2 = Array.from(value.dynamicShapes.entries()).map(([key2, item2]) => {
        const keyBytes = nestedBytes((writer) => {
            encodeLocalNodeId(writer, key2);
        });
        return { key2, item2, keyBytes };
    });
    entries2.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries2.length);
    for (const entry2 of entries2) {
        encodeLocalNodeId(writer, entry2.key2);
        encodeDynamicShape(writer, entry2.item2);
    }
}

/** Decode one DispatchMetadata. */
export function decodeDispatchMetadata(reader: BinaryReader): DispatchMetadata {
    const vtables = (() => { const length0 = reader.readNumber(); const items0: Array<Vtable> = []; for (let index = 0; index < length0; index += 1) { items0.push(decodeVtable(reader)); } return items0; })();
    const dynamicTables = (() => { const length1 = reader.readNumber(); const items1: Array<DynamicTable> = []; for (let index = 0; index < length1; index += 1) { items1.push(decodeDynamicTable(reader)); } return items1; })();
    const dynamicShapes = (() => { const length2 = reader.readNumber(); const items2 = new Map<LocalNodeId, DynamicShape>(); for (let index = 0; index < length2; index += 1) { items2.set(decodeLocalNodeId(reader), decodeDynamicShape(reader)); } return items2; })();

    return {
        vtables,
        dynamicTables,
        dynamicShapes,
    };
}

/** Return one JSON value for one DispatchMetadata. */
export function toJsonDispatchMetadata(value: DispatchMetadata): Json {
    return {
        vtables: value.vtables.map((item0) => toJsonVtable(item0)),
        dynamicTables: value.dynamicTables.map((item0) => toJsonDynamicTable(item0)),
        dynamicShapes: Array.from(value.dynamicShapes.entries()).map(([key0, item0]) => [toJsonLocalNodeId(key0), toJsonDynamicShape(item0)] as const),
    };
}

/** Return one DispatchMetadata from one JSON value. */
export function fromJsonDispatchMetadata(value: Json): DispatchMetadata {
    const object = jsonObject(value);

    return {
        vtables: jsonArray(jsonField(object, "vtables")).map((item0) => fromJsonVtable(item0)),
        dynamicTables: jsonArray(jsonField(object, "dynamicTables")).map((item0) => fromJsonDynamicTable(item0)),
        dynamicShapes: new Map(jsonArray(jsonField(object, "dynamicShapes")).map((entry) => { const items = jsonArray(entry); if (items.length !== 2) { throw new SerdeError(`expected JSON map entry length 2: ${items.length}`); } const key0 = items[0]; const item0 = items[1]; return [fromJsonLocalNodeId(key0), fromJsonDynamicShape(item0)] as const; })),
    };
}

/** Metadata for a class vtable. */
export type Vtable = {
    /** The class type owning this table. */
    readonly ty: LocalNodeId;
    /** The static global containing this table. */
    readonly global: LocalNodeId;
    /** Entries in declaration order. */
    readonly entries: ReadonlyArray<VtableEntry>;
};

export const Vtable = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: Vtable): void {
        encodeVtable(writer, value);
    },

    /** Decode one Vtable. */
    decode(reader: BinaryReader): Vtable {
        return decodeVtable(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Vtable): Json {
        return toJsonVtable(value);
    },

    /** Return one Vtable from one JSON value. */
    fromJson(value: Json): Vtable {
        return fromJsonVtable(value);
    },
};

/** Encode one Vtable. */
export function encodeVtable(writer: BinaryWriter, value: Vtable): void {
    encodeLocalNodeId(writer, value.ty);
    encodeLocalNodeId(writer, value.global);
    writer.writeUnsigned(value.entries.length);
    for (const item2 of value.entries) {
        encodeVtableEntry(writer, item2);
    }
}

/** Decode one Vtable. */
export function decodeVtable(reader: BinaryReader): Vtable {
    const ty = decodeLocalNodeId(reader);
    const global = decodeLocalNodeId(reader);
    const entries = (() => { const length2 = reader.readNumber(); const items2: Array<VtableEntry> = []; for (let index = 0; index < length2; index += 1) { items2.push(decodeVtableEntry(reader)); } return items2; })();

    return {
        ty,
        global,
        entries,
    };
}

/** Return one JSON value for one Vtable. */
export function toJsonVtable(value: Vtable): Json {
    return {
        ty: toJsonLocalNodeId(value.ty),
        global: toJsonLocalNodeId(value.global),
        entries: value.entries.map((item0) => toJsonVtableEntry(item0)),
    };
}

/** Return one Vtable from one JSON value. */
export function fromJsonVtable(value: Json): Vtable {
    const object = jsonObject(value);

    return {
        ty: fromJsonLocalNodeId(jsonField(object, "ty")),
        global: fromJsonLocalNodeId(jsonField(object, "global")),
        entries: jsonArray(jsonField(object, "entries")).map((item0) => fromJsonVtableEntry(item0)),
    };
}

/** Entry in a class vtable. */
export type VtableEntry =
    /** Slot containing the runtime type descriptor. */
    | {
          readonly kind: "typeDescriptor";
      }
    /** Slot containing a drop glue function. */
    | {
          readonly kind: "destructor";
          /** The drop glue function when present. */
          readonly function?: LocalNodeId;
      }
    /** Slot containing a method implementation. */
    | {
          readonly kind: "method";
          /** The concrete method implementation. */
          readonly function: LocalNodeId;
      }
;

export const VtableEntry = {
    /** Slot containing the runtime type descriptor. */
    typeDescriptor(): VtableEntry {
        return { kind: "typeDescriptor" };
    },

    /** Slot containing a drop glue function. */
    destructor(function_: LocalNodeId | undefined): VtableEntry {
        return { kind: "destructor", function: function_ };
    },

    /** Slot containing a method implementation. */
    method(function_: LocalNodeId): VtableEntry {
        return { kind: "method", function: function_ };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: VtableEntry): void {
        encodeVtableEntry(writer, value);
    },

    /** Decode one VtableEntry. */
    decode(reader: BinaryReader): VtableEntry {
        return decodeVtableEntry(reader);
    },

    /** Return this value as JSON. */
    toJson(value: VtableEntry): Json {
        return toJsonVtableEntry(value);
    },

    /** Return one VtableEntry from one JSON value. */
    fromJson(value: Json): VtableEntry {
        return fromJsonVtableEntry(value);
    },
};

/** Encode one VtableEntry. */
export function encodeVtableEntry(writer: BinaryWriter, value: VtableEntry): void {
    switch (value.kind) {
        case "typeDescriptor":
            writer.writeUnsigned(0);
            return;
        case "destructor":
            writer.writeUnsigned(1);
            writer.writeOption(value.function, (value0) => {
                encodeLocalNodeId(writer, value0);
            });
            return;
        case "method":
            writer.writeUnsigned(2);
            encodeLocalNodeId(writer, value.function);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one VtableEntry. */
export function decodeVtableEntry(reader: BinaryReader): VtableEntry {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            return { kind: "typeDescriptor" };
        }
        case 1: {
            const function_ = reader.readOption(() => decodeLocalNodeId(reader));

            return {
                kind: "destructor",
                ...(function_ === undefined ? {} : { function: function_ }),
            };
        }
        case 2: {
            const function_ = decodeLocalNodeId(reader);

            return {
                kind: "method",
                function: function_,
            };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one VtableEntry. */
export function toJsonVtableEntry(value: VtableEntry): Json {
    switch (value.kind) {
        case "typeDescriptor":
            return {
                kind: "typeDescriptor",
            };
        case "destructor":
            return {
                kind: "destructor",
                ...(value.function === undefined ? {} : { function: toJsonLocalNodeId(value.function) }),
            };
        case "method":
            return {
                kind: "method",
                function: toJsonLocalNodeId(value.function),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one VtableEntry from one JSON value. */
export function fromJsonVtableEntry(value: Json): VtableEntry {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "typeDescriptor":
            return {
                kind,
            };
        case "destructor":
            return {
                kind,
                function: jsonOptional(object, "function", (value) => fromJsonLocalNodeId(value)),
            };
        case "method":
            return {
                kind,
                function: fromJsonLocalNodeId(jsonField(object, "function")),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** Metadata for one concrete implementation of one dynamic constraint. */
export type DynamicTable = {
    /** The concrete type providing the implementation. */
    readonly concrete: LocalNodeId;
    /** The dynamic constraint type being dispatched. */
    readonly constraint: LocalNodeId;
    /** The static global containing this table. */
    readonly global: LocalNodeId;
    /** Slots in dynamic shape order. */
    readonly entries: ReadonlyArray<DynamicEntry>;
};

export const DynamicTable = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: DynamicTable): void {
        encodeDynamicTable(writer, value);
    },

    /** Decode one DynamicTable. */
    decode(reader: BinaryReader): DynamicTable {
        return decodeDynamicTable(reader);
    },

    /** Return this value as JSON. */
    toJson(value: DynamicTable): Json {
        return toJsonDynamicTable(value);
    },

    /** Return one DynamicTable from one JSON value. */
    fromJson(value: Json): DynamicTable {
        return fromJsonDynamicTable(value);
    },
};

/** Encode one DynamicTable. */
export function encodeDynamicTable(writer: BinaryWriter, value: DynamicTable): void {
    encodeLocalNodeId(writer, value.concrete);
    encodeLocalNodeId(writer, value.constraint);
    encodeLocalNodeId(writer, value.global);
    writer.writeUnsigned(value.entries.length);
    for (const item3 of value.entries) {
        encodeDynamicEntry(writer, item3);
    }
}

/** Decode one DynamicTable. */
export function decodeDynamicTable(reader: BinaryReader): DynamicTable {
    const concrete = decodeLocalNodeId(reader);
    const constraint = decodeLocalNodeId(reader);
    const global = decodeLocalNodeId(reader);
    const entries = (() => { const length3 = reader.readNumber(); const items3: Array<DynamicEntry> = []; for (let index = 0; index < length3; index += 1) { items3.push(decodeDynamicEntry(reader)); } return items3; })();

    return {
        concrete,
        constraint,
        global,
        entries,
    };
}

/** Return one JSON value for one DynamicTable. */
export function toJsonDynamicTable(value: DynamicTable): Json {
    return {
        concrete: toJsonLocalNodeId(value.concrete),
        constraint: toJsonLocalNodeId(value.constraint),
        global: toJsonLocalNodeId(value.global),
        entries: value.entries.map((item0) => toJsonDynamicEntry(item0)),
    };
}

/** Return one DynamicTable from one JSON value. */
export function fromJsonDynamicTable(value: Json): DynamicTable {
    const object = jsonObject(value);

    return {
        concrete: fromJsonLocalNodeId(jsonField(object, "concrete")),
        constraint: fromJsonLocalNodeId(jsonField(object, "constraint")),
        global: fromJsonLocalNodeId(jsonField(object, "global")),
        entries: jsonArray(jsonField(object, "entries")).map((item0) => fromJsonDynamicEntry(item0)),
    };
}

/** Entry in a dynamic dispatch table. */
export type DynamicEntry =
    /** Slot containing a field offset. */
    | {
          readonly kind: "field";
          /** The field offset in bytes. */
          readonly offset: number;
      }
    /** Slot containing a concrete getter implementation. */
    | {
          readonly kind: "getter";
          /** The concrete getter implementation. */
          readonly function: LocalNodeId;
      }
    /** Slot containing a concrete setter implementation. */
    | {
          readonly kind: "setter";
          /** The concrete setter implementation. */
          readonly function: LocalNodeId;
      }
    /** Slot containing a concrete method implementation. */
    | {
          readonly kind: "method";
          /** The concrete method implementation. */
          readonly function: LocalNodeId;
      }
    /** Slot containing a concrete call implementation. */
    | {
          readonly kind: "call";
          /** The concrete call implementation. */
          readonly function: LocalNodeId;
      }
;

export const DynamicEntry = {
    /** Slot containing a field offset. */
    field(offset: number): DynamicEntry {
        return { kind: "field", offset };
    },

    /** Slot containing a concrete getter implementation. */
    getter(function_: LocalNodeId): DynamicEntry {
        return { kind: "getter", function: function_ };
    },

    /** Slot containing a concrete setter implementation. */
    setter(function_: LocalNodeId): DynamicEntry {
        return { kind: "setter", function: function_ };
    },

    /** Slot containing a concrete method implementation. */
    method(function_: LocalNodeId): DynamicEntry {
        return { kind: "method", function: function_ };
    },

    /** Slot containing a concrete call implementation. */
    call(function_: LocalNodeId): DynamicEntry {
        return { kind: "call", function: function_ };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: DynamicEntry): void {
        encodeDynamicEntry(writer, value);
    },

    /** Decode one DynamicEntry. */
    decode(reader: BinaryReader): DynamicEntry {
        return decodeDynamicEntry(reader);
    },

    /** Return this value as JSON. */
    toJson(value: DynamicEntry): Json {
        return toJsonDynamicEntry(value);
    },

    /** Return one DynamicEntry from one JSON value. */
    fromJson(value: Json): DynamicEntry {
        return fromJsonDynamicEntry(value);
    },
};

/** Encode one DynamicEntry. */
export function encodeDynamicEntry(writer: BinaryWriter, value: DynamicEntry): void {
    switch (value.kind) {
        case "field":
            writer.writeUnsigned(0);
            writer.writeUnsigned(value.offset);
            return;
        case "getter":
            writer.writeUnsigned(1);
            encodeLocalNodeId(writer, value.function);
            return;
        case "setter":
            writer.writeUnsigned(2);
            encodeLocalNodeId(writer, value.function);
            return;
        case "method":
            writer.writeUnsigned(3);
            encodeLocalNodeId(writer, value.function);
            return;
        case "call":
            writer.writeUnsigned(4);
            encodeLocalNodeId(writer, value.function);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one DynamicEntry. */
export function decodeDynamicEntry(reader: BinaryReader): DynamicEntry {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const offset = reader.readNumber();

            return {
                kind: "field",
                offset,
            };
        }
        case 1: {
            const function_ = decodeLocalNodeId(reader);

            return {
                kind: "getter",
                function: function_,
            };
        }
        case 2: {
            const function_ = decodeLocalNodeId(reader);

            return {
                kind: "setter",
                function: function_,
            };
        }
        case 3: {
            const function_ = decodeLocalNodeId(reader);

            return {
                kind: "method",
                function: function_,
            };
        }
        case 4: {
            const function_ = decodeLocalNodeId(reader);

            return {
                kind: "call",
                function: function_,
            };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one DynamicEntry. */
export function toJsonDynamicEntry(value: DynamicEntry): Json {
    switch (value.kind) {
        case "field":
            return {
                kind: "field",
                offset: value.offset,
            };
        case "getter":
            return {
                kind: "getter",
                function: toJsonLocalNodeId(value.function),
            };
        case "setter":
            return {
                kind: "setter",
                function: toJsonLocalNodeId(value.function),
            };
        case "method":
            return {
                kind: "method",
                function: toJsonLocalNodeId(value.function),
            };
        case "call":
            return {
                kind: "call",
                function: toJsonLocalNodeId(value.function),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one DynamicEntry from one JSON value. */
export function fromJsonDynamicEntry(value: Json): DynamicEntry {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "field":
            return {
                kind,
                offset: jsonInteger(jsonField(object, "offset")),
            };
        case "getter":
            return {
                kind,
                function: fromJsonLocalNodeId(jsonField(object, "function")),
            };
        case "setter":
            return {
                kind,
                function: fromJsonLocalNodeId(jsonField(object, "function")),
            };
        case "method":
            return {
                kind,
                function: fromJsonLocalNodeId(jsonField(object, "function")),
            };
        case "call":
            return {
                kind,
                function: fromJsonLocalNodeId(jsonField(object, "function")),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** Slot layout for one dynamic constraint. */
export type DynamicShape = {
    /** The dynamic constraint type owning this shape. */
    readonly constraint: LocalNodeId;
    /** Slots in declaration order. */
    readonly slots: ReadonlyArray<DynamicSlot>;
};

export const DynamicShape = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: DynamicShape): void {
        encodeDynamicShape(writer, value);
    },

    /** Decode one DynamicShape. */
    decode(reader: BinaryReader): DynamicShape {
        return decodeDynamicShape(reader);
    },

    /** Return this value as JSON. */
    toJson(value: DynamicShape): Json {
        return toJsonDynamicShape(value);
    },

    /** Return one DynamicShape from one JSON value. */
    fromJson(value: Json): DynamicShape {
        return fromJsonDynamicShape(value);
    },
};

/** Encode one DynamicShape. */
export function encodeDynamicShape(writer: BinaryWriter, value: DynamicShape): void {
    encodeLocalNodeId(writer, value.constraint);
    writer.writeUnsigned(value.slots.length);
    for (const item1 of value.slots) {
        encodeDynamicSlot(writer, item1);
    }
}

/** Decode one DynamicShape. */
export function decodeDynamicShape(reader: BinaryReader): DynamicShape {
    const constraint = decodeLocalNodeId(reader);
    const slots = (() => { const length1 = reader.readNumber(); const items1: Array<DynamicSlot> = []; for (let index = 0; index < length1; index += 1) { items1.push(decodeDynamicSlot(reader)); } return items1; })();

    return {
        constraint,
        slots,
    };
}

/** Return one JSON value for one DynamicShape. */
export function toJsonDynamicShape(value: DynamicShape): Json {
    return {
        constraint: toJsonLocalNodeId(value.constraint),
        slots: value.slots.map((item0) => toJsonDynamicSlot(item0)),
    };
}

/** Return one DynamicShape from one JSON value. */
export function fromJsonDynamicShape(value: Json): DynamicShape {
    const object = jsonObject(value);

    return {
        constraint: fromJsonLocalNodeId(jsonField(object, "constraint")),
        slots: jsonArray(jsonField(object, "slots")).map((item0) => fromJsonDynamicSlot(item0)),
    };
}

/** Slot descriptor for a dynamic layout. */
export type DynamicSlot =
    /** Field slot. */
    | {
          readonly kind: "field";
          /** The canonical dispatch field id. */
          readonly field: LocalNodeId;
          /** The field name. */
          readonly name: StringId;
      }
    /** Getter slot. */
    | {
          readonly kind: "getter";
          /** The getter name. */
          readonly name: StringId;
          /** The getter signature. */
          readonly signature: LocalNodeId;
      }
    /** Setter slot. */
    | {
          readonly kind: "setter";
          /** The setter name. */
          readonly name: StringId;
          /** The setter signature. */
          readonly signature: LocalNodeId;
      }
    /** Method slot. */
    | {
          readonly kind: "method";
          /** The method name. */
          readonly name: StringId;
          /** The method signature. */
          readonly signature: LocalNodeId;
      }
    /** Call signature slot. */
    | {
          readonly kind: "call";
          /** The call signature. */
          readonly signature: LocalNodeId;
      }
;

export const DynamicSlot = {
    /** Field slot. */
    field(field: LocalNodeId, name: StringId): DynamicSlot {
        return { kind: "field", field, name };
    },

    /** Getter slot. */
    getter(name: StringId, signature: LocalNodeId): DynamicSlot {
        return { kind: "getter", name, signature };
    },

    /** Setter slot. */
    setter(name: StringId, signature: LocalNodeId): DynamicSlot {
        return { kind: "setter", name, signature };
    },

    /** Method slot. */
    method(name: StringId, signature: LocalNodeId): DynamicSlot {
        return { kind: "method", name, signature };
    },

    /** Call signature slot. */
    call(signature: LocalNodeId): DynamicSlot {
        return { kind: "call", signature };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: DynamicSlot): void {
        encodeDynamicSlot(writer, value);
    },

    /** Decode one DynamicSlot. */
    decode(reader: BinaryReader): DynamicSlot {
        return decodeDynamicSlot(reader);
    },

    /** Return this value as JSON. */
    toJson(value: DynamicSlot): Json {
        return toJsonDynamicSlot(value);
    },

    /** Return one DynamicSlot from one JSON value. */
    fromJson(value: Json): DynamicSlot {
        return fromJsonDynamicSlot(value);
    },
};

/** Encode one DynamicSlot. */
export function encodeDynamicSlot(writer: BinaryWriter, value: DynamicSlot): void {
    switch (value.kind) {
        case "field":
            writer.writeUnsigned(0);
            encodeLocalNodeId(writer, value.field);
            encodeStringId(writer, value.name);
            return;
        case "getter":
            writer.writeUnsigned(1);
            encodeStringId(writer, value.name);
            encodeLocalNodeId(writer, value.signature);
            return;
        case "setter":
            writer.writeUnsigned(2);
            encodeStringId(writer, value.name);
            encodeLocalNodeId(writer, value.signature);
            return;
        case "method":
            writer.writeUnsigned(3);
            encodeStringId(writer, value.name);
            encodeLocalNodeId(writer, value.signature);
            return;
        case "call":
            writer.writeUnsigned(4);
            encodeLocalNodeId(writer, value.signature);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one DynamicSlot. */
export function decodeDynamicSlot(reader: BinaryReader): DynamicSlot {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const field = decodeLocalNodeId(reader);
            const name = decodeStringId(reader);

            return {
                kind: "field",
                field,
                name,
            };
        }
        case 1: {
            const name = decodeStringId(reader);
            const signature = decodeLocalNodeId(reader);

            return {
                kind: "getter",
                name,
                signature,
            };
        }
        case 2: {
            const name = decodeStringId(reader);
            const signature = decodeLocalNodeId(reader);

            return {
                kind: "setter",
                name,
                signature,
            };
        }
        case 3: {
            const name = decodeStringId(reader);
            const signature = decodeLocalNodeId(reader);

            return {
                kind: "method",
                name,
                signature,
            };
        }
        case 4: {
            const signature = decodeLocalNodeId(reader);

            return {
                kind: "call",
                signature,
            };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one DynamicSlot. */
export function toJsonDynamicSlot(value: DynamicSlot): Json {
    switch (value.kind) {
        case "field":
            return {
                kind: "field",
                field: toJsonLocalNodeId(value.field),
                name: toJsonStringId(value.name),
            };
        case "getter":
            return {
                kind: "getter",
                name: toJsonStringId(value.name),
                signature: toJsonLocalNodeId(value.signature),
            };
        case "setter":
            return {
                kind: "setter",
                name: toJsonStringId(value.name),
                signature: toJsonLocalNodeId(value.signature),
            };
        case "method":
            return {
                kind: "method",
                name: toJsonStringId(value.name),
                signature: toJsonLocalNodeId(value.signature),
            };
        case "call":
            return {
                kind: "call",
                signature: toJsonLocalNodeId(value.signature),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one DynamicSlot from one JSON value. */
export function fromJsonDynamicSlot(value: Json): DynamicSlot {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "field":
            return {
                kind,
                field: fromJsonLocalNodeId(jsonField(object, "field")),
                name: fromJsonStringId(jsonField(object, "name")),
            };
        case "getter":
            return {
                kind,
                name: fromJsonStringId(jsonField(object, "name")),
                signature: fromJsonLocalNodeId(jsonField(object, "signature")),
            };
        case "setter":
            return {
                kind,
                name: fromJsonStringId(jsonField(object, "name")),
                signature: fromJsonLocalNodeId(jsonField(object, "signature")),
            };
        case "method":
            return {
                kind,
                name: fromJsonStringId(jsonField(object, "name")),
                signature: fromJsonLocalNodeId(jsonField(object, "signature")),
            };
        case "call":
            return {
                kind,
                signature: fromJsonLocalNodeId(jsonField(object, "signature")),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}
