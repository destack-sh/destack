/* generated by destack_bridge_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, jsonBool, jsonField, jsonInteger, jsonObject, jsonString } from "../../../protocol/serde.js";

import { SpaceSetImpl } from "../../../_impl/mir/tree/memory.js";

/** Atomic ordering and scope for one memory operation. */
export type AtomicAccess = {
    /** The memory ordering. */
    readonly ordering: MemoryOrdering;
    /** The synchronization scope. */
    readonly scope: SyncScope;
    /** Whether the access must be preserved as a volatile operation. */
    readonly isVolatile: boolean;
};

export const AtomicAccess = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: AtomicAccess): void {
        encodeAtomicAccess(writer, value);
    },

    /** Decode one AtomicAccess. */
    decode(reader: BinaryReader): AtomicAccess {
        return decodeAtomicAccess(reader);
    },

    /** Return this value as JSON. */
    toJson(value: AtomicAccess): Json {
        return toJsonAtomicAccess(value);
    },

    /** Return one AtomicAccess from one JSON value. */
    fromJson(value: Json): AtomicAccess {
        return fromJsonAtomicAccess(value);
    },
};

/** Encode one AtomicAccess. */
export function encodeAtomicAccess(writer: BinaryWriter, value: AtomicAccess): void {
    encodeMemoryOrdering(writer, value.ordering);
    encodeSyncScope(writer, value.scope);
    writer.writeBool(value.isVolatile);
}

/** Decode one AtomicAccess. */
export function decodeAtomicAccess(reader: BinaryReader): AtomicAccess {
    const ordering = decodeMemoryOrdering(reader);
    const scope = decodeSyncScope(reader);
    const isVolatile = reader.readBool();

    return {
        ordering,
        scope,
        isVolatile,
    };
}

/** Return one JSON value for one AtomicAccess. */
export function toJsonAtomicAccess(value: AtomicAccess): Json {
    return {
        ordering: toJsonMemoryOrdering(value.ordering),
        scope: toJsonSyncScope(value.scope),
        isVolatile: value.isVolatile,
    };
}

/** Return one AtomicAccess from one JSON value. */
export function fromJsonAtomicAccess(value: Json): AtomicAccess {
    const object = jsonObject(value);

    return {
        ordering: fromJsonMemoryOrdering(jsonField(object, "ordering")),
        scope: fromJsonSyncScope(jsonField(object, "scope")),
        isVolatile: jsonBool(jsonField(object, "isVolatile")),
    };
}

/** Memory ordering for atomic operations. */
export type MemoryOrdering = "relaxed" | "acquire" | "release" | "acquireRelease" | "sequentiallyConsistent";

export const MemoryOrdering = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: MemoryOrdering): void {
        encodeMemoryOrdering(writer, value);
    },

    /** Decode one MemoryOrdering. */
    decode(reader: BinaryReader): MemoryOrdering {
        return decodeMemoryOrdering(reader);
    },

    /** Return this value as JSON. */
    toJson(value: MemoryOrdering): Json {
        return toJsonMemoryOrdering(value);
    },

    /** Return one MemoryOrdering from one JSON value. */
    fromJson(value: Json): MemoryOrdering {
        return fromJsonMemoryOrdering(value);
    },
};

/** Encode one MemoryOrdering. */
export function encodeMemoryOrdering(writer: BinaryWriter, value: MemoryOrdering): void {
    switch (value) {
        case "relaxed":
            writer.writeUnsigned(0);
            return;
        case "acquire":
            writer.writeUnsigned(1);
            return;
        case "release":
            writer.writeUnsigned(2);
            return;
        case "acquireRelease":
            writer.writeUnsigned(3);
            return;
        case "sequentiallyConsistent":
            writer.writeUnsigned(4);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one MemoryOrdering. */
export function decodeMemoryOrdering(reader: BinaryReader): MemoryOrdering {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "relaxed";
        case 1:
            return "acquire";
        case 2:
            return "release";
        case 3:
            return "acquireRelease";
        case 4:
            return "sequentiallyConsistent";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one MemoryOrdering. */
export function toJsonMemoryOrdering(value: MemoryOrdering): Json {
    return value;
}

/** Return one MemoryOrdering from one JSON value. */
export function fromJsonMemoryOrdering(value: Json): MemoryOrdering {
    const variant = jsonString(value);

    switch (variant) {
        case "relaxed":
            return "relaxed";
        case "acquire":
            return "acquire";
        case "release":
            return "release";
        case "acquireRelease":
            return "acquireRelease";
        case "sequentiallyConsistent":
            return "sequentiallyConsistent";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** Synchronization scope for atomic operations and fences. */
export type SyncScope = "invocation" | "subgroup" | "workgroup" | "device" | "system";

export const SyncScope = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: SyncScope): void {
        encodeSyncScope(writer, value);
    },

    /** Decode one SyncScope. */
    decode(reader: BinaryReader): SyncScope {
        return decodeSyncScope(reader);
    },

    /** Return this value as JSON. */
    toJson(value: SyncScope): Json {
        return toJsonSyncScope(value);
    },

    /** Return one SyncScope from one JSON value. */
    fromJson(value: Json): SyncScope {
        return fromJsonSyncScope(value);
    },
};

/** Encode one SyncScope. */
export function encodeSyncScope(writer: BinaryWriter, value: SyncScope): void {
    switch (value) {
        case "invocation":
            writer.writeUnsigned(0);
            return;
        case "subgroup":
            writer.writeUnsigned(1);
            return;
        case "workgroup":
            writer.writeUnsigned(2);
            return;
        case "device":
            writer.writeUnsigned(3);
            return;
        case "system":
            writer.writeUnsigned(4);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one SyncScope. */
export function decodeSyncScope(reader: BinaryReader): SyncScope {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "invocation";
        case 1:
            return "subgroup";
        case 2:
            return "workgroup";
        case 3:
            return "device";
        case 4:
            return "system";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one SyncScope. */
export function toJsonSyncScope(value: SyncScope): Json {
    return value;
}

/** Return one SyncScope from one JSON value. */
export function fromJsonSyncScope(value: Json): SyncScope {
    const variant = jsonString(value);

    switch (variant) {
        case "invocation":
            return "invocation";
        case "subgroup":
            return "subgroup";
        case "workgroup":
            return "workgroup";
        case "device":
            return "device";
        case "system":
            return "system";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** Access for one compare exchange operation. */
export type CompareExchangeAccess = {
    /** Access used when the comparison succeeds. */
    readonly success: AtomicAccess;
    /** Ordering used by the failed comparison load. */
    readonly failureOrdering: MemoryOrdering;
};

export const CompareExchangeAccess = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: CompareExchangeAccess): void {
        encodeCompareExchangeAccess(writer, value);
    },

    /** Decode one CompareExchangeAccess. */
    decode(reader: BinaryReader): CompareExchangeAccess {
        return decodeCompareExchangeAccess(reader);
    },

    /** Return this value as JSON. */
    toJson(value: CompareExchangeAccess): Json {
        return toJsonCompareExchangeAccess(value);
    },

    /** Return one CompareExchangeAccess from one JSON value. */
    fromJson(value: Json): CompareExchangeAccess {
        return fromJsonCompareExchangeAccess(value);
    },
};

/** Encode one CompareExchangeAccess. */
export function encodeCompareExchangeAccess(writer: BinaryWriter, value: CompareExchangeAccess): void {
    encodeAtomicAccess(writer, value.success);
    encodeMemoryOrdering(writer, value.failureOrdering);
}

/** Decode one CompareExchangeAccess. */
export function decodeCompareExchangeAccess(reader: BinaryReader): CompareExchangeAccess {
    const success = decodeAtomicAccess(reader);
    const failureOrdering = decodeMemoryOrdering(reader);

    return {
        success,
        failureOrdering,
    };
}

/** Return one JSON value for one CompareExchangeAccess. */
export function toJsonCompareExchangeAccess(value: CompareExchangeAccess): Json {
    return {
        success: toJsonAtomicAccess(value.success),
        failureOrdering: toJsonMemoryOrdering(value.failureOrdering),
    };
}

/** Return one CompareExchangeAccess from one JSON value. */
export function fromJsonCompareExchangeAccess(value: Json): CompareExchangeAccess {
    const object = jsonObject(value);

    return {
        success: fromJsonAtomicAccess(jsonField(object, "success")),
        failureOrdering: fromJsonMemoryOrdering(jsonField(object, "failureOrdering")),
    };
}

/** Read-modify-write operator for atomic memory operations. */
export type AtomicRmwOperator = "exchange" | "add" | "sub" | "and" | "or" | "xor" | "min" | "max" | "umin" | "umax" | "fadd" | "fmin" | "fmax";

export const AtomicRmwOperator = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: AtomicRmwOperator): void {
        encodeAtomicRmwOperator(writer, value);
    },

    /** Decode one AtomicRmwOperator. */
    decode(reader: BinaryReader): AtomicRmwOperator {
        return decodeAtomicRmwOperator(reader);
    },

    /** Return this value as JSON. */
    toJson(value: AtomicRmwOperator): Json {
        return toJsonAtomicRmwOperator(value);
    },

    /** Return one AtomicRmwOperator from one JSON value. */
    fromJson(value: Json): AtomicRmwOperator {
        return fromJsonAtomicRmwOperator(value);
    },
};

/** Encode one AtomicRmwOperator. */
export function encodeAtomicRmwOperator(writer: BinaryWriter, value: AtomicRmwOperator): void {
    switch (value) {
        case "exchange":
            writer.writeUnsigned(0);
            return;
        case "add":
            writer.writeUnsigned(1);
            return;
        case "sub":
            writer.writeUnsigned(2);
            return;
        case "and":
            writer.writeUnsigned(3);
            return;
        case "or":
            writer.writeUnsigned(4);
            return;
        case "xor":
            writer.writeUnsigned(5);
            return;
        case "min":
            writer.writeUnsigned(6);
            return;
        case "max":
            writer.writeUnsigned(7);
            return;
        case "umin":
            writer.writeUnsigned(8);
            return;
        case "umax":
            writer.writeUnsigned(9);
            return;
        case "fadd":
            writer.writeUnsigned(10);
            return;
        case "fmin":
            writer.writeUnsigned(11);
            return;
        case "fmax":
            writer.writeUnsigned(12);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one AtomicRmwOperator. */
export function decodeAtomicRmwOperator(reader: BinaryReader): AtomicRmwOperator {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "exchange";
        case 1:
            return "add";
        case 2:
            return "sub";
        case 3:
            return "and";
        case 4:
            return "or";
        case 5:
            return "xor";
        case 6:
            return "min";
        case 7:
            return "max";
        case 8:
            return "umin";
        case 9:
            return "umax";
        case 10:
            return "fadd";
        case 11:
            return "fmin";
        case 12:
            return "fmax";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one AtomicRmwOperator. */
export function toJsonAtomicRmwOperator(value: AtomicRmwOperator): Json {
    return value;
}

/** Return one AtomicRmwOperator from one JSON value. */
export function fromJsonAtomicRmwOperator(value: Json): AtomicRmwOperator {
    const variant = jsonString(value);

    switch (variant) {
        case "exchange":
            return "exchange";
        case "add":
            return "add";
        case "sub":
            return "sub";
        case "and":
            return "and";
        case "or":
            return "or";
        case "xor":
            return "xor";
        case "min":
            return "min";
        case "max":
            return "max";
        case "umin":
            return "umin";
        case "umax":
            return "umax";
        case "fadd":
            return "fadd";
        case "fmin":
            return "fmin";
        case "fmax":
            return "fmax";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** Fence ordering, scope, and memory visibility. */
export type FenceAccess = {
    /** The memory ordering. */
    readonly ordering: MemoryOrdering;
    /** The synchronization scope. */
    readonly scope: SyncScope;
    /** The memory visibility scope. */
    readonly memoryScope: MemoryScope;
    /** The memory flags. */
    readonly flags: MemoryFlags;
};

export const FenceAccess = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: FenceAccess): void {
        encodeFenceAccess(writer, value);
    },

    /** Decode one FenceAccess. */
    decode(reader: BinaryReader): FenceAccess {
        return decodeFenceAccess(reader);
    },

    /** Return this value as JSON. */
    toJson(value: FenceAccess): Json {
        return toJsonFenceAccess(value);
    },

    /** Return one FenceAccess from one JSON value. */
    fromJson(value: Json): FenceAccess {
        return fromJsonFenceAccess(value);
    },
};

/** Encode one FenceAccess. */
export function encodeFenceAccess(writer: BinaryWriter, value: FenceAccess): void {
    encodeMemoryOrdering(writer, value.ordering);
    encodeSyncScope(writer, value.scope);
    encodeMemoryScope(writer, value.memoryScope);
    encodeMemoryFlags(writer, value.flags);
}

/** Decode one FenceAccess. */
export function decodeFenceAccess(reader: BinaryReader): FenceAccess {
    const ordering = decodeMemoryOrdering(reader);
    const scope = decodeSyncScope(reader);
    const memoryScope = decodeMemoryScope(reader);
    const flags = decodeMemoryFlags(reader);

    return {
        ordering,
        scope,
        memoryScope,
        flags,
    };
}

/** Return one JSON value for one FenceAccess. */
export function toJsonFenceAccess(value: FenceAccess): Json {
    return {
        ordering: toJsonMemoryOrdering(value.ordering),
        scope: toJsonSyncScope(value.scope),
        memoryScope: toJsonMemoryScope(value.memoryScope),
        flags: toJsonMemoryFlags(value.flags),
    };
}

/** Return one FenceAccess from one JSON value. */
export function fromJsonFenceAccess(value: Json): FenceAccess {
    const object = jsonObject(value);

    return {
        ordering: fromJsonMemoryOrdering(jsonField(object, "ordering")),
        scope: fromJsonSyncScope(jsonField(object, "scope")),
        memoryScope: fromJsonMemoryScope(jsonField(object, "memoryScope")),
        flags: fromJsonMemoryFlags(jsonField(object, "flags")),
    };
}

/** Memory scope for fences. */
export type MemoryScope = "invocation" | "subgroup" | "workgroup" | "device" | "system";

export const MemoryScope = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: MemoryScope): void {
        encodeMemoryScope(writer, value);
    },

    /** Decode one MemoryScope. */
    decode(reader: BinaryReader): MemoryScope {
        return decodeMemoryScope(reader);
    },

    /** Return this value as JSON. */
    toJson(value: MemoryScope): Json {
        return toJsonMemoryScope(value);
    },

    /** Return one MemoryScope from one JSON value. */
    fromJson(value: Json): MemoryScope {
        return fromJsonMemoryScope(value);
    },
};

/** Encode one MemoryScope. */
export function encodeMemoryScope(writer: BinaryWriter, value: MemoryScope): void {
    switch (value) {
        case "invocation":
            writer.writeUnsigned(0);
            return;
        case "subgroup":
            writer.writeUnsigned(1);
            return;
        case "workgroup":
            writer.writeUnsigned(2);
            return;
        case "device":
            writer.writeUnsigned(3);
            return;
        case "system":
            writer.writeUnsigned(4);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one MemoryScope. */
export function decodeMemoryScope(reader: BinaryReader): MemoryScope {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "invocation";
        case 1:
            return "subgroup";
        case 2:
            return "workgroup";
        case 3:
            return "device";
        case 4:
            return "system";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one MemoryScope. */
export function toJsonMemoryScope(value: MemoryScope): Json {
    return value;
}

/** Return one MemoryScope from one JSON value. */
export function fromJsonMemoryScope(value: Json): MemoryScope {
    const variant = jsonString(value);

    switch (variant) {
        case "invocation":
            return "invocation";
        case "subgroup":
            return "subgroup";
        case "workgroup":
            return "workgroup";
        case "device":
            return "device";
        case "system":
            return "system";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** Memory flags for fences. */
export type MemoryFlags = {
    /** The memory spaces affected by the fence. */
    readonly spaces: SpaceSet;
    /** Whether this makes writes available to other scopes. */
    readonly makesAvailable: boolean;
    /** Whether this makes writes visible to other scopes. */
    readonly makesVisible: boolean;
};

export const MemoryFlags = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: MemoryFlags): void {
        encodeMemoryFlags(writer, value);
    },

    /** Decode one MemoryFlags. */
    decode(reader: BinaryReader): MemoryFlags {
        return decodeMemoryFlags(reader);
    },

    /** Return this value as JSON. */
    toJson(value: MemoryFlags): Json {
        return toJsonMemoryFlags(value);
    },

    /** Return one MemoryFlags from one JSON value. */
    fromJson(value: Json): MemoryFlags {
        return fromJsonMemoryFlags(value);
    },
};

/** Encode one MemoryFlags. */
export function encodeMemoryFlags(writer: BinaryWriter, value: MemoryFlags): void {
    encodeSpaceSet(writer, value.spaces);
    writer.writeBool(value.makesAvailable);
    writer.writeBool(value.makesVisible);
}

/** Decode one MemoryFlags. */
export function decodeMemoryFlags(reader: BinaryReader): MemoryFlags {
    const spaces = decodeSpaceSet(reader);
    const makesAvailable = reader.readBool();
    const makesVisible = reader.readBool();

    return {
        spaces,
        makesAvailable,
        makesVisible,
    };
}

/** Return one JSON value for one MemoryFlags. */
export function toJsonMemoryFlags(value: MemoryFlags): Json {
    return {
        spaces: toJsonSpaceSet(value.spaces),
        makesAvailable: value.makesAvailable,
        makesVisible: value.makesVisible,
    };
}

/** Return one MemoryFlags from one JSON value. */
export function fromJsonMemoryFlags(value: Json): MemoryFlags {
    const object = jsonObject(value);

    return {
        spaces: fromJsonSpaceSet(jsonField(object, "spaces")),
        makesAvailable: jsonBool(jsonField(object, "makesAvailable")),
        makesVisible: jsonBool(jsonField(object, "makesVisible")),
    };
}

/** Set of backing memory spaces that an operation may access. */
export type SpaceSet = number;

export const SpaceSet = {
    ...SpaceSetImpl,

    /** Encode this value. */
    encode(writer: BinaryWriter, value: SpaceSet): void {
        encodeSpaceSet(writer, value);
    },

    /** Decode one SpaceSet. */
    decode(reader: BinaryReader): SpaceSet {
        return decodeSpaceSet(reader);
    },

    /** Return this value as JSON. */
    toJson(value: SpaceSet): Json {
        return toJsonSpaceSet(value);
    },

    /** Return one SpaceSet from one JSON value. */
    fromJson(value: Json): SpaceSet {
        return fromJsonSpaceSet(value);
    },
};

/** Encode one SpaceSet. */
export function encodeSpaceSet(writer: BinaryWriter, value: SpaceSet): void {
    writer.writeUnsigned(value);
}

/** Decode one SpaceSet. */
export function decodeSpaceSet(reader: BinaryReader): SpaceSet {
    return reader.readNumber();
}

/** Return one JSON value for one SpaceSet. */
export function toJsonSpaceSet(value: SpaceSet): Json {
    return value;
}

/** Return one SpaceSet from one JSON value. */
export function fromJsonSpaceSet(value: Json): SpaceSet {
    return jsonInteger(value);
}
