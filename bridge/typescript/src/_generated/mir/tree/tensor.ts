/* generated by destack_bridge_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, jsonArray, jsonBigint, jsonBool, jsonField, jsonInteger, jsonObject, jsonString } from "../../../protocol/serde.js";

/** Reduction operators for tensor reductions. */
export type TensorReduceOperator = "add" | "multiply" | "min" | "max" | "and" | "or" | "xor";

export const TensorReduceOperator = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: TensorReduceOperator): void {
        encodeTensorReduceOperator(writer, value);
    },

    /** Decode one TensorReduceOperator. */
    decode(reader: BinaryReader): TensorReduceOperator {
        return decodeTensorReduceOperator(reader);
    },

    /** Return this value as JSON. */
    toJson(value: TensorReduceOperator): Json {
        return toJsonTensorReduceOperator(value);
    },

    /** Return one TensorReduceOperator from one JSON value. */
    fromJson(value: Json): TensorReduceOperator {
        return fromJsonTensorReduceOperator(value);
    },
};

/** Encode one TensorReduceOperator. */
export function encodeTensorReduceOperator(writer: BinaryWriter, value: TensorReduceOperator): void {
    switch (value) {
        case "add":
            writer.writeUnsigned(0);
            return;
        case "multiply":
            writer.writeUnsigned(1);
            return;
        case "min":
            writer.writeUnsigned(2);
            return;
        case "max":
            writer.writeUnsigned(3);
            return;
        case "and":
            writer.writeUnsigned(4);
            return;
        case "or":
            writer.writeUnsigned(5);
            return;
        case "xor":
            writer.writeUnsigned(6);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one TensorReduceOperator. */
export function decodeTensorReduceOperator(reader: BinaryReader): TensorReduceOperator {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "add";
        case 1:
            return "multiply";
        case 2:
            return "min";
        case 3:
            return "max";
        case 4:
            return "and";
        case 5:
            return "or";
        case 6:
            return "xor";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one TensorReduceOperator. */
export function toJsonTensorReduceOperator(value: TensorReduceOperator): Json {
    return value;
}

/** Return one TensorReduceOperator from one JSON value. */
export function fromJsonTensorReduceOperator(value: Json): TensorReduceOperator {
    const variant = jsonString(value);

    switch (variant) {
        case "add":
            return "add";
        case "multiply":
            return "multiply";
        case "min":
            return "min";
        case "max":
            return "max";
        case "and":
            return "and";
        case "or":
            return "or";
        case "xor":
            return "xor";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** Index reduction operators for tensor reductions that return indices. */
export type TensorIndexReduceOperator = "min" | "max";

export const TensorIndexReduceOperator = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: TensorIndexReduceOperator): void {
        encodeTensorIndexReduceOperator(writer, value);
    },

    /** Decode one TensorIndexReduceOperator. */
    decode(reader: BinaryReader): TensorIndexReduceOperator {
        return decodeTensorIndexReduceOperator(reader);
    },

    /** Return this value as JSON. */
    toJson(value: TensorIndexReduceOperator): Json {
        return toJsonTensorIndexReduceOperator(value);
    },

    /** Return one TensorIndexReduceOperator from one JSON value. */
    fromJson(value: Json): TensorIndexReduceOperator {
        return fromJsonTensorIndexReduceOperator(value);
    },
};

/** Encode one TensorIndexReduceOperator. */
export function encodeTensorIndexReduceOperator(writer: BinaryWriter, value: TensorIndexReduceOperator): void {
    switch (value) {
        case "min":
            writer.writeUnsigned(0);
            return;
        case "max":
            writer.writeUnsigned(1);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one TensorIndexReduceOperator. */
export function decodeTensorIndexReduceOperator(reader: BinaryReader): TensorIndexReduceOperator {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "min";
        case 1:
            return "max";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one TensorIndexReduceOperator. */
export function toJsonTensorIndexReduceOperator(value: TensorIndexReduceOperator): Json {
    return value;
}

/** Return one TensorIndexReduceOperator from one JSON value. */
export function fromJsonTensorIndexReduceOperator(value: Json): TensorIndexReduceOperator {
    const variant = jsonString(value);

    switch (variant) {
        case "min":
            return "min";
        case "max":
            return "max";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** Tie-breaking behavior for tensor index reductions. */
export type TensorIndexTieBreak = "first" | "last";

export const TensorIndexTieBreak = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: TensorIndexTieBreak): void {
        encodeTensorIndexTieBreak(writer, value);
    },

    /** Decode one TensorIndexTieBreak. */
    decode(reader: BinaryReader): TensorIndexTieBreak {
        return decodeTensorIndexTieBreak(reader);
    },

    /** Return this value as JSON. */
    toJson(value: TensorIndexTieBreak): Json {
        return toJsonTensorIndexTieBreak(value);
    },

    /** Return one TensorIndexTieBreak from one JSON value. */
    fromJson(value: Json): TensorIndexTieBreak {
        return fromJsonTensorIndexTieBreak(value);
    },
};

/** Encode one TensorIndexTieBreak. */
export function encodeTensorIndexTieBreak(writer: BinaryWriter, value: TensorIndexTieBreak): void {
    switch (value) {
        case "first":
            writer.writeUnsigned(0);
            return;
        case "last":
            writer.writeUnsigned(1);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one TensorIndexTieBreak. */
export function decodeTensorIndexTieBreak(reader: BinaryReader): TensorIndexTieBreak {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "first";
        case 1:
            return "last";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one TensorIndexTieBreak. */
export function toJsonTensorIndexTieBreak(value: TensorIndexTieBreak): Json {
    return value;
}

/** Return one TensorIndexTieBreak from one JSON value. */
export function fromJsonTensorIndexTieBreak(value: Json): TensorIndexTieBreak {
    const variant = jsonString(value);

    switch (variant) {
        case "first":
            return "first";
        case "last":
            return "last";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** Update modes for tensor scatter operations. */
export type TensorScatterMode = "replace" | "add" | "multiply" | "min" | "max" | "and" | "or" | "xor";

export const TensorScatterMode = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: TensorScatterMode): void {
        encodeTensorScatterMode(writer, value);
    },

    /** Decode one TensorScatterMode. */
    decode(reader: BinaryReader): TensorScatterMode {
        return decodeTensorScatterMode(reader);
    },

    /** Return this value as JSON. */
    toJson(value: TensorScatterMode): Json {
        return toJsonTensorScatterMode(value);
    },

    /** Return one TensorScatterMode from one JSON value. */
    fromJson(value: Json): TensorScatterMode {
        return fromJsonTensorScatterMode(value);
    },
};

/** Encode one TensorScatterMode. */
export function encodeTensorScatterMode(writer: BinaryWriter, value: TensorScatterMode): void {
    switch (value) {
        case "replace":
            writer.writeUnsigned(0);
            return;
        case "add":
            writer.writeUnsigned(1);
            return;
        case "multiply":
            writer.writeUnsigned(2);
            return;
        case "min":
            writer.writeUnsigned(3);
            return;
        case "max":
            writer.writeUnsigned(4);
            return;
        case "and":
            writer.writeUnsigned(5);
            return;
        case "or":
            writer.writeUnsigned(6);
            return;
        case "xor":
            writer.writeUnsigned(7);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one TensorScatterMode. */
export function decodeTensorScatterMode(reader: BinaryReader): TensorScatterMode {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "replace";
        case 1:
            return "add";
        case 2:
            return "multiply";
        case 3:
            return "min";
        case 4:
            return "max";
        case 5:
            return "and";
        case 6:
            return "or";
        case 7:
            return "xor";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one TensorScatterMode. */
export function toJsonTensorScatterMode(value: TensorScatterMode): Json {
    return value;
}

/** Return one TensorScatterMode from one JSON value. */
export function fromJsonTensorScatterMode(value: Json): TensorScatterMode {
    const variant = jsonString(value);

    switch (variant) {
        case "replace":
            return "replace";
        case "add":
            return "add";
        case "multiply":
            return "multiply";
        case "min":
            return "min";
        case "max":
            return "max";
        case "and":
            return "and";
        case "or":
            return "or";
        case "xor":
            return "xor";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** Conversion modes for tensor element conversions. */
export type TensorConvertMode = "exact" | "roundTiesEven" | "roundTowardZero" | "roundFloor" | "roundCeil" | "saturate";

export const TensorConvertMode = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: TensorConvertMode): void {
        encodeTensorConvertMode(writer, value);
    },

    /** Decode one TensorConvertMode. */
    decode(reader: BinaryReader): TensorConvertMode {
        return decodeTensorConvertMode(reader);
    },

    /** Return this value as JSON. */
    toJson(value: TensorConvertMode): Json {
        return toJsonTensorConvertMode(value);
    },

    /** Return one TensorConvertMode from one JSON value. */
    fromJson(value: Json): TensorConvertMode {
        return fromJsonTensorConvertMode(value);
    },
};

/** Encode one TensorConvertMode. */
export function encodeTensorConvertMode(writer: BinaryWriter, value: TensorConvertMode): void {
    switch (value) {
        case "exact":
            writer.writeUnsigned(0);
            return;
        case "roundTiesEven":
            writer.writeUnsigned(1);
            return;
        case "roundTowardZero":
            writer.writeUnsigned(2);
            return;
        case "roundFloor":
            writer.writeUnsigned(3);
            return;
        case "roundCeil":
            writer.writeUnsigned(4);
            return;
        case "saturate":
            writer.writeUnsigned(5);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one TensorConvertMode. */
export function decodeTensorConvertMode(reader: BinaryReader): TensorConvertMode {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "exact";
        case 1:
            return "roundTiesEven";
        case 2:
            return "roundTowardZero";
        case 3:
            return "roundFloor";
        case 4:
            return "roundCeil";
        case 5:
            return "saturate";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one TensorConvertMode. */
export function toJsonTensorConvertMode(value: TensorConvertMode): Json {
    return value;
}

/** Return one TensorConvertMode from one JSON value. */
export function fromJsonTensorConvertMode(value: Json): TensorConvertMode {
    const variant = jsonString(value);

    switch (variant) {
        case "exact":
            return "exact";
        case "roundTiesEven":
            return "roundTiesEven";
        case "roundTowardZero":
            return "roundTowardZero";
        case "roundFloor":
            return "roundFloor";
        case "roundCeil":
            return "roundCeil";
        case "saturate":
            return "saturate";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** Dimension numbers for tensor dot operations. */
export type TensorDotDimensionNumbers = {
    /** Batch dimensions on the left operand. */
    readonly lhsBatch: ReadonlyArray<number>;
    /** Batch dimensions on the right operand. */
    readonly rhsBatch: ReadonlyArray<number>;
    /** Contracting dimensions on the left operand. */
    readonly lhsContracting: ReadonlyArray<number>;
    /** Contracting dimensions on the right operand. */
    readonly rhsContracting: ReadonlyArray<number>;
};

export const TensorDotDimensionNumbers = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: TensorDotDimensionNumbers): void {
        encodeTensorDotDimensionNumbers(writer, value);
    },

    /** Decode one TensorDotDimensionNumbers. */
    decode(reader: BinaryReader): TensorDotDimensionNumbers {
        return decodeTensorDotDimensionNumbers(reader);
    },

    /** Return this value as JSON. */
    toJson(value: TensorDotDimensionNumbers): Json {
        return toJsonTensorDotDimensionNumbers(value);
    },

    /** Return one TensorDotDimensionNumbers from one JSON value. */
    fromJson(value: Json): TensorDotDimensionNumbers {
        return fromJsonTensorDotDimensionNumbers(value);
    },
};

/** Encode one TensorDotDimensionNumbers. */
export function encodeTensorDotDimensionNumbers(writer: BinaryWriter, value: TensorDotDimensionNumbers): void {
    writer.writeUnsigned(value.lhsBatch.length);
    for (const item0 of value.lhsBatch) {
        writer.writeUnsigned(item0);
    }
    writer.writeUnsigned(value.rhsBatch.length);
    for (const item1 of value.rhsBatch) {
        writer.writeUnsigned(item1);
    }
    writer.writeUnsigned(value.lhsContracting.length);
    for (const item2 of value.lhsContracting) {
        writer.writeUnsigned(item2);
    }
    writer.writeUnsigned(value.rhsContracting.length);
    for (const item3 of value.rhsContracting) {
        writer.writeUnsigned(item3);
    }
}

/** Decode one TensorDotDimensionNumbers. */
export function decodeTensorDotDimensionNumbers(reader: BinaryReader): TensorDotDimensionNumbers {
    const lhsBatch = (() => { const length0 = reader.readNumber(); const items0: Array<number> = []; for (let index = 0; index < length0; index += 1) { items0.push(reader.readNumber()); } return items0; })();
    const rhsBatch = (() => { const length1 = reader.readNumber(); const items1: Array<number> = []; for (let index = 0; index < length1; index += 1) { items1.push(reader.readNumber()); } return items1; })();
    const lhsContracting = (() => { const length2 = reader.readNumber(); const items2: Array<number> = []; for (let index = 0; index < length2; index += 1) { items2.push(reader.readNumber()); } return items2; })();
    const rhsContracting = (() => { const length3 = reader.readNumber(); const items3: Array<number> = []; for (let index = 0; index < length3; index += 1) { items3.push(reader.readNumber()); } return items3; })();

    return {
        lhsBatch,
        rhsBatch,
        lhsContracting,
        rhsContracting,
    };
}

/** Return one JSON value for one TensorDotDimensionNumbers. */
export function toJsonTensorDotDimensionNumbers(value: TensorDotDimensionNumbers): Json {
    return {
        lhsBatch: value.lhsBatch.map((item0) => item0),
        rhsBatch: value.rhsBatch.map((item0) => item0),
        lhsContracting: value.lhsContracting.map((item0) => item0),
        rhsContracting: value.rhsContracting.map((item0) => item0),
    };
}

/** Return one TensorDotDimensionNumbers from one JSON value. */
export function fromJsonTensorDotDimensionNumbers(value: Json): TensorDotDimensionNumbers {
    const object = jsonObject(value);

    return {
        lhsBatch: jsonArray(jsonField(object, "lhsBatch")).map((item0) => jsonInteger(item0)),
        rhsBatch: jsonArray(jsonField(object, "rhsBatch")).map((item0) => jsonInteger(item0)),
        lhsContracting: jsonArray(jsonField(object, "lhsContracting")).map((item0) => jsonInteger(item0)),
        rhsContracting: jsonArray(jsonField(object, "rhsContracting")).map((item0) => jsonInteger(item0)),
    };
}

/** Dimension numbers for tensor convolution operations. */
export type TensorConvolutionDimensionNumbers = {
    /** Batch dimension for the input tensor. */
    readonly inputBatch: number;
    /** Feature dimension for the input tensor. */
    readonly inputFeature: number;
    /** Spatial dimensions for the input tensor. */
    readonly inputSpatial: ReadonlyArray<number>;
    /** Input feature dimension for the kernel tensor. */
    readonly kernelInputFeature: number;
    /** Output feature dimension for the kernel tensor. */
    readonly kernelOutputFeature: number;
    /** Spatial dimensions for the kernel tensor. */
    readonly kernelSpatial: ReadonlyArray<number>;
    /** Batch dimension for the output tensor. */
    readonly outputBatch: number;
    /** Feature dimension for the output tensor. */
    readonly outputFeature: number;
    /** Spatial dimensions for the output tensor. */
    readonly outputSpatial: ReadonlyArray<number>;
};

export const TensorConvolutionDimensionNumbers = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: TensorConvolutionDimensionNumbers): void {
        encodeTensorConvolutionDimensionNumbers(writer, value);
    },

    /** Decode one TensorConvolutionDimensionNumbers. */
    decode(reader: BinaryReader): TensorConvolutionDimensionNumbers {
        return decodeTensorConvolutionDimensionNumbers(reader);
    },

    /** Return this value as JSON. */
    toJson(value: TensorConvolutionDimensionNumbers): Json {
        return toJsonTensorConvolutionDimensionNumbers(value);
    },

    /** Return one TensorConvolutionDimensionNumbers from one JSON value. */
    fromJson(value: Json): TensorConvolutionDimensionNumbers {
        return fromJsonTensorConvolutionDimensionNumbers(value);
    },
};

/** Encode one TensorConvolutionDimensionNumbers. */
export function encodeTensorConvolutionDimensionNumbers(writer: BinaryWriter, value: TensorConvolutionDimensionNumbers): void {
    writer.writeUnsigned(value.inputBatch);
    writer.writeUnsigned(value.inputFeature);
    writer.writeUnsigned(value.inputSpatial.length);
    for (const item2 of value.inputSpatial) {
        writer.writeUnsigned(item2);
    }
    writer.writeUnsigned(value.kernelInputFeature);
    writer.writeUnsigned(value.kernelOutputFeature);
    writer.writeUnsigned(value.kernelSpatial.length);
    for (const item5 of value.kernelSpatial) {
        writer.writeUnsigned(item5);
    }
    writer.writeUnsigned(value.outputBatch);
    writer.writeUnsigned(value.outputFeature);
    writer.writeUnsigned(value.outputSpatial.length);
    for (const item8 of value.outputSpatial) {
        writer.writeUnsigned(item8);
    }
}

/** Decode one TensorConvolutionDimensionNumbers. */
export function decodeTensorConvolutionDimensionNumbers(reader: BinaryReader): TensorConvolutionDimensionNumbers {
    const inputBatch = reader.readNumber();
    const inputFeature = reader.readNumber();
    const inputSpatial = (() => { const length2 = reader.readNumber(); const items2: Array<number> = []; for (let index = 0; index < length2; index += 1) { items2.push(reader.readNumber()); } return items2; })();
    const kernelInputFeature = reader.readNumber();
    const kernelOutputFeature = reader.readNumber();
    const kernelSpatial = (() => { const length5 = reader.readNumber(); const items5: Array<number> = []; for (let index = 0; index < length5; index += 1) { items5.push(reader.readNumber()); } return items5; })();
    const outputBatch = reader.readNumber();
    const outputFeature = reader.readNumber();
    const outputSpatial = (() => { const length8 = reader.readNumber(); const items8: Array<number> = []; for (let index = 0; index < length8; index += 1) { items8.push(reader.readNumber()); } return items8; })();

    return {
        inputBatch,
        inputFeature,
        inputSpatial,
        kernelInputFeature,
        kernelOutputFeature,
        kernelSpatial,
        outputBatch,
        outputFeature,
        outputSpatial,
    };
}

/** Return one JSON value for one TensorConvolutionDimensionNumbers. */
export function toJsonTensorConvolutionDimensionNumbers(value: TensorConvolutionDimensionNumbers): Json {
    return {
        inputBatch: value.inputBatch,
        inputFeature: value.inputFeature,
        inputSpatial: value.inputSpatial.map((item0) => item0),
        kernelInputFeature: value.kernelInputFeature,
        kernelOutputFeature: value.kernelOutputFeature,
        kernelSpatial: value.kernelSpatial.map((item0) => item0),
        outputBatch: value.outputBatch,
        outputFeature: value.outputFeature,
        outputSpatial: value.outputSpatial.map((item0) => item0),
    };
}

/** Return one TensorConvolutionDimensionNumbers from one JSON value. */
export function fromJsonTensorConvolutionDimensionNumbers(value: Json): TensorConvolutionDimensionNumbers {
    const object = jsonObject(value);

    return {
        inputBatch: jsonInteger(jsonField(object, "inputBatch")),
        inputFeature: jsonInteger(jsonField(object, "inputFeature")),
        inputSpatial: jsonArray(jsonField(object, "inputSpatial")).map((item0) => jsonInteger(item0)),
        kernelInputFeature: jsonInteger(jsonField(object, "kernelInputFeature")),
        kernelOutputFeature: jsonInteger(jsonField(object, "kernelOutputFeature")),
        kernelSpatial: jsonArray(jsonField(object, "kernelSpatial")).map((item0) => jsonInteger(item0)),
        outputBatch: jsonInteger(jsonField(object, "outputBatch")),
        outputFeature: jsonInteger(jsonField(object, "outputFeature")),
        outputSpatial: jsonArray(jsonField(object, "outputSpatial")).map((item0) => jsonInteger(item0)),
    };
}

/** Window parameters for tensor convolution. */
export type TensorConvolutionWindow = {
    /** Stride for each spatial dimension. */
    readonly strides: ReadonlyArray<bigint>;
    /** Padding at the low end for each spatial dimension. */
    readonly paddingLow: ReadonlyArray<bigint>;
    /** Padding at the high end for each spatial dimension. */
    readonly paddingHigh: ReadonlyArray<bigint>;
    /** Input dilation for each spatial dimension. */
    readonly lhsDilation: ReadonlyArray<bigint>;
    /** Kernel dilation for each spatial dimension. */
    readonly rhsDilation: ReadonlyArray<bigint>;
    /** Whether each spatial dimension is reversed. */
    readonly windowReversal: ReadonlyArray<boolean>;
};

export const TensorConvolutionWindow = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: TensorConvolutionWindow): void {
        encodeTensorConvolutionWindow(writer, value);
    },

    /** Decode one TensorConvolutionWindow. */
    decode(reader: BinaryReader): TensorConvolutionWindow {
        return decodeTensorConvolutionWindow(reader);
    },

    /** Return this value as JSON. */
    toJson(value: TensorConvolutionWindow): Json {
        return toJsonTensorConvolutionWindow(value);
    },

    /** Return one TensorConvolutionWindow from one JSON value. */
    fromJson(value: Json): TensorConvolutionWindow {
        return fromJsonTensorConvolutionWindow(value);
    },
};

/** Encode one TensorConvolutionWindow. */
export function encodeTensorConvolutionWindow(writer: BinaryWriter, value: TensorConvolutionWindow): void {
    writer.writeUnsigned(value.strides.length);
    for (const item0 of value.strides) {
        writer.writeUnsigned(item0);
    }
    writer.writeUnsigned(value.paddingLow.length);
    for (const item1 of value.paddingLow) {
        writer.writeUnsigned(item1);
    }
    writer.writeUnsigned(value.paddingHigh.length);
    for (const item2 of value.paddingHigh) {
        writer.writeUnsigned(item2);
    }
    writer.writeUnsigned(value.lhsDilation.length);
    for (const item3 of value.lhsDilation) {
        writer.writeUnsigned(item3);
    }
    writer.writeUnsigned(value.rhsDilation.length);
    for (const item4 of value.rhsDilation) {
        writer.writeUnsigned(item4);
    }
    writer.writeUnsigned(value.windowReversal.length);
    for (const item5 of value.windowReversal) {
        writer.writeBool(item5);
    }
}

/** Decode one TensorConvolutionWindow. */
export function decodeTensorConvolutionWindow(reader: BinaryReader): TensorConvolutionWindow {
    const strides = (() => { const length0 = reader.readNumber(); const items0: Array<bigint> = []; for (let index = 0; index < length0; index += 1) { items0.push(reader.readUnsigned()); } return items0; })();
    const paddingLow = (() => { const length1 = reader.readNumber(); const items1: Array<bigint> = []; for (let index = 0; index < length1; index += 1) { items1.push(reader.readUnsigned()); } return items1; })();
    const paddingHigh = (() => { const length2 = reader.readNumber(); const items2: Array<bigint> = []; for (let index = 0; index < length2; index += 1) { items2.push(reader.readUnsigned()); } return items2; })();
    const lhsDilation = (() => { const length3 = reader.readNumber(); const items3: Array<bigint> = []; for (let index = 0; index < length3; index += 1) { items3.push(reader.readUnsigned()); } return items3; })();
    const rhsDilation = (() => { const length4 = reader.readNumber(); const items4: Array<bigint> = []; for (let index = 0; index < length4; index += 1) { items4.push(reader.readUnsigned()); } return items4; })();
    const windowReversal = (() => { const length5 = reader.readNumber(); const items5: Array<boolean> = []; for (let index = 0; index < length5; index += 1) { items5.push(reader.readBool()); } return items5; })();

    return {
        strides,
        paddingLow,
        paddingHigh,
        lhsDilation,
        rhsDilation,
        windowReversal,
    };
}

/** Return one JSON value for one TensorConvolutionWindow. */
export function toJsonTensorConvolutionWindow(value: TensorConvolutionWindow): Json {
    return {
        strides: value.strides.map((item0) => item0.toString()),
        paddingLow: value.paddingLow.map((item0) => item0.toString()),
        paddingHigh: value.paddingHigh.map((item0) => item0.toString()),
        lhsDilation: value.lhsDilation.map((item0) => item0.toString()),
        rhsDilation: value.rhsDilation.map((item0) => item0.toString()),
        windowReversal: value.windowReversal.map((item0) => item0),
    };
}

/** Return one TensorConvolutionWindow from one JSON value. */
export function fromJsonTensorConvolutionWindow(value: Json): TensorConvolutionWindow {
    const object = jsonObject(value);

    return {
        strides: jsonArray(jsonField(object, "strides")).map((item0) => jsonBigint(item0)),
        paddingLow: jsonArray(jsonField(object, "paddingLow")).map((item0) => jsonBigint(item0)),
        paddingHigh: jsonArray(jsonField(object, "paddingHigh")).map((item0) => jsonBigint(item0)),
        lhsDilation: jsonArray(jsonField(object, "lhsDilation")).map((item0) => jsonBigint(item0)),
        rhsDilation: jsonArray(jsonField(object, "rhsDilation")).map((item0) => jsonBigint(item0)),
        windowReversal: jsonArray(jsonField(object, "windowReversal")).map((item0) => jsonBool(item0)),
    };
}

/** Dimension numbers for tensor gather operations. */
export type TensorGatherDimensionNumbers = {
    /** Offset dimensions in the output. */
    readonly offsetDims: ReadonlyArray<number>;
    /** Collapsed slice dimensions in the operand. */
    readonly collapsedSliceDims: ReadonlyArray<number>;
    /** Mapping from index components to operand dimensions. */
    readonly startIndexMap: ReadonlyArray<number>;
    /** Index vector dimension in the indices tensor. */
    readonly indexVectorDim: number;
};

export const TensorGatherDimensionNumbers = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: TensorGatherDimensionNumbers): void {
        encodeTensorGatherDimensionNumbers(writer, value);
    },

    /** Decode one TensorGatherDimensionNumbers. */
    decode(reader: BinaryReader): TensorGatherDimensionNumbers {
        return decodeTensorGatherDimensionNumbers(reader);
    },

    /** Return this value as JSON. */
    toJson(value: TensorGatherDimensionNumbers): Json {
        return toJsonTensorGatherDimensionNumbers(value);
    },

    /** Return one TensorGatherDimensionNumbers from one JSON value. */
    fromJson(value: Json): TensorGatherDimensionNumbers {
        return fromJsonTensorGatherDimensionNumbers(value);
    },
};

/** Encode one TensorGatherDimensionNumbers. */
export function encodeTensorGatherDimensionNumbers(writer: BinaryWriter, value: TensorGatherDimensionNumbers): void {
    writer.writeUnsigned(value.offsetDims.length);
    for (const item0 of value.offsetDims) {
        writer.writeUnsigned(item0);
    }
    writer.writeUnsigned(value.collapsedSliceDims.length);
    for (const item1 of value.collapsedSliceDims) {
        writer.writeUnsigned(item1);
    }
    writer.writeUnsigned(value.startIndexMap.length);
    for (const item2 of value.startIndexMap) {
        writer.writeUnsigned(item2);
    }
    writer.writeUnsigned(value.indexVectorDim);
}

/** Decode one TensorGatherDimensionNumbers. */
export function decodeTensorGatherDimensionNumbers(reader: BinaryReader): TensorGatherDimensionNumbers {
    const offsetDims = (() => { const length0 = reader.readNumber(); const items0: Array<number> = []; for (let index = 0; index < length0; index += 1) { items0.push(reader.readNumber()); } return items0; })();
    const collapsedSliceDims = (() => { const length1 = reader.readNumber(); const items1: Array<number> = []; for (let index = 0; index < length1; index += 1) { items1.push(reader.readNumber()); } return items1; })();
    const startIndexMap = (() => { const length2 = reader.readNumber(); const items2: Array<number> = []; for (let index = 0; index < length2; index += 1) { items2.push(reader.readNumber()); } return items2; })();
    const indexVectorDim = reader.readNumber();

    return {
        offsetDims,
        collapsedSliceDims,
        startIndexMap,
        indexVectorDim,
    };
}

/** Return one JSON value for one TensorGatherDimensionNumbers. */
export function toJsonTensorGatherDimensionNumbers(value: TensorGatherDimensionNumbers): Json {
    return {
        offsetDims: value.offsetDims.map((item0) => item0),
        collapsedSliceDims: value.collapsedSliceDims.map((item0) => item0),
        startIndexMap: value.startIndexMap.map((item0) => item0),
        indexVectorDim: value.indexVectorDim,
    };
}

/** Return one TensorGatherDimensionNumbers from one JSON value. */
export function fromJsonTensorGatherDimensionNumbers(value: Json): TensorGatherDimensionNumbers {
    const object = jsonObject(value);

    return {
        offsetDims: jsonArray(jsonField(object, "offsetDims")).map((item0) => jsonInteger(item0)),
        collapsedSliceDims: jsonArray(jsonField(object, "collapsedSliceDims")).map((item0) => jsonInteger(item0)),
        startIndexMap: jsonArray(jsonField(object, "startIndexMap")).map((item0) => jsonInteger(item0)),
        indexVectorDim: jsonInteger(jsonField(object, "indexVectorDim")),
    };
}

/** Dimension numbers for tensor scatter operations. */
export type TensorScatterDimensionNumbers = {
    /** Dimensions of the update window in the updates tensor. */
    readonly updateWindowDims: ReadonlyArray<number>;
    /** Dimensions inserted into the operand shape. */
    readonly insertedWindowDims: ReadonlyArray<number>;
    /** Mapping from scatter indices to operand dimensions. */
    readonly scatterDimsToOperandDims: ReadonlyArray<number>;
    /** Index vector dimension in the indices tensor. */
    readonly indexVectorDim: number;
};

export const TensorScatterDimensionNumbers = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: TensorScatterDimensionNumbers): void {
        encodeTensorScatterDimensionNumbers(writer, value);
    },

    /** Decode one TensorScatterDimensionNumbers. */
    decode(reader: BinaryReader): TensorScatterDimensionNumbers {
        return decodeTensorScatterDimensionNumbers(reader);
    },

    /** Return this value as JSON. */
    toJson(value: TensorScatterDimensionNumbers): Json {
        return toJsonTensorScatterDimensionNumbers(value);
    },

    /** Return one TensorScatterDimensionNumbers from one JSON value. */
    fromJson(value: Json): TensorScatterDimensionNumbers {
        return fromJsonTensorScatterDimensionNumbers(value);
    },
};

/** Encode one TensorScatterDimensionNumbers. */
export function encodeTensorScatterDimensionNumbers(writer: BinaryWriter, value: TensorScatterDimensionNumbers): void {
    writer.writeUnsigned(value.updateWindowDims.length);
    for (const item0 of value.updateWindowDims) {
        writer.writeUnsigned(item0);
    }
    writer.writeUnsigned(value.insertedWindowDims.length);
    for (const item1 of value.insertedWindowDims) {
        writer.writeUnsigned(item1);
    }
    writer.writeUnsigned(value.scatterDimsToOperandDims.length);
    for (const item2 of value.scatterDimsToOperandDims) {
        writer.writeUnsigned(item2);
    }
    writer.writeUnsigned(value.indexVectorDim);
}

/** Decode one TensorScatterDimensionNumbers. */
export function decodeTensorScatterDimensionNumbers(reader: BinaryReader): TensorScatterDimensionNumbers {
    const updateWindowDims = (() => { const length0 = reader.readNumber(); const items0: Array<number> = []; for (let index = 0; index < length0; index += 1) { items0.push(reader.readNumber()); } return items0; })();
    const insertedWindowDims = (() => { const length1 = reader.readNumber(); const items1: Array<number> = []; for (let index = 0; index < length1; index += 1) { items1.push(reader.readNumber()); } return items1; })();
    const scatterDimsToOperandDims = (() => { const length2 = reader.readNumber(); const items2: Array<number> = []; for (let index = 0; index < length2; index += 1) { items2.push(reader.readNumber()); } return items2; })();
    const indexVectorDim = reader.readNumber();

    return {
        updateWindowDims,
        insertedWindowDims,
        scatterDimsToOperandDims,
        indexVectorDim,
    };
}

/** Return one JSON value for one TensorScatterDimensionNumbers. */
export function toJsonTensorScatterDimensionNumbers(value: TensorScatterDimensionNumbers): Json {
    return {
        updateWindowDims: value.updateWindowDims.map((item0) => item0),
        insertedWindowDims: value.insertedWindowDims.map((item0) => item0),
        scatterDimsToOperandDims: value.scatterDimsToOperandDims.map((item0) => item0),
        indexVectorDim: value.indexVectorDim,
    };
}

/** Return one TensorScatterDimensionNumbers from one JSON value. */
export function fromJsonTensorScatterDimensionNumbers(value: Json): TensorScatterDimensionNumbers {
    const object = jsonObject(value);

    return {
        updateWindowDims: jsonArray(jsonField(object, "updateWindowDims")).map((item0) => jsonInteger(item0)),
        insertedWindowDims: jsonArray(jsonField(object, "insertedWindowDims")).map((item0) => jsonInteger(item0)),
        scatterDimsToOperandDims: jsonArray(jsonField(object, "scatterDimsToOperandDims")).map((item0) => jsonInteger(item0)),
        indexVectorDim: jsonInteger(jsonField(object, "indexVectorDim")),
    };
}
