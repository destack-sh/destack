/* generated by destack_bridge_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, jsonArray, jsonBigint, jsonBool, jsonField, jsonInteger, jsonObject, jsonOptional, jsonString } from "../../../protocol/serde.js";
import type { StringId } from "../../core/string.js";
import type { Constant } from "./constant.js";
import type { Lifetime } from "./lifetime.js";
import type { LifetimeParameter } from "./lifetime.js";
import type { LocalNodeId } from "./node.js";
import type { SignatureParameter } from "./parameter.js";
import { decodeStringId, encodeStringId, fromJsonStringId, toJsonStringId } from "../../core/string.js";
import { decodeConstant, encodeConstant, fromJsonConstant, toJsonConstant } from "./constant.js";
import { decodeLifetime, encodeLifetime, fromJsonLifetime, toJsonLifetime } from "./lifetime.js";
import { decodeLifetimeParameter, encodeLifetimeParameter, fromJsonLifetimeParameter, toJsonLifetimeParameter } from "./lifetime.js";
import { decodeLocalNodeId, encodeLocalNodeId, fromJsonLocalNodeId, toJsonLocalNodeId } from "./node.js";
import { decodeSignatureParameter, encodeSignatureParameter, fromJsonSignatureParameter, toJsonSignatureParameter } from "./parameter.js";

/** A concrete MIR floating-point type. */
export type FloatType = "float16" | "bfloat16" | "float32" | "float64";

export const FloatType = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: FloatType): void {
        encodeFloatType(writer, value);
    },

    /** Decode one FloatType. */
    decode(reader: BinaryReader): FloatType {
        return decodeFloatType(reader);
    },

    /** Return this value as JSON. */
    toJson(value: FloatType): Json {
        return toJsonFloatType(value);
    },

    /** Return one FloatType from one JSON value. */
    fromJson(value: Json): FloatType {
        return fromJsonFloatType(value);
    },
};

/** Encode one FloatType. */
export function encodeFloatType(writer: BinaryWriter, value: FloatType): void {
    switch (value) {
        case "float16":
            writer.writeUnsigned(0);
            return;
        case "bfloat16":
            writer.writeUnsigned(1);
            return;
        case "float32":
            writer.writeUnsigned(2);
            return;
        case "float64":
            writer.writeUnsigned(3);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one FloatType. */
export function decodeFloatType(reader: BinaryReader): FloatType {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "float16";
        case 1:
            return "bfloat16";
        case 2:
            return "float32";
        case 3:
            return "float64";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one FloatType. */
export function toJsonFloatType(value: FloatType): Json {
    return value;
}

/** Return one FloatType from one JSON value. */
export function fromJsonFloatType(value: Json): FloatType {
    const variant = jsonString(value);

    switch (variant) {
        case "float16":
            return "float16";
        case "bfloat16":
            return "bfloat16";
        case "float32":
            return "float32";
        case "float64":
            return "float64";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** Mutability of a storage binding. */
export type Mutability = "immutable" | "mutable";

export const Mutability = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: Mutability): void {
        encodeMutability(writer, value);
    },

    /** Decode one Mutability. */
    decode(reader: BinaryReader): Mutability {
        return decodeMutability(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Mutability): Json {
        return toJsonMutability(value);
    },

    /** Return one Mutability from one JSON value. */
    fromJson(value: Json): Mutability {
        return fromJsonMutability(value);
    },
};

/** Encode one Mutability. */
export function encodeMutability(writer: BinaryWriter, value: Mutability): void {
    switch (value) {
        case "immutable":
            writer.writeUnsigned(0);
            return;
        case "mutable":
            writer.writeUnsigned(1);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one Mutability. */
export function decodeMutability(reader: BinaryReader): Mutability {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "immutable";
        case 1:
            return "mutable";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one Mutability. */
export function toJsonMutability(value: Mutability): Json {
    return value;
}

/** Return one Mutability from one JSON value. */
export function fromJsonMutability(value: Json): Mutability {
    const variant = jsonString(value);

    switch (variant) {
        case "immutable":
            return "immutable";
        case "mutable":
            return "mutable";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** Kind of reference in MIR. */
export type ReferenceKind = "managed" | "unique" | "borrowed" | "raw";

export const ReferenceKind = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ReferenceKind): void {
        encodeReferenceKind(writer, value);
    },

    /** Decode one ReferenceKind. */
    decode(reader: BinaryReader): ReferenceKind {
        return decodeReferenceKind(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ReferenceKind): Json {
        return toJsonReferenceKind(value);
    },

    /** Return one ReferenceKind from one JSON value. */
    fromJson(value: Json): ReferenceKind {
        return fromJsonReferenceKind(value);
    },
};

/** Encode one ReferenceKind. */
export function encodeReferenceKind(writer: BinaryWriter, value: ReferenceKind): void {
    switch (value) {
        case "managed":
            writer.writeUnsigned(0);
            return;
        case "unique":
            writer.writeUnsigned(1);
            return;
        case "borrowed":
            writer.writeUnsigned(2);
            return;
        case "raw":
            writer.writeUnsigned(3);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one ReferenceKind. */
export function decodeReferenceKind(reader: BinaryReader): ReferenceKind {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "managed";
        case 1:
            return "unique";
        case 2:
            return "borrowed";
        case 3:
            return "raw";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one ReferenceKind. */
export function toJsonReferenceKind(value: ReferenceKind): Json {
    return value;
}

/** Return one ReferenceKind from one JSON value. */
export function fromJsonReferenceKind(value: Json): ReferenceKind {
    const variant = jsonString(value);

    switch (variant) {
        case "managed":
            return "managed";
        case "unique":
            return "unique";
        case "borrowed":
            return "borrowed";
        case "raw":
            return "raw";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** Space for a reference. */
export type Space = "local" | "shared" | "frame" | "static";

export const Space = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: Space): void {
        encodeSpace(writer, value);
    },

    /** Decode one Space. */
    decode(reader: BinaryReader): Space {
        return decodeSpace(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Space): Json {
        return toJsonSpace(value);
    },

    /** Return one Space from one JSON value. */
    fromJson(value: Json): Space {
        return fromJsonSpace(value);
    },
};

/** Encode one Space. */
export function encodeSpace(writer: BinaryWriter, value: Space): void {
    switch (value) {
        case "local":
            writer.writeUnsigned(0);
            return;
        case "shared":
            writer.writeUnsigned(1);
            return;
        case "frame":
            writer.writeUnsigned(2);
            return;
        case "static":
            writer.writeUnsigned(3);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one Space. */
export function decodeSpace(reader: BinaryReader): Space {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "local";
        case 1:
            return "shared";
        case 2:
            return "frame";
        case 3:
            return "static";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one Space. */
export function toJsonSpace(value: Space): Json {
    return value;
}

/** Return one Space from one JSON value. */
export function fromJsonSpace(value: Json): Space {
    const variant = jsonString(value);

    switch (variant) {
        case "local":
            return "local";
        case "shared":
            return "shared";
        case "frame":
            return "frame";
        case "static":
            return "static";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** Access exposed by a reference-like value. */
export type Access = "readonly" | "mutable" | "exclusive";

export const Access = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: Access): void {
        encodeAccess(writer, value);
    },

    /** Decode one Access. */
    decode(reader: BinaryReader): Access {
        return decodeAccess(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Access): Json {
        return toJsonAccess(value);
    },

    /** Return one Access from one JSON value. */
    fromJson(value: Json): Access {
        return fromJsonAccess(value);
    },
};

/** Encode one Access. */
export function encodeAccess(writer: BinaryWriter, value: Access): void {
    switch (value) {
        case "readonly":
            writer.writeUnsigned(0);
            return;
        case "mutable":
            writer.writeUnsigned(1);
            return;
        case "exclusive":
            writer.writeUnsigned(2);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one Access. */
export function decodeAccess(reader: BinaryReader): Access {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "readonly";
        case 1:
            return "mutable";
        case 2:
            return "exclusive";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one Access. */
export function toJsonAccess(value: Access): Json {
    return value;
}

/** Return one Access from one JSON value. */
export function fromJsonAccess(value: Json): Access {
    const variant = jsonString(value);

    switch (variant) {
        case "readonly":
            return "readonly";
        case "mutable":
            return "mutable";
        case "exclusive":
            return "exclusive";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** Invalid-value niches carried by pointer-like values. */
export type Nullability = "none" | "null" | "undefined" | "nullOrUndefined";

export const Nullability = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: Nullability): void {
        encodeNullability(writer, value);
    },

    /** Decode one Nullability. */
    decode(reader: BinaryReader): Nullability {
        return decodeNullability(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Nullability): Json {
        return toJsonNullability(value);
    },

    /** Return one Nullability from one JSON value. */
    fromJson(value: Json): Nullability {
        return fromJsonNullability(value);
    },
};

/** Encode one Nullability. */
export function encodeNullability(writer: BinaryWriter, value: Nullability): void {
    switch (value) {
        case "none":
            writer.writeUnsigned(0);
            return;
        case "null":
            writer.writeUnsigned(1);
            return;
        case "undefined":
            writer.writeUnsigned(2);
            return;
        case "nullOrUndefined":
            writer.writeUnsigned(3);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one Nullability. */
export function decodeNullability(reader: BinaryReader): Nullability {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "none";
        case 1:
            return "null";
        case 2:
            return "undefined";
        case 3:
            return "nullOrUndefined";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one Nullability. */
export function toJsonNullability(value: Nullability): Json {
    return value;
}

/** Return one Nullability from one JSON value. */
export function fromJsonNullability(value: Json): Nullability {
    const variant = jsonString(value);

    switch (variant) {
        case "none":
            return "none";
        case "null":
            return "null";
        case "undefined":
            return "undefined";
        case "nullOrUndefined":
            return "nullOrUndefined";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** Copyability of a type. */
export type Copy = "yes" | "no";

export const Copy = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: Copy): void {
        encodeCopy(writer, value);
    },

    /** Decode one Copy. */
    decode(reader: BinaryReader): Copy {
        return decodeCopy(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Copy): Json {
        return toJsonCopy(value);
    },

    /** Return one Copy from one JSON value. */
    fromJson(value: Json): Copy {
        return fromJsonCopy(value);
    },
};

/** Encode one Copy. */
export function encodeCopy(writer: BinaryWriter, value: Copy): void {
    switch (value) {
        case "yes":
            writer.writeUnsigned(0);
            return;
        case "no":
            writer.writeUnsigned(1);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one Copy. */
export function decodeCopy(reader: BinaryReader): Copy {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "yes";
        case 1:
            return "no";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one Copy. */
export function toJsonCopy(value: Copy): Json {
    return value;
}

/** Return one Copy from one JSON value. */
export function fromJsonCopy(value: Json): Copy {
    const variant = jsonString(value);

    switch (variant) {
        case "yes":
            return "yes";
        case "no":
            return "no";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** One physical tagged sum case. */
export type VariantCase = {
    /** The tag constant selecting this case. */
    readonly tag: Constant;
    /** The logical payload type. */
    readonly ty: LocalNodeId;
};

export const VariantCase = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: VariantCase): void {
        encodeVariantCase(writer, value);
    },

    /** Decode one VariantCase. */
    decode(reader: BinaryReader): VariantCase {
        return decodeVariantCase(reader);
    },

    /** Return this value as JSON. */
    toJson(value: VariantCase): Json {
        return toJsonVariantCase(value);
    },

    /** Return one VariantCase from one JSON value. */
    fromJson(value: Json): VariantCase {
        return fromJsonVariantCase(value);
    },
};

/** Encode one VariantCase. */
export function encodeVariantCase(writer: BinaryWriter, value: VariantCase): void {
    encodeConstant(writer, value.tag);
    encodeLocalNodeId(writer, value.ty);
}

/** Decode one VariantCase. */
export function decodeVariantCase(reader: BinaryReader): VariantCase {
    const tag = decodeConstant(reader);
    const ty = decodeLocalNodeId(reader);

    return {
        tag,
        ty,
    };
}

/** Return one JSON value for one VariantCase. */
export function toJsonVariantCase(value: VariantCase): Json {
    return {
        tag: toJsonConstant(value.tag),
        ty: toJsonLocalNodeId(value.ty),
    };
}

/** Return one VariantCase from one JSON value. */
export function fromJsonVariantCase(value: Json): VariantCase {
    const object = jsonObject(value);

    return {
        tag: fromJsonConstant(jsonField(object, "tag")),
        ty: fromJsonLocalNodeId(jsonField(object, "ty")),
    };
}

/** Dimension size for tensor shapes and formats. */
export type TensorDimension =
    /** Compile time static dimension size. */
    | {
          readonly kind: "static";
          readonly static: bigint;
      }
    /** Symbolic runtime dimension shared across tensors. */
    | {
          readonly kind: "symbol";
          readonly symbol: string;
      }
    /** Runtime dynamic dimension size. */
    | {
          readonly kind: "dynamic";
      }
;

export const TensorDimension = {
    /** Compile time static dimension size. */
    "static"(static_: bigint): TensorDimension {
        return { kind: "static", static: static_ };
    },

    /** Symbolic runtime dimension shared across tensors. */
    "symbol"(symbol_: string): TensorDimension {
        return { kind: "symbol", symbol: symbol_ };
    },

    /** Runtime dynamic dimension size. */
    dynamic(): TensorDimension {
        return { kind: "dynamic" };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: TensorDimension): void {
        encodeTensorDimension(writer, value);
    },

    /** Decode one TensorDimension. */
    decode(reader: BinaryReader): TensorDimension {
        return decodeTensorDimension(reader);
    },

    /** Return this value as JSON. */
    toJson(value: TensorDimension): Json {
        return toJsonTensorDimension(value);
    },

    /** Return one TensorDimension from one JSON value. */
    fromJson(value: Json): TensorDimension {
        return fromJsonTensorDimension(value);
    },
};

/** Encode one TensorDimension. */
export function encodeTensorDimension(writer: BinaryWriter, value: TensorDimension): void {
    switch (value.kind) {
        case "static":
            writer.writeUnsigned(0);
            writer.writeUnsigned(value.static);
            return;
        case "symbol":
            writer.writeUnsigned(1);
            writer.writeString(value.symbol);
            return;
        case "dynamic":
            writer.writeUnsigned(2);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one TensorDimension. */
export function decodeTensorDimension(reader: BinaryReader): TensorDimension {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const static_ = reader.readUnsigned();

            return { kind: "static", static: static_ };
        }
        case 1: {
            const symbol_ = reader.readString();

            return { kind: "symbol", symbol: symbol_ };
        }
        case 2: {
            return { kind: "dynamic" };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one TensorDimension. */
export function toJsonTensorDimension(value: TensorDimension): Json {
    switch (value.kind) {
        case "static":
            return {
                kind: "static",
                static: value.static.toString(),
            };
        case "symbol":
            return {
                kind: "symbol",
                symbol: value.symbol,
            };
        case "dynamic":
            return {
                kind: "dynamic",
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one TensorDimension from one JSON value. */
export function fromJsonTensorDimension(value: Json): TensorDimension {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "static":
            return {
                kind,
                static: jsonBigint(jsonField(object, "static")),
            };
        case "symbol":
            return {
                kind,
                symbol: jsonString(jsonField(object, "symbol")),
            };
        case "dynamic":
            return {
                kind,
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** Format for an owning tensor value. */
export type TensorFormat =
    /** Dense contiguous format. */
    | {
          readonly kind: "dense";
          /** The dimension order. */
          readonly order: TensorDimensionOrder;
      }
;

export const TensorFormat = {
    /** Dense contiguous format. */
    dense(order: TensorDimensionOrder): TensorFormat {
        return { kind: "dense", order };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: TensorFormat): void {
        encodeTensorFormat(writer, value);
    },

    /** Decode one TensorFormat. */
    decode(reader: BinaryReader): TensorFormat {
        return decodeTensorFormat(reader);
    },

    /** Return this value as JSON. */
    toJson(value: TensorFormat): Json {
        return toJsonTensorFormat(value);
    },

    /** Return one TensorFormat from one JSON value. */
    fromJson(value: Json): TensorFormat {
        return fromJsonTensorFormat(value);
    },
};

/** Encode one TensorFormat. */
export function encodeTensorFormat(writer: BinaryWriter, value: TensorFormat): void {
    switch (value.kind) {
        case "dense":
            writer.writeUnsigned(0);
            encodeTensorDimensionOrder(writer, value.order);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one TensorFormat. */
export function decodeTensorFormat(reader: BinaryReader): TensorFormat {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const order = decodeTensorDimensionOrder(reader);

            return {
                kind: "dense",
                order,
            };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one TensorFormat. */
export function toJsonTensorFormat(value: TensorFormat): Json {
    switch (value.kind) {
        case "dense":
            return {
                kind: "dense",
                order: toJsonTensorDimensionOrder(value.order),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one TensorFormat from one JSON value. */
export function fromJsonTensorFormat(value: Json): TensorFormat {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "dense":
            return {
                kind,
                order: fromJsonTensorDimensionOrder(jsonField(object, "order")),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** Dimension order for dense tensor storage. */
export type TensorDimensionOrder = "rowMajor" | "columnMajor";

export const TensorDimensionOrder = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: TensorDimensionOrder): void {
        encodeTensorDimensionOrder(writer, value);
    },

    /** Decode one TensorDimensionOrder. */
    decode(reader: BinaryReader): TensorDimensionOrder {
        return decodeTensorDimensionOrder(reader);
    },

    /** Return this value as JSON. */
    toJson(value: TensorDimensionOrder): Json {
        return toJsonTensorDimensionOrder(value);
    },

    /** Return one TensorDimensionOrder from one JSON value. */
    fromJson(value: Json): TensorDimensionOrder {
        return fromJsonTensorDimensionOrder(value);
    },
};

/** Encode one TensorDimensionOrder. */
export function encodeTensorDimensionOrder(writer: BinaryWriter, value: TensorDimensionOrder): void {
    switch (value) {
        case "rowMajor":
            writer.writeUnsigned(0);
            return;
        case "columnMajor":
            writer.writeUnsigned(1);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one TensorDimensionOrder. */
export function decodeTensorDimensionOrder(reader: BinaryReader): TensorDimensionOrder {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "rowMajor";
        case 1:
            return "columnMajor";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one TensorDimensionOrder. */
export function toJsonTensorDimensionOrder(value: TensorDimensionOrder): Json {
    return value;
}

/** Return one TensorDimensionOrder from one JSON value. */
export function fromJsonTensorDimensionOrder(value: Json): TensorDimensionOrder {
    const variant = jsonString(value);

    switch (variant) {
        case "rowMajor":
            return "rowMajor";
        case "columnMajor":
            return "columnMajor";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** Placement descriptor for tensor storage. */
export type TensorSharding =
    /** Tensor storage is not partitioned across a mesh. */
    | {
          readonly kind: "unsharded";
      }
    /** Tensor storage is mapped across a mesh axis by axis. */
    | {
          readonly kind: "sharding";
          /** The per-axis placement descriptors. */
          readonly axes: ReadonlyArray<TensorShardingAxis>;
      }
;

export const TensorSharding = {
    /** Tensor storage is not partitioned across a mesh. */
    unsharded(): TensorSharding {
        return { kind: "unsharded" };
    },

    /** Tensor storage is mapped across a mesh axis by axis. */
    sharding(axes: ReadonlyArray<TensorShardingAxis>): TensorSharding {
        return { kind: "sharding", axes };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: TensorSharding): void {
        encodeTensorSharding(writer, value);
    },

    /** Decode one TensorSharding. */
    decode(reader: BinaryReader): TensorSharding {
        return decodeTensorSharding(reader);
    },

    /** Return this value as JSON. */
    toJson(value: TensorSharding): Json {
        return toJsonTensorSharding(value);
    },

    /** Return one TensorSharding from one JSON value. */
    fromJson(value: Json): TensorSharding {
        return fromJsonTensorSharding(value);
    },
};

/** Encode one TensorSharding. */
export function encodeTensorSharding(writer: BinaryWriter, value: TensorSharding): void {
    switch (value.kind) {
        case "unsharded":
            writer.writeUnsigned(0);
            return;
        case "sharding":
            writer.writeUnsigned(1);
            writer.writeUnsigned(value.axes.length);
            for (const item0 of value.axes) {
                encodeTensorShardingAxis(writer, item0);
            }
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one TensorSharding. */
export function decodeTensorSharding(reader: BinaryReader): TensorSharding {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            return { kind: "unsharded" };
        }
        case 1: {
            const axes = (() => { const length0 = reader.readNumber(); const items0: Array<TensorShardingAxis> = []; for (let index = 0; index < length0; index += 1) { items0.push(decodeTensorShardingAxis(reader)); } return items0; })();

            return {
                kind: "sharding",
                axes,
            };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one TensorSharding. */
export function toJsonTensorSharding(value: TensorSharding): Json {
    switch (value.kind) {
        case "unsharded":
            return {
                kind: "unsharded",
            };
        case "sharding":
            return {
                kind: "sharding",
                axes: value.axes.map((item0) => toJsonTensorShardingAxis(item0)),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one TensorSharding from one JSON value. */
export function fromJsonTensorSharding(value: Json): TensorSharding {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "unsharded":
            return {
                kind,
            };
        case "sharding":
            return {
                kind,
                axes: jsonArray(jsonField(object, "axes")).map((item0) => fromJsonTensorShardingAxis(item0)),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** Per-axis placement descriptor for a sharded tensor. */
export type TensorShardingAxis =
    /** Split one tensor axis across one mesh axis. */
    | {
          readonly kind: "shard";
          /** The tensor axis being split. */
          readonly axis: number;
      }
    /** Replicate values across one mesh axis. */
    | {
          readonly kind: "replicate";
      }
    /** Store partial results across one mesh axis. */
    | {
          readonly kind: "partial";
          /** The reduction used to combine partial values. */
          readonly reduction: TensorReduction;
      }
;

export const TensorShardingAxis = {
    /** Split one tensor axis across one mesh axis. */
    shard(axis: number): TensorShardingAxis {
        return { kind: "shard", axis };
    },

    /** Replicate values across one mesh axis. */
    replicate(): TensorShardingAxis {
        return { kind: "replicate" };
    },

    /** Store partial results across one mesh axis. */
    partial(reduction: TensorReduction): TensorShardingAxis {
        return { kind: "partial", reduction };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: TensorShardingAxis): void {
        encodeTensorShardingAxis(writer, value);
    },

    /** Decode one TensorShardingAxis. */
    decode(reader: BinaryReader): TensorShardingAxis {
        return decodeTensorShardingAxis(reader);
    },

    /** Return this value as JSON. */
    toJson(value: TensorShardingAxis): Json {
        return toJsonTensorShardingAxis(value);
    },

    /** Return one TensorShardingAxis from one JSON value. */
    fromJson(value: Json): TensorShardingAxis {
        return fromJsonTensorShardingAxis(value);
    },
};

/** Encode one TensorShardingAxis. */
export function encodeTensorShardingAxis(writer: BinaryWriter, value: TensorShardingAxis): void {
    switch (value.kind) {
        case "shard":
            writer.writeUnsigned(0);
            writer.writeSigned(value.axis);
            return;
        case "replicate":
            writer.writeUnsigned(1);
            return;
        case "partial":
            writer.writeUnsigned(2);
            encodeTensorReduction(writer, value.reduction);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one TensorShardingAxis. */
export function decodeTensorShardingAxis(reader: BinaryReader): TensorShardingAxis {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const axis = reader.readSignedNumber();

            return {
                kind: "shard",
                axis,
            };
        }
        case 1: {
            return { kind: "replicate" };
        }
        case 2: {
            const reduction = decodeTensorReduction(reader);

            return {
                kind: "partial",
                reduction,
            };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one TensorShardingAxis. */
export function toJsonTensorShardingAxis(value: TensorShardingAxis): Json {
    switch (value.kind) {
        case "shard":
            return {
                kind: "shard",
                axis: value.axis,
            };
        case "replicate":
            return {
                kind: "replicate",
            };
        case "partial":
            return {
                kind: "partial",
                reduction: toJsonTensorReduction(value.reduction),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one TensorShardingAxis from one JSON value. */
export function fromJsonTensorShardingAxis(value: Json): TensorShardingAxis {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "shard":
            return {
                kind,
                axis: jsonInteger(jsonField(object, "axis")),
            };
        case "replicate":
            return {
                kind,
            };
        case "partial":
            return {
                kind,
                reduction: fromJsonTensorReduction(jsonField(object, "reduction")),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** Reduction used when partial tensor shards are combined. */
export type TensorReduction = "add" | "multiply" | "minimum" | "maximum" | "and" | "or";

export const TensorReduction = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: TensorReduction): void {
        encodeTensorReduction(writer, value);
    },

    /** Decode one TensorReduction. */
    decode(reader: BinaryReader): TensorReduction {
        return decodeTensorReduction(reader);
    },

    /** Return this value as JSON. */
    toJson(value: TensorReduction): Json {
        return toJsonTensorReduction(value);
    },

    /** Return one TensorReduction from one JSON value. */
    fromJson(value: Json): TensorReduction {
        return fromJsonTensorReduction(value);
    },
};

/** Encode one TensorReduction. */
export function encodeTensorReduction(writer: BinaryWriter, value: TensorReduction): void {
    switch (value) {
        case "add":
            writer.writeUnsigned(0);
            return;
        case "multiply":
            writer.writeUnsigned(1);
            return;
        case "minimum":
            writer.writeUnsigned(2);
            return;
        case "maximum":
            writer.writeUnsigned(3);
            return;
        case "and":
            writer.writeUnsigned(4);
            return;
        case "or":
            writer.writeUnsigned(5);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one TensorReduction. */
export function decodeTensorReduction(reader: BinaryReader): TensorReduction {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "add";
        case 1:
            return "multiply";
        case 2:
            return "minimum";
        case 3:
            return "maximum";
        case 4:
            return "and";
        case 5:
            return "or";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one TensorReduction. */
export function toJsonTensorReduction(value: TensorReduction): Json {
    return value;
}

/** Return one TensorReduction from one JSON value. */
export function fromJsonTensorReduction(value: Json): TensorReduction {
    const variant = jsonString(value);

    switch (variant) {
        case "add":
            return "add";
        case "multiply":
            return "multiply";
        case "minimum":
            return "minimum";
        case "maximum":
            return "maximum";
        case "and":
            return "and";
        case "or":
            return "or";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** Format descriptor for a tensor view. */
export type TensorViewFormat =
    /** Dense contiguous view. */
    | {
          readonly kind: "dense";
          /** The dimension order. */
          readonly order: TensorDimensionOrder;
      }
    /** Explicit strided view. */
    | {
          readonly kind: "strided";
      }
;

export const TensorViewFormat = {
    /** Dense contiguous view. */
    dense(order: TensorDimensionOrder): TensorViewFormat {
        return { kind: "dense", order };
    },

    /** Explicit strided view. */
    strided(): TensorViewFormat {
        return { kind: "strided" };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: TensorViewFormat): void {
        encodeTensorViewFormat(writer, value);
    },

    /** Decode one TensorViewFormat. */
    decode(reader: BinaryReader): TensorViewFormat {
        return decodeTensorViewFormat(reader);
    },

    /** Return this value as JSON. */
    toJson(value: TensorViewFormat): Json {
        return toJsonTensorViewFormat(value);
    },

    /** Return one TensorViewFormat from one JSON value. */
    fromJson(value: Json): TensorViewFormat {
        return fromJsonTensorViewFormat(value);
    },
};

/** Encode one TensorViewFormat. */
export function encodeTensorViewFormat(writer: BinaryWriter, value: TensorViewFormat): void {
    switch (value.kind) {
        case "dense":
            writer.writeUnsigned(0);
            encodeTensorDimensionOrder(writer, value.order);
            return;
        case "strided":
            writer.writeUnsigned(1);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one TensorViewFormat. */
export function decodeTensorViewFormat(reader: BinaryReader): TensorViewFormat {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const order = decodeTensorDimensionOrder(reader);

            return {
                kind: "dense",
                order,
            };
        }
        case 1: {
            return { kind: "strided" };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one TensorViewFormat. */
export function toJsonTensorViewFormat(value: TensorViewFormat): Json {
    switch (value.kind) {
        case "dense":
            return {
                kind: "dense",
                order: toJsonTensorDimensionOrder(value.order),
            };
        case "strided":
            return {
                kind: "strided",
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one TensorViewFormat from one JSON value. */
export function fromJsonTensorViewFormat(value: Json): TensorViewFormat {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "dense":
            return {
                kind,
                order: fromJsonTensorDimensionOrder(jsonField(object, "order")),
            };
        case "strided":
            return {
                kind,
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** A named type alias in MIR text format. */
export type TypeAlias = {
    /** Alias name (without the leading `@`). */
    readonly name: StringId;
    /** Lifetime parameters in type-local slot order. */
    readonly lifetimes: ReadonlyArray<LifetimeParameter>;
    /** The aliased type. */
    readonly ty: LocalNodeId;
};

export const TypeAlias = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: TypeAlias): void {
        encodeTypeAlias(writer, value);
    },

    /** Decode one TypeAlias. */
    decode(reader: BinaryReader): TypeAlias {
        return decodeTypeAlias(reader);
    },

    /** Return this value as JSON. */
    toJson(value: TypeAlias): Json {
        return toJsonTypeAlias(value);
    },

    /** Return one TypeAlias from one JSON value. */
    fromJson(value: Json): TypeAlias {
        return fromJsonTypeAlias(value);
    },
};

/** Encode one TypeAlias. */
export function encodeTypeAlias(writer: BinaryWriter, value: TypeAlias): void {
    encodeStringId(writer, value.name);
    writer.writeUnsigned(value.lifetimes.length);
    for (const item1 of value.lifetimes) {
        encodeLifetimeParameter(writer, item1);
    }
    encodeLocalNodeId(writer, value.ty);
}

/** Decode one TypeAlias. */
export function decodeTypeAlias(reader: BinaryReader): TypeAlias {
    const name = decodeStringId(reader);
    const lifetimes = (() => { const length1 = reader.readNumber(); const items1: Array<LifetimeParameter> = []; for (let index = 0; index < length1; index += 1) { items1.push(decodeLifetimeParameter(reader)); } return items1; })();
    const ty = decodeLocalNodeId(reader);

    return {
        name,
        lifetimes,
        ty,
    };
}

/** Return one JSON value for one TypeAlias. */
export function toJsonTypeAlias(value: TypeAlias): Json {
    return {
        name: toJsonStringId(value.name),
        lifetimes: value.lifetimes.map((item0) => toJsonLifetimeParameter(item0)),
        ty: toJsonLocalNodeId(value.ty),
    };
}

/** Return one TypeAlias from one JSON value. */
export function fromJsonTypeAlias(value: Json): TypeAlias {
    const object = jsonObject(value);

    return {
        name: fromJsonStringId(jsonField(object, "name")),
        lifetimes: jsonArray(jsonField(object, "lifetimes")).map((item0) => fromJsonLifetimeParameter(item0)),
        ty: fromJsonLocalNodeId(jsonField(object, "ty")),
    };
}

/** A field in a struct type. */
export type Field = {
    /** Name (optional). */
    readonly name?: StringId;
    /** Type of the field. */
    readonly ty: LocalNodeId;
};

export const Field = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: Field): void {
        encodeField(writer, value);
    },

    /** Decode one Field. */
    decode(reader: BinaryReader): Field {
        return decodeField(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Field): Json {
        return toJsonField(value);
    },

    /** Return one Field from one JSON value. */
    fromJson(value: Json): Field {
        return fromJsonField(value);
    },
};

/** Encode one Field. */
export function encodeField(writer: BinaryWriter, value: Field): void {
    writer.writeOption(value.name, (value0) => {
        encodeStringId(writer, value0);
    });
    encodeLocalNodeId(writer, value.ty);
}

/** Decode one Field. */
export function decodeField(reader: BinaryReader): Field {
    const name = reader.readOption(() => decodeStringId(reader));
    const ty = decodeLocalNodeId(reader);

    return {
        ...(name === undefined ? {} : { name }),
        ty,
    };
}

/** Return one JSON value for one Field. */
export function toJsonField(value: Field): Json {
    return {
        ...(value.name === undefined ? {} : { name: toJsonStringId(value.name) }),
        ty: toJsonLocalNodeId(value.ty),
    };
}

/** Return one Field from one JSON value. */
export function fromJsonField(value: Json): Field {
    const object = jsonObject(value);

    return {
        name: jsonOptional(object, "name", (value) => fromJsonStringId(value)),
        ty: fromJsonLocalNodeId(jsonField(object, "ty")),
    };
}

/** Concrete type in MIR (post-monomorphization). */
export type Type =
    /** Invalid type produced while recovering malformed MIR text. */
    | {
          readonly kind: "error";
      }
    /** Void / unit type (no value). */
    | {
          readonly kind: "void";
      }
    /** Boolean (1 bit logical, typically 1 byte). */
    | {
          readonly kind: "boolean";
      }
    /** Integer with explicit width and signedness. */
    | {
          readonly kind: "int";
          readonly width: number;
          readonly isSigned: boolean;
      }
    /** Pointer-sized signed integer. */
    | {
          readonly kind: "isize";
      }
    /** Pointer-sized unsigned integer. */
    | {
          readonly kind: "usize";
      }
    /** Floating point with concrete representation. */
    | {
          readonly kind: "float";
          readonly float: FloatType;
      }
    /** Runtime type descriptor handle. */
    | {
          readonly kind: "typeDescriptor";
      }
    /** Compact runtime type identity token. */
    | {
          readonly kind: "typeId";
      }
    /** Atomic storage cell for one value type. */
    | {
          readonly kind: "atomic";
          /** The stored value type. */
          readonly value: LocalNodeId;
      }
    /** Runtime-erased value satisfying one dynamic constraint. */
    | {
          readonly kind: "dynamic";
          /** The lowered dynamic constraint type. */
          readonly constraint: LocalNodeId;
      }
    /** Type use with applied lifetime arguments. */
    | {
          readonly kind: "withLifetimes";
          /** The type being applied. */
          readonly base: LocalNodeId;
          /** The applied lifetime arguments. */
          readonly lifetimes: ReadonlyArray<Lifetime>;
      }
    /** Reference with explicit kind and access. */
    | {
          readonly kind: "reference";
          /** The reference kind (managed, unique, borrowed, raw). */
          readonly kindValue: ReferenceKind;
          /** Lifetime roots for borrowed references. */
          readonly lifetime: Lifetime;
          /** The space for this reference. */
          readonly space: Space;
          /** The access exposed through this reference. */
          readonly access: Access;
          /** The referenced type. */
          readonly pointee: LocalNodeId;
          /** The nullish values allowed by this reference. */
          readonly nullability: Nullability;
      }
    /** Slice into memory, a repeated element with explicit kind and access. */
    | {
          readonly kind: "slice";
          /** The reference kind of the slice base. */
          readonly kindValue: ReferenceKind;
          /** Lifetime roots for borrowed slices. */
          readonly lifetime: Lifetime;
          /** The element type of the slice. */
          readonly element: LocalNodeId;
          /** The space of the slice base. */
          readonly space: Space;
          /** The element access exposed by the slice. */
          readonly access: Access;
          /** The nullish values allowed by this slice descriptor. */
          readonly nullability: Nullability;
      }
    /** Linear token for one uninitialized allocation. */
    | {
          readonly kind: "uninit";
          /** The value under construction. */
          readonly value: LocalNodeId;
      }
    /** Fixed array: `[T; N]`. */
    | {
          readonly kind: "fixedArray";
          /** The element type of the array. */
          readonly element: LocalNodeId;
          /** The number of elements in the array. */
          readonly length: bigint;
          /** Copy of this fixed array type. */
          readonly copy: Copy;
      }
    /** Tuple: `(T1, T2, ...)`. */
    | {
          readonly kind: "tuple";
          /** The element types of the tuple. */
          readonly elements: ReadonlyArray<LocalNodeId>;
          /** Copy of this tuple type. */
          readonly copy: Copy;
      }
    /** Struct. */
    | {
          readonly kind: "struct";
          /** The fields of the struct. */
          readonly fields: ReadonlyArray<LocalNodeId>;
          /** Copy of this struct type. */
          readonly copy: Copy;
      }
    /** Nominal newtype wrapping an inner type. */
    | {
          readonly kind: "newtype";
          /** The wrapped inner type. */
          readonly inner: LocalNodeId;
          /** Copy of this newtype. */
          readonly copy: Copy;
      }
    /** Physical tagged sum value. */
    | {
          readonly kind: "variant";
          /** The tag value type. */
          readonly tag: LocalNodeId;
          /** The physical payload storage type. */
          readonly storage: LocalNodeId;
          /** The cases keyed by tag value. */
          readonly cases: ReadonlyArray<VariantCase>;
          /** Copy of this variant type. */
          readonly copy: Copy;
      }
    /** Fixed-width vector value. */
    | {
          readonly kind: "vector";
          /** The element type. */
          readonly element: LocalNodeId;
          /** The number of lanes. */
          readonly lanes: number;
          /** Copy of this vector type. */
          readonly copy: Copy;
      }
    /** Ranked tensor value with static or dynamic shape. */
    | {
          readonly kind: "tensor";
          /** The element type. */
          readonly element: LocalNodeId;
          /** The static shape. */
          readonly shape: ReadonlyArray<TensorDimension>;
          /** The tensor format. */
          readonly format: TensorFormat;
          /** The tensor placement. */
          readonly sharding: TensorSharding;
          /** Copy of this tensor type. */
          readonly copy: Copy;
      }
    /** Reference-like view into tensor-shaped memory. */
    | {
          readonly kind: "tensorView";
          /** The reference kind (managed, unique, borrowed, raw). */
          readonly kindValue: ReferenceKind;
          /** Lifetime roots for borrowed tensor views. */
          readonly lifetime: Lifetime;
          /** The space for this view. */
          readonly space: Space;
          /** The access exposed through this view. */
          readonly access: Access;
          /** The element type. */
          readonly element: LocalNodeId;
          /** The static shape. */
          readonly shape: ReadonlyArray<TensorDimension>;
          /** The tensor view format. */
          readonly format: TensorViewFormat;
          /** The tensor placement. */
          readonly sharding: TensorSharding;
          /** The nullish values allowed by this view descriptor. */
          readonly nullability: Nullability;
      }
    /** Bare function signature. */
    | {
          readonly kind: "functionSignature";
          /** Lifetime parameters in signature-local slot order. */
          readonly lifetimes: ReadonlyArray<LifetimeParameter>;
          /** The parameters of the function. */
          readonly parameters: ReadonlyArray<SignatureParameter>;
          /** The result type of the function. */
          readonly result: LocalNodeId;
      }
    /** Function value type. */
    | {
          readonly kind: "function";
          /** The bare function signature. */
          readonly signature: LocalNodeId;
          /** The captured environment reference. */
          readonly environment: LocalNodeId;
      }
    /** Function pointer type. */
    | {
          readonly kind: "functionPointer";
          /** The bare function signature. */
          readonly signature: LocalNodeId;
      }
;

export const Type = {
    /** Invalid type produced while recovering malformed MIR text. */
    error(): Type {
        return { kind: "error" };
    },

    /** Void / unit type (no value). */
    "void"(): Type {
        return { kind: "void" };
    },

    /** Boolean (1 bit logical, typically 1 byte). */
    "boolean"(): Type {
        return { kind: "boolean" };
    },

    /** Integer with explicit width and signedness. */
    int(width: number, isSigned: boolean): Type {
        return { kind: "int", width, isSigned };
    },

    /** Pointer-sized signed integer. */
    isize(): Type {
        return { kind: "isize" };
    },

    /** Pointer-sized unsigned integer. */
    usize(): Type {
        return { kind: "usize" };
    },

    /** Floating point with concrete representation. */
    float(float: FloatType): Type {
        return { kind: "float", float };
    },

    /** Runtime type descriptor handle. */
    typeDescriptor(): Type {
        return { kind: "typeDescriptor" };
    },

    /** Compact runtime type identity token. */
    typeId(): Type {
        return { kind: "typeId" };
    },

    /** Atomic storage cell for one value type. */
    atomic(value: LocalNodeId): Type {
        return { kind: "atomic", value };
    },

    /** Runtime-erased value satisfying one dynamic constraint. */
    dynamic(constraint: LocalNodeId): Type {
        return { kind: "dynamic", constraint };
    },

    /** Type use with applied lifetime arguments. */
    withLifetimes(base: LocalNodeId, lifetimes: ReadonlyArray<Lifetime>): Type {
        return { kind: "withLifetimes", base, lifetimes };
    },

    /** Reference with explicit kind and access. */
    reference(kind: ReferenceKind, lifetime: Lifetime, space: Space, access: Access, pointee: LocalNodeId, nullability: Nullability): Type {
        return { kind: "reference", kindValue: kind, lifetime, space, access, pointee, nullability };
    },

    /** Slice into memory, a repeated element with explicit kind and access. */
    slice(kind: ReferenceKind, lifetime: Lifetime, element: LocalNodeId, space: Space, access: Access, nullability: Nullability): Type {
        return { kind: "slice", kindValue: kind, lifetime, element, space, access, nullability };
    },

    /** Linear token for one uninitialized allocation. */
    uninit(value: LocalNodeId): Type {
        return { kind: "uninit", value };
    },

    /** Fixed array: `[T; N]`. */
    fixedArray(element: LocalNodeId, length: bigint, copy: Copy): Type {
        return { kind: "fixedArray", element, length, copy };
    },

    /** Tuple: `(T1, T2, ...)`. */
    tuple(elements: ReadonlyArray<LocalNodeId>, copy: Copy): Type {
        return { kind: "tuple", elements, copy };
    },

    /** Struct. */
    struct(fields: ReadonlyArray<LocalNodeId>, copy: Copy): Type {
        return { kind: "struct", fields, copy };
    },

    /** Nominal newtype wrapping an inner type. */
    newtype(inner: LocalNodeId, copy: Copy): Type {
        return { kind: "newtype", inner, copy };
    },

    /** Physical tagged sum value. */
    variant(tag: LocalNodeId, storage: LocalNodeId, cases: ReadonlyArray<VariantCase>, copy: Copy): Type {
        return { kind: "variant", tag, storage, cases, copy };
    },

    /** Fixed-width vector value. */
    vector(element: LocalNodeId, lanes: number, copy: Copy): Type {
        return { kind: "vector", element, lanes, copy };
    },

    /** Ranked tensor value with static or dynamic shape. */
    tensor(element: LocalNodeId, shape: ReadonlyArray<TensorDimension>, format: TensorFormat, sharding: TensorSharding, copy: Copy): Type {
        return { kind: "tensor", element, shape, format, sharding, copy };
    },

    /** Reference-like view into tensor-shaped memory. */
    tensorView(kind: ReferenceKind, lifetime: Lifetime, space: Space, access: Access, element: LocalNodeId, shape: ReadonlyArray<TensorDimension>, format: TensorViewFormat, sharding: TensorSharding, nullability: Nullability): Type {
        return { kind: "tensorView", kindValue: kind, lifetime, space, access, element, shape, format, sharding, nullability };
    },

    /** Bare function signature. */
    functionSignature(lifetimes: ReadonlyArray<LifetimeParameter>, parameters: ReadonlyArray<SignatureParameter>, result: LocalNodeId): Type {
        return { kind: "functionSignature", lifetimes, parameters, result };
    },

    /** Function value type. */
    "function"(signature: LocalNodeId, environment: LocalNodeId): Type {
        return { kind: "function", signature, environment };
    },

    /** Function pointer type. */
    functionPointer(signature: LocalNodeId): Type {
        return { kind: "functionPointer", signature };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: Type): void {
        encodeType(writer, value);
    },

    /** Decode one Type. */
    decode(reader: BinaryReader): Type {
        return decodeType(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Type): Json {
        return toJsonType(value);
    },

    /** Return one Type from one JSON value. */
    fromJson(value: Json): Type {
        return fromJsonType(value);
    },
};

/** Encode one Type. */
export function encodeType(writer: BinaryWriter, value: Type): void {
    switch (value.kind) {
        case "error":
            writer.writeUnsigned(0);
            return;
        case "void":
            writer.writeUnsigned(1);
            return;
        case "boolean":
            writer.writeUnsigned(2);
            return;
        case "int":
            writer.writeUnsigned(3);
            writer.writeUnsigned(value.width);
            writer.writeBool(value.isSigned);
            return;
        case "isize":
            writer.writeUnsigned(4);
            return;
        case "usize":
            writer.writeUnsigned(5);
            return;
        case "float":
            writer.writeUnsigned(6);
            encodeFloatType(writer, value.float);
            return;
        case "typeDescriptor":
            writer.writeUnsigned(7);
            return;
        case "typeId":
            writer.writeUnsigned(8);
            return;
        case "atomic":
            writer.writeUnsigned(9);
            encodeLocalNodeId(writer, value.value);
            return;
        case "dynamic":
            writer.writeUnsigned(10);
            encodeLocalNodeId(writer, value.constraint);
            return;
        case "withLifetimes":
            writer.writeUnsigned(11);
            encodeLocalNodeId(writer, value.base);
            writer.writeUnsigned(value.lifetimes.length);
            for (const item1 of value.lifetimes) {
                encodeLifetime(writer, item1);
            }
            return;
        case "reference":
            writer.writeUnsigned(12);
            encodeReferenceKind(writer, value.kindValue);
            encodeLifetime(writer, value.lifetime);
            encodeSpace(writer, value.space);
            encodeAccess(writer, value.access);
            encodeLocalNodeId(writer, value.pointee);
            encodeNullability(writer, value.nullability);
            return;
        case "slice":
            writer.writeUnsigned(13);
            encodeReferenceKind(writer, value.kindValue);
            encodeLifetime(writer, value.lifetime);
            encodeLocalNodeId(writer, value.element);
            encodeSpace(writer, value.space);
            encodeAccess(writer, value.access);
            encodeNullability(writer, value.nullability);
            return;
        case "uninit":
            writer.writeUnsigned(14);
            encodeLocalNodeId(writer, value.value);
            return;
        case "fixedArray":
            writer.writeUnsigned(15);
            encodeLocalNodeId(writer, value.element);
            writer.writeUnsigned(value.length);
            encodeCopy(writer, value.copy);
            return;
        case "tuple":
            writer.writeUnsigned(16);
            writer.writeUnsigned(value.elements.length);
            for (const item0 of value.elements) {
                encodeLocalNodeId(writer, item0);
            }
            encodeCopy(writer, value.copy);
            return;
        case "struct":
            writer.writeUnsigned(17);
            writer.writeUnsigned(value.fields.length);
            for (const item0 of value.fields) {
                encodeLocalNodeId(writer, item0);
            }
            encodeCopy(writer, value.copy);
            return;
        case "newtype":
            writer.writeUnsigned(18);
            encodeLocalNodeId(writer, value.inner);
            encodeCopy(writer, value.copy);
            return;
        case "variant":
            writer.writeUnsigned(19);
            encodeLocalNodeId(writer, value.tag);
            encodeLocalNodeId(writer, value.storage);
            writer.writeUnsigned(value.cases.length);
            for (const item2 of value.cases) {
                encodeVariantCase(writer, item2);
            }
            encodeCopy(writer, value.copy);
            return;
        case "vector":
            writer.writeUnsigned(20);
            encodeLocalNodeId(writer, value.element);
            writer.writeUnsigned(value.lanes);
            encodeCopy(writer, value.copy);
            return;
        case "tensor":
            writer.writeUnsigned(21);
            encodeLocalNodeId(writer, value.element);
            writer.writeUnsigned(value.shape.length);
            for (const item1 of value.shape) {
                encodeTensorDimension(writer, item1);
            }
            encodeTensorFormat(writer, value.format);
            encodeTensorSharding(writer, value.sharding);
            encodeCopy(writer, value.copy);
            return;
        case "tensorView":
            writer.writeUnsigned(22);
            encodeReferenceKind(writer, value.kindValue);
            encodeLifetime(writer, value.lifetime);
            encodeSpace(writer, value.space);
            encodeAccess(writer, value.access);
            encodeLocalNodeId(writer, value.element);
            writer.writeUnsigned(value.shape.length);
            for (const item5 of value.shape) {
                encodeTensorDimension(writer, item5);
            }
            encodeTensorViewFormat(writer, value.format);
            encodeTensorSharding(writer, value.sharding);
            encodeNullability(writer, value.nullability);
            return;
        case "functionSignature":
            writer.writeUnsigned(23);
            writer.writeUnsigned(value.lifetimes.length);
            for (const item0 of value.lifetimes) {
                encodeLifetimeParameter(writer, item0);
            }
            writer.writeUnsigned(value.parameters.length);
            for (const item1 of value.parameters) {
                encodeSignatureParameter(writer, item1);
            }
            encodeLocalNodeId(writer, value.result);
            return;
        case "function":
            writer.writeUnsigned(24);
            encodeLocalNodeId(writer, value.signature);
            encodeLocalNodeId(writer, value.environment);
            return;
        case "functionPointer":
            writer.writeUnsigned(25);
            encodeLocalNodeId(writer, value.signature);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one Type. */
export function decodeType(reader: BinaryReader): Type {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            return { kind: "error" };
        }
        case 1: {
            return { kind: "void" };
        }
        case 2: {
            return { kind: "boolean" };
        }
        case 3: {
            const width = reader.readNumber();
            const isSigned = reader.readBool();

            return {
                kind: "int",
                width,
                isSigned,
            };
        }
        case 4: {
            return { kind: "isize" };
        }
        case 5: {
            return { kind: "usize" };
        }
        case 6: {
            const float = decodeFloatType(reader);

            return { kind: "float", float };
        }
        case 7: {
            return { kind: "typeDescriptor" };
        }
        case 8: {
            return { kind: "typeId" };
        }
        case 9: {
            const value = decodeLocalNodeId(reader);

            return {
                kind: "atomic",
                value,
            };
        }
        case 10: {
            const constraint = decodeLocalNodeId(reader);

            return {
                kind: "dynamic",
                constraint,
            };
        }
        case 11: {
            const base = decodeLocalNodeId(reader);
            const lifetimes = (() => { const length1 = reader.readNumber(); const items1: Array<Lifetime> = []; for (let index = 0; index < length1; index += 1) { items1.push(decodeLifetime(reader)); } return items1; })();

            return {
                kind: "withLifetimes",
                base,
                lifetimes,
            };
        }
        case 12: {
            const kindValue = decodeReferenceKind(reader);
            const lifetime = decodeLifetime(reader);
            const space = decodeSpace(reader);
            const access = decodeAccess(reader);
            const pointee = decodeLocalNodeId(reader);
            const nullability = decodeNullability(reader);

            return {
                kind: "reference",
                kindValue,
                lifetime,
                space,
                access,
                pointee,
                nullability,
            };
        }
        case 13: {
            const kindValue = decodeReferenceKind(reader);
            const lifetime = decodeLifetime(reader);
            const element = decodeLocalNodeId(reader);
            const space = decodeSpace(reader);
            const access = decodeAccess(reader);
            const nullability = decodeNullability(reader);

            return {
                kind: "slice",
                kindValue,
                lifetime,
                element,
                space,
                access,
                nullability,
            };
        }
        case 14: {
            const value = decodeLocalNodeId(reader);

            return {
                kind: "uninit",
                value,
            };
        }
        case 15: {
            const element = decodeLocalNodeId(reader);
            const length = reader.readUnsigned();
            const copy = decodeCopy(reader);

            return {
                kind: "fixedArray",
                element,
                length,
                copy,
            };
        }
        case 16: {
            const elements = (() => { const length0 = reader.readNumber(); const items0: Array<LocalNodeId> = []; for (let index = 0; index < length0; index += 1) { items0.push(decodeLocalNodeId(reader)); } return items0; })();
            const copy = decodeCopy(reader);

            return {
                kind: "tuple",
                elements,
                copy,
            };
        }
        case 17: {
            const fields = (() => { const length0 = reader.readNumber(); const items0: Array<LocalNodeId> = []; for (let index = 0; index < length0; index += 1) { items0.push(decodeLocalNodeId(reader)); } return items0; })();
            const copy = decodeCopy(reader);

            return {
                kind: "struct",
                fields,
                copy,
            };
        }
        case 18: {
            const inner = decodeLocalNodeId(reader);
            const copy = decodeCopy(reader);

            return {
                kind: "newtype",
                inner,
                copy,
            };
        }
        case 19: {
            const tag = decodeLocalNodeId(reader);
            const storage = decodeLocalNodeId(reader);
            const cases = (() => { const length2 = reader.readNumber(); const items2: Array<VariantCase> = []; for (let index = 0; index < length2; index += 1) { items2.push(decodeVariantCase(reader)); } return items2; })();
            const copy = decodeCopy(reader);

            return {
                kind: "variant",
                tag,
                storage,
                cases,
                copy,
            };
        }
        case 20: {
            const element = decodeLocalNodeId(reader);
            const lanes = reader.readNumber();
            const copy = decodeCopy(reader);

            return {
                kind: "vector",
                element,
                lanes,
                copy,
            };
        }
        case 21: {
            const element = decodeLocalNodeId(reader);
            const shape = (() => { const length1 = reader.readNumber(); const items1: Array<TensorDimension> = []; for (let index = 0; index < length1; index += 1) { items1.push(decodeTensorDimension(reader)); } return items1; })();
            const format = decodeTensorFormat(reader);
            const sharding = decodeTensorSharding(reader);
            const copy = decodeCopy(reader);

            return {
                kind: "tensor",
                element,
                shape,
                format,
                sharding,
                copy,
            };
        }
        case 22: {
            const kindValue = decodeReferenceKind(reader);
            const lifetime = decodeLifetime(reader);
            const space = decodeSpace(reader);
            const access = decodeAccess(reader);
            const element = decodeLocalNodeId(reader);
            const shape = (() => { const length5 = reader.readNumber(); const items5: Array<TensorDimension> = []; for (let index = 0; index < length5; index += 1) { items5.push(decodeTensorDimension(reader)); } return items5; })();
            const format = decodeTensorViewFormat(reader);
            const sharding = decodeTensorSharding(reader);
            const nullability = decodeNullability(reader);

            return {
                kind: "tensorView",
                kindValue,
                lifetime,
                space,
                access,
                element,
                shape,
                format,
                sharding,
                nullability,
            };
        }
        case 23: {
            const lifetimes = (() => { const length0 = reader.readNumber(); const items0: Array<LifetimeParameter> = []; for (let index = 0; index < length0; index += 1) { items0.push(decodeLifetimeParameter(reader)); } return items0; })();
            const parameters = (() => { const length1 = reader.readNumber(); const items1: Array<SignatureParameter> = []; for (let index = 0; index < length1; index += 1) { items1.push(decodeSignatureParameter(reader)); } return items1; })();
            const result = decodeLocalNodeId(reader);

            return {
                kind: "functionSignature",
                lifetimes,
                parameters,
                result,
            };
        }
        case 24: {
            const signature = decodeLocalNodeId(reader);
            const environment = decodeLocalNodeId(reader);

            return {
                kind: "function",
                signature,
                environment,
            };
        }
        case 25: {
            const signature = decodeLocalNodeId(reader);

            return {
                kind: "functionPointer",
                signature,
            };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one Type. */
export function toJsonType(value: Type): Json {
    switch (value.kind) {
        case "error":
            return {
                kind: "error",
            };
        case "void":
            return {
                kind: "void",
            };
        case "boolean":
            return {
                kind: "boolean",
            };
        case "int":
            return {
                kind: "int",
                width: value.width,
                isSigned: value.isSigned,
            };
        case "isize":
            return {
                kind: "isize",
            };
        case "usize":
            return {
                kind: "usize",
            };
        case "float":
            return {
                kind: "float",
                float: toJsonFloatType(value.float),
            };
        case "typeDescriptor":
            return {
                kind: "typeDescriptor",
            };
        case "typeId":
            return {
                kind: "typeId",
            };
        case "atomic":
            return {
                kind: "atomic",
                value: toJsonLocalNodeId(value.value),
            };
        case "dynamic":
            return {
                kind: "dynamic",
                constraint: toJsonLocalNodeId(value.constraint),
            };
        case "withLifetimes":
            return {
                kind: "withLifetimes",
                base: toJsonLocalNodeId(value.base),
                lifetimes: value.lifetimes.map((item0) => toJsonLifetime(item0)),
            };
        case "reference":
            return {
                kind: "reference",
                kindValue: toJsonReferenceKind(value.kindValue),
                lifetime: toJsonLifetime(value.lifetime),
                space: toJsonSpace(value.space),
                access: toJsonAccess(value.access),
                pointee: toJsonLocalNodeId(value.pointee),
                nullability: toJsonNullability(value.nullability),
            };
        case "slice":
            return {
                kind: "slice",
                kindValue: toJsonReferenceKind(value.kindValue),
                lifetime: toJsonLifetime(value.lifetime),
                element: toJsonLocalNodeId(value.element),
                space: toJsonSpace(value.space),
                access: toJsonAccess(value.access),
                nullability: toJsonNullability(value.nullability),
            };
        case "uninit":
            return {
                kind: "uninit",
                value: toJsonLocalNodeId(value.value),
            };
        case "fixedArray":
            return {
                kind: "fixedArray",
                element: toJsonLocalNodeId(value.element),
                length: value.length.toString(),
                copy: toJsonCopy(value.copy),
            };
        case "tuple":
            return {
                kind: "tuple",
                elements: value.elements.map((item0) => toJsonLocalNodeId(item0)),
                copy: toJsonCopy(value.copy),
            };
        case "struct":
            return {
                kind: "struct",
                fields: value.fields.map((item0) => toJsonLocalNodeId(item0)),
                copy: toJsonCopy(value.copy),
            };
        case "newtype":
            return {
                kind: "newtype",
                inner: toJsonLocalNodeId(value.inner),
                copy: toJsonCopy(value.copy),
            };
        case "variant":
            return {
                kind: "variant",
                tag: toJsonLocalNodeId(value.tag),
                storage: toJsonLocalNodeId(value.storage),
                cases: value.cases.map((item0) => toJsonVariantCase(item0)),
                copy: toJsonCopy(value.copy),
            };
        case "vector":
            return {
                kind: "vector",
                element: toJsonLocalNodeId(value.element),
                lanes: value.lanes,
                copy: toJsonCopy(value.copy),
            };
        case "tensor":
            return {
                kind: "tensor",
                element: toJsonLocalNodeId(value.element),
                shape: value.shape.map((item0) => toJsonTensorDimension(item0)),
                format: toJsonTensorFormat(value.format),
                sharding: toJsonTensorSharding(value.sharding),
                copy: toJsonCopy(value.copy),
            };
        case "tensorView":
            return {
                kind: "tensorView",
                kindValue: toJsonReferenceKind(value.kindValue),
                lifetime: toJsonLifetime(value.lifetime),
                space: toJsonSpace(value.space),
                access: toJsonAccess(value.access),
                element: toJsonLocalNodeId(value.element),
                shape: value.shape.map((item0) => toJsonTensorDimension(item0)),
                format: toJsonTensorViewFormat(value.format),
                sharding: toJsonTensorSharding(value.sharding),
                nullability: toJsonNullability(value.nullability),
            };
        case "functionSignature":
            return {
                kind: "functionSignature",
                lifetimes: value.lifetimes.map((item0) => toJsonLifetimeParameter(item0)),
                parameters: value.parameters.map((item0) => toJsonSignatureParameter(item0)),
                result: toJsonLocalNodeId(value.result),
            };
        case "function":
            return {
                kind: "function",
                signature: toJsonLocalNodeId(value.signature),
                environment: toJsonLocalNodeId(value.environment),
            };
        case "functionPointer":
            return {
                kind: "functionPointer",
                signature: toJsonLocalNodeId(value.signature),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one Type from one JSON value. */
export function fromJsonType(value: Json): Type {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "error":
            return {
                kind,
            };
        case "void":
            return {
                kind,
            };
        case "boolean":
            return {
                kind,
            };
        case "int":
            return {
                kind,
                width: jsonInteger(jsonField(object, "width")),
                isSigned: jsonBool(jsonField(object, "isSigned")),
            };
        case "isize":
            return {
                kind,
            };
        case "usize":
            return {
                kind,
            };
        case "float":
            return {
                kind,
                float: fromJsonFloatType(jsonField(object, "float")),
            };
        case "typeDescriptor":
            return {
                kind,
            };
        case "typeId":
            return {
                kind,
            };
        case "atomic":
            return {
                kind,
                value: fromJsonLocalNodeId(jsonField(object, "value")),
            };
        case "dynamic":
            return {
                kind,
                constraint: fromJsonLocalNodeId(jsonField(object, "constraint")),
            };
        case "withLifetimes":
            return {
                kind,
                base: fromJsonLocalNodeId(jsonField(object, "base")),
                lifetimes: jsonArray(jsonField(object, "lifetimes")).map((item0) => fromJsonLifetime(item0)),
            };
        case "reference":
            return {
                kind,
                kindValue: fromJsonReferenceKind(jsonField(object, "kindValue")),
                lifetime: fromJsonLifetime(jsonField(object, "lifetime")),
                space: fromJsonSpace(jsonField(object, "space")),
                access: fromJsonAccess(jsonField(object, "access")),
                pointee: fromJsonLocalNodeId(jsonField(object, "pointee")),
                nullability: fromJsonNullability(jsonField(object, "nullability")),
            };
        case "slice":
            return {
                kind,
                kindValue: fromJsonReferenceKind(jsonField(object, "kindValue")),
                lifetime: fromJsonLifetime(jsonField(object, "lifetime")),
                element: fromJsonLocalNodeId(jsonField(object, "element")),
                space: fromJsonSpace(jsonField(object, "space")),
                access: fromJsonAccess(jsonField(object, "access")),
                nullability: fromJsonNullability(jsonField(object, "nullability")),
            };
        case "uninit":
            return {
                kind,
                value: fromJsonLocalNodeId(jsonField(object, "value")),
            };
        case "fixedArray":
            return {
                kind,
                element: fromJsonLocalNodeId(jsonField(object, "element")),
                length: jsonBigint(jsonField(object, "length")),
                copy: fromJsonCopy(jsonField(object, "copy")),
            };
        case "tuple":
            return {
                kind,
                elements: jsonArray(jsonField(object, "elements")).map((item0) => fromJsonLocalNodeId(item0)),
                copy: fromJsonCopy(jsonField(object, "copy")),
            };
        case "struct":
            return {
                kind,
                fields: jsonArray(jsonField(object, "fields")).map((item0) => fromJsonLocalNodeId(item0)),
                copy: fromJsonCopy(jsonField(object, "copy")),
            };
        case "newtype":
            return {
                kind,
                inner: fromJsonLocalNodeId(jsonField(object, "inner")),
                copy: fromJsonCopy(jsonField(object, "copy")),
            };
        case "variant":
            return {
                kind,
                tag: fromJsonLocalNodeId(jsonField(object, "tag")),
                storage: fromJsonLocalNodeId(jsonField(object, "storage")),
                cases: jsonArray(jsonField(object, "cases")).map((item0) => fromJsonVariantCase(item0)),
                copy: fromJsonCopy(jsonField(object, "copy")),
            };
        case "vector":
            return {
                kind,
                element: fromJsonLocalNodeId(jsonField(object, "element")),
                lanes: jsonInteger(jsonField(object, "lanes")),
                copy: fromJsonCopy(jsonField(object, "copy")),
            };
        case "tensor":
            return {
                kind,
                element: fromJsonLocalNodeId(jsonField(object, "element")),
                shape: jsonArray(jsonField(object, "shape")).map((item0) => fromJsonTensorDimension(item0)),
                format: fromJsonTensorFormat(jsonField(object, "format")),
                sharding: fromJsonTensorSharding(jsonField(object, "sharding")),
                copy: fromJsonCopy(jsonField(object, "copy")),
            };
        case "tensorView":
            return {
                kind,
                kindValue: fromJsonReferenceKind(jsonField(object, "kindValue")),
                lifetime: fromJsonLifetime(jsonField(object, "lifetime")),
                space: fromJsonSpace(jsonField(object, "space")),
                access: fromJsonAccess(jsonField(object, "access")),
                element: fromJsonLocalNodeId(jsonField(object, "element")),
                shape: jsonArray(jsonField(object, "shape")).map((item0) => fromJsonTensorDimension(item0)),
                format: fromJsonTensorViewFormat(jsonField(object, "format")),
                sharding: fromJsonTensorSharding(jsonField(object, "sharding")),
                nullability: fromJsonNullability(jsonField(object, "nullability")),
            };
        case "functionSignature":
            return {
                kind,
                lifetimes: jsonArray(jsonField(object, "lifetimes")).map((item0) => fromJsonLifetimeParameter(item0)),
                parameters: jsonArray(jsonField(object, "parameters")).map((item0) => fromJsonSignatureParameter(item0)),
                result: fromJsonLocalNodeId(jsonField(object, "result")),
            };
        case "function":
            return {
                kind,
                signature: fromJsonLocalNodeId(jsonField(object, "signature")),
                environment: fromJsonLocalNodeId(jsonField(object, "environment")),
            };
        case "functionPointer":
            return {
                kind,
                signature: fromJsonLocalNodeId(jsonField(object, "signature")),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}
