/* generated by destack_bridge_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, compareBytes, jsonArray, jsonField, jsonObject, nestedBytes } from "../../../protocol/serde.js";
import type { CastOrigin } from "../tree/cast.js";
import type { GlobalNodeIdAny } from "../tree/node.js";
import type { GlobalTypeId } from "../type/type.js";
import type { ModuleId } from "../../source/file/model/module.js";
import { decodeCastOrigin, encodeCastOrigin, fromJsonCastOrigin, toJsonCastOrigin } from "../tree/cast.js";
import { decodeGlobalNodeIdAny, encodeGlobalNodeIdAny, fromJsonGlobalNodeIdAny, toJsonGlobalNodeIdAny } from "../tree/node.js";
import { decodeGlobalTypeId, encodeGlobalTypeId, fromJsonGlobalTypeId, toJsonGlobalTypeId } from "../type/type.js";
import { decodeModuleId, encodeModuleId, fromJsonModuleId, toJsonModuleId } from "../../source/file/model/module.js";

/** Coercions added by one DIR phase. */
export type CoercionSegment = {
    /** The module id of the coercion segment. */
    readonly moduleId: ModuleId;
    /** Coercions keyed by the value node being coerced. */
    readonly coercions: ReadonlyMap<GlobalNodeIdAny, Coercion>;
};

export const CoercionSegment = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: CoercionSegment): void {
        encodeCoercionSegment(writer, value);
    },

    /** Decode one CoercionSegment. */
    decode(reader: BinaryReader): CoercionSegment {
        return decodeCoercionSegment(reader);
    },

    /** Return this value as JSON. */
    toJson(value: CoercionSegment): Json {
        return toJsonCoercionSegment(value);
    },

    /** Return one CoercionSegment from one JSON value. */
    fromJson(value: Json): CoercionSegment {
        return fromJsonCoercionSegment(value);
    },
};

/** Encode one CoercionSegment. */
export function encodeCoercionSegment(writer: BinaryWriter, value: CoercionSegment): void {
    encodeModuleId(writer, value.moduleId);
    const entries1 = Array.from(value.coercions.entries()).map(([key1, item1]) => {
        const keyBytes = nestedBytes((writer) => {
            encodeGlobalNodeIdAny(writer, key1);
        });
        return { key1, item1, keyBytes };
    });
    entries1.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries1.length);
    for (const entry1 of entries1) {
        encodeGlobalNodeIdAny(writer, entry1.key1);
        encodeCoercion(writer, entry1.item1);
    }
}

/** Decode one CoercionSegment. */
export function decodeCoercionSegment(reader: BinaryReader): CoercionSegment {
    const moduleId = decodeModuleId(reader);
    const coercions = (() => { const length1 = reader.readNumber(); const items1 = new Map<GlobalNodeIdAny, Coercion>(); for (let index = 0; index < length1; index += 1) { items1.set(decodeGlobalNodeIdAny(reader), decodeCoercion(reader)); } return items1; })();

    return {
        moduleId,
        coercions,
    };
}

/** Return one JSON value for one CoercionSegment. */
export function toJsonCoercionSegment(value: CoercionSegment): Json {
    return {
        moduleId: toJsonModuleId(value.moduleId),
        coercions: Array.from(value.coercions.entries()).map(([key0, item0]) => [toJsonGlobalNodeIdAny(key0), toJsonCoercion(item0)] as const),
    };
}

/** Return one CoercionSegment from one JSON value. */
export function fromJsonCoercionSegment(value: Json): CoercionSegment {
    const object = jsonObject(value);

    return {
        moduleId: fromJsonModuleId(jsonField(object, "moduleId")),
        coercions: new Map(jsonArray(jsonField(object, "coercions")).map((entry) => { const items = jsonArray(entry); if (items.length !== 2) { throw new SerdeError(`expected JSON map entry length 2: ${items.length}`); } const key0 = items[0]; const item0 = items[1]; return [fromJsonGlobalNodeIdAny(key0), fromJsonCoercion(item0)] as const; })),
    };
}

/** One type coercion attached to a value node. */
export type Coercion = {
    /** The source type before coercion. */
    readonly source: GlobalTypeId;
    /** The target type after coercion. */
    readonly target: GlobalTypeId;
    /** How the coercion entered DIR. */
    readonly origin: CastOrigin;
};

export const Coercion = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: Coercion): void {
        encodeCoercion(writer, value);
    },

    /** Decode one Coercion. */
    decode(reader: BinaryReader): Coercion {
        return decodeCoercion(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Coercion): Json {
        return toJsonCoercion(value);
    },

    /** Return one Coercion from one JSON value. */
    fromJson(value: Json): Coercion {
        return fromJsonCoercion(value);
    },
};

/** Encode one Coercion. */
export function encodeCoercion(writer: BinaryWriter, value: Coercion): void {
    encodeGlobalTypeId(writer, value.source);
    encodeGlobalTypeId(writer, value.target);
    encodeCastOrigin(writer, value.origin);
}

/** Decode one Coercion. */
export function decodeCoercion(reader: BinaryReader): Coercion {
    const source = decodeGlobalTypeId(reader);
    const target = decodeGlobalTypeId(reader);
    const origin = decodeCastOrigin(reader);

    return {
        source,
        target,
        origin,
    };
}

/** Return one JSON value for one Coercion. */
export function toJsonCoercion(value: Coercion): Json {
    return {
        source: toJsonGlobalTypeId(value.source),
        target: toJsonGlobalTypeId(value.target),
        origin: toJsonCastOrigin(value.origin),
    };
}

/** Return one Coercion from one JSON value. */
export function fromJsonCoercion(value: Json): Coercion {
    const object = jsonObject(value);

    return {
        source: fromJsonGlobalTypeId(jsonField(object, "source")),
        target: fromJsonGlobalTypeId(jsonField(object, "target")),
        origin: fromJsonCastOrigin(jsonField(object, "origin")),
    };
}
