/* generated by destack_bridge_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, compareBytes, jsonArray, jsonField, jsonObject, nestedBytes } from "../../../protocol/serde.js";
import type { GlobalEntry } from "../symbol/global.js";
import type { StaticKey } from "../symbol/key.js";
import type { ModuleId } from "../../source/file/model/module.js";
import { decodeGlobalEntry, encodeGlobalEntry, fromJsonGlobalEntry, toJsonGlobalEntry } from "../symbol/global.js";
import { decodeStaticKey, encodeStaticKey, fromJsonStaticKey, toJsonStaticKey } from "../symbol/key.js";
import { decodeModuleId, encodeModuleId, fromJsonModuleId, toJsonModuleId } from "../../source/file/model/module.js";

/** Global names contributed by one module. */
export type GlobalTable = {
    /** The module id of the global table. */
    readonly moduleId: ModuleId;
    /** Global entries keyed by visible global name. */
    readonly entriesByKey: ReadonlyMap<StaticKey, ReadonlyArray<GlobalEntry>>;
};

export const GlobalTable = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: GlobalTable): void {
        encodeGlobalTable(writer, value);
    },

    /** Decode one GlobalTable. */
    decode(reader: BinaryReader): GlobalTable {
        return decodeGlobalTable(reader);
    },

    /** Return this value as JSON. */
    toJson(value: GlobalTable): Json {
        return toJsonGlobalTable(value);
    },

    /** Return one GlobalTable from one JSON value. */
    fromJson(value: Json): GlobalTable {
        return fromJsonGlobalTable(value);
    },
};

/** Encode one GlobalTable. */
export function encodeGlobalTable(writer: BinaryWriter, value: GlobalTable): void {
    encodeModuleId(writer, value.moduleId);
    const entries1 = Array.from(value.entriesByKey.entries()).map(([key1, item1]) => {
        const keyBytes = nestedBytes((writer) => {
            encodeStaticKey(writer, key1);
        });
        return { key1, item1, keyBytes };
    });
    entries1.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries1.length);
    for (const entry1 of entries1) {
        encodeStaticKey(writer, entry1.key1);
        writer.writeUnsigned(entry1.item1.length);
        for (const item2 of entry1.item1) {
            encodeGlobalEntry(writer, item2);
        }
    }
}

/** Decode one GlobalTable. */
export function decodeGlobalTable(reader: BinaryReader): GlobalTable {
    const moduleId = decodeModuleId(reader);
    const entriesByKey = (() => { const length1 = reader.readNumber(); const items1 = new Map<StaticKey, ReadonlyArray<GlobalEntry>>(); for (let index = 0; index < length1; index += 1) { items1.set(decodeStaticKey(reader), (() => { const length3 = reader.readNumber(); const items3: Array<GlobalEntry> = []; for (let index = 0; index < length3; index += 1) { items3.push(decodeGlobalEntry(reader)); } return items3; })()); } return items1; })();

    return {
        moduleId,
        entriesByKey,
    };
}

/** Return one JSON value for one GlobalTable. */
export function toJsonGlobalTable(value: GlobalTable): Json {
    return {
        moduleId: toJsonModuleId(value.moduleId),
        entriesByKey: Array.from(value.entriesByKey.entries()).map(([key0, item0]) => [toJsonStaticKey(key0), item0.map((item1) => toJsonGlobalEntry(item1))] as const),
    };
}

/** Return one GlobalTable from one JSON value. */
export function fromJsonGlobalTable(value: Json): GlobalTable {
    const object = jsonObject(value);

    return {
        moduleId: fromJsonModuleId(jsonField(object, "moduleId")),
        entriesByKey: new Map(jsonArray(jsonField(object, "entriesByKey")).map((entry) => { const items = jsonArray(entry); if (items.length !== 2) { throw new SerdeError(`expected JSON map entry length 2: ${items.length}`); } const key0 = items[0]; const item0 = items[1]; return [fromJsonStaticKey(key0), jsonArray(item0).map((item1) => fromJsonGlobalEntry(item1))] as const; })),
    };
}
