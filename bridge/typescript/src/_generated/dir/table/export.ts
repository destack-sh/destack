/* generated by destack_bridge_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, compareBytes, jsonArray, jsonField, jsonObject, nestedBytes } from "../../../protocol/serde.js";
import type { ExportEntry } from "../symbol/export.js";
import type { ExportKey } from "../symbol/export.js";
import type { StarExportEntry } from "../symbol/export.js";
import type { ModuleId } from "../../source/file/model/module.js";
import { decodeExportEntry, encodeExportEntry, fromJsonExportEntry, toJsonExportEntry } from "../symbol/export.js";
import { decodeExportKey, encodeExportKey, fromJsonExportKey, toJsonExportKey } from "../symbol/export.js";
import { decodeStarExportEntry, encodeStarExportEntry, fromJsonStarExportEntry, toJsonStarExportEntry } from "../symbol/export.js";
import { decodeModuleId, encodeModuleId, fromJsonModuleId, toJsonModuleId } from "../../source/file/model/module.js";

/** Resolved module exports for one module. */
export type ExportTable = {
    /** The module id of the export table. */
    readonly moduleId: ModuleId;
    /** Local and indirect exports keyed by exported name. */
    readonly exportByKey: ReadonlyMap<ExportKey, ExportEntry>;
    /** Star exports declared by the module. */
    readonly starExports: ReadonlyArray<StarExportEntry>;
};

export const ExportTable = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ExportTable): void {
        encodeExportTable(writer, value);
    },

    /** Decode one ExportTable. */
    decode(reader: BinaryReader): ExportTable {
        return decodeExportTable(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ExportTable): Json {
        return toJsonExportTable(value);
    },

    /** Return one ExportTable from one JSON value. */
    fromJson(value: Json): ExportTable {
        return fromJsonExportTable(value);
    },
};

/** Encode one ExportTable. */
export function encodeExportTable(writer: BinaryWriter, value: ExportTable): void {
    encodeModuleId(writer, value.moduleId);
    const entries1 = Array.from(value.exportByKey.entries()).map(([key1, item1]) => {
        const keyBytes = nestedBytes((writer) => {
            encodeExportKey(writer, key1);
        });
        return { key1, item1, keyBytes };
    });
    entries1.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries1.length);
    for (const entry1 of entries1) {
        encodeExportKey(writer, entry1.key1);
        encodeExportEntry(writer, entry1.item1);
    }
    writer.writeUnsigned(value.starExports.length);
    for (const item2 of value.starExports) {
        encodeStarExportEntry(writer, item2);
    }
}

/** Decode one ExportTable. */
export function decodeExportTable(reader: BinaryReader): ExportTable {
    const moduleId = decodeModuleId(reader);
    const exportByKey = (() => { const length1 = reader.readNumber(); const items1 = new Map<ExportKey, ExportEntry>(); for (let index = 0; index < length1; index += 1) { items1.set(decodeExportKey(reader), decodeExportEntry(reader)); } return items1; })();
    const starExports = (() => { const length2 = reader.readNumber(); const items2: Array<StarExportEntry> = []; for (let index = 0; index < length2; index += 1) { items2.push(decodeStarExportEntry(reader)); } return items2; })();

    return {
        moduleId,
        exportByKey,
        starExports,
    };
}

/** Return one JSON value for one ExportTable. */
export function toJsonExportTable(value: ExportTable): Json {
    return {
        moduleId: toJsonModuleId(value.moduleId),
        exportByKey: Array.from(value.exportByKey.entries()).map(([key0, item0]) => [toJsonExportKey(key0), toJsonExportEntry(item0)] as const),
        starExports: value.starExports.map((item0) => toJsonStarExportEntry(item0)),
    };
}

/** Return one ExportTable from one JSON value. */
export function fromJsonExportTable(value: Json): ExportTable {
    const object = jsonObject(value);

    return {
        moduleId: fromJsonModuleId(jsonField(object, "moduleId")),
        exportByKey: new Map(jsonArray(jsonField(object, "exportByKey")).map((entry) => { const items = jsonArray(entry); if (items.length !== 2) { throw new SerdeError(`expected JSON map entry length 2: ${items.length}`); } const key0 = items[0]; const item0 = items[1]; return [fromJsonExportKey(key0), fromJsonExportEntry(item0)] as const; })),
        starExports: jsonArray(jsonField(object, "starExports")).map((item0) => fromJsonStarExportEntry(item0)),
    };
}
