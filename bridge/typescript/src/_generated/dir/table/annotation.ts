/* generated by destack_bridge_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, compareBytes, jsonArray, jsonField, jsonInteger, jsonObject, jsonOptional, jsonString, nestedBytes } from "../../../protocol/serde.js";
import type { LanguageItem } from "../symbol/language.js";
import type { GlobalSymbolId } from "../symbol/symbol.js";
import type { GlobalNodeIdAny } from "../tree/node.js";
import type { GlobalStaticId } from "../tree/static.js";
import type { ModuleId } from "../../source/file/model/module.js";
import { decodeLanguageItem, encodeLanguageItem, fromJsonLanguageItem, toJsonLanguageItem } from "../symbol/language.js";
import { decodeGlobalSymbolId, encodeGlobalSymbolId, fromJsonGlobalSymbolId, toJsonGlobalSymbolId } from "../symbol/symbol.js";
import { decodeGlobalNodeIdAny, encodeGlobalNodeIdAny, fromJsonGlobalNodeIdAny, toJsonGlobalNodeIdAny } from "../tree/node.js";
import { decodeGlobalStaticId, encodeGlobalStaticId, fromJsonGlobalStaticId, toJsonGlobalStaticId } from "../tree/static.js";
import { decodeModuleId, encodeModuleId, fromJsonModuleId, toJsonModuleId } from "../../source/file/model/module.js";

/** Annotation invocations added by one DIR phase. */
export type AnnotationSegment = {
    /** The module id of the annotation segment. */
    readonly moduleId: ModuleId;
    /** The first annotation invocation id owned by this table segment. */
    readonly firstInvocationId: number;
    /** Annotation invocations owned by this segment. */
    readonly invocations: ReadonlyArray<AnnotationInvocation>;
    /** Annotation invocations attached to each owner. */
    readonly invocationsByOwner: ReadonlyMap<GlobalNodeIdAny, ReadonlyArray<LocalAnnotationId>>;
};

export const AnnotationSegment = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: AnnotationSegment): void {
        encodeAnnotationSegment(writer, value);
    },

    /** Decode one AnnotationSegment. */
    decode(reader: BinaryReader): AnnotationSegment {
        return decodeAnnotationSegment(reader);
    },

    /** Return this value as JSON. */
    toJson(value: AnnotationSegment): Json {
        return toJsonAnnotationSegment(value);
    },

    /** Return one AnnotationSegment from one JSON value. */
    fromJson(value: Json): AnnotationSegment {
        return fromJsonAnnotationSegment(value);
    },
};

/** Encode one AnnotationSegment. */
export function encodeAnnotationSegment(writer: BinaryWriter, value: AnnotationSegment): void {
    encodeModuleId(writer, value.moduleId);
    writer.writeUnsigned(value.firstInvocationId);
    writer.writeUnsigned(value.invocations.length);
    for (const item2 of value.invocations) {
        encodeAnnotationInvocation(writer, item2);
    }
    const entries3 = Array.from(value.invocationsByOwner.entries()).map(([key3, item3]) => {
        const keyBytes = nestedBytes((writer) => {
            encodeGlobalNodeIdAny(writer, key3);
        });
        return { key3, item3, keyBytes };
    });
    entries3.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries3.length);
    for (const entry3 of entries3) {
        encodeGlobalNodeIdAny(writer, entry3.key3);
        writer.writeUnsigned(entry3.item3.length);
        for (const item4 of entry3.item3) {
            encodeLocalAnnotationId(writer, item4);
        }
    }
}

/** Decode one AnnotationSegment. */
export function decodeAnnotationSegment(reader: BinaryReader): AnnotationSegment {
    const moduleId = decodeModuleId(reader);
    const firstInvocationId = reader.readNumber();
    const invocations = (() => { const length2 = reader.readNumber(); const items2: Array<AnnotationInvocation> = []; for (let index = 0; index < length2; index += 1) { items2.push(decodeAnnotationInvocation(reader)); } return items2; })();
    const invocationsByOwner = (() => { const length3 = reader.readNumber(); const items3 = new Map<GlobalNodeIdAny, ReadonlyArray<LocalAnnotationId>>(); for (let index = 0; index < length3; index += 1) { items3.set(decodeGlobalNodeIdAny(reader), (() => { const length5 = reader.readNumber(); const items5: Array<LocalAnnotationId> = []; for (let index = 0; index < length5; index += 1) { items5.push(decodeLocalAnnotationId(reader)); } return items5; })()); } return items3; })();

    return {
        moduleId,
        firstInvocationId,
        invocations,
        invocationsByOwner,
    };
}

/** Return one JSON value for one AnnotationSegment. */
export function toJsonAnnotationSegment(value: AnnotationSegment): Json {
    return {
        moduleId: toJsonModuleId(value.moduleId),
        firstInvocationId: value.firstInvocationId,
        invocations: value.invocations.map((item0) => toJsonAnnotationInvocation(item0)),
        invocationsByOwner: Array.from(value.invocationsByOwner.entries()).map(([key0, item0]) => [toJsonGlobalNodeIdAny(key0), item0.map((item1) => toJsonLocalAnnotationId(item1))] as const),
    };
}

/** Return one AnnotationSegment from one JSON value. */
export function fromJsonAnnotationSegment(value: Json): AnnotationSegment {
    const object = jsonObject(value);

    return {
        moduleId: fromJsonModuleId(jsonField(object, "moduleId")),
        firstInvocationId: jsonInteger(jsonField(object, "firstInvocationId")),
        invocations: jsonArray(jsonField(object, "invocations")).map((item0) => fromJsonAnnotationInvocation(item0)),
        invocationsByOwner: new Map(jsonArray(jsonField(object, "invocationsByOwner")).map((entry) => { const items = jsonArray(entry); if (items.length !== 2) { throw new SerdeError(`expected JSON map entry length 2: ${items.length}`); } const key0 = items[0]; const item0 = items[1]; return [fromJsonGlobalNodeIdAny(key0), jsonArray(item0).map((item1) => fromJsonLocalAnnotationId(item1))] as const; })),
    };
}

/** Checked annotation invocation attached to one owner node. */
export type AnnotationInvocation = {
    /** The decorator node. */
    readonly source: GlobalNodeIdAny;
    /** The node annotated by this invocation. */
    readonly owner: GlobalNodeIdAny;
    /** The decorator target expression. */
    readonly target: GlobalNodeIdAny;
    /** The resolved annotation target. */
    readonly resolution: AnnotationTarget;
    /** The invocation arguments. */
    readonly arguments: ReadonlyArray<AnnotationArgument>;
};

export const AnnotationInvocation = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: AnnotationInvocation): void {
        encodeAnnotationInvocation(writer, value);
    },

    /** Decode one AnnotationInvocation. */
    decode(reader: BinaryReader): AnnotationInvocation {
        return decodeAnnotationInvocation(reader);
    },

    /** Return this value as JSON. */
    toJson(value: AnnotationInvocation): Json {
        return toJsonAnnotationInvocation(value);
    },

    /** Return one AnnotationInvocation from one JSON value. */
    fromJson(value: Json): AnnotationInvocation {
        return fromJsonAnnotationInvocation(value);
    },
};

/** Encode one AnnotationInvocation. */
export function encodeAnnotationInvocation(writer: BinaryWriter, value: AnnotationInvocation): void {
    encodeGlobalNodeIdAny(writer, value.source);
    encodeGlobalNodeIdAny(writer, value.owner);
    encodeGlobalNodeIdAny(writer, value.target);
    encodeAnnotationTarget(writer, value.resolution);
    writer.writeUnsigned(value.arguments.length);
    for (const item4 of value.arguments) {
        encodeAnnotationArgument(writer, item4);
    }
}

/** Decode one AnnotationInvocation. */
export function decodeAnnotationInvocation(reader: BinaryReader): AnnotationInvocation {
    const source = decodeGlobalNodeIdAny(reader);
    const owner = decodeGlobalNodeIdAny(reader);
    const target = decodeGlobalNodeIdAny(reader);
    const resolution = decodeAnnotationTarget(reader);
    const arguments_ = (() => { const length4 = reader.readNumber(); const items4: Array<AnnotationArgument> = []; for (let index = 0; index < length4; index += 1) { items4.push(decodeAnnotationArgument(reader)); } return items4; })();

    return {
        source,
        owner,
        target,
        resolution,
        arguments: arguments_,
    };
}

/** Return one JSON value for one AnnotationInvocation. */
export function toJsonAnnotationInvocation(value: AnnotationInvocation): Json {
    return {
        source: toJsonGlobalNodeIdAny(value.source),
        owner: toJsonGlobalNodeIdAny(value.owner),
        target: toJsonGlobalNodeIdAny(value.target),
        resolution: toJsonAnnotationTarget(value.resolution),
        arguments: value.arguments.map((item0) => toJsonAnnotationArgument(item0)),
    };
}

/** Return one AnnotationInvocation from one JSON value. */
export function fromJsonAnnotationInvocation(value: Json): AnnotationInvocation {
    const object = jsonObject(value);

    return {
        source: fromJsonGlobalNodeIdAny(jsonField(object, "source")),
        owner: fromJsonGlobalNodeIdAny(jsonField(object, "owner")),
        target: fromJsonGlobalNodeIdAny(jsonField(object, "target")),
        resolution: fromJsonAnnotationTarget(jsonField(object, "resolution")),
        arguments: jsonArray(jsonField(object, "arguments")).map((item0) => fromJsonAnnotationArgument(item0)),
    };
}

/** Resolved annotation target. */
export type AnnotationTarget =
    /** Compiler language item annotation. */
    | {
          readonly kind: "languageItem";
          readonly language_item: LanguageItem;
      }
    /** User-defined annotation symbol. */
    | {
          readonly kind: "symbol";
          readonly symbol: GlobalSymbolId;
      }
    /** Unresolved or non-symbol annotation target. */
    | {
          readonly kind: "unknown";
      }
;

export const AnnotationTarget = {
    /** Compiler language item annotation. */
    languageItem(language_item: LanguageItem): AnnotationTarget {
        return { kind: "languageItem", language_item };
    },

    /** User-defined annotation symbol. */
    "symbol"(symbol_: GlobalSymbolId): AnnotationTarget {
        return { kind: "symbol", symbol: symbol_ };
    },

    /** Unresolved or non-symbol annotation target. */
    "unknown"(): AnnotationTarget {
        return { kind: "unknown" };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: AnnotationTarget): void {
        encodeAnnotationTarget(writer, value);
    },

    /** Decode one AnnotationTarget. */
    decode(reader: BinaryReader): AnnotationTarget {
        return decodeAnnotationTarget(reader);
    },

    /** Return this value as JSON. */
    toJson(value: AnnotationTarget): Json {
        return toJsonAnnotationTarget(value);
    },

    /** Return one AnnotationTarget from one JSON value. */
    fromJson(value: Json): AnnotationTarget {
        return fromJsonAnnotationTarget(value);
    },
};

/** Encode one AnnotationTarget. */
export function encodeAnnotationTarget(writer: BinaryWriter, value: AnnotationTarget): void {
    switch (value.kind) {
        case "languageItem":
            writer.writeUnsigned(0);
            encodeLanguageItem(writer, value.language_item);
            return;
        case "symbol":
            writer.writeUnsigned(1);
            encodeGlobalSymbolId(writer, value.symbol);
            return;
        case "unknown":
            writer.writeUnsigned(2);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one AnnotationTarget. */
export function decodeAnnotationTarget(reader: BinaryReader): AnnotationTarget {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const language_item = decodeLanguageItem(reader);

            return { kind: "languageItem", language_item };
        }
        case 1: {
            const symbol_ = decodeGlobalSymbolId(reader);

            return { kind: "symbol", symbol: symbol_ };
        }
        case 2: {
            return { kind: "unknown" };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one AnnotationTarget. */
export function toJsonAnnotationTarget(value: AnnotationTarget): Json {
    switch (value.kind) {
        case "languageItem":
            return {
                kind: "languageItem",
                language_item: toJsonLanguageItem(value.language_item),
            };
        case "symbol":
            return {
                kind: "symbol",
                symbol: toJsonGlobalSymbolId(value.symbol),
            };
        case "unknown":
            return {
                kind: "unknown",
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one AnnotationTarget from one JSON value. */
export function fromJsonAnnotationTarget(value: Json): AnnotationTarget {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "languageItem":
            return {
                kind,
                language_item: fromJsonLanguageItem(jsonField(object, "language_item")),
            };
        case "symbol":
            return {
                kind,
                symbol: fromJsonGlobalSymbolId(jsonField(object, "symbol")),
            };
        case "unknown":
            return {
                kind,
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** Checked annotation argument. */
export type AnnotationArgument = {
    /** The argument node. */
    readonly source: GlobalNodeIdAny;
    /** The committed static value when one exists. */
    readonly value?: GlobalStaticId;
};

export const AnnotationArgument = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: AnnotationArgument): void {
        encodeAnnotationArgument(writer, value);
    },

    /** Decode one AnnotationArgument. */
    decode(reader: BinaryReader): AnnotationArgument {
        return decodeAnnotationArgument(reader);
    },

    /** Return this value as JSON. */
    toJson(value: AnnotationArgument): Json {
        return toJsonAnnotationArgument(value);
    },

    /** Return one AnnotationArgument from one JSON value. */
    fromJson(value: Json): AnnotationArgument {
        return fromJsonAnnotationArgument(value);
    },
};

/** Encode one AnnotationArgument. */
export function encodeAnnotationArgument(writer: BinaryWriter, value: AnnotationArgument): void {
    encodeGlobalNodeIdAny(writer, value.source);
    writer.writeOption(value.value, (value1) => {
        encodeGlobalStaticId(writer, value1);
    });
}

/** Decode one AnnotationArgument. */
export function decodeAnnotationArgument(reader: BinaryReader): AnnotationArgument {
    const source = decodeGlobalNodeIdAny(reader);
    const value = reader.readOption(() => decodeGlobalStaticId(reader));

    return {
        source,
        ...(value === undefined ? {} : { value }),
    };
}

/** Return one JSON value for one AnnotationArgument. */
export function toJsonAnnotationArgument(value: AnnotationArgument): Json {
    return {
        source: toJsonGlobalNodeIdAny(value.source),
        ...(value.value === undefined ? {} : { value: toJsonGlobalStaticId(value.value) }),
    };
}

/** Return one AnnotationArgument from one JSON value. */
export function fromJsonAnnotationArgument(value: Json): AnnotationArgument {
    const object = jsonObject(value);

    return {
        source: fromJsonGlobalNodeIdAny(jsonField(object, "source")),
        value: jsonOptional(object, "value", (value) => fromJsonGlobalStaticId(value)),
    };
}

/** Unique identifier for an annotation invocation. */
export type LocalAnnotationId = number;

export const LocalAnnotationId = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: LocalAnnotationId): void {
        encodeLocalAnnotationId(writer, value);
    },

    /** Decode one LocalAnnotationId. */
    decode(reader: BinaryReader): LocalAnnotationId {
        return decodeLocalAnnotationId(reader);
    },

    /** Return this value as JSON. */
    toJson(value: LocalAnnotationId): Json {
        return toJsonLocalAnnotationId(value);
    },

    /** Return one LocalAnnotationId from one JSON value. */
    fromJson(value: Json): LocalAnnotationId {
        return fromJsonLocalAnnotationId(value);
    },
};

/** Encode one LocalAnnotationId. */
export function encodeLocalAnnotationId(writer: BinaryWriter, value: LocalAnnotationId): void {
    writer.writeUnsigned(value);
}

/** Decode one LocalAnnotationId. */
export function decodeLocalAnnotationId(reader: BinaryReader): LocalAnnotationId {
    return reader.readNumber();
}

/** Return one JSON value for one LocalAnnotationId. */
export function toJsonLocalAnnotationId(value: LocalAnnotationId): Json {
    return value;
}

/** Return one LocalAnnotationId from one JSON value. */
export function fromJsonLocalAnnotationId(value: Json): LocalAnnotationId {
    return jsonInteger(value);
}
