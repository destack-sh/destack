/* generated by destack_bridge_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, jsonArray, jsonField, jsonObject } from "../../../protocol/serde.js";
import type { ModuleEdge } from "../symbol/module.js";
import type { ModuleId } from "../../source/file/model/module.js";
import { decodeModuleEdge, encodeModuleEdge, fromJsonModuleEdge, toJsonModuleEdge } from "../symbol/module.js";
import { decodeModuleId, encodeModuleId, fromJsonModuleId, toJsonModuleId } from "../../source/file/model/module.js";

/** Module import edges added by one DIR phase. */
export type ModuleSegment = {
    /** The module id of the segment. */
    readonly moduleId: ModuleId;
    /** Locally resolved module import edges. */
    readonly edges: ReadonlyArray<ModuleEdge>;
};

export const ModuleSegment = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ModuleSegment): void {
        encodeModuleSegment(writer, value);
    },

    /** Decode one ModuleSegment. */
    decode(reader: BinaryReader): ModuleSegment {
        return decodeModuleSegment(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ModuleSegment): Json {
        return toJsonModuleSegment(value);
    },

    /** Return one ModuleSegment from one JSON value. */
    fromJson(value: Json): ModuleSegment {
        return fromJsonModuleSegment(value);
    },
};

/** Encode one ModuleSegment. */
export function encodeModuleSegment(writer: BinaryWriter, value: ModuleSegment): void {
    encodeModuleId(writer, value.moduleId);
    writer.writeUnsigned(value.edges.length);
    for (const item1 of value.edges) {
        encodeModuleEdge(writer, item1);
    }
}

/** Decode one ModuleSegment. */
export function decodeModuleSegment(reader: BinaryReader): ModuleSegment {
    const moduleId = decodeModuleId(reader);
    const edges = (() => { const length1 = reader.readNumber(); const items1: Array<ModuleEdge> = []; for (let index = 0; index < length1; index += 1) { items1.push(decodeModuleEdge(reader)); } return items1; })();

    return {
        moduleId,
        edges,
    };
}

/** Return one JSON value for one ModuleSegment. */
export function toJsonModuleSegment(value: ModuleSegment): Json {
    return {
        moduleId: toJsonModuleId(value.moduleId),
        edges: value.edges.map((item0) => toJsonModuleEdge(item0)),
    };
}

/** Return one ModuleSegment from one JSON value. */
export function fromJsonModuleSegment(value: Json): ModuleSegment {
    const object = jsonObject(value);

    return {
        moduleId: fromJsonModuleId(jsonField(object, "moduleId")),
        edges: jsonArray(jsonField(object, "edges")).map((item0) => fromJsonModuleEdge(item0)),
    };
}
