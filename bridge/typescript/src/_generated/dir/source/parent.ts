/* generated by destack_bridge_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, jsonArray, jsonField, jsonInteger, jsonObject } from "../../../protocol/serde.js";

/** The NodeParentIndex maps every node of one tree to its parent. */
export type NodeParentIndex = {
    /** First global node id this index covers; slots are keyed relative to it. */
    readonly base: number;
    readonly parentIdByNodeId: ReadonlyArray<number>;
};

export const NodeParentIndex = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: NodeParentIndex): void {
        encodeNodeParentIndex(writer, value);
    },

    /** Decode one NodeParentIndex. */
    decode(reader: BinaryReader): NodeParentIndex {
        return decodeNodeParentIndex(reader);
    },

    /** Return this value as JSON. */
    toJson(value: NodeParentIndex): Json {
        return toJsonNodeParentIndex(value);
    },

    /** Return one NodeParentIndex from one JSON value. */
    fromJson(value: Json): NodeParentIndex {
        return fromJsonNodeParentIndex(value);
    },
};

/** Encode one NodeParentIndex. */
export function encodeNodeParentIndex(writer: BinaryWriter, value: NodeParentIndex): void {
    writer.writeUnsigned(value.base);
    writer.writeUnsigned(value.parentIdByNodeId.length);
    for (const item1 of value.parentIdByNodeId) {
        writer.writeUnsigned(item1);
    }
}

/** Decode one NodeParentIndex. */
export function decodeNodeParentIndex(reader: BinaryReader): NodeParentIndex {
    const base = reader.readNumber();
    const parentIdByNodeId = (() => { const length1 = reader.readNumber(); const items1: Array<number> = []; for (let index = 0; index < length1; index += 1) { items1.push(reader.readNumber()); } return items1; })();

    return {
        base,
        parentIdByNodeId,
    };
}

/** Return one JSON value for one NodeParentIndex. */
export function toJsonNodeParentIndex(value: NodeParentIndex): Json {
    return {
        base: value.base,
        parentIdByNodeId: value.parentIdByNodeId.map((item0) => item0),
    };
}

/** Return one NodeParentIndex from one JSON value. */
export function fromJsonNodeParentIndex(value: Json): NodeParentIndex {
    const object = jsonObject(value);

    return {
        base: jsonInteger(jsonField(object, "base")),
        parentIdByNodeId: jsonArray(jsonField(object, "parentIdByNodeId")).map((item0) => jsonInteger(item0)),
    };
}
