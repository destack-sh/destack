/* generated by destack_bridge_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, jsonField, jsonInteger, jsonObject, jsonString } from "../../../protocol/serde.js";
import type { StringId } from "../../core/string.js";
import type { Span } from "../../source/file/model/span.js";
import { decodeStringId, encodeStringId, fromJsonStringId, toJsonStringId } from "../../core/string.js";
import { decodeSpan, encodeSpan, fromJsonSpan, toJsonSpan } from "../../source/file/model/span.js";

import { CommentNewlinesImpl } from "../../../_impl/dir/source/comment.js";

/** A raw source comment attached through the source side table. */
export type Comment = {
    /** The span of the raw comment, including delimiters. */
    readonly span: Span;
    /** The token boundary this leading comment is attached to. */
    readonly attachedTo: number;
    /** The kind of the comment. */
    readonly kind: CommentKind;
    /** The token-relative comment position. */
    readonly position: CommentPosition;
    /** The newline shape around the comment. */
    readonly newlines: CommentNewlines;
    /** The structured comment content classification. */
    readonly content: CommentContent;
};

export const Comment = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: Comment): void {
        encodeComment(writer, value);
    },

    /** Decode one Comment. */
    decode(reader: BinaryReader): Comment {
        return decodeComment(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Comment): Json {
        return toJsonComment(value);
    },

    /** Return one Comment from one JSON value. */
    fromJson(value: Json): Comment {
        return fromJsonComment(value);
    },
};

/** Encode one Comment. */
export function encodeComment(writer: BinaryWriter, value: Comment): void {
    encodeSpan(writer, value.span);
    writer.writeUnsigned(value.attachedTo);
    encodeCommentKind(writer, value.kind);
    encodeCommentPosition(writer, value.position);
    encodeCommentNewlines(writer, value.newlines);
    encodeCommentContent(writer, value.content);
}

/** Decode one Comment. */
export function decodeComment(reader: BinaryReader): Comment {
    const span = decodeSpan(reader);
    const attachedTo = reader.readNumber();
    const kind = decodeCommentKind(reader);
    const position = decodeCommentPosition(reader);
    const newlines = decodeCommentNewlines(reader);
    const content = decodeCommentContent(reader);

    return {
        span,
        attachedTo,
        kind,
        position,
        newlines,
        content,
    };
}

/** Return one JSON value for one Comment. */
export function toJsonComment(value: Comment): Json {
    return {
        span: toJsonSpan(value.span),
        attachedTo: value.attachedTo,
        kind: toJsonCommentKind(value.kind),
        position: toJsonCommentPosition(value.position),
        newlines: toJsonCommentNewlines(value.newlines),
        content: toJsonCommentContent(value.content),
    };
}

/** Return one Comment from one JSON value. */
export function fromJsonComment(value: Json): Comment {
    const object = jsonObject(value);

    return {
        span: fromJsonSpan(jsonField(object, "span")),
        attachedTo: jsonInteger(jsonField(object, "attachedTo")),
        kind: fromJsonCommentKind(jsonField(object, "kind")),
        position: fromJsonCommentPosition(jsonField(object, "position")),
        newlines: fromJsonCommentNewlines(jsonField(object, "newlines")),
        content: fromJsonCommentContent(jsonField(object, "content")),
    };
}

/** Indicates a line or block comment. */
export type CommentKind = "line" | "singleLineBlock" | "multiLineBlock";

export const CommentKind = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: CommentKind): void {
        encodeCommentKind(writer, value);
    },

    /** Decode one CommentKind. */
    decode(reader: BinaryReader): CommentKind {
        return decodeCommentKind(reader);
    },

    /** Return this value as JSON. */
    toJson(value: CommentKind): Json {
        return toJsonCommentKind(value);
    },

    /** Return one CommentKind from one JSON value. */
    fromJson(value: Json): CommentKind {
        return fromJsonCommentKind(value);
    },
};

/** Encode one CommentKind. */
export function encodeCommentKind(writer: BinaryWriter, value: CommentKind): void {
    switch (value) {
        case "line":
            writer.writeUnsigned(0);
            return;
        case "singleLineBlock":
            writer.writeUnsigned(1);
            return;
        case "multiLineBlock":
            writer.writeUnsigned(2);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one CommentKind. */
export function decodeCommentKind(reader: BinaryReader): CommentKind {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "line";
        case 1:
            return "singleLineBlock";
        case 2:
            return "multiLineBlock";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one CommentKind. */
export function toJsonCommentKind(value: CommentKind): Json {
    return value;
}

/** Return one CommentKind from one JSON value. */
export function fromJsonCommentKind(value: Json): CommentKind {
    const variant = jsonString(value);

    switch (variant) {
        case "line":
            return "line";
        case "singleLineBlock":
            return "singleLineBlock";
        case "multiLineBlock":
            return "multiLineBlock";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** A comment's position relative to a token boundary. */
export type CommentPosition = "leading" | "trailing";

export const CommentPosition = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: CommentPosition): void {
        encodeCommentPosition(writer, value);
    },

    /** Decode one CommentPosition. */
    decode(reader: BinaryReader): CommentPosition {
        return decodeCommentPosition(reader);
    },

    /** Return this value as JSON. */
    toJson(value: CommentPosition): Json {
        return toJsonCommentPosition(value);
    },

    /** Return one CommentPosition from one JSON value. */
    fromJson(value: Json): CommentPosition {
        return fromJsonCommentPosition(value);
    },
};

/** Encode one CommentPosition. */
export function encodeCommentPosition(writer: BinaryWriter, value: CommentPosition): void {
    switch (value) {
        case "leading":
            writer.writeUnsigned(0);
            return;
        case "trailing":
            writer.writeUnsigned(1);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one CommentPosition. */
export function decodeCommentPosition(reader: BinaryReader): CommentPosition {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "leading";
        case 1:
            return "trailing";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one CommentPosition. */
export function toJsonCommentPosition(value: CommentPosition): Json {
    return value;
}

/** Return one CommentPosition from one JSON value. */
export function fromJsonCommentPosition(value: Json): CommentPosition {
    const variant = jsonString(value);

    switch (variant) {
        case "leading":
            return "leading";
        case "trailing":
            return "trailing";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** Newline shape flags captured around one raw comment. */
export type CommentNewlines = {
    /** Bit flags that describe newline boundaries. */
    readonly bits: number;
};

export const CommentNewlines = {
    ...CommentNewlinesImpl,

    /** Encode this value. */
    encode(writer: BinaryWriter, value: CommentNewlines): void {
        encodeCommentNewlines(writer, value);
    },

    /** Decode one CommentNewlines. */
    decode(reader: BinaryReader): CommentNewlines {
        return decodeCommentNewlines(reader);
    },

    /** Return this value as JSON. */
    toJson(value: CommentNewlines): Json {
        return toJsonCommentNewlines(value);
    },

    /** Return one CommentNewlines from one JSON value. */
    fromJson(value: Json): CommentNewlines {
        return fromJsonCommentNewlines(value);
    },
};

/** Encode one CommentNewlines. */
export function encodeCommentNewlines(writer: BinaryWriter, value: CommentNewlines): void {
    writer.writeByte(value.bits);
}

/** Decode one CommentNewlines. */
export function decodeCommentNewlines(reader: BinaryReader): CommentNewlines {
    const bits = reader.readByte();

    return {
        bits,
    };
}

/** Return one JSON value for one CommentNewlines. */
export function toJsonCommentNewlines(value: CommentNewlines): Json {
    return {
        bits: value.bits,
    };
}

/** Return one CommentNewlines from one JSON value. */
export function fromJsonCommentNewlines(value: Json): CommentNewlines {
    const object = jsonObject(value);

    return {
        bits: jsonInteger(jsonField(object, "bits")),
    };
}

/** Structured content classification for one comment. */
export type CommentContent = "none" | "legal" | "jsdoc" | "jsdocLegal";

export const CommentContent = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: CommentContent): void {
        encodeCommentContent(writer, value);
    },

    /** Decode one CommentContent. */
    decode(reader: BinaryReader): CommentContent {
        return decodeCommentContent(reader);
    },

    /** Return this value as JSON. */
    toJson(value: CommentContent): Json {
        return toJsonCommentContent(value);
    },

    /** Return one CommentContent from one JSON value. */
    fromJson(value: Json): CommentContent {
        return fromJsonCommentContent(value);
    },
};

/** Encode one CommentContent. */
export function encodeCommentContent(writer: BinaryWriter, value: CommentContent): void {
    switch (value) {
        case "none":
            writer.writeUnsigned(0);
            return;
        case "legal":
            writer.writeUnsigned(1);
            return;
        case "jsdoc":
            writer.writeUnsigned(2);
            return;
        case "jsdocLegal":
            writer.writeUnsigned(3);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one CommentContent. */
export function decodeCommentContent(reader: BinaryReader): CommentContent {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "none";
        case 1:
            return "legal";
        case 2:
            return "jsdoc";
        case 3:
            return "jsdocLegal";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one CommentContent. */
export function toJsonCommentContent(value: CommentContent): Json {
    return value;
}

/** Return one CommentContent from one JSON value. */
export function fromJsonCommentContent(value: Json): CommentContent {
    const variant = jsonString(value);

    switch (variant) {
        case "none":
            return "none";
        case "legal":
            return "legal";
        case "jsdoc":
            return "jsdoc";
        case "jsdocLegal":
            return "jsdocLegal";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** Normalized documentation attached to one DIR node. */
export type Documentation = {
    /** The normalized documentation text. */
    readonly text: StringId;
};

export const Documentation = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: Documentation): void {
        encodeDocumentation(writer, value);
    },

    /** Decode one Documentation. */
    decode(reader: BinaryReader): Documentation {
        return decodeDocumentation(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Documentation): Json {
        return toJsonDocumentation(value);
    },

    /** Return one Documentation from one JSON value. */
    fromJson(value: Json): Documentation {
        return fromJsonDocumentation(value);
    },
};

/** Encode one Documentation. */
export function encodeDocumentation(writer: BinaryWriter, value: Documentation): void {
    encodeStringId(writer, value.text);
}

/** Decode one Documentation. */
export function decodeDocumentation(reader: BinaryReader): Documentation {
    const text = decodeStringId(reader);

    return {
        text,
    };
}

/** Return one JSON value for one Documentation. */
export function toJsonDocumentation(value: Documentation): Json {
    return {
        text: toJsonStringId(value.text),
    };
}

/** Return one Documentation from one JSON value. */
export function fromJsonDocumentation(value: Json): Documentation {
    const object = jsonObject(value);

    return {
        text: fromJsonStringId(jsonField(object, "text")),
    };
}
