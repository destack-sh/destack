/* generated by destack_bridge_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, jsonField, jsonObject, jsonOptional, jsonString } from "../../../protocol/serde.js";
import type { ExportSelector } from "./export.js";
import type { StaticKey } from "./key.js";
import type { LocalSymbolId } from "./symbol.js";
import type { LocalNodeId } from "../tree/node.js";
import type { ModuleId } from "../../source/file/model/module.js";
import { decodeExportSelector, encodeExportSelector, fromJsonExportSelector, toJsonExportSelector } from "./export.js";
import { decodeStaticKey, encodeStaticKey, fromJsonStaticKey, toJsonStaticKey } from "./key.js";
import { decodeLocalSymbolId, encodeLocalSymbolId, fromJsonLocalSymbolId, toJsonLocalSymbolId } from "./symbol.js";
import { decodeLocalNodeId, encodeLocalNodeId, fromJsonLocalNodeId, toJsonLocalNodeId } from "../tree/node.js";
import { decodeModuleId, encodeModuleId, fromJsonModuleId, toJsonModuleId } from "../../source/file/model/module.js";

/** One global table entry. */
export type GlobalEntry =
    /** A local global declaration. */
    | {
          readonly kind: "local";
          readonly local: LocalGlobalEntry;
      }
    /** A re-exported global declaration. */
    | {
          readonly kind: "indirect";
          readonly indirect: IndirectGlobalEntry;
      }
;

export const GlobalEntry = {
    /** A local global declaration. */
    local(local: LocalGlobalEntry): GlobalEntry {
        return { kind: "local", local };
    },

    /** A re-exported global declaration. */
    indirect(indirect: IndirectGlobalEntry): GlobalEntry {
        return { kind: "indirect", indirect };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: GlobalEntry): void {
        encodeGlobalEntry(writer, value);
    },

    /** Decode one GlobalEntry. */
    decode(reader: BinaryReader): GlobalEntry {
        return decodeGlobalEntry(reader);
    },

    /** Return this value as JSON. */
    toJson(value: GlobalEntry): Json {
        return toJsonGlobalEntry(value);
    },

    /** Return one GlobalEntry from one JSON value. */
    fromJson(value: Json): GlobalEntry {
        return fromJsonGlobalEntry(value);
    },
};

/** Encode one GlobalEntry. */
export function encodeGlobalEntry(writer: BinaryWriter, value: GlobalEntry): void {
    switch (value.kind) {
        case "local":
            writer.writeUnsigned(0);
            encodeLocalGlobalEntry(writer, value.local);
            return;
        case "indirect":
            writer.writeUnsigned(1);
            encodeIndirectGlobalEntry(writer, value.indirect);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one GlobalEntry. */
export function decodeGlobalEntry(reader: BinaryReader): GlobalEntry {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const local = decodeLocalGlobalEntry(reader);

            return { kind: "local", local };
        }
        case 1: {
            const indirect = decodeIndirectGlobalEntry(reader);

            return { kind: "indirect", indirect };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one GlobalEntry. */
export function toJsonGlobalEntry(value: GlobalEntry): Json {
    switch (value.kind) {
        case "local":
            return {
                kind: "local",
                local: toJsonLocalGlobalEntry(value.local),
            };
        case "indirect":
            return {
                kind: "indirect",
                indirect: toJsonIndirectGlobalEntry(value.indirect),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one GlobalEntry from one JSON value. */
export function fromJsonGlobalEntry(value: Json): GlobalEntry {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "local":
            return {
                kind,
                local: fromJsonLocalGlobalEntry(jsonField(object, "local")),
            };
        case "indirect":
            return {
                kind,
                indirect: fromJsonIndirectGlobalEntry(jsonField(object, "indirect")),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** One local global declaration from a symbol declared in the current module. */
export type LocalGlobalEntry = {
    /** The global name. */
    readonly key: StaticKey;
    /** The local symbol exposed as a global. */
    readonly source: LocalSymbolId;
};

export const LocalGlobalEntry = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: LocalGlobalEntry): void {
        encodeLocalGlobalEntry(writer, value);
    },

    /** Decode one LocalGlobalEntry. */
    decode(reader: BinaryReader): LocalGlobalEntry {
        return decodeLocalGlobalEntry(reader);
    },

    /** Return this value as JSON. */
    toJson(value: LocalGlobalEntry): Json {
        return toJsonLocalGlobalEntry(value);
    },

    /** Return one LocalGlobalEntry from one JSON value. */
    fromJson(value: Json): LocalGlobalEntry {
        return fromJsonLocalGlobalEntry(value);
    },
};

/** Encode one LocalGlobalEntry. */
export function encodeLocalGlobalEntry(writer: BinaryWriter, value: LocalGlobalEntry): void {
    encodeStaticKey(writer, value.key);
    encodeLocalSymbolId(writer, value.source);
}

/** Decode one LocalGlobalEntry. */
export function decodeLocalGlobalEntry(reader: BinaryReader): LocalGlobalEntry {
    const key = decodeStaticKey(reader);
    const source = decodeLocalSymbolId(reader);

    return {
        key,
        source,
    };
}

/** Return one JSON value for one LocalGlobalEntry. */
export function toJsonLocalGlobalEntry(value: LocalGlobalEntry): Json {
    return {
        key: toJsonStaticKey(value.key),
        source: toJsonLocalSymbolId(value.source),
    };
}

/** Return one LocalGlobalEntry from one JSON value. */
export function fromJsonLocalGlobalEntry(value: Json): LocalGlobalEntry {
    const object = jsonObject(value);

    return {
        key: fromJsonStaticKey(jsonField(object, "key")),
        source: fromJsonLocalSymbolId(jsonField(object, "source")),
    };
}

/** One named global re-export from another module. */
export type IndirectGlobalEntry = {
    /** The global name. */
    readonly key: StaticKey;
    /** The dependency item that declared the global export. */
    readonly item: LocalNodeId;
    /** The target module selected by the export. */
    readonly target?: ModuleId;
    /** The export selected from the target module. */
    readonly imported: ExportSelector;
};

export const IndirectGlobalEntry = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: IndirectGlobalEntry): void {
        encodeIndirectGlobalEntry(writer, value);
    },

    /** Decode one IndirectGlobalEntry. */
    decode(reader: BinaryReader): IndirectGlobalEntry {
        return decodeIndirectGlobalEntry(reader);
    },

    /** Return this value as JSON. */
    toJson(value: IndirectGlobalEntry): Json {
        return toJsonIndirectGlobalEntry(value);
    },

    /** Return one IndirectGlobalEntry from one JSON value. */
    fromJson(value: Json): IndirectGlobalEntry {
        return fromJsonIndirectGlobalEntry(value);
    },
};

/** Encode one IndirectGlobalEntry. */
export function encodeIndirectGlobalEntry(writer: BinaryWriter, value: IndirectGlobalEntry): void {
    encodeStaticKey(writer, value.key);
    encodeLocalNodeId(writer, value.item);
    writer.writeOption(value.target, (value2) => {
        encodeModuleId(writer, value2);
    });
    encodeExportSelector(writer, value.imported);
}

/** Decode one IndirectGlobalEntry. */
export function decodeIndirectGlobalEntry(reader: BinaryReader): IndirectGlobalEntry {
    const key = decodeStaticKey(reader);
    const item = decodeLocalNodeId(reader);
    const target = reader.readOption(() => decodeModuleId(reader));
    const imported = decodeExportSelector(reader);

    return {
        key,
        item,
        ...(target === undefined ? {} : { target }),
        imported,
    };
}

/** Return one JSON value for one IndirectGlobalEntry. */
export function toJsonIndirectGlobalEntry(value: IndirectGlobalEntry): Json {
    return {
        key: toJsonStaticKey(value.key),
        item: toJsonLocalNodeId(value.item),
        ...(value.target === undefined ? {} : { target: toJsonModuleId(value.target) }),
        imported: toJsonExportSelector(value.imported),
    };
}

/** Return one IndirectGlobalEntry from one JSON value. */
export function fromJsonIndirectGlobalEntry(value: Json): IndirectGlobalEntry {
    const object = jsonObject(value);

    return {
        key: fromJsonStaticKey(jsonField(object, "key")),
        item: fromJsonLocalNodeId(jsonField(object, "item")),
        target: jsonOptional(object, "target", (value) => fromJsonModuleId(value)),
        imported: fromJsonExportSelector(jsonField(object, "imported")),
    };
}
