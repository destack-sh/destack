/* generated by destack_bridge_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, jsonArray, jsonField, jsonInteger, jsonObject } from "../../../protocol/serde.js";
import type { Origin } from "./origin.js";
import { decodeOrigin, encodeOrigin, fromJsonOrigin, toJsonOrigin } from "./origin.js";

/** Sorted sparse table keyed by node id. */
export type SparseNodeMap = {
    /** The sparse entries sorted by node id. */
    readonly entries: ReadonlyArray<SparseNodeEntry>;
};

export const SparseNodeMap = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: SparseNodeMap): void {
        encodeSparseNodeMap(writer, value);
    },

    /** Decode one SparseNodeMap. */
    decode(reader: BinaryReader): SparseNodeMap {
        return decodeSparseNodeMap(reader);
    },

    /** Return this value as JSON. */
    toJson(value: SparseNodeMap): Json {
        return toJsonSparseNodeMap(value);
    },

    /** Return one SparseNodeMap from one JSON value. */
    fromJson(value: Json): SparseNodeMap {
        return fromJsonSparseNodeMap(value);
    },
};

/** Encode one SparseNodeMap. */
export function encodeSparseNodeMap(writer: BinaryWriter, value: SparseNodeMap): void {
    writer.writeUnsigned(value.entries.length);
    for (const item0 of value.entries) {
        encodeSparseNodeEntry(writer, item0);
    }
}

/** Decode one SparseNodeMap. */
export function decodeSparseNodeMap(reader: BinaryReader): SparseNodeMap {
    const entries = (() => { const length0 = reader.readNumber(); const items0: Array<SparseNodeEntry> = []; for (let index = 0; index < length0; index += 1) { items0.push(decodeSparseNodeEntry(reader)); } return items0; })();

    return {
        entries,
    };
}

/** Return one JSON value for one SparseNodeMap. */
export function toJsonSparseNodeMap(value: SparseNodeMap): Json {
    return {
        entries: value.entries.map((item0) => toJsonSparseNodeEntry(item0)),
    };
}

/** Return one SparseNodeMap from one JSON value. */
export function fromJsonSparseNodeMap(value: Json): SparseNodeMap {
    const object = jsonObject(value);

    return {
        entries: jsonArray(jsonField(object, "entries")).map((item0) => fromJsonSparseNodeEntry(item0)),
    };
}

/** One sparse node table entry. */
export type SparseNodeEntry = {
    /** The node id that owns the value. */
    readonly nodeId: number;
    /** The sparse value. */
    readonly value: Origin;
};

export const SparseNodeEntry = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: SparseNodeEntry): void {
        encodeSparseNodeEntry(writer, value);
    },

    /** Decode one SparseNodeEntry. */
    decode(reader: BinaryReader): SparseNodeEntry {
        return decodeSparseNodeEntry(reader);
    },

    /** Return this value as JSON. */
    toJson(value: SparseNodeEntry): Json {
        return toJsonSparseNodeEntry(value);
    },

    /** Return one SparseNodeEntry from one JSON value. */
    fromJson(value: Json): SparseNodeEntry {
        return fromJsonSparseNodeEntry(value);
    },
};

/** Encode one SparseNodeEntry. */
export function encodeSparseNodeEntry(writer: BinaryWriter, value: SparseNodeEntry): void {
    writer.writeUnsigned(value.nodeId);
    encodeOrigin(writer, value.value);
}

/** Decode one SparseNodeEntry. */
export function decodeSparseNodeEntry(reader: BinaryReader): SparseNodeEntry {
    const nodeId = reader.readNumber();
    const value = decodeOrigin(reader);

    return {
        nodeId,
        value,
    };
}

/** Return one JSON value for one SparseNodeEntry. */
export function toJsonSparseNodeEntry(value: SparseNodeEntry): Json {
    return {
        nodeId: value.nodeId,
        value: toJsonOrigin(value.value),
    };
}

/** Return one SparseNodeEntry from one JSON value. */
export function fromJsonSparseNodeEntry(value: Json): SparseNodeEntry {
    const object = jsonObject(value);

    return {
        nodeId: jsonInteger(jsonField(object, "nodeId")),
        value: fromJsonOrigin(jsonField(object, "value")),
    };
}
