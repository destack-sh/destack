/* generated by destack_bridge_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, jsonField, jsonObject, jsonOptional, jsonString } from "../../../protocol/serde.js";
import type { LocalNodeId } from "./node.js";
import { decodeLocalNodeId, encodeLocalNodeId, fromJsonLocalNodeId, toJsonLocalNodeId } from "./node.js";

/** The style of a match expression. */
export type MatchForm = "match" | "switch";

export const MatchForm = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: MatchForm): void {
        encodeMatchForm(writer, value);
    },

    /** Decode one MatchForm. */
    decode(reader: BinaryReader): MatchForm {
        return decodeMatchForm(reader);
    },

    /** Return this value as JSON. */
    toJson(value: MatchForm): Json {
        return toJsonMatchForm(value);
    },

    /** Return one MatchForm from one JSON value. */
    fromJson(value: Json): MatchForm {
        return fromJsonMatchForm(value);
    },
};

/** Encode one MatchForm. */
export function encodeMatchForm(writer: BinaryWriter, value: MatchForm): void {
    switch (value) {
        case "match":
            writer.writeUnsigned(0);
            return;
        case "switch":
            writer.writeUnsigned(1);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one MatchForm. */
export function decodeMatchForm(reader: BinaryReader): MatchForm {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "match";
        case 1:
            return "switch";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one MatchForm. */
export function toJsonMatchForm(value: MatchForm): Json {
    return value;
}

/** Return one MatchForm from one JSON value. */
export function fromJsonMatchForm(value: Json): MatchForm {
    const variant = jsonString(value);

    switch (variant) {
        case "match":
            return "match";
        case "switch":
            return "switch";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** A MatchCase is a match case inside a Match expression. */
export type MatchCase =
    /** A match case with an expression body. */
    | {
          readonly kind: "expression";
          readonly selector: MatchSelector;
          readonly body: LocalNodeId;
      }
    /** A match case with a block body. */
    | {
          readonly kind: "block";
          readonly selector: MatchSelector;
          readonly body: LocalNodeId;
      }
;

export const MatchCase = {
    /** A match case with an expression body. */
    expression(selector: MatchSelector, body: LocalNodeId): MatchCase {
        return { kind: "expression", selector, body };
    },

    /** A match case with a block body. */
    block(selector: MatchSelector, body: LocalNodeId): MatchCase {
        return { kind: "block", selector, body };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: MatchCase): void {
        encodeMatchCase(writer, value);
    },

    /** Decode one MatchCase. */
    decode(reader: BinaryReader): MatchCase {
        return decodeMatchCase(reader);
    },

    /** Return this value as JSON. */
    toJson(value: MatchCase): Json {
        return toJsonMatchCase(value);
    },

    /** Return one MatchCase from one JSON value. */
    fromJson(value: Json): MatchCase {
        return fromJsonMatchCase(value);
    },
};

/** Encode one MatchCase. */
export function encodeMatchCase(writer: BinaryWriter, value: MatchCase): void {
    switch (value.kind) {
        case "expression":
            writer.writeUnsigned(0);
            encodeMatchSelector(writer, value.selector);
            encodeLocalNodeId(writer, value.body);
            return;
        case "block":
            writer.writeUnsigned(1);
            encodeMatchSelector(writer, value.selector);
            encodeLocalNodeId(writer, value.body);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one MatchCase. */
export function decodeMatchCase(reader: BinaryReader): MatchCase {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const selector = decodeMatchSelector(reader);
            const body = decodeLocalNodeId(reader);

            return {
                kind: "expression",
                selector,
                body,
            };
        }
        case 1: {
            const selector = decodeMatchSelector(reader);
            const body = decodeLocalNodeId(reader);

            return {
                kind: "block",
                selector,
                body,
            };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one MatchCase. */
export function toJsonMatchCase(value: MatchCase): Json {
    switch (value.kind) {
        case "expression":
            return {
                kind: "expression",
                selector: toJsonMatchSelector(value.selector),
                body: toJsonLocalNodeId(value.body),
            };
        case "block":
            return {
                kind: "block",
                selector: toJsonMatchSelector(value.selector),
                body: toJsonLocalNodeId(value.body),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one MatchCase from one JSON value. */
export function fromJsonMatchCase(value: Json): MatchCase {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "expression":
            return {
                kind,
                selector: fromJsonMatchSelector(jsonField(object, "selector")),
                body: fromJsonLocalNodeId(jsonField(object, "body")),
            };
        case "block":
            return {
                kind,
                selector: fromJsonMatchSelector(jsonField(object, "selector")),
                body: fromJsonLocalNodeId(jsonField(object, "body")),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** A MatchSelector determines which case is selected in a match/switch expression. */
export type MatchSelector =
    /** A pattern with an optional guard (e.g., `x if x > 0`). */
    | {
          readonly kind: "pattern";
          readonly pattern: LocalNodeId;
          readonly guard?: LocalNodeId;
      }
    /** The default case in a switch statement (`default:`). */
    | {
          readonly kind: "default";
      }
;

export const MatchSelector = {
    /** A pattern with an optional guard (e.g., `x if x > 0`). */
    pattern(pattern: LocalNodeId, guard: LocalNodeId | undefined): MatchSelector {
        return { kind: "pattern", pattern, guard };
    },

    /** The default case in a switch statement (`default:`). */
    "default"(): MatchSelector {
        return { kind: "default" };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: MatchSelector): void {
        encodeMatchSelector(writer, value);
    },

    /** Decode one MatchSelector. */
    decode(reader: BinaryReader): MatchSelector {
        return decodeMatchSelector(reader);
    },

    /** Return this value as JSON. */
    toJson(value: MatchSelector): Json {
        return toJsonMatchSelector(value);
    },

    /** Return one MatchSelector from one JSON value. */
    fromJson(value: Json): MatchSelector {
        return fromJsonMatchSelector(value);
    },
};

/** Encode one MatchSelector. */
export function encodeMatchSelector(writer: BinaryWriter, value: MatchSelector): void {
    switch (value.kind) {
        case "pattern":
            writer.writeUnsigned(0);
            encodeLocalNodeId(writer, value.pattern);
            writer.writeOption(value.guard, (value1) => {
                encodeLocalNodeId(writer, value1);
            });
            return;
        case "default":
            writer.writeUnsigned(1);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one MatchSelector. */
export function decodeMatchSelector(reader: BinaryReader): MatchSelector {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const pattern = decodeLocalNodeId(reader);
            const guard = reader.readOption(() => decodeLocalNodeId(reader));

            return {
                kind: "pattern",
                pattern,
                ...(guard === undefined ? {} : { guard }),
            };
        }
        case 1: {
            return { kind: "default" };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one MatchSelector. */
export function toJsonMatchSelector(value: MatchSelector): Json {
    switch (value.kind) {
        case "pattern":
            return {
                kind: "pattern",
                pattern: toJsonLocalNodeId(value.pattern),
                ...(value.guard === undefined ? {} : { guard: toJsonLocalNodeId(value.guard) }),
            };
        case "default":
            return {
                kind: "default",
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one MatchSelector from one JSON value. */
export function fromJsonMatchSelector(value: Json): MatchSelector {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "pattern":
            return {
                kind,
                pattern: fromJsonLocalNodeId(jsonField(object, "pattern")),
                guard: jsonOptional(object, "guard", (value) => fromJsonLocalNodeId(value)),
            };
        case "default":
            return {
                kind,
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}
