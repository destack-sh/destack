/* generated by destack_bridge_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, jsonString } from "../../../protocol/serde.js";

/** How one cast expression entered the DIR. */
export type CastOrigin = "explicit" | "implicit";

export const CastOrigin = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: CastOrigin): void {
        encodeCastOrigin(writer, value);
    },

    /** Decode one CastOrigin. */
    decode(reader: BinaryReader): CastOrigin {
        return decodeCastOrigin(reader);
    },

    /** Return this value as JSON. */
    toJson(value: CastOrigin): Json {
        return toJsonCastOrigin(value);
    },

    /** Return one CastOrigin from one JSON value. */
    fromJson(value: Json): CastOrigin {
        return fromJsonCastOrigin(value);
    },
};

/** Encode one CastOrigin. */
export function encodeCastOrigin(writer: BinaryWriter, value: CastOrigin): void {
    switch (value) {
        case "explicit":
            writer.writeUnsigned(0);
            return;
        case "implicit":
            writer.writeUnsigned(1);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one CastOrigin. */
export function decodeCastOrigin(reader: BinaryReader): CastOrigin {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "explicit";
        case 1:
            return "implicit";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one CastOrigin. */
export function toJsonCastOrigin(value: CastOrigin): Json {
    return value;
}

/** Return one CastOrigin from one JSON value. */
export function fromJsonCastOrigin(value: Json): CastOrigin {
    const variant = jsonString(value);

    switch (variant) {
        case "explicit":
            return "explicit";
        case "implicit":
            return "implicit";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}
