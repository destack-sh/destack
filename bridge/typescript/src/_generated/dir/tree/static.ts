/* generated by destack_bridge_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, jsonArray, jsonBigint, jsonField, jsonInteger, jsonObject, jsonOptional, jsonString } from "../../../protocol/serde.js";
import type { StaticKey } from "../symbol/key.js";
import type { FunctionSignature } from "./function.js";
import type { ScalarLiteral } from "./literal.js";
import type { GlobalTypeId } from "../type/type.js";
import type { ModuleId } from "../../source/file/model/module.js";
import { decodeStaticKey, encodeStaticKey, fromJsonStaticKey, toJsonStaticKey } from "../symbol/key.js";
import { decodeFunctionSignature, encodeFunctionSignature, fromJsonFunctionSignature, toJsonFunctionSignature } from "./function.js";
import { decodeScalarLiteral, encodeScalarLiteral, fromJsonScalarLiteral, toJsonScalarLiteral } from "./literal.js";
import { decodeGlobalTypeId, encodeGlobalTypeId, fromJsonGlobalTypeId, toJsonGlobalTypeId } from "../type/type.js";
import { decodeModuleId, encodeModuleId, fromJsonModuleId, toJsonModuleId } from "../../source/file/model/module.js";

/** Global static id across modules. */
export type GlobalStaticId = {
    /** The module id of the global static value. */
    readonly moduleId: ModuleId;
    /** The local id of the global static value. */
    readonly localId: LocalStaticId;
};

export const GlobalStaticId = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: GlobalStaticId): void {
        encodeGlobalStaticId(writer, value);
    },

    /** Decode one GlobalStaticId. */
    decode(reader: BinaryReader): GlobalStaticId {
        return decodeGlobalStaticId(reader);
    },

    /** Return this value as JSON. */
    toJson(value: GlobalStaticId): Json {
        return toJsonGlobalStaticId(value);
    },

    /** Return one GlobalStaticId from one JSON value. */
    fromJson(value: Json): GlobalStaticId {
        return fromJsonGlobalStaticId(value);
    },
};

/** Encode one GlobalStaticId. */
export function encodeGlobalStaticId(writer: BinaryWriter, value: GlobalStaticId): void {
    encodeModuleId(writer, value.moduleId);
    encodeLocalStaticId(writer, value.localId);
}

/** Decode one GlobalStaticId. */
export function decodeGlobalStaticId(reader: BinaryReader): GlobalStaticId {
    const moduleId = decodeModuleId(reader);
    const localId = decodeLocalStaticId(reader);

    return {
        moduleId,
        localId,
    };
}

/** Return one JSON value for one GlobalStaticId. */
export function toJsonGlobalStaticId(value: GlobalStaticId): Json {
    return {
        moduleId: toJsonModuleId(value.moduleId),
        localId: toJsonLocalStaticId(value.localId),
    };
}

/** Return one GlobalStaticId from one JSON value. */
export function fromJsonGlobalStaticId(value: Json): GlobalStaticId {
    const object = jsonObject(value);

    return {
        moduleId: fromJsonModuleId(jsonField(object, "moduleId")),
        localId: fromJsonLocalStaticId(jsonField(object, "localId")),
    };
}

/** Unique identifier for a local static value. */
export type LocalStaticId = number;

export const LocalStaticId = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: LocalStaticId): void {
        encodeLocalStaticId(writer, value);
    },

    /** Decode one LocalStaticId. */
    decode(reader: BinaryReader): LocalStaticId {
        return decodeLocalStaticId(reader);
    },

    /** Return this value as JSON. */
    toJson(value: LocalStaticId): Json {
        return toJsonLocalStaticId(value);
    },

    /** Return one LocalStaticId from one JSON value. */
    fromJson(value: Json): LocalStaticId {
        return fromJsonLocalStaticId(value);
    },
};

/** Encode one LocalStaticId. */
export function encodeLocalStaticId(writer: BinaryWriter, value: LocalStaticId): void {
    writer.writeUnsigned(value);
}

/** Decode one LocalStaticId. */
export function decodeLocalStaticId(reader: BinaryReader): LocalStaticId {
    return reader.readNumber();
}

/** Return one JSON value for one LocalStaticId. */
export function toJsonLocalStaticId(value: LocalStaticId): Json {
    return value;
}

/** Return one LocalStaticId from one JSON value. */
export function fromJsonLocalStaticId(value: Json): LocalStaticId {
    return jsonInteger(value);
}

/** Concrete static value produced by checked static evaluation. */
export type StaticTerm =
    /** Scalar literal. */
    | {
          readonly kind: "scalarLiteral";
          readonly value: ScalarLiteral;
      }
    /** Type value. */
    | {
          readonly kind: "type";
          readonly ty: GlobalTypeId;
      }
    /** Array value. */
    | {
          readonly kind: "array";
          readonly elements: ReadonlyArray<StaticTerm>;
      }
    /** Fixed array value. */
    | {
          readonly kind: "fixedArray";
          /** The repeated value. */
          readonly value: StaticTerm;
          /** The fixed array length. */
          readonly length: bigint;
      }
    /** Tuple value. */
    | {
          readonly kind: "tuple";
          readonly elements: ReadonlyArray<StaticTerm>;
      }
    /** Structural object value. */
    | {
          readonly kind: "object";
          /** The object properties. */
          readonly properties: ReadonlyArray<StaticProperty>;
      }
    /** Nominal struct value. */
    | {
          readonly kind: "struct";
          /** The struct type selected for this value. */
          readonly ty: GlobalTypeId;
          /** The struct properties. */
          readonly properties: ReadonlyArray<StaticProperty>;
      }
;

export const StaticTerm = {
    /** Scalar literal. */
    scalarLiteral(value: ScalarLiteral): StaticTerm {
        return { kind: "scalarLiteral", value };
    },

    /** Type value. */
    "type"(ty: GlobalTypeId): StaticTerm {
        return { kind: "type", ty };
    },

    /** Array value. */
    array(elements: ReadonlyArray<StaticTerm>): StaticTerm {
        return { kind: "array", elements };
    },

    /** Fixed array value. */
    fixedArray(value: StaticTerm, length: bigint): StaticTerm {
        return { kind: "fixedArray", value, length };
    },

    /** Tuple value. */
    tuple(elements: ReadonlyArray<StaticTerm>): StaticTerm {
        return { kind: "tuple", elements };
    },

    /** Structural object value. */
    "object"(properties: ReadonlyArray<StaticProperty>): StaticTerm {
        return { kind: "object", properties };
    },

    /** Nominal struct value. */
    struct(ty: GlobalTypeId, properties: ReadonlyArray<StaticProperty>): StaticTerm {
        return { kind: "struct", ty, properties };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: StaticTerm): void {
        encodeStaticTerm(writer, value);
    },

    /** Decode one StaticTerm. */
    decode(reader: BinaryReader): StaticTerm {
        return decodeStaticTerm(reader);
    },

    /** Return this value as JSON. */
    toJson(value: StaticTerm): Json {
        return toJsonStaticTerm(value);
    },

    /** Return one StaticTerm from one JSON value. */
    fromJson(value: Json): StaticTerm {
        return fromJsonStaticTerm(value);
    },
};

/** Encode one StaticTerm. */
export function encodeStaticTerm(writer: BinaryWriter, value: StaticTerm): void {
    switch (value.kind) {
        case "scalarLiteral":
            writer.writeUnsigned(0);
            encodeScalarLiteral(writer, value.value);
            return;
        case "type":
            writer.writeUnsigned(1);
            encodeGlobalTypeId(writer, value.ty);
            return;
        case "array":
            writer.writeUnsigned(2);
            writer.writeUnsigned(value.elements.length);
            for (const item0 of value.elements) {
                encodeStaticTerm(writer, item0);
            }
            return;
        case "fixedArray":
            writer.writeUnsigned(3);
            encodeStaticTerm(writer, value.value);
            writer.writeUnsigned(value.length);
            return;
        case "tuple":
            writer.writeUnsigned(4);
            writer.writeUnsigned(value.elements.length);
            for (const item0 of value.elements) {
                encodeStaticTerm(writer, item0);
            }
            return;
        case "object":
            writer.writeUnsigned(5);
            writer.writeUnsigned(value.properties.length);
            for (const item0 of value.properties) {
                encodeStaticProperty(writer, item0);
            }
            return;
        case "struct":
            writer.writeUnsigned(6);
            encodeGlobalTypeId(writer, value.ty);
            writer.writeUnsigned(value.properties.length);
            for (const item1 of value.properties) {
                encodeStaticProperty(writer, item1);
            }
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one StaticTerm. */
export function decodeStaticTerm(reader: BinaryReader): StaticTerm {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const value = decodeScalarLiteral(reader);

            return {
                kind: "scalarLiteral",
                value,
            };
        }
        case 1: {
            const ty = decodeGlobalTypeId(reader);

            return {
                kind: "type",
                ty,
            };
        }
        case 2: {
            const elements = (() => { const length0 = reader.readNumber(); const items0: Array<StaticTerm> = []; for (let index = 0; index < length0; index += 1) { items0.push(decodeStaticTerm(reader)); } return items0; })();

            return {
                kind: "array",
                elements,
            };
        }
        case 3: {
            const value = decodeStaticTerm(reader);
            const length = reader.readUnsigned();

            return {
                kind: "fixedArray",
                value,
                length,
            };
        }
        case 4: {
            const elements = (() => { const length0 = reader.readNumber(); const items0: Array<StaticTerm> = []; for (let index = 0; index < length0; index += 1) { items0.push(decodeStaticTerm(reader)); } return items0; })();

            return {
                kind: "tuple",
                elements,
            };
        }
        case 5: {
            const properties = (() => { const length0 = reader.readNumber(); const items0: Array<StaticProperty> = []; for (let index = 0; index < length0; index += 1) { items0.push(decodeStaticProperty(reader)); } return items0; })();

            return {
                kind: "object",
                properties,
            };
        }
        case 6: {
            const ty = decodeGlobalTypeId(reader);
            const properties = (() => { const length1 = reader.readNumber(); const items1: Array<StaticProperty> = []; for (let index = 0; index < length1; index += 1) { items1.push(decodeStaticProperty(reader)); } return items1; })();

            return {
                kind: "struct",
                ty,
                properties,
            };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one StaticTerm. */
export function toJsonStaticTerm(value: StaticTerm): Json {
    switch (value.kind) {
        case "scalarLiteral":
            return {
                kind: "scalarLiteral",
                value: toJsonScalarLiteral(value.value),
            };
        case "type":
            return {
                kind: "type",
                ty: toJsonGlobalTypeId(value.ty),
            };
        case "array":
            return {
                kind: "array",
                elements: value.elements.map((item0) => toJsonStaticTerm(item0)),
            };
        case "fixedArray":
            return {
                kind: "fixedArray",
                value: toJsonStaticTerm(value.value),
                length: value.length.toString(),
            };
        case "tuple":
            return {
                kind: "tuple",
                elements: value.elements.map((item0) => toJsonStaticTerm(item0)),
            };
        case "object":
            return {
                kind: "object",
                properties: value.properties.map((item0) => toJsonStaticProperty(item0)),
            };
        case "struct":
            return {
                kind: "struct",
                ty: toJsonGlobalTypeId(value.ty),
                properties: value.properties.map((item0) => toJsonStaticProperty(item0)),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one StaticTerm from one JSON value. */
export function fromJsonStaticTerm(value: Json): StaticTerm {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "scalarLiteral":
            return {
                kind,
                value: fromJsonScalarLiteral(jsonField(object, "value")),
            };
        case "type":
            return {
                kind,
                ty: fromJsonGlobalTypeId(jsonField(object, "ty")),
            };
        case "array":
            return {
                kind,
                elements: jsonArray(jsonField(object, "elements")).map((item0) => fromJsonStaticTerm(item0)),
            };
        case "fixedArray":
            return {
                kind,
                value: fromJsonStaticTerm(jsonField(object, "value")),
                length: jsonBigint(jsonField(object, "length")),
            };
        case "tuple":
            return {
                kind,
                elements: jsonArray(jsonField(object, "elements")).map((item0) => fromJsonStaticTerm(item0)),
            };
        case "object":
            return {
                kind,
                properties: jsonArray(jsonField(object, "properties")).map((item0) => fromJsonStaticProperty(item0)),
            };
        case "struct":
            return {
                kind,
                ty: fromJsonGlobalTypeId(jsonField(object, "ty")),
                properties: jsonArray(jsonField(object, "properties")).map((item0) => fromJsonStaticProperty(item0)),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** Static object property in a checked static context. */
export type StaticProperty =
    /** Static field. */
    | {
          readonly kind: "field";
          /** The property key. */
          readonly key: StaticKey;
          /** The property value. */
          readonly value: StaticTerm;
      }
    /** Static member function. */
    | {
          readonly kind: "method";
          /** The optional method key. */
          readonly key?: StaticKey;
          /** The method signature. */
          readonly signature: FunctionSignature;
          /** The method body. */
          readonly body: StaticTerm;
      }
    /** Static spread. */
    | {
          readonly kind: "spread";
          /** The spread value. */
          readonly value: StaticTerm;
      }
;

export const StaticProperty = {
    /** Static field. */
    field(key: StaticKey, value: StaticTerm): StaticProperty {
        return { kind: "field", key, value };
    },

    /** Static member function. */
    method(key: StaticKey | undefined, signature: FunctionSignature, body: StaticTerm): StaticProperty {
        return { kind: "method", key, signature, body };
    },

    /** Static spread. */
    spread(value: StaticTerm): StaticProperty {
        return { kind: "spread", value };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: StaticProperty): void {
        encodeStaticProperty(writer, value);
    },

    /** Decode one StaticProperty. */
    decode(reader: BinaryReader): StaticProperty {
        return decodeStaticProperty(reader);
    },

    /** Return this value as JSON. */
    toJson(value: StaticProperty): Json {
        return toJsonStaticProperty(value);
    },

    /** Return one StaticProperty from one JSON value. */
    fromJson(value: Json): StaticProperty {
        return fromJsonStaticProperty(value);
    },
};

/** Encode one StaticProperty. */
export function encodeStaticProperty(writer: BinaryWriter, value: StaticProperty): void {
    switch (value.kind) {
        case "field":
            writer.writeUnsigned(0);
            encodeStaticKey(writer, value.key);
            encodeStaticTerm(writer, value.value);
            return;
        case "method":
            writer.writeUnsigned(1);
            writer.writeOption(value.key, (value0) => {
                encodeStaticKey(writer, value0);
            });
            encodeFunctionSignature(writer, value.signature);
            encodeStaticTerm(writer, value.body);
            return;
        case "spread":
            writer.writeUnsigned(2);
            encodeStaticTerm(writer, value.value);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one StaticProperty. */
export function decodeStaticProperty(reader: BinaryReader): StaticProperty {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const key = decodeStaticKey(reader);
            const value = decodeStaticTerm(reader);

            return {
                kind: "field",
                key,
                value,
            };
        }
        case 1: {
            const key = reader.readOption(() => decodeStaticKey(reader));
            const signature = decodeFunctionSignature(reader);
            const body = decodeStaticTerm(reader);

            return {
                kind: "method",
                ...(key === undefined ? {} : { key }),
                signature,
                body,
            };
        }
        case 2: {
            const value = decodeStaticTerm(reader);

            return {
                kind: "spread",
                value,
            };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one StaticProperty. */
export function toJsonStaticProperty(value: StaticProperty): Json {
    switch (value.kind) {
        case "field":
            return {
                kind: "field",
                key: toJsonStaticKey(value.key),
                value: toJsonStaticTerm(value.value),
            };
        case "method":
            return {
                kind: "method",
                ...(value.key === undefined ? {} : { key: toJsonStaticKey(value.key) }),
                signature: toJsonFunctionSignature(value.signature),
                body: toJsonStaticTerm(value.body),
            };
        case "spread":
            return {
                kind: "spread",
                value: toJsonStaticTerm(value.value),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one StaticProperty from one JSON value. */
export function fromJsonStaticProperty(value: Json): StaticProperty {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "field":
            return {
                kind,
                key: fromJsonStaticKey(jsonField(object, "key")),
                value: fromJsonStaticTerm(jsonField(object, "value")),
            };
        case "method":
            return {
                kind,
                key: jsonOptional(object, "key", (value) => fromJsonStaticKey(value)),
                signature: fromJsonFunctionSignature(jsonField(object, "signature")),
                body: fromJsonStaticTerm(jsonField(object, "body")),
            };
        case "spread":
            return {
                kind,
                value: fromJsonStaticTerm(jsonField(object, "value")),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}
