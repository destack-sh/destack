/* generated by destack_bridge_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, jsonArray, jsonField, jsonObject, jsonOptional, jsonString } from "../../../protocol/serde.js";
import type { GlobalSymbolId } from "../symbol/symbol.js";
import type { DefinitionMember } from "../table/definition.js";
import type { NominalHeritage } from "../table/definition.js";
import type { GlobalNodeIdAny } from "../tree/node.js";
import type { LocalGenericTemplateId } from "./generic.js";
import type { GlobalTypeId } from "./type.js";
import { decodeGlobalSymbolId, encodeGlobalSymbolId, fromJsonGlobalSymbolId, toJsonGlobalSymbolId } from "../symbol/symbol.js";
import { decodeDefinitionMember, encodeDefinitionMember, fromJsonDefinitionMember, toJsonDefinitionMember } from "../table/definition.js";
import { decodeNominalHeritage, encodeNominalHeritage, fromJsonNominalHeritage, toJsonNominalHeritage } from "../table/definition.js";
import { decodeGlobalNodeIdAny, encodeGlobalNodeIdAny, fromJsonGlobalNodeIdAny, toJsonGlobalNodeIdAny } from "../tree/node.js";
import { decodeLocalGenericTemplateId, encodeLocalGenericTemplateId, fromJsonLocalGenericTemplateId, toJsonLocalGenericTemplateId } from "./generic.js";
import { decodeGlobalTypeId, encodeGlobalTypeId, fromJsonGlobalTypeId, toJsonGlobalTypeId } from "./type.js";

/** A resolved extension declaration. */
export type Extension = {
    /** The extension declaration's symbol. */
    readonly symbol: GlobalSymbolId;
    /** The extension declaration form. */
    readonly form: ExtensionForm;
    /** The extension's generic template. */
    readonly template?: LocalGenericTemplateId;
    /** The checked receiver target. */
    readonly target: ExtensionTarget;
    /** The implemented interfaces. */
    readonly implements: ReadonlyArray<NominalHeritage>;
    /** The checked where clauses that gate this extension. */
    readonly whereClauses: ReadonlyArray<ExtensionWhereClause>;
    /** The members in declaration order. */
    readonly members: ReadonlyArray<DefinitionMember>;
};

export const Extension = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: Extension): void {
        encodeExtension(writer, value);
    },

    /** Decode one Extension. */
    decode(reader: BinaryReader): Extension {
        return decodeExtension(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Extension): Json {
        return toJsonExtension(value);
    },

    /** Return one Extension from one JSON value. */
    fromJson(value: Json): Extension {
        return fromJsonExtension(value);
    },
};

/** Encode one Extension. */
export function encodeExtension(writer: BinaryWriter, value: Extension): void {
    encodeGlobalSymbolId(writer, value.symbol);
    encodeExtensionForm(writer, value.form);
    writer.writeOption(value.template, (value2) => {
        encodeLocalGenericTemplateId(writer, value2);
    });
    encodeExtensionTarget(writer, value.target);
    writer.writeUnsigned(value.implements.length);
    for (const item4 of value.implements) {
        encodeNominalHeritage(writer, item4);
    }
    writer.writeUnsigned(value.whereClauses.length);
    for (const item5 of value.whereClauses) {
        encodeExtensionWhereClause(writer, item5);
    }
    writer.writeUnsigned(value.members.length);
    for (const item6 of value.members) {
        encodeDefinitionMember(writer, item6);
    }
}

/** Decode one Extension. */
export function decodeExtension(reader: BinaryReader): Extension {
    const symbol_ = decodeGlobalSymbolId(reader);
    const form = decodeExtensionForm(reader);
    const template = reader.readOption(() => decodeLocalGenericTemplateId(reader));
    const target = decodeExtensionTarget(reader);
    const implements_ = (() => { const length4 = reader.readNumber(); const items4: Array<NominalHeritage> = []; for (let index = 0; index < length4; index += 1) { items4.push(decodeNominalHeritage(reader)); } return items4; })();
    const whereClauses = (() => { const length5 = reader.readNumber(); const items5: Array<ExtensionWhereClause> = []; for (let index = 0; index < length5; index += 1) { items5.push(decodeExtensionWhereClause(reader)); } return items5; })();
    const members = (() => { const length6 = reader.readNumber(); const items6: Array<DefinitionMember> = []; for (let index = 0; index < length6; index += 1) { items6.push(decodeDefinitionMember(reader)); } return items6; })();

    return {
        symbol: symbol_,
        form,
        ...(template === undefined ? {} : { template }),
        target,
        implements: implements_,
        whereClauses,
        members,
    };
}

/** Return one JSON value for one Extension. */
export function toJsonExtension(value: Extension): Json {
    return {
        symbol: toJsonGlobalSymbolId(value.symbol),
        form: toJsonExtensionForm(value.form),
        ...(value.template === undefined ? {} : { template: toJsonLocalGenericTemplateId(value.template) }),
        target: toJsonExtensionTarget(value.target),
        implements: value.implements.map((item0) => toJsonNominalHeritage(item0)),
        whereClauses: value.whereClauses.map((item0) => toJsonExtensionWhereClause(item0)),
        members: value.members.map((item0) => toJsonDefinitionMember(item0)),
    };
}

/** Return one Extension from one JSON value. */
export function fromJsonExtension(value: Json): Extension {
    const object = jsonObject(value);

    return {
        symbol: fromJsonGlobalSymbolId(jsonField(object, "symbol")),
        form: fromJsonExtensionForm(jsonField(object, "form")),
        template: jsonOptional(object, "template", (value) => fromJsonLocalGenericTemplateId(value)),
        target: fromJsonExtensionTarget(jsonField(object, "target")),
        implements: jsonArray(jsonField(object, "implements")).map((item0) => fromJsonNominalHeritage(item0)),
        whereClauses: jsonArray(jsonField(object, "whereClauses")).map((item0) => fromJsonExtensionWhereClause(item0)),
        members: jsonArray(jsonField(object, "members")).map((item0) => fromJsonDefinitionMember(item0)),
    };
}

/** How an extension declaration relates to its target type. */
export type ExtensionForm = "inherent" | "local" | "named";

export const ExtensionForm = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ExtensionForm): void {
        encodeExtensionForm(writer, value);
    },

    /** Decode one ExtensionForm. */
    decode(reader: BinaryReader): ExtensionForm {
        return decodeExtensionForm(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ExtensionForm): Json {
        return toJsonExtensionForm(value);
    },

    /** Return one ExtensionForm from one JSON value. */
    fromJson(value: Json): ExtensionForm {
        return fromJsonExtensionForm(value);
    },
};

/** Encode one ExtensionForm. */
export function encodeExtensionForm(writer: BinaryWriter, value: ExtensionForm): void {
    switch (value) {
        case "inherent":
            writer.writeUnsigned(0);
            return;
        case "local":
            writer.writeUnsigned(1);
            return;
        case "named":
            writer.writeUnsigned(2);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one ExtensionForm. */
export function decodeExtensionForm(reader: BinaryReader): ExtensionForm {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "inherent";
        case 1:
            return "local";
        case 2:
            return "named";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one ExtensionForm. */
export function toJsonExtensionForm(value: ExtensionForm): Json {
    return value;
}

/** Return one ExtensionForm from one JSON value. */
export function fromJsonExtensionForm(value: Json): ExtensionForm {
    const variant = jsonString(value);

    switch (variant) {
        case "inherent":
            return "inherent";
        case "local":
            return "local";
        case "named":
            return "named";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** Extension lookup target. */
export type ExtensionTarget =
    /** Extension whose receiver type has a nominal root. */
    | {
          readonly kind: "nominal";
          /** The nominal root used for member lookup. */
          readonly root: GlobalSymbolId;
          /** The checked receiver type. */
          readonly ty: GlobalTypeId;
      }
    /** Extension over an open receiver type. */
    | {
          readonly kind: "blanket";
          /** The checked receiver type. */
          readonly ty: GlobalTypeId;
      }
;

export const ExtensionTarget = {
    /** Extension whose receiver type has a nominal root. */
    nominal(root: GlobalSymbolId, ty: GlobalTypeId): ExtensionTarget {
        return { kind: "nominal", root, ty };
    },

    /** Extension over an open receiver type. */
    blanket(ty: GlobalTypeId): ExtensionTarget {
        return { kind: "blanket", ty };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: ExtensionTarget): void {
        encodeExtensionTarget(writer, value);
    },

    /** Decode one ExtensionTarget. */
    decode(reader: BinaryReader): ExtensionTarget {
        return decodeExtensionTarget(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ExtensionTarget): Json {
        return toJsonExtensionTarget(value);
    },

    /** Return one ExtensionTarget from one JSON value. */
    fromJson(value: Json): ExtensionTarget {
        return fromJsonExtensionTarget(value);
    },
};

/** Encode one ExtensionTarget. */
export function encodeExtensionTarget(writer: BinaryWriter, value: ExtensionTarget): void {
    switch (value.kind) {
        case "nominal":
            writer.writeUnsigned(0);
            encodeGlobalSymbolId(writer, value.root);
            encodeGlobalTypeId(writer, value.ty);
            return;
        case "blanket":
            writer.writeUnsigned(1);
            encodeGlobalTypeId(writer, value.ty);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one ExtensionTarget. */
export function decodeExtensionTarget(reader: BinaryReader): ExtensionTarget {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const root = decodeGlobalSymbolId(reader);
            const ty = decodeGlobalTypeId(reader);

            return {
                kind: "nominal",
                root,
                ty,
            };
        }
        case 1: {
            const ty = decodeGlobalTypeId(reader);

            return {
                kind: "blanket",
                ty,
            };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one ExtensionTarget. */
export function toJsonExtensionTarget(value: ExtensionTarget): Json {
    switch (value.kind) {
        case "nominal":
            return {
                kind: "nominal",
                root: toJsonGlobalSymbolId(value.root),
                ty: toJsonGlobalTypeId(value.ty),
            };
        case "blanket":
            return {
                kind: "blanket",
                ty: toJsonGlobalTypeId(value.ty),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one ExtensionTarget from one JSON value. */
export function fromJsonExtensionTarget(value: Json): ExtensionTarget {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "nominal":
            return {
                kind,
                root: fromJsonGlobalSymbolId(jsonField(object, "root")),
                ty: fromJsonGlobalTypeId(jsonField(object, "ty")),
            };
        case "blanket":
            return {
                kind,
                ty: fromJsonGlobalTypeId(jsonField(object, "ty")),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** A checked where clause attached to one extension. */
export type ExtensionWhereClause = {
    /** The source where clause node. */
    readonly source: GlobalNodeIdAny;
    /** The constrained type. */
    readonly left: GlobalTypeId;
    /** The required constraint type. */
    readonly right: GlobalTypeId;
};

export const ExtensionWhereClause = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ExtensionWhereClause): void {
        encodeExtensionWhereClause(writer, value);
    },

    /** Decode one ExtensionWhereClause. */
    decode(reader: BinaryReader): ExtensionWhereClause {
        return decodeExtensionWhereClause(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ExtensionWhereClause): Json {
        return toJsonExtensionWhereClause(value);
    },

    /** Return one ExtensionWhereClause from one JSON value. */
    fromJson(value: Json): ExtensionWhereClause {
        return fromJsonExtensionWhereClause(value);
    },
};

/** Encode one ExtensionWhereClause. */
export function encodeExtensionWhereClause(writer: BinaryWriter, value: ExtensionWhereClause): void {
    encodeGlobalNodeIdAny(writer, value.source);
    encodeGlobalTypeId(writer, value.left);
    encodeGlobalTypeId(writer, value.right);
}

/** Decode one ExtensionWhereClause. */
export function decodeExtensionWhereClause(reader: BinaryReader): ExtensionWhereClause {
    const source = decodeGlobalNodeIdAny(reader);
    const left = decodeGlobalTypeId(reader);
    const right = decodeGlobalTypeId(reader);

    return {
        source,
        left,
        right,
    };
}

/** Return one JSON value for one ExtensionWhereClause. */
export function toJsonExtensionWhereClause(value: ExtensionWhereClause): Json {
    return {
        source: toJsonGlobalNodeIdAny(value.source),
        left: toJsonGlobalTypeId(value.left),
        right: toJsonGlobalTypeId(value.right),
    };
}

/** Return one ExtensionWhereClause from one JSON value. */
export function fromJsonExtensionWhereClause(value: Json): ExtensionWhereClause {
    const object = jsonObject(value);

    return {
        source: fromJsonGlobalNodeIdAny(jsonField(object, "source")),
        left: fromJsonGlobalTypeId(jsonField(object, "left")),
        right: fromJsonGlobalTypeId(jsonField(object, "right")),
    };
}
