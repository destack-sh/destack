/* generated by destack_bridge_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, compareBytes, jsonArray, jsonField, jsonObject, nestedBytes } from "../../protocol/serde.js";
import type { ComponentId } from "../source/file/model/component.js";
import type { ModuleId } from "../source/file/model/module.js";
import type { ProfileId } from "../source/file/model/profile.js";
import { decodeComponentId, encodeComponentId, fromJsonComponentId, toJsonComponentId } from "../source/file/model/component.js";
import { decodeModuleId, encodeModuleId, fromJsonModuleId, toJsonModuleId } from "../source/file/model/module.js";
import { decodeProfileId, encodeProfileId, fromJsonProfileId, toJsonProfileId } from "../source/file/model/profile.js";

/** Strongly connected component partition of one profile's module graph. */
export type ComponentGraph = {
    /** The profile this partition belongs to. */
    readonly profile: ProfileId;
    /** Imported modules per module, deduplicated in import order. */
    readonly imports: ReadonlyMap<ModuleId, ReadonlyArray<ModuleId>>;
    /** Owning component per module. */
    readonly componentOf: ReadonlyMap<ModuleId, ComponentId>;
    /** Member modules per component, sorted, with the entry first. */
    readonly members: ReadonlyMap<ComponentId, ReadonlyArray<ModuleId>>;
    /** External components each component depends on. */
    readonly dependencies: ReadonlyMap<ComponentId, ReadonlyArray<ComponentId>>;
};

export const ComponentGraph = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ComponentGraph): void {
        encodeComponentGraph(writer, value);
    },

    /** Decode one ComponentGraph. */
    decode(reader: BinaryReader): ComponentGraph {
        return decodeComponentGraph(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ComponentGraph): Json {
        return toJsonComponentGraph(value);
    },

    /** Return one ComponentGraph from one JSON value. */
    fromJson(value: Json): ComponentGraph {
        return fromJsonComponentGraph(value);
    },
};

/** Encode one ComponentGraph. */
export function encodeComponentGraph(writer: BinaryWriter, value: ComponentGraph): void {
    encodeProfileId(writer, value.profile);
    const entries1 = Array.from(value.imports.entries()).map(([key1, item1]) => {
        const keyBytes = nestedBytes((writer) => {
            encodeModuleId(writer, key1);
        });
        return { key1, item1, keyBytes };
    });
    entries1.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries1.length);
    for (const entry1 of entries1) {
        encodeModuleId(writer, entry1.key1);
        writer.writeUnsigned(entry1.item1.length);
        for (const item2 of entry1.item1) {
            encodeModuleId(writer, item2);
        }
    }
    const entries2 = Array.from(value.componentOf.entries()).map(([key2, item2]) => {
        const keyBytes = nestedBytes((writer) => {
            encodeModuleId(writer, key2);
        });
        return { key2, item2, keyBytes };
    });
    entries2.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries2.length);
    for (const entry2 of entries2) {
        encodeModuleId(writer, entry2.key2);
        encodeComponentId(writer, entry2.item2);
    }
    const entries3 = Array.from(value.members.entries()).map(([key3, item3]) => {
        const keyBytes = nestedBytes((writer) => {
            encodeComponentId(writer, key3);
        });
        return { key3, item3, keyBytes };
    });
    entries3.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries3.length);
    for (const entry3 of entries3) {
        encodeComponentId(writer, entry3.key3);
        writer.writeUnsigned(entry3.item3.length);
        for (const item4 of entry3.item3) {
            encodeModuleId(writer, item4);
        }
    }
    const entries4 = Array.from(value.dependencies.entries()).map(([key4, item4]) => {
        const keyBytes = nestedBytes((writer) => {
            encodeComponentId(writer, key4);
        });
        return { key4, item4, keyBytes };
    });
    entries4.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries4.length);
    for (const entry4 of entries4) {
        encodeComponentId(writer, entry4.key4);
        writer.writeUnsigned(entry4.item4.length);
        for (const item5 of entry4.item4) {
            encodeComponentId(writer, item5);
        }
    }
}

/** Decode one ComponentGraph. */
export function decodeComponentGraph(reader: BinaryReader): ComponentGraph {
    const profile = decodeProfileId(reader);
    const imports = (() => { const length1 = reader.readNumber(); const items1 = new Map<ModuleId, ReadonlyArray<ModuleId>>(); for (let index = 0; index < length1; index += 1) { items1.set(decodeModuleId(reader), (() => { const length3 = reader.readNumber(); const items3: Array<ModuleId> = []; for (let index = 0; index < length3; index += 1) { items3.push(decodeModuleId(reader)); } return items3; })()); } return items1; })();
    const componentOf = (() => { const length2 = reader.readNumber(); const items2 = new Map<ModuleId, ComponentId>(); for (let index = 0; index < length2; index += 1) { items2.set(decodeModuleId(reader), decodeComponentId(reader)); } return items2; })();
    const members = (() => { const length3 = reader.readNumber(); const items3 = new Map<ComponentId, ReadonlyArray<ModuleId>>(); for (let index = 0; index < length3; index += 1) { items3.set(decodeComponentId(reader), (() => { const length5 = reader.readNumber(); const items5: Array<ModuleId> = []; for (let index = 0; index < length5; index += 1) { items5.push(decodeModuleId(reader)); } return items5; })()); } return items3; })();
    const dependencies = (() => { const length4 = reader.readNumber(); const items4 = new Map<ComponentId, ReadonlyArray<ComponentId>>(); for (let index = 0; index < length4; index += 1) { items4.set(decodeComponentId(reader), (() => { const length6 = reader.readNumber(); const items6: Array<ComponentId> = []; for (let index = 0; index < length6; index += 1) { items6.push(decodeComponentId(reader)); } return items6; })()); } return items4; })();

    return {
        profile,
        imports,
        componentOf,
        members,
        dependencies,
    };
}

/** Return one JSON value for one ComponentGraph. */
export function toJsonComponentGraph(value: ComponentGraph): Json {
    return {
        profile: toJsonProfileId(value.profile),
        imports: Array.from(value.imports.entries()).map(([key0, item0]) => [toJsonModuleId(key0), item0.map((item1) => toJsonModuleId(item1))] as const),
        componentOf: Array.from(value.componentOf.entries()).map(([key0, item0]) => [toJsonModuleId(key0), toJsonComponentId(item0)] as const),
        members: Array.from(value.members.entries()).map(([key0, item0]) => [toJsonComponentId(key0), item0.map((item1) => toJsonModuleId(item1))] as const),
        dependencies: Array.from(value.dependencies.entries()).map(([key0, item0]) => [toJsonComponentId(key0), item0.map((item1) => toJsonComponentId(item1))] as const),
    };
}

/** Return one ComponentGraph from one JSON value. */
export function fromJsonComponentGraph(value: Json): ComponentGraph {
    const object = jsonObject(value);

    return {
        profile: fromJsonProfileId(jsonField(object, "profile")),
        imports: new Map(jsonArray(jsonField(object, "imports")).map((entry) => { const items = jsonArray(entry); if (items.length !== 2) { throw new SerdeError(`expected JSON map entry length 2: ${items.length}`); } const key0 = items[0]; const item0 = items[1]; return [fromJsonModuleId(key0), jsonArray(item0).map((item1) => fromJsonModuleId(item1))] as const; })),
        componentOf: new Map(jsonArray(jsonField(object, "componentOf")).map((entry) => { const items = jsonArray(entry); if (items.length !== 2) { throw new SerdeError(`expected JSON map entry length 2: ${items.length}`); } const key0 = items[0]; const item0 = items[1]; return [fromJsonModuleId(key0), fromJsonComponentId(item0)] as const; })),
        members: new Map(jsonArray(jsonField(object, "members")).map((entry) => { const items = jsonArray(entry); if (items.length !== 2) { throw new SerdeError(`expected JSON map entry length 2: ${items.length}`); } const key0 = items[0]; const item0 = items[1]; return [fromJsonComponentId(key0), jsonArray(item0).map((item1) => fromJsonModuleId(item1))] as const; })),
        dependencies: new Map(jsonArray(jsonField(object, "dependencies")).map((entry) => { const items = jsonArray(entry); if (items.length !== 2) { throw new SerdeError(`expected JSON map entry length 2: ${items.length}`); } const key0 = items[0]; const item0 = items[1]; return [fromJsonComponentId(key0), jsonArray(item0).map((item1) => fromJsonComponentId(item1))] as const; })),
    };
}
