/* generated by destack_bridge_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, compareBytes, jsonArray, jsonField, jsonObject, jsonString, nestedBytes } from "../../protocol/serde.js";
import type { StringId } from "../core/string.js";
import type { StaticKey } from "../dir/symbol/key.js";
import type { LanguageItem } from "../dir/symbol/language.js";
import type { GlobalSymbolId } from "../dir/symbol/symbol.js";
import type { ImportTarget } from "../dir/table/import.js";
import type { ModuleId } from "../source/file/model/module.js";
import { decodeStringId, encodeStringId, fromJsonStringId, toJsonStringId } from "../core/string.js";
import { decodeStaticKey, encodeStaticKey, fromJsonStaticKey, toJsonStaticKey } from "../dir/symbol/key.js";
import { decodeLanguageItem, encodeLanguageItem, fromJsonLanguageItem, toJsonLanguageItem } from "../dir/symbol/language.js";
import { decodeGlobalSymbolId, encodeGlobalSymbolId, fromJsonGlobalSymbolId, toJsonGlobalSymbolId } from "../dir/symbol/symbol.js";
import { decodeImportTarget, encodeImportTarget, fromJsonImportTarget, toJsonImportTarget } from "../dir/table/import.js";
import { decodeModuleId, encodeModuleId, fromJsonModuleId, toJsonModuleId } from "../source/file/model/module.js";

/** Compiler-known language environment for one profile. */
export type LanguageEnvironment = {
    /** Language item symbols by item id. */
    readonly symbolByItem: ReadonlyMap<LanguageItem, GlobalSymbolId>;
    /** Language items by symbol id. */
    readonly itemsBySymbol: ReadonlyMap<GlobalSymbolId, LanguageItem>;
    /** Builtin symbols by export name. */
    readonly symbols: ReadonlyMap<StringId, GlobalSymbolId>;
};

export const LanguageEnvironment = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: LanguageEnvironment): void {
        encodeLanguageEnvironment(writer, value);
    },

    /** Decode one LanguageEnvironment. */
    decode(reader: BinaryReader): LanguageEnvironment {
        return decodeLanguageEnvironment(reader);
    },

    /** Return this value as JSON. */
    toJson(value: LanguageEnvironment): Json {
        return toJsonLanguageEnvironment(value);
    },

    /** Return one LanguageEnvironment from one JSON value. */
    fromJson(value: Json): LanguageEnvironment {
        return fromJsonLanguageEnvironment(value);
    },
};

/** Encode one LanguageEnvironment. */
export function encodeLanguageEnvironment(writer: BinaryWriter, value: LanguageEnvironment): void {
    const entries0 = Array.from(value.symbolByItem.entries()).map(([key0, item0]) => {
        const keyBytes = nestedBytes((writer) => {
            encodeLanguageItem(writer, key0);
        });
        return { key0, item0, keyBytes };
    });
    entries0.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries0.length);
    for (const entry0 of entries0) {
        encodeLanguageItem(writer, entry0.key0);
        encodeGlobalSymbolId(writer, entry0.item0);
    }
    const entries1 = Array.from(value.itemsBySymbol.entries()).map(([key1, item1]) => {
        const keyBytes = nestedBytes((writer) => {
            encodeGlobalSymbolId(writer, key1);
        });
        return { key1, item1, keyBytes };
    });
    entries1.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries1.length);
    for (const entry1 of entries1) {
        encodeGlobalSymbolId(writer, entry1.key1);
        encodeLanguageItem(writer, entry1.item1);
    }
    const entries2 = Array.from(value.symbols.entries()).map(([key2, item2]) => {
        const keyBytes = nestedBytes((writer) => {
            encodeStringId(writer, key2);
        });
        return { key2, item2, keyBytes };
    });
    entries2.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries2.length);
    for (const entry2 of entries2) {
        encodeStringId(writer, entry2.key2);
        encodeGlobalSymbolId(writer, entry2.item2);
    }
}

/** Decode one LanguageEnvironment. */
export function decodeLanguageEnvironment(reader: BinaryReader): LanguageEnvironment {
    const symbolByItem = (() => { const length0 = reader.readNumber(); const items0 = new Map<LanguageItem, GlobalSymbolId>(); for (let index = 0; index < length0; index += 1) { items0.set(decodeLanguageItem(reader), decodeGlobalSymbolId(reader)); } return items0; })();
    const itemsBySymbol = (() => { const length1 = reader.readNumber(); const items1 = new Map<GlobalSymbolId, LanguageItem>(); for (let index = 0; index < length1; index += 1) { items1.set(decodeGlobalSymbolId(reader), decodeLanguageItem(reader)); } return items1; })();
    const symbols = (() => { const length2 = reader.readNumber(); const items2 = new Map<StringId, GlobalSymbolId>(); for (let index = 0; index < length2; index += 1) { items2.set(decodeStringId(reader), decodeGlobalSymbolId(reader)); } return items2; })();

    return {
        symbolByItem,
        itemsBySymbol,
        symbols,
    };
}

/** Return one JSON value for one LanguageEnvironment. */
export function toJsonLanguageEnvironment(value: LanguageEnvironment): Json {
    return {
        symbolByItem: Array.from(value.symbolByItem.entries()).map(([key0, item0]) => [toJsonLanguageItem(key0), toJsonGlobalSymbolId(item0)] as const),
        itemsBySymbol: Array.from(value.itemsBySymbol.entries()).map(([key0, item0]) => [toJsonGlobalSymbolId(key0), toJsonLanguageItem(item0)] as const),
        symbols: Array.from(value.symbols.entries()).map(([key0, item0]) => [toJsonStringId(key0), toJsonGlobalSymbolId(item0)] as const),
    };
}

/** Return one LanguageEnvironment from one JSON value. */
export function fromJsonLanguageEnvironment(value: Json): LanguageEnvironment {
    const object = jsonObject(value);

    return {
        symbolByItem: new Map(jsonArray(jsonField(object, "symbolByItem")).map((entry) => { const items = jsonArray(entry); if (items.length !== 2) { throw new SerdeError(`expected JSON map entry length 2: ${items.length}`); } const key0 = items[0]; const item0 = items[1]; return [fromJsonLanguageItem(key0), fromJsonGlobalSymbolId(item0)] as const; })),
        itemsBySymbol: new Map(jsonArray(jsonField(object, "itemsBySymbol")).map((entry) => { const items = jsonArray(entry); if (items.length !== 2) { throw new SerdeError(`expected JSON map entry length 2: ${items.length}`); } const key0 = items[0]; const item0 = items[1]; return [fromJsonGlobalSymbolId(key0), fromJsonLanguageItem(item0)] as const; })),
        symbols: new Map(jsonArray(jsonField(object, "symbols")).map((entry) => { const items = jsonArray(entry); if (items.length !== 2) { throw new SerdeError(`expected JSON map entry length 2: ${items.length}`); } const key0 = items[0]; const item0 = items[1]; return [fromJsonStringId(key0), fromJsonGlobalSymbolId(item0)] as const; })),
    };
}

/** Explicit global environment selected for one profile. */
export type GlobalEnvironment = {
    /** Compiler-known language environment. */
    readonly language: LanguageEnvironment;
    /** Explicit global modules in load order. */
    readonly globals: ReadonlyArray<ModuleId>;
    /** Resolved global bindings by key across the global modules. */
    readonly globalTargetsByKey: ReadonlyMap<StaticKey, ReadonlyArray<ImportTarget>>;
};

export const GlobalEnvironment = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: GlobalEnvironment): void {
        encodeGlobalEnvironment(writer, value);
    },

    /** Decode one GlobalEnvironment. */
    decode(reader: BinaryReader): GlobalEnvironment {
        return decodeGlobalEnvironment(reader);
    },

    /** Return this value as JSON. */
    toJson(value: GlobalEnvironment): Json {
        return toJsonGlobalEnvironment(value);
    },

    /** Return one GlobalEnvironment from one JSON value. */
    fromJson(value: Json): GlobalEnvironment {
        return fromJsonGlobalEnvironment(value);
    },
};

/** Encode one GlobalEnvironment. */
export function encodeGlobalEnvironment(writer: BinaryWriter, value: GlobalEnvironment): void {
    encodeLanguageEnvironment(writer, value.language);
    writer.writeUnsigned(value.globals.length);
    for (const item1 of value.globals) {
        encodeModuleId(writer, item1);
    }
    const entries2 = Array.from(value.globalTargetsByKey.entries()).map(([key2, item2]) => {
        const keyBytes = nestedBytes((writer) => {
            encodeStaticKey(writer, key2);
        });
        return { key2, item2, keyBytes };
    });
    entries2.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries2.length);
    for (const entry2 of entries2) {
        encodeStaticKey(writer, entry2.key2);
        writer.writeUnsigned(entry2.item2.length);
        for (const item3 of entry2.item2) {
            encodeImportTarget(writer, item3);
        }
    }
}

/** Decode one GlobalEnvironment. */
export function decodeGlobalEnvironment(reader: BinaryReader): GlobalEnvironment {
    const language = decodeLanguageEnvironment(reader);
    const globals = (() => { const length1 = reader.readNumber(); const items1: Array<ModuleId> = []; for (let index = 0; index < length1; index += 1) { items1.push(decodeModuleId(reader)); } return items1; })();
    const globalTargetsByKey = (() => { const length2 = reader.readNumber(); const items2 = new Map<StaticKey, ReadonlyArray<ImportTarget>>(); for (let index = 0; index < length2; index += 1) { items2.set(decodeStaticKey(reader), (() => { const length4 = reader.readNumber(); const items4: Array<ImportTarget> = []; for (let index = 0; index < length4; index += 1) { items4.push(decodeImportTarget(reader)); } return items4; })()); } return items2; })();

    return {
        language,
        globals,
        globalTargetsByKey,
    };
}

/** Return one JSON value for one GlobalEnvironment. */
export function toJsonGlobalEnvironment(value: GlobalEnvironment): Json {
    return {
        language: toJsonLanguageEnvironment(value.language),
        globals: value.globals.map((item0) => toJsonModuleId(item0)),
        globalTargetsByKey: Array.from(value.globalTargetsByKey.entries()).map(([key0, item0]) => [toJsonStaticKey(key0), item0.map((item1) => toJsonImportTarget(item1))] as const),
    };
}

/** Return one GlobalEnvironment from one JSON value. */
export function fromJsonGlobalEnvironment(value: Json): GlobalEnvironment {
    const object = jsonObject(value);

    return {
        language: fromJsonLanguageEnvironment(jsonField(object, "language")),
        globals: jsonArray(jsonField(object, "globals")).map((item0) => fromJsonModuleId(item0)),
        globalTargetsByKey: new Map(jsonArray(jsonField(object, "globalTargetsByKey")).map((entry) => { const items = jsonArray(entry); if (items.length !== 2) { throw new SerdeError(`expected JSON map entry length 2: ${items.length}`); } const key0 = items[0]; const item0 = items[1]; return [fromJsonStaticKey(key0), jsonArray(item0).map((item1) => fromJsonImportTarget(item1))] as const; })),
    };
}

/** Resolved compiler-known intrinsic bindings for a profile. */
export type LanguageIntrinsics = {
    /** Intrinsic names keyed by symbol id. */
    readonly namesBySymbol: ReadonlyMap<GlobalSymbolId, string>;
    /** Intrinsic symbols keyed by name. */
    readonly symbolsByName: Readonly<Record<string, GlobalSymbolId>>;
};

export const LanguageIntrinsics = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: LanguageIntrinsics): void {
        encodeLanguageIntrinsics(writer, value);
    },

    /** Decode one LanguageIntrinsics. */
    decode(reader: BinaryReader): LanguageIntrinsics {
        return decodeLanguageIntrinsics(reader);
    },

    /** Return this value as JSON. */
    toJson(value: LanguageIntrinsics): Json {
        return toJsonLanguageIntrinsics(value);
    },

    /** Return one LanguageIntrinsics from one JSON value. */
    fromJson(value: Json): LanguageIntrinsics {
        return fromJsonLanguageIntrinsics(value);
    },
};

/** Encode one LanguageIntrinsics. */
export function encodeLanguageIntrinsics(writer: BinaryWriter, value: LanguageIntrinsics): void {
    const entries0 = Array.from(value.namesBySymbol.entries()).map(([key0, item0]) => {
        const keyBytes = nestedBytes((writer) => {
            encodeGlobalSymbolId(writer, key0);
        });
        return { key0, item0, keyBytes };
    });
    entries0.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries0.length);
    for (const entry0 of entries0) {
        encodeGlobalSymbolId(writer, entry0.key0);
        writer.writeString(entry0.item0);
    }
    const entries1 = Object.entries(value.symbolsByName).map(([key1, item1]) => {
        const keyBytes = nestedBytes((writer) => {
            writer.writeString(key1);
        });
        return { key1, item1, keyBytes };
    });
    entries1.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries1.length);
    for (const entry1 of entries1) {
        writer.writeString(entry1.key1);
        encodeGlobalSymbolId(writer, entry1.item1);
    }
}

/** Decode one LanguageIntrinsics. */
export function decodeLanguageIntrinsics(reader: BinaryReader): LanguageIntrinsics {
    const namesBySymbol = (() => { const length0 = reader.readNumber(); const items0 = new Map<GlobalSymbolId, string>(); for (let index = 0; index < length0; index += 1) { items0.set(decodeGlobalSymbolId(reader), reader.readString()); } return items0; })();
    const symbolsByName = (() => { const length1 = reader.readNumber(); const items1: Record<string, GlobalSymbolId> = {}; for (let index = 0; index < length1; index += 1) { const key = reader.readString(); items1[key] = decodeGlobalSymbolId(reader); } return items1; })();

    return {
        namesBySymbol,
        symbolsByName,
    };
}

/** Return one JSON value for one LanguageIntrinsics. */
export function toJsonLanguageIntrinsics(value: LanguageIntrinsics): Json {
    return {
        namesBySymbol: Array.from(value.namesBySymbol.entries()).map(([key0, item0]) => [toJsonGlobalSymbolId(key0), item0] as const),
        symbolsByName: Object.fromEntries(Object.entries(value.symbolsByName).map(([key0, item0]) => [key0, toJsonGlobalSymbolId(item0)] as const)),
    };
}

/** Return one LanguageIntrinsics from one JSON value. */
export function fromJsonLanguageIntrinsics(value: Json): LanguageIntrinsics {
    const object = jsonObject(value);

    return {
        namesBySymbol: new Map(jsonArray(jsonField(object, "namesBySymbol")).map((entry) => { const items = jsonArray(entry); if (items.length !== 2) { throw new SerdeError(`expected JSON map entry length 2: ${items.length}`); } const key0 = items[0]; const item0 = items[1]; return [fromJsonGlobalSymbolId(key0), jsonString(item0)] as const; })),
        symbolsByName: Object.fromEntries(Object.entries(jsonObject(jsonField(object, "symbolsByName"))).map(([key0, item0]) => [key0, fromJsonGlobalSymbolId(item0)] as const)),
    };
}
