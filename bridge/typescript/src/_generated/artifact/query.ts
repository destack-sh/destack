/* generated by destack_bridge_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, jsonArray, jsonField, jsonObject } from "../../protocol/serde.js";
import type { ArtifactVersion } from "./core/version.js";
import type { QueryIndex } from "../qir/index/index.js";
import { decodeArtifactVersion, encodeArtifactVersion, fromJsonArtifactVersion, toJsonArtifactVersion } from "./core/version.js";
import { decodeQueryIndex, encodeQueryIndex, fromJsonQueryIndex, toJsonQueryIndex } from "../qir/index/index.js";

/** Query index for one module profile. */
export type ModuleQueryIndex = {
    /** The indexed query surface. */
    readonly index: QueryIndex;
};

export const ModuleQueryIndex = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ModuleQueryIndex): void {
        encodeModuleQueryIndex(writer, value);
    },

    /** Decode one ModuleQueryIndex. */
    decode(reader: BinaryReader): ModuleQueryIndex {
        return decodeModuleQueryIndex(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ModuleQueryIndex): Json {
        return toJsonModuleQueryIndex(value);
    },

    /** Return one ModuleQueryIndex from one JSON value. */
    fromJson(value: Json): ModuleQueryIndex {
        return fromJsonModuleQueryIndex(value);
    },
};

/** Encode one ModuleQueryIndex. */
export function encodeModuleQueryIndex(writer: BinaryWriter, value: ModuleQueryIndex): void {
    encodeQueryIndex(writer, value.index);
}

/** Decode one ModuleQueryIndex. */
export function decodeModuleQueryIndex(reader: BinaryReader): ModuleQueryIndex {
    const index = decodeQueryIndex(reader);

    return {
        index,
    };
}

/** Return one JSON value for one ModuleQueryIndex. */
export function toJsonModuleQueryIndex(value: ModuleQueryIndex): Json {
    return {
        index: toJsonQueryIndex(value.index),
    };
}

/** Return one ModuleQueryIndex from one JSON value. */
export function fromJsonModuleQueryIndex(value: Json): ModuleQueryIndex {
    const object = jsonObject(value);

    return {
        index: fromJsonQueryIndex(jsonField(object, "index")),
    };
}

/** Query index for one workspace profile. */
export type WorkspaceQueryIndex = {
    /** The module query indexes in this workspace profile. */
    readonly modules: ReadonlyArray<ArtifactVersion>;
};

export const WorkspaceQueryIndex = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: WorkspaceQueryIndex): void {
        encodeWorkspaceQueryIndex(writer, value);
    },

    /** Decode one WorkspaceQueryIndex. */
    decode(reader: BinaryReader): WorkspaceQueryIndex {
        return decodeWorkspaceQueryIndex(reader);
    },

    /** Return this value as JSON. */
    toJson(value: WorkspaceQueryIndex): Json {
        return toJsonWorkspaceQueryIndex(value);
    },

    /** Return one WorkspaceQueryIndex from one JSON value. */
    fromJson(value: Json): WorkspaceQueryIndex {
        return fromJsonWorkspaceQueryIndex(value);
    },
};

/** Encode one WorkspaceQueryIndex. */
export function encodeWorkspaceQueryIndex(writer: BinaryWriter, value: WorkspaceQueryIndex): void {
    writer.writeUnsigned(value.modules.length);
    for (const item0 of value.modules) {
        encodeArtifactVersion(writer, item0);
    }
}

/** Decode one WorkspaceQueryIndex. */
export function decodeWorkspaceQueryIndex(reader: BinaryReader): WorkspaceQueryIndex {
    const modules = (() => { const length0 = reader.readNumber(); const items0: Array<ArtifactVersion> = []; for (let index = 0; index < length0; index += 1) { items0.push(decodeArtifactVersion(reader)); } return items0; })();

    return {
        modules,
    };
}

/** Return one JSON value for one WorkspaceQueryIndex. */
export function toJsonWorkspaceQueryIndex(value: WorkspaceQueryIndex): Json {
    return {
        modules: value.modules.map((item0) => toJsonArtifactVersion(item0)),
    };
}

/** Return one WorkspaceQueryIndex from one JSON value. */
export function fromJsonWorkspaceQueryIndex(value: Json): WorkspaceQueryIndex {
    const object = jsonObject(value);

    return {
        modules: jsonArray(jsonField(object, "modules")).map((item0) => fromJsonArtifactVersion(item0)),
    };
}
