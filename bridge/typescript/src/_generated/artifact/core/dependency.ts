/* generated by destack_bridge_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, jsonBigint, jsonField, jsonObject, jsonString } from "../../../protocol/serde.js";
import type { ArtifactKey } from "./key.js";
import type { ArtifactVersion } from "./version.js";
import type { ComponentId } from "../../source/file/model/component.js";
import type { ContentId } from "../../source/file/model/file.js";
import type { FileId } from "../../source/file/model/file.js";
import type { ModuleId } from "../../source/file/model/module.js";
import { decodeArtifactKey, encodeArtifactKey, fromJsonArtifactKey, toJsonArtifactKey } from "./key.js";
import { decodeArtifactVersion, encodeArtifactVersion, fromJsonArtifactVersion, toJsonArtifactVersion } from "./version.js";
import { decodeComponentId, encodeComponentId, fromJsonComponentId, toJsonComponentId } from "../../source/file/model/component.js";
import { decodeContentId, encodeContentId, fromJsonContentId, toJsonContentId } from "../../source/file/model/file.js";
import { decodeFileId, encodeFileId, fromJsonFileId, toJsonFileId } from "../../source/file/model/file.js";
import { decodeModuleId, encodeModuleId, fromJsonModuleId, toJsonModuleId } from "../../source/file/model/module.js";

/** One exact projected artifact value observed while building an artifact. */
export type ArtifactProjectionDependency = {
    /** The exact artifact version that supplied the projected value. */
    readonly version: ArtifactVersion;
    /** The projected artifact value. */
    readonly projection: ArtifactProjection;
    /** The exact projection fingerprint read from the owner artifact. */
    readonly fingerprint: ArtifactProjectionFingerprint;
};

export const ArtifactProjectionDependency = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ArtifactProjectionDependency): void {
        encodeArtifactProjectionDependency(writer, value);
    },

    /** Decode one ArtifactProjectionDependency. */
    decode(reader: BinaryReader): ArtifactProjectionDependency {
        return decodeArtifactProjectionDependency(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ArtifactProjectionDependency): Json {
        return toJsonArtifactProjectionDependency(value);
    },

    /** Return one ArtifactProjectionDependency from one JSON value. */
    fromJson(value: Json): ArtifactProjectionDependency {
        return fromJsonArtifactProjectionDependency(value);
    },
};

/** Encode one ArtifactProjectionDependency. */
export function encodeArtifactProjectionDependency(writer: BinaryWriter, value: ArtifactProjectionDependency): void {
    encodeArtifactVersion(writer, value.version);
    encodeArtifactProjection(writer, value.projection);
    encodeArtifactProjectionFingerprint(writer, value.fingerprint);
}

/** Decode one ArtifactProjectionDependency. */
export function decodeArtifactProjectionDependency(reader: BinaryReader): ArtifactProjectionDependency {
    const version = decodeArtifactVersion(reader);
    const projection = decodeArtifactProjection(reader);
    const fingerprint = decodeArtifactProjectionFingerprint(reader);

    return {
        version,
        projection,
        fingerprint,
    };
}

/** Return one JSON value for one ArtifactProjectionDependency. */
export function toJsonArtifactProjectionDependency(value: ArtifactProjectionDependency): Json {
    return {
        version: toJsonArtifactVersion(value.version),
        projection: toJsonArtifactProjection(value.projection),
        fingerprint: toJsonArtifactProjectionFingerprint(value.fingerprint),
    };
}

/** Return one ArtifactProjectionDependency from one JSON value. */
export function fromJsonArtifactProjectionDependency(value: Json): ArtifactProjectionDependency {
    const object = jsonObject(value);

    return {
        version: fromJsonArtifactVersion(jsonField(object, "version")),
        projection: fromJsonArtifactProjection(jsonField(object, "projection")),
        fingerprint: fromJsonArtifactProjectionFingerprint(jsonField(object, "fingerprint")),
    };
}

/** One artifact projection selected by owner artifact and projection key. */
export type ArtifactProjection = {
    /** The artifact that owns the projected payload. */
    readonly artifact: ArtifactKey;
    /** The projected value inside the owning artifact. */
    readonly key: ArtifactProjectionKey;
};

export const ArtifactProjection = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ArtifactProjection): void {
        encodeArtifactProjection(writer, value);
    },

    /** Decode one ArtifactProjection. */
    decode(reader: BinaryReader): ArtifactProjection {
        return decodeArtifactProjection(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ArtifactProjection): Json {
        return toJsonArtifactProjection(value);
    },

    /** Return one ArtifactProjection from one JSON value. */
    fromJson(value: Json): ArtifactProjection {
        return fromJsonArtifactProjection(value);
    },
};

/** Encode one ArtifactProjection. */
export function encodeArtifactProjection(writer: BinaryWriter, value: ArtifactProjection): void {
    encodeArtifactKey(writer, value.artifact);
    encodeArtifactProjectionKey(writer, value.key);
}

/** Decode one ArtifactProjection. */
export function decodeArtifactProjection(reader: BinaryReader): ArtifactProjection {
    const artifact = decodeArtifactKey(reader);
    const key = decodeArtifactProjectionKey(reader);

    return {
        artifact,
        key,
    };
}

/** Return one JSON value for one ArtifactProjection. */
export function toJsonArtifactProjection(value: ArtifactProjection): Json {
    return {
        artifact: toJsonArtifactKey(value.artifact),
        key: toJsonArtifactProjectionKey(value.key),
    };
}

/** Return one ArtifactProjection from one JSON value. */
export function fromJsonArtifactProjection(value: Json): ArtifactProjection {
    const object = jsonObject(value);

    return {
        artifact: fromJsonArtifactKey(jsonField(object, "artifact")),
        key: fromJsonArtifactProjectionKey(jsonField(object, "key")),
    };
}

/** One observable projection of an artifact payload. */
export type ArtifactProjectionKey =
    /** A component graph projection. */
    | {
          readonly kind: "componentGraph";
          readonly component_graph: ComponentGraphProjection;
      }
    /** A checked DIR module inside a checked component. */
    | {
          readonly kind: "dirChecked";
          readonly dir_checked: ModuleId;
      }
;

export const ArtifactProjectionKey = {
    /** A component graph projection. */
    componentGraph(component_graph: ComponentGraphProjection): ArtifactProjectionKey {
        return { kind: "componentGraph", component_graph };
    },

    /** A checked DIR module inside a checked component. */
    dirChecked(dir_checked: ModuleId): ArtifactProjectionKey {
        return { kind: "dirChecked", dir_checked };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: ArtifactProjectionKey): void {
        encodeArtifactProjectionKey(writer, value);
    },

    /** Decode one ArtifactProjectionKey. */
    decode(reader: BinaryReader): ArtifactProjectionKey {
        return decodeArtifactProjectionKey(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ArtifactProjectionKey): Json {
        return toJsonArtifactProjectionKey(value);
    },

    /** Return one ArtifactProjectionKey from one JSON value. */
    fromJson(value: Json): ArtifactProjectionKey {
        return fromJsonArtifactProjectionKey(value);
    },
};

/** Encode one ArtifactProjectionKey. */
export function encodeArtifactProjectionKey(writer: BinaryWriter, value: ArtifactProjectionKey): void {
    switch (value.kind) {
        case "componentGraph":
            writer.writeUnsigned(0);
            encodeComponentGraphProjection(writer, value.component_graph);
            return;
        case "dirChecked":
            writer.writeUnsigned(1);
            encodeModuleId(writer, value.dir_checked);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one ArtifactProjectionKey. */
export function decodeArtifactProjectionKey(reader: BinaryReader): ArtifactProjectionKey {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const component_graph = decodeComponentGraphProjection(reader);

            return { kind: "componentGraph", component_graph };
        }
        case 1: {
            const dir_checked = decodeModuleId(reader);

            return { kind: "dirChecked", dir_checked };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one ArtifactProjectionKey. */
export function toJsonArtifactProjectionKey(value: ArtifactProjectionKey): Json {
    switch (value.kind) {
        case "componentGraph":
            return {
                kind: "componentGraph",
                component_graph: toJsonComponentGraphProjection(value.component_graph),
            };
        case "dirChecked":
            return {
                kind: "dirChecked",
                dir_checked: toJsonModuleId(value.dir_checked),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one ArtifactProjectionKey from one JSON value. */
export function fromJsonArtifactProjectionKey(value: Json): ArtifactProjectionKey {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "componentGraph":
            return {
                kind,
                component_graph: fromJsonComponentGraphProjection(jsonField(object, "component_graph")),
            };
        case "dirChecked":
            return {
                kind,
                dir_checked: fromJsonModuleId(jsonField(object, "dir_checked")),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** One observable projection of a component graph artifact. */
export type ComponentGraphProjection =
    /** The component containing one module. */
    | {
          readonly kind: "componentOf";
          readonly component_of: ModuleId;
      }
    /** The component and entry containing one module. */
    | {
          readonly kind: "componentEntryOf";
          readonly component_entry_of: ModuleId;
      }
    /** The sorted modules belonging to one component. */
    | {
          readonly kind: "members";
          readonly members: ComponentId;
      }
    /** The direct external components one component depends on. */
    | {
          readonly kind: "dependencies";
          readonly dependencies: ComponentId;
      }
;

export const ComponentGraphProjection = {
    /** The component containing one module. */
    componentOf(component_of: ModuleId): ComponentGraphProjection {
        return { kind: "componentOf", component_of };
    },

    /** The component and entry containing one module. */
    componentEntryOf(component_entry_of: ModuleId): ComponentGraphProjection {
        return { kind: "componentEntryOf", component_entry_of };
    },

    /** The sorted modules belonging to one component. */
    members(members: ComponentId): ComponentGraphProjection {
        return { kind: "members", members };
    },

    /** The direct external components one component depends on. */
    dependencies(dependencies: ComponentId): ComponentGraphProjection {
        return { kind: "dependencies", dependencies };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: ComponentGraphProjection): void {
        encodeComponentGraphProjection(writer, value);
    },

    /** Decode one ComponentGraphProjection. */
    decode(reader: BinaryReader): ComponentGraphProjection {
        return decodeComponentGraphProjection(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ComponentGraphProjection): Json {
        return toJsonComponentGraphProjection(value);
    },

    /** Return one ComponentGraphProjection from one JSON value. */
    fromJson(value: Json): ComponentGraphProjection {
        return fromJsonComponentGraphProjection(value);
    },
};

/** Encode one ComponentGraphProjection. */
export function encodeComponentGraphProjection(writer: BinaryWriter, value: ComponentGraphProjection): void {
    switch (value.kind) {
        case "componentOf":
            writer.writeUnsigned(0);
            encodeModuleId(writer, value.component_of);
            return;
        case "componentEntryOf":
            writer.writeUnsigned(1);
            encodeModuleId(writer, value.component_entry_of);
            return;
        case "members":
            writer.writeUnsigned(2);
            encodeComponentId(writer, value.members);
            return;
        case "dependencies":
            writer.writeUnsigned(3);
            encodeComponentId(writer, value.dependencies);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one ComponentGraphProjection. */
export function decodeComponentGraphProjection(reader: BinaryReader): ComponentGraphProjection {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const component_of = decodeModuleId(reader);

            return { kind: "componentOf", component_of };
        }
        case 1: {
            const component_entry_of = decodeModuleId(reader);

            return { kind: "componentEntryOf", component_entry_of };
        }
        case 2: {
            const members = decodeComponentId(reader);

            return { kind: "members", members };
        }
        case 3: {
            const dependencies = decodeComponentId(reader);

            return { kind: "dependencies", dependencies };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one ComponentGraphProjection. */
export function toJsonComponentGraphProjection(value: ComponentGraphProjection): Json {
    switch (value.kind) {
        case "componentOf":
            return {
                kind: "componentOf",
                component_of: toJsonModuleId(value.component_of),
            };
        case "componentEntryOf":
            return {
                kind: "componentEntryOf",
                component_entry_of: toJsonModuleId(value.component_entry_of),
            };
        case "members":
            return {
                kind: "members",
                members: toJsonComponentId(value.members),
            };
        case "dependencies":
            return {
                kind: "dependencies",
                dependencies: toJsonComponentId(value.dependencies),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one ComponentGraphProjection from one JSON value. */
export function fromJsonComponentGraphProjection(value: Json): ComponentGraphProjection {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "componentOf":
            return {
                kind,
                component_of: fromJsonModuleId(jsonField(object, "component_of")),
            };
        case "componentEntryOf":
            return {
                kind,
                component_entry_of: fromJsonModuleId(jsonField(object, "component_entry_of")),
            };
        case "members":
            return {
                kind,
                members: fromJsonComponentId(jsonField(object, "members")),
            };
        case "dependencies":
            return {
                kind,
                dependencies: fromJsonComponentId(jsonField(object, "dependencies")),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** Stable fingerprint of one observed artifact projection. */
export type ArtifactProjectionFingerprint = bigint;

export const ArtifactProjectionFingerprint = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ArtifactProjectionFingerprint): void {
        encodeArtifactProjectionFingerprint(writer, value);
    },

    /** Decode one ArtifactProjectionFingerprint. */
    decode(reader: BinaryReader): ArtifactProjectionFingerprint {
        return decodeArtifactProjectionFingerprint(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ArtifactProjectionFingerprint): Json {
        return toJsonArtifactProjectionFingerprint(value);
    },

    /** Return one ArtifactProjectionFingerprint from one JSON value. */
    fromJson(value: Json): ArtifactProjectionFingerprint {
        return fromJsonArtifactProjectionFingerprint(value);
    },
};

/** Encode one ArtifactProjectionFingerprint. */
export function encodeArtifactProjectionFingerprint(writer: BinaryWriter, value: ArtifactProjectionFingerprint): void {
    writer.writeUnsigned(value);
}

/** Decode one ArtifactProjectionFingerprint. */
export function decodeArtifactProjectionFingerprint(reader: BinaryReader): ArtifactProjectionFingerprint {
    return reader.readUnsigned();
}

/** Return one JSON value for one ArtifactProjectionFingerprint. */
export function toJsonArtifactProjectionFingerprint(value: ArtifactProjectionFingerprint): Json {
    return value.toString();
}

/** Return one ArtifactProjectionFingerprint from one JSON value. */
export function fromJsonArtifactProjectionFingerprint(value: Json): ArtifactProjectionFingerprint {
    return jsonBigint(value);
}

/** One exact source file content observed while building an artifact. */
export type SourceDependency = {
    /** The source file id. */
    readonly file: FileId;
    /** The exact source content id. */
    readonly content: ContentId;
};

export const SourceDependency = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: SourceDependency): void {
        encodeSourceDependency(writer, value);
    },

    /** Decode one SourceDependency. */
    decode(reader: BinaryReader): SourceDependency {
        return decodeSourceDependency(reader);
    },

    /** Return this value as JSON. */
    toJson(value: SourceDependency): Json {
        return toJsonSourceDependency(value);
    },

    /** Return one SourceDependency from one JSON value. */
    fromJson(value: Json): SourceDependency {
        return fromJsonSourceDependency(value);
    },
};

/** Encode one SourceDependency. */
export function encodeSourceDependency(writer: BinaryWriter, value: SourceDependency): void {
    encodeFileId(writer, value.file);
    encodeContentId(writer, value.content);
}

/** Decode one SourceDependency. */
export function decodeSourceDependency(reader: BinaryReader): SourceDependency {
    const file = decodeFileId(reader);
    const content = decodeContentId(reader);

    return {
        file,
        content,
    };
}

/** Return one JSON value for one SourceDependency. */
export function toJsonSourceDependency(value: SourceDependency): Json {
    return {
        file: toJsonFileId(value.file),
        content: toJsonContentId(value.content),
    };
}

/** Return one SourceDependency from one JSON value. */
export function fromJsonSourceDependency(value: Json): SourceDependency {
    const object = jsonObject(value);

    return {
        file: fromJsonFileId(jsonField(object, "file")),
        content: fromJsonContentId(jsonField(object, "content")),
    };
}

/** One exact dependency read while building an artifact. */
export type ArtifactDependency =
    /** Another exact artifact version. */
    | {
          readonly kind: "artifact";
          readonly artifact: ArtifactVersion;
      }
    /** One exact projected artifact value. */
    | {
          readonly kind: "projection";
          readonly projection: ArtifactProjectionDependency;
      }
    /** One exact primitive source observation. */
    | {
          readonly kind: "source";
          readonly source: SourceDependency;
      }
;

export const ArtifactDependency = {
    /** Another exact artifact version. */
    artifact(artifact: ArtifactVersion): ArtifactDependency {
        return { kind: "artifact", artifact };
    },

    /** One exact projected artifact value. */
    projection(projection: ArtifactProjectionDependency): ArtifactDependency {
        return { kind: "projection", projection };
    },

    /** One exact primitive source observation. */
    source(source: SourceDependency): ArtifactDependency {
        return { kind: "source", source };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: ArtifactDependency): void {
        encodeArtifactDependency(writer, value);
    },

    /** Decode one ArtifactDependency. */
    decode(reader: BinaryReader): ArtifactDependency {
        return decodeArtifactDependency(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ArtifactDependency): Json {
        return toJsonArtifactDependency(value);
    },

    /** Return one ArtifactDependency from one JSON value. */
    fromJson(value: Json): ArtifactDependency {
        return fromJsonArtifactDependency(value);
    },
};

/** Encode one ArtifactDependency. */
export function encodeArtifactDependency(writer: BinaryWriter, value: ArtifactDependency): void {
    switch (value.kind) {
        case "artifact":
            writer.writeUnsigned(0);
            encodeArtifactVersion(writer, value.artifact);
            return;
        case "projection":
            writer.writeUnsigned(1);
            encodeArtifactProjectionDependency(writer, value.projection);
            return;
        case "source":
            writer.writeUnsigned(2);
            encodeSourceDependency(writer, value.source);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one ArtifactDependency. */
export function decodeArtifactDependency(reader: BinaryReader): ArtifactDependency {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const artifact = decodeArtifactVersion(reader);

            return { kind: "artifact", artifact };
        }
        case 1: {
            const projection = decodeArtifactProjectionDependency(reader);

            return { kind: "projection", projection };
        }
        case 2: {
            const source = decodeSourceDependency(reader);

            return { kind: "source", source };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one ArtifactDependency. */
export function toJsonArtifactDependency(value: ArtifactDependency): Json {
    switch (value.kind) {
        case "artifact":
            return {
                kind: "artifact",
                artifact: toJsonArtifactVersion(value.artifact),
            };
        case "projection":
            return {
                kind: "projection",
                projection: toJsonArtifactProjectionDependency(value.projection),
            };
        case "source":
            return {
                kind: "source",
                source: toJsonSourceDependency(value.source),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one ArtifactDependency from one JSON value. */
export function fromJsonArtifactDependency(value: Json): ArtifactDependency {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "artifact":
            return {
                kind,
                artifact: fromJsonArtifactVersion(jsonField(object, "artifact")),
            };
        case "projection":
            return {
                kind,
                projection: fromJsonArtifactProjectionDependency(jsonField(object, "projection")),
            };
        case "source":
            return {
                kind,
                source: fromJsonSourceDependency(jsonField(object, "source")),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}
