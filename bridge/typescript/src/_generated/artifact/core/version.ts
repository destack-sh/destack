/* generated by destack_bridge_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, jsonField, jsonObject } from "../../../protocol/serde.js";
import type { ArtifactFingerprint } from "./fingerprint.js";
import type { ArtifactKey } from "./key.js";
import { decodeArtifactFingerprint, encodeArtifactFingerprint, fromJsonArtifactFingerprint, toJsonArtifactFingerprint } from "./fingerprint.js";
import { decodeArtifactKey, encodeArtifactKey, fromJsonArtifactKey, toJsonArtifactKey } from "./key.js";

/** One exact live artifact version. */
export type ArtifactVersion = {
    /** The semantic artifact slot. */
    readonly key: ArtifactKey;
    /** The exact semantic fingerprint. */
    readonly fingerprint: ArtifactFingerprint;
};

export const ArtifactVersion = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ArtifactVersion): void {
        encodeArtifactVersion(writer, value);
    },

    /** Decode one ArtifactVersion. */
    decode(reader: BinaryReader): ArtifactVersion {
        return decodeArtifactVersion(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ArtifactVersion): Json {
        return toJsonArtifactVersion(value);
    },

    /** Return one ArtifactVersion from one JSON value. */
    fromJson(value: Json): ArtifactVersion {
        return fromJsonArtifactVersion(value);
    },
};

/** Encode one ArtifactVersion. */
export function encodeArtifactVersion(writer: BinaryWriter, value: ArtifactVersion): void {
    encodeArtifactKey(writer, value.key);
    encodeArtifactFingerprint(writer, value.fingerprint);
}

/** Decode one ArtifactVersion. */
export function decodeArtifactVersion(reader: BinaryReader): ArtifactVersion {
    const key = decodeArtifactKey(reader);
    const fingerprint = decodeArtifactFingerprint(reader);

    return {
        key,
        fingerprint,
    };
}

/** Return one JSON value for one ArtifactVersion. */
export function toJsonArtifactVersion(value: ArtifactVersion): Json {
    return {
        key: toJsonArtifactKey(value.key),
        fingerprint: toJsonArtifactFingerprint(value.fingerprint),
    };
}

/** Return one ArtifactVersion from one JSON value. */
export function fromJsonArtifactVersion(value: Json): ArtifactVersion {
    const object = jsonObject(value);

    return {
        key: fromJsonArtifactKey(jsonField(object, "key")),
        fingerprint: fromJsonArtifactFingerprint(jsonField(object, "fingerprint")),
    };
}
