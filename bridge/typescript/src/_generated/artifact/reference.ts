/* generated by destack_bridge_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, jsonField, jsonObject } from "../../protocol/serde.js";
import type { ArtifactKey } from "./core/key.js";
import type { ArtifactVersion } from "./core/version.js";
import { decodeArtifactKey, encodeArtifactKey, fromJsonArtifactKey, toJsonArtifactKey } from "./core/key.js";
import { decodeArtifactVersion, encodeArtifactVersion, fromJsonArtifactVersion, toJsonArtifactVersion } from "./core/version.js";

/** Stable reference to one artifact payload. */
export type ArtifactReference = {
    /** Artifact key. */
    readonly key: ArtifactKey;
    /** Artifact version. */
    readonly version: ArtifactVersion;
};

export const ArtifactReference = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ArtifactReference): void {
        encodeArtifactReference(writer, value);
    },

    /** Decode one ArtifactReference. */
    decode(reader: BinaryReader): ArtifactReference {
        return decodeArtifactReference(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ArtifactReference): Json {
        return toJsonArtifactReference(value);
    },

    /** Return one ArtifactReference from one JSON value. */
    fromJson(value: Json): ArtifactReference {
        return fromJsonArtifactReference(value);
    },
};

/** Encode one ArtifactReference. */
export function encodeArtifactReference(writer: BinaryWriter, value: ArtifactReference): void {
    encodeArtifactKey(writer, value.key);
    encodeArtifactVersion(writer, value.version);
}

/** Decode one ArtifactReference. */
export function decodeArtifactReference(reader: BinaryReader): ArtifactReference {
    const key = decodeArtifactKey(reader);
    const version = decodeArtifactVersion(reader);

    return {
        key,
        version,
    };
}

/** Return one JSON value for one ArtifactReference. */
export function toJsonArtifactReference(value: ArtifactReference): Json {
    return {
        key: toJsonArtifactKey(value.key),
        version: toJsonArtifactVersion(value.version),
    };
}

/** Return one ArtifactReference from one JSON value. */
export function fromJsonArtifactReference(value: Json): ArtifactReference {
    const object = jsonObject(value);

    return {
        key: fromJsonArtifactKey(jsonField(object, "key")),
        version: fromJsonArtifactVersion(jsonField(object, "version")),
    };
}
