/* generated by destack_bridge_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, jsonString } from "../../protocol/serde.js";

/** Emitted artifact family for a build target. */
export type EmitFormat = "js" | "ts" | "wasm" | "native";

export const EmitFormat = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: EmitFormat): void {
        encodeEmitFormat(writer, value);
    },

    /** Decode one EmitFormat. */
    decode(reader: BinaryReader): EmitFormat {
        return decodeEmitFormat(reader);
    },

    /** Return this value as JSON. */
    toJson(value: EmitFormat): Json {
        return toJsonEmitFormat(value);
    },

    /** Return one EmitFormat from one JSON value. */
    fromJson(value: Json): EmitFormat {
        return fromJsonEmitFormat(value);
    },
};

/** Encode one EmitFormat. */
export function encodeEmitFormat(writer: BinaryWriter, value: EmitFormat): void {
    switch (value) {
        case "js":
            writer.writeUnsigned(0);
            return;
        case "ts":
            writer.writeUnsigned(1);
            return;
        case "wasm":
            writer.writeUnsigned(2);
            return;
        case "native":
            writer.writeUnsigned(3);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one EmitFormat. */
export function decodeEmitFormat(reader: BinaryReader): EmitFormat {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "js";
        case 1:
            return "ts";
        case 2:
            return "wasm";
        case 3:
            return "native";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one EmitFormat. */
export function toJsonEmitFormat(value: EmitFormat): Json {
    return value;
}

/** Return one EmitFormat from one JSON value. */
export function fromJsonEmitFormat(value: Json): EmitFormat {
    const variant = jsonString(value);

    switch (variant) {
        case "js":
            return "js";
        case "ts":
            return "ts";
        case "wasm":
            return "wasm";
        case "native":
            return "native";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}
