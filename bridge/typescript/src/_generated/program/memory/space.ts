/* generated by destack_bridge_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, bytesFromJson, bytesToJson, compareBytes, jsonArray, jsonField, jsonInteger, jsonObject, nestedBytes } from "../../../protocol/serde.js";
import type { StaticId } from "./region.js";
import type { StaticRegion } from "./region.js";
import { decodeStaticId, encodeStaticId, fromJsonStaticId, toJsonStaticId } from "./region.js";
import { decodeStaticRegion, encodeStaticRegion, fromJsonStaticRegion, toJsonStaticRegion } from "./region.js";

/** Static memory. */
export type StaticSpace = {
    /** The static bytes. */
    readonly bytes: Uint8Array | readonly number[];
    /** Static regions. */
    readonly regions: ReadonlyArray<StaticRegion>;
    /** Region index by id. */
    readonly regionById: ReadonlyMap<StaticId, number>;
};

export const StaticSpace = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: StaticSpace): void {
        encodeStaticSpace(writer, value);
    },

    /** Decode one StaticSpace. */
    decode(reader: BinaryReader): StaticSpace {
        return decodeStaticSpace(reader);
    },

    /** Return this value as JSON. */
    toJson(value: StaticSpace): Json {
        return toJsonStaticSpace(value);
    },

    /** Return one StaticSpace from one JSON value. */
    fromJson(value: Json): StaticSpace {
        return fromJsonStaticSpace(value);
    },
};

/** Encode one StaticSpace. */
export function encodeStaticSpace(writer: BinaryWriter, value: StaticSpace): void {
    writer.writeByteSlice(value.bytes);
    writer.writeUnsigned(value.regions.length);
    for (const item1 of value.regions) {
        encodeStaticRegion(writer, item1);
    }
    const entries2 = Array.from(value.regionById.entries()).map(([key2, item2]) => {
        const keyBytes = nestedBytes((writer) => {
            encodeStaticId(writer, key2);
        });
        return { key2, item2, keyBytes };
    });
    entries2.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries2.length);
    for (const entry2 of entries2) {
        encodeStaticId(writer, entry2.key2);
        writer.writeUnsigned(entry2.item2);
    }
}

/** Decode one StaticSpace. */
export function decodeStaticSpace(reader: BinaryReader): StaticSpace {
    const bytes = reader.readByteSlice();
    const regions = (() => { const length1 = reader.readNumber(); const items1: Array<StaticRegion> = []; for (let index = 0; index < length1; index += 1) { items1.push(decodeStaticRegion(reader)); } return items1; })();
    const regionById = (() => { const length2 = reader.readNumber(); const items2 = new Map<StaticId, number>(); for (let index = 0; index < length2; index += 1) { items2.set(decodeStaticId(reader), reader.readNumber()); } return items2; })();

    return {
        bytes,
        regions,
        regionById,
    };
}

/** Return one JSON value for one StaticSpace. */
export function toJsonStaticSpace(value: StaticSpace): Json {
    return {
        bytes: bytesToJson(value.bytes),
        regions: value.regions.map((item0) => toJsonStaticRegion(item0)),
        regionById: Array.from(value.regionById.entries()).map(([key0, item0]) => [toJsonStaticId(key0), item0] as const),
    };
}

/** Return one StaticSpace from one JSON value. */
export function fromJsonStaticSpace(value: Json): StaticSpace {
    const object = jsonObject(value);

    return {
        bytes: bytesFromJson(jsonField(object, "bytes")),
        regions: jsonArray(jsonField(object, "regions")).map((item0) => fromJsonStaticRegion(item0)),
        regionById: new Map(jsonArray(jsonField(object, "regionById")).map((entry) => { const items = jsonArray(entry); if (items.length !== 2) { throw new SerdeError(`expected JSON map entry length 2: ${items.length}`); } const key0 = items[0]; const item0 = items[1]; return [fromJsonStaticId(key0), jsonInteger(item0)] as const; })),
    };
}
