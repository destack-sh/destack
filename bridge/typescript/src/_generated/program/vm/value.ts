/* generated by destack_bridge_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, jsonBigint, jsonBool, jsonField, jsonInteger, jsonObject, jsonString } from "../../../protocol/serde.js";
import type { FloatType } from "../../mir/tree/type.js";
import type { TypeId } from "../type.js";
import type { ReferenceMeta } from "./meta.js";
import { decodeFloatType, encodeFloatType, fromJsonFloatType, toJsonFloatType } from "../../mir/tree/type.js";
import { decodeTypeId, encodeTypeId, fromJsonTypeId, toJsonTypeId } from "../type.js";
import { decodeReferenceMeta, encodeReferenceMeta, fromJsonReferenceMeta, toJsonReferenceMeta } from "./meta.js";

/** Native cell layout for one load or store. */
export type CellLayout =
    /** Void value. */
    | {
          readonly kind: "void";
      }
    /** Boolean value. */
    | {
          readonly kind: "bool";
      }
    /** Signed integer value. */
    | {
          readonly kind: "int";
          readonly width: number;
      }
    /** Unsigned integer value. */
    | {
          readonly kind: "uint";
          readonly width: number;
      }
    /** Float16 value. */
    | {
          readonly kind: "float16";
      }
    /** BF16 value. */
    | {
          readonly kind: "bfloat16";
      }
    /** Float32 value. */
    | {
          readonly kind: "float32";
      }
    /** Float64 value. */
    | {
          readonly kind: "float64";
      }
    /** Local heap reference. */
    | {
          readonly kind: "heapReference";
      }
    /** Shared heap reference. */
    | {
          readonly kind: "sharedHeapReference";
      }
    /** Native address. */
    | {
          readonly kind: "address";
      }
    /** Stack pointer. */
    | {
          readonly kind: "stackPointer";
      }
    /** Frame pointer. */
    | {
          readonly kind: "framePointer";
      }
    /** Static address. */
    | {
          readonly kind: "staticAddress";
      }
    /** Function pointer. */
    | {
          readonly kind: "functionPointer";
      }
;

export const CellLayout = {
    /** Void value. */
    "void"(): CellLayout {
        return { kind: "void" };
    },

    /** Boolean value. */
    bool(): CellLayout {
        return { kind: "bool" };
    },

    /** Signed integer value. */
    int(width: number): CellLayout {
        return { kind: "int", width };
    },

    /** Unsigned integer value. */
    uint(width: number): CellLayout {
        return { kind: "uint", width };
    },

    /** Float16 value. */
    float16(): CellLayout {
        return { kind: "float16" };
    },

    /** BF16 value. */
    bfloat16(): CellLayout {
        return { kind: "bfloat16" };
    },

    /** Float32 value. */
    float32(): CellLayout {
        return { kind: "float32" };
    },

    /** Float64 value. */
    float64(): CellLayout {
        return { kind: "float64" };
    },

    /** Local heap reference. */
    heapReference(): CellLayout {
        return { kind: "heapReference" };
    },

    /** Shared heap reference. */
    sharedHeapReference(): CellLayout {
        return { kind: "sharedHeapReference" };
    },

    /** Native address. */
    address(): CellLayout {
        return { kind: "address" };
    },

    /** Stack pointer. */
    stackPointer(): CellLayout {
        return { kind: "stackPointer" };
    },

    /** Frame pointer. */
    framePointer(): CellLayout {
        return { kind: "framePointer" };
    },

    /** Static address. */
    staticAddress(): CellLayout {
        return { kind: "staticAddress" };
    },

    /** Function pointer. */
    functionPointer(): CellLayout {
        return { kind: "functionPointer" };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: CellLayout): void {
        encodeCellLayout(writer, value);
    },

    /** Decode one CellLayout. */
    decode(reader: BinaryReader): CellLayout {
        return decodeCellLayout(reader);
    },

    /** Return this value as JSON. */
    toJson(value: CellLayout): Json {
        return toJsonCellLayout(value);
    },

    /** Return one CellLayout from one JSON value. */
    fromJson(value: Json): CellLayout {
        return fromJsonCellLayout(value);
    },
};

/** Encode one CellLayout. */
export function encodeCellLayout(writer: BinaryWriter, value: CellLayout): void {
    switch (value.kind) {
        case "void":
            writer.writeUnsigned(0);
            return;
        case "bool":
            writer.writeUnsigned(1);
            return;
        case "int":
            writer.writeUnsigned(2);
            writer.writeByte(value.width);
            return;
        case "uint":
            writer.writeUnsigned(3);
            writer.writeByte(value.width);
            return;
        case "float16":
            writer.writeUnsigned(4);
            return;
        case "bfloat16":
            writer.writeUnsigned(5);
            return;
        case "float32":
            writer.writeUnsigned(6);
            return;
        case "float64":
            writer.writeUnsigned(7);
            return;
        case "heapReference":
            writer.writeUnsigned(8);
            return;
        case "sharedHeapReference":
            writer.writeUnsigned(9);
            return;
        case "address":
            writer.writeUnsigned(10);
            return;
        case "stackPointer":
            writer.writeUnsigned(11);
            return;
        case "framePointer":
            writer.writeUnsigned(12);
            return;
        case "staticAddress":
            writer.writeUnsigned(13);
            return;
        case "functionPointer":
            writer.writeUnsigned(14);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one CellLayout. */
export function decodeCellLayout(reader: BinaryReader): CellLayout {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            return { kind: "void" };
        }
        case 1: {
            return { kind: "bool" };
        }
        case 2: {
            const width = reader.readByte();

            return {
                kind: "int",
                width,
            };
        }
        case 3: {
            const width = reader.readByte();

            return {
                kind: "uint",
                width,
            };
        }
        case 4: {
            return { kind: "float16" };
        }
        case 5: {
            return { kind: "bfloat16" };
        }
        case 6: {
            return { kind: "float32" };
        }
        case 7: {
            return { kind: "float64" };
        }
        case 8: {
            return { kind: "heapReference" };
        }
        case 9: {
            return { kind: "sharedHeapReference" };
        }
        case 10: {
            return { kind: "address" };
        }
        case 11: {
            return { kind: "stackPointer" };
        }
        case 12: {
            return { kind: "framePointer" };
        }
        case 13: {
            return { kind: "staticAddress" };
        }
        case 14: {
            return { kind: "functionPointer" };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one CellLayout. */
export function toJsonCellLayout(value: CellLayout): Json {
    switch (value.kind) {
        case "void":
            return {
                kind: "void",
            };
        case "bool":
            return {
                kind: "bool",
            };
        case "int":
            return {
                kind: "int",
                width: value.width,
            };
        case "uint":
            return {
                kind: "uint",
                width: value.width,
            };
        case "float16":
            return {
                kind: "float16",
            };
        case "bfloat16":
            return {
                kind: "bfloat16",
            };
        case "float32":
            return {
                kind: "float32",
            };
        case "float64":
            return {
                kind: "float64",
            };
        case "heapReference":
            return {
                kind: "heapReference",
            };
        case "sharedHeapReference":
            return {
                kind: "sharedHeapReference",
            };
        case "address":
            return {
                kind: "address",
            };
        case "stackPointer":
            return {
                kind: "stackPointer",
            };
        case "framePointer":
            return {
                kind: "framePointer",
            };
        case "staticAddress":
            return {
                kind: "staticAddress",
            };
        case "functionPointer":
            return {
                kind: "functionPointer",
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one CellLayout from one JSON value. */
export function fromJsonCellLayout(value: Json): CellLayout {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "void":
            return {
                kind,
            };
        case "bool":
            return {
                kind,
            };
        case "int":
            return {
                kind,
                width: jsonInteger(jsonField(object, "width")),
            };
        case "uint":
            return {
                kind,
                width: jsonInteger(jsonField(object, "width")),
            };
        case "float16":
            return {
                kind,
            };
        case "bfloat16":
            return {
                kind,
            };
        case "float32":
            return {
                kind,
            };
        case "float64":
            return {
                kind,
            };
        case "heapReference":
            return {
                kind,
            };
        case "sharedHeapReference":
            return {
                kind,
            };
        case "address":
            return {
                kind,
            };
        case "stackPointer":
            return {
                kind,
            };
        case "framePointer":
            return {
                kind,
            };
        case "staticAddress":
            return {
                kind,
            };
        case "functionPointer":
            return {
                kind,
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** Scalar value shape for typed vector and tensor operations. */
export type ScalarLayout =
    /** Signed or unsigned integers with a bit width. */
    | {
          readonly kind: "int";
          /** The bit width. */
          readonly width: number;
          /** Whether the integer is signed. */
          readonly isSigned: boolean;
      }
    /** Floating-point values with a concrete format. */
    | {
          readonly kind: "float";
          /** The concrete float format. */
          readonly format: FloatType;
      }
    /** Boolean values. */
    | {
          readonly kind: "bool";
      }
;

export const ScalarLayout = {
    /** Signed or unsigned integers with a bit width. */
    int(width: number, isSigned: boolean): ScalarLayout {
        return { kind: "int", width, isSigned };
    },

    /** Floating-point values with a concrete format. */
    float(format: FloatType): ScalarLayout {
        return { kind: "float", format };
    },

    /** Boolean values. */
    bool(): ScalarLayout {
        return { kind: "bool" };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: ScalarLayout): void {
        encodeScalarLayout(writer, value);
    },

    /** Decode one ScalarLayout. */
    decode(reader: BinaryReader): ScalarLayout {
        return decodeScalarLayout(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ScalarLayout): Json {
        return toJsonScalarLayout(value);
    },

    /** Return one ScalarLayout from one JSON value. */
    fromJson(value: Json): ScalarLayout {
        return fromJsonScalarLayout(value);
    },
};

/** Encode one ScalarLayout. */
export function encodeScalarLayout(writer: BinaryWriter, value: ScalarLayout): void {
    switch (value.kind) {
        case "int":
            writer.writeUnsigned(0);
            writer.writeUnsigned(value.width);
            writer.writeBool(value.isSigned);
            return;
        case "float":
            writer.writeUnsigned(1);
            encodeFloatType(writer, value.format);
            return;
        case "bool":
            writer.writeUnsigned(2);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one ScalarLayout. */
export function decodeScalarLayout(reader: BinaryReader): ScalarLayout {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const width = reader.readNumber();
            const isSigned = reader.readBool();

            return {
                kind: "int",
                width,
                isSigned,
            };
        }
        case 1: {
            const format = decodeFloatType(reader);

            return {
                kind: "float",
                format,
            };
        }
        case 2: {
            return { kind: "bool" };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one ScalarLayout. */
export function toJsonScalarLayout(value: ScalarLayout): Json {
    switch (value.kind) {
        case "int":
            return {
                kind: "int",
                width: value.width,
                isSigned: value.isSigned,
            };
        case "float":
            return {
                kind: "float",
                format: toJsonFloatType(value.format),
            };
        case "bool":
            return {
                kind: "bool",
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one ScalarLayout from one JSON value. */
export function fromJsonScalarLayout(value: Json): ScalarLayout {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "int":
            return {
                kind,
                width: jsonInteger(jsonField(object, "width")),
                isSigned: jsonBool(jsonField(object, "isSigned")),
            };
        case "float":
            return {
                kind,
                format: fromJsonFloatType(jsonField(object, "format")),
            };
        case "bool":
            return {
                kind,
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** Runtime value shape used for op selection. */
export type ValueShape =
    /** Void value. */
    | {
          readonly kind: "void";
      }
    /** Boolean value. */
    | {
          readonly kind: "bool";
      }
    /** Signed or unsigned integer with width. */
    | {
          readonly kind: "int";
          readonly width: number;
          readonly signed: boolean;
      }
    /** Floating point value with format. */
    | {
          readonly kind: "float";
          readonly format: FloatType;
      }
    /** Unicode character value. */
    | {
          readonly kind: "char";
      }
    /** Addressable value with pointee type. */
    | {
          readonly kind: "pointer";
          readonly pointee: TypeId;
          readonly addressSpace: AddressSpace;
          readonly reference: ReferenceMeta;
      }
    /** Function pointer value with result type. */
    | {
          readonly kind: "functionPointer";
          readonly result: TypeId;
      }
    /** Aggregate value materialized in frame storage. */
    | {
          readonly kind: "aggregate";
          readonly ty: TypeId;
      }
    /** Fixed-size array value with element type. */
    | {
          readonly kind: "array";
          readonly element: TypeId;
          readonly length: bigint;
      }
;

export const ValueShape = {
    /** Void value. */
    "void"(): ValueShape {
        return { kind: "void" };
    },

    /** Boolean value. */
    bool(): ValueShape {
        return { kind: "bool" };
    },

    /** Signed or unsigned integer with width. */
    int(width: number, signed: boolean): ValueShape {
        return { kind: "int", width, signed };
    },

    /** Floating point value with format. */
    float(format: FloatType): ValueShape {
        return { kind: "float", format };
    },

    /** Unicode character value. */
    char(): ValueShape {
        return { kind: "char" };
    },

    /** Addressable value with pointee type. */
    pointer(pointee: TypeId, addressSpace: AddressSpace, reference: ReferenceMeta): ValueShape {
        return { kind: "pointer", pointee, addressSpace, reference };
    },

    /** Function pointer value with result type. */
    functionPointer(result: TypeId): ValueShape {
        return { kind: "functionPointer", result };
    },

    /** Aggregate value materialized in frame storage. */
    aggregate(ty: TypeId): ValueShape {
        return { kind: "aggregate", ty };
    },

    /** Fixed-size array value with element type. */
    array(element: TypeId, length: bigint): ValueShape {
        return { kind: "array", element, length };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: ValueShape): void {
        encodeValueShape(writer, value);
    },

    /** Decode one ValueShape. */
    decode(reader: BinaryReader): ValueShape {
        return decodeValueShape(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ValueShape): Json {
        return toJsonValueShape(value);
    },

    /** Return one ValueShape from one JSON value. */
    fromJson(value: Json): ValueShape {
        return fromJsonValueShape(value);
    },
};

/** Encode one ValueShape. */
export function encodeValueShape(writer: BinaryWriter, value: ValueShape): void {
    switch (value.kind) {
        case "void":
            writer.writeUnsigned(0);
            return;
        case "bool":
            writer.writeUnsigned(1);
            return;
        case "int":
            writer.writeUnsigned(2);
            writer.writeUnsigned(value.width);
            writer.writeBool(value.signed);
            return;
        case "float":
            writer.writeUnsigned(3);
            encodeFloatType(writer, value.format);
            return;
        case "char":
            writer.writeUnsigned(4);
            return;
        case "pointer":
            writer.writeUnsigned(5);
            encodeTypeId(writer, value.pointee);
            encodeAddressSpace(writer, value.addressSpace);
            encodeReferenceMeta(writer, value.reference);
            return;
        case "functionPointer":
            writer.writeUnsigned(6);
            encodeTypeId(writer, value.result);
            return;
        case "aggregate":
            writer.writeUnsigned(7);
            encodeTypeId(writer, value.ty);
            return;
        case "array":
            writer.writeUnsigned(8);
            encodeTypeId(writer, value.element);
            writer.writeUnsigned(value.length);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one ValueShape. */
export function decodeValueShape(reader: BinaryReader): ValueShape {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            return { kind: "void" };
        }
        case 1: {
            return { kind: "bool" };
        }
        case 2: {
            const width = reader.readNumber();
            const signed = reader.readBool();

            return {
                kind: "int",
                width,
                signed,
            };
        }
        case 3: {
            const format = decodeFloatType(reader);

            return {
                kind: "float",
                format,
            };
        }
        case 4: {
            return { kind: "char" };
        }
        case 5: {
            const pointee = decodeTypeId(reader);
            const addressSpace = decodeAddressSpace(reader);
            const reference = decodeReferenceMeta(reader);

            return {
                kind: "pointer",
                pointee,
                addressSpace,
                reference,
            };
        }
        case 6: {
            const result = decodeTypeId(reader);

            return {
                kind: "functionPointer",
                result,
            };
        }
        case 7: {
            const ty = decodeTypeId(reader);

            return {
                kind: "aggregate",
                ty,
            };
        }
        case 8: {
            const element = decodeTypeId(reader);
            const length = reader.readUnsigned();

            return {
                kind: "array",
                element,
                length,
            };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one ValueShape. */
export function toJsonValueShape(value: ValueShape): Json {
    switch (value.kind) {
        case "void":
            return {
                kind: "void",
            };
        case "bool":
            return {
                kind: "bool",
            };
        case "int":
            return {
                kind: "int",
                width: value.width,
                signed: value.signed,
            };
        case "float":
            return {
                kind: "float",
                format: toJsonFloatType(value.format),
            };
        case "char":
            return {
                kind: "char",
            };
        case "pointer":
            return {
                kind: "pointer",
                pointee: toJsonTypeId(value.pointee),
                addressSpace: toJsonAddressSpace(value.addressSpace),
                reference: toJsonReferenceMeta(value.reference),
            };
        case "functionPointer":
            return {
                kind: "functionPointer",
                result: toJsonTypeId(value.result),
            };
        case "aggregate":
            return {
                kind: "aggregate",
                ty: toJsonTypeId(value.ty),
            };
        case "array":
            return {
                kind: "array",
                element: toJsonTypeId(value.element),
                length: value.length.toString(),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one ValueShape from one JSON value. */
export function fromJsonValueShape(value: Json): ValueShape {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "void":
            return {
                kind,
            };
        case "bool":
            return {
                kind,
            };
        case "int":
            return {
                kind,
                width: jsonInteger(jsonField(object, "width")),
                signed: jsonBool(jsonField(object, "signed")),
            };
        case "float":
            return {
                kind,
                format: fromJsonFloatType(jsonField(object, "format")),
            };
        case "char":
            return {
                kind,
            };
        case "pointer":
            return {
                kind,
                pointee: fromJsonTypeId(jsonField(object, "pointee")),
                addressSpace: fromJsonAddressSpace(jsonField(object, "addressSpace")),
                reference: fromJsonReferenceMeta(jsonField(object, "reference")),
            };
        case "functionPointer":
            return {
                kind,
                result: fromJsonTypeId(jsonField(object, "result")),
            };
        case "aggregate":
            return {
                kind,
                ty: fromJsonTypeId(jsonField(object, "ty")),
            };
        case "array":
            return {
                kind,
                element: fromJsonTypeId(jsonField(object, "element")),
                length: jsonBigint(jsonField(object, "length")),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** Runtime address space for addressable values. */
export type AddressSpace = "local" | "shared" | "raw" | "stack" | "frame" | "static";

export const AddressSpace = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: AddressSpace): void {
        encodeAddressSpace(writer, value);
    },

    /** Decode one AddressSpace. */
    decode(reader: BinaryReader): AddressSpace {
        return decodeAddressSpace(reader);
    },

    /** Return this value as JSON. */
    toJson(value: AddressSpace): Json {
        return toJsonAddressSpace(value);
    },

    /** Return one AddressSpace from one JSON value. */
    fromJson(value: Json): AddressSpace {
        return fromJsonAddressSpace(value);
    },
};

/** Encode one AddressSpace. */
export function encodeAddressSpace(writer: BinaryWriter, value: AddressSpace): void {
    switch (value) {
        case "local":
            writer.writeUnsigned(0);
            return;
        case "shared":
            writer.writeUnsigned(1);
            return;
        case "raw":
            writer.writeUnsigned(2);
            return;
        case "stack":
            writer.writeUnsigned(3);
            return;
        case "frame":
            writer.writeUnsigned(4);
            return;
        case "static":
            writer.writeUnsigned(5);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one AddressSpace. */
export function decodeAddressSpace(reader: BinaryReader): AddressSpace {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "local";
        case 1:
            return "shared";
        case 2:
            return "raw";
        case 3:
            return "stack";
        case 4:
            return "frame";
        case 5:
            return "static";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one AddressSpace. */
export function toJsonAddressSpace(value: AddressSpace): Json {
    return value;
}

/** Return one AddressSpace from one JSON value. */
export function fromJsonAddressSpace(value: Json): AddressSpace {
    const variant = jsonString(value);

    switch (variant) {
        case "local":
            return "local";
        case "shared":
            return "shared";
        case "raw":
            return "raw";
        case "stack":
            return "stack";
        case "frame":
            return "frame";
        case "static":
            return "static";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}
