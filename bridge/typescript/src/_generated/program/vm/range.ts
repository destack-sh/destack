/* generated by destack_bridge_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, jsonBool, jsonField, jsonInteger, jsonObject, jsonString } from "../../../protocol/serde.js";
import type { LocalNodeId } from "../../mir/tree/node.js";
import { decodeLocalNodeId, encodeLocalNodeId, fromJsonLocalNodeId, toJsonLocalNodeId } from "../../mir/tree/node.js";

/** Argument range within one function argument pool. */
export type ArgumentRange = {
    /** Start offset into the argument pool. */
    readonly start: number;
    /** Number of arguments in the range. */
    readonly len: number;
};

export const ArgumentRange = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ArgumentRange): void {
        encodeArgumentRange(writer, value);
    },

    /** Decode one ArgumentRange. */
    decode(reader: BinaryReader): ArgumentRange {
        return decodeArgumentRange(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ArgumentRange): Json {
        return toJsonArgumentRange(value);
    },

    /** Return one ArgumentRange from one JSON value. */
    fromJson(value: Json): ArgumentRange {
        return fromJsonArgumentRange(value);
    },
};

/** Encode one ArgumentRange. */
export function encodeArgumentRange(writer: BinaryWriter, value: ArgumentRange): void {
    writer.writeUnsigned(value.start);
    writer.writeUnsigned(value.len);
}

/** Decode one ArgumentRange. */
export function decodeArgumentRange(reader: BinaryReader): ArgumentRange {
    const start = reader.readNumber();
    const len = reader.readNumber();

    return {
        start,
        len,
    };
}

/** Return one JSON value for one ArgumentRange. */
export function toJsonArgumentRange(value: ArgumentRange): Json {
    return {
        start: value.start,
        len: value.len,
    };
}

/** Return one ArgumentRange from one JSON value. */
export function fromJsonArgumentRange(value: Json): ArgumentRange {
    const object = jsonObject(value);

    return {
        start: jsonInteger(jsonField(object, "start")),
        len: jsonInteger(jsonField(object, "len")),
    };
}

/** Move pair for parameter binding. */
export type MovePair = {
    /** Destination frame slot. */
    readonly dest: MoveSlot;
    /** Source frame slot or void fill. */
    readonly source: MoveSource;
};

export const MovePair = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: MovePair): void {
        encodeMovePair(writer, value);
    },

    /** Decode one MovePair. */
    decode(reader: BinaryReader): MovePair {
        return decodeMovePair(reader);
    },

    /** Return this value as JSON. */
    toJson(value: MovePair): Json {
        return toJsonMovePair(value);
    },

    /** Return one MovePair from one JSON value. */
    fromJson(value: Json): MovePair {
        return fromJsonMovePair(value);
    },
};

/** Encode one MovePair. */
export function encodeMovePair(writer: BinaryWriter, value: MovePair): void {
    encodeMoveSlot(writer, value.dest);
    encodeMoveSource(writer, value.source);
}

/** Decode one MovePair. */
export function decodeMovePair(reader: BinaryReader): MovePair {
    const dest = decodeMoveSlot(reader);
    const source = decodeMoveSource(reader);

    return {
        dest,
        source,
    };
}

/** Return one JSON value for one MovePair. */
export function toJsonMovePair(value: MovePair): Json {
    return {
        dest: toJsonMoveSlot(value.dest),
        source: toJsonMoveSource(value.source),
    };
}

/** Return one MovePair from one JSON value. */
export function fromJsonMovePair(value: Json): MovePair {
    const object = jsonObject(value);

    return {
        dest: fromJsonMoveSlot(jsonField(object, "dest")),
        source: fromJsonMoveSource(jsonField(object, "source")),
    };
}

/** One lowered frame move slot. */
export type MoveSlot = {
    /** The slot value type. */
    readonly ty: LocalNodeId;
    /** Byte offset from the frame base. */
    readonly offset: number;
    /** Slot byte length. */
    readonly byteLen: number;
    /** Whether this slot stores one cell. */
    readonly isCell: boolean;
};

export const MoveSlot = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: MoveSlot): void {
        encodeMoveSlot(writer, value);
    },

    /** Decode one MoveSlot. */
    decode(reader: BinaryReader): MoveSlot {
        return decodeMoveSlot(reader);
    },

    /** Return this value as JSON. */
    toJson(value: MoveSlot): Json {
        return toJsonMoveSlot(value);
    },

    /** Return one MoveSlot from one JSON value. */
    fromJson(value: Json): MoveSlot {
        return fromJsonMoveSlot(value);
    },
};

/** Encode one MoveSlot. */
export function encodeMoveSlot(writer: BinaryWriter, value: MoveSlot): void {
    encodeLocalNodeId(writer, value.ty);
    writer.writeUnsigned(value.offset);
    writer.writeUnsigned(value.byteLen);
    writer.writeBool(value.isCell);
}

/** Decode one MoveSlot. */
export function decodeMoveSlot(reader: BinaryReader): MoveSlot {
    const ty = decodeLocalNodeId(reader);
    const offset = reader.readNumber();
    const byteLen = reader.readNumber();
    const isCell = reader.readBool();

    return {
        ty,
        offset,
        byteLen,
        isCell,
    };
}

/** Return one JSON value for one MoveSlot. */
export function toJsonMoveSlot(value: MoveSlot): Json {
    return {
        ty: toJsonLocalNodeId(value.ty),
        offset: value.offset,
        byteLen: value.byteLen,
        isCell: value.isCell,
    };
}

/** Return one MoveSlot from one JSON value. */
export function fromJsonMoveSlot(value: Json): MoveSlot {
    const object = jsonObject(value);

    return {
        ty: fromJsonLocalNodeId(jsonField(object, "ty")),
        offset: jsonInteger(jsonField(object, "offset")),
        byteLen: jsonInteger(jsonField(object, "byteLen")),
        isCell: jsonBool(jsonField(object, "isCell")),
    };
}

/** Source for one lowered frame move. */
export type MoveSource =
    /** Move from a frame slot. */
    | {
          readonly kind: "slot";
          readonly slot: MoveSlot;
      }
    /** Write the canonical void value. */
    | {
          readonly kind: "void";
      }
;

export const MoveSource = {
    /** Move from a frame slot. */
    slot(slot: MoveSlot): MoveSource {
        return { kind: "slot", slot };
    },

    /** Write the canonical void value. */
    "void"(): MoveSource {
        return { kind: "void" };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: MoveSource): void {
        encodeMoveSource(writer, value);
    },

    /** Decode one MoveSource. */
    decode(reader: BinaryReader): MoveSource {
        return decodeMoveSource(reader);
    },

    /** Return this value as JSON. */
    toJson(value: MoveSource): Json {
        return toJsonMoveSource(value);
    },

    /** Return one MoveSource from one JSON value. */
    fromJson(value: Json): MoveSource {
        return fromJsonMoveSource(value);
    },
};

/** Encode one MoveSource. */
export function encodeMoveSource(writer: BinaryWriter, value: MoveSource): void {
    switch (value.kind) {
        case "slot":
            writer.writeUnsigned(0);
            encodeMoveSlot(writer, value.slot);
            return;
        case "void":
            writer.writeUnsigned(1);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one MoveSource. */
export function decodeMoveSource(reader: BinaryReader): MoveSource {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const slot = decodeMoveSlot(reader);

            return { kind: "slot", slot };
        }
        case 1: {
            return { kind: "void" };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one MoveSource. */
export function toJsonMoveSource(value: MoveSource): Json {
    switch (value.kind) {
        case "slot":
            return {
                kind: "slot",
                slot: toJsonMoveSlot(value.slot),
            };
        case "void":
            return {
                kind: "void",
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one MoveSource from one JSON value. */
export function fromJsonMoveSource(value: Json): MoveSource {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "slot":
            return {
                kind,
                slot: fromJsonMoveSlot(jsonField(object, "slot")),
            };
        case "void":
            return {
                kind,
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** Move range within one function move pool. */
export type MoveRange = {
    /** Start offset into the move pool. */
    readonly start: number;
    /** Number of pairs in the range. */
    readonly len: number;
};

export const MoveRange = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: MoveRange): void {
        encodeMoveRange(writer, value);
    },

    /** Decode one MoveRange. */
    decode(reader: BinaryReader): MoveRange {
        return decodeMoveRange(reader);
    },

    /** Return this value as JSON. */
    toJson(value: MoveRange): Json {
        return toJsonMoveRange(value);
    },

    /** Return one MoveRange from one JSON value. */
    fromJson(value: Json): MoveRange {
        return fromJsonMoveRange(value);
    },
};

/** Encode one MoveRange. */
export function encodeMoveRange(writer: BinaryWriter, value: MoveRange): void {
    writer.writeUnsigned(value.start);
    writer.writeUnsigned(value.len);
}

/** Decode one MoveRange. */
export function decodeMoveRange(reader: BinaryReader): MoveRange {
    const start = reader.readNumber();
    const len = reader.readNumber();

    return {
        start,
        len,
    };
}

/** Return one JSON value for one MoveRange. */
export function toJsonMoveRange(value: MoveRange): Json {
    return {
        start: value.start,
        len: value.len,
    };
}

/** Return one MoveRange from one JSON value. */
export function fromJsonMoveRange(value: Json): MoveRange {
    const object = jsonObject(value);

    return {
        start: jsonInteger(jsonField(object, "start")),
        len: jsonInteger(jsonField(object, "len")),
    };
}
