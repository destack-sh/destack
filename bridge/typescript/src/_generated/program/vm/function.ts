/* generated by destack_bridge_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, compareBytes, jsonArray, jsonBigint, jsonField, jsonInteger, jsonObject, jsonString, nestedBytes } from "../../../protocol/serde.js";
import type { FrameLayoutId } from "../../mir/metadata/frame.js";
import type { LocalNodeId } from "../../mir/tree/node.js";
import type { Value } from "../../mir/tree/value.js";
import type { FunctionId } from "../function.js";
import type { Instruction } from "./instruction.js";
import type { ArgumentRange } from "./range.js";
import type { MovePair } from "./range.js";
import type { MoveRange } from "./range.js";
import { decodeFrameLayoutId, encodeFrameLayoutId, fromJsonFrameLayoutId, toJsonFrameLayoutId } from "../../mir/metadata/frame.js";
import { decodeLocalNodeId, encodeLocalNodeId, fromJsonLocalNodeId, toJsonLocalNodeId } from "../../mir/tree/node.js";
import { decodeValue, encodeValue, fromJsonValue, toJsonValue } from "../../mir/tree/value.js";
import { decodeFunctionId, encodeFunctionId, fromJsonFunctionId, toJsonFunctionId } from "../function.js";
import { decodeInstruction, encodeInstruction, fromJsonInstruction, toJsonInstruction } from "./instruction.js";
import { decodeArgumentRange, encodeArgumentRange, fromJsonArgumentRange, toJsonArgumentRange } from "./range.js";
import { decodeMovePair, encodeMovePair, fromJsonMovePair, toJsonMovePair } from "./range.js";
import { decodeMoveRange, encodeMoveRange, fromJsonMoveRange, toJsonMoveRange } from "./range.js";

/** Lowered function registry owned by one program. */
export type FunctionTable = {
    /** Lowered functions by dense index. */
    readonly functions: ReadonlyArray<Function>;
    /** Call target by function id. */
    readonly targetById: ReadonlyMap<FunctionId, CallTarget>;
};

export const FunctionTable = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: FunctionTable): void {
        encodeFunctionTable(writer, value);
    },

    /** Decode one FunctionTable. */
    decode(reader: BinaryReader): FunctionTable {
        return decodeFunctionTable(reader);
    },

    /** Return this value as JSON. */
    toJson(value: FunctionTable): Json {
        return toJsonFunctionTable(value);
    },

    /** Return one FunctionTable from one JSON value. */
    fromJson(value: Json): FunctionTable {
        return fromJsonFunctionTable(value);
    },
};

/** Encode one FunctionTable. */
export function encodeFunctionTable(writer: BinaryWriter, value: FunctionTable): void {
    writer.writeUnsigned(value.functions.length);
    for (const item0 of value.functions) {
        encodeFunction(writer, item0);
    }
    const entries1 = Array.from(value.targetById.entries()).map(([key1, item1]) => {
        const keyBytes = nestedBytes((writer) => {
            encodeFunctionId(writer, key1);
        });
        return { key1, item1, keyBytes };
    });
    entries1.sort((left, right) => compareBytes(left.keyBytes, right.keyBytes));
    writer.writeUnsigned(entries1.length);
    for (const entry1 of entries1) {
        encodeFunctionId(writer, entry1.key1);
        encodeCallTarget(writer, entry1.item1);
    }
}

/** Decode one FunctionTable. */
export function decodeFunctionTable(reader: BinaryReader): FunctionTable {
    const functions = (() => { const length0 = reader.readNumber(); const items0: Array<Function> = []; for (let index = 0; index < length0; index += 1) { items0.push(decodeFunction(reader)); } return items0; })();
    const targetById = (() => { const length1 = reader.readNumber(); const items1 = new Map<FunctionId, CallTarget>(); for (let index = 0; index < length1; index += 1) { items1.set(decodeFunctionId(reader), decodeCallTarget(reader)); } return items1; })();

    return {
        functions,
        targetById,
    };
}

/** Return one JSON value for one FunctionTable. */
export function toJsonFunctionTable(value: FunctionTable): Json {
    return {
        functions: value.functions.map((item0) => toJsonFunction(item0)),
        targetById: Array.from(value.targetById.entries()).map(([key0, item0]) => [toJsonFunctionId(key0), toJsonCallTarget(item0)] as const),
    };
}

/** Return one FunctionTable from one JSON value. */
export function fromJsonFunctionTable(value: Json): FunctionTable {
    const object = jsonObject(value);

    return {
        functions: jsonArray(jsonField(object, "functions")).map((item0) => fromJsonFunction(item0)),
        targetById: new Map(jsonArray(jsonField(object, "targetById")).map((entry) => { const items = jsonArray(entry); if (items.length !== 2) { throw new SerdeError(`expected JSON map entry length 2: ${items.length}`); } const key0 = items[0]; const item0 = items[1]; return [fromJsonFunctionId(key0), fromJsonCallTarget(item0)] as const; })),
    };
}

/** Lowered function with executable code and frame metadata. */
export type Function = {
    /** Runtime function id. */
    readonly function: FunctionId;
    /** The logical frame layout for this function. */
    readonly frameLayout: FrameLayoutId;
    /** Function parameters. */
    readonly parameters: ArgumentRange;
    /** Entry block index. */
    readonly entry: number;
    /** Contiguous instruction code. */
    readonly code: ReadonlyArray<Instruction>;
    /** Lowered block ranges. */
    readonly blocks: ReadonlyArray<Block>;
    /** Pool of argument values referenced by ranges. */
    readonly argumentPool: ReadonlyArray<Value>;
    /** Pool of value move pairs referenced by ranges. */
    readonly movePool: ReadonlyArray<MovePair>;
};

export const Function = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: Function): void {
        encodeFunction(writer, value);
    },

    /** Decode one Function. */
    decode(reader: BinaryReader): Function {
        return decodeFunction(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Function): Json {
        return toJsonFunction(value);
    },

    /** Return one Function from one JSON value. */
    fromJson(value: Json): Function {
        return fromJsonFunction(value);
    },
};

/** Encode one Function. */
export function encodeFunction(writer: BinaryWriter, value: Function): void {
    encodeFunctionId(writer, value.function);
    encodeFrameLayoutId(writer, value.frameLayout);
    encodeArgumentRange(writer, value.parameters);
    writer.writeUnsigned(value.entry);
    writer.writeUnsigned(value.code.length);
    for (const item4 of value.code) {
        encodeInstruction(writer, item4);
    }
    writer.writeUnsigned(value.blocks.length);
    for (const item5 of value.blocks) {
        encodeBlock(writer, item5);
    }
    writer.writeUnsigned(value.argumentPool.length);
    for (const item6 of value.argumentPool) {
        encodeValue(writer, item6);
    }
    writer.writeUnsigned(value.movePool.length);
    for (const item7 of value.movePool) {
        encodeMovePair(writer, item7);
    }
}

/** Decode one Function. */
export function decodeFunction(reader: BinaryReader): Function {
    const function_ = decodeFunctionId(reader);
    const frameLayout = decodeFrameLayoutId(reader);
    const parameters = decodeArgumentRange(reader);
    const entry = reader.readNumber();
    const code = (() => { const length4 = reader.readNumber(); const items4: Array<Instruction> = []; for (let index = 0; index < length4; index += 1) { items4.push(decodeInstruction(reader)); } return items4; })();
    const blocks = (() => { const length5 = reader.readNumber(); const items5: Array<Block> = []; for (let index = 0; index < length5; index += 1) { items5.push(decodeBlock(reader)); } return items5; })();
    const argumentPool = (() => { const length6 = reader.readNumber(); const items6: Array<Value> = []; for (let index = 0; index < length6; index += 1) { items6.push(decodeValue(reader)); } return items6; })();
    const movePool = (() => { const length7 = reader.readNumber(); const items7: Array<MovePair> = []; for (let index = 0; index < length7; index += 1) { items7.push(decodeMovePair(reader)); } return items7; })();

    return {
        function: function_,
        frameLayout,
        parameters,
        entry,
        code,
        blocks,
        argumentPool,
        movePool,
    };
}

/** Return one JSON value for one Function. */
export function toJsonFunction(value: Function): Json {
    return {
        function: toJsonFunctionId(value.function),
        frameLayout: toJsonFrameLayoutId(value.frameLayout),
        parameters: toJsonArgumentRange(value.parameters),
        entry: value.entry,
        code: value.code.map((item0) => toJsonInstruction(item0)),
        blocks: value.blocks.map((item0) => toJsonBlock(item0)),
        argumentPool: value.argumentPool.map((item0) => toJsonValue(item0)),
        movePool: value.movePool.map((item0) => toJsonMovePair(item0)),
    };
}

/** Return one Function from one JSON value. */
export function fromJsonFunction(value: Json): Function {
    const object = jsonObject(value);

    return {
        function: fromJsonFunctionId(jsonField(object, "function")),
        frameLayout: fromJsonFrameLayoutId(jsonField(object, "frameLayout")),
        parameters: fromJsonArgumentRange(jsonField(object, "parameters")),
        entry: jsonInteger(jsonField(object, "entry")),
        code: jsonArray(jsonField(object, "code")).map((item0) => fromJsonInstruction(item0)),
        blocks: jsonArray(jsonField(object, "blocks")).map((item0) => fromJsonBlock(item0)),
        argumentPool: jsonArray(jsonField(object, "argumentPool")).map((item0) => fromJsonValue(item0)),
        movePool: jsonArray(jsonField(object, "movePool")).map((item0) => fromJsonMovePair(item0)),
    };
}

/** Lowered basic block position inside one function. */
export type Block = {
    /** Original MIR block id. */
    readonly mirBlock: LocalNodeId;
    /** First instruction in the function code. */
    readonly start: number;
    /** Number of instructions in this block. */
    readonly len: number;
    /** Source MIR point for each lowered PC in this block. */
    readonly mirPointByPc: ReadonlyArray<number>;
};

export const Block = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: Block): void {
        encodeBlock(writer, value);
    },

    /** Decode one Block. */
    decode(reader: BinaryReader): Block {
        return decodeBlock(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Block): Json {
        return toJsonBlock(value);
    },

    /** Return one Block from one JSON value. */
    fromJson(value: Json): Block {
        return fromJsonBlock(value);
    },
};

/** Encode one Block. */
export function encodeBlock(writer: BinaryWriter, value: Block): void {
    encodeLocalNodeId(writer, value.mirBlock);
    writer.writeUnsigned(value.start);
    writer.writeUnsigned(value.len);
    writer.writeUnsigned(value.mirPointByPc.length);
    for (const item3 of value.mirPointByPc) {
        writer.writeUnsigned(item3);
    }
}

/** Decode one Block. */
export function decodeBlock(reader: BinaryReader): Block {
    const mirBlock = decodeLocalNodeId(reader);
    const start = reader.readNumber();
    const len = reader.readNumber();
    const mirPointByPc = (() => { const length3 = reader.readNumber(); const items3: Array<number> = []; for (let index = 0; index < length3; index += 1) { items3.push(reader.readNumber()); } return items3; })();

    return {
        mirBlock,
        start,
        len,
        mirPointByPc,
    };
}

/** Return one JSON value for one Block. */
export function toJsonBlock(value: Block): Json {
    return {
        mirBlock: toJsonLocalNodeId(value.mirBlock),
        start: value.start,
        len: value.len,
        mirPointByPc: value.mirPointByPc.map((item0) => item0),
    };
}

/** Return one Block from one JSON value. */
export function fromJsonBlock(value: Json): Block {
    const object = jsonObject(value);

    return {
        mirBlock: fromJsonLocalNodeId(jsonField(object, "mirBlock")),
        start: jsonInteger(jsonField(object, "start")),
        len: jsonInteger(jsonField(object, "len")),
        mirPointByPc: jsonArray(jsonField(object, "mirPointByPc")).map((item0) => jsonInteger(item0)),
    };
}

/** Program call target for one function id. */
export type CallTarget =
    /** The function id names one imported function. */
    | {
          readonly kind: "import";
      }
    /** The function id names one lowered function. */
    | {
          readonly kind: "local";
          readonly local: number;
      }
;

export const CallTarget = {
    /** The function id names one imported function. */
    "import"(): CallTarget {
        return { kind: "import" };
    },

    /** The function id names one lowered function. */
    local(local: number): CallTarget {
        return { kind: "local", local };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: CallTarget): void {
        encodeCallTarget(writer, value);
    },

    /** Decode one CallTarget. */
    decode(reader: BinaryReader): CallTarget {
        return decodeCallTarget(reader);
    },

    /** Return this value as JSON. */
    toJson(value: CallTarget): Json {
        return toJsonCallTarget(value);
    },

    /** Return one CallTarget from one JSON value. */
    fromJson(value: Json): CallTarget {
        return fromJsonCallTarget(value);
    },
};

/** Encode one CallTarget. */
export function encodeCallTarget(writer: BinaryWriter, value: CallTarget): void {
    switch (value.kind) {
        case "import":
            writer.writeUnsigned(0);
            return;
        case "local":
            writer.writeUnsigned(1);
            writer.writeUnsigned(value.local);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one CallTarget. */
export function decodeCallTarget(reader: BinaryReader): CallTarget {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            return { kind: "import" };
        }
        case 1: {
            const local = reader.readNumber();

            return { kind: "local", local };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one CallTarget. */
export function toJsonCallTarget(value: CallTarget): Json {
    switch (value.kind) {
        case "import":
            return {
                kind: "import",
            };
        case "local":
            return {
                kind: "local",
                local: value.local,
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one CallTarget from one JSON value. */
export function fromJsonCallTarget(value: Json): CallTarget {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "import":
            return {
                kind,
            };
        case "local":
            return {
                kind,
                local: jsonInteger(jsonField(object, "local")),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** One lowered switch case. */
export type SwitchCase = {
    /** Match value. */
    readonly value: bigint;
    /** Target block. */
    readonly target: number;
    /** Block parameter moves. */
    readonly moves: MoveRange;
};

export const SwitchCase = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: SwitchCase): void {
        encodeSwitchCase(writer, value);
    },

    /** Decode one SwitchCase. */
    decode(reader: BinaryReader): SwitchCase {
        return decodeSwitchCase(reader);
    },

    /** Return this value as JSON. */
    toJson(value: SwitchCase): Json {
        return toJsonSwitchCase(value);
    },

    /** Return one SwitchCase from one JSON value. */
    fromJson(value: Json): SwitchCase {
        return fromJsonSwitchCase(value);
    },
};

/** Encode one SwitchCase. */
export function encodeSwitchCase(writer: BinaryWriter, value: SwitchCase): void {
    writer.writeSigned(value.value);
    writer.writeUnsigned(value.target);
    encodeMoveRange(writer, value.moves);
}

/** Decode one SwitchCase. */
export function decodeSwitchCase(reader: BinaryReader): SwitchCase {
    const value = reader.readSigned();
    const target = reader.readNumber();
    const moves = decodeMoveRange(reader);

    return {
        value,
        target,
        moves,
    };
}

/** Return one JSON value for one SwitchCase. */
export function toJsonSwitchCase(value: SwitchCase): Json {
    return {
        value: value.value.toString(),
        target: value.target,
        moves: toJsonMoveRange(value.moves),
    };
}

/** Return one SwitchCase from one JSON value. */
export function fromJsonSwitchCase(value: Json): SwitchCase {
    const object = jsonObject(value);

    return {
        value: jsonBigint(jsonField(object, "value")),
        target: jsonInteger(jsonField(object, "target")),
        moves: fromJsonMoveRange(jsonField(object, "moves")),
    };
}
