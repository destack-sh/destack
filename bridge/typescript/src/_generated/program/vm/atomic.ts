/* generated by destack_bridge_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, jsonBool, jsonField, jsonObject, jsonString } from "../../../protocol/serde.js";

/** Address, width, and ordering for one atomic memory operation. */
export type AtomicShape = {
    /** The addressed memory space. */
    readonly address: AtomicAddress;
    /** The atomic payload width. */
    readonly width: AtomicWidth;
    /** The memory ordering. */
    readonly order: AtomicOrder;
    /** Whether integer results should be sign-extended. */
    readonly isSigned: boolean;
};

export const AtomicShape = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: AtomicShape): void {
        encodeAtomicShape(writer, value);
    },

    /** Decode one AtomicShape. */
    decode(reader: BinaryReader): AtomicShape {
        return decodeAtomicShape(reader);
    },

    /** Return this value as JSON. */
    toJson(value: AtomicShape): Json {
        return toJsonAtomicShape(value);
    },

    /** Return one AtomicShape from one JSON value. */
    fromJson(value: Json): AtomicShape {
        return fromJsonAtomicShape(value);
    },
};

/** Encode one AtomicShape. */
export function encodeAtomicShape(writer: BinaryWriter, value: AtomicShape): void {
    encodeAtomicAddress(writer, value.address);
    encodeAtomicWidth(writer, value.width);
    encodeAtomicOrder(writer, value.order);
    writer.writeBool(value.isSigned);
}

/** Decode one AtomicShape. */
export function decodeAtomicShape(reader: BinaryReader): AtomicShape {
    const address = decodeAtomicAddress(reader);
    const width = decodeAtomicWidth(reader);
    const order = decodeAtomicOrder(reader);
    const isSigned = reader.readBool();

    return {
        address,
        width,
        order,
        isSigned,
    };
}

/** Return one JSON value for one AtomicShape. */
export function toJsonAtomicShape(value: AtomicShape): Json {
    return {
        address: toJsonAtomicAddress(value.address),
        width: toJsonAtomicWidth(value.width),
        order: toJsonAtomicOrder(value.order),
        isSigned: value.isSigned,
    };
}

/** Return one AtomicShape from one JSON value. */
export function fromJsonAtomicShape(value: Json): AtomicShape {
    const object = jsonObject(value);

    return {
        address: fromJsonAtomicAddress(jsonField(object, "address")),
        width: fromJsonAtomicWidth(jsonField(object, "width")),
        order: fromJsonAtomicOrder(jsonField(object, "order")),
        isSigned: jsonBool(jsonField(object, "isSigned")),
    };
}

/** Memory address representation selected by atomic lowering. */
export type AtomicAddress = "heap" | "sharedHeap" | "address" | "stack" | "frame" | "static";

export const AtomicAddress = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: AtomicAddress): void {
        encodeAtomicAddress(writer, value);
    },

    /** Decode one AtomicAddress. */
    decode(reader: BinaryReader): AtomicAddress {
        return decodeAtomicAddress(reader);
    },

    /** Return this value as JSON. */
    toJson(value: AtomicAddress): Json {
        return toJsonAtomicAddress(value);
    },

    /** Return one AtomicAddress from one JSON value. */
    fromJson(value: Json): AtomicAddress {
        return fromJsonAtomicAddress(value);
    },
};

/** Encode one AtomicAddress. */
export function encodeAtomicAddress(writer: BinaryWriter, value: AtomicAddress): void {
    switch (value) {
        case "heap":
            writer.writeUnsigned(0);
            return;
        case "sharedHeap":
            writer.writeUnsigned(1);
            return;
        case "address":
            writer.writeUnsigned(2);
            return;
        case "stack":
            writer.writeUnsigned(3);
            return;
        case "frame":
            writer.writeUnsigned(4);
            return;
        case "static":
            writer.writeUnsigned(5);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one AtomicAddress. */
export function decodeAtomicAddress(reader: BinaryReader): AtomicAddress {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "heap";
        case 1:
            return "sharedHeap";
        case 2:
            return "address";
        case 3:
            return "stack";
        case 4:
            return "frame";
        case 5:
            return "static";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one AtomicAddress. */
export function toJsonAtomicAddress(value: AtomicAddress): Json {
    return value;
}

/** Return one AtomicAddress from one JSON value. */
export function fromJsonAtomicAddress(value: Json): AtomicAddress {
    const variant = jsonString(value);

    switch (variant) {
        case "heap":
            return "heap";
        case "sharedHeap":
            return "sharedHeap";
        case "address":
            return "address";
        case "stack":
            return "stack";
        case "frame":
            return "frame";
        case "static":
            return "static";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** Atomic payload width selected by lowering. */
export type AtomicWidth = "width8" | "width16" | "width32" | "width64";

export const AtomicWidth = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: AtomicWidth): void {
        encodeAtomicWidth(writer, value);
    },

    /** Decode one AtomicWidth. */
    decode(reader: BinaryReader): AtomicWidth {
        return decodeAtomicWidth(reader);
    },

    /** Return this value as JSON. */
    toJson(value: AtomicWidth): Json {
        return toJsonAtomicWidth(value);
    },

    /** Return one AtomicWidth from one JSON value. */
    fromJson(value: Json): AtomicWidth {
        return fromJsonAtomicWidth(value);
    },
};

/** Encode one AtomicWidth. */
export function encodeAtomicWidth(writer: BinaryWriter, value: AtomicWidth): void {
    switch (value) {
        case "width8":
            writer.writeUnsigned(0);
            return;
        case "width16":
            writer.writeUnsigned(1);
            return;
        case "width32":
            writer.writeUnsigned(2);
            return;
        case "width64":
            writer.writeUnsigned(3);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one AtomicWidth. */
export function decodeAtomicWidth(reader: BinaryReader): AtomicWidth {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "width8";
        case 1:
            return "width16";
        case 2:
            return "width32";
        case 3:
            return "width64";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one AtomicWidth. */
export function toJsonAtomicWidth(value: AtomicWidth): Json {
    return value;
}

/** Return one AtomicWidth from one JSON value. */
export function fromJsonAtomicWidth(value: Json): AtomicWidth {
    const variant = jsonString(value);

    switch (variant) {
        case "width8":
            return "width8";
        case "width16":
            return "width16";
        case "width32":
            return "width32";
        case "width64":
            return "width64";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** Atomic memory ordering selected by lowering. */
export type AtomicOrder = "relaxed" | "acquire" | "release" | "acquireRelease" | "sequentiallyConsistent";

export const AtomicOrder = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: AtomicOrder): void {
        encodeAtomicOrder(writer, value);
    },

    /** Decode one AtomicOrder. */
    decode(reader: BinaryReader): AtomicOrder {
        return decodeAtomicOrder(reader);
    },

    /** Return this value as JSON. */
    toJson(value: AtomicOrder): Json {
        return toJsonAtomicOrder(value);
    },

    /** Return one AtomicOrder from one JSON value. */
    fromJson(value: Json): AtomicOrder {
        return fromJsonAtomicOrder(value);
    },
};

/** Encode one AtomicOrder. */
export function encodeAtomicOrder(writer: BinaryWriter, value: AtomicOrder): void {
    switch (value) {
        case "relaxed":
            writer.writeUnsigned(0);
            return;
        case "acquire":
            writer.writeUnsigned(1);
            return;
        case "release":
            writer.writeUnsigned(2);
            return;
        case "acquireRelease":
            writer.writeUnsigned(3);
            return;
        case "sequentiallyConsistent":
            writer.writeUnsigned(4);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one AtomicOrder. */
export function decodeAtomicOrder(reader: BinaryReader): AtomicOrder {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "relaxed";
        case 1:
            return "acquire";
        case 2:
            return "release";
        case 3:
            return "acquireRelease";
        case 4:
            return "sequentiallyConsistent";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one AtomicOrder. */
export function toJsonAtomicOrder(value: AtomicOrder): Json {
    return value;
}

/** Return one AtomicOrder from one JSON value. */
export function fromJsonAtomicOrder(value: Json): AtomicOrder {
    const variant = jsonString(value);

    switch (variant) {
        case "relaxed":
            return "relaxed";
        case "acquire":
            return "acquire";
        case "release":
            return "release";
        case "acquireRelease":
            return "acquireRelease";
        case "sequentiallyConsistent":
            return "sequentiallyConsistent";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}
