/* generated by destack_bridge_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, jsonArray, jsonBool, jsonField, jsonInteger, jsonObject, jsonString } from "../../../protocol/serde.js";
import type { FrameStateId } from "../../mir/metadata/frame.js";
import type { LocalNodeId } from "../../mir/tree/node.js";
import type { TensorConvertMode } from "../../mir/tree/tensor.js";
import type { TensorIndexReduceOperator } from "../../mir/tree/tensor.js";
import type { TensorIndexTieBreak } from "../../mir/tree/tensor.js";
import type { TensorReduceOperator } from "../../mir/tree/tensor.js";
import type { TensorScatterMode } from "../../mir/tree/tensor.js";
import type { Value } from "../../mir/tree/value.js";
import type { VectorConvertMode } from "../../mir/tree/vector.js";
import type { VectorReduceOperator } from "../../mir/tree/vector.js";
import type { AtomicOrder } from "./atomic.js";
import type { AtomicShape } from "./atomic.js";
import type { CallTarget } from "./function.js";
import type { Projection } from "./projection.js";
import type { ArgumentRange } from "./range.js";
import type { MoveRange } from "./range.js";
import type { ProjectionId } from "./table.js";
import type { TensorConvolutionId } from "./table.js";
import type { TensorDotId } from "./table.js";
import type { TensorGatherId } from "./table.js";
import type { TensorLayoutId } from "./table.js";
import type { TensorScatterId } from "./table.js";
import type { TensorWindowId } from "./table.js";
import type { U32RangeId } from "./table.js";
import type { TensorAddress } from "./tensor.js";
import type { CellLayout } from "./value.js";
import type { ScalarLayout } from "./value.js";
import type { ValueShape } from "./value.js";
import * as mirTreeIntrinsic from "../../mir/tree/intrinsic.js";
import { decodeFrameStateId, encodeFrameStateId, fromJsonFrameStateId, toJsonFrameStateId } from "../../mir/metadata/frame.js";
import { decodeLocalNodeId, encodeLocalNodeId, fromJsonLocalNodeId, toJsonLocalNodeId } from "../../mir/tree/node.js";
import { decodeTensorConvertMode, encodeTensorConvertMode, fromJsonTensorConvertMode, toJsonTensorConvertMode } from "../../mir/tree/tensor.js";
import { decodeTensorIndexReduceOperator, encodeTensorIndexReduceOperator, fromJsonTensorIndexReduceOperator, toJsonTensorIndexReduceOperator } from "../../mir/tree/tensor.js";
import { decodeTensorIndexTieBreak, encodeTensorIndexTieBreak, fromJsonTensorIndexTieBreak, toJsonTensorIndexTieBreak } from "../../mir/tree/tensor.js";
import { decodeTensorReduceOperator, encodeTensorReduceOperator, fromJsonTensorReduceOperator, toJsonTensorReduceOperator } from "../../mir/tree/tensor.js";
import { decodeTensorScatterMode, encodeTensorScatterMode, fromJsonTensorScatterMode, toJsonTensorScatterMode } from "../../mir/tree/tensor.js";
import { decodeValue, encodeValue, fromJsonValue, toJsonValue } from "../../mir/tree/value.js";
import { decodeVectorConvertMode, encodeVectorConvertMode, fromJsonVectorConvertMode, toJsonVectorConvertMode } from "../../mir/tree/vector.js";
import { decodeVectorReduceOperator, encodeVectorReduceOperator, fromJsonVectorReduceOperator, toJsonVectorReduceOperator } from "../../mir/tree/vector.js";
import { decodeAtomicOrder, encodeAtomicOrder, fromJsonAtomicOrder, toJsonAtomicOrder } from "./atomic.js";
import { decodeAtomicShape, encodeAtomicShape, fromJsonAtomicShape, toJsonAtomicShape } from "./atomic.js";
import { decodeCallTarget, encodeCallTarget, fromJsonCallTarget, toJsonCallTarget } from "./function.js";
import { decodeProjection, encodeProjection, fromJsonProjection, toJsonProjection } from "./projection.js";
import { decodeArgumentRange, encodeArgumentRange, fromJsonArgumentRange, toJsonArgumentRange } from "./range.js";
import { decodeMoveRange, encodeMoveRange, fromJsonMoveRange, toJsonMoveRange } from "./range.js";
import { decodeProjectionId, encodeProjectionId, fromJsonProjectionId, toJsonProjectionId } from "./table.js";
import { decodeTensorConvolutionId, encodeTensorConvolutionId, fromJsonTensorConvolutionId, toJsonTensorConvolutionId } from "./table.js";
import { decodeTensorDotId, encodeTensorDotId, fromJsonTensorDotId, toJsonTensorDotId } from "./table.js";
import { decodeTensorGatherId, encodeTensorGatherId, fromJsonTensorGatherId, toJsonTensorGatherId } from "./table.js";
import { decodeTensorLayoutId, encodeTensorLayoutId, fromJsonTensorLayoutId, toJsonTensorLayoutId } from "./table.js";
import { decodeTensorScatterId, encodeTensorScatterId, fromJsonTensorScatterId, toJsonTensorScatterId } from "./table.js";
import { decodeTensorWindowId, encodeTensorWindowId, fromJsonTensorWindowId, toJsonTensorWindowId } from "./table.js";
import { decodeU32RangeId, encodeU32RangeId, fromJsonU32RangeId, toJsonU32RangeId } from "./table.js";
import { decodeTensorAddress, encodeTensorAddress, fromJsonTensorAddress, toJsonTensorAddress } from "./tensor.js";
import { decodeCellLayout, encodeCellLayout, fromJsonCellLayout, toJsonCellLayout } from "./value.js";
import { decodeScalarLayout, encodeScalarLayout, fromJsonScalarLayout, toJsonScalarLayout } from "./value.js";
import { decodeValueShape, encodeValueShape, fromJsonValueShape, toJsonValueShape } from "./value.js";

/** Aggregate select operation. */
export type AggregateSelect = {
    /** The destination frame offset. */
    readonly destinationOffset: number;
    /** The condition cell offset. */
    readonly conditionOffset: number;
    /** The true source frame offset. */
    readonly thenOffset: number;
    /** The false source frame offset. */
    readonly elseOffset: number;
    /** The selected byte length. */
    readonly byteLen: number;
};

export const AggregateSelect = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: AggregateSelect): void {
        encodeAggregateSelect(writer, value);
    },

    /** Decode one AggregateSelect. */
    decode(reader: BinaryReader): AggregateSelect {
        return decodeAggregateSelect(reader);
    },

    /** Return this value as JSON. */
    toJson(value: AggregateSelect): Json {
        return toJsonAggregateSelect(value);
    },

    /** Return one AggregateSelect from one JSON value. */
    fromJson(value: Json): AggregateSelect {
        return fromJsonAggregateSelect(value);
    },
};

/** Encode one AggregateSelect. */
export function encodeAggregateSelect(writer: BinaryWriter, value: AggregateSelect): void {
    writer.writeUnsigned(value.destinationOffset);
    writer.writeUnsigned(value.conditionOffset);
    writer.writeUnsigned(value.thenOffset);
    writer.writeUnsigned(value.elseOffset);
    writer.writeUnsigned(value.byteLen);
}

/** Decode one AggregateSelect. */
export function decodeAggregateSelect(reader: BinaryReader): AggregateSelect {
    const destinationOffset = reader.readNumber();
    const conditionOffset = reader.readNumber();
    const thenOffset = reader.readNumber();
    const elseOffset = reader.readNumber();
    const byteLen = reader.readNumber();

    return {
        destinationOffset,
        conditionOffset,
        thenOffset,
        elseOffset,
        byteLen,
    };
}

/** Return one JSON value for one AggregateSelect. */
export function toJsonAggregateSelect(value: AggregateSelect): Json {
    return {
        destinationOffset: value.destinationOffset,
        conditionOffset: value.conditionOffset,
        thenOffset: value.thenOffset,
        elseOffset: value.elseOffset,
        byteLen: value.byteLen,
    };
}

/** Return one AggregateSelect from one JSON value. */
export function fromJsonAggregateSelect(value: Json): AggregateSelect {
    const object = jsonObject(value);

    return {
        destinationOffset: jsonInteger(jsonField(object, "destinationOffset")),
        conditionOffset: jsonInteger(jsonField(object, "conditionOffset")),
        thenOffset: jsonInteger(jsonField(object, "thenOffset")),
        elseOffset: jsonInteger(jsonField(object, "elseOffset")),
        byteLen: jsonInteger(jsonField(object, "byteLen")),
    };
}

/** Atomic compare exchange over one scalar value. */
export type AtomicCompareExchange = {
    /** The aggregate destination value. */
    readonly destination: Value;
    /** The pointer cell offset. */
    readonly pointerOffset: number;
    /** The expected value cell offset. */
    readonly expectedOffset: number;
    /** The replacement value cell offset. */
    readonly newValueOffset: number;
    /** The atomic memory shape. */
    readonly shape: AtomicShape;
    /** The failure ordering. */
    readonly failureOrder: AtomicOrder;
    /** Whether the compare exchange is weak. */
    readonly isWeak: boolean;
};

export const AtomicCompareExchange = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: AtomicCompareExchange): void {
        encodeAtomicCompareExchange(writer, value);
    },

    /** Decode one AtomicCompareExchange. */
    decode(reader: BinaryReader): AtomicCompareExchange {
        return decodeAtomicCompareExchange(reader);
    },

    /** Return this value as JSON. */
    toJson(value: AtomicCompareExchange): Json {
        return toJsonAtomicCompareExchange(value);
    },

    /** Return one AtomicCompareExchange from one JSON value. */
    fromJson(value: Json): AtomicCompareExchange {
        return fromJsonAtomicCompareExchange(value);
    },
};

/** Encode one AtomicCompareExchange. */
export function encodeAtomicCompareExchange(writer: BinaryWriter, value: AtomicCompareExchange): void {
    encodeValue(writer, value.destination);
    writer.writeUnsigned(value.pointerOffset);
    writer.writeUnsigned(value.expectedOffset);
    writer.writeUnsigned(value.newValueOffset);
    encodeAtomicShape(writer, value.shape);
    encodeAtomicOrder(writer, value.failureOrder);
    writer.writeBool(value.isWeak);
}

/** Decode one AtomicCompareExchange. */
export function decodeAtomicCompareExchange(reader: BinaryReader): AtomicCompareExchange {
    const destination = decodeValue(reader);
    const pointerOffset = reader.readNumber();
    const expectedOffset = reader.readNumber();
    const newValueOffset = reader.readNumber();
    const shape = decodeAtomicShape(reader);
    const failureOrder = decodeAtomicOrder(reader);
    const isWeak = reader.readBool();

    return {
        destination,
        pointerOffset,
        expectedOffset,
        newValueOffset,
        shape,
        failureOrder,
        isWeak,
    };
}

/** Return one JSON value for one AtomicCompareExchange. */
export function toJsonAtomicCompareExchange(value: AtomicCompareExchange): Json {
    return {
        destination: toJsonValue(value.destination),
        pointerOffset: value.pointerOffset,
        expectedOffset: value.expectedOffset,
        newValueOffset: value.newValueOffset,
        shape: toJsonAtomicShape(value.shape),
        failureOrder: toJsonAtomicOrder(value.failureOrder),
        isWeak: value.isWeak,
    };
}

/** Return one AtomicCompareExchange from one JSON value. */
export function fromJsonAtomicCompareExchange(value: Json): AtomicCompareExchange {
    const object = jsonObject(value);

    return {
        destination: fromJsonValue(jsonField(object, "destination")),
        pointerOffset: jsonInteger(jsonField(object, "pointerOffset")),
        expectedOffset: jsonInteger(jsonField(object, "expectedOffset")),
        newValueOffset: jsonInteger(jsonField(object, "newValueOffset")),
        shape: fromJsonAtomicShape(jsonField(object, "shape")),
        failureOrder: fromJsonAtomicOrder(jsonField(object, "failureOrder")),
        isWeak: jsonBool(jsonField(object, "isWeak")),
    };
}

/** Vector splat operation. */
export type VectorSplat = {
    /** The destination frame offset. */
    readonly destOffset: number;
    /** The splatted value cell offset. */
    readonly valueOffset: number;
    /** The destination element projection. */
    readonly destElement: Projection;
    /** The destination element count. */
    readonly elementCount: number;
};

export const VectorSplat = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: VectorSplat): void {
        encodeVectorSplat(writer, value);
    },

    /** Decode one VectorSplat. */
    decode(reader: BinaryReader): VectorSplat {
        return decodeVectorSplat(reader);
    },

    /** Return this value as JSON. */
    toJson(value: VectorSplat): Json {
        return toJsonVectorSplat(value);
    },

    /** Return one VectorSplat from one JSON value. */
    fromJson(value: Json): VectorSplat {
        return fromJsonVectorSplat(value);
    },
};

/** Encode one VectorSplat. */
export function encodeVectorSplat(writer: BinaryWriter, value: VectorSplat): void {
    writer.writeUnsigned(value.destOffset);
    writer.writeUnsigned(value.valueOffset);
    encodeProjection(writer, value.destElement);
    writer.writeUnsigned(value.elementCount);
}

/** Decode one VectorSplat. */
export function decodeVectorSplat(reader: BinaryReader): VectorSplat {
    const destOffset = reader.readNumber();
    const valueOffset = reader.readNumber();
    const destElement = decodeProjection(reader);
    const elementCount = reader.readNumber();

    return {
        destOffset,
        valueOffset,
        destElement,
        elementCount,
    };
}

/** Return one JSON value for one VectorSplat. */
export function toJsonVectorSplat(value: VectorSplat): Json {
    return {
        destOffset: value.destOffset,
        valueOffset: value.valueOffset,
        destElement: toJsonProjection(value.destElement),
        elementCount: value.elementCount,
    };
}

/** Return one VectorSplat from one JSON value. */
export function fromJsonVectorSplat(value: Json): VectorSplat {
    const object = jsonObject(value);

    return {
        destOffset: jsonInteger(jsonField(object, "destOffset")),
        valueOffset: jsonInteger(jsonField(object, "valueOffset")),
        destElement: fromJsonProjection(jsonField(object, "destElement")),
        elementCount: jsonInteger(jsonField(object, "elementCount")),
    };
}

/** Vector extract operation. */
export type VectorExtract = {
    /** The destination cell offset. */
    readonly destOffset: number;
    /** The source vector frame offset. */
    readonly vectorOffset: number;
    /** The index cell offset. */
    readonly indexOffset: number;
    /** The source element projection. */
    readonly vectorElement: Projection;
    /** The source element count. */
    readonly elementCount: number;
};

export const VectorExtract = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: VectorExtract): void {
        encodeVectorExtract(writer, value);
    },

    /** Decode one VectorExtract. */
    decode(reader: BinaryReader): VectorExtract {
        return decodeVectorExtract(reader);
    },

    /** Return this value as JSON. */
    toJson(value: VectorExtract): Json {
        return toJsonVectorExtract(value);
    },

    /** Return one VectorExtract from one JSON value. */
    fromJson(value: Json): VectorExtract {
        return fromJsonVectorExtract(value);
    },
};

/** Encode one VectorExtract. */
export function encodeVectorExtract(writer: BinaryWriter, value: VectorExtract): void {
    writer.writeUnsigned(value.destOffset);
    writer.writeUnsigned(value.vectorOffset);
    writer.writeUnsigned(value.indexOffset);
    encodeProjection(writer, value.vectorElement);
    writer.writeUnsigned(value.elementCount);
}

/** Decode one VectorExtract. */
export function decodeVectorExtract(reader: BinaryReader): VectorExtract {
    const destOffset = reader.readNumber();
    const vectorOffset = reader.readNumber();
    const indexOffset = reader.readNumber();
    const vectorElement = decodeProjection(reader);
    const elementCount = reader.readNumber();

    return {
        destOffset,
        vectorOffset,
        indexOffset,
        vectorElement,
        elementCount,
    };
}

/** Return one JSON value for one VectorExtract. */
export function toJsonVectorExtract(value: VectorExtract): Json {
    return {
        destOffset: value.destOffset,
        vectorOffset: value.vectorOffset,
        indexOffset: value.indexOffset,
        vectorElement: toJsonProjection(value.vectorElement),
        elementCount: value.elementCount,
    };
}

/** Return one VectorExtract from one JSON value. */
export function fromJsonVectorExtract(value: Json): VectorExtract {
    const object = jsonObject(value);

    return {
        destOffset: jsonInteger(jsonField(object, "destOffset")),
        vectorOffset: jsonInteger(jsonField(object, "vectorOffset")),
        indexOffset: jsonInteger(jsonField(object, "indexOffset")),
        vectorElement: fromJsonProjection(jsonField(object, "vectorElement")),
        elementCount: jsonInteger(jsonField(object, "elementCount")),
    };
}

/** Elementwise vector binary operation. */
export type VectorBinary = {
    /** The destination frame offset. */
    readonly destOffset: number;
    /** The left vector frame offset. */
    readonly leftOffset: number;
    /** The right vector frame offset. */
    readonly rightOffset: number;
    /** The destination element projection. */
    readonly destElement: Projection;
    /** The left source element projection. */
    readonly leftElement: Projection;
    /** The right source element projection. */
    readonly rightElement: Projection;
    /** The element binary kernel. */
    readonly kernel: ElementBinaryKernel;
    /** The vector element scalar layout. */
    readonly elementLayout: ScalarLayout;
    /** The destination element count. */
    readonly elementCount: number;
};

export const VectorBinary = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: VectorBinary): void {
        encodeVectorBinary(writer, value);
    },

    /** Decode one VectorBinary. */
    decode(reader: BinaryReader): VectorBinary {
        return decodeVectorBinary(reader);
    },

    /** Return this value as JSON. */
    toJson(value: VectorBinary): Json {
        return toJsonVectorBinary(value);
    },

    /** Return one VectorBinary from one JSON value. */
    fromJson(value: Json): VectorBinary {
        return fromJsonVectorBinary(value);
    },
};

/** Encode one VectorBinary. */
export function encodeVectorBinary(writer: BinaryWriter, value: VectorBinary): void {
    writer.writeUnsigned(value.destOffset);
    writer.writeUnsigned(value.leftOffset);
    writer.writeUnsigned(value.rightOffset);
    encodeProjection(writer, value.destElement);
    encodeProjection(writer, value.leftElement);
    encodeProjection(writer, value.rightElement);
    encodeElementBinaryKernel(writer, value.kernel);
    encodeScalarLayout(writer, value.elementLayout);
    writer.writeUnsigned(value.elementCount);
}

/** Decode one VectorBinary. */
export function decodeVectorBinary(reader: BinaryReader): VectorBinary {
    const destOffset = reader.readNumber();
    const leftOffset = reader.readNumber();
    const rightOffset = reader.readNumber();
    const destElement = decodeProjection(reader);
    const leftElement = decodeProjection(reader);
    const rightElement = decodeProjection(reader);
    const kernel = decodeElementBinaryKernel(reader);
    const elementLayout = decodeScalarLayout(reader);
    const elementCount = reader.readNumber();

    return {
        destOffset,
        leftOffset,
        rightOffset,
        destElement,
        leftElement,
        rightElement,
        kernel,
        elementLayout,
        elementCount,
    };
}

/** Return one JSON value for one VectorBinary. */
export function toJsonVectorBinary(value: VectorBinary): Json {
    return {
        destOffset: value.destOffset,
        leftOffset: value.leftOffset,
        rightOffset: value.rightOffset,
        destElement: toJsonProjection(value.destElement),
        leftElement: toJsonProjection(value.leftElement),
        rightElement: toJsonProjection(value.rightElement),
        kernel: toJsonElementBinaryKernel(value.kernel),
        elementLayout: toJsonScalarLayout(value.elementLayout),
        elementCount: value.elementCount,
    };
}

/** Return one VectorBinary from one JSON value. */
export function fromJsonVectorBinary(value: Json): VectorBinary {
    const object = jsonObject(value);

    return {
        destOffset: jsonInteger(jsonField(object, "destOffset")),
        leftOffset: jsonInteger(jsonField(object, "leftOffset")),
        rightOffset: jsonInteger(jsonField(object, "rightOffset")),
        destElement: fromJsonProjection(jsonField(object, "destElement")),
        leftElement: fromJsonProjection(jsonField(object, "leftElement")),
        rightElement: fromJsonProjection(jsonField(object, "rightElement")),
        kernel: fromJsonElementBinaryKernel(jsonField(object, "kernel")),
        elementLayout: fromJsonScalarLayout(jsonField(object, "elementLayout")),
        elementCount: jsonInteger(jsonField(object, "elementCount")),
    };
}

/** Elementwise scalar binary kernel. */
export type ElementBinaryKernel = "andBool" | "orBool" | "xorBool" | "eqBool" | "neBool" | "addInt" | "subInt" | "mulInt" | "divInt" | "divUint" | "remInt" | "remUint" | "andInt" | "orInt" | "xorInt" | "shlInt" | "shrInt" | "shrUint" | "eqInt" | "neInt" | "ltInt" | "ltUint" | "leInt" | "leUint" | "gtInt" | "gtUint" | "geInt" | "geUint" | "addF32" | "subF32" | "mulF32" | "divF32" | "addF64" | "subF64" | "mulF64" | "divF64" | "eqF32" | "eqF64" | "neF32" | "neF64" | "ltF32" | "ltF64" | "leF32" | "leF64" | "gtF32" | "gtF64" | "geF32" | "geF64" | "addFloat" | "subFloat" | "mulFloat" | "divFloat" | "eqFloat" | "neFloat" | "ltFloat" | "leFloat" | "gtFloat" | "geFloat";

export const ElementBinaryKernel = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ElementBinaryKernel): void {
        encodeElementBinaryKernel(writer, value);
    },

    /** Decode one ElementBinaryKernel. */
    decode(reader: BinaryReader): ElementBinaryKernel {
        return decodeElementBinaryKernel(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ElementBinaryKernel): Json {
        return toJsonElementBinaryKernel(value);
    },

    /** Return one ElementBinaryKernel from one JSON value. */
    fromJson(value: Json): ElementBinaryKernel {
        return fromJsonElementBinaryKernel(value);
    },
};

/** Encode one ElementBinaryKernel. */
export function encodeElementBinaryKernel(writer: BinaryWriter, value: ElementBinaryKernel): void {
    switch (value) {
        case "andBool":
            writer.writeUnsigned(0);
            return;
        case "orBool":
            writer.writeUnsigned(1);
            return;
        case "xorBool":
            writer.writeUnsigned(2);
            return;
        case "eqBool":
            writer.writeUnsigned(3);
            return;
        case "neBool":
            writer.writeUnsigned(4);
            return;
        case "addInt":
            writer.writeUnsigned(5);
            return;
        case "subInt":
            writer.writeUnsigned(6);
            return;
        case "mulInt":
            writer.writeUnsigned(7);
            return;
        case "divInt":
            writer.writeUnsigned(8);
            return;
        case "divUint":
            writer.writeUnsigned(9);
            return;
        case "remInt":
            writer.writeUnsigned(10);
            return;
        case "remUint":
            writer.writeUnsigned(11);
            return;
        case "andInt":
            writer.writeUnsigned(12);
            return;
        case "orInt":
            writer.writeUnsigned(13);
            return;
        case "xorInt":
            writer.writeUnsigned(14);
            return;
        case "shlInt":
            writer.writeUnsigned(15);
            return;
        case "shrInt":
            writer.writeUnsigned(16);
            return;
        case "shrUint":
            writer.writeUnsigned(17);
            return;
        case "eqInt":
            writer.writeUnsigned(18);
            return;
        case "neInt":
            writer.writeUnsigned(19);
            return;
        case "ltInt":
            writer.writeUnsigned(20);
            return;
        case "ltUint":
            writer.writeUnsigned(21);
            return;
        case "leInt":
            writer.writeUnsigned(22);
            return;
        case "leUint":
            writer.writeUnsigned(23);
            return;
        case "gtInt":
            writer.writeUnsigned(24);
            return;
        case "gtUint":
            writer.writeUnsigned(25);
            return;
        case "geInt":
            writer.writeUnsigned(26);
            return;
        case "geUint":
            writer.writeUnsigned(27);
            return;
        case "addF32":
            writer.writeUnsigned(28);
            return;
        case "subF32":
            writer.writeUnsigned(29);
            return;
        case "mulF32":
            writer.writeUnsigned(30);
            return;
        case "divF32":
            writer.writeUnsigned(31);
            return;
        case "addF64":
            writer.writeUnsigned(32);
            return;
        case "subF64":
            writer.writeUnsigned(33);
            return;
        case "mulF64":
            writer.writeUnsigned(34);
            return;
        case "divF64":
            writer.writeUnsigned(35);
            return;
        case "eqF32":
            writer.writeUnsigned(36);
            return;
        case "eqF64":
            writer.writeUnsigned(37);
            return;
        case "neF32":
            writer.writeUnsigned(38);
            return;
        case "neF64":
            writer.writeUnsigned(39);
            return;
        case "ltF32":
            writer.writeUnsigned(40);
            return;
        case "ltF64":
            writer.writeUnsigned(41);
            return;
        case "leF32":
            writer.writeUnsigned(42);
            return;
        case "leF64":
            writer.writeUnsigned(43);
            return;
        case "gtF32":
            writer.writeUnsigned(44);
            return;
        case "gtF64":
            writer.writeUnsigned(45);
            return;
        case "geF32":
            writer.writeUnsigned(46);
            return;
        case "geF64":
            writer.writeUnsigned(47);
            return;
        case "addFloat":
            writer.writeUnsigned(48);
            return;
        case "subFloat":
            writer.writeUnsigned(49);
            return;
        case "mulFloat":
            writer.writeUnsigned(50);
            return;
        case "divFloat":
            writer.writeUnsigned(51);
            return;
        case "eqFloat":
            writer.writeUnsigned(52);
            return;
        case "neFloat":
            writer.writeUnsigned(53);
            return;
        case "ltFloat":
            writer.writeUnsigned(54);
            return;
        case "leFloat":
            writer.writeUnsigned(55);
            return;
        case "gtFloat":
            writer.writeUnsigned(56);
            return;
        case "geFloat":
            writer.writeUnsigned(57);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one ElementBinaryKernel. */
export function decodeElementBinaryKernel(reader: BinaryReader): ElementBinaryKernel {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "andBool";
        case 1:
            return "orBool";
        case 2:
            return "xorBool";
        case 3:
            return "eqBool";
        case 4:
            return "neBool";
        case 5:
            return "addInt";
        case 6:
            return "subInt";
        case 7:
            return "mulInt";
        case 8:
            return "divInt";
        case 9:
            return "divUint";
        case 10:
            return "remInt";
        case 11:
            return "remUint";
        case 12:
            return "andInt";
        case 13:
            return "orInt";
        case 14:
            return "xorInt";
        case 15:
            return "shlInt";
        case 16:
            return "shrInt";
        case 17:
            return "shrUint";
        case 18:
            return "eqInt";
        case 19:
            return "neInt";
        case 20:
            return "ltInt";
        case 21:
            return "ltUint";
        case 22:
            return "leInt";
        case 23:
            return "leUint";
        case 24:
            return "gtInt";
        case 25:
            return "gtUint";
        case 26:
            return "geInt";
        case 27:
            return "geUint";
        case 28:
            return "addF32";
        case 29:
            return "subF32";
        case 30:
            return "mulF32";
        case 31:
            return "divF32";
        case 32:
            return "addF64";
        case 33:
            return "subF64";
        case 34:
            return "mulF64";
        case 35:
            return "divF64";
        case 36:
            return "eqF32";
        case 37:
            return "eqF64";
        case 38:
            return "neF32";
        case 39:
            return "neF64";
        case 40:
            return "ltF32";
        case 41:
            return "ltF64";
        case 42:
            return "leF32";
        case 43:
            return "leF64";
        case 44:
            return "gtF32";
        case 45:
            return "gtF64";
        case 46:
            return "geF32";
        case 47:
            return "geF64";
        case 48:
            return "addFloat";
        case 49:
            return "subFloat";
        case 50:
            return "mulFloat";
        case 51:
            return "divFloat";
        case 52:
            return "eqFloat";
        case 53:
            return "neFloat";
        case 54:
            return "ltFloat";
        case 55:
            return "leFloat";
        case 56:
            return "gtFloat";
        case 57:
            return "geFloat";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one ElementBinaryKernel. */
export function toJsonElementBinaryKernel(value: ElementBinaryKernel): Json {
    return value;
}

/** Return one ElementBinaryKernel from one JSON value. */
export function fromJsonElementBinaryKernel(value: Json): ElementBinaryKernel {
    const variant = jsonString(value);

    switch (variant) {
        case "andBool":
            return "andBool";
        case "orBool":
            return "orBool";
        case "xorBool":
            return "xorBool";
        case "eqBool":
            return "eqBool";
        case "neBool":
            return "neBool";
        case "addInt":
            return "addInt";
        case "subInt":
            return "subInt";
        case "mulInt":
            return "mulInt";
        case "divInt":
            return "divInt";
        case "divUint":
            return "divUint";
        case "remInt":
            return "remInt";
        case "remUint":
            return "remUint";
        case "andInt":
            return "andInt";
        case "orInt":
            return "orInt";
        case "xorInt":
            return "xorInt";
        case "shlInt":
            return "shlInt";
        case "shrInt":
            return "shrInt";
        case "shrUint":
            return "shrUint";
        case "eqInt":
            return "eqInt";
        case "neInt":
            return "neInt";
        case "ltInt":
            return "ltInt";
        case "ltUint":
            return "ltUint";
        case "leInt":
            return "leInt";
        case "leUint":
            return "leUint";
        case "gtInt":
            return "gtInt";
        case "gtUint":
            return "gtUint";
        case "geInt":
            return "geInt";
        case "geUint":
            return "geUint";
        case "addF32":
            return "addF32";
        case "subF32":
            return "subF32";
        case "mulF32":
            return "mulF32";
        case "divF32":
            return "divF32";
        case "addF64":
            return "addF64";
        case "subF64":
            return "subF64";
        case "mulF64":
            return "mulF64";
        case "divF64":
            return "divF64";
        case "eqF32":
            return "eqF32";
        case "eqF64":
            return "eqF64";
        case "neF32":
            return "neF32";
        case "neF64":
            return "neF64";
        case "ltF32":
            return "ltF32";
        case "ltF64":
            return "ltF64";
        case "leF32":
            return "leF32";
        case "leF64":
            return "leF64";
        case "gtF32":
            return "gtF32";
        case "gtF64":
            return "gtF64";
        case "geF32":
            return "geF32";
        case "geF64":
            return "geF64";
        case "addFloat":
            return "addFloat";
        case "subFloat":
            return "subFloat";
        case "mulFloat":
            return "mulFloat";
        case "divFloat":
            return "divFloat";
        case "eqFloat":
            return "eqFloat";
        case "neFloat":
            return "neFloat";
        case "ltFloat":
            return "ltFloat";
        case "leFloat":
            return "leFloat";
        case "gtFloat":
            return "gtFloat";
        case "geFloat":
            return "geFloat";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** Elementwise vector unary operation. */
export type VectorUnary = {
    /** The destination frame offset. */
    readonly destOffset: number;
    /** The argument vector frame offset. */
    readonly argumentOffset: number;
    /** The destination element projection. */
    readonly destElement: Projection;
    /** The argument element projection. */
    readonly argumentElement: Projection;
    /** The element unary kernel. */
    readonly kernel: ElementUnaryKernel;
    /** The vector element scalar layout. */
    readonly elementLayout: ScalarLayout;
    /** The destination element count. */
    readonly elementCount: number;
};

export const VectorUnary = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: VectorUnary): void {
        encodeVectorUnary(writer, value);
    },

    /** Decode one VectorUnary. */
    decode(reader: BinaryReader): VectorUnary {
        return decodeVectorUnary(reader);
    },

    /** Return this value as JSON. */
    toJson(value: VectorUnary): Json {
        return toJsonVectorUnary(value);
    },

    /** Return one VectorUnary from one JSON value. */
    fromJson(value: Json): VectorUnary {
        return fromJsonVectorUnary(value);
    },
};

/** Encode one VectorUnary. */
export function encodeVectorUnary(writer: BinaryWriter, value: VectorUnary): void {
    writer.writeUnsigned(value.destOffset);
    writer.writeUnsigned(value.argumentOffset);
    encodeProjection(writer, value.destElement);
    encodeProjection(writer, value.argumentElement);
    encodeElementUnaryKernel(writer, value.kernel);
    encodeScalarLayout(writer, value.elementLayout);
    writer.writeUnsigned(value.elementCount);
}

/** Decode one VectorUnary. */
export function decodeVectorUnary(reader: BinaryReader): VectorUnary {
    const destOffset = reader.readNumber();
    const argumentOffset = reader.readNumber();
    const destElement = decodeProjection(reader);
    const argumentElement = decodeProjection(reader);
    const kernel = decodeElementUnaryKernel(reader);
    const elementLayout = decodeScalarLayout(reader);
    const elementCount = reader.readNumber();

    return {
        destOffset,
        argumentOffset,
        destElement,
        argumentElement,
        kernel,
        elementLayout,
        elementCount,
    };
}

/** Return one JSON value for one VectorUnary. */
export function toJsonVectorUnary(value: VectorUnary): Json {
    return {
        destOffset: value.destOffset,
        argumentOffset: value.argumentOffset,
        destElement: toJsonProjection(value.destElement),
        argumentElement: toJsonProjection(value.argumentElement),
        kernel: toJsonElementUnaryKernel(value.kernel),
        elementLayout: toJsonScalarLayout(value.elementLayout),
        elementCount: value.elementCount,
    };
}

/** Return one VectorUnary from one JSON value. */
export function fromJsonVectorUnary(value: Json): VectorUnary {
    const object = jsonObject(value);

    return {
        destOffset: jsonInteger(jsonField(object, "destOffset")),
        argumentOffset: jsonInteger(jsonField(object, "argumentOffset")),
        destElement: fromJsonProjection(jsonField(object, "destElement")),
        argumentElement: fromJsonProjection(jsonField(object, "argumentElement")),
        kernel: fromJsonElementUnaryKernel(jsonField(object, "kernel")),
        elementLayout: fromJsonScalarLayout(jsonField(object, "elementLayout")),
        elementCount: jsonInteger(jsonField(object, "elementCount")),
    };
}

/** Elementwise scalar unary kernel. */
export type ElementUnaryKernel = "notBool" | "negInt" | "notInt" | "negF32" | "negF64" | "negFloat";

export const ElementUnaryKernel = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ElementUnaryKernel): void {
        encodeElementUnaryKernel(writer, value);
    },

    /** Decode one ElementUnaryKernel. */
    decode(reader: BinaryReader): ElementUnaryKernel {
        return decodeElementUnaryKernel(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ElementUnaryKernel): Json {
        return toJsonElementUnaryKernel(value);
    },

    /** Return one ElementUnaryKernel from one JSON value. */
    fromJson(value: Json): ElementUnaryKernel {
        return fromJsonElementUnaryKernel(value);
    },
};

/** Encode one ElementUnaryKernel. */
export function encodeElementUnaryKernel(writer: BinaryWriter, value: ElementUnaryKernel): void {
    switch (value) {
        case "notBool":
            writer.writeUnsigned(0);
            return;
        case "negInt":
            writer.writeUnsigned(1);
            return;
        case "notInt":
            writer.writeUnsigned(2);
            return;
        case "negF32":
            writer.writeUnsigned(3);
            return;
        case "negF64":
            writer.writeUnsigned(4);
            return;
        case "negFloat":
            writer.writeUnsigned(5);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one ElementUnaryKernel. */
export function decodeElementUnaryKernel(reader: BinaryReader): ElementUnaryKernel {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "notBool";
        case 1:
            return "negInt";
        case 2:
            return "notInt";
        case 3:
            return "negF32";
        case 4:
            return "negF64";
        case 5:
            return "negFloat";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one ElementUnaryKernel. */
export function toJsonElementUnaryKernel(value: ElementUnaryKernel): Json {
    return value;
}

/** Return one ElementUnaryKernel from one JSON value. */
export function fromJsonElementUnaryKernel(value: Json): ElementUnaryKernel {
    const variant = jsonString(value);

    switch (variant) {
        case "notBool":
            return "notBool";
        case "negInt":
            return "negInt";
        case "notInt":
            return "notInt";
        case "negF32":
            return "negF32";
        case "negF64":
            return "negF64";
        case "negFloat":
            return "negFloat";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** Vector insert operation. */
export type VectorInsert = {
    /** The destination frame offset. */
    readonly destOffset: number;
    /** The source vector frame offset. */
    readonly vectorOffset: number;
    /** The index cell offset. */
    readonly indexOffset: number;
    /** The inserted value cell offset. */
    readonly valueOffset: number;
    /** The destination element projection. */
    readonly destElement: Projection;
    /** The source element projection. */
    readonly vectorElement: Projection;
    /** The source vector element count. */
    readonly elementCount: number;
};

export const VectorInsert = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: VectorInsert): void {
        encodeVectorInsert(writer, value);
    },

    /** Decode one VectorInsert. */
    decode(reader: BinaryReader): VectorInsert {
        return decodeVectorInsert(reader);
    },

    /** Return this value as JSON. */
    toJson(value: VectorInsert): Json {
        return toJsonVectorInsert(value);
    },

    /** Return one VectorInsert from one JSON value. */
    fromJson(value: Json): VectorInsert {
        return fromJsonVectorInsert(value);
    },
};

/** Encode one VectorInsert. */
export function encodeVectorInsert(writer: BinaryWriter, value: VectorInsert): void {
    writer.writeUnsigned(value.destOffset);
    writer.writeUnsigned(value.vectorOffset);
    writer.writeUnsigned(value.indexOffset);
    writer.writeUnsigned(value.valueOffset);
    encodeProjection(writer, value.destElement);
    encodeProjection(writer, value.vectorElement);
    writer.writeUnsigned(value.elementCount);
}

/** Decode one VectorInsert. */
export function decodeVectorInsert(reader: BinaryReader): VectorInsert {
    const destOffset = reader.readNumber();
    const vectorOffset = reader.readNumber();
    const indexOffset = reader.readNumber();
    const valueOffset = reader.readNumber();
    const destElement = decodeProjection(reader);
    const vectorElement = decodeProjection(reader);
    const elementCount = reader.readNumber();

    return {
        destOffset,
        vectorOffset,
        indexOffset,
        valueOffset,
        destElement,
        vectorElement,
        elementCount,
    };
}

/** Return one JSON value for one VectorInsert. */
export function toJsonVectorInsert(value: VectorInsert): Json {
    return {
        destOffset: value.destOffset,
        vectorOffset: value.vectorOffset,
        indexOffset: value.indexOffset,
        valueOffset: value.valueOffset,
        destElement: toJsonProjection(value.destElement),
        vectorElement: toJsonProjection(value.vectorElement),
        elementCount: value.elementCount,
    };
}

/** Return one VectorInsert from one JSON value. */
export function fromJsonVectorInsert(value: Json): VectorInsert {
    const object = jsonObject(value);

    return {
        destOffset: jsonInteger(jsonField(object, "destOffset")),
        vectorOffset: jsonInteger(jsonField(object, "vectorOffset")),
        indexOffset: jsonInteger(jsonField(object, "indexOffset")),
        valueOffset: jsonInteger(jsonField(object, "valueOffset")),
        destElement: fromJsonProjection(jsonField(object, "destElement")),
        vectorElement: fromJsonProjection(jsonField(object, "vectorElement")),
        elementCount: jsonInteger(jsonField(object, "elementCount")),
    };
}

/** Vector shuffle operation. */
export type VectorShuffle = {
    /** The destination frame offset. */
    readonly destOffset: number;
    /** The left source frame offset. */
    readonly leftOffset: number;
    /** The right source frame offset. */
    readonly rightOffset: number;
    /** The pooled shuffle mask. */
    readonly mask: U32RangeId;
    /** The destination element projection. */
    readonly destElement: Projection;
    /** The left source element projection. */
    readonly leftElement: Projection;
    /** The right source element projection. */
    readonly rightElement: Projection;
    /** The left source element count. */
    readonly leftCount: number;
    /** The right source element count. */
    readonly rightCount: number;
};

export const VectorShuffle = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: VectorShuffle): void {
        encodeVectorShuffle(writer, value);
    },

    /** Decode one VectorShuffle. */
    decode(reader: BinaryReader): VectorShuffle {
        return decodeVectorShuffle(reader);
    },

    /** Return this value as JSON. */
    toJson(value: VectorShuffle): Json {
        return toJsonVectorShuffle(value);
    },

    /** Return one VectorShuffle from one JSON value. */
    fromJson(value: Json): VectorShuffle {
        return fromJsonVectorShuffle(value);
    },
};

/** Encode one VectorShuffle. */
export function encodeVectorShuffle(writer: BinaryWriter, value: VectorShuffle): void {
    writer.writeUnsigned(value.destOffset);
    writer.writeUnsigned(value.leftOffset);
    writer.writeUnsigned(value.rightOffset);
    encodeU32RangeId(writer, value.mask);
    encodeProjection(writer, value.destElement);
    encodeProjection(writer, value.leftElement);
    encodeProjection(writer, value.rightElement);
    writer.writeUnsigned(value.leftCount);
    writer.writeUnsigned(value.rightCount);
}

/** Decode one VectorShuffle. */
export function decodeVectorShuffle(reader: BinaryReader): VectorShuffle {
    const destOffset = reader.readNumber();
    const leftOffset = reader.readNumber();
    const rightOffset = reader.readNumber();
    const mask = decodeU32RangeId(reader);
    const destElement = decodeProjection(reader);
    const leftElement = decodeProjection(reader);
    const rightElement = decodeProjection(reader);
    const leftCount = reader.readNumber();
    const rightCount = reader.readNumber();

    return {
        destOffset,
        leftOffset,
        rightOffset,
        mask,
        destElement,
        leftElement,
        rightElement,
        leftCount,
        rightCount,
    };
}

/** Return one JSON value for one VectorShuffle. */
export function toJsonVectorShuffle(value: VectorShuffle): Json {
    return {
        destOffset: value.destOffset,
        leftOffset: value.leftOffset,
        rightOffset: value.rightOffset,
        mask: toJsonU32RangeId(value.mask),
        destElement: toJsonProjection(value.destElement),
        leftElement: toJsonProjection(value.leftElement),
        rightElement: toJsonProjection(value.rightElement),
        leftCount: value.leftCount,
        rightCount: value.rightCount,
    };
}

/** Return one VectorShuffle from one JSON value. */
export function fromJsonVectorShuffle(value: Json): VectorShuffle {
    const object = jsonObject(value);

    return {
        destOffset: jsonInteger(jsonField(object, "destOffset")),
        leftOffset: jsonInteger(jsonField(object, "leftOffset")),
        rightOffset: jsonInteger(jsonField(object, "rightOffset")),
        mask: fromJsonU32RangeId(jsonField(object, "mask")),
        destElement: fromJsonProjection(jsonField(object, "destElement")),
        leftElement: fromJsonProjection(jsonField(object, "leftElement")),
        rightElement: fromJsonProjection(jsonField(object, "rightElement")),
        leftCount: jsonInteger(jsonField(object, "leftCount")),
        rightCount: jsonInteger(jsonField(object, "rightCount")),
    };
}

/** Vector select operation. */
export type VectorSelect = {
    /** The destination frame offset. */
    readonly destOffset: number;
    /** The mask vector frame offset. */
    readonly maskOffset: number;
    /** The true branch frame offset. */
    readonly thenOffset: number;
    /** The false branch frame offset. */
    readonly elseOffset: number;
    /** The destination element projection. */
    readonly destElement: Projection;
    /** The mask element projection. */
    readonly maskElement: Projection;
    /** The true branch element projection. */
    readonly thenElement: Projection;
    /** The false branch element projection. */
    readonly elseElement: Projection;
    /** The destination element count. */
    readonly elementCount: number;
};

export const VectorSelect = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: VectorSelect): void {
        encodeVectorSelect(writer, value);
    },

    /** Decode one VectorSelect. */
    decode(reader: BinaryReader): VectorSelect {
        return decodeVectorSelect(reader);
    },

    /** Return this value as JSON. */
    toJson(value: VectorSelect): Json {
        return toJsonVectorSelect(value);
    },

    /** Return one VectorSelect from one JSON value. */
    fromJson(value: Json): VectorSelect {
        return fromJsonVectorSelect(value);
    },
};

/** Encode one VectorSelect. */
export function encodeVectorSelect(writer: BinaryWriter, value: VectorSelect): void {
    writer.writeUnsigned(value.destOffset);
    writer.writeUnsigned(value.maskOffset);
    writer.writeUnsigned(value.thenOffset);
    writer.writeUnsigned(value.elseOffset);
    encodeProjection(writer, value.destElement);
    encodeProjection(writer, value.maskElement);
    encodeProjection(writer, value.thenElement);
    encodeProjection(writer, value.elseElement);
    writer.writeUnsigned(value.elementCount);
}

/** Decode one VectorSelect. */
export function decodeVectorSelect(reader: BinaryReader): VectorSelect {
    const destOffset = reader.readNumber();
    const maskOffset = reader.readNumber();
    const thenOffset = reader.readNumber();
    const elseOffset = reader.readNumber();
    const destElement = decodeProjection(reader);
    const maskElement = decodeProjection(reader);
    const thenElement = decodeProjection(reader);
    const elseElement = decodeProjection(reader);
    const elementCount = reader.readNumber();

    return {
        destOffset,
        maskOffset,
        thenOffset,
        elseOffset,
        destElement,
        maskElement,
        thenElement,
        elseElement,
        elementCount,
    };
}

/** Return one JSON value for one VectorSelect. */
export function toJsonVectorSelect(value: VectorSelect): Json {
    return {
        destOffset: value.destOffset,
        maskOffset: value.maskOffset,
        thenOffset: value.thenOffset,
        elseOffset: value.elseOffset,
        destElement: toJsonProjection(value.destElement),
        maskElement: toJsonProjection(value.maskElement),
        thenElement: toJsonProjection(value.thenElement),
        elseElement: toJsonProjection(value.elseElement),
        elementCount: value.elementCount,
    };
}

/** Return one VectorSelect from one JSON value. */
export function fromJsonVectorSelect(value: Json): VectorSelect {
    const object = jsonObject(value);

    return {
        destOffset: jsonInteger(jsonField(object, "destOffset")),
        maskOffset: jsonInteger(jsonField(object, "maskOffset")),
        thenOffset: jsonInteger(jsonField(object, "thenOffset")),
        elseOffset: jsonInteger(jsonField(object, "elseOffset")),
        destElement: fromJsonProjection(jsonField(object, "destElement")),
        maskElement: fromJsonProjection(jsonField(object, "maskElement")),
        thenElement: fromJsonProjection(jsonField(object, "thenElement")),
        elseElement: fromJsonProjection(jsonField(object, "elseElement")),
        elementCount: jsonInteger(jsonField(object, "elementCount")),
    };
}

/** Vector reduction operation. */
export type VectorReduce = {
    /** The destination cell offset. */
    readonly destOffset: number;
    /** The source vector frame offset. */
    readonly vectorOffset: number;
    /** The reduction kernel. */
    readonly kernel: VectorReduceOperator;
    /** The source element projection. */
    readonly vectorElement: Projection;
    /** The vector element scalar layout. */
    readonly elementLayout: ScalarLayout;
    /** The source vector element count. */
    readonly elementCount: number;
};

export const VectorReduce = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: VectorReduce): void {
        encodeVectorReduce(writer, value);
    },

    /** Decode one VectorReduce. */
    decode(reader: BinaryReader): VectorReduce {
        return decodeVectorReduce(reader);
    },

    /** Return this value as JSON. */
    toJson(value: VectorReduce): Json {
        return toJsonVectorReduce(value);
    },

    /** Return one VectorReduce from one JSON value. */
    fromJson(value: Json): VectorReduce {
        return fromJsonVectorReduce(value);
    },
};

/** Encode one VectorReduce. */
export function encodeVectorReduce(writer: BinaryWriter, value: VectorReduce): void {
    writer.writeUnsigned(value.destOffset);
    writer.writeUnsigned(value.vectorOffset);
    encodeVectorReduceOperator(writer, value.kernel);
    encodeProjection(writer, value.vectorElement);
    encodeScalarLayout(writer, value.elementLayout);
    writer.writeUnsigned(value.elementCount);
}

/** Decode one VectorReduce. */
export function decodeVectorReduce(reader: BinaryReader): VectorReduce {
    const destOffset = reader.readNumber();
    const vectorOffset = reader.readNumber();
    const kernel = decodeVectorReduceOperator(reader);
    const vectorElement = decodeProjection(reader);
    const elementLayout = decodeScalarLayout(reader);
    const elementCount = reader.readNumber();

    return {
        destOffset,
        vectorOffset,
        kernel,
        vectorElement,
        elementLayout,
        elementCount,
    };
}

/** Return one JSON value for one VectorReduce. */
export function toJsonVectorReduce(value: VectorReduce): Json {
    return {
        destOffset: value.destOffset,
        vectorOffset: value.vectorOffset,
        kernel: toJsonVectorReduceOperator(value.kernel),
        vectorElement: toJsonProjection(value.vectorElement),
        elementLayout: toJsonScalarLayout(value.elementLayout),
        elementCount: value.elementCount,
    };
}

/** Return one VectorReduce from one JSON value. */
export function fromJsonVectorReduce(value: Json): VectorReduce {
    const object = jsonObject(value);

    return {
        destOffset: jsonInteger(jsonField(object, "destOffset")),
        vectorOffset: jsonInteger(jsonField(object, "vectorOffset")),
        kernel: fromJsonVectorReduceOperator(jsonField(object, "kernel")),
        vectorElement: fromJsonProjection(jsonField(object, "vectorElement")),
        elementLayout: fromJsonScalarLayout(jsonField(object, "elementLayout")),
        elementCount: jsonInteger(jsonField(object, "elementCount")),
    };
}

/** Vector conversion operation. */
export type VectorConvert = {
    /** The destination frame offset. */
    readonly destOffset: number;
    /** The source vector frame offset. */
    readonly vectorOffset: number;
    /** The conversion kernel. */
    readonly mode: VectorConvertMode;
    /** The destination element projection. */
    readonly destElement: Projection;
    /** The source element projection. */
    readonly sourceElement: Projection;
    /** The destination element scalar layout. */
    readonly destLayout: ScalarLayout;
    /** The source element scalar layout. */
    readonly sourceLayout: ScalarLayout;
    /** The destination element count. */
    readonly elementCount: number;
};

export const VectorConvert = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: VectorConvert): void {
        encodeVectorConvert(writer, value);
    },

    /** Decode one VectorConvert. */
    decode(reader: BinaryReader): VectorConvert {
        return decodeVectorConvert(reader);
    },

    /** Return this value as JSON. */
    toJson(value: VectorConvert): Json {
        return toJsonVectorConvert(value);
    },

    /** Return one VectorConvert from one JSON value. */
    fromJson(value: Json): VectorConvert {
        return fromJsonVectorConvert(value);
    },
};

/** Encode one VectorConvert. */
export function encodeVectorConvert(writer: BinaryWriter, value: VectorConvert): void {
    writer.writeUnsigned(value.destOffset);
    writer.writeUnsigned(value.vectorOffset);
    encodeVectorConvertMode(writer, value.mode);
    encodeProjection(writer, value.destElement);
    encodeProjection(writer, value.sourceElement);
    encodeScalarLayout(writer, value.destLayout);
    encodeScalarLayout(writer, value.sourceLayout);
    writer.writeUnsigned(value.elementCount);
}

/** Decode one VectorConvert. */
export function decodeVectorConvert(reader: BinaryReader): VectorConvert {
    const destOffset = reader.readNumber();
    const vectorOffset = reader.readNumber();
    const mode = decodeVectorConvertMode(reader);
    const destElement = decodeProjection(reader);
    const sourceElement = decodeProjection(reader);
    const destLayout = decodeScalarLayout(reader);
    const sourceLayout = decodeScalarLayout(reader);
    const elementCount = reader.readNumber();

    return {
        destOffset,
        vectorOffset,
        mode,
        destElement,
        sourceElement,
        destLayout,
        sourceLayout,
        elementCount,
    };
}

/** Return one JSON value for one VectorConvert. */
export function toJsonVectorConvert(value: VectorConvert): Json {
    return {
        destOffset: value.destOffset,
        vectorOffset: value.vectorOffset,
        mode: toJsonVectorConvertMode(value.mode),
        destElement: toJsonProjection(value.destElement),
        sourceElement: toJsonProjection(value.sourceElement),
        destLayout: toJsonScalarLayout(value.destLayout),
        sourceLayout: toJsonScalarLayout(value.sourceLayout),
        elementCount: value.elementCount,
    };
}

/** Return one VectorConvert from one JSON value. */
export function fromJsonVectorConvert(value: Json): VectorConvert {
    const object = jsonObject(value);

    return {
        destOffset: jsonInteger(jsonField(object, "destOffset")),
        vectorOffset: jsonInteger(jsonField(object, "vectorOffset")),
        mode: fromJsonVectorConvertMode(jsonField(object, "mode")),
        destElement: fromJsonProjection(jsonField(object, "destElement")),
        sourceElement: fromJsonProjection(jsonField(object, "sourceElement")),
        destLayout: fromJsonScalarLayout(jsonField(object, "destLayout")),
        sourceLayout: fromJsonScalarLayout(jsonField(object, "sourceLayout")),
        elementCount: jsonInteger(jsonField(object, "elementCount")),
    };
}

/** Function bind operation. */
export type FunctionBind = {
    /** The environment cell layout. */
    readonly environment: CellLayout;
};

export const FunctionBind = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: FunctionBind): void {
        encodeFunctionBind(writer, value);
    },

    /** Decode one FunctionBind. */
    decode(reader: BinaryReader): FunctionBind {
        return decodeFunctionBind(reader);
    },

    /** Return this value as JSON. */
    toJson(value: FunctionBind): Json {
        return toJsonFunctionBind(value);
    },

    /** Return one FunctionBind from one JSON value. */
    fromJson(value: Json): FunctionBind {
        return fromJsonFunctionBind(value);
    },
};

/** Encode one FunctionBind. */
export function encodeFunctionBind(writer: BinaryWriter, value: FunctionBind): void {
    encodeCellLayout(writer, value.environment);
}

/** Decode one FunctionBind. */
export function decodeFunctionBind(reader: BinaryReader): FunctionBind {
    const environment = decodeCellLayout(reader);

    return {
        environment,
    };
}

/** Return one JSON value for one FunctionBind. */
export function toJsonFunctionBind(value: FunctionBind): Json {
    return {
        environment: toJsonCellLayout(value.environment),
    };
}

/** Return one FunctionBind from one JSON value. */
export function fromJsonFunctionBind(value: Json): FunctionBind {
    const object = jsonObject(value);

    return {
        environment: fromJsonCellLayout(jsonField(object, "environment")),
    };
}

/** Direct function call. */
export type Call = {
    /** The callee function index. */
    readonly function: number;
    /** The resolved call target. */
    readonly target: CallTarget;
    /** The pooled argument range. */
    readonly arguments: ArgumentRange;
    /** The argument frame moves. */
    readonly moves: MoveRange;
};

export const Call = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: Call): void {
        encodeCall(writer, value);
    },

    /** Decode one Call. */
    decode(reader: BinaryReader): Call {
        return decodeCall(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Call): Json {
        return toJsonCall(value);
    },

    /** Return one Call from one JSON value. */
    fromJson(value: Json): Call {
        return fromJsonCall(value);
    },
};

/** Encode one Call. */
export function encodeCall(writer: BinaryWriter, value: Call): void {
    writer.writeUnsigned(value.function);
    encodeCallTarget(writer, value.target);
    encodeArgumentRange(writer, value.arguments);
    encodeMoveRange(writer, value.moves);
}

/** Decode one Call. */
export function decodeCall(reader: BinaryReader): Call {
    const function_ = reader.readNumber();
    const target = decodeCallTarget(reader);
    const arguments_ = decodeArgumentRange(reader);
    const moves = decodeMoveRange(reader);

    return {
        function: function_,
        target,
        arguments: arguments_,
        moves,
    };
}

/** Return one JSON value for one Call. */
export function toJsonCall(value: Call): Json {
    return {
        function: value.function,
        target: toJsonCallTarget(value.target),
        arguments: toJsonArgumentRange(value.arguments),
        moves: toJsonMoveRange(value.moves),
    };
}

/** Return one Call from one JSON value. */
export function fromJsonCall(value: Json): Call {
    const object = jsonObject(value);

    return {
        function: jsonInteger(jsonField(object, "function")),
        target: fromJsonCallTarget(jsonField(object, "target")),
        arguments: fromJsonArgumentRange(jsonField(object, "arguments")),
        moves: fromJsonMoveRange(jsonField(object, "moves")),
    };
}

/** Function call terminator with an explicit continuation. */
export type CallBranch = {
    /** The callee function index. */
    readonly function: number;
    /** The resolved call target. */
    readonly target: CallTarget;
    /** The pooled argument range. */
    readonly arguments: ArgumentRange;
    /** The continuation frame state. */
    readonly targetState: FrameStateId;
};

export const CallBranch = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: CallBranch): void {
        encodeCallBranch(writer, value);
    },

    /** Decode one CallBranch. */
    decode(reader: BinaryReader): CallBranch {
        return decodeCallBranch(reader);
    },

    /** Return this value as JSON. */
    toJson(value: CallBranch): Json {
        return toJsonCallBranch(value);
    },

    /** Return one CallBranch from one JSON value. */
    fromJson(value: Json): CallBranch {
        return fromJsonCallBranch(value);
    },
};

/** Encode one CallBranch. */
export function encodeCallBranch(writer: BinaryWriter, value: CallBranch): void {
    writer.writeUnsigned(value.function);
    encodeCallTarget(writer, value.target);
    encodeArgumentRange(writer, value.arguments);
    encodeFrameStateId(writer, value.targetState);
}

/** Decode one CallBranch. */
export function decodeCallBranch(reader: BinaryReader): CallBranch {
    const function_ = reader.readNumber();
    const target = decodeCallTarget(reader);
    const arguments_ = decodeArgumentRange(reader);
    const targetState = decodeFrameStateId(reader);

    return {
        function: function_,
        target,
        arguments: arguments_,
        targetState,
    };
}

/** Return one JSON value for one CallBranch. */
export function toJsonCallBranch(value: CallBranch): Json {
    return {
        function: value.function,
        target: toJsonCallTarget(value.target),
        arguments: toJsonArgumentRange(value.arguments),
        targetState: toJsonFrameStateId(value.targetState),
    };
}

/** Return one CallBranch from one JSON value. */
export function fromJsonCallBranch(value: Json): CallBranch {
    const object = jsonObject(value);

    return {
        function: jsonInteger(jsonField(object, "function")),
        target: fromJsonCallTarget(jsonField(object, "target")),
        arguments: fromJsonArgumentRange(jsonField(object, "arguments")),
        targetState: fromJsonFrameStateId(jsonField(object, "targetState")),
    };
}

/** Class method call. */
export type CallVirtual = {
    /** The receiver cell offset. */
    readonly receiverOffset: number;
    /** The dispatch table field projection. */
    readonly tableField: ProjectionId;
    /** The dispatch table slot. */
    readonly slot: number;
    /** The pooled argument range. */
    readonly arguments: ArgumentRange;
};

export const CallVirtual = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: CallVirtual): void {
        encodeCallVirtual(writer, value);
    },

    /** Decode one CallVirtual. */
    decode(reader: BinaryReader): CallVirtual {
        return decodeCallVirtual(reader);
    },

    /** Return this value as JSON. */
    toJson(value: CallVirtual): Json {
        return toJsonCallVirtual(value);
    },

    /** Return one CallVirtual from one JSON value. */
    fromJson(value: Json): CallVirtual {
        return fromJsonCallVirtual(value);
    },
};

/** Encode one CallVirtual. */
export function encodeCallVirtual(writer: BinaryWriter, value: CallVirtual): void {
    writer.writeUnsigned(value.receiverOffset);
    encodeProjectionId(writer, value.tableField);
    writer.writeUnsigned(value.slot);
    encodeArgumentRange(writer, value.arguments);
}

/** Decode one CallVirtual. */
export function decodeCallVirtual(reader: BinaryReader): CallVirtual {
    const receiverOffset = reader.readNumber();
    const tableField = decodeProjectionId(reader);
    const slot = reader.readNumber();
    const arguments_ = decodeArgumentRange(reader);

    return {
        receiverOffset,
        tableField,
        slot,
        arguments: arguments_,
    };
}

/** Return one JSON value for one CallVirtual. */
export function toJsonCallVirtual(value: CallVirtual): Json {
    return {
        receiverOffset: value.receiverOffset,
        tableField: toJsonProjectionId(value.tableField),
        slot: value.slot,
        arguments: toJsonArgumentRange(value.arguments),
    };
}

/** Return one CallVirtual from one JSON value. */
export function fromJsonCallVirtual(value: Json): CallVirtual {
    const object = jsonObject(value);

    return {
        receiverOffset: jsonInteger(jsonField(object, "receiverOffset")),
        tableField: fromJsonProjectionId(jsonField(object, "tableField")),
        slot: jsonInteger(jsonField(object, "slot")),
        arguments: fromJsonArgumentRange(jsonField(object, "arguments")),
    };
}

/** Class method call terminator with an explicit continuation. */
export type CallVirtualBranch = {
    /** The receiver cell offset. */
    readonly receiverOffset: number;
    /** The dispatch table field projection. */
    readonly tableField: ProjectionId;
    /** The dispatch table slot. */
    readonly slot: number;
    /** The pooled argument range. */
    readonly arguments: ArgumentRange;
    /** The continuation frame state. */
    readonly targetState: FrameStateId;
};

export const CallVirtualBranch = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: CallVirtualBranch): void {
        encodeCallVirtualBranch(writer, value);
    },

    /** Decode one CallVirtualBranch. */
    decode(reader: BinaryReader): CallVirtualBranch {
        return decodeCallVirtualBranch(reader);
    },

    /** Return this value as JSON. */
    toJson(value: CallVirtualBranch): Json {
        return toJsonCallVirtualBranch(value);
    },

    /** Return one CallVirtualBranch from one JSON value. */
    fromJson(value: Json): CallVirtualBranch {
        return fromJsonCallVirtualBranch(value);
    },
};

/** Encode one CallVirtualBranch. */
export function encodeCallVirtualBranch(writer: BinaryWriter, value: CallVirtualBranch): void {
    writer.writeUnsigned(value.receiverOffset);
    encodeProjectionId(writer, value.tableField);
    writer.writeUnsigned(value.slot);
    encodeArgumentRange(writer, value.arguments);
    encodeFrameStateId(writer, value.targetState);
}

/** Decode one CallVirtualBranch. */
export function decodeCallVirtualBranch(reader: BinaryReader): CallVirtualBranch {
    const receiverOffset = reader.readNumber();
    const tableField = decodeProjectionId(reader);
    const slot = reader.readNumber();
    const arguments_ = decodeArgumentRange(reader);
    const targetState = decodeFrameStateId(reader);

    return {
        receiverOffset,
        tableField,
        slot,
        arguments: arguments_,
        targetState,
    };
}

/** Return one JSON value for one CallVirtualBranch. */
export function toJsonCallVirtualBranch(value: CallVirtualBranch): Json {
    return {
        receiverOffset: value.receiverOffset,
        tableField: toJsonProjectionId(value.tableField),
        slot: value.slot,
        arguments: toJsonArgumentRange(value.arguments),
        targetState: toJsonFrameStateId(value.targetState),
    };
}

/** Return one CallVirtualBranch from one JSON value. */
export function fromJsonCallVirtualBranch(value: Json): CallVirtualBranch {
    const object = jsonObject(value);

    return {
        receiverOffset: jsonInteger(jsonField(object, "receiverOffset")),
        tableField: fromJsonProjectionId(jsonField(object, "tableField")),
        slot: jsonInteger(jsonField(object, "slot")),
        arguments: fromJsonArgumentRange(jsonField(object, "arguments")),
        targetState: fromJsonFrameStateId(jsonField(object, "targetState")),
    };
}

/** Dynamic method call. */
export type CallDynamic = {
    /** The receiver cell offset. */
    readonly receiverOffset: number;
    /** The dynamic table field projection. */
    readonly tableField: ProjectionId;
    /** The dynamic table slot. */
    readonly slot: number;
    /** The pooled argument range. */
    readonly arguments: ArgumentRange;
};

export const CallDynamic = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: CallDynamic): void {
        encodeCallDynamic(writer, value);
    },

    /** Decode one CallDynamic. */
    decode(reader: BinaryReader): CallDynamic {
        return decodeCallDynamic(reader);
    },

    /** Return this value as JSON. */
    toJson(value: CallDynamic): Json {
        return toJsonCallDynamic(value);
    },

    /** Return one CallDynamic from one JSON value. */
    fromJson(value: Json): CallDynamic {
        return fromJsonCallDynamic(value);
    },
};

/** Encode one CallDynamic. */
export function encodeCallDynamic(writer: BinaryWriter, value: CallDynamic): void {
    writer.writeUnsigned(value.receiverOffset);
    encodeProjectionId(writer, value.tableField);
    writer.writeUnsigned(value.slot);
    encodeArgumentRange(writer, value.arguments);
}

/** Decode one CallDynamic. */
export function decodeCallDynamic(reader: BinaryReader): CallDynamic {
    const receiverOffset = reader.readNumber();
    const tableField = decodeProjectionId(reader);
    const slot = reader.readNumber();
    const arguments_ = decodeArgumentRange(reader);

    return {
        receiverOffset,
        tableField,
        slot,
        arguments: arguments_,
    };
}

/** Return one JSON value for one CallDynamic. */
export function toJsonCallDynamic(value: CallDynamic): Json {
    return {
        receiverOffset: value.receiverOffset,
        tableField: toJsonProjectionId(value.tableField),
        slot: value.slot,
        arguments: toJsonArgumentRange(value.arguments),
    };
}

/** Return one CallDynamic from one JSON value. */
export function fromJsonCallDynamic(value: Json): CallDynamic {
    const object = jsonObject(value);

    return {
        receiverOffset: jsonInteger(jsonField(object, "receiverOffset")),
        tableField: fromJsonProjectionId(jsonField(object, "tableField")),
        slot: jsonInteger(jsonField(object, "slot")),
        arguments: fromJsonArgumentRange(jsonField(object, "arguments")),
    };
}

/** Dynamic method call terminator with an explicit continuation. */
export type CallDynamicBranch = {
    /** The receiver cell offset. */
    readonly receiverOffset: number;
    /** The dynamic table field projection. */
    readonly tableField: ProjectionId;
    /** The dynamic table slot. */
    readonly slot: number;
    /** The pooled argument range. */
    readonly arguments: ArgumentRange;
    /** The continuation frame state. */
    readonly targetState: FrameStateId;
};

export const CallDynamicBranch = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: CallDynamicBranch): void {
        encodeCallDynamicBranch(writer, value);
    },

    /** Decode one CallDynamicBranch. */
    decode(reader: BinaryReader): CallDynamicBranch {
        return decodeCallDynamicBranch(reader);
    },

    /** Return this value as JSON. */
    toJson(value: CallDynamicBranch): Json {
        return toJsonCallDynamicBranch(value);
    },

    /** Return one CallDynamicBranch from one JSON value. */
    fromJson(value: Json): CallDynamicBranch {
        return fromJsonCallDynamicBranch(value);
    },
};

/** Encode one CallDynamicBranch. */
export function encodeCallDynamicBranch(writer: BinaryWriter, value: CallDynamicBranch): void {
    writer.writeUnsigned(value.receiverOffset);
    encodeProjectionId(writer, value.tableField);
    writer.writeUnsigned(value.slot);
    encodeArgumentRange(writer, value.arguments);
    encodeFrameStateId(writer, value.targetState);
}

/** Decode one CallDynamicBranch. */
export function decodeCallDynamicBranch(reader: BinaryReader): CallDynamicBranch {
    const receiverOffset = reader.readNumber();
    const tableField = decodeProjectionId(reader);
    const slot = reader.readNumber();
    const arguments_ = decodeArgumentRange(reader);
    const targetState = decodeFrameStateId(reader);

    return {
        receiverOffset,
        tableField,
        slot,
        arguments: arguments_,
        targetState,
    };
}

/** Return one JSON value for one CallDynamicBranch. */
export function toJsonCallDynamicBranch(value: CallDynamicBranch): Json {
    return {
        receiverOffset: value.receiverOffset,
        tableField: toJsonProjectionId(value.tableField),
        slot: value.slot,
        arguments: toJsonArgumentRange(value.arguments),
        targetState: toJsonFrameStateId(value.targetState),
    };
}

/** Return one CallDynamicBranch from one JSON value. */
export function fromJsonCallDynamicBranch(value: Json): CallDynamicBranch {
    const object = jsonObject(value);

    return {
        receiverOffset: jsonInteger(jsonField(object, "receiverOffset")),
        tableField: fromJsonProjectionId(jsonField(object, "tableField")),
        slot: jsonInteger(jsonField(object, "slot")),
        arguments: fromJsonArgumentRange(jsonField(object, "arguments")),
        targetState: fromJsonFrameStateId(jsonField(object, "targetState")),
    };
}

/** Indirect function call. */
export type IndirectCall = {
    /** The callee cell offset. */
    readonly calleeOffset: number;
    /** The expected function signature. */
    readonly signature: LocalNodeId;
    /** The pooled argument range. */
    readonly arguments: ArgumentRange;
};

export const IndirectCall = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: IndirectCall): void {
        encodeIndirectCall(writer, value);
    },

    /** Decode one IndirectCall. */
    decode(reader: BinaryReader): IndirectCall {
        return decodeIndirectCall(reader);
    },

    /** Return this value as JSON. */
    toJson(value: IndirectCall): Json {
        return toJsonIndirectCall(value);
    },

    /** Return one IndirectCall from one JSON value. */
    fromJson(value: Json): IndirectCall {
        return fromJsonIndirectCall(value);
    },
};

/** Encode one IndirectCall. */
export function encodeIndirectCall(writer: BinaryWriter, value: IndirectCall): void {
    writer.writeUnsigned(value.calleeOffset);
    encodeLocalNodeId(writer, value.signature);
    encodeArgumentRange(writer, value.arguments);
}

/** Decode one IndirectCall. */
export function decodeIndirectCall(reader: BinaryReader): IndirectCall {
    const calleeOffset = reader.readNumber();
    const signature = decodeLocalNodeId(reader);
    const arguments_ = decodeArgumentRange(reader);

    return {
        calleeOffset,
        signature,
        arguments: arguments_,
    };
}

/** Return one JSON value for one IndirectCall. */
export function toJsonIndirectCall(value: IndirectCall): Json {
    return {
        calleeOffset: value.calleeOffset,
        signature: toJsonLocalNodeId(value.signature),
        arguments: toJsonArgumentRange(value.arguments),
    };
}

/** Return one IndirectCall from one JSON value. */
export function fromJsonIndirectCall(value: Json): IndirectCall {
    const object = jsonObject(value);

    return {
        calleeOffset: jsonInteger(jsonField(object, "calleeOffset")),
        signature: fromJsonLocalNodeId(jsonField(object, "signature")),
        arguments: fromJsonArgumentRange(jsonField(object, "arguments")),
    };
}

/** Indirect call terminator with an explicit continuation. */
export type IndirectCallBranch = {
    /** The callee cell offset. */
    readonly calleeOffset: number;
    /** The expected function signature. */
    readonly signature: LocalNodeId;
    /** The pooled argument range. */
    readonly arguments: ArgumentRange;
    /** The continuation frame state. */
    readonly targetState: FrameStateId;
};

export const IndirectCallBranch = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: IndirectCallBranch): void {
        encodeIndirectCallBranch(writer, value);
    },

    /** Decode one IndirectCallBranch. */
    decode(reader: BinaryReader): IndirectCallBranch {
        return decodeIndirectCallBranch(reader);
    },

    /** Return this value as JSON. */
    toJson(value: IndirectCallBranch): Json {
        return toJsonIndirectCallBranch(value);
    },

    /** Return one IndirectCallBranch from one JSON value. */
    fromJson(value: Json): IndirectCallBranch {
        return fromJsonIndirectCallBranch(value);
    },
};

/** Encode one IndirectCallBranch. */
export function encodeIndirectCallBranch(writer: BinaryWriter, value: IndirectCallBranch): void {
    writer.writeUnsigned(value.calleeOffset);
    encodeLocalNodeId(writer, value.signature);
    encodeArgumentRange(writer, value.arguments);
    encodeFrameStateId(writer, value.targetState);
}

/** Decode one IndirectCallBranch. */
export function decodeIndirectCallBranch(reader: BinaryReader): IndirectCallBranch {
    const calleeOffset = reader.readNumber();
    const signature = decodeLocalNodeId(reader);
    const arguments_ = decodeArgumentRange(reader);
    const targetState = decodeFrameStateId(reader);

    return {
        calleeOffset,
        signature,
        arguments: arguments_,
        targetState,
    };
}

/** Return one JSON value for one IndirectCallBranch. */
export function toJsonIndirectCallBranch(value: IndirectCallBranch): Json {
    return {
        calleeOffset: value.calleeOffset,
        signature: toJsonLocalNodeId(value.signature),
        arguments: toJsonArgumentRange(value.arguments),
        targetState: toJsonFrameStateId(value.targetState),
    };
}

/** Return one IndirectCallBranch from one JSON value. */
export function fromJsonIndirectCallBranch(value: Json): IndirectCallBranch {
    const object = jsonObject(value);

    return {
        calleeOffset: jsonInteger(jsonField(object, "calleeOffset")),
        signature: fromJsonLocalNodeId(jsonField(object, "signature")),
        arguments: fromJsonArgumentRange(jsonField(object, "arguments")),
        targetState: fromJsonFrameStateId(jsonField(object, "targetState")),
    };
}

/** Load a tensor element from a view. */
export type TensorLoad = {
    /** The destination scalar. */
    readonly destOffset: number;
    /** The source tensor view. */
    readonly viewOffset: number;
    /** The index frame offsets. */
    readonly indices: U32RangeId;
    /** The tensor view layout. */
    readonly viewLayout: TensorLayoutId;
    /** The tensor element projection. */
    readonly element: ProjectionId;
    /** The tensor view backing memory. */
    readonly address: TensorAddress;
};

export const TensorLoad = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: TensorLoad): void {
        encodeTensorLoad(writer, value);
    },

    /** Decode one TensorLoad. */
    decode(reader: BinaryReader): TensorLoad {
        return decodeTensorLoad(reader);
    },

    /** Return this value as JSON. */
    toJson(value: TensorLoad): Json {
        return toJsonTensorLoad(value);
    },

    /** Return one TensorLoad from one JSON value. */
    fromJson(value: Json): TensorLoad {
        return fromJsonTensorLoad(value);
    },
};

/** Encode one TensorLoad. */
export function encodeTensorLoad(writer: BinaryWriter, value: TensorLoad): void {
    writer.writeUnsigned(value.destOffset);
    writer.writeUnsigned(value.viewOffset);
    encodeU32RangeId(writer, value.indices);
    encodeTensorLayoutId(writer, value.viewLayout);
    encodeProjectionId(writer, value.element);
    encodeTensorAddress(writer, value.address);
}

/** Decode one TensorLoad. */
export function decodeTensorLoad(reader: BinaryReader): TensorLoad {
    const destOffset = reader.readNumber();
    const viewOffset = reader.readNumber();
    const indices = decodeU32RangeId(reader);
    const viewLayout = decodeTensorLayoutId(reader);
    const element = decodeProjectionId(reader);
    const address = decodeTensorAddress(reader);

    return {
        destOffset,
        viewOffset,
        indices,
        viewLayout,
        element,
        address,
    };
}

/** Return one JSON value for one TensorLoad. */
export function toJsonTensorLoad(value: TensorLoad): Json {
    return {
        destOffset: value.destOffset,
        viewOffset: value.viewOffset,
        indices: toJsonU32RangeId(value.indices),
        viewLayout: toJsonTensorLayoutId(value.viewLayout),
        element: toJsonProjectionId(value.element),
        address: toJsonTensorAddress(value.address),
    };
}

/** Return one TensorLoad from one JSON value. */
export function fromJsonTensorLoad(value: Json): TensorLoad {
    const object = jsonObject(value);

    return {
        destOffset: jsonInteger(jsonField(object, "destOffset")),
        viewOffset: jsonInteger(jsonField(object, "viewOffset")),
        indices: fromJsonU32RangeId(jsonField(object, "indices")),
        viewLayout: fromJsonTensorLayoutId(jsonField(object, "viewLayout")),
        element: fromJsonProjectionId(jsonField(object, "element")),
        address: fromJsonTensorAddress(jsonField(object, "address")),
    };
}

/** Extract a tensor element from a tensor value. */
export type TensorExtract = {
    /** The destination scalar frame offset. */
    readonly destOffset: number;
    /** The source tensor frame offset. */
    readonly tensorOffset: number;
    /** The index frame offsets. */
    readonly indices: U32RangeId;
    /** The source tensor layout. */
    readonly tensorLayout: TensorLayoutId;
};

export const TensorExtract = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: TensorExtract): void {
        encodeTensorExtract(writer, value);
    },

    /** Decode one TensorExtract. */
    decode(reader: BinaryReader): TensorExtract {
        return decodeTensorExtract(reader);
    },

    /** Return this value as JSON. */
    toJson(value: TensorExtract): Json {
        return toJsonTensorExtract(value);
    },

    /** Return one TensorExtract from one JSON value. */
    fromJson(value: Json): TensorExtract {
        return fromJsonTensorExtract(value);
    },
};

/** Encode one TensorExtract. */
export function encodeTensorExtract(writer: BinaryWriter, value: TensorExtract): void {
    writer.writeUnsigned(value.destOffset);
    writer.writeUnsigned(value.tensorOffset);
    encodeU32RangeId(writer, value.indices);
    encodeTensorLayoutId(writer, value.tensorLayout);
}

/** Decode one TensorExtract. */
export function decodeTensorExtract(reader: BinaryReader): TensorExtract {
    const destOffset = reader.readNumber();
    const tensorOffset = reader.readNumber();
    const indices = decodeU32RangeId(reader);
    const tensorLayout = decodeTensorLayoutId(reader);

    return {
        destOffset,
        tensorOffset,
        indices,
        tensorLayout,
    };
}

/** Return one JSON value for one TensorExtract. */
export function toJsonTensorExtract(value: TensorExtract): Json {
    return {
        destOffset: value.destOffset,
        tensorOffset: value.tensorOffset,
        indices: toJsonU32RangeId(value.indices),
        tensorLayout: toJsonTensorLayoutId(value.tensorLayout),
    };
}

/** Return one TensorExtract from one JSON value. */
export function fromJsonTensorExtract(value: Json): TensorExtract {
    const object = jsonObject(value);

    return {
        destOffset: jsonInteger(jsonField(object, "destOffset")),
        tensorOffset: jsonInteger(jsonField(object, "tensorOffset")),
        indices: fromJsonU32RangeId(jsonField(object, "indices")),
        tensorLayout: fromJsonTensorLayoutId(jsonField(object, "tensorLayout")),
    };
}

/** Elementwise tensor binary operation. */
export type TensorBinary = {
    /** The destination tensor frame offset. */
    readonly destOffset: number;
    /** The left tensor frame offset. */
    readonly leftOffset: number;
    /** The right tensor frame offset. */
    readonly rightOffset: number;
    /** The left tensor layout. */
    readonly leftLayout: TensorLayoutId;
    /** The right tensor layout. */
    readonly rightLayout: TensorLayoutId;
    /** The destination tensor layout. */
    readonly destLayout: TensorLayoutId;
    /** The element binary kernel. */
    readonly kernel: ElementBinaryKernel;
    /** The tensor element scalar layout. */
    readonly elementLayout: ScalarLayout;
};

export const TensorBinary = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: TensorBinary): void {
        encodeTensorBinary(writer, value);
    },

    /** Decode one TensorBinary. */
    decode(reader: BinaryReader): TensorBinary {
        return decodeTensorBinary(reader);
    },

    /** Return this value as JSON. */
    toJson(value: TensorBinary): Json {
        return toJsonTensorBinary(value);
    },

    /** Return one TensorBinary from one JSON value. */
    fromJson(value: Json): TensorBinary {
        return fromJsonTensorBinary(value);
    },
};

/** Encode one TensorBinary. */
export function encodeTensorBinary(writer: BinaryWriter, value: TensorBinary): void {
    writer.writeUnsigned(value.destOffset);
    writer.writeUnsigned(value.leftOffset);
    writer.writeUnsigned(value.rightOffset);
    encodeTensorLayoutId(writer, value.leftLayout);
    encodeTensorLayoutId(writer, value.rightLayout);
    encodeTensorLayoutId(writer, value.destLayout);
    encodeElementBinaryKernel(writer, value.kernel);
    encodeScalarLayout(writer, value.elementLayout);
}

/** Decode one TensorBinary. */
export function decodeTensorBinary(reader: BinaryReader): TensorBinary {
    const destOffset = reader.readNumber();
    const leftOffset = reader.readNumber();
    const rightOffset = reader.readNumber();
    const leftLayout = decodeTensorLayoutId(reader);
    const rightLayout = decodeTensorLayoutId(reader);
    const destLayout = decodeTensorLayoutId(reader);
    const kernel = decodeElementBinaryKernel(reader);
    const elementLayout = decodeScalarLayout(reader);

    return {
        destOffset,
        leftOffset,
        rightOffset,
        leftLayout,
        rightLayout,
        destLayout,
        kernel,
        elementLayout,
    };
}

/** Return one JSON value for one TensorBinary. */
export function toJsonTensorBinary(value: TensorBinary): Json {
    return {
        destOffset: value.destOffset,
        leftOffset: value.leftOffset,
        rightOffset: value.rightOffset,
        leftLayout: toJsonTensorLayoutId(value.leftLayout),
        rightLayout: toJsonTensorLayoutId(value.rightLayout),
        destLayout: toJsonTensorLayoutId(value.destLayout),
        kernel: toJsonElementBinaryKernel(value.kernel),
        elementLayout: toJsonScalarLayout(value.elementLayout),
    };
}

/** Return one TensorBinary from one JSON value. */
export function fromJsonTensorBinary(value: Json): TensorBinary {
    const object = jsonObject(value);

    return {
        destOffset: jsonInteger(jsonField(object, "destOffset")),
        leftOffset: jsonInteger(jsonField(object, "leftOffset")),
        rightOffset: jsonInteger(jsonField(object, "rightOffset")),
        leftLayout: fromJsonTensorLayoutId(jsonField(object, "leftLayout")),
        rightLayout: fromJsonTensorLayoutId(jsonField(object, "rightLayout")),
        destLayout: fromJsonTensorLayoutId(jsonField(object, "destLayout")),
        kernel: fromJsonElementBinaryKernel(jsonField(object, "kernel")),
        elementLayout: fromJsonScalarLayout(jsonField(object, "elementLayout")),
    };
}

/** Contiguous elementwise tensor binary operation. */
export type TensorContiguousBinary = {
    /** The destination tensor frame offset. */
    readonly destOffset: number;
    /** The left tensor frame offset. */
    readonly leftOffset: number;
    /** The right tensor frame offset. */
    readonly rightOffset: number;
    /** The destination tensor layout. */
    readonly destLayout: TensorLayoutId;
    /** The tensor element scalar layout. */
    readonly elementLayout: ScalarLayout;
    /** The contiguous binary kernel selected during lowering. */
    readonly kernel: ElementBinaryKernel;
};

export const TensorContiguousBinary = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: TensorContiguousBinary): void {
        encodeTensorContiguousBinary(writer, value);
    },

    /** Decode one TensorContiguousBinary. */
    decode(reader: BinaryReader): TensorContiguousBinary {
        return decodeTensorContiguousBinary(reader);
    },

    /** Return this value as JSON. */
    toJson(value: TensorContiguousBinary): Json {
        return toJsonTensorContiguousBinary(value);
    },

    /** Return one TensorContiguousBinary from one JSON value. */
    fromJson(value: Json): TensorContiguousBinary {
        return fromJsonTensorContiguousBinary(value);
    },
};

/** Encode one TensorContiguousBinary. */
export function encodeTensorContiguousBinary(writer: BinaryWriter, value: TensorContiguousBinary): void {
    writer.writeUnsigned(value.destOffset);
    writer.writeUnsigned(value.leftOffset);
    writer.writeUnsigned(value.rightOffset);
    encodeTensorLayoutId(writer, value.destLayout);
    encodeScalarLayout(writer, value.elementLayout);
    encodeElementBinaryKernel(writer, value.kernel);
}

/** Decode one TensorContiguousBinary. */
export function decodeTensorContiguousBinary(reader: BinaryReader): TensorContiguousBinary {
    const destOffset = reader.readNumber();
    const leftOffset = reader.readNumber();
    const rightOffset = reader.readNumber();
    const destLayout = decodeTensorLayoutId(reader);
    const elementLayout = decodeScalarLayout(reader);
    const kernel = decodeElementBinaryKernel(reader);

    return {
        destOffset,
        leftOffset,
        rightOffset,
        destLayout,
        elementLayout,
        kernel,
    };
}

/** Return one JSON value for one TensorContiguousBinary. */
export function toJsonTensorContiguousBinary(value: TensorContiguousBinary): Json {
    return {
        destOffset: value.destOffset,
        leftOffset: value.leftOffset,
        rightOffset: value.rightOffset,
        destLayout: toJsonTensorLayoutId(value.destLayout),
        elementLayout: toJsonScalarLayout(value.elementLayout),
        kernel: toJsonElementBinaryKernel(value.kernel),
    };
}

/** Return one TensorContiguousBinary from one JSON value. */
export function fromJsonTensorContiguousBinary(value: Json): TensorContiguousBinary {
    const object = jsonObject(value);

    return {
        destOffset: jsonInteger(jsonField(object, "destOffset")),
        leftOffset: jsonInteger(jsonField(object, "leftOffset")),
        rightOffset: jsonInteger(jsonField(object, "rightOffset")),
        destLayout: fromJsonTensorLayoutId(jsonField(object, "destLayout")),
        elementLayout: fromJsonScalarLayout(jsonField(object, "elementLayout")),
        kernel: fromJsonElementBinaryKernel(jsonField(object, "kernel")),
    };
}

/** Elementwise tensor unary operation. */
export type TensorUnary = {
    /** The destination tensor frame offset. */
    readonly destOffset: number;
    /** The source tensor frame offset. */
    readonly argumentOffset: number;
    /** The argument tensor layout. */
    readonly argumentLayout: TensorLayoutId;
    /** The tensor element scalar layout. */
    readonly elementLayout: ScalarLayout;
    /** The destination tensor layout. */
    readonly destLayout: TensorLayoutId;
    /** The element unary kernel. */
    readonly kernel: ElementUnaryKernel;
};

export const TensorUnary = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: TensorUnary): void {
        encodeTensorUnary(writer, value);
    },

    /** Decode one TensorUnary. */
    decode(reader: BinaryReader): TensorUnary {
        return decodeTensorUnary(reader);
    },

    /** Return this value as JSON. */
    toJson(value: TensorUnary): Json {
        return toJsonTensorUnary(value);
    },

    /** Return one TensorUnary from one JSON value. */
    fromJson(value: Json): TensorUnary {
        return fromJsonTensorUnary(value);
    },
};

/** Encode one TensorUnary. */
export function encodeTensorUnary(writer: BinaryWriter, value: TensorUnary): void {
    writer.writeUnsigned(value.destOffset);
    writer.writeUnsigned(value.argumentOffset);
    encodeTensorLayoutId(writer, value.argumentLayout);
    encodeScalarLayout(writer, value.elementLayout);
    encodeTensorLayoutId(writer, value.destLayout);
    encodeElementUnaryKernel(writer, value.kernel);
}

/** Decode one TensorUnary. */
export function decodeTensorUnary(reader: BinaryReader): TensorUnary {
    const destOffset = reader.readNumber();
    const argumentOffset = reader.readNumber();
    const argumentLayout = decodeTensorLayoutId(reader);
    const elementLayout = decodeScalarLayout(reader);
    const destLayout = decodeTensorLayoutId(reader);
    const kernel = decodeElementUnaryKernel(reader);

    return {
        destOffset,
        argumentOffset,
        argumentLayout,
        elementLayout,
        destLayout,
        kernel,
    };
}

/** Return one JSON value for one TensorUnary. */
export function toJsonTensorUnary(value: TensorUnary): Json {
    return {
        destOffset: value.destOffset,
        argumentOffset: value.argumentOffset,
        argumentLayout: toJsonTensorLayoutId(value.argumentLayout),
        elementLayout: toJsonScalarLayout(value.elementLayout),
        destLayout: toJsonTensorLayoutId(value.destLayout),
        kernel: toJsonElementUnaryKernel(value.kernel),
    };
}

/** Return one TensorUnary from one JSON value. */
export function fromJsonTensorUnary(value: Json): TensorUnary {
    const object = jsonObject(value);

    return {
        destOffset: jsonInteger(jsonField(object, "destOffset")),
        argumentOffset: jsonInteger(jsonField(object, "argumentOffset")),
        argumentLayout: fromJsonTensorLayoutId(jsonField(object, "argumentLayout")),
        elementLayout: fromJsonScalarLayout(jsonField(object, "elementLayout")),
        destLayout: fromJsonTensorLayoutId(jsonField(object, "destLayout")),
        kernel: fromJsonElementUnaryKernel(jsonField(object, "kernel")),
    };
}

/** Contiguous elementwise tensor unary operation. */
export type TensorContiguousUnary = {
    /** The destination tensor frame offset. */
    readonly destOffset: number;
    /** The source tensor frame offset. */
    readonly argumentOffset: number;
    /** The destination tensor layout. */
    readonly destLayout: TensorLayoutId;
    /** The tensor element scalar layout. */
    readonly elementLayout: ScalarLayout;
    /** The contiguous unary kernel selected during lowering. */
    readonly kernel: ElementUnaryKernel;
};

export const TensorContiguousUnary = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: TensorContiguousUnary): void {
        encodeTensorContiguousUnary(writer, value);
    },

    /** Decode one TensorContiguousUnary. */
    decode(reader: BinaryReader): TensorContiguousUnary {
        return decodeTensorContiguousUnary(reader);
    },

    /** Return this value as JSON. */
    toJson(value: TensorContiguousUnary): Json {
        return toJsonTensorContiguousUnary(value);
    },

    /** Return one TensorContiguousUnary from one JSON value. */
    fromJson(value: Json): TensorContiguousUnary {
        return fromJsonTensorContiguousUnary(value);
    },
};

/** Encode one TensorContiguousUnary. */
export function encodeTensorContiguousUnary(writer: BinaryWriter, value: TensorContiguousUnary): void {
    writer.writeUnsigned(value.destOffset);
    writer.writeUnsigned(value.argumentOffset);
    encodeTensorLayoutId(writer, value.destLayout);
    encodeScalarLayout(writer, value.elementLayout);
    encodeElementUnaryKernel(writer, value.kernel);
}

/** Decode one TensorContiguousUnary. */
export function decodeTensorContiguousUnary(reader: BinaryReader): TensorContiguousUnary {
    const destOffset = reader.readNumber();
    const argumentOffset = reader.readNumber();
    const destLayout = decodeTensorLayoutId(reader);
    const elementLayout = decodeScalarLayout(reader);
    const kernel = decodeElementUnaryKernel(reader);

    return {
        destOffset,
        argumentOffset,
        destLayout,
        elementLayout,
        kernel,
    };
}

/** Return one JSON value for one TensorContiguousUnary. */
export function toJsonTensorContiguousUnary(value: TensorContiguousUnary): Json {
    return {
        destOffset: value.destOffset,
        argumentOffset: value.argumentOffset,
        destLayout: toJsonTensorLayoutId(value.destLayout),
        elementLayout: toJsonScalarLayout(value.elementLayout),
        kernel: toJsonElementUnaryKernel(value.kernel),
    };
}

/** Return one TensorContiguousUnary from one JSON value. */
export function fromJsonTensorContiguousUnary(value: Json): TensorContiguousUnary {
    const object = jsonObject(value);

    return {
        destOffset: jsonInteger(jsonField(object, "destOffset")),
        argumentOffset: jsonInteger(jsonField(object, "argumentOffset")),
        destLayout: fromJsonTensorLayoutId(jsonField(object, "destLayout")),
        elementLayout: fromJsonScalarLayout(jsonField(object, "elementLayout")),
        kernel: fromJsonElementUnaryKernel(jsonField(object, "kernel")),
    };
}

/** Store a tensor element into a view. */
export type TensorStore = {
    /** The destination tensor view. */
    readonly viewOffset: number;
    /** The index frame offsets. */
    readonly indices: U32RangeId;
    /** The stored scalar. */
    readonly valueOffset: number;
    /** The tensor view layout. */
    readonly viewLayout: TensorLayoutId;
    /** The tensor element projection. */
    readonly element: ProjectionId;
    /** The tensor view backing memory. */
    readonly address: TensorAddress;
};

export const TensorStore = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: TensorStore): void {
        encodeTensorStore(writer, value);
    },

    /** Decode one TensorStore. */
    decode(reader: BinaryReader): TensorStore {
        return decodeTensorStore(reader);
    },

    /** Return this value as JSON. */
    toJson(value: TensorStore): Json {
        return toJsonTensorStore(value);
    },

    /** Return one TensorStore from one JSON value. */
    fromJson(value: Json): TensorStore {
        return fromJsonTensorStore(value);
    },
};

/** Encode one TensorStore. */
export function encodeTensorStore(writer: BinaryWriter, value: TensorStore): void {
    writer.writeUnsigned(value.viewOffset);
    encodeU32RangeId(writer, value.indices);
    writer.writeUnsigned(value.valueOffset);
    encodeTensorLayoutId(writer, value.viewLayout);
    encodeProjectionId(writer, value.element);
    encodeTensorAddress(writer, value.address);
}

/** Decode one TensorStore. */
export function decodeTensorStore(reader: BinaryReader): TensorStore {
    const viewOffset = reader.readNumber();
    const indices = decodeU32RangeId(reader);
    const valueOffset = reader.readNumber();
    const viewLayout = decodeTensorLayoutId(reader);
    const element = decodeProjectionId(reader);
    const address = decodeTensorAddress(reader);

    return {
        viewOffset,
        indices,
        valueOffset,
        viewLayout,
        element,
        address,
    };
}

/** Return one JSON value for one TensorStore. */
export function toJsonTensorStore(value: TensorStore): Json {
    return {
        viewOffset: value.viewOffset,
        indices: toJsonU32RangeId(value.indices),
        valueOffset: value.valueOffset,
        viewLayout: toJsonTensorLayoutId(value.viewLayout),
        element: toJsonProjectionId(value.element),
        address: toJsonTensorAddress(value.address),
    };
}

/** Return one TensorStore from one JSON value. */
export function fromJsonTensorStore(value: Json): TensorStore {
    const object = jsonObject(value);

    return {
        viewOffset: jsonInteger(jsonField(object, "viewOffset")),
        indices: fromJsonU32RangeId(jsonField(object, "indices")),
        valueOffset: jsonInteger(jsonField(object, "valueOffset")),
        viewLayout: fromJsonTensorLayoutId(jsonField(object, "viewLayout")),
        element: fromJsonProjectionId(jsonField(object, "element")),
        address: fromJsonTensorAddress(jsonField(object, "address")),
    };
}

/** Fill a tensor reference with a scalar value. */
export type TensorFill = {
    /** The filled tensor view. */
    readonly viewOffset: number;
    /** The scalar fill value. */
    readonly valueOffset: number;
    /** The tensor view layout. */
    readonly viewLayout: TensorLayoutId;
    /** The tensor element projection. */
    readonly element: ProjectionId;
    /** The tensor view backing memory. */
    readonly address: TensorAddress;
};

export const TensorFill = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: TensorFill): void {
        encodeTensorFill(writer, value);
    },

    /** Decode one TensorFill. */
    decode(reader: BinaryReader): TensorFill {
        return decodeTensorFill(reader);
    },

    /** Return this value as JSON. */
    toJson(value: TensorFill): Json {
        return toJsonTensorFill(value);
    },

    /** Return one TensorFill from one JSON value. */
    fromJson(value: Json): TensorFill {
        return fromJsonTensorFill(value);
    },
};

/** Encode one TensorFill. */
export function encodeTensorFill(writer: BinaryWriter, value: TensorFill): void {
    writer.writeUnsigned(value.viewOffset);
    writer.writeUnsigned(value.valueOffset);
    encodeTensorLayoutId(writer, value.viewLayout);
    encodeProjectionId(writer, value.element);
    encodeTensorAddress(writer, value.address);
}

/** Decode one TensorFill. */
export function decodeTensorFill(reader: BinaryReader): TensorFill {
    const viewOffset = reader.readNumber();
    const valueOffset = reader.readNumber();
    const viewLayout = decodeTensorLayoutId(reader);
    const element = decodeProjectionId(reader);
    const address = decodeTensorAddress(reader);

    return {
        viewOffset,
        valueOffset,
        viewLayout,
        element,
        address,
    };
}

/** Return one JSON value for one TensorFill. */
export function toJsonTensorFill(value: TensorFill): Json {
    return {
        viewOffset: value.viewOffset,
        valueOffset: value.valueOffset,
        viewLayout: toJsonTensorLayoutId(value.viewLayout),
        element: toJsonProjectionId(value.element),
        address: toJsonTensorAddress(value.address),
    };
}

/** Return one TensorFill from one JSON value. */
export function fromJsonTensorFill(value: Json): TensorFill {
    const object = jsonObject(value);

    return {
        viewOffset: jsonInteger(jsonField(object, "viewOffset")),
        valueOffset: jsonInteger(jsonField(object, "valueOffset")),
        viewLayout: fromJsonTensorLayoutId(jsonField(object, "viewLayout")),
        element: fromJsonProjectionId(jsonField(object, "element")),
        address: fromJsonTensorAddress(jsonField(object, "address")),
    };
}

/** Copy elements between tensor references. */
export type TensorCopy = {
    /** The target tensor view. */
    readonly targetOffset: number;
    /** The source tensor view. */
    readonly sourceOffset: number;
    /** The target tensor view layout. */
    readonly targetLayout: TensorLayoutId;
    /** The source tensor view layout. */
    readonly sourceLayout: TensorLayoutId;
    /** The target element projection. */
    readonly targetElement: ProjectionId;
    /** The source element projection. */
    readonly sourceElement: ProjectionId;
    /** The target tensor backing memory. */
    readonly targetAddress: TensorAddress;
    /** The source tensor backing memory. */
    readonly sourceAddress: TensorAddress;
};

export const TensorCopy = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: TensorCopy): void {
        encodeTensorCopy(writer, value);
    },

    /** Decode one TensorCopy. */
    decode(reader: BinaryReader): TensorCopy {
        return decodeTensorCopy(reader);
    },

    /** Return this value as JSON. */
    toJson(value: TensorCopy): Json {
        return toJsonTensorCopy(value);
    },

    /** Return one TensorCopy from one JSON value. */
    fromJson(value: Json): TensorCopy {
        return fromJsonTensorCopy(value);
    },
};

/** Encode one TensorCopy. */
export function encodeTensorCopy(writer: BinaryWriter, value: TensorCopy): void {
    writer.writeUnsigned(value.targetOffset);
    writer.writeUnsigned(value.sourceOffset);
    encodeTensorLayoutId(writer, value.targetLayout);
    encodeTensorLayoutId(writer, value.sourceLayout);
    encodeProjectionId(writer, value.targetElement);
    encodeProjectionId(writer, value.sourceElement);
    encodeTensorAddress(writer, value.targetAddress);
    encodeTensorAddress(writer, value.sourceAddress);
}

/** Decode one TensorCopy. */
export function decodeTensorCopy(reader: BinaryReader): TensorCopy {
    const targetOffset = reader.readNumber();
    const sourceOffset = reader.readNumber();
    const targetLayout = decodeTensorLayoutId(reader);
    const sourceLayout = decodeTensorLayoutId(reader);
    const targetElement = decodeProjectionId(reader);
    const sourceElement = decodeProjectionId(reader);
    const targetAddress = decodeTensorAddress(reader);
    const sourceAddress = decodeTensorAddress(reader);

    return {
        targetOffset,
        sourceOffset,
        targetLayout,
        sourceLayout,
        targetElement,
        sourceElement,
        targetAddress,
        sourceAddress,
    };
}

/** Return one JSON value for one TensorCopy. */
export function toJsonTensorCopy(value: TensorCopy): Json {
    return {
        targetOffset: value.targetOffset,
        sourceOffset: value.sourceOffset,
        targetLayout: toJsonTensorLayoutId(value.targetLayout),
        sourceLayout: toJsonTensorLayoutId(value.sourceLayout),
        targetElement: toJsonProjectionId(value.targetElement),
        sourceElement: toJsonProjectionId(value.sourceElement),
        targetAddress: toJsonTensorAddress(value.targetAddress),
        sourceAddress: toJsonTensorAddress(value.sourceAddress),
    };
}

/** Return one TensorCopy from one JSON value. */
export function fromJsonTensorCopy(value: Json): TensorCopy {
    const object = jsonObject(value);

    return {
        targetOffset: jsonInteger(jsonField(object, "targetOffset")),
        sourceOffset: jsonInteger(jsonField(object, "sourceOffset")),
        targetLayout: fromJsonTensorLayoutId(jsonField(object, "targetLayout")),
        sourceLayout: fromJsonTensorLayoutId(jsonField(object, "sourceLayout")),
        targetElement: fromJsonProjectionId(jsonField(object, "targetElement")),
        sourceElement: fromJsonProjectionId(jsonField(object, "sourceElement")),
        targetAddress: fromJsonTensorAddress(jsonField(object, "targetAddress")),
        sourceAddress: fromJsonTensorAddress(jsonField(object, "sourceAddress")),
    };
}

/** Cast a dense pointer into a tensor view descriptor. */
export type TensorViewCast = {
    /** The destination tensor view. */
    readonly destOffset: number;
    /** The MIR pointer cell. */
    readonly pointerOffset: number;
    /** The tensor view layout. */
    readonly viewLayout: TensorLayoutId;
};

export const TensorViewCast = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: TensorViewCast): void {
        encodeTensorViewCast(writer, value);
    },

    /** Decode one TensorViewCast. */
    decode(reader: BinaryReader): TensorViewCast {
        return decodeTensorViewCast(reader);
    },

    /** Return this value as JSON. */
    toJson(value: TensorViewCast): Json {
        return toJsonTensorViewCast(value);
    },

    /** Return one TensorViewCast from one JSON value. */
    fromJson(value: Json): TensorViewCast {
        return fromJsonTensorViewCast(value);
    },
};

/** Encode one TensorViewCast. */
export function encodeTensorViewCast(writer: BinaryWriter, value: TensorViewCast): void {
    writer.writeUnsigned(value.destOffset);
    writer.writeUnsigned(value.pointerOffset);
    encodeTensorLayoutId(writer, value.viewLayout);
}

/** Decode one TensorViewCast. */
export function decodeTensorViewCast(reader: BinaryReader): TensorViewCast {
    const destOffset = reader.readNumber();
    const pointerOffset = reader.readNumber();
    const viewLayout = decodeTensorLayoutId(reader);

    return {
        destOffset,
        pointerOffset,
        viewLayout,
    };
}

/** Return one JSON value for one TensorViewCast. */
export function toJsonTensorViewCast(value: TensorViewCast): Json {
    return {
        destOffset: value.destOffset,
        pointerOffset: value.pointerOffset,
        viewLayout: toJsonTensorLayoutId(value.viewLayout),
    };
}

/** Return one TensorViewCast from one JSON value. */
export function fromJsonTensorViewCast(value: Json): TensorViewCast {
    const object = jsonObject(value);

    return {
        destOffset: jsonInteger(jsonField(object, "destOffset")),
        pointerOffset: jsonInteger(jsonField(object, "pointerOffset")),
        viewLayout: fromJsonTensorLayoutId(jsonField(object, "viewLayout")),
    };
}

/** Reshape a tensor into a new shape. */
export type TensorReshape = {
    /** The destination tensor frame offset. */
    readonly destOffset: number;
    /** The source tensor frame offset. */
    readonly tensorOffset: number;
    /** The destination shape values. */
    readonly shape: U32RangeId;
    /** The source tensor layout. */
    readonly sourceLayout: TensorLayoutId;
    /** The destination tensor layout. */
    readonly destLayout: TensorLayoutId;
};

export const TensorReshape = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: TensorReshape): void {
        encodeTensorReshape(writer, value);
    },

    /** Decode one TensorReshape. */
    decode(reader: BinaryReader): TensorReshape {
        return decodeTensorReshape(reader);
    },

    /** Return this value as JSON. */
    toJson(value: TensorReshape): Json {
        return toJsonTensorReshape(value);
    },

    /** Return one TensorReshape from one JSON value. */
    fromJson(value: Json): TensorReshape {
        return fromJsonTensorReshape(value);
    },
};

/** Encode one TensorReshape. */
export function encodeTensorReshape(writer: BinaryWriter, value: TensorReshape): void {
    writer.writeUnsigned(value.destOffset);
    writer.writeUnsigned(value.tensorOffset);
    encodeU32RangeId(writer, value.shape);
    encodeTensorLayoutId(writer, value.sourceLayout);
    encodeTensorLayoutId(writer, value.destLayout);
}

/** Decode one TensorReshape. */
export function decodeTensorReshape(reader: BinaryReader): TensorReshape {
    const destOffset = reader.readNumber();
    const tensorOffset = reader.readNumber();
    const shape = decodeU32RangeId(reader);
    const sourceLayout = decodeTensorLayoutId(reader);
    const destLayout = decodeTensorLayoutId(reader);

    return {
        destOffset,
        tensorOffset,
        shape,
        sourceLayout,
        destLayout,
    };
}

/** Return one JSON value for one TensorReshape. */
export function toJsonTensorReshape(value: TensorReshape): Json {
    return {
        destOffset: value.destOffset,
        tensorOffset: value.tensorOffset,
        shape: toJsonU32RangeId(value.shape),
        sourceLayout: toJsonTensorLayoutId(value.sourceLayout),
        destLayout: toJsonTensorLayoutId(value.destLayout),
    };
}

/** Return one TensorReshape from one JSON value. */
export function fromJsonTensorReshape(value: Json): TensorReshape {
    const object = jsonObject(value);

    return {
        destOffset: jsonInteger(jsonField(object, "destOffset")),
        tensorOffset: jsonInteger(jsonField(object, "tensorOffset")),
        shape: fromJsonU32RangeId(jsonField(object, "shape")),
        sourceLayout: fromJsonTensorLayoutId(jsonField(object, "sourceLayout")),
        destLayout: fromJsonTensorLayoutId(jsonField(object, "destLayout")),
    };
}

/** Broadcast a tensor into a larger shape. */
export type TensorBroadcast = {
    /** The destination tensor frame offset. */
    readonly destOffset: number;
    /** The source tensor frame offset. */
    readonly tensorOffset: number;
    /** The broadcast dimension mapping. */
    readonly dimensions: U32RangeId;
    /** The source tensor layout. */
    readonly sourceLayout: TensorLayoutId;
    /** The destination tensor layout. */
    readonly destLayout: TensorLayoutId;
};

export const TensorBroadcast = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: TensorBroadcast): void {
        encodeTensorBroadcast(writer, value);
    },

    /** Decode one TensorBroadcast. */
    decode(reader: BinaryReader): TensorBroadcast {
        return decodeTensorBroadcast(reader);
    },

    /** Return this value as JSON. */
    toJson(value: TensorBroadcast): Json {
        return toJsonTensorBroadcast(value);
    },

    /** Return one TensorBroadcast from one JSON value. */
    fromJson(value: Json): TensorBroadcast {
        return fromJsonTensorBroadcast(value);
    },
};

/** Encode one TensorBroadcast. */
export function encodeTensorBroadcast(writer: BinaryWriter, value: TensorBroadcast): void {
    writer.writeUnsigned(value.destOffset);
    writer.writeUnsigned(value.tensorOffset);
    encodeU32RangeId(writer, value.dimensions);
    encodeTensorLayoutId(writer, value.sourceLayout);
    encodeTensorLayoutId(writer, value.destLayout);
}

/** Decode one TensorBroadcast. */
export function decodeTensorBroadcast(reader: BinaryReader): TensorBroadcast {
    const destOffset = reader.readNumber();
    const tensorOffset = reader.readNumber();
    const dimensions = decodeU32RangeId(reader);
    const sourceLayout = decodeTensorLayoutId(reader);
    const destLayout = decodeTensorLayoutId(reader);

    return {
        destOffset,
        tensorOffset,
        dimensions,
        sourceLayout,
        destLayout,
    };
}

/** Return one JSON value for one TensorBroadcast. */
export function toJsonTensorBroadcast(value: TensorBroadcast): Json {
    return {
        destOffset: value.destOffset,
        tensorOffset: value.tensorOffset,
        dimensions: toJsonU32RangeId(value.dimensions),
        sourceLayout: toJsonTensorLayoutId(value.sourceLayout),
        destLayout: toJsonTensorLayoutId(value.destLayout),
    };
}

/** Return one TensorBroadcast from one JSON value. */
export function fromJsonTensorBroadcast(value: Json): TensorBroadcast {
    const object = jsonObject(value);

    return {
        destOffset: jsonInteger(jsonField(object, "destOffset")),
        tensorOffset: jsonInteger(jsonField(object, "tensorOffset")),
        dimensions: fromJsonU32RangeId(jsonField(object, "dimensions")),
        sourceLayout: fromJsonTensorLayoutId(jsonField(object, "sourceLayout")),
        destLayout: fromJsonTensorLayoutId(jsonField(object, "destLayout")),
    };
}

/** Permute tensor dimensions. */
export type TensorTranspose = {
    /** The destination tensor frame offset. */
    readonly destOffset: number;
    /** The source tensor frame offset. */
    readonly tensorOffset: number;
    /** The dimension permutation. */
    readonly permutation: U32RangeId;
    /** The source tensor layout. */
    readonly sourceLayout: TensorLayoutId;
    /** The destination tensor layout. */
    readonly destLayout: TensorLayoutId;
};

export const TensorTranspose = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: TensorTranspose): void {
        encodeTensorTranspose(writer, value);
    },

    /** Decode one TensorTranspose. */
    decode(reader: BinaryReader): TensorTranspose {
        return decodeTensorTranspose(reader);
    },

    /** Return this value as JSON. */
    toJson(value: TensorTranspose): Json {
        return toJsonTensorTranspose(value);
    },

    /** Return one TensorTranspose from one JSON value. */
    fromJson(value: Json): TensorTranspose {
        return fromJsonTensorTranspose(value);
    },
};

/** Encode one TensorTranspose. */
export function encodeTensorTranspose(writer: BinaryWriter, value: TensorTranspose): void {
    writer.writeUnsigned(value.destOffset);
    writer.writeUnsigned(value.tensorOffset);
    encodeU32RangeId(writer, value.permutation);
    encodeTensorLayoutId(writer, value.sourceLayout);
    encodeTensorLayoutId(writer, value.destLayout);
}

/** Decode one TensorTranspose. */
export function decodeTensorTranspose(reader: BinaryReader): TensorTranspose {
    const destOffset = reader.readNumber();
    const tensorOffset = reader.readNumber();
    const permutation = decodeU32RangeId(reader);
    const sourceLayout = decodeTensorLayoutId(reader);
    const destLayout = decodeTensorLayoutId(reader);

    return {
        destOffset,
        tensorOffset,
        permutation,
        sourceLayout,
        destLayout,
    };
}

/** Return one JSON value for one TensorTranspose. */
export function toJsonTensorTranspose(value: TensorTranspose): Json {
    return {
        destOffset: value.destOffset,
        tensorOffset: value.tensorOffset,
        permutation: toJsonU32RangeId(value.permutation),
        sourceLayout: toJsonTensorLayoutId(value.sourceLayout),
        destLayout: toJsonTensorLayoutId(value.destLayout),
    };
}

/** Return one TensorTranspose from one JSON value. */
export function fromJsonTensorTranspose(value: Json): TensorTranspose {
    const object = jsonObject(value);

    return {
        destOffset: jsonInteger(jsonField(object, "destOffset")),
        tensorOffset: jsonInteger(jsonField(object, "tensorOffset")),
        permutation: fromJsonU32RangeId(jsonField(object, "permutation")),
        sourceLayout: fromJsonTensorLayoutId(jsonField(object, "sourceLayout")),
        destLayout: fromJsonTensorLayoutId(jsonField(object, "destLayout")),
    };
}

/** Slice a tensor by offsets, sizes, and strides. */
export type TensorSlice = {
    /** The destination tensor frame offset. */
    readonly destOffset: number;
    /** The source tensor frame offset. */
    readonly tensorOffset: number;
    /** The packed offset, size, and stride values. */
    readonly arguments: U32RangeId;
    /** The number of offset values. */
    readonly offsetsCount: number;
    /** The number of size values. */
    readonly sizesCount: number;
    /** The number of stride values. */
    readonly stridesCount: number;
    /** The source tensor layout. */
    readonly sourceLayout: TensorLayoutId;
    /** The destination tensor layout. */
    readonly destLayout: TensorLayoutId;
};

export const TensorSlice = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: TensorSlice): void {
        encodeTensorSlice(writer, value);
    },

    /** Decode one TensorSlice. */
    decode(reader: BinaryReader): TensorSlice {
        return decodeTensorSlice(reader);
    },

    /** Return this value as JSON. */
    toJson(value: TensorSlice): Json {
        return toJsonTensorSlice(value);
    },

    /** Return one TensorSlice from one JSON value. */
    fromJson(value: Json): TensorSlice {
        return fromJsonTensorSlice(value);
    },
};

/** Encode one TensorSlice. */
export function encodeTensorSlice(writer: BinaryWriter, value: TensorSlice): void {
    writer.writeUnsigned(value.destOffset);
    writer.writeUnsigned(value.tensorOffset);
    encodeU32RangeId(writer, value.arguments);
    writer.writeUnsigned(value.offsetsCount);
    writer.writeUnsigned(value.sizesCount);
    writer.writeUnsigned(value.stridesCount);
    encodeTensorLayoutId(writer, value.sourceLayout);
    encodeTensorLayoutId(writer, value.destLayout);
}

/** Decode one TensorSlice. */
export function decodeTensorSlice(reader: BinaryReader): TensorSlice {
    const destOffset = reader.readNumber();
    const tensorOffset = reader.readNumber();
    const arguments_ = decodeU32RangeId(reader);
    const offsetsCount = reader.readNumber();
    const sizesCount = reader.readNumber();
    const stridesCount = reader.readNumber();
    const sourceLayout = decodeTensorLayoutId(reader);
    const destLayout = decodeTensorLayoutId(reader);

    return {
        destOffset,
        tensorOffset,
        arguments: arguments_,
        offsetsCount,
        sizesCount,
        stridesCount,
        sourceLayout,
        destLayout,
    };
}

/** Return one JSON value for one TensorSlice. */
export function toJsonTensorSlice(value: TensorSlice): Json {
    return {
        destOffset: value.destOffset,
        tensorOffset: value.tensorOffset,
        arguments: toJsonU32RangeId(value.arguments),
        offsetsCount: value.offsetsCount,
        sizesCount: value.sizesCount,
        stridesCount: value.stridesCount,
        sourceLayout: toJsonTensorLayoutId(value.sourceLayout),
        destLayout: toJsonTensorLayoutId(value.destLayout),
    };
}

/** Return one TensorSlice from one JSON value. */
export function fromJsonTensorSlice(value: Json): TensorSlice {
    const object = jsonObject(value);

    return {
        destOffset: jsonInteger(jsonField(object, "destOffset")),
        tensorOffset: jsonInteger(jsonField(object, "tensorOffset")),
        arguments: fromJsonU32RangeId(jsonField(object, "arguments")),
        offsetsCount: jsonInteger(jsonField(object, "offsetsCount")),
        sizesCount: jsonInteger(jsonField(object, "sizesCount")),
        stridesCount: jsonInteger(jsonField(object, "stridesCount")),
        sourceLayout: fromJsonTensorLayoutId(jsonField(object, "sourceLayout")),
        destLayout: fromJsonTensorLayoutId(jsonField(object, "destLayout")),
    };
}

/** Pad a tensor with low, high, and interior padding. */
export type TensorPad = {
    /** The destination tensor frame offset. */
    readonly destOffset: number;
    /** The source tensor frame offset. */
    readonly tensorOffset: number;
    /** The packed low, high, and interior padding values. */
    readonly arguments: U32RangeId;
    /** The number of low padding values. */
    readonly lowCount: number;
    /** The number of high padding values. */
    readonly highCount: number;
    /** The number of interior padding values. */
    readonly interiorCount: number;
    /** The padding value cell offset. */
    readonly valueOffset: number;
    /** The source tensor layout. */
    readonly sourceLayout: TensorLayoutId;
    /** The destination tensor layout. */
    readonly destLayout: TensorLayoutId;
};

export const TensorPad = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: TensorPad): void {
        encodeTensorPad(writer, value);
    },

    /** Decode one TensorPad. */
    decode(reader: BinaryReader): TensorPad {
        return decodeTensorPad(reader);
    },

    /** Return this value as JSON. */
    toJson(value: TensorPad): Json {
        return toJsonTensorPad(value);
    },

    /** Return one TensorPad from one JSON value. */
    fromJson(value: Json): TensorPad {
        return fromJsonTensorPad(value);
    },
};

/** Encode one TensorPad. */
export function encodeTensorPad(writer: BinaryWriter, value: TensorPad): void {
    writer.writeUnsigned(value.destOffset);
    writer.writeUnsigned(value.tensorOffset);
    encodeU32RangeId(writer, value.arguments);
    writer.writeUnsigned(value.lowCount);
    writer.writeUnsigned(value.highCount);
    writer.writeUnsigned(value.interiorCount);
    writer.writeUnsigned(value.valueOffset);
    encodeTensorLayoutId(writer, value.sourceLayout);
    encodeTensorLayoutId(writer, value.destLayout);
}

/** Decode one TensorPad. */
export function decodeTensorPad(reader: BinaryReader): TensorPad {
    const destOffset = reader.readNumber();
    const tensorOffset = reader.readNumber();
    const arguments_ = decodeU32RangeId(reader);
    const lowCount = reader.readNumber();
    const highCount = reader.readNumber();
    const interiorCount = reader.readNumber();
    const valueOffset = reader.readNumber();
    const sourceLayout = decodeTensorLayoutId(reader);
    const destLayout = decodeTensorLayoutId(reader);

    return {
        destOffset,
        tensorOffset,
        arguments: arguments_,
        lowCount,
        highCount,
        interiorCount,
        valueOffset,
        sourceLayout,
        destLayout,
    };
}

/** Return one JSON value for one TensorPad. */
export function toJsonTensorPad(value: TensorPad): Json {
    return {
        destOffset: value.destOffset,
        tensorOffset: value.tensorOffset,
        arguments: toJsonU32RangeId(value.arguments),
        lowCount: value.lowCount,
        highCount: value.highCount,
        interiorCount: value.interiorCount,
        valueOffset: value.valueOffset,
        sourceLayout: toJsonTensorLayoutId(value.sourceLayout),
        destLayout: toJsonTensorLayoutId(value.destLayout),
    };
}

/** Return one TensorPad from one JSON value. */
export function fromJsonTensorPad(value: Json): TensorPad {
    const object = jsonObject(value);

    return {
        destOffset: jsonInteger(jsonField(object, "destOffset")),
        tensorOffset: jsonInteger(jsonField(object, "tensorOffset")),
        arguments: fromJsonU32RangeId(jsonField(object, "arguments")),
        lowCount: jsonInteger(jsonField(object, "lowCount")),
        highCount: jsonInteger(jsonField(object, "highCount")),
        interiorCount: jsonInteger(jsonField(object, "interiorCount")),
        valueOffset: jsonInteger(jsonField(object, "valueOffset")),
        sourceLayout: fromJsonTensorLayoutId(jsonField(object, "sourceLayout")),
        destLayout: fromJsonTensorLayoutId(jsonField(object, "destLayout")),
    };
}

/** Concatenate tensors along a dimension. */
export type TensorConcat = {
    /** The destination tensor frame offset. */
    readonly destOffset: number;
    /** The source tensor frame offsets. */
    readonly tensors: U32RangeId;
    /** The source tensor layouts. */
    readonly tensorLayouts: U32RangeId;
    /** The concatenation axis. */
    readonly axis: number;
    /** The destination tensor layout. */
    readonly destLayout: TensorLayoutId;
};

export const TensorConcat = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: TensorConcat): void {
        encodeTensorConcat(writer, value);
    },

    /** Decode one TensorConcat. */
    decode(reader: BinaryReader): TensorConcat {
        return decodeTensorConcat(reader);
    },

    /** Return this value as JSON. */
    toJson(value: TensorConcat): Json {
        return toJsonTensorConcat(value);
    },

    /** Return one TensorConcat from one JSON value. */
    fromJson(value: Json): TensorConcat {
        return fromJsonTensorConcat(value);
    },
};

/** Encode one TensorConcat. */
export function encodeTensorConcat(writer: BinaryWriter, value: TensorConcat): void {
    writer.writeUnsigned(value.destOffset);
    encodeU32RangeId(writer, value.tensors);
    encodeU32RangeId(writer, value.tensorLayouts);
    writer.writeUnsigned(value.axis);
    encodeTensorLayoutId(writer, value.destLayout);
}

/** Decode one TensorConcat. */
export function decodeTensorConcat(reader: BinaryReader): TensorConcat {
    const destOffset = reader.readNumber();
    const tensors = decodeU32RangeId(reader);
    const tensorLayouts = decodeU32RangeId(reader);
    const axis = reader.readNumber();
    const destLayout = decodeTensorLayoutId(reader);

    return {
        destOffset,
        tensors,
        tensorLayouts,
        axis,
        destLayout,
    };
}

/** Return one JSON value for one TensorConcat. */
export function toJsonTensorConcat(value: TensorConcat): Json {
    return {
        destOffset: value.destOffset,
        tensors: toJsonU32RangeId(value.tensors),
        tensorLayouts: toJsonU32RangeId(value.tensorLayouts),
        axis: value.axis,
        destLayout: toJsonTensorLayoutId(value.destLayout),
    };
}

/** Return one TensorConcat from one JSON value. */
export function fromJsonTensorConcat(value: Json): TensorConcat {
    const object = jsonObject(value);

    return {
        destOffset: jsonInteger(jsonField(object, "destOffset")),
        tensors: fromJsonU32RangeId(jsonField(object, "tensors")),
        tensorLayouts: fromJsonU32RangeId(jsonField(object, "tensorLayouts")),
        axis: jsonInteger(jsonField(object, "axis")),
        destLayout: fromJsonTensorLayoutId(jsonField(object, "destLayout")),
    };
}

/** Reduce a tensor along axes. */
export type TensorReduce = {
    /** The destination tensor frame offset. */
    readonly destOffset: number;
    /** The source tensor frame offset. */
    readonly tensorOffset: number;
    /** The initial value cell offset. */
    readonly initialOffset: number;
    /** The reduced axis range. */
    readonly axes: U32RangeId;
    /** The source tensor layout. */
    readonly sourceLayout: TensorLayoutId;
    /** The destination tensor layout. */
    readonly destLayout: TensorLayoutId;
    /** The reduction kernel. */
    readonly kernel: TensorReduceOperator;
};

export const TensorReduce = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: TensorReduce): void {
        encodeTensorReduce(writer, value);
    },

    /** Decode one TensorReduce. */
    decode(reader: BinaryReader): TensorReduce {
        return decodeTensorReduce(reader);
    },

    /** Return this value as JSON. */
    toJson(value: TensorReduce): Json {
        return toJsonTensorReduce(value);
    },

    /** Return one TensorReduce from one JSON value. */
    fromJson(value: Json): TensorReduce {
        return fromJsonTensorReduce(value);
    },
};

/** Encode one TensorReduce. */
export function encodeTensorReduce(writer: BinaryWriter, value: TensorReduce): void {
    writer.writeUnsigned(value.destOffset);
    writer.writeUnsigned(value.tensorOffset);
    writer.writeUnsigned(value.initialOffset);
    encodeU32RangeId(writer, value.axes);
    encodeTensorLayoutId(writer, value.sourceLayout);
    encodeTensorLayoutId(writer, value.destLayout);
    encodeTensorReduceOperator(writer, value.kernel);
}

/** Decode one TensorReduce. */
export function decodeTensorReduce(reader: BinaryReader): TensorReduce {
    const destOffset = reader.readNumber();
    const tensorOffset = reader.readNumber();
    const initialOffset = reader.readNumber();
    const axes = decodeU32RangeId(reader);
    const sourceLayout = decodeTensorLayoutId(reader);
    const destLayout = decodeTensorLayoutId(reader);
    const kernel = decodeTensorReduceOperator(reader);

    return {
        destOffset,
        tensorOffset,
        initialOffset,
        axes,
        sourceLayout,
        destLayout,
        kernel,
    };
}

/** Return one JSON value for one TensorReduce. */
export function toJsonTensorReduce(value: TensorReduce): Json {
    return {
        destOffset: value.destOffset,
        tensorOffset: value.tensorOffset,
        initialOffset: value.initialOffset,
        axes: toJsonU32RangeId(value.axes),
        sourceLayout: toJsonTensorLayoutId(value.sourceLayout),
        destLayout: toJsonTensorLayoutId(value.destLayout),
        kernel: toJsonTensorReduceOperator(value.kernel),
    };
}

/** Return one TensorReduce from one JSON value. */
export function fromJsonTensorReduce(value: Json): TensorReduce {
    const object = jsonObject(value);

    return {
        destOffset: jsonInteger(jsonField(object, "destOffset")),
        tensorOffset: jsonInteger(jsonField(object, "tensorOffset")),
        initialOffset: jsonInteger(jsonField(object, "initialOffset")),
        axes: fromJsonU32RangeId(jsonField(object, "axes")),
        sourceLayout: fromJsonTensorLayoutId(jsonField(object, "sourceLayout")),
        destLayout: fromJsonTensorLayoutId(jsonField(object, "destLayout")),
        kernel: fromJsonTensorReduceOperator(jsonField(object, "kernel")),
    };
}

/** Reduce a tensor along one axis and return selected source indices. */
export type TensorIndexReduce = {
    /** The destination tensor frame offset. */
    readonly destOffset: number;
    /** The source tensor frame offset. */
    readonly tensorOffset: number;
    /** The reduced axis. */
    readonly axis: number;
    /** The source tensor layout. */
    readonly sourceLayout: TensorLayoutId;
    /** The destination tensor layout. */
    readonly destLayout: TensorLayoutId;
    /** The index reduction kernel. */
    readonly kernel: TensorIndexReduceOperator;
    /** The behavior for equal selected values. */
    readonly tieBreak: TensorIndexTieBreak;
};

export const TensorIndexReduce = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: TensorIndexReduce): void {
        encodeTensorIndexReduce(writer, value);
    },

    /** Decode one TensorIndexReduce. */
    decode(reader: BinaryReader): TensorIndexReduce {
        return decodeTensorIndexReduce(reader);
    },

    /** Return this value as JSON. */
    toJson(value: TensorIndexReduce): Json {
        return toJsonTensorIndexReduce(value);
    },

    /** Return one TensorIndexReduce from one JSON value. */
    fromJson(value: Json): TensorIndexReduce {
        return fromJsonTensorIndexReduce(value);
    },
};

/** Encode one TensorIndexReduce. */
export function encodeTensorIndexReduce(writer: BinaryWriter, value: TensorIndexReduce): void {
    writer.writeUnsigned(value.destOffset);
    writer.writeUnsigned(value.tensorOffset);
    writer.writeUnsigned(value.axis);
    encodeTensorLayoutId(writer, value.sourceLayout);
    encodeTensorLayoutId(writer, value.destLayout);
    encodeTensorIndexReduceOperator(writer, value.kernel);
    encodeTensorIndexTieBreak(writer, value.tieBreak);
}

/** Decode one TensorIndexReduce. */
export function decodeTensorIndexReduce(reader: BinaryReader): TensorIndexReduce {
    const destOffset = reader.readNumber();
    const tensorOffset = reader.readNumber();
    const axis = reader.readNumber();
    const sourceLayout = decodeTensorLayoutId(reader);
    const destLayout = decodeTensorLayoutId(reader);
    const kernel = decodeTensorIndexReduceOperator(reader);
    const tieBreak = decodeTensorIndexTieBreak(reader);

    return {
        destOffset,
        tensorOffset,
        axis,
        sourceLayout,
        destLayout,
        kernel,
        tieBreak,
    };
}

/** Return one JSON value for one TensorIndexReduce. */
export function toJsonTensorIndexReduce(value: TensorIndexReduce): Json {
    return {
        destOffset: value.destOffset,
        tensorOffset: value.tensorOffset,
        axis: value.axis,
        sourceLayout: toJsonTensorLayoutId(value.sourceLayout),
        destLayout: toJsonTensorLayoutId(value.destLayout),
        kernel: toJsonTensorIndexReduceOperator(value.kernel),
        tieBreak: toJsonTensorIndexTieBreak(value.tieBreak),
    };
}

/** Return one TensorIndexReduce from one JSON value. */
export function fromJsonTensorIndexReduce(value: Json): TensorIndexReduce {
    const object = jsonObject(value);

    return {
        destOffset: jsonInteger(jsonField(object, "destOffset")),
        tensorOffset: jsonInteger(jsonField(object, "tensorOffset")),
        axis: jsonInteger(jsonField(object, "axis")),
        sourceLayout: fromJsonTensorLayoutId(jsonField(object, "sourceLayout")),
        destLayout: fromJsonTensorLayoutId(jsonField(object, "destLayout")),
        kernel: fromJsonTensorIndexReduceOperator(jsonField(object, "kernel")),
        tieBreak: fromJsonTensorIndexTieBreak(jsonField(object, "tieBreak")),
    };
}

/** Dot product of two tensors. */
export type TensorDot = {
    /** The destination tensor frame offset. */
    readonly destOffset: number;
    /** The left tensor frame offset. */
    readonly leftOffset: number;
    /** The right tensor frame offset. */
    readonly rightOffset: number;
    /** The dot dimension numbers. */
    readonly dimensions: TensorDotId;
    /** The left tensor layout. */
    readonly leftLayout: TensorLayoutId;
    /** The right tensor layout. */
    readonly rightLayout: TensorLayoutId;
    /** The destination tensor layout. */
    readonly destLayout: TensorLayoutId;
    /** The tensor element scalar layout. */
    readonly elementLayout: ScalarLayout;
};

export const TensorDot = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: TensorDot): void {
        encodeTensorDot(writer, value);
    },

    /** Decode one TensorDot. */
    decode(reader: BinaryReader): TensorDot {
        return decodeTensorDot(reader);
    },

    /** Return this value as JSON. */
    toJson(value: TensorDot): Json {
        return toJsonTensorDot(value);
    },

    /** Return one TensorDot from one JSON value. */
    fromJson(value: Json): TensorDot {
        return fromJsonTensorDot(value);
    },
};

/** Encode one TensorDot. */
export function encodeTensorDot(writer: BinaryWriter, value: TensorDot): void {
    writer.writeUnsigned(value.destOffset);
    writer.writeUnsigned(value.leftOffset);
    writer.writeUnsigned(value.rightOffset);
    encodeTensorDotId(writer, value.dimensions);
    encodeTensorLayoutId(writer, value.leftLayout);
    encodeTensorLayoutId(writer, value.rightLayout);
    encodeTensorLayoutId(writer, value.destLayout);
    encodeScalarLayout(writer, value.elementLayout);
}

/** Decode one TensorDot. */
export function decodeTensorDot(reader: BinaryReader): TensorDot {
    const destOffset = reader.readNumber();
    const leftOffset = reader.readNumber();
    const rightOffset = reader.readNumber();
    const dimensions = decodeTensorDotId(reader);
    const leftLayout = decodeTensorLayoutId(reader);
    const rightLayout = decodeTensorLayoutId(reader);
    const destLayout = decodeTensorLayoutId(reader);
    const elementLayout = decodeScalarLayout(reader);

    return {
        destOffset,
        leftOffset,
        rightOffset,
        dimensions,
        leftLayout,
        rightLayout,
        destLayout,
        elementLayout,
    };
}

/** Return one JSON value for one TensorDot. */
export function toJsonTensorDot(value: TensorDot): Json {
    return {
        destOffset: value.destOffset,
        leftOffset: value.leftOffset,
        rightOffset: value.rightOffset,
        dimensions: toJsonTensorDotId(value.dimensions),
        leftLayout: toJsonTensorLayoutId(value.leftLayout),
        rightLayout: toJsonTensorLayoutId(value.rightLayout),
        destLayout: toJsonTensorLayoutId(value.destLayout),
        elementLayout: toJsonScalarLayout(value.elementLayout),
    };
}

/** Return one TensorDot from one JSON value. */
export function fromJsonTensorDot(value: Json): TensorDot {
    const object = jsonObject(value);

    return {
        destOffset: jsonInteger(jsonField(object, "destOffset")),
        leftOffset: jsonInteger(jsonField(object, "leftOffset")),
        rightOffset: jsonInteger(jsonField(object, "rightOffset")),
        dimensions: fromJsonTensorDotId(jsonField(object, "dimensions")),
        leftLayout: fromJsonTensorLayoutId(jsonField(object, "leftLayout")),
        rightLayout: fromJsonTensorLayoutId(jsonField(object, "rightLayout")),
        destLayout: fromJsonTensorLayoutId(jsonField(object, "destLayout")),
        elementLayout: fromJsonScalarLayout(jsonField(object, "elementLayout")),
    };
}

/** Convolution between an input tensor and a kernel tensor. */
export type TensorConvolution = {
    /** The destination tensor frame offset. */
    readonly destOffset: number;
    /** The input tensor frame offset. */
    readonly inputOffset: number;
    /** The kernel tensor frame offset. */
    readonly kernelOffset: number;
    /** The convolution dimension numbers. */
    readonly dimensions: TensorConvolutionId;
    /** The convolution window descriptor. */
    readonly window: TensorWindowId;
    /** The feature group count. */
    readonly featureGroupCount: number;
    /** The batch group count. */
    readonly batchGroupCount: number;
    /** The input tensor layout. */
    readonly inputLayout: TensorLayoutId;
    /** The kernel tensor layout. */
    readonly kernelLayout: TensorLayoutId;
    /** The destination tensor layout. */
    readonly destLayout: TensorLayoutId;
    /** The tensor element scalar layout. */
    readonly elementLayout: ScalarLayout;
};

export const TensorConvolution = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: TensorConvolution): void {
        encodeTensorConvolution(writer, value);
    },

    /** Decode one TensorConvolution. */
    decode(reader: BinaryReader): TensorConvolution {
        return decodeTensorConvolution(reader);
    },

    /** Return this value as JSON. */
    toJson(value: TensorConvolution): Json {
        return toJsonTensorConvolution(value);
    },

    /** Return one TensorConvolution from one JSON value. */
    fromJson(value: Json): TensorConvolution {
        return fromJsonTensorConvolution(value);
    },
};

/** Encode one TensorConvolution. */
export function encodeTensorConvolution(writer: BinaryWriter, value: TensorConvolution): void {
    writer.writeUnsigned(value.destOffset);
    writer.writeUnsigned(value.inputOffset);
    writer.writeUnsigned(value.kernelOffset);
    encodeTensorConvolutionId(writer, value.dimensions);
    encodeTensorWindowId(writer, value.window);
    writer.writeUnsigned(value.featureGroupCount);
    writer.writeUnsigned(value.batchGroupCount);
    encodeTensorLayoutId(writer, value.inputLayout);
    encodeTensorLayoutId(writer, value.kernelLayout);
    encodeTensorLayoutId(writer, value.destLayout);
    encodeScalarLayout(writer, value.elementLayout);
}

/** Decode one TensorConvolution. */
export function decodeTensorConvolution(reader: BinaryReader): TensorConvolution {
    const destOffset = reader.readNumber();
    const inputOffset = reader.readNumber();
    const kernelOffset = reader.readNumber();
    const dimensions = decodeTensorConvolutionId(reader);
    const window = decodeTensorWindowId(reader);
    const featureGroupCount = reader.readNumber();
    const batchGroupCount = reader.readNumber();
    const inputLayout = decodeTensorLayoutId(reader);
    const kernelLayout = decodeTensorLayoutId(reader);
    const destLayout = decodeTensorLayoutId(reader);
    const elementLayout = decodeScalarLayout(reader);

    return {
        destOffset,
        inputOffset,
        kernelOffset,
        dimensions,
        window,
        featureGroupCount,
        batchGroupCount,
        inputLayout,
        kernelLayout,
        destLayout,
        elementLayout,
    };
}

/** Return one JSON value for one TensorConvolution. */
export function toJsonTensorConvolution(value: TensorConvolution): Json {
    return {
        destOffset: value.destOffset,
        inputOffset: value.inputOffset,
        kernelOffset: value.kernelOffset,
        dimensions: toJsonTensorConvolutionId(value.dimensions),
        window: toJsonTensorWindowId(value.window),
        featureGroupCount: value.featureGroupCount,
        batchGroupCount: value.batchGroupCount,
        inputLayout: toJsonTensorLayoutId(value.inputLayout),
        kernelLayout: toJsonTensorLayoutId(value.kernelLayout),
        destLayout: toJsonTensorLayoutId(value.destLayout),
        elementLayout: toJsonScalarLayout(value.elementLayout),
    };
}

/** Return one TensorConvolution from one JSON value. */
export function fromJsonTensorConvolution(value: Json): TensorConvolution {
    const object = jsonObject(value);

    return {
        destOffset: jsonInteger(jsonField(object, "destOffset")),
        inputOffset: jsonInteger(jsonField(object, "inputOffset")),
        kernelOffset: jsonInteger(jsonField(object, "kernelOffset")),
        dimensions: fromJsonTensorConvolutionId(jsonField(object, "dimensions")),
        window: fromJsonTensorWindowId(jsonField(object, "window")),
        featureGroupCount: jsonInteger(jsonField(object, "featureGroupCount")),
        batchGroupCount: jsonInteger(jsonField(object, "batchGroupCount")),
        inputLayout: fromJsonTensorLayoutId(jsonField(object, "inputLayout")),
        kernelLayout: fromJsonTensorLayoutId(jsonField(object, "kernelLayout")),
        destLayout: fromJsonTensorLayoutId(jsonField(object, "destLayout")),
        elementLayout: fromJsonScalarLayout(jsonField(object, "elementLayout")),
    };
}

/** Gather slices from a tensor based on indices. */
export type TensorGather = {
    /** The destination tensor frame offset. */
    readonly destOffset: number;
    /** The source tensor frame offset. */
    readonly sourceOffset: number;
    /** The indices tensor frame offset. */
    readonly indicesOffset: number;
    /** The gather dimension numbers. */
    readonly dimensions: TensorGatherId;
    /** The gathered slice sizes. */
    readonly sliceSizes: U32RangeId;
    /** The source tensor layout. */
    readonly sourceLayout: TensorLayoutId;
    /** The indices tensor layout. */
    readonly indicesLayout: TensorLayoutId;
    /** The destination tensor layout. */
    readonly destLayout: TensorLayoutId;
};

export const TensorGather = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: TensorGather): void {
        encodeTensorGather(writer, value);
    },

    /** Decode one TensorGather. */
    decode(reader: BinaryReader): TensorGather {
        return decodeTensorGather(reader);
    },

    /** Return this value as JSON. */
    toJson(value: TensorGather): Json {
        return toJsonTensorGather(value);
    },

    /** Return one TensorGather from one JSON value. */
    fromJson(value: Json): TensorGather {
        return fromJsonTensorGather(value);
    },
};

/** Encode one TensorGather. */
export function encodeTensorGather(writer: BinaryWriter, value: TensorGather): void {
    writer.writeUnsigned(value.destOffset);
    writer.writeUnsigned(value.sourceOffset);
    writer.writeUnsigned(value.indicesOffset);
    encodeTensorGatherId(writer, value.dimensions);
    encodeU32RangeId(writer, value.sliceSizes);
    encodeTensorLayoutId(writer, value.sourceLayout);
    encodeTensorLayoutId(writer, value.indicesLayout);
    encodeTensorLayoutId(writer, value.destLayout);
}

/** Decode one TensorGather. */
export function decodeTensorGather(reader: BinaryReader): TensorGather {
    const destOffset = reader.readNumber();
    const sourceOffset = reader.readNumber();
    const indicesOffset = reader.readNumber();
    const dimensions = decodeTensorGatherId(reader);
    const sliceSizes = decodeU32RangeId(reader);
    const sourceLayout = decodeTensorLayoutId(reader);
    const indicesLayout = decodeTensorLayoutId(reader);
    const destLayout = decodeTensorLayoutId(reader);

    return {
        destOffset,
        sourceOffset,
        indicesOffset,
        dimensions,
        sliceSizes,
        sourceLayout,
        indicesLayout,
        destLayout,
    };
}

/** Return one JSON value for one TensorGather. */
export function toJsonTensorGather(value: TensorGather): Json {
    return {
        destOffset: value.destOffset,
        sourceOffset: value.sourceOffset,
        indicesOffset: value.indicesOffset,
        dimensions: toJsonTensorGatherId(value.dimensions),
        sliceSizes: toJsonU32RangeId(value.sliceSizes),
        sourceLayout: toJsonTensorLayoutId(value.sourceLayout),
        indicesLayout: toJsonTensorLayoutId(value.indicesLayout),
        destLayout: toJsonTensorLayoutId(value.destLayout),
    };
}

/** Return one TensorGather from one JSON value. */
export function fromJsonTensorGather(value: Json): TensorGather {
    const object = jsonObject(value);

    return {
        destOffset: jsonInteger(jsonField(object, "destOffset")),
        sourceOffset: jsonInteger(jsonField(object, "sourceOffset")),
        indicesOffset: jsonInteger(jsonField(object, "indicesOffset")),
        dimensions: fromJsonTensorGatherId(jsonField(object, "dimensions")),
        sliceSizes: fromJsonU32RangeId(jsonField(object, "sliceSizes")),
        sourceLayout: fromJsonTensorLayoutId(jsonField(object, "sourceLayout")),
        indicesLayout: fromJsonTensorLayoutId(jsonField(object, "indicesLayout")),
        destLayout: fromJsonTensorLayoutId(jsonField(object, "destLayout")),
    };
}

/** Scatter updates into a tensor based on indices. */
export type TensorScatter = {
    /** The destination tensor frame offset. */
    readonly destOffset: number;
    /** The source tensor frame offset. */
    readonly sourceOffset: number;
    /** The indices tensor frame offset. */
    readonly indicesOffset: number;
    /** The updates tensor frame offset. */
    readonly updatesOffset: number;
    /** The scatter dimension numbers. */
    readonly dimensions: TensorScatterId;
    /** The source tensor layout. */
    readonly sourceLayout: TensorLayoutId;
    /** The indices tensor layout. */
    readonly indicesLayout: TensorLayoutId;
    /** The updates tensor layout. */
    readonly updatesLayout: TensorLayoutId;
    /** The destination tensor layout. */
    readonly destLayout: TensorLayoutId;
    /** The tensor element scalar layout. */
    readonly elementLayout: ScalarLayout;
    /** The scatter kernel. */
    readonly mode: TensorScatterMode;
};

export const TensorScatter = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: TensorScatter): void {
        encodeTensorScatter(writer, value);
    },

    /** Decode one TensorScatter. */
    decode(reader: BinaryReader): TensorScatter {
        return decodeTensorScatter(reader);
    },

    /** Return this value as JSON. */
    toJson(value: TensorScatter): Json {
        return toJsonTensorScatter(value);
    },

    /** Return one TensorScatter from one JSON value. */
    fromJson(value: Json): TensorScatter {
        return fromJsonTensorScatter(value);
    },
};

/** Encode one TensorScatter. */
export function encodeTensorScatter(writer: BinaryWriter, value: TensorScatter): void {
    writer.writeUnsigned(value.destOffset);
    writer.writeUnsigned(value.sourceOffset);
    writer.writeUnsigned(value.indicesOffset);
    writer.writeUnsigned(value.updatesOffset);
    encodeTensorScatterId(writer, value.dimensions);
    encodeTensorLayoutId(writer, value.sourceLayout);
    encodeTensorLayoutId(writer, value.indicesLayout);
    encodeTensorLayoutId(writer, value.updatesLayout);
    encodeTensorLayoutId(writer, value.destLayout);
    encodeScalarLayout(writer, value.elementLayout);
    encodeTensorScatterMode(writer, value.mode);
}

/** Decode one TensorScatter. */
export function decodeTensorScatter(reader: BinaryReader): TensorScatter {
    const destOffset = reader.readNumber();
    const sourceOffset = reader.readNumber();
    const indicesOffset = reader.readNumber();
    const updatesOffset = reader.readNumber();
    const dimensions = decodeTensorScatterId(reader);
    const sourceLayout = decodeTensorLayoutId(reader);
    const indicesLayout = decodeTensorLayoutId(reader);
    const updatesLayout = decodeTensorLayoutId(reader);
    const destLayout = decodeTensorLayoutId(reader);
    const elementLayout = decodeScalarLayout(reader);
    const mode = decodeTensorScatterMode(reader);

    return {
        destOffset,
        sourceOffset,
        indicesOffset,
        updatesOffset,
        dimensions,
        sourceLayout,
        indicesLayout,
        updatesLayout,
        destLayout,
        elementLayout,
        mode,
    };
}

/** Return one JSON value for one TensorScatter. */
export function toJsonTensorScatter(value: TensorScatter): Json {
    return {
        destOffset: value.destOffset,
        sourceOffset: value.sourceOffset,
        indicesOffset: value.indicesOffset,
        updatesOffset: value.updatesOffset,
        dimensions: toJsonTensorScatterId(value.dimensions),
        sourceLayout: toJsonTensorLayoutId(value.sourceLayout),
        indicesLayout: toJsonTensorLayoutId(value.indicesLayout),
        updatesLayout: toJsonTensorLayoutId(value.updatesLayout),
        destLayout: toJsonTensorLayoutId(value.destLayout),
        elementLayout: toJsonScalarLayout(value.elementLayout),
        mode: toJsonTensorScatterMode(value.mode),
    };
}

/** Return one TensorScatter from one JSON value. */
export function fromJsonTensorScatter(value: Json): TensorScatter {
    const object = jsonObject(value);

    return {
        destOffset: jsonInteger(jsonField(object, "destOffset")),
        sourceOffset: jsonInteger(jsonField(object, "sourceOffset")),
        indicesOffset: jsonInteger(jsonField(object, "indicesOffset")),
        updatesOffset: jsonInteger(jsonField(object, "updatesOffset")),
        dimensions: fromJsonTensorScatterId(jsonField(object, "dimensions")),
        sourceLayout: fromJsonTensorLayoutId(jsonField(object, "sourceLayout")),
        indicesLayout: fromJsonTensorLayoutId(jsonField(object, "indicesLayout")),
        updatesLayout: fromJsonTensorLayoutId(jsonField(object, "updatesLayout")),
        destLayout: fromJsonTensorLayoutId(jsonField(object, "destLayout")),
        elementLayout: fromJsonScalarLayout(jsonField(object, "elementLayout")),
        mode: fromJsonTensorScatterMode(jsonField(object, "mode")),
    };
}

/** Select tensor elements based on a boolean mask. */
export type TensorSelect = {
    /** The destination tensor frame offset. */
    readonly destOffset: number;
    /** The mask tensor frame offset. */
    readonly maskOffset: number;
    /** The true branch tensor frame offset. */
    readonly thenOffset: number;
    /** The false branch tensor frame offset. */
    readonly elseOffset: number;
    /** The mask tensor layout. */
    readonly maskLayout: TensorLayoutId;
    /** The true branch tensor layout. */
    readonly thenLayout: TensorLayoutId;
    /** The false branch tensor layout. */
    readonly elseLayout: TensorLayoutId;
    /** The destination tensor layout. */
    readonly destLayout: TensorLayoutId;
};

export const TensorSelect = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: TensorSelect): void {
        encodeTensorSelect(writer, value);
    },

    /** Decode one TensorSelect. */
    decode(reader: BinaryReader): TensorSelect {
        return decodeTensorSelect(reader);
    },

    /** Return this value as JSON. */
    toJson(value: TensorSelect): Json {
        return toJsonTensorSelect(value);
    },

    /** Return one TensorSelect from one JSON value. */
    fromJson(value: Json): TensorSelect {
        return fromJsonTensorSelect(value);
    },
};

/** Encode one TensorSelect. */
export function encodeTensorSelect(writer: BinaryWriter, value: TensorSelect): void {
    writer.writeUnsigned(value.destOffset);
    writer.writeUnsigned(value.maskOffset);
    writer.writeUnsigned(value.thenOffset);
    writer.writeUnsigned(value.elseOffset);
    encodeTensorLayoutId(writer, value.maskLayout);
    encodeTensorLayoutId(writer, value.thenLayout);
    encodeTensorLayoutId(writer, value.elseLayout);
    encodeTensorLayoutId(writer, value.destLayout);
}

/** Decode one TensorSelect. */
export function decodeTensorSelect(reader: BinaryReader): TensorSelect {
    const destOffset = reader.readNumber();
    const maskOffset = reader.readNumber();
    const thenOffset = reader.readNumber();
    const elseOffset = reader.readNumber();
    const maskLayout = decodeTensorLayoutId(reader);
    const thenLayout = decodeTensorLayoutId(reader);
    const elseLayout = decodeTensorLayoutId(reader);
    const destLayout = decodeTensorLayoutId(reader);

    return {
        destOffset,
        maskOffset,
        thenOffset,
        elseOffset,
        maskLayout,
        thenLayout,
        elseLayout,
        destLayout,
    };
}

/** Return one JSON value for one TensorSelect. */
export function toJsonTensorSelect(value: TensorSelect): Json {
    return {
        destOffset: value.destOffset,
        maskOffset: value.maskOffset,
        thenOffset: value.thenOffset,
        elseOffset: value.elseOffset,
        maskLayout: toJsonTensorLayoutId(value.maskLayout),
        thenLayout: toJsonTensorLayoutId(value.thenLayout),
        elseLayout: toJsonTensorLayoutId(value.elseLayout),
        destLayout: toJsonTensorLayoutId(value.destLayout),
    };
}

/** Return one TensorSelect from one JSON value. */
export function fromJsonTensorSelect(value: Json): TensorSelect {
    const object = jsonObject(value);

    return {
        destOffset: jsonInteger(jsonField(object, "destOffset")),
        maskOffset: jsonInteger(jsonField(object, "maskOffset")),
        thenOffset: jsonInteger(jsonField(object, "thenOffset")),
        elseOffset: jsonInteger(jsonField(object, "elseOffset")),
        maskLayout: fromJsonTensorLayoutId(jsonField(object, "maskLayout")),
        thenLayout: fromJsonTensorLayoutId(jsonField(object, "thenLayout")),
        elseLayout: fromJsonTensorLayoutId(jsonField(object, "elseLayout")),
        destLayout: fromJsonTensorLayoutId(jsonField(object, "destLayout")),
    };
}

/** Convert a tensor element type. */
export type TensorConvert = {
    /** The destination tensor frame offset. */
    readonly destOffset: number;
    /** The source tensor frame offset. */
    readonly tensorOffset: number;
    /** The source tensor layout. */
    readonly sourceLayout: TensorLayoutId;
    /** The destination tensor layout. */
    readonly destLayout: TensorLayoutId;
    /** The source tensor scalar layout. */
    readonly sourceScalar: ScalarLayout;
    /** The destination tensor scalar layout. */
    readonly destScalar: ScalarLayout;
    /** The conversion kernel. */
    readonly mode: TensorConvertMode;
};

export const TensorConvert = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: TensorConvert): void {
        encodeTensorConvert(writer, value);
    },

    /** Decode one TensorConvert. */
    decode(reader: BinaryReader): TensorConvert {
        return decodeTensorConvert(reader);
    },

    /** Return this value as JSON. */
    toJson(value: TensorConvert): Json {
        return toJsonTensorConvert(value);
    },

    /** Return one TensorConvert from one JSON value. */
    fromJson(value: Json): TensorConvert {
        return fromJsonTensorConvert(value);
    },
};

/** Encode one TensorConvert. */
export function encodeTensorConvert(writer: BinaryWriter, value: TensorConvert): void {
    writer.writeUnsigned(value.destOffset);
    writer.writeUnsigned(value.tensorOffset);
    encodeTensorLayoutId(writer, value.sourceLayout);
    encodeTensorLayoutId(writer, value.destLayout);
    encodeScalarLayout(writer, value.sourceScalar);
    encodeScalarLayout(writer, value.destScalar);
    encodeTensorConvertMode(writer, value.mode);
}

/** Decode one TensorConvert. */
export function decodeTensorConvert(reader: BinaryReader): TensorConvert {
    const destOffset = reader.readNumber();
    const tensorOffset = reader.readNumber();
    const sourceLayout = decodeTensorLayoutId(reader);
    const destLayout = decodeTensorLayoutId(reader);
    const sourceScalar = decodeScalarLayout(reader);
    const destScalar = decodeScalarLayout(reader);
    const mode = decodeTensorConvertMode(reader);

    return {
        destOffset,
        tensorOffset,
        sourceLayout,
        destLayout,
        sourceScalar,
        destScalar,
        mode,
    };
}

/** Return one JSON value for one TensorConvert. */
export function toJsonTensorConvert(value: TensorConvert): Json {
    return {
        destOffset: value.destOffset,
        tensorOffset: value.tensorOffset,
        sourceLayout: toJsonTensorLayoutId(value.sourceLayout),
        destLayout: toJsonTensorLayoutId(value.destLayout),
        sourceScalar: toJsonScalarLayout(value.sourceScalar),
        destScalar: toJsonScalarLayout(value.destScalar),
        mode: toJsonTensorConvertMode(value.mode),
    };
}

/** Return one TensorConvert from one JSON value. */
export function fromJsonTensorConvert(value: Json): TensorConvert {
    const object = jsonObject(value);

    return {
        destOffset: jsonInteger(jsonField(object, "destOffset")),
        tensorOffset: jsonInteger(jsonField(object, "tensorOffset")),
        sourceLayout: fromJsonTensorLayoutId(jsonField(object, "sourceLayout")),
        destLayout: fromJsonTensorLayoutId(jsonField(object, "destLayout")),
        sourceScalar: fromJsonScalarLayout(jsonField(object, "sourceScalar")),
        destScalar: fromJsonScalarLayout(jsonField(object, "destScalar")),
        mode: fromJsonTensorConvertMode(jsonField(object, "mode")),
    };
}

/** Create a view into a tensor reference. */
export type TensorView = {
    /** The destination tensor view. */
    readonly destOffset: number;
    /** The source tensor view. */
    readonly viewOffset: number;
    /** The offset, size, and stride values. */
    readonly arguments: U32RangeId;
    /** The number of offset values. */
    readonly offsetsCount: number;
    /** The number of size values. */
    readonly sizesCount: number;
    /** The number of stride values. */
    readonly stridesCount: number;
    /** The source tensor view layout. */
    readonly sourceLayout: TensorLayoutId;
    /** The destination tensor view layout. */
    readonly destLayout: TensorLayoutId;
    /** The tensor element projection. */
    readonly element: ProjectionId;
    /** The tensor view backing memory. */
    readonly address: TensorAddress;
};

export const TensorView = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: TensorView): void {
        encodeTensorView(writer, value);
    },

    /** Decode one TensorView. */
    decode(reader: BinaryReader): TensorView {
        return decodeTensorView(reader);
    },

    /** Return this value as JSON. */
    toJson(value: TensorView): Json {
        return toJsonTensorView(value);
    },

    /** Return one TensorView from one JSON value. */
    fromJson(value: Json): TensorView {
        return fromJsonTensorView(value);
    },
};

/** Encode one TensorView. */
export function encodeTensorView(writer: BinaryWriter, value: TensorView): void {
    writer.writeUnsigned(value.destOffset);
    writer.writeUnsigned(value.viewOffset);
    encodeU32RangeId(writer, value.arguments);
    writer.writeUnsigned(value.offsetsCount);
    writer.writeUnsigned(value.sizesCount);
    writer.writeUnsigned(value.stridesCount);
    encodeTensorLayoutId(writer, value.sourceLayout);
    encodeTensorLayoutId(writer, value.destLayout);
    encodeProjectionId(writer, value.element);
    encodeTensorAddress(writer, value.address);
}

/** Decode one TensorView. */
export function decodeTensorView(reader: BinaryReader): TensorView {
    const destOffset = reader.readNumber();
    const viewOffset = reader.readNumber();
    const arguments_ = decodeU32RangeId(reader);
    const offsetsCount = reader.readNumber();
    const sizesCount = reader.readNumber();
    const stridesCount = reader.readNumber();
    const sourceLayout = decodeTensorLayoutId(reader);
    const destLayout = decodeTensorLayoutId(reader);
    const element = decodeProjectionId(reader);
    const address = decodeTensorAddress(reader);

    return {
        destOffset,
        viewOffset,
        arguments: arguments_,
        offsetsCount,
        sizesCount,
        stridesCount,
        sourceLayout,
        destLayout,
        element,
        address,
    };
}

/** Return one JSON value for one TensorView. */
export function toJsonTensorView(value: TensorView): Json {
    return {
        destOffset: value.destOffset,
        viewOffset: value.viewOffset,
        arguments: toJsonU32RangeId(value.arguments),
        offsetsCount: value.offsetsCount,
        sizesCount: value.sizesCount,
        stridesCount: value.stridesCount,
        sourceLayout: toJsonTensorLayoutId(value.sourceLayout),
        destLayout: toJsonTensorLayoutId(value.destLayout),
        element: toJsonProjectionId(value.element),
        address: toJsonTensorAddress(value.address),
    };
}

/** Return one TensorView from one JSON value. */
export function fromJsonTensorView(value: Json): TensorView {
    const object = jsonObject(value);

    return {
        destOffset: jsonInteger(jsonField(object, "destOffset")),
        viewOffset: jsonInteger(jsonField(object, "viewOffset")),
        arguments: fromJsonU32RangeId(jsonField(object, "arguments")),
        offsetsCount: jsonInteger(jsonField(object, "offsetsCount")),
        sizesCount: jsonInteger(jsonField(object, "sizesCount")),
        stridesCount: jsonInteger(jsonField(object, "stridesCount")),
        sourceLayout: fromJsonTensorLayoutId(jsonField(object, "sourceLayout")),
        destLayout: fromJsonTensorLayoutId(jsonField(object, "destLayout")),
        element: fromJsonProjectionId(jsonField(object, "element")),
        address: fromJsonTensorAddress(jsonField(object, "address")),
    };
}

/** Intrinsic call. */
export type Intrinsic = {
    /** The intrinsic kernel. */
    readonly kernel: mirTreeIntrinsic.Intrinsic;
    /** The destination shape. */
    readonly dest: IntrinsicDest;
    /** The pooled argument range. */
    readonly arguments: ArgumentRange;
    /** The lowered shape for each argument. */
    readonly layouts: readonly [ValueShape, ValueShape, ValueShape, ValueShape, ValueShape, ValueShape, ValueShape, ValueShape, ValueShape, ValueShape, ValueShape, ValueShape, ValueShape, ValueShape, ValueShape, ValueShape];
    /** The argument count. */
    readonly layoutCount: number;
};

export const Intrinsic = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: Intrinsic): void {
        encodeIntrinsic(writer, value);
    },

    /** Decode one Intrinsic. */
    decode(reader: BinaryReader): Intrinsic {
        return decodeIntrinsic(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Intrinsic): Json {
        return toJsonIntrinsic(value);
    },

    /** Return one Intrinsic from one JSON value. */
    fromJson(value: Json): Intrinsic {
        return fromJsonIntrinsic(value);
    },
};

/** Encode one Intrinsic. */
export function encodeIntrinsic(writer: BinaryWriter, value: Intrinsic): void {
    mirTreeIntrinsic.encodeIntrinsic(writer, value.kernel);
    encodeIntrinsicDest(writer, value.dest);
    encodeArgumentRange(writer, value.arguments);
    for (const item3 of value.layouts) {
        encodeValueShape(writer, item3);
    }
    writer.writeByte(value.layoutCount);
}

/** Decode one Intrinsic. */
export function decodeIntrinsic(reader: BinaryReader): Intrinsic {
    const kernel = mirTreeIntrinsic.decodeIntrinsic(reader);
    const dest = decodeIntrinsicDest(reader);
    const arguments_ = decodeArgumentRange(reader);
    const layouts = [decodeValueShape(reader), decodeValueShape(reader), decodeValueShape(reader), decodeValueShape(reader), decodeValueShape(reader), decodeValueShape(reader), decodeValueShape(reader), decodeValueShape(reader), decodeValueShape(reader), decodeValueShape(reader), decodeValueShape(reader), decodeValueShape(reader), decodeValueShape(reader), decodeValueShape(reader), decodeValueShape(reader), decodeValueShape(reader)] as const;
    const layoutCount = reader.readByte();

    return {
        kernel,
        dest,
        arguments: arguments_,
        layouts,
        layoutCount,
    };
}

/** Return one JSON value for one Intrinsic. */
export function toJsonIntrinsic(value: Intrinsic): Json {
    return {
        kernel: mirTreeIntrinsic.toJsonIntrinsic(value.kernel),
        dest: toJsonIntrinsicDest(value.dest),
        arguments: toJsonArgumentRange(value.arguments),
        layouts: value.layouts.map((item0) => toJsonValueShape(item0)),
        layoutCount: value.layoutCount,
    };
}

/** Return one Intrinsic from one JSON value. */
export function fromJsonIntrinsic(value: Json): Intrinsic {
    const object = jsonObject(value);

    return {
        kernel: mirTreeIntrinsic.fromJsonIntrinsic(jsonField(object, "kernel")),
        dest: fromJsonIntrinsicDest(jsonField(object, "dest")),
        arguments: fromJsonArgumentRange(jsonField(object, "arguments")),
        layouts: (() => { const items = jsonArray(jsonField(object, "layouts")); if (items.length !== 16) { throw new SerdeError(`expected JSON array length 16: ${items.length}`); } return [fromJsonValueShape(items[0]), fromJsonValueShape(items[1]), fromJsonValueShape(items[2]), fromJsonValueShape(items[3]), fromJsonValueShape(items[4]), fromJsonValueShape(items[5]), fromJsonValueShape(items[6]), fromJsonValueShape(items[7]), fromJsonValueShape(items[8]), fromJsonValueShape(items[9]), fromJsonValueShape(items[10]), fromJsonValueShape(items[11]), fromJsonValueShape(items[12]), fromJsonValueShape(items[13]), fromJsonValueShape(items[14]), fromJsonValueShape(items[15])] as const; })(),
        layoutCount: jsonInteger(jsonField(object, "layoutCount")),
    };
}

/** Intrinsic destination. */
export type IntrinsicDest =
    /** No destination. */
    | {
          readonly kind: "none";
      }
    /** Cell destination frame offset. */
    | {
          readonly kind: "cell";
          readonly cell: number;
      }
    /** Frame destination value. */
    | {
          readonly kind: "frame";
          readonly frame: Value;
      }
;

export const IntrinsicDest = {
    /** No destination. */
    none(): IntrinsicDest {
        return { kind: "none" };
    },

    /** Cell destination frame offset. */
    cell(cell: number): IntrinsicDest {
        return { kind: "cell", cell };
    },

    /** Frame destination value. */
    frame(frame: Value): IntrinsicDest {
        return { kind: "frame", frame };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: IntrinsicDest): void {
        encodeIntrinsicDest(writer, value);
    },

    /** Decode one IntrinsicDest. */
    decode(reader: BinaryReader): IntrinsicDest {
        return decodeIntrinsicDest(reader);
    },

    /** Return this value as JSON. */
    toJson(value: IntrinsicDest): Json {
        return toJsonIntrinsicDest(value);
    },

    /** Return one IntrinsicDest from one JSON value. */
    fromJson(value: Json): IntrinsicDest {
        return fromJsonIntrinsicDest(value);
    },
};

/** Encode one IntrinsicDest. */
export function encodeIntrinsicDest(writer: BinaryWriter, value: IntrinsicDest): void {
    switch (value.kind) {
        case "none":
            writer.writeUnsigned(0);
            return;
        case "cell":
            writer.writeUnsigned(1);
            writer.writeUnsigned(value.cell);
            return;
        case "frame":
            writer.writeUnsigned(2);
            encodeValue(writer, value.frame);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one IntrinsicDest. */
export function decodeIntrinsicDest(reader: BinaryReader): IntrinsicDest {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            return { kind: "none" };
        }
        case 1: {
            const cell = reader.readNumber();

            return { kind: "cell", cell };
        }
        case 2: {
            const frame = decodeValue(reader);

            return { kind: "frame", frame };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one IntrinsicDest. */
export function toJsonIntrinsicDest(value: IntrinsicDest): Json {
    switch (value.kind) {
        case "none":
            return {
                kind: "none",
            };
        case "cell":
            return {
                kind: "cell",
                cell: value.cell,
            };
        case "frame":
            return {
                kind: "frame",
                frame: toJsonValue(value.frame),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one IntrinsicDest from one JSON value. */
export function fromJsonIntrinsicDest(value: Json): IntrinsicDest {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "none":
            return {
                kind,
            };
        case "cell":
            return {
                kind,
                cell: jsonInteger(jsonField(object, "cell")),
            };
        case "frame":
            return {
                kind,
                frame: fromJsonValue(jsonField(object, "frame")),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** Tail call to a function. */
export type TailCall = {
    /** The callee function index. */
    readonly function: number;
    /** The resolved call target. */
    readonly target: CallTarget;
    /** The argument frame moves. */
    readonly moves: MoveRange;
};

export const TailCall = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: TailCall): void {
        encodeTailCall(writer, value);
    },

    /** Decode one TailCall. */
    decode(reader: BinaryReader): TailCall {
        return decodeTailCall(reader);
    },

    /** Return this value as JSON. */
    toJson(value: TailCall): Json {
        return toJsonTailCall(value);
    },

    /** Return one TailCall from one JSON value. */
    fromJson(value: Json): TailCall {
        return fromJsonTailCall(value);
    },
};

/** Encode one TailCall. */
export function encodeTailCall(writer: BinaryWriter, value: TailCall): void {
    writer.writeUnsigned(value.function);
    encodeCallTarget(writer, value.target);
    encodeMoveRange(writer, value.moves);
}

/** Decode one TailCall. */
export function decodeTailCall(reader: BinaryReader): TailCall {
    const function_ = reader.readNumber();
    const target = decodeCallTarget(reader);
    const moves = decodeMoveRange(reader);

    return {
        function: function_,
        target,
        moves,
    };
}

/** Return one JSON value for one TailCall. */
export function toJsonTailCall(value: TailCall): Json {
    return {
        function: value.function,
        target: toJsonCallTarget(value.target),
        moves: toJsonMoveRange(value.moves),
    };
}

/** Return one TailCall from one JSON value. */
export function fromJsonTailCall(value: Json): TailCall {
    const object = jsonObject(value);

    return {
        function: jsonInteger(jsonField(object, "function")),
        target: fromJsonCallTarget(jsonField(object, "target")),
        moves: fromJsonMoveRange(jsonField(object, "moves")),
    };
}

/** Class tail call. */
export type TailCallVirtual = {
    /** The receiver cell offset. */
    readonly receiverOffset: number;
    /** The dispatch table field projection. */
    readonly tableField: ProjectionId;
    /** The dispatch table slot. */
    readonly slot: number;
    /** The pooled argument range. */
    readonly arguments: ArgumentRange;
};

export const TailCallVirtual = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: TailCallVirtual): void {
        encodeTailCallVirtual(writer, value);
    },

    /** Decode one TailCallVirtual. */
    decode(reader: BinaryReader): TailCallVirtual {
        return decodeTailCallVirtual(reader);
    },

    /** Return this value as JSON. */
    toJson(value: TailCallVirtual): Json {
        return toJsonTailCallVirtual(value);
    },

    /** Return one TailCallVirtual from one JSON value. */
    fromJson(value: Json): TailCallVirtual {
        return fromJsonTailCallVirtual(value);
    },
};

/** Encode one TailCallVirtual. */
export function encodeTailCallVirtual(writer: BinaryWriter, value: TailCallVirtual): void {
    writer.writeUnsigned(value.receiverOffset);
    encodeProjectionId(writer, value.tableField);
    writer.writeUnsigned(value.slot);
    encodeArgumentRange(writer, value.arguments);
}

/** Decode one TailCallVirtual. */
export function decodeTailCallVirtual(reader: BinaryReader): TailCallVirtual {
    const receiverOffset = reader.readNumber();
    const tableField = decodeProjectionId(reader);
    const slot = reader.readNumber();
    const arguments_ = decodeArgumentRange(reader);

    return {
        receiverOffset,
        tableField,
        slot,
        arguments: arguments_,
    };
}

/** Return one JSON value for one TailCallVirtual. */
export function toJsonTailCallVirtual(value: TailCallVirtual): Json {
    return {
        receiverOffset: value.receiverOffset,
        tableField: toJsonProjectionId(value.tableField),
        slot: value.slot,
        arguments: toJsonArgumentRange(value.arguments),
    };
}

/** Return one TailCallVirtual from one JSON value. */
export function fromJsonTailCallVirtual(value: Json): TailCallVirtual {
    const object = jsonObject(value);

    return {
        receiverOffset: jsonInteger(jsonField(object, "receiverOffset")),
        tableField: fromJsonProjectionId(jsonField(object, "tableField")),
        slot: jsonInteger(jsonField(object, "slot")),
        arguments: fromJsonArgumentRange(jsonField(object, "arguments")),
    };
}

/** Dynamic tail call. */
export type TailCallDynamic = {
    /** The receiver cell offset. */
    readonly receiverOffset: number;
    /** The dynamic table field projection. */
    readonly tableField: ProjectionId;
    /** The dynamic table slot. */
    readonly slot: number;
    /** The pooled argument range. */
    readonly arguments: ArgumentRange;
};

export const TailCallDynamic = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: TailCallDynamic): void {
        encodeTailCallDynamic(writer, value);
    },

    /** Decode one TailCallDynamic. */
    decode(reader: BinaryReader): TailCallDynamic {
        return decodeTailCallDynamic(reader);
    },

    /** Return this value as JSON. */
    toJson(value: TailCallDynamic): Json {
        return toJsonTailCallDynamic(value);
    },

    /** Return one TailCallDynamic from one JSON value. */
    fromJson(value: Json): TailCallDynamic {
        return fromJsonTailCallDynamic(value);
    },
};

/** Encode one TailCallDynamic. */
export function encodeTailCallDynamic(writer: BinaryWriter, value: TailCallDynamic): void {
    writer.writeUnsigned(value.receiverOffset);
    encodeProjectionId(writer, value.tableField);
    writer.writeUnsigned(value.slot);
    encodeArgumentRange(writer, value.arguments);
}

/** Decode one TailCallDynamic. */
export function decodeTailCallDynamic(reader: BinaryReader): TailCallDynamic {
    const receiverOffset = reader.readNumber();
    const tableField = decodeProjectionId(reader);
    const slot = reader.readNumber();
    const arguments_ = decodeArgumentRange(reader);

    return {
        receiverOffset,
        tableField,
        slot,
        arguments: arguments_,
    };
}

/** Return one JSON value for one TailCallDynamic. */
export function toJsonTailCallDynamic(value: TailCallDynamic): Json {
    return {
        receiverOffset: value.receiverOffset,
        tableField: toJsonProjectionId(value.tableField),
        slot: value.slot,
        arguments: toJsonArgumentRange(value.arguments),
    };
}

/** Return one TailCallDynamic from one JSON value. */
export function fromJsonTailCallDynamic(value: Json): TailCallDynamic {
    const object = jsonObject(value);

    return {
        receiverOffset: jsonInteger(jsonField(object, "receiverOffset")),
        tableField: fromJsonProjectionId(jsonField(object, "tableField")),
        slot: jsonInteger(jsonField(object, "slot")),
        arguments: fromJsonArgumentRange(jsonField(object, "arguments")),
    };
}

/** Indirect tail call. */
export type IndirectTailCall = {
    /** The callee cell offset. */
    readonly calleeOffset: number;
    /** The expected function signature. */
    readonly signature: LocalNodeId;
    /** The pooled argument range. */
    readonly arguments: ArgumentRange;
};

export const IndirectTailCall = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: IndirectTailCall): void {
        encodeIndirectTailCall(writer, value);
    },

    /** Decode one IndirectTailCall. */
    decode(reader: BinaryReader): IndirectTailCall {
        return decodeIndirectTailCall(reader);
    },

    /** Return this value as JSON. */
    toJson(value: IndirectTailCall): Json {
        return toJsonIndirectTailCall(value);
    },

    /** Return one IndirectTailCall from one JSON value. */
    fromJson(value: Json): IndirectTailCall {
        return fromJsonIndirectTailCall(value);
    },
};

/** Encode one IndirectTailCall. */
export function encodeIndirectTailCall(writer: BinaryWriter, value: IndirectTailCall): void {
    writer.writeUnsigned(value.calleeOffset);
    encodeLocalNodeId(writer, value.signature);
    encodeArgumentRange(writer, value.arguments);
}

/** Decode one IndirectTailCall. */
export function decodeIndirectTailCall(reader: BinaryReader): IndirectTailCall {
    const calleeOffset = reader.readNumber();
    const signature = decodeLocalNodeId(reader);
    const arguments_ = decodeArgumentRange(reader);

    return {
        calleeOffset,
        signature,
        arguments: arguments_,
    };
}

/** Return one JSON value for one IndirectTailCall. */
export function toJsonIndirectTailCall(value: IndirectTailCall): Json {
    return {
        calleeOffset: value.calleeOffset,
        signature: toJsonLocalNodeId(value.signature),
        arguments: toJsonArgumentRange(value.arguments),
    };
}

/** Return one IndirectTailCall from one JSON value. */
export function fromJsonIndirectTailCall(value: Json): IndirectTailCall {
    const object = jsonObject(value);

    return {
        calleeOffset: jsonInteger(jsonField(object, "calleeOffset")),
        signature: fromJsonLocalNodeId(jsonField(object, "signature")),
        arguments: fromJsonArgumentRange(jsonField(object, "arguments")),
    };
}
