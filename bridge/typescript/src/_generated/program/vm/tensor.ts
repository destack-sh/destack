/* generated by destack_bridge_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, jsonArray, jsonBigint, jsonBool, jsonField, jsonInteger, jsonObject, jsonString } from "../../../protocol/serde.js";
import type { Projection } from "./projection.js";
import type { ScalarLayout } from "./value.js";
import { decodeProjection, encodeProjection, fromJsonProjection, toJsonProjection } from "./projection.js";
import { decodeScalarLayout, encodeScalarLayout, fromJsonScalarLayout, toJsonScalarLayout } from "./value.js";

/** Tensor view backing memory selected by lowering. */
export type TensorAddress = "heap" | "sharedHeap" | "address" | "stack" | "frame" | "static";

export const TensorAddress = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: TensorAddress): void {
        encodeTensorAddress(writer, value);
    },

    /** Decode one TensorAddress. */
    decode(reader: BinaryReader): TensorAddress {
        return decodeTensorAddress(reader);
    },

    /** Return this value as JSON. */
    toJson(value: TensorAddress): Json {
        return toJsonTensorAddress(value);
    },

    /** Return one TensorAddress from one JSON value. */
    fromJson(value: Json): TensorAddress {
        return fromJsonTensorAddress(value);
    },
};

/** Encode one TensorAddress. */
export function encodeTensorAddress(writer: BinaryWriter, value: TensorAddress): void {
    switch (value) {
        case "heap":
            writer.writeUnsigned(0);
            return;
        case "sharedHeap":
            writer.writeUnsigned(1);
            return;
        case "address":
            writer.writeUnsigned(2);
            return;
        case "stack":
            writer.writeUnsigned(3);
            return;
        case "frame":
            writer.writeUnsigned(4);
            return;
        case "static":
            writer.writeUnsigned(5);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one TensorAddress. */
export function decodeTensorAddress(reader: BinaryReader): TensorAddress {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "heap";
        case 1:
            return "sharedHeap";
        case 2:
            return "address";
        case 3:
            return "stack";
        case 4:
            return "frame";
        case 5:
            return "static";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one TensorAddress. */
export function toJsonTensorAddress(value: TensorAddress): Json {
    return value;
}

/** Return one TensorAddress from one JSON value. */
export function fromJsonTensorAddress(value: Json): TensorAddress {
    const variant = jsonString(value);

    switch (variant) {
        case "heap":
            return "heap";
        case "sharedHeap":
            return "sharedHeap";
        case "address":
            return "address";
        case "stack":
            return "stack";
        case "frame":
            return "frame";
        case "static":
            return "static";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** Flattened tensor layout compiled for VM execution. */
export type TensorLayout = {
    /** The tensor payload byte width. */
    readonly byteLen: number;
    /** The static tensor shape. */
    readonly shape: ReadonlyArray<bigint>;
    /** The per-dimension strides in element units. */
    readonly strides: ReadonlyArray<bigint>;
    /** The number of logical tensor elements. */
    readonly elementCount: number;
    /** The number of addressable element positions. */
    readonly elementSpanLen: number;
    /** Whether logical elements are stored contiguously. */
    readonly isContiguous: boolean;
    /** The scalar layout of each element. */
    readonly elementLayout: ScalarLayout;
    /** The frame projection for each element. */
    readonly element: Projection;
};

export const TensorLayout = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: TensorLayout): void {
        encodeTensorLayout(writer, value);
    },

    /** Decode one TensorLayout. */
    decode(reader: BinaryReader): TensorLayout {
        return decodeTensorLayout(reader);
    },

    /** Return this value as JSON. */
    toJson(value: TensorLayout): Json {
        return toJsonTensorLayout(value);
    },

    /** Return one TensorLayout from one JSON value. */
    fromJson(value: Json): TensorLayout {
        return fromJsonTensorLayout(value);
    },
};

/** Encode one TensorLayout. */
export function encodeTensorLayout(writer: BinaryWriter, value: TensorLayout): void {
    writer.writeUnsigned(value.byteLen);
    writer.writeUnsigned(value.shape.length);
    for (const item1 of value.shape) {
        writer.writeUnsigned(item1);
    }
    writer.writeUnsigned(value.strides.length);
    for (const item2 of value.strides) {
        writer.writeUnsigned(item2);
    }
    writer.writeUnsigned(value.elementCount);
    writer.writeUnsigned(value.elementSpanLen);
    writer.writeBool(value.isContiguous);
    encodeScalarLayout(writer, value.elementLayout);
    encodeProjection(writer, value.element);
}

/** Decode one TensorLayout. */
export function decodeTensorLayout(reader: BinaryReader): TensorLayout {
    const byteLen = reader.readNumber();
    const shape = (() => { const length1 = reader.readNumber(); const items1: Array<bigint> = []; for (let index = 0; index < length1; index += 1) { items1.push(reader.readUnsigned()); } return items1; })();
    const strides = (() => { const length2 = reader.readNumber(); const items2: Array<bigint> = []; for (let index = 0; index < length2; index += 1) { items2.push(reader.readUnsigned()); } return items2; })();
    const elementCount = reader.readNumber();
    const elementSpanLen = reader.readNumber();
    const isContiguous = reader.readBool();
    const elementLayout = decodeScalarLayout(reader);
    const element = decodeProjection(reader);

    return {
        byteLen,
        shape,
        strides,
        elementCount,
        elementSpanLen,
        isContiguous,
        elementLayout,
        element,
    };
}

/** Return one JSON value for one TensorLayout. */
export function toJsonTensorLayout(value: TensorLayout): Json {
    return {
        byteLen: value.byteLen,
        shape: value.shape.map((item0) => item0.toString()),
        strides: value.strides.map((item0) => item0.toString()),
        elementCount: value.elementCount,
        elementSpanLen: value.elementSpanLen,
        isContiguous: value.isContiguous,
        elementLayout: toJsonScalarLayout(value.elementLayout),
        element: toJsonProjection(value.element),
    };
}

/** Return one TensorLayout from one JSON value. */
export function fromJsonTensorLayout(value: Json): TensorLayout {
    const object = jsonObject(value);

    return {
        byteLen: jsonInteger(jsonField(object, "byteLen")),
        shape: jsonArray(jsonField(object, "shape")).map((item0) => jsonBigint(item0)),
        strides: jsonArray(jsonField(object, "strides")).map((item0) => jsonBigint(item0)),
        elementCount: jsonInteger(jsonField(object, "elementCount")),
        elementSpanLen: jsonInteger(jsonField(object, "elementSpanLen")),
        isContiguous: jsonBool(jsonField(object, "isContiguous")),
        elementLayout: fromJsonScalarLayout(jsonField(object, "elementLayout")),
        element: fromJsonProjection(jsonField(object, "element")),
    };
}
