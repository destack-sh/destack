/* generated by destack_bridge_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, jsonField, jsonObject, jsonOptional, jsonString } from "../../../protocol/serde.js";
import type { ContentId } from "../../source/file/model/file.js";
import { decodeContentId, encodeContentId, fromJsonContentId, toJsonContentId } from "../../source/file/model/file.js";

/** Relocatable native object image. */
export type Object = {
    /** The object file format. */
    readonly format: ObjectFormat;
    /** The object file bytes. */
    readonly content: ContentId;
    /** Native unwind metadata bytes. */
    readonly unwind?: ContentId;
};

export const Object = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: Object): void {
        encodeObject(writer, value);
    },

    /** Decode one Object. */
    decode(reader: BinaryReader): Object {
        return decodeObject(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Object): Json {
        return toJsonObject(value);
    },

    /** Return one Object from one JSON value. */
    fromJson(value: Json): Object {
        return fromJsonObject(value);
    },
};

/** Encode one Object. */
export function encodeObject(writer: BinaryWriter, value: Object): void {
    encodeObjectFormat(writer, value.format);
    encodeContentId(writer, value.content);
    writer.writeOption(value.unwind, (value2) => {
        encodeContentId(writer, value2);
    });
}

/** Decode one Object. */
export function decodeObject(reader: BinaryReader): Object {
    const format = decodeObjectFormat(reader);
    const content = decodeContentId(reader);
    const unwind = reader.readOption(() => decodeContentId(reader));

    return {
        format,
        content,
        ...(unwind === undefined ? {} : { unwind }),
    };
}

/** Return one JSON value for one Object. */
export function toJsonObject(value: Object): Json {
    return {
        format: toJsonObjectFormat(value.format),
        content: toJsonContentId(value.content),
        ...(value.unwind === undefined ? {} : { unwind: toJsonContentId(value.unwind) }),
    };
}

/** Return one Object from one JSON value. */
export function fromJsonObject(value: Json): Object {
    const object = jsonObject(value);

    return {
        format: fromJsonObjectFormat(jsonField(object, "format")),
        content: fromJsonContentId(jsonField(object, "content")),
        unwind: jsonOptional(object, "unwind", (value) => fromJsonContentId(value)),
    };
}

/** Native object file format. */
export type ObjectFormat = "elf" | "machO" | "coff";

export const ObjectFormat = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ObjectFormat): void {
        encodeObjectFormat(writer, value);
    },

    /** Decode one ObjectFormat. */
    decode(reader: BinaryReader): ObjectFormat {
        return decodeObjectFormat(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ObjectFormat): Json {
        return toJsonObjectFormat(value);
    },

    /** Return one ObjectFormat from one JSON value. */
    fromJson(value: Json): ObjectFormat {
        return fromJsonObjectFormat(value);
    },
};

/** Encode one ObjectFormat. */
export function encodeObjectFormat(writer: BinaryWriter, value: ObjectFormat): void {
    switch (value) {
        case "elf":
            writer.writeUnsigned(0);
            return;
        case "machO":
            writer.writeUnsigned(1);
            return;
        case "coff":
            writer.writeUnsigned(2);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one ObjectFormat. */
export function decodeObjectFormat(reader: BinaryReader): ObjectFormat {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "elf";
        case 1:
            return "machO";
        case 2:
            return "coff";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one ObjectFormat. */
export function toJsonObjectFormat(value: ObjectFormat): Json {
    return value;
}

/** Return one ObjectFormat from one JSON value. */
export function fromJsonObjectFormat(value: Json): ObjectFormat {
    const variant = jsonString(value);

    switch (variant) {
        case "elf":
            return "elf";
        case "machO":
            return "machO";
        case "coff":
            return "coff";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}
