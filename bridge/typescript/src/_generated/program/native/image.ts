/* generated by destack_bridge_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, jsonField, jsonObject, jsonString } from "../../../protocol/serde.js";
import type { Library } from "./library.js";
import type { Object } from "./object.js";
import { decodeLibrary, encodeLibrary, fromJsonLibrary, toJsonLibrary } from "./library.js";
import { decodeObject, encodeObject, fromJsonObject, toJsonObject } from "./object.js";

/** Durable native image payload. */
export type Image =
    /** Native functions are already linked into the current process. */
    | {
          readonly kind: "resident";
      }
    /** Native functions live in a loadable native library. */
    | {
          readonly kind: "library";
          readonly library: Library;
      }
    /** Native functions live in a relocatable object artifact. */
    | {
          readonly kind: "object";
          readonly object: Object;
      }
;

export const Image = {
    /** Native functions are already linked into the current process. */
    resident(): Image {
        return { kind: "resident" };
    },

    /** Native functions live in a loadable native library. */
    library(library: Library): Image {
        return { kind: "library", library };
    },

    /** Native functions live in a relocatable object artifact. */
    "object"(object_: Object): Image {
        return { kind: "object", object: object_ };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: Image): void {
        encodeImage(writer, value);
    },

    /** Decode one Image. */
    decode(reader: BinaryReader): Image {
        return decodeImage(reader);
    },

    /** Return this value as JSON. */
    toJson(value: Image): Json {
        return toJsonImage(value);
    },

    /** Return one Image from one JSON value. */
    fromJson(value: Json): Image {
        return fromJsonImage(value);
    },
};

/** Encode one Image. */
export function encodeImage(writer: BinaryWriter, value: Image): void {
    switch (value.kind) {
        case "resident":
            writer.writeUnsigned(0);
            return;
        case "library":
            writer.writeUnsigned(1);
            encodeLibrary(writer, value.library);
            return;
        case "object":
            writer.writeUnsigned(2);
            encodeObject(writer, value.object);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one Image. */
export function decodeImage(reader: BinaryReader): Image {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            return { kind: "resident" };
        }
        case 1: {
            const library = decodeLibrary(reader);

            return { kind: "library", library };
        }
        case 2: {
            const object_ = decodeObject(reader);

            return { kind: "object", object: object_ };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one Image. */
export function toJsonImage(value: Image): Json {
    switch (value.kind) {
        case "resident":
            return {
                kind: "resident",
            };
        case "library":
            return {
                kind: "library",
                library: toJsonLibrary(value.library),
            };
        case "object":
            return {
                kind: "object",
                object: toJsonObject(value.object),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one Image from one JSON value. */
export function fromJsonImage(value: Json): Image {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "resident":
            return {
                kind,
            };
        case "library":
            return {
                kind,
                library: fromJsonLibrary(jsonField(object, "library")),
            };
        case "object":
            return {
                kind,
                object: fromJsonObject(jsonField(object, "object")),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}
