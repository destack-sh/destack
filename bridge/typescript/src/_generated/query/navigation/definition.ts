/* generated by destack_bridge_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, jsonArray, jsonField, jsonObject, jsonString } from "../../../protocol/serde.js";
import type { QueryPosition } from "../core/target.js";
import type { QueryTarget } from "../core/target.js";
import { decodeQueryPosition, encodeQueryPosition, fromJsonQueryPosition, toJsonQueryPosition } from "../core/target.js";
import { decodeQueryTarget, encodeQueryTarget, fromJsonQueryTarget, toJsonQueryTarget } from "../core/target.js";

/** Request goto definition at a cursor position. */
export type GotoDefinitionRequest = {
    /** The queried position. */
    readonly position: QueryPosition;
};

export const GotoDefinitionRequest = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: GotoDefinitionRequest): void {
        encodeGotoDefinitionRequest(writer, value);
    },

    /** Decode one GotoDefinitionRequest. */
    decode(reader: BinaryReader): GotoDefinitionRequest {
        return decodeGotoDefinitionRequest(reader);
    },

    /** Return this value as JSON. */
    toJson(value: GotoDefinitionRequest): Json {
        return toJsonGotoDefinitionRequest(value);
    },

    /** Return one GotoDefinitionRequest from one JSON value. */
    fromJson(value: Json): GotoDefinitionRequest {
        return fromJsonGotoDefinitionRequest(value);
    },
};

/** Encode one GotoDefinitionRequest. */
export function encodeGotoDefinitionRequest(writer: BinaryWriter, value: GotoDefinitionRequest): void {
    encodeQueryPosition(writer, value.position);
}

/** Decode one GotoDefinitionRequest. */
export function decodeGotoDefinitionRequest(reader: BinaryReader): GotoDefinitionRequest {
    const position = decodeQueryPosition(reader);

    return {
        position,
    };
}

/** Return one JSON value for one GotoDefinitionRequest. */
export function toJsonGotoDefinitionRequest(value: GotoDefinitionRequest): Json {
    return {
        position: toJsonQueryPosition(value.position),
    };
}

/** Return one GotoDefinitionRequest from one JSON value. */
export function fromJsonGotoDefinitionRequest(value: Json): GotoDefinitionRequest {
    const object = jsonObject(value);

    return {
        position: fromJsonQueryPosition(jsonField(object, "position")),
    };
}

/** Request goto declaration at a cursor position. */
export type GotoDeclarationRequest = {
    /** The queried position. */
    readonly position: QueryPosition;
};

export const GotoDeclarationRequest = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: GotoDeclarationRequest): void {
        encodeGotoDeclarationRequest(writer, value);
    },

    /** Decode one GotoDeclarationRequest. */
    decode(reader: BinaryReader): GotoDeclarationRequest {
        return decodeGotoDeclarationRequest(reader);
    },

    /** Return this value as JSON. */
    toJson(value: GotoDeclarationRequest): Json {
        return toJsonGotoDeclarationRequest(value);
    },

    /** Return one GotoDeclarationRequest from one JSON value. */
    fromJson(value: Json): GotoDeclarationRequest {
        return fromJsonGotoDeclarationRequest(value);
    },
};

/** Encode one GotoDeclarationRequest. */
export function encodeGotoDeclarationRequest(writer: BinaryWriter, value: GotoDeclarationRequest): void {
    encodeQueryPosition(writer, value.position);
}

/** Decode one GotoDeclarationRequest. */
export function decodeGotoDeclarationRequest(reader: BinaryReader): GotoDeclarationRequest {
    const position = decodeQueryPosition(reader);

    return {
        position,
    };
}

/** Return one JSON value for one GotoDeclarationRequest. */
export function toJsonGotoDeclarationRequest(value: GotoDeclarationRequest): Json {
    return {
        position: toJsonQueryPosition(value.position),
    };
}

/** Return one GotoDeclarationRequest from one JSON value. */
export function fromJsonGotoDeclarationRequest(value: Json): GotoDeclarationRequest {
    const object = jsonObject(value);

    return {
        position: fromJsonQueryPosition(jsonField(object, "position")),
    };
}

/** Request goto type definition at a cursor position. */
export type GotoTypeDefinitionRequest = {
    /** The queried position. */
    readonly position: QueryPosition;
};

export const GotoTypeDefinitionRequest = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: GotoTypeDefinitionRequest): void {
        encodeGotoTypeDefinitionRequest(writer, value);
    },

    /** Decode one GotoTypeDefinitionRequest. */
    decode(reader: BinaryReader): GotoTypeDefinitionRequest {
        return decodeGotoTypeDefinitionRequest(reader);
    },

    /** Return this value as JSON. */
    toJson(value: GotoTypeDefinitionRequest): Json {
        return toJsonGotoTypeDefinitionRequest(value);
    },

    /** Return one GotoTypeDefinitionRequest from one JSON value. */
    fromJson(value: Json): GotoTypeDefinitionRequest {
        return fromJsonGotoTypeDefinitionRequest(value);
    },
};

/** Encode one GotoTypeDefinitionRequest. */
export function encodeGotoTypeDefinitionRequest(writer: BinaryWriter, value: GotoTypeDefinitionRequest): void {
    encodeQueryPosition(writer, value.position);
}

/** Decode one GotoTypeDefinitionRequest. */
export function decodeGotoTypeDefinitionRequest(reader: BinaryReader): GotoTypeDefinitionRequest {
    const position = decodeQueryPosition(reader);

    return {
        position,
    };
}

/** Return one JSON value for one GotoTypeDefinitionRequest. */
export function toJsonGotoTypeDefinitionRequest(value: GotoTypeDefinitionRequest): Json {
    return {
        position: toJsonQueryPosition(value.position),
    };
}

/** Return one GotoTypeDefinitionRequest from one JSON value. */
export function fromJsonGotoTypeDefinitionRequest(value: Json): GotoTypeDefinitionRequest {
    const object = jsonObject(value);

    return {
        position: fromJsonQueryPosition(jsonField(object, "position")),
    };
}

/** Response payload for goto definition queries. */
export type GotoDefinitionResponse = {
    /** Definition targets. */
    readonly targets: ReadonlyArray<NavigationTarget>;
};

export const GotoDefinitionResponse = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: GotoDefinitionResponse): void {
        encodeGotoDefinitionResponse(writer, value);
    },

    /** Decode one GotoDefinitionResponse. */
    decode(reader: BinaryReader): GotoDefinitionResponse {
        return decodeGotoDefinitionResponse(reader);
    },

    /** Return this value as JSON. */
    toJson(value: GotoDefinitionResponse): Json {
        return toJsonGotoDefinitionResponse(value);
    },

    /** Return one GotoDefinitionResponse from one JSON value. */
    fromJson(value: Json): GotoDefinitionResponse {
        return fromJsonGotoDefinitionResponse(value);
    },
};

/** Encode one GotoDefinitionResponse. */
export function encodeGotoDefinitionResponse(writer: BinaryWriter, value: GotoDefinitionResponse): void {
    writer.writeUnsigned(value.targets.length);
    for (const item0 of value.targets) {
        encodeNavigationTarget(writer, item0);
    }
}

/** Decode one GotoDefinitionResponse. */
export function decodeGotoDefinitionResponse(reader: BinaryReader): GotoDefinitionResponse {
    const targets = (() => { const length0 = reader.readNumber(); const items0: Array<NavigationTarget> = []; for (let index = 0; index < length0; index += 1) { items0.push(decodeNavigationTarget(reader)); } return items0; })();

    return {
        targets,
    };
}

/** Return one JSON value for one GotoDefinitionResponse. */
export function toJsonGotoDefinitionResponse(value: GotoDefinitionResponse): Json {
    return {
        targets: value.targets.map((item0) => toJsonNavigationTarget(item0)),
    };
}

/** Return one GotoDefinitionResponse from one JSON value. */
export function fromJsonGotoDefinitionResponse(value: Json): GotoDefinitionResponse {
    const object = jsonObject(value);

    return {
        targets: jsonArray(jsonField(object, "targets")).map((item0) => fromJsonNavigationTarget(item0)),
    };
}

/** One navigation target. */
export type NavigationTarget = {
    /** The target location and resolved identity. */
    readonly target: QueryTarget;
    /** The relationship to the query origin. */
    readonly relation: NavigationRelation;
};

export const NavigationTarget = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: NavigationTarget): void {
        encodeNavigationTarget(writer, value);
    },

    /** Decode one NavigationTarget. */
    decode(reader: BinaryReader): NavigationTarget {
        return decodeNavigationTarget(reader);
    },

    /** Return this value as JSON. */
    toJson(value: NavigationTarget): Json {
        return toJsonNavigationTarget(value);
    },

    /** Return one NavigationTarget from one JSON value. */
    fromJson(value: Json): NavigationTarget {
        return fromJsonNavigationTarget(value);
    },
};

/** Encode one NavigationTarget. */
export function encodeNavigationTarget(writer: BinaryWriter, value: NavigationTarget): void {
    encodeQueryTarget(writer, value.target);
    encodeNavigationRelation(writer, value.relation);
}

/** Decode one NavigationTarget. */
export function decodeNavigationTarget(reader: BinaryReader): NavigationTarget {
    const target = decodeQueryTarget(reader);
    const relation = decodeNavigationRelation(reader);

    return {
        target,
        relation,
    };
}

/** Return one JSON value for one NavigationTarget. */
export function toJsonNavigationTarget(value: NavigationTarget): Json {
    return {
        target: toJsonQueryTarget(value.target),
        relation: toJsonNavigationRelation(value.relation),
    };
}

/** Return one NavigationTarget from one JSON value. */
export function fromJsonNavigationTarget(value: Json): NavigationTarget {
    const object = jsonObject(value);

    return {
        target: fromJsonQueryTarget(jsonField(object, "target")),
        relation: fromJsonNavigationRelation(jsonField(object, "relation")),
    };
}

/** Relationship between a navigation origin and target. */
export type NavigationRelation = "declaration" | "definition" | "typeDefinition" | "implementation";

export const NavigationRelation = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: NavigationRelation): void {
        encodeNavigationRelation(writer, value);
    },

    /** Decode one NavigationRelation. */
    decode(reader: BinaryReader): NavigationRelation {
        return decodeNavigationRelation(reader);
    },

    /** Return this value as JSON. */
    toJson(value: NavigationRelation): Json {
        return toJsonNavigationRelation(value);
    },

    /** Return one NavigationRelation from one JSON value. */
    fromJson(value: Json): NavigationRelation {
        return fromJsonNavigationRelation(value);
    },
};

/** Encode one NavigationRelation. */
export function encodeNavigationRelation(writer: BinaryWriter, value: NavigationRelation): void {
    switch (value) {
        case "declaration":
            writer.writeUnsigned(0);
            return;
        case "definition":
            writer.writeUnsigned(1);
            return;
        case "typeDefinition":
            writer.writeUnsigned(2);
            return;
        case "implementation":
            writer.writeUnsigned(3);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one NavigationRelation. */
export function decodeNavigationRelation(reader: BinaryReader): NavigationRelation {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "declaration";
        case 1:
            return "definition";
        case 2:
            return "typeDefinition";
        case 3:
            return "implementation";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one NavigationRelation. */
export function toJsonNavigationRelation(value: NavigationRelation): Json {
    return value;
}

/** Return one NavigationRelation from one JSON value. */
export function fromJsonNavigationRelation(value: Json): NavigationRelation {
    const variant = jsonString(value);

    switch (variant) {
        case "declaration":
            return "declaration";
        case "definition":
            return "definition";
        case "typeDefinition":
            return "typeDefinition";
        case "implementation":
            return "implementation";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}

/** Response payload for goto declaration queries. */
export type GotoDeclarationResponse = {
    /** Declaration targets. */
    readonly targets: ReadonlyArray<NavigationTarget>;
};

export const GotoDeclarationResponse = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: GotoDeclarationResponse): void {
        encodeGotoDeclarationResponse(writer, value);
    },

    /** Decode one GotoDeclarationResponse. */
    decode(reader: BinaryReader): GotoDeclarationResponse {
        return decodeGotoDeclarationResponse(reader);
    },

    /** Return this value as JSON. */
    toJson(value: GotoDeclarationResponse): Json {
        return toJsonGotoDeclarationResponse(value);
    },

    /** Return one GotoDeclarationResponse from one JSON value. */
    fromJson(value: Json): GotoDeclarationResponse {
        return fromJsonGotoDeclarationResponse(value);
    },
};

/** Encode one GotoDeclarationResponse. */
export function encodeGotoDeclarationResponse(writer: BinaryWriter, value: GotoDeclarationResponse): void {
    writer.writeUnsigned(value.targets.length);
    for (const item0 of value.targets) {
        encodeNavigationTarget(writer, item0);
    }
}

/** Decode one GotoDeclarationResponse. */
export function decodeGotoDeclarationResponse(reader: BinaryReader): GotoDeclarationResponse {
    const targets = (() => { const length0 = reader.readNumber(); const items0: Array<NavigationTarget> = []; for (let index = 0; index < length0; index += 1) { items0.push(decodeNavigationTarget(reader)); } return items0; })();

    return {
        targets,
    };
}

/** Return one JSON value for one GotoDeclarationResponse. */
export function toJsonGotoDeclarationResponse(value: GotoDeclarationResponse): Json {
    return {
        targets: value.targets.map((item0) => toJsonNavigationTarget(item0)),
    };
}

/** Return one GotoDeclarationResponse from one JSON value. */
export function fromJsonGotoDeclarationResponse(value: Json): GotoDeclarationResponse {
    const object = jsonObject(value);

    return {
        targets: jsonArray(jsonField(object, "targets")).map((item0) => fromJsonNavigationTarget(item0)),
    };
}

/** Response payload for goto type definition queries. */
export type GotoTypeDefinitionResponse = {
    /** Type definition targets. */
    readonly targets: ReadonlyArray<NavigationTarget>;
};

export const GotoTypeDefinitionResponse = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: GotoTypeDefinitionResponse): void {
        encodeGotoTypeDefinitionResponse(writer, value);
    },

    /** Decode one GotoTypeDefinitionResponse. */
    decode(reader: BinaryReader): GotoTypeDefinitionResponse {
        return decodeGotoTypeDefinitionResponse(reader);
    },

    /** Return this value as JSON. */
    toJson(value: GotoTypeDefinitionResponse): Json {
        return toJsonGotoTypeDefinitionResponse(value);
    },

    /** Return one GotoTypeDefinitionResponse from one JSON value. */
    fromJson(value: Json): GotoTypeDefinitionResponse {
        return fromJsonGotoTypeDefinitionResponse(value);
    },
};

/** Encode one GotoTypeDefinitionResponse. */
export function encodeGotoTypeDefinitionResponse(writer: BinaryWriter, value: GotoTypeDefinitionResponse): void {
    writer.writeUnsigned(value.targets.length);
    for (const item0 of value.targets) {
        encodeNavigationTarget(writer, item0);
    }
}

/** Decode one GotoTypeDefinitionResponse. */
export function decodeGotoTypeDefinitionResponse(reader: BinaryReader): GotoTypeDefinitionResponse {
    const targets = (() => { const length0 = reader.readNumber(); const items0: Array<NavigationTarget> = []; for (let index = 0; index < length0; index += 1) { items0.push(decodeNavigationTarget(reader)); } return items0; })();

    return {
        targets,
    };
}

/** Return one JSON value for one GotoTypeDefinitionResponse. */
export function toJsonGotoTypeDefinitionResponse(value: GotoTypeDefinitionResponse): Json {
    return {
        targets: value.targets.map((item0) => toJsonNavigationTarget(item0)),
    };
}

/** Return one GotoTypeDefinitionResponse from one JSON value. */
export function fromJsonGotoTypeDefinitionResponse(value: Json): GotoTypeDefinitionResponse {
    const object = jsonObject(value);

    return {
        targets: jsonArray(jsonField(object, "targets")).map((item0) => fromJsonNavigationTarget(item0)),
    };
}
