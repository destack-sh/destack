/* generated by destack_bridge_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, jsonArray, jsonField, jsonObject, jsonString } from "../../../protocol/serde.js";
import type { QueryPosition } from "../core/target.js";
import type { Span } from "../../source/file/model/span.js";
import { decodeQueryPosition, encodeQueryPosition, fromJsonQueryPosition, toJsonQueryPosition } from "../core/target.js";
import { decodeSpan, encodeSpan, fromJsonSpan, toJsonSpan } from "../../source/file/model/span.js";

/** Request highlights at a cursor position. */
export type DocumentHighlightRequest = {
    /** The queried position. */
    readonly position: QueryPosition;
};

export const DocumentHighlightRequest = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: DocumentHighlightRequest): void {
        encodeDocumentHighlightRequest(writer, value);
    },

    /** Decode one DocumentHighlightRequest. */
    decode(reader: BinaryReader): DocumentHighlightRequest {
        return decodeDocumentHighlightRequest(reader);
    },

    /** Return this value as JSON. */
    toJson(value: DocumentHighlightRequest): Json {
        return toJsonDocumentHighlightRequest(value);
    },

    /** Return one DocumentHighlightRequest from one JSON value. */
    fromJson(value: Json): DocumentHighlightRequest {
        return fromJsonDocumentHighlightRequest(value);
    },
};

/** Encode one DocumentHighlightRequest. */
export function encodeDocumentHighlightRequest(writer: BinaryWriter, value: DocumentHighlightRequest): void {
    encodeQueryPosition(writer, value.position);
}

/** Decode one DocumentHighlightRequest. */
export function decodeDocumentHighlightRequest(reader: BinaryReader): DocumentHighlightRequest {
    const position = decodeQueryPosition(reader);

    return {
        position,
    };
}

/** Return one JSON value for one DocumentHighlightRequest. */
export function toJsonDocumentHighlightRequest(value: DocumentHighlightRequest): Json {
    return {
        position: toJsonQueryPosition(value.position),
    };
}

/** Return one DocumentHighlightRequest from one JSON value. */
export function fromJsonDocumentHighlightRequest(value: Json): DocumentHighlightRequest {
    const object = jsonObject(value);

    return {
        position: fromJsonQueryPosition(jsonField(object, "position")),
    };
}

/** Response payload for document highlight queries. */
export type DocumentHighlightResponse = {
    /** Document highlights. */
    readonly highlights: ReadonlyArray<DocumentHighlight>;
};

export const DocumentHighlightResponse = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: DocumentHighlightResponse): void {
        encodeDocumentHighlightResponse(writer, value);
    },

    /** Decode one DocumentHighlightResponse. */
    decode(reader: BinaryReader): DocumentHighlightResponse {
        return decodeDocumentHighlightResponse(reader);
    },

    /** Return this value as JSON. */
    toJson(value: DocumentHighlightResponse): Json {
        return toJsonDocumentHighlightResponse(value);
    },

    /** Return one DocumentHighlightResponse from one JSON value. */
    fromJson(value: Json): DocumentHighlightResponse {
        return fromJsonDocumentHighlightResponse(value);
    },
};

/** Encode one DocumentHighlightResponse. */
export function encodeDocumentHighlightResponse(writer: BinaryWriter, value: DocumentHighlightResponse): void {
    writer.writeUnsigned(value.highlights.length);
    for (const item0 of value.highlights) {
        encodeDocumentHighlight(writer, item0);
    }
}

/** Decode one DocumentHighlightResponse. */
export function decodeDocumentHighlightResponse(reader: BinaryReader): DocumentHighlightResponse {
    const highlights = (() => { const length0 = reader.readNumber(); const items0: Array<DocumentHighlight> = []; for (let index = 0; index < length0; index += 1) { items0.push(decodeDocumentHighlight(reader)); } return items0; })();

    return {
        highlights,
    };
}

/** Return one JSON value for one DocumentHighlightResponse. */
export function toJsonDocumentHighlightResponse(value: DocumentHighlightResponse): Json {
    return {
        highlights: value.highlights.map((item0) => toJsonDocumentHighlight(item0)),
    };
}

/** Return one DocumentHighlightResponse from one JSON value. */
export function fromJsonDocumentHighlightResponse(value: Json): DocumentHighlightResponse {
    const object = jsonObject(value);

    return {
        highlights: jsonArray(jsonField(object, "highlights")).map((item0) => fromJsonDocumentHighlight(item0)),
    };
}

/** A highlighted range in a document. */
export type DocumentHighlight = {
    /** The highlighted range. */
    readonly range: Span;
    /** The kind of highlight. */
    readonly kind: HighlightKind;
};

export const DocumentHighlight = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: DocumentHighlight): void {
        encodeDocumentHighlight(writer, value);
    },

    /** Decode one DocumentHighlight. */
    decode(reader: BinaryReader): DocumentHighlight {
        return decodeDocumentHighlight(reader);
    },

    /** Return this value as JSON. */
    toJson(value: DocumentHighlight): Json {
        return toJsonDocumentHighlight(value);
    },

    /** Return one DocumentHighlight from one JSON value. */
    fromJson(value: Json): DocumentHighlight {
        return fromJsonDocumentHighlight(value);
    },
};

/** Encode one DocumentHighlight. */
export function encodeDocumentHighlight(writer: BinaryWriter, value: DocumentHighlight): void {
    encodeSpan(writer, value.range);
    encodeHighlightKind(writer, value.kind);
}

/** Decode one DocumentHighlight. */
export function decodeDocumentHighlight(reader: BinaryReader): DocumentHighlight {
    const range = decodeSpan(reader);
    const kind = decodeHighlightKind(reader);

    return {
        range,
        kind,
    };
}

/** Return one JSON value for one DocumentHighlight. */
export function toJsonDocumentHighlight(value: DocumentHighlight): Json {
    return {
        range: toJsonSpan(value.range),
        kind: toJsonHighlightKind(value.kind),
    };
}

/** Return one DocumentHighlight from one JSON value. */
export function fromJsonDocumentHighlight(value: Json): DocumentHighlight {
    const object = jsonObject(value);

    return {
        range: fromJsonSpan(jsonField(object, "range")),
        kind: fromJsonHighlightKind(jsonField(object, "kind")),
    };
}

/** Kind of document highlight. */
export type HighlightKind = "text" | "read" | "write";

export const HighlightKind = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: HighlightKind): void {
        encodeHighlightKind(writer, value);
    },

    /** Decode one HighlightKind. */
    decode(reader: BinaryReader): HighlightKind {
        return decodeHighlightKind(reader);
    },

    /** Return this value as JSON. */
    toJson(value: HighlightKind): Json {
        return toJsonHighlightKind(value);
    },

    /** Return one HighlightKind from one JSON value. */
    fromJson(value: Json): HighlightKind {
        return fromJsonHighlightKind(value);
    },
};

/** Encode one HighlightKind. */
export function encodeHighlightKind(writer: BinaryWriter, value: HighlightKind): void {
    switch (value) {
        case "text":
            writer.writeUnsigned(0);
            return;
        case "read":
            writer.writeUnsigned(1);
            return;
        case "write":
            writer.writeUnsigned(2);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one HighlightKind. */
export function decodeHighlightKind(reader: BinaryReader): HighlightKind {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "text";
        case 1:
            return "read";
        case 2:
            return "write";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one HighlightKind. */
export function toJsonHighlightKind(value: HighlightKind): Json {
    return value;
}

/** Return one HighlightKind from one JSON value. */
export function fromJsonHighlightKind(value: Json): HighlightKind {
    const variant = jsonString(value);

    switch (variant) {
        case "text":
            return "text";
        case "read":
            return "read";
        case "write":
            return "write";
    }

    throw new SerdeError(`unknown enum variant: ${variant}`);
}
