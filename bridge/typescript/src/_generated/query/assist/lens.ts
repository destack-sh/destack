/* generated by destack_bridge_generate, do not edit */

import { BinaryReader, BinaryWriter, Json, SerdeError, jsonArray, jsonField, jsonInteger, jsonObject, jsonString } from "../../../protocol/serde.js";
import type { QueryModule } from "../core/target.js";
import type { Span } from "../../source/file/model/span.js";
import { decodeQueryModule, encodeQueryModule, fromJsonQueryModule, toJsonQueryModule } from "../core/target.js";
import { decodeSpan, encodeSpan, fromJsonSpan, toJsonSpan } from "../../source/file/model/span.js";

/** Request code lenses for a document. */
export type CodeLensesRequest = {
    /** The queried module. */
    readonly module: QueryModule;
};

export const CodeLensesRequest = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: CodeLensesRequest): void {
        encodeCodeLensesRequest(writer, value);
    },

    /** Decode one CodeLensesRequest. */
    decode(reader: BinaryReader): CodeLensesRequest {
        return decodeCodeLensesRequest(reader);
    },

    /** Return this value as JSON. */
    toJson(value: CodeLensesRequest): Json {
        return toJsonCodeLensesRequest(value);
    },

    /** Return one CodeLensesRequest from one JSON value. */
    fromJson(value: Json): CodeLensesRequest {
        return fromJsonCodeLensesRequest(value);
    },
};

/** Encode one CodeLensesRequest. */
export function encodeCodeLensesRequest(writer: BinaryWriter, value: CodeLensesRequest): void {
    encodeQueryModule(writer, value.module);
}

/** Decode one CodeLensesRequest. */
export function decodeCodeLensesRequest(reader: BinaryReader): CodeLensesRequest {
    const module_ = decodeQueryModule(reader);

    return {
        module: module_,
    };
}

/** Return one JSON value for one CodeLensesRequest. */
export function toJsonCodeLensesRequest(value: CodeLensesRequest): Json {
    return {
        module: toJsonQueryModule(value.module),
    };
}

/** Return one CodeLensesRequest from one JSON value. */
export function fromJsonCodeLensesRequest(value: Json): CodeLensesRequest {
    const object = jsonObject(value);

    return {
        module: fromJsonQueryModule(jsonField(object, "module")),
    };
}

/** Request to resolve a code lens. */
export type ResolveCodeLensRequest = {
    /** The code lens to resolve. */
    readonly lens: CodeLens;
};

export const ResolveCodeLensRequest = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ResolveCodeLensRequest): void {
        encodeResolveCodeLensRequest(writer, value);
    },

    /** Decode one ResolveCodeLensRequest. */
    decode(reader: BinaryReader): ResolveCodeLensRequest {
        return decodeResolveCodeLensRequest(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ResolveCodeLensRequest): Json {
        return toJsonResolveCodeLensRequest(value);
    },

    /** Return one ResolveCodeLensRequest from one JSON value. */
    fromJson(value: Json): ResolveCodeLensRequest {
        return fromJsonResolveCodeLensRequest(value);
    },
};

/** Encode one ResolveCodeLensRequest. */
export function encodeResolveCodeLensRequest(writer: BinaryWriter, value: ResolveCodeLensRequest): void {
    encodeCodeLens(writer, value.lens);
}

/** Decode one ResolveCodeLensRequest. */
export function decodeResolveCodeLensRequest(reader: BinaryReader): ResolveCodeLensRequest {
    const lens = decodeCodeLens(reader);

    return {
        lens,
    };
}

/** Return one JSON value for one ResolveCodeLensRequest. */
export function toJsonResolveCodeLensRequest(value: ResolveCodeLensRequest): Json {
    return {
        lens: toJsonCodeLens(value.lens),
    };
}

/** Return one ResolveCodeLensRequest from one JSON value. */
export function fromJsonResolveCodeLensRequest(value: Json): ResolveCodeLensRequest {
    const object = jsonObject(value);

    return {
        lens: fromJsonCodeLens(jsonField(object, "lens")),
    };
}

/** A code lens (inline annotation with optional command). */
export type CodeLens = {
    /** The range this lens applies to. */
    readonly range: Span;
    /** The lens action. */
    readonly action: CodeLensAction;
};

export const CodeLens = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: CodeLens): void {
        encodeCodeLens(writer, value);
    },

    /** Decode one CodeLens. */
    decode(reader: BinaryReader): CodeLens {
        return decodeCodeLens(reader);
    },

    /** Return this value as JSON. */
    toJson(value: CodeLens): Json {
        return toJsonCodeLens(value);
    },

    /** Return one CodeLens from one JSON value. */
    fromJson(value: Json): CodeLens {
        return fromJsonCodeLens(value);
    },
};

/** Encode one CodeLens. */
export function encodeCodeLens(writer: BinaryWriter, value: CodeLens): void {
    encodeSpan(writer, value.range);
    encodeCodeLensAction(writer, value.action);
}

/** Decode one CodeLens. */
export function decodeCodeLens(reader: BinaryReader): CodeLens {
    const range = decodeSpan(reader);
    const action = decodeCodeLensAction(reader);

    return {
        range,
        action,
    };
}

/** Return one JSON value for one CodeLens. */
export function toJsonCodeLens(value: CodeLens): Json {
    return {
        range: toJsonSpan(value.range),
        action: toJsonCodeLensAction(value.action),
    };
}

/** Return one CodeLens from one JSON value. */
export function fromJsonCodeLens(value: Json): CodeLens {
    const object = jsonObject(value);

    return {
        range: fromJsonSpan(jsonField(object, "range")),
        action: fromJsonCodeLensAction(jsonField(object, "action")),
    };
}

/** The action for a code lens. */
export type CodeLensAction =
    /** Show reference count. */
    | {
          readonly kind: "references";
          /** Number of references (excluding declaration). */
          readonly count: number;
      }
    /** Show implementation count. */
    | {
          readonly kind: "implementations";
          /** Number of implementations. */
          readonly count: number;
      }
    /** Run test action. */
    | {
          readonly kind: "runTest";
          /** The test name. */
          readonly testName: string;
      }
    /** Debug test action. */
    | {
          readonly kind: "debugTest";
          /** The test name. */
          readonly testName: string;
      }
    /** Custom lens with title and command. */
    | {
          readonly kind: "custom";
          /** The title to display. */
          readonly title: string;
          /** The command identifier. */
          readonly command: string;
          /** Command arguments (JSON-serializable). */
          readonly arguments: ReadonlyArray<string>;
      }
;

export const CodeLensAction = {
    /** Show reference count. */
    references(count: number): CodeLensAction {
        return { kind: "references", count };
    },

    /** Show implementation count. */
    implementations(count: number): CodeLensAction {
        return { kind: "implementations", count };
    },

    /** Run test action. */
    runTest(testName: string): CodeLensAction {
        return { kind: "runTest", testName };
    },

    /** Debug test action. */
    debugTest(testName: string): CodeLensAction {
        return { kind: "debugTest", testName };
    },

    /** Custom lens with title and command. */
    custom(title: string, command: string, arguments_: ReadonlyArray<string>): CodeLensAction {
        return { kind: "custom", title, command, arguments: arguments_ };
    },

    /** Encode this value. */
    encode(writer: BinaryWriter, value: CodeLensAction): void {
        encodeCodeLensAction(writer, value);
    },

    /** Decode one CodeLensAction. */
    decode(reader: BinaryReader): CodeLensAction {
        return decodeCodeLensAction(reader);
    },

    /** Return this value as JSON. */
    toJson(value: CodeLensAction): Json {
        return toJsonCodeLensAction(value);
    },

    /** Return one CodeLensAction from one JSON value. */
    fromJson(value: Json): CodeLensAction {
        return fromJsonCodeLensAction(value);
    },
};

/** Encode one CodeLensAction. */
export function encodeCodeLensAction(writer: BinaryWriter, value: CodeLensAction): void {
    switch (value.kind) {
        case "references":
            writer.writeUnsigned(0);
            writer.writeUnsigned(value.count);
            return;
        case "implementations":
            writer.writeUnsigned(1);
            writer.writeUnsigned(value.count);
            return;
        case "runTest":
            writer.writeUnsigned(2);
            writer.writeString(value.testName);
            return;
        case "debugTest":
            writer.writeUnsigned(3);
            writer.writeString(value.testName);
            return;
        case "custom":
            writer.writeUnsigned(4);
            writer.writeString(value.title);
            writer.writeString(value.command);
            writer.writeUnsigned(value.arguments.length);
            for (const item2 of value.arguments) {
                writer.writeString(item2);
            }
            return;
    }

    throw new SerdeError("unknown enum variant");
}

/** Decode one CodeLensAction. */
export function decodeCodeLensAction(reader: BinaryReader): CodeLensAction {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const count = reader.readNumber();

            return {
                kind: "references",
                count,
            };
        }
        case 1: {
            const count = reader.readNumber();

            return {
                kind: "implementations",
                count,
            };
        }
        case 2: {
            const testName = reader.readString();

            return {
                kind: "runTest",
                testName,
            };
        }
        case 3: {
            const testName = reader.readString();

            return {
                kind: "debugTest",
                testName,
            };
        }
        case 4: {
            const title = reader.readString();
            const command = reader.readString();
            const arguments_ = (() => { const length2 = reader.readNumber(); const items2: Array<string> = []; for (let index = 0; index < length2; index += 1) { items2.push(reader.readString()); } return items2; })();

            return {
                kind: "custom",
                title,
                command,
                arguments: arguments_,
            };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Return one JSON value for one CodeLensAction. */
export function toJsonCodeLensAction(value: CodeLensAction): Json {
    switch (value.kind) {
        case "references":
            return {
                kind: "references",
                count: value.count,
            };
        case "implementations":
            return {
                kind: "implementations",
                count: value.count,
            };
        case "runTest":
            return {
                kind: "runTest",
                testName: value.testName,
            };
        case "debugTest":
            return {
                kind: "debugTest",
                testName: value.testName,
            };
        case "custom":
            return {
                kind: "custom",
                title: value.title,
                command: value.command,
                arguments: value.arguments.map((item0) => item0),
            };
    }

    throw new SerdeError("unknown enum variant");
}

/** Return one CodeLensAction from one JSON value. */
export function fromJsonCodeLensAction(value: Json): CodeLensAction {
    const object = jsonObject(value);
    const kind = jsonString(jsonField(object, "kind"));

    switch (kind) {
        case "references":
            return {
                kind,
                count: jsonInteger(jsonField(object, "count")),
            };
        case "implementations":
            return {
                kind,
                count: jsonInteger(jsonField(object, "count")),
            };
        case "runTest":
            return {
                kind,
                testName: jsonString(jsonField(object, "testName")),
            };
        case "debugTest":
            return {
                kind,
                testName: jsonString(jsonField(object, "testName")),
            };
        case "custom":
            return {
                kind,
                title: jsonString(jsonField(object, "title")),
                command: jsonString(jsonField(object, "command")),
                arguments: jsonArray(jsonField(object, "arguments")).map((item0) => jsonString(item0)),
            };
    }

    throw new SerdeError(`unknown enum variant: ${kind}`);
}

/** Response payload for code lenses queries. */
export type CodeLensesResponse = {
    /** Code lenses. */
    readonly lenses: ReadonlyArray<CodeLens>;
};

export const CodeLensesResponse = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: CodeLensesResponse): void {
        encodeCodeLensesResponse(writer, value);
    },

    /** Decode one CodeLensesResponse. */
    decode(reader: BinaryReader): CodeLensesResponse {
        return decodeCodeLensesResponse(reader);
    },

    /** Return this value as JSON. */
    toJson(value: CodeLensesResponse): Json {
        return toJsonCodeLensesResponse(value);
    },

    /** Return one CodeLensesResponse from one JSON value. */
    fromJson(value: Json): CodeLensesResponse {
        return fromJsonCodeLensesResponse(value);
    },
};

/** Encode one CodeLensesResponse. */
export function encodeCodeLensesResponse(writer: BinaryWriter, value: CodeLensesResponse): void {
    writer.writeUnsigned(value.lenses.length);
    for (const item0 of value.lenses) {
        encodeCodeLens(writer, item0);
    }
}

/** Decode one CodeLensesResponse. */
export function decodeCodeLensesResponse(reader: BinaryReader): CodeLensesResponse {
    const lenses = (() => { const length0 = reader.readNumber(); const items0: Array<CodeLens> = []; for (let index = 0; index < length0; index += 1) { items0.push(decodeCodeLens(reader)); } return items0; })();

    return {
        lenses,
    };
}

/** Return one JSON value for one CodeLensesResponse. */
export function toJsonCodeLensesResponse(value: CodeLensesResponse): Json {
    return {
        lenses: value.lenses.map((item0) => toJsonCodeLens(item0)),
    };
}

/** Return one CodeLensesResponse from one JSON value. */
export function fromJsonCodeLensesResponse(value: Json): CodeLensesResponse {
    const object = jsonObject(value);

    return {
        lenses: jsonArray(jsonField(object, "lenses")).map((item0) => fromJsonCodeLens(item0)),
    };
}

/** Response payload for code lens resolve queries. */
export type ResolveCodeLensResponse = {
    /** The resolved code lens. */
    readonly lens: CodeLens;
};

export const ResolveCodeLensResponse = {
    /** Encode this value. */
    encode(writer: BinaryWriter, value: ResolveCodeLensResponse): void {
        encodeResolveCodeLensResponse(writer, value);
    },

    /** Decode one ResolveCodeLensResponse. */
    decode(reader: BinaryReader): ResolveCodeLensResponse {
        return decodeResolveCodeLensResponse(reader);
    },

    /** Return this value as JSON. */
    toJson(value: ResolveCodeLensResponse): Json {
        return toJsonResolveCodeLensResponse(value);
    },

    /** Return one ResolveCodeLensResponse from one JSON value. */
    fromJson(value: Json): ResolveCodeLensResponse {
        return fromJsonResolveCodeLensResponse(value);
    },
};

/** Encode one ResolveCodeLensResponse. */
export function encodeResolveCodeLensResponse(writer: BinaryWriter, value: ResolveCodeLensResponse): void {
    encodeCodeLens(writer, value.lens);
}

/** Decode one ResolveCodeLensResponse. */
export function decodeResolveCodeLensResponse(reader: BinaryReader): ResolveCodeLensResponse {
    const lens = decodeCodeLens(reader);

    return {
        lens,
    };
}

/** Return one JSON value for one ResolveCodeLensResponse. */
export function toJsonResolveCodeLensResponse(value: ResolveCodeLensResponse): Json {
    return {
        lens: toJsonCodeLens(value.lens),
    };
}

/** Return one ResolveCodeLensResponse from one JSON value. */
export function fromJsonResolveCodeLensResponse(value: Json): ResolveCodeLensResponse {
    const object = jsonObject(value);

    return {
        lens: fromJsonCodeLens(jsonField(object, "lens")),
    };
}
