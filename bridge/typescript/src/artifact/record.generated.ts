/* generated by destack_bridge_generate, do not edit */

import type { ArtifactDependency } from "./dependency.generated.js";
import type { ArtifactSidecar } from "./sidecar.generated.js";
import type { ArtifactVersion } from "./version.generated.js";
import type { Diagnostic } from "../diagnostic/diagnostic.generated.js";

/** One interned string carried by an artifact record. */
export type ArtifactString = {
    /** Canonical lowercase hex string id. */
    readonly id: string;
    /** Interned string text. */
    readonly text: string;
};

/** Self-contained raw artifact body crossing bridge boundaries. */
export type ArtifactRecord = {
    /** The exact artifact version. */
    readonly version: ArtifactVersion;
    /** Serialized artifact image bytes. */
    readonly image: Uint8Array | readonly number[];
    /** String pool needed to interpret interned ids in the payload. */
    readonly strings: readonly ArtifactString[];
    /** Exact artifact dependencies. */
    readonly dependencies: readonly ArtifactDependency[];
    /** Diagnostics recorded for this artifact version. */
    readonly diagnostics: readonly Diagnostic[];
    /** Artifact sidecars recorded for this artifact version. */
    readonly sidecars: readonly ArtifactSidecar[];
};

