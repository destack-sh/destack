/* generated by destack_bridge_generate, do not edit */

import type { FileContent } from "../source/file.generated.js";

/** One stable sidecar label crossing bridge boundaries. */
export type ArtifactSidecarLabel = {
    /** Label key. */
    readonly key: string;
    /** Label value. */
    readonly value: string;
};

/** One named artifact sidecar crossing bridge boundaries. */
export type ArtifactSidecar = {
    /** Sidecar name. */
    readonly name: string;
    /** Stable labels describing this sidecar. */
    readonly labels: readonly ArtifactSidecarLabel[];
    /** Sidecar content. */
    readonly content: FileContent;
};

