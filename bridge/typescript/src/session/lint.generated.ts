/* generated by destack_bridge_generate, do not edit */

import type { Diagnostic } from "../diagnostic/diagnostic.generated.js";
import type { Module } from "./module.generated.js";
import type { PackageId } from "../source/package.generated.js";
import type { ProfileId } from "../source/profile.generated.js";

/** Scope accepted by lint operations. */
export type Scope =
    /** One module profile. */
    | {
          readonly kind: "module";
          /** Loaded source module. */
          readonly module: Module;
          /** Semantic profile. */
          readonly profile: ProfileId;
      }
    /** One source package. */
    | {
          readonly kind: "package";
          /** Source package. */
          readonly package: PackageId;
      }
    /** Whole workspace. */
    | {
          readonly kind: "workspace";
      }
;

export const Scope = {
    /** One module profile. */
    module(module: Module, profile: ProfileId): Scope {
        return { kind: "module", module, profile };
    },

    /** One source package. */
    package(packageValue: PackageId): Scope {
        return { kind: "package", package: packageValue };
    },

    /** Whole workspace. */
    workspace(): Scope {
        return { kind: "workspace" };
    },

};

/** One linter request. */
export type LintRequest = {
    /** Scope to lint. */
    readonly scope: Scope;
};

/** One linter output. */
export type LintOutput = {
    /** Diagnostics emitted by lint rules. */
    readonly diagnostics: readonly Diagnostic[];
};
