/* generated by destack_bridge_generate, do not edit */

import type { Module } from "./module.generated.js";

/** One document accepted by formatter operations. */
export type Document =
    /** Repository module at one immutable revision. */
    | {
          readonly kind: "module";
          /** Loaded source module. */
          readonly module: Module;
      }
    /** Ad hoc source text. */
    | {
          readonly kind: "text";
          /** Display path used for parser language detection. */
          readonly path: string;
          /** Source text. */
          readonly text: string;
      }
;

export const Document = {
    /** Repository module at one immutable revision. */
    module(module: Module): Document {
        return { kind: "module", module };
    },

    /** Ad hoc source text. */
    text(path: string, text: string): Document {
        return { kind: "text", path, text };
    },

};

/** One formatter request. */
export type FormatRequest = {
    /** Document to format. */
    readonly document: Document;
};

/** One formatter output. */
export type FormatOutput = {
    /** Formatted source text. */
    readonly text: string;
};
