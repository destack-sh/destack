/* generated by destack_bridge_generate, do not edit */

import type { ArtifactVersion } from "../artifact/version.js";
import type { ModuleId } from "../source/module.js";
import type { ProfileId } from "../source/profile.js";

/** Typed projection of one resolved DIR artifact. */
export type DirResolved = {
    /** Exact resolved artifact version. */
    readonly version: ArtifactVersion;
    /** Resolved module id. */
    readonly module: ModuleId;
    /** Resolved semantic profile. */
    readonly profile: ProfileId;
};
