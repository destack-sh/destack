/* generated by destack_bridge_generate, do not edit */

import { Reader, SerdeError, Writer, compareBytes, nestedBytes } from "../../../../protocol/serde.js";
import type { ModuleId } from "../../source/file/model/module.js";
import { decodeModuleId, encodeModuleId } from "../../source/file/model/module.js";

/** Global symbol id across modules. */
export type GlobalSymbolId = {
    /** The module id of the global symbol. */
    readonly moduleId: ModuleId;
    /** The local id of the global symbol. */
    readonly localId: LocalSymbolId;
};

export function encodeGlobalSymbolId(writer: Writer, value: GlobalSymbolId): void {
    encodeModuleId(writer, value["moduleId"]);
    encodeLocalSymbolId(writer, value["localId"]);
}

export function decodeGlobalSymbolId(reader: Reader): GlobalSymbolId {
    const field0 = decodeModuleId(reader);
    const field1 = decodeLocalSymbolId(reader);

    return {
        "moduleId": field0,
        "localId": field1,
    };
}

/** Unique identifier for Symbols. */
export type LocalSymbolId = {
    /** The numeric id. */
    readonly id: number;
};

export function encodeLocalSymbolId(writer: Writer, value: LocalSymbolId): void {
    writer.writeUnsigned(value["id"]);
}

export function decodeLocalSymbolId(reader: Reader): LocalSymbolId {
    const field0 = reader.readNumber();

    return {
        "id": field0,
    };
}
