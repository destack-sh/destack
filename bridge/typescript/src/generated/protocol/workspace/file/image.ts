/* generated by destack_bridge_generate, do not edit */

import { Reader, SerdeError, Writer, compareBytes, nestedBytes } from "../../../../protocol/serde.js";
import type { Diagnostic } from "../../source/diagnostic/diagnostic.js";
import type { FileId } from "../../source/file/model/file.js";
import type { FileType } from "../../source/file/model/type.js";
import type { ModuleId } from "../../source/file/model/module.js";
import type { TextChange } from "../../source/edit/text.js";
import type { Uri } from "../../source/file/path/uri.js";
import { decodeDiagnostic, encodeDiagnostic } from "../../source/diagnostic/diagnostic.js";
import { decodeFileId, encodeFileId } from "../../source/file/model/file.js";
import { decodeFileType, encodeFileType } from "../../source/file/model/type.js";
import { decodeModuleId, encodeModuleId } from "../../source/file/model/module.js";
import { decodeTextChange, encodeTextChange } from "../../source/edit/text.js";
import { decodeUri, encodeUri } from "../../source/file/path/uri.js";

/** File operation applied through a workspace. */
export type FileOperation =
    /** Open editor text content. */
    | {
          readonly kind: "openText";
          /** Path being opened. */
          readonly path: string;
          /** Editor document URI. */
          readonly uri: Uri;
          /** Editor document version. */
          readonly version: number;
          /** Current text content. */
          readonly content: string;
      }
    /** Open editor binary content. */
    | {
          readonly kind: "openBytes";
          /** Path being opened. */
          readonly path: string;
          /** Editor document URI. */
          readonly uri: Uri;
          /** Editor document version. */
          readonly version: number;
          /** Current binary content. */
          readonly content: Uint8Array | readonly number[];
      }
    /** Change editor text content. */
    | {
          readonly kind: "changeText";
          /** Path being changed. */
          readonly path: string;
          /** Editor document URI. */
          readonly uri: Uri;
          /** Editor document version. */
          readonly version: number;
          /** Current text content. */
          readonly content: string;
      }
    /** Change editor binary content. */
    | {
          readonly kind: "changeBytes";
          /** Path being changed. */
          readonly path: string;
          /** Editor document URI. */
          readonly uri: Uri;
          /** Editor document version. */
          readonly version: number;
          /** Current binary content. */
          readonly content: Uint8Array | readonly number[];
      }
    /** Patch editor text content. */
    | {
          readonly kind: "patchText";
          /** Path being patched. */
          readonly path: string;
          /** Editor document URI. */
          readonly uri: Uri;
          /** Editor document version. */
          readonly version: number;
          /** Incremental text changes. */
          readonly changes: ReadonlyArray<TextChange>;
      }
    /** Save editor text content. */
    | {
          readonly kind: "saveText";
          /** Path being saved. */
          readonly path: string;
          /** Current text content. */
          readonly content?: string;
      }
    /** Save editor binary content. */
    | {
          readonly kind: "saveBytes";
          /** Path being saved. */
          readonly path: string;
          /** Current binary content. */
          readonly content?: Uint8Array | readonly number[];
      }
    /** Close editor overlay state and restore filesystem truth. */
    | {
          readonly kind: "close";
          /** Path being closed. */
          readonly path: string;
      }
    /** Write text content to disk and workspace state. */
    | {
          readonly kind: "writeText";
          /** Path being written. */
          readonly path: string;
          /** Current text content. */
          readonly content: string;
      }
    /** Write binary content to disk and workspace state. */
    | {
          readonly kind: "writeBytes";
          /** Path being written. */
          readonly path: string;
          /** Current binary content. */
          readonly content: Uint8Array | readonly number[];
      }
    /** Remove a file from disk and workspace state. */
    | {
          readonly kind: "remove";
          /** Path being removed. */
          readonly path: string;
      }
    /** Move a file on disk and workspace state. */
    | {
          readonly kind: "move";
          /** Source path. */
          readonly from: string;
          /** Destination path. */
          readonly to: string;
      }
;

export const FileOperation = {
    /** Open editor text content. */
    openText(path: string, uri: Uri, version: number, content: string): FileOperation {
        return { kind: "openText", path, uri, version, content };
    },

    /** Open editor binary content. */
    openBytes(path: string, uri: Uri, version: number, content: Uint8Array | readonly number[]): FileOperation {
        return { kind: "openBytes", path, uri, version, content };
    },

    /** Change editor text content. */
    changeText(path: string, uri: Uri, version: number, content: string): FileOperation {
        return { kind: "changeText", path, uri, version, content };
    },

    /** Change editor binary content. */
    changeBytes(path: string, uri: Uri, version: number, content: Uint8Array | readonly number[]): FileOperation {
        return { kind: "changeBytes", path, uri, version, content };
    },

    /** Patch editor text content. */
    patchText(path: string, uri: Uri, version: number, changes: ReadonlyArray<TextChange>): FileOperation {
        return { kind: "patchText", path, uri, version, changes };
    },

    /** Save editor text content. */
    saveText(path: string, content: string | undefined): FileOperation {
        return { kind: "saveText", path, content };
    },

    /** Save editor binary content. */
    saveBytes(path: string, content: Uint8Array | readonly number[] | undefined): FileOperation {
        return { kind: "saveBytes", path, content };
    },

    /** Close editor overlay state and restore filesystem truth. */
    close(path: string): FileOperation {
        return { kind: "close", path };
    },

    /** Write text content to disk and workspace state. */
    writeText(path: string, content: string): FileOperation {
        return { kind: "writeText", path, content };
    },

    /** Write binary content to disk and workspace state. */
    writeBytes(path: string, content: Uint8Array | readonly number[]): FileOperation {
        return { kind: "writeBytes", path, content };
    },

    /** Remove a file from disk and workspace state. */
    remove(path: string): FileOperation {
        return { kind: "remove", path };
    },

    /** Move a file on disk and workspace state. */
    move(from: string, to: string): FileOperation {
        return { kind: "move", from, to };
    },

};

export function encodeFileOperation(writer: Writer, value: FileOperation): void {
    switch (value.kind) {
        case "openText":
            writer.writeUnsigned(0);
            writer.writeString(value["path"]);
            encodeUri(writer, value["uri"]);
            writer.writeSigned(value["version"]);
            writer.writeString(value["content"]);
            return;
        case "openBytes":
            writer.writeUnsigned(1);
            writer.writeString(value["path"]);
            encodeUri(writer, value["uri"]);
            writer.writeSigned(value["version"]);
            writer.writeByteSlice(value["content"]);
            return;
        case "changeText":
            writer.writeUnsigned(2);
            writer.writeString(value["path"]);
            encodeUri(writer, value["uri"]);
            writer.writeSigned(value["version"]);
            writer.writeString(value["content"]);
            return;
        case "changeBytes":
            writer.writeUnsigned(3);
            writer.writeString(value["path"]);
            encodeUri(writer, value["uri"]);
            writer.writeSigned(value["version"]);
            writer.writeByteSlice(value["content"]);
            return;
        case "patchText":
            writer.writeUnsigned(4);
            writer.writeString(value["path"]);
            encodeUri(writer, value["uri"]);
            writer.writeSigned(value["version"]);
            writer.writeUnsigned(value["changes"].length);
            for (const item0 of value["changes"]) {
                encodeTextChange(writer, item0);
            }
            return;
        case "saveText":
            writer.writeUnsigned(5);
            writer.writeString(value["path"]);
            writer.writeOption(value["content"], (value0) => {
                writer.writeString(value0);
            });
            return;
        case "saveBytes":
            writer.writeUnsigned(6);
            writer.writeString(value["path"]);
            writer.writeOption(value["content"], (value0) => {
                writer.writeByteSlice(value0);
            });
            return;
        case "close":
            writer.writeUnsigned(7);
            writer.writeString(value["path"]);
            return;
        case "writeText":
            writer.writeUnsigned(8);
            writer.writeString(value["path"]);
            writer.writeString(value["content"]);
            return;
        case "writeBytes":
            writer.writeUnsigned(9);
            writer.writeString(value["path"]);
            writer.writeByteSlice(value["content"]);
            return;
        case "remove":
            writer.writeUnsigned(10);
            writer.writeString(value["path"]);
            return;
        case "move":
            writer.writeUnsigned(11);
            writer.writeString(value["from"]);
            writer.writeString(value["to"]);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

export function decodeFileOperation(reader: Reader): FileOperation {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const field0 = reader.readString();
            const field1 = decodeUri(reader);
            const field2 = reader.readSignedNumber();
            const field3 = reader.readString();

            return {
                kind: "openText",
                "path": field0,
                "uri": field1,
                "version": field2,
                "content": field3,
            };
        }
        case 1: {
            const field0 = reader.readString();
            const field1 = decodeUri(reader);
            const field2 = reader.readSignedNumber();
            const field3 = reader.readByteSlice();

            return {
                kind: "openBytes",
                "path": field0,
                "uri": field1,
                "version": field2,
                "content": field3,
            };
        }
        case 2: {
            const field0 = reader.readString();
            const field1 = decodeUri(reader);
            const field2 = reader.readSignedNumber();
            const field3 = reader.readString();

            return {
                kind: "changeText",
                "path": field0,
                "uri": field1,
                "version": field2,
                "content": field3,
            };
        }
        case 3: {
            const field0 = reader.readString();
            const field1 = decodeUri(reader);
            const field2 = reader.readSignedNumber();
            const field3 = reader.readByteSlice();

            return {
                kind: "changeBytes",
                "path": field0,
                "uri": field1,
                "version": field2,
                "content": field3,
            };
        }
        case 4: {
            const field0 = reader.readString();
            const field1 = decodeUri(reader);
            const field2 = reader.readSignedNumber();
            const field3 = (() => { const length3 = reader.readNumber(); const items3: Array<TextChange> = []; for (let index = 0; index < length3; index += 1) { items3.push(decodeTextChange(reader)); } return items3; })();

            return {
                kind: "patchText",
                "path": field0,
                "uri": field1,
                "version": field2,
                "changes": field3,
            };
        }
        case 5: {
            const field0 = reader.readString();
            const field1 = reader.readOption(() => reader.readString());

            return {
                kind: "saveText",
                "path": field0,
                ...(field1 === undefined ? {} : { "content": field1 }),
            };
        }
        case 6: {
            const field0 = reader.readString();
            const field1 = reader.readOption(() => reader.readByteSlice());

            return {
                kind: "saveBytes",
                "path": field0,
                ...(field1 === undefined ? {} : { "content": field1 }),
            };
        }
        case 7: {
            const field0 = reader.readString();

            return {
                kind: "close",
                "path": field0,
            };
        }
        case 8: {
            const field0 = reader.readString();
            const field1 = reader.readString();

            return {
                kind: "writeText",
                "path": field0,
                "content": field1,
            };
        }
        case 9: {
            const field0 = reader.readString();
            const field1 = reader.readByteSlice();

            return {
                kind: "writeBytes",
                "path": field0,
                "content": field1,
            };
        }
        case 10: {
            const field0 = reader.readString();

            return {
                kind: "remove",
                "path": field0,
            };
        }
        case 11: {
            const field0 = reader.readString();
            const field1 = reader.readString();

            return {
                kind: "move",
                "from": field0,
                "to": field1,
            };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** File update emitted by the workspace. */
export type FileUpdate = {
    /** Updated module id when known. */
    readonly moduleId?: ModuleId;
    /** Updated file id. */
    readonly fileId: FileId;
    /** Diagnostic uri for this update. */
    readonly diagnosticUri: Uri;
    /** Protocol file version for diagnostics when the file is open. */
    readonly diagnosticVersion?: number;
    /** Updated file image when the file still exists. */
    readonly file?: FileImage;
    /** Whether this update removed the file. */
    readonly isRemoved: boolean;
    /** The coarse change kind for this file. */
    readonly kind: UpdateKind;
    /** Diagnostics for this file. */
    readonly diagnostics: ReadonlyArray<Diagnostic>;
};

export function encodeFileUpdate(writer: Writer, value: FileUpdate): void {
    writer.writeOption(value["moduleId"], (value0) => {
        encodeModuleId(writer, value0);
    });
    encodeFileId(writer, value["fileId"]);
    encodeUri(writer, value["diagnosticUri"]);
    writer.writeOption(value["diagnosticVersion"], (value0) => {
        writer.writeSigned(value0);
    });
    writer.writeOption(value["file"], (value0) => {
        encodeFileImage(writer, value0);
    });
    writer.writeBool(value["isRemoved"]);
    encodeUpdateKind(writer, value["kind"]);
    writer.writeUnsigned(value["diagnostics"].length);
    for (const item0 of value["diagnostics"]) {
        encodeDiagnostic(writer, item0);
    }
}

export function decodeFileUpdate(reader: Reader): FileUpdate {
    const field0 = reader.readOption(() => decodeModuleId(reader));
    const field1 = decodeFileId(reader);
    const field2 = decodeUri(reader);
    const field3 = reader.readOption(() => reader.readSignedNumber());
    const field4 = reader.readOption(() => decodeFileImage(reader));
    const field5 = reader.readBool();
    const field6 = decodeUpdateKind(reader);
    const field7 = (() => { const length7 = reader.readNumber(); const items7: Array<Diagnostic> = []; for (let index = 0; index < length7; index += 1) { items7.push(decodeDiagnostic(reader)); } return items7; })();

    return {
        ...(field0 === undefined ? {} : { "moduleId": field0 }),
        "fileId": field1,
        "diagnosticUri": field2,
        ...(field3 === undefined ? {} : { "diagnosticVersion": field3 }),
        ...(field4 === undefined ? {} : { "file": field4 }),
        "isRemoved": field5,
        "kind": field6,
        "diagnostics": field7,
    };
}

/** In-memory image for one updated file. */
export type FileImage = {
    /** File id in the registry. */
    readonly id: FileId;
    /** File name. */
    readonly name: string;
    /** File uri. */
    readonly uri: Uri;
    /** Optional file path. */
    readonly path?: string;
    /** File type. */
    readonly fileType: FileType;
    /** Optional text content. */
    readonly content?: string;
};

export function encodeFileImage(writer: Writer, value: FileImage): void {
    encodeFileId(writer, value["id"]);
    writer.writeString(value["name"]);
    encodeUri(writer, value["uri"]);
    writer.writeOption(value["path"], (value0) => {
        writer.writeString(value0);
    });
    encodeFileType(writer, value["fileType"]);
    writer.writeOption(value["content"], (value0) => {
        writer.writeString(value0);
    });
}

export function decodeFileImage(reader: Reader): FileImage {
    const field0 = decodeFileId(reader);
    const field1 = reader.readString();
    const field2 = decodeUri(reader);
    const field3 = reader.readOption(() => reader.readString());
    const field4 = decodeFileType(reader);
    const field5 = reader.readOption(() => reader.readString());

    return {
        "id": field0,
        "name": field1,
        "uri": field2,
        ...(field3 === undefined ? {} : { "path": field3 }),
        "fileType": field4,
        ...(field5 === undefined ? {} : { "content": field5 }),
    };
}

/** One coarse kind for a workspace file update. */
export type UpdateKind = "source" | "config";

export function encodeUpdateKind(writer: Writer, value: UpdateKind): void {
    switch (value) {
        case "source":
            writer.writeUnsigned(0);
            return;
        case "config":
            writer.writeUnsigned(1);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

export function decodeUpdateKind(reader: Reader): UpdateKind {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "source";
        case 1:
            return "config";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}
