/* generated by destack_bridge_generate, do not edit */

import { Reader, SerdeError, Writer, compareBytes, nestedBytes } from "../../../../protocol/serde.js";
import type { BuildPayload } from "./build.js";
import type { CachePayload } from "./cache.js";
import type { CheckPayload } from "./check.js";
import type { CleanPayload } from "./clean.js";
import type { CommandMessagePayload } from "./common.js";
import type { CommandOutputChunk } from "./common.js";
import type { CommandOutputFile } from "./common.js";
import type { Diagnostic } from "../../source/diagnostic/diagnostic.js";
import type { DoctorPayload } from "./doctor.js";
import type { FileImage } from "../file/image.js";
import type { FormatPayload } from "./format.js";
import type { InfoPayload } from "./info.js";
import type { LintPayload } from "./check.js";
import type { Message } from "../message.js";
import type { Revision } from "../../repository/revision.js";
import type { RunPayload } from "./run.js";
import type { SettingsPayload } from "./settings.js";
import type { TargetsPayload } from "./targets.js";
import type { TaskPayload } from "./task.js";
import { decodeBuildPayload, encodeBuildPayload } from "./build.js";
import { decodeCachePayload, encodeCachePayload } from "./cache.js";
import { decodeCheckPayload, encodeCheckPayload } from "./check.js";
import { decodeCleanPayload, encodeCleanPayload } from "./clean.js";
import { decodeCommandMessagePayload, encodeCommandMessagePayload } from "./common.js";
import { decodeCommandOutputChunk, encodeCommandOutputChunk } from "./common.js";
import { decodeCommandOutputFile, encodeCommandOutputFile } from "./common.js";
import { decodeDiagnostic, encodeDiagnostic } from "../../source/diagnostic/diagnostic.js";
import { decodeDoctorPayload, encodeDoctorPayload } from "./doctor.js";
import { decodeFileImage, encodeFileImage } from "../file/image.js";
import { decodeFormatPayload, encodeFormatPayload } from "./format.js";
import { decodeInfoPayload, encodeInfoPayload } from "./info.js";
import { decodeLintPayload, encodeLintPayload } from "./check.js";
import { decodeMessage, encodeMessage } from "../message.js";
import { decodeRevision, encodeRevision } from "../../repository/revision.js";
import { decodeRunPayload, encodeRunPayload } from "./run.js";
import { decodeSettingsPayload, encodeSettingsPayload } from "./settings.js";
import { decodeTargetsPayload, encodeTargetsPayload } from "./targets.js";
import { decodeTaskPayload, encodeTaskPayload } from "./task.js";

/** Output produced by one workspace command. */
export type CheckOutput = {
    /** Revision used for this operation. */
    readonly revision: Revision;
    /** Whether the operation succeeded. */
    readonly success: boolean;
    /** Exit code for the operation. */
    readonly exitCode: number;
    /** Diagnostics produced by the operation. */
    readonly diagnostics: ReadonlyArray<Diagnostic>;
    /** File images needed to render diagnostics. */
    readonly files: ReadonlyArray<FileImage>;
    /** Messages produced by operation execution. */
    readonly messages: ReadonlyArray<Message>;
    /** Stream output collected during execution. */
    readonly output: ReadonlyArray<CommandOutputChunk>;
    /** Generated output files. */
    readonly outputs: ReadonlyArray<CommandOutputFile>;
    /** Operation payload. */
    readonly data: CheckPayload;
    /** Count of modules involved. */
    readonly moduleCount: number;
    /** Count of profiles involved. */
    readonly profileCount: number;
    /** Count of targets involved. */
    readonly targetCount: number;
};

export function encodeCheckOutput(writer: Writer, value: CheckOutput): void {
    encodeRevision(writer, value["revision"]);
    writer.writeBool(value["success"]);
    writer.writeSigned(value["exitCode"]);
    writer.writeUnsigned(value["diagnostics"].length);
    for (const item0 of value["diagnostics"]) {
        encodeDiagnostic(writer, item0);
    }
    writer.writeUnsigned(value["files"].length);
    for (const item0 of value["files"]) {
        encodeFileImage(writer, item0);
    }
    writer.writeUnsigned(value["messages"].length);
    for (const item0 of value["messages"]) {
        encodeMessage(writer, item0);
    }
    writer.writeUnsigned(value["output"].length);
    for (const item0 of value["output"]) {
        encodeCommandOutputChunk(writer, item0);
    }
    writer.writeUnsigned(value["outputs"].length);
    for (const item0 of value["outputs"]) {
        encodeCommandOutputFile(writer, item0);
    }
    encodeCheckPayload(writer, value["data"]);
    writer.writeUnsigned(value["moduleCount"]);
    writer.writeUnsigned(value["profileCount"]);
    writer.writeUnsigned(value["targetCount"]);
}

export function decodeCheckOutput(reader: Reader): CheckOutput {
    const field0 = decodeRevision(reader);
    const field1 = reader.readBool();
    const field2 = reader.readSignedNumber();
    const field3 = (() => { const length3 = reader.readNumber(); const items3: Array<Diagnostic> = []; for (let index = 0; index < length3; index += 1) { items3.push(decodeDiagnostic(reader)); } return items3; })();
    const field4 = (() => { const length4 = reader.readNumber(); const items4: Array<FileImage> = []; for (let index = 0; index < length4; index += 1) { items4.push(decodeFileImage(reader)); } return items4; })();
    const field5 = (() => { const length5 = reader.readNumber(); const items5: Array<Message> = []; for (let index = 0; index < length5; index += 1) { items5.push(decodeMessage(reader)); } return items5; })();
    const field6 = (() => { const length6 = reader.readNumber(); const items6: Array<CommandOutputChunk> = []; for (let index = 0; index < length6; index += 1) { items6.push(decodeCommandOutputChunk(reader)); } return items6; })();
    const field7 = (() => { const length7 = reader.readNumber(); const items7: Array<CommandOutputFile> = []; for (let index = 0; index < length7; index += 1) { items7.push(decodeCommandOutputFile(reader)); } return items7; })();
    const field8 = decodeCheckPayload(reader);
    const field9 = reader.readNumber();
    const field10 = reader.readNumber();
    const field11 = reader.readNumber();

    return {
        "revision": field0,
        "success": field1,
        "exitCode": field2,
        "diagnostics": field3,
        "files": field4,
        "messages": field5,
        "output": field6,
        "outputs": field7,
        "data": field8,
        "moduleCount": field9,
        "profileCount": field10,
        "targetCount": field11,
    };
}

/** Output produced by one workspace command. */
export type LintOutput = {
    /** Revision used for this operation. */
    readonly revision: Revision;
    /** Whether the operation succeeded. */
    readonly success: boolean;
    /** Exit code for the operation. */
    readonly exitCode: number;
    /** Diagnostics produced by the operation. */
    readonly diagnostics: ReadonlyArray<Diagnostic>;
    /** File images needed to render diagnostics. */
    readonly files: ReadonlyArray<FileImage>;
    /** Messages produced by operation execution. */
    readonly messages: ReadonlyArray<Message>;
    /** Stream output collected during execution. */
    readonly output: ReadonlyArray<CommandOutputChunk>;
    /** Generated output files. */
    readonly outputs: ReadonlyArray<CommandOutputFile>;
    /** Operation payload. */
    readonly data: LintPayload;
    /** Count of modules involved. */
    readonly moduleCount: number;
    /** Count of profiles involved. */
    readonly profileCount: number;
    /** Count of targets involved. */
    readonly targetCount: number;
};

export function encodeLintOutput(writer: Writer, value: LintOutput): void {
    encodeRevision(writer, value["revision"]);
    writer.writeBool(value["success"]);
    writer.writeSigned(value["exitCode"]);
    writer.writeUnsigned(value["diagnostics"].length);
    for (const item0 of value["diagnostics"]) {
        encodeDiagnostic(writer, item0);
    }
    writer.writeUnsigned(value["files"].length);
    for (const item0 of value["files"]) {
        encodeFileImage(writer, item0);
    }
    writer.writeUnsigned(value["messages"].length);
    for (const item0 of value["messages"]) {
        encodeMessage(writer, item0);
    }
    writer.writeUnsigned(value["output"].length);
    for (const item0 of value["output"]) {
        encodeCommandOutputChunk(writer, item0);
    }
    writer.writeUnsigned(value["outputs"].length);
    for (const item0 of value["outputs"]) {
        encodeCommandOutputFile(writer, item0);
    }
    encodeLintPayload(writer, value["data"]);
    writer.writeUnsigned(value["moduleCount"]);
    writer.writeUnsigned(value["profileCount"]);
    writer.writeUnsigned(value["targetCount"]);
}

export function decodeLintOutput(reader: Reader): LintOutput {
    const field0 = decodeRevision(reader);
    const field1 = reader.readBool();
    const field2 = reader.readSignedNumber();
    const field3 = (() => { const length3 = reader.readNumber(); const items3: Array<Diagnostic> = []; for (let index = 0; index < length3; index += 1) { items3.push(decodeDiagnostic(reader)); } return items3; })();
    const field4 = (() => { const length4 = reader.readNumber(); const items4: Array<FileImage> = []; for (let index = 0; index < length4; index += 1) { items4.push(decodeFileImage(reader)); } return items4; })();
    const field5 = (() => { const length5 = reader.readNumber(); const items5: Array<Message> = []; for (let index = 0; index < length5; index += 1) { items5.push(decodeMessage(reader)); } return items5; })();
    const field6 = (() => { const length6 = reader.readNumber(); const items6: Array<CommandOutputChunk> = []; for (let index = 0; index < length6; index += 1) { items6.push(decodeCommandOutputChunk(reader)); } return items6; })();
    const field7 = (() => { const length7 = reader.readNumber(); const items7: Array<CommandOutputFile> = []; for (let index = 0; index < length7; index += 1) { items7.push(decodeCommandOutputFile(reader)); } return items7; })();
    const field8 = decodeLintPayload(reader);
    const field9 = reader.readNumber();
    const field10 = reader.readNumber();
    const field11 = reader.readNumber();

    return {
        "revision": field0,
        "success": field1,
        "exitCode": field2,
        "diagnostics": field3,
        "files": field4,
        "messages": field5,
        "output": field6,
        "outputs": field7,
        "data": field8,
        "moduleCount": field9,
        "profileCount": field10,
        "targetCount": field11,
    };
}

/** Output produced by one workspace command. */
export type FormatOutput = {
    /** Revision used for this operation. */
    readonly revision: Revision;
    /** Whether the operation succeeded. */
    readonly success: boolean;
    /** Exit code for the operation. */
    readonly exitCode: number;
    /** Diagnostics produced by the operation. */
    readonly diagnostics: ReadonlyArray<Diagnostic>;
    /** File images needed to render diagnostics. */
    readonly files: ReadonlyArray<FileImage>;
    /** Messages produced by operation execution. */
    readonly messages: ReadonlyArray<Message>;
    /** Stream output collected during execution. */
    readonly output: ReadonlyArray<CommandOutputChunk>;
    /** Generated output files. */
    readonly outputs: ReadonlyArray<CommandOutputFile>;
    /** Operation payload. */
    readonly data: FormatPayload;
    /** Count of modules involved. */
    readonly moduleCount: number;
    /** Count of profiles involved. */
    readonly profileCount: number;
    /** Count of targets involved. */
    readonly targetCount: number;
};

export function encodeFormatOutput(writer: Writer, value: FormatOutput): void {
    encodeRevision(writer, value["revision"]);
    writer.writeBool(value["success"]);
    writer.writeSigned(value["exitCode"]);
    writer.writeUnsigned(value["diagnostics"].length);
    for (const item0 of value["diagnostics"]) {
        encodeDiagnostic(writer, item0);
    }
    writer.writeUnsigned(value["files"].length);
    for (const item0 of value["files"]) {
        encodeFileImage(writer, item0);
    }
    writer.writeUnsigned(value["messages"].length);
    for (const item0 of value["messages"]) {
        encodeMessage(writer, item0);
    }
    writer.writeUnsigned(value["output"].length);
    for (const item0 of value["output"]) {
        encodeCommandOutputChunk(writer, item0);
    }
    writer.writeUnsigned(value["outputs"].length);
    for (const item0 of value["outputs"]) {
        encodeCommandOutputFile(writer, item0);
    }
    encodeFormatPayload(writer, value["data"]);
    writer.writeUnsigned(value["moduleCount"]);
    writer.writeUnsigned(value["profileCount"]);
    writer.writeUnsigned(value["targetCount"]);
}

export function decodeFormatOutput(reader: Reader): FormatOutput {
    const field0 = decodeRevision(reader);
    const field1 = reader.readBool();
    const field2 = reader.readSignedNumber();
    const field3 = (() => { const length3 = reader.readNumber(); const items3: Array<Diagnostic> = []; for (let index = 0; index < length3; index += 1) { items3.push(decodeDiagnostic(reader)); } return items3; })();
    const field4 = (() => { const length4 = reader.readNumber(); const items4: Array<FileImage> = []; for (let index = 0; index < length4; index += 1) { items4.push(decodeFileImage(reader)); } return items4; })();
    const field5 = (() => { const length5 = reader.readNumber(); const items5: Array<Message> = []; for (let index = 0; index < length5; index += 1) { items5.push(decodeMessage(reader)); } return items5; })();
    const field6 = (() => { const length6 = reader.readNumber(); const items6: Array<CommandOutputChunk> = []; for (let index = 0; index < length6; index += 1) { items6.push(decodeCommandOutputChunk(reader)); } return items6; })();
    const field7 = (() => { const length7 = reader.readNumber(); const items7: Array<CommandOutputFile> = []; for (let index = 0; index < length7; index += 1) { items7.push(decodeCommandOutputFile(reader)); } return items7; })();
    const field8 = decodeFormatPayload(reader);
    const field9 = reader.readNumber();
    const field10 = reader.readNumber();
    const field11 = reader.readNumber();

    return {
        "revision": field0,
        "success": field1,
        "exitCode": field2,
        "diagnostics": field3,
        "files": field4,
        "messages": field5,
        "output": field6,
        "outputs": field7,
        "data": field8,
        "moduleCount": field9,
        "profileCount": field10,
        "targetCount": field11,
    };
}

/** Output produced by one workspace command. */
export type BuildOutput = {
    /** Revision used for this operation. */
    readonly revision: Revision;
    /** Whether the operation succeeded. */
    readonly success: boolean;
    /** Exit code for the operation. */
    readonly exitCode: number;
    /** Diagnostics produced by the operation. */
    readonly diagnostics: ReadonlyArray<Diagnostic>;
    /** File images needed to render diagnostics. */
    readonly files: ReadonlyArray<FileImage>;
    /** Messages produced by operation execution. */
    readonly messages: ReadonlyArray<Message>;
    /** Stream output collected during execution. */
    readonly output: ReadonlyArray<CommandOutputChunk>;
    /** Generated output files. */
    readonly outputs: ReadonlyArray<CommandOutputFile>;
    /** Operation payload. */
    readonly data: BuildPayload;
    /** Count of modules involved. */
    readonly moduleCount: number;
    /** Count of profiles involved. */
    readonly profileCount: number;
    /** Count of targets involved. */
    readonly targetCount: number;
};

export function encodeBuildOutput(writer: Writer, value: BuildOutput): void {
    encodeRevision(writer, value["revision"]);
    writer.writeBool(value["success"]);
    writer.writeSigned(value["exitCode"]);
    writer.writeUnsigned(value["diagnostics"].length);
    for (const item0 of value["diagnostics"]) {
        encodeDiagnostic(writer, item0);
    }
    writer.writeUnsigned(value["files"].length);
    for (const item0 of value["files"]) {
        encodeFileImage(writer, item0);
    }
    writer.writeUnsigned(value["messages"].length);
    for (const item0 of value["messages"]) {
        encodeMessage(writer, item0);
    }
    writer.writeUnsigned(value["output"].length);
    for (const item0 of value["output"]) {
        encodeCommandOutputChunk(writer, item0);
    }
    writer.writeUnsigned(value["outputs"].length);
    for (const item0 of value["outputs"]) {
        encodeCommandOutputFile(writer, item0);
    }
    encodeBuildPayload(writer, value["data"]);
    writer.writeUnsigned(value["moduleCount"]);
    writer.writeUnsigned(value["profileCount"]);
    writer.writeUnsigned(value["targetCount"]);
}

export function decodeBuildOutput(reader: Reader): BuildOutput {
    const field0 = decodeRevision(reader);
    const field1 = reader.readBool();
    const field2 = reader.readSignedNumber();
    const field3 = (() => { const length3 = reader.readNumber(); const items3: Array<Diagnostic> = []; for (let index = 0; index < length3; index += 1) { items3.push(decodeDiagnostic(reader)); } return items3; })();
    const field4 = (() => { const length4 = reader.readNumber(); const items4: Array<FileImage> = []; for (let index = 0; index < length4; index += 1) { items4.push(decodeFileImage(reader)); } return items4; })();
    const field5 = (() => { const length5 = reader.readNumber(); const items5: Array<Message> = []; for (let index = 0; index < length5; index += 1) { items5.push(decodeMessage(reader)); } return items5; })();
    const field6 = (() => { const length6 = reader.readNumber(); const items6: Array<CommandOutputChunk> = []; for (let index = 0; index < length6; index += 1) { items6.push(decodeCommandOutputChunk(reader)); } return items6; })();
    const field7 = (() => { const length7 = reader.readNumber(); const items7: Array<CommandOutputFile> = []; for (let index = 0; index < length7; index += 1) { items7.push(decodeCommandOutputFile(reader)); } return items7; })();
    const field8 = decodeBuildPayload(reader);
    const field9 = reader.readNumber();
    const field10 = reader.readNumber();
    const field11 = reader.readNumber();

    return {
        "revision": field0,
        "success": field1,
        "exitCode": field2,
        "diagnostics": field3,
        "files": field4,
        "messages": field5,
        "output": field6,
        "outputs": field7,
        "data": field8,
        "moduleCount": field9,
        "profileCount": field10,
        "targetCount": field11,
    };
}

/** Output produced by one workspace command. */
export type RunOutput = {
    /** Revision used for this operation. */
    readonly revision: Revision;
    /** Whether the operation succeeded. */
    readonly success: boolean;
    /** Exit code for the operation. */
    readonly exitCode: number;
    /** Diagnostics produced by the operation. */
    readonly diagnostics: ReadonlyArray<Diagnostic>;
    /** File images needed to render diagnostics. */
    readonly files: ReadonlyArray<FileImage>;
    /** Messages produced by operation execution. */
    readonly messages: ReadonlyArray<Message>;
    /** Stream output collected during execution. */
    readonly output: ReadonlyArray<CommandOutputChunk>;
    /** Generated output files. */
    readonly outputs: ReadonlyArray<CommandOutputFile>;
    /** Operation payload. */
    readonly data?: RunPayload;
    /** Count of modules involved. */
    readonly moduleCount: number;
    /** Count of profiles involved. */
    readonly profileCount: number;
    /** Count of targets involved. */
    readonly targetCount: number;
};

export function encodeRunOutput(writer: Writer, value: RunOutput): void {
    encodeRevision(writer, value["revision"]);
    writer.writeBool(value["success"]);
    writer.writeSigned(value["exitCode"]);
    writer.writeUnsigned(value["diagnostics"].length);
    for (const item0 of value["diagnostics"]) {
        encodeDiagnostic(writer, item0);
    }
    writer.writeUnsigned(value["files"].length);
    for (const item0 of value["files"]) {
        encodeFileImage(writer, item0);
    }
    writer.writeUnsigned(value["messages"].length);
    for (const item0 of value["messages"]) {
        encodeMessage(writer, item0);
    }
    writer.writeUnsigned(value["output"].length);
    for (const item0 of value["output"]) {
        encodeCommandOutputChunk(writer, item0);
    }
    writer.writeUnsigned(value["outputs"].length);
    for (const item0 of value["outputs"]) {
        encodeCommandOutputFile(writer, item0);
    }
    writer.writeOption(value["data"], (value0) => {
        encodeRunPayload(writer, value0);
    });
    writer.writeUnsigned(value["moduleCount"]);
    writer.writeUnsigned(value["profileCount"]);
    writer.writeUnsigned(value["targetCount"]);
}

export function decodeRunOutput(reader: Reader): RunOutput {
    const field0 = decodeRevision(reader);
    const field1 = reader.readBool();
    const field2 = reader.readSignedNumber();
    const field3 = (() => { const length3 = reader.readNumber(); const items3: Array<Diagnostic> = []; for (let index = 0; index < length3; index += 1) { items3.push(decodeDiagnostic(reader)); } return items3; })();
    const field4 = (() => { const length4 = reader.readNumber(); const items4: Array<FileImage> = []; for (let index = 0; index < length4; index += 1) { items4.push(decodeFileImage(reader)); } return items4; })();
    const field5 = (() => { const length5 = reader.readNumber(); const items5: Array<Message> = []; for (let index = 0; index < length5; index += 1) { items5.push(decodeMessage(reader)); } return items5; })();
    const field6 = (() => { const length6 = reader.readNumber(); const items6: Array<CommandOutputChunk> = []; for (let index = 0; index < length6; index += 1) { items6.push(decodeCommandOutputChunk(reader)); } return items6; })();
    const field7 = (() => { const length7 = reader.readNumber(); const items7: Array<CommandOutputFile> = []; for (let index = 0; index < length7; index += 1) { items7.push(decodeCommandOutputFile(reader)); } return items7; })();
    const field8 = reader.readOption(() => decodeRunPayload(reader));
    const field9 = reader.readNumber();
    const field10 = reader.readNumber();
    const field11 = reader.readNumber();

    return {
        "revision": field0,
        "success": field1,
        "exitCode": field2,
        "diagnostics": field3,
        "files": field4,
        "messages": field5,
        "output": field6,
        "outputs": field7,
        ...(field8 === undefined ? {} : { "data": field8 }),
        "moduleCount": field9,
        "profileCount": field10,
        "targetCount": field11,
    };
}

/** Output produced by one workspace command. */
export type TestOutput = {
    /** Revision used for this operation. */
    readonly revision: Revision;
    /** Whether the operation succeeded. */
    readonly success: boolean;
    /** Exit code for the operation. */
    readonly exitCode: number;
    /** Diagnostics produced by the operation. */
    readonly diagnostics: ReadonlyArray<Diagnostic>;
    /** File images needed to render diagnostics. */
    readonly files: ReadonlyArray<FileImage>;
    /** Messages produced by operation execution. */
    readonly messages: ReadonlyArray<Message>;
    /** Stream output collected during execution. */
    readonly output: ReadonlyArray<CommandOutputChunk>;
    /** Generated output files. */
    readonly outputs: ReadonlyArray<CommandOutputFile>;
    /** Operation payload. */
    readonly data: CommandMessagePayload;
    /** Count of modules involved. */
    readonly moduleCount: number;
    /** Count of profiles involved. */
    readonly profileCount: number;
    /** Count of targets involved. */
    readonly targetCount: number;
};

export function encodeTestOutput(writer: Writer, value: TestOutput): void {
    encodeRevision(writer, value["revision"]);
    writer.writeBool(value["success"]);
    writer.writeSigned(value["exitCode"]);
    writer.writeUnsigned(value["diagnostics"].length);
    for (const item0 of value["diagnostics"]) {
        encodeDiagnostic(writer, item0);
    }
    writer.writeUnsigned(value["files"].length);
    for (const item0 of value["files"]) {
        encodeFileImage(writer, item0);
    }
    writer.writeUnsigned(value["messages"].length);
    for (const item0 of value["messages"]) {
        encodeMessage(writer, item0);
    }
    writer.writeUnsigned(value["output"].length);
    for (const item0 of value["output"]) {
        encodeCommandOutputChunk(writer, item0);
    }
    writer.writeUnsigned(value["outputs"].length);
    for (const item0 of value["outputs"]) {
        encodeCommandOutputFile(writer, item0);
    }
    encodeCommandMessagePayload(writer, value["data"]);
    writer.writeUnsigned(value["moduleCount"]);
    writer.writeUnsigned(value["profileCount"]);
    writer.writeUnsigned(value["targetCount"]);
}

export function decodeTestOutput(reader: Reader): TestOutput {
    const field0 = decodeRevision(reader);
    const field1 = reader.readBool();
    const field2 = reader.readSignedNumber();
    const field3 = (() => { const length3 = reader.readNumber(); const items3: Array<Diagnostic> = []; for (let index = 0; index < length3; index += 1) { items3.push(decodeDiagnostic(reader)); } return items3; })();
    const field4 = (() => { const length4 = reader.readNumber(); const items4: Array<FileImage> = []; for (let index = 0; index < length4; index += 1) { items4.push(decodeFileImage(reader)); } return items4; })();
    const field5 = (() => { const length5 = reader.readNumber(); const items5: Array<Message> = []; for (let index = 0; index < length5; index += 1) { items5.push(decodeMessage(reader)); } return items5; })();
    const field6 = (() => { const length6 = reader.readNumber(); const items6: Array<CommandOutputChunk> = []; for (let index = 0; index < length6; index += 1) { items6.push(decodeCommandOutputChunk(reader)); } return items6; })();
    const field7 = (() => { const length7 = reader.readNumber(); const items7: Array<CommandOutputFile> = []; for (let index = 0; index < length7; index += 1) { items7.push(decodeCommandOutputFile(reader)); } return items7; })();
    const field8 = decodeCommandMessagePayload(reader);
    const field9 = reader.readNumber();
    const field10 = reader.readNumber();
    const field11 = reader.readNumber();

    return {
        "revision": field0,
        "success": field1,
        "exitCode": field2,
        "diagnostics": field3,
        "files": field4,
        "messages": field5,
        "output": field6,
        "outputs": field7,
        "data": field8,
        "moduleCount": field9,
        "profileCount": field10,
        "targetCount": field11,
    };
}

/** Output produced by one workspace command. */
export type DocOutput = {
    /** Revision used for this operation. */
    readonly revision: Revision;
    /** Whether the operation succeeded. */
    readonly success: boolean;
    /** Exit code for the operation. */
    readonly exitCode: number;
    /** Diagnostics produced by the operation. */
    readonly diagnostics: ReadonlyArray<Diagnostic>;
    /** File images needed to render diagnostics. */
    readonly files: ReadonlyArray<FileImage>;
    /** Messages produced by operation execution. */
    readonly messages: ReadonlyArray<Message>;
    /** Stream output collected during execution. */
    readonly output: ReadonlyArray<CommandOutputChunk>;
    /** Generated output files. */
    readonly outputs: ReadonlyArray<CommandOutputFile>;
    /** Operation payload. */
    readonly data: CommandMessagePayload;
    /** Count of modules involved. */
    readonly moduleCount: number;
    /** Count of profiles involved. */
    readonly profileCount: number;
    /** Count of targets involved. */
    readonly targetCount: number;
};

export function encodeDocOutput(writer: Writer, value: DocOutput): void {
    encodeRevision(writer, value["revision"]);
    writer.writeBool(value["success"]);
    writer.writeSigned(value["exitCode"]);
    writer.writeUnsigned(value["diagnostics"].length);
    for (const item0 of value["diagnostics"]) {
        encodeDiagnostic(writer, item0);
    }
    writer.writeUnsigned(value["files"].length);
    for (const item0 of value["files"]) {
        encodeFileImage(writer, item0);
    }
    writer.writeUnsigned(value["messages"].length);
    for (const item0 of value["messages"]) {
        encodeMessage(writer, item0);
    }
    writer.writeUnsigned(value["output"].length);
    for (const item0 of value["output"]) {
        encodeCommandOutputChunk(writer, item0);
    }
    writer.writeUnsigned(value["outputs"].length);
    for (const item0 of value["outputs"]) {
        encodeCommandOutputFile(writer, item0);
    }
    encodeCommandMessagePayload(writer, value["data"]);
    writer.writeUnsigned(value["moduleCount"]);
    writer.writeUnsigned(value["profileCount"]);
    writer.writeUnsigned(value["targetCount"]);
}

export function decodeDocOutput(reader: Reader): DocOutput {
    const field0 = decodeRevision(reader);
    const field1 = reader.readBool();
    const field2 = reader.readSignedNumber();
    const field3 = (() => { const length3 = reader.readNumber(); const items3: Array<Diagnostic> = []; for (let index = 0; index < length3; index += 1) { items3.push(decodeDiagnostic(reader)); } return items3; })();
    const field4 = (() => { const length4 = reader.readNumber(); const items4: Array<FileImage> = []; for (let index = 0; index < length4; index += 1) { items4.push(decodeFileImage(reader)); } return items4; })();
    const field5 = (() => { const length5 = reader.readNumber(); const items5: Array<Message> = []; for (let index = 0; index < length5; index += 1) { items5.push(decodeMessage(reader)); } return items5; })();
    const field6 = (() => { const length6 = reader.readNumber(); const items6: Array<CommandOutputChunk> = []; for (let index = 0; index < length6; index += 1) { items6.push(decodeCommandOutputChunk(reader)); } return items6; })();
    const field7 = (() => { const length7 = reader.readNumber(); const items7: Array<CommandOutputFile> = []; for (let index = 0; index < length7; index += 1) { items7.push(decodeCommandOutputFile(reader)); } return items7; })();
    const field8 = decodeCommandMessagePayload(reader);
    const field9 = reader.readNumber();
    const field10 = reader.readNumber();
    const field11 = reader.readNumber();

    return {
        "revision": field0,
        "success": field1,
        "exitCode": field2,
        "diagnostics": field3,
        "files": field4,
        "messages": field5,
        "output": field6,
        "outputs": field7,
        "data": field8,
        "moduleCount": field9,
        "profileCount": field10,
        "targetCount": field11,
    };
}

/** Output produced by one workspace command. */
export type BenchOutput = {
    /** Revision used for this operation. */
    readonly revision: Revision;
    /** Whether the operation succeeded. */
    readonly success: boolean;
    /** Exit code for the operation. */
    readonly exitCode: number;
    /** Diagnostics produced by the operation. */
    readonly diagnostics: ReadonlyArray<Diagnostic>;
    /** File images needed to render diagnostics. */
    readonly files: ReadonlyArray<FileImage>;
    /** Messages produced by operation execution. */
    readonly messages: ReadonlyArray<Message>;
    /** Stream output collected during execution. */
    readonly output: ReadonlyArray<CommandOutputChunk>;
    /** Generated output files. */
    readonly outputs: ReadonlyArray<CommandOutputFile>;
    /** Operation payload. */
    readonly data: CommandMessagePayload;
    /** Count of modules involved. */
    readonly moduleCount: number;
    /** Count of profiles involved. */
    readonly profileCount: number;
    /** Count of targets involved. */
    readonly targetCount: number;
};

export function encodeBenchOutput(writer: Writer, value: BenchOutput): void {
    encodeRevision(writer, value["revision"]);
    writer.writeBool(value["success"]);
    writer.writeSigned(value["exitCode"]);
    writer.writeUnsigned(value["diagnostics"].length);
    for (const item0 of value["diagnostics"]) {
        encodeDiagnostic(writer, item0);
    }
    writer.writeUnsigned(value["files"].length);
    for (const item0 of value["files"]) {
        encodeFileImage(writer, item0);
    }
    writer.writeUnsigned(value["messages"].length);
    for (const item0 of value["messages"]) {
        encodeMessage(writer, item0);
    }
    writer.writeUnsigned(value["output"].length);
    for (const item0 of value["output"]) {
        encodeCommandOutputChunk(writer, item0);
    }
    writer.writeUnsigned(value["outputs"].length);
    for (const item0 of value["outputs"]) {
        encodeCommandOutputFile(writer, item0);
    }
    encodeCommandMessagePayload(writer, value["data"]);
    writer.writeUnsigned(value["moduleCount"]);
    writer.writeUnsigned(value["profileCount"]);
    writer.writeUnsigned(value["targetCount"]);
}

export function decodeBenchOutput(reader: Reader): BenchOutput {
    const field0 = decodeRevision(reader);
    const field1 = reader.readBool();
    const field2 = reader.readSignedNumber();
    const field3 = (() => { const length3 = reader.readNumber(); const items3: Array<Diagnostic> = []; for (let index = 0; index < length3; index += 1) { items3.push(decodeDiagnostic(reader)); } return items3; })();
    const field4 = (() => { const length4 = reader.readNumber(); const items4: Array<FileImage> = []; for (let index = 0; index < length4; index += 1) { items4.push(decodeFileImage(reader)); } return items4; })();
    const field5 = (() => { const length5 = reader.readNumber(); const items5: Array<Message> = []; for (let index = 0; index < length5; index += 1) { items5.push(decodeMessage(reader)); } return items5; })();
    const field6 = (() => { const length6 = reader.readNumber(); const items6: Array<CommandOutputChunk> = []; for (let index = 0; index < length6; index += 1) { items6.push(decodeCommandOutputChunk(reader)); } return items6; })();
    const field7 = (() => { const length7 = reader.readNumber(); const items7: Array<CommandOutputFile> = []; for (let index = 0; index < length7; index += 1) { items7.push(decodeCommandOutputFile(reader)); } return items7; })();
    const field8 = decodeCommandMessagePayload(reader);
    const field9 = reader.readNumber();
    const field10 = reader.readNumber();
    const field11 = reader.readNumber();

    return {
        "revision": field0,
        "success": field1,
        "exitCode": field2,
        "diagnostics": field3,
        "files": field4,
        "messages": field5,
        "output": field6,
        "outputs": field7,
        "data": field8,
        "moduleCount": field9,
        "profileCount": field10,
        "targetCount": field11,
    };
}

/** Output produced by one workspace command. */
export type InfoOutput = {
    /** Revision used for this operation. */
    readonly revision: Revision;
    /** Whether the operation succeeded. */
    readonly success: boolean;
    /** Exit code for the operation. */
    readonly exitCode: number;
    /** Diagnostics produced by the operation. */
    readonly diagnostics: ReadonlyArray<Diagnostic>;
    /** File images needed to render diagnostics. */
    readonly files: ReadonlyArray<FileImage>;
    /** Messages produced by operation execution. */
    readonly messages: ReadonlyArray<Message>;
    /** Stream output collected during execution. */
    readonly output: ReadonlyArray<CommandOutputChunk>;
    /** Generated output files. */
    readonly outputs: ReadonlyArray<CommandOutputFile>;
    /** Operation payload. */
    readonly data: InfoPayload;
    /** Count of modules involved. */
    readonly moduleCount: number;
    /** Count of profiles involved. */
    readonly profileCount: number;
    /** Count of targets involved. */
    readonly targetCount: number;
};

export function encodeInfoOutput(writer: Writer, value: InfoOutput): void {
    encodeRevision(writer, value["revision"]);
    writer.writeBool(value["success"]);
    writer.writeSigned(value["exitCode"]);
    writer.writeUnsigned(value["diagnostics"].length);
    for (const item0 of value["diagnostics"]) {
        encodeDiagnostic(writer, item0);
    }
    writer.writeUnsigned(value["files"].length);
    for (const item0 of value["files"]) {
        encodeFileImage(writer, item0);
    }
    writer.writeUnsigned(value["messages"].length);
    for (const item0 of value["messages"]) {
        encodeMessage(writer, item0);
    }
    writer.writeUnsigned(value["output"].length);
    for (const item0 of value["output"]) {
        encodeCommandOutputChunk(writer, item0);
    }
    writer.writeUnsigned(value["outputs"].length);
    for (const item0 of value["outputs"]) {
        encodeCommandOutputFile(writer, item0);
    }
    encodeInfoPayload(writer, value["data"]);
    writer.writeUnsigned(value["moduleCount"]);
    writer.writeUnsigned(value["profileCount"]);
    writer.writeUnsigned(value["targetCount"]);
}

export function decodeInfoOutput(reader: Reader): InfoOutput {
    const field0 = decodeRevision(reader);
    const field1 = reader.readBool();
    const field2 = reader.readSignedNumber();
    const field3 = (() => { const length3 = reader.readNumber(); const items3: Array<Diagnostic> = []; for (let index = 0; index < length3; index += 1) { items3.push(decodeDiagnostic(reader)); } return items3; })();
    const field4 = (() => { const length4 = reader.readNumber(); const items4: Array<FileImage> = []; for (let index = 0; index < length4; index += 1) { items4.push(decodeFileImage(reader)); } return items4; })();
    const field5 = (() => { const length5 = reader.readNumber(); const items5: Array<Message> = []; for (let index = 0; index < length5; index += 1) { items5.push(decodeMessage(reader)); } return items5; })();
    const field6 = (() => { const length6 = reader.readNumber(); const items6: Array<CommandOutputChunk> = []; for (let index = 0; index < length6; index += 1) { items6.push(decodeCommandOutputChunk(reader)); } return items6; })();
    const field7 = (() => { const length7 = reader.readNumber(); const items7: Array<CommandOutputFile> = []; for (let index = 0; index < length7; index += 1) { items7.push(decodeCommandOutputFile(reader)); } return items7; })();
    const field8 = decodeInfoPayload(reader);
    const field9 = reader.readNumber();
    const field10 = reader.readNumber();
    const field11 = reader.readNumber();

    return {
        "revision": field0,
        "success": field1,
        "exitCode": field2,
        "diagnostics": field3,
        "files": field4,
        "messages": field5,
        "output": field6,
        "outputs": field7,
        "data": field8,
        "moduleCount": field9,
        "profileCount": field10,
        "targetCount": field11,
    };
}

/** Output produced by one workspace command. */
export type TargetsOutput = {
    /** Revision used for this operation. */
    readonly revision: Revision;
    /** Whether the operation succeeded. */
    readonly success: boolean;
    /** Exit code for the operation. */
    readonly exitCode: number;
    /** Diagnostics produced by the operation. */
    readonly diagnostics: ReadonlyArray<Diagnostic>;
    /** File images needed to render diagnostics. */
    readonly files: ReadonlyArray<FileImage>;
    /** Messages produced by operation execution. */
    readonly messages: ReadonlyArray<Message>;
    /** Stream output collected during execution. */
    readonly output: ReadonlyArray<CommandOutputChunk>;
    /** Generated output files. */
    readonly outputs: ReadonlyArray<CommandOutputFile>;
    /** Operation payload. */
    readonly data: TargetsPayload;
    /** Count of modules involved. */
    readonly moduleCount: number;
    /** Count of profiles involved. */
    readonly profileCount: number;
    /** Count of targets involved. */
    readonly targetCount: number;
};

export function encodeTargetsOutput(writer: Writer, value: TargetsOutput): void {
    encodeRevision(writer, value["revision"]);
    writer.writeBool(value["success"]);
    writer.writeSigned(value["exitCode"]);
    writer.writeUnsigned(value["diagnostics"].length);
    for (const item0 of value["diagnostics"]) {
        encodeDiagnostic(writer, item0);
    }
    writer.writeUnsigned(value["files"].length);
    for (const item0 of value["files"]) {
        encodeFileImage(writer, item0);
    }
    writer.writeUnsigned(value["messages"].length);
    for (const item0 of value["messages"]) {
        encodeMessage(writer, item0);
    }
    writer.writeUnsigned(value["output"].length);
    for (const item0 of value["output"]) {
        encodeCommandOutputChunk(writer, item0);
    }
    writer.writeUnsigned(value["outputs"].length);
    for (const item0 of value["outputs"]) {
        encodeCommandOutputFile(writer, item0);
    }
    encodeTargetsPayload(writer, value["data"]);
    writer.writeUnsigned(value["moduleCount"]);
    writer.writeUnsigned(value["profileCount"]);
    writer.writeUnsigned(value["targetCount"]);
}

export function decodeTargetsOutput(reader: Reader): TargetsOutput {
    const field0 = decodeRevision(reader);
    const field1 = reader.readBool();
    const field2 = reader.readSignedNumber();
    const field3 = (() => { const length3 = reader.readNumber(); const items3: Array<Diagnostic> = []; for (let index = 0; index < length3; index += 1) { items3.push(decodeDiagnostic(reader)); } return items3; })();
    const field4 = (() => { const length4 = reader.readNumber(); const items4: Array<FileImage> = []; for (let index = 0; index < length4; index += 1) { items4.push(decodeFileImage(reader)); } return items4; })();
    const field5 = (() => { const length5 = reader.readNumber(); const items5: Array<Message> = []; for (let index = 0; index < length5; index += 1) { items5.push(decodeMessage(reader)); } return items5; })();
    const field6 = (() => { const length6 = reader.readNumber(); const items6: Array<CommandOutputChunk> = []; for (let index = 0; index < length6; index += 1) { items6.push(decodeCommandOutputChunk(reader)); } return items6; })();
    const field7 = (() => { const length7 = reader.readNumber(); const items7: Array<CommandOutputFile> = []; for (let index = 0; index < length7; index += 1) { items7.push(decodeCommandOutputFile(reader)); } return items7; })();
    const field8 = decodeTargetsPayload(reader);
    const field9 = reader.readNumber();
    const field10 = reader.readNumber();
    const field11 = reader.readNumber();

    return {
        "revision": field0,
        "success": field1,
        "exitCode": field2,
        "diagnostics": field3,
        "files": field4,
        "messages": field5,
        "output": field6,
        "outputs": field7,
        "data": field8,
        "moduleCount": field9,
        "profileCount": field10,
        "targetCount": field11,
    };
}

/** Output produced by one workspace command. */
export type CacheOutput = {
    /** Revision used for this operation. */
    readonly revision: Revision;
    /** Whether the operation succeeded. */
    readonly success: boolean;
    /** Exit code for the operation. */
    readonly exitCode: number;
    /** Diagnostics produced by the operation. */
    readonly diagnostics: ReadonlyArray<Diagnostic>;
    /** File images needed to render diagnostics. */
    readonly files: ReadonlyArray<FileImage>;
    /** Messages produced by operation execution. */
    readonly messages: ReadonlyArray<Message>;
    /** Stream output collected during execution. */
    readonly output: ReadonlyArray<CommandOutputChunk>;
    /** Generated output files. */
    readonly outputs: ReadonlyArray<CommandOutputFile>;
    /** Operation payload. */
    readonly data: CachePayload;
    /** Count of modules involved. */
    readonly moduleCount: number;
    /** Count of profiles involved. */
    readonly profileCount: number;
    /** Count of targets involved. */
    readonly targetCount: number;
};

export function encodeCacheOutput(writer: Writer, value: CacheOutput): void {
    encodeRevision(writer, value["revision"]);
    writer.writeBool(value["success"]);
    writer.writeSigned(value["exitCode"]);
    writer.writeUnsigned(value["diagnostics"].length);
    for (const item0 of value["diagnostics"]) {
        encodeDiagnostic(writer, item0);
    }
    writer.writeUnsigned(value["files"].length);
    for (const item0 of value["files"]) {
        encodeFileImage(writer, item0);
    }
    writer.writeUnsigned(value["messages"].length);
    for (const item0 of value["messages"]) {
        encodeMessage(writer, item0);
    }
    writer.writeUnsigned(value["output"].length);
    for (const item0 of value["output"]) {
        encodeCommandOutputChunk(writer, item0);
    }
    writer.writeUnsigned(value["outputs"].length);
    for (const item0 of value["outputs"]) {
        encodeCommandOutputFile(writer, item0);
    }
    encodeCachePayload(writer, value["data"]);
    writer.writeUnsigned(value["moduleCount"]);
    writer.writeUnsigned(value["profileCount"]);
    writer.writeUnsigned(value["targetCount"]);
}

export function decodeCacheOutput(reader: Reader): CacheOutput {
    const field0 = decodeRevision(reader);
    const field1 = reader.readBool();
    const field2 = reader.readSignedNumber();
    const field3 = (() => { const length3 = reader.readNumber(); const items3: Array<Diagnostic> = []; for (let index = 0; index < length3; index += 1) { items3.push(decodeDiagnostic(reader)); } return items3; })();
    const field4 = (() => { const length4 = reader.readNumber(); const items4: Array<FileImage> = []; for (let index = 0; index < length4; index += 1) { items4.push(decodeFileImage(reader)); } return items4; })();
    const field5 = (() => { const length5 = reader.readNumber(); const items5: Array<Message> = []; for (let index = 0; index < length5; index += 1) { items5.push(decodeMessage(reader)); } return items5; })();
    const field6 = (() => { const length6 = reader.readNumber(); const items6: Array<CommandOutputChunk> = []; for (let index = 0; index < length6; index += 1) { items6.push(decodeCommandOutputChunk(reader)); } return items6; })();
    const field7 = (() => { const length7 = reader.readNumber(); const items7: Array<CommandOutputFile> = []; for (let index = 0; index < length7; index += 1) { items7.push(decodeCommandOutputFile(reader)); } return items7; })();
    const field8 = decodeCachePayload(reader);
    const field9 = reader.readNumber();
    const field10 = reader.readNumber();
    const field11 = reader.readNumber();

    return {
        "revision": field0,
        "success": field1,
        "exitCode": field2,
        "diagnostics": field3,
        "files": field4,
        "messages": field5,
        "output": field6,
        "outputs": field7,
        "data": field8,
        "moduleCount": field9,
        "profileCount": field10,
        "targetCount": field11,
    };
}

/** Output produced by one workspace command. */
export type SettingsOutput = {
    /** Revision used for this operation. */
    readonly revision: Revision;
    /** Whether the operation succeeded. */
    readonly success: boolean;
    /** Exit code for the operation. */
    readonly exitCode: number;
    /** Diagnostics produced by the operation. */
    readonly diagnostics: ReadonlyArray<Diagnostic>;
    /** File images needed to render diagnostics. */
    readonly files: ReadonlyArray<FileImage>;
    /** Messages produced by operation execution. */
    readonly messages: ReadonlyArray<Message>;
    /** Stream output collected during execution. */
    readonly output: ReadonlyArray<CommandOutputChunk>;
    /** Generated output files. */
    readonly outputs: ReadonlyArray<CommandOutputFile>;
    /** Operation payload. */
    readonly data: SettingsPayload;
    /** Count of modules involved. */
    readonly moduleCount: number;
    /** Count of profiles involved. */
    readonly profileCount: number;
    /** Count of targets involved. */
    readonly targetCount: number;
};

export function encodeSettingsOutput(writer: Writer, value: SettingsOutput): void {
    encodeRevision(writer, value["revision"]);
    writer.writeBool(value["success"]);
    writer.writeSigned(value["exitCode"]);
    writer.writeUnsigned(value["diagnostics"].length);
    for (const item0 of value["diagnostics"]) {
        encodeDiagnostic(writer, item0);
    }
    writer.writeUnsigned(value["files"].length);
    for (const item0 of value["files"]) {
        encodeFileImage(writer, item0);
    }
    writer.writeUnsigned(value["messages"].length);
    for (const item0 of value["messages"]) {
        encodeMessage(writer, item0);
    }
    writer.writeUnsigned(value["output"].length);
    for (const item0 of value["output"]) {
        encodeCommandOutputChunk(writer, item0);
    }
    writer.writeUnsigned(value["outputs"].length);
    for (const item0 of value["outputs"]) {
        encodeCommandOutputFile(writer, item0);
    }
    encodeSettingsPayload(writer, value["data"]);
    writer.writeUnsigned(value["moduleCount"]);
    writer.writeUnsigned(value["profileCount"]);
    writer.writeUnsigned(value["targetCount"]);
}

export function decodeSettingsOutput(reader: Reader): SettingsOutput {
    const field0 = decodeRevision(reader);
    const field1 = reader.readBool();
    const field2 = reader.readSignedNumber();
    const field3 = (() => { const length3 = reader.readNumber(); const items3: Array<Diagnostic> = []; for (let index = 0; index < length3; index += 1) { items3.push(decodeDiagnostic(reader)); } return items3; })();
    const field4 = (() => { const length4 = reader.readNumber(); const items4: Array<FileImage> = []; for (let index = 0; index < length4; index += 1) { items4.push(decodeFileImage(reader)); } return items4; })();
    const field5 = (() => { const length5 = reader.readNumber(); const items5: Array<Message> = []; for (let index = 0; index < length5; index += 1) { items5.push(decodeMessage(reader)); } return items5; })();
    const field6 = (() => { const length6 = reader.readNumber(); const items6: Array<CommandOutputChunk> = []; for (let index = 0; index < length6; index += 1) { items6.push(decodeCommandOutputChunk(reader)); } return items6; })();
    const field7 = (() => { const length7 = reader.readNumber(); const items7: Array<CommandOutputFile> = []; for (let index = 0; index < length7; index += 1) { items7.push(decodeCommandOutputFile(reader)); } return items7; })();
    const field8 = decodeSettingsPayload(reader);
    const field9 = reader.readNumber();
    const field10 = reader.readNumber();
    const field11 = reader.readNumber();

    return {
        "revision": field0,
        "success": field1,
        "exitCode": field2,
        "diagnostics": field3,
        "files": field4,
        "messages": field5,
        "output": field6,
        "outputs": field7,
        "data": field8,
        "moduleCount": field9,
        "profileCount": field10,
        "targetCount": field11,
    };
}

/** Output produced by one workspace command. */
export type DoctorOutput = {
    /** Revision used for this operation. */
    readonly revision: Revision;
    /** Whether the operation succeeded. */
    readonly success: boolean;
    /** Exit code for the operation. */
    readonly exitCode: number;
    /** Diagnostics produced by the operation. */
    readonly diagnostics: ReadonlyArray<Diagnostic>;
    /** File images needed to render diagnostics. */
    readonly files: ReadonlyArray<FileImage>;
    /** Messages produced by operation execution. */
    readonly messages: ReadonlyArray<Message>;
    /** Stream output collected during execution. */
    readonly output: ReadonlyArray<CommandOutputChunk>;
    /** Generated output files. */
    readonly outputs: ReadonlyArray<CommandOutputFile>;
    /** Operation payload. */
    readonly data: DoctorPayload;
    /** Count of modules involved. */
    readonly moduleCount: number;
    /** Count of profiles involved. */
    readonly profileCount: number;
    /** Count of targets involved. */
    readonly targetCount: number;
};

export function encodeDoctorOutput(writer: Writer, value: DoctorOutput): void {
    encodeRevision(writer, value["revision"]);
    writer.writeBool(value["success"]);
    writer.writeSigned(value["exitCode"]);
    writer.writeUnsigned(value["diagnostics"].length);
    for (const item0 of value["diagnostics"]) {
        encodeDiagnostic(writer, item0);
    }
    writer.writeUnsigned(value["files"].length);
    for (const item0 of value["files"]) {
        encodeFileImage(writer, item0);
    }
    writer.writeUnsigned(value["messages"].length);
    for (const item0 of value["messages"]) {
        encodeMessage(writer, item0);
    }
    writer.writeUnsigned(value["output"].length);
    for (const item0 of value["output"]) {
        encodeCommandOutputChunk(writer, item0);
    }
    writer.writeUnsigned(value["outputs"].length);
    for (const item0 of value["outputs"]) {
        encodeCommandOutputFile(writer, item0);
    }
    encodeDoctorPayload(writer, value["data"]);
    writer.writeUnsigned(value["moduleCount"]);
    writer.writeUnsigned(value["profileCount"]);
    writer.writeUnsigned(value["targetCount"]);
}

export function decodeDoctorOutput(reader: Reader): DoctorOutput {
    const field0 = decodeRevision(reader);
    const field1 = reader.readBool();
    const field2 = reader.readSignedNumber();
    const field3 = (() => { const length3 = reader.readNumber(); const items3: Array<Diagnostic> = []; for (let index = 0; index < length3; index += 1) { items3.push(decodeDiagnostic(reader)); } return items3; })();
    const field4 = (() => { const length4 = reader.readNumber(); const items4: Array<FileImage> = []; for (let index = 0; index < length4; index += 1) { items4.push(decodeFileImage(reader)); } return items4; })();
    const field5 = (() => { const length5 = reader.readNumber(); const items5: Array<Message> = []; for (let index = 0; index < length5; index += 1) { items5.push(decodeMessage(reader)); } return items5; })();
    const field6 = (() => { const length6 = reader.readNumber(); const items6: Array<CommandOutputChunk> = []; for (let index = 0; index < length6; index += 1) { items6.push(decodeCommandOutputChunk(reader)); } return items6; })();
    const field7 = (() => { const length7 = reader.readNumber(); const items7: Array<CommandOutputFile> = []; for (let index = 0; index < length7; index += 1) { items7.push(decodeCommandOutputFile(reader)); } return items7; })();
    const field8 = decodeDoctorPayload(reader);
    const field9 = reader.readNumber();
    const field10 = reader.readNumber();
    const field11 = reader.readNumber();

    return {
        "revision": field0,
        "success": field1,
        "exitCode": field2,
        "diagnostics": field3,
        "files": field4,
        "messages": field5,
        "output": field6,
        "outputs": field7,
        "data": field8,
        "moduleCount": field9,
        "profileCount": field10,
        "targetCount": field11,
    };
}

/** Output produced by one workspace command. */
export type TaskOutput = {
    /** Revision used for this operation. */
    readonly revision: Revision;
    /** Whether the operation succeeded. */
    readonly success: boolean;
    /** Exit code for the operation. */
    readonly exitCode: number;
    /** Diagnostics produced by the operation. */
    readonly diagnostics: ReadonlyArray<Diagnostic>;
    /** File images needed to render diagnostics. */
    readonly files: ReadonlyArray<FileImage>;
    /** Messages produced by operation execution. */
    readonly messages: ReadonlyArray<Message>;
    /** Stream output collected during execution. */
    readonly output: ReadonlyArray<CommandOutputChunk>;
    /** Generated output files. */
    readonly outputs: ReadonlyArray<CommandOutputFile>;
    /** Operation payload. */
    readonly data: TaskPayload;
    /** Count of modules involved. */
    readonly moduleCount: number;
    /** Count of profiles involved. */
    readonly profileCount: number;
    /** Count of targets involved. */
    readonly targetCount: number;
};

export function encodeTaskOutput(writer: Writer, value: TaskOutput): void {
    encodeRevision(writer, value["revision"]);
    writer.writeBool(value["success"]);
    writer.writeSigned(value["exitCode"]);
    writer.writeUnsigned(value["diagnostics"].length);
    for (const item0 of value["diagnostics"]) {
        encodeDiagnostic(writer, item0);
    }
    writer.writeUnsigned(value["files"].length);
    for (const item0 of value["files"]) {
        encodeFileImage(writer, item0);
    }
    writer.writeUnsigned(value["messages"].length);
    for (const item0 of value["messages"]) {
        encodeMessage(writer, item0);
    }
    writer.writeUnsigned(value["output"].length);
    for (const item0 of value["output"]) {
        encodeCommandOutputChunk(writer, item0);
    }
    writer.writeUnsigned(value["outputs"].length);
    for (const item0 of value["outputs"]) {
        encodeCommandOutputFile(writer, item0);
    }
    encodeTaskPayload(writer, value["data"]);
    writer.writeUnsigned(value["moduleCount"]);
    writer.writeUnsigned(value["profileCount"]);
    writer.writeUnsigned(value["targetCount"]);
}

export function decodeTaskOutput(reader: Reader): TaskOutput {
    const field0 = decodeRevision(reader);
    const field1 = reader.readBool();
    const field2 = reader.readSignedNumber();
    const field3 = (() => { const length3 = reader.readNumber(); const items3: Array<Diagnostic> = []; for (let index = 0; index < length3; index += 1) { items3.push(decodeDiagnostic(reader)); } return items3; })();
    const field4 = (() => { const length4 = reader.readNumber(); const items4: Array<FileImage> = []; for (let index = 0; index < length4; index += 1) { items4.push(decodeFileImage(reader)); } return items4; })();
    const field5 = (() => { const length5 = reader.readNumber(); const items5: Array<Message> = []; for (let index = 0; index < length5; index += 1) { items5.push(decodeMessage(reader)); } return items5; })();
    const field6 = (() => { const length6 = reader.readNumber(); const items6: Array<CommandOutputChunk> = []; for (let index = 0; index < length6; index += 1) { items6.push(decodeCommandOutputChunk(reader)); } return items6; })();
    const field7 = (() => { const length7 = reader.readNumber(); const items7: Array<CommandOutputFile> = []; for (let index = 0; index < length7; index += 1) { items7.push(decodeCommandOutputFile(reader)); } return items7; })();
    const field8 = decodeTaskPayload(reader);
    const field9 = reader.readNumber();
    const field10 = reader.readNumber();
    const field11 = reader.readNumber();

    return {
        "revision": field0,
        "success": field1,
        "exitCode": field2,
        "diagnostics": field3,
        "files": field4,
        "messages": field5,
        "output": field6,
        "outputs": field7,
        "data": field8,
        "moduleCount": field9,
        "profileCount": field10,
        "targetCount": field11,
    };
}

/** Output produced by one workspace command. */
export type CleanOutput = {
    /** Revision used for this operation. */
    readonly revision: Revision;
    /** Whether the operation succeeded. */
    readonly success: boolean;
    /** Exit code for the operation. */
    readonly exitCode: number;
    /** Diagnostics produced by the operation. */
    readonly diagnostics: ReadonlyArray<Diagnostic>;
    /** File images needed to render diagnostics. */
    readonly files: ReadonlyArray<FileImage>;
    /** Messages produced by operation execution. */
    readonly messages: ReadonlyArray<Message>;
    /** Stream output collected during execution. */
    readonly output: ReadonlyArray<CommandOutputChunk>;
    /** Generated output files. */
    readonly outputs: ReadonlyArray<CommandOutputFile>;
    /** Operation payload. */
    readonly data: CleanPayload;
    /** Count of modules involved. */
    readonly moduleCount: number;
    /** Count of profiles involved. */
    readonly profileCount: number;
    /** Count of targets involved. */
    readonly targetCount: number;
};

export function encodeCleanOutput(writer: Writer, value: CleanOutput): void {
    encodeRevision(writer, value["revision"]);
    writer.writeBool(value["success"]);
    writer.writeSigned(value["exitCode"]);
    writer.writeUnsigned(value["diagnostics"].length);
    for (const item0 of value["diagnostics"]) {
        encodeDiagnostic(writer, item0);
    }
    writer.writeUnsigned(value["files"].length);
    for (const item0 of value["files"]) {
        encodeFileImage(writer, item0);
    }
    writer.writeUnsigned(value["messages"].length);
    for (const item0 of value["messages"]) {
        encodeMessage(writer, item0);
    }
    writer.writeUnsigned(value["output"].length);
    for (const item0 of value["output"]) {
        encodeCommandOutputChunk(writer, item0);
    }
    writer.writeUnsigned(value["outputs"].length);
    for (const item0 of value["outputs"]) {
        encodeCommandOutputFile(writer, item0);
    }
    encodeCleanPayload(writer, value["data"]);
    writer.writeUnsigned(value["moduleCount"]);
    writer.writeUnsigned(value["profileCount"]);
    writer.writeUnsigned(value["targetCount"]);
}

export function decodeCleanOutput(reader: Reader): CleanOutput {
    const field0 = decodeRevision(reader);
    const field1 = reader.readBool();
    const field2 = reader.readSignedNumber();
    const field3 = (() => { const length3 = reader.readNumber(); const items3: Array<Diagnostic> = []; for (let index = 0; index < length3; index += 1) { items3.push(decodeDiagnostic(reader)); } return items3; })();
    const field4 = (() => { const length4 = reader.readNumber(); const items4: Array<FileImage> = []; for (let index = 0; index < length4; index += 1) { items4.push(decodeFileImage(reader)); } return items4; })();
    const field5 = (() => { const length5 = reader.readNumber(); const items5: Array<Message> = []; for (let index = 0; index < length5; index += 1) { items5.push(decodeMessage(reader)); } return items5; })();
    const field6 = (() => { const length6 = reader.readNumber(); const items6: Array<CommandOutputChunk> = []; for (let index = 0; index < length6; index += 1) { items6.push(decodeCommandOutputChunk(reader)); } return items6; })();
    const field7 = (() => { const length7 = reader.readNumber(); const items7: Array<CommandOutputFile> = []; for (let index = 0; index < length7; index += 1) { items7.push(decodeCommandOutputFile(reader)); } return items7; })();
    const field8 = decodeCleanPayload(reader);
    const field9 = reader.readNumber();
    const field10 = reader.readNumber();
    const field11 = reader.readNumber();

    return {
        "revision": field0,
        "success": field1,
        "exitCode": field2,
        "diagnostics": field3,
        "files": field4,
        "messages": field5,
        "output": field6,
        "outputs": field7,
        "data": field8,
        "moduleCount": field9,
        "profileCount": field10,
        "targetCount": field11,
    };
}
