/* generated by destack_bridge_generate, do not edit */

import { Reader, SerdeError, Writer, compareBytes, nestedBytes } from "../../../../protocol/serde.js";
import type { ArtifactReference } from "../../artifact/reference.js";
import type { CommandEnvVar } from "./common.js";
import type { CommandInput } from "./common.js";
import type { CommandRevision } from "./common.js";
import type { CommandTargetOverrides } from "./common.js";
import type { ManifestOverride } from "./common.js";
import type { TraceSnapshot } from "../../repository/provider/trace.js";
import type { TraceView } from "../../repository/provider/trace.js";
import { decodeArtifactReference, encodeArtifactReference } from "../../artifact/reference.js";
import { decodeCommandEnvVar, encodeCommandEnvVar } from "./common.js";
import { decodeCommandInput, encodeCommandInput } from "./common.js";
import { decodeCommandRevision, encodeCommandRevision } from "./common.js";
import { decodeCommandTargetOverrides, encodeCommandTargetOverrides } from "./common.js";
import { decodeManifestOverride, encodeManifestOverride } from "./common.js";
import { decodeTraceSnapshot, encodeTraceSnapshot } from "../../repository/provider/trace.js";
import { decodeTraceView, encodeTraceView } from "../../repository/provider/trace.js";

/** Request to build target artifacts. */
export type BuildInput = {
    /** Revision selected for this build. */
    readonly revision: CommandRevision;
    /** Input sources for the command. */
    readonly inputs: ReadonlyArray<CommandInput>;
    /** Whether destack.json should resolve inputs when none are provided. */
    readonly configInputs: boolean;
    /** Optional working directory for this command. */
    readonly cwd?: string;
    /** Optional Destack manifest path override. */
    readonly manifest?: string;
    /** Optional target name override. */
    readonly target?: string;
    /** Optional target overrides. */
    readonly targetOverrides?: CommandTargetOverrides;
    /** Optional profile name override. */
    readonly profile?: string;
    /** Optional environment overrides. */
    readonly env: ReadonlyArray<CommandEnvVar>;
    /** Optional manifest overrides. */
    readonly overrides: ReadonlyArray<ManifestOverride>;
    /** Whether the command should watch for changes. */
    readonly watch: boolean;
    /** Whether the command should skip writes. */
    readonly dryRun: boolean;
    /** Trace detail returned in the response. */
    readonly trace: TraceView;
    /** Product name selected for this build. */
    readonly product?: string;
    /** Build outputs requested by the caller. */
    readonly outputs: BuildOutputs;
};

export function encodeBuildInput(writer: Writer, value: BuildInput): void {
    encodeCommandRevision(writer, value["revision"]);
    writer.writeUnsigned(value["inputs"].length);
    for (const item0 of value["inputs"]) {
        encodeCommandInput(writer, item0);
    }
    writer.writeBool(value["configInputs"]);
    writer.writeOption(value["cwd"], (value0) => {
        writer.writeString(value0);
    });
    writer.writeOption(value["manifest"], (value0) => {
        writer.writeString(value0);
    });
    writer.writeOption(value["target"], (value0) => {
        writer.writeString(value0);
    });
    writer.writeOption(value["targetOverrides"], (value0) => {
        encodeCommandTargetOverrides(writer, value0);
    });
    writer.writeOption(value["profile"], (value0) => {
        writer.writeString(value0);
    });
    writer.writeUnsigned(value["env"].length);
    for (const item0 of value["env"]) {
        encodeCommandEnvVar(writer, item0);
    }
    writer.writeUnsigned(value["overrides"].length);
    for (const item0 of value["overrides"]) {
        encodeManifestOverride(writer, item0);
    }
    writer.writeBool(value["watch"]);
    writer.writeBool(value["dryRun"]);
    encodeTraceView(writer, value["trace"]);
    writer.writeOption(value["product"], (value0) => {
        writer.writeString(value0);
    });
    encodeBuildOutputs(writer, value["outputs"]);
}

export function decodeBuildInput(reader: Reader): BuildInput {
    const field0 = decodeCommandRevision(reader);
    const field1 = (() => { const length1 = reader.readNumber(); const items1: Array<CommandInput> = []; for (let index = 0; index < length1; index += 1) { items1.push(decodeCommandInput(reader)); } return items1; })();
    const field2 = reader.readBool();
    const field3 = reader.readOption(() => reader.readString());
    const field4 = reader.readOption(() => reader.readString());
    const field5 = reader.readOption(() => reader.readString());
    const field6 = reader.readOption(() => decodeCommandTargetOverrides(reader));
    const field7 = reader.readOption(() => reader.readString());
    const field8 = (() => { const length8 = reader.readNumber(); const items8: Array<CommandEnvVar> = []; for (let index = 0; index < length8; index += 1) { items8.push(decodeCommandEnvVar(reader)); } return items8; })();
    const field9 = (() => { const length9 = reader.readNumber(); const items9: Array<ManifestOverride> = []; for (let index = 0; index < length9; index += 1) { items9.push(decodeManifestOverride(reader)); } return items9; })();
    const field10 = reader.readBool();
    const field11 = reader.readBool();
    const field12 = decodeTraceView(reader);
    const field13 = reader.readOption(() => reader.readString());
    const field14 = decodeBuildOutputs(reader);

    return {
        "revision": field0,
        "inputs": field1,
        "configInputs": field2,
        ...(field3 === undefined ? {} : { "cwd": field3 }),
        ...(field4 === undefined ? {} : { "manifest": field4 }),
        ...(field5 === undefined ? {} : { "target": field5 }),
        ...(field6 === undefined ? {} : { "targetOverrides": field6 }),
        ...(field7 === undefined ? {} : { "profile": field7 }),
        "env": field8,
        "overrides": field9,
        "watch": field10,
        "dryRun": field11,
        "trace": field12,
        ...(field13 === undefined ? {} : { "product": field13 }),
        "outputs": field14,
    };
}

/** Build output families requested by a caller. */
export type BuildOutputs = {
    /** Return product artifact refs. */
    readonly products: boolean;
    /** Return bundle artifact refs. */
    readonly bundles: boolean;
    /** Return program artifact refs. */
    readonly programs: boolean;
    /** Return per-module asset artifact refs. */
    readonly assets: boolean;
};

export function encodeBuildOutputs(writer: Writer, value: BuildOutputs): void {
    writer.writeBool(value["products"]);
    writer.writeBool(value["bundles"]);
    writer.writeBool(value["programs"]);
    writer.writeBool(value["assets"]);
}

export function decodeBuildOutputs(reader: Reader): BuildOutputs {
    const field0 = reader.readBool();
    const field1 = reader.readBool();
    const field2 = reader.readBool();
    const field3 = reader.readBool();

    return {
        "products": field0,
        "bundles": field1,
        "programs": field2,
        "assets": field3,
    };
}

/** Payload for build command output. */
export type BuildPayload = {
    /** Product artifacts produced by this build. */
    readonly products: ReadonlyArray<ArtifactReference>;
    /** Bundle artifacts produced by this build. */
    readonly bundles: ReadonlyArray<ArtifactReference>;
    /** Program artifacts produced by this build. */
    readonly programs: ReadonlyArray<ArtifactReference>;
    /** Per-module asset artifacts produced by this build. */
    readonly assets: ReadonlyArray<ArtifactReference>;
    /** Trace payload for this build. */
    readonly trace: TraceSnapshot;
};

export function encodeBuildPayload(writer: Writer, value: BuildPayload): void {
    writer.writeUnsigned(value["products"].length);
    for (const item0 of value["products"]) {
        encodeArtifactReference(writer, item0);
    }
    writer.writeUnsigned(value["bundles"].length);
    for (const item0 of value["bundles"]) {
        encodeArtifactReference(writer, item0);
    }
    writer.writeUnsigned(value["programs"].length);
    for (const item0 of value["programs"]) {
        encodeArtifactReference(writer, item0);
    }
    writer.writeUnsigned(value["assets"].length);
    for (const item0 of value["assets"]) {
        encodeArtifactReference(writer, item0);
    }
    encodeTraceSnapshot(writer, value["trace"]);
}

export function decodeBuildPayload(reader: Reader): BuildPayload {
    const field0 = (() => { const length0 = reader.readNumber(); const items0: Array<ArtifactReference> = []; for (let index = 0; index < length0; index += 1) { items0.push(decodeArtifactReference(reader)); } return items0; })();
    const field1 = (() => { const length1 = reader.readNumber(); const items1: Array<ArtifactReference> = []; for (let index = 0; index < length1; index += 1) { items1.push(decodeArtifactReference(reader)); } return items1; })();
    const field2 = (() => { const length2 = reader.readNumber(); const items2: Array<ArtifactReference> = []; for (let index = 0; index < length2; index += 1) { items2.push(decodeArtifactReference(reader)); } return items2; })();
    const field3 = (() => { const length3 = reader.readNumber(); const items3: Array<ArtifactReference> = []; for (let index = 0; index < length3; index += 1) { items3.push(decodeArtifactReference(reader)); } return items3; })();
    const field4 = decodeTraceSnapshot(reader);

    return {
        "products": field0,
        "bundles": field1,
        "programs": field2,
        "assets": field3,
        "trace": field4,
    };
}
