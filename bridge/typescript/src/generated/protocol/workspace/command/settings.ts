/* generated by destack_bridge_generate, do not edit */

import { Reader, SerdeError, Writer, compareBytes, nestedBytes } from "../../../../protocol/serde.js";
import type { CommandEnvVar } from "./common.js";
import type { CommandInput } from "./common.js";
import type { CommandRevision } from "./common.js";
import type { CommandTargetOverrides } from "./common.js";
import type { ManifestOverride } from "./common.js";
import { decodeCommandEnvVar, encodeCommandEnvVar } from "./common.js";
import { decodeCommandInput, encodeCommandInput } from "./common.js";
import { decodeCommandRevision, encodeCommandRevision } from "./common.js";
import { decodeCommandTargetOverrides, encodeCommandTargetOverrides } from "./common.js";
import { decodeManifestOverride, encodeManifestOverride } from "./common.js";

/** Request to return resolved settings. */
export type SettingsInput = {
    /** Revision selected for this settings request. */
    readonly revision: CommandRevision;
    /** Input sources for the command. */
    readonly inputs: ReadonlyArray<CommandInput>;
    /** Whether destack.json should resolve inputs when none are provided. */
    readonly configInputs: boolean;
    /** Optional working directory for this command. */
    readonly cwd?: string;
    /** Optional Destack manifest path override. */
    readonly manifest?: string;
    /** Optional target name override. */
    readonly target?: string;
    /** Optional target overrides. */
    readonly targetOverrides?: CommandTargetOverrides;
    /** Optional profile name override. */
    readonly profile?: string;
    /** Optional environment overrides. */
    readonly env: ReadonlyArray<CommandEnvVar>;
    /** Optional manifest overrides. */
    readonly overrides: ReadonlyArray<ManifestOverride>;
    /** Whether the command should watch for changes. */
    readonly watch: boolean;
    /** Whether the command should skip writes. */
    readonly dryRun: boolean;
};

export function encodeSettingsInput(writer: Writer, value: SettingsInput): void {
    encodeCommandRevision(writer, value["revision"]);
    writer.writeUnsigned(value["inputs"].length);
    for (const item0 of value["inputs"]) {
        encodeCommandInput(writer, item0);
    }
    writer.writeBool(value["configInputs"]);
    writer.writeOption(value["cwd"], (value0) => {
        writer.writeString(value0);
    });
    writer.writeOption(value["manifest"], (value0) => {
        writer.writeString(value0);
    });
    writer.writeOption(value["target"], (value0) => {
        writer.writeString(value0);
    });
    writer.writeOption(value["targetOverrides"], (value0) => {
        encodeCommandTargetOverrides(writer, value0);
    });
    writer.writeOption(value["profile"], (value0) => {
        writer.writeString(value0);
    });
    writer.writeUnsigned(value["env"].length);
    for (const item0 of value["env"]) {
        encodeCommandEnvVar(writer, item0);
    }
    writer.writeUnsigned(value["overrides"].length);
    for (const item0 of value["overrides"]) {
        encodeManifestOverride(writer, item0);
    }
    writer.writeBool(value["watch"]);
    writer.writeBool(value["dryRun"]);
}

export function decodeSettingsInput(reader: Reader): SettingsInput {
    const field0 = decodeCommandRevision(reader);
    const field1 = (() => { const length1 = reader.readNumber(); const items1: Array<CommandInput> = []; for (let index = 0; index < length1; index += 1) { items1.push(decodeCommandInput(reader)); } return items1; })();
    const field2 = reader.readBool();
    const field3 = reader.readOption(() => reader.readString());
    const field4 = reader.readOption(() => reader.readString());
    const field5 = reader.readOption(() => reader.readString());
    const field6 = reader.readOption(() => decodeCommandTargetOverrides(reader));
    const field7 = reader.readOption(() => reader.readString());
    const field8 = (() => { const length8 = reader.readNumber(); const items8: Array<CommandEnvVar> = []; for (let index = 0; index < length8; index += 1) { items8.push(decodeCommandEnvVar(reader)); } return items8; })();
    const field9 = (() => { const length9 = reader.readNumber(); const items9: Array<ManifestOverride> = []; for (let index = 0; index < length9; index += 1) { items9.push(decodeManifestOverride(reader)); } return items9; })();
    const field10 = reader.readBool();
    const field11 = reader.readBool();

    return {
        "revision": field0,
        "inputs": field1,
        "configInputs": field2,
        ...(field3 === undefined ? {} : { "cwd": field3 }),
        ...(field4 === undefined ? {} : { "manifest": field4 }),
        ...(field5 === undefined ? {} : { "target": field5 }),
        ...(field6 === undefined ? {} : { "targetOverrides": field6 }),
        ...(field7 === undefined ? {} : { "profile": field7 }),
        "env": field8,
        "overrides": field9,
        "watch": field10,
        "dryRun": field11,
    };
}

/** Payload for settings command output. */
export type SettingsPayload = {
    /** Machine-local Destack home. */
    readonly home: string;
    /** Package directory. */
    readonly packages: string;
    /** Maximum package directory size in bytes before pruning is requested. */
    readonly packageMaximumBytes?: number;
    /** Workspace-local cache and session directory. */
    readonly workspaceCache: string;
    /** Maximum cache size in bytes before pruning is requested. */
    readonly cacheMaximumBytes?: number;
    /** Workspace-owned vendor directory. */
    readonly vendor: string;
    /** Default registry name. */
    readonly registry?: string;
    /** Known registries. */
    readonly registries: ReadonlyArray<SettingsRegistry>;
    /** Network settings for package and update commands. */
    readonly network: SettingsNetwork;
};

export function encodeSettingsPayload(writer: Writer, value: SettingsPayload): void {
    writer.writeString(value["home"]);
    writer.writeString(value["packages"]);
    writer.writeOption(value["packageMaximumBytes"], (value0) => {
        writer.writeUnsigned(value0);
    });
    writer.writeString(value["workspaceCache"]);
    writer.writeOption(value["cacheMaximumBytes"], (value0) => {
        writer.writeUnsigned(value0);
    });
    writer.writeString(value["vendor"]);
    writer.writeOption(value["registry"], (value0) => {
        writer.writeString(value0);
    });
    writer.writeUnsigned(value["registries"].length);
    for (const item0 of value["registries"]) {
        encodeSettingsRegistry(writer, item0);
    }
    encodeSettingsNetwork(writer, value["network"]);
}

export function decodeSettingsPayload(reader: Reader): SettingsPayload {
    const field0 = reader.readString();
    const field1 = reader.readString();
    const field2 = reader.readOption(() => reader.readNumber());
    const field3 = reader.readString();
    const field4 = reader.readOption(() => reader.readNumber());
    const field5 = reader.readString();
    const field6 = reader.readOption(() => reader.readString());
    const field7 = (() => { const length7 = reader.readNumber(); const items7: Array<SettingsRegistry> = []; for (let index = 0; index < length7; index += 1) { items7.push(decodeSettingsRegistry(reader)); } return items7; })();
    const field8 = decodeSettingsNetwork(reader);

    return {
        "home": field0,
        "packages": field1,
        ...(field2 === undefined ? {} : { "packageMaximumBytes": field2 }),
        "workspaceCache": field3,
        ...(field4 === undefined ? {} : { "cacheMaximumBytes": field4 }),
        "vendor": field5,
        ...(field6 === undefined ? {} : { "registry": field6 }),
        "registries": field7,
        "network": field8,
    };
}

/** Registry settings for command output. */
export type SettingsRegistry = {
    /** Registry name. */
    readonly name: string;
    /** Registry URL. */
    readonly url: string;
    /** Redacted authentication shape. */
    readonly authentication: SettingsRegistryAuthentication;
};

export function encodeSettingsRegistry(writer: Writer, value: SettingsRegistry): void {
    writer.writeString(value["name"]);
    writer.writeString(value["url"]);
    encodeSettingsRegistryAuthentication(writer, value["authentication"]);
}

export function decodeSettingsRegistry(reader: Reader): SettingsRegistry {
    const field0 = reader.readString();
    const field1 = reader.readString();
    const field2 = decodeSettingsRegistryAuthentication(reader);

    return {
        "name": field0,
        "url": field1,
        "authentication": field2,
    };
}

/** Redacted registry authentication shape. */
export type SettingsRegistryAuthentication =
    /** No registry authentication. */
    | {
          readonly kind: "none";
      }
    /** Inline token authentication. */
    | {
          readonly kind: "token";
      }
    /** Token read from one environment variable. */
    | {
          readonly kind: "tokenFromEnvironment";
          /** Environment variable name. */
          readonly variable: string;
      }
    /** Credentials printed by one process command. */
    | {
          readonly kind: "command";
          /** Program to run. */
          readonly program: string;
      }
;

export const SettingsRegistryAuthentication = {
    /** No registry authentication. */
    none(): SettingsRegistryAuthentication {
        return { kind: "none" };
    },

    /** Inline token authentication. */
    token(): SettingsRegistryAuthentication {
        return { kind: "token" };
    },

    /** Token read from one environment variable. */
    tokenFromEnvironment(variable: string): SettingsRegistryAuthentication {
        return { kind: "tokenFromEnvironment", variable };
    },

    /** Credentials printed by one process command. */
    command(program: string): SettingsRegistryAuthentication {
        return { kind: "command", program };
    },

};

export function encodeSettingsRegistryAuthentication(writer: Writer, value: SettingsRegistryAuthentication): void {
    switch (value.kind) {
        case "none":
            writer.writeUnsigned(0);
            return;
        case "token":
            writer.writeUnsigned(1);
            return;
        case "tokenFromEnvironment":
            writer.writeUnsigned(2);
            writer.writeString(value["variable"]);
            return;
        case "command":
            writer.writeUnsigned(3);
            writer.writeString(value["program"]);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

export function decodeSettingsRegistryAuthentication(reader: Reader): SettingsRegistryAuthentication {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            return { kind: "none" };
        }
        case 1: {
            return { kind: "token" };
        }
        case 2: {
            const field0 = reader.readString();

            return {
                kind: "tokenFromEnvironment",
                "variable": field0,
            };
        }
        case 3: {
            const field0 = reader.readString();

            return {
                kind: "command",
                "program": field0,
            };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Network settings for command output. */
export type SettingsNetwork = {
    /** Whether network access should be disabled by default. */
    readonly offline: boolean;
    /** Whether a proxy is configured. */
    readonly hasProxy: boolean;
    /** Request timeout in milliseconds. */
    readonly timeoutMilliseconds?: number;
    /** Number of retries for transient network failures. */
    readonly retryCount?: number;
    /** Maximum concurrent network requests. */
    readonly concurrency?: number;
};

export function encodeSettingsNetwork(writer: Writer, value: SettingsNetwork): void {
    writer.writeBool(value["offline"]);
    writer.writeBool(value["hasProxy"]);
    writer.writeOption(value["timeoutMilliseconds"], (value0) => {
        writer.writeUnsigned(value0);
    });
    writer.writeOption(value["retryCount"], (value0) => {
        writer.writeUnsigned(value0);
    });
    writer.writeOption(value["concurrency"], (value0) => {
        writer.writeUnsigned(value0);
    });
}

export function decodeSettingsNetwork(reader: Reader): SettingsNetwork {
    const field0 = reader.readBool();
    const field1 = reader.readBool();
    const field2 = reader.readOption(() => reader.readNumber());
    const field3 = reader.readOption(() => reader.readNumber());
    const field4 = reader.readOption(() => reader.readNumber());

    return {
        "offline": field0,
        "hasProxy": field1,
        ...(field2 === undefined ? {} : { "timeoutMilliseconds": field2 }),
        ...(field3 === undefined ? {} : { "retryCount": field3 }),
        ...(field4 === undefined ? {} : { "concurrency": field4 }),
    };
}
