/* generated by destack_bridge_generate, do not edit */

import { Reader, SerdeError, Writer, compareBytes, nestedBytes } from "../../../protocol/serde.js";

/** Reason for reloading host source state. */
export type ReloadReason = "manual" | "overflow" | "watch";

export function encodeReloadReason(writer: Writer, value: ReloadReason): void {
    switch (value) {
        case "manual":
            writer.writeUnsigned(0);
            return;
        case "overflow":
            writer.writeUnsigned(1);
            return;
        case "watch":
            writer.writeUnsigned(2);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

export function decodeReloadReason(reader: Reader): ReloadReason {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "manual";
        case 1:
            return "overflow";
        case 2:
            return "watch";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}
