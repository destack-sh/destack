/* generated by destack_bridge_generate, do not edit */

import { Reader, SerdeError, Writer, compareBytes, nestedBytes } from "../../../protocol/serde.js";
import type { FileUpdate } from "./file/image.js";
import { decodeFileUpdate, encodeFileUpdate } from "./file/image.js";

/** Message payload emitted by one workspace operation. */
export type Message = {
    /** Message severity. */
    readonly kind: MessageKind;
    /** Stable message code. */
    readonly code: string;
    /** Human-readable message. */
    readonly message: string;
};

export function encodeMessage(writer: Writer, value: Message): void {
    encodeMessageKind(writer, value["kind"]);
    writer.writeString(value["code"]);
    writer.writeString(value["message"]);
}

export function decodeMessage(reader: Reader): Message {
    const field0 = decodeMessageKind(reader);
    const field1 = reader.readString();
    const field2 = reader.readString();

    return {
        "kind": field0,
        "code": field1,
        "message": field2,
    };
}

/** Message severity for one workspace operation. */
export type MessageKind = "info" | "warning" | "error";

export function encodeMessageKind(writer: Writer, value: MessageKind): void {
    switch (value) {
        case "info":
            writer.writeUnsigned(0);
            return;
        case "warning":
            writer.writeUnsigned(1);
            return;
        case "error":
            writer.writeUnsigned(2);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

export function decodeMessageKind(reader: Reader): MessageKind {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "info";
        case 1:
            return "warning";
        case 2:
            return "error";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Result of applying local workspace updates. */
export type UpdateBatch = {
    /** Update records produced by the operation. */
    readonly updates: ReadonlyArray<FileUpdate>;
    /** Message records produced by the operation. */
    readonly messages: ReadonlyArray<Message>;
};

export function encodeUpdateBatch(writer: Writer, value: UpdateBatch): void {
    writer.writeUnsigned(value["updates"].length);
    for (const item0 of value["updates"]) {
        encodeFileUpdate(writer, item0);
    }
    writer.writeUnsigned(value["messages"].length);
    for (const item0 of value["messages"]) {
        encodeMessage(writer, item0);
    }
}

export function decodeUpdateBatch(reader: Reader): UpdateBatch {
    const field0 = (() => { const length0 = reader.readNumber(); const items0: Array<FileUpdate> = []; for (let index = 0; index < length0; index += 1) { items0.push(decodeFileUpdate(reader)); } return items0; })();
    const field1 = (() => { const length1 = reader.readNumber(); const items1: Array<Message> = []; for (let index = 0; index < length1; index += 1) { items1.push(decodeMessage(reader)); } return items1; })();

    return {
        "updates": field0,
        "messages": field1,
    };
}
