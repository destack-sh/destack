/* generated by destack_bridge_generate, do not edit */

import { Reader, SerdeError, Writer, compareBytes, nestedBytes } from "../../protocol/serde.js";
import type { ArtifactReference } from "./artifact/reference.js";
import type { BenchInput } from "./workspace/command/bench.js";
import type { BuildInput } from "./workspace/command/build.js";
import type { CacheInput } from "./workspace/command/cache.js";
import type { CheckInput } from "./workspace/command/check.js";
import type { CleanInput } from "./workspace/command/clean.js";
import type { CloseRootRequest } from "./root.js";
import type { Content } from "./source/file/model/file.js";
import type { ContentId } from "./source/file/model/file.js";
import type { DocInput } from "./workspace/command/doc.js";
import type { DoctorInput } from "./workspace/command/doctor.js";
import type { ExportRequest } from "./workspace/artifact/export.js";
import type { FileOperationRequest } from "./root.js";
import type { FormatInput } from "./workspace/command/format.js";
import type { HandshakeRequest } from "./handshake.js";
import type { InfoInput } from "./workspace/command/info.js";
import type { LintInput } from "./workspace/command/check.js";
import type { OpenRootRequest } from "./root.js";
import type { ReloadRootRequest } from "./root.js";
import type { RequestId } from "./envelope.js";
import type { RootId } from "./root.js";
import type { RunInput } from "./workspace/command/run.js";
import type { SettingsInput } from "./workspace/command/settings.js";
import type { SourceUpdateRequest } from "./root.js";
import type { TargetsInput } from "./workspace/command/targets.js";
import type { TaskInput } from "./workspace/command/task.js";
import type { TestInput } from "./workspace/command/test.js";
import type { WatchNextRequest } from "./watch.js";
import type { WatchStartRequest } from "./watch.js";
import type { WatchStopRequest } from "./watch.js";
import type { WorkspaceQuery } from "./query.js";
import { decodeArtifactReference, encodeArtifactReference } from "./artifact/reference.js";
import { decodeBenchInput, encodeBenchInput } from "./workspace/command/bench.js";
import { decodeBuildInput, encodeBuildInput } from "./workspace/command/build.js";
import { decodeCacheInput, encodeCacheInput } from "./workspace/command/cache.js";
import { decodeCheckInput, encodeCheckInput } from "./workspace/command/check.js";
import { decodeCleanInput, encodeCleanInput } from "./workspace/command/clean.js";
import { decodeCloseRootRequest, encodeCloseRootRequest } from "./root.js";
import { decodeContent, encodeContent } from "./source/file/model/file.js";
import { decodeContentId, encodeContentId } from "./source/file/model/file.js";
import { decodeDocInput, encodeDocInput } from "./workspace/command/doc.js";
import { decodeDoctorInput, encodeDoctorInput } from "./workspace/command/doctor.js";
import { decodeExportRequest, encodeExportRequest } from "./workspace/artifact/export.js";
import { decodeFileOperationRequest, encodeFileOperationRequest } from "./root.js";
import { decodeFormatInput, encodeFormatInput } from "./workspace/command/format.js";
import { decodeHandshakeRequest, encodeHandshakeRequest } from "./handshake.js";
import { decodeInfoInput, encodeInfoInput } from "./workspace/command/info.js";
import { decodeLintInput, encodeLintInput } from "./workspace/command/check.js";
import { decodeOpenRootRequest, encodeOpenRootRequest } from "./root.js";
import { decodeReloadRootRequest, encodeReloadRootRequest } from "./root.js";
import { decodeRequestId, encodeRequestId } from "./envelope.js";
import { decodeRootId, encodeRootId } from "./root.js";
import { decodeRunInput, encodeRunInput } from "./workspace/command/run.js";
import { decodeSettingsInput, encodeSettingsInput } from "./workspace/command/settings.js";
import { decodeSourceUpdateRequest, encodeSourceUpdateRequest } from "./root.js";
import { decodeTargetsInput, encodeTargetsInput } from "./workspace/command/targets.js";
import { decodeTaskInput, encodeTaskInput } from "./workspace/command/task.js";
import { decodeTestInput, encodeTestInput } from "./workspace/command/test.js";
import { decodeWatchNextRequest, encodeWatchNextRequest } from "./watch.js";
import { decodeWatchStartRequest, encodeWatchStartRequest } from "./watch.js";
import { decodeWatchStopRequest, encodeWatchStopRequest } from "./watch.js";
import { decodeWorkspaceQuery, encodeWorkspaceQuery } from "./query.js";

/** Requests accepted by the workspace protocol. */
export type WorkspaceRequest =
    /** Negotiate protocol version and capabilities. */
    | {
          readonly kind: "handshake";
          readonly handshake: HandshakeRequest;
      }
    /** Check server liveness. */
    | {
          readonly kind: "ping";
      }
    /** Cancel an in flight request. */
    | {
          readonly kind: "cancel";
          readonly id: RequestId;
      }
    /** Request server shutdown. */
    | {
          readonly kind: "shutdown";
      }
    /** Open or register a root. */
    | {
          readonly kind: "openRoot";
          readonly open_root: OpenRootRequest;
      }
    /** Close a root handle. */
    | {
          readonly kind: "closeRoot";
          readonly close_root: CloseRootRequest;
      }
    /** Reload a root. */
    | {
          readonly kind: "reloadRoot";
          readonly reload_root: ReloadRootRequest;
      }
    /** Apply a file operation to a root. */
    | {
          readonly kind: "applyFileOperation";
          readonly apply_file_operation: FileOperationRequest;
      }
    /** Apply a source update to a root. */
    | {
          readonly kind: "applySourceUpdate";
          readonly apply_source_update: SourceUpdateRequest;
      }
    /** Start watching a root. */
    | {
          readonly kind: "startWatch";
          readonly start_watch: WatchStartRequest;
      }
    /** Receive and apply the next watch batch. */
    | {
          readonly kind: "nextWatchBatch";
          readonly next_watch_batch: WatchNextRequest;
      }
    /** Stop watching a root. */
    | {
          readonly kind: "stopWatch";
          readonly stop_watch: WatchStopRequest;
      }
    /** Check source state. */
    | {
          readonly kind: "check";
          /** Root handle. */
          readonly handle: RootId;
          /** Check input. */
          readonly input: CheckInput;
      }
    /** Lint source state. */
    | {
          readonly kind: "lint";
          /** Root handle. */
          readonly handle: RootId;
          /** Lint input. */
          readonly input: LintInput;
      }
    /** Format source files or content. */
    | {
          readonly kind: "format";
          /** Root handle. */
          readonly handle: RootId;
          /** Format input. */
          readonly input: FormatInput;
      }
    /** Build target artifacts. */
    | {
          readonly kind: "build";
          /** Root handle. */
          readonly handle: RootId;
          /** Build input. */
          readonly input: BuildInput;
      }
    /** Run a workspace target. */
    | {
          readonly kind: "run";
          /** Root handle. */
          readonly handle: RootId;
          /** Run input. */
          readonly input: RunInput;
      }
    /** Run workspace tests. */
    | {
          readonly kind: "test";
          /** Root handle. */
          readonly handle: RootId;
          /** Test input. */
          readonly input: TestInput;
      }
    /** Generate documentation. */
    | {
          readonly kind: "doc";
          /** Root handle. */
          readonly handle: RootId;
          /** Documentation input. */
          readonly input: DocInput;
      }
    /** Run benchmarks. */
    | {
          readonly kind: "bench";
          /** Root handle. */
          readonly handle: RootId;
          /** Benchmark input. */
          readonly input: BenchInput;
      }
    /** Return workspace information. */
    | {
          readonly kind: "info";
          /** Root handle. */
          readonly handle: RootId;
          /** Information input. */
          readonly input: InfoInput;
      }
    /** Return configured targets. */
    | {
          readonly kind: "targets";
          /** Root handle. */
          readonly handle: RootId;
          /** Targets input. */
          readonly input: TargetsInput;
      }
    /** Return cache locations. */
    | {
          readonly kind: "cache";
          /** Root handle. */
          readonly handle: RootId;
          /** Cache input. */
          readonly input: CacheInput;
      }
    /** Return resolved settings. */
    | {
          readonly kind: "settings";
          /** Root handle. */
          readonly handle: RootId;
          /** Settings input. */
          readonly input: SettingsInput;
      }
    /** Return workspace health information. */
    | {
          readonly kind: "doctor";
          /** Root handle. */
          readonly handle: RootId;
          /** Doctor input. */
          readonly input: DoctorInput;
      }
    /** Run workspace tasks. */
    | {
          readonly kind: "task";
          /** Root handle. */
          readonly handle: RootId;
          /** Task input. */
          readonly input: TaskInput;
      }
    /** Clean generated state. */
    | {
          readonly kind: "clean";
          /** Root handle. */
          readonly handle: RootId;
          /** Clean input. */
          readonly input: CleanInput;
      }
    /** Return one artifact payload. */
    | {
          readonly kind: "artifact";
          /** Root handle. */
          readonly handle: RootId;
          /** Artifact reference. */
          readonly artifact: ArtifactReference;
      }
    /** Store one content payload. */
    | {
          readonly kind: "store";
          /** Root handle. */
          readonly handle: RootId;
          /** Content payload. */
          readonly content: Content;
      }
    /** Load one content payload. */
    | {
          readonly kind: "load";
          /** Root handle. */
          readonly handle: RootId;
          /** Content id. */
          readonly content: ContentId;
      }
    /** Materialize derived outputs. */
    | {
          readonly kind: "export";
          /** Root handle. */
          readonly handle: RootId;
          /** Export request. */
          readonly request: ExportRequest;
      }
    /** Execute a query. */
    | {
          readonly kind: "query";
          readonly query: WorkspaceQuery;
      }
;

export const WorkspaceRequest = {
    /** Negotiate protocol version and capabilities. */
    handshake(handshake: HandshakeRequest): WorkspaceRequest {
        return { kind: "handshake", handshake };
    },

    /** Check server liveness. */
    ping(): WorkspaceRequest {
        return { kind: "ping" };
    },

    /** Cancel an in flight request. */
    cancel(id: RequestId): WorkspaceRequest {
        return { kind: "cancel", id };
    },

    /** Request server shutdown. */
    shutdown(): WorkspaceRequest {
        return { kind: "shutdown" };
    },

    /** Open or register a root. */
    openRoot(open_root: OpenRootRequest): WorkspaceRequest {
        return { kind: "openRoot", open_root };
    },

    /** Close a root handle. */
    closeRoot(close_root: CloseRootRequest): WorkspaceRequest {
        return { kind: "closeRoot", close_root };
    },

    /** Reload a root. */
    reloadRoot(reload_root: ReloadRootRequest): WorkspaceRequest {
        return { kind: "reloadRoot", reload_root };
    },

    /** Apply a file operation to a root. */
    applyFileOperation(apply_file_operation: FileOperationRequest): WorkspaceRequest {
        return { kind: "applyFileOperation", apply_file_operation };
    },

    /** Apply a source update to a root. */
    applySourceUpdate(apply_source_update: SourceUpdateRequest): WorkspaceRequest {
        return { kind: "applySourceUpdate", apply_source_update };
    },

    /** Start watching a root. */
    startWatch(start_watch: WatchStartRequest): WorkspaceRequest {
        return { kind: "startWatch", start_watch };
    },

    /** Receive and apply the next watch batch. */
    nextWatchBatch(next_watch_batch: WatchNextRequest): WorkspaceRequest {
        return { kind: "nextWatchBatch", next_watch_batch };
    },

    /** Stop watching a root. */
    stopWatch(stop_watch: WatchStopRequest): WorkspaceRequest {
        return { kind: "stopWatch", stop_watch };
    },

    /** Check source state. */
    check(handle: RootId, input: CheckInput): WorkspaceRequest {
        return { kind: "check", handle, input };
    },

    /** Lint source state. */
    lint(handle: RootId, input: LintInput): WorkspaceRequest {
        return { kind: "lint", handle, input };
    },

    /** Format source files or content. */
    format(handle: RootId, input: FormatInput): WorkspaceRequest {
        return { kind: "format", handle, input };
    },

    /** Build target artifacts. */
    build(handle: RootId, input: BuildInput): WorkspaceRequest {
        return { kind: "build", handle, input };
    },

    /** Run a workspace target. */
    run(handle: RootId, input: RunInput): WorkspaceRequest {
        return { kind: "run", handle, input };
    },

    /** Run workspace tests. */
    test(handle: RootId, input: TestInput): WorkspaceRequest {
        return { kind: "test", handle, input };
    },

    /** Generate documentation. */
    doc(handle: RootId, input: DocInput): WorkspaceRequest {
        return { kind: "doc", handle, input };
    },

    /** Run benchmarks. */
    bench(handle: RootId, input: BenchInput): WorkspaceRequest {
        return { kind: "bench", handle, input };
    },

    /** Return workspace information. */
    info(handle: RootId, input: InfoInput): WorkspaceRequest {
        return { kind: "info", handle, input };
    },

    /** Return configured targets. */
    targets(handle: RootId, input: TargetsInput): WorkspaceRequest {
        return { kind: "targets", handle, input };
    },

    /** Return cache locations. */
    cache(handle: RootId, input: CacheInput): WorkspaceRequest {
        return { kind: "cache", handle, input };
    },

    /** Return resolved settings. */
    settings(handle: RootId, input: SettingsInput): WorkspaceRequest {
        return { kind: "settings", handle, input };
    },

    /** Return workspace health information. */
    doctor(handle: RootId, input: DoctorInput): WorkspaceRequest {
        return { kind: "doctor", handle, input };
    },

    /** Run workspace tasks. */
    task(handle: RootId, input: TaskInput): WorkspaceRequest {
        return { kind: "task", handle, input };
    },

    /** Clean generated state. */
    clean(handle: RootId, input: CleanInput): WorkspaceRequest {
        return { kind: "clean", handle, input };
    },

    /** Return one artifact payload. */
    artifact(handle: RootId, artifact: ArtifactReference): WorkspaceRequest {
        return { kind: "artifact", handle, artifact };
    },

    /** Store one content payload. */
    store(handle: RootId, content: Content): WorkspaceRequest {
        return { kind: "store", handle, content };
    },

    /** Load one content payload. */
    load(handle: RootId, content: ContentId): WorkspaceRequest {
        return { kind: "load", handle, content };
    },

    /** Materialize derived outputs. */
    export(handle: RootId, request: ExportRequest): WorkspaceRequest {
        return { kind: "export", handle, request };
    },

    /** Execute a query. */
    query(query: WorkspaceQuery): WorkspaceRequest {
        return { kind: "query", query };
    },

};

export function encodeWorkspaceRequest(writer: Writer, value: WorkspaceRequest): void {
    switch (value.kind) {
        case "handshake":
            writer.writeUnsigned(0);
            encodeHandshakeRequest(writer, value["handshake"]);
            return;
        case "ping":
            writer.writeUnsigned(1);
            return;
        case "cancel":
            writer.writeUnsigned(2);
            encodeRequestId(writer, value["id"]);
            return;
        case "shutdown":
            writer.writeUnsigned(3);
            return;
        case "openRoot":
            writer.writeUnsigned(4);
            encodeOpenRootRequest(writer, value["open_root"]);
            return;
        case "closeRoot":
            writer.writeUnsigned(5);
            encodeCloseRootRequest(writer, value["close_root"]);
            return;
        case "reloadRoot":
            writer.writeUnsigned(6);
            encodeReloadRootRequest(writer, value["reload_root"]);
            return;
        case "applyFileOperation":
            writer.writeUnsigned(7);
            encodeFileOperationRequest(writer, value["apply_file_operation"]);
            return;
        case "applySourceUpdate":
            writer.writeUnsigned(8);
            encodeSourceUpdateRequest(writer, value["apply_source_update"]);
            return;
        case "startWatch":
            writer.writeUnsigned(9);
            encodeWatchStartRequest(writer, value["start_watch"]);
            return;
        case "nextWatchBatch":
            writer.writeUnsigned(10);
            encodeWatchNextRequest(writer, value["next_watch_batch"]);
            return;
        case "stopWatch":
            writer.writeUnsigned(11);
            encodeWatchStopRequest(writer, value["stop_watch"]);
            return;
        case "check":
            writer.writeUnsigned(12);
            encodeRootId(writer, value["handle"]);
            encodeCheckInput(writer, value["input"]);
            return;
        case "lint":
            writer.writeUnsigned(13);
            encodeRootId(writer, value["handle"]);
            encodeLintInput(writer, value["input"]);
            return;
        case "format":
            writer.writeUnsigned(14);
            encodeRootId(writer, value["handle"]);
            encodeFormatInput(writer, value["input"]);
            return;
        case "build":
            writer.writeUnsigned(15);
            encodeRootId(writer, value["handle"]);
            encodeBuildInput(writer, value["input"]);
            return;
        case "run":
            writer.writeUnsigned(16);
            encodeRootId(writer, value["handle"]);
            encodeRunInput(writer, value["input"]);
            return;
        case "test":
            writer.writeUnsigned(17);
            encodeRootId(writer, value["handle"]);
            encodeTestInput(writer, value["input"]);
            return;
        case "doc":
            writer.writeUnsigned(18);
            encodeRootId(writer, value["handle"]);
            encodeDocInput(writer, value["input"]);
            return;
        case "bench":
            writer.writeUnsigned(19);
            encodeRootId(writer, value["handle"]);
            encodeBenchInput(writer, value["input"]);
            return;
        case "info":
            writer.writeUnsigned(20);
            encodeRootId(writer, value["handle"]);
            encodeInfoInput(writer, value["input"]);
            return;
        case "targets":
            writer.writeUnsigned(21);
            encodeRootId(writer, value["handle"]);
            encodeTargetsInput(writer, value["input"]);
            return;
        case "cache":
            writer.writeUnsigned(22);
            encodeRootId(writer, value["handle"]);
            encodeCacheInput(writer, value["input"]);
            return;
        case "settings":
            writer.writeUnsigned(23);
            encodeRootId(writer, value["handle"]);
            encodeSettingsInput(writer, value["input"]);
            return;
        case "doctor":
            writer.writeUnsigned(24);
            encodeRootId(writer, value["handle"]);
            encodeDoctorInput(writer, value["input"]);
            return;
        case "task":
            writer.writeUnsigned(25);
            encodeRootId(writer, value["handle"]);
            encodeTaskInput(writer, value["input"]);
            return;
        case "clean":
            writer.writeUnsigned(26);
            encodeRootId(writer, value["handle"]);
            encodeCleanInput(writer, value["input"]);
            return;
        case "artifact":
            writer.writeUnsigned(27);
            encodeRootId(writer, value["handle"]);
            encodeArtifactReference(writer, value["artifact"]);
            return;
        case "store":
            writer.writeUnsigned(28);
            encodeRootId(writer, value["handle"]);
            encodeContent(writer, value["content"]);
            return;
        case "load":
            writer.writeUnsigned(29);
            encodeRootId(writer, value["handle"]);
            encodeContentId(writer, value["content"]);
            return;
        case "export":
            writer.writeUnsigned(30);
            encodeRootId(writer, value["handle"]);
            encodeExportRequest(writer, value["request"]);
            return;
        case "query":
            writer.writeUnsigned(31);
            encodeWorkspaceQuery(writer, value["query"]);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

export function decodeWorkspaceRequest(reader: Reader): WorkspaceRequest {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            return { kind: "handshake", "handshake": decodeHandshakeRequest(reader) };
        }
        case 1: {
            return { kind: "ping" };
        }
        case 2: {
            const field0 = decodeRequestId(reader);

            return {
                kind: "cancel",
                "id": field0,
            };
        }
        case 3: {
            return { kind: "shutdown" };
        }
        case 4: {
            return { kind: "openRoot", "open_root": decodeOpenRootRequest(reader) };
        }
        case 5: {
            return { kind: "closeRoot", "close_root": decodeCloseRootRequest(reader) };
        }
        case 6: {
            return { kind: "reloadRoot", "reload_root": decodeReloadRootRequest(reader) };
        }
        case 7: {
            return { kind: "applyFileOperation", "apply_file_operation": decodeFileOperationRequest(reader) };
        }
        case 8: {
            return { kind: "applySourceUpdate", "apply_source_update": decodeSourceUpdateRequest(reader) };
        }
        case 9: {
            return { kind: "startWatch", "start_watch": decodeWatchStartRequest(reader) };
        }
        case 10: {
            return { kind: "nextWatchBatch", "next_watch_batch": decodeWatchNextRequest(reader) };
        }
        case 11: {
            return { kind: "stopWatch", "stop_watch": decodeWatchStopRequest(reader) };
        }
        case 12: {
            const field0 = decodeRootId(reader);
            const field1 = decodeCheckInput(reader);

            return {
                kind: "check",
                "handle": field0,
                "input": field1,
            };
        }
        case 13: {
            const field0 = decodeRootId(reader);
            const field1 = decodeLintInput(reader);

            return {
                kind: "lint",
                "handle": field0,
                "input": field1,
            };
        }
        case 14: {
            const field0 = decodeRootId(reader);
            const field1 = decodeFormatInput(reader);

            return {
                kind: "format",
                "handle": field0,
                "input": field1,
            };
        }
        case 15: {
            const field0 = decodeRootId(reader);
            const field1 = decodeBuildInput(reader);

            return {
                kind: "build",
                "handle": field0,
                "input": field1,
            };
        }
        case 16: {
            const field0 = decodeRootId(reader);
            const field1 = decodeRunInput(reader);

            return {
                kind: "run",
                "handle": field0,
                "input": field1,
            };
        }
        case 17: {
            const field0 = decodeRootId(reader);
            const field1 = decodeTestInput(reader);

            return {
                kind: "test",
                "handle": field0,
                "input": field1,
            };
        }
        case 18: {
            const field0 = decodeRootId(reader);
            const field1 = decodeDocInput(reader);

            return {
                kind: "doc",
                "handle": field0,
                "input": field1,
            };
        }
        case 19: {
            const field0 = decodeRootId(reader);
            const field1 = decodeBenchInput(reader);

            return {
                kind: "bench",
                "handle": field0,
                "input": field1,
            };
        }
        case 20: {
            const field0 = decodeRootId(reader);
            const field1 = decodeInfoInput(reader);

            return {
                kind: "info",
                "handle": field0,
                "input": field1,
            };
        }
        case 21: {
            const field0 = decodeRootId(reader);
            const field1 = decodeTargetsInput(reader);

            return {
                kind: "targets",
                "handle": field0,
                "input": field1,
            };
        }
        case 22: {
            const field0 = decodeRootId(reader);
            const field1 = decodeCacheInput(reader);

            return {
                kind: "cache",
                "handle": field0,
                "input": field1,
            };
        }
        case 23: {
            const field0 = decodeRootId(reader);
            const field1 = decodeSettingsInput(reader);

            return {
                kind: "settings",
                "handle": field0,
                "input": field1,
            };
        }
        case 24: {
            const field0 = decodeRootId(reader);
            const field1 = decodeDoctorInput(reader);

            return {
                kind: "doctor",
                "handle": field0,
                "input": field1,
            };
        }
        case 25: {
            const field0 = decodeRootId(reader);
            const field1 = decodeTaskInput(reader);

            return {
                kind: "task",
                "handle": field0,
                "input": field1,
            };
        }
        case 26: {
            const field0 = decodeRootId(reader);
            const field1 = decodeCleanInput(reader);

            return {
                kind: "clean",
                "handle": field0,
                "input": field1,
            };
        }
        case 27: {
            const field0 = decodeRootId(reader);
            const field1 = decodeArtifactReference(reader);

            return {
                kind: "artifact",
                "handle": field0,
                "artifact": field1,
            };
        }
        case 28: {
            const field0 = decodeRootId(reader);
            const field1 = decodeContent(reader);

            return {
                kind: "store",
                "handle": field0,
                "content": field1,
            };
        }
        case 29: {
            const field0 = decodeRootId(reader);
            const field1 = decodeContentId(reader);

            return {
                kind: "load",
                "handle": field0,
                "content": field1,
            };
        }
        case 30: {
            const field0 = decodeRootId(reader);
            const field1 = decodeExportRequest(reader);

            return {
                kind: "export",
                "handle": field0,
                "request": field1,
            };
        }
        case 31: {
            return { kind: "query", "query": decodeWorkspaceQuery(reader) };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}
