/* generated by destack_bridge_generate, do not edit */

import { Reader, SerdeError, Writer, compareBytes, nestedBytes } from "../../protocol/serde.js";
import type { ArtifactVersion } from "./artifact/core/version.js";
import type { BenchOutput } from "./workspace/command/output.js";
import type { BuildOutput } from "./workspace/command/output.js";
import type { CacheOutput } from "./workspace/command/output.js";
import type { CheckOutput } from "./workspace/command/output.js";
import type { CleanOutput } from "./workspace/command/output.js";
import type { Content } from "./source/file/model/file.js";
import type { ContentId } from "./source/file/model/file.js";
import type { DocOutput } from "./workspace/command/output.js";
import type { DoctorOutput } from "./workspace/command/output.js";
import type { ExportResult } from "./workspace/artifact/export.js";
import type { FileOperationResponse } from "./root.js";
import type { FormatOutput } from "./workspace/command/output.js";
import type { HandshakeResponse } from "./handshake.js";
import type { InfoOutput } from "./workspace/command/output.js";
import type { LintOutput } from "./workspace/command/output.js";
import type { ProtocolError } from "./error.js";
import type { RequestId } from "./envelope.js";
import type { RootClosedResponse } from "./root.js";
import type { RootOpenedResponse } from "./root.js";
import type { RootReloadResponse } from "./root.js";
import type { RunOutput } from "./workspace/command/output.js";
import type { SettingsOutput } from "./workspace/command/output.js";
import type { SourceUpdateResponse } from "./root.js";
import type { TargetsOutput } from "./workspace/command/output.js";
import type { TaskOutput } from "./workspace/command/output.js";
import type { TestOutput } from "./workspace/command/output.js";
import type { WatchBatchResponse } from "./watch.js";
import type { WatchStartedResponse } from "./watch.js";
import type { WatchStoppedResponse } from "./watch.js";
import type { WorkspaceQueryResponse } from "./query.js";
import { decodeArtifactVersion, encodeArtifactVersion } from "./artifact/core/version.js";
import { decodeBenchOutput, encodeBenchOutput } from "./workspace/command/output.js";
import { decodeBuildOutput, encodeBuildOutput } from "./workspace/command/output.js";
import { decodeCacheOutput, encodeCacheOutput } from "./workspace/command/output.js";
import { decodeCheckOutput, encodeCheckOutput } from "./workspace/command/output.js";
import { decodeCleanOutput, encodeCleanOutput } from "./workspace/command/output.js";
import { decodeContent, encodeContent } from "./source/file/model/file.js";
import { decodeContentId, encodeContentId } from "./source/file/model/file.js";
import { decodeDocOutput, encodeDocOutput } from "./workspace/command/output.js";
import { decodeDoctorOutput, encodeDoctorOutput } from "./workspace/command/output.js";
import { decodeExportResult, encodeExportResult } from "./workspace/artifact/export.js";
import { decodeFileOperationResponse, encodeFileOperationResponse } from "./root.js";
import { decodeFormatOutput, encodeFormatOutput } from "./workspace/command/output.js";
import { decodeHandshakeResponse, encodeHandshakeResponse } from "./handshake.js";
import { decodeInfoOutput, encodeInfoOutput } from "./workspace/command/output.js";
import { decodeLintOutput, encodeLintOutput } from "./workspace/command/output.js";
import { decodeProtocolError, encodeProtocolError } from "./error.js";
import { decodeRequestId, encodeRequestId } from "./envelope.js";
import { decodeRootClosedResponse, encodeRootClosedResponse } from "./root.js";
import { decodeRootOpenedResponse, encodeRootOpenedResponse } from "./root.js";
import { decodeRootReloadResponse, encodeRootReloadResponse } from "./root.js";
import { decodeRunOutput, encodeRunOutput } from "./workspace/command/output.js";
import { decodeSettingsOutput, encodeSettingsOutput } from "./workspace/command/output.js";
import { decodeSourceUpdateResponse, encodeSourceUpdateResponse } from "./root.js";
import { decodeTargetsOutput, encodeTargetsOutput } from "./workspace/command/output.js";
import { decodeTaskOutput, encodeTaskOutput } from "./workspace/command/output.js";
import { decodeTestOutput, encodeTestOutput } from "./workspace/command/output.js";
import { decodeWatchBatchResponse, encodeWatchBatchResponse } from "./watch.js";
import { decodeWatchStartedResponse, encodeWatchStartedResponse } from "./watch.js";
import { decodeWatchStoppedResponse, encodeWatchStoppedResponse } from "./watch.js";
import { decodeWorkspaceQueryResponse, encodeWorkspaceQueryResponse } from "./query.js";

/** Responses emitted by the workspace protocol. */
export type WorkspaceResponse =
    /** Successful handshake response. */
    | {
          readonly kind: "handshake";
          readonly handshake: HandshakeResponse;
      }
    /** Response to ping. */
    | {
          readonly kind: "pong";
      }
    /** Response to request cancellation. */
    | {
          readonly kind: "canceled";
          readonly id: RequestId;
      }
    /** Response to shutdown request. */
    | {
          readonly kind: "shutdownAck";
      }
    /** Root open response. */
    | {
          readonly kind: "rootOpened";
          readonly root_opened: RootOpenedResponse;
      }
    /** Root close response. */
    | {
          readonly kind: "rootClosed";
          readonly root_closed: RootClosedResponse;
      }
    /** Root reload response. */
    | {
          readonly kind: "rootReloaded";
          readonly root_reloaded: RootReloadResponse;
      }
    /** File operation response. */
    | {
          readonly kind: "fileOperationApplied";
          readonly file_operation_applied: FileOperationResponse;
      }
    /** Source update response. */
    | {
          readonly kind: "sourceUpdated";
          readonly source_updated: SourceUpdateResponse;
      }
    /** Watch start response. */
    | {
          readonly kind: "watchStarted";
          readonly watch_started: WatchStartedResponse;
      }
    /** Watch batch response. */
    | {
          readonly kind: "watchBatchReady";
          readonly watch_batch_ready: WatchBatchResponse;
      }
    /** Watch stop response. */
    | {
          readonly kind: "watchStopped";
          readonly watch_stopped: WatchStoppedResponse;
      }
    /** Check response. */
    | {
          readonly kind: "check";
          readonly check: CheckOutput;
      }
    /** Lint response. */
    | {
          readonly kind: "lint";
          readonly lint: LintOutput;
      }
    /** Format response. */
    | {
          readonly kind: "format";
          readonly format: FormatOutput;
      }
    /** Build response. */
    | {
          readonly kind: "build";
          readonly build: BuildOutput;
      }
    /** Run response. */
    | {
          readonly kind: "run";
          readonly run: RunOutput;
      }
    /** Test response. */
    | {
          readonly kind: "test";
          readonly test: TestOutput;
      }
    /** Documentation response. */
    | {
          readonly kind: "doc";
          readonly doc: DocOutput;
      }
    /** Benchmark response. */
    | {
          readonly kind: "bench";
          readonly bench: BenchOutput;
      }
    /** Information response. */
    | {
          readonly kind: "info";
          readonly info: InfoOutput;
      }
    /** Targets response. */
    | {
          readonly kind: "targets";
          readonly targets: TargetsOutput;
      }
    /** Cache response. */
    | {
          readonly kind: "cache";
          readonly cache: CacheOutput;
      }
    /** Settings response. */
    | {
          readonly kind: "settings";
          readonly settings: SettingsOutput;
      }
    /** Doctor response. */
    | {
          readonly kind: "doctor";
          readonly doctor: DoctorOutput;
      }
    /** Task response. */
    | {
          readonly kind: "task";
          readonly task: TaskOutput;
      }
    /** Clean response. */
    | {
          readonly kind: "clean";
          readonly clean: CleanOutput;
      }
    /** Artifact blob response. */
    | {
          readonly kind: "artifactResult";
          readonly artifact_result: ArtifactBlob;
      }
    /** Store response. */
    | {
          readonly kind: "storeResult";
          readonly store_result: ContentId;
      }
    /** Content payload response. */
    | {
          readonly kind: "loadResult";
          readonly load_result: Content;
      }
    /** Export response. */
    | {
          readonly kind: "exportResult";
          readonly export_result: ExportResult;
      }
    /** Query response. */
    | {
          readonly kind: "queryResult";
          readonly query_result: WorkspaceQueryResponse;
      }
    /** Error response. */
    | {
          readonly kind: "error";
          readonly error: ProtocolError;
      }
;

export const WorkspaceResponse = {
    /** Successful handshake response. */
    handshake(handshake: HandshakeResponse): WorkspaceResponse {
        return { kind: "handshake", handshake };
    },

    /** Response to ping. */
    pong(): WorkspaceResponse {
        return { kind: "pong" };
    },

    /** Response to request cancellation. */
    canceled(id: RequestId): WorkspaceResponse {
        return { kind: "canceled", id };
    },

    /** Response to shutdown request. */
    shutdownAck(): WorkspaceResponse {
        return { kind: "shutdownAck" };
    },

    /** Root open response. */
    rootOpened(root_opened: RootOpenedResponse): WorkspaceResponse {
        return { kind: "rootOpened", root_opened };
    },

    /** Root close response. */
    rootClosed(root_closed: RootClosedResponse): WorkspaceResponse {
        return { kind: "rootClosed", root_closed };
    },

    /** Root reload response. */
    rootReloaded(root_reloaded: RootReloadResponse): WorkspaceResponse {
        return { kind: "rootReloaded", root_reloaded };
    },

    /** File operation response. */
    fileOperationApplied(file_operation_applied: FileOperationResponse): WorkspaceResponse {
        return { kind: "fileOperationApplied", file_operation_applied };
    },

    /** Source update response. */
    sourceUpdated(source_updated: SourceUpdateResponse): WorkspaceResponse {
        return { kind: "sourceUpdated", source_updated };
    },

    /** Watch start response. */
    watchStarted(watch_started: WatchStartedResponse): WorkspaceResponse {
        return { kind: "watchStarted", watch_started };
    },

    /** Watch batch response. */
    watchBatchReady(watch_batch_ready: WatchBatchResponse): WorkspaceResponse {
        return { kind: "watchBatchReady", watch_batch_ready };
    },

    /** Watch stop response. */
    watchStopped(watch_stopped: WatchStoppedResponse): WorkspaceResponse {
        return { kind: "watchStopped", watch_stopped };
    },

    /** Check response. */
    check(check: CheckOutput): WorkspaceResponse {
        return { kind: "check", check };
    },

    /** Lint response. */
    lint(lint: LintOutput): WorkspaceResponse {
        return { kind: "lint", lint };
    },

    /** Format response. */
    format(format: FormatOutput): WorkspaceResponse {
        return { kind: "format", format };
    },

    /** Build response. */
    build(build: BuildOutput): WorkspaceResponse {
        return { kind: "build", build };
    },

    /** Run response. */
    run(run: RunOutput): WorkspaceResponse {
        return { kind: "run", run };
    },

    /** Test response. */
    test(test: TestOutput): WorkspaceResponse {
        return { kind: "test", test };
    },

    /** Documentation response. */
    doc(doc: DocOutput): WorkspaceResponse {
        return { kind: "doc", doc };
    },

    /** Benchmark response. */
    bench(bench: BenchOutput): WorkspaceResponse {
        return { kind: "bench", bench };
    },

    /** Information response. */
    info(info: InfoOutput): WorkspaceResponse {
        return { kind: "info", info };
    },

    /** Targets response. */
    targets(targets: TargetsOutput): WorkspaceResponse {
        return { kind: "targets", targets };
    },

    /** Cache response. */
    cache(cache: CacheOutput): WorkspaceResponse {
        return { kind: "cache", cache };
    },

    /** Settings response. */
    settings(settings: SettingsOutput): WorkspaceResponse {
        return { kind: "settings", settings };
    },

    /** Doctor response. */
    doctor(doctor: DoctorOutput): WorkspaceResponse {
        return { kind: "doctor", doctor };
    },

    /** Task response. */
    task(task: TaskOutput): WorkspaceResponse {
        return { kind: "task", task };
    },

    /** Clean response. */
    clean(clean: CleanOutput): WorkspaceResponse {
        return { kind: "clean", clean };
    },

    /** Artifact blob response. */
    artifactResult(artifact_result: ArtifactBlob): WorkspaceResponse {
        return { kind: "artifactResult", artifact_result };
    },

    /** Store response. */
    storeResult(store_result: ContentId): WorkspaceResponse {
        return { kind: "storeResult", store_result };
    },

    /** Content payload response. */
    loadResult(load_result: Content): WorkspaceResponse {
        return { kind: "loadResult", load_result };
    },

    /** Export response. */
    exportResult(export_result: ExportResult): WorkspaceResponse {
        return { kind: "exportResult", export_result };
    },

    /** Query response. */
    queryResult(query_result: WorkspaceQueryResponse): WorkspaceResponse {
        return { kind: "queryResult", query_result };
    },

    /** Error response. */
    error(error: ProtocolError): WorkspaceResponse {
        return { kind: "error", error };
    },

};

export function encodeWorkspaceResponse(writer: Writer, value: WorkspaceResponse): void {
    switch (value.kind) {
        case "handshake":
            writer.writeUnsigned(0);
            encodeHandshakeResponse(writer, value["handshake"]);
            return;
        case "pong":
            writer.writeUnsigned(1);
            return;
        case "canceled":
            writer.writeUnsigned(2);
            encodeRequestId(writer, value["id"]);
            return;
        case "shutdownAck":
            writer.writeUnsigned(3);
            return;
        case "rootOpened":
            writer.writeUnsigned(4);
            encodeRootOpenedResponse(writer, value["root_opened"]);
            return;
        case "rootClosed":
            writer.writeUnsigned(5);
            encodeRootClosedResponse(writer, value["root_closed"]);
            return;
        case "rootReloaded":
            writer.writeUnsigned(6);
            encodeRootReloadResponse(writer, value["root_reloaded"]);
            return;
        case "fileOperationApplied":
            writer.writeUnsigned(7);
            encodeFileOperationResponse(writer, value["file_operation_applied"]);
            return;
        case "sourceUpdated":
            writer.writeUnsigned(8);
            encodeSourceUpdateResponse(writer, value["source_updated"]);
            return;
        case "watchStarted":
            writer.writeUnsigned(9);
            encodeWatchStartedResponse(writer, value["watch_started"]);
            return;
        case "watchBatchReady":
            writer.writeUnsigned(10);
            encodeWatchBatchResponse(writer, value["watch_batch_ready"]);
            return;
        case "watchStopped":
            writer.writeUnsigned(11);
            encodeWatchStoppedResponse(writer, value["watch_stopped"]);
            return;
        case "check":
            writer.writeUnsigned(12);
            encodeCheckOutput(writer, value["check"]);
            return;
        case "lint":
            writer.writeUnsigned(13);
            encodeLintOutput(writer, value["lint"]);
            return;
        case "format":
            writer.writeUnsigned(14);
            encodeFormatOutput(writer, value["format"]);
            return;
        case "build":
            writer.writeUnsigned(15);
            encodeBuildOutput(writer, value["build"]);
            return;
        case "run":
            writer.writeUnsigned(16);
            encodeRunOutput(writer, value["run"]);
            return;
        case "test":
            writer.writeUnsigned(17);
            encodeTestOutput(writer, value["test"]);
            return;
        case "doc":
            writer.writeUnsigned(18);
            encodeDocOutput(writer, value["doc"]);
            return;
        case "bench":
            writer.writeUnsigned(19);
            encodeBenchOutput(writer, value["bench"]);
            return;
        case "info":
            writer.writeUnsigned(20);
            encodeInfoOutput(writer, value["info"]);
            return;
        case "targets":
            writer.writeUnsigned(21);
            encodeTargetsOutput(writer, value["targets"]);
            return;
        case "cache":
            writer.writeUnsigned(22);
            encodeCacheOutput(writer, value["cache"]);
            return;
        case "settings":
            writer.writeUnsigned(23);
            encodeSettingsOutput(writer, value["settings"]);
            return;
        case "doctor":
            writer.writeUnsigned(24);
            encodeDoctorOutput(writer, value["doctor"]);
            return;
        case "task":
            writer.writeUnsigned(25);
            encodeTaskOutput(writer, value["task"]);
            return;
        case "clean":
            writer.writeUnsigned(26);
            encodeCleanOutput(writer, value["clean"]);
            return;
        case "artifactResult":
            writer.writeUnsigned(27);
            encodeArtifactBlob(writer, value["artifact_result"]);
            return;
        case "storeResult":
            writer.writeUnsigned(28);
            encodeContentId(writer, value["store_result"]);
            return;
        case "loadResult":
            writer.writeUnsigned(29);
            encodeContent(writer, value["load_result"]);
            return;
        case "exportResult":
            writer.writeUnsigned(30);
            encodeExportResult(writer, value["export_result"]);
            return;
        case "queryResult":
            writer.writeUnsigned(31);
            encodeWorkspaceQueryResponse(writer, value["query_result"]);
            return;
        case "error":
            writer.writeUnsigned(32);
            encodeProtocolError(writer, value["error"]);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

export function decodeWorkspaceResponse(reader: Reader): WorkspaceResponse {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            return { kind: "handshake", "handshake": decodeHandshakeResponse(reader) };
        }
        case 1: {
            return { kind: "pong" };
        }
        case 2: {
            const field0 = decodeRequestId(reader);

            return {
                kind: "canceled",
                "id": field0,
            };
        }
        case 3: {
            return { kind: "shutdownAck" };
        }
        case 4: {
            return { kind: "rootOpened", "root_opened": decodeRootOpenedResponse(reader) };
        }
        case 5: {
            return { kind: "rootClosed", "root_closed": decodeRootClosedResponse(reader) };
        }
        case 6: {
            return { kind: "rootReloaded", "root_reloaded": decodeRootReloadResponse(reader) };
        }
        case 7: {
            return { kind: "fileOperationApplied", "file_operation_applied": decodeFileOperationResponse(reader) };
        }
        case 8: {
            return { kind: "sourceUpdated", "source_updated": decodeSourceUpdateResponse(reader) };
        }
        case 9: {
            return { kind: "watchStarted", "watch_started": decodeWatchStartedResponse(reader) };
        }
        case 10: {
            return { kind: "watchBatchReady", "watch_batch_ready": decodeWatchBatchResponse(reader) };
        }
        case 11: {
            return { kind: "watchStopped", "watch_stopped": decodeWatchStoppedResponse(reader) };
        }
        case 12: {
            return { kind: "check", "check": decodeCheckOutput(reader) };
        }
        case 13: {
            return { kind: "lint", "lint": decodeLintOutput(reader) };
        }
        case 14: {
            return { kind: "format", "format": decodeFormatOutput(reader) };
        }
        case 15: {
            return { kind: "build", "build": decodeBuildOutput(reader) };
        }
        case 16: {
            return { kind: "run", "run": decodeRunOutput(reader) };
        }
        case 17: {
            return { kind: "test", "test": decodeTestOutput(reader) };
        }
        case 18: {
            return { kind: "doc", "doc": decodeDocOutput(reader) };
        }
        case 19: {
            return { kind: "bench", "bench": decodeBenchOutput(reader) };
        }
        case 20: {
            return { kind: "info", "info": decodeInfoOutput(reader) };
        }
        case 21: {
            return { kind: "targets", "targets": decodeTargetsOutput(reader) };
        }
        case 22: {
            return { kind: "cache", "cache": decodeCacheOutput(reader) };
        }
        case 23: {
            return { kind: "settings", "settings": decodeSettingsOutput(reader) };
        }
        case 24: {
            return { kind: "doctor", "doctor": decodeDoctorOutput(reader) };
        }
        case 25: {
            return { kind: "task", "task": decodeTaskOutput(reader) };
        }
        case 26: {
            return { kind: "clean", "clean": decodeCleanOutput(reader) };
        }
        case 27: {
            return { kind: "artifactResult", "artifact_result": decodeArtifactBlob(reader) };
        }
        case 28: {
            return { kind: "storeResult", "store_result": decodeContentId(reader) };
        }
        case 29: {
            return { kind: "loadResult", "load_result": decodeContent(reader) };
        }
        case 30: {
            return { kind: "exportResult", "export_result": decodeExportResult(reader) };
        }
        case 31: {
            return { kind: "queryResult", "query_result": decodeWorkspaceQueryResponse(reader) };
        }
        case 32: {
            return { kind: "error", "error": decodeProtocolError(reader) };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Serialized artifact payload returned by the workspace protocol. */
export type ArtifactBlob = {
    /** Exact artifact version. */
    readonly version: ArtifactVersion;
    /** Serialized artifact payload bytes. */
    readonly bytes: Uint8Array | readonly number[];
};

export function encodeArtifactBlob(writer: Writer, value: ArtifactBlob): void {
    encodeArtifactVersion(writer, value["version"]);
    writer.writeByteSlice(value["bytes"]);
}

export function decodeArtifactBlob(reader: Reader): ArtifactBlob {
    const field0 = decodeArtifactVersion(reader);
    const field1 = reader.readByteSlice();

    return {
        "version": field0,
        "bytes": field1,
    };
}
