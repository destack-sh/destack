/* generated by destack_bridge_generate, do not edit */

import { Reader, SerdeError, Writer, compareBytes, nestedBytes } from "../../../../../protocol/serde.js";

/** The type of line ending to apply to the printed input. */
export type LineEnding = "lineFeed" | "carriageReturnLineFeed" | "carriageReturn";

export function encodeLineEnding(writer: Writer, value: LineEnding): void {
    switch (value) {
        case "lineFeed":
            writer.writeUnsigned(0);
            return;
        case "carriageReturnLineFeed":
            writer.writeUnsigned(1);
            return;
        case "carriageReturn":
            writer.writeUnsigned(2);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

export function decodeLineEnding(reader: Reader): LineEnding {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "lineFeed";
        case 1:
            return "carriageReturnLineFeed";
        case 2:
            return "carriageReturn";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** The indent style. */
export type IndentStyle = "tab" | "space";

export function encodeIndentStyle(writer: Writer, value: IndentStyle): void {
    switch (value) {
        case "tab":
            writer.writeUnsigned(0);
            return;
        case "space":
            writer.writeUnsigned(1);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

export function decodeIndentStyle(reader: Reader): IndentStyle {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "tab";
        case 1:
            return "space";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}
