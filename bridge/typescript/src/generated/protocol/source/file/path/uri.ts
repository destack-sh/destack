/* generated by destack_bridge_generate, do not edit */

import { Reader, SerdeError, Writer, compareBytes, nestedBytes } from "../../../../../protocol/serde.js";

/** A generic URI. */
export type Uri = {
    readonly 0: string;
};

export function encodeUri(writer: Writer, value: Uri): void {
    writer.writeString(value[0]);
}

export function decodeUri(reader: Reader): Uri {
    const field0 = reader.readString();

    return {
        0: field0,
    };
}
