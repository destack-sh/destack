/* generated by destack_bridge_generate, do not edit */

import { Reader, SerdeError, Writer, compareBytes, nestedBytes } from "../../../../../protocol/serde.js";

/** Stable identifier for one source component. */
export type ComponentId = {
    readonly 0: bigint;
};

export function encodeComponentId(writer: Writer, value: ComponentId): void {
    writer.writeUnsigned(value[0]);
}

export function decodeComponentId(reader: Reader): ComponentId {
    const field0 = reader.readUnsigned();

    return {
        0: field0,
    };
}
