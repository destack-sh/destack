/* generated by destack_bridge_generate, do not edit */

import { Reader, SerdeError, Writer, compareBytes, nestedBytes } from "../../../../../protocol/serde.js";
import type { PackageId } from "./package.js";
import { decodePackageId, encodePackageId } from "./package.js";

/** Unique identifier for one product within a package. */
export type ProductId = {
    /** The owning package id. */
    readonly packageId: PackageId;
    /** The stable key for this product within its package. */
    readonly productKey: ProductKey;
};

export function encodeProductId(writer: Writer, value: ProductId): void {
    encodePackageId(writer, value["packageId"]);
    encodeProductKey(writer, value["productKey"]);
}

export function decodeProductId(reader: Reader): ProductId {
    const field0 = decodePackageId(reader);
    const field1 = decodeProductKey(reader);

    return {
        "packageId": field0,
        "productKey": field1,
    };
}

/** Stable key for one product within a package. */
export type ProductKey = {
    readonly 0: bigint;
};

export function encodeProductKey(writer: Writer, value: ProductKey): void {
    writer.writeUnsigned(value[0]);
}

export function decodeProductKey(reader: Reader): ProductKey {
    const field0 = reader.readUnsigned();

    return {
        0: field0,
    };
}
