/* generated by destack_bridge_generate, do not edit */

import { Reader, SerdeError, Writer, compareBytes, nestedBytes } from "../../../../../protocol/serde.js";

/** The exact identity of one content payload. */
export type ContentId = {
    readonly 0: bigint;
};

export function encodeContentId(writer: Writer, value: ContentId): void {
    writer.writeUnsigned(value[0]);
}

export function decodeContentId(reader: Reader): ContentId {
    const field0 = reader.readUnsigned();

    return {
        0: field0,
    };
}

/** One exact content payload. */
export type Content =
    /** Text content. */
    | {
          readonly kind: "text";
          readonly content: string;
      }
    /** Binary content. */
    | {
          readonly kind: "binary";
          readonly content: Uint8Array | readonly number[];
      }
;

export const Content = {
    /** Text content. */
    text(content: string): Content {
        return { kind: "text", content };
    },

    /** Binary content. */
    binary(content: Uint8Array | readonly number[]): Content {
        return { kind: "binary", content };
    },

};

export function encodeContent(writer: Writer, value: Content): void {
    switch (value.kind) {
        case "text":
            writer.writeUnsigned(0);
            writer.writeString(value["content"]);
            return;
        case "binary":
            writer.writeUnsigned(1);
            writer.writeByteSlice(value["content"]);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

export function decodeContent(reader: Reader): Content {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const field0 = reader.readString();

            return {
                kind: "text",
                "content": field0,
            };
        }
        case 1: {
            const field0 = reader.readByteSlice();

            return {
                kind: "binary",
                "content": field0,
            };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** The id of a File. */
export type FileId = {
    readonly 0: bigint;
};

export function encodeFileId(writer: Writer, value: FileId): void {
    writer.writeUnsigned(value[0]);
}

export function decodeFileId(reader: Reader): FileId {
    const field0 = reader.readUnsigned();

    return {
        0: field0,
    };
}
