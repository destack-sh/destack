/* generated by destack_bridge_generate, do not edit */

import { Reader, SerdeError, Writer, compareBytes, nestedBytes } from "../../../../../protocol/serde.js";
import type { FileId } from "./file.js";
import { decodeFileId, encodeFileId } from "./file.js";

/** A source range in bytes (in some File). */
export type Span = {
    /** The file that the Span belongs to. */
    readonly file: FileId;
    /** The start position of the Span in bytes (absolute, inclusive). */
    readonly start: number;
    /** The end position of the Span in bytes (absolute, exclusive). */
    readonly end: number;
};

export function encodeSpan(writer: Writer, value: Span): void {
    encodeFileId(writer, value["file"]);
    writer.writeUnsigned(value["start"]);
    writer.writeUnsigned(value["end"]);
}

export function decodeSpan(reader: Reader): Span {
    const field0 = decodeFileId(reader);
    const field1 = reader.readNumber();
    const field2 = reader.readNumber();

    return {
        "file": field0,
        "start": field1,
        "end": field2,
    };
}
