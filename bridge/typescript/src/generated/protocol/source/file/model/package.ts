/* generated by destack_bridge_generate, do not edit */

import { Reader, SerdeError, Writer, compareBytes, nestedBytes } from "../../../../../protocol/serde.js";

/** Unique identifier for one source package. */
export type PackageId = {
    readonly 0: bigint;
};

export function encodePackageId(writer: Writer, value: PackageId): void {
    writer.writeUnsigned(value[0]);
}

export function decodePackageId(reader: Reader): PackageId {
    const field0 = reader.readUnsigned();

    return {
        0: field0,
    };
}
