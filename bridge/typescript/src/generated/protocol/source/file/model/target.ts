/* generated by destack_bridge_generate, do not edit */

import { Reader, SerdeError, Writer, compareBytes, nestedBytes } from "../../../../../protocol/serde.js";
import type { PackageId } from "./package.js";
import { decodePackageId, encodePackageId } from "./package.js";

/** Unique identifier for a build target within a package. */
export type TargetId = {
    /** The owning package id. */
    readonly packageId: PackageId;
    /** The stable key for this target within its package. */
    readonly targetKey: TargetKey;
};

export function encodeTargetId(writer: Writer, value: TargetId): void {
    encodePackageId(writer, value["packageId"]);
    encodeTargetKey(writer, value["targetKey"]);
}

export function decodeTargetId(reader: Reader): TargetId {
    const field0 = decodePackageId(reader);
    const field1 = decodeTargetKey(reader);

    return {
        "packageId": field0,
        "targetKey": field1,
    };
}

/** Stable key for one target within a package. */
export type TargetKey = {
    readonly 0: bigint;
};

export function encodeTargetKey(writer: Writer, value: TargetKey): void {
    writer.writeUnsigned(value[0]);
}

export function decodeTargetKey(reader: Reader): TargetKey {
    const field0 = reader.readUnsigned();

    return {
        0: field0,
    };
}
