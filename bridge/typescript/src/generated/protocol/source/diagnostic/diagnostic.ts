/* generated by destack_bridge_generate, do not edit */

import { Reader, SerdeError, Writer, compareBytes, nestedBytes } from "../../../../protocol/serde.js";
import type { DiagnosticHelp } from "./label.js";
import type { DiagnosticLabel } from "./label.js";
import type { DiagnosticNote } from "./label.js";
import type { DiagnosticSeverity } from "./severity.js";
import type { DiagnosticSuggestion } from "./suggestion.js";
import type { DiagnosticTag } from "./severity.js";
import { decodeDiagnosticHelp, encodeDiagnosticHelp } from "./label.js";
import { decodeDiagnosticLabel, encodeDiagnosticLabel } from "./label.js";
import { decodeDiagnosticNote, encodeDiagnosticNote } from "./label.js";
import { decodeDiagnosticSeverity, encodeDiagnosticSeverity } from "./severity.js";
import { decodeDiagnosticSuggestion, encodeDiagnosticSuggestion } from "./suggestion.js";
import { decodeDiagnosticTag, encodeDiagnosticTag } from "./severity.js";

/** A final renderable diagnostic. */
export type Diagnostic = {
    /** The stable identifier of the diagnostic (like `E001` or `W017`). */
    readonly code: string;
    /** The DiagnosticSeverity of the diagnostic. */
    readonly severity: DiagnosticSeverity;
    /** The message of the diagnostic. */
    readonly message: string;
    /** The main source label. */
    readonly primary: DiagnosticLabel;
    /** Additional source labels. */
    readonly labels: ReadonlyArray<DiagnosticLabel>;
    /** Extra context for understanding the diagnostic. */
    readonly notes: ReadonlyArray<DiagnosticNote>;
    /** Guidance for fixing or avoiding the diagnostic. */
    readonly helps: ReadonlyArray<DiagnosticHelp>;
    /** The suggestions for the diagnostic. */
    readonly suggestions: ReadonlyArray<DiagnosticSuggestion>;
    /** Extra semantic tags. */
    readonly tags: ReadonlyArray<DiagnosticTag>;
};

export function encodeDiagnostic(writer: Writer, value: Diagnostic): void {
    writer.writeString(value["code"]);
    encodeDiagnosticSeverity(writer, value["severity"]);
    writer.writeString(value["message"]);
    encodeDiagnosticLabel(writer, value["primary"]);
    writer.writeUnsigned(value["labels"].length);
    for (const item0 of value["labels"]) {
        encodeDiagnosticLabel(writer, item0);
    }
    writer.writeUnsigned(value["notes"].length);
    for (const item0 of value["notes"]) {
        encodeDiagnosticNote(writer, item0);
    }
    writer.writeUnsigned(value["helps"].length);
    for (const item0 of value["helps"]) {
        encodeDiagnosticHelp(writer, item0);
    }
    writer.writeUnsigned(value["suggestions"].length);
    for (const item0 of value["suggestions"]) {
        encodeDiagnosticSuggestion(writer, item0);
    }
    writer.writeUnsigned(value["tags"].length);
    for (const item0 of value["tags"]) {
        encodeDiagnosticTag(writer, item0);
    }
}

export function decodeDiagnostic(reader: Reader): Diagnostic {
    const field0 = reader.readString();
    const field1 = decodeDiagnosticSeverity(reader);
    const field2 = reader.readString();
    const field3 = decodeDiagnosticLabel(reader);
    const field4 = (() => { const length4 = reader.readNumber(); const items4: Array<DiagnosticLabel> = []; for (let index = 0; index < length4; index += 1) { items4.push(decodeDiagnosticLabel(reader)); } return items4; })();
    const field5 = (() => { const length5 = reader.readNumber(); const items5: Array<DiagnosticNote> = []; for (let index = 0; index < length5; index += 1) { items5.push(decodeDiagnosticNote(reader)); } return items5; })();
    const field6 = (() => { const length6 = reader.readNumber(); const items6: Array<DiagnosticHelp> = []; for (let index = 0; index < length6; index += 1) { items6.push(decodeDiagnosticHelp(reader)); } return items6; })();
    const field7 = (() => { const length7 = reader.readNumber(); const items7: Array<DiagnosticSuggestion> = []; for (let index = 0; index < length7; index += 1) { items7.push(decodeDiagnosticSuggestion(reader)); } return items7; })();
    const field8 = (() => { const length8 = reader.readNumber(); const items8: Array<DiagnosticTag> = []; for (let index = 0; index < length8; index += 1) { items8.push(decodeDiagnosticTag(reader)); } return items8; })();

    return {
        "code": field0,
        "severity": field1,
        "message": field2,
        "primary": field3,
        "labels": field4,
        "notes": field5,
        "helps": field6,
        "suggestions": field7,
        "tags": field8,
    };
}
