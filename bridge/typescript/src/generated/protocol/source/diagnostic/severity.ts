/* generated by destack_bridge_generate, do not edit */

import { Reader, SerdeError, Writer, compareBytes, nestedBytes } from "../../../../protocol/serde.js";

/** The level of a diagnostic. */
export type DiagnosticSeverity = "note" | "warning" | "error";

export function encodeDiagnosticSeverity(writer: Writer, value: DiagnosticSeverity): void {
    switch (value) {
        case "note":
            writer.writeUnsigned(0);
            return;
        case "warning":
            writer.writeUnsigned(1);
            return;
        case "error":
            writer.writeUnsigned(2);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

export function decodeDiagnosticSeverity(reader: Reader): DiagnosticSeverity {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "note";
        case 1:
            return "warning";
        case 2:
            return "error";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Extra semantic tag for a diagnostic. */
export type DiagnosticTag = "unnecessary" | "deprecated";

export function encodeDiagnosticTag(writer: Writer, value: DiagnosticTag): void {
    switch (value) {
        case "unnecessary":
            writer.writeUnsigned(0);
            return;
        case "deprecated":
            writer.writeUnsigned(1);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

export function decodeDiagnosticTag(reader: Reader): DiagnosticTag {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "unnecessary";
        case 1:
            return "deprecated";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}
