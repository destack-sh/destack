/* generated by destack_bridge_generate, do not edit */

import { Reader, SerdeError, Writer, compareBytes, nestedBytes } from "../../../../protocol/serde.js";
import type { PatchSet } from "../../source/edit/edit.js";
import type { QueryPosition } from "../core/target.js";
import { decodePatchSet, encodePatchSet } from "../../source/edit/edit.js";
import { decodeQueryPosition, encodeQueryPosition } from "../core/target.js";

/** Request payload for change signature queries. */
export type ChangeSignatureRequest = {
    /** The queried position. */
    readonly position: QueryPosition;
    /** The new parameter list, comma-separated. */
    readonly newParameters: string;
    /** The new argument list, comma-separated. */
    readonly newArguments: string;
};

export function encodeChangeSignatureRequest(writer: Writer, value: ChangeSignatureRequest): void {
    encodeQueryPosition(writer, value["position"]);
    writer.writeString(value["newParameters"]);
    writer.writeString(value["newArguments"]);
}

export function decodeChangeSignatureRequest(reader: Reader): ChangeSignatureRequest {
    const field0 = decodeQueryPosition(reader);
    const field1 = reader.readString();
    const field2 = reader.readString();

    return {
        "position": field0,
        "newParameters": field1,
        "newArguments": field2,
    };
}

/** Response payload for change signature queries. */
export type ChangeSignatureResponse = {
    /** Change signature edit, if available. */
    readonly edit?: PatchSet;
};

export function encodeChangeSignatureResponse(writer: Writer, value: ChangeSignatureResponse): void {
    writer.writeOption(value["edit"], (value0) => {
        encodePatchSet(writer, value0);
    });
}

export function decodeChangeSignatureResponse(reader: Reader): ChangeSignatureResponse {
    const field0 = reader.readOption(() => decodePatchSet(reader));

    return {
        ...(field0 === undefined ? {} : { "edit": field0 }),
    };
}
