/* generated by destack_bridge_generate, do not edit */

import { Reader, SerdeError, Writer, compareBytes, nestedBytes } from "../../../../protocol/serde.js";
import type { PatchSet } from "../../source/edit/edit.js";
import type { QueryRange } from "../core/target.js";
import { decodePatchSet, encodePatchSet } from "../../source/edit/edit.js";
import { decodeQueryRange, encodeQueryRange } from "../core/target.js";

/** Request payload for extract variable queries. */
export type ExtractVariableRequest = {
    /** The selected source range. */
    readonly range: QueryRange;
    /** The name for the extracted variable. */
    readonly newName: string;
};

export function encodeExtractVariableRequest(writer: Writer, value: ExtractVariableRequest): void {
    encodeQueryRange(writer, value["range"]);
    writer.writeString(value["newName"]);
}

export function decodeExtractVariableRequest(reader: Reader): ExtractVariableRequest {
    const field0 = decodeQueryRange(reader);
    const field1 = reader.readString();

    return {
        "range": field0,
        "newName": field1,
    };
}

/** Response payload for extract variable queries. */
export type ExtractVariableResponse = {
    /** Extract variable edit, if available. */
    readonly edit?: PatchSet;
};

export function encodeExtractVariableResponse(writer: Writer, value: ExtractVariableResponse): void {
    writer.writeOption(value["edit"], (value0) => {
        encodePatchSet(writer, value0);
    });
}

export function decodeExtractVariableResponse(reader: Reader): ExtractVariableResponse {
    const field0 = reader.readOption(() => decodePatchSet(reader));

    return {
        ...(field0 === undefined ? {} : { "edit": field0 }),
    };
}
