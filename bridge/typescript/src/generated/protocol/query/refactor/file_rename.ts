/* generated by destack_bridge_generate, do not edit */

import { Reader, SerdeError, Writer, compareBytes, nestedBytes } from "../../../../protocol/serde.js";
import type { PatchSet } from "../../source/edit/edit.js";
import type { ProfileId } from "../../source/file/model/profile.js";
import { decodePatchSet, encodePatchSet } from "../../source/edit/edit.js";
import { decodeProfileId, encodeProfileId } from "../../source/file/model/profile.js";

/** Request payload for file rename edits. */
export type RenameFilesRequest = {
    /** Profiles that should participate in specifier rewrites. */
    readonly profileIds: ReadonlyArray<ProfileId>;
    /** The file rename entries to apply. */
    readonly renames: ReadonlyArray<FileRenameEntry>;
};

export function encodeRenameFilesRequest(writer: Writer, value: RenameFilesRequest): void {
    writer.writeUnsigned(value["profileIds"].length);
    for (const item0 of value["profileIds"]) {
        encodeProfileId(writer, item0);
    }
    writer.writeUnsigned(value["renames"].length);
    for (const item0 of value["renames"]) {
        encodeFileRenameEntry(writer, item0);
    }
}

export function decodeRenameFilesRequest(reader: Reader): RenameFilesRequest {
    const field0 = (() => { const length0 = reader.readNumber(); const items0: Array<ProfileId> = []; for (let index = 0; index < length0; index += 1) { items0.push(decodeProfileId(reader)); } return items0; })();
    const field1 = (() => { const length1 = reader.readNumber(); const items1: Array<FileRenameEntry> = []; for (let index = 0; index < length1; index += 1) { items1.push(decodeFileRenameEntry(reader)); } return items1; })();

    return {
        "profileIds": field0,
        "renames": field1,
    };
}

/** A file rename entry for refactor queries. */
export type FileRenameEntry = {
    /** The old path before the rename. */
    readonly oldPath: string;
    /** The new path after the rename. */
    readonly newPath: string;
};

export function encodeFileRenameEntry(writer: Writer, value: FileRenameEntry): void {
    writer.writeString(value["oldPath"]);
    writer.writeString(value["newPath"]);
}

export function decodeFileRenameEntry(reader: Reader): FileRenameEntry {
    const field0 = reader.readString();
    const field1 = reader.readString();

    return {
        "oldPath": field0,
        "newPath": field1,
    };
}

/** Response payload for file rename queries. */
export type RenameFilesResponse = {
    /** File rename edit, if available. */
    readonly edit?: PatchSet;
};

export function encodeRenameFilesResponse(writer: Writer, value: RenameFilesResponse): void {
    writer.writeOption(value["edit"], (value0) => {
        encodePatchSet(writer, value0);
    });
}

export function decodeRenameFilesResponse(reader: Reader): RenameFilesResponse {
    const field0 = reader.readOption(() => decodePatchSet(reader));

    return {
        ...(field0 === undefined ? {} : { "edit": field0 }),
    };
}
