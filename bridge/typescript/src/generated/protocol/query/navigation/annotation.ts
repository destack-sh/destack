/* generated by destack_bridge_generate, do not edit */

import { Reader, SerdeError, Writer, compareBytes, nestedBytes } from "../../../../protocol/serde.js";
import type { ProfileId } from "../../source/file/model/profile.js";
import type { QueryModule } from "../core/target.js";
import type { QueryTarget } from "../core/target.js";
import { decodeProfileId, encodeProfileId } from "../../source/file/model/profile.js";
import { decodeQueryModule, encodeQueryModule } from "../core/target.js";
import { decodeQueryTarget, encodeQueryTarget } from "../core/target.js";

/** Request payload for annotation queries. */
export type AnnotationsRequest = {
    /** The query scope. */
    readonly scope: AnnotationScope;
    /** The annotation name filter. */
    readonly name?: string;
};

export function encodeAnnotationsRequest(writer: Writer, value: AnnotationsRequest): void {
    encodeAnnotationScope(writer, value["scope"]);
    writer.writeOption(value["name"], (value0) => {
        writer.writeString(value0);
    });
}

export function decodeAnnotationsRequest(reader: Reader): AnnotationsRequest {
    const field0 = decodeAnnotationScope(reader);
    const field1 = reader.readOption(() => reader.readString());

    return {
        "scope": field0,
        ...(field1 === undefined ? {} : { "name": field1 }),
    };
}

/** Scope for annotation queries. */
export type AnnotationScope =
    /** One module. */
    | {
          readonly kind: "module";
          readonly module: QueryModule;
      }
    /** Workspace profiles. */
    | {
          readonly kind: "workspace";
          /** The profiles to search. */
          readonly profileIds: ReadonlyArray<ProfileId>;
      }
;

export const AnnotationScope = {
    /** One module. */
    module(module: QueryModule): AnnotationScope {
        return { kind: "module", module };
    },

    /** Workspace profiles. */
    workspace(profileIds: ReadonlyArray<ProfileId>): AnnotationScope {
        return { kind: "workspace", profileIds };
    },

};

export function encodeAnnotationScope(writer: Writer, value: AnnotationScope): void {
    switch (value.kind) {
        case "module":
            writer.writeUnsigned(0);
            encodeQueryModule(writer, value["module"]);
            return;
        case "workspace":
            writer.writeUnsigned(1);
            writer.writeUnsigned(value["profileIds"].length);
            for (const item0 of value["profileIds"]) {
                encodeProfileId(writer, item0);
            }
            return;
    }

    throw new SerdeError("unknown enum variant");
}

export function decodeAnnotationScope(reader: Reader): AnnotationScope {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            return { kind: "module", "module": decodeQueryModule(reader) };
        }
        case 1: {
            const field0 = (() => { const length0 = reader.readNumber(); const items0: Array<ProfileId> = []; for (let index = 0; index < length0; index += 1) { items0.push(decodeProfileId(reader)); } return items0; })();

            return {
                kind: "workspace",
                "profileIds": field0,
            };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Response payload for annotation queries. */
export type AnnotationsResponse = {
    /** Matching annotations. */
    readonly annotations: ReadonlyArray<AnnotationItem>;
};

export function encodeAnnotationsResponse(writer: Writer, value: AnnotationsResponse): void {
    writer.writeUnsigned(value["annotations"].length);
    for (const item0 of value["annotations"]) {
        encodeAnnotationItem(writer, item0);
    }
}

export function decodeAnnotationsResponse(reader: Reader): AnnotationsResponse {
    const field0 = (() => { const length0 = reader.readNumber(); const items0: Array<AnnotationItem> = []; for (let index = 0; index < length0; index += 1) { items0.push(decodeAnnotationItem(reader)); } return items0; })();

    return {
        "annotations": field0,
    };
}

/** One annotation query item. */
export type AnnotationItem = {
    /** The annotation name when syntactically known. */
    readonly name?: string;
    /** The decorator expression target. */
    readonly decorator: QueryTarget;
    /** The annotated target. */
    readonly target: QueryTarget;
    /** The resolved annotation role. */
    readonly role: AnnotationRole;
};

export function encodeAnnotationItem(writer: Writer, value: AnnotationItem): void {
    writer.writeOption(value["name"], (value0) => {
        writer.writeString(value0);
    });
    encodeQueryTarget(writer, value["decorator"]);
    encodeQueryTarget(writer, value["target"]);
    encodeAnnotationRole(writer, value["role"]);
}

export function decodeAnnotationItem(reader: Reader): AnnotationItem {
    const field0 = reader.readOption(() => reader.readString());
    const field1 = decodeQueryTarget(reader);
    const field2 = decodeQueryTarget(reader);
    const field3 = decodeAnnotationRole(reader);

    return {
        ...(field0 === undefined ? {} : { "name": field0 }),
        "decorator": field1,
        "target": field2,
        "role": field3,
    };
}

/** Role of one annotation expression. */
export type AnnotationRole = "annotation" | "decorator" | "unknown";

export function encodeAnnotationRole(writer: Writer, value: AnnotationRole): void {
    switch (value) {
        case "annotation":
            writer.writeUnsigned(0);
            return;
        case "decorator":
            writer.writeUnsigned(1);
            return;
        case "unknown":
            writer.writeUnsigned(2);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

export function decodeAnnotationRole(reader: Reader): AnnotationRole {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "annotation";
        case 1:
            return "decorator";
        case 2:
            return "unknown";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}
