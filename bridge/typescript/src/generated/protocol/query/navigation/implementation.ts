/* generated by destack_bridge_generate, do not edit */

import { Reader, SerdeError, Writer, compareBytes, nestedBytes } from "../../../../protocol/serde.js";
import type { NavigationTarget } from "./definition.js";
import type { QueryPosition } from "../core/target.js";
import { decodeNavigationTarget, encodeNavigationTarget } from "./definition.js";
import { decodeQueryPosition, encodeQueryPosition } from "../core/target.js";

/** Request goto implementation at a cursor position. */
export type GotoImplementationRequest = {
    /** The queried position. */
    readonly position: QueryPosition;
};

export function encodeGotoImplementationRequest(writer: Writer, value: GotoImplementationRequest): void {
    encodeQueryPosition(writer, value["position"]);
}

export function decodeGotoImplementationRequest(reader: Reader): GotoImplementationRequest {
    const field0 = decodeQueryPosition(reader);

    return {
        "position": field0,
    };
}

/** Response payload for goto implementation queries. */
export type GotoImplementationResponse = {
    /** Implementation targets. */
    readonly targets: ReadonlyArray<NavigationTarget>;
};

export function encodeGotoImplementationResponse(writer: Writer, value: GotoImplementationResponse): void {
    writer.writeUnsigned(value["targets"].length);
    for (const item0 of value["targets"]) {
        encodeNavigationTarget(writer, item0);
    }
}

export function decodeGotoImplementationResponse(reader: Reader): GotoImplementationResponse {
    const field0 = (() => { const length0 = reader.readNumber(); const items0: Array<NavigationTarget> = []; for (let index = 0; index < length0; index += 1) { items0.push(decodeNavigationTarget(reader)); } return items0; })();

    return {
        "targets": field0,
    };
}
