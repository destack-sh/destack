/* generated by destack_bridge_generate, do not edit */

import { Reader, SerdeError, Writer, compareBytes, nestedBytes } from "../../../../protocol/serde.js";
import type { QueryPosition } from "../core/target.js";
import type { Span } from "../../source/file/model/span.js";
import { decodeQueryPosition, encodeQueryPosition } from "../core/target.js";
import { decodeSpan, encodeSpan } from "../../source/file/model/span.js";

/** Request highlights at a cursor position. */
export type DocumentHighlightRequest = {
    /** The queried position. */
    readonly position: QueryPosition;
};

export function encodeDocumentHighlightRequest(writer: Writer, value: DocumentHighlightRequest): void {
    encodeQueryPosition(writer, value["position"]);
}

export function decodeDocumentHighlightRequest(reader: Reader): DocumentHighlightRequest {
    const field0 = decodeQueryPosition(reader);

    return {
        "position": field0,
    };
}

/** Response payload for document highlight queries. */
export type DocumentHighlightResponse = {
    /** Document highlights. */
    readonly highlights: ReadonlyArray<DocumentHighlight>;
};

export function encodeDocumentHighlightResponse(writer: Writer, value: DocumentHighlightResponse): void {
    writer.writeUnsigned(value["highlights"].length);
    for (const item0 of value["highlights"]) {
        encodeDocumentHighlight(writer, item0);
    }
}

export function decodeDocumentHighlightResponse(reader: Reader): DocumentHighlightResponse {
    const field0 = (() => { const length0 = reader.readNumber(); const items0: Array<DocumentHighlight> = []; for (let index = 0; index < length0; index += 1) { items0.push(decodeDocumentHighlight(reader)); } return items0; })();

    return {
        "highlights": field0,
    };
}

/** A highlighted range in a document. */
export type DocumentHighlight = {
    /** The highlighted range. */
    readonly range: Span;
    /** The kind of highlight. */
    readonly kind: HighlightKind;
};

export function encodeDocumentHighlight(writer: Writer, value: DocumentHighlight): void {
    encodeSpan(writer, value["range"]);
    encodeHighlightKind(writer, value["kind"]);
}

export function decodeDocumentHighlight(reader: Reader): DocumentHighlight {
    const field0 = decodeSpan(reader);
    const field1 = decodeHighlightKind(reader);

    return {
        "range": field0,
        "kind": field1,
    };
}

/** Kind of document highlight. */
export type HighlightKind = "text" | "read" | "write";

export function encodeHighlightKind(writer: Writer, value: HighlightKind): void {
    switch (value) {
        case "text":
            writer.writeUnsigned(0);
            return;
        case "read":
            writer.writeUnsigned(1);
            return;
        case "write":
            writer.writeUnsigned(2);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

export function decodeHighlightKind(reader: Reader): HighlightKind {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "text";
        case 1:
            return "read";
        case 2:
            return "write";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}
