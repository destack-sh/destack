/* generated by destack_bridge_generate, do not edit */

import { Reader, SerdeError, Writer, compareBytes, nestedBytes } from "../../../../protocol/serde.js";
import type { QueryPosition } from "../core/target.js";
import type { QueryTarget } from "../core/target.js";
import { decodeQueryPosition, encodeQueryPosition } from "../core/target.js";
import { decodeQueryTarget, encodeQueryTarget } from "../core/target.js";

/** Request the type hierarchy item at a cursor position. */
export type TypeHierarchyItemRequest = {
    /** The queried position. */
    readonly position: QueryPosition;
};

export function encodeTypeHierarchyItemRequest(writer: Writer, value: TypeHierarchyItemRequest): void {
    encodeQueryPosition(writer, value["position"]);
}

export function decodeTypeHierarchyItemRequest(reader: Reader): TypeHierarchyItemRequest {
    const field0 = decodeQueryPosition(reader);

    return {
        "position": field0,
    };
}

/** Request type hierarchy supertypes. */
export type TypeHierarchySupertypesRequest = {
    /** The type hierarchy item to expand. */
    readonly item: TypeHierarchyItem;
};

export function encodeTypeHierarchySupertypesRequest(writer: Writer, value: TypeHierarchySupertypesRequest): void {
    encodeTypeHierarchyItem(writer, value["item"]);
}

export function decodeTypeHierarchySupertypesRequest(reader: Reader): TypeHierarchySupertypesRequest {
    const field0 = decodeTypeHierarchyItem(reader);

    return {
        "item": field0,
    };
}

/** An item in the type hierarchy. */
export type TypeHierarchyItem = {
    /** The name of the type. */
    readonly name: string;
    /** The kind of type. */
    readonly kind: TypeHierarchyKind;
    /** Detail (e.g., generic parameters). */
    readonly detail?: string;
    /** The target source and resolved identity. */
    readonly target: QueryTarget;
};

export function encodeTypeHierarchyItem(writer: Writer, value: TypeHierarchyItem): void {
    writer.writeString(value["name"]);
    encodeTypeHierarchyKind(writer, value["kind"]);
    writer.writeOption(value["detail"], (value0) => {
        writer.writeString(value0);
    });
    encodeQueryTarget(writer, value["target"]);
}

export function decodeTypeHierarchyItem(reader: Reader): TypeHierarchyItem {
    const field0 = reader.readString();
    const field1 = decodeTypeHierarchyKind(reader);
    const field2 = reader.readOption(() => reader.readString());
    const field3 = decodeQueryTarget(reader);

    return {
        "name": field0,
        "kind": field1,
        ...(field2 === undefined ? {} : { "detail": field2 }),
        "target": field3,
    };
}

/** Kind of type hierarchy item. */
export type TypeHierarchyKind = "class" | "interface" | "struct" | "enum" | "typeAlias";

export function encodeTypeHierarchyKind(writer: Writer, value: TypeHierarchyKind): void {
    switch (value) {
        case "class":
            writer.writeUnsigned(0);
            return;
        case "interface":
            writer.writeUnsigned(1);
            return;
        case "struct":
            writer.writeUnsigned(2);
            return;
        case "enum":
            writer.writeUnsigned(3);
            return;
        case "typeAlias":
            writer.writeUnsigned(4);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

export function decodeTypeHierarchyKind(reader: Reader): TypeHierarchyKind {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "class";
        case 1:
            return "interface";
        case 2:
            return "struct";
        case 3:
            return "enum";
        case 4:
            return "typeAlias";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}

/** Request type hierarchy subtypes. */
export type TypeHierarchySubtypesRequest = {
    /** The type hierarchy item to expand. */
    readonly item: TypeHierarchyItem;
};

export function encodeTypeHierarchySubtypesRequest(writer: Writer, value: TypeHierarchySubtypesRequest): void {
    encodeTypeHierarchyItem(writer, value["item"]);
}

export function decodeTypeHierarchySubtypesRequest(reader: Reader): TypeHierarchySubtypesRequest {
    const field0 = decodeTypeHierarchyItem(reader);

    return {
        "item": field0,
    };
}

/** Response payload for type hierarchy item queries. */
export type TypeHierarchyItemResponse = {
    /** Type hierarchy item, if available. */
    readonly item?: TypeHierarchyItem;
};

export function encodeTypeHierarchyItemResponse(writer: Writer, value: TypeHierarchyItemResponse): void {
    writer.writeOption(value["item"], (value0) => {
        encodeTypeHierarchyItem(writer, value0);
    });
}

export function decodeTypeHierarchyItemResponse(reader: Reader): TypeHierarchyItemResponse {
    const field0 = reader.readOption(() => decodeTypeHierarchyItem(reader));

    return {
        ...(field0 === undefined ? {} : { "item": field0 }),
    };
}

/** Response payload for type hierarchy supertypes queries. */
export type TypeHierarchySupertypesResponse = {
    /** Type hierarchy items. */
    readonly items: ReadonlyArray<TypeHierarchyItem>;
};

export function encodeTypeHierarchySupertypesResponse(writer: Writer, value: TypeHierarchySupertypesResponse): void {
    writer.writeUnsigned(value["items"].length);
    for (const item0 of value["items"]) {
        encodeTypeHierarchyItem(writer, item0);
    }
}

export function decodeTypeHierarchySupertypesResponse(reader: Reader): TypeHierarchySupertypesResponse {
    const field0 = (() => { const length0 = reader.readNumber(); const items0: Array<TypeHierarchyItem> = []; for (let index = 0; index < length0; index += 1) { items0.push(decodeTypeHierarchyItem(reader)); } return items0; })();

    return {
        "items": field0,
    };
}

/** Response payload for type hierarchy subtypes queries. */
export type TypeHierarchySubtypesResponse = {
    /** Type hierarchy items. */
    readonly items: ReadonlyArray<TypeHierarchyItem>;
};

export function encodeTypeHierarchySubtypesResponse(writer: Writer, value: TypeHierarchySubtypesResponse): void {
    writer.writeUnsigned(value["items"].length);
    for (const item0 of value["items"]) {
        encodeTypeHierarchyItem(writer, item0);
    }
}

export function decodeTypeHierarchySubtypesResponse(reader: Reader): TypeHierarchySubtypesResponse {
    const field0 = (() => { const length0 = reader.readNumber(); const items0: Array<TypeHierarchyItem> = []; for (let index = 0; index < length0; index += 1) { items0.push(decodeTypeHierarchyItem(reader)); } return items0; })();

    return {
        "items": field0,
    };
}
