/* generated by destack_bridge_generate, do not edit */

import { Reader, SerdeError, Writer, compareBytes, nestedBytes } from "../../../../protocol/serde.js";
import type { QueryPosition } from "../core/target.js";
import type { QueryTarget } from "../core/target.js";
import { decodeQueryPosition, encodeQueryPosition } from "../core/target.js";
import { decodeQueryTarget, encodeQueryTarget } from "../core/target.js";

/** Request find references at a cursor position. */
export type FindReferencesRequest = {
    /** The queried position. */
    readonly position: QueryPosition;
    /** Whether to include the declaration in results. */
    readonly includeDeclaration: boolean;
};

export function encodeFindReferencesRequest(writer: Writer, value: FindReferencesRequest): void {
    encodeQueryPosition(writer, value["position"]);
    writer.writeBool(value["includeDeclaration"]);
}

export function decodeFindReferencesRequest(reader: Reader): FindReferencesRequest {
    const field0 = decodeQueryPosition(reader);
    const field1 = reader.readBool();

    return {
        "position": field0,
        "includeDeclaration": field1,
    };
}

/** Response payload for find references queries. */
export type FindReferencesResponse = {
    /** Reference occurrences. */
    readonly references: ReadonlyArray<Reference>;
};

export function encodeFindReferencesResponse(writer: Writer, value: FindReferencesResponse): void {
    writer.writeUnsigned(value["references"].length);
    for (const item0 of value["references"]) {
        encodeReference(writer, item0);
    }
}

export function decodeFindReferencesResponse(reader: Reader): FindReferencesResponse {
    const field0 = (() => { const length0 = reader.readNumber(); const items0: Array<Reference> = []; for (let index = 0; index < length0; index += 1) { items0.push(decodeReference(reader)); } return items0; })();

    return {
        "references": field0,
    };
}

/** One symbol reference occurrence. */
export type Reference = {
    /** The referenced source target. */
    readonly target: QueryTarget;
    /** The reference role. */
    readonly role: ReferenceRole;
};

export function encodeReference(writer: Writer, value: Reference): void {
    encodeQueryTarget(writer, value["target"]);
    encodeReferenceRole(writer, value["role"]);
}

export function decodeReference(reader: Reader): Reference {
    const field0 = decodeQueryTarget(reader);
    const field1 = decodeReferenceRole(reader);

    return {
        "target": field0,
        "role": field1,
    };
}

/** Role of one reference occurrence. */
export type ReferenceRole = "declaration" | "read" | "write" | "type" | "import" | "export";

export function encodeReferenceRole(writer: Writer, value: ReferenceRole): void {
    switch (value) {
        case "declaration":
            writer.writeUnsigned(0);
            return;
        case "read":
            writer.writeUnsigned(1);
            return;
        case "write":
            writer.writeUnsigned(2);
            return;
        case "type":
            writer.writeUnsigned(3);
            return;
        case "import":
            writer.writeUnsigned(4);
            return;
        case "export":
            writer.writeUnsigned(5);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

export function decodeReferenceRole(reader: Reader): ReferenceRole {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "declaration";
        case 1:
            return "read";
        case 2:
            return "write";
        case 3:
            return "type";
        case 4:
            return "import";
        case 5:
            return "export";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}
