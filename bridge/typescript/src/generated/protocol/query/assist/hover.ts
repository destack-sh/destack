/* generated by destack_bridge_generate, do not edit */

import { Reader, SerdeError, Writer, compareBytes, nestedBytes } from "../../../../protocol/serde.js";
import type { QueryPosition } from "../core/target.js";
import type { Span } from "../../source/file/model/span.js";
import { decodeQueryPosition, encodeQueryPosition } from "../core/target.js";
import { decodeSpan, encodeSpan } from "../../source/file/model/span.js";

/** Request hover information at a cursor position. */
export type HoverRequest = {
    /** The queried position. */
    readonly position: QueryPosition;
};

export function encodeHoverRequest(writer: Writer, value: HoverRequest): void {
    encodeQueryPosition(writer, value["position"]);
}

export function decodeHoverRequest(reader: Reader): HoverRequest {
    const field0 = decodeQueryPosition(reader);

    return {
        "position": field0,
    };
}

/** Response payload for hover queries. */
export type HoverResponse = {
    /** Hover information, if available. */
    readonly hover?: Hover;
};

export function encodeHoverResponse(writer: Writer, value: HoverResponse): void {
    writer.writeOption(value["hover"], (value0) => {
        encodeHover(writer, value0);
    });
}

export function decodeHoverResponse(reader: Reader): HoverResponse {
    const field0 = reader.readOption(() => decodeHover(reader));

    return {
        ...(field0 === undefined ? {} : { "hover": field0 }),
    };
}

/** Hover payload for a source position. */
export type Hover = {
    /** The type/signature in code format. */
    readonly signature: string;
    /** Documentation (markdown). */
    readonly documentation?: string;
    /** Resolved type information when available. */
    readonly typeText?: string;
    /** Source location text when available. */
    readonly location?: string;
    /** The range of the hovered element. */
    readonly range?: Span;
};

export function encodeHover(writer: Writer, value: Hover): void {
    writer.writeString(value["signature"]);
    writer.writeOption(value["documentation"], (value0) => {
        writer.writeString(value0);
    });
    writer.writeOption(value["typeText"], (value0) => {
        writer.writeString(value0);
    });
    writer.writeOption(value["location"], (value0) => {
        writer.writeString(value0);
    });
    writer.writeOption(value["range"], (value0) => {
        encodeSpan(writer, value0);
    });
}

export function decodeHover(reader: Reader): Hover {
    const field0 = reader.readString();
    const field1 = reader.readOption(() => reader.readString());
    const field2 = reader.readOption(() => reader.readString());
    const field3 = reader.readOption(() => reader.readString());
    const field4 = reader.readOption(() => decodeSpan(reader));

    return {
        "signature": field0,
        ...(field1 === undefined ? {} : { "documentation": field1 }),
        ...(field2 === undefined ? {} : { "typeText": field2 }),
        ...(field3 === undefined ? {} : { "location": field3 }),
        ...(field4 === undefined ? {} : { "range": field4 }),
    };
}
