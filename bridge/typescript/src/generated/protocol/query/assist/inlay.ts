/* generated by destack_bridge_generate, do not edit */

import { Reader, SerdeError, Writer, compareBytes, nestedBytes } from "../../../../protocol/serde.js";
import type { QueryRange } from "../core/target.js";
import { decodeQueryRange, encodeQueryRange } from "../core/target.js";

/** Request inlay hints for a range in a document. */
export type InlayHintsRequest = {
    /** The queried range. */
    readonly range: QueryRange;
};

export function encodeInlayHintsRequest(writer: Writer, value: InlayHintsRequest): void {
    encodeQueryRange(writer, value["range"]);
}

export function decodeInlayHintsRequest(reader: Reader): InlayHintsRequest {
    const field0 = decodeQueryRange(reader);

    return {
        "range": field0,
    };
}

/** Response payload for inlay hints queries. */
export type InlayHintsResponse = {
    /** Inlay hints. */
    readonly hints: ReadonlyArray<InlayHint>;
};

export function encodeInlayHintsResponse(writer: Writer, value: InlayHintsResponse): void {
    writer.writeUnsigned(value["hints"].length);
    for (const item0 of value["hints"]) {
        encodeInlayHint(writer, item0);
    }
}

export function decodeInlayHintsResponse(reader: Reader): InlayHintsResponse {
    const field0 = (() => { const length0 = reader.readNumber(); const items0: Array<InlayHint> = []; for (let index = 0; index < length0; index += 1) { items0.push(decodeInlayHint(reader)); } return items0; })();

    return {
        "hints": field0,
    };
}

/** An inlay hint (virtual text shown inline). */
export type InlayHint = {
    /** Position where the hint should be displayed. */
    readonly position: number;
    /** The hint text. */
    readonly label: string;
    /** The kind of hint. */
    readonly kind: InlayHintKind;
    /** Whether there should be padding before the hint. */
    readonly paddingLeft: boolean;
    /** Whether there should be padding after the hint. */
    readonly paddingRight: boolean;
};

export function encodeInlayHint(writer: Writer, value: InlayHint): void {
    writer.writeUnsigned(value["position"]);
    writer.writeString(value["label"]);
    encodeInlayHintKind(writer, value["kind"]);
    writer.writeBool(value["paddingLeft"]);
    writer.writeBool(value["paddingRight"]);
}

export function decodeInlayHint(reader: Reader): InlayHint {
    const field0 = reader.readNumber();
    const field1 = reader.readString();
    const field2 = decodeInlayHintKind(reader);
    const field3 = reader.readBool();
    const field4 = reader.readBool();

    return {
        "position": field0,
        "label": field1,
        "kind": field2,
        "paddingLeft": field3,
        "paddingRight": field4,
    };
}

/** Kind of inlay hint. */
export type InlayHintKind = "type" | "parameter";

export function encodeInlayHintKind(writer: Writer, value: InlayHintKind): void {
    switch (value) {
        case "type":
            writer.writeUnsigned(0);
            return;
        case "parameter":
            writer.writeUnsigned(1);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

export function decodeInlayHintKind(reader: Reader): InlayHintKind {
    const variant = reader.readNumber();

    switch (variant) {
        case 0:
            return "type";
        case 1:
            return "parameter";
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}
