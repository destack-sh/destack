/* generated by destack_bridge_generate, do not edit */

import { Reader, SerdeError, Writer, compareBytes, nestedBytes } from "../../../protocol/serde.js";

/** Content identity for one repository source and environment state. */
export type Revision = {
    readonly 0: Uint8Array | readonly number[];
};

export function encodeRevision(writer: Writer, value: Revision): void {
    writer.writeBytes(value[0]);
}

export function decodeRevision(reader: Reader): Revision {
    const field0 = reader.readBytes(32);

    return {
        0: field0,
    };
}
