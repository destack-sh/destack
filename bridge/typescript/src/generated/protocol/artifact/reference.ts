/* generated by destack_bridge_generate, do not edit */

import { Reader, SerdeError, Writer, compareBytes, nestedBytes } from "../../../protocol/serde.js";
import type { ArtifactKey } from "./core/key.js";
import type { ArtifactVersion } from "./core/version.js";
import { decodeArtifactKey, encodeArtifactKey } from "./core/key.js";
import { decodeArtifactVersion, encodeArtifactVersion } from "./core/version.js";

/** Stable reference to one artifact payload. */
export type ArtifactReference = {
    /** Artifact key. */
    readonly key: ArtifactKey;
    /** Artifact version. */
    readonly version: ArtifactVersion;
};

export function encodeArtifactReference(writer: Writer, value: ArtifactReference): void {
    encodeArtifactKey(writer, value["key"]);
    encodeArtifactVersion(writer, value["version"]);
}

export function decodeArtifactReference(reader: Reader): ArtifactReference {
    const field0 = decodeArtifactKey(reader);
    const field1 = decodeArtifactVersion(reader);

    return {
        "key": field0,
        "version": field1,
    };
}
