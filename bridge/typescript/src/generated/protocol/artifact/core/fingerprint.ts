/* generated by destack_bridge_generate, do not edit */

import { Reader, SerdeError, Writer, compareBytes, nestedBytes } from "../../../../protocol/serde.js";

/** Deterministic identity of one artifact's complete semantic dependencies. */
export type ArtifactFingerprint = {
    readonly 0: bigint;
};

export function encodeArtifactFingerprint(writer: Writer, value: ArtifactFingerprint): void {
    writer.writeUnsigned(value[0]);
}

export function decodeArtifactFingerprint(reader: Reader): ArtifactFingerprint {
    const field0 = reader.readUnsigned();

    return {
        0: field0,
    };
}
