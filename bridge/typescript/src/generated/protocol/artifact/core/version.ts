/* generated by destack_bridge_generate, do not edit */

import { Reader, SerdeError, Writer, compareBytes, nestedBytes } from "../../../../protocol/serde.js";
import type { ArtifactFingerprint } from "./fingerprint.js";
import type { ArtifactKey } from "./key.js";
import { decodeArtifactFingerprint, encodeArtifactFingerprint } from "./fingerprint.js";
import { decodeArtifactKey, encodeArtifactKey } from "./key.js";

/** One exact live artifact version. */
export type ArtifactVersion = {
    /** The semantic artifact slot. */
    readonly key: ArtifactKey;
    /** The exact semantic fingerprint. */
    readonly fingerprint: ArtifactFingerprint;
};

export function encodeArtifactVersion(writer: Writer, value: ArtifactVersion): void {
    encodeArtifactKey(writer, value["key"]);
    encodeArtifactFingerprint(writer, value["fingerprint"]);
}

export function decodeArtifactVersion(reader: Reader): ArtifactVersion {
    const field0 = decodeArtifactKey(reader);
    const field1 = decodeArtifactFingerprint(reader);

    return {
        "key": field0,
        "fingerprint": field1,
    };
}
