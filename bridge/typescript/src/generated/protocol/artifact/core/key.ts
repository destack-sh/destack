/* generated by destack_bridge_generate, do not edit */

import { Reader, SerdeError, Writer, compareBytes, nestedBytes } from "../../../../protocol/serde.js";
import type { ComponentId } from "../../source/file/model/component.js";
import type { ModuleId } from "../../source/file/model/module.js";
import type { PackageId } from "../../source/file/model/package.js";
import type { ProductId } from "../../source/file/model/product.js";
import type { ProfileId } from "../../source/file/model/profile.js";
import type { TargetId } from "../../source/file/model/target.js";
import { decodeComponentId, encodeComponentId } from "../../source/file/model/component.js";
import { decodeModuleId, encodeModuleId } from "../../source/file/model/module.js";
import { decodePackageId, encodePackageId } from "../../source/file/model/package.js";
import { decodeProductId, encodeProductId } from "../../source/file/model/product.js";
import { decodeProfileId, encodeProfileId } from "../../source/file/model/profile.js";
import { decodeTargetId, encodeTargetId } from "../../source/file/model/target.js";

/** Semantic artifact identity. */
export type ArtifactKey =
    /** Toolchain build payload for one target. */
    | {
          readonly kind: "build";
          readonly target: TargetId;
      }
    /** Parsed module DIR. */
    | {
          readonly kind: "dirParsed";
          readonly module: ModuleId;
      }
    /** Parsed non-code module data. */
    | {
          readonly kind: "data";
          readonly module: ModuleId;
      }
    /** Explicit global environment for one profile. */
    | {
          readonly kind: "globalEnvironment";
          readonly profile: ProfileId;
      }
    /** Active dependency index for one profile. */
    | {
          readonly kind: "packageIndex";
          readonly profile: ProfileId;
      }
    /** Strongly connected component partition for one profile. */
    | {
          readonly kind: "componentGraph";
          readonly profile: ProfileId;
      }
    /** Whole-program analysis for one profile and target. */
    | {
          readonly kind: "programAnalysis";
          readonly profile: ProfileId;
          readonly target: TargetId;
      }
    /** Bound DIR. */
    | {
          readonly kind: "dirBound";
          readonly module: ModuleId;
          readonly profile: ProfileId;
      }
    /** Imported DIR. */
    | {
          readonly kind: "dirImported";
          readonly module: ModuleId;
          readonly profile: ProfileId;
      }
    /** Expanded DIR. */
    | {
          readonly kind: "dirExpanded";
          readonly module: ModuleId;
          readonly profile: ProfileId;
      }
    /** Exported DIR. */
    | {
          readonly kind: "dirExported";
          readonly module: ModuleId;
          readonly profile: ProfileId;
      }
    /** Resolved DIR imports. */
    | {
          readonly kind: "dirResolved";
          readonly module: ModuleId;
          readonly profile: ProfileId;
      }
    /** Checked DIR component. */
    | {
          readonly kind: "dirCheckedComponent";
          readonly entry: ModuleId;
          readonly component: ComponentId;
          readonly profile: ProfileId;
      }
    /** Checked DIR facade. */
    | {
          readonly kind: "dirChecked";
          readonly module: ModuleId;
          readonly profile: ProfileId;
      }
    /** Materialized DIR. */
    | {
          readonly kind: "dirMaterialized";
          readonly module: ModuleId;
          readonly profile: ProfileId;
      }
    /** Elaborated DIR. */
    | {
          readonly kind: "dirElaborated";
          readonly module: ModuleId;
          readonly profile: ProfileId;
      }
    /** Lowered MIR before optimization. */
    | {
          readonly kind: "mirLowered";
          readonly module: ModuleId;
          readonly profile: ProfileId;
          readonly target: TargetId;
      }
    /** Verified MIR after required semantic verification. */
    | {
          readonly kind: "mirVerified";
          readonly module: ModuleId;
          readonly profile: ProfileId;
          readonly target: TargetId;
      }
    /** Per-module link summary for whole-program analysis. */
    | {
          readonly kind: "mirAnalyzed";
          readonly module: ModuleId;
          readonly profile: ProfileId;
          readonly target: TargetId;
      }
    /** Optimized MIR. */
    | {
          readonly kind: "mirOptimized";
          readonly module: ModuleId;
          readonly profile: ProfileId;
          readonly target: TargetId;
      }
    /** Query index for one module profile. */
    | {
          readonly kind: "moduleQueryIndex";
          readonly module: ModuleId;
          readonly profile: ProfileId;
      }
    /** Query index for one workspace profile. */
    | {
          readonly kind: "workspaceQueryIndex";
          readonly profile: ProfileId;
      }
    /** One structured linker input for one target. */
    | {
          readonly kind: "script";
          readonly module: ModuleId;
          readonly target: TargetId;
      }
    /** One compiled-code linker input for one target. */
    | {
          readonly kind: "object";
          readonly module: ModuleId;
          readonly target: TargetId;
      }
    /** One opaque linker input for one target. */
    | {
          readonly kind: "asset";
          readonly module: ModuleId;
          readonly target: TargetId;
      }
    /** Linked file graph for one package target. */
    | {
          readonly kind: "bundle";
          readonly package: PackageId;
          readonly target: TargetId;
      }
    /** Program for one package target. */
    | {
          readonly kind: "program";
          readonly package: PackageId;
          readonly target: TargetId;
      }
    /** Linked product assembled from configured target artifacts. */
    | {
          readonly kind: "product";
          readonly package: PackageId;
          readonly product: ProductId;
      }
    /** Realized lint diagnostics for one module profile. */
    | {
          readonly kind: "moduleLinted";
          readonly module: ModuleId;
          readonly profile: ProfileId;
      }
    /** Realized lint diagnostics for one package. */
    | {
          readonly kind: "packageLinted";
          readonly package: PackageId;
      }
    /** Realized lint diagnostics for the workspace. */
    | {
          readonly kind: "workspaceLinted";
      }
;

export const ArtifactKey = {
    /** Toolchain build payload for one target. */
    build(target: TargetId): ArtifactKey {
        return { kind: "build", target };
    },

    /** Parsed module DIR. */
    dirParsed(module: ModuleId): ArtifactKey {
        return { kind: "dirParsed", module };
    },

    /** Parsed non-code module data. */
    data(module: ModuleId): ArtifactKey {
        return { kind: "data", module };
    },

    /** Explicit global environment for one profile. */
    globalEnvironment(profile: ProfileId): ArtifactKey {
        return { kind: "globalEnvironment", profile };
    },

    /** Active dependency index for one profile. */
    packageIndex(profile: ProfileId): ArtifactKey {
        return { kind: "packageIndex", profile };
    },

    /** Strongly connected component partition for one profile. */
    componentGraph(profile: ProfileId): ArtifactKey {
        return { kind: "componentGraph", profile };
    },

    /** Whole-program analysis for one profile and target. */
    programAnalysis(profile: ProfileId, target: TargetId): ArtifactKey {
        return { kind: "programAnalysis", profile, target };
    },

    /** Bound DIR. */
    dirBound(module: ModuleId, profile: ProfileId): ArtifactKey {
        return { kind: "dirBound", module, profile };
    },

    /** Imported DIR. */
    dirImported(module: ModuleId, profile: ProfileId): ArtifactKey {
        return { kind: "dirImported", module, profile };
    },

    /** Expanded DIR. */
    dirExpanded(module: ModuleId, profile: ProfileId): ArtifactKey {
        return { kind: "dirExpanded", module, profile };
    },

    /** Exported DIR. */
    dirExported(module: ModuleId, profile: ProfileId): ArtifactKey {
        return { kind: "dirExported", module, profile };
    },

    /** Resolved DIR imports. */
    dirResolved(module: ModuleId, profile: ProfileId): ArtifactKey {
        return { kind: "dirResolved", module, profile };
    },

    /** Checked DIR component. */
    dirCheckedComponent(entry: ModuleId, component: ComponentId, profile: ProfileId): ArtifactKey {
        return { kind: "dirCheckedComponent", entry, component, profile };
    },

    /** Checked DIR facade. */
    dirChecked(module: ModuleId, profile: ProfileId): ArtifactKey {
        return { kind: "dirChecked", module, profile };
    },

    /** Materialized DIR. */
    dirMaterialized(module: ModuleId, profile: ProfileId): ArtifactKey {
        return { kind: "dirMaterialized", module, profile };
    },

    /** Elaborated DIR. */
    dirElaborated(module: ModuleId, profile: ProfileId): ArtifactKey {
        return { kind: "dirElaborated", module, profile };
    },

    /** Lowered MIR before optimization. */
    mirLowered(module: ModuleId, profile: ProfileId, target: TargetId): ArtifactKey {
        return { kind: "mirLowered", module, profile, target };
    },

    /** Verified MIR after required semantic verification. */
    mirVerified(module: ModuleId, profile: ProfileId, target: TargetId): ArtifactKey {
        return { kind: "mirVerified", module, profile, target };
    },

    /** Per-module link summary for whole-program analysis. */
    mirAnalyzed(module: ModuleId, profile: ProfileId, target: TargetId): ArtifactKey {
        return { kind: "mirAnalyzed", module, profile, target };
    },

    /** Optimized MIR. */
    mirOptimized(module: ModuleId, profile: ProfileId, target: TargetId): ArtifactKey {
        return { kind: "mirOptimized", module, profile, target };
    },

    /** Query index for one module profile. */
    moduleQueryIndex(module: ModuleId, profile: ProfileId): ArtifactKey {
        return { kind: "moduleQueryIndex", module, profile };
    },

    /** Query index for one workspace profile. */
    workspaceQueryIndex(profile: ProfileId): ArtifactKey {
        return { kind: "workspaceQueryIndex", profile };
    },

    /** One structured linker input for one target. */
    script(module: ModuleId, target: TargetId): ArtifactKey {
        return { kind: "script", module, target };
    },

    /** One compiled-code linker input for one target. */
    object(module: ModuleId, target: TargetId): ArtifactKey {
        return { kind: "object", module, target };
    },

    /** One opaque linker input for one target. */
    asset(module: ModuleId, target: TargetId): ArtifactKey {
        return { kind: "asset", module, target };
    },

    /** Linked file graph for one package target. */
    bundle(packageValue: PackageId, target: TargetId): ArtifactKey {
        return { kind: "bundle", package: packageValue, target };
    },

    /** Program for one package target. */
    program(packageValue: PackageId, target: TargetId): ArtifactKey {
        return { kind: "program", package: packageValue, target };
    },

    /** Linked product assembled from configured target artifacts. */
    product(packageValue: PackageId, product: ProductId): ArtifactKey {
        return { kind: "product", package: packageValue, product };
    },

    /** Realized lint diagnostics for one module profile. */
    moduleLinted(module: ModuleId, profile: ProfileId): ArtifactKey {
        return { kind: "moduleLinted", module, profile };
    },

    /** Realized lint diagnostics for one package. */
    packageLinted(packageValue: PackageId): ArtifactKey {
        return { kind: "packageLinted", package: packageValue };
    },

    /** Realized lint diagnostics for the workspace. */
    workspaceLinted(): ArtifactKey {
        return { kind: "workspaceLinted" };
    },

};

export function encodeArtifactKey(writer: Writer, value: ArtifactKey): void {
    switch (value.kind) {
        case "build":
            writer.writeUnsigned(0);
            encodeTargetId(writer, value["target"]);
            return;
        case "dirParsed":
            writer.writeUnsigned(1);
            encodeModuleId(writer, value["module"]);
            return;
        case "data":
            writer.writeUnsigned(2);
            encodeModuleId(writer, value["module"]);
            return;
        case "globalEnvironment":
            writer.writeUnsigned(3);
            encodeProfileId(writer, value["profile"]);
            return;
        case "packageIndex":
            writer.writeUnsigned(4);
            encodeProfileId(writer, value["profile"]);
            return;
        case "componentGraph":
            writer.writeUnsigned(5);
            encodeProfileId(writer, value["profile"]);
            return;
        case "programAnalysis":
            writer.writeUnsigned(6);
            encodeProfileId(writer, value["profile"]);
            encodeTargetId(writer, value["target"]);
            return;
        case "dirBound":
            writer.writeUnsigned(7);
            encodeModuleId(writer, value["module"]);
            encodeProfileId(writer, value["profile"]);
            return;
        case "dirImported":
            writer.writeUnsigned(8);
            encodeModuleId(writer, value["module"]);
            encodeProfileId(writer, value["profile"]);
            return;
        case "dirExpanded":
            writer.writeUnsigned(9);
            encodeModuleId(writer, value["module"]);
            encodeProfileId(writer, value["profile"]);
            return;
        case "dirExported":
            writer.writeUnsigned(10);
            encodeModuleId(writer, value["module"]);
            encodeProfileId(writer, value["profile"]);
            return;
        case "dirResolved":
            writer.writeUnsigned(11);
            encodeModuleId(writer, value["module"]);
            encodeProfileId(writer, value["profile"]);
            return;
        case "dirCheckedComponent":
            writer.writeUnsigned(12);
            encodeModuleId(writer, value["entry"]);
            encodeComponentId(writer, value["component"]);
            encodeProfileId(writer, value["profile"]);
            return;
        case "dirChecked":
            writer.writeUnsigned(13);
            encodeModuleId(writer, value["module"]);
            encodeProfileId(writer, value["profile"]);
            return;
        case "dirMaterialized":
            writer.writeUnsigned(14);
            encodeModuleId(writer, value["module"]);
            encodeProfileId(writer, value["profile"]);
            return;
        case "dirElaborated":
            writer.writeUnsigned(15);
            encodeModuleId(writer, value["module"]);
            encodeProfileId(writer, value["profile"]);
            return;
        case "mirLowered":
            writer.writeUnsigned(16);
            encodeModuleId(writer, value["module"]);
            encodeProfileId(writer, value["profile"]);
            encodeTargetId(writer, value["target"]);
            return;
        case "mirVerified":
            writer.writeUnsigned(17);
            encodeModuleId(writer, value["module"]);
            encodeProfileId(writer, value["profile"]);
            encodeTargetId(writer, value["target"]);
            return;
        case "mirAnalyzed":
            writer.writeUnsigned(18);
            encodeModuleId(writer, value["module"]);
            encodeProfileId(writer, value["profile"]);
            encodeTargetId(writer, value["target"]);
            return;
        case "mirOptimized":
            writer.writeUnsigned(19);
            encodeModuleId(writer, value["module"]);
            encodeProfileId(writer, value["profile"]);
            encodeTargetId(writer, value["target"]);
            return;
        case "moduleQueryIndex":
            writer.writeUnsigned(20);
            encodeModuleId(writer, value["module"]);
            encodeProfileId(writer, value["profile"]);
            return;
        case "workspaceQueryIndex":
            writer.writeUnsigned(21);
            encodeProfileId(writer, value["profile"]);
            return;
        case "script":
            writer.writeUnsigned(22);
            encodeModuleId(writer, value["module"]);
            encodeTargetId(writer, value["target"]);
            return;
        case "object":
            writer.writeUnsigned(23);
            encodeModuleId(writer, value["module"]);
            encodeTargetId(writer, value["target"]);
            return;
        case "asset":
            writer.writeUnsigned(24);
            encodeModuleId(writer, value["module"]);
            encodeTargetId(writer, value["target"]);
            return;
        case "bundle":
            writer.writeUnsigned(25);
            encodePackageId(writer, value["package"]);
            encodeTargetId(writer, value["target"]);
            return;
        case "program":
            writer.writeUnsigned(26);
            encodePackageId(writer, value["package"]);
            encodeTargetId(writer, value["target"]);
            return;
        case "product":
            writer.writeUnsigned(27);
            encodePackageId(writer, value["package"]);
            encodeProductId(writer, value["product"]);
            return;
        case "moduleLinted":
            writer.writeUnsigned(28);
            encodeModuleId(writer, value["module"]);
            encodeProfileId(writer, value["profile"]);
            return;
        case "packageLinted":
            writer.writeUnsigned(29);
            encodePackageId(writer, value["package"]);
            return;
        case "workspaceLinted":
            writer.writeUnsigned(30);
            return;
    }

    throw new SerdeError("unknown enum variant");
}

export function decodeArtifactKey(reader: Reader): ArtifactKey {
    const variant = reader.readNumber();

    switch (variant) {
        case 0: {
            const field0 = decodeTargetId(reader);

            return {
                kind: "build",
                "target": field0,
            };
        }
        case 1: {
            const field0 = decodeModuleId(reader);

            return {
                kind: "dirParsed",
                "module": field0,
            };
        }
        case 2: {
            const field0 = decodeModuleId(reader);

            return {
                kind: "data",
                "module": field0,
            };
        }
        case 3: {
            const field0 = decodeProfileId(reader);

            return {
                kind: "globalEnvironment",
                "profile": field0,
            };
        }
        case 4: {
            const field0 = decodeProfileId(reader);

            return {
                kind: "packageIndex",
                "profile": field0,
            };
        }
        case 5: {
            const field0 = decodeProfileId(reader);

            return {
                kind: "componentGraph",
                "profile": field0,
            };
        }
        case 6: {
            const field0 = decodeProfileId(reader);
            const field1 = decodeTargetId(reader);

            return {
                kind: "programAnalysis",
                "profile": field0,
                "target": field1,
            };
        }
        case 7: {
            const field0 = decodeModuleId(reader);
            const field1 = decodeProfileId(reader);

            return {
                kind: "dirBound",
                "module": field0,
                "profile": field1,
            };
        }
        case 8: {
            const field0 = decodeModuleId(reader);
            const field1 = decodeProfileId(reader);

            return {
                kind: "dirImported",
                "module": field0,
                "profile": field1,
            };
        }
        case 9: {
            const field0 = decodeModuleId(reader);
            const field1 = decodeProfileId(reader);

            return {
                kind: "dirExpanded",
                "module": field0,
                "profile": field1,
            };
        }
        case 10: {
            const field0 = decodeModuleId(reader);
            const field1 = decodeProfileId(reader);

            return {
                kind: "dirExported",
                "module": field0,
                "profile": field1,
            };
        }
        case 11: {
            const field0 = decodeModuleId(reader);
            const field1 = decodeProfileId(reader);

            return {
                kind: "dirResolved",
                "module": field0,
                "profile": field1,
            };
        }
        case 12: {
            const field0 = decodeModuleId(reader);
            const field1 = decodeComponentId(reader);
            const field2 = decodeProfileId(reader);

            return {
                kind: "dirCheckedComponent",
                "entry": field0,
                "component": field1,
                "profile": field2,
            };
        }
        case 13: {
            const field0 = decodeModuleId(reader);
            const field1 = decodeProfileId(reader);

            return {
                kind: "dirChecked",
                "module": field0,
                "profile": field1,
            };
        }
        case 14: {
            const field0 = decodeModuleId(reader);
            const field1 = decodeProfileId(reader);

            return {
                kind: "dirMaterialized",
                "module": field0,
                "profile": field1,
            };
        }
        case 15: {
            const field0 = decodeModuleId(reader);
            const field1 = decodeProfileId(reader);

            return {
                kind: "dirElaborated",
                "module": field0,
                "profile": field1,
            };
        }
        case 16: {
            const field0 = decodeModuleId(reader);
            const field1 = decodeProfileId(reader);
            const field2 = decodeTargetId(reader);

            return {
                kind: "mirLowered",
                "module": field0,
                "profile": field1,
                "target": field2,
            };
        }
        case 17: {
            const field0 = decodeModuleId(reader);
            const field1 = decodeProfileId(reader);
            const field2 = decodeTargetId(reader);

            return {
                kind: "mirVerified",
                "module": field0,
                "profile": field1,
                "target": field2,
            };
        }
        case 18: {
            const field0 = decodeModuleId(reader);
            const field1 = decodeProfileId(reader);
            const field2 = decodeTargetId(reader);

            return {
                kind: "mirAnalyzed",
                "module": field0,
                "profile": field1,
                "target": field2,
            };
        }
        case 19: {
            const field0 = decodeModuleId(reader);
            const field1 = decodeProfileId(reader);
            const field2 = decodeTargetId(reader);

            return {
                kind: "mirOptimized",
                "module": field0,
                "profile": field1,
                "target": field2,
            };
        }
        case 20: {
            const field0 = decodeModuleId(reader);
            const field1 = decodeProfileId(reader);

            return {
                kind: "moduleQueryIndex",
                "module": field0,
                "profile": field1,
            };
        }
        case 21: {
            const field0 = decodeProfileId(reader);

            return {
                kind: "workspaceQueryIndex",
                "profile": field0,
            };
        }
        case 22: {
            const field0 = decodeModuleId(reader);
            const field1 = decodeTargetId(reader);

            return {
                kind: "script",
                "module": field0,
                "target": field1,
            };
        }
        case 23: {
            const field0 = decodeModuleId(reader);
            const field1 = decodeTargetId(reader);

            return {
                kind: "object",
                "module": field0,
                "target": field1,
            };
        }
        case 24: {
            const field0 = decodeModuleId(reader);
            const field1 = decodeTargetId(reader);

            return {
                kind: "asset",
                "module": field0,
                "target": field1,
            };
        }
        case 25: {
            const field0 = decodePackageId(reader);
            const field1 = decodeTargetId(reader);

            return {
                kind: "bundle",
                "package": field0,
                "target": field1,
            };
        }
        case 26: {
            const field0 = decodePackageId(reader);
            const field1 = decodeTargetId(reader);

            return {
                kind: "program",
                "package": field0,
                "target": field1,
            };
        }
        case 27: {
            const field0 = decodePackageId(reader);
            const field1 = decodeProductId(reader);

            return {
                kind: "product",
                "package": field0,
                "product": field1,
            };
        }
        case 28: {
            const field0 = decodeModuleId(reader);
            const field1 = decodeProfileId(reader);

            return {
                kind: "moduleLinted",
                "module": field0,
                "profile": field1,
            };
        }
        case 29: {
            const field0 = decodePackageId(reader);

            return {
                kind: "packageLinted",
                "package": field0,
            };
        }
        case 30: {
            return { kind: "workspaceLinted" };
        }
    }

    throw new SerdeError(`unknown enum variant index: ${variant}`);
}
