/* generated by destack_bridge_generate, do not edit */

import { Reader, SerdeError, Writer, compareBytes, nestedBytes } from "../../protocol/serde.js";

/** Frame header metadata. */
export type FrameHeader = {
    /** Magic bytes for protocol frames. */
    readonly magic: Uint8Array | readonly number[];
    /** Frame format version. */
    readonly version: number;
    /** Reserved flags. */
    readonly flags: number;
    /** Payload length in bytes. */
    readonly payloadLen: number;
};

export function encodeFrameHeader(writer: Writer, value: FrameHeader): void {
    writer.writeBytes(value["magic"]);
    writer.writeUnsigned(value["version"]);
    writer.writeUnsigned(value["flags"]);
    writer.writeUnsigned(value["payloadLen"]);
}

export function decodeFrameHeader(reader: Reader): FrameHeader {
    const field0 = reader.readBytes(4);
    const field1 = reader.readNumber();
    const field2 = reader.readNumber();
    const field3 = reader.readNumber();

    return {
        "magic": field0,
        "version": field1,
        "flags": field2,
        "payloadLen": field3,
    };
}
