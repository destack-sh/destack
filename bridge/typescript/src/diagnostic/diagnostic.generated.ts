/* generated by destack_bridge_generate, do not edit */

import type { BatchEdit } from "./edit.generated.js";
import type { FileContentId } from "../source/file.generated.js";
import type { Span } from "../source/span.generated.js";

/** Diagnostic severity crossing bridge boundaries. */
export type DiagnosticSeverity = "note" | "warning" | "error";

/** Extra semantic diagnostic tag crossing bridge boundaries. */
export type DiagnosticTag = "unnecessary" | "deprecated";

/** Whether a suggestion can be applied automatically. */
export type Applicability = "automatic" | "unsafe" | "dangerous";

/** One concrete source label in a diagnostic. */
export type DiagnosticLabel = {
    /** Exact file content containing the span. */
    readonly content: FileContentId;
    /** Concrete source span. */
    readonly span: Span;
    /** Optional label shown on the span. */
    readonly message?: string;
};

/** Extra context for understanding a diagnostic. */
export type DiagnosticNote = {
    /** Note message. */
    readonly message: string;
};

/** Guidance for fixing or avoiding a diagnostic. */
export type DiagnosticHelp = {
    /** Help message. */
    readonly message: string;
};

/** One suggested source change for a diagnostic. */
export type DiagnosticSuggestion = {
    /** Exact source edits for machine application. */
    readonly edits: BatchEdit;
    /** Source labels to show with the suggestion. */
    readonly labels: readonly DiagnosticLabel[];
    /** Suggestion message. */
    readonly message: string;
    /** Suggestion applicability. */
    readonly applicability: Applicability;
};

/** One final renderable diagnostic. */
export type Diagnostic = {
    /** Stable diagnostic code. */
    readonly code: string;
    /** Diagnostic severity. */
    readonly severity: DiagnosticSeverity;
    /** Diagnostic message. */
    readonly message: string;
    /** Main source label. */
    readonly primary: DiagnosticLabel;
    /** Additional source labels. */
    readonly labels: readonly DiagnosticLabel[];
    /** Extra context. */
    readonly notes: readonly DiagnosticNote[];
    /** Fixing or avoidance guidance. */
    readonly helps: readonly DiagnosticHelp[];
    /** Suggested source changes. */
    readonly suggestions: readonly DiagnosticSuggestion[];
    /** Extra semantic tags. */
    readonly tags: readonly DiagnosticTag[];
};

