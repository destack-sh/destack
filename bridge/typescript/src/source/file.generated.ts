/* generated by destack_bridge_generate, do not edit */

/** External file id crossing bridge boundaries. */
export type FileId = {
    /** Canonical lowercase hex file id. */
    readonly id: string;
};

/** External file content id crossing bridge boundaries. */
export type FileContentId = {
    /** Canonical lowercase hex file content id. */
    readonly id: string;
};

/** Full file content crossing bridge boundaries. */
export type FileContent =
    /** Text file content. */
    | {
          readonly kind: "text";
          /** Text content. */
          readonly content: string;
      }
    /** Binary file content. */
    | {
          readonly kind: "binary";
          /** Binary content. */
          readonly content: Uint8Array | readonly number[];
      }
;

