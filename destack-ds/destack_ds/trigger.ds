use destack::*;

/// A TriggerEvent is an Event that corresponds to a Trigger.
#event struct TriggerEvent {
	trigger: EntityReference<Trigger>,
}

/// A Trigger is a dynamic binding of an Event to a target.
#entity struct Trigger {
	where: Condition,
	target: EntityReference<Entity>,
}