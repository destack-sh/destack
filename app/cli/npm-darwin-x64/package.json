{
    "name": "@destack/cli-darwin-x64",
    "version": "0.55.3",
    "license": "MIT",
    "description": "Destack CLI binaries for darwin x64",
    "os": ["darwin"],
    "cpu": ["x64"],
    "files": ["bin/destack", "bin/ds", "bin/dsc", "bin/dsx"],
    "scripts": {
        "prepublishOnly": "bun ../npm/validate-platform-package.ts",
        "publish:dry": "npm publish --dry-run --access public",
        "publish:live": "npm publish --access public"
    },
    "publishConfig": {
        "access": "public"
    }
}
