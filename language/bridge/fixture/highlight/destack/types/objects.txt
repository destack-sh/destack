// SYNTAX TEST "source.ds"

// types / objects / structural
type UserRecord = { id: UserId; name: string };
// <- keyword.declaration.type.ds

// types / objects / index signatures
type Dictionary = { readonly [key: string]: string };
// <- keyword.declaration.type.ds

// types / objects / mapped keys
type Flags<T> = { readonly [K in keyof T]?: boolean };
// <- keyword.declaration.type.ds

