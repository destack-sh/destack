// SYNTAX TEST "source.tspp"

// memory / moves / owned values through bindings and returns
function take(value: ^User): ^User {
// <- keyword.declaration.function.tspp
    let next: ^User = value;
    return next;
}
