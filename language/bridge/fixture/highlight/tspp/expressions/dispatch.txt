// SYNTAX TEST "source.tspp"

// expressions / dispatch / overloads interfaces operators and unions
function parse(value: string): string {
// <- keyword.declaration.function.tspp
    return value;
}

function parse(value: int32): int32 {
    return value + 1;
}

const parsed = parse("42");
//             ^^^^^ entity.name.function.call.tspp

value satisfies Printable & Serializable;
//    ^^^^^^^^^ keyword.operator.type.binary.tspp

newtype interface Add<T = this> {
// <- keyword.declaration.type.tspp
    type Output;
    add(other: T): this.Output;
}

extension of Vector2 implements Add<Vector2> {
    add(other: Vector2): this.Output {
        return other;
    }
}
