// SYNTAX TEST "source.tspp"

// expressions / literals / scalars strings templates and regex
const scalar = 0b1010 + 0o17 + 0xff + 123n;
//             ^^^^^^ constant.numeric.binary.tspp

const text = "bytes" + "raw" + 'c';
//           ^ punctuation.definition.string.begin.tspp

const label = `user:${id}`;
//            ^ punctuation.definition.string.begin.tspp

const matcher = /[a-z]+\d?/gi;
//              ^ string.regexp.tspp

// expressions / literals / arrays tuples and objects
const items = [head, ...tail];
// <- keyword.declaration.variable.tspp

const object = { kind: "ok", value, next };
