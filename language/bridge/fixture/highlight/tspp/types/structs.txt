// SYNTAX TEST "source.tspp"

// types / structs / assignability and construction
struct Point {
// <- keyword.declaration.type.tspp
    x: int32;
    y: int32;
}

const point = Point { x: 1, y: 2 };
