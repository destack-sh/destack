// SYNTAX TEST "source.tspp"

// types / enums / backing members and nominality
enum Status {
// <- keyword.declaration.type.tspp
    Active = 1,
    Inactive = 2,

    static Default = Status.Active;
}
