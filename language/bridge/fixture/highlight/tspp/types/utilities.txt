// SYNTAX TEST "source.tspp"

// types / utilities / built in type operators
type Patch = Partial<User> & Pick<User, "id" | "name">;
//           ^^^^^^^ support.type.builtin.tspp

type Result = Exclude<keyof Patch, "password">;
//                    ^^^^^ keyword.operator.type.unary.tspp
