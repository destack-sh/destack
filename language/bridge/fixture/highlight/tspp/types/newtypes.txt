// SYNTAX TEST "source.tspp"

// types / newtypes / aliases
newtype UserId = uint64;
// <- keyword.declaration.type.tspp

newtype Rectangle = {
// <- keyword.declaration.type.tspp
    start: Point;
    end: Point;
};

// types / newtypes / discriminated unions
newtype Shape =
    | { kind: "rectangle"; width: int32; height: int32 }
    | { kind: "circle"; radius: int32 };

// types / newtypes / constructors
const id = UserId(1);
//         ^^^^^^ entity.name.function.call.tspp
