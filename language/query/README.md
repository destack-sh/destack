# Query

Semantic language queries for the Destack toolchain.

## Testing

Run these commands from the repository root.

```sh
cargo check -p destack_query
cargo test -p destack_test --test query
```
