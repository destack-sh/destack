// generated by generate-bindings: do not edit

use super::callbacks::{IosHostContactCallbacks, call_ios_contact_callback};
use super::types::{
    HostContactCreateResponse, HostContactDraft, HostContactPageResponse, HostContactQuery,
    HostContactResponse,
};
use crate::host::HOST_STATUS_INVALID_ARGUMENT;
use crate::platform::abi::NativeStringRef;

/// Forward the `list` contact request through the iOS host ABI.
#[unsafe(no_mangle)]
pub(crate) unsafe extern "C" fn destack_host_ios_contact_list(
    session_handle: u64,
    query: HostContactQuery,
    response: *mut HostContactPageResponse,
) -> u32 {
    if response.is_null() {
        return HOST_STATUS_INVALID_ARGUMENT;
    }

    call_ios_contact_callback(
        session_handle,
        |callbacks| callbacks.list,
        |callback| unsafe { callback(session_handle, query, response) },
    )
}

/// Forward the `search` contact request through the iOS host ABI.
#[unsafe(no_mangle)]
pub(crate) unsafe extern "C" fn destack_host_ios_contact_search(
    session_handle: u64,
    query_text: NativeStringRef,
    query: HostContactQuery,
    response: *mut HostContactPageResponse,
) -> u32 {
    if response.is_null() {
        return HOST_STATUS_INVALID_ARGUMENT;
    }

    call_ios_contact_callback(
        session_handle,
        |callbacks| callbacks.search,
        |callback| unsafe { callback(session_handle, query_text, query, response) },
    )
}

/// Forward the `read` contact request through the iOS host ABI.
#[unsafe(no_mangle)]
pub(crate) unsafe extern "C" fn destack_host_ios_contact_read(
    session_handle: u64,
    id: NativeStringRef,
    response: *mut HostContactResponse,
) -> u32 {
    if response.is_null() {
        return HOST_STATUS_INVALID_ARGUMENT;
    }

    call_ios_contact_callback(
        session_handle,
        |callbacks| callbacks.read,
        |callback| unsafe { callback(session_handle, id, response) },
    )
}

/// Forward the `create` contact request through the iOS host ABI.
#[unsafe(no_mangle)]
pub(crate) unsafe extern "C" fn destack_host_ios_contact_create(
    session_handle: u64,
    draft: HostContactDraft,
    response: *mut HostContactCreateResponse,
) -> u32 {
    if response.is_null() {
        return HOST_STATUS_INVALID_ARGUMENT;
    }

    call_ios_contact_callback(
        session_handle,
        |callbacks| callbacks.create,
        |callback| unsafe { callback(session_handle, draft, response) },
    )
}

/// Forward the `update` contact request through the iOS host ABI.
#[unsafe(no_mangle)]
pub(crate) unsafe extern "C" fn destack_host_ios_contact_update(
    session_handle: u64,
    id: NativeStringRef,
    draft: HostContactDraft,
) -> u32 {
    call_ios_contact_callback(
        session_handle,
        |callbacks| callbacks.update,
        |callback| unsafe { callback(session_handle, id, draft) },
    )
}

/// Forward the `delete_contact` contact request through the iOS host ABI.
#[unsafe(no_mangle)]
pub(crate) unsafe extern "C" fn destack_host_ios_contact_delete_contact(
    session_handle: u64,
    id: NativeStringRef,
) -> u32 {
    call_ios_contact_callback(
        session_handle,
        |callbacks| callbacks.delete_contact,
        |callback| unsafe { callback(session_handle, id) },
    )
}
