// generated by generate-bindings: do not edit

use super::callbacks::{IosHostCalendarCallbacks, call_ios_calendar_callback};
use super::types::{
    HostCalendarEventCreateResponse, HostCalendarEventDraft, HostCalendarEventListResponse,
    HostCalendarEventQuery, HostCalendarEventReadResponse, HostCalendarListResponse,
};
use crate::host::HOST_STATUS_INVALID_ARGUMENT;
use crate::platform::abi::NativeStringRef;

/// Forward the `list` calendar request through the iOS host ABI.
#[unsafe(no_mangle)]
pub(crate) unsafe extern "C" fn destack_host_ios_calendar_list(
    session_handle: u64,
    response: *mut HostCalendarListResponse,
) -> u32 {
    if response.is_null() {
        return HOST_STATUS_INVALID_ARGUMENT;
    }

    call_ios_calendar_callback(
        session_handle,
        |callbacks| callbacks.list,
        |callback| unsafe { callback(session_handle, response) },
    )
}

/// Forward the `event_list` calendar request through the iOS host ABI.
#[unsafe(no_mangle)]
pub(crate) unsafe extern "C" fn destack_host_ios_calendar_event_list(
    session_handle: u64,
    query: HostCalendarEventQuery,
    response: *mut HostCalendarEventListResponse,
) -> u32 {
    if response.is_null() {
        return HOST_STATUS_INVALID_ARGUMENT;
    }

    call_ios_calendar_callback(
        session_handle,
        |callbacks| callbacks.event_list,
        |callback| unsafe { callback(session_handle, query, response) },
    )
}

/// Forward the `event_read` calendar request through the iOS host ABI.
#[unsafe(no_mangle)]
pub(crate) unsafe extern "C" fn destack_host_ios_calendar_event_read(
    session_handle: u64,
    id: NativeStringRef,
    response: *mut HostCalendarEventReadResponse,
) -> u32 {
    if response.is_null() {
        return HOST_STATUS_INVALID_ARGUMENT;
    }

    call_ios_calendar_callback(
        session_handle,
        |callbacks| callbacks.event_read,
        |callback| unsafe { callback(session_handle, id, response) },
    )
}

/// Forward the `event_create` calendar request through the iOS host ABI.
#[unsafe(no_mangle)]
pub(crate) unsafe extern "C" fn destack_host_ios_calendar_event_create(
    session_handle: u64,
    draft: HostCalendarEventDraft,
    response: *mut HostCalendarEventCreateResponse,
) -> u32 {
    if response.is_null() {
        return HOST_STATUS_INVALID_ARGUMENT;
    }

    call_ios_calendar_callback(
        session_handle,
        |callbacks| callbacks.event_create,
        |callback| unsafe { callback(session_handle, draft, response) },
    )
}

/// Forward the `event_update` calendar request through the iOS host ABI.
#[unsafe(no_mangle)]
pub(crate) unsafe extern "C" fn destack_host_ios_calendar_event_update(
    session_handle: u64,
    id: NativeStringRef,
    draft: HostCalendarEventDraft,
) -> u32 {
    call_ios_calendar_callback(
        session_handle,
        |callbacks| callbacks.event_update,
        |callback| unsafe { callback(session_handle, id, draft) },
    )
}

/// Forward the `event_delete` calendar request through the iOS host ABI.
#[unsafe(no_mangle)]
pub(crate) unsafe extern "C" fn destack_host_ios_calendar_event_delete(
    session_handle: u64,
    id: NativeStringRef,
) -> u32 {
    call_ios_calendar_callback(
        session_handle,
        |callbacks| callbacks.event_delete,
        |callback| unsafe { callback(session_handle, id) },
    )
}
