// generated by generate-bindings: do not edit

use super::callbacks::{IosHostBackgroundCallbacks, call_ios_background_callback};
use super::types::{
    HostBackgroundCompleteRequest, HostBackgroundListResponse, HostBackgroundStatusResponse,
    HostBackgroundTaskOptions, HostBackgroundTriggerTestRequest, HostBackgroundTriggerTestResponse,
    HostBackgroundUnregisterRequest,
};
use crate::host::HOST_STATUS_INVALID_ARGUMENT;

/// Forward the `status` background request through the iOS host ABI.
#[unsafe(no_mangle)]
pub(crate) unsafe extern "C" fn destack_host_ios_background_status(
    session_handle: u64,
    response: *mut HostBackgroundStatusResponse,
) -> u32 {
    if response.is_null() {
        return HOST_STATUS_INVALID_ARGUMENT;
    }

    call_ios_background_callback(
        session_handle,
        |callbacks| callbacks.status,
        |callback| unsafe { callback(session_handle, response) },
    )
}

/// Forward the `list` background request through the iOS host ABI.
#[unsafe(no_mangle)]
pub(crate) unsafe extern "C" fn destack_host_ios_background_list(
    session_handle: u64,
    response: *mut HostBackgroundListResponse,
) -> u32 {
    if response.is_null() {
        return HOST_STATUS_INVALID_ARGUMENT;
    }

    call_ios_background_callback(
        session_handle,
        |callbacks| callbacks.list,
        |callback| unsafe { callback(session_handle, response) },
    )
}

/// Forward the `register_task` background request through the iOS host ABI.
#[unsafe(no_mangle)]
pub(crate) unsafe extern "C" fn destack_host_ios_background_register_task(
    session_handle: u64,
    request: HostBackgroundTaskOptions,
) -> u32 {
    call_ios_background_callback(
        session_handle,
        |callbacks| callbacks.register_task,
        |callback| unsafe { callback(session_handle, request) },
    )
}

/// Forward the `unregister` background request through the iOS host ABI.
#[unsafe(no_mangle)]
pub(crate) unsafe extern "C" fn destack_host_ios_background_unregister(
    session_handle: u64,
    request: HostBackgroundUnregisterRequest,
) -> u32 {
    call_ios_background_callback(
        session_handle,
        |callbacks| callbacks.unregister,
        |callback| unsafe { callback(session_handle, request) },
    )
}

/// Forward the `trigger_test` background request through the iOS host ABI.
#[unsafe(no_mangle)]
pub(crate) unsafe extern "C" fn destack_host_ios_background_trigger_test(
    session_handle: u64,
    request: HostBackgroundTriggerTestRequest,
    response: *mut HostBackgroundTriggerTestResponse,
) -> u32 {
    if response.is_null() {
        return HOST_STATUS_INVALID_ARGUMENT;
    }

    call_ios_background_callback(
        session_handle,
        |callbacks| callbacks.trigger_test,
        |callback| unsafe { callback(session_handle, request, response) },
    )
}

/// Forward the `complete` background request through the iOS host ABI.
#[unsafe(no_mangle)]
pub(crate) unsafe extern "C" fn destack_host_ios_background_complete(
    session_handle: u64,
    request: HostBackgroundCompleteRequest,
) -> u32 {
    call_ios_background_callback(
        session_handle,
        |callbacks| callbacks.complete,
        |callback| unsafe { callback(session_handle, request) },
    )
}
