// generated by generate-bindings: do not edit

use crate::diagnostic::{RuntimeError, RuntimeResult};
use crate::host::abi::calendar::{
    HostCalendarAccess, HostCalendarAttendee, HostCalendarAvailability, HostCalendarDescriptor,
    HostCalendarEvent, HostCalendarEventCreateResponse, HostCalendarEventDraft,
    HostCalendarEventListResponse, HostCalendarEventQuery, HostCalendarEventReadResponse,
    HostCalendarListResponse, HostCalendarParticipantStatus, HostCalendarRecurrenceFrequency,
    HostCalendarRecurrenceRule, HostCalendarRecurrenceWeekday, HostCalendarReminder,
    HostCalendarReminderKind,
};
use crate::host::core::callback::decode_callback_host_required_string_ref;
use crate::platform::PlatformError;
use crate::platform::abi::NativeStringRef;
use crate::platform::os::abi_generated::{
    CalendarAbsoluteReminderValue, CalendarAccess, CalendarAttendeeValue, CalendarAvailability,
    CalendarDescriptorValue, CalendarEventDraftValue, CalendarEventQueryValue, CalendarEventValue,
    CalendarParticipantStatus, CalendarRecurrenceFrequency, CalendarRecurrenceRuleValue,
    CalendarRecurrenceWeekdayValue, CalendarRelativeReminderValue, CalendarReminderValue,
};
use crate::runtime::BindingCallContext;

/// Encode one calendar event-query payload for the host ABI.
pub(crate) fn encode_calendar_event_query(
    binding: &BindingCallContext,
    query: &CalendarEventQueryValue,
) -> HostCalendarEventQuery {
    HostCalendarEventQuery {
        calendar_ids: binding.store_string_slice(
            query
                .calendar_ids
                .iter()
                .map(NativeStringRef::from)
                .collect(),
        ),
        start_unix_ns: query.start_unix_ns,
        end_unix_ns: query.end_unix_ns,
        has_limit: query.limit.is_some(),
        limit: query.limit.unwrap_or_default(),
        include_canceled: query.include_canceled,
        include_declined: query.include_declined,
        include_recurrence_instances: query.include_recurrence_instances,
    }
}

/// Encode one calendar event-draft payload for the host ABI.
pub(crate) fn encode_calendar_event_draft(
    binding: &BindingCallContext,
    draft: &CalendarEventDraftValue,
) -> HostCalendarEventDraft {
    HostCalendarEventDraft {
        calendar_id: binding.store_string(&draft.calendar_id),
        title: binding.store_string(&draft.title),
        has_notes: draft.notes.is_some(),
        notes: binding.store_string(draft.notes.as_deref().unwrap_or("")),
        has_location: draft.location.is_some(),
        location: binding.store_string(draft.location.as_deref().unwrap_or("")),
        start_unix_ns: draft.start_unix_ns,
        end_unix_ns: draft.end_unix_ns,
        all_day: draft.all_day,
        has_time_zone: draft.time_zone.is_some(),
        time_zone: binding.store_string(draft.time_zone.as_deref().unwrap_or("")),
        availability: encode_calendar_availability(draft.availability),
        has_url: draft.url.is_some(),
        url: binding.store_string(draft.url.as_deref().unwrap_or("")),
        has_recurrence_rule: draft.recurrence_rule.is_some(),
        recurrence_rule: draft
            .recurrence_rule
            .as_ref()
            .map(|rule| encode_calendar_recurrence_rule(binding, rule))
            .unwrap_or_else(|| empty_calendar_recurrence_rule(binding)),
        has_attendees: draft.attendees.is_some(),
        attendees: binding.store_slice(
            draft
                .attendees
                .as_ref()
                .map(|attendees| {
                    attendees
                        .iter()
                        .map(|attendee| encode_calendar_attendee(binding, attendee))
                        .collect()
                })
                .unwrap_or_default(),
        ),
        has_reminders: draft.reminders.is_some(),
        reminders: binding.store_slice(
            draft
                .reminders
                .as_ref()
                .map(|reminders| reminders.iter().map(encode_calendar_reminder).collect())
                .unwrap_or_default(),
        ),
    }
}

/// Decode one host calendar-list response payload.
pub(crate) unsafe fn decode_calendar_list_response(
    response: HostCalendarListResponse,
    operation: &'static str,
) -> RuntimeResult<Vec<CalendarDescriptorValue>> {
    let calendars = unsafe { response.calendars.as_slice()? };
    calendars
        .iter()
        .copied()
        .map(|calendar| decode_calendar_descriptor(calendar, operation))
        .collect()
}

/// Decode one host calendar event-list response payload.
pub(crate) unsafe fn decode_calendar_event_list_response(
    response: HostCalendarEventListResponse,
    operation: &'static str,
) -> RuntimeResult<Vec<CalendarEventValue>> {
    let events = unsafe { response.events.as_slice()? };
    events
        .iter()
        .copied()
        .map(|event| decode_calendar_event(event, operation))
        .collect()
}

/// Decode one host calendar event-read response payload.
pub(crate) unsafe fn decode_calendar_event_read_response(
    response: HostCalendarEventReadResponse,
    operation: &'static str,
) -> RuntimeResult<CalendarEventValue> {
    if !response.has_event {
        return Err(RuntimeError::from(PlatformError::invalid_argument_value(
            "response.event",
            format!("{operation} returned one missing response.event"),
        ))
        .boxed());
    }
    decode_calendar_event(response.event, operation)
}

/// Decode one host calendar event-create response payload.
pub(crate) unsafe fn decode_calendar_event_create_response(
    response: HostCalendarEventCreateResponse,
    operation: &'static str,
) -> RuntimeResult<String> {
    if !response.has_id {
        return Err(RuntimeError::from(PlatformError::invalid_argument_value(
            "response.id",
            format!("{operation} returned one missing response.id"),
        ))
        .boxed());
    }
    decode_callback_host_required_string_ref(
        operation,
        "HostCalendarEventCreateResponse.id",
        response.id,
    )
}

/// Encode one calendar access payload for the host ABI.
fn encode_calendar_access(access: CalendarAccess) -> HostCalendarAccess {
    match access {
        CalendarAccess::Read => HostCalendarAccess::Read,
        CalendarAccess::Write => HostCalendarAccess::Write,
    }
}

/// Decode one host calendar access payload.
fn decode_calendar_access(access: HostCalendarAccess) -> CalendarAccess {
    match access {
        HostCalendarAccess::Read => CalendarAccess::Read,
        HostCalendarAccess::Write => CalendarAccess::Write,
    }
}

/// Encode one calendar availability payload for the host ABI.
fn encode_calendar_availability(availability: CalendarAvailability) -> HostCalendarAvailability {
    match availability {
        CalendarAvailability::Busy => HostCalendarAvailability::Busy,
        CalendarAvailability::Free => HostCalendarAvailability::Free,
        CalendarAvailability::Tentative => HostCalendarAvailability::Tentative,
        CalendarAvailability::OutOfOffice => HostCalendarAvailability::OutOfOffice,
        CalendarAvailability::Unavailable => HostCalendarAvailability::Unavailable,
        CalendarAvailability::Unknown => HostCalendarAvailability::Unknown,
    }
}

/// Decode one host calendar availability payload.
fn decode_calendar_availability(availability: HostCalendarAvailability) -> CalendarAvailability {
    match availability {
        HostCalendarAvailability::Busy => CalendarAvailability::Busy,
        HostCalendarAvailability::Free => CalendarAvailability::Free,
        HostCalendarAvailability::Tentative => CalendarAvailability::Tentative,
        HostCalendarAvailability::OutOfOffice => CalendarAvailability::OutOfOffice,
        HostCalendarAvailability::Unavailable => CalendarAvailability::Unavailable,
        HostCalendarAvailability::Unknown => CalendarAvailability::Unknown,
    }
}

/// Encode one participant-status payload for the host ABI.
fn encode_calendar_participant_status(
    status: CalendarParticipantStatus,
) -> HostCalendarParticipantStatus {
    match status {
        CalendarParticipantStatus::Unknown => HostCalendarParticipantStatus::Unknown,
        CalendarParticipantStatus::Pending => HostCalendarParticipantStatus::Pending,
        CalendarParticipantStatus::Accepted => HostCalendarParticipantStatus::Accepted,
        CalendarParticipantStatus::Tentative => HostCalendarParticipantStatus::Tentative,
        CalendarParticipantStatus::Declined => HostCalendarParticipantStatus::Declined,
        CalendarParticipantStatus::Delegated => HostCalendarParticipantStatus::Delegated,
        CalendarParticipantStatus::Completed => HostCalendarParticipantStatus::Completed,
        CalendarParticipantStatus::InProcess => HostCalendarParticipantStatus::InProcess,
    }
}

/// Decode one host participant-status payload.
fn decode_calendar_participant_status(
    status: HostCalendarParticipantStatus,
) -> CalendarParticipantStatus {
    match status {
        HostCalendarParticipantStatus::Unknown => CalendarParticipantStatus::Unknown,
        HostCalendarParticipantStatus::Pending => CalendarParticipantStatus::Pending,
        HostCalendarParticipantStatus::Accepted => CalendarParticipantStatus::Accepted,
        HostCalendarParticipantStatus::Tentative => CalendarParticipantStatus::Tentative,
        HostCalendarParticipantStatus::Declined => CalendarParticipantStatus::Declined,
        HostCalendarParticipantStatus::Delegated => CalendarParticipantStatus::Delegated,
        HostCalendarParticipantStatus::Completed => CalendarParticipantStatus::Completed,
        HostCalendarParticipantStatus::InProcess => CalendarParticipantStatus::InProcess,
    }
}

/// Encode one recurrence-frequency payload for the host ABI.
fn encode_calendar_recurrence_frequency(
    frequency: CalendarRecurrenceFrequency,
) -> HostCalendarRecurrenceFrequency {
    match frequency {
        CalendarRecurrenceFrequency::Daily => HostCalendarRecurrenceFrequency::Daily,
        CalendarRecurrenceFrequency::Weekly => HostCalendarRecurrenceFrequency::Weekly,
        CalendarRecurrenceFrequency::Monthly => HostCalendarRecurrenceFrequency::Monthly,
        CalendarRecurrenceFrequency::Yearly => HostCalendarRecurrenceFrequency::Yearly,
    }
}

/// Decode one host recurrence-frequency payload.
fn decode_calendar_recurrence_frequency(
    frequency: HostCalendarRecurrenceFrequency,
) -> CalendarRecurrenceFrequency {
    match frequency {
        HostCalendarRecurrenceFrequency::Daily => CalendarRecurrenceFrequency::Daily,
        HostCalendarRecurrenceFrequency::Weekly => CalendarRecurrenceFrequency::Weekly,
        HostCalendarRecurrenceFrequency::Monthly => CalendarRecurrenceFrequency::Monthly,
        HostCalendarRecurrenceFrequency::Yearly => CalendarRecurrenceFrequency::Yearly,
    }
}

/// Encode one calendar recurrence-weekday payload.
fn encode_calendar_recurrence_weekday(
    weekday: CalendarRecurrenceWeekdayValue,
) -> HostCalendarRecurrenceWeekday {
    HostCalendarRecurrenceWeekday {
        day: weekday.day,
        has_week_number: weekday.week_number.is_some(),
        week_number: weekday.week_number.unwrap_or_default(),
    }
}

/// Decode one host calendar recurrence-weekday payload.
fn decode_calendar_recurrence_weekday(
    weekday: HostCalendarRecurrenceWeekday,
) -> CalendarRecurrenceWeekdayValue {
    CalendarRecurrenceWeekdayValue {
        day: weekday.day,
        week_number: weekday.has_week_number.then_some(weekday.week_number),
    }
}

/// Encode one calendar recurrence-rule payload.
fn encode_calendar_recurrence_rule(
    binding: &BindingCallContext,
    rule: &CalendarRecurrenceRuleValue,
) -> HostCalendarRecurrenceRule {
    HostCalendarRecurrenceRule {
        frequency: encode_calendar_recurrence_frequency(rule.frequency),
        interval: rule.interval,
        has_count: rule.count.is_some(),
        count: rule.count.unwrap_or_default(),
        has_until_unix_ns: rule.until_unix_ns.is_some(),
        until_unix_ns: rule.until_unix_ns.unwrap_or_default(),
        by_week_days: binding.store_slice(rule.by_week_days.clone()),
        by_weekday_ordinals: binding.store_slice(
            rule.by_weekday_ordinals
                .iter()
                .copied()
                .map(encode_calendar_recurrence_weekday)
                .collect(),
        ),
        by_month_days: binding.store_slice(rule.by_month_days.clone()),
        by_months: binding.store_slice(rule.by_months.clone()),
        by_year_days: binding.store_slice(rule.by_year_days.clone()),
        by_week_numbers: binding.store_slice(rule.by_week_numbers.clone()),
        by_set_positions: binding.store_slice(rule.by_set_positions.clone()),
    }
}

/// Decode one host calendar recurrence-rule payload.
unsafe fn decode_calendar_recurrence_rule(
    rule: HostCalendarRecurrenceRule,
) -> RuntimeResult<CalendarRecurrenceRuleValue> {
    let by_week_days = unsafe { rule.by_week_days.as_slice()? }.to_vec();
    let by_weekday_ordinals = unsafe { rule.by_weekday_ordinals.as_slice()? }
        .iter()
        .copied()
        .map(decode_calendar_recurrence_weekday)
        .collect();
    let by_month_days = unsafe { rule.by_month_days.as_slice()? }.to_vec();
    let by_months = unsafe { rule.by_months.as_slice()? }.to_vec();
    let by_year_days = unsafe { rule.by_year_days.as_slice()? }.to_vec();
    let by_week_numbers = unsafe { rule.by_week_numbers.as_slice()? }.to_vec();
    let by_set_positions = unsafe { rule.by_set_positions.as_slice()? }.to_vec();
    Ok(CalendarRecurrenceRuleValue {
        frequency: decode_calendar_recurrence_frequency(rule.frequency),
        interval: rule.interval,
        count: rule.has_count.then_some(rule.count),
        until_unix_ns: rule.has_until_unix_ns.then_some(rule.until_unix_ns),
        by_week_days,
        by_weekday_ordinals,
        by_month_days,
        by_months,
        by_year_days,
        by_week_numbers,
        by_set_positions,
    })
}

/// Return one empty calendar recurrence-rule payload.
fn empty_calendar_recurrence_rule(binding: &BindingCallContext) -> HostCalendarRecurrenceRule {
    HostCalendarRecurrenceRule {
        frequency: HostCalendarRecurrenceFrequency::Daily,
        interval: 0,
        has_count: false,
        count: 0,
        has_until_unix_ns: false,
        until_unix_ns: 0,
        by_week_days: binding.store_slice(Vec::new()),
        by_weekday_ordinals: binding.store_slice(Vec::new()),
        by_month_days: binding.store_slice(Vec::new()),
        by_months: binding.store_slice(Vec::new()),
        by_year_days: binding.store_slice(Vec::new()),
        by_week_numbers: binding.store_slice(Vec::new()),
        by_set_positions: binding.store_slice(Vec::new()),
    }
}

/// Encode one calendar attendee payload.
fn encode_calendar_attendee(
    binding: &BindingCallContext,
    attendee: &CalendarAttendeeValue,
) -> HostCalendarAttendee {
    HostCalendarAttendee {
        has_id: attendee.id.is_some(),
        id: binding.store_string(attendee.id.as_deref().unwrap_or("")),
        has_name: attendee.name.is_some(),
        name: binding.store_string(attendee.name.as_deref().unwrap_or("")),
        has_email: attendee.email.is_some(),
        email: binding.store_string(attendee.email.as_deref().unwrap_or("")),
        optional: attendee.optional,
        organizer: attendee.organizer,
        response_status: encode_calendar_participant_status(attendee.response_status),
    }
}

/// Decode one host calendar attendee payload.
fn decode_calendar_attendee(
    attendee: HostCalendarAttendee,
    operation: &'static str,
) -> RuntimeResult<CalendarAttendeeValue> {
    Ok(CalendarAttendeeValue {
        id: decode_optional_string_ref(
            operation,
            "HostCalendarAttendee.id",
            attendee.has_id,
            attendee.id,
        )?,
        name: decode_optional_string_ref(
            operation,
            "HostCalendarAttendee.name",
            attendee.has_name,
            attendee.name,
        )?,
        email: decode_optional_string_ref(
            operation,
            "HostCalendarAttendee.email",
            attendee.has_email,
            attendee.email,
        )?,
        optional: attendee.optional,
        organizer: attendee.organizer,
        response_status: decode_calendar_participant_status(attendee.response_status),
    })
}

/// Encode one calendar reminder payload.
fn encode_calendar_reminder(reminder: &CalendarReminderValue) -> HostCalendarReminder {
    match reminder {
        CalendarReminderValue::CalendarAbsoluteReminder(value) => HostCalendarReminder {
            kind: HostCalendarReminderKind::Absolute,
            absolute_unix_ns: value.absolute_unix_ns,
            minutes_before_start: 0,
        },
        CalendarReminderValue::CalendarRelativeReminder(value) => HostCalendarReminder {
            kind: HostCalendarReminderKind::Relative,
            absolute_unix_ns: 0,
            minutes_before_start: value.minutes_before_start,
        },
    }
}

/// Decode one host calendar reminder payload.
fn decode_calendar_reminder(reminder: HostCalendarReminder) -> CalendarReminderValue {
    match reminder.kind {
        HostCalendarReminderKind::Absolute => {
            CalendarReminderValue::CalendarAbsoluteReminder(CalendarAbsoluteReminderValue {
                kind: "absolute".to_string(),
                absolute_unix_ns: reminder.absolute_unix_ns,
            })
        }
        HostCalendarReminderKind::Relative => {
            CalendarReminderValue::CalendarRelativeReminder(CalendarRelativeReminderValue {
                kind: "relative".to_string(),
                minutes_before_start: reminder.minutes_before_start,
            })
        }
    }
}

/// Decode one host calendar descriptor payload.
fn decode_calendar_descriptor(
    descriptor: HostCalendarDescriptor,
    operation: &'static str,
) -> RuntimeResult<CalendarDescriptorValue> {
    Ok(CalendarDescriptorValue {
        id: decode_callback_host_required_string_ref(
            operation,
            "HostCalendarDescriptor.id",
            descriptor.id,
        )?,
        title: decode_callback_host_required_string_ref(
            operation,
            "HostCalendarDescriptor.title",
            descriptor.title,
        )?,
        source: decode_callback_host_required_string_ref(
            operation,
            "HostCalendarDescriptor.source",
            descriptor.source,
        )?,
        owner: decode_optional_string_ref(
            operation,
            "HostCalendarDescriptor.owner",
            descriptor.has_owner,
            descriptor.owner,
        )?,
        color_argb: descriptor.color_argb,
        primary: descriptor.primary,
        access: decode_calendar_access(descriptor.access),
    })
}

/// Decode one host calendar event payload.
fn decode_calendar_event(
    event: HostCalendarEvent,
    operation: &'static str,
) -> RuntimeResult<CalendarEventValue> {
    let recurrence_rule = if event.has_recurrence_rule {
        Some(unsafe { decode_calendar_recurrence_rule(event.recurrence_rule)? })
    } else {
        None
    };
    let attendees = if event.has_attendees {
        Some(
            unsafe { event.attendees.as_slice()? }
                .iter()
                .copied()
                .map(|attendee| decode_calendar_attendee(attendee, operation))
                .collect::<RuntimeResult<Vec<_>>>()?,
        )
    } else {
        None
    };
    let reminders = if event.has_reminders {
        Some(
            unsafe { event.reminders.as_slice()? }
                .iter()
                .copied()
                .map(decode_calendar_reminder)
                .collect(),
        )
    } else {
        None
    };
    Ok(CalendarEventValue {
        id: decode_callback_host_required_string_ref(operation, "HostCalendarEvent.id", event.id)?,
        calendar_id: decode_callback_host_required_string_ref(
            operation,
            "HostCalendarEvent.calendar_id",
            event.calendar_id,
        )?,
        title: decode_callback_host_required_string_ref(
            operation,
            "HostCalendarEvent.title",
            event.title,
        )?,
        notes: decode_optional_string_ref(
            operation,
            "HostCalendarEvent.notes",
            event.has_notes,
            event.notes,
        )?,
        location: decode_optional_string_ref(
            operation,
            "HostCalendarEvent.location",
            event.has_location,
            event.location,
        )?,
        start_unix_ns: event.start_unix_ns,
        end_unix_ns: event.end_unix_ns,
        all_day: event.all_day,
        canceled: event.canceled,
        time_zone: decode_optional_string_ref(
            operation,
            "HostCalendarEvent.time_zone",
            event.has_time_zone,
            event.time_zone,
        )?,
        availability: decode_calendar_availability(event.availability),
        url: decode_optional_string_ref(
            operation,
            "HostCalendarEvent.url",
            event.has_url,
            event.url,
        )?,
        organizer_name: decode_optional_string_ref(
            operation,
            "HostCalendarEvent.organizer_name",
            event.has_organizer_name,
            event.organizer_name,
        )?,
        organizer_email: decode_optional_string_ref(
            operation,
            "HostCalendarEvent.organizer_email",
            event.has_organizer_email,
            event.organizer_email,
        )?,
        recurring: event.recurring,
        recurrence_master_id: decode_optional_string_ref(
            operation,
            "HostCalendarEvent.recurrence_master_id",
            event.has_recurrence_master_id,
            event.recurrence_master_id,
        )?,
        recurrence_id_unix_ns: event
            .has_recurrence_id_unix_ns
            .then_some(event.recurrence_id_unix_ns),
        recurrence_rule,
        attendees,
        reminders,
    })
}

/// Decode one optional host string reference.
fn decode_optional_string_ref(
    operation: &'static str,
    field_name: &'static str,
    is_present: bool,
    value: crate::platform::abi::NativeStringRef,
) -> RuntimeResult<Option<String>> {
    if !is_present {
        return Ok(None);
    }
    decode_callback_host_required_string_ref(operation, field_name, value).map(Some)
}
