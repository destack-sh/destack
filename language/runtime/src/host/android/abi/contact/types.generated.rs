// generated by generate-bindings: do not edit

#![allow(dead_code)]

pub(crate) use super::callbacks::*;

use crate::platform::abi::{NativeSlice, NativeStringRef};

/// One host contact query payload.
#[derive(Clone, Copy, Debug)]
#[repr(C)]
pub(crate) struct HostContactQuery {
    /// The opaque cursor from one prior list or search call.
    pub cursor: crate::host::abi::core::HostAbiOptional<NativeStringRef>,
    /// The maximum returned contacts for this page.
    pub limit: crate::host::abi::core::HostAbiOptional<u32>,
    /// Whether phone values should be returned.
    pub include_phones: bool,
    /// Whether email values should be returned.
    pub include_emails: bool,
    /// Whether postal-address values should be returned.
    pub include_addresses: bool,
    /// Whether organization metadata should be returned.
    pub include_organization: bool,
    /// Whether note fields should be returned.
    pub include_notes: bool,
}

/// One host contact-name payload.
#[derive(Clone, Copy, Debug)]
#[repr(C)]
pub(crate) struct HostContactName {
    /// The given or first name.
    pub given_name: NativeStringRef,
    /// The middle name.
    pub middle_name: NativeStringRef,
    /// The family or last name.
    pub family_name: NativeStringRef,
    /// The honorific prefix.
    pub prefix: NativeStringRef,
    /// The honorific suffix.
    pub suffix: NativeStringRef,
    /// The nickname.
    pub nickname: NativeStringRef,
    /// The phonetic given name.
    pub phonetic_given_name: NativeStringRef,
    /// The phonetic family name.
    pub phonetic_family_name: NativeStringRef,
}

/// One host contact-phone payload.
#[derive(Clone, Copy, Debug)]
#[repr(C)]
pub(crate) struct HostContactPhone {
    /// The user-visible label for this phone value.
    pub label: NativeStringRef,
    /// The original phone number string.
    pub number: NativeStringRef,
    /// The normalized phone number string when available.
    pub normalized_number: NativeStringRef,
    /// Whether this phone value is marked as primary.
    pub primary: bool,
}

/// One host contact-email payload.
#[derive(Clone, Copy, Debug)]
#[repr(C)]
pub(crate) struct HostContactEmail {
    /// The user-visible label for this email value.
    pub label: NativeStringRef,
    /// The email address.
    pub address: NativeStringRef,
    /// Whether this email value is marked as primary.
    pub primary: bool,
}

/// One host contact-address payload.
#[derive(Clone, Copy, Debug)]
#[repr(C)]
pub(crate) struct HostContactAddress {
    /// The user-visible label for this address value.
    pub label: NativeStringRef,
    /// The street-line payload.
    pub street: NativeStringRef,
    /// The city payload.
    pub city: NativeStringRef,
    /// The region or state payload.
    pub region: NativeStringRef,
    /// The postal-code payload.
    pub postal_code: NativeStringRef,
    /// The country payload.
    pub country: NativeStringRef,
    /// The country-code payload.
    pub country_code: NativeStringRef,
}

/// One host contact-organization payload.
#[derive(Clone, Copy, Debug)]
#[repr(C)]
pub(crate) struct HostContactOrganization {
    /// The company or organization name.
    pub company: NativeStringRef,
    /// The department name.
    pub department: NativeStringRef,
    /// The job title.
    pub title: NativeStringRef,
}

/// One host contact payload.
#[derive(Clone, Copy, Debug)]
#[repr(C)]
pub(crate) struct HostContact {
    /// The stable host contact identifier.
    pub id: NativeStringRef,
    /// The structured name payload.
    pub name: HostContactName,
    /// The phone values.
    pub phones: NativeSlice<HostContactPhone>,
    /// The email values.
    pub emails: NativeSlice<HostContactEmail>,
    /// The address values.
    pub addresses: NativeSlice<HostContactAddress>,
    /// The organization metadata.
    pub organization: HostContactOrganization,
    /// The contact note payload.
    pub note: NativeStringRef,
}

/// One host contact draft payload.
#[derive(Clone, Copy, Debug)]
#[repr(C)]
pub(crate) struct HostContactDraft {
    /// The structured name payload.
    pub name: HostContactName,
    /// The phone values.
    pub phones: NativeSlice<HostContactPhone>,
    /// The email values.
    pub emails: NativeSlice<HostContactEmail>,
    /// The address values.
    pub addresses: NativeSlice<HostContactAddress>,
    /// The organization metadata.
    pub organization: HostContactOrganization,
    /// The contact note payload.
    pub note: NativeStringRef,
}

/// One host contact page payload.
#[derive(Clone, Copy, Debug)]
#[repr(C)]
pub(crate) struct HostContactPage {
    /// The listed contacts for this page.
    pub contacts: NativeSlice<HostContact>,
    /// The opaque next-page cursor.
    pub next_cursor: NativeStringRef,
    /// Whether more contacts are available.
    pub has_more: bool,
}

/// One host contact-page response payload.
#[derive(Clone, Copy, Debug)]
#[repr(C)]
pub(crate) struct HostContactPageResponse {
    /// The request status code.
    pub status: u32,
    /// The returned contact page when available.
    pub page: crate::host::abi::core::HostAbiOptional<HostContactPage>,
}

/// One host contact-read response payload.
#[derive(Clone, Copy, Debug)]
#[repr(C)]
pub(crate) struct HostContactResponse {
    /// The request status code.
    pub status: u32,
    /// The returned contact when available.
    pub contact: crate::host::abi::core::HostAbiOptional<HostContact>,
}

/// One host contact-create response payload.
#[derive(Clone, Copy, Debug)]
#[repr(C)]
pub(crate) struct HostContactCreateResponse {
    /// The request status code.
    pub status: u32,
    /// The created contact identifier when available.
    pub id: crate::host::abi::core::HostAbiOptional<NativeStringRef>,
}
