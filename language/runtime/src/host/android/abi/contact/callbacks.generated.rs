// generated by generate-bindings: do not edit

use super::types::{
    HostContactCreateResponse, HostContactDraft, HostContactPageResponse, HostContactQuery,
    HostContactResponse,
};
use crate::host::android::abi::binding::invoke_android_binding_callback;
use crate::platform::abi::NativeStringRef;

/// The host callback for `list`.
pub(crate) type AndroidHostContactListCallback = unsafe extern "C" fn(
    session_handle: u64,
    query: HostContactQuery,
    response: *mut HostContactPageResponse,
) -> u32;

/// The host callback for `search`.
pub(crate) type AndroidHostContactSearchCallback = unsafe extern "C" fn(
    session_handle: u64,
    query_text: NativeStringRef,
    query: HostContactQuery,
    response: *mut HostContactPageResponse,
) -> u32;

/// The host callback for `read`.
pub(crate) type AndroidHostContactReadCallback = unsafe extern "C" fn(
    session_handle: u64,
    id: NativeStringRef,
    response: *mut HostContactResponse,
) -> u32;

/// The host callback for `create`.
pub(crate) type AndroidHostContactCreateCallback = unsafe extern "C" fn(
    session_handle: u64,
    draft: HostContactDraft,
    response: *mut HostContactCreateResponse,
) -> u32;

/// The host callback for `update`.
pub(crate) type AndroidHostContactUpdateCallback =
    unsafe extern "C" fn(session_handle: u64, id: NativeStringRef, draft: HostContactDraft) -> u32;

/// The host callback for `delete_contact`.
pub(crate) type AndroidHostContactDeleteContactCallback =
    unsafe extern "C" fn(session_handle: u64, id: NativeStringRef) -> u32;

/// Callback table for Android host contact request interop.
#[derive(Clone, Copy, Debug, Default)]
#[repr(C)]
pub(crate) struct AndroidHostContactCallbacks {
    /// The callback for `list`.
    pub list: Option<AndroidHostContactListCallback>,
    /// The callback for `search`.
    pub search: Option<AndroidHostContactSearchCallback>,
    /// The callback for `read`.
    pub read: Option<AndroidHostContactReadCallback>,
    /// The callback for `create`.
    pub create: Option<AndroidHostContactCreateCallback>,
    /// The callback for `update`.
    pub update: Option<AndroidHostContactUpdateCallback>,
    /// The callback for `delete_contact`.
    pub delete_contact: Option<AndroidHostContactDeleteContactCallback>,
}

/// Resolve and invoke one Android host contact callback.
pub(super) fn call_android_contact_callback<T: Copy>(
    session_handle: u64,
    resolve: impl FnOnce(&AndroidHostContactCallbacks) -> Option<T>,
    invoke: impl FnOnce(T) -> u32,
) -> u32 {
    invoke_android_binding_callback(
        session_handle,
        |bindings| resolve(&bindings.contact),
        invoke,
    )
}
