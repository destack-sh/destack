// generated by generate-bindings: do not edit

use super::callbacks::{call_android_usb_callback, AndroidHostUsbCallbacks};
use crate::host::abi::usb::{AndroidHostUsbDeviceDescriptorHeader, AndroidHostUsbHotplugEventHeader};
use crate::host::core::HOST_STATUS_INVALID_ARGUMENT;
use crate::platform::abi::{NativeSlice};
use crate::platform::abi::NativeStringRef;

/// Forward the `device_list` usb request through the Android host ABI.
#[unsafe(no_mangle)]
pub(crate) unsafe extern "C" fn destack_host_android_usb_device_list(
    session_handle: u64,
    devices: NativeSlice<AndroidHostUsbDeviceDescriptorHeader>,
    device_count_written: *mut u32,
    string_bytes: NativeSlice<u8>,
    string_bytes_written: *mut u32,
    port_bytes: NativeSlice<u8>,
    port_bytes_written: *mut u32,
) -> u32 {
    if device_count_written.is_null() {
        return HOST_STATUS_INVALID_ARGUMENT;
    }

    if string_bytes_written.is_null() {
        return HOST_STATUS_INVALID_ARGUMENT;
    }

    if port_bytes_written.is_null() {
        return HOST_STATUS_INVALID_ARGUMENT;
    }

    call_android_usb_callback(
        session_handle,
        |callbacks| callbacks.device_list,
        |callback| unsafe { callback(session_handle, devices, device_count_written, string_bytes, string_bytes_written, port_bytes, port_bytes_written) },
    )
}

/// Forward the `watch_open` usb request through the Android host ABI.
#[unsafe(no_mangle)]
pub(crate) unsafe extern "C" fn destack_host_android_usb_watch_open(
    session_handle: u64,
    watch_id: *mut u64,
) -> u32 {
    if watch_id.is_null() {
        return HOST_STATUS_INVALID_ARGUMENT;
    }

    call_android_usb_callback(
        session_handle,
        |callbacks| callbacks.watch_open,
        |callback| unsafe { callback(session_handle, watch_id) },
    )
}

/// Forward the `watch_close` usb request through the Android host ABI.
#[unsafe(no_mangle)]
pub(crate) unsafe extern "C" fn destack_host_android_usb_watch_close(
    session_handle: u64,
    watch_id: u64,
) -> u32 {
    call_android_usb_callback(
        session_handle,
        |callbacks| callbacks.watch_close,
        |callback| unsafe { callback(session_handle, watch_id) },
    )
}

/// Forward the `watch_read` usb request through the Android host ABI.
#[unsafe(no_mangle)]
pub(crate) unsafe extern "C" fn destack_host_android_usb_watch_read(
    session_handle: u64,
    watch_id: u64,
    timeout_ns: u64,
    event: *mut AndroidHostUsbHotplugEventHeader,
    string_bytes: NativeSlice<u8>,
    string_bytes_written: *mut u32,
    port_bytes: NativeSlice<u8>,
    port_bytes_written: *mut u32,
) -> u32 {
    if event.is_null() {
        return HOST_STATUS_INVALID_ARGUMENT;
    }

    if string_bytes_written.is_null() {
        return HOST_STATUS_INVALID_ARGUMENT;
    }

    if port_bytes_written.is_null() {
        return HOST_STATUS_INVALID_ARGUMENT;
    }

    call_android_usb_callback(
        session_handle,
        |callbacks| callbacks.watch_read,
        |callback| unsafe { callback(session_handle, watch_id, timeout_ns, event, string_bytes, string_bytes_written, port_bytes, port_bytes_written) },
    )
}

/// Forward the `watch_try_read` usb request through the Android host ABI.
#[unsafe(no_mangle)]
pub(crate) unsafe extern "C" fn destack_host_android_usb_watch_try_read(
    session_handle: u64,
    watch_id: u64,
    event: *mut AndroidHostUsbHotplugEventHeader,
    string_bytes: NativeSlice<u8>,
    string_bytes_written: *mut u32,
    port_bytes: NativeSlice<u8>,
    port_bytes_written: *mut u32,
) -> u32 {
    if event.is_null() {
        return HOST_STATUS_INVALID_ARGUMENT;
    }

    if string_bytes_written.is_null() {
        return HOST_STATUS_INVALID_ARGUMENT;
    }

    if port_bytes_written.is_null() {
        return HOST_STATUS_INVALID_ARGUMENT;
    }

    call_android_usb_callback(
        session_handle,
        |callbacks| callbacks.watch_try_read,
        |callback| unsafe { callback(session_handle, watch_id, event, string_bytes, string_bytes_written, port_bytes, port_bytes_written) },
    )
}

/// Forward the `open` usb request through the Android host ABI.
#[unsafe(no_mangle)]
pub(crate) unsafe extern "C" fn destack_host_android_usb_open(
    session_handle: u64,
    id: NativeStringRef,
    file_descriptor: *mut i32,
) -> u32 {
    if file_descriptor.is_null() {
        return HOST_STATUS_INVALID_ARGUMENT;
    }

    call_android_usb_callback(
        session_handle,
        |callbacks| callbacks.open,
        |callback| unsafe { callback(session_handle, id, file_descriptor) },
    )
}
