// generated by generate-bindings: do not edit

use super::callbacks::{AndroidHostCryptoCallbacks, call_android_crypto_callback};
use crate::host::android::abi::binding::resolve_android_binding_callback;
use crate::host::{HOST_STATUS_INVALID_ARGUMENT, HOST_STATUS_NOT_SUPPORTED, HOST_STATUS_OK};
use crate::platform::abi::{NativeSlice, NativeStringRef};

/// Forward the `supports_hardware_key` crypto request through the Android host ABI.
#[unsafe(no_mangle)]
pub(crate) unsafe extern "C" fn destack_host_android_crypto_supports_hardware_key(
    session_handle: u64,
    store_kind: u32,
) -> u32 {
    call_android_crypto_callback(
        session_handle,
        |callbacks| callbacks.supports_hardware_key,
        |callback| unsafe { callback(session_handle, store_kind) },
    )
}

/// Forward the `generate_hardware_key_pair` crypto request through the Android host ABI.
#[unsafe(no_mangle)]
pub(crate) unsafe extern "C" fn destack_host_android_crypto_generate_hardware_key_pair(
    session_handle: u64,
    store_kind: u32,
    key_algorithm: u32,
    named_curve: u32,
    modulus_bits: u32,
    public_exponent: u32,
    key_label: NativeStringRef,
) -> u32 {
    call_android_crypto_callback(
        session_handle,
        |callbacks| callbacks.generate_hardware_key_pair,
        |callback| unsafe {
            callback(
                session_handle,
                store_kind,
                key_algorithm,
                named_curve,
                modulus_bits,
                public_exponent,
                key_label,
            )
        },
    )
}

/// Forward the `generate_hardware_secret_key` crypto request through the Android host ABI.
#[unsafe(no_mangle)]
pub(crate) unsafe extern "C" fn destack_host_android_crypto_generate_hardware_secret_key(
    session_handle: u64,
    store_kind: u32,
    key_algorithm: u32,
    digest_algorithm: u32,
    key_size_bits: u32,
    key_usage_mask: u32,
    key_label: NativeStringRef,
) -> u32 {
    call_android_crypto_callback(
        session_handle,
        |callbacks| callbacks.generate_hardware_secret_key,
        |callback| unsafe {
            callback(
                session_handle,
                store_kind,
                key_algorithm,
                digest_algorithm,
                key_size_bits,
                key_usage_mask,
                key_label,
            )
        },
    )
}

/// Forward the `export_hardware_public_key` crypto request through the Android host ABI.
#[unsafe(no_mangle)]
pub(crate) unsafe extern "C" fn destack_host_android_crypto_export_hardware_public_key(
    session_handle: u64,
    key_algorithm: u32,
    key_label: NativeStringRef,
    output: NativeSlice<u8>,
    output_written: *mut u32,
) -> u32 {
    if output_written.is_null() {
        return HOST_STATUS_INVALID_ARGUMENT;
    }

    call_android_crypto_callback(
        session_handle,
        |callbacks| callbacks.export_hardware_public_key,
        |callback| unsafe {
            callback(
                session_handle,
                key_algorithm,
                key_label,
                output,
                output_written,
            )
        },
    )
}

/// Forward the `sign_hardware_key` crypto request through the Android host ABI.
#[unsafe(no_mangle)]
pub(crate) unsafe extern "C" fn destack_host_android_crypto_sign_hardware_key(
    session_handle: u64,
    key_algorithm: u32,
    key_label: NativeStringRef,
    signature_algorithm: u32,
    digest_algorithm: u32,
    salt_length_bytes: u32,
    payload: NativeSlice<u8>,
    output_signature: NativeSlice<u8>,
    output_written: *mut u32,
) -> u32 {
    if output_written.is_null() {
        return HOST_STATUS_INVALID_ARGUMENT;
    }

    call_android_crypto_callback(
        session_handle,
        |callbacks| callbacks.sign_hardware_key,
        |callback| unsafe {
            callback(
                session_handle,
                key_algorithm,
                key_label,
                signature_algorithm,
                digest_algorithm,
                salt_length_bytes,
                payload,
                output_signature,
                output_written,
            )
        },
    )
}

/// Forward the `decrypt_hardware_key` crypto request through the Android host ABI.
#[unsafe(no_mangle)]
pub(crate) unsafe extern "C" fn destack_host_android_crypto_decrypt_hardware_key(
    session_handle: u64,
    key_algorithm: u32,
    key_label: NativeStringRef,
    encryption_algorithm: u32,
    digest_algorithm: u32,
    label: NativeSlice<u8>,
    payload: NativeSlice<u8>,
    output_plaintext: NativeSlice<u8>,
    output_written: *mut u32,
) -> u32 {
    if output_written.is_null() {
        return HOST_STATUS_INVALID_ARGUMENT;
    }

    call_android_crypto_callback(
        session_handle,
        |callbacks| callbacks.decrypt_hardware_key,
        |callback| unsafe {
            callback(
                session_handle,
                key_algorithm,
                key_label,
                encryption_algorithm,
                digest_algorithm,
                label,
                payload,
                output_plaintext,
                output_written,
            )
        },
    )
}

/// Forward the `encrypt_hardware_secret_key` crypto request through the Android host ABI.
#[unsafe(no_mangle)]
pub(crate) unsafe extern "C" fn destack_host_android_crypto_encrypt_hardware_secret_key(
    session_handle: u64,
    key_algorithm: u32,
    key_label: NativeStringRef,
    cipher_algorithm: u32,
    nonce: NativeSlice<u8>,
    additional_data: NativeSlice<u8>,
    tag_length_bytes: u32,
    payload: NativeSlice<u8>,
    output_ciphertext: NativeSlice<u8>,
    output_tag: NativeSlice<u8>,
    output_ciphertext_written: *mut u32,
    output_tag_written: *mut u32,
) -> u32 {
    if output_ciphertext_written.is_null() {
        return HOST_STATUS_INVALID_ARGUMENT;
    }

    if output_tag_written.is_null() {
        return HOST_STATUS_INVALID_ARGUMENT;
    }

    call_android_crypto_callback(
        session_handle,
        |callbacks| callbacks.encrypt_hardware_secret_key,
        |callback| unsafe {
            callback(
                session_handle,
                key_algorithm,
                key_label,
                cipher_algorithm,
                nonce,
                additional_data,
                tag_length_bytes,
                payload,
                output_ciphertext,
                output_tag,
                output_ciphertext_written,
                output_tag_written,
            )
        },
    )
}

/// Forward the `decrypt_hardware_secret_key` crypto request through the Android host ABI.
#[unsafe(no_mangle)]
pub(crate) unsafe extern "C" fn destack_host_android_crypto_decrypt_hardware_secret_key(
    session_handle: u64,
    key_algorithm: u32,
    key_label: NativeStringRef,
    cipher_algorithm: u32,
    nonce: NativeSlice<u8>,
    additional_data: NativeSlice<u8>,
    tag: NativeSlice<u8>,
    payload: NativeSlice<u8>,
    output_plaintext: NativeSlice<u8>,
    output_written: *mut u32,
) -> u32 {
    if output_written.is_null() {
        return HOST_STATUS_INVALID_ARGUMENT;
    }

    call_android_crypto_callback(
        session_handle,
        |callbacks| callbacks.decrypt_hardware_secret_key,
        |callback| unsafe {
            callback(
                session_handle,
                key_algorithm,
                key_label,
                cipher_algorithm,
                nonce,
                additional_data,
                tag,
                payload,
                output_plaintext,
                output_written,
            )
        },
    )
}

/// Forward the `compute_hardware_mac` crypto request through the Android host ABI.
#[unsafe(no_mangle)]
pub(crate) unsafe extern "C" fn destack_host_android_crypto_compute_hardware_mac(
    session_handle: u64,
    key_algorithm: u32,
    key_label: NativeStringRef,
    mac_algorithm: u32,
    digest_algorithm: u32,
    tag_length_bytes: u32,
    payload: NativeSlice<u8>,
    output_tag: NativeSlice<u8>,
    output_written: *mut u32,
) -> u32 {
    if output_written.is_null() {
        return HOST_STATUS_INVALID_ARGUMENT;
    }

    call_android_crypto_callback(
        session_handle,
        |callbacks| callbacks.compute_hardware_mac,
        |callback| unsafe {
            callback(
                session_handle,
                key_algorithm,
                key_label,
                mac_algorithm,
                digest_algorithm,
                tag_length_bytes,
                payload,
                output_tag,
                output_written,
            )
        },
    )
}

/// Forward the `derive_hardware_shared_secret` crypto request through the Android host ABI.
#[unsafe(no_mangle)]
pub(crate) unsafe extern "C" fn destack_host_android_crypto_derive_hardware_shared_secret(
    session_handle: u64,
    key_algorithm: u32,
    key_label: NativeStringRef,
    named_curve: u32,
    peer_public_spki: NativeSlice<u8>,
    output_shared_secret: NativeSlice<u8>,
    output_written: *mut u32,
) -> u32 {
    if output_written.is_null() {
        return HOST_STATUS_INVALID_ARGUMENT;
    }

    call_android_crypto_callback(
        session_handle,
        |callbacks| callbacks.derive_hardware_shared_secret,
        |callback| unsafe {
            callback(
                session_handle,
                key_algorithm,
                key_label,
                named_curve,
                peer_public_spki,
                output_shared_secret,
                output_written,
            )
        },
    )
}

/// Forward the `delete_hardware_key` crypto request through the Android host ABI.
#[unsafe(no_mangle)]
pub(crate) unsafe extern "C" fn destack_host_android_crypto_delete_hardware_key(
    session_handle: u64,
    key_algorithm: u32,
    key_label: NativeStringRef,
) -> u32 {
    call_android_crypto_callback(
        session_handle,
        |callbacks| callbacks.delete_hardware_key,
        |callback| unsafe { callback(session_handle, key_algorithm, key_label) },
    )
}

/// Forward the `import_certificate` crypto request through the Android host ABI.
#[unsafe(no_mangle)]
pub(crate) unsafe extern "C" fn destack_host_android_crypto_import_certificate(
    session_handle: u64,
    store_kind: u32,
    certificate_der: NativeSlice<u8>,
) -> u32 {
    call_android_crypto_callback(
        session_handle,
        |callbacks| callbacks.import_certificate,
        |callback| unsafe { callback(session_handle, store_kind, certificate_der) },
    )
}

/// Forward the `delete_certificate` crypto request through the Android host ABI.
#[unsafe(no_mangle)]
pub(crate) unsafe extern "C" fn destack_host_android_crypto_delete_certificate(
    session_handle: u64,
    store_kind: u32,
    certificate_der: NativeSlice<u8>,
) -> u32 {
    call_android_crypto_callback(
        session_handle,
        |callbacks| callbacks.delete_certificate,
        |callback| unsafe { callback(session_handle, store_kind, certificate_der) },
    )
}

/// Probe one Android host lane for one hardware-backed key-pair algorithm.
#[unsafe(no_mangle)]
pub(crate) unsafe extern "C" fn destack_host_android_crypto_supports_hardware_key_pair(
    session_handle: u64,
    store_kind: u32,
    key_algorithm: u32,
) -> u32 {
    let callbacks =
        match resolve_android_binding_callback(session_handle, |bindings| Some(bindings.crypto)) {
            Ok(callbacks) => callbacks,
            Err(status) => return status,
        };

    let callbacks: AndroidHostCryptoCallbacks = callbacks;

    let is_supported = (callbacks.generate_hardware_key_pair.is_some())
        && (callbacks.export_hardware_public_key.is_some())
        && (callbacks.sign_hardware_key.is_some())
        && (callbacks.delete_hardware_key.is_some())
        && (((key_algorithm == 1) && (callbacks.decrypt_hardware_key.is_some()))
            || ((key_algorithm == 2) && (callbacks.derive_hardware_shared_secret.is_some())));
    if !is_supported {
        return HOST_STATUS_NOT_SUPPORTED;
    }

    let Some(callback) = callbacks.supports_hardware_key else {
        return HOST_STATUS_OK;
    };

    unsafe { callback(session_handle, store_kind) }
}

/// Probe one Android host lane for one hardware-backed secret-key algorithm.
#[unsafe(no_mangle)]
pub(crate) unsafe extern "C" fn destack_host_android_crypto_supports_hardware_secret_key(
    session_handle: u64,
    store_kind: u32,
    key_algorithm: u32,
) -> u32 {
    let callbacks =
        match resolve_android_binding_callback(session_handle, |bindings| Some(bindings.crypto)) {
            Ok(callbacks) => callbacks,
            Err(status) => return status,
        };

    let callbacks: AndroidHostCryptoCallbacks = callbacks;

    let is_supported = (callbacks.generate_hardware_secret_key.is_some())
        && (callbacks.delete_hardware_key.is_some())
        && (((key_algorithm == 3)
            && (callbacks.encrypt_hardware_secret_key.is_some())
            && (callbacks.decrypt_hardware_secret_key.is_some()))
            || ((key_algorithm == 4) && (callbacks.compute_hardware_mac.is_some())));
    if !is_supported {
        return HOST_STATUS_NOT_SUPPORTED;
    }

    let Some(callback) = callbacks.supports_hardware_key else {
        return HOST_STATUS_OK;
    };

    unsafe { callback(session_handle, store_kind) }
}

/// Probe one Android host lane for certificate write support.
#[unsafe(no_mangle)]
pub(crate) unsafe extern "C" fn destack_host_android_crypto_supports_certificate_write(
    session_handle: u64,
    store_kind: u32,
) -> u32 {
    let callbacks =
        match resolve_android_binding_callback(session_handle, |bindings| Some(bindings.crypto)) {
            Ok(callbacks) => callbacks,
            Err(status) => return status,
        };

    let callbacks: AndroidHostCryptoCallbacks = callbacks;

    let is_supported = ((store_kind == 1) || (store_kind == 2) || (store_kind == 3))
        && (callbacks.import_certificate.is_some())
        && (callbacks.delete_certificate.is_some());
    if !is_supported {
        return HOST_STATUS_NOT_SUPPORTED;
    }

    HOST_STATUS_OK
}
