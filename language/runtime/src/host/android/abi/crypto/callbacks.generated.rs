// generated by generate-bindings: do not edit

use crate::host::android::abi::binding::invoke_android_binding_callback;
use crate::platform::abi::{NativeSlice, NativeStringRef};

/// The host callback for `supports_hardware_key`.
pub(crate) type AndroidHostCryptoSupportsHardwareKeyCallback =
    unsafe extern "C" fn(session_handle: u64, store_kind: u32) -> u32;

/// The host callback for `generate_hardware_key_pair`.
pub(crate) type AndroidHostCryptoGenerateHardwareKeyPairCallback = unsafe extern "C" fn(
    session_handle: u64,
    store_kind: u32,
    key_algorithm: u32,
    named_curve: u32,
    modulus_bits: u32,
    public_exponent: u32,
    key_label: NativeStringRef,
) -> u32;

/// The host callback for `generate_hardware_secret_key`.
pub(crate) type AndroidHostCryptoGenerateHardwareSecretKeyCallback = unsafe extern "C" fn(
    session_handle: u64,
    store_kind: u32,
    key_algorithm: u32,
    digest_algorithm: u32,
    key_size_bits: u32,
    key_usage_mask: u32,
    key_label: NativeStringRef,
) -> u32;

/// The host callback for `export_hardware_public_key`.
pub(crate) type AndroidHostCryptoExportHardwarePublicKeyCallback = unsafe extern "C" fn(
    session_handle: u64,
    key_algorithm: u32,
    key_label: NativeStringRef,
    output: NativeSlice<u8>,
    output_written: *mut u32,
) -> u32;

/// The host callback for `sign_hardware_key`.
pub(crate) type AndroidHostCryptoSignHardwareKeyCallback = unsafe extern "C" fn(
    session_handle: u64,
    key_algorithm: u32,
    key_label: NativeStringRef,
    signature_algorithm: u32,
    digest_algorithm: u32,
    salt_length_bytes: u32,
    payload: NativeSlice<u8>,
    output_signature: NativeSlice<u8>,
    output_written: *mut u32,
) -> u32;

/// The host callback for `decrypt_hardware_key`.
pub(crate) type AndroidHostCryptoDecryptHardwareKeyCallback = unsafe extern "C" fn(
    session_handle: u64,
    key_algorithm: u32,
    key_label: NativeStringRef,
    encryption_algorithm: u32,
    digest_algorithm: u32,
    label: NativeSlice<u8>,
    payload: NativeSlice<u8>,
    output_plaintext: NativeSlice<u8>,
    output_written: *mut u32,
) -> u32;

/// The host callback for `encrypt_hardware_secret_key`.
pub(crate) type AndroidHostCryptoEncryptHardwareSecretKeyCallback = unsafe extern "C" fn(
    session_handle: u64,
    key_algorithm: u32,
    key_label: NativeStringRef,
    cipher_algorithm: u32,
    nonce: NativeSlice<u8>,
    additional_data: NativeSlice<u8>,
    tag_length_bytes: u32,
    payload: NativeSlice<u8>,
    output_ciphertext: NativeSlice<u8>,
    output_tag: NativeSlice<u8>,
    output_ciphertext_written: *mut u32,
    output_tag_written: *mut u32,
) -> u32;

/// The host callback for `decrypt_hardware_secret_key`.
pub(crate) type AndroidHostCryptoDecryptHardwareSecretKeyCallback = unsafe extern "C" fn(
    session_handle: u64,
    key_algorithm: u32,
    key_label: NativeStringRef,
    cipher_algorithm: u32,
    nonce: NativeSlice<u8>,
    additional_data: NativeSlice<u8>,
    tag: NativeSlice<u8>,
    payload: NativeSlice<u8>,
    output_plaintext: NativeSlice<u8>,
    output_written: *mut u32,
) -> u32;

/// The host callback for `compute_hardware_mac`.
pub(crate) type AndroidHostCryptoComputeHardwareMacCallback = unsafe extern "C" fn(
    session_handle: u64,
    key_algorithm: u32,
    key_label: NativeStringRef,
    mac_algorithm: u32,
    digest_algorithm: u32,
    tag_length_bytes: u32,
    payload: NativeSlice<u8>,
    output_tag: NativeSlice<u8>,
    output_written: *mut u32,
) -> u32;

/// The host callback for `derive_hardware_shared_secret`.
pub(crate) type AndroidHostCryptoDeriveHardwareSharedSecretCallback = unsafe extern "C" fn(
    session_handle: u64,
    key_algorithm: u32,
    key_label: NativeStringRef,
    named_curve: u32,
    peer_public_spki: NativeSlice<u8>,
    output_shared_secret: NativeSlice<u8>,
    output_written: *mut u32,
) -> u32;

/// The host callback for `delete_hardware_key`.
pub(crate) type AndroidHostCryptoDeleteHardwareKeyCallback = unsafe extern "C" fn(
    session_handle: u64,
    key_algorithm: u32,
    key_label: NativeStringRef,
) -> u32;

/// The host callback for `import_certificate`.
pub(crate) type AndroidHostCryptoImportCertificateCallback = unsafe extern "C" fn(
    session_handle: u64,
    store_kind: u32,
    certificate_der: NativeSlice<u8>,
) -> u32;

/// The host callback for `delete_certificate`.
pub(crate) type AndroidHostCryptoDeleteCertificateCallback = unsafe extern "C" fn(
    session_handle: u64,
    store_kind: u32,
    certificate_der: NativeSlice<u8>,
) -> u32;

/// Callback table for Android host crypto request interop.
#[derive(Clone, Copy, Debug, Default)]
#[repr(C)]
pub(crate) struct AndroidHostCryptoCallbacks {
    /// The callback for `supports_hardware_key`.
    pub supports_hardware_key: Option<AndroidHostCryptoSupportsHardwareKeyCallback>,
    /// The callback for `generate_hardware_key_pair`.
    pub generate_hardware_key_pair: Option<AndroidHostCryptoGenerateHardwareKeyPairCallback>,
    /// The callback for `generate_hardware_secret_key`.
    pub generate_hardware_secret_key: Option<AndroidHostCryptoGenerateHardwareSecretKeyCallback>,
    /// The callback for `export_hardware_public_key`.
    pub export_hardware_public_key: Option<AndroidHostCryptoExportHardwarePublicKeyCallback>,
    /// The callback for `sign_hardware_key`.
    pub sign_hardware_key: Option<AndroidHostCryptoSignHardwareKeyCallback>,
    /// The callback for `decrypt_hardware_key`.
    pub decrypt_hardware_key: Option<AndroidHostCryptoDecryptHardwareKeyCallback>,
    /// The callback for `encrypt_hardware_secret_key`.
    pub encrypt_hardware_secret_key: Option<AndroidHostCryptoEncryptHardwareSecretKeyCallback>,
    /// The callback for `decrypt_hardware_secret_key`.
    pub decrypt_hardware_secret_key: Option<AndroidHostCryptoDecryptHardwareSecretKeyCallback>,
    /// The callback for `compute_hardware_mac`.
    pub compute_hardware_mac: Option<AndroidHostCryptoComputeHardwareMacCallback>,
    /// The callback for `derive_hardware_shared_secret`.
    pub derive_hardware_shared_secret: Option<AndroidHostCryptoDeriveHardwareSharedSecretCallback>,
    /// The callback for `delete_hardware_key`.
    pub delete_hardware_key: Option<AndroidHostCryptoDeleteHardwareKeyCallback>,
    /// The callback for `import_certificate`.
    pub import_certificate: Option<AndroidHostCryptoImportCertificateCallback>,
    /// The callback for `delete_certificate`.
    pub delete_certificate: Option<AndroidHostCryptoDeleteCertificateCallback>,
}

/// Resolve and invoke one Android host crypto callback.
pub(super) fn call_android_crypto_callback<T: Copy>(
    session_handle: u64,
    resolve: impl FnOnce(&AndroidHostCryptoCallbacks) -> Option<T>,
    invoke: impl FnOnce(T) -> u32,
) -> u32 {
    invoke_android_binding_callback(session_handle, |bindings| resolve(&bindings.crypto), invoke)
}
