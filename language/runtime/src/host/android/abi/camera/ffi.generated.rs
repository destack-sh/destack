// generated by generate-bindings: do not edit

use super::callbacks::{AndroidHostCameraCallbacks, call_android_camera_callback};
use crate::host::abi::camera::{
    AndroidHostCameraDeviceDescriptorHeader, AndroidHostCameraFrameHeader,
    AndroidHostCameraRecordingCapabilitiesHeader, AndroidHostCameraRecordingOptionsHeader,
    AndroidHostCameraStreamCapabilityHeader, AndroidHostCameraStreamConfigHeader,
};
use crate::host::core::HOST_STATUS_INVALID_ARGUMENT;
use crate::platform::abi::{NativeSlice, NativeStringRef};

/// Forward the `device_list` camera request through the Android host seam.
pub unsafe fn destack_host_android_camera_device_list(
    session_handle: u64,
    devices: NativeSlice<AndroidHostCameraDeviceDescriptorHeader>,
    device_count_written: *mut u32,
    string_bytes: NativeSlice<u8>,
    string_bytes_written: *mut u32,
) -> u32 {
    if device_count_written.is_null() {
        return HOST_STATUS_INVALID_ARGUMENT;
    }

    if string_bytes_written.is_null() {
        return HOST_STATUS_INVALID_ARGUMENT;
    }

    call_android_camera_callback(
        session_handle,
        |callbacks| callbacks.device_list,
        |callback| unsafe {
            callback(
                session_handle,
                devices,
                device_count_written,
                string_bytes,
                string_bytes_written,
            )
        },
    )
}

/// Forward the `device_open` camera request through the Android host seam.
pub unsafe fn destack_host_android_camera_device_open(
    session_handle: u64,
    id: NativeStringRef,
    session_id: *mut u64,
) -> u32 {
    if session_id.is_null() {
        return HOST_STATUS_INVALID_ARGUMENT;
    }

    call_android_camera_callback(
        session_handle,
        |callbacks| callbacks.device_open,
        |callback| unsafe { callback(session_handle, id, session_id) },
    )
}

/// Forward the `device_close` camera request through the Android host seam.
pub unsafe fn destack_host_android_camera_device_close(
    session_handle: u64,
    session_id: u64,
) -> u32 {
    call_android_camera_callback(
        session_handle,
        |callbacks| callbacks.device_close,
        |callback| unsafe { callback(session_handle, session_id) },
    )
}

/// Forward the `stream_config_list` camera request through the Android host seam.
pub unsafe fn destack_host_android_camera_stream_config_list(
    session_handle: u64,
    session_id: u64,
    configs: NativeSlice<AndroidHostCameraStreamConfigHeader>,
    config_count_written: *mut u32,
) -> u32 {
    if config_count_written.is_null() {
        return HOST_STATUS_INVALID_ARGUMENT;
    }

    call_android_camera_callback(
        session_handle,
        |callbacks| callbacks.stream_config_list,
        |callback| unsafe { callback(session_handle, session_id, configs, config_count_written) },
    )
}

/// Forward the `stream_capability_list` camera request through the Android host seam.
pub unsafe fn destack_host_android_camera_stream_capability_list(
    session_handle: u64,
    session_id: u64,
    capabilities: NativeSlice<AndroidHostCameraStreamCapabilityHeader>,
    capability_count_written: *mut u32,
) -> u32 {
    if capability_count_written.is_null() {
        return HOST_STATUS_INVALID_ARGUMENT;
    }

    call_android_camera_callback(
        session_handle,
        |callbacks| callbacks.stream_capability_list,
        |callback| unsafe {
            callback(
                session_handle,
                session_id,
                capabilities,
                capability_count_written,
            )
        },
    )
}

/// Forward the `stream_open` camera request through the Android host seam.
pub unsafe fn destack_host_android_camera_stream_open(
    session_handle: u64,
    session_id: u64,
    config: AndroidHostCameraStreamConfigHeader,
    stream_id: *mut u64,
) -> u32 {
    if stream_id.is_null() {
        return HOST_STATUS_INVALID_ARGUMENT;
    }

    call_android_camera_callback(
        session_handle,
        |callbacks| callbacks.stream_open,
        |callback| unsafe { callback(session_handle, session_id, config, stream_id) },
    )
}

/// Forward the `stream_close` camera request through the Android host seam.
pub unsafe fn destack_host_android_camera_stream_close(session_handle: u64, stream_id: u64) -> u32 {
    call_android_camera_callback(
        session_handle,
        |callbacks| callbacks.stream_close,
        |callback| unsafe { callback(session_handle, stream_id) },
    )
}

/// Forward the `stream_start` camera request through the Android host seam.
pub unsafe fn destack_host_android_camera_stream_start(session_handle: u64, stream_id: u64) -> u32 {
    call_android_camera_callback(
        session_handle,
        |callbacks| callbacks.stream_start,
        |callback| unsafe { callback(session_handle, stream_id) },
    )
}

/// Forward the `stream_stop` camera request through the Android host seam.
pub unsafe fn destack_host_android_camera_stream_stop(session_handle: u64, stream_id: u64) -> u32 {
    call_android_camera_callback(
        session_handle,
        |callbacks| callbacks.stream_stop,
        |callback| unsafe { callback(session_handle, stream_id) },
    )
}

/// Forward the `stream_read` camera request through the Android host seam.
pub unsafe fn destack_host_android_camera_stream_read(
    session_handle: u64,
    stream_id: u64,
    timeout_ns: u64,
    header: *mut AndroidHostCameraFrameHeader,
    bytes: NativeSlice<u8>,
    bytes_written: *mut u32,
) -> u32 {
    if header.is_null() {
        return HOST_STATUS_INVALID_ARGUMENT;
    }

    if bytes_written.is_null() {
        return HOST_STATUS_INVALID_ARGUMENT;
    }

    call_android_camera_callback(
        session_handle,
        |callbacks| callbacks.stream_read,
        |callback| unsafe {
            callback(
                session_handle,
                stream_id,
                timeout_ns,
                header,
                bytes,
                bytes_written,
            )
        },
    )
}

/// Forward the `stream_try_read` camera request through the Android host seam.
pub unsafe fn destack_host_android_camera_stream_try_read(
    session_handle: u64,
    stream_id: u64,
    header: *mut AndroidHostCameraFrameHeader,
    bytes: NativeSlice<u8>,
    bytes_written: *mut u32,
) -> u32 {
    if header.is_null() {
        return HOST_STATUS_INVALID_ARGUMENT;
    }

    if bytes_written.is_null() {
        return HOST_STATUS_INVALID_ARGUMENT;
    }

    call_android_camera_callback(
        session_handle,
        |callbacks| callbacks.stream_try_read,
        |callback| unsafe { callback(session_handle, stream_id, header, bytes, bytes_written) },
    )
}

/// Forward the `stream_take_photo` camera request through the Android host seam.
pub unsafe fn destack_host_android_camera_stream_take_photo(
    session_handle: u64,
    stream_id: u64,
    timeout_ns: u64,
    header: *mut AndroidHostCameraFrameHeader,
    bytes: NativeSlice<u8>,
    bytes_written: *mut u32,
) -> u32 {
    if header.is_null() {
        return HOST_STATUS_INVALID_ARGUMENT;
    }

    if bytes_written.is_null() {
        return HOST_STATUS_INVALID_ARGUMENT;
    }

    call_android_camera_callback(
        session_handle,
        |callbacks| callbacks.stream_take_photo,
        |callback| unsafe {
            callback(
                session_handle,
                stream_id,
                timeout_ns,
                header,
                bytes,
                bytes_written,
            )
        },
    )
}

/// Forward the `stream_config` camera request through the Android host seam.
pub unsafe fn destack_host_android_camera_stream_config(
    session_handle: u64,
    stream_id: u64,
    config: *mut AndroidHostCameraStreamConfigHeader,
) -> u32 {
    if config.is_null() {
        return HOST_STATUS_INVALID_ARGUMENT;
    }

    call_android_camera_callback(
        session_handle,
        |callbacks| callbacks.stream_config,
        |callback| unsafe { callback(session_handle, stream_id, config) },
    )
}

/// Forward the `stream_recording_capabilities` camera request through the Android host seam.
pub unsafe fn destack_host_android_camera_stream_recording_capabilities(
    session_handle: u64,
    stream_id: u64,
    capabilities: *mut AndroidHostCameraRecordingCapabilitiesHeader,
) -> u32 {
    if capabilities.is_null() {
        return HOST_STATUS_INVALID_ARGUMENT;
    }

    call_android_camera_callback(
        session_handle,
        |callbacks| callbacks.stream_recording_capabilities,
        |callback| unsafe { callback(session_handle, stream_id, capabilities) },
    )
}

/// Forward the `stream_start_recording` camera request through the Android host seam.
pub unsafe fn destack_host_android_camera_stream_start_recording(
    session_handle: u64,
    stream_id: u64,
    options: AndroidHostCameraRecordingOptionsHeader,
    output_path: NativeStringRef,
) -> u32 {
    call_android_camera_callback(
        session_handle,
        |callbacks| callbacks.stream_start_recording,
        |callback| unsafe { callback(session_handle, stream_id, options, output_path) },
    )
}

/// Forward the `stream_pause_recording` camera request through the Android host seam.
pub unsafe fn destack_host_android_camera_stream_pause_recording(
    session_handle: u64,
    stream_id: u64,
) -> u32 {
    call_android_camera_callback(
        session_handle,
        |callbacks| callbacks.stream_pause_recording,
        |callback| unsafe { callback(session_handle, stream_id) },
    )
}

/// Forward the `stream_resume_recording` camera request through the Android host seam.
pub unsafe fn destack_host_android_camera_stream_resume_recording(
    session_handle: u64,
    stream_id: u64,
) -> u32 {
    call_android_camera_callback(
        session_handle,
        |callbacks| callbacks.stream_resume_recording,
        |callback| unsafe { callback(session_handle, stream_id) },
    )
}

/// Forward the `stream_stop_recording` camera request through the Android host seam.
pub unsafe fn destack_host_android_camera_stream_stop_recording(
    session_handle: u64,
    stream_id: u64,
    timeout_ns: u64,
) -> u32 {
    call_android_camera_callback(
        session_handle,
        |callbacks| callbacks.stream_stop_recording,
        |callback| unsafe { callback(session_handle, stream_id, timeout_ns) },
    )
}

/// Forward the `stream_get_u64` camera request through the Android host seam.
pub unsafe fn destack_host_android_camera_stream_get_u64(
    session_handle: u64,
    stream_id: u64,
    selector: u32,
    value: *mut u64,
) -> u32 {
    if value.is_null() {
        return HOST_STATUS_INVALID_ARGUMENT;
    }

    call_android_camera_callback(
        session_handle,
        |callbacks| callbacks.stream_get_u64,
        |callback| unsafe { callback(session_handle, stream_id, selector, value) },
    )
}

/// Forward the `stream_set_u64` camera request through the Android host seam.
pub unsafe fn destack_host_android_camera_stream_set_u64(
    session_handle: u64,
    stream_id: u64,
    selector: u32,
    value: u64,
) -> u32 {
    call_android_camera_callback(
        session_handle,
        |callbacks| callbacks.stream_set_u64,
        |callback| unsafe { callback(session_handle, stream_id, selector, value) },
    )
}

/// Forward the `stream_get_u32` camera request through the Android host seam.
pub unsafe fn destack_host_android_camera_stream_get_u32(
    session_handle: u64,
    stream_id: u64,
    selector: u32,
    value: *mut u32,
) -> u32 {
    if value.is_null() {
        return HOST_STATUS_INVALID_ARGUMENT;
    }

    call_android_camera_callback(
        session_handle,
        |callbacks| callbacks.stream_get_u32,
        |callback| unsafe { callback(session_handle, stream_id, selector, value) },
    )
}

/// Forward the `stream_set_u32` camera request through the Android host seam.
pub unsafe fn destack_host_android_camera_stream_set_u32(
    session_handle: u64,
    stream_id: u64,
    selector: u32,
    value: u32,
) -> u32 {
    call_android_camera_callback(
        session_handle,
        |callbacks| callbacks.stream_set_u32,
        |callback| unsafe { callback(session_handle, stream_id, selector, value) },
    )
}

/// Forward the `stream_get_f64` camera request through the Android host seam.
pub unsafe fn destack_host_android_camera_stream_get_f64(
    session_handle: u64,
    stream_id: u64,
    selector: u32,
    value: *mut f64,
) -> u32 {
    if value.is_null() {
        return HOST_STATUS_INVALID_ARGUMENT;
    }

    call_android_camera_callback(
        session_handle,
        |callbacks| callbacks.stream_get_f64,
        |callback| unsafe { callback(session_handle, stream_id, selector, value) },
    )
}

/// Forward the `stream_set_f64` camera request through the Android host seam.
pub unsafe fn destack_host_android_camera_stream_set_f64(
    session_handle: u64,
    stream_id: u64,
    selector: u32,
    value: f64,
) -> u32 {
    call_android_camera_callback(
        session_handle,
        |callbacks| callbacks.stream_set_f64,
        |callback| unsafe { callback(session_handle, stream_id, selector, value) },
    )
}

/// Forward the `stream_get_range_f64` camera request through the Android host seam.
pub unsafe fn destack_host_android_camera_stream_get_range_f64(
    session_handle: u64,
    stream_id: u64,
    selector: u32,
    minimum: *mut f64,
    maximum: *mut f64,
    step: *mut f64,
) -> u32 {
    if minimum.is_null() {
        return HOST_STATUS_INVALID_ARGUMENT;
    }

    if maximum.is_null() {
        return HOST_STATUS_INVALID_ARGUMENT;
    }

    if step.is_null() {
        return HOST_STATUS_INVALID_ARGUMENT;
    }

    call_android_camera_callback(
        session_handle,
        |callbacks| callbacks.stream_get_range_f64,
        |callback| unsafe { callback(session_handle, stream_id, selector, minimum, maximum, step) },
    )
}

/// Forward the `stream_get_range_u64` camera request through the Android host seam.
pub unsafe fn destack_host_android_camera_stream_get_range_u64(
    session_handle: u64,
    stream_id: u64,
    selector: u32,
    minimum: *mut u64,
    maximum: *mut u64,
    step: *mut u64,
) -> u32 {
    if minimum.is_null() {
        return HOST_STATUS_INVALID_ARGUMENT;
    }

    if maximum.is_null() {
        return HOST_STATUS_INVALID_ARGUMENT;
    }

    if step.is_null() {
        return HOST_STATUS_INVALID_ARGUMENT;
    }

    call_android_camera_callback(
        session_handle,
        |callbacks| callbacks.stream_get_range_u64,
        |callback| unsafe { callback(session_handle, stream_id, selector, minimum, maximum, step) },
    )
}

/// Forward the `stream_get_range_u32` camera request through the Android host seam.
pub unsafe fn destack_host_android_camera_stream_get_range_u32(
    session_handle: u64,
    stream_id: u64,
    selector: u32,
    minimum: *mut u32,
    maximum: *mut u32,
    step: *mut u32,
) -> u32 {
    if minimum.is_null() {
        return HOST_STATUS_INVALID_ARGUMENT;
    }

    if maximum.is_null() {
        return HOST_STATUS_INVALID_ARGUMENT;
    }

    if step.is_null() {
        return HOST_STATUS_INVALID_ARGUMENT;
    }

    call_android_camera_callback(
        session_handle,
        |callbacks| callbacks.stream_get_range_u32,
        |callback| unsafe { callback(session_handle, stream_id, selector, minimum, maximum, step) },
    )
}

/// Forward the `stream_exposure_compensation` camera control getter through the Android host seam.
pub unsafe fn destack_host_android_camera_stream_exposure_compensation(
    session_handle: u64,
    stream_id: u64,
    value: *mut f64,
) -> u32 {
    unsafe { destack_host_android_camera_stream_get_f64(session_handle, stream_id, 1, value) }
}

/// Forward the `stream_set_exposure_compensation` camera control setter through the Android host seam.
pub unsafe fn destack_host_android_camera_stream_set_exposure_compensation(
    session_handle: u64,
    stream_id: u64,
    value: f64,
) -> u32 {
    unsafe { destack_host_android_camera_stream_set_f64(session_handle, stream_id, 1, value) }
}

/// Forward the `stream_exposure_compensation_range` camera control range query through the Android host seam.
pub unsafe fn destack_host_android_camera_stream_exposure_compensation_range(
    session_handle: u64,
    stream_id: u64,
    minimum: *mut f64,
    maximum: *mut f64,
    step: *mut f64,
) -> u32 {
    unsafe {
        destack_host_android_camera_stream_get_range_f64(
            session_handle,
            stream_id,
            1,
            minimum,
            maximum,
            step,
        )
    }
}

/// Forward the `stream_exposure_mode` camera control getter through the Android host seam.
pub unsafe fn destack_host_android_camera_stream_exposure_mode(
    session_handle: u64,
    stream_id: u64,
    value: *mut u32,
) -> u32 {
    unsafe { destack_host_android_camera_stream_get_u32(session_handle, stream_id, 2, value) }
}

/// Forward the `stream_set_exposure_mode` camera control setter through the Android host seam.
pub unsafe fn destack_host_android_camera_stream_set_exposure_mode(
    session_handle: u64,
    stream_id: u64,
    value: u32,
) -> u32 {
    unsafe { destack_host_android_camera_stream_set_u32(session_handle, stream_id, 2, value) }
}

/// Forward the `stream_exposure_time_ns` camera control getter through the Android host seam.
pub unsafe fn destack_host_android_camera_stream_exposure_time_ns(
    session_handle: u64,
    stream_id: u64,
    value: *mut u64,
) -> u32 {
    unsafe { destack_host_android_camera_stream_get_u64(session_handle, stream_id, 3, value) }
}

/// Forward the `stream_set_exposure_time_ns` camera control setter through the Android host seam.
pub unsafe fn destack_host_android_camera_stream_set_exposure_time_ns(
    session_handle: u64,
    stream_id: u64,
    value: u64,
) -> u32 {
    unsafe { destack_host_android_camera_stream_set_u64(session_handle, stream_id, 3, value) }
}

/// Forward the `stream_exposure_time_range` camera control range query through the Android host seam.
pub unsafe fn destack_host_android_camera_stream_exposure_time_range(
    session_handle: u64,
    stream_id: u64,
    minimum: *mut u64,
    maximum: *mut u64,
    step: *mut u64,
) -> u32 {
    unsafe {
        destack_host_android_camera_stream_get_range_u64(
            session_handle,
            stream_id,
            3,
            minimum,
            maximum,
            step,
        )
    }
}

/// Forward the `stream_sensor_iso` camera control getter through the Android host seam.
pub unsafe fn destack_host_android_camera_stream_sensor_iso(
    session_handle: u64,
    stream_id: u64,
    value: *mut u32,
) -> u32 {
    unsafe { destack_host_android_camera_stream_get_u32(session_handle, stream_id, 4, value) }
}

/// Forward the `stream_set_sensor_iso` camera control setter through the Android host seam.
pub unsafe fn destack_host_android_camera_stream_set_sensor_iso(
    session_handle: u64,
    stream_id: u64,
    value: u32,
) -> u32 {
    unsafe { destack_host_android_camera_stream_set_u32(session_handle, stream_id, 4, value) }
}

/// Forward the `stream_sensor_iso_range` camera control range query through the Android host seam.
pub unsafe fn destack_host_android_camera_stream_sensor_iso_range(
    session_handle: u64,
    stream_id: u64,
    minimum: *mut u32,
    maximum: *mut u32,
    step: *mut u32,
) -> u32 {
    unsafe {
        destack_host_android_camera_stream_get_range_u32(
            session_handle,
            stream_id,
            4,
            minimum,
            maximum,
            step,
        )
    }
}

/// Forward the `stream_white_balance_kelvin` camera control getter through the Android host seam.
pub unsafe fn destack_host_android_camera_stream_white_balance_kelvin(
    session_handle: u64,
    stream_id: u64,
    value: *mut u32,
) -> u32 {
    unsafe { destack_host_android_camera_stream_get_u32(session_handle, stream_id, 5, value) }
}

/// Forward the `stream_set_white_balance_kelvin` camera control setter through the Android host seam.
pub unsafe fn destack_host_android_camera_stream_set_white_balance_kelvin(
    session_handle: u64,
    stream_id: u64,
    value: u32,
) -> u32 {
    unsafe { destack_host_android_camera_stream_set_u32(session_handle, stream_id, 5, value) }
}

/// Forward the `stream_white_balance_range` camera control range query through the Android host seam.
pub unsafe fn destack_host_android_camera_stream_white_balance_range(
    session_handle: u64,
    stream_id: u64,
    minimum: *mut u32,
    maximum: *mut u32,
    step: *mut u32,
) -> u32 {
    unsafe {
        destack_host_android_camera_stream_get_range_u32(
            session_handle,
            stream_id,
            5,
            minimum,
            maximum,
            step,
        )
    }
}

/// Forward the `stream_white_balance_mode` camera control getter through the Android host seam.
pub unsafe fn destack_host_android_camera_stream_white_balance_mode(
    session_handle: u64,
    stream_id: u64,
    value: *mut u32,
) -> u32 {
    unsafe { destack_host_android_camera_stream_get_u32(session_handle, stream_id, 6, value) }
}

/// Forward the `stream_set_white_balance_mode` camera control setter through the Android host seam.
pub unsafe fn destack_host_android_camera_stream_set_white_balance_mode(
    session_handle: u64,
    stream_id: u64,
    value: u32,
) -> u32 {
    unsafe { destack_host_android_camera_stream_set_u32(session_handle, stream_id, 6, value) }
}

/// Forward the `stream_focus_distance_diopters` camera control getter through the Android host seam.
pub unsafe fn destack_host_android_camera_stream_focus_distance_diopters(
    session_handle: u64,
    stream_id: u64,
    value: *mut f64,
) -> u32 {
    unsafe { destack_host_android_camera_stream_get_f64(session_handle, stream_id, 7, value) }
}

/// Forward the `stream_set_focus_distance_diopters` camera control setter through the Android host seam.
pub unsafe fn destack_host_android_camera_stream_set_focus_distance_diopters(
    session_handle: u64,
    stream_id: u64,
    value: f64,
) -> u32 {
    unsafe { destack_host_android_camera_stream_set_f64(session_handle, stream_id, 7, value) }
}

/// Forward the `stream_focus_distance_range` camera control range query through the Android host seam.
pub unsafe fn destack_host_android_camera_stream_focus_distance_range(
    session_handle: u64,
    stream_id: u64,
    minimum: *mut f64,
    maximum: *mut f64,
    step: *mut f64,
) -> u32 {
    unsafe {
        destack_host_android_camera_stream_get_range_f64(
            session_handle,
            stream_id,
            7,
            minimum,
            maximum,
            step,
        )
    }
}

/// Forward the `stream_focus_mode` camera control getter through the Android host seam.
pub unsafe fn destack_host_android_camera_stream_focus_mode(
    session_handle: u64,
    stream_id: u64,
    value: *mut u32,
) -> u32 {
    unsafe { destack_host_android_camera_stream_get_u32(session_handle, stream_id, 8, value) }
}

/// Forward the `stream_set_focus_mode` camera control setter through the Android host seam.
pub unsafe fn destack_host_android_camera_stream_set_focus_mode(
    session_handle: u64,
    stream_id: u64,
    value: u32,
) -> u32 {
    unsafe { destack_host_android_camera_stream_set_u32(session_handle, stream_id, 8, value) }
}

/// Forward the `stream_brightness` camera control getter through the Android host seam.
pub unsafe fn destack_host_android_camera_stream_brightness(
    session_handle: u64,
    stream_id: u64,
    value: *mut f64,
) -> u32 {
    unsafe { destack_host_android_camera_stream_get_f64(session_handle, stream_id, 9, value) }
}

/// Forward the `stream_set_brightness` camera control setter through the Android host seam.
pub unsafe fn destack_host_android_camera_stream_set_brightness(
    session_handle: u64,
    stream_id: u64,
    value: f64,
) -> u32 {
    unsafe { destack_host_android_camera_stream_set_f64(session_handle, stream_id, 9, value) }
}

/// Forward the `stream_brightness_range` camera control range query through the Android host seam.
pub unsafe fn destack_host_android_camera_stream_brightness_range(
    session_handle: u64,
    stream_id: u64,
    minimum: *mut f64,
    maximum: *mut f64,
    step: *mut f64,
) -> u32 {
    unsafe {
        destack_host_android_camera_stream_get_range_f64(
            session_handle,
            stream_id,
            9,
            minimum,
            maximum,
            step,
        )
    }
}

/// Forward the `stream_contrast` camera control getter through the Android host seam.
pub unsafe fn destack_host_android_camera_stream_contrast(
    session_handle: u64,
    stream_id: u64,
    value: *mut f64,
) -> u32 {
    unsafe { destack_host_android_camera_stream_get_f64(session_handle, stream_id, 10, value) }
}

/// Forward the `stream_set_contrast` camera control setter through the Android host seam.
pub unsafe fn destack_host_android_camera_stream_set_contrast(
    session_handle: u64,
    stream_id: u64,
    value: f64,
) -> u32 {
    unsafe { destack_host_android_camera_stream_set_f64(session_handle, stream_id, 10, value) }
}

/// Forward the `stream_contrast_range` camera control range query through the Android host seam.
pub unsafe fn destack_host_android_camera_stream_contrast_range(
    session_handle: u64,
    stream_id: u64,
    minimum: *mut f64,
    maximum: *mut f64,
    step: *mut f64,
) -> u32 {
    unsafe {
        destack_host_android_camera_stream_get_range_f64(
            session_handle,
            stream_id,
            10,
            minimum,
            maximum,
            step,
        )
    }
}

/// Forward the `stream_saturation` camera control getter through the Android host seam.
pub unsafe fn destack_host_android_camera_stream_saturation(
    session_handle: u64,
    stream_id: u64,
    value: *mut f64,
) -> u32 {
    unsafe { destack_host_android_camera_stream_get_f64(session_handle, stream_id, 11, value) }
}

/// Forward the `stream_set_saturation` camera control setter through the Android host seam.
pub unsafe fn destack_host_android_camera_stream_set_saturation(
    session_handle: u64,
    stream_id: u64,
    value: f64,
) -> u32 {
    unsafe { destack_host_android_camera_stream_set_f64(session_handle, stream_id, 11, value) }
}

/// Forward the `stream_saturation_range` camera control range query through the Android host seam.
pub unsafe fn destack_host_android_camera_stream_saturation_range(
    session_handle: u64,
    stream_id: u64,
    minimum: *mut f64,
    maximum: *mut f64,
    step: *mut f64,
) -> u32 {
    unsafe {
        destack_host_android_camera_stream_get_range_f64(
            session_handle,
            stream_id,
            11,
            minimum,
            maximum,
            step,
        )
    }
}

/// Forward the `stream_sharpness` camera control getter through the Android host seam.
pub unsafe fn destack_host_android_camera_stream_sharpness(
    session_handle: u64,
    stream_id: u64,
    value: *mut f64,
) -> u32 {
    unsafe { destack_host_android_camera_stream_get_f64(session_handle, stream_id, 12, value) }
}

/// Forward the `stream_set_sharpness` camera control setter through the Android host seam.
pub unsafe fn destack_host_android_camera_stream_set_sharpness(
    session_handle: u64,
    stream_id: u64,
    value: f64,
) -> u32 {
    unsafe { destack_host_android_camera_stream_set_f64(session_handle, stream_id, 12, value) }
}

/// Forward the `stream_sharpness_range` camera control range query through the Android host seam.
pub unsafe fn destack_host_android_camera_stream_sharpness_range(
    session_handle: u64,
    stream_id: u64,
    minimum: *mut f64,
    maximum: *mut f64,
    step: *mut f64,
) -> u32 {
    unsafe {
        destack_host_android_camera_stream_get_range_f64(
            session_handle,
            stream_id,
            12,
            minimum,
            maximum,
            step,
        )
    }
}

/// Forward the `stream_pan_degrees` camera control getter through the Android host seam.
pub unsafe fn destack_host_android_camera_stream_pan_degrees(
    session_handle: u64,
    stream_id: u64,
    value: *mut f64,
) -> u32 {
    unsafe { destack_host_android_camera_stream_get_f64(session_handle, stream_id, 13, value) }
}

/// Forward the `stream_set_pan_degrees` camera control setter through the Android host seam.
pub unsafe fn destack_host_android_camera_stream_set_pan_degrees(
    session_handle: u64,
    stream_id: u64,
    value: f64,
) -> u32 {
    unsafe { destack_host_android_camera_stream_set_f64(session_handle, stream_id, 13, value) }
}

/// Forward the `stream_pan_range` camera control range query through the Android host seam.
pub unsafe fn destack_host_android_camera_stream_pan_range(
    session_handle: u64,
    stream_id: u64,
    minimum: *mut f64,
    maximum: *mut f64,
    step: *mut f64,
) -> u32 {
    unsafe {
        destack_host_android_camera_stream_get_range_f64(
            session_handle,
            stream_id,
            13,
            minimum,
            maximum,
            step,
        )
    }
}

/// Forward the `stream_tilt_degrees` camera control getter through the Android host seam.
pub unsafe fn destack_host_android_camera_stream_tilt_degrees(
    session_handle: u64,
    stream_id: u64,
    value: *mut f64,
) -> u32 {
    unsafe { destack_host_android_camera_stream_get_f64(session_handle, stream_id, 14, value) }
}

/// Forward the `stream_set_tilt_degrees` camera control setter through the Android host seam.
pub unsafe fn destack_host_android_camera_stream_set_tilt_degrees(
    session_handle: u64,
    stream_id: u64,
    value: f64,
) -> u32 {
    unsafe { destack_host_android_camera_stream_set_f64(session_handle, stream_id, 14, value) }
}

/// Forward the `stream_tilt_range` camera control range query through the Android host seam.
pub unsafe fn destack_host_android_camera_stream_tilt_range(
    session_handle: u64,
    stream_id: u64,
    minimum: *mut f64,
    maximum: *mut f64,
    step: *mut f64,
) -> u32 {
    unsafe {
        destack_host_android_camera_stream_get_range_f64(
            session_handle,
            stream_id,
            14,
            minimum,
            maximum,
            step,
        )
    }
}

/// Forward the `stream_zoom_ratio` camera control getter through the Android host seam.
pub unsafe fn destack_host_android_camera_stream_zoom_ratio(
    session_handle: u64,
    stream_id: u64,
    value: *mut f64,
) -> u32 {
    unsafe { destack_host_android_camera_stream_get_f64(session_handle, stream_id, 15, value) }
}

/// Forward the `stream_set_zoom_ratio` camera control setter through the Android host seam.
pub unsafe fn destack_host_android_camera_stream_set_zoom_ratio(
    session_handle: u64,
    stream_id: u64,
    value: f64,
) -> u32 {
    unsafe { destack_host_android_camera_stream_set_f64(session_handle, stream_id, 15, value) }
}

/// Forward the `stream_zoom_ratio_range` camera control range query through the Android host seam.
pub unsafe fn destack_host_android_camera_stream_zoom_ratio_range(
    session_handle: u64,
    stream_id: u64,
    minimum: *mut f64,
    maximum: *mut f64,
    step: *mut f64,
) -> u32 {
    unsafe {
        destack_host_android_camera_stream_get_range_f64(
            session_handle,
            stream_id,
            15,
            minimum,
            maximum,
            step,
        )
    }
}

/// Forward the `stream_stabilization_mode` camera control getter through the Android host seam.
pub unsafe fn destack_host_android_camera_stream_stabilization_mode(
    session_handle: u64,
    stream_id: u64,
    value: *mut u32,
) -> u32 {
    unsafe { destack_host_android_camera_stream_get_u32(session_handle, stream_id, 16, value) }
}

/// Forward the `stream_set_stabilization_mode` camera control setter through the Android host seam.
pub unsafe fn destack_host_android_camera_stream_set_stabilization_mode(
    session_handle: u64,
    stream_id: u64,
    value: u32,
) -> u32 {
    unsafe { destack_host_android_camera_stream_set_u32(session_handle, stream_id, 16, value) }
}

/// Forward the `stream_torch_mode` camera control getter through the Android host seam.
pub unsafe fn destack_host_android_camera_stream_torch_mode(
    session_handle: u64,
    stream_id: u64,
    value: *mut u32,
) -> u32 {
    unsafe { destack_host_android_camera_stream_get_u32(session_handle, stream_id, 17, value) }
}

/// Forward the `stream_set_torch_mode` camera control setter through the Android host seam.
pub unsafe fn destack_host_android_camera_stream_set_torch_mode(
    session_handle: u64,
    stream_id: u64,
    value: u32,
) -> u32 {
    unsafe { destack_host_android_camera_stream_set_u32(session_handle, stream_id, 17, value) }
}
