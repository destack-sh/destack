// generated by generate-bindings: do not edit

use super::callbacks::{AndroidHostCredentialsCallbacks, call_android_credentials_callback};
use crate::host::core::HOST_STATUS_INVALID_ARGUMENT;
use crate::platform::abi::{NativeSlice, NativeStringRef};

/// Forward the `read` credentials request through the Android host ABI.
#[unsafe(no_mangle)]
pub(crate) unsafe extern "C" fn destack_host_android_credentials_read(
    session_handle: u64,
    service: NativeStringRef,
    account: NativeStringRef,
    access_group: NativeStringRef,
    require_authentication: bool,
    output: NativeSlice<u8>,
    output_written: *mut u32,
    created_unix_ns: *mut u64,
    modified_unix_ns: *mut u64,
) -> u32 {
    if output_written.is_null() {
        return HOST_STATUS_INVALID_ARGUMENT;
    }

    if created_unix_ns.is_null() {
        return HOST_STATUS_INVALID_ARGUMENT;
    }

    if modified_unix_ns.is_null() {
        return HOST_STATUS_INVALID_ARGUMENT;
    }

    call_android_credentials_callback(
        session_handle,
        |callbacks| callbacks.read,
        |callback| unsafe {
            callback(
                session_handle,
                service,
                account,
                access_group,
                require_authentication,
                output,
                output_written,
                created_unix_ns,
                modified_unix_ns,
            )
        },
    )
}

/// Forward the `write` credentials request through the Android host ABI.
#[unsafe(no_mangle)]
pub(crate) unsafe extern "C" fn destack_host_android_credentials_write(
    session_handle: u64,
    service: NativeStringRef,
    account: NativeStringRef,
    access_group: NativeStringRef,
    payload: NativeSlice<u8>,
    accessibility: u32,
    authentication_policy: u32,
    replace_existing: bool,
) -> u32 {
    call_android_credentials_callback(
        session_handle,
        |callbacks| callbacks.write,
        |callback| unsafe {
            callback(
                session_handle,
                service,
                account,
                access_group,
                payload,
                accessibility,
                authentication_policy,
                replace_existing,
            )
        },
    )
}

/// Forward the `delete` credentials request through the Android host ABI.
#[unsafe(no_mangle)]
pub(crate) unsafe extern "C" fn destack_host_android_credentials_delete(
    session_handle: u64,
    service: NativeStringRef,
    account: NativeStringRef,
    access_group: NativeStringRef,
) -> u32 {
    call_android_credentials_callback(
        session_handle,
        |callbacks| callbacks.delete,
        |callback| unsafe { callback(session_handle, service, account, access_group) },
    )
}

/// Forward the `contains` credentials request through the Android host ABI.
#[unsafe(no_mangle)]
pub(crate) unsafe extern "C" fn destack_host_android_credentials_contains(
    session_handle: u64,
    service: NativeStringRef,
    account: NativeStringRef,
    access_group: NativeStringRef,
    is_present: *mut bool,
) -> u32 {
    if is_present.is_null() {
        return HOST_STATUS_INVALID_ARGUMENT;
    }

    call_android_credentials_callback(
        session_handle,
        |callbacks| callbacks.contains,
        |callback| unsafe { callback(session_handle, service, account, access_group, is_present) },
    )
}

/// Forward the `authenticate` credentials request through the Android host ABI.
#[unsafe(no_mangle)]
pub(crate) unsafe extern "C" fn destack_host_android_credentials_authenticate(
    session_handle: u64,
    title: NativeStringRef,
    subtitle: NativeStringRef,
    message: NativeStringRef,
    requirement: u32,
    authenticated: *mut bool,
    mechanism: *mut u32,
) -> u32 {
    if authenticated.is_null() {
        return HOST_STATUS_INVALID_ARGUMENT;
    }

    if mechanism.is_null() {
        return HOST_STATUS_INVALID_ARGUMENT;
    }

    call_android_credentials_callback(
        session_handle,
        |callbacks| callbacks.authenticate,
        |callback| unsafe {
            callback(
                session_handle,
                title,
                subtitle,
                message,
                requirement,
                authenticated,
                mechanism,
            )
        },
    )
}
