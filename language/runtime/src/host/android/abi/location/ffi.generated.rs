// generated by generate-bindings: do not edit

use super::callbacks::{call_android_location_callback, AndroidHostLocationCallbacks};
use crate::host::abi::location::{LocationSample, LocationWatchOptions};
use crate::host::core::HOST_STATUS_INVALID_ARGUMENT;
use crate::platform::abi::NativeStringRef;

/// Forward the `services_enabled` location request through the Android host ABI.
#[unsafe(no_mangle)]
pub(crate) unsafe extern "C" fn destack_host_android_location_services_enabled(
    session_handle: u64,
    is_enabled: *mut bool,
) -> u32 {
    if is_enabled.is_null() {
        return HOST_STATUS_INVALID_ARGUMENT;
    }

    call_android_location_callback(
        session_handle,
        |callbacks| callbacks.services_enabled,
        |callback| unsafe { callback(session_handle, is_enabled) },
    )
}

/// Forward the `last_known` location request through the Android host ABI.
#[unsafe(no_mangle)]
pub(crate) unsafe extern "C" fn destack_host_android_location_last_known(
    session_handle: u64,
    sample: *mut LocationSample,
) -> u32 {
    if sample.is_null() {
        return HOST_STATUS_INVALID_ARGUMENT;
    }

    call_android_location_callback(
        session_handle,
        |callbacks| callbacks.last_known,
        |callback| unsafe { callback(session_handle, sample) },
    )
}

/// Forward the `watch_open` location request through the Android host ABI.
#[unsafe(no_mangle)]
pub(crate) unsafe extern "C" fn destack_host_android_location_watch_open(
    session_handle: u64,
    watch_id: NativeStringRef,
    options: LocationWatchOptions,
) -> u32 {
    call_android_location_callback(
        session_handle,
        |callbacks| callbacks.watch_open,
        |callback| unsafe { callback(session_handle, watch_id, options) },
    )
}

/// Forward the `watch_close` location request through the Android host ABI.
#[unsafe(no_mangle)]
pub(crate) unsafe extern "C" fn destack_host_android_location_watch_close(
    session_handle: u64,
    watch_id: NativeStringRef,
) -> u32 {
    call_android_location_callback(
        session_handle,
        |callbacks| callbacks.watch_close,
        |callback| unsafe { callback(session_handle, watch_id) },
    )
}
