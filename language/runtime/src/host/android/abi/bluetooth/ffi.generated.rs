// generated by generate-bindings: do not edit

use super::callbacks::{AndroidHostBluetoothCallbacks, call_android_bluetooth_callback};
use super::types::{
    AndroidHostBluetoothAdapterDescriptorHeader, AndroidHostBluetoothDeviceDescriptorHeader,
    AndroidHostBluetoothGattCharacteristicHeader, AndroidHostBluetoothGattDescriptorHeader,
    AndroidHostBluetoothGattServiceHeader, AndroidHostBluetoothScanEventHeader,
    AndroidHostBluetoothScanFilterHeader, AndroidHostBluetoothSessionEventHeader,
};
use crate::host::HOST_STATUS_INVALID_ARGUMENT;
use crate::platform::abi::{NativeSlice, NativeStringRef};

/// Forward the `adapter_list` bluetooth request through the Android host ABI.
#[unsafe(no_mangle)]
pub(crate) unsafe extern "C" fn destack_host_android_bluetooth_adapter_list(
    session_handle: u64,
    adapters: NativeSlice<AndroidHostBluetoothAdapterDescriptorHeader>,
    adapter_count_written: *mut u32,
    string_bytes: NativeSlice<u8>,
    string_bytes_written: *mut u32,
) -> u32 {
    if adapter_count_written.is_null() {
        return HOST_STATUS_INVALID_ARGUMENT;
    }

    if string_bytes_written.is_null() {
        return HOST_STATUS_INVALID_ARGUMENT;
    }

    call_android_bluetooth_callback(
        session_handle,
        |callbacks| callbacks.adapter_list,
        |callback| unsafe {
            callback(
                session_handle,
                adapters,
                adapter_count_written,
                string_bytes,
                string_bytes_written,
            )
        },
    )
}

/// Forward the `scan_open` bluetooth request through the Android host ABI.
#[unsafe(no_mangle)]
pub(crate) unsafe extern "C" fn destack_host_android_bluetooth_scan_open(
    session_handle: u64,
    adapter_id: NativeStringRef,
    filter: AndroidHostBluetoothScanFilterHeader,
    filter_flags: u32,
    filter_bytes: NativeSlice<u8>,
    session_id: *mut u64,
) -> u32 {
    if session_id.is_null() {
        return HOST_STATUS_INVALID_ARGUMENT;
    }

    call_android_bluetooth_callback(
        session_handle,
        |callbacks| callbacks.scan_open,
        |callback| unsafe {
            callback(
                session_handle,
                adapter_id,
                filter,
                filter_flags,
                filter_bytes,
                session_id,
            )
        },
    )
}

/// Forward the `scan_close` bluetooth request through the Android host ABI.
#[unsafe(no_mangle)]
pub(crate) unsafe extern "C" fn destack_host_android_bluetooth_scan_close(
    session_handle: u64,
    session_id: u64,
) -> u32 {
    call_android_bluetooth_callback(
        session_handle,
        |callbacks| callbacks.scan_close,
        |callback| unsafe { callback(session_handle, session_id) },
    )
}

/// Forward the `scan_read` bluetooth request through the Android host ABI.
#[unsafe(no_mangle)]
pub(crate) unsafe extern "C" fn destack_host_android_bluetooth_scan_read(
    session_handle: u64,
    session_id: u64,
    timeout_ns: u64,
    devices: NativeSlice<AndroidHostBluetoothDeviceDescriptorHeader>,
    device_count_written: *mut u32,
    string_bytes: NativeSlice<u8>,
    string_bytes_written: *mut u32,
) -> u32 {
    if device_count_written.is_null() {
        return HOST_STATUS_INVALID_ARGUMENT;
    }

    if string_bytes_written.is_null() {
        return HOST_STATUS_INVALID_ARGUMENT;
    }

    call_android_bluetooth_callback(
        session_handle,
        |callbacks| callbacks.scan_read,
        |callback| unsafe {
            callback(
                session_handle,
                session_id,
                timeout_ns,
                devices,
                device_count_written,
                string_bytes,
                string_bytes_written,
            )
        },
    )
}

/// Forward the `scan_try_read` bluetooth request through the Android host ABI.
#[unsafe(no_mangle)]
pub(crate) unsafe extern "C" fn destack_host_android_bluetooth_scan_try_read(
    session_handle: u64,
    session_id: u64,
    devices: NativeSlice<AndroidHostBluetoothDeviceDescriptorHeader>,
    device_count_written: *mut u32,
    string_bytes: NativeSlice<u8>,
    string_bytes_written: *mut u32,
) -> u32 {
    if device_count_written.is_null() {
        return HOST_STATUS_INVALID_ARGUMENT;
    }

    if string_bytes_written.is_null() {
        return HOST_STATUS_INVALID_ARGUMENT;
    }

    call_android_bluetooth_callback(
        session_handle,
        |callbacks| callbacks.scan_try_read,
        |callback| unsafe {
            callback(
                session_handle,
                session_id,
                devices,
                device_count_written,
                string_bytes,
                string_bytes_written,
            )
        },
    )
}

/// Forward the `scan_read_event` bluetooth request through the Android host ABI.
#[unsafe(no_mangle)]
pub(crate) unsafe extern "C" fn destack_host_android_bluetooth_scan_read_event(
    session_handle: u64,
    session_id: u64,
    timeout_ns: u64,
    event: *mut AndroidHostBluetoothScanEventHeader,
    string_bytes: NativeSlice<u8>,
    string_bytes_written: *mut u32,
) -> u32 {
    if event.is_null() {
        return HOST_STATUS_INVALID_ARGUMENT;
    }

    if string_bytes_written.is_null() {
        return HOST_STATUS_INVALID_ARGUMENT;
    }

    call_android_bluetooth_callback(
        session_handle,
        |callbacks| callbacks.scan_read_event,
        |callback| unsafe {
            callback(
                session_handle,
                session_id,
                timeout_ns,
                event,
                string_bytes,
                string_bytes_written,
            )
        },
    )
}

/// Forward the `scan_try_read_event` bluetooth request through the Android host ABI.
#[unsafe(no_mangle)]
pub(crate) unsafe extern "C" fn destack_host_android_bluetooth_scan_try_read_event(
    session_handle: u64,
    session_id: u64,
    event: *mut AndroidHostBluetoothScanEventHeader,
    string_bytes: NativeSlice<u8>,
    string_bytes_written: *mut u32,
) -> u32 {
    if event.is_null() {
        return HOST_STATUS_INVALID_ARGUMENT;
    }

    if string_bytes_written.is_null() {
        return HOST_STATUS_INVALID_ARGUMENT;
    }

    call_android_bluetooth_callback(
        session_handle,
        |callbacks| callbacks.scan_try_read_event,
        |callback| unsafe {
            callback(
                session_handle,
                session_id,
                event,
                string_bytes,
                string_bytes_written,
            )
        },
    )
}

/// Forward the `open` bluetooth request through the Android host ABI.
#[unsafe(no_mangle)]
pub(crate) unsafe extern "C" fn destack_host_android_bluetooth_open(
    session_handle: u64,
    adapter_id: NativeStringRef,
    device_id: NativeStringRef,
    session_id: *mut u64,
    descriptor: *mut AndroidHostBluetoothDeviceDescriptorHeader,
    string_bytes: NativeSlice<u8>,
    string_bytes_written: *mut u32,
) -> u32 {
    if session_id.is_null() {
        return HOST_STATUS_INVALID_ARGUMENT;
    }

    if descriptor.is_null() {
        return HOST_STATUS_INVALID_ARGUMENT;
    }

    if string_bytes_written.is_null() {
        return HOST_STATUS_INVALID_ARGUMENT;
    }

    call_android_bluetooth_callback(
        session_handle,
        |callbacks| callbacks.open,
        |callback| unsafe {
            callback(
                session_handle,
                adapter_id,
                device_id,
                session_id,
                descriptor,
                string_bytes,
                string_bytes_written,
            )
        },
    )
}

/// Forward the `close` bluetooth request through the Android host ABI.
#[unsafe(no_mangle)]
pub(crate) unsafe extern "C" fn destack_host_android_bluetooth_close(
    session_handle: u64,
    session_id: u64,
) -> u32 {
    call_android_bluetooth_callback(
        session_handle,
        |callbacks| callbacks.close,
        |callback| unsafe { callback(session_handle, session_id) },
    )
}

/// Forward the `pair` bluetooth request through the Android host ABI.
#[unsafe(no_mangle)]
pub(crate) unsafe extern "C" fn destack_host_android_bluetooth_pair(
    session_handle: u64,
    session_id: u64,
    timeout_ns: u64,
) -> u32 {
    call_android_bluetooth_callback(
        session_handle,
        |callbacks| callbacks.pair,
        |callback| unsafe { callback(session_handle, session_id, timeout_ns) },
    )
}

/// Forward the `unpair` bluetooth request through the Android host ABI.
#[unsafe(no_mangle)]
pub(crate) unsafe extern "C" fn destack_host_android_bluetooth_unpair(
    session_handle: u64,
    adapter_id: NativeStringRef,
    device_id: NativeStringRef,
) -> u32 {
    call_android_bluetooth_callback(
        session_handle,
        |callbacks| callbacks.unpair,
        |callback| unsafe { callback(session_handle, adapter_id, device_id) },
    )
}

/// Forward the `read_rssi` bluetooth request through the Android host ABI.
#[unsafe(no_mangle)]
pub(crate) unsafe extern "C" fn destack_host_android_bluetooth_read_rssi(
    session_handle: u64,
    session_id: u64,
    timeout_ns: u64,
    rssi_dbm: *mut i16,
) -> u32 {
    if rssi_dbm.is_null() {
        return HOST_STATUS_INVALID_ARGUMENT;
    }

    call_android_bluetooth_callback(
        session_handle,
        |callbacks| callbacks.read_rssi,
        |callback| unsafe { callback(session_handle, session_id, timeout_ns, rssi_dbm) },
    )
}

/// Forward the `session_read_event` bluetooth request through the Android host ABI.
#[unsafe(no_mangle)]
pub(crate) unsafe extern "C" fn destack_host_android_bluetooth_session_read_event(
    session_handle: u64,
    session_id: u64,
    timeout_ns: u64,
    event: *mut AndroidHostBluetoothSessionEventHeader,
) -> u32 {
    if event.is_null() {
        return HOST_STATUS_INVALID_ARGUMENT;
    }

    call_android_bluetooth_callback(
        session_handle,
        |callbacks| callbacks.session_read_event,
        |callback| unsafe { callback(session_handle, session_id, timeout_ns, event) },
    )
}

/// Forward the `session_try_read_event` bluetooth request through the Android host ABI.
#[unsafe(no_mangle)]
pub(crate) unsafe extern "C" fn destack_host_android_bluetooth_session_try_read_event(
    session_handle: u64,
    session_id: u64,
    event: *mut AndroidHostBluetoothSessionEventHeader,
) -> u32 {
    if event.is_null() {
        return HOST_STATUS_INVALID_ARGUMENT;
    }

    call_android_bluetooth_callback(
        session_handle,
        |callbacks| callbacks.session_try_read_event,
        |callback| unsafe { callback(session_handle, session_id, event) },
    )
}

/// Forward the `gatt_service_list` bluetooth request through the Android host ABI.
#[unsafe(no_mangle)]
pub(crate) unsafe extern "C" fn destack_host_android_bluetooth_gatt_service_list(
    session_handle: u64,
    session_id: u64,
    services: NativeSlice<AndroidHostBluetoothGattServiceHeader>,
    service_count_written: *mut u32,
    string_bytes: NativeSlice<u8>,
    string_bytes_written: *mut u32,
) -> u32 {
    if service_count_written.is_null() {
        return HOST_STATUS_INVALID_ARGUMENT;
    }

    if string_bytes_written.is_null() {
        return HOST_STATUS_INVALID_ARGUMENT;
    }

    call_android_bluetooth_callback(
        session_handle,
        |callbacks| callbacks.gatt_service_list,
        |callback| unsafe {
            callback(
                session_handle,
                session_id,
                services,
                service_count_written,
                string_bytes,
                string_bytes_written,
            )
        },
    )
}

/// Forward the `gatt_characteristic_list` bluetooth request through the Android host ABI.
#[unsafe(no_mangle)]
pub(crate) unsafe extern "C" fn destack_host_android_bluetooth_gatt_characteristic_list(
    session_handle: u64,
    session_id: u64,
    service_id: NativeStringRef,
    characteristics: NativeSlice<AndroidHostBluetoothGattCharacteristicHeader>,
    characteristic_count_written: *mut u32,
    string_bytes: NativeSlice<u8>,
    string_bytes_written: *mut u32,
) -> u32 {
    if characteristic_count_written.is_null() {
        return HOST_STATUS_INVALID_ARGUMENT;
    }

    if string_bytes_written.is_null() {
        return HOST_STATUS_INVALID_ARGUMENT;
    }

    call_android_bluetooth_callback(
        session_handle,
        |callbacks| callbacks.gatt_characteristic_list,
        |callback| unsafe {
            callback(
                session_handle,
                session_id,
                service_id,
                characteristics,
                characteristic_count_written,
                string_bytes,
                string_bytes_written,
            )
        },
    )
}

/// Forward the `gatt_descriptor_list` bluetooth request through the Android host ABI.
#[unsafe(no_mangle)]
pub(crate) unsafe extern "C" fn destack_host_android_bluetooth_gatt_descriptor_list(
    session_handle: u64,
    session_id: u64,
    characteristic_id: NativeStringRef,
    descriptors: NativeSlice<AndroidHostBluetoothGattDescriptorHeader>,
    descriptor_count_written: *mut u32,
    string_bytes: NativeSlice<u8>,
    string_bytes_written: *mut u32,
) -> u32 {
    if descriptor_count_written.is_null() {
        return HOST_STATUS_INVALID_ARGUMENT;
    }

    if string_bytes_written.is_null() {
        return HOST_STATUS_INVALID_ARGUMENT;
    }

    call_android_bluetooth_callback(
        session_handle,
        |callbacks| callbacks.gatt_descriptor_list,
        |callback| unsafe {
            callback(
                session_handle,
                session_id,
                characteristic_id,
                descriptors,
                descriptor_count_written,
                string_bytes,
                string_bytes_written,
            )
        },
    )
}

/// Forward the `gatt_mtu` bluetooth request through the Android host ABI.
#[unsafe(no_mangle)]
pub(crate) unsafe extern "C" fn destack_host_android_bluetooth_gatt_mtu(
    session_handle: u64,
    session_id: u64,
    mtu: *mut u16,
) -> u32 {
    if mtu.is_null() {
        return HOST_STATUS_INVALID_ARGUMENT;
    }

    call_android_bluetooth_callback(
        session_handle,
        |callbacks| callbacks.gatt_mtu,
        |callback| unsafe { callback(session_handle, session_id, mtu) },
    )
}

/// Forward the `gatt_read` bluetooth request through the Android host ABI.
#[unsafe(no_mangle)]
pub(crate) unsafe extern "C" fn destack_host_android_bluetooth_gatt_read(
    session_handle: u64,
    session_id: u64,
    characteristic_id: NativeStringRef,
    timeout_ns: u64,
    bytes: NativeSlice<u8>,
    bytes_written: *mut u32,
) -> u32 {
    if bytes_written.is_null() {
        return HOST_STATUS_INVALID_ARGUMENT;
    }

    call_android_bluetooth_callback(
        session_handle,
        |callbacks| callbacks.gatt_read,
        |callback| unsafe {
            callback(
                session_handle,
                session_id,
                characteristic_id,
                timeout_ns,
                bytes,
                bytes_written,
            )
        },
    )
}

/// Forward the `gatt_read_descriptor` bluetooth request through the Android host ABI.
#[unsafe(no_mangle)]
pub(crate) unsafe extern "C" fn destack_host_android_bluetooth_gatt_read_descriptor(
    session_handle: u64,
    session_id: u64,
    descriptor_id: NativeStringRef,
    timeout_ns: u64,
    bytes: NativeSlice<u8>,
    bytes_written: *mut u32,
) -> u32 {
    if bytes_written.is_null() {
        return HOST_STATUS_INVALID_ARGUMENT;
    }

    call_android_bluetooth_callback(
        session_handle,
        |callbacks| callbacks.gatt_read_descriptor,
        |callback| unsafe {
            callback(
                session_handle,
                session_id,
                descriptor_id,
                timeout_ns,
                bytes,
                bytes_written,
            )
        },
    )
}

/// Forward the `gatt_write` bluetooth request through the Android host ABI.
#[unsafe(no_mangle)]
pub(crate) unsafe extern "C" fn destack_host_android_bluetooth_gatt_write(
    session_handle: u64,
    session_id: u64,
    characteristic_id: NativeStringRef,
    mode: u32,
    value: NativeSlice<u8>,
    timeout_ns: u64,
) -> u32 {
    call_android_bluetooth_callback(
        session_handle,
        |callbacks| callbacks.gatt_write,
        |callback| unsafe {
            callback(
                session_handle,
                session_id,
                characteristic_id,
                mode,
                value,
                timeout_ns,
            )
        },
    )
}

/// Forward the `gatt_write_descriptor` bluetooth request through the Android host ABI.
#[unsafe(no_mangle)]
pub(crate) unsafe extern "C" fn destack_host_android_bluetooth_gatt_write_descriptor(
    session_handle: u64,
    session_id: u64,
    descriptor_id: NativeStringRef,
    value: NativeSlice<u8>,
    timeout_ns: u64,
) -> u32 {
    call_android_bluetooth_callback(
        session_handle,
        |callbacks| callbacks.gatt_write_descriptor,
        |callback| unsafe {
            callback(session_handle, session_id, descriptor_id, value, timeout_ns)
        },
    )
}

/// Forward the `gatt_subscribe` bluetooth request through the Android host ABI.
#[unsafe(no_mangle)]
pub(crate) unsafe extern "C" fn destack_host_android_bluetooth_gatt_subscribe(
    session_handle: u64,
    session_id: u64,
    characteristic_id: NativeStringRef,
    subscription_id: *mut u64,
) -> u32 {
    if subscription_id.is_null() {
        return HOST_STATUS_INVALID_ARGUMENT;
    }

    call_android_bluetooth_callback(
        session_handle,
        |callbacks| callbacks.gatt_subscribe,
        |callback| unsafe {
            callback(
                session_handle,
                session_id,
                characteristic_id,
                subscription_id,
            )
        },
    )
}

/// Forward the `gatt_unsubscribe` bluetooth request through the Android host ABI.
#[unsafe(no_mangle)]
pub(crate) unsafe extern "C" fn destack_host_android_bluetooth_gatt_unsubscribe(
    session_handle: u64,
    subscription_id: u64,
) -> u32 {
    call_android_bluetooth_callback(
        session_handle,
        |callbacks| callbacks.gatt_unsubscribe,
        |callback| unsafe { callback(session_handle, subscription_id) },
    )
}

/// Forward the `gatt_read_event` bluetooth request through the Android host ABI.
#[unsafe(no_mangle)]
pub(crate) unsafe extern "C" fn destack_host_android_bluetooth_gatt_read_event(
    session_handle: u64,
    subscription_id: u64,
    timeout_ns: u64,
    event: *mut NativeSlice<u8>,
    timestamp_ns: *mut u64,
) -> u32 {
    if event.is_null() {
        return HOST_STATUS_INVALID_ARGUMENT;
    }

    if timestamp_ns.is_null() {
        return HOST_STATUS_INVALID_ARGUMENT;
    }

    call_android_bluetooth_callback(
        session_handle,
        |callbacks| callbacks.gatt_read_event,
        |callback| unsafe {
            callback(
                session_handle,
                subscription_id,
                timeout_ns,
                event,
                timestamp_ns,
            )
        },
    )
}

/// Forward the `gatt_try_read_event` bluetooth request through the Android host ABI.
#[unsafe(no_mangle)]
pub(crate) unsafe extern "C" fn destack_host_android_bluetooth_gatt_try_read_event(
    session_handle: u64,
    subscription_id: u64,
    event: *mut NativeSlice<u8>,
    timestamp_ns: *mut u64,
) -> u32 {
    if event.is_null() {
        return HOST_STATUS_INVALID_ARGUMENT;
    }

    if timestamp_ns.is_null() {
        return HOST_STATUS_INVALID_ARGUMENT;
    }

    call_android_bluetooth_callback(
        session_handle,
        |callbacks| callbacks.gatt_try_read_event,
        |callback| unsafe { callback(session_handle, subscription_id, event, timestamp_ns) },
    )
}
