// generated by generate-bindings: do not edit

use super::types::{
    AndroidHostBluetoothAdapterDescriptorHeader, AndroidHostBluetoothDeviceDescriptorHeader,
    AndroidHostBluetoothGattCharacteristicHeader, AndroidHostBluetoothGattDescriptorHeader,
    AndroidHostBluetoothGattServiceHeader, AndroidHostBluetoothScanEventHeader,
    AndroidHostBluetoothScanFilterHeader, AndroidHostBluetoothSessionEventHeader,
};
use crate::host::android::abi::binding::invoke_android_binding_callback;
use crate::platform::abi::{NativeSlice, NativeStringRef};

/// The host callback for `adapter_list`.
pub(crate) type AndroidHostBluetoothAdapterListCallback = unsafe extern "C" fn(
    session_handle: u64,
    adapters: NativeSlice<AndroidHostBluetoothAdapterDescriptorHeader>,
    adapter_count_written: *mut u32,
    string_bytes: NativeSlice<u8>,
    string_bytes_written: *mut u32,
) -> u32;

/// The host callback for `scan_open`.
pub(crate) type AndroidHostBluetoothScanOpenCallback = unsafe extern "C" fn(
    session_handle: u64,
    adapter_id: NativeStringRef,
    filter: AndroidHostBluetoothScanFilterHeader,
    filter_flags: u32,
    filter_bytes: NativeSlice<u8>,
    session_id: *mut u64,
) -> u32;

/// The host callback for `scan_close`.
pub(crate) type AndroidHostBluetoothScanCloseCallback =
    unsafe extern "C" fn(session_handle: u64, session_id: u64) -> u32;

/// The host callback for `scan_read`.
pub(crate) type AndroidHostBluetoothScanReadCallback = unsafe extern "C" fn(
    session_handle: u64,
    session_id: u64,
    timeout_ns: u64,
    devices: NativeSlice<AndroidHostBluetoothDeviceDescriptorHeader>,
    device_count_written: *mut u32,
    string_bytes: NativeSlice<u8>,
    string_bytes_written: *mut u32,
) -> u32;

/// The host callback for `scan_try_read`.
pub(crate) type AndroidHostBluetoothScanTryReadCallback = unsafe extern "C" fn(
    session_handle: u64,
    session_id: u64,
    devices: NativeSlice<AndroidHostBluetoothDeviceDescriptorHeader>,
    device_count_written: *mut u32,
    string_bytes: NativeSlice<u8>,
    string_bytes_written: *mut u32,
) -> u32;

/// The host callback for `scan_read_event`.
pub(crate) type AndroidHostBluetoothScanReadEventCallback = unsafe extern "C" fn(
    session_handle: u64,
    session_id: u64,
    timeout_ns: u64,
    event: *mut AndroidHostBluetoothScanEventHeader,
    string_bytes: NativeSlice<u8>,
    string_bytes_written: *mut u32,
) -> u32;

/// The host callback for `scan_try_read_event`.
pub(crate) type AndroidHostBluetoothScanTryReadEventCallback = unsafe extern "C" fn(
    session_handle: u64,
    session_id: u64,
    event: *mut AndroidHostBluetoothScanEventHeader,
    string_bytes: NativeSlice<u8>,
    string_bytes_written: *mut u32,
) -> u32;

/// The host callback for `open`.
pub(crate) type AndroidHostBluetoothOpenCallback = unsafe extern "C" fn(
    session_handle: u64,
    adapter_id: NativeStringRef,
    device_id: NativeStringRef,
    session_id: *mut u64,
    descriptor: *mut AndroidHostBluetoothDeviceDescriptorHeader,
    string_bytes: NativeSlice<u8>,
    string_bytes_written: *mut u32,
) -> u32;

/// The host callback for `close`.
pub(crate) type AndroidHostBluetoothCloseCallback =
    unsafe extern "C" fn(session_handle: u64, session_id: u64) -> u32;

/// The host callback for `pair`.
pub(crate) type AndroidHostBluetoothPairCallback =
    unsafe extern "C" fn(session_handle: u64, session_id: u64, timeout_ns: u64) -> u32;

/// The host callback for `unpair`.
pub(crate) type AndroidHostBluetoothUnpairCallback = unsafe extern "C" fn(
    session_handle: u64,
    adapter_id: NativeStringRef,
    device_id: NativeStringRef,
) -> u32;

/// The host callback for `read_rssi`.
pub(crate) type AndroidHostBluetoothReadRssiCallback = unsafe extern "C" fn(
    session_handle: u64,
    session_id: u64,
    timeout_ns: u64,
    rssi_dbm: *mut i16,
) -> u32;

/// The host callback for `session_read_event`.
pub(crate) type AndroidHostBluetoothSessionReadEventCallback = unsafe extern "C" fn(
    session_handle: u64,
    session_id: u64,
    timeout_ns: u64,
    event: *mut AndroidHostBluetoothSessionEventHeader,
) -> u32;

/// The host callback for `session_try_read_event`.
pub(crate) type AndroidHostBluetoothSessionTryReadEventCallback = unsafe extern "C" fn(
    session_handle: u64,
    session_id: u64,
    event: *mut AndroidHostBluetoothSessionEventHeader,
) -> u32;

/// The host callback for `gatt_service_list`.
pub(crate) type AndroidHostBluetoothGattServiceListCallback = unsafe extern "C" fn(
    session_handle: u64,
    session_id: u64,
    services: NativeSlice<AndroidHostBluetoothGattServiceHeader>,
    service_count_written: *mut u32,
    string_bytes: NativeSlice<u8>,
    string_bytes_written: *mut u32,
) -> u32;

/// The host callback for `gatt_characteristic_list`.
pub(crate) type AndroidHostBluetoothGattCharacteristicListCallback = unsafe extern "C" fn(
    session_handle: u64,
    session_id: u64,
    service_id: NativeStringRef,
    characteristics: NativeSlice<AndroidHostBluetoothGattCharacteristicHeader>,
    characteristic_count_written: *mut u32,
    string_bytes: NativeSlice<u8>,
    string_bytes_written: *mut u32,
) -> u32;

/// The host callback for `gatt_descriptor_list`.
pub(crate) type AndroidHostBluetoothGattDescriptorListCallback = unsafe extern "C" fn(
    session_handle: u64,
    session_id: u64,
    characteristic_id: NativeStringRef,
    descriptors: NativeSlice<AndroidHostBluetoothGattDescriptorHeader>,
    descriptor_count_written: *mut u32,
    string_bytes: NativeSlice<u8>,
    string_bytes_written: *mut u32,
) -> u32;

/// The host callback for `gatt_mtu`.
pub(crate) type AndroidHostBluetoothGattMtuCallback =
    unsafe extern "C" fn(session_handle: u64, session_id: u64, mtu: *mut u16) -> u32;

/// The host callback for `gatt_read`.
pub(crate) type AndroidHostBluetoothGattReadCallback = unsafe extern "C" fn(
    session_handle: u64,
    session_id: u64,
    characteristic_id: NativeStringRef,
    timeout_ns: u64,
    bytes: NativeSlice<u8>,
    bytes_written: *mut u32,
) -> u32;

/// The host callback for `gatt_read_descriptor`.
pub(crate) type AndroidHostBluetoothGattReadDescriptorCallback = unsafe extern "C" fn(
    session_handle: u64,
    session_id: u64,
    descriptor_id: NativeStringRef,
    timeout_ns: u64,
    bytes: NativeSlice<u8>,
    bytes_written: *mut u32,
) -> u32;

/// The host callback for `gatt_write`.
pub(crate) type AndroidHostBluetoothGattWriteCallback = unsafe extern "C" fn(
    session_handle: u64,
    session_id: u64,
    characteristic_id: NativeStringRef,
    mode: u32,
    value: NativeSlice<u8>,
    timeout_ns: u64,
) -> u32;

/// The host callback for `gatt_write_descriptor`.
pub(crate) type AndroidHostBluetoothGattWriteDescriptorCallback = unsafe extern "C" fn(
    session_handle: u64,
    session_id: u64,
    descriptor_id: NativeStringRef,
    value: NativeSlice<u8>,
    timeout_ns: u64,
) -> u32;

/// The host callback for `gatt_subscribe`.
pub(crate) type AndroidHostBluetoothGattSubscribeCallback = unsafe extern "C" fn(
    session_handle: u64,
    session_id: u64,
    characteristic_id: NativeStringRef,
    subscription_id: *mut u64,
) -> u32;

/// The host callback for `gatt_unsubscribe`.
pub(crate) type AndroidHostBluetoothGattUnsubscribeCallback =
    unsafe extern "C" fn(session_handle: u64, subscription_id: u64) -> u32;

/// The host callback for `gatt_read_event`.
pub(crate) type AndroidHostBluetoothGattReadEventCallback = unsafe extern "C" fn(
    session_handle: u64,
    subscription_id: u64,
    timeout_ns: u64,
    event: *mut NativeSlice<u8>,
    timestamp_ns: *mut u64,
) -> u32;

/// The host callback for `gatt_try_read_event`.
pub(crate) type AndroidHostBluetoothGattTryReadEventCallback = unsafe extern "C" fn(
    session_handle: u64,
    subscription_id: u64,
    event: *mut NativeSlice<u8>,
    timestamp_ns: *mut u64,
) -> u32;

/// Callback table for Android host bluetooth request interop.
#[derive(Clone, Copy, Debug, Default)]
#[repr(C)]
pub(crate) struct AndroidHostBluetoothCallbacks {
    /// The callback for `adapter_list`.
    pub adapter_list: Option<AndroidHostBluetoothAdapterListCallback>,
    /// The callback for `scan_open`.
    pub scan_open: Option<AndroidHostBluetoothScanOpenCallback>,
    /// The callback for `scan_close`.
    pub scan_close: Option<AndroidHostBluetoothScanCloseCallback>,
    /// The callback for `scan_read`.
    pub scan_read: Option<AndroidHostBluetoothScanReadCallback>,
    /// The callback for `scan_try_read`.
    pub scan_try_read: Option<AndroidHostBluetoothScanTryReadCallback>,
    /// The callback for `scan_read_event`.
    pub scan_read_event: Option<AndroidHostBluetoothScanReadEventCallback>,
    /// The callback for `scan_try_read_event`.
    pub scan_try_read_event: Option<AndroidHostBluetoothScanTryReadEventCallback>,
    /// The callback for `open`.
    pub open: Option<AndroidHostBluetoothOpenCallback>,
    /// The callback for `close`.
    pub close: Option<AndroidHostBluetoothCloseCallback>,
    /// The callback for `pair`.
    pub pair: Option<AndroidHostBluetoothPairCallback>,
    /// The callback for `unpair`.
    pub unpair: Option<AndroidHostBluetoothUnpairCallback>,
    /// The callback for `read_rssi`.
    pub read_rssi: Option<AndroidHostBluetoothReadRssiCallback>,
    /// The callback for `session_read_event`.
    pub session_read_event: Option<AndroidHostBluetoothSessionReadEventCallback>,
    /// The callback for `session_try_read_event`.
    pub session_try_read_event: Option<AndroidHostBluetoothSessionTryReadEventCallback>,
    /// The callback for `gatt_service_list`.
    pub gatt_service_list: Option<AndroidHostBluetoothGattServiceListCallback>,
    /// The callback for `gatt_characteristic_list`.
    pub gatt_characteristic_list: Option<AndroidHostBluetoothGattCharacteristicListCallback>,
    /// The callback for `gatt_descriptor_list`.
    pub gatt_descriptor_list: Option<AndroidHostBluetoothGattDescriptorListCallback>,
    /// The callback for `gatt_mtu`.
    pub gatt_mtu: Option<AndroidHostBluetoothGattMtuCallback>,
    /// The callback for `gatt_read`.
    pub gatt_read: Option<AndroidHostBluetoothGattReadCallback>,
    /// The callback for `gatt_read_descriptor`.
    pub gatt_read_descriptor: Option<AndroidHostBluetoothGattReadDescriptorCallback>,
    /// The callback for `gatt_write`.
    pub gatt_write: Option<AndroidHostBluetoothGattWriteCallback>,
    /// The callback for `gatt_write_descriptor`.
    pub gatt_write_descriptor: Option<AndroidHostBluetoothGattWriteDescriptorCallback>,
    /// The callback for `gatt_subscribe`.
    pub gatt_subscribe: Option<AndroidHostBluetoothGattSubscribeCallback>,
    /// The callback for `gatt_unsubscribe`.
    pub gatt_unsubscribe: Option<AndroidHostBluetoothGattUnsubscribeCallback>,
    /// The callback for `gatt_read_event`.
    pub gatt_read_event: Option<AndroidHostBluetoothGattReadEventCallback>,
    /// The callback for `gatt_try_read_event`.
    pub gatt_try_read_event: Option<AndroidHostBluetoothGattTryReadEventCallback>,
}

/// Resolve and invoke one Android host bluetooth callback.
pub(super) fn call_android_bluetooth_callback<T: Copy>(
    session_handle: u64,
    resolve: impl FnOnce(&AndroidHostBluetoothCallbacks) -> Option<T>,
    invoke: impl FnOnce(T) -> u32,
) -> u32 {
    invoke_android_binding_callback(
        session_handle,
        |bindings| resolve(&bindings.bluetooth),
        invoke,
    )
}
