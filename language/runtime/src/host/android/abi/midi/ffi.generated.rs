// generated by generate-bindings: do not edit

use super::callbacks::{AndroidHostMidiCallbacks, call_android_midi_callback};
use super::types::{
    AndroidHostMidiEventHeader, AndroidHostMidiInputRecordHeader, AndroidHostMidiOpenedPortHeader,
    AndroidHostMidiOutputRecordHeader, AndroidHostMidiPortDescriptorHeader,
};
use crate::host::HOST_STATUS_INVALID_ARGUMENT;
use crate::platform::abi::{NativeSlice, NativeStringRef};

/// Forward the `describe_backend` midi request through the Android host ABI.
#[unsafe(no_mangle)]
pub(crate) unsafe extern "C" fn destack_host_android_midi_describe_backend(
    session_handle: u64,
    capability_flags: *mut u64,
    supported_data_formats: *mut u32,
    supported_protocols: *mut u32,
) -> u32 {
    if capability_flags.is_null() {
        return HOST_STATUS_INVALID_ARGUMENT;
    }

    if supported_data_formats.is_null() {
        return HOST_STATUS_INVALID_ARGUMENT;
    }

    if supported_protocols.is_null() {
        return HOST_STATUS_INVALID_ARGUMENT;
    }

    call_android_midi_callback(
        session_handle,
        |callbacks| callbacks.describe_backend,
        |callback| unsafe {
            callback(
                session_handle,
                capability_flags,
                supported_data_formats,
                supported_protocols,
            )
        },
    )
}

/// Forward the `input_port_list` midi request through the Android host ABI.
#[unsafe(no_mangle)]
pub(crate) unsafe extern "C" fn destack_host_android_midi_input_port_list(
    session_handle: u64,
    flags: u32,
    headers: NativeSlice<AndroidHostMidiPortDescriptorHeader>,
    header_count_written: *mut u32,
    string_bytes: NativeSlice<u8>,
    string_bytes_written: *mut u32,
) -> u32 {
    if header_count_written.is_null() {
        return HOST_STATUS_INVALID_ARGUMENT;
    }

    if string_bytes_written.is_null() {
        return HOST_STATUS_INVALID_ARGUMENT;
    }

    call_android_midi_callback(
        session_handle,
        |callbacks| callbacks.input_port_list,
        |callback| unsafe {
            callback(
                session_handle,
                flags,
                headers,
                header_count_written,
                string_bytes,
                string_bytes_written,
            )
        },
    )
}

/// Forward the `output_port_list` midi request through the Android host ABI.
#[unsafe(no_mangle)]
pub(crate) unsafe extern "C" fn destack_host_android_midi_output_port_list(
    session_handle: u64,
    flags: u32,
    headers: NativeSlice<AndroidHostMidiPortDescriptorHeader>,
    header_count_written: *mut u32,
    string_bytes: NativeSlice<u8>,
    string_bytes_written: *mut u32,
) -> u32 {
    if header_count_written.is_null() {
        return HOST_STATUS_INVALID_ARGUMENT;
    }

    if string_bytes_written.is_null() {
        return HOST_STATUS_INVALID_ARGUMENT;
    }

    call_android_midi_callback(
        session_handle,
        |callbacks| callbacks.output_port_list,
        |callback| unsafe {
            callback(
                session_handle,
                flags,
                headers,
                header_count_written,
                string_bytes,
                string_bytes_written,
            )
        },
    )
}

/// Forward the `input_port_open` midi request through the Android host ABI.
#[unsafe(no_mangle)]
pub(crate) unsafe extern "C" fn destack_host_android_midi_input_port_open(
    session_handle: u64,
    id: NativeStringRef,
    data_format: u32,
    protocol: u32,
    queue_capacity: u32,
    opened_port: *mut AndroidHostMidiOpenedPortHeader,
    string_bytes: NativeSlice<u8>,
    string_bytes_written: *mut u32,
) -> u32 {
    if opened_port.is_null() {
        return HOST_STATUS_INVALID_ARGUMENT;
    }

    if string_bytes_written.is_null() {
        return HOST_STATUS_INVALID_ARGUMENT;
    }

    call_android_midi_callback(
        session_handle,
        |callbacks| callbacks.input_port_open,
        |callback| unsafe {
            callback(
                session_handle,
                id,
                data_format,
                protocol,
                queue_capacity,
                opened_port,
                string_bytes,
                string_bytes_written,
            )
        },
    )
}

/// Forward the `output_port_open` midi request through the Android host ABI.
#[unsafe(no_mangle)]
pub(crate) unsafe extern "C" fn destack_host_android_midi_output_port_open(
    session_handle: u64,
    id: NativeStringRef,
    data_format: u32,
    protocol: u32,
    opened_port: *mut AndroidHostMidiOpenedPortHeader,
    string_bytes: NativeSlice<u8>,
    string_bytes_written: *mut u32,
) -> u32 {
    if opened_port.is_null() {
        return HOST_STATUS_INVALID_ARGUMENT;
    }

    if string_bytes_written.is_null() {
        return HOST_STATUS_INVALID_ARGUMENT;
    }

    call_android_midi_callback(
        session_handle,
        |callbacks| callbacks.output_port_open,
        |callback| unsafe {
            callback(
                session_handle,
                id,
                data_format,
                protocol,
                opened_port,
                string_bytes,
                string_bytes_written,
            )
        },
    )
}

/// Forward the `input_virtual_create` midi request through the Android host ABI.
#[unsafe(no_mangle)]
pub(crate) unsafe extern "C" fn destack_host_android_midi_input_virtual_create(
    session_handle: u64,
    name: NativeStringRef,
    manufacturer: NativeStringRef,
    model: NativeStringRef,
    version: NativeStringRef,
    data_format: u32,
    protocol: u32,
    queue_capacity: u32,
    opened_port: *mut AndroidHostMidiOpenedPortHeader,
    string_bytes: NativeSlice<u8>,
    string_bytes_written: *mut u32,
) -> u32 {
    if opened_port.is_null() {
        return HOST_STATUS_INVALID_ARGUMENT;
    }

    if string_bytes_written.is_null() {
        return HOST_STATUS_INVALID_ARGUMENT;
    }

    call_android_midi_callback(
        session_handle,
        |callbacks| callbacks.input_virtual_create,
        |callback| unsafe {
            callback(
                session_handle,
                name,
                manufacturer,
                model,
                version,
                data_format,
                protocol,
                queue_capacity,
                opened_port,
                string_bytes,
                string_bytes_written,
            )
        },
    )
}

/// Forward the `output_virtual_create` midi request through the Android host ABI.
#[unsafe(no_mangle)]
pub(crate) unsafe extern "C" fn destack_host_android_midi_output_virtual_create(
    session_handle: u64,
    name: NativeStringRef,
    manufacturer: NativeStringRef,
    model: NativeStringRef,
    version: NativeStringRef,
    data_format: u32,
    protocol: u32,
    opened_port: *mut AndroidHostMidiOpenedPortHeader,
    string_bytes: NativeSlice<u8>,
    string_bytes_written: *mut u32,
) -> u32 {
    if opened_port.is_null() {
        return HOST_STATUS_INVALID_ARGUMENT;
    }

    if string_bytes_written.is_null() {
        return HOST_STATUS_INVALID_ARGUMENT;
    }

    call_android_midi_callback(
        session_handle,
        |callbacks| callbacks.output_virtual_create,
        |callback| unsafe {
            callback(
                session_handle,
                name,
                manufacturer,
                model,
                version,
                data_format,
                protocol,
                opened_port,
                string_bytes,
                string_bytes_written,
            )
        },
    )
}

/// Forward the `input_port_close` midi request through the Android host ABI.
#[unsafe(no_mangle)]
pub(crate) unsafe extern "C" fn destack_host_android_midi_input_port_close(
    session_handle: u64,
    session_id: u64,
) -> u32 {
    call_android_midi_callback(
        session_handle,
        |callbacks| callbacks.input_port_close,
        |callback| unsafe { callback(session_handle, session_id) },
    )
}

/// Forward the `output_port_close` midi request through the Android host ABI.
#[unsafe(no_mangle)]
pub(crate) unsafe extern "C" fn destack_host_android_midi_output_port_close(
    session_handle: u64,
    session_id: u64,
) -> u32 {
    call_android_midi_callback(
        session_handle,
        |callbacks| callbacks.output_port_close,
        |callback| unsafe { callback(session_handle, session_id) },
    )
}

/// Forward the `input_read` midi request through the Android host ABI.
#[unsafe(no_mangle)]
pub(crate) unsafe extern "C" fn destack_host_android_midi_input_read(
    session_handle: u64,
    session_id: u64,
    max_records: u32,
    timeout_ns: u64,
    headers: NativeSlice<AndroidHostMidiInputRecordHeader>,
    record_count_written: *mut u32,
    blob_bytes: NativeSlice<u8>,
    blob_bytes_written: *mut u32,
) -> u32 {
    if record_count_written.is_null() {
        return HOST_STATUS_INVALID_ARGUMENT;
    }

    if blob_bytes_written.is_null() {
        return HOST_STATUS_INVALID_ARGUMENT;
    }

    call_android_midi_callback(
        session_handle,
        |callbacks| callbacks.input_read,
        |callback| unsafe {
            callback(
                session_handle,
                session_id,
                max_records,
                timeout_ns,
                headers,
                record_count_written,
                blob_bytes,
                blob_bytes_written,
            )
        },
    )
}

/// Forward the `event_open` midi request through the Android host ABI.
#[unsafe(no_mangle)]
pub(crate) unsafe extern "C" fn destack_host_android_midi_event_open(
    session_handle: u64,
    flags: u32,
    direction_mask: u32,
    session_id: *mut u64,
) -> u32 {
    if session_id.is_null() {
        return HOST_STATUS_INVALID_ARGUMENT;
    }

    call_android_midi_callback(
        session_handle,
        |callbacks| callbacks.event_open,
        |callback| unsafe { callback(session_handle, flags, direction_mask, session_id) },
    )
}

/// Forward the `event_read` midi request through the Android host ABI.
#[unsafe(no_mangle)]
pub(crate) unsafe extern "C" fn destack_host_android_midi_event_read(
    session_handle: u64,
    session_id: u64,
    max_events: u32,
    timeout_ns: u64,
    headers: NativeSlice<AndroidHostMidiEventHeader>,
    event_count_written: *mut u32,
    string_bytes: NativeSlice<u8>,
    string_bytes_written: *mut u32,
) -> u32 {
    if event_count_written.is_null() {
        return HOST_STATUS_INVALID_ARGUMENT;
    }

    if string_bytes_written.is_null() {
        return HOST_STATUS_INVALID_ARGUMENT;
    }

    call_android_midi_callback(
        session_handle,
        |callbacks| callbacks.event_read,
        |callback| unsafe {
            callback(
                session_handle,
                session_id,
                max_events,
                timeout_ns,
                headers,
                event_count_written,
                string_bytes,
                string_bytes_written,
            )
        },
    )
}

/// Forward the `event_close` midi request through the Android host ABI.
#[unsafe(no_mangle)]
pub(crate) unsafe extern "C" fn destack_host_android_midi_event_close(
    session_handle: u64,
    session_id: u64,
) -> u32 {
    call_android_midi_callback(
        session_handle,
        |callbacks| callbacks.event_close,
        |callback| unsafe { callback(session_handle, session_id) },
    )
}

/// Forward the `output_write` midi request through the Android host ABI.
#[unsafe(no_mangle)]
pub(crate) unsafe extern "C" fn destack_host_android_midi_output_write(
    session_handle: u64,
    session_id: u64,
    headers: NativeSlice<AndroidHostMidiOutputRecordHeader>,
    record_count: u32,
    blob_bytes: NativeSlice<u8>,
    records_written: *mut u32,
) -> u32 {
    if records_written.is_null() {
        return HOST_STATUS_INVALID_ARGUMENT;
    }

    call_android_midi_callback(
        session_handle,
        |callbacks| callbacks.output_write,
        |callback| unsafe {
            callback(
                session_handle,
                session_id,
                headers,
                record_count,
                blob_bytes,
                records_written,
            )
        },
    )
}
