// generated by generate-bindings: do not edit

use super::types::{
    AndroidHostMidiEventHeader, AndroidHostMidiInputRecordHeader, AndroidHostMidiOpenedPortHeader,
    AndroidHostMidiOutputRecordHeader, AndroidHostMidiPortDescriptorHeader,
};
use crate::host::android::abi::binding::invoke_android_binding_callback;
use crate::platform::abi::{NativeSlice, NativeStringRef};

/// The host callback for `describe_backend`.
pub(crate) type AndroidHostMidiDescribeBackendCallback = unsafe extern "C" fn(
    session_handle: u64,
    capability_flags: *mut u64,
    supported_data_formats: *mut u32,
    supported_protocols: *mut u32,
) -> u32;

/// The host callback for `input_port_list`.
pub(crate) type AndroidHostMidiInputPortListCallback = unsafe extern "C" fn(
    session_handle: u64,
    flags: u32,
    headers: NativeSlice<AndroidHostMidiPortDescriptorHeader>,
    header_count_written: *mut u32,
    string_bytes: NativeSlice<u8>,
    string_bytes_written: *mut u32,
) -> u32;

/// The host callback for `output_port_list`.
pub(crate) type AndroidHostMidiOutputPortListCallback = unsafe extern "C" fn(
    session_handle: u64,
    flags: u32,
    headers: NativeSlice<AndroidHostMidiPortDescriptorHeader>,
    header_count_written: *mut u32,
    string_bytes: NativeSlice<u8>,
    string_bytes_written: *mut u32,
) -> u32;

/// The host callback for `input_port_open`.
pub(crate) type AndroidHostMidiInputPortOpenCallback = unsafe extern "C" fn(
    session_handle: u64,
    id: NativeStringRef,
    data_format: u32,
    protocol: u32,
    queue_capacity: u32,
    opened_port: *mut AndroidHostMidiOpenedPortHeader,
    string_bytes: NativeSlice<u8>,
    string_bytes_written: *mut u32,
) -> u32;

/// The host callback for `output_port_open`.
pub(crate) type AndroidHostMidiOutputPortOpenCallback = unsafe extern "C" fn(
    session_handle: u64,
    id: NativeStringRef,
    data_format: u32,
    protocol: u32,
    opened_port: *mut AndroidHostMidiOpenedPortHeader,
    string_bytes: NativeSlice<u8>,
    string_bytes_written: *mut u32,
) -> u32;

/// The host callback for `input_virtual_create`.
pub(crate) type AndroidHostMidiInputVirtualCreateCallback = unsafe extern "C" fn(
    session_handle: u64,
    name: NativeStringRef,
    manufacturer: NativeStringRef,
    model: NativeStringRef,
    version: NativeStringRef,
    data_format: u32,
    protocol: u32,
    queue_capacity: u32,
    opened_port: *mut AndroidHostMidiOpenedPortHeader,
    string_bytes: NativeSlice<u8>,
    string_bytes_written: *mut u32,
) -> u32;

/// The host callback for `output_virtual_create`.
pub(crate) type AndroidHostMidiOutputVirtualCreateCallback = unsafe extern "C" fn(
    session_handle: u64,
    name: NativeStringRef,
    manufacturer: NativeStringRef,
    model: NativeStringRef,
    version: NativeStringRef,
    data_format: u32,
    protocol: u32,
    opened_port: *mut AndroidHostMidiOpenedPortHeader,
    string_bytes: NativeSlice<u8>,
    string_bytes_written: *mut u32,
) -> u32;

/// The host callback for `input_port_close`.
pub(crate) type AndroidHostMidiInputPortCloseCallback =
    unsafe extern "C" fn(session_handle: u64, session_id: u64) -> u32;

/// The host callback for `output_port_close`.
pub(crate) type AndroidHostMidiOutputPortCloseCallback =
    unsafe extern "C" fn(session_handle: u64, session_id: u64) -> u32;

/// The host callback for `input_read`.
pub(crate) type AndroidHostMidiInputReadCallback = unsafe extern "C" fn(
    session_handle: u64,
    session_id: u64,
    max_records: u32,
    timeout_ns: u64,
    headers: NativeSlice<AndroidHostMidiInputRecordHeader>,
    record_count_written: *mut u32,
    blob_bytes: NativeSlice<u8>,
    blob_bytes_written: *mut u32,
) -> u32;

/// The host callback for `event_open`.
pub(crate) type AndroidHostMidiEventOpenCallback = unsafe extern "C" fn(
    session_handle: u64,
    flags: u32,
    direction_mask: u32,
    session_id: *mut u64,
) -> u32;

/// The host callback for `event_read`.
pub(crate) type AndroidHostMidiEventReadCallback = unsafe extern "C" fn(
    session_handle: u64,
    session_id: u64,
    max_events: u32,
    timeout_ns: u64,
    headers: NativeSlice<AndroidHostMidiEventHeader>,
    event_count_written: *mut u32,
    string_bytes: NativeSlice<u8>,
    string_bytes_written: *mut u32,
) -> u32;

/// The host callback for `event_close`.
pub(crate) type AndroidHostMidiEventCloseCallback =
    unsafe extern "C" fn(session_handle: u64, session_id: u64) -> u32;

/// The host callback for `output_write`.
pub(crate) type AndroidHostMidiOutputWriteCallback = unsafe extern "C" fn(
    session_handle: u64,
    session_id: u64,
    headers: NativeSlice<AndroidHostMidiOutputRecordHeader>,
    record_count: u32,
    blob_bytes: NativeSlice<u8>,
    records_written: *mut u32,
) -> u32;

/// Callback table for Android host midi request interop.
#[derive(Clone, Copy, Debug, Default)]
#[repr(C)]
pub(crate) struct AndroidHostMidiCallbacks {
    /// The callback for `describe_backend`.
    pub describe_backend: Option<AndroidHostMidiDescribeBackendCallback>,
    /// The callback for `input_port_list`.
    pub input_port_list: Option<AndroidHostMidiInputPortListCallback>,
    /// The callback for `output_port_list`.
    pub output_port_list: Option<AndroidHostMidiOutputPortListCallback>,
    /// The callback for `input_port_open`.
    pub input_port_open: Option<AndroidHostMidiInputPortOpenCallback>,
    /// The callback for `output_port_open`.
    pub output_port_open: Option<AndroidHostMidiOutputPortOpenCallback>,
    /// The callback for `input_virtual_create`.
    pub input_virtual_create: Option<AndroidHostMidiInputVirtualCreateCallback>,
    /// The callback for `output_virtual_create`.
    pub output_virtual_create: Option<AndroidHostMidiOutputVirtualCreateCallback>,
    /// The callback for `input_port_close`.
    pub input_port_close: Option<AndroidHostMidiInputPortCloseCallback>,
    /// The callback for `output_port_close`.
    pub output_port_close: Option<AndroidHostMidiOutputPortCloseCallback>,
    /// The callback for `input_read`.
    pub input_read: Option<AndroidHostMidiInputReadCallback>,
    /// The callback for `event_open`.
    pub event_open: Option<AndroidHostMidiEventOpenCallback>,
    /// The callback for `event_read`.
    pub event_read: Option<AndroidHostMidiEventReadCallback>,
    /// The callback for `event_close`.
    pub event_close: Option<AndroidHostMidiEventCloseCallback>,
    /// The callback for `output_write`.
    pub output_write: Option<AndroidHostMidiOutputWriteCallback>,
}

/// Resolve and invoke one Android host midi callback.
pub(super) fn call_android_midi_callback<T: Copy>(
    session_handle: u64,
    resolve: impl FnOnce(&AndroidHostMidiCallbacks) -> Option<T>,
    invoke: impl FnOnce(T) -> u32,
) -> u32 {
    invoke_android_binding_callback(session_handle, |bindings| resolve(&bindings.midi), invoke)
}
