// generated by generate-bindings: do not edit

use super::callbacks::{call_android_notification_callback, AndroidHostNotificationCallbacks};
use crate::host::abi::notification::{HostNotificationRequest};
use crate::platform::abi::NativeStringRef;

/// Forward the `post` notification request through the Android host ABI.
#[unsafe(no_mangle)]
pub(crate) unsafe extern "C" fn destack_host_android_notification_post(
    session_handle: u64,
    request: HostNotificationRequest,
) -> u32 {
    call_android_notification_callback(
        session_handle,
        |callbacks| callbacks.post,
        |callback| unsafe { callback(session_handle, request) },
    )
}

/// Forward the `cancel` notification request through the Android host ABI.
#[unsafe(no_mangle)]
pub(crate) unsafe extern "C" fn destack_host_android_notification_cancel(
    session_handle: u64,
    identifier: NativeStringRef,
) -> u32 {
    call_android_notification_callback(
        session_handle,
        |callbacks| callbacks.cancel,
        |callback| unsafe { callback(session_handle, identifier) },
    )
}

/// Forward the `cancel_all` notification request through the Android host ABI.
#[unsafe(no_mangle)]
pub(crate) unsafe extern "C" fn destack_host_android_notification_cancel_all(
    session_handle: u64,
) -> u32 {
    call_android_notification_callback(
        session_handle,
        |callbacks| callbacks.cancel_all,
        |callback| unsafe { callback(session_handle) },
    )
}
