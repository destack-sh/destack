// generated by generate-bindings: do not edit

use crate::diagnostic::RuntimeResult;
use crate::host::os::android::ingress::core::android_host_queue;
use crate::host::{HostEvent, HostLocationEvent, HostSessionHandle};
use crate::platform::os::LocationSampleValue;

/// The runtime function that receives one location sample.
pub(crate) fn android_notify_location_sample(
    session_handle: HostSessionHandle,
    watch_id: &str,
    sample: LocationSampleValue,
) -> RuntimeResult<()> {
    let queue = android_host_queue(session_handle)?;

    queue.enqueue(HostEvent::Location(Box::new(HostLocationEvent {
        watch_id: watch_id.to_string(),

        sample,
    })));

    Ok(())
}
