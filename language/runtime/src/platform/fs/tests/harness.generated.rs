// generated by generate-bindings: test harness, do not edit

#![allow(dead_code)]
#![allow(unused_imports)]
#![allow(clippy::type_complexity)]

use crate::diagnostic::{RuntimeError, RuntimeResult};
use crate::platform::abi::{NativeSlice, NativeStringRef, NativeStringSlice};
use crate::platform::fs::tests::FsHarnessContext;
use crate::platform::fs::{
    AccessMode, AllocFlags, AtFlags, CopyFlags, Dirent, DirentKind, DirentNext, DirentNextEnd,
    DirentNextEndVm, DirentNextEntry, DirentNextEntryVm, DirentNextVm, DirentVm, FdFlags,
    FileAdvice, FileLockFlags, FileMode, FileOffset, FileSize, MmapAdvice, MmapFlags, MmapProt,
    MmapSyncFlags, NodeDevice, OpenFlags, OpenOptions, OpenOptionsVm, OpenResolveFlags, OsPath,
    OsPathBytes, OsPathBytesVm, OsPathUtf16, OsPathUtf16Vm, OsPathVm, PathBytes, PathBytesVm,
    PathUtf16, PathUtf16Vm, ReadWriteFlags, RenameFlags, SeekWhence, SpliceCursor, SpliceCursorVm,
    SpliceFlags, Stat, StatFs, StatFsFlags, StatFsVm, StatVm, StatusFlags, Statx, StatxFlags,
    StatxMask, StatxVm, SymlinkType, SyncFlags, WatchBatch, WatchBatchVm, WatchCreateEvent,
    WatchCreateEventVm, WatchEvent, WatchEventMetadata, WatchEventMetadataVm, WatchEventVm,
    WatchMask, WatchMetadataEvent, WatchMetadataEventVm, WatchModifyEvent, WatchModifyEventVm,
    WatchOptions, WatchOptionsVm, WatchOverflowEvent, WatchOverflowEventVm, WatchRemoveEvent,
    WatchRemoveEventVm, WatchRenameEvent, WatchRenameEventVm, XattrFlags, native as fs_native,
    vm as fs_vm,
};
use crate::platform::{
    NativeAbiCodec, NativeArray, PlatformError as HarnessPlatformError, VmAbiCodec, VmArray,
    VmSlice, fs, resource,
};
use destack_vm as vm;

impl<'call> FsHarnessContext<'call> {
    /// Return the generated VM context if available.
    #[allow(clippy::mut_from_ref)]
    fn generated_vm_context_mut(&self) -> Option<&mut vm::ExternalCallContext<'_>> {
        self.vm_context
            .map(|context| unsafe { &mut *(context as *mut vm::ExternalCallContext<'_>) })
    }

    /// Return one standardized value payload for native and VM variants.
    pub(crate) fn harness_value<Native, Vm>(&self, native: Native) -> HarnessValue<Native, Vm> {
        HarnessValue::Native(native)
    }

    /// Return one standardized value payload for VM and native variants.
    pub(crate) fn harness_value_vm<Native, Vm>(&self, vm: Vm) -> HarnessValue<Native, Vm> {
        HarnessValue::Vm(vm)
    }

    /// Encode one materialized value into one native or VM harness payload.
    pub(crate) fn harness_value_from<Native, Vm>(
        &mut self,
        value: Native::Value,
    ) -> RuntimeResult<HarnessValue<Native, Vm>>
    where
        Native: NativeAbiCodec,
        Vm: VmAbiCodec<Value = Native::Value>,
    {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let context = &mut context.write();
                let value = Vm::from_value(context, value)?;
                Ok(HarnessValue::Vm(value))
            }
            None => {
                let value = Native::from_value(self.call_context, value);
                Ok(HarnessValue::Native(value))
            }
        }
    }

    /// Decode one native or VM harness payload into one materialized value.
    pub(crate) fn harness_value_into<Native, Vm>(
        &mut self,
        value: HarnessValue<Native, Vm>,
    ) -> RuntimeResult<Native::Value>
    where
        Native: NativeAbiCodec,
        Vm: VmAbiCodec<Value = Native::Value>,
    {
        match value {
            HarnessValue::Native(value) => unsafe { Native::into_value(value) },
            HarnessValue::Vm(value) => {
                let context = self.generated_vm_context_mut().ok_or_else(|| {
                    RuntimeError::from(HarnessPlatformError::invalid_argument_value(
                        "context",
                        "expected vm context",
                    ))
                    .boxed()
                })?;
                let context = &context.read();
                value.into_value(context)
            }
        }
    }

    /// Check file access permissions.
    ///
    /// Check file access permissions via host kernel APIs.
    /// Return values and failures map directly to host contracts so higher layers can apply policy explicitly.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the kernel feature is unavailable.
    /// Uses access(2) on Unix and GetFileAttributesW plus ACL checks on Windows.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `fs.metadata`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_fs_access(
        &mut self,
        path: HarnessValue<OsPath, OsPathVm>,
        mode: AccessMode,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let path = path.into_vm("path")?;
                fs_vm::destack_fs_access(self.call_context, context, path, mode)
            }
            None => unsafe {
                let path = path.into_native("path")?;
                fs_native::destack_fs_access(self.call_context, path, mode)
            },
        }
    }

    /// Check file access permissions relative to a directory handle.
    ///
    /// Check file access permissions relative to a directory handle via host kernel APIs.
    /// Paths are forwarded from `OsPath` without runtime normalization or canonicalization, and permission checks follow host filesystem rules.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the kernel feature is unavailable.
    /// Uses faccessat(2) on Unix and relative path checks via native handles on Windows.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `fs.metadata`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_fs_accessat(
        &mut self,
        dir: resource::DirectoryHandle,
        path: HarnessValue<OsPath, OsPathVm>,
        mode: AccessMode,
        flags: AtFlags,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let path = path.into_vm("path")?;
                fs_vm::destack_fs_accessat(self.call_context, context, dir, path, mode, flags)
            }
            None => unsafe {
                let path = path.into_native("path")?;
                fs_native::destack_fs_accessat(self.call_context, dir, path, mode, flags)
            },
        }
    }

    /// Change file permissions.
    ///
    /// Change file permissions via host kernel APIs.
    /// Return values and failures map directly to host contracts so higher layers can apply policy explicitly.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the kernel feature is unavailable.
    /// Uses chmod(2) on Unix and file attribute/security updates on Windows.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `fs.chmod`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_fs_chmod(
        &mut self,
        path: HarnessValue<OsPath, OsPathVm>,
        mode: FileMode,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let path = path.into_vm("path")?;
                fs_vm::destack_fs_chmod(self.call_context, context, path, mode)
            }
            None => unsafe {
                let path = path.into_native("path")?;
                fs_native::destack_fs_chmod(self.call_context, path, mode)
            },
        }
    }

    /// Change file owner and group.
    ///
    /// Change file owner and group via host kernel APIs.
    /// Return values and failures map directly to host contracts so higher layers can apply policy explicitly.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the kernel feature is unavailable.
    /// Uses chown(2) on Unix and token/owner updates where supported on Windows.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `fs.chown`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_fs_chown(
        &mut self,
        path: HarnessValue<OsPath, OsPathVm>,
        uid: u32,
        gid: u32,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let path = path.into_vm("path")?;
                fs_vm::destack_fs_chown(self.call_context, context, path, uid, gid)
            }
            None => unsafe {
                let path = path.into_native("path")?;
                fs_native::destack_fs_chown(self.call_context, path, uid, gid)
            },
        }
    }

    /// Change file permissions by handle.
    ///
    /// Change file permissions by handle via host kernel APIs.
    /// Return values and failures map directly to host contracts so higher layers can apply policy explicitly.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the kernel feature is unavailable.
    /// Uses fchmod(2) on Unix and handle-based mode updates on Windows.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `fs.chmod`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_fs_fchmod(
        &mut self,
        handle: resource::FileHandle,
        mode: FileMode,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => fs_vm::destack_fs_fchmod(self.call_context, context, handle, mode),
            None => unsafe { fs_native::destack_fs_fchmod(self.call_context, handle, mode) },
        }
    }

    /// Change file permissions relative to a directory handle.
    ///
    /// Change file permissions relative to a directory handle via host kernel APIs.
    /// Paths are forwarded from `OsPath` without runtime normalization or canonicalization, and permission checks follow host filesystem rules.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the kernel feature is unavailable.
    /// Uses fchmodat(2) on Unix and handle-relative mode updates on Windows.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `fs.chmod`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_fs_fchmodat(
        &mut self,
        dir: resource::DirectoryHandle,
        path: HarnessValue<OsPath, OsPathVm>,
        mode: FileMode,
        flags: AtFlags,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let path = path.into_vm("path")?;
                fs_vm::destack_fs_fchmodat(self.call_context, context, dir, path, mode, flags)
            }
            None => unsafe {
                let path = path.into_native("path")?;
                fs_native::destack_fs_fchmodat(self.call_context, dir, path, mode, flags)
            },
        }
    }

    /// Change file owner and group by handle.
    ///
    /// Change file owner and group by handle via host kernel APIs.
    /// Return values and failures map directly to host contracts so higher layers can apply policy explicitly.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the kernel feature is unavailable.
    /// Uses fchown(2) on Unix and handle owner updates where supported on Windows.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `fs.chown`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_fs_fchown(
        &mut self,
        handle: resource::FileHandle,
        uid: u32,
        gid: u32,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => fs_vm::destack_fs_fchown(self.call_context, context, handle, uid, gid),
            None => unsafe { fs_native::destack_fs_fchown(self.call_context, handle, uid, gid) },
        }
    }

    /// Change file owner and group relative to a directory handle.
    ///
    /// Change file owner and group relative to a directory handle via host kernel APIs.
    /// Paths are forwarded from `OsPath` without runtime normalization or canonicalization, and permission checks follow host filesystem rules.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the kernel feature is unavailable.
    /// Uses fchownat(2) on Unix and handle-relative owner updates where supported on Windows.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `fs.chown`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_fs_fchownat(
        &mut self,
        dir: resource::DirectoryHandle,
        path: HarnessValue<OsPath, OsPathVm>,
        uid: u32,
        gid: u32,
        flags: AtFlags,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let path = path.into_vm("path")?;
                fs_vm::destack_fs_fchownat(self.call_context, context, dir, path, uid, gid, flags)
            }
            None => unsafe {
                let path = path.into_native("path")?;
                fs_native::destack_fs_fchownat(self.call_context, dir, path, uid, gid, flags)
            },
        }
    }

    /// Update access and modification times by handle.
    ///
    /// Update access and modification times by handle via host kernel APIs.
    /// Return values and failures map directly to host contracts so higher layers can apply policy explicitly.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the kernel feature is unavailable.
    /// Uses futimens/futimes on Unix and SetFileTime on Windows.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `fs.metadata`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_fs_futimes(
        &mut self,
        handle: resource::FileHandle,
        atimens: u64,
        mtimens: u64,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                fs_vm::destack_fs_futimes(self.call_context, context, handle, atimens, mtimens)
            }
            None => unsafe {
                fs_native::destack_fs_futimes(self.call_context, handle, atimens, mtimens)
            },
        }
    }

    /// Update access and modification times without following symlinks.
    ///
    /// Update access and modification times without following symlinks via host kernel APIs.
    /// Paths are forwarded from `OsPath` without runtime normalization or canonicalization, and permission checks follow host filesystem rules.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the kernel feature is unavailable.
    /// Uses lutimes/utimensat with nofollow on Unix and reparse-point time updates on Windows.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `fs.metadata`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_fs_lutimes(
        &mut self,
        path: HarnessValue<OsPath, OsPathVm>,
        atimens: u64,
        mtimens: u64,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let path = path.into_vm("path")?;
                fs_vm::destack_fs_lutimes(self.call_context, context, path, atimens, mtimens)
            }
            None => unsafe {
                let path = path.into_native("path")?;
                fs_native::destack_fs_lutimes(self.call_context, path, atimens, mtimens)
            },
        }
    }

    /// Update access and modification times relative to a directory handle.
    ///
    /// Update access and modification times relative to a directory handle via host kernel APIs.
    /// Paths are forwarded from `OsPath` without runtime normalization or canonicalization, and permission checks follow host filesystem rules.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the kernel feature is unavailable.
    /// Uses utimensat(2) on Unix and handle-relative SetFileTime on Windows.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `fs.metadata`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_fs_utimensat(
        &mut self,
        dir: resource::DirectoryHandle,
        path: HarnessValue<OsPath, OsPathVm>,
        atimens: u64,
        mtimens: u64,
        flags: AtFlags,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let path = path.into_vm("path")?;
                fs_vm::destack_fs_utimensat(
                    self.call_context,
                    context,
                    dir,
                    path,
                    atimens,
                    mtimens,
                    flags,
                )
            }
            None => unsafe {
                let path = path.into_native("path")?;
                fs_native::destack_fs_utimensat(
                    self.call_context,
                    dir,
                    path,
                    atimens,
                    mtimens,
                    flags,
                )
            },
        }
    }

    /// Update access and modification times.
    ///
    /// Update access and modification times via host kernel APIs.
    /// Return values and failures map directly to host contracts so higher layers can apply policy explicitly.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the kernel feature is unavailable.
    /// Uses utimensat/utimes on Unix and SetFileTime on Windows.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `fs.metadata`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_fs_utimes(
        &mut self,
        path: HarnessValue<OsPath, OsPathVm>,
        atimens: u64,
        mtimens: u64,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let path = path.into_vm("path")?;
                fs_vm::destack_fs_utimes(self.call_context, context, path, atimens, mtimens)
            }
            None => unsafe {
                let path = path.into_native("path")?;
                fs_native::destack_fs_utimes(self.call_context, path, atimens, mtimens)
            },
        }
    }

    /// Close a directory handle.
    ///
    /// Close the target handle by forwarding the descriptor teardown to the host kernel.
    /// Paths are forwarded from `OsPath` without runtime normalization or canonicalization, and permission checks follow host filesystem rules.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the kernel feature is unavailable.
    /// Uses closedir(3) on Unix and FindClose/CloseHandle on Windows.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `fs.handle`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_fs_closedir(
        &mut self,
        handle: resource::DirectoryHandle,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => fs_vm::destack_fs_closedir(self.call_context, context, handle),
            None => unsafe { fs_native::destack_fs_closedir(self.call_context, handle) },
        }
    }

    /// Resolve the directory descriptor for an open directory handle.
    ///
    /// Extract the underlying file descriptor or handle value from an open directory stream.
    /// The returned handle is valid only while the source directory handle remains open.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the kernel feature is unavailable.
    /// Uses dirfd(3) on Unix and directory handle extraction on Windows.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `fs.handle`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_fs_dirfd(
        &mut self,
        handle: resource::DirectoryHandle,
    ) -> RuntimeResult<resource::FileHandle> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = fs_vm::destack_fs_dirfd(self.call_context, context, handle)?;
                Ok(out)
            }
            None => {
                let mut out = std::mem::MaybeUninit::<resource::FileHandle>::uninit();
                unsafe {
                    fs_native::destack_fs_dirfd(self.call_context, out.as_mut_ptr(), handle)?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// Create a directory.
    ///
    /// Create a single directory entry at the provided path with the supplied mode bits.
    /// Paths are forwarded from `OsPath` without runtime normalization or canonicalization, and permission checks follow host filesystem rules.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the kernel feature is unavailable.
    /// Uses mkdir(2) on Unix and CreateDirectoryW on Windows.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `fs.write`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_fs_mkdir(
        &mut self,
        path: HarnessValue<OsPath, OsPathVm>,
        mode: FileMode,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let path = path.into_vm("path")?;
                fs_vm::destack_fs_mkdir(self.call_context, context, path, mode)
            }
            None => unsafe {
                let path = path.into_native("path")?;
                fs_native::destack_fs_mkdir(self.call_context, path, mode)
            },
        }
    }

    /// Create a directory relative to a directory handle.
    ///
    /// Create a single directory entry relative to an existing directory descriptor.
    /// Paths are forwarded from `OsPath` without runtime normalization or canonicalization, and permission checks follow host filesystem rules.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the kernel feature is unavailable.
    /// Uses mkdirat(2) on Unix and handle-relative directory create on Windows.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `fs.write`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_fs_mkdirat(
        &mut self,
        dir: resource::DirectoryHandle,
        path: HarnessValue<OsPath, OsPathVm>,
        mode: FileMode,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let path = path.into_vm("path")?;
                fs_vm::destack_fs_mkdirat(self.call_context, context, dir, path, mode)
            }
            None => unsafe {
                let path = path.into_native("path")?;
                fs_native::destack_fs_mkdirat(self.call_context, dir, path, mode)
            },
        }
    }

    /// Create a temporary directory.
    ///
    /// Create a unique temporary directory by replacing the trailing `XXXXXX` suffix in `template`.
    /// The resulting directory is created at the caller-supplied path, not in an implicit host temp root.
    /// Paths are forwarded from `OsPath` without runtime normalization or canonicalization, and permission checks follow host filesystem rules.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the kernel feature is unavailable.
    /// Uses mkdtemp(3) on Unix and a CreateDirectoryW-based template loop on Windows.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `fs.temp`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_fs_mkdtemp(
        &mut self,
        template: HarnessValue<OsPath, OsPathVm>,
    ) -> RuntimeResult<HarnessValue<OsPath, OsPathVm>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let template = template.into_vm("template")?;
                let out = fs_vm::destack_fs_mkdtemp(self.call_context, context, template)?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let template = template.into_native("template")?;
                let mut out = std::mem::MaybeUninit::<OsPath>::uninit();
                unsafe {
                    fs_native::destack_fs_mkdtemp(self.call_context, out.as_mut_ptr(), template)?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Open a directory and return a handle.
    ///
    /// Open the target resource with the requested flags and return the host handle exposed by the kernel.
    /// Paths are forwarded from `OsPath` without runtime normalization or canonicalization, and permission checks follow host filesystem rules.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the kernel feature is unavailable.
    /// Uses opendir/readdir on Unix and FindFirstFileW directory enumeration on Windows.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `fs.read`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_fs_opendir(
        &mut self,
        path: HarnessValue<OsPath, OsPathVm>,
    ) -> RuntimeResult<resource::DirectoryHandle> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let path = path.into_vm("path")?;
                let out = fs_vm::destack_fs_opendir(self.call_context, context, path)?;
                Ok(out)
            }
            None => {
                let path = path.into_native("path")?;
                let mut out = std::mem::MaybeUninit::<resource::DirectoryHandle>::uninit();
                unsafe {
                    fs_native::destack_fs_opendir(self.call_context, out.as_mut_ptr(), path)?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// Read directory entries from an open directory handle.
    ///
    /// Read the full directory stream from the current cursor until the host reports end-of-directory.
    /// Entry ordering and type classification follow host directory iteration semantics.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the kernel feature is unavailable.
    /// Uses readdir(3) loop on Unix and FindNextFileW loop on Windows.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `fs.read`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_fs_readdir(
        &mut self,
        handle: resource::DirectoryHandle,
    ) -> RuntimeResult<HarnessValue<NativeArray<Dirent>, VmArray<DirentVm>>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = fs_vm::destack_fs_readdir(self.call_context, context, handle)?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let mut out = std::mem::MaybeUninit::<NativeArray<Dirent>>::uninit();
                unsafe {
                    fs_native::destack_fs_readdir(self.call_context, out.as_mut_ptr(), handle)?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Read a single directory entry from an open directory handle.
    ///
    /// Read at most one entry from the current directory cursor and advance the host iterator.
    /// Callers can iterate deterministically by repeatedly invoking this operation until `kind` is `"end"`.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the kernel feature is unavailable.
    /// Uses readdir(3) step on Unix and FindNextFileW step on Windows.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `fs.read`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_fs_readdir_next(
        &mut self,
        handle: resource::DirectoryHandle,
    ) -> RuntimeResult<HarnessValue<DirentNext, DirentNextVm>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = fs_vm::destack_fs_readdir_next(self.call_context, context, handle)?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let mut out = std::mem::MaybeUninit::<DirentNext>::uninit();
                unsafe {
                    fs_native::destack_fs_readdir_next(
                        self.call_context,
                        out.as_mut_ptr(),
                        handle,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Reset an open directory handle to the first entry.
    ///
    /// Reset the directory iteration cursor to the beginning of the stream.
    /// Paths are forwarded from `OsPath` without runtime normalization or canonicalization, and permission checks follow host filesystem rules.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the kernel feature is unavailable.
    /// Uses rewinddir(3) on Unix and enumeration reset on Windows.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `fs.read`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_fs_rewinddir(
        &mut self,
        handle: resource::DirectoryHandle,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => fs_vm::destack_fs_rewinddir(self.call_context, context, handle),
            None => unsafe { fs_native::destack_fs_rewinddir(self.call_context, handle) },
        }
    }

    /// Remove a directory.
    ///
    /// Remove the target resource through a single host namespace operation with no runtime fallback path.
    /// Paths are forwarded from `OsPath` without runtime normalization or canonicalization, and permission checks follow host filesystem rules.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the kernel feature is unavailable.
    /// Uses rmdir(2) on Unix and RemoveDirectoryW on Windows.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `fs.write`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_fs_rmdir(
        &mut self,
        path: HarnessValue<OsPath, OsPathVm>,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let path = path.into_vm("path")?;
                fs_vm::destack_fs_rmdir(self.call_context, context, path)
            }
            None => unsafe {
                let path = path.into_native("path")?;
                fs_native::destack_fs_rmdir(self.call_context, path)
            },
        }
    }

    /// Close an open file handle.
    ///
    /// Close the target handle by forwarding the descriptor teardown to the host kernel.
    /// The descriptor becomes invalid immediately for subsequent read or write operations.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the kernel feature is unavailable.
    /// Uses close(2) on Unix and CloseHandle on Windows.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `fs.handle`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_fs_close(&mut self, handle: resource::FileHandle) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => fs_vm::destack_fs_close(self.call_context, context, handle),
            None => unsafe { fs_native::destack_fs_close(self.call_context, handle) },
        }
    }

    /// Copy a range between file descriptors.
    ///
    /// Copy bytes from one file descriptor range into another descriptor range.
    /// Source and destination offsets are applied exactly as provided to the host operation.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the kernel feature is unavailable.
    /// Uses copy_file_range(2) on linux and runtime copy fallback on other targets.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `fs.read`, `fs.write`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_fs_copy_file_range(
        &mut self,
        src: resource::FileHandle,
        srcoffset: FileOffset,
        dst: resource::FileHandle,
        dstoffset: FileOffset,
        length: FileSize,
    ) -> RuntimeResult<u64> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = fs_vm::destack_fs_copy_file_range(
                    self.call_context,
                    context,
                    src,
                    srcoffset,
                    dst,
                    dstoffset,
                    length,
                )?;
                Ok(out)
            }
            None => {
                let mut out = std::mem::MaybeUninit::<u64>::uninit();
                unsafe {
                    fs_native::destack_fs_copy_file_range(
                        self.call_context,
                        out.as_mut_ptr(),
                        src,
                        srcoffset,
                        dst,
                        dstoffset,
                        length,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// Duplicate a file handle.
    ///
    /// Duplicate one descriptor and return a new descriptor that references the same open file description.
    /// Both descriptors share file-offset and status-flag state per host dup semantics.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the kernel feature is unavailable.
    /// Uses dup(2) on Unix and DuplicateHandle on Windows.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `fs.handle`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_fs_dup(
        &mut self,
        handle: resource::FileHandle,
    ) -> RuntimeResult<resource::FileHandle> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = fs_vm::destack_fs_dup(self.call_context, context, handle)?;
                Ok(out)
            }
            None => {
                let mut out = std::mem::MaybeUninit::<resource::FileHandle>::uninit();
                unsafe {
                    fs_native::destack_fs_dup(self.call_context, out.as_mut_ptr(), handle)?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// Duplicate a file handle to a specific target.
    ///
    /// Duplicate one descriptor onto a caller-provided target descriptor number.
    /// Existing target descriptor state is replaced according to host dup2 semantics.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the kernel feature is unavailable.
    /// Uses dup2(2) on Unix and DuplicateHandle target replacement on Windows.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `fs.handle`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_fs_dup2(
        &mut self,
        handle: resource::FileHandle,
        target: resource::FileHandle,
    ) -> RuntimeResult<resource::FileHandle> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = fs_vm::destack_fs_dup2(self.call_context, context, handle, target)?;
                Ok(out)
            }
            None => {
                let mut out = std::mem::MaybeUninit::<resource::FileHandle>::uninit();
                unsafe {
                    fs_native::destack_fs_dup2(
                        self.call_context,
                        out.as_mut_ptr(),
                        handle,
                        target,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// Duplicate a file handle to a specific target with flags.
    ///
    /// Duplicate one descriptor onto a target descriptor while applying explicit duplication flags.
    /// Flag support and close-on-exec semantics follow host dup3 behavior.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the kernel feature is unavailable.
    /// Uses dup3(2) on linux and runtime emulation on other targets.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `fs.handle`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_fs_dup3(
        &mut self,
        handle: resource::FileHandle,
        target: resource::FileHandle,
        flags: OpenFlags,
    ) -> RuntimeResult<resource::FileHandle> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out =
                    fs_vm::destack_fs_dup3(self.call_context, context, handle, target, flags)?;
                Ok(out)
            }
            None => {
                let mut out = std::mem::MaybeUninit::<resource::FileHandle>::uninit();
                unsafe {
                    fs_native::destack_fs_dup3(
                        self.call_context,
                        out.as_mut_ptr(),
                        handle,
                        target,
                        flags,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// Advise the kernel about access patterns.
    ///
    /// Provide expected access pattern hints for one descriptor range.
    /// Advice is best effort and does not change correctness or visibility semantics.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the kernel feature is unavailable.
    /// Uses posix_fadvise(2) on Unix and notSupported on Windows.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `fs.metadata`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_fs_fadvise(
        &mut self,
        handle: resource::FileHandle,
        offset: FileOffset,
        length: FileSize,
        advice: FileAdvice,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => fs_vm::destack_fs_fadvise(
                self.call_context,
                context,
                handle,
                offset,
                length,
                advice,
            ),
            None => unsafe {
                fs_native::destack_fs_fadvise(self.call_context, handle, offset, length, advice)
            },
        }
    }

    /// Allocate or punch file space.
    ///
    /// Reserve, deallocate, or punch one byte range using host allocation controls.
    /// Flag combinations define keep-size and hole-punch behavior where supported.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the kernel feature is unavailable.
    /// Uses fallocate(2) or posix_fallocate on Unix and allocation/truncate APIs on Windows.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `fs.write`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_fs_fallocate(
        &mut self,
        handle: resource::FileHandle,
        offset: FileOffset,
        length: FileSize,
        flags: AllocFlags,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => fs_vm::destack_fs_fallocate(
                self.call_context,
                context,
                handle,
                offset,
                length,
                flags,
            ),
            None => unsafe {
                fs_native::destack_fs_fallocate(self.call_context, handle, offset, length, flags)
            },
        }
    }

    /// Synchronize file data only.
    ///
    /// Flush file data pages for the target descriptor without requiring full metadata durability.
    /// Metadata needed for data reachability may still be persisted per host kernel rules.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the kernel feature is unavailable.
    /// Uses fdatasync(2) on Unix and FlushFileBuffers on Windows.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `fs.sync`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_fs_fdatasync(
        &mut self,
        handle: resource::FileHandle,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => fs_vm::destack_fs_fdatasync(self.call_context, context, handle),
            None => unsafe { fs_native::destack_fs_fdatasync(self.call_context, handle) },
        }
    }

    /// Synchronize a file's in-core state with storage.
    ///
    /// Synchronize buffered file state to storage for the target file descriptor.
    /// Completion guarantees and writeback scope follow host kernel fsync semantics.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the kernel feature is unavailable.
    /// Uses fsync(2) on Unix and FlushFileBuffers on Windows.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `fs.sync`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_fs_fsync(&mut self, handle: resource::FileHandle) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => fs_vm::destack_fs_fsync(self.call_context, context, handle),
            None => unsafe { fs_native::destack_fs_fsync(self.call_context, handle) },
        }
    }

    /// Truncate a file by handle.
    ///
    /// Truncate the target file to the requested size using host file-size control APIs.
    /// Growth behavior for sparse expansion and zero-fill follows host filesystem policy.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the kernel feature is unavailable.
    /// Uses ftruncate(2) on Unix and SetEndOfFile on Windows.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `fs.write`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_fs_ftruncate(
        &mut self,
        handle: resource::FileHandle,
        size: FileOffset,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => fs_vm::destack_fs_ftruncate(self.call_context, context, handle, size),
            None => unsafe { fs_native::destack_fs_ftruncate(self.call_context, handle, size) },
        }
    }

    /// Read file descriptor flags.
    ///
    /// Read descriptor flags such as close-on-exec from the target file descriptor.
    /// Returned bits reflect current host descriptor state at call time.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the kernel feature is unavailable.
    /// Uses fcntl(F_GETFD) on Unix and runtime handle metadata on Windows.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `fs.handle`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_fs_get_fd_flags(
        &mut self,
        handle: resource::FileHandle,
    ) -> RuntimeResult<FdFlags> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = fs_vm::destack_fs_get_fd_flags(self.call_context, context, handle)?;
                Ok(out)
            }
            None => {
                let mut out = std::mem::MaybeUninit::<FdFlags>::uninit();
                unsafe {
                    fs_native::destack_fs_get_fd_flags(
                        self.call_context,
                        out.as_mut_ptr(),
                        handle,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// Read file status flags.
    ///
    /// Read status flags such as append and nonblocking from the target file descriptor.
    /// Returned bits reflect current host descriptor state at call time.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the kernel feature is unavailable.
    /// Uses fcntl(F_GETFL) on Unix and runtime handle metadata on Windows.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `fs.handle`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_fs_get_status_flags(
        &mut self,
        handle: resource::FileHandle,
    ) -> RuntimeResult<StatusFlags> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = fs_vm::destack_fs_get_status_flags(self.call_context, context, handle)?;
                Ok(out)
            }
            None => {
                let mut out = std::mem::MaybeUninit::<StatusFlags>::uninit();
                unsafe {
                    fs_native::destack_fs_get_status_flags(
                        self.call_context,
                        out.as_mut_ptr(),
                        handle,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// Apply file locks to a file handle.
    ///
    /// Apply, release, or test advisory locking state for one file descriptor.
    /// Lock scope and conflict behavior follow host flock/fcntl locking semantics.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the kernel feature is unavailable.
    /// Uses flock/fcntl on Unix and LockFileEx/UnlockFileEx on Windows.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `fs.lock`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_fs_lock(
        &mut self,
        handle: resource::FileHandle,
        flags: FileLockFlags,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => fs_vm::destack_fs_lock(self.call_context, context, handle, flags),
            None => unsafe { fs_native::destack_fs_lock(self.call_context, handle, flags) },
        }
    }

    /// Open a file and return a handle.
    ///
    /// Open one filesystem entry by path and return a host-backed file handle.
    /// Flag interpretation, creation behavior, and inheritance defaults follow host open semantics.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the kernel feature is unavailable.
    /// Uses open(2) on Unix and CreateFileW on Windows.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `fs.read`, `fs.write`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_fs_open(
        &mut self,
        path: HarnessValue<OsPath, OsPathVm>,
        flags: OpenFlags,
        mode: FileMode,
    ) -> RuntimeResult<resource::FileHandle> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let path = path.into_vm("path")?;
                let out = fs_vm::destack_fs_open(self.call_context, context, path, flags, mode)?;
                Ok(out)
            }
            None => {
                let path = path.into_native("path")?;
                let mut out = std::mem::MaybeUninit::<resource::FileHandle>::uninit();
                unsafe {
                    fs_native::destack_fs_open(
                        self.call_context,
                        out.as_mut_ptr(),
                        path,
                        flags,
                        mode,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// Open a file relative to a directory handle.
    ///
    /// Open one filesystem entry resolved relative to an explicit directory handle.
    /// This avoids ambient current-working-directory resolution and keeps caller-controlled base directory scope.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the kernel feature is unavailable.
    /// Uses openat(2) on Unix and NtCreateFile relative opens on Windows.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `fs.read`, `fs.write`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_fs_openat(
        &mut self,
        dir: resource::DirectoryHandle,
        path: HarnessValue<OsPath, OsPathVm>,
        flags: OpenFlags,
        mode: FileMode,
    ) -> RuntimeResult<resource::FileHandle> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let path = path.into_vm("path")?;
                let out =
                    fs_vm::destack_fs_openat(self.call_context, context, dir, path, flags, mode)?;
                Ok(out)
            }
            None => {
                let path = path.into_native("path")?;
                let mut out = std::mem::MaybeUninit::<resource::FileHandle>::uninit();
                unsafe {
                    fs_native::destack_fs_openat(
                        self.call_context,
                        out.as_mut_ptr(),
                        dir,
                        path,
                        flags,
                        mode,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// Open a file relative to a directory handle with openat2 semantics.
    ///
    /// Open one filesystem entry relative to an explicit directory handle with resolve policy flags.
    /// Resolve behavior is passed through to supported hosts and rejected when the host backend cannot honor requested guarantees.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the kernel feature is unavailable.
    /// Uses openat2(2) on Linux and Android, falls back to openat semantics on other Unix targets when resolve flags are empty, and maps to openat semantics on Windows with resolve flags rejected.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `fs.read`, `fs.write`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_fs_openat2(
        &mut self,
        dir: resource::DirectoryHandle,
        path: HarnessValue<OsPath, OsPathVm>,
        how: HarnessValue<OpenOptions, OpenOptionsVm>,
    ) -> RuntimeResult<resource::FileHandle> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let path = path.into_vm("path")?;
                let how = how.into_vm("how")?;
                let out = fs_vm::destack_fs_openat2(self.call_context, context, dir, path, how)?;
                Ok(out)
            }
            None => {
                let path = path.into_native("path")?;
                let how = how.into_native("how")?;
                let mut out = std::mem::MaybeUninit::<resource::FileHandle>::uninit();
                unsafe {
                    fs_native::destack_fs_openat2(
                        self.call_context,
                        out.as_mut_ptr(),
                        dir,
                        path,
                        how,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// Read from a file at the given file offset.
    ///
    /// Read bytes into one contiguous caller-provided buffer at an explicit file offset.
    /// The descriptor's current file position is not changed by positioned reads.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the kernel feature is unavailable.
    /// Uses pread(2) on Unix and positioned ReadFile on Windows.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `fs.read`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_fs_pread(
        &mut self,
        handle: resource::FileHandle,
        buffer: HarnessValue<NativeSlice<u8>, VmSlice<u8>>,
        offset: FileOffset,
    ) -> RuntimeResult<u64> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let buffer = buffer.into_vm("buffer")?;
                let out =
                    fs_vm::destack_fs_pread(self.call_context, context, handle, buffer, offset)?;
                Ok(out)
            }
            None => {
                let buffer = buffer.into_native("buffer")?;
                let mut out = std::mem::MaybeUninit::<u64>::uninit();
                unsafe {
                    fs_native::destack_fs_pread(
                        self.call_context,
                        out.as_mut_ptr(),
                        handle,
                        buffer,
                        offset,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// Read into multiple buffers at the given file offset.
    ///
    /// Read bytes into a scatter buffer list at an explicit file offset.
    /// The descriptor's current file position is not changed by positioned vectored reads.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the kernel feature is unavailable.
    /// Uses preadv(2) on Unix and vectored positioned file I/O loop on Windows.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `fs.read`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_fs_preadv(
        &mut self,
        handle: resource::FileHandle,
        buffers: HarnessValue<NativeSlice<NativeSlice<u8>>, VmSlice<VmSlice<u8>>>,
        offset: FileOffset,
    ) -> RuntimeResult<u64> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let buffers = buffers.into_vm("buffers")?;
                let out =
                    fs_vm::destack_fs_preadv(self.call_context, context, handle, buffers, offset)?;
                Ok(out)
            }
            None => {
                let buffers = buffers.into_native("buffers")?;
                let mut out = std::mem::MaybeUninit::<u64>::uninit();
                unsafe {
                    fs_native::destack_fs_preadv(
                        self.call_context,
                        out.as_mut_ptr(),
                        handle,
                        buffers,
                        offset,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// Read into multiple buffers at the given file offset with explicit read flags.
    ///
    /// Read bytes into a scatter buffer list at an explicit file offset and apply host read flags.
    /// Flag bits are passed through directly and may enable nowait or high-priority reads on supported kernels.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the kernel feature is unavailable.
    /// Uses preadv2(2) on Linux and runtime fallback to preadv on other targets when flags are zero.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `fs.read`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_fs_preadv2(
        &mut self,
        handle: resource::FileHandle,
        buffers: HarnessValue<NativeSlice<NativeSlice<u8>>, VmSlice<VmSlice<u8>>>,
        offset: FileOffset,
        flags: ReadWriteFlags,
    ) -> RuntimeResult<u64> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let buffers = buffers.into_vm("buffers")?;
                let out = fs_vm::destack_fs_preadv2(
                    self.call_context,
                    context,
                    handle,
                    buffers,
                    offset,
                    flags,
                )?;
                Ok(out)
            }
            None => {
                let buffers = buffers.into_native("buffers")?;
                let mut out = std::mem::MaybeUninit::<u64>::uninit();
                unsafe {
                    fs_native::destack_fs_preadv2(
                        self.call_context,
                        out.as_mut_ptr(),
                        handle,
                        buffers,
                        offset,
                        flags,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// Write to a file at the given file offset.
    ///
    /// Write bytes from one contiguous caller-provided buffer at an explicit file offset.
    /// The descriptor's current file position is not changed by positioned writes.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the kernel feature is unavailable.
    /// Uses pwrite(2) on Unix and positioned WriteFile on Windows.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `fs.write`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_fs_pwrite(
        &mut self,
        handle: resource::FileHandle,
        buffer: HarnessValue<NativeSlice<u8>, VmSlice<u8>>,
        offset: FileOffset,
    ) -> RuntimeResult<u64> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let buffer = buffer.into_vm("buffer")?;
                let out =
                    fs_vm::destack_fs_pwrite(self.call_context, context, handle, buffer, offset)?;
                Ok(out)
            }
            None => {
                let buffer = buffer.into_native("buffer")?;
                let mut out = std::mem::MaybeUninit::<u64>::uninit();
                unsafe {
                    fs_native::destack_fs_pwrite(
                        self.call_context,
                        out.as_mut_ptr(),
                        handle,
                        buffer,
                        offset,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// Write from multiple buffers at the given file offset.
    ///
    /// Write bytes from a gather buffer list at an explicit file offset.
    /// The descriptor's current file position is not changed by positioned vectored writes.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the kernel feature is unavailable.
    /// Uses pwritev(2) on Unix and vectored positioned file I/O loop on Windows.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `fs.write`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_fs_pwritev(
        &mut self,
        handle: resource::FileHandle,
        buffers: HarnessValue<NativeSlice<NativeSlice<u8>>, VmSlice<VmSlice<u8>>>,
        offset: FileOffset,
    ) -> RuntimeResult<u64> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let buffers = buffers.into_vm("buffers")?;
                let out =
                    fs_vm::destack_fs_pwritev(self.call_context, context, handle, buffers, offset)?;
                Ok(out)
            }
            None => {
                let buffers = buffers.into_native("buffers")?;
                let mut out = std::mem::MaybeUninit::<u64>::uninit();
                unsafe {
                    fs_native::destack_fs_pwritev(
                        self.call_context,
                        out.as_mut_ptr(),
                        handle,
                        buffers,
                        offset,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// Write from multiple buffers at the given file offset with explicit write flags.
    ///
    /// Write bytes from a gather buffer list at an explicit file offset and apply host write flags.
    /// Flag bits are passed through directly and may enable append, sync, or nowait behavior on supported kernels.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the kernel feature is unavailable.
    /// Uses pwritev2(2) on Linux and runtime fallback to pwritev on other targets when flags are zero.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `fs.write`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_fs_pwritev2(
        &mut self,
        handle: resource::FileHandle,
        buffers: HarnessValue<NativeSlice<NativeSlice<u8>>, VmSlice<VmSlice<u8>>>,
        offset: FileOffset,
        flags: ReadWriteFlags,
    ) -> RuntimeResult<u64> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let buffers = buffers.into_vm("buffers")?;
                let out = fs_vm::destack_fs_pwritev2(
                    self.call_context,
                    context,
                    handle,
                    buffers,
                    offset,
                    flags,
                )?;
                Ok(out)
            }
            None => {
                let buffers = buffers.into_native("buffers")?;
                let mut out = std::mem::MaybeUninit::<u64>::uninit();
                unsafe {
                    fs_native::destack_fs_pwritev2(
                        self.call_context,
                        out.as_mut_ptr(),
                        handle,
                        buffers,
                        offset,
                        flags,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// Read from a file into the provided slice.
    ///
    /// Read bytes into one contiguous caller-provided buffer from the current file position.
    /// The file position advances by the exact byte count returned by the host.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the kernel feature is unavailable.
    /// Uses read(2) on Unix and ReadFile on Windows.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `fs.read`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_fs_read(
        &mut self,
        handle: resource::FileHandle,
        buffer: HarnessValue<NativeSlice<u8>, VmSlice<u8>>,
    ) -> RuntimeResult<u64> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let buffer = buffer.into_vm("buffer")?;
                let out = fs_vm::destack_fs_read(self.call_context, context, handle, buffer)?;
                Ok(out)
            }
            None => {
                let buffer = buffer.into_native("buffer")?;
                let mut out = std::mem::MaybeUninit::<u64>::uninit();
                unsafe {
                    fs_native::destack_fs_read(
                        self.call_context,
                        out.as_mut_ptr(),
                        handle,
                        buffer,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// Read into multiple buffers.
    ///
    /// Read bytes into a scatter buffer list from the current file position.
    /// Buffer fill order follows host iovec semantics and advances the file position by bytes read.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the kernel feature is unavailable.
    /// Uses readv(2) on Unix and vectored file I/O loop on Windows.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `fs.read`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_fs_readv(
        &mut self,
        handle: resource::FileHandle,
        buffers: HarnessValue<NativeSlice<NativeSlice<u8>>, VmSlice<VmSlice<u8>>>,
    ) -> RuntimeResult<u64> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let buffers = buffers.into_vm("buffers")?;
                let out = fs_vm::destack_fs_readv(self.call_context, context, handle, buffers)?;
                Ok(out)
            }
            None => {
                let buffers = buffers.into_native("buffers")?;
                let mut out = std::mem::MaybeUninit::<u64>::uninit();
                unsafe {
                    fs_native::destack_fs_readv(
                        self.call_context,
                        out.as_mut_ptr(),
                        handle,
                        buffers,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// Seek within a file and return the new offset.
    ///
    /// Reposition the descriptor file offset using the supplied origin and delta.
    /// Returned offset is the new descriptor position after host seek processing.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the kernel feature is unavailable.
    /// Uses lseek(2) on Unix and SetFilePointerEx on Windows.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `fs.handle`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_fs_seek(
        &mut self,
        handle: resource::FileHandle,
        offset: FileOffset,
        whence: SeekWhence,
    ) -> RuntimeResult<FileOffset> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out =
                    fs_vm::destack_fs_seek(self.call_context, context, handle, offset, whence)?;
                Ok(out)
            }
            None => {
                let mut out = std::mem::MaybeUninit::<FileOffset>::uninit();
                unsafe {
                    fs_native::destack_fs_seek(
                        self.call_context,
                        out.as_mut_ptr(),
                        handle,
                        offset,
                        whence,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// Send file data to a socket.
    ///
    /// Transfer file bytes directly from storage-backed pages to a socket endpoint.
    /// Host fast-path behavior may bypass user-space copies when supported.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the kernel feature is unavailable.
    /// Uses sendfile(2) on Unix variants and TransmitFile with userspace fallback on Windows.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `fs.read`, `fs.write`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_fs_sendfile(
        &mut self,
        socket: resource::SocketHandle,
        file: resource::FileHandle,
        offset: FileOffset,
        length: FileSize,
    ) -> RuntimeResult<u64> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = fs_vm::destack_fs_sendfile(
                    self.call_context,
                    context,
                    socket,
                    file,
                    offset,
                    length,
                )?;
                Ok(out)
            }
            None => {
                let mut out = std::mem::MaybeUninit::<u64>::uninit();
                unsafe {
                    fs_native::destack_fs_sendfile(
                        self.call_context,
                        out.as_mut_ptr(),
                        socket,
                        file,
                        offset,
                        length,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// Write file descriptor flags.
    ///
    /// Write descriptor flags such as close-on-exec to the target file descriptor.
    /// Unsupported flag bits are rejected according to host descriptor control rules.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the kernel feature is unavailable.
    /// Uses fcntl(F_SETFD) on Unix and runtime handle metadata on Windows.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `fs.handle`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_fs_set_fd_flags(
        &mut self,
        handle: resource::FileHandle,
        flags: FdFlags,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                fs_vm::destack_fs_set_fd_flags(self.call_context, context, handle, flags)
            }
            None => unsafe { fs_native::destack_fs_set_fd_flags(self.call_context, handle, flags) },
        }
    }

    /// Write file status flags.
    ///
    /// Write status flags such as append and nonblocking to the target file descriptor.
    /// Unsupported or immutable status bits are rejected by host fcntl validation.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the kernel feature is unavailable.
    /// Uses fcntl(F_SETFL) on Unix and runtime handle metadata on Windows.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `fs.handle`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_fs_set_status_flags(
        &mut self,
        handle: resource::FileHandle,
        flags: StatusFlags,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                fs_vm::destack_fs_set_status_flags(self.call_context, context, handle, flags)
            }
            None => unsafe {
                fs_native::destack_fs_set_status_flags(self.call_context, handle, flags)
            },
        }
    }

    /// Transfer bytes between descriptors using kernel splice pipelines.
    ///
    /// Move bytes between descriptor endpoints and optionally update explicit cursors for each side.
    /// This operation is intended for zero-copy file, pipe, and socket data paths where the host supports splice semantics.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the kernel feature is unavailable.
    /// Uses splice(2) on Linux and runtime fallback on targets without splice support.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `io.zero.copy`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_fs_splice(
        &mut self,
        source: resource::ResourceId,
        sourcecursor: HarnessValue<SpliceCursor, SpliceCursorVm>,
        target: resource::ResourceId,
        targetcursor: HarnessValue<SpliceCursor, SpliceCursorVm>,
        length: FileSize,
        flags: SpliceFlags,
    ) -> RuntimeResult<u64> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let sourcecursor = sourcecursor.into_vm("sourcecursor")?;
                let targetcursor = targetcursor.into_vm("targetcursor")?;
                let out = fs_vm::destack_fs_splice(
                    self.call_context,
                    context,
                    source,
                    sourcecursor,
                    target,
                    targetcursor,
                    length,
                    flags,
                )?;
                Ok(out)
            }
            None => {
                let sourcecursor = sourcecursor.into_native("sourcecursor")?;
                let targetcursor = targetcursor.into_native("targetcursor")?;
                let mut out = std::mem::MaybeUninit::<u64>::uninit();
                unsafe {
                    fs_native::destack_fs_splice(
                        self.call_context,
                        out.as_mut_ptr(),
                        source,
                        sourcecursor,
                        target,
                        targetcursor,
                        length,
                        flags,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// Synchronize a file range.
    ///
    /// Request writeback of one byte range for the target descriptor.
    /// Range ordering, blocking behavior, and fallback support follow host kernel policy.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the kernel feature is unavailable.
    /// Uses sync_file_range(2) on linux and runtime fallback on other targets.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `fs.sync`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_fs_sync_file_range(
        &mut self,
        handle: resource::FileHandle,
        offset: FileOffset,
        length: FileSize,
        flags: SyncFlags,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => fs_vm::destack_fs_sync_file_range(
                self.call_context,
                context,
                handle,
                offset,
                length,
                flags,
            ),
            None => unsafe {
                fs_native::destack_fs_sync_file_range(
                    self.call_context,
                    handle,
                    offset,
                    length,
                    flags,
                )
            },
        }
    }

    /// Synchronize a filesystem by file handle.
    ///
    /// Flush pending filesystem writeback for the mount that contains this handle.
    /// Scope and ordering guarantees follow host mount-level sync semantics.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the kernel feature is unavailable.
    /// Uses syncfs(2) on Unix and volume flush APIs on Windows.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `fs.sync`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_fs_syncfs(&mut self, handle: resource::FileHandle) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => fs_vm::destack_fs_syncfs(self.call_context, context, handle),
            None => unsafe { fs_native::destack_fs_syncfs(self.call_context, handle) },
        }
    }

    /// Duplicate bytes from one pipe to another without consuming source bytes.
    ///
    /// Clone bytes between two pipe descriptors while preserving source pipe contents.
    /// This operation is useful for fanout pipelines where consumers share the same byte stream.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the kernel feature is unavailable.
    /// Uses tee(2) on Linux and runtime fallback on targets without tee support.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `io.zero.copy`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_fs_tee(
        &mut self,
        sourcepipe: resource::PipeHandle,
        targetpipe: resource::PipeHandle,
        length: FileSize,
        flags: SpliceFlags,
    ) -> RuntimeResult<u64> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = fs_vm::destack_fs_tee(
                    self.call_context,
                    context,
                    sourcepipe,
                    targetpipe,
                    length,
                    flags,
                )?;
                Ok(out)
            }
            None => {
                let mut out = std::mem::MaybeUninit::<u64>::uninit();
                unsafe {
                    fs_native::destack_fs_tee(
                        self.call_context,
                        out.as_mut_ptr(),
                        sourcepipe,
                        targetpipe,
                        length,
                        flags,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// Truncate a file.
    ///
    /// Truncate the target file to the requested size using host file-size control APIs.
    /// Growth behavior for sparse expansion and zero-fill follows host filesystem policy.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the kernel feature is unavailable.
    /// Uses truncate(2) on Unix and SetEndOfFile via path handle on Windows.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `fs.write`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_fs_truncate(
        &mut self,
        path: HarnessValue<OsPath, OsPathVm>,
        size: FileOffset,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let path = path.into_vm("path")?;
                fs_vm::destack_fs_truncate(self.call_context, context, path, size)
            }
            None => unsafe {
                let path = path.into_native("path")?;
                fs_native::destack_fs_truncate(self.call_context, path, size)
            },
        }
    }

    /// Map user memory pages into a pipe as queued pipe buffers.
    ///
    /// Publish one set of user buffers into a pipe endpoint for downstream splice pipelines.
    /// Host kernels may pin pages or copy data depending on flags and memory state.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the kernel feature is unavailable.
    /// Uses vmsplice(2) on Linux and runtime fallback on targets without vmsplice support.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `io.zero.copy`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_fs_vmsplice(
        &mut self,
        pipe: resource::PipeHandle,
        buffers: HarnessValue<NativeSlice<NativeSlice<u8>>, VmSlice<VmSlice<u8>>>,
        flags: SpliceFlags,
    ) -> RuntimeResult<u64> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let buffers = buffers.into_vm("buffers")?;
                let out =
                    fs_vm::destack_fs_vmsplice(self.call_context, context, pipe, buffers, flags)?;
                Ok(out)
            }
            None => {
                let buffers = buffers.into_native("buffers")?;
                let mut out = std::mem::MaybeUninit::<u64>::uninit();
                unsafe {
                    fs_native::destack_fs_vmsplice(
                        self.call_context,
                        out.as_mut_ptr(),
                        pipe,
                        buffers,
                        flags,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// Write to a file from the provided slice.
    ///
    /// Write bytes from one contiguous caller-provided buffer at the current file position.
    /// The file position advances by the exact byte count accepted by the host.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the kernel feature is unavailable.
    /// Uses write(2) on Unix and WriteFile on Windows.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `fs.write`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_fs_write(
        &mut self,
        handle: resource::FileHandle,
        buffer: HarnessValue<NativeSlice<u8>, VmSlice<u8>>,
    ) -> RuntimeResult<u64> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let buffer = buffer.into_vm("buffer")?;
                let out = fs_vm::destack_fs_write(self.call_context, context, handle, buffer)?;
                Ok(out)
            }
            None => {
                let buffer = buffer.into_native("buffer")?;
                let mut out = std::mem::MaybeUninit::<u64>::uninit();
                unsafe {
                    fs_native::destack_fs_write(
                        self.call_context,
                        out.as_mut_ptr(),
                        handle,
                        buffer,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// Write from multiple buffers.
    ///
    /// Write bytes from a gather buffer list at the current file position.
    /// Buffer consumption order follows host iovec semantics and advances the file position by bytes written.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the kernel feature is unavailable.
    /// Uses writev(2) on Unix and vectored file I/O loop on Windows.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `fs.write`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_fs_writev(
        &mut self,
        handle: resource::FileHandle,
        buffers: HarnessValue<NativeSlice<NativeSlice<u8>>, VmSlice<VmSlice<u8>>>,
    ) -> RuntimeResult<u64> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let buffers = buffers.into_vm("buffers")?;
                let out = fs_vm::destack_fs_writev(self.call_context, context, handle, buffers)?;
                Ok(out)
            }
            None => {
                let buffers = buffers.into_native("buffers")?;
                let mut out = std::mem::MaybeUninit::<u64>::uninit();
                unsafe {
                    fs_native::destack_fs_writev(
                        self.call_context,
                        out.as_mut_ptr(),
                        handle,
                        buffers,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// Advise the kernel about access patterns.
    ///
    /// Advise the kernel about access patterns via host kernel APIs.
    /// Return values and failures map directly to host contracts so higher layers can apply policy explicitly.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the kernel feature is unavailable.
    /// Uses madvise(2) on Unix and advisory memory APIs where available on Windows.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `fs.mmap`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_fs_madvise(
        &mut self,
        mapping: HarnessValue<NativeSlice<u8>, VmSlice<u8>>,
        advice: MmapAdvice,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let mapping = mapping.into_vm("mapping")?;
                fs_vm::destack_fs_madvise(self.call_context, context, mapping, advice)
            }
            None => unsafe {
                let mapping = mapping.into_native("mapping")?;
                fs_native::destack_fs_madvise(self.call_context, mapping, advice)
            },
        }
    }

    /// Create an anonymous memory mapping.
    ///
    /// Map an anonymous zero-initialized region into virtual memory using host allocation primitives.
    /// Page alignment, commit behavior, and memory fault semantics follow host virtual-memory rules.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the kernel feature is unavailable.
    /// Uses mmap(2) MAP_ANONYMOUS on Unix and VirtualAlloc/MapViewOfFile on Windows.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `fs.mmap`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_fs_mmap_anonymous(
        &mut self,
        length: FileSize,
        prot: MmapProt,
        flags: MmapFlags,
    ) -> RuntimeResult<HarnessValue<NativeSlice<u8>, VmSlice<u8>>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = fs_vm::destack_fs_mmap_anonymous(
                    self.call_context,
                    context,
                    length,
                    prot,
                    flags,
                )?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let mut out = std::mem::MaybeUninit::<NativeSlice<u8>>::uninit();
                unsafe {
                    fs_native::destack_fs_mmap_anonymous(
                        self.call_context,
                        out.as_mut_ptr(),
                        length,
                        prot,
                        flags,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Create a file-backed memory mapping.
    ///
    /// Map a file-backed region into virtual memory using the requested offset, length, and protection.
    /// Page alignment, commit behavior, and memory fault semantics follow host virtual-memory rules.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the kernel feature is unavailable.
    /// Uses mmap(2) MAP_SHARED/MAP_PRIVATE on Unix and CreateFileMapping/MapViewOfFile on Windows.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `fs.mmap`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_fs_mmap_file(
        &mut self,
        handle: resource::FileHandle,
        offset: FileOffset,
        length: FileSize,
        prot: MmapProt,
        flags: MmapFlags,
    ) -> RuntimeResult<HarnessValue<NativeSlice<u8>, VmSlice<u8>>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = fs_vm::destack_fs_mmap_file(
                    self.call_context,
                    context,
                    handle,
                    offset,
                    length,
                    prot,
                    flags,
                )?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let mut out = std::mem::MaybeUninit::<NativeSlice<u8>>::uninit();
                unsafe {
                    fs_native::destack_fs_mmap_file(
                        self.call_context,
                        out.as_mut_ptr(),
                        handle,
                        offset,
                        length,
                        prot,
                        flags,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Change memory protection for a mapping.
    ///
    /// Change memory protection for a mapping via host kernel APIs.
    /// Page alignment, commit behavior, and memory fault semantics follow host virtual-memory rules.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the kernel feature is unavailable.
    /// Uses mprotect(2) on Unix and VirtualProtect on Windows.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `fs.mmap`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_fs_mprotect(
        &mut self,
        mapping: HarnessValue<NativeSlice<u8>, VmSlice<u8>>,
        prot: MmapProt,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let mapping = mapping.into_vm("mapping")?;
                fs_vm::destack_fs_mprotect(self.call_context, context, mapping, prot)
            }
            None => unsafe {
                let mapping = mapping.into_native("mapping")?;
                fs_native::destack_fs_mprotect(self.call_context, mapping, prot)
            },
        }
    }

    /// Flush a mapping to storage.
    ///
    /// Flush a mapping to storage via host kernel APIs.
    /// Return values and failures map directly to host contracts so higher layers can apply policy explicitly.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the kernel feature is unavailable.
    /// Uses msync(2) on Unix and FlushViewOfFile on Windows.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `fs.mmap`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_fs_msync(
        &mut self,
        mapping: HarnessValue<NativeSlice<u8>, VmSlice<u8>>,
        flags: MmapSyncFlags,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let mapping = mapping.into_vm("mapping")?;
                fs_vm::destack_fs_msync(self.call_context, context, mapping, flags)
            }
            None => unsafe {
                let mapping = mapping.into_native("mapping")?;
                fs_native::destack_fs_msync(self.call_context, mapping, flags)
            },
        }
    }

    /// Unmap a memory region.
    ///
    /// Unmap the specified virtual-memory range and release its mapping resources.
    /// Page alignment, commit behavior, and memory fault semantics follow host virtual-memory rules.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the kernel feature is unavailable.
    /// Uses munmap(2) on Unix and UnmapViewOfFile/VirtualFree on Windows.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `fs.mmap`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_fs_munmap(
        &mut self,
        mapping: HarnessValue<NativeSlice<u8>, VmSlice<u8>>,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let mapping = mapping.into_vm("mapping")?;
                fs_vm::destack_fs_munmap(self.call_context, context, mapping)
            }
            None => unsafe {
                let mapping = mapping.into_native("mapping")?;
                fs_native::destack_fs_munmap(self.call_context, mapping)
            },
        }
    }

    /// Copy a file.
    ///
    /// Copy file contents from source path to destination path.
    /// Copy flags control overwrite behavior, and the destination mode follows host copy semantics.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the kernel feature is unavailable.
    /// Uses copy_file_range/copy fallback on Unix and CopyFileW on Windows.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `fs.read`, `fs.write`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_fs_copyfile(
        &mut self,
        from: HarnessValue<OsPath, OsPathVm>,
        to: HarnessValue<OsPath, OsPathVm>,
        flags: CopyFlags,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let from = from.into_vm("from")?;
                let to = to.into_vm("to")?;
                fs_vm::destack_fs_copyfile(self.call_context, context, from, to, flags)
            }
            None => unsafe {
                let from = from.into_native("from")?;
                let to = to.into_native("to")?;
                fs_native::destack_fs_copyfile(self.call_context, from, to, flags)
            },
        }
    }

    /// Create a hard link.
    ///
    /// Create a hard-link entry that points to an existing inode without copying file contents.
    /// Source and destination remain independent path entries with shared storage identity.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the kernel feature is unavailable.
    /// Uses link(2) on Unix and CreateHardLinkW on Windows.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `fs.link`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_fs_link(
        &mut self,
        existingpath: HarnessValue<OsPath, OsPathVm>,
        newpath: HarnessValue<OsPath, OsPathVm>,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let existingpath = existingpath.into_vm("existingpath")?;
                let newpath = newpath.into_vm("newpath")?;
                fs_vm::destack_fs_link(self.call_context, context, existingpath, newpath)
            }
            None => unsafe {
                let existingpath = existingpath.into_native("existingpath")?;
                let newpath = newpath.into_native("newpath")?;
                fs_native::destack_fs_link(self.call_context, existingpath, newpath)
            },
        }
    }

    /// Create a hard link relative to directory handles.
    ///
    /// Create a hard-link entry using directory-relative paths for both source and destination.
    /// Relative resolution keeps both lookup roots explicit and avoids ambient cwd lookup.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the kernel feature is unavailable.
    /// Uses linkat(2) on Unix and handle-relative hard-link creation on Windows.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `fs.link`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_fs_linkat(
        &mut self,
        existingdir: resource::DirectoryHandle,
        existingpath: HarnessValue<OsPath, OsPathVm>,
        newdir: resource::DirectoryHandle,
        newpath: HarnessValue<OsPath, OsPathVm>,
        flags: AtFlags,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let existingpath = existingpath.into_vm("existingpath")?;
                let newpath = newpath.into_vm("newpath")?;
                fs_vm::destack_fs_linkat(
                    self.call_context,
                    context,
                    existingdir,
                    existingpath,
                    newdir,
                    newpath,
                    flags,
                )
            }
            None => unsafe {
                let existingpath = existingpath.into_native("existingpath")?;
                let newpath = newpath.into_native("newpath")?;
                fs_native::destack_fs_linkat(
                    self.call_context,
                    existingdir,
                    existingpath,
                    newdir,
                    newpath,
                    flags,
                )
            },
        }
    }

    /// Create a FIFO special file.
    ///
    /// Create a FIFO special file node at the target path.
    /// The created node participates in host pipe semantics when opened for I/O.
    ///
    /// # Platform
    /// Unix only. This operation returns `notSupported` on Windows.
    /// Uses mkfifo(2) on Unix and notSupported on Windows.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `fs.special`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_fs_mkfifo(
        &mut self,
        path: HarnessValue<OsPath, OsPathVm>,
        mode: FileMode,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let path = path.into_vm("path")?;
                fs_vm::destack_fs_mkfifo(self.call_context, context, path, mode)
            }
            None => unsafe {
                let path = path.into_native("path")?;
                fs_native::destack_fs_mkfifo(self.call_context, path, mode)
            },
        }
    }

    /// Create a FIFO special file relative to a directory handle.
    ///
    /// Create a FIFO special file node at a directory-relative path.
    /// Relative node creation keeps lookup scope anchored to `dir`.
    ///
    /// # Platform
    /// Unix only. This operation returns `notSupported` on Windows.
    /// Uses mkfifoat(2) on Unix and notSupported on Windows.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `fs.special`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_fs_mkfifoat(
        &mut self,
        dir: resource::DirectoryHandle,
        path: HarnessValue<OsPath, OsPathVm>,
        mode: FileMode,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let path = path.into_vm("path")?;
                fs_vm::destack_fs_mkfifoat(self.call_context, context, dir, path, mode)
            }
            None => unsafe {
                let path = path.into_native("path")?;
                fs_native::destack_fs_mkfifoat(self.call_context, dir, path, mode)
            },
        }
    }

    /// Create a filesystem node.
    ///
    /// Create a filesystem node with the requested mode and device number.
    /// Node interpretation follows host mknod rules for file type and device payload.
    ///
    /// # Platform
    /// Unix only. This operation returns `notSupported` on Windows.
    /// Uses mknod(2) on Unix and notSupported on Windows.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `fs.special`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_fs_mknod(
        &mut self,
        path: HarnessValue<OsPath, OsPathVm>,
        mode: FileMode,
        device: NodeDevice,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let path = path.into_vm("path")?;
                fs_vm::destack_fs_mknod(self.call_context, context, path, mode, device)
            }
            None => unsafe {
                let path = path.into_native("path")?;
                fs_native::destack_fs_mknod(self.call_context, path, mode, device)
            },
        }
    }

    /// Create a filesystem node relative to a directory handle.
    ///
    /// Create a filesystem node at a directory-relative path with the requested mode and device number.
    /// Relative creation keeps lookup scope anchored to `dir`.
    ///
    /// # Platform
    /// Unix only. This operation returns `notSupported` on Windows.
    /// Uses mknodat(2) on Unix and notSupported on Windows.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `fs.special`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_fs_mknodat(
        &mut self,
        dir: resource::DirectoryHandle,
        path: HarnessValue<OsPath, OsPathVm>,
        mode: FileMode,
        device: NodeDevice,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let path = path.into_vm("path")?;
                fs_vm::destack_fs_mknodat(self.call_context, context, dir, path, mode, device)
            }
            None => unsafe {
                let path = path.into_native("path")?;
                fs_native::destack_fs_mknodat(self.call_context, dir, path, mode, device)
            },
        }
    }

    /// Read a symbolic link.
    ///
    /// Read the link payload stored at the target path and return it as an `OsPath`.
    /// The returned path is link data and is not canonicalized or dereferenced.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the kernel feature is unavailable.
    /// Uses readlink(2) on Unix and reparse-point target query on Windows.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `fs.read`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_fs_readlink(
        &mut self,
        path: HarnessValue<OsPath, OsPathVm>,
    ) -> RuntimeResult<HarnessValue<OsPath, OsPathVm>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let path = path.into_vm("path")?;
                let out = fs_vm::destack_fs_readlink(self.call_context, context, path)?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let path = path.into_native("path")?;
                let mut out = std::mem::MaybeUninit::<OsPath>::uninit();
                unsafe {
                    fs_native::destack_fs_readlink(self.call_context, out.as_mut_ptr(), path)?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Read a symbolic link relative to a directory handle.
    ///
    /// Read the link payload stored at a directory-relative target path.
    /// The returned path is raw link data and is not dereferenced during the read.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the kernel feature is unavailable.
    /// Uses readlinkat(2) on Unix and handle-relative target query on Windows.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `fs.read`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_fs_readlinkat(
        &mut self,
        dir: resource::DirectoryHandle,
        path: HarnessValue<OsPath, OsPathVm>,
    ) -> RuntimeResult<HarnessValue<OsPath, OsPathVm>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let path = path.into_vm("path")?;
                let out = fs_vm::destack_fs_readlinkat(self.call_context, context, dir, path)?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let path = path.into_native("path")?;
                let mut out = std::mem::MaybeUninit::<OsPath>::uninit();
                unsafe {
                    fs_native::destack_fs_readlinkat(
                        self.call_context,
                        out.as_mut_ptr(),
                        dir,
                        path,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Resolve a path to its canonical form.
    ///
    /// Resolve the input path to a canonical absolute form using host path-resolution rules.
    /// Canonicalization follows host symlink, mount, and case-normalization behavior.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the kernel feature is unavailable.
    /// Uses realpath(3) on Unix and GetFinalPathNameByHandleW on Windows.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `fs.metadata`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_fs_realpath(
        &mut self,
        path: HarnessValue<OsPath, OsPathVm>,
    ) -> RuntimeResult<HarnessValue<OsPath, OsPathVm>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let path = path.into_vm("path")?;
                let out = fs_vm::destack_fs_realpath(self.call_context, context, path)?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let path = path.into_native("path")?;
                let mut out = std::mem::MaybeUninit::<OsPath>::uninit();
                unsafe {
                    fs_native::destack_fs_realpath(self.call_context, out.as_mut_ptr(), path)?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Rename or move a file.
    ///
    /// Rename one path entry to a new absolute or relative path in the current process namespace.
    /// The operation targets plain path names and does not expose directory-handle scoping.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the kernel feature is unavailable.
    /// Uses rename(2) on Unix and MoveFileExW on Windows.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `fs.write`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_fs_rename(
        &mut self,
        from: HarnessValue<OsPath, OsPathVm>,
        to: HarnessValue<OsPath, OsPathVm>,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let from = from.into_vm("from")?;
                let to = to.into_vm("to")?;
                fs_vm::destack_fs_rename(self.call_context, context, from, to)
            }
            None => unsafe {
                let from = from.into_native("from")?;
                let to = to.into_native("to")?;
                fs_native::destack_fs_rename(self.call_context, from, to)
            },
        }
    }

    /// Rename or move a file relative to directory handles.
    ///
    /// Rename one path entry where both source and destination are resolved relative to explicit directory handles.
    /// This avoids ambient current-working-directory resolution for both sides of the rename.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the kernel feature is unavailable.
    /// Uses renameat(2) on Unix and handle-relative rename on Windows.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `fs.write`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_fs_renameat(
        &mut self,
        fromdir: resource::DirectoryHandle,
        from: HarnessValue<OsPath, OsPathVm>,
        todir: resource::DirectoryHandle,
        to: HarnessValue<OsPath, OsPathVm>,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let from = from.into_vm("from")?;
                let to = to.into_vm("to")?;
                fs_vm::destack_fs_renameat(self.call_context, context, fromdir, from, todir, to)
            }
            None => unsafe {
                let from = from.into_native("from")?;
                let to = to.into_native("to")?;
                fs_native::destack_fs_renameat(self.call_context, fromdir, from, todir, to)
            },
        }
    }

    /// Rename or move a file relative to directory handles with renameat2 semantics.
    ///
    /// Rename one path entry with explicit rename flags controlling replace and exchange behavior.
    /// Flag handling follows host support levels and returns notSupported when the requested mode is unavailable.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the kernel feature is unavailable.
    /// Uses renameat2(2) on linux and runtime emulation/fallback on other targets.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `fs.write`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_fs_renameat2(
        &mut self,
        fromdir: resource::DirectoryHandle,
        from: HarnessValue<OsPath, OsPathVm>,
        todir: resource::DirectoryHandle,
        to: HarnessValue<OsPath, OsPathVm>,
        flags: RenameFlags,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let from = from.into_vm("from")?;
                let to = to.into_vm("to")?;
                fs_vm::destack_fs_renameat2(
                    self.call_context,
                    context,
                    fromdir,
                    from,
                    todir,
                    to,
                    flags,
                )
            }
            None => unsafe {
                let from = from.into_native("from")?;
                let to = to.into_native("to")?;
                fs_native::destack_fs_renameat2(self.call_context, fromdir, from, todir, to, flags)
            },
        }
    }

    /// Create a symbolic link.
    ///
    /// Create a symbolic-link entry that stores the provided target path payload.
    /// Target bytes are persisted as link data and are not resolved during creation.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the kernel feature is unavailable.
    /// Uses symlink(2) on Unix and CreateSymbolicLinkW on Windows.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `fs.link`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_fs_symlink(
        &mut self,
        target: HarnessValue<OsPath, OsPathVm>,
        path: HarnessValue<OsPath, OsPathVm>,
        kind: SymlinkType,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let target = target.into_vm("target")?;
                let path = path.into_vm("path")?;
                fs_vm::destack_fs_symlink(self.call_context, context, target, path, kind)
            }
            None => unsafe {
                let target = target.into_native("target")?;
                let path = path.into_native("path")?;
                fs_native::destack_fs_symlink(self.call_context, target, path, kind)
            },
        }
    }

    /// Create a symbolic link relative to a directory handle.
    ///
    /// Create a symbolic-link entry using a directory-relative destination path.
    /// Destination lookup uses `dir` while `target` bytes are stored verbatim by the host.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the kernel feature is unavailable.
    /// Uses symlinkat(2) on Unix and handle-relative symlink creation on Windows.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `fs.link`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_fs_symlinkat(
        &mut self,
        target: HarnessValue<OsPath, OsPathVm>,
        dir: resource::DirectoryHandle,
        path: HarnessValue<OsPath, OsPathVm>,
        kind: SymlinkType,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let target = target.into_vm("target")?;
                let path = path.into_vm("path")?;
                fs_vm::destack_fs_symlinkat(self.call_context, context, target, dir, path, kind)
            }
            None => unsafe {
                let target = target.into_native("target")?;
                let path = path.into_native("path")?;
                fs_native::destack_fs_symlinkat(self.call_context, target, dir, path, kind)
            },
        }
    }

    /// Unlink a file.
    ///
    /// Remove one directory entry that names a non-directory filesystem object.
    /// Data blocks are reclaimed by the host once link count and open-handle rules allow.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the kernel feature is unavailable.
    /// Uses unlink(2) on Unix and DeleteFileW on Windows.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `fs.write`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_fs_unlink(
        &mut self,
        path: HarnessValue<OsPath, OsPathVm>,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let path = path.into_vm("path")?;
                fs_vm::destack_fs_unlink(self.call_context, context, path)
            }
            None => unsafe {
                let path = path.into_native("path")?;
                fs_native::destack_fs_unlink(self.call_context, path)
            },
        }
    }

    /// Unlink a file relative to a directory handle.
    ///
    /// Remove one directory entry resolved from `dir` for a non-directory filesystem object.
    /// Relative unlink avoids ambient cwd traversal and keeps deletion scope explicit.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the kernel feature is unavailable.
    /// Uses unlinkat(2) on Unix and handle-relative delete on Windows.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `fs.write`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_fs_unlinkat(
        &mut self,
        dir: resource::DirectoryHandle,
        path: HarnessValue<OsPath, OsPathVm>,
        flags: AtFlags,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let path = path.into_vm("path")?;
                fs_vm::destack_fs_unlinkat(self.call_context, context, dir, path, flags)
            }
            None => unsafe {
                let path = path.into_native("path")?;
                fs_native::destack_fs_unlinkat(self.call_context, dir, path, flags)
            },
        }
    }

    /// Stat a file by handle.
    ///
    /// Stat a file by handle via host kernel APIs.
    /// Paths are forwarded from `OsPath` without runtime normalization or canonicalization, and permission checks follow host filesystem rules.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the kernel feature is unavailable.
    /// Uses fstat(2) on Unix and GetFileInformationByHandleEx on Windows.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `fs.metadata`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_fs_fstat(
        &mut self,
        handle: resource::FileHandle,
    ) -> RuntimeResult<HarnessValue<Stat, StatVm>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = fs_vm::destack_fs_fstat(self.call_context, context, handle)?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let mut out = std::mem::MaybeUninit::<Stat>::uninit();
                unsafe {
                    fs_native::destack_fs_fstat(self.call_context, out.as_mut_ptr(), handle)?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Stat a filesystem by handle.
    ///
    /// Stat a filesystem by handle via host kernel APIs.
    /// Paths are forwarded from `OsPath` without runtime normalization or canonicalization, and permission checks follow host filesystem rules.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the kernel feature is unavailable.
    /// Uses fstatfs/statvfs by handle on Unix and volume information by handle on Windows.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `fs.metadata`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_fs_fstatfs(
        &mut self,
        handle: resource::FileHandle,
    ) -> RuntimeResult<HarnessValue<StatFs, StatFsVm>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = fs_vm::destack_fs_fstatfs(self.call_context, context, handle)?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let mut out = std::mem::MaybeUninit::<StatFs>::uninit();
                unsafe {
                    fs_native::destack_fs_fstatfs(self.call_context, out.as_mut_ptr(), handle)?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Stat a file without following symlinks.
    ///
    /// Stat a file without following symlinks via host kernel APIs.
    /// Paths are forwarded from `OsPath` without runtime normalization or canonicalization, and permission checks follow host filesystem rules.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the kernel feature is unavailable.
    /// Uses lstat(2) on Unix and reparse-point aware metadata query on Windows.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `fs.metadata`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_fs_lstat(
        &mut self,
        path: HarnessValue<OsPath, OsPathVm>,
    ) -> RuntimeResult<HarnessValue<Stat, StatVm>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let path = path.into_vm("path")?;
                let out = fs_vm::destack_fs_lstat(self.call_context, context, path)?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let path = path.into_native("path")?;
                let mut out = std::mem::MaybeUninit::<Stat>::uninit();
                unsafe {
                    fs_native::destack_fs_lstat(self.call_context, out.as_mut_ptr(), path)?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Stat a file.
    ///
    /// Stat a file via host kernel APIs.
    /// Paths are forwarded from `OsPath` without runtime normalization or canonicalization, and permission checks follow host filesystem rules.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the kernel feature is unavailable.
    /// Uses stat(2) on Unix and GetFileInformationByHandleEx on Windows.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `fs.metadata`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_fs_stat(
        &mut self,
        path: HarnessValue<OsPath, OsPathVm>,
    ) -> RuntimeResult<HarnessValue<Stat, StatVm>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let path = path.into_vm("path")?;
                let out = fs_vm::destack_fs_stat(self.call_context, context, path)?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let path = path.into_native("path")?;
                let mut out = std::mem::MaybeUninit::<Stat>::uninit();
                unsafe {
                    fs_native::destack_fs_stat(self.call_context, out.as_mut_ptr(), path)?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Stat a file relative to a directory handle.
    ///
    /// Stat a file relative to a directory handle via host kernel APIs.
    /// Paths are forwarded from `OsPath` without runtime normalization or canonicalization, and permission checks follow host filesystem rules.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the kernel feature is unavailable.
    /// Uses fstatat(2) on Unix and handle-relative stat on Windows.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `fs.metadata`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_fs_statat(
        &mut self,
        dir: resource::DirectoryHandle,
        path: HarnessValue<OsPath, OsPathVm>,
        flags: AtFlags,
    ) -> RuntimeResult<HarnessValue<Stat, StatVm>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let path = path.into_vm("path")?;
                let out = fs_vm::destack_fs_statat(self.call_context, context, dir, path, flags)?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let path = path.into_native("path")?;
                let mut out = std::mem::MaybeUninit::<Stat>::uninit();
                unsafe {
                    fs_native::destack_fs_statat(
                        self.call_context,
                        out.as_mut_ptr(),
                        dir,
                        path,
                        flags,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Stat a filesystem.
    ///
    /// Stat a filesystem via host kernel APIs.
    /// Paths are forwarded from `OsPath` without runtime normalization or canonicalization, and permission checks follow host filesystem rules.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the kernel feature is unavailable.
    /// Uses statfs/statvfs on Unix and GetDiskFreeSpaceExW/GetVolumeInformationW on Windows.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `fs.metadata`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_fs_statfs(
        &mut self,
        path: HarnessValue<OsPath, OsPathVm>,
    ) -> RuntimeResult<HarnessValue<StatFs, StatFsVm>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let path = path.into_vm("path")?;
                let out = fs_vm::destack_fs_statfs(self.call_context, context, path)?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let path = path.into_native("path")?;
                let mut out = std::mem::MaybeUninit::<StatFs>::uninit();
                unsafe {
                    fs_native::destack_fs_statfs(self.call_context, out.as_mut_ptr(), path)?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Stat a path with statx semantics.
    ///
    /// Stat a path with statx semantics via host kernel APIs.
    /// Paths are forwarded from `OsPath` without runtime normalization or canonicalization, and permission checks follow host filesystem rules.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the kernel feature is unavailable.
    /// Uses statx(2) on linux and runtime fallback on other targets.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `fs.metadata`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_fs_statx(
        &mut self,
        dir: resource::DirectoryHandle,
        path: HarnessValue<OsPath, OsPathVm>,
        flags: StatxFlags,
        mask: StatxMask,
    ) -> RuntimeResult<HarnessValue<Statx, StatxVm>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let path = path.into_vm("path")?;
                let out =
                    fs_vm::destack_fs_statx(self.call_context, context, dir, path, flags, mask)?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let path = path.into_native("path")?;
                let mut out = std::mem::MaybeUninit::<Statx>::uninit();
                unsafe {
                    fs_native::destack_fs_statx(
                        self.call_context,
                        out.as_mut_ptr(),
                        dir,
                        path,
                        flags,
                        mask,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Start watching a path and return a watch handle.
    ///
    /// Registers the path with the native watch backend and starts event delivery for the selected mask.
    /// Event ordering and coalescing behavior are backend defined.
    ///
    /// # Platform
    /// Unix and Windows.
    /// Uses inotify on Linux, kqueue on BSD, FSEvents on macOS, and ReadDirectoryChangesW on Windows.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `fs.watch`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_fs_watch(
        &mut self,
        path: HarnessValue<OsPath, OsPathVm>,
        options: HarnessValue<WatchOptions, WatchOptionsVm>,
    ) -> RuntimeResult<resource::WatchHandle> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let path = path.into_vm("path")?;
                let options = options.into_vm("options")?;
                let out = fs_vm::destack_fs_watch(self.call_context, context, path, options)?;
                Ok(out)
            }
            None => {
                let path = path.into_native("path")?;
                let options = options.into_native("options")?;
                let mut out = std::mem::MaybeUninit::<resource::WatchHandle>::uninit();
                unsafe {
                    fs_native::destack_fs_watch(
                        self.call_context,
                        out.as_mut_ptr(),
                        path,
                        options,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// Close a watch handle.
    ///
    /// Unregisters the watch from the backend and releases associated runtime resources.
    /// No further events are delivered after close succeeds.
    ///
    /// # Platform
    /// Unix and Windows.
    /// Uses backend specific handle close and unregister operations.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `fs.watch`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_fs_watch_close(
        &mut self,
        handle: resource::WatchHandle,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => fs_vm::destack_fs_watch_close(self.call_context, context, handle),
            None => unsafe { fs_native::destack_fs_watch_close(self.call_context, handle) },
        }
    }

    /// Read a batch of events from a watch handle.
    ///
    /// Reads available watch records from the backend queue and reports overflow explicitly when events were dropped.
    /// Callers should treat `overflowed` as a signal to resynchronize state.
    ///
    /// # Platform
    /// Unix and Windows.
    /// Uses inotify event reads on Linux, kevent on BSD, FSEvents stream reads on macOS, and ReadDirectoryChangesW reads on Windows.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `fs.watch`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_fs_watch_read(
        &mut self,
        handle: resource::WatchHandle,
    ) -> RuntimeResult<HarnessValue<WatchBatch, WatchBatchVm>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = fs_vm::destack_fs_watch_read(self.call_context, context, handle)?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let mut out = std::mem::MaybeUninit::<WatchBatch>::uninit();
                unsafe {
                    fs_native::destack_fs_watch_read(self.call_context, out.as_mut_ptr(), handle)?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Start watching a path relative to a directory handle.
    ///
    /// Resolves the path relative to the supplied directory and registers the resulting entry with the backend watcher.
    /// Event ordering and coalescing behavior are backend defined.
    ///
    /// # Platform
    /// Unix and Windows.
    /// Uses inotify on Linux, kqueue on BSD, FSEvents on macOS, and ReadDirectoryChangesW on Windows.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `fs.watch`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_fs_watchat(
        &mut self,
        directory: resource::DirectoryHandle,
        path: HarnessValue<OsPath, OsPathVm>,
        options: HarnessValue<WatchOptions, WatchOptionsVm>,
    ) -> RuntimeResult<resource::WatchHandle> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let path = path.into_vm("path")?;
                let options = options.into_vm("options")?;
                let out = fs_vm::destack_fs_watchat(
                    self.call_context,
                    context,
                    directory,
                    path,
                    options,
                )?;
                Ok(out)
            }
            None => {
                let path = path.into_native("path")?;
                let options = options.into_native("options")?;
                let mut out = std::mem::MaybeUninit::<resource::WatchHandle>::uninit();
                unsafe {
                    fs_native::destack_fs_watchat(
                        self.call_context,
                        out.as_mut_ptr(),
                        directory,
                        path,
                        options,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// Read an extended attribute by handle.
    ///
    /// Transfer bytes directly between caller buffers and host descriptors using short I/O semantics.
    /// Partial transfers are preserved exactly as reported by the host, and callers must loop when full completion is required.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the kernel feature is unavailable.
    /// Uses fgetxattr(2) on Unix and handle-based xattr query where available on Windows.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `fs.xattr`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_fs_fgetxattr(
        &mut self,
        handle: resource::FileHandle,
        name: HarnessValue<NativeStringRef, vm::StringHandle>,
    ) -> RuntimeResult<HarnessValue<NativeArray<u8>, VmArray<u8>>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let name = name.into_vm("name")?;
                let out = fs_vm::destack_fs_fgetxattr(self.call_context, context, handle, name)?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let name = name.into_native("name")?;
                let mut out = std::mem::MaybeUninit::<NativeArray<u8>>::uninit();
                unsafe {
                    fs_native::destack_fs_fgetxattr(
                        self.call_context,
                        out.as_mut_ptr(),
                        handle,
                        name,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Read an extended attribute by handle with a raw name payload.
    ///
    /// Transfer bytes directly between caller buffers and host descriptors using short I/O semantics.
    /// Raw name bytes preserve host namespace data without UTF transcoding.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the kernel feature is unavailable.
    /// Uses fgetxattr(2) on Unix and handle-based xattr query where available on Windows.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `fs.xattr`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_fs_fgetxattr_bytes(
        &mut self,
        handle: resource::FileHandle,
        name: HarnessValue<NativeSlice<u8>, VmSlice<u8>>,
    ) -> RuntimeResult<HarnessValue<NativeArray<u8>, VmArray<u8>>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let name = name.into_vm("name")?;
                let out =
                    fs_vm::destack_fs_fgetxattr_bytes(self.call_context, context, handle, name)?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let name = name.into_native("name")?;
                let mut out = std::mem::MaybeUninit::<NativeArray<u8>>::uninit();
                unsafe {
                    fs_native::destack_fs_fgetxattr_bytes(
                        self.call_context,
                        out.as_mut_ptr(),
                        handle,
                        name,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// List extended attribute names by handle.
    ///
    /// List extended attribute names by handle via host kernel APIs.
    /// Return values and failures map directly to host contracts so higher layers can apply policy explicitly.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the kernel feature is unavailable.
    /// Uses flistxattr(2) on Unix and handle-based xattr enumeration on Windows.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `fs.xattr`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_fs_flistxattr(
        &mut self,
        handle: resource::FileHandle,
    ) -> RuntimeResult<HarnessValue<NativeArray<NativeStringRef>, VmArray<vm::StringHandle>>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = fs_vm::destack_fs_flistxattr(self.call_context, context, handle)?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let mut out = std::mem::MaybeUninit::<NativeArray<NativeStringRef>>::uninit();
                unsafe {
                    fs_native::destack_fs_flistxattr(self.call_context, out.as_mut_ptr(), handle)?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// List extended attribute names by handle as raw byte payloads.
    ///
    /// List extended attribute names by handle via host kernel APIs.
    /// Raw name bytes preserve host namespace data without UTF transcoding.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the kernel feature is unavailable.
    /// Uses flistxattr(2) on Unix and handle-based xattr enumeration on Windows.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `fs.xattr`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_fs_flistxattr_bytes(
        &mut self,
        handle: resource::FileHandle,
    ) -> RuntimeResult<HarnessValue<NativeArray<NativeArray<u8>>, VmArray<VmArray<u8>>>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = fs_vm::destack_fs_flistxattr_bytes(self.call_context, context, handle)?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let mut out = std::mem::MaybeUninit::<NativeArray<NativeArray<u8>>>::uninit();
                unsafe {
                    fs_native::destack_fs_flistxattr_bytes(
                        self.call_context,
                        out.as_mut_ptr(),
                        handle,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Remove an extended attribute by handle.
    ///
    /// Remove the target resource through a single host namespace operation with no runtime fallback path.
    /// Return values and failures map directly to host contracts so higher layers can apply policy explicitly.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the kernel feature is unavailable.
    /// Uses fremovexattr(2) on Unix and handle-based xattr delete on Windows.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `fs.xattr`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_fs_fremovexattr(
        &mut self,
        handle: resource::FileHandle,
        name: HarnessValue<NativeStringRef, vm::StringHandle>,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let name = name.into_vm("name")?;
                fs_vm::destack_fs_fremovexattr(self.call_context, context, handle, name)
            }
            None => unsafe {
                let name = name.into_native("name")?;
                fs_native::destack_fs_fremovexattr(self.call_context, handle, name)
            },
        }
    }

    /// Remove an extended attribute by handle with a raw name payload.
    ///
    /// Remove the target resource through a single host namespace operation with no runtime fallback path.
    /// Raw name bytes preserve host namespace data without UTF transcoding.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the kernel feature is unavailable.
    /// Uses fremovexattr(2) on Unix and handle-based xattr delete on Windows.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `fs.xattr`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_fs_fremovexattr_bytes(
        &mut self,
        handle: resource::FileHandle,
        name: HarnessValue<NativeSlice<u8>, VmSlice<u8>>,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let name = name.into_vm("name")?;
                fs_vm::destack_fs_fremovexattr_bytes(self.call_context, context, handle, name)
            }
            None => unsafe {
                let name = name.into_native("name")?;
                fs_native::destack_fs_fremovexattr_bytes(self.call_context, handle, name)
            },
        }
    }

    /// Set an extended attribute by handle.
    ///
    /// Set the requested control value on the descriptor through the native option interface.
    /// The binding performs one control transaction and returns the exact host outcome without policy retries.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the kernel feature is unavailable.
    /// Uses fsetxattr(2) on Unix and handle-based xattr write where available on Windows.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `fs.xattr`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_fs_fsetxattr(
        &mut self,
        handle: resource::FileHandle,
        name: HarnessValue<NativeStringRef, vm::StringHandle>,
        argument_value: HarnessValue<NativeSlice<u8>, VmSlice<u8>>,
        flags: XattrFlags,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let name = name.into_vm("name")?;
                let argument_value = argument_value.into_vm("argument_value")?;
                fs_vm::destack_fs_fsetxattr(
                    self.call_context,
                    context,
                    handle,
                    name,
                    argument_value,
                    flags,
                )
            }
            None => unsafe {
                let name = name.into_native("name")?;
                let argument_value = argument_value.into_native("argument_value")?;
                fs_native::destack_fs_fsetxattr(
                    self.call_context,
                    handle,
                    name,
                    argument_value,
                    flags,
                )
            },
        }
    }

    /// Set an extended attribute by handle with a raw name payload.
    ///
    /// Set the requested control value on the descriptor through the native option interface.
    /// Raw name bytes preserve host namespace data without UTF transcoding.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the kernel feature is unavailable.
    /// Uses fsetxattr(2) on Unix and handle-based xattr write where available on Windows.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `fs.xattr`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_fs_fsetxattr_bytes(
        &mut self,
        handle: resource::FileHandle,
        name: HarnessValue<NativeSlice<u8>, VmSlice<u8>>,
        argument_value: HarnessValue<NativeSlice<u8>, VmSlice<u8>>,
        flags: XattrFlags,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let name = name.into_vm("name")?;
                let argument_value = argument_value.into_vm("argument_value")?;
                fs_vm::destack_fs_fsetxattr_bytes(
                    self.call_context,
                    context,
                    handle,
                    name,
                    argument_value,
                    flags,
                )
            }
            None => unsafe {
                let name = name.into_native("name")?;
                let argument_value = argument_value.into_native("argument_value")?;
                fs_native::destack_fs_fsetxattr_bytes(
                    self.call_context,
                    handle,
                    name,
                    argument_value,
                    flags,
                )
            },
        }
    }

    /// Read an extended attribute by path.
    ///
    /// Transfer bytes directly between caller buffers and host descriptors using short I/O semantics.
    /// Partial transfers are preserved exactly as reported by the host, and callers must loop when full completion is required.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the kernel feature is unavailable.
    /// Uses getxattr(2) on Unix and extended-attribute APIs where available on Windows.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `fs.xattr`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_fs_getxattr(
        &mut self,
        path: HarnessValue<OsPath, OsPathVm>,
        name: HarnessValue<NativeStringRef, vm::StringHandle>,
    ) -> RuntimeResult<HarnessValue<NativeArray<u8>, VmArray<u8>>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let path = path.into_vm("path")?;
                let name = name.into_vm("name")?;
                let out = fs_vm::destack_fs_getxattr(self.call_context, context, path, name)?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let path = path.into_native("path")?;
                let name = name.into_native("name")?;
                let mut out = std::mem::MaybeUninit::<NativeArray<u8>>::uninit();
                unsafe {
                    fs_native::destack_fs_getxattr(
                        self.call_context,
                        out.as_mut_ptr(),
                        path,
                        name,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Read an extended attribute by path with a raw name payload.
    ///
    /// Transfer bytes directly between caller buffers and host descriptors using short I/O semantics.
    /// Raw name bytes preserve host namespace data without UTF transcoding.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the kernel feature is unavailable.
    /// Uses getxattr(2) on Unix and extended-attribute APIs where available on Windows.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `fs.xattr`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_fs_getxattr_bytes(
        &mut self,
        path: HarnessValue<OsPath, OsPathVm>,
        name: HarnessValue<NativeSlice<u8>, VmSlice<u8>>,
    ) -> RuntimeResult<HarnessValue<NativeArray<u8>, VmArray<u8>>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let path = path.into_vm("path")?;
                let name = name.into_vm("name")?;
                let out = fs_vm::destack_fs_getxattr_bytes(self.call_context, context, path, name)?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let path = path.into_native("path")?;
                let name = name.into_native("name")?;
                let mut out = std::mem::MaybeUninit::<NativeArray<u8>>::uninit();
                unsafe {
                    fs_native::destack_fs_getxattr_bytes(
                        self.call_context,
                        out.as_mut_ptr(),
                        path,
                        name,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Read an extended attribute without following symlinks.
    ///
    /// Transfer bytes directly between caller buffers and host descriptors using short I/O semantics.
    /// Partial transfers are preserved exactly as reported by the host, and callers must loop when full completion is required.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the kernel feature is unavailable.
    /// Uses lgetxattr(2) on Unix and reparse-aware xattr query where available on Windows.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `fs.xattr`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_fs_lgetxattr(
        &mut self,
        path: HarnessValue<OsPath, OsPathVm>,
        name: HarnessValue<NativeStringRef, vm::StringHandle>,
    ) -> RuntimeResult<HarnessValue<NativeArray<u8>, VmArray<u8>>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let path = path.into_vm("path")?;
                let name = name.into_vm("name")?;
                let out = fs_vm::destack_fs_lgetxattr(self.call_context, context, path, name)?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let path = path.into_native("path")?;
                let name = name.into_native("name")?;
                let mut out = std::mem::MaybeUninit::<NativeArray<u8>>::uninit();
                unsafe {
                    fs_native::destack_fs_lgetxattr(
                        self.call_context,
                        out.as_mut_ptr(),
                        path,
                        name,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Read an extended attribute without following symlinks, using a raw name payload.
    ///
    /// Transfer bytes directly between caller buffers and host descriptors using short I/O semantics.
    /// Raw name bytes preserve host namespace data without UTF transcoding.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the kernel feature is unavailable.
    /// Uses lgetxattr(2) on Unix and reparse-aware xattr query where available on Windows.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `fs.xattr`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_fs_lgetxattr_bytes(
        &mut self,
        path: HarnessValue<OsPath, OsPathVm>,
        name: HarnessValue<NativeSlice<u8>, VmSlice<u8>>,
    ) -> RuntimeResult<HarnessValue<NativeArray<u8>, VmArray<u8>>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let path = path.into_vm("path")?;
                let name = name.into_vm("name")?;
                let out =
                    fs_vm::destack_fs_lgetxattr_bytes(self.call_context, context, path, name)?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let path = path.into_native("path")?;
                let name = name.into_native("name")?;
                let mut out = std::mem::MaybeUninit::<NativeArray<u8>>::uninit();
                unsafe {
                    fs_native::destack_fs_lgetxattr_bytes(
                        self.call_context,
                        out.as_mut_ptr(),
                        path,
                        name,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// List extended attribute names by path.
    ///
    /// List extended attribute names by path via host kernel APIs.
    /// Paths are forwarded from `OsPath` without runtime normalization or canonicalization, and permission checks follow host filesystem rules.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the kernel feature is unavailable.
    /// Uses listxattr(2) on Unix and xattr enumeration APIs where available on Windows.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `fs.xattr`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_fs_listxattr(
        &mut self,
        path: HarnessValue<OsPath, OsPathVm>,
    ) -> RuntimeResult<HarnessValue<NativeArray<NativeStringRef>, VmArray<vm::StringHandle>>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let path = path.into_vm("path")?;
                let out = fs_vm::destack_fs_listxattr(self.call_context, context, path)?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let path = path.into_native("path")?;
                let mut out = std::mem::MaybeUninit::<NativeArray<NativeStringRef>>::uninit();
                unsafe {
                    fs_native::destack_fs_listxattr(self.call_context, out.as_mut_ptr(), path)?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// List extended attribute names by path as raw byte payloads.
    ///
    /// List extended attribute names by path via host kernel APIs.
    /// Raw name bytes preserve host namespace data without UTF transcoding.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the kernel feature is unavailable.
    /// Uses listxattr(2) on Unix and xattr enumeration APIs where available on Windows.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `fs.xattr`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_fs_listxattr_bytes(
        &mut self,
        path: HarnessValue<OsPath, OsPathVm>,
    ) -> RuntimeResult<HarnessValue<NativeArray<NativeArray<u8>>, VmArray<VmArray<u8>>>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let path = path.into_vm("path")?;
                let out = fs_vm::destack_fs_listxattr_bytes(self.call_context, context, path)?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let path = path.into_native("path")?;
                let mut out = std::mem::MaybeUninit::<NativeArray<NativeArray<u8>>>::uninit();
                unsafe {
                    fs_native::destack_fs_listxattr_bytes(
                        self.call_context,
                        out.as_mut_ptr(),
                        path,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// List extended attribute names without following symlinks.
    ///
    /// List extended attribute names without following symlinks via host kernel APIs.
    /// Paths are forwarded from `OsPath` without runtime normalization or canonicalization, and permission checks follow host filesystem rules.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the kernel feature is unavailable.
    /// Uses llistxattr(2) on Unix and reparse-aware xattr enumeration on Windows.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `fs.xattr`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_fs_llistxattr(
        &mut self,
        path: HarnessValue<OsPath, OsPathVm>,
    ) -> RuntimeResult<HarnessValue<NativeArray<NativeStringRef>, VmArray<vm::StringHandle>>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let path = path.into_vm("path")?;
                let out = fs_vm::destack_fs_llistxattr(self.call_context, context, path)?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let path = path.into_native("path")?;
                let mut out = std::mem::MaybeUninit::<NativeArray<NativeStringRef>>::uninit();
                unsafe {
                    fs_native::destack_fs_llistxattr(self.call_context, out.as_mut_ptr(), path)?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// List extended attribute names without following symlinks as raw byte payloads.
    ///
    /// List extended attribute names without following symlinks via host kernel APIs.
    /// Raw name bytes preserve host namespace data without UTF transcoding.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the kernel feature is unavailable.
    /// Uses llistxattr(2) on Unix and reparse-aware xattr enumeration on Windows.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `fs.xattr`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_fs_llistxattr_bytes(
        &mut self,
        path: HarnessValue<OsPath, OsPathVm>,
    ) -> RuntimeResult<HarnessValue<NativeArray<NativeArray<u8>>, VmArray<VmArray<u8>>>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let path = path.into_vm("path")?;
                let out = fs_vm::destack_fs_llistxattr_bytes(self.call_context, context, path)?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let path = path.into_native("path")?;
                let mut out = std::mem::MaybeUninit::<NativeArray<NativeArray<u8>>>::uninit();
                unsafe {
                    fs_native::destack_fs_llistxattr_bytes(
                        self.call_context,
                        out.as_mut_ptr(),
                        path,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Remove an extended attribute without following symlinks.
    ///
    /// Remove the target resource through a single host namespace operation with no runtime fallback path.
    /// Paths are forwarded from `OsPath` without runtime normalization or canonicalization, and permission checks follow host filesystem rules.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the kernel feature is unavailable.
    /// Uses lremovexattr(2) on Unix and reparse-aware xattr delete on Windows.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `fs.xattr`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_fs_lremovexattr(
        &mut self,
        path: HarnessValue<OsPath, OsPathVm>,
        name: HarnessValue<NativeStringRef, vm::StringHandle>,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let path = path.into_vm("path")?;
                let name = name.into_vm("name")?;
                fs_vm::destack_fs_lremovexattr(self.call_context, context, path, name)
            }
            None => unsafe {
                let path = path.into_native("path")?;
                let name = name.into_native("name")?;
                fs_native::destack_fs_lremovexattr(self.call_context, path, name)
            },
        }
    }

    /// Remove an extended attribute without following symlinks, using a raw name payload.
    ///
    /// Remove the target resource through a single host namespace operation with no runtime fallback path.
    /// Raw name bytes preserve host namespace data without UTF transcoding.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the kernel feature is unavailable.
    /// Uses lremovexattr(2) on Unix and reparse-aware xattr delete on Windows.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `fs.xattr`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_fs_lremovexattr_bytes(
        &mut self,
        path: HarnessValue<OsPath, OsPathVm>,
        name: HarnessValue<NativeSlice<u8>, VmSlice<u8>>,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let path = path.into_vm("path")?;
                let name = name.into_vm("name")?;
                fs_vm::destack_fs_lremovexattr_bytes(self.call_context, context, path, name)
            }
            None => unsafe {
                let path = path.into_native("path")?;
                let name = name.into_native("name")?;
                fs_native::destack_fs_lremovexattr_bytes(self.call_context, path, name)
            },
        }
    }

    /// Set an extended attribute without following symlinks.
    ///
    /// Set the requested control value on the descriptor through the native option interface.
    /// Paths are forwarded from `OsPath` without runtime normalization or canonicalization, and permission checks follow host filesystem rules.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the kernel feature is unavailable.
    /// Uses lsetxattr(2) on Unix and reparse-aware xattr write where available on Windows.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `fs.xattr`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_fs_lsetxattr(
        &mut self,
        path: HarnessValue<OsPath, OsPathVm>,
        name: HarnessValue<NativeStringRef, vm::StringHandle>,
        argument_value: HarnessValue<NativeSlice<u8>, VmSlice<u8>>,
        flags: XattrFlags,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let path = path.into_vm("path")?;
                let name = name.into_vm("name")?;
                let argument_value = argument_value.into_vm("argument_value")?;
                fs_vm::destack_fs_lsetxattr(
                    self.call_context,
                    context,
                    path,
                    name,
                    argument_value,
                    flags,
                )
            }
            None => unsafe {
                let path = path.into_native("path")?;
                let name = name.into_native("name")?;
                let argument_value = argument_value.into_native("argument_value")?;
                fs_native::destack_fs_lsetxattr(
                    self.call_context,
                    path,
                    name,
                    argument_value,
                    flags,
                )
            },
        }
    }

    /// Set an extended attribute without following symlinks, using a raw name payload.
    ///
    /// Set the requested control value on the descriptor through the native option interface.
    /// Raw name bytes preserve host namespace data without UTF transcoding.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the kernel feature is unavailable.
    /// Uses lsetxattr(2) on Unix and reparse-aware xattr write where available on Windows.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `fs.xattr`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_fs_lsetxattr_bytes(
        &mut self,
        path: HarnessValue<OsPath, OsPathVm>,
        name: HarnessValue<NativeSlice<u8>, VmSlice<u8>>,
        argument_value: HarnessValue<NativeSlice<u8>, VmSlice<u8>>,
        flags: XattrFlags,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let path = path.into_vm("path")?;
                let name = name.into_vm("name")?;
                let argument_value = argument_value.into_vm("argument_value")?;
                fs_vm::destack_fs_lsetxattr_bytes(
                    self.call_context,
                    context,
                    path,
                    name,
                    argument_value,
                    flags,
                )
            }
            None => unsafe {
                let path = path.into_native("path")?;
                let name = name.into_native("name")?;
                let argument_value = argument_value.into_native("argument_value")?;
                fs_native::destack_fs_lsetxattr_bytes(
                    self.call_context,
                    path,
                    name,
                    argument_value,
                    flags,
                )
            },
        }
    }

    /// Remove an extended attribute by path.
    ///
    /// Remove the target resource through a single host namespace operation with no runtime fallback path.
    /// Paths are forwarded from `OsPath` without runtime normalization or canonicalization, and permission checks follow host filesystem rules.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the kernel feature is unavailable.
    /// Uses removexattr(2) on Unix and xattr delete APIs where available on Windows.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `fs.xattr`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_fs_removexattr(
        &mut self,
        path: HarnessValue<OsPath, OsPathVm>,
        name: HarnessValue<NativeStringRef, vm::StringHandle>,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let path = path.into_vm("path")?;
                let name = name.into_vm("name")?;
                fs_vm::destack_fs_removexattr(self.call_context, context, path, name)
            }
            None => unsafe {
                let path = path.into_native("path")?;
                let name = name.into_native("name")?;
                fs_native::destack_fs_removexattr(self.call_context, path, name)
            },
        }
    }

    /// Remove an extended attribute by path with a raw name payload.
    ///
    /// Remove the target resource through a single host namespace operation with no runtime fallback path.
    /// Raw name bytes preserve host namespace data without UTF transcoding.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the kernel feature is unavailable.
    /// Uses removexattr(2) on Unix and xattr delete APIs where available on Windows.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `fs.xattr`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_fs_removexattr_bytes(
        &mut self,
        path: HarnessValue<OsPath, OsPathVm>,
        name: HarnessValue<NativeSlice<u8>, VmSlice<u8>>,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let path = path.into_vm("path")?;
                let name = name.into_vm("name")?;
                fs_vm::destack_fs_removexattr_bytes(self.call_context, context, path, name)
            }
            None => unsafe {
                let path = path.into_native("path")?;
                let name = name.into_native("name")?;
                fs_native::destack_fs_removexattr_bytes(self.call_context, path, name)
            },
        }
    }

    /// Set an extended attribute by path.
    ///
    /// Set the requested control value on the descriptor through the native option interface.
    /// Paths are forwarded from `OsPath` without runtime normalization or canonicalization, and permission checks follow host filesystem rules.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the kernel feature is unavailable.
    /// Uses setxattr(2) on Unix and extended-attribute APIs where available on Windows.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `fs.xattr`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_fs_setxattr(
        &mut self,
        path: HarnessValue<OsPath, OsPathVm>,
        name: HarnessValue<NativeStringRef, vm::StringHandle>,
        argument_value: HarnessValue<NativeSlice<u8>, VmSlice<u8>>,
        flags: XattrFlags,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let path = path.into_vm("path")?;
                let name = name.into_vm("name")?;
                let argument_value = argument_value.into_vm("argument_value")?;
                fs_vm::destack_fs_setxattr(
                    self.call_context,
                    context,
                    path,
                    name,
                    argument_value,
                    flags,
                )
            }
            None => unsafe {
                let path = path.into_native("path")?;
                let name = name.into_native("name")?;
                let argument_value = argument_value.into_native("argument_value")?;
                fs_native::destack_fs_setxattr(self.call_context, path, name, argument_value, flags)
            },
        }
    }

    /// Set an extended attribute by path with a raw name payload.
    ///
    /// Set the requested control value on the descriptor through the native option interface.
    /// Raw name bytes preserve host namespace data without UTF transcoding.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the kernel feature is unavailable.
    /// Uses setxattr(2) on Unix and extended-attribute APIs where available on Windows.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `fs.xattr`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_fs_setxattr_bytes(
        &mut self,
        path: HarnessValue<OsPath, OsPathVm>,
        name: HarnessValue<NativeSlice<u8>, VmSlice<u8>>,
        argument_value: HarnessValue<NativeSlice<u8>, VmSlice<u8>>,
        flags: XattrFlags,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let path = path.into_vm("path")?;
                let name = name.into_vm("name")?;
                let argument_value = argument_value.into_vm("argument_value")?;
                fs_vm::destack_fs_setxattr_bytes(
                    self.call_context,
                    context,
                    path,
                    name,
                    argument_value,
                    flags,
                )
            }
            None => unsafe {
                let path = path.into_native("path")?;
                let name = name.into_native("name")?;
                let argument_value = argument_value.into_native("argument_value")?;
                fs_native::destack_fs_setxattr_bytes(
                    self.call_context,
                    path,
                    name,
                    argument_value,
                    flags,
                )
            },
        }
    }
}

/// Wrapper that carries one native or VM value for generated harness bindings.
pub(crate) enum HarnessValue<Native, Vm> {
    /// Native value variant.
    Native(Native),
    /// VM value variant.
    Vm(Vm),
}

impl<Native: Clone, Vm: Clone> Clone for HarnessValue<Native, Vm> {
    fn clone(&self) -> Self {
        match self {
            HarnessValue::Native(value) => HarnessValue::Native(value.clone()),
            HarnessValue::Vm(value) => HarnessValue::Vm(value.clone()),
        }
    }
}

impl<Native, Vm> HarnessValue<Native, Vm> {
    /// Convert one wrapper into one native value.
    fn into_native(self, label: &str) -> RuntimeResult<Native> {
        match self {
            HarnessValue::Native(value) => Ok(value),
            HarnessValue::Vm(_) => Err(RuntimeError::from(
                HarnessPlatformError::invalid_argument_value(
                    label,
                    "expected native harness value",
                ),
            )
            .boxed()),
        }
    }

    /// Convert one wrapper into one VM value.
    fn into_vm(self, label: &str) -> RuntimeResult<Vm> {
        match self {
            HarnessValue::Vm(value) => Ok(value),
            HarnessValue::Native(_) => Err(RuntimeError::from(
                HarnessPlatformError::invalid_argument_value(label, "expected VM harness value"),
            )
            .boxed()),
        }
    }
}
