// generated by generate-bindings: test harness, do not edit

#![allow(dead_code)]
#![allow(unused_imports)]
#![allow(clippy::type_complexity)]

use crate::diagnostic::{RuntimeError, RuntimeResult};
use crate::platform::audio::tests::AudioHarnessContext;
use crate::platform::audio::{
    AudioBackend, AudioBackendCapabilityFlags, AudioBackendDescriptor, AudioBackendDescriptorVm,
    AudioBackendDisconnectedEvent, AudioBackendDisconnectedEventVm, AudioBackendResetEvent,
    AudioBackendResetEventVm, AudioBackendSelectionPolicy, AudioChannelLayout, AudioClockDomain,
    AudioClockQuality, AudioClockSnapshot, AudioClockSnapshotVm, AudioDefaultCaptureChangedEvent,
    AudioDefaultCaptureChangedEventVm, AudioDefaultLoopbackChangedEvent,
    AudioDefaultLoopbackChangedEventVm, AudioDefaultPlaybackChangedEvent,
    AudioDefaultPlaybackChangedEventVm, AudioDeviceAddedEvent, AudioDeviceAddedEventVm,
    AudioDeviceCapabilityFlags, AudioDeviceDescriptor, AudioDeviceDescriptorVm,
    AudioDeviceDirection, AudioDeviceFormatChangedEvent, AudioDeviceFormatChangedEventVm,
    AudioDeviceListFlags, AudioDeviceListRequest, AudioDeviceListRequestVm, AudioDeviceOpenFlags,
    AudioDeviceOpenOptions, AudioDeviceOpenOptionsVm, AudioDeviceRemovedEvent,
    AudioDeviceRemovedEventVm, AudioDeviceReroutedEvent, AudioDeviceReroutedEventVm, AudioEvent,
    AudioEventDeliveryMode, AudioEventMetadata, AudioEventMetadataVm, AudioEventOverflowPolicy,
    AudioEventSource, AudioEventSubscriptionFlags, AudioEventSubscriptionOptions,
    AudioEventSubscriptionOptionsVm, AudioEventVm, AudioInterruptionBeganEvent,
    AudioInterruptionBeganEventVm, AudioInterruptionEndedEvent, AudioInterruptionEndedEventVm,
    AudioSampleFormat, AudioShareMode, AudioStreamAvailability, AudioStreamAvailabilityVm,
    AudioStreamClockDomain, AudioStreamConfig, AudioStreamConfigVm, AudioStreamDescriptor,
    AudioStreamDescriptorVm, AudioStreamDeviceChangedEvent, AudioStreamDeviceChangedEventVm,
    AudioStreamFlags, AudioStreamOpenOptions, AudioStreamOpenOptionsVm,
    AudioStreamRequirementFlags, AudioStreamState, AudioStreamStateChangedEvent,
    AudioStreamStateChangedEventVm, AudioStreamStateKind, AudioStreamStateVm,
    AudioStreamStatusFlags, AudioStreamSupport, AudioStreamSupportVm, AudioStreamTiming,
    AudioStreamTimingVm, AudioStreamTransferMode, AudioStreamXRunEvent, AudioStreamXRunEventVm,
    AudioSupportedEventSubscriptionFlags, AudioSupportedStreamClockDomains,
    AudioSupportedStreamFlags, AudioSupportedStreamRequirementFlags, native as audio_native,
    vm as audio_vm,
};
use crate::platform::{
    NativeAbiCodec, NativeArray, NativeSlice, NativeStringRef, NativeStringSlice,
    PlatformError as HarnessPlatformError, VmAbiCodec, VmArray, VmSlice, core, fs, resource,
};
use destack_vm as vm;

impl<'call> AudioHarnessContext<'call> {
    /// Return the generated VM context if available.
    #[allow(clippy::mut_from_ref)]
    fn generated_vm_context_mut(&self) -> Option<&mut vm::ExternalCallContext<'_>> {
        self.vm_context
            .map(|context| unsafe { &mut *(context as *mut vm::ExternalCallContext<'_>) })
    }

    /// Return one standardized value payload for native and VM variants.
    pub(crate) fn harness_value<Native, Vm>(&self, native: Native) -> HarnessValue<Native, Vm> {
        HarnessValue::Native(native)
    }

    /// Return one standardized value payload for VM and native variants.
    pub(crate) fn harness_value_vm<Native, Vm>(&self, vm: Vm) -> HarnessValue<Native, Vm> {
        HarnessValue::Vm(vm)
    }

    /// Encode one materialized value into one native or VM harness payload.
    pub(crate) fn harness_value_from<Native, Vm>(
        &mut self,
        value: Native::Value,
    ) -> RuntimeResult<HarnessValue<Native, Vm>>
    where
        Native: NativeAbiCodec,
        Vm: VmAbiCodec<Value = Native::Value>,
    {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let context = &mut context.write();
                let value = Vm::from_value(context, value)?;
                Ok(HarnessValue::Vm(value))
            }
            None => {
                let value = Native::from_value(self.call_context, value);
                Ok(HarnessValue::Native(value))
            }
        }
    }

    /// Decode one native or VM harness payload into one materialized value.
    pub(crate) fn harness_value_into<Native, Vm>(
        &mut self,
        value: HarnessValue<Native, Vm>,
    ) -> RuntimeResult<Native::Value>
    where
        Native: NativeAbiCodec,
        Vm: VmAbiCodec<Value = Native::Value>,
    {
        match value {
            HarnessValue::Native(value) => unsafe { Native::into_value(value) },
            HarnessValue::Vm(value) => {
                let context = self.generated_vm_context_mut().ok_or_else(|| {
                    RuntimeError::from(HarnessPlatformError::invalid_argument_value(
                        "context",
                        "expected vm context",
                    ))
                    .boxed()
                })?;
                let context = &context.read();
                value.into_value(context)
            }
        }
    }

    /// List host audio backends.
    ///
    /// Enumerate backend selectors, support state, and backend-level feature flags.
    ///
    /// # Platform
    /// Unix and Windows.
    /// Mirrors cubeb `cubeb_get_backend_names`, libsoundio `soundio_backend_count` plus `soundio_get_backend`, and miniaudio `ma_get_enabled_backends`.
    /// Mirrors PortAudio host-api enumeration through `PaHostApiTypeId`.
    ///
    /// # Errors
    /// Returns ioNotFound, ioInvalidData, notSupported.
    ///
    /// # Security
    /// Requires `audio.device`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_audio_backend_list(
        &mut self,
    ) -> RuntimeResult<
        HarnessValue<NativeSlice<AudioBackendDescriptor>, VmSlice<AudioBackendDescriptorVm>>,
    > {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = audio_vm::destack_audio_backend_list(self.call_context, context)?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let mut out =
                    std::mem::MaybeUninit::<NativeSlice<AudioBackendDescriptor>>::uninit();
                unsafe {
                    audio_native::destack_audio_backend_list(self.call_context, out.as_mut_ptr())?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Read one timestamp in one selected clock domain.
    ///
    /// Read one clock timestamp for one process-wide domain.
    /// Domain availability and precision follow host platform behavior.
    ///
    /// # Platform
    /// Unix and Windows.
    /// Mirrors host monotonic and wall clock query semantics.
    ///
    /// # Errors
    /// Returns invalidArgument, ioNotFound, ioInvalidData, notSupported.
    ///
    /// # Security
    /// Requires `audio.control`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_audio_clock_now(
        &mut self,
        domain: AudioClockDomain,
    ) -> RuntimeResult<u64> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = audio_vm::destack_audio_clock_now(self.call_context, context, domain)?;
                Ok(out)
            }
            None => {
                let mut out = std::mem::MaybeUninit::<u64>::uninit();
                unsafe {
                    audio_native::destack_audio_clock_now(
                        self.call_context,
                        out.as_mut_ptr(),
                        domain,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// Read one stream clock snapshot.
    ///
    /// Read one synchronized stream-position and selected clock-domain timestamp snapshot.
    /// Snapshot values are advisory and can change immediately after read.
    /// This is the strict lane-select API.
    /// For one full best-effort snapshot without lane-specific errors use `audio.stream.timing`.
    /// Domain-specific lanes like `InputAdc`, `OutputDac`, and `Device` can return `notSupported` when the opened stream does not expose them.
    ///
    /// # Platform
    /// Unix and Windows.
    /// Mirrors PortAudio `PaStreamCallbackTimeInfo` input and output timestamp correlation.
    /// Mirrors ASIO `bufferSwitchTimeInfo` and time-info correlation semantics.
    /// Mirrors cubeb `cubeb_stream_get_position` plus latency-correlation snapshots.
    ///
    /// # Errors
    /// Returns invalidArgument, ioNotFound, ioInvalidData, notSupported.
    ///
    /// # Security
    /// Requires `audio.control`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_audio_stream_clock(
        &mut self,
        handle: resource::AudioStreamHandle,
        domain: AudioStreamClockDomain,
    ) -> RuntimeResult<HarnessValue<AudioClockSnapshot, AudioClockSnapshotVm>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = audio_vm::destack_audio_stream_clock(
                    self.call_context,
                    context,
                    handle,
                    domain,
                )?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let mut out = std::mem::MaybeUninit::<AudioClockSnapshot>::uninit();
                unsafe {
                    audio_native::destack_audio_stream_clock(
                        self.call_context,
                        out.as_mut_ptr(),
                        handle,
                        domain,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Close one audio device endpoint.
    ///
    /// Close one opened audio endpoint and release host resources.
    /// Close semantics follow host backend teardown behavior.
    ///
    /// # Platform
    /// Unix and Windows.
    /// Uses ALSA, PulseAudio, PipeWire, CoreAudio, WASAPI, AAudio, OpenSL ES, JACK, and ASIO where available endpoint close operations.
    ///
    /// # Errors
    /// Returns invalidArgument, ioNotFound, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `audio.device`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_audio_device_close(
        &mut self,
        handle: resource::AudioDeviceHandle,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                audio_vm::destack_audio_device_close(self.call_context, context, handle)
            }
            None => unsafe { audio_native::destack_audio_device_close(self.call_context, handle) },
        }
    }

    /// Read one default device identifier for the selected direction.
    ///
    /// Resolve one default host audio endpoint for the selected direction and backend policy.
    /// Default selection can change asynchronously as host policy changes.
    ///
    /// # Platform
    /// Unix and Windows.
    /// Mirrors cubeb and libsoundio default-endpoint query semantics.
    /// Mirrors SDL default logical-device routing behavior for playback and recording defaults.
    ///
    /// # Errors
    /// Returns invalidArgument, ioNotFound, ioInvalidData, notSupported.
    ///
    /// # Security
    /// Requires `audio.device`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_audio_device_default(
        &mut self,
        direction: AudioDeviceDirection,
        backend: AudioBackend,
        backendpolicy: AudioBackendSelectionPolicy,
    ) -> RuntimeResult<HarnessValue<NativeStringRef, vm::StringHandle>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = audio_vm::destack_audio_device_default(
                    self.call_context,
                    context,
                    direction,
                    backend,
                    backendpolicy,
                )?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let mut out = std::mem::MaybeUninit::<NativeStringRef>::uninit();
                unsafe {
                    audio_native::destack_audio_device_default(
                        self.call_context,
                        out.as_mut_ptr(),
                        direction,
                        backend,
                        backendpolicy,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Read metadata for one opened device endpoint.
    ///
    /// Read one normalized snapshot for one opened device handle.
    /// Snapshot values are advisory and can change as host routes are updated.
    ///
    /// # Platform
    /// Unix and Windows.
    /// Uses ALSA, PulseAudio, PipeWire, CoreAudio, WASAPI, AAudio, OpenSL ES, JACK, and ASIO where available endpoint information query APIs.
    ///
    /// # Errors
    /// Returns invalidArgument, ioNotFound, ioInvalidData, notSupported.
    ///
    /// # Security
    /// Requires `audio.device`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_audio_device_descriptor(
        &mut self,
        handle: resource::AudioDeviceHandle,
    ) -> RuntimeResult<HarnessValue<AudioDeviceDescriptor, AudioDeviceDescriptorVm>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out =
                    audio_vm::destack_audio_device_descriptor(self.call_context, context, handle)?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let mut out = std::mem::MaybeUninit::<AudioDeviceDescriptor>::uninit();
                unsafe {
                    audio_native::destack_audio_device_descriptor(
                        self.call_context,
                        out.as_mut_ptr(),
                        handle,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// List available audio devices.
    ///
    /// Enumerate host audio endpoints and return stable identifiers for later open operations.
    /// Device visibility and ordering follow host backend semantics.
    ///
    /// # Platform
    /// Unix and Windows.
    /// Uses ALSA, PulseAudio, PipeWire, CoreAudio, WASAPI, AAudio, OpenSL ES, JACK, and ASIO where available device enumeration.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, notSupported.
    ///
    /// # Security
    /// Requires `audio.device`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_audio_device_list(
        &mut self,
        request: HarnessValue<AudioDeviceListRequest, AudioDeviceListRequestVm>,
    ) -> RuntimeResult<
        HarnessValue<NativeSlice<AudioDeviceDescriptor>, VmSlice<AudioDeviceDescriptorVm>>,
    > {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let request = request.into_vm("request")?;
                let out = audio_vm::destack_audio_device_list(self.call_context, context, request)?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let request = request.into_native("request")?;
                let mut out = std::mem::MaybeUninit::<NativeSlice<AudioDeviceDescriptor>>::uninit();
                unsafe {
                    audio_native::destack_audio_device_list(
                        self.call_context,
                        out.as_mut_ptr(),
                        request,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Open one audio device endpoint.
    ///
    /// Open one host audio endpoint for playback, capture, duplex, or loopback operation.
    /// Handle lifetime and exclusivity semantics follow host backend rules.
    ///
    /// # Platform
    /// Unix and Windows.
    /// Uses ALSA, PulseAudio, PipeWire, CoreAudio, WASAPI, AAudio, OpenSL ES, JACK, and ASIO where available endpoint open operations.
    ///
    /// # Errors
    /// Returns invalidArgument, ioNotFound, ioPermissionDenied, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `audio.device`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_audio_device_open(
        &mut self,
        id: HarnessValue<NativeStringRef, vm::StringHandle>,
        options: HarnessValue<AudioDeviceOpenOptions, AudioDeviceOpenOptionsVm>,
    ) -> RuntimeResult<resource::AudioDeviceHandle> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let id = id.into_vm("id")?;
                let options = options.into_vm("options")?;
                let out =
                    audio_vm::destack_audio_device_open(self.call_context, context, id, options)?;
                Ok(out)
            }
            None => {
                let id = id.into_native("id")?;
                let options = options.into_native("options")?;
                let mut out = std::mem::MaybeUninit::<resource::AudioDeviceHandle>::uninit();
                unsafe {
                    audio_native::destack_audio_device_open(
                        self.call_context,
                        out.as_mut_ptr(),
                        id,
                        options,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// Trigger one backend rescan.
    ///
    /// Request one immediate backend device rescan.
    /// This allows recovery from stale backend snapshots after hotplug churn.
    ///
    /// # Platform
    /// Unix and Windows.
    /// Mirrors libsoundio `soundio_force_device_scan` semantics and backend-native refresh flows.
    ///
    /// # Errors
    /// Returns ioNotFound, ioWouldBlock, ioInvalidData, notSupported.
    ///
    /// # Security
    /// Requires `audio.device.monitor`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_audio_device_rescan(
        &mut self,
        backend: AudioBackend,
        backendpolicy: AudioBackendSelectionPolicy,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => audio_vm::destack_audio_device_rescan(
                self.call_context,
                context,
                backend,
                backendpolicy,
            ),
            None => unsafe {
                audio_native::destack_audio_device_rescan(self.call_context, backend, backendpolicy)
            },
        }
    }

    /// Close one audio event subscription.
    ///
    /// Close one event subscription and release backend notification resources.
    /// Pending events are discarded.
    ///
    /// # Platform
    /// Unix and Windows.
    /// Uses ALSA, PulseAudio, PipeWire, CoreAudio, WASAPI, AAudio, OpenSL ES, JACK, and ASIO where available notification unregistration APIs.
    ///
    /// # Errors
    /// Returns invalidArgument, ioNotFound, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `audio.device.monitor`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_audio_event_close(
        &mut self,
        handle: resource::AudioEventHandle,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                audio_vm::destack_audio_event_close(self.call_context, context, handle)
            }
            None => unsafe { audio_native::destack_audio_event_close(self.call_context, handle) },
        }
    }

    /// Open one audio event subscription.
    ///
    /// Open one backend event subscription for device and optional stream events.
    /// Subscription routing and queue depth follow host backend behavior.
    ///
    /// # Platform
    /// Unix and Windows.
    /// Mirrors cubeb device and stream change callbacks.
    /// Mirrors libsoundio device-change and backend-disconnect callback families.
    /// Mirrors miniaudio `ma_device_notification_proc` notification routing.
    ///
    /// # Errors
    /// Returns invalidArgument, ioNotFound, ioPermissionDenied, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `audio.device.monitor`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_audio_event_open(
        &mut self,
        options: HarnessValue<AudioEventSubscriptionOptions, AudioEventSubscriptionOptionsVm>,
    ) -> RuntimeResult<resource::AudioEventHandle> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let options = options.into_vm("options")?;
                let out = audio_vm::destack_audio_event_open(self.call_context, context, options)?;
                Ok(out)
            }
            None => {
                let options = options.into_native("options")?;
                let mut out = std::mem::MaybeUninit::<resource::AudioEventHandle>::uninit();
                unsafe {
                    audio_native::destack_audio_event_open(
                        self.call_context,
                        out.as_mut_ptr(),
                        options,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// Wait for one audio event.
    ///
    /// Wait for one pending event from one subscription queue.
    /// Timeout uses nanoseconds in the runtime monotonic domain.
    ///
    /// # Platform
    /// Unix and Windows.
    /// Uses ALSA, PulseAudio, PipeWire, CoreAudio, WASAPI, AAudio, OpenSL ES, JACK, and ASIO where available event wait or callback-queue drain operations.
    ///
    /// # Errors
    /// Returns invalidArgument, ioNotFound, ioInterrupted, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `audio.device.monitor`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_audio_event_read(
        &mut self,
        handle: resource::AudioEventHandle,
        timeoutns: u64,
    ) -> RuntimeResult<HarnessValue<AudioEvent, AudioEventVm>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = audio_vm::destack_audio_event_read(
                    self.call_context,
                    context,
                    handle,
                    timeoutns,
                )?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let mut out = std::mem::MaybeUninit::<AudioEvent>::uninit();
                unsafe {
                    audio_native::destack_audio_event_read(
                        self.call_context,
                        out.as_mut_ptr(),
                        handle,
                        timeoutns,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Wait for one batch of audio events.
    ///
    /// Wait for pending events from one subscription queue and return up to `maxEvents` events.
    /// Timeout uses nanoseconds in the runtime monotonic domain.
    /// Empty queue state is reported through ioWouldBlock.
    ///
    /// # Platform
    /// Unix and Windows.
    /// Uses ALSA, PulseAudio, PipeWire, CoreAudio, WASAPI, AAudio, OpenSL ES, JACK, and ASIO queue-drain operations where available.
    ///
    /// # Errors
    /// Returns invalidArgument, ioNotFound, ioInterrupted, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `audio.device.monitor`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_audio_event_read_batch(
        &mut self,
        handle: resource::AudioEventHandle,
        maxevents: u32,
        timeoutns: u64,
    ) -> RuntimeResult<HarnessValue<NativeSlice<AudioEvent>, VmSlice<AudioEventVm>>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = audio_vm::destack_audio_event_read_batch(
                    self.call_context,
                    context,
                    handle,
                    maxevents,
                    timeoutns,
                )?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let mut out = std::mem::MaybeUninit::<NativeSlice<AudioEvent>>::uninit();
                unsafe {
                    audio_native::destack_audio_event_read_batch(
                        self.call_context,
                        out.as_mut_ptr(),
                        handle,
                        maxevents,
                        timeoutns,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Poll one audio event without blocking.
    ///
    /// Poll one pending event from one subscription queue.
    /// Empty queue state is reported through ioWouldBlock.
    ///
    /// # Platform
    /// Unix and Windows.
    /// Uses ALSA, PulseAudio, PipeWire, CoreAudio, WASAPI, AAudio, OpenSL ES, JACK, and ASIO where available nonblocking event queue reads.
    ///
    /// # Errors
    /// Returns invalidArgument, ioNotFound, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `audio.device.monitor`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_audio_event_try_read(
        &mut self,
        handle: resource::AudioEventHandle,
    ) -> RuntimeResult<HarnessValue<AudioEvent, AudioEventVm>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out =
                    audio_vm::destack_audio_event_try_read(self.call_context, context, handle)?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let mut out = std::mem::MaybeUninit::<AudioEvent>::uninit();
                unsafe {
                    audio_native::destack_audio_event_try_read(
                        self.call_context,
                        out.as_mut_ptr(),
                        handle,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Poll one batch of audio events without blocking.
    ///
    /// Poll pending events from one subscription queue and return up to `maxEvents` events.
    /// Empty queue state is reported through ioWouldBlock.
    ///
    /// # Platform
    /// Unix and Windows.
    /// Uses ALSA, PulseAudio, PipeWire, CoreAudio, WASAPI, AAudio, OpenSL ES, JACK, and ASIO nonblocking queue-drain operations where available.
    ///
    /// # Errors
    /// Returns invalidArgument, ioNotFound, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `audio.device.monitor`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_audio_event_try_read_batch(
        &mut self,
        handle: resource::AudioEventHandle,
        maxevents: u32,
    ) -> RuntimeResult<HarnessValue<NativeSlice<AudioEvent>, VmSlice<AudioEventVm>>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = audio_vm::destack_audio_event_try_read_batch(
                    self.call_context,
                    context,
                    handle,
                    maxevents,
                )?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let mut out = std::mem::MaybeUninit::<NativeSlice<AudioEvent>>::uninit();
                unsafe {
                    audio_native::destack_audio_event_try_read_batch(
                        self.call_context,
                        out.as_mut_ptr(),
                        handle,
                        maxevents,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Abort one audio stream immediately.
    ///
    /// Request one immediate stream stop without graceful drain.
    /// Pending buffered data can be discarded.
    ///
    /// # Platform
    /// Unix and Windows.
    /// Mirrors PortAudio `Pa_AbortStream` semantics.
    ///
    /// # Errors
    /// Returns invalidArgument, ioNotFound, ioInterrupted, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `audio.stream`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_audio_stream_abort(
        &mut self,
        handle: resource::AudioStreamHandle,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                audio_vm::destack_audio_stream_abort(self.call_context, context, handle)
            }
            None => unsafe { audio_native::destack_audio_stream_abort(self.call_context, handle) },
        }
    }

    /// Read one stream immediate availability sample.
    ///
    /// Read one point-in-time sample of immediately readable and writable frame counts.
    /// Values are advisory and can change immediately after read.
    ///
    /// # Platform
    /// Unix and Windows.
    /// Uses ALSA, PulseAudio, PipeWire, CoreAudio, WASAPI, AAudio, OpenSL ES, JACK, and ASIO where available stream-space query operations.
    ///
    /// # Errors
    /// Returns invalidArgument, ioNotFound, ioInvalidData, notSupported.
    ///
    /// # Security
    /// Requires `audio.control`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_audio_stream_availability(
        &mut self,
        handle: resource::AudioStreamHandle,
    ) -> RuntimeResult<HarnessValue<AudioStreamAvailability, AudioStreamAvailabilityVm>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = audio_vm::destack_audio_stream_availability(
                    self.call_context,
                    context,
                    handle,
                )?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let mut out = std::mem::MaybeUninit::<AudioStreamAvailability>::uninit();
                unsafe {
                    audio_native::destack_audio_stream_availability(
                        self.call_context,
                        out.as_mut_ptr(),
                        handle,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Close one audio stream.
    ///
    /// Close one host audio stream and release backend buffers and synchronization state.
    /// Stream handle becomes invalid after close completes.
    ///
    /// # Platform
    /// Unix and Windows.
    /// Uses ALSA, PulseAudio, PipeWire, CoreAudio, WASAPI, AAudio, OpenSL ES, JACK, and ASIO where available stream close operations.
    ///
    /// # Errors
    /// Returns invalidArgument, ioNotFound, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `audio.stream`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_audio_stream_close(
        &mut self,
        handle: resource::AudioStreamHandle,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                audio_vm::destack_audio_stream_close(self.call_context, context, handle)
            }
            None => unsafe { audio_native::destack_audio_stream_close(self.call_context, handle) },
        }
    }

    /// Read one stream negotiated configuration descriptor.
    ///
    /// Read one normalized view of negotiated stream parameters and backend mode.
    /// Values reflect backend negotiation outcomes and can differ from open-time requests.
    ///
    /// # Platform
    /// Unix and Windows.
    /// Uses ALSA, PulseAudio, PipeWire, CoreAudio, WASAPI, AAudio, OpenSL ES, JACK, and ASIO where available stream-parameter query operations.
    ///
    /// # Errors
    /// Returns invalidArgument, ioNotFound, ioInvalidData, notSupported.
    ///
    /// # Security
    /// Requires `audio.control`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_audio_stream_descriptor(
        &mut self,
        handle: resource::AudioStreamHandle,
    ) -> RuntimeResult<HarnessValue<AudioStreamDescriptor, AudioStreamDescriptorVm>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out =
                    audio_vm::destack_audio_stream_descriptor(self.call_context, context, handle)?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let mut out = std::mem::MaybeUninit::<AudioStreamDescriptor>::uninit();
                unsafe {
                    audio_native::destack_audio_stream_descriptor(
                        self.call_context,
                        out.as_mut_ptr(),
                        handle,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Drain one playback stream.
    ///
    /// Wait for one playback stream to consume currently queued samples.
    /// Drain timeout is expressed in nanoseconds in the runtime monotonic domain.
    ///
    /// # Platform
    /// Unix and Windows.
    /// Uses ALSA, PulseAudio, PipeWire, CoreAudio, WASAPI, AAudio, OpenSL ES, JACK, and ASIO where available drain or synchronized-stop operations.
    ///
    /// # Errors
    /// Returns invalidArgument, ioNotFound, ioInterrupted, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `audio.playback`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_audio_stream_drain(
        &mut self,
        handle: resource::AudioStreamHandle,
        timeoutns: u64,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                audio_vm::destack_audio_stream_drain(self.call_context, context, handle, timeoutns)
            }
            None => unsafe {
                audio_native::destack_audio_stream_drain(self.call_context, handle, timeoutns)
            },
        }
    }

    /// Flush buffered stream data.
    ///
    /// Drop pending buffered data for one stream without closing it.
    /// Flushing semantics are backend-defined for capture and duplex streams.
    ///
    /// # Platform
    /// Unix and Windows.
    /// Uses ALSA, PulseAudio, PipeWire, CoreAudio, WASAPI, AAudio, OpenSL ES, JACK, and ASIO where available stream flush or reset operations.
    ///
    /// # Errors
    /// Returns invalidArgument, ioNotFound, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `audio.control`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_audio_stream_flush(
        &mut self,
        handle: resource::AudioStreamHandle,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                audio_vm::destack_audio_stream_flush(self.call_context, context, handle)
            }
            None => unsafe { audio_native::destack_audio_stream_flush(self.call_context, handle) },
        }
    }

    /// Open one audio stream on one device.
    ///
    /// Create one host audio stream with explicit sample format, channel, and period configuration.
    /// Open options carry optional tuning hints and strict requirement lanes.
    /// Any unsatisfied requirement must fail open with `notSupported`.
    /// Buffering and latency behavior follow host backend contracts.
    ///
    /// # Platform
    /// Unix and Windows.
    /// Uses ALSA, PulseAudio, PipeWire, CoreAudio, WASAPI, AAudio, OpenSL ES, JACK, and ASIO where available stream creation APIs.
    ///
    /// # Errors
    /// Returns invalidArgument, ioNotFound, ioPermissionDenied, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `audio.stream`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_audio_stream_open(
        &mut self,
        device: resource::AudioDeviceHandle,
        config: HarnessValue<AudioStreamConfig, AudioStreamConfigVm>,
        options: HarnessValue<AudioStreamOpenOptions, AudioStreamOpenOptionsVm>,
    ) -> RuntimeResult<resource::AudioStreamHandle> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let config = config.into_vm("config")?;
                let options = options.into_vm("options")?;
                let out = audio_vm::destack_audio_stream_open(
                    self.call_context,
                    context,
                    device,
                    config,
                    options,
                )?;
                Ok(out)
            }
            None => {
                let config = config.into_native("config")?;
                let options = options.into_native("options")?;
                let mut out = std::mem::MaybeUninit::<resource::AudioStreamHandle>::uninit();
                unsafe {
                    audio_native::destack_audio_stream_open(
                        self.call_context,
                        out.as_mut_ptr(),
                        device,
                        config,
                        options,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// Pause or resume one audio stream.
    ///
    /// Transition one running stream into paused state and back.
    /// Pause support is backend-dependent.
    ///
    /// # Platform
    /// Unix and Windows.
    /// Mirrors libsoundio `soundio_outstream_pause` and SDL stream-device pause semantics.
    ///
    /// # Errors
    /// Returns invalidArgument, ioNotFound, ioInterrupted, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `audio.stream`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_audio_stream_pause(
        &mut self,
        handle: resource::AudioStreamHandle,
        pause: bool,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                audio_vm::destack_audio_stream_pause(self.call_context, context, handle, pause)
            }
            None => unsafe {
                audio_native::destack_audio_stream_pause(self.call_context, handle, pause)
            },
        }
    }

    /// Read one packet of captured audio frames.
    ///
    /// Read one packet of captured interleaved audio frames from the capture stream.
    /// Packet sizing and buffering follow host backend capture contracts.
    ///
    /// # Platform
    /// Unix and Windows.
    /// Uses ALSA, PulseAudio, PipeWire, CoreAudio, WASAPI, AAudio, OpenSL ES, JACK, and ASIO where available stream read or capture-client operations.
    ///
    /// # Errors
    /// Returns invalidArgument, ioNotFound, ioWouldBlock, ioInvalidData, notSupported.
    ///
    /// # Security
    /// Requires `audio.capture`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_audio_stream_read(
        &mut self,
        handle: resource::AudioStreamHandle,
        maxbytes: u32,
    ) -> RuntimeResult<HarnessValue<NativeSlice<u8>, VmSlice<u8>>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = audio_vm::destack_audio_stream_read(
                    self.call_context,
                    context,
                    handle,
                    maxbytes,
                )?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let mut out = std::mem::MaybeUninit::<NativeSlice<u8>>::uninit();
                unsafe {
                    audio_native::destack_audio_stream_read(
                        self.call_context,
                        out.as_mut_ptr(),
                        handle,
                        maxbytes,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Read one packet into vectorized buffers.
    ///
    /// Read one packet of captured audio frames into multiple byte slices.
    /// Buffers can represent segmented interleaved payloads or channel planes when non-interleaved mode is active.
    ///
    /// # Platform
    /// Unix and Windows.
    /// Mirrors readv-style capture behavior and backend non-interleaved lanes where available.
    ///
    /// # Errors
    /// Returns invalidArgument, ioNotFound, ioWouldBlock, ioInvalidData, notSupported.
    ///
    /// # Security
    /// Requires `audio.capture`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_audio_stream_readv(
        &mut self,
        handle: resource::AudioStreamHandle,
        buffers: HarnessValue<NativeSlice<NativeSlice<u8>>, VmSlice<VmSlice<u8>>>,
    ) -> RuntimeResult<u64> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let buffers = buffers.into_vm("buffers")?;
                let out = audio_vm::destack_audio_stream_readv(
                    self.call_context,
                    context,
                    handle,
                    buffers,
                )?;
                Ok(out)
            }
            None => {
                let buffers = buffers.into_native("buffers")?;
                let mut out = std::mem::MaybeUninit::<u64>::uninit();
                unsafe {
                    audio_native::destack_audio_stream_readv(
                        self.call_context,
                        out.as_mut_ptr(),
                        handle,
                        buffers,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// Set one stream mute state.
    ///
    /// Apply one mute state for one stream processing lane.
    /// This controls stream-level mute and does not imply global endpoint mute ownership.
    ///
    /// # Platform
    /// Unix and Windows.
    /// Uses ALSA, PulseAudio, PipeWire, CoreAudio, WASAPI, AAudio, OpenSL ES, JACK, and ASIO stream-level mute paths where available.
    ///
    /// # Errors
    /// Returns invalidArgument, ioNotFound, ioPermissionDenied, notSupported.
    ///
    /// # Security
    /// Requires `audio.control`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_audio_stream_set_mute(
        &mut self,
        handle: resource::AudioStreamHandle,
        muted: bool,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                audio_vm::destack_audio_stream_set_mute(self.call_context, context, handle, muted)
            }
            None => unsafe {
                audio_native::destack_audio_stream_set_mute(self.call_context, handle, muted)
            },
        }
    }

    /// Set one stream name.
    ///
    /// Apply one stream label for host mixers and diagnostics where supported.
    /// Backend label visibility and truncation follow host policy.
    ///
    /// # Platform
    /// Unix and Windows.
    /// Mirrors cubeb `cubeb_stream_set_name` behavior where available.
    ///
    /// # Errors
    /// Returns invalidArgument, ioNotFound, ioPermissionDenied, notSupported.
    ///
    /// # Security
    /// Requires `audio.control`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_audio_stream_set_name(
        &mut self,
        handle: resource::AudioStreamHandle,
        name: HarnessValue<NativeStringRef, vm::StringHandle>,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let name = name.into_vm("name")?;
                audio_vm::destack_audio_stream_set_name(self.call_context, context, handle, name)
            }
            None => unsafe {
                let name = name.into_native("name")?;
                audio_native::destack_audio_stream_set_name(self.call_context, handle, name)
            },
        }
    }

    /// Set one stream gain multiplier.
    ///
    /// Apply one linear gain multiplier for one stream processing lane.
    /// This controls stream-level gain and does not imply global endpoint mixer ownership.
    ///
    /// # Platform
    /// Unix and Windows.
    /// Uses ALSA, PulseAudio, PipeWire, CoreAudio, WASAPI, AAudio, OpenSL ES, JACK, and ASIO stream-level gain paths where available.
    ///
    /// # Errors
    /// Returns invalidArgument, ioNotFound, ioPermissionDenied, notSupported.
    ///
    /// # Security
    /// Requires `audio.control`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_audio_stream_set_volume(
        &mut self,
        handle: resource::AudioStreamHandle,
        lineargain: f64,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => audio_vm::destack_audio_stream_set_volume(
                self.call_context,
                context,
                handle,
                lineargain,
            ),
            None => unsafe {
                audio_native::destack_audio_stream_set_volume(self.call_context, handle, lineargain)
            },
        }
    }

    /// Start one audio stream.
    ///
    /// Transition one opened stream to running state and begin host DMA or scheduler processing.
    /// Start timing follows host backend scheduling semantics.
    ///
    /// # Platform
    /// Unix and Windows.
    /// Uses ALSA, PulseAudio, PipeWire, CoreAudio, WASAPI, AAudio, OpenSL ES, JACK, and ASIO where available stream start operations.
    ///
    /// # Errors
    /// Returns invalidArgument, ioNotFound, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `audio.stream`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_audio_stream_start(
        &mut self,
        handle: resource::AudioStreamHandle,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                audio_vm::destack_audio_stream_start(self.call_context, context, handle)
            }
            None => unsafe { audio_native::destack_audio_stream_start(self.call_context, handle) },
        }
    }

    /// Read one stream state.
    ///
    /// Read one point-in-time state sample of stream run state and backend buffering metrics.
    /// State values are advisory and can change immediately after read.
    ///
    /// # Platform
    /// Unix and Windows.
    /// Uses ALSA, PulseAudio, PipeWire, CoreAudio, WASAPI, AAudio, OpenSL ES, JACK, and ASIO where available stream query primitives.
    ///
    /// # Errors
    /// Returns invalidArgument, ioNotFound, ioInvalidData, notSupported.
    ///
    /// # Security
    /// Requires `audio.control`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_audio_stream_state(
        &mut self,
        handle: resource::AudioStreamHandle,
    ) -> RuntimeResult<HarnessValue<AudioStreamState, AudioStreamStateVm>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = audio_vm::destack_audio_stream_state(self.call_context, context, handle)?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let mut out = std::mem::MaybeUninit::<AudioStreamState>::uninit();
                unsafe {
                    audio_native::destack_audio_stream_state(
                        self.call_context,
                        out.as_mut_ptr(),
                        handle,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Stop one audio stream.
    ///
    /// Transition one running stream to stopped state and flush host backend scheduling.
    /// Buffered frames can be discarded based on host backend semantics.
    ///
    /// # Platform
    /// Unix and Windows.
    /// Uses ALSA, PulseAudio, PipeWire, CoreAudio, WASAPI, AAudio, OpenSL ES, JACK, and ASIO where available stream stop operations.
    ///
    /// # Errors
    /// Returns invalidArgument, ioNotFound, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `audio.stream`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_audio_stream_stop(
        &mut self,
        handle: resource::AudioStreamHandle,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                audio_vm::destack_audio_stream_stop(self.call_context, context, handle)
            }
            None => unsafe { audio_native::destack_audio_stream_stop(self.call_context, handle) },
        }
    }

    /// Check one audio stream configuration for backend support.
    ///
    /// Check one stream configuration and return backend negotiation results without opening one long-lived stream handle.
    /// Requirement flags are resolved into `satisfiedRequirements` and `unsatisfiedRequirements`.
    ///
    /// # Platform
    /// Unix and Windows.
    /// Mirrors PortAudio `Pa_IsFormatSupported` intent and miniaudio native-format probing behavior.
    ///
    /// # Errors
    /// Returns invalidArgument, ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `audio.stream`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_audio_stream_support(
        &mut self,
        device: resource::AudioDeviceHandle,
        config: HarnessValue<AudioStreamConfig, AudioStreamConfigVm>,
        options: HarnessValue<AudioStreamOpenOptions, AudioStreamOpenOptionsVm>,
    ) -> RuntimeResult<HarnessValue<AudioStreamSupport, AudioStreamSupportVm>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let config = config.into_vm("config")?;
                let options = options.into_vm("options")?;
                let out = audio_vm::destack_audio_stream_support(
                    self.call_context,
                    context,
                    device,
                    config,
                    options,
                )?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let config = config.into_native("config")?;
                let options = options.into_native("options")?;
                let mut out = std::mem::MaybeUninit::<AudioStreamSupport>::uninit();
                unsafe {
                    audio_native::destack_audio_stream_support(
                        self.call_context,
                        out.as_mut_ptr(),
                        device,
                        config,
                        options,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Read one stream timing sample.
    ///
    /// Read one full timing sample that correlates stream position and available backend clocks.
    /// Missing optional lanes are reported through `has*` fields instead of `notSupported`.
    /// Timing values are intended for drift correction and synchronization.
    ///
    /// # Platform
    /// Unix and Windows.
    /// Uses ALSA, PulseAudio, PipeWire, CoreAudio, WASAPI, AAudio, OpenSL ES, JACK, and ASIO where available stream-clock query operations.
    ///
    /// # Errors
    /// Returns invalidArgument, ioNotFound, ioInvalidData, notSupported.
    ///
    /// # Security
    /// Requires `audio.control`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_audio_stream_timing(
        &mut self,
        handle: resource::AudioStreamHandle,
    ) -> RuntimeResult<HarnessValue<AudioStreamTiming, AudioStreamTimingVm>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out =
                    audio_vm::destack_audio_stream_timing(self.call_context, context, handle)?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let mut out = std::mem::MaybeUninit::<AudioStreamTiming>::uninit();
                unsafe {
                    audio_native::destack_audio_stream_timing(
                        self.call_context,
                        out.as_mut_ptr(),
                        handle,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Try to read one packet of captured audio frames without blocking.
    ///
    /// Read one packet of captured interleaved audio frames without waiting.
    /// Empty input state is reported through ioWouldBlock.
    ///
    /// # Platform
    /// Unix and Windows.
    /// Uses ALSA, PulseAudio, PipeWire, CoreAudio, WASAPI, AAudio, OpenSL ES, JACK, and ASIO where available nonblocking stream read operations.
    ///
    /// # Errors
    /// Returns invalidArgument, ioNotFound, ioWouldBlock, ioInvalidData, notSupported.
    ///
    /// # Security
    /// Requires `audio.capture`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_audio_stream_try_read(
        &mut self,
        handle: resource::AudioStreamHandle,
        maxbytes: u32,
    ) -> RuntimeResult<HarnessValue<NativeSlice<u8>, VmSlice<u8>>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = audio_vm::destack_audio_stream_try_read(
                    self.call_context,
                    context,
                    handle,
                    maxbytes,
                )?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let mut out = std::mem::MaybeUninit::<NativeSlice<u8>>::uninit();
                unsafe {
                    audio_native::destack_audio_stream_try_read(
                        self.call_context,
                        out.as_mut_ptr(),
                        handle,
                        maxbytes,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Try to read one packet into vectorized buffers without blocking.
    ///
    /// Read one packet of captured audio frames into multiple byte slices without waiting.
    /// Empty input state is reported through ioWouldBlock.
    ///
    /// # Platform
    /// Unix and Windows.
    /// Mirrors nonblocking readv-style capture behavior.
    ///
    /// # Errors
    /// Returns invalidArgument, ioNotFound, ioWouldBlock, ioInvalidData, notSupported.
    ///
    /// # Security
    /// Requires `audio.capture`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_audio_stream_try_readv(
        &mut self,
        handle: resource::AudioStreamHandle,
        buffers: HarnessValue<NativeSlice<NativeSlice<u8>>, VmSlice<VmSlice<u8>>>,
    ) -> RuntimeResult<u64> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let buffers = buffers.into_vm("buffers")?;
                let out = audio_vm::destack_audio_stream_try_readv(
                    self.call_context,
                    context,
                    handle,
                    buffers,
                )?;
                Ok(out)
            }
            None => {
                let buffers = buffers.into_native("buffers")?;
                let mut out = std::mem::MaybeUninit::<u64>::uninit();
                unsafe {
                    audio_native::destack_audio_stream_try_readv(
                        self.call_context,
                        out.as_mut_ptr(),
                        handle,
                        buffers,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// Try to write one packet of audio frames without blocking.
    ///
    /// Submit one packet of interleaved audio frames to the playback stream without waiting.
    /// Empty output space is reported through ioWouldBlock.
    ///
    /// # Platform
    /// Unix and Windows.
    /// Uses ALSA, PulseAudio, PipeWire, CoreAudio, WASAPI, AAudio, OpenSL ES, JACK, and ASIO where available nonblocking stream write operations.
    ///
    /// # Errors
    /// Returns invalidArgument, ioNotFound, ioWouldBlock, ioInvalidData, notSupported.
    ///
    /// # Security
    /// Requires `audio.playback`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_audio_stream_try_write(
        &mut self,
        handle: resource::AudioStreamHandle,
        data: HarnessValue<NativeSlice<u8>, VmSlice<u8>>,
    ) -> RuntimeResult<u64> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let data = data.into_vm("data")?;
                let out = audio_vm::destack_audio_stream_try_write(
                    self.call_context,
                    context,
                    handle,
                    data,
                )?;
                Ok(out)
            }
            None => {
                let data = data.into_native("data")?;
                let mut out = std::mem::MaybeUninit::<u64>::uninit();
                unsafe {
                    audio_native::destack_audio_stream_try_write(
                        self.call_context,
                        out.as_mut_ptr(),
                        handle,
                        data,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// Try to write one packet from vectorized buffers without blocking.
    ///
    /// Submit one packet of audio frames from multiple byte slices without waiting.
    /// Empty output space is reported through ioWouldBlock.
    ///
    /// # Platform
    /// Unix and Windows.
    /// Mirrors nonblocking writev-style submission behavior.
    ///
    /// # Errors
    /// Returns invalidArgument, ioNotFound, ioWouldBlock, ioInvalidData, notSupported.
    ///
    /// # Security
    /// Requires `audio.playback`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_audio_stream_try_writev(
        &mut self,
        handle: resource::AudioStreamHandle,
        buffers: HarnessValue<NativeSlice<NativeSlice<u8>>, VmSlice<VmSlice<u8>>>,
    ) -> RuntimeResult<u64> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let buffers = buffers.into_vm("buffers")?;
                let out = audio_vm::destack_audio_stream_try_writev(
                    self.call_context,
                    context,
                    handle,
                    buffers,
                )?;
                Ok(out)
            }
            None => {
                let buffers = buffers.into_native("buffers")?;
                let mut out = std::mem::MaybeUninit::<u64>::uninit();
                unsafe {
                    audio_native::destack_audio_stream_try_writev(
                        self.call_context,
                        out.as_mut_ptr(),
                        handle,
                        buffers,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// Write one packet of audio frames.
    ///
    /// Submit one packet of interleaved audio frames to the playback stream.
    /// Short writes can occur when host buffers are near capacity.
    ///
    /// # Platform
    /// Unix and Windows.
    /// Uses ALSA, PulseAudio, PipeWire, CoreAudio, WASAPI, AAudio, OpenSL ES, JACK, and ASIO where available stream write or render-client operations.
    ///
    /// # Errors
    /// Returns invalidArgument, ioNotFound, ioWouldBlock, ioInvalidData, notSupported.
    ///
    /// # Security
    /// Requires `audio.playback`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_audio_stream_write(
        &mut self,
        handle: resource::AudioStreamHandle,
        data: HarnessValue<NativeSlice<u8>, VmSlice<u8>>,
    ) -> RuntimeResult<u64> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let data = data.into_vm("data")?;
                let out =
                    audio_vm::destack_audio_stream_write(self.call_context, context, handle, data)?;
                Ok(out)
            }
            None => {
                let data = data.into_native("data")?;
                let mut out = std::mem::MaybeUninit::<u64>::uninit();
                unsafe {
                    audio_native::destack_audio_stream_write(
                        self.call_context,
                        out.as_mut_ptr(),
                        handle,
                        data,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// Write one packet for one target presentation time.
    ///
    /// Submit one packet of interleaved audio frames for one target presentation timestamp.
    /// Scheduling precision depends on host backend timing guarantees.
    /// This is one optional scheduling lane and can return `notSupported` when unavailable.
    ///
    /// # Platform
    /// Unix and Windows.
    /// Uses ALSA, PulseAudio, PipeWire, CoreAudio, WASAPI, AAudio, OpenSL ES, JACK, and ASIO where available scheduled-render operations when available.
    ///
    /// # Errors
    /// Returns invalidArgument, ioNotFound, ioWouldBlock, ioInvalidData, notSupported.
    ///
    /// # Security
    /// Requires `audio.playback.schedule`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_audio_stream_write_at(
        &mut self,
        handle: resource::AudioStreamHandle,
        data: HarnessValue<NativeSlice<u8>, VmSlice<u8>>,
        presentationtimens: u64,
    ) -> RuntimeResult<u64> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let data = data.into_vm("data")?;
                let out = audio_vm::destack_audio_stream_write_at(
                    self.call_context,
                    context,
                    handle,
                    data,
                    presentationtimens,
                )?;
                Ok(out)
            }
            None => {
                let data = data.into_native("data")?;
                let mut out = std::mem::MaybeUninit::<u64>::uninit();
                unsafe {
                    audio_native::destack_audio_stream_write_at(
                        self.call_context,
                        out.as_mut_ptr(),
                        handle,
                        data,
                        presentationtimens,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// Write one vectorized packet for one target presentation time.
    ///
    /// Submit one packet of audio frames from multiple byte slices for one target presentation timestamp.
    /// Buffers can represent segmented interleaved payloads or channel planes when non-interleaved mode is active.
    /// Scheduling precision depends on host backend timing guarantees.
    ///
    /// # Platform
    /// Unix and Windows.
    /// Mirrors scheduled-render operations where available and extends them for writev-style payload submission.
    ///
    /// # Errors
    /// Returns invalidArgument, ioNotFound, ioWouldBlock, ioInvalidData, notSupported.
    ///
    /// # Security
    /// Requires `audio.playback.schedule`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_audio_stream_write_atv(
        &mut self,
        handle: resource::AudioStreamHandle,
        buffers: HarnessValue<NativeSlice<NativeSlice<u8>>, VmSlice<VmSlice<u8>>>,
        presentationtimens: u64,
    ) -> RuntimeResult<u64> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let buffers = buffers.into_vm("buffers")?;
                let out = audio_vm::destack_audio_stream_write_atv(
                    self.call_context,
                    context,
                    handle,
                    buffers,
                    presentationtimens,
                )?;
                Ok(out)
            }
            None => {
                let buffers = buffers.into_native("buffers")?;
                let mut out = std::mem::MaybeUninit::<u64>::uninit();
                unsafe {
                    audio_native::destack_audio_stream_write_atv(
                        self.call_context,
                        out.as_mut_ptr(),
                        handle,
                        buffers,
                        presentationtimens,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// Write one packet from vectorized buffers.
    ///
    /// Submit one packet of audio frames from multiple byte slices.
    /// Buffers can represent segmented interleaved payloads or channel planes when non-interleaved mode is active.
    ///
    /// # Platform
    /// Unix and Windows.
    /// Mirrors writev-style submission behavior and backend non-interleaved lanes where available.
    ///
    /// # Errors
    /// Returns invalidArgument, ioNotFound, ioWouldBlock, ioInvalidData, notSupported.
    ///
    /// # Security
    /// Requires `audio.playback`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_audio_stream_writev(
        &mut self,
        handle: resource::AudioStreamHandle,
        buffers: HarnessValue<NativeSlice<NativeSlice<u8>>, VmSlice<VmSlice<u8>>>,
    ) -> RuntimeResult<u64> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let buffers = buffers.into_vm("buffers")?;
                let out = audio_vm::destack_audio_stream_writev(
                    self.call_context,
                    context,
                    handle,
                    buffers,
                )?;
                Ok(out)
            }
            None => {
                let buffers = buffers.into_native("buffers")?;
                let mut out = std::mem::MaybeUninit::<u64>::uninit();
                unsafe {
                    audio_native::destack_audio_stream_writev(
                        self.call_context,
                        out.as_mut_ptr(),
                        handle,
                        buffers,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }
}

/// Wrapper that carries one native or VM value for generated harness bindings.
pub(crate) enum HarnessValue<Native, Vm> {
    /// Native value variant.
    Native(Native),
    /// VM value variant.
    Vm(Vm),
}

impl<Native: Clone, Vm: Clone> Clone for HarnessValue<Native, Vm> {
    fn clone(&self) -> Self {
        match self {
            HarnessValue::Native(value) => HarnessValue::Native(value.clone()),
            HarnessValue::Vm(value) => HarnessValue::Vm(value.clone()),
        }
    }
}

impl<Native, Vm> HarnessValue<Native, Vm> {
    /// Convert one wrapper into one native value.
    fn into_native(self, label: &str) -> RuntimeResult<Native> {
        match self {
            HarnessValue::Native(value) => Ok(value),
            HarnessValue::Vm(_) => Err(RuntimeError::from(
                HarnessPlatformError::invalid_argument_value(
                    label,
                    "expected native harness value",
                ),
            )
            .boxed()),
        }
    }

    /// Convert one wrapper into one VM value.
    fn into_vm(self, label: &str) -> RuntimeResult<Vm> {
        match self {
            HarnessValue::Vm(value) => Ok(value),
            HarnessValue::Native(_) => Err(RuntimeError::from(
                HarnessPlatformError::invalid_argument_value(label, "expected VM harness value"),
            )
            .boxed()),
        }
    }
}
