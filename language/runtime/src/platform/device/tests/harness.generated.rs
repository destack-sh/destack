// generated by generate-bindings: test harness, do not edit

#![allow(dead_code)]
#![allow(unused_imports)]
#![allow(clippy::type_complexity)]

use super::*;
use crate::diagnostic::{RuntimeError, RuntimeResult};
use crate::platform::device::{
    DeviceControlOperation, DeviceControlOperationVm, native as device_native, vm as device_vm,
};
use crate::platform::{
    NativeArray, NativeSlice, PlatformError as HarnessPlatformError, VmArray, VmSlice, fs, resource,
};
use destack_vm as vm;

impl<'call> DeviceHarnessContext<'call> {
    /// Return the generated VM context if available.
    #[allow(clippy::mut_from_ref)]
    fn generated_vm_context_mut(&self) -> Option<&mut vm::ExternalCallContext<'_>> {
        self.vm_context
            .map(|context| unsafe { &mut *(context as *mut vm::ExternalCallContext<'_>) })
    }

    /// Return one standardized value payload for native and VM variants.
    pub(crate) fn harness_value<Native, Vm>(&self, native: Native) -> HarnessValue<Native, Vm> {
        HarnessValue::Native(native)
    }

    /// Return one standardized value payload for VM and native variants.
    pub(crate) fn harness_value_vm<Native, Vm>(&self, vm: Vm) -> HarnessValue<Native, Vm> {
        HarnessValue::Vm(vm)
    }

    /// Run a device control request.
    ///
    /// Execute one host device-control operation with input and output buffers.
    /// Request meaning and binary payload layout are device-specific by design.
    ///
    /// # Platform
    /// Unix and Windows.
    /// Uses ioctl(2) on Unix and DeviceIoControl on Windows.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `device.control`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_device_control(
        &mut self,
        handle: resource::DeviceHandle,
        operation: HarnessValue<DeviceControlOperation, DeviceControlOperationVm>,
        input: HarnessValue<NativeSlice<u8>, VmSlice<u8>>,
        output: HarnessValue<NativeSlice<u8>, VmSlice<u8>>,
    ) -> RuntimeResult<u64> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let operation = operation.into_vm("operation")?;
                let input = input.into_vm("input")?;
                let output = output.into_vm("output")?;
                let out = device_vm::destack_device_control(
                    self.call_context,
                    context,
                    handle,
                    operation,
                    input,
                    output,
                )?;
                Ok(out)
            }
            None => {
                let operation = operation.into_native("operation")?;
                let input = input.into_native("input")?;
                let output = output.into_native("output")?;
                let mut out = std::mem::MaybeUninit::<u64>::uninit();
                unsafe {
                    device_native::destack_device_control(
                        self.call_context,
                        out.as_mut_ptr(),
                        handle,
                        operation,
                        input,
                        output,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// Close a device endpoint.
    ///
    /// Close one previously opened device handle.
    /// Close semantics for pending I O follow host kernel behavior.
    ///
    /// # Platform
    /// Unix and Windows.
    /// Uses close(2) on Unix and CloseHandle on Windows.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `device.read`, `device.write`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_device_close(
        &mut self,
        handle: resource::DeviceHandle,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => device_vm::destack_device_close(self.call_context, context, handle),
            None => unsafe { device_native::destack_device_close(self.call_context, handle) },
        }
    }

    /// Open a device endpoint.
    ///
    /// Open one host device node with the requested open flags.
    /// Access checks and device availability are enforced by the host kernel.
    ///
    /// # Platform
    /// Unix and Windows.
    /// Uses open(2) on Unix and CreateFileW on Windows device paths.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `device.read`, `device.write`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_device_open(
        &mut self,
        path: HarnessValue<fs::OsPath, fs::OsPathVm>,
        flags: u32,
        mode: u32,
    ) -> RuntimeResult<resource::DeviceHandle> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let path = path.into_vm("path")?;
                let out =
                    device_vm::destack_device_open(self.call_context, context, path, flags, mode)?;
                Ok(out)
            }
            None => {
                let path = path.into_native("path")?;
                let mut out = std::mem::MaybeUninit::<resource::DeviceHandle>::uninit();
                unsafe {
                    device_native::destack_device_open(
                        self.call_context,
                        out.as_mut_ptr(),
                        path,
                        flags,
                        mode,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// Read bytes from a device endpoint.
    ///
    /// Read bytes into caller-provided memory and return the number of bytes transferred.
    /// Partial reads are preserved exactly as reported by the host.
    ///
    /// # Platform
    /// Unix and Windows.
    /// Uses read(2) on Unix and ReadFile on Windows.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `device.read`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_device_read(
        &mut self,
        handle: resource::DeviceHandle,
        buffer: HarnessValue<NativeSlice<u8>, VmSlice<u8>>,
    ) -> RuntimeResult<u64> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let buffer = buffer.into_vm("buffer")?;
                let out =
                    device_vm::destack_device_read(self.call_context, context, handle, buffer)?;
                Ok(out)
            }
            None => {
                let buffer = buffer.into_native("buffer")?;
                let mut out = std::mem::MaybeUninit::<u64>::uninit();
                unsafe {
                    device_native::destack_device_read(
                        self.call_context,
                        out.as_mut_ptr(),
                        handle,
                        buffer,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// Write bytes to a device endpoint.
    ///
    /// Write bytes from caller-provided memory and return the number of bytes transferred.
    /// Partial writes are preserved exactly as reported by the host.
    ///
    /// # Platform
    /// Unix and Windows.
    /// Uses write(2) on Unix and WriteFile on Windows.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `device.write`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_device_write(
        &mut self,
        handle: resource::DeviceHandle,
        buffer: HarnessValue<NativeSlice<u8>, VmSlice<u8>>,
    ) -> RuntimeResult<u64> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let buffer = buffer.into_vm("buffer")?;
                let out =
                    device_vm::destack_device_write(self.call_context, context, handle, buffer)?;
                Ok(out)
            }
            None => {
                let buffer = buffer.into_native("buffer")?;
                let mut out = std::mem::MaybeUninit::<u64>::uninit();
                unsafe {
                    device_native::destack_device_write(
                        self.call_context,
                        out.as_mut_ptr(),
                        handle,
                        buffer,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }
}

/// Wrapper that carries one native or VM value for generated harness bindings.
pub(crate) enum HarnessValue<Native, Vm> {
    /// Native value variant.
    Native(Native),
    /// VM value variant.
    Vm(Vm),
}

impl<Native, Vm> HarnessValue<Native, Vm> {
    /// Convert one wrapper into one native value.
    fn into_native(self, label: &str) -> RuntimeResult<Native> {
        match self {
            HarnessValue::Native(value) => Ok(value),
            HarnessValue::Vm(_) => Err(RuntimeError::from(
                HarnessPlatformError::invalid_argument_value(
                    label,
                    "expected native harness value",
                ),
            )
            .boxed()),
        }
    }

    /// Convert one wrapper into one VM value.
    fn into_vm(self, label: &str) -> RuntimeResult<Vm> {
        match self {
            HarnessValue::Vm(value) => Ok(value),
            HarnessValue::Native(_) => Err(RuntimeError::from(
                HarnessPlatformError::invalid_argument_value(label, "expected VM harness value"),
            )
            .boxed()),
        }
    }
}
