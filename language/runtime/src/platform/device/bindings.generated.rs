// generated by generate-bindings: do not edit

#![allow(unused_imports)]
#![allow(clippy::clone_on_copy)]
#![allow(clippy::type_complexity)]

use crate::diagnostic::{RuntimeError, RuntimeResult};
use crate::platform::bindings::{
    BindingBlocking, BindingDescriptor, BindingRegistry, BindingReplayKind, BindingScope,
    NativeBinding, NativeBindingSet, ReplayPolicy, RuntimeWorld, native_call,
};
use crate::platform::device::{DeviceControlOperation, DeviceControlOperationVm};
use crate::platform::{
    NativeSlice, PlatformError, RuntimeStatus, VmArray, VmSlice, abi as platform_abi,
};
use crate::vm_binding_set;
use destack_vm as vm;
use destack_vm::Isolate;

use crate::binding;
use crate::runtime::{RuntimeCallContext, with_runtime_call_context};

use serde::{Deserialize, Serialize};

use crate::platform::device::simulated::{
    native as platform_simulated_native, vm as platform_simulated_vm,
};
use crate::platform::device::{native as platform_native, vm as platform_vm};
use crate::platform::{
    device as platform_device, fs as platform_fs, fs, resource as platform_resource, resource,
};

/// Read a positional argument value.
#[allow(dead_code)]
fn arg_value(
    args: &[vm::Value],
    index: usize,
    name: &'static str,
    expected: &'static str,
) -> RuntimeResult<vm::Value> {
    let value = args.get(index).copied().ok_or_else(|| {
        RuntimeError::from(PlatformError::invalid_argument_type(name, expected)).boxed()
    })?;

    Ok(value)
}

/// Decode an unsigned integer argument with an explicit width.
#[allow(dead_code)]
fn decode_uint(
    value: vm::Value,
    name: &'static str,
    expected: &'static str,
    bits: u8,
) -> RuntimeResult<u64> {
    let (raw, width) = value.as_uint_with_width().ok_or_else(|| {
        RuntimeError::from(PlatformError::invalid_argument_type(name, expected)).boxed()
    })?;
    if width != bits {
        return Err(
            RuntimeError::from(PlatformError::invalid_argument_type(name, expected)).boxed(),
        );
    }

    Ok(raw)
}

/// Decode a u8 argument.
#[allow(dead_code)]
fn decode_uint8(value: vm::Value, name: &'static str, expected: &'static str) -> RuntimeResult<u8> {
    Ok(decode_uint(value, name, expected, 8)? as u8)
}

/// Decode a u32 argument.
#[allow(dead_code)]
fn decode_uint32(
    value: vm::Value,
    name: &'static str,
    expected: &'static str,
) -> RuntimeResult<u32> {
    Ok(decode_uint(value, name, expected, 32)? as u32)
}

/// Decode a u64 argument.
#[allow(dead_code)]
fn decode_uint64(
    value: vm::Value,
    name: &'static str,
    expected: &'static str,
) -> RuntimeResult<u64> {
    decode_uint(value, name, expected, 64)
}

/// Decode a slice argument.
fn decode_slice<T>(
    context: &mut vm::ExternalCallContext<'_>,
    value: vm::Value,
    name: &'static str,
    expected: &'static str,
) -> RuntimeResult<VmSlice<T>> {
    VmSlice::<T>::from_value(context, value, name, expected)
}

/// Decode an array argument.
fn decode_array<T>(
    context: &mut vm::ExternalCallContext<'_>,
    value: vm::Value,
    name: &'static str,
    expected: &'static str,
) -> RuntimeResult<VmArray<T>> {
    VmArray::<T>::from_value(context, value, name, expected)
}

/// Decode arguments for destack.device.control.control.
#[inline]
fn decode_destack_device_control_control_args(
    context: &mut vm::ExternalCallContext<'_>,
    args: &[vm::Value],
) -> RuntimeResult<(
    resource::DeviceHandle,
    DeviceControlOperationVm,
    VmSlice<u8>,
    VmSlice<u8>,
)> {
    let handle_value = arg_value(args, 0, "handle", "DeviceHandle")?;
    let handle_inner_inner = decode_uint64(handle_value, "handle_inner_inner", "DeviceHandle")?;
    let handle_inner = resource::ResourceId(handle_inner_inner);
    let handle = resource::DeviceHandle(handle_inner);
    let operation_value = arg_value(args, 1, "operation", "DeviceControlOperation")?;
    let operation = {
        if operation_value.tag() != vm::ValueTag::Aggregate {
            return Err(RuntimeError::from(PlatformError::invalid_argument_type(
                "operation",
                "DeviceControlOperation",
            ))
            .boxed());
        }
        let slots = context
            .aggregate_slots(operation_value)
            .map_err(|error| RuntimeError::from(error).boxed())?;
        if slots.len() != 2 {
            return Err(RuntimeError::from(PlatformError::invalid_argument_value(
                "operation",
                "expected 2 fields",
            ))
            .boxed());
        }
        let operation_request = decode_uint64(slots[0], "operation_request", "request")?;
        let operation_flags = decode_uint32(slots[1], "operation_flags", "flags")?;
        DeviceControlOperationVm {
            request: operation_request,
            flags: operation_flags,
        }
    };
    let input_value = arg_value(args, 2, "input", "Slice<uint8>")?;
    let input = decode_slice::<u8>(context, input_value, "input", "Slice<uint8>")?;
    let output_value = arg_value(args, 3, "output", "Slice<uint8>")?;
    let output = decode_slice::<u8>(context, output_value, "output", "Slice<uint8>")?;
    Ok((handle, operation, input, output))
}

/// Encode the result for destack.device.control.control.
#[inline]
fn encode_destack_device_control_control_result(
    context: &mut vm::ExternalCallContext<'_>,
    result: RuntimeResult<u64>,
) -> RuntimeResult<vm::Value> {
    // ignore unused context
    let _ = context;

    result.map(|value| vm::Value::uint(value, 64))
}

/// Decode arguments for destack.device.io.close.
#[inline]
fn decode_destack_device_io_close_args(
    context: &mut vm::ExternalCallContext<'_>,
    args: &[vm::Value],
) -> RuntimeResult<(resource::DeviceHandle,)> {
    // ignore unused context
    let _ = context;

    let handle_value = arg_value(args, 0, "handle", "DeviceHandle")?;
    let handle_inner_inner = decode_uint64(handle_value, "handle_inner_inner", "DeviceHandle")?;
    let handle_inner = resource::ResourceId(handle_inner_inner);
    let handle = resource::DeviceHandle(handle_inner);
    Ok((handle,))
}

/// Encode the result for destack.device.io.close.
#[inline]
fn encode_destack_device_io_close_result(
    context: &mut vm::ExternalCallContext<'_>,
    result: RuntimeResult<()>,
) -> RuntimeResult<vm::Value> {
    // ignore unused context
    let _ = context;

    result.map(|_| vm::Value::VOID)
}

/// Decode arguments for destack.device.io.open.
#[inline]
fn decode_destack_device_io_open_args(
    context: &mut vm::ExternalCallContext<'_>,
    args: &[vm::Value],
) -> RuntimeResult<(fs::OsPathVm, u32, u32)> {
    let path_value = arg_value(args, 0, "path", "OsPath")?;
    let path = {
        if path_value.tag() != vm::ValueTag::Aggregate {
            return Err(
                RuntimeError::from(PlatformError::invalid_argument_type("path", "OsPath")).boxed(),
            );
        }
        let slots = context
            .aggregate_slots(path_value)
            .map_err(|error| RuntimeError::from(error).boxed())?;
        if slots.len() != 3 {
            return Err(RuntimeError::from(PlatformError::invalid_argument_value(
                "path",
                "expected 3 fields",
            ))
            .boxed());
        }
        let path_encoding_raw = decode_uint8(slots[0], "path_encoding_raw", "encoding")?;
        let path_encoding = match path_encoding_raw {
            1u8 => fs::PathEncoding::Bytes,
            2u8 => fs::PathEncoding::Utf16,
            _ => {
                return Err(RuntimeError::from(PlatformError::invalid_argument_value(
                    "path_encoding",
                    "unknown fs::PathEncoding value",
                ))
                .boxed());
            }
        };
        let path_bytes_inner = decode_array::<u8>(context, slots[1], "path_bytes_inner", "bytes")?;
        let path_bytes = platform_fs::PathBytesAbi::<platform_abi::VmAbi>(path_bytes_inner);
        let path_utf16_inner = decode_array::<u16>(context, slots[2], "path_utf16_inner", "utf16")?;
        let path_utf16 = platform_fs::PathUtf16Abi::<platform_abi::VmAbi>(path_utf16_inner);
        fs::OsPathVm {
            encoding: path_encoding,
            bytes: path_bytes,
            utf16: path_utf16,
        }
    };
    let flags_value = arg_value(args, 1, "flags", "uint32")?;
    let flags = decode_uint32(flags_value, "flags", "uint32")?;
    let mode_value = arg_value(args, 2, "mode", "uint32")?;
    let mode = decode_uint32(mode_value, "mode", "uint32")?;
    Ok((path, flags, mode))
}

/// Encode the result for destack.device.io.open.
#[inline]
fn encode_destack_device_io_open_result(
    context: &mut vm::ExternalCallContext<'_>,
    result: RuntimeResult<resource::DeviceHandle>,
) -> RuntimeResult<vm::Value> {
    // ignore unused context
    let _ = context;

    result.map(|value| vm::Value::uint(value.0.0, 64))
}

/// Decode arguments for destack.device.io.read.
#[inline]
fn decode_destack_device_io_read_args(
    context: &mut vm::ExternalCallContext<'_>,
    args: &[vm::Value],
) -> RuntimeResult<(resource::DeviceHandle, VmSlice<u8>)> {
    let handle_value = arg_value(args, 0, "handle", "DeviceHandle")?;
    let handle_inner_inner = decode_uint64(handle_value, "handle_inner_inner", "DeviceHandle")?;
    let handle_inner = resource::ResourceId(handle_inner_inner);
    let handle = resource::DeviceHandle(handle_inner);
    let buffer_value = arg_value(args, 1, "buffer", "Slice<uint8>")?;
    let buffer = decode_slice::<u8>(context, buffer_value, "buffer", "Slice<uint8>")?;
    Ok((handle, buffer))
}

/// Encode the result for destack.device.io.read.
#[inline]
fn encode_destack_device_io_read_result(
    context: &mut vm::ExternalCallContext<'_>,
    result: RuntimeResult<u64>,
) -> RuntimeResult<vm::Value> {
    // ignore unused context
    let _ = context;

    result.map(|value| vm::Value::uint(value, 64))
}

/// Decode arguments for destack.device.io.write.
#[inline]
fn decode_destack_device_io_write_args(
    context: &mut vm::ExternalCallContext<'_>,
    args: &[vm::Value],
) -> RuntimeResult<(resource::DeviceHandle, VmSlice<u8>)> {
    let handle_value = arg_value(args, 0, "handle", "DeviceHandle")?;
    let handle_inner_inner = decode_uint64(handle_value, "handle_inner_inner", "DeviceHandle")?;
    let handle_inner = resource::ResourceId(handle_inner_inner);
    let handle = resource::DeviceHandle(handle_inner);
    let buffer_value = arg_value(args, 1, "buffer", "Slice<uint8>")?;
    let buffer = decode_slice::<u8>(context, buffer_value, "buffer", "Slice<uint8>")?;
    Ok((handle, buffer))
}

/// Encode the result for destack.device.io.write.
#[inline]
fn encode_destack_device_io_write_result(
    context: &mut vm::ExternalCallContext<'_>,
    result: RuntimeResult<u64>,
) -> RuntimeResult<vm::Value> {
    // ignore unused context
    let _ = context;

    result.map(|value| vm::Value::uint(value, 64))
}

/// Replay payload for destack.device.control.control.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
struct DeviceControlControlReplay {
    /// Replay result payload.
    pub result: Result<u64, PlatformError>,
}

/// Replay payload for destack.device.io.close.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
struct DeviceIoCloseReplay {
    /// Replay result payload.
    pub result: Result<(), PlatformError>,
}

/// Replay payload for destack.device.io.open.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
struct DeviceIoOpenReplay {
    /// Replay result payload.
    pub result: Result<resource::DeviceHandle, PlatformError>,
}

/// Replay payload for destack.device.io.read.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
struct DeviceIoReadReplay {
    /// Replay result payload.
    pub result: Result<u64, PlatformError>,
}

/// Replay payload for destack.device.io.write.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
struct DeviceIoWriteReplay {
    /// Replay result payload.
    pub result: Result<u64, PlatformError>,
}

/// Binding descriptor for destack.device.control.control.
pub const DEVICE_CONTROL_CONTROL: BindingDescriptor =
    BindingDescriptor::external_with_requires_and_behavior(
        "destack.device.control.control",
        "export function control(handle: DeviceHandle, operation: DeviceControlOperation, input: Slice<uint8>, output: Slice<uint8>): Result<uint64, PlatformError>",
        ReplayPolicy::Recordable,
        BindingReplayKind::Regular,
        &["device.control"],
        BindingScope::Os,
        BindingBlocking::Sometimes,
    );

/// Binding descriptor for destack.device.io.close.
pub const DEVICE_IO_CLOSE: BindingDescriptor =
    BindingDescriptor::external_with_requires_and_behavior(
        "destack.device.io.close",
        "export function close(handle: DeviceHandle): Result<void, PlatformError>",
        ReplayPolicy::Recordable,
        BindingReplayKind::Regular,
        &["device.read", "device.write"],
        BindingScope::Os,
        BindingBlocking::Sometimes,
    );

/// Binding descriptor for destack.device.io.open.
pub const DEVICE_IO_OPEN: BindingDescriptor =
    BindingDescriptor::external_with_requires_and_behavior(
        "destack.device.io.open",
        "export function open(path: OsPath, flags: uint32, mode: uint32): Result<DeviceHandle, PlatformError>",
        ReplayPolicy::Recordable,
        BindingReplayKind::Regular,
        &["device.read", "device.write"],
        BindingScope::Os,
        BindingBlocking::Sometimes,
    );

/// Binding descriptor for destack.device.io.read.
pub const DEVICE_IO_READ: BindingDescriptor =
    BindingDescriptor::external_with_requires_and_behavior(
        "destack.device.io.read",
        "export function read(handle: DeviceHandle, buffer: Slice<uint8>): Result<uint64, PlatformError>",
        ReplayPolicy::Recordable,
        BindingReplayKind::Regular,
        &["device.read"],
        BindingScope::Os,
        BindingBlocking::Sometimes,
    );

/// Binding descriptor for destack.device.io.write.
pub const DEVICE_IO_WRITE: BindingDescriptor =
    BindingDescriptor::external_with_requires_and_behavior(
        "destack.device.io.write",
        "export function write(handle: DeviceHandle, buffer: Slice<uint8>): Result<uint64, PlatformError>",
        ReplayPolicy::Recordable,
        BindingReplayKind::Regular,
        &["device.write"],
        BindingScope::Os,
        BindingBlocking::Sometimes,
    );

/// Binding descriptors for device.
pub const BINDINGS: &[BindingDescriptor] = &[
    DEVICE_CONTROL_CONTROL,
    DEVICE_IO_CLOSE,
    DEVICE_IO_OPEN,
    DEVICE_IO_READ,
    DEVICE_IO_WRITE,
];

/// Native binding set for device.
pub const DEVICE_NATIVE_BINDINGS: NativeBindingSet = NativeBindingSet {
    name: "device",
    bindings: &[
        NativeBinding::new(
            DEVICE_CONTROL_CONTROL,
            "destack.device.control.control",
            destack_device_control_control as *const (),
        ),
        NativeBinding::new(
            DEVICE_IO_CLOSE,
            "destack.device.io.close",
            destack_device_io_close as *const (),
        ),
        NativeBinding::new(
            DEVICE_IO_OPEN,
            "destack.device.io.open",
            destack_device_io_open as *const (),
        ),
        NativeBinding::new(
            DEVICE_IO_READ,
            "destack.device.io.read",
            destack_device_io_read as *const (),
        ),
        NativeBinding::new(
            DEVICE_IO_WRITE,
            "destack.device.io.write",
            destack_device_io_write as *const (),
        ),
    ],
};

/// Native replay implementations for device bindings.
#[inline]
fn destack_device_control_control_replay(
    context: &RuntimeCallContext,
    world: RuntimeWorld,
    out: *mut u64,
    handle: resource::DeviceHandle,
    operation: DeviceControlOperation,
    input: NativeSlice<u8>,
    output: NativeSlice<u8>,
) -> RuntimeResult<()> {
    let _ = (&handle, &operation, &input, &output);

    context.replay().run_binding_with_payload_policy(
        DEVICE_CONTROL_CONTROL,
        context.replay_payload_for(DEVICE_CONTROL_CONTROL)?,
        || match world {
            RuntimeWorld::Host => unsafe {
                platform_native::destack_device_control(
                    context, out, handle, operation, input, output,
                )
            },
            RuntimeWorld::Simulated => unsafe {
                platform_simulated_native::destack_device_control(
                    context, out, handle, operation, input, output,
                )
            },
        },
        |result| {
            if let Ok(()) = result {
                let result_value = unsafe {
                    if out.is_null() {
                        return Err(RuntimeError::from(PlatformError::null_pointer("out")).boxed());
                    }
                    *out
                };
                let result_recorded = result_value;
                let payload = DeviceControlControlReplay {
                    result: Ok(result_recorded),
                };
                return Ok(Some(payload));
            }

            if let Err(error) = result {
                let payload = {
                    let result = Err(PlatformError::from(error.as_ref()));
                    DeviceControlControlReplay { result }
                };
                return Ok(Some(payload));
            }

            Ok(None)
        },
        |payload| {
            // replay result
            match payload.result {
                Ok(value) => {
                    let value_native = value;
                    unsafe {
                        std::ptr::write(out, value_native);
                    }
                    Ok(())
                }
                Err(error) => Err(RuntimeError::from(error).boxed()),
            }
        },
    )
}

#[inline]
fn destack_device_io_close_replay(
    context: &RuntimeCallContext,
    world: RuntimeWorld,
    handle: resource::DeviceHandle,
) -> RuntimeResult<()> {
    let _ = &handle;

    context.replay().run_binding_with_payload_policy(
        DEVICE_IO_CLOSE,
        context.replay_payload_for(DEVICE_IO_CLOSE)?,
        || match world {
            RuntimeWorld::Host => unsafe { platform_native::destack_device_close(context, handle) },
            RuntimeWorld::Simulated => unsafe {
                platform_simulated_native::destack_device_close(context, handle)
            },
        },
        |result| {
            if let Ok(()) = result {
                let result_recorded = ();
                let payload = DeviceIoCloseReplay {
                    result: Ok(result_recorded),
                };
                return Ok(Some(payload));
            }

            if let Err(error) = result {
                let payload = {
                    let result = Err(PlatformError::from(error.as_ref()));
                    DeviceIoCloseReplay { result }
                };
                return Ok(Some(payload));
            }

            Ok(None)
        },
        |payload| {
            // replay result
            match payload.result {
                Ok(()) => Ok(()),
                Err(error) => Err(RuntimeError::from(error).boxed()),
            }
        },
    )
}

#[inline]
fn destack_device_io_open_replay(
    context: &RuntimeCallContext,
    world: RuntimeWorld,
    out: *mut resource::DeviceHandle,
    path: fs::OsPath,
    flags: u32,
    mode: u32,
) -> RuntimeResult<()> {
    let _ = (&path, &flags, &mode);

    context.replay().run_binding_with_payload_policy(
        DEVICE_IO_OPEN,
        context.replay_payload_for(DEVICE_IO_OPEN)?,
        || match world {
            RuntimeWorld::Host => unsafe {
                platform_native::destack_device_open(context, out, path, flags, mode)
            },
            RuntimeWorld::Simulated => unsafe {
                platform_simulated_native::destack_device_open(context, out, path, flags, mode)
            },
        },
        |result| {
            if let Ok(()) = result {
                let result_value = unsafe {
                    if out.is_null() {
                        return Err(RuntimeError::from(PlatformError::null_pointer("out")).boxed());
                    }
                    *out
                };
                let result_recorded = result_value;
                let payload = DeviceIoOpenReplay {
                    result: Ok(result_recorded),
                };
                return Ok(Some(payload));
            }

            if let Err(error) = result {
                let payload = {
                    let result = Err(PlatformError::from(error.as_ref()));
                    DeviceIoOpenReplay { result }
                };
                return Ok(Some(payload));
            }

            Ok(None)
        },
        |payload| {
            // replay result
            match payload.result {
                Ok(value) => {
                    let value_native = value;
                    unsafe {
                        std::ptr::write(out, value_native);
                    }
                    Ok(())
                }
                Err(error) => Err(RuntimeError::from(error).boxed()),
            }
        },
    )
}

#[inline]
fn destack_device_io_read_replay(
    context: &RuntimeCallContext,
    world: RuntimeWorld,
    out: *mut u64,
    handle: resource::DeviceHandle,
    buffer: NativeSlice<u8>,
) -> RuntimeResult<()> {
    let _ = (&handle, &buffer);

    context.replay().run_binding_with_payload_policy(
        DEVICE_IO_READ,
        context.replay_payload_for(DEVICE_IO_READ)?,
        || match world {
            RuntimeWorld::Host => unsafe {
                platform_native::destack_device_read(context, out, handle, buffer)
            },
            RuntimeWorld::Simulated => unsafe {
                platform_simulated_native::destack_device_read(context, out, handle, buffer)
            },
        },
        |result| {
            if let Ok(()) = result {
                let result_value = unsafe {
                    if out.is_null() {
                        return Err(RuntimeError::from(PlatformError::null_pointer("out")).boxed());
                    }
                    *out
                };
                let result_recorded = result_value;
                let payload = DeviceIoReadReplay {
                    result: Ok(result_recorded),
                };
                return Ok(Some(payload));
            }

            if let Err(error) = result {
                let payload = {
                    let result = Err(PlatformError::from(error.as_ref()));
                    DeviceIoReadReplay { result }
                };
                return Ok(Some(payload));
            }

            Ok(None)
        },
        |payload| {
            // replay result
            match payload.result {
                Ok(value) => {
                    let value_native = value;
                    unsafe {
                        std::ptr::write(out, value_native);
                    }
                    Ok(())
                }
                Err(error) => Err(RuntimeError::from(error).boxed()),
            }
        },
    )
}

#[inline]
fn destack_device_io_write_replay(
    context: &RuntimeCallContext,
    world: RuntimeWorld,
    out: *mut u64,
    handle: resource::DeviceHandle,
    buffer: NativeSlice<u8>,
) -> RuntimeResult<()> {
    let _ = (&handle, &buffer);

    context.replay().run_binding_with_payload_policy(
        DEVICE_IO_WRITE,
        context.replay_payload_for(DEVICE_IO_WRITE)?,
        || match world {
            RuntimeWorld::Host => unsafe {
                platform_native::destack_device_write(context, out, handle, buffer)
            },
            RuntimeWorld::Simulated => unsafe {
                platform_simulated_native::destack_device_write(context, out, handle, buffer)
            },
        },
        |result| {
            if let Ok(()) = result {
                let result_value = unsafe {
                    if out.is_null() {
                        return Err(RuntimeError::from(PlatformError::null_pointer("out")).boxed());
                    }
                    *out
                };
                let result_recorded = result_value;
                let payload = DeviceIoWriteReplay {
                    result: Ok(result_recorded),
                };
                return Ok(Some(payload));
            }

            if let Err(error) = result {
                let payload = {
                    let result = Err(PlatformError::from(error.as_ref()));
                    DeviceIoWriteReplay { result }
                };
                return Ok(Some(payload));
            }

            Ok(None)
        },
        |payload| {
            // replay result
            match payload.result {
                Ok(value) => {
                    let value_native = value;
                    unsafe {
                        std::ptr::write(out, value_native);
                    }
                    Ok(())
                }
                Err(error) => Err(RuntimeError::from(error).boxed()),
            }
        },
    )
}

/// Native export wrappers for device bindings.
#[unsafe(export_name = "destack.device.control.control")]
pub unsafe extern "C" fn destack_device_control_control(
    out: *mut u64,
    handle: resource::DeviceHandle,
    operation: DeviceControlOperation,
    input: NativeSlice<u8>,
    output: NativeSlice<u8>,
) -> RuntimeStatus {
    native_call(|context| {
        if out.is_null() {
            return Err(RuntimeError::from(PlatformError::null_pointer("out")).boxed());
        }
        let _ = (&out, &handle, &operation, &input, &output);

        let world = context.check_and_resolve_world(DEVICE_CONTROL_CONTROL)?;
        destack_device_control_control_replay(context, world, out, handle, operation, input, output)
    })
}

#[unsafe(export_name = "destack.device.io.close")]
pub unsafe extern "C" fn destack_device_io_close(handle: resource::DeviceHandle) -> RuntimeStatus {
    native_call(|context| {
        let _ = &handle;

        let world = context.check_and_resolve_world(DEVICE_IO_CLOSE)?;
        destack_device_io_close_replay(context, world, handle)
    })
}

#[unsafe(export_name = "destack.device.io.open")]
pub unsafe extern "C" fn destack_device_io_open(
    out: *mut resource::DeviceHandle,
    path: fs::OsPath,
    flags: u32,
    mode: u32,
) -> RuntimeStatus {
    native_call(|context| {
        if out.is_null() {
            return Err(RuntimeError::from(PlatformError::null_pointer("out")).boxed());
        }
        let _ = (&out, &path, &flags, &mode);

        let world = context.check_and_resolve_world(DEVICE_IO_OPEN)?;
        destack_device_io_open_replay(context, world, out, path, flags, mode)
    })
}

#[unsafe(export_name = "destack.device.io.read")]
pub unsafe extern "C" fn destack_device_io_read(
    out: *mut u64,
    handle: resource::DeviceHandle,
    buffer: NativeSlice<u8>,
) -> RuntimeStatus {
    native_call(|context| {
        if out.is_null() {
            return Err(RuntimeError::from(PlatformError::null_pointer("out")).boxed());
        }
        let _ = (&out, &handle, &buffer);

        let world = context.check_and_resolve_world(DEVICE_IO_READ)?;
        destack_device_io_read_replay(context, world, out, handle, buffer)
    })
}

#[unsafe(export_name = "destack.device.io.write")]
pub unsafe extern "C" fn destack_device_io_write(
    out: *mut u64,
    handle: resource::DeviceHandle,
    buffer: NativeSlice<u8>,
) -> RuntimeStatus {
    native_call(|context| {
        if out.is_null() {
            return Err(RuntimeError::from(PlatformError::null_pointer("out")).boxed());
        }
        let _ = (&out, &handle, &buffer);

        let world = context.check_and_resolve_world(DEVICE_IO_WRITE)?;
        destack_device_io_write_replay(context, world, out, handle, buffer)
    })
}

/// VM replay implementations for device bindings.
#[inline]
fn destack_device_control_control_vm_replay(
    runtime: &RuntimeCallContext,
    context: &mut vm::ExternalCallContext<'_>,
    world: RuntimeWorld,
    handle: resource::DeviceHandle,
    operation: DeviceControlOperationVm,
    input: VmSlice<u8>,
    output: VmSlice<u8>,
) -> RuntimeResult<vm::Value> {
    let result = runtime
        .replay()
        .run_binding_with_context_and_payload_policy(
            DEVICE_CONTROL_CONTROL,
            runtime.replay_payload_for(DEVICE_CONTROL_CONTROL)?,
            context,
            |context| match world {
                RuntimeWorld::Host => platform_vm::destack_device_control(
                    runtime, context, handle, operation, input, output,
                ),
                RuntimeWorld::Simulated => platform_simulated_vm::destack_device_control(
                    runtime, context, handle, operation, input, output,
                ),
            },
            |context, result| {
                let _ = &context;
                if let Ok(value) = result {
                    let result_value: u64 = value.clone();
                    let result_recorded = result_value;
                    let payload = DeviceControlControlReplay {
                        result: Ok(result_recorded),
                    };
                    return Ok(Some(payload));
                }

                if let Err(error) = result {
                    let payload = {
                        let result = Err(PlatformError::from(error.as_ref()));
                        DeviceControlControlReplay { result }
                    };
                    return Ok(Some(payload));
                }

                Ok(None)
            },
            |context, payload| {
                let _ = &context;
                // replay result
                match payload.result {
                    Ok(value) => {
                        let vm_result = value;
                        Ok(vm_result)
                    }
                    Err(error) => Err(RuntimeError::from(error).boxed()),
                }
            },
        );
    let result = encode_destack_device_control_control_result(context, result)?;
    Ok(result)
}

#[inline]
fn destack_device_io_close_vm_replay(
    runtime: &RuntimeCallContext,
    context: &mut vm::ExternalCallContext<'_>,
    world: RuntimeWorld,
    handle: resource::DeviceHandle,
) -> RuntimeResult<vm::Value> {
    let result = runtime
        .replay()
        .run_binding_with_context_and_payload_policy(
            DEVICE_IO_CLOSE,
            runtime.replay_payload_for(DEVICE_IO_CLOSE)?,
            context,
            |context| match world {
                RuntimeWorld::Host => platform_vm::destack_device_close(runtime, context, handle),
                RuntimeWorld::Simulated => {
                    platform_simulated_vm::destack_device_close(runtime, context, handle)
                }
            },
            |context, result| {
                let _ = &context;
                if let Ok(()) = result {
                    let result_recorded = ();
                    let payload = DeviceIoCloseReplay {
                        result: Ok(result_recorded),
                    };
                    return Ok(Some(payload));
                }

                if let Err(error) = result {
                    let payload = {
                        let result = Err(PlatformError::from(error.as_ref()));
                        DeviceIoCloseReplay { result }
                    };
                    return Ok(Some(payload));
                }

                Ok(None)
            },
            |context, payload| {
                let _ = &context;
                // replay result
                match payload.result {
                    Ok(()) => Ok(()),
                    Err(error) => Err(RuntimeError::from(error).boxed()),
                }
            },
        );
    let result = encode_destack_device_io_close_result(context, result)?;
    Ok(result)
}

#[inline]
fn destack_device_io_open_vm_replay(
    runtime: &RuntimeCallContext,
    context: &mut vm::ExternalCallContext<'_>,
    world: RuntimeWorld,
    path: fs::OsPathVm,
    flags: u32,
    mode: u32,
) -> RuntimeResult<vm::Value> {
    let result = runtime
        .replay()
        .run_binding_with_context_and_payload_policy(
            DEVICE_IO_OPEN,
            runtime.replay_payload_for(DEVICE_IO_OPEN)?,
            context,
            |context| match world {
                RuntimeWorld::Host => {
                    platform_vm::destack_device_open(runtime, context, path, flags, mode)
                }
                RuntimeWorld::Simulated => {
                    platform_simulated_vm::destack_device_open(runtime, context, path, flags, mode)
                }
            },
            |context, result| {
                let _ = &context;
                if let Ok(value) = result {
                    let result_value: resource::DeviceHandle = value.clone();
                    let result_recorded = result_value;
                    let payload = DeviceIoOpenReplay {
                        result: Ok(result_recorded),
                    };
                    return Ok(Some(payload));
                }

                if let Err(error) = result {
                    let payload = {
                        let result = Err(PlatformError::from(error.as_ref()));
                        DeviceIoOpenReplay { result }
                    };
                    return Ok(Some(payload));
                }

                Ok(None)
            },
            |context, payload| {
                let _ = &context;
                // replay result
                match payload.result {
                    Ok(value) => {
                        let vm_result = value;
                        Ok(vm_result)
                    }
                    Err(error) => Err(RuntimeError::from(error).boxed()),
                }
            },
        );
    let result = encode_destack_device_io_open_result(context, result)?;
    Ok(result)
}

#[inline]
fn destack_device_io_read_vm_replay(
    runtime: &RuntimeCallContext,
    context: &mut vm::ExternalCallContext<'_>,
    world: RuntimeWorld,
    handle: resource::DeviceHandle,
    buffer: VmSlice<u8>,
) -> RuntimeResult<vm::Value> {
    let result = runtime
        .replay()
        .run_binding_with_context_and_payload_policy(
            DEVICE_IO_READ,
            runtime.replay_payload_for(DEVICE_IO_READ)?,
            context,
            |context| match world {
                RuntimeWorld::Host => {
                    platform_vm::destack_device_read(runtime, context, handle, buffer)
                }
                RuntimeWorld::Simulated => {
                    platform_simulated_vm::destack_device_read(runtime, context, handle, buffer)
                }
            },
            |context, result| {
                let _ = &context;
                if let Ok(value) = result {
                    let result_value: u64 = value.clone();
                    let result_recorded = result_value;
                    let payload = DeviceIoReadReplay {
                        result: Ok(result_recorded),
                    };
                    return Ok(Some(payload));
                }

                if let Err(error) = result {
                    let payload = {
                        let result = Err(PlatformError::from(error.as_ref()));
                        DeviceIoReadReplay { result }
                    };
                    return Ok(Some(payload));
                }

                Ok(None)
            },
            |context, payload| {
                let _ = &context;
                // replay result
                match payload.result {
                    Ok(value) => {
                        let vm_result = value;
                        Ok(vm_result)
                    }
                    Err(error) => Err(RuntimeError::from(error).boxed()),
                }
            },
        );
    let result = encode_destack_device_io_read_result(context, result)?;
    Ok(result)
}

#[inline]
fn destack_device_io_write_vm_replay(
    runtime: &RuntimeCallContext,
    context: &mut vm::ExternalCallContext<'_>,
    world: RuntimeWorld,
    handle: resource::DeviceHandle,
    buffer: VmSlice<u8>,
) -> RuntimeResult<vm::Value> {
    let result = runtime
        .replay()
        .run_binding_with_context_and_payload_policy(
            DEVICE_IO_WRITE,
            runtime.replay_payload_for(DEVICE_IO_WRITE)?,
            context,
            |context| match world {
                RuntimeWorld::Host => {
                    platform_vm::destack_device_write(runtime, context, handle, buffer)
                }
                RuntimeWorld::Simulated => {
                    platform_simulated_vm::destack_device_write(runtime, context, handle, buffer)
                }
            },
            |context, result| {
                let _ = &context;
                if let Ok(value) = result {
                    let result_value: u64 = value.clone();
                    let result_recorded = result_value;
                    let payload = DeviceIoWriteReplay {
                        result: Ok(result_recorded),
                    };
                    return Ok(Some(payload));
                }

                if let Err(error) = result {
                    let payload = {
                        let result = Err(PlatformError::from(error.as_ref()));
                        DeviceIoWriteReplay { result }
                    };
                    return Ok(Some(payload));
                }

                Ok(None)
            },
            |context, payload| {
                let _ = &context;
                // replay result
                match payload.result {
                    Ok(value) => {
                        let vm_result = value;
                        Ok(vm_result)
                    }
                    Err(error) => Err(RuntimeError::from(error).boxed()),
                }
            },
        );
    let result = encode_destack_device_io_write_result(context, result)?;
    Ok(result)
}

/// Register VM bindings for device.
pub fn register_device_vm_bindings(registry: &mut BindingRegistry, isolate: &mut Isolate) {
    {
        binding!(
            registry,
            isolate,
            DEVICE_CONTROL_CONTROL,
            move |context, args| {
                with_runtime_call_context(|runtime| {
                    // decode args
                    let (handle, operation, input, output) =
                        decode_destack_device_control_control_args(context, args)?;

                    // execute binding
                    let world = runtime.check_and_resolve_world(DEVICE_CONTROL_CONTROL)?;
                    destack_device_control_control_vm_replay(
                        runtime, context, world, handle, operation, input, output,
                    )
                })
                .map_err(Into::into)
            }
        );
    }
    {
        binding!(registry, isolate, DEVICE_IO_CLOSE, move |context, args| {
            with_runtime_call_context(|runtime| {
                // decode args
                let (handle,) = decode_destack_device_io_close_args(context, args)?;

                // execute binding
                let world = runtime.check_and_resolve_world(DEVICE_IO_CLOSE)?;
                destack_device_io_close_vm_replay(runtime, context, world, handle)
            })
            .map_err(Into::into)
        });
    }
    {
        binding!(registry, isolate, DEVICE_IO_OPEN, move |context, args| {
            with_runtime_call_context(|runtime| {
                // decode args
                let (path, flags, mode) = decode_destack_device_io_open_args(context, args)?;

                // execute binding
                let world = runtime.check_and_resolve_world(DEVICE_IO_OPEN)?;
                destack_device_io_open_vm_replay(runtime, context, world, path, flags, mode)
            })
            .map_err(Into::into)
        });
    }
    {
        binding!(registry, isolate, DEVICE_IO_READ, move |context, args| {
            with_runtime_call_context(|runtime| {
                // decode args
                let (handle, buffer) = decode_destack_device_io_read_args(context, args)?;

                // execute binding
                let world = runtime.check_and_resolve_world(DEVICE_IO_READ)?;
                destack_device_io_read_vm_replay(runtime, context, world, handle, buffer)
            })
            .map_err(Into::into)
        });
    }
    {
        binding!(registry, isolate, DEVICE_IO_WRITE, move |context, args| {
            with_runtime_call_context(|runtime| {
                // decode args
                let (handle, buffer) = decode_destack_device_io_write_args(context, args)?;

                // execute binding
                let world = runtime.check_and_resolve_world(DEVICE_IO_WRITE)?;
                destack_device_io_write_vm_replay(runtime, context, world, handle, buffer)
            })
            .map_err(Into::into)
        });
    }
}

/// Install VM bindings for device.
pub fn install_device_vm_bindings(registry: &mut BindingRegistry, isolate: &mut Isolate) {
    register_device_vm_bindings(registry, isolate);
}

vm_binding_set!(pub DEVICE_VM_BINDINGS, "device", install_device_vm_bindings);
