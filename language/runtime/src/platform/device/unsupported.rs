// generated by generate-bindings: stub, do not edit

#![allow(clippy::missing_safety_doc)]
use crate::diagnostic::{RuntimeError, RuntimeResult};
use crate::platform::PlatformError;
use crate::platform::abi::{NativeArray, NativeSlice, NativeStringRef};

use crate::runtime::BindingCallContext;

use crate::platform::device::{
    BluetoothAdapterDescriptor, BluetoothAdapterEvent, BluetoothDeviceDescriptor,
    BluetoothGattCharacteristic, BluetoothGattDescriptor, BluetoothGattService,
    BluetoothGattValueEvent, BluetoothGattWriteMode, BluetoothScanEvent, BluetoothScanFilter,
    BluetoothSessionEvent, CameraControlCapabilities, CameraControlPatch, CameraControlState,
    CameraDeviceDescriptor, CameraFrame, CameraPhoto, CameraPhotoCapabilities, CameraPhotoSettings,
    CameraPhotoState, CameraRecording, CameraRecordingCapabilities, CameraRecordingOptions,
    CameraRecordingState, CameraStreamCapability, CameraStreamConfig, CameraWatchEvent,
    MidiBackendDescriptor, MidiEvent, MidiEventSubscriptionOptions, MidiInputPortOpenOptions,
    MidiInputRecord, MidiOutputPortOpenOptions, MidiOutputRecord, MidiPortDescriptor,
    MidiPortListOptions, MidiVirtualInputCreateOptions, MidiVirtualOutputCreateOptions,
    SerialEvent, SerialInputSignals, SerialOutputSignals, SerialPortConfig, SerialPortDescriptor,
    SerialPortOpenOptions, SerialWatchEvent, UsbBosCapabilityDescriptor,
    UsbConfigurationDescriptor, UsbControlSetup, UsbDeviceDescriptor, UsbEndpointSelector,
    UsbHotplugEvent, UsbInTransferResult, UsbIsochronousTransferResult, UsbOutTransferResult,
    UsbStringDescriptor,
};
use crate::platform::resource;

/// Binding for `destack.device.bluetooth.adapterList`.
pub(crate) unsafe fn destack_device_bluetooth_adapter_list(
    _binding: &BindingCallContext,
    out: *mut NativeSlice<BluetoothAdapterDescriptor>,
) -> RuntimeResult<()> {
    if out.is_null() {
        return Err(RuntimeError::from(PlatformError::null_pointer("out")).boxed());
    }
    let _ = out;

    Err(RuntimeError::from(PlatformError::not_supported(
        "destack.device.bluetooth.adapterList",
    ))
    .boxed())
}

/// Binding for `destack.device.bluetooth.adapterWatchClose`.
pub(crate) unsafe fn destack_device_bluetooth_adapter_watch_close(
    _binding: &BindingCallContext,
    handle: resource::BluetoothAdapterWatchHandle,
) -> RuntimeResult<()> {
    let _ = handle;

    Err(RuntimeError::from(PlatformError::not_supported(
        "destack.device.bluetooth.adapterWatchClose",
    ))
    .boxed())
}

/// Binding for `destack.device.bluetooth.adapterWatchOpen`.
pub(crate) unsafe fn destack_device_bluetooth_adapter_watch_open(
    _binding: &BindingCallContext,
    out: *mut resource::BluetoothAdapterWatchHandle,
) -> RuntimeResult<()> {
    if out.is_null() {
        return Err(RuntimeError::from(PlatformError::null_pointer("out")).boxed());
    }
    let _ = out;

    Err(RuntimeError::from(PlatformError::not_supported(
        "destack.device.bluetooth.adapterWatchOpen",
    ))
    .boxed())
}

/// Binding for `destack.device.bluetooth.adapterWatchRead`.
pub(crate) unsafe fn destack_device_bluetooth_adapter_watch_read(
    _binding: &BindingCallContext,
    out: *mut BluetoothAdapterEvent,
    handle: resource::BluetoothAdapterWatchHandle,
    timeoutns: u64,
) -> RuntimeResult<()> {
    if out.is_null() {
        return Err(RuntimeError::from(PlatformError::null_pointer("out")).boxed());
    }
    let _ = (out, handle, timeoutns);

    Err(RuntimeError::from(PlatformError::not_supported(
        "destack.device.bluetooth.adapterWatchRead",
    ))
    .boxed())
}

/// Binding for `destack.device.bluetooth.adapterWatchTryRead`.
pub(crate) unsafe fn destack_device_bluetooth_adapter_watch_try_read(
    _binding: &BindingCallContext,
    out: *mut BluetoothAdapterEvent,
    handle: resource::BluetoothAdapterWatchHandle,
) -> RuntimeResult<()> {
    if out.is_null() {
        return Err(RuntimeError::from(PlatformError::null_pointer("out")).boxed());
    }
    let _ = (out, handle);

    Err(RuntimeError::from(PlatformError::not_supported(
        "destack.device.bluetooth.adapterWatchTryRead",
    ))
    .boxed())
}

/// Binding for `destack.device.bluetooth.gatt.characteristicList`.
pub(crate) unsafe fn destack_device_bluetooth_gatt_characteristic_list(
    _binding: &BindingCallContext,
    out: *mut NativeSlice<BluetoothGattCharacteristic>,
    handle: resource::BluetoothDeviceHandle,
    serviceid: NativeStringRef,
) -> RuntimeResult<()> {
    if out.is_null() {
        return Err(RuntimeError::from(PlatformError::null_pointer("out")).boxed());
    }
    let _ = (out, handle, serviceid);

    Err(RuntimeError::from(PlatformError::not_supported(
        "destack.device.bluetooth.gatt.characteristicList",
    ))
    .boxed())
}

/// Binding for `destack.device.bluetooth.gatt.descriptorList`.
pub(crate) unsafe fn destack_device_bluetooth_gatt_descriptor_list(
    _binding: &BindingCallContext,
    out: *mut NativeSlice<BluetoothGattDescriptor>,
    handle: resource::BluetoothDeviceHandle,
    characteristicid: NativeStringRef,
) -> RuntimeResult<()> {
    if out.is_null() {
        return Err(RuntimeError::from(PlatformError::null_pointer("out")).boxed());
    }
    let _ = (out, handle, characteristicid);

    Err(RuntimeError::from(PlatformError::not_supported(
        "destack.device.bluetooth.gatt.descriptorList",
    ))
    .boxed())
}

/// Binding for `destack.device.bluetooth.gatt.mtu`.
pub(crate) unsafe fn destack_device_bluetooth_gatt_mtu(
    _binding: &BindingCallContext,
    out: *mut u16,
    handle: resource::BluetoothDeviceHandle,
) -> RuntimeResult<()> {
    if out.is_null() {
        return Err(RuntimeError::from(PlatformError::null_pointer("out")).boxed());
    }
    let _ = (out, handle);

    Err(RuntimeError::from(PlatformError::not_supported(
        "destack.device.bluetooth.gatt.mtu",
    ))
    .boxed())
}

/// Binding for `destack.device.bluetooth.gatt.read`.
pub(crate) unsafe fn destack_device_bluetooth_gatt_read(
    _binding: &BindingCallContext,
    out: *mut NativeSlice<u8>,
    handle: resource::BluetoothDeviceHandle,
    characteristicid: NativeStringRef,
    timeoutns: u64,
) -> RuntimeResult<()> {
    if out.is_null() {
        return Err(RuntimeError::from(PlatformError::null_pointer("out")).boxed());
    }
    let _ = (out, handle, characteristicid, timeoutns);

    Err(RuntimeError::from(PlatformError::not_supported(
        "destack.device.bluetooth.gatt.read",
    ))
    .boxed())
}

/// Binding for `destack.device.bluetooth.gatt.readDescriptor`.
pub(crate) unsafe fn destack_device_bluetooth_gatt_read_descriptor(
    _binding: &BindingCallContext,
    out: *mut NativeSlice<u8>,
    handle: resource::BluetoothDeviceHandle,
    descriptorid: NativeStringRef,
    timeoutns: u64,
) -> RuntimeResult<()> {
    if out.is_null() {
        return Err(RuntimeError::from(PlatformError::null_pointer("out")).boxed());
    }
    let _ = (out, handle, descriptorid, timeoutns);

    Err(RuntimeError::from(PlatformError::not_supported(
        "destack.device.bluetooth.gatt.readDescriptor",
    ))
    .boxed())
}

/// Binding for `destack.device.bluetooth.gatt.readEvent`.
pub(crate) unsafe fn destack_device_bluetooth_gatt_read_event(
    _binding: &BindingCallContext,
    out: *mut BluetoothGattValueEvent,
    handle: resource::BluetoothSubscriptionHandle,
    timeoutns: u64,
) -> RuntimeResult<()> {
    if out.is_null() {
        return Err(RuntimeError::from(PlatformError::null_pointer("out")).boxed());
    }
    let _ = (out, handle, timeoutns);

    Err(RuntimeError::from(PlatformError::not_supported(
        "destack.device.bluetooth.gatt.readEvent",
    ))
    .boxed())
}

/// Binding for `destack.device.bluetooth.gatt.serviceList`.
pub(crate) unsafe fn destack_device_bluetooth_gatt_service_list(
    _binding: &BindingCallContext,
    out: *mut NativeSlice<BluetoothGattService>,
    handle: resource::BluetoothDeviceHandle,
) -> RuntimeResult<()> {
    if out.is_null() {
        return Err(RuntimeError::from(PlatformError::null_pointer("out")).boxed());
    }
    let _ = (out, handle);

    Err(RuntimeError::from(PlatformError::not_supported(
        "destack.device.bluetooth.gatt.serviceList",
    ))
    .boxed())
}

/// Binding for `destack.device.bluetooth.gatt.subscribe`.
pub(crate) unsafe fn destack_device_bluetooth_gatt_subscribe(
    _binding: &BindingCallContext,
    out: *mut resource::BluetoothSubscriptionHandle,
    handle: resource::BluetoothDeviceHandle,
    characteristicid: NativeStringRef,
) -> RuntimeResult<()> {
    if out.is_null() {
        return Err(RuntimeError::from(PlatformError::null_pointer("out")).boxed());
    }
    let _ = (out, handle, characteristicid);

    Err(RuntimeError::from(PlatformError::not_supported(
        "destack.device.bluetooth.gatt.subscribe",
    ))
    .boxed())
}

/// Binding for `destack.device.bluetooth.gatt.tryReadEvent`.
pub(crate) unsafe fn destack_device_bluetooth_gatt_try_read_event(
    _binding: &BindingCallContext,
    out: *mut BluetoothGattValueEvent,
    handle: resource::BluetoothSubscriptionHandle,
) -> RuntimeResult<()> {
    if out.is_null() {
        return Err(RuntimeError::from(PlatformError::null_pointer("out")).boxed());
    }
    let _ = (out, handle);

    Err(RuntimeError::from(PlatformError::not_supported(
        "destack.device.bluetooth.gatt.tryReadEvent",
    ))
    .boxed())
}

/// Binding for `destack.device.bluetooth.gatt.unsubscribe`.
pub(crate) unsafe fn destack_device_bluetooth_gatt_unsubscribe(
    _binding: &BindingCallContext,
    handle: resource::BluetoothSubscriptionHandle,
) -> RuntimeResult<()> {
    let _ = handle;

    Err(RuntimeError::from(PlatformError::not_supported(
        "destack.device.bluetooth.gatt.unsubscribe",
    ))
    .boxed())
}

/// Binding for `destack.device.bluetooth.gatt.write`.
pub(crate) unsafe fn destack_device_bluetooth_gatt_write(
    _binding: &BindingCallContext,
    handle: resource::BluetoothDeviceHandle,
    characteristicid: NativeStringRef,
    argument_value: NativeSlice<u8>,
    mode: BluetoothGattWriteMode,
    timeoutns: u64,
) -> RuntimeResult<()> {
    let _ = (handle, characteristicid, argument_value, mode, timeoutns);

    Err(RuntimeError::from(PlatformError::not_supported(
        "destack.device.bluetooth.gatt.write",
    ))
    .boxed())
}

/// Binding for `destack.device.bluetooth.gatt.writeDescriptor`.
pub(crate) unsafe fn destack_device_bluetooth_gatt_write_descriptor(
    _binding: &BindingCallContext,
    handle: resource::BluetoothDeviceHandle,
    descriptorid: NativeStringRef,
    argument_value: NativeSlice<u8>,
    timeoutns: u64,
) -> RuntimeResult<()> {
    let _ = (handle, descriptorid, argument_value, timeoutns);

    Err(RuntimeError::from(PlatformError::not_supported(
        "destack.device.bluetooth.gatt.writeDescriptor",
    ))
    .boxed())
}

/// Binding for `destack.device.bluetooth.scan.close`.
pub(crate) unsafe fn destack_device_bluetooth_scan_close(
    _binding: &BindingCallContext,
    handle: resource::BluetoothScanHandle,
) -> RuntimeResult<()> {
    let _ = handle;

    Err(RuntimeError::from(PlatformError::not_supported(
        "destack.device.bluetooth.scan.close",
    ))
    .boxed())
}

/// Binding for `destack.device.bluetooth.scan.open`.
pub(crate) unsafe fn destack_device_bluetooth_scan_open(
    _binding: &BindingCallContext,
    out: *mut resource::BluetoothScanHandle,
    adapterid: NativeStringRef,
    filter: Option<BluetoothScanFilter>,
) -> RuntimeResult<()> {
    if out.is_null() {
        return Err(RuntimeError::from(PlatformError::null_pointer("out")).boxed());
    }
    let _ = (out, adapterid, filter);

    Err(RuntimeError::from(PlatformError::not_supported(
        "destack.device.bluetooth.scan.open",
    ))
    .boxed())
}

/// Binding for `destack.device.bluetooth.scan.readEvent`.
pub(crate) unsafe fn destack_device_bluetooth_scan_read_event(
    _binding: &BindingCallContext,
    out: *mut BluetoothScanEvent,
    handle: resource::BluetoothScanHandle,
    timeoutns: u64,
) -> RuntimeResult<()> {
    if out.is_null() {
        return Err(RuntimeError::from(PlatformError::null_pointer("out")).boxed());
    }
    let _ = (out, handle, timeoutns);

    Err(RuntimeError::from(PlatformError::not_supported(
        "destack.device.bluetooth.scan.readEvent",
    ))
    .boxed())
}

/// Binding for `destack.device.bluetooth.scan.tryReadEvent`.
pub(crate) unsafe fn destack_device_bluetooth_scan_try_read_event(
    _binding: &BindingCallContext,
    out: *mut BluetoothScanEvent,
    handle: resource::BluetoothScanHandle,
) -> RuntimeResult<()> {
    if out.is_null() {
        return Err(RuntimeError::from(PlatformError::null_pointer("out")).boxed());
    }
    let _ = (out, handle);

    Err(RuntimeError::from(PlatformError::not_supported(
        "destack.device.bluetooth.scan.tryReadEvent",
    ))
    .boxed())
}

/// Binding for `destack.device.bluetooth.session.close`.
pub(crate) unsafe fn destack_device_bluetooth_close(
    _binding: &BindingCallContext,
    handle: resource::BluetoothDeviceHandle,
) -> RuntimeResult<()> {
    let _ = handle;

    Err(RuntimeError::from(PlatformError::not_supported(
        "destack.device.bluetooth.session.close",
    ))
    .boxed())
}

/// Binding for `destack.device.bluetooth.session.descriptor`.
pub(crate) unsafe fn destack_device_bluetooth_descriptor(
    _binding: &BindingCallContext,
    out: *mut BluetoothDeviceDescriptor,
    handle: resource::BluetoothDeviceHandle,
) -> RuntimeResult<()> {
    if out.is_null() {
        return Err(RuntimeError::from(PlatformError::null_pointer("out")).boxed());
    }
    let _ = (out, handle);

    Err(RuntimeError::from(PlatformError::not_supported(
        "destack.device.bluetooth.session.descriptor",
    ))
    .boxed())
}

/// Binding for `destack.device.bluetooth.session.open`.
pub(crate) unsafe fn destack_device_bluetooth_open(
    _binding: &BindingCallContext,
    out: *mut resource::BluetoothDeviceHandle,
    adapterid: NativeStringRef,
    deviceid: NativeStringRef,
) -> RuntimeResult<()> {
    if out.is_null() {
        return Err(RuntimeError::from(PlatformError::null_pointer("out")).boxed());
    }
    let _ = (out, adapterid, deviceid);

    Err(RuntimeError::from(PlatformError::not_supported(
        "destack.device.bluetooth.session.open",
    ))
    .boxed())
}

/// Binding for `destack.device.bluetooth.session.pair`.
pub(crate) unsafe fn destack_device_bluetooth_pair(
    _binding: &BindingCallContext,
    handle: resource::BluetoothDeviceHandle,
    timeoutns: u64,
) -> RuntimeResult<()> {
    let _ = (handle, timeoutns);

    Err(RuntimeError::from(PlatformError::not_supported(
        "destack.device.bluetooth.session.pair",
    ))
    .boxed())
}

/// Binding for `destack.device.bluetooth.session.readEvent`.
pub(crate) unsafe fn destack_device_bluetooth_session_read_event(
    _binding: &BindingCallContext,
    out: *mut BluetoothSessionEvent,
    handle: resource::BluetoothDeviceHandle,
    timeoutns: u64,
) -> RuntimeResult<()> {
    if out.is_null() {
        return Err(RuntimeError::from(PlatformError::null_pointer("out")).boxed());
    }
    let _ = (out, handle, timeoutns);

    Err(RuntimeError::from(PlatformError::not_supported(
        "destack.device.bluetooth.session.readEvent",
    ))
    .boxed())
}

/// Binding for `destack.device.bluetooth.session.rssi`.
pub(crate) unsafe fn destack_device_bluetooth_read_rssi(
    _binding: &BindingCallContext,
    out: *mut i32,
    handle: resource::BluetoothDeviceHandle,
    timeoutns: u64,
) -> RuntimeResult<()> {
    if out.is_null() {
        return Err(RuntimeError::from(PlatformError::null_pointer("out")).boxed());
    }
    let _ = (out, handle, timeoutns);

    Err(RuntimeError::from(PlatformError::not_supported(
        "destack.device.bluetooth.session.rssi",
    ))
    .boxed())
}

/// Binding for `destack.device.bluetooth.session.tryReadEvent`.
pub(crate) unsafe fn destack_device_bluetooth_session_try_read_event(
    _binding: &BindingCallContext,
    out: *mut BluetoothSessionEvent,
    handle: resource::BluetoothDeviceHandle,
) -> RuntimeResult<()> {
    if out.is_null() {
        return Err(RuntimeError::from(PlatformError::null_pointer("out")).boxed());
    }
    let _ = (out, handle);

    Err(RuntimeError::from(PlatformError::not_supported(
        "destack.device.bluetooth.session.tryReadEvent",
    ))
    .boxed())
}

/// Binding for `destack.device.bluetooth.session.unpair`.
pub(crate) unsafe fn destack_device_bluetooth_unpair(
    _binding: &BindingCallContext,
    adapterid: NativeStringRef,
    deviceid: NativeStringRef,
) -> RuntimeResult<()> {
    let _ = (adapterid, deviceid);

    Err(RuntimeError::from(PlatformError::not_supported(
        "destack.device.bluetooth.session.unpair",
    ))
    .boxed())
}

/// Binding for `destack.device.camera.device.close`.
pub(crate) unsafe fn destack_device_camera_device_close(
    _binding: &BindingCallContext,
    handle: resource::CameraDeviceHandle,
) -> RuntimeResult<()> {
    let _ = handle;

    Err(RuntimeError::from(PlatformError::not_supported(
        "destack.device.camera.device.close",
    ))
    .boxed())
}

/// Binding for `destack.device.camera.device.list`.
pub(crate) unsafe fn destack_device_camera_device_list(
    _binding: &BindingCallContext,
    out: *mut NativeSlice<CameraDeviceDescriptor>,
) -> RuntimeResult<()> {
    if out.is_null() {
        return Err(RuntimeError::from(PlatformError::null_pointer("out")).boxed());
    }
    let _ = out;

    Err(RuntimeError::from(PlatformError::not_supported(
        "destack.device.camera.device.list",
    ))
    .boxed())
}

/// Binding for `destack.device.camera.device.open`.
pub(crate) unsafe fn destack_device_camera_device_open(
    _binding: &BindingCallContext,
    out: *mut resource::CameraDeviceHandle,
    id: NativeStringRef,
) -> RuntimeResult<()> {
    if out.is_null() {
        return Err(RuntimeError::from(PlatformError::null_pointer("out")).boxed());
    }
    let _ = (out, id);

    Err(RuntimeError::from(PlatformError::not_supported(
        "destack.device.camera.device.open",
    ))
    .boxed())
}

/// Binding for `destack.device.camera.device.streamCapabilityList`.
pub(crate) unsafe fn destack_device_camera_device_stream_capability_list(
    _binding: &BindingCallContext,
    out: *mut NativeSlice<CameraStreamCapability>,
    handle: resource::CameraDeviceHandle,
) -> RuntimeResult<()> {
    if out.is_null() {
        return Err(RuntimeError::from(PlatformError::null_pointer("out")).boxed());
    }
    let _ = (out, handle);

    Err(RuntimeError::from(PlatformError::not_supported(
        "destack.device.camera.device.streamCapabilityList",
    ))
    .boxed())
}

/// Binding for `destack.device.camera.device.watchClose`.
pub(crate) unsafe fn destack_device_camera_device_watch_close(
    _binding: &BindingCallContext,
    handle: resource::CameraWatchHandle,
) -> RuntimeResult<()> {
    let _ = handle;

    Err(RuntimeError::from(PlatformError::not_supported(
        "destack.device.camera.device.watchClose",
    ))
    .boxed())
}

/// Binding for `destack.device.camera.device.watchOpen`.
pub(crate) unsafe fn destack_device_camera_device_watch_open(
    _binding: &BindingCallContext,
    out: *mut resource::CameraWatchHandle,
) -> RuntimeResult<()> {
    if out.is_null() {
        return Err(RuntimeError::from(PlatformError::null_pointer("out")).boxed());
    }
    let _ = out;

    Err(RuntimeError::from(PlatformError::not_supported(
        "destack.device.camera.device.watchOpen",
    ))
    .boxed())
}

/// Binding for `destack.device.camera.device.watchRead`.
pub(crate) unsafe fn destack_device_camera_device_watch_read(
    _binding: &BindingCallContext,
    out: *mut CameraWatchEvent,
    handle: resource::CameraWatchHandle,
    timeoutns: u64,
) -> RuntimeResult<()> {
    if out.is_null() {
        return Err(RuntimeError::from(PlatformError::null_pointer("out")).boxed());
    }
    let _ = (out, handle, timeoutns);

    Err(RuntimeError::from(PlatformError::not_supported(
        "destack.device.camera.device.watchRead",
    ))
    .boxed())
}

/// Binding for `destack.device.camera.device.watchTryRead`.
pub(crate) unsafe fn destack_device_camera_device_watch_try_read(
    _binding: &BindingCallContext,
    out: *mut CameraWatchEvent,
    handle: resource::CameraWatchHandle,
) -> RuntimeResult<()> {
    if out.is_null() {
        return Err(RuntimeError::from(PlatformError::null_pointer("out")).boxed());
    }
    let _ = (out, handle);

    Err(RuntimeError::from(PlatformError::not_supported(
        "destack.device.camera.device.watchTryRead",
    ))
    .boxed())
}

/// Binding for `destack.device.camera.stream.close`.
pub(crate) unsafe fn destack_device_camera_stream_close(
    _binding: &BindingCallContext,
    handle: resource::CameraStreamHandle,
) -> RuntimeResult<()> {
    let _ = handle;

    Err(RuntimeError::from(PlatformError::not_supported(
        "destack.device.camera.stream.close",
    ))
    .boxed())
}

/// Binding for `destack.device.camera.stream.config`.
pub(crate) unsafe fn destack_device_camera_stream_config(
    _binding: &BindingCallContext,
    out: *mut CameraStreamConfig,
    handle: resource::CameraStreamHandle,
) -> RuntimeResult<()> {
    if out.is_null() {
        return Err(RuntimeError::from(PlatformError::null_pointer("out")).boxed());
    }
    let _ = (out, handle);

    Err(RuntimeError::from(PlatformError::not_supported(
        "destack.device.camera.stream.config",
    ))
    .boxed())
}

/// Binding for `destack.device.camera.stream.configureControls`.
pub(crate) unsafe fn destack_device_camera_stream_configure_controls(
    _binding: &BindingCallContext,
    handle: resource::CameraStreamHandle,
    controls: CameraControlPatch,
) -> RuntimeResult<()> {
    let _ = (handle, controls);

    Err(RuntimeError::from(PlatformError::not_supported(
        "destack.device.camera.stream.configureControls",
    ))
    .boxed())
}

/// Binding for `destack.device.camera.stream.controlCapabilities`.
pub(crate) unsafe fn destack_device_camera_stream_control_capabilities(
    _binding: &BindingCallContext,
    out: *mut CameraControlCapabilities,
    handle: resource::CameraStreamHandle,
) -> RuntimeResult<()> {
    if out.is_null() {
        return Err(RuntimeError::from(PlatformError::null_pointer("out")).boxed());
    }
    let _ = (out, handle);

    Err(RuntimeError::from(PlatformError::not_supported(
        "destack.device.camera.stream.controlCapabilities",
    ))
    .boxed())
}

/// Binding for `destack.device.camera.stream.controlState`.
pub(crate) unsafe fn destack_device_camera_stream_control_state(
    _binding: &BindingCallContext,
    out: *mut CameraControlState,
    handle: resource::CameraStreamHandle,
) -> RuntimeResult<()> {
    if out.is_null() {
        return Err(RuntimeError::from(PlatformError::null_pointer("out")).boxed());
    }
    let _ = (out, handle);

    Err(RuntimeError::from(PlatformError::not_supported(
        "destack.device.camera.stream.controlState",
    ))
    .boxed())
}

/// Binding for `destack.device.camera.stream.open`.
pub(crate) unsafe fn destack_device_camera_stream_open(
    _binding: &BindingCallContext,
    out: *mut resource::CameraStreamHandle,
    device: resource::CameraDeviceHandle,
    config: CameraStreamConfig,
) -> RuntimeResult<()> {
    if out.is_null() {
        return Err(RuntimeError::from(PlatformError::null_pointer("out")).boxed());
    }
    let _ = (out, device, config);

    Err(RuntimeError::from(PlatformError::not_supported(
        "destack.device.camera.stream.open",
    ))
    .boxed())
}

/// Binding for `destack.device.camera.stream.pauseRecording`.
pub(crate) unsafe fn destack_device_camera_stream_pause_recording(
    _binding: &BindingCallContext,
    handle: resource::CameraStreamHandle,
) -> RuntimeResult<()> {
    let _ = handle;

    Err(RuntimeError::from(PlatformError::not_supported(
        "destack.device.camera.stream.pauseRecording",
    ))
    .boxed())
}

/// Binding for `destack.device.camera.stream.photoCapabilities`.
pub(crate) unsafe fn destack_device_camera_stream_photo_capabilities(
    _binding: &BindingCallContext,
    out: *mut CameraPhotoCapabilities,
    handle: resource::CameraStreamHandle,
) -> RuntimeResult<()> {
    if out.is_null() {
        return Err(RuntimeError::from(PlatformError::null_pointer("out")).boxed());
    }
    let _ = (out, handle);

    Err(RuntimeError::from(PlatformError::not_supported(
        "destack.device.camera.stream.photoCapabilities",
    ))
    .boxed())
}

/// Binding for `destack.device.camera.stream.photoState`.
pub(crate) unsafe fn destack_device_camera_stream_photo_state(
    _binding: &BindingCallContext,
    out: *mut CameraPhotoState,
    handle: resource::CameraStreamHandle,
) -> RuntimeResult<()> {
    if out.is_null() {
        return Err(RuntimeError::from(PlatformError::null_pointer("out")).boxed());
    }
    let _ = (out, handle);

    Err(RuntimeError::from(PlatformError::not_supported(
        "destack.device.camera.stream.photoState",
    ))
    .boxed())
}

/// Binding for `destack.device.camera.stream.read`.
pub(crate) unsafe fn destack_device_camera_stream_read(
    _binding: &BindingCallContext,
    out: *mut CameraFrame,
    handle: resource::CameraStreamHandle,
    timeoutns: u64,
) -> RuntimeResult<()> {
    if out.is_null() {
        return Err(RuntimeError::from(PlatformError::null_pointer("out")).boxed());
    }
    let _ = (out, handle, timeoutns);

    Err(RuntimeError::from(PlatformError::not_supported(
        "destack.device.camera.stream.read",
    ))
    .boxed())
}

/// Binding for `destack.device.camera.stream.recordingCapabilities`.
pub(crate) unsafe fn destack_device_camera_stream_recording_capabilities(
    _binding: &BindingCallContext,
    out: *mut CameraRecordingCapabilities,
    handle: resource::CameraStreamHandle,
) -> RuntimeResult<()> {
    if out.is_null() {
        return Err(RuntimeError::from(PlatformError::null_pointer("out")).boxed());
    }
    let _ = (out, handle);

    Err(RuntimeError::from(PlatformError::not_supported(
        "destack.device.camera.stream.recordingCapabilities",
    ))
    .boxed())
}

/// Binding for `destack.device.camera.stream.recordingState`.
pub(crate) unsafe fn destack_device_camera_stream_recording_state(
    _binding: &BindingCallContext,
    out: *mut CameraRecordingState,
    handle: resource::CameraStreamHandle,
) -> RuntimeResult<()> {
    if out.is_null() {
        return Err(RuntimeError::from(PlatformError::null_pointer("out")).boxed());
    }
    let _ = (out, handle);

    Err(RuntimeError::from(PlatformError::not_supported(
        "destack.device.camera.stream.recordingState",
    ))
    .boxed())
}

/// Binding for `destack.device.camera.stream.resumeRecording`.
pub(crate) unsafe fn destack_device_camera_stream_resume_recording(
    _binding: &BindingCallContext,
    handle: resource::CameraStreamHandle,
) -> RuntimeResult<()> {
    let _ = handle;

    Err(RuntimeError::from(PlatformError::not_supported(
        "destack.device.camera.stream.resumeRecording",
    ))
    .boxed())
}

/// Binding for `destack.device.camera.stream.start`.
pub(crate) unsafe fn destack_device_camera_stream_start(
    _binding: &BindingCallContext,
    handle: resource::CameraStreamHandle,
) -> RuntimeResult<()> {
    let _ = handle;

    Err(RuntimeError::from(PlatformError::not_supported(
        "destack.device.camera.stream.start",
    ))
    .boxed())
}

/// Binding for `destack.device.camera.stream.startRecording`.
pub(crate) unsafe fn destack_device_camera_stream_start_recording(
    _binding: &BindingCallContext,
    handle: resource::CameraStreamHandle,
    options: CameraRecordingOptions,
) -> RuntimeResult<()> {
    let _ = (handle, options);

    Err(RuntimeError::from(PlatformError::not_supported(
        "destack.device.camera.stream.startRecording",
    ))
    .boxed())
}

/// Binding for `destack.device.camera.stream.stop`.
pub(crate) unsafe fn destack_device_camera_stream_stop(
    _binding: &BindingCallContext,
    handle: resource::CameraStreamHandle,
) -> RuntimeResult<()> {
    let _ = handle;

    Err(RuntimeError::from(PlatformError::not_supported(
        "destack.device.camera.stream.stop",
    ))
    .boxed())
}

/// Binding for `destack.device.camera.stream.stopRecording`.
pub(crate) unsafe fn destack_device_camera_stream_stop_recording(
    _binding: &BindingCallContext,
    out: *mut CameraRecording,
    handle: resource::CameraStreamHandle,
    timeoutns: u64,
) -> RuntimeResult<()> {
    if out.is_null() {
        return Err(RuntimeError::from(PlatformError::null_pointer("out")).boxed());
    }
    let _ = (out, handle, timeoutns);

    Err(RuntimeError::from(PlatformError::not_supported(
        "destack.device.camera.stream.stopRecording",
    ))
    .boxed())
}

/// Binding for `destack.device.camera.stream.takePhoto`.
pub(crate) unsafe fn destack_device_camera_stream_take_photo(
    _binding: &BindingCallContext,
    out: *mut CameraPhoto,
    handle: resource::CameraStreamHandle,
    settings: CameraPhotoSettings,
    timeoutns: u64,
) -> RuntimeResult<()> {
    if out.is_null() {
        return Err(RuntimeError::from(PlatformError::null_pointer("out")).boxed());
    }
    let _ = (out, handle, settings, timeoutns);

    Err(RuntimeError::from(PlatformError::not_supported(
        "destack.device.camera.stream.takePhoto",
    ))
    .boxed())
}

/// Binding for `destack.device.camera.stream.tryRead`.
pub(crate) unsafe fn destack_device_camera_stream_try_read(
    _binding: &BindingCallContext,
    out: *mut CameraFrame,
    handle: resource::CameraStreamHandle,
) -> RuntimeResult<()> {
    if out.is_null() {
        return Err(RuntimeError::from(PlatformError::null_pointer("out")).boxed());
    }
    let _ = (out, handle);

    Err(RuntimeError::from(PlatformError::not_supported(
        "destack.device.camera.stream.tryRead",
    ))
    .boxed())
}

/// Binding for `destack.device.midi.backend.list`.
pub(crate) unsafe fn destack_device_midi_backend_list(
    _binding: &BindingCallContext,
    out: *mut NativeSlice<MidiBackendDescriptor>,
) -> RuntimeResult<()> {
    if out.is_null() {
        return Err(RuntimeError::from(PlatformError::null_pointer("out")).boxed());
    }
    let _ = out;

    Err(RuntimeError::from(PlatformError::not_supported(
        "destack.device.midi.backend.list",
    ))
    .boxed())
}

/// Binding for `destack.device.midi.event.close`.
pub(crate) unsafe fn destack_device_midi_event_close(
    _binding: &BindingCallContext,
    handle: resource::MidiEventHandle,
) -> RuntimeResult<()> {
    let _ = handle;

    Err(RuntimeError::from(PlatformError::not_supported(
        "destack.device.midi.event.close",
    ))
    .boxed())
}

/// Binding for `destack.device.midi.event.open`.
pub(crate) unsafe fn destack_device_midi_event_open(
    _binding: &BindingCallContext,
    out: *mut resource::MidiEventHandle,
    options: MidiEventSubscriptionOptions,
) -> RuntimeResult<()> {
    if out.is_null() {
        return Err(RuntimeError::from(PlatformError::null_pointer("out")).boxed());
    }
    let _ = (out, options);

    Err(RuntimeError::from(PlatformError::not_supported(
        "destack.device.midi.event.open",
    ))
    .boxed())
}

/// Binding for `destack.device.midi.event.read`.
pub(crate) unsafe fn destack_device_midi_event_read(
    _binding: &BindingCallContext,
    out: *mut MidiEvent,
    handle: resource::MidiEventHandle,
    timeoutns: u64,
) -> RuntimeResult<()> {
    if out.is_null() {
        return Err(RuntimeError::from(PlatformError::null_pointer("out")).boxed());
    }
    let _ = (out, handle, timeoutns);

    Err(RuntimeError::from(PlatformError::not_supported(
        "destack.device.midi.event.read",
    ))
    .boxed())
}

/// Binding for `destack.device.midi.event.readBatch`.
pub(crate) unsafe fn destack_device_midi_event_read_batch(
    _binding: &BindingCallContext,
    out: *mut NativeSlice<MidiEvent>,
    handle: resource::MidiEventHandle,
    maxevents: u32,
    timeoutns: u64,
) -> RuntimeResult<()> {
    if out.is_null() {
        return Err(RuntimeError::from(PlatformError::null_pointer("out")).boxed());
    }
    let _ = (out, handle, maxevents, timeoutns);

    Err(RuntimeError::from(PlatformError::not_supported(
        "destack.device.midi.event.readBatch",
    ))
    .boxed())
}

/// Binding for `destack.device.midi.event.tryRead`.
pub(crate) unsafe fn destack_device_midi_event_try_read(
    _binding: &BindingCallContext,
    out: *mut MidiEvent,
    handle: resource::MidiEventHandle,
) -> RuntimeResult<()> {
    if out.is_null() {
        return Err(RuntimeError::from(PlatformError::null_pointer("out")).boxed());
    }
    let _ = (out, handle);

    Err(RuntimeError::from(PlatformError::not_supported(
        "destack.device.midi.event.tryRead",
    ))
    .boxed())
}

/// Binding for `destack.device.midi.event.tryReadBatch`.
pub(crate) unsafe fn destack_device_midi_event_try_read_batch(
    _binding: &BindingCallContext,
    out: *mut NativeSlice<MidiEvent>,
    handle: resource::MidiEventHandle,
    maxevents: u32,
) -> RuntimeResult<()> {
    if out.is_null() {
        return Err(RuntimeError::from(PlatformError::null_pointer("out")).boxed());
    }
    let _ = (out, handle, maxevents);

    Err(RuntimeError::from(PlatformError::not_supported(
        "destack.device.midi.event.tryReadBatch",
    ))
    .boxed())
}

/// Binding for `destack.device.midi.input.port.close`.
pub(crate) unsafe fn destack_device_midi_input_port_close(
    _binding: &BindingCallContext,
    handle: resource::MidiInputPortHandle,
) -> RuntimeResult<()> {
    let _ = handle;

    Err(RuntimeError::from(PlatformError::not_supported(
        "destack.device.midi.input.port.close",
    ))
    .boxed())
}

/// Binding for `destack.device.midi.input.port.descriptor`.
pub(crate) unsafe fn destack_device_midi_input_port_descriptor(
    _binding: &BindingCallContext,
    out: *mut MidiPortDescriptor,
    handle: resource::MidiInputPortHandle,
) -> RuntimeResult<()> {
    if out.is_null() {
        return Err(RuntimeError::from(PlatformError::null_pointer("out")).boxed());
    }
    let _ = (out, handle);

    Err(RuntimeError::from(PlatformError::not_supported(
        "destack.device.midi.input.port.descriptor",
    ))
    .boxed())
}

/// Binding for `destack.device.midi.input.port.list`.
pub(crate) unsafe fn destack_device_midi_input_port_list(
    _binding: &BindingCallContext,
    out: *mut NativeSlice<MidiPortDescriptor>,
    options: MidiPortListOptions,
) -> RuntimeResult<()> {
    if out.is_null() {
        return Err(RuntimeError::from(PlatformError::null_pointer("out")).boxed());
    }
    let _ = (out, options);

    Err(RuntimeError::from(PlatformError::not_supported(
        "destack.device.midi.input.port.list",
    ))
    .boxed())
}

/// Binding for `destack.device.midi.input.port.open`.
pub(crate) unsafe fn destack_device_midi_input_port_open(
    _binding: &BindingCallContext,
    out: *mut resource::MidiInputPortHandle,
    id: NativeStringRef,
    options: MidiInputPortOpenOptions,
) -> RuntimeResult<()> {
    if out.is_null() {
        return Err(RuntimeError::from(PlatformError::null_pointer("out")).boxed());
    }
    let _ = (out, id, options);

    Err(RuntimeError::from(PlatformError::not_supported(
        "destack.device.midi.input.port.open",
    ))
    .boxed())
}

/// Binding for `destack.device.midi.input.read`.
pub(crate) unsafe fn destack_device_midi_input_read(
    _binding: &BindingCallContext,
    out: *mut MidiInputRecord,
    handle: resource::MidiInputPortHandle,
    timeoutns: u64,
) -> RuntimeResult<()> {
    if out.is_null() {
        return Err(RuntimeError::from(PlatformError::null_pointer("out")).boxed());
    }
    let _ = (out, handle, timeoutns);

    Err(RuntimeError::from(PlatformError::not_supported(
        "destack.device.midi.input.read",
    ))
    .boxed())
}

/// Binding for `destack.device.midi.input.readBatch`.
pub(crate) unsafe fn destack_device_midi_input_read_batch(
    _binding: &BindingCallContext,
    out: *mut NativeArray<MidiInputRecord>,
    handle: resource::MidiInputPortHandle,
    maxrecords: u32,
    timeoutns: u64,
) -> RuntimeResult<()> {
    if out.is_null() {
        return Err(RuntimeError::from(PlatformError::null_pointer("out")).boxed());
    }
    let _ = (out, handle, maxrecords, timeoutns);

    Err(RuntimeError::from(PlatformError::not_supported(
        "destack.device.midi.input.readBatch",
    ))
    .boxed())
}

/// Binding for `destack.device.midi.input.tryRead`.
pub(crate) unsafe fn destack_device_midi_input_try_read(
    _binding: &BindingCallContext,
    out: *mut MidiInputRecord,
    handle: resource::MidiInputPortHandle,
) -> RuntimeResult<()> {
    if out.is_null() {
        return Err(RuntimeError::from(PlatformError::null_pointer("out")).boxed());
    }
    let _ = (out, handle);

    Err(RuntimeError::from(PlatformError::not_supported(
        "destack.device.midi.input.tryRead",
    ))
    .boxed())
}

/// Binding for `destack.device.midi.input.tryReadBatch`.
pub(crate) unsafe fn destack_device_midi_input_try_read_batch(
    _binding: &BindingCallContext,
    out: *mut NativeArray<MidiInputRecord>,
    handle: resource::MidiInputPortHandle,
    maxrecords: u32,
) -> RuntimeResult<()> {
    if out.is_null() {
        return Err(RuntimeError::from(PlatformError::null_pointer("out")).boxed());
    }
    let _ = (out, handle, maxrecords);

    Err(RuntimeError::from(PlatformError::not_supported(
        "destack.device.midi.input.tryReadBatch",
    ))
    .boxed())
}

/// Binding for `destack.device.midi.input.virtual.create`.
pub(crate) unsafe fn destack_device_midi_input_virtual_create(
    _binding: &BindingCallContext,
    out: *mut resource::MidiInputPortHandle,
    options: MidiVirtualInputCreateOptions,
) -> RuntimeResult<()> {
    if out.is_null() {
        return Err(RuntimeError::from(PlatformError::null_pointer("out")).boxed());
    }
    let _ = (out, options);

    Err(RuntimeError::from(PlatformError::not_supported(
        "destack.device.midi.input.virtual.create",
    ))
    .boxed())
}

/// Binding for `destack.device.midi.output.port.close`.
pub(crate) unsafe fn destack_device_midi_output_port_close(
    _binding: &BindingCallContext,
    handle: resource::MidiOutputPortHandle,
) -> RuntimeResult<()> {
    let _ = handle;

    Err(RuntimeError::from(PlatformError::not_supported(
        "destack.device.midi.output.port.close",
    ))
    .boxed())
}

/// Binding for `destack.device.midi.output.port.descriptor`.
pub(crate) unsafe fn destack_device_midi_output_port_descriptor(
    _binding: &BindingCallContext,
    out: *mut MidiPortDescriptor,
    handle: resource::MidiOutputPortHandle,
) -> RuntimeResult<()> {
    if out.is_null() {
        return Err(RuntimeError::from(PlatformError::null_pointer("out")).boxed());
    }
    let _ = (out, handle);

    Err(RuntimeError::from(PlatformError::not_supported(
        "destack.device.midi.output.port.descriptor",
    ))
    .boxed())
}

/// Binding for `destack.device.midi.output.port.list`.
pub(crate) unsafe fn destack_device_midi_output_port_list(
    _binding: &BindingCallContext,
    out: *mut NativeSlice<MidiPortDescriptor>,
    options: MidiPortListOptions,
) -> RuntimeResult<()> {
    if out.is_null() {
        return Err(RuntimeError::from(PlatformError::null_pointer("out")).boxed());
    }
    let _ = (out, options);

    Err(RuntimeError::from(PlatformError::not_supported(
        "destack.device.midi.output.port.list",
    ))
    .boxed())
}

/// Binding for `destack.device.midi.output.port.open`.
pub(crate) unsafe fn destack_device_midi_output_port_open(
    _binding: &BindingCallContext,
    out: *mut resource::MidiOutputPortHandle,
    id: NativeStringRef,
    options: MidiOutputPortOpenOptions,
) -> RuntimeResult<()> {
    if out.is_null() {
        return Err(RuntimeError::from(PlatformError::null_pointer("out")).boxed());
    }
    let _ = (out, id, options);

    Err(RuntimeError::from(PlatformError::not_supported(
        "destack.device.midi.output.port.open",
    ))
    .boxed())
}

/// Binding for `destack.device.midi.output.virtual.create`.
pub(crate) unsafe fn destack_device_midi_output_virtual_create(
    _binding: &BindingCallContext,
    out: *mut resource::MidiOutputPortHandle,
    options: MidiVirtualOutputCreateOptions,
) -> RuntimeResult<()> {
    if out.is_null() {
        return Err(RuntimeError::from(PlatformError::null_pointer("out")).boxed());
    }
    let _ = (out, options);

    Err(RuntimeError::from(PlatformError::not_supported(
        "destack.device.midi.output.virtual.create",
    ))
    .boxed())
}

/// Binding for `destack.device.midi.output.write`.
pub(crate) unsafe fn destack_device_midi_output_write(
    _binding: &BindingCallContext,
    out: *mut u32,
    handle: resource::MidiOutputPortHandle,
    records: NativeArray<MidiOutputRecord>,
) -> RuntimeResult<()> {
    if out.is_null() {
        return Err(RuntimeError::from(PlatformError::null_pointer("out")).boxed());
    }
    let _ = (out, handle, records);

    Err(RuntimeError::from(PlatformError::not_supported(
        "destack.device.midi.output.write",
    ))
    .boxed())
}

/// Binding for `destack.device.serial.close`.
pub(crate) unsafe fn destack_device_serial_close(
    _binding: &BindingCallContext,
    handle: resource::SerialPortHandle,
) -> RuntimeResult<()> {
    let _ = handle;

    Err(RuntimeError::from(PlatformError::not_supported("destack.device.serial.close")).boxed())
}

/// Binding for `destack.device.serial.config`.
pub(crate) unsafe fn destack_device_serial_config(
    _binding: &BindingCallContext,
    out: *mut SerialPortConfig,
    handle: resource::SerialPortHandle,
) -> RuntimeResult<()> {
    if out.is_null() {
        return Err(RuntimeError::from(PlatformError::null_pointer("out")).boxed());
    }
    let _ = (out, handle);

    Err(RuntimeError::from(PlatformError::not_supported("destack.device.serial.config")).boxed())
}

/// Binding for `destack.device.serial.configure`.
pub(crate) unsafe fn destack_device_serial_configure(
    _binding: &BindingCallContext,
    handle: resource::SerialPortHandle,
    config: SerialPortConfig,
) -> RuntimeResult<()> {
    let _ = (handle, config);

    Err(RuntimeError::from(PlatformError::not_supported(
        "destack.device.serial.configure",
    ))
    .boxed())
}

/// Binding for `destack.device.serial.descriptor`.
pub(crate) unsafe fn destack_device_serial_descriptor(
    _binding: &BindingCallContext,
    out: *mut SerialPortDescriptor,
    handle: resource::SerialPortHandle,
) -> RuntimeResult<()> {
    if out.is_null() {
        return Err(RuntimeError::from(PlatformError::null_pointer("out")).boxed());
    }
    let _ = (out, handle);

    Err(RuntimeError::from(PlatformError::not_supported(
        "destack.device.serial.descriptor",
    ))
    .boxed())
}

/// Binding for `destack.device.serial.discardInput`.
pub(crate) unsafe fn destack_device_serial_discard_input(
    _binding: &BindingCallContext,
    handle: resource::SerialPortHandle,
) -> RuntimeResult<()> {
    let _ = handle;

    Err(RuntimeError::from(PlatformError::not_supported(
        "destack.device.serial.discardInput",
    ))
    .boxed())
}

/// Binding for `destack.device.serial.discardOutput`.
pub(crate) unsafe fn destack_device_serial_discard_output(
    _binding: &BindingCallContext,
    handle: resource::SerialPortHandle,
) -> RuntimeResult<()> {
    let _ = handle;

    Err(RuntimeError::from(PlatformError::not_supported(
        "destack.device.serial.discardOutput",
    ))
    .boxed())
}

/// Binding for `destack.device.serial.drain`.
pub(crate) unsafe fn destack_device_serial_drain(
    _binding: &BindingCallContext,
    handle: resource::SerialPortHandle,
) -> RuntimeResult<()> {
    let _ = handle;

    Err(RuntimeError::from(PlatformError::not_supported("destack.device.serial.drain")).boxed())
}

/// Binding for `destack.device.serial.getSignals`.
pub(crate) unsafe fn destack_device_serial_get_signals(
    _binding: &BindingCallContext,
    out: *mut SerialInputSignals,
    handle: resource::SerialPortHandle,
) -> RuntimeResult<()> {
    if out.is_null() {
        return Err(RuntimeError::from(PlatformError::null_pointer("out")).boxed());
    }
    let _ = (out, handle);

    Err(RuntimeError::from(PlatformError::not_supported(
        "destack.device.serial.getSignals",
    ))
    .boxed())
}

/// Binding for `destack.device.serial.list`.
pub(crate) unsafe fn destack_device_serial_list(
    _binding: &BindingCallContext,
    out: *mut NativeSlice<SerialPortDescriptor>,
) -> RuntimeResult<()> {
    if out.is_null() {
        return Err(RuntimeError::from(PlatformError::null_pointer("out")).boxed());
    }
    let _ = out;

    Err(RuntimeError::from(PlatformError::not_supported("destack.device.serial.list")).boxed())
}

/// Binding for `destack.device.serial.open`.
pub(crate) unsafe fn destack_device_serial_open(
    _binding: &BindingCallContext,
    out: *mut resource::SerialPortHandle,
    id: NativeStringRef,
    options: SerialPortOpenOptions,
) -> RuntimeResult<()> {
    if out.is_null() {
        return Err(RuntimeError::from(PlatformError::null_pointer("out")).boxed());
    }
    let _ = (out, id, options);

    Err(RuntimeError::from(PlatformError::not_supported("destack.device.serial.open")).boxed())
}

/// Binding for `destack.device.serial.readEvent`.
pub(crate) unsafe fn destack_device_serial_read_event(
    _binding: &BindingCallContext,
    out: *mut SerialEvent,
    handle: resource::SerialPortHandle,
    timeoutns: u64,
) -> RuntimeResult<()> {
    if out.is_null() {
        return Err(RuntimeError::from(PlatformError::null_pointer("out")).boxed());
    }
    let _ = (out, handle, timeoutns);

    Err(RuntimeError::from(PlatformError::not_supported(
        "destack.device.serial.readEvent",
    ))
    .boxed())
}

/// Binding for `destack.device.serial.readInto`.
pub(crate) unsafe fn destack_device_serial_read_into(
    _binding: &BindingCallContext,
    out: *mut u64,
    handle: resource::SerialPortHandle,
    buffer: NativeSlice<u8>,
    timeoutns: u64,
) -> RuntimeResult<()> {
    if out.is_null() {
        return Err(RuntimeError::from(PlatformError::null_pointer("out")).boxed());
    }
    let _ = (out, handle, buffer, timeoutns);

    Err(RuntimeError::from(PlatformError::not_supported(
        "destack.device.serial.readInto",
    ))
    .boxed())
}

/// Binding for `destack.device.serial.setSignals`.
pub(crate) unsafe fn destack_device_serial_set_signals(
    _binding: &BindingCallContext,
    handle: resource::SerialPortHandle,
    signals: SerialOutputSignals,
) -> RuntimeResult<()> {
    let _ = (handle, signals);

    Err(RuntimeError::from(PlatformError::not_supported(
        "destack.device.serial.setSignals",
    ))
    .boxed())
}

/// Binding for `destack.device.serial.tryReadEvent`.
pub(crate) unsafe fn destack_device_serial_try_read_event(
    _binding: &BindingCallContext,
    out: *mut SerialEvent,
    handle: resource::SerialPortHandle,
) -> RuntimeResult<()> {
    if out.is_null() {
        return Err(RuntimeError::from(PlatformError::null_pointer("out")).boxed());
    }
    let _ = (out, handle);

    Err(RuntimeError::from(PlatformError::not_supported(
        "destack.device.serial.tryReadEvent",
    ))
    .boxed())
}

/// Binding for `destack.device.serial.tryReadInto`.
pub(crate) unsafe fn destack_device_serial_try_read_into(
    _binding: &BindingCallContext,
    out: *mut u64,
    handle: resource::SerialPortHandle,
    buffer: NativeSlice<u8>,
) -> RuntimeResult<()> {
    if out.is_null() {
        return Err(RuntimeError::from(PlatformError::null_pointer("out")).boxed());
    }
    let _ = (out, handle, buffer);

    Err(RuntimeError::from(PlatformError::not_supported(
        "destack.device.serial.tryReadInto",
    ))
    .boxed())
}

/// Binding for `destack.device.serial.watchClose`.
pub(crate) unsafe fn destack_device_serial_watch_close(
    _binding: &BindingCallContext,
    handle: resource::SerialWatchHandle,
) -> RuntimeResult<()> {
    let _ = handle;

    Err(RuntimeError::from(PlatformError::not_supported(
        "destack.device.serial.watchClose",
    ))
    .boxed())
}

/// Binding for `destack.device.serial.watchOpen`.
pub(crate) unsafe fn destack_device_serial_watch_open(
    _binding: &BindingCallContext,
    out: *mut resource::SerialWatchHandle,
) -> RuntimeResult<()> {
    if out.is_null() {
        return Err(RuntimeError::from(PlatformError::null_pointer("out")).boxed());
    }
    let _ = out;

    Err(RuntimeError::from(PlatformError::not_supported(
        "destack.device.serial.watchOpen",
    ))
    .boxed())
}

/// Binding for `destack.device.serial.watchRead`.
pub(crate) unsafe fn destack_device_serial_watch_read(
    _binding: &BindingCallContext,
    out: *mut SerialWatchEvent,
    handle: resource::SerialWatchHandle,
    timeoutns: u64,
) -> RuntimeResult<()> {
    if out.is_null() {
        return Err(RuntimeError::from(PlatformError::null_pointer("out")).boxed());
    }
    let _ = (out, handle, timeoutns);

    Err(RuntimeError::from(PlatformError::not_supported(
        "destack.device.serial.watchRead",
    ))
    .boxed())
}

/// Binding for `destack.device.serial.watchTryRead`.
pub(crate) unsafe fn destack_device_serial_watch_try_read(
    _binding: &BindingCallContext,
    out: *mut SerialWatchEvent,
    handle: resource::SerialWatchHandle,
) -> RuntimeResult<()> {
    if out.is_null() {
        return Err(RuntimeError::from(PlatformError::null_pointer("out")).boxed());
    }
    let _ = (out, handle);

    Err(RuntimeError::from(PlatformError::not_supported(
        "destack.device.serial.watchTryRead",
    ))
    .boxed())
}

/// Binding for `destack.device.serial.write`.
pub(crate) unsafe fn destack_device_serial_write(
    _binding: &BindingCallContext,
    out: *mut u64,
    handle: resource::SerialPortHandle,
    data: NativeSlice<u8>,
    timeoutns: u64,
) -> RuntimeResult<()> {
    if out.is_null() {
        return Err(RuntimeError::from(PlatformError::null_pointer("out")).boxed());
    }
    let _ = (out, handle, data, timeoutns);

    Err(RuntimeError::from(PlatformError::not_supported("destack.device.serial.write")).boxed())
}

/// Binding for `destack.device.usb.bosCapabilityList`.
pub(crate) unsafe fn destack_device_usb_bos_capability_list(
    _binding: &BindingCallContext,
    out: *mut NativeSlice<UsbBosCapabilityDescriptor>,
    handle: resource::UsbDeviceHandle,
) -> RuntimeResult<()> {
    if out.is_null() {
        return Err(RuntimeError::from(PlatformError::null_pointer("out")).boxed());
    }
    let _ = (out, handle);

    Err(RuntimeError::from(PlatformError::not_supported(
        "destack.device.usb.bosCapabilityList",
    ))
    .boxed())
}

/// Binding for `destack.device.usb.bulkRead`.
pub(crate) unsafe fn destack_device_usb_bulk_read(
    _binding: &BindingCallContext,
    out: *mut UsbInTransferResult,
    handle: resource::UsbDeviceHandle,
    endpoint: UsbEndpointSelector,
    maxbytes: u32,
    timeoutns: u64,
) -> RuntimeResult<()> {
    if out.is_null() {
        return Err(RuntimeError::from(PlatformError::null_pointer("out")).boxed());
    }
    let _ = (out, handle, endpoint, maxbytes, timeoutns);

    Err(RuntimeError::from(PlatformError::not_supported("destack.device.usb.bulkRead")).boxed())
}

/// Binding for `destack.device.usb.bulkWrite`.
pub(crate) unsafe fn destack_device_usb_bulk_write(
    _binding: &BindingCallContext,
    out: *mut UsbOutTransferResult,
    handle: resource::UsbDeviceHandle,
    endpoint: UsbEndpointSelector,
    argument_bytes: NativeSlice<u8>,
    timeoutns: u64,
) -> RuntimeResult<()> {
    if out.is_null() {
        return Err(RuntimeError::from(PlatformError::null_pointer("out")).boxed());
    }
    let _ = (out, handle, endpoint, argument_bytes, timeoutns);

    Err(RuntimeError::from(PlatformError::not_supported("destack.device.usb.bulkWrite")).boxed())
}

/// Binding for `destack.device.usb.claimInterface`.
pub(crate) unsafe fn destack_device_usb_claim_interface(
    _binding: &BindingCallContext,
    handle: resource::UsbDeviceHandle,
    interfacenumber: u8,
) -> RuntimeResult<()> {
    let _ = (handle, interfacenumber);

    Err(RuntimeError::from(PlatformError::not_supported(
        "destack.device.usb.claimInterface",
    ))
    .boxed())
}

/// Binding for `destack.device.usb.clearHalt`.
pub(crate) unsafe fn destack_device_usb_clear_halt(
    _binding: &BindingCallContext,
    handle: resource::UsbDeviceHandle,
    endpoint: UsbEndpointSelector,
) -> RuntimeResult<()> {
    let _ = (handle, endpoint);

    Err(RuntimeError::from(PlatformError::not_supported("destack.device.usb.clearHalt")).boxed())
}

/// Binding for `destack.device.usb.close`.
pub(crate) unsafe fn destack_device_usb_close(
    _binding: &BindingCallContext,
    handle: resource::UsbDeviceHandle,
) -> RuntimeResult<()> {
    let _ = handle;

    Err(RuntimeError::from(PlatformError::not_supported("destack.device.usb.close")).boxed())
}

/// Binding for `destack.device.usb.configurationGet`.
pub(crate) unsafe fn destack_device_usb_configuration_get(
    _binding: &BindingCallContext,
    out: *mut u8,
    handle: resource::UsbDeviceHandle,
) -> RuntimeResult<()> {
    if out.is_null() {
        return Err(RuntimeError::from(PlatformError::null_pointer("out")).boxed());
    }
    let _ = (out, handle);

    Err(RuntimeError::from(PlatformError::not_supported(
        "destack.device.usb.configurationGet",
    ))
    .boxed())
}

/// Binding for `destack.device.usb.configurationList`.
pub(crate) unsafe fn destack_device_usb_configuration_list(
    _binding: &BindingCallContext,
    out: *mut NativeSlice<UsbConfigurationDescriptor>,
    handle: resource::UsbDeviceHandle,
) -> RuntimeResult<()> {
    if out.is_null() {
        return Err(RuntimeError::from(PlatformError::null_pointer("out")).boxed());
    }
    let _ = (out, handle);

    Err(RuntimeError::from(PlatformError::not_supported(
        "destack.device.usb.configurationList",
    ))
    .boxed())
}

/// Binding for `destack.device.usb.configurationSet`.
pub(crate) unsafe fn destack_device_usb_configuration_set(
    _binding: &BindingCallContext,
    handle: resource::UsbDeviceHandle,
    configurationvalue: u8,
) -> RuntimeResult<()> {
    let _ = (handle, configurationvalue);

    Err(RuntimeError::from(PlatformError::not_supported(
        "destack.device.usb.configurationSet",
    ))
    .boxed())
}

/// Binding for `destack.device.usb.controlRead`.
pub(crate) unsafe fn destack_device_usb_control_read(
    _binding: &BindingCallContext,
    out: *mut UsbInTransferResult,
    handle: resource::UsbDeviceHandle,
    setup: UsbControlSetup,
    timeoutns: u64,
) -> RuntimeResult<()> {
    if out.is_null() {
        return Err(RuntimeError::from(PlatformError::null_pointer("out")).boxed());
    }
    let _ = (out, handle, setup, timeoutns);

    Err(RuntimeError::from(PlatformError::not_supported(
        "destack.device.usb.controlRead",
    ))
    .boxed())
}

/// Binding for `destack.device.usb.controlWrite`.
pub(crate) unsafe fn destack_device_usb_control_write(
    _binding: &BindingCallContext,
    out: *mut UsbOutTransferResult,
    handle: resource::UsbDeviceHandle,
    setup: UsbControlSetup,
    argument_bytes: NativeSlice<u8>,
    timeoutns: u64,
) -> RuntimeResult<()> {
    if out.is_null() {
        return Err(RuntimeError::from(PlatformError::null_pointer("out")).boxed());
    }
    let _ = (out, handle, setup, argument_bytes, timeoutns);

    Err(RuntimeError::from(PlatformError::not_supported(
        "destack.device.usb.controlWrite",
    ))
    .boxed())
}

/// Binding for `destack.device.usb.descriptor`.
pub(crate) unsafe fn destack_device_usb_descriptor(
    _binding: &BindingCallContext,
    out: *mut UsbDeviceDescriptor,
    handle: resource::UsbDeviceHandle,
) -> RuntimeResult<()> {
    if out.is_null() {
        return Err(RuntimeError::from(PlatformError::null_pointer("out")).boxed());
    }
    let _ = (out, handle);

    Err(RuntimeError::from(PlatformError::not_supported(
        "destack.device.usb.descriptor",
    ))
    .boxed())
}

/// Binding for `destack.device.usb.interruptRead`.
pub(crate) unsafe fn destack_device_usb_interrupt_read(
    _binding: &BindingCallContext,
    out: *mut UsbInTransferResult,
    handle: resource::UsbDeviceHandle,
    endpoint: UsbEndpointSelector,
    maxbytes: u32,
    timeoutns: u64,
) -> RuntimeResult<()> {
    if out.is_null() {
        return Err(RuntimeError::from(PlatformError::null_pointer("out")).boxed());
    }
    let _ = (out, handle, endpoint, maxbytes, timeoutns);

    Err(RuntimeError::from(PlatformError::not_supported(
        "destack.device.usb.interruptRead",
    ))
    .boxed())
}

/// Binding for `destack.device.usb.interruptWrite`.
pub(crate) unsafe fn destack_device_usb_interrupt_write(
    _binding: &BindingCallContext,
    out: *mut UsbOutTransferResult,
    handle: resource::UsbDeviceHandle,
    endpoint: UsbEndpointSelector,
    argument_bytes: NativeSlice<u8>,
    timeoutns: u64,
) -> RuntimeResult<()> {
    if out.is_null() {
        return Err(RuntimeError::from(PlatformError::null_pointer("out")).boxed());
    }
    let _ = (out, handle, endpoint, argument_bytes, timeoutns);

    Err(RuntimeError::from(PlatformError::not_supported(
        "destack.device.usb.interruptWrite",
    ))
    .boxed())
}

/// Binding for `destack.device.usb.isochronousRead`.
pub(crate) unsafe fn destack_device_usb_isochronous_read(
    _binding: &BindingCallContext,
    out: *mut UsbIsochronousTransferResult,
    handle: resource::UsbDeviceHandle,
    endpoint: UsbEndpointSelector,
    packetsizes: NativeSlice<u32>,
    timeoutns: u64,
) -> RuntimeResult<()> {
    if out.is_null() {
        return Err(RuntimeError::from(PlatformError::null_pointer("out")).boxed());
    }
    let _ = (out, handle, endpoint, packetsizes, timeoutns);

    Err(RuntimeError::from(PlatformError::not_supported(
        "destack.device.usb.isochronousRead",
    ))
    .boxed())
}

/// Binding for `destack.device.usb.isochronousWrite`.
pub(crate) unsafe fn destack_device_usb_isochronous_write(
    _binding: &BindingCallContext,
    out: *mut UsbIsochronousTransferResult,
    handle: resource::UsbDeviceHandle,
    endpoint: UsbEndpointSelector,
    argument_bytes: NativeSlice<u8>,
    packetsizes: NativeSlice<u32>,
    timeoutns: u64,
) -> RuntimeResult<()> {
    if out.is_null() {
        return Err(RuntimeError::from(PlatformError::null_pointer("out")).boxed());
    }
    let _ = (
        out,
        handle,
        endpoint,
        argument_bytes,
        packetsizes,
        timeoutns,
    );

    Err(RuntimeError::from(PlatformError::not_supported(
        "destack.device.usb.isochronousWrite",
    ))
    .boxed())
}

/// Binding for `destack.device.usb.list`.
pub(crate) unsafe fn destack_device_usb_list(
    _binding: &BindingCallContext,
    out: *mut NativeSlice<UsbDeviceDescriptor>,
) -> RuntimeResult<()> {
    if out.is_null() {
        return Err(RuntimeError::from(PlatformError::null_pointer("out")).boxed());
    }
    let _ = out;

    Err(RuntimeError::from(PlatformError::not_supported("destack.device.usb.list")).boxed())
}

/// Binding for `destack.device.usb.open`.
pub(crate) unsafe fn destack_device_usb_open(
    _binding: &BindingCallContext,
    out: *mut resource::UsbDeviceHandle,
    id: NativeStringRef,
) -> RuntimeResult<()> {
    if out.is_null() {
        return Err(RuntimeError::from(PlatformError::null_pointer("out")).boxed());
    }
    let _ = (out, id);

    Err(RuntimeError::from(PlatformError::not_supported("destack.device.usb.open")).boxed())
}

/// Binding for `destack.device.usb.releaseInterface`.
pub(crate) unsafe fn destack_device_usb_release_interface(
    _binding: &BindingCallContext,
    handle: resource::UsbDeviceHandle,
    interfacenumber: u8,
) -> RuntimeResult<()> {
    let _ = (handle, interfacenumber);

    Err(RuntimeError::from(PlatformError::not_supported(
        "destack.device.usb.releaseInterface",
    ))
    .boxed())
}

/// Binding for `destack.device.usb.reset`.
pub(crate) unsafe fn destack_device_usb_reset(
    _binding: &BindingCallContext,
    handle: resource::UsbDeviceHandle,
) -> RuntimeResult<()> {
    let _ = handle;

    Err(RuntimeError::from(PlatformError::not_supported("destack.device.usb.reset")).boxed())
}

/// Binding for `destack.device.usb.setInterfaceAlternateSetting`.
pub(crate) unsafe fn destack_device_usb_set_interface_alternate_setting(
    _binding: &BindingCallContext,
    handle: resource::UsbDeviceHandle,
    interfacenumber: u8,
    alternatesetting: u8,
) -> RuntimeResult<()> {
    let _ = (handle, interfacenumber, alternatesetting);

    Err(RuntimeError::from(PlatformError::not_supported(
        "destack.device.usb.setInterfaceAlternateSetting",
    ))
    .boxed())
}

/// Binding for `destack.device.usb.stringDescriptor`.
pub(crate) unsafe fn destack_device_usb_string_descriptor(
    _binding: &BindingCallContext,
    out: *mut UsbStringDescriptor,
    handle: resource::UsbDeviceHandle,
    languageid: u16,
) -> RuntimeResult<()> {
    if out.is_null() {
        return Err(RuntimeError::from(PlatformError::null_pointer("out")).boxed());
    }
    let _ = (out, handle, languageid);

    Err(RuntimeError::from(PlatformError::not_supported(
        "destack.device.usb.stringDescriptor",
    ))
    .boxed())
}

/// Binding for `destack.device.usb.stringLanguageList`.
pub(crate) unsafe fn destack_device_usb_string_language_list(
    _binding: &BindingCallContext,
    out: *mut NativeSlice<u16>,
    handle: resource::UsbDeviceHandle,
) -> RuntimeResult<()> {
    if out.is_null() {
        return Err(RuntimeError::from(PlatformError::null_pointer("out")).boxed());
    }
    let _ = (out, handle);

    Err(RuntimeError::from(PlatformError::not_supported(
        "destack.device.usb.stringLanguageList",
    ))
    .boxed())
}

/// Binding for `destack.device.usb.transferCancel`.
pub(crate) unsafe fn destack_device_usb_transfer_cancel(
    _binding: &BindingCallContext,
    handle: resource::UsbDeviceHandle,
    endpoint: UsbEndpointSelector,
) -> RuntimeResult<()> {
    let _ = (handle, endpoint);

    Err(RuntimeError::from(PlatformError::not_supported(
        "destack.device.usb.transferCancel",
    ))
    .boxed())
}

/// Binding for `destack.device.usb.transferCancelAll`.
pub(crate) unsafe fn destack_device_usb_transfer_cancel_all(
    _binding: &BindingCallContext,
    handle: resource::UsbDeviceHandle,
) -> RuntimeResult<()> {
    let _ = handle;

    Err(RuntimeError::from(PlatformError::not_supported(
        "destack.device.usb.transferCancelAll",
    ))
    .boxed())
}

/// Binding for `destack.device.usb.watchClose`.
pub(crate) unsafe fn destack_device_usb_watch_close(
    _binding: &BindingCallContext,
    handle: resource::UsbWatchHandle,
) -> RuntimeResult<()> {
    let _ = handle;

    Err(RuntimeError::from(PlatformError::not_supported(
        "destack.device.usb.watchClose",
    ))
    .boxed())
}

/// Binding for `destack.device.usb.watchOpen`.
pub(crate) unsafe fn destack_device_usb_watch_open(
    _binding: &BindingCallContext,
    out: *mut resource::UsbWatchHandle,
) -> RuntimeResult<()> {
    if out.is_null() {
        return Err(RuntimeError::from(PlatformError::null_pointer("out")).boxed());
    }
    let _ = out;

    Err(RuntimeError::from(PlatformError::not_supported("destack.device.usb.watchOpen")).boxed())
}

/// Binding for `destack.device.usb.watchRead`.
pub(crate) unsafe fn destack_device_usb_watch_read(
    _binding: &BindingCallContext,
    out: *mut UsbHotplugEvent,
    handle: resource::UsbWatchHandle,
    timeoutns: u64,
) -> RuntimeResult<()> {
    if out.is_null() {
        return Err(RuntimeError::from(PlatformError::null_pointer("out")).boxed());
    }
    let _ = (out, handle, timeoutns);

    Err(RuntimeError::from(PlatformError::not_supported("destack.device.usb.watchRead")).boxed())
}

/// Binding for `destack.device.usb.watchTryRead`.
pub(crate) unsafe fn destack_device_usb_watch_try_read(
    _binding: &BindingCallContext,
    out: *mut UsbHotplugEvent,
    handle: resource::UsbWatchHandle,
) -> RuntimeResult<()> {
    if out.is_null() {
        return Err(RuntimeError::from(PlatformError::null_pointer("out")).boxed());
    }
    let _ = (out, handle);

    Err(RuntimeError::from(PlatformError::not_supported(
        "destack.device.usb.watchTryRead",
    ))
    .boxed())
}
