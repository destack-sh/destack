// generated by generate-bindings: test harness, do not edit

#![allow(dead_code)]
#![allow(unused_imports)]
#![allow(clippy::type_complexity)]

use crate::diagnostic::{RuntimeError, RuntimeResult};
use crate::platform::abi::{NativeSlice, NativeStringRef, NativeStringSlice};
use crate::platform::device::midi::tests::MidiHarnessContext;
use crate::platform::device::{
    MidiBackend, MidiBackendCapabilityFlags, MidiBackendDescriptor, MidiBackendDescriptorVm,
    MidiBackendDisconnectedEvent, MidiBackendDisconnectedEventVm, MidiBackendSelectionPolicy,
    MidiDataFormat, MidiDataFormatFlags, MidiEvent, MidiEventDeliveryMode, MidiEventMetadata,
    MidiEventMetadataVm, MidiEventOverflowPolicy, MidiEventSource, MidiEventSubscriptionFlags,
    MidiEventSubscriptionOptions, MidiEventSubscriptionOptionsVm, MidiEventVm,
    MidiInputPortOpenOptions, MidiInputPortOpenOptionsVm, MidiInputRecord, MidiInputRecordVm,
    MidiOutputPortOpenOptions, MidiOutputPortOpenOptionsVm, MidiOutputRecord, MidiOutputRecordVm,
    MidiPortAddedEvent, MidiPortAddedEventVm, MidiPortChangedEvent, MidiPortChangedEventVm,
    MidiPortDescriptor, MidiPortDescriptorVm, MidiPortDirection, MidiPortDirectionFlags,
    MidiPortListFlags, MidiPortListOptions, MidiPortListOptionsVm, MidiPortRemovedEvent,
    MidiPortRemovedEventVm, MidiProtocol, MidiProtocolFlags, MidiRecordFraming,
    MidiVirtualInputCreateOptions, MidiVirtualInputCreateOptionsVm, MidiVirtualOutputCreateOptions,
    MidiVirtualOutputCreateOptionsVm, native as midi_native, vm as midi_vm,
};
use crate::platform::{
    NativeAbiCodec, NativeArray, PlatformError as HarnessPlatformError, VmAbiCodec, VmArray,
    VmSlice, core, fs, resource,
};
use destack_vm as vm;

impl<'call> MidiHarnessContext<'call> {
    /// Return the generated VM context if available.
    #[allow(clippy::mut_from_ref)]
    fn generated_vm_context_mut(&self) -> Option<&mut vm::BindingContext<'_>> {
        self.vm_context
            .map(|context| unsafe { &mut *(context as *mut vm::BindingContext<'_>) })
    }

    /// Return one standardized value payload for native and VM variants.
    pub(crate) fn harness_value<Native, Vm>(&self, native: Native) -> HarnessValue<Native, Vm> {
        HarnessValue::Native(native)
    }

    /// Return one standardized value payload for VM and native variants.
    pub(crate) fn harness_value_vm<Native, Vm>(&self, vm: Vm) -> HarnessValue<Native, Vm> {
        HarnessValue::Vm(vm)
    }

    /// Encode one value into one native or VM harness payload.
    pub(crate) fn harness_value_from<Native, Vm>(
        &mut self,
        value: Native::Value,
    ) -> RuntimeResult<HarnessValue<Native, Vm>>
    where
        Native: NativeAbiCodec,
        Vm: VmAbiCodec<Value = Native::Value>,
    {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let context = &mut context.write();
                let value = Vm::from_value(context, value)?;
                Ok(HarnessValue::Vm(value))
            }
            None => {
                let value = Native::from_value(self.call_context, value);
                Ok(HarnessValue::Native(value))
            }
        }
    }

    /// Decode one native or VM harness payload into one value.
    pub(crate) fn harness_value_into<Native, Vm>(
        &mut self,
        value: HarnessValue<Native, Vm>,
    ) -> RuntimeResult<Native::Value>
    where
        Native: NativeAbiCodec,
        Vm: VmAbiCodec<Value = Native::Value>,
    {
        match value {
            HarnessValue::Native(value) => unsafe { Native::into_value(value) },
            HarnessValue::Vm(value) => {
                let context = self.generated_vm_context_mut().ok_or_else(|| {
                    RuntimeError::from(HarnessPlatformError::invalid_argument_value(
                        "context",
                        "expected vm context",
                    ))
                    .boxed()
                })?;
                let context = &context.read();
                value.into_value(context)
            }
        }
    }

    /// List host MIDI backends.
    pub(crate) fn destack_device_midi_backend_list(
        &mut self,
    ) -> RuntimeResult<
        HarnessValue<NativeSlice<MidiBackendDescriptor>, VmSlice<MidiBackendDescriptorVm>>,
    > {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = midi_vm::destack_device_midi_backend_list(self.call_context, context)?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let mut out = std::mem::MaybeUninit::<NativeSlice<MidiBackendDescriptor>>::uninit();
                unsafe {
                    midi_native::destack_device_midi_backend_list(
                        self.call_context,
                        out.as_mut_ptr(),
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Close one MIDI topology event subscription.
    pub(crate) fn destack_device_midi_event_close(
        &mut self,
        handle: resource::MidiEventHandle,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                midi_vm::destack_device_midi_event_close(self.call_context, context, handle)
            }
            None => unsafe {
                midi_native::destack_device_midi_event_close(self.call_context, handle)
            },
        }
    }

    /// Open one MIDI topology event subscription.
    pub(crate) fn destack_device_midi_event_open(
        &mut self,
        options: HarnessValue<MidiEventSubscriptionOptions, MidiEventSubscriptionOptionsVm>,
    ) -> RuntimeResult<resource::MidiEventHandle> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let options = options.into_vm("options")?;
                let out =
                    midi_vm::destack_device_midi_event_open(self.call_context, context, options)?;
                Ok(out)
            }
            None => {
                let options = options.into_native("options")?;
                let mut out = std::mem::MaybeUninit::<resource::MidiEventHandle>::uninit();
                unsafe {
                    midi_native::destack_device_midi_event_open(
                        self.call_context,
                        out.as_mut_ptr(),
                        options,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// Wait for one MIDI topology event.
    pub(crate) fn destack_device_midi_event_read(
        &mut self,
        handle: resource::MidiEventHandle,
        timeoutns: u64,
    ) -> RuntimeResult<HarnessValue<MidiEvent, MidiEventVm>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = midi_vm::destack_device_midi_event_read(
                    self.call_context,
                    context,
                    handle,
                    timeoutns,
                )?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let mut out = std::mem::MaybeUninit::<MidiEvent>::uninit();
                unsafe {
                    midi_native::destack_device_midi_event_read(
                        self.call_context,
                        out.as_mut_ptr(),
                        handle,
                        timeoutns,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Wait for one batch of MIDI topology events.
    pub(crate) fn destack_device_midi_event_read_batch(
        &mut self,
        handle: resource::MidiEventHandle,
        maxevents: u32,
        timeoutns: u64,
    ) -> RuntimeResult<HarnessValue<NativeSlice<MidiEvent>, VmSlice<MidiEventVm>>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = midi_vm::destack_device_midi_event_read_batch(
                    self.call_context,
                    context,
                    handle,
                    maxevents,
                    timeoutns,
                )?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let mut out = std::mem::MaybeUninit::<NativeSlice<MidiEvent>>::uninit();
                unsafe {
                    midi_native::destack_device_midi_event_read_batch(
                        self.call_context,
                        out.as_mut_ptr(),
                        handle,
                        maxevents,
                        timeoutns,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Poll one MIDI topology event without blocking.
    pub(crate) fn destack_device_midi_event_try_read(
        &mut self,
        handle: resource::MidiEventHandle,
    ) -> RuntimeResult<HarnessValue<MidiEvent, MidiEventVm>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = midi_vm::destack_device_midi_event_try_read(
                    self.call_context,
                    context,
                    handle,
                )?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let mut out = std::mem::MaybeUninit::<MidiEvent>::uninit();
                unsafe {
                    midi_native::destack_device_midi_event_try_read(
                        self.call_context,
                        out.as_mut_ptr(),
                        handle,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Poll one batch of MIDI topology events without blocking.
    pub(crate) fn destack_device_midi_event_try_read_batch(
        &mut self,
        handle: resource::MidiEventHandle,
        maxevents: u32,
    ) -> RuntimeResult<HarnessValue<NativeSlice<MidiEvent>, VmSlice<MidiEventVm>>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = midi_vm::destack_device_midi_event_try_read_batch(
                    self.call_context,
                    context,
                    handle,
                    maxevents,
                )?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let mut out = std::mem::MaybeUninit::<NativeSlice<MidiEvent>>::uninit();
                unsafe {
                    midi_native::destack_device_midi_event_try_read_batch(
                        self.call_context,
                        out.as_mut_ptr(),
                        handle,
                        maxevents,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Close one opened MIDI input endpoint.
    pub(crate) fn destack_device_midi_input_port_close(
        &mut self,
        handle: resource::MidiInputPortHandle,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                midi_vm::destack_device_midi_input_port_close(self.call_context, context, handle)
            }
            None => unsafe {
                midi_native::destack_device_midi_input_port_close(self.call_context, handle)
            },
        }
    }

    /// Describe one opened MIDI input endpoint.
    pub(crate) fn destack_device_midi_input_port_descriptor(
        &mut self,
        handle: resource::MidiInputPortHandle,
    ) -> RuntimeResult<HarnessValue<MidiPortDescriptor, MidiPortDescriptorVm>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = midi_vm::destack_device_midi_input_port_descriptor(
                    self.call_context,
                    context,
                    handle,
                )?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let mut out = std::mem::MaybeUninit::<MidiPortDescriptor>::uninit();
                unsafe {
                    midi_native::destack_device_midi_input_port_descriptor(
                        self.call_context,
                        out.as_mut_ptr(),
                        handle,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// List available MIDI input endpoints.
    pub(crate) fn destack_device_midi_input_port_list(
        &mut self,
        options: HarnessValue<MidiPortListOptions, MidiPortListOptionsVm>,
    ) -> RuntimeResult<HarnessValue<NativeSlice<MidiPortDescriptor>, VmSlice<MidiPortDescriptorVm>>>
    {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let options = options.into_vm("options")?;
                let out = midi_vm::destack_device_midi_input_port_list(
                    self.call_context,
                    context,
                    options,
                )?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let options = options.into_native("options")?;
                let mut out = std::mem::MaybeUninit::<NativeSlice<MidiPortDescriptor>>::uninit();
                unsafe {
                    midi_native::destack_device_midi_input_port_list(
                        self.call_context,
                        out.as_mut_ptr(),
                        options,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Open one MIDI input endpoint.
    pub(crate) fn destack_device_midi_input_port_open(
        &mut self,
        id: HarnessValue<NativeStringRef, vm::StringHandle>,
        options: HarnessValue<MidiInputPortOpenOptions, MidiInputPortOpenOptionsVm>,
    ) -> RuntimeResult<resource::MidiInputPortHandle> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let id = id.into_vm("id")?;
                let options = options.into_vm("options")?;
                let out = midi_vm::destack_device_midi_input_port_open(
                    self.call_context,
                    context,
                    id,
                    options,
                )?;
                Ok(out)
            }
            None => {
                let id = id.into_native("id")?;
                let options = options.into_native("options")?;
                let mut out = std::mem::MaybeUninit::<resource::MidiInputPortHandle>::uninit();
                unsafe {
                    midi_native::destack_device_midi_input_port_open(
                        self.call_context,
                        out.as_mut_ptr(),
                        id,
                        options,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// Read one MIDI input record.
    pub(crate) fn destack_device_midi_input_read(
        &mut self,
        handle: resource::MidiInputPortHandle,
        timeoutns: u64,
    ) -> RuntimeResult<HarnessValue<MidiInputRecord, MidiInputRecordVm>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = midi_vm::destack_device_midi_input_read(
                    self.call_context,
                    context,
                    handle,
                    timeoutns,
                )?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let mut out = std::mem::MaybeUninit::<MidiInputRecord>::uninit();
                unsafe {
                    midi_native::destack_device_midi_input_read(
                        self.call_context,
                        out.as_mut_ptr(),
                        handle,
                        timeoutns,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Read one batch of MIDI input records.
    pub(crate) fn destack_device_midi_input_read_batch(
        &mut self,
        handle: resource::MidiInputPortHandle,
        maxrecords: u32,
        timeoutns: u64,
    ) -> RuntimeResult<HarnessValue<NativeArray<MidiInputRecord>, VmArray<MidiInputRecordVm>>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = midi_vm::destack_device_midi_input_read_batch(
                    self.call_context,
                    context,
                    handle,
                    maxrecords,
                    timeoutns,
                )?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let mut out = std::mem::MaybeUninit::<NativeArray<MidiInputRecord>>::uninit();
                unsafe {
                    midi_native::destack_device_midi_input_read_batch(
                        self.call_context,
                        out.as_mut_ptr(),
                        handle,
                        maxrecords,
                        timeoutns,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Poll one MIDI input record without blocking.
    pub(crate) fn destack_device_midi_input_try_read(
        &mut self,
        handle: resource::MidiInputPortHandle,
    ) -> RuntimeResult<HarnessValue<MidiInputRecord, MidiInputRecordVm>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = midi_vm::destack_device_midi_input_try_read(
                    self.call_context,
                    context,
                    handle,
                )?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let mut out = std::mem::MaybeUninit::<MidiInputRecord>::uninit();
                unsafe {
                    midi_native::destack_device_midi_input_try_read(
                        self.call_context,
                        out.as_mut_ptr(),
                        handle,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Poll one batch of MIDI input records without blocking.
    pub(crate) fn destack_device_midi_input_try_read_batch(
        &mut self,
        handle: resource::MidiInputPortHandle,
        maxrecords: u32,
    ) -> RuntimeResult<HarnessValue<NativeArray<MidiInputRecord>, VmArray<MidiInputRecordVm>>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = midi_vm::destack_device_midi_input_try_read_batch(
                    self.call_context,
                    context,
                    handle,
                    maxrecords,
                )?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let mut out = std::mem::MaybeUninit::<NativeArray<MidiInputRecord>>::uninit();
                unsafe {
                    midi_native::destack_device_midi_input_try_read_batch(
                        self.call_context,
                        out.as_mut_ptr(),
                        handle,
                        maxrecords,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Create one virtual MIDI input endpoint.
    pub(crate) fn destack_device_midi_input_virtual_create(
        &mut self,
        options: HarnessValue<MidiVirtualInputCreateOptions, MidiVirtualInputCreateOptionsVm>,
    ) -> RuntimeResult<resource::MidiInputPortHandle> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let options = options.into_vm("options")?;
                let out = midi_vm::destack_device_midi_input_virtual_create(
                    self.call_context,
                    context,
                    options,
                )?;
                Ok(out)
            }
            None => {
                let options = options.into_native("options")?;
                let mut out = std::mem::MaybeUninit::<resource::MidiInputPortHandle>::uninit();
                unsafe {
                    midi_native::destack_device_midi_input_virtual_create(
                        self.call_context,
                        out.as_mut_ptr(),
                        options,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// Close one opened MIDI output endpoint.
    pub(crate) fn destack_device_midi_output_port_close(
        &mut self,
        handle: resource::MidiOutputPortHandle,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                midi_vm::destack_device_midi_output_port_close(self.call_context, context, handle)
            }
            None => unsafe {
                midi_native::destack_device_midi_output_port_close(self.call_context, handle)
            },
        }
    }

    /// Describe one opened MIDI output endpoint.
    pub(crate) fn destack_device_midi_output_port_descriptor(
        &mut self,
        handle: resource::MidiOutputPortHandle,
    ) -> RuntimeResult<HarnessValue<MidiPortDescriptor, MidiPortDescriptorVm>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = midi_vm::destack_device_midi_output_port_descriptor(
                    self.call_context,
                    context,
                    handle,
                )?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let mut out = std::mem::MaybeUninit::<MidiPortDescriptor>::uninit();
                unsafe {
                    midi_native::destack_device_midi_output_port_descriptor(
                        self.call_context,
                        out.as_mut_ptr(),
                        handle,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// List available MIDI output endpoints.
    pub(crate) fn destack_device_midi_output_port_list(
        &mut self,
        options: HarnessValue<MidiPortListOptions, MidiPortListOptionsVm>,
    ) -> RuntimeResult<HarnessValue<NativeSlice<MidiPortDescriptor>, VmSlice<MidiPortDescriptorVm>>>
    {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let options = options.into_vm("options")?;
                let out = midi_vm::destack_device_midi_output_port_list(
                    self.call_context,
                    context,
                    options,
                )?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let options = options.into_native("options")?;
                let mut out = std::mem::MaybeUninit::<NativeSlice<MidiPortDescriptor>>::uninit();
                unsafe {
                    midi_native::destack_device_midi_output_port_list(
                        self.call_context,
                        out.as_mut_ptr(),
                        options,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Open one MIDI output endpoint.
    pub(crate) fn destack_device_midi_output_port_open(
        &mut self,
        id: HarnessValue<NativeStringRef, vm::StringHandle>,
        options: HarnessValue<MidiOutputPortOpenOptions, MidiOutputPortOpenOptionsVm>,
    ) -> RuntimeResult<resource::MidiOutputPortHandle> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let id = id.into_vm("id")?;
                let options = options.into_vm("options")?;
                let out = midi_vm::destack_device_midi_output_port_open(
                    self.call_context,
                    context,
                    id,
                    options,
                )?;
                Ok(out)
            }
            None => {
                let id = id.into_native("id")?;
                let options = options.into_native("options")?;
                let mut out = std::mem::MaybeUninit::<resource::MidiOutputPortHandle>::uninit();
                unsafe {
                    midi_native::destack_device_midi_output_port_open(
                        self.call_context,
                        out.as_mut_ptr(),
                        id,
                        options,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// Create one virtual MIDI output endpoint.
    pub(crate) fn destack_device_midi_output_virtual_create(
        &mut self,
        options: HarnessValue<MidiVirtualOutputCreateOptions, MidiVirtualOutputCreateOptionsVm>,
    ) -> RuntimeResult<resource::MidiOutputPortHandle> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let options = options.into_vm("options")?;
                let out = midi_vm::destack_device_midi_output_virtual_create(
                    self.call_context,
                    context,
                    options,
                )?;
                Ok(out)
            }
            None => {
                let options = options.into_native("options")?;
                let mut out = std::mem::MaybeUninit::<resource::MidiOutputPortHandle>::uninit();
                unsafe {
                    midi_native::destack_device_midi_output_virtual_create(
                        self.call_context,
                        out.as_mut_ptr(),
                        options,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// Write one batch of outbound MIDI records.
    pub(crate) fn destack_device_midi_output_write(
        &mut self,
        handle: resource::MidiOutputPortHandle,
        records: HarnessValue<NativeArray<MidiOutputRecord>, VmArray<MidiOutputRecordVm>>,
    ) -> RuntimeResult<u32> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let records = records.into_vm("records")?;
                let out = midi_vm::destack_device_midi_output_write(
                    self.call_context,
                    context,
                    handle,
                    records,
                )?;
                Ok(out)
            }
            None => {
                let records = records.into_native("records")?;
                let mut out = std::mem::MaybeUninit::<u32>::uninit();
                unsafe {
                    midi_native::destack_device_midi_output_write(
                        self.call_context,
                        out.as_mut_ptr(),
                        handle,
                        records,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }
}

/// Wrapper that carries one native or VM value for generated harness bindings.
pub(crate) enum HarnessValue<Native, Vm> {
    /// Native value variant.
    Native(Native),
    /// VM value variant.
    Vm(Vm),
}

impl<Native: Clone, Vm: Clone> Clone for HarnessValue<Native, Vm> {
    fn clone(&self) -> Self {
        match self {
            HarnessValue::Native(value) => HarnessValue::Native(value.clone()),
            HarnessValue::Vm(value) => HarnessValue::Vm(value.clone()),
        }
    }
}

impl<Native, Vm> HarnessValue<Native, Vm> {
    /// Convert one wrapper into one native value.
    fn into_native(self, label: &str) -> RuntimeResult<Native> {
        match self {
            HarnessValue::Native(value) => Ok(value),
            HarnessValue::Vm(_) => Err(RuntimeError::from(
                HarnessPlatformError::invalid_argument_value(
                    label,
                    "expected native harness value",
                ),
            )
            .boxed()),
        }
    }

    /// Convert one wrapper into one VM value.
    fn into_vm(self, label: &str) -> RuntimeResult<Vm> {
        match self {
            HarnessValue::Vm(value) => Ok(value),
            HarnessValue::Native(_) => Err(RuntimeError::from(
                HarnessPlatformError::invalid_argument_value(label, "expected VM harness value"),
            )
            .boxed()),
        }
    }
}
