// generated by generate-bindings: test harness, do not edit

#![allow(dead_code)]
#![allow(unused_imports)]

use super::*;
use crate::diagnostic::{RuntimeError, RuntimeResult};
use crate::platform::console::{native as console_native, vm as console_vm};
use crate::platform::{NativeStringRef, PlatformError as HarnessPlatformError};
use destack_vm as vm;

impl<'call> ConsoleHarnessContext<'call> {
    /// Return the generated VM context if available.
    #[allow(clippy::mut_from_ref)]
    fn generated_vm_context_mut(&self) -> Option<&mut vm::ExternalCallContext<'_>> {
        self.vm_context
            .map(|context| unsafe { &mut *(context as *mut vm::ExternalCallContext<'_>) })
    }

    /// Return one standardized value payload for native and VM variants.
    pub(crate) fn harness_value<Native, Vm>(&self, native: Native) -> HarnessValue<Native, Vm> {
        HarnessValue::Native(native)
    }

    /// Return one standardized value payload for VM and native variants.
    pub(crate) fn harness_value_vm<Native, Vm>(&self, vm: Vm) -> HarnessValue<Native, Vm> {
        HarnessValue::Vm(vm)
    }

    /// Write an error line to stderr.
    ///
    /// Writes the provided string payload directly to the selected console stream.
    /// Does not apply formatting, buffering, or retry behavior beyond the underlying platform write call.
    ///
    /// # Platform
    /// all runtime targets that include the native platform runtime.
    /// Uses write(2) to stderr on Unix, WriteFile on Windows stderr handle.
    ///
    /// # Errors
    /// Returns ioWriteFailed, ioBrokenPipe, notSupported.
    ///
    /// # Security
    /// Requires `console.write`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_console_error(
        &mut self,
        argument_value: HarnessValue<NativeStringRef, vm::StringHandle>,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let argument_value = argument_value.into_vm("argument_value")?;
                console_vm::destack_console_error(self.call_context, context, argument_value)
            }
            None => {
                let argument_value = argument_value.into_native("argument_value")?;
                unsafe { console_native::destack_console_error(self.call_context, argument_value) }
            }
        }
    }

    /// Write an info line to stdout.
    ///
    /// Writes the provided string payload directly to the selected console stream.
    /// Does not apply formatting, buffering, or retry behavior beyond the underlying platform write call.
    ///
    /// # Platform
    /// all runtime targets that include the native platform runtime.
    /// Uses write(2) to stdout on Unix, WriteFile on Windows stdout handle.
    ///
    /// # Errors
    /// Returns ioWriteFailed, ioBrokenPipe, notSupported.
    ///
    /// # Security
    /// Requires `console.write`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_console_info(
        &mut self,
        argument_value: HarnessValue<NativeStringRef, vm::StringHandle>,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let argument_value = argument_value.into_vm("argument_value")?;
                console_vm::destack_console_info(self.call_context, context, argument_value)
            }
            None => {
                let argument_value = argument_value.into_native("argument_value")?;
                unsafe { console_native::destack_console_info(self.call_context, argument_value) }
            }
        }
    }

    /// Write a line to stdout.
    ///
    /// Writes the provided string payload directly to the selected console stream.
    /// Does not apply formatting, buffering, or retry behavior beyond the underlying platform write call.
    ///
    /// # Platform
    /// all runtime targets that include the native platform runtime.
    /// Uses write(2) to stdout on Unix, WriteFile on Windows stdout handle.
    ///
    /// # Errors
    /// Returns ioWriteFailed, ioBrokenPipe, notSupported.
    ///
    /// # Security
    /// Requires `console.write`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_console_log(
        &mut self,
        argument_value: HarnessValue<NativeStringRef, vm::StringHandle>,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let argument_value = argument_value.into_vm("argument_value")?;
                console_vm::destack_console_log(self.call_context, context, argument_value)
            }
            None => {
                let argument_value = argument_value.into_native("argument_value")?;
                unsafe { console_native::destack_console_log(self.call_context, argument_value) }
            }
        }
    }

    /// Write a warning line to stderr.
    ///
    /// Writes the provided string payload directly to the selected console stream.
    /// Does not apply formatting, buffering, or retry behavior beyond the underlying platform write call.
    ///
    /// # Platform
    /// all runtime targets that include the native platform runtime.
    /// Uses write(2) to stderr on Unix, WriteFile on Windows stderr handle.
    ///
    /// # Errors
    /// Returns ioWriteFailed, ioBrokenPipe, notSupported.
    ///
    /// # Security
    /// Requires `console.write`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_console_warn(
        &mut self,
        argument_value: HarnessValue<NativeStringRef, vm::StringHandle>,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let argument_value = argument_value.into_vm("argument_value")?;
                console_vm::destack_console_warn(self.call_context, context, argument_value)
            }
            None => {
                let argument_value = argument_value.into_native("argument_value")?;
                unsafe { console_native::destack_console_warn(self.call_context, argument_value) }
            }
        }
    }
}

/// Wrapper that carries one native or VM value for generated harness bindings.
pub(crate) enum HarnessValue<Native, Vm> {
    /// Native value variant.
    Native(Native),
    /// VM value variant.
    Vm(Vm),
}

impl<Native, Vm> HarnessValue<Native, Vm> {
    /// Convert one wrapper into one native value.
    fn into_native(self, label: &str) -> RuntimeResult<Native> {
        match self {
            HarnessValue::Native(value) => Ok(value),
            HarnessValue::Vm(_) => Err(RuntimeError::from(
                HarnessPlatformError::invalid_argument_value(
                    label,
                    "expected native harness value",
                ),
            )
            .boxed()),
        }
    }

    /// Convert one wrapper into one VM value.
    fn into_vm(self, label: &str) -> RuntimeResult<Vm> {
        match self {
            HarnessValue::Vm(value) => Ok(value),
            HarnessValue::Native(_) => Err(RuntimeError::from(
                HarnessPlatformError::invalid_argument_value(label, "expected VM harness value"),
            )
            .boxed()),
        }
    }
}
