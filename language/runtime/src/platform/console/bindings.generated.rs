// generated by generate-bindings: do not edit

#![allow(unused_imports)]
#![allow(clippy::clone_on_copy)]
#![allow(clippy::type_complexity)]

use crate::diagnostic::{RuntimeError, RuntimeResult};
use crate::platform::bindings::{
    BindingBlocking, BindingDescriptor, BindingRegistry, BindingReplayKind, BindingScope,
    NativeBinding, NativeBindingSet, ReplayPolicy, RuntimeWorld, native_call,
};
use crate::platform::{NativeStringRef, PlatformError, RuntimeStatus, abi as platform_abi};
use crate::runtime::{RuntimeCallContext, with_runtime_call_context};
use crate::{binding, vm_binding_set};
use destack_vm as vm;
use destack_vm::Isolate;

use serde::{Deserialize, Serialize};

use crate::platform::console as platform_console;
use crate::platform::console::simulated::{
    native as platform_simulated_native, vm as platform_simulated_vm,
};
use crate::platform::console::{native as platform_native, vm as platform_vm};

/// Read a positional argument value.
#[allow(dead_code)]
fn arg_value(
    args: &[vm::Value],
    index: usize,
    name: &'static str,
    expected: &'static str,
) -> RuntimeResult<vm::Value> {
    let value = args.get(index).copied().ok_or_else(|| {
        RuntimeError::from(PlatformError::invalid_argument_type(name, expected)).boxed()
    })?;

    Ok(value)
}

/// Decode a string argument.
#[allow(dead_code)]
fn decode_string(
    value: vm::Value,
    name: &'static str,
    expected: &'static str,
) -> RuntimeResult<vm::StringHandle> {
    if value.tag() != vm::ValueTag::String {
        return Err(
            RuntimeError::from(PlatformError::invalid_argument_type(name, expected)).boxed(),
        );
    }

    Ok(vm::StringHandle::new(value))
}

/// Decode arguments for destack.console.console.error.
#[inline]
fn decode_destack_console_console_error_args(
    _context: &mut vm::ExternalCallContext<'_>,
    args: &[vm::Value],
) -> RuntimeResult<(vm::StringHandle,)> {
    let argument_value_value = arg_value(args, 0, "argument_value", "string")?;
    let argument_value = decode_string(argument_value_value, "argument_value", "string")?;
    Ok((argument_value,))
}

/// Encode the result for destack.console.console.error.
#[inline]
fn encode_destack_console_console_error_result(
    _context: &mut vm::ExternalCallContext<'_>,
    result: RuntimeResult<()>,
) -> RuntimeResult<vm::Value> {
    result.map(|_| vm::Value::VOID)
}

/// Decode arguments for destack.console.console.info.
#[inline]
fn decode_destack_console_console_info_args(
    _context: &mut vm::ExternalCallContext<'_>,
    args: &[vm::Value],
) -> RuntimeResult<(vm::StringHandle,)> {
    let argument_value_value = arg_value(args, 0, "argument_value", "string")?;
    let argument_value = decode_string(argument_value_value, "argument_value", "string")?;
    Ok((argument_value,))
}

/// Encode the result for destack.console.console.info.
#[inline]
fn encode_destack_console_console_info_result(
    _context: &mut vm::ExternalCallContext<'_>,
    result: RuntimeResult<()>,
) -> RuntimeResult<vm::Value> {
    result.map(|_| vm::Value::VOID)
}

/// Decode arguments for destack.console.console.log.
#[inline]
fn decode_destack_console_console_log_args(
    _context: &mut vm::ExternalCallContext<'_>,
    args: &[vm::Value],
) -> RuntimeResult<(vm::StringHandle,)> {
    let argument_value_value = arg_value(args, 0, "argument_value", "string")?;
    let argument_value = decode_string(argument_value_value, "argument_value", "string")?;
    Ok((argument_value,))
}

/// Encode the result for destack.console.console.log.
#[inline]
fn encode_destack_console_console_log_result(
    _context: &mut vm::ExternalCallContext<'_>,
    result: RuntimeResult<()>,
) -> RuntimeResult<vm::Value> {
    result.map(|_| vm::Value::VOID)
}

/// Decode arguments for destack.console.console.warn.
#[inline]
fn decode_destack_console_console_warn_args(
    _context: &mut vm::ExternalCallContext<'_>,
    args: &[vm::Value],
) -> RuntimeResult<(vm::StringHandle,)> {
    let argument_value_value = arg_value(args, 0, "argument_value", "string")?;
    let argument_value = decode_string(argument_value_value, "argument_value", "string")?;
    Ok((argument_value,))
}

/// Encode the result for destack.console.console.warn.
#[inline]
fn encode_destack_console_console_warn_result(
    _context: &mut vm::ExternalCallContext<'_>,
    result: RuntimeResult<()>,
) -> RuntimeResult<vm::Value> {
    result.map(|_| vm::Value::VOID)
}

/// Replay payload for destack.console.console.error.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
struct ConsoleConsoleErrorReplay {
    /// Replay result payload.
    pub result: Result<(), PlatformError>,
}

/// Replay payload for destack.console.console.info.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
struct ConsoleConsoleInfoReplay {
    /// Replay result payload.
    pub result: Result<(), PlatformError>,
}

/// Replay payload for destack.console.console.log.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
struct ConsoleConsoleLogReplay {
    /// Replay result payload.
    pub result: Result<(), PlatformError>,
}

/// Replay payload for destack.console.console.warn.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
struct ConsoleConsoleWarnReplay {
    /// Replay result payload.
    pub result: Result<(), PlatformError>,
}

/// Binding descriptor for destack.console.console.error.
pub const CONSOLE_CONSOLE_ERROR: BindingDescriptor =
    BindingDescriptor::external_with_requires_and_behavior(
        "destack.console.console.error",
        "export function error(value: string): Result<void, PlatformError>",
        ReplayPolicy::Recordable,
        BindingReplayKind::Regular,
        &["console.write"],
        BindingScope::Os,
        BindingBlocking::Sometimes,
    );

/// Binding descriptor for destack.console.console.info.
pub const CONSOLE_CONSOLE_INFO: BindingDescriptor =
    BindingDescriptor::external_with_requires_and_behavior(
        "destack.console.console.info",
        "export function info(value: string): Result<void, PlatformError>",
        ReplayPolicy::Recordable,
        BindingReplayKind::Regular,
        &["console.write"],
        BindingScope::Os,
        BindingBlocking::Sometimes,
    );

/// Binding descriptor for destack.console.console.log.
pub const CONSOLE_CONSOLE_LOG: BindingDescriptor =
    BindingDescriptor::external_with_requires_and_behavior(
        "destack.console.console.log",
        "export function log(value: string): Result<void, PlatformError>",
        ReplayPolicy::Recordable,
        BindingReplayKind::Regular,
        &["console.write"],
        BindingScope::Os,
        BindingBlocking::Sometimes,
    );

/// Binding descriptor for destack.console.console.warn.
pub const CONSOLE_CONSOLE_WARN: BindingDescriptor =
    BindingDescriptor::external_with_requires_and_behavior(
        "destack.console.console.warn",
        "export function warn(value: string): Result<void, PlatformError>",
        ReplayPolicy::Recordable,
        BindingReplayKind::Regular,
        &["console.write"],
        BindingScope::Os,
        BindingBlocking::Sometimes,
    );

/// Binding descriptors for console.
pub const BINDINGS: &[BindingDescriptor] = &[
    CONSOLE_CONSOLE_ERROR,
    CONSOLE_CONSOLE_INFO,
    CONSOLE_CONSOLE_LOG,
    CONSOLE_CONSOLE_WARN,
];

/// Native binding set for console.
pub const CONSOLE_NATIVE_BINDINGS: NativeBindingSet = NativeBindingSet {
    name: "console",
    bindings: &[
        NativeBinding::new(
            CONSOLE_CONSOLE_ERROR,
            "destack.console.console.error",
            destack_console_console_error as *const (),
        ),
        NativeBinding::new(
            CONSOLE_CONSOLE_INFO,
            "destack.console.console.info",
            destack_console_console_info as *const (),
        ),
        NativeBinding::new(
            CONSOLE_CONSOLE_LOG,
            "destack.console.console.log",
            destack_console_console_log as *const (),
        ),
        NativeBinding::new(
            CONSOLE_CONSOLE_WARN,
            "destack.console.console.warn",
            destack_console_console_warn as *const (),
        ),
    ],
};

/// Native replay implementations for console bindings.
#[inline]
fn destack_console_console_error_replay(
    context: &RuntimeCallContext,
    world: RuntimeWorld,
    argument_value: NativeStringRef,
) -> RuntimeResult<()> {
    let _ = &argument_value;

    context.replay().run_binding_with_payload_policy(
        CONSOLE_CONSOLE_ERROR,
        context.replay_payload_for(CONSOLE_CONSOLE_ERROR)?,
        || match world {
            RuntimeWorld::Host => unsafe {
                platform_native::destack_console_error(context, argument_value)
            },
            RuntimeWorld::Simulated => unsafe {
                platform_simulated_native::destack_console_error(context, argument_value)
            },
        },
        |result| {
            if let Ok(()) = result {
                let result_recorded = ();
                let payload = ConsoleConsoleErrorReplay {
                    result: Ok(result_recorded),
                };
                return Ok(Some(payload));
            }

            if let Err(error) = result {
                let payload = {
                    let result = Err(PlatformError::from(error.as_ref()));
                    ConsoleConsoleErrorReplay { result }
                };
                return Ok(Some(payload));
            }

            Ok(None)
        },
        |payload| {
            // replay result
            match payload.result {
                Ok(()) => Ok(()),
                Err(error) => Err(RuntimeError::from(error).boxed()),
            }
        },
    )
}

#[inline]
fn destack_console_console_info_replay(
    context: &RuntimeCallContext,
    world: RuntimeWorld,
    argument_value: NativeStringRef,
) -> RuntimeResult<()> {
    let _ = &argument_value;

    context.replay().run_binding_with_payload_policy(
        CONSOLE_CONSOLE_INFO,
        context.replay_payload_for(CONSOLE_CONSOLE_INFO)?,
        || match world {
            RuntimeWorld::Host => unsafe {
                platform_native::destack_console_info(context, argument_value)
            },
            RuntimeWorld::Simulated => unsafe {
                platform_simulated_native::destack_console_info(context, argument_value)
            },
        },
        |result| {
            if let Ok(()) = result {
                let result_recorded = ();
                let payload = ConsoleConsoleInfoReplay {
                    result: Ok(result_recorded),
                };
                return Ok(Some(payload));
            }

            if let Err(error) = result {
                let payload = {
                    let result = Err(PlatformError::from(error.as_ref()));
                    ConsoleConsoleInfoReplay { result }
                };
                return Ok(Some(payload));
            }

            Ok(None)
        },
        |payload| {
            // replay result
            match payload.result {
                Ok(()) => Ok(()),
                Err(error) => Err(RuntimeError::from(error).boxed()),
            }
        },
    )
}

#[inline]
fn destack_console_console_log_replay(
    context: &RuntimeCallContext,
    world: RuntimeWorld,
    argument_value: NativeStringRef,
) -> RuntimeResult<()> {
    let _ = &argument_value;

    context.replay().run_binding_with_payload_policy(
        CONSOLE_CONSOLE_LOG,
        context.replay_payload_for(CONSOLE_CONSOLE_LOG)?,
        || match world {
            RuntimeWorld::Host => unsafe {
                platform_native::destack_console_log(context, argument_value)
            },
            RuntimeWorld::Simulated => unsafe {
                platform_simulated_native::destack_console_log(context, argument_value)
            },
        },
        |result| {
            if let Ok(()) = result {
                let result_recorded = ();
                let payload = ConsoleConsoleLogReplay {
                    result: Ok(result_recorded),
                };
                return Ok(Some(payload));
            }

            if let Err(error) = result {
                let payload = {
                    let result = Err(PlatformError::from(error.as_ref()));
                    ConsoleConsoleLogReplay { result }
                };
                return Ok(Some(payload));
            }

            Ok(None)
        },
        |payload| {
            // replay result
            match payload.result {
                Ok(()) => Ok(()),
                Err(error) => Err(RuntimeError::from(error).boxed()),
            }
        },
    )
}

#[inline]
fn destack_console_console_warn_replay(
    context: &RuntimeCallContext,
    world: RuntimeWorld,
    argument_value: NativeStringRef,
) -> RuntimeResult<()> {
    let _ = &argument_value;

    context.replay().run_binding_with_payload_policy(
        CONSOLE_CONSOLE_WARN,
        context.replay_payload_for(CONSOLE_CONSOLE_WARN)?,
        || match world {
            RuntimeWorld::Host => unsafe {
                platform_native::destack_console_warn(context, argument_value)
            },
            RuntimeWorld::Simulated => unsafe {
                platform_simulated_native::destack_console_warn(context, argument_value)
            },
        },
        |result| {
            if let Ok(()) = result {
                let result_recorded = ();
                let payload = ConsoleConsoleWarnReplay {
                    result: Ok(result_recorded),
                };
                return Ok(Some(payload));
            }

            if let Err(error) = result {
                let payload = {
                    let result = Err(PlatformError::from(error.as_ref()));
                    ConsoleConsoleWarnReplay { result }
                };
                return Ok(Some(payload));
            }

            Ok(None)
        },
        |payload| {
            // replay result
            match payload.result {
                Ok(()) => Ok(()),
                Err(error) => Err(RuntimeError::from(error).boxed()),
            }
        },
    )
}

/// Native export wrappers for console bindings.
#[unsafe(export_name = "destack.console.console.error")]
pub unsafe extern "C" fn destack_console_console_error(
    argument_value: NativeStringRef,
) -> RuntimeStatus {
    native_call(|context| {
        let _ = &argument_value;

        context.check_policy(CONSOLE_CONSOLE_ERROR)?;
        let world = context.check_and_resolve_world(CONSOLE_CONSOLE_ERROR)?;
        destack_console_console_error_replay(context, world, argument_value)
    })
}

#[unsafe(export_name = "destack.console.console.info")]
pub unsafe extern "C" fn destack_console_console_info(
    argument_value: NativeStringRef,
) -> RuntimeStatus {
    native_call(|context| {
        let _ = &argument_value;

        context.check_policy(CONSOLE_CONSOLE_INFO)?;
        let world = context.check_and_resolve_world(CONSOLE_CONSOLE_INFO)?;
        destack_console_console_info_replay(context, world, argument_value)
    })
}

#[unsafe(export_name = "destack.console.console.log")]
pub unsafe extern "C" fn destack_console_console_log(
    argument_value: NativeStringRef,
) -> RuntimeStatus {
    native_call(|context| {
        let _ = &argument_value;

        context.check_policy(CONSOLE_CONSOLE_LOG)?;
        let world = context.check_and_resolve_world(CONSOLE_CONSOLE_LOG)?;
        destack_console_console_log_replay(context, world, argument_value)
    })
}

#[unsafe(export_name = "destack.console.console.warn")]
pub unsafe extern "C" fn destack_console_console_warn(
    argument_value: NativeStringRef,
) -> RuntimeStatus {
    native_call(|context| {
        let _ = &argument_value;

        context.check_policy(CONSOLE_CONSOLE_WARN)?;
        let world = context.check_and_resolve_world(CONSOLE_CONSOLE_WARN)?;
        destack_console_console_warn_replay(context, world, argument_value)
    })
}

/// VM replay implementations for console bindings.
#[inline]
fn destack_console_console_error_vm_replay(
    runtime: &RuntimeCallContext,
    context: &mut vm::ExternalCallContext<'_>,
    world: RuntimeWorld,
    argument_value: vm::StringHandle,
) -> RuntimeResult<vm::Value> {
    let result = runtime
        .replay()
        .run_binding_with_context_and_payload_policy(
            CONSOLE_CONSOLE_ERROR,
            runtime.replay_payload_for(CONSOLE_CONSOLE_ERROR)?,
            context,
            |context| match world {
                RuntimeWorld::Host => {
                    platform_vm::destack_console_error(runtime, context, argument_value)
                }
                RuntimeWorld::Simulated => {
                    platform_simulated_vm::destack_console_error(runtime, context, argument_value)
                }
            },
            |context, result| {
                let _ = &context;
                if let Ok(()) = result {
                    let result_recorded = ();
                    let payload = ConsoleConsoleErrorReplay {
                        result: Ok(result_recorded),
                    };
                    return Ok(Some(payload));
                }

                if let Err(error) = result {
                    let payload = {
                        let result = Err(PlatformError::from(error.as_ref()));
                        ConsoleConsoleErrorReplay { result }
                    };
                    return Ok(Some(payload));
                }

                Ok(None)
            },
            |context, payload| {
                let _ = &context;
                // replay result
                match payload.result {
                    Ok(()) => Ok(()),
                    Err(error) => Err(RuntimeError::from(error).boxed()),
                }
            },
        );
    let result = encode_destack_console_console_error_result(context, result)?;
    Ok(result)
}

#[inline]
fn destack_console_console_info_vm_replay(
    runtime: &RuntimeCallContext,
    context: &mut vm::ExternalCallContext<'_>,
    world: RuntimeWorld,
    argument_value: vm::StringHandle,
) -> RuntimeResult<vm::Value> {
    let result = runtime
        .replay()
        .run_binding_with_context_and_payload_policy(
            CONSOLE_CONSOLE_INFO,
            runtime.replay_payload_for(CONSOLE_CONSOLE_INFO)?,
            context,
            |context| match world {
                RuntimeWorld::Host => {
                    platform_vm::destack_console_info(runtime, context, argument_value)
                }
                RuntimeWorld::Simulated => {
                    platform_simulated_vm::destack_console_info(runtime, context, argument_value)
                }
            },
            |context, result| {
                let _ = &context;
                if let Ok(()) = result {
                    let result_recorded = ();
                    let payload = ConsoleConsoleInfoReplay {
                        result: Ok(result_recorded),
                    };
                    return Ok(Some(payload));
                }

                if let Err(error) = result {
                    let payload = {
                        let result = Err(PlatformError::from(error.as_ref()));
                        ConsoleConsoleInfoReplay { result }
                    };
                    return Ok(Some(payload));
                }

                Ok(None)
            },
            |context, payload| {
                let _ = &context;
                // replay result
                match payload.result {
                    Ok(()) => Ok(()),
                    Err(error) => Err(RuntimeError::from(error).boxed()),
                }
            },
        );
    let result = encode_destack_console_console_info_result(context, result)?;
    Ok(result)
}

#[inline]
fn destack_console_console_log_vm_replay(
    runtime: &RuntimeCallContext,
    context: &mut vm::ExternalCallContext<'_>,
    world: RuntimeWorld,
    argument_value: vm::StringHandle,
) -> RuntimeResult<vm::Value> {
    let result = runtime
        .replay()
        .run_binding_with_context_and_payload_policy(
            CONSOLE_CONSOLE_LOG,
            runtime.replay_payload_for(CONSOLE_CONSOLE_LOG)?,
            context,
            |context| match world {
                RuntimeWorld::Host => {
                    platform_vm::destack_console_log(runtime, context, argument_value)
                }
                RuntimeWorld::Simulated => {
                    platform_simulated_vm::destack_console_log(runtime, context, argument_value)
                }
            },
            |context, result| {
                let _ = &context;
                if let Ok(()) = result {
                    let result_recorded = ();
                    let payload = ConsoleConsoleLogReplay {
                        result: Ok(result_recorded),
                    };
                    return Ok(Some(payload));
                }

                if let Err(error) = result {
                    let payload = {
                        let result = Err(PlatformError::from(error.as_ref()));
                        ConsoleConsoleLogReplay { result }
                    };
                    return Ok(Some(payload));
                }

                Ok(None)
            },
            |context, payload| {
                let _ = &context;
                // replay result
                match payload.result {
                    Ok(()) => Ok(()),
                    Err(error) => Err(RuntimeError::from(error).boxed()),
                }
            },
        );
    let result = encode_destack_console_console_log_result(context, result)?;
    Ok(result)
}

#[inline]
fn destack_console_console_warn_vm_replay(
    runtime: &RuntimeCallContext,
    context: &mut vm::ExternalCallContext<'_>,
    world: RuntimeWorld,
    argument_value: vm::StringHandle,
) -> RuntimeResult<vm::Value> {
    let result = runtime
        .replay()
        .run_binding_with_context_and_payload_policy(
            CONSOLE_CONSOLE_WARN,
            runtime.replay_payload_for(CONSOLE_CONSOLE_WARN)?,
            context,
            |context| match world {
                RuntimeWorld::Host => {
                    platform_vm::destack_console_warn(runtime, context, argument_value)
                }
                RuntimeWorld::Simulated => {
                    platform_simulated_vm::destack_console_warn(runtime, context, argument_value)
                }
            },
            |context, result| {
                let _ = &context;
                if let Ok(()) = result {
                    let result_recorded = ();
                    let payload = ConsoleConsoleWarnReplay {
                        result: Ok(result_recorded),
                    };
                    return Ok(Some(payload));
                }

                if let Err(error) = result {
                    let payload = {
                        let result = Err(PlatformError::from(error.as_ref()));
                        ConsoleConsoleWarnReplay { result }
                    };
                    return Ok(Some(payload));
                }

                Ok(None)
            },
            |context, payload| {
                let _ = &context;
                // replay result
                match payload.result {
                    Ok(()) => Ok(()),
                    Err(error) => Err(RuntimeError::from(error).boxed()),
                }
            },
        );
    let result = encode_destack_console_console_warn_result(context, result)?;
    Ok(result)
}

/// Register VM bindings for console.
pub fn register_console_vm_bindings(registry: &mut BindingRegistry, isolate: &mut Isolate) {
    {
        binding!(
            registry,
            isolate,
            CONSOLE_CONSOLE_ERROR,
            move |context, args| {
                with_runtime_call_context(|runtime| {
                    // decode args
                    let (argument_value,) =
                        decode_destack_console_console_error_args(context, args)?;

                    // execute binding
                    runtime.check_policy(CONSOLE_CONSOLE_ERROR)?;
                    let world = runtime.check_and_resolve_world(CONSOLE_CONSOLE_ERROR)?;
                    destack_console_console_error_vm_replay(runtime, context, world, argument_value)
                })
                .map_err(Into::into)
            }
        );
    }
    {
        binding!(
            registry,
            isolate,
            CONSOLE_CONSOLE_INFO,
            move |context, args| {
                with_runtime_call_context(|runtime| {
                    // decode args
                    let (argument_value,) =
                        decode_destack_console_console_info_args(context, args)?;

                    // execute binding
                    runtime.check_policy(CONSOLE_CONSOLE_INFO)?;
                    let world = runtime.check_and_resolve_world(CONSOLE_CONSOLE_INFO)?;
                    destack_console_console_info_vm_replay(runtime, context, world, argument_value)
                })
                .map_err(Into::into)
            }
        );
    }
    {
        binding!(
            registry,
            isolate,
            CONSOLE_CONSOLE_LOG,
            move |context, args| {
                with_runtime_call_context(|runtime| {
                    // decode args
                    let (argument_value,) = decode_destack_console_console_log_args(context, args)?;

                    // execute binding
                    runtime.check_policy(CONSOLE_CONSOLE_LOG)?;
                    let world = runtime.check_and_resolve_world(CONSOLE_CONSOLE_LOG)?;
                    destack_console_console_log_vm_replay(runtime, context, world, argument_value)
                })
                .map_err(Into::into)
            }
        );
    }
    {
        binding!(
            registry,
            isolate,
            CONSOLE_CONSOLE_WARN,
            move |context, args| {
                with_runtime_call_context(|runtime| {
                    // decode args
                    let (argument_value,) =
                        decode_destack_console_console_warn_args(context, args)?;

                    // execute binding
                    runtime.check_policy(CONSOLE_CONSOLE_WARN)?;
                    let world = runtime.check_and_resolve_world(CONSOLE_CONSOLE_WARN)?;
                    destack_console_console_warn_vm_replay(runtime, context, world, argument_value)
                })
                .map_err(Into::into)
            }
        );
    }
}

/// Install VM bindings for console.
pub fn install_console_vm_bindings(registry: &mut BindingRegistry, isolate: &mut Isolate) {
    register_console_vm_bindings(registry, isolate);
}

vm_binding_set!(pub CONSOLE_VM_BINDINGS, "console", install_console_vm_bindings);
