// generated by generate-bindings: test harness, do not edit

#![allow(dead_code)]
#![allow(unused_imports)]
#![allow(clippy::type_complexity)]

use crate::diagnostic::{RuntimeError, RuntimeResult};
use crate::platform::abi::{NativeSlice, NativeStringRef, NativeStringSlice};
use crate::platform::tls::tests::TlsHarnessContext;
use crate::platform::tls::{
    TlsContextOptions, TlsContextOptionsVm, TlsHandshakeStatus, TlsHostnameVerificationMode,
    TlsRole, TlsSessionResumptionMode, TlsSessionResumptionState, TlsVersion, native as tls_native,
    vm as tls_vm,
};
use crate::platform::{
    NativeAbiCodec, NativeArray, PlatformError as HarnessPlatformError, VmAbiCodec, VmArray,
    VmSlice, fs, resource,
};
use destack_vm as vm;

impl<'call> TlsHarnessContext<'call> {
    /// Return the generated VM context if available.
    #[allow(clippy::mut_from_ref)]
    fn generated_vm_context_mut(&self) -> Option<&mut vm::ExternalCallContext<'_>> {
        self.vm_context
            .map(|context| unsafe { &mut *(context as *mut vm::ExternalCallContext<'_>) })
    }

    /// Return one standardized value payload for native and VM variants.
    pub(crate) fn harness_value<Native, Vm>(&self, native: Native) -> HarnessValue<Native, Vm> {
        HarnessValue::Native(native)
    }

    /// Return one standardized value payload for VM and native variants.
    pub(crate) fn harness_value_vm<Native, Vm>(&self, vm: Vm) -> HarnessValue<Native, Vm> {
        HarnessValue::Vm(vm)
    }

    /// Encode one value into one native or VM harness payload.
    pub(crate) fn harness_value_from<Native, Vm>(
        &mut self,
        value: Native::Value,
    ) -> RuntimeResult<HarnessValue<Native, Vm>>
    where
        Native: NativeAbiCodec,
        Vm: VmAbiCodec<Value = Native::Value>,
    {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let context = &mut context.write();
                let value = Vm::from_value(context, value)?;
                Ok(HarnessValue::Vm(value))
            }
            None => {
                let value = Native::from_value(self.call_context, value);
                Ok(HarnessValue::Native(value))
            }
        }
    }

    /// Decode one native or VM harness payload into one value.
    pub(crate) fn harness_value_into<Native, Vm>(
        &mut self,
        value: HarnessValue<Native, Vm>,
    ) -> RuntimeResult<Native::Value>
    where
        Native: NativeAbiCodec,
        Vm: VmAbiCodec<Value = Native::Value>,
    {
        match value {
            HarnessValue::Native(value) => unsafe { Native::into_value(value) },
            HarnessValue::Vm(value) => {
                let context = self.generated_vm_context_mut().ok_or_else(|| {
                    RuntimeError::from(HarnessPlatformError::invalid_argument_value(
                        "context",
                        "expected vm context",
                    ))
                    .boxed()
                })?;
                let context = &context.read();
                value.into_value(context)
            }
        }
    }

    /// Call one generated platform test harness binding.
    pub(crate) fn destack_tls_context_close(
        &mut self,
        handle: resource::TlsContextHandle,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => tls_vm::destack_tls_context_close(self.call_context, context, handle),
            None => unsafe { tls_native::destack_tls_context_close(self.call_context, handle) },
        }
    }

    /// Call one generated platform test harness binding.
    pub(crate) fn destack_tls_context_open(
        &mut self,
        options: HarnessValue<TlsContextOptions, TlsContextOptionsVm>,
    ) -> RuntimeResult<resource::TlsContextHandle> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let options = options.into_vm("options")?;
                let out = tls_vm::destack_tls_context_open(self.call_context, context, options)?;
                Ok(out)
            }
            None => {
                let options = options.into_native("options")?;
                let mut out = std::mem::MaybeUninit::<resource::TlsContextHandle>::uninit();
                unsafe {
                    tls_native::destack_tls_context_open(
                        self.call_context,
                        out.as_mut_ptr(),
                        options,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// Call one generated platform test harness binding.
    pub(crate) fn destack_tls_context_set_cipher_suites(
        &mut self,
        handle: resource::TlsContextHandle,
        suites: HarnessValue<NativeStringSlice, VmSlice<vm::StringHandle>>,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let suites = suites.into_vm("suites")?;
                tls_vm::destack_tls_context_set_cipher_suites(
                    self.call_context,
                    context,
                    handle,
                    suites,
                )
            }
            None => unsafe {
                let suites = suites.into_native("suites")?;
                tls_native::destack_tls_context_set_cipher_suites(self.call_context, handle, suites)
            },
        }
    }

    /// Call one generated platform test harness binding.
    pub(crate) fn destack_tls_context_set_groups(
        &mut self,
        handle: resource::TlsContextHandle,
        groups: HarnessValue<NativeStringSlice, VmSlice<vm::StringHandle>>,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let groups = groups.into_vm("groups")?;
                tls_vm::destack_tls_context_set_groups(self.call_context, context, handle, groups)
            }
            None => unsafe {
                let groups = groups.into_native("groups")?;
                tls_native::destack_tls_context_set_groups(self.call_context, handle, groups)
            },
        }
    }

    /// Call one generated platform test harness binding.
    pub(crate) fn destack_tls_context_set_hostname_verification_mode(
        &mut self,
        handle: resource::TlsContextHandle,
        mode: TlsHostnameVerificationMode,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => tls_vm::destack_tls_context_set_hostname_verification_mode(
                self.call_context,
                context,
                handle,
                mode,
            ),
            None => unsafe {
                tls_native::destack_tls_context_set_hostname_verification_mode(
                    self.call_context,
                    handle,
                    mode,
                )
            },
        }
    }

    /// Call one generated platform test harness binding.
    pub(crate) fn destack_tls_context_set_identity_pem(
        &mut self,
        handle: resource::TlsContextHandle,
        certificatechainpem: HarnessValue<NativeSlice<u8>, VmSlice<u8>>,
        privatekeypem: HarnessValue<NativeSlice<u8>, VmSlice<u8>>,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let certificatechainpem = certificatechainpem.into_vm("certificatechainpem")?;
                let privatekeypem = privatekeypem.into_vm("privatekeypem")?;
                tls_vm::destack_tls_context_set_identity_pem(
                    self.call_context,
                    context,
                    handle,
                    certificatechainpem,
                    privatekeypem,
                )
            }
            None => unsafe {
                let certificatechainpem = certificatechainpem.into_native("certificatechainpem")?;
                let privatekeypem = privatekeypem.into_native("privatekeypem")?;
                tls_native::destack_tls_context_set_identity_pem(
                    self.call_context,
                    handle,
                    certificatechainpem,
                    privatekeypem,
                )
            },
        }
    }

    /// Call one generated platform test harness binding.
    pub(crate) fn destack_tls_context_set_session_resumption(
        &mut self,
        handle: resource::TlsContextHandle,
        mode: TlsSessionResumptionMode,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => tls_vm::destack_tls_context_set_session_resumption(
                self.call_context,
                context,
                handle,
                mode,
            ),
            None => unsafe {
                tls_native::destack_tls_context_set_session_resumption(
                    self.call_context,
                    handle,
                    mode,
                )
            },
        }
    }

    /// Call one generated platform test harness binding.
    pub(crate) fn destack_tls_context_set_signature_algorithms(
        &mut self,
        handle: resource::TlsContextHandle,
        algorithms: HarnessValue<NativeStringSlice, VmSlice<vm::StringHandle>>,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let algorithms = algorithms.into_vm("algorithms")?;
                tls_vm::destack_tls_context_set_signature_algorithms(
                    self.call_context,
                    context,
                    handle,
                    algorithms,
                )
            }
            None => unsafe {
                let algorithms = algorithms.into_native("algorithms")?;
                tls_native::destack_tls_context_set_signature_algorithms(
                    self.call_context,
                    handle,
                    algorithms,
                )
            },
        }
    }

    /// Call one generated platform test harness binding.
    pub(crate) fn destack_tls_context_set_trust_anchors_pem(
        &mut self,
        handle: resource::TlsContextHandle,
        trustanchorspem: HarnessValue<NativeSlice<u8>, VmSlice<u8>>,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let trustanchorspem = trustanchorspem.into_vm("trustanchorspem")?;
                tls_vm::destack_tls_context_set_trust_anchors_pem(
                    self.call_context,
                    context,
                    handle,
                    trustanchorspem,
                )
            }
            None => unsafe {
                let trustanchorspem = trustanchorspem.into_native("trustanchorspem")?;
                tls_native::destack_tls_context_set_trust_anchors_pem(
                    self.call_context,
                    handle,
                    trustanchorspem,
                )
            },
        }
    }

    /// Call one generated platform test harness binding.
    pub(crate) fn destack_tls_session_close(
        &mut self,
        handle: resource::TlsSessionHandle,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => tls_vm::destack_tls_session_close(self.call_context, context, handle),
            None => unsafe { tls_native::destack_tls_session_close(self.call_context, handle) },
        }
    }

    /// Call one generated platform test harness binding.
    pub(crate) fn destack_tls_session_export_keying_material(
        &mut self,
        handle: resource::TlsSessionHandle,
        label: HarnessValue<NativeStringRef, vm::StringHandle>,
        argument_context: HarnessValue<NativeSlice<u8>, VmSlice<u8>>,
        outputlength: u32,
    ) -> RuntimeResult<HarnessValue<NativeSlice<u8>, VmSlice<u8>>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let label = label.into_vm("label")?;
                let argument_context = argument_context.into_vm("argument_context")?;
                let out = tls_vm::destack_tls_session_export_keying_material(
                    self.call_context,
                    context,
                    handle,
                    label,
                    argument_context,
                    outputlength,
                )?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let label = label.into_native("label")?;
                let argument_context = argument_context.into_native("argument_context")?;
                let mut out = std::mem::MaybeUninit::<NativeSlice<u8>>::uninit();
                unsafe {
                    tls_native::destack_tls_session_export_keying_material(
                        self.call_context,
                        out.as_mut_ptr(),
                        handle,
                        label,
                        argument_context,
                        outputlength,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Call one generated platform test harness binding.
    pub(crate) fn destack_tls_session_handshake(
        &mut self,
        handle: resource::TlsSessionHandle,
    ) -> RuntimeResult<TlsHandshakeStatus> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out =
                    tls_vm::destack_tls_session_handshake(self.call_context, context, handle)?;
                Ok(out)
            }
            None => {
                let mut out = std::mem::MaybeUninit::<TlsHandshakeStatus>::uninit();
                unsafe {
                    tls_native::destack_tls_session_handshake(
                        self.call_context,
                        out.as_mut_ptr(),
                        handle,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// Call one generated platform test harness binding.
    pub(crate) fn destack_tls_session_negotiated_alpn(
        &mut self,
        handle: resource::TlsSessionHandle,
    ) -> RuntimeResult<HarnessValue<NativeSlice<u8>, VmSlice<u8>>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = tls_vm::destack_tls_session_negotiated_alpn(
                    self.call_context,
                    context,
                    handle,
                )?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let mut out = std::mem::MaybeUninit::<NativeSlice<u8>>::uninit();
                unsafe {
                    tls_native::destack_tls_session_negotiated_alpn(
                        self.call_context,
                        out.as_mut_ptr(),
                        handle,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Call one generated platform test harness binding.
    pub(crate) fn destack_tls_session_open(
        &mut self,
        argument_context: resource::TlsContextHandle,
        socket: resource::SocketHandle,
        servername: HarnessValue<NativeStringRef, vm::StringHandle>,
    ) -> RuntimeResult<resource::TlsSessionHandle> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let servername = servername.into_vm("servername")?;
                let out = tls_vm::destack_tls_session_open(
                    self.call_context,
                    context,
                    argument_context,
                    socket,
                    servername,
                )?;
                Ok(out)
            }
            None => {
                let servername = servername.into_native("servername")?;
                let mut out = std::mem::MaybeUninit::<resource::TlsSessionHandle>::uninit();
                unsafe {
                    tls_native::destack_tls_session_open(
                        self.call_context,
                        out.as_mut_ptr(),
                        argument_context,
                        socket,
                        servername,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// Call one generated platform test harness binding.
    pub(crate) fn destack_tls_session_peer_certificates_pem(
        &mut self,
        handle: resource::TlsSessionHandle,
    ) -> RuntimeResult<HarnessValue<NativeSlice<u8>, VmSlice<u8>>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = tls_vm::destack_tls_session_peer_certificates_pem(
                    self.call_context,
                    context,
                    handle,
                )?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let mut out = std::mem::MaybeUninit::<NativeSlice<u8>>::uninit();
                unsafe {
                    tls_native::destack_tls_session_peer_certificates_pem(
                        self.call_context,
                        out.as_mut_ptr(),
                        handle,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Call one generated platform test harness binding.
    pub(crate) fn destack_tls_session_read(
        &mut self,
        handle: resource::TlsSessionHandle,
        buffer: HarnessValue<NativeSlice<u8>, VmSlice<u8>>,
    ) -> RuntimeResult<u64> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let buffer = buffer.into_vm("buffer")?;
                let out =
                    tls_vm::destack_tls_session_read(self.call_context, context, handle, buffer)?;
                Ok(out)
            }
            None => {
                let buffer = buffer.into_native("buffer")?;
                let mut out = std::mem::MaybeUninit::<u64>::uninit();
                unsafe {
                    tls_native::destack_tls_session_read(
                        self.call_context,
                        out.as_mut_ptr(),
                        handle,
                        buffer,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// Call one generated platform test harness binding.
    pub(crate) fn destack_tls_session_resumption_state(
        &mut self,
        handle: resource::TlsSessionHandle,
    ) -> RuntimeResult<TlsSessionResumptionState> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = tls_vm::destack_tls_session_resumption_state(
                    self.call_context,
                    context,
                    handle,
                )?;
                Ok(out)
            }
            None => {
                let mut out = std::mem::MaybeUninit::<TlsSessionResumptionState>::uninit();
                unsafe {
                    tls_native::destack_tls_session_resumption_state(
                        self.call_context,
                        out.as_mut_ptr(),
                        handle,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// Call one generated platform test harness binding.
    pub(crate) fn destack_tls_session_shutdown(
        &mut self,
        handle: resource::TlsSessionHandle,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                tls_vm::destack_tls_session_shutdown(self.call_context, context, handle)
            }
            None => unsafe { tls_native::destack_tls_session_shutdown(self.call_context, handle) },
        }
    }

    /// Call one generated platform test harness binding.
    pub(crate) fn destack_tls_session_write(
        &mut self,
        handle: resource::TlsSessionHandle,
        buffer: HarnessValue<NativeSlice<u8>, VmSlice<u8>>,
    ) -> RuntimeResult<u64> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let buffer = buffer.into_vm("buffer")?;
                let out =
                    tls_vm::destack_tls_session_write(self.call_context, context, handle, buffer)?;
                Ok(out)
            }
            None => {
                let buffer = buffer.into_native("buffer")?;
                let mut out = std::mem::MaybeUninit::<u64>::uninit();
                unsafe {
                    tls_native::destack_tls_session_write(
                        self.call_context,
                        out.as_mut_ptr(),
                        handle,
                        buffer,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }
}

/// Wrapper that carries one native or VM value for generated harness bindings.
pub(crate) enum HarnessValue<Native, Vm> {
    /// Native value variant.
    Native(Native),
    /// VM value variant.
    Vm(Vm),
}

impl<Native: Clone, Vm: Clone> Clone for HarnessValue<Native, Vm> {
    fn clone(&self) -> Self {
        match self {
            HarnessValue::Native(value) => HarnessValue::Native(value.clone()),
            HarnessValue::Vm(value) => HarnessValue::Vm(value.clone()),
        }
    }
}

impl<Native, Vm> HarnessValue<Native, Vm> {
    /// Convert one wrapper into one native value.
    fn into_native(self, label: &str) -> RuntimeResult<Native> {
        match self {
            HarnessValue::Native(value) => Ok(value),
            HarnessValue::Vm(_) => Err(RuntimeError::from(
                HarnessPlatformError::invalid_argument_value(
                    label,
                    "expected native harness value",
                ),
            )
            .boxed()),
        }
    }

    /// Convert one wrapper into one VM value.
    fn into_vm(self, label: &str) -> RuntimeResult<Vm> {
        match self {
            HarnessValue::Vm(value) => Ok(value),
            HarnessValue::Native(_) => Err(RuntimeError::from(
                HarnessPlatformError::invalid_argument_value(label, "expected VM harness value"),
            )
            .boxed()),
        }
    }
}
