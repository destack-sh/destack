// generated by generate-bindings: test harness, do not edit

#![allow(dead_code)]
#![allow(unused_imports)]

use super::*;
use crate::diagnostic::{RuntimeError, RuntimeResult};
use crate::platform::random::{
    RandomStream, RandomStreamDomain, RandomStreamState, RandomStreamStateVm, SecureRandomInfo,
    SecureRandomInfoVm, SecureRandomSource, native as random_native, vm as random_vm,
};
use crate::platform::{
    NativeArray, NativeSlice, NativeStringRef, PlatformError as HarnessPlatformError, VmArray,
    VmSlice,
};
use destack_vm as vm;

impl<'call> RandomHarnessContext<'call> {
    /// Return the generated VM context if available.
    #[allow(clippy::mut_from_ref)]
    fn generated_vm_context_mut(&self) -> Option<&mut vm::ExternalCallContext<'_>> {
        self.vm_context
            .map(|context| unsafe { &mut *(context as *mut vm::ExternalCallContext<'_>) })
    }

    /// Return one standardized value payload for native and VM variants.
    pub(crate) fn harness_value<Native, Vm>(&self, native: Native) -> HarnessValue<Native, Vm> {
        HarnessValue::Native(native)
    }

    /// Return one standardized value payload for VM and native variants.
    pub(crate) fn harness_value_vm<Native, Vm>(&self, vm: Vm) -> HarnessValue<Native, Vm> {
        HarnessValue::Vm(vm)
    }

    /// Fill a slice with cryptographically secure random bytes.
    ///
    /// Read entropy from host cryptographic RNG facilities.
    /// Entropy quality and blocking behavior follow host kernel guarantees.
    ///
    /// # Platform
    /// Unix, Windows, and Wasi where host entropy APIs are available.
    /// Uses getrandom(2) or getentropy on Unix and BCryptGenRandom on Windows.
    ///
    /// # Errors
    /// Returns randomUnavailable, notSupported.
    ///
    /// # Security
    /// Requires `random.secure`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_random_secure_bytes(
        &mut self,
        buffer: HarnessValue<NativeSlice<u8>, VmSlice<u8>>,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let buffer = buffer.into_vm("buffer")?;
                random_vm::destack_random_secure_bytes(self.call_context, context, buffer)
            }
            None => {
                let buffer = buffer.into_native("buffer")?;
                unsafe { random_native::destack_random_secure_bytes(self.call_context, buffer) }
            }
        }
    }

    /// Fill a slice with secure random bytes without blocking.
    ///
    /// Try to read secure entropy without blocking the current execution context.
    /// Fails with `ioWouldBlock` when the host source requires blocking.
    ///
    /// # Platform
    /// Unix, Windows, and Wasi where host entropy APIs are available.
    /// Uses nonblocking host entropy APIs when available and runtime fallbacks otherwise.
    ///
    /// # Errors
    /// Returns randomUnavailable, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `random.secure`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_random_secure_bytes_try(
        &mut self,
        buffer: HarnessValue<NativeSlice<u8>, VmSlice<u8>>,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let buffer = buffer.into_vm("buffer")?;
                random_vm::destack_random_secure_bytes_try(self.call_context, context, buffer)
            }
            None => {
                let buffer = buffer.into_native("buffer")?;
                unsafe { random_native::destack_random_secure_bytes_try(self.call_context, buffer) }
            }
        }
    }

    /// Query secure randomness source metadata.
    ///
    /// Return source metadata for the secure random backend selected by the runtime.
    /// Metadata values are normalized across host operating systems.
    ///
    /// # Platform
    /// Unix, Windows, and Wasi where host entropy APIs are available.
    /// Uses runtime source selection metadata.
    ///
    /// # Errors
    /// Returns randomUnavailable, notSupported.
    ///
    /// # Security
    /// Requires `random.secure`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_random_secure_info(
        &mut self,
    ) -> RuntimeResult<HarnessValue<SecureRandomInfo, SecureRandomInfoVm>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = random_vm::destack_random_secure_info(self.call_context, context)?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let mut out = std::mem::MaybeUninit::<SecureRandomInfo>::uninit();
                unsafe {
                    random_native::destack_random_secure_info(self.call_context, out.as_mut_ptr())?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Export deterministic stream state.
    ///
    /// Serialize one stream state into a versioned byte payload for checkpointing.
    /// State bytes are opaque to callers and validated on restore.
    ///
    /// # Platform
    /// Runtime-level operation available on all native runtime targets.
    /// Uses runtime deterministic PRNG state serialization.
    ///
    /// # Errors
    /// Returns randomUnavailable, invalidArgument, notSupported.
    ///
    /// # Security
    /// Requires `random.deterministic`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_random_stream_export(
        &mut self,
        stream: RandomStream,
    ) -> RuntimeResult<HarnessValue<RandomStreamState, RandomStreamStateVm>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out =
                    random_vm::destack_random_stream_export(self.call_context, context, stream)?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let mut out = std::mem::MaybeUninit::<RandomStreamState>::uninit();
                unsafe {
                    random_native::destack_random_stream_export(
                        self.call_context,
                        out.as_mut_ptr(),
                        stream,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Fill a slice with deterministic random bytes from the default stream.
    ///
    /// Advance the default stream to fill one mutable buffer.
    /// Buffer fill order is deterministic for a fixed stream state.
    ///
    /// # Platform
    /// Runtime-level operation available on all native runtime targets.
    /// Uses runtime deterministic PRNG state.
    ///
    /// # Errors
    /// Returns randomUnavailable, notSupported.
    ///
    /// # Security
    /// Requires `random.deterministic`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_random_fill_bytes(
        &mut self,
        buffer: HarnessValue<NativeSlice<u8>, VmSlice<u8>>,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let buffer = buffer.into_vm("buffer")?;
                random_vm::destack_random_fill_bytes(self.call_context, context, buffer)
            }
            None => {
                let buffer = buffer.into_native("buffer")?;
                unsafe { random_native::destack_random_fill_bytes(self.call_context, buffer) }
            }
        }
    }

    /// Fill a slice with deterministic random bytes from a specific stream.
    ///
    /// Advance the selected stream to fill one mutable buffer.
    /// Buffer fill order is deterministic for a fixed stream state.
    ///
    /// # Platform
    /// Runtime-level operation available on all native runtime targets.
    /// Uses runtime deterministic PRNG state.
    ///
    /// # Errors
    /// Returns randomUnavailable, notSupported.
    ///
    /// # Security
    /// Requires `random.deterministic`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_random_fill_bytes_from(
        &mut self,
        stream: RandomStream,
        buffer: HarnessValue<NativeSlice<u8>, VmSlice<u8>>,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let buffer = buffer.into_vm("buffer")?;
                random_vm::destack_random_fill_bytes_from(
                    self.call_context,
                    context,
                    stream,
                    buffer,
                )
            }
            None => {
                let buffer = buffer.into_native("buffer")?;
                unsafe {
                    random_native::destack_random_fill_bytes_from(self.call_context, stream, buffer)
                }
            }
        }
    }

    /// Import deterministic stream state.
    ///
    /// Restore one stream state from a versioned byte payload.
    /// Restored state replaces the prior stream state atomically.
    ///
    /// # Platform
    /// Runtime-level operation available on all native runtime targets.
    /// Uses runtime deterministic PRNG state deserialization.
    ///
    /// # Errors
    /// Returns randomUnavailable, invalidArgument, ioInvalidData, notSupported.
    ///
    /// # Security
    /// Requires `random.deterministic`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_random_stream_import(
        &mut self,
        stream: RandomStream,
        state: HarnessValue<RandomStreamState, RandomStreamStateVm>,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let state = state.into_vm("state")?;
                random_vm::destack_random_stream_import(self.call_context, context, stream, state)
            }
            None => {
                let state = state.into_native("state")?;
                unsafe {
                    random_native::destack_random_stream_import(self.call_context, stream, state)
                }
            }
        }
    }

    /// Allocate a deterministic random stream in one domain.
    ///
    /// Create one runtime-managed deterministic PRNG stream in the requested domain.
    /// Domain assignment controls stream inheritance and replay grouping behavior.
    ///
    /// # Platform
    /// Runtime-level operation available on all native runtime targets.
    /// Uses runtime deterministic PRNG state.
    ///
    /// # Errors
    /// Returns randomUnavailable, invalidArgument, notSupported.
    ///
    /// # Security
    /// Requires `random.deterministic`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_random_stream_in(
        &mut self,
        domain: RandomStreamDomain,
    ) -> RuntimeResult<RandomStream> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = random_vm::destack_random_stream_in(self.call_context, context, domain)?;
                Ok(out)
            }
            None => {
                let mut out = std::mem::MaybeUninit::<RandomStream>::uninit();
                unsafe {
                    random_native::destack_random_stream_in(
                        self.call_context,
                        out.as_mut_ptr(),
                        domain,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// Advance a deterministic stream by one jump count.
    ///
    /// Move one stream forward by a deterministic jump count without generating intermediate values.
    /// Jump semantics are runtime-defined and versioned for replay compatibility.
    ///
    /// # Platform
    /// Runtime-level operation available on all native runtime targets.
    /// Uses runtime deterministic PRNG jump logic.
    ///
    /// # Errors
    /// Returns randomUnavailable, invalidArgument, notSupported.
    ///
    /// # Security
    /// Requires `random.deterministic`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_random_stream_jump(
        &mut self,
        stream: RandomStream,
        jump: u64,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                random_vm::destack_random_stream_jump(self.call_context, context, stream, jump)
            }
            None => unsafe {
                random_native::destack_random_stream_jump(self.call_context, stream, jump)
            },
        }
    }

    /// Return a deterministic random uint64 from the default stream.
    ///
    /// Advance the default deterministic PRNG stream and return one value.
    /// Stream step behavior is stable for replay within one runtime version.
    ///
    /// # Platform
    /// Runtime-level operation available on all native runtime targets.
    /// Uses runtime deterministic PRNG state.
    ///
    /// # Errors
    /// Returns randomUnavailable, notSupported.
    ///
    /// # Security
    /// Requires `random.deterministic`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_random_next_u64(&mut self) -> RuntimeResult<u64> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = random_vm::destack_random_next_u64(self.call_context, context)?;
                Ok(out)
            }
            None => {
                let mut out = std::mem::MaybeUninit::<u64>::uninit();
                unsafe {
                    random_native::destack_random_next_u64(self.call_context, out.as_mut_ptr())?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// Return a deterministic random uint64 from a specific stream.
    ///
    /// Advance the selected deterministic PRNG stream and return one value.
    /// Stream step behavior is stable for replay within one runtime version.
    ///
    /// # Platform
    /// Runtime-level operation available on all native runtime targets.
    /// Uses runtime deterministic PRNG state.
    ///
    /// # Errors
    /// Returns randomUnavailable, notSupported.
    ///
    /// # Security
    /// Requires `random.deterministic`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_random_next_u64_from(
        &mut self,
        stream: RandomStream,
    ) -> RuntimeResult<u64> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out =
                    random_vm::destack_random_next_u64_from(self.call_context, context, stream)?;
                Ok(out)
            }
            None => {
                let mut out = std::mem::MaybeUninit::<u64>::uninit();
                unsafe {
                    random_native::destack_random_next_u64_from(
                        self.call_context,
                        out.as_mut_ptr(),
                        stream,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// Split a deterministic stream into one child stream.
    ///
    /// Derive one child stream from one parent stream using deterministic split semantics.
    /// Parent and child sequences remain stable for replay in one runtime version.
    ///
    /// # Platform
    /// Runtime-level operation available on all native runtime targets.
    /// Uses runtime deterministic PRNG split logic.
    ///
    /// # Errors
    /// Returns randomUnavailable, invalidArgument, notSupported.
    ///
    /// # Security
    /// Requires `random.deterministic`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_random_stream_split(
        &mut self,
        parent: RandomStream,
    ) -> RuntimeResult<RandomStream> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out =
                    random_vm::destack_random_stream_split(self.call_context, context, parent)?;
                Ok(out)
            }
            None => {
                let mut out = std::mem::MaybeUninit::<RandomStream>::uninit();
                unsafe {
                    random_native::destack_random_stream_split(
                        self.call_context,
                        out.as_mut_ptr(),
                        parent,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// Allocate a deterministic random stream identifier.
    ///
    /// Create one runtime-managed deterministic PRNG stream.
    /// Stream seeding follows runtime determinism and replay policy.
    ///
    /// # Platform
    /// Runtime-level operation available on all native runtime targets.
    /// Uses runtime deterministic PRNG state.
    ///
    /// # Errors
    /// Returns randomUnavailable, notSupported.
    ///
    /// # Security
    /// Requires `random.deterministic`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_random_stream(&mut self) -> RuntimeResult<RandomStream> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = random_vm::destack_random_stream(self.call_context, context)?;
                Ok(out)
            }
            None => {
                let mut out = std::mem::MaybeUninit::<RandomStream>::uninit();
                unsafe {
                    random_native::destack_random_stream(self.call_context, out.as_mut_ptr())?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }
}

/// Wrapper that carries one native or VM value for generated harness bindings.
pub(crate) enum HarnessValue<Native, Vm> {
    /// Native value variant.
    Native(Native),
    /// VM value variant.
    Vm(Vm),
}

impl<Native, Vm> HarnessValue<Native, Vm> {
    /// Convert one wrapper into one native value.
    fn into_native(self, label: &str) -> RuntimeResult<Native> {
        match self {
            HarnessValue::Native(value) => Ok(value),
            HarnessValue::Vm(_) => Err(RuntimeError::from(
                HarnessPlatformError::invalid_argument_value(
                    label,
                    "expected native harness value",
                ),
            )
            .boxed()),
        }
    }

    /// Convert one wrapper into one VM value.
    fn into_vm(self, label: &str) -> RuntimeResult<Vm> {
        match self {
            HarnessValue::Vm(value) => Ok(value),
            HarnessValue::Native(_) => Err(RuntimeError::from(
                HarnessPlatformError::invalid_argument_value(label, "expected VM harness value"),
            )
            .boxed()),
        }
    }
}
