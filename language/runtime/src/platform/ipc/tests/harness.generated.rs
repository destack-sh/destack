// generated by generate-bindings: test harness, do not edit

#![allow(dead_code)]
#![allow(unused_imports)]
#![allow(clippy::type_complexity)]

use crate::diagnostic::{RuntimeError, RuntimeResult};
use crate::platform::abi::{NativeSlice, NativeStringRef, NativeStringSlice};
use crate::platform::ipc::tests::IpcHarnessContext;
use crate::platform::ipc::{
    MessageQueueReceive, MessageQueueReceiveVm, PipePair, PipePairVm, SharedMemoryMapping,
    SharedMemoryMappingVm, UnixPeerCredentials, UnixPeerCredentialsVm, UnixReceiveAncillary,
    UnixReceiveAncillaryVm, native as ipc_native, vm as ipc_vm,
};
use crate::platform::{
    NativeAbiCodec, NativeArray, PlatformError as HarnessPlatformError, VmAbiCodec, VmArray,
    VmSlice, fs, resource,
};
use destack_vm as vm;

impl<'call> IpcHarnessContext<'call> {
    /// Return the generated VM context if available.
    #[allow(clippy::mut_from_ref)]
    fn generated_vm_context_mut(&self) -> Option<&mut vm::BindingContext<'_>> {
        self.vm_context
            .map(|context| unsafe { &mut *(context as *mut vm::BindingContext<'_>) })
    }

    /// Return one standardized value payload for native and VM variants.
    pub(crate) fn harness_value<Native, Vm>(&self, native: Native) -> HarnessValue<Native, Vm> {
        HarnessValue::Native(native)
    }

    /// Return one standardized value payload for VM and native variants.
    pub(crate) fn harness_value_vm<Native, Vm>(&self, vm: Vm) -> HarnessValue<Native, Vm> {
        HarnessValue::Vm(vm)
    }

    /// Encode one value into one native or VM harness payload.
    pub(crate) fn harness_value_from<Native, Vm>(
        &mut self,
        value: Native::Value,
    ) -> RuntimeResult<HarnessValue<Native, Vm>>
    where
        Native: NativeAbiCodec,
        Vm: VmAbiCodec<Value = Native::Value>,
    {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let context = &mut context.write();
                let value = Vm::from_value(context, value)?;
                Ok(HarnessValue::Vm(value))
            }
            None => {
                let value = Native::from_value(self.call_context, value);
                Ok(HarnessValue::Native(value))
            }
        }
    }

    /// Decode one native or VM harness payload into one value.
    pub(crate) fn harness_value_into<Native, Vm>(
        &mut self,
        value: HarnessValue<Native, Vm>,
    ) -> RuntimeResult<Native::Value>
    where
        Native: NativeAbiCodec,
        Vm: VmAbiCodec<Value = Native::Value>,
    {
        match value {
            HarnessValue::Native(value) => unsafe { Native::into_value(value) },
            HarnessValue::Vm(value) => {
                let context = self.generated_vm_context_mut().ok_or_else(|| {
                    RuntimeError::from(HarnessPlatformError::invalid_argument_value(
                        "context",
                        "expected vm context",
                    ))
                    .boxed()
                })?;
                let context = &context.read();
                value.into_value(context)
            }
        }
    }

    /// Call one generated platform test harness binding.
    pub(crate) fn destack_ipc_message_queue_close(
        &mut self,
        handle: resource::MessageQueueHandle,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                ipc_vm::destack_ipc_message_queue_close(self.call_context, context, handle)
            }
            None => unsafe {
                ipc_native::destack_ipc_message_queue_close(self.call_context, handle)
            },
        }
    }

    /// Call one generated platform test harness binding.
    pub(crate) fn destack_ipc_message_queue_open(
        &mut self,
        name: HarnessValue<NativeStringRef, vm::StringHandle>,
        flags: u32,
        mode: u32,
        maxmessages: u32,
        maxmessagebytes: u32,
    ) -> RuntimeResult<resource::MessageQueueHandle> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let name = name.into_vm("name")?;
                let out = ipc_vm::destack_ipc_message_queue_open(
                    self.call_context,
                    context,
                    name,
                    flags,
                    mode,
                    maxmessages,
                    maxmessagebytes,
                )?;
                Ok(out)
            }
            None => {
                let name = name.into_native("name")?;
                let mut out = std::mem::MaybeUninit::<resource::MessageQueueHandle>::uninit();
                unsafe {
                    ipc_native::destack_ipc_message_queue_open(
                        self.call_context,
                        out.as_mut_ptr(),
                        name,
                        flags,
                        mode,
                        maxmessages,
                        maxmessagebytes,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// Call one generated platform test harness binding.
    pub(crate) fn destack_ipc_message_queue_receive(
        &mut self,
        handle: resource::MessageQueueHandle,
        timeoutns: u64,
        buffer: HarnessValue<NativeSlice<u8>, VmSlice<u8>>,
    ) -> RuntimeResult<HarnessValue<MessageQueueReceive, MessageQueueReceiveVm>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let buffer = buffer.into_vm("buffer")?;
                let out = ipc_vm::destack_ipc_message_queue_receive(
                    self.call_context,
                    context,
                    handle,
                    timeoutns,
                    buffer,
                )?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let buffer = buffer.into_native("buffer")?;
                let mut out = std::mem::MaybeUninit::<MessageQueueReceive>::uninit();
                unsafe {
                    ipc_native::destack_ipc_message_queue_receive(
                        self.call_context,
                        out.as_mut_ptr(),
                        handle,
                        timeoutns,
                        buffer,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Call one generated platform test harness binding.
    pub(crate) fn destack_ipc_message_queue_send(
        &mut self,
        handle: resource::MessageQueueHandle,
        priority: u32,
        timeoutns: u64,
        argument_payload: HarnessValue<NativeSlice<u8>, VmSlice<u8>>,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let argument_payload = argument_payload.into_vm("argument_payload")?;
                ipc_vm::destack_ipc_message_queue_send(
                    self.call_context,
                    context,
                    handle,
                    priority,
                    timeoutns,
                    argument_payload,
                )
            }
            None => unsafe {
                let argument_payload = argument_payload.into_native("argument_payload")?;
                ipc_native::destack_ipc_message_queue_send(
                    self.call_context,
                    handle,
                    priority,
                    timeoutns,
                    argument_payload,
                )
            },
        }
    }

    /// Call one generated platform test harness binding.
    pub(crate) fn destack_ipc_message_queue_unlink(
        &mut self,
        name: HarnessValue<NativeStringRef, vm::StringHandle>,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let name = name.into_vm("name")?;
                ipc_vm::destack_ipc_message_queue_unlink(self.call_context, context, name)
            }
            None => unsafe {
                let name = name.into_native("name")?;
                ipc_native::destack_ipc_message_queue_unlink(self.call_context, name)
            },
        }
    }

    /// Call one generated platform test harness binding.
    pub(crate) fn destack_ipc_pipe_close(
        &mut self,
        handle: resource::PipeHandle,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => ipc_vm::destack_ipc_pipe_close(self.call_context, context, handle),
            None => unsafe { ipc_native::destack_ipc_pipe_close(self.call_context, handle) },
        }
    }

    /// Call one generated platform test harness binding.
    pub(crate) fn destack_ipc_pipe_open(
        &mut self,
        flags: u32,
    ) -> RuntimeResult<HarnessValue<PipePair, PipePairVm>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = ipc_vm::destack_ipc_pipe_open(self.call_context, context, flags)?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let mut out = std::mem::MaybeUninit::<PipePair>::uninit();
                unsafe {
                    ipc_native::destack_ipc_pipe_open(self.call_context, out.as_mut_ptr(), flags)?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Call one generated platform test harness binding.
    pub(crate) fn destack_ipc_pipe_read(
        &mut self,
        handle: resource::PipeHandle,
        buffer: HarnessValue<NativeSlice<u8>, VmSlice<u8>>,
    ) -> RuntimeResult<u64> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let buffer = buffer.into_vm("buffer")?;
                let out =
                    ipc_vm::destack_ipc_pipe_read(self.call_context, context, handle, buffer)?;
                Ok(out)
            }
            None => {
                let buffer = buffer.into_native("buffer")?;
                let mut out = std::mem::MaybeUninit::<u64>::uninit();
                unsafe {
                    ipc_native::destack_ipc_pipe_read(
                        self.call_context,
                        out.as_mut_ptr(),
                        handle,
                        buffer,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// Call one generated platform test harness binding.
    pub(crate) fn destack_ipc_pipe_write(
        &mut self,
        handle: resource::PipeHandle,
        buffer: HarnessValue<NativeSlice<u8>, VmSlice<u8>>,
    ) -> RuntimeResult<u64> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let buffer = buffer.into_vm("buffer")?;
                let out =
                    ipc_vm::destack_ipc_pipe_write(self.call_context, context, handle, buffer)?;
                Ok(out)
            }
            None => {
                let buffer = buffer.into_native("buffer")?;
                let mut out = std::mem::MaybeUninit::<u64>::uninit();
                unsafe {
                    ipc_native::destack_ipc_pipe_write(
                        self.call_context,
                        out.as_mut_ptr(),
                        handle,
                        buffer,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// Call one generated platform test harness binding.
    pub(crate) fn destack_ipc_shared_memory_close(
        &mut self,
        handle: resource::SharedMemoryHandle,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                ipc_vm::destack_ipc_shared_memory_close(self.call_context, context, handle)
            }
            None => unsafe {
                ipc_native::destack_ipc_shared_memory_close(self.call_context, handle)
            },
        }
    }

    /// Call one generated platform test harness binding.
    pub(crate) fn destack_ipc_shared_memory_create(
        &mut self,
        name: HarnessValue<NativeStringRef, vm::StringHandle>,
        size: u64,
        flags: u32,
    ) -> RuntimeResult<resource::SharedMemoryHandle> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let name = name.into_vm("name")?;
                let out = ipc_vm::destack_ipc_shared_memory_create(
                    self.call_context,
                    context,
                    name,
                    size,
                    flags,
                )?;
                Ok(out)
            }
            None => {
                let name = name.into_native("name")?;
                let mut out = std::mem::MaybeUninit::<resource::SharedMemoryHandle>::uninit();
                unsafe {
                    ipc_native::destack_ipc_shared_memory_create(
                        self.call_context,
                        out.as_mut_ptr(),
                        name,
                        size,
                        flags,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// Call one generated platform test harness binding.
    pub(crate) fn destack_ipc_shared_memory_map(
        &mut self,
        handle: resource::SharedMemoryHandle,
        offset: u64,
        length: u64,
        flags: u32,
    ) -> RuntimeResult<HarnessValue<SharedMemoryMapping, SharedMemoryMappingVm>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = ipc_vm::destack_ipc_shared_memory_map(
                    self.call_context,
                    context,
                    handle,
                    offset,
                    length,
                    flags,
                )?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let mut out = std::mem::MaybeUninit::<SharedMemoryMapping>::uninit();
                unsafe {
                    ipc_native::destack_ipc_shared_memory_map(
                        self.call_context,
                        out.as_mut_ptr(),
                        handle,
                        offset,
                        length,
                        flags,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Call one generated platform test harness binding.
    pub(crate) fn destack_ipc_shared_memory_open(
        &mut self,
        name: HarnessValue<NativeStringRef, vm::StringHandle>,
        flags: u32,
    ) -> RuntimeResult<resource::SharedMemoryHandle> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let name = name.into_vm("name")?;
                let out = ipc_vm::destack_ipc_shared_memory_open(
                    self.call_context,
                    context,
                    name,
                    flags,
                )?;
                Ok(out)
            }
            None => {
                let name = name.into_native("name")?;
                let mut out = std::mem::MaybeUninit::<resource::SharedMemoryHandle>::uninit();
                unsafe {
                    ipc_native::destack_ipc_shared_memory_open(
                        self.call_context,
                        out.as_mut_ptr(),
                        name,
                        flags,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// Call one generated platform test harness binding.
    pub(crate) fn destack_ipc_shared_memory_unmap(
        &mut self,
        address: u64,
        length: u64,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                ipc_vm::destack_ipc_shared_memory_unmap(self.call_context, context, address, length)
            }
            None => unsafe {
                ipc_native::destack_ipc_shared_memory_unmap(self.call_context, address, length)
            },
        }
    }

    /// Call one generated platform test harness binding.
    pub(crate) fn destack_ipc_futex_wait(
        &mut self,
        sharedmemory: resource::SharedMemoryHandle,
        offset: u64,
        expected: u32,
        timeoutns: u64,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => ipc_vm::destack_ipc_futex_wait(
                self.call_context,
                context,
                sharedmemory,
                offset,
                expected,
                timeoutns,
            ),
            None => unsafe {
                ipc_native::destack_ipc_futex_wait(
                    self.call_context,
                    sharedmemory,
                    offset,
                    expected,
                    timeoutns,
                )
            },
        }
    }

    /// Call one generated platform test harness binding.
    pub(crate) fn destack_ipc_futex_wake(
        &mut self,
        sharedmemory: resource::SharedMemoryHandle,
        offset: u64,
        count: u32,
    ) -> RuntimeResult<u32> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = ipc_vm::destack_ipc_futex_wake(
                    self.call_context,
                    context,
                    sharedmemory,
                    offset,
                    count,
                )?;
                Ok(out)
            }
            None => {
                let mut out = std::mem::MaybeUninit::<u32>::uninit();
                unsafe {
                    ipc_native::destack_ipc_futex_wake(
                        self.call_context,
                        out.as_mut_ptr(),
                        sharedmemory,
                        offset,
                        count,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// Call one generated platform test harness binding.
    pub(crate) fn destack_ipc_semaphore_create(
        &mut self,
        name: HarnessValue<NativeStringRef, vm::StringHandle>,
        initial: u32,
        flags: u32,
    ) -> RuntimeResult<resource::SemaphoreHandle> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let name = name.into_vm("name")?;
                let out = ipc_vm::destack_ipc_semaphore_create(
                    self.call_context,
                    context,
                    name,
                    initial,
                    flags,
                )?;
                Ok(out)
            }
            None => {
                let name = name.into_native("name")?;
                let mut out = std::mem::MaybeUninit::<resource::SemaphoreHandle>::uninit();
                unsafe {
                    ipc_native::destack_ipc_semaphore_create(
                        self.call_context,
                        out.as_mut_ptr(),
                        name,
                        initial,
                        flags,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// Call one generated platform test harness binding.
    pub(crate) fn destack_ipc_semaphore_post(
        &mut self,
        handle: resource::SemaphoreHandle,
        count: u32,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                ipc_vm::destack_ipc_semaphore_post(self.call_context, context, handle, count)
            }
            None => unsafe {
                ipc_native::destack_ipc_semaphore_post(self.call_context, handle, count)
            },
        }
    }

    /// Call one generated platform test harness binding.
    pub(crate) fn destack_ipc_semaphore_wait(
        &mut self,
        handle: resource::SemaphoreHandle,
        timeoutns: u64,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                ipc_vm::destack_ipc_semaphore_wait(self.call_context, context, handle, timeoutns)
            }
            None => unsafe {
                ipc_native::destack_ipc_semaphore_wait(self.call_context, handle, timeoutns)
            },
        }
    }

    /// Call one generated platform test harness binding.
    pub(crate) fn destack_ipc_unix_receive(
        &mut self,
        socket: resource::SocketHandle,
        maxhandles: u32,
    ) -> RuntimeResult<HarnessValue<UnixReceiveAncillary, UnixReceiveAncillaryVm>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = ipc_vm::destack_ipc_unix_receive(
                    self.call_context,
                    context,
                    socket,
                    maxhandles,
                )?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let mut out = std::mem::MaybeUninit::<UnixReceiveAncillary>::uninit();
                unsafe {
                    ipc_native::destack_ipc_unix_receive(
                        self.call_context,
                        out.as_mut_ptr(),
                        socket,
                        maxhandles,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Call one generated platform test harness binding.
    pub(crate) fn destack_ipc_unix_send(
        &mut self,
        socket: resource::SocketHandle,
        argument_payload: HarnessValue<NativeSlice<u8>, VmSlice<u8>>,
        handles: HarnessValue<
            NativeSlice<resource::TransferredHandle>,
            VmSlice<resource::TransferredHandle>,
        >,
    ) -> RuntimeResult<u64> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let argument_payload = argument_payload.into_vm("argument_payload")?;
                let handles = handles.into_vm("handles")?;
                let out = ipc_vm::destack_ipc_unix_send(
                    self.call_context,
                    context,
                    socket,
                    argument_payload,
                    handles,
                )?;
                Ok(out)
            }
            None => {
                let argument_payload = argument_payload.into_native("argument_payload")?;
                let handles = handles.into_native("handles")?;
                let mut out = std::mem::MaybeUninit::<u64>::uninit();
                unsafe {
                    ipc_native::destack_ipc_unix_send(
                        self.call_context,
                        out.as_mut_ptr(),
                        socket,
                        argument_payload,
                        handles,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }
}

/// Wrapper that carries one native or VM value for generated harness bindings.
pub(crate) enum HarnessValue<Native, Vm> {
    /// Native value variant.
    Native(Native),
    /// VM value variant.
    Vm(Vm),
}

impl<Native: Clone, Vm: Clone> Clone for HarnessValue<Native, Vm> {
    fn clone(&self) -> Self {
        match self {
            HarnessValue::Native(value) => HarnessValue::Native(value.clone()),
            HarnessValue::Vm(value) => HarnessValue::Vm(value.clone()),
        }
    }
}

impl<Native, Vm> HarnessValue<Native, Vm> {
    /// Convert one wrapper into one native value.
    fn into_native(self, label: &str) -> RuntimeResult<Native> {
        match self {
            HarnessValue::Native(value) => Ok(value),
            HarnessValue::Vm(_) => Err(RuntimeError::from(
                HarnessPlatformError::invalid_argument_value(
                    label,
                    "expected native harness value",
                ),
            )
            .boxed()),
        }
    }

    /// Convert one wrapper into one VM value.
    fn into_vm(self, label: &str) -> RuntimeResult<Vm> {
        match self {
            HarnessValue::Vm(value) => Ok(value),
            HarnessValue::Native(_) => Err(RuntimeError::from(
                HarnessPlatformError::invalid_argument_value(label, "expected VM harness value"),
            )
            .boxed()),
        }
    }
}
