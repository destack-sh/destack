// generated by generate-bindings: test harness, do not edit

#![allow(dead_code)]
#![allow(unused_imports)]
#![allow(clippy::type_complexity)]

use super::*;
use crate::diagnostic::{RuntimeError, RuntimeResult};
use crate::platform::security::{
    PlatformCapability, PlatformCapabilityVm, SecurityFilter, SecurityFilterKind, SecurityFilterVm,
    SecurityPolicyMode, SecurityPolicyRule, SecurityPolicyRuleVm, native as security_native,
    vm as security_vm,
};
use crate::platform::{
    NativeSlice, NativeStringRef, PlatformError as HarnessPlatformError, VmSlice, resource,
};
use destack_vm as vm;

impl<'call> SecurityHarnessContext<'call> {
    /// Return the generated VM context if available.
    #[allow(clippy::mut_from_ref)]
    fn generated_vm_context_mut(&self) -> Option<&mut vm::ExternalCallContext<'_>> {
        self.vm_context
            .map(|context| unsafe { &mut *(context as *mut vm::ExternalCallContext<'_>) })
    }

    /// Return one standardized value payload for native and VM variants.
    pub(crate) fn harness_value<Native, Vm>(&self, native: Native) -> HarnessValue<Native, Vm> {
        HarnessValue::Native(native)
    }

    /// Return one standardized value payload for VM and native variants.
    pub(crate) fn harness_value_vm<Native, Vm>(&self, vm: Vm) -> HarnessValue<Native, Vm> {
        HarnessValue::Vm(vm)
    }

    /// Check one capability.
    ///
    /// Evaluate whether one capability is active in the current runtime context.
    /// Capability checks use runtime policy rules and sandbox scope inheritance.
    ///
    /// # Platform
    /// Runtime-managed on all targets.
    /// Uses runtime security policy state.
    ///
    /// # Errors
    /// Returns invalidArgument, ioPermissionDenied, notSupported.
    ///
    /// # Security
    /// Requires `security.policy.read`.
    ///
    /// # Replay
    /// Deterministic.
    pub(crate) fn destack_security_capability_has(
        &mut self,
        capability: HarnessValue<PlatformCapability, PlatformCapabilityVm>,
    ) -> RuntimeResult<bool> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let capability = capability.into_vm("capability")?;
                let out = security_vm::destack_security_capability_has(
                    self.call_context,
                    context,
                    capability,
                )?;
                Ok(out)
            }
            None => {
                let capability = capability.into_native("capability")?;
                let mut out = std::mem::MaybeUninit::<bool>::uninit();
                unsafe {
                    security_native::destack_security_capability_has(
                        self.call_context,
                        out.as_mut_ptr(),
                        capability,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// List active capabilities.
    ///
    /// Return the currently active runtime capability set for the executing context.
    /// Capabilities are normalized to the canonical intrinsic capability string space.
    ///
    /// # Platform
    /// Runtime-managed on all targets.
    /// Uses runtime security policy state.
    ///
    /// # Errors
    /// Returns invalidArgument, ioPermissionDenied, notSupported.
    ///
    /// # Security
    /// Requires `security.policy.read`.
    ///
    /// # Replay
    /// Deterministic.
    pub(crate) fn destack_security_capability_list(
        &mut self,
    ) -> RuntimeResult<HarnessValue<NativeSlice<PlatformCapability>, VmSlice<PlatformCapabilityVm>>>
    {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out =
                    security_vm::destack_security_capability_list(self.call_context, context)?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let mut out = std::mem::MaybeUninit::<NativeSlice<PlatformCapability>>::uninit();
                unsafe {
                    security_native::destack_security_capability_list(
                        self.call_context,
                        out.as_mut_ptr(),
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Install one host filter for a sandbox scope.
    ///
    /// Install one host-enforced filter descriptor for a sandbox scope.
    /// Filter parsing and host mapping are selected by the filter kind.
    ///
    /// # Platform
    /// Hybrid across runtime and host enforcement hooks.
    /// Uses runtime-to-host policy adapters for seccomp, pledge, landlock, seatbelt, or token restrictions.
    ///
    /// # Errors
    /// Returns invalidArgument, ioNotFound, ioPermissionDenied, ioInvalidData, notSupported.
    ///
    /// # Security
    /// Requires `security.filter`.
    ///
    /// # Replay
    /// External, nonrecordable.
    pub(crate) fn destack_security_sandbox_install_filter(
        &mut self,
        handle: resource::SandboxHandle,
        filter: HarnessValue<SecurityFilter, SecurityFilterVm>,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let filter = filter.into_vm("filter")?;
                security_vm::destack_security_sandbox_install_filter(
                    self.call_context,
                    context,
                    handle,
                    filter,
                )
            }
            None => {
                let filter = filter.into_native("filter")?;
                unsafe {
                    security_native::destack_security_sandbox_install_filter(
                        self.call_context,
                        handle,
                        filter,
                    )
                }
            }
        }
    }

    /// Seal one sandbox policy.
    ///
    /// Transition one sandbox scope into sealed mode.
    /// Sealed policy rejects further capability broadening.
    ///
    /// # Platform
    /// Runtime-managed on all targets.
    /// Uses runtime sandbox policy controls.
    ///
    /// # Errors
    /// Returns invalidArgument, ioNotFound, ioPermissionDenied, notSupported.
    ///
    /// # Security
    /// Requires `security.restrict`.
    ///
    /// # Replay
    /// Deterministic.
    pub(crate) fn destack_security_sandbox_seal(
        &mut self,
        handle: resource::SandboxHandle,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                security_vm::destack_security_sandbox_seal(self.call_context, context, handle)
            }
            None => unsafe {
                security_native::destack_security_sandbox_seal(self.call_context, handle)
            },
        }
    }

    /// Apply an explicit capability set to one sandbox scope.
    ///
    /// Replace one sandbox capability set with explicit allow-list semantics.
    /// Capability inheritance and scope visibility follow runtime policy rules.
    ///
    /// # Platform
    /// Runtime-managed on all targets.
    /// Uses runtime sandbox policy controls.
    ///
    /// # Errors
    /// Returns invalidArgument, ioNotFound, ioPermissionDenied, notSupported.
    ///
    /// # Security
    /// Requires `security.restrict`.
    ///
    /// # Replay
    /// Deterministic.
    pub(crate) fn destack_security_sandbox_set_capabilities(
        &mut self,
        handle: resource::SandboxHandle,
        capabilities: HarnessValue<NativeSlice<PlatformCapability>, VmSlice<PlatformCapabilityVm>>,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let capabilities = capabilities.into_vm("capabilities")?;
                security_vm::destack_security_sandbox_set_capabilities(
                    self.call_context,
                    context,
                    handle,
                    capabilities,
                )
            }
            None => {
                let capabilities = capabilities.into_native("capabilities")?;
                unsafe {
                    security_native::destack_security_sandbox_set_capabilities(
                        self.call_context,
                        handle,
                        capabilities,
                    )
                }
            }
        }
    }

    /// Read policy capabilities for one named scope.
    ///
    /// Read one policy scope and return its effective capability set.
    /// Scope resolution and inheritance follow runtime security policy rules.
    ///
    /// # Platform
    /// Runtime-managed on all targets.
    /// Uses runtime security policy state.
    ///
    /// # Errors
    /// Returns invalidArgument, ioNotFound, ioPermissionDenied, notSupported.
    ///
    /// # Security
    /// Requires `security.policy.read`.
    ///
    /// # Replay
    /// Deterministic.
    pub(crate) fn destack_security_policy_get(
        &mut self,
        scope: HarnessValue<NativeStringRef, vm::StringHandle>,
    ) -> RuntimeResult<HarnessValue<NativeSlice<PlatformCapability>, VmSlice<PlatformCapabilityVm>>>
    {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let scope = scope.into_vm("scope")?;
                let out =
                    security_vm::destack_security_policy_get(self.call_context, context, scope)?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let scope = scope.into_native("scope")?;
                let mut out = std::mem::MaybeUninit::<NativeSlice<PlatformCapability>>::uninit();
                unsafe {
                    security_native::destack_security_policy_get(
                        self.call_context,
                        out.as_mut_ptr(),
                        scope,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Read structured policy rules for one named scope.
    ///
    /// Read one policy scope and return explicit capability rules with allow, deny, and audit decisions.
    /// Rule ordering and precedence follow runtime policy resolution semantics.
    ///
    /// # Platform
    /// Runtime-managed on all targets.
    /// Uses runtime security policy state.
    ///
    /// # Errors
    /// Returns invalidArgument, ioNotFound, ioPermissionDenied, notSupported.
    ///
    /// # Security
    /// Requires `security.policy.read`.
    ///
    /// # Replay
    /// Deterministic.
    pub(crate) fn destack_security_policy_get_rules(
        &mut self,
        scope: HarnessValue<NativeStringRef, vm::StringHandle>,
    ) -> RuntimeResult<HarnessValue<NativeSlice<SecurityPolicyRule>, VmSlice<SecurityPolicyRuleVm>>>
    {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let scope = scope.into_vm("scope")?;
                let out = security_vm::destack_security_policy_get_rules(
                    self.call_context,
                    context,
                    scope,
                )?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let scope = scope.into_native("scope")?;
                let mut out = std::mem::MaybeUninit::<NativeSlice<SecurityPolicyRule>>::uninit();
                unsafe {
                    security_native::destack_security_policy_get_rules(
                        self.call_context,
                        out.as_mut_ptr(),
                        scope,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Replace policy capabilities for one named scope.
    ///
    /// Replace one policy scope with an explicit capability set.
    /// Policy writes are validated against runtime security admin policy.
    ///
    /// # Platform
    /// Runtime-managed on all targets.
    /// Uses runtime security policy state.
    ///
    /// # Errors
    /// Returns invalidArgument, ioPermissionDenied, notSupported.
    ///
    /// # Security
    /// Requires `security.policy.write`.
    ///
    /// # Replay
    /// Deterministic.
    pub(crate) fn destack_security_policy_set(
        &mut self,
        scope: HarnessValue<NativeStringRef, vm::StringHandle>,
        capabilities: HarnessValue<NativeSlice<PlatformCapability>, VmSlice<PlatformCapabilityVm>>,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let scope = scope.into_vm("scope")?;
                let capabilities = capabilities.into_vm("capabilities")?;
                security_vm::destack_security_policy_set(
                    self.call_context,
                    context,
                    scope,
                    capabilities,
                )
            }
            None => {
                let scope = scope.into_native("scope")?;
                let capabilities = capabilities.into_native("capabilities")?;
                unsafe {
                    security_native::destack_security_policy_set(
                        self.call_context,
                        scope,
                        capabilities,
                    )
                }
            }
        }
    }

    /// Replace structured policy rules for one named scope.
    ///
    /// Replace one policy scope with explicit capability rules and decision modes.
    /// Rule conflicts and precedence are validated by runtime policy authoring rules.
    ///
    /// # Platform
    /// Runtime-managed on all targets.
    /// Uses runtime security policy state.
    ///
    /// # Errors
    /// Returns invalidArgument, ioPermissionDenied, notSupported.
    ///
    /// # Security
    /// Requires `security.policy.write`.
    ///
    /// # Replay
    /// Deterministic.
    pub(crate) fn destack_security_policy_set_rules(
        &mut self,
        scope: HarnessValue<NativeStringRef, vm::StringHandle>,
        rules: HarnessValue<NativeSlice<SecurityPolicyRule>, VmSlice<SecurityPolicyRuleVm>>,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let scope = scope.into_vm("scope")?;
                let rules = rules.into_vm("rules")?;
                security_vm::destack_security_policy_set_rules(
                    self.call_context,
                    context,
                    scope,
                    rules,
                )
            }
            None => {
                let scope = scope.into_native("scope")?;
                let rules = rules.into_native("rules")?;
                unsafe {
                    security_native::destack_security_policy_set_rules(
                        self.call_context,
                        scope,
                        rules,
                    )
                }
            }
        }
    }

    /// Enter a sandbox scope.
    ///
    /// Create and enter one new runtime sandbox scope.
    /// Scope inherits baseline policy then applies runtime sandbox restrictions.
    ///
    /// # Platform
    /// Runtime-managed on all targets.
    /// Uses runtime sandbox scope state.
    ///
    /// # Errors
    /// Returns invalidArgument, ioPermissionDenied, notSupported.
    ///
    /// # Security
    /// Requires `security.sandbox`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_security_sandbox_enter(
        &mut self,
        name: HarnessValue<NativeStringRef, vm::StringHandle>,
    ) -> RuntimeResult<resource::SandboxHandle> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let name = name.into_vm("name")?;
                let out =
                    security_vm::destack_security_sandbox_enter(self.call_context, context, name)?;
                Ok(out)
            }
            None => {
                let name = name.into_native("name")?;
                let mut out = std::mem::MaybeUninit::<resource::SandboxHandle>::uninit();
                unsafe {
                    security_native::destack_security_sandbox_enter(
                        self.call_context,
                        out.as_mut_ptr(),
                        name,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// Leave a sandbox scope.
    ///
    /// Exit one previously entered sandbox scope.
    /// Scope unwinding follows strict runtime stack discipline.
    ///
    /// # Platform
    /// Runtime-managed on all targets.
    /// Uses runtime sandbox scope state.
    ///
    /// # Errors
    /// Returns invalidArgument, ioNotFound, ioPermissionDenied, notSupported.
    ///
    /// # Security
    /// Requires `security.sandbox`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_security_sandbox_exit(
        &mut self,
        handle: resource::SandboxHandle,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                security_vm::destack_security_sandbox_exit(self.call_context, context, handle)
            }
            None => unsafe {
                security_native::destack_security_sandbox_exit(self.call_context, handle)
            },
        }
    }
}

/// Wrapper that carries one native or VM value for generated harness bindings.
pub(crate) enum HarnessValue<Native, Vm> {
    /// Native value variant.
    Native(Native),
    /// VM value variant.
    Vm(Vm),
}

impl<Native, Vm> HarnessValue<Native, Vm> {
    /// Convert one wrapper into one native value.
    fn into_native(self, label: &str) -> RuntimeResult<Native> {
        match self {
            HarnessValue::Native(value) => Ok(value),
            HarnessValue::Vm(_) => Err(RuntimeError::from(
                HarnessPlatformError::invalid_argument_value(
                    label,
                    "expected native harness value",
                ),
            )
            .boxed()),
        }
    }

    /// Convert one wrapper into one VM value.
    fn into_vm(self, label: &str) -> RuntimeResult<Vm> {
        match self {
            HarnessValue::Vm(value) => Ok(value),
            HarnessValue::Native(_) => Err(RuntimeError::from(
                HarnessPlatformError::invalid_argument_value(label, "expected VM harness value"),
            )
            .boxed()),
        }
    }
}
