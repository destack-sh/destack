// generated by generate-bindings: do not edit

use crate::diagnostic::RuntimeError;
use crate::platform::PlatformError;
use crate::platform::bindings::{
    BindingDescriptor, BindingRegistry, NativeBinding, NativeBindingSet, ReplayPolicy,
};
use crate::platform::process::{ProcessId, Signal};
use crate::runtime::with_runtime_call_context;
use crate::{binding, vm_binding_set};
use destack_vm as vm;
use destack_vm::Isolate;

use crate::platform::process::{native, vm as platform_vm};

/// Binding descriptor for destack.process.args.
pub const ARGS: BindingDescriptor = BindingDescriptor::external(
    "destack.process.args",
    "export function args(): Result<Slice<string>, PlatformError>",
    ReplayPolicy::Recordable,
    None,
);

/// Binding descriptor for destack.process.chdir.
pub const CHDIR: BindingDescriptor = BindingDescriptor::external(
    "destack.process.chdir",
    "export function chdir(path: string): Result<void, PlatformError>",
    ReplayPolicy::Recordable,
    None,
);

/// Binding descriptor for destack.process.cwd.
pub const CWD: BindingDescriptor = BindingDescriptor::external(
    "destack.process.cwd",
    "export function cwd(): Result<string, PlatformError>",
    ReplayPolicy::Recordable,
    None,
);

/// Binding descriptor for destack.process.envDelete.
pub const ENV_DELETE: BindingDescriptor = BindingDescriptor::external(
    "destack.process.envDelete",
    "export function envDelete(name: string): Result<void, PlatformError>",
    ReplayPolicy::Recordable,
    None,
);

/// Binding descriptor for destack.process.envGet.
pub const ENV_GET: BindingDescriptor = BindingDescriptor::external(
    "destack.process.envGet",
    "export function envGet(name: string): Result<string, PlatformError>",
    ReplayPolicy::Recordable,
    None,
);

/// Binding descriptor for destack.process.envSet.
pub const ENV_SET: BindingDescriptor = BindingDescriptor::external(
    "destack.process.envSet",
    "export function envSet(name: string, value: string): Result<void, PlatformError>",
    ReplayPolicy::Recordable,
    None,
);

/// Binding descriptor for destack.process.exit.
pub const EXIT: BindingDescriptor = BindingDescriptor::external(
    "destack.process.exit",
    "export function exit(code: uint32): Result<void, PlatformError>",
    ReplayPolicy::Recordable,
    None,
);

/// Binding descriptor for destack.process.gid.
pub const GID: BindingDescriptor = BindingDescriptor::external(
    "destack.process.gid",
    "export function gid(): Result<GroupId, PlatformError>",
    ReplayPolicy::Recordable,
    None,
);

/// Binding descriptor for destack.process.kill.
pub const KILL: BindingDescriptor = BindingDescriptor::external(
    "destack.process.kill",
    "export function kill(pid: ProcessId, signal: Signal): Result<void, PlatformError>",
    ReplayPolicy::Recordable,
    None,
);

/// Binding descriptor for destack.process.pid.
pub const PID: BindingDescriptor = BindingDescriptor::external(
    "destack.process.pid",
    "export function pid(): Result<ProcessId, PlatformError>",
    ReplayPolicy::Recordable,
    None,
);

/// Binding descriptor for destack.process.ppid.
pub const PPID: BindingDescriptor = BindingDescriptor::external(
    "destack.process.ppid",
    "export function ppid(): Result<ProcessId, PlatformError>",
    ReplayPolicy::Recordable,
    None,
);

/// Binding descriptor for destack.process.uid.
pub const UID: BindingDescriptor = BindingDescriptor::external(
    "destack.process.uid",
    "export function uid(): Result<UserId, PlatformError>",
    ReplayPolicy::Recordable,
    None,
);

/// Binding descriptor for destack.process.umask.
pub const UMASK: BindingDescriptor = BindingDescriptor::external(
    "destack.process.umask",
    "export function umask(mask: uint32): Result<uint32, PlatformError>",
    ReplayPolicy::Recordable,
    None,
);

/// Binding descriptors for process.
pub const BINDINGS: &[BindingDescriptor] = &[
    ARGS, CHDIR, CWD, ENV_DELETE, ENV_GET, ENV_SET, EXIT, GID, KILL, PID, PPID, UID, UMASK,
];

/// Native binding set for process.
pub const PROCESS_NATIVE_BINDINGS: NativeBindingSet = NativeBindingSet {
    name: "process",
    bindings: &[
        NativeBinding::new(
            ARGS,
            "destack.process.args",
            native::destack_process_args as *const (),
        ),
        NativeBinding::new(
            CHDIR,
            "destack.process.chdir",
            native::destack_process_chdir as *const (),
        ),
        NativeBinding::new(
            CWD,
            "destack.process.cwd",
            native::destack_process_cwd as *const (),
        ),
        NativeBinding::new(
            ENV_DELETE,
            "destack.process.envDelete",
            native::destack_process_env_delete as *const (),
        ),
        NativeBinding::new(
            ENV_GET,
            "destack.process.envGet",
            native::destack_process_env_get as *const (),
        ),
        NativeBinding::new(
            ENV_SET,
            "destack.process.envSet",
            native::destack_process_env_set as *const (),
        ),
        NativeBinding::new(
            EXIT,
            "destack.process.exit",
            native::destack_process_exit as *const (),
        ),
        NativeBinding::new(
            GID,
            "destack.process.gid",
            native::destack_process_gid as *const (),
        ),
        NativeBinding::new(
            KILL,
            "destack.process.kill",
            native::destack_process_kill as *const (),
        ),
        NativeBinding::new(
            PID,
            "destack.process.pid",
            native::destack_process_pid as *const (),
        ),
        NativeBinding::new(
            PPID,
            "destack.process.ppid",
            native::destack_process_ppid as *const (),
        ),
        NativeBinding::new(
            UID,
            "destack.process.uid",
            native::destack_process_uid as *const (),
        ),
        NativeBinding::new(
            UMASK,
            "destack.process.umask",
            native::destack_process_umask as *const (),
        ),
    ],
};

/// Register VM bindings for process.
pub fn register_process_vm_bindings(registry: &mut BindingRegistry, isolate: &mut Isolate) {
    {
        binding!(registry, isolate, ARGS, move |context, _args| {
            with_runtime_call_context(|runtime| {
                runtime.check_policy(ARGS)?;
                let result = platform_vm::destack_process_args(runtime, context);
                result.map(|value| value.to_value(context))
            })
            .map_err(Into::into)
        });
    }
    {
        binding!(registry, isolate, CHDIR, move |context, args| {
            with_runtime_call_context(|runtime| {
                runtime.check_policy(CHDIR)?;
                let path_value = *args.first().ok_or_else(|| {
                    RuntimeError::platform(PlatformError::invalid_argument_type("path", "string"))
                        .boxed()
                })?;
                if path_value.tag() != vm::ValueTag::String {
                    return Err(RuntimeError::platform(PlatformError::invalid_argument_type(
                        "path", "string",
                    ))
                    .boxed());
                }
                let path = vm::StringHandle::new(path_value);
                let result = platform_vm::destack_process_chdir(runtime, context, path);
                result.map(|_| vm::Value::VOID)
            })
            .map_err(Into::into)
        });
    }
    {
        binding!(registry, isolate, CWD, move |context, _args| {
            with_runtime_call_context(|runtime| {
                runtime.check_policy(CWD)?;
                let result = platform_vm::destack_process_cwd(runtime, context);
                result.map(|value| value.value())
            })
            .map_err(Into::into)
        });
    }
    {
        binding!(registry, isolate, ENV_DELETE, move |context, args| {
            with_runtime_call_context(|runtime| {
                runtime.check_policy(ENV_DELETE)?;
                let name_value = *args.first().ok_or_else(|| {
                    RuntimeError::platform(PlatformError::invalid_argument_type("name", "string"))
                        .boxed()
                })?;
                if name_value.tag() != vm::ValueTag::String {
                    return Err(RuntimeError::platform(PlatformError::invalid_argument_type(
                        "name", "string",
                    ))
                    .boxed());
                }
                let name = vm::StringHandle::new(name_value);
                let result = platform_vm::destack_process_env_delete(runtime, context, name);
                result.map(|_| vm::Value::VOID)
            })
            .map_err(Into::into)
        });
    }
    {
        binding!(registry, isolate, ENV_GET, move |context, args| {
            with_runtime_call_context(|runtime| {
                runtime.check_policy(ENV_GET)?;
                let name_value = *args.first().ok_or_else(|| {
                    RuntimeError::platform(PlatformError::invalid_argument_type("name", "string"))
                        .boxed()
                })?;
                if name_value.tag() != vm::ValueTag::String {
                    return Err(RuntimeError::platform(PlatformError::invalid_argument_type(
                        "name", "string",
                    ))
                    .boxed());
                }
                let name = vm::StringHandle::new(name_value);
                let result = platform_vm::destack_process_env_get(runtime, context, name);
                result.map(|value| value.value())
            })
            .map_err(Into::into)
        });
    }
    {
        binding!(registry, isolate, ENV_SET, move |context, args| {
            with_runtime_call_context(|runtime| {
                runtime.check_policy(ENV_SET)?;
                let name_value = *args.first().ok_or_else(|| {
                    RuntimeError::platform(PlatformError::invalid_argument_type("name", "string"))
                        .boxed()
                })?;
                if name_value.tag() != vm::ValueTag::String {
                    return Err(RuntimeError::platform(PlatformError::invalid_argument_type(
                        "name", "string",
                    ))
                    .boxed());
                }
                let name = vm::StringHandle::new(name_value);
                let value_value = *args.get(1).ok_or_else(|| {
                    RuntimeError::platform(PlatformError::invalid_argument_type("value", "string"))
                        .boxed()
                })?;
                if value_value.tag() != vm::ValueTag::String {
                    return Err(RuntimeError::platform(PlatformError::invalid_argument_type(
                        "value", "string",
                    ))
                    .boxed());
                }
                let value = vm::StringHandle::new(value_value);
                let result = platform_vm::destack_process_env_set(runtime, context, name, value);
                result.map(|_| vm::Value::VOID)
            })
            .map_err(Into::into)
        });
    }
    {
        binding!(registry, isolate, EXIT, move |context, args| {
            with_runtime_call_context(|runtime| {
                runtime.check_policy(EXIT)?;
                let code_value = *args.first().ok_or_else(|| {
                    RuntimeError::platform(PlatformError::invalid_argument_type("code", "uint32"))
                        .boxed()
                })?;
                let (code, width) = code_value.as_uint_with_width().ok_or_else(|| {
                    RuntimeError::platform(PlatformError::invalid_argument_type("code", "uint32"))
                        .boxed()
                })?;
                if width != 32 {
                    return Err(RuntimeError::platform(PlatformError::invalid_argument_type(
                        "code", "uint32",
                    ))
                    .boxed());
                }
                let code = code as u32;
                let result = platform_vm::destack_process_exit(runtime, context, code);
                result.map(|_| vm::Value::VOID)
            })
            .map_err(Into::into)
        });
    }
    {
        binding!(registry, isolate, GID, move |context, _args| {
            with_runtime_call_context(|runtime| {
                runtime.check_policy(GID)?;
                let result = platform_vm::destack_process_gid(runtime, context);
                result.map(|value| vm::Value::uint(value.0 as u64, 32))
            })
            .map_err(Into::into)
        });
    }
    {
        binding!(registry, isolate, KILL, move |context, args| {
            with_runtime_call_context(|runtime| {
                runtime.check_policy(KILL)?;
                let pid_value = *args.first().ok_or_else(|| {
                    RuntimeError::platform(PlatformError::invalid_argument_type("pid", "ProcessId"))
                        .boxed()
                })?;
                let (pid_inner, width) = pid_value.as_uint_with_width().ok_or_else(|| {
                    RuntimeError::platform(PlatformError::invalid_argument_type(
                        "pid_inner",
                        "ProcessId",
                    ))
                    .boxed()
                })?;
                if width != 32 {
                    return Err(RuntimeError::platform(PlatformError::invalid_argument_type(
                        "pid_inner",
                        "ProcessId",
                    ))
                    .boxed());
                }
                let pid_inner = pid_inner as u32;
                let pid = ProcessId(pid_inner);
                let signal_value = *args.get(1).ok_or_else(|| {
                    RuntimeError::platform(PlatformError::invalid_argument_type("signal", "Signal"))
                        .boxed()
                })?;
                let (signal_inner, width) = signal_value.as_uint_with_width().ok_or_else(|| {
                    RuntimeError::platform(PlatformError::invalid_argument_type(
                        "signal_inner",
                        "Signal",
                    ))
                    .boxed()
                })?;
                if width != 32 {
                    return Err(RuntimeError::platform(PlatformError::invalid_argument_type(
                        "signal_inner",
                        "Signal",
                    ))
                    .boxed());
                }
                let signal_inner = signal_inner as u32;
                let signal = Signal(signal_inner);
                let result = platform_vm::destack_process_kill(runtime, context, pid, signal);
                result.map(|_| vm::Value::VOID)
            })
            .map_err(Into::into)
        });
    }
    {
        binding!(registry, isolate, PID, move |context, _args| {
            with_runtime_call_context(|runtime| {
                runtime.check_policy(PID)?;
                let result = platform_vm::destack_process_pid(runtime, context);
                result.map(|value| vm::Value::uint(value.0 as u64, 32))
            })
            .map_err(Into::into)
        });
    }
    {
        binding!(registry, isolate, PPID, move |context, _args| {
            with_runtime_call_context(|runtime| {
                runtime.check_policy(PPID)?;
                let result = platform_vm::destack_process_ppid(runtime, context);
                result.map(|value| vm::Value::uint(value.0 as u64, 32))
            })
            .map_err(Into::into)
        });
    }
    {
        binding!(registry, isolate, UID, move |context, _args| {
            with_runtime_call_context(|runtime| {
                runtime.check_policy(UID)?;
                let result = platform_vm::destack_process_uid(runtime, context);
                result.map(|value| vm::Value::uint(value.0 as u64, 32))
            })
            .map_err(Into::into)
        });
    }
    {
        binding!(registry, isolate, UMASK, move |context, args| {
            with_runtime_call_context(|runtime| {
                runtime.check_policy(UMASK)?;
                let mask_value = *args.first().ok_or_else(|| {
                    RuntimeError::platform(PlatformError::invalid_argument_type("mask", "uint32"))
                        .boxed()
                })?;
                let (mask, width) = mask_value.as_uint_with_width().ok_or_else(|| {
                    RuntimeError::platform(PlatformError::invalid_argument_type("mask", "uint32"))
                        .boxed()
                })?;
                if width != 32 {
                    return Err(RuntimeError::platform(PlatformError::invalid_argument_type(
                        "mask", "uint32",
                    ))
                    .boxed());
                }
                let mask = mask as u32;
                let result = platform_vm::destack_process_umask(runtime, context, mask);
                result.map(|value| vm::Value::uint(value as u64, 32))
            })
            .map_err(Into::into)
        });
    }
}

/// Install VM bindings for process.
pub fn install_process_vm_bindings(registry: &mut BindingRegistry, isolate: &mut Isolate) {
    register_process_vm_bindings(registry, isolate);
}

vm_binding_set!(pub PROCESS_VM_BINDINGS, "process", install_process_vm_bindings);
