// generated by generate-bindings: test harness, do not edit

#![allow(dead_code)]
#![allow(unused_imports)]
#![allow(clippy::type_complexity)]

use crate::diagnostic::{RuntimeError, RuntimeResult};
use crate::platform::abi::{NativeSlice, NativeStringRef, NativeStringSlice};
use crate::platform::process::tests::ProcessHarnessContext;
use crate::platform::process::{
    ExecAtFlags, GroupId, ProcessFdAction, ProcessFdActionClose, ProcessFdActionCloseVm,
    ProcessFdActionDup2, ProcessFdActionDup2Vm, ProcessFdActionOpen, ProcessFdActionOpenVm,
    ProcessFdActionVm, ProcessFdFlags, ProcessFdSignalFlags, ProcessGroupIds, ProcessGroupIdsVm,
    ProcessId, ProcessLimit, ProcessLimitResource, ProcessLimitVm, ProcessNamespaceKind,
    ProcessSchedulerConfig, ProcessSchedulerConfigVm, ProcessSchedulerPolicy, ProcessSpawnOptions,
    ProcessSpawnOptionsVm, ProcessStdio, ProcessStdioDescriptor, ProcessStdioDescriptorVm,
    ProcessStdioFile, ProcessStdioFileVm, ProcessStdioInherit, ProcessStdioInheritVm,
    ProcessStdioNull, ProcessStdioNullVm, ProcessStdioPipe, ProcessStdioPipeVm, ProcessStdioVm,
    ProcessUnshareFlags, ProcessUserIds, ProcessUserIdsVm, ProcessWaitContinuedStatus,
    ProcessWaitContinuedStatusVm, ProcessWaitExitedStatus, ProcessWaitExitedStatusVm,
    ProcessWaitFlags, ProcessWaitRunningStatus, ProcessWaitRunningStatusVm,
    ProcessWaitSignaledStatus, ProcessWaitSignaledStatusVm, ProcessWaitStatus, ProcessWaitStatusVm,
    ProcessWaitStoppedStatus, ProcessWaitStoppedStatusVm, Signal, SignalEvent, SignalEventVm,
    SignalFdFlags, SignalMaskHow, SyscallFilterFlags, UserId, native as process_native,
    vm as process_vm,
};
use crate::platform::{
    NativeAbiCodec, NativeArray, PlatformError as HarnessPlatformError, VmAbiCodec, VmArray,
    VmSlice, fs, resource, thread,
};
use destack_vm as vm;

impl<'call> ProcessHarnessContext<'call> {
    /// Return the generated VM context if available.
    #[allow(clippy::mut_from_ref)]
    fn generated_vm_context_mut(&self) -> Option<&mut vm::ExternalCallContext<'_>> {
        self.vm_context
            .map(|context| unsafe { &mut *(context as *mut vm::ExternalCallContext<'_>) })
    }

    /// Return one standardized value payload for native and VM variants.
    pub(crate) fn harness_value<Native, Vm>(&self, native: Native) -> HarnessValue<Native, Vm> {
        HarnessValue::Native(native)
    }

    /// Return one standardized value payload for VM and native variants.
    pub(crate) fn harness_value_vm<Native, Vm>(&self, vm: Vm) -> HarnessValue<Native, Vm> {
        HarnessValue::Vm(vm)
    }

    /// Encode one value into one native or VM harness payload.
    pub(crate) fn harness_value_from<Native, Vm>(
        &mut self,
        value: Native::Value,
    ) -> RuntimeResult<HarnessValue<Native, Vm>>
    where
        Native: NativeAbiCodec,
        Vm: VmAbiCodec<Value = Native::Value>,
    {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let context = &mut context.write();
                let value = Vm::from_value(context, value)?;
                Ok(HarnessValue::Vm(value))
            }
            None => {
                let value = Native::from_value(self.call_context, value);
                Ok(HarnessValue::Native(value))
            }
        }
    }

    /// Decode one native or VM harness payload into one value.
    pub(crate) fn harness_value_into<Native, Vm>(
        &mut self,
        value: HarnessValue<Native, Vm>,
    ) -> RuntimeResult<Native::Value>
    where
        Native: NativeAbiCodec,
        Vm: VmAbiCodec<Value = Native::Value>,
    {
        match value {
            HarnessValue::Native(value) => unsafe { Native::into_value(value) },
            HarnessValue::Vm(value) => {
                let context = self.generated_vm_context_mut().ok_or_else(|| {
                    RuntimeError::from(HarnessPlatformError::invalid_argument_value(
                        "context",
                        "expected vm context",
                    ))
                    .boxed()
                })?;
                let context = &context.read();
                value.into_value(context)
            }
        }
    }

    /// Call one generated platform test harness binding.
    pub(crate) fn destack_process_args(
        &mut self,
    ) -> RuntimeResult<HarnessValue<NativeStringSlice, VmSlice<vm::StringHandle>>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = process_vm::destack_process_args(self.call_context, context)?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let mut out = std::mem::MaybeUninit::<NativeStringSlice>::uninit();
                unsafe {
                    process_native::destack_process_args(self.call_context, out.as_mut_ptr())?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Call one generated platform test harness binding.
    pub(crate) fn destack_process_chdir(
        &mut self,
        path: HarnessValue<fs::OsPath, fs::OsPathVm>,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let path = path.into_vm("path")?;
                process_vm::destack_process_chdir(self.call_context, context, path)
            }
            None => unsafe {
                let path = path.into_native("path")?;
                process_native::destack_process_chdir(self.call_context, path)
            },
        }
    }

    /// Call one generated platform test harness binding.
    pub(crate) fn destack_process_cwd(
        &mut self,
    ) -> RuntimeResult<HarnessValue<fs::OsPath, fs::OsPathVm>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = process_vm::destack_process_cwd(self.call_context, context)?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let mut out = std::mem::MaybeUninit::<fs::OsPath>::uninit();
                unsafe {
                    process_native::destack_process_cwd(self.call_context, out.as_mut_ptr())?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Call one generated platform test harness binding.
    pub(crate) fn destack_process_env_delete(
        &mut self,
        name: HarnessValue<NativeStringRef, vm::StringHandle>,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let name = name.into_vm("name")?;
                process_vm::destack_process_env_delete(self.call_context, context, name)
            }
            None => unsafe {
                let name = name.into_native("name")?;
                process_native::destack_process_env_delete(self.call_context, name)
            },
        }
    }

    /// Call one generated platform test harness binding.
    pub(crate) fn destack_process_env_delete_bytes(
        &mut self,
        name: HarnessValue<NativeSlice<u8>, VmSlice<u8>>,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let name = name.into_vm("name")?;
                process_vm::destack_process_env_delete_bytes(self.call_context, context, name)
            }
            None => unsafe {
                let name = name.into_native("name")?;
                process_native::destack_process_env_delete_bytes(self.call_context, name)
            },
        }
    }

    /// Call one generated platform test harness binding.
    pub(crate) fn destack_process_env_get(
        &mut self,
        name: HarnessValue<NativeStringRef, vm::StringHandle>,
    ) -> RuntimeResult<HarnessValue<NativeStringRef, vm::StringHandle>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let name = name.into_vm("name")?;
                let out = process_vm::destack_process_env_get(self.call_context, context, name)?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let name = name.into_native("name")?;
                let mut out = std::mem::MaybeUninit::<NativeStringRef>::uninit();
                unsafe {
                    process_native::destack_process_env_get(
                        self.call_context,
                        out.as_mut_ptr(),
                        name,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Call one generated platform test harness binding.
    pub(crate) fn destack_process_env_get_bytes(
        &mut self,
        name: HarnessValue<NativeSlice<u8>, VmSlice<u8>>,
    ) -> RuntimeResult<HarnessValue<NativeArray<u8>, VmArray<u8>>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let name = name.into_vm("name")?;
                let out =
                    process_vm::destack_process_env_get_bytes(self.call_context, context, name)?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let name = name.into_native("name")?;
                let mut out = std::mem::MaybeUninit::<NativeArray<u8>>::uninit();
                unsafe {
                    process_native::destack_process_env_get_bytes(
                        self.call_context,
                        out.as_mut_ptr(),
                        name,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Call one generated platform test harness binding.
    pub(crate) fn destack_process_env_set(
        &mut self,
        name: HarnessValue<NativeStringRef, vm::StringHandle>,
        argument_value: HarnessValue<NativeStringRef, vm::StringHandle>,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let name = name.into_vm("name")?;
                let argument_value = argument_value.into_vm("argument_value")?;
                process_vm::destack_process_env_set(
                    self.call_context,
                    context,
                    name,
                    argument_value,
                )
            }
            None => unsafe {
                let name = name.into_native("name")?;
                let argument_value = argument_value.into_native("argument_value")?;
                process_native::destack_process_env_set(self.call_context, name, argument_value)
            },
        }
    }

    /// Call one generated platform test harness binding.
    pub(crate) fn destack_process_env_set_bytes(
        &mut self,
        name: HarnessValue<NativeSlice<u8>, VmSlice<u8>>,
        argument_value: HarnessValue<NativeSlice<u8>, VmSlice<u8>>,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let name = name.into_vm("name")?;
                let argument_value = argument_value.into_vm("argument_value")?;
                process_vm::destack_process_env_set_bytes(
                    self.call_context,
                    context,
                    name,
                    argument_value,
                )
            }
            None => unsafe {
                let name = name.into_native("name")?;
                let argument_value = argument_value.into_native("argument_value")?;
                process_native::destack_process_env_set_bytes(
                    self.call_context,
                    name,
                    argument_value,
                )
            },
        }
    }

    /// Call one generated platform test harness binding.
    pub(crate) fn destack_process_fexec(
        &mut self,
        executable: resource::FileHandle,
        arguments: HarnessValue<NativeStringSlice, VmSlice<vm::StringHandle>>,
        environment: HarnessValue<NativeStringSlice, VmSlice<vm::StringHandle>>,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let arguments = arguments.into_vm("arguments")?;
                let environment = environment.into_vm("environment")?;
                process_vm::destack_process_fexec(
                    self.call_context,
                    context,
                    executable,
                    arguments,
                    environment,
                )
            }
            None => unsafe {
                let arguments = arguments.into_native("arguments")?;
                let environment = environment.into_native("environment")?;
                process_native::destack_process_fexec(
                    self.call_context,
                    executable,
                    arguments,
                    environment,
                )
            },
        }
    }

    /// Call one generated platform test harness binding.
    pub(crate) fn destack_process_exec(
        &mut self,
        command: HarnessValue<fs::OsPath, fs::OsPathVm>,
        arguments: HarnessValue<NativeStringSlice, VmSlice<vm::StringHandle>>,
        environment: HarnessValue<NativeStringSlice, VmSlice<vm::StringHandle>>,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let command = command.into_vm("command")?;
                let arguments = arguments.into_vm("arguments")?;
                let environment = environment.into_vm("environment")?;
                process_vm::destack_process_exec(
                    self.call_context,
                    context,
                    command,
                    arguments,
                    environment,
                )
            }
            None => unsafe {
                let command = command.into_native("command")?;
                let arguments = arguments.into_native("arguments")?;
                let environment = environment.into_native("environment")?;
                process_native::destack_process_exec(
                    self.call_context,
                    command,
                    arguments,
                    environment,
                )
            },
        }
    }

    /// Call one generated platform test harness binding.
    pub(crate) fn destack_process_execat(
        &mut self,
        directory: resource::DirectoryHandle,
        path: HarnessValue<fs::OsPath, fs::OsPathVm>,
        arguments: HarnessValue<NativeStringSlice, VmSlice<vm::StringHandle>>,
        environment: HarnessValue<NativeStringSlice, VmSlice<vm::StringHandle>>,
        flags: ExecAtFlags,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let path = path.into_vm("path")?;
                let arguments = arguments.into_vm("arguments")?;
                let environment = environment.into_vm("environment")?;
                process_vm::destack_process_execat(
                    self.call_context,
                    context,
                    directory,
                    path,
                    arguments,
                    environment,
                    flags,
                )
            }
            None => unsafe {
                let path = path.into_native("path")?;
                let arguments = arguments.into_native("arguments")?;
                let environment = environment.into_native("environment")?;
                process_native::destack_process_execat(
                    self.call_context,
                    directory,
                    path,
                    arguments,
                    environment,
                    flags,
                )
            },
        }
    }

    /// Call one generated platform test harness binding.
    pub(crate) fn destack_process_exit(&mut self, code: u32) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => process_vm::destack_process_exit(self.call_context, context, code),
            None => unsafe { process_native::destack_process_exit(self.call_context, code) },
        }
    }

    /// Call one generated platform test harness binding.
    pub(crate) fn destack_process_process_fd_close(
        &mut self,
        handle: resource::ProcessFdHandle,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                process_vm::destack_process_process_fd_close(self.call_context, context, handle)
            }
            None => unsafe {
                process_native::destack_process_process_fd_close(self.call_context, handle)
            },
        }
    }

    /// Call one generated platform test harness binding.
    pub(crate) fn destack_process_process_fd_open(
        &mut self,
        pid: ProcessId,
        flags: ProcessFdFlags,
    ) -> RuntimeResult<resource::ProcessFdHandle> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = process_vm::destack_process_process_fd_open(
                    self.call_context,
                    context,
                    pid,
                    flags,
                )?;
                Ok(out)
            }
            None => {
                let mut out = std::mem::MaybeUninit::<resource::ProcessFdHandle>::uninit();
                unsafe {
                    process_native::destack_process_process_fd_open(
                        self.call_context,
                        out.as_mut_ptr(),
                        pid,
                        flags,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// Call one generated platform test harness binding.
    pub(crate) fn destack_process_process_fd_send_signal(
        &mut self,
        handle: resource::ProcessFdHandle,
        signal: Signal,
        flags: ProcessFdSignalFlags,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => process_vm::destack_process_process_fd_send_signal(
                self.call_context,
                context,
                handle,
                signal,
                flags,
            ),
            None => unsafe {
                process_native::destack_process_process_fd_send_signal(
                    self.call_context,
                    handle,
                    signal,
                    flags,
                )
            },
        }
    }

    /// Call one generated platform test harness binding.
    pub(crate) fn destack_process_process_fd_try_wait(
        &mut self,
        handle: resource::ProcessFdHandle,
    ) -> RuntimeResult<HarnessValue<ProcessWaitStatus, ProcessWaitStatusVm>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = process_vm::destack_process_process_fd_try_wait(
                    self.call_context,
                    context,
                    handle,
                )?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let mut out = std::mem::MaybeUninit::<ProcessWaitStatus>::uninit();
                unsafe {
                    process_native::destack_process_process_fd_try_wait(
                        self.call_context,
                        out.as_mut_ptr(),
                        handle,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Call one generated platform test harness binding.
    pub(crate) fn destack_process_process_fd_wait(
        &mut self,
        handle: resource::ProcessFdHandle,
        timeoutns: u64,
    ) -> RuntimeResult<HarnessValue<ProcessWaitStatus, ProcessWaitStatusVm>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = process_vm::destack_process_process_fd_wait(
                    self.call_context,
                    context,
                    handle,
                    timeoutns,
                )?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let mut out = std::mem::MaybeUninit::<ProcessWaitStatus>::uninit();
                unsafe {
                    process_native::destack_process_process_fd_wait(
                        self.call_context,
                        out.as_mut_ptr(),
                        handle,
                        timeoutns,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Call one generated platform test harness binding.
    pub(crate) fn destack_process_signal_fd_close(
        &mut self,
        handle: resource::SignalFdHandle,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                process_vm::destack_process_signal_fd_close(self.call_context, context, handle)
            }
            None => unsafe {
                process_native::destack_process_signal_fd_close(self.call_context, handle)
            },
        }
    }

    /// Call one generated platform test harness binding.
    pub(crate) fn destack_process_signal_fd_open(
        &mut self,
        signals: HarnessValue<NativeSlice<Signal>, VmSlice<Signal>>,
        flags: SignalFdFlags,
    ) -> RuntimeResult<resource::SignalFdHandle> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let signals = signals.into_vm("signals")?;
                let out = process_vm::destack_process_signal_fd_open(
                    self.call_context,
                    context,
                    signals,
                    flags,
                )?;
                Ok(out)
            }
            None => {
                let signals = signals.into_native("signals")?;
                let mut out = std::mem::MaybeUninit::<resource::SignalFdHandle>::uninit();
                unsafe {
                    process_native::destack_process_signal_fd_open(
                        self.call_context,
                        out.as_mut_ptr(),
                        signals,
                        flags,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// Call one generated platform test harness binding.
    pub(crate) fn destack_process_signal_fd_read(
        &mut self,
        handle: resource::SignalFdHandle,
    ) -> RuntimeResult<HarnessValue<SignalEvent, SignalEventVm>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out =
                    process_vm::destack_process_signal_fd_read(self.call_context, context, handle)?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let mut out = std::mem::MaybeUninit::<SignalEvent>::uninit();
                unsafe {
                    process_native::destack_process_signal_fd_read(
                        self.call_context,
                        out.as_mut_ptr(),
                        handle,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Call one generated platform test harness binding.
    pub(crate) fn destack_process_signal_fd_set_mask(
        &mut self,
        handle: resource::SignalFdHandle,
        signals: HarnessValue<NativeSlice<Signal>, VmSlice<Signal>>,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let signals = signals.into_vm("signals")?;
                process_vm::destack_process_signal_fd_set_mask(
                    self.call_context,
                    context,
                    handle,
                    signals,
                )
            }
            None => unsafe {
                let signals = signals.into_native("signals")?;
                process_native::destack_process_signal_fd_set_mask(
                    self.call_context,
                    handle,
                    signals,
                )
            },
        }
    }

    /// Call one generated platform test harness binding.
    pub(crate) fn destack_process_signal_fd_try_read(
        &mut self,
        handle: resource::SignalFdHandle,
    ) -> RuntimeResult<HarnessValue<SignalEvent, SignalEventVm>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = process_vm::destack_process_signal_fd_try_read(
                    self.call_context,
                    context,
                    handle,
                )?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let mut out = std::mem::MaybeUninit::<SignalEvent>::uninit();
                unsafe {
                    process_native::destack_process_signal_fd_try_read(
                        self.call_context,
                        out.as_mut_ptr(),
                        handle,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Call one generated platform test harness binding.
    pub(crate) fn destack_process_stdio_stderr(&mut self) -> RuntimeResult<resource::FileHandle> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = process_vm::destack_process_stdio_stderr(self.call_context, context)?;
                Ok(out)
            }
            None => {
                let mut out = std::mem::MaybeUninit::<resource::FileHandle>::uninit();
                unsafe {
                    process_native::destack_process_stdio_stderr(
                        self.call_context,
                        out.as_mut_ptr(),
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// Call one generated platform test harness binding.
    pub(crate) fn destack_process_stdio_stdin(&mut self) -> RuntimeResult<resource::FileHandle> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = process_vm::destack_process_stdio_stdin(self.call_context, context)?;
                Ok(out)
            }
            None => {
                let mut out = std::mem::MaybeUninit::<resource::FileHandle>::uninit();
                unsafe {
                    process_native::destack_process_stdio_stdin(
                        self.call_context,
                        out.as_mut_ptr(),
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// Call one generated platform test harness binding.
    pub(crate) fn destack_process_stdio_stdout(&mut self) -> RuntimeResult<resource::FileHandle> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = process_vm::destack_process_stdio_stdout(self.call_context, context)?;
                Ok(out)
            }
            None => {
                let mut out = std::mem::MaybeUninit::<resource::FileHandle>::uninit();
                unsafe {
                    process_native::destack_process_stdio_stdout(
                        self.call_context,
                        out.as_mut_ptr(),
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// Call one generated platform test harness binding.
    pub(crate) fn destack_process_cgroup_get_limit(
        &mut self,
        path: HarnessValue<NativeStringRef, vm::StringHandle>,
        resource: ProcessLimitResource,
    ) -> RuntimeResult<HarnessValue<ProcessLimit, ProcessLimitVm>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let path = path.into_vm("path")?;
                let out = process_vm::destack_process_cgroup_get_limit(
                    self.call_context,
                    context,
                    path,
                    resource,
                )?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let path = path.into_native("path")?;
                let mut out = std::mem::MaybeUninit::<ProcessLimit>::uninit();
                unsafe {
                    process_native::destack_process_cgroup_get_limit(
                        self.call_context,
                        out.as_mut_ptr(),
                        path,
                        resource,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Call one generated platform test harness binding.
    pub(crate) fn destack_process_cgroup_join(
        &mut self,
        path: HarnessValue<NativeStringRef, vm::StringHandle>,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let path = path.into_vm("path")?;
                process_vm::destack_process_cgroup_join(self.call_context, context, path)
            }
            None => unsafe {
                let path = path.into_native("path")?;
                process_native::destack_process_cgroup_join(self.call_context, path)
            },
        }
    }

    /// Call one generated platform test harness binding.
    pub(crate) fn destack_process_cgroup_set_limit(
        &mut self,
        path: HarnessValue<NativeStringRef, vm::StringHandle>,
        resource: ProcessLimitResource,
        limit: HarnessValue<ProcessLimit, ProcessLimitVm>,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let path = path.into_vm("path")?;
                let limit = limit.into_vm("limit")?;
                process_vm::destack_process_cgroup_set_limit(
                    self.call_context,
                    context,
                    path,
                    resource,
                    limit,
                )
            }
            None => unsafe {
                let path = path.into_native("path")?;
                let limit = limit.into_native("limit")?;
                process_native::destack_process_cgroup_set_limit(
                    self.call_context,
                    path,
                    resource,
                    limit,
                )
            },
        }
    }

    /// Call one generated platform test harness binding.
    pub(crate) fn destack_process_job_assign(
        &mut self,
        name: HarnessValue<NativeStringRef, vm::StringHandle>,
        pids: HarnessValue<NativeSlice<ProcessId>, VmSlice<ProcessId>>,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let name = name.into_vm("name")?;
                let pids = pids.into_vm("pids")?;
                process_vm::destack_process_job_assign(self.call_context, context, name, pids)
            }
            None => unsafe {
                let name = name.into_native("name")?;
                let pids = pids.into_native("pids")?;
                process_native::destack_process_job_assign(self.call_context, name, pids)
            },
        }
    }

    /// Call one generated platform test harness binding.
    pub(crate) fn destack_process_job_set_limit(
        &mut self,
        name: HarnessValue<NativeStringRef, vm::StringHandle>,
        resource: ProcessLimitResource,
        limit: HarnessValue<ProcessLimit, ProcessLimitVm>,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let name = name.into_vm("name")?;
                let limit = limit.into_vm("limit")?;
                process_vm::destack_process_job_set_limit(
                    self.call_context,
                    context,
                    name,
                    resource,
                    limit,
                )
            }
            None => unsafe {
                let name = name.into_native("name")?;
                let limit = limit.into_native("limit")?;
                process_native::destack_process_job_set_limit(
                    self.call_context,
                    name,
                    resource,
                    limit,
                )
            },
        }
    }

    /// Call one generated platform test harness binding.
    pub(crate) fn destack_process_egid(&mut self) -> RuntimeResult<GroupId> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = process_vm::destack_process_egid(self.call_context, context)?;
                Ok(out)
            }
            None => {
                let mut out = std::mem::MaybeUninit::<GroupId>::uninit();
                unsafe {
                    process_native::destack_process_egid(self.call_context, out.as_mut_ptr())?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// Call one generated platform test harness binding.
    pub(crate) fn destack_process_euid(&mut self) -> RuntimeResult<UserId> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = process_vm::destack_process_euid(self.call_context, context)?;
                Ok(out)
            }
            None => {
                let mut out = std::mem::MaybeUninit::<UserId>::uninit();
                unsafe {
                    process_native::destack_process_euid(self.call_context, out.as_mut_ptr())?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// Call one generated platform test harness binding.
    pub(crate) fn destack_process_gid(&mut self) -> RuntimeResult<GroupId> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = process_vm::destack_process_gid(self.call_context, context)?;
                Ok(out)
            }
            None => {
                let mut out = std::mem::MaybeUninit::<GroupId>::uninit();
                unsafe {
                    process_native::destack_process_gid(self.call_context, out.as_mut_ptr())?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// Call one generated platform test harness binding.
    pub(crate) fn destack_process_group_ids(
        &mut self,
    ) -> RuntimeResult<HarnessValue<ProcessGroupIds, ProcessGroupIdsVm>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = process_vm::destack_process_group_ids(self.call_context, context)?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let mut out = std::mem::MaybeUninit::<ProcessGroupIds>::uninit();
                unsafe {
                    process_native::destack_process_group_ids(self.call_context, out.as_mut_ptr())?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Call one generated platform test harness binding.
    pub(crate) fn destack_process_groups(
        &mut self,
    ) -> RuntimeResult<HarnessValue<NativeSlice<GroupId>, VmSlice<GroupId>>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = process_vm::destack_process_groups(self.call_context, context)?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let mut out = std::mem::MaybeUninit::<NativeSlice<GroupId>>::uninit();
                unsafe {
                    process_native::destack_process_groups(self.call_context, out.as_mut_ptr())?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Call one generated platform test harness binding.
    pub(crate) fn destack_process_pid(&mut self) -> RuntimeResult<ProcessId> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = process_vm::destack_process_pid(self.call_context, context)?;
                Ok(out)
            }
            None => {
                let mut out = std::mem::MaybeUninit::<ProcessId>::uninit();
                unsafe {
                    process_native::destack_process_pid(self.call_context, out.as_mut_ptr())?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// Call one generated platform test harness binding.
    pub(crate) fn destack_process_ppid(&mut self) -> RuntimeResult<ProcessId> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = process_vm::destack_process_ppid(self.call_context, context)?;
                Ok(out)
            }
            None => {
                let mut out = std::mem::MaybeUninit::<ProcessId>::uninit();
                unsafe {
                    process_native::destack_process_ppid(self.call_context, out.as_mut_ptr())?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// Call one generated platform test harness binding.
    pub(crate) fn destack_process_set_egid(&mut self, groupid: GroupId) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                process_vm::destack_process_set_egid(self.call_context, context, groupid)
            }
            None => unsafe { process_native::destack_process_set_egid(self.call_context, groupid) },
        }
    }

    /// Call one generated platform test harness binding.
    pub(crate) fn destack_process_set_euid(&mut self, userid: UserId) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                process_vm::destack_process_set_euid(self.call_context, context, userid)
            }
            None => unsafe { process_native::destack_process_set_euid(self.call_context, userid) },
        }
    }

    /// Call one generated platform test harness binding.
    pub(crate) fn destack_process_set_gid(&mut self, groupid: GroupId) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                process_vm::destack_process_set_gid(self.call_context, context, groupid)
            }
            None => unsafe { process_native::destack_process_set_gid(self.call_context, groupid) },
        }
    }

    /// Call one generated platform test harness binding.
    pub(crate) fn destack_process_set_group_ids(
        &mut self,
        ids: HarnessValue<ProcessGroupIds, ProcessGroupIdsVm>,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let ids = ids.into_vm("ids")?;
                process_vm::destack_process_set_group_ids(self.call_context, context, ids)
            }
            None => unsafe {
                let ids = ids.into_native("ids")?;
                process_native::destack_process_set_group_ids(self.call_context, ids)
            },
        }
    }

    /// Call one generated platform test harness binding.
    pub(crate) fn destack_process_set_groups(
        &mut self,
        groups: HarnessValue<NativeSlice<GroupId>, VmSlice<GroupId>>,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let groups = groups.into_vm("groups")?;
                process_vm::destack_process_set_groups(self.call_context, context, groups)
            }
            None => unsafe {
                let groups = groups.into_native("groups")?;
                process_native::destack_process_set_groups(self.call_context, groups)
            },
        }
    }

    /// Call one generated platform test harness binding.
    pub(crate) fn destack_process_set_uid(&mut self, userid: UserId) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                process_vm::destack_process_set_uid(self.call_context, context, userid)
            }
            None => unsafe { process_native::destack_process_set_uid(self.call_context, userid) },
        }
    }

    /// Call one generated platform test harness binding.
    pub(crate) fn destack_process_set_user_ids(
        &mut self,
        ids: HarnessValue<ProcessUserIds, ProcessUserIdsVm>,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let ids = ids.into_vm("ids")?;
                process_vm::destack_process_set_user_ids(self.call_context, context, ids)
            }
            None => unsafe {
                let ids = ids.into_native("ids")?;
                process_native::destack_process_set_user_ids(self.call_context, ids)
            },
        }
    }

    /// Call one generated platform test harness binding.
    pub(crate) fn destack_process_uid(&mut self) -> RuntimeResult<UserId> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = process_vm::destack_process_uid(self.call_context, context)?;
                Ok(out)
            }
            None => {
                let mut out = std::mem::MaybeUninit::<UserId>::uninit();
                unsafe {
                    process_native::destack_process_uid(self.call_context, out.as_mut_ptr())?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// Call one generated platform test harness binding.
    pub(crate) fn destack_process_user_ids(
        &mut self,
    ) -> RuntimeResult<HarnessValue<ProcessUserIds, ProcessUserIdsVm>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = process_vm::destack_process_user_ids(self.call_context, context)?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let mut out = std::mem::MaybeUninit::<ProcessUserIds>::uninit();
                unsafe {
                    process_native::destack_process_user_ids(self.call_context, out.as_mut_ptr())?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Call one generated platform test harness binding.
    pub(crate) fn destack_process_chroot(
        &mut self,
        path: HarnessValue<fs::OsPath, fs::OsPathVm>,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let path = path.into_vm("path")?;
                process_vm::destack_process_chroot(self.call_context, context, path)
            }
            None => unsafe {
                let path = path.into_native("path")?;
                process_native::destack_process_chroot(self.call_context, path)
            },
        }
    }

    /// Call one generated platform test harness binding.
    pub(crate) fn destack_process_install_syscall_filter(
        &mut self,
        program: HarnessValue<NativeArray<u8>, VmArray<u8>>,
        flags: SyscallFilterFlags,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let program = program.into_vm("program")?;
                process_vm::destack_process_install_syscall_filter(
                    self.call_context,
                    context,
                    program,
                    flags,
                )
            }
            None => unsafe {
                let program = program.into_native("program")?;
                process_native::destack_process_install_syscall_filter(
                    self.call_context,
                    program,
                    flags,
                )
            },
        }
    }

    /// Call one generated platform test harness binding.
    pub(crate) fn destack_process_set_host_name(
        &mut self,
        name: HarnessValue<NativeStringRef, vm::StringHandle>,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let name = name.into_vm("name")?;
                process_vm::destack_process_set_host_name(self.call_context, context, name)
            }
            None => unsafe {
                let name = name.into_native("name")?;
                process_native::destack_process_set_host_name(self.call_context, name)
            },
        }
    }

    /// Call one generated platform test harness binding.
    pub(crate) fn destack_process_set_network_namespace(
        &mut self,
        path: HarnessValue<fs::OsPath, fs::OsPathVm>,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let path = path.into_vm("path")?;
                process_vm::destack_process_set_network_namespace(self.call_context, context, path)
            }
            None => unsafe {
                let path = path.into_native("path")?;
                process_native::destack_process_set_network_namespace(self.call_context, path)
            },
        }
    }

    /// Call one generated platform test harness binding.
    pub(crate) fn destack_process_setns(
        &mut self,
        pid: ProcessId,
        namespace: ProcessNamespaceKind,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                process_vm::destack_process_setns(self.call_context, context, pid, namespace)
            }
            None => unsafe {
                process_native::destack_process_setns(self.call_context, pid, namespace)
            },
        }
    }

    /// Call one generated platform test harness binding.
    pub(crate) fn destack_process_unshare(
        &mut self,
        flags: ProcessUnshareFlags,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => process_vm::destack_process_unshare(self.call_context, context, flags),
            None => unsafe { process_native::destack_process_unshare(self.call_context, flags) },
        }
    }

    /// Call one generated platform test harness binding.
    pub(crate) fn destack_process_get_limit(
        &mut self,
        resource: ProcessLimitResource,
    ) -> RuntimeResult<HarnessValue<ProcessLimit, ProcessLimitVm>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out =
                    process_vm::destack_process_get_limit(self.call_context, context, resource)?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let mut out = std::mem::MaybeUninit::<ProcessLimit>::uninit();
                unsafe {
                    process_native::destack_process_get_limit(
                        self.call_context,
                        out.as_mut_ptr(),
                        resource,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Call one generated platform test harness binding.
    pub(crate) fn destack_process_set_limit(
        &mut self,
        resource: ProcessLimitResource,
        limit: HarnessValue<ProcessLimit, ProcessLimitVm>,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let limit = limit.into_vm("limit")?;
                process_vm::destack_process_set_limit(self.call_context, context, resource, limit)
            }
            None => unsafe {
                let limit = limit.into_native("limit")?;
                process_native::destack_process_set_limit(self.call_context, resource, limit)
            },
        }
    }

    /// Call one generated platform test harness binding.
    pub(crate) fn destack_process_get_affinity(
        &mut self,
        pid: ProcessId,
    ) -> RuntimeResult<HarnessValue<thread::ThreadCpuSet, thread::ThreadCpuSetVm>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out =
                    process_vm::destack_process_get_affinity(self.call_context, context, pid)?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let mut out = std::mem::MaybeUninit::<thread::ThreadCpuSet>::uninit();
                unsafe {
                    process_native::destack_process_get_affinity(
                        self.call_context,
                        out.as_mut_ptr(),
                        pid,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Call one generated platform test harness binding.
    pub(crate) fn destack_process_get_priority(&mut self, pid: ProcessId) -> RuntimeResult<i32> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out =
                    process_vm::destack_process_get_priority(self.call_context, context, pid)?;
                Ok(out)
            }
            None => {
                let mut out = std::mem::MaybeUninit::<i32>::uninit();
                unsafe {
                    process_native::destack_process_get_priority(
                        self.call_context,
                        out.as_mut_ptr(),
                        pid,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// Call one generated platform test harness binding.
    pub(crate) fn destack_process_get_scheduler(
        &mut self,
        pid: ProcessId,
    ) -> RuntimeResult<HarnessValue<ProcessSchedulerConfig, ProcessSchedulerConfigVm>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out =
                    process_vm::destack_process_get_scheduler(self.call_context, context, pid)?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let mut out = std::mem::MaybeUninit::<ProcessSchedulerConfig>::uninit();
                unsafe {
                    process_native::destack_process_get_scheduler(
                        self.call_context,
                        out.as_mut_ptr(),
                        pid,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Call one generated platform test harness binding.
    pub(crate) fn destack_process_set_affinity(
        &mut self,
        pid: ProcessId,
        cpus: HarnessValue<thread::ThreadCpuSet, thread::ThreadCpuSetVm>,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let cpus = cpus.into_vm("cpus")?;
                process_vm::destack_process_set_affinity(self.call_context, context, pid, cpus)
            }
            None => unsafe {
                let cpus = cpus.into_native("cpus")?;
                process_native::destack_process_set_affinity(self.call_context, pid, cpus)
            },
        }
    }

    /// Call one generated platform test harness binding.
    pub(crate) fn destack_process_set_priority(
        &mut self,
        pid: ProcessId,
        priority: i32,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                process_vm::destack_process_set_priority(self.call_context, context, pid, priority)
            }
            None => unsafe {
                process_native::destack_process_set_priority(self.call_context, pid, priority)
            },
        }
    }

    /// Call one generated platform test harness binding.
    pub(crate) fn destack_process_set_scheduler(
        &mut self,
        pid: ProcessId,
        config: HarnessValue<ProcessSchedulerConfig, ProcessSchedulerConfigVm>,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let config = config.into_vm("config")?;
                process_vm::destack_process_set_scheduler(self.call_context, context, pid, config)
            }
            None => unsafe {
                let config = config.into_native("config")?;
                process_native::destack_process_set_scheduler(self.call_context, pid, config)
            },
        }
    }

    /// Call one generated platform test harness binding.
    pub(crate) fn destack_process_yield_now(&mut self) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => process_vm::destack_process_yield_now(self.call_context, context),
            None => unsafe { process_native::destack_process_yield_now(self.call_context) },
        }
    }

    /// Call one generated platform test harness binding.
    pub(crate) fn destack_process_getpgid(&mut self, pid: ProcessId) -> RuntimeResult<ProcessId> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = process_vm::destack_process_getpgid(self.call_context, context, pid)?;
                Ok(out)
            }
            None => {
                let mut out = std::mem::MaybeUninit::<ProcessId>::uninit();
                unsafe {
                    process_native::destack_process_getpgid(
                        self.call_context,
                        out.as_mut_ptr(),
                        pid,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// Call one generated platform test harness binding.
    pub(crate) fn destack_process_setpgid(
        &mut self,
        pid: ProcessId,
        pgid: ProcessId,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                process_vm::destack_process_setpgid(self.call_context, context, pid, pgid)
            }
            None => unsafe {
                process_native::destack_process_setpgid(self.call_context, pid, pgid)
            },
        }
    }

    /// Call one generated platform test harness binding.
    pub(crate) fn destack_process_setsid(&mut self) -> RuntimeResult<ProcessId> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = process_vm::destack_process_setsid(self.call_context, context)?;
                Ok(out)
            }
            None => {
                let mut out = std::mem::MaybeUninit::<ProcessId>::uninit();
                unsafe {
                    process_native::destack_process_setsid(self.call_context, out.as_mut_ptr())?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// Call one generated platform test harness binding.
    pub(crate) fn destack_process_kill(
        &mut self,
        pid: ProcessId,
        signal: Signal,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                process_vm::destack_process_kill(self.call_context, context, pid, signal)
            }
            None => unsafe { process_native::destack_process_kill(self.call_context, pid, signal) },
        }
    }

    /// Call one generated platform test harness binding.
    pub(crate) fn destack_process_signal_mask_read(
        &mut self,
    ) -> RuntimeResult<HarnessValue<NativeArray<Signal>, VmArray<Signal>>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = process_vm::destack_process_signal_mask_read(self.call_context, context)?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let mut out = std::mem::MaybeUninit::<NativeArray<Signal>>::uninit();
                unsafe {
                    process_native::destack_process_signal_mask_read(
                        self.call_context,
                        out.as_mut_ptr(),
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Call one generated platform test harness binding.
    pub(crate) fn destack_process_signal_mask_update(
        &mut self,
        how: SignalMaskHow,
        signals: HarnessValue<NativeSlice<Signal>, VmSlice<Signal>>,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let signals = signals.into_vm("signals")?;
                process_vm::destack_process_signal_mask_update(
                    self.call_context,
                    context,
                    how,
                    signals,
                )
            }
            None => unsafe {
                let signals = signals.into_native("signals")?;
                process_native::destack_process_signal_mask_update(self.call_context, how, signals)
            },
        }
    }

    /// Call one generated platform test harness binding.
    pub(crate) fn destack_process_signal_receive(
        &mut self,
        handle: resource::SignalHandle,
    ) -> RuntimeResult<HarnessValue<SignalEvent, SignalEventVm>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out =
                    process_vm::destack_process_signal_receive(self.call_context, context, handle)?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let mut out = std::mem::MaybeUninit::<SignalEvent>::uninit();
                unsafe {
                    process_native::destack_process_signal_receive(
                        self.call_context,
                        out.as_mut_ptr(),
                        handle,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Call one generated platform test harness binding.
    pub(crate) fn destack_process_signal_subscribe(
        &mut self,
        signal: Signal,
    ) -> RuntimeResult<resource::SignalHandle> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = process_vm::destack_process_signal_subscribe(
                    self.call_context,
                    context,
                    signal,
                )?;
                Ok(out)
            }
            None => {
                let mut out = std::mem::MaybeUninit::<resource::SignalHandle>::uninit();
                unsafe {
                    process_native::destack_process_signal_subscribe(
                        self.call_context,
                        out.as_mut_ptr(),
                        signal,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// Call one generated platform test harness binding.
    pub(crate) fn destack_process_signal_try_receive(
        &mut self,
        handle: resource::SignalHandle,
    ) -> RuntimeResult<HarnessValue<SignalEvent, SignalEventVm>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = process_vm::destack_process_signal_try_receive(
                    self.call_context,
                    context,
                    handle,
                )?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let mut out = std::mem::MaybeUninit::<SignalEvent>::uninit();
                unsafe {
                    process_native::destack_process_signal_try_receive(
                        self.call_context,
                        out.as_mut_ptr(),
                        handle,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Call one generated platform test harness binding.
    pub(crate) fn destack_process_signal_try_wait(
        &mut self,
        signals: HarnessValue<NativeSlice<Signal>, VmSlice<Signal>>,
    ) -> RuntimeResult<HarnessValue<SignalEvent, SignalEventVm>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let signals = signals.into_vm("signals")?;
                let out = process_vm::destack_process_signal_try_wait(
                    self.call_context,
                    context,
                    signals,
                )?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let signals = signals.into_native("signals")?;
                let mut out = std::mem::MaybeUninit::<SignalEvent>::uninit();
                unsafe {
                    process_native::destack_process_signal_try_wait(
                        self.call_context,
                        out.as_mut_ptr(),
                        signals,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Call one generated platform test harness binding.
    pub(crate) fn destack_process_signal_unsubscribe(
        &mut self,
        handle: resource::SignalHandle,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                process_vm::destack_process_signal_unsubscribe(self.call_context, context, handle)
            }
            None => unsafe {
                process_native::destack_process_signal_unsubscribe(self.call_context, handle)
            },
        }
    }

    /// Call one generated platform test harness binding.
    pub(crate) fn destack_process_signal_wait(
        &mut self,
        signals: HarnessValue<NativeSlice<Signal>, VmSlice<Signal>>,
    ) -> RuntimeResult<HarnessValue<SignalEvent, SignalEventVm>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let signals = signals.into_vm("signals")?;
                let out =
                    process_vm::destack_process_signal_wait(self.call_context, context, signals)?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let signals = signals.into_native("signals")?;
                let mut out = std::mem::MaybeUninit::<SignalEvent>::uninit();
                unsafe {
                    process_native::destack_process_signal_wait(
                        self.call_context,
                        out.as_mut_ptr(),
                        signals,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Call one generated platform test harness binding.
    pub(crate) fn destack_process_spawn(
        &mut self,
        command: HarnessValue<fs::OsPath, fs::OsPathVm>,
        arguments: HarnessValue<NativeStringSlice, VmSlice<vm::StringHandle>>,
        environment: HarnessValue<NativeStringSlice, VmSlice<vm::StringHandle>>,
        options: HarnessValue<ProcessSpawnOptions, ProcessSpawnOptionsVm>,
    ) -> RuntimeResult<resource::ProcessHandle> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let command = command.into_vm("command")?;
                let arguments = arguments.into_vm("arguments")?;
                let environment = environment.into_vm("environment")?;
                let options = options.into_vm("options")?;
                let out = process_vm::destack_process_spawn(
                    self.call_context,
                    context,
                    command,
                    arguments,
                    environment,
                    options,
                )?;
                Ok(out)
            }
            None => {
                let command = command.into_native("command")?;
                let arguments = arguments.into_native("arguments")?;
                let environment = environment.into_native("environment")?;
                let options = options.into_native("options")?;
                let mut out = std::mem::MaybeUninit::<resource::ProcessHandle>::uninit();
                unsafe {
                    process_native::destack_process_spawn(
                        self.call_context,
                        out.as_mut_ptr(),
                        command,
                        arguments,
                        environment,
                        options,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// Call one generated platform test harness binding.
    pub(crate) fn destack_process_spawn_with_actions(
        &mut self,
        command: HarnessValue<fs::OsPath, fs::OsPathVm>,
        arguments: HarnessValue<NativeStringSlice, VmSlice<vm::StringHandle>>,
        environment: HarnessValue<NativeStringSlice, VmSlice<vm::StringHandle>>,
        options: HarnessValue<ProcessSpawnOptions, ProcessSpawnOptionsVm>,
        stdio: HarnessValue<NativeSlice<ProcessStdio>, VmSlice<ProcessStdioVm>>,
        actions: HarnessValue<NativeSlice<ProcessFdAction>, VmSlice<ProcessFdActionVm>>,
    ) -> RuntimeResult<resource::ProcessHandle> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let command = command.into_vm("command")?;
                let arguments = arguments.into_vm("arguments")?;
                let environment = environment.into_vm("environment")?;
                let options = options.into_vm("options")?;
                let stdio = stdio.into_vm("stdio")?;
                let actions = actions.into_vm("actions")?;
                let out = process_vm::destack_process_spawn_with_actions(
                    self.call_context,
                    context,
                    command,
                    arguments,
                    environment,
                    options,
                    stdio,
                    actions,
                )?;
                Ok(out)
            }
            None => {
                let command = command.into_native("command")?;
                let arguments = arguments.into_native("arguments")?;
                let environment = environment.into_native("environment")?;
                let options = options.into_native("options")?;
                let stdio = stdio.into_native("stdio")?;
                let actions = actions.into_native("actions")?;
                let mut out = std::mem::MaybeUninit::<resource::ProcessHandle>::uninit();
                unsafe {
                    process_native::destack_process_spawn_with_actions(
                        self.call_context,
                        out.as_mut_ptr(),
                        command,
                        arguments,
                        environment,
                        options,
                        stdio,
                        actions,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// Call one generated platform test harness binding.
    pub(crate) fn destack_process_umask(&mut self, mask: u32) -> RuntimeResult<u32> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = process_vm::destack_process_umask(self.call_context, context, mask)?;
                Ok(out)
            }
            None => {
                let mut out = std::mem::MaybeUninit::<u32>::uninit();
                unsafe {
                    process_native::destack_process_umask(
                        self.call_context,
                        out.as_mut_ptr(),
                        mask,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// Call one generated platform test harness binding.
    pub(crate) fn destack_process_wait(
        &mut self,
        handle: resource::ProcessHandle,
        flags: ProcessWaitFlags,
    ) -> RuntimeResult<HarnessValue<ProcessWaitStatus, ProcessWaitStatusVm>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out =
                    process_vm::destack_process_wait(self.call_context, context, handle, flags)?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let mut out = std::mem::MaybeUninit::<ProcessWaitStatus>::uninit();
                unsafe {
                    process_native::destack_process_wait(
                        self.call_context,
                        out.as_mut_ptr(),
                        handle,
                        flags,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Call one generated platform test harness binding.
    pub(crate) fn destack_process_wait_pid(
        &mut self,
        pid: ProcessId,
        flags: ProcessWaitFlags,
    ) -> RuntimeResult<HarnessValue<ProcessWaitStatus, ProcessWaitStatusVm>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out =
                    process_vm::destack_process_wait_pid(self.call_context, context, pid, flags)?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let mut out = std::mem::MaybeUninit::<ProcessWaitStatus>::uninit();
                unsafe {
                    process_native::destack_process_wait_pid(
                        self.call_context,
                        out.as_mut_ptr(),
                        pid,
                        flags,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Call one generated platform test harness binding.
    pub(crate) fn destack_process_try_wait(
        &mut self,
        handle: resource::ProcessHandle,
    ) -> RuntimeResult<HarnessValue<ProcessWaitStatus, ProcessWaitStatusVm>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = process_vm::destack_process_try_wait(self.call_context, context, handle)?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let mut out = std::mem::MaybeUninit::<ProcessWaitStatus>::uninit();
                unsafe {
                    process_native::destack_process_try_wait(
                        self.call_context,
                        out.as_mut_ptr(),
                        handle,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }
}

/// Wrapper that carries one native or VM value for generated harness bindings.
pub(crate) enum HarnessValue<Native, Vm> {
    /// Native value variant.
    Native(Native),
    /// VM value variant.
    Vm(Vm),
}

impl<Native: Clone, Vm: Clone> Clone for HarnessValue<Native, Vm> {
    fn clone(&self) -> Self {
        match self {
            HarnessValue::Native(value) => HarnessValue::Native(value.clone()),
            HarnessValue::Vm(value) => HarnessValue::Vm(value.clone()),
        }
    }
}

impl<Native, Vm> HarnessValue<Native, Vm> {
    /// Convert one wrapper into one native value.
    fn into_native(self, label: &str) -> RuntimeResult<Native> {
        match self {
            HarnessValue::Native(value) => Ok(value),
            HarnessValue::Vm(_) => Err(RuntimeError::from(
                HarnessPlatformError::invalid_argument_value(
                    label,
                    "expected native harness value",
                ),
            )
            .boxed()),
        }
    }

    /// Convert one wrapper into one VM value.
    fn into_vm(self, label: &str) -> RuntimeResult<Vm> {
        match self {
            HarnessValue::Vm(value) => Ok(value),
            HarnessValue::Native(_) => Err(RuntimeError::from(
                HarnessPlatformError::invalid_argument_value(label, "expected VM harness value"),
            )
            .boxed()),
        }
    }
}
