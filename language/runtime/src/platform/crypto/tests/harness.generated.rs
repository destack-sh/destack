// generated by generate-bindings: test harness, do not edit

#![allow(dead_code)]
#![allow(unused_imports)]
#![allow(clippy::type_complexity)]

use super::*;
use crate::diagnostic::{RuntimeError, RuntimeResult};
use crate::platform::crypto::{
    CryptoAgreementDeriveKeyRequest, CryptoAgreementDeriveKeyRequestVm, CryptoArgon2idRequest,
    CryptoArgon2idRequestVm, CryptoAsymmetricEncryptionAlgorithm,
    CryptoAsymmetricEncryptionParameters, CryptoAsymmetricEncryptionParametersVm,
    CryptoCertificateDescriptor, CryptoCertificateDescriptorVm, CryptoCertificateFormat,
    CryptoCertificateIdentityKind, CryptoCertificateListEntry, CryptoCertificateListEntryVm,
    CryptoCertificateListPage, CryptoCertificateListPageVm, CryptoCertificatePurpose,
    CryptoCertificateQuery, CryptoCertificateQueryVm, CryptoCertificateRevocationMode,
    CryptoCertificateValidity, CryptoCertificateValidityVm, CryptoCertificateVerifyError,
    CryptoCertificateVerifyIdentity, CryptoCertificateVerifyIdentityVm,
    CryptoCertificateVerifyRequest, CryptoCertificateVerifyRequestVm,
    CryptoCertificateVerifyResult, CryptoCertificateVerifyResultVm, CryptoCipherAlgorithm,
    CryptoCipherDirection, CryptoCipherOutput, CryptoCipherOutputVm, CryptoCipherParameters,
    CryptoCipherParametersVm, CryptoDigestAlgorithm, CryptoHkdfRequest, CryptoHkdfRequestVm,
    CryptoKdfAlgorithm, CryptoKeyAgreementAlgorithm, CryptoKeyAlgorithm, CryptoKeyDescriptor,
    CryptoKeyDescriptorAes, CryptoKeyDescriptorAesVm, CryptoKeyDescriptorChaCha20,
    CryptoKeyDescriptorChaCha20Vm, CryptoKeyDescriptorEc, CryptoKeyDescriptorEcVm,
    CryptoKeyDescriptorEd448, CryptoKeyDescriptorEd448Vm, CryptoKeyDescriptorEd25519,
    CryptoKeyDescriptorEd25519Vm, CryptoKeyDescriptorHmac, CryptoKeyDescriptorHmacVm,
    CryptoKeyDescriptorRsa, CryptoKeyDescriptorRsaVm, CryptoKeyDescriptorVm,
    CryptoKeyDescriptorX448, CryptoKeyDescriptorX448Vm, CryptoKeyDescriptorX25519,
    CryptoKeyDescriptorX25519Vm, CryptoKeyFormat, CryptoKeyGenerationRequest,
    CryptoKeyGenerationRequestAes, CryptoKeyGenerationRequestAesVm,
    CryptoKeyGenerationRequestChaCha20, CryptoKeyGenerationRequestChaCha20Vm,
    CryptoKeyGenerationRequestEc, CryptoKeyGenerationRequestEcVm, CryptoKeyGenerationRequestEd448,
    CryptoKeyGenerationRequestEd448Vm, CryptoKeyGenerationRequestEd25519,
    CryptoKeyGenerationRequestEd25519Vm, CryptoKeyGenerationRequestHmac,
    CryptoKeyGenerationRequestHmacVm, CryptoKeyGenerationRequestRsa,
    CryptoKeyGenerationRequestRsaVm, CryptoKeyGenerationRequestVm, CryptoKeyGenerationRequestX448,
    CryptoKeyGenerationRequestX448Vm, CryptoKeyGenerationRequestX25519,
    CryptoKeyGenerationRequestX25519Vm, CryptoKeyImportRequest, CryptoKeyImportRequestAes,
    CryptoKeyImportRequestAesVm, CryptoKeyImportRequestChaCha20, CryptoKeyImportRequestChaCha20Vm,
    CryptoKeyImportRequestEc, CryptoKeyImportRequestEcVm, CryptoKeyImportRequestEd448,
    CryptoKeyImportRequestEd448Vm, CryptoKeyImportRequestEd25519, CryptoKeyImportRequestEd25519Vm,
    CryptoKeyImportRequestHmac, CryptoKeyImportRequestHmacVm, CryptoKeyImportRequestRsa,
    CryptoKeyImportRequestRsaVm, CryptoKeyImportRequestVm, CryptoKeyImportRequestX448,
    CryptoKeyImportRequestX448Vm, CryptoKeyImportRequestX25519, CryptoKeyImportRequestX25519Vm,
    CryptoKeyKind, CryptoKeyListEntry, CryptoKeyListEntryVm, CryptoKeyListPage,
    CryptoKeyListPageVm, CryptoKeyPair, CryptoKeyPairVm, CryptoKeyQuery, CryptoKeyQueryVm,
    CryptoKeyResidency, CryptoKeyUsageMask, CryptoKeyWrapAlgorithm, CryptoKeyWrapParameters,
    CryptoKeyWrapParametersVm, CryptoMacAlgorithm, CryptoMacParameters, CryptoMacParametersVm,
    CryptoNamedCurve, CryptoPbkdf2Request, CryptoPbkdf2RequestVm, CryptoPrivateKeyExportRequest,
    CryptoPrivateKeyExportRequestVm, CryptoScryptRequest, CryptoScryptRequestVm,
    CryptoSignatureAlgorithm, CryptoSignatureParameters, CryptoSignatureParametersVm,
    CryptoStoreAgreementCapability, CryptoStoreAgreementCapabilityVm,
    CryptoStoreAsymmetricEncryptionCapability, CryptoStoreAsymmetricEncryptionCapabilityVm,
    CryptoStoreCapability, CryptoStoreCapabilityVm, CryptoStoreCertificateCapability,
    CryptoStoreCertificateCapabilityVm, CryptoStoreCipherCapability, CryptoStoreCipherCapabilityVm,
    CryptoStoreIdentity, CryptoStoreIdentityVm, CryptoStoreKeyCapability,
    CryptoStoreKeyCapabilityVm, CryptoStoreKeyWrapCapability, CryptoStoreKeyWrapCapabilityVm,
    CryptoStoreKind, CryptoStoreMacCapability, CryptoStoreMacCapabilityVm, CryptoStoreOptions,
    CryptoStoreOptionsVm, CryptoStoreProvenance, CryptoStoreProvenanceVm, CryptoStoreProvider,
    CryptoStoreSignatureCapability, CryptoStoreSignatureCapabilityVm, native as crypto_native,
    vm as crypto_vm,
};
use crate::platform::{
    NativeArray, NativeSlice, NativeStringRef, PlatformError as HarnessPlatformError, VmArray,
    VmSlice, resource,
};
use destack_vm as vm;

impl<'call> CryptoHarnessContext<'call> {
    /// Return the generated VM context if available.
    #[allow(clippy::mut_from_ref)]
    fn generated_vm_context_mut(&self) -> Option<&mut vm::ExternalCallContext<'_>> {
        self.vm_context
            .map(|context| unsafe { &mut *(context as *mut vm::ExternalCallContext<'_>) })
    }

    /// Return one standardized value payload for native and VM variants.
    pub(crate) fn harness_value<Native, Vm>(&self, native: Native) -> HarnessValue<Native, Vm> {
        HarnessValue::Native(native)
    }

    /// Return one standardized value payload for VM and native variants.
    pub(crate) fn harness_value_vm<Native, Vm>(&self, vm: Vm) -> HarnessValue<Native, Vm> {
        HarnessValue::Vm(vm)
    }

    /// Derive one symmetric key from one local private key and one peer public key.
    ///
    /// This operation performs key agreement and an explicit KDF stage.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the crypto feature is unavailable.
    /// Uses OpenSSL key-agreement and KDF primitives for software providers, and host key APIs for host-managed keys: Security.framework on Apple, CNG on Windows, and Android keystore callbacks when registered.
    ///
    /// # Errors
    /// Returns invalidArgument, ioPermissionDenied, ioInvalidData, notSupported.
    ///
    /// # Security
    /// Requires `crypto.key.agree`, `crypto.kdf`.
    ///
    /// # Replay
    /// External, nonrecordable.
    pub(crate) fn destack_crypto_agreement_derive_key(
        &mut self,
        privatekey: resource::CryptoKeyHandle,
        peerpublickey: resource::CryptoKeyHandle,
        request: HarnessValue<CryptoAgreementDeriveKeyRequest, CryptoAgreementDeriveKeyRequestVm>,
    ) -> RuntimeResult<HarnessValue<NativeSlice<u8>, VmSlice<u8>>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let request = request.into_vm("request")?;
                let out = crypto_vm::destack_crypto_agreement_derive_key(
                    self.call_context,
                    context,
                    privatekey,
                    peerpublickey,
                    request,
                )?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let request = request.into_native("request")?;
                let mut out = std::mem::MaybeUninit::<NativeSlice<u8>>::uninit();
                unsafe {
                    crypto_native::destack_crypto_agreement_derive_key(
                        self.call_context,
                        out.as_mut_ptr(),
                        privatekey,
                        peerpublickey,
                        request,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Derive one shared secret from one local private key and one peer public key.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the crypto feature is unavailable.
    /// Uses OpenSSL key-agreement primitives for software providers, and host key APIs for host-managed keys: Security.framework on Apple, CNG on Windows, and Android keystore callbacks when registered.
    ///
    /// # Errors
    /// Returns invalidArgument, ioPermissionDenied, ioInvalidData, notSupported.
    ///
    /// # Security
    /// Requires `crypto.key.agree`.
    ///
    /// # Replay
    /// External, nonrecordable.
    pub(crate) fn destack_crypto_agreement_derive_shared_secret(
        &mut self,
        privatekey: resource::CryptoKeyHandle,
        peerpublickey: resource::CryptoKeyHandle,
        algorithm: CryptoKeyAgreementAlgorithm,
    ) -> RuntimeResult<HarnessValue<NativeSlice<u8>, VmSlice<u8>>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = crypto_vm::destack_crypto_agreement_derive_shared_secret(
                    self.call_context,
                    context,
                    privatekey,
                    peerpublickey,
                    algorithm,
                )?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let mut out = std::mem::MaybeUninit::<NativeSlice<u8>>::uninit();
                unsafe {
                    crypto_native::destack_crypto_agreement_derive_shared_secret(
                        self.call_context,
                        out.as_mut_ptr(),
                        privatekey,
                        peerpublickey,
                        algorithm,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Delete one certificate from one store when allowed.
    ///
    /// Remove one certificate object and invalidate the handle.
    /// Deletion permissions and persistence are enforced by runtime store policies.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the crypto feature is unavailable.
    /// Uses OpenSSL software certificate parsing and verification, plus host trust stores: Security.framework keychain and trust settings on Apple, and Crypt32 or CNG stores on Windows.
    ///
    /// # Errors
    /// Returns invalidArgument, ioPermissionDenied, ioInvalidData, notSupported.
    ///
    /// # Security
    /// Requires `crypto.certificate.write`.
    ///
    /// # Replay
    /// External, nonrecordable.
    pub(crate) fn destack_crypto_certificate_delete(
        &mut self,
        handle: resource::CryptoCertificateHandle,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                crypto_vm::destack_crypto_certificate_delete(self.call_context, context, handle)
            }
            None => unsafe {
                crypto_native::destack_crypto_certificate_delete(self.call_context, handle)
            },
        }
    }

    /// Return one certificate descriptor.
    ///
    /// Query one certificate handle and return normalized identity and validity metadata.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the crypto feature is unavailable.
    /// Uses OpenSSL software certificate parsing and verification, plus host trust stores: Security.framework keychain and trust settings on Apple, and Crypt32 or CNG stores on Windows.
    ///
    /// # Errors
    /// Returns invalidArgument, ioPermissionDenied, ioInvalidData, notSupported.
    ///
    /// # Security
    /// Requires `crypto.certificate.read`.
    ///
    /// # Replay
    /// External, nonrecordable.
    pub(crate) fn destack_crypto_certificate_descriptor(
        &mut self,
        handle: resource::CryptoCertificateHandle,
    ) -> RuntimeResult<HarnessValue<CryptoCertificateDescriptor, CryptoCertificateDescriptorVm>>
    {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = crypto_vm::destack_crypto_certificate_descriptor(
                    self.call_context,
                    context,
                    handle,
                )?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let mut out = std::mem::MaybeUninit::<CryptoCertificateDescriptor>::uninit();
                unsafe {
                    crypto_native::destack_crypto_certificate_descriptor(
                        self.call_context,
                        out.as_mut_ptr(),
                        handle,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Export one certificate from one handle.
    ///
    /// Serialize one certificate handle into the requested encoding format.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the crypto feature is unavailable.
    /// Uses OpenSSL software certificate parsing and verification, plus host trust stores: Security.framework keychain and trust settings on Apple, and Crypt32 or CNG stores on Windows.
    ///
    /// # Errors
    /// Returns invalidArgument, ioPermissionDenied, ioInvalidData, notSupported.
    ///
    /// # Security
    /// Requires `crypto.certificate.read`.
    ///
    /// # Replay
    /// External, nonrecordable.
    pub(crate) fn destack_crypto_certificate_export(
        &mut self,
        handle: resource::CryptoCertificateHandle,
        format: CryptoCertificateFormat,
    ) -> RuntimeResult<HarnessValue<NativeSlice<u8>, VmSlice<u8>>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = crypto_vm::destack_crypto_certificate_export(
                    self.call_context,
                    context,
                    handle,
                    format,
                )?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let mut out = std::mem::MaybeUninit::<NativeSlice<u8>>::uninit();
                unsafe {
                    crypto_native::destack_crypto_certificate_export(
                        self.call_context,
                        out.as_mut_ptr(),
                        handle,
                        format,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Import one certificate into one store.
    ///
    /// Parse and import one certificate blob into one store and return one certificate handle.
    /// Import visibility and persistence are enforced by runtime store policies.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the crypto feature is unavailable.
    /// Uses OpenSSL software certificate parsing and verification, plus host trust stores: Security.framework keychain and trust settings on Apple, and Crypt32 or CNG stores on Windows.
    ///
    /// # Errors
    /// Returns invalidArgument, ioPermissionDenied, ioInvalidData, notSupported.
    ///
    /// # Security
    /// Requires `crypto.certificate.write`.
    ///
    /// # Replay
    /// External, nonrecordable.
    pub(crate) fn destack_crypto_certificate_import(
        &mut self,
        store: resource::CryptoStoreHandle,
        format: CryptoCertificateFormat,
        certificate: HarnessValue<NativeSlice<u8>, VmSlice<u8>>,
    ) -> RuntimeResult<resource::CryptoCertificateHandle> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let certificate = certificate.into_vm("certificate")?;
                let out = crypto_vm::destack_crypto_certificate_import(
                    self.call_context,
                    context,
                    store,
                    format,
                    certificate,
                )?;
                Ok(out)
            }
            None => {
                let certificate = certificate.into_native("certificate")?;
                let mut out = std::mem::MaybeUninit::<resource::CryptoCertificateHandle>::uninit();
                unsafe {
                    crypto_native::destack_crypto_certificate_import(
                        self.call_context,
                        out.as_mut_ptr(),
                        store,
                        format,
                        certificate,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// Verify one certificate chain against one trust policy.
    ///
    /// Build and verify one certificate path for the requested purpose and verification time.
    /// Chain building and policy evaluation follow runtime trust engine behavior.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the crypto feature is unavailable.
    /// Uses OpenSSL software certificate parsing and verification, plus host trust stores: Security.framework keychain and trust settings on Apple, and Crypt32 or CNG stores on Windows.
    ///
    /// # Errors
    /// Returns invalidArgument, ioPermissionDenied, ioInvalidData, notSupported.
    ///
    /// # Security
    /// Requires `crypto.certificate.read`.
    ///
    /// # Replay
    /// External, nonrecordable.
    pub(crate) fn destack_crypto_certificate_verify(
        &mut self,
        request: HarnessValue<CryptoCertificateVerifyRequest, CryptoCertificateVerifyRequestVm>,
    ) -> RuntimeResult<HarnessValue<CryptoCertificateVerifyResult, CryptoCertificateVerifyResultVm>>
    {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let request = request.into_vm("request")?;
                let out = crypto_vm::destack_crypto_certificate_verify(
                    self.call_context,
                    context,
                    request,
                )?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let request = request.into_native("request")?;
                let mut out = std::mem::MaybeUninit::<CryptoCertificateVerifyResult>::uninit();
                unsafe {
                    crypto_native::destack_crypto_certificate_verify(
                        self.call_context,
                        out.as_mut_ptr(),
                        request,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Close one streaming cipher context.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the crypto feature is unavailable.
    /// Uses OpenSSL EVP symmetric-cipher primitives for software keys, and host key APIs for host-managed secret keys when available.
    ///
    /// # Errors
    /// Returns invalidArgument, ioNotFound, notSupported.
    ///
    /// # Security
    /// Requires `crypto.cipher`.
    ///
    /// # Replay
    /// External, nonrecordable.
    pub(crate) fn destack_crypto_cipher_close(
        &mut self,
        handle: resource::CryptoCipherHandle,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                crypto_vm::destack_crypto_cipher_close(self.call_context, context, handle)
            }
            None => unsafe {
                crypto_native::destack_crypto_cipher_close(self.call_context, handle)
            },
        }
    }

    /// Decrypt one payload in one shot.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the crypto feature is unavailable.
    /// Uses OpenSSL EVP symmetric-cipher primitives for software keys, and host key APIs for host-managed secret keys when available.
    ///
    /// # Errors
    /// Returns invalidArgument, ioPermissionDenied, ioInvalidData, notSupported.
    ///
    /// # Security
    /// Requires `crypto.cipher`.
    ///
    /// # Replay
    /// External, nonrecordable.
    pub(crate) fn destack_crypto_cipher_decrypt(
        &mut self,
        key: resource::CryptoKeyHandle,
        parameters: HarnessValue<CryptoCipherParameters, CryptoCipherParametersVm>,
        argument_payload: HarnessValue<NativeSlice<u8>, VmSlice<u8>>,
    ) -> RuntimeResult<HarnessValue<NativeSlice<u8>, VmSlice<u8>>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let parameters = parameters.into_vm("parameters")?;
                let argument_payload = argument_payload.into_vm("argument_payload")?;
                let out = crypto_vm::destack_crypto_cipher_decrypt(
                    self.call_context,
                    context,
                    key,
                    parameters,
                    argument_payload,
                )?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let parameters = parameters.into_native("parameters")?;
                let argument_payload = argument_payload.into_native("argument_payload")?;
                let mut out = std::mem::MaybeUninit::<NativeSlice<u8>>::uninit();
                unsafe {
                    crypto_native::destack_crypto_cipher_decrypt(
                        self.call_context,
                        out.as_mut_ptr(),
                        key,
                        parameters,
                        argument_payload,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Encrypt one payload in one shot.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the crypto feature is unavailable.
    /// Uses OpenSSL EVP symmetric-cipher primitives for software keys, and host key APIs for host-managed secret keys when available.
    ///
    /// # Errors
    /// Returns invalidArgument, ioPermissionDenied, ioInvalidData, notSupported.
    ///
    /// # Security
    /// Requires `crypto.cipher`.
    ///
    /// # Replay
    /// External, nonrecordable.
    pub(crate) fn destack_crypto_cipher_encrypt(
        &mut self,
        key: resource::CryptoKeyHandle,
        parameters: HarnessValue<CryptoCipherParameters, CryptoCipherParametersVm>,
        argument_payload: HarnessValue<NativeSlice<u8>, VmSlice<u8>>,
    ) -> RuntimeResult<HarnessValue<CryptoCipherOutput, CryptoCipherOutputVm>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let parameters = parameters.into_vm("parameters")?;
                let argument_payload = argument_payload.into_vm("argument_payload")?;
                let out = crypto_vm::destack_crypto_cipher_encrypt(
                    self.call_context,
                    context,
                    key,
                    parameters,
                    argument_payload,
                )?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let parameters = parameters.into_native("parameters")?;
                let argument_payload = argument_payload.into_native("argument_payload")?;
                let mut out = std::mem::MaybeUninit::<CryptoCipherOutput>::uninit();
                unsafe {
                    crypto_native::destack_crypto_cipher_encrypt(
                        self.call_context,
                        out.as_mut_ptr(),
                        key,
                        parameters,
                        argument_payload,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Finalize one streaming cipher context.
    ///
    /// Provide one final payload chunk.
    /// Return output bytes and one authentication tag when applicable.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the crypto feature is unavailable.
    /// Uses OpenSSL EVP symmetric-cipher primitives for software keys, and host key APIs for host-managed secret keys when available.
    ///
    /// # Errors
    /// Returns invalidArgument, ioInvalidData, notSupported.
    ///
    /// # Security
    /// Requires `crypto.cipher`.
    ///
    /// # Replay
    /// External, nonrecordable.
    pub(crate) fn destack_crypto_cipher_finish(
        &mut self,
        handle: resource::CryptoCipherHandle,
        finalpayload: HarnessValue<NativeSlice<u8>, VmSlice<u8>>,
    ) -> RuntimeResult<HarnessValue<CryptoCipherOutput, CryptoCipherOutputVm>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let finalpayload = finalpayload.into_vm("finalpayload")?;
                let out = crypto_vm::destack_crypto_cipher_finish(
                    self.call_context,
                    context,
                    handle,
                    finalpayload,
                )?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let finalpayload = finalpayload.into_native("finalpayload")?;
                let mut out = std::mem::MaybeUninit::<CryptoCipherOutput>::uninit();
                unsafe {
                    crypto_native::destack_crypto_cipher_finish(
                        self.call_context,
                        out.as_mut_ptr(),
                        handle,
                        finalpayload,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Open one streaming cipher context.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the crypto feature is unavailable.
    /// Uses OpenSSL EVP symmetric-cipher primitives for software keys, and host key APIs for host-managed secret keys when available.
    ///
    /// # Errors
    /// Returns invalidArgument, ioPermissionDenied, ioInvalidData, notSupported.
    ///
    /// # Security
    /// Requires `crypto.cipher`.
    ///
    /// # Replay
    /// External, nonrecordable.
    pub(crate) fn destack_crypto_cipher_open(
        &mut self,
        key: resource::CryptoKeyHandle,
        direction: CryptoCipherDirection,
        parameters: HarnessValue<CryptoCipherParameters, CryptoCipherParametersVm>,
    ) -> RuntimeResult<resource::CryptoCipherHandle> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let parameters = parameters.into_vm("parameters")?;
                let out = crypto_vm::destack_crypto_cipher_open(
                    self.call_context,
                    context,
                    key,
                    direction,
                    parameters,
                )?;
                Ok(out)
            }
            None => {
                let parameters = parameters.into_native("parameters")?;
                let mut out = std::mem::MaybeUninit::<resource::CryptoCipherHandle>::uninit();
                unsafe {
                    crypto_native::destack_crypto_cipher_open(
                        self.call_context,
                        out.as_mut_ptr(),
                        key,
                        direction,
                        parameters,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// Reset one streaming cipher context with new parameters.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the crypto feature is unavailable.
    /// Uses OpenSSL EVP symmetric-cipher primitives for software keys, and host key APIs for host-managed secret keys when available.
    ///
    /// # Errors
    /// Returns invalidArgument, ioInvalidData, notSupported.
    ///
    /// # Security
    /// Requires `crypto.cipher`.
    ///
    /// # Replay
    /// External, nonrecordable.
    pub(crate) fn destack_crypto_cipher_reset(
        &mut self,
        handle: resource::CryptoCipherHandle,
        parameters: HarnessValue<CryptoCipherParameters, CryptoCipherParametersVm>,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let parameters = parameters.into_vm("parameters")?;
                crypto_vm::destack_crypto_cipher_reset(
                    self.call_context,
                    context,
                    handle,
                    parameters,
                )
            }
            None => {
                let parameters = parameters.into_native("parameters")?;
                unsafe {
                    crypto_native::destack_crypto_cipher_reset(
                        self.call_context,
                        handle,
                        parameters,
                    )
                }
            }
        }
    }

    /// Update one streaming cipher context with one payload chunk.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the crypto feature is unavailable.
    /// Uses OpenSSL EVP symmetric-cipher primitives for software keys, and host key APIs for host-managed secret keys when available.
    ///
    /// # Errors
    /// Returns invalidArgument, ioInvalidData, notSupported.
    ///
    /// # Security
    /// Requires `crypto.cipher`.
    ///
    /// # Replay
    /// External, nonrecordable.
    pub(crate) fn destack_crypto_cipher_update(
        &mut self,
        handle: resource::CryptoCipherHandle,
        argument_payload: HarnessValue<NativeSlice<u8>, VmSlice<u8>>,
    ) -> RuntimeResult<HarnessValue<NativeSlice<u8>, VmSlice<u8>>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let argument_payload = argument_payload.into_vm("argument_payload")?;
                let out = crypto_vm::destack_crypto_cipher_update(
                    self.call_context,
                    context,
                    handle,
                    argument_payload,
                )?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let argument_payload = argument_payload.into_native("argument_payload")?;
                let mut out = std::mem::MaybeUninit::<NativeSlice<u8>>::uninit();
                unsafe {
                    crypto_native::destack_crypto_cipher_update(
                        self.call_context,
                        out.as_mut_ptr(),
                        handle,
                        argument_payload,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Update additional authenticated data for one streaming cipher context.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the crypto feature is unavailable.
    /// Uses OpenSSL EVP symmetric-cipher primitives for software keys, and host key APIs for host-managed secret keys when available.
    ///
    /// # Errors
    /// Returns invalidArgument, ioInvalidData, notSupported.
    ///
    /// # Security
    /// Requires `crypto.cipher`.
    ///
    /// # Replay
    /// External, nonrecordable.
    pub(crate) fn destack_crypto_cipher_update_additional_data(
        &mut self,
        handle: resource::CryptoCipherHandle,
        additionaldata: HarnessValue<NativeSlice<u8>, VmSlice<u8>>,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let additionaldata = additionaldata.into_vm("additionaldata")?;
                crypto_vm::destack_crypto_cipher_update_additional_data(
                    self.call_context,
                    context,
                    handle,
                    additionaldata,
                )
            }
            None => {
                let additionaldata = additionaldata.into_native("additionaldata")?;
                unsafe {
                    crypto_native::destack_crypto_cipher_update_additional_data(
                        self.call_context,
                        handle,
                        additionaldata,
                    )
                }
            }
        }
    }

    /// Close one streaming digest context.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the crypto feature is unavailable.
    /// Uses OpenSSL EVP digest primitives on Unix and Windows.
    ///
    /// # Errors
    /// Returns invalidArgument, ioNotFound, notSupported.
    ///
    /// # Security
    /// Requires `crypto.digest`.
    ///
    /// # Replay
    /// External, nonrecordable.
    pub(crate) fn destack_crypto_digest_close(
        &mut self,
        handle: resource::CryptoDigestHandle,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                crypto_vm::destack_crypto_digest_close(self.call_context, context, handle)
            }
            None => unsafe {
                crypto_native::destack_crypto_digest_close(self.call_context, handle)
            },
        }
    }

    /// Compute one digest in one shot.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the crypto feature is unavailable.
    /// Uses OpenSSL EVP digest primitives on Unix and Windows.
    ///
    /// # Errors
    /// Returns invalidArgument, ioInvalidData, notSupported.
    ///
    /// # Security
    /// Requires `crypto.digest`.
    ///
    /// # Replay
    /// External, nonrecordable.
    pub(crate) fn destack_crypto_digest_compute(
        &mut self,
        algorithm: CryptoDigestAlgorithm,
        argument_payload: HarnessValue<NativeSlice<u8>, VmSlice<u8>>,
    ) -> RuntimeResult<HarnessValue<NativeSlice<u8>, VmSlice<u8>>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let argument_payload = argument_payload.into_vm("argument_payload")?;
                let out = crypto_vm::destack_crypto_digest_compute(
                    self.call_context,
                    context,
                    algorithm,
                    argument_payload,
                )?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let argument_payload = argument_payload.into_native("argument_payload")?;
                let mut out = std::mem::MaybeUninit::<NativeSlice<u8>>::uninit();
                unsafe {
                    crypto_native::destack_crypto_digest_compute(
                        self.call_context,
                        out.as_mut_ptr(),
                        algorithm,
                        argument_payload,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Finalize one streaming digest context and return one digest output.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the crypto feature is unavailable.
    /// Uses OpenSSL EVP digest primitives on Unix and Windows.
    ///
    /// # Errors
    /// Returns invalidArgument, ioInvalidData, notSupported.
    ///
    /// # Security
    /// Requires `crypto.digest`.
    ///
    /// # Replay
    /// External, nonrecordable.
    pub(crate) fn destack_crypto_digest_finish(
        &mut self,
        handle: resource::CryptoDigestHandle,
    ) -> RuntimeResult<HarnessValue<NativeSlice<u8>, VmSlice<u8>>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out =
                    crypto_vm::destack_crypto_digest_finish(self.call_context, context, handle)?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let mut out = std::mem::MaybeUninit::<NativeSlice<u8>>::uninit();
                unsafe {
                    crypto_native::destack_crypto_digest_finish(
                        self.call_context,
                        out.as_mut_ptr(),
                        handle,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Open one streaming digest context.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the crypto feature is unavailable.
    /// Uses OpenSSL EVP digest primitives on Unix and Windows.
    ///
    /// # Errors
    /// Returns invalidArgument, ioInvalidData, notSupported.
    ///
    /// # Security
    /// Requires `crypto.digest`.
    ///
    /// # Replay
    /// External, nonrecordable.
    pub(crate) fn destack_crypto_digest_open(
        &mut self,
        algorithm: CryptoDigestAlgorithm,
    ) -> RuntimeResult<resource::CryptoDigestHandle> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out =
                    crypto_vm::destack_crypto_digest_open(self.call_context, context, algorithm)?;
                Ok(out)
            }
            None => {
                let mut out = std::mem::MaybeUninit::<resource::CryptoDigestHandle>::uninit();
                unsafe {
                    crypto_native::destack_crypto_digest_open(
                        self.call_context,
                        out.as_mut_ptr(),
                        algorithm,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// Reset one streaming digest context to its initial state.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the crypto feature is unavailable.
    /// Uses OpenSSL EVP digest primitives on Unix and Windows.
    ///
    /// # Errors
    /// Returns invalidArgument, ioInvalidData, notSupported.
    ///
    /// # Security
    /// Requires `crypto.digest`.
    ///
    /// # Replay
    /// External, nonrecordable.
    pub(crate) fn destack_crypto_digest_reset(
        &mut self,
        handle: resource::CryptoDigestHandle,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                crypto_vm::destack_crypto_digest_reset(self.call_context, context, handle)
            }
            None => unsafe {
                crypto_native::destack_crypto_digest_reset(self.call_context, handle)
            },
        }
    }

    /// Update one streaming digest context.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the crypto feature is unavailable.
    /// Uses OpenSSL EVP digest primitives on Unix and Windows.
    ///
    /// # Errors
    /// Returns invalidArgument, ioInvalidData, notSupported.
    ///
    /// # Security
    /// Requires `crypto.digest`.
    ///
    /// # Replay
    /// External, nonrecordable.
    pub(crate) fn destack_crypto_digest_update(
        &mut self,
        handle: resource::CryptoDigestHandle,
        argument_payload: HarnessValue<NativeSlice<u8>, VmSlice<u8>>,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let argument_payload = argument_payload.into_vm("argument_payload")?;
                crypto_vm::destack_crypto_digest_update(
                    self.call_context,
                    context,
                    handle,
                    argument_payload,
                )
            }
            None => {
                let argument_payload = argument_payload.into_native("argument_payload")?;
                unsafe {
                    crypto_native::destack_crypto_digest_update(
                        self.call_context,
                        handle,
                        argument_payload,
                    )
                }
            }
        }
    }

    /// Derive one key with Argon2id.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the crypto feature is unavailable.
    /// Uses OpenSSL KDF primitives on Unix and Windows.
    ///
    /// # Errors
    /// Returns invalidArgument, ioInvalidData, notSupported.
    ///
    /// # Security
    /// Requires `crypto.kdf`.
    ///
    /// # Replay
    /// External, nonrecordable.
    pub(crate) fn destack_crypto_kdf_argon2id(
        &mut self,
        request: HarnessValue<CryptoArgon2idRequest, CryptoArgon2idRequestVm>,
    ) -> RuntimeResult<HarnessValue<NativeSlice<u8>, VmSlice<u8>>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let request = request.into_vm("request")?;
                let out =
                    crypto_vm::destack_crypto_kdf_argon2id(self.call_context, context, request)?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let request = request.into_native("request")?;
                let mut out = std::mem::MaybeUninit::<NativeSlice<u8>>::uninit();
                unsafe {
                    crypto_native::destack_crypto_kdf_argon2id(
                        self.call_context,
                        out.as_mut_ptr(),
                        request,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Derive one key with HKDF.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the crypto feature is unavailable.
    /// Uses OpenSSL KDF primitives on Unix and Windows.
    ///
    /// # Errors
    /// Returns invalidArgument, ioInvalidData, notSupported.
    ///
    /// # Security
    /// Requires `crypto.kdf`.
    ///
    /// # Replay
    /// External, nonrecordable.
    pub(crate) fn destack_crypto_kdf_hkdf(
        &mut self,
        request: HarnessValue<CryptoHkdfRequest, CryptoHkdfRequestVm>,
    ) -> RuntimeResult<HarnessValue<NativeSlice<u8>, VmSlice<u8>>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let request = request.into_vm("request")?;
                let out = crypto_vm::destack_crypto_kdf_hkdf(self.call_context, context, request)?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let request = request.into_native("request")?;
                let mut out = std::mem::MaybeUninit::<NativeSlice<u8>>::uninit();
                unsafe {
                    crypto_native::destack_crypto_kdf_hkdf(
                        self.call_context,
                        out.as_mut_ptr(),
                        request,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Derive one key with PBKDF2.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the crypto feature is unavailable.
    /// Uses OpenSSL KDF primitives on Unix and Windows.
    ///
    /// # Errors
    /// Returns invalidArgument, ioInvalidData, notSupported.
    ///
    /// # Security
    /// Requires `crypto.kdf`.
    ///
    /// # Replay
    /// External, nonrecordable.
    pub(crate) fn destack_crypto_kdf_pbkdf2(
        &mut self,
        request: HarnessValue<CryptoPbkdf2Request, CryptoPbkdf2RequestVm>,
    ) -> RuntimeResult<HarnessValue<NativeSlice<u8>, VmSlice<u8>>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let request = request.into_vm("request")?;
                let out =
                    crypto_vm::destack_crypto_kdf_pbkdf2(self.call_context, context, request)?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let request = request.into_native("request")?;
                let mut out = std::mem::MaybeUninit::<NativeSlice<u8>>::uninit();
                unsafe {
                    crypto_native::destack_crypto_kdf_pbkdf2(
                        self.call_context,
                        out.as_mut_ptr(),
                        request,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Derive one key with scrypt.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the crypto feature is unavailable.
    /// Uses OpenSSL KDF primitives on Unix and Windows.
    ///
    /// # Errors
    /// Returns invalidArgument, ioInvalidData, notSupported.
    ///
    /// # Security
    /// Requires `crypto.kdf`.
    ///
    /// # Replay
    /// External, nonrecordable.
    pub(crate) fn destack_crypto_kdf_scrypt(
        &mut self,
        request: HarnessValue<CryptoScryptRequest, CryptoScryptRequestVm>,
    ) -> RuntimeResult<HarnessValue<NativeSlice<u8>, VmSlice<u8>>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let request = request.into_vm("request")?;
                let out =
                    crypto_vm::destack_crypto_kdf_scrypt(self.call_context, context, request)?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let request = request.into_native("request")?;
                let mut out = std::mem::MaybeUninit::<NativeSlice<u8>>::uninit();
                unsafe {
                    crypto_native::destack_crypto_kdf_scrypt(
                        self.call_context,
                        out.as_mut_ptr(),
                        request,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Decrypt one payload with one asymmetric key.
    ///
    /// Decrypt one payload using one store-backed private key.
    /// Padding and label semantics are controlled by encryption parameters.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the crypto feature is unavailable.
    /// Uses OpenSSL software key-management primitives and host key stores: Security.framework on Apple, CNG on Windows, and Android keystore callbacks when configured for hardware-backed storage.
    ///
    /// # Errors
    /// Returns invalidArgument, ioPermissionDenied, ioInvalidData, notSupported.
    ///
    /// # Security
    /// Requires `crypto.key.decrypt`.
    ///
    /// # Replay
    /// External, nonrecordable.
    pub(crate) fn destack_crypto_key_decrypt(
        &mut self,
        handle: resource::CryptoKeyHandle,
        parameters: HarnessValue<
            CryptoAsymmetricEncryptionParameters,
            CryptoAsymmetricEncryptionParametersVm,
        >,
        argument_payload: HarnessValue<NativeSlice<u8>, VmSlice<u8>>,
    ) -> RuntimeResult<HarnessValue<NativeSlice<u8>, VmSlice<u8>>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let parameters = parameters.into_vm("parameters")?;
                let argument_payload = argument_payload.into_vm("argument_payload")?;
                let out = crypto_vm::destack_crypto_key_decrypt(
                    self.call_context,
                    context,
                    handle,
                    parameters,
                    argument_payload,
                )?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let parameters = parameters.into_native("parameters")?;
                let argument_payload = argument_payload.into_native("argument_payload")?;
                let mut out = std::mem::MaybeUninit::<NativeSlice<u8>>::uninit();
                unsafe {
                    crypto_native::destack_crypto_key_decrypt(
                        self.call_context,
                        out.as_mut_ptr(),
                        handle,
                        parameters,
                        argument_payload,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Delete one key object.
    ///
    /// Delete one store-backed key object and invalidate this handle.
    /// Deletion permissions and persistence policies are enforced by runtime store policy.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the crypto feature is unavailable.
    /// Uses OpenSSL software key-management primitives and host key stores: Security.framework on Apple, CNG on Windows, and Android keystore callbacks when configured for hardware-backed storage.
    ///
    /// # Errors
    /// Returns invalidArgument, ioPermissionDenied, ioInvalidData, notSupported.
    ///
    /// # Security
    /// Requires `crypto.store.write`.
    ///
    /// # Replay
    /// External, nonrecordable.
    pub(crate) fn destack_crypto_key_delete(
        &mut self,
        handle: resource::CryptoKeyHandle,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                crypto_vm::destack_crypto_key_delete(self.call_context, context, handle)
            }
            None => unsafe { crypto_native::destack_crypto_key_delete(self.call_context, handle) },
        }
    }

    /// Return one key descriptor.
    ///
    /// Query one key object and return normalized metadata fields.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the crypto feature is unavailable.
    /// Uses OpenSSL software key-management primitives and host key stores: Security.framework on Apple, CNG on Windows, and Android keystore callbacks when configured for hardware-backed storage.
    ///
    /// # Errors
    /// Returns invalidArgument, ioPermissionDenied, ioInvalidData, notSupported.
    ///
    /// # Security
    /// Requires `crypto.store.read`.
    ///
    /// # Replay
    /// External, nonrecordable.
    pub(crate) fn destack_crypto_key_descriptor(
        &mut self,
        handle: resource::CryptoKeyHandle,
    ) -> RuntimeResult<HarnessValue<CryptoKeyDescriptor, CryptoKeyDescriptorVm>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out =
                    crypto_vm::destack_crypto_key_descriptor(self.call_context, context, handle)?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let mut out = std::mem::MaybeUninit::<CryptoKeyDescriptor>::uninit();
                unsafe {
                    crypto_native::destack_crypto_key_descriptor(
                        self.call_context,
                        out.as_mut_ptr(),
                        handle,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Encrypt one payload with one asymmetric key.
    ///
    /// Encrypt one payload using one store-backed public key.
    /// Padding and label semantics are controlled by encryption parameters.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the crypto feature is unavailable.
    /// Uses OpenSSL software key-management primitives and host key stores: Security.framework on Apple, CNG on Windows, and Android keystore callbacks when configured for hardware-backed storage.
    ///
    /// # Errors
    /// Returns invalidArgument, ioPermissionDenied, ioInvalidData, notSupported.
    ///
    /// # Security
    /// Requires `crypto.key.encrypt`.
    ///
    /// # Replay
    /// External, nonrecordable.
    pub(crate) fn destack_crypto_key_encrypt(
        &mut self,
        handle: resource::CryptoKeyHandle,
        parameters: HarnessValue<
            CryptoAsymmetricEncryptionParameters,
            CryptoAsymmetricEncryptionParametersVm,
        >,
        argument_payload: HarnessValue<NativeSlice<u8>, VmSlice<u8>>,
    ) -> RuntimeResult<HarnessValue<NativeSlice<u8>, VmSlice<u8>>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let parameters = parameters.into_vm("parameters")?;
                let argument_payload = argument_payload.into_vm("argument_payload")?;
                let out = crypto_vm::destack_crypto_key_encrypt(
                    self.call_context,
                    context,
                    handle,
                    parameters,
                    argument_payload,
                )?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let parameters = parameters.into_native("parameters")?;
                let argument_payload = argument_payload.into_native("argument_payload")?;
                let mut out = std::mem::MaybeUninit::<NativeSlice<u8>>::uninit();
                unsafe {
                    crypto_native::destack_crypto_key_encrypt(
                        self.call_context,
                        out.as_mut_ptr(),
                        handle,
                        parameters,
                        argument_payload,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Export one private key.
    ///
    /// Export one private key representation in the requested encoding format.
    /// Output format and passphrase are provided by `CryptoPrivateKeyExportRequest`.
    /// Encrypted PKCS#8 output requires one non-empty passphrase.
    /// The operation fails when store policy marks this key as non-exportable.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the crypto feature is unavailable.
    /// Uses OpenSSL software key-management primitives and host key stores: Security.framework on Apple, CNG on Windows, and Android keystore callbacks when configured for hardware-backed storage.
    ///
    /// # Errors
    /// Returns invalidArgument, ioPermissionDenied, ioInvalidData, notSupported.
    ///
    /// # Security
    /// Requires `crypto.store.read`.
    ///
    /// # Replay
    /// External, nonrecordable.
    pub(crate) fn destack_crypto_key_export_private(
        &mut self,
        handle: resource::CryptoKeyHandle,
        request: HarnessValue<CryptoPrivateKeyExportRequest, CryptoPrivateKeyExportRequestVm>,
    ) -> RuntimeResult<HarnessValue<NativeSlice<u8>, VmSlice<u8>>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let request = request.into_vm("request")?;
                let out = crypto_vm::destack_crypto_key_export_private(
                    self.call_context,
                    context,
                    handle,
                    request,
                )?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let request = request.into_native("request")?;
                let mut out = std::mem::MaybeUninit::<NativeSlice<u8>>::uninit();
                unsafe {
                    crypto_native::destack_crypto_key_export_private(
                        self.call_context,
                        out.as_mut_ptr(),
                        handle,
                        request,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Export one public key.
    ///
    /// Export one public key representation in the requested encoding format.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the crypto feature is unavailable.
    /// Uses OpenSSL software key-management primitives and host key stores: Security.framework on Apple, CNG on Windows, and Android keystore callbacks when configured for hardware-backed storage.
    ///
    /// # Errors
    /// Returns invalidArgument, ioPermissionDenied, ioInvalidData, notSupported.
    ///
    /// # Security
    /// Requires `crypto.store.read`.
    ///
    /// # Replay
    /// External, nonrecordable.
    pub(crate) fn destack_crypto_key_export_public(
        &mut self,
        handle: resource::CryptoKeyHandle,
        format: CryptoKeyFormat,
    ) -> RuntimeResult<HarnessValue<NativeSlice<u8>, VmSlice<u8>>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = crypto_vm::destack_crypto_key_export_public(
                    self.call_context,
                    context,
                    handle,
                    format,
                )?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let mut out = std::mem::MaybeUninit::<NativeSlice<u8>>::uninit();
                unsafe {
                    crypto_native::destack_crypto_key_export_public(
                        self.call_context,
                        out.as_mut_ptr(),
                        handle,
                        format,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Export one secret key.
    ///
    /// Export one symmetric or raw-secret key representation in the requested encoding format.
    /// The operation fails when store policy marks this key as non-exportable.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the crypto feature is unavailable.
    /// Uses OpenSSL software key-management primitives and host key stores: Security.framework on Apple, CNG on Windows, and Android keystore callbacks when configured for hardware-backed storage.
    ///
    /// # Errors
    /// Returns invalidArgument, ioPermissionDenied, ioInvalidData, notSupported.
    ///
    /// # Security
    /// Requires `crypto.store.read`.
    ///
    /// # Replay
    /// External, nonrecordable.
    pub(crate) fn destack_crypto_key_export_secret(
        &mut self,
        handle: resource::CryptoKeyHandle,
        format: CryptoKeyFormat,
    ) -> RuntimeResult<HarnessValue<NativeSlice<u8>, VmSlice<u8>>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = crypto_vm::destack_crypto_key_export_secret(
                    self.call_context,
                    context,
                    handle,
                    format,
                )?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let mut out = std::mem::MaybeUninit::<NativeSlice<u8>>::uninit();
                unsafe {
                    crypto_native::destack_crypto_key_export_secret(
                        self.call_context,
                        out.as_mut_ptr(),
                        handle,
                        format,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Generate one asymmetric key pair.
    ///
    /// Create one store-backed asymmetric key pair and return public and private handles.
    /// Generation policy and persistence semantics follow runtime store behavior.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the crypto feature is unavailable.
    /// Uses OpenSSL software key-management primitives and host key stores: Security.framework on Apple, CNG on Windows, and Android keystore callbacks when configured for hardware-backed storage.
    ///
    /// # Errors
    /// Returns invalidArgument, ioPermissionDenied, ioInvalidData, notSupported.
    ///
    /// # Security
    /// Requires `crypto.key.generate`.
    ///
    /// # Replay
    /// External, nonrecordable.
    pub(crate) fn destack_crypto_key_generate_pair(
        &mut self,
        store: resource::CryptoStoreHandle,
        request: HarnessValue<CryptoKeyGenerationRequest, CryptoKeyGenerationRequestVm>,
    ) -> RuntimeResult<HarnessValue<CryptoKeyPair, CryptoKeyPairVm>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let request = request.into_vm("request")?;
                let out = crypto_vm::destack_crypto_key_generate_pair(
                    self.call_context,
                    context,
                    store,
                    request,
                )?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let request = request.into_native("request")?;
                let mut out = std::mem::MaybeUninit::<CryptoKeyPair>::uninit();
                unsafe {
                    crypto_native::destack_crypto_key_generate_pair(
                        self.call_context,
                        out.as_mut_ptr(),
                        store,
                        request,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Generate one symmetric key.
    ///
    /// Create one store-backed secret key object.
    /// Generation policy and persistence semantics follow runtime store behavior.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the crypto feature is unavailable.
    /// Uses OpenSSL software key-management primitives and host key stores: Security.framework on Apple, CNG on Windows, and Android keystore callbacks when configured for hardware-backed storage.
    /// Hardware-backed secret-key generation is available when the selected host store exposes symmetric hardware-key callbacks.
    ///
    /// # Errors
    /// Returns invalidArgument, ioPermissionDenied, ioInvalidData, notSupported.
    ///
    /// # Security
    /// Requires `crypto.key.generate`.
    ///
    /// # Replay
    /// External, nonrecordable.
    pub(crate) fn destack_crypto_key_generate_secret(
        &mut self,
        store: resource::CryptoStoreHandle,
        request: HarnessValue<CryptoKeyGenerationRequest, CryptoKeyGenerationRequestVm>,
    ) -> RuntimeResult<resource::CryptoKeyHandle> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let request = request.into_vm("request")?;
                let out = crypto_vm::destack_crypto_key_generate_secret(
                    self.call_context,
                    context,
                    store,
                    request,
                )?;
                Ok(out)
            }
            None => {
                let request = request.into_native("request")?;
                let mut out = std::mem::MaybeUninit::<resource::CryptoKeyHandle>::uninit();
                unsafe {
                    crypto_native::destack_crypto_key_generate_secret(
                        self.call_context,
                        out.as_mut_ptr(),
                        store,
                        request,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// Import one key object.
    ///
    /// Parse and import one key blob into one store.
    /// Key visibility and persistence follow runtime store policies.
    /// Encrypted PKCS#8 inputs require one non-empty `request.passphrase`.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the crypto feature is unavailable.
    /// Uses OpenSSL software key-management primitives and host key stores: Security.framework on Apple, CNG on Windows, and Android keystore callbacks when configured for hardware-backed storage.
    ///
    /// # Errors
    /// Returns invalidArgument, ioPermissionDenied, ioInvalidData, notSupported.
    ///
    /// # Security
    /// Requires `crypto.store.write`.
    ///
    /// # Replay
    /// External, nonrecordable.
    pub(crate) fn destack_crypto_key_import(
        &mut self,
        store: resource::CryptoStoreHandle,
        request: HarnessValue<CryptoKeyImportRequest, CryptoKeyImportRequestVm>,
    ) -> RuntimeResult<resource::CryptoKeyHandle> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let request = request.into_vm("request")?;
                let out = crypto_vm::destack_crypto_key_import(
                    self.call_context,
                    context,
                    store,
                    request,
                )?;
                Ok(out)
            }
            None => {
                let request = request.into_native("request")?;
                let mut out = std::mem::MaybeUninit::<resource::CryptoKeyHandle>::uninit();
                unsafe {
                    crypto_native::destack_crypto_key_import(
                        self.call_context,
                        out.as_mut_ptr(),
                        store,
                        request,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// Sign one payload.
    ///
    /// Produce one signature over one payload using one store-backed private key.
    /// Payload hashing behavior is controlled by signature parameters.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the crypto feature is unavailable.
    /// Uses OpenSSL software key-management primitives and host key stores: Security.framework on Apple, CNG on Windows, and Android keystore callbacks when configured for hardware-backed storage.
    ///
    /// # Errors
    /// Returns invalidArgument, ioPermissionDenied, ioInvalidData, notSupported.
    ///
    /// # Security
    /// Requires `crypto.key.sign`.
    ///
    /// # Replay
    /// External, nonrecordable.
    pub(crate) fn destack_crypto_key_sign(
        &mut self,
        handle: resource::CryptoKeyHandle,
        parameters: HarnessValue<CryptoSignatureParameters, CryptoSignatureParametersVm>,
        argument_payload: HarnessValue<NativeSlice<u8>, VmSlice<u8>>,
    ) -> RuntimeResult<HarnessValue<NativeSlice<u8>, VmSlice<u8>>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let parameters = parameters.into_vm("parameters")?;
                let argument_payload = argument_payload.into_vm("argument_payload")?;
                let out = crypto_vm::destack_crypto_key_sign(
                    self.call_context,
                    context,
                    handle,
                    parameters,
                    argument_payload,
                )?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let parameters = parameters.into_native("parameters")?;
                let argument_payload = argument_payload.into_native("argument_payload")?;
                let mut out = std::mem::MaybeUninit::<NativeSlice<u8>>::uninit();
                unsafe {
                    crypto_native::destack_crypto_key_sign(
                        self.call_context,
                        out.as_mut_ptr(),
                        handle,
                        parameters,
                        argument_payload,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Unwrap one key.
    ///
    /// Decrypt and import one wrapped key object into one store.
    /// Import semantics follow runtime store policy and the import request.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the crypto feature is unavailable.
    /// Uses OpenSSL software key-management primitives and host key stores: Security.framework on Apple, CNG on Windows, and Android keystore callbacks when configured for hardware-backed storage.
    ///
    /// # Errors
    /// Returns invalidArgument, ioPermissionDenied, ioInvalidData, notSupported.
    ///
    /// # Security
    /// Requires `crypto.key.unwrap`, `crypto.store.write`.
    ///
    /// # Replay
    /// External, nonrecordable.
    pub(crate) fn destack_crypto_key_unwrap(
        &mut self,
        store: resource::CryptoStoreHandle,
        wrappingkey: resource::CryptoKeyHandle,
        wrappedkey: HarnessValue<NativeSlice<u8>, VmSlice<u8>>,
        parameters: HarnessValue<CryptoKeyWrapParameters, CryptoKeyWrapParametersVm>,
        request: HarnessValue<CryptoKeyImportRequest, CryptoKeyImportRequestVm>,
    ) -> RuntimeResult<resource::CryptoKeyHandle> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let wrappedkey = wrappedkey.into_vm("wrappedkey")?;
                let parameters = parameters.into_vm("parameters")?;
                let request = request.into_vm("request")?;
                let out = crypto_vm::destack_crypto_key_unwrap(
                    self.call_context,
                    context,
                    store,
                    wrappingkey,
                    wrappedkey,
                    parameters,
                    request,
                )?;
                Ok(out)
            }
            None => {
                let wrappedkey = wrappedkey.into_native("wrappedkey")?;
                let parameters = parameters.into_native("parameters")?;
                let request = request.into_native("request")?;
                let mut out = std::mem::MaybeUninit::<resource::CryptoKeyHandle>::uninit();
                unsafe {
                    crypto_native::destack_crypto_key_unwrap(
                        self.call_context,
                        out.as_mut_ptr(),
                        store,
                        wrappingkey,
                        wrappedkey,
                        parameters,
                        request,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// Verify one signature.
    ///
    /// Verify one signature over one payload using one store-backed public key.
    /// Payload hashing behavior is controlled by signature parameters.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the crypto feature is unavailable.
    /// Uses OpenSSL software key-management primitives and host key stores: Security.framework on Apple, CNG on Windows, and Android keystore callbacks when configured for hardware-backed storage.
    ///
    /// # Errors
    /// Returns invalidArgument, ioPermissionDenied, ioInvalidData, notSupported.
    ///
    /// # Security
    /// Requires `crypto.key.verify`.
    ///
    /// # Replay
    /// External, nonrecordable.
    pub(crate) fn destack_crypto_key_verify(
        &mut self,
        handle: resource::CryptoKeyHandle,
        parameters: HarnessValue<CryptoSignatureParameters, CryptoSignatureParametersVm>,
        argument_payload: HarnessValue<NativeSlice<u8>, VmSlice<u8>>,
        signature: HarnessValue<NativeSlice<u8>, VmSlice<u8>>,
    ) -> RuntimeResult<bool> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let parameters = parameters.into_vm("parameters")?;
                let argument_payload = argument_payload.into_vm("argument_payload")?;
                let signature = signature.into_vm("signature")?;
                let out = crypto_vm::destack_crypto_key_verify(
                    self.call_context,
                    context,
                    handle,
                    parameters,
                    argument_payload,
                    signature,
                )?;
                Ok(out)
            }
            None => {
                let parameters = parameters.into_native("parameters")?;
                let argument_payload = argument_payload.into_native("argument_payload")?;
                let signature = signature.into_native("signature")?;
                let mut out = std::mem::MaybeUninit::<bool>::uninit();
                unsafe {
                    crypto_native::destack_crypto_key_verify(
                        self.call_context,
                        out.as_mut_ptr(),
                        handle,
                        parameters,
                        argument_payload,
                        signature,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// Wrap one key.
    ///
    /// Export and encrypt one key object under one wrapping key.
    /// Wrapping semantics are selected by `CryptoKeyWrapParameters`.
    /// `RsaOaep` uses asymmetric OAEP wrapping and `AesKw` or `AesKwp` use RFC 3394 or RFC 5649 key-wrap semantics.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the crypto feature is unavailable.
    /// Uses OpenSSL software key-management primitives and host key stores: Security.framework on Apple, CNG on Windows, and Android keystore callbacks when configured for hardware-backed storage.
    ///
    /// # Errors
    /// Returns invalidArgument, ioPermissionDenied, ioInvalidData, notSupported.
    ///
    /// # Security
    /// Requires `crypto.key.wrap`.
    ///
    /// # Replay
    /// External, nonrecordable.
    pub(crate) fn destack_crypto_key_wrap(
        &mut self,
        wrappingkey: resource::CryptoKeyHandle,
        keytowrap: resource::CryptoKeyHandle,
        format: CryptoKeyFormat,
        parameters: HarnessValue<CryptoKeyWrapParameters, CryptoKeyWrapParametersVm>,
    ) -> RuntimeResult<HarnessValue<NativeSlice<u8>, VmSlice<u8>>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let parameters = parameters.into_vm("parameters")?;
                let out = crypto_vm::destack_crypto_key_wrap(
                    self.call_context,
                    context,
                    wrappingkey,
                    keytowrap,
                    format,
                    parameters,
                )?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let parameters = parameters.into_native("parameters")?;
                let mut out = std::mem::MaybeUninit::<NativeSlice<u8>>::uninit();
                unsafe {
                    crypto_native::destack_crypto_key_wrap(
                        self.call_context,
                        out.as_mut_ptr(),
                        wrappingkey,
                        keytowrap,
                        format,
                        parameters,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Close one streaming MAC context.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the crypto feature is unavailable.
    /// Uses OpenSSL EVP MAC primitives for software keys, and host key APIs for host-managed secret keys when available.
    ///
    /// # Errors
    /// Returns invalidArgument, ioNotFound, notSupported.
    ///
    /// # Security
    /// Requires `crypto.mac`.
    ///
    /// # Replay
    /// External, nonrecordable.
    pub(crate) fn destack_crypto_mac_close(
        &mut self,
        handle: resource::CryptoMacHandle,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                crypto_vm::destack_crypto_mac_close(self.call_context, context, handle)
            }
            None => unsafe { crypto_native::destack_crypto_mac_close(self.call_context, handle) },
        }
    }

    /// Compute one message authentication code in one shot.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the crypto feature is unavailable.
    /// Uses OpenSSL EVP MAC primitives for software keys, and host key APIs for host-managed secret keys when available.
    ///
    /// # Errors
    /// Returns invalidArgument, ioPermissionDenied, ioInvalidData, notSupported.
    ///
    /// # Security
    /// Requires `crypto.mac`.
    ///
    /// # Replay
    /// External, nonrecordable.
    pub(crate) fn destack_crypto_mac_compute(
        &mut self,
        key: resource::CryptoKeyHandle,
        parameters: HarnessValue<CryptoMacParameters, CryptoMacParametersVm>,
        argument_payload: HarnessValue<NativeSlice<u8>, VmSlice<u8>>,
    ) -> RuntimeResult<HarnessValue<NativeSlice<u8>, VmSlice<u8>>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let parameters = parameters.into_vm("parameters")?;
                let argument_payload = argument_payload.into_vm("argument_payload")?;
                let out = crypto_vm::destack_crypto_mac_compute(
                    self.call_context,
                    context,
                    key,
                    parameters,
                    argument_payload,
                )?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let parameters = parameters.into_native("parameters")?;
                let argument_payload = argument_payload.into_native("argument_payload")?;
                let mut out = std::mem::MaybeUninit::<NativeSlice<u8>>::uninit();
                unsafe {
                    crypto_native::destack_crypto_mac_compute(
                        self.call_context,
                        out.as_mut_ptr(),
                        key,
                        parameters,
                        argument_payload,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Finalize one streaming MAC context and return one tag.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the crypto feature is unavailable.
    /// Uses OpenSSL EVP MAC primitives for software keys, and host key APIs for host-managed secret keys when available.
    ///
    /// # Errors
    /// Returns invalidArgument, ioInvalidData, notSupported.
    ///
    /// # Security
    /// Requires `crypto.mac`.
    ///
    /// # Replay
    /// External, nonrecordable.
    pub(crate) fn destack_crypto_mac_finish(
        &mut self,
        handle: resource::CryptoMacHandle,
    ) -> RuntimeResult<HarnessValue<NativeSlice<u8>, VmSlice<u8>>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = crypto_vm::destack_crypto_mac_finish(self.call_context, context, handle)?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let mut out = std::mem::MaybeUninit::<NativeSlice<u8>>::uninit();
                unsafe {
                    crypto_native::destack_crypto_mac_finish(
                        self.call_context,
                        out.as_mut_ptr(),
                        handle,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Open one streaming MAC context.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the crypto feature is unavailable.
    /// Uses OpenSSL EVP MAC primitives for software keys, and host key APIs for host-managed secret keys when available.
    ///
    /// # Errors
    /// Returns invalidArgument, ioPermissionDenied, ioInvalidData, notSupported.
    ///
    /// # Security
    /// Requires `crypto.mac`.
    ///
    /// # Replay
    /// External, nonrecordable.
    pub(crate) fn destack_crypto_mac_open(
        &mut self,
        key: resource::CryptoKeyHandle,
        parameters: HarnessValue<CryptoMacParameters, CryptoMacParametersVm>,
    ) -> RuntimeResult<resource::CryptoMacHandle> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let parameters = parameters.into_vm("parameters")?;
                let out = crypto_vm::destack_crypto_mac_open(
                    self.call_context,
                    context,
                    key,
                    parameters,
                )?;
                Ok(out)
            }
            None => {
                let parameters = parameters.into_native("parameters")?;
                let mut out = std::mem::MaybeUninit::<resource::CryptoMacHandle>::uninit();
                unsafe {
                    crypto_native::destack_crypto_mac_open(
                        self.call_context,
                        out.as_mut_ptr(),
                        key,
                        parameters,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// Reset one streaming MAC context to its initial state.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the crypto feature is unavailable.
    /// Uses OpenSSL EVP MAC primitives for software keys, and host key APIs for host-managed secret keys when available.
    ///
    /// # Errors
    /// Returns invalidArgument, ioInvalidData, notSupported.
    ///
    /// # Security
    /// Requires `crypto.mac`.
    ///
    /// # Replay
    /// External, nonrecordable.
    pub(crate) fn destack_crypto_mac_reset(
        &mut self,
        handle: resource::CryptoMacHandle,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                crypto_vm::destack_crypto_mac_reset(self.call_context, context, handle)
            }
            None => unsafe { crypto_native::destack_crypto_mac_reset(self.call_context, handle) },
        }
    }

    /// Update one streaming MAC context.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the crypto feature is unavailable.
    /// Uses OpenSSL EVP MAC primitives for software keys, and host key APIs for host-managed secret keys when available.
    ///
    /// # Errors
    /// Returns invalidArgument, ioInvalidData, notSupported.
    ///
    /// # Security
    /// Requires `crypto.mac`.
    ///
    /// # Replay
    /// External, nonrecordable.
    pub(crate) fn destack_crypto_mac_update(
        &mut self,
        handle: resource::CryptoMacHandle,
        argument_payload: HarnessValue<NativeSlice<u8>, VmSlice<u8>>,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let argument_payload = argument_payload.into_vm("argument_payload")?;
                crypto_vm::destack_crypto_mac_update(
                    self.call_context,
                    context,
                    handle,
                    argument_payload,
                )
            }
            None => {
                let argument_payload = argument_payload.into_native("argument_payload")?;
                unsafe {
                    crypto_native::destack_crypto_mac_update(
                        self.call_context,
                        handle,
                        argument_payload,
                    )
                }
            }
        }
    }

    /// Verify one message authentication code in one shot.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the crypto feature is unavailable.
    /// Uses OpenSSL EVP MAC primitives for software keys, and host key APIs for host-managed secret keys when available.
    ///
    /// # Errors
    /// Returns invalidArgument, ioPermissionDenied, ioInvalidData, notSupported.
    ///
    /// # Security
    /// Requires `crypto.mac`.
    ///
    /// # Replay
    /// External, nonrecordable.
    pub(crate) fn destack_crypto_mac_verify(
        &mut self,
        key: resource::CryptoKeyHandle,
        parameters: HarnessValue<CryptoMacParameters, CryptoMacParametersVm>,
        argument_payload: HarnessValue<NativeSlice<u8>, VmSlice<u8>>,
        tag: HarnessValue<NativeSlice<u8>, VmSlice<u8>>,
    ) -> RuntimeResult<bool> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let parameters = parameters.into_vm("parameters")?;
                let argument_payload = argument_payload.into_vm("argument_payload")?;
                let tag = tag.into_vm("tag")?;
                let out = crypto_vm::destack_crypto_mac_verify(
                    self.call_context,
                    context,
                    key,
                    parameters,
                    argument_payload,
                    tag,
                )?;
                Ok(out)
            }
            None => {
                let parameters = parameters.into_native("parameters")?;
                let argument_payload = argument_payload.into_native("argument_payload")?;
                let tag = tag.into_native("tag")?;
                let mut out = std::mem::MaybeUninit::<bool>::uninit();
                unsafe {
                    crypto_native::destack_crypto_mac_verify(
                        self.call_context,
                        out.as_mut_ptr(),
                        key,
                        parameters,
                        argument_payload,
                        tag,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// List supported key-agreement algorithms.
    ///
    /// Return key-agreement algorithms available through active host provider implementations.
    /// Results are capability snapshots and may vary across hosts and runtime builds.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the crypto feature is unavailable.
    /// Uses runtime crypto capability introspection over OpenSSL software providers and host key stores: Security.framework on Apple, CNG or Crypt32 on Windows, and Android keystore callbacks when registered.
    ///
    /// # Errors
    /// Returns ioInvalidData, notSupported.
    ///
    /// # Security
    /// Requires `crypto.probe`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_crypto_probe_agreement_algorithms(
        &mut self,
    ) -> RuntimeResult<
        HarnessValue<
            NativeSlice<CryptoKeyAgreementAlgorithm>,
            VmSlice<CryptoKeyAgreementAlgorithm>,
        >,
    > {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = crypto_vm::destack_crypto_probe_agreement_algorithms(
                    self.call_context,
                    context,
                )?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let mut out =
                    std::mem::MaybeUninit::<NativeSlice<CryptoKeyAgreementAlgorithm>>::uninit();
                unsafe {
                    crypto_native::destack_crypto_probe_agreement_algorithms(
                        self.call_context,
                        out.as_mut_ptr(),
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// List supported cipher algorithms.
    ///
    /// Return cipher algorithms available through active host provider implementations.
    /// Results are capability snapshots and may vary across hosts and runtime builds.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the crypto feature is unavailable.
    /// Uses runtime crypto capability introspection over OpenSSL software providers and host key stores: Security.framework on Apple, CNG or Crypt32 on Windows, and Android keystore callbacks when registered.
    ///
    /// # Errors
    /// Returns ioInvalidData, notSupported.
    ///
    /// # Security
    /// Requires `crypto.probe`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_crypto_probe_cipher_algorithms(
        &mut self,
    ) -> RuntimeResult<
        HarnessValue<NativeSlice<CryptoCipherAlgorithm>, VmSlice<CryptoCipherAlgorithm>>,
    > {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out =
                    crypto_vm::destack_crypto_probe_cipher_algorithms(self.call_context, context)?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let mut out = std::mem::MaybeUninit::<NativeSlice<CryptoCipherAlgorithm>>::uninit();
                unsafe {
                    crypto_native::destack_crypto_probe_cipher_algorithms(
                        self.call_context,
                        out.as_mut_ptr(),
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// List supported digest algorithms.
    ///
    /// Return digest algorithms available through active host provider implementations.
    /// Results are capability snapshots and may vary across hosts and runtime builds.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the crypto feature is unavailable.
    /// Uses runtime crypto capability introspection over OpenSSL software providers and host key stores: Security.framework on Apple, CNG or Crypt32 on Windows, and Android keystore callbacks when registered.
    ///
    /// # Errors
    /// Returns ioInvalidData, notSupported.
    ///
    /// # Security
    /// Requires `crypto.probe`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_crypto_probe_digest_algorithms(
        &mut self,
    ) -> RuntimeResult<
        HarnessValue<NativeSlice<CryptoDigestAlgorithm>, VmSlice<CryptoDigestAlgorithm>>,
    > {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out =
                    crypto_vm::destack_crypto_probe_digest_algorithms(self.call_context, context)?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let mut out = std::mem::MaybeUninit::<NativeSlice<CryptoDigestAlgorithm>>::uninit();
                unsafe {
                    crypto_native::destack_crypto_probe_digest_algorithms(
                        self.call_context,
                        out.as_mut_ptr(),
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// List supported KDF algorithms.
    ///
    /// Return key-derivation algorithms available through active host provider implementations.
    /// Results are capability snapshots and may vary across hosts and runtime builds.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the crypto feature is unavailable.
    /// Uses runtime crypto capability introspection over OpenSSL software providers and host key stores: Security.framework on Apple, CNG or Crypt32 on Windows, and Android keystore callbacks when registered.
    ///
    /// # Errors
    /// Returns ioInvalidData, notSupported.
    ///
    /// # Security
    /// Requires `crypto.probe`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_crypto_probe_kdf_algorithms(
        &mut self,
    ) -> RuntimeResult<HarnessValue<NativeSlice<CryptoKdfAlgorithm>, VmSlice<CryptoKdfAlgorithm>>>
    {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out =
                    crypto_vm::destack_crypto_probe_kdf_algorithms(self.call_context, context)?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let mut out = std::mem::MaybeUninit::<NativeSlice<CryptoKdfAlgorithm>>::uninit();
                unsafe {
                    crypto_native::destack_crypto_probe_kdf_algorithms(
                        self.call_context,
                        out.as_mut_ptr(),
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// List supported key algorithm families.
    ///
    /// Return the key algorithm families available through the active host provider set.
    /// Results are capability snapshots and may vary across hosts and runtime builds.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the crypto feature is unavailable.
    /// Uses runtime crypto capability introspection over OpenSSL software providers and host key stores: Security.framework on Apple, CNG or Crypt32 on Windows, and Android keystore callbacks when registered.
    ///
    /// # Errors
    /// Returns ioInvalidData, notSupported.
    ///
    /// # Security
    /// Requires `crypto.probe`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_crypto_probe_key_algorithms(
        &mut self,
    ) -> RuntimeResult<HarnessValue<NativeSlice<CryptoKeyAlgorithm>, VmSlice<CryptoKeyAlgorithm>>>
    {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out =
                    crypto_vm::destack_crypto_probe_key_algorithms(self.call_context, context)?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let mut out = std::mem::MaybeUninit::<NativeSlice<CryptoKeyAlgorithm>>::uninit();
                unsafe {
                    crypto_native::destack_crypto_probe_key_algorithms(
                        self.call_context,
                        out.as_mut_ptr(),
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// List supported key formats.
    ///
    /// Return the key encoding formats supported by active host provider implementations.
    /// Results are capability snapshots and may vary across hosts and runtime builds.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the crypto feature is unavailable.
    /// Uses runtime crypto capability introspection over OpenSSL software providers and host key stores: Security.framework on Apple, CNG or Crypt32 on Windows, and Android keystore callbacks when registered.
    ///
    /// # Errors
    /// Returns ioInvalidData, notSupported.
    ///
    /// # Security
    /// Requires `crypto.probe`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_crypto_probe_key_formats(
        &mut self,
    ) -> RuntimeResult<HarnessValue<NativeSlice<CryptoKeyFormat>, VmSlice<CryptoKeyFormat>>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = crypto_vm::destack_crypto_probe_key_formats(self.call_context, context)?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let mut out = std::mem::MaybeUninit::<NativeSlice<CryptoKeyFormat>>::uninit();
                unsafe {
                    crypto_native::destack_crypto_probe_key_formats(
                        self.call_context,
                        out.as_mut_ptr(),
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// List supported key residencies.
    ///
    /// Return key residencies available through active host provider implementations.
    /// Results are capability snapshots and may vary across hosts and runtime builds.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the crypto feature is unavailable.
    /// Uses runtime crypto capability introspection over OpenSSL software providers and host key stores: Security.framework on Apple, CNG or Crypt32 on Windows, and Android keystore callbacks when registered.
    ///
    /// # Errors
    /// Returns ioInvalidData, notSupported.
    ///
    /// # Security
    /// Requires `crypto.probe`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_crypto_probe_key_residencies(
        &mut self,
    ) -> RuntimeResult<HarnessValue<NativeSlice<CryptoKeyResidency>, VmSlice<CryptoKeyResidency>>>
    {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out =
                    crypto_vm::destack_crypto_probe_key_residencies(self.call_context, context)?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let mut out = std::mem::MaybeUninit::<NativeSlice<CryptoKeyResidency>>::uninit();
                unsafe {
                    crypto_native::destack_crypto_probe_key_residencies(
                        self.call_context,
                        out.as_mut_ptr(),
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// List supported key-wrap algorithms.
    ///
    /// Return key-wrap algorithms available through active host provider implementations.
    /// Results are capability snapshots and may vary across hosts and runtime builds.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the crypto feature is unavailable.
    /// Uses runtime crypto capability introspection over OpenSSL software providers and host key stores: Security.framework on Apple, CNG or Crypt32 on Windows, and Android keystore callbacks when registered.
    ///
    /// # Errors
    /// Returns ioInvalidData, notSupported.
    ///
    /// # Security
    /// Requires `crypto.probe`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_crypto_probe_key_wrap_algorithms(
        &mut self,
    ) -> RuntimeResult<
        HarnessValue<NativeSlice<CryptoKeyWrapAlgorithm>, VmSlice<CryptoKeyWrapAlgorithm>>,
    > {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = crypto_vm::destack_crypto_probe_key_wrap_algorithms(
                    self.call_context,
                    context,
                )?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let mut out =
                    std::mem::MaybeUninit::<NativeSlice<CryptoKeyWrapAlgorithm>>::uninit();
                unsafe {
                    crypto_native::destack_crypto_probe_key_wrap_algorithms(
                        self.call_context,
                        out.as_mut_ptr(),
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// List supported MAC algorithms.
    ///
    /// Return message-authentication algorithms available through active host providers.
    /// Results are capability snapshots and may vary across hosts and runtime builds.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the crypto feature is unavailable.
    /// Uses runtime crypto capability introspection over OpenSSL software providers and host key stores: Security.framework on Apple, CNG or Crypt32 on Windows, and Android keystore callbacks when registered.
    ///
    /// # Errors
    /// Returns ioInvalidData, notSupported.
    ///
    /// # Security
    /// Requires `crypto.probe`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_crypto_probe_mac_algorithms(
        &mut self,
    ) -> RuntimeResult<HarnessValue<NativeSlice<CryptoMacAlgorithm>, VmSlice<CryptoMacAlgorithm>>>
    {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out =
                    crypto_vm::destack_crypto_probe_mac_algorithms(self.call_context, context)?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let mut out = std::mem::MaybeUninit::<NativeSlice<CryptoMacAlgorithm>>::uninit();
                unsafe {
                    crypto_native::destack_crypto_probe_mac_algorithms(
                        self.call_context,
                        out.as_mut_ptr(),
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// List supported named curves.
    ///
    /// Return elliptic-curve families available through active host provider implementations.
    /// Results are capability snapshots and may vary across hosts and runtime builds.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the crypto feature is unavailable.
    /// Uses runtime crypto capability introspection over OpenSSL software providers and host key stores: Security.framework on Apple, CNG or Crypt32 on Windows, and Android keystore callbacks when registered.
    ///
    /// # Errors
    /// Returns ioInvalidData, notSupported.
    ///
    /// # Security
    /// Requires `crypto.probe`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_crypto_probe_named_curves(
        &mut self,
    ) -> RuntimeResult<HarnessValue<NativeSlice<CryptoNamedCurve>, VmSlice<CryptoNamedCurve>>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = crypto_vm::destack_crypto_probe_named_curves(self.call_context, context)?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let mut out = std::mem::MaybeUninit::<NativeSlice<CryptoNamedCurve>>::uninit();
                unsafe {
                    crypto_native::destack_crypto_probe_named_curves(
                        self.call_context,
                        out.as_mut_ptr(),
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// List supported signature algorithms.
    ///
    /// Return signature algorithms available through active host provider implementations.
    /// Results are capability snapshots and may vary across hosts and runtime builds.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the crypto feature is unavailable.
    /// Uses runtime crypto capability introspection over OpenSSL software providers and host key stores: Security.framework on Apple, CNG or Crypt32 on Windows, and Android keystore callbacks when registered.
    ///
    /// # Errors
    /// Returns ioInvalidData, notSupported.
    ///
    /// # Security
    /// Requires `crypto.probe`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_crypto_probe_signature_algorithms(
        &mut self,
    ) -> RuntimeResult<
        HarnessValue<NativeSlice<CryptoSignatureAlgorithm>, VmSlice<CryptoSignatureAlgorithm>>,
    > {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = crypto_vm::destack_crypto_probe_signature_algorithms(
                    self.call_context,
                    context,
                )?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let mut out =
                    std::mem::MaybeUninit::<NativeSlice<CryptoSignatureAlgorithm>>::uninit();
                unsafe {
                    crypto_native::destack_crypto_probe_signature_algorithms(
                        self.call_context,
                        out.as_mut_ptr(),
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Allocate one random byte vector with the requested length.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the crypto feature is unavailable.
    /// Uses OpenSSL RAND primitives backed by host entropy sources on Unix and Windows.
    ///
    /// # Errors
    /// Returns invalidArgument, ioWouldBlock, ioInvalidData, notSupported.
    ///
    /// # Security
    /// Requires `crypto.random`.
    ///
    /// # Replay
    /// External, nonrecordable.
    pub(crate) fn destack_crypto_random_bytes(
        &mut self,
        length: u32,
    ) -> RuntimeResult<HarnessValue<NativeSlice<u8>, VmSlice<u8>>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out =
                    crypto_vm::destack_crypto_random_bytes(self.call_context, context, length)?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let mut out = std::mem::MaybeUninit::<NativeSlice<u8>>::uninit();
                unsafe {
                    crypto_native::destack_crypto_random_bytes(
                        self.call_context,
                        out.as_mut_ptr(),
                        length,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Fill one mutable byte slice with cryptographically secure random bytes.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the crypto feature is unavailable.
    /// Uses OpenSSL RAND primitives backed by host entropy sources on Unix and Windows.
    ///
    /// # Errors
    /// Returns invalidArgument, ioWouldBlock, ioInvalidData, notSupported.
    ///
    /// # Security
    /// Requires `crypto.random`.
    ///
    /// # Replay
    /// External, nonrecordable.
    pub(crate) fn destack_crypto_random_fill(
        &mut self,
        buffer: HarnessValue<NativeSlice<u8>, VmSlice<u8>>,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let buffer = buffer.into_vm("buffer")?;
                crypto_vm::destack_crypto_random_fill(self.call_context, context, buffer)
            }
            None => {
                let buffer = buffer.into_native("buffer")?;
                unsafe { crypto_native::destack_crypto_random_fill(self.call_context, buffer) }
            }
        }
    }

    /// Close one crypto store.
    ///
    /// Release one runtime crypto store handle.
    /// Open key and certificate handles remain valid according to runtime store lifetime rules.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the crypto feature is unavailable.
    /// Uses runtime crypto store primitives over OpenSSL software providers and host stores: Security.framework keychain and trust stores on Apple, CNG and Crypt32 stores on Windows, and Android software providers plus keystore callbacks when host callbacks are configured.
    /// Operations may return `notSupported` when host stores are unavailable.
    ///
    /// # Errors
    /// Returns invalidArgument, ioNotFound, notSupported.
    ///
    /// # Security
    /// Requires `crypto.store.read`.
    ///
    /// # Replay
    /// External, nonrecordable.
    pub(crate) fn destack_crypto_store_close(
        &mut self,
        handle: resource::CryptoStoreHandle,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                crypto_vm::destack_crypto_store_close(self.call_context, context, handle)
            }
            None => unsafe { crypto_native::destack_crypto_store_close(self.call_context, handle) },
        }
    }

    /// List certificates from one store.
    ///
    /// Enumerate certificate entries that match one query selector.
    /// Result ordering and visibility follow runtime store policies and caller permissions.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the crypto feature is unavailable.
    /// Uses runtime crypto store primitives over OpenSSL software providers and host stores: Security.framework keychain and trust stores on Apple, CNG and Crypt32 stores on Windows, and Android software providers plus keystore callbacks when host callbacks are configured.
    /// Operations may return `notSupported` when host stores are unavailable.
    ///
    /// # Errors
    /// Returns invalidArgument, ioPermissionDenied, ioInvalidData, notSupported.
    ///
    /// # Security
    /// Requires `crypto.store.read`.
    ///
    /// # Replay
    /// External, nonrecordable.
    pub(crate) fn destack_crypto_store_list_certificates(
        &mut self,
        handle: resource::CryptoStoreHandle,
        query: HarnessValue<CryptoCertificateQuery, CryptoCertificateQueryVm>,
    ) -> RuntimeResult<HarnessValue<CryptoCertificateListPage, CryptoCertificateListPageVm>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let query = query.into_vm("query")?;
                let out = crypto_vm::destack_crypto_store_list_certificates(
                    self.call_context,
                    context,
                    handle,
                    query,
                )?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let query = query.into_native("query")?;
                let mut out = std::mem::MaybeUninit::<CryptoCertificateListPage>::uninit();
                unsafe {
                    crypto_native::destack_crypto_store_list_certificates(
                        self.call_context,
                        out.as_mut_ptr(),
                        handle,
                        query,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// List keys from one store.
    ///
    /// Enumerate key entries that match one query selector.
    /// Result ordering and visibility follow runtime store policies and caller permissions.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the crypto feature is unavailable.
    /// Uses runtime crypto store primitives over OpenSSL software providers and host stores: Security.framework keychain and trust stores on Apple, CNG and Crypt32 stores on Windows, and Android software providers plus keystore callbacks when host callbacks are configured.
    /// Operations may return `notSupported` when host stores are unavailable.
    ///
    /// # Errors
    /// Returns invalidArgument, ioPermissionDenied, ioInvalidData, notSupported.
    ///
    /// # Security
    /// Requires `crypto.store.read`.
    ///
    /// # Replay
    /// External, nonrecordable.
    pub(crate) fn destack_crypto_store_list_keys(
        &mut self,
        handle: resource::CryptoStoreHandle,
        query: HarnessValue<CryptoKeyQuery, CryptoKeyQueryVm>,
    ) -> RuntimeResult<HarnessValue<CryptoKeyListPage, CryptoKeyListPageVm>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let query = query.into_vm("query")?;
                let out = crypto_vm::destack_crypto_store_list_keys(
                    self.call_context,
                    context,
                    handle,
                    query,
                )?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let query = query.into_native("query")?;
                let mut out = std::mem::MaybeUninit::<CryptoKeyListPage>::uninit();
                unsafe {
                    crypto_native::destack_crypto_store_list_keys(
                        self.call_context,
                        out.as_mut_ptr(),
                        handle,
                        query,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Open one crypto store.
    ///
    /// Create one runtime crypto store handle for key and certificate operations.
    /// Provider selection and access scope follow runtime crypto store semantics.
    /// `Ephemeral` and `Provider` store support is required.
    /// Host-backed `System`, `User`, and `Machine` support is host dependent.
    /// Host-backed stores may expose certificate reads while rejecting key or certificate writes.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the crypto feature is unavailable.
    /// Uses runtime crypto store primitives over OpenSSL software providers and host stores: Security.framework keychain and trust stores on Apple, CNG and Crypt32 stores on Windows, and Android software providers plus keystore callbacks when host callbacks are configured.
    /// Operations may return `notSupported` when host stores are unavailable.
    ///
    /// # Errors
    /// Returns invalidArgument, ioPermissionDenied, ioInvalidData, notSupported.
    ///
    /// # Security
    /// Requires `crypto.store.read`.
    ///
    /// # Replay
    /// External, nonrecordable.
    pub(crate) fn destack_crypto_store_open(
        &mut self,
        options: HarnessValue<CryptoStoreOptions, CryptoStoreOptionsVm>,
    ) -> RuntimeResult<resource::CryptoStoreHandle> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let options = options.into_vm("options")?;
                let out =
                    crypto_vm::destack_crypto_store_open(self.call_context, context, options)?;
                Ok(out)
            }
            None => {
                let options = options.into_native("options")?;
                let mut out = std::mem::MaybeUninit::<resource::CryptoStoreHandle>::uninit();
                unsafe {
                    crypto_native::destack_crypto_store_open(
                        self.call_context,
                        out.as_mut_ptr(),
                        options,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// Return capabilities for one store backend identity.
    ///
    /// Query one store kind and optional provider and return effective capability policy.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the crypto feature is unavailable.
    /// Uses runtime crypto store capability introspection over OpenSSL software providers and host stores: Security.framework keychain and trust stores on Apple, CNG and Crypt32 stores on Windows, and Android software providers plus keystore callbacks when host callbacks are configured.
    ///
    /// # Errors
    /// Returns invalidArgument, ioInvalidData, notSupported.
    ///
    /// # Security
    /// Requires `crypto.probe`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_crypto_store_probe_capability(
        &mut self,
        kind: CryptoStoreKind,
        provider: CryptoStoreProvider,
    ) -> RuntimeResult<HarnessValue<CryptoStoreCapability, CryptoStoreCapabilityVm>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = crypto_vm::destack_crypto_store_probe_capability(
                    self.call_context,
                    context,
                    kind,
                    provider,
                )?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let mut out = std::mem::MaybeUninit::<CryptoStoreCapability>::uninit();
                unsafe {
                    crypto_native::destack_crypto_store_probe_capability(
                        self.call_context,
                        out.as_mut_ptr(),
                        kind,
                        provider,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// List store backend kinds that are currently available.
    ///
    /// Return one runtime capability snapshot for store backends that can be opened.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the crypto feature is unavailable.
    /// Uses runtime crypto store capability introspection over OpenSSL software providers and host stores: Security.framework keychain and trust stores on Apple, CNG and Crypt32 stores on Windows, and Android software providers plus keystore callbacks when host callbacks are configured.
    ///
    /// # Errors
    /// Returns ioInvalidData, notSupported.
    ///
    /// # Security
    /// Requires `crypto.probe`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_crypto_store_probe_kinds(
        &mut self,
    ) -> RuntimeResult<HarnessValue<NativeArray<CryptoStoreKind>, VmArray<CryptoStoreKind>>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = crypto_vm::destack_crypto_store_probe_kinds(self.call_context, context)?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let mut out = std::mem::MaybeUninit::<NativeArray<CryptoStoreKind>>::uninit();
                unsafe {
                    crypto_native::destack_crypto_store_probe_kinds(
                        self.call_context,
                        out.as_mut_ptr(),
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }
}

/// Wrapper that carries one native or VM value for generated harness bindings.
pub(crate) enum HarnessValue<Native, Vm> {
    /// Native value variant.
    Native(Native),
    /// VM value variant.
    Vm(Vm),
}

impl<Native, Vm> HarnessValue<Native, Vm> {
    /// Convert one wrapper into one native value.
    fn into_native(self, label: &str) -> RuntimeResult<Native> {
        match self {
            HarnessValue::Native(value) => Ok(value),
            HarnessValue::Vm(_) => Err(RuntimeError::from(
                HarnessPlatformError::invalid_argument_value(
                    label,
                    "expected native harness value",
                ),
            )
            .boxed()),
        }
    }

    /// Convert one wrapper into one VM value.
    fn into_vm(self, label: &str) -> RuntimeResult<Vm> {
        match self {
            HarnessValue::Vm(value) => Ok(value),
            HarnessValue::Native(_) => Err(RuntimeError::from(
                HarnessPlatformError::invalid_argument_value(label, "expected VM harness value"),
            )
            .boxed()),
        }
    }
}
