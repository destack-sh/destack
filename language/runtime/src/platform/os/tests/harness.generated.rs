// generated by generate-bindings: test harness, do not edit

#![allow(dead_code)]
#![allow(unused_imports)]

use super::*;
use crate::diagnostic::{RuntimeError, RuntimeResult};
use crate::platform::os::{
    HostIdentity, HostIdentityVm, LoadAverage, LoadAverageVm, MountEntry, MountEntryVm, PowerState,
    SystemInfo, SystemInfoVm, native as os_native, vm as os_vm,
};
use crate::platform::{
    NativeArray, NativeStringRef, PlatformError as HarnessPlatformError, VmArray, fs,
};
use destack_vm as vm;

impl<'call> OsHarnessContext<'call> {
    /// Return the generated VM context if available.
    #[allow(clippy::mut_from_ref)]
    fn generated_vm_context_mut(&self) -> Option<&mut vm::ExternalCallContext<'_>> {
        self.vm_context
            .map(|context| unsafe { &mut *(context as *mut vm::ExternalCallContext<'_>) })
    }

    /// Return one standardized value payload for native and VM variants.
    pub(crate) fn harness_value<Native, Vm>(&self, native: Native) -> HarnessValue<Native, Vm> {
        HarnessValue::Native(native)
    }

    /// Return one standardized value payload for VM and native variants.
    pub(crate) fn harness_value_vm<Native, Vm>(&self, vm: Vm) -> HarnessValue<Native, Vm> {
        HarnessValue::Vm(vm)
    }

    /// Read host identity.
    ///
    /// Return one normalized host identity payload.
    /// Identity fields are sourced from host kernel and runtime normalization rules.
    ///
    /// # Platform
    /// Unix, Windows, and Wasi.
    /// Uses uname and hostname APIs on Unix, version and hostname APIs on Windows, and wasi host metadata.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, notSupported.
    ///
    /// # Security
    /// Requires `os.hostname`, `os.sysinfo`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_os_host_identity(
        &mut self,
    ) -> RuntimeResult<HarnessValue<HostIdentity, HostIdentityVm>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = os_vm::destack_os_host_identity(self.call_context, context)?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let mut out = std::mem::MaybeUninit::<HostIdentity>::uninit();
                unsafe {
                    os_native::destack_os_host_identity(self.call_context, out.as_mut_ptr())?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Read host boot time.
    ///
    /// Return the Unix timestamp for host boot time in nanoseconds.
    /// Timestamp origin and precision follow host timekeeping interfaces.
    ///
    /// # Platform
    /// Unix and Windows.
    /// Uses boot-time sysctl or procfs style sources on Unix and boot-time system info on Windows.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, notSupported.
    ///
    /// # Security
    /// Requires `os.sysinfo`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_os_boot_time_unix_ns(&mut self) -> RuntimeResult<u64> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = os_vm::destack_os_boot_time_unix_ns(self.call_context, context)?;
                Ok(out)
            }
            None => {
                let mut out = std::mem::MaybeUninit::<u64>::uninit();
                unsafe {
                    os_native::destack_os_boot_time_unix_ns(self.call_context, out.as_mut_ptr())?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// Read host load averages.
    ///
    /// Return host load averages over one, five, and fifteen minute windows.
    /// Values reflect host scheduler accounting and may be unavailable on some kernels.
    ///
    /// # Platform
    /// Unix only.
    /// Uses getloadavg style interfaces or kernel load-average exports.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, notSupported.
    ///
    /// # Security
    /// Requires `os.sysinfo`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_os_load_average(
        &mut self,
    ) -> RuntimeResult<HarnessValue<LoadAverage, LoadAverageVm>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = os_vm::destack_os_load_average(self.call_context, context)?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let mut out = std::mem::MaybeUninit::<LoadAverage>::uninit();
                unsafe {
                    os_native::destack_os_load_average(self.call_context, out.as_mut_ptr())?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Read host system information.
    ///
    /// Return one normalized system-information payload.
    /// Topology and capacity fields are sampled from host APIs at call time.
    ///
    /// # Platform
    /// Unix, Windows, and Wasi.
    /// Uses sysconf/sysinfo-style APIs on Unix, GlobalMemoryStatusEx and processor APIs on Windows, and wasi sysinfo support.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, notSupported.
    ///
    /// # Security
    /// Requires `os.sysinfo`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_os_system_info(
        &mut self,
    ) -> RuntimeResult<HarnessValue<SystemInfo, SystemInfoVm>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = os_vm::destack_os_system_info(self.call_context, context)?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let mut out = std::mem::MaybeUninit::<SystemInfo>::uninit();
                unsafe {
                    os_native::destack_os_system_info(self.call_context, out.as_mut_ptr())?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Read host uptime.
    ///
    /// Return host uptime in nanoseconds from system boot.
    /// Uptime source follows host monotonic uptime facilities.
    ///
    /// # Platform
    /// Unix, Windows, and Wasi.
    /// Uses clock_gettime style uptime on Unix, GetTickCount64 style uptime on Windows, and wasi clocks when available.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, notSupported.
    ///
    /// # Security
    /// Requires `os.sysinfo`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_os_uptime_ns(&mut self) -> RuntimeResult<u64> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = os_vm::destack_os_uptime_ns(self.call_context, context)?;
                Ok(out)
            }
            None => {
                let mut out = std::mem::MaybeUninit::<u64>::uninit();
                unsafe {
                    os_native::destack_os_uptime_ns(self.call_context, out.as_mut_ptr())?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// Mount one filesystem target.
    ///
    /// Mount one source on one target path with explicit flags and data.
    /// Mount privilege checks and propagation policy are host-defined.
    ///
    /// # Platform
    /// Unix and Windows.
    /// Uses mount(2)-family APIs on Unix and volume-mount APIs on Windows.
    ///
    /// # Errors
    /// Returns invalidArgument, ioPermissionDenied, ioInvalidData, notSupported.
    ///
    /// # Security
    /// Requires `os.mount`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_os_add(
        &mut self,
        source: HarnessValue<fs::OsPath, fs::OsPathVm>,
        target: HarnessValue<fs::OsPath, fs::OsPathVm>,
        filesystem: HarnessValue<NativeStringRef, vm::StringHandle>,
        flags: u64,
        data: HarnessValue<NativeStringRef, vm::StringHandle>,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let source = source.into_vm("source")?;
                let target = target.into_vm("target")?;
                let filesystem = filesystem.into_vm("filesystem")?;
                let data = data.into_vm("data")?;
                os_vm::destack_os_add(
                    self.call_context,
                    context,
                    source,
                    target,
                    filesystem,
                    flags,
                    data,
                )
            }
            None => {
                let source = source.into_native("source")?;
                let target = target.into_native("target")?;
                let filesystem = filesystem.into_native("filesystem")?;
                let data = data.into_native("data")?;
                unsafe {
                    os_native::destack_os_add(
                        self.call_context,
                        source,
                        target,
                        filesystem,
                        flags,
                        data,
                    )
                }
            }
        }
    }

    /// Enumerate mount table entries.
    ///
    /// Return one snapshot of the current host mount table.
    /// Entry shape is normalized but field availability is host-dependent.
    ///
    /// # Platform
    /// Unix and Windows.
    /// Uses mount table APIs on Unix and volume enumeration APIs on Windows.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, notSupported.
    ///
    /// # Security
    /// Requires `os.mount`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_os_list(
        &mut self,
    ) -> RuntimeResult<HarnessValue<NativeArray<MountEntry>, VmArray<MountEntryVm>>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = os_vm::destack_os_list(self.call_context, context)?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let mut out = std::mem::MaybeUninit::<NativeArray<MountEntry>>::uninit();
                unsafe {
                    os_native::destack_os_list(self.call_context, out.as_mut_ptr())?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Unmount one filesystem target.
    ///
    /// Unmount one target path with explicit unmount flags.
    /// Forced unmount behavior follows host kernel semantics.
    ///
    /// # Platform
    /// Unix and Windows.
    /// Uses umount or unmount APIs on Unix and volume unmount APIs on Windows.
    ///
    /// # Errors
    /// Returns invalidArgument, ioPermissionDenied, ioInvalidData, notSupported.
    ///
    /// # Security
    /// Requires `os.mount`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_os_remove(
        &mut self,
        target: HarnessValue<fs::OsPath, fs::OsPathVm>,
        flags: u32,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let target = target.into_vm("target")?;
                os_vm::destack_os_remove(self.call_context, context, target, flags)
            }
            None => {
                let target = target.into_native("target")?;
                unsafe { os_native::destack_os_remove(self.call_context, target, flags) }
            }
        }
    }

    /// Read current host power state.
    ///
    /// Return one normalized host power-state classification.
    /// State mapping follows runtime normalization over host power APIs.
    ///
    /// # Platform
    /// Unix and Windows.
    /// Uses host power management APIs such as sysfs and IOKit on Unix-like systems and GetSystemPowerStatus on Windows.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, notSupported.
    ///
    /// # Security
    /// Requires `os.power`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_os_power_state(&mut self) -> RuntimeResult<PowerState> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = os_vm::destack_os_power_state(self.call_context, context)?;
                Ok(out)
            }
            None => {
                let mut out = std::mem::MaybeUninit::<PowerState>::uninit();
                unsafe {
                    os_native::destack_os_power_state(self.call_context, out.as_mut_ptr())?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// Request host suspend.
    ///
    /// Request one host suspend transition through platform power APIs.
    /// Request acceptance and timing are host-policy and privilege dependent.
    ///
    /// # Platform
    /// Unix and Windows.
    /// Uses host power-management APIs where supported.
    ///
    /// # Errors
    /// Returns ioPermissionDenied, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `os.power`.
    ///
    /// # Replay
    /// External, nonrecordable.
    pub(crate) fn destack_os_suspend(&mut self) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => os_vm::destack_os_suspend(self.call_context, context),
            None => unsafe { os_native::destack_os_suspend(self.call_context) },
        }
    }
}

/// Wrapper that carries one native or VM value for generated harness bindings.
pub(crate) enum HarnessValue<Native, Vm> {
    /// Native value variant.
    Native(Native),
    /// VM value variant.
    Vm(Vm),
}

impl<Native, Vm> HarnessValue<Native, Vm> {
    /// Convert one wrapper into one native value.
    fn into_native(self, label: &str) -> RuntimeResult<Native> {
        match self {
            HarnessValue::Native(value) => Ok(value),
            HarnessValue::Vm(_) => Err(RuntimeError::from(
                HarnessPlatformError::invalid_argument_value(
                    label,
                    "expected native harness value",
                ),
            )
            .boxed()),
        }
    }

    /// Convert one wrapper into one VM value.
    fn into_vm(self, label: &str) -> RuntimeResult<Vm> {
        match self {
            HarnessValue::Vm(value) => Ok(value),
            HarnessValue::Native(_) => Err(RuntimeError::from(
                HarnessPlatformError::invalid_argument_value(label, "expected VM harness value"),
            )
            .boxed()),
        }
    }
}
