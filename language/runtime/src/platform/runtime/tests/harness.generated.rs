// generated by generate-bindings: test harness, do not edit

#![allow(dead_code)]
#![allow(unused_imports, dead_code)]
#![allow(clippy::type_complexity)]

use crate::diagnostic::{RuntimeError, RuntimeResult};
use crate::platform::runtime::tests::RuntimeHarnessContext;
use crate::platform::runtime::{
    AgentCreateOptions, AgentCreateOptionsVm, AgentDescriptor, AgentDescriptorVm, AgentFilter,
    AgentFilterVm, AgentHandle, AgentId, BranchDescriptor, BranchDescriptorVm, BranchFilter,
    BranchFilterVm, BranchId, CheckpointDescriptor, CheckpointDescriptorVm, CheckpointFilter,
    CheckpointFilterVm, CheckpointId, EngineDescriptor, EngineDescriptorKind, EngineDescriptorVm,
    EventLoopDescriptor, EventLoopDescriptorVm, HeapDescriptor, HeapDescriptorVm, ImageDescriptor,
    ImageDescriptorVm, ImageFilter, ImageFilterVm, ImageId, ObservationEventKind,
    ObservationHandle, ObservationOptions, ObservationOptionsVm, ObservationRecord,
    ObservationRecordVm, ResourceDescriptor, ResourceDescriptorVm, ResourceFilter,
    ResourceFilterVm, RevisionDescriptor, RevisionDescriptorVm, RevisionFilter, RevisionFilterVm,
    RevisionId, RuntimeCreateOptions, RuntimeCreateOptionsVm, RuntimeDescriptor,
    RuntimeDescriptorVm, RuntimeEngineKind, RuntimeExecutionMode, RuntimeFilter, RuntimeFilterVm,
    RuntimeHandle, RuntimeId, RuntimeLabel, RuntimeLabelSelector, RuntimeLabelSelectorVm,
    RuntimeLabelVm, RuntimeTickOutcome, RuntimeWorldKind, SnapshotDescriptor, SnapshotDescriptorVm,
    SnapshotFormat, SnapshotId, TopologyEdge, TopologyEdgeFilter, TopologyEdgeFilterVm,
    TopologyEdgeId, TopologyEdgeIdVm, TopologyEdgeKind, TopologyEdgeKindVm, TopologyEdgeVm,
    TopologyEntity, TopologyEntityFilter, TopologyEntityFilterVm, TopologyEntityId,
    TopologyEntityIdVm, TopologyEntityKind, TopologyEntityKindVm, TopologyEntityVm,
    TraceCursorHandle, TraceCursorOptions, TraceCursorOptionsVm, TraceDescriptor,
    TraceDescriptorVm, TraceEventKind, TraceRecord, TraceRecordVm, TraceSequence,
    WorldCreateOptions, WorldCreateOptionsVm, WorldDescriptor, WorldDescriptorVm, WorldHandle,
    WorldResourceId, WorldResourceIdVm, WorldViewHandle, WorldViewOptions, WorldViewOptionsVm,
    native as runtime_native, vm as runtime_vm,
};
use crate::platform::{
    NativeArray, NativeStringRef, PlatformError as HarnessPlatformError, VmArray, resource,
};
use destack_vm as vm;

impl<'call> RuntimeHarnessContext<'call> {
    /// Return the generated VM context if available.
    #[allow(clippy::mut_from_ref)]
    fn generated_vm_context_mut(&self) -> Option<&mut vm::ExternalCallContext<'_>> {
        self.vm_context
            .map(|context| unsafe { &mut *(context as *mut vm::ExternalCallContext<'_>) })
    }

    /// Return one standardized value payload for native and VM variants.
    pub(crate) fn harness_value<Native, Vm>(&self, native: Native) -> HarnessValue<Native, Vm> {
        HarnessValue::Native(native)
    }

    /// Return one standardized value payload for VM and native variants.
    pub(crate) fn harness_value_vm<Native, Vm>(&self, vm: Vm) -> HarnessValue<Native, Vm> {
        HarnessValue::Vm(vm)
    }

    /// Close one agent.
    ///
    /// Remove one execution lane from its owning runtime and release its lane-local state.
    /// Resource teardown follows runtime and resource policy.
    ///
    /// # Platform
    /// Runtime-managed on all targets.
    /// Uses runtime agent teardown logic.
    ///
    /// # Errors
    /// Returns invalidArgument, ioNotFound, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `runtime.agent.control`.
    ///
    /// # Replay
    /// Deterministic.
    pub(crate) fn destack_runtime_agent_close(&mut self, agent: AgentHandle) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                runtime_vm::destack_runtime_agent_close(self.call_context, context, agent)
            }
            None => unsafe {
                runtime_native::destack_runtime_agent_close(self.call_context, agent)
            },
        }
    }

    /// Spawn one agent in one runtime.
    ///
    /// Create one execution lane inside one runtime with explicit metadata options.
    /// The agent is attached to the runtime's current world and trace context.
    ///
    /// # Platform
    /// Runtime-managed on all targets.
    /// Uses runtime agent construction logic.
    ///
    /// # Errors
    /// Returns invalidArgument, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `runtime.agent.create`.
    ///
    /// # Replay
    /// Deterministic.
    pub(crate) fn destack_runtime_agent_create(
        &mut self,
        runtimehandle: RuntimeHandle,
        options: HarnessValue<Option<AgentCreateOptions>, Option<AgentCreateOptionsVm>>,
    ) -> RuntimeResult<AgentHandle> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let options = options.into_vm("options")?;
                let out = runtime_vm::destack_runtime_agent_create(
                    self.call_context,
                    context,
                    runtimehandle,
                    options,
                )?;
                Ok(out)
            }
            None => {
                let options = options.into_native("options")?;
                let mut out = std::mem::MaybeUninit::<AgentHandle>::uninit();
                unsafe {
                    runtime_native::destack_runtime_agent_create(
                        self.call_context,
                        out.as_mut_ptr(),
                        runtimehandle,
                        options,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// Describe one agent.
    ///
    /// Return one structured agent descriptor for one live agent handle.
    /// Descriptor fields summarize ownership, labels, pending-work state, and resource counts.
    ///
    /// # Platform
    /// Runtime-managed on all targets.
    /// Uses runtime agent state only.
    ///
    /// # Errors
    /// Returns invalidArgument, ioNotFound, notSupported.
    ///
    /// # Security
    /// Requires `runtime.agent.read`.
    ///
    /// # Replay
    /// Deterministic.
    pub(crate) fn destack_runtime_agent_describe(
        &mut self,
        agent: AgentHandle,
    ) -> RuntimeResult<HarnessValue<AgentDescriptor, AgentDescriptorVm>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out =
                    runtime_vm::destack_runtime_agent_describe(self.call_context, context, agent)?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let mut out = std::mem::MaybeUninit::<AgentDescriptor>::uninit();
                unsafe {
                    runtime_native::destack_runtime_agent_describe(
                        self.call_context,
                        out.as_mut_ptr(),
                        agent,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Close one runtime.
    ///
    /// Remove one runtime container from its owning world and release its live execution state.
    /// Agent teardown follows runtime shutdown policy.
    ///
    /// # Platform
    /// Runtime-managed on all targets.
    /// Uses runtime teardown logic.
    ///
    /// # Errors
    /// Returns invalidArgument, ioNotFound, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `runtime.runtime.control`.
    ///
    /// # Replay
    /// Deterministic.
    pub(crate) fn destack_runtime_runtime_close(
        &mut self,
        runtimehandle: RuntimeHandle,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                runtime_vm::destack_runtime_runtime_close(self.call_context, context, runtimehandle)
            }
            None => unsafe {
                runtime_native::destack_runtime_runtime_close(self.call_context, runtimehandle)
            },
        }
    }

    /// Spawn one runtime in one world.
    ///
    /// Create one process-like runtime container inside one world with explicit metadata options.
    /// The new runtime is attached to the world's active branch and trace state.
    ///
    /// # Platform
    /// Runtime-managed on all targets.
    /// Uses runtime world ownership and runtime construction logic.
    ///
    /// # Errors
    /// Returns invalidArgument, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `runtime.runtime.create`.
    ///
    /// # Replay
    /// Deterministic.
    pub(crate) fn destack_runtime_runtime_create(
        &mut self,
        argument_world: WorldHandle,
        options: HarnessValue<Option<RuntimeCreateOptions>, Option<RuntimeCreateOptionsVm>>,
    ) -> RuntimeResult<RuntimeHandle> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let options = options.into_vm("options")?;
                let out = runtime_vm::destack_runtime_runtime_create(
                    self.call_context,
                    context,
                    argument_world,
                    options,
                )?;
                Ok(out)
            }
            None => {
                let options = options.into_native("options")?;
                let mut out = std::mem::MaybeUninit::<RuntimeHandle>::uninit();
                unsafe {
                    runtime_native::destack_runtime_runtime_create(
                        self.call_context,
                        out.as_mut_ptr(),
                        argument_world,
                        options,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// Describe one runtime.
    ///
    /// Return one structured runtime descriptor for one live runtime handle.
    /// Descriptor fields summarize runtime ownership, primary agent, labels, and agent counts.
    ///
    /// # Platform
    /// Runtime-managed on all targets.
    /// Uses runtime state only.
    ///
    /// # Errors
    /// Returns invalidArgument, ioNotFound, notSupported.
    ///
    /// # Security
    /// Requires `runtime.runtime.read`.
    ///
    /// # Replay
    /// Deterministic.
    pub(crate) fn destack_runtime_runtime_describe(
        &mut self,
        runtimehandle: RuntimeHandle,
    ) -> RuntimeResult<HarnessValue<RuntimeDescriptor, RuntimeDescriptorVm>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = runtime_vm::destack_runtime_runtime_describe(
                    self.call_context,
                    context,
                    runtimehandle,
                )?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let mut out = std::mem::MaybeUninit::<RuntimeDescriptor>::uninit();
                unsafe {
                    runtime_native::destack_runtime_runtime_describe(
                        self.call_context,
                        out.as_mut_ptr(),
                        runtimehandle,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Close one world.
    ///
    /// Tear down one low-level world control object and release its owned runtime state.
    /// Active runtimes, agents, and attached runtime resources are closed according to runtime shutdown policy.
    ///
    /// # Platform
    /// Runtime-managed on all targets.
    /// Uses runtime world teardown logic.
    ///
    /// # Errors
    /// Returns invalidArgument, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `runtime.world.control`.
    ///
    /// # Replay
    /// Deterministic.
    pub(crate) fn destack_runtime_world_close(
        &mut self,
        argument_world: WorldHandle,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                runtime_vm::destack_runtime_world_close(self.call_context, context, argument_world)
            }
            None => unsafe {
                runtime_native::destack_runtime_world_close(self.call_context, argument_world)
            },
        }
    }

    /// Create one world.
    ///
    /// Allocate one new low-level world control object with explicit runtime options.
    /// World creation initializes clocks, lineage, topology, simulation, trace, and other world-owned state.
    ///
    /// # Platform
    /// Runtime-managed on all targets.
    /// Uses runtime world construction rather than host syscalls.
    ///
    /// # Errors
    /// Returns invalidArgument, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `runtime.world.create`.
    ///
    /// # Replay
    /// Deterministic.
    pub(crate) fn destack_runtime_world_create(
        &mut self,
        options: HarnessValue<Option<WorldCreateOptions>, Option<WorldCreateOptionsVm>>,
    ) -> RuntimeResult<WorldHandle> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let options = options.into_vm("options")?;
                let out =
                    runtime_vm::destack_runtime_world_create(self.call_context, context, options)?;
                Ok(out)
            }
            None => {
                let options = options.into_native("options")?;
                let mut out = std::mem::MaybeUninit::<WorldHandle>::uninit();
                unsafe {
                    runtime_native::destack_runtime_world_create(
                        self.call_context,
                        out.as_mut_ptr(),
                        options,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// Describe one world.
    ///
    /// Return one structured world descriptor for one live world handle.
    /// Descriptor fields reflect the current active branch, revision, time state, and runtime counts.
    ///
    /// # Platform
    /// Runtime-managed on all targets.
    /// Uses runtime world state only.
    ///
    /// # Errors
    /// Returns invalidArgument, ioNotFound, notSupported.
    ///
    /// # Security
    /// Requires `runtime.world.read`.
    ///
    /// # Replay
    /// Deterministic.
    pub(crate) fn destack_runtime_world_describe(
        &mut self,
        argument_world: WorldHandle,
    ) -> RuntimeResult<HarnessValue<WorldDescriptor, WorldDescriptorVm>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = runtime_vm::destack_runtime_world_describe(
                    self.call_context,
                    context,
                    argument_world,
                )?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let mut out = std::mem::MaybeUninit::<WorldDescriptor>::uninit();
                unsafe {
                    runtime_native::destack_runtime_world_describe(
                        self.call_context,
                        out.as_mut_ptr(),
                        argument_world,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Advance one world by one scheduler step.
    ///
    /// Execute one world-level deterministic advance step across runtimes, agents, clocks, and simulation.
    /// The returned outcome distinguishes idle, progressed, and time-advance results.
    ///
    /// # Platform
    /// Runtime-managed on all targets.
    /// Uses world scheduler and simulation coordination.
    ///
    /// # Errors
    /// Returns invalidArgument, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `runtime.world.control`.
    ///
    /// # Replay
    /// Deterministic.
    pub(crate) fn destack_runtime_world_tick(
        &mut self,
        argument_world: WorldHandle,
    ) -> RuntimeResult<RuntimeTickOutcome> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = runtime_vm::destack_runtime_world_tick(
                    self.call_context,
                    context,
                    argument_world,
                )?;
                Ok(out)
            }
            None => {
                let mut out = std::mem::MaybeUninit::<RuntimeTickOutcome>::uninit();
                unsafe {
                    runtime_native::destack_runtime_world_tick(
                        self.call_context,
                        out.as_mut_ptr(),
                        argument_world,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// List agents visible through one pinned world view.
    ///
    /// Enumerate agent descriptors from the image backing one pinned world view.
    /// When one runtime identifier is provided, only agents owned by that runtime are returned.
    ///
    /// # Platform
    /// Runtime-managed on all targets.
    /// Uses pinned image and topology state only.
    ///
    /// # Errors
    /// Returns invalidArgument, ioNotFound, notSupported.
    ///
    /// # Security
    /// Requires `runtime.inspect.read`.
    ///
    /// # Replay
    /// Deterministic.
    pub(crate) fn destack_runtime_agent_list(
        &mut self,
        view: WorldViewHandle,
        filter: HarnessValue<Option<AgentFilter>, Option<AgentFilterVm>>,
        after: Option<AgentId>,
        limit: Option<u32>,
    ) -> RuntimeResult<HarnessValue<NativeArray<AgentDescriptor>, VmArray<AgentDescriptorVm>>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let filter = filter.into_vm("filter")?;
                let out = runtime_vm::destack_runtime_agent_list(
                    self.call_context,
                    context,
                    view,
                    filter,
                    after,
                    limit,
                )?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let filter = filter.into_native("filter")?;
                let mut out = std::mem::MaybeUninit::<NativeArray<AgentDescriptor>>::uninit();
                unsafe {
                    runtime_native::destack_runtime_agent_list(
                        self.call_context,
                        out.as_mut_ptr(),
                        view,
                        filter,
                        after,
                        limit,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Describe one agent visible through one pinned world view.
    ///
    /// Return one agent descriptor from the image backing one pinned world view.
    /// Descriptor values are stable for the lifetime of the view.
    ///
    /// # Platform
    /// Runtime-managed on all targets.
    /// Uses pinned image and topology state only.
    ///
    /// # Errors
    /// Returns invalidArgument, ioNotFound, notSupported.
    ///
    /// # Security
    /// Requires `runtime.inspect.read`.
    ///
    /// # Replay
    /// Deterministic.
    pub(crate) fn destack_runtime_agent_view(
        &mut self,
        view: WorldViewHandle,
        agentid: AgentId,
    ) -> RuntimeResult<HarnessValue<AgentDescriptor, AgentDescriptorVm>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = runtime_vm::destack_runtime_agent_view(
                    self.call_context,
                    context,
                    view,
                    agentid,
                )?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let mut out = std::mem::MaybeUninit::<AgentDescriptor>::uninit();
                unsafe {
                    runtime_native::destack_runtime_agent_view(
                        self.call_context,
                        out.as_mut_ptr(),
                        view,
                        agentid,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// List topology edges visible through one pinned world view.
    ///
    /// Enumerate topology edges from the image backing one pinned world view.
    /// Descriptor values are stable for the lifetime of the view.
    ///
    /// # Platform
    /// Runtime-managed on all targets.
    /// Uses pinned topology state only.
    ///
    /// # Errors
    /// Returns invalidArgument, ioNotFound, notSupported.
    ///
    /// # Security
    /// Requires `runtime.inspect.read`.
    ///
    /// # Replay
    /// Deterministic.
    pub(crate) fn destack_runtime_edge_list(
        &mut self,
        view: WorldViewHandle,
        filter: HarnessValue<Option<TopologyEdgeFilter>, Option<TopologyEdgeFilterVm>>,
        after: HarnessValue<Option<TopologyEdgeId>, Option<TopologyEdgeIdVm>>,
        limit: Option<u32>,
    ) -> RuntimeResult<HarnessValue<NativeArray<TopologyEdge>, VmArray<TopologyEdgeVm>>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let filter = filter.into_vm("filter")?;
                let after = after.into_vm("after")?;
                let out = runtime_vm::destack_runtime_edge_list(
                    self.call_context,
                    context,
                    view,
                    filter,
                    after,
                    limit,
                )?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let filter = filter.into_native("filter")?;
                let after = after.into_native("after")?;
                let mut out = std::mem::MaybeUninit::<NativeArray<TopologyEdge>>::uninit();
                unsafe {
                    runtime_native::destack_runtime_edge_list(
                        self.call_context,
                        out.as_mut_ptr(),
                        view,
                        filter,
                        after,
                        limit,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Describe one topology edge visible through one pinned world view.
    ///
    /// Return one topology edge from the image backing one pinned world view.
    /// Descriptor values are stable for the lifetime of the view.
    ///
    /// # Platform
    /// Runtime-managed on all targets.
    /// Uses pinned topology state only.
    ///
    /// # Errors
    /// Returns invalidArgument, ioNotFound, notSupported.
    ///
    /// # Security
    /// Requires `runtime.inspect.read`.
    ///
    /// # Replay
    /// Deterministic.
    pub(crate) fn destack_runtime_edge_view(
        &mut self,
        view: WorldViewHandle,
        edgeid: HarnessValue<TopologyEdgeId, TopologyEdgeIdVm>,
    ) -> RuntimeResult<HarnessValue<TopologyEdge, TopologyEdgeVm>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let edgeid = edgeid.into_vm("edgeid")?;
                let out = runtime_vm::destack_runtime_edge_view(
                    self.call_context,
                    context,
                    view,
                    edgeid,
                )?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let edgeid = edgeid.into_native("edgeid")?;
                let mut out = std::mem::MaybeUninit::<TopologyEdge>::uninit();
                unsafe {
                    runtime_native::destack_runtime_edge_view(
                        self.call_context,
                        out.as_mut_ptr(),
                        view,
                        edgeid,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Describe the execution engine for one agent visible through one pinned world view.
    ///
    /// Return one engine summary from the image backing one pinned world view.
    /// Descriptor values are stable for the lifetime of the view.
    ///
    /// # Platform
    /// Runtime-managed on all targets.
    /// Uses pinned image state only.
    ///
    /// # Errors
    /// Returns invalidArgument, ioNotFound, notSupported.
    ///
    /// # Security
    /// Requires `runtime.inspect.read`.
    ///
    /// # Replay
    /// Deterministic.
    pub(crate) fn destack_runtime_engine_view(
        &mut self,
        view: WorldViewHandle,
        agentid: AgentId,
    ) -> RuntimeResult<HarnessValue<EngineDescriptor, EngineDescriptorVm>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = runtime_vm::destack_runtime_engine_view(
                    self.call_context,
                    context,
                    view,
                    agentid,
                )?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let mut out = std::mem::MaybeUninit::<EngineDescriptor>::uninit();
                unsafe {
                    runtime_native::destack_runtime_engine_view(
                        self.call_context,
                        out.as_mut_ptr(),
                        view,
                        agentid,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// List topology entities visible through one pinned world view.
    ///
    /// Enumerate topology entities from the image backing one pinned world view.
    /// Descriptor values are stable for the lifetime of the view.
    ///
    /// # Platform
    /// Runtime-managed on all targets.
    /// Uses pinned topology state only.
    ///
    /// # Errors
    /// Returns invalidArgument, ioNotFound, notSupported.
    ///
    /// # Security
    /// Requires `runtime.inspect.read`.
    ///
    /// # Replay
    /// Deterministic.
    pub(crate) fn destack_runtime_entity_list(
        &mut self,
        view: WorldViewHandle,
        filter: HarnessValue<Option<TopologyEntityFilter>, Option<TopologyEntityFilterVm>>,
        after: HarnessValue<Option<TopologyEntityId>, Option<TopologyEntityIdVm>>,
        limit: Option<u32>,
    ) -> RuntimeResult<HarnessValue<NativeArray<TopologyEntity>, VmArray<TopologyEntityVm>>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let filter = filter.into_vm("filter")?;
                let after = after.into_vm("after")?;
                let out = runtime_vm::destack_runtime_entity_list(
                    self.call_context,
                    context,
                    view,
                    filter,
                    after,
                    limit,
                )?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let filter = filter.into_native("filter")?;
                let after = after.into_native("after")?;
                let mut out = std::mem::MaybeUninit::<NativeArray<TopologyEntity>>::uninit();
                unsafe {
                    runtime_native::destack_runtime_entity_list(
                        self.call_context,
                        out.as_mut_ptr(),
                        view,
                        filter,
                        after,
                        limit,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Describe one topology entity visible through one pinned world view.
    ///
    /// Return one topology entity from the image backing one pinned world view.
    /// Descriptor values are stable for the lifetime of the view.
    ///
    /// # Platform
    /// Runtime-managed on all targets.
    /// Uses pinned topology state only.
    ///
    /// # Errors
    /// Returns invalidArgument, ioNotFound, notSupported.
    ///
    /// # Security
    /// Requires `runtime.inspect.read`.
    ///
    /// # Replay
    /// Deterministic.
    pub(crate) fn destack_runtime_entity_view(
        &mut self,
        view: WorldViewHandle,
        entityid: HarnessValue<TopologyEntityId, TopologyEntityIdVm>,
    ) -> RuntimeResult<HarnessValue<TopologyEntity, TopologyEntityVm>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let entityid = entityid.into_vm("entityid")?;
                let out = runtime_vm::destack_runtime_entity_view(
                    self.call_context,
                    context,
                    view,
                    entityid,
                )?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let entityid = entityid.into_native("entityid")?;
                let mut out = std::mem::MaybeUninit::<TopologyEntity>::uninit();
                unsafe {
                    runtime_native::destack_runtime_entity_view(
                        self.call_context,
                        out.as_mut_ptr(),
                        view,
                        entityid,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Describe the event loop for one agent visible through one pinned world view.
    ///
    /// Return one event-loop summary from the image backing one pinned world view.
    /// Descriptor values are stable for the lifetime of the view.
    ///
    /// # Platform
    /// Runtime-managed on all targets.
    /// Uses pinned image state only.
    ///
    /// # Errors
    /// Returns invalidArgument, ioNotFound, notSupported.
    ///
    /// # Security
    /// Requires `runtime.inspect.read`.
    ///
    /// # Replay
    /// Deterministic.
    pub(crate) fn destack_runtime_event_loop_view(
        &mut self,
        view: WorldViewHandle,
        agentid: AgentId,
    ) -> RuntimeResult<HarnessValue<EventLoopDescriptor, EventLoopDescriptorVm>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = runtime_vm::destack_runtime_event_loop_view(
                    self.call_context,
                    context,
                    view,
                    agentid,
                )?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let mut out = std::mem::MaybeUninit::<EventLoopDescriptor>::uninit();
                unsafe {
                    runtime_native::destack_runtime_event_loop_view(
                        self.call_context,
                        out.as_mut_ptr(),
                        view,
                        agentid,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Describe the heap for one agent visible through one pinned world view.
    ///
    /// Return one heap summary from the image backing one pinned world view.
    /// Descriptor values are stable for the lifetime of the view.
    ///
    /// # Platform
    /// Runtime-managed on all targets.
    /// Uses pinned image state only.
    ///
    /// # Errors
    /// Returns invalidArgument, ioNotFound, notSupported.
    ///
    /// # Security
    /// Requires `runtime.inspect.read`.
    ///
    /// # Replay
    /// Deterministic.
    pub(crate) fn destack_runtime_heap_view(
        &mut self,
        view: WorldViewHandle,
        agentid: AgentId,
    ) -> RuntimeResult<HarnessValue<HeapDescriptor, HeapDescriptorVm>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = runtime_vm::destack_runtime_heap_view(
                    self.call_context,
                    context,
                    view,
                    agentid,
                )?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let mut out = std::mem::MaybeUninit::<HeapDescriptor>::uninit();
                unsafe {
                    runtime_native::destack_runtime_heap_view(
                        self.call_context,
                        out.as_mut_ptr(),
                        view,
                        agentid,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Describe the pinned image for one world view.
    ///
    /// Return one image descriptor for the image backing the pinned revision in one world view.
    /// Descriptor values are stable for the lifetime of the view.
    ///
    /// # Platform
    /// Runtime-managed on all targets.
    /// Uses pinned lineage and image state only.
    ///
    /// # Errors
    /// Returns invalidArgument, ioNotFound, notSupported.
    ///
    /// # Security
    /// Requires `runtime.inspect.read`.
    ///
    /// # Replay
    /// Deterministic.
    pub(crate) fn destack_runtime_image_view(
        &mut self,
        view: WorldViewHandle,
    ) -> RuntimeResult<HarnessValue<ImageDescriptor, ImageDescriptorVm>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = runtime_vm::destack_runtime_image_view(self.call_context, context, view)?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let mut out = std::mem::MaybeUninit::<ImageDescriptor>::uninit();
                unsafe {
                    runtime_native::destack_runtime_image_view(
                        self.call_context,
                        out.as_mut_ptr(),
                        view,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// List logical world resources visible through one pinned world view.
    ///
    /// Enumerate logical resource descriptors from the image and topology backing one pinned world view.
    /// Descriptor values are stable for the lifetime of the view.
    ///
    /// # Platform
    /// Runtime-managed on all targets.
    /// Uses pinned image and topology state only.
    ///
    /// # Errors
    /// Returns invalidArgument, ioNotFound, notSupported.
    ///
    /// # Security
    /// Requires `runtime.inspect.read`.
    ///
    /// # Replay
    /// Deterministic.
    pub(crate) fn destack_runtime_resource_list(
        &mut self,
        view: WorldViewHandle,
        filter: HarnessValue<Option<ResourceFilter>, Option<ResourceFilterVm>>,
        after: HarnessValue<Option<WorldResourceId>, Option<WorldResourceIdVm>>,
        limit: Option<u32>,
    ) -> RuntimeResult<HarnessValue<NativeArray<ResourceDescriptor>, VmArray<ResourceDescriptorVm>>>
    {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let filter = filter.into_vm("filter")?;
                let after = after.into_vm("after")?;
                let out = runtime_vm::destack_runtime_resource_list(
                    self.call_context,
                    context,
                    view,
                    filter,
                    after,
                    limit,
                )?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let filter = filter.into_native("filter")?;
                let after = after.into_native("after")?;
                let mut out = std::mem::MaybeUninit::<NativeArray<ResourceDescriptor>>::uninit();
                unsafe {
                    runtime_native::destack_runtime_resource_list(
                        self.call_context,
                        out.as_mut_ptr(),
                        view,
                        filter,
                        after,
                        limit,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Describe one logical world resource visible through one pinned world view.
    ///
    /// Return one resource descriptor from the image and topology backing one pinned world view.
    /// Descriptor values are stable for the lifetime of the view.
    ///
    /// # Platform
    /// Runtime-managed on all targets.
    /// Uses pinned image and topology state only.
    ///
    /// # Errors
    /// Returns invalidArgument, ioNotFound, notSupported.
    ///
    /// # Security
    /// Requires `runtime.inspect.read`.
    ///
    /// # Replay
    /// Deterministic.
    pub(crate) fn destack_runtime_resource_view(
        &mut self,
        view: WorldViewHandle,
        resourceid: HarnessValue<WorldResourceId, WorldResourceIdVm>,
    ) -> RuntimeResult<HarnessValue<ResourceDescriptor, ResourceDescriptorVm>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let resourceid = resourceid.into_vm("resourceid")?;
                let out = runtime_vm::destack_runtime_resource_view(
                    self.call_context,
                    context,
                    view,
                    resourceid,
                )?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let resourceid = resourceid.into_native("resourceid")?;
                let mut out = std::mem::MaybeUninit::<ResourceDescriptor>::uninit();
                unsafe {
                    runtime_native::destack_runtime_resource_view(
                        self.call_context,
                        out.as_mut_ptr(),
                        view,
                        resourceid,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Describe the pinned revision for one world view.
    ///
    /// Return one revision descriptor for the exact revision pinned by one world view.
    /// Descriptor values are stable for the lifetime of the view.
    ///
    /// # Platform
    /// Runtime-managed on all targets.
    /// Uses pinned lineage and image state only.
    ///
    /// # Errors
    /// Returns invalidArgument, ioNotFound, notSupported.
    ///
    /// # Security
    /// Requires `runtime.inspect.read`.
    ///
    /// # Replay
    /// Deterministic.
    pub(crate) fn destack_runtime_revision_view(
        &mut self,
        view: WorldViewHandle,
    ) -> RuntimeResult<HarnessValue<RevisionDescriptor, RevisionDescriptorVm>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out =
                    runtime_vm::destack_runtime_revision_view(self.call_context, context, view)?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let mut out = std::mem::MaybeUninit::<RevisionDescriptor>::uninit();
                unsafe {
                    runtime_native::destack_runtime_revision_view(
                        self.call_context,
                        out.as_mut_ptr(),
                        view,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// List runtimes visible through one pinned world view.
    ///
    /// Enumerate runtime descriptors from the image backing one pinned world view.
    /// Descriptor values are stable for the lifetime of the view.
    ///
    /// # Platform
    /// Runtime-managed on all targets.
    /// Uses pinned image and topology state only.
    ///
    /// # Errors
    /// Returns invalidArgument, ioNotFound, notSupported.
    ///
    /// # Security
    /// Requires `runtime.inspect.read`.
    ///
    /// # Replay
    /// Deterministic.
    pub(crate) fn destack_runtime_runtime_list(
        &mut self,
        view: WorldViewHandle,
        filter: HarnessValue<Option<RuntimeFilter>, Option<RuntimeFilterVm>>,
        after: Option<RuntimeId>,
        limit: Option<u32>,
    ) -> RuntimeResult<HarnessValue<NativeArray<RuntimeDescriptor>, VmArray<RuntimeDescriptorVm>>>
    {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let filter = filter.into_vm("filter")?;
                let out = runtime_vm::destack_runtime_runtime_list(
                    self.call_context,
                    context,
                    view,
                    filter,
                    after,
                    limit,
                )?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let filter = filter.into_native("filter")?;
                let mut out = std::mem::MaybeUninit::<NativeArray<RuntimeDescriptor>>::uninit();
                unsafe {
                    runtime_native::destack_runtime_runtime_list(
                        self.call_context,
                        out.as_mut_ptr(),
                        view,
                        filter,
                        after,
                        limit,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Describe one runtime visible through one pinned world view.
    ///
    /// Return one runtime descriptor from the image backing one pinned world view.
    /// Descriptor values are stable for the lifetime of the view.
    ///
    /// # Platform
    /// Runtime-managed on all targets.
    /// Uses pinned image and topology state only.
    ///
    /// # Errors
    /// Returns invalidArgument, ioNotFound, notSupported.
    ///
    /// # Security
    /// Requires `runtime.inspect.read`.
    ///
    /// # Replay
    /// Deterministic.
    pub(crate) fn destack_runtime_runtime_view(
        &mut self,
        view: WorldViewHandle,
        runtimeid: RuntimeId,
    ) -> RuntimeResult<HarnessValue<RuntimeDescriptor, RuntimeDescriptorVm>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = runtime_vm::destack_runtime_runtime_view(
                    self.call_context,
                    context,
                    view,
                    runtimeid,
                )?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let mut out = std::mem::MaybeUninit::<RuntimeDescriptor>::uninit();
                unsafe {
                    runtime_native::destack_runtime_runtime_view(
                        self.call_context,
                        out.as_mut_ptr(),
                        view,
                        runtimeid,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Describe the pinned trace state for one world view.
    ///
    /// Return one trace descriptor for the exact trace position captured by one world view.
    /// Descriptor values are stable for the lifetime of the view.
    ///
    /// # Platform
    /// Runtime-managed on all targets.
    /// Uses pinned lineage and trace state only.
    ///
    /// # Errors
    /// Returns invalidArgument, ioNotFound, notSupported.
    ///
    /// # Security
    /// Requires `runtime.inspect.read`.
    ///
    /// # Replay
    /// Deterministic.
    pub(crate) fn destack_runtime_trace_view(
        &mut self,
        view: WorldViewHandle,
    ) -> RuntimeResult<HarnessValue<TraceDescriptor, TraceDescriptorVm>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = runtime_vm::destack_runtime_trace_view(self.call_context, context, view)?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let mut out = std::mem::MaybeUninit::<TraceDescriptor>::uninit();
                unsafe {
                    runtime_native::destack_runtime_trace_view(
                        self.call_context,
                        out.as_mut_ptr(),
                        view,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Describe the world visible through one pinned world view.
    ///
    /// Return one world descriptor as observed through one pinned world view.
    /// Descriptor values are stable for the lifetime of the view.
    ///
    /// # Platform
    /// Runtime-managed on all targets.
    /// Uses pinned inspection state only.
    ///
    /// # Errors
    /// Returns invalidArgument, ioNotFound, notSupported.
    ///
    /// # Security
    /// Requires `runtime.inspect.read`.
    ///
    /// # Replay
    /// Deterministic.
    pub(crate) fn destack_runtime_world_view(
        &mut self,
        view: WorldViewHandle,
    ) -> RuntimeResult<HarnessValue<WorldDescriptor, WorldDescriptorVm>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = runtime_vm::destack_runtime_world_view(self.call_context, context, view)?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let mut out = std::mem::MaybeUninit::<WorldDescriptor>::uninit();
                unsafe {
                    runtime_native::destack_runtime_world_view(
                        self.call_context,
                        out.as_mut_ptr(),
                        view,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Close one pinned world view.
    ///
    /// Release one previously opened world view and its pinned state.
    /// Closing one view does not affect the underlying world, trace, or lineage objects.
    ///
    /// # Platform
    /// Runtime-managed on all targets.
    /// Uses runtime inspection teardown logic.
    ///
    /// # Errors
    /// Returns invalidArgument, ioNotFound, notSupported.
    ///
    /// # Security
    /// Requires `runtime.inspect.read`.
    ///
    /// # Replay
    /// Deterministic.
    pub(crate) fn destack_runtime_world_view_close(
        &mut self,
        view: WorldViewHandle,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                runtime_vm::destack_runtime_world_view_close(self.call_context, context, view)
            }
            None => unsafe {
                runtime_native::destack_runtime_world_view_close(self.call_context, view)
            },
        }
    }

    /// Open one pinned world view.
    ///
    /// Create one read-consistent world view over the current world head or one explicit pinned revision.
    /// Views provide stable reads across lineage, state, and trace metadata without racing live mutation.
    ///
    /// # Platform
    /// Runtime-managed on all targets.
    /// Uses runtime inspection and revision pinning logic.
    ///
    /// # Errors
    /// Returns invalidArgument, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `runtime.inspect.read`.
    ///
    /// # Replay
    /// Deterministic.
    pub(crate) fn destack_runtime_world_view_open(
        &mut self,
        argument_world: WorldHandle,
        options: HarnessValue<Option<WorldViewOptions>, Option<WorldViewOptionsVm>>,
    ) -> RuntimeResult<WorldViewHandle> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let options = options.into_vm("options")?;
                let out = runtime_vm::destack_runtime_world_view_open(
                    self.call_context,
                    context,
                    argument_world,
                    options,
                )?;
                Ok(out)
            }
            None => {
                let options = options.into_native("options")?;
                let mut out = std::mem::MaybeUninit::<WorldViewHandle>::uninit();
                unsafe {
                    runtime_native::destack_runtime_world_view_open(
                        self.call_context,
                        out.as_mut_ptr(),
                        argument_world,
                        options,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// Describe one branch.
    ///
    /// Return one branch descriptor for one branch identifier in one world lineage.
    /// Descriptors include the current head revision and branch labels.
    ///
    /// # Platform
    /// Runtime-managed on all targets.
    /// Uses world lineage state only.
    ///
    /// # Errors
    /// Returns invalidArgument, ioNotFound, notSupported.
    ///
    /// # Security
    /// Requires `runtime.lineage.read`.
    ///
    /// # Replay
    /// Deterministic.
    pub(crate) fn destack_runtime_branch_describe(
        &mut self,
        argument_world: WorldHandle,
        branchid: BranchId,
    ) -> RuntimeResult<HarnessValue<BranchDescriptor, BranchDescriptorVm>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = runtime_vm::destack_runtime_branch_describe(
                    self.call_context,
                    context,
                    argument_world,
                    branchid,
                )?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let mut out = std::mem::MaybeUninit::<BranchDescriptor>::uninit();
                unsafe {
                    runtime_native::destack_runtime_branch_describe(
                        self.call_context,
                        out.as_mut_ptr(),
                        argument_world,
                        branchid,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// List branches in one world lineage.
    ///
    /// Enumerate branch descriptors in stable branch-id order starting after the optional cursor.
    /// This is the low-level bulk enumeration surface for world branch heads.
    ///
    /// # Platform
    /// Runtime-managed on all targets.
    /// Uses world lineage state only.
    ///
    /// # Errors
    /// Returns invalidArgument, ioNotFound, notSupported.
    ///
    /// # Security
    /// Requires `runtime.lineage.read`.
    ///
    /// # Replay
    /// Deterministic.
    pub(crate) fn destack_runtime_branch_list(
        &mut self,
        argument_world: WorldHandle,
        filter: HarnessValue<Option<BranchFilter>, Option<BranchFilterVm>>,
        after: Option<BranchId>,
        limit: Option<u32>,
    ) -> RuntimeResult<HarnessValue<NativeArray<BranchDescriptor>, VmArray<BranchDescriptorVm>>>
    {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let filter = filter.into_vm("filter")?;
                let out = runtime_vm::destack_runtime_branch_list(
                    self.call_context,
                    context,
                    argument_world,
                    filter,
                    after,
                    limit,
                )?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let filter = filter.into_native("filter")?;
                let mut out = std::mem::MaybeUninit::<NativeArray<BranchDescriptor>>::uninit();
                unsafe {
                    runtime_native::destack_runtime_branch_list(
                        self.call_context,
                        out.as_mut_ptr(),
                        argument_world,
                        filter,
                        after,
                        limit,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Create one checkpoint on the active branch.
    ///
    /// Materialize one durable checkpoint at the world's current active revision.
    /// The checkpoint anchors one revision and may force image capture according to runtime policy.
    ///
    /// # Platform
    /// Runtime-managed on all targets.
    /// Uses world lineage and image capture logic.
    ///
    /// # Errors
    /// Returns invalidArgument, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `runtime.lineage.control`.
    ///
    /// # Replay
    /// Deterministic.
    pub(crate) fn destack_runtime_checkpoint_create(
        &mut self,
        argument_world: WorldHandle,
        name: HarnessValue<Option<NativeStringRef>, Option<vm::StringHandle>>,
        labels: HarnessValue<Option<NativeArray<RuntimeLabel>>, Option<VmArray<RuntimeLabelVm>>>,
    ) -> RuntimeResult<CheckpointId> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let name = name.into_vm("name")?;
                let labels = labels.into_vm("labels")?;
                let out = runtime_vm::destack_runtime_checkpoint_create(
                    self.call_context,
                    context,
                    argument_world,
                    name,
                    labels,
                )?;
                Ok(out)
            }
            None => {
                let name = name.into_native("name")?;
                let labels = labels.into_native("labels")?;
                let mut out = std::mem::MaybeUninit::<CheckpointId>::uninit();
                unsafe {
                    runtime_native::destack_runtime_checkpoint_create(
                        self.call_context,
                        out.as_mut_ptr(),
                        argument_world,
                        name,
                        labels,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// Describe one checkpoint.
    ///
    /// Return one checkpoint descriptor for one checkpoint identifier in one world lineage.
    /// Descriptors include the anchored revision and checkpoint labels.
    ///
    /// # Platform
    /// Runtime-managed on all targets.
    /// Uses world lineage state only.
    ///
    /// # Errors
    /// Returns invalidArgument, ioNotFound, notSupported.
    ///
    /// # Security
    /// Requires `runtime.lineage.read`.
    ///
    /// # Replay
    /// Deterministic.
    pub(crate) fn destack_runtime_checkpoint_describe(
        &mut self,
        argument_world: WorldHandle,
        checkpointid: CheckpointId,
    ) -> RuntimeResult<HarnessValue<CheckpointDescriptor, CheckpointDescriptorVm>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = runtime_vm::destack_runtime_checkpoint_describe(
                    self.call_context,
                    context,
                    argument_world,
                    checkpointid,
                )?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let mut out = std::mem::MaybeUninit::<CheckpointDescriptor>::uninit();
                unsafe {
                    runtime_native::destack_runtime_checkpoint_describe(
                        self.call_context,
                        out.as_mut_ptr(),
                        argument_world,
                        checkpointid,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// List checkpoints in one world lineage.
    ///
    /// Enumerate checkpoint descriptors in stable checkpoint-id order starting after the optional cursor.
    /// This is the low-level bulk enumeration surface for checkpoint tooling and inspection.
    ///
    /// # Platform
    /// Runtime-managed on all targets.
    /// Uses world lineage state only.
    ///
    /// # Errors
    /// Returns invalidArgument, ioNotFound, notSupported.
    ///
    /// # Security
    /// Requires `runtime.lineage.read`.
    ///
    /// # Replay
    /// Deterministic.
    pub(crate) fn destack_runtime_checkpoint_list(
        &mut self,
        argument_world: WorldHandle,
        filter: HarnessValue<Option<CheckpointFilter>, Option<CheckpointFilterVm>>,
        after: Option<CheckpointId>,
        limit: Option<u32>,
    ) -> RuntimeResult<
        HarnessValue<NativeArray<CheckpointDescriptor>, VmArray<CheckpointDescriptorVm>>,
    > {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let filter = filter.into_vm("filter")?;
                let out = runtime_vm::destack_runtime_checkpoint_list(
                    self.call_context,
                    context,
                    argument_world,
                    filter,
                    after,
                    limit,
                )?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let filter = filter.into_native("filter")?;
                let mut out = std::mem::MaybeUninit::<NativeArray<CheckpointDescriptor>>::uninit();
                unsafe {
                    runtime_native::destack_runtime_checkpoint_list(
                        self.call_context,
                        out.as_mut_ptr(),
                        argument_world,
                        filter,
                        after,
                        limit,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Capture one image at the active revision.
    ///
    /// Materialize one immutable image for the world's current active revision.
    /// Image capture may share lower-level heap and VM image backing with prior revisions.
    ///
    /// # Platform
    /// Runtime-managed on all targets.
    /// Uses world image capture logic.
    ///
    /// # Errors
    /// Returns invalidArgument, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `runtime.snapshot.create`.
    ///
    /// # Replay
    /// Deterministic.
    pub(crate) fn destack_runtime_image_capture(
        &mut self,
        argument_world: WorldHandle,
    ) -> RuntimeResult<ImageId> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = runtime_vm::destack_runtime_image_capture(
                    self.call_context,
                    context,
                    argument_world,
                )?;
                Ok(out)
            }
            None => {
                let mut out = std::mem::MaybeUninit::<ImageId>::uninit();
                unsafe {
                    runtime_native::destack_runtime_image_capture(
                        self.call_context,
                        out.as_mut_ptr(),
                        argument_world,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// Describe one image.
    ///
    /// Return one image descriptor for one materialized image in one world lineage.
    /// Descriptors summarize the owning revision and optional shared backing size.
    ///
    /// # Platform
    /// Runtime-managed on all targets.
    /// Uses world lineage and image metadata only.
    ///
    /// # Errors
    /// Returns invalidArgument, ioNotFound, notSupported.
    ///
    /// # Security
    /// Requires `runtime.snapshot.read`.
    ///
    /// # Replay
    /// Deterministic.
    pub(crate) fn destack_runtime_image_describe(
        &mut self,
        argument_world: WorldHandle,
        imageid: ImageId,
    ) -> RuntimeResult<HarnessValue<ImageDescriptor, ImageDescriptorVm>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = runtime_vm::destack_runtime_image_describe(
                    self.call_context,
                    context,
                    argument_world,
                    imageid,
                )?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let mut out = std::mem::MaybeUninit::<ImageDescriptor>::uninit();
                unsafe {
                    runtime_native::destack_runtime_image_describe(
                        self.call_context,
                        out.as_mut_ptr(),
                        argument_world,
                        imageid,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// List images in one world lineage.
    ///
    /// Enumerate image descriptors in stable image-id order starting after the optional cursor.
    /// This is the low-level bulk enumeration surface for materialized world images.
    ///
    /// # Platform
    /// Runtime-managed on all targets.
    /// Uses world image metadata only.
    ///
    /// # Errors
    /// Returns invalidArgument, ioNotFound, notSupported.
    ///
    /// # Security
    /// Requires `runtime.snapshot.read`.
    ///
    /// # Replay
    /// Deterministic.
    pub(crate) fn destack_runtime_image_list(
        &mut self,
        argument_world: WorldHandle,
        filter: HarnessValue<Option<ImageFilter>, Option<ImageFilterVm>>,
        after: Option<ImageId>,
        limit: Option<u32>,
    ) -> RuntimeResult<HarnessValue<NativeArray<ImageDescriptor>, VmArray<ImageDescriptorVm>>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let filter = filter.into_vm("filter")?;
                let out = runtime_vm::destack_runtime_image_list(
                    self.call_context,
                    context,
                    argument_world,
                    filter,
                    after,
                    limit,
                )?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let filter = filter.into_native("filter")?;
                let mut out = std::mem::MaybeUninit::<NativeArray<ImageDescriptor>>::uninit();
                unsafe {
                    runtime_native::destack_runtime_image_list(
                        self.call_context,
                        out.as_mut_ptr(),
                        argument_world,
                        filter,
                        after,
                        limit,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Describe one revision.
    ///
    /// Return one revision descriptor for one revision identifier in one world lineage.
    /// Descriptors include the captured trace sequence, clock instants, and optional image reference.
    ///
    /// # Platform
    /// Runtime-managed on all targets.
    /// Uses world lineage state only.
    ///
    /// # Errors
    /// Returns invalidArgument, ioNotFound, notSupported.
    ///
    /// # Security
    /// Requires `runtime.lineage.read`.
    ///
    /// # Replay
    /// Deterministic.
    pub(crate) fn destack_runtime_revision_describe(
        &mut self,
        argument_world: WorldHandle,
        revisionid: RevisionId,
    ) -> RuntimeResult<HarnessValue<RevisionDescriptor, RevisionDescriptorVm>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = runtime_vm::destack_runtime_revision_describe(
                    self.call_context,
                    context,
                    argument_world,
                    revisionid,
                )?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let mut out = std::mem::MaybeUninit::<RevisionDescriptor>::uninit();
                unsafe {
                    runtime_native::destack_runtime_revision_describe(
                        self.call_context,
                        out.as_mut_ptr(),
                        argument_world,
                        revisionid,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// List revisions in one world lineage.
    ///
    /// Enumerate revision descriptors in stable revision-id order starting after the optional cursor.
    /// When one branch identifier is provided, only revisions from that branch are returned.
    ///
    /// # Platform
    /// Runtime-managed on all targets.
    /// Uses world lineage state only.
    ///
    /// # Errors
    /// Returns invalidArgument, ioNotFound, notSupported.
    ///
    /// # Security
    /// Requires `runtime.lineage.read`.
    ///
    /// # Replay
    /// Deterministic.
    pub(crate) fn destack_runtime_revision_list(
        &mut self,
        argument_world: WorldHandle,
        filter: HarnessValue<Option<RevisionFilter>, Option<RevisionFilterVm>>,
        after: Option<RevisionId>,
        limit: Option<u32>,
    ) -> RuntimeResult<HarnessValue<NativeArray<RevisionDescriptor>, VmArray<RevisionDescriptorVm>>>
    {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let filter = filter.into_vm("filter")?;
                let out = runtime_vm::destack_runtime_revision_list(
                    self.call_context,
                    context,
                    argument_world,
                    filter,
                    after,
                    limit,
                )?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let filter = filter.into_native("filter")?;
                let mut out = std::mem::MaybeUninit::<NativeArray<RevisionDescriptor>>::uninit();
                unsafe {
                    runtime_native::destack_runtime_revision_list(
                        self.call_context,
                        out.as_mut_ptr(),
                        argument_world,
                        filter,
                        after,
                        limit,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Return the active branch for one world.
    ///
    /// Read the current active branch identifier for one live world.
    /// Worlds always execute on exactly one active branch at a time.
    ///
    /// # Platform
    /// Runtime-managed on all targets.
    /// Uses world lineage state only.
    ///
    /// # Errors
    /// Returns invalidArgument, ioNotFound, notSupported.
    ///
    /// # Security
    /// Requires `runtime.lineage.read`.
    ///
    /// # Replay
    /// Deterministic.
    pub(crate) fn destack_runtime_world_branch(
        &mut self,
        argument_world: WorldHandle,
    ) -> RuntimeResult<BranchId> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = runtime_vm::destack_runtime_world_branch(
                    self.call_context,
                    context,
                    argument_world,
                )?;
                Ok(out)
            }
            None => {
                let mut out = std::mem::MaybeUninit::<BranchId>::uninit();
                unsafe {
                    runtime_native::destack_runtime_world_branch(
                        self.call_context,
                        out.as_mut_ptr(),
                        argument_world,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// Fork one child world from one revision.
    ///
    /// Create one new world handle from one selected revision in the source world lineage.
    /// The forked world shares immutable lineage and image backing where possible and diverges only on later mutation.
    ///
    /// # Platform
    /// Runtime-managed on all targets.
    /// Uses world fork and lineage sharing logic.
    ///
    /// # Errors
    /// Returns invalidArgument, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `runtime.world.create`.
    ///
    /// # Replay
    /// Deterministic.
    pub(crate) fn destack_runtime_world_fork(
        &mut self,
        argument_world: WorldHandle,
        revisionid: RevisionId,
        name: HarnessValue<Option<NativeStringRef>, Option<vm::StringHandle>>,
        labels: HarnessValue<Option<NativeArray<RuntimeLabel>>, Option<VmArray<RuntimeLabelVm>>>,
    ) -> RuntimeResult<WorldHandle> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let name = name.into_vm("name")?;
                let labels = labels.into_vm("labels")?;
                let out = runtime_vm::destack_runtime_world_fork(
                    self.call_context,
                    context,
                    argument_world,
                    revisionid,
                    name,
                    labels,
                )?;
                Ok(out)
            }
            None => {
                let name = name.into_native("name")?;
                let labels = labels.into_native("labels")?;
                let mut out = std::mem::MaybeUninit::<WorldHandle>::uninit();
                unsafe {
                    runtime_native::destack_runtime_world_fork(
                        self.call_context,
                        out.as_mut_ptr(),
                        argument_world,
                        revisionid,
                        name,
                        labels,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// Return the active revision for one world.
    ///
    /// Read the current active revision identifier for one live world.
    /// The active revision reflects the current trace position and optional image materialization.
    ///
    /// # Platform
    /// Runtime-managed on all targets.
    /// Uses world lineage state only.
    ///
    /// # Errors
    /// Returns invalidArgument, ioNotFound, notSupported.
    ///
    /// # Security
    /// Requires `runtime.lineage.read`.
    ///
    /// # Replay
    /// Deterministic.
    pub(crate) fn destack_runtime_world_revision(
        &mut self,
        argument_world: WorldHandle,
    ) -> RuntimeResult<RevisionId> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = runtime_vm::destack_runtime_world_revision(
                    self.call_context,
                    context,
                    argument_world,
                )?;
                Ok(out)
            }
            None => {
                let mut out = std::mem::MaybeUninit::<RevisionId>::uninit();
                unsafe {
                    runtime_native::destack_runtime_world_revision(
                        self.call_context,
                        out.as_mut_ptr(),
                        argument_world,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// Rewind one world to one checkpoint.
    ///
    /// Restore one live world to the revision anchored by the requested checkpoint.
    /// Rewind keeps the same live world handle while replacing its active revision and image state.
    ///
    /// # Platform
    /// Runtime-managed on all targets.
    /// Uses world restore and lineage control logic.
    ///
    /// # Errors
    /// Returns invalidArgument, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `runtime.lineage.control`.
    ///
    /// # Replay
    /// Deterministic.
    pub(crate) fn destack_runtime_world_rewind_checkpoint(
        &mut self,
        argument_world: WorldHandle,
        checkpointid: CheckpointId,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => runtime_vm::destack_runtime_world_rewind_checkpoint(
                self.call_context,
                context,
                argument_world,
                checkpointid,
            ),
            None => unsafe {
                runtime_native::destack_runtime_world_rewind_checkpoint(
                    self.call_context,
                    argument_world,
                    checkpointid,
                )
            },
        }
    }

    /// Rewind one world to one revision.
    ///
    /// Restore one live world to the requested revision identifier.
    /// Rewind keeps the same live world handle while replacing its active revision and image state.
    ///
    /// # Platform
    /// Runtime-managed on all targets.
    /// Uses world restore and lineage control logic.
    ///
    /// # Errors
    /// Returns invalidArgument, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `runtime.lineage.control`.
    ///
    /// # Replay
    /// Deterministic.
    pub(crate) fn destack_runtime_world_rewind_revision(
        &mut self,
        argument_world: WorldHandle,
        revisionid: RevisionId,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => runtime_vm::destack_runtime_world_rewind_revision(
                self.call_context,
                context,
                argument_world,
                revisionid,
            ),
            None => unsafe {
                runtime_native::destack_runtime_world_rewind_revision(
                    self.call_context,
                    argument_world,
                    revisionid,
                )
            },
        }
    }

    /// Close one runtime observation subscription.
    ///
    /// Release one previously opened observation subscription and its buffered event state.
    /// Closing one observation feed does not affect the underlying world.
    ///
    /// # Platform
    /// Runtime-managed on all targets.
    /// Uses runtime observation teardown logic.
    ///
    /// # Errors
    /// Returns invalidArgument, ioNotFound, notSupported.
    ///
    /// # Security
    /// Requires `runtime.observation.read`.
    ///
    /// # Replay
    /// Deterministic.
    pub(crate) fn destack_runtime_observation_close(
        &mut self,
        handle: ObservationHandle,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                runtime_vm::destack_runtime_observation_close(self.call_context, context, handle)
            }
            None => unsafe {
                runtime_native::destack_runtime_observation_close(self.call_context, handle)
            },
        }
    }

    /// Read the next batch of observation records.
    ///
    /// Decode up to the requested limit of live observation records from one open subscription.
    /// Observation records may include topology, resource, scheduler, diagnostic, and profile events.
    ///
    /// # Platform
    /// Runtime-managed on all targets.
    /// Uses runtime observation feed state.
    ///
    /// # Errors
    /// Returns invalidArgument, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `runtime.observation.read`.
    ///
    /// # Replay
    /// Deterministic.
    pub(crate) fn destack_runtime_observation_next(
        &mut self,
        handle: ObservationHandle,
        limit: Option<u32>,
    ) -> RuntimeResult<HarnessValue<NativeArray<ObservationRecord>, VmArray<ObservationRecordVm>>>
    {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = runtime_vm::destack_runtime_observation_next(
                    self.call_context,
                    context,
                    handle,
                    limit,
                )?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let mut out = std::mem::MaybeUninit::<NativeArray<ObservationRecord>>::uninit();
                unsafe {
                    runtime_native::destack_runtime_observation_next(
                        self.call_context,
                        out.as_mut_ptr(),
                        handle,
                        limit,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Open one runtime observation subscription.
    ///
    /// Create one low-level subscription for live runtime observation events from one world.
    /// Observation feeds are distinct from the structured causal trace and may include higher-volume diagnostic streams.
    ///
    /// # Platform
    /// Runtime-managed on all targets.
    /// Uses runtime observation and subscription state.
    ///
    /// # Errors
    /// Returns invalidArgument, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `runtime.observation.read`.
    ///
    /// # Replay
    /// Deterministic.
    pub(crate) fn destack_runtime_observation_open(
        &mut self,
        argument_world: WorldHandle,
        options: HarnessValue<Option<ObservationOptions>, Option<ObservationOptionsVm>>,
    ) -> RuntimeResult<ObservationHandle> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let options = options.into_vm("options")?;
                let out = runtime_vm::destack_runtime_observation_open(
                    self.call_context,
                    context,
                    argument_world,
                    options,
                )?;
                Ok(out)
            }
            None => {
                let options = options.into_native("options")?;
                let mut out = std::mem::MaybeUninit::<ObservationHandle>::uninit();
                unsafe {
                    runtime_native::destack_runtime_observation_open(
                        self.call_context,
                        out.as_mut_ptr(),
                        argument_world,
                        options,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// Export one snapshot from one image.
    ///
    /// Serialize one materialized image into the requested snapshot format and return its snapshot identifier.
    /// Fast snapshots prefer runtime-specific backing, while portable snapshots favor compatibility metadata and transportability.
    ///
    /// # Platform
    /// Runtime-managed on all targets.
    /// Uses runtime snapshot export logic.
    ///
    /// # Errors
    /// Returns invalidArgument, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `runtime.snapshot.create`.
    ///
    /// # Replay
    /// Deterministic.
    pub(crate) fn destack_runtime_snapshot_create(
        &mut self,
        argument_world: WorldHandle,
        imageid: ImageId,
        format: SnapshotFormat,
    ) -> RuntimeResult<SnapshotId> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = runtime_vm::destack_runtime_snapshot_create(
                    self.call_context,
                    context,
                    argument_world,
                    imageid,
                    format,
                )?;
                Ok(out)
            }
            None => {
                let mut out = std::mem::MaybeUninit::<SnapshotId>::uninit();
                unsafe {
                    runtime_native::destack_runtime_snapshot_create(
                        self.call_context,
                        out.as_mut_ptr(),
                        argument_world,
                        imageid,
                        format,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// Describe one snapshot.
    ///
    /// Return one snapshot descriptor for one stored snapshot identifier in one world lineage.
    /// Descriptors summarize the backing image, format, and optional serialized size.
    ///
    /// # Platform
    /// Runtime-managed on all targets.
    /// Uses runtime snapshot metadata only.
    ///
    /// # Errors
    /// Returns invalidArgument, ioNotFound, notSupported.
    ///
    /// # Security
    /// Requires `runtime.snapshot.read`.
    ///
    /// # Replay
    /// Deterministic.
    pub(crate) fn destack_runtime_snapshot_describe(
        &mut self,
        argument_world: WorldHandle,
        snapshotid: SnapshotId,
    ) -> RuntimeResult<HarnessValue<SnapshotDescriptor, SnapshotDescriptorVm>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = runtime_vm::destack_runtime_snapshot_describe(
                    self.call_context,
                    context,
                    argument_world,
                    snapshotid,
                )?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let mut out = std::mem::MaybeUninit::<SnapshotDescriptor>::uninit();
                unsafe {
                    runtime_native::destack_runtime_snapshot_describe(
                        self.call_context,
                        out.as_mut_ptr(),
                        argument_world,
                        snapshotid,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Import one serialized snapshot payload.
    ///
    /// Decode one serialized snapshot payload and register it in the current world lineage.
    /// Imported snapshots may later be described, read again, or restored.
    ///
    /// # Platform
    /// Runtime-managed on all targets.
    /// Uses runtime snapshot import logic.
    ///
    /// # Errors
    /// Returns invalidArgument, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `runtime.snapshot.create`.
    ///
    /// # Replay
    /// Deterministic.
    pub(crate) fn destack_runtime_snapshot_import(
        &mut self,
        argument_world: WorldHandle,
        argument_payload: HarnessValue<NativeArray<u8>, VmArray<u8>>,
    ) -> RuntimeResult<SnapshotId> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let argument_payload = argument_payload.into_vm("argument_payload")?;
                let out = runtime_vm::destack_runtime_snapshot_import(
                    self.call_context,
                    context,
                    argument_world,
                    argument_payload,
                )?;
                Ok(out)
            }
            None => {
                let argument_payload = argument_payload.into_native("argument_payload")?;
                let mut out = std::mem::MaybeUninit::<SnapshotId>::uninit();
                unsafe {
                    runtime_native::destack_runtime_snapshot_import(
                        self.call_context,
                        out.as_mut_ptr(),
                        argument_world,
                        argument_payload,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// List snapshots in one world lineage.
    ///
    /// Enumerate snapshot descriptors in stable snapshot-id order starting after the optional cursor.
    /// This is the low-level bulk enumeration surface for stored snapshot artifacts.
    ///
    /// # Platform
    /// Runtime-managed on all targets.
    /// Uses runtime snapshot metadata only.
    ///
    /// # Errors
    /// Returns invalidArgument, ioNotFound, notSupported.
    ///
    /// # Security
    /// Requires `runtime.snapshot.read`.
    ///
    /// # Replay
    /// Deterministic.
    pub(crate) fn destack_runtime_snapshot_list(
        &mut self,
        argument_world: WorldHandle,
        after: Option<SnapshotId>,
        limit: Option<u32>,
    ) -> RuntimeResult<HarnessValue<NativeArray<SnapshotDescriptor>, VmArray<SnapshotDescriptorVm>>>
    {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = runtime_vm::destack_runtime_snapshot_list(
                    self.call_context,
                    context,
                    argument_world,
                    after,
                    limit,
                )?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let mut out = std::mem::MaybeUninit::<NativeArray<SnapshotDescriptor>>::uninit();
                unsafe {
                    runtime_native::destack_runtime_snapshot_list(
                        self.call_context,
                        out.as_mut_ptr(),
                        argument_world,
                        after,
                        limit,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Read one serialized snapshot payload.
    ///
    /// Return the encoded bytes for one stored snapshot identifier.
    /// Encoded bytes may be used for transport, persistence, or offline analysis.
    ///
    /// # Platform
    /// Runtime-managed on all targets.
    /// Uses runtime snapshot storage only.
    ///
    /// # Errors
    /// Returns invalidArgument, ioNotFound, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `runtime.snapshot.read`.
    ///
    /// # Replay
    /// Deterministic.
    pub(crate) fn destack_runtime_snapshot_read(
        &mut self,
        argument_world: WorldHandle,
        snapshotid: SnapshotId,
    ) -> RuntimeResult<HarnessValue<NativeArray<u8>, VmArray<u8>>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = runtime_vm::destack_runtime_snapshot_read(
                    self.call_context,
                    context,
                    argument_world,
                    snapshotid,
                )?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let mut out = std::mem::MaybeUninit::<NativeArray<u8>>::uninit();
                unsafe {
                    runtime_native::destack_runtime_snapshot_read(
                        self.call_context,
                        out.as_mut_ptr(),
                        argument_world,
                        snapshotid,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Restore one world from one image.
    ///
    /// Replace one live world's active state with the requested materialized image.
    /// Image restore keeps the same world handle while switching its active revision and state payload.
    ///
    /// # Platform
    /// Runtime-managed on all targets.
    /// Uses runtime image restore logic.
    ///
    /// # Errors
    /// Returns invalidArgument, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `runtime.snapshot.restore`.
    ///
    /// # Replay
    /// Deterministic.
    pub(crate) fn destack_runtime_restore_image(
        &mut self,
        argument_world: WorldHandle,
        imageid: ImageId,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => runtime_vm::destack_runtime_restore_image(
                self.call_context,
                context,
                argument_world,
                imageid,
            ),
            None => unsafe {
                runtime_native::destack_runtime_restore_image(
                    self.call_context,
                    argument_world,
                    imageid,
                )
            },
        }
    }

    /// Restore one world from one snapshot.
    ///
    /// Replace one live world's active state with the requested stored snapshot.
    /// Snapshot restore may reconstruct one image before activating the restored revision.
    ///
    /// # Platform
    /// Runtime-managed on all targets.
    /// Uses runtime snapshot restore logic.
    ///
    /// # Errors
    /// Returns invalidArgument, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `runtime.snapshot.restore`.
    ///
    /// # Replay
    /// Deterministic.
    pub(crate) fn destack_runtime_restore_snapshot(
        &mut self,
        argument_world: WorldHandle,
        snapshotid: SnapshotId,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => runtime_vm::destack_runtime_restore_snapshot(
                self.call_context,
                context,
                argument_world,
                snapshotid,
            ),
            None => unsafe {
                runtime_native::destack_runtime_restore_snapshot(
                    self.call_context,
                    argument_world,
                    snapshotid,
                )
            },
        }
    }

    /// Close one causal trace cursor.
    ///
    /// Release one previously opened trace cursor and its pinned reader state.
    /// Closing one cursor does not affect the underlying world trace.
    ///
    /// # Platform
    /// Runtime-managed on all targets.
    /// Uses world trace reader teardown logic.
    ///
    /// # Errors
    /// Returns invalidArgument, ioNotFound, notSupported.
    ///
    /// # Security
    /// Requires `runtime.trace.read`.
    ///
    /// # Replay
    /// Deterministic.
    pub(crate) fn destack_runtime_trace_close(
        &mut self,
        cursor: TraceCursorHandle,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                runtime_vm::destack_runtime_trace_close(self.call_context, context, cursor)
            }
            None => unsafe {
                runtime_native::destack_runtime_trace_close(self.call_context, cursor)
            },
        }
    }

    /// Describe one world's causal trace.
    ///
    /// Return one summary of the active branch and current sequence boundary for one world trace.
    /// This is the low-level causal trace metadata surface rather than one debug sink interface.
    ///
    /// # Platform
    /// Runtime-managed on all targets.
    /// Uses world trace state only.
    ///
    /// # Errors
    /// Returns invalidArgument, ioNotFound, notSupported.
    ///
    /// # Security
    /// Requires `runtime.trace.read`.
    ///
    /// # Replay
    /// Deterministic.
    pub(crate) fn destack_runtime_trace_describe(
        &mut self,
        argument_world: WorldHandle,
    ) -> RuntimeResult<HarnessValue<TraceDescriptor, TraceDescriptorVm>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = runtime_vm::destack_runtime_trace_describe(
                    self.call_context,
                    context,
                    argument_world,
                )?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let mut out = std::mem::MaybeUninit::<TraceDescriptor>::uninit();
                unsafe {
                    runtime_native::destack_runtime_trace_describe(
                        self.call_context,
                        out.as_mut_ptr(),
                        argument_world,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Append one explicit trace marker.
    ///
    /// Record one explicit causal marker in the active world trace and return its sequence number.
    /// Markers are part of the structured causal trace rather than one debug sink side channel.
    ///
    /// # Platform
    /// Runtime-managed on all targets.
    /// Uses world trace append logic.
    ///
    /// # Errors
    /// Returns invalidArgument, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `runtime.trace.control`.
    ///
    /// # Replay
    /// Deterministic.
    pub(crate) fn destack_runtime_trace_mark(
        &mut self,
        argument_world: WorldHandle,
        label: HarnessValue<NativeStringRef, vm::StringHandle>,
    ) -> RuntimeResult<TraceSequence> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let label = label.into_vm("label")?;
                let out = runtime_vm::destack_runtime_trace_mark(
                    self.call_context,
                    context,
                    argument_world,
                    label,
                )?;
                Ok(out)
            }
            None => {
                let label = label.into_native("label")?;
                let mut out = std::mem::MaybeUninit::<TraceSequence>::uninit();
                unsafe {
                    runtime_native::destack_runtime_trace_mark(
                        self.call_context,
                        out.as_mut_ptr(),
                        argument_world,
                        label,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// Read the next batch of causal trace records.
    ///
    /// Decode up to the requested limit of causal trace records from one open cursor.
    /// Record payloads are returned as structured metadata plus optional encoded payload bytes.
    ///
    /// # Platform
    /// Runtime-managed on all targets.
    /// Uses world trace reader state.
    ///
    /// # Errors
    /// Returns invalidArgument, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `runtime.trace.read`.
    ///
    /// # Replay
    /// Deterministic.
    pub(crate) fn destack_runtime_trace_next(
        &mut self,
        cursor: TraceCursorHandle,
        limit: Option<u32>,
    ) -> RuntimeResult<HarnessValue<NativeArray<TraceRecord>, VmArray<TraceRecordVm>>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = runtime_vm::destack_runtime_trace_next(
                    self.call_context,
                    context,
                    cursor,
                    limit,
                )?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let mut out = std::mem::MaybeUninit::<NativeArray<TraceRecord>>::uninit();
                unsafe {
                    runtime_native::destack_runtime_trace_next(
                        self.call_context,
                        out.as_mut_ptr(),
                        cursor,
                        limit,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Open one causal trace cursor.
    ///
    /// Create one cursor for reading structured causal trace records from one world.
    /// Cursors start at one optional sequence boundary and advance independently from the live trace head.
    ///
    /// # Platform
    /// Runtime-managed on all targets.
    /// Uses world trace reader state.
    ///
    /// # Errors
    /// Returns invalidArgument, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `runtime.trace.read`.
    ///
    /// # Replay
    /// Deterministic.
    pub(crate) fn destack_runtime_trace_open(
        &mut self,
        argument_world: WorldHandle,
        options: HarnessValue<Option<TraceCursorOptions>, Option<TraceCursorOptionsVm>>,
    ) -> RuntimeResult<TraceCursorHandle> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let options = options.into_vm("options")?;
                let out = runtime_vm::destack_runtime_trace_open(
                    self.call_context,
                    context,
                    argument_world,
                    options,
                )?;
                Ok(out)
            }
            None => {
                let options = options.into_native("options")?;
                let mut out = std::mem::MaybeUninit::<TraceCursorHandle>::uninit();
                unsafe {
                    runtime_native::destack_runtime_trace_open(
                        self.call_context,
                        out.as_mut_ptr(),
                        argument_world,
                        options,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// Seek one causal trace cursor to one checkpoint boundary.
    ///
    /// Reposition one open trace cursor so the next read starts at the trace sequence anchored by the requested checkpoint.
    /// This is the low-level checkpoint-to-trace bridge for debugging and replay tooling.
    ///
    /// # Platform
    /// Runtime-managed on all targets.
    /// Uses world trace and lineage metadata.
    ///
    /// # Errors
    /// Returns invalidArgument, ioNotFound, notSupported.
    ///
    /// # Security
    /// Requires `runtime.trace.read`.
    ///
    /// # Replay
    /// Deterministic.
    pub(crate) fn destack_runtime_trace_seek_checkpoint(
        &mut self,
        cursor: TraceCursorHandle,
        checkpointid: CheckpointId,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => runtime_vm::destack_runtime_trace_seek_checkpoint(
                self.call_context,
                context,
                cursor,
                checkpointid,
            ),
            None => unsafe {
                runtime_native::destack_runtime_trace_seek_checkpoint(
                    self.call_context,
                    cursor,
                    checkpointid,
                )
            },
        }
    }

    /// Seek one causal trace cursor to one revision boundary.
    ///
    /// Reposition one open trace cursor so the next read starts at the trace sequence captured by the requested revision.
    /// This is the low-level revision-to-trace bridge for debugging and replay tooling.
    ///
    /// # Platform
    /// Runtime-managed on all targets.
    /// Uses world trace and lineage metadata.
    ///
    /// # Errors
    /// Returns invalidArgument, ioNotFound, notSupported.
    ///
    /// # Security
    /// Requires `runtime.trace.read`.
    ///
    /// # Replay
    /// Deterministic.
    pub(crate) fn destack_runtime_trace_seek_revision(
        &mut self,
        cursor: TraceCursorHandle,
        revisionid: RevisionId,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => runtime_vm::destack_runtime_trace_seek_revision(
                self.call_context,
                context,
                cursor,
                revisionid,
            ),
            None => unsafe {
                runtime_native::destack_runtime_trace_seek_revision(
                    self.call_context,
                    cursor,
                    revisionid,
                )
            },
        }
    }

    /// Seek one causal trace cursor to one sequence.
    ///
    /// Reposition one open trace cursor so the next read starts at the requested sequence number.
    /// Seeking does not mutate the underlying world trace.
    ///
    /// # Platform
    /// Runtime-managed on all targets.
    /// Uses world trace reader state.
    ///
    /// # Errors
    /// Returns invalidArgument, ioNotFound, notSupported.
    ///
    /// # Security
    /// Requires `runtime.trace.read`.
    ///
    /// # Replay
    /// Deterministic.
    pub(crate) fn destack_runtime_trace_seek_sequence(
        &mut self,
        cursor: TraceCursorHandle,
        sequence: TraceSequence,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => runtime_vm::destack_runtime_trace_seek_sequence(
                self.call_context,
                context,
                cursor,
                sequence,
            ),
            None => unsafe {
                runtime_native::destack_runtime_trace_seek_sequence(
                    self.call_context,
                    cursor,
                    sequence,
                )
            },
        }
    }

    /// Return the current sequence position of one causal trace cursor.
    ///
    /// Read the next sequence number that would be returned by one open trace cursor.
    /// This is the low-level cursor-position primitive for trace tooling and resumption.
    ///
    /// # Platform
    /// Runtime-managed on all targets.
    /// Uses world trace reader state.
    ///
    /// # Errors
    /// Returns invalidArgument, ioNotFound, notSupported.
    ///
    /// # Security
    /// Requires `runtime.trace.read`.
    ///
    /// # Replay
    /// Deterministic.
    pub(crate) fn destack_runtime_trace_tell(
        &mut self,
        cursor: TraceCursorHandle,
    ) -> RuntimeResult<TraceSequence> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out =
                    runtime_vm::destack_runtime_trace_tell(self.call_context, context, cursor)?;
                Ok(out)
            }
            None => {
                let mut out = std::mem::MaybeUninit::<TraceSequence>::uninit();
                unsafe {
                    runtime_native::destack_runtime_trace_tell(
                        self.call_context,
                        out.as_mut_ptr(),
                        cursor,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }
}

/// Wrapper that carries one native or VM value for generated harness bindings.
pub(crate) enum HarnessValue<Native, Vm> {
    /// Native value variant.
    Native(Native),
    /// VM value variant.
    Vm(Vm),
}

impl<Native, Vm> HarnessValue<Native, Vm> {
    /// Convert one wrapper into one native value.
    fn into_native(self, label: &str) -> RuntimeResult<Native> {
        match self {
            HarnessValue::Native(value) => Ok(value),
            HarnessValue::Vm(_) => Err(RuntimeError::from(
                HarnessPlatformError::invalid_argument_value(
                    label,
                    "expected native harness value",
                ),
            )
            .boxed()),
        }
    }

    /// Convert one wrapper into one VM value.
    fn into_vm(self, label: &str) -> RuntimeResult<Vm> {
        match self {
            HarnessValue::Vm(value) => Ok(value),
            HarnessValue::Native(_) => Err(RuntimeError::from(
                HarnessPlatformError::invalid_argument_value(label, "expected VM harness value"),
            )
            .boxed()),
        }
    }
}
