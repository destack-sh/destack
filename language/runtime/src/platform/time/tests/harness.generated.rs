// generated by generate-bindings: test harness, do not edit

#![allow(dead_code)]
#![allow(unused_imports)]
#![allow(clippy::type_complexity)]

use crate::diagnostic::{RuntimeError, RuntimeResult};
use crate::platform::time::tests::TimeHarnessContext;
use crate::platform::time::{
    ClockId, ClockMetadata, ClockMetadataVm, ClockSource, SleepClock, TimerClock, TimerFlags,
    TimerOptions, TimerOptionsVm, native as time_native, vm as time_vm,
};
use crate::platform::{PlatformError as HarnessPlatformError, resource};
use destack_vm as vm;

impl<'call> TimeHarnessContext<'call> {
    /// Return the generated VM context if available.
    #[allow(clippy::mut_from_ref)]
    fn generated_vm_context_mut(&self) -> Option<&mut vm::ExternalCallContext<'_>> {
        self.vm_context
            .map(|context| unsafe { &mut *(context as *mut vm::ExternalCallContext<'_>) })
    }

    /// Return one standardized value payload for native and VM variants.
    pub(crate) fn harness_value<Native, Vm>(&self, native: Native) -> HarnessValue<Native, Vm> {
        HarnessValue::Native(native)
    }

    /// Return one standardized value payload for VM and native variants.
    pub(crate) fn harness_value_vm<Native, Vm>(&self, vm: Vm) -> HarnessValue<Native, Vm> {
        HarnessValue::Vm(vm)
    }

    /// Query one selected clock metadata.
    ///
    /// Return metadata for one clock identifier, including source and resolution.
    /// Metadata is normalized by runtime policy across host operating systems.
    ///
    /// # Platform
    /// Runtime-integrated clock available on Unix and Windows targets.
    /// Uses runtime clock registry metadata and host clock probes.
    ///
    /// # Errors
    /// Returns timeUnavailable, ioInterrupted, invalidArgument, notSupported.
    ///
    /// # Security
    /// Requires `time.monotonic.read`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_time_clock_metadata(
        &mut self,
        clock: ClockId,
    ) -> RuntimeResult<HarnessValue<ClockMetadata, ClockMetadataVm>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = time_vm::destack_time_clock_metadata(self.call_context, context, clock)?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let mut out = std::mem::MaybeUninit::<ClockMetadata>::uninit();
                unsafe {
                    time_native::destack_time_clock_metadata(
                        self.call_context,
                        out.as_mut_ptr(),
                        clock,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Return monotonic time in nanoseconds.
    ///
    /// Read a monotonic clock source that does not move backwards.
    /// Resolution and origin are platform-specific.
    ///
    /// # Platform
    /// Runtime-integrated clock available on Unix and Windows targets.
    /// Uses clock_gettime(CLOCK_MONOTONIC) on Unix and QueryPerformanceCounter on Windows.
    ///
    /// # Errors
    /// Returns timeUnavailable, ioInterrupted, invalidArgument, notSupported.
    ///
    /// # Security
    /// Requires `time.monotonic.read`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_time_mono_ns(&mut self) -> RuntimeResult<u64> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = time_vm::destack_time_mono_ns(self.call_context, context)?;
                Ok(out)
            }
            None => {
                let mut out = std::mem::MaybeUninit::<u64>::uninit();
                unsafe {
                    time_native::destack_time_mono_ns(self.call_context, out.as_mut_ptr())?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// Read one selected clock in nanoseconds.
    ///
    /// Read one clock value by explicit clock identifier.
    /// Clock identifier support varies by host and runtime configuration.
    ///
    /// # Platform
    /// Runtime-integrated clock available on Unix and Windows targets.
    /// Uses host clock APIs selected by clock identifier and runtime policy.
    ///
    /// # Errors
    /// Returns timeUnavailable, ioInterrupted, invalidArgument, notSupported.
    ///
    /// # Security
    /// Requires `time.monotonic.read`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_time_now_ns(&mut self, clock: ClockId) -> RuntimeResult<u64> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = time_vm::destack_time_now_ns(self.call_context, context, clock)?;
                Ok(out)
            }
            None => {
                let mut out = std::mem::MaybeUninit::<u64>::uninit();
                unsafe {
                    time_native::destack_time_now_ns(self.call_context, out.as_mut_ptr(), clock)?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// Return process CPU time in nanoseconds.
    ///
    /// Read CPU time consumed by the current process.
    /// Time excludes wall-clock idle periods and depends on host accounting granularity.
    ///
    /// # Platform
    /// Unix and Windows.
    /// Uses CLOCK_PROCESS_CPUTIME_ID or getrusage on Unix and GetProcessTimes on Windows.
    ///
    /// # Errors
    /// Returns timeUnavailable, ioInterrupted, invalidArgument, notSupported.
    ///
    /// # Security
    /// Requires `time.monotonic.read`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_time_process_cpu_ns(&mut self) -> RuntimeResult<u64> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = time_vm::destack_time_process_cpu_ns(self.call_context, context)?;
                Ok(out)
            }
            None => {
                let mut out = std::mem::MaybeUninit::<u64>::uninit();
                unsafe {
                    time_native::destack_time_process_cpu_ns(self.call_context, out.as_mut_ptr())?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// Return current thread CPU time in nanoseconds.
    ///
    /// Read CPU time consumed by the current thread.
    /// Time excludes wall-clock idle periods and depends on host accounting granularity.
    ///
    /// # Platform
    /// Unix and Windows.
    /// Uses CLOCK_THREAD_CPUTIME_ID or thread clocks on Unix and GetThreadTimes on Windows.
    ///
    /// # Errors
    /// Returns timeUnavailable, ioInterrupted, invalidArgument, notSupported.
    ///
    /// # Security
    /// Requires `time.monotonic.read`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_time_thread_cpu_ns(&mut self) -> RuntimeResult<u64> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = time_vm::destack_time_thread_cpu_ns(self.call_context, context)?;
                Ok(out)
            }
            None => {
                let mut out = std::mem::MaybeUninit::<u64>::uninit();
                unsafe {
                    time_native::destack_time_thread_cpu_ns(self.call_context, out.as_mut_ptr())?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// Return wall clock time in nanoseconds since the runtime epoch.
    ///
    /// Read wall-clock time with runtime policy controls for determinism and replay.
    /// Epoch choice and leap-second behavior follow the active runtime time policy.
    ///
    /// # Platform
    /// Runtime-integrated clock available on Unix and Windows targets.
    /// Uses clock_gettime(CLOCK_REALTIME) on Unix and GetSystemTimePreciseAsFileTime on Windows.
    ///
    /// # Errors
    /// Returns timeUnavailable, ioInterrupted, invalidArgument, notSupported.
    ///
    /// # Security
    /// Requires `time.wall.read`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_time_wall_ns(&mut self) -> RuntimeResult<u64> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = time_vm::destack_time_wall_ns(self.call_context, context)?;
                Ok(out)
            }
            None => {
                let mut out = std::mem::MaybeUninit::<u64>::uninit();
                unsafe {
                    time_native::destack_time_wall_ns(self.call_context, out.as_mut_ptr())?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// Sleep for at least the given duration in nanoseconds.
    ///
    /// Suspend the current execution context for the requested minimum duration.
    /// Wakeup precision depends on host timer granularity and scheduler latency.
    ///
    /// # Platform
    /// Runtime-integrated sleep available on Unix and Windows targets.
    /// Uses nanosleep(2) on Unix and waitable timers or Sleep on Windows.
    ///
    /// # Errors
    /// Returns timeUnavailable, ioInterrupted, invalidArgument, notSupported.
    ///
    /// # Security
    /// Requires `time.wall.sleep`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_time_sleep_ns(&mut self, duration: u64) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => time_vm::destack_time_sleep_ns(self.call_context, context, duration),
            None => unsafe { time_native::destack_time_sleep_ns(self.call_context, duration) },
        }
    }

    /// Sleep for at least the given duration on one clock domain.
    ///
    /// Suspend the current execution context for the requested duration on the selected clock domain.
    /// Wakeup precision depends on host timer granularity and scheduler latency.
    ///
    /// # Platform
    /// Runtime-integrated sleep available on Unix and Windows targets.
    /// Uses host sleep primitives selected by runtime for the requested clock domain.
    ///
    /// # Errors
    /// Returns timeUnavailable, ioInterrupted, invalidArgument, notSupported.
    ///
    /// # Security
    /// Requires `time.wall.sleep`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_time_sleep_on_ns(
        &mut self,
        duration: u64,
        clock: SleepClock,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                time_vm::destack_time_sleep_on_ns(self.call_context, context, duration, clock)
            }
            None => unsafe {
                time_native::destack_time_sleep_on_ns(self.call_context, duration, clock)
            },
        }
    }

    /// Sleep until the given wall-clock deadline in nanoseconds.
    ///
    /// Suspend execution until the requested deadline is reached in the active wall-clock domain.
    /// Deadline interpretation follows runtime wall-clock policy.
    ///
    /// # Platform
    /// Runtime-integrated sleep available on Unix and Windows targets.
    /// Uses clock_nanosleep on Unix when available and waitable deadline timers on Windows.
    ///
    /// # Errors
    /// Returns timeUnavailable, ioInterrupted, invalidArgument, notSupported.
    ///
    /// # Security
    /// Requires `time.wall.sleep`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_time_sleep_until_ns(&mut self, deadline: u64) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                time_vm::destack_time_sleep_until_ns(self.call_context, context, deadline)
            }
            None => unsafe {
                time_native::destack_time_sleep_until_ns(self.call_context, deadline)
            },
        }
    }

    /// Sleep until one deadline on one clock domain.
    ///
    /// Suspend execution until the requested deadline in the selected clock domain.
    /// Deadline interpretation follows runtime policy and host clock behavior.
    ///
    /// # Platform
    /// Runtime-integrated sleep available on Unix and Windows targets.
    /// Uses host deadline sleep primitives selected by runtime for the requested clock domain.
    ///
    /// # Errors
    /// Returns timeUnavailable, ioInterrupted, invalidArgument, notSupported.
    ///
    /// # Security
    /// Requires `time.wall.sleep`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_time_sleep_until_on_ns(
        &mut self,
        deadline: u64,
        clock: SleepClock,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                time_vm::destack_time_sleep_until_on_ns(self.call_context, context, deadline, clock)
            }
            None => unsafe {
                time_native::destack_time_sleep_until_on_ns(self.call_context, deadline, clock)
            },
        }
    }

    /// Schedule a timer for an absolute deadline.
    ///
    /// Register one timer that fires at a specific deadline in nanoseconds with explicit timer options.
    /// Deadline interpretation follows runtime wall-clock and monotonic policy.
    ///
    /// # Platform
    /// Runtime-integrated operation on Unix, Windows, and Wasi targets.
    /// Uses runtime scheduler timer queues.
    ///
    /// # Errors
    /// Returns timeUnavailable, invalidArgument, notSupported.
    ///
    /// # Security
    /// Requires `time.timer`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_time_timer_at(
        &mut self,
        deadlinens: u64,
        options: HarnessValue<TimerOptions, TimerOptionsVm>,
    ) -> RuntimeResult<resource::TimerHandle> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let options = options.into_vm("options")?;
                let out = time_vm::destack_time_timer_at(
                    self.call_context,
                    context,
                    deadlinens,
                    options,
                )?;
                Ok(out)
            }
            None => {
                let options = options.into_native("options")?;
                let mut out = std::mem::MaybeUninit::<resource::TimerHandle>::uninit();
                unsafe {
                    time_native::destack_time_timer_at(
                        self.call_context,
                        out.as_mut_ptr(),
                        deadlinens,
                        options,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// Cancel one scheduled timer.
    ///
    /// Remove one timer from the runtime scheduler.
    /// Cancellation is idempotent when supported by the runtime implementation.
    ///
    /// # Platform
    /// Runtime-level operation available on all native runtime targets.
    /// Uses runtime scheduler timer queues.
    ///
    /// # Errors
    /// Returns timeUnavailable, invalidArgument, notSupported.
    ///
    /// # Security
    /// Requires `time.timer`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_time_timer_cancel(
        &mut self,
        handle: resource::TimerHandle,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => time_vm::destack_time_timer_cancel(self.call_context, context, handle),
            None => unsafe { time_native::destack_time_timer_cancel(self.call_context, handle) },
        }
    }

    /// Schedule a repeating timer.
    ///
    /// Register one timer that fires repeatedly at a fixed period with explicit timer options.
    /// Drift and catch-up behavior follow runtime timer policy.
    ///
    /// # Platform
    /// Runtime-integrated operation on Unix, Windows, and Wasi targets.
    /// Uses runtime scheduler timer queues.
    ///
    /// # Errors
    /// Returns timeUnavailable, invalidArgument, notSupported.
    ///
    /// # Security
    /// Requires `time.timer`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_time_timer_interval(
        &mut self,
        periodns: u64,
        options: HarnessValue<TimerOptions, TimerOptionsVm>,
    ) -> RuntimeResult<resource::TimerHandle> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let options = options.into_vm("options")?;
                let out = time_vm::destack_time_timer_interval(
                    self.call_context,
                    context,
                    periodns,
                    options,
                )?;
                Ok(out)
            }
            None => {
                let options = options.into_native("options")?;
                let mut out = std::mem::MaybeUninit::<resource::TimerHandle>::uninit();
                unsafe {
                    time_native::destack_time_timer_interval(
                        self.call_context,
                        out.as_mut_ptr(),
                        periodns,
                        options,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// Return whether one timer is currently active.
    ///
    /// Read active-state metadata for one timer handle.
    /// Active state reflects runtime scheduler ownership and cancellation state.
    ///
    /// # Platform
    /// Runtime-level operation available on all native runtime targets.
    /// Uses runtime scheduler timer queues.
    ///
    /// # Errors
    /// Returns timeUnavailable, invalidArgument, notSupported.
    ///
    /// # Security
    /// Requires `time.timer`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_time_timer_is_active(
        &mut self,
        handle: resource::TimerHandle,
    ) -> RuntimeResult<bool> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out =
                    time_vm::destack_time_timer_is_active(self.call_context, context, handle)?;
                Ok(out)
            }
            None => {
                let mut out = std::mem::MaybeUninit::<bool>::uninit();
                unsafe {
                    time_native::destack_time_timer_is_active(
                        self.call_context,
                        out.as_mut_ptr(),
                        handle,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// Schedule a one-shot timer.
    ///
    /// Register one timer that fires once after a relative delay with explicit timer options.
    /// The handle remains valid until explicit cancel or one-shot completion.
    ///
    /// # Platform
    /// Runtime-integrated operation on Unix, Windows, and Wasi targets.
    /// Uses runtime scheduler timer queues.
    ///
    /// # Errors
    /// Returns timeUnavailable, invalidArgument, notSupported.
    ///
    /// # Security
    /// Requires `time.timer`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_time_timer_once(
        &mut self,
        delayns: u64,
        options: HarnessValue<TimerOptions, TimerOptionsVm>,
    ) -> RuntimeResult<resource::TimerHandle> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let options = options.into_vm("options")?;
                let out =
                    time_vm::destack_time_timer_once(self.call_context, context, delayns, options)?;
                Ok(out)
            }
            None => {
                let options = options.into_native("options")?;
                let mut out = std::mem::MaybeUninit::<resource::TimerHandle>::uninit();
                unsafe {
                    time_native::destack_time_timer_once(
                        self.call_context,
                        out.as_mut_ptr(),
                        delayns,
                        options,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// Pause one running timer.
    ///
    /// Suspend one timer without discarding its scheduling state.
    /// Resume behavior and retained delay follow the active runtime timer policy.
    ///
    /// # Platform
    /// Runtime-level operation available on all native runtime targets.
    /// Uses runtime scheduler timer queues.
    ///
    /// # Errors
    /// Returns timeUnavailable, invalidArgument, notSupported.
    ///
    /// # Security
    /// Requires `time.timer`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_time_timer_pause(
        &mut self,
        handle: resource::TimerHandle,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => time_vm::destack_time_timer_pause(self.call_context, context, handle),
            None => unsafe { time_native::destack_time_timer_pause(self.call_context, handle) },
        }
    }

    /// Return remaining timer delay in nanoseconds.
    ///
    /// Read remaining delay for one timer relative to its configured clock domain.
    /// Remaining delay is zero when timer has fired or is inactive.
    ///
    /// # Platform
    /// Runtime-level operation available on all native runtime targets.
    /// Uses runtime scheduler timer queues.
    ///
    /// # Errors
    /// Returns timeUnavailable, invalidArgument, notSupported.
    ///
    /// # Security
    /// Requires `time.timer`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_time_timer_remaining_ns(
        &mut self,
        handle: resource::TimerHandle,
    ) -> RuntimeResult<u64> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out =
                    time_vm::destack_time_timer_remaining_ns(self.call_context, context, handle)?;
                Ok(out)
            }
            None => {
                let mut out = std::mem::MaybeUninit::<u64>::uninit();
                unsafe {
                    time_native::destack_time_timer_remaining_ns(
                        self.call_context,
                        out.as_mut_ptr(),
                        handle,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// Reset one timer with a new relative delay.
    ///
    /// Replace one timer schedule with a new relative delay.
    /// Reset semantics preserve timer identity and replay ordering.
    ///
    /// # Platform
    /// Runtime-level operation available on all native runtime targets.
    /// Uses runtime scheduler timer queues.
    ///
    /// # Errors
    /// Returns timeUnavailable, invalidArgument, notSupported.
    ///
    /// # Security
    /// Requires `time.timer`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_time_timer_reset(
        &mut self,
        handle: resource::TimerHandle,
        delayns: u64,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                time_vm::destack_time_timer_reset(self.call_context, context, handle, delayns)
            }
            None => unsafe {
                time_native::destack_time_timer_reset(self.call_context, handle, delayns)
            },
        }
    }

    /// Resume one paused timer.
    ///
    /// Reactivate one paused timer in the runtime scheduler.
    /// Resume timing semantics follow runtime timer policy.
    ///
    /// # Platform
    /// Runtime-level operation available on all native runtime targets.
    /// Uses runtime scheduler timer queues.
    ///
    /// # Errors
    /// Returns timeUnavailable, invalidArgument, notSupported.
    ///
    /// # Security
    /// Requires `time.timer`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_time_timer_resume(
        &mut self,
        handle: resource::TimerHandle,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => time_vm::destack_time_timer_resume(self.call_context, context, handle),
            None => unsafe { time_native::destack_time_timer_resume(self.call_context, handle) },
        }
    }

    /// Update one timer interval period.
    ///
    /// Replace one interval timer period while preserving timer identity.
    /// Update semantics are runtime-defined for already-expired intervals.
    ///
    /// # Platform
    /// Runtime-level operation available on all native runtime targets.
    /// Uses runtime scheduler timer queues.
    ///
    /// # Errors
    /// Returns timeUnavailable, invalidArgument, notSupported.
    ///
    /// # Security
    /// Requires `time.timer`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_time_timer_update_interval(
        &mut self,
        handle: resource::TimerHandle,
        periodns: u64,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => time_vm::destack_time_timer_update_interval(
                self.call_context,
                context,
                handle,
                periodns,
            ),
            None => unsafe {
                time_native::destack_time_timer_update_interval(self.call_context, handle, periodns)
            },
        }
    }
}

/// Wrapper that carries one native or VM value for generated harness bindings.
pub(crate) enum HarnessValue<Native, Vm> {
    /// Native value variant.
    Native(Native),
    /// VM value variant.
    Vm(Vm),
}

impl<Native, Vm> HarnessValue<Native, Vm> {
    /// Convert one wrapper into one native value.
    fn into_native(self, label: &str) -> RuntimeResult<Native> {
        match self {
            HarnessValue::Native(value) => Ok(value),
            HarnessValue::Vm(_) => Err(RuntimeError::from(
                HarnessPlatformError::invalid_argument_value(
                    label,
                    "expected native harness value",
                ),
            )
            .boxed()),
        }
    }

    /// Convert one wrapper into one VM value.
    fn into_vm(self, label: &str) -> RuntimeResult<Vm> {
        match self {
            HarnessValue::Vm(value) => Ok(value),
            HarnessValue::Native(_) => Err(RuntimeError::from(
                HarnessPlatformError::invalid_argument_value(label, "expected VM harness value"),
            )
            .boxed()),
        }
    }
}
