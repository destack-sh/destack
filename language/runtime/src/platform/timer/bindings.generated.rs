// generated by generate-bindings: do not edit

#![allow(unused_imports)]
#![allow(clippy::clone_on_copy)]
#![allow(clippy::type_complexity)]

use crate::diagnostic::{RuntimeError, RuntimeResult};
use crate::platform::bindings::{
    BindingBlocking, BindingDescriptor, BindingRegistry, BindingReplayKind, BindingScope,
    NativeBinding, NativeBindingSet, ReplayPolicy, RuntimeWorld, native_call,
};
use crate::platform::timer::{
    TimerClock, TimerFdClock, TimerFdFlags, TimerFdSetFlags, TimerFdSpec, TimerFdSpecVm,
    TimerFlags, TimerOptions, TimerOptionsVm,
};
use crate::platform::{PlatformError, RuntimeStatus, abi as platform_abi};
use crate::vm_binding_set;
use destack_vm as vm;
use destack_vm::Isolate;

use crate::binding;
use crate::runtime::{RuntimeCallContext, with_runtime_call_context};

use serde::{Deserialize, Serialize};

use crate::platform::timer::runtime::{
    native as platform_runtime_native, vm as platform_runtime_vm,
};
use crate::platform::timer::simulated::{
    native as platform_simulated_native, vm as platform_simulated_vm,
};
use crate::platform::timer::{native as platform_native, vm as platform_vm};
use crate::platform::{resource as platform_resource, resource, timer as platform_timer};

/// Read a positional argument value.
#[allow(dead_code)]
fn arg_value(
    args: &[vm::Value],
    index: usize,
    name: &'static str,
    expected: &'static str,
) -> RuntimeResult<vm::Value> {
    let value = args.get(index).copied().ok_or_else(|| {
        RuntimeError::from(PlatformError::invalid_argument_type(name, expected)).boxed()
    })?;

    Ok(value)
}

/// Decode a boolean argument.
#[allow(dead_code)]
fn decode_bool(
    value: vm::Value,
    name: &'static str,
    expected: &'static str,
) -> RuntimeResult<bool> {
    value.as_bool().ok_or_else(|| {
        RuntimeError::from(PlatformError::invalid_argument_type(name, expected)).boxed()
    })
}

/// Decode an unsigned integer argument with an explicit width.
#[allow(dead_code)]
fn decode_uint(
    value: vm::Value,
    name: &'static str,
    expected: &'static str,
    bits: u8,
) -> RuntimeResult<u64> {
    let (raw, width) = value.as_uint_with_width().ok_or_else(|| {
        RuntimeError::from(PlatformError::invalid_argument_type(name, expected)).boxed()
    })?;
    if width != bits {
        return Err(
            RuntimeError::from(PlatformError::invalid_argument_type(name, expected)).boxed(),
        );
    }

    Ok(raw)
}

/// Decode a u8 argument.
#[allow(dead_code)]
fn decode_uint8(value: vm::Value, name: &'static str, expected: &'static str) -> RuntimeResult<u8> {
    Ok(decode_uint(value, name, expected, 8)? as u8)
}

/// Decode a u32 argument.
#[allow(dead_code)]
fn decode_uint32(
    value: vm::Value,
    name: &'static str,
    expected: &'static str,
) -> RuntimeResult<u32> {
    Ok(decode_uint(value, name, expected, 32)? as u32)
}

/// Decode a u64 argument.
#[allow(dead_code)]
fn decode_uint64(
    value: vm::Value,
    name: &'static str,
    expected: &'static str,
) -> RuntimeResult<u64> {
    decode_uint(value, name, expected, 64)
}

/// Decode arguments for destack.timer.control.cancel.
#[inline]
fn decode_destack_timer_control_cancel_args(
    _context: &mut vm::ExternalCallContext<'_>,
    args: &[vm::Value],
) -> RuntimeResult<(resource::TimerHandle,)> {
    let handle_value = arg_value(args, 0, "handle", "TimerHandle")?;
    let handle_inner_inner = decode_uint64(handle_value, "handle_inner_inner", "TimerHandle")?;
    let handle_inner = resource::ResourceId(handle_inner_inner);
    let handle = resource::TimerHandle(handle_inner);
    Ok((handle,))
}

/// Encode the result for destack.timer.control.cancel.
#[inline]
fn encode_destack_timer_control_cancel_result(
    _context: &mut vm::ExternalCallContext<'_>,
    result: RuntimeResult<()>,
) -> RuntimeResult<vm::Value> {
    result.map(|_| vm::Value::VOID)
}

/// Decode arguments for destack.timer.control.isActive.
#[inline]
fn decode_destack_timer_control_is_active_args(
    _context: &mut vm::ExternalCallContext<'_>,
    args: &[vm::Value],
) -> RuntimeResult<(resource::TimerHandle,)> {
    let handle_value = arg_value(args, 0, "handle", "TimerHandle")?;
    let handle_inner_inner = decode_uint64(handle_value, "handle_inner_inner", "TimerHandle")?;
    let handle_inner = resource::ResourceId(handle_inner_inner);
    let handle = resource::TimerHandle(handle_inner);
    Ok((handle,))
}

/// Encode the result for destack.timer.control.isActive.
#[inline]
fn encode_destack_timer_control_is_active_result(
    _context: &mut vm::ExternalCallContext<'_>,
    result: RuntimeResult<bool>,
) -> RuntimeResult<vm::Value> {
    result.map(vm::Value::bool)
}

/// Decode arguments for destack.timer.control.pause.
#[inline]
fn decode_destack_timer_control_pause_args(
    _context: &mut vm::ExternalCallContext<'_>,
    args: &[vm::Value],
) -> RuntimeResult<(resource::TimerHandle,)> {
    let handle_value = arg_value(args, 0, "handle", "TimerHandle")?;
    let handle_inner_inner = decode_uint64(handle_value, "handle_inner_inner", "TimerHandle")?;
    let handle_inner = resource::ResourceId(handle_inner_inner);
    let handle = resource::TimerHandle(handle_inner);
    Ok((handle,))
}

/// Encode the result for destack.timer.control.pause.
#[inline]
fn encode_destack_timer_control_pause_result(
    _context: &mut vm::ExternalCallContext<'_>,
    result: RuntimeResult<()>,
) -> RuntimeResult<vm::Value> {
    result.map(|_| vm::Value::VOID)
}

/// Decode arguments for destack.timer.control.remainingNs.
#[inline]
fn decode_destack_timer_control_remaining_ns_args(
    _context: &mut vm::ExternalCallContext<'_>,
    args: &[vm::Value],
) -> RuntimeResult<(resource::TimerHandle,)> {
    let handle_value = arg_value(args, 0, "handle", "TimerHandle")?;
    let handle_inner_inner = decode_uint64(handle_value, "handle_inner_inner", "TimerHandle")?;
    let handle_inner = resource::ResourceId(handle_inner_inner);
    let handle = resource::TimerHandle(handle_inner);
    Ok((handle,))
}

/// Encode the result for destack.timer.control.remainingNs.
#[inline]
fn encode_destack_timer_control_remaining_ns_result(
    _context: &mut vm::ExternalCallContext<'_>,
    result: RuntimeResult<u64>,
) -> RuntimeResult<vm::Value> {
    result.map(|value| vm::Value::uint(value, 64))
}

/// Decode arguments for destack.timer.control.reset.
#[inline]
fn decode_destack_timer_control_reset_args(
    _context: &mut vm::ExternalCallContext<'_>,
    args: &[vm::Value],
) -> RuntimeResult<(resource::TimerHandle, u64)> {
    let handle_value = arg_value(args, 0, "handle", "TimerHandle")?;
    let handle_inner_inner = decode_uint64(handle_value, "handle_inner_inner", "TimerHandle")?;
    let handle_inner = resource::ResourceId(handle_inner_inner);
    let handle = resource::TimerHandle(handle_inner);
    let delayns_value = arg_value(args, 1, "delayns", "uint64")?;
    let delayns = decode_uint64(delayns_value, "delayns", "uint64")?;
    Ok((handle, delayns))
}

/// Encode the result for destack.timer.control.reset.
#[inline]
fn encode_destack_timer_control_reset_result(
    _context: &mut vm::ExternalCallContext<'_>,
    result: RuntimeResult<()>,
) -> RuntimeResult<vm::Value> {
    result.map(|_| vm::Value::VOID)
}

/// Decode arguments for destack.timer.control.resume.
#[inline]
fn decode_destack_timer_control_resume_args(
    _context: &mut vm::ExternalCallContext<'_>,
    args: &[vm::Value],
) -> RuntimeResult<(resource::TimerHandle,)> {
    let handle_value = arg_value(args, 0, "handle", "TimerHandle")?;
    let handle_inner_inner = decode_uint64(handle_value, "handle_inner_inner", "TimerHandle")?;
    let handle_inner = resource::ResourceId(handle_inner_inner);
    let handle = resource::TimerHandle(handle_inner);
    Ok((handle,))
}

/// Encode the result for destack.timer.control.resume.
#[inline]
fn encode_destack_timer_control_resume_result(
    _context: &mut vm::ExternalCallContext<'_>,
    result: RuntimeResult<()>,
) -> RuntimeResult<vm::Value> {
    result.map(|_| vm::Value::VOID)
}

/// Decode arguments for destack.timer.control.updateInterval.
#[inline]
fn decode_destack_timer_control_update_interval_args(
    _context: &mut vm::ExternalCallContext<'_>,
    args: &[vm::Value],
) -> RuntimeResult<(resource::TimerHandle, u64)> {
    let handle_value = arg_value(args, 0, "handle", "TimerHandle")?;
    let handle_inner_inner = decode_uint64(handle_value, "handle_inner_inner", "TimerHandle")?;
    let handle_inner = resource::ResourceId(handle_inner_inner);
    let handle = resource::TimerHandle(handle_inner);
    let periodns_value = arg_value(args, 1, "periodns", "uint64")?;
    let periodns = decode_uint64(periodns_value, "periodns", "uint64")?;
    Ok((handle, periodns))
}

/// Encode the result for destack.timer.control.updateInterval.
#[inline]
fn encode_destack_timer_control_update_interval_result(
    _context: &mut vm::ExternalCallContext<'_>,
    result: RuntimeResult<()>,
) -> RuntimeResult<vm::Value> {
    result.map(|_| vm::Value::VOID)
}

/// Decode arguments for destack.timer.fd.close.
#[inline]
fn decode_destack_timer_fd_close_args(
    _context: &mut vm::ExternalCallContext<'_>,
    args: &[vm::Value],
) -> RuntimeResult<(resource::TimerFdHandle,)> {
    let handle_value = arg_value(args, 0, "handle", "TimerFdHandle")?;
    let handle_inner_inner = decode_uint64(handle_value, "handle_inner_inner", "TimerFdHandle")?;
    let handle_inner = resource::ResourceId(handle_inner_inner);
    let handle = resource::TimerFdHandle(handle_inner);
    Ok((handle,))
}

/// Encode the result for destack.timer.fd.close.
#[inline]
fn encode_destack_timer_fd_close_result(
    _context: &mut vm::ExternalCallContext<'_>,
    result: RuntimeResult<()>,
) -> RuntimeResult<vm::Value> {
    result.map(|_| vm::Value::VOID)
}

/// Decode arguments for destack.timer.fd.get.
#[inline]
fn decode_destack_timer_fd_get_args(
    _context: &mut vm::ExternalCallContext<'_>,
    args: &[vm::Value],
) -> RuntimeResult<(resource::TimerFdHandle,)> {
    let handle_value = arg_value(args, 0, "handle", "TimerFdHandle")?;
    let handle_inner_inner = decode_uint64(handle_value, "handle_inner_inner", "TimerFdHandle")?;
    let handle_inner = resource::ResourceId(handle_inner_inner);
    let handle = resource::TimerFdHandle(handle_inner);
    Ok((handle,))
}

/// Encode the result for destack.timer.fd.get.
#[inline]
fn encode_destack_timer_fd_get_result(
    context: &mut vm::ExternalCallContext<'_>,
    result: RuntimeResult<TimerFdSpecVm>,
) -> RuntimeResult<vm::Value> {
    result.map(|value| {
        let field_0 = vm::Value::uint(value.initial_ns, 64);
        let field_1 = vm::Value::uint(value.interval_ns, 64);
        context.allocate_aggregate(vec![field_0, field_1])
    })
}

/// Decode arguments for destack.timer.fd.open.
#[inline]
fn decode_destack_timer_fd_open_args(
    _context: &mut vm::ExternalCallContext<'_>,
    args: &[vm::Value],
) -> RuntimeResult<(TimerFdClock, TimerFdFlags)> {
    let clock_value = arg_value(args, 0, "clock", "TimerFdClock")?;
    let clock_raw = decode_uint8(clock_value, "clock_raw", "TimerFdClock")?;
    let clock = match clock_raw {
        1u8 => TimerFdClock::Realtime,
        2u8 => TimerFdClock::Monotonic,
        3u8 => TimerFdClock::Boottime,
        _ => {
            return Err(RuntimeError::from(PlatformError::invalid_argument_value(
                "clock",
                "unknown TimerFdClock value",
            ))
            .boxed());
        }
    };
    let flags_value = arg_value(args, 1, "flags", "TimerFdFlags")?;
    let flags_inner = decode_uint32(flags_value, "flags_inner", "TimerFdFlags")?;
    let flags = TimerFdFlags(flags_inner);
    Ok((clock, flags))
}

/// Encode the result for destack.timer.fd.open.
#[inline]
fn encode_destack_timer_fd_open_result(
    _context: &mut vm::ExternalCallContext<'_>,
    result: RuntimeResult<resource::TimerFdHandle>,
) -> RuntimeResult<vm::Value> {
    result.map(|value| vm::Value::uint(value.0.0, 64))
}

/// Decode arguments for destack.timer.fd.read.
#[inline]
fn decode_destack_timer_fd_read_args(
    _context: &mut vm::ExternalCallContext<'_>,
    args: &[vm::Value],
) -> RuntimeResult<(resource::TimerFdHandle,)> {
    let handle_value = arg_value(args, 0, "handle", "TimerFdHandle")?;
    let handle_inner_inner = decode_uint64(handle_value, "handle_inner_inner", "TimerFdHandle")?;
    let handle_inner = resource::ResourceId(handle_inner_inner);
    let handle = resource::TimerFdHandle(handle_inner);
    Ok((handle,))
}

/// Encode the result for destack.timer.fd.read.
#[inline]
fn encode_destack_timer_fd_read_result(
    _context: &mut vm::ExternalCallContext<'_>,
    result: RuntimeResult<u64>,
) -> RuntimeResult<vm::Value> {
    result.map(|value| vm::Value::uint(value, 64))
}

/// Decode arguments for destack.timer.fd.set.
#[inline]
fn decode_destack_timer_fd_set_args(
    context: &mut vm::ExternalCallContext<'_>,
    args: &[vm::Value],
) -> RuntimeResult<(resource::TimerFdHandle, TimerFdSpecVm, TimerFdSetFlags)> {
    let handle_value = arg_value(args, 0, "handle", "TimerFdHandle")?;
    let handle_inner_inner = decode_uint64(handle_value, "handle_inner_inner", "TimerFdHandle")?;
    let handle_inner = resource::ResourceId(handle_inner_inner);
    let handle = resource::TimerFdHandle(handle_inner);
    let spec_value = arg_value(args, 1, "spec", "TimerFdSpec")?;
    let spec = {
        if spec_value.tag() != vm::ValueTag::Aggregate {
            return Err(RuntimeError::from(PlatformError::invalid_argument_type(
                "spec",
                "TimerFdSpec",
            ))
            .boxed());
        }
        let slots = context
            .aggregate_slots(spec_value)
            .map_err(|error| RuntimeError::from(error).boxed())?;
        if slots.len() != 2 {
            return Err(RuntimeError::from(PlatformError::invalid_argument_value(
                "spec",
                "expected 2 fields",
            ))
            .boxed());
        }
        let spec_initial_ns = decode_uint64(slots[0], "spec_initial_ns", "initialNs")?;
        let spec_interval_ns = decode_uint64(slots[1], "spec_interval_ns", "intervalNs")?;
        TimerFdSpecVm {
            initial_ns: spec_initial_ns,
            interval_ns: spec_interval_ns,
        }
    };
    let flags_value = arg_value(args, 2, "flags", "TimerFdSetFlags")?;
    let flags_inner = decode_uint32(flags_value, "flags_inner", "TimerFdSetFlags")?;
    let flags = TimerFdSetFlags(flags_inner);
    Ok((handle, spec, flags))
}

/// Encode the result for destack.timer.fd.set.
#[inline]
fn encode_destack_timer_fd_set_result(
    _context: &mut vm::ExternalCallContext<'_>,
    result: RuntimeResult<()>,
) -> RuntimeResult<vm::Value> {
    result.map(|_| vm::Value::VOID)
}

/// Decode arguments for destack.timer.schedule.at.
#[inline]
fn decode_destack_timer_schedule_at_args(
    context: &mut vm::ExternalCallContext<'_>,
    args: &[vm::Value],
) -> RuntimeResult<(u64, TimerOptionsVm)> {
    let deadlinens_value = arg_value(args, 0, "deadlinens", "uint64")?;
    let deadlinens = decode_uint64(deadlinens_value, "deadlinens", "uint64")?;
    let options_value = arg_value(args, 1, "options", "TimerOptions")?;
    let options = {
        if options_value.tag() != vm::ValueTag::Aggregate {
            return Err(RuntimeError::from(PlatformError::invalid_argument_type(
                "options",
                "TimerOptions",
            ))
            .boxed());
        }
        let slots = context
            .aggregate_slots(options_value)
            .map_err(|error| RuntimeError::from(error).boxed())?;
        if slots.len() != 2 {
            return Err(RuntimeError::from(PlatformError::invalid_argument_value(
                "options",
                "expected 2 fields",
            ))
            .boxed());
        }
        let options_clock_raw = decode_uint8(slots[0], "options_clock_raw", "clock")?;
        let options_clock = match options_clock_raw {
            1u8 => TimerClock::Wall,
            2u8 => TimerClock::Monotonic,
            _ => {
                return Err(RuntimeError::from(PlatformError::invalid_argument_value(
                    "options_clock",
                    "unknown TimerClock value",
                ))
                .boxed());
            }
        };
        let options_flags_inner = decode_uint32(slots[1], "options_flags_inner", "flags")?;
        let options_flags = TimerFlags(options_flags_inner);
        TimerOptionsVm {
            clock: options_clock,
            flags: options_flags,
        }
    };
    Ok((deadlinens, options))
}

/// Encode the result for destack.timer.schedule.at.
#[inline]
fn encode_destack_timer_schedule_at_result(
    _context: &mut vm::ExternalCallContext<'_>,
    result: RuntimeResult<resource::TimerHandle>,
) -> RuntimeResult<vm::Value> {
    result.map(|value| vm::Value::uint(value.0.0, 64))
}

/// Decode arguments for destack.timer.schedule.interval.
#[inline]
fn decode_destack_timer_schedule_interval_args(
    context: &mut vm::ExternalCallContext<'_>,
    args: &[vm::Value],
) -> RuntimeResult<(u64, TimerOptionsVm)> {
    let periodns_value = arg_value(args, 0, "periodns", "uint64")?;
    let periodns = decode_uint64(periodns_value, "periodns", "uint64")?;
    let options_value = arg_value(args, 1, "options", "TimerOptions")?;
    let options = {
        if options_value.tag() != vm::ValueTag::Aggregate {
            return Err(RuntimeError::from(PlatformError::invalid_argument_type(
                "options",
                "TimerOptions",
            ))
            .boxed());
        }
        let slots = context
            .aggregate_slots(options_value)
            .map_err(|error| RuntimeError::from(error).boxed())?;
        if slots.len() != 2 {
            return Err(RuntimeError::from(PlatformError::invalid_argument_value(
                "options",
                "expected 2 fields",
            ))
            .boxed());
        }
        let options_clock_raw = decode_uint8(slots[0], "options_clock_raw", "clock")?;
        let options_clock = match options_clock_raw {
            1u8 => TimerClock::Wall,
            2u8 => TimerClock::Monotonic,
            _ => {
                return Err(RuntimeError::from(PlatformError::invalid_argument_value(
                    "options_clock",
                    "unknown TimerClock value",
                ))
                .boxed());
            }
        };
        let options_flags_inner = decode_uint32(slots[1], "options_flags_inner", "flags")?;
        let options_flags = TimerFlags(options_flags_inner);
        TimerOptionsVm {
            clock: options_clock,
            flags: options_flags,
        }
    };
    Ok((periodns, options))
}

/// Encode the result for destack.timer.schedule.interval.
#[inline]
fn encode_destack_timer_schedule_interval_result(
    _context: &mut vm::ExternalCallContext<'_>,
    result: RuntimeResult<resource::TimerHandle>,
) -> RuntimeResult<vm::Value> {
    result.map(|value| vm::Value::uint(value.0.0, 64))
}

/// Decode arguments for destack.timer.schedule.once.
#[inline]
fn decode_destack_timer_schedule_once_args(
    context: &mut vm::ExternalCallContext<'_>,
    args: &[vm::Value],
) -> RuntimeResult<(u64, TimerOptionsVm)> {
    let delayns_value = arg_value(args, 0, "delayns", "uint64")?;
    let delayns = decode_uint64(delayns_value, "delayns", "uint64")?;
    let options_value = arg_value(args, 1, "options", "TimerOptions")?;
    let options = {
        if options_value.tag() != vm::ValueTag::Aggregate {
            return Err(RuntimeError::from(PlatformError::invalid_argument_type(
                "options",
                "TimerOptions",
            ))
            .boxed());
        }
        let slots = context
            .aggregate_slots(options_value)
            .map_err(|error| RuntimeError::from(error).boxed())?;
        if slots.len() != 2 {
            return Err(RuntimeError::from(PlatformError::invalid_argument_value(
                "options",
                "expected 2 fields",
            ))
            .boxed());
        }
        let options_clock_raw = decode_uint8(slots[0], "options_clock_raw", "clock")?;
        let options_clock = match options_clock_raw {
            1u8 => TimerClock::Wall,
            2u8 => TimerClock::Monotonic,
            _ => {
                return Err(RuntimeError::from(PlatformError::invalid_argument_value(
                    "options_clock",
                    "unknown TimerClock value",
                ))
                .boxed());
            }
        };
        let options_flags_inner = decode_uint32(slots[1], "options_flags_inner", "flags")?;
        let options_flags = TimerFlags(options_flags_inner);
        TimerOptionsVm {
            clock: options_clock,
            flags: options_flags,
        }
    };
    Ok((delayns, options))
}

/// Encode the result for destack.timer.schedule.once.
#[inline]
fn encode_destack_timer_schedule_once_result(
    _context: &mut vm::ExternalCallContext<'_>,
    result: RuntimeResult<resource::TimerHandle>,
) -> RuntimeResult<vm::Value> {
    result.map(|value| vm::Value::uint(value.0.0, 64))
}

/// Replay payload for destack.timer.control.cancel.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
struct TimerControlCancelReplay {
    /// Replay result payload.
    pub result: Result<(), PlatformError>,
}

/// Replay payload for destack.timer.control.isActive.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
struct TimerControlIsActiveReplay {
    /// Replay result payload.
    pub result: Result<bool, PlatformError>,
}

/// Replay payload for destack.timer.control.pause.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
struct TimerControlPauseReplay {
    /// Replay result payload.
    pub result: Result<(), PlatformError>,
}

/// Replay payload for destack.timer.control.remainingNs.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
struct TimerControlRemainingNsReplay {
    /// Replay result payload.
    pub result: Result<u64, PlatformError>,
}

/// Replay payload for destack.timer.control.reset.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
struct TimerControlResetReplay {
    /// Replay result payload.
    pub result: Result<(), PlatformError>,
}

/// Replay payload for destack.timer.control.resume.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
struct TimerControlResumeReplay {
    /// Replay result payload.
    pub result: Result<(), PlatformError>,
}

/// Replay payload for destack.timer.control.updateInterval.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
struct TimerControlUpdateIntervalReplay {
    /// Replay result payload.
    pub result: Result<(), PlatformError>,
}

/// Replay payload for destack.timer.fd.close.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
struct TimerFdCloseReplay {
    /// Replay result payload.
    pub result: Result<(), PlatformError>,
}

/// Replay payload for destack.timer.fd.get.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
struct TimerFdGetReplay {
    /// Replay result payload.
    pub result: Result<TimerFdSpec, PlatformError>,
}

/// Replay payload for destack.timer.fd.open.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
struct TimerFdOpenReplay {
    /// Replay result payload.
    pub result: Result<resource::TimerFdHandle, PlatformError>,
}

/// Replay payload for destack.timer.fd.read.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
struct TimerFdReadReplay {
    /// Replay result payload.
    pub result: Result<u64, PlatformError>,
}

/// Replay payload for destack.timer.fd.set.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
struct TimerFdSetReplay {
    /// Replay result payload.
    pub result: Result<(), PlatformError>,
}

/// Replay payload for destack.timer.schedule.at.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
struct TimerScheduleAtReplay {
    /// Replay result payload.
    pub result: Result<resource::TimerHandle, PlatformError>,
}

/// Replay payload for destack.timer.schedule.interval.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
struct TimerScheduleIntervalReplay {
    /// Replay result payload.
    pub result: Result<resource::TimerHandle, PlatformError>,
}

/// Replay payload for destack.timer.schedule.once.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
struct TimerScheduleOnceReplay {
    /// Replay result payload.
    pub result: Result<resource::TimerHandle, PlatformError>,
}

/// Binding descriptor for destack.timer.control.cancel.
pub const TIMER_CONTROL_CANCEL: BindingDescriptor =
    BindingDescriptor::external_with_requires_and_behavior(
        "destack.timer.control.cancel",
        "export function cancel(handle: TimerHandle): Result<void, PlatformError>",
        ReplayPolicy::Recordable,
        BindingReplayKind::Regular,
        &["time.timer"],
        BindingScope::Runtime,
        BindingBlocking::Never,
    )
    .with_host_platforms(&[
        "android",
        "dragonfly",
        "freebsd",
        "haiku",
        "illumos",
        "ios",
        "linux",
        "macos",
        "netbsd",
        "openbsd",
        "solaris",
        "wasi",
        "windows",
    ]);

/// Binding descriptor for destack.timer.control.isActive.
pub const TIMER_CONTROL_IS_ACTIVE: BindingDescriptor =
    BindingDescriptor::external_with_requires_and_behavior(
        "destack.timer.control.isActive",
        "export function isActive(handle: TimerHandle): Result<boolean, PlatformError>",
        ReplayPolicy::Recordable,
        BindingReplayKind::Regular,
        &["time.timer"],
        BindingScope::Runtime,
        BindingBlocking::Never,
    )
    .with_host_platforms(&[
        "android",
        "dragonfly",
        "freebsd",
        "haiku",
        "illumos",
        "ios",
        "linux",
        "macos",
        "netbsd",
        "openbsd",
        "solaris",
        "wasi",
        "windows",
    ]);

/// Binding descriptor for destack.timer.control.pause.
pub const TIMER_CONTROL_PAUSE: BindingDescriptor =
    BindingDescriptor::external_with_requires_and_behavior(
        "destack.timer.control.pause",
        "export function pause(handle: TimerHandle): Result<void, PlatformError>",
        ReplayPolicy::Recordable,
        BindingReplayKind::Regular,
        &["time.timer"],
        BindingScope::Runtime,
        BindingBlocking::Never,
    )
    .with_host_platforms(&[
        "android",
        "dragonfly",
        "freebsd",
        "haiku",
        "illumos",
        "ios",
        "linux",
        "macos",
        "netbsd",
        "openbsd",
        "solaris",
        "wasi",
        "windows",
    ]);

/// Binding descriptor for destack.timer.control.remainingNs.
pub const TIMER_CONTROL_REMAINING_NS: BindingDescriptor =
    BindingDescriptor::external_with_requires_and_behavior(
        "destack.timer.control.remainingNs",
        "export function remainingNs(handle: TimerHandle): Result<uint64, PlatformError>",
        ReplayPolicy::Recordable,
        BindingReplayKind::Regular,
        &["time.timer"],
        BindingScope::Runtime,
        BindingBlocking::Never,
    )
    .with_host_platforms(&[
        "android",
        "dragonfly",
        "freebsd",
        "haiku",
        "illumos",
        "ios",
        "linux",
        "macos",
        "netbsd",
        "openbsd",
        "solaris",
        "wasi",
        "windows",
    ]);

/// Binding descriptor for destack.timer.control.reset.
pub const TIMER_CONTROL_RESET: BindingDescriptor =
    BindingDescriptor::external_with_requires_and_behavior(
        "destack.timer.control.reset",
        "export function reset(handle: TimerHandle, delayNs: uint64): Result<void, PlatformError>",
        ReplayPolicy::Recordable,
        BindingReplayKind::Regular,
        &["time.timer"],
        BindingScope::Runtime,
        BindingBlocking::Never,
    )
    .with_host_platforms(&[
        "android",
        "dragonfly",
        "freebsd",
        "haiku",
        "illumos",
        "ios",
        "linux",
        "macos",
        "netbsd",
        "openbsd",
        "solaris",
        "wasi",
        "windows",
    ]);

/// Binding descriptor for destack.timer.control.resume.
pub const TIMER_CONTROL_RESUME: BindingDescriptor =
    BindingDescriptor::external_with_requires_and_behavior(
        "destack.timer.control.resume",
        "export function resume(handle: TimerHandle): Result<void, PlatformError>",
        ReplayPolicy::Recordable,
        BindingReplayKind::Regular,
        &["time.timer"],
        BindingScope::Runtime,
        BindingBlocking::Never,
    )
    .with_host_platforms(&[
        "android",
        "dragonfly",
        "freebsd",
        "haiku",
        "illumos",
        "ios",
        "linux",
        "macos",
        "netbsd",
        "openbsd",
        "solaris",
        "wasi",
        "windows",
    ]);

/// Binding descriptor for destack.timer.control.updateInterval.
pub const TIMER_CONTROL_UPDATE_INTERVAL: BindingDescriptor = BindingDescriptor::external_with_requires_and_behavior(
    "destack.timer.control.updateInterval",
    "export function updateInterval(handle: TimerHandle, periodNs: uint64): Result<void, PlatformError>",
    ReplayPolicy::Recordable,
    BindingReplayKind::Regular,
    &["time.timer"],
    BindingScope::Runtime,
    BindingBlocking::Never,
)
    .with_host_platforms(&["android", "dragonfly", "freebsd", "haiku", "illumos", "ios", "linux", "macos", "netbsd", "openbsd", "solaris", "wasi", "windows"]);

/// Binding descriptor for destack.timer.fd.close.
pub const TIMER_FD_CLOSE: BindingDescriptor =
    BindingDescriptor::external_with_requires_and_behavior(
        "destack.timer.fd.close",
        "export function timerFdClose(handle: TimerFdHandle): Result<void, PlatformError>",
        ReplayPolicy::Recordable,
        BindingReplayKind::Regular,
        &["time.timerfd"],
        BindingScope::Host,
        BindingBlocking::Sometimes,
    )
    .with_host_platforms(&[
        "android",
        "dragonfly",
        "freebsd",
        "haiku",
        "illumos",
        "ios",
        "linux",
        "macos",
        "netbsd",
        "openbsd",
        "solaris",
        "windows",
    ]);

/// Binding descriptor for destack.timer.fd.get.
pub const TIMER_FD_GET: BindingDescriptor = BindingDescriptor::external_with_requires_and_behavior(
    "destack.timer.fd.get",
    "export function timerFdGet(handle: TimerFdHandle): Result<TimerFdSpec, PlatformError>",
    ReplayPolicy::Recordable,
    BindingReplayKind::Regular,
    &["time.timerfd"],
    BindingScope::Host,
    BindingBlocking::Sometimes,
)
.with_host_platforms(&[
    "android",
    "dragonfly",
    "freebsd",
    "haiku",
    "illumos",
    "ios",
    "linux",
    "macos",
    "netbsd",
    "openbsd",
    "solaris",
    "windows",
]);

/// Binding descriptor for destack.timer.fd.open.
pub const TIMER_FD_OPEN: BindingDescriptor = BindingDescriptor::external_with_requires_and_behavior(
    "destack.timer.fd.open",
    "export function timerFdOpen(clock: TimerFdClock, flags: TimerFdFlags): Result<TimerFdHandle, PlatformError>",
    ReplayPolicy::Recordable,
    BindingReplayKind::Regular,
    &["time.timerfd"],
    BindingScope::Host,
    BindingBlocking::Sometimes,
)
    .with_host_platforms(&["android", "dragonfly", "freebsd", "haiku", "illumos", "ios", "linux", "macos", "netbsd", "openbsd", "solaris", "windows"]);

/// Binding descriptor for destack.timer.fd.read.
pub const TIMER_FD_READ: BindingDescriptor =
    BindingDescriptor::external_with_requires_and_behavior(
        "destack.timer.fd.read",
        "export function timerFdRead(handle: TimerFdHandle): Result<uint64, PlatformError>",
        ReplayPolicy::Recordable,
        BindingReplayKind::Regular,
        &["time.timerfd"],
        BindingScope::Host,
        BindingBlocking::Sometimes,
    )
    .with_host_platforms(&[
        "android",
        "dragonfly",
        "freebsd",
        "haiku",
        "illumos",
        "ios",
        "linux",
        "macos",
        "netbsd",
        "openbsd",
        "solaris",
        "windows",
    ]);

/// Binding descriptor for destack.timer.fd.set.
pub const TIMER_FD_SET: BindingDescriptor = BindingDescriptor::external_with_requires_and_behavior(
    "destack.timer.fd.set",
    "export function timerFdSet(handle: TimerFdHandle, spec: TimerFdSpec, flags: TimerFdSetFlags): Result<void, PlatformError>",
    ReplayPolicy::Recordable,
    BindingReplayKind::Regular,
    &["time.timerfd"],
    BindingScope::Host,
    BindingBlocking::Sometimes,
)
    .with_host_platforms(&["android", "dragonfly", "freebsd", "haiku", "illumos", "ios", "linux", "macos", "netbsd", "openbsd", "solaris", "windows"]);

/// Binding descriptor for destack.timer.schedule.at.
pub const TIMER_SCHEDULE_AT: BindingDescriptor = BindingDescriptor::external_with_requires_and_behavior(
    "destack.timer.schedule.at",
    "export function at(deadlineNs: uint64, options: TimerOptions): Result<TimerHandle, PlatformError>",
    ReplayPolicy::Recordable,
    BindingReplayKind::Regular,
    &["time.timer"],
    BindingScope::Runtime,
    BindingBlocking::Never,
)
    .with_host_platforms(&["android", "dragonfly", "freebsd", "haiku", "illumos", "ios", "linux", "macos", "netbsd", "openbsd", "solaris", "wasi", "windows"]);

/// Binding descriptor for destack.timer.schedule.interval.
pub const TIMER_SCHEDULE_INTERVAL: BindingDescriptor = BindingDescriptor::external_with_requires_and_behavior(
    "destack.timer.schedule.interval",
    "export function interval(periodNs: uint64, options: TimerOptions): Result<TimerHandle, PlatformError>",
    ReplayPolicy::Recordable,
    BindingReplayKind::Regular,
    &["time.timer"],
    BindingScope::Runtime,
    BindingBlocking::Never,
)
    .with_host_platforms(&["android", "dragonfly", "freebsd", "haiku", "illumos", "ios", "linux", "macos", "netbsd", "openbsd", "solaris", "wasi", "windows"]);

/// Binding descriptor for destack.timer.schedule.once.
pub const TIMER_SCHEDULE_ONCE: BindingDescriptor = BindingDescriptor::external_with_requires_and_behavior(
    "destack.timer.schedule.once",
    "export function once(delayNs: uint64, options: TimerOptions): Result<TimerHandle, PlatformError>",
    ReplayPolicy::Recordable,
    BindingReplayKind::Regular,
    &["time.timer"],
    BindingScope::Runtime,
    BindingBlocking::Never,
)
    .with_host_platforms(&["android", "dragonfly", "freebsd", "haiku", "illumos", "ios", "linux", "macos", "netbsd", "openbsd", "solaris", "wasi", "windows"]);

/// Binding descriptors for timer.
pub const BINDINGS: &[BindingDescriptor] = &[
    TIMER_CONTROL_CANCEL,
    TIMER_CONTROL_IS_ACTIVE,
    TIMER_CONTROL_PAUSE,
    TIMER_CONTROL_REMAINING_NS,
    TIMER_CONTROL_RESET,
    TIMER_CONTROL_RESUME,
    TIMER_CONTROL_UPDATE_INTERVAL,
    TIMER_FD_CLOSE,
    TIMER_FD_GET,
    TIMER_FD_OPEN,
    TIMER_FD_READ,
    TIMER_FD_SET,
    TIMER_SCHEDULE_AT,
    TIMER_SCHEDULE_INTERVAL,
    TIMER_SCHEDULE_ONCE,
];

/// Native binding set for timer.
pub const TIMER_NATIVE_BINDINGS: NativeBindingSet = NativeBindingSet {
    name: "timer",
    bindings: &[
        NativeBinding::new(
            TIMER_CONTROL_CANCEL,
            "destack.timer.control.cancel",
            destack_timer_control_cancel as *const (),
        ),
        NativeBinding::new(
            TIMER_CONTROL_IS_ACTIVE,
            "destack.timer.control.isActive",
            destack_timer_control_is_active as *const (),
        ),
        NativeBinding::new(
            TIMER_CONTROL_PAUSE,
            "destack.timer.control.pause",
            destack_timer_control_pause as *const (),
        ),
        NativeBinding::new(
            TIMER_CONTROL_REMAINING_NS,
            "destack.timer.control.remainingNs",
            destack_timer_control_remaining_ns as *const (),
        ),
        NativeBinding::new(
            TIMER_CONTROL_RESET,
            "destack.timer.control.reset",
            destack_timer_control_reset as *const (),
        ),
        NativeBinding::new(
            TIMER_CONTROL_RESUME,
            "destack.timer.control.resume",
            destack_timer_control_resume as *const (),
        ),
        NativeBinding::new(
            TIMER_CONTROL_UPDATE_INTERVAL,
            "destack.timer.control.updateInterval",
            destack_timer_control_update_interval as *const (),
        ),
        NativeBinding::new(
            TIMER_FD_CLOSE,
            "destack.timer.fd.close",
            destack_timer_fd_close as *const (),
        ),
        NativeBinding::new(
            TIMER_FD_GET,
            "destack.timer.fd.get",
            destack_timer_fd_get as *const (),
        ),
        NativeBinding::new(
            TIMER_FD_OPEN,
            "destack.timer.fd.open",
            destack_timer_fd_open as *const (),
        ),
        NativeBinding::new(
            TIMER_FD_READ,
            "destack.timer.fd.read",
            destack_timer_fd_read as *const (),
        ),
        NativeBinding::new(
            TIMER_FD_SET,
            "destack.timer.fd.set",
            destack_timer_fd_set as *const (),
        ),
        NativeBinding::new(
            TIMER_SCHEDULE_AT,
            "destack.timer.schedule.at",
            destack_timer_schedule_at as *const (),
        ),
        NativeBinding::new(
            TIMER_SCHEDULE_INTERVAL,
            "destack.timer.schedule.interval",
            destack_timer_schedule_interval as *const (),
        ),
        NativeBinding::new(
            TIMER_SCHEDULE_ONCE,
            "destack.timer.schedule.once",
            destack_timer_schedule_once as *const (),
        ),
    ],
};

/// Native replay implementations for timer bindings.
#[inline]
fn destack_timer_control_cancel_replay(
    context: &RuntimeCallContext,
    handle: resource::TimerHandle,
) -> RuntimeResult<()> {
    let _ = &handle;

    context.replay().run_binding_with_payload_policy(
        TIMER_CONTROL_CANCEL,
        context.replay_payload_for(TIMER_CONTROL_CANCEL)?,
        || unsafe { platform_runtime_native::destack_timer_cancel(context, handle) },
        |result| {
            if let Ok(()) = result {
                let result_recorded = ();
                let payload = TimerControlCancelReplay {
                    result: Ok(result_recorded),
                };
                return Ok(Some(payload));
            }

            if let Err(error) = result {
                let payload = {
                    let result = Err(PlatformError::from(error.as_ref()));
                    TimerControlCancelReplay { result }
                };
                return Ok(Some(payload));
            }

            Ok(None)
        },
        |payload| {
            // replay result
            match payload.result {
                Ok(()) => Ok(()),
                Err(error) => Err(RuntimeError::from(error).boxed()),
            }
        },
    )
}

#[inline]
fn destack_timer_control_is_active_replay(
    context: &RuntimeCallContext,
    out: *mut bool,
    handle: resource::TimerHandle,
) -> RuntimeResult<()> {
    let _ = &handle;

    context.replay().run_binding_with_payload_policy(
        TIMER_CONTROL_IS_ACTIVE,
        context.replay_payload_for(TIMER_CONTROL_IS_ACTIVE)?,
        || unsafe { platform_runtime_native::destack_timer_is_active(context, out, handle) },
        |result| {
            if let Ok(()) = result {
                let result_value = unsafe {
                    if out.is_null() {
                        return Err(RuntimeError::from(PlatformError::null_pointer("out")).boxed());
                    }
                    *out
                };
                let result_recorded = result_value;
                let payload = TimerControlIsActiveReplay {
                    result: Ok(result_recorded),
                };
                return Ok(Some(payload));
            }

            if let Err(error) = result {
                let payload = {
                    let result = Err(PlatformError::from(error.as_ref()));
                    TimerControlIsActiveReplay { result }
                };
                return Ok(Some(payload));
            }

            Ok(None)
        },
        |payload| {
            // replay result
            match payload.result {
                Ok(value) => {
                    let value_native = value;
                    unsafe {
                        std::ptr::write(out, value_native);
                    }
                    Ok(())
                }
                Err(error) => Err(RuntimeError::from(error).boxed()),
            }
        },
    )
}

#[inline]
fn destack_timer_control_pause_replay(
    context: &RuntimeCallContext,
    handle: resource::TimerHandle,
) -> RuntimeResult<()> {
    let _ = &handle;

    context.replay().run_binding_with_payload_policy(
        TIMER_CONTROL_PAUSE,
        context.replay_payload_for(TIMER_CONTROL_PAUSE)?,
        || unsafe { platform_runtime_native::destack_timer_pause(context, handle) },
        |result| {
            if let Ok(()) = result {
                let result_recorded = ();
                let payload = TimerControlPauseReplay {
                    result: Ok(result_recorded),
                };
                return Ok(Some(payload));
            }

            if let Err(error) = result {
                let payload = {
                    let result = Err(PlatformError::from(error.as_ref()));
                    TimerControlPauseReplay { result }
                };
                return Ok(Some(payload));
            }

            Ok(None)
        },
        |payload| {
            // replay result
            match payload.result {
                Ok(()) => Ok(()),
                Err(error) => Err(RuntimeError::from(error).boxed()),
            }
        },
    )
}

#[inline]
fn destack_timer_control_remaining_ns_replay(
    context: &RuntimeCallContext,
    out: *mut u64,
    handle: resource::TimerHandle,
) -> RuntimeResult<()> {
    let _ = &handle;

    context.replay().run_binding_with_payload_policy(
        TIMER_CONTROL_REMAINING_NS,
        context.replay_payload_for(TIMER_CONTROL_REMAINING_NS)?,
        || unsafe { platform_runtime_native::destack_timer_remaining_ns(context, out, handle) },
        |result| {
            if let Ok(()) = result {
                let result_value = unsafe {
                    if out.is_null() {
                        return Err(RuntimeError::from(PlatformError::null_pointer("out")).boxed());
                    }
                    *out
                };
                let result_recorded = result_value;
                let payload = TimerControlRemainingNsReplay {
                    result: Ok(result_recorded),
                };
                return Ok(Some(payload));
            }

            if let Err(error) = result {
                let payload = {
                    let result = Err(PlatformError::from(error.as_ref()));
                    TimerControlRemainingNsReplay { result }
                };
                return Ok(Some(payload));
            }

            Ok(None)
        },
        |payload| {
            // replay result
            match payload.result {
                Ok(value) => {
                    let value_native = value;
                    unsafe {
                        std::ptr::write(out, value_native);
                    }
                    Ok(())
                }
                Err(error) => Err(RuntimeError::from(error).boxed()),
            }
        },
    )
}

#[inline]
fn destack_timer_control_reset_replay(
    context: &RuntimeCallContext,
    handle: resource::TimerHandle,
    delayns: u64,
) -> RuntimeResult<()> {
    let _ = (&handle, &delayns);

    context.replay().run_binding_with_payload_policy(
        TIMER_CONTROL_RESET,
        context.replay_payload_for(TIMER_CONTROL_RESET)?,
        || unsafe { platform_runtime_native::destack_timer_reset(context, handle, delayns) },
        |result| {
            if let Ok(()) = result {
                let result_recorded = ();
                let payload = TimerControlResetReplay {
                    result: Ok(result_recorded),
                };
                return Ok(Some(payload));
            }

            if let Err(error) = result {
                let payload = {
                    let result = Err(PlatformError::from(error.as_ref()));
                    TimerControlResetReplay { result }
                };
                return Ok(Some(payload));
            }

            Ok(None)
        },
        |payload| {
            // replay result
            match payload.result {
                Ok(()) => Ok(()),
                Err(error) => Err(RuntimeError::from(error).boxed()),
            }
        },
    )
}

#[inline]
fn destack_timer_control_resume_replay(
    context: &RuntimeCallContext,
    handle: resource::TimerHandle,
) -> RuntimeResult<()> {
    let _ = &handle;

    context.replay().run_binding_with_payload_policy(
        TIMER_CONTROL_RESUME,
        context.replay_payload_for(TIMER_CONTROL_RESUME)?,
        || unsafe { platform_runtime_native::destack_timer_resume(context, handle) },
        |result| {
            if let Ok(()) = result {
                let result_recorded = ();
                let payload = TimerControlResumeReplay {
                    result: Ok(result_recorded),
                };
                return Ok(Some(payload));
            }

            if let Err(error) = result {
                let payload = {
                    let result = Err(PlatformError::from(error.as_ref()));
                    TimerControlResumeReplay { result }
                };
                return Ok(Some(payload));
            }

            Ok(None)
        },
        |payload| {
            // replay result
            match payload.result {
                Ok(()) => Ok(()),
                Err(error) => Err(RuntimeError::from(error).boxed()),
            }
        },
    )
}

#[inline]
fn destack_timer_control_update_interval_replay(
    context: &RuntimeCallContext,
    handle: resource::TimerHandle,
    periodns: u64,
) -> RuntimeResult<()> {
    let _ = (&handle, &periodns);

    context.replay().run_binding_with_payload_policy(
        TIMER_CONTROL_UPDATE_INTERVAL,
        context.replay_payload_for(TIMER_CONTROL_UPDATE_INTERVAL)?,
        || unsafe {
            platform_runtime_native::destack_timer_update_interval(context, handle, periodns)
        },
        |result| {
            if let Ok(()) = result {
                let result_recorded = ();
                let payload = TimerControlUpdateIntervalReplay {
                    result: Ok(result_recorded),
                };
                return Ok(Some(payload));
            }

            if let Err(error) = result {
                let payload = {
                    let result = Err(PlatformError::from(error.as_ref()));
                    TimerControlUpdateIntervalReplay { result }
                };
                return Ok(Some(payload));
            }

            Ok(None)
        },
        |payload| {
            // replay result
            match payload.result {
                Ok(()) => Ok(()),
                Err(error) => Err(RuntimeError::from(error).boxed()),
            }
        },
    )
}

#[inline]
fn destack_timer_fd_close_replay(
    context: &RuntimeCallContext,
    world: RuntimeWorld,
    handle: resource::TimerFdHandle,
) -> RuntimeResult<()> {
    let _ = &handle;

    context.replay().run_binding_with_payload_policy(
        TIMER_FD_CLOSE,
        context.replay_payload_for(TIMER_FD_CLOSE)?,
        || match world {
            RuntimeWorld::Host => unsafe {
                platform_native::destack_timer_timer_fd_close(context, handle)
            },
            RuntimeWorld::Simulated => unsafe {
                platform_simulated_native::destack_timer_timer_fd_close(context, handle)
            },
        },
        |result| {
            if let Ok(()) = result {
                let result_recorded = ();
                let payload = TimerFdCloseReplay {
                    result: Ok(result_recorded),
                };
                return Ok(Some(payload));
            }

            if let Err(error) = result {
                let payload = {
                    let result = Err(PlatformError::from(error.as_ref()));
                    TimerFdCloseReplay { result }
                };
                return Ok(Some(payload));
            }

            Ok(None)
        },
        |payload| {
            // replay result
            match payload.result {
                Ok(()) => Ok(()),
                Err(error) => Err(RuntimeError::from(error).boxed()),
            }
        },
    )
}

#[inline]
fn destack_timer_fd_get_replay(
    context: &RuntimeCallContext,
    world: RuntimeWorld,
    out: *mut TimerFdSpec,
    handle: resource::TimerFdHandle,
) -> RuntimeResult<()> {
    let _ = &handle;

    context.replay().run_binding_with_payload_policy(
        TIMER_FD_GET,
        context.replay_payload_for(TIMER_FD_GET)?,
        || match world {
            RuntimeWorld::Host => unsafe {
                platform_native::destack_timer_timer_fd_get(context, out, handle)
            },
            RuntimeWorld::Simulated => unsafe {
                platform_simulated_native::destack_timer_timer_fd_get(context, out, handle)
            },
        },
        |result| {
            if let Ok(()) = result {
                let result_value = unsafe {
                    if out.is_null() {
                        return Err(RuntimeError::from(PlatformError::null_pointer("out")).boxed());
                    }
                    *out
                };
                let result_recorded_initial_ns = result_value.initial_ns;
                let result_recorded_interval_ns = result_value.interval_ns;
                let result_recorded = TimerFdSpec {
                    initial_ns: result_recorded_initial_ns,
                    interval_ns: result_recorded_interval_ns,
                };
                let payload = TimerFdGetReplay {
                    result: Ok(result_recorded),
                };
                return Ok(Some(payload));
            }

            if let Err(error) = result {
                let payload = {
                    let result = Err(PlatformError::from(error.as_ref()));
                    TimerFdGetReplay { result }
                };
                return Ok(Some(payload));
            }

            Ok(None)
        },
        |payload| {
            // replay result
            match payload.result {
                Ok(value) => {
                    let value_native_initial_ns = value.initial_ns;
                    let value_native_interval_ns = value.interval_ns;
                    let value_native = TimerFdSpec {
                        initial_ns: value_native_initial_ns,
                        interval_ns: value_native_interval_ns,
                    };
                    unsafe {
                        std::ptr::write(out, value_native);
                    }
                    Ok(())
                }
                Err(error) => Err(RuntimeError::from(error).boxed()),
            }
        },
    )
}

#[inline]
fn destack_timer_fd_open_replay(
    context: &RuntimeCallContext,
    world: RuntimeWorld,
    out: *mut resource::TimerFdHandle,
    clock: TimerFdClock,
    flags: TimerFdFlags,
) -> RuntimeResult<()> {
    let _ = (&clock, &flags);

    context.replay().run_binding_with_payload_policy(
        TIMER_FD_OPEN,
        context.replay_payload_for(TIMER_FD_OPEN)?,
        || match world {
            RuntimeWorld::Host => unsafe {
                platform_native::destack_timer_timer_fd_open(context, out, clock, flags)
            },
            RuntimeWorld::Simulated => unsafe {
                platform_simulated_native::destack_timer_timer_fd_open(context, out, clock, flags)
            },
        },
        |result| {
            if let Ok(()) = result {
                let result_value = unsafe {
                    if out.is_null() {
                        return Err(RuntimeError::from(PlatformError::null_pointer("out")).boxed());
                    }
                    *out
                };
                let result_recorded = result_value;
                let payload = TimerFdOpenReplay {
                    result: Ok(result_recorded),
                };
                return Ok(Some(payload));
            }

            if let Err(error) = result {
                let payload = {
                    let result = Err(PlatformError::from(error.as_ref()));
                    TimerFdOpenReplay { result }
                };
                return Ok(Some(payload));
            }

            Ok(None)
        },
        |payload| {
            // replay result
            match payload.result {
                Ok(value) => {
                    let value_native = value;
                    unsafe {
                        std::ptr::write(out, value_native);
                    }
                    Ok(())
                }
                Err(error) => Err(RuntimeError::from(error).boxed()),
            }
        },
    )
}

#[inline]
fn destack_timer_fd_read_replay(
    context: &RuntimeCallContext,
    world: RuntimeWorld,
    out: *mut u64,
    handle: resource::TimerFdHandle,
) -> RuntimeResult<()> {
    let _ = &handle;

    context.replay().run_binding_with_payload_policy(
        TIMER_FD_READ,
        context.replay_payload_for(TIMER_FD_READ)?,
        || match world {
            RuntimeWorld::Host => unsafe {
                platform_native::destack_timer_timer_fd_read(context, out, handle)
            },
            RuntimeWorld::Simulated => unsafe {
                platform_simulated_native::destack_timer_timer_fd_read(context, out, handle)
            },
        },
        |result| {
            if let Ok(()) = result {
                let result_value = unsafe {
                    if out.is_null() {
                        return Err(RuntimeError::from(PlatformError::null_pointer("out")).boxed());
                    }
                    *out
                };
                let result_recorded = result_value;
                let payload = TimerFdReadReplay {
                    result: Ok(result_recorded),
                };
                return Ok(Some(payload));
            }

            if let Err(error) = result {
                let payload = {
                    let result = Err(PlatformError::from(error.as_ref()));
                    TimerFdReadReplay { result }
                };
                return Ok(Some(payload));
            }

            Ok(None)
        },
        |payload| {
            // replay result
            match payload.result {
                Ok(value) => {
                    let value_native = value;
                    unsafe {
                        std::ptr::write(out, value_native);
                    }
                    Ok(())
                }
                Err(error) => Err(RuntimeError::from(error).boxed()),
            }
        },
    )
}

#[inline]
fn destack_timer_fd_set_replay(
    context: &RuntimeCallContext,
    world: RuntimeWorld,
    handle: resource::TimerFdHandle,
    spec: TimerFdSpec,
    flags: TimerFdSetFlags,
) -> RuntimeResult<()> {
    let _ = (&handle, &spec, &flags);

    context.replay().run_binding_with_payload_policy(
        TIMER_FD_SET,
        context.replay_payload_for(TIMER_FD_SET)?,
        || match world {
            RuntimeWorld::Host => unsafe {
                platform_native::destack_timer_timer_fd_set(context, handle, spec, flags)
            },
            RuntimeWorld::Simulated => unsafe {
                platform_simulated_native::destack_timer_timer_fd_set(context, handle, spec, flags)
            },
        },
        |result| {
            if let Ok(()) = result {
                let result_recorded = ();
                let payload = TimerFdSetReplay {
                    result: Ok(result_recorded),
                };
                return Ok(Some(payload));
            }

            if let Err(error) = result {
                let payload = {
                    let result = Err(PlatformError::from(error.as_ref()));
                    TimerFdSetReplay { result }
                };
                return Ok(Some(payload));
            }

            Ok(None)
        },
        |payload| {
            // replay result
            match payload.result {
                Ok(()) => Ok(()),
                Err(error) => Err(RuntimeError::from(error).boxed()),
            }
        },
    )
}

#[inline]
fn destack_timer_schedule_at_replay(
    context: &RuntimeCallContext,
    out: *mut resource::TimerHandle,
    deadlinens: u64,
    options: TimerOptions,
) -> RuntimeResult<()> {
    let _ = (&deadlinens, &options);

    context.replay().run_binding_with_payload_policy(
        TIMER_SCHEDULE_AT,
        context.replay_payload_for(TIMER_SCHEDULE_AT)?,
        || unsafe { platform_runtime_native::destack_timer_at(context, out, deadlinens, options) },
        |result| {
            if let Ok(()) = result {
                let result_value = unsafe {
                    if out.is_null() {
                        return Err(RuntimeError::from(PlatformError::null_pointer("out")).boxed());
                    }
                    *out
                };
                let result_recorded = result_value;
                let payload = TimerScheduleAtReplay {
                    result: Ok(result_recorded),
                };
                return Ok(Some(payload));
            }

            if let Err(error) = result {
                let payload = {
                    let result = Err(PlatformError::from(error.as_ref()));
                    TimerScheduleAtReplay { result }
                };
                return Ok(Some(payload));
            }

            Ok(None)
        },
        |payload| {
            // replay result
            match payload.result {
                Ok(value) => {
                    let value_native = value;
                    unsafe {
                        std::ptr::write(out, value_native);
                    }
                    Ok(())
                }
                Err(error) => Err(RuntimeError::from(error).boxed()),
            }
        },
    )
}

#[inline]
fn destack_timer_schedule_interval_replay(
    context: &RuntimeCallContext,
    out: *mut resource::TimerHandle,
    periodns: u64,
    options: TimerOptions,
) -> RuntimeResult<()> {
    let _ = (&periodns, &options);

    context.replay().run_binding_with_payload_policy(
        TIMER_SCHEDULE_INTERVAL,
        context.replay_payload_for(TIMER_SCHEDULE_INTERVAL)?,
        || unsafe {
            platform_runtime_native::destack_timer_interval(context, out, periodns, options)
        },
        |result| {
            if let Ok(()) = result {
                let result_value = unsafe {
                    if out.is_null() {
                        return Err(RuntimeError::from(PlatformError::null_pointer("out")).boxed());
                    }
                    *out
                };
                let result_recorded = result_value;
                let payload = TimerScheduleIntervalReplay {
                    result: Ok(result_recorded),
                };
                return Ok(Some(payload));
            }

            if let Err(error) = result {
                let payload = {
                    let result = Err(PlatformError::from(error.as_ref()));
                    TimerScheduleIntervalReplay { result }
                };
                return Ok(Some(payload));
            }

            Ok(None)
        },
        |payload| {
            // replay result
            match payload.result {
                Ok(value) => {
                    let value_native = value;
                    unsafe {
                        std::ptr::write(out, value_native);
                    }
                    Ok(())
                }
                Err(error) => Err(RuntimeError::from(error).boxed()),
            }
        },
    )
}

#[inline]
fn destack_timer_schedule_once_replay(
    context: &RuntimeCallContext,
    out: *mut resource::TimerHandle,
    delayns: u64,
    options: TimerOptions,
) -> RuntimeResult<()> {
    let _ = (&delayns, &options);

    context.replay().run_binding_with_payload_policy(
        TIMER_SCHEDULE_ONCE,
        context.replay_payload_for(TIMER_SCHEDULE_ONCE)?,
        || unsafe { platform_runtime_native::destack_timer_once(context, out, delayns, options) },
        |result| {
            if let Ok(()) = result {
                let result_value = unsafe {
                    if out.is_null() {
                        return Err(RuntimeError::from(PlatformError::null_pointer("out")).boxed());
                    }
                    *out
                };
                let result_recorded = result_value;
                let payload = TimerScheduleOnceReplay {
                    result: Ok(result_recorded),
                };
                return Ok(Some(payload));
            }

            if let Err(error) = result {
                let payload = {
                    let result = Err(PlatformError::from(error.as_ref()));
                    TimerScheduleOnceReplay { result }
                };
                return Ok(Some(payload));
            }

            Ok(None)
        },
        |payload| {
            // replay result
            match payload.result {
                Ok(value) => {
                    let value_native = value;
                    unsafe {
                        std::ptr::write(out, value_native);
                    }
                    Ok(())
                }
                Err(error) => Err(RuntimeError::from(error).boxed()),
            }
        },
    )
}

/// Native export wrappers for timer bindings.
#[unsafe(export_name = "destack.timer.control.cancel")]
pub unsafe extern "C" fn destack_timer_control_cancel(
    handle: resource::TimerHandle,
) -> RuntimeStatus {
    native_call(|context| {
        let _ = &handle;

        context.check_policy(TIMER_CONTROL_CANCEL)?;
        destack_timer_control_cancel_replay(context, handle)
    })
}

#[unsafe(export_name = "destack.timer.control.isActive")]
pub unsafe extern "C" fn destack_timer_control_is_active(
    out: *mut bool,
    handle: resource::TimerHandle,
) -> RuntimeStatus {
    native_call(|context| {
        if out.is_null() {
            return Err(RuntimeError::from(PlatformError::null_pointer("out")).boxed());
        }
        let _ = (&out, &handle);

        context.check_policy(TIMER_CONTROL_IS_ACTIVE)?;
        destack_timer_control_is_active_replay(context, out, handle)
    })
}

#[unsafe(export_name = "destack.timer.control.pause")]
pub unsafe extern "C" fn destack_timer_control_pause(
    handle: resource::TimerHandle,
) -> RuntimeStatus {
    native_call(|context| {
        let _ = &handle;

        context.check_policy(TIMER_CONTROL_PAUSE)?;
        destack_timer_control_pause_replay(context, handle)
    })
}

#[unsafe(export_name = "destack.timer.control.remainingNs")]
pub unsafe extern "C" fn destack_timer_control_remaining_ns(
    out: *mut u64,
    handle: resource::TimerHandle,
) -> RuntimeStatus {
    native_call(|context| {
        if out.is_null() {
            return Err(RuntimeError::from(PlatformError::null_pointer("out")).boxed());
        }
        let _ = (&out, &handle);

        context.check_policy(TIMER_CONTROL_REMAINING_NS)?;
        destack_timer_control_remaining_ns_replay(context, out, handle)
    })
}

#[unsafe(export_name = "destack.timer.control.reset")]
pub unsafe extern "C" fn destack_timer_control_reset(
    handle: resource::TimerHandle,
    delayns: u64,
) -> RuntimeStatus {
    native_call(|context| {
        let _ = (&handle, &delayns);

        context.check_policy(TIMER_CONTROL_RESET)?;
        destack_timer_control_reset_replay(context, handle, delayns)
    })
}

#[unsafe(export_name = "destack.timer.control.resume")]
pub unsafe extern "C" fn destack_timer_control_resume(
    handle: resource::TimerHandle,
) -> RuntimeStatus {
    native_call(|context| {
        let _ = &handle;

        context.check_policy(TIMER_CONTROL_RESUME)?;
        destack_timer_control_resume_replay(context, handle)
    })
}

#[unsafe(export_name = "destack.timer.control.updateInterval")]
pub unsafe extern "C" fn destack_timer_control_update_interval(
    handle: resource::TimerHandle,
    periodns: u64,
) -> RuntimeStatus {
    native_call(|context| {
        let _ = (&handle, &periodns);

        context.check_policy(TIMER_CONTROL_UPDATE_INTERVAL)?;
        destack_timer_control_update_interval_replay(context, handle, periodns)
    })
}

#[unsafe(export_name = "destack.timer.fd.close")]
pub unsafe extern "C" fn destack_timer_fd_close(handle: resource::TimerFdHandle) -> RuntimeStatus {
    native_call(|context| {
        let _ = &handle;

        context.check_policy(TIMER_FD_CLOSE)?;
        let world = context.check_and_resolve_world(TIMER_FD_CLOSE)?;
        destack_timer_fd_close_replay(context, world, handle)
    })
}

#[unsafe(export_name = "destack.timer.fd.get")]
pub unsafe extern "C" fn destack_timer_fd_get(
    out: *mut TimerFdSpec,
    handle: resource::TimerFdHandle,
) -> RuntimeStatus {
    native_call(|context| {
        if out.is_null() {
            return Err(RuntimeError::from(PlatformError::null_pointer("out")).boxed());
        }
        let _ = (&out, &handle);

        context.check_policy(TIMER_FD_GET)?;
        let world = context.check_and_resolve_world(TIMER_FD_GET)?;
        destack_timer_fd_get_replay(context, world, out, handle)
    })
}

#[unsafe(export_name = "destack.timer.fd.open")]
pub unsafe extern "C" fn destack_timer_fd_open(
    out: *mut resource::TimerFdHandle,
    clock: TimerFdClock,
    flags: TimerFdFlags,
) -> RuntimeStatus {
    native_call(|context| {
        if out.is_null() {
            return Err(RuntimeError::from(PlatformError::null_pointer("out")).boxed());
        }
        let _ = (&out, &clock, &flags);

        context.check_policy(TIMER_FD_OPEN)?;
        let world = context.check_and_resolve_world(TIMER_FD_OPEN)?;
        destack_timer_fd_open_replay(context, world, out, clock, flags)
    })
}

#[unsafe(export_name = "destack.timer.fd.read")]
pub unsafe extern "C" fn destack_timer_fd_read(
    out: *mut u64,
    handle: resource::TimerFdHandle,
) -> RuntimeStatus {
    native_call(|context| {
        if out.is_null() {
            return Err(RuntimeError::from(PlatformError::null_pointer("out")).boxed());
        }
        let _ = (&out, &handle);

        context.check_policy(TIMER_FD_READ)?;
        let world = context.check_and_resolve_world(TIMER_FD_READ)?;
        destack_timer_fd_read_replay(context, world, out, handle)
    })
}

#[unsafe(export_name = "destack.timer.fd.set")]
pub unsafe extern "C" fn destack_timer_fd_set(
    handle: resource::TimerFdHandle,
    spec: TimerFdSpec,
    flags: TimerFdSetFlags,
) -> RuntimeStatus {
    native_call(|context| {
        let _ = (&handle, &spec, &flags);

        context.check_policy(TIMER_FD_SET)?;
        let world = context.check_and_resolve_world(TIMER_FD_SET)?;
        destack_timer_fd_set_replay(context, world, handle, spec, flags)
    })
}

#[unsafe(export_name = "destack.timer.schedule.at")]
pub unsafe extern "C" fn destack_timer_schedule_at(
    out: *mut resource::TimerHandle,
    deadlinens: u64,
    options: TimerOptions,
) -> RuntimeStatus {
    native_call(|context| {
        if out.is_null() {
            return Err(RuntimeError::from(PlatformError::null_pointer("out")).boxed());
        }
        let _ = (&out, &deadlinens, &options);

        context.check_policy(TIMER_SCHEDULE_AT)?;
        destack_timer_schedule_at_replay(context, out, deadlinens, options)
    })
}

#[unsafe(export_name = "destack.timer.schedule.interval")]
pub unsafe extern "C" fn destack_timer_schedule_interval(
    out: *mut resource::TimerHandle,
    periodns: u64,
    options: TimerOptions,
) -> RuntimeStatus {
    native_call(|context| {
        if out.is_null() {
            return Err(RuntimeError::from(PlatformError::null_pointer("out")).boxed());
        }
        let _ = (&out, &periodns, &options);

        context.check_policy(TIMER_SCHEDULE_INTERVAL)?;
        destack_timer_schedule_interval_replay(context, out, periodns, options)
    })
}

#[unsafe(export_name = "destack.timer.schedule.once")]
pub unsafe extern "C" fn destack_timer_schedule_once(
    out: *mut resource::TimerHandle,
    delayns: u64,
    options: TimerOptions,
) -> RuntimeStatus {
    native_call(|context| {
        if out.is_null() {
            return Err(RuntimeError::from(PlatformError::null_pointer("out")).boxed());
        }
        let _ = (&out, &delayns, &options);

        context.check_policy(TIMER_SCHEDULE_ONCE)?;
        destack_timer_schedule_once_replay(context, out, delayns, options)
    })
}

/// VM replay implementations for timer bindings.
#[inline]
fn destack_timer_control_cancel_vm_replay(
    runtime: &RuntimeCallContext,
    context: &mut vm::ExternalCallContext<'_>,
    handle: resource::TimerHandle,
) -> RuntimeResult<vm::Value> {
    let result = runtime
        .replay()
        .run_binding_with_context_and_payload_policy(
            TIMER_CONTROL_CANCEL,
            runtime.replay_payload_for(TIMER_CONTROL_CANCEL)?,
            context,
            |context| platform_runtime_vm::destack_timer_cancel(runtime, context, handle),
            |context, result| {
                let _ = &context;
                if let Ok(()) = result {
                    let result_recorded = ();
                    let payload = TimerControlCancelReplay {
                        result: Ok(result_recorded),
                    };
                    return Ok(Some(payload));
                }

                if let Err(error) = result {
                    let payload = {
                        let result = Err(PlatformError::from(error.as_ref()));
                        TimerControlCancelReplay { result }
                    };
                    return Ok(Some(payload));
                }

                Ok(None)
            },
            |context, payload| {
                let _ = &context;
                // replay result
                match payload.result {
                    Ok(()) => Ok(()),
                    Err(error) => Err(RuntimeError::from(error).boxed()),
                }
            },
        );
    let result = encode_destack_timer_control_cancel_result(context, result)?;
    Ok(result)
}

#[inline]
fn destack_timer_control_is_active_vm_replay(
    runtime: &RuntimeCallContext,
    context: &mut vm::ExternalCallContext<'_>,
    handle: resource::TimerHandle,
) -> RuntimeResult<vm::Value> {
    let result = runtime
        .replay()
        .run_binding_with_context_and_payload_policy(
            TIMER_CONTROL_IS_ACTIVE,
            runtime.replay_payload_for(TIMER_CONTROL_IS_ACTIVE)?,
            context,
            |context| platform_runtime_vm::destack_timer_is_active(runtime, context, handle),
            |context, result| {
                let _ = &context;
                if let Ok(value) = result {
                    let result_value: bool = value.clone();
                    let result_recorded = result_value;
                    let payload = TimerControlIsActiveReplay {
                        result: Ok(result_recorded),
                    };
                    return Ok(Some(payload));
                }

                if let Err(error) = result {
                    let payload = {
                        let result = Err(PlatformError::from(error.as_ref()));
                        TimerControlIsActiveReplay { result }
                    };
                    return Ok(Some(payload));
                }

                Ok(None)
            },
            |context, payload| {
                let _ = &context;
                // replay result
                match payload.result {
                    Ok(value) => {
                        let vm_result = value;
                        Ok(vm_result)
                    }
                    Err(error) => Err(RuntimeError::from(error).boxed()),
                }
            },
        );
    let result = encode_destack_timer_control_is_active_result(context, result)?;
    Ok(result)
}

#[inline]
fn destack_timer_control_pause_vm_replay(
    runtime: &RuntimeCallContext,
    context: &mut vm::ExternalCallContext<'_>,
    handle: resource::TimerHandle,
) -> RuntimeResult<vm::Value> {
    let result = runtime
        .replay()
        .run_binding_with_context_and_payload_policy(
            TIMER_CONTROL_PAUSE,
            runtime.replay_payload_for(TIMER_CONTROL_PAUSE)?,
            context,
            |context| platform_runtime_vm::destack_timer_pause(runtime, context, handle),
            |context, result| {
                let _ = &context;
                if let Ok(()) = result {
                    let result_recorded = ();
                    let payload = TimerControlPauseReplay {
                        result: Ok(result_recorded),
                    };
                    return Ok(Some(payload));
                }

                if let Err(error) = result {
                    let payload = {
                        let result = Err(PlatformError::from(error.as_ref()));
                        TimerControlPauseReplay { result }
                    };
                    return Ok(Some(payload));
                }

                Ok(None)
            },
            |context, payload| {
                let _ = &context;
                // replay result
                match payload.result {
                    Ok(()) => Ok(()),
                    Err(error) => Err(RuntimeError::from(error).boxed()),
                }
            },
        );
    let result = encode_destack_timer_control_pause_result(context, result)?;
    Ok(result)
}

#[inline]
fn destack_timer_control_remaining_ns_vm_replay(
    runtime: &RuntimeCallContext,
    context: &mut vm::ExternalCallContext<'_>,
    handle: resource::TimerHandle,
) -> RuntimeResult<vm::Value> {
    let result = runtime
        .replay()
        .run_binding_with_context_and_payload_policy(
            TIMER_CONTROL_REMAINING_NS,
            runtime.replay_payload_for(TIMER_CONTROL_REMAINING_NS)?,
            context,
            |context| platform_runtime_vm::destack_timer_remaining_ns(runtime, context, handle),
            |context, result| {
                let _ = &context;
                if let Ok(value) = result {
                    let result_value: u64 = value.clone();
                    let result_recorded = result_value;
                    let payload = TimerControlRemainingNsReplay {
                        result: Ok(result_recorded),
                    };
                    return Ok(Some(payload));
                }

                if let Err(error) = result {
                    let payload = {
                        let result = Err(PlatformError::from(error.as_ref()));
                        TimerControlRemainingNsReplay { result }
                    };
                    return Ok(Some(payload));
                }

                Ok(None)
            },
            |context, payload| {
                let _ = &context;
                // replay result
                match payload.result {
                    Ok(value) => {
                        let vm_result = value;
                        Ok(vm_result)
                    }
                    Err(error) => Err(RuntimeError::from(error).boxed()),
                }
            },
        );
    let result = encode_destack_timer_control_remaining_ns_result(context, result)?;
    Ok(result)
}

#[inline]
fn destack_timer_control_reset_vm_replay(
    runtime: &RuntimeCallContext,
    context: &mut vm::ExternalCallContext<'_>,
    handle: resource::TimerHandle,
    delayns: u64,
) -> RuntimeResult<vm::Value> {
    let result = runtime
        .replay()
        .run_binding_with_context_and_payload_policy(
            TIMER_CONTROL_RESET,
            runtime.replay_payload_for(TIMER_CONTROL_RESET)?,
            context,
            |context| platform_runtime_vm::destack_timer_reset(runtime, context, handle, delayns),
            |context, result| {
                let _ = &context;
                if let Ok(()) = result {
                    let result_recorded = ();
                    let payload = TimerControlResetReplay {
                        result: Ok(result_recorded),
                    };
                    return Ok(Some(payload));
                }

                if let Err(error) = result {
                    let payload = {
                        let result = Err(PlatformError::from(error.as_ref()));
                        TimerControlResetReplay { result }
                    };
                    return Ok(Some(payload));
                }

                Ok(None)
            },
            |context, payload| {
                let _ = &context;
                // replay result
                match payload.result {
                    Ok(()) => Ok(()),
                    Err(error) => Err(RuntimeError::from(error).boxed()),
                }
            },
        );
    let result = encode_destack_timer_control_reset_result(context, result)?;
    Ok(result)
}

#[inline]
fn destack_timer_control_resume_vm_replay(
    runtime: &RuntimeCallContext,
    context: &mut vm::ExternalCallContext<'_>,
    handle: resource::TimerHandle,
) -> RuntimeResult<vm::Value> {
    let result = runtime
        .replay()
        .run_binding_with_context_and_payload_policy(
            TIMER_CONTROL_RESUME,
            runtime.replay_payload_for(TIMER_CONTROL_RESUME)?,
            context,
            |context| platform_runtime_vm::destack_timer_resume(runtime, context, handle),
            |context, result| {
                let _ = &context;
                if let Ok(()) = result {
                    let result_recorded = ();
                    let payload = TimerControlResumeReplay {
                        result: Ok(result_recorded),
                    };
                    return Ok(Some(payload));
                }

                if let Err(error) = result {
                    let payload = {
                        let result = Err(PlatformError::from(error.as_ref()));
                        TimerControlResumeReplay { result }
                    };
                    return Ok(Some(payload));
                }

                Ok(None)
            },
            |context, payload| {
                let _ = &context;
                // replay result
                match payload.result {
                    Ok(()) => Ok(()),
                    Err(error) => Err(RuntimeError::from(error).boxed()),
                }
            },
        );
    let result = encode_destack_timer_control_resume_result(context, result)?;
    Ok(result)
}

#[inline]
fn destack_timer_control_update_interval_vm_replay(
    runtime: &RuntimeCallContext,
    context: &mut vm::ExternalCallContext<'_>,
    handle: resource::TimerHandle,
    periodns: u64,
) -> RuntimeResult<vm::Value> {
    let result = runtime
        .replay()
        .run_binding_with_context_and_payload_policy(
            TIMER_CONTROL_UPDATE_INTERVAL,
            runtime.replay_payload_for(TIMER_CONTROL_UPDATE_INTERVAL)?,
            context,
            |context| {
                platform_runtime_vm::destack_timer_update_interval(
                    runtime, context, handle, periodns,
                )
            },
            |context, result| {
                let _ = &context;
                if let Ok(()) = result {
                    let result_recorded = ();
                    let payload = TimerControlUpdateIntervalReplay {
                        result: Ok(result_recorded),
                    };
                    return Ok(Some(payload));
                }

                if let Err(error) = result {
                    let payload = {
                        let result = Err(PlatformError::from(error.as_ref()));
                        TimerControlUpdateIntervalReplay { result }
                    };
                    return Ok(Some(payload));
                }

                Ok(None)
            },
            |context, payload| {
                let _ = &context;
                // replay result
                match payload.result {
                    Ok(()) => Ok(()),
                    Err(error) => Err(RuntimeError::from(error).boxed()),
                }
            },
        );
    let result = encode_destack_timer_control_update_interval_result(context, result)?;
    Ok(result)
}

#[inline]
fn destack_timer_fd_close_vm_replay(
    runtime: &RuntimeCallContext,
    context: &mut vm::ExternalCallContext<'_>,
    world: RuntimeWorld,
    handle: resource::TimerFdHandle,
) -> RuntimeResult<vm::Value> {
    let result = runtime
        .replay()
        .run_binding_with_context_and_payload_policy(
            TIMER_FD_CLOSE,
            runtime.replay_payload_for(TIMER_FD_CLOSE)?,
            context,
            |context| match world {
                RuntimeWorld::Host => {
                    platform_vm::destack_timer_timer_fd_close(runtime, context, handle)
                }
                RuntimeWorld::Simulated => {
                    platform_simulated_vm::destack_timer_timer_fd_close(runtime, context, handle)
                }
            },
            |context, result| {
                let _ = &context;
                if let Ok(()) = result {
                    let result_recorded = ();
                    let payload = TimerFdCloseReplay {
                        result: Ok(result_recorded),
                    };
                    return Ok(Some(payload));
                }

                if let Err(error) = result {
                    let payload = {
                        let result = Err(PlatformError::from(error.as_ref()));
                        TimerFdCloseReplay { result }
                    };
                    return Ok(Some(payload));
                }

                Ok(None)
            },
            |context, payload| {
                let _ = &context;
                // replay result
                match payload.result {
                    Ok(()) => Ok(()),
                    Err(error) => Err(RuntimeError::from(error).boxed()),
                }
            },
        );
    let result = encode_destack_timer_fd_close_result(context, result)?;
    Ok(result)
}

#[inline]
fn destack_timer_fd_get_vm_replay(
    runtime: &RuntimeCallContext,
    context: &mut vm::ExternalCallContext<'_>,
    world: RuntimeWorld,
    handle: resource::TimerFdHandle,
) -> RuntimeResult<vm::Value> {
    let result = runtime
        .replay()
        .run_binding_with_context_and_payload_policy(
            TIMER_FD_GET,
            runtime.replay_payload_for(TIMER_FD_GET)?,
            context,
            |context| match world {
                RuntimeWorld::Host => {
                    platform_vm::destack_timer_timer_fd_get(runtime, context, handle)
                }
                RuntimeWorld::Simulated => {
                    platform_simulated_vm::destack_timer_timer_fd_get(runtime, context, handle)
                }
            },
            |context, result| {
                let _ = &context;
                if let Ok(value) = result {
                    let result_value: TimerFdSpecVm = value.clone();
                    let result_recorded_initial_ns = result_value.initial_ns;
                    let result_recorded_interval_ns = result_value.interval_ns;
                    let result_recorded = TimerFdSpec {
                        initial_ns: result_recorded_initial_ns,
                        interval_ns: result_recorded_interval_ns,
                    };
                    let payload = TimerFdGetReplay {
                        result: Ok(result_recorded),
                    };
                    return Ok(Some(payload));
                }

                if let Err(error) = result {
                    let payload = {
                        let result = Err(PlatformError::from(error.as_ref()));
                        TimerFdGetReplay { result }
                    };
                    return Ok(Some(payload));
                }

                Ok(None)
            },
            |context, payload| {
                let _ = &context;
                // replay result
                match payload.result {
                    Ok(value) => {
                        let vm_result_initial_ns = value.initial_ns;
                        let vm_result_interval_ns = value.interval_ns;
                        let vm_result = TimerFdSpecVm {
                            initial_ns: vm_result_initial_ns,
                            interval_ns: vm_result_interval_ns,
                        };
                        Ok(vm_result)
                    }
                    Err(error) => Err(RuntimeError::from(error).boxed()),
                }
            },
        );
    let result = encode_destack_timer_fd_get_result(context, result)?;
    Ok(result)
}

#[inline]
fn destack_timer_fd_open_vm_replay(
    runtime: &RuntimeCallContext,
    context: &mut vm::ExternalCallContext<'_>,
    world: RuntimeWorld,
    clock: TimerFdClock,
    flags: TimerFdFlags,
) -> RuntimeResult<vm::Value> {
    let result = runtime
        .replay()
        .run_binding_with_context_and_payload_policy(
            TIMER_FD_OPEN,
            runtime.replay_payload_for(TIMER_FD_OPEN)?,
            context,
            |context| match world {
                RuntimeWorld::Host => {
                    platform_vm::destack_timer_timer_fd_open(runtime, context, clock, flags)
                }
                RuntimeWorld::Simulated => platform_simulated_vm::destack_timer_timer_fd_open(
                    runtime, context, clock, flags,
                ),
            },
            |context, result| {
                let _ = &context;
                if let Ok(value) = result {
                    let result_value: resource::TimerFdHandle = value.clone();
                    let result_recorded = result_value;
                    let payload = TimerFdOpenReplay {
                        result: Ok(result_recorded),
                    };
                    return Ok(Some(payload));
                }

                if let Err(error) = result {
                    let payload = {
                        let result = Err(PlatformError::from(error.as_ref()));
                        TimerFdOpenReplay { result }
                    };
                    return Ok(Some(payload));
                }

                Ok(None)
            },
            |context, payload| {
                let _ = &context;
                // replay result
                match payload.result {
                    Ok(value) => {
                        let vm_result = value;
                        Ok(vm_result)
                    }
                    Err(error) => Err(RuntimeError::from(error).boxed()),
                }
            },
        );
    let result = encode_destack_timer_fd_open_result(context, result)?;
    Ok(result)
}

#[inline]
fn destack_timer_fd_read_vm_replay(
    runtime: &RuntimeCallContext,
    context: &mut vm::ExternalCallContext<'_>,
    world: RuntimeWorld,
    handle: resource::TimerFdHandle,
) -> RuntimeResult<vm::Value> {
    let result = runtime
        .replay()
        .run_binding_with_context_and_payload_policy(
            TIMER_FD_READ,
            runtime.replay_payload_for(TIMER_FD_READ)?,
            context,
            |context| match world {
                RuntimeWorld::Host => {
                    platform_vm::destack_timer_timer_fd_read(runtime, context, handle)
                }
                RuntimeWorld::Simulated => {
                    platform_simulated_vm::destack_timer_timer_fd_read(runtime, context, handle)
                }
            },
            |context, result| {
                let _ = &context;
                if let Ok(value) = result {
                    let result_value: u64 = value.clone();
                    let result_recorded = result_value;
                    let payload = TimerFdReadReplay {
                        result: Ok(result_recorded),
                    };
                    return Ok(Some(payload));
                }

                if let Err(error) = result {
                    let payload = {
                        let result = Err(PlatformError::from(error.as_ref()));
                        TimerFdReadReplay { result }
                    };
                    return Ok(Some(payload));
                }

                Ok(None)
            },
            |context, payload| {
                let _ = &context;
                // replay result
                match payload.result {
                    Ok(value) => {
                        let vm_result = value;
                        Ok(vm_result)
                    }
                    Err(error) => Err(RuntimeError::from(error).boxed()),
                }
            },
        );
    let result = encode_destack_timer_fd_read_result(context, result)?;
    Ok(result)
}

#[inline]
fn destack_timer_fd_set_vm_replay(
    runtime: &RuntimeCallContext,
    context: &mut vm::ExternalCallContext<'_>,
    world: RuntimeWorld,
    handle: resource::TimerFdHandle,
    spec: TimerFdSpecVm,
    flags: TimerFdSetFlags,
) -> RuntimeResult<vm::Value> {
    let result = runtime
        .replay()
        .run_binding_with_context_and_payload_policy(
            TIMER_FD_SET,
            runtime.replay_payload_for(TIMER_FD_SET)?,
            context,
            |context| match world {
                RuntimeWorld::Host => {
                    platform_vm::destack_timer_timer_fd_set(runtime, context, handle, spec, flags)
                }
                RuntimeWorld::Simulated => platform_simulated_vm::destack_timer_timer_fd_set(
                    runtime, context, handle, spec, flags,
                ),
            },
            |context, result| {
                let _ = &context;
                if let Ok(()) = result {
                    let result_recorded = ();
                    let payload = TimerFdSetReplay {
                        result: Ok(result_recorded),
                    };
                    return Ok(Some(payload));
                }

                if let Err(error) = result {
                    let payload = {
                        let result = Err(PlatformError::from(error.as_ref()));
                        TimerFdSetReplay { result }
                    };
                    return Ok(Some(payload));
                }

                Ok(None)
            },
            |context, payload| {
                let _ = &context;
                // replay result
                match payload.result {
                    Ok(()) => Ok(()),
                    Err(error) => Err(RuntimeError::from(error).boxed()),
                }
            },
        );
    let result = encode_destack_timer_fd_set_result(context, result)?;
    Ok(result)
}

#[inline]
fn destack_timer_schedule_at_vm_replay(
    runtime: &RuntimeCallContext,
    context: &mut vm::ExternalCallContext<'_>,
    deadlinens: u64,
    options: TimerOptionsVm,
) -> RuntimeResult<vm::Value> {
    let result = runtime
        .replay()
        .run_binding_with_context_and_payload_policy(
            TIMER_SCHEDULE_AT,
            runtime.replay_payload_for(TIMER_SCHEDULE_AT)?,
            context,
            |context| platform_runtime_vm::destack_timer_at(runtime, context, deadlinens, options),
            |context, result| {
                let _ = &context;
                if let Ok(value) = result {
                    let result_value: resource::TimerHandle = value.clone();
                    let result_recorded = result_value;
                    let payload = TimerScheduleAtReplay {
                        result: Ok(result_recorded),
                    };
                    return Ok(Some(payload));
                }

                if let Err(error) = result {
                    let payload = {
                        let result = Err(PlatformError::from(error.as_ref()));
                        TimerScheduleAtReplay { result }
                    };
                    return Ok(Some(payload));
                }

                Ok(None)
            },
            |context, payload| {
                let _ = &context;
                // replay result
                match payload.result {
                    Ok(value) => {
                        let vm_result = value;
                        Ok(vm_result)
                    }
                    Err(error) => Err(RuntimeError::from(error).boxed()),
                }
            },
        );
    let result = encode_destack_timer_schedule_at_result(context, result)?;
    Ok(result)
}

#[inline]
fn destack_timer_schedule_interval_vm_replay(
    runtime: &RuntimeCallContext,
    context: &mut vm::ExternalCallContext<'_>,
    periodns: u64,
    options: TimerOptionsVm,
) -> RuntimeResult<vm::Value> {
    let result = runtime
        .replay()
        .run_binding_with_context_and_payload_policy(
            TIMER_SCHEDULE_INTERVAL,
            runtime.replay_payload_for(TIMER_SCHEDULE_INTERVAL)?,
            context,
            |context| {
                platform_runtime_vm::destack_timer_interval(runtime, context, periodns, options)
            },
            |context, result| {
                let _ = &context;
                if let Ok(value) = result {
                    let result_value: resource::TimerHandle = value.clone();
                    let result_recorded = result_value;
                    let payload = TimerScheduleIntervalReplay {
                        result: Ok(result_recorded),
                    };
                    return Ok(Some(payload));
                }

                if let Err(error) = result {
                    let payload = {
                        let result = Err(PlatformError::from(error.as_ref()));
                        TimerScheduleIntervalReplay { result }
                    };
                    return Ok(Some(payload));
                }

                Ok(None)
            },
            |context, payload| {
                let _ = &context;
                // replay result
                match payload.result {
                    Ok(value) => {
                        let vm_result = value;
                        Ok(vm_result)
                    }
                    Err(error) => Err(RuntimeError::from(error).boxed()),
                }
            },
        );
    let result = encode_destack_timer_schedule_interval_result(context, result)?;
    Ok(result)
}

#[inline]
fn destack_timer_schedule_once_vm_replay(
    runtime: &RuntimeCallContext,
    context: &mut vm::ExternalCallContext<'_>,
    delayns: u64,
    options: TimerOptionsVm,
) -> RuntimeResult<vm::Value> {
    let result = runtime
        .replay()
        .run_binding_with_context_and_payload_policy(
            TIMER_SCHEDULE_ONCE,
            runtime.replay_payload_for(TIMER_SCHEDULE_ONCE)?,
            context,
            |context| platform_runtime_vm::destack_timer_once(runtime, context, delayns, options),
            |context, result| {
                let _ = &context;
                if let Ok(value) = result {
                    let result_value: resource::TimerHandle = value.clone();
                    let result_recorded = result_value;
                    let payload = TimerScheduleOnceReplay {
                        result: Ok(result_recorded),
                    };
                    return Ok(Some(payload));
                }

                if let Err(error) = result {
                    let payload = {
                        let result = Err(PlatformError::from(error.as_ref()));
                        TimerScheduleOnceReplay { result }
                    };
                    return Ok(Some(payload));
                }

                Ok(None)
            },
            |context, payload| {
                let _ = &context;
                // replay result
                match payload.result {
                    Ok(value) => {
                        let vm_result = value;
                        Ok(vm_result)
                    }
                    Err(error) => Err(RuntimeError::from(error).boxed()),
                }
            },
        );
    let result = encode_destack_timer_schedule_once_result(context, result)?;
    Ok(result)
}

/// Register VM bindings for timer.
pub fn register_timer_vm_bindings(registry: &mut BindingRegistry, isolate: &mut Isolate) {
    {
        binding!(
            registry,
            isolate,
            TIMER_CONTROL_CANCEL,
            move |context, args| {
                with_runtime_call_context(|runtime| {
                    // decode args
                    let (handle,) = decode_destack_timer_control_cancel_args(context, args)?;

                    // execute binding
                    runtime.check_policy(TIMER_CONTROL_CANCEL)?;
                    destack_timer_control_cancel_vm_replay(runtime, context, handle)
                })
                .map_err(Into::into)
            }
        );
    }
    {
        binding!(
            registry,
            isolate,
            TIMER_CONTROL_IS_ACTIVE,
            move |context, args| {
                with_runtime_call_context(|runtime| {
                    // decode args
                    let (handle,) = decode_destack_timer_control_is_active_args(context, args)?;

                    // execute binding
                    runtime.check_policy(TIMER_CONTROL_IS_ACTIVE)?;
                    destack_timer_control_is_active_vm_replay(runtime, context, handle)
                })
                .map_err(Into::into)
            }
        );
    }
    {
        binding!(
            registry,
            isolate,
            TIMER_CONTROL_PAUSE,
            move |context, args| {
                with_runtime_call_context(|runtime| {
                    // decode args
                    let (handle,) = decode_destack_timer_control_pause_args(context, args)?;

                    // execute binding
                    runtime.check_policy(TIMER_CONTROL_PAUSE)?;
                    destack_timer_control_pause_vm_replay(runtime, context, handle)
                })
                .map_err(Into::into)
            }
        );
    }
    {
        binding!(
            registry,
            isolate,
            TIMER_CONTROL_REMAINING_NS,
            move |context, args| {
                with_runtime_call_context(|runtime| {
                    // decode args
                    let (handle,) = decode_destack_timer_control_remaining_ns_args(context, args)?;

                    // execute binding
                    runtime.check_policy(TIMER_CONTROL_REMAINING_NS)?;
                    destack_timer_control_remaining_ns_vm_replay(runtime, context, handle)
                })
                .map_err(Into::into)
            }
        );
    }
    {
        binding!(
            registry,
            isolate,
            TIMER_CONTROL_RESET,
            move |context, args| {
                with_runtime_call_context(|runtime| {
                    // decode args
                    let (handle, delayns) = decode_destack_timer_control_reset_args(context, args)?;

                    // execute binding
                    runtime.check_policy(TIMER_CONTROL_RESET)?;
                    destack_timer_control_reset_vm_replay(runtime, context, handle, delayns)
                })
                .map_err(Into::into)
            }
        );
    }
    {
        binding!(
            registry,
            isolate,
            TIMER_CONTROL_RESUME,
            move |context, args| {
                with_runtime_call_context(|runtime| {
                    // decode args
                    let (handle,) = decode_destack_timer_control_resume_args(context, args)?;

                    // execute binding
                    runtime.check_policy(TIMER_CONTROL_RESUME)?;
                    destack_timer_control_resume_vm_replay(runtime, context, handle)
                })
                .map_err(Into::into)
            }
        );
    }
    {
        binding!(
            registry,
            isolate,
            TIMER_CONTROL_UPDATE_INTERVAL,
            move |context, args| {
                with_runtime_call_context(|runtime| {
                    // decode args
                    let (handle, periodns) =
                        decode_destack_timer_control_update_interval_args(context, args)?;

                    // execute binding
                    runtime.check_policy(TIMER_CONTROL_UPDATE_INTERVAL)?;
                    destack_timer_control_update_interval_vm_replay(
                        runtime, context, handle, periodns,
                    )
                })
                .map_err(Into::into)
            }
        );
    }
    {
        binding!(registry, isolate, TIMER_FD_CLOSE, move |context, args| {
            with_runtime_call_context(|runtime| {
                // decode args
                let (handle,) = decode_destack_timer_fd_close_args(context, args)?;

                // execute binding
                runtime.check_policy(TIMER_FD_CLOSE)?;
                let world = runtime.check_and_resolve_world(TIMER_FD_CLOSE)?;
                destack_timer_fd_close_vm_replay(runtime, context, world, handle)
            })
            .map_err(Into::into)
        });
    }
    {
        binding!(registry, isolate, TIMER_FD_GET, move |context, args| {
            with_runtime_call_context(|runtime| {
                // decode args
                let (handle,) = decode_destack_timer_fd_get_args(context, args)?;

                // execute binding
                runtime.check_policy(TIMER_FD_GET)?;
                let world = runtime.check_and_resolve_world(TIMER_FD_GET)?;
                destack_timer_fd_get_vm_replay(runtime, context, world, handle)
            })
            .map_err(Into::into)
        });
    }
    {
        binding!(registry, isolate, TIMER_FD_OPEN, move |context, args| {
            with_runtime_call_context(|runtime| {
                // decode args
                let (clock, flags) = decode_destack_timer_fd_open_args(context, args)?;

                // execute binding
                runtime.check_policy(TIMER_FD_OPEN)?;
                let world = runtime.check_and_resolve_world(TIMER_FD_OPEN)?;
                destack_timer_fd_open_vm_replay(runtime, context, world, clock, flags)
            })
            .map_err(Into::into)
        });
    }
    {
        binding!(registry, isolate, TIMER_FD_READ, move |context, args| {
            with_runtime_call_context(|runtime| {
                // decode args
                let (handle,) = decode_destack_timer_fd_read_args(context, args)?;

                // execute binding
                runtime.check_policy(TIMER_FD_READ)?;
                let world = runtime.check_and_resolve_world(TIMER_FD_READ)?;
                destack_timer_fd_read_vm_replay(runtime, context, world, handle)
            })
            .map_err(Into::into)
        });
    }
    {
        binding!(registry, isolate, TIMER_FD_SET, move |context, args| {
            with_runtime_call_context(|runtime| {
                // decode args
                let (handle, spec, flags) = decode_destack_timer_fd_set_args(context, args)?;

                // execute binding
                runtime.check_policy(TIMER_FD_SET)?;
                let world = runtime.check_and_resolve_world(TIMER_FD_SET)?;
                destack_timer_fd_set_vm_replay(runtime, context, world, handle, spec, flags)
            })
            .map_err(Into::into)
        });
    }
    {
        binding!(
            registry,
            isolate,
            TIMER_SCHEDULE_AT,
            move |context, args| {
                with_runtime_call_context(|runtime| {
                    // decode args
                    let (deadlinens, options) =
                        decode_destack_timer_schedule_at_args(context, args)?;

                    // execute binding
                    runtime.check_policy(TIMER_SCHEDULE_AT)?;
                    destack_timer_schedule_at_vm_replay(runtime, context, deadlinens, options)
                })
                .map_err(Into::into)
            }
        );
    }
    {
        binding!(
            registry,
            isolate,
            TIMER_SCHEDULE_INTERVAL,
            move |context, args| {
                with_runtime_call_context(|runtime| {
                    // decode args
                    let (periodns, options) =
                        decode_destack_timer_schedule_interval_args(context, args)?;

                    // execute binding
                    runtime.check_policy(TIMER_SCHEDULE_INTERVAL)?;
                    destack_timer_schedule_interval_vm_replay(runtime, context, periodns, options)
                })
                .map_err(Into::into)
            }
        );
    }
    {
        binding!(
            registry,
            isolate,
            TIMER_SCHEDULE_ONCE,
            move |context, args| {
                with_runtime_call_context(|runtime| {
                    // decode args
                    let (delayns, options) =
                        decode_destack_timer_schedule_once_args(context, args)?;

                    // execute binding
                    runtime.check_policy(TIMER_SCHEDULE_ONCE)?;
                    destack_timer_schedule_once_vm_replay(runtime, context, delayns, options)
                })
                .map_err(Into::into)
            }
        );
    }
}

/// Install VM bindings for timer.
pub fn install_timer_vm_bindings(registry: &mut BindingRegistry, isolate: &mut Isolate) {
    register_timer_vm_bindings(registry, isolate);
}

vm_binding_set!(pub TIMER_VM_BINDINGS, "timer", install_timer_vm_bindings);
