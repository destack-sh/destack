// generated by generate-bindings: test harness, do not edit

#![allow(dead_code)]
#![allow(unused_imports)]
#![allow(clippy::type_complexity)]

use super::*;
use crate::diagnostic::{RuntimeError, RuntimeResult};
use crate::platform::tty::{
    PtyPair, PtyPairVm, TtyMode, TtyModeVm, TtySize, TtySizeVm, native as tty_native, vm as tty_vm,
};
use crate::platform::{NativeSlice, PlatformError as HarnessPlatformError, VmSlice, resource};
use destack_vm as vm;

impl<'call> TtyHarnessContext<'call> {
    /// Return the generated VM context if available.
    #[allow(clippy::mut_from_ref)]
    fn generated_vm_context_mut(&self) -> Option<&mut vm::ExternalCallContext<'_>> {
        self.vm_context
            .map(|context| unsafe { &mut *(context as *mut vm::ExternalCallContext<'_>) })
    }

    /// Return one standardized value payload for native and VM variants.
    pub(crate) fn harness_value<Native, Vm>(&self, native: Native) -> HarnessValue<Native, Vm> {
        HarnessValue::Native(native)
    }

    /// Return one standardized value payload for VM and native variants.
    pub(crate) fn harness_value_vm<Native, Vm>(&self, vm: Vm) -> HarnessValue<Native, Vm> {
        HarnessValue::Vm(vm)
    }

    /// Read bytes from a terminal.
    ///
    /// Read one byte sequence from one terminal handle into caller memory.
    /// Read mode and canonical processing depend on active terminal mode settings.
    ///
    /// # Platform
    /// Unix and Windows.
    /// Uses read(2) on Unix terminals and ReadConsole or ReadFile on Windows consoles.
    ///
    /// # Errors
    /// Returns invalidArgument, ioNotFound, ioWouldBlock, ioInvalidData, notSupported.
    ///
    /// # Security
    /// Requires `tty.read`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_tty_read(
        &mut self,
        handle: resource::TtyHandle,
        buffer: HarnessValue<NativeSlice<u8>, VmSlice<u8>>,
    ) -> RuntimeResult<u64> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let buffer = buffer.into_vm("buffer")?;
                let out = tty_vm::destack_tty_read(self.call_context, context, handle, buffer)?;
                Ok(out)
            }
            None => {
                let buffer = buffer.into_native("buffer")?;
                let mut out = std::mem::MaybeUninit::<u64>::uninit();
                unsafe {
                    tty_native::destack_tty_read(
                        self.call_context,
                        out.as_mut_ptr(),
                        handle,
                        buffer,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// Write bytes to a terminal.
    ///
    /// Write one byte sequence from caller memory to one terminal handle.
    /// Encoding and newline translation follow host terminal API behavior.
    ///
    /// # Platform
    /// Unix and Windows.
    /// Uses write(2) on Unix terminals and WriteConsole or WriteFile on Windows consoles.
    ///
    /// # Errors
    /// Returns invalidArgument, ioNotFound, ioWouldBlock, ioInvalidData, notSupported.
    ///
    /// # Security
    /// Requires `tty.write`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_tty_write(
        &mut self,
        handle: resource::TtyHandle,
        buffer: HarnessValue<NativeSlice<u8>, VmSlice<u8>>,
    ) -> RuntimeResult<u64> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let buffer = buffer.into_vm("buffer")?;
                let out = tty_vm::destack_tty_write(self.call_context, context, handle, buffer)?;
                Ok(out)
            }
            None => {
                let buffer = buffer.into_native("buffer")?;
                let mut out = std::mem::MaybeUninit::<u64>::uninit();
                unsafe {
                    tty_native::destack_tty_write(
                        self.call_context,
                        out.as_mut_ptr(),
                        handle,
                        buffer,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(out)
            }
        }
    }

    /// Read terminal mode flags.
    ///
    /// Read one terminal mode snapshot for one terminal handle.
    /// Mode mapping is normalized across host terminal APIs.
    ///
    /// # Platform
    /// Unix and Windows.
    /// Uses termios get attributes on Unix and GetConsoleMode on Windows.
    ///
    /// # Errors
    /// Returns invalidArgument, ioNotFound, ioWouldBlock, ioInvalidData, notSupported.
    ///
    /// # Security
    /// Requires `tty.mode`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_tty_get_mode(
        &mut self,
        handle: resource::TtyHandle,
    ) -> RuntimeResult<HarnessValue<TtyMode, TtyModeVm>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = tty_vm::destack_tty_get_mode(self.call_context, context, handle)?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let mut out = std::mem::MaybeUninit::<TtyMode>::uninit();
                unsafe {
                    tty_native::destack_tty_get_mode(self.call_context, out.as_mut_ptr(), handle)?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Apply terminal mode flags.
    ///
    /// Apply one terminal mode snapshot to one terminal handle.
    /// Mode transition timing and unsupported bits follow host API behavior.
    ///
    /// # Platform
    /// Unix and Windows.
    /// Uses termios set attributes on Unix and SetConsoleMode on Windows.
    ///
    /// # Errors
    /// Returns invalidArgument, ioNotFound, ioWouldBlock, ioInvalidData, notSupported.
    ///
    /// # Security
    /// Requires `tty.mode`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_tty_set_mode(
        &mut self,
        handle: resource::TtyHandle,
        mode: HarnessValue<TtyMode, TtyModeVm>,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let mode = mode.into_vm("mode")?;
                tty_vm::destack_tty_set_mode(self.call_context, context, handle, mode)
            }
            None => {
                let mode = mode.into_native("mode")?;
                unsafe { tty_native::destack_tty_set_mode(self.call_context, handle, mode) }
            }
        }
    }

    /// Close one pseudo-terminal controller.
    ///
    /// Close one pseudo-terminal controller endpoint.
    /// Worker endpoint behavior after close follows host pseudo-terminal semantics.
    ///
    /// # Platform
    /// Unix and Windows.
    /// Uses host pseudo-terminal handle close APIs.
    ///
    /// # Errors
    /// Returns invalidArgument, ioNotFound, ioWouldBlock, ioInvalidData, notSupported.
    ///
    /// # Security
    /// Requires `tty.mode`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_tty_pty_close(
        &mut self,
        handle: resource::PtyHandle,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => tty_vm::destack_tty_pty_close(self.call_context, context, handle),
            None => unsafe { tty_native::destack_tty_pty_close(self.call_context, handle) },
        }
    }

    /// Open one pseudo-terminal pair.
    ///
    /// Create one controller and worker terminal endpoint pair.
    /// Endpoint ownership and inheritance follow host pseudo-terminal semantics.
    ///
    /// # Platform
    /// Unix and Windows.
    /// Uses posix_openpt and openpty on Unix and ConPTY on Windows.
    ///
    /// # Errors
    /// Returns invalidArgument, ioPermissionDenied, ioWouldBlock, ioInvalidData, notSupported.
    ///
    /// # Security
    /// Requires `tty.mode`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_tty_pty_open(
        &mut self,
        rows: u32,
        columns: u32,
        flags: u32,
    ) -> RuntimeResult<HarnessValue<PtyPair, PtyPairVm>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out =
                    tty_vm::destack_tty_pty_open(self.call_context, context, rows, columns, flags)?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let mut out = std::mem::MaybeUninit::<PtyPair>::uninit();
                unsafe {
                    tty_native::destack_tty_pty_open(
                        self.call_context,
                        out.as_mut_ptr(),
                        rows,
                        columns,
                        flags,
                    )?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Read terminal size.
    ///
    /// Read one terminal size snapshot for one terminal handle.
    /// Pixel fields may be zero when host APIs do not provide pixel metrics.
    ///
    /// # Platform
    /// Unix and Windows.
    /// Uses TIOCGWINSZ on Unix and console buffer APIs on Windows.
    ///
    /// # Errors
    /// Returns invalidArgument, ioNotFound, ioWouldBlock, ioInvalidData, notSupported.
    ///
    /// # Security
    /// Requires `tty.size`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_tty_get_size(
        &mut self,
        handle: resource::TtyHandle,
    ) -> RuntimeResult<HarnessValue<TtySize, TtySizeVm>> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let out = tty_vm::destack_tty_get_size(self.call_context, context, handle)?;
                Ok(HarnessValue::Vm(out))
            }
            None => {
                let mut out = std::mem::MaybeUninit::<TtySize>::uninit();
                unsafe {
                    tty_native::destack_tty_get_size(self.call_context, out.as_mut_ptr(), handle)?;
                }
                let out = unsafe { out.assume_init() };
                Ok(HarnessValue::Native(out))
            }
        }
    }

    /// Apply terminal size.
    ///
    /// Apply one terminal size to one terminal handle.
    /// Resize propagation to attached sessions follows host terminal semantics.
    ///
    /// # Platform
    /// Unix and Windows.
    /// Uses TIOCSWINSZ on Unix and console size APIs on Windows.
    ///
    /// # Errors
    /// Returns invalidArgument, ioNotFound, ioWouldBlock, ioInvalidData, notSupported.
    ///
    /// # Security
    /// Requires `tty.size`.
    ///
    /// # Replay
    /// External, recordable.
    pub(crate) fn destack_tty_set_size(
        &mut self,
        handle: resource::TtyHandle,
        size: HarnessValue<TtySize, TtySizeVm>,
    ) -> RuntimeResult<()> {
        match self.generated_vm_context_mut() {
            Some(context) => {
                let size = size.into_vm("size")?;
                tty_vm::destack_tty_set_size(self.call_context, context, handle, size)
            }
            None => {
                let size = size.into_native("size")?;
                unsafe { tty_native::destack_tty_set_size(self.call_context, handle, size) }
            }
        }
    }
}

/// Wrapper that carries one native or VM value for generated harness bindings.
pub(crate) enum HarnessValue<Native, Vm> {
    /// Native value variant.
    Native(Native),
    /// VM value variant.
    Vm(Vm),
}

impl<Native, Vm> HarnessValue<Native, Vm> {
    /// Convert one wrapper into one native value.
    fn into_native(self, label: &str) -> RuntimeResult<Native> {
        match self {
            HarnessValue::Native(value) => Ok(value),
            HarnessValue::Vm(_) => Err(RuntimeError::from(
                HarnessPlatformError::invalid_argument_value(
                    label,
                    "expected native harness value",
                ),
            )
            .boxed()),
        }
    }

    /// Convert one wrapper into one VM value.
    fn into_vm(self, label: &str) -> RuntimeResult<Vm> {
        match self {
            HarnessValue::Vm(value) => Ok(value),
            HarnessValue::Native(_) => Err(RuntimeError::from(
                HarnessPlatformError::invalid_argument_value(label, "expected VM harness value"),
            )
            .boxed()),
        }
    }
}
