// generated by generate-bindings: do not edit

import Foundation
import RuntimeHostAppleBridgeC
import RuntimeHostAppleCore

/// Submit one host permission request.
typealias PermissionRequestCallback =
  @convention(c) (
    UInt64,
    DestackRustHostPermissionRequest
  ) -> UInt32

/// Open one host permission settings surface.
typealias PermissionOpenSettingsCallback =
  @convention(c) (
    UInt64
  ) -> UInt32

let permissionRequestCallback: PermissionRequestCallback = {
  sessionHandle,
  request
  in
  handlePermissionRequest(
    sessionHandle: sessionHandle,
    request: request
  )
}

let permissionOpenSettingsCallback: PermissionOpenSettingsCallback = {
  sessionHandle
  in
  handlePermissionOpenSettings(
    sessionHandle: sessionHandle
  )
}

/// Build one permission callback bundle.
func makePermissionCallbacks() -> IosHostPermissionCallbacks {
  IosHostPermissionCallbacks(
    request: permissionRequestCallback,
    open_settings: permissionOpenSettingsCallback
  )
}

/// Handle one runtime callback asking to submit one host permission request.
func handlePermissionRequest(
  sessionHandle: UInt64,
  request: DestackRustHostPermissionRequest
) -> UInt32 {
  let decodedRequest: RuntimeHostPermissionRequest
  do {
    decodedRequest = try decodeBridgeHostPermissionRequest(request)
  } catch {
    return hostStatusInvalidArgument
  }

  return runOnMainThread {
    withResolvedRuntimeBridge(
      sessionHandle: sessionHandle,
      onMissing: { hostStatusNotFound }
    ) { bridge, runtimeHost in
      bridge.permissionRequest(
        runtimeHost: runtimeHost,
        decodedRequest
      )
    }
  }
}

/// Handle one runtime callback asking to open one host permission settings surface.
func handlePermissionOpenSettings(
  sessionHandle: UInt64
) -> UInt32 {
  return runOnMainThread {
    withResolvedRuntimeBridge(
      sessionHandle: sessionHandle,
      onMissing: { hostStatusNotFound }
    ) { bridge, runtimeHost in
      bridge.permissionOpenSettings(
        runtimeHost: runtimeHost
      )
    }
  }
}

/// One temporary native allocation arena for one bridge callback.
final class PermissionBridgeArena {
  /// The raw deallocation actions recorded for this callback.
  private var deallocations: [() -> Void] = []

  /// Remove every recorded allocation before one new callback payload is encoded.
  func reset() {
    for deallocate in deallocations.reversed() {
      deallocate()
    }

    deallocations.removeAll(keepingCapacity: true)
  }

  /// Release every recorded native allocation.
  deinit {
    for deallocate in deallocations.reversed() {
      deallocate()
    }
  }

  /// Allocate one copied UTF-8 buffer for one Swift string.
  func makeStringRef(
    _ value: String
  ) -> DestackRustStringRef {
    let bytes = Array(value.utf8)
    if bytes.isEmpty {
      return DestackRustStringRef(data: nil, len: 0)
    }

    let storage = UnsafeMutablePointer<UInt8>.allocate(capacity: bytes.count)
    storage.initialize(from: bytes, count: bytes.count)
    deallocations.append {
      storage.deinitialize(count: bytes.count)
      storage.deallocate()
    }

    return DestackRustStringRef(
      data: UnsafePointer(storage),
      len: UInt32(bytes.count)
    )
  }

  /// Allocate one copied string-slice payload.
  func makeStringSlice(
    _ values: [String]
  ) -> DestackRustStringSlice {
    let data = makeArray(count: values.count) { buffer in
      for (index, value) in values.enumerated() {
        buffer[index] = makeStringRef(value)
      }
    }

    return DestackRustStringSlice(
      data: data,
      len: UInt32(values.count)
    )
  }

  /// Allocate one copied native array and fill it with one builder closure.
  func makeArray<Element>(
    count: Int,
    fill: (UnsafeMutableBufferPointer<Element>) -> Void
  ) -> UnsafeMutablePointer<Element>? {
    if count == 0 {
      return nil
    }

    let storage = UnsafeMutablePointer<Element>.allocate(capacity: count)
    let buffer = UnsafeMutableBufferPointer(start: storage, count: count)
    fill(buffer)
    deallocations.append {
      storage.deinitialize(count: count)
      storage.deallocate()
    }

    return storage
  }
}

/// Return the thread-local bridge arena for one callback thread.
func currentPermissionBridgeArena() -> PermissionBridgeArena {
  let dictionary = Thread.current.threadDictionary
  let key = "dev.destack.runtime.apple.permission-bridge-arena"

  if let arena = dictionary[key] as? PermissionBridgeArena {
    arena.reset()

    return arena
  }

  let arena = PermissionBridgeArena()
  dictionary[key] = arena

  return arena
}

/// Build one Swift HostPermission from one bridge raw value.
func decodeBridgeHostPermission(
  _ value: DestackRustHostPermission
) throws -> RuntimeHostPermission {
  guard let value = RuntimeHostPermission(rawValue: .init(value)) else {
    throw BridgeStringError.invalidStringSlice
  }

  return value
}

/// Build one Swift HostPermissionRequest from one bridge payload.
func decodeBridgeHostPermissionRequest(
  _ value: DestackRustHostPermissionRequest
) throws -> RuntimeHostPermissionRequest {
  RuntimeHostPermissionRequest(
    requestID:
      HostRequestID(rawValue: value.request_id),
    permission:
      try decodeBridgeHostPermission(value.permission)
  )
}

/// Encode one Swift HostPermission as one bridge raw value.
func encodeBridgeHostPermission(
  _ value: RuntimeHostPermission
) -> DestackRustHostPermission {
  Int32(value.rawValue)
}

/// Encode one Swift HostPermissionRequest as one bridge payload.
func encodeBridgeHostPermissionRequest(
  _ value: RuntimeHostPermissionRequest,
  arena: PermissionBridgeArena
) -> DestackRustHostPermissionRequest {
  DestackRustHostPermissionRequest(
    request_id:
      value.requestID.rawValue,
    permission:
      encodeBridgeHostPermission(value.permission)
  )
}

func encodeBridgeHostPermissionRequest(
  _ value: RuntimeHostPermissionRequest
) -> DestackRustHostPermissionRequest {
  let arena = currentPermissionBridgeArena()

  return encodeBridgeHostPermissionRequest(value, arena: arena)
}

/// Encode one Swift HostPermissionRequest as one C bridge payload.
func withNativeHostPermissionRequest<T>(
  _ value: RuntimeHostPermissionRequest,
  body: (DestackRustHostPermissionRequest) -> T
) -> T {
  let arena = currentPermissionBridgeArena()

  return body(encodeBridgeHostPermissionRequest(value, arena: arena))
}

/// Encode one Swift HostPermissionEvent as one bridge payload.
func encodeBridgeHostPermissionEvent(
  _ value: RuntimeHostPermissionEvent,
  arena: PermissionBridgeArena
) -> DestackRustHostPermissionEvent {
  DestackRustHostPermissionEvent(
    request_id:
      value.requestID.rawValue,
    permission:
      encodeBridgeHostPermission(value.permission),
    is_granted:
      value.isGranted
  )
}

func encodeBridgeHostPermissionEvent(
  _ value: RuntimeHostPermissionEvent
) -> DestackRustHostPermissionEvent {
  let arena = currentPermissionBridgeArena()

  return encodeBridgeHostPermissionEvent(value, arena: arena)
}

/// Encode one Swift HostPermissionEvent as one C bridge payload.
func withNativeHostPermissionEvent<T>(
  _ value: RuntimeHostPermissionEvent,
  body: (DestackRustHostPermissionEvent) -> T
) -> T {
  let arena = currentPermissionBridgeArena()

  return body(encodeBridgeHostPermissionEvent(value, arena: arena))
}
