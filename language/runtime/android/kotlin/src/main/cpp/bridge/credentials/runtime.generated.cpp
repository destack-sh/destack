// generated by generate-bindings: do not edit

#include "../types.h"
#include "../jni.h"
#include "../runtime_abi.generated.h"
#include "callbacks.generated.h"
#include "runtime.generated.h"

#include <deque>
#include <string>
#include <vector>

namespace {

/// Convert one host status code into one runtime status.
RuntimeStatus runtime_status_from_code(uint32_t code) {
    RuntimeStatus status = {
        .code = code,
        .error_id = 0,
    };

    return status;
}

}

/// Forward the `read` credentials request through one attached bridge session.
uint32_t call_credentials_read_for_session(
    uint64_t session_handle,
    NativeStringRef service,
    NativeStringRef account,
    NativeStringRef access_group,
    bool require_authentication,
    NativeU8Slice output,
    uint32_t *output_written,
    uint64_t *created_unix_ns,
    uint64_t *modified_unix_ns
) {
    JNIEnv *env = nullptr;
    bool did_attach_thread = false;

    if (!resolve_jni_env(&env, &did_attach_thread)) {
        return HOST_STATUS_FAILED;
    }

    uint32_t status = call_credentials_read(env, session_handle, service, account, access_group, require_authentication, output, output_written, created_unix_ns, modified_unix_ns);

    detach_jni_thread(did_attach_thread);

    return status;
}

/// Forward the `write` credentials request through one attached bridge session.
uint32_t call_credentials_write_for_session(
    uint64_t session_handle,
    NativeStringRef service,
    NativeStringRef account,
    NativeStringRef access_group,
    NativeU8Slice payload,
    uint32_t accessibility,
    uint32_t authentication_policy,
    bool replace_existing
) {
    JNIEnv *env = nullptr;
    bool did_attach_thread = false;

    if (!resolve_jni_env(&env, &did_attach_thread)) {
        return HOST_STATUS_FAILED;
    }

    uint32_t status = call_credentials_write(env, session_handle, service, account, access_group, payload, accessibility, authentication_policy, replace_existing);

    detach_jni_thread(did_attach_thread);

    return status;
}

/// Forward the `delete` credentials request through one attached bridge session.
uint32_t call_credentials_delete_for_session(
    uint64_t session_handle,
    NativeStringRef service,
    NativeStringRef account,
    NativeStringRef access_group
) {
    JNIEnv *env = nullptr;
    bool did_attach_thread = false;

    if (!resolve_jni_env(&env, &did_attach_thread)) {
        return HOST_STATUS_FAILED;
    }

    uint32_t status = call_credentials_delete(env, session_handle, service, account, access_group);

    detach_jni_thread(did_attach_thread);

    return status;
}

/// Forward the `contains` credentials request through one attached bridge session.
uint32_t call_credentials_contains_for_session(
    uint64_t session_handle,
    NativeStringRef service,
    NativeStringRef account,
    NativeStringRef access_group,
    bool *is_present
) {
    JNIEnv *env = nullptr;
    bool did_attach_thread = false;

    if (!resolve_jni_env(&env, &did_attach_thread)) {
        return HOST_STATUS_FAILED;
    }

    uint32_t status = call_credentials_contains(env, session_handle, service, account, access_group, is_present);

    detach_jni_thread(did_attach_thread);

    return status;
}

/// Forward the `authenticate` credentials request through one attached bridge session.
uint32_t call_credentials_authenticate_for_session(
    uint64_t session_handle,
    NativeStringRef title,
    NativeStringRef subtitle,
    NativeStringRef message,
    uint32_t requirement,
    bool *authenticated,
    uint32_t *mechanism
) {
    JNIEnv *env = nullptr;
    bool did_attach_thread = false;

    if (!resolve_jni_env(&env, &did_attach_thread)) {
        return HOST_STATUS_FAILED;
    }

    uint32_t status = call_credentials_authenticate(env, session_handle, title, subtitle, message, requirement, authenticated, mechanism);

    detach_jni_thread(did_attach_thread);

    return status;
}

/// Build one Android host credentials callback bundle.
AndroidHostCredentialsCallbacks make_credentials_callbacks() {
    return AndroidHostCredentialsCallbacks {
        .read = call_credentials_read_for_session,
        .write = call_credentials_write_for_session,
        .delete_ = call_credentials_delete_for_session,
        .contains = call_credentials_contains_for_session,
        .authenticate = call_credentials_authenticate_for_session,
    };
}
