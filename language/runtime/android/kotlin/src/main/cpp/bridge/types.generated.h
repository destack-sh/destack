// generated by generate-bindings: do not edit

#ifndef DESTACK_RUNTIME_ANDROID_BRIDGE_TYPES_GENERATED_H
#define DESTACK_RUNTIME_ANDROID_BRIDGE_TYPES_GENERATED_H

enum class HostBackgroundStatus : uint32_t;
enum class HostBackgroundTriggerKind : uint32_t;
enum class HostBackgroundTaskResult : uint32_t;
enum class HostBackgroundNetworkRequirement : uint32_t;
enum class HostBackgroundConflictPolicy : uint32_t;
enum class HostBackgroundTaskScheduleKind : uint32_t;
struct HostBackgroundTaskSchedule;
struct HostBackgroundTaskOptions;
struct HostBackgroundStatusResponse;
struct HostBackgroundTaskDescriptor;
struct HostBackgroundListResponse;
struct HostBackgroundUnregisterRequest;
struct HostBackgroundTriggerTestRequest;
struct HostBackgroundTriggerTestResponse;
struct HostBackgroundCompleteRequest;
struct HostBackgroundEventMetadata;
enum class HostBackgroundEventKind : uint32_t;
struct HostBackgroundEvent;
struct AndroidHostBluetoothAdapterDescriptorHeader;
struct AndroidHostBluetoothDeviceDescriptorHeader;
struct AndroidHostBluetoothScanFilterHeader;
struct AndroidHostBluetoothGattServiceHeader;
struct AndroidHostBluetoothGattCharacteristicHeader;
struct AndroidHostBluetoothGattDescriptorHeader;
struct AndroidHostBluetoothScanEventHeader;
struct AndroidHostBluetoothSessionEventHeader;
enum class HostCalendarAccess : uint32_t;
enum class HostCalendarAvailability : uint32_t;
enum class HostCalendarParticipantStatus : uint32_t;
enum class HostCalendarRecurrenceFrequency : uint32_t;
enum class HostCalendarReminderKind : uint32_t;
struct HostCalendarRecurrenceWeekday;
struct HostCalendarRecurrenceRule;
struct HostCalendarAttendee;
struct HostCalendarReminder;
struct HostCalendarDescriptor;
struct HostCalendarEvent;
struct HostCalendarEventDraft;
struct HostCalendarEventQuery;
struct HostCalendarListResponse;
struct HostCalendarEventListResponse;
struct HostCalendarEventReadResponse;
struct HostCalendarEventCreateResponse;
struct AndroidHostCameraRecordingCapabilitiesHeader;
struct AndroidHostCameraRecordingOptionsHeader;
struct AndroidHostCameraDeviceDescriptorHeader;
struct AndroidHostCameraStreamConfigHeader;
struct AndroidHostCameraStreamCapabilityHeader;
struct AndroidHostCameraFrameHeader;
struct HostContactQuery;
struct HostContactName;
struct HostContactPhone;
struct HostContactEmail;
struct HostContactAddress;
struct HostContactOrganization;
struct HostContact;
struct HostContactDraft;
struct HostContactPage;
struct HostContactPageResponse;
struct HostContactResponse;
struct HostContactCreateResponse;
struct HostDocumentRequest;
struct HostDocumentDescriptor;
enum class LocationAccuracy : int32_t;
struct LocationSample;
struct LocationWatchOptions;
struct LocationServicesResponse;
struct LocationLastKnownResponse;
enum class HostMediaAssetKind : uint32_t;
struct HostMediaListRequest;
struct HostMediaAssetDescriptor;
struct HostMediaListResult;
struct HostMediaListResponse;
struct HostMediaReadResponse;
struct HostMediaImportPathRequest;
struct HostMediaImportPathResponse;
struct HostMediaDeleteRequest;
struct HostMediaDeleteResponse;
struct AndroidHostMidiPortDescriptorHeader;
struct AndroidHostMidiOpenedPortHeader;
struct AndroidHostMidiInputRecordHeader;
struct AndroidHostMidiOutputRecordHeader;
struct AndroidHostMidiEventHeader;
struct HostNotificationRequest;
enum class HostNotificationEventKind : uint32_t;
struct HostNotificationEvent;
struct HostPermissionRequest;
struct HostPermissionEvent;
enum class HostTextInputType : int32_t;
struct HostTextInputRange;
struct HostTextInputRectangle;
struct HostTextInputTransform2D;
struct HostTextInputGeometry;
struct HostTextInputConfiguration;
struct HostTextInputState;
struct HostTextInputOpenRequest;
struct HostTextInputCloseRequest;
struct HostTextInputGeometryRequest;
struct HostTextInputStateRequest;
struct HostTextInputEvent;
struct AndroidHostUsbDeviceDescriptorHeader;
struct AndroidHostUsbHotplugEventHeader;

/// One HostBackgroundTaskDescriptor slice passed through the Android bridge.
struct HostBackgroundTaskDescriptorSlice {
    const HostBackgroundTaskDescriptor *data;
    uint32_t len;
};

/// One AndroidHostBluetoothAdapterDescriptorHeader slice passed through the Android bridge.
struct AndroidHostBluetoothAdapterDescriptorHeaderSlice {
    const AndroidHostBluetoothAdapterDescriptorHeader *data;
    uint32_t len;
};

/// One AndroidHostBluetoothDeviceDescriptorHeader slice passed through the Android bridge.
struct AndroidHostBluetoothDeviceDescriptorHeaderSlice {
    const AndroidHostBluetoothDeviceDescriptorHeader *data;
    uint32_t len;
};

/// One AndroidHostBluetoothGattServiceHeader slice passed through the Android bridge.
struct AndroidHostBluetoothGattServiceHeaderSlice {
    const AndroidHostBluetoothGattServiceHeader *data;
    uint32_t len;
};

/// One AndroidHostBluetoothGattCharacteristicHeader slice passed through the Android bridge.
struct AndroidHostBluetoothGattCharacteristicHeaderSlice {
    const AndroidHostBluetoothGattCharacteristicHeader *data;
    uint32_t len;
};

/// One AndroidHostBluetoothGattDescriptorHeader slice passed through the Android bridge.
struct AndroidHostBluetoothGattDescriptorHeaderSlice {
    const AndroidHostBluetoothGattDescriptorHeader *data;
    uint32_t len;
};

/// One HostCalendarDescriptor slice passed through the Android bridge.
struct HostCalendarDescriptorSlice {
    const HostCalendarDescriptor *data;
    uint32_t len;
};

/// One HostCalendarEvent slice passed through the Android bridge.
struct HostCalendarEventSlice {
    const HostCalendarEvent *data;
    uint32_t len;
};

/// One HostCalendarRecurrenceWeekday slice passed through the Android bridge.
struct HostCalendarRecurrenceWeekdaySlice {
    const HostCalendarRecurrenceWeekday *data;
    uint32_t len;
};

/// One HostCalendarAttendee slice passed through the Android bridge.
struct HostCalendarAttendeeSlice {
    const HostCalendarAttendee *data;
    uint32_t len;
};

/// One HostCalendarReminder slice passed through the Android bridge.
struct HostCalendarReminderSlice {
    const HostCalendarReminder *data;
    uint32_t len;
};

/// One AndroidHostCameraDeviceDescriptorHeader slice passed through the Android bridge.
struct AndroidHostCameraDeviceDescriptorHeaderSlice {
    const AndroidHostCameraDeviceDescriptorHeader *data;
    uint32_t len;
};

/// One AndroidHostCameraStreamConfigHeader slice passed through the Android bridge.
struct AndroidHostCameraStreamConfigHeaderSlice {
    const AndroidHostCameraStreamConfigHeader *data;
    uint32_t len;
};

/// One AndroidHostCameraStreamCapabilityHeader slice passed through the Android bridge.
struct AndroidHostCameraStreamCapabilityHeaderSlice {
    const AndroidHostCameraStreamCapabilityHeader *data;
    uint32_t len;
};

/// One HostContact slice passed through the Android bridge.
struct HostContactSlice {
    const HostContact *data;
    uint32_t len;
};

/// One HostContactPhone slice passed through the Android bridge.
struct HostContactPhoneSlice {
    const HostContactPhone *data;
    uint32_t len;
};

/// One HostContactEmail slice passed through the Android bridge.
struct HostContactEmailSlice {
    const HostContactEmail *data;
    uint32_t len;
};

/// One HostContactAddress slice passed through the Android bridge.
struct HostContactAddressSlice {
    const HostContactAddress *data;
    uint32_t len;
};

/// One HostDocumentDescriptor slice passed through the Android bridge.
struct HostDocumentDescriptorSlice {
    const HostDocumentDescriptor *data;
    uint32_t len;
};

/// One HostMediaAssetKind slice passed through the Android bridge.
struct HostMediaAssetKindSlice {
    const HostMediaAssetKind *data;
    uint32_t len;
};

/// One HostMediaAssetDescriptor slice passed through the Android bridge.
struct HostMediaAssetDescriptorSlice {
    const HostMediaAssetDescriptor *data;
    uint32_t len;
};

/// One AndroidHostMidiPortDescriptorHeader slice passed through the Android bridge.
struct AndroidHostMidiPortDescriptorHeaderSlice {
    const AndroidHostMidiPortDescriptorHeader *data;
    uint32_t len;
};

/// One AndroidHostMidiInputRecordHeader slice passed through the Android bridge.
struct AndroidHostMidiInputRecordHeaderSlice {
    const AndroidHostMidiInputRecordHeader *data;
    uint32_t len;
};

/// One AndroidHostMidiEventHeader slice passed through the Android bridge.
struct AndroidHostMidiEventHeaderSlice {
    const AndroidHostMidiEventHeader *data;
    uint32_t len;
};

/// One AndroidHostMidiOutputRecordHeader slice passed through the Android bridge.
struct AndroidHostMidiOutputRecordHeaderSlice {
    const AndroidHostMidiOutputRecordHeader *data;
    uint32_t len;
};

/// One AndroidHostUsbDeviceDescriptorHeader slice passed through the Android bridge.
struct AndroidHostUsbDeviceDescriptorHeaderSlice {
    const AndroidHostUsbDeviceDescriptorHeader *data;
    uint32_t len;
};

/// One host background scheduler status payload.
enum class HostBackgroundStatus : uint32_t {
    /// Background scheduling is unavailable.
    Unavailable = 1,
    /// Background scheduling is restricted.
    Restricted = 2,
    /// Background scheduling is available.
    Available = 3,
};

/// One host background trigger kind payload.
enum class HostBackgroundTriggerKind : uint32_t {
    /// One app-refresh style task.
    AppRefresh = 1,
    /// One processing style task.
    Processing = 2,
};

/// One host background task-result payload.
enum class HostBackgroundTaskResult : uint32_t {
    /// The task completed successfully.
    Success = 1,
    /// The task should be retried later.
    Retry = 2,
    /// The task failed and should not be retried automatically.
    Failure = 3,
};

/// One host background network-requirement payload.
enum class HostBackgroundNetworkRequirement : uint32_t {
    /// No network route is required.
    None = 1,
    /// One network route is required.
    Connected = 2,
    /// One unmetered network route is required.
    Unmetered = 3,
};

/// One host background conflict-policy payload.
enum class HostBackgroundConflictPolicy : uint32_t {
    /// Replace one existing registration.
    Replace = 1,
    /// Keep one existing registration.
    Keep = 2,
};

/// One host background schedule-kind payload.
enum class HostBackgroundTaskScheduleKind : uint32_t {
    /// One single future execution.
    Once = 1,
    /// One recurring execution stream.
    Recurring = 2,
};

/// One host background task-schedule payload.
struct HostBackgroundTaskSchedule {
    /// The declared schedule class.
    HostBackgroundTaskScheduleKind kind;
    /// Whether the earliest execution target is present.
    bool has_earliest_begin_unix_ns;
    /// The earliest execution target in UTC nanoseconds.
    uint64_t earliest_begin_unix_ns;
    /// Whether the repeat interval is present.
    bool has_repeat_interval_ns;
    /// The repeat interval in nanoseconds for recurring schedules.
    uint64_t repeat_interval_ns;
};

/// One host background task-options payload.
struct HostBackgroundTaskOptions {
    /// The stable task identifier.
    NativeStringRef identifier;
    /// The trigger class.
    HostBackgroundTriggerKind trigger;
    /// The requested schedule payload.
    HostBackgroundTaskSchedule schedule;
    /// The requested network requirement.
    HostBackgroundNetworkRequirement network;
    /// Whether charging power is required.
    bool requires_charging;
    /// Whether idle mode is required.
    bool requires_idle;
    /// The registration conflict policy.
    HostBackgroundConflictPolicy conflict_policy;
};

/// One host background status response payload.
struct HostBackgroundStatusResponse {
    /// The request status code.
    uint32_t status;
    /// Whether one scheduler status is present.
    bool has_scheduler_status;
    /// The scheduler status when present.
    HostBackgroundStatus scheduler_status;
};

/// One host background task-descriptor payload.
struct HostBackgroundTaskDescriptor {
    /// The stable task identifier.
    NativeStringRef identifier;
    /// The trigger class.
    HostBackgroundTriggerKind trigger;
    /// The effective schedule payload.
    HostBackgroundTaskSchedule schedule;
    /// The effective network requirement.
    HostBackgroundNetworkRequirement network;
    /// Whether charging power is required.
    bool requires_charging;
    /// Whether idle mode is required.
    bool requires_idle;
    /// The registration conflict policy.
    HostBackgroundConflictPolicy conflict_policy;
};

/// One host background task-list response payload.
struct HostBackgroundListResponse {
    /// The request status code.
    uint32_t status;
    /// The returned task descriptors.
    HostBackgroundTaskDescriptorSlice descriptors;
};

/// One host background unregister request payload.
struct HostBackgroundUnregisterRequest {
    /// The stable task identifier.
    NativeStringRef identifier;
};

/// One host background trigger-test request payload.
struct HostBackgroundTriggerTestRequest {
    /// The stable task identifier.
    NativeStringRef identifier;
};

/// One host background trigger-test response payload.
struct HostBackgroundTriggerTestResponse {
    /// The request status code.
    uint32_t status;
    /// Whether the host triggered one task execution.
    bool is_triggered;
};

/// One host background complete request payload.
struct HostBackgroundCompleteRequest {
    /// The stable execution identifier.
    NativeStringRef execution_id;
    /// The completion result.
    HostBackgroundTaskResult result;
};

/// One host background event metadata payload.
struct HostBackgroundEventMetadata {
    /// The monotonic event timestamp in nanoseconds.
    uint64_t timestamp_ns;
    /// The monotonic sequence number for this event stream.
    uint64_t sequence;
    /// The stable task identifier.
    NativeStringRef identifier;
    /// The stable execution identifier for this scheduled execution.
    NativeStringRef execution_id;
    /// The host execution deadline in UTC nanoseconds.
    uint64_t deadline_unix_ns;
};

/// One host background event kind payload.
enum class HostBackgroundEventKind : uint32_t {
    /// One task-ready event.
    TaskReady = 1,
    /// One task-expired event.
    TaskExpired = 2,
};

/// One host background event payload.
struct HostBackgroundEvent {
    /// The event kind.
    HostBackgroundEventKind kind;
    /// The shared event metadata.
    HostBackgroundEventMetadata metadata;
};

/// Fixed-size host bluetooth adapter descriptor header.
struct AndroidHostBluetoothAdapterDescriptorHeader {
    /// Offset of the stable id string.
    uint32_t id_offset;
    /// Length of the stable id string.
    uint32_t id_len;
    /// Offset of the adapter name string.
    uint32_t name_offset;
    /// Length of the adapter name string.
    uint32_t name_len;
    /// Capability flags bitset.
    uint32_t capability_flags;
    /// Transport flags bitset.
    uint32_t transport_flags;
    /// Whether the adapter is powered.
    uint32_t is_powered;
    /// Whether the adapter is discoverable.
    uint32_t is_discoverable;
    /// Whether the adapter is currently discovering.
    uint32_t is_discovering;
};

/// Fixed-size host bluetooth device descriptor header.
struct AndroidHostBluetoothDeviceDescriptorHeader {
    /// Offset of the stable id string.
    uint32_t id_offset;
    /// Length of the stable id string.
    uint32_t id_len;
    /// Offset of the adapter id string.
    uint32_t adapter_id_offset;
    /// Length of the adapter id string.
    uint32_t adapter_id_len;
    /// Offset of the device name string.
    uint32_t name_offset;
    /// Length of the device name string.
    uint32_t name_len;
    /// Offset of the local-name string.
    uint32_t local_name_offset;
    /// Length of the local-name string.
    uint32_t local_name_len;
    /// Offset of the manufacturer string.
    uint32_t manufacturer_offset;
    /// Length of the manufacturer string.
    uint32_t manufacturer_len;
    /// Offset of the model string.
    uint32_t model_offset;
    /// Length of the model string.
    uint32_t model_len;
    /// Offset of the hardware address string.
    uint32_t address_offset;
    /// Length of the hardware address string.
    uint32_t address_len;
    /// Offset of the serialized advertisement service-uuid payload.
    uint32_t service_uuids_offset;
    /// Length of the serialized advertisement service-uuid payload.
    uint32_t service_uuids_len;
    /// Offset of the serialized advertisement manufacturer-data payload.
    uint32_t manufacturer_data_offset;
    /// Length of the serialized advertisement manufacturer-data payload.
    uint32_t manufacturer_data_len;
    /// Offset of the serialized advertisement service-data payload.
    uint32_t service_data_offset;
    /// Length of the serialized advertisement service-data payload.
    uint32_t service_data_len;
    /// Transport code.
    uint32_t transport;
    /// RSSI in dBm.
    int32_t rssi_dbm;
    /// Current pair-state code.
    uint32_t pair_state;
    /// Primary PHY flags.
    uint32_t phy_flags;
    /// Whether the device is connected.
    uint32_t is_connected;
};

/// Fixed-size host bluetooth scan-filter header.
struct AndroidHostBluetoothScanFilterHeader {
    /// Whether the filter is present.
    uint32_t is_present;
    /// Offset of the exact-name string.
    uint32_t name_offset;
    /// Length of the exact-name string.
    uint32_t name_len;
    /// Offset of the name-prefix string.
    uint32_t name_prefix_offset;
    /// Length of the name-prefix string.
    uint32_t name_prefix_len;
    /// Whether the minimum RSSI is present.
    uint32_t has_minimum_rssi;
    /// Minimum RSSI threshold in dBm.
    int32_t minimum_rssi_dbm;
    /// Requested scan-mode code.
    uint32_t scan_mode;
    /// Requested primary PHY code.
    uint32_t primary_phy;
    /// Requested secondary PHY code.
    uint32_t secondary_phy;
    /// Whether repeated devices should be kept.
    uint32_t keep_repeated_devices;
    /// Offset of the serialized service-uuid filter payload.
    uint32_t service_uuids_offset;
    /// Length of the serialized service-uuid filter payload.
    uint32_t service_uuids_len;
    /// Offset of the serialized manufacturer-data filter payload.
    uint32_t manufacturer_data_offset;
    /// Length of the serialized manufacturer-data filter payload.
    uint32_t manufacturer_data_len;
    /// Offset of the serialized service-data filter payload.
    uint32_t service_data_offset;
    /// Length of the serialized service-data filter payload.
    uint32_t service_data_len;
};

/// Fixed-size host bluetooth GATT service header.
struct AndroidHostBluetoothGattServiceHeader {
    /// Offset of the service id string.
    uint32_t id_offset;
    /// Length of the service id string.
    uint32_t id_len;
    /// Offset of the UUID string.
    uint32_t uuid_offset;
    /// Length of the UUID string.
    uint32_t uuid_len;
    /// Whether the service is primary.
    uint32_t is_primary;
};

/// Fixed-size host bluetooth GATT characteristic header.
struct AndroidHostBluetoothGattCharacteristicHeader {
    /// Offset of the characteristic id string.
    uint32_t id_offset;
    /// Length of the characteristic id string.
    uint32_t id_len;
    /// Offset of the service id string.
    uint32_t service_id_offset;
    /// Length of the service id string.
    uint32_t service_id_len;
    /// Offset of the UUID string.
    uint32_t uuid_offset;
    /// Length of the UUID string.
    uint32_t uuid_len;
    /// Property flags bitset.
    uint32_t property_flags;
};

/// Fixed-size host bluetooth GATT descriptor header.
struct AndroidHostBluetoothGattDescriptorHeader {
    /// Offset of the descriptor id string.
    uint32_t id_offset;
    /// Length of the descriptor id string.
    uint32_t id_len;
    /// Offset of the characteristic id string.
    uint32_t characteristic_id_offset;
    /// Length of the characteristic id string.
    uint32_t characteristic_id_len;
    /// Offset of the UUID string.
    uint32_t uuid_offset;
    /// Length of the UUID string.
    uint32_t uuid_len;
};

/// Fixed-size host bluetooth scan event header.
struct AndroidHostBluetoothScanEventHeader {
    /// Event timestamp in monotonic nanoseconds.
    uint64_t timestamp_ns;
    /// Event kind code.
    uint32_t kind;
    /// Embedded descriptor payload.
    AndroidHostBluetoothDeviceDescriptorHeader descriptor;
};

/// Fixed-size host bluetooth session event header.
struct AndroidHostBluetoothSessionEventHeader {
    /// Event timestamp in monotonic nanoseconds.
    uint64_t timestamp_ns;
    /// Event kind code.
    uint32_t kind;
    /// Pair-state code for pair-state events.
    uint32_t pair_state;
    /// Reserved flags.
    uint32_t flags;
};

/// One host calendar access payload.
enum class HostCalendarAccess : uint32_t {
    /// One read-only calendar.
    Read = 1,
    /// One read-write calendar.
    Write = 2,
};

/// One host calendar availability payload.
enum class HostCalendarAvailability : uint32_t {
    /// One busy slot.
    Busy = 1,
    /// One free slot.
    Free = 2,
    /// One tentative slot.
    Tentative = 3,
    /// One out-of-office slot.
    OutOfOffice = 4,
    /// One unavailable slot.
    Unavailable = 5,
    /// One unknown slot.
    Unknown = 6,
};

/// One host calendar participant-status payload.
enum class HostCalendarParticipantStatus : uint32_t {
    /// One unknown response.
    Unknown = 1,
    /// One pending response.
    Pending = 2,
    /// One accepted response.
    Accepted = 3,
    /// One tentative response.
    Tentative = 4,
    /// One declined response.
    Declined = 5,
    /// One delegated response.
    Delegated = 6,
    /// One completed response.
    Completed = 7,
    /// One in-process response.
    InProcess = 8,
};

/// One host calendar recurrence-frequency payload.
enum class HostCalendarRecurrenceFrequency : uint32_t {
    /// One daily recurrence.
    Daily = 1,
    /// One weekly recurrence.
    Weekly = 2,
    /// One monthly recurrence.
    Monthly = 3,
    /// One yearly recurrence.
    Yearly = 4,
};

/// One host calendar reminder-kind payload.
enum class HostCalendarReminderKind : uint32_t {
    /// One absolute reminder.
    Absolute = 1,
    /// One relative reminder.
    Relative = 2,
};

/// One host calendar recurrence-weekday payload.
struct HostCalendarRecurrenceWeekday {
    /// The weekday number in ISO-8601 encoding, 1 through 7.
    uint8_t day;
    /// Whether the weekday carries one ordinal week number.
    bool has_week_number;
    /// The ordinal week number when present.
    int8_t week_number;
};

/// One host calendar recurrence-rule payload.
struct HostCalendarRecurrenceRule {
    /// The recurrence frequency.
    HostCalendarRecurrenceFrequency frequency;
    /// The recurrence interval.
    uint32_t interval;
    /// Whether the recurrence carries one occurrence count.
    bool has_count;
    /// The bounded occurrence count when present.
    uint32_t count;
    /// Whether the recurrence carries one end timestamp.
    bool has_until_unix_ns;
    /// The bounded end timestamp in UTC nanoseconds when present.
    uint64_t until_unix_ns;
    /// The weekday numbers in ISO-8601 encoding.
    NativeU8Slice by_week_days;
    /// The weekday selectors with optional ordinals.
    HostCalendarRecurrenceWeekdaySlice by_weekday_ordinals;
    /// The day-of-month set.
    NativeI8Slice by_month_days;
    /// The month set, 1 through 12.
    NativeU8Slice by_months;
    /// The day-of-year set.
    NativeI16Slice by_year_days;
    /// The week-of-year set.
    NativeI8Slice by_week_numbers;
    /// The final set-position filters.
    NativeI16Slice by_set_positions;
};

/// One host calendar attendee payload.
struct HostCalendarAttendee {
    /// Whether the attendee carries one stable identifier.
    bool has_id;
    /// The stable attendee identifier when present.
    NativeStringRef id;
    /// Whether the attendee carries one display name.
    bool has_name;
    /// The display name when present.
    NativeStringRef name;
    /// Whether the attendee carries one email address.
    bool has_email;
    /// The email address when present.
    NativeStringRef email;
    /// Whether the attendee is optional.
    bool optional;
    /// Whether the attendee is the organizer.
    bool organizer;
    /// The attendee response status.
    HostCalendarParticipantStatus response_status;
};

/// One host calendar reminder payload.
struct HostCalendarReminder {
    /// The reminder variant kind.
    HostCalendarReminderKind kind;
    /// The absolute reminder timestamp in UTC nanoseconds.
    uint64_t absolute_unix_ns;
    /// The minutes before the event start for relative reminders.
    int32_t minutes_before_start;
};

/// One host calendar descriptor payload.
struct HostCalendarDescriptor {
    /// The stable calendar identifier.
    NativeStringRef id;
    /// The calendar title payload.
    NativeStringRef title;
    /// The source or account label payload.
    NativeStringRef source;
    /// Whether the descriptor carries one owner label.
    bool has_owner;
    /// The owner label when present.
    NativeStringRef owner;
    /// The ARGB color payload.
    uint32_t color_argb;
    /// Whether this is the primary write target.
    bool primary;
    /// The calendar access mode.
    HostCalendarAccess access;
};

/// One host calendar event payload.
struct HostCalendarEvent {
    /// The stable event identifier.
    NativeStringRef id;
    /// The calendar identifier.
    NativeStringRef calendar_id;
    /// The event title payload.
    NativeStringRef title;
    /// Whether the event carries one notes payload.
    bool has_notes;
    /// The notes payload when present.
    NativeStringRef notes;
    /// Whether the event carries one location payload.
    bool has_location;
    /// The location payload when present.
    NativeStringRef location;
    /// The start timestamp in UTC nanoseconds.
    uint64_t start_unix_ns;
    /// The end timestamp in UTC nanoseconds.
    uint64_t end_unix_ns;
    /// Whether the event is all-day.
    bool all_day;
    /// Whether the event is canceled.
    bool canceled;
    /// Whether the event carries one timezone identifier.
    bool has_time_zone;
    /// The timezone identifier when present.
    NativeStringRef time_zone;
    /// The event availability class.
    HostCalendarAvailability availability;
    /// Whether the event carries one URL.
    bool has_url;
    /// The event URL when present.
    NativeStringRef url;
    /// Whether the event carries one organizer name.
    bool has_organizer_name;
    /// The organizer name when present.
    NativeStringRef organizer_name;
    /// Whether the event carries one organizer email.
    bool has_organizer_email;
    /// The organizer email when present.
    NativeStringRef organizer_email;
    /// Whether the event is one recurring item.
    bool recurring;
    /// Whether the event carries one recurrence-master identifier.
    bool has_recurrence_master_id;
    /// The recurrence-master identifier when present.
    NativeStringRef recurrence_master_id;
    /// Whether the event carries one recurrence-instance timestamp.
    bool has_recurrence_id_unix_ns;
    /// The recurrence-instance timestamp when present.
    uint64_t recurrence_id_unix_ns;
    /// Whether the event carries one recurrence rule.
    bool has_recurrence_rule;
    /// The recurrence rule when present.
    HostCalendarRecurrenceRule recurrence_rule;
    /// Whether the event carries one attendee list.
    bool has_attendees;
    /// The attendee list when present.
    HostCalendarAttendeeSlice attendees;
    /// Whether the event carries one reminder list.
    bool has_reminders;
    /// The reminder list when present.
    HostCalendarReminderSlice reminders;
};

/// One host calendar event-draft payload.
struct HostCalendarEventDraft {
    /// The calendar identifier.
    NativeStringRef calendar_id;
    /// The event title payload.
    NativeStringRef title;
    /// Whether the draft carries one notes payload.
    bool has_notes;
    /// The notes payload when present.
    NativeStringRef notes;
    /// Whether the draft carries one location payload.
    bool has_location;
    /// The location payload when present.
    NativeStringRef location;
    /// The start timestamp in UTC nanoseconds.
    uint64_t start_unix_ns;
    /// The end timestamp in UTC nanoseconds.
    uint64_t end_unix_ns;
    /// Whether the event is all-day.
    bool all_day;
    /// Whether the draft carries one timezone identifier.
    bool has_time_zone;
    /// The timezone identifier when present.
    NativeStringRef time_zone;
    /// The event availability class.
    HostCalendarAvailability availability;
    /// Whether the draft carries one URL.
    bool has_url;
    /// The event URL when present.
    NativeStringRef url;
    /// Whether the draft carries one recurrence rule.
    bool has_recurrence_rule;
    /// The recurrence rule when present.
    HostCalendarRecurrenceRule recurrence_rule;
    /// Whether the draft carries one attendee list.
    bool has_attendees;
    /// The attendee list when present.
    HostCalendarAttendeeSlice attendees;
    /// Whether the draft carries one reminder list.
    bool has_reminders;
    /// The reminder list when present.
    HostCalendarReminderSlice reminders;
};

/// One host calendar event-query payload.
struct HostCalendarEventQuery {
    /// The selected calendar identifiers, empty means every readable calendar.
    NativeStringSlice calendar_ids;
    /// The query start timestamp in UTC nanoseconds.
    uint64_t start_unix_ns;
    /// The query end timestamp in UTC nanoseconds.
    uint64_t end_unix_ns;
    /// Whether the query carries one page limit.
    bool has_limit;
    /// The maximum returned event count when present.
    uint32_t limit;
    /// Whether canceled events should be included.
    bool include_canceled;
    /// Whether declined events should be included.
    bool include_declined;
    /// Whether expanded recurrence instances should be included.
    bool include_recurrence_instances;
};

/// One host calendar-list response payload.
struct HostCalendarListResponse {
    /// The request status code.
    uint32_t status;
    /// The returned calendars.
    HostCalendarDescriptorSlice calendars;
};

/// One host calendar event-list response payload.
struct HostCalendarEventListResponse {
    /// The request status code.
    uint32_t status;
    /// The returned events.
    HostCalendarEventSlice events;
};

/// One host calendar event-read response payload.
struct HostCalendarEventReadResponse {
    /// The request status code.
    uint32_t status;
    /// Whether the response carries one event.
    bool has_event;
    /// The returned event when present.
    HostCalendarEvent event;
};

/// One host calendar event-create response payload.
struct HostCalendarEventCreateResponse {
    /// The request status code.
    uint32_t status;
    /// Whether the response carries one created identifier.
    bool has_id;
    /// The created identifier when present.
    NativeStringRef id;
};

/// Fixed-size host camera recording capability header.
struct AndroidHostCameraRecordingCapabilitiesHeader {
    /// Supported container flags.
    uint32_t container_flags;
    /// Supported video codec flags.
    uint32_t video_codec_flags;
    /// Whether audio recording is supported.
    uint32_t audio_supported;
    /// Supported audio codec flags.
    uint32_t audio_codec_flags;
    /// Whether pause and resume are supported.
    uint32_t pause_supported;
    /// Maximum supported video bit rate in bits per second.
    uint64_t maximum_video_bit_rate;
    /// Whether the maximum video bit rate is present.
    uint32_t has_maximum_video_bit_rate;
    /// Maximum supported audio bit rate in bits per second.
    uint64_t maximum_audio_bit_rate;
    /// Whether the maximum audio bit rate is present.
    uint32_t has_maximum_audio_bit_rate;
};

/// Fixed-size host camera recording options header.
struct AndroidHostCameraRecordingOptionsHeader {
    /// Preferred recording container code.
    uint32_t container;
    /// Preferred video codec code.
    uint32_t video_codec;
    /// Whether the audio-enabled flag is present.
    uint32_t has_audio_enabled;
    /// Requested audio-enabled value when present.
    uint32_t audio_enabled;
    /// Preferred audio codec code.
    uint32_t audio_codec;
    /// Requested video bit rate in bits per second.
    uint64_t video_bit_rate;
    /// Whether the video bit rate is present.
    uint32_t has_video_bit_rate;
    /// Requested audio bit rate in bits per second.
    uint64_t audio_bit_rate;
    /// Whether the audio bit rate is present.
    uint32_t has_audio_bit_rate;
    /// Requested key-frame interval in frames.
    uint32_t key_frame_interval_frames;
    /// Whether the key-frame interval is present.
    uint32_t has_key_frame_interval_frames;
    /// Requested maximum duration in nanoseconds.
    uint64_t maximum_duration_ns;
    /// Whether the maximum duration is present.
    uint32_t has_maximum_duration_ns;
    /// Requested maximum output size in bytes.
    uint64_t maximum_bytes;
    /// Whether the maximum output size is present.
    uint32_t has_maximum_bytes;
};

/// Fixed-size host camera device descriptor header.
struct AndroidHostCameraDeviceDescriptorHeader {
    /// Offset of the stable id string.
    uint32_t id_offset;
    /// Length of the stable id string.
    uint32_t id_len;
    /// Offset of the optional group id string.
    uint32_t group_id_offset;
    /// Length of the optional group id string.
    uint32_t group_id_len;
    /// Offset of the display name string.
    uint32_t name_offset;
    /// Length of the display name string.
    uint32_t name_len;
    /// Offset of the manufacturer string.
    uint32_t manufacturer_offset;
    /// Length of the manufacturer string.
    uint32_t manufacturer_len;
    /// Facing-mode code.
    uint32_t facing_mode;
    /// Whether depth is supported.
    uint32_t depth_capable;
};

/// Fixed-size host camera stream config header.
struct AndroidHostCameraStreamConfigHeader {
    /// Frame width in pixels.
    uint32_t width;
    /// Frame height in pixels.
    uint32_t height;
    /// Frame rate in milli-hertz.
    uint32_t frame_rate_milli_hz;
    /// Pixel-format code.
    uint32_t pixel_format;
    /// Pixel-format family code.
    uint32_t pixel_format_family;
    /// Whether the format is compressed.
    uint32_t is_compressed;
};

/// Fixed-size host camera stream capability header.
struct AndroidHostCameraStreamCapabilityHeader {
    /// Base stream configuration.
    AndroidHostCameraStreamConfigHeader config;
    /// Minimum frame rate in milli-hertz.
    uint32_t minimum_frame_rate_milli_hz;
    /// Maximum frame rate in milli-hertz.
    uint32_t maximum_frame_rate_milli_hz;
    /// Supported color-space flags.
    uint32_t color_space_flags;
    /// Supported dynamic-range flags.
    uint32_t dynamic_range_flags;
    /// Supported exposure-mode flags.
    uint32_t exposure_mode_flags;
    /// Supported white-balance-mode flags.
    uint32_t white_balance_mode_flags;
    /// Supported focus-mode flags.
    uint32_t focus_mode_flags;
    /// Supported stabilization-mode flags.
    uint32_t stabilization_mode_flags;
    /// Supported torch-mode flags.
    uint32_t torch_mode_flags;
};

/// Fixed-size host camera frame header.
struct AndroidHostCameraFrameHeader {
    /// Frame timestamp in monotonic nanoseconds.
    uint64_t timestamp_ns;
    /// Frame sequence number.
    uint64_t sequence;
    /// Width in pixels.
    uint32_t width;
    /// Height in pixels.
    uint32_t height;
    /// Pixel-format code.
    uint32_t pixel_format;
    /// Pixel-format family code.
    uint32_t pixel_format_family;
    /// Whether the format is compressed.
    uint32_t is_compressed;
    /// Color-space code.
    uint32_t color_space;
    /// Plane count.
    uint32_t plane_count;
    /// Frame byte length.
    uint32_t bytes_len;
};

/// One host contact query payload.
struct HostContactQuery {
    /// Whether the query carries one cursor.
    bool has_cursor;
    /// The opaque cursor from one prior list or search call.
    NativeStringRef cursor;
    /// Whether the query carries one limit.
    bool has_limit;
    /// The maximum returned contacts for this page.
    uint32_t limit;
    /// Whether phone values should be returned.
    bool include_phones;
    /// Whether email values should be returned.
    bool include_emails;
    /// Whether postal-address values should be returned.
    bool include_addresses;
    /// Whether organization metadata should be returned.
    bool include_organization;
    /// Whether note fields should be returned.
    bool include_notes;
};

/// One host contact-name payload.
struct HostContactName {
    /// The given or first name.
    NativeStringRef given_name;
    /// The middle name.
    NativeStringRef middle_name;
    /// The family or last name.
    NativeStringRef family_name;
    /// The honorific prefix.
    NativeStringRef prefix;
    /// The honorific suffix.
    NativeStringRef suffix;
    /// The nickname.
    NativeStringRef nickname;
    /// The phonetic given name.
    NativeStringRef phonetic_given_name;
    /// The phonetic family name.
    NativeStringRef phonetic_family_name;
};

/// One host contact-phone payload.
struct HostContactPhone {
    /// The user-visible label for this phone value.
    NativeStringRef label;
    /// The original phone number string.
    NativeStringRef number;
    /// The normalized phone number string when available.
    NativeStringRef normalized_number;
    /// Whether this phone value is marked as primary.
    bool primary;
};

/// One host contact-email payload.
struct HostContactEmail {
    /// The user-visible label for this email value.
    NativeStringRef label;
    /// The email address.
    NativeStringRef address;
    /// Whether this email value is marked as primary.
    bool primary;
};

/// One host contact-address payload.
struct HostContactAddress {
    /// The user-visible label for this address value.
    NativeStringRef label;
    /// The street-line payload.
    NativeStringRef street;
    /// The city payload.
    NativeStringRef city;
    /// The region or state payload.
    NativeStringRef region;
    /// The postal-code payload.
    NativeStringRef postal_code;
    /// The country payload.
    NativeStringRef country;
    /// The country-code payload.
    NativeStringRef country_code;
};

/// One host contact-organization payload.
struct HostContactOrganization {
    /// The company or organization name.
    NativeStringRef company;
    /// The department name.
    NativeStringRef department;
    /// The job title.
    NativeStringRef title;
};

/// One host contact payload.
struct HostContact {
    /// The stable host contact identifier.
    NativeStringRef id;
    /// The structured name payload.
    HostContactName name;
    /// The phone values.
    HostContactPhoneSlice phones;
    /// The email values.
    HostContactEmailSlice emails;
    /// The address values.
    HostContactAddressSlice addresses;
    /// The organization metadata.
    HostContactOrganization organization;
    /// The contact note payload.
    NativeStringRef note;
};

/// One host contact draft payload.
struct HostContactDraft {
    /// The structured name payload.
    HostContactName name;
    /// The phone values.
    HostContactPhoneSlice phones;
    /// The email values.
    HostContactEmailSlice emails;
    /// The address values.
    HostContactAddressSlice addresses;
    /// The organization metadata.
    HostContactOrganization organization;
    /// The contact note payload.
    NativeStringRef note;
};

/// One host contact page payload.
struct HostContactPage {
    /// The listed contacts for this page.
    HostContactSlice contacts;
    /// The opaque next-page cursor.
    NativeStringRef next_cursor;
    /// Whether more contacts are available.
    bool has_more;
};

/// One host contact-page response payload.
struct HostContactPageResponse {
    /// The request status code.
    uint32_t status;
    /// Whether the response includes one page.
    bool has_page;
    /// The returned contact page when available.
    HostContactPage page;
};

/// One host contact-read response payload.
struct HostContactResponse {
    /// The request status code.
    uint32_t status;
    /// Whether the response includes one contact.
    bool has_contact;
    /// The returned contact when available.
    HostContact contact;
};

/// One host contact-create response payload.
struct HostContactCreateResponse {
    /// The request status code.
    uint32_t status;
    /// Whether the response includes one created contact identifier.
    bool has_id;
    /// The created contact identifier when available.
    NativeStringRef id;
};

/// One host document request payload.
struct HostDocumentRequest {
    /// The stable request identifier for this interactive host flow.
    uint64_t request_id;
    /// MIME-type filters, empty means any type.
    NativeStringSlice mime_types;
    /// File-extension filters without the leading dot.
    NativeStringSlice extensions;
    /// Whether multiple documents may be selected.
    bool allows_multiple_selection;
    /// Whether directory selection is allowed.
    bool allows_directory_selection;
    /// Whether the host should copy selected files into one runtime-visible sandbox path when possible.
    bool copies_to_sandbox;
};

/// One host document descriptor payload.
struct HostDocumentDescriptor {
    /// The stable URI or content identifier returned by the host.
    NativeStringRef uri;
    /// The normalized document name.
    NativeStringRef name;
    /// Whether the host provided one MIME type.
    bool has_mime_type;
    /// The normalized content type when available.
    NativeStringRef mime_type;
    /// Whether the host provided one size.
    bool has_size_bytes;
    /// The document size in bytes when available.
    uint64_t size_bytes;
    /// Whether the host provided one modification timestamp.
    bool has_modified_unix_ns;
    /// The document modification timestamp in UTC nanoseconds when available.
    uint64_t modified_unix_ns;
    /// Whether this descriptor represents one directory.
    bool is_directory;
    /// Whether the host provided one local path.
    bool has_local_path;
    /// The optional host-local path when the host exposes one directly.
    NativeStringRef local_path;
};

/// One location accuracy preference.
enum class LocationAccuracy : int32_t {
    /// Passive.
    Passive = 1,
    /// Low.
    Low = 2,
    /// Balanced.
    Balanced = 3,
    /// High.
    High = 4,
    /// Best.
    Best = 5,
};

/// One location sample payload.
struct LocationSample {
    /// Latitude in degrees.
    double latitude_degrees;
    /// Longitude in degrees.
    double longitude_degrees;
    /// Altitude in meters above mean sea level when available.
    double altitude_meters;
    /// Horizontal accuracy radius in meters.
    double horizontal_accuracy_meters;
    /// Vertical accuracy in meters when available.
    double vertical_accuracy_meters;
    /// Speed in meters per second when available.
    double speed_meters_per_second;
    /// Heading in degrees when available.
    double heading_degrees;
    /// UTC timestamp in nanoseconds.
    uint64_t timestamp_unix_ns;
};

/// One location watch-options payload.
struct LocationWatchOptions {
    /// Accuracy preference.
    LocationAccuracy accuracy;
    /// Minimum interval between updates in nanoseconds.
    uint64_t minimum_interval_ns;
    /// Minimum distance delta in meters.
    double minimum_distance_meters;
    /// Whether heading should be included when available.
    bool include_heading;
};

/// One location-services response payload.
struct LocationServicesResponse {
    /// The request status code.
    uint32_t status;
    /// Whether location services are enabled.
    bool is_enabled;
};

/// One last-known location response payload.
struct LocationLastKnownResponse {
    /// The request status code.
    uint32_t status;
    /// Whether the response includes one sample.
    bool has_sample;
    /// The returned sample when available.
    LocationSample sample;
};

/// One host media asset-kind payload.
enum class HostMediaAssetKind : uint32_t {
    /// One image asset.
    Image = 1,
    /// One video asset.
    Video = 2,
    /// One audio asset.
    Audio = 3,
    /// One non-standard asset.
    Other = 4,
};

/// One host media-list request payload.
struct HostMediaListRequest {
    /// Whether the request carries one cursor.
    bool has_cursor;
    /// The opaque cursor from one prior media-list call.
    NativeStringRef cursor;
    /// Whether the request carries one limit.
    bool has_limit;
    /// The maximum returned assets for this page.
    uint32_t limit;
    /// The requested asset kinds, empty means every kind.
    HostMediaAssetKindSlice kinds;
    /// Whether hidden assets should be included.
    bool include_hidden;
};

/// One host media asset-descriptor payload.
struct HostMediaAssetDescriptor {
    /// The stable asset identifier.
    NativeStringRef identifier;
    /// The host URI for this asset.
    NativeStringRef uri;
    /// The asset filename payload.
    NativeStringRef filename;
    /// The asset MIME type payload when available.
    NativeStringRef mime_type;
    /// The asset class.
    HostMediaAssetKind kind;
    /// The asset width in pixels when available.
    uint32_t width;
    /// The asset height in pixels when available.
    uint32_t height;
    /// The asset duration in milliseconds for time-based assets.
    uint64_t duration_ms;
    /// The asset size in bytes when available.
    uint64_t size_bytes;
    /// The asset creation timestamp in UTC nanoseconds when available.
    uint64_t created_unix_ns;
    /// The asset modification timestamp in UTC nanoseconds when available.
    uint64_t modified_unix_ns;
};

/// One host media-list result payload.
struct HostMediaListResult {
    /// The returned assets for this page.
    HostMediaAssetDescriptorSlice assets;
    /// The opaque next-page cursor, empty when absent.
    NativeStringRef next_cursor;
    /// Whether more assets are available.
    bool has_more;
};

/// One host media-list response payload.
struct HostMediaListResponse {
    /// The request status code.
    uint32_t status;
    /// Whether the response includes one page.
    bool has_page;
    /// The returned page when available.
    HostMediaListResult page;
};

/// One host media-read response payload.
struct HostMediaReadResponse {
    /// The request status code.
    uint32_t status;
    /// Whether the response includes one descriptor.
    bool has_descriptor;
    /// The returned descriptor when available.
    HostMediaAssetDescriptor descriptor;
};

/// One host media-import request payload.
struct HostMediaImportPathRequest {
    /// The local path to import into the host media library.
    NativeStringRef path;
    /// The requested asset kind.
    HostMediaAssetKind kind;
};

/// One host media-import response payload.
struct HostMediaImportPathResponse {
    /// The request status code.
    uint32_t status;
    /// Whether the response includes one imported asset identifier.
    bool has_identifier;
    /// The imported asset identifier when available.
    NativeStringRef identifier;
};

/// One host media-delete request payload.
struct HostMediaDeleteRequest {
    /// The stable host media identifiers to delete.
    NativeStringSlice identifiers;
};

/// One host media-delete response payload.
struct HostMediaDeleteResponse {
    /// The request status code.
    uint32_t status;
    /// The number of deleted assets.
    uint32_t deleted_count;
};

/// Fixed-size host MIDI port descriptor header.
struct AndroidHostMidiPortDescriptorHeader {
    /// Offset of the stable id string inside the shared string buffer.
    uint32_t id_offset;
    /// Length of the stable id string inside the shared string buffer.
    uint32_t id_len;
    /// Offset of the optional group id string inside the shared string buffer.
    uint32_t group_id_offset;
    /// Length of the optional group id string inside the shared string buffer.
    uint32_t group_id_len;
    /// Offset of the optional backend id string inside the shared string buffer.
    uint32_t backend_id_offset;
    /// Length of the optional backend id string inside the shared string buffer.
    uint32_t backend_id_len;
    /// Offset of the endpoint name string inside the shared string buffer.
    uint32_t name_offset;
    /// Length of the endpoint name string inside the shared string buffer.
    uint32_t name_len;
    /// Offset of the optional group name string inside the shared string buffer.
    uint32_t group_name_offset;
    /// Length of the optional group name string inside the shared string buffer.
    uint32_t group_name_len;
    /// Offset of the optional manufacturer string inside the shared string buffer.
    uint32_t manufacturer_offset;
    /// Length of the optional manufacturer string inside the shared string buffer.
    uint32_t manufacturer_len;
    /// Offset of the optional model string inside the shared string buffer.
    uint32_t model_offset;
    /// Length of the optional model string inside the shared string buffer.
    uint32_t model_len;
    /// Offset of the optional version string inside the shared string buffer.
    uint32_t version_offset;
    /// Length of the optional version string inside the shared string buffer.
    uint32_t version_len;
    /// Supported transport formats for this endpoint.
    uint32_t supported_data_formats;
    /// Default transport format code, or zero when unspecified.
    uint32_t default_data_format;
    /// Supported protocol flags for this endpoint.
    uint32_t supported_protocols;
    /// Default protocol code, or zero when unspecified.
    uint32_t default_protocol;
    /// Whether the endpoint is virtual.
    uint32_t is_virtual;
    /// Whether the endpoint is currently connected.
    uint32_t is_connected;
};

/// Fixed-size host MIDI opened-port payload.
struct AndroidHostMidiOpenedPortHeader {
    /// Opaque host session identifier.
    uint64_t session_id;
    /// Descriptor snapshot for the opened endpoint.
    AndroidHostMidiPortDescriptorHeader descriptor;
};

/// Fixed-size host MIDI input record header.
struct AndroidHostMidiInputRecordHeader {
    /// Receive timestamp in runtime monotonic nanoseconds.
    uint64_t received_at_ns;
    /// Offset of the optional source id string inside the shared blob.
    uint32_t source_id_offset;
    /// Length of the optional source id string inside the shared blob.
    uint32_t source_id_len;
    /// Offset of the record payload inside the shared blob.
    uint32_t data_offset;
    /// Length of the record payload inside the shared blob.
    uint32_t data_len;
    /// Transport data-format code.
    uint32_t data_format;
    /// Protocol code, or zero when unspecified.
    uint32_t protocol;
    /// Record framing code.
    uint32_t framing;
};

/// Fixed-size host MIDI output record header.
struct AndroidHostMidiOutputRecordHeader {
    /// Scheduled send timestamp in runtime monotonic nanoseconds.
    uint64_t send_at_ns;
    /// Whether the record carries one scheduled send timestamp.
    uint32_t has_send_at;
    /// Offset of the record payload inside the shared blob.
    uint32_t data_offset;
    /// Length of the record payload inside the shared blob.
    uint32_t data_len;
    /// Transport data-format code.
    uint32_t data_format;
    /// Protocol code, or zero when unspecified.
    uint32_t protocol;
    /// Record framing code.
    uint32_t framing;
};

/// Fixed-size host MIDI topology event header.
struct AndroidHostMidiEventHeader {
    /// Event timestamp in runtime monotonic nanoseconds.
    uint64_t timestamp_ns;
    /// Number of dropped events before this event.
    uint64_t dropped_count;
    /// Event kind code.
    uint32_t kind;
    /// Event direction code, or zero when absent.
    uint32_t direction;
    /// Future backend-disconnection detail flags.
    uint32_t flags;
    /// Offset of the optional removed-port runtime id string.
    uint32_t id_offset;
    /// Length of the optional removed-port runtime id string.
    uint32_t id_len;
    /// Offset of the optional removed-port group id string.
    uint32_t group_id_offset;
    /// Length of the optional removed-port group id string.
    uint32_t group_id_len;
    /// Embedded descriptor payload for added and changed events.
    AndroidHostMidiPortDescriptorHeader descriptor;
};

/// One host notification request payload.
struct HostNotificationRequest {
    /// The stable runtime notification identifier.
    NativeStringRef identifier;
    /// The primary notification title.
    NativeStringRef title;
    /// The primary notification body text.
    NativeStringRef body;
};

/// One host notification event-kind payload.
enum class HostNotificationEventKind : uint32_t {
    /// The notification was delivered.
    Delivered = 1,
    /// The notification was activated by the user.
    Activated = 2,
    /// The notification was dismissed.
    Dismissed = 3,
};

/// One host notification event payload.
struct HostNotificationEvent {
    /// The notification interaction kind.
    HostNotificationEventKind kind;
    /// The event sequence number for this stream.
    uint64_t sequence;
    /// The monotonic event timestamp in nanoseconds.
    uint64_t timestamp_ns;
    /// The simplified request associated with this event.
    HostNotificationRequest request;
    /// Whether the host provided one action identifier.
    bool has_action_identifier;
    /// The action identifier for interactive notifications when available.
    NativeStringRef action_identifier;
};

/// One host permission request payload.
struct HostPermissionRequest {
    /// The stable request identifier for this interactive host flow.
    uint64_t request_id;
    /// The normalized permission name requested by the runtime.
    NativeStringRef permission;
};

/// One host permission event payload.
struct HostPermissionEvent {
    /// The stable request identifier for this interactive host flow.
    uint64_t request_id;
    /// The normalized permission name.
    NativeStringRef permission;
    /// Whether the permission was granted.
    bool is_granted;
};

/// One host text-input type hint payload.
enum class HostTextInputType : int32_t {
    /// Plain text entry.
    Text = 0,
    /// Numeric entry.
    Number = 1,
    /// Email entry.
    Email = 2,
    /// URL entry.
    Url = 3,
    /// Password entry.
    Password = 4,
    /// Phone entry.
    Phone = 5,
    /// Search entry.
    Search = 6,
};

/// One host text range payload.
struct HostTextInputRange {
    /// The inclusive selection start offset.
    uint32_t start_offset;
    /// The exclusive selection end offset.
    uint32_t end_offset;
};

/// One host text rectangle payload.
struct HostTextInputRectangle {
    /// The left edge in local logical units.
    double x;
    /// The top edge in local logical units.
    double y;
    /// The rectangle width in local logical units.
    double width;
    /// The rectangle height in local logical units.
    double height;
};

/// One host text transform payload.
struct HostTextInputTransform2D {
    /// The first-row X coefficient.
    double xx;
    /// The first-row Y coefficient.
    double xy;
    /// The second-row X coefficient.
    double yx;
    /// The second-row Y coefficient.
    double yy;
    /// The translation X component.
    double tx;
    /// The translation Y component.
    double ty;
};

/// One host text geometry payload.
struct HostTextInputGeometry {
    /// The local-to-target transform.
    HostTextInputTransform2D local_to_target_transform;
    /// The full editor rectangle.
    HostTextInputRectangle editor_rectangle;
    /// Whether one caret rectangle is present.
    bool has_caret_rectangle;
    /// The caret rectangle when present.
    HostTextInputRectangle caret_rectangle;
    /// Whether one composing rectangle is present.
    bool has_composing_rectangle;
    /// The composing rectangle when present.
    HostTextInputRectangle composing_rectangle;
};

/// One host text session configuration payload.
struct HostTextInputConfiguration {
    /// The stable runtime text session identifier.
    uint64_t session_id;
    /// The text input type hint.
    HostTextInputType input_type;
    /// Whether the session is multiline.
    bool is_multiline;
    /// Whether the session is secure or password-like.
    bool is_secure;
};

/// One host text state payload.
struct HostTextInputState {
    /// The current text payload.
    NativeStringRef text;
    /// The current selection range.
    HostTextInputRange selection;
    /// Whether one composing range is present.
    bool has_composing;
    /// The composing range when present.
    HostTextInputRange composing;
};

/// One host text open request payload.
struct HostTextInputOpenRequest {
    /// The text session configuration.
    HostTextInputConfiguration configuration;
    /// The initial renderer-owned text state.
    HostTextInputState state;
};

/// One host text close request payload.
struct HostTextInputCloseRequest {
    /// The stable runtime text session identifier.
    uint64_t session_id;
};

/// One host text-geometry update payload.
struct HostTextInputGeometryRequest {
    /// The stable runtime text session identifier.
    uint64_t session_id;
    /// The next renderer-owned geometry payload.
    HostTextInputGeometry geometry;
};

/// One host text-state update payload.
struct HostTextInputStateRequest {
    /// The stable runtime text session identifier.
    uint64_t session_id;
    /// The next renderer-owned text state.
    HostTextInputState state;
};

/// One host text ingress event payload.
struct HostTextInputEvent {
    /// The stable runtime text session identifier.
    uint64_t session_id;
    /// The current text-session state.
    HostTextInputState state;
};

/// Fixed-size host USB device descriptor header.
struct AndroidHostUsbDeviceDescriptorHeader {
    /// Offset of the stable id string.
    uint32_t id_offset;
    /// Length of the stable id string.
    uint32_t id_len;
    /// Offset of the manufacturer string.
    uint32_t manufacturer_offset;
    /// Length of the manufacturer string.
    uint32_t manufacturer_len;
    /// Offset of the product string.
    uint32_t product_offset;
    /// Length of the product string.
    uint32_t product_len;
    /// Offset of the serial string.
    uint32_t serial_number_offset;
    /// Length of the serial string.
    uint32_t serial_number_len;
    /// Offset of the port path bytes.
    uint32_t port_path_offset;
    /// Length of the port path bytes.
    uint32_t port_path_len;
    /// USB specification version in binary coded decimal form.
    uint16_t usb_version_bcd;
    /// Whether the USB version is present.
    uint32_t has_usb_version_bcd;
    /// Device release version in binary coded decimal form.
    uint16_t device_version_bcd;
    /// Whether the device version is present.
    uint32_t has_device_version_bcd;
    /// USB vendor identifier.
    uint16_t vendor_id;
    /// USB product identifier.
    uint16_t product_id;
    /// Device class code.
    uint8_t class_code;
    /// Device subclass code.
    uint8_t subclass_code;
    /// Device protocol code.
    uint8_t protocol_code;
    /// Device speed code.
    uint32_t speed;
    /// Whether the speed is present.
    uint32_t has_speed;
    /// Bus number when available.
    uint8_t bus_number;
    /// Whether the bus number is present.
    uint32_t has_bus_number;
};

/// Fixed-size host USB hotplug event header.
struct AndroidHostUsbHotplugEventHeader {
    /// Event timestamp in monotonic nanoseconds.
    uint64_t timestamp_ns;
    /// Event kind code.
    uint32_t kind;
    /// Embedded device descriptor payload.
    AndroidHostUsbDeviceDescriptorHeader descriptor;
};


/// The background callbacks registered for one runtime session.
struct AndroidHostBackgroundCallbacks {
    /// The status callback.
    uint32_t (*status)(uint64_t session_handle, HostBackgroundStatusResponse *response);
    /// The list callback.
    uint32_t (*list)(uint64_t session_handle, HostBackgroundListResponse *response);
    /// The register-task callback.
    uint32_t (*register_task)(uint64_t session_handle, HostBackgroundTaskOptions request);
    /// The unregister callback.
    uint32_t (*unregister)(uint64_t session_handle, HostBackgroundUnregisterRequest request);
    /// The trigger-test callback.
    uint32_t (*trigger_test)(uint64_t session_handle, HostBackgroundTriggerTestRequest request, HostBackgroundTriggerTestResponse *response);
    /// The complete callback.
    uint32_t (*complete)(uint64_t session_handle, HostBackgroundCompleteRequest request);
};

/// The bluetooth callbacks registered for one runtime session.
struct AndroidHostBluetoothCallbacks {
    /// The adapter-list callback.
    uint32_t (*adapter_list)(uint64_t session_handle, AndroidHostBluetoothAdapterDescriptorHeaderSlice adapters, uint32_t *adapter_count_written, NativeU8Slice string_bytes, uint32_t *string_bytes_written);
    /// The scan-open callback.
    uint32_t (*scan_open)(uint64_t session_handle, NativeStringRef adapter_id, AndroidHostBluetoothScanFilterHeader filter, uint32_t filter_flags, NativeU8Slice filter_bytes, uint64_t *session_id);
    /// The scan-close callback.
    uint32_t (*scan_close)(uint64_t session_handle, uint64_t session_id);
    /// The scan-read callback.
    uint32_t (*scan_read)(uint64_t session_handle, uint64_t session_id, uint64_t timeout_ns, AndroidHostBluetoothDeviceDescriptorHeaderSlice devices, uint32_t *device_count_written, NativeU8Slice string_bytes, uint32_t *string_bytes_written);
    /// The scan-try-read callback.
    uint32_t (*scan_try_read)(uint64_t session_handle, uint64_t session_id, AndroidHostBluetoothDeviceDescriptorHeaderSlice devices, uint32_t *device_count_written, NativeU8Slice string_bytes, uint32_t *string_bytes_written);
    /// The scan-read-event callback.
    uint32_t (*scan_read_event)(uint64_t session_handle, uint64_t session_id, uint64_t timeout_ns, AndroidHostBluetoothScanEventHeader *event, NativeU8Slice string_bytes, uint32_t *string_bytes_written);
    /// The scan-try-read-event callback.
    uint32_t (*scan_try_read_event)(uint64_t session_handle, uint64_t session_id, AndroidHostBluetoothScanEventHeader *event, NativeU8Slice string_bytes, uint32_t *string_bytes_written);
    /// The open callback.
    uint32_t (*open)(uint64_t session_handle, NativeStringRef adapter_id, NativeStringRef device_id, uint64_t *session_id, AndroidHostBluetoothDeviceDescriptorHeader *descriptor, NativeU8Slice string_bytes, uint32_t *string_bytes_written);
    /// The close callback.
    uint32_t (*close)(uint64_t session_handle, uint64_t session_id);
    /// The pair callback.
    uint32_t (*pair)(uint64_t session_handle, uint64_t session_id, uint64_t timeout_ns);
    /// The unpair callback.
    uint32_t (*unpair)(uint64_t session_handle, NativeStringRef adapter_id, NativeStringRef device_id);
    /// The read-rssi callback.
    uint32_t (*read_rssi)(uint64_t session_handle, uint64_t session_id, uint64_t timeout_ns, int16_t *rssi_dbm);
    /// The session-read-event callback.
    uint32_t (*session_read_event)(uint64_t session_handle, uint64_t session_id, uint64_t timeout_ns, AndroidHostBluetoothSessionEventHeader *event);
    /// The session-try-read-event callback.
    uint32_t (*session_try_read_event)(uint64_t session_handle, uint64_t session_id, AndroidHostBluetoothSessionEventHeader *event);
    /// The gatt-service-list callback.
    uint32_t (*gatt_service_list)(uint64_t session_handle, uint64_t session_id, AndroidHostBluetoothGattServiceHeaderSlice services, uint32_t *service_count_written, NativeU8Slice string_bytes, uint32_t *string_bytes_written);
    /// The gatt-characteristic-list callback.
    uint32_t (*gatt_characteristic_list)(uint64_t session_handle, uint64_t session_id, NativeStringRef service_id, AndroidHostBluetoothGattCharacteristicHeaderSlice characteristics, uint32_t *characteristic_count_written, NativeU8Slice string_bytes, uint32_t *string_bytes_written);
    /// The gatt-descriptor-list callback.
    uint32_t (*gatt_descriptor_list)(uint64_t session_handle, uint64_t session_id, NativeStringRef characteristic_id, AndroidHostBluetoothGattDescriptorHeaderSlice descriptors, uint32_t *descriptor_count_written, NativeU8Slice string_bytes, uint32_t *string_bytes_written);
    /// The gatt-mtu callback.
    uint32_t (*gatt_mtu)(uint64_t session_handle, uint64_t session_id, uint16_t *mtu);
    /// The gatt-read callback.
    uint32_t (*gatt_read)(uint64_t session_handle, uint64_t session_id, NativeStringRef characteristic_id, uint64_t timeout_ns, NativeU8Slice bytes, uint32_t *bytes_written);
    /// The gatt-read-descriptor callback.
    uint32_t (*gatt_read_descriptor)(uint64_t session_handle, uint64_t session_id, NativeStringRef descriptor_id, uint64_t timeout_ns, NativeU8Slice bytes, uint32_t *bytes_written);
    /// The gatt-write callback.
    uint32_t (*gatt_write)(uint64_t session_handle, uint64_t session_id, NativeStringRef characteristic_id, uint32_t mode, NativeU8Slice value, uint64_t timeout_ns);
    /// The gatt-write-descriptor callback.
    uint32_t (*gatt_write_descriptor)(uint64_t session_handle, uint64_t session_id, NativeStringRef descriptor_id, NativeU8Slice value, uint64_t timeout_ns);
    /// The gatt-subscribe callback.
    uint32_t (*gatt_subscribe)(uint64_t session_handle, uint64_t session_id, NativeStringRef characteristic_id, uint64_t *subscription_id);
    /// The gatt-unsubscribe callback.
    uint32_t (*gatt_unsubscribe)(uint64_t session_handle, uint64_t subscription_id);
    /// The gatt-read-event callback.
    uint32_t (*gatt_read_event)(uint64_t session_handle, uint64_t subscription_id, uint64_t timeout_ns, NativeU8Slice *event, uint64_t *timestamp_ns);
    /// The gatt-try-read-event callback.
    uint32_t (*gatt_try_read_event)(uint64_t session_handle, uint64_t subscription_id, NativeU8Slice *event, uint64_t *timestamp_ns);
};

/// The calendar callbacks registered for one runtime session.
struct AndroidHostCalendarCallbacks {
    /// The list callback.
    uint32_t (*list)(uint64_t session_handle, HostCalendarListResponse *response);
    /// The event-list callback.
    uint32_t (*event_list)(uint64_t session_handle, HostCalendarEventQuery query, HostCalendarEventListResponse *response);
    /// The event-read callback.
    uint32_t (*event_read)(uint64_t session_handle, NativeStringRef id, HostCalendarEventReadResponse *response);
    /// The event-create callback.
    uint32_t (*event_create)(uint64_t session_handle, HostCalendarEventDraft draft, HostCalendarEventCreateResponse *response);
    /// The event-update callback.
    uint32_t (*event_update)(uint64_t session_handle, NativeStringRef id, HostCalendarEventDraft draft);
    /// The event-delete callback.
    uint32_t (*event_delete)(uint64_t session_handle, NativeStringRef id);
};

/// The camera callbacks registered for one runtime session.
struct AndroidHostCameraCallbacks {
    /// The device-list callback.
    uint32_t (*device_list)(uint64_t session_handle, AndroidHostCameraDeviceDescriptorHeaderSlice devices, uint32_t *device_count_written, NativeU8Slice string_bytes, uint32_t *string_bytes_written);
    /// The device-open callback.
    uint32_t (*device_open)(uint64_t session_handle, NativeStringRef id, uint64_t *session_id);
    /// The device-close callback.
    uint32_t (*device_close)(uint64_t session_handle, uint64_t session_id);
    /// The stream-config-list callback.
    uint32_t (*stream_config_list)(uint64_t session_handle, uint64_t session_id, AndroidHostCameraStreamConfigHeaderSlice configs, uint32_t *config_count_written);
    /// The stream-capability-list callback.
    uint32_t (*stream_capability_list)(uint64_t session_handle, uint64_t session_id, AndroidHostCameraStreamCapabilityHeaderSlice capabilities, uint32_t *capability_count_written);
    /// The stream-open callback.
    uint32_t (*stream_open)(uint64_t session_handle, uint64_t session_id, AndroidHostCameraStreamConfigHeader config, uint64_t *stream_id);
    /// The stream-close callback.
    uint32_t (*stream_close)(uint64_t session_handle, uint64_t stream_id);
    /// The stream-start callback.
    uint32_t (*stream_start)(uint64_t session_handle, uint64_t stream_id);
    /// The stream-stop callback.
    uint32_t (*stream_stop)(uint64_t session_handle, uint64_t stream_id);
    /// The stream-read callback.
    uint32_t (*stream_read)(uint64_t session_handle, uint64_t stream_id, uint64_t timeout_ns, AndroidHostCameraFrameHeader *header, NativeU8Slice bytes, uint32_t *bytes_written);
    /// The stream-try-read callback.
    uint32_t (*stream_try_read)(uint64_t session_handle, uint64_t stream_id, AndroidHostCameraFrameHeader *header, NativeU8Slice bytes, uint32_t *bytes_written);
    /// The stream-take-photo callback.
    uint32_t (*stream_take_photo)(uint64_t session_handle, uint64_t stream_id, uint64_t timeout_ns, AndroidHostCameraFrameHeader *header, NativeU8Slice bytes, uint32_t *bytes_written);
    /// The stream-config callback.
    uint32_t (*stream_config)(uint64_t session_handle, uint64_t stream_id, AndroidHostCameraStreamConfigHeader *config);
    /// The stream-recording-capabilities callback.
    uint32_t (*stream_recording_capabilities)(uint64_t session_handle, uint64_t stream_id, AndroidHostCameraRecordingCapabilitiesHeader *capabilities);
    /// The stream-start-recording callback.
    uint32_t (*stream_start_recording)(uint64_t session_handle, uint64_t stream_id, AndroidHostCameraRecordingOptionsHeader options, NativeStringRef output_path);
    /// The stream-pause-recording callback.
    uint32_t (*stream_pause_recording)(uint64_t session_handle, uint64_t stream_id);
    /// The stream-resume-recording callback.
    uint32_t (*stream_resume_recording)(uint64_t session_handle, uint64_t stream_id);
    /// The stream-stop-recording callback.
    uint32_t (*stream_stop_recording)(uint64_t session_handle, uint64_t stream_id, uint64_t timeout_ns);
    /// The stream-get-u64 callback.
    uint32_t (*stream_get_u64)(uint64_t session_handle, uint64_t stream_id, uint32_t selector, uint64_t *value);
    /// The stream-set-u64 callback.
    uint32_t (*stream_set_u64)(uint64_t session_handle, uint64_t stream_id, uint32_t selector, uint64_t value);
    /// The stream-get-u32 callback.
    uint32_t (*stream_get_u32)(uint64_t session_handle, uint64_t stream_id, uint32_t selector, uint32_t *value);
    /// The stream-set-u32 callback.
    uint32_t (*stream_set_u32)(uint64_t session_handle, uint64_t stream_id, uint32_t selector, uint32_t value);
    /// The stream-get-f64 callback.
    uint32_t (*stream_get_f64)(uint64_t session_handle, uint64_t stream_id, uint32_t selector, double *value);
    /// The stream-set-f64 callback.
    uint32_t (*stream_set_f64)(uint64_t session_handle, uint64_t stream_id, uint32_t selector, double value);
    /// The stream-get-range-f64 callback.
    uint32_t (*stream_get_range_f64)(uint64_t session_handle, uint64_t stream_id, uint32_t selector, double *minimum, double *maximum, double *step);
    /// The stream-get-range-u64 callback.
    uint32_t (*stream_get_range_u64)(uint64_t session_handle, uint64_t stream_id, uint32_t selector, uint64_t *minimum, uint64_t *maximum, uint64_t *step);
    /// The stream-get-range-u32 callback.
    uint32_t (*stream_get_range_u32)(uint64_t session_handle, uint64_t stream_id, uint32_t selector, uint32_t *minimum, uint32_t *maximum, uint32_t *step);
};

/// The contact callbacks registered for one runtime session.
struct AndroidHostContactCallbacks {
    /// The list callback.
    uint32_t (*list)(uint64_t session_handle, HostContactQuery query, HostContactPageResponse *response);
    /// The search callback.
    uint32_t (*search)(uint64_t session_handle, NativeStringRef query_text, HostContactQuery query, HostContactPageResponse *response);
    /// The read callback.
    uint32_t (*read)(uint64_t session_handle, NativeStringRef id, HostContactResponse *response);
    /// The create callback.
    uint32_t (*create)(uint64_t session_handle, HostContactDraft draft, HostContactCreateResponse *response);
    /// The update callback.
    uint32_t (*update)(uint64_t session_handle, NativeStringRef id, HostContactDraft draft);
    /// The delete-contact callback.
    uint32_t (*delete_contact)(uint64_t session_handle, NativeStringRef id);
};

/// The credentials callbacks registered for one runtime session.
struct AndroidHostCredentialsCallbacks {
    /// The read callback.
    uint32_t (*read)(uint64_t session_handle, NativeStringRef service, NativeStringRef account, NativeStringRef access_group, bool require_authentication, NativeU8Slice output, uint32_t *output_written, uint64_t *created_unix_ns, uint64_t *modified_unix_ns);
    /// The write callback.
    uint32_t (*write)(uint64_t session_handle, NativeStringRef service, NativeStringRef account, NativeStringRef access_group, NativeU8Slice payload, uint32_t accessibility, uint32_t authentication_policy, bool replace_existing);
    /// The delete callback.
    uint32_t (*delete_)(uint64_t session_handle, NativeStringRef service, NativeStringRef account, NativeStringRef access_group);
    /// The contains callback.
    uint32_t (*contains)(uint64_t session_handle, NativeStringRef service, NativeStringRef account, NativeStringRef access_group, bool *is_present);
    /// The authenticate callback.
    uint32_t (*authenticate)(uint64_t session_handle, NativeStringRef title, NativeStringRef subtitle, NativeStringRef message, uint32_t requirement, bool *authenticated, uint32_t *mechanism);
};

/// The crypto callbacks registered for one runtime session.
struct AndroidHostCryptoCallbacks {
    /// The supports-hardware-key callback.
    uint32_t (*supports_hardware_key)(uint64_t session_handle, uint32_t store_kind);
    /// The generate-hardware-key-pair callback.
    uint32_t (*generate_hardware_key_pair)(uint64_t session_handle, uint32_t store_kind, uint32_t key_algorithm, uint32_t named_curve, uint32_t modulus_bits, uint32_t public_exponent, NativeStringRef key_label);
    /// The generate-hardware-secret-key callback.
    uint32_t (*generate_hardware_secret_key)(uint64_t session_handle, uint32_t store_kind, uint32_t key_algorithm, uint32_t digest_algorithm, uint32_t key_size_bits, uint32_t key_usage_mask, NativeStringRef key_label);
    /// The export-hardware-public-key callback.
    uint32_t (*export_hardware_public_key)(uint64_t session_handle, uint32_t key_algorithm, NativeStringRef key_label, NativeU8Slice output, uint32_t *output_written);
    /// The sign-hardware-key callback.
    uint32_t (*sign_hardware_key)(uint64_t session_handle, uint32_t key_algorithm, NativeStringRef key_label, uint32_t signature_algorithm, uint32_t digest_algorithm, uint32_t salt_length_bytes, NativeU8Slice payload, NativeU8Slice output_signature, uint32_t *output_written);
    /// The decrypt-hardware-key callback.
    uint32_t (*decrypt_hardware_key)(uint64_t session_handle, uint32_t key_algorithm, NativeStringRef key_label, uint32_t encryption_algorithm, uint32_t digest_algorithm, NativeU8Slice label, NativeU8Slice payload, NativeU8Slice output_plaintext, uint32_t *output_written);
    /// The encrypt-hardware-secret-key callback.
    uint32_t (*encrypt_hardware_secret_key)(uint64_t session_handle, uint32_t key_algorithm, NativeStringRef key_label, uint32_t cipher_algorithm, NativeU8Slice nonce, NativeU8Slice additional_data, uint32_t tag_length_bytes, NativeU8Slice payload, NativeU8Slice output_ciphertext, NativeU8Slice output_tag, uint32_t *output_ciphertext_written, uint32_t *output_tag_written);
    /// The decrypt-hardware-secret-key callback.
    uint32_t (*decrypt_hardware_secret_key)(uint64_t session_handle, uint32_t key_algorithm, NativeStringRef key_label, uint32_t cipher_algorithm, NativeU8Slice nonce, NativeU8Slice additional_data, NativeU8Slice tag, NativeU8Slice payload, NativeU8Slice output_plaintext, uint32_t *output_written);
    /// The compute-hardware-mac callback.
    uint32_t (*compute_hardware_mac)(uint64_t session_handle, uint32_t key_algorithm, NativeStringRef key_label, uint32_t mac_algorithm, uint32_t digest_algorithm, uint32_t tag_length_bytes, NativeU8Slice payload, NativeU8Slice output_tag, uint32_t *output_written);
    /// The derive-hardware-shared-secret callback.
    uint32_t (*derive_hardware_shared_secret)(uint64_t session_handle, uint32_t key_algorithm, NativeStringRef key_label, uint32_t named_curve, NativeU8Slice peer_public_spki, NativeU8Slice output_shared_secret, uint32_t *output_written);
    /// The delete-hardware-key callback.
    uint32_t (*delete_hardware_key)(uint64_t session_handle, uint32_t key_algorithm, NativeStringRef key_label);
    /// The import-certificate callback.
    uint32_t (*import_certificate)(uint64_t session_handle, uint32_t store_kind, NativeU8Slice certificate_der);
    /// The delete-certificate callback.
    uint32_t (*delete_certificate)(uint64_t session_handle, uint32_t store_kind, NativeU8Slice certificate_der);
};

/// The document callbacks registered for one runtime session.
struct AndroidHostDocumentCallbacks {
    /// The pick callback.
    uint32_t (*pick)(uint64_t session_handle, HostDocumentRequest request);
};

/// The intent callbacks registered for one runtime session.
struct AndroidHostIntentCallbacks {
    /// The can-open-url callback.
    uint32_t (*can_open_url)(uint64_t session_handle, NativeStringRef url, bool *is_supported);
    /// The open-url callback.
    uint32_t (*open_url)(uint64_t session_handle, NativeStringRef url);
    /// The open-path callback.
    uint32_t (*open_path)(uint64_t session_handle, NativeStringRef path);
    /// The share-text callback.
    uint32_t (*share_text)(uint64_t session_handle, NativeStringRef text, bool has_mime_type, NativeStringRef mime_type);
    /// The share-paths callback.
    uint32_t (*share_paths)(uint64_t session_handle, NativeStringSlice paths, bool has_mime_type, NativeStringRef mime_type);
};

/// The location callbacks registered for one runtime session.
struct AndroidHostLocationCallbacks {
    /// The services-enabled callback.
    uint32_t (*services_enabled)(uint64_t session_handle, LocationServicesResponse *response);
    /// The last-known callback.
    uint32_t (*last_known)(uint64_t session_handle, LocationLastKnownResponse *response);
    /// The watch-open callback.
    uint32_t (*watch_open)(uint64_t session_handle, NativeStringRef watch_id, LocationWatchOptions options);
    /// The watch-close callback.
    uint32_t (*watch_close)(uint64_t session_handle, NativeStringRef watch_id);
};

/// The media callbacks registered for one runtime session.
struct AndroidHostMediaCallbacks {
    /// The list callback.
    uint32_t (*list)(uint64_t session_handle, HostMediaListRequest request, HostMediaListResponse *response);
    /// The read callback.
    uint32_t (*read)(uint64_t session_handle, NativeStringRef identifier, HostMediaReadResponse *response);
    /// The import-path callback.
    uint32_t (*import_path)(uint64_t session_handle, HostMediaImportPathRequest request, HostMediaImportPathResponse *response);
    /// The delete callback.
    uint32_t (*delete_)(uint64_t session_handle, HostMediaDeleteRequest request, HostMediaDeleteResponse *response);
};

/// The midi callbacks registered for one runtime session.
struct AndroidHostMidiCallbacks {
    /// The describe-backend callback.
    uint32_t (*describe_backend)(uint64_t session_handle, uint64_t *capability_flags, uint32_t *supported_data_formats, uint32_t *supported_protocols);
    /// The input-port-list callback.
    uint32_t (*input_port_list)(uint64_t session_handle, uint32_t flags, AndroidHostMidiPortDescriptorHeaderSlice headers, uint32_t *header_count_written, NativeU8Slice string_bytes, uint32_t *string_bytes_written);
    /// The output-port-list callback.
    uint32_t (*output_port_list)(uint64_t session_handle, uint32_t flags, AndroidHostMidiPortDescriptorHeaderSlice headers, uint32_t *header_count_written, NativeU8Slice string_bytes, uint32_t *string_bytes_written);
    /// The input-port-open callback.
    uint32_t (*input_port_open)(uint64_t session_handle, NativeStringRef id, uint32_t data_format, uint32_t protocol, uint32_t queue_capacity, AndroidHostMidiOpenedPortHeader *opened_port, NativeU8Slice string_bytes, uint32_t *string_bytes_written);
    /// The output-port-open callback.
    uint32_t (*output_port_open)(uint64_t session_handle, NativeStringRef id, uint32_t data_format, uint32_t protocol, AndroidHostMidiOpenedPortHeader *opened_port, NativeU8Slice string_bytes, uint32_t *string_bytes_written);
    /// The input-virtual-create callback.
    uint32_t (*input_virtual_create)(uint64_t session_handle, NativeStringRef name, NativeStringRef manufacturer, NativeStringRef model, NativeStringRef version, uint32_t data_format, uint32_t protocol, uint32_t queue_capacity, AndroidHostMidiOpenedPortHeader *opened_port, NativeU8Slice string_bytes, uint32_t *string_bytes_written);
    /// The output-virtual-create callback.
    uint32_t (*output_virtual_create)(uint64_t session_handle, NativeStringRef name, NativeStringRef manufacturer, NativeStringRef model, NativeStringRef version, uint32_t data_format, uint32_t protocol, AndroidHostMidiOpenedPortHeader *opened_port, NativeU8Slice string_bytes, uint32_t *string_bytes_written);
    /// The input-port-close callback.
    uint32_t (*input_port_close)(uint64_t session_handle, uint64_t session_id);
    /// The output-port-close callback.
    uint32_t (*output_port_close)(uint64_t session_handle, uint64_t session_id);
    /// The input-read callback.
    uint32_t (*input_read)(uint64_t session_handle, uint64_t session_id, uint32_t max_records, uint64_t timeout_ns, AndroidHostMidiInputRecordHeaderSlice headers, uint32_t *record_count_written, NativeU8Slice blob_bytes, uint32_t *blob_bytes_written);
    /// The event-open callback.
    uint32_t (*event_open)(uint64_t session_handle, uint32_t flags, uint32_t direction_mask, uint64_t *session_id);
    /// The event-read callback.
    uint32_t (*event_read)(uint64_t session_handle, uint64_t session_id, uint32_t max_events, uint64_t timeout_ns, AndroidHostMidiEventHeaderSlice headers, uint32_t *event_count_written, NativeU8Slice string_bytes, uint32_t *string_bytes_written);
    /// The event-close callback.
    uint32_t (*event_close)(uint64_t session_handle, uint64_t session_id);
    /// The output-write callback.
    uint32_t (*output_write)(uint64_t session_handle, uint64_t session_id, AndroidHostMidiOutputRecordHeaderSlice headers, uint32_t record_count, NativeU8Slice blob_bytes, uint32_t *records_written);
};

/// The notification callbacks registered for one runtime session.
struct AndroidHostNotificationCallbacks {
    /// The post callback.
    uint32_t (*post)(uint64_t session_handle, HostNotificationRequest request);
    /// The cancel callback.
    uint32_t (*cancel)(uint64_t session_handle, NativeStringRef identifier);
    /// The cancel-all callback.
    uint32_t (*cancel_all)(uint64_t session_handle);
};

/// The permission callbacks registered for one runtime session.
struct AndroidHostPermissionCallbacks {
    /// The request callback.
    uint32_t (*request)(uint64_t session_handle, HostPermissionRequest request);
    /// The open-settings callback.
    uint32_t (*open_settings)(uint64_t session_handle);
};

/// The text callbacks registered for one runtime session.
struct AndroidHostTextCallbacks {
    /// The open callback.
    uint32_t (*open)(uint64_t session_handle, HostTextInputOpenRequest request);
    /// The close callback.
    uint32_t (*close)(uint64_t session_handle, HostTextInputCloseRequest request);
    /// The set-geometry callback.
    uint32_t (*set_geometry)(uint64_t session_handle, HostTextInputGeometryRequest request);
    /// The set-state callback.
    uint32_t (*set_state)(uint64_t session_handle, HostTextInputStateRequest request);
};

/// The usb callbacks registered for one runtime session.
struct AndroidHostUsbCallbacks {
    /// The device-list callback.
    uint32_t (*device_list)(uint64_t session_handle, AndroidHostUsbDeviceDescriptorHeaderSlice devices, uint32_t *device_count_written, NativeU8Slice string_bytes, uint32_t *string_bytes_written, NativeU8Slice port_bytes, uint32_t *port_bytes_written);
    /// The watch-open callback.
    uint32_t (*watch_open)(uint64_t session_handle, uint64_t *watch_id);
    /// The watch-close callback.
    uint32_t (*watch_close)(uint64_t session_handle, uint64_t watch_id);
    /// The watch-read callback.
    uint32_t (*watch_read)(uint64_t session_handle, uint64_t watch_id, uint64_t timeout_ns, AndroidHostUsbHotplugEventHeader *event, NativeU8Slice string_bytes, uint32_t *string_bytes_written, NativeU8Slice port_bytes, uint32_t *port_bytes_written);
    /// The watch-try-read callback.
    uint32_t (*watch_try_read)(uint64_t session_handle, uint64_t watch_id, AndroidHostUsbHotplugEventHeader *event, NativeU8Slice string_bytes, uint32_t *string_bytes_written, NativeU8Slice port_bytes, uint32_t *port_bytes_written);
    /// The open callback.
    uint32_t (*open)(uint64_t session_handle, NativeStringRef id, int32_t *file_descriptor);
};

/// The mobile runtime-bridge callbacks registered for one runtime session.
struct AndroidRuntimeBridgeBindings {
    /// The document callbacks.
    AndroidHostDocumentCallbacks document;
    /// The permission callbacks.
    AndroidHostPermissionCallbacks permission;
    /// The calendar callbacks.
    AndroidHostCalendarCallbacks calendar;
    /// The contact callbacks.
    AndroidHostContactCallbacks contact;
    /// The intent callbacks.
    AndroidHostIntentCallbacks intent;
    /// The location callbacks.
    AndroidHostLocationCallbacks location;
    /// The media callbacks.
    AndroidHostMediaCallbacks media;
    /// The notification callbacks.
    AndroidHostNotificationCallbacks notification;
    /// The text callbacks.
    AndroidHostTextCallbacks text;
    /// The background callbacks.
    AndroidHostBackgroundCallbacks background;
};

/// The runtime function that registers one mobile bridge callback table for one session.
using RegisterRuntimeBridgeBindingsFunction =
    uint32_t (*)(uint64_t session_handle, AndroidRuntimeBridgeBindings callbacks);
/// The runtime function that unregisters one mobile bridge callback table for one session.
using UnregisterRuntimeBridgeBindingsFunction =
    void (*)(uint64_t session_handle);

/// The runtime function that receives one document result.
using NotifyDocumentResultFunction =
    RuntimeStatus (*)(
        uint64_t session_handle,
        uint64_t request_id,
        HostDocumentDescriptorSlice documents
    );

/// The runtime function that receives one notification event.
using NotifyNotificationEventFunction =
    RuntimeStatus (*)(
        uint64_t session_handle,
        HostNotificationEvent event
    );

/// The runtime function that receives one intent open-url event.
using NotifyIntentOpenUrlFunction =
    RuntimeStatus (*)(
        uint64_t session_handle,
        bool has_source,
        NativeStringRef source,
        NativeStringRef url
    );

/// The runtime function that receives one intent open-file event.
using NotifyIntentOpenFileFunction =
    RuntimeStatus (*)(
        uint64_t session_handle,
        bool has_source,
        NativeStringRef source,
        NativeStringRef path,
        bool has_mime_type,
        NativeStringRef mime_type
    );

/// The runtime function that receives one intent share-text event.
using NotifyIntentShareTextFunction =
    RuntimeStatus (*)(
        uint64_t session_handle,
        bool has_source,
        NativeStringRef source,
        NativeStringRef text,
        bool has_mime_type,
        NativeStringRef mime_type
    );

/// The runtime function that receives one intent share-files event.
using NotifyIntentShareFilesFunction =
    RuntimeStatus (*)(
        uint64_t session_handle,
        bool has_source,
        NativeStringRef source,
        NativeStringSlice paths,
        bool has_mime_type,
        NativeStringRef mime_type
    );

/// The runtime function that receives one intent custom-action event.
using NotifyIntentCustomActionFunction =
    RuntimeStatus (*)(
        uint64_t session_handle,
        bool has_source,
        NativeStringRef source,
        NativeStringRef action,
        bool has_url,
        NativeStringRef url,
        NativeStringSlice paths,
        bool has_text,
        NativeStringRef text,
        bool has_mime_type,
        NativeStringRef mime_type
    );

/// The runtime function that receives one permission result.
using NotifyPermissionResultFunction =
    RuntimeStatus (*)(
        uint64_t session_handle,
        HostPermissionEvent event
    );

/// The runtime function that receives one location sample.
using NotifyLocationSampleFunction =
    RuntimeStatus (*)(
        uint64_t session_handle,
        NativeStringRef watch_id,
        LocationSample sample
    );

/// The runtime function that receives one text-input state event.
using NotifyTextInputStateFunction =
    RuntimeStatus (*)(
        uint64_t session_handle,
        HostTextInputEvent event
    );

/// The runtime function that receives one background event.
using NotifyBackgroundEventFunction =
    RuntimeStatus (*)(
        uint64_t session_handle,
        HostBackgroundEvent event
    );

#endif
