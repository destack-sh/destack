// generated by generate-bindings: do not edit

#include "../types.h"
#include "../jni.h"
#include "../loader.h"
#include "callbacks.generated.h"
#include "runtime.generated.h"

#include <deque>
#include <string>
#include <vector>

namespace {

/// Convert one host status code into one runtime status.
RuntimeStatus runtime_status_from_code(uint32_t code) {
    RuntimeStatus status = {
        .code = code,
        .error_id = 0,
    };

    return status;
}

}

/// Forward the `list` media request through one attached bridge session.
uint32_t call_media_list_for_session(
    uint64_t session_handle,
    HostMediaListRequest request,
    HostMediaListResponse *response
) {
    JNIEnv *env = nullptr;
    bool did_attach_thread = false;

    if (!resolve_jni_env(&env, &did_attach_thread)) {
        return HOST_STATUS_FAILED;
    }

    uint32_t status = call_media_list(env, session_handle, request, response);

    detach_jni_thread(did_attach_thread);

    return status;
}

/// Forward the `read` media request through one attached bridge session.
uint32_t call_media_read_for_session(
    uint64_t session_handle,
    NativeStringRef identifier,
    HostMediaReadResponse *response
) {
    JNIEnv *env = nullptr;
    bool did_attach_thread = false;

    if (!resolve_jni_env(&env, &did_attach_thread)) {
        return HOST_STATUS_FAILED;
    }

    uint32_t status = call_media_read(env, session_handle, identifier, response);

    detach_jni_thread(did_attach_thread);

    return status;
}

/// Forward the `import_path` media request through one attached bridge session.
uint32_t call_media_import_path_for_session(
    uint64_t session_handle,
    HostMediaImportPathRequest request,
    HostMediaImportPathResponse *response
) {
    JNIEnv *env = nullptr;
    bool did_attach_thread = false;

    if (!resolve_jni_env(&env, &did_attach_thread)) {
        return HOST_STATUS_FAILED;
    }

    uint32_t status = call_media_import_path(env, session_handle, request, response);

    detach_jni_thread(did_attach_thread);

    return status;
}

/// Forward the `delete` media request through one attached bridge session.
uint32_t call_media_delete_for_session(
    uint64_t session_handle,
    HostMediaDeleteRequest request,
    HostMediaDeleteResponse *response
) {
    JNIEnv *env = nullptr;
    bool did_attach_thread = false;

    if (!resolve_jni_env(&env, &did_attach_thread)) {
        return HOST_STATUS_FAILED;
    }

    uint32_t status = call_media_delete(env, session_handle, request, response);

    detach_jni_thread(did_attach_thread);

    return status;
}

/// Build one Android host media callback bundle.
AndroidHostMediaCallbacks make_media_callbacks() {
    return AndroidHostMediaCallbacks {
        .list = call_media_list_for_session,
        .read = call_media_read_for_session,
        .import_path = call_media_import_path_for_session,
        .delete_ = call_media_delete_for_session,
    };
}
