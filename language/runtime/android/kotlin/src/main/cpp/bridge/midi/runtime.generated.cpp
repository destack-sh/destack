// generated by generate-bindings: do not edit

#include "../types.h"
#include "../jni.h"
#include "../loader.h"
#include "callbacks.generated.h"
#include "runtime.generated.h"

#include <deque>
#include <string>
#include <vector>

namespace {

/// Convert one host status code into one runtime status.
RuntimeStatus runtime_status_from_code(uint32_t code) {
    RuntimeStatus status = {
        .code = code,
        .error_id = 0,
    };

    return status;
}

}

/// Forward the `describe_backend` midi request through one attached bridge session.
uint32_t call_midi_describe_backend_for_session(
    uint64_t session_handle,
    uint64_t *capability_flags,
    uint32_t *supported_data_formats,
    uint32_t *supported_protocols
) {
    JNIEnv *env = nullptr;
    bool did_attach_thread = false;

    if (!resolve_jni_env(&env, &did_attach_thread)) {
        return HOST_STATUS_FAILED;
    }

    uint32_t status = call_midi_describe_backend(env, session_handle, capability_flags, supported_data_formats, supported_protocols);

    detach_jni_thread(did_attach_thread);

    return status;
}

/// Forward the `input_port_list` midi request through one attached bridge session.
uint32_t call_midi_input_port_list_for_session(
    uint64_t session_handle,
    uint32_t flags,
    AndroidHostMidiPortDescriptorHeaderSlice headers,
    uint32_t *header_count_written,
    NativeU8Slice string_bytes,
    uint32_t *string_bytes_written
) {
    JNIEnv *env = nullptr;
    bool did_attach_thread = false;

    if (!resolve_jni_env(&env, &did_attach_thread)) {
        return HOST_STATUS_FAILED;
    }

    uint32_t status = call_midi_input_port_list(env, session_handle, flags, headers, header_count_written, string_bytes, string_bytes_written);

    detach_jni_thread(did_attach_thread);

    return status;
}

/// Forward the `output_port_list` midi request through one attached bridge session.
uint32_t call_midi_output_port_list_for_session(
    uint64_t session_handle,
    uint32_t flags,
    AndroidHostMidiPortDescriptorHeaderSlice headers,
    uint32_t *header_count_written,
    NativeU8Slice string_bytes,
    uint32_t *string_bytes_written
) {
    JNIEnv *env = nullptr;
    bool did_attach_thread = false;

    if (!resolve_jni_env(&env, &did_attach_thread)) {
        return HOST_STATUS_FAILED;
    }

    uint32_t status = call_midi_output_port_list(env, session_handle, flags, headers, header_count_written, string_bytes, string_bytes_written);

    detach_jni_thread(did_attach_thread);

    return status;
}

/// Forward the `input_port_open` midi request through one attached bridge session.
uint32_t call_midi_input_port_open_for_session(
    uint64_t session_handle,
    NativeStringRef id,
    uint32_t data_format,
    uint32_t protocol,
    uint32_t queue_capacity,
    AndroidHostMidiOpenedPortHeader *opened_port,
    NativeU8Slice string_bytes,
    uint32_t *string_bytes_written
) {
    JNIEnv *env = nullptr;
    bool did_attach_thread = false;

    if (!resolve_jni_env(&env, &did_attach_thread)) {
        return HOST_STATUS_FAILED;
    }

    uint32_t status = call_midi_input_port_open(env, session_handle, id, data_format, protocol, queue_capacity, opened_port, string_bytes, string_bytes_written);

    detach_jni_thread(did_attach_thread);

    return status;
}

/// Forward the `output_port_open` midi request through one attached bridge session.
uint32_t call_midi_output_port_open_for_session(
    uint64_t session_handle,
    NativeStringRef id,
    uint32_t data_format,
    uint32_t protocol,
    AndroidHostMidiOpenedPortHeader *opened_port,
    NativeU8Slice string_bytes,
    uint32_t *string_bytes_written
) {
    JNIEnv *env = nullptr;
    bool did_attach_thread = false;

    if (!resolve_jni_env(&env, &did_attach_thread)) {
        return HOST_STATUS_FAILED;
    }

    uint32_t status = call_midi_output_port_open(env, session_handle, id, data_format, protocol, opened_port, string_bytes, string_bytes_written);

    detach_jni_thread(did_attach_thread);

    return status;
}

/// Forward the `input_virtual_create` midi request through one attached bridge session.
uint32_t call_midi_input_virtual_create_for_session(
    uint64_t session_handle,
    NativeStringRef name,
    NativeStringRef manufacturer,
    NativeStringRef model,
    NativeStringRef version,
    uint32_t data_format,
    uint32_t protocol,
    uint32_t queue_capacity,
    AndroidHostMidiOpenedPortHeader *opened_port,
    NativeU8Slice string_bytes,
    uint32_t *string_bytes_written
) {
    JNIEnv *env = nullptr;
    bool did_attach_thread = false;

    if (!resolve_jni_env(&env, &did_attach_thread)) {
        return HOST_STATUS_FAILED;
    }

    uint32_t status = call_midi_input_virtual_create(env, session_handle, name, manufacturer, model, version, data_format, protocol, queue_capacity, opened_port, string_bytes, string_bytes_written);

    detach_jni_thread(did_attach_thread);

    return status;
}

/// Forward the `output_virtual_create` midi request through one attached bridge session.
uint32_t call_midi_output_virtual_create_for_session(
    uint64_t session_handle,
    NativeStringRef name,
    NativeStringRef manufacturer,
    NativeStringRef model,
    NativeStringRef version,
    uint32_t data_format,
    uint32_t protocol,
    AndroidHostMidiOpenedPortHeader *opened_port,
    NativeU8Slice string_bytes,
    uint32_t *string_bytes_written
) {
    JNIEnv *env = nullptr;
    bool did_attach_thread = false;

    if (!resolve_jni_env(&env, &did_attach_thread)) {
        return HOST_STATUS_FAILED;
    }

    uint32_t status = call_midi_output_virtual_create(env, session_handle, name, manufacturer, model, version, data_format, protocol, opened_port, string_bytes, string_bytes_written);

    detach_jni_thread(did_attach_thread);

    return status;
}

/// Forward the `input_port_close` midi request through one attached bridge session.
uint32_t call_midi_input_port_close_for_session(
    uint64_t session_handle,
    uint64_t session_id
) {
    JNIEnv *env = nullptr;
    bool did_attach_thread = false;

    if (!resolve_jni_env(&env, &did_attach_thread)) {
        return HOST_STATUS_FAILED;
    }

    uint32_t status = call_midi_input_port_close(env, session_handle, session_id);

    detach_jni_thread(did_attach_thread);

    return status;
}

/// Forward the `output_port_close` midi request through one attached bridge session.
uint32_t call_midi_output_port_close_for_session(
    uint64_t session_handle,
    uint64_t session_id
) {
    JNIEnv *env = nullptr;
    bool did_attach_thread = false;

    if (!resolve_jni_env(&env, &did_attach_thread)) {
        return HOST_STATUS_FAILED;
    }

    uint32_t status = call_midi_output_port_close(env, session_handle, session_id);

    detach_jni_thread(did_attach_thread);

    return status;
}

/// Forward the `input_read` midi request through one attached bridge session.
uint32_t call_midi_input_read_for_session(
    uint64_t session_handle,
    uint64_t session_id,
    uint32_t max_records,
    uint64_t timeout_ns,
    AndroidHostMidiInputRecordHeaderSlice headers,
    uint32_t *record_count_written,
    NativeU8Slice blob_bytes,
    uint32_t *blob_bytes_written
) {
    JNIEnv *env = nullptr;
    bool did_attach_thread = false;

    if (!resolve_jni_env(&env, &did_attach_thread)) {
        return HOST_STATUS_FAILED;
    }

    uint32_t status = call_midi_input_read(env, session_handle, session_id, max_records, timeout_ns, headers, record_count_written, blob_bytes, blob_bytes_written);

    detach_jni_thread(did_attach_thread);

    return status;
}

/// Forward the `event_open` midi request through one attached bridge session.
uint32_t call_midi_event_open_for_session(
    uint64_t session_handle,
    uint32_t flags,
    uint32_t direction_mask,
    uint64_t *session_id
) {
    JNIEnv *env = nullptr;
    bool did_attach_thread = false;

    if (!resolve_jni_env(&env, &did_attach_thread)) {
        return HOST_STATUS_FAILED;
    }

    uint32_t status = call_midi_event_open(env, session_handle, flags, direction_mask, session_id);

    detach_jni_thread(did_attach_thread);

    return status;
}

/// Forward the `event_read` midi request through one attached bridge session.
uint32_t call_midi_event_read_for_session(
    uint64_t session_handle,
    uint64_t session_id,
    uint32_t max_events,
    uint64_t timeout_ns,
    AndroidHostMidiEventHeaderSlice headers,
    uint32_t *event_count_written,
    NativeU8Slice string_bytes,
    uint32_t *string_bytes_written
) {
    JNIEnv *env = nullptr;
    bool did_attach_thread = false;

    if (!resolve_jni_env(&env, &did_attach_thread)) {
        return HOST_STATUS_FAILED;
    }

    uint32_t status = call_midi_event_read(env, session_handle, session_id, max_events, timeout_ns, headers, event_count_written, string_bytes, string_bytes_written);

    detach_jni_thread(did_attach_thread);

    return status;
}

/// Forward the `event_close` midi request through one attached bridge session.
uint32_t call_midi_event_close_for_session(
    uint64_t session_handle,
    uint64_t session_id
) {
    JNIEnv *env = nullptr;
    bool did_attach_thread = false;

    if (!resolve_jni_env(&env, &did_attach_thread)) {
        return HOST_STATUS_FAILED;
    }

    uint32_t status = call_midi_event_close(env, session_handle, session_id);

    detach_jni_thread(did_attach_thread);

    return status;
}

/// Forward the `output_write` midi request through one attached bridge session.
uint32_t call_midi_output_write_for_session(
    uint64_t session_handle,
    uint64_t session_id,
    AndroidHostMidiOutputRecordHeaderSlice headers,
    uint32_t record_count,
    NativeU8Slice blob_bytes,
    uint32_t *records_written
) {
    JNIEnv *env = nullptr;
    bool did_attach_thread = false;

    if (!resolve_jni_env(&env, &did_attach_thread)) {
        return HOST_STATUS_FAILED;
    }

    uint32_t status = call_midi_output_write(env, session_handle, session_id, headers, record_count, blob_bytes, records_written);

    detach_jni_thread(did_attach_thread);

    return status;
}

/// Build one Android host midi callback bundle.
AndroidHostMidiCallbacks make_midi_callbacks() {
    return AndroidHostMidiCallbacks {
        .describe_backend = call_midi_describe_backend_for_session,
        .input_port_list = call_midi_input_port_list_for_session,
        .output_port_list = call_midi_output_port_list_for_session,
        .input_port_open = call_midi_input_port_open_for_session,
        .output_port_open = call_midi_output_port_open_for_session,
        .input_virtual_create = call_midi_input_virtual_create_for_session,
        .output_virtual_create = call_midi_output_virtual_create_for_session,
        .input_port_close = call_midi_input_port_close_for_session,
        .output_port_close = call_midi_output_port_close_for_session,
        .input_read = call_midi_input_read_for_session,
        .event_open = call_midi_event_open_for_session,
        .event_read = call_midi_event_read_for_session,
        .event_close = call_midi_event_close_for_session,
        .output_write = call_midi_output_write_for_session,
    };
}
