// generated by generate-bindings: do not edit

#include "../types.h"
#include "../jni.h"
#include "../registry.h"
#include "callbacks.generated.h"

#include <deque>
#include <string>
#include <vector>

namespace {

jclass bridge_class = nullptr;
jmethodID open_method = nullptr;
jmethodID close_method = nullptr;
jmethodID setGeometry_method = nullptr;
jmethodID setState_method = nullptr;

/// Build one JNI string borrowed from one native string reference.
jstring java_string_or_null(JNIEnv *env, NativeStringRef value) {
    if (value.data == nullptr) {
        return nullptr;
    }

    return new_java_string(env, value);
}

/// Resolve the text bridge methods from one runtime bridge instance.
bool resolve_bridge_methods(JNIEnv *env, jobject bridge) {
    if (bridge_class == nullptr) {
        jclass local_class = env->GetObjectClass(bridge);
        if (local_class == nullptr) {
            return false;
        }

        bridge_class = reinterpret_cast<jclass>(env->NewGlobalRef(local_class));
        env->DeleteLocalRef(local_class);
        if (bridge_class == nullptr) {
            return false;
        }
    }

    if (open_method == nullptr) {
        open_method = env->GetMethodID(
            bridge_class,
            "textOpen",
            "(JIZZLjava/lang/String;IIZII)I"
        );
    }

    if (close_method == nullptr) {
        close_method = env->GetMethodID(
            bridge_class,
            "textClose",
            "(J)I"
        );
    }

    if (setGeometry_method == nullptr) {
        setGeometry_method = env->GetMethodID(
            bridge_class,
            "textSetGeometry",
            "(JDDDDDDDDDDZDDDDZDDDD)I"
        );
    }

    if (setState_method == nullptr) {
        setState_method = env->GetMethodID(
            bridge_class,
            "textSetState",
            "(JLjava/lang/String;IIZII)I"
        );
    }

    return
        open_method != nullptr &&
        close_method != nullptr &&
        setGeometry_method != nullptr &&
        setState_method != nullptr;
}

}

bool resolve_text_methods(JNIEnv *env, jobject bridge) {
    return resolve_bridge_methods(env, bridge);
}

/// Call the text-open entrypoint on one registered bridge.
uint32_t call_text_open(
    JNIEnv *env,
    uint64_t session_handle,
    HostTextInputOpenRequest request
) {
    jobject bridge = resolve_bridge(env, session_handle);
    if (bridge == nullptr) {
        return HOST_STATUS_NOT_FOUND;
    }

    jstring text_value = java_string_or_null(env, request.state.text);

    jint status = env->CallIntMethod(
        bridge,
        open_method,
        static_cast<jlong>(request.configuration.session_id),
        static_cast<jint>(request.configuration.input_type),
        request.configuration.is_multiline ? JNI_TRUE : JNI_FALSE,
        request.configuration.is_secure ? JNI_TRUE : JNI_FALSE,
        text_value,
        static_cast<jint>(request.state.selection.start_offset),
        static_cast<jint>(request.state.selection.end_offset),
        request.state.has_composing ? JNI_TRUE : JNI_FALSE,
        static_cast<jint>(request.state.composing.start_offset),
        static_cast<jint>(request.state.composing.end_offset)
    );
    if (text_value != nullptr) { env->DeleteLocalRef(text_value); }
    env->DeleteLocalRef(bridge);

    if (env->ExceptionCheck()) {
        env->ExceptionClear();
        return HOST_STATUS_FAILED;
    }

    return static_cast<uint32_t>(status);
}

/// Call the text-close entrypoint on one registered bridge.
uint32_t call_text_close(
    JNIEnv *env,
    uint64_t session_handle,
    HostTextInputCloseRequest request
) {
    jobject bridge = resolve_bridge(env, session_handle);
    if (bridge == nullptr) {
        return HOST_STATUS_NOT_FOUND;
    }


    jint status = env->CallIntMethod(
        bridge,
        close_method,
        static_cast<jlong>(request.session_id)
    );
    env->DeleteLocalRef(bridge);

    if (env->ExceptionCheck()) {
        env->ExceptionClear();
        return HOST_STATUS_FAILED;
    }

    return static_cast<uint32_t>(status);
}

/// Call the text-set-geometry entrypoint on one registered bridge.
uint32_t call_text_set_geometry(
    JNIEnv *env,
    uint64_t session_handle,
    HostTextInputGeometryRequest request
) {
    jobject bridge = resolve_bridge(env, session_handle);
    if (bridge == nullptr) {
        return HOST_STATUS_NOT_FOUND;
    }


    jint status = env->CallIntMethod(
        bridge,
        setGeometry_method,
        static_cast<jlong>(request.session_id),
        static_cast<jdouble>(request.geometry.local_to_target_transform.xx),
        static_cast<jdouble>(request.geometry.local_to_target_transform.xy),
        static_cast<jdouble>(request.geometry.local_to_target_transform.yx),
        static_cast<jdouble>(request.geometry.local_to_target_transform.yy),
        static_cast<jdouble>(request.geometry.local_to_target_transform.tx),
        static_cast<jdouble>(request.geometry.local_to_target_transform.ty),
        static_cast<jdouble>(request.geometry.editor_rectangle.x),
        static_cast<jdouble>(request.geometry.editor_rectangle.y),
        static_cast<jdouble>(request.geometry.editor_rectangle.width),
        static_cast<jdouble>(request.geometry.editor_rectangle.height),
        request.geometry.has_caret_rectangle ? JNI_TRUE : JNI_FALSE,
        static_cast<jdouble>(request.geometry.caret_rectangle.x),
        static_cast<jdouble>(request.geometry.caret_rectangle.y),
        static_cast<jdouble>(request.geometry.caret_rectangle.width),
        static_cast<jdouble>(request.geometry.caret_rectangle.height),
        request.geometry.has_composing_rectangle ? JNI_TRUE : JNI_FALSE,
        static_cast<jdouble>(request.geometry.composing_rectangle.x),
        static_cast<jdouble>(request.geometry.composing_rectangle.y),
        static_cast<jdouble>(request.geometry.composing_rectangle.width),
        static_cast<jdouble>(request.geometry.composing_rectangle.height)
    );
    env->DeleteLocalRef(bridge);

    if (env->ExceptionCheck()) {
        env->ExceptionClear();
        return HOST_STATUS_FAILED;
    }

    return static_cast<uint32_t>(status);
}

/// Call the text-set-state entrypoint on one registered bridge.
uint32_t call_text_set_state(
    JNIEnv *env,
    uint64_t session_handle,
    HostTextInputStateRequest request
) {
    jobject bridge = resolve_bridge(env, session_handle);
    if (bridge == nullptr) {
        return HOST_STATUS_NOT_FOUND;
    }

    jstring text_value = java_string_or_null(env, request.state.text);

    jint status = env->CallIntMethod(
        bridge,
        setState_method,
        static_cast<jlong>(request.session_id),
        text_value,
        static_cast<jint>(request.state.selection.start_offset),
        static_cast<jint>(request.state.selection.end_offset),
        request.state.has_composing ? JNI_TRUE : JNI_FALSE,
        static_cast<jint>(request.state.composing.start_offset),
        static_cast<jint>(request.state.composing.end_offset)
    );
    if (text_value != nullptr) { env->DeleteLocalRef(text_value); }
    env->DeleteLocalRef(bridge);

    if (env->ExceptionCheck()) {
        env->ExceptionClear();
        return HOST_STATUS_FAILED;
    }

    return static_cast<uint32_t>(status);
}
