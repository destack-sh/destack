// generated by generate-bindings: do not edit

#ifndef DESTACK_RUNTIME_ANDROID_BRIDGE_CREDENTIALS_RUNTIME_H
#define DESTACK_RUNTIME_ANDROID_BRIDGE_CREDENTIALS_RUNTIME_H

#include "../types.h"

/// Forward the `read` credentials request through one attached bridge session.
uint32_t call_credentials_read_for_session(
    uint64_t session_handle,
    NativeStringRef service,
    NativeStringRef account,
    NativeStringRef access_group,
    bool require_authentication,
    NativeU8Slice output,
    uint32_t *output_written,
    uint64_t *created_unix_ns,
    uint64_t *modified_unix_ns
);

/// Forward the `write` credentials request through one attached bridge session.
uint32_t call_credentials_write_for_session(
    uint64_t session_handle,
    NativeStringRef service,
    NativeStringRef account,
    NativeStringRef access_group,
    NativeU8Slice payload,
    uint32_t accessibility,
    uint32_t authentication_policy,
    bool replace_existing
);

/// Forward the `delete` credentials request through one attached bridge session.
uint32_t call_credentials_delete_for_session(
    uint64_t session_handle,
    NativeStringRef service,
    NativeStringRef account,
    NativeStringRef access_group
);

/// Forward the `contains` credentials request through one attached bridge session.
uint32_t call_credentials_contains_for_session(
    uint64_t session_handle,
    NativeStringRef service,
    NativeStringRef account,
    NativeStringRef access_group,
    bool *is_present
);

/// Forward the `authenticate` credentials request through one attached bridge session.
uint32_t call_credentials_authenticate_for_session(
    uint64_t session_handle,
    NativeStringRef title,
    NativeStringRef subtitle,
    NativeStringRef message,
    uint32_t requirement,
    bool *authenticated,
    uint32_t *mechanism
);

/// Build one Android host credentials callback bundle.
AndroidHostCredentialsCallbacks make_credentials_callbacks();

#endif
