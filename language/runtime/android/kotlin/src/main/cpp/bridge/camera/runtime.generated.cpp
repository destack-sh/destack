// generated by generate-bindings: do not edit

#include "../types.h"
#include "../jni.h"
#include "../loader.h"
#include "callbacks.generated.h"
#include "runtime.generated.h"

#include <deque>
#include <string>
#include <vector>

namespace {

/// Convert one host status code into one runtime status.
RuntimeStatus runtime_status_from_code(uint32_t code) {
    RuntimeStatus status = {
        .code = code,
        .error_id = 0,
    };

    return status;
}

}

/// Forward the `device_list` camera request through one attached bridge session.
uint32_t call_camera_device_list_for_session(
    uint64_t session_handle,
    AndroidHostCameraDeviceDescriptorHeaderSlice devices,
    uint32_t *device_count_written,
    NativeU8Slice string_bytes,
    uint32_t *string_bytes_written
) {
    JNIEnv *env = nullptr;
    bool did_attach_thread = false;

    if (!resolve_jni_env(&env, &did_attach_thread)) {
        return HOST_STATUS_FAILED;
    }

    uint32_t status = call_camera_device_list(env, session_handle, devices, device_count_written, string_bytes, string_bytes_written);

    detach_jni_thread(did_attach_thread);

    return status;
}

/// Forward the `device_open` camera request through one attached bridge session.
uint32_t call_camera_device_open_for_session(
    uint64_t session_handle,
    NativeStringRef id,
    uint64_t *session_id
) {
    JNIEnv *env = nullptr;
    bool did_attach_thread = false;

    if (!resolve_jni_env(&env, &did_attach_thread)) {
        return HOST_STATUS_FAILED;
    }

    uint32_t status = call_camera_device_open(env, session_handle, id, session_id);

    detach_jni_thread(did_attach_thread);

    return status;
}

/// Forward the `device_close` camera request through one attached bridge session.
uint32_t call_camera_device_close_for_session(
    uint64_t session_handle,
    uint64_t session_id
) {
    JNIEnv *env = nullptr;
    bool did_attach_thread = false;

    if (!resolve_jni_env(&env, &did_attach_thread)) {
        return HOST_STATUS_FAILED;
    }

    uint32_t status = call_camera_device_close(env, session_handle, session_id);

    detach_jni_thread(did_attach_thread);

    return status;
}

/// Forward the `stream_config_list` camera request through one attached bridge session.
uint32_t call_camera_stream_config_list_for_session(
    uint64_t session_handle,
    uint64_t session_id,
    AndroidHostCameraStreamConfigHeaderSlice configs,
    uint32_t *config_count_written
) {
    JNIEnv *env = nullptr;
    bool did_attach_thread = false;

    if (!resolve_jni_env(&env, &did_attach_thread)) {
        return HOST_STATUS_FAILED;
    }

    uint32_t status = call_camera_stream_config_list(env, session_handle, session_id, configs, config_count_written);

    detach_jni_thread(did_attach_thread);

    return status;
}

/// Forward the `stream_capability_list` camera request through one attached bridge session.
uint32_t call_camera_stream_capability_list_for_session(
    uint64_t session_handle,
    uint64_t session_id,
    AndroidHostCameraStreamCapabilityHeaderSlice capabilities,
    uint32_t *capability_count_written
) {
    JNIEnv *env = nullptr;
    bool did_attach_thread = false;

    if (!resolve_jni_env(&env, &did_attach_thread)) {
        return HOST_STATUS_FAILED;
    }

    uint32_t status = call_camera_stream_capability_list(env, session_handle, session_id, capabilities, capability_count_written);

    detach_jni_thread(did_attach_thread);

    return status;
}

/// Forward the `stream_open` camera request through one attached bridge session.
uint32_t call_camera_stream_open_for_session(
    uint64_t session_handle,
    uint64_t session_id,
    AndroidHostCameraStreamConfigHeader config,
    uint64_t *stream_id
) {
    JNIEnv *env = nullptr;
    bool did_attach_thread = false;

    if (!resolve_jni_env(&env, &did_attach_thread)) {
        return HOST_STATUS_FAILED;
    }

    uint32_t status = call_camera_stream_open(env, session_handle, session_id, config, stream_id);

    detach_jni_thread(did_attach_thread);

    return status;
}

/// Forward the `stream_close` camera request through one attached bridge session.
uint32_t call_camera_stream_close_for_session(
    uint64_t session_handle,
    uint64_t stream_id
) {
    JNIEnv *env = nullptr;
    bool did_attach_thread = false;

    if (!resolve_jni_env(&env, &did_attach_thread)) {
        return HOST_STATUS_FAILED;
    }

    uint32_t status = call_camera_stream_close(env, session_handle, stream_id);

    detach_jni_thread(did_attach_thread);

    return status;
}

/// Forward the `stream_start` camera request through one attached bridge session.
uint32_t call_camera_stream_start_for_session(
    uint64_t session_handle,
    uint64_t stream_id
) {
    JNIEnv *env = nullptr;
    bool did_attach_thread = false;

    if (!resolve_jni_env(&env, &did_attach_thread)) {
        return HOST_STATUS_FAILED;
    }

    uint32_t status = call_camera_stream_start(env, session_handle, stream_id);

    detach_jni_thread(did_attach_thread);

    return status;
}

/// Forward the `stream_stop` camera request through one attached bridge session.
uint32_t call_camera_stream_stop_for_session(
    uint64_t session_handle,
    uint64_t stream_id
) {
    JNIEnv *env = nullptr;
    bool did_attach_thread = false;

    if (!resolve_jni_env(&env, &did_attach_thread)) {
        return HOST_STATUS_FAILED;
    }

    uint32_t status = call_camera_stream_stop(env, session_handle, stream_id);

    detach_jni_thread(did_attach_thread);

    return status;
}

/// Forward the `stream_read` camera request through one attached bridge session.
uint32_t call_camera_stream_read_for_session(
    uint64_t session_handle,
    uint64_t stream_id,
    uint64_t timeout_ns,
    AndroidHostCameraFrameHeader *header,
    NativeU8Slice bytes,
    uint32_t *bytes_written
) {
    JNIEnv *env = nullptr;
    bool did_attach_thread = false;

    if (!resolve_jni_env(&env, &did_attach_thread)) {
        return HOST_STATUS_FAILED;
    }

    uint32_t status = call_camera_stream_read(env, session_handle, stream_id, timeout_ns, header, bytes, bytes_written);

    detach_jni_thread(did_attach_thread);

    return status;
}

/// Forward the `stream_try_read` camera request through one attached bridge session.
uint32_t call_camera_stream_try_read_for_session(
    uint64_t session_handle,
    uint64_t stream_id,
    AndroidHostCameraFrameHeader *header,
    NativeU8Slice bytes,
    uint32_t *bytes_written
) {
    JNIEnv *env = nullptr;
    bool did_attach_thread = false;

    if (!resolve_jni_env(&env, &did_attach_thread)) {
        return HOST_STATUS_FAILED;
    }

    uint32_t status = call_camera_stream_try_read(env, session_handle, stream_id, header, bytes, bytes_written);

    detach_jni_thread(did_attach_thread);

    return status;
}

/// Forward the `stream_take_photo` camera request through one attached bridge session.
uint32_t call_camera_stream_take_photo_for_session(
    uint64_t session_handle,
    uint64_t stream_id,
    uint64_t timeout_ns,
    AndroidHostCameraFrameHeader *header,
    NativeU8Slice bytes,
    uint32_t *bytes_written
) {
    JNIEnv *env = nullptr;
    bool did_attach_thread = false;

    if (!resolve_jni_env(&env, &did_attach_thread)) {
        return HOST_STATUS_FAILED;
    }

    uint32_t status = call_camera_stream_take_photo(env, session_handle, stream_id, timeout_ns, header, bytes, bytes_written);

    detach_jni_thread(did_attach_thread);

    return status;
}

/// Forward the `stream_config` camera request through one attached bridge session.
uint32_t call_camera_stream_config_for_session(
    uint64_t session_handle,
    uint64_t stream_id,
    AndroidHostCameraStreamConfigHeader *config
) {
    JNIEnv *env = nullptr;
    bool did_attach_thread = false;

    if (!resolve_jni_env(&env, &did_attach_thread)) {
        return HOST_STATUS_FAILED;
    }

    uint32_t status = call_camera_stream_config(env, session_handle, stream_id, config);

    detach_jni_thread(did_attach_thread);

    return status;
}

/// Forward the `stream_recording_capabilities` camera request through one attached bridge session.
uint32_t call_camera_stream_recording_capabilities_for_session(
    uint64_t session_handle,
    uint64_t stream_id,
    AndroidHostCameraRecordingCapabilitiesHeader *capabilities
) {
    JNIEnv *env = nullptr;
    bool did_attach_thread = false;

    if (!resolve_jni_env(&env, &did_attach_thread)) {
        return HOST_STATUS_FAILED;
    }

    uint32_t status = call_camera_stream_recording_capabilities(env, session_handle, stream_id, capabilities);

    detach_jni_thread(did_attach_thread);

    return status;
}

/// Forward the `stream_start_recording` camera request through one attached bridge session.
uint32_t call_camera_stream_start_recording_for_session(
    uint64_t session_handle,
    uint64_t stream_id,
    AndroidHostCameraRecordingOptionsHeader options,
    NativeStringRef output_path
) {
    JNIEnv *env = nullptr;
    bool did_attach_thread = false;

    if (!resolve_jni_env(&env, &did_attach_thread)) {
        return HOST_STATUS_FAILED;
    }

    uint32_t status = call_camera_stream_start_recording(env, session_handle, stream_id, options, output_path);

    detach_jni_thread(did_attach_thread);

    return status;
}

/// Forward the `stream_pause_recording` camera request through one attached bridge session.
uint32_t call_camera_stream_pause_recording_for_session(
    uint64_t session_handle,
    uint64_t stream_id
) {
    JNIEnv *env = nullptr;
    bool did_attach_thread = false;

    if (!resolve_jni_env(&env, &did_attach_thread)) {
        return HOST_STATUS_FAILED;
    }

    uint32_t status = call_camera_stream_pause_recording(env, session_handle, stream_id);

    detach_jni_thread(did_attach_thread);

    return status;
}

/// Forward the `stream_resume_recording` camera request through one attached bridge session.
uint32_t call_camera_stream_resume_recording_for_session(
    uint64_t session_handle,
    uint64_t stream_id
) {
    JNIEnv *env = nullptr;
    bool did_attach_thread = false;

    if (!resolve_jni_env(&env, &did_attach_thread)) {
        return HOST_STATUS_FAILED;
    }

    uint32_t status = call_camera_stream_resume_recording(env, session_handle, stream_id);

    detach_jni_thread(did_attach_thread);

    return status;
}

/// Forward the `stream_stop_recording` camera request through one attached bridge session.
uint32_t call_camera_stream_stop_recording_for_session(
    uint64_t session_handle,
    uint64_t stream_id,
    uint64_t timeout_ns
) {
    JNIEnv *env = nullptr;
    bool did_attach_thread = false;

    if (!resolve_jni_env(&env, &did_attach_thread)) {
        return HOST_STATUS_FAILED;
    }

    uint32_t status = call_camera_stream_stop_recording(env, session_handle, stream_id, timeout_ns);

    detach_jni_thread(did_attach_thread);

    return status;
}

/// Forward the `stream_get_u64` camera request through one attached bridge session.
uint32_t call_camera_stream_get_u64_for_session(
    uint64_t session_handle,
    uint64_t stream_id,
    uint32_t selector,
    uint64_t *value
) {
    JNIEnv *env = nullptr;
    bool did_attach_thread = false;

    if (!resolve_jni_env(&env, &did_attach_thread)) {
        return HOST_STATUS_FAILED;
    }

    uint32_t status = call_camera_stream_get_u64(env, session_handle, stream_id, selector, value);

    detach_jni_thread(did_attach_thread);

    return status;
}

/// Forward the `stream_set_u64` camera request through one attached bridge session.
uint32_t call_camera_stream_set_u64_for_session(
    uint64_t session_handle,
    uint64_t stream_id,
    uint32_t selector,
    uint64_t value
) {
    JNIEnv *env = nullptr;
    bool did_attach_thread = false;

    if (!resolve_jni_env(&env, &did_attach_thread)) {
        return HOST_STATUS_FAILED;
    }

    uint32_t status = call_camera_stream_set_u64(env, session_handle, stream_id, selector, value);

    detach_jni_thread(did_attach_thread);

    return status;
}

/// Forward the `stream_get_u32` camera request through one attached bridge session.
uint32_t call_camera_stream_get_u32_for_session(
    uint64_t session_handle,
    uint64_t stream_id,
    uint32_t selector,
    uint32_t *value
) {
    JNIEnv *env = nullptr;
    bool did_attach_thread = false;

    if (!resolve_jni_env(&env, &did_attach_thread)) {
        return HOST_STATUS_FAILED;
    }

    uint32_t status = call_camera_stream_get_u32(env, session_handle, stream_id, selector, value);

    detach_jni_thread(did_attach_thread);

    return status;
}

/// Forward the `stream_set_u32` camera request through one attached bridge session.
uint32_t call_camera_stream_set_u32_for_session(
    uint64_t session_handle,
    uint64_t stream_id,
    uint32_t selector,
    uint32_t value
) {
    JNIEnv *env = nullptr;
    bool did_attach_thread = false;

    if (!resolve_jni_env(&env, &did_attach_thread)) {
        return HOST_STATUS_FAILED;
    }

    uint32_t status = call_camera_stream_set_u32(env, session_handle, stream_id, selector, value);

    detach_jni_thread(did_attach_thread);

    return status;
}

/// Forward the `stream_get_f64` camera request through one attached bridge session.
uint32_t call_camera_stream_get_f64_for_session(
    uint64_t session_handle,
    uint64_t stream_id,
    uint32_t selector,
    double *value
) {
    JNIEnv *env = nullptr;
    bool did_attach_thread = false;

    if (!resolve_jni_env(&env, &did_attach_thread)) {
        return HOST_STATUS_FAILED;
    }

    uint32_t status = call_camera_stream_get_f64(env, session_handle, stream_id, selector, value);

    detach_jni_thread(did_attach_thread);

    return status;
}

/// Forward the `stream_set_f64` camera request through one attached bridge session.
uint32_t call_camera_stream_set_f64_for_session(
    uint64_t session_handle,
    uint64_t stream_id,
    uint32_t selector,
    double value
) {
    JNIEnv *env = nullptr;
    bool did_attach_thread = false;

    if (!resolve_jni_env(&env, &did_attach_thread)) {
        return HOST_STATUS_FAILED;
    }

    uint32_t status = call_camera_stream_set_f64(env, session_handle, stream_id, selector, value);

    detach_jni_thread(did_attach_thread);

    return status;
}

/// Forward the `stream_get_range_f64` camera request through one attached bridge session.
uint32_t call_camera_stream_get_range_f64_for_session(
    uint64_t session_handle,
    uint64_t stream_id,
    uint32_t selector,
    double *minimum,
    double *maximum,
    double *step
) {
    JNIEnv *env = nullptr;
    bool did_attach_thread = false;

    if (!resolve_jni_env(&env, &did_attach_thread)) {
        return HOST_STATUS_FAILED;
    }

    uint32_t status = call_camera_stream_get_range_f64(env, session_handle, stream_id, selector, minimum, maximum, step);

    detach_jni_thread(did_attach_thread);

    return status;
}

/// Forward the `stream_get_range_u64` camera request through one attached bridge session.
uint32_t call_camera_stream_get_range_u64_for_session(
    uint64_t session_handle,
    uint64_t stream_id,
    uint32_t selector,
    uint64_t *minimum,
    uint64_t *maximum,
    uint64_t *step
) {
    JNIEnv *env = nullptr;
    bool did_attach_thread = false;

    if (!resolve_jni_env(&env, &did_attach_thread)) {
        return HOST_STATUS_FAILED;
    }

    uint32_t status = call_camera_stream_get_range_u64(env, session_handle, stream_id, selector, minimum, maximum, step);

    detach_jni_thread(did_attach_thread);

    return status;
}

/// Forward the `stream_get_range_u32` camera request through one attached bridge session.
uint32_t call_camera_stream_get_range_u32_for_session(
    uint64_t session_handle,
    uint64_t stream_id,
    uint32_t selector,
    uint32_t *minimum,
    uint32_t *maximum,
    uint32_t *step
) {
    JNIEnv *env = nullptr;
    bool did_attach_thread = false;

    if (!resolve_jni_env(&env, &did_attach_thread)) {
        return HOST_STATUS_FAILED;
    }

    uint32_t status = call_camera_stream_get_range_u32(env, session_handle, stream_id, selector, minimum, maximum, step);

    detach_jni_thread(did_attach_thread);

    return status;
}

/// Build one Android host camera callback bundle.
AndroidHostCameraCallbacks make_camera_callbacks() {
    return AndroidHostCameraCallbacks {
        .device_list = call_camera_device_list_for_session,
        .device_open = call_camera_device_open_for_session,
        .device_close = call_camera_device_close_for_session,
        .stream_config_list = call_camera_stream_config_list_for_session,
        .stream_capability_list = call_camera_stream_capability_list_for_session,
        .stream_open = call_camera_stream_open_for_session,
        .stream_close = call_camera_stream_close_for_session,
        .stream_start = call_camera_stream_start_for_session,
        .stream_stop = call_camera_stream_stop_for_session,
        .stream_read = call_camera_stream_read_for_session,
        .stream_try_read = call_camera_stream_try_read_for_session,
        .stream_take_photo = call_camera_stream_take_photo_for_session,
        .stream_config = call_camera_stream_config_for_session,
        .stream_recording_capabilities = call_camera_stream_recording_capabilities_for_session,
        .stream_start_recording = call_camera_stream_start_recording_for_session,
        .stream_pause_recording = call_camera_stream_pause_recording_for_session,
        .stream_resume_recording = call_camera_stream_resume_recording_for_session,
        .stream_stop_recording = call_camera_stream_stop_recording_for_session,
        .stream_get_u64 = call_camera_stream_get_u64_for_session,
        .stream_set_u64 = call_camera_stream_set_u64_for_session,
        .stream_get_u32 = call_camera_stream_get_u32_for_session,
        .stream_set_u32 = call_camera_stream_set_u32_for_session,
        .stream_get_f64 = call_camera_stream_get_f64_for_session,
        .stream_set_f64 = call_camera_stream_set_f64_for_session,
        .stream_get_range_f64 = call_camera_stream_get_range_f64_for_session,
        .stream_get_range_u64 = call_camera_stream_get_range_u64_for_session,
        .stream_get_range_u32 = call_camera_stream_get_range_u32_for_session,
    };
}
