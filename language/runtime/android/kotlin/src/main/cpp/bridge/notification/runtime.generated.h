// generated by generate-bindings: do not edit

#ifndef DESTACK_RUNTIME_ANDROID_BRIDGE_NOTIFICATION_RUNTIME_H
#define DESTACK_RUNTIME_ANDROID_BRIDGE_NOTIFICATION_RUNTIME_H

#include "../types.h"

/// Forward the `post` notification request through one attached bridge session.
uint32_t call_notification_post_for_session(
    uint64_t session_handle,
    HostNotificationRequest request
);

/// Forward the `cancel` notification request through one attached bridge session.
uint32_t call_notification_cancel_for_session(
    uint64_t session_handle,
    NativeStringRef identifier
);

/// Forward the `cancel_all` notification request through one attached bridge session.
uint32_t call_notification_cancel_all_for_session(
    uint64_t session_handle
);

/// Build one Android host notification callback bundle.
AndroidHostNotificationCallbacks make_notification_callbacks();

/// Register the notification runtime ingress JNI methods.
bool register_notification_runtime_natives(JNIEnv *env);

/// Send the `notification_event` notification ingress payload into the runtime ingress path.
RuntimeStatus send_notification_event(
    uint64_t session_handle,
    HostNotificationEvent event
);

#endif
