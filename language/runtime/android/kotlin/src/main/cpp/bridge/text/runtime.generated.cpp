// generated by generate-bindings: do not edit

#include "../types.h"
#include "../jni.h"
#include "../runtime_abi.generated.h"
#include "callbacks.generated.h"
#include "runtime.generated.h"

#include <deque>
#include <string>
#include <vector>

namespace {

/// Convert one host status code into one runtime status.
RuntimeStatus runtime_status_from_code(uint32_t code) {
    RuntimeStatus status = {
        .code = code,
        .error_id = 0,
    };

    return status;
}

/// Borrow one runtime string reference from one JNI string.
NativeStringRef native_string_ref_from_jstring(JNIEnv *env, jstring value) {
    if (value == nullptr) {
        return {
            .data = nullptr,
            .len = 0,
        };
    }

    const char *chars = env->GetStringUTFChars(value, nullptr);
    jsize length = env->GetStringUTFLength(value);

    return {
        .data = reinterpret_cast<const uint8_t *>(chars),
        .len = static_cast<uint32_t>(length),
    };
}

/// Release one runtime string reference borrowed from one JNI string.
void release_native_string_ref(JNIEnv *env, jstring value, NativeStringRef ref) {
    if (value == nullptr || ref.data == nullptr) {
        return;
    }

    env->ReleaseStringUTFChars(
        value,
        reinterpret_cast<const char *>(ref.data)
    );
}

}

/// Forward the `open` text request through one attached bridge session.
uint32_t call_text_open_for_session(
    uint64_t session_handle,
    HostTextInputOpenRequest request
) {
    JNIEnv *env = nullptr;
    bool did_attach_thread = false;

    if (!resolve_jni_env(&env, &did_attach_thread)) {
        return HOST_STATUS_FAILED;
    }

    uint32_t status = call_text_open(env, session_handle, request);

    detach_jni_thread(did_attach_thread);

    return status;
}

/// Forward the `close` text request through one attached bridge session.
uint32_t call_text_close_for_session(
    uint64_t session_handle,
    HostTextInputCloseRequest request
) {
    JNIEnv *env = nullptr;
    bool did_attach_thread = false;

    if (!resolve_jni_env(&env, &did_attach_thread)) {
        return HOST_STATUS_FAILED;
    }

    uint32_t status = call_text_close(env, session_handle, request);

    detach_jni_thread(did_attach_thread);

    return status;
}

/// Forward the `set_geometry` text request through one attached bridge session.
uint32_t call_text_set_geometry_for_session(
    uint64_t session_handle,
    HostTextInputGeometryRequest request
) {
    JNIEnv *env = nullptr;
    bool did_attach_thread = false;

    if (!resolve_jni_env(&env, &did_attach_thread)) {
        return HOST_STATUS_FAILED;
    }

    uint32_t status = call_text_set_geometry(env, session_handle, request);

    detach_jni_thread(did_attach_thread);

    return status;
}

/// Forward the `set_state` text request through one attached bridge session.
uint32_t call_text_set_state_for_session(
    uint64_t session_handle,
    HostTextInputStateRequest request
) {
    JNIEnv *env = nullptr;
    bool did_attach_thread = false;

    if (!resolve_jni_env(&env, &did_attach_thread)) {
        return HOST_STATUS_FAILED;
    }

    uint32_t status = call_text_set_state(env, session_handle, request);

    detach_jni_thread(did_attach_thread);

    return status;
}

/// Build one Android host text callback bundle.
AndroidHostTextCallbacks make_text_callbacks() {
    return AndroidHostTextCallbacks {
        .open = call_text_open_for_session,
        .close = call_text_close_for_session,
        .set_geometry = call_text_set_geometry_for_session,
        .set_state = call_text_set_state_for_session,
    };
}

/// Deliver one text-session state event into one runtime session.
RuntimeStatus send_text_input_state(
    uint64_t session_handle,
    HostTextInputEvent event
) {
    return destack_host_android_notify_text_input_state(session_handle, event);
}
