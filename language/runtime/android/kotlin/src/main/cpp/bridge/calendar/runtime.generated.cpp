// generated by generate-bindings: do not edit

#include "../types.h"
#include "../jni.h"
#include "../loader.h"
#include "callbacks.generated.h"
#include "runtime.generated.h"

#include <deque>
#include <string>
#include <vector>

namespace {

/// Convert one host status code into one runtime status.
RuntimeStatus runtime_status_from_code(uint32_t code) {
    RuntimeStatus status = {
        .code = code,
        .error_id = 0,
    };

    return status;
}

}

/// Forward the `list` calendar request through one attached bridge session.
uint32_t call_calendar_list_for_session(
    uint64_t session_handle,
    HostCalendarListResponse *response
) {
    JNIEnv *env = nullptr;
    bool did_attach_thread = false;

    if (!resolve_jni_env(&env, &did_attach_thread)) {
        return HOST_STATUS_FAILED;
    }

    uint32_t status = call_calendar_list(env, session_handle, response);

    detach_jni_thread(did_attach_thread);

    return status;
}

/// Forward the `event_list` calendar request through one attached bridge session.
uint32_t call_calendar_event_list_for_session(
    uint64_t session_handle,
    HostCalendarEventQuery query,
    HostCalendarEventListResponse *response
) {
    JNIEnv *env = nullptr;
    bool did_attach_thread = false;

    if (!resolve_jni_env(&env, &did_attach_thread)) {
        return HOST_STATUS_FAILED;
    }

    uint32_t status = call_calendar_event_list(env, session_handle, query, response);

    detach_jni_thread(did_attach_thread);

    return status;
}

/// Forward the `event_read` calendar request through one attached bridge session.
uint32_t call_calendar_event_read_for_session(
    uint64_t session_handle,
    NativeStringRef id,
    HostCalendarEventReadResponse *response
) {
    JNIEnv *env = nullptr;
    bool did_attach_thread = false;

    if (!resolve_jni_env(&env, &did_attach_thread)) {
        return HOST_STATUS_FAILED;
    }

    uint32_t status = call_calendar_event_read(env, session_handle, id, response);

    detach_jni_thread(did_attach_thread);

    return status;
}

/// Forward the `event_create` calendar request through one attached bridge session.
uint32_t call_calendar_event_create_for_session(
    uint64_t session_handle,
    HostCalendarEventDraft draft,
    HostCalendarEventCreateResponse *response
) {
    JNIEnv *env = nullptr;
    bool did_attach_thread = false;

    if (!resolve_jni_env(&env, &did_attach_thread)) {
        return HOST_STATUS_FAILED;
    }

    uint32_t status = call_calendar_event_create(env, session_handle, draft, response);

    detach_jni_thread(did_attach_thread);

    return status;
}

/// Forward the `event_update` calendar request through one attached bridge session.
uint32_t call_calendar_event_update_for_session(
    uint64_t session_handle,
    NativeStringRef id,
    HostCalendarEventDraft draft
) {
    JNIEnv *env = nullptr;
    bool did_attach_thread = false;

    if (!resolve_jni_env(&env, &did_attach_thread)) {
        return HOST_STATUS_FAILED;
    }

    uint32_t status = call_calendar_event_update(env, session_handle, id, draft);

    detach_jni_thread(did_attach_thread);

    return status;
}

/// Forward the `event_delete` calendar request through one attached bridge session.
uint32_t call_calendar_event_delete_for_session(
    uint64_t session_handle,
    NativeStringRef id
) {
    JNIEnv *env = nullptr;
    bool did_attach_thread = false;

    if (!resolve_jni_env(&env, &did_attach_thread)) {
        return HOST_STATUS_FAILED;
    }

    uint32_t status = call_calendar_event_delete(env, session_handle, id);

    detach_jni_thread(did_attach_thread);

    return status;
}

/// Build one Android host calendar callback bundle.
AndroidHostCalendarCallbacks make_calendar_callbacks() {
    return AndroidHostCalendarCallbacks {
        .list = call_calendar_list_for_session,
        .event_list = call_calendar_event_list_for_session,
        .event_read = call_calendar_event_read_for_session,
        .event_create = call_calendar_event_create_for_session,
        .event_update = call_calendar_event_update_for_session,
        .event_delete = call_calendar_event_delete_for_session,
    };
}
