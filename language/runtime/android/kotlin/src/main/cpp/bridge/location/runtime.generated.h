// generated by generate-bindings: do not edit

#ifndef DESTACK_RUNTIME_ANDROID_BRIDGE_LOCATION_RUNTIME_H
#define DESTACK_RUNTIME_ANDROID_BRIDGE_LOCATION_RUNTIME_H

#include "../types.h"

/// Forward the `services_enabled` location request through one attached bridge session.
uint32_t call_location_services_enabled_for_session(
    uint64_t session_handle,
    LocationServicesResponse *response
);

/// Forward the `last_known` location request through one attached bridge session.
uint32_t call_location_last_known_for_session(
    uint64_t session_handle,
    LocationLastKnownResponse *response
);

/// Forward the `watch_open` location request through one attached bridge session.
uint32_t call_location_watch_open_for_session(
    uint64_t session_handle,
    NativeStringRef watch_id,
    LocationWatchOptions options
);

/// Forward the `watch_close` location request through one attached bridge session.
uint32_t call_location_watch_close_for_session(
    uint64_t session_handle,
    NativeStringRef watch_id
);

/// Build one Android host location callback bundle.
AndroidHostLocationCallbacks make_location_callbacks();

/// Register the location runtime ingress JNI methods.
bool register_location_runtime_natives(JNIEnv *env);

/// Send the `location_sample` location ingress payload into the runtime ingress path.
RuntimeStatus send_location_sample(
    uint64_t session_handle,
    NativeStringRef watch_id,
    LocationSample sample
);

#endif
