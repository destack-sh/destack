// generated by generate-bindings: do not edit

#include "../types.h"
#include "../jni.h"
#include "../registry.h"
#include "callbacks.generated.h"

#include <deque>
#include <string>
#include <vector>

namespace {

jclass bridge_class = nullptr;
jmethodID status_method = nullptr;
jmethodID list_method = nullptr;
jmethodID registerTask_method = nullptr;
jmethodID unregister_method = nullptr;
jmethodID triggerTest_method = nullptr;
jmethodID complete_method = nullptr;

/// Build one JNI string borrowed from one native string reference.
jstring java_string_or_null(JNIEnv *env, NativeStringRef value) {
    if (value.data == nullptr) {
        return nullptr;
    }

    return new_java_string(env, value);
}

jobject box_int(JNIEnv *env, jint value) {
    jclass value_class = env->FindClass("java/lang/Integer");
    if (value_class == nullptr) {
        return nullptr;
    }

    jmethodID constructor = env->GetMethodID(value_class, "<init>", "(I)V");
    if (constructor == nullptr) {
        env->DeleteLocalRef(value_class);
        return nullptr;
    }

    jobject value_object = env->NewObject(value_class, constructor, value);
    env->DeleteLocalRef(value_class);
    return value_object;
}

jobject box_long(JNIEnv *env, jlong value) {
    jclass value_class = env->FindClass("java/lang/Long");
    if (value_class == nullptr) {
        return nullptr;
    }

    jmethodID constructor = env->GetMethodID(value_class, "<init>", "(J)V");
    if (constructor == nullptr) {
        env->DeleteLocalRef(value_class);
        return nullptr;
    }

    jobject value_object = env->NewObject(value_class, constructor, value);
    env->DeleteLocalRef(value_class);
    return value_object;
}

jobject box_boolean(JNIEnv *env, jboolean value) {
    jclass value_class = env->FindClass("java/lang/Boolean");
    if (value_class == nullptr) {
        return nullptr;
    }

    jmethodID constructor = env->GetMethodID(value_class, "<init>", "(Z)V");
    if (constructor == nullptr) {
        env->DeleteLocalRef(value_class);
        return nullptr;
    }

    jobject value_object = env->NewObject(value_class, constructor, value);
    env->DeleteLocalRef(value_class);
    return value_object;
}

jobject box_double(JNIEnv *env, jdouble value) {
    jclass value_class = env->FindClass("java/lang/Double");
    if (value_class == nullptr) {
        return nullptr;
    }

    jmethodID constructor = env->GetMethodID(value_class, "<init>", "(D)V");
    if (constructor == nullptr) {
        env->DeleteLocalRef(value_class);
        return nullptr;
    }

    jobject value_object = env->NewObject(value_class, constructor, value);
    env->DeleteLocalRef(value_class);
    return value_object;
}

/// Convert one Java string into one native string reference backed by stable storage.
NativeStringRef string_ref_from_java(
    JNIEnv *env,
    jstring value,
    std::deque<std::string> *storage
) {
    if (value == nullptr) {
        return NativeStringRef {
            .data = nullptr,
            .len = 0,
        };
    }

    const char *chars = env->GetStringUTFChars(value, nullptr);
    if (chars == nullptr) {
        return NativeStringRef {
            .data = nullptr,
            .len = 0,
        };
    }

    jsize length = env->GetStringUTFLength(value);
    storage->emplace_back(chars, chars + length);
    env->ReleaseStringUTFChars(value, chars);

    const std::string &owned = storage->back();

    return NativeStringRef {
        .data = reinterpret_cast<const uint8_t *>(owned.data()),
        .len = static_cast<uint32_t>(owned.size()),
    };
}

thread_local std::deque<std::string> result_string_storage;

void clear_result_decode_storage() {
    result_string_storage.clear();
}

HostBackgroundStatusResponse decode_HostBackgroundStatusResponse(JNIEnv *env, jobject value, std::deque<std::string> *string_storage);
HostBackgroundStatus decode_HostBackgroundStatus(JNIEnv *env, jobject value);
HostBackgroundListResponse decode_HostBackgroundListResponse(JNIEnv *env, jobject value, std::deque<std::string> *string_storage);
HostBackgroundTaskDescriptor decode_HostBackgroundTaskDescriptor(JNIEnv *env, jobject value, std::deque<std::string> *string_storage);
HostBackgroundTriggerKind decode_HostBackgroundTriggerKind(JNIEnv *env, jobject value);
HostBackgroundTaskSchedule decode_HostBackgroundTaskSchedule(JNIEnv *env, jobject value, std::deque<std::string> *string_storage);
HostBackgroundTaskScheduleKind decode_HostBackgroundTaskScheduleKind(JNIEnv *env, jobject value);
HostBackgroundNetworkRequirement decode_HostBackgroundNetworkRequirement(JNIEnv *env, jobject value);
HostBackgroundConflictPolicy decode_HostBackgroundConflictPolicy(JNIEnv *env, jobject value);
HostBackgroundTriggerTestResponse decode_HostBackgroundTriggerTestResponse(JNIEnv *env, jobject value, std::deque<std::string> *string_storage);
HostBackgroundTaskDescriptorSlice decode_HostBackgroundTaskDescriptorSlice(JNIEnv *env, jobject value, std::deque<std::string> *string_storage);

jmethodID resolve_instance_method(
    JNIEnv *env,
    jobject value,
    const char *name,
    const char *signature
) {
    jclass value_class = env->GetObjectClass(value);
    if (value_class == nullptr) {
        return nullptr;
    }

    jmethodID method = env->GetMethodID(value_class, name, signature);
    env->DeleteLocalRef(value_class);

    return method;
}

jobject call_object_getter(
    JNIEnv *env,
    jobject value,
    const char *name,
    const char *signature
) {
    jmethodID method = resolve_instance_method(env, value, name, signature);
    if (method == nullptr) {
        return nullptr;
    }

    return env->CallObjectMethod(value, method);
}

jint call_int_getter(JNIEnv *env, jobject value, const char *name) {
    jmethodID method = resolve_instance_method(env, value, name, "()I");
    return method == nullptr ? 0 : env->CallIntMethod(value, method);
}

jlong call_long_getter(JNIEnv *env, jobject value, const char *name) {
    jmethodID method = resolve_instance_method(env, value, name, "()J");
    return method == nullptr ? 0 : env->CallLongMethod(value, method);
}

jdouble call_double_getter(JNIEnv *env, jobject value, const char *name) {
    jmethodID method = resolve_instance_method(env, value, name, "()D");
    return method == nullptr ? 0.0 : env->CallDoubleMethod(value, method);
}

jboolean call_boolean_getter(JNIEnv *env, jobject value, const char *name) {
    jmethodID method = resolve_instance_method(env, value, name, "()Z");
    return method == nullptr ? JNI_FALSE : env->CallBooleanMethod(value, method);
}

uint64_t decode_boxed_u64(JNIEnv *env, jobject value) {
    jmethodID method = resolve_instance_method(env, value, "longValue", "()J");
    return method == nullptr ? 0 : static_cast<uint64_t>(env->CallLongMethod(value, method));
}

jint call_list_size(JNIEnv *env, jobject value) {
    jmethodID method = resolve_instance_method(env, value, "size", "()I");
    return method == nullptr ? 0 : env->CallIntMethod(value, method);
}

jobject call_list_get(JNIEnv *env, jobject value, jint index) {
    jmethodID method = resolve_instance_method(env, value, "get", "(I)Ljava/lang/Object;");
    return method == nullptr ? nullptr : env->CallObjectMethod(value, method, index);
}

HostBackgroundStatusResponse decode_HostBackgroundStatusResponse(JNIEnv *env, jobject value, std::deque<std::string> *string_storage) {
    HostBackgroundStatusResponse decoded = {};

    decoded.status = static_cast<uint32_t>(call_int_getter(env, value, "getStatus"));

    jobject scheduler_status_value = call_object_getter(env, value, "getSchedulerStatus", "()Ldev/destack/runtime/android/module/background/RuntimeHostBackgroundStatus;");
    decoded.scheduler_status = ([&]() -> OptionalHostBackgroundStatus { if (scheduler_status_value == nullptr) { return { .has_value = false, .value = {} }; } return { .has_value = true, .value = decode_HostBackgroundStatus(env, scheduler_status_value) }; })();
    if (scheduler_status_value != nullptr) { env->DeleteLocalRef(scheduler_status_value); }


    return decoded;
}

HostBackgroundStatus decode_HostBackgroundStatus(JNIEnv *env, jobject value) {
    jint ordinal = call_int_getter(env, value, "ordinal");

    return static_cast<HostBackgroundStatus>(ordinal + 1);
}

HostBackgroundListResponse decode_HostBackgroundListResponse(JNIEnv *env, jobject value, std::deque<std::string> *string_storage) {
    HostBackgroundListResponse decoded = {};

    decoded.status = static_cast<uint32_t>(call_int_getter(env, value, "getStatus"));

    jobject descriptors_value = call_object_getter(env, value, "getDescriptors", "()Ljava/util/List;");
    decoded.descriptors = decode_HostBackgroundTaskDescriptorSlice(env, descriptors_value, string_storage);
    if (descriptors_value != nullptr) { env->DeleteLocalRef(descriptors_value); }


    return decoded;
}

HostBackgroundTaskDescriptor decode_HostBackgroundTaskDescriptor(JNIEnv *env, jobject value, std::deque<std::string> *string_storage) {
    HostBackgroundTaskDescriptor decoded = {};

    jobject identifier_value = call_object_getter(env, value, "getIdentifier", "()Ljava/lang/String;");
    decoded.identifier = string_ref_from_java(env, static_cast<jstring>(identifier_value), string_storage);
    if (identifier_value != nullptr) { env->DeleteLocalRef(identifier_value); }

    jobject trigger_value = call_object_getter(env, value, "getTrigger", "()Ldev/destack/runtime/android/module/background/RuntimeHostBackgroundTriggerKind;");
    decoded.trigger = decode_HostBackgroundTriggerKind(env, trigger_value);
    if (trigger_value != nullptr) { env->DeleteLocalRef(trigger_value); }

    jobject schedule_value = call_object_getter(env, value, "getSchedule", "()Ldev/destack/runtime/android/module/background/RuntimeHostBackgroundTaskSchedule;");
    decoded.schedule = decode_HostBackgroundTaskSchedule(env, schedule_value, string_storage);
    if (schedule_value != nullptr) { env->DeleteLocalRef(schedule_value); }

    jobject network_value = call_object_getter(env, value, "getNetwork", "()Ldev/destack/runtime/android/module/background/RuntimeHostBackgroundNetworkRequirement;");
    decoded.network = decode_HostBackgroundNetworkRequirement(env, network_value);
    if (network_value != nullptr) { env->DeleteLocalRef(network_value); }

    decoded.requires_charging = call_boolean_getter(env, value, "getRequiresCharging") == JNI_TRUE;

    decoded.requires_idle = call_boolean_getter(env, value, "getRequiresIdle") == JNI_TRUE;

    jobject conflict_policy_value = call_object_getter(env, value, "getConflictPolicy", "()Ldev/destack/runtime/android/module/background/RuntimeHostBackgroundConflictPolicy;");
    decoded.conflict_policy = decode_HostBackgroundConflictPolicy(env, conflict_policy_value);
    if (conflict_policy_value != nullptr) { env->DeleteLocalRef(conflict_policy_value); }


    return decoded;
}

HostBackgroundTriggerKind decode_HostBackgroundTriggerKind(JNIEnv *env, jobject value) {
    jint ordinal = call_int_getter(env, value, "ordinal");

    return static_cast<HostBackgroundTriggerKind>(ordinal + 1);
}

HostBackgroundTaskSchedule decode_HostBackgroundTaskSchedule(JNIEnv *env, jobject value, std::deque<std::string> *string_storage) {
    HostBackgroundTaskSchedule decoded = {};

    jobject kind_value = call_object_getter(env, value, "getKind", "()Ldev/destack/runtime/android/module/background/RuntimeHostBackgroundTaskScheduleKind;");
    decoded.kind = decode_HostBackgroundTaskScheduleKind(env, kind_value);
    if (kind_value != nullptr) { env->DeleteLocalRef(kind_value); }

    jobject earliest_begin_unix_ns_value = call_object_getter(env, value, "getEarliestBeginUnixNs", "()Ljava/lang/Long;");
    decoded.earliest_begin_unix_ns = ([&]() -> OptionalU64 { if (earliest_begin_unix_ns_value == nullptr) { return { .has_value = false, .value = {} }; } return { .has_value = true, .value = decode_boxed_u64(env, earliest_begin_unix_ns_value) }; })();
    if (earliest_begin_unix_ns_value != nullptr) { env->DeleteLocalRef(earliest_begin_unix_ns_value); }

    jobject repeat_interval_ns_value = call_object_getter(env, value, "getRepeatIntervalNs", "()Ljava/lang/Long;");
    decoded.repeat_interval_ns = ([&]() -> OptionalU64 { if (repeat_interval_ns_value == nullptr) { return { .has_value = false, .value = {} }; } return { .has_value = true, .value = decode_boxed_u64(env, repeat_interval_ns_value) }; })();
    if (repeat_interval_ns_value != nullptr) { env->DeleteLocalRef(repeat_interval_ns_value); }


    return decoded;
}

HostBackgroundTaskScheduleKind decode_HostBackgroundTaskScheduleKind(JNIEnv *env, jobject value) {
    jint ordinal = call_int_getter(env, value, "ordinal");

    return static_cast<HostBackgroundTaskScheduleKind>(ordinal + 1);
}

HostBackgroundNetworkRequirement decode_HostBackgroundNetworkRequirement(JNIEnv *env, jobject value) {
    jint ordinal = call_int_getter(env, value, "ordinal");

    return static_cast<HostBackgroundNetworkRequirement>(ordinal + 1);
}

HostBackgroundConflictPolicy decode_HostBackgroundConflictPolicy(JNIEnv *env, jobject value) {
    jint ordinal = call_int_getter(env, value, "ordinal");

    return static_cast<HostBackgroundConflictPolicy>(ordinal + 1);
}

HostBackgroundTriggerTestResponse decode_HostBackgroundTriggerTestResponse(JNIEnv *env, jobject value, std::deque<std::string> *string_storage) {
    HostBackgroundTriggerTestResponse decoded = {};

    decoded.status = static_cast<uint32_t>(call_int_getter(env, value, "getStatus"));

    decoded.is_triggered = call_boolean_getter(env, value, "isTriggered") == JNI_TRUE;


    return decoded;
}

HostBackgroundTaskDescriptorSlice decode_HostBackgroundTaskDescriptorSlice(JNIEnv *env, jobject value, std::deque<std::string> *string_storage) {
    thread_local std::vector<HostBackgroundTaskDescriptor> storage;
    storage.clear();

    if (value == nullptr) {
        return HostBackgroundTaskDescriptorSlice { .data = nullptr, .len = 0 };
    }

    jint count = call_list_size(env, value);
    storage.reserve(static_cast<size_t>(count));
    for (jint index = 0; index < count; index += 1) {
        jobject element = call_list_get(env, value, index);
        if (element == nullptr) {
            storage.clear();
            return HostBackgroundTaskDescriptorSlice { .data = nullptr, .len = 0 };
        }

        storage.push_back(decode_HostBackgroundTaskDescriptor(env, element, string_storage));
        env->DeleteLocalRef(element);
    }

    return HostBackgroundTaskDescriptorSlice {
        .data = storage.empty() ? nullptr : storage.data(),
        .len = static_cast<uint32_t>(storage.size()),
    };
}


/// Resolve the background bridge methods from one runtime bridge instance.
bool resolve_bridge_methods(JNIEnv *env, jobject bridge) {
    if (bridge_class == nullptr) {
        jclass local_class = env->GetObjectClass(bridge);
        if (local_class == nullptr) {
            return false;
        }

        bridge_class = reinterpret_cast<jclass>(env->NewGlobalRef(local_class));
        env->DeleteLocalRef(local_class);
        if (bridge_class == nullptr) {
            return false;
        }
    }

    if (status_method == nullptr) {
        status_method = env->GetMethodID(
            bridge_class,
            "backgroundStatus",
            "()Ldev/destack/runtime/android/bridge/RuntimeHostBackgroundStatusResponse;"
        );
    }

    if (list_method == nullptr) {
        list_method = env->GetMethodID(
            bridge_class,
            "backgroundList",
            "()Ldev/destack/runtime/android/bridge/RuntimeHostBackgroundListResponse;"
        );
    }

    if (registerTask_method == nullptr) {
        registerTask_method = env->GetMethodID(
            bridge_class,
            "backgroundRegisterTask",
            "(Ljava/lang/String;IILjava/lang/Long;Ljava/lang/Long;IZZI)I"
        );
    }

    if (unregister_method == nullptr) {
        unregister_method = env->GetMethodID(
            bridge_class,
            "backgroundUnregister",
            "(Ljava/lang/String;)I"
        );
    }

    if (triggerTest_method == nullptr) {
        triggerTest_method = env->GetMethodID(
            bridge_class,
            "backgroundTriggerTest",
            "(Ljava/lang/String;)Ldev/destack/runtime/android/bridge/RuntimeHostBackgroundTriggerTestResponse;"
        );
    }

    if (complete_method == nullptr) {
        complete_method = env->GetMethodID(
            bridge_class,
            "backgroundComplete",
            "(Ljava/lang/String;I)I"
        );
    }

    return
        status_method != nullptr &&
        list_method != nullptr &&
        registerTask_method != nullptr &&
        unregister_method != nullptr &&
        triggerTest_method != nullptr &&
        complete_method != nullptr;
}

}

bool resolve_background_methods(JNIEnv *env, jobject bridge) {
    return resolve_bridge_methods(env, bridge);
}

/// Call the background-status entrypoint on one registered bridge.
uint32_t call_background_status(
    JNIEnv *env,
    uint64_t session_handle,
    HostBackgroundStatusResponse *response
) {
    jobject bridge = resolve_bridge(env, session_handle);
    if (bridge == nullptr) {
        return HOST_STATUS_NOT_FOUND;
    }


    jobject response_object = env->CallObjectMethod(
        bridge,
        status_method
    );
    if (response_object == nullptr) {
        if (env->ExceptionCheck()) {
            env->ExceptionClear();
        }
        env->DeleteLocalRef(bridge);
        return HOST_STATUS_FAILED;
    }

    clear_result_decode_storage();
    std::deque<std::string> *string_storage = &result_string_storage;

    jint status = call_int_getter(env, response_object, "getStatus");
    if (status == HOST_STATUS_OK) {
        jobject response_value = call_object_getter(env, response_object, "getResponse", "()Ldev/destack/runtime/android/module/background/RuntimeHostBackgroundStatusResponse;");
        if (response_value == nullptr) {
            status = HOST_STATUS_FAILED;
        } else {
            *response = decode_HostBackgroundStatusResponse(env, response_value, string_storage);
            env->DeleteLocalRef(response_value);
        }
    }
    env->DeleteLocalRef(response_object);
    env->DeleteLocalRef(bridge);

    if (env->ExceptionCheck()) {
        env->ExceptionClear();
        return HOST_STATUS_FAILED;
    }

    return static_cast<uint32_t>(status);
}

/// Call the background-list entrypoint on one registered bridge.
uint32_t call_background_list(
    JNIEnv *env,
    uint64_t session_handle,
    HostBackgroundListResponse *response
) {
    jobject bridge = resolve_bridge(env, session_handle);
    if (bridge == nullptr) {
        return HOST_STATUS_NOT_FOUND;
    }


    jobject response_object = env->CallObjectMethod(
        bridge,
        list_method
    );
    if (response_object == nullptr) {
        if (env->ExceptionCheck()) {
            env->ExceptionClear();
        }
        env->DeleteLocalRef(bridge);
        return HOST_STATUS_FAILED;
    }

    clear_result_decode_storage();
    std::deque<std::string> *string_storage = &result_string_storage;

    jint status = call_int_getter(env, response_object, "getStatus");
    if (status == HOST_STATUS_OK) {
        jobject response_value = call_object_getter(env, response_object, "getResponse", "()Ldev/destack/runtime/android/module/background/RuntimeHostBackgroundListResponse;");
        if (response_value == nullptr) {
            status = HOST_STATUS_FAILED;
        } else {
            *response = decode_HostBackgroundListResponse(env, response_value, string_storage);
            env->DeleteLocalRef(response_value);
        }
    }
    env->DeleteLocalRef(response_object);
    env->DeleteLocalRef(bridge);

    if (env->ExceptionCheck()) {
        env->ExceptionClear();
        return HOST_STATUS_FAILED;
    }

    return static_cast<uint32_t>(status);
}

/// Call the background-register-task entrypoint on one registered bridge.
uint32_t call_background_register_task(
    JNIEnv *env,
    uint64_t session_handle,
    HostBackgroundTaskOptions request
) {
    jobject bridge = resolve_bridge(env, session_handle);
    if (bridge == nullptr) {
        return HOST_STATUS_NOT_FOUND;
    }

    jstring identifier_value = java_string_or_null(env, request.identifier);
    jobject trigger_value = encode_HostBackgroundTriggerKind(env, request.trigger);
    jobject kind_value = encode_HostBackgroundTaskScheduleKind(env, request.schedule.kind);
    jobject earliest_begin_unix_ns_value = request.schedule.earliest_begin_unix_ns.has_value ? box_long(env, static_cast<jlong>(request.schedule.earliest_begin_unix_ns.value)) : nullptr;
    jobject repeat_interval_ns_value = request.schedule.repeat_interval_ns.has_value ? box_long(env, static_cast<jlong>(request.schedule.repeat_interval_ns.value)) : nullptr;
    jobject network_value = encode_HostBackgroundNetworkRequirement(env, request.network);
    jobject conflict_policy_value = encode_HostBackgroundConflictPolicy(env, request.conflict_policy);

    jint status = env->CallIntMethod(
        bridge,
        registerTask_method,
        identifier_value,
        trigger_value,
        kind_value,
        earliest_begin_unix_ns_value,
        repeat_interval_ns_value,
        network_value,
        request.requires_charging ? JNI_TRUE : JNI_FALSE,
        request.requires_idle ? JNI_TRUE : JNI_FALSE,
        conflict_policy_value
    );
    if (identifier_value != nullptr) { env->DeleteLocalRef(identifier_value); }
    if (trigger_value != nullptr) { env->DeleteLocalRef(trigger_value); }
    if (kind_value != nullptr) { env->DeleteLocalRef(kind_value); }
    if (earliest_begin_unix_ns_value != nullptr) { env->DeleteLocalRef(earliest_begin_unix_ns_value); }
    if (repeat_interval_ns_value != nullptr) { env->DeleteLocalRef(repeat_interval_ns_value); }
    if (network_value != nullptr) { env->DeleteLocalRef(network_value); }
    if (conflict_policy_value != nullptr) { env->DeleteLocalRef(conflict_policy_value); }
    env->DeleteLocalRef(bridge);

    if (env->ExceptionCheck()) {
        env->ExceptionClear();
        return HOST_STATUS_FAILED;
    }

    return static_cast<uint32_t>(status);
}

/// Call the background-unregister entrypoint on one registered bridge.
uint32_t call_background_unregister(
    JNIEnv *env,
    uint64_t session_handle,
    HostBackgroundUnregisterRequest request
) {
    jobject bridge = resolve_bridge(env, session_handle);
    if (bridge == nullptr) {
        return HOST_STATUS_NOT_FOUND;
    }

    jstring identifier_value = java_string_or_null(env, request.identifier);

    jint status = env->CallIntMethod(
        bridge,
        unregister_method,
        identifier_value
    );
    if (identifier_value != nullptr) { env->DeleteLocalRef(identifier_value); }
    env->DeleteLocalRef(bridge);

    if (env->ExceptionCheck()) {
        env->ExceptionClear();
        return HOST_STATUS_FAILED;
    }

    return static_cast<uint32_t>(status);
}

/// Call the background-trigger-test entrypoint on one registered bridge.
uint32_t call_background_trigger_test(
    JNIEnv *env,
    uint64_t session_handle,
    HostBackgroundTriggerTestRequest request,
    HostBackgroundTriggerTestResponse *response
) {
    jobject bridge = resolve_bridge(env, session_handle);
    if (bridge == nullptr) {
        return HOST_STATUS_NOT_FOUND;
    }

    jstring identifier_value = java_string_or_null(env, request.identifier);

    jobject response_object = env->CallObjectMethod(
        bridge,
        triggerTest_method,
        identifier_value
    );
    if (response_object == nullptr) {
        if (identifier_value != nullptr) { env->DeleteLocalRef(identifier_value); }
        if (env->ExceptionCheck()) {
            env->ExceptionClear();
        }
        env->DeleteLocalRef(bridge);
        return HOST_STATUS_FAILED;
    }

    clear_result_decode_storage();
    std::deque<std::string> *string_storage = &result_string_storage;

    jint status = call_int_getter(env, response_object, "getStatus");
    if (status == HOST_STATUS_OK) {
        jobject response_value = call_object_getter(env, response_object, "getResponse", "()Ldev/destack/runtime/android/module/background/RuntimeHostBackgroundTriggerTestResponse;");
        if (response_value == nullptr) {
            status = HOST_STATUS_FAILED;
        } else {
            *response = decode_HostBackgroundTriggerTestResponse(env, response_value, string_storage);
            env->DeleteLocalRef(response_value);
        }
    }
    if (identifier_value != nullptr) { env->DeleteLocalRef(identifier_value); }
    env->DeleteLocalRef(response_object);
    env->DeleteLocalRef(bridge);

    if (env->ExceptionCheck()) {
        env->ExceptionClear();
        return HOST_STATUS_FAILED;
    }

    return static_cast<uint32_t>(status);
}

/// Call the background-complete entrypoint on one registered bridge.
uint32_t call_background_complete(
    JNIEnv *env,
    uint64_t session_handle,
    HostBackgroundCompleteRequest request
) {
    jobject bridge = resolve_bridge(env, session_handle);
    if (bridge == nullptr) {
        return HOST_STATUS_NOT_FOUND;
    }

    jstring execution_id_value = java_string_or_null(env, request.execution_id);
    jobject result_value = encode_HostBackgroundTaskResult(env, request.result);

    jint status = env->CallIntMethod(
        bridge,
        complete_method,
        execution_id_value,
        result_value
    );
    if (execution_id_value != nullptr) { env->DeleteLocalRef(execution_id_value); }
    if (result_value != nullptr) { env->DeleteLocalRef(result_value); }
    env->DeleteLocalRef(bridge);

    if (env->ExceptionCheck()) {
        env->ExceptionClear();
        return HOST_STATUS_FAILED;
    }

    return static_cast<uint32_t>(status);
}
