// generated by generate-bindings: do not edit

#include "../types.h"
#include "../jni.h"
#include "../registry.h"
#include "callbacks.generated.h"

#include <deque>
#include <string>
#include <vector>

namespace {

jclass bridge_class = nullptr;
jmethodID read_method = nullptr;
jmethodID write_method = nullptr;
jmethodID delete_method = nullptr;
jmethodID contains_method = nullptr;
jmethodID authenticate_method = nullptr;

/// Build one JNI string borrowed from one native string reference.
jstring java_string_or_null(JNIEnv *env, NativeStringRef value) {
    if (value.data == nullptr) {
        return nullptr;
    }

    return new_java_string(env, value);
}

jmethodID resolve_constructor(JNIEnv *env, jclass value_class, const char *signature) {
    return env->GetMethodID(value_class, "<init>", signature);
}

thread_local std::deque<std::string> result_string_storage;
thread_local std::deque<std::vector<uint8_t>> result_u8_storage;

void clear_result_decode_storage() {
    result_string_storage.clear();
    result_u8_storage.clear();
}

jint call_list_size(JNIEnv *env, jobject value);
jobject call_list_get(JNIEnv *env, jobject value, jint index);

jint call_int_getter(JNIEnv *env, jobject value, const char *name);

jintArray encode_u8_slice(JNIEnv *env, NativeU8Slice values) {
    jintArray array = env->NewIntArray(static_cast<jsize>(values.len));
    if (array == nullptr) {
        return nullptr;
    }

    std::vector<jint> buffer(static_cast<size_t>(values.len));
    for (uint32_t index = 0; index < values.len; index += 1) {
        buffer[static_cast<size_t>(index)] = static_cast<jint>(values.data[index]);
    }

    env->SetIntArrayRegion(array, 0, static_cast<jsize>(values.len), buffer.data());
    return array;
}

NativeU8Slice decode_u8_list(JNIEnv *env, jobject list) {
    if (list == nullptr) {
        return NativeU8Slice {
            .data = nullptr,
            .len = 0,
        };
    }

    jint size = call_list_size(env, list);
    result_u8_storage.emplace_back();
    auto &storage = result_u8_storage.back();
    storage.reserve(static_cast<size_t>(size));

    for (jint index = 0; index < size; index += 1) {
        jobject value = call_list_get(env, list, index);
        jint resolved = call_int_getter(env, value, "intValue");
        storage.push_back(static_cast<uint8_t>(resolved));
        env->DeleteLocalRef(value);
    }

    return NativeU8Slice {
        .data = storage.data(),
        .len = static_cast<uint32_t>(storage.size()),
    };
}


jmethodID resolve_instance_method(
    JNIEnv *env,
    jobject value,
    const char *name,
    const char *signature
) {
    jclass value_class = env->GetObjectClass(value);
    if (value_class == nullptr) {
        return nullptr;
    }

    jmethodID method = env->GetMethodID(value_class, name, signature);
    env->DeleteLocalRef(value_class);

    return method;
}

jobject call_object_getter(
    JNIEnv *env,
    jobject value,
    const char *name,
    const char *signature
) {
    jmethodID method = resolve_instance_method(env, value, name, signature);
    if (method == nullptr) {
        return nullptr;
    }

    return env->CallObjectMethod(value, method);
}

jint call_int_getter(JNIEnv *env, jobject value, const char *name) {
    jmethodID method = resolve_instance_method(env, value, name, "()I");
    return method == nullptr ? 0 : env->CallIntMethod(value, method);
}

jlong call_long_getter(JNIEnv *env, jobject value, const char *name) {
    jmethodID method = resolve_instance_method(env, value, name, "()J");
    return method == nullptr ? 0 : env->CallLongMethod(value, method);
}

jdouble call_double_getter(JNIEnv *env, jobject value, const char *name) {
    jmethodID method = resolve_instance_method(env, value, name, "()D");
    return method == nullptr ? 0.0 : env->CallDoubleMethod(value, method);
}

jboolean call_boolean_getter(JNIEnv *env, jobject value, const char *name) {
    jmethodID method = resolve_instance_method(env, value, name, "()Z");
    return method == nullptr ? JNI_FALSE : env->CallBooleanMethod(value, method);
}

uint64_t decode_boxed_u64(JNIEnv *env, jobject value) {
    jmethodID method = resolve_instance_method(env, value, "longValue", "()J");
    return method == nullptr ? 0 : static_cast<uint64_t>(env->CallLongMethod(value, method));
}

jint call_list_size(JNIEnv *env, jobject value) {
    jmethodID method = resolve_instance_method(env, value, "size", "()I");
    return method == nullptr ? 0 : env->CallIntMethod(value, method);
}

jobject call_list_get(JNIEnv *env, jobject value, jint index) {
    jmethodID method = resolve_instance_method(env, value, "get", "(I)Ljava/lang/Object;");
    return method == nullptr ? nullptr : env->CallObjectMethod(value, method, index);
}


/// Resolve the credentials bridge methods from one runtime bridge instance.
bool resolve_bridge_methods(JNIEnv *env, jobject bridge) {
    if (bridge_class == nullptr) {
        jclass local_class = env->GetObjectClass(bridge);
        if (local_class == nullptr) {
            return false;
        }

        bridge_class = reinterpret_cast<jclass>(env->NewGlobalRef(local_class));
        env->DeleteLocalRef(local_class);
        if (bridge_class == nullptr) {
            return false;
        }
    }

    if (read_method == nullptr) {
        read_method = env->GetMethodID(
            bridge_class,
            "credentialsRead",
            "(Ljava/lang/String;Ljava/lang/String;Ljava/lang/String;Z[I)Ldev/destack/runtime/android/bridge/RuntimeHostCredentialsReadResponse;"
        );
    }

    if (write_method == nullptr) {
        write_method = env->GetMethodID(
            bridge_class,
            "credentialsWrite",
            "(Ljava/lang/String;Ljava/lang/String;Ljava/lang/String;[IIIZ)I"
        );
    }

    if (delete_method == nullptr) {
        delete_method = env->GetMethodID(
            bridge_class,
            "credentialsDelete",
            "(Ljava/lang/String;Ljava/lang/String;Ljava/lang/String;)I"
        );
    }

    if (contains_method == nullptr) {
        contains_method = env->GetMethodID(
            bridge_class,
            "credentialsContains",
            "(Ljava/lang/String;Ljava/lang/String;Ljava/lang/String;)Ldev/destack/runtime/android/bridge/RuntimeHostCredentialsContainsResponse;"
        );
    }

    if (authenticate_method == nullptr) {
        authenticate_method = env->GetMethodID(
            bridge_class,
            "credentialsAuthenticate",
            "(Ljava/lang/String;Ljava/lang/String;Ljava/lang/String;I)Ldev/destack/runtime/android/bridge/RuntimeHostCredentialsAuthenticateResponse;"
        );
    }

    return
        read_method != nullptr &&
        write_method != nullptr &&
        delete_method != nullptr &&
        contains_method != nullptr &&
        authenticate_method != nullptr;
}

}

bool resolve_credentials_methods(JNIEnv *env, jobject bridge) {
    return resolve_bridge_methods(env, bridge);
}

/// Call the credentials-read entrypoint on one registered bridge.
uint32_t call_credentials_read(
    JNIEnv *env,
    uint64_t session_handle,
    NativeStringRef service,
    NativeStringRef account,
    NativeStringRef access_group,
    bool require_authentication,
    NativeU8Slice output,
    uint32_t *output_written,
    uint64_t *created_unix_ns,
    uint64_t *modified_unix_ns
) {
    jobject bridge = resolve_bridge(env, session_handle);
    if (bridge == nullptr) {
        return HOST_STATUS_NOT_FOUND;
    }

    jstring service_value = java_string_or_null(env, service);
    jstring account_value = java_string_or_null(env, account);
    jstring access_group_value = java_string_or_null(env, access_group);
    jintArray output_value = encode_u8_slice(env, output);
    if (output_value == nullptr) {
        if (service_value != nullptr) { env->DeleteLocalRef(service_value); }
        if (account_value != nullptr) { env->DeleteLocalRef(account_value); }
        if (access_group_value != nullptr) { env->DeleteLocalRef(access_group_value); }
        if (env->ExceptionCheck()) {
            env->ExceptionClear();
        }
        env->DeleteLocalRef(bridge);
        return HOST_STATUS_FAILED;
    }

    jobject response_object = env->CallObjectMethod(
        bridge,
        read_method,
        service_value,
        account_value,
        access_group_value,
        require_authentication ? JNI_TRUE : JNI_FALSE,
        output_value
    );
    if (response_object == nullptr) {
        if (service_value != nullptr) { env->DeleteLocalRef(service_value); }
        if (account_value != nullptr) { env->DeleteLocalRef(account_value); }
        if (access_group_value != nullptr) { env->DeleteLocalRef(access_group_value); }
        if (output_value != nullptr) { env->DeleteLocalRef(output_value); }
        if (env->ExceptionCheck()) {
            env->ExceptionClear();
        }
        env->DeleteLocalRef(bridge);
        return HOST_STATUS_FAILED;
    }

    jint status = call_int_getter(env, response_object, "getStatus");
    if (status == HOST_STATUS_OK) {
        *output_written = static_cast<uint32_t>(call_int_getter(env, response_object, "getOutputWritten"));
        *created_unix_ns = static_cast<uint64_t>(call_long_getter(env, response_object, "getCreatedUnixNs"));
        *modified_unix_ns = static_cast<uint64_t>(call_long_getter(env, response_object, "getModifiedUnixNs"));
    }
    if (service_value != nullptr) { env->DeleteLocalRef(service_value); }
    if (account_value != nullptr) { env->DeleteLocalRef(account_value); }
    if (access_group_value != nullptr) { env->DeleteLocalRef(access_group_value); }
    if (output_value != nullptr) { env->DeleteLocalRef(output_value); }
    env->DeleteLocalRef(response_object);
    env->DeleteLocalRef(bridge);

    if (env->ExceptionCheck()) {
        env->ExceptionClear();
        return HOST_STATUS_FAILED;
    }

    return static_cast<uint32_t>(status);
}

/// Call the credentials-write entrypoint on one registered bridge.
uint32_t call_credentials_write(
    JNIEnv *env,
    uint64_t session_handle,
    NativeStringRef service,
    NativeStringRef account,
    NativeStringRef access_group,
    NativeU8Slice payload,
    uint32_t accessibility,
    uint32_t authentication_policy,
    bool replace_existing
) {
    jobject bridge = resolve_bridge(env, session_handle);
    if (bridge == nullptr) {
        return HOST_STATUS_NOT_FOUND;
    }

    jstring service_value = java_string_or_null(env, service);
    jstring account_value = java_string_or_null(env, account);
    jstring access_group_value = java_string_or_null(env, access_group);
    jintArray payload_value = encode_u8_slice(env, payload);
    if (payload_value == nullptr) {
        if (service_value != nullptr) { env->DeleteLocalRef(service_value); }
        if (account_value != nullptr) { env->DeleteLocalRef(account_value); }
        if (access_group_value != nullptr) { env->DeleteLocalRef(access_group_value); }
        if (env->ExceptionCheck()) {
            env->ExceptionClear();
        }
        env->DeleteLocalRef(bridge);
        return HOST_STATUS_FAILED;
    }

    jint status = env->CallIntMethod(
        bridge,
        write_method,
        service_value,
        account_value,
        access_group_value,
        payload_value,
        static_cast<jint>(accessibility),
        static_cast<jint>(authentication_policy),
        replace_existing ? JNI_TRUE : JNI_FALSE
    );
    if (service_value != nullptr) { env->DeleteLocalRef(service_value); }
    if (account_value != nullptr) { env->DeleteLocalRef(account_value); }
    if (access_group_value != nullptr) { env->DeleteLocalRef(access_group_value); }
    if (payload_value != nullptr) { env->DeleteLocalRef(payload_value); }
    env->DeleteLocalRef(bridge);

    if (env->ExceptionCheck()) {
        env->ExceptionClear();
        return HOST_STATUS_FAILED;
    }

    return static_cast<uint32_t>(status);
}

/// Call the credentials-delete entrypoint on one registered bridge.
uint32_t call_credentials_delete(
    JNIEnv *env,
    uint64_t session_handle,
    NativeStringRef service,
    NativeStringRef account,
    NativeStringRef access_group
) {
    jobject bridge = resolve_bridge(env, session_handle);
    if (bridge == nullptr) {
        return HOST_STATUS_NOT_FOUND;
    }

    jstring service_value = java_string_or_null(env, service);
    jstring account_value = java_string_or_null(env, account);
    jstring access_group_value = java_string_or_null(env, access_group);

    jint status = env->CallIntMethod(
        bridge,
        delete_method,
        service_value,
        account_value,
        access_group_value
    );
    if (service_value != nullptr) { env->DeleteLocalRef(service_value); }
    if (account_value != nullptr) { env->DeleteLocalRef(account_value); }
    if (access_group_value != nullptr) { env->DeleteLocalRef(access_group_value); }
    env->DeleteLocalRef(bridge);

    if (env->ExceptionCheck()) {
        env->ExceptionClear();
        return HOST_STATUS_FAILED;
    }

    return static_cast<uint32_t>(status);
}

/// Call the credentials-contains entrypoint on one registered bridge.
uint32_t call_credentials_contains(
    JNIEnv *env,
    uint64_t session_handle,
    NativeStringRef service,
    NativeStringRef account,
    NativeStringRef access_group,
    bool *is_present
) {
    jobject bridge = resolve_bridge(env, session_handle);
    if (bridge == nullptr) {
        return HOST_STATUS_NOT_FOUND;
    }

    jstring service_value = java_string_or_null(env, service);
    jstring account_value = java_string_or_null(env, account);
    jstring access_group_value = java_string_or_null(env, access_group);

    jobject response_object = env->CallObjectMethod(
        bridge,
        contains_method,
        service_value,
        account_value,
        access_group_value
    );
    if (response_object == nullptr) {
        if (service_value != nullptr) { env->DeleteLocalRef(service_value); }
        if (account_value != nullptr) { env->DeleteLocalRef(account_value); }
        if (access_group_value != nullptr) { env->DeleteLocalRef(access_group_value); }
        if (env->ExceptionCheck()) {
            env->ExceptionClear();
        }
        env->DeleteLocalRef(bridge);
        return HOST_STATUS_FAILED;
    }

    jint status = call_int_getter(env, response_object, "getStatus");
    if (status == HOST_STATUS_OK) {
        *is_present = call_boolean_getter(env, response_object, "isPresent") == JNI_TRUE;
    }
    if (service_value != nullptr) { env->DeleteLocalRef(service_value); }
    if (account_value != nullptr) { env->DeleteLocalRef(account_value); }
    if (access_group_value != nullptr) { env->DeleteLocalRef(access_group_value); }
    env->DeleteLocalRef(response_object);
    env->DeleteLocalRef(bridge);

    if (env->ExceptionCheck()) {
        env->ExceptionClear();
        return HOST_STATUS_FAILED;
    }

    return static_cast<uint32_t>(status);
}

/// Call the credentials-authenticate entrypoint on one registered bridge.
uint32_t call_credentials_authenticate(
    JNIEnv *env,
    uint64_t session_handle,
    NativeStringRef title,
    NativeStringRef subtitle,
    NativeStringRef message,
    uint32_t requirement,
    bool *authenticated,
    uint32_t *mechanism
) {
    jobject bridge = resolve_bridge(env, session_handle);
    if (bridge == nullptr) {
        return HOST_STATUS_NOT_FOUND;
    }

    jstring title_value = java_string_or_null(env, title);
    jstring subtitle_value = java_string_or_null(env, subtitle);
    jstring message_value = java_string_or_null(env, message);

    jobject response_object = env->CallObjectMethod(
        bridge,
        authenticate_method,
        title_value,
        subtitle_value,
        message_value,
        static_cast<jint>(requirement)
    );
    if (response_object == nullptr) {
        if (title_value != nullptr) { env->DeleteLocalRef(title_value); }
        if (subtitle_value != nullptr) { env->DeleteLocalRef(subtitle_value); }
        if (message_value != nullptr) { env->DeleteLocalRef(message_value); }
        if (env->ExceptionCheck()) {
            env->ExceptionClear();
        }
        env->DeleteLocalRef(bridge);
        return HOST_STATUS_FAILED;
    }

    jint status = call_int_getter(env, response_object, "getStatus");
    if (status == HOST_STATUS_OK) {
        *authenticated = call_boolean_getter(env, response_object, "getAuthenticated") == JNI_TRUE;
        *mechanism = static_cast<uint32_t>(call_int_getter(env, response_object, "getMechanism"));
    }
    if (title_value != nullptr) { env->DeleteLocalRef(title_value); }
    if (subtitle_value != nullptr) { env->DeleteLocalRef(subtitle_value); }
    if (message_value != nullptr) { env->DeleteLocalRef(message_value); }
    env->DeleteLocalRef(response_object);
    env->DeleteLocalRef(bridge);

    if (env->ExceptionCheck()) {
        env->ExceptionClear();
        return HOST_STATUS_FAILED;
    }

    return static_cast<uint32_t>(status);
}
