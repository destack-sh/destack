// generated by generate-bindings: do not edit

#include "../types.h"
#include "../jni.h"
#include "../loader.h"
#include "callbacks.generated.h"
#include "runtime.generated.h"

#include <deque>
#include <string>
#include <vector>

namespace {

/// Convert one host status code into one runtime status.
RuntimeStatus runtime_status_from_code(uint32_t code) {
    RuntimeStatus status = {
        .code = code,
        .error_id = 0,
    };

    return status;
}

/// Borrow one runtime string reference from one JNI string.
NativeStringRef native_string_ref_from_jstring(JNIEnv *env, jstring value) {
    if (value == nullptr) {
        return {
            .data = nullptr,
            .len = 0,
        };
    }

    const char *chars = env->GetStringUTFChars(value, nullptr);
    jsize length = env->GetStringUTFLength(value);

    return {
        .data = reinterpret_cast<const uint8_t *>(chars),
        .len = static_cast<uint32_t>(length),
    };
}

/// Release one runtime string reference borrowed from one JNI string.
void release_native_string_ref(JNIEnv *env, jstring value, NativeStringRef ref) {
    if (value == nullptr || ref.data == nullptr) {
        return;
    }

    env->ReleaseStringUTFChars(
        value,
        reinterpret_cast<const char *>(ref.data)
    );
}

}

/// Forward the `can_open_url` intent request through one attached bridge session.
uint32_t call_intent_can_open_url_for_session(
    uint64_t session_handle,
    NativeStringRef url,
    bool *is_supported
) {
    JNIEnv *env = nullptr;
    bool did_attach_thread = false;

    if (!resolve_jni_env(&env, &did_attach_thread)) {
        return HOST_STATUS_FAILED;
    }

    uint32_t status = call_intent_can_open_url(env, session_handle, url, is_supported);

    detach_jni_thread(did_attach_thread);

    return status;
}

/// Forward the `open_url` intent request through one attached bridge session.
uint32_t call_intent_open_url_for_session(
    uint64_t session_handle,
    NativeStringRef url
) {
    JNIEnv *env = nullptr;
    bool did_attach_thread = false;

    if (!resolve_jni_env(&env, &did_attach_thread)) {
        return HOST_STATUS_FAILED;
    }

    uint32_t status = call_intent_open_url(env, session_handle, url);

    detach_jni_thread(did_attach_thread);

    return status;
}

/// Forward the `open_path` intent request through one attached bridge session.
uint32_t call_intent_open_path_for_session(
    uint64_t session_handle,
    NativeStringRef path
) {
    JNIEnv *env = nullptr;
    bool did_attach_thread = false;

    if (!resolve_jni_env(&env, &did_attach_thread)) {
        return HOST_STATUS_FAILED;
    }

    uint32_t status = call_intent_open_path(env, session_handle, path);

    detach_jni_thread(did_attach_thread);

    return status;
}

/// Forward the `share_text` intent request through one attached bridge session.
uint32_t call_intent_share_text_for_session(
    uint64_t session_handle,
    NativeStringRef text,
    OptionalStringRef mime_type
) {
    JNIEnv *env = nullptr;
    bool did_attach_thread = false;

    if (!resolve_jni_env(&env, &did_attach_thread)) {
        return HOST_STATUS_FAILED;
    }

    uint32_t status = call_intent_share_text(env, session_handle, text, mime_type);

    detach_jni_thread(did_attach_thread);

    return status;
}

/// Forward the `share_paths` intent request through one attached bridge session.
uint32_t call_intent_share_paths_for_session(
    uint64_t session_handle,
    NativeStringSlice paths,
    OptionalStringRef mime_type
) {
    JNIEnv *env = nullptr;
    bool did_attach_thread = false;

    if (!resolve_jni_env(&env, &did_attach_thread)) {
        return HOST_STATUS_FAILED;
    }

    uint32_t status = call_intent_share_paths(env, session_handle, paths, mime_type);

    detach_jni_thread(did_attach_thread);

    return status;
}

/// Build one Android host intent callback bundle.
AndroidHostIntentCallbacks make_intent_callbacks() {
    return AndroidHostIntentCallbacks {
        .can_open_url = call_intent_can_open_url_for_session,
        .open_url = call_intent_open_url_for_session,
        .open_path = call_intent_open_path_for_session,
        .share_text = call_intent_share_text_for_session,
        .share_paths = call_intent_share_paths_for_session,
    };
}

/// Deliver one intent event into one runtime session.
RuntimeStatus send_notify_intent_event(
    uint64_t session_handle,
    HostIntentEvent event
) {
    RuntimeBindings bindings = {};
    RuntimeStatus status = runtime_status_from_code(resolve_runtime_bindings(&bindings));
    if (status.code != HOST_STATUS_OK) {
        return status;
    }

    return bindings.notify_intent_event(session_handle, event);
}
