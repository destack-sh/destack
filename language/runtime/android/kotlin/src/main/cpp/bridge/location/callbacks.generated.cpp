// generated by generate-bindings: do not edit

#include "../types.h"
#include "../jni.h"
#include "../registry.h"
#include "callbacks.generated.h"

#include <deque>
#include <string>
#include <vector>

namespace {

jclass bridge_class = nullptr;
jmethodID servicesEnabled_method = nullptr;
jmethodID lastKnown_method = nullptr;
jmethodID watchOpen_method = nullptr;
jmethodID watchClose_method = nullptr;

/// Build one JNI string borrowed from one native string reference.
jstring java_string_or_null(JNIEnv *env, NativeStringRef value) {
    if (value.data == nullptr) {
        return nullptr;
    }

    return new_java_string(env, value);
}

thread_local std::deque<std::string> result_string_storage;

void clear_result_decode_storage() {
    result_string_storage.clear();
}

LocationServicesResponse decode_LocationServicesResponse(JNIEnv *env, jobject value, std::deque<std::string> *string_storage);
LocationLastKnownResponse decode_LocationLastKnownResponse(JNIEnv *env, jobject value, std::deque<std::string> *string_storage);
LocationSample decode_LocationSample(JNIEnv *env, jobject value, std::deque<std::string> *string_storage);

jmethodID resolve_instance_method(
    JNIEnv *env,
    jobject value,
    const char *name,
    const char *signature
) {
    jclass value_class = env->GetObjectClass(value);
    if (value_class == nullptr) {
        return nullptr;
    }

    jmethodID method = env->GetMethodID(value_class, name, signature);
    env->DeleteLocalRef(value_class);

    return method;
}

jobject call_object_getter(
    JNIEnv *env,
    jobject value,
    const char *name,
    const char *signature
) {
    jmethodID method = resolve_instance_method(env, value, name, signature);
    if (method == nullptr) {
        return nullptr;
    }

    return env->CallObjectMethod(value, method);
}

jint call_int_getter(JNIEnv *env, jobject value, const char *name) {
    jmethodID method = resolve_instance_method(env, value, name, "()I");
    return method == nullptr ? 0 : env->CallIntMethod(value, method);
}

jlong call_long_getter(JNIEnv *env, jobject value, const char *name) {
    jmethodID method = resolve_instance_method(env, value, name, "()J");
    return method == nullptr ? 0 : env->CallLongMethod(value, method);
}

jdouble call_double_getter(JNIEnv *env, jobject value, const char *name) {
    jmethodID method = resolve_instance_method(env, value, name, "()D");
    return method == nullptr ? 0.0 : env->CallDoubleMethod(value, method);
}

jboolean call_boolean_getter(JNIEnv *env, jobject value, const char *name) {
    jmethodID method = resolve_instance_method(env, value, name, "()Z");
    return method == nullptr ? JNI_FALSE : env->CallBooleanMethod(value, method);
}

uint64_t decode_boxed_u64(JNIEnv *env, jobject value) {
    jmethodID method = resolve_instance_method(env, value, "longValue", "()J");
    return method == nullptr ? 0 : static_cast<uint64_t>(env->CallLongMethod(value, method));
}

jint call_list_size(JNIEnv *env, jobject value) {
    jmethodID method = resolve_instance_method(env, value, "size", "()I");
    return method == nullptr ? 0 : env->CallIntMethod(value, method);
}

jobject call_list_get(JNIEnv *env, jobject value, jint index) {
    jmethodID method = resolve_instance_method(env, value, "get", "(I)Ljava/lang/Object;");
    return method == nullptr ? nullptr : env->CallObjectMethod(value, method, index);
}

LocationServicesResponse decode_LocationServicesResponse(JNIEnv *env, jobject value, std::deque<std::string> *string_storage) {
    LocationServicesResponse decoded = {};

    decoded.status = static_cast<uint32_t>(call_int_getter(env, value, "getStatus"));

    decoded.is_enabled = call_boolean_getter(env, value, "isEnabled") == JNI_TRUE;


    return decoded;
}

LocationLastKnownResponse decode_LocationLastKnownResponse(JNIEnv *env, jobject value, std::deque<std::string> *string_storage) {
    LocationLastKnownResponse decoded = {};

    decoded.status = static_cast<uint32_t>(call_int_getter(env, value, "getStatus"));

    jobject sample_value = call_object_getter(env, value, "getSample", "()Ldev/destack/runtime/android/module/location/RuntimeLocationSample;");
    decoded.sample = ([&]() -> OptionalLocationSample { if (sample_value == nullptr) { return { .has_value = false, .value = {} }; } return { .has_value = true, .value = decode_LocationSample(env, sample_value, string_storage) }; })();
    if (sample_value != nullptr) { env->DeleteLocalRef(sample_value); }


    return decoded;
}

LocationSample decode_LocationSample(JNIEnv *env, jobject value, std::deque<std::string> *string_storage) {
    LocationSample decoded = {};

    decoded.latitude_degrees = static_cast<double>(call_double_getter(env, value, "getLatitudeDegrees"));

    decoded.longitude_degrees = static_cast<double>(call_double_getter(env, value, "getLongitudeDegrees"));

    decoded.altitude_meters = static_cast<double>(call_double_getter(env, value, "getAltitudeMeters"));

    decoded.horizontal_accuracy_meters = static_cast<double>(call_double_getter(env, value, "getHorizontalAccuracyMeters"));

    decoded.vertical_accuracy_meters = static_cast<double>(call_double_getter(env, value, "getVerticalAccuracyMeters"));

    decoded.speed_meters_per_second = static_cast<double>(call_double_getter(env, value, "getSpeedMetersPerSecond"));

    decoded.heading_degrees = static_cast<double>(call_double_getter(env, value, "getHeadingDegrees"));

    decoded.timestamp_unix_ns = static_cast<uint64_t>(call_long_getter(env, value, "getTimestampUnixNs"));


    return decoded;
}


/// Resolve the location bridge methods from one runtime bridge instance.
bool resolve_bridge_methods(JNIEnv *env, jobject bridge) {
    if (bridge_class == nullptr) {
        jclass local_class = env->GetObjectClass(bridge);
        if (local_class == nullptr) {
            return false;
        }

        bridge_class = reinterpret_cast<jclass>(env->NewGlobalRef(local_class));
        env->DeleteLocalRef(local_class);
        if (bridge_class == nullptr) {
            return false;
        }
    }

    if (servicesEnabled_method == nullptr) {
        servicesEnabled_method = env->GetMethodID(
            bridge_class,
            "locationServicesEnabled",
            "()Ldev/destack/runtime/android/bridge/RuntimeHostLocationServicesEnabledResponse;"
        );
    }

    if (lastKnown_method == nullptr) {
        lastKnown_method = env->GetMethodID(
            bridge_class,
            "locationLastKnown",
            "()Ldev/destack/runtime/android/bridge/RuntimeHostLocationLastKnownResponse;"
        );
    }

    if (watchOpen_method == nullptr) {
        watchOpen_method = env->GetMethodID(
            bridge_class,
            "locationWatchOpen",
            "(Ljava/lang/String;IJDZ)I"
        );
    }

    if (watchClose_method == nullptr) {
        watchClose_method = env->GetMethodID(
            bridge_class,
            "locationWatchClose",
            "(Ljava/lang/String;)I"
        );
    }

    return
        servicesEnabled_method != nullptr &&
        lastKnown_method != nullptr &&
        watchOpen_method != nullptr &&
        watchClose_method != nullptr;
}

}

bool resolve_location_methods(JNIEnv *env, jobject bridge) {
    return resolve_bridge_methods(env, bridge);
}

/// Call the location-services-enabled entrypoint on one registered bridge.
uint32_t call_location_services_enabled(
    JNIEnv *env,
    uint64_t session_handle,
    LocationServicesResponse *response
) {
    jobject bridge = resolve_bridge(env, session_handle);
    if (bridge == nullptr) {
        return HOST_STATUS_NOT_FOUND;
    }


    jobject response_object = env->CallObjectMethod(
        bridge,
        servicesEnabled_method
    );
    if (response_object == nullptr) {
        if (env->ExceptionCheck()) {
            env->ExceptionClear();
        }
        env->DeleteLocalRef(bridge);
        return HOST_STATUS_FAILED;
    }

    clear_result_decode_storage();
    std::deque<std::string> *string_storage = &result_string_storage;

    jint status = call_int_getter(env, response_object, "getStatus");
    if (status == HOST_STATUS_OK) {
        jobject response_value = call_object_getter(env, response_object, "getResponse", "()Ldev/destack/runtime/android/module/location/RuntimeLocationServicesResponse;");
        if (response_value == nullptr) {
            status = HOST_STATUS_FAILED;
        } else {
            *response = decode_LocationServicesResponse(env, response_value, string_storage);
            env->DeleteLocalRef(response_value);
        }
    }
    env->DeleteLocalRef(response_object);
    env->DeleteLocalRef(bridge);

    if (env->ExceptionCheck()) {
        env->ExceptionClear();
        return HOST_STATUS_FAILED;
    }

    return static_cast<uint32_t>(status);
}

/// Call the location-last-known entrypoint on one registered bridge.
uint32_t call_location_last_known(
    JNIEnv *env,
    uint64_t session_handle,
    LocationLastKnownResponse *response
) {
    jobject bridge = resolve_bridge(env, session_handle);
    if (bridge == nullptr) {
        return HOST_STATUS_NOT_FOUND;
    }


    jobject response_object = env->CallObjectMethod(
        bridge,
        lastKnown_method
    );
    if (response_object == nullptr) {
        if (env->ExceptionCheck()) {
            env->ExceptionClear();
        }
        env->DeleteLocalRef(bridge);
        return HOST_STATUS_FAILED;
    }

    clear_result_decode_storage();
    std::deque<std::string> *string_storage = &result_string_storage;

    jint status = call_int_getter(env, response_object, "getStatus");
    if (status == HOST_STATUS_OK) {
        jobject response_value = call_object_getter(env, response_object, "getResponse", "()Ldev/destack/runtime/android/module/location/RuntimeLocationLastKnownResponse;");
        if (response_value == nullptr) {
            status = HOST_STATUS_FAILED;
        } else {
            *response = decode_LocationLastKnownResponse(env, response_value, string_storage);
            env->DeleteLocalRef(response_value);
        }
    }
    env->DeleteLocalRef(response_object);
    env->DeleteLocalRef(bridge);

    if (env->ExceptionCheck()) {
        env->ExceptionClear();
        return HOST_STATUS_FAILED;
    }

    return static_cast<uint32_t>(status);
}

/// Call the location-watch-open entrypoint on one registered bridge.
uint32_t call_location_watch_open(
    JNIEnv *env,
    uint64_t session_handle,
    NativeStringRef watch_id,
    LocationWatchOptions options
) {
    jobject bridge = resolve_bridge(env, session_handle);
    if (bridge == nullptr) {
        return HOST_STATUS_NOT_FOUND;
    }

    jstring watch_id_value = java_string_or_null(env, watch_id);
    jobject accuracy_value = encode_LocationAccuracy(env, options.accuracy);

    jint status = env->CallIntMethod(
        bridge,
        watchOpen_method,
        watch_id_value,
        accuracy_value,
        static_cast<jlong>(options.minimum_interval_ns),
        static_cast<jdouble>(options.minimum_distance_meters),
        options.include_heading ? JNI_TRUE : JNI_FALSE
    );
    if (watch_id_value != nullptr) { env->DeleteLocalRef(watch_id_value); }
    if (accuracy_value != nullptr) { env->DeleteLocalRef(accuracy_value); }
    env->DeleteLocalRef(bridge);

    if (env->ExceptionCheck()) {
        env->ExceptionClear();
        return HOST_STATUS_FAILED;
    }

    return static_cast<uint32_t>(status);
}

/// Call the location-watch-close entrypoint on one registered bridge.
uint32_t call_location_watch_close(
    JNIEnv *env,
    uint64_t session_handle,
    NativeStringRef watch_id
) {
    jobject bridge = resolve_bridge(env, session_handle);
    if (bridge == nullptr) {
        return HOST_STATUS_NOT_FOUND;
    }

    jstring watch_id_value = java_string_or_null(env, watch_id);

    jint status = env->CallIntMethod(
        bridge,
        watchClose_method,
        watch_id_value
    );
    if (watch_id_value != nullptr) { env->DeleteLocalRef(watch_id_value); }
    env->DeleteLocalRef(bridge);

    if (env->ExceptionCheck()) {
        env->ExceptionClear();
        return HOST_STATUS_FAILED;
    }

    return static_cast<uint32_t>(status);
}
