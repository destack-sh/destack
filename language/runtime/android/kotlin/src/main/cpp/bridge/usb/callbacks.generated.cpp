// generated by generate-bindings: do not edit

#include "../types.h"
#include "../jni.h"
#include "../registry.h"
#include "callbacks.generated.h"

#include <deque>
#include <string>
#include <vector>

namespace {

jclass bridge_class = nullptr;
jmethodID deviceList_method = nullptr;
jmethodID watchOpen_method = nullptr;
jmethodID watchClose_method = nullptr;
jmethodID watchRead_method = nullptr;
jmethodID watchTryRead_method = nullptr;
jmethodID open_method = nullptr;

/// Build one JNI string borrowed from one native string reference.
jstring java_string_or_null(JNIEnv *env, NativeStringRef value) {
    if (value.data == nullptr) {
        return nullptr;
    }

    return new_java_string(env, value);
}

jmethodID resolve_constructor(JNIEnv *env, jclass value_class, const char *signature) {
    return env->GetMethodID(value_class, "<init>", signature);
}

thread_local std::deque<std::string> result_string_storage;
thread_local std::deque<std::vector<uint8_t>> result_u8_storage;

void clear_result_decode_storage() {
    result_string_storage.clear();
    result_u8_storage.clear();
}

jint call_list_size(JNIEnv *env, jobject value);
jobject call_list_get(JNIEnv *env, jobject value, jint index);
jint call_int_getter(JNIEnv *env, jobject value, const char *name);

jintArray encode_u8_slice(JNIEnv *env, NativeU8Slice values) {
    jintArray array = env->NewIntArray(static_cast<jsize>(values.len));
    if (array == nullptr) {
        return nullptr;
    }

    std::vector<jint> buffer(static_cast<size_t>(values.len));
    for (uint32_t index = 0; index < values.len; index += 1) {
        buffer[static_cast<size_t>(index)] = static_cast<jint>(values.data[index]);
    }

    env->SetIntArrayRegion(array, 0, static_cast<jsize>(values.len), buffer.data());
    return array;
}

NativeU8Slice decode_u8_list(JNIEnv *env, jobject list) {
    if (list == nullptr) {
        return NativeU8Slice {
            .data = nullptr,
            .len = 0,
        };
    }

    jint size = call_list_size(env, list);
    result_u8_storage.emplace_back();
    auto &storage = result_u8_storage.back();
    storage.reserve(static_cast<size_t>(size));

    for (jint index = 0; index < size; index += 1) {
        jobject value = call_list_get(env, list, index);
        jint resolved = call_int_getter(env, value, "intValue");
        storage.push_back(static_cast<uint8_t>(resolved));
        env->DeleteLocalRef(value);
    }

    return NativeU8Slice {
        .data = storage.data(),
        .len = static_cast<uint32_t>(storage.size()),
    };
}

jobject encode_AndroidHostUsbDeviceDescriptorHeader(JNIEnv *env, AndroidHostUsbDeviceDescriptorHeader value);
jobjectArray encode_AndroidHostUsbDeviceDescriptorHeaderSlice(JNIEnv *env, AndroidHostUsbDeviceDescriptorHeaderSlice value);

jobject encode_AndroidHostUsbDeviceDescriptorHeader(JNIEnv *env, AndroidHostUsbDeviceDescriptorHeader value) {
    jclass value_class = env->FindClass("dev/destack/runtime/android/module/usb/RuntimeAndroidHostUsbDeviceDescriptorHeader");
    if (value_class == nullptr) {
        return nullptr;
    }

    jmethodID constructor = resolve_constructor(env, value_class, "(IIIIIIIIIIIIIIIIIIIIIII)V");
    if (constructor == nullptr) {
        env->DeleteLocalRef(value_class);
        return nullptr;
    }


    jobject result = env->NewObject(
        value_class,
        constructor,
        static_cast<jint>(value.id_offset),
        static_cast<jint>(value.id_len),
        static_cast<jint>(value.manufacturer_offset),
        static_cast<jint>(value.manufacturer_len),
        static_cast<jint>(value.product_offset),
        static_cast<jint>(value.product_len),
        static_cast<jint>(value.serial_number_offset),
        static_cast<jint>(value.serial_number_len),
        static_cast<jint>(value.port_path_offset),
        static_cast<jint>(value.port_path_len),
        static_cast<jint>(value.usb_version_bcd),
        static_cast<jint>(value.has_usb_version_bcd),
        static_cast<jint>(value.device_version_bcd),
        static_cast<jint>(value.has_device_version_bcd),
        static_cast<jint>(value.vendor_id),
        static_cast<jint>(value.product_id),
        static_cast<jint>(value.class_code),
        static_cast<jint>(value.subclass_code),
        static_cast<jint>(value.protocol_code),
        static_cast<jint>(value.speed),
        static_cast<jint>(value.has_speed),
        static_cast<jint>(value.bus_number),
        static_cast<jint>(value.has_bus_number)
    );

    env->DeleteLocalRef(value_class);

    return result;
}

jobjectArray encode_AndroidHostUsbDeviceDescriptorHeaderSlice(JNIEnv *env, AndroidHostUsbDeviceDescriptorHeaderSlice value) {
    jclass element_class = env->FindClass("dev/destack/runtime/android/module/usb/RuntimeAndroidHostUsbDeviceDescriptorHeader");
    if (element_class == nullptr) {
        return nullptr;
    }

    jobjectArray array = env->NewObjectArray(static_cast<jsize>(value.len), element_class, nullptr);
    env->DeleteLocalRef(element_class);
    if (array == nullptr) {
        return nullptr;
    }

    for (uint32_t index = 0; index < value.len; index += 1) {
        jobject element = encode_AndroidHostUsbDeviceDescriptorHeader(env, value.data[index]);
        if (element == nullptr) {
            env->DeleteLocalRef(array);
            return nullptr;
        }

        env->SetObjectArrayElement(array, static_cast<jsize>(index), element);
        env->DeleteLocalRef(element);
    }

    return array;
}


AndroidHostUsbHotplugEventHeader decode_AndroidHostUsbHotplugEventHeader(JNIEnv *env, jobject value, std::deque<std::string> *string_storage);
AndroidHostUsbDeviceDescriptorHeader decode_AndroidHostUsbDeviceDescriptorHeader(JNIEnv *env, jobject value, std::deque<std::string> *string_storage);

jmethodID resolve_instance_method(
    JNIEnv *env,
    jobject value,
    const char *name,
    const char *signature
) {
    jclass value_class = env->GetObjectClass(value);
    if (value_class == nullptr) {
        return nullptr;
    }

    jmethodID method = env->GetMethodID(value_class, name, signature);
    env->DeleteLocalRef(value_class);

    return method;
}

jobject call_object_getter(
    JNIEnv *env,
    jobject value,
    const char *name,
    const char *signature
) {
    jmethodID method = resolve_instance_method(env, value, name, signature);
    if (method == nullptr) {
        return nullptr;
    }

    return env->CallObjectMethod(value, method);
}

jint call_int_getter(JNIEnv *env, jobject value, const char *name) {
    jmethodID method = resolve_instance_method(env, value, name, "()I");
    return method == nullptr ? 0 : env->CallIntMethod(value, method);
}

jlong call_long_getter(JNIEnv *env, jobject value, const char *name) {
    jmethodID method = resolve_instance_method(env, value, name, "()J");
    return method == nullptr ? 0 : env->CallLongMethod(value, method);
}

jdouble call_double_getter(JNIEnv *env, jobject value, const char *name) {
    jmethodID method = resolve_instance_method(env, value, name, "()D");
    return method == nullptr ? 0.0 : env->CallDoubleMethod(value, method);
}

jboolean call_boolean_getter(JNIEnv *env, jobject value, const char *name) {
    jmethodID method = resolve_instance_method(env, value, name, "()Z");
    return method == nullptr ? JNI_FALSE : env->CallBooleanMethod(value, method);
}

uint64_t decode_boxed_u64(JNIEnv *env, jobject value) {
    jmethodID method = resolve_instance_method(env, value, "longValue", "()J");
    return method == nullptr ? 0 : static_cast<uint64_t>(env->CallLongMethod(value, method));
}

jint call_list_size(JNIEnv *env, jobject value) {
    jmethodID method = resolve_instance_method(env, value, "size", "()I");
    return method == nullptr ? 0 : env->CallIntMethod(value, method);
}

jobject call_list_get(JNIEnv *env, jobject value, jint index) {
    jmethodID method = resolve_instance_method(env, value, "get", "(I)Ljava/lang/Object;");
    return method == nullptr ? nullptr : env->CallObjectMethod(value, method, index);
}

AndroidHostUsbHotplugEventHeader decode_AndroidHostUsbHotplugEventHeader(JNIEnv *env, jobject value, std::deque<std::string> *string_storage) {
    AndroidHostUsbHotplugEventHeader decoded = {};

    decoded.timestamp_ns = static_cast<uint64_t>(call_long_getter(env, value, "getTimestampNs"));

    decoded.kind = static_cast<uint32_t>(call_int_getter(env, value, "getKind"));

    jobject descriptor_value = call_object_getter(env, value, "getDescriptor", "()Ldev/destack/runtime/android/module/usb/RuntimeAndroidHostUsbDeviceDescriptorHeader;");
    decoded.descriptor = decode_AndroidHostUsbDeviceDescriptorHeader(env, descriptor_value, string_storage);
    if (descriptor_value != nullptr) { env->DeleteLocalRef(descriptor_value); }


    return decoded;
}

AndroidHostUsbDeviceDescriptorHeader decode_AndroidHostUsbDeviceDescriptorHeader(JNIEnv *env, jobject value, std::deque<std::string> *string_storage) {
    AndroidHostUsbDeviceDescriptorHeader decoded = {};

    decoded.id_offset = static_cast<uint32_t>(call_int_getter(env, value, "getIdOffset"));

    decoded.id_len = static_cast<uint32_t>(call_int_getter(env, value, "getIdLen"));

    decoded.manufacturer_offset = static_cast<uint32_t>(call_int_getter(env, value, "getManufacturerOffset"));

    decoded.manufacturer_len = static_cast<uint32_t>(call_int_getter(env, value, "getManufacturerLen"));

    decoded.product_offset = static_cast<uint32_t>(call_int_getter(env, value, "getProductOffset"));

    decoded.product_len = static_cast<uint32_t>(call_int_getter(env, value, "getProductLen"));

    decoded.serial_number_offset = static_cast<uint32_t>(call_int_getter(env, value, "getSerialNumberOffset"));

    decoded.serial_number_len = static_cast<uint32_t>(call_int_getter(env, value, "getSerialNumberLen"));

    decoded.port_path_offset = static_cast<uint32_t>(call_int_getter(env, value, "getPortPathOffset"));

    decoded.port_path_len = static_cast<uint32_t>(call_int_getter(env, value, "getPortPathLen"));

    decoded.usb_version_bcd = static_cast<uint16_t>(call_int_getter(env, value, "getUsbVersionBcd"));

    decoded.has_usb_version_bcd = static_cast<uint32_t>(call_int_getter(env, value, "getHasUsbVersionBcd"));

    decoded.device_version_bcd = static_cast<uint16_t>(call_int_getter(env, value, "getDeviceVersionBcd"));

    decoded.has_device_version_bcd = static_cast<uint32_t>(call_int_getter(env, value, "getHasDeviceVersionBcd"));

    decoded.vendor_id = static_cast<uint16_t>(call_int_getter(env, value, "getVendorId"));

    decoded.product_id = static_cast<uint16_t>(call_int_getter(env, value, "getProductId"));

    decoded.class_code = static_cast<uint8_t>(call_int_getter(env, value, "getClassCode"));

    decoded.subclass_code = static_cast<uint8_t>(call_int_getter(env, value, "getSubclassCode"));

    decoded.protocol_code = static_cast<uint8_t>(call_int_getter(env, value, "getProtocolCode"));

    decoded.speed = static_cast<uint32_t>(call_int_getter(env, value, "getSpeed"));

    decoded.has_speed = static_cast<uint32_t>(call_int_getter(env, value, "getHasSpeed"));

    decoded.bus_number = static_cast<uint8_t>(call_int_getter(env, value, "getBusNumber"));

    decoded.has_bus_number = static_cast<uint32_t>(call_int_getter(env, value, "getHasBusNumber"));


    return decoded;
}


/// Resolve the usb bridge methods from one runtime bridge instance.
bool resolve_bridge_methods(JNIEnv *env, jobject bridge) {
    if (bridge_class == nullptr) {
        jclass local_class = env->GetObjectClass(bridge);
        if (local_class == nullptr) {
            return false;
        }

        bridge_class = reinterpret_cast<jclass>(env->NewGlobalRef(local_class));
        env->DeleteLocalRef(local_class);
        if (bridge_class == nullptr) {
            return false;
        }
    }

    if (deviceList_method == nullptr) {
        deviceList_method = env->GetMethodID(
            bridge_class,
            "usbDeviceList",
            "([Ldev/destack/runtime/android/module/usb/RuntimeAndroidHostUsbDeviceDescriptorHeader;[I[I)Ldev/destack/runtime/android/module/usb/RuntimeHostUsbDeviceListResponse;"
        );
    }

    if (watchOpen_method == nullptr) {
        watchOpen_method = env->GetMethodID(
            bridge_class,
            "usbWatchOpen",
            "()Ldev/destack/runtime/android/module/usb/RuntimeHostUsbWatchOpenResponse;"
        );
    }

    if (watchClose_method == nullptr) {
        watchClose_method = env->GetMethodID(
            bridge_class,
            "usbWatchClose",
            "(J)I"
        );
    }

    if (watchRead_method == nullptr) {
        watchRead_method = env->GetMethodID(
            bridge_class,
            "usbWatchRead",
            "(JJ[I[I)Ldev/destack/runtime/android/module/usb/RuntimeHostUsbWatchReadResponse;"
        );
    }

    if (watchTryRead_method == nullptr) {
        watchTryRead_method = env->GetMethodID(
            bridge_class,
            "usbWatchTryRead",
            "(J[I[I)Ldev/destack/runtime/android/module/usb/RuntimeHostUsbWatchTryReadResponse;"
        );
    }

    if (open_method == nullptr) {
        open_method = env->GetMethodID(
            bridge_class,
            "usbOpen",
            "(Ljava/lang/String;)Ldev/destack/runtime/android/module/usb/RuntimeHostUsbOpenResponse;"
        );
    }

    return
        deviceList_method != nullptr &&
        watchOpen_method != nullptr &&
        watchClose_method != nullptr &&
        watchRead_method != nullptr &&
        watchTryRead_method != nullptr &&
        open_method != nullptr;
}

}

bool resolve_usb_methods(JNIEnv *env, jobject bridge) {
    return resolve_bridge_methods(env, bridge);
}

/// Call the usb-device-list entrypoint on one registered bridge.
uint32_t call_usb_device_list(
    JNIEnv *env,
    uint64_t session_handle,
    AndroidHostUsbDeviceDescriptorHeaderSlice devices,
    uint32_t *device_count_written,
    NativeU8Slice string_bytes,
    uint32_t *string_bytes_written,
    NativeU8Slice port_bytes,
    uint32_t *port_bytes_written
) {
    jobject bridge = resolve_bridge(env, session_handle);
    if (bridge == nullptr) {
        return HOST_STATUS_NOT_FOUND;
    }

    jobjectArray devices_value = encode_AndroidHostUsbDeviceDescriptorHeaderSlice(env, devices);
    if (devices_value == nullptr) {
        if (env->ExceptionCheck()) {
            env->ExceptionClear();
        }
        env->DeleteLocalRef(bridge);
        return HOST_STATUS_FAILED;
    }
    jintArray string_bytes_value = encode_u8_slice(env, string_bytes);
    if (string_bytes_value == nullptr) {
        if (devices_value != nullptr) { env->DeleteLocalRef(devices_value); }
        if (env->ExceptionCheck()) {
            env->ExceptionClear();
        }
        env->DeleteLocalRef(bridge);
        return HOST_STATUS_FAILED;
    }
    jintArray port_bytes_value = encode_u8_slice(env, port_bytes);
    if (port_bytes_value == nullptr) {
        if (devices_value != nullptr) { env->DeleteLocalRef(devices_value); }
        if (string_bytes_value != nullptr) { env->DeleteLocalRef(string_bytes_value); }
        if (env->ExceptionCheck()) {
            env->ExceptionClear();
        }
        env->DeleteLocalRef(bridge);
        return HOST_STATUS_FAILED;
    }

    jobject response_object = env->CallObjectMethod(
        bridge,
        deviceList_method,
        devices_value,
        string_bytes_value,
        port_bytes_value
    );
    if (response_object == nullptr) {
        if (devices_value != nullptr) { env->DeleteLocalRef(devices_value); }
        if (string_bytes_value != nullptr) { env->DeleteLocalRef(string_bytes_value); }
        if (port_bytes_value != nullptr) { env->DeleteLocalRef(port_bytes_value); }
        if (env->ExceptionCheck()) {
            env->ExceptionClear();
        }
        env->DeleteLocalRef(bridge);
        return HOST_STATUS_FAILED;
    }

    jint status = call_int_getter(env, response_object, "getStatus");
    if (status == HOST_STATUS_OK) {
        *device_count_written = static_cast<uint32_t>(call_int_getter(env, response_object, "getDeviceCountWritten"));
        *string_bytes_written = static_cast<uint32_t>(call_int_getter(env, response_object, "getStringBytesWritten"));
        *port_bytes_written = static_cast<uint32_t>(call_int_getter(env, response_object, "getPortBytesWritten"));
    }
    if (devices_value != nullptr) { env->DeleteLocalRef(devices_value); }
    if (string_bytes_value != nullptr) { env->DeleteLocalRef(string_bytes_value); }
    if (port_bytes_value != nullptr) { env->DeleteLocalRef(port_bytes_value); }
    env->DeleteLocalRef(response_object);
    env->DeleteLocalRef(bridge);

    if (env->ExceptionCheck()) {
        env->ExceptionClear();
        return HOST_STATUS_FAILED;
    }

    return static_cast<uint32_t>(status);
}

/// Call the usb-watch-open entrypoint on one registered bridge.
uint32_t call_usb_watch_open(
    JNIEnv *env,
    uint64_t session_handle,
    uint64_t *watch_id
) {
    jobject bridge = resolve_bridge(env, session_handle);
    if (bridge == nullptr) {
        return HOST_STATUS_NOT_FOUND;
    }


    jobject response_object = env->CallObjectMethod(
        bridge,
        watchOpen_method
    );
    if (response_object == nullptr) {
        if (env->ExceptionCheck()) {
            env->ExceptionClear();
        }
        env->DeleteLocalRef(bridge);
        return HOST_STATUS_FAILED;
    }

    jint status = call_int_getter(env, response_object, "getStatus");
    if (status == HOST_STATUS_OK) {
        *watch_id = static_cast<uint64_t>(call_long_getter(env, response_object, "getWatchId"));
    }
    env->DeleteLocalRef(response_object);
    env->DeleteLocalRef(bridge);

    if (env->ExceptionCheck()) {
        env->ExceptionClear();
        return HOST_STATUS_FAILED;
    }

    return static_cast<uint32_t>(status);
}

/// Call the usb-watch-close entrypoint on one registered bridge.
uint32_t call_usb_watch_close(
    JNIEnv *env,
    uint64_t session_handle,
    uint64_t watch_id
) {
    jobject bridge = resolve_bridge(env, session_handle);
    if (bridge == nullptr) {
        return HOST_STATUS_NOT_FOUND;
    }


    jint status = env->CallIntMethod(
        bridge,
        watchClose_method,
        static_cast<jlong>(watch_id)
    );
    env->DeleteLocalRef(bridge);

    if (env->ExceptionCheck()) {
        env->ExceptionClear();
        return HOST_STATUS_FAILED;
    }

    return static_cast<uint32_t>(status);
}

/// Call the usb-watch-read entrypoint on one registered bridge.
uint32_t call_usb_watch_read(
    JNIEnv *env,
    uint64_t session_handle,
    uint64_t watch_id,
    uint64_t timeout_ns,
    AndroidHostUsbHotplugEventHeader *event,
    NativeU8Slice string_bytes,
    uint32_t *string_bytes_written,
    NativeU8Slice port_bytes,
    uint32_t *port_bytes_written
) {
    jobject bridge = resolve_bridge(env, session_handle);
    if (bridge == nullptr) {
        return HOST_STATUS_NOT_FOUND;
    }

    jintArray string_bytes_value = encode_u8_slice(env, string_bytes);
    if (string_bytes_value == nullptr) {
        if (env->ExceptionCheck()) {
            env->ExceptionClear();
        }
        env->DeleteLocalRef(bridge);
        return HOST_STATUS_FAILED;
    }
    jintArray port_bytes_value = encode_u8_slice(env, port_bytes);
    if (port_bytes_value == nullptr) {
        if (string_bytes_value != nullptr) { env->DeleteLocalRef(string_bytes_value); }
        if (env->ExceptionCheck()) {
            env->ExceptionClear();
        }
        env->DeleteLocalRef(bridge);
        return HOST_STATUS_FAILED;
    }

    jobject response_object = env->CallObjectMethod(
        bridge,
        watchRead_method,
        static_cast<jlong>(watch_id),
        static_cast<jlong>(timeout_ns),
        string_bytes_value,
        port_bytes_value
    );
    if (response_object == nullptr) {
        if (string_bytes_value != nullptr) { env->DeleteLocalRef(string_bytes_value); }
        if (port_bytes_value != nullptr) { env->DeleteLocalRef(port_bytes_value); }
        if (env->ExceptionCheck()) {
            env->ExceptionClear();
        }
        env->DeleteLocalRef(bridge);
        return HOST_STATUS_FAILED;
    }

    clear_result_decode_storage();
    std::deque<std::string> *string_storage = &result_string_storage;

    jint status = call_int_getter(env, response_object, "getStatus");
    if (status == HOST_STATUS_OK) {
        jobject event_value = call_object_getter(env, response_object, "getEvent", "()Ldev/destack/runtime/android/module/usb/RuntimeAndroidHostUsbHotplugEventHeader;");
        if (event_value == nullptr) {
            status = HOST_STATUS_FAILED;
        } else {
            *event = decode_AndroidHostUsbHotplugEventHeader(env, event_value, string_storage);
            env->DeleteLocalRef(event_value);
        }
        *string_bytes_written = static_cast<uint32_t>(call_int_getter(env, response_object, "getStringBytesWritten"));
        *port_bytes_written = static_cast<uint32_t>(call_int_getter(env, response_object, "getPortBytesWritten"));
    }
    if (string_bytes_value != nullptr) { env->DeleteLocalRef(string_bytes_value); }
    if (port_bytes_value != nullptr) { env->DeleteLocalRef(port_bytes_value); }
    env->DeleteLocalRef(response_object);
    env->DeleteLocalRef(bridge);

    if (env->ExceptionCheck()) {
        env->ExceptionClear();
        return HOST_STATUS_FAILED;
    }

    return static_cast<uint32_t>(status);
}

/// Call the usb-watch-try-read entrypoint on one registered bridge.
uint32_t call_usb_watch_try_read(
    JNIEnv *env,
    uint64_t session_handle,
    uint64_t watch_id,
    AndroidHostUsbHotplugEventHeader *event,
    NativeU8Slice string_bytes,
    uint32_t *string_bytes_written,
    NativeU8Slice port_bytes,
    uint32_t *port_bytes_written
) {
    jobject bridge = resolve_bridge(env, session_handle);
    if (bridge == nullptr) {
        return HOST_STATUS_NOT_FOUND;
    }

    jintArray string_bytes_value = encode_u8_slice(env, string_bytes);
    if (string_bytes_value == nullptr) {
        if (env->ExceptionCheck()) {
            env->ExceptionClear();
        }
        env->DeleteLocalRef(bridge);
        return HOST_STATUS_FAILED;
    }
    jintArray port_bytes_value = encode_u8_slice(env, port_bytes);
    if (port_bytes_value == nullptr) {
        if (string_bytes_value != nullptr) { env->DeleteLocalRef(string_bytes_value); }
        if (env->ExceptionCheck()) {
            env->ExceptionClear();
        }
        env->DeleteLocalRef(bridge);
        return HOST_STATUS_FAILED;
    }

    jobject response_object = env->CallObjectMethod(
        bridge,
        watchTryRead_method,
        static_cast<jlong>(watch_id),
        string_bytes_value,
        port_bytes_value
    );
    if (response_object == nullptr) {
        if (string_bytes_value != nullptr) { env->DeleteLocalRef(string_bytes_value); }
        if (port_bytes_value != nullptr) { env->DeleteLocalRef(port_bytes_value); }
        if (env->ExceptionCheck()) {
            env->ExceptionClear();
        }
        env->DeleteLocalRef(bridge);
        return HOST_STATUS_FAILED;
    }

    clear_result_decode_storage();
    std::deque<std::string> *string_storage = &result_string_storage;

    jint status = call_int_getter(env, response_object, "getStatus");
    if (status == HOST_STATUS_OK) {
        jobject event_value = call_object_getter(env, response_object, "getEvent", "()Ldev/destack/runtime/android/module/usb/RuntimeAndroidHostUsbHotplugEventHeader;");
        if (event_value == nullptr) {
            status = HOST_STATUS_FAILED;
        } else {
            *event = decode_AndroidHostUsbHotplugEventHeader(env, event_value, string_storage);
            env->DeleteLocalRef(event_value);
        }
        *string_bytes_written = static_cast<uint32_t>(call_int_getter(env, response_object, "getStringBytesWritten"));
        *port_bytes_written = static_cast<uint32_t>(call_int_getter(env, response_object, "getPortBytesWritten"));
    }
    if (string_bytes_value != nullptr) { env->DeleteLocalRef(string_bytes_value); }
    if (port_bytes_value != nullptr) { env->DeleteLocalRef(port_bytes_value); }
    env->DeleteLocalRef(response_object);
    env->DeleteLocalRef(bridge);

    if (env->ExceptionCheck()) {
        env->ExceptionClear();
        return HOST_STATUS_FAILED;
    }

    return static_cast<uint32_t>(status);
}

/// Call the usb-open entrypoint on one registered bridge.
uint32_t call_usb_open(
    JNIEnv *env,
    uint64_t session_handle,
    NativeStringRef id,
    int32_t *file_descriptor
) {
    jobject bridge = resolve_bridge(env, session_handle);
    if (bridge == nullptr) {
        return HOST_STATUS_NOT_FOUND;
    }

    jstring id_value = java_string_or_null(env, id);

    jobject response_object = env->CallObjectMethod(
        bridge,
        open_method,
        id_value
    );
    if (response_object == nullptr) {
        if (id_value != nullptr) { env->DeleteLocalRef(id_value); }
        if (env->ExceptionCheck()) {
            env->ExceptionClear();
        }
        env->DeleteLocalRef(bridge);
        return HOST_STATUS_FAILED;
    }

    jint status = call_int_getter(env, response_object, "getStatus");
    if (status == HOST_STATUS_OK) {
        *file_descriptor = static_cast<int32_t>(call_int_getter(env, response_object, "getFileDescriptor"));
    }
    if (id_value != nullptr) { env->DeleteLocalRef(id_value); }
    env->DeleteLocalRef(response_object);
    env->DeleteLocalRef(bridge);

    if (env->ExceptionCheck()) {
        env->ExceptionClear();
        return HOST_STATUS_FAILED;
    }

    return static_cast<uint32_t>(status);
}
