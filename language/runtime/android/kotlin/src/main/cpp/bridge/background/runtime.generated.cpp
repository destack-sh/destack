// generated by generate-bindings: do not edit

#include "../types.h"
#include "../jni.h"
#include "../loader.h"
#include "callbacks.generated.h"
#include "runtime.generated.h"

#include <deque>
#include <string>
#include <vector>

namespace {

/// Convert one host status code into one runtime status.
RuntimeStatus runtime_status_from_code(uint32_t code) {
    RuntimeStatus status = {
        .code = code,
        .error_id = 0,
    };

    return status;
}

/// Borrow one runtime string reference from one JNI string.
NativeStringRef native_string_ref_from_jstring(JNIEnv *env, jstring value) {
    if (value == nullptr) {
        return {
            .data = nullptr,
            .len = 0,
        };
    }

    const char *chars = env->GetStringUTFChars(value, nullptr);
    jsize length = env->GetStringUTFLength(value);

    return {
        .data = reinterpret_cast<const uint8_t *>(chars),
        .len = static_cast<uint32_t>(length),
    };
}

/// Release one runtime string reference borrowed from one JNI string.
void release_native_string_ref(JNIEnv *env, jstring value, NativeStringRef ref) {
    if (value == nullptr || ref.data == nullptr) {
        return;
    }

    env->ReleaseStringUTFChars(
        value,
        reinterpret_cast<const char *>(ref.data)
    );
}

}

/// Forward the `status` background request through one attached bridge session.
uint32_t call_background_status_for_session(
    uint64_t session_handle,
    HostBackgroundStatusResponse *response
) {
    JNIEnv *env = nullptr;
    bool did_attach_thread = false;

    if (!resolve_jni_env(&env, &did_attach_thread)) {
        return HOST_STATUS_FAILED;
    }

    uint32_t status = call_background_status(env, session_handle, response);

    detach_jni_thread(did_attach_thread);

    return status;
}

/// Forward the `list` background request through one attached bridge session.
uint32_t call_background_list_for_session(
    uint64_t session_handle,
    HostBackgroundListResponse *response
) {
    JNIEnv *env = nullptr;
    bool did_attach_thread = false;

    if (!resolve_jni_env(&env, &did_attach_thread)) {
        return HOST_STATUS_FAILED;
    }

    uint32_t status = call_background_list(env, session_handle, response);

    detach_jni_thread(did_attach_thread);

    return status;
}

/// Forward the `register_task` background request through one attached bridge session.
uint32_t call_background_register_task_for_session(
    uint64_t session_handle,
    HostBackgroundTaskOptions request
) {
    JNIEnv *env = nullptr;
    bool did_attach_thread = false;

    if (!resolve_jni_env(&env, &did_attach_thread)) {
        return HOST_STATUS_FAILED;
    }

    uint32_t status = call_background_register_task(env, session_handle, request);

    detach_jni_thread(did_attach_thread);

    return status;
}

/// Forward the `unregister` background request through one attached bridge session.
uint32_t call_background_unregister_for_session(
    uint64_t session_handle,
    HostBackgroundUnregisterRequest request
) {
    JNIEnv *env = nullptr;
    bool did_attach_thread = false;

    if (!resolve_jni_env(&env, &did_attach_thread)) {
        return HOST_STATUS_FAILED;
    }

    uint32_t status = call_background_unregister(env, session_handle, request);

    detach_jni_thread(did_attach_thread);

    return status;
}

/// Forward the `trigger_test` background request through one attached bridge session.
uint32_t call_background_trigger_test_for_session(
    uint64_t session_handle,
    HostBackgroundTriggerTestRequest request,
    HostBackgroundTriggerTestResponse *response
) {
    JNIEnv *env = nullptr;
    bool did_attach_thread = false;

    if (!resolve_jni_env(&env, &did_attach_thread)) {
        return HOST_STATUS_FAILED;
    }

    uint32_t status = call_background_trigger_test(env, session_handle, request, response);

    detach_jni_thread(did_attach_thread);

    return status;
}

/// Forward the `complete` background request through one attached bridge session.
uint32_t call_background_complete_for_session(
    uint64_t session_handle,
    HostBackgroundCompleteRequest request
) {
    JNIEnv *env = nullptr;
    bool did_attach_thread = false;

    if (!resolve_jni_env(&env, &did_attach_thread)) {
        return HOST_STATUS_FAILED;
    }

    uint32_t status = call_background_complete(env, session_handle, request);

    detach_jni_thread(did_attach_thread);

    return status;
}

/// Build one Android host background callback bundle.
AndroidHostBackgroundCallbacks make_background_callbacks() {
    return AndroidHostBackgroundCallbacks {
        .status = call_background_status_for_session,
        .list = call_background_list_for_session,
        .register_task = call_background_register_task_for_session,
        .unregister = call_background_unregister_for_session,
        .trigger_test = call_background_trigger_test_for_session,
        .complete = call_background_complete_for_session,
    };
}

/// Deliver one background event into one runtime session.
RuntimeStatus send_notify_background_event(
    uint64_t session_handle,
    HostBackgroundEvent event
) {
    RuntimeBindings bindings = {};
    RuntimeStatus status = runtime_status_from_code(resolve_runtime_bindings(&bindings));
    if (status.code != HOST_STATUS_OK) {
        return status;
    }

    return bindings.notify_background_event(session_handle, event);
}
