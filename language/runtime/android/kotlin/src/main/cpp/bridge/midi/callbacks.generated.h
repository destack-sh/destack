// generated by generate-bindings: do not edit

#ifndef DESTACK_RUNTIME_ANDROID_BRIDGE_MIDI_METHODS_H
#define DESTACK_RUNTIME_ANDROID_BRIDGE_MIDI_METHODS_H

#include "../types.h"

/// Resolve the midi bridge methods from one runtime bridge instance.
bool resolve_midi_methods(JNIEnv *env, jobject bridge);
/// Call the midi-describe-backend entrypoint on one registered bridge.
uint32_t call_midi_describe_backend(
    JNIEnv *env,
    uint64_t session_handle,
    uint64_t *capability_flags,
    uint32_t *supported_data_formats,
    uint32_t *supported_protocols
);

/// Call the midi-input-port-list entrypoint on one registered bridge.
uint32_t call_midi_input_port_list(
    JNIEnv *env,
    uint64_t session_handle,
    uint32_t flags,
    AndroidHostMidiPortDescriptorHeaderSlice headers,
    uint32_t *header_count_written,
    NativeU8Slice string_bytes,
    uint32_t *string_bytes_written
);

/// Call the midi-output-port-list entrypoint on one registered bridge.
uint32_t call_midi_output_port_list(
    JNIEnv *env,
    uint64_t session_handle,
    uint32_t flags,
    AndroidHostMidiPortDescriptorHeaderSlice headers,
    uint32_t *header_count_written,
    NativeU8Slice string_bytes,
    uint32_t *string_bytes_written
);

/// Call the midi-input-port-open entrypoint on one registered bridge.
uint32_t call_midi_input_port_open(
    JNIEnv *env,
    uint64_t session_handle,
    NativeStringRef id,
    uint32_t data_format,
    uint32_t protocol,
    uint32_t queue_capacity,
    AndroidHostMidiOpenedPortHeader *opened_port,
    NativeU8Slice string_bytes,
    uint32_t *string_bytes_written
);

/// Call the midi-output-port-open entrypoint on one registered bridge.
uint32_t call_midi_output_port_open(
    JNIEnv *env,
    uint64_t session_handle,
    NativeStringRef id,
    uint32_t data_format,
    uint32_t protocol,
    AndroidHostMidiOpenedPortHeader *opened_port,
    NativeU8Slice string_bytes,
    uint32_t *string_bytes_written
);

/// Call the midi-input-virtual-create entrypoint on one registered bridge.
uint32_t call_midi_input_virtual_create(
    JNIEnv *env,
    uint64_t session_handle,
    NativeStringRef name,
    NativeStringRef manufacturer,
    NativeStringRef model,
    NativeStringRef version,
    uint32_t data_format,
    uint32_t protocol,
    uint32_t queue_capacity,
    AndroidHostMidiOpenedPortHeader *opened_port,
    NativeU8Slice string_bytes,
    uint32_t *string_bytes_written
);

/// Call the midi-output-virtual-create entrypoint on one registered bridge.
uint32_t call_midi_output_virtual_create(
    JNIEnv *env,
    uint64_t session_handle,
    NativeStringRef name,
    NativeStringRef manufacturer,
    NativeStringRef model,
    NativeStringRef version,
    uint32_t data_format,
    uint32_t protocol,
    AndroidHostMidiOpenedPortHeader *opened_port,
    NativeU8Slice string_bytes,
    uint32_t *string_bytes_written
);

/// Call the midi-input-port-close entrypoint on one registered bridge.
uint32_t call_midi_input_port_close(
    JNIEnv *env,
    uint64_t session_handle,
    uint64_t session_id
);

/// Call the midi-output-port-close entrypoint on one registered bridge.
uint32_t call_midi_output_port_close(
    JNIEnv *env,
    uint64_t session_handle,
    uint64_t session_id
);

/// Call the midi-input-read entrypoint on one registered bridge.
uint32_t call_midi_input_read(
    JNIEnv *env,
    uint64_t session_handle,
    uint64_t session_id,
    uint32_t max_records,
    uint64_t timeout_ns,
    AndroidHostMidiInputRecordHeaderSlice headers,
    uint32_t *record_count_written,
    NativeU8Slice blob_bytes,
    uint32_t *blob_bytes_written
);

/// Call the midi-event-open entrypoint on one registered bridge.
uint32_t call_midi_event_open(
    JNIEnv *env,
    uint64_t session_handle,
    uint32_t flags,
    uint32_t direction_mask,
    uint64_t *session_id
);

/// Call the midi-event-read entrypoint on one registered bridge.
uint32_t call_midi_event_read(
    JNIEnv *env,
    uint64_t session_handle,
    uint64_t session_id,
    uint32_t max_events,
    uint64_t timeout_ns,
    AndroidHostMidiEventHeaderSlice headers,
    uint32_t *event_count_written,
    NativeU8Slice string_bytes,
    uint32_t *string_bytes_written
);

/// Call the midi-event-close entrypoint on one registered bridge.
uint32_t call_midi_event_close(
    JNIEnv *env,
    uint64_t session_handle,
    uint64_t session_id
);

/// Call the midi-output-write entrypoint on one registered bridge.
uint32_t call_midi_output_write(
    JNIEnv *env,
    uint64_t session_handle,
    uint64_t session_id,
    AndroidHostMidiOutputRecordHeaderSlice headers,
    uint32_t record_count,
    NativeU8Slice blob_bytes,
    uint32_t *records_written
);

#endif
