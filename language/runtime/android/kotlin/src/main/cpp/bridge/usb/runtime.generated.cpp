// generated by generate-bindings: do not edit

#include "../types.h"
#include "../jni.h"
#include "../loader.h"
#include "callbacks.generated.h"
#include "runtime.generated.h"

#include <deque>
#include <string>
#include <vector>

namespace {

/// Convert one host status code into one runtime status.
RuntimeStatus runtime_status_from_code(uint32_t code) {
    RuntimeStatus status = {
        .code = code,
        .error_id = 0,
    };

    return status;
}

}

/// Forward the `device_list` usb request through one attached bridge session.
uint32_t call_usb_device_list_for_session(
    uint64_t session_handle,
    AndroidHostUsbDeviceDescriptorHeaderSlice devices,
    uint32_t *device_count_written,
    NativeU8Slice string_bytes,
    uint32_t *string_bytes_written,
    NativeU8Slice port_bytes,
    uint32_t *port_bytes_written
) {
    JNIEnv *env = nullptr;
    bool did_attach_thread = false;

    if (!resolve_jni_env(&env, &did_attach_thread)) {
        return HOST_STATUS_FAILED;
    }

    uint32_t status = call_usb_device_list(env, session_handle, devices, device_count_written, string_bytes, string_bytes_written, port_bytes, port_bytes_written);

    detach_jni_thread(did_attach_thread);

    return status;
}

/// Forward the `watch_open` usb request through one attached bridge session.
uint32_t call_usb_watch_open_for_session(
    uint64_t session_handle,
    uint64_t *watch_id
) {
    JNIEnv *env = nullptr;
    bool did_attach_thread = false;

    if (!resolve_jni_env(&env, &did_attach_thread)) {
        return HOST_STATUS_FAILED;
    }

    uint32_t status = call_usb_watch_open(env, session_handle, watch_id);

    detach_jni_thread(did_attach_thread);

    return status;
}

/// Forward the `watch_close` usb request through one attached bridge session.
uint32_t call_usb_watch_close_for_session(
    uint64_t session_handle,
    uint64_t watch_id
) {
    JNIEnv *env = nullptr;
    bool did_attach_thread = false;

    if (!resolve_jni_env(&env, &did_attach_thread)) {
        return HOST_STATUS_FAILED;
    }

    uint32_t status = call_usb_watch_close(env, session_handle, watch_id);

    detach_jni_thread(did_attach_thread);

    return status;
}

/// Forward the `watch_read` usb request through one attached bridge session.
uint32_t call_usb_watch_read_for_session(
    uint64_t session_handle,
    uint64_t watch_id,
    uint64_t timeout_ns,
    AndroidHostUsbHotplugEventHeader *event,
    NativeU8Slice string_bytes,
    uint32_t *string_bytes_written,
    NativeU8Slice port_bytes,
    uint32_t *port_bytes_written
) {
    JNIEnv *env = nullptr;
    bool did_attach_thread = false;

    if (!resolve_jni_env(&env, &did_attach_thread)) {
        return HOST_STATUS_FAILED;
    }

    uint32_t status = call_usb_watch_read(env, session_handle, watch_id, timeout_ns, event, string_bytes, string_bytes_written, port_bytes, port_bytes_written);

    detach_jni_thread(did_attach_thread);

    return status;
}

/// Forward the `watch_try_read` usb request through one attached bridge session.
uint32_t call_usb_watch_try_read_for_session(
    uint64_t session_handle,
    uint64_t watch_id,
    AndroidHostUsbHotplugEventHeader *event,
    NativeU8Slice string_bytes,
    uint32_t *string_bytes_written,
    NativeU8Slice port_bytes,
    uint32_t *port_bytes_written
) {
    JNIEnv *env = nullptr;
    bool did_attach_thread = false;

    if (!resolve_jni_env(&env, &did_attach_thread)) {
        return HOST_STATUS_FAILED;
    }

    uint32_t status = call_usb_watch_try_read(env, session_handle, watch_id, event, string_bytes, string_bytes_written, port_bytes, port_bytes_written);

    detach_jni_thread(did_attach_thread);

    return status;
}

/// Forward the `open` usb request through one attached bridge session.
uint32_t call_usb_open_for_session(
    uint64_t session_handle,
    NativeStringRef id,
    int32_t *file_descriptor
) {
    JNIEnv *env = nullptr;
    bool did_attach_thread = false;

    if (!resolve_jni_env(&env, &did_attach_thread)) {
        return HOST_STATUS_FAILED;
    }

    uint32_t status = call_usb_open(env, session_handle, id, file_descriptor);

    detach_jni_thread(did_attach_thread);

    return status;
}

/// Build one Android host usb callback bundle.
AndroidHostUsbCallbacks make_usb_callbacks() {
    return AndroidHostUsbCallbacks {
        .device_list = call_usb_device_list_for_session,
        .watch_open = call_usb_watch_open_for_session,
        .watch_close = call_usb_watch_close_for_session,
        .watch_read = call_usb_watch_read_for_session,
        .watch_try_read = call_usb_watch_try_read_for_session,
        .open = call_usb_open_for_session,
    };
}
