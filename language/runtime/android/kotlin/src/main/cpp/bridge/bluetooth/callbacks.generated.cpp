// generated by generate-bindings: do not edit

#include "../types.h"
#include "../jni.h"
#include "../registry.h"
#include "callbacks.generated.h"

#include <deque>
#include <string>
#include <vector>

namespace {

jclass bridge_class = nullptr;
jmethodID adapterList_method = nullptr;
jmethodID scanOpen_method = nullptr;
jmethodID scanClose_method = nullptr;
jmethodID scanRead_method = nullptr;
jmethodID scanTryRead_method = nullptr;
jmethodID scanReadEvent_method = nullptr;
jmethodID scanTryReadEvent_method = nullptr;
jmethodID open_method = nullptr;
jmethodID close_method = nullptr;
jmethodID pair_method = nullptr;
jmethodID unpair_method = nullptr;
jmethodID readRssi_method = nullptr;
jmethodID sessionReadEvent_method = nullptr;
jmethodID sessionTryReadEvent_method = nullptr;
jmethodID gattServiceList_method = nullptr;
jmethodID gattCharacteristicList_method = nullptr;
jmethodID gattDescriptorList_method = nullptr;
jmethodID gattMtu_method = nullptr;
jmethodID gattRead_method = nullptr;
jmethodID gattReadDescriptor_method = nullptr;
jmethodID gattWrite_method = nullptr;
jmethodID gattWriteDescriptor_method = nullptr;
jmethodID gattSubscribe_method = nullptr;
jmethodID gattUnsubscribe_method = nullptr;
jmethodID gattReadEvent_method = nullptr;
jmethodID gattTryReadEvent_method = nullptr;

/// Build one JNI string borrowed from one native string reference.
jstring java_string_or_null(JNIEnv *env, NativeStringRef value) {
    if (value.data == nullptr) {
        return nullptr;
    }

    return new_java_string(env, value);
}

jmethodID resolve_constructor(JNIEnv *env, jclass value_class, const char *signature) {
    return env->GetMethodID(value_class, "<init>", signature);
}

thread_local std::deque<std::string> result_string_storage;
thread_local std::deque<std::vector<uint8_t>> result_u8_storage;

void clear_result_decode_storage() {
    result_string_storage.clear();
    result_u8_storage.clear();
}

jint call_list_size(JNIEnv *env, jobject value);
jobject call_list_get(JNIEnv *env, jobject value, jint index);
jint call_int_getter(JNIEnv *env, jobject value, const char *name);

jintArray encode_u8_slice(JNIEnv *env, NativeU8Slice values) {
    jintArray array = env->NewIntArray(static_cast<jsize>(values.len));
    if (array == nullptr) {
        return nullptr;
    }

    std::vector<jint> buffer(static_cast<size_t>(values.len));
    for (uint32_t index = 0; index < values.len; index += 1) {
        buffer[static_cast<size_t>(index)] = static_cast<jint>(values.data[index]);
    }

    env->SetIntArrayRegion(array, 0, static_cast<jsize>(values.len), buffer.data());
    return array;
}

NativeU8Slice decode_u8_list(JNIEnv *env, jobject list) {
    if (list == nullptr) {
        return NativeU8Slice {
            .data = nullptr,
            .len = 0,
        };
    }

    jint size = call_list_size(env, list);
    result_u8_storage.emplace_back();
    auto &storage = result_u8_storage.back();
    storage.reserve(static_cast<size_t>(size));

    for (jint index = 0; index < size; index += 1) {
        jobject value = call_list_get(env, list, index);
        jint resolved = call_int_getter(env, value, "intValue");
        storage.push_back(static_cast<uint8_t>(resolved));
        env->DeleteLocalRef(value);
    }

    return NativeU8Slice {
        .data = storage.data(),
        .len = static_cast<uint32_t>(storage.size()),
    };
}

jobject encode_AndroidHostBluetoothAdapterDescriptorHeader(JNIEnv *env, AndroidHostBluetoothAdapterDescriptorHeader value);
jobject encode_AndroidHostBluetoothDeviceDescriptorHeader(JNIEnv *env, AndroidHostBluetoothDeviceDescriptorHeader value);
jobject encode_AndroidHostBluetoothGattServiceHeader(JNIEnv *env, AndroidHostBluetoothGattServiceHeader value);
jobject encode_AndroidHostBluetoothGattCharacteristicHeader(JNIEnv *env, AndroidHostBluetoothGattCharacteristicHeader value);
jobject encode_AndroidHostBluetoothGattDescriptorHeader(JNIEnv *env, AndroidHostBluetoothGattDescriptorHeader value);
jobjectArray encode_AndroidHostBluetoothAdapterDescriptorHeaderSlice(JNIEnv *env, AndroidHostBluetoothAdapterDescriptorHeaderSlice value);
jobjectArray encode_AndroidHostBluetoothDeviceDescriptorHeaderSlice(JNIEnv *env, AndroidHostBluetoothDeviceDescriptorHeaderSlice value);
jobjectArray encode_AndroidHostBluetoothGattServiceHeaderSlice(JNIEnv *env, AndroidHostBluetoothGattServiceHeaderSlice value);
jobjectArray encode_AndroidHostBluetoothGattCharacteristicHeaderSlice(JNIEnv *env, AndroidHostBluetoothGattCharacteristicHeaderSlice value);
jobjectArray encode_AndroidHostBluetoothGattDescriptorHeaderSlice(JNIEnv *env, AndroidHostBluetoothGattDescriptorHeaderSlice value);

jobject encode_AndroidHostBluetoothAdapterDescriptorHeader(JNIEnv *env, AndroidHostBluetoothAdapterDescriptorHeader value) {
    jclass value_class = env->FindClass("dev/destack/runtime/android/module/bluetooth/RuntimeAndroidHostBluetoothAdapterDescriptorHeader");
    if (value_class == nullptr) {
        return nullptr;
    }

    jmethodID constructor = resolve_constructor(env, value_class, "(IIIIIIIII)V");
    if (constructor == nullptr) {
        env->DeleteLocalRef(value_class);
        return nullptr;
    }


    jobject result = env->NewObject(
        value_class,
        constructor,
        static_cast<jint>(value.id_offset),
        static_cast<jint>(value.id_len),
        static_cast<jint>(value.name_offset),
        static_cast<jint>(value.name_len),
        static_cast<jint>(value.capability_flags),
        static_cast<jint>(value.transport_flags),
        static_cast<jint>(value.is_powered),
        static_cast<jint>(value.is_discoverable),
        static_cast<jint>(value.is_discovering)
    );

    env->DeleteLocalRef(value_class);

    return result;
}

jobject encode_AndroidHostBluetoothDeviceDescriptorHeader(JNIEnv *env, AndroidHostBluetoothDeviceDescriptorHeader value) {
    jclass value_class = env->FindClass("dev/destack/runtime/android/module/bluetooth/RuntimeAndroidHostBluetoothDeviceDescriptorHeader");
    if (value_class == nullptr) {
        return nullptr;
    }

    jmethodID constructor = resolve_constructor(env, value_class, "(IIIIIIIIIIIIIIIIIIIIIIIII)V");
    if (constructor == nullptr) {
        env->DeleteLocalRef(value_class);
        return nullptr;
    }


    jobject result = env->NewObject(
        value_class,
        constructor,
        static_cast<jint>(value.id_offset),
        static_cast<jint>(value.id_len),
        static_cast<jint>(value.adapter_id_offset),
        static_cast<jint>(value.adapter_id_len),
        static_cast<jint>(value.name_offset),
        static_cast<jint>(value.name_len),
        static_cast<jint>(value.local_name_offset),
        static_cast<jint>(value.local_name_len),
        static_cast<jint>(value.manufacturer_offset),
        static_cast<jint>(value.manufacturer_len),
        static_cast<jint>(value.model_offset),
        static_cast<jint>(value.model_len),
        static_cast<jint>(value.address_offset),
        static_cast<jint>(value.address_len),
        static_cast<jint>(value.service_uuids_offset),
        static_cast<jint>(value.service_uuids_len),
        static_cast<jint>(value.manufacturer_data_offset),
        static_cast<jint>(value.manufacturer_data_len),
        static_cast<jint>(value.service_data_offset),
        static_cast<jint>(value.service_data_len),
        static_cast<jint>(value.transport),
        static_cast<jint>(value.rssi_dbm),
        static_cast<jint>(value.pair_state),
        static_cast<jint>(value.phy_flags),
        static_cast<jint>(value.is_connected)
    );

    env->DeleteLocalRef(value_class);

    return result;
}

jobject encode_AndroidHostBluetoothGattServiceHeader(JNIEnv *env, AndroidHostBluetoothGattServiceHeader value) {
    jclass value_class = env->FindClass("dev/destack/runtime/android/module/bluetooth/RuntimeAndroidHostBluetoothGattServiceHeader");
    if (value_class == nullptr) {
        return nullptr;
    }

    jmethodID constructor = resolve_constructor(env, value_class, "(IIIII)V");
    if (constructor == nullptr) {
        env->DeleteLocalRef(value_class);
        return nullptr;
    }


    jobject result = env->NewObject(
        value_class,
        constructor,
        static_cast<jint>(value.id_offset),
        static_cast<jint>(value.id_len),
        static_cast<jint>(value.uuid_offset),
        static_cast<jint>(value.uuid_len),
        static_cast<jint>(value.is_primary)
    );

    env->DeleteLocalRef(value_class);

    return result;
}

jobject encode_AndroidHostBluetoothGattCharacteristicHeader(JNIEnv *env, AndroidHostBluetoothGattCharacteristicHeader value) {
    jclass value_class = env->FindClass("dev/destack/runtime/android/module/bluetooth/RuntimeAndroidHostBluetoothGattCharacteristicHeader");
    if (value_class == nullptr) {
        return nullptr;
    }

    jmethodID constructor = resolve_constructor(env, value_class, "(IIIIIII)V");
    if (constructor == nullptr) {
        env->DeleteLocalRef(value_class);
        return nullptr;
    }


    jobject result = env->NewObject(
        value_class,
        constructor,
        static_cast<jint>(value.id_offset),
        static_cast<jint>(value.id_len),
        static_cast<jint>(value.service_id_offset),
        static_cast<jint>(value.service_id_len),
        static_cast<jint>(value.uuid_offset),
        static_cast<jint>(value.uuid_len),
        static_cast<jint>(value.property_flags)
    );

    env->DeleteLocalRef(value_class);

    return result;
}

jobject encode_AndroidHostBluetoothGattDescriptorHeader(JNIEnv *env, AndroidHostBluetoothGattDescriptorHeader value) {
    jclass value_class = env->FindClass("dev/destack/runtime/android/module/bluetooth/RuntimeAndroidHostBluetoothGattDescriptorHeader");
    if (value_class == nullptr) {
        return nullptr;
    }

    jmethodID constructor = resolve_constructor(env, value_class, "(IIIIII)V");
    if (constructor == nullptr) {
        env->DeleteLocalRef(value_class);
        return nullptr;
    }


    jobject result = env->NewObject(
        value_class,
        constructor,
        static_cast<jint>(value.id_offset),
        static_cast<jint>(value.id_len),
        static_cast<jint>(value.characteristic_id_offset),
        static_cast<jint>(value.characteristic_id_len),
        static_cast<jint>(value.uuid_offset),
        static_cast<jint>(value.uuid_len)
    );

    env->DeleteLocalRef(value_class);

    return result;
}

jobjectArray encode_AndroidHostBluetoothAdapterDescriptorHeaderSlice(JNIEnv *env, AndroidHostBluetoothAdapterDescriptorHeaderSlice value) {
    jclass element_class = env->FindClass("dev/destack/runtime/android/module/bluetooth/RuntimeAndroidHostBluetoothAdapterDescriptorHeader");
    if (element_class == nullptr) {
        return nullptr;
    }

    jobjectArray array = env->NewObjectArray(static_cast<jsize>(value.len), element_class, nullptr);
    env->DeleteLocalRef(element_class);
    if (array == nullptr) {
        return nullptr;
    }

    for (uint32_t index = 0; index < value.len; index += 1) {
        jobject element = encode_AndroidHostBluetoothAdapterDescriptorHeader(env, value.data[index]);
        if (element == nullptr) {
            env->DeleteLocalRef(array);
            return nullptr;
        }

        env->SetObjectArrayElement(array, static_cast<jsize>(index), element);
        env->DeleteLocalRef(element);
    }

    return array;
}

jobjectArray encode_AndroidHostBluetoothDeviceDescriptorHeaderSlice(JNIEnv *env, AndroidHostBluetoothDeviceDescriptorHeaderSlice value) {
    jclass element_class = env->FindClass("dev/destack/runtime/android/module/bluetooth/RuntimeAndroidHostBluetoothDeviceDescriptorHeader");
    if (element_class == nullptr) {
        return nullptr;
    }

    jobjectArray array = env->NewObjectArray(static_cast<jsize>(value.len), element_class, nullptr);
    env->DeleteLocalRef(element_class);
    if (array == nullptr) {
        return nullptr;
    }

    for (uint32_t index = 0; index < value.len; index += 1) {
        jobject element = encode_AndroidHostBluetoothDeviceDescriptorHeader(env, value.data[index]);
        if (element == nullptr) {
            env->DeleteLocalRef(array);
            return nullptr;
        }

        env->SetObjectArrayElement(array, static_cast<jsize>(index), element);
        env->DeleteLocalRef(element);
    }

    return array;
}

jobjectArray encode_AndroidHostBluetoothGattServiceHeaderSlice(JNIEnv *env, AndroidHostBluetoothGattServiceHeaderSlice value) {
    jclass element_class = env->FindClass("dev/destack/runtime/android/module/bluetooth/RuntimeAndroidHostBluetoothGattServiceHeader");
    if (element_class == nullptr) {
        return nullptr;
    }

    jobjectArray array = env->NewObjectArray(static_cast<jsize>(value.len), element_class, nullptr);
    env->DeleteLocalRef(element_class);
    if (array == nullptr) {
        return nullptr;
    }

    for (uint32_t index = 0; index < value.len; index += 1) {
        jobject element = encode_AndroidHostBluetoothGattServiceHeader(env, value.data[index]);
        if (element == nullptr) {
            env->DeleteLocalRef(array);
            return nullptr;
        }

        env->SetObjectArrayElement(array, static_cast<jsize>(index), element);
        env->DeleteLocalRef(element);
    }

    return array;
}

jobjectArray encode_AndroidHostBluetoothGattCharacteristicHeaderSlice(JNIEnv *env, AndroidHostBluetoothGattCharacteristicHeaderSlice value) {
    jclass element_class = env->FindClass("dev/destack/runtime/android/module/bluetooth/RuntimeAndroidHostBluetoothGattCharacteristicHeader");
    if (element_class == nullptr) {
        return nullptr;
    }

    jobjectArray array = env->NewObjectArray(static_cast<jsize>(value.len), element_class, nullptr);
    env->DeleteLocalRef(element_class);
    if (array == nullptr) {
        return nullptr;
    }

    for (uint32_t index = 0; index < value.len; index += 1) {
        jobject element = encode_AndroidHostBluetoothGattCharacteristicHeader(env, value.data[index]);
        if (element == nullptr) {
            env->DeleteLocalRef(array);
            return nullptr;
        }

        env->SetObjectArrayElement(array, static_cast<jsize>(index), element);
        env->DeleteLocalRef(element);
    }

    return array;
}

jobjectArray encode_AndroidHostBluetoothGattDescriptorHeaderSlice(JNIEnv *env, AndroidHostBluetoothGattDescriptorHeaderSlice value) {
    jclass element_class = env->FindClass("dev/destack/runtime/android/module/bluetooth/RuntimeAndroidHostBluetoothGattDescriptorHeader");
    if (element_class == nullptr) {
        return nullptr;
    }

    jobjectArray array = env->NewObjectArray(static_cast<jsize>(value.len), element_class, nullptr);
    env->DeleteLocalRef(element_class);
    if (array == nullptr) {
        return nullptr;
    }

    for (uint32_t index = 0; index < value.len; index += 1) {
        jobject element = encode_AndroidHostBluetoothGattDescriptorHeader(env, value.data[index]);
        if (element == nullptr) {
            env->DeleteLocalRef(array);
            return nullptr;
        }

        env->SetObjectArrayElement(array, static_cast<jsize>(index), element);
        env->DeleteLocalRef(element);
    }

    return array;
}


AndroidHostBluetoothScanEventHeader decode_AndroidHostBluetoothScanEventHeader(JNIEnv *env, jobject value, std::deque<std::string> *string_storage);
AndroidHostBluetoothDeviceDescriptorHeader decode_AndroidHostBluetoothDeviceDescriptorHeader(JNIEnv *env, jobject value, std::deque<std::string> *string_storage);
AndroidHostBluetoothSessionEventHeader decode_AndroidHostBluetoothSessionEventHeader(JNIEnv *env, jobject value, std::deque<std::string> *string_storage);

jmethodID resolve_instance_method(
    JNIEnv *env,
    jobject value,
    const char *name,
    const char *signature
) {
    jclass value_class = env->GetObjectClass(value);
    if (value_class == nullptr) {
        return nullptr;
    }

    jmethodID method = env->GetMethodID(value_class, name, signature);
    env->DeleteLocalRef(value_class);

    return method;
}

jobject call_object_getter(
    JNIEnv *env,
    jobject value,
    const char *name,
    const char *signature
) {
    jmethodID method = resolve_instance_method(env, value, name, signature);
    if (method == nullptr) {
        return nullptr;
    }

    return env->CallObjectMethod(value, method);
}

jint call_int_getter(JNIEnv *env, jobject value, const char *name) {
    jmethodID method = resolve_instance_method(env, value, name, "()I");
    return method == nullptr ? 0 : env->CallIntMethod(value, method);
}

jlong call_long_getter(JNIEnv *env, jobject value, const char *name) {
    jmethodID method = resolve_instance_method(env, value, name, "()J");
    return method == nullptr ? 0 : env->CallLongMethod(value, method);
}

jdouble call_double_getter(JNIEnv *env, jobject value, const char *name) {
    jmethodID method = resolve_instance_method(env, value, name, "()D");
    return method == nullptr ? 0.0 : env->CallDoubleMethod(value, method);
}

jboolean call_boolean_getter(JNIEnv *env, jobject value, const char *name) {
    jmethodID method = resolve_instance_method(env, value, name, "()Z");
    return method == nullptr ? JNI_FALSE : env->CallBooleanMethod(value, method);
}

uint64_t decode_boxed_u64(JNIEnv *env, jobject value) {
    jmethodID method = resolve_instance_method(env, value, "longValue", "()J");
    return method == nullptr ? 0 : static_cast<uint64_t>(env->CallLongMethod(value, method));
}

jint call_list_size(JNIEnv *env, jobject value) {
    jmethodID method = resolve_instance_method(env, value, "size", "()I");
    return method == nullptr ? 0 : env->CallIntMethod(value, method);
}

jobject call_list_get(JNIEnv *env, jobject value, jint index) {
    jmethodID method = resolve_instance_method(env, value, "get", "(I)Ljava/lang/Object;");
    return method == nullptr ? nullptr : env->CallObjectMethod(value, method, index);
}

AndroidHostBluetoothScanEventHeader decode_AndroidHostBluetoothScanEventHeader(JNIEnv *env, jobject value, std::deque<std::string> *string_storage) {
    AndroidHostBluetoothScanEventHeader decoded = {};

    decoded.timestamp_ns = static_cast<uint64_t>(call_long_getter(env, value, "getTimestampNs"));

    decoded.kind = static_cast<uint32_t>(call_int_getter(env, value, "getKind"));

    jobject descriptor_value = call_object_getter(env, value, "getDescriptor", "()Ldev/destack/runtime/android/module/bluetooth/RuntimeAndroidHostBluetoothDeviceDescriptorHeader;");
    decoded.descriptor = decode_AndroidHostBluetoothDeviceDescriptorHeader(env, descriptor_value, string_storage);
    if (descriptor_value != nullptr) { env->DeleteLocalRef(descriptor_value); }


    return decoded;
}

AndroidHostBluetoothDeviceDescriptorHeader decode_AndroidHostBluetoothDeviceDescriptorHeader(JNIEnv *env, jobject value, std::deque<std::string> *string_storage) {
    AndroidHostBluetoothDeviceDescriptorHeader decoded = {};

    decoded.id_offset = static_cast<uint32_t>(call_int_getter(env, value, "getIdOffset"));

    decoded.id_len = static_cast<uint32_t>(call_int_getter(env, value, "getIdLen"));

    decoded.adapter_id_offset = static_cast<uint32_t>(call_int_getter(env, value, "getAdapterIdOffset"));

    decoded.adapter_id_len = static_cast<uint32_t>(call_int_getter(env, value, "getAdapterIdLen"));

    decoded.name_offset = static_cast<uint32_t>(call_int_getter(env, value, "getNameOffset"));

    decoded.name_len = static_cast<uint32_t>(call_int_getter(env, value, "getNameLen"));

    decoded.local_name_offset = static_cast<uint32_t>(call_int_getter(env, value, "getLocalNameOffset"));

    decoded.local_name_len = static_cast<uint32_t>(call_int_getter(env, value, "getLocalNameLen"));

    decoded.manufacturer_offset = static_cast<uint32_t>(call_int_getter(env, value, "getManufacturerOffset"));

    decoded.manufacturer_len = static_cast<uint32_t>(call_int_getter(env, value, "getManufacturerLen"));

    decoded.model_offset = static_cast<uint32_t>(call_int_getter(env, value, "getModelOffset"));

    decoded.model_len = static_cast<uint32_t>(call_int_getter(env, value, "getModelLen"));

    decoded.address_offset = static_cast<uint32_t>(call_int_getter(env, value, "getAddressOffset"));

    decoded.address_len = static_cast<uint32_t>(call_int_getter(env, value, "getAddressLen"));

    decoded.service_uuids_offset = static_cast<uint32_t>(call_int_getter(env, value, "getServiceUuidsOffset"));

    decoded.service_uuids_len = static_cast<uint32_t>(call_int_getter(env, value, "getServiceUuidsLen"));

    decoded.manufacturer_data_offset = static_cast<uint32_t>(call_int_getter(env, value, "getManufacturerDataOffset"));

    decoded.manufacturer_data_len = static_cast<uint32_t>(call_int_getter(env, value, "getManufacturerDataLen"));

    decoded.service_data_offset = static_cast<uint32_t>(call_int_getter(env, value, "getServiceDataOffset"));

    decoded.service_data_len = static_cast<uint32_t>(call_int_getter(env, value, "getServiceDataLen"));

    decoded.transport = static_cast<uint32_t>(call_int_getter(env, value, "getTransport"));

    decoded.rssi_dbm = static_cast<int32_t>(call_int_getter(env, value, "getRssiDbm"));

    decoded.pair_state = static_cast<uint32_t>(call_int_getter(env, value, "getPairState"));

    decoded.phy_flags = static_cast<uint32_t>(call_int_getter(env, value, "getPhyFlags"));

    decoded.is_connected = static_cast<uint32_t>(call_int_getter(env, value, "getIsConnected"));


    return decoded;
}

AndroidHostBluetoothSessionEventHeader decode_AndroidHostBluetoothSessionEventHeader(JNIEnv *env, jobject value, std::deque<std::string> *string_storage) {
    AndroidHostBluetoothSessionEventHeader decoded = {};

    decoded.timestamp_ns = static_cast<uint64_t>(call_long_getter(env, value, "getTimestampNs"));

    decoded.kind = static_cast<uint32_t>(call_int_getter(env, value, "getKind"));

    decoded.pair_state = static_cast<uint32_t>(call_int_getter(env, value, "getPairState"));

    decoded.flags = static_cast<uint32_t>(call_int_getter(env, value, "getFlags"));


    return decoded;
}


/// Resolve the bluetooth bridge methods from one runtime bridge instance.
bool resolve_bridge_methods(JNIEnv *env, jobject bridge) {
    if (bridge_class == nullptr) {
        jclass local_class = env->GetObjectClass(bridge);
        if (local_class == nullptr) {
            return false;
        }

        bridge_class = reinterpret_cast<jclass>(env->NewGlobalRef(local_class));
        env->DeleteLocalRef(local_class);
        if (bridge_class == nullptr) {
            return false;
        }
    }

    if (adapterList_method == nullptr) {
        adapterList_method = env->GetMethodID(
            bridge_class,
            "bluetoothAdapterList",
            "([Ldev/destack/runtime/android/module/bluetooth/RuntimeAndroidHostBluetoothAdapterDescriptorHeader;[I)Ldev/destack/runtime/android/module/bluetooth/RuntimeHostBluetoothAdapterListResponse;"
        );
    }

    if (scanOpen_method == nullptr) {
        scanOpen_method = env->GetMethodID(
            bridge_class,
            "bluetoothScanOpen",
            "(Ljava/lang/String;IIIIIIIIIIIIIIIIII[I)Ldev/destack/runtime/android/module/bluetooth/RuntimeHostBluetoothScanOpenResponse;"
        );
    }

    if (scanClose_method == nullptr) {
        scanClose_method = env->GetMethodID(
            bridge_class,
            "bluetoothScanClose",
            "(J)I"
        );
    }

    if (scanRead_method == nullptr) {
        scanRead_method = env->GetMethodID(
            bridge_class,
            "bluetoothScanRead",
            "(JJ[Ldev/destack/runtime/android/module/bluetooth/RuntimeAndroidHostBluetoothDeviceDescriptorHeader;[I)Ldev/destack/runtime/android/module/bluetooth/RuntimeHostBluetoothScanReadResponse;"
        );
    }

    if (scanTryRead_method == nullptr) {
        scanTryRead_method = env->GetMethodID(
            bridge_class,
            "bluetoothScanTryRead",
            "(J[Ldev/destack/runtime/android/module/bluetooth/RuntimeAndroidHostBluetoothDeviceDescriptorHeader;[I)Ldev/destack/runtime/android/module/bluetooth/RuntimeHostBluetoothScanTryReadResponse;"
        );
    }

    if (scanReadEvent_method == nullptr) {
        scanReadEvent_method = env->GetMethodID(
            bridge_class,
            "bluetoothScanReadEvent",
            "(JJ[I)Ldev/destack/runtime/android/module/bluetooth/RuntimeHostBluetoothScanReadEventResponse;"
        );
    }

    if (scanTryReadEvent_method == nullptr) {
        scanTryReadEvent_method = env->GetMethodID(
            bridge_class,
            "bluetoothScanTryReadEvent",
            "(J[I)Ldev/destack/runtime/android/module/bluetooth/RuntimeHostBluetoothScanTryReadEventResponse;"
        );
    }

    if (open_method == nullptr) {
        open_method = env->GetMethodID(
            bridge_class,
            "bluetoothOpen",
            "(Ljava/lang/String;Ljava/lang/String;[I)Ldev/destack/runtime/android/module/bluetooth/RuntimeHostBluetoothOpenResponse;"
        );
    }

    if (close_method == nullptr) {
        close_method = env->GetMethodID(
            bridge_class,
            "bluetoothClose",
            "(J)I"
        );
    }

    if (pair_method == nullptr) {
        pair_method = env->GetMethodID(
            bridge_class,
            "bluetoothPair",
            "(JJ)I"
        );
    }

    if (unpair_method == nullptr) {
        unpair_method = env->GetMethodID(
            bridge_class,
            "bluetoothUnpair",
            "(Ljava/lang/String;Ljava/lang/String;)I"
        );
    }

    if (readRssi_method == nullptr) {
        readRssi_method = env->GetMethodID(
            bridge_class,
            "bluetoothReadRssi",
            "(JJ)Ldev/destack/runtime/android/module/bluetooth/RuntimeHostBluetoothReadRssiResponse;"
        );
    }

    if (sessionReadEvent_method == nullptr) {
        sessionReadEvent_method = env->GetMethodID(
            bridge_class,
            "bluetoothSessionReadEvent",
            "(JJ)Ldev/destack/runtime/android/module/bluetooth/RuntimeHostBluetoothSessionReadEventResponse;"
        );
    }

    if (sessionTryReadEvent_method == nullptr) {
        sessionTryReadEvent_method = env->GetMethodID(
            bridge_class,
            "bluetoothSessionTryReadEvent",
            "(J)Ldev/destack/runtime/android/module/bluetooth/RuntimeHostBluetoothSessionTryReadEventResponse;"
        );
    }

    if (gattServiceList_method == nullptr) {
        gattServiceList_method = env->GetMethodID(
            bridge_class,
            "bluetoothGattServiceList",
            "(J[Ldev/destack/runtime/android/module/bluetooth/RuntimeAndroidHostBluetoothGattServiceHeader;[I)Ldev/destack/runtime/android/module/bluetooth/RuntimeHostBluetoothGattServiceListResponse;"
        );
    }

    if (gattCharacteristicList_method == nullptr) {
        gattCharacteristicList_method = env->GetMethodID(
            bridge_class,
            "bluetoothGattCharacteristicList",
            "(JLjava/lang/String;[Ldev/destack/runtime/android/module/bluetooth/RuntimeAndroidHostBluetoothGattCharacteristicHeader;[I)Ldev/destack/runtime/android/module/bluetooth/RuntimeHostBluetoothGattCharacteristicListResponse;"
        );
    }

    if (gattDescriptorList_method == nullptr) {
        gattDescriptorList_method = env->GetMethodID(
            bridge_class,
            "bluetoothGattDescriptorList",
            "(JLjava/lang/String;[Ldev/destack/runtime/android/module/bluetooth/RuntimeAndroidHostBluetoothGattDescriptorHeader;[I)Ldev/destack/runtime/android/module/bluetooth/RuntimeHostBluetoothGattDescriptorListResponse;"
        );
    }

    if (gattMtu_method == nullptr) {
        gattMtu_method = env->GetMethodID(
            bridge_class,
            "bluetoothGattMtu",
            "(J)Ldev/destack/runtime/android/module/bluetooth/RuntimeHostBluetoothGattMtuResponse;"
        );
    }

    if (gattRead_method == nullptr) {
        gattRead_method = env->GetMethodID(
            bridge_class,
            "bluetoothGattRead",
            "(JLjava/lang/String;J[I)Ldev/destack/runtime/android/module/bluetooth/RuntimeHostBluetoothGattReadResponse;"
        );
    }

    if (gattReadDescriptor_method == nullptr) {
        gattReadDescriptor_method = env->GetMethodID(
            bridge_class,
            "bluetoothGattReadDescriptor",
            "(JLjava/lang/String;J[I)Ldev/destack/runtime/android/module/bluetooth/RuntimeHostBluetoothGattReadDescriptorResponse;"
        );
    }

    if (gattWrite_method == nullptr) {
        gattWrite_method = env->GetMethodID(
            bridge_class,
            "bluetoothGattWrite",
            "(JLjava/lang/String;I[IJ)I"
        );
    }

    if (gattWriteDescriptor_method == nullptr) {
        gattWriteDescriptor_method = env->GetMethodID(
            bridge_class,
            "bluetoothGattWriteDescriptor",
            "(JLjava/lang/String;[IJ)I"
        );
    }

    if (gattSubscribe_method == nullptr) {
        gattSubscribe_method = env->GetMethodID(
            bridge_class,
            "bluetoothGattSubscribe",
            "(JLjava/lang/String;)Ldev/destack/runtime/android/module/bluetooth/RuntimeHostBluetoothGattSubscribeResponse;"
        );
    }

    if (gattUnsubscribe_method == nullptr) {
        gattUnsubscribe_method = env->GetMethodID(
            bridge_class,
            "bluetoothGattUnsubscribe",
            "(J)I"
        );
    }

    if (gattReadEvent_method == nullptr) {
        gattReadEvent_method = env->GetMethodID(
            bridge_class,
            "bluetoothGattReadEvent",
            "(JJ)Ldev/destack/runtime/android/module/bluetooth/RuntimeHostBluetoothGattReadEventResponse;"
        );
    }

    if (gattTryReadEvent_method == nullptr) {
        gattTryReadEvent_method = env->GetMethodID(
            bridge_class,
            "bluetoothGattTryReadEvent",
            "(J)Ldev/destack/runtime/android/module/bluetooth/RuntimeHostBluetoothGattTryReadEventResponse;"
        );
    }

    return
        adapterList_method != nullptr &&
        scanOpen_method != nullptr &&
        scanClose_method != nullptr &&
        scanRead_method != nullptr &&
        scanTryRead_method != nullptr &&
        scanReadEvent_method != nullptr &&
        scanTryReadEvent_method != nullptr &&
        open_method != nullptr &&
        close_method != nullptr &&
        pair_method != nullptr &&
        unpair_method != nullptr &&
        readRssi_method != nullptr &&
        sessionReadEvent_method != nullptr &&
        sessionTryReadEvent_method != nullptr &&
        gattServiceList_method != nullptr &&
        gattCharacteristicList_method != nullptr &&
        gattDescriptorList_method != nullptr &&
        gattMtu_method != nullptr &&
        gattRead_method != nullptr &&
        gattReadDescriptor_method != nullptr &&
        gattWrite_method != nullptr &&
        gattWriteDescriptor_method != nullptr &&
        gattSubscribe_method != nullptr &&
        gattUnsubscribe_method != nullptr &&
        gattReadEvent_method != nullptr &&
        gattTryReadEvent_method != nullptr;
}

}

bool resolve_bluetooth_methods(JNIEnv *env, jobject bridge) {
    return resolve_bridge_methods(env, bridge);
}

/// Call the bluetooth-adapter-list entrypoint on one registered bridge.
uint32_t call_bluetooth_adapter_list(
    JNIEnv *env,
    uint64_t session_handle,
    AndroidHostBluetoothAdapterDescriptorHeaderSlice adapters,
    uint32_t *adapter_count_written,
    NativeU8Slice string_bytes,
    uint32_t *string_bytes_written
) {
    jobject bridge = resolve_bridge(env, session_handle);
    if (bridge == nullptr) {
        return HOST_STATUS_NOT_FOUND;
    }

    jobjectArray adapters_value = encode_AndroidHostBluetoothAdapterDescriptorHeaderSlice(env, adapters);
    if (adapters_value == nullptr) {
        if (env->ExceptionCheck()) {
            env->ExceptionClear();
        }
        env->DeleteLocalRef(bridge);
        return HOST_STATUS_FAILED;
    }
    jintArray string_bytes_value = encode_u8_slice(env, string_bytes);
    if (string_bytes_value == nullptr) {
        if (adapters_value != nullptr) { env->DeleteLocalRef(adapters_value); }
        if (env->ExceptionCheck()) {
            env->ExceptionClear();
        }
        env->DeleteLocalRef(bridge);
        return HOST_STATUS_FAILED;
    }

    jobject response_object = env->CallObjectMethod(
        bridge,
        adapterList_method,
        adapters_value,
        string_bytes_value
    );
    if (response_object == nullptr) {
        if (adapters_value != nullptr) { env->DeleteLocalRef(adapters_value); }
        if (string_bytes_value != nullptr) { env->DeleteLocalRef(string_bytes_value); }
        if (env->ExceptionCheck()) {
            env->ExceptionClear();
        }
        env->DeleteLocalRef(bridge);
        return HOST_STATUS_FAILED;
    }

    jint status = call_int_getter(env, response_object, "getStatus");
    if (status == HOST_STATUS_OK) {
        *adapter_count_written = static_cast<uint32_t>(call_int_getter(env, response_object, "getAdapterCountWritten"));
        *string_bytes_written = static_cast<uint32_t>(call_int_getter(env, response_object, "getStringBytesWritten"));
    }
    if (adapters_value != nullptr) { env->DeleteLocalRef(adapters_value); }
    if (string_bytes_value != nullptr) { env->DeleteLocalRef(string_bytes_value); }
    env->DeleteLocalRef(response_object);
    env->DeleteLocalRef(bridge);

    if (env->ExceptionCheck()) {
        env->ExceptionClear();
        return HOST_STATUS_FAILED;
    }

    return static_cast<uint32_t>(status);
}

/// Call the bluetooth-scan-open entrypoint on one registered bridge.
uint32_t call_bluetooth_scan_open(
    JNIEnv *env,
    uint64_t session_handle,
    NativeStringRef adapter_id,
    AndroidHostBluetoothScanFilterHeader filter,
    uint32_t filter_flags,
    NativeU8Slice filter_bytes,
    uint64_t *session_id
) {
    jobject bridge = resolve_bridge(env, session_handle);
    if (bridge == nullptr) {
        return HOST_STATUS_NOT_FOUND;
    }

    jstring adapter_id_value = java_string_or_null(env, adapter_id);
    jintArray filter_bytes_value = encode_u8_slice(env, filter_bytes);
    if (filter_bytes_value == nullptr) {
        if (adapter_id_value != nullptr) { env->DeleteLocalRef(adapter_id_value); }
        if (env->ExceptionCheck()) {
            env->ExceptionClear();
        }
        env->DeleteLocalRef(bridge);
        return HOST_STATUS_FAILED;
    }

    jobject response_object = env->CallObjectMethod(
        bridge,
        scanOpen_method,
        adapter_id_value,
        static_cast<jint>(filter.is_present),
        static_cast<jint>(filter.name_offset),
        static_cast<jint>(filter.name_len),
        static_cast<jint>(filter.name_prefix_offset),
        static_cast<jint>(filter.name_prefix_len),
        static_cast<jint>(filter.has_minimum_rssi),
        static_cast<jint>(filter.minimum_rssi_dbm),
        static_cast<jint>(filter.scan_mode),
        static_cast<jint>(filter.primary_phy),
        static_cast<jint>(filter.secondary_phy),
        static_cast<jint>(filter.keep_repeated_devices),
        static_cast<jint>(filter.service_uuids_offset),
        static_cast<jint>(filter.service_uuids_len),
        static_cast<jint>(filter.manufacturer_data_offset),
        static_cast<jint>(filter.manufacturer_data_len),
        static_cast<jint>(filter.service_data_offset),
        static_cast<jint>(filter.service_data_len),
        static_cast<jint>(filter_flags),
        filter_bytes_value
    );
    if (response_object == nullptr) {
        if (adapter_id_value != nullptr) { env->DeleteLocalRef(adapter_id_value); }
        if (filter_bytes_value != nullptr) { env->DeleteLocalRef(filter_bytes_value); }
        if (env->ExceptionCheck()) {
            env->ExceptionClear();
        }
        env->DeleteLocalRef(bridge);
        return HOST_STATUS_FAILED;
    }

    jint status = call_int_getter(env, response_object, "getStatus");
    if (status == HOST_STATUS_OK) {
        *session_id = static_cast<uint64_t>(call_long_getter(env, response_object, "getSessionId"));
    }
    if (adapter_id_value != nullptr) { env->DeleteLocalRef(adapter_id_value); }
    if (filter_bytes_value != nullptr) { env->DeleteLocalRef(filter_bytes_value); }
    env->DeleteLocalRef(response_object);
    env->DeleteLocalRef(bridge);

    if (env->ExceptionCheck()) {
        env->ExceptionClear();
        return HOST_STATUS_FAILED;
    }

    return static_cast<uint32_t>(status);
}

/// Call the bluetooth-scan-close entrypoint on one registered bridge.
uint32_t call_bluetooth_scan_close(
    JNIEnv *env,
    uint64_t session_handle,
    uint64_t session_id
) {
    jobject bridge = resolve_bridge(env, session_handle);
    if (bridge == nullptr) {
        return HOST_STATUS_NOT_FOUND;
    }


    jint status = env->CallIntMethod(
        bridge,
        scanClose_method,
        static_cast<jlong>(session_id)
    );
    env->DeleteLocalRef(bridge);

    if (env->ExceptionCheck()) {
        env->ExceptionClear();
        return HOST_STATUS_FAILED;
    }

    return static_cast<uint32_t>(status);
}

/// Call the bluetooth-scan-read entrypoint on one registered bridge.
uint32_t call_bluetooth_scan_read(
    JNIEnv *env,
    uint64_t session_handle,
    uint64_t session_id,
    uint64_t timeout_ns,
    AndroidHostBluetoothDeviceDescriptorHeaderSlice devices,
    uint32_t *device_count_written,
    NativeU8Slice string_bytes,
    uint32_t *string_bytes_written
) {
    jobject bridge = resolve_bridge(env, session_handle);
    if (bridge == nullptr) {
        return HOST_STATUS_NOT_FOUND;
    }

    jobjectArray devices_value = encode_AndroidHostBluetoothDeviceDescriptorHeaderSlice(env, devices);
    if (devices_value == nullptr) {
        if (env->ExceptionCheck()) {
            env->ExceptionClear();
        }
        env->DeleteLocalRef(bridge);
        return HOST_STATUS_FAILED;
    }
    jintArray string_bytes_value = encode_u8_slice(env, string_bytes);
    if (string_bytes_value == nullptr) {
        if (devices_value != nullptr) { env->DeleteLocalRef(devices_value); }
        if (env->ExceptionCheck()) {
            env->ExceptionClear();
        }
        env->DeleteLocalRef(bridge);
        return HOST_STATUS_FAILED;
    }

    jobject response_object = env->CallObjectMethod(
        bridge,
        scanRead_method,
        static_cast<jlong>(session_id),
        static_cast<jlong>(timeout_ns),
        devices_value,
        string_bytes_value
    );
    if (response_object == nullptr) {
        if (devices_value != nullptr) { env->DeleteLocalRef(devices_value); }
        if (string_bytes_value != nullptr) { env->DeleteLocalRef(string_bytes_value); }
        if (env->ExceptionCheck()) {
            env->ExceptionClear();
        }
        env->DeleteLocalRef(bridge);
        return HOST_STATUS_FAILED;
    }

    jint status = call_int_getter(env, response_object, "getStatus");
    if (status == HOST_STATUS_OK) {
        *device_count_written = static_cast<uint32_t>(call_int_getter(env, response_object, "getDeviceCountWritten"));
        *string_bytes_written = static_cast<uint32_t>(call_int_getter(env, response_object, "getStringBytesWritten"));
    }
    if (devices_value != nullptr) { env->DeleteLocalRef(devices_value); }
    if (string_bytes_value != nullptr) { env->DeleteLocalRef(string_bytes_value); }
    env->DeleteLocalRef(response_object);
    env->DeleteLocalRef(bridge);

    if (env->ExceptionCheck()) {
        env->ExceptionClear();
        return HOST_STATUS_FAILED;
    }

    return static_cast<uint32_t>(status);
}

/// Call the bluetooth-scan-try-read entrypoint on one registered bridge.
uint32_t call_bluetooth_scan_try_read(
    JNIEnv *env,
    uint64_t session_handle,
    uint64_t session_id,
    AndroidHostBluetoothDeviceDescriptorHeaderSlice devices,
    uint32_t *device_count_written,
    NativeU8Slice string_bytes,
    uint32_t *string_bytes_written
) {
    jobject bridge = resolve_bridge(env, session_handle);
    if (bridge == nullptr) {
        return HOST_STATUS_NOT_FOUND;
    }

    jobjectArray devices_value = encode_AndroidHostBluetoothDeviceDescriptorHeaderSlice(env, devices);
    if (devices_value == nullptr) {
        if (env->ExceptionCheck()) {
            env->ExceptionClear();
        }
        env->DeleteLocalRef(bridge);
        return HOST_STATUS_FAILED;
    }
    jintArray string_bytes_value = encode_u8_slice(env, string_bytes);
    if (string_bytes_value == nullptr) {
        if (devices_value != nullptr) { env->DeleteLocalRef(devices_value); }
        if (env->ExceptionCheck()) {
            env->ExceptionClear();
        }
        env->DeleteLocalRef(bridge);
        return HOST_STATUS_FAILED;
    }

    jobject response_object = env->CallObjectMethod(
        bridge,
        scanTryRead_method,
        static_cast<jlong>(session_id),
        devices_value,
        string_bytes_value
    );
    if (response_object == nullptr) {
        if (devices_value != nullptr) { env->DeleteLocalRef(devices_value); }
        if (string_bytes_value != nullptr) { env->DeleteLocalRef(string_bytes_value); }
        if (env->ExceptionCheck()) {
            env->ExceptionClear();
        }
        env->DeleteLocalRef(bridge);
        return HOST_STATUS_FAILED;
    }

    jint status = call_int_getter(env, response_object, "getStatus");
    if (status == HOST_STATUS_OK) {
        *device_count_written = static_cast<uint32_t>(call_int_getter(env, response_object, "getDeviceCountWritten"));
        *string_bytes_written = static_cast<uint32_t>(call_int_getter(env, response_object, "getStringBytesWritten"));
    }
    if (devices_value != nullptr) { env->DeleteLocalRef(devices_value); }
    if (string_bytes_value != nullptr) { env->DeleteLocalRef(string_bytes_value); }
    env->DeleteLocalRef(response_object);
    env->DeleteLocalRef(bridge);

    if (env->ExceptionCheck()) {
        env->ExceptionClear();
        return HOST_STATUS_FAILED;
    }

    return static_cast<uint32_t>(status);
}

/// Call the bluetooth-scan-read-event entrypoint on one registered bridge.
uint32_t call_bluetooth_scan_read_event(
    JNIEnv *env,
    uint64_t session_handle,
    uint64_t session_id,
    uint64_t timeout_ns,
    AndroidHostBluetoothScanEventHeader *event,
    NativeU8Slice string_bytes,
    uint32_t *string_bytes_written
) {
    jobject bridge = resolve_bridge(env, session_handle);
    if (bridge == nullptr) {
        return HOST_STATUS_NOT_FOUND;
    }

    jintArray string_bytes_value = encode_u8_slice(env, string_bytes);
    if (string_bytes_value == nullptr) {
        if (env->ExceptionCheck()) {
            env->ExceptionClear();
        }
        env->DeleteLocalRef(bridge);
        return HOST_STATUS_FAILED;
    }

    jobject response_object = env->CallObjectMethod(
        bridge,
        scanReadEvent_method,
        static_cast<jlong>(session_id),
        static_cast<jlong>(timeout_ns),
        string_bytes_value
    );
    if (response_object == nullptr) {
        if (string_bytes_value != nullptr) { env->DeleteLocalRef(string_bytes_value); }
        if (env->ExceptionCheck()) {
            env->ExceptionClear();
        }
        env->DeleteLocalRef(bridge);
        return HOST_STATUS_FAILED;
    }

    clear_result_decode_storage();
    std::deque<std::string> *string_storage = &result_string_storage;

    jint status = call_int_getter(env, response_object, "getStatus");
    if (status == HOST_STATUS_OK) {
        jobject event_value = call_object_getter(env, response_object, "getEvent", "()Ldev/destack/runtime/android/module/bluetooth/RuntimeAndroidHostBluetoothScanEventHeader;");
        if (event_value == nullptr) {
            status = HOST_STATUS_FAILED;
        } else {
            *event = decode_AndroidHostBluetoothScanEventHeader(env, event_value, string_storage);
            env->DeleteLocalRef(event_value);
        }
        *string_bytes_written = static_cast<uint32_t>(call_int_getter(env, response_object, "getStringBytesWritten"));
    }
    if (string_bytes_value != nullptr) { env->DeleteLocalRef(string_bytes_value); }
    env->DeleteLocalRef(response_object);
    env->DeleteLocalRef(bridge);

    if (env->ExceptionCheck()) {
        env->ExceptionClear();
        return HOST_STATUS_FAILED;
    }

    return static_cast<uint32_t>(status);
}

/// Call the bluetooth-scan-try-read-event entrypoint on one registered bridge.
uint32_t call_bluetooth_scan_try_read_event(
    JNIEnv *env,
    uint64_t session_handle,
    uint64_t session_id,
    AndroidHostBluetoothScanEventHeader *event,
    NativeU8Slice string_bytes,
    uint32_t *string_bytes_written
) {
    jobject bridge = resolve_bridge(env, session_handle);
    if (bridge == nullptr) {
        return HOST_STATUS_NOT_FOUND;
    }

    jintArray string_bytes_value = encode_u8_slice(env, string_bytes);
    if (string_bytes_value == nullptr) {
        if (env->ExceptionCheck()) {
            env->ExceptionClear();
        }
        env->DeleteLocalRef(bridge);
        return HOST_STATUS_FAILED;
    }

    jobject response_object = env->CallObjectMethod(
        bridge,
        scanTryReadEvent_method,
        static_cast<jlong>(session_id),
        string_bytes_value
    );
    if (response_object == nullptr) {
        if (string_bytes_value != nullptr) { env->DeleteLocalRef(string_bytes_value); }
        if (env->ExceptionCheck()) {
            env->ExceptionClear();
        }
        env->DeleteLocalRef(bridge);
        return HOST_STATUS_FAILED;
    }

    clear_result_decode_storage();
    std::deque<std::string> *string_storage = &result_string_storage;

    jint status = call_int_getter(env, response_object, "getStatus");
    if (status == HOST_STATUS_OK) {
        jobject event_value = call_object_getter(env, response_object, "getEvent", "()Ldev/destack/runtime/android/module/bluetooth/RuntimeAndroidHostBluetoothScanEventHeader;");
        if (event_value == nullptr) {
            status = HOST_STATUS_FAILED;
        } else {
            *event = decode_AndroidHostBluetoothScanEventHeader(env, event_value, string_storage);
            env->DeleteLocalRef(event_value);
        }
        *string_bytes_written = static_cast<uint32_t>(call_int_getter(env, response_object, "getStringBytesWritten"));
    }
    if (string_bytes_value != nullptr) { env->DeleteLocalRef(string_bytes_value); }
    env->DeleteLocalRef(response_object);
    env->DeleteLocalRef(bridge);

    if (env->ExceptionCheck()) {
        env->ExceptionClear();
        return HOST_STATUS_FAILED;
    }

    return static_cast<uint32_t>(status);
}

/// Call the bluetooth-open entrypoint on one registered bridge.
uint32_t call_bluetooth_open(
    JNIEnv *env,
    uint64_t session_handle,
    NativeStringRef adapter_id,
    NativeStringRef device_id,
    uint64_t *session_id,
    AndroidHostBluetoothDeviceDescriptorHeader *descriptor,
    NativeU8Slice string_bytes,
    uint32_t *string_bytes_written
) {
    jobject bridge = resolve_bridge(env, session_handle);
    if (bridge == nullptr) {
        return HOST_STATUS_NOT_FOUND;
    }

    jstring adapter_id_value = java_string_or_null(env, adapter_id);
    jstring device_id_value = java_string_or_null(env, device_id);
    jintArray string_bytes_value = encode_u8_slice(env, string_bytes);
    if (string_bytes_value == nullptr) {
        if (adapter_id_value != nullptr) { env->DeleteLocalRef(adapter_id_value); }
        if (device_id_value != nullptr) { env->DeleteLocalRef(device_id_value); }
        if (env->ExceptionCheck()) {
            env->ExceptionClear();
        }
        env->DeleteLocalRef(bridge);
        return HOST_STATUS_FAILED;
    }

    jobject response_object = env->CallObjectMethod(
        bridge,
        open_method,
        adapter_id_value,
        device_id_value,
        string_bytes_value
    );
    if (response_object == nullptr) {
        if (adapter_id_value != nullptr) { env->DeleteLocalRef(adapter_id_value); }
        if (device_id_value != nullptr) { env->DeleteLocalRef(device_id_value); }
        if (string_bytes_value != nullptr) { env->DeleteLocalRef(string_bytes_value); }
        if (env->ExceptionCheck()) {
            env->ExceptionClear();
        }
        env->DeleteLocalRef(bridge);
        return HOST_STATUS_FAILED;
    }

    clear_result_decode_storage();
    std::deque<std::string> *string_storage = &result_string_storage;

    jint status = call_int_getter(env, response_object, "getStatus");
    if (status == HOST_STATUS_OK) {
        *session_id = static_cast<uint64_t>(call_long_getter(env, response_object, "getSessionId"));
        jobject descriptor_value = call_object_getter(env, response_object, "getDescriptor", "()Ldev/destack/runtime/android/module/bluetooth/RuntimeAndroidHostBluetoothDeviceDescriptorHeader;");
        if (descriptor_value == nullptr) {
            status = HOST_STATUS_FAILED;
        } else {
            *descriptor = decode_AndroidHostBluetoothDeviceDescriptorHeader(env, descriptor_value, string_storage);
            env->DeleteLocalRef(descriptor_value);
        }
        *string_bytes_written = static_cast<uint32_t>(call_int_getter(env, response_object, "getStringBytesWritten"));
    }
    if (adapter_id_value != nullptr) { env->DeleteLocalRef(adapter_id_value); }
    if (device_id_value != nullptr) { env->DeleteLocalRef(device_id_value); }
    if (string_bytes_value != nullptr) { env->DeleteLocalRef(string_bytes_value); }
    env->DeleteLocalRef(response_object);
    env->DeleteLocalRef(bridge);

    if (env->ExceptionCheck()) {
        env->ExceptionClear();
        return HOST_STATUS_FAILED;
    }

    return static_cast<uint32_t>(status);
}

/// Call the bluetooth-close entrypoint on one registered bridge.
uint32_t call_bluetooth_close(
    JNIEnv *env,
    uint64_t session_handle,
    uint64_t session_id
) {
    jobject bridge = resolve_bridge(env, session_handle);
    if (bridge == nullptr) {
        return HOST_STATUS_NOT_FOUND;
    }


    jint status = env->CallIntMethod(
        bridge,
        close_method,
        static_cast<jlong>(session_id)
    );
    env->DeleteLocalRef(bridge);

    if (env->ExceptionCheck()) {
        env->ExceptionClear();
        return HOST_STATUS_FAILED;
    }

    return static_cast<uint32_t>(status);
}

/// Call the bluetooth-pair entrypoint on one registered bridge.
uint32_t call_bluetooth_pair(
    JNIEnv *env,
    uint64_t session_handle,
    uint64_t session_id,
    uint64_t timeout_ns
) {
    jobject bridge = resolve_bridge(env, session_handle);
    if (bridge == nullptr) {
        return HOST_STATUS_NOT_FOUND;
    }


    jint status = env->CallIntMethod(
        bridge,
        pair_method,
        static_cast<jlong>(session_id),
        static_cast<jlong>(timeout_ns)
    );
    env->DeleteLocalRef(bridge);

    if (env->ExceptionCheck()) {
        env->ExceptionClear();
        return HOST_STATUS_FAILED;
    }

    return static_cast<uint32_t>(status);
}

/// Call the bluetooth-unpair entrypoint on one registered bridge.
uint32_t call_bluetooth_unpair(
    JNIEnv *env,
    uint64_t session_handle,
    NativeStringRef adapter_id,
    NativeStringRef device_id
) {
    jobject bridge = resolve_bridge(env, session_handle);
    if (bridge == nullptr) {
        return HOST_STATUS_NOT_FOUND;
    }

    jstring adapter_id_value = java_string_or_null(env, adapter_id);
    jstring device_id_value = java_string_or_null(env, device_id);

    jint status = env->CallIntMethod(
        bridge,
        unpair_method,
        adapter_id_value,
        device_id_value
    );
    if (adapter_id_value != nullptr) { env->DeleteLocalRef(adapter_id_value); }
    if (device_id_value != nullptr) { env->DeleteLocalRef(device_id_value); }
    env->DeleteLocalRef(bridge);

    if (env->ExceptionCheck()) {
        env->ExceptionClear();
        return HOST_STATUS_FAILED;
    }

    return static_cast<uint32_t>(status);
}

/// Call the bluetooth-read-rssi entrypoint on one registered bridge.
uint32_t call_bluetooth_read_rssi(
    JNIEnv *env,
    uint64_t session_handle,
    uint64_t session_id,
    uint64_t timeout_ns,
    int16_t *rssi_dbm
) {
    jobject bridge = resolve_bridge(env, session_handle);
    if (bridge == nullptr) {
        return HOST_STATUS_NOT_FOUND;
    }


    jobject response_object = env->CallObjectMethod(
        bridge,
        readRssi_method,
        static_cast<jlong>(session_id),
        static_cast<jlong>(timeout_ns)
    );
    if (response_object == nullptr) {
        if (env->ExceptionCheck()) {
            env->ExceptionClear();
        }
        env->DeleteLocalRef(bridge);
        return HOST_STATUS_FAILED;
    }

    jint status = call_int_getter(env, response_object, "getStatus");
    if (status == HOST_STATUS_OK) {
        *rssi_dbm = static_cast<int16_t>(call_int_getter(env, response_object, "getRssiDbm"));
    }
    env->DeleteLocalRef(response_object);
    env->DeleteLocalRef(bridge);

    if (env->ExceptionCheck()) {
        env->ExceptionClear();
        return HOST_STATUS_FAILED;
    }

    return static_cast<uint32_t>(status);
}

/// Call the bluetooth-session-read-event entrypoint on one registered bridge.
uint32_t call_bluetooth_session_read_event(
    JNIEnv *env,
    uint64_t session_handle,
    uint64_t session_id,
    uint64_t timeout_ns,
    AndroidHostBluetoothSessionEventHeader *event
) {
    jobject bridge = resolve_bridge(env, session_handle);
    if (bridge == nullptr) {
        return HOST_STATUS_NOT_FOUND;
    }


    jobject response_object = env->CallObjectMethod(
        bridge,
        sessionReadEvent_method,
        static_cast<jlong>(session_id),
        static_cast<jlong>(timeout_ns)
    );
    if (response_object == nullptr) {
        if (env->ExceptionCheck()) {
            env->ExceptionClear();
        }
        env->DeleteLocalRef(bridge);
        return HOST_STATUS_FAILED;
    }

    clear_result_decode_storage();
    std::deque<std::string> *string_storage = &result_string_storage;

    jint status = call_int_getter(env, response_object, "getStatus");
    if (status == HOST_STATUS_OK) {
        jobject event_value = call_object_getter(env, response_object, "getEvent", "()Ldev/destack/runtime/android/module/bluetooth/RuntimeAndroidHostBluetoothSessionEventHeader;");
        if (event_value == nullptr) {
            status = HOST_STATUS_FAILED;
        } else {
            *event = decode_AndroidHostBluetoothSessionEventHeader(env, event_value, string_storage);
            env->DeleteLocalRef(event_value);
        }
    }
    env->DeleteLocalRef(response_object);
    env->DeleteLocalRef(bridge);

    if (env->ExceptionCheck()) {
        env->ExceptionClear();
        return HOST_STATUS_FAILED;
    }

    return static_cast<uint32_t>(status);
}

/// Call the bluetooth-session-try-read-event entrypoint on one registered bridge.
uint32_t call_bluetooth_session_try_read_event(
    JNIEnv *env,
    uint64_t session_handle,
    uint64_t session_id,
    AndroidHostBluetoothSessionEventHeader *event
) {
    jobject bridge = resolve_bridge(env, session_handle);
    if (bridge == nullptr) {
        return HOST_STATUS_NOT_FOUND;
    }


    jobject response_object = env->CallObjectMethod(
        bridge,
        sessionTryReadEvent_method,
        static_cast<jlong>(session_id)
    );
    if (response_object == nullptr) {
        if (env->ExceptionCheck()) {
            env->ExceptionClear();
        }
        env->DeleteLocalRef(bridge);
        return HOST_STATUS_FAILED;
    }

    clear_result_decode_storage();
    std::deque<std::string> *string_storage = &result_string_storage;

    jint status = call_int_getter(env, response_object, "getStatus");
    if (status == HOST_STATUS_OK) {
        jobject event_value = call_object_getter(env, response_object, "getEvent", "()Ldev/destack/runtime/android/module/bluetooth/RuntimeAndroidHostBluetoothSessionEventHeader;");
        if (event_value == nullptr) {
            status = HOST_STATUS_FAILED;
        } else {
            *event = decode_AndroidHostBluetoothSessionEventHeader(env, event_value, string_storage);
            env->DeleteLocalRef(event_value);
        }
    }
    env->DeleteLocalRef(response_object);
    env->DeleteLocalRef(bridge);

    if (env->ExceptionCheck()) {
        env->ExceptionClear();
        return HOST_STATUS_FAILED;
    }

    return static_cast<uint32_t>(status);
}

/// Call the bluetooth-gatt-service-list entrypoint on one registered bridge.
uint32_t call_bluetooth_gatt_service_list(
    JNIEnv *env,
    uint64_t session_handle,
    uint64_t session_id,
    AndroidHostBluetoothGattServiceHeaderSlice services,
    uint32_t *service_count_written,
    NativeU8Slice string_bytes,
    uint32_t *string_bytes_written
) {
    jobject bridge = resolve_bridge(env, session_handle);
    if (bridge == nullptr) {
        return HOST_STATUS_NOT_FOUND;
    }

    jobjectArray services_value = encode_AndroidHostBluetoothGattServiceHeaderSlice(env, services);
    if (services_value == nullptr) {
        if (env->ExceptionCheck()) {
            env->ExceptionClear();
        }
        env->DeleteLocalRef(bridge);
        return HOST_STATUS_FAILED;
    }
    jintArray string_bytes_value = encode_u8_slice(env, string_bytes);
    if (string_bytes_value == nullptr) {
        if (services_value != nullptr) { env->DeleteLocalRef(services_value); }
        if (env->ExceptionCheck()) {
            env->ExceptionClear();
        }
        env->DeleteLocalRef(bridge);
        return HOST_STATUS_FAILED;
    }

    jobject response_object = env->CallObjectMethod(
        bridge,
        gattServiceList_method,
        static_cast<jlong>(session_id),
        services_value,
        string_bytes_value
    );
    if (response_object == nullptr) {
        if (services_value != nullptr) { env->DeleteLocalRef(services_value); }
        if (string_bytes_value != nullptr) { env->DeleteLocalRef(string_bytes_value); }
        if (env->ExceptionCheck()) {
            env->ExceptionClear();
        }
        env->DeleteLocalRef(bridge);
        return HOST_STATUS_FAILED;
    }

    jint status = call_int_getter(env, response_object, "getStatus");
    if (status == HOST_STATUS_OK) {
        *service_count_written = static_cast<uint32_t>(call_int_getter(env, response_object, "getServiceCountWritten"));
        *string_bytes_written = static_cast<uint32_t>(call_int_getter(env, response_object, "getStringBytesWritten"));
    }
    if (services_value != nullptr) { env->DeleteLocalRef(services_value); }
    if (string_bytes_value != nullptr) { env->DeleteLocalRef(string_bytes_value); }
    env->DeleteLocalRef(response_object);
    env->DeleteLocalRef(bridge);

    if (env->ExceptionCheck()) {
        env->ExceptionClear();
        return HOST_STATUS_FAILED;
    }

    return static_cast<uint32_t>(status);
}

/// Call the bluetooth-gatt-characteristic-list entrypoint on one registered bridge.
uint32_t call_bluetooth_gatt_characteristic_list(
    JNIEnv *env,
    uint64_t session_handle,
    uint64_t session_id,
    NativeStringRef service_id,
    AndroidHostBluetoothGattCharacteristicHeaderSlice characteristics,
    uint32_t *characteristic_count_written,
    NativeU8Slice string_bytes,
    uint32_t *string_bytes_written
) {
    jobject bridge = resolve_bridge(env, session_handle);
    if (bridge == nullptr) {
        return HOST_STATUS_NOT_FOUND;
    }

    jstring service_id_value = java_string_or_null(env, service_id);
    jobjectArray characteristics_value = encode_AndroidHostBluetoothGattCharacteristicHeaderSlice(env, characteristics);
    if (characteristics_value == nullptr) {
        if (service_id_value != nullptr) { env->DeleteLocalRef(service_id_value); }
        if (env->ExceptionCheck()) {
            env->ExceptionClear();
        }
        env->DeleteLocalRef(bridge);
        return HOST_STATUS_FAILED;
    }
    jintArray string_bytes_value = encode_u8_slice(env, string_bytes);
    if (string_bytes_value == nullptr) {
        if (service_id_value != nullptr) { env->DeleteLocalRef(service_id_value); }
        if (characteristics_value != nullptr) { env->DeleteLocalRef(characteristics_value); }
        if (env->ExceptionCheck()) {
            env->ExceptionClear();
        }
        env->DeleteLocalRef(bridge);
        return HOST_STATUS_FAILED;
    }

    jobject response_object = env->CallObjectMethod(
        bridge,
        gattCharacteristicList_method,
        static_cast<jlong>(session_id),
        service_id_value,
        characteristics_value,
        string_bytes_value
    );
    if (response_object == nullptr) {
        if (service_id_value != nullptr) { env->DeleteLocalRef(service_id_value); }
        if (characteristics_value != nullptr) { env->DeleteLocalRef(characteristics_value); }
        if (string_bytes_value != nullptr) { env->DeleteLocalRef(string_bytes_value); }
        if (env->ExceptionCheck()) {
            env->ExceptionClear();
        }
        env->DeleteLocalRef(bridge);
        return HOST_STATUS_FAILED;
    }

    jint status = call_int_getter(env, response_object, "getStatus");
    if (status == HOST_STATUS_OK) {
        *characteristic_count_written = static_cast<uint32_t>(call_int_getter(env, response_object, "getCharacteristicCountWritten"));
        *string_bytes_written = static_cast<uint32_t>(call_int_getter(env, response_object, "getStringBytesWritten"));
    }
    if (service_id_value != nullptr) { env->DeleteLocalRef(service_id_value); }
    if (characteristics_value != nullptr) { env->DeleteLocalRef(characteristics_value); }
    if (string_bytes_value != nullptr) { env->DeleteLocalRef(string_bytes_value); }
    env->DeleteLocalRef(response_object);
    env->DeleteLocalRef(bridge);

    if (env->ExceptionCheck()) {
        env->ExceptionClear();
        return HOST_STATUS_FAILED;
    }

    return static_cast<uint32_t>(status);
}

/// Call the bluetooth-gatt-descriptor-list entrypoint on one registered bridge.
uint32_t call_bluetooth_gatt_descriptor_list(
    JNIEnv *env,
    uint64_t session_handle,
    uint64_t session_id,
    NativeStringRef characteristic_id,
    AndroidHostBluetoothGattDescriptorHeaderSlice descriptors,
    uint32_t *descriptor_count_written,
    NativeU8Slice string_bytes,
    uint32_t *string_bytes_written
) {
    jobject bridge = resolve_bridge(env, session_handle);
    if (bridge == nullptr) {
        return HOST_STATUS_NOT_FOUND;
    }

    jstring characteristic_id_value = java_string_or_null(env, characteristic_id);
    jobjectArray descriptors_value = encode_AndroidHostBluetoothGattDescriptorHeaderSlice(env, descriptors);
    if (descriptors_value == nullptr) {
        if (characteristic_id_value != nullptr) { env->DeleteLocalRef(characteristic_id_value); }
        if (env->ExceptionCheck()) {
            env->ExceptionClear();
        }
        env->DeleteLocalRef(bridge);
        return HOST_STATUS_FAILED;
    }
    jintArray string_bytes_value = encode_u8_slice(env, string_bytes);
    if (string_bytes_value == nullptr) {
        if (characteristic_id_value != nullptr) { env->DeleteLocalRef(characteristic_id_value); }
        if (descriptors_value != nullptr) { env->DeleteLocalRef(descriptors_value); }
        if (env->ExceptionCheck()) {
            env->ExceptionClear();
        }
        env->DeleteLocalRef(bridge);
        return HOST_STATUS_FAILED;
    }

    jobject response_object = env->CallObjectMethod(
        bridge,
        gattDescriptorList_method,
        static_cast<jlong>(session_id),
        characteristic_id_value,
        descriptors_value,
        string_bytes_value
    );
    if (response_object == nullptr) {
        if (characteristic_id_value != nullptr) { env->DeleteLocalRef(characteristic_id_value); }
        if (descriptors_value != nullptr) { env->DeleteLocalRef(descriptors_value); }
        if (string_bytes_value != nullptr) { env->DeleteLocalRef(string_bytes_value); }
        if (env->ExceptionCheck()) {
            env->ExceptionClear();
        }
        env->DeleteLocalRef(bridge);
        return HOST_STATUS_FAILED;
    }

    jint status = call_int_getter(env, response_object, "getStatus");
    if (status == HOST_STATUS_OK) {
        *descriptor_count_written = static_cast<uint32_t>(call_int_getter(env, response_object, "getDescriptorCountWritten"));
        *string_bytes_written = static_cast<uint32_t>(call_int_getter(env, response_object, "getStringBytesWritten"));
    }
    if (characteristic_id_value != nullptr) { env->DeleteLocalRef(characteristic_id_value); }
    if (descriptors_value != nullptr) { env->DeleteLocalRef(descriptors_value); }
    if (string_bytes_value != nullptr) { env->DeleteLocalRef(string_bytes_value); }
    env->DeleteLocalRef(response_object);
    env->DeleteLocalRef(bridge);

    if (env->ExceptionCheck()) {
        env->ExceptionClear();
        return HOST_STATUS_FAILED;
    }

    return static_cast<uint32_t>(status);
}

/// Call the bluetooth-gatt-mtu entrypoint on one registered bridge.
uint32_t call_bluetooth_gatt_mtu(
    JNIEnv *env,
    uint64_t session_handle,
    uint64_t session_id,
    uint16_t *mtu
) {
    jobject bridge = resolve_bridge(env, session_handle);
    if (bridge == nullptr) {
        return HOST_STATUS_NOT_FOUND;
    }


    jobject response_object = env->CallObjectMethod(
        bridge,
        gattMtu_method,
        static_cast<jlong>(session_id)
    );
    if (response_object == nullptr) {
        if (env->ExceptionCheck()) {
            env->ExceptionClear();
        }
        env->DeleteLocalRef(bridge);
        return HOST_STATUS_FAILED;
    }

    jint status = call_int_getter(env, response_object, "getStatus");
    if (status == HOST_STATUS_OK) {
        *mtu = static_cast<uint16_t>(call_int_getter(env, response_object, "getMtu"));
    }
    env->DeleteLocalRef(response_object);
    env->DeleteLocalRef(bridge);

    if (env->ExceptionCheck()) {
        env->ExceptionClear();
        return HOST_STATUS_FAILED;
    }

    return static_cast<uint32_t>(status);
}

/// Call the bluetooth-gatt-read entrypoint on one registered bridge.
uint32_t call_bluetooth_gatt_read(
    JNIEnv *env,
    uint64_t session_handle,
    uint64_t session_id,
    NativeStringRef characteristic_id,
    uint64_t timeout_ns,
    NativeU8Slice bytes,
    uint32_t *bytes_written
) {
    jobject bridge = resolve_bridge(env, session_handle);
    if (bridge == nullptr) {
        return HOST_STATUS_NOT_FOUND;
    }

    jstring characteristic_id_value = java_string_or_null(env, characteristic_id);
    jintArray bytes_value = encode_u8_slice(env, bytes);
    if (bytes_value == nullptr) {
        if (characteristic_id_value != nullptr) { env->DeleteLocalRef(characteristic_id_value); }
        if (env->ExceptionCheck()) {
            env->ExceptionClear();
        }
        env->DeleteLocalRef(bridge);
        return HOST_STATUS_FAILED;
    }

    jobject response_object = env->CallObjectMethod(
        bridge,
        gattRead_method,
        static_cast<jlong>(session_id),
        characteristic_id_value,
        static_cast<jlong>(timeout_ns),
        bytes_value
    );
    if (response_object == nullptr) {
        if (characteristic_id_value != nullptr) { env->DeleteLocalRef(characteristic_id_value); }
        if (bytes_value != nullptr) { env->DeleteLocalRef(bytes_value); }
        if (env->ExceptionCheck()) {
            env->ExceptionClear();
        }
        env->DeleteLocalRef(bridge);
        return HOST_STATUS_FAILED;
    }

    jint status = call_int_getter(env, response_object, "getStatus");
    if (status == HOST_STATUS_OK) {
        *bytes_written = static_cast<uint32_t>(call_int_getter(env, response_object, "getBytesWritten"));
    }
    if (characteristic_id_value != nullptr) { env->DeleteLocalRef(characteristic_id_value); }
    if (bytes_value != nullptr) { env->DeleteLocalRef(bytes_value); }
    env->DeleteLocalRef(response_object);
    env->DeleteLocalRef(bridge);

    if (env->ExceptionCheck()) {
        env->ExceptionClear();
        return HOST_STATUS_FAILED;
    }

    return static_cast<uint32_t>(status);
}

/// Call the bluetooth-gatt-read-descriptor entrypoint on one registered bridge.
uint32_t call_bluetooth_gatt_read_descriptor(
    JNIEnv *env,
    uint64_t session_handle,
    uint64_t session_id,
    NativeStringRef descriptor_id,
    uint64_t timeout_ns,
    NativeU8Slice bytes,
    uint32_t *bytes_written
) {
    jobject bridge = resolve_bridge(env, session_handle);
    if (bridge == nullptr) {
        return HOST_STATUS_NOT_FOUND;
    }

    jstring descriptor_id_value = java_string_or_null(env, descriptor_id);
    jintArray bytes_value = encode_u8_slice(env, bytes);
    if (bytes_value == nullptr) {
        if (descriptor_id_value != nullptr) { env->DeleteLocalRef(descriptor_id_value); }
        if (env->ExceptionCheck()) {
            env->ExceptionClear();
        }
        env->DeleteLocalRef(bridge);
        return HOST_STATUS_FAILED;
    }

    jobject response_object = env->CallObjectMethod(
        bridge,
        gattReadDescriptor_method,
        static_cast<jlong>(session_id),
        descriptor_id_value,
        static_cast<jlong>(timeout_ns),
        bytes_value
    );
    if (response_object == nullptr) {
        if (descriptor_id_value != nullptr) { env->DeleteLocalRef(descriptor_id_value); }
        if (bytes_value != nullptr) { env->DeleteLocalRef(bytes_value); }
        if (env->ExceptionCheck()) {
            env->ExceptionClear();
        }
        env->DeleteLocalRef(bridge);
        return HOST_STATUS_FAILED;
    }

    jint status = call_int_getter(env, response_object, "getStatus");
    if (status == HOST_STATUS_OK) {
        *bytes_written = static_cast<uint32_t>(call_int_getter(env, response_object, "getBytesWritten"));
    }
    if (descriptor_id_value != nullptr) { env->DeleteLocalRef(descriptor_id_value); }
    if (bytes_value != nullptr) { env->DeleteLocalRef(bytes_value); }
    env->DeleteLocalRef(response_object);
    env->DeleteLocalRef(bridge);

    if (env->ExceptionCheck()) {
        env->ExceptionClear();
        return HOST_STATUS_FAILED;
    }

    return static_cast<uint32_t>(status);
}

/// Call the bluetooth-gatt-write entrypoint on one registered bridge.
uint32_t call_bluetooth_gatt_write(
    JNIEnv *env,
    uint64_t session_handle,
    uint64_t session_id,
    NativeStringRef characteristic_id,
    uint32_t mode,
    NativeU8Slice value,
    uint64_t timeout_ns
) {
    jobject bridge = resolve_bridge(env, session_handle);
    if (bridge == nullptr) {
        return HOST_STATUS_NOT_FOUND;
    }

    jstring characteristic_id_value = java_string_or_null(env, characteristic_id);
    jintArray value_value = encode_u8_slice(env, value);
    if (value_value == nullptr) {
        if (characteristic_id_value != nullptr) { env->DeleteLocalRef(characteristic_id_value); }
        if (env->ExceptionCheck()) {
            env->ExceptionClear();
        }
        env->DeleteLocalRef(bridge);
        return HOST_STATUS_FAILED;
    }

    jint status = env->CallIntMethod(
        bridge,
        gattWrite_method,
        static_cast<jlong>(session_id),
        characteristic_id_value,
        static_cast<jint>(mode),
        value_value,
        static_cast<jlong>(timeout_ns)
    );
    if (characteristic_id_value != nullptr) { env->DeleteLocalRef(characteristic_id_value); }
    if (value_value != nullptr) { env->DeleteLocalRef(value_value); }
    env->DeleteLocalRef(bridge);

    if (env->ExceptionCheck()) {
        env->ExceptionClear();
        return HOST_STATUS_FAILED;
    }

    return static_cast<uint32_t>(status);
}

/// Call the bluetooth-gatt-write-descriptor entrypoint on one registered bridge.
uint32_t call_bluetooth_gatt_write_descriptor(
    JNIEnv *env,
    uint64_t session_handle,
    uint64_t session_id,
    NativeStringRef descriptor_id,
    NativeU8Slice value,
    uint64_t timeout_ns
) {
    jobject bridge = resolve_bridge(env, session_handle);
    if (bridge == nullptr) {
        return HOST_STATUS_NOT_FOUND;
    }

    jstring descriptor_id_value = java_string_or_null(env, descriptor_id);
    jintArray value_value = encode_u8_slice(env, value);
    if (value_value == nullptr) {
        if (descriptor_id_value != nullptr) { env->DeleteLocalRef(descriptor_id_value); }
        if (env->ExceptionCheck()) {
            env->ExceptionClear();
        }
        env->DeleteLocalRef(bridge);
        return HOST_STATUS_FAILED;
    }

    jint status = env->CallIntMethod(
        bridge,
        gattWriteDescriptor_method,
        static_cast<jlong>(session_id),
        descriptor_id_value,
        value_value,
        static_cast<jlong>(timeout_ns)
    );
    if (descriptor_id_value != nullptr) { env->DeleteLocalRef(descriptor_id_value); }
    if (value_value != nullptr) { env->DeleteLocalRef(value_value); }
    env->DeleteLocalRef(bridge);

    if (env->ExceptionCheck()) {
        env->ExceptionClear();
        return HOST_STATUS_FAILED;
    }

    return static_cast<uint32_t>(status);
}

/// Call the bluetooth-gatt-subscribe entrypoint on one registered bridge.
uint32_t call_bluetooth_gatt_subscribe(
    JNIEnv *env,
    uint64_t session_handle,
    uint64_t session_id,
    NativeStringRef characteristic_id,
    uint64_t *subscription_id
) {
    jobject bridge = resolve_bridge(env, session_handle);
    if (bridge == nullptr) {
        return HOST_STATUS_NOT_FOUND;
    }

    jstring characteristic_id_value = java_string_or_null(env, characteristic_id);

    jobject response_object = env->CallObjectMethod(
        bridge,
        gattSubscribe_method,
        static_cast<jlong>(session_id),
        characteristic_id_value
    );
    if (response_object == nullptr) {
        if (characteristic_id_value != nullptr) { env->DeleteLocalRef(characteristic_id_value); }
        if (env->ExceptionCheck()) {
            env->ExceptionClear();
        }
        env->DeleteLocalRef(bridge);
        return HOST_STATUS_FAILED;
    }

    jint status = call_int_getter(env, response_object, "getStatus");
    if (status == HOST_STATUS_OK) {
        *subscription_id = static_cast<uint64_t>(call_long_getter(env, response_object, "getSubscriptionId"));
    }
    if (characteristic_id_value != nullptr) { env->DeleteLocalRef(characteristic_id_value); }
    env->DeleteLocalRef(response_object);
    env->DeleteLocalRef(bridge);

    if (env->ExceptionCheck()) {
        env->ExceptionClear();
        return HOST_STATUS_FAILED;
    }

    return static_cast<uint32_t>(status);
}

/// Call the bluetooth-gatt-unsubscribe entrypoint on one registered bridge.
uint32_t call_bluetooth_gatt_unsubscribe(
    JNIEnv *env,
    uint64_t session_handle,
    uint64_t subscription_id
) {
    jobject bridge = resolve_bridge(env, session_handle);
    if (bridge == nullptr) {
        return HOST_STATUS_NOT_FOUND;
    }


    jint status = env->CallIntMethod(
        bridge,
        gattUnsubscribe_method,
        static_cast<jlong>(subscription_id)
    );
    env->DeleteLocalRef(bridge);

    if (env->ExceptionCheck()) {
        env->ExceptionClear();
        return HOST_STATUS_FAILED;
    }

    return static_cast<uint32_t>(status);
}

/// Call the bluetooth-gatt-read-event entrypoint on one registered bridge.
uint32_t call_bluetooth_gatt_read_event(
    JNIEnv *env,
    uint64_t session_handle,
    uint64_t subscription_id,
    uint64_t timeout_ns,
    NativeU8Slice *event,
    uint64_t *timestamp_ns
) {
    jobject bridge = resolve_bridge(env, session_handle);
    if (bridge == nullptr) {
        return HOST_STATUS_NOT_FOUND;
    }


    jobject response_object = env->CallObjectMethod(
        bridge,
        gattReadEvent_method,
        static_cast<jlong>(subscription_id),
        static_cast<jlong>(timeout_ns)
    );
    if (response_object == nullptr) {
        if (env->ExceptionCheck()) {
            env->ExceptionClear();
        }
        env->DeleteLocalRef(bridge);
        return HOST_STATUS_FAILED;
    }

    clear_result_decode_storage();
    std::deque<std::string> *string_storage = &result_string_storage;

    jint status = call_int_getter(env, response_object, "getStatus");
    if (status == HOST_STATUS_OK) {
        jobject event_value = call_object_getter(env, response_object, "getEvent", "()Ljava/util/List;");
        if (event_value == nullptr) {
            status = HOST_STATUS_FAILED;
        } else {
            *event = decode_u8_list(env, event_value);
            env->DeleteLocalRef(event_value);
        }
        *timestamp_ns = static_cast<uint64_t>(call_long_getter(env, response_object, "getTimestampNs"));
    }
    env->DeleteLocalRef(response_object);
    env->DeleteLocalRef(bridge);

    if (env->ExceptionCheck()) {
        env->ExceptionClear();
        return HOST_STATUS_FAILED;
    }

    return static_cast<uint32_t>(status);
}

/// Call the bluetooth-gatt-try-read-event entrypoint on one registered bridge.
uint32_t call_bluetooth_gatt_try_read_event(
    JNIEnv *env,
    uint64_t session_handle,
    uint64_t subscription_id,
    NativeU8Slice *event,
    uint64_t *timestamp_ns
) {
    jobject bridge = resolve_bridge(env, session_handle);
    if (bridge == nullptr) {
        return HOST_STATUS_NOT_FOUND;
    }


    jobject response_object = env->CallObjectMethod(
        bridge,
        gattTryReadEvent_method,
        static_cast<jlong>(subscription_id)
    );
    if (response_object == nullptr) {
        if (env->ExceptionCheck()) {
            env->ExceptionClear();
        }
        env->DeleteLocalRef(bridge);
        return HOST_STATUS_FAILED;
    }

    clear_result_decode_storage();
    std::deque<std::string> *string_storage = &result_string_storage;

    jint status = call_int_getter(env, response_object, "getStatus");
    if (status == HOST_STATUS_OK) {
        jobject event_value = call_object_getter(env, response_object, "getEvent", "()Ljava/util/List;");
        if (event_value == nullptr) {
            status = HOST_STATUS_FAILED;
        } else {
            *event = decode_u8_list(env, event_value);
            env->DeleteLocalRef(event_value);
        }
        *timestamp_ns = static_cast<uint64_t>(call_long_getter(env, response_object, "getTimestampNs"));
    }
    env->DeleteLocalRef(response_object);
    env->DeleteLocalRef(bridge);

    if (env->ExceptionCheck()) {
        env->ExceptionClear();
        return HOST_STATUS_FAILED;
    }

    return static_cast<uint32_t>(status);
}
