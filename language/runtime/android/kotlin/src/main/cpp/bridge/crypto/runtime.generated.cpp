// generated by generate-bindings: do not edit

#include "../types.h"
#include "../jni.h"
#include "../runtime_abi.generated.h"
#include "callbacks.generated.h"
#include "runtime.generated.h"

#include <deque>
#include <string>
#include <vector>

namespace {

/// Convert one host status code into one runtime status.
RuntimeStatus runtime_status_from_code(uint32_t code) {
    RuntimeStatus status = {
        .code = code,
        .error_id = 0,
    };

    return status;
}

}

/// Forward the `supports_hardware_key` crypto request through one attached bridge session.
uint32_t call_crypto_supports_hardware_key_for_session(
    uint64_t session_handle,
    uint32_t store_kind
) {
    JNIEnv *env = nullptr;
    bool did_attach_thread = false;

    if (!resolve_jni_env(&env, &did_attach_thread)) {
        return HOST_STATUS_FAILED;
    }

    uint32_t status = call_crypto_supports_hardware_key(env, session_handle, store_kind);

    detach_jni_thread(did_attach_thread);

    return status;
}

/// Forward the `generate_hardware_key_pair` crypto request through one attached bridge session.
uint32_t call_crypto_generate_hardware_key_pair_for_session(
    uint64_t session_handle,
    uint32_t store_kind,
    uint32_t key_algorithm,
    uint32_t named_curve,
    uint32_t modulus_bits,
    uint32_t public_exponent,
    NativeStringRef key_label
) {
    JNIEnv *env = nullptr;
    bool did_attach_thread = false;

    if (!resolve_jni_env(&env, &did_attach_thread)) {
        return HOST_STATUS_FAILED;
    }

    uint32_t status = call_crypto_generate_hardware_key_pair(env, session_handle, store_kind, key_algorithm, named_curve, modulus_bits, public_exponent, key_label);

    detach_jni_thread(did_attach_thread);

    return status;
}

/// Forward the `generate_hardware_secret_key` crypto request through one attached bridge session.
uint32_t call_crypto_generate_hardware_secret_key_for_session(
    uint64_t session_handle,
    uint32_t store_kind,
    uint32_t key_algorithm,
    uint32_t digest_algorithm,
    uint32_t key_size_bits,
    uint32_t key_usage_mask,
    NativeStringRef key_label
) {
    JNIEnv *env = nullptr;
    bool did_attach_thread = false;

    if (!resolve_jni_env(&env, &did_attach_thread)) {
        return HOST_STATUS_FAILED;
    }

    uint32_t status = call_crypto_generate_hardware_secret_key(env, session_handle, store_kind, key_algorithm, digest_algorithm, key_size_bits, key_usage_mask, key_label);

    detach_jni_thread(did_attach_thread);

    return status;
}

/// Forward the `export_hardware_public_key` crypto request through one attached bridge session.
uint32_t call_crypto_export_hardware_public_key_for_session(
    uint64_t session_handle,
    uint32_t key_algorithm,
    NativeStringRef key_label,
    NativeU8Slice output,
    uint32_t *output_written
) {
    JNIEnv *env = nullptr;
    bool did_attach_thread = false;

    if (!resolve_jni_env(&env, &did_attach_thread)) {
        return HOST_STATUS_FAILED;
    }

    uint32_t status = call_crypto_export_hardware_public_key(env, session_handle, key_algorithm, key_label, output, output_written);

    detach_jni_thread(did_attach_thread);

    return status;
}

/// Forward the `sign_hardware_key` crypto request through one attached bridge session.
uint32_t call_crypto_sign_hardware_key_for_session(
    uint64_t session_handle,
    uint32_t key_algorithm,
    NativeStringRef key_label,
    uint32_t signature_algorithm,
    uint32_t digest_algorithm,
    uint32_t salt_length_bytes,
    NativeU8Slice payload,
    NativeU8Slice output_signature,
    uint32_t *output_written
) {
    JNIEnv *env = nullptr;
    bool did_attach_thread = false;

    if (!resolve_jni_env(&env, &did_attach_thread)) {
        return HOST_STATUS_FAILED;
    }

    uint32_t status = call_crypto_sign_hardware_key(env, session_handle, key_algorithm, key_label, signature_algorithm, digest_algorithm, salt_length_bytes, payload, output_signature, output_written);

    detach_jni_thread(did_attach_thread);

    return status;
}

/// Forward the `decrypt_hardware_key` crypto request through one attached bridge session.
uint32_t call_crypto_decrypt_hardware_key_for_session(
    uint64_t session_handle,
    uint32_t key_algorithm,
    NativeStringRef key_label,
    uint32_t encryption_algorithm,
    uint32_t digest_algorithm,
    NativeU8Slice label,
    NativeU8Slice payload,
    NativeU8Slice output_plaintext,
    uint32_t *output_written
) {
    JNIEnv *env = nullptr;
    bool did_attach_thread = false;

    if (!resolve_jni_env(&env, &did_attach_thread)) {
        return HOST_STATUS_FAILED;
    }

    uint32_t status = call_crypto_decrypt_hardware_key(env, session_handle, key_algorithm, key_label, encryption_algorithm, digest_algorithm, label, payload, output_plaintext, output_written);

    detach_jni_thread(did_attach_thread);

    return status;
}

/// Forward the `encrypt_hardware_secret_key` crypto request through one attached bridge session.
uint32_t call_crypto_encrypt_hardware_secret_key_for_session(
    uint64_t session_handle,
    uint32_t key_algorithm,
    NativeStringRef key_label,
    uint32_t cipher_algorithm,
    NativeU8Slice nonce,
    NativeU8Slice additional_data,
    uint32_t tag_length_bytes,
    NativeU8Slice payload,
    NativeU8Slice output_ciphertext,
    NativeU8Slice output_tag,
    uint32_t *output_ciphertext_written,
    uint32_t *output_tag_written
) {
    JNIEnv *env = nullptr;
    bool did_attach_thread = false;

    if (!resolve_jni_env(&env, &did_attach_thread)) {
        return HOST_STATUS_FAILED;
    }

    uint32_t status = call_crypto_encrypt_hardware_secret_key(env, session_handle, key_algorithm, key_label, cipher_algorithm, nonce, additional_data, tag_length_bytes, payload, output_ciphertext, output_tag, output_ciphertext_written, output_tag_written);

    detach_jni_thread(did_attach_thread);

    return status;
}

/// Forward the `decrypt_hardware_secret_key` crypto request through one attached bridge session.
uint32_t call_crypto_decrypt_hardware_secret_key_for_session(
    uint64_t session_handle,
    uint32_t key_algorithm,
    NativeStringRef key_label,
    uint32_t cipher_algorithm,
    NativeU8Slice nonce,
    NativeU8Slice additional_data,
    NativeU8Slice tag,
    NativeU8Slice payload,
    NativeU8Slice output_plaintext,
    uint32_t *output_written
) {
    JNIEnv *env = nullptr;
    bool did_attach_thread = false;

    if (!resolve_jni_env(&env, &did_attach_thread)) {
        return HOST_STATUS_FAILED;
    }

    uint32_t status = call_crypto_decrypt_hardware_secret_key(env, session_handle, key_algorithm, key_label, cipher_algorithm, nonce, additional_data, tag, payload, output_plaintext, output_written);

    detach_jni_thread(did_attach_thread);

    return status;
}

/// Forward the `compute_hardware_mac` crypto request through one attached bridge session.
uint32_t call_crypto_compute_hardware_mac_for_session(
    uint64_t session_handle,
    uint32_t key_algorithm,
    NativeStringRef key_label,
    uint32_t mac_algorithm,
    uint32_t digest_algorithm,
    uint32_t tag_length_bytes,
    NativeU8Slice payload,
    NativeU8Slice output_tag,
    uint32_t *output_written
) {
    JNIEnv *env = nullptr;
    bool did_attach_thread = false;

    if (!resolve_jni_env(&env, &did_attach_thread)) {
        return HOST_STATUS_FAILED;
    }

    uint32_t status = call_crypto_compute_hardware_mac(env, session_handle, key_algorithm, key_label, mac_algorithm, digest_algorithm, tag_length_bytes, payload, output_tag, output_written);

    detach_jni_thread(did_attach_thread);

    return status;
}

/// Forward the `derive_hardware_shared_secret` crypto request through one attached bridge session.
uint32_t call_crypto_derive_hardware_shared_secret_for_session(
    uint64_t session_handle,
    uint32_t key_algorithm,
    NativeStringRef key_label,
    uint32_t named_curve,
    NativeU8Slice peer_public_spki,
    NativeU8Slice output_shared_secret,
    uint32_t *output_written
) {
    JNIEnv *env = nullptr;
    bool did_attach_thread = false;

    if (!resolve_jni_env(&env, &did_attach_thread)) {
        return HOST_STATUS_FAILED;
    }

    uint32_t status = call_crypto_derive_hardware_shared_secret(env, session_handle, key_algorithm, key_label, named_curve, peer_public_spki, output_shared_secret, output_written);

    detach_jni_thread(did_attach_thread);

    return status;
}

/// Forward the `delete_hardware_key` crypto request through one attached bridge session.
uint32_t call_crypto_delete_hardware_key_for_session(
    uint64_t session_handle,
    uint32_t key_algorithm,
    NativeStringRef key_label
) {
    JNIEnv *env = nullptr;
    bool did_attach_thread = false;

    if (!resolve_jni_env(&env, &did_attach_thread)) {
        return HOST_STATUS_FAILED;
    }

    uint32_t status = call_crypto_delete_hardware_key(env, session_handle, key_algorithm, key_label);

    detach_jni_thread(did_attach_thread);

    return status;
}

/// Forward the `import_certificate` crypto request through one attached bridge session.
uint32_t call_crypto_import_certificate_for_session(
    uint64_t session_handle,
    uint32_t store_kind,
    NativeU8Slice certificate_der
) {
    JNIEnv *env = nullptr;
    bool did_attach_thread = false;

    if (!resolve_jni_env(&env, &did_attach_thread)) {
        return HOST_STATUS_FAILED;
    }

    uint32_t status = call_crypto_import_certificate(env, session_handle, store_kind, certificate_der);

    detach_jni_thread(did_attach_thread);

    return status;
}

/// Forward the `delete_certificate` crypto request through one attached bridge session.
uint32_t call_crypto_delete_certificate_for_session(
    uint64_t session_handle,
    uint32_t store_kind,
    NativeU8Slice certificate_der
) {
    JNIEnv *env = nullptr;
    bool did_attach_thread = false;

    if (!resolve_jni_env(&env, &did_attach_thread)) {
        return HOST_STATUS_FAILED;
    }

    uint32_t status = call_crypto_delete_certificate(env, session_handle, store_kind, certificate_der);

    detach_jni_thread(did_attach_thread);

    return status;
}

/// Build one Android host crypto callback bundle.
AndroidHostCryptoCallbacks make_crypto_callbacks() {
    return AndroidHostCryptoCallbacks {
        .supports_hardware_key = call_crypto_supports_hardware_key_for_session,
        .generate_hardware_key_pair = call_crypto_generate_hardware_key_pair_for_session,
        .generate_hardware_secret_key = call_crypto_generate_hardware_secret_key_for_session,
        .export_hardware_public_key = call_crypto_export_hardware_public_key_for_session,
        .sign_hardware_key = call_crypto_sign_hardware_key_for_session,
        .decrypt_hardware_key = call_crypto_decrypt_hardware_key_for_session,
        .encrypt_hardware_secret_key = call_crypto_encrypt_hardware_secret_key_for_session,
        .decrypt_hardware_secret_key = call_crypto_decrypt_hardware_secret_key_for_session,
        .compute_hardware_mac = call_crypto_compute_hardware_mac_for_session,
        .derive_hardware_shared_secret = call_crypto_derive_hardware_shared_secret_for_session,
        .delete_hardware_key = call_crypto_delete_hardware_key_for_session,
        .import_certificate = call_crypto_import_certificate_for_session,
        .delete_certificate = call_crypto_delete_certificate_for_session,
    };
}
