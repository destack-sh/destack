// generated by generate-bindings: do not edit

#include "../types.h"
#include "../jni.h"
#include "../loader.h"
#include "callbacks.generated.h"
#include "runtime.generated.h"

#include <deque>
#include <string>
#include <vector>

namespace {

/// Convert one host status code into one runtime status.
RuntimeStatus runtime_status_from_code(uint32_t code) {
    RuntimeStatus status = {
        .code = code,
        .error_id = 0,
    };

    return status;
}

}

/// Forward the `list` contact request through one attached bridge session.
uint32_t call_contact_list_for_session(
    uint64_t session_handle,
    HostContactQuery query,
    HostContactPageResponse *response
) {
    JNIEnv *env = nullptr;
    bool did_attach_thread = false;

    if (!resolve_jni_env(&env, &did_attach_thread)) {
        return HOST_STATUS_FAILED;
    }

    uint32_t status = call_contact_list(env, session_handle, query, response);

    detach_jni_thread(did_attach_thread);

    return status;
}

/// Forward the `search` contact request through one attached bridge session.
uint32_t call_contact_search_for_session(
    uint64_t session_handle,
    NativeStringRef query_text,
    HostContactQuery query,
    HostContactPageResponse *response
) {
    JNIEnv *env = nullptr;
    bool did_attach_thread = false;

    if (!resolve_jni_env(&env, &did_attach_thread)) {
        return HOST_STATUS_FAILED;
    }

    uint32_t status = call_contact_search(env, session_handle, query_text, query, response);

    detach_jni_thread(did_attach_thread);

    return status;
}

/// Forward the `read` contact request through one attached bridge session.
uint32_t call_contact_read_for_session(
    uint64_t session_handle,
    NativeStringRef id,
    HostContactResponse *response
) {
    JNIEnv *env = nullptr;
    bool did_attach_thread = false;

    if (!resolve_jni_env(&env, &did_attach_thread)) {
        return HOST_STATUS_FAILED;
    }

    uint32_t status = call_contact_read(env, session_handle, id, response);

    detach_jni_thread(did_attach_thread);

    return status;
}

/// Forward the `create` contact request through one attached bridge session.
uint32_t call_contact_create_for_session(
    uint64_t session_handle,
    HostContactDraft draft,
    HostContactCreateResponse *response
) {
    JNIEnv *env = nullptr;
    bool did_attach_thread = false;

    if (!resolve_jni_env(&env, &did_attach_thread)) {
        return HOST_STATUS_FAILED;
    }

    uint32_t status = call_contact_create(env, session_handle, draft, response);

    detach_jni_thread(did_attach_thread);

    return status;
}

/// Forward the `update` contact request through one attached bridge session.
uint32_t call_contact_update_for_session(
    uint64_t session_handle,
    NativeStringRef id,
    HostContactDraft draft
) {
    JNIEnv *env = nullptr;
    bool did_attach_thread = false;

    if (!resolve_jni_env(&env, &did_attach_thread)) {
        return HOST_STATUS_FAILED;
    }

    uint32_t status = call_contact_update(env, session_handle, id, draft);

    detach_jni_thread(did_attach_thread);

    return status;
}

/// Forward the `delete_contact` contact request through one attached bridge session.
uint32_t call_contact_delete_contact_for_session(
    uint64_t session_handle,
    NativeStringRef id
) {
    JNIEnv *env = nullptr;
    bool did_attach_thread = false;

    if (!resolve_jni_env(&env, &did_attach_thread)) {
        return HOST_STATUS_FAILED;
    }

    uint32_t status = call_contact_delete_contact(env, session_handle, id);

    detach_jni_thread(did_attach_thread);

    return status;
}

/// Build one Android host contact callback bundle.
AndroidHostContactCallbacks make_contact_callbacks() {
    return AndroidHostContactCallbacks {
        .list = call_contact_list_for_session,
        .search = call_contact_search_for_session,
        .read = call_contact_read_for_session,
        .create = call_contact_create_for_session,
        .update = call_contact_update_for_session,
        .delete_contact = call_contact_delete_contact_for_session,
    };
}
