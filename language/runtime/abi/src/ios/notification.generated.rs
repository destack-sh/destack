// generated by generate-bindings: do not edit

use crate::diagnostic::RuntimeStatus;
use crate::host::abi::notification::{HostNotificationEvent, HostNotificationRequest};
use crate::host::apple::abi::ingress::destack_host_ios_notify_notification_event as runtime_notify_notification_event;
use crate::host::apple::abi::notification::{
    destack_host_ios_notification_cancel as runtime_notification_cancel,
    destack_host_ios_notification_cancel_all as runtime_notification_cancel_all,
    destack_host_ios_notification_post as runtime_notification_post,
};
use crate::platform::abi::NativeStringRef;

/// Forward the `post` notification request through the ABI artifact.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn destack_host_ios_notification_post(
    session_handle: u64,
    request: HostNotificationRequest,
) -> u32 {
    unsafe { runtime_notification_post(session_handle, request) }
}

/// Forward the `cancel` notification request through the ABI artifact.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn destack_host_ios_notification_cancel(
    session_handle: u64,
    identifier: NativeStringRef,
) -> u32 {
    unsafe { runtime_notification_cancel(session_handle, identifier) }
}

/// Forward the `cancel_all` notification request through the ABI artifact.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn destack_host_ios_notification_cancel_all(session_handle: u64) -> u32 {
    unsafe { runtime_notification_cancel_all(session_handle) }
}

/// Forward the `notify_notification_event` ingress through the ABI artifact.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn destack_host_ios_notify_notification_event(
    runtime_id: u64,
    event: HostNotificationEvent,
) -> RuntimeStatus {
    unsafe { runtime_notify_notification_event(runtime_id, event) }
}
