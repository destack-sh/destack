// generated by generate-bindings: do not edit

use crate::host::abi::contact::HostContactCreateResponse;
use crate::host::abi::contact::HostContactDraft;
use crate::host::abi::contact::HostContactPageResponse;
use crate::host::abi::contact::HostContactQuery;
use crate::host::abi::contact::HostContactResponse;
use crate::host::apple::abi::contact::destack_host_ios_contact_create as runtime_contact_create;
use crate::host::apple::abi::contact::destack_host_ios_contact_delete_contact as runtime_contact_delete_contact;
use crate::host::apple::abi::contact::destack_host_ios_contact_list as runtime_contact_list;
use crate::host::apple::abi::contact::destack_host_ios_contact_read as runtime_contact_read;
use crate::host::apple::abi::contact::destack_host_ios_contact_search as runtime_contact_search;
use crate::host::apple::abi::contact::destack_host_ios_contact_update as runtime_contact_update;
use crate::platform::abi::NativeStringRef;

/// Forward the `list` contact request through the ABI crate.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn destack_host_ios_contact_list(
    session_handle: u64,
    query: HostContactQuery,
    response: *mut HostContactPageResponse,
) -> u32 {
    unsafe {
        runtime_contact_list(
            session_handle,
            query,
            response,
        )
    }
}

/// Forward the `search` contact request through the ABI crate.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn destack_host_ios_contact_search(
    session_handle: u64,
    query_text: NativeStringRef,
    query: HostContactQuery,
    response: *mut HostContactPageResponse,
) -> u32 {
    unsafe {
        runtime_contact_search(
            session_handle,
            query_text,
            query,
            response,
        )
    }
}

/// Forward the `read` contact request through the ABI crate.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn destack_host_ios_contact_read(
    session_handle: u64,
    id: NativeStringRef,
    response: *mut HostContactResponse,
) -> u32 {
    unsafe {
        runtime_contact_read(
            session_handle,
            id,
            response,
        )
    }
}

/// Forward the `create` contact request through the ABI crate.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn destack_host_ios_contact_create(
    session_handle: u64,
    draft: HostContactDraft,
    response: *mut HostContactCreateResponse,
) -> u32 {
    unsafe {
        runtime_contact_create(
            session_handle,
            draft,
            response,
        )
    }
}

/// Forward the `update` contact request through the ABI crate.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn destack_host_ios_contact_update(
    session_handle: u64,
    id: NativeStringRef,
    draft: HostContactDraft,
) -> u32 {
    unsafe {
        runtime_contact_update(
            session_handle,
            id,
            draft,
        )
    }
}

/// Forward the `delete_contact` contact request through the ABI crate.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn destack_host_ios_contact_delete_contact(
    session_handle: u64,
    id: NativeStringRef,
) -> u32 {
    unsafe {
        runtime_contact_delete_contact(
            session_handle,
            id,
        )
    }
}
