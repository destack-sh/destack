// generated by generate-bindings: do not edit

use crate::diagnostic::RuntimeStatus;
use crate::host::abi::background::HostBackgroundCompleteRequest;
use crate::host::abi::background::HostBackgroundEvent;
use crate::host::abi::background::HostBackgroundListResponse;
use crate::host::abi::background::HostBackgroundStatusResponse;
use crate::host::abi::background::HostBackgroundTaskOptions;
use crate::host::abi::background::HostBackgroundTriggerTestRequest;
use crate::host::abi::background::HostBackgroundTriggerTestResponse;
use crate::host::abi::background::HostBackgroundUnregisterRequest;
use crate::host::apple::abi::background::destack_host_ios_background_complete as runtime_background_complete;
use crate::host::apple::abi::background::destack_host_ios_background_list as runtime_background_list;
use crate::host::apple::abi::background::destack_host_ios_background_register_task as runtime_background_register_task;
use crate::host::apple::abi::background::destack_host_ios_background_status as runtime_background_status;
use crate::host::apple::abi::background::destack_host_ios_background_trigger_test as runtime_background_trigger_test;
use crate::host::apple::abi::background::destack_host_ios_background_unregister as runtime_background_unregister;
use crate::host::apple::abi::ingress::destack_host_ios_notify_background_event as runtime_notify_background_event;

/// Forward the `status` background request through the ABI crate.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn destack_host_ios_background_status(
    session_handle: u64,
    response: *mut HostBackgroundStatusResponse,
) -> u32 {
    unsafe {
        runtime_background_status(
            session_handle,
            response,
        )
    }
}

/// Forward the `list` background request through the ABI crate.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn destack_host_ios_background_list(
    session_handle: u64,
    response: *mut HostBackgroundListResponse,
) -> u32 {
    unsafe {
        runtime_background_list(
            session_handle,
            response,
        )
    }
}

/// Forward the `register_task` background request through the ABI crate.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn destack_host_ios_background_register_task(
    session_handle: u64,
    request: HostBackgroundTaskOptions,
) -> u32 {
    unsafe {
        runtime_background_register_task(
            session_handle,
            request,
        )
    }
}

/// Forward the `unregister` background request through the ABI crate.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn destack_host_ios_background_unregister(
    session_handle: u64,
    request: HostBackgroundUnregisterRequest,
) -> u32 {
    unsafe {
        runtime_background_unregister(
            session_handle,
            request,
        )
    }
}

/// Forward the `trigger_test` background request through the ABI crate.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn destack_host_ios_background_trigger_test(
    session_handle: u64,
    request: HostBackgroundTriggerTestRequest,
    response: *mut HostBackgroundTriggerTestResponse,
) -> u32 {
    unsafe {
        runtime_background_trigger_test(
            session_handle,
            request,
            response,
        )
    }
}

/// Forward the `complete` background request through the ABI crate.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn destack_host_ios_background_complete(
    session_handle: u64,
    request: HostBackgroundCompleteRequest,
) -> u32 {
    unsafe {
        runtime_background_complete(
            session_handle,
            request,
        )
    }
}

/// Forward the `notify_background_event` ingress through the ABI crate.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn destack_host_ios_notify_background_event(
    runtime_id: u64,
    event: HostBackgroundEvent,
) -> RuntimeStatus {
    unsafe { runtime_notify_background_event(
        runtime_id,
        event,
    ) }
}
