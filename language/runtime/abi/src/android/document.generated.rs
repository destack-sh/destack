// generated by generate-bindings: do not edit

use crate::diagnostic::RuntimeStatus;
use crate::host::abi::document::HostDocumentRequest;
use crate::host::android::abi::document::destack_host_android_document_pick as runtime_document_pick;
use crate::host::android::abi::ingress::destack_host_android_notify_document_result as runtime_notify_document_result;
use crate::platform::abi::NativeSlice;
use crate::platform::os::DocumentDescriptorValue;

/// Forward the `pick` document request through the ABI artifact.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn destack_host_android_document_pick(
    session_handle: u64,
    request: HostDocumentRequest,
) -> u32 {
    unsafe {
        runtime_document_pick(
            session_handle,
            request,
        )
    }
}

/// Forward the `notify_document_result` ingress through the ABI artifact.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn destack_host_android_notify_document_result(
    runtime_id: u64,
    request_id: u64,
    documents: NativeSlice<DocumentDescriptorValue>,
) -> RuntimeStatus {
    unsafe { runtime_notify_document_result(
        runtime_id,
        request_id,
        documents,
    ) }
}
