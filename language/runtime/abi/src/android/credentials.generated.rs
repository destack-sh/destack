// generated by generate-bindings: do not edit

use crate::host::android::abi::credentials::destack_host_android_credentials_authenticate as runtime_credentials_authenticate;
use crate::host::android::abi::credentials::destack_host_android_credentials_contains as runtime_credentials_contains;
use crate::host::android::abi::credentials::destack_host_android_credentials_delete as runtime_credentials_delete;
use crate::host::android::abi::credentials::destack_host_android_credentials_read as runtime_credentials_read;
use crate::host::android::abi::credentials::destack_host_android_credentials_write as runtime_credentials_write;
use crate::platform::abi::NativeSlice;
use crate::platform::abi::NativeStringRef;

/// Forward the `read` credentials request through the ABI artifact.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn destack_host_android_credentials_read(
    session_handle: u64,
    service: NativeStringRef,
    account: NativeStringRef,
    access_group: NativeStringRef,
    require_authentication: bool,
    output: NativeSlice<u8>,
    output_written: *mut u32,
    created_unix_ns: *mut u64,
    modified_unix_ns: *mut u64,
) -> u32 {
    unsafe {
        runtime_credentials_read(
            session_handle,
            service,
            account,
            access_group,
            require_authentication,
            output,
            output_written,
            created_unix_ns,
            modified_unix_ns,
        )
    }
}

/// Forward the `write` credentials request through the ABI artifact.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn destack_host_android_credentials_write(
    session_handle: u64,
    service: NativeStringRef,
    account: NativeStringRef,
    access_group: NativeStringRef,
    payload: NativeSlice<u8>,
    accessibility: u32,
    authentication_policy: u32,
    replace_existing: bool,
) -> u32 {
    unsafe {
        runtime_credentials_write(
            session_handle,
            service,
            account,
            access_group,
            payload,
            accessibility,
            authentication_policy,
            replace_existing,
        )
    }
}

/// Forward the `delete` credentials request through the ABI artifact.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn destack_host_android_credentials_delete(
    session_handle: u64,
    service: NativeStringRef,
    account: NativeStringRef,
    access_group: NativeStringRef,
) -> u32 {
    unsafe {
        runtime_credentials_delete(
            session_handle,
            service,
            account,
            access_group,
        )
    }
}

/// Forward the `contains` credentials request through the ABI artifact.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn destack_host_android_credentials_contains(
    session_handle: u64,
    service: NativeStringRef,
    account: NativeStringRef,
    access_group: NativeStringRef,
    is_present: *mut bool,
) -> u32 {
    unsafe {
        runtime_credentials_contains(
            session_handle,
            service,
            account,
            access_group,
            is_present,
        )
    }
}

/// Forward the `authenticate` credentials request through the ABI artifact.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn destack_host_android_credentials_authenticate(
    session_handle: u64,
    title: NativeStringRef,
    subtitle: NativeStringRef,
    message: NativeStringRef,
    requirement: u32,
    authenticated: *mut bool,
    mechanism: *mut u32,
) -> u32 {
    unsafe {
        runtime_credentials_authenticate(
            session_handle,
            title,
            subtitle,
            message,
            requirement,
            authenticated,
            mechanism,
        )
    }
}
