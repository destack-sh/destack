// generated by generate-bindings: do not edit

use crate::host::android::abi::usb::{
    AndroidHostUsbDeviceDescriptorHeader, AndroidHostUsbHotplugEventHeader,
    destack_host_android_usb_device_list as runtime_usb_device_list,
    destack_host_android_usb_open as runtime_usb_open,
    destack_host_android_usb_watch_close as runtime_usb_watch_close,
    destack_host_android_usb_watch_open as runtime_usb_watch_open,
    destack_host_android_usb_watch_read as runtime_usb_watch_read,
    destack_host_android_usb_watch_try_read as runtime_usb_watch_try_read,
};
use crate::platform::abi::{NativeSlice, NativeStringRef};

/// Forward the `device_list` usb request through the ABI artifact.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn destack_host_android_usb_device_list(
    session_handle: u64,
    devices: NativeSlice<AndroidHostUsbDeviceDescriptorHeader>,
    device_count_written: *mut u32,
    string_bytes: NativeSlice<u8>,
    string_bytes_written: *mut u32,
    port_bytes: NativeSlice<u8>,
    port_bytes_written: *mut u32,
) -> u32 {
    unsafe {
        runtime_usb_device_list(
            session_handle,
            devices,
            device_count_written,
            string_bytes,
            string_bytes_written,
            port_bytes,
            port_bytes_written,
        )
    }
}

/// Forward the `watch_open` usb request through the ABI artifact.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn destack_host_android_usb_watch_open(
    session_handle: u64,
    watch_id: *mut u64,
) -> u32 {
    unsafe { runtime_usb_watch_open(session_handle, watch_id) }
}

/// Forward the `watch_close` usb request through the ABI artifact.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn destack_host_android_usb_watch_close(
    session_handle: u64,
    watch_id: u64,
) -> u32 {
    unsafe { runtime_usb_watch_close(session_handle, watch_id) }
}

/// Forward the `watch_read` usb request through the ABI artifact.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn destack_host_android_usb_watch_read(
    session_handle: u64,
    watch_id: u64,
    timeout_ns: u64,
    event: *mut AndroidHostUsbHotplugEventHeader,
    string_bytes: NativeSlice<u8>,
    string_bytes_written: *mut u32,
    port_bytes: NativeSlice<u8>,
    port_bytes_written: *mut u32,
) -> u32 {
    unsafe {
        runtime_usb_watch_read(
            session_handle,
            watch_id,
            timeout_ns,
            event,
            string_bytes,
            string_bytes_written,
            port_bytes,
            port_bytes_written,
        )
    }
}

/// Forward the `watch_try_read` usb request through the ABI artifact.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn destack_host_android_usb_watch_try_read(
    session_handle: u64,
    watch_id: u64,
    event: *mut AndroidHostUsbHotplugEventHeader,
    string_bytes: NativeSlice<u8>,
    string_bytes_written: *mut u32,
    port_bytes: NativeSlice<u8>,
    port_bytes_written: *mut u32,
) -> u32 {
    unsafe {
        runtime_usb_watch_try_read(
            session_handle,
            watch_id,
            event,
            string_bytes,
            string_bytes_written,
            port_bytes,
            port_bytes_written,
        )
    }
}

/// Forward the `open` usb request through the ABI artifact.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn destack_host_android_usb_open(
    session_handle: u64,
    id: NativeStringRef,
    file_descriptor: *mut i32,
) -> u32 {
    unsafe { runtime_usb_open(session_handle, id, file_descriptor) }
}
