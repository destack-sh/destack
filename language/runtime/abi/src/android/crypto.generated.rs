// generated by generate-bindings: do not edit

use crate::host::android::abi::crypto::{
    destack_host_android_crypto_compute_hardware_mac as runtime_crypto_compute_hardware_mac,
    destack_host_android_crypto_decrypt_hardware_key as runtime_crypto_decrypt_hardware_key,
    destack_host_android_crypto_decrypt_hardware_secret_key as runtime_crypto_decrypt_hardware_secret_key,
    destack_host_android_crypto_delete_certificate as runtime_crypto_delete_certificate,
    destack_host_android_crypto_delete_hardware_key as runtime_crypto_delete_hardware_key,
    destack_host_android_crypto_derive_hardware_shared_secret as runtime_crypto_derive_hardware_shared_secret,
    destack_host_android_crypto_encrypt_hardware_secret_key as runtime_crypto_encrypt_hardware_secret_key,
    destack_host_android_crypto_export_hardware_public_key as runtime_crypto_export_hardware_public_key,
    destack_host_android_crypto_generate_hardware_key_pair as runtime_crypto_generate_hardware_key_pair,
    destack_host_android_crypto_generate_hardware_secret_key as runtime_crypto_generate_hardware_secret_key,
    destack_host_android_crypto_import_certificate as runtime_crypto_import_certificate,
    destack_host_android_crypto_sign_hardware_key as runtime_crypto_sign_hardware_key,
    destack_host_android_crypto_supports_hardware_key as runtime_crypto_supports_hardware_key,
};
use crate::platform::abi::{NativeSlice, NativeStringRef};

/// Forward the `supports_hardware_key` crypto request through the ABI crate.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn destack_host_android_crypto_supports_hardware_key(
    session_handle: u64,
    store_kind: u32,
) -> u32 {
    unsafe { runtime_crypto_supports_hardware_key(session_handle, store_kind) }
}

/// Forward the `generate_hardware_key_pair` crypto request through the ABI crate.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn destack_host_android_crypto_generate_hardware_key_pair(
    session_handle: u64,
    store_kind: u32,
    key_algorithm: u32,
    named_curve: u32,
    modulus_bits: u32,
    public_exponent: u32,
    key_label: NativeStringRef,
) -> u32 {
    unsafe {
        runtime_crypto_generate_hardware_key_pair(
            session_handle,
            store_kind,
            key_algorithm,
            named_curve,
            modulus_bits,
            public_exponent,
            key_label,
        )
    }
}

/// Forward the `generate_hardware_secret_key` crypto request through the ABI crate.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn destack_host_android_crypto_generate_hardware_secret_key(
    session_handle: u64,
    store_kind: u32,
    key_algorithm: u32,
    digest_algorithm: u32,
    key_size_bits: u32,
    key_usage_mask: u32,
    key_label: NativeStringRef,
) -> u32 {
    unsafe {
        runtime_crypto_generate_hardware_secret_key(
            session_handle,
            store_kind,
            key_algorithm,
            digest_algorithm,
            key_size_bits,
            key_usage_mask,
            key_label,
        )
    }
}

/// Forward the `export_hardware_public_key` crypto request through the ABI crate.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn destack_host_android_crypto_export_hardware_public_key(
    session_handle: u64,
    key_algorithm: u32,
    key_label: NativeStringRef,
    output: NativeSlice<u8>,
    output_written: *mut u32,
) -> u32 {
    unsafe {
        runtime_crypto_export_hardware_public_key(
            session_handle,
            key_algorithm,
            key_label,
            output,
            output_written,
        )
    }
}

/// Forward the `sign_hardware_key` crypto request through the ABI crate.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn destack_host_android_crypto_sign_hardware_key(
    session_handle: u64,
    key_algorithm: u32,
    key_label: NativeStringRef,
    signature_algorithm: u32,
    digest_algorithm: u32,
    salt_length_bytes: u32,
    payload: NativeSlice<u8>,
    output_signature: NativeSlice<u8>,
    output_written: *mut u32,
) -> u32 {
    unsafe {
        runtime_crypto_sign_hardware_key(
            session_handle,
            key_algorithm,
            key_label,
            signature_algorithm,
            digest_algorithm,
            salt_length_bytes,
            payload,
            output_signature,
            output_written,
        )
    }
}

/// Forward the `decrypt_hardware_key` crypto request through the ABI crate.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn destack_host_android_crypto_decrypt_hardware_key(
    session_handle: u64,
    key_algorithm: u32,
    key_label: NativeStringRef,
    encryption_algorithm: u32,
    digest_algorithm: u32,
    label: NativeSlice<u8>,
    payload: NativeSlice<u8>,
    output_plaintext: NativeSlice<u8>,
    output_written: *mut u32,
) -> u32 {
    unsafe {
        runtime_crypto_decrypt_hardware_key(
            session_handle,
            key_algorithm,
            key_label,
            encryption_algorithm,
            digest_algorithm,
            label,
            payload,
            output_plaintext,
            output_written,
        )
    }
}

/// Forward the `encrypt_hardware_secret_key` crypto request through the ABI crate.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn destack_host_android_crypto_encrypt_hardware_secret_key(
    session_handle: u64,
    key_algorithm: u32,
    key_label: NativeStringRef,
    cipher_algorithm: u32,
    nonce: NativeSlice<u8>,
    additional_data: NativeSlice<u8>,
    tag_length_bytes: u32,
    payload: NativeSlice<u8>,
    output_ciphertext: NativeSlice<u8>,
    output_tag: NativeSlice<u8>,
    output_ciphertext_written: *mut u32,
    output_tag_written: *mut u32,
) -> u32 {
    unsafe {
        runtime_crypto_encrypt_hardware_secret_key(
            session_handle,
            key_algorithm,
            key_label,
            cipher_algorithm,
            nonce,
            additional_data,
            tag_length_bytes,
            payload,
            output_ciphertext,
            output_tag,
            output_ciphertext_written,
            output_tag_written,
        )
    }
}

/// Forward the `decrypt_hardware_secret_key` crypto request through the ABI crate.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn destack_host_android_crypto_decrypt_hardware_secret_key(
    session_handle: u64,
    key_algorithm: u32,
    key_label: NativeStringRef,
    cipher_algorithm: u32,
    nonce: NativeSlice<u8>,
    additional_data: NativeSlice<u8>,
    tag: NativeSlice<u8>,
    payload: NativeSlice<u8>,
    output_plaintext: NativeSlice<u8>,
    output_written: *mut u32,
) -> u32 {
    unsafe {
        runtime_crypto_decrypt_hardware_secret_key(
            session_handle,
            key_algorithm,
            key_label,
            cipher_algorithm,
            nonce,
            additional_data,
            tag,
            payload,
            output_plaintext,
            output_written,
        )
    }
}

/// Forward the `compute_hardware_mac` crypto request through the ABI crate.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn destack_host_android_crypto_compute_hardware_mac(
    session_handle: u64,
    key_algorithm: u32,
    key_label: NativeStringRef,
    mac_algorithm: u32,
    digest_algorithm: u32,
    tag_length_bytes: u32,
    payload: NativeSlice<u8>,
    output_tag: NativeSlice<u8>,
    output_written: *mut u32,
) -> u32 {
    unsafe {
        runtime_crypto_compute_hardware_mac(
            session_handle,
            key_algorithm,
            key_label,
            mac_algorithm,
            digest_algorithm,
            tag_length_bytes,
            payload,
            output_tag,
            output_written,
        )
    }
}

/// Forward the `derive_hardware_shared_secret` crypto request through the ABI crate.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn destack_host_android_crypto_derive_hardware_shared_secret(
    session_handle: u64,
    key_algorithm: u32,
    key_label: NativeStringRef,
    named_curve: u32,
    peer_public_spki: NativeSlice<u8>,
    output_shared_secret: NativeSlice<u8>,
    output_written: *mut u32,
) -> u32 {
    unsafe {
        runtime_crypto_derive_hardware_shared_secret(
            session_handle,
            key_algorithm,
            key_label,
            named_curve,
            peer_public_spki,
            output_shared_secret,
            output_written,
        )
    }
}

/// Forward the `delete_hardware_key` crypto request through the ABI crate.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn destack_host_android_crypto_delete_hardware_key(
    session_handle: u64,
    key_algorithm: u32,
    key_label: NativeStringRef,
) -> u32 {
    unsafe { runtime_crypto_delete_hardware_key(session_handle, key_algorithm, key_label) }
}

/// Forward the `import_certificate` crypto request through the ABI crate.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn destack_host_android_crypto_import_certificate(
    session_handle: u64,
    store_kind: u32,
    certificate_der: NativeSlice<u8>,
) -> u32 {
    unsafe { runtime_crypto_import_certificate(session_handle, store_kind, certificate_der) }
}

/// Forward the `delete_certificate` crypto request through the ABI crate.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn destack_host_android_crypto_delete_certificate(
    session_handle: u64,
    store_kind: u32,
    certificate_der: NativeSlice<u8>,
) -> u32 {
    unsafe { runtime_crypto_delete_certificate(session_handle, store_kind, certificate_der) }
}
