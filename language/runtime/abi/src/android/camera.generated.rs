// generated by generate-bindings: do not edit

use crate::host::abi::camera::AndroidHostCameraDeviceDescriptorHeader;
use crate::host::abi::camera::AndroidHostCameraFrameHeader;
use crate::host::abi::camera::AndroidHostCameraRecordingCapabilitiesHeader;
use crate::host::abi::camera::AndroidHostCameraRecordingOptionsHeader;
use crate::host::abi::camera::AndroidHostCameraStreamCapabilityHeader;
use crate::host::abi::camera::AndroidHostCameraStreamConfigHeader;
use crate::host::android::abi::camera::destack_host_android_camera_device_close as runtime_camera_device_close;
use crate::host::android::abi::camera::destack_host_android_camera_device_list as runtime_camera_device_list;
use crate::host::android::abi::camera::destack_host_android_camera_device_open as runtime_camera_device_open;
use crate::host::android::abi::camera::destack_host_android_camera_stream_capability_list as runtime_camera_stream_capability_list;
use crate::host::android::abi::camera::destack_host_android_camera_stream_close as runtime_camera_stream_close;
use crate::host::android::abi::camera::destack_host_android_camera_stream_config as runtime_camera_stream_config;
use crate::host::android::abi::camera::destack_host_android_camera_stream_config_list as runtime_camera_stream_config_list;
use crate::host::android::abi::camera::destack_host_android_camera_stream_get_f64 as runtime_camera_stream_get_f64;
use crate::host::android::abi::camera::destack_host_android_camera_stream_get_range_f64 as runtime_camera_stream_get_range_f64;
use crate::host::android::abi::camera::destack_host_android_camera_stream_get_range_u32 as runtime_camera_stream_get_range_u32;
use crate::host::android::abi::camera::destack_host_android_camera_stream_get_range_u64 as runtime_camera_stream_get_range_u64;
use crate::host::android::abi::camera::destack_host_android_camera_stream_get_u32 as runtime_camera_stream_get_u32;
use crate::host::android::abi::camera::destack_host_android_camera_stream_get_u64 as runtime_camera_stream_get_u64;
use crate::host::android::abi::camera::destack_host_android_camera_stream_open as runtime_camera_stream_open;
use crate::host::android::abi::camera::destack_host_android_camera_stream_pause_recording as runtime_camera_stream_pause_recording;
use crate::host::android::abi::camera::destack_host_android_camera_stream_read as runtime_camera_stream_read;
use crate::host::android::abi::camera::destack_host_android_camera_stream_recording_capabilities as runtime_camera_stream_recording_capabilities;
use crate::host::android::abi::camera::destack_host_android_camera_stream_resume_recording as runtime_camera_stream_resume_recording;
use crate::host::android::abi::camera::destack_host_android_camera_stream_set_f64 as runtime_camera_stream_set_f64;
use crate::host::android::abi::camera::destack_host_android_camera_stream_set_u32 as runtime_camera_stream_set_u32;
use crate::host::android::abi::camera::destack_host_android_camera_stream_set_u64 as runtime_camera_stream_set_u64;
use crate::host::android::abi::camera::destack_host_android_camera_stream_start as runtime_camera_stream_start;
use crate::host::android::abi::camera::destack_host_android_camera_stream_start_recording as runtime_camera_stream_start_recording;
use crate::host::android::abi::camera::destack_host_android_camera_stream_stop as runtime_camera_stream_stop;
use crate::host::android::abi::camera::destack_host_android_camera_stream_stop_recording as runtime_camera_stream_stop_recording;
use crate::host::android::abi::camera::destack_host_android_camera_stream_take_photo as runtime_camera_stream_take_photo;
use crate::host::android::abi::camera::destack_host_android_camera_stream_try_read as runtime_camera_stream_try_read;
use crate::platform::abi::NativeSlice;
use crate::platform::abi::NativeStringRef;

/// Forward the `device_list` camera request through the ABI crate.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn destack_host_android_camera_device_list(
    session_handle: u64,
    devices: NativeSlice<AndroidHostCameraDeviceDescriptorHeader>,
    device_count_written: *mut u32,
    string_bytes: NativeSlice<u8>,
    string_bytes_written: *mut u32,
) -> u32 {
    unsafe {
        runtime_camera_device_list(
            session_handle,
            devices,
            device_count_written,
            string_bytes,
            string_bytes_written,
        )
    }
}

/// Forward the `device_open` camera request through the ABI crate.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn destack_host_android_camera_device_open(
    session_handle: u64,
    id: NativeStringRef,
    session_id: *mut u64,
) -> u32 {
    unsafe {
        runtime_camera_device_open(
            session_handle,
            id,
            session_id,
        )
    }
}

/// Forward the `device_close` camera request through the ABI crate.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn destack_host_android_camera_device_close(
    session_handle: u64,
    session_id: u64,
) -> u32 {
    unsafe {
        runtime_camera_device_close(
            session_handle,
            session_id,
        )
    }
}

/// Forward the `stream_config_list` camera request through the ABI crate.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn destack_host_android_camera_stream_config_list(
    session_handle: u64,
    session_id: u64,
    configs: NativeSlice<AndroidHostCameraStreamConfigHeader>,
    config_count_written: *mut u32,
) -> u32 {
    unsafe {
        runtime_camera_stream_config_list(
            session_handle,
            session_id,
            configs,
            config_count_written,
        )
    }
}

/// Forward the `stream_capability_list` camera request through the ABI crate.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn destack_host_android_camera_stream_capability_list(
    session_handle: u64,
    session_id: u64,
    capabilities: NativeSlice<AndroidHostCameraStreamCapabilityHeader>,
    capability_count_written: *mut u32,
) -> u32 {
    unsafe {
        runtime_camera_stream_capability_list(
            session_handle,
            session_id,
            capabilities,
            capability_count_written,
        )
    }
}

/// Forward the `stream_open` camera request through the ABI crate.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn destack_host_android_camera_stream_open(
    session_handle: u64,
    session_id: u64,
    config: AndroidHostCameraStreamConfigHeader,
    stream_id: *mut u64,
) -> u32 {
    unsafe {
        runtime_camera_stream_open(
            session_handle,
            session_id,
            config,
            stream_id,
        )
    }
}

/// Forward the `stream_close` camera request through the ABI crate.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn destack_host_android_camera_stream_close(
    session_handle: u64,
    stream_id: u64,
) -> u32 {
    unsafe {
        runtime_camera_stream_close(
            session_handle,
            stream_id,
        )
    }
}

/// Forward the `stream_start` camera request through the ABI crate.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn destack_host_android_camera_stream_start(
    session_handle: u64,
    stream_id: u64,
) -> u32 {
    unsafe {
        runtime_camera_stream_start(
            session_handle,
            stream_id,
        )
    }
}

/// Forward the `stream_stop` camera request through the ABI crate.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn destack_host_android_camera_stream_stop(
    session_handle: u64,
    stream_id: u64,
) -> u32 {
    unsafe {
        runtime_camera_stream_stop(
            session_handle,
            stream_id,
        )
    }
}

/// Forward the `stream_read` camera request through the ABI crate.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn destack_host_android_camera_stream_read(
    session_handle: u64,
    stream_id: u64,
    timeout_ns: u64,
    header: *mut AndroidHostCameraFrameHeader,
    bytes: NativeSlice<u8>,
    bytes_written: *mut u32,
) -> u32 {
    unsafe {
        runtime_camera_stream_read(
            session_handle,
            stream_id,
            timeout_ns,
            header,
            bytes,
            bytes_written,
        )
    }
}

/// Forward the `stream_try_read` camera request through the ABI crate.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn destack_host_android_camera_stream_try_read(
    session_handle: u64,
    stream_id: u64,
    header: *mut AndroidHostCameraFrameHeader,
    bytes: NativeSlice<u8>,
    bytes_written: *mut u32,
) -> u32 {
    unsafe {
        runtime_camera_stream_try_read(
            session_handle,
            stream_id,
            header,
            bytes,
            bytes_written,
        )
    }
}

/// Forward the `stream_take_photo` camera request through the ABI crate.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn destack_host_android_camera_stream_take_photo(
    session_handle: u64,
    stream_id: u64,
    timeout_ns: u64,
    header: *mut AndroidHostCameraFrameHeader,
    bytes: NativeSlice<u8>,
    bytes_written: *mut u32,
) -> u32 {
    unsafe {
        runtime_camera_stream_take_photo(
            session_handle,
            stream_id,
            timeout_ns,
            header,
            bytes,
            bytes_written,
        )
    }
}

/// Forward the `stream_config` camera request through the ABI crate.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn destack_host_android_camera_stream_config(
    session_handle: u64,
    stream_id: u64,
    config: *mut AndroidHostCameraStreamConfigHeader,
) -> u32 {
    unsafe {
        runtime_camera_stream_config(
            session_handle,
            stream_id,
            config,
        )
    }
}

/// Forward the `stream_recording_capabilities` camera request through the ABI crate.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn destack_host_android_camera_stream_recording_capabilities(
    session_handle: u64,
    stream_id: u64,
    capabilities: *mut AndroidHostCameraRecordingCapabilitiesHeader,
) -> u32 {
    unsafe {
        runtime_camera_stream_recording_capabilities(
            session_handle,
            stream_id,
            capabilities,
        )
    }
}

/// Forward the `stream_start_recording` camera request through the ABI crate.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn destack_host_android_camera_stream_start_recording(
    session_handle: u64,
    stream_id: u64,
    options: AndroidHostCameraRecordingOptionsHeader,
    output_path: NativeStringRef,
) -> u32 {
    unsafe {
        runtime_camera_stream_start_recording(
            session_handle,
            stream_id,
            options,
            output_path,
        )
    }
}

/// Forward the `stream_pause_recording` camera request through the ABI crate.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn destack_host_android_camera_stream_pause_recording(
    session_handle: u64,
    stream_id: u64,
) -> u32 {
    unsafe {
        runtime_camera_stream_pause_recording(
            session_handle,
            stream_id,
        )
    }
}

/// Forward the `stream_resume_recording` camera request through the ABI crate.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn destack_host_android_camera_stream_resume_recording(
    session_handle: u64,
    stream_id: u64,
) -> u32 {
    unsafe {
        runtime_camera_stream_resume_recording(
            session_handle,
            stream_id,
        )
    }
}

/// Forward the `stream_stop_recording` camera request through the ABI crate.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn destack_host_android_camera_stream_stop_recording(
    session_handle: u64,
    stream_id: u64,
    timeout_ns: u64,
) -> u32 {
    unsafe {
        runtime_camera_stream_stop_recording(
            session_handle,
            stream_id,
            timeout_ns,
        )
    }
}

/// Forward the `stream_get_u64` camera request through the ABI crate.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn destack_host_android_camera_stream_get_u64(
    session_handle: u64,
    stream_id: u64,
    selector: u32,
    value: *mut u64,
) -> u32 {
    unsafe {
        runtime_camera_stream_get_u64(
            session_handle,
            stream_id,
            selector,
            value,
        )
    }
}

/// Forward the `stream_set_u64` camera request through the ABI crate.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn destack_host_android_camera_stream_set_u64(
    session_handle: u64,
    stream_id: u64,
    selector: u32,
    value: u64,
) -> u32 {
    unsafe {
        runtime_camera_stream_set_u64(
            session_handle,
            stream_id,
            selector,
            value,
        )
    }
}

/// Forward the `stream_get_u32` camera request through the ABI crate.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn destack_host_android_camera_stream_get_u32(
    session_handle: u64,
    stream_id: u64,
    selector: u32,
    value: *mut u32,
) -> u32 {
    unsafe {
        runtime_camera_stream_get_u32(
            session_handle,
            stream_id,
            selector,
            value,
        )
    }
}

/// Forward the `stream_set_u32` camera request through the ABI crate.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn destack_host_android_camera_stream_set_u32(
    session_handle: u64,
    stream_id: u64,
    selector: u32,
    value: u32,
) -> u32 {
    unsafe {
        runtime_camera_stream_set_u32(
            session_handle,
            stream_id,
            selector,
            value,
        )
    }
}

/// Forward the `stream_get_f64` camera request through the ABI crate.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn destack_host_android_camera_stream_get_f64(
    session_handle: u64,
    stream_id: u64,
    selector: u32,
    value: *mut f64,
) -> u32 {
    unsafe {
        runtime_camera_stream_get_f64(
            session_handle,
            stream_id,
            selector,
            value,
        )
    }
}

/// Forward the `stream_set_f64` camera request through the ABI crate.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn destack_host_android_camera_stream_set_f64(
    session_handle: u64,
    stream_id: u64,
    selector: u32,
    value: f64,
) -> u32 {
    unsafe {
        runtime_camera_stream_set_f64(
            session_handle,
            stream_id,
            selector,
            value,
        )
    }
}

/// Forward the `stream_get_range_f64` camera request through the ABI crate.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn destack_host_android_camera_stream_get_range_f64(
    session_handle: u64,
    stream_id: u64,
    selector: u32,
    minimum: *mut f64,
    maximum: *mut f64,
    step: *mut f64,
) -> u32 {
    unsafe {
        runtime_camera_stream_get_range_f64(
            session_handle,
            stream_id,
            selector,
            minimum,
            maximum,
            step,
        )
    }
}

/// Forward the `stream_get_range_u64` camera request through the ABI crate.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn destack_host_android_camera_stream_get_range_u64(
    session_handle: u64,
    stream_id: u64,
    selector: u32,
    minimum: *mut u64,
    maximum: *mut u64,
    step: *mut u64,
) -> u32 {
    unsafe {
        runtime_camera_stream_get_range_u64(
            session_handle,
            stream_id,
            selector,
            minimum,
            maximum,
            step,
        )
    }
}

/// Forward the `stream_get_range_u32` camera request through the ABI crate.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn destack_host_android_camera_stream_get_range_u32(
    session_handle: u64,
    stream_id: u64,
    selector: u32,
    minimum: *mut u32,
    maximum: *mut u32,
    step: *mut u32,
) -> u32 {
    unsafe {
        runtime_camera_stream_get_range_u32(
            session_handle,
            stream_id,
            selector,
            minimum,
            maximum,
            step,
        )
    }
}
