// generated by generate-bindings: do not edit

use crate::diagnostic::RuntimeStatus;
use crate::host::android::abi::ingress::{
    destack_host_android_notify_intent_custom_action as runtime_notify_intent_custom_action,
    destack_host_android_notify_intent_open_file as runtime_notify_intent_open_file,
    destack_host_android_notify_intent_open_url as runtime_notify_intent_open_url,
    destack_host_android_notify_intent_share_files as runtime_notify_intent_share_files,
    destack_host_android_notify_intent_share_text as runtime_notify_intent_share_text,
};
use crate::host::android::abi::intent::{
    destack_host_android_intent_can_open_url as runtime_intent_can_open_url,
    destack_host_android_intent_open_path as runtime_intent_open_path,
    destack_host_android_intent_open_url as runtime_intent_open_url,
    destack_host_android_intent_share_paths as runtime_intent_share_paths,
    destack_host_android_intent_share_text as runtime_intent_share_text,
};
use crate::platform::abi::{NativeStringRef, NativeStringSlice};

/// Forward the `can_open_url` intent request through the ABI artifact.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn destack_host_android_intent_can_open_url(
    session_handle: u64,
    url: NativeStringRef,
    is_supported: *mut bool,
) -> u32 {
    unsafe { runtime_intent_can_open_url(session_handle, url, is_supported) }
}

/// Forward the `open_url` intent request through the ABI artifact.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn destack_host_android_intent_open_url(
    session_handle: u64,
    url: NativeStringRef,
) -> u32 {
    unsafe { runtime_intent_open_url(session_handle, url) }
}

/// Forward the `open_path` intent request through the ABI artifact.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn destack_host_android_intent_open_path(
    session_handle: u64,
    path: NativeStringRef,
) -> u32 {
    unsafe { runtime_intent_open_path(session_handle, path) }
}

/// Forward the `share_text` intent request through the ABI artifact.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn destack_host_android_intent_share_text(
    session_handle: u64,
    text: NativeStringRef,
    has_mime_type: bool,
    mime_type: NativeStringRef,
) -> u32 {
    unsafe { runtime_intent_share_text(session_handle, text, has_mime_type, mime_type) }
}

/// Forward the `share_paths` intent request through the ABI artifact.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn destack_host_android_intent_share_paths(
    session_handle: u64,
    paths: NativeStringSlice,
    has_mime_type: bool,
    mime_type: NativeStringRef,
) -> u32 {
    unsafe { runtime_intent_share_paths(session_handle, paths, has_mime_type, mime_type) }
}

/// Forward the `notify_intent_open_url` runtime binding through the ABI artifact.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn destack_host_android_notify_intent_open_url(
    session_handle: u64,
    has_source: bool,
    source: NativeStringRef,
    url: NativeStringRef,
) -> RuntimeStatus {
    unsafe { runtime_notify_intent_open_url(session_handle, has_source, source, url) }
}

/// Forward the `notify_intent_open_file` runtime binding through the ABI artifact.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn destack_host_android_notify_intent_open_file(
    session_handle: u64,
    has_source: bool,
    source: NativeStringRef,
    path: NativeStringRef,
    has_mime_type: bool,
    mime_type: NativeStringRef,
) -> RuntimeStatus {
    unsafe {
        runtime_notify_intent_open_file(
            session_handle,
            has_source,
            source,
            path,
            has_mime_type,
            mime_type,
        )
    }
}

/// Forward the `notify_intent_share_text` runtime binding through the ABI artifact.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn destack_host_android_notify_intent_share_text(
    session_handle: u64,
    has_source: bool,
    source: NativeStringRef,
    text: NativeStringRef,
    has_mime_type: bool,
    mime_type: NativeStringRef,
) -> RuntimeStatus {
    unsafe {
        runtime_notify_intent_share_text(
            session_handle,
            has_source,
            source,
            text,
            has_mime_type,
            mime_type,
        )
    }
}

/// Forward the `notify_intent_share_files` runtime binding through the ABI artifact.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn destack_host_android_notify_intent_share_files(
    session_handle: u64,
    has_source: bool,
    source: NativeStringRef,
    paths: NativeStringSlice,
    has_mime_type: bool,
    mime_type: NativeStringRef,
) -> RuntimeStatus {
    unsafe {
        runtime_notify_intent_share_files(
            session_handle,
            has_source,
            source,
            paths,
            has_mime_type,
            mime_type,
        )
    }
}

/// Forward the `notify_intent_custom_action` runtime binding through the ABI artifact.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn destack_host_android_notify_intent_custom_action(
    session_handle: u64,
    has_source: bool,
    source: NativeStringRef,
    action: NativeStringRef,
    has_url: bool,
    url: NativeStringRef,
    paths: NativeStringSlice,
    has_text: bool,
    text: NativeStringRef,
    has_mime_type: bool,
    mime_type: NativeStringRef,
) -> RuntimeStatus {
    unsafe {
        runtime_notify_intent_custom_action(
            session_handle,
            has_source,
            source,
            action,
            has_url,
            url,
            paths,
            has_text,
            text,
            has_mime_type,
            mime_type,
        )
    }
}
