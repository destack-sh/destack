use dyst_dir::Module;

use crate::Compiler;

impl<'a> Compiler<'a> {
    /// Lower a module into DIR.
    pub fn lower_module(&mut self, module: &Module) {
        todo!("lower_module({module:?})")
    }
}
