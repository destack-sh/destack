/// Marker export interface for contexts.
export interface Context { }

/// Default context.
export struct DefaultContext {
    /// The default allocator.
    defaultAllocator: Allocator
    /// The temporary allocator.
    temporaryAllocator: Allocator
    /// The allocator.
    allocator: Allocator
}

extension Context { }