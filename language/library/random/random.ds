import { HostError } from "destack:error/host";
import * as host from "./host";

/// Fill a buffer with host-provided random bytes.
export function fillBytes(buffer: &[uint8]): Result<void, HostError> {
    host.fillRandomBytes(buffer);
}
