import { Array } from "destack:collections";
import { PlatformError } from "destack:platform/error";
import { CryptoDigestHandle } from "destack:platform/resource";
import { CryptoDigestAlgorithm } from "./algorithm.ds";

/// Compute one digest in one shot.
@binding("destack.crypto.digest.compute", {
    effect: "external",
    replay: "forbidden",
    requires: ["crypto.digest"],
    families: ["windows", "unix"],
    simulation: true,
})
export declare function computeDigest(
    algorithm: CryptoDigestAlgorithm,
    payload: &readonly [uint8],
): Result<^Array<uint8>, PlatformError>;

/// Open one streaming digest context.
@binding("destack.crypto.digest.open", {
    effect: "external",
    replay: "forbidden",
    requires: ["crypto.digest"],
    families: ["windows", "unix"],
    simulation: true,
})
export declare function openDigest(
    algorithm: CryptoDigestAlgorithm,
): Result<CryptoDigestHandle, PlatformError>;

/// Update one streaming digest context.
@binding("destack.crypto.digest.update", {
    effect: "external",
    replay: "forbidden",
    requires: ["crypto.digest"],
    families: ["windows", "unix"],
    simulation: true,
})
export declare function updateDigest(
    digest: CryptoDigestHandle,
    payload: &readonly [uint8],
): Result<void, PlatformError>;

/// Finalize one streaming digest context and return one digest output.
@binding("destack.crypto.digest.finish", {
    effect: "external",
    replay: "forbidden",
    requires: ["crypto.digest"],
    families: ["windows", "unix"],
    simulation: true,
})
export declare function finishDigest(
    digest: CryptoDigestHandle,
): Result<^Array<uint8>, PlatformError>;

/// Close one streaming digest context.
@binding("destack.crypto.digest.close", {
    effect: "external",
    replay: "forbidden",
    requires: ["crypto.digest"],
    families: ["windows", "unix"],
    simulation: true,
})
export declare function closeDigest(digest: CryptoDigestHandle): Result<void, PlatformError>;
