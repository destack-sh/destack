import type { PlatformError } from "../error/index.ds";

/// Read the host virtual-memory page size.
@binding("destack.memory.query.pageSize", {
    effect: "external",
    requires: ["memory.query"],
    families: ["windows", "unix"],
    simulation: true,
})
export declare function pageSize(): Result<uint64, PlatformError>;

/// Read the host allocation granularity.
@binding("destack.memory.query.allocationGranularity", {
    effect: "external",
    requires: ["memory.query"],
    families: ["windows", "unix"],
    simulation: true,
})
export declare function allocationGranularity(): Result<uint64, PlatformError>;

/// Read the host huge-page allocation size when available.
@binding("destack.memory.query.hugePageSize", {
    effect: "external",
    requires: ["memory.query"],
    families: ["windows", "unix"],
    simulation: true,
})
export declare function hugePageSize(): Result<uint64 | void, PlatformError>;
