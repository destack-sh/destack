import { PlatformError } from "destack:platform/error";
import { IoCompletionHandle, ResourceId } from "destack:platform/resource";

/// Completion event flags.
export newtype IoCompletionFlags = uint32;

/// Completion event.
export struct IoCompletionEvent {
    /// User-assigned completion key.
    key: uint64;
    /// Host result value.
    result: int64;
    /// Completion flags.
    flags: IoCompletionFlags;
}

/// Open a completion queue.
@binding("destack.io.completion.open", {
    effect: "external",
    requires: ["host.io.completion"],
    families: ["windows", "unix"],
    simulation: true,
})
export declare function openIoCompletion(
    entries: uint32,
): Result<IoCompletionHandle, PlatformError>;

/// Close a completion queue.
@binding("destack.io.completion.close", {
    effect: "external",
    requires: ["host.io.completion"],
    families: ["windows", "unix"],
    simulation: true,
})
export declare function closeIoCompletion(
    completion: IoCompletionHandle,
): Result<void, PlatformError>;

/// Bind one resource to a completion queue.
@binding("destack.io.completion.bind", {
    effect: "external",
    requires: ["host.io.completion"],
    families: ["windows", "unix"],
    simulation: true,
})
export declare function bindIoCompletion(
    completion: IoCompletionHandle,
    resource: ResourceId,
    key: uint64,
): Result<void, PlatformError>;

/// Unbind one resource from a completion queue.
@binding("destack.io.completion.unbind", {
    effect: "external",
    requires: ["host.io.completion"],
    families: ["windows", "unix"],
    simulation: true,
})
export declare function unbindIoCompletion(
    completion: IoCompletionHandle,
    resource: ResourceId,
): Result<void, PlatformError>;

/// Post one synthetic completion event.
@binding("destack.io.completion.post", {
    effect: "external",
    requires: ["host.io.completion"],
    families: ["windows", "unix"],
    simulation: true,
})
export declare function postIoCompletion(
    completion: IoCompletionHandle,
    event: IoCompletionEvent,
): Result<void, PlatformError>;

/// Wait for completion events.
@binding("destack.io.completion.wait", {
    effect: "external",
    requires: ["host.io.completion"],
    families: ["windows", "unix"],
    simulation: true,
})
export declare function waitIoCompletion(
    completion: IoCompletionHandle,
    events: &[IoCompletionEvent],
    timeoutNs?: uint64,
): Result<uint32, PlatformError>;

/// Cancel queued completions for one resource.
@binding("destack.io.completion.cancel", {
    effect: "external",
    requires: ["host.io.completion"],
    families: ["windows", "unix"],
    simulation: true,
})
export declare function cancelIoCompletion(
    completion: IoCompletionHandle,
    resource: ResourceId,
): Result<uint32, PlatformError>;
