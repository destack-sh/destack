import { Slice } from "core";
import type { PlatformError } from "../error/index.ds";
import { FileHandle, ProcessFdHandle, SignalFdHandle } from "../resource/index.ds";
import { ProcessId } from "./ids.ds";
import { Signal, SignalEvent } from "./signals.ds";
import { ProcessWaitStatus } from "./wait.ds";

/// Process file descriptor flags.
export newtype ProcessFdFlags = uint32;

/// Process file descriptor signal flags.
export newtype ProcessFdSignalFlags = uint32;

/// Signal file descriptor flags.
export newtype SignalFdFlags = uint32;

/// Open one standard input stream handle.
@binding("destack.process.fd.stdioStdin", {
    effect: "external",
    requires: ["process.stdio"],
    families: ["windows", "unix"],
    simulation: true,
})
export declare function stdioStdin(): Result<FileHandle, PlatformError>;

/// Open one standard output stream handle.
@binding("destack.process.fd.stdioStdout", {
    effect: "external",
    requires: ["process.stdio"],
    families: ["windows", "unix"],
    simulation: true,
})
export declare function stdioStdout(): Result<FileHandle, PlatformError>;

/// Open one standard error stream handle.
@binding("destack.process.fd.stdioStderr", {
    effect: "external",
    requires: ["process.stdio"],
    families: ["windows", "unix"],
    simulation: true,
})
export declare function stdioStderr(): Result<FileHandle, PlatformError>;

/// Open one process descriptor for the target process id.
@binding("destack.process.fd.processFdOpen", {
    effect: "external",
    requires: ["process.handle"],
    families: ["windows", "unix"],
    simulation: true,
})
export declare function processFdOpen(
    pid: ProcessId,
    flags: ProcessFdFlags,
): Result<ProcessFdHandle, PlatformError>;

/// Close one process descriptor.
@binding("destack.process.fd.processFdClose", {
    effect: "external",
    requires: ["process.handle"],
    families: ["windows", "unix"],
    simulation: true,
})
export declare function processFdClose(handle: ProcessFdHandle): Result<void, PlatformError>;

/// Send one signal through a process descriptor.
@binding("destack.process.fd.processFdSendSignal", {
    effect: "external",
    requires: ["process.signal.send"],
    families: ["windows", "unix"],
    simulation: true,
})
export declare function processFdSendSignal(
    handle: ProcessFdHandle,
    signal: Signal,
    flags: ProcessFdSignalFlags,
): Result<void, PlatformError>;

/// Wait for one process descriptor state transition.
@binding("destack.process.fd.processFdWait", {
    effect: "external",
    requires: ["process.wait"],
    families: ["windows", "unix"],
    simulation: true,
})
export declare function processFdWait(
    handle: ProcessFdHandle,
    timeoutNs: uint64,
): Result<ProcessWaitStatus, PlatformError>;

/// Poll one process descriptor state transition without blocking.
@binding("destack.process.fd.processFdTryWait", {
    effect: "external",
    requires: ["process.wait"],
    families: ["windows", "unix"],
    simulation: true,
})
export declare function processFdTryWait(
    handle: ProcessFdHandle,
): Result<ProcessWaitStatus, PlatformError>;

/// Open one signal descriptor for the provided signal mask.
@binding("destack.process.fd.signalFdOpen", {
    effect: "external",
    requires: ["process.signal.receive"],
    families: ["windows", "unix"],
    simulation: true,
})
export declare function signalFdOpen(
    signals: Slice<Signal>,
    flags: SignalFdFlags,
): Result<SignalFdHandle, PlatformError>;

/// Close one signal descriptor.
@binding("destack.process.fd.signalFdClose", {
    effect: "external",
    requires: ["process.signal.receive"],
    families: ["windows", "unix"],
    simulation: true,
})
export declare function signalFdClose(handle: SignalFdHandle): Result<void, PlatformError>;

/// Read one queued signal event from a signal descriptor.
@binding("destack.process.fd.signalFdRead", {
    effect: "external",
    requires: ["process.signal.receive"],
    families: ["windows", "unix"],
    simulation: true,
})
export declare function signalFdRead(handle: SignalFdHandle): Result<SignalEvent, PlatformError>;

/// Poll one queued signal event from a signal descriptor without blocking.
@binding("destack.process.fd.signalFdTryRead", {
    effect: "external",
    requires: ["process.signal.receive"],
    families: ["windows", "unix"],
    simulation: true,
})
export declare function signalFdTryRead(handle: SignalFdHandle): Result<SignalEvent, PlatformError>;

/// Replace the active signal mask for one signal descriptor.
@binding("destack.process.fd.signalFdSetMask", {
    effect: "external",
    requires: ["process.signal.receive"],
    families: ["windows", "unix"],
    simulation: true,
})
export declare function signalFdSetMask(
    handle: SignalFdHandle,
    signals: Slice<Signal>,
): Result<void, PlatformError>;
