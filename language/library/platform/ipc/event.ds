import type { PlatformError } from "../error/index.ds";
import { EventCounterHandle } from "../resource/index.ds";

/// Event counter creation flags.
export newtype EventCounterFlags = uint32;

/// Create the event counter in nonblocking mode.
export const EVENT_COUNTER_NONBLOCK: EventCounterFlags = EventCounterFlags(0x1);

/// Create the event counter as close-on-exec.
export const EVENT_COUNTER_CLOEXEC: EventCounterFlags = EventCounterFlags(0x2);

/// Create one Linux event counter.
@binding("destack.ipc.eventCounter.create", {
    effect: "external",
    requires: ["ipc.eventCounter"],
    platforms: ["linux"],
    simulation: true,
})
export declare function createEventCounter(
    initial: uint64,
    flags: EventCounterFlags,
): Result<EventCounterHandle, PlatformError>;

/// Close one event counter handle.
@binding("destack.ipc.eventCounter.close", {
    effect: "external",
    requires: ["ipc.eventCounter"],
    platforms: ["linux"],
    simulation: true,
})
export declare function closeEventCounter(event: EventCounterHandle): Result<void, PlatformError>;

/// Read one event counter value.
@binding("destack.ipc.eventCounter.read", {
    effect: "external",
    requires: ["ipc.eventCounter"],
    platforms: ["linux"],
    simulation: true,
})
export declare function readEventCounter(event: EventCounterHandle): Result<uint64, PlatformError>;

/// Add one value to an event counter.
@binding("destack.ipc.eventCounter.write", {
    effect: "external",
    requires: ["ipc.eventCounter"],
    platforms: ["linux"],
    simulation: true,
})
export declare function writeEventCounter(
    event: EventCounterHandle,
    value: uint64,
): Result<void, PlatformError>;
