/// Forbid managed values in a function.
///
/// ```
/// @noManaged
/// function processFrame() { }
/// ```
@languageItem("decorator.noManaged")
export newtype noManaged = ();

/// Forbid managed values and raw heap allocations in a function.
///
/// ```
/// @noHeap
/// function processFrame() { }
/// ```
@languageItem("decorator.noHeap")
export newtype noHeap = ();
