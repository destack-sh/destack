/// Interface for the `-` operator.
///
/// `a - b` becomes `a.subtract(b)`.
@languageItem("ops.Subtract")
export newtype interface Subtract<T = this> {
    /// The result type.
    type Output;

    /// Subtract `other` from this value.
    subtract(other: T): this.Output;
}
