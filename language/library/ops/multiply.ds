/// Interface for the `*` operator.
///
/// `a * b` becomes `a.multiply(b)`.
@languageItem("ops.Multiply")
export newtype interface Multiply<T = this> {
    /// The result type.
    type Output;

    /// Multiply this value by `other`.
    multiply(other: T): this.Output;
}
