/// Interface for unary `-`.
///
/// `-a` becomes `a.negate()`.
@languageItem("ops.Negate")
export newtype interface Negate {
    /// The result type.
    type Output;

    /// Negate this value.
    negate(): this.Output;
}
