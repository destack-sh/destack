import { Extern } from "destack:ffi";

/// Marks a declaration as externally linked.
///
/// ```ds
/// @extern("C")
/// function open(path: *uint8): int32;
/// ```
@languageItem("decorator.extern")
export newtype extern = (Extern,) | ();
