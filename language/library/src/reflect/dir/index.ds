/// One reflected DIR declaration.
export newtype Declaration = intrinsic;

/// One reflected DIR member.
export newtype Member = intrinsic;

/// One reflected DIR scope.
export newtype Scope = intrinsic;

/// One reflected DIR symbol.
export newtype Symbol = intrinsic;

/// One reflected static property key.
export newtype StaticKey = intrinsic;
