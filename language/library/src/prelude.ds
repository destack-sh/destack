global {
    // destack:reflect
    export { alignOf, sizeOf, strideOf, Type } from "destack:reflect";

    // destack:types
    export {
        Awaited,
        Boolean,
        Capitalize,
        ConstructorParameters,
        Exclude,
        Extract,
        Function,
        InstanceType,
        Lowercase,
        MutableFields,
        NoInfer,
        NonNullable,
        Omit,
        OmitThisParameter,
        Parameters,
        Partial,
        Pick,
        PropertyKey,
        Readonly,
        Record,
        Required,
        ReturnType,
        ThisParameterType,
        ThisType,
        Uncapitalize,
        Uppercase,
    } from "destack:types";

    // destack:memory
    export {
        Access,
        AccessOf,
        AllocationCapacityOverflow,
        AllocationError,
        AllocationOutOfMemory,
        AllocationUnsupported,
        AsyncDispose,
        Borrowed,
        Box,
        Clone,
        Concrete,
        Copy,
        Cow,
        CowBorrowed,
        CowOwned,
        Default,
        Dispose,
        Drop,
        Dynamic,
        DynamicSafe,
        drop,
        forget,
        ImmutableOf,
        Lifetime,
        Managed,
        ManuallyDrop,
        MaybeUninit,
        MutableBorrowed,
        Owned,
        Ownership,
        Phantom,
        Pin,
        Place,
        PlaceOf,
        Pointer,
        Raw,
        ReadonlyBorrowed,
        Region,
        SharedSafe,
        Space,
        StaticLifetime,
        Unpin,
        ValueOf,
        WithAccess,
        Zeroable,
    } from "destack:memory";

    // destack:ops
    export {
        Add,
        And,
        Break,
        Compare,
        Continue,
        ControlFlow,
        Debug,
        DefaultEqual,
        Dereference,
        Display,
        Divide,
        Equal,
        Equality,
        Equate,
        FromResidual,
        Hash,
        Hasher,
        Index,
        IndexSet,
        Multiply,
        Negate,
        Not,
        Or,
        Ordering,
        PartialCompare,
        PartialEqual,
        Plus,
        Power,
        Remainder,
        ShiftLeft,
        ShiftRight,
        ShiftRightUnsigned,
        StrictEqual,
        Subtract,
        Try,
        ValueEquality,
        Xor,
    } from "destack:ops";

    // destack:range
    export {
        Bound,
        Range,
        RangeBounds,
        RangeFrom,
        RangeFull,
        RangeInclusive,
        RangeIterator,
        RangeTo,
        RangeToInclusive,
        Step,
    } from "destack:range";

    // destack:regexp
    export {
        RegExp,
        RegExpGroups,
        RegExpIndexGroups,
        RegExpIndices,
        RegExpMatch,
        RegExpSpan,
    } from "destack:regexp";

    // destack:error
    export {
        abort,
        captureStackTrace,
        Err,
        Error,
        formatStackTrace,
        Ok,
        panic,
        RangeError,
        Report,
        Result,
        SourceLocation,
        StackFrame,
        StackFrameId,
        StackTrace,
        StackTraceOptions,
        TypeError,
        todo,
        trap,
        unreachable,
    } from "destack:error";

    // destack:convert
    export { As, Borrow, From, Into, ToOwned, TryFrom, TryInto } from "destack:convert";

    // destack:serde
    export { Deserialize, Deserializer, Serialize, Serializer } from "destack:serde";

    // destack:json
    export {
        JSON,
        Json,
        JsonArray,
        JsonError,
        JsonErrorKind,
        JsonObject,
        JsonParseContext,
        JsonPathSegment,
        JsonReplacer,
        JsonReviver,
    } from "destack:json";

    // destack:random
    export { console } from "destack:console";

    export { Random, RandomSeed } from "destack:random";

    // destack:async
    export {
        AsyncGenerator,
        AsyncIterable,
        AsyncIterator,
        Generator,
        GeneratorResult,
        Promise,
    } from "destack:async";

    // destack:iter
    export {
        Extend,
        FromIterator,
        Iterable,
        Iterator,
        IteratorResult,
        IteratorReturn,
        IteratorYield,
    } from "destack:iter";

    // destack:collections
    export {
        Array,
        BinaryHeap,
        ConcurrentMap,
        ConcurrentQueue,
        ConcurrentSet,
        Deque,
        FixedArray,
        LinkedList,
        Map,
        MapEntry,
        MapEntryHandle,
        ReadonlyArray,
        Set,
        Slab,
        SlabKey,
        Slice,
        SmallArray,
        SortedMap,
        SortedMapEntry,
        SortedMapEntryHandle,
        SortedSet,
    } from "destack:collections";

    // destack:math
    export {
        Arithmetic,
        BigInt,
        Cast,
        Complex,
        Complexd,
        Complexf,
        convert,
        extract,
        Infinity,
        insert,
        LaneCount,
        LaneOf,
        Mask,
        Math,
        Matrix2,
        Matrix2d,
        Matrix2f,
        Matrix2i,
        Matrix3,
        Matrix3d,
        Matrix3f,
        Matrix3i,
        Matrix4,
        Matrix4d,
        Matrix4f,
        Matrix4i,
        NaN,
        Number,
        Quaternion,
        Quaterniond,
        Quaternionf,
        reduceAdd,
        reduceAnd,
        reduceMax,
        reduceMin,
        reduceMul,
        reduceOr,
        reduceXor,
        select,
        shuffle,
        splat,
        Vector,
        Vector2,
        Vector2d,
        Vector2f,
        Vector2i,
        Vector3,
        Vector3d,
        Vector3f,
        Vector3i,
        Vector4,
        Vector4d,
        Vector4f,
        Vector4i,
        Wrapping,
    } from "destack:math";

    // destack:string
    export { Character, String } from "destack:string";

    // destack:decorator
    export {
        allow,
        CaptureDirective,
        CaptureMode,
        capture,
        cold,
        DiagnosticControlOptions,
        DiagnosticId,
        deny,
        deprecated,
        derive,
        expect,
        experimental,
        forbid,
        hot,
        InlineHint,
        inline,
        intrinsic,
        languageItem,
        likely,
        mustUse,
        noAliasingMutableBorrows,
        noDynamicDispatch,
        noHeap,
        noImplicitReceivers,
        noinline,
        noManaged,
        noReflection,
        noRuntime,
        noUnsafe,
        noUnwind,
        pure,
        repr,
        safe,
        tailcall,
        unlikely,
        unroll,
        unsafe,
        warn,
    } from "destack:decorator";

    // destack:module
    export { Derive, Module } from "destack:module";

    // destack:runtime
    export {
        Action,
        BindingOptions,
        BindingProvider,
        binding,
        HostAction,
        RuntimeAction,
    } from "destack:runtime";
}
