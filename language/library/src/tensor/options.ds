import { Device, Mesh, Stream } from "destack:compute";

import { Dense, MemoryOrder, TensorFormat } from "./layout.ds";
import { ConvertMode, ScatterMode } from "./mode.ds";
import { Placement, Unsharded } from "./sharding.ds";
import { TensorView } from "./view.ds";
import { ConvolutionGroups } from "./window.ds";

/// Execution options for tensor work.
export struct TensorExecutionOptions {
    /// Stream used to order the work.
    stream?: Stream;
}

/// Allocation options for a new tensor.
export struct TensorCreateOptions<
    const out Rank: usize,
    in out F: TensorFormat = Dense<MemoryOrder.RowMajor>,
    P: Placement = Unsharded,
> {
    /// Tensor placement.
    placement?: P;

    /// Device used for unsharded placement.
    device?: Device;

    /// Mesh used for sharded placement.
    mesh?: Mesh;

    /// Stream used to order initialization work.
    stream?: Stream;
}

/// Output options for tensor operations.
export struct TensorOutputOptions<
    T,
    const Rank: usize,
    F: TensorFormat = Dense<MemoryOrder.RowMajor>,
    P: Placement = Unsharded,
> {
    /// Tensor placement.
    placement?: P;

    /// Device used for unsharded placement.
    device?: Device;

    /// Mesh used for sharded placement.
    mesh?: Mesh;

    /// Stream used to order the work.
    stream?: Stream;

    /// Exclusive output view to write into.
    out?: TensorView<T, Rank, F, P, "exclusive">;
}

/// Output options for tensor reductions.
export struct TensorReduceOptions<
    T,
    const Rank: usize,
    F: TensorFormat = Dense<MemoryOrder.RowMajor>,
    P: Placement = Unsharded,
> {
    /// Whether reduced axes remain with extent one.
    keepDims: boolean = false;

    /// Result placement.
    placement?: P;

    /// Device used for unsharded placement.
    device?: Device;

    /// Mesh used for sharded placement.
    mesh?: Mesh;

    /// Stream used to order the work.
    stream?: Stream;

    /// Exclusive output view to write into.
    out?: TensorView<T, Rank, F, P, "exclusive">;
}

/// Output options for tensor conversion.
export struct TensorConvertOptions<
    T,
    const Rank: usize,
    F: TensorFormat = Dense<MemoryOrder.RowMajor>,
    P: Placement = Unsharded,
> {
    /// Conversion mode.
    mode?: ConvertMode;

    /// Result placement.
    placement?: P;

    /// Device used for unsharded placement.
    device?: Device;

    /// Mesh used for sharded placement.
    mesh?: Mesh;

    /// Stream used to order the work.
    stream?: Stream;

    /// Exclusive output view to write into.
    out?: TensorView<T, Rank, F, P, "exclusive">;
}

/// Output options for tensor convolution.
export struct TensorConvolutionOptions<
    T,
    const Rank: usize,
    F: TensorFormat = Dense<MemoryOrder.RowMajor>,
    P: Placement = Unsharded,
> {
    /// Convolution group counts.
    groups?: ConvolutionGroups;

    /// Result placement.
    placement?: P;

    /// Device used for unsharded placement.
    device?: Device;

    /// Mesh used for sharded placement.
    mesh?: Mesh;

    /// Stream used to order the work.
    stream?: Stream;

    /// Exclusive output view to write into.
    out?: TensorView<T, Rank, F, P, "exclusive">;
}

/// Output options for tensor scatter.
export struct TensorScatterOptions<
    T,
    const Rank: usize,
    F: TensorFormat = Dense<MemoryOrder.RowMajor>,
    P: Placement = Unsharded,
> {
    /// Update mode.
    mode?: ScatterMode;

    /// Result placement.
    placement?: P;

    /// Device used for unsharded placement.
    device?: Device;

    /// Mesh used for sharded placement.
    mesh?: Mesh;

    /// Stream used to order the work.
    stream?: Stream;

    /// Exclusive output view to write into.
    out?: TensorView<T, Rank, F, P, "exclusive">;
}
