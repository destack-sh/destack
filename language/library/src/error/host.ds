import { Error } from "./error.ds";

/// Error code returned by host bindings.
@languageItem("error.HostErrorCode")
export enum HostErrorCode {
    /// Unclassified invalid argument.
    InvalidArgument = 1000,
    /// Invalid argument type.
    InvalidArgumentType = 1001,
    /// Invalid argument value.
    InvalidArgumentValue = 1002,
    /// Null pointer passed across the host boundary.
    NullPointer = 1100,
    /// Feature is not supported.
    NotSupported = 1200,

    /// Generic I/O error.
    Io = 2000,
    /// I/O read failed.
    IoReadFailed = 2100,
    /// I/O write failed.
    IoWriteFailed = 2101,
    /// File or resource not found.
    IoNotFound = 2102,
    /// Permission denied.
    IoPermissionDenied = 2103,
    /// File or resource already exists.
    IoAlreadyExists = 2104,
    /// Path component is not a directory.
    IoNotDirectory = 2105,
    /// Path points to a directory where a file is required.
    IoIsDirectory = 2106,
    /// Directory is not empty.
    IoNotEmpty = 2107,
    /// Read-only file system.
    IoReadOnly = 2108,
    /// Path name is too long.
    IoNameTooLong = 2109,
    /// File is too large.
    IoFileTooLarge = 2110,
    /// Too many open files.
    IoTooManyOpenFiles = 2111,
    /// System file table is full.
    IoFileTableOverflow = 2112,
    /// Invalid data or format.
    IoInvalidData = 2113,
    /// Cross-device link not permitted.
    IoCrossDevice = 2114,
    /// Broken pipe.
    IoBrokenPipe = 2115,
    /// I/O timed out.
    IoTimedOut = 2116,
    /// I/O interrupted.
    IoInterrupted = 2117,
    /// Resource is busy.
    IoBusy = 2118,
    /// Operation would block.
    IoWouldBlock = 2119,

    /// Generic network error.
    Net = 3000,
    /// Connection refused.
    NetConnectionRefused = 3100,
    /// Connection timed out.
    NetTimedOut = 3101,
    /// Connection reset.
    NetConnectionReset = 3102,
    /// Address already in use.
    NetAddressInUse = 3103,
    /// Address not available.
    NetAddressNotAvailable = 3104,
    /// Network unreachable.
    NetNetworkUnreachable = 3105,
    /// Host unreachable.
    NetHostUnreachable = 3106,
    /// Connection aborted.
    NetConnectionAborted = 3107,
    /// Broken pipe.
    NetBrokenPipe = 3108,
    /// DNS lookup failed.
    NetDnsFailed = 3109,
    /// Socket is not connected.
    NetNotConnected = 3110,
    /// Socket is already connected.
    NetAlreadyConnected = 3111,
    /// Message is too large for target transport.
    NetMessageTooLarge = 3112,
    /// Handle is not a socket.
    NetNotSocket = 3113,
    /// Protocol operation failed.
    NetProtocolError = 3114,
    /// Operation is in progress.
    NetInProgress = 3115,
    /// Socket is shutdown for this operation.
    NetShutdown = 3116,
    /// Address family is not supported.
    NetUnsupportedFamily = 3117,
    /// Protocol is not supported.
    NetUnsupportedProtocol = 3118,
    /// No buffer space available.
    NetNoBufferSpace = 3119,

    /// Generic process error.
    Process = 4000,
    /// Process spawn failed.
    ProcessSpawnFailed = 4100,
    /// Process not found.
    ProcessNotFound = 4101,
    /// Process permission denied.
    ProcessPermissionDenied = 4102,
    /// Process exec failed.
    ProcessExecFailed = 4103,
    /// Process wait failed.
    ProcessWaitFailed = 4104,
    /// Process terminated by signal.
    ProcessSignaled = 4105,
    /// Process operation timed out.
    ProcessTimedOut = 4106,

    /// Randomness error.
    Random = 5000,
    /// Randomness unavailable.
    RandomUnavailable = 5100,

    /// Time source error.
    Time = 6000,
    /// Time source unavailable.
    TimeUnavailable = 6100,

    /// Generic IPC error.
    Ipc = 7000,
    /// IPC payload too large.
    IpcMessageTooLarge = 7100,
    /// IPC operation timed out.
    IpcTimedOut = 7101,
    /// IPC endpoint is closed.
    IpcClosed = 7102,
    /// IPC operation would block.
    IpcWouldBlock = 7103,
    /// IPC resource already exists.
    IpcAlreadyExists = 7104,

    /// Generic security error.
    Security = 7200,
    /// Security policy denied operation.
    SecurityDenied = 7210,
    /// Security policy violation.
    SecurityViolation = 7211,

    /// Generic FFI error.
    Ffi = 7400,
    /// FFI library load failed.
    FfiLibraryLoadFailed = 7410,
    /// FFI symbol not found.
    FfiSymbolNotFound = 7411,
    /// FFI call failed.
    FfiCallFailed = 7412,

    /// Generic device error.
    Device = 7500,
    /// Device unavailable.
    DeviceUnavailable = 7510,

    /// Generic display error.
    Display = 7600,
    /// Display unavailable.
    DisplayUnavailable = 7610,

    /// Generic audio error.
    Audio = 7700,
    /// Audio device unavailable.
    AudioUnavailable = 7710,

    /// Generic GPU error.
    Gpu = 7800,
    /// GPU device unavailable.
    GpuUnavailable = 7810,
    /// GPU out of memory.
    GpuOutOfMemory = 7811,
    /// GPU device lost.
    GpuDeviceLost = 7812,

    /// Generic resource-table error.
    Resource = 7900,
    /// Resource not found.
    ResourceNotFound = 7910,
    /// Resource already closed.
    ResourceClosed = 7911,
    /// Resource busy.
    ResourceBusy = 7912,
    /// Resource type mismatch.
    ResourceTypeMismatch = 7913,

    /// Generic I/O driver error.
    IoDriver = 8000,
    /// I/O submission failed.
    IoSubmissionFailed = 8010,
    /// I/O completion failed.
    IoCompletionFailed = 8011,
    /// I/O operation canceled.
    IoCancelled = 8012,

    /// Generic host error.
    Generic = 9000,
}

/// POSIX errno source payload.
export struct HostSystemSourceErrno {
    /// Discriminator for this system-source variant.
    kind: "errno";
    /// Numeric errno value.
    value: int32;
    /// Optional symbolic errno name.
    name?: string;
}

/// Winsock source payload.
export struct HostSystemSourceWinsock {
    /// Discriminator for this system-source variant.
    kind: "winsock";
    /// Numeric WSA error value.
    value: int32;
    /// Optional symbolic WSA name.
    name?: string;
}

/// HRESULT source payload.
export struct HostSystemSourceHResult {
    /// Discriminator for this system-source variant.
    kind: "hresult";
    /// Numeric HRESULT value.
    value: int32;
    /// Optional symbolic HRESULT name.
    name?: string;
}

/// EAI source payload.
export struct HostSystemSourceEai {
    /// Discriminator for this system-source variant.
    kind: "eai";
    /// Numeric getaddrinfo/getnameinfo error value.
    value: int32;
    /// Optional symbolic EAI name.
    name?: string;
}

/// Signal source payload.
export struct HostSystemSourceSignal {
    /// Discriminator for this system-source variant.
    kind: "signal";
    /// Numeric signal value.
    value: int32;
    /// Optional symbolic signal name.
    name?: string;
}

/// Other source payload.
export struct HostSystemSourceOther {
    /// Discriminator for this system-source variant.
    kind: "other";
    /// Numeric source value.
    value: int32;
    /// Optional symbolic source name.
    name?: string;
}

/// Host-level system source metadata.
@languageItem("error.HostSystemSource")
export newtype HostSystemSource =
    | HostSystemSourceErrno
    | HostSystemSourceWinsock
    | HostSystemSourceHResult
    | HostSystemSourceEai
    | HostSystemSourceSignal
    | HostSystemSourceOther;

/// Byte-path payload attached to an error context.
export struct HostPathPayloadBytes {
    /// Discriminator for this path-payload variant.
    kind: "bytes";
    /// Raw path bytes.
    bytes: uint8[];
}

/// UTF-16 path payload attached to an error context.
export struct HostPathPayloadUtf16 {
    /// Discriminator for this path-payload variant.
    kind: "utf16";
    /// UTF-16 path code units.
    utf16: uint16[];
}

/// Path payload attached to an error context.
@languageItem("error.HostPathPayload")
export newtype HostPathPayload = HostPathPayloadBytes | HostPathPayloadUtf16;

/// Generic host-error context payload.
export struct HostErrorContextGeneric {
    /// Discriminator for this error-context variant.
    kind: "generic";
    /// Optional syscall or host API name.
    syscall?: string;
    /// Optional argument name.
    argument?: string;
    /// Optional pointer label.
    pointer?: string;
    /// Optional feature identifier.
    feature?: string;
}

/// I/O host-error context payload.
export struct HostErrorContextIo {
    /// Discriminator for this error-context variant.
    kind: "io";
    /// Optional syscall or host API name.
    syscall?: string;
    /// Optional primary path payload.
    path?: HostPathPayload;
    /// Optional destination path payload.
    dest?: HostPathPayload;
    /// Optional primary path text fallback.
    pathText?: string;
    /// Optional destination path text fallback.
    destText?: string;
    /// Optional file descriptor.
    fd?: int32;
}

/// Network host-error context payload.
export struct HostErrorContextNet {
    /// Discriminator for this error-context variant.
    kind: "net";
    /// Optional syscall or host API name.
    syscall?: string;
    /// Optional network address text.
    address?: string;
    /// Optional network port.
    port?: uint16;
    /// Optional host name.
    hostname?: string;
}

/// Process host-error context payload.
export struct HostErrorContextProcess {
    /// Discriminator for this error-context variant.
    kind: "process";
    /// Optional syscall or host API name.
    syscall?: string;
    /// Optional process id.
    pid?: uint64;
    /// Optional signal name.
    signal?: string;
    /// Optional exit code.
    exitCode?: int32;
}

/// Timer host-error context payload.
export struct HostErrorContextTimer {
    /// Discriminator for this error-context variant.
    kind: "timer";
    /// Optional syscall or host API name.
    syscall?: string;
    /// Optional timer id.
    timerId?: uint64;
    /// Optional timer deadline.
    deadlineNs?: uint64;
}

/// Resource host-error context payload.
export struct HostErrorContextResource {
    /// Discriminator for this error-context variant.
    kind: "resource";
    /// Optional syscall or host API name.
    syscall?: string;
    /// Optional resource id.
    resourceId?: uint64;
    /// Optional resource kind.
    resourceKind?: string;
}

/// Security host-error context payload.
export struct HostErrorContextSecurity {
    /// Discriminator for this error-context variant.
    kind: "security";
    /// Optional syscall or host API name.
    syscall?: string;
    /// Optional action name.
    action?: string;
    /// Optional policy name.
    policy?: string;
}

/// FFI host-error context payload.
export struct HostErrorContextFfi {
    /// Discriminator for this error-context variant.
    kind: "ffi";
    /// Optional syscall or host API name.
    syscall?: string;
    /// Optional FFI library name.
    library?: string;
    /// Optional FFI symbol name.
    symbol?: string;
}

/// IPC host-error context payload.
export struct HostErrorContextIpc {
    /// Discriminator for this error-context variant.
    kind: "ipc";
    /// Optional syscall or host API name.
    syscall?: string;
    /// Optional primary path payload.
    path?: HostPathPayload;
    /// Optional primary path text fallback.
    pathText?: string;
    /// Optional file descriptor.
    fd?: int32;
}

/// Device host-error context payload.
export struct HostErrorContextDevice {
    /// Discriminator for this error-context variant.
    kind: "device";
    /// Optional syscall or host API name.
    syscall?: string;
    /// Optional primary path payload.
    path?: HostPathPayload;
    /// Optional primary path text fallback.
    pathText?: string;
    /// Optional feature identifier.
    feature?: string;
}

/// Display host-error context payload.
export struct HostErrorContextDisplay {
    /// Discriminator for this error-context variant.
    kind: "display";
    /// Optional syscall or host API name.
    syscall?: string;
    /// Optional feature identifier.
    feature?: string;
}

/// Audio host-error context payload.
export struct HostErrorContextAudio {
    /// Discriminator for this error-context variant.
    kind: "audio";
    /// Optional syscall or host API name.
    syscall?: string;
    /// Optional feature identifier.
    feature?: string;
}

/// GPU host-error context payload.
export struct HostErrorContextGpu {
    /// Discriminator for this error-context variant.
    kind: "gpu";
    /// Optional syscall or host API name.
    syscall?: string;
    /// Optional feature identifier.
    feature?: string;
}

/// I/O-driver host-error context payload.
export struct HostErrorContextIoDriver {
    /// Discriminator for this error-context variant.
    kind: "ioDriver";
    /// Optional syscall or host API name.
    syscall?: string;
    /// Optional file descriptor.
    fd?: int32;
    /// Optional feature identifier.
    feature?: string;
}

/// Typed context for host error metadata.
@languageItem("error.HostErrorContext")
export newtype HostErrorContext =
    | HostErrorContextGeneric
    | HostErrorContextIo
    | HostErrorContextNet
    | HostErrorContextProcess
    | HostErrorContextTimer
    | HostErrorContextResource
    | HostErrorContextSecurity
    | HostErrorContextFfi
    | HostErrorContextIpc
    | HostErrorContextDevice
    | HostErrorContextDisplay
    | HostErrorContextAudio
    | HostErrorContextGpu
    | HostErrorContextIoDriver;

/// Error returned by host bindings.
@languageItem("error.HostError")
export local newtype HostError = {
    code: HostErrorCode;
    op?: string;
    systemSource?: HostSystemSource;
    context?: HostErrorContext;
    message?: string;
};

export extension of HostError implements Error {
    /// Format this error for users.
    display(this: &readonly this): string {
        if (this.message == undefined) {
            "host error"
        } else {
            this.message
        }
    }
}

/// Error bindings for runtime error propagation.
@binding("destack.error.error.takeHostError", {
    effect: "deterministic",
    requires: ["runtime.trace.read"],
    families: ["windows", "unix"],
    hosts: ["wasi"],
    provider: "runtime",
})
export declare function takeHostError(errorId: uint64): Result<HostError, HostError>;
