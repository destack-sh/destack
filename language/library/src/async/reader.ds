import { IoError } from "destack:error";

import { Task } from "./task.ds";

/// Asynchronous byte reader.
@languageItem("async.AsyncReader")
export interface AsyncReader {
    /// Read bytes into `buffer`.
    read(buffer: &exclusive [uint8]): Task<Result<usize, IoError>>;
}
