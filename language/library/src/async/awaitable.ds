/// Value whose result can suspend the current fiber.
@languageItem("async.Awaitable")
export local newtype interface Awaitable<T> {
    /// Block the current fiber until `value` produces its result.
    static block(value: this): T;
}
