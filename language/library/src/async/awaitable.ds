import { Waiter } from "./continuation.ds";

/// Value whose result can resume asynchronous execution.
@languageItem("async.Awaitable")
export local newtype interface Awaitable<T> {
    /// Park one waiter until `value` produces its result.
    static park(value: this, waiter: Waiter<T>): void;
}
