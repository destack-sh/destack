import { IoError } from "destack:error";
import { SeekFrom } from "destack:sync";

/// Asynchronous seekable byte stream.
export interface AsyncSeeker {
    /// Seek to a new stream position.
    seek(from: SeekFrom): Task<Result<uint64, IoError>>;
}
