/// Interface for the `**` operator.
///
/// `a ** b` becomes `a.power(b)`.
@languageItem("ops.Power")
export newtype interface Power<T = this> {
    /// The result type.
    type Output;

    /// Raise this value to `other`.
    power(this, other: T): this.Output;
}
