import { TimeClockBinding } from "./clock.ds";
import { TimeTimerBinding } from "./timer.ds";

/// TimeBinding binding family.
@languageItem("time.TimeBinding")
export interface TimeBinding {
    /// Clock bindings.
    readonly clock: TimeClockBinding;
    /// Timer bindings.
    readonly timer: TimeTimerBinding;
}
