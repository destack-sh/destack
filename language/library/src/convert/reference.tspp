import { Access, Borrowed, Region } from "tspp:memory";

/// Interface for reference conversion.
@languageItem("convert.As")
export newtype interface As<T, const A: Access = "readonly"> {
    /// Borrow this value as `T`.
    as<const R: Region>(this: Borrowed<this, R, A>): Borrowed<T, R, A>;
}
