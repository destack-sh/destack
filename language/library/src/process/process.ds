import { Context } from "destack:context";
import { HostError } from "destack:error/host";

/// Return the process arguments.
export function args(): Result<^Array<^string>, HostError> {
    Context.current().process.argument.listArguments()
}
