import { IoError, Result, todo } from "tspp:error";

/// Byte reader.
@languageItem("sync.Reader")
export interface Reader {
    /// Read bytes into `buffer`.
    read(&this, buffer: &[uint8]): Result<usize, IoError>;
}

/// Read until end of input.
export function readAll(reader: &Reader): Result<uint8[], IoError> {
    todo("sync.readAll")
}
