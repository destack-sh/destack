==============================================
types / primitives / object / object to object
==============================================

const x: object = {} as object;

---

(program
  (lexical_declaration
    (variable_declarator
      (identifier)
      (type_annotation
        (predefined_type))
      (as_expression
        (object)
        (predefined_type)))))

======================================================
types / primitives / object / object literal to object
======================================================

const x: object = { a: 1, b: "hello" };

---

(program
  (lexical_declaration
    (variable_declarator
      (identifier)
      (type_annotation
        (predefined_type))
      (object
        (pair
          (property_identifier)
          (number))
        (pair
          (property_identifier)
          (string
            (string_fragment)))))))

=============================================
types / primitives / object / array to object
=============================================

const x: object = [1, 2, 3];

---

(program
  (lexical_declaration
    (variable_declarator
      (identifier)
      (type_annotation
        (predefined_type))
      (array
        (number)
        (number)
        (number)))))

================================================
types / primitives / object / function to object
================================================

const fn = () => {};
const x: object = fn;

---

(program
  (lexical_declaration
    (variable_declarator
      (destack_if_let_pattern
        (identifier))
      (arrow_function
        (formal_parameters)
        (statement_block))))
  (lexical_declaration
    (variable_declarator
      (identifier)
      (type_annotation
        (predefined_type))
      (identifier))))

=============================================================
types / primitives / object / number not assignable to object
=============================================================

const x: object = 42;

---

(program
  (lexical_declaration
    (variable_declarator
      (identifier)
      (type_annotation
        (predefined_type))
      (number))))

=============================================================
types / primitives / object / string not assignable to object
=============================================================

const x: object = "hello";

---

(program
  (lexical_declaration
    (variable_declarator
      (identifier)
      (type_annotation
        (predefined_type))
      (string
        (string_fragment)))))

==============================================================
types / primitives / object / boolean not assignable to object
==============================================================

const x: object = true;

---

(program
  (lexical_declaration
    (variable_declarator
      (identifier)
      (type_annotation
        (predefined_type))
      (true))))

===========================================================
types / primitives / object / null not assignable to object
===========================================================

const x: object = null;

---

(program
  (lexical_declaration
    (variable_declarator
      (identifier)
      (type_annotation
        (predefined_type))
      (null))))

================================================================
types / primitives / object / undefined not assignable to object
================================================================

const x: object = undefined;

---

(program
  (lexical_declaration
    (variable_declarator
      (identifier)
      (type_annotation
        (predefined_type))
      (undefined))))
