============================================================
expressions / literals / regex / regex literal yields RegExp
============================================================

let value: RegExp = /abc/;

---

(program
  (lexical_declaration
    (variable_declarator
      (identifier)
      (type_annotation
        (type_identifier))
      (regex
        (regex_pattern)))))

===============================================================
expressions / literals / regex / regex literals are not strings
===============================================================

let value: string = /abc/;

---

(program
  (lexical_declaration
    (variable_declarator
      (identifier)
      (type_annotation
        (predefined_type))
      (regex
        (regex_pattern)))))

=====================================================================
expressions / literals / regex / regex literals expose RegExp members
=====================================================================

const value = /abc/;
const text = value.toString();
text satisfies string;

---

(program
  (lexical_declaration
    (variable_declarator
      (destack_if_let_pattern
        (identifier))
      (regex
        (regex_pattern))))
  (lexical_declaration
    (variable_declarator
      (destack_if_let_pattern
        (identifier))
      (call_expression
        (member_expression
          (identifier)
          (property_identifier))
        (arguments))))
  (expression_statement
    (satisfies_expression
      (identifier)
      (satisfies_type
        (predefined_type)))))

=======================================================================
expressions / literals / regex / regex literals work with RegExp unions
=======================================================================

let value: RegExp | int32 = /abc/;
value satisfies RegExp | int32;

---

(program
  (lexical_declaration
    (variable_declarator
      (identifier)
      (type_annotation
        (union_type
          (type_identifier)
          (type_identifier)))
      (regex
        (regex_pattern))))
  (expression_statement
    (binary_expression
      (satisfies_expression
        (identifier)
        (satisfies_type
          (type_identifier)))
      (identifier))))

=======================================================================
expressions / literals / regex / regex literals reject boolean contexts
=======================================================================

let value: boolean = /abc/;

---

(program
  (lexical_declaration
    (variable_declarator
      (identifier)
      (type_annotation
        (predefined_type))
      (regex
        (regex_pattern)))))
