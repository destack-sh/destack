========================================================================
runtime / modules / cycles / unannotated cycles require type annotations
========================================================================

import { y } from "./b.ds";

export const x = y;

---

(program
  (import_statement
    (import_clause
      (named_imports
        (import_specifier
          (identifier))))
    (string
      (string_fragment)))
  (export_statement
    (lexical_declaration
      (variable_declarator
        (destack_if_let_pattern
          (identifier))
        (identifier)))))

========================================================================
runtime / modules / cycles / unannotated cycles require type annotations
========================================================================

import { x } from "./a.ds";

export const y = x;

---

(program
  (import_statement
    (import_clause
      (named_imports
        (import_specifier
          (identifier))))
    (string
      (string_fragment)))
  (export_statement
    (lexical_declaration
      (variable_declarator
        (destack_if_let_pattern
          (identifier))
        (identifier)))))

=======================================================================================
runtime / modules / cycles / partially annotated cycles still require local annotations
=======================================================================================

import { y } from "./b.ds";

export const x: number = y;

---

(program
  (import_statement
    (import_clause
      (named_imports
        (import_specifier
          (identifier))))
    (string
      (string_fragment)))
  (export_statement
    (lexical_declaration
      (variable_declarator
        (identifier)
        (type_annotation
          (predefined_type))
        (identifier)))))

=======================================================================================
runtime / modules / cycles / partially annotated cycles still require local annotations
=======================================================================================

import { x } from "./a.ds";

export const y = x;

---

(program
  (import_statement
    (import_clause
      (named_imports
        (import_specifier
          (identifier))))
    (string
      (string_fragment)))
  (export_statement
    (lexical_declaration
      (variable_declarator
        (destack_if_let_pattern
          (identifier))
        (identifier)))))

===============================================================
runtime / modules / cycles / fully annotated cycles are allowed
===============================================================

import { y } from "./b.ds";

export const x: number = y;

---

(program
  (import_statement
    (import_clause
      (named_imports
        (import_specifier
          (identifier))))
    (string
      (string_fragment)))
  (export_statement
    (lexical_declaration
      (variable_declarator
        (identifier)
        (type_annotation
          (predefined_type))
        (identifier)))))

===============================================================
runtime / modules / cycles / fully annotated cycles are allowed
===============================================================

import { x } from "./a.ds";

export const y: number = x;

---

(program
  (import_statement
    (import_clause
      (named_imports
        (import_specifier
          (identifier))))
    (string
      (string_fragment)))
  (export_statement
    (lexical_declaration
      (variable_declarator
        (identifier)
        (type_annotation
          (predefined_type))
        (identifier)))))

=====================================================================================
runtime / modules / cycles / unannotated three-module cycles require type annotations
=====================================================================================

import { y } from "./b.ds";

export const x = y;

---

(program
  (import_statement
    (import_clause
      (named_imports
        (import_specifier
          (identifier))))
    (string
      (string_fragment)))
  (export_statement
    (lexical_declaration
      (variable_declarator
        (destack_if_let_pattern
          (identifier))
        (identifier)))))

=====================================================================================
runtime / modules / cycles / unannotated three-module cycles require type annotations
=====================================================================================

import { z } from "./c.ds";

export const y = z;

---

(program
  (import_statement
    (import_clause
      (named_imports
        (import_specifier
          (identifier))))
    (string
      (string_fragment)))
  (export_statement
    (lexical_declaration
      (variable_declarator
        (destack_if_let_pattern
          (identifier))
        (identifier)))))

=====================================================================================
runtime / modules / cycles / unannotated three-module cycles require type annotations
=====================================================================================

import { x } from "./a.ds";

export const z = x;

---

(program
  (import_statement
    (import_clause
      (named_imports
        (import_specifier
          (identifier))))
    (string
      (string_fragment)))
  (export_statement
    (lexical_declaration
      (variable_declarator
        (destack_if_let_pattern
          (identifier))
        (identifier)))))

====================================================================================
runtime / modules / cycles / three-module cycles need all inferred exports annotated
====================================================================================

import { y } from "./b.ds";

export const x: number = y;

---

(program
  (import_statement
    (import_clause
      (named_imports
        (import_specifier
          (identifier))))
    (string
      (string_fragment)))
  (export_statement
    (lexical_declaration
      (variable_declarator
        (identifier)
        (type_annotation
          (predefined_type))
        (identifier)))))

====================================================================================
runtime / modules / cycles / three-module cycles need all inferred exports annotated
====================================================================================

import { z } from "./c.ds";

export const y = z;

---

(program
  (import_statement
    (import_clause
      (named_imports
        (import_specifier
          (identifier))))
    (string
      (string_fragment)))
  (export_statement
    (lexical_declaration
      (variable_declarator
        (destack_if_let_pattern
          (identifier))
        (identifier)))))

====================================================================================
runtime / modules / cycles / three-module cycles need all inferred exports annotated
====================================================================================

import { x } from "./a.ds";

export const z = x;

---

(program
  (import_statement
    (import_clause
      (named_imports
        (import_specifier
          (identifier))))
    (string
      (string_fragment)))
  (export_statement
    (lexical_declaration
      (variable_declarator
        (destack_if_let_pattern
          (identifier))
        (identifier)))))

======================================================================
runtime / modules / cycles / namespace cycles require type annotations
======================================================================

import * as b from "./b.ds";

export const x = b.y;

---

(program
  (import_statement
    (import_clause
      (namespace_import
        (identifier)))
    (string
      (string_fragment)))
  (export_statement
    (lexical_declaration
      (variable_declarator
        (destack_if_let_pattern
          (identifier))
        (member_expression
          (identifier)
          (property_identifier))))))

======================================================================
runtime / modules / cycles / namespace cycles require type annotations
======================================================================

import * as a from "./a.ds";

export const y = a.x;

---

(program
  (import_statement
    (import_clause
      (namespace_import
        (identifier)))
    (string
      (string_fragment)))
  (export_statement
    (lexical_declaration
      (variable_declarator
        (destack_if_let_pattern
          (identifier))
        (member_expression
          (identifier)
          (property_identifier))))))

=================================================================================
runtime / modules / cycles / namespace cycles need all inferred exports annotated
=================================================================================

import * as b from "./b.ds";

export const x: number = b.y;

---

(program
  (import_statement
    (import_clause
      (namespace_import
        (identifier)))
    (string
      (string_fragment)))
  (export_statement
    (lexical_declaration
      (variable_declarator
        (identifier)
        (type_annotation
          (predefined_type))
        (member_expression
          (identifier)
          (property_identifier))))))

=================================================================================
runtime / modules / cycles / namespace cycles need all inferred exports annotated
=================================================================================

import * as a from "./a.ds";

export const y = a.x;

---

(program
  (import_statement
    (import_clause
      (namespace_import
        (identifier)))
    (string
      (string_fragment)))
  (export_statement
    (lexical_declaration
      (variable_declarator
        (destack_if_let_pattern
          (identifier))
        (member_expression
          (identifier)
          (property_identifier))))))

=======================================================================
runtime / modules / cycles / reexported cycles require type annotations
=======================================================================

import { y } from "./forward.ds";

export const x = y;

---

(program
  (import_statement
    (import_clause
      (named_imports
        (import_specifier
          (identifier))))
    (string
      (string_fragment)))
  (export_statement
    (lexical_declaration
      (variable_declarator
        (destack_if_let_pattern
          (identifier))
        (identifier)))))

=======================================================================
runtime / modules / cycles / reexported cycles require type annotations
=======================================================================

export { y } from "./b.ds";

---

(program
  (export_statement
    (export_clause
      (export_specifier
        (identifier)))
    (string
      (string_fragment))))

=======================================================================
runtime / modules / cycles / reexported cycles require type annotations
=======================================================================

import { x } from "./a.ds";

export const y = x;

---

(program
  (import_statement
    (import_clause
      (named_imports
        (import_specifier
          (identifier))))
    (string
      (string_fragment)))
  (export_statement
    (lexical_declaration
      (variable_declarator
        (destack_if_let_pattern
          (identifier))
        (identifier)))))

==================================================================================
runtime / modules / cycles / reexported cycles need all inferred exports annotated
==================================================================================

import { y } from "./forward.ds";

export const x: number = y;

---

(program
  (import_statement
    (import_clause
      (named_imports
        (import_specifier
          (identifier))))
    (string
      (string_fragment)))
  (export_statement
    (lexical_declaration
      (variable_declarator
        (identifier)
        (type_annotation
          (predefined_type))
        (identifier)))))

==================================================================================
runtime / modules / cycles / reexported cycles need all inferred exports annotated
==================================================================================

export { y } from "./b.ds";

---

(program
  (export_statement
    (export_clause
      (export_specifier
        (identifier)))
    (string
      (string_fragment))))

==================================================================================
runtime / modules / cycles / reexported cycles need all inferred exports annotated
==================================================================================

import { x } from "./a.ds";

export const y = x;

---

(program
  (import_statement
    (import_clause
      (named_imports
        (import_specifier
          (identifier))))
    (string
      (string_fragment)))
  (export_statement
    (lexical_declaration
      (variable_declarator
        (destack_if_let_pattern
          (identifier))
        (identifier)))))
