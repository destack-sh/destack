=======================================================
runtime / modules / text / text files import as strings
=======================================================

import content from "./readme.txt.ds";

content satisfies string;

---

(program
  (import_statement
    (import_clause
      (identifier))
    (string
      (string_fragment)))
  (expression_statement
    (satisfies_expression
      (identifier)
      (satisfies_type
        (predefined_type)))))

========================================================================
runtime / modules / text / text import attributes override data defaults
========================================================================

import content from "./data.json.ds" with { type: "text" };

content satisfies string;

---

(program
  (import_statement
    (import_clause
      (identifier))
    (string
      (string_fragment))
    (import_attribute
      (object
        (pair
          (property_identifier)
          (string
            (string_fragment))))))
  (expression_statement
    (satisfies_expression
      (identifier)
      (satisfies_type
        (predefined_type)))))
