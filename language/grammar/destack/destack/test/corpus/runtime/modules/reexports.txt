=================================================================
runtime / modules / reexports / named reexports keep value shapes
=================================================================

export const value = { ok: true };

---

(program
  (export_statement
    (lexical_declaration
      (variable_declarator
        (destack_if_let_pattern
          (identifier))
        (object
          (pair
            (property_identifier)
            (true)))))))

=================================================================
runtime / modules / reexports / named reexports keep value shapes
=================================================================

export { value } from "./a.ds";

---

(program
  (export_statement
    (export_clause
      (export_specifier
        (identifier)))
    (string
      (string_fragment))))

=================================================================
runtime / modules / reexports / named reexports keep value shapes
=================================================================

import { value } from "./b.ds";

value.ok satisfies boolean;

---

(program
  (import_statement
    (import_clause
      (named_imports
        (import_specifier
          (identifier))))
    (string
      (string_fragment)))
  (expression_statement
    (satisfies_expression
      (member_expression
        (identifier)
        (property_identifier))
      (satisfies_type
        (predefined_type)))))

=====================================================================
runtime / modules / reexports / multi-hop reexports keep value shapes
=====================================================================

export const value = { ok: true };

---

(program
  (export_statement
    (lexical_declaration
      (variable_declarator
        (destack_if_let_pattern
          (identifier))
        (object
          (pair
            (property_identifier)
            (true)))))))

=====================================================================
runtime / modules / reexports / multi-hop reexports keep value shapes
=====================================================================

export { value } from "./a.ds";

---

(program
  (export_statement
    (export_clause
      (export_specifier
        (identifier)))
    (string
      (string_fragment))))

=====================================================================
runtime / modules / reexports / multi-hop reexports keep value shapes
=====================================================================

export { value } from "./b.ds";

---

(program
  (export_statement
    (export_clause
      (export_specifier
        (identifier)))
    (string
      (string_fragment))))

=====================================================================
runtime / modules / reexports / multi-hop reexports keep value shapes
=====================================================================

import { value } from "./c.ds";

value.ok satisfies boolean;

---

(program
  (import_statement
    (import_clause
      (named_imports
        (import_specifier
          (identifier))))
    (string
      (string_fragment)))
  (expression_statement
    (satisfies_expression
      (member_expression
        (identifier)
        (property_identifier))
      (satisfies_type
        (predefined_type)))))

=======================================================================
runtime / modules / reexports / export star preserves type-only exports
=======================================================================

export type User = { name: string };
export const value = 1;

---

(program
  (export_statement
    (type_alias_declaration
      (type_identifier)
      (object_type
        (property_signature
          (property_identifier)
          (type_annotation
            (predefined_type))))))
  (export_statement
    (lexical_declaration
      (variable_declarator
        (destack_if_let_pattern
          (identifier))
        (number)))))

=======================================================================
runtime / modules / reexports / export star preserves type-only exports
=======================================================================

export * from "./types.ds";

---

(program
  (export_statement
    (string
      (string_fragment))))

=======================================================================
runtime / modules / reexports / export star preserves type-only exports
=======================================================================

import { User, value } from "./mod.ds";

value satisfies number;
type Alias = User;
User;

---

(program
  (import_statement
    (import_clause
      (named_imports
        (import_specifier
          (identifier))
        (import_specifier
          (identifier))))
    (string
      (string_fragment)))
  (expression_statement
    (satisfies_expression
      (identifier)
      (satisfies_type
        (predefined_type))))
  (type_alias_declaration
    (type_identifier)
    (type_identifier))
  (expression_statement
    (identifier)))

===================================================================
runtime / modules / reexports / renamed reexports keep value shapes
===================================================================

export const value = { ok: true };

---

(program
  (export_statement
    (lexical_declaration
      (variable_declarator
        (destack_if_let_pattern
          (identifier))
        (object
          (pair
            (property_identifier)
            (true)))))))

===================================================================
runtime / modules / reexports / renamed reexports keep value shapes
===================================================================

export { value as renamed } from "./a.ds";

---

(program
  (export_statement
    (export_clause
      (export_specifier
        (identifier)
        (identifier)))
    (string
      (string_fragment))))

===================================================================
runtime / modules / reexports / renamed reexports keep value shapes
===================================================================

import { renamed } from "./b.ds";

renamed.ok satisfies boolean;

---

(program
  (import_statement
    (import_clause
      (named_imports
        (import_specifier
          (identifier))))
    (string
      (string_fragment)))
  (expression_statement
    (satisfies_expression
      (member_expression
        (identifier)
        (property_identifier))
      (satisfies_type
        (predefined_type)))))

==============================================================
runtime / modules / reexports / export star keeps value shapes
==============================================================

export const value = { ok: true };

---

(program
  (export_statement
    (lexical_declaration
      (variable_declarator
        (destack_if_let_pattern
          (identifier))
        (object
          (pair
            (property_identifier)
            (true)))))))

==============================================================
runtime / modules / reexports / export star keeps value shapes
==============================================================

export * from "./a.ds";

---

(program
  (export_statement
    (string
      (string_fragment))))

==============================================================
runtime / modules / reexports / export star keeps value shapes
==============================================================

import { value } from "./b.ds";

value.ok satisfies boolean;

---

(program
  (import_statement
    (import_clause
      (named_imports
        (import_specifier
          (identifier))))
    (string
      (string_fragment)))
  (expression_statement
    (satisfies_expression
      (member_expression
        (identifier)
        (property_identifier))
      (satisfies_type
        (predefined_type)))))

======================================================================
runtime / modules / reexports / export type reexports remain type-only
======================================================================

export type User = { name: string };

---

(program
  (export_statement
    (type_alias_declaration
      (type_identifier)
      (object_type
        (property_signature
          (property_identifier)
          (type_annotation
            (predefined_type)))))))

======================================================================
runtime / modules / reexports / export type reexports remain type-only
======================================================================

export type { User } from "./types.ds";

---

(program
  (export_statement
    (export_clause
      (export_specifier
        (identifier)))
    (string
      (string_fragment))))

======================================================================
runtime / modules / reexports / export type reexports remain type-only
======================================================================

import { User } from "./mod.ds";

type Alias = User;
User;

---

(program
  (import_statement
    (import_clause
      (named_imports
        (import_specifier
          (identifier))))
    (string
      (string_fragment)))
  (type_alias_declaration
    (type_identifier)
    (type_identifier))
  (expression_statement
    (identifier)))

=======================================================================
runtime / modules / reexports / export type specifiers remain type-only
=======================================================================

export type User = { name: string };

---

(program
  (export_statement
    (type_alias_declaration
      (type_identifier)
      (object_type
        (property_signature
          (property_identifier)
          (type_annotation
            (predefined_type)))))))

=======================================================================
runtime / modules / reexports / export type specifiers remain type-only
=======================================================================

export { type User } from "./types.ds";

---

(program
  (export_statement
    (export_clause
      (export_specifier
        (identifier)))
    (string
      (string_fragment))))

=======================================================================
runtime / modules / reexports / export type specifiers remain type-only
=======================================================================

import { User } from "./mod.ds";

type Alias = User;
User;

---

(program
  (import_statement
    (import_clause
      (named_imports
        (import_specifier
          (identifier))))
    (string
      (string_fragment)))
  (type_alias_declaration
    (type_identifier)
    (type_identifier))
  (expression_statement
    (identifier)))

=====================================================================
runtime / modules / reexports / export type star reexports types only
=====================================================================

export type User = { name: string };

---

(program
  (export_statement
    (type_alias_declaration
      (type_identifier)
      (object_type
        (property_signature
          (property_identifier)
          (type_annotation
            (predefined_type)))))))

=====================================================================
runtime / modules / reexports / export type star reexports types only
=====================================================================

export type * from "./types.ds";

---

(program
  (export_statement
    (string
      (string_fragment))))

=====================================================================
runtime / modules / reexports / export type star reexports types only
=====================================================================

import { User } from "./mod.ds";

type Alias = User;
User;

---

(program
  (import_statement
    (import_clause
      (named_imports
        (import_specifier
          (identifier))))
    (string
      (string_fragment)))
  (type_alias_declaration
    (type_identifier)
    (type_identifier))
  (expression_statement
    (identifier)))

============================================================================
runtime / modules / reexports / export type star rejects runtime value usage
============================================================================

export interface User {
    name: string;
}

---

(program
  (export_statement
    (interface_declaration
      (type_identifier)
      (interface_body
        (destack_property_signature
          (property_identifier)
          (type_annotation
            (predefined_type)))))))

============================================================================
runtime / modules / reexports / export type star rejects runtime value usage
============================================================================

export type * from "./types.ds";

---

(program
  (export_statement
    (string
      (string_fragment))))

============================================================================
runtime / modules / reexports / export type star rejects runtime value usage
============================================================================

import { User } from "./mod.ds";

const value = User;

---

(program
  (import_statement
    (import_clause
      (named_imports
        (import_specifier
          (identifier))))
    (string
      (string_fragment)))
  (lexical_declaration
    (variable_declarator
      (destack_if_let_pattern
        (identifier))
      (identifier))))

=====================================================================
runtime / modules / reexports / namespace reexports keep value shapes
=====================================================================

export const value = { ok: true };

---

(program
  (export_statement
    (lexical_declaration
      (variable_declarator
        (destack_if_let_pattern
          (identifier))
        (object
          (pair
            (property_identifier)
            (true)))))))

=====================================================================
runtime / modules / reexports / namespace reexports keep value shapes
=====================================================================

export * as ns from "./a.ds";

---

(program
  (export_statement
    (namespace_export
      (identifier))
    (string
      (string_fragment))))

=====================================================================
runtime / modules / reexports / namespace reexports keep value shapes
=====================================================================

import { ns } from "./b.ds";

ns.value.ok satisfies boolean;

---

(program
  (import_statement
    (import_clause
      (named_imports
        (import_specifier
          (identifier))))
    (string
      (string_fragment)))
  (expression_statement
    (satisfies_expression
      (member_expression
        (member_expression
          (identifier)
          (property_identifier))
        (property_identifier))
      (satisfies_type
        (predefined_type)))))

===================================================================
runtime / modules / reexports / default reexports keep value shapes
===================================================================

export default function make() {
    return { ok: true };
}

---

(program
  (export_statement
    (function_declaration
      (identifier)
      (formal_parameters)
      (statement_block
        (return_statement
          (object
            (pair
              (property_identifier)
              (true))))))))

===================================================================
runtime / modules / reexports / default reexports keep value shapes
===================================================================

export { default as make } from "./defaults.ds";

---

(program
  (export_statement
    (export_clause
      (export_specifier
        (identifier)
        (identifier)))
    (string
      (string_fragment))))

===================================================================
runtime / modules / reexports / default reexports keep value shapes
===================================================================

import { make } from "./reexport.ds";

make().ok satisfies boolean;

---

(program
  (import_statement
    (import_clause
      (named_imports
        (import_specifier
          (identifier))))
    (string
      (string_fragment)))
  (expression_statement
    (satisfies_expression
      (member_expression
        (call_expression
          (identifier)
          (arguments))
        (property_identifier))
      (satisfies_type
        (predefined_type)))))
