# tree-sitter-destack

This directory is the canonical `tree-sitter-destack` fork in the Destack repository.
It includes a Destack dialect entry at `destack/`.
The fork is maintained directly in this repository.

Upstream compatibility dialects are preserved in `typescript/` and `tsx/`.
Destack editor integrations should target the `destack/` grammar path.
