use crate::harness::TestResult;
use crate::query::{QueryExpectation, QueryTestSession};

/// Run a code_actions test.
///
/// Verifies that code actions are available at the expected positions.
pub fn run(_session: &QueryTestSession, _expectation: Option<&QueryExpectation>) -> TestResult {
    todo!("#Incomplete: code_actions test runner")
}
