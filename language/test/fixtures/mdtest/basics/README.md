# Basics

Tests for fundamental type inference and type checking.

Covers literals (numbers, strings, booleans, arrays, objects), primitive types,
special types (null, undefined, void, any), and basic type annotations.
