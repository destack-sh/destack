# Inheritance

Tests for class inheritance (`extends`) and interface implementation (`implements`).

## Topics

- `extends.md` - class inheritance (single and multi-level)
- `implements.md` - interface implementation
