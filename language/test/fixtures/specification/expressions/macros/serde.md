# Serde

Serde support is generated by derive macros and consumed through format-specific protocols.

## json

### serde derives satisfy json protocols

Derived serialization satisfies the library protocols.

```ds
import { decode, encode, JSON, Json, JsonDeserialize, JsonSerialize } from "destack:json";

@derive(Serialize, Deserialize)
struct ServerConfig {
    host: string;
    port: uint16;
    secure: boolean;
}

const config = decode<ServerConfig>("{\"host\":\"127.0.0.1\",\"port\":8080,\"secure\":true}")?;

config satisfies ServerConfig;
config satisfies JsonSerialize;
ServerConfig satisfies JsonDeserialize;

const text = encode(config)?;
text satisfies string;

const value = JSON.parse(text)?;
value satisfies Json;
JSON.stringify(value)? satisfies string;
```

### serde derives compose through fields

Field types bring their own derived implementations.

```ds
import { decode, encode } from "destack:json";

@derive(Serialize, Deserialize)
struct DatabaseConfig {
    url: string;
    poolSize: uint16;
}

@derive(Serialize, Deserialize)
struct AppConfig {
    database: DatabaseConfig;
    tags: string[];
}

const config = decode<AppConfig>(
    "{\"database\":{\"url\":\"postgres://local\",\"poolSize\":8},\"tags\":[\"dev\"]}",
)?;

config.database.poolSize satisfies uint16;
encode(config)? satisfies string;
```

### serde derive requires serializable fields

A field without serialization support fails the derive.

```ds
import { encode } from "destack:json";

class Socket {}

@derive(Serialize)
struct Connection {
    socket: Socket;
}

const connection = Connection {
    socket: new Socket(),
};

encode(connection)?;
```

- contains: Serialize
