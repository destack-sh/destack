# Enums

Enums are nominal and have explicit backing type behavior.
These tests cover backing type behavior, nominal assignability, members, and module forwarding.

## Coverage

- `enum.md`: Backing types, nominal assignment boundaries, and enum members.
- `modules.md`: Cross-module enum identity and re-export forwarding.
