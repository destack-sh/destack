# Intrinsic Types

## intrinsic indexing

### intrinsic types cannot be indexed

> Intrinsic types cannot be indexed.

```ts:main.ts
type Bad = intrinsic["foo"];
```

- contains: intrinsic types cannot be indexed
