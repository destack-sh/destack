# Dynamic Resolution

Dynamic resolution specifies member access and method calls on union receivers.
These tests ensure that resolution considers all variants and composes with extensions and overload ordering.
