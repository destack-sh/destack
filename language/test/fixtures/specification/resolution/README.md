# Resolution

Resolution covers how names, members, calls, and overloads are selected.
Destack adds extensions and real overloading while still matching TypeScript behavior where applicable.

Extension visibility and overload ordering are core resolution invariants.
Dynamic resolution on union receivers is also specified here rather than under expressions.
