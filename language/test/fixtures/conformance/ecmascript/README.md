# ECMAScript Conformance Fixtures

This tree stores ECMAScript compatibility corpora and adapted runnable fixtures.
It is intended to cover the ECMAScript overlap claim through imported suites and related supporting fixtures.
