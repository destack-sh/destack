// SYNTAX TEST "source.ds"

// types / reflection / typeof
type ConfigValue = typeof config.value;
// <- keyword.declaration.type.ds

// types / reflection / keyof
type UserKeys = keyof User;
// <- keyword.declaration.type.ds

// types / reflection / indexed access
type UserName = User["name"];
// <- keyword.declaration.type.ds

