// SYNTAX TEST "source.ds"

// types / static / arrays associated guards imports and parameters
type Buffer<const N: uint, const Tag: string = "default"> = [uint8; N];
//          ^^^^^ storage.modifier.ds

declare let value: Buffer<4, "fast">;
//                 ^^^^^^ entity.name.type.ds

declare let sized: Buffer<2 + 2, "sum">;

type Tagged<const Tag: string> = { tag: Tag };

type Shape<const ...Dims: usize> = [usize; Dims];
//         ^^^^^ storage.modifier.ds
