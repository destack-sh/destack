// SYNTAX TEST "source.ds"

// memory / synchronization / shared mutexes
let counter: shared Mutex<Counter> = new Mutex(Counter { value: 0 });
// <- keyword.declaration.type.ds
const guard = counter.lock();
//                    ^^^^ entity.name.function.call.ds

// memory / synchronization / shared constants
shared const world: World = new World();
// <- keyword.other.modifier.ds
