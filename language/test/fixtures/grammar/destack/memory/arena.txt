// SYNTAX TEST "source.ds"

// memory / arena / arena handles and allocated values
let arena: Arena<User> = Arena.create();
//         ^^^^^ entity.name.type.ds

let user: &readonly User = arena.allocate(User.create());
//        ^ keyword.operator.elementwise.ds
