// SYNTAX TEST "source.ds"

// memory / conversions / owned to borrowed
let point = ^Point { x: 1 };
// <- keyword.declaration.variable.ds
let borrow = &point;
// <- keyword.declaration.variable.ds

// memory / conversions / mutable to readonly
let view: &readonly Point = borrow;
// <- keyword.declaration.variable.ds
