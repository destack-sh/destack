// SYNTAX TEST "source.ds"

// memory / stability / writes through borrows
let point = ^Point { x: 1 };
// <- keyword.declaration.type.ds
let borrow = &point;

borrow.x = 2;

// memory / stability / stable views
let name: &readonly string = &readonly user.name;
// <- keyword.declaration.type.ds
