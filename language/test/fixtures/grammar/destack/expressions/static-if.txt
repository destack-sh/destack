// SYNTAX TEST "source.ds"

// expressions / static-if / declarations
@if(import.meta.runtime == "js")
// <- entity.name.function.decorator.ds
function runtimeName(): string {
    "js"
}

// expressions / static-if / statements
function size<comptime Wide: bool>(): uint {
    @if(Wide)
//  ^^^ entity.name.function.decorator.ds
    return 8;

    @if(!Wide)
    return 4;
}

// expressions / static-if / members
struct Buffer<comptime Inline: bool> {
    @if(Inline)
//  ^^^ entity.name.function.decorator.ds
    data: InlineData;
}

