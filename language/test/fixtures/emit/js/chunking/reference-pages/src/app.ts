import { getPageTitle, getReferencePage } from "./page-reference";

/** The docs page state for the app entry. */
export const appReferencePage = getReferencePage("github");
/** The architecture reference title for the app entry. */
export const architectureReferenceTitle = getPageTitle("architecture");
