export const productName="Workspace",featureName="Control",releaseChannel="preview";export const unreadActivityCount=3;export function formatMetricLabel(count){return`${count} pending approvals`;};const shellLabel=`${productName} ${featureName}`,activityLabel=unreadActivityCount===0?"Inbox clear":formatMetricLabel(unreadActivityCount),channelLabel=releaseChannel?`channel:${releaseChannel}`:"channel:stable";export const pageTitle=shellLabel,summaryLine=`${shellLabel} • ${activityLabel} • ${channelLabel}`;
