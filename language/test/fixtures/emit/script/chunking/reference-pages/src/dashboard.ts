import { getPageTitle, getReferencePage } from "./page-reference";

/** The docs page state for the dashboard entry. */
export const dashboardReferencePage = getReferencePage("configuration");
/** The welcome title for the dashboard entry. */
export const dashboardWelcomeTitle = getPageTitle("");
