/** The overview route slug for the course area. */
export const overviewRoute = "courses-overview";

/** The dashboard route slug for the course area. */
export const dashboardRoute = "courses-dashboard";

/** The reports route slug for the course area. */
export const reportsRoute = "courses-reports";

/** The settings route slug for the course area. */
export const settingsRoute = "courses-settings";

/** Read the overview route for the course area. */
export function getOverviewRoute() {
    return overviewRoute;
}

/** Read the dashboard route for the course area. */
export function getDashboardRoute() {
    return dashboardRoute;
}

/** Read the reports route for the course area. */
export function getReportsRoute() {
    return reportsRoute;
}
