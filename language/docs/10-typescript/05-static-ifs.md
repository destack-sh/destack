---
title: Static Ifs
description: "`@if` static gating"
---

# Static Ifs

- `@if(staticTerm)` removes imports, declarations, members, statements, cases, arguments, fields, and similar contributions before checking and output
- `@if` is resolved before inference
