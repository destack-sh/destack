# Memory

Forkable virtual memory used by heaps, stacks, and other runtime-managed byte spaces.

## Testing

Run these from the repository root.

```sh
cargo test -p destack_memory
cargo check -p destack_memory --target wasm32-unknown-unknown
```
