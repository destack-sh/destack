[workspace]
# fuzzer is in its own workspace

[package]
name = "dyst_ast_fuzz"
description = "Dyst AST Fuzz"
edition = "2024"
authors = ["Symbol Industries"]
license = "proprietary"
rust-version = "1.88.0"

[package.metadata]
cargo-fuzz = true

[dependencies]
dyst_ast = { path = ".." }
dyst_source = { path = "../../source" }
dyst_token = { path = "../../token" }

arbitrary = { version = "1.4.2", features = ["derive"] }
libfuzzer-sys = "0.4.10"

[[bin]]
name = "dyst_ast_fuzz"
path = "fuzz_targets/ast.rs"
test = false
doc = false
