# ast

Destack AST (Abstract Syntax Tree) definitions.
Defines all node types produced by the parser for representing Destack source code.

## Layout

```
src/
├── token/       Token types, identifiers, spans
├── tree/        AST node definitions (expressions, declarations, patterns, etc.)
└── dump/        AST dumping utilities
```

