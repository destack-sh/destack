use dyst_source::{FileContent, FileId, FileType};

use crate::{Transpiler, TranspilerUnit, TranspilerUnitId};

/// An artifact generated by a TranspilerUnit.
#[derive(Debug, Clone)]
pub struct TranspilerArtifact {
    /// The id of the unit that generated this artifact.
    pub unit_id: TranspilerUnitId,
    /// The id of the file that generated this artifact.
    pub file_id: FileId,
    /// The type of the artifact.
    pub ty: FileType,
    /// The content of the artifact.
    pub content: FileContent,
}

impl<'a> Transpiler<'a> {
    /// Print a transpiled unit into an artifact.
    pub fn print_unit(&self, unit: &TranspilerUnit) -> TranspilerArtifact {
        todo!("print_unit {unit:?}");
    }
}
