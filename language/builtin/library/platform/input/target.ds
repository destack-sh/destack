import { WindowHandle } from "../resource/index.ds";

/// Optional window target for input operations.
export struct InputWindowTarget {
    /// Window target handle when one explicit window scope is required.
    /// When omitted, the active backend input focus context is used.
    window?: WindowHandle;
}
