/// Interface for concatenation.
///
/// `a.concat(b)` returns the concatenated value.
export newtype interface Concatenate<T = this> {
    /// The result type.
    type Output;

    /// Concatenate this value with `other`.
    concat(other: T): this.Output;
}
