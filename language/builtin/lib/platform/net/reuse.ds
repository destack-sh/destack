import type { PlatformError } from "../error/index.ds";
import { SocketHandle } from "../resource/index.ds";

/// Socket reuse option bindings for the native runtime.
declare namespace net {
    /// Enable or disable SO_REUSEADDR.
    @binding("destack.net.setReuseAddr", { effect: "external", replay: "recordable" })
    export function setReuseAddr(
        handle: SocketHandle,
        enabled: boolean,
    ): Result<void, PlatformError>;

    /// Enable or disable SO_REUSEPORT.
    @binding("destack.net.setReusePort", { effect: "external", replay: "recordable" })
    export function setReusePort(
        handle: SocketHandle,
        enabled: boolean,
    ): Result<void, PlatformError>;
}
