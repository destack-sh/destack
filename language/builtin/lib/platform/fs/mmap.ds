import { Slice } from "native";
import type { PlatformError } from "../error/index.ds";
import { FileHandle } from "../resource/index.ds";
import { FileOffset, FileSize } from "./flags.ds";

/// Memory protection flags for mappings.
export newtype MmapProt = uint32;

/// Mapping flags.
export newtype MmapFlags = uint32;

/// Sync flags for msync.
export newtype MmapSyncFlags = uint32;

/// Advice for madvise.
export enum MmapAdvice {
    /// Default behavior.
    Normal = 0,
    /// Access will be sequential.
    Sequential = 1,
    /// Access will be random.
    Random = 2,
    /// Will need data soon.
    WillNeed = 3,
    /// Will not need data soon.
    DontNeed = 4,
}

/// Memory mapping bindings for the native runtime.
declare namespace fs {
    /// Create a file-backed memory mapping.
    ///
    /// Map a file-backed region into virtual memory using the requested offset, length, and protection.
    /// Page alignment, commit behavior, and fault semantics follow host VM policy.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the kernel feature is unavailable.
    /// Uses mmap(2) MAP_SHARED/MAP_PRIVATE on Unix and CreateFileMapping/MapViewOfFile on Windows.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `fs.mmap`.
    ///
    /// # Replay
    /// External, recordable.
    @binding("destack.fs.mmap.mmapFile", { effect: "external", replay: "recordable", requires: ["fs.mmap"], scope: "os", blocking: "sometimes" })
    export function mmapFile(
        handle: FileHandle,
        offset: FileOffset,
        length: FileSize,
        prot: MmapProt,
        flags: MmapFlags,
    ): Result<Slice<uint8>, PlatformError>;

    /// Create an anonymous memory mapping.
    ///
    /// Map an anonymous zero-initialized region into virtual memory using host allocation primitives.
    /// Backing pages are managed by the host kernel and are not file-persistent.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the kernel feature is unavailable.
    /// Uses mmap(2) MAP_ANONYMOUS on Unix and VirtualAlloc/MapViewOfFile on Windows.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `fs.mmap`.
    ///
    /// # Replay
    /// External, recordable.
    @binding("destack.fs.mmap.mmapAnonymous", { effect: "external", replay: "recordable", requires: ["fs.mmap"], scope: "os", blocking: "sometimes" })
    export function mmapAnonymous(
        length: FileSize,
        prot: MmapProt,
        flags: MmapFlags,
    ): Result<Slice<uint8>, PlatformError>;

    /// Unmap a memory region.
    ///
    /// Unmap the specified virtual-memory range and release its mapping resources.
    /// Access after unmap is invalid and follows host memory fault behavior.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the kernel feature is unavailable.
    /// Uses munmap(2) on Unix and UnmapViewOfFile/VirtualFree on Windows.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `fs.mmap`.
    ///
    /// # Replay
    /// External, recordable.
    @binding("destack.fs.mmap.munmap", { effect: "external", replay: "recordable", requires: ["fs.mmap"], scope: "os", blocking: "sometimes" })
    export function munmap(mapping: Slice<uint8>): Result<void, PlatformError>;

    /// Change memory protection for a mapping.
    ///
    /// Update memory access permissions for the provided mapped region.
    /// Protection transitions are validated against host page and mapping constraints.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the kernel feature is unavailable.
    /// Uses mprotect(2) on Unix and VirtualProtect on Windows.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `fs.mmap`.
    ///
    /// # Replay
    /// External, recordable.
    @binding("destack.fs.mmap.mprotect", { effect: "external", replay: "recordable", requires: ["fs.mmap"], scope: "os", blocking: "sometimes" })
    export function mprotect(
        mapping: Slice<uint8>,
        prot: MmapProt,
    ): Result<void, PlatformError>;

    /// Flush a mapping to storage.
    ///
    /// Flush dirty pages in the mapped region to backing storage.
    /// Writeback scope and completion guarantees follow host `msync` semantics.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the kernel feature is unavailable.
    /// Uses msync(2) on Unix and FlushViewOfFile on Windows.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `fs.mmap`.
    ///
    /// # Replay
    /// External, recordable.
    @binding("destack.fs.mmap.msync", { effect: "external", replay: "recordable", requires: ["fs.mmap"], scope: "os", blocking: "sometimes" })
    export function msync(
        mapping: Slice<uint8>,
        flags: MmapSyncFlags,
    ): Result<void, PlatformError>;

    /// Advise the kernel about access patterns.
    ///
    /// Provide expected access behavior hints for the mapped region.
    /// Advice is best effort and may be ignored by the host memory manager.
    ///
    /// # Platform
    /// Unix and Windows. Operations return `notSupported` when the kernel feature is unavailable.
    /// Uses madvise(2) on Unix and advisory memory APIs where available on Windows.
    ///
    /// # Errors
    /// Returns ioNotFound, ioPermissionDenied, ioInvalidData, ioWouldBlock, notSupported.
    ///
    /// # Security
    /// Requires `fs.mmap`.
    ///
    /// # Replay
    /// External, recordable.
    @binding("destack.fs.mmap.madvise", { effect: "external", replay: "recordable", requires: ["fs.mmap"], scope: "os", blocking: "sometimes" })
    export function madvise(
        mapping: Slice<uint8>,
        advice: MmapAdvice,
    ): Result<void, PlatformError>;
}
