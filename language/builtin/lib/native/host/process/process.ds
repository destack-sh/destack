import { extern } from "../../../../core/intrinsic/decorator.ds";

/// Process bindings for native runtime.
declare namespace process {
    /// Return the process arguments.
    @extern("destack.process.args")
    export function args(): Array<string>;
}
