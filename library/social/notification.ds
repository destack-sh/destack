using dyst

/// Notification status.
enum NotificationStatus { Unread = 1, Read = 2, Dismissed = 3, Expired = 4, Rescinded = 5 }

/// Base Notification event.
@event
struct NotificationEvent {
    /// The Notification this event pertains to.
    notification: EntityReference<Notification>,
}

/// Notification sent.
@event
struct NotificationSentEvent using NotificationEvent {}
/// Notification rescinded.
@event
struct NotificationRescindedEvent using NotificationEvent {}
/// Notification read.
@event
struct NotificationReadEvent using NotificationEvent {}
/// Notification dismissed.
@event
struct NotificationDismissedEvent using NotificationEvent {}
/// Notification expired.
@event
struct NotificationExpiredEvent using NotificationEvent {}

/// A Notification is a message about something.
@entity
struct Notification {
    /// The title of the Notification.
    title: String,
    /// The status of the Notification.
    status: NotificationStatus,
    /// Optional text content.
    text: ?Text,
}


