import { Vector2, Vector3 } from "../vector.ds"

/// A triangle mesh in 2D defined by vertices and triangle indices.
export struct Mesh2D {
    vertices: Vector2[]
    triangles: uint32[]
}

/// A triangle mesh in 3D defined by vertices and triangle indices.
export struct Mesh3D {
    vertices: Vector3[]
    triangles: uint32[]
}

