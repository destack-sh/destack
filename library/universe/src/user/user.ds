import * as ds from "destack"

/// A User is a human ..Destack.
@entity
export struct User {
    /// The URL-friendly slug of the User.
    slug: string
    /// When the User last logged in.
    lastLoggedInAt: Timestamp?
    /// The email address of the User.
    email: string?
}

