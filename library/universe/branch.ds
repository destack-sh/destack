import destack

/// The type of a Branch.
export enum BranchType {
    Partial = 2
    Full = 10
    Root = 11
}

/// A Branch is a version of a Snapshot.
export struct Branch {
    /// The Branch type.
    type: BranchType
}

