{
    "name": "@destack-sh/napi",
    "version": "0.51.0",
    "license": "MIT",
    "type": "module",
    "main": "./src/index.ts",
    "scripts": {
        "build": "napi build --release --platform --output-dir ./src/generated",
        "prepublishOnly": "bun run build",
        "publish:dry": "npm publish --dry-run --access public",
        "publish:live": "npm publish --access public"
    },
    "dependencies": {},
    "devDependencies": {
        "@napi-rs/cli": "^3.4.1"
    }
}
