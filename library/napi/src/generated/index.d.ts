/* auto-generated by NAPI-RS */
/* eslint-disable */
/** A workspace containing multiple program roots. */
export declare class Workspace {
  /** Create a new Workspace. */
  constructor(options: WorkspaceOptions)
  /** Add a root to the workspace. */
  addRoot(root: string): void
  /** Get the current working directory. */
  get cwd(): string
  /** Get the number of programs in the workspace. */
  get programCount(): number
  /** Get the workspace root directory. */
  root(): string
}

/** NAPI wrapper for the Rust workspace service. */
export declare class WorkspaceService {
  /** Create a new WorkspaceService. */
  constructor(options: WorkspaceServiceOptions)
  /** Return the session cwd. */
  get cwd(): string
  /** Return the number of active program handles. */
  get programHandleCount(): number
  /** Open a workspace root. */
  openWorkspaceRoot(root: string): void
  /** Close a workspace root. */
  closeWorkspaceRoot(root: string): void
  /** Return whether a workspace root is active. */
  hasWorkspaceRoot(root: string): boolean
  /** Remove a workspace root. */
  removeWorkspaceRoot(root: string): boolean
  /** Clear cache entries for all roots. */
  clearCacheAll(): void
  /** Shutdown the service. */
  shutdown(): void
  /** Return the workspace handle for a root as a decimal string. */
  handleForRoot(root: string): string
  /** Apply a text update for a virtual file. */
  updateVirtualFile(path: string, content: string): WorkspaceServiceResult
  /** Apply a virtual file update. */
  applyVirtualUpdate(path: string, update: WorkspaceVirtualUpdate): WorkspaceServiceResult
  /** Apply watch events. */
  applyWatchEvents(events: Array<WorkspaceWatchEvent>): WorkspaceServiceResult
  /** Rescan all roots. */
  rescanAll(reason: WorkspaceServiceRescanReason): WorkspaceServiceResult
  /** Rescan selected roots. */
  rescanRoots(roots: Array<string>, analyze: boolean): WorkspaceServiceResult
  /** Analyze the module owning a path. */
  analyzePath(path: string): WorkspaceAnalyzeOutcome
  /** Ensure semantic query state is ready for a path. */
  ensureAnalyzedForPath(path: string): void
  /** Execute a query for the workspace that owns a path using a JSON payload. */
  queryForPathJson(path: string, requestJson: string): string
  /** Execute a query for a specific workspace handle using a JSON payload. */
  queryForHandleJson(handle: string, requestJson: string): string
}

/** An alias entry mapping a pattern to target values. */
export interface AliasEntry {
  /** The pattern to match (e.g., "@/*"). */
  pattern: string
  /** The target values to alias to. */
  targets: Array<AliasValue>
}

/** Alias value for module resolution. */
export interface AliasValue {
  /** The path to alias to (None means ignore/false). */
  path?: string
}

/** Suggestion applicability. */
export declare const enum Applicability {
  /** The suggestion can be applied automatically. */
  Automatic = 0,
  /** The suggestion may be unsafe to apply automatically. */
  Dangerous = 1
}

/** Options for check operations. */
export interface CheckOptions {
  /** The current working directory. */
  cwd: string
  /** The workspace roots to open initially. */
  roots: Array<string>
  /** Compiler options for check diagnostics. */
  compiler: CompilerOptions
}

/** Result payload for check operations. */
export interface CheckResult {
  /** Diagnostics emitted by this check. */
  diagnostics: Array<Diagnostic>
  /** Number of diagnostics. */
  diagnosticCount: number
  /** Whether diagnostics include at least one error. */
  hasErrors: boolean
  /** Whether diagnostics include at least one warning. */
  hasWarnings: boolean
}

/** Check a file synchronously. */
export declare function checkSync(path: string, content: string, options?: CheckOptions | undefined | null): CheckResult

/** The options for compiling a workspace. */
export interface CompilerOptions {
  /** The diagnostic options. */
  diagnostic: DiagnosticOptions
  /** The number of worker threads to use. */
  workers: number
  /** Whether to follow imports automatically. */
  followImports: boolean
  /** Options for resolving imports. */
  importResolve: ResolveOptions
  /** How unresolved imports should be handled during resolve. */
  resolveMode: CompilerResolveMode
  /** Whether to disallow ambiguous tree literal syntax. */
  disallowAmbiguousTreeLiteral: boolean
  /** Default integer width (if not specified). */
  defaultIntWidth: number
  /** Default float width (if not specified). */
  defaultFloatWidth: number
  /** Whether to make prelude items available. */
  injectPrelude: boolean
  /** Whether to load profile libraries by default. */
  loadLibs: boolean
  /** Whether to generate source maps. */
  sourceMap: boolean
  /** Whether to use ternary expressions for simple if else value expressions. */
  elaborateWithTernary: boolean
  /** Whether to split multi declarator let statements into individual lets. */
  elaborateSplitDeclarators: boolean
  /** Whether to make implicit returns explicit. */
  elaborateExplicitReturn: boolean
  /** Whether to wrap implicit casts inserted during elaborate in parentheses. */
  elaborateParenthesizeCasts: boolean
  /** Whether to retain comptime expressions as comments after execution. */
  retainComptimeAsComment: boolean
  /** Maximum length of retained comptime comments. */
  retainComptimeCommentMaxLength: number
  /** Whether to overwrite existing files. */
  emitOverwrite: boolean
  /** Whether to create parent directories if they do not exist. */
  emitCreateDirs: boolean
  /** Dry run: report what would be written without writing. */
  emitDryRun: boolean
  /** Whether to collect detailed timing tags. */
  timings: boolean
  /** Whether to validate builtin declaration libraries eagerly. */
  validateBuiltinLibs: boolean
  /** Whether to verify MIR after building it. */
  verifyMir: boolean
}

/** How unresolved imports should be handled during resolve. */
export declare const enum CompilerResolveMode {
  /** Emit errors for unresolved imports. */
  Strict = 0,
  /** Emit warnings for unresolved imports and continue. */
  Lenient = 1
}

/** Get the default check options. */
export declare function defaultCheckOptions(): CheckOptions

/** Get the default compiler options. */
export declare function defaultCompilerOptions(): CompilerOptions

/** Get the default format options. */
export declare function defaultFormatOptions(): FormatOptions

/** Get the default linter options. */
export declare function defaultLinterOptions(): LinterOptions

/** Get the default lint request options. */
export declare function defaultLinterRequestOptions(): LinterRequestOptions

/** Get the default parse options. */
export declare function defaultParseOptions(): ParseOptions

/** Get the default resolve options. */
export declare function defaultResolveOptions(): ResolveOptions

/** Get the default run options. */
export declare function defaultRunOptions(): RunOptions

/** Get the default transform options. */
export declare function defaultTransformOptions(): TransformOptions

/** Get the default workspace options. */
export declare function defaultWorkspaceOptions(): WorkspaceOptions

/** Get the default workspace service options. */
export declare function defaultWorkspaceServiceOptions(): WorkspaceServiceOptions

/** Shared diagnostic payload. */
export interface Diagnostic {
  /** Stable diagnostic code. */
  code: string
  /** Original diagnostic code before mapping. */
  originalCode?: string
  /** Diagnostic severity. */
  severity: DiagnosticSeverity
  /** Original diagnostic severity before mapping. */
  originalSeverity?: DiagnosticSeverity
  /** Diagnostic message. */
  message: string
  /** Primary file id for this diagnostic. */
  fileId: number
  /** Primary labeled span. */
  primarySpan: LabeledSpan
  /** Primary highlight spans. */
  primaryHighlightSpans?: Array<LabeledSpan>
  /** Secondary spans. */
  secondarySpans?: Array<LabeledSpan>
  /** Suggestions for this diagnostic. */
  suggestions?: Array<Suggestion>
}

/** Diagnostic options for re-mapping errors and warnings. */
export interface DiagnosticOptions {
  /** Which warning codes to error on (as errors). */
  errorWarnings: Array<string>
  /** Which error codes to suppress (as warnings). */
  suppressErrors: Array<string>
  /** Which warning codes to suppress. */
  suppressWarnings: Array<string>
}

/** Diagnostic severity. */
export declare const enum DiagnosticSeverity {
  /** Informational note. */
  Note = 0,
  /** Warning diagnostic. */
  Warning = 1,
  /** Error diagnostic. */
  Error = 2
}

/** How to enforce file extensions. */
export declare const enum EnforceExtension {
  /** Enforce file extensions (path must include extension). */
  Enabled = 0,
  /** Do not enforce file extensions (resolve tries appending extensions from the list). */
  Disabled = 1
}

/** Extension alias mapping entry. */
export interface ExtensionAliasEntry {
  /** Extension key (for example ".js"). */
  extension: string
  /** Aliases for this extension. */
  aliases: Array<string>
}

/** The format of a source file. */
export declare const enum FileType {
  /** `.ds` */
  Destack = 0,
  /** `.d.ds` */
  DestackDeclaration = 1,
  /** `.dst` */
  DestackText = 2,
  /** `.dsb` */
  DestackBinary = 3,
  /** `.js` */
  JavaScript = 4,
  /** `.jsx` */
  JavaScriptXml = 5,
  /** `.ts` */
  TypeScript = 6,
  /** `.tsx` */
  TypeScriptXml = 7,
  /** `.d.ts` */
  TypeScriptDeclaration = 8,
  /** `.txt` */
  Text = 9,
  /** `.toml` */
  Toml = 10,
  /** `.yaml`, `.yml` */
  Yaml = 11,
  /** `.json` */
  Json = 12,
  /** `.env` */
  Env = 13,
  /** `.html`, `.htm` */
  Html = 14,
  /** `.md` */
  Markdown = 15,
  /** `.css` */
  Css = 16,
  /** `.svg` */
  Svg = 17,
  /** `.wasm` */
  Wasm = 18,
  /** `.node` */
  Node = 19,
  /** Source map `.map`. */
  SourceMap = 20,
  /** Object file `.o`. */
  Object = 21,
  /** Destack AST cache `.ast`. */
  DestackAst = 22,
  /** Destack DIR cache `.dir`. */
  DestackDir = 23,
  /** Destack MIR cache `.mir`, `.dsmir`. */
  DestackMir = 24,
  /** Image files. */
  Image = 25,
  /** Font files. */
  Font = 26,
  /** Audio files. */
  Audio = 27,
  /** Video files. */
  Video = 28,
  /** 3D model files. */
  Model = 29,
  /** AI and ML model files. */
  Neural = 30,
  /** Document files. */
  Document = 31,
  /** Binary unknown format. */
  Binary = 32,
  /** Unknown file type. */
  Unknown = 33
}

/** Options for format operations. */
export interface FormatOptions {
  /** The type of line ending to apply to the printed input. */
  lineEnding: LineEnding
  /** The indent style. */
  indentStyle: IndentStyle
  /** Spaces per indent. */
  indentWidth: number
  /** Maximum line length (best effort). */
  lineWidth: number
}

/** Result payload for format operations. */
export interface FormatResult {
  /** Formatted output code. */
  code: string
}

/** Format a file synchronously. */
export declare function formatSync(path: string, content: string, options?: FormatOptions | undefined | null): FormatResult

/** The indent style. */
export declare const enum IndentStyle {
  /** Use tabs to indent. */
  Tab = 0,
  /** Use spaces to indent. */
  Space = 1
}

/** Labeled span payload for diagnostics. */
export interface LabeledSpan {
  /** Source span payload. */
  span: Span
  /** Label for this span. */
  label: string
}

/** The type of line ending to apply to the printed input. */
export declare const enum LineEnding {
  /** Line feed only (
  ), common on Linux and macOS as well as inside git repos. */
  LineFeed = 0,
  /** Carriage return + line feed characters (\r
  ), common on Windows. */
  CarriageReturnLineFeed = 1,
  /** Carriage return character only (\r), used very rarely. */
  CarriageReturn = 2
}

/** Preferred array type syntax. */
export declare const enum LinterArrayTypeStyle {
  /** Prefer T[] syntax. */
  Array = 0,
  /** Prefer Array<T> syntax. */
  Generic = 1
}

/** Linter category. */
export declare const enum LinterCategory {
  /** Correctness category. */
  Correctness = 0,
  /** Suspicious category. */
  Suspicious = 1,
  /** Performance category. */
  Performance = 2,
  /** Style category. */
  Style = 3,
  /** Security category. */
  Security = 4,
  /** Complexity category. */
  Complexity = 5,
  /** Restriction category. */
  Restriction = 6
}

/** Category level severity override. */
export interface LinterCategoryOverride {
  /** Category to override. */
  category: LinterCategory
  /** Severity for this category. */
  severity: LinterSeverity
}

/** Filename case style. */
export declare const enum LinterFilenameCase {
  /** kebab case style. */
  Kebab = 0,
  /** snake case style. */
  Snake = 1,
  /** camel case style. */
  Camel = 2,
  /** pascal case style. */
  Pascal = 3
}

/** Linter rule and threshold options. */
export interface LinterOptions {
  /** Whether linting is enabled. */
  enabled: boolean
  /** Base preset. */
  preset: LinterPreset
  /** Category level severity overrides. */
  categories: Array<LinterCategoryOverride>
  /** Individual rule severity overrides. */
  overrides: Array<LinterRuleOverride>
  /** Include declaration files when evaluating declaration gated rules. */
  includeDeclarationFiles: boolean
  /** Allow explicit void to intentionally discard promise results. */
  allowVoidDiscard: boolean
  /** Check callback positions in no-misused-promises. */
  checkMisusedPromisesInCallbacks: boolean
  /** Check conditionals in no-misused-promises. */
  checkMisusedPromisesInConditionals: boolean
  /** Parameter name prefixes ignored by no-unused-parameters. */
  ignoredUnusedParameterPrefixes: Array<string>
  /** Maximum boolean parameters or fields. */
  maxBooleans: number
  /** Maximum branches in a single conditional. */
  maxBranchingFactor: number
  /** Maximum cognitive complexity. */
  maxCognitiveComplexity: number
  /** Maximum cyclomatic complexity. */
  maxCyclomaticComplexity: number
  /** Maximum nesting depth. */
  maxDepth: number
  /** Maximum static parameters. */
  maxStaticParams: number
  /** Maximum lines per file. */
  maxLines: number
  /** Maximum lines per function. */
  maxLinesPerFunction: number
  /** Maximum callback nesting. */
  maxNestedCallbacks: number
  /** Maximum function parameters. */
  maxParams: number
  /** Maximum statements per function. */
  maxStatements: number
  /** Maximum return statements per function. */
  maxReturnStatements: number
  /** Maximum switch cases per switch statement. */
  maxSwitchCases: number
  /** Maximum variants in a union type or enum. */
  maxTypeVariants: number
  /** Maximum fields in a struct, class, or interface. */
  maxTypeFields: number
  /** Maximum type complexity. */
  maxTypeComplexity: number
  /** Maximum occurrences of the same string literal before warning. */
  maxDuplicateStringOccurrences: number
  /** Minimum lines required for duplicate code checks. */
  minDuplicateCodeLines: number
  /** Minimum tokens required for duplicate code checks. */
  minDuplicateCodeTokens: number
  /** Minimum similarity percent for near duplicate checks. */
  minDuplicateCodeNearSimilarity: number
  /** Maximum statements in a try block. */
  maxTryBlockStatements: number
  /** Preferred array type syntax. */
  arrayType: LinterArrayTypeStyle
  /** Preferred type definition syntax. */
  typeDefinitionStyle: LinterTypeDefinitionStyle
  /** Required catch clause error name. */
  catchErrorName: string
  /** Required filename case style. */
  filenameCase: LinterFilenameCase
  /** Allowed uppercase keyword prefixes for inline comments. */
  commentKeywords: Array<string>
  /** Allowed tags for keyword comments. */
  commentKeywordTags: Array<string>
  /** Minimum non empty lines required for separator heading comments. */
  commentSeparatorHeadingMinLines: number
  /** Magic numbers to allow. */
  allowedMagicNumbers: Array<number>
  /** Globals to restrict. */
  restrictedGlobals: Array<string>
  /** Import paths to restrict. */
  restrictedImports: Array<string>
  /** Comment terms to warn on. */
  warningCommentTerms: Array<string>
}

/** Linter preset. */
export declare const enum LinterPreset {
  /** No rules enabled by default. */
  None = 0,
  /** Recommended rules enabled. */
  Recommended = 1,
  /** All rules enabled. */
  All = 2
}

/** Request options for lint operations. */
export interface LinterRequestOptions {
  /** The current working directory. */
  cwd: string
  /** The workspace roots to open initially. */
  roots: Array<string>
  /** Compiler options for lint diagnostics. */
  compiler: CompilerOptions
  /** Linter options for lint diagnostics. */
  linter: LinterOptions
}

/** Result payload for lint operations. */
export interface LinterResult {
  /** Diagnostics emitted by this lint pass. */
  diagnostics: Array<Diagnostic>
  /** Number of diagnostics. */
  diagnosticCount: number
  /** Whether diagnostics include at least one error. */
  hasErrors: boolean
  /** Whether diagnostics include at least one warning. */
  hasWarnings: boolean
}

/** Rule level severity override. */
export interface LinterRuleOverride {
  /** Rule id to override. */
  rule: string
  /** Severity for this rule. */
  severity: LinterSeverity
}

/** Linter severity. */
export declare const enum LinterSeverity {
  /** Rule is disabled. */
  Off = 0,
  /** Rule produces notes. */
  Note = 1,
  /** Rule produces warnings. */
  Warning = 2,
  /** Rule produces errors. */
  Error = 3
}

/** Preferred type definition syntax. */
export declare const enum LinterTypeDefinitionStyle {
  /** Prefer type aliases. */
  Type = 0,
  /** Prefer interface declarations. */
  Interface = 1
}

/** Lint a file synchronously. */
export declare function lintSync(path: string, content: string, options?: LinterRequestOptions | undefined | null): LinterResult

/** The module output format. */
export declare const enum ModuleFormat {
  /** CommonJS modules. */
  CommonJs = 0,
  /** AMD modules. */
  Amd = 1,
  /** UMD modules. */
  Umd = 2,
  /** SystemJS modules. */
  System = 3,
  /** ES2015 modules. */
  Es2015 = 4,
  /** ES2020 modules. */
  Es2020 = 5,
  /** ES2022 modules. */
  Es2022 = 6,
  /** ESNext modules. */
  EsNext = 7,
  /** Node16 modules. */
  Node16 = 8,
  /** NodeNext modules. */
  NodeNext = 9,
  /** Preserve original module syntax. */
  Preserve = 10,
  /** No module system. */
  None = 11
}

/** The type of module content. */
export declare const enum ModuleType {
  /** Code module content. */
  Code = 0,
  /** Data module content. */
  Data = 1,
  /** Text module content. */
  Text = 2,
  /** Binary module content. */
  Binary = 3
}

/** Options for parse operations. */
export interface ParseOptions {
  /** The current working directory. */
  cwd: string
  /** The workspace roots to open initially. */
  roots: Array<string>
  /** Compiler options for parse diagnostics. */
  compiler: CompilerOptions
}

/** Parse result payload. */
export interface ParseResult {
  /** Parsed program payload. */
  program: Program
  /** Parse diagnostics captured during update. */
  diagnostics: Array<Diagnostic>
  /** Number of parse diagnostics. */
  diagnosticCount: number
  /** Return whether diagnostics include at least one error. */
  hasErrors: boolean
}

/** Parse a file synchronously. */
export declare function parseSync(path: string, content: string, options?: ParseOptions | undefined | null): ParseResult

/** Parsed program payload. */
export interface Program {
  /** Source path used to parse the module. */
  path: string
  /** Serialized module AST payload. */
  astJson: string
  /** Number of top level roots in the module. */
  rootCount: number
  /** Number of semantic tokens in the module. */
  tokenCount: number
  /** Number of side tokens in the module. */
  sideTokenCount: number
}

/** The result of a successful module resolution. */
export interface Resolution {
  /** The resolved path. */
  path: string
  /** Optional query string (e.g., "?foo"). */
  query?: string
  /** Optional fragment (e.g., "#bar"). */
  fragment?: string
}

/** Resolution options for module resolution. */
export interface ResolveOptions {
  /** Current working directory to start from. */
  cwd?: string
  /** TypeScript configuration options. */
  tsconfig: TypeScriptOptions
  /** Aliases to import or require certain modules more easily. */
  alias: Array<AliasEntry>
  /** Condition names for exports field which defines entry points of a package. */
  conditions: Array<string>
  /** Whether and how to enforce file extensions. */
  enforceExtension: EnforceExtension
  /** Attempt to resolve these extensions in order. */
  extensions: Array<string>
  /** Request passed to resolve is already fully specified. */
  isFullySpecified: boolean
  /** Redirect module requests when normal resolving fails. */
  fallback: Array<AliasEntry>
  /** Extension aliases for resolving import requests. */
  extensionAlias: Array<ExtensionAliasEntry>
  /** Main files in description files (e.g., ["index"]). */
  mainFiles: Array<string>
  /** Directories to resolve modules from (e.g., ["node_modules"]). */
  modules: Array<string>
  /** Resolve to a context instead of a file. */
  resolveToContext: boolean
  /** Prefer to resolve module requests as relative requests. */
  preferRelative: boolean
  /** Prefer to resolve server-relative urls as absolute paths. */
  preferAbsolute: boolean
  /** Path restrictions for resolved modules. */
  restrictions: Array<string>
  /** A list of directories where requests of server-relative URLs are resolved. */
  roots: Array<string>
  /** Whether to resolve symlinks to their symlinked location. */
  canonicalizeSymlinks: boolean
}

/** Resolve a module specifier synchronously. */
export declare function resolveSync(specifier: string, from: string, options?: ResolveOptions | undefined | null): Resolution

/** Cache stats payload for run operations. */
export interface RunCacheStats {
  /** Cache hits from memory. */
  hitsMemory: number
  /** Cache hits from disk. */
  hitsDisk: number
  /** Cache misses. */
  misses: number
  /** Cache writes to memory. */
  writesMemory: number
  /** Cache writes to disk. */
  writesDisk: number
  /** Cache errors. */
  errors: number
  /** Cache hit rate. */
  hitRate: number
}

/** Config override for run operations. */
export interface RunConfigOverride {
  /** Override path (for example compilerOptions.strict). */
  path: string
  /** Override payload as JSON. */
  valueJson: string
}

/** Environment variable override for run operations. */
export interface RunEnvironmentVariable {
  /** Environment variable name. */
  key: string
  /** Environment variable value. */
  value: string
}

/** Input payload for run operations. */
export interface RunInput {
  /** The input kind. */
  kind: RunInputKind
  /** File path for file inputs. */
  path?: string
  /** Virtual file name for inline or stdin inputs. */
  name?: string
  /** Source content for inline or stdin inputs. */
  content?: string
  /** Optional file type override for inline or stdin inputs. */
  fileType?: FileType
}

/** Input kind for run operations. */
export declare const enum RunInputKind {
  /** Execute a physical file path. */
  File = 0,
  /** Execute inline content. */
  Inline = 1,
  /** Execute stdin content. */
  Stdin = 2
}

/** Execution mode for run operations. */
export declare const enum RunMode {
  /** Execute entry module mode. */
  Program = 0,
  /** Evaluate inline or stdin mode without printing. */
  Eval = 1,
  /** Evaluate inline or stdin mode and print the result. */
  EvalPrint = 2
}

/** Options for run operations. */
export interface RunOptions {
  /** The current working directory. */
  cwd: string
  /** Workspace roots to open. */
  roots: Array<string>
  /** Compiler options for execution. */
  compiler: CompilerOptions
  /** Run mode. */
  mode: RunMode
  /** Entry function name. */
  entry: string
  /** Command line arguments passed to the program. */
  args: Array<string>
  /** Optional target name override. */
  target?: string
  /** Optional target overrides as JSON. */
  targetOverridesJson?: string
  /** Optional runtime overrides as JSON. */
  runtimeOverridesJson?: string
  /** Optional cache directory override. */
  cacheDir?: string
  /** Optional dsconfig path override. */
  configPath?: string
  /** Optional profile name override. */
  profile?: string
  /** Environment variable overrides. */
  env: Array<RunEnvironmentVariable>
  /** Config overrides. */
  overrides: Array<RunConfigOverride>
  /** Whether to allow dsconfig input fallback when no inputs are provided. */
  allowDsconfigFallback: boolean
  /** Whether to skip writes. */
  dryRun: boolean
}

/** Output chunk payload for run operations. */
export interface RunOutputChunk {
  /** Output stream kind. */
  stream: RunOutputStream
  /** Output bytes. */
  bytes: Array<number>
}

/** Output stream for run operations. */
export declare const enum RunOutputStream {
  /** Standard output. */
  Stdout = 0,
  /** Standard error. */
  Stderr = 1
}

/** Structured payload for run results. */
export interface RunPayload {
  /** Run payload kind. */
  kind: RunPayloadKind
  /** JSON value payload for successful runs. */
  valueJson?: string
  /** Runtime error message payload for failed runs. */
  message?: string
}

/** Run payload kind. */
export declare const enum RunPayloadKind {
  /** Run completed with a value payload. */
  Value = 0,
  /** Run failed with a runtime error payload. */
  RuntimeError = 1,
  /** Run payload kind is unknown. */
  Unknown = 2
}

/** Result payload for run operations. */
export interface RunResult {
  /** Whether command execution succeeded. */
  success: boolean
  /** Command exit code. */
  exitCode: number
  /** Diagnostics emitted by this run. */
  diagnostics: Array<Diagnostic>
  /** Number of diagnostics. */
  diagnosticCount: number
  /** Whether diagnostics include at least one error. */
  hasErrors: boolean
  /** Whether diagnostics include at least one warning. */
  hasWarnings: boolean
  /** Output chunks captured during execution. */
  output: Array<RunOutputChunk>
  /** Structured run payload data when available. */
  payload?: RunPayload
  /** Number of modules involved. */
  moduleCount: number
  /** Number of profiles involved. */
  profileCount: number
  /** Number of targets involved. */
  targetCount: number
  /** Command stats when available. */
  stats?: RunStats
}

/** Command stats payload for run operations. */
export interface RunStats {
  /** Elapsed time in milliseconds. */
  elapsedMs: number
  /** Number of tasks completed. */
  tasksCompleted: number
  /** Number of tasks failed. */
  tasksFailed: number
  /** Number of tasks skipped. */
  tasksSkipped: number
  /** Number of modules processed. */
  modulesProcessed: number
  /** Number of lines processed. */
  linesProcessed: number
  /** Number of slow tasks detected. */
  slowTasks: number
  /** Cache statistics when available. */
  cache?: RunCacheStats
  /** Timing tag statistics when available. */
  timings?: Array<RunTimingTagStats>
}

/** Run a program synchronously. */
export declare function runSync(input: RunInput, options?: RunOptions | undefined | null): RunResult

/** Timing stats payload for run operations. */
export interface RunTimingTagStats {
  /** Timing tag name. */
  name: string
  /** Total time spent in this tag in milliseconds. */
  durationMs: number
  /** Number of samples recorded. */
  sampleCount: number
}

/** Span payload for diagnostics. */
export interface Span {
  /** File id for this span. */
  fileId: number
  /** Span start offset. */
  start: number
  /** Span end offset. */
  end: number
}

/** Suggestion payload for diagnostics. */
export interface Suggestion {
  /** Spans involved in the suggestion. */
  spans: Array<LabeledSpan>
  /** Replacement text when available. */
  replacement?: string
  /** Suggestion message. */
  message: string
  /** Suggestion style. */
  style: SuggestionStyle
  /** Suggestion applicability. */
  applicability: Applicability
}

/** Suggestion style. */
export declare const enum SuggestionStyle {
  /** Standard suggestion display. */
  Normal = 0,
  /** Short suggestion display. */
  Short = 1,
  /** Hidden suggestion display. */
  Hidden = 2,
  /** Verbose suggestion display. */
  Verbose = 3
}

/** Options for transform file operations. */
export interface TransformOptions {
  /** The current working directory. */
  cwd: string
  /** The workspace roots to open initially. */
  roots: Array<string>
  /** Compiler options for analysis and generation. */
  compiler: CompilerOptions
  /** Output target format. */
  target: TransformTarget
}

/** Result payload for transform operations. */
export interface TransformResult {
  /** Emitted output code. */
  code: string
}

/** Transform a file synchronously. */
export declare function transformSync(path: string, content: string, options?: TransformOptions | undefined | null): TransformResult

/** Transform target format. */
export declare const enum TransformTarget {
  /** Emit TypeScript output. */
  TypeScript = 0,
  /** Emit JavaScript output. */
  JavaScript = 1
}

/** How to discover the TypeScript configuration file. */
export declare const enum TypeScriptDiscovery {
  /** Disable TypeScript configuration discovery. */
  Disabled = 0,
  /** Auto-discover the TypeScript configuration file. */
  Automatic = 1
}

/** TypeScript configuration options. */
export interface TypeScriptOptions {
  /** How to discover the TypeScript configuration file. */
  discovery: TypeScriptDiscovery
  /** Path to a specific tsconfig.json file (used when discovery is Disabled). */
  configFile?: string
  /** How to handle references. */
  references: TypeScriptReferences
  /** Paths used for manual references. */
  referencePaths: Array<string>
}

/** How to handle TypeScript project references. */
export declare const enum TypeScriptReferences {
  /** Disable references. */
  Disabled = 0,
  /** Auto-discover references from tsconfig.json. */
  Automatic = 1,
  /** Resolve only manually provided references. */
  Manual = 2
}

/** Outcome payload for explicit analyze requests. */
export interface WorkspaceAnalyzeOutcome {
  /** Whether semantic query state is ready for the analyzed module. */
  semanticQueryReady: boolean
  /** Detail when semantic query state is not ready. */
  detail?: string
}

/** File snapshot payload. */
export interface WorkspaceFileSnapshot {
  /** File id in the registry. */
  id: number
  /** File name. */
  name: string
  /** File uri. */
  uri: string
  /** Optional file path. */
  path?: string
  /** File type label. */
  fileType: string
  /** Optional text content. */
  content?: string
}

/** Invalidation summary payload. */
export interface WorkspaceInvalidation {
  /** The file id that triggered invalidation. */
  fileId: number
  /** Updated file version. */
  fileVersion: string
  /** Invalidation categories for the file. */
  kinds: Array<WorkspaceInvalidationKind>
  /** Invalidated module ids. */
  modules: Array<WorkspaceModuleId>
  /** Invalidated package ids. */
  packages: Array<string>
  /** Profiles with bumped versions. */
  profiles: Array<number>
  /** Profile graphs dropped due to invalidation. */
  graphsDropped: Array<number>
}

/** Invalidation kind payload. */
export declare const enum WorkspaceInvalidationKind {
  /** Source module content changed. */
  ModuleSource = 0,
  /** Package dsconfig changed. */
  DsConfig = 1,
  /** Tsconfig changed. */
  TsConfig = 2,
  /** No known mapping for the file. */
  Unknown = 3
}

/** Workspace message payload. */
export interface WorkspaceMessage {
  /** Message severity. */
  kind: WorkspaceServiceMessageKind
  /** Stable message code. */
  code: string
  /** Human-readable message. */
  message: string
}

/** Module id payload. */
export interface WorkspaceModuleId {
  /** The owning package id. */
  packageId: string
  /** Local identifier within the package. */
  localId: number
}

/** Options for creating a Workspace. */
export interface WorkspaceOptions {
  /** The current working directory. */
  cwd: string
}

/** Workspace message kind. */
export declare const enum WorkspaceServiceMessageKind {
  /** Informational message. */
  Info = 0,
  /** Warning message. */
  Warning = 1,
  /** Error message. */
  Error = 2
}

/** Options for creating a WorkspaceService. */
export interface WorkspaceServiceOptions {
  /** The current working directory. */
  cwd: string
  /** The workspace roots to open initially. */
  roots: Array<string>
  /** Compiler options for analysis and updates. */
  compiler?: CompilerOptions
}

/** Rescan reason for workspace operations. */
export declare const enum WorkspaceServiceRescanReason {
  /** Requested during startup synchronization. */
  Startup = 0,
  /** Requested after watcher overflow. */
  Overflow = 1,
  /** Requested manually. */
  Manual = 2,
  /** Requested after watch updates. */
  Update = 3
}

/** Result payload for workspace service updates. */
export interface WorkspaceServiceResult {
  /** Update records produced by the operation. */
  updates: Array<WorkspaceUpdateRecord>
  /** Message records produced by the operation. */
  messages: Array<WorkspaceMessage>
}

/** Workspace update record payload. */
export interface WorkspaceUpdateRecord {
  /** Updated module id when known. */
  moduleId?: WorkspaceModuleId
  /** Updated file id. */
  fileId: number
  /** Updated file snapshot. */
  file: WorkspaceFileSnapshot
  /** Invalidation summary for this update. */
  invalidation: WorkspaceInvalidation
  /** Diagnostics produced by this update. */
  diagnostics: Array<Diagnostic>
}

/** Virtual file update payload. */
export interface WorkspaceVirtualUpdate {
  /** The update kind. */
  kind: WorkspaceVirtualUpdateKind
  /** Text payload for text updates. */
  content?: string
  /** Byte payload for binary updates. */
  bytes?: Array<number>
}

/** File update kind for virtual updates. */
export declare const enum WorkspaceVirtualUpdateKind {
  /** Replace file content with text. */
  Text = 0,
  /** Replace file content with bytes. */
  Bytes = 1,
  /** Bump file version without changing content. */
  Touch = 2,
  /** Mark file as removed. */
  Removed = 3
}

/** File watch event payload. */
export interface WorkspaceWatchEvent {
  /** The file path. */
  path: string
  /** The previous path for rename events. */
  previousPath?: string
  /** The watch event kind. */
  kind: WorkspaceWatchEventKind
}

/** Watch event kind. */
export declare const enum WorkspaceWatchEventKind {
  /** A file was created. */
  Created = 0,
  /** A file was modified. */
  Modified = 1,
  /** A file was deleted. */
  Deleted = 2,
  /** A file was renamed. */
  Renamed = 3,
  /** Watcher reported overflow. */
  Overflow = 4
}
