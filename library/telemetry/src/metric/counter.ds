import * as ds from "destack"

/// A Counter Metric.
@entity
export struct CounterMetric extends Metric { }

/// A Counter Measurement.
@event
export struct CounterMeasurementEvent {
    /// The Counter metric definition.
    definition: Reference<CounterMetric>

    ...MeasurementEvent
}

