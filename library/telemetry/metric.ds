use destack

/// An Entity that represents a Metric.
@entity
struct Metric { }

/// An Event that represents a Measurement.
@event
struct MeasurementEvent {
    /// The metric definition self measurement is for.
    definition: Reference<Metric>
}

