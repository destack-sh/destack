/// A Result value which may hold Success(T) or Error(E).
union Result<T, E> {
    Success(T),
    Error(E),
}

implement Result {
    /// Check if the Result is a Success.
    function isSuccess(self) => boolean {
        match self {
            Success(_) => true
            Error(_) => false
        }
    }

    /// Check if the Result is an Error.
    function isError(self) => boolean {
        match self {
            Success(_) => false
            Error(_) => true
        }
    }
}