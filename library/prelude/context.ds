/// Marker trait for contexts.
public trait Context;

/// Default context.
public struct DefaultContext {
    public allocator: Allocator,
    // logger, tracer, stack trace, ...
    // time, location, ... (maybe put that into library)
}

implement Context for DefaultContext;