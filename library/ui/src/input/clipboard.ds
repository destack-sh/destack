import * as ds from "destack"

/// Clipboard base event
@event
export struct ClipboardEvent extends InputEvent { }

/// Copy event
@event
export struct CopyEvent extends ClipboardEvent { }

/// Cut event
@event
export struct CutEvent extends ClipboardEvent { }

/// Paste event
@event
export struct PasteEvent extends ClipboardEvent { }

