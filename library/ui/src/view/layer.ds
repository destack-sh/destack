import * as ds from "destack";

/// A Layer is a container for Views
@entity
export class Layer {
    /// Whether the layer is visible
    isVisible?: boolean,
    /// Layer opacity
    opacity?: float32,
}

