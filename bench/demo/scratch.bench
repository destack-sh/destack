--- math.instruct ---
require symbolx.stdlib@latest

task calculate :: (input: string) -> number:
"Compute a mathematical value from any expression"

task 'solve equation' :: (equation: string, variable: string) -> number | string:
"Solve an equation for a variable"

capability 'basic math':
"Perform basic arithmetic operations"
    task calculate
    task 'solve equation'

--- scratchpad.instruct ---
import capability 'basic math' from .math

# 'using' applies a symbol to the current scope
# should the using apply to the task? compile? both?
# what about 'using' for other symbols?
# using capability 'basic math'

task 'count fruits' :: (input: string) -> number:
"Count the number of distinct fruits mentioned"

# compile main:
#    pass

# # meta statements
# Transform/generate new bench (as text with eval or as AST)
# ## meta
# meta code generate:
# ```python
# ...?
# ```

# ## raw/source
# interpret the statement as source text
# (where raw is basically a meta code block like `return eval(value)`)
# raw some_weird_thing:
# ```bench
# ...
# ```

# or just anonymous statement
# source:
# ```bench
# ...
# ```


# # annotations
# ## @generated
# ## @deprecated

# # value types syntax

# ## map
# [string: number]

# ## tuple
# string, number