# Generated by Django 4.1.5 on 2023-02-27 16:46

from django.db import migrations, models


def create_owned_deployments(apps, schema_editor):
    Deployment = apps.get_model("bench", "Deployment")
    ProjectVersion = apps.get_model("bench", "ProjectVersion")

    # set owned to True for existing Deployments
    Deployment.objects.update(owned=True)

    # create owned deployments for every project version
    # (imitate what ProjectManager.create_project does now)
    for project_version in ProjectVersion.objects.all():
        if project_version.deployments.count() == 0:
            project = project_version.project
            Deployment.objects.create(
                project=project,
                project_version=project_version,
                organization=project.organization,
                user=project.user,
                type="ADHOC",
                status="INACTIVE",
                owned=True,
            )


class Migration(migrations.Migration):

    dependencies = [
        ("bench", "0008_projectversion_tag_and_more"),
    ]

    operations = [
        migrations.AddField(
            model_name="deployment",
            name="owned",
            field=models.BooleanField(default=False),
        ),
        migrations.RunPython(create_owned_deployments, migrations.RunPython.noop),
    ]
