name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: read

concurrency:
  group: release-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

env:
  CARGO_TERM_COLOR: always

jobs:
  release-metadata-check:
    name: Release Metadata Check
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          persist-credentials: false

      - name: Install just
        uses: taiki-e/install-action@29932c26a55563997b9acacb544fc35fd54aa781

      - name: Check Release Metadata
        run: just validate-release "${GITHUB_REF_NAME}"

  language-check:
    name: Language Check
    runs-on: ubuntu-latest
    timeout-minutes: 240
    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          persist-credentials: false

      - name: Install Rust Bun and just
        uses: ./.github/actions/setup-rust-bun-just
        with:
          rust_components: rustfmt clippy
          install_js_dependencies: "true"

      - name: Check Language
        run: just language/check-full

  library-check:
    name: Library Check
    runs-on: ubuntu-latest
    timeout-minutes: 120
    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          persist-credentials: false

      - name: Install Rust Bun and just
        uses: ./.github/actions/setup-rust-bun-just
        with:
          rust_components: rustfmt clippy
          install_js_dependencies: "true"

      - name: Check Library
        run: just library/check-full

  service-check:
    name: Service Check
    runs-on: ubuntu-latest
    timeout-minutes: 120
    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          persist-credentials: false

      - name: Install Rust Bun and just
        uses: ./.github/actions/setup-rust-bun-just
        with:
          rust_components: rustfmt clippy
          install_js_dependencies: "true"

      - name: Check Service
        run: just service/check-full

  app-check:
    name: App Check
    runs-on: ubuntu-latest
    timeout-minutes: 120
    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          persist-credentials: false

      - name: Install Rust Bun and just
        uses: ./.github/actions/setup-rust-bun-just
        with:
          rust_components: rustfmt clippy
          install_js_dependencies: "true"

      - name: Check App
        run: just app/check-full

  bridge-check:
    name: Bridge Check
    runs-on: ubuntu-latest
    timeout-minutes: 120
    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          persist-credentials: false

      - name: Install Rust Bun and just
        uses: ./.github/actions/setup-rust-bun-just
        with:
          rust_components: rustfmt clippy
          rust_targets: wasm32-wasip2
          install_js_dependencies: "true"

      - name: Check Bridge
        run: just bridge/check-full --pin-destack-revision "${GITHUB_SHA}"

  language-runtime-linux-check:
    name: Language Runtime Check (Linux)
    uses: ./.github/workflows/language-runtime-linux-check.yml

  language-runtime-macos-check:
    name: Language Runtime Check (macOS)
    uses: ./.github/workflows/language-runtime-macos-check.yml

  language-runtime-windows-check:
    name: Language Runtime Check (Windows)
    uses: ./.github/workflows/language-runtime-windows-check.yml

  release-artifacts:
    name: Release Artifacts
    needs:
      - release-metadata-check
      - language-check
      - library-check
      - service-check
      - app-check
      - bridge-check
      - language-runtime-linux-check
      - language-runtime-macos-check
      - language-runtime-windows-check
    uses: ./.github/workflows/release-channel-package.yml
    with:
      channel_label: Release
      artifact_prefix: release
      release_version: ${{ github.ref_name }}
      release_tag: ${{ github.ref_name }}
      release_channel: stable

  github-release-create:
    name: GitHub Release Create
    runs-on: ubuntu-latest
    needs:
      - release-artifacts
    if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/v')
    environment: release
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          persist-credentials: false

      - name: Download Release Schemas
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093
        with:
          name: release-schemas-${{ github.sha }}
          path: release-schemas

      - name: Download CLI Release Assets
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093
        with:
          name: release-cli-assets-${{ github.sha }}
          path: release-cli-assets

      - name: Check Release Signing Configuration
        run: |
          if [ -z "${RELEASE_GPG_PRIVATE_KEY}" ]; then
            echo "missing RELEASE_GPG_PRIVATE_KEY secret"
            exit 1
          fi
          if [ -z "${RELEASE_GPG_PASSPHRASE}" ]; then
            echo "missing RELEASE_GPG_PASSPHRASE secret"
            exit 1
          fi
          if [ -z "${RELEASE_GPG_KEY_ID}" ]; then
            echo "missing RELEASE_GPG_KEY_ID variable"
            exit 1
          fi
        env:
          RELEASE_GPG_PRIVATE_KEY: ${{ secrets.RELEASE_GPG_PRIVATE_KEY }}
          RELEASE_GPG_PASSPHRASE: ${{ secrets.RELEASE_GPG_PASSPHRASE }}
          RELEASE_GPG_KEY_ID: ${{ vars.RELEASE_GPG_KEY_ID }}

      - name: Install Release Signing Key
        run: |
          printf '%s\n' "${RELEASE_GPG_PRIVATE_KEY}" > release-signing.key.asc
          gpg --batch --import release-signing.key.asc
          rm -f release-signing.key.asc
        env:
          RELEASE_GPG_PRIVATE_KEY: ${{ secrets.RELEASE_GPG_PRIVATE_KEY }}

      - name: Sign Release Integrity Files
        run: |
          if [ ! -f "release-cli-assets/SHA256SUMS" ]; then
            echo "missing release-cli-assets/SHA256SUMS"
            exit 1
          fi
          if [ ! -f "release-cli-assets/manifest.json" ]; then
            echo "missing release-cli-assets/manifest.json"
            exit 1
          fi
          if [ ! -f "app/cli/install/install.sh" ]; then
            echo "missing app/cli/install/install.sh"
            exit 1
          fi
          if [ ! -f "app/cli/install/install.ps1" ]; then
            echo "missing app/cli/install/install.ps1"
            exit 1
          fi

          gpg --batch --yes --pinentry-mode loopback --passphrase "${RELEASE_GPG_PASSPHRASE}" --local-user "${RELEASE_GPG_KEY_ID}" --armor --detach-sign --output release-cli-assets/SHA256SUMS.asc release-cli-assets/SHA256SUMS
          gpg --batch --yes --pinentry-mode loopback --passphrase "${RELEASE_GPG_PASSPHRASE}" --local-user "${RELEASE_GPG_KEY_ID}" --armor --detach-sign --output release-cli-assets/manifest.json.asc release-cli-assets/manifest.json
          gpg --batch --yes --pinentry-mode loopback --passphrase "${RELEASE_GPG_PASSPHRASE}" --local-user "${RELEASE_GPG_KEY_ID}" --armor --detach-sign --output release-cli-assets/install.sh.asc app/cli/install/install.sh
          gpg --batch --yes --pinentry-mode loopback --passphrase "${RELEASE_GPG_PASSPHRASE}" --local-user "${RELEASE_GPG_KEY_ID}" --armor --detach-sign --output release-cli-assets/install.ps1.asc app/cli/install/install.ps1
        env:
          RELEASE_GPG_PASSPHRASE: ${{ secrets.RELEASE_GPG_PASSPHRASE }}
          RELEASE_GPG_KEY_ID: ${{ vars.RELEASE_GPG_KEY_ID }}

      - name: Create GitHub Release
        uses: softprops/action-gh-release@a06a81a03ee405af7f2048a818ed3f03bbf83c7b
        with:
          generate_release_notes: true
          files: |
            release-schemas/*
            release-cli-assets/*
            app/cli/install/install.sh
            app/cli/install/install.ps1
            app/cli/install/release-signing-public.asc

  package-publish:
    name: Package Publish
    runs-on: ubuntu-latest
    timeout-minutes: 120
    needs:
      - release-artifacts
      - github-release-create
    if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/v')
    environment: release
    permissions:
      contents: read
      id-token: write
    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          persist-credentials: false

      - name: Check Release Tag Version
        run: bash dev/ci/validate-release-tag-version.sh "${GITHUB_REF_NAME}"

      - name: Install Rust Bun and just
        uses: ./.github/actions/setup-rust-bun-just

      - name: Install Python
        uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065
        with:
          python-version: "3.12"

      - name: Download CLI Release Assets
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093
        with:
          name: release-cli-assets-${{ github.sha }}
          path: release-cli-assets

      - name: Download Release Signatures
        run: |
          release_download_base="https://github.com/${GITHUB_REPOSITORY}/releases/download/${GITHUB_REF_NAME}"
          curl --fail --location --silent --show-error "${release_download_base}/SHA256SUMS.asc" --output "release-cli-assets/SHA256SUMS.asc"
          curl --fail --location --silent --show-error "${release_download_base}/manifest.json.asc" --output "release-cli-assets/manifest.json.asc"
          curl --fail --location --silent --show-error "${release_download_base}/install.sh.asc" --output "release-cli-assets/install.sh.asc"
          curl --fail --location --silent --show-error "${release_download_base}/install.ps1.asc" --output "release-cli-assets/install.ps1.asc"

      - name: Check Release Signatures
        run: |
          export GNUPGHOME
          GNUPGHOME="$(mktemp -d)"
          chmod 700 "${GNUPGHOME}"
          trap 'rm -rf "${GNUPGHOME}"' EXIT

          gpg --batch --import app/cli/install/release-signing-public.asc
          gpg --batch --verify release-cli-assets/SHA256SUMS.asc release-cli-assets/SHA256SUMS
          gpg --batch --verify release-cli-assets/manifest.json.asc release-cli-assets/manifest.json
          gpg --batch --verify release-cli-assets/install.sh.asc app/cli/install/install.sh
          gpg --batch --verify release-cli-assets/install.ps1.asc app/cli/install/install.ps1

      - name: Install JS Dependencies
        run: bun install --frozen-lockfile

      - name: Check Bridge Publish Toolchains
        run: just bridge/ensure-toolchain

      - name: Stage CLI npm Binaries
        run: just app/stage-cli-binaries-from-artifacts "$(cat VERSION.txt)" "release-cli-assets"

      - name: Check Publish Configuration
        run: |
          if [ -z "${CARGO_TOKEN}" ]; then
            echo "missing CARGO_TOKEN secret"
            exit 1
          fi
          if [ -z "${VSCE_PAT}" ]; then
            echo "missing VSCE_PAT secret"
            exit 1
          fi

          if [ "${PUBLISH_ZED}" = "true" ]; then
            if [ -z "${ZED_GITHUB_TOKEN}" ]; then
              echo "missing ZED_GITHUB_TOKEN secret"
              exit 1
            fi
            if [ -z "${ZED_REGISTRY_PUSH_TO}" ]; then
              echo "missing ZED_REGISTRY_PUSH_TO variable"
              exit 1
            fi
          fi
        env:
          CARGO_TOKEN: ${{ secrets.CARGO_TOKEN }}
          VSCE_PAT: ${{ secrets.VSCE_PAT }}
          PUBLISH_ZED: ${{ vars.RELEASE_PUBLISH_ZED }}
          ZED_GITHUB_TOKEN: ${{ secrets.ZED_GITHUB_TOKEN }}
          ZED_REGISTRY_PUSH_TO: ${{ vars.ZED_REGISTRY_PUSH_TO }}

      - name: Publish npm packages
        run: |
          just library/publish ""
          just template/publish-create-destack-live
          just app/publish ""
          just bridge/publish-npm ""
        env:
          NPM_CONFIG_PROVENANCE: "true"

      - name: Publish Cargo packages
        run: just bridge/publish-cargo ""
        env:
          CARGO_TOKEN: ${{ secrets.CARGO_TOKEN }}

      - name: Build PyPI distributions
        run: |
          python3 -m pip install --upgrade pip build twine
          (cd bridge/python && python3 -m build && python3 -m twine check dist/*)
          (cd bridge/python/aliases/destack-py && python3 -m build && python3 -m twine check dist/*)

      - name: Publish PyPI package
        uses: pypa/gh-action-pypi-publish@ec4db0b4ddc65acdf4bff5fa45ac92d78b56bdf0
        with:
          packages-dir: bridge/python/dist

      - name: Publish PyPI legacy alias package
        uses: pypa/gh-action-pypi-publish@ec4db0b4ddc65acdf4bff5fa45ac92d78b56bdf0
        with:
          packages-dir: bridge/python/aliases/destack-py/dist

      - name: Publish VS Code extension
        run: just bridge/publish-vscode ""
        env:
          VSCE_PAT: ${{ secrets.VSCE_PAT }}

      - name: Publish zed registry PR
        if: vars.RELEASE_PUBLISH_ZED == 'true'
        run: just bridge/publish-zed "" "$(cat VERSION.txt)"
        env:
          GH_TOKEN: ${{ secrets.ZED_GITHUB_TOKEN }}
          DESTACK_ZED_REGISTRY_PUSH_TO: ${{ vars.ZED_REGISTRY_PUSH_TO }}
