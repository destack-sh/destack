name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: read

concurrency:
  group: release-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

env:
  CARGO_TERM_COLOR: always

jobs:
  release-metadata-check:
    name: Release Metadata Check
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          persist-credentials: false

      - name: Install just
        uses: taiki-e/install-action@29932c26a55563997b9acacb544fc35fd54aa781

      - name: Check Release Metadata
        run: just validate-release "${GITHUB_REF_NAME}"

  language-check:
    name: Language Check
    runs-on: ubuntu-latest
    timeout-minutes: 240
    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          persist-credentials: false

      - name: Install Language Check Toolchain
        uses: ./.github/actions/setup-language-check
        with:
          rust_components: rustfmt clippy
          install_js_dependencies: "true"

      - name: Check Language
        run: just language/full

  library-check:
    name: Library Check
    runs-on: ubuntu-latest
    timeout-minutes: 120
    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          persist-credentials: false

      - name: Install Rust Bun and just
        uses: ./.github/actions/setup-rust-bun-just
        with:
          rust_components: rustfmt clippy
          install_js_dependencies: "true"

      - name: Check Library
        run: just library/full

  service-check:
    name: Service Check
    runs-on: ubuntu-latest
    timeout-minutes: 120
    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          persist-credentials: false

      - name: Install Rust Bun and just
        uses: ./.github/actions/setup-rust-bun-just
        with:
          rust_components: rustfmt clippy
          install_js_dependencies: "true"

      - name: Check Service
        run: just service/full

  app-check:
    name: App Check
    runs-on: ubuntu-latest
    timeout-minutes: 120
    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          persist-credentials: false

      - name: Install Rust Bun and just
        uses: ./.github/actions/setup-rust-bun-just
        with:
          rust_components: rustfmt clippy
          install_js_dependencies: "true"

      - name: Check App
        run: just app/full

  bridge-check:
    name: Bridge Check
    runs-on: ubuntu-latest
    timeout-minutes: 120
    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          persist-credentials: false

      - name: Install Rust Bun and just
        uses: ./.github/actions/setup-rust-bun-just
        with:
          rust_components: rustfmt clippy
          rust_targets: wasm32-wasip2
          install_js_dependencies: "true"

      - name: Check Bridge
        run: just bridge/full --pin-destack-revision "${GITHUB_SHA}"

  runtime-android-check:
    name: Runtime Check (Android)
    uses: ./.github/workflows/runtime-android-check.yml

  runtime-ios-check:
    name: Runtime Check (iOS)
    uses: ./.github/workflows/runtime-ios-check.yml

  runtime-linux-check:
    name: Runtime Check (Linux)
    uses: ./.github/workflows/runtime-linux-check.yml

  runtime-macos-check:
    name: Runtime Check (macOS)
    uses: ./.github/workflows/runtime-macos-check.yml

  runtime-windows-check:
    name: Runtime Check (Windows)
    uses: ./.github/workflows/runtime-windows-check.yml

  release-build:
    name: Release Build
    runs-on: ubuntu-latest
    timeout-minutes: 120
    needs:
      - release-metadata-check
      - language-check
      - library-check
      - service-check
      - app-check
      - bridge-check
      - runtime-android-check
      - runtime-ios-check
      - runtime-linux-check
      - runtime-macos-check
      - runtime-windows-check
    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          persist-credentials: false

      - name: Install Rust and just
        uses: ./.github/actions/setup-rust-just

      - name: Build Language
        run: just language/build

      - name: Build CLI Schema
        run: just app/generate-schema

      - name: Upload Release Schemas
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        with:
          name: release-schemas-${{ github.sha }}
          path: |
            workspace/generated/dsconfig.schema.json
            app/cli/generated/cli-report.schema.json
          if-no-files-found: error

  cli-build:
    name: CLI Build (${{ matrix.target }})
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 120
    needs:
      - release-metadata-check
      - language-check
      - library-check
      - service-check
      - app-check
      - bridge-check
      - runtime-android-check
      - runtime-ios-check
      - runtime-linux-check
      - runtime-macos-check
      - runtime-windows-check
    strategy:
      fail-fast: false
      matrix:
        include:
          - runner: ubuntu-latest
            target: x86_64-unknown-linux-gnu
            linker: ""
          - runner: ubuntu-latest
            target: aarch64-unknown-linux-gnu
            linker: aarch64-linux-gnu-gcc
          - runner: macos-15-intel
            target: x86_64-apple-darwin
            linker: ""
          - runner: macos-14
            target: aarch64-apple-darwin
            linker: ""
          - runner: windows-latest
            target: x86_64-pc-windows-msvc
            linker: ""
    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          persist-credentials: false

      - name: Install Rust and just
        uses: ./.github/actions/setup-rust-just
        with:
          rust_targets: ${{ matrix.target }}

      - name: Install Linux Cross Compiler
        if: runner.os == 'Linux' && matrix.target == 'aarch64-unknown-linux-gnu'
        run: |
          sudo apt-get update
          sudo apt-get install -y gcc-aarch64-linux-gnu

      - name: Build CLI
        run: cargo build --release -p destack_cli --target "${{ matrix.target }}"
        env:
          CARGO_TARGET_AARCH64_UNKNOWN_LINUX_GNU_LINKER: ${{ matrix.linker }}

      - name: Stage Unix CLI Binaries
        if: runner.os != 'Windows'
        run: |
          mkdir -p "release-binaries/${{ matrix.target }}"
          cp "target/${{ matrix.target }}/release/destack" "release-binaries/${{ matrix.target }}/destack"
          cp "target/${{ matrix.target }}/release/ds" "release-binaries/${{ matrix.target }}/ds"
          cp "target/${{ matrix.target }}/release/dsc" "release-binaries/${{ matrix.target }}/dsc"
          cp "target/${{ matrix.target }}/release/dsx" "release-binaries/${{ matrix.target }}/dsx"

      - name: Stage Windows CLI Binaries
        if: runner.os == 'Windows'
        shell: powershell
        run: |
          New-Item -ItemType Directory -Path "release-binaries/${{ matrix.target }}" -Force | Out-Null
          Copy-Item "target/${{ matrix.target }}/release/destack.exe" "release-binaries/${{ matrix.target }}/destack.exe" -Force
          Copy-Item "target/${{ matrix.target }}/release/ds.exe" "release-binaries/${{ matrix.target }}/ds.exe" -Force
          Copy-Item "target/${{ matrix.target }}/release/dsc.exe" "release-binaries/${{ matrix.target }}/dsc.exe" -Force
          Copy-Item "target/${{ matrix.target }}/release/dsx.exe" "release-binaries/${{ matrix.target }}/dsx.exe" -Force

      - name: Upload CLI Binaries
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        with:
          name: cli-binaries-${{ matrix.target }}-${{ github.sha }}
          path: release-binaries/${{ matrix.target }}
          if-no-files-found: error

  cli-package:
    name: CLI Package
    runs-on: ubuntu-latest
    timeout-minutes: 60
    needs:
      - cli-build
    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          persist-credentials: false

      - name: Download Linux x64 CLI Binaries
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093
        with:
          name: cli-binaries-x86_64-unknown-linux-gnu-${{ github.sha }}
          path: release-binaries/x86_64-unknown-linux-gnu

      - name: Download Linux arm64 CLI Binaries
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093
        with:
          name: cli-binaries-aarch64-unknown-linux-gnu-${{ github.sha }}
          path: release-binaries/aarch64-unknown-linux-gnu

      - name: Download macOS x64 CLI Binaries
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093
        with:
          name: cli-binaries-x86_64-apple-darwin-${{ github.sha }}
          path: release-binaries/x86_64-apple-darwin

      - name: Download macOS arm64 CLI Binaries
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093
        with:
          name: cli-binaries-aarch64-apple-darwin-${{ github.sha }}
          path: release-binaries/aarch64-apple-darwin

      - name: Download Windows x64 CLI Binaries
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093
        with:
          name: cli-binaries-x86_64-pc-windows-msvc-${{ github.sha }}
          path: release-binaries/x86_64-pc-windows-msvc

      - name: Stage CLI Binaries
        run: |
          mkdir -p target/x86_64-unknown-linux-gnu/release
          mkdir -p target/aarch64-unknown-linux-gnu/release
          mkdir -p target/x86_64-apple-darwin/release
          mkdir -p target/aarch64-apple-darwin/release
          mkdir -p target/x86_64-pc-windows-msvc/release

          cp release-binaries/x86_64-unknown-linux-gnu/* target/x86_64-unknown-linux-gnu/release/
          cp release-binaries/aarch64-unknown-linux-gnu/* target/aarch64-unknown-linux-gnu/release/
          cp release-binaries/x86_64-apple-darwin/* target/x86_64-apple-darwin/release/
          cp release-binaries/aarch64-apple-darwin/* target/aarch64-apple-darwin/release/
          cp release-binaries/x86_64-pc-windows-msvc/* target/x86_64-pc-windows-msvc/release/

      - name: Package CLI Release Assets
        run: bash app/cli/install/package-release-artifacts.sh "$(cat VERSION.txt)" release-cli-assets

      - name: Check CLI Release Assets
        run: bash app/cli/install/validate-release-artifacts.sh "$(cat VERSION.txt)" release-cli-assets

      - name: Check CLI Installer
        run: bash app/cli/install/test-install.sh "$(cat VERSION.txt)" release-cli-assets

      - name: Upload CLI Release Assets
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        with:
          name: release-cli-assets-${{ github.sha }}
          path: release-cli-assets/*
          if-no-files-found: error

  windows-installer-smoke-check:
    name: Windows Installer Smoke Check
    runs-on: windows-latest
    timeout-minutes: 30
    needs:
      - cli-package
    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          persist-credentials: false

      - name: Install Python
        uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065
        with:
          python-version: "3.12"

      - name: Download CLI Release Assets
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093
        with:
          name: release-cli-assets-${{ github.sha }}
          path: release-cli-assets

      - name: Check Windows Installer
        shell: powershell
        run: |
          $version = (Get-Content "VERSION.txt").Trim()
          $releaseTag = "v$version"
          $tempRoot = Join-Path $env:RUNNER_TEMP "destack-install-smoke-$env:GITHUB_RUN_ID"
          $releaseDirectory = Join-Path $tempRoot "releases\download\$releaseTag"
          $installDirectory = Join-Path $tempRoot "install\bin"
          $port = Get-Random -Minimum 20000 -Maximum 60000
          $pythonCommand = $null
          $pythonArguments = @()

          New-Item -ItemType Directory -Path $releaseDirectory -Force | Out-Null
          Copy-Item "release-cli-assets/destack-$version-x86_64-pc-windows-msvc.zip" "$releaseDirectory/" -Force
          Copy-Item "release-cli-assets/SHA256SUMS" "$releaseDirectory/" -Force

          if ($null -ne (Get-Command "python" -ErrorAction SilentlyContinue)) {
            $pythonCommand = "python"
          } elseif ($null -ne (Get-Command "py" -ErrorAction SilentlyContinue)) {
            $pythonCommand = "py"
            $pythonArguments += "-3"
          } else {
            throw "missing python runtime on windows runner"
          }

          $pythonArguments += @("-m", "http.server", "$port", "--bind", "127.0.0.1", "--directory", "$tempRoot")

          $serverProcess = $null
          try {
            $serverProcess = Start-Process `
              -FilePath $pythonCommand `
              -ArgumentList $pythonArguments `
              -PassThru

            $healthUrl = "http://127.0.0.1:$port/releases/download/$releaseTag/SHA256SUMS"
            $attempt = 0
            while ($attempt -lt 10) {
              try {
                Invoke-WebRequest -Uri $healthUrl | Out-Null
                break
              } catch {
                Start-Sleep -Seconds 1
                $attempt += 1
              }
            }

            if ($attempt -ge 10) {
              throw "local installer smoke server did not start in time"
            }

            $env:DESTACK_VERSION = $version
            $env:DESTACK_RELEASE_BASE_URL = "http://127.0.0.1:$port/releases/download"
            $env:DESTACK_INSTALL = $installDirectory
            $env:DESTACK_NO_MODIFY_PATH = "1"
            .\app\cli\install\install.ps1

            & (Join-Path $installDirectory "destack.exe") --version | Out-Null
            & (Join-Path $installDirectory "ds.exe") --version | Out-Null
          } finally {
            if ($null -ne $serverProcess -and -not $serverProcess.HasExited) {
              Stop-Process -Id $serverProcess.Id -Force
            }
          }

  github-release-create:
    name: GitHub Release Create
    runs-on: ubuntu-latest
    needs:
      - release-build
      - cli-package
      - windows-installer-smoke-check
    if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/v')
    environment: release
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          persist-credentials: false

      - name: Download Release Schemas
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093
        with:
          name: release-schemas-${{ github.sha }}
          path: release-schemas

      - name: Download CLI Release Assets
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093
        with:
          name: release-cli-assets-${{ github.sha }}
          path: release-cli-assets

      - name: Check Release Signing Configuration
        run: |
          if [ -z "${RELEASE_GPG_PRIVATE_KEY}" ]; then
            echo "missing RELEASE_GPG_PRIVATE_KEY secret"
            exit 1
          fi
          if [ -z "${RELEASE_GPG_PASSPHRASE}" ]; then
            echo "missing RELEASE_GPG_PASSPHRASE secret"
            exit 1
          fi
          if [ -z "${RELEASE_GPG_KEY_ID}" ]; then
            echo "missing RELEASE_GPG_KEY_ID variable"
            exit 1
          fi
        env:
          RELEASE_GPG_PRIVATE_KEY: ${{ secrets.RELEASE_GPG_PRIVATE_KEY }}
          RELEASE_GPG_PASSPHRASE: ${{ secrets.RELEASE_GPG_PASSPHRASE }}
          RELEASE_GPG_KEY_ID: ${{ vars.RELEASE_GPG_KEY_ID }}

      - name: Install Release Signing Key
        run: |
          printf '%s\n' "${RELEASE_GPG_PRIVATE_KEY}" > release-signing.key.asc
          gpg --batch --import release-signing.key.asc
          rm -f release-signing.key.asc
        env:
          RELEASE_GPG_PRIVATE_KEY: ${{ secrets.RELEASE_GPG_PRIVATE_KEY }}

      - name: Sign Release Integrity Files
        run: |
          if [ ! -f "release-cli-assets/SHA256SUMS" ]; then
            echo "missing release-cli-assets/SHA256SUMS"
            exit 1
          fi
          if [ ! -f "release-cli-assets/manifest.json" ]; then
            echo "missing release-cli-assets/manifest.json"
            exit 1
          fi
          if [ ! -f "app/cli/install/install.sh" ]; then
            echo "missing app/cli/install/install.sh"
            exit 1
          fi
          if [ ! -f "app/cli/install/install.ps1" ]; then
            echo "missing app/cli/install/install.ps1"
            exit 1
          fi

          gpg --batch --yes --pinentry-mode loopback --passphrase "${RELEASE_GPG_PASSPHRASE}" --local-user "${RELEASE_GPG_KEY_ID}" --armor --detach-sign --output release-cli-assets/SHA256SUMS.asc release-cli-assets/SHA256SUMS
          gpg --batch --yes --pinentry-mode loopback --passphrase "${RELEASE_GPG_PASSPHRASE}" --local-user "${RELEASE_GPG_KEY_ID}" --armor --detach-sign --output release-cli-assets/manifest.json.asc release-cli-assets/manifest.json
          gpg --batch --yes --pinentry-mode loopback --passphrase "${RELEASE_GPG_PASSPHRASE}" --local-user "${RELEASE_GPG_KEY_ID}" --armor --detach-sign --output release-cli-assets/install.sh.asc app/cli/install/install.sh
          gpg --batch --yes --pinentry-mode loopback --passphrase "${RELEASE_GPG_PASSPHRASE}" --local-user "${RELEASE_GPG_KEY_ID}" --armor --detach-sign --output release-cli-assets/install.ps1.asc app/cli/install/install.ps1
        env:
          RELEASE_GPG_PASSPHRASE: ${{ secrets.RELEASE_GPG_PASSPHRASE }}
          RELEASE_GPG_KEY_ID: ${{ vars.RELEASE_GPG_KEY_ID }}

      - name: Create GitHub Release
        uses: softprops/action-gh-release@a06a81a03ee405af7f2048a818ed3f03bbf83c7b
        with:
          generate_release_notes: true
          files: |
            release-schemas/*
            release-cli-assets/*
            app/cli/install/install.sh
            app/cli/install/install.ps1
            app/cli/install/release-signing-public.asc

  package-publish:
    name: Package Publish
    runs-on: ubuntu-latest
    timeout-minutes: 120
    needs:
      - release-build
      - cli-package
      - windows-installer-smoke-check
      - github-release-create
    if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/v')
    environment: release
    permissions:
      contents: read
      id-token: write
    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          persist-credentials: false

      - name: Check Release Tag Version
        run: bash ci/validate-release-tag-version.sh "${GITHUB_REF_NAME}"

      - name: Install Rust Bun and just
        uses: ./.github/actions/setup-rust-bun-just

      - name: Install Python
        uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065
        with:
          python-version: "3.12"

      - name: Install Ruby
        uses: ruby/setup-ruby@e5517072e87f198d9533967ae13d97c11b604005
        with:
          ruby-version: "3.4"

      - name: Install Dart
        uses: dart-lang/setup-dart@e51d8e571e22473a2ddebf0ef8a2123f0ab2c02c

      - name: Download CLI Release Assets
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093
        with:
          name: release-cli-assets-${{ github.sha }}
          path: release-cli-assets

      - name: Download Release Signatures
        run: |
          release_download_base="https://github.com/${GITHUB_REPOSITORY}/releases/download/${GITHUB_REF_NAME}"
          curl --fail --location --silent --show-error "${release_download_base}/SHA256SUMS.asc" --output "release-cli-assets/SHA256SUMS.asc"
          curl --fail --location --silent --show-error "${release_download_base}/manifest.json.asc" --output "release-cli-assets/manifest.json.asc"
          curl --fail --location --silent --show-error "${release_download_base}/install.sh.asc" --output "release-cli-assets/install.sh.asc"
          curl --fail --location --silent --show-error "${release_download_base}/install.ps1.asc" --output "release-cli-assets/install.ps1.asc"

      - name: Check Release Signatures
        run: |
          export GNUPGHOME
          GNUPGHOME="$(mktemp -d)"
          chmod 700 "${GNUPGHOME}"
          trap 'rm -rf "${GNUPGHOME}"' EXIT

          gpg --batch --import app/cli/install/release-signing-public.asc
          gpg --batch --verify release-cli-assets/SHA256SUMS.asc release-cli-assets/SHA256SUMS
          gpg --batch --verify release-cli-assets/manifest.json.asc release-cli-assets/manifest.json
          gpg --batch --verify release-cli-assets/install.sh.asc app/cli/install/install.sh
          gpg --batch --verify release-cli-assets/install.ps1.asc app/cli/install/install.ps1

      - name: Install JS Dependencies
        run: bun install --frozen-lockfile

      - name: Install Bridge Publish Toolchains
        run: DESTACK_AUTO_INSTALL_TOOLCHAINS=1 just bridge/ensure-toolchain

      - name: Stage CLI npm Binaries
        run: just app/stage-cli-binaries-from-artifacts "$(cat VERSION.txt)" "release-cli-assets"

      - name: Check Publish Configuration
        run: |
          if [ -z "${CARGO_TOKEN}" ]; then
            echo "missing CARGO_TOKEN secret"
            exit 1
          fi
          if [ -z "${VSCE_PAT}" ]; then
            echo "missing VSCE_PAT secret"
            exit 1
          fi
          if [ -z "${MAVEN_REPOSITORY_USERNAME}" ]; then
            echo "missing MAVEN_REPOSITORY_USERNAME secret"
            exit 1
          fi
          if [ -z "${MAVEN_REPOSITORY_PASSWORD}" ]; then
            echo "missing MAVEN_REPOSITORY_PASSWORD secret"
            exit 1
          fi
          if [ -z "${MAVEN_GPG_PRIVATE_KEY}" ]; then
            echo "missing MAVEN_GPG_PRIVATE_KEY secret"
            exit 1
          fi
          if [ -z "${MAVEN_GPG_PASSPHRASE}" ]; then
            echo "missing MAVEN_GPG_PASSPHRASE secret"
            exit 1
          fi
          if [ -z "${MAVEN_GPG_KEY_ID}" ]; then
            echo "missing MAVEN_GPG_KEY_ID variable"
            exit 1
          fi
          if [ -z "${HEX_API_KEY}" ]; then
            echo "missing HEX_API_KEY secret"
            exit 1
          fi

          if [ -z "${NUGET_PUBLISH_USERNAME}" ]; then
            echo "missing NUGET_PUBLISH_USERNAME variable"
            exit 1
          fi

          if [ -z "${RUBYGEMS_OIDC_ROLE}" ]; then
            echo "missing RUBYGEMS_OIDC_ROLE variable"
            exit 1
          fi

          if [ "${PUBLISH_ZED}" = "true" ]; then
            if [ -z "${ZED_GITHUB_TOKEN}" ]; then
              echo "missing ZED_GITHUB_TOKEN secret"
              exit 1
            fi
            if [ -z "${ZED_REGISTRY_PUSH_TO}" ]; then
              echo "missing ZED_REGISTRY_PUSH_TO variable"
              exit 1
            fi
          fi
        env:
          NUGET_PUBLISH_USERNAME: ${{ vars.NUGET_PUBLISH_USERNAME }}
          RUBYGEMS_OIDC_ROLE: ${{ vars.RUBYGEMS_OIDC_ROLE }}
          CARGO_TOKEN: ${{ secrets.CARGO_TOKEN }}
          VSCE_PAT: ${{ secrets.VSCE_PAT }}
          MAVEN_REPOSITORY_USERNAME: ${{ secrets.MAVEN_REPOSITORY_USERNAME }}
          MAVEN_REPOSITORY_PASSWORD: ${{ secrets.MAVEN_REPOSITORY_PASSWORD }}
          MAVEN_GPG_PRIVATE_KEY: ${{ secrets.MAVEN_GPG_PRIVATE_KEY }}
          MAVEN_GPG_PASSPHRASE: ${{ secrets.MAVEN_GPG_PASSPHRASE }}
          MAVEN_GPG_KEY_ID: ${{ vars.MAVEN_GPG_KEY_ID }}
          HEX_API_KEY: ${{ secrets.HEX_API_KEY }}
          PUBLISH_ZED: ${{ vars.RELEASE_PUBLISH_ZED }}
          ZED_GITHUB_TOKEN: ${{ secrets.ZED_GITHUB_TOKEN }}
          ZED_REGISTRY_PUSH_TO: ${{ vars.ZED_REGISTRY_PUSH_TO }}

      - name: Install NuGet Credentials
        id: nuget-login
        uses: NuGet/login@d22cc5f58ff5b88bf9bd452535b4335137e24544
        with:
          user: ${{ vars.NUGET_PUBLISH_USERNAME }}

      - name: Install RubyGems Credentials
        uses: rubygems/configure-rubygems-credentials@bc6dd217f8a4f919d6835fcfefd470ef821f5c44
        with:
          role-to-assume: ${{ vars.RUBYGEMS_OIDC_ROLE }}

      - name: Publish npm packages
        run: |
          just library/publish ""
          just template/publish-create-destack-live
          just app/publish ""
          just bridge/publish-npm ""
          just bridge/publish-bun ""
          just bridge/publish-vite ""
        env:
          NPM_CONFIG_PROVENANCE: "true"

      - name: Publish Cargo packages
        run: just bridge/publish-cargo ""
        env:
          CARGO_TOKEN: ${{ secrets.CARGO_TOKEN }}

      - name: Publish NuGet package
        run: just bridge/publish-nuget ""
        env:
          NUGET_API_KEY: ${{ steps.nuget-login.outputs.NUGET_API_KEY }}
          NUGET_OIDC_API_KEY: ${{ steps.nuget-login.outputs.NUGET_API_KEY }}

      - name: Publish Maven package
        run: just bridge/publish-maven ""
        env:
          MAVEN_REPOSITORY_USERNAME: ${{ secrets.MAVEN_REPOSITORY_USERNAME }}
          MAVEN_REPOSITORY_PASSWORD: ${{ secrets.MAVEN_REPOSITORY_PASSWORD }}
          MAVEN_GPG_PRIVATE_KEY: ${{ secrets.MAVEN_GPG_PRIVATE_KEY }}
          MAVEN_GPG_PASSPHRASE: ${{ secrets.MAVEN_GPG_PASSPHRASE }}
          MAVEN_GPG_KEY_ID: ${{ vars.MAVEN_GPG_KEY_ID }}

      - name: Publish Ruby gem
        run: just bridge/publish-rubygems ""

      - name: Publish Hex package
        run: just bridge/publish-hex ""
        env:
          HEX_API_KEY: ${{ secrets.HEX_API_KEY }}

      - name: Publish pub.dev package with OIDC
        run: just bridge/publish-pub ""
        env:
          DESTACK_PUB_USE_OIDC: "1"

      - name: Build PyPI distributions
        run: |
          python3 -m pip install --upgrade pip build twine
          (cd bridge/python && python3 -m build && python3 -m twine check dist/*)
          (cd bridge/python/aliases/destack-py && python3 -m build && python3 -m twine check dist/*)

      - name: Publish PyPI package
        uses: pypa/gh-action-pypi-publish@ec4db0b4ddc65acdf4bff5fa45ac92d78b56bdf0
        with:
          packages-dir: bridge/python/dist

      - name: Publish PyPI legacy alias package
        uses: pypa/gh-action-pypi-publish@ec4db0b4ddc65acdf4bff5fa45ac92d78b56bdf0
        with:
          packages-dir: bridge/python/aliases/destack-py/dist

      - name: Publish VS Code extension
        run: just bridge/publish-vscode ""
        env:
          VSCE_PAT: ${{ secrets.VSCE_PAT }}

      - name: Publish zed registry PR
        if: vars.RELEASE_PUBLISH_ZED == 'true'
        run: just bridge/publish-zed "" "$(cat VERSION.txt)"
        env:
          GH_TOKEN: ${{ secrets.ZED_GITHUB_TOKEN }}
          DESTACK_ZED_REGISTRY_PUSH_TO: ${{ vars.ZED_REGISTRY_PUSH_TO }}
