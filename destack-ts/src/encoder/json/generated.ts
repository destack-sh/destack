import type { BuiltinObject, Graph, GraphConnection, Session, Encoder } from '@destack/language';
import { NODE_CLASS_BY_TYPE, STRUCT_CLASS_BY_TYPE } from '@destack/language/registry';
import { JSON_OBJECT_ENCODERS, _JsonObjectEncoder, getObjectKey } from '@destack/encoder/json/generate';
import { Temporal } from 'temporal-polyfill';
import { uuid4, uuid7, toNanoId } from '@destack/utils/uuid';
import { timedeltaToISOFormat, timedeltaFromISOFormat, base64Encode, base64Decode } from '@destack/utils';
import { RunStopRequestedEvent, FontType, CascadeAction, Trigger, Quaternion, EllipseShape2D, CopyEvent, DragLeaveEvent, TypeCardinality, Align, Client, FillSize, RoleEvent, EntitlementRevokedEvent, Ellipse2D, MembershipEvent, ReactionAddedEvent, GradientType, Entitlement, Resource, ModelDeveloper, ViewEvent, BuiltinDefinition, TraitType, NodeType, MembershipJoinedEvent, GridSpan2, JoinType, Polygon2D, StringFormat, Struct, ConstraintType, InviteRejectedEvent, EditType, ClickEvent, Vector4i, MembershipLeftEvent, ShadowStyle, TripleClickEvent, Transition, GraphKey, Scene, Shape2D, ArrowHeadType, LogLevel, RunResumeRequestedEvent, Fill, ToolType, MethodType, MouseButton, Universe, NodeDefinitionType, ShadowPosition, NumberConstraint, CutEvent, StrokePoint, FillType, UserStatus, StructDefinitionReference, NodeReference, RoleType, Node, Datum, FillStyle, RunPauseRequestedEvent, Expression, DropEvent, StarRemovedEvent, NotificationDismissedEvent, ObjectDefinitionType, PointerLeaveEvent, ActionDefinition, RunEvent, Layout, Tenancy, GaugeMetric, BorderStyle, Value, ArrowShape2D, ConstantDefinition, NotificationRescindedEvent, StarEvent, OrganizationStatus, NotificationEvent, SnapshotStatus, TextAlign, CustomProperty, FocusOutEvent, Event, PointerLongPressEvent, Migration, Machine, Variant, HistogramMeasurementEvent, IndexType, SplitView, Notification, StringConstraint, TimerPausedEvent, ColorType, Overflow, TextView, ColorStyle, Sort, PointerEvent, SceneEvent, HistogramMetric, EdgeType, GradientStop, Organization, Length, SanctionEvent, CounterMeasurementEvent, Star, RunFailedEvent, SignalEvent, Axis2, CollectionConstraint, Team, Vector2i, MigrationOperation, PointerEnterEvent, EdgeDirection, FolderType, Stroke, MigrationType, RoleAssignedEvent, FocusEvent, SortType, Anchor, Offset2, Metric, AggregationType, Entity3D, PasteEvent, NumberInputView, MeasurementEvent, ScalarType, StrokeType, PropertyType, NodeDefinition, InviteEvent, LayoutView, ContentView, FillPosition, TimerEvent, TextSpanType, CustomEnum, RuntimeLanguage, ObjectDefinitionReference, LengthType, Method, GradientStyle, ExpressionType, Entity2D, EntitlementEvent, PropertyReferenceType, ClientType, Shape3D, ScheduleFrequency, FocusInEvent, DatabaseType, Timer, InputView, RegionArea, FontWeight, Invite, Color, Palette, TimerStartedEvent, TimerCompletedEvent, EditEvent, MethodCardinality, LineShape2D, PlatformType, Vector3i, ResourceStatus, TimerType, MouseEvent, DoubleClickEvent, Vector3, Join, ValueFactory, PointerMoveEvent, CounterMetric, EffectType, SpringType, Rectangle2D, MigrationDefinition, DragStartEvent, GaugeMeasurementEvent, SortMode, PointerOverEvent, LabelView, DragEvent, FontStyle, ClipboardEvent, InviteAcceptedEvent, NumberFormat, GraphDomain, KeyDownEvent, Border, Permission, Reaction, EventStatus, TransitionType, EntitlementExpiredEvent, Effect, Encoding, Axis3, Shadow, Space, File, LogEvent, PointerUpEvent, FrameView, TransitionStyle, NotificationSentEvent, Theme, Layer, TextSpan, PathShape2D, Condition, Record, Direction, StarAddedEvent, FollowEvent, CustomStruct, Database, Follow, DragEnterEvent, TriggerType, Handle, Line2D, PointerDownEvent, Script, StructDefinitionType, KeyEvent, Folder, FontSize, DragEndEvent, SanctionType, ModelProvider, Snapshot, Text, ColorShade, Branch, UniverseCategory, Route, EntitlementGrantedEvent, Distribute, SingleClickEvent, ConditionalType, TextSplitType, EnumType, TagDefinition, EntitlementRequestedEvent, DayOfWeek, RunCompletedEvent, Tag, EnvironmentType, SnapshotType, EnumDefinition, IconType, WheelEvent, DragOverEvent, Run, IndexDefinition, Materialization, Corner2, NotificationStatus, Easing, BranchType, PermissionDefinition, ReactionEvent, InviteRescindedEvent, Aggregation, NotificationExpiredEvent, Select, Membership, RegionContinent, FunctionType, FollowAddedEvent, TraitDefinition, OffscreenBehavior, FollowRemovedEvent, TimerCancelledEvent, Service, Style, OperatingSystem, Grid2, TimerResumedEvent, PropertyDefinition, Region, User, RunStartedEvent, PolygonShape2D, BorderType, EntitlementType, ReactionRemovedEvent, Month, EffectStyle, SanctionRevokedEvent, Arrow2D, KeyUpEvent, Function, StructType, Gradient, Font, FileType, View, NodeDefinitionReference, NotificationReadEvent, StrokePath, RectangleShape2D, MigrationOperationDefinition, RunResumedEvent, SanctionRequestedEvent, Tagging, LayerType, StructDefinition, EditOperation, MethodDefinition, FileFormat, RunStatus, Role, SliderInputView, Cloud, InputEvent, ObjectKind, QueryType, Icon, ColorHue, Constraint, Vector4, ConstraintDefinition, OptionDefinition, SanctionExpiredEvent, DatumMutable, KeyPressEvent, InviteSentEvent, Entity, Inset2, Type, FileRetentionMode, CustomEvent, Query, RepeatType, ModeType, TextTransform, Stage, PrimitiveType, ColorIntent, Path2D, Action, Schedule, RoleUnassignedEvent, SanctionGrantedEvent, StrokeCap, Environment, Vector2, PropertyReference, CustomOption, Index, StrokeStyle, SpanEvent, TextDecoration, RunPausedEvent, TriggerEvent, MachineType, Sanction, ShadowType } from '@destack/language';
export const JSON_ENCODERS: { [key: string]: _JsonObjectEncoder } = {};
let loaded = false;
export function loadEncoders(): void {
    if (loaded) {
      return;
    }
    loaded = true;

    class TagJsonEncoder implements _JsonObjectEncoder {
      packObject(object: Tag): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "TAG";
        objectJson["id"] = object.id;
        if (object.parentPtr != null) {
          objectJson["parent"] = object.parentPtr.pack(1);
        }
        objectJson["space"] = object.spacePtr.pack(1);
        objectJson["materialization"] = Materialization[object.materialization];
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.instancePtr != null) {
          objectJson["instance"] = object.instancePtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["updatedAt"] = object.updatedAt.toString({ timeZoneName: 'never' });
        objectJson["updatedEpoch"] = Number(object.updatedEpoch);
        objectJson["updatedBy"] = object.updatedByPtr.pack(1);
        if (object.deletedAt != null) {
          objectJson["deletedAt"] = object.deletedAt.toString({ timeZoneName: 'never' });
        }
        if (object._ownedByPtr != null) {
          objectJson["ownedBy"] = object._ownedByPtr.pack(1);
        }
        objectJson["name"] = object._name;
        objectJson["orderKey"] = object.orderKey;
        if (Object.keys(object._customValues).length > 0) {
          const packedCustomValues: { [key: string]: any } = {} as any;
          for (const [key, value] of Object.entries(object._customValues)) {
            packedCustomValues[String(key)] = value.pack(1);
          }
          objectJson["customValues"] = packedCustomValues;
        }
        if (object._scriptPtr != null) {
          objectJson["script"] = object._scriptPtr.pack(1);
        }
        if (object.isExtensible != null) {
          objectJson["isExtensible"] = object.isExtensible;
        }
        if (object.sourcePtr != null) {
          objectJson["source"] = object.sourcePtr.pack(1);
        }
        if (object._key != null) {
          objectJson["key"] = object._key;
        }
        if (object._icon != null) {
          objectJson["icon"] = object._icon.pack(1);
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): Tag {
        const _Value = STRUCT_CLASS_BY_TYPE[100] as typeof Value;
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const _Icon = STRUCT_CLASS_BY_TYPE[400031] as typeof Icon;
        const iconValue = objectJson["icon"];
        const unpackedIcon = iconValue != undefined ? _Icon.unpack(1, iconValue, _session) as Icon : null;
        const parentPtrValue = objectJson["parent"];
        const unpackedParentPtr = parentPtrValue != undefined ? _NodeReference.unpack(1, parentPtrValue, _session) as NodeReference : null;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const instancePtrValue = objectJson["instance"];
        const unpackedInstancePtr = instancePtrValue != undefined ? _NodeReference.unpack(1, instancePtrValue, _session) as NodeReference : null;
        const deletedAtValue = objectJson["deletedAt"];
        const unpackedDeletedAt = deletedAtValue != undefined ? Temporal.Instant.from(deletedAtValue).toZonedDateTimeISO('UTC') : null;
        const ownedByPtrValue = objectJson["ownedBy"];
        const unpackedOwnedByPtr = ownedByPtrValue != undefined ? _NodeReference.unpack(1, ownedByPtrValue, _session) as NodeReference : null;
        const unpackedCustomValues = {} as any;
        if (objectJson["customValues"] != undefined) {
          for (const [key, value] of Object.entries(objectJson["customValues"])) {
            unpackedCustomValues[key] = _Value.unpack(1, value as any, _session) as Value;
          }
        }
        const scriptPtrValue = objectJson["script"];
        const unpackedScriptPtr = scriptPtrValue != undefined ? _NodeReference.unpack(1, scriptPtrValue, _session) as NodeReference : null;
        const isExtensibleValue = objectJson["isExtensible"];
        const unpackedIsExtensible = isExtensibleValue != undefined ? isExtensibleValue : null;
        const sourcePtrValue = objectJson["source"];
        const unpackedSourcePtr = sourcePtrValue != undefined ? _NodeReference.unpack(1, sourcePtrValue, _session) as NodeReference : null;
        const keyValue = objectJson["key"];
        const unpackedKey = keyValue != undefined ? keyValue : null;
        return new (NODE_CLASS_BY_TYPE[12000] as typeof Tag)({
          icon: unpackedIcon,
          parent: unpackedParentPtr,
          materialization: Materialization[objectJson["materialization"]] as any,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          instance: unpackedInstancePtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          updatedAt: Temporal.Instant.from(objectJson["updatedAt"]).toZonedDateTimeISO('UTC'),
          updatedEpoch: Number(objectJson["updatedEpoch"]),
          updatedBy: _NodeReference.unpack(1, objectJson["updatedBy"], _session) as NodeReference,
          deletedAt: unpackedDeletedAt,
          ownedBy: unpackedOwnedByPtr,
          name: objectJson["name"],
          orderKey: objectJson["orderKey"],
          customValues: unpackedCustomValues,
          script: unpackedScriptPtr,
          isExtensible: unpackedIsExtensible,
          source: unpackedSourcePtr,
          key: unpackedKey,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 12000)] = new TagJsonEncoder();

    class TaggingJsonEncoder implements _JsonObjectEncoder {
      packObject(object: Tagging): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "TAGGING";
        objectJson["id"] = object.id;
        if (object.parentPtr != null) {
          objectJson["parent"] = object.parentPtr.pack(1);
        }
        objectJson["space"] = object.spacePtr.pack(1);
        objectJson["materialization"] = Materialization[object.materialization];
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.instancePtr != null) {
          objectJson["instance"] = object.instancePtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["updatedAt"] = object.updatedAt.toString({ timeZoneName: 'never' });
        objectJson["updatedEpoch"] = Number(object.updatedEpoch);
        objectJson["updatedBy"] = object.updatedByPtr.pack(1);
        if (object.deletedAt != null) {
          objectJson["deletedAt"] = object.deletedAt.toString({ timeZoneName: 'never' });
        }
        if (object._ownedByPtr != null) {
          objectJson["ownedBy"] = object._ownedByPtr.pack(1);
        }
        objectJson["name"] = object._name;
        objectJson["orderKey"] = object.orderKey;
        if (Object.keys(object._customValues).length > 0) {
          const packedCustomValues: { [key: string]: any } = {} as any;
          for (const [key, value] of Object.entries(object._customValues)) {
            packedCustomValues[String(key)] = value.pack(1);
          }
          objectJson["customValues"] = packedCustomValues;
        }
        if (object._scriptPtr != null) {
          objectJson["script"] = object._scriptPtr.pack(1);
        }
        if (object.isExtensible != null) {
          objectJson["isExtensible"] = object.isExtensible;
        }
        if (object.sourcePtr != null) {
          objectJson["source"] = object.sourcePtr.pack(1);
        }
        if (object._key != null) {
          objectJson["key"] = object._key;
        }
        objectJson["tag"] = object._tagPtr.pack(1);
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): Tagging {
        const _Value = STRUCT_CLASS_BY_TYPE[100] as typeof Value;
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const parentPtrValue = objectJson["parent"];
        const unpackedParentPtr = parentPtrValue != undefined ? _NodeReference.unpack(1, parentPtrValue, _session) as NodeReference : null;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const instancePtrValue = objectJson["instance"];
        const unpackedInstancePtr = instancePtrValue != undefined ? _NodeReference.unpack(1, instancePtrValue, _session) as NodeReference : null;
        const deletedAtValue = objectJson["deletedAt"];
        const unpackedDeletedAt = deletedAtValue != undefined ? Temporal.Instant.from(deletedAtValue).toZonedDateTimeISO('UTC') : null;
        const ownedByPtrValue = objectJson["ownedBy"];
        const unpackedOwnedByPtr = ownedByPtrValue != undefined ? _NodeReference.unpack(1, ownedByPtrValue, _session) as NodeReference : null;
        const unpackedCustomValues = {} as any;
        if (objectJson["customValues"] != undefined) {
          for (const [key, value] of Object.entries(objectJson["customValues"])) {
            unpackedCustomValues[key] = _Value.unpack(1, value as any, _session) as Value;
          }
        }
        const scriptPtrValue = objectJson["script"];
        const unpackedScriptPtr = scriptPtrValue != undefined ? _NodeReference.unpack(1, scriptPtrValue, _session) as NodeReference : null;
        const isExtensibleValue = objectJson["isExtensible"];
        const unpackedIsExtensible = isExtensibleValue != undefined ? isExtensibleValue : null;
        const sourcePtrValue = objectJson["source"];
        const unpackedSourcePtr = sourcePtrValue != undefined ? _NodeReference.unpack(1, sourcePtrValue, _session) as NodeReference : null;
        const keyValue = objectJson["key"];
        const unpackedKey = keyValue != undefined ? keyValue : null;
        return new (NODE_CLASS_BY_TYPE[12100] as typeof Tagging)({
          tag: _NodeReference.unpack(1, objectJson["tag"], _session) as NodeReference,
          parent: unpackedParentPtr,
          materialization: Materialization[objectJson["materialization"]] as any,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          instance: unpackedInstancePtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          updatedAt: Temporal.Instant.from(objectJson["updatedAt"]).toZonedDateTimeISO('UTC'),
          updatedEpoch: Number(objectJson["updatedEpoch"]),
          updatedBy: _NodeReference.unpack(1, objectJson["updatedBy"], _session) as NodeReference,
          deletedAt: unpackedDeletedAt,
          ownedBy: unpackedOwnedByPtr,
          name: objectJson["name"],
          orderKey: objectJson["orderKey"],
          customValues: unpackedCustomValues,
          script: unpackedScriptPtr,
          isExtensible: unpackedIsExtensible,
          source: unpackedSourcePtr,
          key: unpackedKey,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 12100)] = new TaggingJsonEncoder();

    class CustomEventJsonEncoder implements _JsonObjectEncoder {
      packObject(object: CustomEvent): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "CUSTOM_EVENT";
        objectJson["id"] = object.id;
        if (object.parentPtr != null) {
          objectJson["parent"] = object.parentPtr.pack(1);
        }
        objectJson["space"] = object.spacePtr.pack(1);
        objectJson["materialization"] = Materialization[object.materialization];
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.instancePtr != null) {
          objectJson["instance"] = object.instancePtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["updatedAt"] = object.updatedAt.toString({ timeZoneName: 'never' });
        objectJson["updatedEpoch"] = Number(object.updatedEpoch);
        objectJson["updatedBy"] = object.updatedByPtr.pack(1);
        if (object.deletedAt != null) {
          objectJson["deletedAt"] = object.deletedAt.toString({ timeZoneName: 'never' });
        }
        if (object._ownedByPtr != null) {
          objectJson["ownedBy"] = object._ownedByPtr.pack(1);
        }
        objectJson["name"] = object._name;
        objectJson["orderKey"] = object.orderKey;
        if (Object.keys(object._customValues).length > 0) {
          const packedCustomValues: { [key: string]: any } = {} as any;
          for (const [key, value] of Object.entries(object._customValues)) {
            packedCustomValues[String(key)] = value.pack(1);
          }
          objectJson["customValues"] = packedCustomValues;
        }
        if (object._scriptPtr != null) {
          objectJson["script"] = object._scriptPtr.pack(1);
        }
        if (object.isExtensible != null) {
          objectJson["isExtensible"] = object.isExtensible;
        }
        if (object.sourcePtr != null) {
          objectJson["source"] = object.sourcePtr.pack(1);
        }
        if (object._key != null) {
          objectJson["key"] = object._key;
        }
        if (object._icon != null) {
          objectJson["icon"] = object._icon.pack(1);
        }
        if (object._baseType != null) {
          objectJson["baseType"] = object._baseType.pack(1);
        }
        if (object._selfTraits.length > 0) {
          const packedSelfTraits: any[] = [];
          for (const item of object._selfTraits) {
            packedSelfTraits.push(item.pack(1));
          }
          objectJson["selfTraits"] = packedSelfTraits;
        }
        objectJson["isAbstract"] = object._isAbstract;
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): CustomEvent {
        const _NodeDefinitionReference = STRUCT_CLASS_BY_TYPE[13] as typeof NodeDefinitionReference;
        const _Value = STRUCT_CLASS_BY_TYPE[100] as typeof Value;
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const _Icon = STRUCT_CLASS_BY_TYPE[400031] as typeof Icon;
        const iconValue = objectJson["icon"];
        const unpackedIcon = iconValue != undefined ? _Icon.unpack(1, iconValue, _session) as Icon : null;
        const baseTypeValue = objectJson["baseType"];
        const unpackedBaseType = baseTypeValue != undefined ? _NodeDefinitionReference.unpack(1, baseTypeValue, _session) as NodeDefinitionReference : null;
        const unpackedSelfTraits: any[] = [];
        if (objectJson["selfTraits"] != undefined) {
          for (const item of objectJson["selfTraits"]) {
            unpackedSelfTraits.push(_NodeDefinitionReference.unpack(1, item, _session) as NodeDefinitionReference)
          }
        }
        const parentPtrValue = objectJson["parent"];
        const unpackedParentPtr = parentPtrValue != undefined ? _NodeReference.unpack(1, parentPtrValue, _session) as NodeReference : null;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const instancePtrValue = objectJson["instance"];
        const unpackedInstancePtr = instancePtrValue != undefined ? _NodeReference.unpack(1, instancePtrValue, _session) as NodeReference : null;
        const deletedAtValue = objectJson["deletedAt"];
        const unpackedDeletedAt = deletedAtValue != undefined ? Temporal.Instant.from(deletedAtValue).toZonedDateTimeISO('UTC') : null;
        const ownedByPtrValue = objectJson["ownedBy"];
        const unpackedOwnedByPtr = ownedByPtrValue != undefined ? _NodeReference.unpack(1, ownedByPtrValue, _session) as NodeReference : null;
        const unpackedCustomValues = {} as any;
        if (objectJson["customValues"] != undefined) {
          for (const [key, value] of Object.entries(objectJson["customValues"])) {
            unpackedCustomValues[key] = _Value.unpack(1, value as any, _session) as Value;
          }
        }
        const scriptPtrValue = objectJson["script"];
        const unpackedScriptPtr = scriptPtrValue != undefined ? _NodeReference.unpack(1, scriptPtrValue, _session) as NodeReference : null;
        const isExtensibleValue = objectJson["isExtensible"];
        const unpackedIsExtensible = isExtensibleValue != undefined ? isExtensibleValue : null;
        const sourcePtrValue = objectJson["source"];
        const unpackedSourcePtr = sourcePtrValue != undefined ? _NodeReference.unpack(1, sourcePtrValue, _session) as NodeReference : null;
        const keyValue = objectJson["key"];
        const unpackedKey = keyValue != undefined ? keyValue : null;
        return new (NODE_CLASS_BY_TYPE[20000] as typeof CustomEvent)({
          icon: unpackedIcon,
          baseType: unpackedBaseType,
          selfTraits: unpackedSelfTraits,
          isAbstract: objectJson["isAbstract"],
          parent: unpackedParentPtr,
          materialization: Materialization[objectJson["materialization"]] as any,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          instance: unpackedInstancePtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          updatedAt: Temporal.Instant.from(objectJson["updatedAt"]).toZonedDateTimeISO('UTC'),
          updatedEpoch: Number(objectJson["updatedEpoch"]),
          updatedBy: _NodeReference.unpack(1, objectJson["updatedBy"], _session) as NodeReference,
          deletedAt: unpackedDeletedAt,
          ownedBy: unpackedOwnedByPtr,
          name: objectJson["name"],
          orderKey: objectJson["orderKey"],
          customValues: unpackedCustomValues,
          script: unpackedScriptPtr,
          isExtensible: unpackedIsExtensible,
          source: unpackedSourcePtr,
          key: unpackedKey,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 20000)] = new CustomEventJsonEncoder();

    class EditEventJsonEncoder implements _JsonObjectEncoder {
      packObject(object: EditEvent): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "EDIT_EVENT";
        objectJson["id"] = object.id;
        objectJson["space"] = object.spacePtr.pack(1);
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.causedByPtr != null) {
          objectJson["causedBy"] = object.causedByPtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["client"] = object.clientPtr.pack(1);
        objectJson["clientNonce"] = object.clientNonce;
        objectJson["clientCreatedAt"] = object.clientCreatedAt.toString({ timeZoneName: 'never' });
        objectJson["clientEpoch"] = Number(object.clientEpoch);
        objectJson["status"] = EventStatus[object.status];
        objectJson["type"] = EditType[object.type];
        objectJson["node"] = object.nodePtr.pack(1);
        if (object.operation != null) {
          objectJson["operation"] = EditOperation[object.operation];
        }
        if (object.propertyId != null) {
          objectJson["propertyId"] = Number(object.propertyId);
        }
        if (object.customPropertyPtr != null) {
          objectJson["customProperty"] = object.customPropertyPtr.pack(1);
        }
        if (object.key != null) {
          objectJson["key"] = object.key.pack(1);
        }
        if (object.value != null) {
          objectJson["value"] = object.value.pack(1);
        }
        if (object.reverseOperation != null) {
          objectJson["reverseOperation"] = EditOperation[object.reverseOperation];
        }
        if (object.reverseValue != null) {
          objectJson["reverseValue"] = object.reverseValue.pack(1);
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): EditEvent {
        const _Value = STRUCT_CLASS_BY_TYPE[100] as typeof Value;
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const operationValue = objectJson["operation"];
        const unpackedOperation = operationValue != undefined ? EditOperation[operationValue] as any : null;
        const propertyIdValue = objectJson["propertyId"];
        const unpackedPropertyId = propertyIdValue != undefined ? Number(propertyIdValue) : null;
        const customPropertyPtrValue = objectJson["customProperty"];
        const unpackedCustomPropertyPtr = customPropertyPtrValue != undefined ? _NodeReference.unpack(1, customPropertyPtrValue, _session) as NodeReference : null;
        const keyValue = objectJson["key"];
        const unpackedKey = keyValue != undefined ? _Value.unpack(1, keyValue, _session) as Value : null;
        const valueValue = objectJson["value"];
        const unpackedValue = valueValue != undefined ? _Value.unpack(1, valueValue, _session) as Value : null;
        const reverseOperationValue = objectJson["reverseOperation"];
        const unpackedReverseOperation = reverseOperationValue != undefined ? EditOperation[reverseOperationValue] as any : null;
        const reverseValueValue = objectJson["reverseValue"];
        const unpackedReverseValue = reverseValueValue != undefined ? _Value.unpack(1, reverseValueValue, _session) as Value : null;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const causedByPtrValue = objectJson["causedBy"];
        const unpackedCausedByPtr = causedByPtrValue != undefined ? _NodeReference.unpack(1, causedByPtrValue, _session) as NodeReference : null;
        return new (NODE_CLASS_BY_TYPE[90100] as typeof EditEvent)({
          type: EditType[objectJson["type"]] as any,
          node: _NodeReference.unpack(1, objectJson["node"], _session) as NodeReference,
          operation: unpackedOperation,
          propertyId: unpackedPropertyId,
          customProperty: unpackedCustomPropertyPtr,
          key: unpackedKey,
          value: unpackedValue,
          reverseOperation: unpackedReverseOperation,
          reverseValue: unpackedReverseValue,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          causedBy: unpackedCausedByPtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          client: _NodeReference.unpack(1, objectJson["client"], _session) as NodeReference,
          clientNonce: objectJson["clientNonce"],
          clientCreatedAt: Temporal.Instant.from(objectJson["clientCreatedAt"]).toZonedDateTimeISO('UTC'),
          clientEpoch: Number(objectJson["clientEpoch"]),
          status: EventStatus[objectJson["status"]] as any,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 90100)] = new EditEventJsonEncoder();

    class PermissionJsonEncoder implements _JsonObjectEncoder {
      packObject(object: Permission): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "PERMISSION";
        objectJson["id"] = object.id;
        if (object.parentPtr != null) {
          objectJson["parent"] = object.parentPtr.pack(1);
        }
        objectJson["space"] = object.spacePtr.pack(1);
        objectJson["materialization"] = Materialization[object.materialization];
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.instancePtr != null) {
          objectJson["instance"] = object.instancePtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["updatedAt"] = object.updatedAt.toString({ timeZoneName: 'never' });
        objectJson["updatedEpoch"] = Number(object.updatedEpoch);
        objectJson["updatedBy"] = object.updatedByPtr.pack(1);
        if (object.deletedAt != null) {
          objectJson["deletedAt"] = object.deletedAt.toString({ timeZoneName: 'never' });
        }
        if (object._ownedByPtr != null) {
          objectJson["ownedBy"] = object._ownedByPtr.pack(1);
        }
        objectJson["name"] = object._name;
        objectJson["orderKey"] = object.orderKey;
        if (Object.keys(object._customValues).length > 0) {
          const packedCustomValues: { [key: string]: any } = {} as any;
          for (const [key, value] of Object.entries(object._customValues)) {
            packedCustomValues[String(key)] = value.pack(1);
          }
          objectJson["customValues"] = packedCustomValues;
        }
        if (object._scriptPtr != null) {
          objectJson["script"] = object._scriptPtr.pack(1);
        }
        if (object.isExtensible != null) {
          objectJson["isExtensible"] = object.isExtensible;
        }
        if (object.sourcePtr != null) {
          objectJson["source"] = object.sourcePtr.pack(1);
        }
        if (object._key != null) {
          objectJson["key"] = object._key;
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): Permission {
        const _Value = STRUCT_CLASS_BY_TYPE[100] as typeof Value;
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const parentPtrValue = objectJson["parent"];
        const unpackedParentPtr = parentPtrValue != undefined ? _NodeReference.unpack(1, parentPtrValue, _session) as NodeReference : null;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const instancePtrValue = objectJson["instance"];
        const unpackedInstancePtr = instancePtrValue != undefined ? _NodeReference.unpack(1, instancePtrValue, _session) as NodeReference : null;
        const deletedAtValue = objectJson["deletedAt"];
        const unpackedDeletedAt = deletedAtValue != undefined ? Temporal.Instant.from(deletedAtValue).toZonedDateTimeISO('UTC') : null;
        const ownedByPtrValue = objectJson["ownedBy"];
        const unpackedOwnedByPtr = ownedByPtrValue != undefined ? _NodeReference.unpack(1, ownedByPtrValue, _session) as NodeReference : null;
        const unpackedCustomValues = {} as any;
        if (objectJson["customValues"] != undefined) {
          for (const [key, value] of Object.entries(objectJson["customValues"])) {
            unpackedCustomValues[key] = _Value.unpack(1, value as any, _session) as Value;
          }
        }
        const scriptPtrValue = objectJson["script"];
        const unpackedScriptPtr = scriptPtrValue != undefined ? _NodeReference.unpack(1, scriptPtrValue, _session) as NodeReference : null;
        const isExtensibleValue = objectJson["isExtensible"];
        const unpackedIsExtensible = isExtensibleValue != undefined ? isExtensibleValue : null;
        const sourcePtrValue = objectJson["source"];
        const unpackedSourcePtr = sourcePtrValue != undefined ? _NodeReference.unpack(1, sourcePtrValue, _session) as NodeReference : null;
        const keyValue = objectJson["key"];
        const unpackedKey = keyValue != undefined ? keyValue : null;
        return new (NODE_CLASS_BY_TYPE[50000] as typeof Permission)({
          parent: unpackedParentPtr,
          materialization: Materialization[objectJson["materialization"]] as any,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          instance: unpackedInstancePtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          updatedAt: Temporal.Instant.from(objectJson["updatedAt"]).toZonedDateTimeISO('UTC'),
          updatedEpoch: Number(objectJson["updatedEpoch"]),
          updatedBy: _NodeReference.unpack(1, objectJson["updatedBy"], _session) as NodeReference,
          deletedAt: unpackedDeletedAt,
          ownedBy: unpackedOwnedByPtr,
          name: objectJson["name"],
          orderKey: objectJson["orderKey"],
          customValues: unpackedCustomValues,
          script: unpackedScriptPtr,
          isExtensible: unpackedIsExtensible,
          source: unpackedSourcePtr,
          key: unpackedKey,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 50000)] = new PermissionJsonEncoder();

    class MethodJsonEncoder implements _JsonObjectEncoder {
      packObject(object: Method): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "METHOD";
        objectJson["id"] = object.id;
        if (object.parentPtr != null) {
          objectJson["parent"] = object.parentPtr.pack(1);
        }
        objectJson["space"] = object.spacePtr.pack(1);
        objectJson["materialization"] = Materialization[object.materialization];
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.instancePtr != null) {
          objectJson["instance"] = object.instancePtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["updatedAt"] = object.updatedAt.toString({ timeZoneName: 'never' });
        objectJson["updatedEpoch"] = Number(object.updatedEpoch);
        objectJson["updatedBy"] = object.updatedByPtr.pack(1);
        if (object.deletedAt != null) {
          objectJson["deletedAt"] = object.deletedAt.toString({ timeZoneName: 'never' });
        }
        if (object._ownedByPtr != null) {
          objectJson["ownedBy"] = object._ownedByPtr.pack(1);
        }
        objectJson["name"] = object._name;
        objectJson["orderKey"] = object.orderKey;
        if (Object.keys(object._customValues).length > 0) {
          const packedCustomValues: { [key: string]: any } = {} as any;
          for (const [key, value] of Object.entries(object._customValues)) {
            packedCustomValues[String(key)] = value.pack(1);
          }
          objectJson["customValues"] = packedCustomValues;
        }
        if (object._scriptPtr != null) {
          objectJson["script"] = object._scriptPtr.pack(1);
        }
        if (object.isExtensible != null) {
          objectJson["isExtensible"] = object.isExtensible;
        }
        if (object.sourcePtr != null) {
          objectJson["source"] = object.sourcePtr.pack(1);
        }
        if (object._key != null) {
          objectJson["key"] = object._key;
        }
        objectJson["type"] = MethodType[object._type];
        if (object._text != null) {
          objectJson["text"] = object._text.pack(1);
        }
        objectJson["cardinality"] = MethodCardinality[object._cardinality];
        if (object._platforms.length > 0) {
          const packedPlatforms: any[] = [];
          for (const item of object._platforms) {
            packedPlatforms.push(PlatformType[item]);
          }
          objectJson["platforms"] = packedPlatforms;
        }
        if (object._languages.length > 0) {
          const packedLanguages: any[] = [];
          for (const item of object._languages) {
            packedLanguages.push(RuntimeLanguage[item]);
          }
          objectJson["languages"] = packedLanguages;
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): Method {
        const _Value = STRUCT_CLASS_BY_TYPE[100] as typeof Value;
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const _Text = STRUCT_CLASS_BY_TYPE[400020] as typeof Text;
        const textValue = objectJson["text"];
        const unpackedText = textValue != undefined ? _Text.unpack(1, textValue, _session) as Text : null;
        const unpackedPlatforms: any[] = [];
        if (objectJson["platforms"] != undefined) {
          for (const item of objectJson["platforms"]) {
            unpackedPlatforms.push(PlatformType[item] as any)
          }
        }
        const unpackedLanguages: any[] = [];
        if (objectJson["languages"] != undefined) {
          for (const item of objectJson["languages"]) {
            unpackedLanguages.push(RuntimeLanguage[item] as any)
          }
        }
        const parentPtrValue = objectJson["parent"];
        const unpackedParentPtr = parentPtrValue != undefined ? _NodeReference.unpack(1, parentPtrValue, _session) as NodeReference : null;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const instancePtrValue = objectJson["instance"];
        const unpackedInstancePtr = instancePtrValue != undefined ? _NodeReference.unpack(1, instancePtrValue, _session) as NodeReference : null;
        const deletedAtValue = objectJson["deletedAt"];
        const unpackedDeletedAt = deletedAtValue != undefined ? Temporal.Instant.from(deletedAtValue).toZonedDateTimeISO('UTC') : null;
        const ownedByPtrValue = objectJson["ownedBy"];
        const unpackedOwnedByPtr = ownedByPtrValue != undefined ? _NodeReference.unpack(1, ownedByPtrValue, _session) as NodeReference : null;
        const unpackedCustomValues = {} as any;
        if (objectJson["customValues"] != undefined) {
          for (const [key, value] of Object.entries(objectJson["customValues"])) {
            unpackedCustomValues[key] = _Value.unpack(1, value as any, _session) as Value;
          }
        }
        const scriptPtrValue = objectJson["script"];
        const unpackedScriptPtr = scriptPtrValue != undefined ? _NodeReference.unpack(1, scriptPtrValue, _session) as NodeReference : null;
        const isExtensibleValue = objectJson["isExtensible"];
        const unpackedIsExtensible = isExtensibleValue != undefined ? isExtensibleValue : null;
        const sourcePtrValue = objectJson["source"];
        const unpackedSourcePtr = sourcePtrValue != undefined ? _NodeReference.unpack(1, sourcePtrValue, _session) as NodeReference : null;
        const keyValue = objectJson["key"];
        const unpackedKey = keyValue != undefined ? keyValue : null;
        return new (NODE_CLASS_BY_TYPE[40000] as typeof Method)({
          type: MethodType[objectJson["type"]] as any,
          text: unpackedText,
          cardinality: MethodCardinality[objectJson["cardinality"]] as any,
          platforms: unpackedPlatforms,
          languages: unpackedLanguages,
          parent: unpackedParentPtr,
          materialization: Materialization[objectJson["materialization"]] as any,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          instance: unpackedInstancePtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          updatedAt: Temporal.Instant.from(objectJson["updatedAt"]).toZonedDateTimeISO('UTC'),
          updatedEpoch: Number(objectJson["updatedEpoch"]),
          updatedBy: _NodeReference.unpack(1, objectJson["updatedBy"], _session) as NodeReference,
          deletedAt: unpackedDeletedAt,
          ownedBy: unpackedOwnedByPtr,
          name: objectJson["name"],
          orderKey: objectJson["orderKey"],
          customValues: unpackedCustomValues,
          script: unpackedScriptPtr,
          isExtensible: unpackedIsExtensible,
          source: unpackedSourcePtr,
          key: unpackedKey,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 40000)] = new MethodJsonEncoder();

    class ActionJsonEncoder implements _JsonObjectEncoder {
      packObject(object: Action): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "ACTION";
        objectJson["id"] = object.id;
        if (object.parentPtr != null) {
          objectJson["parent"] = object.parentPtr.pack(1);
        }
        objectJson["space"] = object.spacePtr.pack(1);
        objectJson["materialization"] = Materialization[object.materialization];
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.instancePtr != null) {
          objectJson["instance"] = object.instancePtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["updatedAt"] = object.updatedAt.toString({ timeZoneName: 'never' });
        objectJson["updatedEpoch"] = Number(object.updatedEpoch);
        objectJson["updatedBy"] = object.updatedByPtr.pack(1);
        if (object.deletedAt != null) {
          objectJson["deletedAt"] = object.deletedAt.toString({ timeZoneName: 'never' });
        }
        if (object._ownedByPtr != null) {
          objectJson["ownedBy"] = object._ownedByPtr.pack(1);
        }
        objectJson["name"] = object._name;
        objectJson["orderKey"] = object.orderKey;
        if (Object.keys(object._customValues).length > 0) {
          const packedCustomValues: { [key: string]: any } = {} as any;
          for (const [key, value] of Object.entries(object._customValues)) {
            packedCustomValues[String(key)] = value.pack(1);
          }
          objectJson["customValues"] = packedCustomValues;
        }
        if (object._scriptPtr != null) {
          objectJson["script"] = object._scriptPtr.pack(1);
        }
        if (object.isExtensible != null) {
          objectJson["isExtensible"] = object.isExtensible;
        }
        if (object.sourcePtr != null) {
          objectJson["source"] = object.sourcePtr.pack(1);
        }
        if (object._key != null) {
          objectJson["key"] = object._key;
        }
        objectJson["type"] = MethodType[object._type];
        if (object._text != null) {
          objectJson["text"] = object._text.pack(1);
        }
        objectJson["cardinality"] = MethodCardinality[object._cardinality];
        if (object._platforms.length > 0) {
          const packedPlatforms: any[] = [];
          for (const item of object._platforms) {
            packedPlatforms.push(PlatformType[item]);
          }
          objectJson["platforms"] = packedPlatforms;
        }
        if (object._languages.length > 0) {
          const packedLanguages: any[] = [];
          for (const item of object._languages) {
            packedLanguages.push(RuntimeLanguage[item]);
          }
          objectJson["languages"] = packedLanguages;
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): Action {
        const _Value = STRUCT_CLASS_BY_TYPE[100] as typeof Value;
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const _Text = STRUCT_CLASS_BY_TYPE[400020] as typeof Text;
        const textValue = objectJson["text"];
        const unpackedText = textValue != undefined ? _Text.unpack(1, textValue, _session) as Text : null;
        const unpackedPlatforms: any[] = [];
        if (objectJson["platforms"] != undefined) {
          for (const item of objectJson["platforms"]) {
            unpackedPlatforms.push(PlatformType[item] as any)
          }
        }
        const unpackedLanguages: any[] = [];
        if (objectJson["languages"] != undefined) {
          for (const item of objectJson["languages"]) {
            unpackedLanguages.push(RuntimeLanguage[item] as any)
          }
        }
        const parentPtrValue = objectJson["parent"];
        const unpackedParentPtr = parentPtrValue != undefined ? _NodeReference.unpack(1, parentPtrValue, _session) as NodeReference : null;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const instancePtrValue = objectJson["instance"];
        const unpackedInstancePtr = instancePtrValue != undefined ? _NodeReference.unpack(1, instancePtrValue, _session) as NodeReference : null;
        const deletedAtValue = objectJson["deletedAt"];
        const unpackedDeletedAt = deletedAtValue != undefined ? Temporal.Instant.from(deletedAtValue).toZonedDateTimeISO('UTC') : null;
        const ownedByPtrValue = objectJson["ownedBy"];
        const unpackedOwnedByPtr = ownedByPtrValue != undefined ? _NodeReference.unpack(1, ownedByPtrValue, _session) as NodeReference : null;
        const unpackedCustomValues = {} as any;
        if (objectJson["customValues"] != undefined) {
          for (const [key, value] of Object.entries(objectJson["customValues"])) {
            unpackedCustomValues[key] = _Value.unpack(1, value as any, _session) as Value;
          }
        }
        const scriptPtrValue = objectJson["script"];
        const unpackedScriptPtr = scriptPtrValue != undefined ? _NodeReference.unpack(1, scriptPtrValue, _session) as NodeReference : null;
        const isExtensibleValue = objectJson["isExtensible"];
        const unpackedIsExtensible = isExtensibleValue != undefined ? isExtensibleValue : null;
        const sourcePtrValue = objectJson["source"];
        const unpackedSourcePtr = sourcePtrValue != undefined ? _NodeReference.unpack(1, sourcePtrValue, _session) as NodeReference : null;
        const keyValue = objectJson["key"];
        const unpackedKey = keyValue != undefined ? keyValue : null;
        return new (NODE_CLASS_BY_TYPE[40100] as typeof Action)({
          type: MethodType[objectJson["type"]] as any,
          text: unpackedText,
          cardinality: MethodCardinality[objectJson["cardinality"]] as any,
          platforms: unpackedPlatforms,
          languages: unpackedLanguages,
          parent: unpackedParentPtr,
          materialization: Materialization[objectJson["materialization"]] as any,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          instance: unpackedInstancePtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          updatedAt: Temporal.Instant.from(objectJson["updatedAt"]).toZonedDateTimeISO('UTC'),
          updatedEpoch: Number(objectJson["updatedEpoch"]),
          updatedBy: _NodeReference.unpack(1, objectJson["updatedBy"], _session) as NodeReference,
          deletedAt: unpackedDeletedAt,
          ownedBy: unpackedOwnedByPtr,
          name: objectJson["name"],
          orderKey: objectJson["orderKey"],
          customValues: unpackedCustomValues,
          script: unpackedScriptPtr,
          isExtensible: unpackedIsExtensible,
          source: unpackedSourcePtr,
          key: unpackedKey,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 40100)] = new ActionJsonEncoder();

    class CustomEnumJsonEncoder implements _JsonObjectEncoder {
      packObject(object: CustomEnum): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "CUSTOM_ENUM";
        objectJson["id"] = object.id;
        if (object.parentPtr != null) {
          objectJson["parent"] = object.parentPtr.pack(1);
        }
        objectJson["space"] = object.spacePtr.pack(1);
        objectJson["materialization"] = Materialization[object.materialization];
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.instancePtr != null) {
          objectJson["instance"] = object.instancePtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["updatedAt"] = object.updatedAt.toString({ timeZoneName: 'never' });
        objectJson["updatedEpoch"] = Number(object.updatedEpoch);
        objectJson["updatedBy"] = object.updatedByPtr.pack(1);
        if (object.deletedAt != null) {
          objectJson["deletedAt"] = object.deletedAt.toString({ timeZoneName: 'never' });
        }
        if (object._ownedByPtr != null) {
          objectJson["ownedBy"] = object._ownedByPtr.pack(1);
        }
        objectJson["name"] = object._name;
        objectJson["orderKey"] = object.orderKey;
        if (Object.keys(object._customValues).length > 0) {
          const packedCustomValues: { [key: string]: any } = {} as any;
          for (const [key, value] of Object.entries(object._customValues)) {
            packedCustomValues[String(key)] = value.pack(1);
          }
          objectJson["customValues"] = packedCustomValues;
        }
        if (object._scriptPtr != null) {
          objectJson["script"] = object._scriptPtr.pack(1);
        }
        if (object.isExtensible != null) {
          objectJson["isExtensible"] = object.isExtensible;
        }
        if (object.sourcePtr != null) {
          objectJson["source"] = object.sourcePtr.pack(1);
        }
        if (object._key != null) {
          objectJson["key"] = object._key;
        }
        if (object._icon != null) {
          objectJson["icon"] = object._icon.pack(1);
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): CustomEnum {
        const _Value = STRUCT_CLASS_BY_TYPE[100] as typeof Value;
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const _Icon = STRUCT_CLASS_BY_TYPE[400031] as typeof Icon;
        const iconValue = objectJson["icon"];
        const unpackedIcon = iconValue != undefined ? _Icon.unpack(1, iconValue, _session) as Icon : null;
        const parentPtrValue = objectJson["parent"];
        const unpackedParentPtr = parentPtrValue != undefined ? _NodeReference.unpack(1, parentPtrValue, _session) as NodeReference : null;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const instancePtrValue = objectJson["instance"];
        const unpackedInstancePtr = instancePtrValue != undefined ? _NodeReference.unpack(1, instancePtrValue, _session) as NodeReference : null;
        const deletedAtValue = objectJson["deletedAt"];
        const unpackedDeletedAt = deletedAtValue != undefined ? Temporal.Instant.from(deletedAtValue).toZonedDateTimeISO('UTC') : null;
        const ownedByPtrValue = objectJson["ownedBy"];
        const unpackedOwnedByPtr = ownedByPtrValue != undefined ? _NodeReference.unpack(1, ownedByPtrValue, _session) as NodeReference : null;
        const unpackedCustomValues = {} as any;
        if (objectJson["customValues"] != undefined) {
          for (const [key, value] of Object.entries(objectJson["customValues"])) {
            unpackedCustomValues[key] = _Value.unpack(1, value as any, _session) as Value;
          }
        }
        const scriptPtrValue = objectJson["script"];
        const unpackedScriptPtr = scriptPtrValue != undefined ? _NodeReference.unpack(1, scriptPtrValue, _session) as NodeReference : null;
        const isExtensibleValue = objectJson["isExtensible"];
        const unpackedIsExtensible = isExtensibleValue != undefined ? isExtensibleValue : null;
        const sourcePtrValue = objectJson["source"];
        const unpackedSourcePtr = sourcePtrValue != undefined ? _NodeReference.unpack(1, sourcePtrValue, _session) as NodeReference : null;
        const keyValue = objectJson["key"];
        const unpackedKey = keyValue != undefined ? keyValue : null;
        return new (NODE_CLASS_BY_TYPE[20200] as typeof CustomEnum)({
          icon: unpackedIcon,
          parent: unpackedParentPtr,
          materialization: Materialization[objectJson["materialization"]] as any,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          instance: unpackedInstancePtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          updatedAt: Temporal.Instant.from(objectJson["updatedAt"]).toZonedDateTimeISO('UTC'),
          updatedEpoch: Number(objectJson["updatedEpoch"]),
          updatedBy: _NodeReference.unpack(1, objectJson["updatedBy"], _session) as NodeReference,
          deletedAt: unpackedDeletedAt,
          ownedBy: unpackedOwnedByPtr,
          name: objectJson["name"],
          orderKey: objectJson["orderKey"],
          customValues: unpackedCustomValues,
          script: unpackedScriptPtr,
          isExtensible: unpackedIsExtensible,
          source: unpackedSourcePtr,
          key: unpackedKey,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 20200)] = new CustomEnumJsonEncoder();

    class CustomOptionJsonEncoder implements _JsonObjectEncoder {
      packObject(object: CustomOption): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "CUSTOM_OPTION";
        objectJson["id"] = object.id;
        if (object.parentPtr != null) {
          objectJson["parent"] = object.parentPtr.pack(1);
        }
        objectJson["space"] = object.spacePtr.pack(1);
        objectJson["materialization"] = Materialization[object.materialization];
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.instancePtr != null) {
          objectJson["instance"] = object.instancePtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["updatedAt"] = object.updatedAt.toString({ timeZoneName: 'never' });
        objectJson["updatedEpoch"] = Number(object.updatedEpoch);
        objectJson["updatedBy"] = object.updatedByPtr.pack(1);
        if (object.deletedAt != null) {
          objectJson["deletedAt"] = object.deletedAt.toString({ timeZoneName: 'never' });
        }
        if (object._ownedByPtr != null) {
          objectJson["ownedBy"] = object._ownedByPtr.pack(1);
        }
        objectJson["name"] = object._name;
        objectJson["orderKey"] = object.orderKey;
        if (Object.keys(object._customValues).length > 0) {
          const packedCustomValues: { [key: string]: any } = {} as any;
          for (const [key, value] of Object.entries(object._customValues)) {
            packedCustomValues[String(key)] = value.pack(1);
          }
          objectJson["customValues"] = packedCustomValues;
        }
        if (object._scriptPtr != null) {
          objectJson["script"] = object._scriptPtr.pack(1);
        }
        if (object.isExtensible != null) {
          objectJson["isExtensible"] = object.isExtensible;
        }
        if (object.sourcePtr != null) {
          objectJson["source"] = object.sourcePtr.pack(1);
        }
        if (object._key != null) {
          objectJson["key"] = object._key;
        }
        if (object._icon != null) {
          objectJson["icon"] = object._icon.pack(1);
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): CustomOption {
        const _Value = STRUCT_CLASS_BY_TYPE[100] as typeof Value;
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const _Icon = STRUCT_CLASS_BY_TYPE[400031] as typeof Icon;
        const parentPtrValue = objectJson["parent"];
        const unpackedParentPtr = parentPtrValue != undefined ? _NodeReference.unpack(1, parentPtrValue, _session) as NodeReference : null;
        const iconValue = objectJson["icon"];
        const unpackedIcon = iconValue != undefined ? _Icon.unpack(1, iconValue, _session) as Icon : null;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const instancePtrValue = objectJson["instance"];
        const unpackedInstancePtr = instancePtrValue != undefined ? _NodeReference.unpack(1, instancePtrValue, _session) as NodeReference : null;
        const deletedAtValue = objectJson["deletedAt"];
        const unpackedDeletedAt = deletedAtValue != undefined ? Temporal.Instant.from(deletedAtValue).toZonedDateTimeISO('UTC') : null;
        const ownedByPtrValue = objectJson["ownedBy"];
        const unpackedOwnedByPtr = ownedByPtrValue != undefined ? _NodeReference.unpack(1, ownedByPtrValue, _session) as NodeReference : null;
        const unpackedCustomValues = {} as any;
        if (objectJson["customValues"] != undefined) {
          for (const [key, value] of Object.entries(objectJson["customValues"])) {
            unpackedCustomValues[key] = _Value.unpack(1, value as any, _session) as Value;
          }
        }
        const scriptPtrValue = objectJson["script"];
        const unpackedScriptPtr = scriptPtrValue != undefined ? _NodeReference.unpack(1, scriptPtrValue, _session) as NodeReference : null;
        const isExtensibleValue = objectJson["isExtensible"];
        const unpackedIsExtensible = isExtensibleValue != undefined ? isExtensibleValue : null;
        const sourcePtrValue = objectJson["source"];
        const unpackedSourcePtr = sourcePtrValue != undefined ? _NodeReference.unpack(1, sourcePtrValue, _session) as NodeReference : null;
        const keyValue = objectJson["key"];
        const unpackedKey = keyValue != undefined ? keyValue : null;
        return new (NODE_CLASS_BY_TYPE[20400] as typeof CustomOption)({
          parent: unpackedParentPtr,
          icon: unpackedIcon,
          materialization: Materialization[objectJson["materialization"]] as any,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          instance: unpackedInstancePtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          updatedAt: Temporal.Instant.from(objectJson["updatedAt"]).toZonedDateTimeISO('UTC'),
          updatedEpoch: Number(objectJson["updatedEpoch"]),
          updatedBy: _NodeReference.unpack(1, objectJson["updatedBy"], _session) as NodeReference,
          deletedAt: unpackedDeletedAt,
          ownedBy: unpackedOwnedByPtr,
          name: objectJson["name"],
          orderKey: objectJson["orderKey"],
          customValues: unpackedCustomValues,
          script: unpackedScriptPtr,
          isExtensible: unpackedIsExtensible,
          source: unpackedSourcePtr,
          key: unpackedKey,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 20400)] = new CustomOptionJsonEncoder();

    class IndexJsonEncoder implements _JsonObjectEncoder {
      packObject(object: Index): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "INDEX";
        objectJson["id"] = object.id;
        if (object.parentPtr != null) {
          objectJson["parent"] = object.parentPtr.pack(1);
        }
        objectJson["space"] = object.spacePtr.pack(1);
        objectJson["materialization"] = Materialization[object.materialization];
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.instancePtr != null) {
          objectJson["instance"] = object.instancePtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["updatedAt"] = object.updatedAt.toString({ timeZoneName: 'never' });
        objectJson["updatedEpoch"] = Number(object.updatedEpoch);
        objectJson["updatedBy"] = object.updatedByPtr.pack(1);
        if (object.deletedAt != null) {
          objectJson["deletedAt"] = object.deletedAt.toString({ timeZoneName: 'never' });
        }
        if (object._ownedByPtr != null) {
          objectJson["ownedBy"] = object._ownedByPtr.pack(1);
        }
        objectJson["name"] = object._name;
        objectJson["orderKey"] = object.orderKey;
        if (Object.keys(object._customValues).length > 0) {
          const packedCustomValues: { [key: string]: any } = {} as any;
          for (const [key, value] of Object.entries(object._customValues)) {
            packedCustomValues[String(key)] = value.pack(1);
          }
          objectJson["customValues"] = packedCustomValues;
        }
        if (object._scriptPtr != null) {
          objectJson["script"] = object._scriptPtr.pack(1);
        }
        if (object.isExtensible != null) {
          objectJson["isExtensible"] = object.isExtensible;
        }
        if (object.sourcePtr != null) {
          objectJson["source"] = object.sourcePtr.pack(1);
        }
        if (object._key != null) {
          objectJson["key"] = object._key;
        }
        objectJson["type"] = IndexType[object._type];
        if (object._properties.length > 0) {
          const packedProperties: any[] = [];
          for (const item of object._properties) {
            packedProperties.push(item.pack(1));
          }
          objectJson["properties"] = packedProperties;
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): Index {
        const _Value = STRUCT_CLASS_BY_TYPE[100] as typeof Value;
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const _PropertyReference = STRUCT_CLASS_BY_TYPE[1001] as typeof PropertyReference;
        const unpackedProperties: any[] = [];
        if (objectJson["properties"] != undefined) {
          for (const item of objectJson["properties"]) {
            unpackedProperties.push(_PropertyReference.unpack(1, item, _session) as PropertyReference)
          }
        }
        const parentPtrValue = objectJson["parent"];
        const unpackedParentPtr = parentPtrValue != undefined ? _NodeReference.unpack(1, parentPtrValue, _session) as NodeReference : null;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const instancePtrValue = objectJson["instance"];
        const unpackedInstancePtr = instancePtrValue != undefined ? _NodeReference.unpack(1, instancePtrValue, _session) as NodeReference : null;
        const deletedAtValue = objectJson["deletedAt"];
        const unpackedDeletedAt = deletedAtValue != undefined ? Temporal.Instant.from(deletedAtValue).toZonedDateTimeISO('UTC') : null;
        const ownedByPtrValue = objectJson["ownedBy"];
        const unpackedOwnedByPtr = ownedByPtrValue != undefined ? _NodeReference.unpack(1, ownedByPtrValue, _session) as NodeReference : null;
        const unpackedCustomValues = {} as any;
        if (objectJson["customValues"] != undefined) {
          for (const [key, value] of Object.entries(objectJson["customValues"])) {
            unpackedCustomValues[key] = _Value.unpack(1, value as any, _session) as Value;
          }
        }
        const scriptPtrValue = objectJson["script"];
        const unpackedScriptPtr = scriptPtrValue != undefined ? _NodeReference.unpack(1, scriptPtrValue, _session) as NodeReference : null;
        const isExtensibleValue = objectJson["isExtensible"];
        const unpackedIsExtensible = isExtensibleValue != undefined ? isExtensibleValue : null;
        const sourcePtrValue = objectJson["source"];
        const unpackedSourcePtr = sourcePtrValue != undefined ? _NodeReference.unpack(1, sourcePtrValue, _session) as NodeReference : null;
        const keyValue = objectJson["key"];
        const unpackedKey = keyValue != undefined ? keyValue : null;
        return new (NODE_CLASS_BY_TYPE[30100] as typeof Index)({
          type: IndexType[objectJson["type"]] as any,
          properties: unpackedProperties,
          parent: unpackedParentPtr,
          materialization: Materialization[objectJson["materialization"]] as any,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          instance: unpackedInstancePtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          updatedAt: Temporal.Instant.from(objectJson["updatedAt"]).toZonedDateTimeISO('UTC'),
          updatedEpoch: Number(objectJson["updatedEpoch"]),
          updatedBy: _NodeReference.unpack(1, objectJson["updatedBy"], _session) as NodeReference,
          deletedAt: unpackedDeletedAt,
          ownedBy: unpackedOwnedByPtr,
          name: objectJson["name"],
          orderKey: objectJson["orderKey"],
          customValues: unpackedCustomValues,
          script: unpackedScriptPtr,
          isExtensible: unpackedIsExtensible,
          source: unpackedSourcePtr,
          key: unpackedKey,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 30100)] = new IndexJsonEncoder();

    class ConstraintJsonEncoder implements _JsonObjectEncoder {
      packObject(object: Constraint): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "CONSTRAINT";
        objectJson["id"] = object.id;
        if (object.parentPtr != null) {
          objectJson["parent"] = object.parentPtr.pack(1);
        }
        objectJson["space"] = object.spacePtr.pack(1);
        objectJson["materialization"] = Materialization[object.materialization];
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.instancePtr != null) {
          objectJson["instance"] = object.instancePtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["updatedAt"] = object.updatedAt.toString({ timeZoneName: 'never' });
        objectJson["updatedEpoch"] = Number(object.updatedEpoch);
        objectJson["updatedBy"] = object.updatedByPtr.pack(1);
        if (object.deletedAt != null) {
          objectJson["deletedAt"] = object.deletedAt.toString({ timeZoneName: 'never' });
        }
        if (object._ownedByPtr != null) {
          objectJson["ownedBy"] = object._ownedByPtr.pack(1);
        }
        objectJson["name"] = object._name;
        objectJson["orderKey"] = object.orderKey;
        if (Object.keys(object._customValues).length > 0) {
          const packedCustomValues: { [key: string]: any } = {} as any;
          for (const [key, value] of Object.entries(object._customValues)) {
            packedCustomValues[String(key)] = value.pack(1);
          }
          objectJson["customValues"] = packedCustomValues;
        }
        if (object._scriptPtr != null) {
          objectJson["script"] = object._scriptPtr.pack(1);
        }
        if (object.isExtensible != null) {
          objectJson["isExtensible"] = object.isExtensible;
        }
        if (object.sourcePtr != null) {
          objectJson["source"] = object.sourcePtr.pack(1);
        }
        if (object._key != null) {
          objectJson["key"] = object._key;
        }
        objectJson["type"] = ConstraintType[object._type];
        if (object._properties.length > 0) {
          const packedProperties: any[] = [];
          for (const item of object._properties) {
            packedProperties.push(item.pack(1));
          }
          objectJson["properties"] = packedProperties;
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): Constraint {
        const _Value = STRUCT_CLASS_BY_TYPE[100] as typeof Value;
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const _PropertyReference = STRUCT_CLASS_BY_TYPE[1001] as typeof PropertyReference;
        const unpackedProperties: any[] = [];
        if (objectJson["properties"] != undefined) {
          for (const item of objectJson["properties"]) {
            unpackedProperties.push(_PropertyReference.unpack(1, item, _session) as PropertyReference)
          }
        }
        const parentPtrValue = objectJson["parent"];
        const unpackedParentPtr = parentPtrValue != undefined ? _NodeReference.unpack(1, parentPtrValue, _session) as NodeReference : null;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const instancePtrValue = objectJson["instance"];
        const unpackedInstancePtr = instancePtrValue != undefined ? _NodeReference.unpack(1, instancePtrValue, _session) as NodeReference : null;
        const deletedAtValue = objectJson["deletedAt"];
        const unpackedDeletedAt = deletedAtValue != undefined ? Temporal.Instant.from(deletedAtValue).toZonedDateTimeISO('UTC') : null;
        const ownedByPtrValue = objectJson["ownedBy"];
        const unpackedOwnedByPtr = ownedByPtrValue != undefined ? _NodeReference.unpack(1, ownedByPtrValue, _session) as NodeReference : null;
        const unpackedCustomValues = {} as any;
        if (objectJson["customValues"] != undefined) {
          for (const [key, value] of Object.entries(objectJson["customValues"])) {
            unpackedCustomValues[key] = _Value.unpack(1, value as any, _session) as Value;
          }
        }
        const scriptPtrValue = objectJson["script"];
        const unpackedScriptPtr = scriptPtrValue != undefined ? _NodeReference.unpack(1, scriptPtrValue, _session) as NodeReference : null;
        const isExtensibleValue = objectJson["isExtensible"];
        const unpackedIsExtensible = isExtensibleValue != undefined ? isExtensibleValue : null;
        const sourcePtrValue = objectJson["source"];
        const unpackedSourcePtr = sourcePtrValue != undefined ? _NodeReference.unpack(1, sourcePtrValue, _session) as NodeReference : null;
        const keyValue = objectJson["key"];
        const unpackedKey = keyValue != undefined ? keyValue : null;
        return new (NODE_CLASS_BY_TYPE[30200] as typeof Constraint)({
          type: ConstraintType[objectJson["type"]] as any,
          properties: unpackedProperties,
          parent: unpackedParentPtr,
          materialization: Materialization[objectJson["materialization"]] as any,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          instance: unpackedInstancePtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          updatedAt: Temporal.Instant.from(objectJson["updatedAt"]).toZonedDateTimeISO('UTC'),
          updatedEpoch: Number(objectJson["updatedEpoch"]),
          updatedBy: _NodeReference.unpack(1, objectJson["updatedBy"], _session) as NodeReference,
          deletedAt: unpackedDeletedAt,
          ownedBy: unpackedOwnedByPtr,
          name: objectJson["name"],
          orderKey: objectJson["orderKey"],
          customValues: unpackedCustomValues,
          script: unpackedScriptPtr,
          isExtensible: unpackedIsExtensible,
          source: unpackedSourcePtr,
          key: unpackedKey,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 30200)] = new ConstraintJsonEncoder();

    class MigrationJsonEncoder implements _JsonObjectEncoder {
      packObject(object: Migration): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "MIGRATION";
        objectJson["id"] = object.id;
        if (object.parentPtr != null) {
          objectJson["parent"] = object.parentPtr.pack(1);
        }
        objectJson["space"] = object.spacePtr.pack(1);
        objectJson["materialization"] = Materialization[object.materialization];
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.instancePtr != null) {
          objectJson["instance"] = object.instancePtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["updatedAt"] = object.updatedAt.toString({ timeZoneName: 'never' });
        objectJson["updatedEpoch"] = Number(object.updatedEpoch);
        objectJson["updatedBy"] = object.updatedByPtr.pack(1);
        if (object.deletedAt != null) {
          objectJson["deletedAt"] = object.deletedAt.toString({ timeZoneName: 'never' });
        }
        if (object._ownedByPtr != null) {
          objectJson["ownedBy"] = object._ownedByPtr.pack(1);
        }
        objectJson["name"] = object._name;
        objectJson["orderKey"] = object.orderKey;
        if (Object.keys(object._customValues).length > 0) {
          const packedCustomValues: { [key: string]: any } = {} as any;
          for (const [key, value] of Object.entries(object._customValues)) {
            packedCustomValues[String(key)] = value.pack(1);
          }
          objectJson["customValues"] = packedCustomValues;
        }
        if (object._scriptPtr != null) {
          objectJson["script"] = object._scriptPtr.pack(1);
        }
        if (object.isExtensible != null) {
          objectJson["isExtensible"] = object.isExtensible;
        }
        if (object.sourcePtr != null) {
          objectJson["source"] = object.sourcePtr.pack(1);
        }
        if (object._key != null) {
          objectJson["key"] = object._key;
        }
        objectJson["type"] = MigrationType[object._type];
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): Migration {
        const _Value = STRUCT_CLASS_BY_TYPE[100] as typeof Value;
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const parentPtrValue = objectJson["parent"];
        const unpackedParentPtr = parentPtrValue != undefined ? _NodeReference.unpack(1, parentPtrValue, _session) as NodeReference : null;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const instancePtrValue = objectJson["instance"];
        const unpackedInstancePtr = instancePtrValue != undefined ? _NodeReference.unpack(1, instancePtrValue, _session) as NodeReference : null;
        const deletedAtValue = objectJson["deletedAt"];
        const unpackedDeletedAt = deletedAtValue != undefined ? Temporal.Instant.from(deletedAtValue).toZonedDateTimeISO('UTC') : null;
        const ownedByPtrValue = objectJson["ownedBy"];
        const unpackedOwnedByPtr = ownedByPtrValue != undefined ? _NodeReference.unpack(1, ownedByPtrValue, _session) as NodeReference : null;
        const unpackedCustomValues = {} as any;
        if (objectJson["customValues"] != undefined) {
          for (const [key, value] of Object.entries(objectJson["customValues"])) {
            unpackedCustomValues[key] = _Value.unpack(1, value as any, _session) as Value;
          }
        }
        const scriptPtrValue = objectJson["script"];
        const unpackedScriptPtr = scriptPtrValue != undefined ? _NodeReference.unpack(1, scriptPtrValue, _session) as NodeReference : null;
        const isExtensibleValue = objectJson["isExtensible"];
        const unpackedIsExtensible = isExtensibleValue != undefined ? isExtensibleValue : null;
        const sourcePtrValue = objectJson["source"];
        const unpackedSourcePtr = sourcePtrValue != undefined ? _NodeReference.unpack(1, sourcePtrValue, _session) as NodeReference : null;
        const keyValue = objectJson["key"];
        const unpackedKey = keyValue != undefined ? keyValue : null;
        return new (NODE_CLASS_BY_TYPE[31000] as typeof Migration)({
          type: MigrationType[objectJson["type"]] as any,
          parent: unpackedParentPtr,
          materialization: Materialization[objectJson["materialization"]] as any,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          instance: unpackedInstancePtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          updatedAt: Temporal.Instant.from(objectJson["updatedAt"]).toZonedDateTimeISO('UTC'),
          updatedEpoch: Number(objectJson["updatedEpoch"]),
          updatedBy: _NodeReference.unpack(1, objectJson["updatedBy"], _session) as NodeReference,
          deletedAt: unpackedDeletedAt,
          ownedBy: unpackedOwnedByPtr,
          name: objectJson["name"],
          orderKey: objectJson["orderKey"],
          customValues: unpackedCustomValues,
          script: unpackedScriptPtr,
          isExtensible: unpackedIsExtensible,
          source: unpackedSourcePtr,
          key: unpackedKey,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 31000)] = new MigrationJsonEncoder();

    class MigrationOperationJsonEncoder implements _JsonObjectEncoder {
      packObject(object: MigrationOperation): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "MIGRATION_OPERATION";
        objectJson["id"] = object.id;
        if (object.parentPtr != null) {
          objectJson["parent"] = object.parentPtr.pack(1);
        }
        objectJson["space"] = object.spacePtr.pack(1);
        objectJson["materialization"] = Materialization[object.materialization];
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.instancePtr != null) {
          objectJson["instance"] = object.instancePtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["updatedAt"] = object.updatedAt.toString({ timeZoneName: 'never' });
        objectJson["updatedEpoch"] = Number(object.updatedEpoch);
        objectJson["updatedBy"] = object.updatedByPtr.pack(1);
        if (object.deletedAt != null) {
          objectJson["deletedAt"] = object.deletedAt.toString({ timeZoneName: 'never' });
        }
        if (object._ownedByPtr != null) {
          objectJson["ownedBy"] = object._ownedByPtr.pack(1);
        }
        objectJson["name"] = object._name;
        objectJson["orderKey"] = object.orderKey;
        if (Object.keys(object._customValues).length > 0) {
          const packedCustomValues: { [key: string]: any } = {} as any;
          for (const [key, value] of Object.entries(object._customValues)) {
            packedCustomValues[String(key)] = value.pack(1);
          }
          objectJson["customValues"] = packedCustomValues;
        }
        if (object._scriptPtr != null) {
          objectJson["script"] = object._scriptPtr.pack(1);
        }
        if (object.isExtensible != null) {
          objectJson["isExtensible"] = object.isExtensible;
        }
        if (object.sourcePtr != null) {
          objectJson["source"] = object.sourcePtr.pack(1);
        }
        if (object._key != null) {
          objectJson["key"] = object._key;
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): MigrationOperation {
        const _Value = STRUCT_CLASS_BY_TYPE[100] as typeof Value;
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const parentPtrValue = objectJson["parent"];
        const unpackedParentPtr = parentPtrValue != undefined ? _NodeReference.unpack(1, parentPtrValue, _session) as NodeReference : null;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const instancePtrValue = objectJson["instance"];
        const unpackedInstancePtr = instancePtrValue != undefined ? _NodeReference.unpack(1, instancePtrValue, _session) as NodeReference : null;
        const deletedAtValue = objectJson["deletedAt"];
        const unpackedDeletedAt = deletedAtValue != undefined ? Temporal.Instant.from(deletedAtValue).toZonedDateTimeISO('UTC') : null;
        const ownedByPtrValue = objectJson["ownedBy"];
        const unpackedOwnedByPtr = ownedByPtrValue != undefined ? _NodeReference.unpack(1, ownedByPtrValue, _session) as NodeReference : null;
        const unpackedCustomValues = {} as any;
        if (objectJson["customValues"] != undefined) {
          for (const [key, value] of Object.entries(objectJson["customValues"])) {
            unpackedCustomValues[key] = _Value.unpack(1, value as any, _session) as Value;
          }
        }
        const scriptPtrValue = objectJson["script"];
        const unpackedScriptPtr = scriptPtrValue != undefined ? _NodeReference.unpack(1, scriptPtrValue, _session) as NodeReference : null;
        const isExtensibleValue = objectJson["isExtensible"];
        const unpackedIsExtensible = isExtensibleValue != undefined ? isExtensibleValue : null;
        const sourcePtrValue = objectJson["source"];
        const unpackedSourcePtr = sourcePtrValue != undefined ? _NodeReference.unpack(1, sourcePtrValue, _session) as NodeReference : null;
        const keyValue = objectJson["key"];
        const unpackedKey = keyValue != undefined ? keyValue : null;
        return new (NODE_CLASS_BY_TYPE[31100] as typeof MigrationOperation)({
          parent: unpackedParentPtr,
          materialization: Materialization[objectJson["materialization"]] as any,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          instance: unpackedInstancePtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          updatedAt: Temporal.Instant.from(objectJson["updatedAt"]).toZonedDateTimeISO('UTC'),
          updatedEpoch: Number(objectJson["updatedEpoch"]),
          updatedBy: _NodeReference.unpack(1, objectJson["updatedBy"], _session) as NodeReference,
          deletedAt: unpackedDeletedAt,
          ownedBy: unpackedOwnedByPtr,
          name: objectJson["name"],
          orderKey: objectJson["orderKey"],
          customValues: unpackedCustomValues,
          script: unpackedScriptPtr,
          isExtensible: unpackedIsExtensible,
          source: unpackedSourcePtr,
          key: unpackedKey,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 31100)] = new MigrationOperationJsonEncoder();

    class CustomPropertyJsonEncoder implements _JsonObjectEncoder {
      packObject(object: CustomProperty): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "CUSTOM_PROPERTY";
        objectJson["id"] = object.id;
        if (object.parentPtr != null) {
          objectJson["parent"] = object.parentPtr.pack(1);
        }
        objectJson["space"] = object.spacePtr.pack(1);
        objectJson["materialization"] = Materialization[object.materialization];
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.instancePtr != null) {
          objectJson["instance"] = object.instancePtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["updatedAt"] = object.updatedAt.toString({ timeZoneName: 'never' });
        objectJson["updatedEpoch"] = Number(object.updatedEpoch);
        objectJson["updatedBy"] = object.updatedByPtr.pack(1);
        if (object.deletedAt != null) {
          objectJson["deletedAt"] = object.deletedAt.toString({ timeZoneName: 'never' });
        }
        if (object._ownedByPtr != null) {
          objectJson["ownedBy"] = object._ownedByPtr.pack(1);
        }
        objectJson["name"] = object._name;
        objectJson["orderKey"] = object.orderKey;
        if (Object.keys(object._customValues).length > 0) {
          const packedCustomValues: { [key: string]: any } = {} as any;
          for (const [key, value] of Object.entries(object._customValues)) {
            packedCustomValues[String(key)] = value.pack(1);
          }
          objectJson["customValues"] = packedCustomValues;
        }
        if (object._scriptPtr != null) {
          objectJson["script"] = object._scriptPtr.pack(1);
        }
        if (object.isExtensible != null) {
          objectJson["isExtensible"] = object.isExtensible;
        }
        if (object.sourcePtr != null) {
          objectJson["source"] = object.sourcePtr.pack(1);
        }
        if (object._key != null) {
          objectJson["key"] = object._key;
        }
        objectJson["type"] = PropertyType[object._type];
        if (object._icon != null) {
          objectJson["icon"] = object._icon.pack(1);
        }
        objectJson["cardinality"] = TypeCardinality[object._cardinality];
        objectJson["scalarType"] = ScalarType[object._scalarType];
        if (object._primitiveType != null) {
          objectJson["primitiveType"] = PrimitiveType[object._primitiveType];
        }
        if (object._enumType != null) {
          objectJson["enumType"] = EnumType[object._enumType];
        }
        if (object._nodeTypes.length > 0) {
          const packedNodeTypes: any[] = [];
          for (const item of object._nodeTypes) {
            packedNodeTypes.push(NodeType[item]);
          }
          objectJson["nodeTypes"] = packedNodeTypes;
        }
        if (object._structType != null) {
          objectJson["structType"] = StructType[object._structType];
        }
        if (object._keyType != null) {
          objectJson["keyType"] = object._keyType.pack(1);
        }
        if (object._value != null) {
          objectJson["value"] = object._value.pack(1);
        }
        if (object._valueFactory != null) {
          objectJson["valueFactory"] = ValueFactory[object._valueFactory];
        }
        if (object._collectionConstraint != null) {
          objectJson["collectionConstraint"] = object._collectionConstraint.pack(1);
        }
        if (object._stringConstraint != null) {
          objectJson["stringConstraint"] = object._stringConstraint.pack(1);
        }
        if (object._numberConstraint != null) {
          objectJson["numberConstraint"] = object._numberConstraint.pack(1);
        }
        if (object._edgeType != null) {
          objectJson["edgeType"] = EdgeType[object._edgeType];
        }
        if (object._cascade != null) {
          objectJson["cascade"] = CascadeAction[object._cascade];
        }
        if (object._isRequired != null) {
          objectJson["isRequired"] = object._isRequired;
        }
        if (object._isUnique != null) {
          objectJson["isUnique"] = object._isUnique;
        }
        if (object._isComputed != null) {
          objectJson["isComputed"] = object._isComputed;
        }
        if (object._isReadonly != null) {
          objectJson["isReadonly"] = object._isReadonly;
        }
        if (object._isMain != null) {
          objectJson["isMain"] = object._isMain;
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): CustomProperty {
        const _Value = STRUCT_CLASS_BY_TYPE[100] as typeof Value;
        const _Type = STRUCT_CLASS_BY_TYPE[101] as typeof Type;
        const _NumberConstraint = STRUCT_CLASS_BY_TYPE[110] as typeof NumberConstraint;
        const _StringConstraint = STRUCT_CLASS_BY_TYPE[111] as typeof StringConstraint;
        const _CollectionConstraint = STRUCT_CLASS_BY_TYPE[112] as typeof CollectionConstraint;
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const _Icon = STRUCT_CLASS_BY_TYPE[400031] as typeof Icon;
        const iconValue = objectJson["icon"];
        const unpackedIcon = iconValue != undefined ? _Icon.unpack(1, iconValue, _session) as Icon : null;
        const primitiveTypeValue = objectJson["primitiveType"];
        const unpackedPrimitiveType = primitiveTypeValue != undefined ? PrimitiveType[primitiveTypeValue] as any : null;
        const enumTypeValue = objectJson["enumType"];
        const unpackedEnumType = enumTypeValue != undefined ? EnumType[enumTypeValue] as any : null;
        const unpackedNodeTypes: any[] = [];
        if (objectJson["nodeTypes"] != undefined) {
          for (const item of objectJson["nodeTypes"]) {
            unpackedNodeTypes.push(NodeType[item] as any)
          }
        }
        const structTypeValue = objectJson["structType"];
        const unpackedStructType = structTypeValue != undefined ? StructType[structTypeValue] as any : null;
        const keyTypeValue = objectJson["keyType"];
        const unpackedKeyType = keyTypeValue != undefined ? _Type.unpack(1, keyTypeValue, _session) as Type : null;
        const valueValue = objectJson["value"];
        const unpackedValue = valueValue != undefined ? _Value.unpack(1, valueValue, _session) as Value : null;
        const valueFactoryValue = objectJson["valueFactory"];
        const unpackedValueFactory = valueFactoryValue != undefined ? ValueFactory[valueFactoryValue] as any : null;
        const collectionConstraintValue = objectJson["collectionConstraint"];
        const unpackedCollectionConstraint = collectionConstraintValue != undefined ? _CollectionConstraint.unpack(1, collectionConstraintValue, _session) as CollectionConstraint : null;
        const stringConstraintValue = objectJson["stringConstraint"];
        const unpackedStringConstraint = stringConstraintValue != undefined ? _StringConstraint.unpack(1, stringConstraintValue, _session) as StringConstraint : null;
        const numberConstraintValue = objectJson["numberConstraint"];
        const unpackedNumberConstraint = numberConstraintValue != undefined ? _NumberConstraint.unpack(1, numberConstraintValue, _session) as NumberConstraint : null;
        const edgeTypeValue = objectJson["edgeType"];
        const unpackedEdgeType = edgeTypeValue != undefined ? EdgeType[edgeTypeValue] as any : null;
        const cascadeValue = objectJson["cascade"];
        const unpackedCascade = cascadeValue != undefined ? CascadeAction[cascadeValue] as any : null;
        const isRequiredValue = objectJson["isRequired"];
        const unpackedIsRequired = isRequiredValue != undefined ? isRequiredValue : null;
        const isUniqueValue = objectJson["isUnique"];
        const unpackedIsUnique = isUniqueValue != undefined ? isUniqueValue : null;
        const isComputedValue = objectJson["isComputed"];
        const unpackedIsComputed = isComputedValue != undefined ? isComputedValue : null;
        const isReadonlyValue = objectJson["isReadonly"];
        const unpackedIsReadonly = isReadonlyValue != undefined ? isReadonlyValue : null;
        const isMainValue = objectJson["isMain"];
        const unpackedIsMain = isMainValue != undefined ? isMainValue : null;
        const parentPtrValue = objectJson["parent"];
        const unpackedParentPtr = parentPtrValue != undefined ? _NodeReference.unpack(1, parentPtrValue, _session) as NodeReference : null;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const instancePtrValue = objectJson["instance"];
        const unpackedInstancePtr = instancePtrValue != undefined ? _NodeReference.unpack(1, instancePtrValue, _session) as NodeReference : null;
        const deletedAtValue = objectJson["deletedAt"];
        const unpackedDeletedAt = deletedAtValue != undefined ? Temporal.Instant.from(deletedAtValue).toZonedDateTimeISO('UTC') : null;
        const ownedByPtrValue = objectJson["ownedBy"];
        const unpackedOwnedByPtr = ownedByPtrValue != undefined ? _NodeReference.unpack(1, ownedByPtrValue, _session) as NodeReference : null;
        const unpackedCustomValues = {} as any;
        if (objectJson["customValues"] != undefined) {
          for (const [key, value] of Object.entries(objectJson["customValues"])) {
            unpackedCustomValues[key] = _Value.unpack(1, value as any, _session) as Value;
          }
        }
        const scriptPtrValue = objectJson["script"];
        const unpackedScriptPtr = scriptPtrValue != undefined ? _NodeReference.unpack(1, scriptPtrValue, _session) as NodeReference : null;
        const isExtensibleValue = objectJson["isExtensible"];
        const unpackedIsExtensible = isExtensibleValue != undefined ? isExtensibleValue : null;
        const sourcePtrValue = objectJson["source"];
        const unpackedSourcePtr = sourcePtrValue != undefined ? _NodeReference.unpack(1, sourcePtrValue, _session) as NodeReference : null;
        const keyValue = objectJson["key"];
        const unpackedKey = keyValue != undefined ? keyValue : null;
        return new (NODE_CLASS_BY_TYPE[20300] as typeof CustomProperty)({
          type: PropertyType[objectJson["type"]] as any,
          icon: unpackedIcon,
          cardinality: TypeCardinality[objectJson["cardinality"]] as any,
          scalarType: ScalarType[objectJson["scalarType"]] as any,
          primitiveType: unpackedPrimitiveType,
          enumType: unpackedEnumType,
          nodeTypes: unpackedNodeTypes,
          structType: unpackedStructType,
          keyType: unpackedKeyType,
          value: unpackedValue,
          valueFactory: unpackedValueFactory,
          collectionConstraint: unpackedCollectionConstraint,
          stringConstraint: unpackedStringConstraint,
          numberConstraint: unpackedNumberConstraint,
          edgeType: unpackedEdgeType,
          cascade: unpackedCascade,
          isRequired: unpackedIsRequired,
          isUnique: unpackedIsUnique,
          isComputed: unpackedIsComputed,
          isReadonly: unpackedIsReadonly,
          isMain: unpackedIsMain,
          parent: unpackedParentPtr,
          materialization: Materialization[objectJson["materialization"]] as any,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          instance: unpackedInstancePtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          updatedAt: Temporal.Instant.from(objectJson["updatedAt"]).toZonedDateTimeISO('UTC'),
          updatedEpoch: Number(objectJson["updatedEpoch"]),
          updatedBy: _NodeReference.unpack(1, objectJson["updatedBy"], _session) as NodeReference,
          deletedAt: unpackedDeletedAt,
          ownedBy: unpackedOwnedByPtr,
          name: objectJson["name"],
          orderKey: objectJson["orderKey"],
          customValues: unpackedCustomValues,
          script: unpackedScriptPtr,
          isExtensible: unpackedIsExtensible,
          source: unpackedSourcePtr,
          key: unpackedKey,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 20300)] = new CustomPropertyJsonEncoder();

    class SpaceJsonEncoder implements _JsonObjectEncoder {
      packObject(object: Space): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "SPACE";
        objectJson["id"] = object.id;
        if (object.parentPtr != null) {
          objectJson["parent"] = object.parentPtr.pack(1);
        }
        objectJson["space"] = object.spacePtr.pack(1);
        objectJson["materialization"] = Materialization[object.materialization];
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.instancePtr != null) {
          objectJson["instance"] = object.instancePtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["updatedAt"] = object.updatedAt.toString({ timeZoneName: 'never' });
        objectJson["updatedEpoch"] = Number(object.updatedEpoch);
        objectJson["updatedBy"] = object.updatedByPtr.pack(1);
        if (object.deletedAt != null) {
          objectJson["deletedAt"] = object.deletedAt.toString({ timeZoneName: 'never' });
        }
        if (object._ownedByPtr != null) {
          objectJson["ownedBy"] = object._ownedByPtr.pack(1);
        }
        objectJson["name"] = object._name;
        objectJson["orderKey"] = object.orderKey;
        if (Object.keys(object._customValues).length > 0) {
          const packedCustomValues: { [key: string]: any } = {} as any;
          for (const [key, value] of Object.entries(object._customValues)) {
            packedCustomValues[String(key)] = value.pack(1);
          }
          objectJson["customValues"] = packedCustomValues;
        }
        if (object._scriptPtr != null) {
          objectJson["script"] = object._scriptPtr.pack(1);
        }
        if (object.isExtensible != null) {
          objectJson["isExtensible"] = object.isExtensible;
        }
        if (object.sourcePtr != null) {
          objectJson["source"] = object.sourcePtr.pack(1);
        }
        if (object._key != null) {
          objectJson["key"] = object._key;
        }
        objectJson["slug"] = object._slug;
        if (object._handlePtr != null) {
          objectJson["handle"] = object._handlePtr.pack(1);
        }
        objectJson["region"] = Region[object._region];
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): Space {
        const _Value = STRUCT_CLASS_BY_TYPE[100] as typeof Value;
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const handlePtrValue = objectJson["handle"];
        const unpackedHandlePtr = handlePtrValue != undefined ? _NodeReference.unpack(1, handlePtrValue, _session) as NodeReference : null;
        const parentPtrValue = objectJson["parent"];
        const unpackedParentPtr = parentPtrValue != undefined ? _NodeReference.unpack(1, parentPtrValue, _session) as NodeReference : null;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const instancePtrValue = objectJson["instance"];
        const unpackedInstancePtr = instancePtrValue != undefined ? _NodeReference.unpack(1, instancePtrValue, _session) as NodeReference : null;
        const deletedAtValue = objectJson["deletedAt"];
        const unpackedDeletedAt = deletedAtValue != undefined ? Temporal.Instant.from(deletedAtValue).toZonedDateTimeISO('UTC') : null;
        const ownedByPtrValue = objectJson["ownedBy"];
        const unpackedOwnedByPtr = ownedByPtrValue != undefined ? _NodeReference.unpack(1, ownedByPtrValue, _session) as NodeReference : null;
        const unpackedCustomValues = {} as any;
        if (objectJson["customValues"] != undefined) {
          for (const [key, value] of Object.entries(objectJson["customValues"])) {
            unpackedCustomValues[key] = _Value.unpack(1, value as any, _session) as Value;
          }
        }
        const scriptPtrValue = objectJson["script"];
        const unpackedScriptPtr = scriptPtrValue != undefined ? _NodeReference.unpack(1, scriptPtrValue, _session) as NodeReference : null;
        const isExtensibleValue = objectJson["isExtensible"];
        const unpackedIsExtensible = isExtensibleValue != undefined ? isExtensibleValue : null;
        const sourcePtrValue = objectJson["source"];
        const unpackedSourcePtr = sourcePtrValue != undefined ? _NodeReference.unpack(1, sourcePtrValue, _session) as NodeReference : null;
        const keyValue = objectJson["key"];
        const unpackedKey = keyValue != undefined ? keyValue : null;
        return new (NODE_CLASS_BY_TYPE[1100] as typeof Space)({
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          slug: objectJson["slug"],
          handle: unpackedHandlePtr,
          region: Region[objectJson["region"]] as any,
          parent: unpackedParentPtr,
          materialization: Materialization[objectJson["materialization"]] as any,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          instance: unpackedInstancePtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          updatedAt: Temporal.Instant.from(objectJson["updatedAt"]).toZonedDateTimeISO('UTC'),
          updatedEpoch: Number(objectJson["updatedEpoch"]),
          updatedBy: _NodeReference.unpack(1, objectJson["updatedBy"], _session) as NodeReference,
          deletedAt: unpackedDeletedAt,
          ownedBy: unpackedOwnedByPtr,
          name: objectJson["name"],
          orderKey: objectJson["orderKey"],
          customValues: unpackedCustomValues,
          script: unpackedScriptPtr,
          isExtensible: unpackedIsExtensible,
          source: unpackedSourcePtr,
          key: unpackedKey,
          id: objectJson["id"],
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 1100)] = new SpaceJsonEncoder();

    class CustomStructJsonEncoder implements _JsonObjectEncoder {
      packObject(object: CustomStruct): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "CUSTOM_STRUCT";
        objectJson["id"] = object.id;
        if (object.parentPtr != null) {
          objectJson["parent"] = object.parentPtr.pack(1);
        }
        objectJson["space"] = object.spacePtr.pack(1);
        objectJson["materialization"] = Materialization[object.materialization];
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.instancePtr != null) {
          objectJson["instance"] = object.instancePtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["updatedAt"] = object.updatedAt.toString({ timeZoneName: 'never' });
        objectJson["updatedEpoch"] = Number(object.updatedEpoch);
        objectJson["updatedBy"] = object.updatedByPtr.pack(1);
        if (object.deletedAt != null) {
          objectJson["deletedAt"] = object.deletedAt.toString({ timeZoneName: 'never' });
        }
        if (object._ownedByPtr != null) {
          objectJson["ownedBy"] = object._ownedByPtr.pack(1);
        }
        objectJson["name"] = object._name;
        objectJson["orderKey"] = object.orderKey;
        if (Object.keys(object._customValues).length > 0) {
          const packedCustomValues: { [key: string]: any } = {} as any;
          for (const [key, value] of Object.entries(object._customValues)) {
            packedCustomValues[String(key)] = value.pack(1);
          }
          objectJson["customValues"] = packedCustomValues;
        }
        if (object._scriptPtr != null) {
          objectJson["script"] = object._scriptPtr.pack(1);
        }
        if (object.isExtensible != null) {
          objectJson["isExtensible"] = object.isExtensible;
        }
        if (object.sourcePtr != null) {
          objectJson["source"] = object.sourcePtr.pack(1);
        }
        if (object._key != null) {
          objectJson["key"] = object._key;
        }
        if (object._icon != null) {
          objectJson["icon"] = object._icon.pack(1);
        }
        if (object._baseType != null) {
          objectJson["baseType"] = object._baseType.pack(1);
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): CustomStruct {
        const _StructDefinitionReference = STRUCT_CLASS_BY_TYPE[16] as typeof StructDefinitionReference;
        const _Value = STRUCT_CLASS_BY_TYPE[100] as typeof Value;
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const _Icon = STRUCT_CLASS_BY_TYPE[400031] as typeof Icon;
        const iconValue = objectJson["icon"];
        const unpackedIcon = iconValue != undefined ? _Icon.unpack(1, iconValue, _session) as Icon : null;
        const baseTypeValue = objectJson["baseType"];
        const unpackedBaseType = baseTypeValue != undefined ? _StructDefinitionReference.unpack(1, baseTypeValue, _session) as StructDefinitionReference : null;
        const parentPtrValue = objectJson["parent"];
        const unpackedParentPtr = parentPtrValue != undefined ? _NodeReference.unpack(1, parentPtrValue, _session) as NodeReference : null;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const instancePtrValue = objectJson["instance"];
        const unpackedInstancePtr = instancePtrValue != undefined ? _NodeReference.unpack(1, instancePtrValue, _session) as NodeReference : null;
        const deletedAtValue = objectJson["deletedAt"];
        const unpackedDeletedAt = deletedAtValue != undefined ? Temporal.Instant.from(deletedAtValue).toZonedDateTimeISO('UTC') : null;
        const ownedByPtrValue = objectJson["ownedBy"];
        const unpackedOwnedByPtr = ownedByPtrValue != undefined ? _NodeReference.unpack(1, ownedByPtrValue, _session) as NodeReference : null;
        const unpackedCustomValues = {} as any;
        if (objectJson["customValues"] != undefined) {
          for (const [key, value] of Object.entries(objectJson["customValues"])) {
            unpackedCustomValues[key] = _Value.unpack(1, value as any, _session) as Value;
          }
        }
        const scriptPtrValue = objectJson["script"];
        const unpackedScriptPtr = scriptPtrValue != undefined ? _NodeReference.unpack(1, scriptPtrValue, _session) as NodeReference : null;
        const isExtensibleValue = objectJson["isExtensible"];
        const unpackedIsExtensible = isExtensibleValue != undefined ? isExtensibleValue : null;
        const sourcePtrValue = objectJson["source"];
        const unpackedSourcePtr = sourcePtrValue != undefined ? _NodeReference.unpack(1, sourcePtrValue, _session) as NodeReference : null;
        const keyValue = objectJson["key"];
        const unpackedKey = keyValue != undefined ? keyValue : null;
        return new (NODE_CLASS_BY_TYPE[20100] as typeof CustomStruct)({
          icon: unpackedIcon,
          baseType: unpackedBaseType,
          parent: unpackedParentPtr,
          materialization: Materialization[objectJson["materialization"]] as any,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          instance: unpackedInstancePtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          updatedAt: Temporal.Instant.from(objectJson["updatedAt"]).toZonedDateTimeISO('UTC'),
          updatedEpoch: Number(objectJson["updatedEpoch"]),
          updatedBy: _NodeReference.unpack(1, objectJson["updatedBy"], _session) as NodeReference,
          deletedAt: unpackedDeletedAt,
          ownedBy: unpackedOwnedByPtr,
          name: objectJson["name"],
          orderKey: objectJson["orderKey"],
          customValues: unpackedCustomValues,
          script: unpackedScriptPtr,
          isExtensible: unpackedIsExtensible,
          source: unpackedSourcePtr,
          key: unpackedKey,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 20100)] = new CustomStructJsonEncoder();

    class BranchJsonEncoder implements _JsonObjectEncoder {
      packObject(object: Branch): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "BRANCH";
        objectJson["id"] = object.id;
        if (object.parentPtr != null) {
          objectJson["parent"] = object.parentPtr.pack(1);
        }
        objectJson["space"] = object.spacePtr.pack(1);
        objectJson["materialization"] = Materialization[object.materialization];
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.instancePtr != null) {
          objectJson["instance"] = object.instancePtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["updatedAt"] = object.updatedAt.toString({ timeZoneName: 'never' });
        objectJson["updatedEpoch"] = Number(object.updatedEpoch);
        objectJson["updatedBy"] = object.updatedByPtr.pack(1);
        if (object.deletedAt != null) {
          objectJson["deletedAt"] = object.deletedAt.toString({ timeZoneName: 'never' });
        }
        if (object._ownedByPtr != null) {
          objectJson["ownedBy"] = object._ownedByPtr.pack(1);
        }
        objectJson["name"] = object._name;
        objectJson["orderKey"] = object.orderKey;
        if (Object.keys(object._customValues).length > 0) {
          const packedCustomValues: { [key: string]: any } = {} as any;
          for (const [key, value] of Object.entries(object._customValues)) {
            packedCustomValues[String(key)] = value.pack(1);
          }
          objectJson["customValues"] = packedCustomValues;
        }
        if (object._scriptPtr != null) {
          objectJson["script"] = object._scriptPtr.pack(1);
        }
        if (object.isExtensible != null) {
          objectJson["isExtensible"] = object.isExtensible;
        }
        if (object.sourcePtr != null) {
          objectJson["source"] = object.sourcePtr.pack(1);
        }
        if (object._key != null) {
          objectJson["key"] = object._key;
        }
        objectJson["type"] = BranchType[object._type];
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): Branch {
        const _Value = STRUCT_CLASS_BY_TYPE[100] as typeof Value;
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const parentPtrValue = objectJson["parent"];
        const unpackedParentPtr = parentPtrValue != undefined ? _NodeReference.unpack(1, parentPtrValue, _session) as NodeReference : null;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const instancePtrValue = objectJson["instance"];
        const unpackedInstancePtr = instancePtrValue != undefined ? _NodeReference.unpack(1, instancePtrValue, _session) as NodeReference : null;
        const deletedAtValue = objectJson["deletedAt"];
        const unpackedDeletedAt = deletedAtValue != undefined ? Temporal.Instant.from(deletedAtValue).toZonedDateTimeISO('UTC') : null;
        const ownedByPtrValue = objectJson["ownedBy"];
        const unpackedOwnedByPtr = ownedByPtrValue != undefined ? _NodeReference.unpack(1, ownedByPtrValue, _session) as NodeReference : null;
        const unpackedCustomValues = {} as any;
        if (objectJson["customValues"] != undefined) {
          for (const [key, value] of Object.entries(objectJson["customValues"])) {
            unpackedCustomValues[key] = _Value.unpack(1, value as any, _session) as Value;
          }
        }
        const scriptPtrValue = objectJson["script"];
        const unpackedScriptPtr = scriptPtrValue != undefined ? _NodeReference.unpack(1, scriptPtrValue, _session) as NodeReference : null;
        const isExtensibleValue = objectJson["isExtensible"];
        const unpackedIsExtensible = isExtensibleValue != undefined ? isExtensibleValue : null;
        const sourcePtrValue = objectJson["source"];
        const unpackedSourcePtr = sourcePtrValue != undefined ? _NodeReference.unpack(1, sourcePtrValue, _session) as NodeReference : null;
        const keyValue = objectJson["key"];
        const unpackedKey = keyValue != undefined ? keyValue : null;
        return new (NODE_CLASS_BY_TYPE[2000] as typeof Branch)({
          parent: unpackedParentPtr,
          type: BranchType[objectJson["type"]] as any,
          materialization: Materialization[objectJson["materialization"]] as any,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          instance: unpackedInstancePtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          updatedAt: Temporal.Instant.from(objectJson["updatedAt"]).toZonedDateTimeISO('UTC'),
          updatedEpoch: Number(objectJson["updatedEpoch"]),
          updatedBy: _NodeReference.unpack(1, objectJson["updatedBy"], _session) as NodeReference,
          deletedAt: unpackedDeletedAt,
          ownedBy: unpackedOwnedByPtr,
          name: objectJson["name"],
          orderKey: objectJson["orderKey"],
          customValues: unpackedCustomValues,
          script: unpackedScriptPtr,
          isExtensible: unpackedIsExtensible,
          source: unpackedSourcePtr,
          key: unpackedKey,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 2000)] = new BranchJsonEncoder();

    class SnapshotJsonEncoder implements _JsonObjectEncoder {
      packObject(object: Snapshot): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "SNAPSHOT";
        objectJson["id"] = object.id;
        if (object.parentPtr != null) {
          objectJson["parent"] = object.parentPtr.pack(1);
        }
        objectJson["space"] = object.spacePtr.pack(1);
        objectJson["materialization"] = Materialization[object.materialization];
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.instancePtr != null) {
          objectJson["instance"] = object.instancePtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["updatedAt"] = object.updatedAt.toString({ timeZoneName: 'never' });
        objectJson["updatedEpoch"] = Number(object.updatedEpoch);
        objectJson["updatedBy"] = object.updatedByPtr.pack(1);
        if (object.deletedAt != null) {
          objectJson["deletedAt"] = object.deletedAt.toString({ timeZoneName: 'never' });
        }
        if (object._ownedByPtr != null) {
          objectJson["ownedBy"] = object._ownedByPtr.pack(1);
        }
        objectJson["name"] = object._name;
        objectJson["orderKey"] = object.orderKey;
        if (Object.keys(object._customValues).length > 0) {
          const packedCustomValues: { [key: string]: any } = {} as any;
          for (const [key, value] of Object.entries(object._customValues)) {
            packedCustomValues[String(key)] = value.pack(1);
          }
          objectJson["customValues"] = packedCustomValues;
        }
        if (object._scriptPtr != null) {
          objectJson["script"] = object._scriptPtr.pack(1);
        }
        if (object.isExtensible != null) {
          objectJson["isExtensible"] = object.isExtensible;
        }
        if (object.sourcePtr != null) {
          objectJson["source"] = object.sourcePtr.pack(1);
        }
        if (object._key != null) {
          objectJson["key"] = object._key;
        }
        objectJson["type"] = SnapshotType[object._type];
        objectJson["status"] = SnapshotStatus[object._status];
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): Snapshot {
        const _Value = STRUCT_CLASS_BY_TYPE[100] as typeof Value;
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const parentPtrValue = objectJson["parent"];
        const unpackedParentPtr = parentPtrValue != undefined ? _NodeReference.unpack(1, parentPtrValue, _session) as NodeReference : null;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const instancePtrValue = objectJson["instance"];
        const unpackedInstancePtr = instancePtrValue != undefined ? _NodeReference.unpack(1, instancePtrValue, _session) as NodeReference : null;
        const deletedAtValue = objectJson["deletedAt"];
        const unpackedDeletedAt = deletedAtValue != undefined ? Temporal.Instant.from(deletedAtValue).toZonedDateTimeISO('UTC') : null;
        const ownedByPtrValue = objectJson["ownedBy"];
        const unpackedOwnedByPtr = ownedByPtrValue != undefined ? _NodeReference.unpack(1, ownedByPtrValue, _session) as NodeReference : null;
        const unpackedCustomValues = {} as any;
        if (objectJson["customValues"] != undefined) {
          for (const [key, value] of Object.entries(objectJson["customValues"])) {
            unpackedCustomValues[key] = _Value.unpack(1, value as any, _session) as Value;
          }
        }
        const scriptPtrValue = objectJson["script"];
        const unpackedScriptPtr = scriptPtrValue != undefined ? _NodeReference.unpack(1, scriptPtrValue, _session) as NodeReference : null;
        const isExtensibleValue = objectJson["isExtensible"];
        const unpackedIsExtensible = isExtensibleValue != undefined ? isExtensibleValue : null;
        const sourcePtrValue = objectJson["source"];
        const unpackedSourcePtr = sourcePtrValue != undefined ? _NodeReference.unpack(1, sourcePtrValue, _session) as NodeReference : null;
        const keyValue = objectJson["key"];
        const unpackedKey = keyValue != undefined ? keyValue : null;
        return new (NODE_CLASS_BY_TYPE[2100] as typeof Snapshot)({
          parent: unpackedParentPtr,
          type: SnapshotType[objectJson["type"]] as any,
          status: SnapshotStatus[objectJson["status"]] as any,
          materialization: Materialization[objectJson["materialization"]] as any,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          instance: unpackedInstancePtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          updatedAt: Temporal.Instant.from(objectJson["updatedAt"]).toZonedDateTimeISO('UTC'),
          updatedEpoch: Number(objectJson["updatedEpoch"]),
          updatedBy: _NodeReference.unpack(1, objectJson["updatedBy"], _session) as NodeReference,
          deletedAt: unpackedDeletedAt,
          ownedBy: unpackedOwnedByPtr,
          name: objectJson["name"],
          orderKey: objectJson["orderKey"],
          customValues: unpackedCustomValues,
          script: unpackedScriptPtr,
          isExtensible: unpackedIsExtensible,
          source: unpackedSourcePtr,
          key: unpackedKey,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 2100)] = new SnapshotJsonEncoder();

    class EntitlementRequestedEventJsonEncoder implements _JsonObjectEncoder {
      packObject(object: EntitlementRequestedEvent): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "ENTITLEMENT_REQUESTED_EVENT";
        objectJson["id"] = object.id;
        objectJson["space"] = object.spacePtr.pack(1);
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.causedByPtr != null) {
          objectJson["causedBy"] = object.causedByPtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["client"] = object.clientPtr.pack(1);
        objectJson["clientNonce"] = object.clientNonce;
        objectJson["clientCreatedAt"] = object.clientCreatedAt.toString({ timeZoneName: 'never' });
        objectJson["clientEpoch"] = Number(object.clientEpoch);
        objectJson["status"] = EventStatus[object.status];
        objectJson["node"] = object.nodePtr.pack(1);
        objectJson["target"] = object.targetPtr.pack(1);
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): EntitlementRequestedEvent {
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const causedByPtrValue = objectJson["causedBy"];
        const unpackedCausedByPtr = causedByPtrValue != undefined ? _NodeReference.unpack(1, causedByPtrValue, _session) as NodeReference : null;
        return new (NODE_CLASS_BY_TYPE[360502] as typeof EntitlementRequestedEvent)({
          node: _NodeReference.unpack(1, objectJson["node"], _session) as NodeReference,
          target: _NodeReference.unpack(1, objectJson["target"], _session) as NodeReference,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          causedBy: unpackedCausedByPtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          client: _NodeReference.unpack(1, objectJson["client"], _session) as NodeReference,
          clientNonce: objectJson["clientNonce"],
          clientCreatedAt: Temporal.Instant.from(objectJson["clientCreatedAt"]).toZonedDateTimeISO('UTC'),
          clientEpoch: Number(objectJson["clientEpoch"]),
          status: EventStatus[objectJson["status"]] as any,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 360502)] = new EntitlementRequestedEventJsonEncoder();

    class EntitlementGrantedEventJsonEncoder implements _JsonObjectEncoder {
      packObject(object: EntitlementGrantedEvent): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "ENTITLEMENT_GRANTED_EVENT";
        objectJson["id"] = object.id;
        objectJson["space"] = object.spacePtr.pack(1);
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.causedByPtr != null) {
          objectJson["causedBy"] = object.causedByPtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["client"] = object.clientPtr.pack(1);
        objectJson["clientNonce"] = object.clientNonce;
        objectJson["clientCreatedAt"] = object.clientCreatedAt.toString({ timeZoneName: 'never' });
        objectJson["clientEpoch"] = Number(object.clientEpoch);
        objectJson["status"] = EventStatus[object.status];
        objectJson["node"] = object.nodePtr.pack(1);
        objectJson["target"] = object.targetPtr.pack(1);
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): EntitlementGrantedEvent {
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const causedByPtrValue = objectJson["causedBy"];
        const unpackedCausedByPtr = causedByPtrValue != undefined ? _NodeReference.unpack(1, causedByPtrValue, _session) as NodeReference : null;
        return new (NODE_CLASS_BY_TYPE[360503] as typeof EntitlementGrantedEvent)({
          node: _NodeReference.unpack(1, objectJson["node"], _session) as NodeReference,
          target: _NodeReference.unpack(1, objectJson["target"], _session) as NodeReference,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          causedBy: unpackedCausedByPtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          client: _NodeReference.unpack(1, objectJson["client"], _session) as NodeReference,
          clientNonce: objectJson["clientNonce"],
          clientCreatedAt: Temporal.Instant.from(objectJson["clientCreatedAt"]).toZonedDateTimeISO('UTC'),
          clientEpoch: Number(objectJson["clientEpoch"]),
          status: EventStatus[objectJson["status"]] as any,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 360503)] = new EntitlementGrantedEventJsonEncoder();

    class EntitlementRevokedEventJsonEncoder implements _JsonObjectEncoder {
      packObject(object: EntitlementRevokedEvent): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "ENTITLEMENT_REVOKED_EVENT";
        objectJson["id"] = object.id;
        objectJson["space"] = object.spacePtr.pack(1);
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.causedByPtr != null) {
          objectJson["causedBy"] = object.causedByPtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["client"] = object.clientPtr.pack(1);
        objectJson["clientNonce"] = object.clientNonce;
        objectJson["clientCreatedAt"] = object.clientCreatedAt.toString({ timeZoneName: 'never' });
        objectJson["clientEpoch"] = Number(object.clientEpoch);
        objectJson["status"] = EventStatus[object.status];
        objectJson["node"] = object.nodePtr.pack(1);
        objectJson["target"] = object.targetPtr.pack(1);
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): EntitlementRevokedEvent {
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const causedByPtrValue = objectJson["causedBy"];
        const unpackedCausedByPtr = causedByPtrValue != undefined ? _NodeReference.unpack(1, causedByPtrValue, _session) as NodeReference : null;
        return new (NODE_CLASS_BY_TYPE[360504] as typeof EntitlementRevokedEvent)({
          node: _NodeReference.unpack(1, objectJson["node"], _session) as NodeReference,
          target: _NodeReference.unpack(1, objectJson["target"], _session) as NodeReference,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          causedBy: unpackedCausedByPtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          client: _NodeReference.unpack(1, objectJson["client"], _session) as NodeReference,
          clientNonce: objectJson["clientNonce"],
          clientCreatedAt: Temporal.Instant.from(objectJson["clientCreatedAt"]).toZonedDateTimeISO('UTC'),
          clientEpoch: Number(objectJson["clientEpoch"]),
          status: EventStatus[objectJson["status"]] as any,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 360504)] = new EntitlementRevokedEventJsonEncoder();

    class EntitlementExpiredEventJsonEncoder implements _JsonObjectEncoder {
      packObject(object: EntitlementExpiredEvent): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "ENTITLEMENT_EXPIRED_EVENT";
        objectJson["id"] = object.id;
        objectJson["space"] = object.spacePtr.pack(1);
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.causedByPtr != null) {
          objectJson["causedBy"] = object.causedByPtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["client"] = object.clientPtr.pack(1);
        objectJson["clientNonce"] = object.clientNonce;
        objectJson["clientCreatedAt"] = object.clientCreatedAt.toString({ timeZoneName: 'never' });
        objectJson["clientEpoch"] = Number(object.clientEpoch);
        objectJson["status"] = EventStatus[object.status];
        objectJson["node"] = object.nodePtr.pack(1);
        objectJson["target"] = object.targetPtr.pack(1);
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): EntitlementExpiredEvent {
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const causedByPtrValue = objectJson["causedBy"];
        const unpackedCausedByPtr = causedByPtrValue != undefined ? _NodeReference.unpack(1, causedByPtrValue, _session) as NodeReference : null;
        return new (NODE_CLASS_BY_TYPE[360505] as typeof EntitlementExpiredEvent)({
          node: _NodeReference.unpack(1, objectJson["node"], _session) as NodeReference,
          target: _NodeReference.unpack(1, objectJson["target"], _session) as NodeReference,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          causedBy: unpackedCausedByPtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          client: _NodeReference.unpack(1, objectJson["client"], _session) as NodeReference,
          clientNonce: objectJson["clientNonce"],
          clientCreatedAt: Temporal.Instant.from(objectJson["clientCreatedAt"]).toZonedDateTimeISO('UTC'),
          clientEpoch: Number(objectJson["clientEpoch"]),
          status: EventStatus[objectJson["status"]] as any,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 360505)] = new EntitlementExpiredEventJsonEncoder();

    class EntitlementJsonEncoder implements _JsonObjectEncoder {
      packObject(object: Entitlement): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "ENTITLEMENT";
        objectJson["id"] = object.id;
        if (object.parentPtr != null) {
          objectJson["parent"] = object.parentPtr.pack(1);
        }
        objectJson["space"] = object.spacePtr.pack(1);
        objectJson["materialization"] = Materialization[object.materialization];
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.instancePtr != null) {
          objectJson["instance"] = object.instancePtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["updatedAt"] = object.updatedAt.toString({ timeZoneName: 'never' });
        objectJson["updatedEpoch"] = Number(object.updatedEpoch);
        objectJson["updatedBy"] = object.updatedByPtr.pack(1);
        if (object.deletedAt != null) {
          objectJson["deletedAt"] = object.deletedAt.toString({ timeZoneName: 'never' });
        }
        if (object._ownedByPtr != null) {
          objectJson["ownedBy"] = object._ownedByPtr.pack(1);
        }
        objectJson["name"] = object._name;
        objectJson["orderKey"] = object.orderKey;
        if (Object.keys(object._customValues).length > 0) {
          const packedCustomValues: { [key: string]: any } = {} as any;
          for (const [key, value] of Object.entries(object._customValues)) {
            packedCustomValues[String(key)] = value.pack(1);
          }
          objectJson["customValues"] = packedCustomValues;
        }
        if (object._scriptPtr != null) {
          objectJson["script"] = object._scriptPtr.pack(1);
        }
        if (object.isExtensible != null) {
          objectJson["isExtensible"] = object.isExtensible;
        }
        if (object.sourcePtr != null) {
          objectJson["source"] = object.sourcePtr.pack(1);
        }
        if (object._key != null) {
          objectJson["key"] = object._key;
        }
        objectJson["type"] = EntitlementType[object._type];
        if (object._expiresAt != null) {
          objectJson["expiresAt"] = object._expiresAt.toString({ timeZoneName: 'never' });
        }
        objectJson["target"] = object._targetPtr.pack(1);
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): Entitlement {
        const _Value = STRUCT_CLASS_BY_TYPE[100] as typeof Value;
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const expiresAtValue = objectJson["expiresAt"];
        const unpackedExpiresAt = expiresAtValue != undefined ? Temporal.Instant.from(expiresAtValue).toZonedDateTimeISO('UTC') : null;
        const parentPtrValue = objectJson["parent"];
        const unpackedParentPtr = parentPtrValue != undefined ? _NodeReference.unpack(1, parentPtrValue, _session) as NodeReference : null;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const instancePtrValue = objectJson["instance"];
        const unpackedInstancePtr = instancePtrValue != undefined ? _NodeReference.unpack(1, instancePtrValue, _session) as NodeReference : null;
        const deletedAtValue = objectJson["deletedAt"];
        const unpackedDeletedAt = deletedAtValue != undefined ? Temporal.Instant.from(deletedAtValue).toZonedDateTimeISO('UTC') : null;
        const ownedByPtrValue = objectJson["ownedBy"];
        const unpackedOwnedByPtr = ownedByPtrValue != undefined ? _NodeReference.unpack(1, ownedByPtrValue, _session) as NodeReference : null;
        const unpackedCustomValues = {} as any;
        if (objectJson["customValues"] != undefined) {
          for (const [key, value] of Object.entries(objectJson["customValues"])) {
            unpackedCustomValues[key] = _Value.unpack(1, value as any, _session) as Value;
          }
        }
        const scriptPtrValue = objectJson["script"];
        const unpackedScriptPtr = scriptPtrValue != undefined ? _NodeReference.unpack(1, scriptPtrValue, _session) as NodeReference : null;
        const isExtensibleValue = objectJson["isExtensible"];
        const unpackedIsExtensible = isExtensibleValue != undefined ? isExtensibleValue : null;
        const sourcePtrValue = objectJson["source"];
        const unpackedSourcePtr = sourcePtrValue != undefined ? _NodeReference.unpack(1, sourcePtrValue, _session) as NodeReference : null;
        const keyValue = objectJson["key"];
        const unpackedKey = keyValue != undefined ? keyValue : null;
        return new (NODE_CLASS_BY_TYPE[360500] as typeof Entitlement)({
          type: EntitlementType[objectJson["type"]] as any,
          expiresAt: unpackedExpiresAt,
          target: _NodeReference.unpack(1, objectJson["target"], _session) as NodeReference,
          parent: unpackedParentPtr,
          materialization: Materialization[objectJson["materialization"]] as any,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          instance: unpackedInstancePtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          updatedAt: Temporal.Instant.from(objectJson["updatedAt"]).toZonedDateTimeISO('UTC'),
          updatedEpoch: Number(objectJson["updatedEpoch"]),
          updatedBy: _NodeReference.unpack(1, objectJson["updatedBy"], _session) as NodeReference,
          deletedAt: unpackedDeletedAt,
          ownedBy: unpackedOwnedByPtr,
          name: objectJson["name"],
          orderKey: objectJson["orderKey"],
          customValues: unpackedCustomValues,
          script: unpackedScriptPtr,
          isExtensible: unpackedIsExtensible,
          source: unpackedSourcePtr,
          key: unpackedKey,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 360500)] = new EntitlementJsonEncoder();

    class InviteSentEventJsonEncoder implements _JsonObjectEncoder {
      packObject(object: InviteSentEvent): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "INVITE_SENT_EVENT";
        objectJson["id"] = object.id;
        objectJson["space"] = object.spacePtr.pack(1);
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.causedByPtr != null) {
          objectJson["causedBy"] = object.causedByPtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["client"] = object.clientPtr.pack(1);
        objectJson["clientNonce"] = object.clientNonce;
        objectJson["clientCreatedAt"] = object.clientCreatedAt.toString({ timeZoneName: 'never' });
        objectJson["clientEpoch"] = Number(object.clientEpoch);
        objectJson["status"] = EventStatus[object.status];
        objectJson["node"] = object.nodePtr.pack(1);
        objectJson["joinable"] = object.joinablePtr.pack(1);
        objectJson["member"] = object.memberPtr.pack(1);
        objectJson["role"] = object.rolePtr.pack(1);
        objectJson["roleType"] = RoleType[object.roleType];
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): InviteSentEvent {
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const causedByPtrValue = objectJson["causedBy"];
        const unpackedCausedByPtr = causedByPtrValue != undefined ? _NodeReference.unpack(1, causedByPtrValue, _session) as NodeReference : null;
        return new (NODE_CLASS_BY_TYPE[360102] as typeof InviteSentEvent)({
          role: _NodeReference.unpack(1, objectJson["role"], _session) as NodeReference,
          roleType: RoleType[objectJson["roleType"]] as any,
          node: _NodeReference.unpack(1, objectJson["node"], _session) as NodeReference,
          joinable: _NodeReference.unpack(1, objectJson["joinable"], _session) as NodeReference,
          member: _NodeReference.unpack(1, objectJson["member"], _session) as NodeReference,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          causedBy: unpackedCausedByPtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          client: _NodeReference.unpack(1, objectJson["client"], _session) as NodeReference,
          clientNonce: objectJson["clientNonce"],
          clientCreatedAt: Temporal.Instant.from(objectJson["clientCreatedAt"]).toZonedDateTimeISO('UTC'),
          clientEpoch: Number(objectJson["clientEpoch"]),
          status: EventStatus[objectJson["status"]] as any,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 360102)] = new InviteSentEventJsonEncoder();

    class InviteRescindedEventJsonEncoder implements _JsonObjectEncoder {
      packObject(object: InviteRescindedEvent): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "INVITE_RESCINDED_EVENT";
        objectJson["id"] = object.id;
        objectJson["space"] = object.spacePtr.pack(1);
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.causedByPtr != null) {
          objectJson["causedBy"] = object.causedByPtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["client"] = object.clientPtr.pack(1);
        objectJson["clientNonce"] = object.clientNonce;
        objectJson["clientCreatedAt"] = object.clientCreatedAt.toString({ timeZoneName: 'never' });
        objectJson["clientEpoch"] = Number(object.clientEpoch);
        objectJson["status"] = EventStatus[object.status];
        objectJson["node"] = object.nodePtr.pack(1);
        objectJson["joinable"] = object.joinablePtr.pack(1);
        objectJson["member"] = object.memberPtr.pack(1);
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): InviteRescindedEvent {
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const causedByPtrValue = objectJson["causedBy"];
        const unpackedCausedByPtr = causedByPtrValue != undefined ? _NodeReference.unpack(1, causedByPtrValue, _session) as NodeReference : null;
        return new (NODE_CLASS_BY_TYPE[360103] as typeof InviteRescindedEvent)({
          node: _NodeReference.unpack(1, objectJson["node"], _session) as NodeReference,
          joinable: _NodeReference.unpack(1, objectJson["joinable"], _session) as NodeReference,
          member: _NodeReference.unpack(1, objectJson["member"], _session) as NodeReference,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          causedBy: unpackedCausedByPtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          client: _NodeReference.unpack(1, objectJson["client"], _session) as NodeReference,
          clientNonce: objectJson["clientNonce"],
          clientCreatedAt: Temporal.Instant.from(objectJson["clientCreatedAt"]).toZonedDateTimeISO('UTC'),
          clientEpoch: Number(objectJson["clientEpoch"]),
          status: EventStatus[objectJson["status"]] as any,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 360103)] = new InviteRescindedEventJsonEncoder();

    class InviteAcceptedEventJsonEncoder implements _JsonObjectEncoder {
      packObject(object: InviteAcceptedEvent): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "INVITE_ACCEPTED_EVENT";
        objectJson["id"] = object.id;
        objectJson["space"] = object.spacePtr.pack(1);
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.causedByPtr != null) {
          objectJson["causedBy"] = object.causedByPtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["client"] = object.clientPtr.pack(1);
        objectJson["clientNonce"] = object.clientNonce;
        objectJson["clientCreatedAt"] = object.clientCreatedAt.toString({ timeZoneName: 'never' });
        objectJson["clientEpoch"] = Number(object.clientEpoch);
        objectJson["status"] = EventStatus[object.status];
        objectJson["node"] = object.nodePtr.pack(1);
        objectJson["joinable"] = object.joinablePtr.pack(1);
        objectJson["member"] = object.memberPtr.pack(1);
        objectJson["role"] = object.rolePtr.pack(1);
        objectJson["roleType"] = RoleType[object.roleType];
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): InviteAcceptedEvent {
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const causedByPtrValue = objectJson["causedBy"];
        const unpackedCausedByPtr = causedByPtrValue != undefined ? _NodeReference.unpack(1, causedByPtrValue, _session) as NodeReference : null;
        return new (NODE_CLASS_BY_TYPE[360104] as typeof InviteAcceptedEvent)({
          role: _NodeReference.unpack(1, objectJson["role"], _session) as NodeReference,
          roleType: RoleType[objectJson["roleType"]] as any,
          node: _NodeReference.unpack(1, objectJson["node"], _session) as NodeReference,
          joinable: _NodeReference.unpack(1, objectJson["joinable"], _session) as NodeReference,
          member: _NodeReference.unpack(1, objectJson["member"], _session) as NodeReference,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          causedBy: unpackedCausedByPtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          client: _NodeReference.unpack(1, objectJson["client"], _session) as NodeReference,
          clientNonce: objectJson["clientNonce"],
          clientCreatedAt: Temporal.Instant.from(objectJson["clientCreatedAt"]).toZonedDateTimeISO('UTC'),
          clientEpoch: Number(objectJson["clientEpoch"]),
          status: EventStatus[objectJson["status"]] as any,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 360104)] = new InviteAcceptedEventJsonEncoder();

    class InviteRejectedEventJsonEncoder implements _JsonObjectEncoder {
      packObject(object: InviteRejectedEvent): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "INVITE_REJECTED_EVENT";
        objectJson["id"] = object.id;
        objectJson["space"] = object.spacePtr.pack(1);
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.causedByPtr != null) {
          objectJson["causedBy"] = object.causedByPtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["client"] = object.clientPtr.pack(1);
        objectJson["clientNonce"] = object.clientNonce;
        objectJson["clientCreatedAt"] = object.clientCreatedAt.toString({ timeZoneName: 'never' });
        objectJson["clientEpoch"] = Number(object.clientEpoch);
        objectJson["status"] = EventStatus[object.status];
        objectJson["node"] = object.nodePtr.pack(1);
        objectJson["joinable"] = object.joinablePtr.pack(1);
        objectJson["member"] = object.memberPtr.pack(1);
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): InviteRejectedEvent {
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const causedByPtrValue = objectJson["causedBy"];
        const unpackedCausedByPtr = causedByPtrValue != undefined ? _NodeReference.unpack(1, causedByPtrValue, _session) as NodeReference : null;
        return new (NODE_CLASS_BY_TYPE[360105] as typeof InviteRejectedEvent)({
          node: _NodeReference.unpack(1, objectJson["node"], _session) as NodeReference,
          joinable: _NodeReference.unpack(1, objectJson["joinable"], _session) as NodeReference,
          member: _NodeReference.unpack(1, objectJson["member"], _session) as NodeReference,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          causedBy: unpackedCausedByPtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          client: _NodeReference.unpack(1, objectJson["client"], _session) as NodeReference,
          clientNonce: objectJson["clientNonce"],
          clientCreatedAt: Temporal.Instant.from(objectJson["clientCreatedAt"]).toZonedDateTimeISO('UTC'),
          clientEpoch: Number(objectJson["clientEpoch"]),
          status: EventStatus[objectJson["status"]] as any,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 360105)] = new InviteRejectedEventJsonEncoder();

    class InviteJsonEncoder implements _JsonObjectEncoder {
      packObject(object: Invite): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "INVITE";
        objectJson["id"] = object.id;
        if (object.parentPtr != null) {
          objectJson["parent"] = object.parentPtr.pack(1);
        }
        objectJson["space"] = object.spacePtr.pack(1);
        objectJson["materialization"] = Materialization[object.materialization];
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.instancePtr != null) {
          objectJson["instance"] = object.instancePtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["updatedAt"] = object.updatedAt.toString({ timeZoneName: 'never' });
        objectJson["updatedEpoch"] = Number(object.updatedEpoch);
        objectJson["updatedBy"] = object.updatedByPtr.pack(1);
        if (object.deletedAt != null) {
          objectJson["deletedAt"] = object.deletedAt.toString({ timeZoneName: 'never' });
        }
        if (object._ownedByPtr != null) {
          objectJson["ownedBy"] = object._ownedByPtr.pack(1);
        }
        objectJson["name"] = object._name;
        objectJson["orderKey"] = object.orderKey;
        if (Object.keys(object._customValues).length > 0) {
          const packedCustomValues: { [key: string]: any } = {} as any;
          for (const [key, value] of Object.entries(object._customValues)) {
            packedCustomValues[String(key)] = value.pack(1);
          }
          objectJson["customValues"] = packedCustomValues;
        }
        if (object._scriptPtr != null) {
          objectJson["script"] = object._scriptPtr.pack(1);
        }
        if (object.isExtensible != null) {
          objectJson["isExtensible"] = object.isExtensible;
        }
        if (object.sourcePtr != null) {
          objectJson["source"] = object.sourcePtr.pack(1);
        }
        if (object._key != null) {
          objectJson["key"] = object._key;
        }
        objectJson["member"] = object._memberPtr.pack(1);
        if (object._rolePtr != null) {
          objectJson["role"] = object._rolePtr.pack(1);
        }
        if (object._roleType != null) {
          objectJson["roleType"] = RoleType[object._roleType];
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): Invite {
        const _Value = STRUCT_CLASS_BY_TYPE[100] as typeof Value;
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const rolePtrValue = objectJson["role"];
        const unpackedRolePtr = rolePtrValue != undefined ? _NodeReference.unpack(1, rolePtrValue, _session) as NodeReference : null;
        const roleTypeValue = objectJson["roleType"];
        const unpackedRoleType = roleTypeValue != undefined ? RoleType[roleTypeValue] as any : null;
        const parentPtrValue = objectJson["parent"];
        const unpackedParentPtr = parentPtrValue != undefined ? _NodeReference.unpack(1, parentPtrValue, _session) as NodeReference : null;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const instancePtrValue = objectJson["instance"];
        const unpackedInstancePtr = instancePtrValue != undefined ? _NodeReference.unpack(1, instancePtrValue, _session) as NodeReference : null;
        const deletedAtValue = objectJson["deletedAt"];
        const unpackedDeletedAt = deletedAtValue != undefined ? Temporal.Instant.from(deletedAtValue).toZonedDateTimeISO('UTC') : null;
        const ownedByPtrValue = objectJson["ownedBy"];
        const unpackedOwnedByPtr = ownedByPtrValue != undefined ? _NodeReference.unpack(1, ownedByPtrValue, _session) as NodeReference : null;
        const unpackedCustomValues = {} as any;
        if (objectJson["customValues"] != undefined) {
          for (const [key, value] of Object.entries(objectJson["customValues"])) {
            unpackedCustomValues[key] = _Value.unpack(1, value as any, _session) as Value;
          }
        }
        const scriptPtrValue = objectJson["script"];
        const unpackedScriptPtr = scriptPtrValue != undefined ? _NodeReference.unpack(1, scriptPtrValue, _session) as NodeReference : null;
        const isExtensibleValue = objectJson["isExtensible"];
        const unpackedIsExtensible = isExtensibleValue != undefined ? isExtensibleValue : null;
        const sourcePtrValue = objectJson["source"];
        const unpackedSourcePtr = sourcePtrValue != undefined ? _NodeReference.unpack(1, sourcePtrValue, _session) as NodeReference : null;
        const keyValue = objectJson["key"];
        const unpackedKey = keyValue != undefined ? keyValue : null;
        return new (NODE_CLASS_BY_TYPE[360100] as typeof Invite)({
          member: _NodeReference.unpack(1, objectJson["member"], _session) as NodeReference,
          role: unpackedRolePtr,
          roleType: unpackedRoleType,
          parent: unpackedParentPtr,
          materialization: Materialization[objectJson["materialization"]] as any,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          instance: unpackedInstancePtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          updatedAt: Temporal.Instant.from(objectJson["updatedAt"]).toZonedDateTimeISO('UTC'),
          updatedEpoch: Number(objectJson["updatedEpoch"]),
          updatedBy: _NodeReference.unpack(1, objectJson["updatedBy"], _session) as NodeReference,
          deletedAt: unpackedDeletedAt,
          ownedBy: unpackedOwnedByPtr,
          name: objectJson["name"],
          orderKey: objectJson["orderKey"],
          customValues: unpackedCustomValues,
          script: unpackedScriptPtr,
          isExtensible: unpackedIsExtensible,
          source: unpackedSourcePtr,
          key: unpackedKey,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 360100)] = new InviteJsonEncoder();

    class MembershipJoinedEventJsonEncoder implements _JsonObjectEncoder {
      packObject(object: MembershipJoinedEvent): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "MEMBERSHIP_JOINED_EVENT";
        objectJson["id"] = object.id;
        objectJson["space"] = object.spacePtr.pack(1);
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.causedByPtr != null) {
          objectJson["causedBy"] = object.causedByPtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["client"] = object.clientPtr.pack(1);
        objectJson["clientNonce"] = object.clientNonce;
        objectJson["clientCreatedAt"] = object.clientCreatedAt.toString({ timeZoneName: 'never' });
        objectJson["clientEpoch"] = Number(object.clientEpoch);
        objectJson["status"] = EventStatus[object.status];
        objectJson["node"] = object.nodePtr.pack(1);
        objectJson["joinable"] = object.joinablePtr.pack(1);
        objectJson["member"] = object.memberPtr.pack(1);
        objectJson["role"] = object.rolePtr.pack(1);
        objectJson["roleType"] = RoleType[object.roleType];
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): MembershipJoinedEvent {
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const causedByPtrValue = objectJson["causedBy"];
        const unpackedCausedByPtr = causedByPtrValue != undefined ? _NodeReference.unpack(1, causedByPtrValue, _session) as NodeReference : null;
        return new (NODE_CLASS_BY_TYPE[360002] as typeof MembershipJoinedEvent)({
          role: _NodeReference.unpack(1, objectJson["role"], _session) as NodeReference,
          roleType: RoleType[objectJson["roleType"]] as any,
          node: _NodeReference.unpack(1, objectJson["node"], _session) as NodeReference,
          joinable: _NodeReference.unpack(1, objectJson["joinable"], _session) as NodeReference,
          member: _NodeReference.unpack(1, objectJson["member"], _session) as NodeReference,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          causedBy: unpackedCausedByPtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          client: _NodeReference.unpack(1, objectJson["client"], _session) as NodeReference,
          clientNonce: objectJson["clientNonce"],
          clientCreatedAt: Temporal.Instant.from(objectJson["clientCreatedAt"]).toZonedDateTimeISO('UTC'),
          clientEpoch: Number(objectJson["clientEpoch"]),
          status: EventStatus[objectJson["status"]] as any,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 360002)] = new MembershipJoinedEventJsonEncoder();

    class MembershipLeftEventJsonEncoder implements _JsonObjectEncoder {
      packObject(object: MembershipLeftEvent): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "MEMBERSHIP_LEFT_EVENT";
        objectJson["id"] = object.id;
        objectJson["space"] = object.spacePtr.pack(1);
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.causedByPtr != null) {
          objectJson["causedBy"] = object.causedByPtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["client"] = object.clientPtr.pack(1);
        objectJson["clientNonce"] = object.clientNonce;
        objectJson["clientCreatedAt"] = object.clientCreatedAt.toString({ timeZoneName: 'never' });
        objectJson["clientEpoch"] = Number(object.clientEpoch);
        objectJson["status"] = EventStatus[object.status];
        objectJson["node"] = object.nodePtr.pack(1);
        objectJson["joinable"] = object.joinablePtr.pack(1);
        objectJson["member"] = object.memberPtr.pack(1);
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): MembershipLeftEvent {
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const causedByPtrValue = objectJson["causedBy"];
        const unpackedCausedByPtr = causedByPtrValue != undefined ? _NodeReference.unpack(1, causedByPtrValue, _session) as NodeReference : null;
        return new (NODE_CLASS_BY_TYPE[360003] as typeof MembershipLeftEvent)({
          node: _NodeReference.unpack(1, objectJson["node"], _session) as NodeReference,
          joinable: _NodeReference.unpack(1, objectJson["joinable"], _session) as NodeReference,
          member: _NodeReference.unpack(1, objectJson["member"], _session) as NodeReference,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          causedBy: unpackedCausedByPtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          client: _NodeReference.unpack(1, objectJson["client"], _session) as NodeReference,
          clientNonce: objectJson["clientNonce"],
          clientCreatedAt: Temporal.Instant.from(objectJson["clientCreatedAt"]).toZonedDateTimeISO('UTC'),
          clientEpoch: Number(objectJson["clientEpoch"]),
          status: EventStatus[objectJson["status"]] as any,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 360003)] = new MembershipLeftEventJsonEncoder();

    class MembershipJsonEncoder implements _JsonObjectEncoder {
      packObject(object: Membership): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "MEMBERSHIP";
        objectJson["id"] = object.id;
        if (object.parentPtr != null) {
          objectJson["parent"] = object.parentPtr.pack(1);
        }
        objectJson["space"] = object.spacePtr.pack(1);
        objectJson["materialization"] = Materialization[object.materialization];
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.instancePtr != null) {
          objectJson["instance"] = object.instancePtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["updatedAt"] = object.updatedAt.toString({ timeZoneName: 'never' });
        objectJson["updatedEpoch"] = Number(object.updatedEpoch);
        objectJson["updatedBy"] = object.updatedByPtr.pack(1);
        if (object.deletedAt != null) {
          objectJson["deletedAt"] = object.deletedAt.toString({ timeZoneName: 'never' });
        }
        if (object._ownedByPtr != null) {
          objectJson["ownedBy"] = object._ownedByPtr.pack(1);
        }
        objectJson["name"] = object._name;
        objectJson["orderKey"] = object.orderKey;
        if (Object.keys(object._customValues).length > 0) {
          const packedCustomValues: { [key: string]: any } = {} as any;
          for (const [key, value] of Object.entries(object._customValues)) {
            packedCustomValues[String(key)] = value.pack(1);
          }
          objectJson["customValues"] = packedCustomValues;
        }
        if (object._scriptPtr != null) {
          objectJson["script"] = object._scriptPtr.pack(1);
        }
        if (object.isExtensible != null) {
          objectJson["isExtensible"] = object.isExtensible;
        }
        if (object.sourcePtr != null) {
          objectJson["source"] = object.sourcePtr.pack(1);
        }
        if (object._key != null) {
          objectJson["key"] = object._key;
        }
        objectJson["member"] = object._memberPtr.pack(1);
        if (object._rolePtr != null) {
          objectJson["role"] = object._rolePtr.pack(1);
        }
        if (object._roleType != null) {
          objectJson["roleType"] = RoleType[object._roleType];
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): Membership {
        const _Value = STRUCT_CLASS_BY_TYPE[100] as typeof Value;
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const rolePtrValue = objectJson["role"];
        const unpackedRolePtr = rolePtrValue != undefined ? _NodeReference.unpack(1, rolePtrValue, _session) as NodeReference : null;
        const roleTypeValue = objectJson["roleType"];
        const unpackedRoleType = roleTypeValue != undefined ? RoleType[roleTypeValue] as any : null;
        const parentPtrValue = objectJson["parent"];
        const unpackedParentPtr = parentPtrValue != undefined ? _NodeReference.unpack(1, parentPtrValue, _session) as NodeReference : null;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const instancePtrValue = objectJson["instance"];
        const unpackedInstancePtr = instancePtrValue != undefined ? _NodeReference.unpack(1, instancePtrValue, _session) as NodeReference : null;
        const deletedAtValue = objectJson["deletedAt"];
        const unpackedDeletedAt = deletedAtValue != undefined ? Temporal.Instant.from(deletedAtValue).toZonedDateTimeISO('UTC') : null;
        const ownedByPtrValue = objectJson["ownedBy"];
        const unpackedOwnedByPtr = ownedByPtrValue != undefined ? _NodeReference.unpack(1, ownedByPtrValue, _session) as NodeReference : null;
        const unpackedCustomValues = {} as any;
        if (objectJson["customValues"] != undefined) {
          for (const [key, value] of Object.entries(objectJson["customValues"])) {
            unpackedCustomValues[key] = _Value.unpack(1, value as any, _session) as Value;
          }
        }
        const scriptPtrValue = objectJson["script"];
        const unpackedScriptPtr = scriptPtrValue != undefined ? _NodeReference.unpack(1, scriptPtrValue, _session) as NodeReference : null;
        const isExtensibleValue = objectJson["isExtensible"];
        const unpackedIsExtensible = isExtensibleValue != undefined ? isExtensibleValue : null;
        const sourcePtrValue = objectJson["source"];
        const unpackedSourcePtr = sourcePtrValue != undefined ? _NodeReference.unpack(1, sourcePtrValue, _session) as NodeReference : null;
        const keyValue = objectJson["key"];
        const unpackedKey = keyValue != undefined ? keyValue : null;
        return new (NODE_CLASS_BY_TYPE[360000] as typeof Membership)({
          member: _NodeReference.unpack(1, objectJson["member"], _session) as NodeReference,
          role: unpackedRolePtr,
          roleType: unpackedRoleType,
          parent: unpackedParentPtr,
          materialization: Materialization[objectJson["materialization"]] as any,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          instance: unpackedInstancePtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          updatedAt: Temporal.Instant.from(objectJson["updatedAt"]).toZonedDateTimeISO('UTC'),
          updatedEpoch: Number(objectJson["updatedEpoch"]),
          updatedBy: _NodeReference.unpack(1, objectJson["updatedBy"], _session) as NodeReference,
          deletedAt: unpackedDeletedAt,
          ownedBy: unpackedOwnedByPtr,
          name: objectJson["name"],
          orderKey: objectJson["orderKey"],
          customValues: unpackedCustomValues,
          script: unpackedScriptPtr,
          isExtensible: unpackedIsExtensible,
          source: unpackedSourcePtr,
          key: unpackedKey,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 360000)] = new MembershipJsonEncoder();

    class RoleAssignedEventJsonEncoder implements _JsonObjectEncoder {
      packObject(object: RoleAssignedEvent): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "ROLE_ASSIGNED_EVENT";
        objectJson["id"] = object.id;
        objectJson["space"] = object.spacePtr.pack(1);
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.causedByPtr != null) {
          objectJson["causedBy"] = object.causedByPtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["client"] = object.clientPtr.pack(1);
        objectJson["clientNonce"] = object.clientNonce;
        objectJson["clientCreatedAt"] = object.clientCreatedAt.toString({ timeZoneName: 'never' });
        objectJson["clientEpoch"] = Number(object.clientEpoch);
        objectJson["status"] = EventStatus[object.status];
        objectJson["node"] = object.nodePtr.pack(1);
        objectJson["actor"] = object.actorPtr.pack(1);
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): RoleAssignedEvent {
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const causedByPtrValue = objectJson["causedBy"];
        const unpackedCausedByPtr = causedByPtrValue != undefined ? _NodeReference.unpack(1, causedByPtrValue, _session) as NodeReference : null;
        return new (NODE_CLASS_BY_TYPE[360202] as typeof RoleAssignedEvent)({
          node: _NodeReference.unpack(1, objectJson["node"], _session) as NodeReference,
          actor: _NodeReference.unpack(1, objectJson["actor"], _session) as NodeReference,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          causedBy: unpackedCausedByPtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          client: _NodeReference.unpack(1, objectJson["client"], _session) as NodeReference,
          clientNonce: objectJson["clientNonce"],
          clientCreatedAt: Temporal.Instant.from(objectJson["clientCreatedAt"]).toZonedDateTimeISO('UTC'),
          clientEpoch: Number(objectJson["clientEpoch"]),
          status: EventStatus[objectJson["status"]] as any,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 360202)] = new RoleAssignedEventJsonEncoder();

    class RoleUnassignedEventJsonEncoder implements _JsonObjectEncoder {
      packObject(object: RoleUnassignedEvent): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "ROLE_UNASSIGNED_EVENT";
        objectJson["id"] = object.id;
        objectJson["space"] = object.spacePtr.pack(1);
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.causedByPtr != null) {
          objectJson["causedBy"] = object.causedByPtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["client"] = object.clientPtr.pack(1);
        objectJson["clientNonce"] = object.clientNonce;
        objectJson["clientCreatedAt"] = object.clientCreatedAt.toString({ timeZoneName: 'never' });
        objectJson["clientEpoch"] = Number(object.clientEpoch);
        objectJson["status"] = EventStatus[object.status];
        objectJson["node"] = object.nodePtr.pack(1);
        objectJson["actor"] = object.actorPtr.pack(1);
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): RoleUnassignedEvent {
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const causedByPtrValue = objectJson["causedBy"];
        const unpackedCausedByPtr = causedByPtrValue != undefined ? _NodeReference.unpack(1, causedByPtrValue, _session) as NodeReference : null;
        return new (NODE_CLASS_BY_TYPE[360203] as typeof RoleUnassignedEvent)({
          node: _NodeReference.unpack(1, objectJson["node"], _session) as NodeReference,
          actor: _NodeReference.unpack(1, objectJson["actor"], _session) as NodeReference,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          causedBy: unpackedCausedByPtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          client: _NodeReference.unpack(1, objectJson["client"], _session) as NodeReference,
          clientNonce: objectJson["clientNonce"],
          clientCreatedAt: Temporal.Instant.from(objectJson["clientCreatedAt"]).toZonedDateTimeISO('UTC'),
          clientEpoch: Number(objectJson["clientEpoch"]),
          status: EventStatus[objectJson["status"]] as any,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 360203)] = new RoleUnassignedEventJsonEncoder();

    class RoleJsonEncoder implements _JsonObjectEncoder {
      packObject(object: Role): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "ROLE";
        objectJson["id"] = object.id;
        if (object.parentPtr != null) {
          objectJson["parent"] = object.parentPtr.pack(1);
        }
        objectJson["space"] = object.spacePtr.pack(1);
        objectJson["materialization"] = Materialization[object.materialization];
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.instancePtr != null) {
          objectJson["instance"] = object.instancePtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["updatedAt"] = object.updatedAt.toString({ timeZoneName: 'never' });
        objectJson["updatedEpoch"] = Number(object.updatedEpoch);
        objectJson["updatedBy"] = object.updatedByPtr.pack(1);
        if (object.deletedAt != null) {
          objectJson["deletedAt"] = object.deletedAt.toString({ timeZoneName: 'never' });
        }
        if (object._ownedByPtr != null) {
          objectJson["ownedBy"] = object._ownedByPtr.pack(1);
        }
        objectJson["name"] = object._name;
        objectJson["orderKey"] = object.orderKey;
        if (Object.keys(object._customValues).length > 0) {
          const packedCustomValues: { [key: string]: any } = {} as any;
          for (const [key, value] of Object.entries(object._customValues)) {
            packedCustomValues[String(key)] = value.pack(1);
          }
          objectJson["customValues"] = packedCustomValues;
        }
        if (object._scriptPtr != null) {
          objectJson["script"] = object._scriptPtr.pack(1);
        }
        if (object.isExtensible != null) {
          objectJson["isExtensible"] = object.isExtensible;
        }
        if (object.sourcePtr != null) {
          objectJson["source"] = object.sourcePtr.pack(1);
        }
        if (object._key != null) {
          objectJson["key"] = object._key;
        }
        objectJson["type"] = RoleType[object._type];
        if (object._icon != null) {
          objectJson["icon"] = object._icon.pack(1);
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): Role {
        const _Value = STRUCT_CLASS_BY_TYPE[100] as typeof Value;
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const _Icon = STRUCT_CLASS_BY_TYPE[400031] as typeof Icon;
        const iconValue = objectJson["icon"];
        const unpackedIcon = iconValue != undefined ? _Icon.unpack(1, iconValue, _session) as Icon : null;
        const parentPtrValue = objectJson["parent"];
        const unpackedParentPtr = parentPtrValue != undefined ? _NodeReference.unpack(1, parentPtrValue, _session) as NodeReference : null;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const instancePtrValue = objectJson["instance"];
        const unpackedInstancePtr = instancePtrValue != undefined ? _NodeReference.unpack(1, instancePtrValue, _session) as NodeReference : null;
        const deletedAtValue = objectJson["deletedAt"];
        const unpackedDeletedAt = deletedAtValue != undefined ? Temporal.Instant.from(deletedAtValue).toZonedDateTimeISO('UTC') : null;
        const ownedByPtrValue = objectJson["ownedBy"];
        const unpackedOwnedByPtr = ownedByPtrValue != undefined ? _NodeReference.unpack(1, ownedByPtrValue, _session) as NodeReference : null;
        const unpackedCustomValues = {} as any;
        if (objectJson["customValues"] != undefined) {
          for (const [key, value] of Object.entries(objectJson["customValues"])) {
            unpackedCustomValues[key] = _Value.unpack(1, value as any, _session) as Value;
          }
        }
        const scriptPtrValue = objectJson["script"];
        const unpackedScriptPtr = scriptPtrValue != undefined ? _NodeReference.unpack(1, scriptPtrValue, _session) as NodeReference : null;
        const isExtensibleValue = objectJson["isExtensible"];
        const unpackedIsExtensible = isExtensibleValue != undefined ? isExtensibleValue : null;
        const sourcePtrValue = objectJson["source"];
        const unpackedSourcePtr = sourcePtrValue != undefined ? _NodeReference.unpack(1, sourcePtrValue, _session) as NodeReference : null;
        const keyValue = objectJson["key"];
        const unpackedKey = keyValue != undefined ? keyValue : null;
        return new (NODE_CLASS_BY_TYPE[360200] as typeof Role)({
          type: RoleType[objectJson["type"]] as any,
          icon: unpackedIcon,
          parent: unpackedParentPtr,
          materialization: Materialization[objectJson["materialization"]] as any,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          instance: unpackedInstancePtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          updatedAt: Temporal.Instant.from(objectJson["updatedAt"]).toZonedDateTimeISO('UTC'),
          updatedEpoch: Number(objectJson["updatedEpoch"]),
          updatedBy: _NodeReference.unpack(1, objectJson["updatedBy"], _session) as NodeReference,
          deletedAt: unpackedDeletedAt,
          ownedBy: unpackedOwnedByPtr,
          name: objectJson["name"],
          orderKey: objectJson["orderKey"],
          customValues: unpackedCustomValues,
          script: unpackedScriptPtr,
          isExtensible: unpackedIsExtensible,
          source: unpackedSourcePtr,
          key: unpackedKey,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 360200)] = new RoleJsonEncoder();

    class SanctionRequestedEventJsonEncoder implements _JsonObjectEncoder {
      packObject(object: SanctionRequestedEvent): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "SANCTION_REQUESTED_EVENT";
        objectJson["id"] = object.id;
        objectJson["space"] = object.spacePtr.pack(1);
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.causedByPtr != null) {
          objectJson["causedBy"] = object.causedByPtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["client"] = object.clientPtr.pack(1);
        objectJson["clientNonce"] = object.clientNonce;
        objectJson["clientCreatedAt"] = object.clientCreatedAt.toString({ timeZoneName: 'never' });
        objectJson["clientEpoch"] = Number(object.clientEpoch);
        objectJson["status"] = EventStatus[object.status];
        objectJson["node"] = object.nodePtr.pack(1);
        objectJson["target"] = object.targetPtr.pack(1);
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): SanctionRequestedEvent {
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const causedByPtrValue = objectJson["causedBy"];
        const unpackedCausedByPtr = causedByPtrValue != undefined ? _NodeReference.unpack(1, causedByPtrValue, _session) as NodeReference : null;
        return new (NODE_CLASS_BY_TYPE[360402] as typeof SanctionRequestedEvent)({
          node: _NodeReference.unpack(1, objectJson["node"], _session) as NodeReference,
          target: _NodeReference.unpack(1, objectJson["target"], _session) as NodeReference,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          causedBy: unpackedCausedByPtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          client: _NodeReference.unpack(1, objectJson["client"], _session) as NodeReference,
          clientNonce: objectJson["clientNonce"],
          clientCreatedAt: Temporal.Instant.from(objectJson["clientCreatedAt"]).toZonedDateTimeISO('UTC'),
          clientEpoch: Number(objectJson["clientEpoch"]),
          status: EventStatus[objectJson["status"]] as any,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 360402)] = new SanctionRequestedEventJsonEncoder();

    class SanctionGrantedEventJsonEncoder implements _JsonObjectEncoder {
      packObject(object: SanctionGrantedEvent): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "SANCTION_GRANTED_EVENT";
        objectJson["id"] = object.id;
        objectJson["space"] = object.spacePtr.pack(1);
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.causedByPtr != null) {
          objectJson["causedBy"] = object.causedByPtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["client"] = object.clientPtr.pack(1);
        objectJson["clientNonce"] = object.clientNonce;
        objectJson["clientCreatedAt"] = object.clientCreatedAt.toString({ timeZoneName: 'never' });
        objectJson["clientEpoch"] = Number(object.clientEpoch);
        objectJson["status"] = EventStatus[object.status];
        objectJson["node"] = object.nodePtr.pack(1);
        objectJson["target"] = object.targetPtr.pack(1);
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): SanctionGrantedEvent {
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const causedByPtrValue = objectJson["causedBy"];
        const unpackedCausedByPtr = causedByPtrValue != undefined ? _NodeReference.unpack(1, causedByPtrValue, _session) as NodeReference : null;
        return new (NODE_CLASS_BY_TYPE[360403] as typeof SanctionGrantedEvent)({
          node: _NodeReference.unpack(1, objectJson["node"], _session) as NodeReference,
          target: _NodeReference.unpack(1, objectJson["target"], _session) as NodeReference,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          causedBy: unpackedCausedByPtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          client: _NodeReference.unpack(1, objectJson["client"], _session) as NodeReference,
          clientNonce: objectJson["clientNonce"],
          clientCreatedAt: Temporal.Instant.from(objectJson["clientCreatedAt"]).toZonedDateTimeISO('UTC'),
          clientEpoch: Number(objectJson["clientEpoch"]),
          status: EventStatus[objectJson["status"]] as any,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 360403)] = new SanctionGrantedEventJsonEncoder();

    class SanctionRevokedEventJsonEncoder implements _JsonObjectEncoder {
      packObject(object: SanctionRevokedEvent): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "SANCTION_REVOKED_EVENT";
        objectJson["id"] = object.id;
        objectJson["space"] = object.spacePtr.pack(1);
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.causedByPtr != null) {
          objectJson["causedBy"] = object.causedByPtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["client"] = object.clientPtr.pack(1);
        objectJson["clientNonce"] = object.clientNonce;
        objectJson["clientCreatedAt"] = object.clientCreatedAt.toString({ timeZoneName: 'never' });
        objectJson["clientEpoch"] = Number(object.clientEpoch);
        objectJson["status"] = EventStatus[object.status];
        objectJson["node"] = object.nodePtr.pack(1);
        objectJson["target"] = object.targetPtr.pack(1);
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): SanctionRevokedEvent {
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const causedByPtrValue = objectJson["causedBy"];
        const unpackedCausedByPtr = causedByPtrValue != undefined ? _NodeReference.unpack(1, causedByPtrValue, _session) as NodeReference : null;
        return new (NODE_CLASS_BY_TYPE[360404] as typeof SanctionRevokedEvent)({
          node: _NodeReference.unpack(1, objectJson["node"], _session) as NodeReference,
          target: _NodeReference.unpack(1, objectJson["target"], _session) as NodeReference,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          causedBy: unpackedCausedByPtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          client: _NodeReference.unpack(1, objectJson["client"], _session) as NodeReference,
          clientNonce: objectJson["clientNonce"],
          clientCreatedAt: Temporal.Instant.from(objectJson["clientCreatedAt"]).toZonedDateTimeISO('UTC'),
          clientEpoch: Number(objectJson["clientEpoch"]),
          status: EventStatus[objectJson["status"]] as any,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 360404)] = new SanctionRevokedEventJsonEncoder();

    class SanctionExpiredEventJsonEncoder implements _JsonObjectEncoder {
      packObject(object: SanctionExpiredEvent): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "SANCTION_EXPIRED_EVENT";
        objectJson["id"] = object.id;
        objectJson["space"] = object.spacePtr.pack(1);
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.causedByPtr != null) {
          objectJson["causedBy"] = object.causedByPtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["client"] = object.clientPtr.pack(1);
        objectJson["clientNonce"] = object.clientNonce;
        objectJson["clientCreatedAt"] = object.clientCreatedAt.toString({ timeZoneName: 'never' });
        objectJson["clientEpoch"] = Number(object.clientEpoch);
        objectJson["status"] = EventStatus[object.status];
        objectJson["node"] = object.nodePtr.pack(1);
        objectJson["target"] = object.targetPtr.pack(1);
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): SanctionExpiredEvent {
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const causedByPtrValue = objectJson["causedBy"];
        const unpackedCausedByPtr = causedByPtrValue != undefined ? _NodeReference.unpack(1, causedByPtrValue, _session) as NodeReference : null;
        return new (NODE_CLASS_BY_TYPE[360405] as typeof SanctionExpiredEvent)({
          node: _NodeReference.unpack(1, objectJson["node"], _session) as NodeReference,
          target: _NodeReference.unpack(1, objectJson["target"], _session) as NodeReference,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          causedBy: unpackedCausedByPtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          client: _NodeReference.unpack(1, objectJson["client"], _session) as NodeReference,
          clientNonce: objectJson["clientNonce"],
          clientCreatedAt: Temporal.Instant.from(objectJson["clientCreatedAt"]).toZonedDateTimeISO('UTC'),
          clientEpoch: Number(objectJson["clientEpoch"]),
          status: EventStatus[objectJson["status"]] as any,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 360405)] = new SanctionExpiredEventJsonEncoder();

    class SanctionJsonEncoder implements _JsonObjectEncoder {
      packObject(object: Sanction): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "SANCTION";
        objectJson["id"] = object.id;
        if (object.parentPtr != null) {
          objectJson["parent"] = object.parentPtr.pack(1);
        }
        objectJson["space"] = object.spacePtr.pack(1);
        objectJson["materialization"] = Materialization[object.materialization];
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.instancePtr != null) {
          objectJson["instance"] = object.instancePtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["updatedAt"] = object.updatedAt.toString({ timeZoneName: 'never' });
        objectJson["updatedEpoch"] = Number(object.updatedEpoch);
        objectJson["updatedBy"] = object.updatedByPtr.pack(1);
        if (object.deletedAt != null) {
          objectJson["deletedAt"] = object.deletedAt.toString({ timeZoneName: 'never' });
        }
        if (object._ownedByPtr != null) {
          objectJson["ownedBy"] = object._ownedByPtr.pack(1);
        }
        objectJson["name"] = object._name;
        objectJson["orderKey"] = object.orderKey;
        if (Object.keys(object._customValues).length > 0) {
          const packedCustomValues: { [key: string]: any } = {} as any;
          for (const [key, value] of Object.entries(object._customValues)) {
            packedCustomValues[String(key)] = value.pack(1);
          }
          objectJson["customValues"] = packedCustomValues;
        }
        if (object._scriptPtr != null) {
          objectJson["script"] = object._scriptPtr.pack(1);
        }
        if (object.isExtensible != null) {
          objectJson["isExtensible"] = object.isExtensible;
        }
        if (object.sourcePtr != null) {
          objectJson["source"] = object.sourcePtr.pack(1);
        }
        if (object._key != null) {
          objectJson["key"] = object._key;
        }
        objectJson["type"] = SanctionType[object._type];
        if (object._expiresAt != null) {
          objectJson["expiresAt"] = object._expiresAt.toString({ timeZoneName: 'never' });
        }
        objectJson["target"] = object._targetPtr.pack(1);
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): Sanction {
        const _Value = STRUCT_CLASS_BY_TYPE[100] as typeof Value;
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const expiresAtValue = objectJson["expiresAt"];
        const unpackedExpiresAt = expiresAtValue != undefined ? Temporal.Instant.from(expiresAtValue).toZonedDateTimeISO('UTC') : null;
        const parentPtrValue = objectJson["parent"];
        const unpackedParentPtr = parentPtrValue != undefined ? _NodeReference.unpack(1, parentPtrValue, _session) as NodeReference : null;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const instancePtrValue = objectJson["instance"];
        const unpackedInstancePtr = instancePtrValue != undefined ? _NodeReference.unpack(1, instancePtrValue, _session) as NodeReference : null;
        const deletedAtValue = objectJson["deletedAt"];
        const unpackedDeletedAt = deletedAtValue != undefined ? Temporal.Instant.from(deletedAtValue).toZonedDateTimeISO('UTC') : null;
        const ownedByPtrValue = objectJson["ownedBy"];
        const unpackedOwnedByPtr = ownedByPtrValue != undefined ? _NodeReference.unpack(1, ownedByPtrValue, _session) as NodeReference : null;
        const unpackedCustomValues = {} as any;
        if (objectJson["customValues"] != undefined) {
          for (const [key, value] of Object.entries(objectJson["customValues"])) {
            unpackedCustomValues[key] = _Value.unpack(1, value as any, _session) as Value;
          }
        }
        const scriptPtrValue = objectJson["script"];
        const unpackedScriptPtr = scriptPtrValue != undefined ? _NodeReference.unpack(1, scriptPtrValue, _session) as NodeReference : null;
        const isExtensibleValue = objectJson["isExtensible"];
        const unpackedIsExtensible = isExtensibleValue != undefined ? isExtensibleValue : null;
        const sourcePtrValue = objectJson["source"];
        const unpackedSourcePtr = sourcePtrValue != undefined ? _NodeReference.unpack(1, sourcePtrValue, _session) as NodeReference : null;
        const keyValue = objectJson["key"];
        const unpackedKey = keyValue != undefined ? keyValue : null;
        return new (NODE_CLASS_BY_TYPE[360400] as typeof Sanction)({
          type: SanctionType[objectJson["type"]] as any,
          expiresAt: unpackedExpiresAt,
          target: _NodeReference.unpack(1, objectJson["target"], _session) as NodeReference,
          parent: unpackedParentPtr,
          materialization: Materialization[objectJson["materialization"]] as any,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          instance: unpackedInstancePtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          updatedAt: Temporal.Instant.from(objectJson["updatedAt"]).toZonedDateTimeISO('UTC'),
          updatedEpoch: Number(objectJson["updatedEpoch"]),
          updatedBy: _NodeReference.unpack(1, objectJson["updatedBy"], _session) as NodeReference,
          deletedAt: unpackedDeletedAt,
          ownedBy: unpackedOwnedByPtr,
          name: objectJson["name"],
          orderKey: objectJson["orderKey"],
          customValues: unpackedCustomValues,
          script: unpackedScriptPtr,
          isExtensible: unpackedIsExtensible,
          source: unpackedSourcePtr,
          key: unpackedKey,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 360400)] = new SanctionJsonEncoder();

    class ColorStyleJsonEncoder implements _JsonObjectEncoder {
      packObject(object: ColorStyle): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "COLOR_STYLE";
        objectJson["id"] = object.id;
        if (object.parentPtr != null) {
          objectJson["parent"] = object.parentPtr.pack(1);
        }
        objectJson["space"] = object.spacePtr.pack(1);
        objectJson["materialization"] = Materialization[object.materialization];
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.instancePtr != null) {
          objectJson["instance"] = object.instancePtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["updatedAt"] = object.updatedAt.toString({ timeZoneName: 'never' });
        objectJson["updatedEpoch"] = Number(object.updatedEpoch);
        objectJson["updatedBy"] = object.updatedByPtr.pack(1);
        if (object.deletedAt != null) {
          objectJson["deletedAt"] = object.deletedAt.toString({ timeZoneName: 'never' });
        }
        if (object._ownedByPtr != null) {
          objectJson["ownedBy"] = object._ownedByPtr.pack(1);
        }
        objectJson["name"] = object._name;
        objectJson["orderKey"] = object.orderKey;
        if (Object.keys(object._customValues).length > 0) {
          const packedCustomValues: { [key: string]: any } = {} as any;
          for (const [key, value] of Object.entries(object._customValues)) {
            packedCustomValues[String(key)] = value.pack(1);
          }
          objectJson["customValues"] = packedCustomValues;
        }
        if (object._scriptPtr != null) {
          objectJson["script"] = object._scriptPtr.pack(1);
        }
        if (object.isExtensible != null) {
          objectJson["isExtensible"] = object.isExtensible;
        }
        if (object.sourcePtr != null) {
          objectJson["source"] = object.sourcePtr.pack(1);
        }
        if (object._key != null) {
          objectJson["key"] = object._key;
        }
        objectJson["type"] = ColorType[object._type];
        if (object._hue != null) {
          objectJson["hue"] = ColorHue[object._hue];
        }
        if (object._shade != null) {
          objectJson["shade"] = ColorShade[object._shade];
        }
        if (object._intent != null) {
          objectJson["intent"] = ColorIntent[object._intent];
        }
        if (object._x != null) {
          objectJson["x"] = Number(object._x);
        }
        if (object._y != null) {
          objectJson["y"] = Number(object._y);
        }
        if (object._z != null) {
          objectJson["z"] = Number(object._z);
        }
        if (object._alpha != null) {
          objectJson["alpha"] = Number(object._alpha);
        }
        if (object._dark != null) {
          objectJson["dark"] = object._dark.pack(1);
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): ColorStyle {
        const _Value = STRUCT_CLASS_BY_TYPE[100] as typeof Value;
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const _Color = STRUCT_CLASS_BY_TYPE[2100300] as typeof Color;
        const hueValue = objectJson["hue"];
        const unpackedHue = hueValue != undefined ? ColorHue[hueValue] as any : null;
        const shadeValue = objectJson["shade"];
        const unpackedShade = shadeValue != undefined ? ColorShade[shadeValue] as any : null;
        const intentValue = objectJson["intent"];
        const unpackedIntent = intentValue != undefined ? ColorIntent[intentValue] as any : null;
        const xValue = objectJson["x"];
        const unpackedX = xValue != undefined ? Number(xValue) : null;
        const yValue = objectJson["y"];
        const unpackedY = yValue != undefined ? Number(yValue) : null;
        const zValue = objectJson["z"];
        const unpackedZ = zValue != undefined ? Number(zValue) : null;
        const alphaValue = objectJson["alpha"];
        const unpackedAlpha = alphaValue != undefined ? Number(alphaValue) : null;
        const darkValue = objectJson["dark"];
        const unpackedDark = darkValue != undefined ? _Color.unpack(1, darkValue, _session) as Color : null;
        const parentPtrValue = objectJson["parent"];
        const unpackedParentPtr = parentPtrValue != undefined ? _NodeReference.unpack(1, parentPtrValue, _session) as NodeReference : null;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const instancePtrValue = objectJson["instance"];
        const unpackedInstancePtr = instancePtrValue != undefined ? _NodeReference.unpack(1, instancePtrValue, _session) as NodeReference : null;
        const deletedAtValue = objectJson["deletedAt"];
        const unpackedDeletedAt = deletedAtValue != undefined ? Temporal.Instant.from(deletedAtValue).toZonedDateTimeISO('UTC') : null;
        const ownedByPtrValue = objectJson["ownedBy"];
        const unpackedOwnedByPtr = ownedByPtrValue != undefined ? _NodeReference.unpack(1, ownedByPtrValue, _session) as NodeReference : null;
        const unpackedCustomValues = {} as any;
        if (objectJson["customValues"] != undefined) {
          for (const [key, value] of Object.entries(objectJson["customValues"])) {
            unpackedCustomValues[key] = _Value.unpack(1, value as any, _session) as Value;
          }
        }
        const scriptPtrValue = objectJson["script"];
        const unpackedScriptPtr = scriptPtrValue != undefined ? _NodeReference.unpack(1, scriptPtrValue, _session) as NodeReference : null;
        const isExtensibleValue = objectJson["isExtensible"];
        const unpackedIsExtensible = isExtensibleValue != undefined ? isExtensibleValue : null;
        const sourcePtrValue = objectJson["source"];
        const unpackedSourcePtr = sourcePtrValue != undefined ? _NodeReference.unpack(1, sourcePtrValue, _session) as NodeReference : null;
        const keyValue = objectJson["key"];
        const unpackedKey = keyValue != undefined ? keyValue : null;
        return new (NODE_CLASS_BY_TYPE[2100300] as typeof ColorStyle)({
          type: ColorType[objectJson["type"]] as any,
          hue: unpackedHue,
          shade: unpackedShade,
          intent: unpackedIntent,
          x: unpackedX,
          y: unpackedY,
          z: unpackedZ,
          alpha: unpackedAlpha,
          dark: unpackedDark,
          parent: unpackedParentPtr,
          materialization: Materialization[objectJson["materialization"]] as any,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          instance: unpackedInstancePtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          updatedAt: Temporal.Instant.from(objectJson["updatedAt"]).toZonedDateTimeISO('UTC'),
          updatedEpoch: Number(objectJson["updatedEpoch"]),
          updatedBy: _NodeReference.unpack(1, objectJson["updatedBy"], _session) as NodeReference,
          deletedAt: unpackedDeletedAt,
          ownedBy: unpackedOwnedByPtr,
          name: objectJson["name"],
          orderKey: objectJson["orderKey"],
          customValues: unpackedCustomValues,
          script: unpackedScriptPtr,
          isExtensible: unpackedIsExtensible,
          source: unpackedSourcePtr,
          key: unpackedKey,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 2100300)] = new ColorStyleJsonEncoder();

    class BorderStyleJsonEncoder implements _JsonObjectEncoder {
      packObject(object: BorderStyle): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "BORDER_STYLE";
        objectJson["id"] = object.id;
        if (object.parentPtr != null) {
          objectJson["parent"] = object.parentPtr.pack(1);
        }
        objectJson["space"] = object.spacePtr.pack(1);
        objectJson["materialization"] = Materialization[object.materialization];
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.instancePtr != null) {
          objectJson["instance"] = object.instancePtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["updatedAt"] = object.updatedAt.toString({ timeZoneName: 'never' });
        objectJson["updatedEpoch"] = Number(object.updatedEpoch);
        objectJson["updatedBy"] = object.updatedByPtr.pack(1);
        if (object.deletedAt != null) {
          objectJson["deletedAt"] = object.deletedAt.toString({ timeZoneName: 'never' });
        }
        if (object._ownedByPtr != null) {
          objectJson["ownedBy"] = object._ownedByPtr.pack(1);
        }
        objectJson["name"] = object._name;
        objectJson["orderKey"] = object.orderKey;
        if (Object.keys(object._customValues).length > 0) {
          const packedCustomValues: { [key: string]: any } = {} as any;
          for (const [key, value] of Object.entries(object._customValues)) {
            packedCustomValues[String(key)] = value.pack(1);
          }
          objectJson["customValues"] = packedCustomValues;
        }
        if (object._scriptPtr != null) {
          objectJson["script"] = object._scriptPtr.pack(1);
        }
        if (object.isExtensible != null) {
          objectJson["isExtensible"] = object.isExtensible;
        }
        if (object.sourcePtr != null) {
          objectJson["source"] = object.sourcePtr.pack(1);
        }
        if (object._key != null) {
          objectJson["key"] = object._key;
        }
        objectJson["type"] = BorderType[object._type];
        if (object._color != null) {
          objectJson["color"] = object._color.pack(1);
        }
        if (object._width != null) {
          objectJson["width"] = object._width.pack(1);
        }
        if (object._stylePtr != null) {
          objectJson["style"] = object._stylePtr.pack(1);
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): BorderStyle {
        const _Value = STRUCT_CLASS_BY_TYPE[100] as typeof Value;
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const _Color = STRUCT_CLASS_BY_TYPE[2100300] as typeof Color;
        const _Inset2 = STRUCT_CLASS_BY_TYPE[2400023] as typeof Inset2;
        const colorValue = objectJson["color"];
        const unpackedColor = colorValue != undefined ? _Color.unpack(1, colorValue, _session) as Color : null;
        const widthValue = objectJson["width"];
        const unpackedWidth = widthValue != undefined ? _Inset2.unpack(1, widthValue, _session) as Inset2 : null;
        const stylePtrValue = objectJson["style"];
        const unpackedStylePtr = stylePtrValue != undefined ? _NodeReference.unpack(1, stylePtrValue, _session) as NodeReference : null;
        const parentPtrValue = objectJson["parent"];
        const unpackedParentPtr = parentPtrValue != undefined ? _NodeReference.unpack(1, parentPtrValue, _session) as NodeReference : null;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const instancePtrValue = objectJson["instance"];
        const unpackedInstancePtr = instancePtrValue != undefined ? _NodeReference.unpack(1, instancePtrValue, _session) as NodeReference : null;
        const deletedAtValue = objectJson["deletedAt"];
        const unpackedDeletedAt = deletedAtValue != undefined ? Temporal.Instant.from(deletedAtValue).toZonedDateTimeISO('UTC') : null;
        const ownedByPtrValue = objectJson["ownedBy"];
        const unpackedOwnedByPtr = ownedByPtrValue != undefined ? _NodeReference.unpack(1, ownedByPtrValue, _session) as NodeReference : null;
        const unpackedCustomValues = {} as any;
        if (objectJson["customValues"] != undefined) {
          for (const [key, value] of Object.entries(objectJson["customValues"])) {
            unpackedCustomValues[key] = _Value.unpack(1, value as any, _session) as Value;
          }
        }
        const scriptPtrValue = objectJson["script"];
        const unpackedScriptPtr = scriptPtrValue != undefined ? _NodeReference.unpack(1, scriptPtrValue, _session) as NodeReference : null;
        const isExtensibleValue = objectJson["isExtensible"];
        const unpackedIsExtensible = isExtensibleValue != undefined ? isExtensibleValue : null;
        const sourcePtrValue = objectJson["source"];
        const unpackedSourcePtr = sourcePtrValue != undefined ? _NodeReference.unpack(1, sourcePtrValue, _session) as NodeReference : null;
        const keyValue = objectJson["key"];
        const unpackedKey = keyValue != undefined ? keyValue : null;
        return new (NODE_CLASS_BY_TYPE[2100600] as typeof BorderStyle)({
          type: BorderType[objectJson["type"]] as any,
          color: unpackedColor,
          width: unpackedWidth,
          style: unpackedStylePtr,
          parent: unpackedParentPtr,
          materialization: Materialization[objectJson["materialization"]] as any,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          instance: unpackedInstancePtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          updatedAt: Temporal.Instant.from(objectJson["updatedAt"]).toZonedDateTimeISO('UTC'),
          updatedEpoch: Number(objectJson["updatedEpoch"]),
          updatedBy: _NodeReference.unpack(1, objectJson["updatedBy"], _session) as NodeReference,
          deletedAt: unpackedDeletedAt,
          ownedBy: unpackedOwnedByPtr,
          name: objectJson["name"],
          orderKey: objectJson["orderKey"],
          customValues: unpackedCustomValues,
          script: unpackedScriptPtr,
          isExtensible: unpackedIsExtensible,
          source: unpackedSourcePtr,
          key: unpackedKey,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 2100600)] = new BorderStyleJsonEncoder();

    class GradientStyleJsonEncoder implements _JsonObjectEncoder {
      packObject(object: GradientStyle): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "GRADIENT_STYLE";
        objectJson["id"] = object.id;
        if (object.parentPtr != null) {
          objectJson["parent"] = object.parentPtr.pack(1);
        }
        objectJson["space"] = object.spacePtr.pack(1);
        objectJson["materialization"] = Materialization[object.materialization];
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.instancePtr != null) {
          objectJson["instance"] = object.instancePtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["updatedAt"] = object.updatedAt.toString({ timeZoneName: 'never' });
        objectJson["updatedEpoch"] = Number(object.updatedEpoch);
        objectJson["updatedBy"] = object.updatedByPtr.pack(1);
        if (object.deletedAt != null) {
          objectJson["deletedAt"] = object.deletedAt.toString({ timeZoneName: 'never' });
        }
        if (object._ownedByPtr != null) {
          objectJson["ownedBy"] = object._ownedByPtr.pack(1);
        }
        objectJson["name"] = object._name;
        objectJson["orderKey"] = object.orderKey;
        if (Object.keys(object._customValues).length > 0) {
          const packedCustomValues: { [key: string]: any } = {} as any;
          for (const [key, value] of Object.entries(object._customValues)) {
            packedCustomValues[String(key)] = value.pack(1);
          }
          objectJson["customValues"] = packedCustomValues;
        }
        if (object._scriptPtr != null) {
          objectJson["script"] = object._scriptPtr.pack(1);
        }
        if (object.isExtensible != null) {
          objectJson["isExtensible"] = object.isExtensible;
        }
        if (object.sourcePtr != null) {
          objectJson["source"] = object.sourcePtr.pack(1);
        }
        if (object._key != null) {
          objectJson["key"] = object._key;
        }
        objectJson["type"] = GradientType[object._type];
        if (object._angle != null) {
          objectJson["angle"] = Number(object._angle);
        }
        if (object._stops.length > 0) {
          const packedStops: any[] = [];
          for (const item of object._stops) {
            packedStops.push(item.pack(1));
          }
          objectJson["stops"] = packedStops;
        }
        if (object._centerAnchor != null) {
          objectJson["centerAnchor"] = object._centerAnchor.pack(1);
        }
        if (object._dark != null) {
          objectJson["dark"] = object._dark.pack(1);
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): GradientStyle {
        const _Value = STRUCT_CLASS_BY_TYPE[100] as typeof Value;
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const _Gradient = STRUCT_CLASS_BY_TYPE[2100800] as typeof Gradient;
        const _GradientStop = STRUCT_CLASS_BY_TYPE[2100801] as typeof GradientStop;
        const _Axis2 = STRUCT_CLASS_BY_TYPE[2400025] as typeof Axis2;
        const angleValue = objectJson["angle"];
        const unpackedAngle = angleValue != undefined ? Number(angleValue) : null;
        const unpackedStops: any[] = [];
        if (objectJson["stops"] != undefined) {
          for (const item of objectJson["stops"]) {
            unpackedStops.push(_GradientStop.unpack(1, item, _session) as GradientStop)
          }
        }
        const centerAnchorValue = objectJson["centerAnchor"];
        const unpackedCenterAnchor = centerAnchorValue != undefined ? _Axis2.unpack(1, centerAnchorValue, _session) as Axis2 : null;
        const darkValue = objectJson["dark"];
        const unpackedDark = darkValue != undefined ? _Gradient.unpack(1, darkValue, _session) as Gradient : null;
        const parentPtrValue = objectJson["parent"];
        const unpackedParentPtr = parentPtrValue != undefined ? _NodeReference.unpack(1, parentPtrValue, _session) as NodeReference : null;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const instancePtrValue = objectJson["instance"];
        const unpackedInstancePtr = instancePtrValue != undefined ? _NodeReference.unpack(1, instancePtrValue, _session) as NodeReference : null;
        const deletedAtValue = objectJson["deletedAt"];
        const unpackedDeletedAt = deletedAtValue != undefined ? Temporal.Instant.from(deletedAtValue).toZonedDateTimeISO('UTC') : null;
        const ownedByPtrValue = objectJson["ownedBy"];
        const unpackedOwnedByPtr = ownedByPtrValue != undefined ? _NodeReference.unpack(1, ownedByPtrValue, _session) as NodeReference : null;
        const unpackedCustomValues = {} as any;
        if (objectJson["customValues"] != undefined) {
          for (const [key, value] of Object.entries(objectJson["customValues"])) {
            unpackedCustomValues[key] = _Value.unpack(1, value as any, _session) as Value;
          }
        }
        const scriptPtrValue = objectJson["script"];
        const unpackedScriptPtr = scriptPtrValue != undefined ? _NodeReference.unpack(1, scriptPtrValue, _session) as NodeReference : null;
        const isExtensibleValue = objectJson["isExtensible"];
        const unpackedIsExtensible = isExtensibleValue != undefined ? isExtensibleValue : null;
        const sourcePtrValue = objectJson["source"];
        const unpackedSourcePtr = sourcePtrValue != undefined ? _NodeReference.unpack(1, sourcePtrValue, _session) as NodeReference : null;
        const keyValue = objectJson["key"];
        const unpackedKey = keyValue != undefined ? keyValue : null;
        return new (NODE_CLASS_BY_TYPE[2100800] as typeof GradientStyle)({
          type: GradientType[objectJson["type"]] as any,
          angle: unpackedAngle,
          stops: unpackedStops,
          centerAnchor: unpackedCenterAnchor,
          dark: unpackedDark,
          parent: unpackedParentPtr,
          materialization: Materialization[objectJson["materialization"]] as any,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          instance: unpackedInstancePtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          updatedAt: Temporal.Instant.from(objectJson["updatedAt"]).toZonedDateTimeISO('UTC'),
          updatedEpoch: Number(objectJson["updatedEpoch"]),
          updatedBy: _NodeReference.unpack(1, objectJson["updatedBy"], _session) as NodeReference,
          deletedAt: unpackedDeletedAt,
          ownedBy: unpackedOwnedByPtr,
          name: objectJson["name"],
          orderKey: objectJson["orderKey"],
          customValues: unpackedCustomValues,
          script: unpackedScriptPtr,
          isExtensible: unpackedIsExtensible,
          source: unpackedSourcePtr,
          key: unpackedKey,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 2100800)] = new GradientStyleJsonEncoder();

    class FillStyleJsonEncoder implements _JsonObjectEncoder {
      packObject(object: FillStyle): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "FILL_STYLE";
        objectJson["id"] = object.id;
        if (object.parentPtr != null) {
          objectJson["parent"] = object.parentPtr.pack(1);
        }
        objectJson["space"] = object.spacePtr.pack(1);
        objectJson["materialization"] = Materialization[object.materialization];
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.instancePtr != null) {
          objectJson["instance"] = object.instancePtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["updatedAt"] = object.updatedAt.toString({ timeZoneName: 'never' });
        objectJson["updatedEpoch"] = Number(object.updatedEpoch);
        objectJson["updatedBy"] = object.updatedByPtr.pack(1);
        if (object.deletedAt != null) {
          objectJson["deletedAt"] = object.deletedAt.toString({ timeZoneName: 'never' });
        }
        if (object._ownedByPtr != null) {
          objectJson["ownedBy"] = object._ownedByPtr.pack(1);
        }
        objectJson["name"] = object._name;
        objectJson["orderKey"] = object.orderKey;
        if (Object.keys(object._customValues).length > 0) {
          const packedCustomValues: { [key: string]: any } = {} as any;
          for (const [key, value] of Object.entries(object._customValues)) {
            packedCustomValues[String(key)] = value.pack(1);
          }
          objectJson["customValues"] = packedCustomValues;
        }
        if (object._scriptPtr != null) {
          objectJson["script"] = object._scriptPtr.pack(1);
        }
        if (object.isExtensible != null) {
          objectJson["isExtensible"] = object.isExtensible;
        }
        if (object.sourcePtr != null) {
          objectJson["source"] = object.sourcePtr.pack(1);
        }
        if (object._key != null) {
          objectJson["key"] = object._key;
        }
        objectJson["type"] = FillType[object._type];
        if (object._color != null) {
          objectJson["color"] = object._color.pack(1);
        }
        if (object._gradient != null) {
          objectJson["gradient"] = object._gradient.pack(1);
        }
        if (object._imagePtr != null) {
          objectJson["image"] = object._imagePtr.pack(1);
        }
        if (object._position != null) {
          objectJson["position"] = FillPosition[object._position];
        }
        if (object._size != null) {
          objectJson["size"] = FillSize[object._size];
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): FillStyle {
        const _Value = STRUCT_CLASS_BY_TYPE[100] as typeof Value;
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const _Color = STRUCT_CLASS_BY_TYPE[2100300] as typeof Color;
        const _Gradient = STRUCT_CLASS_BY_TYPE[2100800] as typeof Gradient;
        const colorValue = objectJson["color"];
        const unpackedColor = colorValue != undefined ? _Color.unpack(1, colorValue, _session) as Color : null;
        const gradientValue = objectJson["gradient"];
        const unpackedGradient = gradientValue != undefined ? _Gradient.unpack(1, gradientValue, _session) as Gradient : null;
        const imagePtrValue = objectJson["image"];
        const unpackedImagePtr = imagePtrValue != undefined ? _NodeReference.unpack(1, imagePtrValue, _session) as NodeReference : null;
        const positionValue = objectJson["position"];
        const unpackedPosition = positionValue != undefined ? FillPosition[positionValue] as any : null;
        const sizeValue = objectJson["size"];
        const unpackedSize = sizeValue != undefined ? FillSize[sizeValue] as any : null;
        const parentPtrValue = objectJson["parent"];
        const unpackedParentPtr = parentPtrValue != undefined ? _NodeReference.unpack(1, parentPtrValue, _session) as NodeReference : null;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const instancePtrValue = objectJson["instance"];
        const unpackedInstancePtr = instancePtrValue != undefined ? _NodeReference.unpack(1, instancePtrValue, _session) as NodeReference : null;
        const deletedAtValue = objectJson["deletedAt"];
        const unpackedDeletedAt = deletedAtValue != undefined ? Temporal.Instant.from(deletedAtValue).toZonedDateTimeISO('UTC') : null;
        const ownedByPtrValue = objectJson["ownedBy"];
        const unpackedOwnedByPtr = ownedByPtrValue != undefined ? _NodeReference.unpack(1, ownedByPtrValue, _session) as NodeReference : null;
        const unpackedCustomValues = {} as any;
        if (objectJson["customValues"] != undefined) {
          for (const [key, value] of Object.entries(objectJson["customValues"])) {
            unpackedCustomValues[key] = _Value.unpack(1, value as any, _session) as Value;
          }
        }
        const scriptPtrValue = objectJson["script"];
        const unpackedScriptPtr = scriptPtrValue != undefined ? _NodeReference.unpack(1, scriptPtrValue, _session) as NodeReference : null;
        const isExtensibleValue = objectJson["isExtensible"];
        const unpackedIsExtensible = isExtensibleValue != undefined ? isExtensibleValue : null;
        const sourcePtrValue = objectJson["source"];
        const unpackedSourcePtr = sourcePtrValue != undefined ? _NodeReference.unpack(1, sourcePtrValue, _session) as NodeReference : null;
        const keyValue = objectJson["key"];
        const unpackedKey = keyValue != undefined ? keyValue : null;
        return new (NODE_CLASS_BY_TYPE[2100400] as typeof FillStyle)({
          type: FillType[objectJson["type"]] as any,
          color: unpackedColor,
          gradient: unpackedGradient,
          image: unpackedImagePtr,
          position: unpackedPosition,
          size: unpackedSize,
          parent: unpackedParentPtr,
          materialization: Materialization[objectJson["materialization"]] as any,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          instance: unpackedInstancePtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          updatedAt: Temporal.Instant.from(objectJson["updatedAt"]).toZonedDateTimeISO('UTC'),
          updatedEpoch: Number(objectJson["updatedEpoch"]),
          updatedBy: _NodeReference.unpack(1, objectJson["updatedBy"], _session) as NodeReference,
          deletedAt: unpackedDeletedAt,
          ownedBy: unpackedOwnedByPtr,
          name: objectJson["name"],
          orderKey: objectJson["orderKey"],
          customValues: unpackedCustomValues,
          script: unpackedScriptPtr,
          isExtensible: unpackedIsExtensible,
          source: unpackedSourcePtr,
          key: unpackedKey,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 2100400)] = new FillStyleJsonEncoder();

    class FontStyleJsonEncoder implements _JsonObjectEncoder {
      packObject(object: FontStyle): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "FONT_STYLE";
        objectJson["id"] = object.id;
        if (object.parentPtr != null) {
          objectJson["parent"] = object.parentPtr.pack(1);
        }
        objectJson["space"] = object.spacePtr.pack(1);
        objectJson["materialization"] = Materialization[object.materialization];
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.instancePtr != null) {
          objectJson["instance"] = object.instancePtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["updatedAt"] = object.updatedAt.toString({ timeZoneName: 'never' });
        objectJson["updatedEpoch"] = Number(object.updatedEpoch);
        objectJson["updatedBy"] = object.updatedByPtr.pack(1);
        if (object.deletedAt != null) {
          objectJson["deletedAt"] = object.deletedAt.toString({ timeZoneName: 'never' });
        }
        if (object._ownedByPtr != null) {
          objectJson["ownedBy"] = object._ownedByPtr.pack(1);
        }
        objectJson["name"] = object._name;
        objectJson["orderKey"] = object.orderKey;
        if (Object.keys(object._customValues).length > 0) {
          const packedCustomValues: { [key: string]: any } = {} as any;
          for (const [key, value] of Object.entries(object._customValues)) {
            packedCustomValues[String(key)] = value.pack(1);
          }
          objectJson["customValues"] = packedCustomValues;
        }
        if (object._scriptPtr != null) {
          objectJson["script"] = object._scriptPtr.pack(1);
        }
        if (object.isExtensible != null) {
          objectJson["isExtensible"] = object.isExtensible;
        }
        if (object.sourcePtr != null) {
          objectJson["source"] = object.sourcePtr.pack(1);
        }
        if (object._key != null) {
          objectJson["key"] = object._key;
        }
        objectJson["type"] = FontType[object._type];
        if (object._weight != null) {
          objectJson["weight"] = FontWeight[object._weight];
        }
        if (object._color != null) {
          objectJson["color"] = object._color.pack(1);
        }
        if (object._size != null) {
          objectJson["size"] = FontSize[object._size];
        }
        if (object._align != null) {
          objectJson["align"] = TextAlign[object._align];
        }
        if (object._lineHeight != null) {
          objectJson["lineHeight"] = object._lineHeight.pack(1);
        }
        if (object._letterSpacing != null) {
          objectJson["letterSpacing"] = object._letterSpacing.pack(1);
        }
        if (object._decoration != null) {
          objectJson["decoration"] = TextDecoration[object._decoration];
        }
        if (object._transform != null) {
          objectJson["transform"] = TextTransform[object._transform];
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): FontStyle {
        const _Value = STRUCT_CLASS_BY_TYPE[100] as typeof Value;
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const _Length = STRUCT_CLASS_BY_TYPE[1800001] as typeof Length;
        const _Fill = STRUCT_CLASS_BY_TYPE[2100400] as typeof Fill;
        const weightValue = objectJson["weight"];
        const unpackedWeight = weightValue != undefined ? FontWeight[weightValue] as any : null;
        const colorValue = objectJson["color"];
        const unpackedColor = colorValue != undefined ? _Fill.unpack(1, colorValue, _session) as Fill : null;
        const sizeValue = objectJson["size"];
        const unpackedSize = sizeValue != undefined ? FontSize[sizeValue] as any : null;
        const alignValue = objectJson["align"];
        const unpackedAlign = alignValue != undefined ? TextAlign[alignValue] as any : null;
        const lineHeightValue = objectJson["lineHeight"];
        const unpackedLineHeight = lineHeightValue != undefined ? _Length.unpack(1, lineHeightValue, _session) as Length : null;
        const letterSpacingValue = objectJson["letterSpacing"];
        const unpackedLetterSpacing = letterSpacingValue != undefined ? _Length.unpack(1, letterSpacingValue, _session) as Length : null;
        const decorationValue = objectJson["decoration"];
        const unpackedDecoration = decorationValue != undefined ? TextDecoration[decorationValue] as any : null;
        const transformValue = objectJson["transform"];
        const unpackedTransform = transformValue != undefined ? TextTransform[transformValue] as any : null;
        const parentPtrValue = objectJson["parent"];
        const unpackedParentPtr = parentPtrValue != undefined ? _NodeReference.unpack(1, parentPtrValue, _session) as NodeReference : null;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const instancePtrValue = objectJson["instance"];
        const unpackedInstancePtr = instancePtrValue != undefined ? _NodeReference.unpack(1, instancePtrValue, _session) as NodeReference : null;
        const deletedAtValue = objectJson["deletedAt"];
        const unpackedDeletedAt = deletedAtValue != undefined ? Temporal.Instant.from(deletedAtValue).toZonedDateTimeISO('UTC') : null;
        const ownedByPtrValue = objectJson["ownedBy"];
        const unpackedOwnedByPtr = ownedByPtrValue != undefined ? _NodeReference.unpack(1, ownedByPtrValue, _session) as NodeReference : null;
        const unpackedCustomValues = {} as any;
        if (objectJson["customValues"] != undefined) {
          for (const [key, value] of Object.entries(objectJson["customValues"])) {
            unpackedCustomValues[key] = _Value.unpack(1, value as any, _session) as Value;
          }
        }
        const scriptPtrValue = objectJson["script"];
        const unpackedScriptPtr = scriptPtrValue != undefined ? _NodeReference.unpack(1, scriptPtrValue, _session) as NodeReference : null;
        const isExtensibleValue = objectJson["isExtensible"];
        const unpackedIsExtensible = isExtensibleValue != undefined ? isExtensibleValue : null;
        const sourcePtrValue = objectJson["source"];
        const unpackedSourcePtr = sourcePtrValue != undefined ? _NodeReference.unpack(1, sourcePtrValue, _session) as NodeReference : null;
        const keyValue = objectJson["key"];
        const unpackedKey = keyValue != undefined ? keyValue : null;
        return new (NODE_CLASS_BY_TYPE[2100500] as typeof FontStyle)({
          type: FontType[objectJson["type"]] as any,
          weight: unpackedWeight,
          color: unpackedColor,
          size: unpackedSize,
          align: unpackedAlign,
          lineHeight: unpackedLineHeight,
          letterSpacing: unpackedLetterSpacing,
          decoration: unpackedDecoration,
          transform: unpackedTransform,
          parent: unpackedParentPtr,
          materialization: Materialization[objectJson["materialization"]] as any,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          instance: unpackedInstancePtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          updatedAt: Temporal.Instant.from(objectJson["updatedAt"]).toZonedDateTimeISO('UTC'),
          updatedEpoch: Number(objectJson["updatedEpoch"]),
          updatedBy: _NodeReference.unpack(1, objectJson["updatedBy"], _session) as NodeReference,
          deletedAt: unpackedDeletedAt,
          ownedBy: unpackedOwnedByPtr,
          name: objectJson["name"],
          orderKey: objectJson["orderKey"],
          customValues: unpackedCustomValues,
          script: unpackedScriptPtr,
          isExtensible: unpackedIsExtensible,
          source: unpackedSourcePtr,
          key: unpackedKey,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 2100500)] = new FontStyleJsonEncoder();

    class PaletteJsonEncoder implements _JsonObjectEncoder {
      packObject(object: Palette): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "PALETTE";
        objectJson["id"] = object.id;
        if (object.parentPtr != null) {
          objectJson["parent"] = object.parentPtr.pack(1);
        }
        objectJson["space"] = object.spacePtr.pack(1);
        objectJson["materialization"] = Materialization[object.materialization];
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.instancePtr != null) {
          objectJson["instance"] = object.instancePtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["updatedAt"] = object.updatedAt.toString({ timeZoneName: 'never' });
        objectJson["updatedEpoch"] = Number(object.updatedEpoch);
        objectJson["updatedBy"] = object.updatedByPtr.pack(1);
        if (object.deletedAt != null) {
          objectJson["deletedAt"] = object.deletedAt.toString({ timeZoneName: 'never' });
        }
        if (object._ownedByPtr != null) {
          objectJson["ownedBy"] = object._ownedByPtr.pack(1);
        }
        objectJson["name"] = object._name;
        objectJson["orderKey"] = object.orderKey;
        if (Object.keys(object._customValues).length > 0) {
          const packedCustomValues: { [key: string]: any } = {} as any;
          for (const [key, value] of Object.entries(object._customValues)) {
            packedCustomValues[String(key)] = value.pack(1);
          }
          objectJson["customValues"] = packedCustomValues;
        }
        if (object._scriptPtr != null) {
          objectJson["script"] = object._scriptPtr.pack(1);
        }
        if (object.isExtensible != null) {
          objectJson["isExtensible"] = object.isExtensible;
        }
        if (object.sourcePtr != null) {
          objectJson["source"] = object.sourcePtr.pack(1);
        }
        if (object._key != null) {
          objectJson["key"] = object._key;
        }
        if (object._icon != null) {
          objectJson["icon"] = object._icon.pack(1);
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): Palette {
        const _Value = STRUCT_CLASS_BY_TYPE[100] as typeof Value;
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const _Icon = STRUCT_CLASS_BY_TYPE[400031] as typeof Icon;
        const iconValue = objectJson["icon"];
        const unpackedIcon = iconValue != undefined ? _Icon.unpack(1, iconValue, _session) as Icon : null;
        const parentPtrValue = objectJson["parent"];
        const unpackedParentPtr = parentPtrValue != undefined ? _NodeReference.unpack(1, parentPtrValue, _session) as NodeReference : null;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const instancePtrValue = objectJson["instance"];
        const unpackedInstancePtr = instancePtrValue != undefined ? _NodeReference.unpack(1, instancePtrValue, _session) as NodeReference : null;
        const deletedAtValue = objectJson["deletedAt"];
        const unpackedDeletedAt = deletedAtValue != undefined ? Temporal.Instant.from(deletedAtValue).toZonedDateTimeISO('UTC') : null;
        const ownedByPtrValue = objectJson["ownedBy"];
        const unpackedOwnedByPtr = ownedByPtrValue != undefined ? _NodeReference.unpack(1, ownedByPtrValue, _session) as NodeReference : null;
        const unpackedCustomValues = {} as any;
        if (objectJson["customValues"] != undefined) {
          for (const [key, value] of Object.entries(objectJson["customValues"])) {
            unpackedCustomValues[key] = _Value.unpack(1, value as any, _session) as Value;
          }
        }
        const scriptPtrValue = objectJson["script"];
        const unpackedScriptPtr = scriptPtrValue != undefined ? _NodeReference.unpack(1, scriptPtrValue, _session) as NodeReference : null;
        const isExtensibleValue = objectJson["isExtensible"];
        const unpackedIsExtensible = isExtensibleValue != undefined ? isExtensibleValue : null;
        const sourcePtrValue = objectJson["source"];
        const unpackedSourcePtr = sourcePtrValue != undefined ? _NodeReference.unpack(1, sourcePtrValue, _session) as NodeReference : null;
        const keyValue = objectJson["key"];
        const unpackedKey = keyValue != undefined ? keyValue : null;
        return new (NODE_CLASS_BY_TYPE[2100100] as typeof Palette)({
          icon: unpackedIcon,
          parent: unpackedParentPtr,
          materialization: Materialization[objectJson["materialization"]] as any,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          instance: unpackedInstancePtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          updatedAt: Temporal.Instant.from(objectJson["updatedAt"]).toZonedDateTimeISO('UTC'),
          updatedEpoch: Number(objectJson["updatedEpoch"]),
          updatedBy: _NodeReference.unpack(1, objectJson["updatedBy"], _session) as NodeReference,
          deletedAt: unpackedDeletedAt,
          ownedBy: unpackedOwnedByPtr,
          name: objectJson["name"],
          orderKey: objectJson["orderKey"],
          customValues: unpackedCustomValues,
          script: unpackedScriptPtr,
          isExtensible: unpackedIsExtensible,
          source: unpackedSourcePtr,
          key: unpackedKey,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 2100100)] = new PaletteJsonEncoder();

    class ShadowStyleJsonEncoder implements _JsonObjectEncoder {
      packObject(object: ShadowStyle): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "SHADOW_STYLE";
        objectJson["id"] = object.id;
        if (object.parentPtr != null) {
          objectJson["parent"] = object.parentPtr.pack(1);
        }
        objectJson["space"] = object.spacePtr.pack(1);
        objectJson["materialization"] = Materialization[object.materialization];
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.instancePtr != null) {
          objectJson["instance"] = object.instancePtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["updatedAt"] = object.updatedAt.toString({ timeZoneName: 'never' });
        objectJson["updatedEpoch"] = Number(object.updatedEpoch);
        objectJson["updatedBy"] = object.updatedByPtr.pack(1);
        if (object.deletedAt != null) {
          objectJson["deletedAt"] = object.deletedAt.toString({ timeZoneName: 'never' });
        }
        if (object._ownedByPtr != null) {
          objectJson["ownedBy"] = object._ownedByPtr.pack(1);
        }
        objectJson["name"] = object._name;
        objectJson["orderKey"] = object.orderKey;
        if (Object.keys(object._customValues).length > 0) {
          const packedCustomValues: { [key: string]: any } = {} as any;
          for (const [key, value] of Object.entries(object._customValues)) {
            packedCustomValues[String(key)] = value.pack(1);
          }
          objectJson["customValues"] = packedCustomValues;
        }
        if (object._scriptPtr != null) {
          objectJson["script"] = object._scriptPtr.pack(1);
        }
        if (object.isExtensible != null) {
          objectJson["isExtensible"] = object.isExtensible;
        }
        if (object.sourcePtr != null) {
          objectJson["source"] = object.sourcePtr.pack(1);
        }
        if (object._key != null) {
          objectJson["key"] = object._key;
        }
        objectJson["type"] = ShadowType[object._type];
        if (object._color != null) {
          objectJson["color"] = object._color.pack(1);
        }
        objectJson["position"] = ShadowPosition[object._position];
        if (object._offset != null) {
          objectJson["offset"] = object._offset.pack(1);
        }
        if (object._blur != null) {
          objectJson["blur"] = Number(object._blur);
        }
        if (object._spread != null) {
          objectJson["spread"] = Number(object._spread);
        }
        if (object._diffusion != null) {
          objectJson["diffusion"] = Number(object._diffusion);
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): ShadowStyle {
        const _Value = STRUCT_CLASS_BY_TYPE[100] as typeof Value;
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const _Color = STRUCT_CLASS_BY_TYPE[2100300] as typeof Color;
        const _Axis2 = STRUCT_CLASS_BY_TYPE[2400025] as typeof Axis2;
        const colorValue = objectJson["color"];
        const unpackedColor = colorValue != undefined ? _Color.unpack(1, colorValue, _session) as Color : null;
        const offsetValue = objectJson["offset"];
        const unpackedOffset = offsetValue != undefined ? _Axis2.unpack(1, offsetValue, _session) as Axis2 : null;
        const blurValue = objectJson["blur"];
        const unpackedBlur = blurValue != undefined ? Number(blurValue) : null;
        const spreadValue = objectJson["spread"];
        const unpackedSpread = spreadValue != undefined ? Number(spreadValue) : null;
        const diffusionValue = objectJson["diffusion"];
        const unpackedDiffusion = diffusionValue != undefined ? Number(diffusionValue) : null;
        const parentPtrValue = objectJson["parent"];
        const unpackedParentPtr = parentPtrValue != undefined ? _NodeReference.unpack(1, parentPtrValue, _session) as NodeReference : null;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const instancePtrValue = objectJson["instance"];
        const unpackedInstancePtr = instancePtrValue != undefined ? _NodeReference.unpack(1, instancePtrValue, _session) as NodeReference : null;
        const deletedAtValue = objectJson["deletedAt"];
        const unpackedDeletedAt = deletedAtValue != undefined ? Temporal.Instant.from(deletedAtValue).toZonedDateTimeISO('UTC') : null;
        const ownedByPtrValue = objectJson["ownedBy"];
        const unpackedOwnedByPtr = ownedByPtrValue != undefined ? _NodeReference.unpack(1, ownedByPtrValue, _session) as NodeReference : null;
        const unpackedCustomValues = {} as any;
        if (objectJson["customValues"] != undefined) {
          for (const [key, value] of Object.entries(objectJson["customValues"])) {
            unpackedCustomValues[key] = _Value.unpack(1, value as any, _session) as Value;
          }
        }
        const scriptPtrValue = objectJson["script"];
        const unpackedScriptPtr = scriptPtrValue != undefined ? _NodeReference.unpack(1, scriptPtrValue, _session) as NodeReference : null;
        const isExtensibleValue = objectJson["isExtensible"];
        const unpackedIsExtensible = isExtensibleValue != undefined ? isExtensibleValue : null;
        const sourcePtrValue = objectJson["source"];
        const unpackedSourcePtr = sourcePtrValue != undefined ? _NodeReference.unpack(1, sourcePtrValue, _session) as NodeReference : null;
        const keyValue = objectJson["key"];
        const unpackedKey = keyValue != undefined ? keyValue : null;
        return new (NODE_CLASS_BY_TYPE[2100700] as typeof ShadowStyle)({
          type: ShadowType[objectJson["type"]] as any,
          color: unpackedColor,
          position: ShadowPosition[objectJson["position"]] as any,
          offset: unpackedOffset,
          blur: unpackedBlur,
          spread: unpackedSpread,
          diffusion: unpackedDiffusion,
          parent: unpackedParentPtr,
          materialization: Materialization[objectJson["materialization"]] as any,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          instance: unpackedInstancePtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          updatedAt: Temporal.Instant.from(objectJson["updatedAt"]).toZonedDateTimeISO('UTC'),
          updatedEpoch: Number(objectJson["updatedEpoch"]),
          updatedBy: _NodeReference.unpack(1, objectJson["updatedBy"], _session) as NodeReference,
          deletedAt: unpackedDeletedAt,
          ownedBy: unpackedOwnedByPtr,
          name: objectJson["name"],
          orderKey: objectJson["orderKey"],
          customValues: unpackedCustomValues,
          script: unpackedScriptPtr,
          isExtensible: unpackedIsExtensible,
          source: unpackedSourcePtr,
          key: unpackedKey,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 2100700)] = new ShadowStyleJsonEncoder();

    class StrokeStyleJsonEncoder implements _JsonObjectEncoder {
      packObject(object: StrokeStyle): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "STROKE_STYLE";
        objectJson["id"] = object.id;
        if (object.parentPtr != null) {
          objectJson["parent"] = object.parentPtr.pack(1);
        }
        objectJson["space"] = object.spacePtr.pack(1);
        objectJson["materialization"] = Materialization[object.materialization];
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.instancePtr != null) {
          objectJson["instance"] = object.instancePtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["updatedAt"] = object.updatedAt.toString({ timeZoneName: 'never' });
        objectJson["updatedEpoch"] = Number(object.updatedEpoch);
        objectJson["updatedBy"] = object.updatedByPtr.pack(1);
        if (object.deletedAt != null) {
          objectJson["deletedAt"] = object.deletedAt.toString({ timeZoneName: 'never' });
        }
        if (object._ownedByPtr != null) {
          objectJson["ownedBy"] = object._ownedByPtr.pack(1);
        }
        objectJson["name"] = object._name;
        objectJson["orderKey"] = object.orderKey;
        if (Object.keys(object._customValues).length > 0) {
          const packedCustomValues: { [key: string]: any } = {} as any;
          for (const [key, value] of Object.entries(object._customValues)) {
            packedCustomValues[String(key)] = value.pack(1);
          }
          objectJson["customValues"] = packedCustomValues;
        }
        if (object._scriptPtr != null) {
          objectJson["script"] = object._scriptPtr.pack(1);
        }
        if (object.isExtensible != null) {
          objectJson["isExtensible"] = object.isExtensible;
        }
        if (object.sourcePtr != null) {
          objectJson["source"] = object.sourcePtr.pack(1);
        }
        if (object._key != null) {
          objectJson["key"] = object._key;
        }
        objectJson["type"] = StrokeType[object._type];
        objectJson["size"] = Number(object._size);
        objectJson["thinning"] = Number(object._thinning);
        objectJson["smoothing"] = Number(object._smoothing);
        objectJson["streamline"] = Number(object._streamline);
        objectJson["easing"] = Easing[object._easing];
        if (object._start != null) {
          objectJson["start"] = object._start.pack(1);
        }
        if (object._end != null) {
          objectJson["end"] = object._end.pack(1);
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): StrokeStyle {
        const _Value = STRUCT_CLASS_BY_TYPE[100] as typeof Value;
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const _StrokeCap = STRUCT_CLASS_BY_TYPE[2101101] as typeof StrokeCap;
        const startValue = objectJson["start"];
        const unpackedStart = startValue != undefined ? _StrokeCap.unpack(1, startValue, _session) as StrokeCap : null;
        const endValue = objectJson["end"];
        const unpackedEnd = endValue != undefined ? _StrokeCap.unpack(1, endValue, _session) as StrokeCap : null;
        const parentPtrValue = objectJson["parent"];
        const unpackedParentPtr = parentPtrValue != undefined ? _NodeReference.unpack(1, parentPtrValue, _session) as NodeReference : null;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const instancePtrValue = objectJson["instance"];
        const unpackedInstancePtr = instancePtrValue != undefined ? _NodeReference.unpack(1, instancePtrValue, _session) as NodeReference : null;
        const deletedAtValue = objectJson["deletedAt"];
        const unpackedDeletedAt = deletedAtValue != undefined ? Temporal.Instant.from(deletedAtValue).toZonedDateTimeISO('UTC') : null;
        const ownedByPtrValue = objectJson["ownedBy"];
        const unpackedOwnedByPtr = ownedByPtrValue != undefined ? _NodeReference.unpack(1, ownedByPtrValue, _session) as NodeReference : null;
        const unpackedCustomValues = {} as any;
        if (objectJson["customValues"] != undefined) {
          for (const [key, value] of Object.entries(objectJson["customValues"])) {
            unpackedCustomValues[key] = _Value.unpack(1, value as any, _session) as Value;
          }
        }
        const scriptPtrValue = objectJson["script"];
        const unpackedScriptPtr = scriptPtrValue != undefined ? _NodeReference.unpack(1, scriptPtrValue, _session) as NodeReference : null;
        const isExtensibleValue = objectJson["isExtensible"];
        const unpackedIsExtensible = isExtensibleValue != undefined ? isExtensibleValue : null;
        const sourcePtrValue = objectJson["source"];
        const unpackedSourcePtr = sourcePtrValue != undefined ? _NodeReference.unpack(1, sourcePtrValue, _session) as NodeReference : null;
        const keyValue = objectJson["key"];
        const unpackedKey = keyValue != undefined ? keyValue : null;
        return new (NODE_CLASS_BY_TYPE[2101100] as typeof StrokeStyle)({
          type: StrokeType[objectJson["type"]] as any,
          size: Number(objectJson["size"]),
          thinning: Number(objectJson["thinning"]),
          smoothing: Number(objectJson["smoothing"]),
          streamline: Number(objectJson["streamline"]),
          easing: Easing[objectJson["easing"]] as any,
          start: unpackedStart,
          end: unpackedEnd,
          parent: unpackedParentPtr,
          materialization: Materialization[objectJson["materialization"]] as any,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          instance: unpackedInstancePtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          updatedAt: Temporal.Instant.from(objectJson["updatedAt"]).toZonedDateTimeISO('UTC'),
          updatedEpoch: Number(objectJson["updatedEpoch"]),
          updatedBy: _NodeReference.unpack(1, objectJson["updatedBy"], _session) as NodeReference,
          deletedAt: unpackedDeletedAt,
          ownedBy: unpackedOwnedByPtr,
          name: objectJson["name"],
          orderKey: objectJson["orderKey"],
          customValues: unpackedCustomValues,
          script: unpackedScriptPtr,
          isExtensible: unpackedIsExtensible,
          source: unpackedSourcePtr,
          key: unpackedKey,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 2101100)] = new StrokeStyleJsonEncoder();

    class ThemeJsonEncoder implements _JsonObjectEncoder {
      packObject(object: Theme): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "THEME";
        objectJson["id"] = object.id;
        if (object.parentPtr != null) {
          objectJson["parent"] = object.parentPtr.pack(1);
        }
        objectJson["space"] = object.spacePtr.pack(1);
        objectJson["materialization"] = Materialization[object.materialization];
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.instancePtr != null) {
          objectJson["instance"] = object.instancePtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["updatedAt"] = object.updatedAt.toString({ timeZoneName: 'never' });
        objectJson["updatedEpoch"] = Number(object.updatedEpoch);
        objectJson["updatedBy"] = object.updatedByPtr.pack(1);
        if (object.deletedAt != null) {
          objectJson["deletedAt"] = object.deletedAt.toString({ timeZoneName: 'never' });
        }
        if (object._ownedByPtr != null) {
          objectJson["ownedBy"] = object._ownedByPtr.pack(1);
        }
        objectJson["name"] = object._name;
        objectJson["orderKey"] = object.orderKey;
        if (Object.keys(object._customValues).length > 0) {
          const packedCustomValues: { [key: string]: any } = {} as any;
          for (const [key, value] of Object.entries(object._customValues)) {
            packedCustomValues[String(key)] = value.pack(1);
          }
          objectJson["customValues"] = packedCustomValues;
        }
        if (object._scriptPtr != null) {
          objectJson["script"] = object._scriptPtr.pack(1);
        }
        if (object.isExtensible != null) {
          objectJson["isExtensible"] = object.isExtensible;
        }
        if (object.sourcePtr != null) {
          objectJson["source"] = object.sourcePtr.pack(1);
        }
        if (object._key != null) {
          objectJson["key"] = object._key;
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): Theme {
        const _Value = STRUCT_CLASS_BY_TYPE[100] as typeof Value;
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const parentPtrValue = objectJson["parent"];
        const unpackedParentPtr = parentPtrValue != undefined ? _NodeReference.unpack(1, parentPtrValue, _session) as NodeReference : null;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const instancePtrValue = objectJson["instance"];
        const unpackedInstancePtr = instancePtrValue != undefined ? _NodeReference.unpack(1, instancePtrValue, _session) as NodeReference : null;
        const deletedAtValue = objectJson["deletedAt"];
        const unpackedDeletedAt = deletedAtValue != undefined ? Temporal.Instant.from(deletedAtValue).toZonedDateTimeISO('UTC') : null;
        const ownedByPtrValue = objectJson["ownedBy"];
        const unpackedOwnedByPtr = ownedByPtrValue != undefined ? _NodeReference.unpack(1, ownedByPtrValue, _session) as NodeReference : null;
        const unpackedCustomValues = {} as any;
        if (objectJson["customValues"] != undefined) {
          for (const [key, value] of Object.entries(objectJson["customValues"])) {
            unpackedCustomValues[key] = _Value.unpack(1, value as any, _session) as Value;
          }
        }
        const scriptPtrValue = objectJson["script"];
        const unpackedScriptPtr = scriptPtrValue != undefined ? _NodeReference.unpack(1, scriptPtrValue, _session) as NodeReference : null;
        const isExtensibleValue = objectJson["isExtensible"];
        const unpackedIsExtensible = isExtensibleValue != undefined ? isExtensibleValue : null;
        const sourcePtrValue = objectJson["source"];
        const unpackedSourcePtr = sourcePtrValue != undefined ? _NodeReference.unpack(1, sourcePtrValue, _session) as NodeReference : null;
        const keyValue = objectJson["key"];
        const unpackedKey = keyValue != undefined ? keyValue : null;
        return new (NODE_CLASS_BY_TYPE[2100000] as typeof Theme)({
          parent: unpackedParentPtr,
          materialization: Materialization[objectJson["materialization"]] as any,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          instance: unpackedInstancePtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          updatedAt: Temporal.Instant.from(objectJson["updatedAt"]).toZonedDateTimeISO('UTC'),
          updatedEpoch: Number(objectJson["updatedEpoch"]),
          updatedBy: _NodeReference.unpack(1, objectJson["updatedBy"], _session) as NodeReference,
          deletedAt: unpackedDeletedAt,
          ownedBy: unpackedOwnedByPtr,
          name: objectJson["name"],
          orderKey: objectJson["orderKey"],
          customValues: unpackedCustomValues,
          script: unpackedScriptPtr,
          isExtensible: unpackedIsExtensible,
          source: unpackedSourcePtr,
          key: unpackedKey,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 2100000)] = new ThemeJsonEncoder();

    class TransitionStyleJsonEncoder implements _JsonObjectEncoder {
      packObject(object: TransitionStyle): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "TRANSITION_STYLE";
        objectJson["id"] = object.id;
        if (object.parentPtr != null) {
          objectJson["parent"] = object.parentPtr.pack(1);
        }
        objectJson["space"] = object.spacePtr.pack(1);
        objectJson["materialization"] = Materialization[object.materialization];
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.instancePtr != null) {
          objectJson["instance"] = object.instancePtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["updatedAt"] = object.updatedAt.toString({ timeZoneName: 'never' });
        objectJson["updatedEpoch"] = Number(object.updatedEpoch);
        objectJson["updatedBy"] = object.updatedByPtr.pack(1);
        if (object.deletedAt != null) {
          objectJson["deletedAt"] = object.deletedAt.toString({ timeZoneName: 'never' });
        }
        if (object._ownedByPtr != null) {
          objectJson["ownedBy"] = object._ownedByPtr.pack(1);
        }
        objectJson["name"] = object._name;
        objectJson["orderKey"] = object.orderKey;
        if (Object.keys(object._customValues).length > 0) {
          const packedCustomValues: { [key: string]: any } = {} as any;
          for (const [key, value] of Object.entries(object._customValues)) {
            packedCustomValues[String(key)] = value.pack(1);
          }
          objectJson["customValues"] = packedCustomValues;
        }
        if (object._scriptPtr != null) {
          objectJson["script"] = object._scriptPtr.pack(1);
        }
        if (object.isExtensible != null) {
          objectJson["isExtensible"] = object.isExtensible;
        }
        if (object.sourcePtr != null) {
          objectJson["source"] = object.sourcePtr.pack(1);
        }
        if (object._key != null) {
          objectJson["key"] = object._key;
        }
        objectJson["type"] = TransitionType[object._type];
        if (object._delay != null) {
          objectJson["delay"] = Number(object._delay);
        }
        if (object._duration != null) {
          objectJson["duration"] = Number(object._duration);
        }
        if (object._ease.length > 0) {
          const packedEase: any[] = [];
          for (const item of object._ease) {
            packedEase.push(Number(item));
          }
          objectJson["ease"] = packedEase;
        }
        if (object._stiffness != null) {
          objectJson["stiffness"] = Number(object._stiffness);
        }
        if (object._damping != null) {
          objectJson["damping"] = Number(object._damping);
        }
        if (object._mass != null) {
          objectJson["mass"] = Number(object._mass);
        }
        if (object._bounce != null) {
          objectJson["bounce"] = Number(object._bounce);
        }
        if (object._springType != null) {
          objectJson["springType"] = SpringType[object._springType];
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): TransitionStyle {
        const _Value = STRUCT_CLASS_BY_TYPE[100] as typeof Value;
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const delayValue = objectJson["delay"];
        const unpackedDelay = delayValue != undefined ? Number(delayValue) : null;
        const durationValue = objectJson["duration"];
        const unpackedDuration = durationValue != undefined ? Number(durationValue) : null;
        const unpackedEase: any[] = [];
        if (objectJson["ease"] != undefined) {
          for (const item of objectJson["ease"]) {
            unpackedEase.push(Number(item))
          }
        }
        const stiffnessValue = objectJson["stiffness"];
        const unpackedStiffness = stiffnessValue != undefined ? Number(stiffnessValue) : null;
        const dampingValue = objectJson["damping"];
        const unpackedDamping = dampingValue != undefined ? Number(dampingValue) : null;
        const massValue = objectJson["mass"];
        const unpackedMass = massValue != undefined ? Number(massValue) : null;
        const bounceValue = objectJson["bounce"];
        const unpackedBounce = bounceValue != undefined ? Number(bounceValue) : null;
        const springTypeValue = objectJson["springType"];
        const unpackedSpringType = springTypeValue != undefined ? SpringType[springTypeValue] as any : null;
        const parentPtrValue = objectJson["parent"];
        const unpackedParentPtr = parentPtrValue != undefined ? _NodeReference.unpack(1, parentPtrValue, _session) as NodeReference : null;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const instancePtrValue = objectJson["instance"];
        const unpackedInstancePtr = instancePtrValue != undefined ? _NodeReference.unpack(1, instancePtrValue, _session) as NodeReference : null;
        const deletedAtValue = objectJson["deletedAt"];
        const unpackedDeletedAt = deletedAtValue != undefined ? Temporal.Instant.from(deletedAtValue).toZonedDateTimeISO('UTC') : null;
        const ownedByPtrValue = objectJson["ownedBy"];
        const unpackedOwnedByPtr = ownedByPtrValue != undefined ? _NodeReference.unpack(1, ownedByPtrValue, _session) as NodeReference : null;
        const unpackedCustomValues = {} as any;
        if (objectJson["customValues"] != undefined) {
          for (const [key, value] of Object.entries(objectJson["customValues"])) {
            unpackedCustomValues[key] = _Value.unpack(1, value as any, _session) as Value;
          }
        }
        const scriptPtrValue = objectJson["script"];
        const unpackedScriptPtr = scriptPtrValue != undefined ? _NodeReference.unpack(1, scriptPtrValue, _session) as NodeReference : null;
        const isExtensibleValue = objectJson["isExtensible"];
        const unpackedIsExtensible = isExtensibleValue != undefined ? isExtensibleValue : null;
        const sourcePtrValue = objectJson["source"];
        const unpackedSourcePtr = sourcePtrValue != undefined ? _NodeReference.unpack(1, sourcePtrValue, _session) as NodeReference : null;
        const keyValue = objectJson["key"];
        const unpackedKey = keyValue != undefined ? keyValue : null;
        return new (NODE_CLASS_BY_TYPE[2200000] as typeof TransitionStyle)({
          type: TransitionType[objectJson["type"]] as any,
          delay: unpackedDelay,
          duration: unpackedDuration,
          ease: unpackedEase,
          stiffness: unpackedStiffness,
          damping: unpackedDamping,
          mass: unpackedMass,
          bounce: unpackedBounce,
          springType: unpackedSpringType,
          parent: unpackedParentPtr,
          materialization: Materialization[objectJson["materialization"]] as any,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          instance: unpackedInstancePtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          updatedAt: Temporal.Instant.from(objectJson["updatedAt"]).toZonedDateTimeISO('UTC'),
          updatedEpoch: Number(objectJson["updatedEpoch"]),
          updatedBy: _NodeReference.unpack(1, objectJson["updatedBy"], _session) as NodeReference,
          deletedAt: unpackedDeletedAt,
          ownedBy: unpackedOwnedByPtr,
          name: objectJson["name"],
          orderKey: objectJson["orderKey"],
          customValues: unpackedCustomValues,
          script: unpackedScriptPtr,
          isExtensible: unpackedIsExtensible,
          source: unpackedSourcePtr,
          key: unpackedKey,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 2200000)] = new TransitionStyleJsonEncoder();

    class EffectStyleJsonEncoder implements _JsonObjectEncoder {
      packObject(object: EffectStyle): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "EFFECT_STYLE";
        objectJson["id"] = object.id;
        if (object.parentPtr != null) {
          objectJson["parent"] = object.parentPtr.pack(1);
        }
        objectJson["space"] = object.spacePtr.pack(1);
        objectJson["materialization"] = Materialization[object.materialization];
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.instancePtr != null) {
          objectJson["instance"] = object.instancePtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["updatedAt"] = object.updatedAt.toString({ timeZoneName: 'never' });
        objectJson["updatedEpoch"] = Number(object.updatedEpoch);
        objectJson["updatedBy"] = object.updatedByPtr.pack(1);
        if (object.deletedAt != null) {
          objectJson["deletedAt"] = object.deletedAt.toString({ timeZoneName: 'never' });
        }
        if (object._ownedByPtr != null) {
          objectJson["ownedBy"] = object._ownedByPtr.pack(1);
        }
        objectJson["name"] = object._name;
        objectJson["orderKey"] = object.orderKey;
        if (Object.keys(object._customValues).length > 0) {
          const packedCustomValues: { [key: string]: any } = {} as any;
          for (const [key, value] of Object.entries(object._customValues)) {
            packedCustomValues[String(key)] = value.pack(1);
          }
          objectJson["customValues"] = packedCustomValues;
        }
        if (object._scriptPtr != null) {
          objectJson["script"] = object._scriptPtr.pack(1);
        }
        if (object.isExtensible != null) {
          objectJson["isExtensible"] = object.isExtensible;
        }
        if (object.sourcePtr != null) {
          objectJson["source"] = object.sourcePtr.pack(1);
        }
        if (object._key != null) {
          objectJson["key"] = object._key;
        }
        objectJson["type"] = EffectType[object._type];
        if (object._opacity != null) {
          objectJson["opacity"] = Number(object._opacity);
        }
        if (object._offset != null) {
          objectJson["offset"] = object._offset.pack(1);
        }
        if (object._scale != null) {
          objectJson["scale"] = Number(object._scale);
        }
        if (object._rotate != null) {
          objectJson["rotate"] = object._rotate.pack(1);
        }
        if (object._skew != null) {
          objectJson["skew"] = object._skew.pack(1);
        }
        if (object._perspective != null) {
          objectJson["perspective"] = Number(object._perspective);
        }
        if (object._delay != null) {
          objectJson["delay"] = timedeltaToISOFormat(object._delay);
        }
        if (object._duration != null) {
          objectJson["duration"] = Number(object._duration);
        }
        if (object._threshold != null) {
          objectJson["threshold"] = Number(object._threshold);
        }
        if (object._once != null) {
          objectJson["once"] = object._once;
        }
        if (object._repeat != null) {
          objectJson["repeat"] = RepeatType[object._repeat];
        }
        if (object._split != null) {
          objectJson["split"] = TextSplitType[object._split];
        }
        if (object._offscreen != null) {
          objectJson["offscreen"] = OffscreenBehavior[object._offscreen];
        }
        if (object._transition != null) {
          objectJson["transition"] = object._transition.pack(1);
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): EffectStyle {
        const _Value = STRUCT_CLASS_BY_TYPE[100] as typeof Value;
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const _Transition = STRUCT_CLASS_BY_TYPE[2200000] as typeof Transition;
        const _Vector2 = STRUCT_CLASS_BY_TYPE[2400000] as typeof Vector2;
        const _Axis3 = STRUCT_CLASS_BY_TYPE[2400026] as typeof Axis3;
        const opacityValue = objectJson["opacity"];
        const unpackedOpacity = opacityValue != undefined ? Number(opacityValue) : null;
        const offsetValue = objectJson["offset"];
        const unpackedOffset = offsetValue != undefined ? _Vector2.unpack(1, offsetValue, _session) as Vector2 : null;
        const scaleValue = objectJson["scale"];
        const unpackedScale = scaleValue != undefined ? Number(scaleValue) : null;
        const rotateValue = objectJson["rotate"];
        const unpackedRotate = rotateValue != undefined ? _Axis3.unpack(1, rotateValue, _session) as Axis3 : null;
        const skewValue = objectJson["skew"];
        const unpackedSkew = skewValue != undefined ? _Vector2.unpack(1, skewValue, _session) as Vector2 : null;
        const perspectiveValue = objectJson["perspective"];
        const unpackedPerspective = perspectiveValue != undefined ? Number(perspectiveValue) : null;
        const delayValue = objectJson["delay"];
        const unpackedDelay = delayValue != undefined ? timedeltaFromISOFormat(delayValue) : null;
        const durationValue = objectJson["duration"];
        const unpackedDuration = durationValue != undefined ? Number(durationValue) : null;
        const thresholdValue = objectJson["threshold"];
        const unpackedThreshold = thresholdValue != undefined ? Number(thresholdValue) : null;
        const onceValue = objectJson["once"];
        const unpackedOnce = onceValue != undefined ? onceValue : null;
        const repeatValue = objectJson["repeat"];
        const unpackedRepeat = repeatValue != undefined ? RepeatType[repeatValue] as any : null;
        const splitValue = objectJson["split"];
        const unpackedSplit = splitValue != undefined ? TextSplitType[splitValue] as any : null;
        const offscreenValue = objectJson["offscreen"];
        const unpackedOffscreen = offscreenValue != undefined ? OffscreenBehavior[offscreenValue] as any : null;
        const transitionValue = objectJson["transition"];
        const unpackedTransition = transitionValue != undefined ? _Transition.unpack(1, transitionValue, _session) as Transition : null;
        const parentPtrValue = objectJson["parent"];
        const unpackedParentPtr = parentPtrValue != undefined ? _NodeReference.unpack(1, parentPtrValue, _session) as NodeReference : null;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const instancePtrValue = objectJson["instance"];
        const unpackedInstancePtr = instancePtrValue != undefined ? _NodeReference.unpack(1, instancePtrValue, _session) as NodeReference : null;
        const deletedAtValue = objectJson["deletedAt"];
        const unpackedDeletedAt = deletedAtValue != undefined ? Temporal.Instant.from(deletedAtValue).toZonedDateTimeISO('UTC') : null;
        const ownedByPtrValue = objectJson["ownedBy"];
        const unpackedOwnedByPtr = ownedByPtrValue != undefined ? _NodeReference.unpack(1, ownedByPtrValue, _session) as NodeReference : null;
        const unpackedCustomValues = {} as any;
        if (objectJson["customValues"] != undefined) {
          for (const [key, value] of Object.entries(objectJson["customValues"])) {
            unpackedCustomValues[key] = _Value.unpack(1, value as any, _session) as Value;
          }
        }
        const scriptPtrValue = objectJson["script"];
        const unpackedScriptPtr = scriptPtrValue != undefined ? _NodeReference.unpack(1, scriptPtrValue, _session) as NodeReference : null;
        const isExtensibleValue = objectJson["isExtensible"];
        const unpackedIsExtensible = isExtensibleValue != undefined ? isExtensibleValue : null;
        const sourcePtrValue = objectJson["source"];
        const unpackedSourcePtr = sourcePtrValue != undefined ? _NodeReference.unpack(1, sourcePtrValue, _session) as NodeReference : null;
        const keyValue = objectJson["key"];
        const unpackedKey = keyValue != undefined ? keyValue : null;
        return new (NODE_CLASS_BY_TYPE[2200100] as typeof EffectStyle)({
          type: EffectType[objectJson["type"]] as any,
          opacity: unpackedOpacity,
          offset: unpackedOffset,
          scale: unpackedScale,
          rotate: unpackedRotate,
          skew: unpackedSkew,
          perspective: unpackedPerspective,
          delay: unpackedDelay,
          duration: unpackedDuration,
          threshold: unpackedThreshold,
          once: unpackedOnce,
          repeat: unpackedRepeat,
          split: unpackedSplit,
          offscreen: unpackedOffscreen,
          transition: unpackedTransition,
          parent: unpackedParentPtr,
          materialization: Materialization[objectJson["materialization"]] as any,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          instance: unpackedInstancePtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          updatedAt: Temporal.Instant.from(objectJson["updatedAt"]).toZonedDateTimeISO('UTC'),
          updatedEpoch: Number(objectJson["updatedEpoch"]),
          updatedBy: _NodeReference.unpack(1, objectJson["updatedBy"], _session) as NodeReference,
          deletedAt: unpackedDeletedAt,
          ownedBy: unpackedOwnedByPtr,
          name: objectJson["name"],
          orderKey: objectJson["orderKey"],
          customValues: unpackedCustomValues,
          script: unpackedScriptPtr,
          isExtensible: unpackedIsExtensible,
          source: unpackedSourcePtr,
          key: unpackedKey,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 2200100)] = new EffectStyleJsonEncoder();

    class FileJsonEncoder implements _JsonObjectEncoder {
      packObject(object: File): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "FILE";
        objectJson["id"] = object.id;
        if (object.parentPtr != null) {
          objectJson["parent"] = object.parentPtr.pack(1);
        }
        objectJson["space"] = object.spacePtr.pack(1);
        objectJson["materialization"] = Materialization[object.materialization];
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.instancePtr != null) {
          objectJson["instance"] = object.instancePtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["updatedAt"] = object.updatedAt.toString({ timeZoneName: 'never' });
        objectJson["updatedEpoch"] = Number(object.updatedEpoch);
        objectJson["updatedBy"] = object.updatedByPtr.pack(1);
        if (object.deletedAt != null) {
          objectJson["deletedAt"] = object.deletedAt.toString({ timeZoneName: 'never' });
        }
        if (object._ownedByPtr != null) {
          objectJson["ownedBy"] = object._ownedByPtr.pack(1);
        }
        objectJson["name"] = object._name;
        objectJson["orderKey"] = object.orderKey;
        if (Object.keys(object._customValues).length > 0) {
          const packedCustomValues: { [key: string]: any } = {} as any;
          for (const [key, value] of Object.entries(object._customValues)) {
            packedCustomValues[String(key)] = value.pack(1);
          }
          objectJson["customValues"] = packedCustomValues;
        }
        if (object._scriptPtr != null) {
          objectJson["script"] = object._scriptPtr.pack(1);
        }
        if (object.isExtensible != null) {
          objectJson["isExtensible"] = object.isExtensible;
        }
        if (object.sourcePtr != null) {
          objectJson["source"] = object.sourcePtr.pack(1);
        }
        if (object._key != null) {
          objectJson["key"] = object._key;
        }
        objectJson["type"] = FileType[object._type];
        if (object._status != null) {
          objectJson["status"] = ResourceStatus[object._status];
        }
        if (object._region != null) {
          objectJson["region"] = Region[object._region];
        }
        if (object._mimeType != null) {
          objectJson["mimeType"] = object._mimeType;
        }
        if (object._format != null) {
          objectJson["format"] = FileFormat[object._format];
        }
        if (object._size != null) {
          objectJson["size"] = Number(object._size);
        }
        if (object._sha256 != null) {
          objectJson["sha256"] = object._sha256;
        }
        if (object._width != null) {
          objectJson["width"] = Number(object._width);
        }
        if (object._height != null) {
          objectJson["height"] = Number(object._height);
        }
        if (object._aspectRatio != null) {
          objectJson["aspectRatio"] = Number(object._aspectRatio);
        }
        if (object._codec != null) {
          objectJson["codec"] = object._codec;
        }
        if (object._duration != null) {
          objectJson["duration"] = timedeltaToISOFormat(object._duration);
        }
        if (object._url != null) {
          objectJson["url"] = object._url;
        }
        if (object._contentUrl != null) {
          objectJson["contentUrl"] = object._contentUrl;
        }
        if (object._thumbnailUrl != null) {
          objectJson["thumbnailUrl"] = object._thumbnailUrl;
        }
        if (object._faviconUrl != null) {
          objectJson["faviconUrl"] = object._faviconUrl;
        }
        if (object._thumbnailWidth != null) {
          objectJson["thumbnailWidth"] = Number(object._thumbnailWidth);
        }
        if (object._thumbnailHeight != null) {
          objectJson["thumbnailHeight"] = Number(object._thumbnailHeight);
        }
        if (object._content != null) {
          objectJson["content"] = base64Encode(object._content);
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): File {
        const _Value = STRUCT_CLASS_BY_TYPE[100] as typeof Value;
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const parentPtrValue = objectJson["parent"];
        const unpackedParentPtr = parentPtrValue != undefined ? _NodeReference.unpack(1, parentPtrValue, _session) as NodeReference : null;
        const mimeTypeValue = objectJson["mimeType"];
        const unpackedMimeType = mimeTypeValue != undefined ? mimeTypeValue : null;
        const formatValue = objectJson["format"];
        const unpackedFormat = formatValue != undefined ? FileFormat[formatValue] as any : null;
        const sizeValue = objectJson["size"];
        const unpackedSize = sizeValue != undefined ? Number(sizeValue) : null;
        const sha256Value = objectJson["sha256"];
        const unpackedSha256 = sha256Value != undefined ? sha256Value : null;
        const widthValue = objectJson["width"];
        const unpackedWidth = widthValue != undefined ? Number(widthValue) : null;
        const heightValue = objectJson["height"];
        const unpackedHeight = heightValue != undefined ? Number(heightValue) : null;
        const aspectRatioValue = objectJson["aspectRatio"];
        const unpackedAspectRatio = aspectRatioValue != undefined ? Number(aspectRatioValue) : null;
        const codecValue = objectJson["codec"];
        const unpackedCodec = codecValue != undefined ? codecValue : null;
        const durationValue = objectJson["duration"];
        const unpackedDuration = durationValue != undefined ? timedeltaFromISOFormat(durationValue) : null;
        const urlValue = objectJson["url"];
        const unpackedUrl = urlValue != undefined ? urlValue : null;
        const contentUrlValue = objectJson["contentUrl"];
        const unpackedContentUrl = contentUrlValue != undefined ? contentUrlValue : null;
        const thumbnailUrlValue = objectJson["thumbnailUrl"];
        const unpackedThumbnailUrl = thumbnailUrlValue != undefined ? thumbnailUrlValue : null;
        const faviconUrlValue = objectJson["faviconUrl"];
        const unpackedFaviconUrl = faviconUrlValue != undefined ? faviconUrlValue : null;
        const thumbnailWidthValue = objectJson["thumbnailWidth"];
        const unpackedThumbnailWidth = thumbnailWidthValue != undefined ? Number(thumbnailWidthValue) : null;
        const thumbnailHeightValue = objectJson["thumbnailHeight"];
        const unpackedThumbnailHeight = thumbnailHeightValue != undefined ? Number(thumbnailHeightValue) : null;
        const contentValue = objectJson["content"];
        const unpackedContent = contentValue != undefined ? base64Decode(contentValue) : null;
        const statusValue = objectJson["status"];
        const unpackedStatus = statusValue != undefined ? ResourceStatus[statusValue] as any : null;
        const regionValue = objectJson["region"];
        const unpackedRegion = regionValue != undefined ? Region[regionValue] as any : null;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const instancePtrValue = objectJson["instance"];
        const unpackedInstancePtr = instancePtrValue != undefined ? _NodeReference.unpack(1, instancePtrValue, _session) as NodeReference : null;
        const deletedAtValue = objectJson["deletedAt"];
        const unpackedDeletedAt = deletedAtValue != undefined ? Temporal.Instant.from(deletedAtValue).toZonedDateTimeISO('UTC') : null;
        const ownedByPtrValue = objectJson["ownedBy"];
        const unpackedOwnedByPtr = ownedByPtrValue != undefined ? _NodeReference.unpack(1, ownedByPtrValue, _session) as NodeReference : null;
        const unpackedCustomValues = {} as any;
        if (objectJson["customValues"] != undefined) {
          for (const [key, value] of Object.entries(objectJson["customValues"])) {
            unpackedCustomValues[key] = _Value.unpack(1, value as any, _session) as Value;
          }
        }
        const scriptPtrValue = objectJson["script"];
        const unpackedScriptPtr = scriptPtrValue != undefined ? _NodeReference.unpack(1, scriptPtrValue, _session) as NodeReference : null;
        const isExtensibleValue = objectJson["isExtensible"];
        const unpackedIsExtensible = isExtensibleValue != undefined ? isExtensibleValue : null;
        const sourcePtrValue = objectJson["source"];
        const unpackedSourcePtr = sourcePtrValue != undefined ? _NodeReference.unpack(1, sourcePtrValue, _session) as NodeReference : null;
        const keyValue = objectJson["key"];
        const unpackedKey = keyValue != undefined ? keyValue : null;
        return new (NODE_CLASS_BY_TYPE[480000] as typeof File)({
          parent: unpackedParentPtr,
          type: FileType[objectJson["type"]] as any,
          mimeType: unpackedMimeType,
          format: unpackedFormat,
          size: unpackedSize,
          sha256: unpackedSha256,
          width: unpackedWidth,
          height: unpackedHeight,
          aspectRatio: unpackedAspectRatio,
          codec: unpackedCodec,
          duration: unpackedDuration,
          url: unpackedUrl,
          contentUrl: unpackedContentUrl,
          thumbnailUrl: unpackedThumbnailUrl,
          faviconUrl: unpackedFaviconUrl,
          thumbnailWidth: unpackedThumbnailWidth,
          thumbnailHeight: unpackedThumbnailHeight,
          content: unpackedContent,
          status: unpackedStatus,
          region: unpackedRegion,
          materialization: Materialization[objectJson["materialization"]] as any,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          instance: unpackedInstancePtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          updatedAt: Temporal.Instant.from(objectJson["updatedAt"]).toZonedDateTimeISO('UTC'),
          updatedEpoch: Number(objectJson["updatedEpoch"]),
          updatedBy: _NodeReference.unpack(1, objectJson["updatedBy"], _session) as NodeReference,
          deletedAt: unpackedDeletedAt,
          ownedBy: unpackedOwnedByPtr,
          name: objectJson["name"],
          orderKey: objectJson["orderKey"],
          customValues: unpackedCustomValues,
          script: unpackedScriptPtr,
          isExtensible: unpackedIsExtensible,
          source: unpackedSourcePtr,
          key: unpackedKey,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 480000)] = new FileJsonEncoder();

    class EnvironmentJsonEncoder implements _JsonObjectEncoder {
      packObject(object: Environment): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "ENVIRONMENT";
        objectJson["id"] = object.id;
        if (object.parentPtr != null) {
          objectJson["parent"] = object.parentPtr.pack(1);
        }
        objectJson["space"] = object.spacePtr.pack(1);
        objectJson["materialization"] = Materialization[object.materialization];
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.instancePtr != null) {
          objectJson["instance"] = object.instancePtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["updatedAt"] = object.updatedAt.toString({ timeZoneName: 'never' });
        objectJson["updatedEpoch"] = Number(object.updatedEpoch);
        objectJson["updatedBy"] = object.updatedByPtr.pack(1);
        if (object.deletedAt != null) {
          objectJson["deletedAt"] = object.deletedAt.toString({ timeZoneName: 'never' });
        }
        if (object._ownedByPtr != null) {
          objectJson["ownedBy"] = object._ownedByPtr.pack(1);
        }
        objectJson["name"] = object._name;
        objectJson["orderKey"] = object.orderKey;
        if (Object.keys(object._customValues).length > 0) {
          const packedCustomValues: { [key: string]: any } = {} as any;
          for (const [key, value] of Object.entries(object._customValues)) {
            packedCustomValues[String(key)] = value.pack(1);
          }
          objectJson["customValues"] = packedCustomValues;
        }
        if (object._scriptPtr != null) {
          objectJson["script"] = object._scriptPtr.pack(1);
        }
        if (object.isExtensible != null) {
          objectJson["isExtensible"] = object.isExtensible;
        }
        if (object.sourcePtr != null) {
          objectJson["source"] = object.sourcePtr.pack(1);
        }
        if (object._key != null) {
          objectJson["key"] = object._key;
        }
        if (object._icon != null) {
          objectJson["icon"] = object._icon.pack(1);
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): Environment {
        const _Value = STRUCT_CLASS_BY_TYPE[100] as typeof Value;
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const _Icon = STRUCT_CLASS_BY_TYPE[400031] as typeof Icon;
        const parentPtrValue = objectJson["parent"];
        const unpackedParentPtr = parentPtrValue != undefined ? _NodeReference.unpack(1, parentPtrValue, _session) as NodeReference : null;
        const iconValue = objectJson["icon"];
        const unpackedIcon = iconValue != undefined ? _Icon.unpack(1, iconValue, _session) as Icon : null;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const instancePtrValue = objectJson["instance"];
        const unpackedInstancePtr = instancePtrValue != undefined ? _NodeReference.unpack(1, instancePtrValue, _session) as NodeReference : null;
        const deletedAtValue = objectJson["deletedAt"];
        const unpackedDeletedAt = deletedAtValue != undefined ? Temporal.Instant.from(deletedAtValue).toZonedDateTimeISO('UTC') : null;
        const ownedByPtrValue = objectJson["ownedBy"];
        const unpackedOwnedByPtr = ownedByPtrValue != undefined ? _NodeReference.unpack(1, ownedByPtrValue, _session) as NodeReference : null;
        const unpackedCustomValues = {} as any;
        if (objectJson["customValues"] != undefined) {
          for (const [key, value] of Object.entries(objectJson["customValues"])) {
            unpackedCustomValues[key] = _Value.unpack(1, value as any, _session) as Value;
          }
        }
        const scriptPtrValue = objectJson["script"];
        const unpackedScriptPtr = scriptPtrValue != undefined ? _NodeReference.unpack(1, scriptPtrValue, _session) as NodeReference : null;
        const isExtensibleValue = objectJson["isExtensible"];
        const unpackedIsExtensible = isExtensibleValue != undefined ? isExtensibleValue : null;
        const sourcePtrValue = objectJson["source"];
        const unpackedSourcePtr = sourcePtrValue != undefined ? _NodeReference.unpack(1, sourcePtrValue, _session) as NodeReference : null;
        const keyValue = objectJson["key"];
        const unpackedKey = keyValue != undefined ? keyValue : null;
        return new (NODE_CLASS_BY_TYPE[1100000] as typeof Environment)({
          parent: unpackedParentPtr,
          icon: unpackedIcon,
          materialization: Materialization[objectJson["materialization"]] as any,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          instance: unpackedInstancePtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          updatedAt: Temporal.Instant.from(objectJson["updatedAt"]).toZonedDateTimeISO('UTC'),
          updatedEpoch: Number(objectJson["updatedEpoch"]),
          updatedBy: _NodeReference.unpack(1, objectJson["updatedBy"], _session) as NodeReference,
          deletedAt: unpackedDeletedAt,
          ownedBy: unpackedOwnedByPtr,
          name: objectJson["name"],
          orderKey: objectJson["orderKey"],
          customValues: unpackedCustomValues,
          script: unpackedScriptPtr,
          isExtensible: unpackedIsExtensible,
          source: unpackedSourcePtr,
          key: unpackedKey,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 1100000)] = new EnvironmentJsonEncoder();

    class LogEventJsonEncoder implements _JsonObjectEncoder {
      packObject(object: LogEvent): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "LOG_EVENT";
        objectJson["id"] = object.id;
        objectJson["space"] = object.spacePtr.pack(1);
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.causedByPtr != null) {
          objectJson["causedBy"] = object.causedByPtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["client"] = object.clientPtr.pack(1);
        objectJson["clientNonce"] = object.clientNonce;
        objectJson["clientCreatedAt"] = object.clientCreatedAt.toString({ timeZoneName: 'never' });
        objectJson["clientEpoch"] = Number(object.clientEpoch);
        objectJson["status"] = EventStatus[object.status];
        if (object.nodePtr != null) {
          objectJson["node"] = object.nodePtr.pack(1);
        }
        objectJson["content"] = object.content;
        if (Object.keys(object.attributes).length > 0) {
          const packedAttributes: { [key: string]: any } = {} as any;
          for (const [key, value] of Object.entries(object.attributes)) {
            packedAttributes[String(key)] = value;
          }
          objectJson["attributes"] = packedAttributes;
        }
        objectJson["level"] = LogLevel[object.level];
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): LogEvent {
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const unpackedAttributes = {} as any;
        if (objectJson["attributes"] != undefined) {
          for (const [key, value] of Object.entries(objectJson["attributes"])) {
            unpackedAttributes[key] = value as any;
          }
        }
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const causedByPtrValue = objectJson["causedBy"];
        const unpackedCausedByPtr = causedByPtrValue != undefined ? _NodeReference.unpack(1, causedByPtrValue, _session) as NodeReference : null;
        const nodePtrValue = objectJson["node"];
        const unpackedNodePtr = nodePtrValue != undefined ? _NodeReference.unpack(1, nodePtrValue, _session) as NodeReference : null;
        return new (NODE_CLASS_BY_TYPE[1110011] as typeof LogEvent)({
          content: objectJson["content"],
          attributes: unpackedAttributes,
          level: LogLevel[objectJson["level"]] as any,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          causedBy: unpackedCausedByPtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          client: _NodeReference.unpack(1, objectJson["client"], _session) as NodeReference,
          clientNonce: objectJson["clientNonce"],
          clientCreatedAt: Temporal.Instant.from(objectJson["clientCreatedAt"]).toZonedDateTimeISO('UTC'),
          clientEpoch: Number(objectJson["clientEpoch"]),
          status: EventStatus[objectJson["status"]] as any,
          node: unpackedNodePtr,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 1110011)] = new LogEventJsonEncoder();

    class RunStartedEventJsonEncoder implements _JsonObjectEncoder {
      packObject(object: RunStartedEvent): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "RUN_STARTED_EVENT";
        objectJson["id"] = object.id;
        objectJson["space"] = object.spacePtr.pack(1);
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.causedByPtr != null) {
          objectJson["causedBy"] = object.causedByPtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["client"] = object.clientPtr.pack(1);
        objectJson["clientNonce"] = object.clientNonce;
        objectJson["clientCreatedAt"] = object.clientCreatedAt.toString({ timeZoneName: 'never' });
        objectJson["clientEpoch"] = Number(object.clientEpoch);
        objectJson["status"] = EventStatus[object.status];
        objectJson["node"] = object.nodePtr.pack(1);
        if (object.targetPtr != null) {
          objectJson["target"] = object.targetPtr.pack(1);
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): RunStartedEvent {
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const targetPtrValue = objectJson["target"];
        const unpackedTargetPtr = targetPtrValue != undefined ? _NodeReference.unpack(1, targetPtrValue, _session) as NodeReference : null;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const causedByPtrValue = objectJson["causedBy"];
        const unpackedCausedByPtr = causedByPtrValue != undefined ? _NodeReference.unpack(1, causedByPtrValue, _session) as NodeReference : null;
        return new (NODE_CLASS_BY_TYPE[1110002] as typeof RunStartedEvent)({
          node: _NodeReference.unpack(1, objectJson["node"], _session) as NodeReference,
          target: unpackedTargetPtr,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          causedBy: unpackedCausedByPtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          client: _NodeReference.unpack(1, objectJson["client"], _session) as NodeReference,
          clientNonce: objectJson["clientNonce"],
          clientCreatedAt: Temporal.Instant.from(objectJson["clientCreatedAt"]).toZonedDateTimeISO('UTC'),
          clientEpoch: Number(objectJson["clientEpoch"]),
          status: EventStatus[objectJson["status"]] as any,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 1110002)] = new RunStartedEventJsonEncoder();

    class RunPauseRequestedEventJsonEncoder implements _JsonObjectEncoder {
      packObject(object: RunPauseRequestedEvent): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "RUN_PAUSE_REQUESTED_EVENT";
        objectJson["id"] = object.id;
        objectJson["space"] = object.spacePtr.pack(1);
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.causedByPtr != null) {
          objectJson["causedBy"] = object.causedByPtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["client"] = object.clientPtr.pack(1);
        objectJson["clientNonce"] = object.clientNonce;
        objectJson["clientCreatedAt"] = object.clientCreatedAt.toString({ timeZoneName: 'never' });
        objectJson["clientEpoch"] = Number(object.clientEpoch);
        objectJson["status"] = EventStatus[object.status];
        objectJson["node"] = object.nodePtr.pack(1);
        if (object.targetPtr != null) {
          objectJson["target"] = object.targetPtr.pack(1);
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): RunPauseRequestedEvent {
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const targetPtrValue = objectJson["target"];
        const unpackedTargetPtr = targetPtrValue != undefined ? _NodeReference.unpack(1, targetPtrValue, _session) as NodeReference : null;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const causedByPtrValue = objectJson["causedBy"];
        const unpackedCausedByPtr = causedByPtrValue != undefined ? _NodeReference.unpack(1, causedByPtrValue, _session) as NodeReference : null;
        return new (NODE_CLASS_BY_TYPE[1110003] as typeof RunPauseRequestedEvent)({
          node: _NodeReference.unpack(1, objectJson["node"], _session) as NodeReference,
          target: unpackedTargetPtr,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          causedBy: unpackedCausedByPtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          client: _NodeReference.unpack(1, objectJson["client"], _session) as NodeReference,
          clientNonce: objectJson["clientNonce"],
          clientCreatedAt: Temporal.Instant.from(objectJson["clientCreatedAt"]).toZonedDateTimeISO('UTC'),
          clientEpoch: Number(objectJson["clientEpoch"]),
          status: EventStatus[objectJson["status"]] as any,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 1110003)] = new RunPauseRequestedEventJsonEncoder();

    class RunPausedEventJsonEncoder implements _JsonObjectEncoder {
      packObject(object: RunPausedEvent): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "RUN_PAUSED_EVENT";
        objectJson["id"] = object.id;
        objectJson["space"] = object.spacePtr.pack(1);
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.causedByPtr != null) {
          objectJson["causedBy"] = object.causedByPtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["client"] = object.clientPtr.pack(1);
        objectJson["clientNonce"] = object.clientNonce;
        objectJson["clientCreatedAt"] = object.clientCreatedAt.toString({ timeZoneName: 'never' });
        objectJson["clientEpoch"] = Number(object.clientEpoch);
        objectJson["status"] = EventStatus[object.status];
        objectJson["node"] = object.nodePtr.pack(1);
        if (object.targetPtr != null) {
          objectJson["target"] = object.targetPtr.pack(1);
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): RunPausedEvent {
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const targetPtrValue = objectJson["target"];
        const unpackedTargetPtr = targetPtrValue != undefined ? _NodeReference.unpack(1, targetPtrValue, _session) as NodeReference : null;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const causedByPtrValue = objectJson["causedBy"];
        const unpackedCausedByPtr = causedByPtrValue != undefined ? _NodeReference.unpack(1, causedByPtrValue, _session) as NodeReference : null;
        return new (NODE_CLASS_BY_TYPE[1110004] as typeof RunPausedEvent)({
          node: _NodeReference.unpack(1, objectJson["node"], _session) as NodeReference,
          target: unpackedTargetPtr,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          causedBy: unpackedCausedByPtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          client: _NodeReference.unpack(1, objectJson["client"], _session) as NodeReference,
          clientNonce: objectJson["clientNonce"],
          clientCreatedAt: Temporal.Instant.from(objectJson["clientCreatedAt"]).toZonedDateTimeISO('UTC'),
          clientEpoch: Number(objectJson["clientEpoch"]),
          status: EventStatus[objectJson["status"]] as any,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 1110004)] = new RunPausedEventJsonEncoder();

    class RunResumeRequestedEventJsonEncoder implements _JsonObjectEncoder {
      packObject(object: RunResumeRequestedEvent): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "RUN_RESUME_REQUESTED_EVENT";
        objectJson["id"] = object.id;
        objectJson["space"] = object.spacePtr.pack(1);
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.causedByPtr != null) {
          objectJson["causedBy"] = object.causedByPtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["client"] = object.clientPtr.pack(1);
        objectJson["clientNonce"] = object.clientNonce;
        objectJson["clientCreatedAt"] = object.clientCreatedAt.toString({ timeZoneName: 'never' });
        objectJson["clientEpoch"] = Number(object.clientEpoch);
        objectJson["status"] = EventStatus[object.status];
        objectJson["node"] = object.nodePtr.pack(1);
        if (object.targetPtr != null) {
          objectJson["target"] = object.targetPtr.pack(1);
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): RunResumeRequestedEvent {
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const targetPtrValue = objectJson["target"];
        const unpackedTargetPtr = targetPtrValue != undefined ? _NodeReference.unpack(1, targetPtrValue, _session) as NodeReference : null;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const causedByPtrValue = objectJson["causedBy"];
        const unpackedCausedByPtr = causedByPtrValue != undefined ? _NodeReference.unpack(1, causedByPtrValue, _session) as NodeReference : null;
        return new (NODE_CLASS_BY_TYPE[1110005] as typeof RunResumeRequestedEvent)({
          node: _NodeReference.unpack(1, objectJson["node"], _session) as NodeReference,
          target: unpackedTargetPtr,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          causedBy: unpackedCausedByPtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          client: _NodeReference.unpack(1, objectJson["client"], _session) as NodeReference,
          clientNonce: objectJson["clientNonce"],
          clientCreatedAt: Temporal.Instant.from(objectJson["clientCreatedAt"]).toZonedDateTimeISO('UTC'),
          clientEpoch: Number(objectJson["clientEpoch"]),
          status: EventStatus[objectJson["status"]] as any,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 1110005)] = new RunResumeRequestedEventJsonEncoder();

    class RunResumedEventJsonEncoder implements _JsonObjectEncoder {
      packObject(object: RunResumedEvent): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "RUN_RESUMED_EVENT";
        objectJson["id"] = object.id;
        objectJson["space"] = object.spacePtr.pack(1);
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.causedByPtr != null) {
          objectJson["causedBy"] = object.causedByPtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["client"] = object.clientPtr.pack(1);
        objectJson["clientNonce"] = object.clientNonce;
        objectJson["clientCreatedAt"] = object.clientCreatedAt.toString({ timeZoneName: 'never' });
        objectJson["clientEpoch"] = Number(object.clientEpoch);
        objectJson["status"] = EventStatus[object.status];
        objectJson["node"] = object.nodePtr.pack(1);
        if (object.targetPtr != null) {
          objectJson["target"] = object.targetPtr.pack(1);
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): RunResumedEvent {
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const targetPtrValue = objectJson["target"];
        const unpackedTargetPtr = targetPtrValue != undefined ? _NodeReference.unpack(1, targetPtrValue, _session) as NodeReference : null;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const causedByPtrValue = objectJson["causedBy"];
        const unpackedCausedByPtr = causedByPtrValue != undefined ? _NodeReference.unpack(1, causedByPtrValue, _session) as NodeReference : null;
        return new (NODE_CLASS_BY_TYPE[1110006] as typeof RunResumedEvent)({
          node: _NodeReference.unpack(1, objectJson["node"], _session) as NodeReference,
          target: unpackedTargetPtr,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          causedBy: unpackedCausedByPtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          client: _NodeReference.unpack(1, objectJson["client"], _session) as NodeReference,
          clientNonce: objectJson["clientNonce"],
          clientCreatedAt: Temporal.Instant.from(objectJson["clientCreatedAt"]).toZonedDateTimeISO('UTC'),
          clientEpoch: Number(objectJson["clientEpoch"]),
          status: EventStatus[objectJson["status"]] as any,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 1110006)] = new RunResumedEventJsonEncoder();

    class RunStopRequestedEventJsonEncoder implements _JsonObjectEncoder {
      packObject(object: RunStopRequestedEvent): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "RUN_STOP_REQUESTED_EVENT";
        objectJson["id"] = object.id;
        objectJson["space"] = object.spacePtr.pack(1);
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.causedByPtr != null) {
          objectJson["causedBy"] = object.causedByPtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["client"] = object.clientPtr.pack(1);
        objectJson["clientNonce"] = object.clientNonce;
        objectJson["clientCreatedAt"] = object.clientCreatedAt.toString({ timeZoneName: 'never' });
        objectJson["clientEpoch"] = Number(object.clientEpoch);
        objectJson["status"] = EventStatus[object.status];
        objectJson["node"] = object.nodePtr.pack(1);
        if (object.targetPtr != null) {
          objectJson["target"] = object.targetPtr.pack(1);
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): RunStopRequestedEvent {
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const targetPtrValue = objectJson["target"];
        const unpackedTargetPtr = targetPtrValue != undefined ? _NodeReference.unpack(1, targetPtrValue, _session) as NodeReference : null;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const causedByPtrValue = objectJson["causedBy"];
        const unpackedCausedByPtr = causedByPtrValue != undefined ? _NodeReference.unpack(1, causedByPtrValue, _session) as NodeReference : null;
        return new (NODE_CLASS_BY_TYPE[1110007] as typeof RunStopRequestedEvent)({
          node: _NodeReference.unpack(1, objectJson["node"], _session) as NodeReference,
          target: unpackedTargetPtr,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          causedBy: unpackedCausedByPtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          client: _NodeReference.unpack(1, objectJson["client"], _session) as NodeReference,
          clientNonce: objectJson["clientNonce"],
          clientCreatedAt: Temporal.Instant.from(objectJson["clientCreatedAt"]).toZonedDateTimeISO('UTC'),
          clientEpoch: Number(objectJson["clientEpoch"]),
          status: EventStatus[objectJson["status"]] as any,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 1110007)] = new RunStopRequestedEventJsonEncoder();

    class RunFailedEventJsonEncoder implements _JsonObjectEncoder {
      packObject(object: RunFailedEvent): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "RUN_FAILED_EVENT";
        objectJson["id"] = object.id;
        objectJson["space"] = object.spacePtr.pack(1);
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.causedByPtr != null) {
          objectJson["causedBy"] = object.causedByPtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["client"] = object.clientPtr.pack(1);
        objectJson["clientNonce"] = object.clientNonce;
        objectJson["clientCreatedAt"] = object.clientCreatedAt.toString({ timeZoneName: 'never' });
        objectJson["clientEpoch"] = Number(object.clientEpoch);
        objectJson["status"] = EventStatus[object.status];
        objectJson["node"] = object.nodePtr.pack(1);
        if (object.targetPtr != null) {
          objectJson["target"] = object.targetPtr.pack(1);
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): RunFailedEvent {
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const targetPtrValue = objectJson["target"];
        const unpackedTargetPtr = targetPtrValue != undefined ? _NodeReference.unpack(1, targetPtrValue, _session) as NodeReference : null;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const causedByPtrValue = objectJson["causedBy"];
        const unpackedCausedByPtr = causedByPtrValue != undefined ? _NodeReference.unpack(1, causedByPtrValue, _session) as NodeReference : null;
        return new (NODE_CLASS_BY_TYPE[1110008] as typeof RunFailedEvent)({
          node: _NodeReference.unpack(1, objectJson["node"], _session) as NodeReference,
          target: unpackedTargetPtr,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          causedBy: unpackedCausedByPtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          client: _NodeReference.unpack(1, objectJson["client"], _session) as NodeReference,
          clientNonce: objectJson["clientNonce"],
          clientCreatedAt: Temporal.Instant.from(objectJson["clientCreatedAt"]).toZonedDateTimeISO('UTC'),
          clientEpoch: Number(objectJson["clientEpoch"]),
          status: EventStatus[objectJson["status"]] as any,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 1110008)] = new RunFailedEventJsonEncoder();

    class RunCompletedEventJsonEncoder implements _JsonObjectEncoder {
      packObject(object: RunCompletedEvent): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "RUN_COMPLETED_EVENT";
        objectJson["id"] = object.id;
        objectJson["space"] = object.spacePtr.pack(1);
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.causedByPtr != null) {
          objectJson["causedBy"] = object.causedByPtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["client"] = object.clientPtr.pack(1);
        objectJson["clientNonce"] = object.clientNonce;
        objectJson["clientCreatedAt"] = object.clientCreatedAt.toString({ timeZoneName: 'never' });
        objectJson["clientEpoch"] = Number(object.clientEpoch);
        objectJson["status"] = EventStatus[object.status];
        objectJson["node"] = object.nodePtr.pack(1);
        if (object.targetPtr != null) {
          objectJson["target"] = object.targetPtr.pack(1);
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): RunCompletedEvent {
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const targetPtrValue = objectJson["target"];
        const unpackedTargetPtr = targetPtrValue != undefined ? _NodeReference.unpack(1, targetPtrValue, _session) as NodeReference : null;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const causedByPtrValue = objectJson["causedBy"];
        const unpackedCausedByPtr = causedByPtrValue != undefined ? _NodeReference.unpack(1, causedByPtrValue, _session) as NodeReference : null;
        return new (NODE_CLASS_BY_TYPE[1110009] as typeof RunCompletedEvent)({
          node: _NodeReference.unpack(1, objectJson["node"], _session) as NodeReference,
          target: unpackedTargetPtr,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          causedBy: unpackedCausedByPtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          client: _NodeReference.unpack(1, objectJson["client"], _session) as NodeReference,
          clientNonce: objectJson["clientNonce"],
          clientCreatedAt: Temporal.Instant.from(objectJson["clientCreatedAt"]).toZonedDateTimeISO('UTC'),
          clientEpoch: Number(objectJson["clientEpoch"]),
          status: EventStatus[objectJson["status"]] as any,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 1110009)] = new RunCompletedEventJsonEncoder();

    class SpanEventJsonEncoder implements _JsonObjectEncoder {
      packObject(object: SpanEvent): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "SPAN_EVENT";
        objectJson["id"] = object.id;
        objectJson["space"] = object.spacePtr.pack(1);
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.causedByPtr != null) {
          objectJson["causedBy"] = object.causedByPtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["client"] = object.clientPtr.pack(1);
        objectJson["clientNonce"] = object.clientNonce;
        objectJson["clientCreatedAt"] = object.clientCreatedAt.toString({ timeZoneName: 'never' });
        objectJson["clientEpoch"] = Number(object.clientEpoch);
        objectJson["status"] = EventStatus[object.status];
        objectJson["node"] = object.nodePtr.pack(1);
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): SpanEvent {
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const causedByPtrValue = objectJson["causedBy"];
        const unpackedCausedByPtr = causedByPtrValue != undefined ? _NodeReference.unpack(1, causedByPtrValue, _session) as NodeReference : null;
        return new (NODE_CLASS_BY_TYPE[1110010] as typeof SpanEvent)({
          node: _NodeReference.unpack(1, objectJson["node"], _session) as NodeReference,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          causedBy: unpackedCausedByPtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          client: _NodeReference.unpack(1, objectJson["client"], _session) as NodeReference,
          clientNonce: objectJson["clientNonce"],
          clientCreatedAt: Temporal.Instant.from(objectJson["clientCreatedAt"]).toZonedDateTimeISO('UTC'),
          clientEpoch: Number(objectJson["clientEpoch"]),
          status: EventStatus[objectJson["status"]] as any,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 1110010)] = new SpanEventJsonEncoder();

    class ArrowShape2DJsonEncoder implements _JsonObjectEncoder {
      packObject(object: ArrowShape2D): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "ARROW_SHAPE2D";
        objectJson["id"] = object.id;
        if (object.parentPtr != null) {
          objectJson["parent"] = object.parentPtr.pack(1);
        }
        objectJson["space"] = object.spacePtr.pack(1);
        objectJson["materialization"] = Materialization[object.materialization];
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.instancePtr != null) {
          objectJson["instance"] = object.instancePtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["updatedAt"] = object.updatedAt.toString({ timeZoneName: 'never' });
        objectJson["updatedEpoch"] = Number(object.updatedEpoch);
        objectJson["updatedBy"] = object.updatedByPtr.pack(1);
        if (object.deletedAt != null) {
          objectJson["deletedAt"] = object.deletedAt.toString({ timeZoneName: 'never' });
        }
        if (object._ownedByPtr != null) {
          objectJson["ownedBy"] = object._ownedByPtr.pack(1);
        }
        objectJson["name"] = object._name;
        objectJson["orderKey"] = object.orderKey;
        if (Object.keys(object._customValues).length > 0) {
          const packedCustomValues: { [key: string]: any } = {} as any;
          for (const [key, value] of Object.entries(object._customValues)) {
            packedCustomValues[String(key)] = value.pack(1);
          }
          objectJson["customValues"] = packedCustomValues;
        }
        if (object._scriptPtr != null) {
          objectJson["script"] = object._scriptPtr.pack(1);
        }
        if (object.isExtensible != null) {
          objectJson["isExtensible"] = object.isExtensible;
        }
        if (object.sourcePtr != null) {
          objectJson["source"] = object.sourcePtr.pack(1);
        }
        if (object._key != null) {
          objectJson["key"] = object._key;
        }
        if (object._position != null) {
          objectJson["position"] = object._position.pack(1);
        }
        if (object._offset != null) {
          objectJson["offset"] = object._offset.pack(1);
        }
        if (object._scale != null) {
          objectJson["scale"] = object._scale.pack(1);
        }
        if (object._rotation != null) {
          objectJson["rotation"] = object._rotation.pack(1);
        }
        if (object._skew != null) {
          objectJson["skew"] = object._skew.pack(1);
        }
        if (object._origin != null) {
          objectJson["origin"] = object._origin.pack(1);
        }
        if (object._anchor != null) {
          objectJson["anchor"] = Anchor[object._anchor];
        }
        if (object._stroke != null) {
          objectJson["stroke"] = object._stroke.pack(1);
        }
        objectJson["startType"] = ArrowHeadType[object._startType];
        objectJson["start"] = object._start.pack(1);
        objectJson["endType"] = ArrowHeadType[object._endType];
        objectJson["end"] = object._end.pack(1);
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): ArrowShape2D {
        const _Value = STRUCT_CLASS_BY_TYPE[100] as typeof Value;
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const _Stroke = STRUCT_CLASS_BY_TYPE[2101100] as typeof Stroke;
        const _Vector2 = STRUCT_CLASS_BY_TYPE[2400000] as typeof Vector2;
        const _Offset2 = STRUCT_CLASS_BY_TYPE[2400020] as typeof Offset2;
        const strokeValue = objectJson["stroke"];
        const unpackedStroke = strokeValue != undefined ? _Stroke.unpack(1, strokeValue, _session) as Stroke : null;
        const positionValue = objectJson["position"];
        const unpackedPosition = positionValue != undefined ? _Vector2.unpack(1, positionValue, _session) as Vector2 : null;
        const offsetValue = objectJson["offset"];
        const unpackedOffset = offsetValue != undefined ? _Offset2.unpack(1, offsetValue, _session) as Offset2 : null;
        const scaleValue = objectJson["scale"];
        const unpackedScale = scaleValue != undefined ? _Vector2.unpack(1, scaleValue, _session) as Vector2 : null;
        const rotationValue = objectJson["rotation"];
        const unpackedRotation = rotationValue != undefined ? _Vector2.unpack(1, rotationValue, _session) as Vector2 : null;
        const skewValue = objectJson["skew"];
        const unpackedSkew = skewValue != undefined ? _Vector2.unpack(1, skewValue, _session) as Vector2 : null;
        const originValue = objectJson["origin"];
        const unpackedOrigin = originValue != undefined ? _Vector2.unpack(1, originValue, _session) as Vector2 : null;
        const anchorValue = objectJson["anchor"];
        const unpackedAnchor = anchorValue != undefined ? Anchor[anchorValue] as any : null;
        const parentPtrValue = objectJson["parent"];
        const unpackedParentPtr = parentPtrValue != undefined ? _NodeReference.unpack(1, parentPtrValue, _session) as NodeReference : null;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const instancePtrValue = objectJson["instance"];
        const unpackedInstancePtr = instancePtrValue != undefined ? _NodeReference.unpack(1, instancePtrValue, _session) as NodeReference : null;
        const deletedAtValue = objectJson["deletedAt"];
        const unpackedDeletedAt = deletedAtValue != undefined ? Temporal.Instant.from(deletedAtValue).toZonedDateTimeISO('UTC') : null;
        const ownedByPtrValue = objectJson["ownedBy"];
        const unpackedOwnedByPtr = ownedByPtrValue != undefined ? _NodeReference.unpack(1, ownedByPtrValue, _session) as NodeReference : null;
        const unpackedCustomValues = {} as any;
        if (objectJson["customValues"] != undefined) {
          for (const [key, value] of Object.entries(objectJson["customValues"])) {
            unpackedCustomValues[key] = _Value.unpack(1, value as any, _session) as Value;
          }
        }
        const scriptPtrValue = objectJson["script"];
        const unpackedScriptPtr = scriptPtrValue != undefined ? _NodeReference.unpack(1, scriptPtrValue, _session) as NodeReference : null;
        const isExtensibleValue = objectJson["isExtensible"];
        const unpackedIsExtensible = isExtensibleValue != undefined ? isExtensibleValue : null;
        const sourcePtrValue = objectJson["source"];
        const unpackedSourcePtr = sourcePtrValue != undefined ? _NodeReference.unpack(1, sourcePtrValue, _session) as NodeReference : null;
        const keyValue = objectJson["key"];
        const unpackedKey = keyValue != undefined ? keyValue : null;
        return new (NODE_CLASS_BY_TYPE[2410200] as typeof ArrowShape2D)({
          startType: ArrowHeadType[objectJson["startType"]] as any,
          start: _Vector2.unpack(1, objectJson["start"], _session) as Vector2,
          endType: ArrowHeadType[objectJson["endType"]] as any,
          end: _Vector2.unpack(1, objectJson["end"], _session) as Vector2,
          stroke: unpackedStroke,
          position: unpackedPosition,
          offset: unpackedOffset,
          scale: unpackedScale,
          rotation: unpackedRotation,
          skew: unpackedSkew,
          origin: unpackedOrigin,
          anchor: unpackedAnchor,
          parent: unpackedParentPtr,
          materialization: Materialization[objectJson["materialization"]] as any,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          instance: unpackedInstancePtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          updatedAt: Temporal.Instant.from(objectJson["updatedAt"]).toZonedDateTimeISO('UTC'),
          updatedEpoch: Number(objectJson["updatedEpoch"]),
          updatedBy: _NodeReference.unpack(1, objectJson["updatedBy"], _session) as NodeReference,
          deletedAt: unpackedDeletedAt,
          ownedBy: unpackedOwnedByPtr,
          name: objectJson["name"],
          orderKey: objectJson["orderKey"],
          customValues: unpackedCustomValues,
          script: unpackedScriptPtr,
          isExtensible: unpackedIsExtensible,
          source: unpackedSourcePtr,
          key: unpackedKey,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 2410200)] = new ArrowShape2DJsonEncoder();

    class EllipseShape2DJsonEncoder implements _JsonObjectEncoder {
      packObject(object: EllipseShape2D): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "ELLIPSE_SHAPE2D";
        objectJson["id"] = object.id;
        if (object.parentPtr != null) {
          objectJson["parent"] = object.parentPtr.pack(1);
        }
        objectJson["space"] = object.spacePtr.pack(1);
        objectJson["materialization"] = Materialization[object.materialization];
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.instancePtr != null) {
          objectJson["instance"] = object.instancePtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["updatedAt"] = object.updatedAt.toString({ timeZoneName: 'never' });
        objectJson["updatedEpoch"] = Number(object.updatedEpoch);
        objectJson["updatedBy"] = object.updatedByPtr.pack(1);
        if (object.deletedAt != null) {
          objectJson["deletedAt"] = object.deletedAt.toString({ timeZoneName: 'never' });
        }
        if (object._ownedByPtr != null) {
          objectJson["ownedBy"] = object._ownedByPtr.pack(1);
        }
        objectJson["name"] = object._name;
        objectJson["orderKey"] = object.orderKey;
        if (Object.keys(object._customValues).length > 0) {
          const packedCustomValues: { [key: string]: any } = {} as any;
          for (const [key, value] of Object.entries(object._customValues)) {
            packedCustomValues[String(key)] = value.pack(1);
          }
          objectJson["customValues"] = packedCustomValues;
        }
        if (object._scriptPtr != null) {
          objectJson["script"] = object._scriptPtr.pack(1);
        }
        if (object.isExtensible != null) {
          objectJson["isExtensible"] = object.isExtensible;
        }
        if (object.sourcePtr != null) {
          objectJson["source"] = object.sourcePtr.pack(1);
        }
        if (object._key != null) {
          objectJson["key"] = object._key;
        }
        if (object._position != null) {
          objectJson["position"] = object._position.pack(1);
        }
        if (object._offset != null) {
          objectJson["offset"] = object._offset.pack(1);
        }
        if (object._scale != null) {
          objectJson["scale"] = object._scale.pack(1);
        }
        if (object._rotation != null) {
          objectJson["rotation"] = object._rotation.pack(1);
        }
        if (object._skew != null) {
          objectJson["skew"] = object._skew.pack(1);
        }
        if (object._origin != null) {
          objectJson["origin"] = object._origin.pack(1);
        }
        if (object._anchor != null) {
          objectJson["anchor"] = Anchor[object._anchor];
        }
        if (object._stroke != null) {
          objectJson["stroke"] = object._stroke.pack(1);
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): EllipseShape2D {
        const _Value = STRUCT_CLASS_BY_TYPE[100] as typeof Value;
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const _Stroke = STRUCT_CLASS_BY_TYPE[2101100] as typeof Stroke;
        const _Vector2 = STRUCT_CLASS_BY_TYPE[2400000] as typeof Vector2;
        const _Offset2 = STRUCT_CLASS_BY_TYPE[2400020] as typeof Offset2;
        const strokeValue = objectJson["stroke"];
        const unpackedStroke = strokeValue != undefined ? _Stroke.unpack(1, strokeValue, _session) as Stroke : null;
        const positionValue = objectJson["position"];
        const unpackedPosition = positionValue != undefined ? _Vector2.unpack(1, positionValue, _session) as Vector2 : null;
        const offsetValue = objectJson["offset"];
        const unpackedOffset = offsetValue != undefined ? _Offset2.unpack(1, offsetValue, _session) as Offset2 : null;
        const scaleValue = objectJson["scale"];
        const unpackedScale = scaleValue != undefined ? _Vector2.unpack(1, scaleValue, _session) as Vector2 : null;
        const rotationValue = objectJson["rotation"];
        const unpackedRotation = rotationValue != undefined ? _Vector2.unpack(1, rotationValue, _session) as Vector2 : null;
        const skewValue = objectJson["skew"];
        const unpackedSkew = skewValue != undefined ? _Vector2.unpack(1, skewValue, _session) as Vector2 : null;
        const originValue = objectJson["origin"];
        const unpackedOrigin = originValue != undefined ? _Vector2.unpack(1, originValue, _session) as Vector2 : null;
        const anchorValue = objectJson["anchor"];
        const unpackedAnchor = anchorValue != undefined ? Anchor[anchorValue] as any : null;
        const parentPtrValue = objectJson["parent"];
        const unpackedParentPtr = parentPtrValue != undefined ? _NodeReference.unpack(1, parentPtrValue, _session) as NodeReference : null;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const instancePtrValue = objectJson["instance"];
        const unpackedInstancePtr = instancePtrValue != undefined ? _NodeReference.unpack(1, instancePtrValue, _session) as NodeReference : null;
        const deletedAtValue = objectJson["deletedAt"];
        const unpackedDeletedAt = deletedAtValue != undefined ? Temporal.Instant.from(deletedAtValue).toZonedDateTimeISO('UTC') : null;
        const ownedByPtrValue = objectJson["ownedBy"];
        const unpackedOwnedByPtr = ownedByPtrValue != undefined ? _NodeReference.unpack(1, ownedByPtrValue, _session) as NodeReference : null;
        const unpackedCustomValues = {} as any;
        if (objectJson["customValues"] != undefined) {
          for (const [key, value] of Object.entries(objectJson["customValues"])) {
            unpackedCustomValues[key] = _Value.unpack(1, value as any, _session) as Value;
          }
        }
        const scriptPtrValue = objectJson["script"];
        const unpackedScriptPtr = scriptPtrValue != undefined ? _NodeReference.unpack(1, scriptPtrValue, _session) as NodeReference : null;
        const isExtensibleValue = objectJson["isExtensible"];
        const unpackedIsExtensible = isExtensibleValue != undefined ? isExtensibleValue : null;
        const sourcePtrValue = objectJson["source"];
        const unpackedSourcePtr = sourcePtrValue != undefined ? _NodeReference.unpack(1, sourcePtrValue, _session) as NodeReference : null;
        const keyValue = objectJson["key"];
        const unpackedKey = keyValue != undefined ? keyValue : null;
        return new (NODE_CLASS_BY_TYPE[2410400] as typeof EllipseShape2D)({
          stroke: unpackedStroke,
          position: unpackedPosition,
          offset: unpackedOffset,
          scale: unpackedScale,
          rotation: unpackedRotation,
          skew: unpackedSkew,
          origin: unpackedOrigin,
          anchor: unpackedAnchor,
          parent: unpackedParentPtr,
          materialization: Materialization[objectJson["materialization"]] as any,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          instance: unpackedInstancePtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          updatedAt: Temporal.Instant.from(objectJson["updatedAt"]).toZonedDateTimeISO('UTC'),
          updatedEpoch: Number(objectJson["updatedEpoch"]),
          updatedBy: _NodeReference.unpack(1, objectJson["updatedBy"], _session) as NodeReference,
          deletedAt: unpackedDeletedAt,
          ownedBy: unpackedOwnedByPtr,
          name: objectJson["name"],
          orderKey: objectJson["orderKey"],
          customValues: unpackedCustomValues,
          script: unpackedScriptPtr,
          isExtensible: unpackedIsExtensible,
          source: unpackedSourcePtr,
          key: unpackedKey,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 2410400)] = new EllipseShape2DJsonEncoder();

    class LineShape2DJsonEncoder implements _JsonObjectEncoder {
      packObject(object: LineShape2D): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "LINE_SHAPE2D";
        objectJson["id"] = object.id;
        if (object.parentPtr != null) {
          objectJson["parent"] = object.parentPtr.pack(1);
        }
        objectJson["space"] = object.spacePtr.pack(1);
        objectJson["materialization"] = Materialization[object.materialization];
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.instancePtr != null) {
          objectJson["instance"] = object.instancePtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["updatedAt"] = object.updatedAt.toString({ timeZoneName: 'never' });
        objectJson["updatedEpoch"] = Number(object.updatedEpoch);
        objectJson["updatedBy"] = object.updatedByPtr.pack(1);
        if (object.deletedAt != null) {
          objectJson["deletedAt"] = object.deletedAt.toString({ timeZoneName: 'never' });
        }
        if (object._ownedByPtr != null) {
          objectJson["ownedBy"] = object._ownedByPtr.pack(1);
        }
        objectJson["name"] = object._name;
        objectJson["orderKey"] = object.orderKey;
        if (Object.keys(object._customValues).length > 0) {
          const packedCustomValues: { [key: string]: any } = {} as any;
          for (const [key, value] of Object.entries(object._customValues)) {
            packedCustomValues[String(key)] = value.pack(1);
          }
          objectJson["customValues"] = packedCustomValues;
        }
        if (object._scriptPtr != null) {
          objectJson["script"] = object._scriptPtr.pack(1);
        }
        if (object.isExtensible != null) {
          objectJson["isExtensible"] = object.isExtensible;
        }
        if (object.sourcePtr != null) {
          objectJson["source"] = object.sourcePtr.pack(1);
        }
        if (object._key != null) {
          objectJson["key"] = object._key;
        }
        if (object._position != null) {
          objectJson["position"] = object._position.pack(1);
        }
        if (object._offset != null) {
          objectJson["offset"] = object._offset.pack(1);
        }
        if (object._scale != null) {
          objectJson["scale"] = object._scale.pack(1);
        }
        if (object._rotation != null) {
          objectJson["rotation"] = object._rotation.pack(1);
        }
        if (object._skew != null) {
          objectJson["skew"] = object._skew.pack(1);
        }
        if (object._origin != null) {
          objectJson["origin"] = object._origin.pack(1);
        }
        if (object._anchor != null) {
          objectJson["anchor"] = Anchor[object._anchor];
        }
        if (object._stroke != null) {
          objectJson["stroke"] = object._stroke.pack(1);
        }
        objectJson["start"] = object._start.pack(1);
        objectJson["end"] = object._end.pack(1);
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): LineShape2D {
        const _Value = STRUCT_CLASS_BY_TYPE[100] as typeof Value;
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const _Stroke = STRUCT_CLASS_BY_TYPE[2101100] as typeof Stroke;
        const _Vector2 = STRUCT_CLASS_BY_TYPE[2400000] as typeof Vector2;
        const _Offset2 = STRUCT_CLASS_BY_TYPE[2400020] as typeof Offset2;
        const strokeValue = objectJson["stroke"];
        const unpackedStroke = strokeValue != undefined ? _Stroke.unpack(1, strokeValue, _session) as Stroke : null;
        const positionValue = objectJson["position"];
        const unpackedPosition = positionValue != undefined ? _Vector2.unpack(1, positionValue, _session) as Vector2 : null;
        const offsetValue = objectJson["offset"];
        const unpackedOffset = offsetValue != undefined ? _Offset2.unpack(1, offsetValue, _session) as Offset2 : null;
        const scaleValue = objectJson["scale"];
        const unpackedScale = scaleValue != undefined ? _Vector2.unpack(1, scaleValue, _session) as Vector2 : null;
        const rotationValue = objectJson["rotation"];
        const unpackedRotation = rotationValue != undefined ? _Vector2.unpack(1, rotationValue, _session) as Vector2 : null;
        const skewValue = objectJson["skew"];
        const unpackedSkew = skewValue != undefined ? _Vector2.unpack(1, skewValue, _session) as Vector2 : null;
        const originValue = objectJson["origin"];
        const unpackedOrigin = originValue != undefined ? _Vector2.unpack(1, originValue, _session) as Vector2 : null;
        const anchorValue = objectJson["anchor"];
        const unpackedAnchor = anchorValue != undefined ? Anchor[anchorValue] as any : null;
        const parentPtrValue = objectJson["parent"];
        const unpackedParentPtr = parentPtrValue != undefined ? _NodeReference.unpack(1, parentPtrValue, _session) as NodeReference : null;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const instancePtrValue = objectJson["instance"];
        const unpackedInstancePtr = instancePtrValue != undefined ? _NodeReference.unpack(1, instancePtrValue, _session) as NodeReference : null;
        const deletedAtValue = objectJson["deletedAt"];
        const unpackedDeletedAt = deletedAtValue != undefined ? Temporal.Instant.from(deletedAtValue).toZonedDateTimeISO('UTC') : null;
        const ownedByPtrValue = objectJson["ownedBy"];
        const unpackedOwnedByPtr = ownedByPtrValue != undefined ? _NodeReference.unpack(1, ownedByPtrValue, _session) as NodeReference : null;
        const unpackedCustomValues = {} as any;
        if (objectJson["customValues"] != undefined) {
          for (const [key, value] of Object.entries(objectJson["customValues"])) {
            unpackedCustomValues[key] = _Value.unpack(1, value as any, _session) as Value;
          }
        }
        const scriptPtrValue = objectJson["script"];
        const unpackedScriptPtr = scriptPtrValue != undefined ? _NodeReference.unpack(1, scriptPtrValue, _session) as NodeReference : null;
        const isExtensibleValue = objectJson["isExtensible"];
        const unpackedIsExtensible = isExtensibleValue != undefined ? isExtensibleValue : null;
        const sourcePtrValue = objectJson["source"];
        const unpackedSourcePtr = sourcePtrValue != undefined ? _NodeReference.unpack(1, sourcePtrValue, _session) as NodeReference : null;
        const keyValue = objectJson["key"];
        const unpackedKey = keyValue != undefined ? keyValue : null;
        return new (NODE_CLASS_BY_TYPE[2410100] as typeof LineShape2D)({
          start: _Vector2.unpack(1, objectJson["start"], _session) as Vector2,
          end: _Vector2.unpack(1, objectJson["end"], _session) as Vector2,
          stroke: unpackedStroke,
          position: unpackedPosition,
          offset: unpackedOffset,
          scale: unpackedScale,
          rotation: unpackedRotation,
          skew: unpackedSkew,
          origin: unpackedOrigin,
          anchor: unpackedAnchor,
          parent: unpackedParentPtr,
          materialization: Materialization[objectJson["materialization"]] as any,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          instance: unpackedInstancePtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          updatedAt: Temporal.Instant.from(objectJson["updatedAt"]).toZonedDateTimeISO('UTC'),
          updatedEpoch: Number(objectJson["updatedEpoch"]),
          updatedBy: _NodeReference.unpack(1, objectJson["updatedBy"], _session) as NodeReference,
          deletedAt: unpackedDeletedAt,
          ownedBy: unpackedOwnedByPtr,
          name: objectJson["name"],
          orderKey: objectJson["orderKey"],
          customValues: unpackedCustomValues,
          script: unpackedScriptPtr,
          isExtensible: unpackedIsExtensible,
          source: unpackedSourcePtr,
          key: unpackedKey,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 2410100)] = new LineShape2DJsonEncoder();

    class PathShape2DJsonEncoder implements _JsonObjectEncoder {
      packObject(object: PathShape2D): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "PATH_SHAPE2D";
        objectJson["id"] = object.id;
        if (object.parentPtr != null) {
          objectJson["parent"] = object.parentPtr.pack(1);
        }
        objectJson["space"] = object.spacePtr.pack(1);
        objectJson["materialization"] = Materialization[object.materialization];
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.instancePtr != null) {
          objectJson["instance"] = object.instancePtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["updatedAt"] = object.updatedAt.toString({ timeZoneName: 'never' });
        objectJson["updatedEpoch"] = Number(object.updatedEpoch);
        objectJson["updatedBy"] = object.updatedByPtr.pack(1);
        if (object.deletedAt != null) {
          objectJson["deletedAt"] = object.deletedAt.toString({ timeZoneName: 'never' });
        }
        if (object._ownedByPtr != null) {
          objectJson["ownedBy"] = object._ownedByPtr.pack(1);
        }
        objectJson["name"] = object._name;
        objectJson["orderKey"] = object.orderKey;
        if (Object.keys(object._customValues).length > 0) {
          const packedCustomValues: { [key: string]: any } = {} as any;
          for (const [key, value] of Object.entries(object._customValues)) {
            packedCustomValues[String(key)] = value.pack(1);
          }
          objectJson["customValues"] = packedCustomValues;
        }
        if (object._scriptPtr != null) {
          objectJson["script"] = object._scriptPtr.pack(1);
        }
        if (object.isExtensible != null) {
          objectJson["isExtensible"] = object.isExtensible;
        }
        if (object.sourcePtr != null) {
          objectJson["source"] = object.sourcePtr.pack(1);
        }
        if (object._key != null) {
          objectJson["key"] = object._key;
        }
        if (object._position != null) {
          objectJson["position"] = object._position.pack(1);
        }
        if (object._offset != null) {
          objectJson["offset"] = object._offset.pack(1);
        }
        if (object._scale != null) {
          objectJson["scale"] = object._scale.pack(1);
        }
        if (object._rotation != null) {
          objectJson["rotation"] = object._rotation.pack(1);
        }
        if (object._skew != null) {
          objectJson["skew"] = object._skew.pack(1);
        }
        if (object._origin != null) {
          objectJson["origin"] = object._origin.pack(1);
        }
        if (object._anchor != null) {
          objectJson["anchor"] = Anchor[object._anchor];
        }
        if (object._stroke != null) {
          objectJson["stroke"] = object._stroke.pack(1);
        }
        if (object._points.length > 0) {
          const packedPoints: any[] = [];
          for (const item of object._points) {
            packedPoints.push(item.pack(1));
          }
          objectJson["points"] = packedPoints;
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): PathShape2D {
        const _Value = STRUCT_CLASS_BY_TYPE[100] as typeof Value;
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const _Stroke = STRUCT_CLASS_BY_TYPE[2101100] as typeof Stroke;
        const _Vector2 = STRUCT_CLASS_BY_TYPE[2400000] as typeof Vector2;
        const _Offset2 = STRUCT_CLASS_BY_TYPE[2400020] as typeof Offset2;
        const unpackedPoints: any[] = [];
        if (objectJson["points"] != undefined) {
          for (const item of objectJson["points"]) {
            unpackedPoints.push(_Vector2.unpack(1, item, _session) as Vector2)
          }
        }
        const strokeValue = objectJson["stroke"];
        const unpackedStroke = strokeValue != undefined ? _Stroke.unpack(1, strokeValue, _session) as Stroke : null;
        const positionValue = objectJson["position"];
        const unpackedPosition = positionValue != undefined ? _Vector2.unpack(1, positionValue, _session) as Vector2 : null;
        const offsetValue = objectJson["offset"];
        const unpackedOffset = offsetValue != undefined ? _Offset2.unpack(1, offsetValue, _session) as Offset2 : null;
        const scaleValue = objectJson["scale"];
        const unpackedScale = scaleValue != undefined ? _Vector2.unpack(1, scaleValue, _session) as Vector2 : null;
        const rotationValue = objectJson["rotation"];
        const unpackedRotation = rotationValue != undefined ? _Vector2.unpack(1, rotationValue, _session) as Vector2 : null;
        const skewValue = objectJson["skew"];
        const unpackedSkew = skewValue != undefined ? _Vector2.unpack(1, skewValue, _session) as Vector2 : null;
        const originValue = objectJson["origin"];
        const unpackedOrigin = originValue != undefined ? _Vector2.unpack(1, originValue, _session) as Vector2 : null;
        const anchorValue = objectJson["anchor"];
        const unpackedAnchor = anchorValue != undefined ? Anchor[anchorValue] as any : null;
        const parentPtrValue = objectJson["parent"];
        const unpackedParentPtr = parentPtrValue != undefined ? _NodeReference.unpack(1, parentPtrValue, _session) as NodeReference : null;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const instancePtrValue = objectJson["instance"];
        const unpackedInstancePtr = instancePtrValue != undefined ? _NodeReference.unpack(1, instancePtrValue, _session) as NodeReference : null;
        const deletedAtValue = objectJson["deletedAt"];
        const unpackedDeletedAt = deletedAtValue != undefined ? Temporal.Instant.from(deletedAtValue).toZonedDateTimeISO('UTC') : null;
        const ownedByPtrValue = objectJson["ownedBy"];
        const unpackedOwnedByPtr = ownedByPtrValue != undefined ? _NodeReference.unpack(1, ownedByPtrValue, _session) as NodeReference : null;
        const unpackedCustomValues = {} as any;
        if (objectJson["customValues"] != undefined) {
          for (const [key, value] of Object.entries(objectJson["customValues"])) {
            unpackedCustomValues[key] = _Value.unpack(1, value as any, _session) as Value;
          }
        }
        const scriptPtrValue = objectJson["script"];
        const unpackedScriptPtr = scriptPtrValue != undefined ? _NodeReference.unpack(1, scriptPtrValue, _session) as NodeReference : null;
        const isExtensibleValue = objectJson["isExtensible"];
        const unpackedIsExtensible = isExtensibleValue != undefined ? isExtensibleValue : null;
        const sourcePtrValue = objectJson["source"];
        const unpackedSourcePtr = sourcePtrValue != undefined ? _NodeReference.unpack(1, sourcePtrValue, _session) as NodeReference : null;
        const keyValue = objectJson["key"];
        const unpackedKey = keyValue != undefined ? keyValue : null;
        return new (NODE_CLASS_BY_TYPE[2410600] as typeof PathShape2D)({
          points: unpackedPoints,
          stroke: unpackedStroke,
          position: unpackedPosition,
          offset: unpackedOffset,
          scale: unpackedScale,
          rotation: unpackedRotation,
          skew: unpackedSkew,
          origin: unpackedOrigin,
          anchor: unpackedAnchor,
          parent: unpackedParentPtr,
          materialization: Materialization[objectJson["materialization"]] as any,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          instance: unpackedInstancePtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          updatedAt: Temporal.Instant.from(objectJson["updatedAt"]).toZonedDateTimeISO('UTC'),
          updatedEpoch: Number(objectJson["updatedEpoch"]),
          updatedBy: _NodeReference.unpack(1, objectJson["updatedBy"], _session) as NodeReference,
          deletedAt: unpackedDeletedAt,
          ownedBy: unpackedOwnedByPtr,
          name: objectJson["name"],
          orderKey: objectJson["orderKey"],
          customValues: unpackedCustomValues,
          script: unpackedScriptPtr,
          isExtensible: unpackedIsExtensible,
          source: unpackedSourcePtr,
          key: unpackedKey,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 2410600)] = new PathShape2DJsonEncoder();

    class PolygonShape2DJsonEncoder implements _JsonObjectEncoder {
      packObject(object: PolygonShape2D): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "POLYGON_SHAPE2D";
        objectJson["id"] = object.id;
        if (object.parentPtr != null) {
          objectJson["parent"] = object.parentPtr.pack(1);
        }
        objectJson["space"] = object.spacePtr.pack(1);
        objectJson["materialization"] = Materialization[object.materialization];
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.instancePtr != null) {
          objectJson["instance"] = object.instancePtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["updatedAt"] = object.updatedAt.toString({ timeZoneName: 'never' });
        objectJson["updatedEpoch"] = Number(object.updatedEpoch);
        objectJson["updatedBy"] = object.updatedByPtr.pack(1);
        if (object.deletedAt != null) {
          objectJson["deletedAt"] = object.deletedAt.toString({ timeZoneName: 'never' });
        }
        if (object._ownedByPtr != null) {
          objectJson["ownedBy"] = object._ownedByPtr.pack(1);
        }
        objectJson["name"] = object._name;
        objectJson["orderKey"] = object.orderKey;
        if (Object.keys(object._customValues).length > 0) {
          const packedCustomValues: { [key: string]: any } = {} as any;
          for (const [key, value] of Object.entries(object._customValues)) {
            packedCustomValues[String(key)] = value.pack(1);
          }
          objectJson["customValues"] = packedCustomValues;
        }
        if (object._scriptPtr != null) {
          objectJson["script"] = object._scriptPtr.pack(1);
        }
        if (object.isExtensible != null) {
          objectJson["isExtensible"] = object.isExtensible;
        }
        if (object.sourcePtr != null) {
          objectJson["source"] = object.sourcePtr.pack(1);
        }
        if (object._key != null) {
          objectJson["key"] = object._key;
        }
        if (object._position != null) {
          objectJson["position"] = object._position.pack(1);
        }
        if (object._offset != null) {
          objectJson["offset"] = object._offset.pack(1);
        }
        if (object._scale != null) {
          objectJson["scale"] = object._scale.pack(1);
        }
        if (object._rotation != null) {
          objectJson["rotation"] = object._rotation.pack(1);
        }
        if (object._skew != null) {
          objectJson["skew"] = object._skew.pack(1);
        }
        if (object._origin != null) {
          objectJson["origin"] = object._origin.pack(1);
        }
        if (object._anchor != null) {
          objectJson["anchor"] = Anchor[object._anchor];
        }
        if (object._stroke != null) {
          objectJson["stroke"] = object._stroke.pack(1);
        }
        if (object._points.length > 0) {
          const packedPoints: any[] = [];
          for (const item of object._points) {
            packedPoints.push(item.pack(1));
          }
          objectJson["points"] = packedPoints;
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): PolygonShape2D {
        const _Value = STRUCT_CLASS_BY_TYPE[100] as typeof Value;
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const _Stroke = STRUCT_CLASS_BY_TYPE[2101100] as typeof Stroke;
        const _Vector2 = STRUCT_CLASS_BY_TYPE[2400000] as typeof Vector2;
        const _Offset2 = STRUCT_CLASS_BY_TYPE[2400020] as typeof Offset2;
        const unpackedPoints: any[] = [];
        if (objectJson["points"] != undefined) {
          for (const item of objectJson["points"]) {
            unpackedPoints.push(_Vector2.unpack(1, item, _session) as Vector2)
          }
        }
        const strokeValue = objectJson["stroke"];
        const unpackedStroke = strokeValue != undefined ? _Stroke.unpack(1, strokeValue, _session) as Stroke : null;
        const positionValue = objectJson["position"];
        const unpackedPosition = positionValue != undefined ? _Vector2.unpack(1, positionValue, _session) as Vector2 : null;
        const offsetValue = objectJson["offset"];
        const unpackedOffset = offsetValue != undefined ? _Offset2.unpack(1, offsetValue, _session) as Offset2 : null;
        const scaleValue = objectJson["scale"];
        const unpackedScale = scaleValue != undefined ? _Vector2.unpack(1, scaleValue, _session) as Vector2 : null;
        const rotationValue = objectJson["rotation"];
        const unpackedRotation = rotationValue != undefined ? _Vector2.unpack(1, rotationValue, _session) as Vector2 : null;
        const skewValue = objectJson["skew"];
        const unpackedSkew = skewValue != undefined ? _Vector2.unpack(1, skewValue, _session) as Vector2 : null;
        const originValue = objectJson["origin"];
        const unpackedOrigin = originValue != undefined ? _Vector2.unpack(1, originValue, _session) as Vector2 : null;
        const anchorValue = objectJson["anchor"];
        const unpackedAnchor = anchorValue != undefined ? Anchor[anchorValue] as any : null;
        const parentPtrValue = objectJson["parent"];
        const unpackedParentPtr = parentPtrValue != undefined ? _NodeReference.unpack(1, parentPtrValue, _session) as NodeReference : null;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const instancePtrValue = objectJson["instance"];
        const unpackedInstancePtr = instancePtrValue != undefined ? _NodeReference.unpack(1, instancePtrValue, _session) as NodeReference : null;
        const deletedAtValue = objectJson["deletedAt"];
        const unpackedDeletedAt = deletedAtValue != undefined ? Temporal.Instant.from(deletedAtValue).toZonedDateTimeISO('UTC') : null;
        const ownedByPtrValue = objectJson["ownedBy"];
        const unpackedOwnedByPtr = ownedByPtrValue != undefined ? _NodeReference.unpack(1, ownedByPtrValue, _session) as NodeReference : null;
        const unpackedCustomValues = {} as any;
        if (objectJson["customValues"] != undefined) {
          for (const [key, value] of Object.entries(objectJson["customValues"])) {
            unpackedCustomValues[key] = _Value.unpack(1, value as any, _session) as Value;
          }
        }
        const scriptPtrValue = objectJson["script"];
        const unpackedScriptPtr = scriptPtrValue != undefined ? _NodeReference.unpack(1, scriptPtrValue, _session) as NodeReference : null;
        const isExtensibleValue = objectJson["isExtensible"];
        const unpackedIsExtensible = isExtensibleValue != undefined ? isExtensibleValue : null;
        const sourcePtrValue = objectJson["source"];
        const unpackedSourcePtr = sourcePtrValue != undefined ? _NodeReference.unpack(1, sourcePtrValue, _session) as NodeReference : null;
        const keyValue = objectJson["key"];
        const unpackedKey = keyValue != undefined ? keyValue : null;
        return new (NODE_CLASS_BY_TYPE[2410500] as typeof PolygonShape2D)({
          points: unpackedPoints,
          stroke: unpackedStroke,
          position: unpackedPosition,
          offset: unpackedOffset,
          scale: unpackedScale,
          rotation: unpackedRotation,
          skew: unpackedSkew,
          origin: unpackedOrigin,
          anchor: unpackedAnchor,
          parent: unpackedParentPtr,
          materialization: Materialization[objectJson["materialization"]] as any,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          instance: unpackedInstancePtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          updatedAt: Temporal.Instant.from(objectJson["updatedAt"]).toZonedDateTimeISO('UTC'),
          updatedEpoch: Number(objectJson["updatedEpoch"]),
          updatedBy: _NodeReference.unpack(1, objectJson["updatedBy"], _session) as NodeReference,
          deletedAt: unpackedDeletedAt,
          ownedBy: unpackedOwnedByPtr,
          name: objectJson["name"],
          orderKey: objectJson["orderKey"],
          customValues: unpackedCustomValues,
          script: unpackedScriptPtr,
          isExtensible: unpackedIsExtensible,
          source: unpackedSourcePtr,
          key: unpackedKey,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 2410500)] = new PolygonShape2DJsonEncoder();

    class RectangleShape2DJsonEncoder implements _JsonObjectEncoder {
      packObject(object: RectangleShape2D): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "RECTANGLE_SHAPE2D";
        objectJson["id"] = object.id;
        if (object.parentPtr != null) {
          objectJson["parent"] = object.parentPtr.pack(1);
        }
        objectJson["space"] = object.spacePtr.pack(1);
        objectJson["materialization"] = Materialization[object.materialization];
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.instancePtr != null) {
          objectJson["instance"] = object.instancePtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["updatedAt"] = object.updatedAt.toString({ timeZoneName: 'never' });
        objectJson["updatedEpoch"] = Number(object.updatedEpoch);
        objectJson["updatedBy"] = object.updatedByPtr.pack(1);
        if (object.deletedAt != null) {
          objectJson["deletedAt"] = object.deletedAt.toString({ timeZoneName: 'never' });
        }
        if (object._ownedByPtr != null) {
          objectJson["ownedBy"] = object._ownedByPtr.pack(1);
        }
        objectJson["name"] = object._name;
        objectJson["orderKey"] = object.orderKey;
        if (Object.keys(object._customValues).length > 0) {
          const packedCustomValues: { [key: string]: any } = {} as any;
          for (const [key, value] of Object.entries(object._customValues)) {
            packedCustomValues[String(key)] = value.pack(1);
          }
          objectJson["customValues"] = packedCustomValues;
        }
        if (object._scriptPtr != null) {
          objectJson["script"] = object._scriptPtr.pack(1);
        }
        if (object.isExtensible != null) {
          objectJson["isExtensible"] = object.isExtensible;
        }
        if (object.sourcePtr != null) {
          objectJson["source"] = object.sourcePtr.pack(1);
        }
        if (object._key != null) {
          objectJson["key"] = object._key;
        }
        if (object._position != null) {
          objectJson["position"] = object._position.pack(1);
        }
        if (object._offset != null) {
          objectJson["offset"] = object._offset.pack(1);
        }
        if (object._scale != null) {
          objectJson["scale"] = object._scale.pack(1);
        }
        if (object._rotation != null) {
          objectJson["rotation"] = object._rotation.pack(1);
        }
        if (object._skew != null) {
          objectJson["skew"] = object._skew.pack(1);
        }
        if (object._origin != null) {
          objectJson["origin"] = object._origin.pack(1);
        }
        if (object._anchor != null) {
          objectJson["anchor"] = Anchor[object._anchor];
        }
        if (object._stroke != null) {
          objectJson["stroke"] = object._stroke.pack(1);
        }
        if (object._width != null) {
          objectJson["width"] = object._width.pack(1);
        }
        if (object._height != null) {
          objectJson["height"] = object._height.pack(1);
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): RectangleShape2D {
        const _Value = STRUCT_CLASS_BY_TYPE[100] as typeof Value;
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const _Stroke = STRUCT_CLASS_BY_TYPE[2101100] as typeof Stroke;
        const _Vector2 = STRUCT_CLASS_BY_TYPE[2400000] as typeof Vector2;
        const _Offset2 = STRUCT_CLASS_BY_TYPE[2400020] as typeof Offset2;
        const widthValue = objectJson["width"];
        const unpackedWidth = widthValue != undefined ? _Vector2.unpack(1, widthValue, _session) as Vector2 : null;
        const heightValue = objectJson["height"];
        const unpackedHeight = heightValue != undefined ? _Vector2.unpack(1, heightValue, _session) as Vector2 : null;
        const strokeValue = objectJson["stroke"];
        const unpackedStroke = strokeValue != undefined ? _Stroke.unpack(1, strokeValue, _session) as Stroke : null;
        const positionValue = objectJson["position"];
        const unpackedPosition = positionValue != undefined ? _Vector2.unpack(1, positionValue, _session) as Vector2 : null;
        const offsetValue = objectJson["offset"];
        const unpackedOffset = offsetValue != undefined ? _Offset2.unpack(1, offsetValue, _session) as Offset2 : null;
        const scaleValue = objectJson["scale"];
        const unpackedScale = scaleValue != undefined ? _Vector2.unpack(1, scaleValue, _session) as Vector2 : null;
        const rotationValue = objectJson["rotation"];
        const unpackedRotation = rotationValue != undefined ? _Vector2.unpack(1, rotationValue, _session) as Vector2 : null;
        const skewValue = objectJson["skew"];
        const unpackedSkew = skewValue != undefined ? _Vector2.unpack(1, skewValue, _session) as Vector2 : null;
        const originValue = objectJson["origin"];
        const unpackedOrigin = originValue != undefined ? _Vector2.unpack(1, originValue, _session) as Vector2 : null;
        const anchorValue = objectJson["anchor"];
        const unpackedAnchor = anchorValue != undefined ? Anchor[anchorValue] as any : null;
        const parentPtrValue = objectJson["parent"];
        const unpackedParentPtr = parentPtrValue != undefined ? _NodeReference.unpack(1, parentPtrValue, _session) as NodeReference : null;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const instancePtrValue = objectJson["instance"];
        const unpackedInstancePtr = instancePtrValue != undefined ? _NodeReference.unpack(1, instancePtrValue, _session) as NodeReference : null;
        const deletedAtValue = objectJson["deletedAt"];
        const unpackedDeletedAt = deletedAtValue != undefined ? Temporal.Instant.from(deletedAtValue).toZonedDateTimeISO('UTC') : null;
        const ownedByPtrValue = objectJson["ownedBy"];
        const unpackedOwnedByPtr = ownedByPtrValue != undefined ? _NodeReference.unpack(1, ownedByPtrValue, _session) as NodeReference : null;
        const unpackedCustomValues = {} as any;
        if (objectJson["customValues"] != undefined) {
          for (const [key, value] of Object.entries(objectJson["customValues"])) {
            unpackedCustomValues[key] = _Value.unpack(1, value as any, _session) as Value;
          }
        }
        const scriptPtrValue = objectJson["script"];
        const unpackedScriptPtr = scriptPtrValue != undefined ? _NodeReference.unpack(1, scriptPtrValue, _session) as NodeReference : null;
        const isExtensibleValue = objectJson["isExtensible"];
        const unpackedIsExtensible = isExtensibleValue != undefined ? isExtensibleValue : null;
        const sourcePtrValue = objectJson["source"];
        const unpackedSourcePtr = sourcePtrValue != undefined ? _NodeReference.unpack(1, sourcePtrValue, _session) as NodeReference : null;
        const keyValue = objectJson["key"];
        const unpackedKey = keyValue != undefined ? keyValue : null;
        return new (NODE_CLASS_BY_TYPE[2410300] as typeof RectangleShape2D)({
          width: unpackedWidth,
          height: unpackedHeight,
          stroke: unpackedStroke,
          position: unpackedPosition,
          offset: unpackedOffset,
          scale: unpackedScale,
          rotation: unpackedRotation,
          skew: unpackedSkew,
          origin: unpackedOrigin,
          anchor: unpackedAnchor,
          parent: unpackedParentPtr,
          materialization: Materialization[objectJson["materialization"]] as any,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          instance: unpackedInstancePtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          updatedAt: Temporal.Instant.from(objectJson["updatedAt"]).toZonedDateTimeISO('UTC'),
          updatedEpoch: Number(objectJson["updatedEpoch"]),
          updatedBy: _NodeReference.unpack(1, objectJson["updatedBy"], _session) as NodeReference,
          deletedAt: unpackedDeletedAt,
          ownedBy: unpackedOwnedByPtr,
          name: objectJson["name"],
          orderKey: objectJson["orderKey"],
          customValues: unpackedCustomValues,
          script: unpackedScriptPtr,
          isExtensible: unpackedIsExtensible,
          source: unpackedSourcePtr,
          key: unpackedKey,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 2410300)] = new RectangleShape2DJsonEncoder();

    class DatabaseJsonEncoder implements _JsonObjectEncoder {
      packObject(object: Database): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "DATABASE";
        objectJson["id"] = object.id;
        if (object.parentPtr != null) {
          objectJson["parent"] = object.parentPtr.pack(1);
        }
        objectJson["space"] = object.spacePtr.pack(1);
        objectJson["materialization"] = Materialization[object.materialization];
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.instancePtr != null) {
          objectJson["instance"] = object.instancePtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["updatedAt"] = object.updatedAt.toString({ timeZoneName: 'never' });
        objectJson["updatedEpoch"] = Number(object.updatedEpoch);
        objectJson["updatedBy"] = object.updatedByPtr.pack(1);
        if (object.deletedAt != null) {
          objectJson["deletedAt"] = object.deletedAt.toString({ timeZoneName: 'never' });
        }
        if (object._ownedByPtr != null) {
          objectJson["ownedBy"] = object._ownedByPtr.pack(1);
        }
        objectJson["name"] = object._name;
        objectJson["orderKey"] = object.orderKey;
        if (Object.keys(object._customValues).length > 0) {
          const packedCustomValues: { [key: string]: any } = {} as any;
          for (const [key, value] of Object.entries(object._customValues)) {
            packedCustomValues[String(key)] = value.pack(1);
          }
          objectJson["customValues"] = packedCustomValues;
        }
        if (object._scriptPtr != null) {
          objectJson["script"] = object._scriptPtr.pack(1);
        }
        if (object.isExtensible != null) {
          objectJson["isExtensible"] = object.isExtensible;
        }
        if (object.sourcePtr != null) {
          objectJson["source"] = object.sourcePtr.pack(1);
        }
        if (object._key != null) {
          objectJson["key"] = object._key;
        }
        objectJson["type"] = DatabaseType[object._type];
        if (object._icon != null) {
          objectJson["icon"] = object._icon.pack(1);
        }
        if (object._status != null) {
          objectJson["status"] = ResourceStatus[object._status];
        }
        if (object._region != null) {
          objectJson["region"] = Region[object._region];
        }
        if (object._galaxyName != null) {
          objectJson["galaxyName"] = object._galaxyName;
        }
        objectJson["externalName"] = object._externalName;
        if (object._customSchemaName != null) {
          objectJson["customSchemaName"] = object._customSchemaName;
        }
        objectJson["tenancy"] = Tenancy[object._tenancy];
        if (object._connectionUrl != null) {
          objectJson["connectionUrl"] = object._connectionUrl;
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): Database {
        const _Value = STRUCT_CLASS_BY_TYPE[100] as typeof Value;
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const _Icon = STRUCT_CLASS_BY_TYPE[400031] as typeof Icon;
        const iconValue = objectJson["icon"];
        const unpackedIcon = iconValue != undefined ? _Icon.unpack(1, iconValue, _session) as Icon : null;
        const galaxyNameValue = objectJson["galaxyName"];
        const unpackedGalaxyName = galaxyNameValue != undefined ? galaxyNameValue : null;
        const customSchemaNameValue = objectJson["customSchemaName"];
        const unpackedCustomSchemaName = customSchemaNameValue != undefined ? customSchemaNameValue : null;
        const connectionUrlValue = objectJson["connectionUrl"];
        const unpackedConnectionUrl = connectionUrlValue != undefined ? connectionUrlValue : null;
        const statusValue = objectJson["status"];
        const unpackedStatus = statusValue != undefined ? ResourceStatus[statusValue] as any : null;
        const regionValue = objectJson["region"];
        const unpackedRegion = regionValue != undefined ? Region[regionValue] as any : null;
        const parentPtrValue = objectJson["parent"];
        const unpackedParentPtr = parentPtrValue != undefined ? _NodeReference.unpack(1, parentPtrValue, _session) as NodeReference : null;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const instancePtrValue = objectJson["instance"];
        const unpackedInstancePtr = instancePtrValue != undefined ? _NodeReference.unpack(1, instancePtrValue, _session) as NodeReference : null;
        const deletedAtValue = objectJson["deletedAt"];
        const unpackedDeletedAt = deletedAtValue != undefined ? Temporal.Instant.from(deletedAtValue).toZonedDateTimeISO('UTC') : null;
        const ownedByPtrValue = objectJson["ownedBy"];
        const unpackedOwnedByPtr = ownedByPtrValue != undefined ? _NodeReference.unpack(1, ownedByPtrValue, _session) as NodeReference : null;
        const unpackedCustomValues = {} as any;
        if (objectJson["customValues"] != undefined) {
          for (const [key, value] of Object.entries(objectJson["customValues"])) {
            unpackedCustomValues[key] = _Value.unpack(1, value as any, _session) as Value;
          }
        }
        const scriptPtrValue = objectJson["script"];
        const unpackedScriptPtr = scriptPtrValue != undefined ? _NodeReference.unpack(1, scriptPtrValue, _session) as NodeReference : null;
        const isExtensibleValue = objectJson["isExtensible"];
        const unpackedIsExtensible = isExtensibleValue != undefined ? isExtensibleValue : null;
        const sourcePtrValue = objectJson["source"];
        const unpackedSourcePtr = sourcePtrValue != undefined ? _NodeReference.unpack(1, sourcePtrValue, _session) as NodeReference : null;
        const keyValue = objectJson["key"];
        const unpackedKey = keyValue != undefined ? keyValue : null;
        return new (NODE_CLASS_BY_TYPE[1000000] as typeof Database)({
          type: DatabaseType[objectJson["type"]] as any,
          icon: unpackedIcon,
          galaxyName: unpackedGalaxyName,
          externalName: objectJson["externalName"],
          customSchemaName: unpackedCustomSchemaName,
          tenancy: Tenancy[objectJson["tenancy"]] as any,
          connectionUrl: unpackedConnectionUrl,
          status: unpackedStatus,
          region: unpackedRegion,
          parent: unpackedParentPtr,
          materialization: Materialization[objectJson["materialization"]] as any,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          instance: unpackedInstancePtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          updatedAt: Temporal.Instant.from(objectJson["updatedAt"]).toZonedDateTimeISO('UTC'),
          updatedEpoch: Number(objectJson["updatedEpoch"]),
          updatedBy: _NodeReference.unpack(1, objectJson["updatedBy"], _session) as NodeReference,
          deletedAt: unpackedDeletedAt,
          ownedBy: unpackedOwnedByPtr,
          name: objectJson["name"],
          orderKey: objectJson["orderKey"],
          customValues: unpackedCustomValues,
          script: unpackedScriptPtr,
          isExtensible: unpackedIsExtensible,
          source: unpackedSourcePtr,
          key: unpackedKey,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 1000000)] = new DatabaseJsonEncoder();

    class MachineJsonEncoder implements _JsonObjectEncoder {
      packObject(object: Machine): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "MACHINE";
        objectJson["id"] = object.id;
        if (object.parentPtr != null) {
          objectJson["parent"] = object.parentPtr.pack(1);
        }
        objectJson["space"] = object.spacePtr.pack(1);
        objectJson["materialization"] = Materialization[object.materialization];
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.instancePtr != null) {
          objectJson["instance"] = object.instancePtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["updatedAt"] = object.updatedAt.toString({ timeZoneName: 'never' });
        objectJson["updatedEpoch"] = Number(object.updatedEpoch);
        objectJson["updatedBy"] = object.updatedByPtr.pack(1);
        if (object.deletedAt != null) {
          objectJson["deletedAt"] = object.deletedAt.toString({ timeZoneName: 'never' });
        }
        if (object._ownedByPtr != null) {
          objectJson["ownedBy"] = object._ownedByPtr.pack(1);
        }
        objectJson["name"] = object._name;
        objectJson["orderKey"] = object.orderKey;
        if (Object.keys(object._customValues).length > 0) {
          const packedCustomValues: { [key: string]: any } = {} as any;
          for (const [key, value] of Object.entries(object._customValues)) {
            packedCustomValues[String(key)] = value.pack(1);
          }
          objectJson["customValues"] = packedCustomValues;
        }
        if (object._scriptPtr != null) {
          objectJson["script"] = object._scriptPtr.pack(1);
        }
        if (object.isExtensible != null) {
          objectJson["isExtensible"] = object.isExtensible;
        }
        if (object.sourcePtr != null) {
          objectJson["source"] = object.sourcePtr.pack(1);
        }
        if (object._key != null) {
          objectJson["key"] = object._key;
        }
        objectJson["type"] = MachineType[object._type];
        if (object._status != null) {
          objectJson["status"] = ResourceStatus[object._status];
        }
        if (object._region != null) {
          objectJson["region"] = Region[object._region];
        }
        objectJson["version"] = object._version;
        if (object._externalName != null) {
          objectJson["externalName"] = object._externalName;
        }
        if (object._externalId != null) {
          objectJson["externalId"] = object._externalId;
        }
        if (object._imageId != null) {
          objectJson["imageId"] = object._imageId;
        }
        objectJson["cpu"] = Number(object._cpu);
        objectJson["ram"] = Number(object._ram);
        objectJson["width"] = Number(object._width);
        objectJson["height"] = Number(object._height);
        objectJson["isHeadless"] = object._isHeadless;
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): Machine {
        const _Value = STRUCT_CLASS_BY_TYPE[100] as typeof Value;
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const externalNameValue = objectJson["externalName"];
        const unpackedExternalName = externalNameValue != undefined ? externalNameValue : null;
        const externalIdValue = objectJson["externalId"];
        const unpackedExternalId = externalIdValue != undefined ? externalIdValue : null;
        const imageIdValue = objectJson["imageId"];
        const unpackedImageId = imageIdValue != undefined ? imageIdValue : null;
        const statusValue = objectJson["status"];
        const unpackedStatus = statusValue != undefined ? ResourceStatus[statusValue] as any : null;
        const regionValue = objectJson["region"];
        const unpackedRegion = regionValue != undefined ? Region[regionValue] as any : null;
        const parentPtrValue = objectJson["parent"];
        const unpackedParentPtr = parentPtrValue != undefined ? _NodeReference.unpack(1, parentPtrValue, _session) as NodeReference : null;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const instancePtrValue = objectJson["instance"];
        const unpackedInstancePtr = instancePtrValue != undefined ? _NodeReference.unpack(1, instancePtrValue, _session) as NodeReference : null;
        const deletedAtValue = objectJson["deletedAt"];
        const unpackedDeletedAt = deletedAtValue != undefined ? Temporal.Instant.from(deletedAtValue).toZonedDateTimeISO('UTC') : null;
        const ownedByPtrValue = objectJson["ownedBy"];
        const unpackedOwnedByPtr = ownedByPtrValue != undefined ? _NodeReference.unpack(1, ownedByPtrValue, _session) as NodeReference : null;
        const unpackedCustomValues = {} as any;
        if (objectJson["customValues"] != undefined) {
          for (const [key, value] of Object.entries(objectJson["customValues"])) {
            unpackedCustomValues[key] = _Value.unpack(1, value as any, _session) as Value;
          }
        }
        const scriptPtrValue = objectJson["script"];
        const unpackedScriptPtr = scriptPtrValue != undefined ? _NodeReference.unpack(1, scriptPtrValue, _session) as NodeReference : null;
        const isExtensibleValue = objectJson["isExtensible"];
        const unpackedIsExtensible = isExtensibleValue != undefined ? isExtensibleValue : null;
        const sourcePtrValue = objectJson["source"];
        const unpackedSourcePtr = sourcePtrValue != undefined ? _NodeReference.unpack(1, sourcePtrValue, _session) as NodeReference : null;
        const keyValue = objectJson["key"];
        const unpackedKey = keyValue != undefined ? keyValue : null;
        return new (NODE_CLASS_BY_TYPE[1001000] as typeof Machine)({
          type: MachineType[objectJson["type"]] as any,
          version: objectJson["version"],
          externalName: unpackedExternalName,
          externalId: unpackedExternalId,
          imageId: unpackedImageId,
          cpu: Number(objectJson["cpu"]),
          ram: Number(objectJson["ram"]),
          width: Number(objectJson["width"]),
          height: Number(objectJson["height"]),
          isHeadless: objectJson["isHeadless"],
          status: unpackedStatus,
          region: unpackedRegion,
          parent: unpackedParentPtr,
          materialization: Materialization[objectJson["materialization"]] as any,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          instance: unpackedInstancePtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          updatedAt: Temporal.Instant.from(objectJson["updatedAt"]).toZonedDateTimeISO('UTC'),
          updatedEpoch: Number(objectJson["updatedEpoch"]),
          updatedBy: _NodeReference.unpack(1, objectJson["updatedBy"], _session) as NodeReference,
          deletedAt: unpackedDeletedAt,
          ownedBy: unpackedOwnedByPtr,
          name: objectJson["name"],
          orderKey: objectJson["orderKey"],
          customValues: unpackedCustomValues,
          script: unpackedScriptPtr,
          isExtensible: unpackedIsExtensible,
          source: unpackedSourcePtr,
          key: unpackedKey,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 1001000)] = new MachineJsonEncoder();

    class CopyEventJsonEncoder implements _JsonObjectEncoder {
      packObject(object: CopyEvent): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "COPY_EVENT";
        objectJson["id"] = object.id;
        objectJson["space"] = object.spacePtr.pack(1);
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.causedByPtr != null) {
          objectJson["causedBy"] = object.causedByPtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["client"] = object.clientPtr.pack(1);
        objectJson["clientNonce"] = object.clientNonce;
        objectJson["clientCreatedAt"] = object.clientCreatedAt.toString({ timeZoneName: 'never' });
        objectJson["clientEpoch"] = Number(object.clientEpoch);
        objectJson["status"] = EventStatus[object.status];
        if (object.nodePtr != null) {
          objectJson["node"] = object.nodePtr.pack(1);
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): CopyEvent {
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const nodePtrValue = objectJson["node"];
        const unpackedNodePtr = nodePtrValue != undefined ? _NodeReference.unpack(1, nodePtrValue, _session) as NodeReference : null;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const causedByPtrValue = objectJson["causedBy"];
        const unpackedCausedByPtr = causedByPtrValue != undefined ? _NodeReference.unpack(1, causedByPtrValue, _session) as NodeReference : null;
        return new (NODE_CLASS_BY_TYPE[2000501] as typeof CopyEvent)({
          node: unpackedNodePtr,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          causedBy: unpackedCausedByPtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          client: _NodeReference.unpack(1, objectJson["client"], _session) as NodeReference,
          clientNonce: objectJson["clientNonce"],
          clientCreatedAt: Temporal.Instant.from(objectJson["clientCreatedAt"]).toZonedDateTimeISO('UTC'),
          clientEpoch: Number(objectJson["clientEpoch"]),
          status: EventStatus[objectJson["status"]] as any,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 2000501)] = new CopyEventJsonEncoder();

    class CutEventJsonEncoder implements _JsonObjectEncoder {
      packObject(object: CutEvent): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "CUT_EVENT";
        objectJson["id"] = object.id;
        objectJson["space"] = object.spacePtr.pack(1);
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.causedByPtr != null) {
          objectJson["causedBy"] = object.causedByPtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["client"] = object.clientPtr.pack(1);
        objectJson["clientNonce"] = object.clientNonce;
        objectJson["clientCreatedAt"] = object.clientCreatedAt.toString({ timeZoneName: 'never' });
        objectJson["clientEpoch"] = Number(object.clientEpoch);
        objectJson["status"] = EventStatus[object.status];
        if (object.nodePtr != null) {
          objectJson["node"] = object.nodePtr.pack(1);
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): CutEvent {
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const nodePtrValue = objectJson["node"];
        const unpackedNodePtr = nodePtrValue != undefined ? _NodeReference.unpack(1, nodePtrValue, _session) as NodeReference : null;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const causedByPtrValue = objectJson["causedBy"];
        const unpackedCausedByPtr = causedByPtrValue != undefined ? _NodeReference.unpack(1, causedByPtrValue, _session) as NodeReference : null;
        return new (NODE_CLASS_BY_TYPE[2000502] as typeof CutEvent)({
          node: unpackedNodePtr,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          causedBy: unpackedCausedByPtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          client: _NodeReference.unpack(1, objectJson["client"], _session) as NodeReference,
          clientNonce: objectJson["clientNonce"],
          clientCreatedAt: Temporal.Instant.from(objectJson["clientCreatedAt"]).toZonedDateTimeISO('UTC'),
          clientEpoch: Number(objectJson["clientEpoch"]),
          status: EventStatus[objectJson["status"]] as any,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 2000502)] = new CutEventJsonEncoder();

    class PasteEventJsonEncoder implements _JsonObjectEncoder {
      packObject(object: PasteEvent): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "PASTE_EVENT";
        objectJson["id"] = object.id;
        objectJson["space"] = object.spacePtr.pack(1);
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.causedByPtr != null) {
          objectJson["causedBy"] = object.causedByPtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["client"] = object.clientPtr.pack(1);
        objectJson["clientNonce"] = object.clientNonce;
        objectJson["clientCreatedAt"] = object.clientCreatedAt.toString({ timeZoneName: 'never' });
        objectJson["clientEpoch"] = Number(object.clientEpoch);
        objectJson["status"] = EventStatus[object.status];
        if (object.nodePtr != null) {
          objectJson["node"] = object.nodePtr.pack(1);
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): PasteEvent {
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const nodePtrValue = objectJson["node"];
        const unpackedNodePtr = nodePtrValue != undefined ? _NodeReference.unpack(1, nodePtrValue, _session) as NodeReference : null;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const causedByPtrValue = objectJson["causedBy"];
        const unpackedCausedByPtr = causedByPtrValue != undefined ? _NodeReference.unpack(1, causedByPtrValue, _session) as NodeReference : null;
        return new (NODE_CLASS_BY_TYPE[2000503] as typeof PasteEvent)({
          node: unpackedNodePtr,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          causedBy: unpackedCausedByPtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          client: _NodeReference.unpack(1, objectJson["client"], _session) as NodeReference,
          clientNonce: objectJson["clientNonce"],
          clientCreatedAt: Temporal.Instant.from(objectJson["clientCreatedAt"]).toZonedDateTimeISO('UTC'),
          clientEpoch: Number(objectJson["clientEpoch"]),
          status: EventStatus[objectJson["status"]] as any,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 2000503)] = new PasteEventJsonEncoder();

    class DragStartEventJsonEncoder implements _JsonObjectEncoder {
      packObject(object: DragStartEvent): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "DRAG_START_EVENT";
        objectJson["id"] = object.id;
        objectJson["space"] = object.spacePtr.pack(1);
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.causedByPtr != null) {
          objectJson["causedBy"] = object.causedByPtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["client"] = object.clientPtr.pack(1);
        objectJson["clientNonce"] = object.clientNonce;
        objectJson["clientCreatedAt"] = object.clientCreatedAt.toString({ timeZoneName: 'never' });
        objectJson["clientEpoch"] = Number(object.clientEpoch);
        objectJson["status"] = EventStatus[object.status];
        if (object.nodePtr != null) {
          objectJson["node"] = object.nodePtr.pack(1);
        }
        objectJson["position"] = object.position.pack(1);
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): DragStartEvent {
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const _Vector2 = STRUCT_CLASS_BY_TYPE[2400000] as typeof Vector2;
        const nodePtrValue = objectJson["node"];
        const unpackedNodePtr = nodePtrValue != undefined ? _NodeReference.unpack(1, nodePtrValue, _session) as NodeReference : null;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const causedByPtrValue = objectJson["causedBy"];
        const unpackedCausedByPtr = causedByPtrValue != undefined ? _NodeReference.unpack(1, causedByPtrValue, _session) as NodeReference : null;
        return new (NODE_CLASS_BY_TYPE[2000401] as typeof DragStartEvent)({
          position: _Vector2.unpack(1, objectJson["position"], _session) as Vector2,
          node: unpackedNodePtr,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          causedBy: unpackedCausedByPtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          client: _NodeReference.unpack(1, objectJson["client"], _session) as NodeReference,
          clientNonce: objectJson["clientNonce"],
          clientCreatedAt: Temporal.Instant.from(objectJson["clientCreatedAt"]).toZonedDateTimeISO('UTC'),
          clientEpoch: Number(objectJson["clientEpoch"]),
          status: EventStatus[objectJson["status"]] as any,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 2000401)] = new DragStartEventJsonEncoder();

    class DragEndEventJsonEncoder implements _JsonObjectEncoder {
      packObject(object: DragEndEvent): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "DRAG_END_EVENT";
        objectJson["id"] = object.id;
        objectJson["space"] = object.spacePtr.pack(1);
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.causedByPtr != null) {
          objectJson["causedBy"] = object.causedByPtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["client"] = object.clientPtr.pack(1);
        objectJson["clientNonce"] = object.clientNonce;
        objectJson["clientCreatedAt"] = object.clientCreatedAt.toString({ timeZoneName: 'never' });
        objectJson["clientEpoch"] = Number(object.clientEpoch);
        objectJson["status"] = EventStatus[object.status];
        if (object.nodePtr != null) {
          objectJson["node"] = object.nodePtr.pack(1);
        }
        objectJson["position"] = object.position.pack(1);
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): DragEndEvent {
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const _Vector2 = STRUCT_CLASS_BY_TYPE[2400000] as typeof Vector2;
        const nodePtrValue = objectJson["node"];
        const unpackedNodePtr = nodePtrValue != undefined ? _NodeReference.unpack(1, nodePtrValue, _session) as NodeReference : null;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const causedByPtrValue = objectJson["causedBy"];
        const unpackedCausedByPtr = causedByPtrValue != undefined ? _NodeReference.unpack(1, causedByPtrValue, _session) as NodeReference : null;
        return new (NODE_CLASS_BY_TYPE[2000402] as typeof DragEndEvent)({
          position: _Vector2.unpack(1, objectJson["position"], _session) as Vector2,
          node: unpackedNodePtr,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          causedBy: unpackedCausedByPtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          client: _NodeReference.unpack(1, objectJson["client"], _session) as NodeReference,
          clientNonce: objectJson["clientNonce"],
          clientCreatedAt: Temporal.Instant.from(objectJson["clientCreatedAt"]).toZonedDateTimeISO('UTC'),
          clientEpoch: Number(objectJson["clientEpoch"]),
          status: EventStatus[objectJson["status"]] as any,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 2000402)] = new DragEndEventJsonEncoder();

    class DragOverEventJsonEncoder implements _JsonObjectEncoder {
      packObject(object: DragOverEvent): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "DRAG_OVER_EVENT";
        objectJson["id"] = object.id;
        objectJson["space"] = object.spacePtr.pack(1);
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.causedByPtr != null) {
          objectJson["causedBy"] = object.causedByPtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["client"] = object.clientPtr.pack(1);
        objectJson["clientNonce"] = object.clientNonce;
        objectJson["clientCreatedAt"] = object.clientCreatedAt.toString({ timeZoneName: 'never' });
        objectJson["clientEpoch"] = Number(object.clientEpoch);
        objectJson["status"] = EventStatus[object.status];
        if (object.nodePtr != null) {
          objectJson["node"] = object.nodePtr.pack(1);
        }
        objectJson["position"] = object.position.pack(1);
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): DragOverEvent {
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const _Vector2 = STRUCT_CLASS_BY_TYPE[2400000] as typeof Vector2;
        const nodePtrValue = objectJson["node"];
        const unpackedNodePtr = nodePtrValue != undefined ? _NodeReference.unpack(1, nodePtrValue, _session) as NodeReference : null;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const causedByPtrValue = objectJson["causedBy"];
        const unpackedCausedByPtr = causedByPtrValue != undefined ? _NodeReference.unpack(1, causedByPtrValue, _session) as NodeReference : null;
        return new (NODE_CLASS_BY_TYPE[2000403] as typeof DragOverEvent)({
          position: _Vector2.unpack(1, objectJson["position"], _session) as Vector2,
          node: unpackedNodePtr,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          causedBy: unpackedCausedByPtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          client: _NodeReference.unpack(1, objectJson["client"], _session) as NodeReference,
          clientNonce: objectJson["clientNonce"],
          clientCreatedAt: Temporal.Instant.from(objectJson["clientCreatedAt"]).toZonedDateTimeISO('UTC'),
          clientEpoch: Number(objectJson["clientEpoch"]),
          status: EventStatus[objectJson["status"]] as any,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 2000403)] = new DragOverEventJsonEncoder();

    class DragEnterEventJsonEncoder implements _JsonObjectEncoder {
      packObject(object: DragEnterEvent): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "DRAG_ENTER_EVENT";
        objectJson["id"] = object.id;
        objectJson["space"] = object.spacePtr.pack(1);
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.causedByPtr != null) {
          objectJson["causedBy"] = object.causedByPtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["client"] = object.clientPtr.pack(1);
        objectJson["clientNonce"] = object.clientNonce;
        objectJson["clientCreatedAt"] = object.clientCreatedAt.toString({ timeZoneName: 'never' });
        objectJson["clientEpoch"] = Number(object.clientEpoch);
        objectJson["status"] = EventStatus[object.status];
        if (object.nodePtr != null) {
          objectJson["node"] = object.nodePtr.pack(1);
        }
        objectJson["position"] = object.position.pack(1);
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): DragEnterEvent {
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const _Vector2 = STRUCT_CLASS_BY_TYPE[2400000] as typeof Vector2;
        const nodePtrValue = objectJson["node"];
        const unpackedNodePtr = nodePtrValue != undefined ? _NodeReference.unpack(1, nodePtrValue, _session) as NodeReference : null;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const causedByPtrValue = objectJson["causedBy"];
        const unpackedCausedByPtr = causedByPtrValue != undefined ? _NodeReference.unpack(1, causedByPtrValue, _session) as NodeReference : null;
        return new (NODE_CLASS_BY_TYPE[2000404] as typeof DragEnterEvent)({
          position: _Vector2.unpack(1, objectJson["position"], _session) as Vector2,
          node: unpackedNodePtr,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          causedBy: unpackedCausedByPtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          client: _NodeReference.unpack(1, objectJson["client"], _session) as NodeReference,
          clientNonce: objectJson["clientNonce"],
          clientCreatedAt: Temporal.Instant.from(objectJson["clientCreatedAt"]).toZonedDateTimeISO('UTC'),
          clientEpoch: Number(objectJson["clientEpoch"]),
          status: EventStatus[objectJson["status"]] as any,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 2000404)] = new DragEnterEventJsonEncoder();

    class DragLeaveEventJsonEncoder implements _JsonObjectEncoder {
      packObject(object: DragLeaveEvent): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "DRAG_LEAVE_EVENT";
        objectJson["id"] = object.id;
        objectJson["space"] = object.spacePtr.pack(1);
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.causedByPtr != null) {
          objectJson["causedBy"] = object.causedByPtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["client"] = object.clientPtr.pack(1);
        objectJson["clientNonce"] = object.clientNonce;
        objectJson["clientCreatedAt"] = object.clientCreatedAt.toString({ timeZoneName: 'never' });
        objectJson["clientEpoch"] = Number(object.clientEpoch);
        objectJson["status"] = EventStatus[object.status];
        if (object.nodePtr != null) {
          objectJson["node"] = object.nodePtr.pack(1);
        }
        objectJson["position"] = object.position.pack(1);
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): DragLeaveEvent {
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const _Vector2 = STRUCT_CLASS_BY_TYPE[2400000] as typeof Vector2;
        const nodePtrValue = objectJson["node"];
        const unpackedNodePtr = nodePtrValue != undefined ? _NodeReference.unpack(1, nodePtrValue, _session) as NodeReference : null;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const causedByPtrValue = objectJson["causedBy"];
        const unpackedCausedByPtr = causedByPtrValue != undefined ? _NodeReference.unpack(1, causedByPtrValue, _session) as NodeReference : null;
        return new (NODE_CLASS_BY_TYPE[2000405] as typeof DragLeaveEvent)({
          position: _Vector2.unpack(1, objectJson["position"], _session) as Vector2,
          node: unpackedNodePtr,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          causedBy: unpackedCausedByPtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          client: _NodeReference.unpack(1, objectJson["client"], _session) as NodeReference,
          clientNonce: objectJson["clientNonce"],
          clientCreatedAt: Temporal.Instant.from(objectJson["clientCreatedAt"]).toZonedDateTimeISO('UTC'),
          clientEpoch: Number(objectJson["clientEpoch"]),
          status: EventStatus[objectJson["status"]] as any,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 2000405)] = new DragLeaveEventJsonEncoder();

    class DropEventJsonEncoder implements _JsonObjectEncoder {
      packObject(object: DropEvent): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "DROP_EVENT";
        objectJson["id"] = object.id;
        objectJson["space"] = object.spacePtr.pack(1);
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.causedByPtr != null) {
          objectJson["causedBy"] = object.causedByPtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["client"] = object.clientPtr.pack(1);
        objectJson["clientNonce"] = object.clientNonce;
        objectJson["clientCreatedAt"] = object.clientCreatedAt.toString({ timeZoneName: 'never' });
        objectJson["clientEpoch"] = Number(object.clientEpoch);
        objectJson["status"] = EventStatus[object.status];
        if (object.nodePtr != null) {
          objectJson["node"] = object.nodePtr.pack(1);
        }
        objectJson["position"] = object.position.pack(1);
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): DropEvent {
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const _Vector2 = STRUCT_CLASS_BY_TYPE[2400000] as typeof Vector2;
        const nodePtrValue = objectJson["node"];
        const unpackedNodePtr = nodePtrValue != undefined ? _NodeReference.unpack(1, nodePtrValue, _session) as NodeReference : null;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const causedByPtrValue = objectJson["causedBy"];
        const unpackedCausedByPtr = causedByPtrValue != undefined ? _NodeReference.unpack(1, causedByPtrValue, _session) as NodeReference : null;
        return new (NODE_CLASS_BY_TYPE[2000406] as typeof DropEvent)({
          position: _Vector2.unpack(1, objectJson["position"], _session) as Vector2,
          node: unpackedNodePtr,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          causedBy: unpackedCausedByPtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          client: _NodeReference.unpack(1, objectJson["client"], _session) as NodeReference,
          clientNonce: objectJson["clientNonce"],
          clientCreatedAt: Temporal.Instant.from(objectJson["clientCreatedAt"]).toZonedDateTimeISO('UTC'),
          clientEpoch: Number(objectJson["clientEpoch"]),
          status: EventStatus[objectJson["status"]] as any,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 2000406)] = new DropEventJsonEncoder();

    class FocusInEventJsonEncoder implements _JsonObjectEncoder {
      packObject(object: FocusInEvent): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "FOCUS_IN_EVENT";
        objectJson["id"] = object.id;
        objectJson["space"] = object.spacePtr.pack(1);
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.causedByPtr != null) {
          objectJson["causedBy"] = object.causedByPtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["client"] = object.clientPtr.pack(1);
        objectJson["clientNonce"] = object.clientNonce;
        objectJson["clientCreatedAt"] = object.clientCreatedAt.toString({ timeZoneName: 'never' });
        objectJson["clientEpoch"] = Number(object.clientEpoch);
        objectJson["status"] = EventStatus[object.status];
        if (object.nodePtr != null) {
          objectJson["node"] = object.nodePtr.pack(1);
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): FocusInEvent {
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const nodePtrValue = objectJson["node"];
        const unpackedNodePtr = nodePtrValue != undefined ? _NodeReference.unpack(1, nodePtrValue, _session) as NodeReference : null;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const causedByPtrValue = objectJson["causedBy"];
        const unpackedCausedByPtr = causedByPtrValue != undefined ? _NodeReference.unpack(1, causedByPtrValue, _session) as NodeReference : null;
        return new (NODE_CLASS_BY_TYPE[2000601] as typeof FocusInEvent)({
          node: unpackedNodePtr,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          causedBy: unpackedCausedByPtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          client: _NodeReference.unpack(1, objectJson["client"], _session) as NodeReference,
          clientNonce: objectJson["clientNonce"],
          clientCreatedAt: Temporal.Instant.from(objectJson["clientCreatedAt"]).toZonedDateTimeISO('UTC'),
          clientEpoch: Number(objectJson["clientEpoch"]),
          status: EventStatus[objectJson["status"]] as any,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 2000601)] = new FocusInEventJsonEncoder();

    class FocusOutEventJsonEncoder implements _JsonObjectEncoder {
      packObject(object: FocusOutEvent): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "FOCUS_OUT_EVENT";
        objectJson["id"] = object.id;
        objectJson["space"] = object.spacePtr.pack(1);
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.causedByPtr != null) {
          objectJson["causedBy"] = object.causedByPtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["client"] = object.clientPtr.pack(1);
        objectJson["clientNonce"] = object.clientNonce;
        objectJson["clientCreatedAt"] = object.clientCreatedAt.toString({ timeZoneName: 'never' });
        objectJson["clientEpoch"] = Number(object.clientEpoch);
        objectJson["status"] = EventStatus[object.status];
        if (object.nodePtr != null) {
          objectJson["node"] = object.nodePtr.pack(1);
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): FocusOutEvent {
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const nodePtrValue = objectJson["node"];
        const unpackedNodePtr = nodePtrValue != undefined ? _NodeReference.unpack(1, nodePtrValue, _session) as NodeReference : null;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const causedByPtrValue = objectJson["causedBy"];
        const unpackedCausedByPtr = causedByPtrValue != undefined ? _NodeReference.unpack(1, causedByPtrValue, _session) as NodeReference : null;
        return new (NODE_CLASS_BY_TYPE[2000602] as typeof FocusOutEvent)({
          node: unpackedNodePtr,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          causedBy: unpackedCausedByPtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          client: _NodeReference.unpack(1, objectJson["client"], _session) as NodeReference,
          clientNonce: objectJson["clientNonce"],
          clientCreatedAt: Temporal.Instant.from(objectJson["clientCreatedAt"]).toZonedDateTimeISO('UTC'),
          clientEpoch: Number(objectJson["clientEpoch"]),
          status: EventStatus[objectJson["status"]] as any,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 2000602)] = new FocusOutEventJsonEncoder();

    class KeyDownEventJsonEncoder implements _JsonObjectEncoder {
      packObject(object: KeyDownEvent): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "KEY_DOWN_EVENT";
        objectJson["id"] = object.id;
        objectJson["space"] = object.spacePtr.pack(1);
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.causedByPtr != null) {
          objectJson["causedBy"] = object.causedByPtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["client"] = object.clientPtr.pack(1);
        objectJson["clientNonce"] = object.clientNonce;
        objectJson["clientCreatedAt"] = object.clientCreatedAt.toString({ timeZoneName: 'never' });
        objectJson["clientEpoch"] = Number(object.clientEpoch);
        objectJson["status"] = EventStatus[object.status];
        if (object.nodePtr != null) {
          objectJson["node"] = object.nodePtr.pack(1);
        }
        objectJson["key"] = object.key;
        objectJson["code"] = object.code;
        objectJson["isRepeat"] = object.isRepeat;
        objectJson["isRedacted"] = object.isRedacted;
        objectJson["shiftKey"] = object.shiftKey;
        objectJson["altKey"] = object.altKey;
        objectJson["ctrlKey"] = object.ctrlKey;
        objectJson["metaKey"] = object.metaKey;
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): KeyDownEvent {
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const nodePtrValue = objectJson["node"];
        const unpackedNodePtr = nodePtrValue != undefined ? _NodeReference.unpack(1, nodePtrValue, _session) as NodeReference : null;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const causedByPtrValue = objectJson["causedBy"];
        const unpackedCausedByPtr = causedByPtrValue != undefined ? _NodeReference.unpack(1, causedByPtrValue, _session) as NodeReference : null;
        return new (NODE_CLASS_BY_TYPE[2000301] as typeof KeyDownEvent)({
          key: objectJson["key"],
          code: objectJson["code"],
          isRepeat: objectJson["isRepeat"],
          isRedacted: objectJson["isRedacted"],
          shiftKey: objectJson["shiftKey"],
          altKey: objectJson["altKey"],
          ctrlKey: objectJson["ctrlKey"],
          metaKey: objectJson["metaKey"],
          node: unpackedNodePtr,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          causedBy: unpackedCausedByPtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          client: _NodeReference.unpack(1, objectJson["client"], _session) as NodeReference,
          clientNonce: objectJson["clientNonce"],
          clientCreatedAt: Temporal.Instant.from(objectJson["clientCreatedAt"]).toZonedDateTimeISO('UTC'),
          clientEpoch: Number(objectJson["clientEpoch"]),
          status: EventStatus[objectJson["status"]] as any,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 2000301)] = new KeyDownEventJsonEncoder();

    class KeyUpEventJsonEncoder implements _JsonObjectEncoder {
      packObject(object: KeyUpEvent): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "KEY_UP_EVENT";
        objectJson["id"] = object.id;
        objectJson["space"] = object.spacePtr.pack(1);
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.causedByPtr != null) {
          objectJson["causedBy"] = object.causedByPtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["client"] = object.clientPtr.pack(1);
        objectJson["clientNonce"] = object.clientNonce;
        objectJson["clientCreatedAt"] = object.clientCreatedAt.toString({ timeZoneName: 'never' });
        objectJson["clientEpoch"] = Number(object.clientEpoch);
        objectJson["status"] = EventStatus[object.status];
        if (object.nodePtr != null) {
          objectJson["node"] = object.nodePtr.pack(1);
        }
        objectJson["key"] = object.key;
        objectJson["code"] = object.code;
        objectJson["isRepeat"] = object.isRepeat;
        objectJson["isRedacted"] = object.isRedacted;
        objectJson["shiftKey"] = object.shiftKey;
        objectJson["altKey"] = object.altKey;
        objectJson["ctrlKey"] = object.ctrlKey;
        objectJson["metaKey"] = object.metaKey;
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): KeyUpEvent {
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const nodePtrValue = objectJson["node"];
        const unpackedNodePtr = nodePtrValue != undefined ? _NodeReference.unpack(1, nodePtrValue, _session) as NodeReference : null;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const causedByPtrValue = objectJson["causedBy"];
        const unpackedCausedByPtr = causedByPtrValue != undefined ? _NodeReference.unpack(1, causedByPtrValue, _session) as NodeReference : null;
        return new (NODE_CLASS_BY_TYPE[2000302] as typeof KeyUpEvent)({
          key: objectJson["key"],
          code: objectJson["code"],
          isRepeat: objectJson["isRepeat"],
          isRedacted: objectJson["isRedacted"],
          shiftKey: objectJson["shiftKey"],
          altKey: objectJson["altKey"],
          ctrlKey: objectJson["ctrlKey"],
          metaKey: objectJson["metaKey"],
          node: unpackedNodePtr,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          causedBy: unpackedCausedByPtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          client: _NodeReference.unpack(1, objectJson["client"], _session) as NodeReference,
          clientNonce: objectJson["clientNonce"],
          clientCreatedAt: Temporal.Instant.from(objectJson["clientCreatedAt"]).toZonedDateTimeISO('UTC'),
          clientEpoch: Number(objectJson["clientEpoch"]),
          status: EventStatus[objectJson["status"]] as any,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 2000302)] = new KeyUpEventJsonEncoder();

    class KeyPressEventJsonEncoder implements _JsonObjectEncoder {
      packObject(object: KeyPressEvent): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "KEY_PRESS_EVENT";
        objectJson["id"] = object.id;
        objectJson["space"] = object.spacePtr.pack(1);
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.causedByPtr != null) {
          objectJson["causedBy"] = object.causedByPtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["client"] = object.clientPtr.pack(1);
        objectJson["clientNonce"] = object.clientNonce;
        objectJson["clientCreatedAt"] = object.clientCreatedAt.toString({ timeZoneName: 'never' });
        objectJson["clientEpoch"] = Number(object.clientEpoch);
        objectJson["status"] = EventStatus[object.status];
        if (object.nodePtr != null) {
          objectJson["node"] = object.nodePtr.pack(1);
        }
        objectJson["key"] = object.key;
        objectJson["code"] = object.code;
        objectJson["isRepeat"] = object.isRepeat;
        objectJson["isRedacted"] = object.isRedacted;
        objectJson["shiftKey"] = object.shiftKey;
        objectJson["altKey"] = object.altKey;
        objectJson["ctrlKey"] = object.ctrlKey;
        objectJson["metaKey"] = object.metaKey;
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): KeyPressEvent {
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const nodePtrValue = objectJson["node"];
        const unpackedNodePtr = nodePtrValue != undefined ? _NodeReference.unpack(1, nodePtrValue, _session) as NodeReference : null;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const causedByPtrValue = objectJson["causedBy"];
        const unpackedCausedByPtr = causedByPtrValue != undefined ? _NodeReference.unpack(1, causedByPtrValue, _session) as NodeReference : null;
        return new (NODE_CLASS_BY_TYPE[2000303] as typeof KeyPressEvent)({
          key: objectJson["key"],
          code: objectJson["code"],
          isRepeat: objectJson["isRepeat"],
          isRedacted: objectJson["isRedacted"],
          shiftKey: objectJson["shiftKey"],
          altKey: objectJson["altKey"],
          ctrlKey: objectJson["ctrlKey"],
          metaKey: objectJson["metaKey"],
          node: unpackedNodePtr,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          causedBy: unpackedCausedByPtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          client: _NodeReference.unpack(1, objectJson["client"], _session) as NodeReference,
          clientNonce: objectJson["clientNonce"],
          clientCreatedAt: Temporal.Instant.from(objectJson["clientCreatedAt"]).toZonedDateTimeISO('UTC'),
          clientEpoch: Number(objectJson["clientEpoch"]),
          status: EventStatus[objectJson["status"]] as any,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 2000303)] = new KeyPressEventJsonEncoder();

    class PointerDownEventJsonEncoder implements _JsonObjectEncoder {
      packObject(object: PointerDownEvent): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "POINTER_DOWN_EVENT";
        objectJson["id"] = object.id;
        objectJson["space"] = object.spacePtr.pack(1);
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.causedByPtr != null) {
          objectJson["causedBy"] = object.causedByPtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["client"] = object.clientPtr.pack(1);
        objectJson["clientNonce"] = object.clientNonce;
        objectJson["clientCreatedAt"] = object.clientCreatedAt.toString({ timeZoneName: 'never' });
        objectJson["clientEpoch"] = Number(object.clientEpoch);
        objectJson["status"] = EventStatus[object.status];
        if (object.nodePtr != null) {
          objectJson["node"] = object.nodePtr.pack(1);
        }
        objectJson["position"] = object.position.pack(1);
        if (object.pressure != null) {
          objectJson["pressure"] = Number(object.pressure);
        }
        objectJson["shiftKey"] = object.shiftKey;
        objectJson["altKey"] = object.altKey;
        objectJson["ctrlKey"] = object.ctrlKey;
        objectJson["metaKey"] = object.metaKey;
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): PointerDownEvent {
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const _Vector2 = STRUCT_CLASS_BY_TYPE[2400000] as typeof Vector2;
        const pressureValue = objectJson["pressure"];
        const unpackedPressure = pressureValue != undefined ? Number(pressureValue) : null;
        const nodePtrValue = objectJson["node"];
        const unpackedNodePtr = nodePtrValue != undefined ? _NodeReference.unpack(1, nodePtrValue, _session) as NodeReference : null;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const causedByPtrValue = objectJson["causedBy"];
        const unpackedCausedByPtr = causedByPtrValue != undefined ? _NodeReference.unpack(1, causedByPtrValue, _session) as NodeReference : null;
        return new (NODE_CLASS_BY_TYPE[2000101] as typeof PointerDownEvent)({
          position: _Vector2.unpack(1, objectJson["position"], _session) as Vector2,
          pressure: unpackedPressure,
          shiftKey: objectJson["shiftKey"],
          altKey: objectJson["altKey"],
          ctrlKey: objectJson["ctrlKey"],
          metaKey: objectJson["metaKey"],
          node: unpackedNodePtr,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          causedBy: unpackedCausedByPtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          client: _NodeReference.unpack(1, objectJson["client"], _session) as NodeReference,
          clientNonce: objectJson["clientNonce"],
          clientCreatedAt: Temporal.Instant.from(objectJson["clientCreatedAt"]).toZonedDateTimeISO('UTC'),
          clientEpoch: Number(objectJson["clientEpoch"]),
          status: EventStatus[objectJson["status"]] as any,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 2000101)] = new PointerDownEventJsonEncoder();

    class PointerUpEventJsonEncoder implements _JsonObjectEncoder {
      packObject(object: PointerUpEvent): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "POINTER_UP_EVENT";
        objectJson["id"] = object.id;
        objectJson["space"] = object.spacePtr.pack(1);
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.causedByPtr != null) {
          objectJson["causedBy"] = object.causedByPtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["client"] = object.clientPtr.pack(1);
        objectJson["clientNonce"] = object.clientNonce;
        objectJson["clientCreatedAt"] = object.clientCreatedAt.toString({ timeZoneName: 'never' });
        objectJson["clientEpoch"] = Number(object.clientEpoch);
        objectJson["status"] = EventStatus[object.status];
        if (object.nodePtr != null) {
          objectJson["node"] = object.nodePtr.pack(1);
        }
        objectJson["position"] = object.position.pack(1);
        if (object.pressure != null) {
          objectJson["pressure"] = Number(object.pressure);
        }
        objectJson["shiftKey"] = object.shiftKey;
        objectJson["altKey"] = object.altKey;
        objectJson["ctrlKey"] = object.ctrlKey;
        objectJson["metaKey"] = object.metaKey;
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): PointerUpEvent {
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const _Vector2 = STRUCT_CLASS_BY_TYPE[2400000] as typeof Vector2;
        const pressureValue = objectJson["pressure"];
        const unpackedPressure = pressureValue != undefined ? Number(pressureValue) : null;
        const nodePtrValue = objectJson["node"];
        const unpackedNodePtr = nodePtrValue != undefined ? _NodeReference.unpack(1, nodePtrValue, _session) as NodeReference : null;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const causedByPtrValue = objectJson["causedBy"];
        const unpackedCausedByPtr = causedByPtrValue != undefined ? _NodeReference.unpack(1, causedByPtrValue, _session) as NodeReference : null;
        return new (NODE_CLASS_BY_TYPE[2000102] as typeof PointerUpEvent)({
          position: _Vector2.unpack(1, objectJson["position"], _session) as Vector2,
          pressure: unpackedPressure,
          shiftKey: objectJson["shiftKey"],
          altKey: objectJson["altKey"],
          ctrlKey: objectJson["ctrlKey"],
          metaKey: objectJson["metaKey"],
          node: unpackedNodePtr,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          causedBy: unpackedCausedByPtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          client: _NodeReference.unpack(1, objectJson["client"], _session) as NodeReference,
          clientNonce: objectJson["clientNonce"],
          clientCreatedAt: Temporal.Instant.from(objectJson["clientCreatedAt"]).toZonedDateTimeISO('UTC'),
          clientEpoch: Number(objectJson["clientEpoch"]),
          status: EventStatus[objectJson["status"]] as any,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 2000102)] = new PointerUpEventJsonEncoder();

    class PointerMoveEventJsonEncoder implements _JsonObjectEncoder {
      packObject(object: PointerMoveEvent): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "POINTER_MOVE_EVENT";
        objectJson["id"] = object.id;
        objectJson["space"] = object.spacePtr.pack(1);
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.causedByPtr != null) {
          objectJson["causedBy"] = object.causedByPtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["client"] = object.clientPtr.pack(1);
        objectJson["clientNonce"] = object.clientNonce;
        objectJson["clientCreatedAt"] = object.clientCreatedAt.toString({ timeZoneName: 'never' });
        objectJson["clientEpoch"] = Number(object.clientEpoch);
        objectJson["status"] = EventStatus[object.status];
        if (object.nodePtr != null) {
          objectJson["node"] = object.nodePtr.pack(1);
        }
        objectJson["position"] = object.position.pack(1);
        if (object.pressure != null) {
          objectJson["pressure"] = Number(object.pressure);
        }
        objectJson["shiftKey"] = object.shiftKey;
        objectJson["altKey"] = object.altKey;
        objectJson["ctrlKey"] = object.ctrlKey;
        objectJson["metaKey"] = object.metaKey;
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): PointerMoveEvent {
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const _Vector2 = STRUCT_CLASS_BY_TYPE[2400000] as typeof Vector2;
        const pressureValue = objectJson["pressure"];
        const unpackedPressure = pressureValue != undefined ? Number(pressureValue) : null;
        const nodePtrValue = objectJson["node"];
        const unpackedNodePtr = nodePtrValue != undefined ? _NodeReference.unpack(1, nodePtrValue, _session) as NodeReference : null;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const causedByPtrValue = objectJson["causedBy"];
        const unpackedCausedByPtr = causedByPtrValue != undefined ? _NodeReference.unpack(1, causedByPtrValue, _session) as NodeReference : null;
        return new (NODE_CLASS_BY_TYPE[2000103] as typeof PointerMoveEvent)({
          position: _Vector2.unpack(1, objectJson["position"], _session) as Vector2,
          pressure: unpackedPressure,
          shiftKey: objectJson["shiftKey"],
          altKey: objectJson["altKey"],
          ctrlKey: objectJson["ctrlKey"],
          metaKey: objectJson["metaKey"],
          node: unpackedNodePtr,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          causedBy: unpackedCausedByPtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          client: _NodeReference.unpack(1, objectJson["client"], _session) as NodeReference,
          clientNonce: objectJson["clientNonce"],
          clientCreatedAt: Temporal.Instant.from(objectJson["clientCreatedAt"]).toZonedDateTimeISO('UTC'),
          clientEpoch: Number(objectJson["clientEpoch"]),
          status: EventStatus[objectJson["status"]] as any,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 2000103)] = new PointerMoveEventJsonEncoder();

    class PointerEnterEventJsonEncoder implements _JsonObjectEncoder {
      packObject(object: PointerEnterEvent): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "POINTER_ENTER_EVENT";
        objectJson["id"] = object.id;
        objectJson["space"] = object.spacePtr.pack(1);
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.causedByPtr != null) {
          objectJson["causedBy"] = object.causedByPtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["client"] = object.clientPtr.pack(1);
        objectJson["clientNonce"] = object.clientNonce;
        objectJson["clientCreatedAt"] = object.clientCreatedAt.toString({ timeZoneName: 'never' });
        objectJson["clientEpoch"] = Number(object.clientEpoch);
        objectJson["status"] = EventStatus[object.status];
        if (object.nodePtr != null) {
          objectJson["node"] = object.nodePtr.pack(1);
        }
        objectJson["position"] = object.position.pack(1);
        if (object.pressure != null) {
          objectJson["pressure"] = Number(object.pressure);
        }
        objectJson["shiftKey"] = object.shiftKey;
        objectJson["altKey"] = object.altKey;
        objectJson["ctrlKey"] = object.ctrlKey;
        objectJson["metaKey"] = object.metaKey;
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): PointerEnterEvent {
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const _Vector2 = STRUCT_CLASS_BY_TYPE[2400000] as typeof Vector2;
        const pressureValue = objectJson["pressure"];
        const unpackedPressure = pressureValue != undefined ? Number(pressureValue) : null;
        const nodePtrValue = objectJson["node"];
        const unpackedNodePtr = nodePtrValue != undefined ? _NodeReference.unpack(1, nodePtrValue, _session) as NodeReference : null;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const causedByPtrValue = objectJson["causedBy"];
        const unpackedCausedByPtr = causedByPtrValue != undefined ? _NodeReference.unpack(1, causedByPtrValue, _session) as NodeReference : null;
        return new (NODE_CLASS_BY_TYPE[2000104] as typeof PointerEnterEvent)({
          position: _Vector2.unpack(1, objectJson["position"], _session) as Vector2,
          pressure: unpackedPressure,
          shiftKey: objectJson["shiftKey"],
          altKey: objectJson["altKey"],
          ctrlKey: objectJson["ctrlKey"],
          metaKey: objectJson["metaKey"],
          node: unpackedNodePtr,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          causedBy: unpackedCausedByPtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          client: _NodeReference.unpack(1, objectJson["client"], _session) as NodeReference,
          clientNonce: objectJson["clientNonce"],
          clientCreatedAt: Temporal.Instant.from(objectJson["clientCreatedAt"]).toZonedDateTimeISO('UTC'),
          clientEpoch: Number(objectJson["clientEpoch"]),
          status: EventStatus[objectJson["status"]] as any,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 2000104)] = new PointerEnterEventJsonEncoder();

    class PointerOverEventJsonEncoder implements _JsonObjectEncoder {
      packObject(object: PointerOverEvent): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "POINTER_OVER_EVENT";
        objectJson["id"] = object.id;
        objectJson["space"] = object.spacePtr.pack(1);
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.causedByPtr != null) {
          objectJson["causedBy"] = object.causedByPtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["client"] = object.clientPtr.pack(1);
        objectJson["clientNonce"] = object.clientNonce;
        objectJson["clientCreatedAt"] = object.clientCreatedAt.toString({ timeZoneName: 'never' });
        objectJson["clientEpoch"] = Number(object.clientEpoch);
        objectJson["status"] = EventStatus[object.status];
        if (object.nodePtr != null) {
          objectJson["node"] = object.nodePtr.pack(1);
        }
        objectJson["position"] = object.position.pack(1);
        if (object.pressure != null) {
          objectJson["pressure"] = Number(object.pressure);
        }
        objectJson["shiftKey"] = object.shiftKey;
        objectJson["altKey"] = object.altKey;
        objectJson["ctrlKey"] = object.ctrlKey;
        objectJson["metaKey"] = object.metaKey;
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): PointerOverEvent {
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const _Vector2 = STRUCT_CLASS_BY_TYPE[2400000] as typeof Vector2;
        const pressureValue = objectJson["pressure"];
        const unpackedPressure = pressureValue != undefined ? Number(pressureValue) : null;
        const nodePtrValue = objectJson["node"];
        const unpackedNodePtr = nodePtrValue != undefined ? _NodeReference.unpack(1, nodePtrValue, _session) as NodeReference : null;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const causedByPtrValue = objectJson["causedBy"];
        const unpackedCausedByPtr = causedByPtrValue != undefined ? _NodeReference.unpack(1, causedByPtrValue, _session) as NodeReference : null;
        return new (NODE_CLASS_BY_TYPE[2000105] as typeof PointerOverEvent)({
          position: _Vector2.unpack(1, objectJson["position"], _session) as Vector2,
          pressure: unpackedPressure,
          shiftKey: objectJson["shiftKey"],
          altKey: objectJson["altKey"],
          ctrlKey: objectJson["ctrlKey"],
          metaKey: objectJson["metaKey"],
          node: unpackedNodePtr,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          causedBy: unpackedCausedByPtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          client: _NodeReference.unpack(1, objectJson["client"], _session) as NodeReference,
          clientNonce: objectJson["clientNonce"],
          clientCreatedAt: Temporal.Instant.from(objectJson["clientCreatedAt"]).toZonedDateTimeISO('UTC'),
          clientEpoch: Number(objectJson["clientEpoch"]),
          status: EventStatus[objectJson["status"]] as any,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 2000105)] = new PointerOverEventJsonEncoder();

    class PointerLeaveEventJsonEncoder implements _JsonObjectEncoder {
      packObject(object: PointerLeaveEvent): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "POINTER_LEAVE_EVENT";
        objectJson["id"] = object.id;
        objectJson["space"] = object.spacePtr.pack(1);
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.causedByPtr != null) {
          objectJson["causedBy"] = object.causedByPtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["client"] = object.clientPtr.pack(1);
        objectJson["clientNonce"] = object.clientNonce;
        objectJson["clientCreatedAt"] = object.clientCreatedAt.toString({ timeZoneName: 'never' });
        objectJson["clientEpoch"] = Number(object.clientEpoch);
        objectJson["status"] = EventStatus[object.status];
        if (object.nodePtr != null) {
          objectJson["node"] = object.nodePtr.pack(1);
        }
        objectJson["position"] = object.position.pack(1);
        if (object.pressure != null) {
          objectJson["pressure"] = Number(object.pressure);
        }
        objectJson["shiftKey"] = object.shiftKey;
        objectJson["altKey"] = object.altKey;
        objectJson["ctrlKey"] = object.ctrlKey;
        objectJson["metaKey"] = object.metaKey;
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): PointerLeaveEvent {
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const _Vector2 = STRUCT_CLASS_BY_TYPE[2400000] as typeof Vector2;
        const pressureValue = objectJson["pressure"];
        const unpackedPressure = pressureValue != undefined ? Number(pressureValue) : null;
        const nodePtrValue = objectJson["node"];
        const unpackedNodePtr = nodePtrValue != undefined ? _NodeReference.unpack(1, nodePtrValue, _session) as NodeReference : null;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const causedByPtrValue = objectJson["causedBy"];
        const unpackedCausedByPtr = causedByPtrValue != undefined ? _NodeReference.unpack(1, causedByPtrValue, _session) as NodeReference : null;
        return new (NODE_CLASS_BY_TYPE[2000106] as typeof PointerLeaveEvent)({
          position: _Vector2.unpack(1, objectJson["position"], _session) as Vector2,
          pressure: unpackedPressure,
          shiftKey: objectJson["shiftKey"],
          altKey: objectJson["altKey"],
          ctrlKey: objectJson["ctrlKey"],
          metaKey: objectJson["metaKey"],
          node: unpackedNodePtr,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          causedBy: unpackedCausedByPtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          client: _NodeReference.unpack(1, objectJson["client"], _session) as NodeReference,
          clientNonce: objectJson["clientNonce"],
          clientCreatedAt: Temporal.Instant.from(objectJson["clientCreatedAt"]).toZonedDateTimeISO('UTC'),
          clientEpoch: Number(objectJson["clientEpoch"]),
          status: EventStatus[objectJson["status"]] as any,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 2000106)] = new PointerLeaveEventJsonEncoder();

    class PointerLongPressEventJsonEncoder implements _JsonObjectEncoder {
      packObject(object: PointerLongPressEvent): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "POINTER_LONG_PRESS_EVENT";
        objectJson["id"] = object.id;
        objectJson["space"] = object.spacePtr.pack(1);
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.causedByPtr != null) {
          objectJson["causedBy"] = object.causedByPtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["client"] = object.clientPtr.pack(1);
        objectJson["clientNonce"] = object.clientNonce;
        objectJson["clientCreatedAt"] = object.clientCreatedAt.toString({ timeZoneName: 'never' });
        objectJson["clientEpoch"] = Number(object.clientEpoch);
        objectJson["status"] = EventStatus[object.status];
        if (object.nodePtr != null) {
          objectJson["node"] = object.nodePtr.pack(1);
        }
        objectJson["position"] = object.position.pack(1);
        if (object.pressure != null) {
          objectJson["pressure"] = Number(object.pressure);
        }
        objectJson["shiftKey"] = object.shiftKey;
        objectJson["altKey"] = object.altKey;
        objectJson["ctrlKey"] = object.ctrlKey;
        objectJson["metaKey"] = object.metaKey;
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): PointerLongPressEvent {
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const _Vector2 = STRUCT_CLASS_BY_TYPE[2400000] as typeof Vector2;
        const pressureValue = objectJson["pressure"];
        const unpackedPressure = pressureValue != undefined ? Number(pressureValue) : null;
        const nodePtrValue = objectJson["node"];
        const unpackedNodePtr = nodePtrValue != undefined ? _NodeReference.unpack(1, nodePtrValue, _session) as NodeReference : null;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const causedByPtrValue = objectJson["causedBy"];
        const unpackedCausedByPtr = causedByPtrValue != undefined ? _NodeReference.unpack(1, causedByPtrValue, _session) as NodeReference : null;
        return new (NODE_CLASS_BY_TYPE[2000107] as typeof PointerLongPressEvent)({
          position: _Vector2.unpack(1, objectJson["position"], _session) as Vector2,
          pressure: unpackedPressure,
          shiftKey: objectJson["shiftKey"],
          altKey: objectJson["altKey"],
          ctrlKey: objectJson["ctrlKey"],
          metaKey: objectJson["metaKey"],
          node: unpackedNodePtr,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          causedBy: unpackedCausedByPtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          client: _NodeReference.unpack(1, objectJson["client"], _session) as NodeReference,
          clientNonce: objectJson["clientNonce"],
          clientCreatedAt: Temporal.Instant.from(objectJson["clientCreatedAt"]).toZonedDateTimeISO('UTC'),
          clientEpoch: Number(objectJson["clientEpoch"]),
          status: EventStatus[objectJson["status"]] as any,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 2000107)] = new PointerLongPressEventJsonEncoder();

    class SingleClickEventJsonEncoder implements _JsonObjectEncoder {
      packObject(object: SingleClickEvent): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "SINGLE_CLICK_EVENT";
        objectJson["id"] = object.id;
        objectJson["space"] = object.spacePtr.pack(1);
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.causedByPtr != null) {
          objectJson["causedBy"] = object.causedByPtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["client"] = object.clientPtr.pack(1);
        objectJson["clientNonce"] = object.clientNonce;
        objectJson["clientCreatedAt"] = object.clientCreatedAt.toString({ timeZoneName: 'never' });
        objectJson["clientEpoch"] = Number(object.clientEpoch);
        objectJson["status"] = EventStatus[object.status];
        if (object.nodePtr != null) {
          objectJson["node"] = object.nodePtr.pack(1);
        }
        objectJson["position"] = object.position.pack(1);
        if (object.pressure != null) {
          objectJson["pressure"] = Number(object.pressure);
        }
        objectJson["shiftKey"] = object.shiftKey;
        objectJson["altKey"] = object.altKey;
        objectJson["ctrlKey"] = object.ctrlKey;
        objectJson["metaKey"] = object.metaKey;
        objectJson["button"] = MouseButton[object.button];
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): SingleClickEvent {
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const _Vector2 = STRUCT_CLASS_BY_TYPE[2400000] as typeof Vector2;
        const pressureValue = objectJson["pressure"];
        const unpackedPressure = pressureValue != undefined ? Number(pressureValue) : null;
        const nodePtrValue = objectJson["node"];
        const unpackedNodePtr = nodePtrValue != undefined ? _NodeReference.unpack(1, nodePtrValue, _session) as NodeReference : null;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const causedByPtrValue = objectJson["causedBy"];
        const unpackedCausedByPtr = causedByPtrValue != undefined ? _NodeReference.unpack(1, causedByPtrValue, _session) as NodeReference : null;
        return new (NODE_CLASS_BY_TYPE[2000202] as typeof SingleClickEvent)({
          button: MouseButton[objectJson["button"]] as any,
          position: _Vector2.unpack(1, objectJson["position"], _session) as Vector2,
          pressure: unpackedPressure,
          shiftKey: objectJson["shiftKey"],
          altKey: objectJson["altKey"],
          ctrlKey: objectJson["ctrlKey"],
          metaKey: objectJson["metaKey"],
          node: unpackedNodePtr,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          causedBy: unpackedCausedByPtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          client: _NodeReference.unpack(1, objectJson["client"], _session) as NodeReference,
          clientNonce: objectJson["clientNonce"],
          clientCreatedAt: Temporal.Instant.from(objectJson["clientCreatedAt"]).toZonedDateTimeISO('UTC'),
          clientEpoch: Number(objectJson["clientEpoch"]),
          status: EventStatus[objectJson["status"]] as any,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 2000202)] = new SingleClickEventJsonEncoder();

    class DoubleClickEventJsonEncoder implements _JsonObjectEncoder {
      packObject(object: DoubleClickEvent): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "DOUBLE_CLICK_EVENT";
        objectJson["id"] = object.id;
        objectJson["space"] = object.spacePtr.pack(1);
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.causedByPtr != null) {
          objectJson["causedBy"] = object.causedByPtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["client"] = object.clientPtr.pack(1);
        objectJson["clientNonce"] = object.clientNonce;
        objectJson["clientCreatedAt"] = object.clientCreatedAt.toString({ timeZoneName: 'never' });
        objectJson["clientEpoch"] = Number(object.clientEpoch);
        objectJson["status"] = EventStatus[object.status];
        if (object.nodePtr != null) {
          objectJson["node"] = object.nodePtr.pack(1);
        }
        objectJson["position"] = object.position.pack(1);
        if (object.pressure != null) {
          objectJson["pressure"] = Number(object.pressure);
        }
        objectJson["shiftKey"] = object.shiftKey;
        objectJson["altKey"] = object.altKey;
        objectJson["ctrlKey"] = object.ctrlKey;
        objectJson["metaKey"] = object.metaKey;
        objectJson["button"] = MouseButton[object.button];
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): DoubleClickEvent {
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const _Vector2 = STRUCT_CLASS_BY_TYPE[2400000] as typeof Vector2;
        const pressureValue = objectJson["pressure"];
        const unpackedPressure = pressureValue != undefined ? Number(pressureValue) : null;
        const nodePtrValue = objectJson["node"];
        const unpackedNodePtr = nodePtrValue != undefined ? _NodeReference.unpack(1, nodePtrValue, _session) as NodeReference : null;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const causedByPtrValue = objectJson["causedBy"];
        const unpackedCausedByPtr = causedByPtrValue != undefined ? _NodeReference.unpack(1, causedByPtrValue, _session) as NodeReference : null;
        return new (NODE_CLASS_BY_TYPE[2000203] as typeof DoubleClickEvent)({
          button: MouseButton[objectJson["button"]] as any,
          position: _Vector2.unpack(1, objectJson["position"], _session) as Vector2,
          pressure: unpackedPressure,
          shiftKey: objectJson["shiftKey"],
          altKey: objectJson["altKey"],
          ctrlKey: objectJson["ctrlKey"],
          metaKey: objectJson["metaKey"],
          node: unpackedNodePtr,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          causedBy: unpackedCausedByPtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          client: _NodeReference.unpack(1, objectJson["client"], _session) as NodeReference,
          clientNonce: objectJson["clientNonce"],
          clientCreatedAt: Temporal.Instant.from(objectJson["clientCreatedAt"]).toZonedDateTimeISO('UTC'),
          clientEpoch: Number(objectJson["clientEpoch"]),
          status: EventStatus[objectJson["status"]] as any,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 2000203)] = new DoubleClickEventJsonEncoder();

    class TripleClickEventJsonEncoder implements _JsonObjectEncoder {
      packObject(object: TripleClickEvent): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "TRIPLE_CLICK_EVENT";
        objectJson["id"] = object.id;
        objectJson["space"] = object.spacePtr.pack(1);
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.causedByPtr != null) {
          objectJson["causedBy"] = object.causedByPtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["client"] = object.clientPtr.pack(1);
        objectJson["clientNonce"] = object.clientNonce;
        objectJson["clientCreatedAt"] = object.clientCreatedAt.toString({ timeZoneName: 'never' });
        objectJson["clientEpoch"] = Number(object.clientEpoch);
        objectJson["status"] = EventStatus[object.status];
        if (object.nodePtr != null) {
          objectJson["node"] = object.nodePtr.pack(1);
        }
        objectJson["position"] = object.position.pack(1);
        if (object.pressure != null) {
          objectJson["pressure"] = Number(object.pressure);
        }
        objectJson["shiftKey"] = object.shiftKey;
        objectJson["altKey"] = object.altKey;
        objectJson["ctrlKey"] = object.ctrlKey;
        objectJson["metaKey"] = object.metaKey;
        objectJson["button"] = MouseButton[object.button];
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): TripleClickEvent {
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const _Vector2 = STRUCT_CLASS_BY_TYPE[2400000] as typeof Vector2;
        const pressureValue = objectJson["pressure"];
        const unpackedPressure = pressureValue != undefined ? Number(pressureValue) : null;
        const nodePtrValue = objectJson["node"];
        const unpackedNodePtr = nodePtrValue != undefined ? _NodeReference.unpack(1, nodePtrValue, _session) as NodeReference : null;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const causedByPtrValue = objectJson["causedBy"];
        const unpackedCausedByPtr = causedByPtrValue != undefined ? _NodeReference.unpack(1, causedByPtrValue, _session) as NodeReference : null;
        return new (NODE_CLASS_BY_TYPE[2000204] as typeof TripleClickEvent)({
          button: MouseButton[objectJson["button"]] as any,
          position: _Vector2.unpack(1, objectJson["position"], _session) as Vector2,
          pressure: unpackedPressure,
          shiftKey: objectJson["shiftKey"],
          altKey: objectJson["altKey"],
          ctrlKey: objectJson["ctrlKey"],
          metaKey: objectJson["metaKey"],
          node: unpackedNodePtr,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          causedBy: unpackedCausedByPtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          client: _NodeReference.unpack(1, objectJson["client"], _session) as NodeReference,
          clientNonce: objectJson["clientNonce"],
          clientCreatedAt: Temporal.Instant.from(objectJson["clientCreatedAt"]).toZonedDateTimeISO('UTC'),
          clientEpoch: Number(objectJson["clientEpoch"]),
          status: EventStatus[objectJson["status"]] as any,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 2000204)] = new TripleClickEventJsonEncoder();

    class WheelEventJsonEncoder implements _JsonObjectEncoder {
      packObject(object: WheelEvent): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "WHEEL_EVENT";
        objectJson["id"] = object.id;
        objectJson["space"] = object.spacePtr.pack(1);
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.causedByPtr != null) {
          objectJson["causedBy"] = object.causedByPtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["client"] = object.clientPtr.pack(1);
        objectJson["clientNonce"] = object.clientNonce;
        objectJson["clientCreatedAt"] = object.clientCreatedAt.toString({ timeZoneName: 'never' });
        objectJson["clientEpoch"] = Number(object.clientEpoch);
        objectJson["status"] = EventStatus[object.status];
        if (object.nodePtr != null) {
          objectJson["node"] = object.nodePtr.pack(1);
        }
        objectJson["position"] = object.position.pack(1);
        if (object.pressure != null) {
          objectJson["pressure"] = Number(object.pressure);
        }
        objectJson["shiftKey"] = object.shiftKey;
        objectJson["altKey"] = object.altKey;
        objectJson["ctrlKey"] = object.ctrlKey;
        objectJson["metaKey"] = object.metaKey;
        objectJson["button"] = MouseButton[object.button];
        objectJson["delta"] = object.delta.pack(1);
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): WheelEvent {
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const _Vector2 = STRUCT_CLASS_BY_TYPE[2400000] as typeof Vector2;
        const pressureValue = objectJson["pressure"];
        const unpackedPressure = pressureValue != undefined ? Number(pressureValue) : null;
        const nodePtrValue = objectJson["node"];
        const unpackedNodePtr = nodePtrValue != undefined ? _NodeReference.unpack(1, nodePtrValue, _session) as NodeReference : null;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const causedByPtrValue = objectJson["causedBy"];
        const unpackedCausedByPtr = causedByPtrValue != undefined ? _NodeReference.unpack(1, causedByPtrValue, _session) as NodeReference : null;
        return new (NODE_CLASS_BY_TYPE[2000210] as typeof WheelEvent)({
          delta: _Vector2.unpack(1, objectJson["delta"], _session) as Vector2,
          button: MouseButton[objectJson["button"]] as any,
          position: _Vector2.unpack(1, objectJson["position"], _session) as Vector2,
          pressure: unpackedPressure,
          shiftKey: objectJson["shiftKey"],
          altKey: objectJson["altKey"],
          ctrlKey: objectJson["ctrlKey"],
          metaKey: objectJson["metaKey"],
          node: unpackedNodePtr,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          causedBy: unpackedCausedByPtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          client: _NodeReference.unpack(1, objectJson["client"], _session) as NodeReference,
          clientNonce: objectJson["clientNonce"],
          clientCreatedAt: Temporal.Instant.from(objectJson["clientCreatedAt"]).toZonedDateTimeISO('UTC'),
          clientEpoch: Number(objectJson["clientEpoch"]),
          status: EventStatus[objectJson["status"]] as any,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 2000210)] = new WheelEventJsonEncoder();

    class ScriptJsonEncoder implements _JsonObjectEncoder {
      packObject(object: Script): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "SCRIPT";
        objectJson["id"] = object.id;
        if (object.parentPtr != null) {
          objectJson["parent"] = object.parentPtr.pack(1);
        }
        objectJson["space"] = object.spacePtr.pack(1);
        objectJson["materialization"] = Materialization[object.materialization];
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.instancePtr != null) {
          objectJson["instance"] = object.instancePtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["updatedAt"] = object.updatedAt.toString({ timeZoneName: 'never' });
        objectJson["updatedEpoch"] = Number(object.updatedEpoch);
        objectJson["updatedBy"] = object.updatedByPtr.pack(1);
        if (object.deletedAt != null) {
          objectJson["deletedAt"] = object.deletedAt.toString({ timeZoneName: 'never' });
        }
        if (object._ownedByPtr != null) {
          objectJson["ownedBy"] = object._ownedByPtr.pack(1);
        }
        objectJson["name"] = object._name;
        objectJson["orderKey"] = object.orderKey;
        if (Object.keys(object._customValues).length > 0) {
          const packedCustomValues: { [key: string]: any } = {} as any;
          for (const [key, value] of Object.entries(object._customValues)) {
            packedCustomValues[String(key)] = value.pack(1);
          }
          objectJson["customValues"] = packedCustomValues;
        }
        if (object._scriptPtr != null) {
          objectJson["script"] = object._scriptPtr.pack(1);
        }
        if (object.isExtensible != null) {
          objectJson["isExtensible"] = object.isExtensible;
        }
        if (object.sourcePtr != null) {
          objectJson["source"] = object.sourcePtr.pack(1);
        }
        if (object._key != null) {
          objectJson["key"] = object._key;
        }
        objectJson["code"] = object._code;
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): Script {
        const _Value = STRUCT_CLASS_BY_TYPE[100] as typeof Value;
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const parentPtrValue = objectJson["parent"];
        const unpackedParentPtr = parentPtrValue != undefined ? _NodeReference.unpack(1, parentPtrValue, _session) as NodeReference : null;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const instancePtrValue = objectJson["instance"];
        const unpackedInstancePtr = instancePtrValue != undefined ? _NodeReference.unpack(1, instancePtrValue, _session) as NodeReference : null;
        const deletedAtValue = objectJson["deletedAt"];
        const unpackedDeletedAt = deletedAtValue != undefined ? Temporal.Instant.from(deletedAtValue).toZonedDateTimeISO('UTC') : null;
        const ownedByPtrValue = objectJson["ownedBy"];
        const unpackedOwnedByPtr = ownedByPtrValue != undefined ? _NodeReference.unpack(1, ownedByPtrValue, _session) as NodeReference : null;
        const unpackedCustomValues = {} as any;
        if (objectJson["customValues"] != undefined) {
          for (const [key, value] of Object.entries(objectJson["customValues"])) {
            unpackedCustomValues[key] = _Value.unpack(1, value as any, _session) as Value;
          }
        }
        const scriptPtrValue = objectJson["script"];
        const unpackedScriptPtr = scriptPtrValue != undefined ? _NodeReference.unpack(1, scriptPtrValue, _session) as NodeReference : null;
        const isExtensibleValue = objectJson["isExtensible"];
        const unpackedIsExtensible = isExtensibleValue != undefined ? isExtensibleValue : null;
        const sourcePtrValue = objectJson["source"];
        const unpackedSourcePtr = sourcePtrValue != undefined ? _NodeReference.unpack(1, sourcePtrValue, _session) as NodeReference : null;
        const keyValue = objectJson["key"];
        const unpackedKey = keyValue != undefined ? keyValue : null;
        return new (NODE_CLASS_BY_TYPE[700000] as typeof Script)({
          code: objectJson["code"],
          parent: unpackedParentPtr,
          materialization: Materialization[objectJson["materialization"]] as any,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          instance: unpackedInstancePtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          updatedAt: Temporal.Instant.from(objectJson["updatedAt"]).toZonedDateTimeISO('UTC'),
          updatedEpoch: Number(objectJson["updatedEpoch"]),
          updatedBy: _NodeReference.unpack(1, objectJson["updatedBy"], _session) as NodeReference,
          deletedAt: unpackedDeletedAt,
          ownedBy: unpackedOwnedByPtr,
          name: objectJson["name"],
          orderKey: objectJson["orderKey"],
          customValues: unpackedCustomValues,
          script: unpackedScriptPtr,
          isExtensible: unpackedIsExtensible,
          source: unpackedSourcePtr,
          key: unpackedKey,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 700000)] = new ScriptJsonEncoder();

    class ServiceJsonEncoder implements _JsonObjectEncoder {
      packObject(object: Service): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "SERVICE";
        objectJson["id"] = object.id;
        if (object.parentPtr != null) {
          objectJson["parent"] = object.parentPtr.pack(1);
        }
        objectJson["space"] = object.spacePtr.pack(1);
        objectJson["materialization"] = Materialization[object.materialization];
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.instancePtr != null) {
          objectJson["instance"] = object.instancePtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["updatedAt"] = object.updatedAt.toString({ timeZoneName: 'never' });
        objectJson["updatedEpoch"] = Number(object.updatedEpoch);
        objectJson["updatedBy"] = object.updatedByPtr.pack(1);
        if (object.deletedAt != null) {
          objectJson["deletedAt"] = object.deletedAt.toString({ timeZoneName: 'never' });
        }
        if (object._ownedByPtr != null) {
          objectJson["ownedBy"] = object._ownedByPtr.pack(1);
        }
        objectJson["name"] = object._name;
        objectJson["orderKey"] = object.orderKey;
        if (Object.keys(object._customValues).length > 0) {
          const packedCustomValues: { [key: string]: any } = {} as any;
          for (const [key, value] of Object.entries(object._customValues)) {
            packedCustomValues[String(key)] = value.pack(1);
          }
          objectJson["customValues"] = packedCustomValues;
        }
        if (object._scriptPtr != null) {
          objectJson["script"] = object._scriptPtr.pack(1);
        }
        if (object.isExtensible != null) {
          objectJson["isExtensible"] = object.isExtensible;
        }
        if (object.sourcePtr != null) {
          objectJson["source"] = object.sourcePtr.pack(1);
        }
        if (object._key != null) {
          objectJson["key"] = object._key;
        }
        if (object._icon != null) {
          objectJson["icon"] = object._icon.pack(1);
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): Service {
        const _Value = STRUCT_CLASS_BY_TYPE[100] as typeof Value;
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const _Icon = STRUCT_CLASS_BY_TYPE[400031] as typeof Icon;
        const iconValue = objectJson["icon"];
        const unpackedIcon = iconValue != undefined ? _Icon.unpack(1, iconValue, _session) as Icon : null;
        const parentPtrValue = objectJson["parent"];
        const unpackedParentPtr = parentPtrValue != undefined ? _NodeReference.unpack(1, parentPtrValue, _session) as NodeReference : null;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const instancePtrValue = objectJson["instance"];
        const unpackedInstancePtr = instancePtrValue != undefined ? _NodeReference.unpack(1, instancePtrValue, _session) as NodeReference : null;
        const deletedAtValue = objectJson["deletedAt"];
        const unpackedDeletedAt = deletedAtValue != undefined ? Temporal.Instant.from(deletedAtValue).toZonedDateTimeISO('UTC') : null;
        const ownedByPtrValue = objectJson["ownedBy"];
        const unpackedOwnedByPtr = ownedByPtrValue != undefined ? _NodeReference.unpack(1, ownedByPtrValue, _session) as NodeReference : null;
        const unpackedCustomValues = {} as any;
        if (objectJson["customValues"] != undefined) {
          for (const [key, value] of Object.entries(objectJson["customValues"])) {
            unpackedCustomValues[key] = _Value.unpack(1, value as any, _session) as Value;
          }
        }
        const scriptPtrValue = objectJson["script"];
        const unpackedScriptPtr = scriptPtrValue != undefined ? _NodeReference.unpack(1, scriptPtrValue, _session) as NodeReference : null;
        const isExtensibleValue = objectJson["isExtensible"];
        const unpackedIsExtensible = isExtensibleValue != undefined ? isExtensibleValue : null;
        const sourcePtrValue = objectJson["source"];
        const unpackedSourcePtr = sourcePtrValue != undefined ? _NodeReference.unpack(1, sourcePtrValue, _session) as NodeReference : null;
        const keyValue = objectJson["key"];
        const unpackedKey = keyValue != undefined ? keyValue : null;
        return new (NODE_CLASS_BY_TYPE[10300] as typeof Service)({
          icon: unpackedIcon,
          parent: unpackedParentPtr,
          materialization: Materialization[objectJson["materialization"]] as any,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          instance: unpackedInstancePtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          updatedAt: Temporal.Instant.from(objectJson["updatedAt"]).toZonedDateTimeISO('UTC'),
          updatedEpoch: Number(objectJson["updatedEpoch"]),
          updatedBy: _NodeReference.unpack(1, objectJson["updatedBy"], _session) as NodeReference,
          deletedAt: unpackedDeletedAt,
          ownedBy: unpackedOwnedByPtr,
          name: objectJson["name"],
          orderKey: objectJson["orderKey"],
          customValues: unpackedCustomValues,
          script: unpackedScriptPtr,
          isExtensible: unpackedIsExtensible,
          source: unpackedSourcePtr,
          key: unpackedKey,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 10300)] = new ServiceJsonEncoder();

    class TimerStartedEventJsonEncoder implements _JsonObjectEncoder {
      packObject(object: TimerStartedEvent): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "TIMER_STARTED_EVENT";
        objectJson["id"] = object.id;
        objectJson["space"] = object.spacePtr.pack(1);
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.causedByPtr != null) {
          objectJson["causedBy"] = object.causedByPtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["client"] = object.clientPtr.pack(1);
        objectJson["clientNonce"] = object.clientNonce;
        objectJson["clientCreatedAt"] = object.clientCreatedAt.toString({ timeZoneName: 'never' });
        objectJson["clientEpoch"] = Number(object.clientEpoch);
        objectJson["status"] = EventStatus[object.status];
        objectJson["node"] = object.nodePtr.pack(1);
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): TimerStartedEvent {
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const causedByPtrValue = objectJson["causedBy"];
        const unpackedCausedByPtr = causedByPtrValue != undefined ? _NodeReference.unpack(1, causedByPtrValue, _session) as NodeReference : null;
        return new (NODE_CLASS_BY_TYPE[705102] as typeof TimerStartedEvent)({
          node: _NodeReference.unpack(1, objectJson["node"], _session) as NodeReference,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          causedBy: unpackedCausedByPtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          client: _NodeReference.unpack(1, objectJson["client"], _session) as NodeReference,
          clientNonce: objectJson["clientNonce"],
          clientCreatedAt: Temporal.Instant.from(objectJson["clientCreatedAt"]).toZonedDateTimeISO('UTC'),
          clientEpoch: Number(objectJson["clientEpoch"]),
          status: EventStatus[objectJson["status"]] as any,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 705102)] = new TimerStartedEventJsonEncoder();

    class TimerPausedEventJsonEncoder implements _JsonObjectEncoder {
      packObject(object: TimerPausedEvent): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "TIMER_PAUSED_EVENT";
        objectJson["id"] = object.id;
        objectJson["space"] = object.spacePtr.pack(1);
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.causedByPtr != null) {
          objectJson["causedBy"] = object.causedByPtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["client"] = object.clientPtr.pack(1);
        objectJson["clientNonce"] = object.clientNonce;
        objectJson["clientCreatedAt"] = object.clientCreatedAt.toString({ timeZoneName: 'never' });
        objectJson["clientEpoch"] = Number(object.clientEpoch);
        objectJson["status"] = EventStatus[object.status];
        objectJson["node"] = object.nodePtr.pack(1);
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): TimerPausedEvent {
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const causedByPtrValue = objectJson["causedBy"];
        const unpackedCausedByPtr = causedByPtrValue != undefined ? _NodeReference.unpack(1, causedByPtrValue, _session) as NodeReference : null;
        return new (NODE_CLASS_BY_TYPE[705103] as typeof TimerPausedEvent)({
          node: _NodeReference.unpack(1, objectJson["node"], _session) as NodeReference,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          causedBy: unpackedCausedByPtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          client: _NodeReference.unpack(1, objectJson["client"], _session) as NodeReference,
          clientNonce: objectJson["clientNonce"],
          clientCreatedAt: Temporal.Instant.from(objectJson["clientCreatedAt"]).toZonedDateTimeISO('UTC'),
          clientEpoch: Number(objectJson["clientEpoch"]),
          status: EventStatus[objectJson["status"]] as any,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 705103)] = new TimerPausedEventJsonEncoder();

    class TimerResumedEventJsonEncoder implements _JsonObjectEncoder {
      packObject(object: TimerResumedEvent): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "TIMER_RESUMED_EVENT";
        objectJson["id"] = object.id;
        objectJson["space"] = object.spacePtr.pack(1);
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.causedByPtr != null) {
          objectJson["causedBy"] = object.causedByPtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["client"] = object.clientPtr.pack(1);
        objectJson["clientNonce"] = object.clientNonce;
        objectJson["clientCreatedAt"] = object.clientCreatedAt.toString({ timeZoneName: 'never' });
        objectJson["clientEpoch"] = Number(object.clientEpoch);
        objectJson["status"] = EventStatus[object.status];
        objectJson["node"] = object.nodePtr.pack(1);
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): TimerResumedEvent {
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const causedByPtrValue = objectJson["causedBy"];
        const unpackedCausedByPtr = causedByPtrValue != undefined ? _NodeReference.unpack(1, causedByPtrValue, _session) as NodeReference : null;
        return new (NODE_CLASS_BY_TYPE[705104] as typeof TimerResumedEvent)({
          node: _NodeReference.unpack(1, objectJson["node"], _session) as NodeReference,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          causedBy: unpackedCausedByPtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          client: _NodeReference.unpack(1, objectJson["client"], _session) as NodeReference,
          clientNonce: objectJson["clientNonce"],
          clientCreatedAt: Temporal.Instant.from(objectJson["clientCreatedAt"]).toZonedDateTimeISO('UTC'),
          clientEpoch: Number(objectJson["clientEpoch"]),
          status: EventStatus[objectJson["status"]] as any,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 705104)] = new TimerResumedEventJsonEncoder();

    class TimerCompletedEventJsonEncoder implements _JsonObjectEncoder {
      packObject(object: TimerCompletedEvent): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "TIMER_COMPLETED_EVENT";
        objectJson["id"] = object.id;
        objectJson["space"] = object.spacePtr.pack(1);
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.causedByPtr != null) {
          objectJson["causedBy"] = object.causedByPtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["client"] = object.clientPtr.pack(1);
        objectJson["clientNonce"] = object.clientNonce;
        objectJson["clientCreatedAt"] = object.clientCreatedAt.toString({ timeZoneName: 'never' });
        objectJson["clientEpoch"] = Number(object.clientEpoch);
        objectJson["status"] = EventStatus[object.status];
        objectJson["node"] = object.nodePtr.pack(1);
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): TimerCompletedEvent {
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const causedByPtrValue = objectJson["causedBy"];
        const unpackedCausedByPtr = causedByPtrValue != undefined ? _NodeReference.unpack(1, causedByPtrValue, _session) as NodeReference : null;
        return new (NODE_CLASS_BY_TYPE[705105] as typeof TimerCompletedEvent)({
          node: _NodeReference.unpack(1, objectJson["node"], _session) as NodeReference,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          causedBy: unpackedCausedByPtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          client: _NodeReference.unpack(1, objectJson["client"], _session) as NodeReference,
          clientNonce: objectJson["clientNonce"],
          clientCreatedAt: Temporal.Instant.from(objectJson["clientCreatedAt"]).toZonedDateTimeISO('UTC'),
          clientEpoch: Number(objectJson["clientEpoch"]),
          status: EventStatus[objectJson["status"]] as any,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 705105)] = new TimerCompletedEventJsonEncoder();

    class TimerCancelledEventJsonEncoder implements _JsonObjectEncoder {
      packObject(object: TimerCancelledEvent): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "TIMER_CANCELLED_EVENT";
        objectJson["id"] = object.id;
        objectJson["space"] = object.spacePtr.pack(1);
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.causedByPtr != null) {
          objectJson["causedBy"] = object.causedByPtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["client"] = object.clientPtr.pack(1);
        objectJson["clientNonce"] = object.clientNonce;
        objectJson["clientCreatedAt"] = object.clientCreatedAt.toString({ timeZoneName: 'never' });
        objectJson["clientEpoch"] = Number(object.clientEpoch);
        objectJson["status"] = EventStatus[object.status];
        objectJson["node"] = object.nodePtr.pack(1);
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): TimerCancelledEvent {
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const causedByPtrValue = objectJson["causedBy"];
        const unpackedCausedByPtr = causedByPtrValue != undefined ? _NodeReference.unpack(1, causedByPtrValue, _session) as NodeReference : null;
        return new (NODE_CLASS_BY_TYPE[705106] as typeof TimerCancelledEvent)({
          node: _NodeReference.unpack(1, objectJson["node"], _session) as NodeReference,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          causedBy: unpackedCausedByPtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          client: _NodeReference.unpack(1, objectJson["client"], _session) as NodeReference,
          clientNonce: objectJson["clientNonce"],
          clientCreatedAt: Temporal.Instant.from(objectJson["clientCreatedAt"]).toZonedDateTimeISO('UTC'),
          clientEpoch: Number(objectJson["clientEpoch"]),
          status: EventStatus[objectJson["status"]] as any,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 705106)] = new TimerCancelledEventJsonEncoder();

    class TimerJsonEncoder implements _JsonObjectEncoder {
      packObject(object: Timer): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "TIMER";
        objectJson["id"] = object.id;
        if (object.parentPtr != null) {
          objectJson["parent"] = object.parentPtr.pack(1);
        }
        objectJson["space"] = object.spacePtr.pack(1);
        objectJson["materialization"] = Materialization[object.materialization];
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.instancePtr != null) {
          objectJson["instance"] = object.instancePtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["updatedAt"] = object.updatedAt.toString({ timeZoneName: 'never' });
        objectJson["updatedEpoch"] = Number(object.updatedEpoch);
        objectJson["updatedBy"] = object.updatedByPtr.pack(1);
        if (object.deletedAt != null) {
          objectJson["deletedAt"] = object.deletedAt.toString({ timeZoneName: 'never' });
        }
        if (object._ownedByPtr != null) {
          objectJson["ownedBy"] = object._ownedByPtr.pack(1);
        }
        objectJson["name"] = object._name;
        objectJson["orderKey"] = object.orderKey;
        if (Object.keys(object._customValues).length > 0) {
          const packedCustomValues: { [key: string]: any } = {} as any;
          for (const [key, value] of Object.entries(object._customValues)) {
            packedCustomValues[String(key)] = value.pack(1);
          }
          objectJson["customValues"] = packedCustomValues;
        }
        if (object._scriptPtr != null) {
          objectJson["script"] = object._scriptPtr.pack(1);
        }
        if (object.isExtensible != null) {
          objectJson["isExtensible"] = object.isExtensible;
        }
        if (object.sourcePtr != null) {
          objectJson["source"] = object.sourcePtr.pack(1);
        }
        if (object._key != null) {
          objectJson["key"] = object._key;
        }
        objectJson["type"] = TimerType[object._type];
        if (object._schedule != null) {
          objectJson["schedule"] = object._schedule.pack(1);
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): Timer {
        const _Value = STRUCT_CLASS_BY_TYPE[100] as typeof Value;
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const _Schedule = STRUCT_CLASS_BY_TYPE[700001] as typeof Schedule;
        const scheduleValue = objectJson["schedule"];
        const unpackedSchedule = scheduleValue != undefined ? _Schedule.unpack(1, scheduleValue, _session) as Schedule : null;
        const parentPtrValue = objectJson["parent"];
        const unpackedParentPtr = parentPtrValue != undefined ? _NodeReference.unpack(1, parentPtrValue, _session) as NodeReference : null;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const instancePtrValue = objectJson["instance"];
        const unpackedInstancePtr = instancePtrValue != undefined ? _NodeReference.unpack(1, instancePtrValue, _session) as NodeReference : null;
        const deletedAtValue = objectJson["deletedAt"];
        const unpackedDeletedAt = deletedAtValue != undefined ? Temporal.Instant.from(deletedAtValue).toZonedDateTimeISO('UTC') : null;
        const ownedByPtrValue = objectJson["ownedBy"];
        const unpackedOwnedByPtr = ownedByPtrValue != undefined ? _NodeReference.unpack(1, ownedByPtrValue, _session) as NodeReference : null;
        const unpackedCustomValues = {} as any;
        if (objectJson["customValues"] != undefined) {
          for (const [key, value] of Object.entries(objectJson["customValues"])) {
            unpackedCustomValues[key] = _Value.unpack(1, value as any, _session) as Value;
          }
        }
        const scriptPtrValue = objectJson["script"];
        const unpackedScriptPtr = scriptPtrValue != undefined ? _NodeReference.unpack(1, scriptPtrValue, _session) as NodeReference : null;
        const isExtensibleValue = objectJson["isExtensible"];
        const unpackedIsExtensible = isExtensibleValue != undefined ? isExtensibleValue : null;
        const sourcePtrValue = objectJson["source"];
        const unpackedSourcePtr = sourcePtrValue != undefined ? _NodeReference.unpack(1, sourcePtrValue, _session) as NodeReference : null;
        const keyValue = objectJson["key"];
        const unpackedKey = keyValue != undefined ? keyValue : null;
        return new (NODE_CLASS_BY_TYPE[705100] as typeof Timer)({
          type: TimerType[objectJson["type"]] as any,
          schedule: unpackedSchedule,
          parent: unpackedParentPtr,
          materialization: Materialization[objectJson["materialization"]] as any,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          instance: unpackedInstancePtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          updatedAt: Temporal.Instant.from(objectJson["updatedAt"]).toZonedDateTimeISO('UTC'),
          updatedEpoch: Number(objectJson["updatedEpoch"]),
          updatedBy: _NodeReference.unpack(1, objectJson["updatedBy"], _session) as NodeReference,
          deletedAt: unpackedDeletedAt,
          ownedBy: unpackedOwnedByPtr,
          name: objectJson["name"],
          orderKey: objectJson["orderKey"],
          customValues: unpackedCustomValues,
          script: unpackedScriptPtr,
          isExtensible: unpackedIsExtensible,
          source: unpackedSourcePtr,
          key: unpackedKey,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 705100)] = new TimerJsonEncoder();

    class TriggerJsonEncoder implements _JsonObjectEncoder {
      packObject(object: Trigger): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "TRIGGER";
        objectJson["id"] = object.id;
        if (object.parentPtr != null) {
          objectJson["parent"] = object.parentPtr.pack(1);
        }
        objectJson["space"] = object.spacePtr.pack(1);
        objectJson["materialization"] = Materialization[object.materialization];
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.instancePtr != null) {
          objectJson["instance"] = object.instancePtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["updatedAt"] = object.updatedAt.toString({ timeZoneName: 'never' });
        objectJson["updatedEpoch"] = Number(object.updatedEpoch);
        objectJson["updatedBy"] = object.updatedByPtr.pack(1);
        if (object.deletedAt != null) {
          objectJson["deletedAt"] = object.deletedAt.toString({ timeZoneName: 'never' });
        }
        if (object._ownedByPtr != null) {
          objectJson["ownedBy"] = object._ownedByPtr.pack(1);
        }
        objectJson["name"] = object._name;
        objectJson["orderKey"] = object.orderKey;
        if (Object.keys(object._customValues).length > 0) {
          const packedCustomValues: { [key: string]: any } = {} as any;
          for (const [key, value] of Object.entries(object._customValues)) {
            packedCustomValues[String(key)] = value.pack(1);
          }
          objectJson["customValues"] = packedCustomValues;
        }
        if (object._scriptPtr != null) {
          objectJson["script"] = object._scriptPtr.pack(1);
        }
        if (object.isExtensible != null) {
          objectJson["isExtensible"] = object.isExtensible;
        }
        if (object.sourcePtr != null) {
          objectJson["source"] = object.sourcePtr.pack(1);
        }
        if (object._key != null) {
          objectJson["key"] = object._key;
        }
        if (object._icon != null) {
          objectJson["icon"] = object._icon.pack(1);
        }
        if (object._event != null) {
          objectJson["event"] = object._event.pack(1);
        }
        if (object._where != null) {
          objectJson["where"] = object._where.pack(1);
        }
        if (object._targetPtr != null) {
          objectJson["target"] = object._targetPtr.pack(1);
        }
        if (Object.keys(object._arguments).length > 0) {
          const packedArguments: { [key: string]: any } = {} as any;
          for (const [key, value] of Object.entries(object._arguments)) {
            packedArguments[String(key)] = value.pack(1);
          }
          objectJson["arguments"] = packedArguments;
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): Trigger {
        const _NodeDefinitionReference = STRUCT_CLASS_BY_TYPE[13] as typeof NodeDefinitionReference;
        const _Value = STRUCT_CLASS_BY_TYPE[100] as typeof Value;
        const _Condition = STRUCT_CLASS_BY_TYPE[204] as typeof Condition;
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const _Icon = STRUCT_CLASS_BY_TYPE[400031] as typeof Icon;
        const iconValue = objectJson["icon"];
        const unpackedIcon = iconValue != undefined ? _Icon.unpack(1, iconValue, _session) as Icon : null;
        const eventValue = objectJson["event"];
        const unpackedEvent = eventValue != undefined ? _NodeDefinitionReference.unpack(1, eventValue, _session) as NodeDefinitionReference : null;
        const whereValue = objectJson["where"];
        const unpackedWhere = whereValue != undefined ? _Condition.unpack(1, whereValue, _session) as Condition : null;
        const targetPtrValue = objectJson["target"];
        const unpackedTargetPtr = targetPtrValue != undefined ? _NodeReference.unpack(1, targetPtrValue, _session) as NodeReference : null;
        const unpackedArguments = {} as any;
        if (objectJson["arguments"] != undefined) {
          for (const [key, value] of Object.entries(objectJson["arguments"])) {
            unpackedArguments[key] = _Value.unpack(1, value as any, _session) as Value;
          }
        }
        const parentPtrValue = objectJson["parent"];
        const unpackedParentPtr = parentPtrValue != undefined ? _NodeReference.unpack(1, parentPtrValue, _session) as NodeReference : null;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const instancePtrValue = objectJson["instance"];
        const unpackedInstancePtr = instancePtrValue != undefined ? _NodeReference.unpack(1, instancePtrValue, _session) as NodeReference : null;
        const deletedAtValue = objectJson["deletedAt"];
        const unpackedDeletedAt = deletedAtValue != undefined ? Temporal.Instant.from(deletedAtValue).toZonedDateTimeISO('UTC') : null;
        const ownedByPtrValue = objectJson["ownedBy"];
        const unpackedOwnedByPtr = ownedByPtrValue != undefined ? _NodeReference.unpack(1, ownedByPtrValue, _session) as NodeReference : null;
        const unpackedCustomValues = {} as any;
        if (objectJson["customValues"] != undefined) {
          for (const [key, value] of Object.entries(objectJson["customValues"])) {
            unpackedCustomValues[key] = _Value.unpack(1, value as any, _session) as Value;
          }
        }
        const scriptPtrValue = objectJson["script"];
        const unpackedScriptPtr = scriptPtrValue != undefined ? _NodeReference.unpack(1, scriptPtrValue, _session) as NodeReference : null;
        const isExtensibleValue = objectJson["isExtensible"];
        const unpackedIsExtensible = isExtensibleValue != undefined ? isExtensibleValue : null;
        const sourcePtrValue = objectJson["source"];
        const unpackedSourcePtr = sourcePtrValue != undefined ? _NodeReference.unpack(1, sourcePtrValue, _session) as NodeReference : null;
        const keyValue = objectJson["key"];
        const unpackedKey = keyValue != undefined ? keyValue : null;
        return new (NODE_CLASS_BY_TYPE[705000] as typeof Trigger)({
          icon: unpackedIcon,
          event: unpackedEvent,
          where: unpackedWhere,
          target: unpackedTargetPtr,
          arguments: unpackedArguments,
          parent: unpackedParentPtr,
          materialization: Materialization[objectJson["materialization"]] as any,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          instance: unpackedInstancePtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          updatedAt: Temporal.Instant.from(objectJson["updatedAt"]).toZonedDateTimeISO('UTC'),
          updatedEpoch: Number(objectJson["updatedEpoch"]),
          updatedBy: _NodeReference.unpack(1, objectJson["updatedBy"], _session) as NodeReference,
          deletedAt: unpackedDeletedAt,
          ownedBy: unpackedOwnedByPtr,
          name: objectJson["name"],
          orderKey: objectJson["orderKey"],
          customValues: unpackedCustomValues,
          script: unpackedScriptPtr,
          isExtensible: unpackedIsExtensible,
          source: unpackedSourcePtr,
          key: unpackedKey,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 705000)] = new TriggerJsonEncoder();

    class GaugeMetricJsonEncoder implements _JsonObjectEncoder {
      packObject(object: GaugeMetric): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "GAUGE_METRIC";
        objectJson["id"] = object.id;
        if (object.parentPtr != null) {
          objectJson["parent"] = object.parentPtr.pack(1);
        }
        objectJson["space"] = object.spacePtr.pack(1);
        objectJson["materialization"] = Materialization[object.materialization];
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.instancePtr != null) {
          objectJson["instance"] = object.instancePtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["updatedAt"] = object.updatedAt.toString({ timeZoneName: 'never' });
        objectJson["updatedEpoch"] = Number(object.updatedEpoch);
        objectJson["updatedBy"] = object.updatedByPtr.pack(1);
        if (object.deletedAt != null) {
          objectJson["deletedAt"] = object.deletedAt.toString({ timeZoneName: 'never' });
        }
        if (object._ownedByPtr != null) {
          objectJson["ownedBy"] = object._ownedByPtr.pack(1);
        }
        objectJson["name"] = object._name;
        objectJson["orderKey"] = object.orderKey;
        if (Object.keys(object._customValues).length > 0) {
          const packedCustomValues: { [key: string]: any } = {} as any;
          for (const [key, value] of Object.entries(object._customValues)) {
            packedCustomValues[String(key)] = value.pack(1);
          }
          objectJson["customValues"] = packedCustomValues;
        }
        if (object._scriptPtr != null) {
          objectJson["script"] = object._scriptPtr.pack(1);
        }
        if (object.isExtensible != null) {
          objectJson["isExtensible"] = object.isExtensible;
        }
        if (object.sourcePtr != null) {
          objectJson["source"] = object.sourcePtr.pack(1);
        }
        if (object._key != null) {
          objectJson["key"] = object._key;
        }
        if (object._icon != null) {
          objectJson["icon"] = object._icon.pack(1);
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): GaugeMetric {
        const _Value = STRUCT_CLASS_BY_TYPE[100] as typeof Value;
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const _Icon = STRUCT_CLASS_BY_TYPE[400031] as typeof Icon;
        const iconValue = objectJson["icon"];
        const unpackedIcon = iconValue != undefined ? _Icon.unpack(1, iconValue, _session) as Icon : null;
        const parentPtrValue = objectJson["parent"];
        const unpackedParentPtr = parentPtrValue != undefined ? _NodeReference.unpack(1, parentPtrValue, _session) as NodeReference : null;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const instancePtrValue = objectJson["instance"];
        const unpackedInstancePtr = instancePtrValue != undefined ? _NodeReference.unpack(1, instancePtrValue, _session) as NodeReference : null;
        const deletedAtValue = objectJson["deletedAt"];
        const unpackedDeletedAt = deletedAtValue != undefined ? Temporal.Instant.from(deletedAtValue).toZonedDateTimeISO('UTC') : null;
        const ownedByPtrValue = objectJson["ownedBy"];
        const unpackedOwnedByPtr = ownedByPtrValue != undefined ? _NodeReference.unpack(1, ownedByPtrValue, _session) as NodeReference : null;
        const unpackedCustomValues = {} as any;
        if (objectJson["customValues"] != undefined) {
          for (const [key, value] of Object.entries(objectJson["customValues"])) {
            unpackedCustomValues[key] = _Value.unpack(1, value as any, _session) as Value;
          }
        }
        const scriptPtrValue = objectJson["script"];
        const unpackedScriptPtr = scriptPtrValue != undefined ? _NodeReference.unpack(1, scriptPtrValue, _session) as NodeReference : null;
        const isExtensibleValue = objectJson["isExtensible"];
        const unpackedIsExtensible = isExtensibleValue != undefined ? isExtensibleValue : null;
        const sourcePtrValue = objectJson["source"];
        const unpackedSourcePtr = sourcePtrValue != undefined ? _NodeReference.unpack(1, sourcePtrValue, _session) as NodeReference : null;
        const keyValue = objectJson["key"];
        const unpackedKey = keyValue != undefined ? keyValue : null;
        return new (NODE_CLASS_BY_TYPE[1200000] as typeof GaugeMetric)({
          icon: unpackedIcon,
          parent: unpackedParentPtr,
          materialization: Materialization[objectJson["materialization"]] as any,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          instance: unpackedInstancePtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          updatedAt: Temporal.Instant.from(objectJson["updatedAt"]).toZonedDateTimeISO('UTC'),
          updatedEpoch: Number(objectJson["updatedEpoch"]),
          updatedBy: _NodeReference.unpack(1, objectJson["updatedBy"], _session) as NodeReference,
          deletedAt: unpackedDeletedAt,
          ownedBy: unpackedOwnedByPtr,
          name: objectJson["name"],
          orderKey: objectJson["orderKey"],
          customValues: unpackedCustomValues,
          script: unpackedScriptPtr,
          isExtensible: unpackedIsExtensible,
          source: unpackedSourcePtr,
          key: unpackedKey,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 1200000)] = new GaugeMetricJsonEncoder();

    class GaugeMeasurementEventJsonEncoder implements _JsonObjectEncoder {
      packObject(object: GaugeMeasurementEvent): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "GAUGE_MEASUREMENT_EVENT";
        objectJson["id"] = object.id;
        objectJson["space"] = object.spacePtr.pack(1);
        objectJson["definition"] = object.definitionPtr.pack(1);
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.causedByPtr != null) {
          objectJson["causedBy"] = object.causedByPtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["client"] = object.clientPtr.pack(1);
        objectJson["clientNonce"] = object.clientNonce;
        objectJson["clientCreatedAt"] = object.clientCreatedAt.toString({ timeZoneName: 'never' });
        objectJson["clientEpoch"] = Number(object.clientEpoch);
        objectJson["status"] = EventStatus[object.status];
        if (object.nodePtr != null) {
          objectJson["node"] = object.nodePtr.pack(1);
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): GaugeMeasurementEvent {
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const causedByPtrValue = objectJson["causedBy"];
        const unpackedCausedByPtr = causedByPtrValue != undefined ? _NodeReference.unpack(1, causedByPtrValue, _session) as NodeReference : null;
        const nodePtrValue = objectJson["node"];
        const unpackedNodePtr = nodePtrValue != undefined ? _NodeReference.unpack(1, nodePtrValue, _session) as NodeReference : null;
        return new (NODE_CLASS_BY_TYPE[1200001] as typeof GaugeMeasurementEvent)({
          definition: _NodeReference.unpack(1, objectJson["definition"], _session) as NodeReference,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          causedBy: unpackedCausedByPtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          client: _NodeReference.unpack(1, objectJson["client"], _session) as NodeReference,
          clientNonce: objectJson["clientNonce"],
          clientCreatedAt: Temporal.Instant.from(objectJson["clientCreatedAt"]).toZonedDateTimeISO('UTC'),
          clientEpoch: Number(objectJson["clientEpoch"]),
          status: EventStatus[objectJson["status"]] as any,
          node: unpackedNodePtr,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 1200001)] = new GaugeMeasurementEventJsonEncoder();

    class CounterMetricJsonEncoder implements _JsonObjectEncoder {
      packObject(object: CounterMetric): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "COUNTER_METRIC";
        objectJson["id"] = object.id;
        if (object.parentPtr != null) {
          objectJson["parent"] = object.parentPtr.pack(1);
        }
        objectJson["space"] = object.spacePtr.pack(1);
        objectJson["materialization"] = Materialization[object.materialization];
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.instancePtr != null) {
          objectJson["instance"] = object.instancePtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["updatedAt"] = object.updatedAt.toString({ timeZoneName: 'never' });
        objectJson["updatedEpoch"] = Number(object.updatedEpoch);
        objectJson["updatedBy"] = object.updatedByPtr.pack(1);
        if (object.deletedAt != null) {
          objectJson["deletedAt"] = object.deletedAt.toString({ timeZoneName: 'never' });
        }
        if (object._ownedByPtr != null) {
          objectJson["ownedBy"] = object._ownedByPtr.pack(1);
        }
        objectJson["name"] = object._name;
        objectJson["orderKey"] = object.orderKey;
        if (Object.keys(object._customValues).length > 0) {
          const packedCustomValues: { [key: string]: any } = {} as any;
          for (const [key, value] of Object.entries(object._customValues)) {
            packedCustomValues[String(key)] = value.pack(1);
          }
          objectJson["customValues"] = packedCustomValues;
        }
        if (object._scriptPtr != null) {
          objectJson["script"] = object._scriptPtr.pack(1);
        }
        if (object.isExtensible != null) {
          objectJson["isExtensible"] = object.isExtensible;
        }
        if (object.sourcePtr != null) {
          objectJson["source"] = object.sourcePtr.pack(1);
        }
        if (object._key != null) {
          objectJson["key"] = object._key;
        }
        if (object._icon != null) {
          objectJson["icon"] = object._icon.pack(1);
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): CounterMetric {
        const _Value = STRUCT_CLASS_BY_TYPE[100] as typeof Value;
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const _Icon = STRUCT_CLASS_BY_TYPE[400031] as typeof Icon;
        const iconValue = objectJson["icon"];
        const unpackedIcon = iconValue != undefined ? _Icon.unpack(1, iconValue, _session) as Icon : null;
        const parentPtrValue = objectJson["parent"];
        const unpackedParentPtr = parentPtrValue != undefined ? _NodeReference.unpack(1, parentPtrValue, _session) as NodeReference : null;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const instancePtrValue = objectJson["instance"];
        const unpackedInstancePtr = instancePtrValue != undefined ? _NodeReference.unpack(1, instancePtrValue, _session) as NodeReference : null;
        const deletedAtValue = objectJson["deletedAt"];
        const unpackedDeletedAt = deletedAtValue != undefined ? Temporal.Instant.from(deletedAtValue).toZonedDateTimeISO('UTC') : null;
        const ownedByPtrValue = objectJson["ownedBy"];
        const unpackedOwnedByPtr = ownedByPtrValue != undefined ? _NodeReference.unpack(1, ownedByPtrValue, _session) as NodeReference : null;
        const unpackedCustomValues = {} as any;
        if (objectJson["customValues"] != undefined) {
          for (const [key, value] of Object.entries(objectJson["customValues"])) {
            unpackedCustomValues[key] = _Value.unpack(1, value as any, _session) as Value;
          }
        }
        const scriptPtrValue = objectJson["script"];
        const unpackedScriptPtr = scriptPtrValue != undefined ? _NodeReference.unpack(1, scriptPtrValue, _session) as NodeReference : null;
        const isExtensibleValue = objectJson["isExtensible"];
        const unpackedIsExtensible = isExtensibleValue != undefined ? isExtensibleValue : null;
        const sourcePtrValue = objectJson["source"];
        const unpackedSourcePtr = sourcePtrValue != undefined ? _NodeReference.unpack(1, sourcePtrValue, _session) as NodeReference : null;
        const keyValue = objectJson["key"];
        const unpackedKey = keyValue != undefined ? keyValue : null;
        return new (NODE_CLASS_BY_TYPE[1200100] as typeof CounterMetric)({
          icon: unpackedIcon,
          parent: unpackedParentPtr,
          materialization: Materialization[objectJson["materialization"]] as any,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          instance: unpackedInstancePtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          updatedAt: Temporal.Instant.from(objectJson["updatedAt"]).toZonedDateTimeISO('UTC'),
          updatedEpoch: Number(objectJson["updatedEpoch"]),
          updatedBy: _NodeReference.unpack(1, objectJson["updatedBy"], _session) as NodeReference,
          deletedAt: unpackedDeletedAt,
          ownedBy: unpackedOwnedByPtr,
          name: objectJson["name"],
          orderKey: objectJson["orderKey"],
          customValues: unpackedCustomValues,
          script: unpackedScriptPtr,
          isExtensible: unpackedIsExtensible,
          source: unpackedSourcePtr,
          key: unpackedKey,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 1200100)] = new CounterMetricJsonEncoder();

    class CounterMeasurementEventJsonEncoder implements _JsonObjectEncoder {
      packObject(object: CounterMeasurementEvent): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "COUNTER_MEASUREMENT_EVENT";
        objectJson["id"] = object.id;
        objectJson["space"] = object.spacePtr.pack(1);
        objectJson["definition"] = object.definitionPtr.pack(1);
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.causedByPtr != null) {
          objectJson["causedBy"] = object.causedByPtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["client"] = object.clientPtr.pack(1);
        objectJson["clientNonce"] = object.clientNonce;
        objectJson["clientCreatedAt"] = object.clientCreatedAt.toString({ timeZoneName: 'never' });
        objectJson["clientEpoch"] = Number(object.clientEpoch);
        objectJson["status"] = EventStatus[object.status];
        if (object.nodePtr != null) {
          objectJson["node"] = object.nodePtr.pack(1);
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): CounterMeasurementEvent {
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const causedByPtrValue = objectJson["causedBy"];
        const unpackedCausedByPtr = causedByPtrValue != undefined ? _NodeReference.unpack(1, causedByPtrValue, _session) as NodeReference : null;
        const nodePtrValue = objectJson["node"];
        const unpackedNodePtr = nodePtrValue != undefined ? _NodeReference.unpack(1, nodePtrValue, _session) as NodeReference : null;
        return new (NODE_CLASS_BY_TYPE[1200101] as typeof CounterMeasurementEvent)({
          definition: _NodeReference.unpack(1, objectJson["definition"], _session) as NodeReference,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          causedBy: unpackedCausedByPtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          client: _NodeReference.unpack(1, objectJson["client"], _session) as NodeReference,
          clientNonce: objectJson["clientNonce"],
          clientCreatedAt: Temporal.Instant.from(objectJson["clientCreatedAt"]).toZonedDateTimeISO('UTC'),
          clientEpoch: Number(objectJson["clientEpoch"]),
          status: EventStatus[objectJson["status"]] as any,
          node: unpackedNodePtr,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 1200101)] = new CounterMeasurementEventJsonEncoder();

    class HistogramMetricJsonEncoder implements _JsonObjectEncoder {
      packObject(object: HistogramMetric): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "HISTOGRAM_METRIC";
        objectJson["id"] = object.id;
        if (object.parentPtr != null) {
          objectJson["parent"] = object.parentPtr.pack(1);
        }
        objectJson["space"] = object.spacePtr.pack(1);
        objectJson["materialization"] = Materialization[object.materialization];
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.instancePtr != null) {
          objectJson["instance"] = object.instancePtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["updatedAt"] = object.updatedAt.toString({ timeZoneName: 'never' });
        objectJson["updatedEpoch"] = Number(object.updatedEpoch);
        objectJson["updatedBy"] = object.updatedByPtr.pack(1);
        if (object.deletedAt != null) {
          objectJson["deletedAt"] = object.deletedAt.toString({ timeZoneName: 'never' });
        }
        if (object._ownedByPtr != null) {
          objectJson["ownedBy"] = object._ownedByPtr.pack(1);
        }
        objectJson["name"] = object._name;
        objectJson["orderKey"] = object.orderKey;
        if (Object.keys(object._customValues).length > 0) {
          const packedCustomValues: { [key: string]: any } = {} as any;
          for (const [key, value] of Object.entries(object._customValues)) {
            packedCustomValues[String(key)] = value.pack(1);
          }
          objectJson["customValues"] = packedCustomValues;
        }
        if (object._scriptPtr != null) {
          objectJson["script"] = object._scriptPtr.pack(1);
        }
        if (object.isExtensible != null) {
          objectJson["isExtensible"] = object.isExtensible;
        }
        if (object.sourcePtr != null) {
          objectJson["source"] = object.sourcePtr.pack(1);
        }
        if (object._key != null) {
          objectJson["key"] = object._key;
        }
        if (object._icon != null) {
          objectJson["icon"] = object._icon.pack(1);
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): HistogramMetric {
        const _Value = STRUCT_CLASS_BY_TYPE[100] as typeof Value;
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const _Icon = STRUCT_CLASS_BY_TYPE[400031] as typeof Icon;
        const iconValue = objectJson["icon"];
        const unpackedIcon = iconValue != undefined ? _Icon.unpack(1, iconValue, _session) as Icon : null;
        const parentPtrValue = objectJson["parent"];
        const unpackedParentPtr = parentPtrValue != undefined ? _NodeReference.unpack(1, parentPtrValue, _session) as NodeReference : null;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const instancePtrValue = objectJson["instance"];
        const unpackedInstancePtr = instancePtrValue != undefined ? _NodeReference.unpack(1, instancePtrValue, _session) as NodeReference : null;
        const deletedAtValue = objectJson["deletedAt"];
        const unpackedDeletedAt = deletedAtValue != undefined ? Temporal.Instant.from(deletedAtValue).toZonedDateTimeISO('UTC') : null;
        const ownedByPtrValue = objectJson["ownedBy"];
        const unpackedOwnedByPtr = ownedByPtrValue != undefined ? _NodeReference.unpack(1, ownedByPtrValue, _session) as NodeReference : null;
        const unpackedCustomValues = {} as any;
        if (objectJson["customValues"] != undefined) {
          for (const [key, value] of Object.entries(objectJson["customValues"])) {
            unpackedCustomValues[key] = _Value.unpack(1, value as any, _session) as Value;
          }
        }
        const scriptPtrValue = objectJson["script"];
        const unpackedScriptPtr = scriptPtrValue != undefined ? _NodeReference.unpack(1, scriptPtrValue, _session) as NodeReference : null;
        const isExtensibleValue = objectJson["isExtensible"];
        const unpackedIsExtensible = isExtensibleValue != undefined ? isExtensibleValue : null;
        const sourcePtrValue = objectJson["source"];
        const unpackedSourcePtr = sourcePtrValue != undefined ? _NodeReference.unpack(1, sourcePtrValue, _session) as NodeReference : null;
        const keyValue = objectJson["key"];
        const unpackedKey = keyValue != undefined ? keyValue : null;
        return new (NODE_CLASS_BY_TYPE[1200200] as typeof HistogramMetric)({
          icon: unpackedIcon,
          parent: unpackedParentPtr,
          materialization: Materialization[objectJson["materialization"]] as any,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          instance: unpackedInstancePtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          updatedAt: Temporal.Instant.from(objectJson["updatedAt"]).toZonedDateTimeISO('UTC'),
          updatedEpoch: Number(objectJson["updatedEpoch"]),
          updatedBy: _NodeReference.unpack(1, objectJson["updatedBy"], _session) as NodeReference,
          deletedAt: unpackedDeletedAt,
          ownedBy: unpackedOwnedByPtr,
          name: objectJson["name"],
          orderKey: objectJson["orderKey"],
          customValues: unpackedCustomValues,
          script: unpackedScriptPtr,
          isExtensible: unpackedIsExtensible,
          source: unpackedSourcePtr,
          key: unpackedKey,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 1200200)] = new HistogramMetricJsonEncoder();

    class HistogramMeasurementEventJsonEncoder implements _JsonObjectEncoder {
      packObject(object: HistogramMeasurementEvent): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "HISTOGRAM_MEASUREMENT_EVENT";
        objectJson["id"] = object.id;
        objectJson["space"] = object.spacePtr.pack(1);
        objectJson["definition"] = object.definitionPtr.pack(1);
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.causedByPtr != null) {
          objectJson["causedBy"] = object.causedByPtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["client"] = object.clientPtr.pack(1);
        objectJson["clientNonce"] = object.clientNonce;
        objectJson["clientCreatedAt"] = object.clientCreatedAt.toString({ timeZoneName: 'never' });
        objectJson["clientEpoch"] = Number(object.clientEpoch);
        objectJson["status"] = EventStatus[object.status];
        if (object.nodePtr != null) {
          objectJson["node"] = object.nodePtr.pack(1);
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): HistogramMeasurementEvent {
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const causedByPtrValue = objectJson["causedBy"];
        const unpackedCausedByPtr = causedByPtrValue != undefined ? _NodeReference.unpack(1, causedByPtrValue, _session) as NodeReference : null;
        const nodePtrValue = objectJson["node"];
        const unpackedNodePtr = nodePtrValue != undefined ? _NodeReference.unpack(1, nodePtrValue, _session) as NodeReference : null;
        return new (NODE_CLASS_BY_TYPE[1200201] as typeof HistogramMeasurementEvent)({
          definition: _NodeReference.unpack(1, objectJson["definition"], _session) as NodeReference,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          causedBy: unpackedCausedByPtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          client: _NodeReference.unpack(1, objectJson["client"], _session) as NodeReference,
          clientNonce: objectJson["clientNonce"],
          clientCreatedAt: Temporal.Instant.from(objectJson["clientCreatedAt"]).toZonedDateTimeISO('UTC'),
          clientEpoch: Number(objectJson["clientEpoch"]),
          status: EventStatus[objectJson["status"]] as any,
          node: unpackedNodePtr,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 1200201)] = new HistogramMeasurementEventJsonEncoder();

    class LayerJsonEncoder implements _JsonObjectEncoder {
      packObject(object: Layer): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "LAYER";
        objectJson["id"] = object.id;
        if (object.parentPtr != null) {
          objectJson["parent"] = object.parentPtr.pack(1);
        }
        objectJson["space"] = object.spacePtr.pack(1);
        objectJson["materialization"] = Materialization[object.materialization];
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.instancePtr != null) {
          objectJson["instance"] = object.instancePtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["updatedAt"] = object.updatedAt.toString({ timeZoneName: 'never' });
        objectJson["updatedEpoch"] = Number(object.updatedEpoch);
        objectJson["updatedBy"] = object.updatedByPtr.pack(1);
        if (object.deletedAt != null) {
          objectJson["deletedAt"] = object.deletedAt.toString({ timeZoneName: 'never' });
        }
        if (object._ownedByPtr != null) {
          objectJson["ownedBy"] = object._ownedByPtr.pack(1);
        }
        objectJson["name"] = object._name;
        objectJson["orderKey"] = object.orderKey;
        if (Object.keys(object._customValues).length > 0) {
          const packedCustomValues: { [key: string]: any } = {} as any;
          for (const [key, value] of Object.entries(object._customValues)) {
            packedCustomValues[String(key)] = value.pack(1);
          }
          objectJson["customValues"] = packedCustomValues;
        }
        if (object._scriptPtr != null) {
          objectJson["script"] = object._scriptPtr.pack(1);
        }
        if (object.isExtensible != null) {
          objectJson["isExtensible"] = object.isExtensible;
        }
        if (object.sourcePtr != null) {
          objectJson["source"] = object.sourcePtr.pack(1);
        }
        if (object._key != null) {
          objectJson["key"] = object._key;
        }
        objectJson["type"] = LayerType[object._type];
        if (object._icon != null) {
          objectJson["icon"] = object._icon.pack(1);
        }
        if (object._isVisible != null) {
          objectJson["isVisible"] = object._isVisible;
        }
        if (object._opacity != null) {
          objectJson["opacity"] = Number(object._opacity);
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): Layer {
        const _Value = STRUCT_CLASS_BY_TYPE[100] as typeof Value;
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const _Icon = STRUCT_CLASS_BY_TYPE[400031] as typeof Icon;
        const iconValue = objectJson["icon"];
        const unpackedIcon = iconValue != undefined ? _Icon.unpack(1, iconValue, _session) as Icon : null;
        const isVisibleValue = objectJson["isVisible"];
        const unpackedIsVisible = isVisibleValue != undefined ? isVisibleValue : null;
        const opacityValue = objectJson["opacity"];
        const unpackedOpacity = opacityValue != undefined ? Number(opacityValue) : null;
        const parentPtrValue = objectJson["parent"];
        const unpackedParentPtr = parentPtrValue != undefined ? _NodeReference.unpack(1, parentPtrValue, _session) as NodeReference : null;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const instancePtrValue = objectJson["instance"];
        const unpackedInstancePtr = instancePtrValue != undefined ? _NodeReference.unpack(1, instancePtrValue, _session) as NodeReference : null;
        const deletedAtValue = objectJson["deletedAt"];
        const unpackedDeletedAt = deletedAtValue != undefined ? Temporal.Instant.from(deletedAtValue).toZonedDateTimeISO('UTC') : null;
        const ownedByPtrValue = objectJson["ownedBy"];
        const unpackedOwnedByPtr = ownedByPtrValue != undefined ? _NodeReference.unpack(1, ownedByPtrValue, _session) as NodeReference : null;
        const unpackedCustomValues = {} as any;
        if (objectJson["customValues"] != undefined) {
          for (const [key, value] of Object.entries(objectJson["customValues"])) {
            unpackedCustomValues[key] = _Value.unpack(1, value as any, _session) as Value;
          }
        }
        const scriptPtrValue = objectJson["script"];
        const unpackedScriptPtr = scriptPtrValue != undefined ? _NodeReference.unpack(1, scriptPtrValue, _session) as NodeReference : null;
        const isExtensibleValue = objectJson["isExtensible"];
        const unpackedIsExtensible = isExtensibleValue != undefined ? isExtensibleValue : null;
        const sourcePtrValue = objectJson["source"];
        const unpackedSourcePtr = sourcePtrValue != undefined ? _NodeReference.unpack(1, sourcePtrValue, _session) as NodeReference : null;
        const keyValue = objectJson["key"];
        const unpackedKey = keyValue != undefined ? keyValue : null;
        return new (NODE_CLASS_BY_TYPE[1700300] as typeof Layer)({
          type: LayerType[objectJson["type"]] as any,
          icon: unpackedIcon,
          isVisible: unpackedIsVisible,
          opacity: unpackedOpacity,
          parent: unpackedParentPtr,
          materialization: Materialization[objectJson["materialization"]] as any,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          instance: unpackedInstancePtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          updatedAt: Temporal.Instant.from(objectJson["updatedAt"]).toZonedDateTimeISO('UTC'),
          updatedEpoch: Number(objectJson["updatedEpoch"]),
          updatedBy: _NodeReference.unpack(1, objectJson["updatedBy"], _session) as NodeReference,
          deletedAt: unpackedDeletedAt,
          ownedBy: unpackedOwnedByPtr,
          name: objectJson["name"],
          orderKey: objectJson["orderKey"],
          customValues: unpackedCustomValues,
          script: unpackedScriptPtr,
          isExtensible: unpackedIsExtensible,
          source: unpackedSourcePtr,
          key: unpackedKey,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 1700300)] = new LayerJsonEncoder();

    class FrameViewJsonEncoder implements _JsonObjectEncoder {
      packObject(object: FrameView): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "FRAME_VIEW";
        objectJson["id"] = object.id;
        if (object.parentPtr != null) {
          objectJson["parent"] = object.parentPtr.pack(1);
        }
        objectJson["space"] = object.spacePtr.pack(1);
        objectJson["materialization"] = Materialization[object.materialization];
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.instancePtr != null) {
          objectJson["instance"] = object.instancePtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["updatedAt"] = object.updatedAt.toString({ timeZoneName: 'never' });
        objectJson["updatedEpoch"] = Number(object.updatedEpoch);
        objectJson["updatedBy"] = object.updatedByPtr.pack(1);
        if (object.deletedAt != null) {
          objectJson["deletedAt"] = object.deletedAt.toString({ timeZoneName: 'never' });
        }
        if (object._ownedByPtr != null) {
          objectJson["ownedBy"] = object._ownedByPtr.pack(1);
        }
        objectJson["name"] = object._name;
        objectJson["orderKey"] = object.orderKey;
        if (Object.keys(object._customValues).length > 0) {
          const packedCustomValues: { [key: string]: any } = {} as any;
          for (const [key, value] of Object.entries(object._customValues)) {
            packedCustomValues[String(key)] = value.pack(1);
          }
          objectJson["customValues"] = packedCustomValues;
        }
        if (object._scriptPtr != null) {
          objectJson["script"] = object._scriptPtr.pack(1);
        }
        if (object.isExtensible != null) {
          objectJson["isExtensible"] = object.isExtensible;
        }
        if (object.sourcePtr != null) {
          objectJson["source"] = object.sourcePtr.pack(1);
        }
        if (object._key != null) {
          objectJson["key"] = object._key;
        }
        if (object._position != null) {
          objectJson["position"] = object._position.pack(1);
        }
        if (object._offset != null) {
          objectJson["offset"] = object._offset.pack(1);
        }
        if (object._scale != null) {
          objectJson["scale"] = object._scale.pack(1);
        }
        if (object._rotation != null) {
          objectJson["rotation"] = object._rotation.pack(1);
        }
        if (object._skew != null) {
          objectJson["skew"] = object._skew.pack(1);
        }
        if (object._origin != null) {
          objectJson["origin"] = object._origin.pack(1);
        }
        if (object._anchor != null) {
          objectJson["anchor"] = Anchor[object._anchor];
        }
        if (object._width != null) {
          objectJson["width"] = object._width.pack(1);
        }
        if (object._height != null) {
          objectJson["height"] = object._height.pack(1);
        }
        if (object._minWidth != null) {
          objectJson["minWidth"] = object._minWidth.pack(1);
        }
        if (object._minHeight != null) {
          objectJson["minHeight"] = object._minHeight.pack(1);
        }
        if (object._maxWidth != null) {
          objectJson["maxWidth"] = object._maxWidth.pack(1);
        }
        if (object._maxHeight != null) {
          objectJson["maxHeight"] = object._maxHeight.pack(1);
        }
        if (object._isVisible != null) {
          objectJson["isVisible"] = object._isVisible;
        }
        if (object._opacity != null) {
          objectJson["opacity"] = Number(object._opacity);
        }
        if (object._fill != null) {
          objectJson["fill"] = object._fill.pack(1);
        }
        if (object._shadow != null) {
          objectJson["shadow"] = object._shadow.pack(1);
        }
        if (object._border != null) {
          objectJson["border"] = object._border.pack(1);
        }
        if (object._radius != null) {
          objectJson["radius"] = object._radius.pack(1);
        }
        if (object._layout != null) {
          objectJson["layout"] = Layout[object._layout];
        }
        if (object._direction != null) {
          objectJson["direction"] = Direction[object._direction];
        }
        if (object._distribute != null) {
          objectJson["distribute"] = Distribute[object._distribute];
        }
        if (object._align != null) {
          objectJson["align"] = Align[object._align];
        }
        if (object._gap != null) {
          objectJson["gap"] = object._gap.pack(1);
        }
        if (object._padding != null) {
          objectJson["padding"] = object._padding.pack(1);
        }
        if (object._grid != null) {
          objectJson["grid"] = object._grid.pack(1);
        }
        if (object._gridSpan != null) {
          objectJson["gridSpan"] = object._gridSpan.pack(1);
        }
        if (object._aspectRatio != null) {
          objectJson["aspectRatio"] = Number(object._aspectRatio);
        }
        if (object._isWrap != null) {
          objectJson["isWrap"] = object._isWrap;
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): FrameView {
        const _Value = STRUCT_CLASS_BY_TYPE[100] as typeof Value;
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const _Length = STRUCT_CLASS_BY_TYPE[1800001] as typeof Length;
        const _Fill = STRUCT_CLASS_BY_TYPE[2100400] as typeof Fill;
        const _Border = STRUCT_CLASS_BY_TYPE[2100600] as typeof Border;
        const _Shadow = STRUCT_CLASS_BY_TYPE[2100700] as typeof Shadow;
        const _Vector2 = STRUCT_CLASS_BY_TYPE[2400000] as typeof Vector2;
        const _Offset2 = STRUCT_CLASS_BY_TYPE[2400020] as typeof Offset2;
        const _Grid2 = STRUCT_CLASS_BY_TYPE[2400021] as typeof Grid2;
        const _GridSpan2 = STRUCT_CLASS_BY_TYPE[2400022] as typeof GridSpan2;
        const _Inset2 = STRUCT_CLASS_BY_TYPE[2400023] as typeof Inset2;
        const _Corner2 = STRUCT_CLASS_BY_TYPE[2400024] as typeof Corner2;
        const _Axis2 = STRUCT_CLASS_BY_TYPE[2400025] as typeof Axis2;
        const layoutValue = objectJson["layout"];
        const unpackedLayout = layoutValue != undefined ? Layout[layoutValue] as any : null;
        const directionValue = objectJson["direction"];
        const unpackedDirection = directionValue != undefined ? Direction[directionValue] as any : null;
        const distributeValue = objectJson["distribute"];
        const unpackedDistribute = distributeValue != undefined ? Distribute[distributeValue] as any : null;
        const alignValue = objectJson["align"];
        const unpackedAlign = alignValue != undefined ? Align[alignValue] as any : null;
        const gapValue = objectJson["gap"];
        const unpackedGap = gapValue != undefined ? _Axis2.unpack(1, gapValue, _session) as Axis2 : null;
        const paddingValue = objectJson["padding"];
        const unpackedPadding = paddingValue != undefined ? _Inset2.unpack(1, paddingValue, _session) as Inset2 : null;
        const gridValue = objectJson["grid"];
        const unpackedGrid = gridValue != undefined ? _Grid2.unpack(1, gridValue, _session) as Grid2 : null;
        const gridSpanValue = objectJson["gridSpan"];
        const unpackedGridSpan = gridSpanValue != undefined ? _GridSpan2.unpack(1, gridSpanValue, _session) as GridSpan2 : null;
        const aspectRatioValue = objectJson["aspectRatio"];
        const unpackedAspectRatio = aspectRatioValue != undefined ? Number(aspectRatioValue) : null;
        const isWrapValue = objectJson["isWrap"];
        const unpackedIsWrap = isWrapValue != undefined ? isWrapValue : null;
        const widthValue = objectJson["width"];
        const unpackedWidth = widthValue != undefined ? _Length.unpack(1, widthValue, _session) as Length : null;
        const heightValue = objectJson["height"];
        const unpackedHeight = heightValue != undefined ? _Length.unpack(1, heightValue, _session) as Length : null;
        const minWidthValue = objectJson["minWidth"];
        const unpackedMinWidth = minWidthValue != undefined ? _Length.unpack(1, minWidthValue, _session) as Length : null;
        const minHeightValue = objectJson["minHeight"];
        const unpackedMinHeight = minHeightValue != undefined ? _Length.unpack(1, minHeightValue, _session) as Length : null;
        const maxWidthValue = objectJson["maxWidth"];
        const unpackedMaxWidth = maxWidthValue != undefined ? _Length.unpack(1, maxWidthValue, _session) as Length : null;
        const maxHeightValue = objectJson["maxHeight"];
        const unpackedMaxHeight = maxHeightValue != undefined ? _Length.unpack(1, maxHeightValue, _session) as Length : null;
        const isVisibleValue = objectJson["isVisible"];
        const unpackedIsVisible = isVisibleValue != undefined ? isVisibleValue : null;
        const opacityValue = objectJson["opacity"];
        const unpackedOpacity = opacityValue != undefined ? Number(opacityValue) : null;
        const fillValue = objectJson["fill"];
        const unpackedFill = fillValue != undefined ? _Fill.unpack(1, fillValue, _session) as Fill : null;
        const shadowValue = objectJson["shadow"];
        const unpackedShadow = shadowValue != undefined ? _Shadow.unpack(1, shadowValue, _session) as Shadow : null;
        const borderValue = objectJson["border"];
        const unpackedBorder = borderValue != undefined ? _Border.unpack(1, borderValue, _session) as Border : null;
        const radiusValue = objectJson["radius"];
        const unpackedRadius = radiusValue != undefined ? _Corner2.unpack(1, radiusValue, _session) as Corner2 : null;
        const positionValue = objectJson["position"];
        const unpackedPosition = positionValue != undefined ? _Vector2.unpack(1, positionValue, _session) as Vector2 : null;
        const offsetValue = objectJson["offset"];
        const unpackedOffset = offsetValue != undefined ? _Offset2.unpack(1, offsetValue, _session) as Offset2 : null;
        const scaleValue = objectJson["scale"];
        const unpackedScale = scaleValue != undefined ? _Vector2.unpack(1, scaleValue, _session) as Vector2 : null;
        const rotationValue = objectJson["rotation"];
        const unpackedRotation = rotationValue != undefined ? _Vector2.unpack(1, rotationValue, _session) as Vector2 : null;
        const skewValue = objectJson["skew"];
        const unpackedSkew = skewValue != undefined ? _Vector2.unpack(1, skewValue, _session) as Vector2 : null;
        const originValue = objectJson["origin"];
        const unpackedOrigin = originValue != undefined ? _Vector2.unpack(1, originValue, _session) as Vector2 : null;
        const anchorValue = objectJson["anchor"];
        const unpackedAnchor = anchorValue != undefined ? Anchor[anchorValue] as any : null;
        const parentPtrValue = objectJson["parent"];
        const unpackedParentPtr = parentPtrValue != undefined ? _NodeReference.unpack(1, parentPtrValue, _session) as NodeReference : null;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const instancePtrValue = objectJson["instance"];
        const unpackedInstancePtr = instancePtrValue != undefined ? _NodeReference.unpack(1, instancePtrValue, _session) as NodeReference : null;
        const deletedAtValue = objectJson["deletedAt"];
        const unpackedDeletedAt = deletedAtValue != undefined ? Temporal.Instant.from(deletedAtValue).toZonedDateTimeISO('UTC') : null;
        const ownedByPtrValue = objectJson["ownedBy"];
        const unpackedOwnedByPtr = ownedByPtrValue != undefined ? _NodeReference.unpack(1, ownedByPtrValue, _session) as NodeReference : null;
        const unpackedCustomValues = {} as any;
        if (objectJson["customValues"] != undefined) {
          for (const [key, value] of Object.entries(objectJson["customValues"])) {
            unpackedCustomValues[key] = _Value.unpack(1, value as any, _session) as Value;
          }
        }
        const scriptPtrValue = objectJson["script"];
        const unpackedScriptPtr = scriptPtrValue != undefined ? _NodeReference.unpack(1, scriptPtrValue, _session) as NodeReference : null;
        const isExtensibleValue = objectJson["isExtensible"];
        const unpackedIsExtensible = isExtensibleValue != undefined ? isExtensibleValue : null;
        const sourcePtrValue = objectJson["source"];
        const unpackedSourcePtr = sourcePtrValue != undefined ? _NodeReference.unpack(1, sourcePtrValue, _session) as NodeReference : null;
        const keyValue = objectJson["key"];
        const unpackedKey = keyValue != undefined ? keyValue : null;
        return new (NODE_CLASS_BY_TYPE[1800200] as typeof FrameView)({
          layout: unpackedLayout,
          direction: unpackedDirection,
          distribute: unpackedDistribute,
          align: unpackedAlign,
          gap: unpackedGap,
          padding: unpackedPadding,
          grid: unpackedGrid,
          gridSpan: unpackedGridSpan,
          aspectRatio: unpackedAspectRatio,
          isWrap: unpackedIsWrap,
          width: unpackedWidth,
          height: unpackedHeight,
          minWidth: unpackedMinWidth,
          minHeight: unpackedMinHeight,
          maxWidth: unpackedMaxWidth,
          maxHeight: unpackedMaxHeight,
          isVisible: unpackedIsVisible,
          opacity: unpackedOpacity,
          fill: unpackedFill,
          shadow: unpackedShadow,
          border: unpackedBorder,
          radius: unpackedRadius,
          position: unpackedPosition,
          offset: unpackedOffset,
          scale: unpackedScale,
          rotation: unpackedRotation,
          skew: unpackedSkew,
          origin: unpackedOrigin,
          anchor: unpackedAnchor,
          parent: unpackedParentPtr,
          materialization: Materialization[objectJson["materialization"]] as any,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          instance: unpackedInstancePtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          updatedAt: Temporal.Instant.from(objectJson["updatedAt"]).toZonedDateTimeISO('UTC'),
          updatedEpoch: Number(objectJson["updatedEpoch"]),
          updatedBy: _NodeReference.unpack(1, objectJson["updatedBy"], _session) as NodeReference,
          deletedAt: unpackedDeletedAt,
          ownedBy: unpackedOwnedByPtr,
          name: objectJson["name"],
          orderKey: objectJson["orderKey"],
          customValues: unpackedCustomValues,
          script: unpackedScriptPtr,
          isExtensible: unpackedIsExtensible,
          source: unpackedSourcePtr,
          key: unpackedKey,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 1800200)] = new FrameViewJsonEncoder();

    class LabelViewJsonEncoder implements _JsonObjectEncoder {
      packObject(object: LabelView): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "LABEL_VIEW";
        objectJson["id"] = object.id;
        if (object.parentPtr != null) {
          objectJson["parent"] = object.parentPtr.pack(1);
        }
        objectJson["space"] = object.spacePtr.pack(1);
        objectJson["materialization"] = Materialization[object.materialization];
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.instancePtr != null) {
          objectJson["instance"] = object.instancePtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["updatedAt"] = object.updatedAt.toString({ timeZoneName: 'never' });
        objectJson["updatedEpoch"] = Number(object.updatedEpoch);
        objectJson["updatedBy"] = object.updatedByPtr.pack(1);
        if (object.deletedAt != null) {
          objectJson["deletedAt"] = object.deletedAt.toString({ timeZoneName: 'never' });
        }
        if (object._ownedByPtr != null) {
          objectJson["ownedBy"] = object._ownedByPtr.pack(1);
        }
        objectJson["name"] = object._name;
        objectJson["orderKey"] = object.orderKey;
        if (Object.keys(object._customValues).length > 0) {
          const packedCustomValues: { [key: string]: any } = {} as any;
          for (const [key, value] of Object.entries(object._customValues)) {
            packedCustomValues[String(key)] = value.pack(1);
          }
          objectJson["customValues"] = packedCustomValues;
        }
        if (object._scriptPtr != null) {
          objectJson["script"] = object._scriptPtr.pack(1);
        }
        if (object.isExtensible != null) {
          objectJson["isExtensible"] = object.isExtensible;
        }
        if (object.sourcePtr != null) {
          objectJson["source"] = object.sourcePtr.pack(1);
        }
        if (object._key != null) {
          objectJson["key"] = object._key;
        }
        if (object._position != null) {
          objectJson["position"] = object._position.pack(1);
        }
        if (object._offset != null) {
          objectJson["offset"] = object._offset.pack(1);
        }
        if (object._scale != null) {
          objectJson["scale"] = object._scale.pack(1);
        }
        if (object._rotation != null) {
          objectJson["rotation"] = object._rotation.pack(1);
        }
        if (object._skew != null) {
          objectJson["skew"] = object._skew.pack(1);
        }
        if (object._origin != null) {
          objectJson["origin"] = object._origin.pack(1);
        }
        if (object._anchor != null) {
          objectJson["anchor"] = Anchor[object._anchor];
        }
        if (object._width != null) {
          objectJson["width"] = object._width.pack(1);
        }
        if (object._height != null) {
          objectJson["height"] = object._height.pack(1);
        }
        if (object._minWidth != null) {
          objectJson["minWidth"] = object._minWidth.pack(1);
        }
        if (object._minHeight != null) {
          objectJson["minHeight"] = object._minHeight.pack(1);
        }
        if (object._maxWidth != null) {
          objectJson["maxWidth"] = object._maxWidth.pack(1);
        }
        if (object._maxHeight != null) {
          objectJson["maxHeight"] = object._maxHeight.pack(1);
        }
        if (object._isVisible != null) {
          objectJson["isVisible"] = object._isVisible;
        }
        if (object._opacity != null) {
          objectJson["opacity"] = Number(object._opacity);
        }
        if (object._fill != null) {
          objectJson["fill"] = object._fill.pack(1);
        }
        if (object._shadow != null) {
          objectJson["shadow"] = object._shadow.pack(1);
        }
        if (object._border != null) {
          objectJson["border"] = object._border.pack(1);
        }
        if (object._radius != null) {
          objectJson["radius"] = object._radius.pack(1);
        }
        if (object._layout != null) {
          objectJson["layout"] = Layout[object._layout];
        }
        if (object._direction != null) {
          objectJson["direction"] = Direction[object._direction];
        }
        if (object._distribute != null) {
          objectJson["distribute"] = Distribute[object._distribute];
        }
        if (object._align != null) {
          objectJson["align"] = Align[object._align];
        }
        if (object._gap != null) {
          objectJson["gap"] = object._gap.pack(1);
        }
        if (object._padding != null) {
          objectJson["padding"] = object._padding.pack(1);
        }
        if (object._grid != null) {
          objectJson["grid"] = object._grid.pack(1);
        }
        if (object._gridSpan != null) {
          objectJson["gridSpan"] = object._gridSpan.pack(1);
        }
        if (object._aspectRatio != null) {
          objectJson["aspectRatio"] = Number(object._aspectRatio);
        }
        if (object._isWrap != null) {
          objectJson["isWrap"] = object._isWrap;
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): LabelView {
        const _Value = STRUCT_CLASS_BY_TYPE[100] as typeof Value;
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const _Length = STRUCT_CLASS_BY_TYPE[1800001] as typeof Length;
        const _Fill = STRUCT_CLASS_BY_TYPE[2100400] as typeof Fill;
        const _Border = STRUCT_CLASS_BY_TYPE[2100600] as typeof Border;
        const _Shadow = STRUCT_CLASS_BY_TYPE[2100700] as typeof Shadow;
        const _Vector2 = STRUCT_CLASS_BY_TYPE[2400000] as typeof Vector2;
        const _Offset2 = STRUCT_CLASS_BY_TYPE[2400020] as typeof Offset2;
        const _Grid2 = STRUCT_CLASS_BY_TYPE[2400021] as typeof Grid2;
        const _GridSpan2 = STRUCT_CLASS_BY_TYPE[2400022] as typeof GridSpan2;
        const _Inset2 = STRUCT_CLASS_BY_TYPE[2400023] as typeof Inset2;
        const _Corner2 = STRUCT_CLASS_BY_TYPE[2400024] as typeof Corner2;
        const _Axis2 = STRUCT_CLASS_BY_TYPE[2400025] as typeof Axis2;
        const layoutValue = objectJson["layout"];
        const unpackedLayout = layoutValue != undefined ? Layout[layoutValue] as any : null;
        const directionValue = objectJson["direction"];
        const unpackedDirection = directionValue != undefined ? Direction[directionValue] as any : null;
        const distributeValue = objectJson["distribute"];
        const unpackedDistribute = distributeValue != undefined ? Distribute[distributeValue] as any : null;
        const alignValue = objectJson["align"];
        const unpackedAlign = alignValue != undefined ? Align[alignValue] as any : null;
        const gapValue = objectJson["gap"];
        const unpackedGap = gapValue != undefined ? _Axis2.unpack(1, gapValue, _session) as Axis2 : null;
        const paddingValue = objectJson["padding"];
        const unpackedPadding = paddingValue != undefined ? _Inset2.unpack(1, paddingValue, _session) as Inset2 : null;
        const gridValue = objectJson["grid"];
        const unpackedGrid = gridValue != undefined ? _Grid2.unpack(1, gridValue, _session) as Grid2 : null;
        const gridSpanValue = objectJson["gridSpan"];
        const unpackedGridSpan = gridSpanValue != undefined ? _GridSpan2.unpack(1, gridSpanValue, _session) as GridSpan2 : null;
        const aspectRatioValue = objectJson["aspectRatio"];
        const unpackedAspectRatio = aspectRatioValue != undefined ? Number(aspectRatioValue) : null;
        const isWrapValue = objectJson["isWrap"];
        const unpackedIsWrap = isWrapValue != undefined ? isWrapValue : null;
        const widthValue = objectJson["width"];
        const unpackedWidth = widthValue != undefined ? _Length.unpack(1, widthValue, _session) as Length : null;
        const heightValue = objectJson["height"];
        const unpackedHeight = heightValue != undefined ? _Length.unpack(1, heightValue, _session) as Length : null;
        const minWidthValue = objectJson["minWidth"];
        const unpackedMinWidth = minWidthValue != undefined ? _Length.unpack(1, minWidthValue, _session) as Length : null;
        const minHeightValue = objectJson["minHeight"];
        const unpackedMinHeight = minHeightValue != undefined ? _Length.unpack(1, minHeightValue, _session) as Length : null;
        const maxWidthValue = objectJson["maxWidth"];
        const unpackedMaxWidth = maxWidthValue != undefined ? _Length.unpack(1, maxWidthValue, _session) as Length : null;
        const maxHeightValue = objectJson["maxHeight"];
        const unpackedMaxHeight = maxHeightValue != undefined ? _Length.unpack(1, maxHeightValue, _session) as Length : null;
        const isVisibleValue = objectJson["isVisible"];
        const unpackedIsVisible = isVisibleValue != undefined ? isVisibleValue : null;
        const opacityValue = objectJson["opacity"];
        const unpackedOpacity = opacityValue != undefined ? Number(opacityValue) : null;
        const fillValue = objectJson["fill"];
        const unpackedFill = fillValue != undefined ? _Fill.unpack(1, fillValue, _session) as Fill : null;
        const shadowValue = objectJson["shadow"];
        const unpackedShadow = shadowValue != undefined ? _Shadow.unpack(1, shadowValue, _session) as Shadow : null;
        const borderValue = objectJson["border"];
        const unpackedBorder = borderValue != undefined ? _Border.unpack(1, borderValue, _session) as Border : null;
        const radiusValue = objectJson["radius"];
        const unpackedRadius = radiusValue != undefined ? _Corner2.unpack(1, radiusValue, _session) as Corner2 : null;
        const positionValue = objectJson["position"];
        const unpackedPosition = positionValue != undefined ? _Vector2.unpack(1, positionValue, _session) as Vector2 : null;
        const offsetValue = objectJson["offset"];
        const unpackedOffset = offsetValue != undefined ? _Offset2.unpack(1, offsetValue, _session) as Offset2 : null;
        const scaleValue = objectJson["scale"];
        const unpackedScale = scaleValue != undefined ? _Vector2.unpack(1, scaleValue, _session) as Vector2 : null;
        const rotationValue = objectJson["rotation"];
        const unpackedRotation = rotationValue != undefined ? _Vector2.unpack(1, rotationValue, _session) as Vector2 : null;
        const skewValue = objectJson["skew"];
        const unpackedSkew = skewValue != undefined ? _Vector2.unpack(1, skewValue, _session) as Vector2 : null;
        const originValue = objectJson["origin"];
        const unpackedOrigin = originValue != undefined ? _Vector2.unpack(1, originValue, _session) as Vector2 : null;
        const anchorValue = objectJson["anchor"];
        const unpackedAnchor = anchorValue != undefined ? Anchor[anchorValue] as any : null;
        const parentPtrValue = objectJson["parent"];
        const unpackedParentPtr = parentPtrValue != undefined ? _NodeReference.unpack(1, parentPtrValue, _session) as NodeReference : null;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const instancePtrValue = objectJson["instance"];
        const unpackedInstancePtr = instancePtrValue != undefined ? _NodeReference.unpack(1, instancePtrValue, _session) as NodeReference : null;
        const deletedAtValue = objectJson["deletedAt"];
        const unpackedDeletedAt = deletedAtValue != undefined ? Temporal.Instant.from(deletedAtValue).toZonedDateTimeISO('UTC') : null;
        const ownedByPtrValue = objectJson["ownedBy"];
        const unpackedOwnedByPtr = ownedByPtrValue != undefined ? _NodeReference.unpack(1, ownedByPtrValue, _session) as NodeReference : null;
        const unpackedCustomValues = {} as any;
        if (objectJson["customValues"] != undefined) {
          for (const [key, value] of Object.entries(objectJson["customValues"])) {
            unpackedCustomValues[key] = _Value.unpack(1, value as any, _session) as Value;
          }
        }
        const scriptPtrValue = objectJson["script"];
        const unpackedScriptPtr = scriptPtrValue != undefined ? _NodeReference.unpack(1, scriptPtrValue, _session) as NodeReference : null;
        const isExtensibleValue = objectJson["isExtensible"];
        const unpackedIsExtensible = isExtensibleValue != undefined ? isExtensibleValue : null;
        const sourcePtrValue = objectJson["source"];
        const unpackedSourcePtr = sourcePtrValue != undefined ? _NodeReference.unpack(1, sourcePtrValue, _session) as NodeReference : null;
        const keyValue = objectJson["key"];
        const unpackedKey = keyValue != undefined ? keyValue : null;
        return new (NODE_CLASS_BY_TYPE[1800300] as typeof LabelView)({
          layout: unpackedLayout,
          direction: unpackedDirection,
          distribute: unpackedDistribute,
          align: unpackedAlign,
          gap: unpackedGap,
          padding: unpackedPadding,
          grid: unpackedGrid,
          gridSpan: unpackedGridSpan,
          aspectRatio: unpackedAspectRatio,
          isWrap: unpackedIsWrap,
          width: unpackedWidth,
          height: unpackedHeight,
          minWidth: unpackedMinWidth,
          minHeight: unpackedMinHeight,
          maxWidth: unpackedMaxWidth,
          maxHeight: unpackedMaxHeight,
          isVisible: unpackedIsVisible,
          opacity: unpackedOpacity,
          fill: unpackedFill,
          shadow: unpackedShadow,
          border: unpackedBorder,
          radius: unpackedRadius,
          position: unpackedPosition,
          offset: unpackedOffset,
          scale: unpackedScale,
          rotation: unpackedRotation,
          skew: unpackedSkew,
          origin: unpackedOrigin,
          anchor: unpackedAnchor,
          parent: unpackedParentPtr,
          materialization: Materialization[objectJson["materialization"]] as any,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          instance: unpackedInstancePtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          updatedAt: Temporal.Instant.from(objectJson["updatedAt"]).toZonedDateTimeISO('UTC'),
          updatedEpoch: Number(objectJson["updatedEpoch"]),
          updatedBy: _NodeReference.unpack(1, objectJson["updatedBy"], _session) as NodeReference,
          deletedAt: unpackedDeletedAt,
          ownedBy: unpackedOwnedByPtr,
          name: objectJson["name"],
          orderKey: objectJson["orderKey"],
          customValues: unpackedCustomValues,
          script: unpackedScriptPtr,
          isExtensible: unpackedIsExtensible,
          source: unpackedSourcePtr,
          key: unpackedKey,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 1800300)] = new LabelViewJsonEncoder();

    class NumberInputViewJsonEncoder implements _JsonObjectEncoder {
      packObject(object: NumberInputView): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "NUMBER_INPUT_VIEW";
        objectJson["id"] = object.id;
        if (object.parentPtr != null) {
          objectJson["parent"] = object.parentPtr.pack(1);
        }
        objectJson["space"] = object.spacePtr.pack(1);
        objectJson["materialization"] = Materialization[object.materialization];
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.instancePtr != null) {
          objectJson["instance"] = object.instancePtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["updatedAt"] = object.updatedAt.toString({ timeZoneName: 'never' });
        objectJson["updatedEpoch"] = Number(object.updatedEpoch);
        objectJson["updatedBy"] = object.updatedByPtr.pack(1);
        if (object.deletedAt != null) {
          objectJson["deletedAt"] = object.deletedAt.toString({ timeZoneName: 'never' });
        }
        if (object._ownedByPtr != null) {
          objectJson["ownedBy"] = object._ownedByPtr.pack(1);
        }
        objectJson["name"] = object._name;
        objectJson["orderKey"] = object.orderKey;
        if (Object.keys(object._customValues).length > 0) {
          const packedCustomValues: { [key: string]: any } = {} as any;
          for (const [key, value] of Object.entries(object._customValues)) {
            packedCustomValues[String(key)] = value.pack(1);
          }
          objectJson["customValues"] = packedCustomValues;
        }
        if (object._scriptPtr != null) {
          objectJson["script"] = object._scriptPtr.pack(1);
        }
        if (object.isExtensible != null) {
          objectJson["isExtensible"] = object.isExtensible;
        }
        if (object.sourcePtr != null) {
          objectJson["source"] = object.sourcePtr.pack(1);
        }
        if (object._key != null) {
          objectJson["key"] = object._key;
        }
        if (object._position != null) {
          objectJson["position"] = object._position.pack(1);
        }
        if (object._offset != null) {
          objectJson["offset"] = object._offset.pack(1);
        }
        if (object._scale != null) {
          objectJson["scale"] = object._scale.pack(1);
        }
        if (object._rotation != null) {
          objectJson["rotation"] = object._rotation.pack(1);
        }
        if (object._skew != null) {
          objectJson["skew"] = object._skew.pack(1);
        }
        if (object._origin != null) {
          objectJson["origin"] = object._origin.pack(1);
        }
        if (object._anchor != null) {
          objectJson["anchor"] = Anchor[object._anchor];
        }
        if (object._width != null) {
          objectJson["width"] = object._width.pack(1);
        }
        if (object._height != null) {
          objectJson["height"] = object._height.pack(1);
        }
        if (object._minWidth != null) {
          objectJson["minWidth"] = object._minWidth.pack(1);
        }
        if (object._minHeight != null) {
          objectJson["minHeight"] = object._minHeight.pack(1);
        }
        if (object._maxWidth != null) {
          objectJson["maxWidth"] = object._maxWidth.pack(1);
        }
        if (object._maxHeight != null) {
          objectJson["maxHeight"] = object._maxHeight.pack(1);
        }
        if (object._isVisible != null) {
          objectJson["isVisible"] = object._isVisible;
        }
        if (object._opacity != null) {
          objectJson["opacity"] = Number(object._opacity);
        }
        if (object._fill != null) {
          objectJson["fill"] = object._fill.pack(1);
        }
        if (object._shadow != null) {
          objectJson["shadow"] = object._shadow.pack(1);
        }
        if (object._border != null) {
          objectJson["border"] = object._border.pack(1);
        }
        if (object._radius != null) {
          objectJson["radius"] = object._radius.pack(1);
        }
        if (object._value != null) {
          objectJson["value"] = Number(object._value);
        }
        if (object._placeholder != null) {
          objectJson["placeholder"] = object._placeholder;
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): NumberInputView {
        const _Value = STRUCT_CLASS_BY_TYPE[100] as typeof Value;
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const _Length = STRUCT_CLASS_BY_TYPE[1800001] as typeof Length;
        const _Fill = STRUCT_CLASS_BY_TYPE[2100400] as typeof Fill;
        const _Border = STRUCT_CLASS_BY_TYPE[2100600] as typeof Border;
        const _Shadow = STRUCT_CLASS_BY_TYPE[2100700] as typeof Shadow;
        const _Vector2 = STRUCT_CLASS_BY_TYPE[2400000] as typeof Vector2;
        const _Offset2 = STRUCT_CLASS_BY_TYPE[2400020] as typeof Offset2;
        const _Corner2 = STRUCT_CLASS_BY_TYPE[2400024] as typeof Corner2;
        const valueValue = objectJson["value"];
        const unpackedValue = valueValue != undefined ? Number(valueValue) : null;
        const placeholderValue = objectJson["placeholder"];
        const unpackedPlaceholder = placeholderValue != undefined ? placeholderValue : null;
        const widthValue = objectJson["width"];
        const unpackedWidth = widthValue != undefined ? _Length.unpack(1, widthValue, _session) as Length : null;
        const heightValue = objectJson["height"];
        const unpackedHeight = heightValue != undefined ? _Length.unpack(1, heightValue, _session) as Length : null;
        const minWidthValue = objectJson["minWidth"];
        const unpackedMinWidth = minWidthValue != undefined ? _Length.unpack(1, minWidthValue, _session) as Length : null;
        const minHeightValue = objectJson["minHeight"];
        const unpackedMinHeight = minHeightValue != undefined ? _Length.unpack(1, minHeightValue, _session) as Length : null;
        const maxWidthValue = objectJson["maxWidth"];
        const unpackedMaxWidth = maxWidthValue != undefined ? _Length.unpack(1, maxWidthValue, _session) as Length : null;
        const maxHeightValue = objectJson["maxHeight"];
        const unpackedMaxHeight = maxHeightValue != undefined ? _Length.unpack(1, maxHeightValue, _session) as Length : null;
        const isVisibleValue = objectJson["isVisible"];
        const unpackedIsVisible = isVisibleValue != undefined ? isVisibleValue : null;
        const opacityValue = objectJson["opacity"];
        const unpackedOpacity = opacityValue != undefined ? Number(opacityValue) : null;
        const fillValue = objectJson["fill"];
        const unpackedFill = fillValue != undefined ? _Fill.unpack(1, fillValue, _session) as Fill : null;
        const shadowValue = objectJson["shadow"];
        const unpackedShadow = shadowValue != undefined ? _Shadow.unpack(1, shadowValue, _session) as Shadow : null;
        const borderValue = objectJson["border"];
        const unpackedBorder = borderValue != undefined ? _Border.unpack(1, borderValue, _session) as Border : null;
        const radiusValue = objectJson["radius"];
        const unpackedRadius = radiusValue != undefined ? _Corner2.unpack(1, radiusValue, _session) as Corner2 : null;
        const positionValue = objectJson["position"];
        const unpackedPosition = positionValue != undefined ? _Vector2.unpack(1, positionValue, _session) as Vector2 : null;
        const offsetValue = objectJson["offset"];
        const unpackedOffset = offsetValue != undefined ? _Offset2.unpack(1, offsetValue, _session) as Offset2 : null;
        const scaleValue = objectJson["scale"];
        const unpackedScale = scaleValue != undefined ? _Vector2.unpack(1, scaleValue, _session) as Vector2 : null;
        const rotationValue = objectJson["rotation"];
        const unpackedRotation = rotationValue != undefined ? _Vector2.unpack(1, rotationValue, _session) as Vector2 : null;
        const skewValue = objectJson["skew"];
        const unpackedSkew = skewValue != undefined ? _Vector2.unpack(1, skewValue, _session) as Vector2 : null;
        const originValue = objectJson["origin"];
        const unpackedOrigin = originValue != undefined ? _Vector2.unpack(1, originValue, _session) as Vector2 : null;
        const anchorValue = objectJson["anchor"];
        const unpackedAnchor = anchorValue != undefined ? Anchor[anchorValue] as any : null;
        const parentPtrValue = objectJson["parent"];
        const unpackedParentPtr = parentPtrValue != undefined ? _NodeReference.unpack(1, parentPtrValue, _session) as NodeReference : null;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const instancePtrValue = objectJson["instance"];
        const unpackedInstancePtr = instancePtrValue != undefined ? _NodeReference.unpack(1, instancePtrValue, _session) as NodeReference : null;
        const deletedAtValue = objectJson["deletedAt"];
        const unpackedDeletedAt = deletedAtValue != undefined ? Temporal.Instant.from(deletedAtValue).toZonedDateTimeISO('UTC') : null;
        const ownedByPtrValue = objectJson["ownedBy"];
        const unpackedOwnedByPtr = ownedByPtrValue != undefined ? _NodeReference.unpack(1, ownedByPtrValue, _session) as NodeReference : null;
        const unpackedCustomValues = {} as any;
        if (objectJson["customValues"] != undefined) {
          for (const [key, value] of Object.entries(objectJson["customValues"])) {
            unpackedCustomValues[key] = _Value.unpack(1, value as any, _session) as Value;
          }
        }
        const scriptPtrValue = objectJson["script"];
        const unpackedScriptPtr = scriptPtrValue != undefined ? _NodeReference.unpack(1, scriptPtrValue, _session) as NodeReference : null;
        const isExtensibleValue = objectJson["isExtensible"];
        const unpackedIsExtensible = isExtensibleValue != undefined ? isExtensibleValue : null;
        const sourcePtrValue = objectJson["source"];
        const unpackedSourcePtr = sourcePtrValue != undefined ? _NodeReference.unpack(1, sourcePtrValue, _session) as NodeReference : null;
        const keyValue = objectJson["key"];
        const unpackedKey = keyValue != undefined ? keyValue : null;
        return new (NODE_CLASS_BY_TYPE[1810100] as typeof NumberInputView)({
          value: unpackedValue,
          placeholder: unpackedPlaceholder,
          width: unpackedWidth,
          height: unpackedHeight,
          minWidth: unpackedMinWidth,
          minHeight: unpackedMinHeight,
          maxWidth: unpackedMaxWidth,
          maxHeight: unpackedMaxHeight,
          isVisible: unpackedIsVisible,
          opacity: unpackedOpacity,
          fill: unpackedFill,
          shadow: unpackedShadow,
          border: unpackedBorder,
          radius: unpackedRadius,
          position: unpackedPosition,
          offset: unpackedOffset,
          scale: unpackedScale,
          rotation: unpackedRotation,
          skew: unpackedSkew,
          origin: unpackedOrigin,
          anchor: unpackedAnchor,
          parent: unpackedParentPtr,
          materialization: Materialization[objectJson["materialization"]] as any,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          instance: unpackedInstancePtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          updatedAt: Temporal.Instant.from(objectJson["updatedAt"]).toZonedDateTimeISO('UTC'),
          updatedEpoch: Number(objectJson["updatedEpoch"]),
          updatedBy: _NodeReference.unpack(1, objectJson["updatedBy"], _session) as NodeReference,
          deletedAt: unpackedDeletedAt,
          ownedBy: unpackedOwnedByPtr,
          name: objectJson["name"],
          orderKey: objectJson["orderKey"],
          customValues: unpackedCustomValues,
          script: unpackedScriptPtr,
          isExtensible: unpackedIsExtensible,
          source: unpackedSourcePtr,
          key: unpackedKey,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 1810100)] = new NumberInputViewJsonEncoder();

    class SliderInputViewJsonEncoder implements _JsonObjectEncoder {
      packObject(object: SliderInputView): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "SLIDER_INPUT_VIEW";
        objectJson["id"] = object.id;
        if (object.parentPtr != null) {
          objectJson["parent"] = object.parentPtr.pack(1);
        }
        objectJson["space"] = object.spacePtr.pack(1);
        objectJson["materialization"] = Materialization[object.materialization];
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.instancePtr != null) {
          objectJson["instance"] = object.instancePtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["updatedAt"] = object.updatedAt.toString({ timeZoneName: 'never' });
        objectJson["updatedEpoch"] = Number(object.updatedEpoch);
        objectJson["updatedBy"] = object.updatedByPtr.pack(1);
        if (object.deletedAt != null) {
          objectJson["deletedAt"] = object.deletedAt.toString({ timeZoneName: 'never' });
        }
        if (object._ownedByPtr != null) {
          objectJson["ownedBy"] = object._ownedByPtr.pack(1);
        }
        objectJson["name"] = object._name;
        objectJson["orderKey"] = object.orderKey;
        if (Object.keys(object._customValues).length > 0) {
          const packedCustomValues: { [key: string]: any } = {} as any;
          for (const [key, value] of Object.entries(object._customValues)) {
            packedCustomValues[String(key)] = value.pack(1);
          }
          objectJson["customValues"] = packedCustomValues;
        }
        if (object._scriptPtr != null) {
          objectJson["script"] = object._scriptPtr.pack(1);
        }
        if (object.isExtensible != null) {
          objectJson["isExtensible"] = object.isExtensible;
        }
        if (object.sourcePtr != null) {
          objectJson["source"] = object.sourcePtr.pack(1);
        }
        if (object._key != null) {
          objectJson["key"] = object._key;
        }
        if (object._position != null) {
          objectJson["position"] = object._position.pack(1);
        }
        if (object._offset != null) {
          objectJson["offset"] = object._offset.pack(1);
        }
        if (object._scale != null) {
          objectJson["scale"] = object._scale.pack(1);
        }
        if (object._rotation != null) {
          objectJson["rotation"] = object._rotation.pack(1);
        }
        if (object._skew != null) {
          objectJson["skew"] = object._skew.pack(1);
        }
        if (object._origin != null) {
          objectJson["origin"] = object._origin.pack(1);
        }
        if (object._anchor != null) {
          objectJson["anchor"] = Anchor[object._anchor];
        }
        if (object._width != null) {
          objectJson["width"] = object._width.pack(1);
        }
        if (object._height != null) {
          objectJson["height"] = object._height.pack(1);
        }
        if (object._minWidth != null) {
          objectJson["minWidth"] = object._minWidth.pack(1);
        }
        if (object._minHeight != null) {
          objectJson["minHeight"] = object._minHeight.pack(1);
        }
        if (object._maxWidth != null) {
          objectJson["maxWidth"] = object._maxWidth.pack(1);
        }
        if (object._maxHeight != null) {
          objectJson["maxHeight"] = object._maxHeight.pack(1);
        }
        if (object._isVisible != null) {
          objectJson["isVisible"] = object._isVisible;
        }
        if (object._opacity != null) {
          objectJson["opacity"] = Number(object._opacity);
        }
        if (object._fill != null) {
          objectJson["fill"] = object._fill.pack(1);
        }
        if (object._shadow != null) {
          objectJson["shadow"] = object._shadow.pack(1);
        }
        if (object._border != null) {
          objectJson["border"] = object._border.pack(1);
        }
        if (object._radius != null) {
          objectJson["radius"] = object._radius.pack(1);
        }
        if (object._value != null) {
          objectJson["value"] = Number(object._value);
        }
        if (object._minValue != null) {
          objectJson["minValue"] = Number(object._minValue);
        }
        if (object._maxValue != null) {
          objectJson["maxValue"] = Number(object._maxValue);
        }
        if (object._step != null) {
          objectJson["step"] = Number(object._step);
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): SliderInputView {
        const _Value = STRUCT_CLASS_BY_TYPE[100] as typeof Value;
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const _Length = STRUCT_CLASS_BY_TYPE[1800001] as typeof Length;
        const _Fill = STRUCT_CLASS_BY_TYPE[2100400] as typeof Fill;
        const _Border = STRUCT_CLASS_BY_TYPE[2100600] as typeof Border;
        const _Shadow = STRUCT_CLASS_BY_TYPE[2100700] as typeof Shadow;
        const _Vector2 = STRUCT_CLASS_BY_TYPE[2400000] as typeof Vector2;
        const _Offset2 = STRUCT_CLASS_BY_TYPE[2400020] as typeof Offset2;
        const _Corner2 = STRUCT_CLASS_BY_TYPE[2400024] as typeof Corner2;
        const valueValue = objectJson["value"];
        const unpackedValue = valueValue != undefined ? Number(valueValue) : null;
        const minValueValue = objectJson["minValue"];
        const unpackedMinValue = minValueValue != undefined ? Number(minValueValue) : null;
        const maxValueValue = objectJson["maxValue"];
        const unpackedMaxValue = maxValueValue != undefined ? Number(maxValueValue) : null;
        const stepValue = objectJson["step"];
        const unpackedStep = stepValue != undefined ? Number(stepValue) : null;
        const widthValue = objectJson["width"];
        const unpackedWidth = widthValue != undefined ? _Length.unpack(1, widthValue, _session) as Length : null;
        const heightValue = objectJson["height"];
        const unpackedHeight = heightValue != undefined ? _Length.unpack(1, heightValue, _session) as Length : null;
        const minWidthValue = objectJson["minWidth"];
        const unpackedMinWidth = minWidthValue != undefined ? _Length.unpack(1, minWidthValue, _session) as Length : null;
        const minHeightValue = objectJson["minHeight"];
        const unpackedMinHeight = minHeightValue != undefined ? _Length.unpack(1, minHeightValue, _session) as Length : null;
        const maxWidthValue = objectJson["maxWidth"];
        const unpackedMaxWidth = maxWidthValue != undefined ? _Length.unpack(1, maxWidthValue, _session) as Length : null;
        const maxHeightValue = objectJson["maxHeight"];
        const unpackedMaxHeight = maxHeightValue != undefined ? _Length.unpack(1, maxHeightValue, _session) as Length : null;
        const isVisibleValue = objectJson["isVisible"];
        const unpackedIsVisible = isVisibleValue != undefined ? isVisibleValue : null;
        const opacityValue = objectJson["opacity"];
        const unpackedOpacity = opacityValue != undefined ? Number(opacityValue) : null;
        const fillValue = objectJson["fill"];
        const unpackedFill = fillValue != undefined ? _Fill.unpack(1, fillValue, _session) as Fill : null;
        const shadowValue = objectJson["shadow"];
        const unpackedShadow = shadowValue != undefined ? _Shadow.unpack(1, shadowValue, _session) as Shadow : null;
        const borderValue = objectJson["border"];
        const unpackedBorder = borderValue != undefined ? _Border.unpack(1, borderValue, _session) as Border : null;
        const radiusValue = objectJson["radius"];
        const unpackedRadius = radiusValue != undefined ? _Corner2.unpack(1, radiusValue, _session) as Corner2 : null;
        const positionValue = objectJson["position"];
        const unpackedPosition = positionValue != undefined ? _Vector2.unpack(1, positionValue, _session) as Vector2 : null;
        const offsetValue = objectJson["offset"];
        const unpackedOffset = offsetValue != undefined ? _Offset2.unpack(1, offsetValue, _session) as Offset2 : null;
        const scaleValue = objectJson["scale"];
        const unpackedScale = scaleValue != undefined ? _Vector2.unpack(1, scaleValue, _session) as Vector2 : null;
        const rotationValue = objectJson["rotation"];
        const unpackedRotation = rotationValue != undefined ? _Vector2.unpack(1, rotationValue, _session) as Vector2 : null;
        const skewValue = objectJson["skew"];
        const unpackedSkew = skewValue != undefined ? _Vector2.unpack(1, skewValue, _session) as Vector2 : null;
        const originValue = objectJson["origin"];
        const unpackedOrigin = originValue != undefined ? _Vector2.unpack(1, originValue, _session) as Vector2 : null;
        const anchorValue = objectJson["anchor"];
        const unpackedAnchor = anchorValue != undefined ? Anchor[anchorValue] as any : null;
        const parentPtrValue = objectJson["parent"];
        const unpackedParentPtr = parentPtrValue != undefined ? _NodeReference.unpack(1, parentPtrValue, _session) as NodeReference : null;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const instancePtrValue = objectJson["instance"];
        const unpackedInstancePtr = instancePtrValue != undefined ? _NodeReference.unpack(1, instancePtrValue, _session) as NodeReference : null;
        const deletedAtValue = objectJson["deletedAt"];
        const unpackedDeletedAt = deletedAtValue != undefined ? Temporal.Instant.from(deletedAtValue).toZonedDateTimeISO('UTC') : null;
        const ownedByPtrValue = objectJson["ownedBy"];
        const unpackedOwnedByPtr = ownedByPtrValue != undefined ? _NodeReference.unpack(1, ownedByPtrValue, _session) as NodeReference : null;
        const unpackedCustomValues = {} as any;
        if (objectJson["customValues"] != undefined) {
          for (const [key, value] of Object.entries(objectJson["customValues"])) {
            unpackedCustomValues[key] = _Value.unpack(1, value as any, _session) as Value;
          }
        }
        const scriptPtrValue = objectJson["script"];
        const unpackedScriptPtr = scriptPtrValue != undefined ? _NodeReference.unpack(1, scriptPtrValue, _session) as NodeReference : null;
        const isExtensibleValue = objectJson["isExtensible"];
        const unpackedIsExtensible = isExtensibleValue != undefined ? isExtensibleValue : null;
        const sourcePtrValue = objectJson["source"];
        const unpackedSourcePtr = sourcePtrValue != undefined ? _NodeReference.unpack(1, sourcePtrValue, _session) as NodeReference : null;
        const keyValue = objectJson["key"];
        const unpackedKey = keyValue != undefined ? keyValue : null;
        return new (NODE_CLASS_BY_TYPE[1810200] as typeof SliderInputView)({
          value: unpackedValue,
          minValue: unpackedMinValue,
          maxValue: unpackedMaxValue,
          step: unpackedStep,
          width: unpackedWidth,
          height: unpackedHeight,
          minWidth: unpackedMinWidth,
          minHeight: unpackedMinHeight,
          maxWidth: unpackedMaxWidth,
          maxHeight: unpackedMaxHeight,
          isVisible: unpackedIsVisible,
          opacity: unpackedOpacity,
          fill: unpackedFill,
          shadow: unpackedShadow,
          border: unpackedBorder,
          radius: unpackedRadius,
          position: unpackedPosition,
          offset: unpackedOffset,
          scale: unpackedScale,
          rotation: unpackedRotation,
          skew: unpackedSkew,
          origin: unpackedOrigin,
          anchor: unpackedAnchor,
          parent: unpackedParentPtr,
          materialization: Materialization[objectJson["materialization"]] as any,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          instance: unpackedInstancePtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          updatedAt: Temporal.Instant.from(objectJson["updatedAt"]).toZonedDateTimeISO('UTC'),
          updatedEpoch: Number(objectJson["updatedEpoch"]),
          updatedBy: _NodeReference.unpack(1, objectJson["updatedBy"], _session) as NodeReference,
          deletedAt: unpackedDeletedAt,
          ownedBy: unpackedOwnedByPtr,
          name: objectJson["name"],
          orderKey: objectJson["orderKey"],
          customValues: unpackedCustomValues,
          script: unpackedScriptPtr,
          isExtensible: unpackedIsExtensible,
          source: unpackedSourcePtr,
          key: unpackedKey,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 1810200)] = new SliderInputViewJsonEncoder();

    class SplitViewJsonEncoder implements _JsonObjectEncoder {
      packObject(object: SplitView): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "SPLIT_VIEW";
        objectJson["id"] = object.id;
        if (object.parentPtr != null) {
          objectJson["parent"] = object.parentPtr.pack(1);
        }
        objectJson["space"] = object.spacePtr.pack(1);
        objectJson["materialization"] = Materialization[object.materialization];
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.instancePtr != null) {
          objectJson["instance"] = object.instancePtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["updatedAt"] = object.updatedAt.toString({ timeZoneName: 'never' });
        objectJson["updatedEpoch"] = Number(object.updatedEpoch);
        objectJson["updatedBy"] = object.updatedByPtr.pack(1);
        if (object.deletedAt != null) {
          objectJson["deletedAt"] = object.deletedAt.toString({ timeZoneName: 'never' });
        }
        if (object._ownedByPtr != null) {
          objectJson["ownedBy"] = object._ownedByPtr.pack(1);
        }
        objectJson["name"] = object._name;
        objectJson["orderKey"] = object.orderKey;
        if (Object.keys(object._customValues).length > 0) {
          const packedCustomValues: { [key: string]: any } = {} as any;
          for (const [key, value] of Object.entries(object._customValues)) {
            packedCustomValues[String(key)] = value.pack(1);
          }
          objectJson["customValues"] = packedCustomValues;
        }
        if (object._scriptPtr != null) {
          objectJson["script"] = object._scriptPtr.pack(1);
        }
        if (object.isExtensible != null) {
          objectJson["isExtensible"] = object.isExtensible;
        }
        if (object.sourcePtr != null) {
          objectJson["source"] = object.sourcePtr.pack(1);
        }
        if (object._key != null) {
          objectJson["key"] = object._key;
        }
        if (object._position != null) {
          objectJson["position"] = object._position.pack(1);
        }
        if (object._offset != null) {
          objectJson["offset"] = object._offset.pack(1);
        }
        if (object._scale != null) {
          objectJson["scale"] = object._scale.pack(1);
        }
        if (object._rotation != null) {
          objectJson["rotation"] = object._rotation.pack(1);
        }
        if (object._skew != null) {
          objectJson["skew"] = object._skew.pack(1);
        }
        if (object._origin != null) {
          objectJson["origin"] = object._origin.pack(1);
        }
        if (object._anchor != null) {
          objectJson["anchor"] = Anchor[object._anchor];
        }
        if (object._width != null) {
          objectJson["width"] = object._width.pack(1);
        }
        if (object._height != null) {
          objectJson["height"] = object._height.pack(1);
        }
        if (object._minWidth != null) {
          objectJson["minWidth"] = object._minWidth.pack(1);
        }
        if (object._minHeight != null) {
          objectJson["minHeight"] = object._minHeight.pack(1);
        }
        if (object._maxWidth != null) {
          objectJson["maxWidth"] = object._maxWidth.pack(1);
        }
        if (object._maxHeight != null) {
          objectJson["maxHeight"] = object._maxHeight.pack(1);
        }
        if (object._isVisible != null) {
          objectJson["isVisible"] = object._isVisible;
        }
        if (object._opacity != null) {
          objectJson["opacity"] = Number(object._opacity);
        }
        if (object._fill != null) {
          objectJson["fill"] = object._fill.pack(1);
        }
        if (object._shadow != null) {
          objectJson["shadow"] = object._shadow.pack(1);
        }
        if (object._border != null) {
          objectJson["border"] = object._border.pack(1);
        }
        if (object._radius != null) {
          objectJson["radius"] = object._radius.pack(1);
        }
        if (object._layout != null) {
          objectJson["layout"] = Layout[object._layout];
        }
        if (object._direction != null) {
          objectJson["direction"] = Direction[object._direction];
        }
        if (object._distribute != null) {
          objectJson["distribute"] = Distribute[object._distribute];
        }
        if (object._align != null) {
          objectJson["align"] = Align[object._align];
        }
        if (object._gap != null) {
          objectJson["gap"] = object._gap.pack(1);
        }
        if (object._padding != null) {
          objectJson["padding"] = object._padding.pack(1);
        }
        if (object._grid != null) {
          objectJson["grid"] = object._grid.pack(1);
        }
        if (object._gridSpan != null) {
          objectJson["gridSpan"] = object._gridSpan.pack(1);
        }
        if (object._aspectRatio != null) {
          objectJson["aspectRatio"] = Number(object._aspectRatio);
        }
        if (object._isWrap != null) {
          objectJson["isWrap"] = object._isWrap;
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): SplitView {
        const _Value = STRUCT_CLASS_BY_TYPE[100] as typeof Value;
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const _Length = STRUCT_CLASS_BY_TYPE[1800001] as typeof Length;
        const _Fill = STRUCT_CLASS_BY_TYPE[2100400] as typeof Fill;
        const _Border = STRUCT_CLASS_BY_TYPE[2100600] as typeof Border;
        const _Shadow = STRUCT_CLASS_BY_TYPE[2100700] as typeof Shadow;
        const _Vector2 = STRUCT_CLASS_BY_TYPE[2400000] as typeof Vector2;
        const _Offset2 = STRUCT_CLASS_BY_TYPE[2400020] as typeof Offset2;
        const _Grid2 = STRUCT_CLASS_BY_TYPE[2400021] as typeof Grid2;
        const _GridSpan2 = STRUCT_CLASS_BY_TYPE[2400022] as typeof GridSpan2;
        const _Inset2 = STRUCT_CLASS_BY_TYPE[2400023] as typeof Inset2;
        const _Corner2 = STRUCT_CLASS_BY_TYPE[2400024] as typeof Corner2;
        const _Axis2 = STRUCT_CLASS_BY_TYPE[2400025] as typeof Axis2;
        const layoutValue = objectJson["layout"];
        const unpackedLayout = layoutValue != undefined ? Layout[layoutValue] as any : null;
        const directionValue = objectJson["direction"];
        const unpackedDirection = directionValue != undefined ? Direction[directionValue] as any : null;
        const distributeValue = objectJson["distribute"];
        const unpackedDistribute = distributeValue != undefined ? Distribute[distributeValue] as any : null;
        const alignValue = objectJson["align"];
        const unpackedAlign = alignValue != undefined ? Align[alignValue] as any : null;
        const gapValue = objectJson["gap"];
        const unpackedGap = gapValue != undefined ? _Axis2.unpack(1, gapValue, _session) as Axis2 : null;
        const paddingValue = objectJson["padding"];
        const unpackedPadding = paddingValue != undefined ? _Inset2.unpack(1, paddingValue, _session) as Inset2 : null;
        const gridValue = objectJson["grid"];
        const unpackedGrid = gridValue != undefined ? _Grid2.unpack(1, gridValue, _session) as Grid2 : null;
        const gridSpanValue = objectJson["gridSpan"];
        const unpackedGridSpan = gridSpanValue != undefined ? _GridSpan2.unpack(1, gridSpanValue, _session) as GridSpan2 : null;
        const aspectRatioValue = objectJson["aspectRatio"];
        const unpackedAspectRatio = aspectRatioValue != undefined ? Number(aspectRatioValue) : null;
        const isWrapValue = objectJson["isWrap"];
        const unpackedIsWrap = isWrapValue != undefined ? isWrapValue : null;
        const widthValue = objectJson["width"];
        const unpackedWidth = widthValue != undefined ? _Length.unpack(1, widthValue, _session) as Length : null;
        const heightValue = objectJson["height"];
        const unpackedHeight = heightValue != undefined ? _Length.unpack(1, heightValue, _session) as Length : null;
        const minWidthValue = objectJson["minWidth"];
        const unpackedMinWidth = minWidthValue != undefined ? _Length.unpack(1, minWidthValue, _session) as Length : null;
        const minHeightValue = objectJson["minHeight"];
        const unpackedMinHeight = minHeightValue != undefined ? _Length.unpack(1, minHeightValue, _session) as Length : null;
        const maxWidthValue = objectJson["maxWidth"];
        const unpackedMaxWidth = maxWidthValue != undefined ? _Length.unpack(1, maxWidthValue, _session) as Length : null;
        const maxHeightValue = objectJson["maxHeight"];
        const unpackedMaxHeight = maxHeightValue != undefined ? _Length.unpack(1, maxHeightValue, _session) as Length : null;
        const isVisibleValue = objectJson["isVisible"];
        const unpackedIsVisible = isVisibleValue != undefined ? isVisibleValue : null;
        const opacityValue = objectJson["opacity"];
        const unpackedOpacity = opacityValue != undefined ? Number(opacityValue) : null;
        const fillValue = objectJson["fill"];
        const unpackedFill = fillValue != undefined ? _Fill.unpack(1, fillValue, _session) as Fill : null;
        const shadowValue = objectJson["shadow"];
        const unpackedShadow = shadowValue != undefined ? _Shadow.unpack(1, shadowValue, _session) as Shadow : null;
        const borderValue = objectJson["border"];
        const unpackedBorder = borderValue != undefined ? _Border.unpack(1, borderValue, _session) as Border : null;
        const radiusValue = objectJson["radius"];
        const unpackedRadius = radiusValue != undefined ? _Corner2.unpack(1, radiusValue, _session) as Corner2 : null;
        const positionValue = objectJson["position"];
        const unpackedPosition = positionValue != undefined ? _Vector2.unpack(1, positionValue, _session) as Vector2 : null;
        const offsetValue = objectJson["offset"];
        const unpackedOffset = offsetValue != undefined ? _Offset2.unpack(1, offsetValue, _session) as Offset2 : null;
        const scaleValue = objectJson["scale"];
        const unpackedScale = scaleValue != undefined ? _Vector2.unpack(1, scaleValue, _session) as Vector2 : null;
        const rotationValue = objectJson["rotation"];
        const unpackedRotation = rotationValue != undefined ? _Vector2.unpack(1, rotationValue, _session) as Vector2 : null;
        const skewValue = objectJson["skew"];
        const unpackedSkew = skewValue != undefined ? _Vector2.unpack(1, skewValue, _session) as Vector2 : null;
        const originValue = objectJson["origin"];
        const unpackedOrigin = originValue != undefined ? _Vector2.unpack(1, originValue, _session) as Vector2 : null;
        const anchorValue = objectJson["anchor"];
        const unpackedAnchor = anchorValue != undefined ? Anchor[anchorValue] as any : null;
        const parentPtrValue = objectJson["parent"];
        const unpackedParentPtr = parentPtrValue != undefined ? _NodeReference.unpack(1, parentPtrValue, _session) as NodeReference : null;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const instancePtrValue = objectJson["instance"];
        const unpackedInstancePtr = instancePtrValue != undefined ? _NodeReference.unpack(1, instancePtrValue, _session) as NodeReference : null;
        const deletedAtValue = objectJson["deletedAt"];
        const unpackedDeletedAt = deletedAtValue != undefined ? Temporal.Instant.from(deletedAtValue).toZonedDateTimeISO('UTC') : null;
        const ownedByPtrValue = objectJson["ownedBy"];
        const unpackedOwnedByPtr = ownedByPtrValue != undefined ? _NodeReference.unpack(1, ownedByPtrValue, _session) as NodeReference : null;
        const unpackedCustomValues = {} as any;
        if (objectJson["customValues"] != undefined) {
          for (const [key, value] of Object.entries(objectJson["customValues"])) {
            unpackedCustomValues[key] = _Value.unpack(1, value as any, _session) as Value;
          }
        }
        const scriptPtrValue = objectJson["script"];
        const unpackedScriptPtr = scriptPtrValue != undefined ? _NodeReference.unpack(1, scriptPtrValue, _session) as NodeReference : null;
        const isExtensibleValue = objectJson["isExtensible"];
        const unpackedIsExtensible = isExtensibleValue != undefined ? isExtensibleValue : null;
        const sourcePtrValue = objectJson["source"];
        const unpackedSourcePtr = sourcePtrValue != undefined ? _NodeReference.unpack(1, sourcePtrValue, _session) as NodeReference : null;
        const keyValue = objectJson["key"];
        const unpackedKey = keyValue != undefined ? keyValue : null;
        return new (NODE_CLASS_BY_TYPE[1800400] as typeof SplitView)({
          layout: unpackedLayout,
          direction: unpackedDirection,
          distribute: unpackedDistribute,
          align: unpackedAlign,
          gap: unpackedGap,
          padding: unpackedPadding,
          grid: unpackedGrid,
          gridSpan: unpackedGridSpan,
          aspectRatio: unpackedAspectRatio,
          isWrap: unpackedIsWrap,
          width: unpackedWidth,
          height: unpackedHeight,
          minWidth: unpackedMinWidth,
          minHeight: unpackedMinHeight,
          maxWidth: unpackedMaxWidth,
          maxHeight: unpackedMaxHeight,
          isVisible: unpackedIsVisible,
          opacity: unpackedOpacity,
          fill: unpackedFill,
          shadow: unpackedShadow,
          border: unpackedBorder,
          radius: unpackedRadius,
          position: unpackedPosition,
          offset: unpackedOffset,
          scale: unpackedScale,
          rotation: unpackedRotation,
          skew: unpackedSkew,
          origin: unpackedOrigin,
          anchor: unpackedAnchor,
          parent: unpackedParentPtr,
          materialization: Materialization[objectJson["materialization"]] as any,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          instance: unpackedInstancePtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          updatedAt: Temporal.Instant.from(objectJson["updatedAt"]).toZonedDateTimeISO('UTC'),
          updatedEpoch: Number(objectJson["updatedEpoch"]),
          updatedBy: _NodeReference.unpack(1, objectJson["updatedBy"], _session) as NodeReference,
          deletedAt: unpackedDeletedAt,
          ownedBy: unpackedOwnedByPtr,
          name: objectJson["name"],
          orderKey: objectJson["orderKey"],
          customValues: unpackedCustomValues,
          script: unpackedScriptPtr,
          isExtensible: unpackedIsExtensible,
          source: unpackedSourcePtr,
          key: unpackedKey,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 1800400)] = new SplitViewJsonEncoder();

    class TextViewJsonEncoder implements _JsonObjectEncoder {
      packObject(object: TextView): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "TEXT_VIEW";
        objectJson["id"] = object.id;
        if (object.parentPtr != null) {
          objectJson["parent"] = object.parentPtr.pack(1);
        }
        objectJson["space"] = object.spacePtr.pack(1);
        objectJson["materialization"] = Materialization[object.materialization];
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.instancePtr != null) {
          objectJson["instance"] = object.instancePtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["updatedAt"] = object.updatedAt.toString({ timeZoneName: 'never' });
        objectJson["updatedEpoch"] = Number(object.updatedEpoch);
        objectJson["updatedBy"] = object.updatedByPtr.pack(1);
        if (object.deletedAt != null) {
          objectJson["deletedAt"] = object.deletedAt.toString({ timeZoneName: 'never' });
        }
        if (object._ownedByPtr != null) {
          objectJson["ownedBy"] = object._ownedByPtr.pack(1);
        }
        objectJson["name"] = object._name;
        objectJson["orderKey"] = object.orderKey;
        if (Object.keys(object._customValues).length > 0) {
          const packedCustomValues: { [key: string]: any } = {} as any;
          for (const [key, value] of Object.entries(object._customValues)) {
            packedCustomValues[String(key)] = value.pack(1);
          }
          objectJson["customValues"] = packedCustomValues;
        }
        if (object._scriptPtr != null) {
          objectJson["script"] = object._scriptPtr.pack(1);
        }
        if (object.isExtensible != null) {
          objectJson["isExtensible"] = object.isExtensible;
        }
        if (object.sourcePtr != null) {
          objectJson["source"] = object.sourcePtr.pack(1);
        }
        if (object._key != null) {
          objectJson["key"] = object._key;
        }
        if (object._position != null) {
          objectJson["position"] = object._position.pack(1);
        }
        if (object._offset != null) {
          objectJson["offset"] = object._offset.pack(1);
        }
        if (object._scale != null) {
          objectJson["scale"] = object._scale.pack(1);
        }
        if (object._rotation != null) {
          objectJson["rotation"] = object._rotation.pack(1);
        }
        if (object._skew != null) {
          objectJson["skew"] = object._skew.pack(1);
        }
        if (object._origin != null) {
          objectJson["origin"] = object._origin.pack(1);
        }
        if (object._anchor != null) {
          objectJson["anchor"] = Anchor[object._anchor];
        }
        if (object._width != null) {
          objectJson["width"] = object._width.pack(1);
        }
        if (object._height != null) {
          objectJson["height"] = object._height.pack(1);
        }
        if (object._minWidth != null) {
          objectJson["minWidth"] = object._minWidth.pack(1);
        }
        if (object._minHeight != null) {
          objectJson["minHeight"] = object._minHeight.pack(1);
        }
        if (object._maxWidth != null) {
          objectJson["maxWidth"] = object._maxWidth.pack(1);
        }
        if (object._maxHeight != null) {
          objectJson["maxHeight"] = object._maxHeight.pack(1);
        }
        if (object._isVisible != null) {
          objectJson["isVisible"] = object._isVisible;
        }
        if (object._opacity != null) {
          objectJson["opacity"] = Number(object._opacity);
        }
        if (object._fill != null) {
          objectJson["fill"] = object._fill.pack(1);
        }
        if (object._shadow != null) {
          objectJson["shadow"] = object._shadow.pack(1);
        }
        if (object._border != null) {
          objectJson["border"] = object._border.pack(1);
        }
        if (object._radius != null) {
          objectJson["radius"] = object._radius.pack(1);
        }
        if (object._font != null) {
          objectJson["font"] = object._font.pack(1);
        }
        if (object._color != null) {
          objectJson["color"] = object._color.pack(1);
        }
        if (object._text != null) {
          objectJson["text"] = object._text.pack(1);
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): TextView {
        const _Value = STRUCT_CLASS_BY_TYPE[100] as typeof Value;
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const _Text = STRUCT_CLASS_BY_TYPE[400020] as typeof Text;
        const _Length = STRUCT_CLASS_BY_TYPE[1800001] as typeof Length;
        const _Fill = STRUCT_CLASS_BY_TYPE[2100400] as typeof Fill;
        const _Font = STRUCT_CLASS_BY_TYPE[2100500] as typeof Font;
        const _Border = STRUCT_CLASS_BY_TYPE[2100600] as typeof Border;
        const _Shadow = STRUCT_CLASS_BY_TYPE[2100700] as typeof Shadow;
        const _Vector2 = STRUCT_CLASS_BY_TYPE[2400000] as typeof Vector2;
        const _Offset2 = STRUCT_CLASS_BY_TYPE[2400020] as typeof Offset2;
        const _Corner2 = STRUCT_CLASS_BY_TYPE[2400024] as typeof Corner2;
        const textValue = objectJson["text"];
        const unpackedText = textValue != undefined ? _Text.unpack(1, textValue, _session) as Text : null;
        const fontValue = objectJson["font"];
        const unpackedFont = fontValue != undefined ? _Font.unpack(1, fontValue, _session) as Font : null;
        const colorValue = objectJson["color"];
        const unpackedColor = colorValue != undefined ? _Fill.unpack(1, colorValue, _session) as Fill : null;
        const widthValue = objectJson["width"];
        const unpackedWidth = widthValue != undefined ? _Length.unpack(1, widthValue, _session) as Length : null;
        const heightValue = objectJson["height"];
        const unpackedHeight = heightValue != undefined ? _Length.unpack(1, heightValue, _session) as Length : null;
        const minWidthValue = objectJson["minWidth"];
        const unpackedMinWidth = minWidthValue != undefined ? _Length.unpack(1, minWidthValue, _session) as Length : null;
        const minHeightValue = objectJson["minHeight"];
        const unpackedMinHeight = minHeightValue != undefined ? _Length.unpack(1, minHeightValue, _session) as Length : null;
        const maxWidthValue = objectJson["maxWidth"];
        const unpackedMaxWidth = maxWidthValue != undefined ? _Length.unpack(1, maxWidthValue, _session) as Length : null;
        const maxHeightValue = objectJson["maxHeight"];
        const unpackedMaxHeight = maxHeightValue != undefined ? _Length.unpack(1, maxHeightValue, _session) as Length : null;
        const isVisibleValue = objectJson["isVisible"];
        const unpackedIsVisible = isVisibleValue != undefined ? isVisibleValue : null;
        const opacityValue = objectJson["opacity"];
        const unpackedOpacity = opacityValue != undefined ? Number(opacityValue) : null;
        const fillValue = objectJson["fill"];
        const unpackedFill = fillValue != undefined ? _Fill.unpack(1, fillValue, _session) as Fill : null;
        const shadowValue = objectJson["shadow"];
        const unpackedShadow = shadowValue != undefined ? _Shadow.unpack(1, shadowValue, _session) as Shadow : null;
        const borderValue = objectJson["border"];
        const unpackedBorder = borderValue != undefined ? _Border.unpack(1, borderValue, _session) as Border : null;
        const radiusValue = objectJson["radius"];
        const unpackedRadius = radiusValue != undefined ? _Corner2.unpack(1, radiusValue, _session) as Corner2 : null;
        const positionValue = objectJson["position"];
        const unpackedPosition = positionValue != undefined ? _Vector2.unpack(1, positionValue, _session) as Vector2 : null;
        const offsetValue = objectJson["offset"];
        const unpackedOffset = offsetValue != undefined ? _Offset2.unpack(1, offsetValue, _session) as Offset2 : null;
        const scaleValue = objectJson["scale"];
        const unpackedScale = scaleValue != undefined ? _Vector2.unpack(1, scaleValue, _session) as Vector2 : null;
        const rotationValue = objectJson["rotation"];
        const unpackedRotation = rotationValue != undefined ? _Vector2.unpack(1, rotationValue, _session) as Vector2 : null;
        const skewValue = objectJson["skew"];
        const unpackedSkew = skewValue != undefined ? _Vector2.unpack(1, skewValue, _session) as Vector2 : null;
        const originValue = objectJson["origin"];
        const unpackedOrigin = originValue != undefined ? _Vector2.unpack(1, originValue, _session) as Vector2 : null;
        const anchorValue = objectJson["anchor"];
        const unpackedAnchor = anchorValue != undefined ? Anchor[anchorValue] as any : null;
        const parentPtrValue = objectJson["parent"];
        const unpackedParentPtr = parentPtrValue != undefined ? _NodeReference.unpack(1, parentPtrValue, _session) as NodeReference : null;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const instancePtrValue = objectJson["instance"];
        const unpackedInstancePtr = instancePtrValue != undefined ? _NodeReference.unpack(1, instancePtrValue, _session) as NodeReference : null;
        const deletedAtValue = objectJson["deletedAt"];
        const unpackedDeletedAt = deletedAtValue != undefined ? Temporal.Instant.from(deletedAtValue).toZonedDateTimeISO('UTC') : null;
        const ownedByPtrValue = objectJson["ownedBy"];
        const unpackedOwnedByPtr = ownedByPtrValue != undefined ? _NodeReference.unpack(1, ownedByPtrValue, _session) as NodeReference : null;
        const unpackedCustomValues = {} as any;
        if (objectJson["customValues"] != undefined) {
          for (const [key, value] of Object.entries(objectJson["customValues"])) {
            unpackedCustomValues[key] = _Value.unpack(1, value as any, _session) as Value;
          }
        }
        const scriptPtrValue = objectJson["script"];
        const unpackedScriptPtr = scriptPtrValue != undefined ? _NodeReference.unpack(1, scriptPtrValue, _session) as NodeReference : null;
        const isExtensibleValue = objectJson["isExtensible"];
        const unpackedIsExtensible = isExtensibleValue != undefined ? isExtensibleValue : null;
        const sourcePtrValue = objectJson["source"];
        const unpackedSourcePtr = sourcePtrValue != undefined ? _NodeReference.unpack(1, sourcePtrValue, _session) as NodeReference : null;
        const keyValue = objectJson["key"];
        const unpackedKey = keyValue != undefined ? keyValue : null;
        return new (NODE_CLASS_BY_TYPE[1805100] as typeof TextView)({
          text: unpackedText,
          font: unpackedFont,
          color: unpackedColor,
          width: unpackedWidth,
          height: unpackedHeight,
          minWidth: unpackedMinWidth,
          minHeight: unpackedMinHeight,
          maxWidth: unpackedMaxWidth,
          maxHeight: unpackedMaxHeight,
          isVisible: unpackedIsVisible,
          opacity: unpackedOpacity,
          fill: unpackedFill,
          shadow: unpackedShadow,
          border: unpackedBorder,
          radius: unpackedRadius,
          position: unpackedPosition,
          offset: unpackedOffset,
          scale: unpackedScale,
          rotation: unpackedRotation,
          skew: unpackedSkew,
          origin: unpackedOrigin,
          anchor: unpackedAnchor,
          parent: unpackedParentPtr,
          materialization: Materialization[objectJson["materialization"]] as any,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          instance: unpackedInstancePtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          updatedAt: Temporal.Instant.from(objectJson["updatedAt"]).toZonedDateTimeISO('UTC'),
          updatedEpoch: Number(objectJson["updatedEpoch"]),
          updatedBy: _NodeReference.unpack(1, objectJson["updatedBy"], _session) as NodeReference,
          deletedAt: unpackedDeletedAt,
          ownedBy: unpackedOwnedByPtr,
          name: objectJson["name"],
          orderKey: objectJson["orderKey"],
          customValues: unpackedCustomValues,
          script: unpackedScriptPtr,
          isExtensible: unpackedIsExtensible,
          source: unpackedSourcePtr,
          key: unpackedKey,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 1805100)] = new TextViewJsonEncoder();

    class SceneJsonEncoder implements _JsonObjectEncoder {
      packObject(object: Scene): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "SCENE";
        objectJson["id"] = object.id;
        if (object.parentPtr != null) {
          objectJson["parent"] = object.parentPtr.pack(1);
        }
        objectJson["space"] = object.spacePtr.pack(1);
        objectJson["materialization"] = Materialization[object.materialization];
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.instancePtr != null) {
          objectJson["instance"] = object.instancePtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["updatedAt"] = object.updatedAt.toString({ timeZoneName: 'never' });
        objectJson["updatedEpoch"] = Number(object.updatedEpoch);
        objectJson["updatedBy"] = object.updatedByPtr.pack(1);
        if (object.deletedAt != null) {
          objectJson["deletedAt"] = object.deletedAt.toString({ timeZoneName: 'never' });
        }
        if (object._ownedByPtr != null) {
          objectJson["ownedBy"] = object._ownedByPtr.pack(1);
        }
        objectJson["name"] = object._name;
        objectJson["orderKey"] = object.orderKey;
        if (Object.keys(object._customValues).length > 0) {
          const packedCustomValues: { [key: string]: any } = {} as any;
          for (const [key, value] of Object.entries(object._customValues)) {
            packedCustomValues[String(key)] = value.pack(1);
          }
          objectJson["customValues"] = packedCustomValues;
        }
        if (object._scriptPtr != null) {
          objectJson["script"] = object._scriptPtr.pack(1);
        }
        if (object.isExtensible != null) {
          objectJson["isExtensible"] = object.isExtensible;
        }
        if (object.sourcePtr != null) {
          objectJson["source"] = object.sourcePtr.pack(1);
        }
        if (object._key != null) {
          objectJson["key"] = object._key;
        }
        if (object._icon != null) {
          objectJson["icon"] = object._icon.pack(1);
        }
        if (object._rootViewPtr != null) {
          objectJson["rootView"] = object._rootViewPtr.pack(1);
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): Scene {
        const _Value = STRUCT_CLASS_BY_TYPE[100] as typeof Value;
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const _Icon = STRUCT_CLASS_BY_TYPE[400031] as typeof Icon;
        const rootViewPtrValue = objectJson["rootView"];
        const unpackedRootViewPtr = rootViewPtrValue != undefined ? _NodeReference.unpack(1, rootViewPtrValue, _session) as NodeReference : null;
        const iconValue = objectJson["icon"];
        const unpackedIcon = iconValue != undefined ? _Icon.unpack(1, iconValue, _session) as Icon : null;
        const parentPtrValue = objectJson["parent"];
        const unpackedParentPtr = parentPtrValue != undefined ? _NodeReference.unpack(1, parentPtrValue, _session) as NodeReference : null;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const instancePtrValue = objectJson["instance"];
        const unpackedInstancePtr = instancePtrValue != undefined ? _NodeReference.unpack(1, instancePtrValue, _session) as NodeReference : null;
        const deletedAtValue = objectJson["deletedAt"];
        const unpackedDeletedAt = deletedAtValue != undefined ? Temporal.Instant.from(deletedAtValue).toZonedDateTimeISO('UTC') : null;
        const ownedByPtrValue = objectJson["ownedBy"];
        const unpackedOwnedByPtr = ownedByPtrValue != undefined ? _NodeReference.unpack(1, ownedByPtrValue, _session) as NodeReference : null;
        const unpackedCustomValues = {} as any;
        if (objectJson["customValues"] != undefined) {
          for (const [key, value] of Object.entries(objectJson["customValues"])) {
            unpackedCustomValues[key] = _Value.unpack(1, value as any, _session) as Value;
          }
        }
        const scriptPtrValue = objectJson["script"];
        const unpackedScriptPtr = scriptPtrValue != undefined ? _NodeReference.unpack(1, scriptPtrValue, _session) as NodeReference : null;
        const isExtensibleValue = objectJson["isExtensible"];
        const unpackedIsExtensible = isExtensibleValue != undefined ? isExtensibleValue : null;
        const sourcePtrValue = objectJson["source"];
        const unpackedSourcePtr = sourcePtrValue != undefined ? _NodeReference.unpack(1, sourcePtrValue, _session) as NodeReference : null;
        const keyValue = objectJson["key"];
        const unpackedKey = keyValue != undefined ? keyValue : null;
        return new (NODE_CLASS_BY_TYPE[1700200] as typeof Scene)({
          rootView: unpackedRootViewPtr,
          icon: unpackedIcon,
          parent: unpackedParentPtr,
          materialization: Materialization[objectJson["materialization"]] as any,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          instance: unpackedInstancePtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          updatedAt: Temporal.Instant.from(objectJson["updatedAt"]).toZonedDateTimeISO('UTC'),
          updatedEpoch: Number(objectJson["updatedEpoch"]),
          updatedBy: _NodeReference.unpack(1, objectJson["updatedBy"], _session) as NodeReference,
          deletedAt: unpackedDeletedAt,
          ownedBy: unpackedOwnedByPtr,
          name: objectJson["name"],
          orderKey: objectJson["orderKey"],
          customValues: unpackedCustomValues,
          script: unpackedScriptPtr,
          isExtensible: unpackedIsExtensible,
          source: unpackedSourcePtr,
          key: unpackedKey,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 1700200)] = new SceneJsonEncoder();

    class StageJsonEncoder implements _JsonObjectEncoder {
      packObject(object: Stage): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "STAGE";
        objectJson["id"] = object.id;
        if (object.parentPtr != null) {
          objectJson["parent"] = object.parentPtr.pack(1);
        }
        objectJson["space"] = object.spacePtr.pack(1);
        objectJson["materialization"] = Materialization[object.materialization];
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.instancePtr != null) {
          objectJson["instance"] = object.instancePtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["updatedAt"] = object.updatedAt.toString({ timeZoneName: 'never' });
        objectJson["updatedEpoch"] = Number(object.updatedEpoch);
        objectJson["updatedBy"] = object.updatedByPtr.pack(1);
        if (object.deletedAt != null) {
          objectJson["deletedAt"] = object.deletedAt.toString({ timeZoneName: 'never' });
        }
        if (object._ownedByPtr != null) {
          objectJson["ownedBy"] = object._ownedByPtr.pack(1);
        }
        objectJson["name"] = object._name;
        objectJson["orderKey"] = object.orderKey;
        if (Object.keys(object._customValues).length > 0) {
          const packedCustomValues: { [key: string]: any } = {} as any;
          for (const [key, value] of Object.entries(object._customValues)) {
            packedCustomValues[String(key)] = value.pack(1);
          }
          objectJson["customValues"] = packedCustomValues;
        }
        if (object._scriptPtr != null) {
          objectJson["script"] = object._scriptPtr.pack(1);
        }
        if (object.isExtensible != null) {
          objectJson["isExtensible"] = object.isExtensible;
        }
        if (object.sourcePtr != null) {
          objectJson["source"] = object.sourcePtr.pack(1);
        }
        if (object._key != null) {
          objectJson["key"] = object._key;
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): Stage {
        const _Value = STRUCT_CLASS_BY_TYPE[100] as typeof Value;
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const parentPtrValue = objectJson["parent"];
        const unpackedParentPtr = parentPtrValue != undefined ? _NodeReference.unpack(1, parentPtrValue, _session) as NodeReference : null;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const instancePtrValue = objectJson["instance"];
        const unpackedInstancePtr = instancePtrValue != undefined ? _NodeReference.unpack(1, instancePtrValue, _session) as NodeReference : null;
        const deletedAtValue = objectJson["deletedAt"];
        const unpackedDeletedAt = deletedAtValue != undefined ? Temporal.Instant.from(deletedAtValue).toZonedDateTimeISO('UTC') : null;
        const ownedByPtrValue = objectJson["ownedBy"];
        const unpackedOwnedByPtr = ownedByPtrValue != undefined ? _NodeReference.unpack(1, ownedByPtrValue, _session) as NodeReference : null;
        const unpackedCustomValues = {} as any;
        if (objectJson["customValues"] != undefined) {
          for (const [key, value] of Object.entries(objectJson["customValues"])) {
            unpackedCustomValues[key] = _Value.unpack(1, value as any, _session) as Value;
          }
        }
        const scriptPtrValue = objectJson["script"];
        const unpackedScriptPtr = scriptPtrValue != undefined ? _NodeReference.unpack(1, scriptPtrValue, _session) as NodeReference : null;
        const isExtensibleValue = objectJson["isExtensible"];
        const unpackedIsExtensible = isExtensibleValue != undefined ? isExtensibleValue : null;
        const sourcePtrValue = objectJson["source"];
        const unpackedSourcePtr = sourcePtrValue != undefined ? _NodeReference.unpack(1, sourcePtrValue, _session) as NodeReference : null;
        const keyValue = objectJson["key"];
        const unpackedKey = keyValue != undefined ? keyValue : null;
        return new (NODE_CLASS_BY_TYPE[1700000] as typeof Stage)({
          parent: unpackedParentPtr,
          materialization: Materialization[objectJson["materialization"]] as any,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          instance: unpackedInstancePtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          updatedAt: Temporal.Instant.from(objectJson["updatedAt"]).toZonedDateTimeISO('UTC'),
          updatedEpoch: Number(objectJson["updatedEpoch"]),
          updatedBy: _NodeReference.unpack(1, objectJson["updatedBy"], _session) as NodeReference,
          deletedAt: unpackedDeletedAt,
          ownedBy: unpackedOwnedByPtr,
          name: objectJson["name"],
          orderKey: objectJson["orderKey"],
          customValues: unpackedCustomValues,
          script: unpackedScriptPtr,
          isExtensible: unpackedIsExtensible,
          source: unpackedSourcePtr,
          key: unpackedKey,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 1700000)] = new StageJsonEncoder();

    class FollowJsonEncoder implements _JsonObjectEncoder {
      packObject(object: Follow): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "FOLLOW";
        objectJson["id"] = object.id;
        if (object.parentPtr != null) {
          objectJson["parent"] = object.parentPtr.pack(1);
        }
        objectJson["space"] = object.spacePtr.pack(1);
        objectJson["materialization"] = Materialization[object.materialization];
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.instancePtr != null) {
          objectJson["instance"] = object.instancePtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["updatedAt"] = object.updatedAt.toString({ timeZoneName: 'never' });
        objectJson["updatedEpoch"] = Number(object.updatedEpoch);
        objectJson["updatedBy"] = object.updatedByPtr.pack(1);
        if (object.deletedAt != null) {
          objectJson["deletedAt"] = object.deletedAt.toString({ timeZoneName: 'never' });
        }
        if (object._ownedByPtr != null) {
          objectJson["ownedBy"] = object._ownedByPtr.pack(1);
        }
        objectJson["name"] = object._name;
        objectJson["orderKey"] = object.orderKey;
        if (Object.keys(object._customValues).length > 0) {
          const packedCustomValues: { [key: string]: any } = {} as any;
          for (const [key, value] of Object.entries(object._customValues)) {
            packedCustomValues[String(key)] = value.pack(1);
          }
          objectJson["customValues"] = packedCustomValues;
        }
        if (object._scriptPtr != null) {
          objectJson["script"] = object._scriptPtr.pack(1);
        }
        if (object.isExtensible != null) {
          objectJson["isExtensible"] = object.isExtensible;
        }
        if (object.sourcePtr != null) {
          objectJson["source"] = object.sourcePtr.pack(1);
        }
        if (object._key != null) {
          objectJson["key"] = object._key;
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): Follow {
        const _Value = STRUCT_CLASS_BY_TYPE[100] as typeof Value;
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const parentPtrValue = objectJson["parent"];
        const unpackedParentPtr = parentPtrValue != undefined ? _NodeReference.unpack(1, parentPtrValue, _session) as NodeReference : null;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const instancePtrValue = objectJson["instance"];
        const unpackedInstancePtr = instancePtrValue != undefined ? _NodeReference.unpack(1, instancePtrValue, _session) as NodeReference : null;
        const deletedAtValue = objectJson["deletedAt"];
        const unpackedDeletedAt = deletedAtValue != undefined ? Temporal.Instant.from(deletedAtValue).toZonedDateTimeISO('UTC') : null;
        const ownedByPtrValue = objectJson["ownedBy"];
        const unpackedOwnedByPtr = ownedByPtrValue != undefined ? _NodeReference.unpack(1, ownedByPtrValue, _session) as NodeReference : null;
        const unpackedCustomValues = {} as any;
        if (objectJson["customValues"] != undefined) {
          for (const [key, value] of Object.entries(objectJson["customValues"])) {
            unpackedCustomValues[key] = _Value.unpack(1, value as any, _session) as Value;
          }
        }
        const scriptPtrValue = objectJson["script"];
        const unpackedScriptPtr = scriptPtrValue != undefined ? _NodeReference.unpack(1, scriptPtrValue, _session) as NodeReference : null;
        const isExtensibleValue = objectJson["isExtensible"];
        const unpackedIsExtensible = isExtensibleValue != undefined ? isExtensibleValue : null;
        const sourcePtrValue = objectJson["source"];
        const unpackedSourcePtr = sourcePtrValue != undefined ? _NodeReference.unpack(1, sourcePtrValue, _session) as NodeReference : null;
        const keyValue = objectJson["key"];
        const unpackedKey = keyValue != undefined ? keyValue : null;
        return new (NODE_CLASS_BY_TYPE[1400200] as typeof Follow)({
          parent: unpackedParentPtr,
          materialization: Materialization[objectJson["materialization"]] as any,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          instance: unpackedInstancePtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          updatedAt: Temporal.Instant.from(objectJson["updatedAt"]).toZonedDateTimeISO('UTC'),
          updatedEpoch: Number(objectJson["updatedEpoch"]),
          updatedBy: _NodeReference.unpack(1, objectJson["updatedBy"], _session) as NodeReference,
          deletedAt: unpackedDeletedAt,
          ownedBy: unpackedOwnedByPtr,
          name: objectJson["name"],
          orderKey: objectJson["orderKey"],
          customValues: unpackedCustomValues,
          script: unpackedScriptPtr,
          isExtensible: unpackedIsExtensible,
          source: unpackedSourcePtr,
          key: unpackedKey,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 1400200)] = new FollowJsonEncoder();

    class FollowEventJsonEncoder implements _JsonObjectEncoder {
      packObject(object: FollowEvent): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "FOLLOW_EVENT";
        objectJson["id"] = object.id;
        objectJson["space"] = object.spacePtr.pack(1);
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.causedByPtr != null) {
          objectJson["causedBy"] = object.causedByPtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["client"] = object.clientPtr.pack(1);
        objectJson["clientNonce"] = object.clientNonce;
        objectJson["clientCreatedAt"] = object.clientCreatedAt.toString({ timeZoneName: 'never' });
        objectJson["clientEpoch"] = Number(object.clientEpoch);
        objectJson["status"] = EventStatus[object.status];
        objectJson["node"] = object.nodePtr.pack(1);
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): FollowEvent {
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const causedByPtrValue = objectJson["causedBy"];
        const unpackedCausedByPtr = causedByPtrValue != undefined ? _NodeReference.unpack(1, causedByPtrValue, _session) as NodeReference : null;
        return new (NODE_CLASS_BY_TYPE[1400201] as typeof FollowEvent)({
          node: _NodeReference.unpack(1, objectJson["node"], _session) as NodeReference,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          causedBy: unpackedCausedByPtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          client: _NodeReference.unpack(1, objectJson["client"], _session) as NodeReference,
          clientNonce: objectJson["clientNonce"],
          clientCreatedAt: Temporal.Instant.from(objectJson["clientCreatedAt"]).toZonedDateTimeISO('UTC'),
          clientEpoch: Number(objectJson["clientEpoch"]),
          status: EventStatus[objectJson["status"]] as any,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 1400201)] = new FollowEventJsonEncoder();

    class FollowAddedEventJsonEncoder implements _JsonObjectEncoder {
      packObject(object: FollowAddedEvent): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "FOLLOW_ADDED_EVENT";
        objectJson["id"] = object.id;
        objectJson["space"] = object.spacePtr.pack(1);
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.causedByPtr != null) {
          objectJson["causedBy"] = object.causedByPtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["client"] = object.clientPtr.pack(1);
        objectJson["clientNonce"] = object.clientNonce;
        objectJson["clientCreatedAt"] = object.clientCreatedAt.toString({ timeZoneName: 'never' });
        objectJson["clientEpoch"] = Number(object.clientEpoch);
        objectJson["status"] = EventStatus[object.status];
        objectJson["node"] = object.nodePtr.pack(1);
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): FollowAddedEvent {
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const causedByPtrValue = objectJson["causedBy"];
        const unpackedCausedByPtr = causedByPtrValue != undefined ? _NodeReference.unpack(1, causedByPtrValue, _session) as NodeReference : null;
        return new (NODE_CLASS_BY_TYPE[1400202] as typeof FollowAddedEvent)({
          node: _NodeReference.unpack(1, objectJson["node"], _session) as NodeReference,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          causedBy: unpackedCausedByPtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          client: _NodeReference.unpack(1, objectJson["client"], _session) as NodeReference,
          clientNonce: objectJson["clientNonce"],
          clientCreatedAt: Temporal.Instant.from(objectJson["clientCreatedAt"]).toZonedDateTimeISO('UTC'),
          clientEpoch: Number(objectJson["clientEpoch"]),
          status: EventStatus[objectJson["status"]] as any,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 1400202)] = new FollowAddedEventJsonEncoder();

    class FollowRemovedEventJsonEncoder implements _JsonObjectEncoder {
      packObject(object: FollowRemovedEvent): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "FOLLOW_REMOVED_EVENT";
        objectJson["id"] = object.id;
        objectJson["space"] = object.spacePtr.pack(1);
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.causedByPtr != null) {
          objectJson["causedBy"] = object.causedByPtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["client"] = object.clientPtr.pack(1);
        objectJson["clientNonce"] = object.clientNonce;
        objectJson["clientCreatedAt"] = object.clientCreatedAt.toString({ timeZoneName: 'never' });
        objectJson["clientEpoch"] = Number(object.clientEpoch);
        objectJson["status"] = EventStatus[object.status];
        objectJson["node"] = object.nodePtr.pack(1);
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): FollowRemovedEvent {
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const causedByPtrValue = objectJson["causedBy"];
        const unpackedCausedByPtr = causedByPtrValue != undefined ? _NodeReference.unpack(1, causedByPtrValue, _session) as NodeReference : null;
        return new (NODE_CLASS_BY_TYPE[1400203] as typeof FollowRemovedEvent)({
          node: _NodeReference.unpack(1, objectJson["node"], _session) as NodeReference,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          causedBy: unpackedCausedByPtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          client: _NodeReference.unpack(1, objectJson["client"], _session) as NodeReference,
          clientNonce: objectJson["clientNonce"],
          clientCreatedAt: Temporal.Instant.from(objectJson["clientCreatedAt"]).toZonedDateTimeISO('UTC'),
          clientEpoch: Number(objectJson["clientEpoch"]),
          status: EventStatus[objectJson["status"]] as any,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 1400203)] = new FollowRemovedEventJsonEncoder();

    class NotificationSentEventJsonEncoder implements _JsonObjectEncoder {
      packObject(object: NotificationSentEvent): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "NOTIFICATION_SENT_EVENT";
        objectJson["id"] = object.id;
        objectJson["space"] = object.spacePtr.pack(1);
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.causedByPtr != null) {
          objectJson["causedBy"] = object.causedByPtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["client"] = object.clientPtr.pack(1);
        objectJson["clientNonce"] = object.clientNonce;
        objectJson["clientCreatedAt"] = object.clientCreatedAt.toString({ timeZoneName: 'never' });
        objectJson["clientEpoch"] = Number(object.clientEpoch);
        objectJson["status"] = EventStatus[object.status];
        objectJson["node"] = object.nodePtr.pack(1);
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): NotificationSentEvent {
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const causedByPtrValue = objectJson["causedBy"];
        const unpackedCausedByPtr = causedByPtrValue != undefined ? _NodeReference.unpack(1, causedByPtrValue, _session) as NodeReference : null;
        return new (NODE_CLASS_BY_TYPE[1400502] as typeof NotificationSentEvent)({
          node: _NodeReference.unpack(1, objectJson["node"], _session) as NodeReference,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          causedBy: unpackedCausedByPtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          client: _NodeReference.unpack(1, objectJson["client"], _session) as NodeReference,
          clientNonce: objectJson["clientNonce"],
          clientCreatedAt: Temporal.Instant.from(objectJson["clientCreatedAt"]).toZonedDateTimeISO('UTC'),
          clientEpoch: Number(objectJson["clientEpoch"]),
          status: EventStatus[objectJson["status"]] as any,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 1400502)] = new NotificationSentEventJsonEncoder();

    class NotificationRescindedEventJsonEncoder implements _JsonObjectEncoder {
      packObject(object: NotificationRescindedEvent): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "NOTIFICATION_RESCINDED_EVENT";
        objectJson["id"] = object.id;
        objectJson["space"] = object.spacePtr.pack(1);
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.causedByPtr != null) {
          objectJson["causedBy"] = object.causedByPtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["client"] = object.clientPtr.pack(1);
        objectJson["clientNonce"] = object.clientNonce;
        objectJson["clientCreatedAt"] = object.clientCreatedAt.toString({ timeZoneName: 'never' });
        objectJson["clientEpoch"] = Number(object.clientEpoch);
        objectJson["status"] = EventStatus[object.status];
        objectJson["node"] = object.nodePtr.pack(1);
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): NotificationRescindedEvent {
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const causedByPtrValue = objectJson["causedBy"];
        const unpackedCausedByPtr = causedByPtrValue != undefined ? _NodeReference.unpack(1, causedByPtrValue, _session) as NodeReference : null;
        return new (NODE_CLASS_BY_TYPE[1400503] as typeof NotificationRescindedEvent)({
          node: _NodeReference.unpack(1, objectJson["node"], _session) as NodeReference,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          causedBy: unpackedCausedByPtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          client: _NodeReference.unpack(1, objectJson["client"], _session) as NodeReference,
          clientNonce: objectJson["clientNonce"],
          clientCreatedAt: Temporal.Instant.from(objectJson["clientCreatedAt"]).toZonedDateTimeISO('UTC'),
          clientEpoch: Number(objectJson["clientEpoch"]),
          status: EventStatus[objectJson["status"]] as any,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 1400503)] = new NotificationRescindedEventJsonEncoder();

    class NotificationReadEventJsonEncoder implements _JsonObjectEncoder {
      packObject(object: NotificationReadEvent): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "NOTIFICATION_READ_EVENT";
        objectJson["id"] = object.id;
        objectJson["space"] = object.spacePtr.pack(1);
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.causedByPtr != null) {
          objectJson["causedBy"] = object.causedByPtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["client"] = object.clientPtr.pack(1);
        objectJson["clientNonce"] = object.clientNonce;
        objectJson["clientCreatedAt"] = object.clientCreatedAt.toString({ timeZoneName: 'never' });
        objectJson["clientEpoch"] = Number(object.clientEpoch);
        objectJson["status"] = EventStatus[object.status];
        objectJson["node"] = object.nodePtr.pack(1);
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): NotificationReadEvent {
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const causedByPtrValue = objectJson["causedBy"];
        const unpackedCausedByPtr = causedByPtrValue != undefined ? _NodeReference.unpack(1, causedByPtrValue, _session) as NodeReference : null;
        return new (NODE_CLASS_BY_TYPE[1400504] as typeof NotificationReadEvent)({
          node: _NodeReference.unpack(1, objectJson["node"], _session) as NodeReference,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          causedBy: unpackedCausedByPtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          client: _NodeReference.unpack(1, objectJson["client"], _session) as NodeReference,
          clientNonce: objectJson["clientNonce"],
          clientCreatedAt: Temporal.Instant.from(objectJson["clientCreatedAt"]).toZonedDateTimeISO('UTC'),
          clientEpoch: Number(objectJson["clientEpoch"]),
          status: EventStatus[objectJson["status"]] as any,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 1400504)] = new NotificationReadEventJsonEncoder();

    class NotificationDismissedEventJsonEncoder implements _JsonObjectEncoder {
      packObject(object: NotificationDismissedEvent): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "NOTIFICATION_DISMISSED_EVENT";
        objectJson["id"] = object.id;
        objectJson["space"] = object.spacePtr.pack(1);
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.causedByPtr != null) {
          objectJson["causedBy"] = object.causedByPtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["client"] = object.clientPtr.pack(1);
        objectJson["clientNonce"] = object.clientNonce;
        objectJson["clientCreatedAt"] = object.clientCreatedAt.toString({ timeZoneName: 'never' });
        objectJson["clientEpoch"] = Number(object.clientEpoch);
        objectJson["status"] = EventStatus[object.status];
        objectJson["node"] = object.nodePtr.pack(1);
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): NotificationDismissedEvent {
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const causedByPtrValue = objectJson["causedBy"];
        const unpackedCausedByPtr = causedByPtrValue != undefined ? _NodeReference.unpack(1, causedByPtrValue, _session) as NodeReference : null;
        return new (NODE_CLASS_BY_TYPE[1400505] as typeof NotificationDismissedEvent)({
          node: _NodeReference.unpack(1, objectJson["node"], _session) as NodeReference,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          causedBy: unpackedCausedByPtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          client: _NodeReference.unpack(1, objectJson["client"], _session) as NodeReference,
          clientNonce: objectJson["clientNonce"],
          clientCreatedAt: Temporal.Instant.from(objectJson["clientCreatedAt"]).toZonedDateTimeISO('UTC'),
          clientEpoch: Number(objectJson["clientEpoch"]),
          status: EventStatus[objectJson["status"]] as any,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 1400505)] = new NotificationDismissedEventJsonEncoder();

    class NotificationExpiredEventJsonEncoder implements _JsonObjectEncoder {
      packObject(object: NotificationExpiredEvent): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "NOTIFICATION_EXPIRED_EVENT";
        objectJson["id"] = object.id;
        objectJson["space"] = object.spacePtr.pack(1);
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.causedByPtr != null) {
          objectJson["causedBy"] = object.causedByPtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["client"] = object.clientPtr.pack(1);
        objectJson["clientNonce"] = object.clientNonce;
        objectJson["clientCreatedAt"] = object.clientCreatedAt.toString({ timeZoneName: 'never' });
        objectJson["clientEpoch"] = Number(object.clientEpoch);
        objectJson["status"] = EventStatus[object.status];
        objectJson["node"] = object.nodePtr.pack(1);
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): NotificationExpiredEvent {
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const causedByPtrValue = objectJson["causedBy"];
        const unpackedCausedByPtr = causedByPtrValue != undefined ? _NodeReference.unpack(1, causedByPtrValue, _session) as NodeReference : null;
        return new (NODE_CLASS_BY_TYPE[1400506] as typeof NotificationExpiredEvent)({
          node: _NodeReference.unpack(1, objectJson["node"], _session) as NodeReference,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          causedBy: unpackedCausedByPtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          client: _NodeReference.unpack(1, objectJson["client"], _session) as NodeReference,
          clientNonce: objectJson["clientNonce"],
          clientCreatedAt: Temporal.Instant.from(objectJson["clientCreatedAt"]).toZonedDateTimeISO('UTC'),
          clientEpoch: Number(objectJson["clientEpoch"]),
          status: EventStatus[objectJson["status"]] as any,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 1400506)] = new NotificationExpiredEventJsonEncoder();

    class NotificationJsonEncoder implements _JsonObjectEncoder {
      packObject(object: Notification): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "NOTIFICATION";
        objectJson["id"] = object.id;
        if (object.parentPtr != null) {
          objectJson["parent"] = object.parentPtr.pack(1);
        }
        objectJson["space"] = object.spacePtr.pack(1);
        objectJson["materialization"] = Materialization[object.materialization];
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.instancePtr != null) {
          objectJson["instance"] = object.instancePtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["updatedAt"] = object.updatedAt.toString({ timeZoneName: 'never' });
        objectJson["updatedEpoch"] = Number(object.updatedEpoch);
        objectJson["updatedBy"] = object.updatedByPtr.pack(1);
        if (object.deletedAt != null) {
          objectJson["deletedAt"] = object.deletedAt.toString({ timeZoneName: 'never' });
        }
        if (object._ownedByPtr != null) {
          objectJson["ownedBy"] = object._ownedByPtr.pack(1);
        }
        objectJson["name"] = object._name;
        objectJson["orderKey"] = object.orderKey;
        if (Object.keys(object._customValues).length > 0) {
          const packedCustomValues: { [key: string]: any } = {} as any;
          for (const [key, value] of Object.entries(object._customValues)) {
            packedCustomValues[String(key)] = value.pack(1);
          }
          objectJson["customValues"] = packedCustomValues;
        }
        if (object._scriptPtr != null) {
          objectJson["script"] = object._scriptPtr.pack(1);
        }
        if (object.isExtensible != null) {
          objectJson["isExtensible"] = object.isExtensible;
        }
        if (object.sourcePtr != null) {
          objectJson["source"] = object.sourcePtr.pack(1);
        }
        if (object._key != null) {
          objectJson["key"] = object._key;
        }
        objectJson["title"] = object._title;
        objectJson["status"] = NotificationStatus[object._status];
        if (object._text != null) {
          objectJson["text"] = object._text.pack(1);
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): Notification {
        const _Value = STRUCT_CLASS_BY_TYPE[100] as typeof Value;
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const _Text = STRUCT_CLASS_BY_TYPE[400020] as typeof Text;
        const textValue = objectJson["text"];
        const unpackedText = textValue != undefined ? _Text.unpack(1, textValue, _session) as Text : null;
        const parentPtrValue = objectJson["parent"];
        const unpackedParentPtr = parentPtrValue != undefined ? _NodeReference.unpack(1, parentPtrValue, _session) as NodeReference : null;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const instancePtrValue = objectJson["instance"];
        const unpackedInstancePtr = instancePtrValue != undefined ? _NodeReference.unpack(1, instancePtrValue, _session) as NodeReference : null;
        const deletedAtValue = objectJson["deletedAt"];
        const unpackedDeletedAt = deletedAtValue != undefined ? Temporal.Instant.from(deletedAtValue).toZonedDateTimeISO('UTC') : null;
        const ownedByPtrValue = objectJson["ownedBy"];
        const unpackedOwnedByPtr = ownedByPtrValue != undefined ? _NodeReference.unpack(1, ownedByPtrValue, _session) as NodeReference : null;
        const unpackedCustomValues = {} as any;
        if (objectJson["customValues"] != undefined) {
          for (const [key, value] of Object.entries(objectJson["customValues"])) {
            unpackedCustomValues[key] = _Value.unpack(1, value as any, _session) as Value;
          }
        }
        const scriptPtrValue = objectJson["script"];
        const unpackedScriptPtr = scriptPtrValue != undefined ? _NodeReference.unpack(1, scriptPtrValue, _session) as NodeReference : null;
        const isExtensibleValue = objectJson["isExtensible"];
        const unpackedIsExtensible = isExtensibleValue != undefined ? isExtensibleValue : null;
        const sourcePtrValue = objectJson["source"];
        const unpackedSourcePtr = sourcePtrValue != undefined ? _NodeReference.unpack(1, sourcePtrValue, _session) as NodeReference : null;
        const keyValue = objectJson["key"];
        const unpackedKey = keyValue != undefined ? keyValue : null;
        return new (NODE_CLASS_BY_TYPE[1400500] as typeof Notification)({
          title: objectJson["title"],
          status: NotificationStatus[objectJson["status"]] as any,
          text: unpackedText,
          parent: unpackedParentPtr,
          materialization: Materialization[objectJson["materialization"]] as any,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          instance: unpackedInstancePtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          updatedAt: Temporal.Instant.from(objectJson["updatedAt"]).toZonedDateTimeISO('UTC'),
          updatedEpoch: Number(objectJson["updatedEpoch"]),
          updatedBy: _NodeReference.unpack(1, objectJson["updatedBy"], _session) as NodeReference,
          deletedAt: unpackedDeletedAt,
          ownedBy: unpackedOwnedByPtr,
          name: objectJson["name"],
          orderKey: objectJson["orderKey"],
          customValues: unpackedCustomValues,
          script: unpackedScriptPtr,
          isExtensible: unpackedIsExtensible,
          source: unpackedSourcePtr,
          key: unpackedKey,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 1400500)] = new NotificationJsonEncoder();

    class ReactionJsonEncoder implements _JsonObjectEncoder {
      packObject(object: Reaction): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "REACTION";
        objectJson["id"] = object.id;
        if (object.parentPtr != null) {
          objectJson["parent"] = object.parentPtr.pack(1);
        }
        objectJson["space"] = object.spacePtr.pack(1);
        objectJson["materialization"] = Materialization[object.materialization];
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.instancePtr != null) {
          objectJson["instance"] = object.instancePtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["updatedAt"] = object.updatedAt.toString({ timeZoneName: 'never' });
        objectJson["updatedEpoch"] = Number(object.updatedEpoch);
        objectJson["updatedBy"] = object.updatedByPtr.pack(1);
        if (object.deletedAt != null) {
          objectJson["deletedAt"] = object.deletedAt.toString({ timeZoneName: 'never' });
        }
        if (object._ownedByPtr != null) {
          objectJson["ownedBy"] = object._ownedByPtr.pack(1);
        }
        objectJson["name"] = object._name;
        objectJson["orderKey"] = object.orderKey;
        if (Object.keys(object._customValues).length > 0) {
          const packedCustomValues: { [key: string]: any } = {} as any;
          for (const [key, value] of Object.entries(object._customValues)) {
            packedCustomValues[String(key)] = value.pack(1);
          }
          objectJson["customValues"] = packedCustomValues;
        }
        if (object._scriptPtr != null) {
          objectJson["script"] = object._scriptPtr.pack(1);
        }
        if (object.isExtensible != null) {
          objectJson["isExtensible"] = object.isExtensible;
        }
        if (object.sourcePtr != null) {
          objectJson["source"] = object.sourcePtr.pack(1);
        }
        if (object._key != null) {
          objectJson["key"] = object._key;
        }
        objectJson["content"] = object._content;
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): Reaction {
        const _Value = STRUCT_CLASS_BY_TYPE[100] as typeof Value;
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const parentPtrValue = objectJson["parent"];
        const unpackedParentPtr = parentPtrValue != undefined ? _NodeReference.unpack(1, parentPtrValue, _session) as NodeReference : null;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const instancePtrValue = objectJson["instance"];
        const unpackedInstancePtr = instancePtrValue != undefined ? _NodeReference.unpack(1, instancePtrValue, _session) as NodeReference : null;
        const deletedAtValue = objectJson["deletedAt"];
        const unpackedDeletedAt = deletedAtValue != undefined ? Temporal.Instant.from(deletedAtValue).toZonedDateTimeISO('UTC') : null;
        const ownedByPtrValue = objectJson["ownedBy"];
        const unpackedOwnedByPtr = ownedByPtrValue != undefined ? _NodeReference.unpack(1, ownedByPtrValue, _session) as NodeReference : null;
        const unpackedCustomValues = {} as any;
        if (objectJson["customValues"] != undefined) {
          for (const [key, value] of Object.entries(objectJson["customValues"])) {
            unpackedCustomValues[key] = _Value.unpack(1, value as any, _session) as Value;
          }
        }
        const scriptPtrValue = objectJson["script"];
        const unpackedScriptPtr = scriptPtrValue != undefined ? _NodeReference.unpack(1, scriptPtrValue, _session) as NodeReference : null;
        const isExtensibleValue = objectJson["isExtensible"];
        const unpackedIsExtensible = isExtensibleValue != undefined ? isExtensibleValue : null;
        const sourcePtrValue = objectJson["source"];
        const unpackedSourcePtr = sourcePtrValue != undefined ? _NodeReference.unpack(1, sourcePtrValue, _session) as NodeReference : null;
        const keyValue = objectJson["key"];
        const unpackedKey = keyValue != undefined ? keyValue : null;
        return new (NODE_CLASS_BY_TYPE[1400000] as typeof Reaction)({
          content: objectJson["content"],
          parent: unpackedParentPtr,
          materialization: Materialization[objectJson["materialization"]] as any,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          instance: unpackedInstancePtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          updatedAt: Temporal.Instant.from(objectJson["updatedAt"]).toZonedDateTimeISO('UTC'),
          updatedEpoch: Number(objectJson["updatedEpoch"]),
          updatedBy: _NodeReference.unpack(1, objectJson["updatedBy"], _session) as NodeReference,
          deletedAt: unpackedDeletedAt,
          ownedBy: unpackedOwnedByPtr,
          name: objectJson["name"],
          orderKey: objectJson["orderKey"],
          customValues: unpackedCustomValues,
          script: unpackedScriptPtr,
          isExtensible: unpackedIsExtensible,
          source: unpackedSourcePtr,
          key: unpackedKey,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 1400000)] = new ReactionJsonEncoder();

    class ReactionEventJsonEncoder implements _JsonObjectEncoder {
      packObject(object: ReactionEvent): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "REACTION_EVENT";
        objectJson["id"] = object.id;
        objectJson["space"] = object.spacePtr.pack(1);
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.causedByPtr != null) {
          objectJson["causedBy"] = object.causedByPtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["client"] = object.clientPtr.pack(1);
        objectJson["clientNonce"] = object.clientNonce;
        objectJson["clientCreatedAt"] = object.clientCreatedAt.toString({ timeZoneName: 'never' });
        objectJson["clientEpoch"] = Number(object.clientEpoch);
        objectJson["status"] = EventStatus[object.status];
        objectJson["node"] = object.nodePtr.pack(1);
        objectJson["content"] = object.content;
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): ReactionEvent {
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const causedByPtrValue = objectJson["causedBy"];
        const unpackedCausedByPtr = causedByPtrValue != undefined ? _NodeReference.unpack(1, causedByPtrValue, _session) as NodeReference : null;
        return new (NODE_CLASS_BY_TYPE[1400001] as typeof ReactionEvent)({
          node: _NodeReference.unpack(1, objectJson["node"], _session) as NodeReference,
          content: objectJson["content"],
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          causedBy: unpackedCausedByPtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          client: _NodeReference.unpack(1, objectJson["client"], _session) as NodeReference,
          clientNonce: objectJson["clientNonce"],
          clientCreatedAt: Temporal.Instant.from(objectJson["clientCreatedAt"]).toZonedDateTimeISO('UTC'),
          clientEpoch: Number(objectJson["clientEpoch"]),
          status: EventStatus[objectJson["status"]] as any,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 1400001)] = new ReactionEventJsonEncoder();

    class ReactionAddedEventJsonEncoder implements _JsonObjectEncoder {
      packObject(object: ReactionAddedEvent): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "REACTION_ADDED_EVENT";
        objectJson["id"] = object.id;
        objectJson["space"] = object.spacePtr.pack(1);
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.causedByPtr != null) {
          objectJson["causedBy"] = object.causedByPtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["client"] = object.clientPtr.pack(1);
        objectJson["clientNonce"] = object.clientNonce;
        objectJson["clientCreatedAt"] = object.clientCreatedAt.toString({ timeZoneName: 'never' });
        objectJson["clientEpoch"] = Number(object.clientEpoch);
        objectJson["status"] = EventStatus[object.status];
        objectJson["node"] = object.nodePtr.pack(1);
        objectJson["content"] = object.content;
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): ReactionAddedEvent {
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const causedByPtrValue = objectJson["causedBy"];
        const unpackedCausedByPtr = causedByPtrValue != undefined ? _NodeReference.unpack(1, causedByPtrValue, _session) as NodeReference : null;
        return new (NODE_CLASS_BY_TYPE[1400002] as typeof ReactionAddedEvent)({
          node: _NodeReference.unpack(1, objectJson["node"], _session) as NodeReference,
          content: objectJson["content"],
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          causedBy: unpackedCausedByPtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          client: _NodeReference.unpack(1, objectJson["client"], _session) as NodeReference,
          clientNonce: objectJson["clientNonce"],
          clientCreatedAt: Temporal.Instant.from(objectJson["clientCreatedAt"]).toZonedDateTimeISO('UTC'),
          clientEpoch: Number(objectJson["clientEpoch"]),
          status: EventStatus[objectJson["status"]] as any,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 1400002)] = new ReactionAddedEventJsonEncoder();

    class ReactionRemovedEventJsonEncoder implements _JsonObjectEncoder {
      packObject(object: ReactionRemovedEvent): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "REACTION_REMOVED_EVENT";
        objectJson["id"] = object.id;
        objectJson["space"] = object.spacePtr.pack(1);
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.causedByPtr != null) {
          objectJson["causedBy"] = object.causedByPtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["client"] = object.clientPtr.pack(1);
        objectJson["clientNonce"] = object.clientNonce;
        objectJson["clientCreatedAt"] = object.clientCreatedAt.toString({ timeZoneName: 'never' });
        objectJson["clientEpoch"] = Number(object.clientEpoch);
        objectJson["status"] = EventStatus[object.status];
        objectJson["node"] = object.nodePtr.pack(1);
        objectJson["content"] = object.content;
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): ReactionRemovedEvent {
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const causedByPtrValue = objectJson["causedBy"];
        const unpackedCausedByPtr = causedByPtrValue != undefined ? _NodeReference.unpack(1, causedByPtrValue, _session) as NodeReference : null;
        return new (NODE_CLASS_BY_TYPE[1400003] as typeof ReactionRemovedEvent)({
          node: _NodeReference.unpack(1, objectJson["node"], _session) as NodeReference,
          content: objectJson["content"],
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          causedBy: unpackedCausedByPtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          client: _NodeReference.unpack(1, objectJson["client"], _session) as NodeReference,
          clientNonce: objectJson["clientNonce"],
          clientCreatedAt: Temporal.Instant.from(objectJson["clientCreatedAt"]).toZonedDateTimeISO('UTC'),
          clientEpoch: Number(objectJson["clientEpoch"]),
          status: EventStatus[objectJson["status"]] as any,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 1400003)] = new ReactionRemovedEventJsonEncoder();

    class StarJsonEncoder implements _JsonObjectEncoder {
      packObject(object: Star): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "STAR";
        objectJson["id"] = object.id;
        if (object.parentPtr != null) {
          objectJson["parent"] = object.parentPtr.pack(1);
        }
        objectJson["space"] = object.spacePtr.pack(1);
        objectJson["materialization"] = Materialization[object.materialization];
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.instancePtr != null) {
          objectJson["instance"] = object.instancePtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["updatedAt"] = object.updatedAt.toString({ timeZoneName: 'never' });
        objectJson["updatedEpoch"] = Number(object.updatedEpoch);
        objectJson["updatedBy"] = object.updatedByPtr.pack(1);
        if (object.deletedAt != null) {
          objectJson["deletedAt"] = object.deletedAt.toString({ timeZoneName: 'never' });
        }
        if (object._ownedByPtr != null) {
          objectJson["ownedBy"] = object._ownedByPtr.pack(1);
        }
        objectJson["name"] = object._name;
        objectJson["orderKey"] = object.orderKey;
        if (Object.keys(object._customValues).length > 0) {
          const packedCustomValues: { [key: string]: any } = {} as any;
          for (const [key, value] of Object.entries(object._customValues)) {
            packedCustomValues[String(key)] = value.pack(1);
          }
          objectJson["customValues"] = packedCustomValues;
        }
        if (object._scriptPtr != null) {
          objectJson["script"] = object._scriptPtr.pack(1);
        }
        if (object.isExtensible != null) {
          objectJson["isExtensible"] = object.isExtensible;
        }
        if (object.sourcePtr != null) {
          objectJson["source"] = object.sourcePtr.pack(1);
        }
        if (object._key != null) {
          objectJson["key"] = object._key;
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): Star {
        const _Value = STRUCT_CLASS_BY_TYPE[100] as typeof Value;
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const parentPtrValue = objectJson["parent"];
        const unpackedParentPtr = parentPtrValue != undefined ? _NodeReference.unpack(1, parentPtrValue, _session) as NodeReference : null;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const instancePtrValue = objectJson["instance"];
        const unpackedInstancePtr = instancePtrValue != undefined ? _NodeReference.unpack(1, instancePtrValue, _session) as NodeReference : null;
        const deletedAtValue = objectJson["deletedAt"];
        const unpackedDeletedAt = deletedAtValue != undefined ? Temporal.Instant.from(deletedAtValue).toZonedDateTimeISO('UTC') : null;
        const ownedByPtrValue = objectJson["ownedBy"];
        const unpackedOwnedByPtr = ownedByPtrValue != undefined ? _NodeReference.unpack(1, ownedByPtrValue, _session) as NodeReference : null;
        const unpackedCustomValues = {} as any;
        if (objectJson["customValues"] != undefined) {
          for (const [key, value] of Object.entries(objectJson["customValues"])) {
            unpackedCustomValues[key] = _Value.unpack(1, value as any, _session) as Value;
          }
        }
        const scriptPtrValue = objectJson["script"];
        const unpackedScriptPtr = scriptPtrValue != undefined ? _NodeReference.unpack(1, scriptPtrValue, _session) as NodeReference : null;
        const isExtensibleValue = objectJson["isExtensible"];
        const unpackedIsExtensible = isExtensibleValue != undefined ? isExtensibleValue : null;
        const sourcePtrValue = objectJson["source"];
        const unpackedSourcePtr = sourcePtrValue != undefined ? _NodeReference.unpack(1, sourcePtrValue, _session) as NodeReference : null;
        const keyValue = objectJson["key"];
        const unpackedKey = keyValue != undefined ? keyValue : null;
        return new (NODE_CLASS_BY_TYPE[1400100] as typeof Star)({
          parent: unpackedParentPtr,
          materialization: Materialization[objectJson["materialization"]] as any,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          instance: unpackedInstancePtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          updatedAt: Temporal.Instant.from(objectJson["updatedAt"]).toZonedDateTimeISO('UTC'),
          updatedEpoch: Number(objectJson["updatedEpoch"]),
          updatedBy: _NodeReference.unpack(1, objectJson["updatedBy"], _session) as NodeReference,
          deletedAt: unpackedDeletedAt,
          ownedBy: unpackedOwnedByPtr,
          name: objectJson["name"],
          orderKey: objectJson["orderKey"],
          customValues: unpackedCustomValues,
          script: unpackedScriptPtr,
          isExtensible: unpackedIsExtensible,
          source: unpackedSourcePtr,
          key: unpackedKey,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 1400100)] = new StarJsonEncoder();

    class StarEventJsonEncoder implements _JsonObjectEncoder {
      packObject(object: StarEvent): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "STAR_EVENT";
        objectJson["id"] = object.id;
        objectJson["space"] = object.spacePtr.pack(1);
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.causedByPtr != null) {
          objectJson["causedBy"] = object.causedByPtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["client"] = object.clientPtr.pack(1);
        objectJson["clientNonce"] = object.clientNonce;
        objectJson["clientCreatedAt"] = object.clientCreatedAt.toString({ timeZoneName: 'never' });
        objectJson["clientEpoch"] = Number(object.clientEpoch);
        objectJson["status"] = EventStatus[object.status];
        objectJson["node"] = object.nodePtr.pack(1);
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): StarEvent {
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const causedByPtrValue = objectJson["causedBy"];
        const unpackedCausedByPtr = causedByPtrValue != undefined ? _NodeReference.unpack(1, causedByPtrValue, _session) as NodeReference : null;
        return new (NODE_CLASS_BY_TYPE[1400101] as typeof StarEvent)({
          node: _NodeReference.unpack(1, objectJson["node"], _session) as NodeReference,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          causedBy: unpackedCausedByPtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          client: _NodeReference.unpack(1, objectJson["client"], _session) as NodeReference,
          clientNonce: objectJson["clientNonce"],
          clientCreatedAt: Temporal.Instant.from(objectJson["clientCreatedAt"]).toZonedDateTimeISO('UTC'),
          clientEpoch: Number(objectJson["clientEpoch"]),
          status: EventStatus[objectJson["status"]] as any,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 1400101)] = new StarEventJsonEncoder();

    class StarAddedEventJsonEncoder implements _JsonObjectEncoder {
      packObject(object: StarAddedEvent): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "STAR_ADDED_EVENT";
        objectJson["id"] = object.id;
        objectJson["space"] = object.spacePtr.pack(1);
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.causedByPtr != null) {
          objectJson["causedBy"] = object.causedByPtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["client"] = object.clientPtr.pack(1);
        objectJson["clientNonce"] = object.clientNonce;
        objectJson["clientCreatedAt"] = object.clientCreatedAt.toString({ timeZoneName: 'never' });
        objectJson["clientEpoch"] = Number(object.clientEpoch);
        objectJson["status"] = EventStatus[object.status];
        objectJson["node"] = object.nodePtr.pack(1);
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): StarAddedEvent {
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const causedByPtrValue = objectJson["causedBy"];
        const unpackedCausedByPtr = causedByPtrValue != undefined ? _NodeReference.unpack(1, causedByPtrValue, _session) as NodeReference : null;
        return new (NODE_CLASS_BY_TYPE[1400102] as typeof StarAddedEvent)({
          node: _NodeReference.unpack(1, objectJson["node"], _session) as NodeReference,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          causedBy: unpackedCausedByPtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          client: _NodeReference.unpack(1, objectJson["client"], _session) as NodeReference,
          clientNonce: objectJson["clientNonce"],
          clientCreatedAt: Temporal.Instant.from(objectJson["clientCreatedAt"]).toZonedDateTimeISO('UTC'),
          clientEpoch: Number(objectJson["clientEpoch"]),
          status: EventStatus[objectJson["status"]] as any,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 1400102)] = new StarAddedEventJsonEncoder();

    class StarRemovedEventJsonEncoder implements _JsonObjectEncoder {
      packObject(object: StarRemovedEvent): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "STAR_REMOVED_EVENT";
        objectJson["id"] = object.id;
        objectJson["space"] = object.spacePtr.pack(1);
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.causedByPtr != null) {
          objectJson["causedBy"] = object.causedByPtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["client"] = object.clientPtr.pack(1);
        objectJson["clientNonce"] = object.clientNonce;
        objectJson["clientCreatedAt"] = object.clientCreatedAt.toString({ timeZoneName: 'never' });
        objectJson["clientEpoch"] = Number(object.clientEpoch);
        objectJson["status"] = EventStatus[object.status];
        objectJson["node"] = object.nodePtr.pack(1);
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): StarRemovedEvent {
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const causedByPtrValue = objectJson["causedBy"];
        const unpackedCausedByPtr = causedByPtrValue != undefined ? _NodeReference.unpack(1, causedByPtrValue, _session) as NodeReference : null;
        return new (NODE_CLASS_BY_TYPE[1400103] as typeof StarRemovedEvent)({
          node: _NodeReference.unpack(1, objectJson["node"], _session) as NodeReference,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          causedBy: unpackedCausedByPtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          client: _NodeReference.unpack(1, objectJson["client"], _session) as NodeReference,
          clientNonce: objectJson["clientNonce"],
          clientCreatedAt: Temporal.Instant.from(objectJson["clientCreatedAt"]).toZonedDateTimeISO('UTC'),
          clientEpoch: Number(objectJson["clientEpoch"]),
          status: EventStatus[objectJson["status"]] as any,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 1400103)] = new StarRemovedEventJsonEncoder();

    class FolderJsonEncoder implements _JsonObjectEncoder {
      packObject(object: Folder): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "FOLDER";
        objectJson["id"] = object.id;
        if (object.parentPtr != null) {
          objectJson["parent"] = object.parentPtr.pack(1);
        }
        objectJson["space"] = object.spacePtr.pack(1);
        objectJson["materialization"] = Materialization[object.materialization];
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.instancePtr != null) {
          objectJson["instance"] = object.instancePtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["updatedAt"] = object.updatedAt.toString({ timeZoneName: 'never' });
        objectJson["updatedEpoch"] = Number(object.updatedEpoch);
        objectJson["updatedBy"] = object.updatedByPtr.pack(1);
        if (object.deletedAt != null) {
          objectJson["deletedAt"] = object.deletedAt.toString({ timeZoneName: 'never' });
        }
        if (object._ownedByPtr != null) {
          objectJson["ownedBy"] = object._ownedByPtr.pack(1);
        }
        objectJson["name"] = object._name;
        objectJson["orderKey"] = object.orderKey;
        if (Object.keys(object._customValues).length > 0) {
          const packedCustomValues: { [key: string]: any } = {} as any;
          for (const [key, value] of Object.entries(object._customValues)) {
            packedCustomValues[String(key)] = value.pack(1);
          }
          objectJson["customValues"] = packedCustomValues;
        }
        if (object._scriptPtr != null) {
          objectJson["script"] = object._scriptPtr.pack(1);
        }
        if (object.isExtensible != null) {
          objectJson["isExtensible"] = object.isExtensible;
        }
        if (object.sourcePtr != null) {
          objectJson["source"] = object.sourcePtr.pack(1);
        }
        if (object._key != null) {
          objectJson["key"] = object._key;
        }
        objectJson["type"] = FolderType[object._type];
        if (object._icon != null) {
          objectJson["icon"] = object._icon.pack(1);
        }
        if (object._slug != null) {
          objectJson["slug"] = object._slug;
        }
        if (object._mainScenePtr != null) {
          objectJson["mainScene"] = object._mainScenePtr.pack(1);
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): Folder {
        const _Value = STRUCT_CLASS_BY_TYPE[100] as typeof Value;
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const _Icon = STRUCT_CLASS_BY_TYPE[400031] as typeof Icon;
        const parentPtrValue = objectJson["parent"];
        const unpackedParentPtr = parentPtrValue != undefined ? _NodeReference.unpack(1, parentPtrValue, _session) as NodeReference : null;
        const iconValue = objectJson["icon"];
        const unpackedIcon = iconValue != undefined ? _Icon.unpack(1, iconValue, _session) as Icon : null;
        const slugValue = objectJson["slug"];
        const unpackedSlug = slugValue != undefined ? slugValue : null;
        const mainScenePtrValue = objectJson["mainScene"];
        const unpackedMainScenePtr = mainScenePtrValue != undefined ? _NodeReference.unpack(1, mainScenePtrValue, _session) as NodeReference : null;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const instancePtrValue = objectJson["instance"];
        const unpackedInstancePtr = instancePtrValue != undefined ? _NodeReference.unpack(1, instancePtrValue, _session) as NodeReference : null;
        const deletedAtValue = objectJson["deletedAt"];
        const unpackedDeletedAt = deletedAtValue != undefined ? Temporal.Instant.from(deletedAtValue).toZonedDateTimeISO('UTC') : null;
        const ownedByPtrValue = objectJson["ownedBy"];
        const unpackedOwnedByPtr = ownedByPtrValue != undefined ? _NodeReference.unpack(1, ownedByPtrValue, _session) as NodeReference : null;
        const unpackedCustomValues = {} as any;
        if (objectJson["customValues"] != undefined) {
          for (const [key, value] of Object.entries(objectJson["customValues"])) {
            unpackedCustomValues[key] = _Value.unpack(1, value as any, _session) as Value;
          }
        }
        const scriptPtrValue = objectJson["script"];
        const unpackedScriptPtr = scriptPtrValue != undefined ? _NodeReference.unpack(1, scriptPtrValue, _session) as NodeReference : null;
        const isExtensibleValue = objectJson["isExtensible"];
        const unpackedIsExtensible = isExtensibleValue != undefined ? isExtensibleValue : null;
        const sourcePtrValue = objectJson["source"];
        const unpackedSourcePtr = sourcePtrValue != undefined ? _NodeReference.unpack(1, sourcePtrValue, _session) as NodeReference : null;
        const keyValue = objectJson["key"];
        const unpackedKey = keyValue != undefined ? keyValue : null;
        return new (NODE_CLASS_BY_TYPE[240000] as typeof Folder)({
          parent: unpackedParentPtr,
          type: FolderType[objectJson["type"]] as any,
          icon: unpackedIcon,
          slug: unpackedSlug,
          mainScene: unpackedMainScenePtr,
          materialization: Materialization[objectJson["materialization"]] as any,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          instance: unpackedInstancePtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          updatedAt: Temporal.Instant.from(objectJson["updatedAt"]).toZonedDateTimeISO('UTC'),
          updatedEpoch: Number(objectJson["updatedEpoch"]),
          updatedBy: _NodeReference.unpack(1, objectJson["updatedBy"], _session) as NodeReference,
          deletedAt: unpackedDeletedAt,
          ownedBy: unpackedOwnedByPtr,
          name: objectJson["name"],
          orderKey: objectJson["orderKey"],
          customValues: unpackedCustomValues,
          script: unpackedScriptPtr,
          isExtensible: unpackedIsExtensible,
          source: unpackedSourcePtr,
          key: unpackedKey,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 240000)] = new FolderJsonEncoder();

    class ClientJsonEncoder implements _JsonObjectEncoder {
      packObject(object: Client): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "CLIENT";
        objectJson["id"] = object.id;
        if (object.parentPtr != null) {
          objectJson["parent"] = object.parentPtr.pack(1);
        }
        objectJson["space"] = object.spacePtr.pack(1);
        objectJson["materialization"] = Materialization[object.materialization];
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.instancePtr != null) {
          objectJson["instance"] = object.instancePtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["updatedAt"] = object.updatedAt.toString({ timeZoneName: 'never' });
        objectJson["updatedEpoch"] = Number(object.updatedEpoch);
        objectJson["updatedBy"] = object.updatedByPtr.pack(1);
        if (object.deletedAt != null) {
          objectJson["deletedAt"] = object.deletedAt.toString({ timeZoneName: 'never' });
        }
        if (object._ownedByPtr != null) {
          objectJson["ownedBy"] = object._ownedByPtr.pack(1);
        }
        objectJson["name"] = object._name;
        objectJson["orderKey"] = object.orderKey;
        if (object._browserVersion != null) {
          objectJson["browserVersion"] = object._browserVersion;
        }
        if (Object.keys(object._customValues).length > 0) {
          const packedCustomValues: { [key: string]: any } = {} as any;
          for (const [key, value] of Object.entries(object._customValues)) {
            packedCustomValues[String(key)] = value.pack(1);
          }
          objectJson["customValues"] = packedCustomValues;
        }
        if (object._scriptPtr != null) {
          objectJson["script"] = object._scriptPtr.pack(1);
        }
        if (object.isExtensible != null) {
          objectJson["isExtensible"] = object.isExtensible;
        }
        if (object.sourcePtr != null) {
          objectJson["source"] = object.sourcePtr.pack(1);
        }
        if (object._key != null) {
          objectJson["key"] = object._key;
        }
        objectJson["type"] = ClientType[object._type];
        if (object._machinePtr != null) {
          objectJson["machine"] = object._machinePtr.pack(1);
        }
        if (object._userPtr != null) {
          objectJson["user"] = object._userPtr.pack(1);
        }
        if (object._accessToken != null) {
          objectJson["accessToken"] = object._accessToken;
        }
        if (object._seenAt != null) {
          objectJson["seenAt"] = object._seenAt.toString({ timeZoneName: 'never' });
        }
        if (object._loggedInAt != null) {
          objectJson["loggedInAt"] = object._loggedInAt.toString({ timeZoneName: 'never' });
        }
        if (object._deviceType != null) {
          objectJson["deviceType"] = object._deviceType;
        }
        if (object._deviceName != null) {
          objectJson["deviceName"] = object._deviceName;
        }
        if (object._operatingSystem != null) {
          objectJson["operatingSystem"] = object._operatingSystem;
        }
        if (object._browserName != null) {
          objectJson["browserName"] = object._browserName;
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): Client {
        const _Value = STRUCT_CLASS_BY_TYPE[100] as typeof Value;
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const machinePtrValue = objectJson["machine"];
        const unpackedMachinePtr = machinePtrValue != undefined ? _NodeReference.unpack(1, machinePtrValue, _session) as NodeReference : null;
        const userPtrValue = objectJson["user"];
        const unpackedUserPtr = userPtrValue != undefined ? _NodeReference.unpack(1, userPtrValue, _session) as NodeReference : null;
        const accessTokenValue = objectJson["accessToken"];
        const unpackedAccessToken = accessTokenValue != undefined ? accessTokenValue : null;
        const seenAtValue = objectJson["seenAt"];
        const unpackedSeenAt = seenAtValue != undefined ? Temporal.Instant.from(seenAtValue).toZonedDateTimeISO('UTC') : null;
        const loggedInAtValue = objectJson["loggedInAt"];
        const unpackedLoggedInAt = loggedInAtValue != undefined ? Temporal.Instant.from(loggedInAtValue).toZonedDateTimeISO('UTC') : null;
        const deviceTypeValue = objectJson["deviceType"];
        const unpackedDeviceType = deviceTypeValue != undefined ? deviceTypeValue : null;
        const deviceNameValue = objectJson["deviceName"];
        const unpackedDeviceName = deviceNameValue != undefined ? deviceNameValue : null;
        const operatingSystemValue = objectJson["operatingSystem"];
        const unpackedOperatingSystem = operatingSystemValue != undefined ? operatingSystemValue : null;
        const browserNameValue = objectJson["browserName"];
        const unpackedBrowserName = browserNameValue != undefined ? browserNameValue : null;
        const browserVersionValue = objectJson["browserVersion"];
        const unpackedBrowserVersion = browserVersionValue != undefined ? browserVersionValue : null;
        const parentPtrValue = objectJson["parent"];
        const unpackedParentPtr = parentPtrValue != undefined ? _NodeReference.unpack(1, parentPtrValue, _session) as NodeReference : null;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const instancePtrValue = objectJson["instance"];
        const unpackedInstancePtr = instancePtrValue != undefined ? _NodeReference.unpack(1, instancePtrValue, _session) as NodeReference : null;
        const deletedAtValue = objectJson["deletedAt"];
        const unpackedDeletedAt = deletedAtValue != undefined ? Temporal.Instant.from(deletedAtValue).toZonedDateTimeISO('UTC') : null;
        const ownedByPtrValue = objectJson["ownedBy"];
        const unpackedOwnedByPtr = ownedByPtrValue != undefined ? _NodeReference.unpack(1, ownedByPtrValue, _session) as NodeReference : null;
        const unpackedCustomValues = {} as any;
        if (objectJson["customValues"] != undefined) {
          for (const [key, value] of Object.entries(objectJson["customValues"])) {
            unpackedCustomValues[key] = _Value.unpack(1, value as any, _session) as Value;
          }
        }
        const scriptPtrValue = objectJson["script"];
        const unpackedScriptPtr = scriptPtrValue != undefined ? _NodeReference.unpack(1, scriptPtrValue, _session) as NodeReference : null;
        const isExtensibleValue = objectJson["isExtensible"];
        const unpackedIsExtensible = isExtensibleValue != undefined ? isExtensibleValue : null;
        const sourcePtrValue = objectJson["source"];
        const unpackedSourcePtr = sourcePtrValue != undefined ? _NodeReference.unpack(1, sourcePtrValue, _session) as NodeReference : null;
        const keyValue = objectJson["key"];
        const unpackedKey = keyValue != undefined ? keyValue : null;
        return new (NODE_CLASS_BY_TYPE[121300] as typeof Client)({
          type: ClientType[objectJson["type"]] as any,
          machine: unpackedMachinePtr,
          user: unpackedUserPtr,
          accessToken: unpackedAccessToken,
          seenAt: unpackedSeenAt,
          loggedInAt: unpackedLoggedInAt,
          deviceType: unpackedDeviceType,
          deviceName: unpackedDeviceName,
          operatingSystem: unpackedOperatingSystem,
          browserName: unpackedBrowserName,
          browserVersion: unpackedBrowserVersion,
          parent: unpackedParentPtr,
          materialization: Materialization[objectJson["materialization"]] as any,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          instance: unpackedInstancePtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          updatedAt: Temporal.Instant.from(objectJson["updatedAt"]).toZonedDateTimeISO('UTC'),
          updatedEpoch: Number(objectJson["updatedEpoch"]),
          updatedBy: _NodeReference.unpack(1, objectJson["updatedBy"], _session) as NodeReference,
          deletedAt: unpackedDeletedAt,
          ownedBy: unpackedOwnedByPtr,
          name: objectJson["name"],
          orderKey: objectJson["orderKey"],
          customValues: unpackedCustomValues,
          script: unpackedScriptPtr,
          isExtensible: unpackedIsExtensible,
          source: unpackedSourcePtr,
          key: unpackedKey,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 121300)] = new ClientJsonEncoder();

    class HandleJsonEncoder implements _JsonObjectEncoder {
      packObject(object: Handle): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "HANDLE";
        objectJson["id"] = object.id;
        if (object.parentPtr != null) {
          objectJson["parent"] = object.parentPtr.pack(1);
        }
        objectJson["space"] = object.spacePtr.pack(1);
        objectJson["materialization"] = Materialization[object.materialization];
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.instancePtr != null) {
          objectJson["instance"] = object.instancePtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["updatedAt"] = object.updatedAt.toString({ timeZoneName: 'never' });
        objectJson["updatedEpoch"] = Number(object.updatedEpoch);
        objectJson["updatedBy"] = object.updatedByPtr.pack(1);
        if (object.deletedAt != null) {
          objectJson["deletedAt"] = object.deletedAt.toString({ timeZoneName: 'never' });
        }
        if (object._ownedByPtr != null) {
          objectJson["ownedBy"] = object._ownedByPtr.pack(1);
        }
        objectJson["name"] = object._name;
        objectJson["orderKey"] = object.orderKey;
        if (Object.keys(object._customValues).length > 0) {
          const packedCustomValues: { [key: string]: any } = {} as any;
          for (const [key, value] of Object.entries(object._customValues)) {
            packedCustomValues[String(key)] = value.pack(1);
          }
          objectJson["customValues"] = packedCustomValues;
        }
        if (object._scriptPtr != null) {
          objectJson["script"] = object._scriptPtr.pack(1);
        }
        if (object.isExtensible != null) {
          objectJson["isExtensible"] = object.isExtensible;
        }
        if (object.sourcePtr != null) {
          objectJson["source"] = object.sourcePtr.pack(1);
        }
        if (object._key != null) {
          objectJson["key"] = object._key;
        }
        objectJson["slug"] = object._slug;
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): Handle {
        const _Value = STRUCT_CLASS_BY_TYPE[100] as typeof Value;
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const parentPtrValue = objectJson["parent"];
        const unpackedParentPtr = parentPtrValue != undefined ? _NodeReference.unpack(1, parentPtrValue, _session) as NodeReference : null;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const instancePtrValue = objectJson["instance"];
        const unpackedInstancePtr = instancePtrValue != undefined ? _NodeReference.unpack(1, instancePtrValue, _session) as NodeReference : null;
        const deletedAtValue = objectJson["deletedAt"];
        const unpackedDeletedAt = deletedAtValue != undefined ? Temporal.Instant.from(deletedAtValue).toZonedDateTimeISO('UTC') : null;
        const ownedByPtrValue = objectJson["ownedBy"];
        const unpackedOwnedByPtr = ownedByPtrValue != undefined ? _NodeReference.unpack(1, ownedByPtrValue, _session) as NodeReference : null;
        const unpackedCustomValues = {} as any;
        if (objectJson["customValues"] != undefined) {
          for (const [key, value] of Object.entries(objectJson["customValues"])) {
            unpackedCustomValues[key] = _Value.unpack(1, value as any, _session) as Value;
          }
        }
        const scriptPtrValue = objectJson["script"];
        const unpackedScriptPtr = scriptPtrValue != undefined ? _NodeReference.unpack(1, scriptPtrValue, _session) as NodeReference : null;
        const isExtensibleValue = objectJson["isExtensible"];
        const unpackedIsExtensible = isExtensibleValue != undefined ? isExtensibleValue : null;
        const sourcePtrValue = objectJson["source"];
        const unpackedSourcePtr = sourcePtrValue != undefined ? _NodeReference.unpack(1, sourcePtrValue, _session) as NodeReference : null;
        const keyValue = objectJson["key"];
        const unpackedKey = keyValue != undefined ? keyValue : null;
        return new (NODE_CLASS_BY_TYPE[100200] as typeof Handle)({
          parent: unpackedParentPtr,
          slug: objectJson["slug"],
          materialization: Materialization[objectJson["materialization"]] as any,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          instance: unpackedInstancePtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          updatedAt: Temporal.Instant.from(objectJson["updatedAt"]).toZonedDateTimeISO('UTC'),
          updatedEpoch: Number(objectJson["updatedEpoch"]),
          updatedBy: _NodeReference.unpack(1, objectJson["updatedBy"], _session) as NodeReference,
          deletedAt: unpackedDeletedAt,
          ownedBy: unpackedOwnedByPtr,
          name: objectJson["name"],
          orderKey: objectJson["orderKey"],
          customValues: unpackedCustomValues,
          script: unpackedScriptPtr,
          isExtensible: unpackedIsExtensible,
          source: unpackedSourcePtr,
          key: unpackedKey,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 100200)] = new HandleJsonEncoder();

    class OrganizationJsonEncoder implements _JsonObjectEncoder {
      packObject(object: Organization): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "ORGANIZATION";
        objectJson["id"] = object.id;
        if (object.parentPtr != null) {
          objectJson["parent"] = object.parentPtr.pack(1);
        }
        objectJson["space"] = object.spacePtr.pack(1);
        objectJson["materialization"] = Materialization[object.materialization];
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.instancePtr != null) {
          objectJson["instance"] = object.instancePtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["updatedAt"] = object.updatedAt.toString({ timeZoneName: 'never' });
        objectJson["updatedEpoch"] = Number(object.updatedEpoch);
        objectJson["updatedBy"] = object.updatedByPtr.pack(1);
        if (object.deletedAt != null) {
          objectJson["deletedAt"] = object.deletedAt.toString({ timeZoneName: 'never' });
        }
        if (object._ownedByPtr != null) {
          objectJson["ownedBy"] = object._ownedByPtr.pack(1);
        }
        objectJson["name"] = object._name;
        objectJson["orderKey"] = object.orderKey;
        if (Object.keys(object._customValues).length > 0) {
          const packedCustomValues: { [key: string]: any } = {} as any;
          for (const [key, value] of Object.entries(object._customValues)) {
            packedCustomValues[String(key)] = value.pack(1);
          }
          objectJson["customValues"] = packedCustomValues;
        }
        if (object._scriptPtr != null) {
          objectJson["script"] = object._scriptPtr.pack(1);
        }
        if (object.isExtensible != null) {
          objectJson["isExtensible"] = object.isExtensible;
        }
        if (object.sourcePtr != null) {
          objectJson["source"] = object.sourcePtr.pack(1);
        }
        if (object._key != null) {
          objectJson["key"] = object._key;
        }
        objectJson["slug"] = object._slug;
        objectJson["status"] = OrganizationStatus[object._status];
        if (object._handlePtr != null) {
          objectJson["handle"] = object._handlePtr.pack(1);
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): Organization {
        const _Value = STRUCT_CLASS_BY_TYPE[100] as typeof Value;
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const parentPtrValue = objectJson["parent"];
        const unpackedParentPtr = parentPtrValue != undefined ? _NodeReference.unpack(1, parentPtrValue, _session) as NodeReference : null;
        const handlePtrValue = objectJson["handle"];
        const unpackedHandlePtr = handlePtrValue != undefined ? _NodeReference.unpack(1, handlePtrValue, _session) as NodeReference : null;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const instancePtrValue = objectJson["instance"];
        const unpackedInstancePtr = instancePtrValue != undefined ? _NodeReference.unpack(1, instancePtrValue, _session) as NodeReference : null;
        const deletedAtValue = objectJson["deletedAt"];
        const unpackedDeletedAt = deletedAtValue != undefined ? Temporal.Instant.from(deletedAtValue).toZonedDateTimeISO('UTC') : null;
        const ownedByPtrValue = objectJson["ownedBy"];
        const unpackedOwnedByPtr = ownedByPtrValue != undefined ? _NodeReference.unpack(1, ownedByPtrValue, _session) as NodeReference : null;
        const unpackedCustomValues = {} as any;
        if (objectJson["customValues"] != undefined) {
          for (const [key, value] of Object.entries(objectJson["customValues"])) {
            unpackedCustomValues[key] = _Value.unpack(1, value as any, _session) as Value;
          }
        }
        const scriptPtrValue = objectJson["script"];
        const unpackedScriptPtr = scriptPtrValue != undefined ? _NodeReference.unpack(1, scriptPtrValue, _session) as NodeReference : null;
        const isExtensibleValue = objectJson["isExtensible"];
        const unpackedIsExtensible = isExtensibleValue != undefined ? isExtensibleValue : null;
        const sourcePtrValue = objectJson["source"];
        const unpackedSourcePtr = sourcePtrValue != undefined ? _NodeReference.unpack(1, sourcePtrValue, _session) as NodeReference : null;
        const keyValue = objectJson["key"];
        const unpackedKey = keyValue != undefined ? keyValue : null;
        return new (NODE_CLASS_BY_TYPE[122000] as typeof Organization)({
          parent: unpackedParentPtr,
          slug: objectJson["slug"],
          status: OrganizationStatus[objectJson["status"]] as any,
          handle: unpackedHandlePtr,
          materialization: Materialization[objectJson["materialization"]] as any,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          instance: unpackedInstancePtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          updatedAt: Temporal.Instant.from(objectJson["updatedAt"]).toZonedDateTimeISO('UTC'),
          updatedEpoch: Number(objectJson["updatedEpoch"]),
          updatedBy: _NodeReference.unpack(1, objectJson["updatedBy"], _session) as NodeReference,
          deletedAt: unpackedDeletedAt,
          ownedBy: unpackedOwnedByPtr,
          name: objectJson["name"],
          orderKey: objectJson["orderKey"],
          customValues: unpackedCustomValues,
          script: unpackedScriptPtr,
          isExtensible: unpackedIsExtensible,
          source: unpackedSourcePtr,
          key: unpackedKey,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 122000)] = new OrganizationJsonEncoder();

    class TeamJsonEncoder implements _JsonObjectEncoder {
      packObject(object: Team): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "TEAM";
        objectJson["id"] = object.id;
        if (object.parentPtr != null) {
          objectJson["parent"] = object.parentPtr.pack(1);
        }
        objectJson["space"] = object.spacePtr.pack(1);
        objectJson["materialization"] = Materialization[object.materialization];
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.instancePtr != null) {
          objectJson["instance"] = object.instancePtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["updatedAt"] = object.updatedAt.toString({ timeZoneName: 'never' });
        objectJson["updatedEpoch"] = Number(object.updatedEpoch);
        objectJson["updatedBy"] = object.updatedByPtr.pack(1);
        if (object.deletedAt != null) {
          objectJson["deletedAt"] = object.deletedAt.toString({ timeZoneName: 'never' });
        }
        if (object._ownedByPtr != null) {
          objectJson["ownedBy"] = object._ownedByPtr.pack(1);
        }
        objectJson["name"] = object._name;
        objectJson["orderKey"] = object.orderKey;
        if (Object.keys(object._customValues).length > 0) {
          const packedCustomValues: { [key: string]: any } = {} as any;
          for (const [key, value] of Object.entries(object._customValues)) {
            packedCustomValues[String(key)] = value.pack(1);
          }
          objectJson["customValues"] = packedCustomValues;
        }
        if (object._scriptPtr != null) {
          objectJson["script"] = object._scriptPtr.pack(1);
        }
        if (object.isExtensible != null) {
          objectJson["isExtensible"] = object.isExtensible;
        }
        if (object.sourcePtr != null) {
          objectJson["source"] = object.sourcePtr.pack(1);
        }
        if (object._key != null) {
          objectJson["key"] = object._key;
        }
        objectJson["slug"] = object._slug;
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): Team {
        const _Value = STRUCT_CLASS_BY_TYPE[100] as typeof Value;
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const parentPtrValue = objectJson["parent"];
        const unpackedParentPtr = parentPtrValue != undefined ? _NodeReference.unpack(1, parentPtrValue, _session) as NodeReference : null;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const instancePtrValue = objectJson["instance"];
        const unpackedInstancePtr = instancePtrValue != undefined ? _NodeReference.unpack(1, instancePtrValue, _session) as NodeReference : null;
        const deletedAtValue = objectJson["deletedAt"];
        const unpackedDeletedAt = deletedAtValue != undefined ? Temporal.Instant.from(deletedAtValue).toZonedDateTimeISO('UTC') : null;
        const ownedByPtrValue = objectJson["ownedBy"];
        const unpackedOwnedByPtr = ownedByPtrValue != undefined ? _NodeReference.unpack(1, ownedByPtrValue, _session) as NodeReference : null;
        const unpackedCustomValues = {} as any;
        if (objectJson["customValues"] != undefined) {
          for (const [key, value] of Object.entries(objectJson["customValues"])) {
            unpackedCustomValues[key] = _Value.unpack(1, value as any, _session) as Value;
          }
        }
        const scriptPtrValue = objectJson["script"];
        const unpackedScriptPtr = scriptPtrValue != undefined ? _NodeReference.unpack(1, scriptPtrValue, _session) as NodeReference : null;
        const isExtensibleValue = objectJson["isExtensible"];
        const unpackedIsExtensible = isExtensibleValue != undefined ? isExtensibleValue : null;
        const sourcePtrValue = objectJson["source"];
        const unpackedSourcePtr = sourcePtrValue != undefined ? _NodeReference.unpack(1, sourcePtrValue, _session) as NodeReference : null;
        const keyValue = objectJson["key"];
        const unpackedKey = keyValue != undefined ? keyValue : null;
        return new (NODE_CLASS_BY_TYPE[122100] as typeof Team)({
          parent: unpackedParentPtr,
          slug: objectJson["slug"],
          materialization: Materialization[objectJson["materialization"]] as any,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          instance: unpackedInstancePtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          updatedAt: Temporal.Instant.from(objectJson["updatedAt"]).toZonedDateTimeISO('UTC'),
          updatedEpoch: Number(objectJson["updatedEpoch"]),
          updatedBy: _NodeReference.unpack(1, objectJson["updatedBy"], _session) as NodeReference,
          deletedAt: unpackedDeletedAt,
          ownedBy: unpackedOwnedByPtr,
          name: objectJson["name"],
          orderKey: objectJson["orderKey"],
          customValues: unpackedCustomValues,
          script: unpackedScriptPtr,
          isExtensible: unpackedIsExtensible,
          source: unpackedSourcePtr,
          key: unpackedKey,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 122100)] = new TeamJsonEncoder();

    class UserJsonEncoder implements _JsonObjectEncoder {
      packObject(object: User): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "USER";
        objectJson["id"] = object.id;
        if (object.parentPtr != null) {
          objectJson["parent"] = object.parentPtr.pack(1);
        }
        objectJson["space"] = object.spacePtr.pack(1);
        objectJson["materialization"] = Materialization[object.materialization];
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        objectJson["branch"] = object.branchPtr.pack(1);
        objectJson["snapshot"] = object.snapshotPtr.pack(1);
        if (object.precededByPtr != null) {
          objectJson["precededBy"] = object.precededByPtr.pack(1);
        }
        if (object.instancePtr != null) {
          objectJson["instance"] = object.instancePtr.pack(1);
        }
        objectJson["createdAt"] = object.createdAt.toString({ timeZoneName: 'never' });
        objectJson["createdEpoch"] = Number(object.createdEpoch);
        objectJson["createdBy"] = object.createdByPtr.pack(1);
        objectJson["updatedAt"] = object.updatedAt.toString({ timeZoneName: 'never' });
        objectJson["updatedEpoch"] = Number(object.updatedEpoch);
        objectJson["updatedBy"] = object.updatedByPtr.pack(1);
        if (object.deletedAt != null) {
          objectJson["deletedAt"] = object.deletedAt.toString({ timeZoneName: 'never' });
        }
        if (object._ownedByPtr != null) {
          objectJson["ownedBy"] = object._ownedByPtr.pack(1);
        }
        objectJson["name"] = object._name;
        objectJson["orderKey"] = object.orderKey;
        if (Object.keys(object._customValues).length > 0) {
          const packedCustomValues: { [key: string]: any } = {} as any;
          for (const [key, value] of Object.entries(object._customValues)) {
            packedCustomValues[String(key)] = value.pack(1);
          }
          objectJson["customValues"] = packedCustomValues;
        }
        if (object._scriptPtr != null) {
          objectJson["script"] = object._scriptPtr.pack(1);
        }
        if (object.isExtensible != null) {
          objectJson["isExtensible"] = object.isExtensible;
        }
        if (object.sourcePtr != null) {
          objectJson["source"] = object.sourcePtr.pack(1);
        }
        if (object._key != null) {
          objectJson["key"] = object._key;
        }
        objectJson["slug"] = object._slug;
        objectJson["status"] = UserStatus[object._status];
        if (object._lastLoggedInAt != null) {
          objectJson["lastLoggedInAt"] = object._lastLoggedInAt.toString({ timeZoneName: 'never' });
        }
        objectJson["isStaff"] = object._isStaff;
        if (object._handlePtr != null) {
          objectJson["handle"] = object._handlePtr.pack(1);
        }
        if (object._email != null) {
          objectJson["email"] = object._email;
        }
        if (object._passwordSalt != null) {
          objectJson["passwordSalt"] = base64Encode(object._passwordSalt);
        }
        if (object._passwordHash != null) {
          objectJson["passwordHash"] = base64Encode(object._passwordHash);
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): User {
        const _Value = STRUCT_CLASS_BY_TYPE[100] as typeof Value;
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const parentPtrValue = objectJson["parent"];
        const unpackedParentPtr = parentPtrValue != undefined ? _NodeReference.unpack(1, parentPtrValue, _session) as NodeReference : null;
        const lastLoggedInAtValue = objectJson["lastLoggedInAt"];
        const unpackedLastLoggedInAt = lastLoggedInAtValue != undefined ? Temporal.Instant.from(lastLoggedInAtValue).toZonedDateTimeISO('UTC') : null;
        const handlePtrValue = objectJson["handle"];
        const unpackedHandlePtr = handlePtrValue != undefined ? _NodeReference.unpack(1, handlePtrValue, _session) as NodeReference : null;
        const emailValue = objectJson["email"];
        const unpackedEmail = emailValue != undefined ? emailValue : null;
        const passwordSaltValue = objectJson["passwordSalt"];
        const unpackedPasswordSalt = passwordSaltValue != undefined ? base64Decode(passwordSaltValue) : null;
        const passwordHashValue = objectJson["passwordHash"];
        const unpackedPasswordHash = passwordHashValue != undefined ? base64Decode(passwordHashValue) : null;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        const precededByPtrValue = objectJson["precededBy"];
        const unpackedPrecededByPtr = precededByPtrValue != undefined ? _NodeReference.unpack(1, precededByPtrValue, _session) as NodeReference : null;
        const instancePtrValue = objectJson["instance"];
        const unpackedInstancePtr = instancePtrValue != undefined ? _NodeReference.unpack(1, instancePtrValue, _session) as NodeReference : null;
        const deletedAtValue = objectJson["deletedAt"];
        const unpackedDeletedAt = deletedAtValue != undefined ? Temporal.Instant.from(deletedAtValue).toZonedDateTimeISO('UTC') : null;
        const ownedByPtrValue = objectJson["ownedBy"];
        const unpackedOwnedByPtr = ownedByPtrValue != undefined ? _NodeReference.unpack(1, ownedByPtrValue, _session) as NodeReference : null;
        const unpackedCustomValues = {} as any;
        if (objectJson["customValues"] != undefined) {
          for (const [key, value] of Object.entries(objectJson["customValues"])) {
            unpackedCustomValues[key] = _Value.unpack(1, value as any, _session) as Value;
          }
        }
        const scriptPtrValue = objectJson["script"];
        const unpackedScriptPtr = scriptPtrValue != undefined ? _NodeReference.unpack(1, scriptPtrValue, _session) as NodeReference : null;
        const isExtensibleValue = objectJson["isExtensible"];
        const unpackedIsExtensible = isExtensibleValue != undefined ? isExtensibleValue : null;
        const sourcePtrValue = objectJson["source"];
        const unpackedSourcePtr = sourcePtrValue != undefined ? _NodeReference.unpack(1, sourcePtrValue, _session) as NodeReference : null;
        const keyValue = objectJson["key"];
        const unpackedKey = keyValue != undefined ? keyValue : null;
        return new (NODE_CLASS_BY_TYPE[121000] as typeof User)({
          parent: unpackedParentPtr,
          slug: objectJson["slug"],
          status: UserStatus[objectJson["status"]] as any,
          lastLoggedInAt: unpackedLastLoggedInAt,
          isStaff: objectJson["isStaff"],
          handle: unpackedHandlePtr,
          email: unpackedEmail,
          passwordSalt: unpackedPasswordSalt,
          passwordHash: unpackedPasswordHash,
          materialization: Materialization[objectJson["materialization"]] as any,
          definition: unpackedDefinitionPtr,
          branch: _NodeReference.unpack(1, objectJson["branch"], _session) as NodeReference,
          snapshot: _NodeReference.unpack(1, objectJson["snapshot"], _session) as NodeReference,
          precededBy: unpackedPrecededByPtr,
          instance: unpackedInstancePtr,
          createdAt: Temporal.Instant.from(objectJson["createdAt"]).toZonedDateTimeISO('UTC'),
          createdEpoch: Number(objectJson["createdEpoch"]),
          createdBy: _NodeReference.unpack(1, objectJson["createdBy"], _session) as NodeReference,
          updatedAt: Temporal.Instant.from(objectJson["updatedAt"]).toZonedDateTimeISO('UTC'),
          updatedEpoch: Number(objectJson["updatedEpoch"]),
          updatedBy: _NodeReference.unpack(1, objectJson["updatedBy"], _session) as NodeReference,
          deletedAt: unpackedDeletedAt,
          ownedBy: unpackedOwnedByPtr,
          name: objectJson["name"],
          orderKey: objectJson["orderKey"],
          customValues: unpackedCustomValues,
          script: unpackedScriptPtr,
          isExtensible: unpackedIsExtensible,
          source: unpackedSourcePtr,
          key: unpackedKey,
          id: objectJson["id"],
          space: _NodeReference.unpack(1, objectJson["space"], _session) as NodeReference,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(1, 121000)] = new UserJsonEncoder();

    class NodeDefinitionReferenceJsonEncoder implements _JsonObjectEncoder {
      packObject(object: NodeDefinitionReference): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "NODE_DEFINITION_REFERENCE";
        objectJson["type"] = NodeDefinitionType[object.type];
        objectJson["nodeType"] = NodeType[object.nodeType];
        if (object.definitionPtr != null) {
          objectJson["definition"] = object.definitionPtr.pack(1);
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): NodeDefinitionReference {
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const definitionPtrValue = objectJson["definition"];
        const unpackedDefinitionPtr = definitionPtrValue != undefined ? _NodeReference.unpack(1, definitionPtrValue, _session) as NodeReference : null;
        return new (STRUCT_CLASS_BY_TYPE[13] as typeof NodeDefinitionReference)({
          type: NodeDefinitionType[objectJson["type"]] as any,
          nodeType: NodeType[objectJson["nodeType"]] as any,
          definition: unpackedDefinitionPtr,
          _packedCache: [{ encoding: 1, isBytes: false, packed: objectJson }],
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(2, 13)] = new NodeDefinitionReferenceJsonEncoder();

    class ObjectDefinitionReferenceJsonEncoder implements _JsonObjectEncoder {
      packObject(object: ObjectDefinitionReference): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "OBJECT_DEFINITION_REFERENCE";
        objectJson["type"] = ObjectDefinitionType[object.type];
        if (object.nodeType != null) {
          objectJson["nodeType"] = NodeType[object.nodeType];
        }
        if (object.traitType != null) {
          objectJson["traitType"] = TraitType[object.traitType];
        }
        if (object.structType != null) {
          objectJson["structType"] = StructType[object.structType];
        }
        if (object.customDefinitionPtr != null) {
          objectJson["customDefinition"] = object.customDefinitionPtr.pack(1);
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): ObjectDefinitionReference {
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const nodeTypeValue = objectJson["nodeType"];
        const unpackedNodeType = nodeTypeValue != undefined ? NodeType[nodeTypeValue] as any : null;
        const traitTypeValue = objectJson["traitType"];
        const unpackedTraitType = traitTypeValue != undefined ? TraitType[traitTypeValue] as any : null;
        const structTypeValue = objectJson["structType"];
        const unpackedStructType = structTypeValue != undefined ? StructType[structTypeValue] as any : null;
        const customDefinitionPtrValue = objectJson["customDefinition"];
        const unpackedCustomDefinitionPtr = customDefinitionPtrValue != undefined ? _NodeReference.unpack(1, customDefinitionPtrValue, _session) as NodeReference : null;
        return new (STRUCT_CLASS_BY_TYPE[11] as typeof ObjectDefinitionReference)({
          type: ObjectDefinitionType[objectJson["type"]] as any,
          nodeType: unpackedNodeType,
          traitType: unpackedTraitType,
          structType: unpackedStructType,
          customDefinition: unpackedCustomDefinitionPtr,
          _packedCache: [{ encoding: 1, isBytes: false, packed: objectJson }],
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(2, 11)] = new ObjectDefinitionReferenceJsonEncoder();

    class StructDefinitionReferenceJsonEncoder implements _JsonObjectEncoder {
      packObject(object: StructDefinitionReference): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "STRUCT_DEFINITION_REFERENCE";
        objectJson["type"] = StructDefinitionType[object.type];
        if (object.structType != null) {
          objectJson["structType"] = StructType[object.structType];
        }
        objectJson["definition"] = object.definitionPtr.pack(1);
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): StructDefinitionReference {
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const structTypeValue = objectJson["structType"];
        const unpackedStructType = structTypeValue != undefined ? StructType[structTypeValue] as any : null;
        return new (STRUCT_CLASS_BY_TYPE[16] as typeof StructDefinitionReference)({
          type: StructDefinitionType[objectJson["type"]] as any,
          structType: unpackedStructType,
          definition: _NodeReference.unpack(1, objectJson["definition"], _session) as NodeReference,
          _packedCache: [{ encoding: 1, isBytes: false, packed: objectJson }],
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(2, 16)] = new StructDefinitionReferenceJsonEncoder();

    class PropertyReferenceJsonEncoder implements _JsonObjectEncoder {
      packObject(object: PropertyReference): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "PROPERTY_REFERENCE";
        objectJson["type"] = PropertyReferenceType[object.type];
        if (object.nodeType != null) {
          objectJson["nodeType"] = NodeType[object.nodeType];
        }
        if (object.traitType != null) {
          objectJson["traitType"] = TraitType[object.traitType];
        }
        if (object.structType != null) {
          objectJson["structType"] = StructType[object.structType];
        }
        if (object.id != null) {
          objectJson["id"] = Number(object.id);
        }
        if (object.customPropertyPtr != null) {
          objectJson["customProperty"] = object.customPropertyPtr.pack(1);
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): PropertyReference {
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const nodeTypeValue = objectJson["nodeType"];
        const unpackedNodeType = nodeTypeValue != undefined ? NodeType[nodeTypeValue] as any : null;
        const traitTypeValue = objectJson["traitType"];
        const unpackedTraitType = traitTypeValue != undefined ? TraitType[traitTypeValue] as any : null;
        const structTypeValue = objectJson["structType"];
        const unpackedStructType = structTypeValue != undefined ? StructType[structTypeValue] as any : null;
        const idValue = objectJson["id"];
        const unpackedId = idValue != undefined ? Number(idValue) : null;
        const customPropertyPtrValue = objectJson["customProperty"];
        const unpackedCustomPropertyPtr = customPropertyPtrValue != undefined ? _NodeReference.unpack(1, customPropertyPtrValue, _session) as NodeReference : null;
        return new (STRUCT_CLASS_BY_TYPE[1001] as typeof PropertyReference)({
          type: PropertyReferenceType[objectJson["type"]] as any,
          nodeType: unpackedNodeType,
          traitType: unpackedTraitType,
          structType: unpackedStructType,
          id: unpackedId,
          customProperty: unpackedCustomPropertyPtr,
          _packedCache: [{ encoding: 1, isBytes: false, packed: objectJson }],
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(2, 1001)] = new PropertyReferenceJsonEncoder();

    class NodeReferenceJsonEncoder implements _JsonObjectEncoder {
      packObject(object: NodeReference): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "NODE_REFERENCE";
        objectJson["type"] = NodeType[object.type];
        objectJson["id"] = object.id;
        objectJson["spaceId"] = object.spaceId;
        if (object.definitionId != null) {
          objectJson["definitionId"] = object.definitionId;
        }
        objectJson["branchId"] = object.branchId;
        objectJson["snapshotId"] = object.snapshotId;
        if (object.storeKey != null) {
          objectJson["storeKey"] = GraphKey[object.storeKey];
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): NodeReference {

        const definitionIdValue = objectJson["definitionId"];
        const unpackedDefinitionId = definitionIdValue != undefined ? definitionIdValue : null;
        const storeKeyValue = objectJson["storeKey"];
        const unpackedStoreKey = storeKeyValue != undefined ? GraphKey[storeKeyValue] as any : null;
        return new (STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference)({
          type: NodeType[objectJson["type"]] as any,
          id: objectJson["id"],
          spaceId: objectJson["spaceId"],
          definitionId: unpackedDefinitionId,
          branchId: objectJson["branchId"],
          snapshotId: objectJson["snapshotId"],
          storeKey: unpackedStoreKey,
          _packedCache: [{ encoding: 1, isBytes: false, packed: objectJson }],
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(2, 1000)] = new NodeReferenceJsonEncoder();

    class NodeDefinitionJsonEncoder implements _JsonObjectEncoder {
      packObject(object: NodeDefinition): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "NODE_DEFINITION";
        objectJson["id"] = Number(object.id);
        objectJson["type"] = NodeType[object.type];
        objectJson["name"] = object.name;
        if (object.icon != null) {
          objectJson["icon"] = object.icon.pack(1);
        }
        if (object.description != null) {
          objectJson["description"] = object.description;
        }
        if (object.taggings.length > 0) {
          const packedTaggings: any[] = [];
          for (const item of object.taggings) {
            packedTaggings.push(Number(item));
          }
          objectJson["taggings"] = packedTaggings;
        }
        objectJson["isAbstract"] = object.isAbstract;
        objectJson["isExtensible"] = object.isExtensible;
        objectJson["isFinal"] = object.isFinal;
        objectJson["isFrozen"] = object.isFrozen;
        if (object.properties.length > 0) {
          const packedProperties: any[] = [];
          for (const item of object.properties) {
            packedProperties.push(item.pack(1));
          }
          objectJson["properties"] = packedProperties;
        }
        if (object.indexes.length > 0) {
          const packedIndexes: any[] = [];
          for (const item of object.indexes) {
            packedIndexes.push(item.pack(1));
          }
          objectJson["indexes"] = packedIndexes;
        }
        if (object.constraints.length > 0) {
          const packedConstraints: any[] = [];
          for (const item of object.constraints) {
            packedConstraints.push(item.pack(1));
          }
          objectJson["constraints"] = packedConstraints;
        }
        if (object.permissions.length > 0) {
          const packedPermissions: any[] = [];
          for (const item of object.permissions) {
            packedPermissions.push(item.pack(1));
          }
          objectJson["permissions"] = packedPermissions;
        }
        if (object.methods.length > 0) {
          const packedMethods: any[] = [];
          for (const item of object.methods) {
            packedMethods.push(item.pack(1));
          }
          objectJson["methods"] = packedMethods;
        }
        if (object.actions.length > 0) {
          const packedActions: any[] = [];
          for (const item of object.actions) {
            packedActions.push(item.pack(1));
          }
          objectJson["actions"] = packedActions;
        }
        if (object.constants.length > 0) {
          const packedConstants: any[] = [];
          for (const item of object.constants) {
            packedConstants.push(item.pack(1));
          }
          objectJson["constants"] = packedConstants;
        }
        if (object.baseType != null) {
          objectJson["baseType"] = NodeType[object.baseType];
        }
        if (object.extendedBy.length > 0) {
          const packedExtendedBy: any[] = [];
          for (const item of object.extendedBy) {
            packedExtendedBy.push(NodeType[item]);
          }
          objectJson["extendedBy"] = packedExtendedBy;
        }
        if (object.inherits.length > 0) {
          const packedInherits: any[] = [];
          for (const item of object.inherits) {
            packedInherits.push(NodeType[item]);
          }
          objectJson["inherits"] = packedInherits;
        }
        if (object.inheritedBy.length > 0) {
          const packedInheritedBy: any[] = [];
          for (const item of object.inheritedBy) {
            packedInheritedBy.push(NodeType[item]);
          }
          objectJson["inheritedBy"] = packedInheritedBy;
        }
        if (object.traits.length > 0) {
          const packedTraits: any[] = [];
          for (const item of object.traits) {
            packedTraits.push(TraitType[item]);
          }
          objectJson["traits"] = packedTraits;
        }
        if (object.selfTraits.length > 0) {
          const packedSelfTraits: any[] = [];
          for (const item of object.selfTraits) {
            packedSelfTraits.push(TraitType[item]);
          }
          objectJson["selfTraits"] = packedSelfTraits;
        }
        if (object.eventTypes.length > 0) {
          const packedEventTypes: any[] = [];
          for (const item of object.eventTypes) {
            packedEventTypes.push(NodeType[item]);
          }
          objectJson["eventTypes"] = packedEventTypes;
        }
        if (object.selfEventTypes.length > 0) {
          const packedSelfEventTypes: any[] = [];
          for (const item of object.selfEventTypes) {
            packedSelfEventTypes.push(NodeType[item]);
          }
          objectJson["selfEventTypes"] = packedSelfEventTypes;
        }
        if (object.enumTypes.length > 0) {
          const packedEnumTypes: any[] = [];
          for (const item of object.enumTypes) {
            packedEnumTypes.push(EnumType[item]);
          }
          objectJson["enumTypes"] = packedEnumTypes;
        }
        if (object.selfEnumTypes.length > 0) {
          const packedSelfEnumTypes: any[] = [];
          for (const item of object.selfEnumTypes) {
            packedSelfEnumTypes.push(EnumType[item]);
          }
          objectJson["selfEnumTypes"] = packedSelfEnumTypes;
        }
        if (object.parentTypes.length > 0) {
          const packedParentTypes: any[] = [];
          for (const item of object.parentTypes) {
            packedParentTypes.push(NodeType[item]);
          }
          objectJson["parentTypes"] = packedParentTypes;
        }
        if (object.childTypes.length > 0) {
          const packedChildTypes: any[] = [];
          for (const item of object.childTypes) {
            packedChildTypes.push(NodeType[item]);
          }
          objectJson["childTypes"] = packedChildTypes;
        }
        if (object.ancestorTypes.length > 0) {
          const packedAncestorTypes: any[] = [];
          for (const item of object.ancestorTypes) {
            packedAncestorTypes.push(NodeType[item]);
          }
          objectJson["ancestorTypes"] = packedAncestorTypes;
        }
        if (object.descendantTypes.length > 0) {
          const packedDescendantTypes: any[] = [];
          for (const item of object.descendantTypes) {
            packedDescendantTypes.push(NodeType[item]);
          }
          objectJson["descendantTypes"] = packedDescendantTypes;
        }
        if (object.expectedParentTypes.length > 0) {
          const packedExpectedParentTypes: any[] = [];
          for (const item of object.expectedParentTypes) {
            packedExpectedParentTypes.push(NodeType[item]);
          }
          objectJson["expectedParentTypes"] = packedExpectedParentTypes;
        }
        if (object.expectedChildTypes.length > 0) {
          const packedExpectedChildTypes: any[] = [];
          for (const item of object.expectedChildTypes) {
            packedExpectedChildTypes.push(NodeType[item]);
          }
          objectJson["expectedChildTypes"] = packedExpectedChildTypes;
        }
        if (object.expectedAncestorTypes.length > 0) {
          const packedExpectedAncestorTypes: any[] = [];
          for (const item of object.expectedAncestorTypes) {
            packedExpectedAncestorTypes.push(NodeType[item]);
          }
          objectJson["expectedAncestorTypes"] = packedExpectedAncestorTypes;
        }
        if (object.expectedDescendantTypes.length > 0) {
          const packedExpectedDescendantTypes: any[] = [];
          for (const item of object.expectedDescendantTypes) {
            packedExpectedDescendantTypes.push(NodeType[item]);
          }
          objectJson["expectedDescendantTypes"] = packedExpectedDescendantTypes;
        }
        if (object.domain != null) {
          objectJson["domain"] = GraphDomain[object.domain];
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): NodeDefinition {
        const _PropertyDefinition = STRUCT_CLASS_BY_TYPE[18] as typeof PropertyDefinition;
        const _ConstantDefinition = STRUCT_CLASS_BY_TYPE[19] as typeof ConstantDefinition;
        const _IndexDefinition = STRUCT_CLASS_BY_TYPE[30100] as typeof IndexDefinition;
        const _ConstraintDefinition = STRUCT_CLASS_BY_TYPE[30200] as typeof ConstraintDefinition;
        const _MethodDefinition = STRUCT_CLASS_BY_TYPE[40000] as typeof MethodDefinition;
        const _ActionDefinition = STRUCT_CLASS_BY_TYPE[40100] as typeof ActionDefinition;
        const _PermissionDefinition = STRUCT_CLASS_BY_TYPE[50000] as typeof PermissionDefinition;
        const _Icon = STRUCT_CLASS_BY_TYPE[400031] as typeof Icon;
        const unpackedProperties: any[] = [];
        if (objectJson["properties"] != undefined) {
          for (const item of objectJson["properties"]) {
            unpackedProperties.push(_PropertyDefinition.unpack(1, item, _session) as PropertyDefinition)
          }
        }
        const unpackedIndexes: any[] = [];
        if (objectJson["indexes"] != undefined) {
          for (const item of objectJson["indexes"]) {
            unpackedIndexes.push(_IndexDefinition.unpack(1, item, _session) as IndexDefinition)
          }
        }
        const unpackedConstraints: any[] = [];
        if (objectJson["constraints"] != undefined) {
          for (const item of objectJson["constraints"]) {
            unpackedConstraints.push(_ConstraintDefinition.unpack(1, item, _session) as ConstraintDefinition)
          }
        }
        const unpackedPermissions: any[] = [];
        if (objectJson["permissions"] != undefined) {
          for (const item of objectJson["permissions"]) {
            unpackedPermissions.push(_PermissionDefinition.unpack(1, item, _session) as PermissionDefinition)
          }
        }
        const unpackedMethods: any[] = [];
        if (objectJson["methods"] != undefined) {
          for (const item of objectJson["methods"]) {
            unpackedMethods.push(_MethodDefinition.unpack(1, item, _session) as MethodDefinition)
          }
        }
        const unpackedActions: any[] = [];
        if (objectJson["actions"] != undefined) {
          for (const item of objectJson["actions"]) {
            unpackedActions.push(_ActionDefinition.unpack(1, item, _session) as ActionDefinition)
          }
        }
        const unpackedConstants: any[] = [];
        if (objectJson["constants"] != undefined) {
          for (const item of objectJson["constants"]) {
            unpackedConstants.push(_ConstantDefinition.unpack(1, item, _session) as ConstantDefinition)
          }
        }
        const baseTypeValue = objectJson["baseType"];
        const unpackedBaseType = baseTypeValue != undefined ? NodeType[baseTypeValue] as any : null;
        const unpackedExtendedBy: any[] = [];
        if (objectJson["extendedBy"] != undefined) {
          for (const item of objectJson["extendedBy"]) {
            unpackedExtendedBy.push(NodeType[item] as any)
          }
        }
        const unpackedInherits: any[] = [];
        if (objectJson["inherits"] != undefined) {
          for (const item of objectJson["inherits"]) {
            unpackedInherits.push(NodeType[item] as any)
          }
        }
        const unpackedInheritedBy: any[] = [];
        if (objectJson["inheritedBy"] != undefined) {
          for (const item of objectJson["inheritedBy"]) {
            unpackedInheritedBy.push(NodeType[item] as any)
          }
        }
        const unpackedTraits: any[] = [];
        if (objectJson["traits"] != undefined) {
          for (const item of objectJson["traits"]) {
            unpackedTraits.push(TraitType[item] as any)
          }
        }
        const unpackedSelfTraits: any[] = [];
        if (objectJson["selfTraits"] != undefined) {
          for (const item of objectJson["selfTraits"]) {
            unpackedSelfTraits.push(TraitType[item] as any)
          }
        }
        const unpackedEventTypes: any[] = [];
        if (objectJson["eventTypes"] != undefined) {
          for (const item of objectJson["eventTypes"]) {
            unpackedEventTypes.push(NodeType[item] as any)
          }
        }
        const unpackedSelfEventTypes: any[] = [];
        if (objectJson["selfEventTypes"] != undefined) {
          for (const item of objectJson["selfEventTypes"]) {
            unpackedSelfEventTypes.push(NodeType[item] as any)
          }
        }
        const unpackedEnumTypes: any[] = [];
        if (objectJson["enumTypes"] != undefined) {
          for (const item of objectJson["enumTypes"]) {
            unpackedEnumTypes.push(EnumType[item] as any)
          }
        }
        const unpackedSelfEnumTypes: any[] = [];
        if (objectJson["selfEnumTypes"] != undefined) {
          for (const item of objectJson["selfEnumTypes"]) {
            unpackedSelfEnumTypes.push(EnumType[item] as any)
          }
        }
        const unpackedParentTypes: any[] = [];
        if (objectJson["parentTypes"] != undefined) {
          for (const item of objectJson["parentTypes"]) {
            unpackedParentTypes.push(NodeType[item] as any)
          }
        }
        const unpackedChildTypes: any[] = [];
        if (objectJson["childTypes"] != undefined) {
          for (const item of objectJson["childTypes"]) {
            unpackedChildTypes.push(NodeType[item] as any)
          }
        }
        const unpackedAncestorTypes: any[] = [];
        if (objectJson["ancestorTypes"] != undefined) {
          for (const item of objectJson["ancestorTypes"]) {
            unpackedAncestorTypes.push(NodeType[item] as any)
          }
        }
        const unpackedDescendantTypes: any[] = [];
        if (objectJson["descendantTypes"] != undefined) {
          for (const item of objectJson["descendantTypes"]) {
            unpackedDescendantTypes.push(NodeType[item] as any)
          }
        }
        const unpackedExpectedParentTypes: any[] = [];
        if (objectJson["expectedParentTypes"] != undefined) {
          for (const item of objectJson["expectedParentTypes"]) {
            unpackedExpectedParentTypes.push(NodeType[item] as any)
          }
        }
        const unpackedExpectedChildTypes: any[] = [];
        if (objectJson["expectedChildTypes"] != undefined) {
          for (const item of objectJson["expectedChildTypes"]) {
            unpackedExpectedChildTypes.push(NodeType[item] as any)
          }
        }
        const unpackedExpectedAncestorTypes: any[] = [];
        if (objectJson["expectedAncestorTypes"] != undefined) {
          for (const item of objectJson["expectedAncestorTypes"]) {
            unpackedExpectedAncestorTypes.push(NodeType[item] as any)
          }
        }
        const unpackedExpectedDescendantTypes: any[] = [];
        if (objectJson["expectedDescendantTypes"] != undefined) {
          for (const item of objectJson["expectedDescendantTypes"]) {
            unpackedExpectedDescendantTypes.push(NodeType[item] as any)
          }
        }
        const domainValue = objectJson["domain"];
        const unpackedDomain = domainValue != undefined ? GraphDomain[domainValue] as any : null;
        const iconValue = objectJson["icon"];
        const unpackedIcon = iconValue != undefined ? _Icon.unpack(1, iconValue, _session) as Icon : null;
        const descriptionValue = objectJson["description"];
        const unpackedDescription = descriptionValue != undefined ? descriptionValue : null;
        const unpackedTaggings: any[] = [];
        if (objectJson["taggings"] != undefined) {
          for (const item of objectJson["taggings"]) {
            unpackedTaggings.push(Number(item))
          }
        }
        return new (STRUCT_CLASS_BY_TYPE[12] as typeof NodeDefinition)({
          type: NodeType[objectJson["type"]] as any,
          isAbstract: objectJson["isAbstract"],
          isExtensible: objectJson["isExtensible"],
          isFinal: objectJson["isFinal"],
          isFrozen: objectJson["isFrozen"],
          properties: unpackedProperties,
          indexes: unpackedIndexes,
          constraints: unpackedConstraints,
          permissions: unpackedPermissions,
          methods: unpackedMethods,
          actions: unpackedActions,
          constants: unpackedConstants,
          baseType: unpackedBaseType,
          extendedBy: unpackedExtendedBy,
          inherits: unpackedInherits,
          inheritedBy: unpackedInheritedBy,
          traits: unpackedTraits,
          selfTraits: unpackedSelfTraits,
          eventTypes: unpackedEventTypes,
          selfEventTypes: unpackedSelfEventTypes,
          enumTypes: unpackedEnumTypes,
          selfEnumTypes: unpackedSelfEnumTypes,
          parentTypes: unpackedParentTypes,
          childTypes: unpackedChildTypes,
          ancestorTypes: unpackedAncestorTypes,
          descendantTypes: unpackedDescendantTypes,
          expectedParentTypes: unpackedExpectedParentTypes,
          expectedChildTypes: unpackedExpectedChildTypes,
          expectedAncestorTypes: unpackedExpectedAncestorTypes,
          expectedDescendantTypes: unpackedExpectedDescendantTypes,
          domain: unpackedDomain,
          id: Number(objectJson["id"]),
          name: objectJson["name"],
          icon: unpackedIcon,
          description: unpackedDescription,
          taggings: unpackedTaggings,
          _packedCache: [{ encoding: 1, isBytes: false, packed: objectJson }],
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(2, 12)] = new NodeDefinitionJsonEncoder();

    class TraitDefinitionJsonEncoder implements _JsonObjectEncoder {
      packObject(object: TraitDefinition): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "TRAIT_DEFINITION";
        objectJson["id"] = Number(object.id);
        objectJson["type"] = TraitType[object.type];
        objectJson["name"] = object.name;
        if (object.icon != null) {
          objectJson["icon"] = object.icon.pack(1);
        }
        if (object.description != null) {
          objectJson["description"] = object.description;
        }
        if (object.taggings.length > 0) {
          const packedTaggings: any[] = [];
          for (const item of object.taggings) {
            packedTaggings.push(Number(item));
          }
          objectJson["taggings"] = packedTaggings;
        }
        objectJson["alias"] = object.alias;
        objectJson["isExtensible"] = object.isExtensible;
        if (object.permissions.length > 0) {
          const packedPermissions: any[] = [];
          for (const item of object.permissions) {
            packedPermissions.push(item.pack(1));
          }
          objectJson["permissions"] = packedPermissions;
        }
        if (object.selfTraits.length > 0) {
          const packedSelfTraits: any[] = [];
          for (const item of object.selfTraits) {
            packedSelfTraits.push(TraitType[item]);
          }
          objectJson["selfTraits"] = packedSelfTraits;
        }
        if (object.traits.length > 0) {
          const packedTraits: any[] = [];
          for (const item of object.traits) {
            packedTraits.push(TraitType[item]);
          }
          objectJson["traits"] = packedTraits;
        }
        if (object.eventTypes.length > 0) {
          const packedEventTypes: any[] = [];
          for (const item of object.eventTypes) {
            packedEventTypes.push(NodeType[item]);
          }
          objectJson["eventTypes"] = packedEventTypes;
        }
        if (object.selfEventTypes.length > 0) {
          const packedSelfEventTypes: any[] = [];
          for (const item of object.selfEventTypes) {
            packedSelfEventTypes.push(NodeType[item]);
          }
          objectJson["selfEventTypes"] = packedSelfEventTypes;
        }
        if (object.enumTypes.length > 0) {
          const packedEnumTypes: any[] = [];
          for (const item of object.enumTypes) {
            packedEnumTypes.push(EnumType[item]);
          }
          objectJson["enumTypes"] = packedEnumTypes;
        }
        if (object.selfEnumTypes.length > 0) {
          const packedSelfEnumTypes: any[] = [];
          for (const item of object.selfEnumTypes) {
            packedSelfEnumTypes.push(EnumType[item]);
          }
          objectJson["selfEnumTypes"] = packedSelfEnumTypes;
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): TraitDefinition {
        const _PermissionDefinition = STRUCT_CLASS_BY_TYPE[50000] as typeof PermissionDefinition;
        const _Icon = STRUCT_CLASS_BY_TYPE[400031] as typeof Icon;
        const unpackedPermissions: any[] = [];
        if (objectJson["permissions"] != undefined) {
          for (const item of objectJson["permissions"]) {
            unpackedPermissions.push(_PermissionDefinition.unpack(1, item, _session) as PermissionDefinition)
          }
        }
        const unpackedSelfTraits: any[] = [];
        if (objectJson["selfTraits"] != undefined) {
          for (const item of objectJson["selfTraits"]) {
            unpackedSelfTraits.push(TraitType[item] as any)
          }
        }
        const unpackedTraits: any[] = [];
        if (objectJson["traits"] != undefined) {
          for (const item of objectJson["traits"]) {
            unpackedTraits.push(TraitType[item] as any)
          }
        }
        const unpackedEventTypes: any[] = [];
        if (objectJson["eventTypes"] != undefined) {
          for (const item of objectJson["eventTypes"]) {
            unpackedEventTypes.push(NodeType[item] as any)
          }
        }
        const unpackedSelfEventTypes: any[] = [];
        if (objectJson["selfEventTypes"] != undefined) {
          for (const item of objectJson["selfEventTypes"]) {
            unpackedSelfEventTypes.push(NodeType[item] as any)
          }
        }
        const unpackedEnumTypes: any[] = [];
        if (objectJson["enumTypes"] != undefined) {
          for (const item of objectJson["enumTypes"]) {
            unpackedEnumTypes.push(EnumType[item] as any)
          }
        }
        const unpackedSelfEnumTypes: any[] = [];
        if (objectJson["selfEnumTypes"] != undefined) {
          for (const item of objectJson["selfEnumTypes"]) {
            unpackedSelfEnumTypes.push(EnumType[item] as any)
          }
        }
        const iconValue = objectJson["icon"];
        const unpackedIcon = iconValue != undefined ? _Icon.unpack(1, iconValue, _session) as Icon : null;
        const descriptionValue = objectJson["description"];
        const unpackedDescription = descriptionValue != undefined ? descriptionValue : null;
        const unpackedTaggings: any[] = [];
        if (objectJson["taggings"] != undefined) {
          for (const item of objectJson["taggings"]) {
            unpackedTaggings.push(Number(item))
          }
        }
        return new (STRUCT_CLASS_BY_TYPE[14] as typeof TraitDefinition)({
          type: TraitType[objectJson["type"]] as any,
          alias: objectJson["alias"],
          isExtensible: objectJson["isExtensible"],
          permissions: unpackedPermissions,
          selfTraits: unpackedSelfTraits,
          traits: unpackedTraits,
          eventTypes: unpackedEventTypes,
          selfEventTypes: unpackedSelfEventTypes,
          enumTypes: unpackedEnumTypes,
          selfEnumTypes: unpackedSelfEnumTypes,
          id: Number(objectJson["id"]),
          name: objectJson["name"],
          icon: unpackedIcon,
          description: unpackedDescription,
          taggings: unpackedTaggings,
          _packedCache: [{ encoding: 1, isBytes: false, packed: objectJson }],
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(2, 14)] = new TraitDefinitionJsonEncoder();

    class StructDefinitionJsonEncoder implements _JsonObjectEncoder {
      packObject(object: StructDefinition): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "STRUCT_DEFINITION";
        objectJson["id"] = Number(object.id);
        objectJson["type"] = StructType[object.type];
        objectJson["name"] = object.name;
        if (object.icon != null) {
          objectJson["icon"] = object.icon.pack(1);
        }
        if (object.description != null) {
          objectJson["description"] = object.description;
        }
        if (object.taggings.length > 0) {
          const packedTaggings: any[] = [];
          for (const item of object.taggings) {
            packedTaggings.push(Number(item));
          }
          objectJson["taggings"] = packedTaggings;
        }
        objectJson["isFrozen"] = object.isFrozen;
        objectJson["isAbstract"] = object.isAbstract;
        objectJson["isExtensible"] = object.isExtensible;
        if (object.properties.length > 0) {
          const packedProperties: any[] = [];
          for (const item of object.properties) {
            packedProperties.push(item.pack(1));
          }
          objectJson["properties"] = packedProperties;
        }
        if (object.methods.length > 0) {
          const packedMethods: any[] = [];
          for (const item of object.methods) {
            packedMethods.push(item.pack(1));
          }
          objectJson["methods"] = packedMethods;
        }
        if (object.actions.length > 0) {
          const packedActions: any[] = [];
          for (const item of object.actions) {
            packedActions.push(item.pack(1));
          }
          objectJson["actions"] = packedActions;
        }
        if (object.constants.length > 0) {
          const packedConstants: any[] = [];
          for (const item of object.constants) {
            packedConstants.push(item.pack(1));
          }
          objectJson["constants"] = packedConstants;
        }
        if (object.tags.length > 0) {
          const packedTags: any[] = [];
          for (const item of object.tags) {
            packedTags.push(item.pack(1));
          }
          objectJson["tags"] = packedTags;
        }
        if (object.baseType != null) {
          objectJson["baseType"] = StructType[object.baseType];
        }
        if (object.extendedBy.length > 0) {
          const packedExtendedBy: any[] = [];
          for (const item of object.extendedBy) {
            packedExtendedBy.push(StructType[item]);
          }
          objectJson["extendedBy"] = packedExtendedBy;
        }
        if (object.inherits.length > 0) {
          const packedInherits: any[] = [];
          for (const item of object.inherits) {
            packedInherits.push(StructType[item]);
          }
          objectJson["inherits"] = packedInherits;
        }
        if (object.inheritedBy.length > 0) {
          const packedInheritedBy: any[] = [];
          for (const item of object.inheritedBy) {
            packedInheritedBy.push(StructType[item]);
          }
          objectJson["inheritedBy"] = packedInheritedBy;
        }
        if (object.enumTypes.length > 0) {
          const packedEnumTypes: any[] = [];
          for (const item of object.enumTypes) {
            packedEnumTypes.push(EnumType[item]);
          }
          objectJson["enumTypes"] = packedEnumTypes;
        }
        if (object.selfEnumTypes.length > 0) {
          const packedSelfEnumTypes: any[] = [];
          for (const item of object.selfEnumTypes) {
            packedSelfEnumTypes.push(EnumType[item]);
          }
          objectJson["selfEnumTypes"] = packedSelfEnumTypes;
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): StructDefinition {
        const _PropertyDefinition = STRUCT_CLASS_BY_TYPE[18] as typeof PropertyDefinition;
        const _ConstantDefinition = STRUCT_CLASS_BY_TYPE[19] as typeof ConstantDefinition;
        const _TagDefinition = STRUCT_CLASS_BY_TYPE[21] as typeof TagDefinition;
        const _MethodDefinition = STRUCT_CLASS_BY_TYPE[40000] as typeof MethodDefinition;
        const _ActionDefinition = STRUCT_CLASS_BY_TYPE[40100] as typeof ActionDefinition;
        const _Icon = STRUCT_CLASS_BY_TYPE[400031] as typeof Icon;
        const unpackedProperties: any[] = [];
        if (objectJson["properties"] != undefined) {
          for (const item of objectJson["properties"]) {
            unpackedProperties.push(_PropertyDefinition.unpack(1, item, _session) as PropertyDefinition)
          }
        }
        const unpackedMethods: any[] = [];
        if (objectJson["methods"] != undefined) {
          for (const item of objectJson["methods"]) {
            unpackedMethods.push(_MethodDefinition.unpack(1, item, _session) as MethodDefinition)
          }
        }
        const unpackedActions: any[] = [];
        if (objectJson["actions"] != undefined) {
          for (const item of objectJson["actions"]) {
            unpackedActions.push(_ActionDefinition.unpack(1, item, _session) as ActionDefinition)
          }
        }
        const unpackedConstants: any[] = [];
        if (objectJson["constants"] != undefined) {
          for (const item of objectJson["constants"]) {
            unpackedConstants.push(_ConstantDefinition.unpack(1, item, _session) as ConstantDefinition)
          }
        }
        const unpackedTags: any[] = [];
        if (objectJson["tags"] != undefined) {
          for (const item of objectJson["tags"]) {
            unpackedTags.push(_TagDefinition.unpack(1, item, _session) as TagDefinition)
          }
        }
        const baseTypeValue = objectJson["baseType"];
        const unpackedBaseType = baseTypeValue != undefined ? StructType[baseTypeValue] as any : null;
        const unpackedExtendedBy: any[] = [];
        if (objectJson["extendedBy"] != undefined) {
          for (const item of objectJson["extendedBy"]) {
            unpackedExtendedBy.push(StructType[item] as any)
          }
        }
        const unpackedInherits: any[] = [];
        if (objectJson["inherits"] != undefined) {
          for (const item of objectJson["inherits"]) {
            unpackedInherits.push(StructType[item] as any)
          }
        }
        const unpackedInheritedBy: any[] = [];
        if (objectJson["inheritedBy"] != undefined) {
          for (const item of objectJson["inheritedBy"]) {
            unpackedInheritedBy.push(StructType[item] as any)
          }
        }
        const unpackedEnumTypes: any[] = [];
        if (objectJson["enumTypes"] != undefined) {
          for (const item of objectJson["enumTypes"]) {
            unpackedEnumTypes.push(EnumType[item] as any)
          }
        }
        const unpackedSelfEnumTypes: any[] = [];
        if (objectJson["selfEnumTypes"] != undefined) {
          for (const item of objectJson["selfEnumTypes"]) {
            unpackedSelfEnumTypes.push(EnumType[item] as any)
          }
        }
        const iconValue = objectJson["icon"];
        const unpackedIcon = iconValue != undefined ? _Icon.unpack(1, iconValue, _session) as Icon : null;
        const descriptionValue = objectJson["description"];
        const unpackedDescription = descriptionValue != undefined ? descriptionValue : null;
        const unpackedTaggings: any[] = [];
        if (objectJson["taggings"] != undefined) {
          for (const item of objectJson["taggings"]) {
            unpackedTaggings.push(Number(item))
          }
        }
        return new (STRUCT_CLASS_BY_TYPE[15] as typeof StructDefinition)({
          type: StructType[objectJson["type"]] as any,
          isFrozen: objectJson["isFrozen"],
          isAbstract: objectJson["isAbstract"],
          isExtensible: objectJson["isExtensible"],
          properties: unpackedProperties,
          methods: unpackedMethods,
          actions: unpackedActions,
          constants: unpackedConstants,
          tags: unpackedTags,
          baseType: unpackedBaseType,
          extendedBy: unpackedExtendedBy,
          inherits: unpackedInherits,
          inheritedBy: unpackedInheritedBy,
          enumTypes: unpackedEnumTypes,
          selfEnumTypes: unpackedSelfEnumTypes,
          id: Number(objectJson["id"]),
          name: objectJson["name"],
          icon: unpackedIcon,
          description: unpackedDescription,
          taggings: unpackedTaggings,
          _packedCache: [{ encoding: 1, isBytes: false, packed: objectJson }],
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(2, 15)] = new StructDefinitionJsonEncoder();

    class EnumDefinitionJsonEncoder implements _JsonObjectEncoder {
      packObject(object: EnumDefinition): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "ENUM_DEFINITION";
        objectJson["id"] = Number(object.id);
        objectJson["type"] = EnumType[object.type];
        objectJson["name"] = object.name;
        if (object.icon != null) {
          objectJson["icon"] = object.icon.pack(1);
        }
        if (object.description != null) {
          objectJson["description"] = object.description;
        }
        if (object.options.length > 0) {
          const packedOptions: any[] = [];
          for (const item of object.options) {
            packedOptions.push(item.pack(1));
          }
          objectJson["options"] = packedOptions;
        }
        if (object.taggings.length > 0) {
          const packedTaggings: any[] = [];
          for (const item of object.taggings) {
            packedTaggings.push(Number(item));
          }
          objectJson["taggings"] = packedTaggings;
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): EnumDefinition {
        const _OptionDefinition = STRUCT_CLASS_BY_TYPE[20] as typeof OptionDefinition;
        const _Icon = STRUCT_CLASS_BY_TYPE[400031] as typeof Icon;
        const unpackedOptions: any[] = [];
        if (objectJson["options"] != undefined) {
          for (const item of objectJson["options"]) {
            unpackedOptions.push(_OptionDefinition.unpack(1, item, _session) as OptionDefinition)
          }
        }
        const iconValue = objectJson["icon"];
        const unpackedIcon = iconValue != undefined ? _Icon.unpack(1, iconValue, _session) as Icon : null;
        const descriptionValue = objectJson["description"];
        const unpackedDescription = descriptionValue != undefined ? descriptionValue : null;
        const unpackedTaggings: any[] = [];
        if (objectJson["taggings"] != undefined) {
          for (const item of objectJson["taggings"]) {
            unpackedTaggings.push(Number(item))
          }
        }
        return new (STRUCT_CLASS_BY_TYPE[17] as typeof EnumDefinition)({
          type: EnumType[objectJson["type"]] as any,
          options: unpackedOptions,
          id: Number(objectJson["id"]),
          name: objectJson["name"],
          icon: unpackedIcon,
          description: unpackedDescription,
          taggings: unpackedTaggings,
          _packedCache: [{ encoding: 1, isBytes: false, packed: objectJson }],
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(2, 17)] = new EnumDefinitionJsonEncoder();

    class PropertyDefinitionJsonEncoder implements _JsonObjectEncoder {
      packObject(object: PropertyDefinition): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "PROPERTY_DEFINITION";
        objectJson["id"] = Number(object.id);
        objectJson["type"] = PropertyType[object.type];
        objectJson["name"] = object.name;
        if (object.icon != null) {
          objectJson["icon"] = object.icon.pack(1);
        }
        if (object.description != null) {
          objectJson["description"] = object.description;
        }
        objectJson["object"] = object.object.pack(1);
        objectJson["originalObject"] = object.originalObject.pack(1);
        if (object.taggings.length > 0) {
          const packedTaggings: any[] = [];
          for (const item of object.taggings) {
            packedTaggings.push(Number(item));
          }
          objectJson["taggings"] = packedTaggings;
        }
        objectJson["cardinality"] = TypeCardinality[object.cardinality];
        objectJson["scalarType"] = ScalarType[object.scalarType];
        if (object.primitiveType != null) {
          objectJson["primitiveType"] = PrimitiveType[object.primitiveType];
        }
        if (object.enumType != null) {
          objectJson["enumType"] = EnumType[object.enumType];
        }
        if (object.nodeTypes.length > 0) {
          const packedNodeTypes: any[] = [];
          for (const item of object.nodeTypes) {
            packedNodeTypes.push(NodeType[item]);
          }
          objectJson["nodeTypes"] = packedNodeTypes;
        }
        if (object.structType != null) {
          objectJson["structType"] = StructType[object.structType];
        }
        if (object.keyType != null) {
          objectJson["keyType"] = object.keyType.pack(1);
        }
        if (object.value != null) {
          objectJson["value"] = object.value.pack(1);
        }
        if (object.valueFactory != null) {
          objectJson["valueFactory"] = ValueFactory[object.valueFactory];
        }
        if (object.collectionConstraint != null) {
          objectJson["collectionConstraint"] = object.collectionConstraint.pack(1);
        }
        if (object.stringConstraint != null) {
          objectJson["stringConstraint"] = object.stringConstraint.pack(1);
        }
        if (object.numberConstraint != null) {
          objectJson["numberConstraint"] = object.numberConstraint.pack(1);
        }
        if (object.edgeType != null) {
          objectJson["edgeType"] = EdgeType[object.edgeType];
        }
        if (object.cascade != null) {
          objectJson["cascade"] = CascadeAction[object.cascade];
        }
        objectJson["isRequired"] = object.isRequired;
        objectJson["isUnique"] = object.isUnique;
        objectJson["isReadonly"] = object.isReadonly;
        objectJson["isMain"] = object.isMain;
        objectJson["isWired"] = object.isWired;
        objectJson["isStored"] = object.isStored;
        objectJson["isRepr"] = object.isRepr;
        objectJson["isHash"] = object.isHash;
        objectJson["isEq"] = object.isEq;
        objectJson["isInternal"] = object.isInternal;
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): PropertyDefinition {
        const _ObjectDefinitionReference = STRUCT_CLASS_BY_TYPE[11] as typeof ObjectDefinitionReference;
        const _Value = STRUCT_CLASS_BY_TYPE[100] as typeof Value;
        const _Type = STRUCT_CLASS_BY_TYPE[101] as typeof Type;
        const _NumberConstraint = STRUCT_CLASS_BY_TYPE[110] as typeof NumberConstraint;
        const _StringConstraint = STRUCT_CLASS_BY_TYPE[111] as typeof StringConstraint;
        const _CollectionConstraint = STRUCT_CLASS_BY_TYPE[112] as typeof CollectionConstraint;
        const _Icon = STRUCT_CLASS_BY_TYPE[400031] as typeof Icon;
        const primitiveTypeValue = objectJson["primitiveType"];
        const unpackedPrimitiveType = primitiveTypeValue != undefined ? PrimitiveType[primitiveTypeValue] as any : null;
        const enumTypeValue = objectJson["enumType"];
        const unpackedEnumType = enumTypeValue != undefined ? EnumType[enumTypeValue] as any : null;
        const unpackedNodeTypes: any[] = [];
        if (objectJson["nodeTypes"] != undefined) {
          for (const item of objectJson["nodeTypes"]) {
            unpackedNodeTypes.push(NodeType[item] as any)
          }
        }
        const structTypeValue = objectJson["structType"];
        const unpackedStructType = structTypeValue != undefined ? StructType[structTypeValue] as any : null;
        const keyTypeValue = objectJson["keyType"];
        const unpackedKeyType = keyTypeValue != undefined ? _Type.unpack(1, keyTypeValue, _session) as Type : null;
        const valueValue = objectJson["value"];
        const unpackedValue = valueValue != undefined ? _Value.unpack(1, valueValue, _session) as Value : null;
        const valueFactoryValue = objectJson["valueFactory"];
        const unpackedValueFactory = valueFactoryValue != undefined ? ValueFactory[valueFactoryValue] as any : null;
        const collectionConstraintValue = objectJson["collectionConstraint"];
        const unpackedCollectionConstraint = collectionConstraintValue != undefined ? _CollectionConstraint.unpack(1, collectionConstraintValue, _session) as CollectionConstraint : null;
        const stringConstraintValue = objectJson["stringConstraint"];
        const unpackedStringConstraint = stringConstraintValue != undefined ? _StringConstraint.unpack(1, stringConstraintValue, _session) as StringConstraint : null;
        const numberConstraintValue = objectJson["numberConstraint"];
        const unpackedNumberConstraint = numberConstraintValue != undefined ? _NumberConstraint.unpack(1, numberConstraintValue, _session) as NumberConstraint : null;
        const edgeTypeValue = objectJson["edgeType"];
        const unpackedEdgeType = edgeTypeValue != undefined ? EdgeType[edgeTypeValue] as any : null;
        const cascadeValue = objectJson["cascade"];
        const unpackedCascade = cascadeValue != undefined ? CascadeAction[cascadeValue] as any : null;
        const iconValue = objectJson["icon"];
        const unpackedIcon = iconValue != undefined ? _Icon.unpack(1, iconValue, _session) as Icon : null;
        const descriptionValue = objectJson["description"];
        const unpackedDescription = descriptionValue != undefined ? descriptionValue : null;
        const unpackedTaggings: any[] = [];
        if (objectJson["taggings"] != undefined) {
          for (const item of objectJson["taggings"]) {
            unpackedTaggings.push(Number(item))
          }
        }
        return new (STRUCT_CLASS_BY_TYPE[18] as typeof PropertyDefinition)({
          type: PropertyType[objectJson["type"]] as any,
          object: _ObjectDefinitionReference.unpack(1, objectJson["object"], _session) as ObjectDefinitionReference,
          originalObject: _ObjectDefinitionReference.unpack(1, objectJson["originalObject"], _session) as ObjectDefinitionReference,
          cardinality: TypeCardinality[objectJson["cardinality"]] as any,
          scalarType: ScalarType[objectJson["scalarType"]] as any,
          primitiveType: unpackedPrimitiveType,
          enumType: unpackedEnumType,
          nodeTypes: unpackedNodeTypes,
          structType: unpackedStructType,
          keyType: unpackedKeyType,
          value: unpackedValue,
          valueFactory: unpackedValueFactory,
          collectionConstraint: unpackedCollectionConstraint,
          stringConstraint: unpackedStringConstraint,
          numberConstraint: unpackedNumberConstraint,
          edgeType: unpackedEdgeType,
          cascade: unpackedCascade,
          isRequired: objectJson["isRequired"],
          isUnique: objectJson["isUnique"],
          isReadonly: objectJson["isReadonly"],
          isMain: objectJson["isMain"],
          isWired: objectJson["isWired"],
          isStored: objectJson["isStored"],
          isRepr: objectJson["isRepr"],
          isHash: objectJson["isHash"],
          isEq: objectJson["isEq"],
          isInternal: objectJson["isInternal"],
          id: Number(objectJson["id"]),
          name: objectJson["name"],
          icon: unpackedIcon,
          description: unpackedDescription,
          taggings: unpackedTaggings,
          _packedCache: [{ encoding: 1, isBytes: false, packed: objectJson }],
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(2, 18)] = new PropertyDefinitionJsonEncoder();

    class OptionDefinitionJsonEncoder implements _JsonObjectEncoder {
      packObject(object: OptionDefinition): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "OPTION_DEFINITION";
        objectJson["id"] = Number(object.id);
        objectJson["type"] = EnumType[object.type];
        objectJson["name"] = object.name;
        if (object.icon != null) {
          objectJson["icon"] = object.icon.pack(1);
        }
        if (object.description != null) {
          objectJson["description"] = object.description;
        }
        if (object.taggings.length > 0) {
          const packedTaggings: any[] = [];
          for (const item of object.taggings) {
            packedTaggings.push(Number(item));
          }
          objectJson["taggings"] = packedTaggings;
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): OptionDefinition {
        const _Icon = STRUCT_CLASS_BY_TYPE[400031] as typeof Icon;
        const iconValue = objectJson["icon"];
        const unpackedIcon = iconValue != undefined ? _Icon.unpack(1, iconValue, _session) as Icon : null;
        const descriptionValue = objectJson["description"];
        const unpackedDescription = descriptionValue != undefined ? descriptionValue : null;
        const unpackedTaggings: any[] = [];
        if (objectJson["taggings"] != undefined) {
          for (const item of objectJson["taggings"]) {
            unpackedTaggings.push(Number(item))
          }
        }
        return new (STRUCT_CLASS_BY_TYPE[20] as typeof OptionDefinition)({
          type: EnumType[objectJson["type"]] as any,
          id: Number(objectJson["id"]),
          name: objectJson["name"],
          icon: unpackedIcon,
          description: unpackedDescription,
          taggings: unpackedTaggings,
          _packedCache: [{ encoding: 1, isBytes: false, packed: objectJson }],
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(2, 20)] = new OptionDefinitionJsonEncoder();

    class ConstantDefinitionJsonEncoder implements _JsonObjectEncoder {
      packObject(object: ConstantDefinition): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "CONSTANT_DEFINITION";
        objectJson["id"] = Number(object.id);
        objectJson["name"] = object.name;
        if (object.icon != null) {
          objectJson["icon"] = object.icon.pack(1);
        }
        if (object.description != null) {
          objectJson["description"] = object.description;
        }
        if (object.taggings.length > 0) {
          const packedTaggings: any[] = [];
          for (const item of object.taggings) {
            packedTaggings.push(Number(item));
          }
          objectJson["taggings"] = packedTaggings;
        }
        objectJson["value"] = object.value.pack(1);
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): ConstantDefinition {
        const _Value = STRUCT_CLASS_BY_TYPE[100] as typeof Value;
        const _Icon = STRUCT_CLASS_BY_TYPE[400031] as typeof Icon;
        const iconValue = objectJson["icon"];
        const unpackedIcon = iconValue != undefined ? _Icon.unpack(1, iconValue, _session) as Icon : null;
        const descriptionValue = objectJson["description"];
        const unpackedDescription = descriptionValue != undefined ? descriptionValue : null;
        const unpackedTaggings: any[] = [];
        if (objectJson["taggings"] != undefined) {
          for (const item of objectJson["taggings"]) {
            unpackedTaggings.push(Number(item))
          }
        }
        return new (STRUCT_CLASS_BY_TYPE[19] as typeof ConstantDefinition)({
          value: _Value.unpack(1, objectJson["value"], _session) as Value,
          id: Number(objectJson["id"]),
          name: objectJson["name"],
          icon: unpackedIcon,
          description: unpackedDescription,
          taggings: unpackedTaggings,
          _packedCache: [{ encoding: 1, isBytes: false, packed: objectJson }],
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(2, 19)] = new ConstantDefinitionJsonEncoder();

    class TagDefinitionJsonEncoder implements _JsonObjectEncoder {
      packObject(object: TagDefinition): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "TAG_DEFINITION";
        objectJson["id"] = Number(object.id);
        objectJson["name"] = object.name;
        if (object.icon != null) {
          objectJson["icon"] = object.icon.pack(1);
        }
        if (object.description != null) {
          objectJson["description"] = object.description;
        }
        if (object.taggings.length > 0) {
          const packedTaggings: any[] = [];
          for (const item of object.taggings) {
            packedTaggings.push(Number(item));
          }
          objectJson["taggings"] = packedTaggings;
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): TagDefinition {
        const _Icon = STRUCT_CLASS_BY_TYPE[400031] as typeof Icon;
        const iconValue = objectJson["icon"];
        const unpackedIcon = iconValue != undefined ? _Icon.unpack(1, iconValue, _session) as Icon : null;
        const descriptionValue = objectJson["description"];
        const unpackedDescription = descriptionValue != undefined ? descriptionValue : null;
        const unpackedTaggings: any[] = [];
        if (objectJson["taggings"] != undefined) {
          for (const item of objectJson["taggings"]) {
            unpackedTaggings.push(Number(item))
          }
        }
        return new (STRUCT_CLASS_BY_TYPE[21] as typeof TagDefinition)({
          id: Number(objectJson["id"]),
          name: objectJson["name"],
          icon: unpackedIcon,
          description: unpackedDescription,
          taggings: unpackedTaggings,
          _packedCache: [{ encoding: 1, isBytes: false, packed: objectJson }],
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(2, 21)] = new TagDefinitionJsonEncoder();

    class IndexDefinitionJsonEncoder implements _JsonObjectEncoder {
      packObject(object: IndexDefinition): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "INDEX_DEFINITION";
        objectJson["id"] = Number(object.id);
        objectJson["type"] = IndexType[object.type];
        objectJson["name"] = object.name;
        if (object.icon != null) {
          objectJson["icon"] = object.icon.pack(1);
        }
        if (object.description != null) {
          objectJson["description"] = object.description;
        }
        if (object.properties.length > 0) {
          const packedProperties: any[] = [];
          for (const item of object.properties) {
            packedProperties.push(item.pack(1));
          }
          objectJson["properties"] = packedProperties;
        }
        if (object.cover.length > 0) {
          const packedCover: any[] = [];
          for (const item of object.cover) {
            packedCover.push(item.pack(1));
          }
          objectJson["cover"] = packedCover;
        }
        if (object.taggings.length > 0) {
          const packedTaggings: any[] = [];
          for (const item of object.taggings) {
            packedTaggings.push(Number(item));
          }
          objectJson["taggings"] = packedTaggings;
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): IndexDefinition {
        const _PropertyReference = STRUCT_CLASS_BY_TYPE[1001] as typeof PropertyReference;
        const _Icon = STRUCT_CLASS_BY_TYPE[400031] as typeof Icon;
        const unpackedProperties: any[] = [];
        if (objectJson["properties"] != undefined) {
          for (const item of objectJson["properties"]) {
            unpackedProperties.push(_PropertyReference.unpack(1, item, _session) as PropertyReference)
          }
        }
        const unpackedCover: any[] = [];
        if (objectJson["cover"] != undefined) {
          for (const item of objectJson["cover"]) {
            unpackedCover.push(_PropertyReference.unpack(1, item, _session) as PropertyReference)
          }
        }
        const iconValue = objectJson["icon"];
        const unpackedIcon = iconValue != undefined ? _Icon.unpack(1, iconValue, _session) as Icon : null;
        const descriptionValue = objectJson["description"];
        const unpackedDescription = descriptionValue != undefined ? descriptionValue : null;
        const unpackedTaggings: any[] = [];
        if (objectJson["taggings"] != undefined) {
          for (const item of objectJson["taggings"]) {
            unpackedTaggings.push(Number(item))
          }
        }
        return new (STRUCT_CLASS_BY_TYPE[30100] as typeof IndexDefinition)({
          type: IndexType[objectJson["type"]] as any,
          properties: unpackedProperties,
          cover: unpackedCover,
          id: Number(objectJson["id"]),
          name: objectJson["name"],
          icon: unpackedIcon,
          description: unpackedDescription,
          taggings: unpackedTaggings,
          _packedCache: [{ encoding: 1, isBytes: false, packed: objectJson }],
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(2, 30100)] = new IndexDefinitionJsonEncoder();

    class ConstraintDefinitionJsonEncoder implements _JsonObjectEncoder {
      packObject(object: ConstraintDefinition): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "CONSTRAINT_DEFINITION";
        objectJson["id"] = Number(object.id);
        objectJson["type"] = ConstraintType[object.type];
        objectJson["name"] = object.name;
        if (object.icon != null) {
          objectJson["icon"] = object.icon.pack(1);
        }
        if (object.description != null) {
          objectJson["description"] = object.description;
        }
        if (object.properties.length > 0) {
          const packedProperties: any[] = [];
          for (const item of object.properties) {
            packedProperties.push(item.pack(1));
          }
          objectJson["properties"] = packedProperties;
        }
        if (object.taggings.length > 0) {
          const packedTaggings: any[] = [];
          for (const item of object.taggings) {
            packedTaggings.push(Number(item));
          }
          objectJson["taggings"] = packedTaggings;
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): ConstraintDefinition {
        const _PropertyReference = STRUCT_CLASS_BY_TYPE[1001] as typeof PropertyReference;
        const _Icon = STRUCT_CLASS_BY_TYPE[400031] as typeof Icon;
        const unpackedProperties: any[] = [];
        if (objectJson["properties"] != undefined) {
          for (const item of objectJson["properties"]) {
            unpackedProperties.push(_PropertyReference.unpack(1, item, _session) as PropertyReference)
          }
        }
        const iconValue = objectJson["icon"];
        const unpackedIcon = iconValue != undefined ? _Icon.unpack(1, iconValue, _session) as Icon : null;
        const descriptionValue = objectJson["description"];
        const unpackedDescription = descriptionValue != undefined ? descriptionValue : null;
        const unpackedTaggings: any[] = [];
        if (objectJson["taggings"] != undefined) {
          for (const item of objectJson["taggings"]) {
            unpackedTaggings.push(Number(item))
          }
        }
        return new (STRUCT_CLASS_BY_TYPE[30200] as typeof ConstraintDefinition)({
          type: ConstraintType[objectJson["type"]] as any,
          properties: unpackedProperties,
          id: Number(objectJson["id"]),
          name: objectJson["name"],
          icon: unpackedIcon,
          description: unpackedDescription,
          taggings: unpackedTaggings,
          _packedCache: [{ encoding: 1, isBytes: false, packed: objectJson }],
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(2, 30200)] = new ConstraintDefinitionJsonEncoder();

    class PermissionDefinitionJsonEncoder implements _JsonObjectEncoder {
      packObject(object: PermissionDefinition): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "PERMISSION_DEFINITION";
        objectJson["id"] = Number(object.id);
        objectJson["name"] = object.name;
        if (object.icon != null) {
          objectJson["icon"] = object.icon.pack(1);
        }
        if (object.description != null) {
          objectJson["description"] = object.description;
        }
        if (object.taggings.length > 0) {
          const packedTaggings: any[] = [];
          for (const item of object.taggings) {
            packedTaggings.push(Number(item));
          }
          objectJson["taggings"] = packedTaggings;
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): PermissionDefinition {
        const _Icon = STRUCT_CLASS_BY_TYPE[400031] as typeof Icon;
        const iconValue = objectJson["icon"];
        const unpackedIcon = iconValue != undefined ? _Icon.unpack(1, iconValue, _session) as Icon : null;
        const descriptionValue = objectJson["description"];
        const unpackedDescription = descriptionValue != undefined ? descriptionValue : null;
        const unpackedTaggings: any[] = [];
        if (objectJson["taggings"] != undefined) {
          for (const item of objectJson["taggings"]) {
            unpackedTaggings.push(Number(item))
          }
        }
        return new (STRUCT_CLASS_BY_TYPE[50000] as typeof PermissionDefinition)({
          id: Number(objectJson["id"]),
          name: objectJson["name"],
          icon: unpackedIcon,
          description: unpackedDescription,
          taggings: unpackedTaggings,
          _packedCache: [{ encoding: 1, isBytes: false, packed: objectJson }],
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(2, 50000)] = new PermissionDefinitionJsonEncoder();

    class MethodDefinitionJsonEncoder implements _JsonObjectEncoder {
      packObject(object: MethodDefinition): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "METHOD_DEFINITION";
        objectJson["id"] = Number(object.id);
        objectJson["type"] = MethodType[object.type];
        objectJson["name"] = object.name;
        if (object.icon != null) {
          objectJson["icon"] = object.icon.pack(1);
        }
        if (object.description != null) {
          objectJson["description"] = object.description;
        }
        if (object.properties.length > 0) {
          const packedProperties: any[] = [];
          for (const item of object.properties) {
            packedProperties.push(item.pack(1));
          }
          objectJson["properties"] = packedProperties;
        }
        if (object.taggings.length > 0) {
          const packedTaggings: any[] = [];
          for (const item of object.taggings) {
            packedTaggings.push(Number(item));
          }
          objectJson["taggings"] = packedTaggings;
        }
        objectJson["cardinality"] = MethodCardinality[object.cardinality];
        if (object.platforms.length > 0) {
          const packedPlatforms: any[] = [];
          for (const item of object.platforms) {
            packedPlatforms.push(PlatformType[item]);
          }
          objectJson["platforms"] = packedPlatforms;
        }
        if (object.languages.length > 0) {
          const packedLanguages: any[] = [];
          for (const item of object.languages) {
            packedLanguages.push(RuntimeLanguage[item]);
          }
          objectJson["languages"] = packedLanguages;
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): MethodDefinition {
        const _PropertyDefinition = STRUCT_CLASS_BY_TYPE[18] as typeof PropertyDefinition;
        const _Icon = STRUCT_CLASS_BY_TYPE[400031] as typeof Icon;
        const unpackedProperties: any[] = [];
        if (objectJson["properties"] != undefined) {
          for (const item of objectJson["properties"]) {
            unpackedProperties.push(_PropertyDefinition.unpack(1, item, _session) as PropertyDefinition)
          }
        }
        const unpackedPlatforms: any[] = [];
        if (objectJson["platforms"] != undefined) {
          for (const item of objectJson["platforms"]) {
            unpackedPlatforms.push(PlatformType[item] as any)
          }
        }
        const unpackedLanguages: any[] = [];
        if (objectJson["languages"] != undefined) {
          for (const item of objectJson["languages"]) {
            unpackedLanguages.push(RuntimeLanguage[item] as any)
          }
        }
        const iconValue = objectJson["icon"];
        const unpackedIcon = iconValue != undefined ? _Icon.unpack(1, iconValue, _session) as Icon : null;
        const descriptionValue = objectJson["description"];
        const unpackedDescription = descriptionValue != undefined ? descriptionValue : null;
        const unpackedTaggings: any[] = [];
        if (objectJson["taggings"] != undefined) {
          for (const item of objectJson["taggings"]) {
            unpackedTaggings.push(Number(item))
          }
        }
        return new (STRUCT_CLASS_BY_TYPE[40000] as typeof MethodDefinition)({
          type: MethodType[objectJson["type"]] as any,
          properties: unpackedProperties,
          cardinality: MethodCardinality[objectJson["cardinality"]] as any,
          platforms: unpackedPlatforms,
          languages: unpackedLanguages,
          id: Number(objectJson["id"]),
          name: objectJson["name"],
          icon: unpackedIcon,
          description: unpackedDescription,
          taggings: unpackedTaggings,
          _packedCache: [{ encoding: 1, isBytes: false, packed: objectJson }],
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(2, 40000)] = new MethodDefinitionJsonEncoder();

    class ActionDefinitionJsonEncoder implements _JsonObjectEncoder {
      packObject(object: ActionDefinition): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "ACTION_DEFINITION";
        objectJson["id"] = Number(object.id);
        objectJson["type"] = MethodType[object.type];
        objectJson["name"] = object.name;
        if (object.icon != null) {
          objectJson["icon"] = object.icon.pack(1);
        }
        if (object.description != null) {
          objectJson["description"] = object.description;
        }
        if (object.properties.length > 0) {
          const packedProperties: any[] = [];
          for (const item of object.properties) {
            packedProperties.push(item.pack(1));
          }
          objectJson["properties"] = packedProperties;
        }
        if (object.taggings.length > 0) {
          const packedTaggings: any[] = [];
          for (const item of object.taggings) {
            packedTaggings.push(Number(item));
          }
          objectJson["taggings"] = packedTaggings;
        }
        objectJson["cardinality"] = MethodCardinality[object.cardinality];
        if (object.platforms.length > 0) {
          const packedPlatforms: any[] = [];
          for (const item of object.platforms) {
            packedPlatforms.push(PlatformType[item]);
          }
          objectJson["platforms"] = packedPlatforms;
        }
        if (object.languages.length > 0) {
          const packedLanguages: any[] = [];
          for (const item of object.languages) {
            packedLanguages.push(RuntimeLanguage[item]);
          }
          objectJson["languages"] = packedLanguages;
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): ActionDefinition {
        const _PropertyDefinition = STRUCT_CLASS_BY_TYPE[18] as typeof PropertyDefinition;
        const _Icon = STRUCT_CLASS_BY_TYPE[400031] as typeof Icon;
        const unpackedProperties: any[] = [];
        if (objectJson["properties"] != undefined) {
          for (const item of objectJson["properties"]) {
            unpackedProperties.push(_PropertyDefinition.unpack(1, item, _session) as PropertyDefinition)
          }
        }
        const unpackedPlatforms: any[] = [];
        if (objectJson["platforms"] != undefined) {
          for (const item of objectJson["platforms"]) {
            unpackedPlatforms.push(PlatformType[item] as any)
          }
        }
        const unpackedLanguages: any[] = [];
        if (objectJson["languages"] != undefined) {
          for (const item of objectJson["languages"]) {
            unpackedLanguages.push(RuntimeLanguage[item] as any)
          }
        }
        const iconValue = objectJson["icon"];
        const unpackedIcon = iconValue != undefined ? _Icon.unpack(1, iconValue, _session) as Icon : null;
        const descriptionValue = objectJson["description"];
        const unpackedDescription = descriptionValue != undefined ? descriptionValue : null;
        const unpackedTaggings: any[] = [];
        if (objectJson["taggings"] != undefined) {
          for (const item of objectJson["taggings"]) {
            unpackedTaggings.push(Number(item))
          }
        }
        return new (STRUCT_CLASS_BY_TYPE[40100] as typeof ActionDefinition)({
          type: MethodType[objectJson["type"]] as any,
          properties: unpackedProperties,
          cardinality: MethodCardinality[objectJson["cardinality"]] as any,
          platforms: unpackedPlatforms,
          languages: unpackedLanguages,
          id: Number(objectJson["id"]),
          name: objectJson["name"],
          icon: unpackedIcon,
          description: unpackedDescription,
          taggings: unpackedTaggings,
          _packedCache: [{ encoding: 1, isBytes: false, packed: objectJson }],
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(2, 40100)] = new ActionDefinitionJsonEncoder();

    class IconJsonEncoder implements _JsonObjectEncoder {
      packObject(object: Icon): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "ICON";
        objectJson["type"] = IconType[object.type];
        if (object.emoji != null) {
          objectJson["emoji"] = object.emoji;
        }
        if (object.faName != null) {
          objectJson["faName"] = object.faName;
        }
        if (object.vscName != null) {
          objectJson["vscName"] = object.vscName;
        }
        if (object.filePtr != null) {
          objectJson["file"] = object.filePtr.pack(1);
        }
        if (object.fileUrl != null) {
          objectJson["fileUrl"] = object.fileUrl;
        }
        if (object.color != null) {
          objectJson["color"] = object.color.pack(1);
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): Icon {
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const _Color = STRUCT_CLASS_BY_TYPE[2100300] as typeof Color;
        const emojiValue = objectJson["emoji"];
        const unpackedEmoji = emojiValue != undefined ? emojiValue : null;
        const faNameValue = objectJson["faName"];
        const unpackedFaName = faNameValue != undefined ? faNameValue : null;
        const vscNameValue = objectJson["vscName"];
        const unpackedVscName = vscNameValue != undefined ? vscNameValue : null;
        const filePtrValue = objectJson["file"];
        const unpackedFilePtr = filePtrValue != undefined ? _NodeReference.unpack(1, filePtrValue, _session) as NodeReference : null;
        const fileUrlValue = objectJson["fileUrl"];
        const unpackedFileUrl = fileUrlValue != undefined ? fileUrlValue : null;
        const colorValue = objectJson["color"];
        const unpackedColor = colorValue != undefined ? _Color.unpack(1, colorValue, _session) as Color : null;
        return new (STRUCT_CLASS_BY_TYPE[400031] as typeof Icon)({
          type: IconType[objectJson["type"]] as any,
          emoji: unpackedEmoji,
          faName: unpackedFaName,
          vscName: unpackedVscName,
          file: unpackedFilePtr,
          fileUrl: unpackedFileUrl,
          color: unpackedColor,
          _packedCache: [{ encoding: 1, isBytes: false, packed: objectJson }],
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(2, 400031)] = new IconJsonEncoder();

    class MigrationDefinitionJsonEncoder implements _JsonObjectEncoder {
      packObject(object: MigrationDefinition): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "MIGRATION_DEFINITION";
        objectJson["id"] = Number(object.id);
        objectJson["type"] = MigrationType[object.type];
        objectJson["name"] = object.name;
        if (object.icon != null) {
          objectJson["icon"] = object.icon.pack(1);
        }
        if (object.description != null) {
          objectJson["description"] = object.description;
        }
        if (object.taggings.length > 0) {
          const packedTaggings: any[] = [];
          for (const item of object.taggings) {
            packedTaggings.push(Number(item));
          }
          objectJson["taggings"] = packedTaggings;
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): MigrationDefinition {
        const _Icon = STRUCT_CLASS_BY_TYPE[400031] as typeof Icon;
        const iconValue = objectJson["icon"];
        const unpackedIcon = iconValue != undefined ? _Icon.unpack(1, iconValue, _session) as Icon : null;
        const descriptionValue = objectJson["description"];
        const unpackedDescription = descriptionValue != undefined ? descriptionValue : null;
        const unpackedTaggings: any[] = [];
        if (objectJson["taggings"] != undefined) {
          for (const item of objectJson["taggings"]) {
            unpackedTaggings.push(Number(item))
          }
        }
        return new (STRUCT_CLASS_BY_TYPE[31000] as typeof MigrationDefinition)({
          type: MigrationType[objectJson["type"]] as any,
          id: Number(objectJson["id"]),
          name: objectJson["name"],
          icon: unpackedIcon,
          description: unpackedDescription,
          taggings: unpackedTaggings,
          _packedCache: [{ encoding: 1, isBytes: false, packed: objectJson }],
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(2, 31000)] = new MigrationDefinitionJsonEncoder();

    class MigrationOperationDefinitionJsonEncoder implements _JsonObjectEncoder {
      packObject(object: MigrationOperationDefinition): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "MIGRATION_OPERATION_DEFINITION";
        objectJson["id"] = Number(object.id);
        objectJson["name"] = object.name;
        if (object.icon != null) {
          objectJson["icon"] = object.icon.pack(1);
        }
        if (object.description != null) {
          objectJson["description"] = object.description;
        }
        if (object.taggings.length > 0) {
          const packedTaggings: any[] = [];
          for (const item of object.taggings) {
            packedTaggings.push(Number(item));
          }
          objectJson["taggings"] = packedTaggings;
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): MigrationOperationDefinition {
        const _Icon = STRUCT_CLASS_BY_TYPE[400031] as typeof Icon;
        const iconValue = objectJson["icon"];
        const unpackedIcon = iconValue != undefined ? _Icon.unpack(1, iconValue, _session) as Icon : null;
        const descriptionValue = objectJson["description"];
        const unpackedDescription = descriptionValue != undefined ? descriptionValue : null;
        const unpackedTaggings: any[] = [];
        if (objectJson["taggings"] != undefined) {
          for (const item of objectJson["taggings"]) {
            unpackedTaggings.push(Number(item))
          }
        }
        return new (STRUCT_CLASS_BY_TYPE[31100] as typeof MigrationOperationDefinition)({
          id: Number(objectJson["id"]),
          name: objectJson["name"],
          icon: unpackedIcon,
          description: unpackedDescription,
          taggings: unpackedTaggings,
          _packedCache: [{ encoding: 1, isBytes: false, packed: objectJson }],
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(2, 31100)] = new MigrationOperationDefinitionJsonEncoder();

    class StringConstraintJsonEncoder implements _JsonObjectEncoder {
      packObject(object: StringConstraint): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "STRING_CONSTRAINT";
        if (object.format != null) {
          objectJson["format"] = StringFormat[object.format];
        }
        if (object.regex != null) {
          objectJson["regex"] = object.regex;
        }
        if (object.startsWith != null) {
          objectJson["startsWith"] = object.startsWith;
        }
        if (object.endsWith != null) {
          objectJson["endsWith"] = object.endsWith;
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): StringConstraint {

        const formatValue = objectJson["format"];
        const unpackedFormat = formatValue != undefined ? StringFormat[formatValue] as any : null;
        const regexValue = objectJson["regex"];
        const unpackedRegex = regexValue != undefined ? regexValue : null;
        const startsWithValue = objectJson["startsWith"];
        const unpackedStartsWith = startsWithValue != undefined ? startsWithValue : null;
        const endsWithValue = objectJson["endsWith"];
        const unpackedEndsWith = endsWithValue != undefined ? endsWithValue : null;
        return new (STRUCT_CLASS_BY_TYPE[111] as typeof StringConstraint)({
          format: unpackedFormat,
          regex: unpackedRegex,
          startsWith: unpackedStartsWith,
          endsWith: unpackedEndsWith,
          _packedCache: [{ encoding: 1, isBytes: false, packed: objectJson }],
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(2, 111)] = new StringConstraintJsonEncoder();

    class NumberConstraintJsonEncoder implements _JsonObjectEncoder {
      packObject(object: NumberConstraint): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "NUMBER_CONSTRAINT";
        if (object.format != null) {
          objectJson["format"] = NumberFormat[object.format];
        }
        if (object.minValue != null) {
          objectJson["minValue"] = Number(object.minValue);
        }
        if (object.maxValue != null) {
          objectJson["maxValue"] = Number(object.maxValue);
        }
        if (object.stepValue != null) {
          objectJson["stepValue"] = Number(object.stepValue);
        }
        if (object.precision != null) {
          objectJson["precision"] = Number(object.precision);
        }
        if (object.scale != null) {
          objectJson["scale"] = Number(object.scale);
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): NumberConstraint {

        const formatValue = objectJson["format"];
        const unpackedFormat = formatValue != undefined ? NumberFormat[formatValue] as any : null;
        const minValueValue = objectJson["minValue"];
        const unpackedMinValue = minValueValue != undefined ? Number(minValueValue) : null;
        const maxValueValue = objectJson["maxValue"];
        const unpackedMaxValue = maxValueValue != undefined ? Number(maxValueValue) : null;
        const stepValueValue = objectJson["stepValue"];
        const unpackedStepValue = stepValueValue != undefined ? Number(stepValueValue) : null;
        const precisionValue = objectJson["precision"];
        const unpackedPrecision = precisionValue != undefined ? Number(precisionValue) : null;
        const scaleValue = objectJson["scale"];
        const unpackedScale = scaleValue != undefined ? Number(scaleValue) : null;
        return new (STRUCT_CLASS_BY_TYPE[110] as typeof NumberConstraint)({
          format: unpackedFormat,
          minValue: unpackedMinValue,
          maxValue: unpackedMaxValue,
          stepValue: unpackedStepValue,
          precision: unpackedPrecision,
          scale: unpackedScale,
          _packedCache: [{ encoding: 1, isBytes: false, packed: objectJson }],
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(2, 110)] = new NumberConstraintJsonEncoder();

    class CollectionConstraintJsonEncoder implements _JsonObjectEncoder {
      packObject(object: CollectionConstraint): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "COLLECTION_CONSTRAINT";
        if (object.minLength != null) {
          objectJson["minLength"] = Number(object.minLength);
        }
        if (object.maxLength != null) {
          objectJson["maxLength"] = Number(object.maxLength);
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): CollectionConstraint {

        const minLengthValue = objectJson["minLength"];
        const unpackedMinLength = minLengthValue != undefined ? Number(minLengthValue) : null;
        const maxLengthValue = objectJson["maxLength"];
        const unpackedMaxLength = maxLengthValue != undefined ? Number(maxLengthValue) : null;
        return new (STRUCT_CLASS_BY_TYPE[112] as typeof CollectionConstraint)({
          minLength: unpackedMinLength,
          maxLength: unpackedMaxLength,
          _packedCache: [{ encoding: 1, isBytes: false, packed: objectJson }],
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(2, 112)] = new CollectionConstraintJsonEncoder();

    class TypeJsonEncoder implements _JsonObjectEncoder {
      packObject(object: Type): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "TYPE";
        if (object.name != null) {
          objectJson["name"] = object.name;
        }
        objectJson["cardinality"] = TypeCardinality[object.cardinality];
        objectJson["scalarType"] = ScalarType[object.scalarType];
        if (object.primitiveType != null) {
          objectJson["primitiveType"] = PrimitiveType[object.primitiveType];
        }
        if (object.enumType != null) {
          objectJson["enumType"] = EnumType[object.enumType];
        }
        if (object.nodeTypes.length > 0) {
          const packedNodeTypes: any[] = [];
          for (const item of object.nodeTypes) {
            packedNodeTypes.push(NodeType[item]);
          }
          objectJson["nodeTypes"] = packedNodeTypes;
        }
        if (object.structType != null) {
          objectJson["structType"] = StructType[object.structType];
        }
        if (object.keyType != null) {
          objectJson["keyType"] = object.keyType.pack(1);
        }
        if (object.value != null) {
          objectJson["value"] = object.value.pack(1);
        }
        if (object.valueFactory != null) {
          objectJson["valueFactory"] = ValueFactory[object.valueFactory];
        }
        if (object.collectionConstraint != null) {
          objectJson["collectionConstraint"] = object.collectionConstraint.pack(1);
        }
        if (object.stringConstraint != null) {
          objectJson["stringConstraint"] = object.stringConstraint.pack(1);
        }
        if (object.numberConstraint != null) {
          objectJson["numberConstraint"] = object.numberConstraint.pack(1);
        }
        if (object.isRequired != null) {
          objectJson["isRequired"] = object.isRequired;
        }
        if (object.isMain != null) {
          objectJson["isMain"] = object.isMain;
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): Type {
        const _Value = STRUCT_CLASS_BY_TYPE[100] as typeof Value;
        const _Type = STRUCT_CLASS_BY_TYPE[101] as typeof Type;
        const _NumberConstraint = STRUCT_CLASS_BY_TYPE[110] as typeof NumberConstraint;
        const _StringConstraint = STRUCT_CLASS_BY_TYPE[111] as typeof StringConstraint;
        const _CollectionConstraint = STRUCT_CLASS_BY_TYPE[112] as typeof CollectionConstraint;
        const nameValue = objectJson["name"];
        const unpackedName = nameValue != undefined ? nameValue : null;
        const primitiveTypeValue = objectJson["primitiveType"];
        const unpackedPrimitiveType = primitiveTypeValue != undefined ? PrimitiveType[primitiveTypeValue] as any : null;
        const enumTypeValue = objectJson["enumType"];
        const unpackedEnumType = enumTypeValue != undefined ? EnumType[enumTypeValue] as any : null;
        const unpackedNodeTypes: any[] = [];
        if (objectJson["nodeTypes"] != undefined) {
          for (const item of objectJson["nodeTypes"]) {
            unpackedNodeTypes.push(NodeType[item] as any)
          }
        }
        const structTypeValue = objectJson["structType"];
        const unpackedStructType = structTypeValue != undefined ? StructType[structTypeValue] as any : null;
        const keyTypeValue = objectJson["keyType"];
        const unpackedKeyType = keyTypeValue != undefined ? _Type.unpack(1, keyTypeValue, _session) as Type : null;
        const valueValue = objectJson["value"];
        const unpackedValue = valueValue != undefined ? _Value.unpack(1, valueValue, _session) as Value : null;
        const valueFactoryValue = objectJson["valueFactory"];
        const unpackedValueFactory = valueFactoryValue != undefined ? ValueFactory[valueFactoryValue] as any : null;
        const collectionConstraintValue = objectJson["collectionConstraint"];
        const unpackedCollectionConstraint = collectionConstraintValue != undefined ? _CollectionConstraint.unpack(1, collectionConstraintValue, _session) as CollectionConstraint : null;
        const stringConstraintValue = objectJson["stringConstraint"];
        const unpackedStringConstraint = stringConstraintValue != undefined ? _StringConstraint.unpack(1, stringConstraintValue, _session) as StringConstraint : null;
        const numberConstraintValue = objectJson["numberConstraint"];
        const unpackedNumberConstraint = numberConstraintValue != undefined ? _NumberConstraint.unpack(1, numberConstraintValue, _session) as NumberConstraint : null;
        const isRequiredValue = objectJson["isRequired"];
        const unpackedIsRequired = isRequiredValue != undefined ? isRequiredValue : null;
        const isMainValue = objectJson["isMain"];
        const unpackedIsMain = isMainValue != undefined ? isMainValue : null;
        return new (STRUCT_CLASS_BY_TYPE[101] as typeof Type)({
          name: unpackedName,
          cardinality: TypeCardinality[objectJson["cardinality"]] as any,
          scalarType: ScalarType[objectJson["scalarType"]] as any,
          primitiveType: unpackedPrimitiveType,
          enumType: unpackedEnumType,
          nodeTypes: unpackedNodeTypes,
          structType: unpackedStructType,
          keyType: unpackedKeyType,
          value: unpackedValue,
          valueFactory: unpackedValueFactory,
          collectionConstraint: unpackedCollectionConstraint,
          stringConstraint: unpackedStringConstraint,
          numberConstraint: unpackedNumberConstraint,
          isRequired: unpackedIsRequired,
          isMain: unpackedIsMain,
          _packedCache: [{ encoding: 1, isBytes: false, packed: objectJson }],
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(2, 101)] = new TypeJsonEncoder();

    class ValueJsonEncoder implements _JsonObjectEncoder {
      packObject(object: Value): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "VALUE";
        objectJson["type"] = object.type.pack(1);
        if (object.value != null) {
          objectJson["value"] = object.value;
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): Value {
        const _Type = STRUCT_CLASS_BY_TYPE[101] as typeof Type;
        const valueValue = objectJson["value"];
        const unpackedValue = valueValue != undefined ? valueValue : null;
        return new (STRUCT_CLASS_BY_TYPE[100] as typeof Value)({
          type: _Type.unpack(1, objectJson["type"], _session) as Type,
          value: unpackedValue,
          _packedCache: [{ encoding: 1, isBytes: false, packed: objectJson }],
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(2, 100)] = new ValueJsonEncoder();

    class FunctionJsonEncoder implements _JsonObjectEncoder {
      packObject(object: Function): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "FUNCTION";
        objectJson["type"] = FunctionType[object.type];
        objectJson["left"] = object.left.pack(1);
        if (object.right != null) {
          objectJson["right"] = object.right.pack(1);
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): Function {
        const _Expression = STRUCT_CLASS_BY_TYPE[200] as typeof Expression;
        const rightValue = objectJson["right"];
        const unpackedRight = rightValue != undefined ? _Expression.unpack(1, rightValue, _session) as Expression : null;
        return new (STRUCT_CLASS_BY_TYPE[201] as typeof Function)({
          type: FunctionType[objectJson["type"]] as any,
          left: _Expression.unpack(1, objectJson["left"], _session) as Expression,
          right: unpackedRight,
          _packedCache: [{ encoding: 1, isBytes: false, packed: objectJson }],
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(2, 201)] = new FunctionJsonEncoder();

    class ConditionJsonEncoder implements _JsonObjectEncoder {
      packObject(object: Condition): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "CONDITION";
        objectJson["type"] = ConditionalType[object.type];
        objectJson["left"] = object.left.pack(1);
        if (object.right != null) {
          objectJson["right"] = object.right.pack(1);
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): Condition {
        const _Expression = STRUCT_CLASS_BY_TYPE[200] as typeof Expression;
        const rightValue = objectJson["right"];
        const unpackedRight = rightValue != undefined ? _Expression.unpack(1, rightValue, _session) as Expression : null;
        return new (STRUCT_CLASS_BY_TYPE[204] as typeof Condition)({
          type: ConditionalType[objectJson["type"]] as any,
          left: _Expression.unpack(1, objectJson["left"], _session) as Expression,
          right: unpackedRight,
          _packedCache: [{ encoding: 1, isBytes: false, packed: objectJson }],
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(2, 204)] = new ConditionJsonEncoder();

    class AggregationJsonEncoder implements _JsonObjectEncoder {
      packObject(object: Aggregation): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "AGGREGATION";
        objectJson["type"] = AggregationType[object.type];
        if (object.expression != null) {
          objectJson["expression"] = object.expression.pack(1);
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): Aggregation {
        const _Expression = STRUCT_CLASS_BY_TYPE[200] as typeof Expression;
        const expressionValue = objectJson["expression"];
        const unpackedExpression = expressionValue != undefined ? _Expression.unpack(1, expressionValue, _session) as Expression : null;
        return new (STRUCT_CLASS_BY_TYPE[203] as typeof Aggregation)({
          type: AggregationType[objectJson["type"]] as any,
          expression: unpackedExpression,
          _packedCache: [{ encoding: 1, isBytes: false, packed: objectJson }],
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(2, 203)] = new AggregationJsonEncoder();

    class ExpressionJsonEncoder implements _JsonObjectEncoder {
      packObject(object: Expression): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "EXPRESSION";
        objectJson["type"] = ExpressionType[object.type];
        if (object.literal != null) {
          objectJson["literal"] = object.literal.pack(1);
        }
        if (object.attribute != null) {
          objectJson["attribute"] = object.attribute.pack(1);
        }
        if (object.condition != null) {
          objectJson["condition"] = object.condition.pack(1);
        }
        if (object.function != null) {
          objectJson["function"] = object.function.pack(1);
        }
        if (object.aggregation != null) {
          objectJson["aggregation"] = object.aggregation.pack(1);
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): Expression {
        const _Value = STRUCT_CLASS_BY_TYPE[100] as typeof Value;
        const _Function = STRUCT_CLASS_BY_TYPE[201] as typeof Function;
        const _Aggregation = STRUCT_CLASS_BY_TYPE[203] as typeof Aggregation;
        const _Condition = STRUCT_CLASS_BY_TYPE[204] as typeof Condition;
        const _PropertyReference = STRUCT_CLASS_BY_TYPE[1001] as typeof PropertyReference;
        const literalValue = objectJson["literal"];
        const unpackedLiteral = literalValue != undefined ? _Value.unpack(1, literalValue, _session) as Value : null;
        const attributeValue = objectJson["attribute"];
        const unpackedAttribute = attributeValue != undefined ? _PropertyReference.unpack(1, attributeValue, _session) as PropertyReference : null;
        const conditionValue = objectJson["condition"];
        const unpackedCondition = conditionValue != undefined ? _Condition.unpack(1, conditionValue, _session) as Condition : null;
        const functionValue = objectJson["function"];
        const unpackedFunction = functionValue != undefined ? _Function.unpack(1, functionValue, _session) as Function : null;
        const aggregationValue = objectJson["aggregation"];
        const unpackedAggregation = aggregationValue != undefined ? _Aggregation.unpack(1, aggregationValue, _session) as Aggregation : null;
        return new (STRUCT_CLASS_BY_TYPE[200] as typeof Expression)({
          type: ExpressionType[objectJson["type"]] as any,
          literal: unpackedLiteral,
          attribute: unpackedAttribute,
          condition: unpackedCondition,
          function: unpackedFunction,
          aggregation: unpackedAggregation,
          _packedCache: [{ encoding: 1, isBytes: false, packed: objectJson }],
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(2, 200)] = new ExpressionJsonEncoder();

    class SortJsonEncoder implements _JsonObjectEncoder {
      packObject(object: Sort): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "SORT";
        objectJson["type"] = SortType[object.type];
        objectJson["by"] = object.by.pack(1);
        if (object.mode != null) {
          objectJson["mode"] = SortMode[object.mode];
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): Sort {
        const _Expression = STRUCT_CLASS_BY_TYPE[200] as typeof Expression;
        const modeValue = objectJson["mode"];
        const unpackedMode = modeValue != undefined ? SortMode[modeValue] as any : null;
        return new (STRUCT_CLASS_BY_TYPE[205] as typeof Sort)({
          type: SortType[objectJson["type"]] as any,
          by: _Expression.unpack(1, objectJson["by"], _session) as Expression,
          mode: unpackedMode,
          _packedCache: [{ encoding: 1, isBytes: false, packed: objectJson }],
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(2, 205)] = new SortJsonEncoder();

    class SelectJsonEncoder implements _JsonObjectEncoder {
      packObject(object: Select): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "SELECT";
        if (object.attributes.length > 0) {
          const packedAttributes: any[] = [];
          for (const item of object.attributes) {
            packedAttributes.push(item.pack(1));
          }
          objectJson["attributes"] = packedAttributes;
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): Select {
        const _PropertyReference = STRUCT_CLASS_BY_TYPE[1001] as typeof PropertyReference;
        const unpackedAttributes: any[] = [];
        if (objectJson["attributes"] != undefined) {
          for (const item of objectJson["attributes"]) {
            unpackedAttributes.push(_PropertyReference.unpack(1, item, _session) as PropertyReference)
          }
        }
        return new (STRUCT_CLASS_BY_TYPE[206] as typeof Select)({
          attributes: unpackedAttributes,
          _packedCache: [{ encoding: 1, isBytes: false, packed: objectJson }],
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(2, 206)] = new SelectJsonEncoder();

    class JoinJsonEncoder implements _JsonObjectEncoder {
      packObject(object: Join): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "JOIN";
        objectJson["type"] = JoinType[object.type];
        objectJson["recursive"] = object.recursive;
        if (object.depth != null) {
          objectJson["depth"] = Number(object.depth);
        }
        if (object.on != null) {
          objectJson["on"] = object.on.pack(1);
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): Join {
        const _Condition = STRUCT_CLASS_BY_TYPE[204] as typeof Condition;
        const depthValue = objectJson["depth"];
        const unpackedDepth = depthValue != undefined ? Number(depthValue) : null;
        const onValue = objectJson["on"];
        const unpackedOn = onValue != undefined ? _Condition.unpack(1, onValue, _session) as Condition : null;
        return new (STRUCT_CLASS_BY_TYPE[202] as typeof Join)({
          type: JoinType[objectJson["type"]] as any,
          recursive: objectJson["recursive"],
          depth: unpackedDepth,
          on: unpackedOn,
          _packedCache: [{ encoding: 1, isBytes: false, packed: objectJson }],
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(2, 202)] = new JoinJsonEncoder();

    class QueryJsonEncoder implements _JsonObjectEncoder {
      packObject(object: Query): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "QUERY";
        objectJson["id"] = object.id;
        objectJson["type"] = QueryType[object.type];
        objectJson["domain"] = GraphDomain[object.domain];
        objectJson["name"] = object.name;
        objectJson["definition"] = object.definition.pack(1);
        if (object.subqueries.length > 0) {
          const packedSubqueries: any[] = [];
          for (const item of object.subqueries) {
            packedSubqueries.push(item.pack(1));
          }
          objectJson["subqueries"] = packedSubqueries;
        }
        if (object.join != null) {
          objectJson["join"] = object.join.pack(1);
        }
        if (object.select != null) {
          objectJson["select"] = object.select.pack(1);
        }
        if (object.where != null) {
          objectJson["where"] = object.where.pack(1);
        }
        if (object.having != null) {
          objectJson["having"] = object.having.pack(1);
        }
        if (object.groupBy.length > 0) {
          const packedGroupBy: any[] = [];
          for (const item of object.groupBy) {
            packedGroupBy.push(item.pack(1));
          }
          objectJson["groupBy"] = packedGroupBy;
        }
        if (object.aggregation != null) {
          objectJson["aggregation"] = object.aggregation.pack(1);
        }
        if (object.sort.length > 0) {
          const packedSort: any[] = [];
          for (const item of object.sort) {
            packedSort.push(item.pack(1));
          }
          objectJson["sort"] = packedSort;
        }
        if (object.limit != null) {
          objectJson["limit"] = Number(object.limit);
        }
        if (object.offset != null) {
          objectJson["offset"] = Number(object.offset);
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): Query {
        const _NodeDefinitionReference = STRUCT_CLASS_BY_TYPE[13] as typeof NodeDefinitionReference;
        const _Expression = STRUCT_CLASS_BY_TYPE[200] as typeof Expression;
        const _Join = STRUCT_CLASS_BY_TYPE[202] as typeof Join;
        const _Aggregation = STRUCT_CLASS_BY_TYPE[203] as typeof Aggregation;
        const _Condition = STRUCT_CLASS_BY_TYPE[204] as typeof Condition;
        const _Sort = STRUCT_CLASS_BY_TYPE[205] as typeof Sort;
        const _Select = STRUCT_CLASS_BY_TYPE[206] as typeof Select;
        const _Query = STRUCT_CLASS_BY_TYPE[300] as typeof Query;
        const unpackedSubqueries: any[] = [];
        if (objectJson["subqueries"] != undefined) {
          for (const item of objectJson["subqueries"]) {
            unpackedSubqueries.push(_Query.unpack(1, item, _session) as Query)
          }
        }
        const joinValue = objectJson["join"];
        const unpackedJoin = joinValue != undefined ? _Join.unpack(1, joinValue, _session) as Join : null;
        const selectValue = objectJson["select"];
        const unpackedSelect = selectValue != undefined ? _Select.unpack(1, selectValue, _session) as Select : null;
        const whereValue = objectJson["where"];
        const unpackedWhere = whereValue != undefined ? _Condition.unpack(1, whereValue, _session) as Condition : null;
        const havingValue = objectJson["having"];
        const unpackedHaving = havingValue != undefined ? _Condition.unpack(1, havingValue, _session) as Condition : null;
        const unpackedGroupBy: any[] = [];
        if (objectJson["groupBy"] != undefined) {
          for (const item of objectJson["groupBy"]) {
            unpackedGroupBy.push(_Expression.unpack(1, item, _session) as Expression)
          }
        }
        const aggregationValue = objectJson["aggregation"];
        const unpackedAggregation = aggregationValue != undefined ? _Aggregation.unpack(1, aggregationValue, _session) as Aggregation : null;
        const unpackedSort: any[] = [];
        if (objectJson["sort"] != undefined) {
          for (const item of objectJson["sort"]) {
            unpackedSort.push(_Sort.unpack(1, item, _session) as Sort)
          }
        }
        const limitValue = objectJson["limit"];
        const unpackedLimit = limitValue != undefined ? Number(limitValue) : null;
        const offsetValue = objectJson["offset"];
        const unpackedOffset = offsetValue != undefined ? Number(offsetValue) : null;
        return new (STRUCT_CLASS_BY_TYPE[300] as typeof Query)({
          id: objectJson["id"],
          type: QueryType[objectJson["type"]] as any,
          domain: GraphDomain[objectJson["domain"]] as any,
          name: objectJson["name"],
          definition: _NodeDefinitionReference.unpack(1, objectJson["definition"], _session) as NodeDefinitionReference,
          subqueries: unpackedSubqueries,
          join: unpackedJoin,
          select: unpackedSelect,
          where: unpackedWhere,
          having: unpackedHaving,
          groupBy: unpackedGroupBy,
          aggregation: unpackedAggregation,
          sort: unpackedSort,
          limit: unpackedLimit,
          offset: unpackedOffset,
          _packedCache: [{ encoding: 1, isBytes: false, packed: objectJson }],
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(2, 300)] = new QueryJsonEncoder();

    class DatumJsonEncoder implements _JsonObjectEncoder {
      packObject(object: Datum): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "DATUM";
        objectJson["definition"] = object.definitionPtr.pack(1);
        if (Object.keys(object.customValues).length > 0) {
          const packedCustomValues: { [key: string]: any } = {} as any;
          for (const [key, value] of Object.entries(object.customValues)) {
            packedCustomValues[String(key)] = value.pack(1);
          }
          objectJson["customValues"] = packedCustomValues;
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): Datum {
        const _Value = STRUCT_CLASS_BY_TYPE[100] as typeof Value;
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const unpackedCustomValues = {} as any;
        if (objectJson["customValues"] != undefined) {
          for (const [key, value] of Object.entries(objectJson["customValues"])) {
            unpackedCustomValues[key] = _Value.unpack(1, value as any, _session) as Value;
          }
        }
        return new (STRUCT_CLASS_BY_TYPE[2] as typeof Datum)({
          definition: _NodeReference.unpack(1, objectJson["definition"], _session) as NodeReference,
          customValues: unpackedCustomValues,
          _packedCache: [{ encoding: 1, isBytes: false, packed: objectJson }],
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(2, 2)] = new DatumJsonEncoder();

    class DatumMutableJsonEncoder implements _JsonObjectEncoder {
      packObject(object: DatumMutable): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "DATUM_MUTABLE";
        objectJson["definition"] = object.definitionPtr.pack(1);
        if (Object.keys(object.customValues).length > 0) {
          const packedCustomValues: { [key: string]: any } = {} as any;
          for (const [key, value] of Object.entries(object.customValues)) {
            packedCustomValues[String(key)] = value.pack(1);
          }
          objectJson["customValues"] = packedCustomValues;
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): DatumMutable {
        const _Value = STRUCT_CLASS_BY_TYPE[100] as typeof Value;
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const unpackedCustomValues = {} as any;
        if (objectJson["customValues"] != undefined) {
          for (const [key, value] of Object.entries(objectJson["customValues"])) {
            unpackedCustomValues[key] = _Value.unpack(1, value as any, _session) as Value;
          }
        }
        return new (STRUCT_CLASS_BY_TYPE[3] as typeof DatumMutable)({
          definition: _NodeReference.unpack(1, objectJson["definition"], _session) as NodeReference,
          customValues: unpackedCustomValues,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(2, 3)] = new DatumMutableJsonEncoder();

    class TextSpanJsonEncoder implements _JsonObjectEncoder {
      packObject(object: TextSpan): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "TEXT_SPAN";
        objectJson["type"] = TextSpanType[object.type];
        if (object.content != null) {
          objectJson["content"] = object.content;
        }
        if (object.nodePtr != null) {
          objectJson["node"] = object.nodePtr.pack(1);
        }
        if (object.url != null) {
          objectJson["url"] = object.url;
        }
        if (object.isBold != null) {
          objectJson["isBold"] = object.isBold;
        }
        if (object.isItalic != null) {
          objectJson["isItalic"] = object.isItalic;
        }
        if (object.isStrikethrough != null) {
          objectJson["isStrikethrough"] = object.isStrikethrough;
        }
        if (object.isUnderline != null) {
          objectJson["isUnderline"] = object.isUnderline;
        }
        if (object.isCode != null) {
          objectJson["isCode"] = object.isCode;
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): TextSpan {
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const contentValue = objectJson["content"];
        const unpackedContent = contentValue != undefined ? contentValue : null;
        const nodePtrValue = objectJson["node"];
        const unpackedNodePtr = nodePtrValue != undefined ? _NodeReference.unpack(1, nodePtrValue, _session) as NodeReference : null;
        const urlValue = objectJson["url"];
        const unpackedUrl = urlValue != undefined ? urlValue : null;
        const isBoldValue = objectJson["isBold"];
        const unpackedIsBold = isBoldValue != undefined ? isBoldValue : null;
        const isItalicValue = objectJson["isItalic"];
        const unpackedIsItalic = isItalicValue != undefined ? isItalicValue : null;
        const isStrikethroughValue = objectJson["isStrikethrough"];
        const unpackedIsStrikethrough = isStrikethroughValue != undefined ? isStrikethroughValue : null;
        const isUnderlineValue = objectJson["isUnderline"];
        const unpackedIsUnderline = isUnderlineValue != undefined ? isUnderlineValue : null;
        const isCodeValue = objectJson["isCode"];
        const unpackedIsCode = isCodeValue != undefined ? isCodeValue : null;
        return new (STRUCT_CLASS_BY_TYPE[400021] as typeof TextSpan)({
          type: TextSpanType[objectJson["type"]] as any,
          content: unpackedContent,
          node: unpackedNodePtr,
          url: unpackedUrl,
          isBold: unpackedIsBold,
          isItalic: unpackedIsItalic,
          isStrikethrough: unpackedIsStrikethrough,
          isUnderline: unpackedIsUnderline,
          isCode: unpackedIsCode,
          _packedCache: [{ encoding: 1, isBytes: false, packed: objectJson }],
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(2, 400021)] = new TextSpanJsonEncoder();

    class TextJsonEncoder implements _JsonObjectEncoder {
      packObject(object: Text): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "TEXT";
        if (object.spans.length > 0) {
          const packedSpans: any[] = [];
          for (const item of object.spans) {
            packedSpans.push(item.pack(1));
          }
          objectJson["spans"] = packedSpans;
        }
        if (object.isBold != null) {
          objectJson["isBold"] = object.isBold;
        }
        if (object.isItalic != null) {
          objectJson["isItalic"] = object.isItalic;
        }
        if (object.isStrikethrough != null) {
          objectJson["isStrikethrough"] = object.isStrikethrough;
        }
        if (object.isUnderline != null) {
          objectJson["isUnderline"] = object.isUnderline;
        }
        if (object.isCode != null) {
          objectJson["isCode"] = object.isCode;
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): Text {
        const _TextSpan = STRUCT_CLASS_BY_TYPE[400021] as typeof TextSpan;
        const unpackedSpans: any[] = [];
        if (objectJson["spans"] != undefined) {
          for (const item of objectJson["spans"]) {
            unpackedSpans.push(_TextSpan.unpack(1, item, _session) as TextSpan)
          }
        }
        const isBoldValue = objectJson["isBold"];
        const unpackedIsBold = isBoldValue != undefined ? isBoldValue : null;
        const isItalicValue = objectJson["isItalic"];
        const unpackedIsItalic = isItalicValue != undefined ? isItalicValue : null;
        const isStrikethroughValue = objectJson["isStrikethrough"];
        const unpackedIsStrikethrough = isStrikethroughValue != undefined ? isStrikethroughValue : null;
        const isUnderlineValue = objectJson["isUnderline"];
        const unpackedIsUnderline = isUnderlineValue != undefined ? isUnderlineValue : null;
        const isCodeValue = objectJson["isCode"];
        const unpackedIsCode = isCodeValue != undefined ? isCodeValue : null;
        return new (STRUCT_CLASS_BY_TYPE[400020] as typeof Text)({
          spans: unpackedSpans,
          isBold: unpackedIsBold,
          isItalic: unpackedIsItalic,
          isStrikethrough: unpackedIsStrikethrough,
          isUnderline: unpackedIsUnderline,
          isCode: unpackedIsCode,
          _packedCache: [{ encoding: 1, isBytes: false, packed: objectJson }],
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(2, 400020)] = new TextJsonEncoder();

    class ColorJsonEncoder implements _JsonObjectEncoder {
      packObject(object: Color): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "COLOR";
        objectJson["type"] = ColorType[object.type];
        if (object.stylePtr != null) {
          objectJson["style"] = object.stylePtr.pack(1);
        }
        if (object.hue != null) {
          objectJson["hue"] = ColorHue[object.hue];
        }
        if (object.shade != null) {
          objectJson["shade"] = ColorShade[object.shade];
        }
        if (object.intent != null) {
          objectJson["intent"] = ColorIntent[object.intent];
        }
        if (object.x != null) {
          objectJson["x"] = Number(object.x);
        }
        if (object.y != null) {
          objectJson["y"] = Number(object.y);
        }
        if (object.z != null) {
          objectJson["z"] = Number(object.z);
        }
        if (object.alpha != null) {
          objectJson["alpha"] = Number(object.alpha);
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): Color {
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const stylePtrValue = objectJson["style"];
        const unpackedStylePtr = stylePtrValue != undefined ? _NodeReference.unpack(1, stylePtrValue, _session) as NodeReference : null;
        const hueValue = objectJson["hue"];
        const unpackedHue = hueValue != undefined ? ColorHue[hueValue] as any : null;
        const shadeValue = objectJson["shade"];
        const unpackedShade = shadeValue != undefined ? ColorShade[shadeValue] as any : null;
        const intentValue = objectJson["intent"];
        const unpackedIntent = intentValue != undefined ? ColorIntent[intentValue] as any : null;
        const xValue = objectJson["x"];
        const unpackedX = xValue != undefined ? Number(xValue) : null;
        const yValue = objectJson["y"];
        const unpackedY = yValue != undefined ? Number(yValue) : null;
        const zValue = objectJson["z"];
        const unpackedZ = zValue != undefined ? Number(zValue) : null;
        const alphaValue = objectJson["alpha"];
        const unpackedAlpha = alphaValue != undefined ? Number(alphaValue) : null;
        return new (STRUCT_CLASS_BY_TYPE[2100300] as typeof Color)({
          type: ColorType[objectJson["type"]] as any,
          style: unpackedStylePtr,
          hue: unpackedHue,
          shade: unpackedShade,
          intent: unpackedIntent,
          x: unpackedX,
          y: unpackedY,
          z: unpackedZ,
          alpha: unpackedAlpha,
          _packedCache: [{ encoding: 1, isBytes: false, packed: objectJson }],
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(2, 2100300)] = new ColorJsonEncoder();

    class BorderJsonEncoder implements _JsonObjectEncoder {
      packObject(object: Border): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "BORDER";
        objectJson["type"] = BorderType[object.type];
        if (object.color != null) {
          objectJson["color"] = object.color.pack(1);
        }
        if (object.width != null) {
          objectJson["width"] = object.width.pack(1);
        }
        if (object.stylePtr != null) {
          objectJson["style"] = object.stylePtr.pack(1);
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): Border {
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const _Color = STRUCT_CLASS_BY_TYPE[2100300] as typeof Color;
        const _Inset2 = STRUCT_CLASS_BY_TYPE[2400023] as typeof Inset2;
        const colorValue = objectJson["color"];
        const unpackedColor = colorValue != undefined ? _Color.unpack(1, colorValue, _session) as Color : null;
        const widthValue = objectJson["width"];
        const unpackedWidth = widthValue != undefined ? _Inset2.unpack(1, widthValue, _session) as Inset2 : null;
        const stylePtrValue = objectJson["style"];
        const unpackedStylePtr = stylePtrValue != undefined ? _NodeReference.unpack(1, stylePtrValue, _session) as NodeReference : null;
        return new (STRUCT_CLASS_BY_TYPE[2100600] as typeof Border)({
          type: BorderType[objectJson["type"]] as any,
          color: unpackedColor,
          width: unpackedWidth,
          style: unpackedStylePtr,
          _packedCache: [{ encoding: 1, isBytes: false, packed: objectJson }],
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(2, 2100600)] = new BorderJsonEncoder();

    class GradientStopJsonEncoder implements _JsonObjectEncoder {
      packObject(object: GradientStop): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "GRADIENT_STOP";
        if (object.color != null) {
          objectJson["color"] = object.color.pack(1);
        }
        objectJson["position"] = Number(object.position);
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): GradientStop {
        const _Color = STRUCT_CLASS_BY_TYPE[2100300] as typeof Color;
        const colorValue = objectJson["color"];
        const unpackedColor = colorValue != undefined ? _Color.unpack(1, colorValue, _session) as Color : null;
        return new (STRUCT_CLASS_BY_TYPE[2100801] as typeof GradientStop)({
          color: unpackedColor,
          position: Number(objectJson["position"]),
          _packedCache: [{ encoding: 1, isBytes: false, packed: objectJson }],
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(2, 2100801)] = new GradientStopJsonEncoder();

    class GradientJsonEncoder implements _JsonObjectEncoder {
      packObject(object: Gradient): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "GRADIENT";
        objectJson["type"] = GradientType[object.type];
        if (object.stylePtr != null) {
          objectJson["style"] = object.stylePtr.pack(1);
        }
        if (object.angle != null) {
          objectJson["angle"] = Number(object.angle);
        }
        if (object.stops.length > 0) {
          const packedStops: any[] = [];
          for (const item of object.stops) {
            packedStops.push(item.pack(1));
          }
          objectJson["stops"] = packedStops;
        }
        if (object.centerAnchor != null) {
          objectJson["centerAnchor"] = object.centerAnchor.pack(1);
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): Gradient {
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const _GradientStop = STRUCT_CLASS_BY_TYPE[2100801] as typeof GradientStop;
        const _Axis2 = STRUCT_CLASS_BY_TYPE[2400025] as typeof Axis2;
        const stylePtrValue = objectJson["style"];
        const unpackedStylePtr = stylePtrValue != undefined ? _NodeReference.unpack(1, stylePtrValue, _session) as NodeReference : null;
        const angleValue = objectJson["angle"];
        const unpackedAngle = angleValue != undefined ? Number(angleValue) : null;
        const unpackedStops: any[] = [];
        if (objectJson["stops"] != undefined) {
          for (const item of objectJson["stops"]) {
            unpackedStops.push(_GradientStop.unpack(1, item, _session) as GradientStop)
          }
        }
        const centerAnchorValue = objectJson["centerAnchor"];
        const unpackedCenterAnchor = centerAnchorValue != undefined ? _Axis2.unpack(1, centerAnchorValue, _session) as Axis2 : null;
        return new (STRUCT_CLASS_BY_TYPE[2100800] as typeof Gradient)({
          type: GradientType[objectJson["type"]] as any,
          style: unpackedStylePtr,
          angle: unpackedAngle,
          stops: unpackedStops,
          centerAnchor: unpackedCenterAnchor,
          _packedCache: [{ encoding: 1, isBytes: false, packed: objectJson }],
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(2, 2100800)] = new GradientJsonEncoder();

    class FillJsonEncoder implements _JsonObjectEncoder {
      packObject(object: Fill): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "FILL";
        objectJson["type"] = FillType[object.type];
        if (object.stylePtr != null) {
          objectJson["style"] = object.stylePtr.pack(1);
        }
        if (object.color != null) {
          objectJson["color"] = object.color.pack(1);
        }
        if (object.gradient != null) {
          objectJson["gradient"] = object.gradient.pack(1);
        }
        if (object.imagePtr != null) {
          objectJson["image"] = object.imagePtr.pack(1);
        }
        if (object.position != null) {
          objectJson["position"] = FillPosition[object.position];
        }
        if (object.size != null) {
          objectJson["size"] = FillSize[object.size];
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): Fill {
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const _Color = STRUCT_CLASS_BY_TYPE[2100300] as typeof Color;
        const _Gradient = STRUCT_CLASS_BY_TYPE[2100800] as typeof Gradient;
        const stylePtrValue = objectJson["style"];
        const unpackedStylePtr = stylePtrValue != undefined ? _NodeReference.unpack(1, stylePtrValue, _session) as NodeReference : null;
        const colorValue = objectJson["color"];
        const unpackedColor = colorValue != undefined ? _Color.unpack(1, colorValue, _session) as Color : null;
        const gradientValue = objectJson["gradient"];
        const unpackedGradient = gradientValue != undefined ? _Gradient.unpack(1, gradientValue, _session) as Gradient : null;
        const imagePtrValue = objectJson["image"];
        const unpackedImagePtr = imagePtrValue != undefined ? _NodeReference.unpack(1, imagePtrValue, _session) as NodeReference : null;
        const positionValue = objectJson["position"];
        const unpackedPosition = positionValue != undefined ? FillPosition[positionValue] as any : null;
        const sizeValue = objectJson["size"];
        const unpackedSize = sizeValue != undefined ? FillSize[sizeValue] as any : null;
        return new (STRUCT_CLASS_BY_TYPE[2100400] as typeof Fill)({
          type: FillType[objectJson["type"]] as any,
          style: unpackedStylePtr,
          color: unpackedColor,
          gradient: unpackedGradient,
          image: unpackedImagePtr,
          position: unpackedPosition,
          size: unpackedSize,
          _packedCache: [{ encoding: 1, isBytes: false, packed: objectJson }],
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(2, 2100400)] = new FillJsonEncoder();

    class FontJsonEncoder implements _JsonObjectEncoder {
      packObject(object: Font): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "FONT";
        objectJson["type"] = FontType[object.type];
        if (object.stylePtr != null) {
          objectJson["style"] = object.stylePtr.pack(1);
        }
        if (object.weight != null) {
          objectJson["weight"] = FontWeight[object.weight];
        }
        if (object.color != null) {
          objectJson["color"] = object.color.pack(1);
        }
        if (object.size != null) {
          objectJson["size"] = FontSize[object.size];
        }
        if (object.align != null) {
          objectJson["align"] = TextAlign[object.align];
        }
        if (object.lineHeight != null) {
          objectJson["lineHeight"] = object.lineHeight.pack(1);
        }
        if (object.letterSpacing != null) {
          objectJson["letterSpacing"] = object.letterSpacing.pack(1);
        }
        if (object.decoration != null) {
          objectJson["decoration"] = TextDecoration[object.decoration];
        }
        if (object.transform != null) {
          objectJson["transform"] = TextTransform[object.transform];
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): Font {
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const _Length = STRUCT_CLASS_BY_TYPE[1800001] as typeof Length;
        const _Fill = STRUCT_CLASS_BY_TYPE[2100400] as typeof Fill;
        const stylePtrValue = objectJson["style"];
        const unpackedStylePtr = stylePtrValue != undefined ? _NodeReference.unpack(1, stylePtrValue, _session) as NodeReference : null;
        const weightValue = objectJson["weight"];
        const unpackedWeight = weightValue != undefined ? FontWeight[weightValue] as any : null;
        const colorValue = objectJson["color"];
        const unpackedColor = colorValue != undefined ? _Fill.unpack(1, colorValue, _session) as Fill : null;
        const sizeValue = objectJson["size"];
        const unpackedSize = sizeValue != undefined ? FontSize[sizeValue] as any : null;
        const alignValue = objectJson["align"];
        const unpackedAlign = alignValue != undefined ? TextAlign[alignValue] as any : null;
        const lineHeightValue = objectJson["lineHeight"];
        const unpackedLineHeight = lineHeightValue != undefined ? _Length.unpack(1, lineHeightValue, _session) as Length : null;
        const letterSpacingValue = objectJson["letterSpacing"];
        const unpackedLetterSpacing = letterSpacingValue != undefined ? _Length.unpack(1, letterSpacingValue, _session) as Length : null;
        const decorationValue = objectJson["decoration"];
        const unpackedDecoration = decorationValue != undefined ? TextDecoration[decorationValue] as any : null;
        const transformValue = objectJson["transform"];
        const unpackedTransform = transformValue != undefined ? TextTransform[transformValue] as any : null;
        return new (STRUCT_CLASS_BY_TYPE[2100500] as typeof Font)({
          type: FontType[objectJson["type"]] as any,
          style: unpackedStylePtr,
          weight: unpackedWeight,
          color: unpackedColor,
          size: unpackedSize,
          align: unpackedAlign,
          lineHeight: unpackedLineHeight,
          letterSpacing: unpackedLetterSpacing,
          decoration: unpackedDecoration,
          transform: unpackedTransform,
          _packedCache: [{ encoding: 1, isBytes: false, packed: objectJson }],
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(2, 2100500)] = new FontJsonEncoder();

    class ShadowJsonEncoder implements _JsonObjectEncoder {
      packObject(object: Shadow): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "SHADOW";
        objectJson["type"] = ShadowType[object.type];
        if (object.stylePtr != null) {
          objectJson["style"] = object.stylePtr.pack(1);
        }
        if (object.color != null) {
          objectJson["color"] = object.color.pack(1);
        }
        objectJson["position"] = ShadowPosition[object.position];
        if (object.offset != null) {
          objectJson["offset"] = object.offset.pack(1);
        }
        if (object.blur != null) {
          objectJson["blur"] = Number(object.blur);
        }
        if (object.spread != null) {
          objectJson["spread"] = Number(object.spread);
        }
        if (object.diffusion != null) {
          objectJson["diffusion"] = Number(object.diffusion);
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): Shadow {
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const _Color = STRUCT_CLASS_BY_TYPE[2100300] as typeof Color;
        const _Axis2 = STRUCT_CLASS_BY_TYPE[2400025] as typeof Axis2;
        const stylePtrValue = objectJson["style"];
        const unpackedStylePtr = stylePtrValue != undefined ? _NodeReference.unpack(1, stylePtrValue, _session) as NodeReference : null;
        const colorValue = objectJson["color"];
        const unpackedColor = colorValue != undefined ? _Color.unpack(1, colorValue, _session) as Color : null;
        const offsetValue = objectJson["offset"];
        const unpackedOffset = offsetValue != undefined ? _Axis2.unpack(1, offsetValue, _session) as Axis2 : null;
        const blurValue = objectJson["blur"];
        const unpackedBlur = blurValue != undefined ? Number(blurValue) : null;
        const spreadValue = objectJson["spread"];
        const unpackedSpread = spreadValue != undefined ? Number(spreadValue) : null;
        const diffusionValue = objectJson["diffusion"];
        const unpackedDiffusion = diffusionValue != undefined ? Number(diffusionValue) : null;
        return new (STRUCT_CLASS_BY_TYPE[2100700] as typeof Shadow)({
          type: ShadowType[objectJson["type"]] as any,
          style: unpackedStylePtr,
          color: unpackedColor,
          position: ShadowPosition[objectJson["position"]] as any,
          offset: unpackedOffset,
          blur: unpackedBlur,
          spread: unpackedSpread,
          diffusion: unpackedDiffusion,
          _packedCache: [{ encoding: 1, isBytes: false, packed: objectJson }],
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(2, 2100700)] = new ShadowJsonEncoder();

    class StrokeJsonEncoder implements _JsonObjectEncoder {
      packObject(object: Stroke): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "STROKE";
        objectJson["type"] = StrokeType[object.type];
        objectJson["size"] = Number(object.size);
        objectJson["thinning"] = Number(object.thinning);
        objectJson["smoothing"] = Number(object.smoothing);
        objectJson["streamline"] = Number(object.streamline);
        objectJson["easing"] = Easing[object.easing];
        if (object.color != null) {
          objectJson["color"] = object.color.pack(1);
        }
        if (object.start != null) {
          objectJson["start"] = object.start.pack(1);
        }
        if (object.end != null) {
          objectJson["end"] = object.end.pack(1);
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): Stroke {
        const _Color = STRUCT_CLASS_BY_TYPE[2100300] as typeof Color;
        const _StrokeCap = STRUCT_CLASS_BY_TYPE[2101101] as typeof StrokeCap;
        const colorValue = objectJson["color"];
        const unpackedColor = colorValue != undefined ? _Color.unpack(1, colorValue, _session) as Color : null;
        const startValue = objectJson["start"];
        const unpackedStart = startValue != undefined ? _StrokeCap.unpack(1, startValue, _session) as StrokeCap : null;
        const endValue = objectJson["end"];
        const unpackedEnd = endValue != undefined ? _StrokeCap.unpack(1, endValue, _session) as StrokeCap : null;
        return new (STRUCT_CLASS_BY_TYPE[2101100] as typeof Stroke)({
          type: StrokeType[objectJson["type"]] as any,
          size: Number(objectJson["size"]),
          thinning: Number(objectJson["thinning"]),
          smoothing: Number(objectJson["smoothing"]),
          streamline: Number(objectJson["streamline"]),
          easing: Easing[objectJson["easing"]] as any,
          color: unpackedColor,
          start: unpackedStart,
          end: unpackedEnd,
          _packedCache: [{ encoding: 1, isBytes: false, packed: objectJson }],
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(2, 2101100)] = new StrokeJsonEncoder();

    class StrokeCapJsonEncoder implements _JsonObjectEncoder {
      packObject(object: StrokeCap): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "STROKE_CAP";
        objectJson["cap"] = object.cap;
        objectJson["taper"] = object.taper;
        objectJson["easing"] = Easing[object.easing];
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): StrokeCap {

        return new (STRUCT_CLASS_BY_TYPE[2101101] as typeof StrokeCap)({
          cap: objectJson["cap"],
          taper: objectJson["taper"],
          easing: Easing[objectJson["easing"]] as any,
          _packedCache: [{ encoding: 1, isBytes: false, packed: objectJson }],
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(2, 2101101)] = new StrokeCapJsonEncoder();

    class StrokePointJsonEncoder implements _JsonObjectEncoder {
      packObject(object: StrokePoint): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "STROKE_POINT";
        objectJson["point"] = object.point.pack(1);
        objectJson["originalPoint"] = object.originalPoint.pack(1);
        objectJson["pressure"] = Number(object.pressure);
        objectJson["direction"] = object.direction.pack(1);
        objectJson["distance"] = Number(object.distance);
        objectJson["runningLength"] = Number(object.runningLength);
        objectJson["radius"] = Number(object.radius);
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): StrokePoint {
        const _Vector2 = STRUCT_CLASS_BY_TYPE[2400000] as typeof Vector2;
        return new (STRUCT_CLASS_BY_TYPE[2101103] as typeof StrokePoint)({
          point: _Vector2.unpack(1, objectJson["point"], _session) as Vector2,
          originalPoint: _Vector2.unpack(1, objectJson["originalPoint"], _session) as Vector2,
          pressure: Number(objectJson["pressure"]),
          direction: _Vector2.unpack(1, objectJson["direction"], _session) as Vector2,
          distance: Number(objectJson["distance"]),
          runningLength: Number(objectJson["runningLength"]),
          radius: Number(objectJson["radius"]),
          _packedCache: [{ encoding: 1, isBytes: false, packed: objectJson }],
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(2, 2101103)] = new StrokePointJsonEncoder();

    class StrokePathJsonEncoder implements _JsonObjectEncoder {
      packObject(object: StrokePath): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "STROKE_PATH";
        if (object.points.length > 0) {
          const packedPoints: any[] = [];
          for (const item of object.points) {
            packedPoints.push(item.pack(1));
          }
          objectJson["points"] = packedPoints;
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): StrokePath {
        const _StrokePoint = STRUCT_CLASS_BY_TYPE[2101103] as typeof StrokePoint;
        const unpackedPoints: any[] = [];
        if (objectJson["points"] != undefined) {
          for (const item of objectJson["points"]) {
            unpackedPoints.push(_StrokePoint.unpack(1, item, _session) as StrokePoint)
          }
        }
        return new (STRUCT_CLASS_BY_TYPE[2101102] as typeof StrokePath)({
          points: unpackedPoints,
          _packedCache: [{ encoding: 1, isBytes: false, packed: objectJson }],
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(2, 2101102)] = new StrokePathJsonEncoder();

    class TransitionJsonEncoder implements _JsonObjectEncoder {
      packObject(object: Transition): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "TRANSITION";
        objectJson["type"] = TransitionType[object.type];
        if (object.stylePtr != null) {
          objectJson["style"] = object.stylePtr.pack(1);
        }
        if (object.delay != null) {
          objectJson["delay"] = Number(object.delay);
        }
        if (object.duration != null) {
          objectJson["duration"] = Number(object.duration);
        }
        if (object.ease.length > 0) {
          const packedEase: any[] = [];
          for (const item of object.ease) {
            packedEase.push(Number(item));
          }
          objectJson["ease"] = packedEase;
        }
        if (object.stiffness != null) {
          objectJson["stiffness"] = Number(object.stiffness);
        }
        if (object.damping != null) {
          objectJson["damping"] = Number(object.damping);
        }
        if (object.mass != null) {
          objectJson["mass"] = Number(object.mass);
        }
        if (object.bounce != null) {
          objectJson["bounce"] = Number(object.bounce);
        }
        if (object.springType != null) {
          objectJson["springType"] = SpringType[object.springType];
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): Transition {
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const stylePtrValue = objectJson["style"];
        const unpackedStylePtr = stylePtrValue != undefined ? _NodeReference.unpack(1, stylePtrValue, _session) as NodeReference : null;
        const delayValue = objectJson["delay"];
        const unpackedDelay = delayValue != undefined ? Number(delayValue) : null;
        const durationValue = objectJson["duration"];
        const unpackedDuration = durationValue != undefined ? Number(durationValue) : null;
        const unpackedEase: any[] = [];
        if (objectJson["ease"] != undefined) {
          for (const item of objectJson["ease"]) {
            unpackedEase.push(Number(item))
          }
        }
        const stiffnessValue = objectJson["stiffness"];
        const unpackedStiffness = stiffnessValue != undefined ? Number(stiffnessValue) : null;
        const dampingValue = objectJson["damping"];
        const unpackedDamping = dampingValue != undefined ? Number(dampingValue) : null;
        const massValue = objectJson["mass"];
        const unpackedMass = massValue != undefined ? Number(massValue) : null;
        const bounceValue = objectJson["bounce"];
        const unpackedBounce = bounceValue != undefined ? Number(bounceValue) : null;
        const springTypeValue = objectJson["springType"];
        const unpackedSpringType = springTypeValue != undefined ? SpringType[springTypeValue] as any : null;
        return new (STRUCT_CLASS_BY_TYPE[2200000] as typeof Transition)({
          type: TransitionType[objectJson["type"]] as any,
          style: unpackedStylePtr,
          delay: unpackedDelay,
          duration: unpackedDuration,
          ease: unpackedEase,
          stiffness: unpackedStiffness,
          damping: unpackedDamping,
          mass: unpackedMass,
          bounce: unpackedBounce,
          springType: unpackedSpringType,
          _packedCache: [{ encoding: 1, isBytes: false, packed: objectJson }],
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(2, 2200000)] = new TransitionJsonEncoder();

    class EffectJsonEncoder implements _JsonObjectEncoder {
      packObject(object: Effect): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "EFFECT";
        objectJson["type"] = EffectType[object.type];
        if (object.stylePtr != null) {
          objectJson["style"] = object.stylePtr.pack(1);
        }
        if (object.opacity != null) {
          objectJson["opacity"] = Number(object.opacity);
        }
        if (object.offset != null) {
          objectJson["offset"] = object.offset.pack(1);
        }
        if (object.scale != null) {
          objectJson["scale"] = Number(object.scale);
        }
        if (object.rotate != null) {
          objectJson["rotate"] = object.rotate.pack(1);
        }
        if (object.skew != null) {
          objectJson["skew"] = object.skew.pack(1);
        }
        if (object.perspective != null) {
          objectJson["perspective"] = Number(object.perspective);
        }
        if (object.delay != null) {
          objectJson["delay"] = timedeltaToISOFormat(object.delay);
        }
        if (object.duration != null) {
          objectJson["duration"] = Number(object.duration);
        }
        if (object.threshold != null) {
          objectJson["threshold"] = Number(object.threshold);
        }
        if (object.once != null) {
          objectJson["once"] = object.once;
        }
        if (object.repeat != null) {
          objectJson["repeat"] = RepeatType[object.repeat];
        }
        if (object.split != null) {
          objectJson["split"] = TextSplitType[object.split];
        }
        if (object.offscreen != null) {
          objectJson["offscreen"] = OffscreenBehavior[object.offscreen];
        }
        if (object.transition != null) {
          objectJson["transition"] = object.transition.pack(1);
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): Effect {
        const _NodeReference = STRUCT_CLASS_BY_TYPE[1000] as typeof NodeReference;
        const _Transition = STRUCT_CLASS_BY_TYPE[2200000] as typeof Transition;
        const _Vector2 = STRUCT_CLASS_BY_TYPE[2400000] as typeof Vector2;
        const _Axis3 = STRUCT_CLASS_BY_TYPE[2400026] as typeof Axis3;
        const stylePtrValue = objectJson["style"];
        const unpackedStylePtr = stylePtrValue != undefined ? _NodeReference.unpack(1, stylePtrValue, _session) as NodeReference : null;
        const opacityValue = objectJson["opacity"];
        const unpackedOpacity = opacityValue != undefined ? Number(opacityValue) : null;
        const offsetValue = objectJson["offset"];
        const unpackedOffset = offsetValue != undefined ? _Vector2.unpack(1, offsetValue, _session) as Vector2 : null;
        const scaleValue = objectJson["scale"];
        const unpackedScale = scaleValue != undefined ? Number(scaleValue) : null;
        const rotateValue = objectJson["rotate"];
        const unpackedRotate = rotateValue != undefined ? _Axis3.unpack(1, rotateValue, _session) as Axis3 : null;
        const skewValue = objectJson["skew"];
        const unpackedSkew = skewValue != undefined ? _Vector2.unpack(1, skewValue, _session) as Vector2 : null;
        const perspectiveValue = objectJson["perspective"];
        const unpackedPerspective = perspectiveValue != undefined ? Number(perspectiveValue) : null;
        const delayValue = objectJson["delay"];
        const unpackedDelay = delayValue != undefined ? timedeltaFromISOFormat(delayValue) : null;
        const durationValue = objectJson["duration"];
        const unpackedDuration = durationValue != undefined ? Number(durationValue) : null;
        const thresholdValue = objectJson["threshold"];
        const unpackedThreshold = thresholdValue != undefined ? Number(thresholdValue) : null;
        const onceValue = objectJson["once"];
        const unpackedOnce = onceValue != undefined ? onceValue : null;
        const repeatValue = objectJson["repeat"];
        const unpackedRepeat = repeatValue != undefined ? RepeatType[repeatValue] as any : null;
        const splitValue = objectJson["split"];
        const unpackedSplit = splitValue != undefined ? TextSplitType[splitValue] as any : null;
        const offscreenValue = objectJson["offscreen"];
        const unpackedOffscreen = offscreenValue != undefined ? OffscreenBehavior[offscreenValue] as any : null;
        const transitionValue = objectJson["transition"];
        const unpackedTransition = transitionValue != undefined ? _Transition.unpack(1, transitionValue, _session) as Transition : null;
        return new (STRUCT_CLASS_BY_TYPE[2200100] as typeof Effect)({
          type: EffectType[objectJson["type"]] as any,
          style: unpackedStylePtr,
          opacity: unpackedOpacity,
          offset: unpackedOffset,
          scale: unpackedScale,
          rotate: unpackedRotate,
          skew: unpackedSkew,
          perspective: unpackedPerspective,
          delay: unpackedDelay,
          duration: unpackedDuration,
          threshold: unpackedThreshold,
          once: unpackedOnce,
          repeat: unpackedRepeat,
          split: unpackedSplit,
          offscreen: unpackedOffscreen,
          transition: unpackedTransition,
          _packedCache: [{ encoding: 1, isBytes: false, packed: objectJson }],
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(2, 2200100)] = new EffectJsonEncoder();

    class Arrow2DJsonEncoder implements _JsonObjectEncoder {
      packObject(object: Arrow2D): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "ARROW2D";
        objectJson["startType"] = ArrowHeadType[object.startType];
        objectJson["start"] = object.start.pack(1);
        objectJson["endType"] = ArrowHeadType[object.endType];
        objectJson["end"] = object.end.pack(1);
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): Arrow2D {
        const _Vector2 = STRUCT_CLASS_BY_TYPE[2400000] as typeof Vector2;
        return new (STRUCT_CLASS_BY_TYPE[2411200] as typeof Arrow2D)({
          startType: ArrowHeadType[objectJson["startType"]] as any,
          start: _Vector2.unpack(1, objectJson["start"], _session) as Vector2,
          endType: ArrowHeadType[objectJson["endType"]] as any,
          end: _Vector2.unpack(1, objectJson["end"], _session) as Vector2,
          _packedCache: [{ encoding: 1, isBytes: false, packed: objectJson }],
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(2, 2411200)] = new Arrow2DJsonEncoder();

    class Ellipse2DJsonEncoder implements _JsonObjectEncoder {
      packObject(object: Ellipse2D): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "ELLIPSE2D";
        if (object.stroke != null) {
          objectJson["stroke"] = object.stroke.pack(1);
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): Ellipse2D {
        const _Stroke = STRUCT_CLASS_BY_TYPE[2101100] as typeof Stroke;
        const strokeValue = objectJson["stroke"];
        const unpackedStroke = strokeValue != undefined ? _Stroke.unpack(1, strokeValue, _session) as Stroke : null;
        return new (STRUCT_CLASS_BY_TYPE[2411400] as typeof Ellipse2D)({
          stroke: unpackedStroke,
          _packedCache: [{ encoding: 1, isBytes: false, packed: objectJson }],
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(2, 2411400)] = new Ellipse2DJsonEncoder();

    class Line2DJsonEncoder implements _JsonObjectEncoder {
      packObject(object: Line2D): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "LINE2D";
        if (object.stroke != null) {
          objectJson["stroke"] = object.stroke.pack(1);
        }
        objectJson["start"] = object.start.pack(1);
        objectJson["end"] = object.end.pack(1);
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): Line2D {
        const _Stroke = STRUCT_CLASS_BY_TYPE[2101100] as typeof Stroke;
        const _Vector2 = STRUCT_CLASS_BY_TYPE[2400000] as typeof Vector2;
        const strokeValue = objectJson["stroke"];
        const unpackedStroke = strokeValue != undefined ? _Stroke.unpack(1, strokeValue, _session) as Stroke : null;
        return new (STRUCT_CLASS_BY_TYPE[2411100] as typeof Line2D)({
          stroke: unpackedStroke,
          start: _Vector2.unpack(1, objectJson["start"], _session) as Vector2,
          end: _Vector2.unpack(1, objectJson["end"], _session) as Vector2,
          _packedCache: [{ encoding: 1, isBytes: false, packed: objectJson }],
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(2, 2411100)] = new Line2DJsonEncoder();

    class Path2DJsonEncoder implements _JsonObjectEncoder {
      packObject(object: Path2D): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "PATH2D";
        if (object.stroke != null) {
          objectJson["stroke"] = object.stroke.pack(1);
        }
        if (object.points.length > 0) {
          const packedPoints: any[] = [];
          for (const item of object.points) {
            packedPoints.push(item.pack(1));
          }
          objectJson["points"] = packedPoints;
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): Path2D {
        const _Stroke = STRUCT_CLASS_BY_TYPE[2101100] as typeof Stroke;
        const _Vector2 = STRUCT_CLASS_BY_TYPE[2400000] as typeof Vector2;
        const strokeValue = objectJson["stroke"];
        const unpackedStroke = strokeValue != undefined ? _Stroke.unpack(1, strokeValue, _session) as Stroke : null;
        const unpackedPoints: any[] = [];
        if (objectJson["points"] != undefined) {
          for (const item of objectJson["points"]) {
            unpackedPoints.push(_Vector2.unpack(1, item, _session) as Vector2)
          }
        }
        return new (STRUCT_CLASS_BY_TYPE[2411600] as typeof Path2D)({
          stroke: unpackedStroke,
          points: unpackedPoints,
          _packedCache: [{ encoding: 1, isBytes: false, packed: objectJson }],
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(2, 2411600)] = new Path2DJsonEncoder();

    class Polygon2DJsonEncoder implements _JsonObjectEncoder {
      packObject(object: Polygon2D): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "POLYGON2D";
        if (object.stroke != null) {
          objectJson["stroke"] = object.stroke.pack(1);
        }
        if (object.points.length > 0) {
          const packedPoints: any[] = [];
          for (const item of object.points) {
            packedPoints.push(item.pack(1));
          }
          objectJson["points"] = packedPoints;
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): Polygon2D {
        const _Stroke = STRUCT_CLASS_BY_TYPE[2101100] as typeof Stroke;
        const _Vector2 = STRUCT_CLASS_BY_TYPE[2400000] as typeof Vector2;
        const strokeValue = objectJson["stroke"];
        const unpackedStroke = strokeValue != undefined ? _Stroke.unpack(1, strokeValue, _session) as Stroke : null;
        const unpackedPoints: any[] = [];
        if (objectJson["points"] != undefined) {
          for (const item of objectJson["points"]) {
            unpackedPoints.push(_Vector2.unpack(1, item, _session) as Vector2)
          }
        }
        return new (STRUCT_CLASS_BY_TYPE[2411500] as typeof Polygon2D)({
          stroke: unpackedStroke,
          points: unpackedPoints,
          _packedCache: [{ encoding: 1, isBytes: false, packed: objectJson }],
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(2, 2411500)] = new Polygon2DJsonEncoder();

    class Vector2JsonEncoder implements _JsonObjectEncoder {
      packObject(object: Vector2): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "VECTOR2";
        objectJson["x"] = Number(object.x);
        objectJson["y"] = Number(object.y);
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): Vector2 {

        return new (STRUCT_CLASS_BY_TYPE[2400000] as typeof Vector2)({
          x: Number(objectJson["x"]),
          y: Number(objectJson["y"]),
          _packedCache: [{ encoding: 1, isBytes: false, packed: objectJson }],
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(2, 2400000)] = new Vector2JsonEncoder();

    class Vector3JsonEncoder implements _JsonObjectEncoder {
      packObject(object: Vector3): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "VECTOR3";
        objectJson["x"] = Number(object.x);
        objectJson["y"] = Number(object.y);
        objectJson["z"] = Number(object.z);
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): Vector3 {

        return new (STRUCT_CLASS_BY_TYPE[2400002] as typeof Vector3)({
          x: Number(objectJson["x"]),
          y: Number(objectJson["y"]),
          z: Number(objectJson["z"]),
          _packedCache: [{ encoding: 1, isBytes: false, packed: objectJson }],
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(2, 2400002)] = new Vector3JsonEncoder();

    class Vector4JsonEncoder implements _JsonObjectEncoder {
      packObject(object: Vector4): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "VECTOR4";
        objectJson["x"] = Number(object.x);
        objectJson["y"] = Number(object.y);
        objectJson["z"] = Number(object.z);
        objectJson["w"] = Number(object.w);
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): Vector4 {

        return new (STRUCT_CLASS_BY_TYPE[2400004] as typeof Vector4)({
          x: Number(objectJson["x"]),
          y: Number(objectJson["y"]),
          z: Number(objectJson["z"]),
          w: Number(objectJson["w"]),
          _packedCache: [{ encoding: 1, isBytes: false, packed: objectJson }],
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(2, 2400004)] = new Vector4JsonEncoder();

    class Vector2iJsonEncoder implements _JsonObjectEncoder {
      packObject(object: Vector2i): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "VECTOR2I";
        objectJson["x"] = Number(object.x);
        objectJson["y"] = Number(object.y);
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): Vector2i {

        return new (STRUCT_CLASS_BY_TYPE[2400001] as typeof Vector2i)({
          x: Number(objectJson["x"]),
          y: Number(objectJson["y"]),
          _packedCache: [{ encoding: 1, isBytes: false, packed: objectJson }],
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(2, 2400001)] = new Vector2iJsonEncoder();

    class Vector3iJsonEncoder implements _JsonObjectEncoder {
      packObject(object: Vector3i): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "VECTOR3I";
        objectJson["x"] = Number(object.x);
        objectJson["y"] = Number(object.y);
        objectJson["z"] = Number(object.z);
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): Vector3i {

        return new (STRUCT_CLASS_BY_TYPE[2400003] as typeof Vector3i)({
          x: Number(objectJson["x"]),
          y: Number(objectJson["y"]),
          z: Number(objectJson["z"]),
          _packedCache: [{ encoding: 1, isBytes: false, packed: objectJson }],
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(2, 2400003)] = new Vector3iJsonEncoder();

    class Vector4iJsonEncoder implements _JsonObjectEncoder {
      packObject(object: Vector4i): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "VECTOR4I";
        objectJson["x"] = Number(object.x);
        objectJson["y"] = Number(object.y);
        objectJson["z"] = Number(object.z);
        objectJson["w"] = Number(object.w);
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): Vector4i {

        return new (STRUCT_CLASS_BY_TYPE[2400005] as typeof Vector4i)({
          x: Number(objectJson["x"]),
          y: Number(objectJson["y"]),
          z: Number(objectJson["z"]),
          w: Number(objectJson["w"]),
          _packedCache: [{ encoding: 1, isBytes: false, packed: objectJson }],
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(2, 2400005)] = new Vector4iJsonEncoder();

    class QuaternionJsonEncoder implements _JsonObjectEncoder {
      packObject(object: Quaternion): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "QUATERNION";
        objectJson["x"] = Number(object.x);
        objectJson["y"] = Number(object.y);
        objectJson["z"] = Number(object.z);
        objectJson["w"] = Number(object.w);
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): Quaternion {

        return new (STRUCT_CLASS_BY_TYPE[2400010] as typeof Quaternion)({
          x: Number(objectJson["x"]),
          y: Number(objectJson["y"]),
          z: Number(objectJson["z"]),
          w: Number(objectJson["w"]),
          _packedCache: [{ encoding: 1, isBytes: false, packed: objectJson }],
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(2, 2400010)] = new QuaternionJsonEncoder();

    class Rectangle2DJsonEncoder implements _JsonObjectEncoder {
      packObject(object: Rectangle2D): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "RECTANGLE2D";
        if (object.stroke != null) {
          objectJson["stroke"] = object.stroke.pack(1);
        }
        if (object.width != null) {
          objectJson["width"] = object.width.pack(1);
        }
        if (object.height != null) {
          objectJson["height"] = object.height.pack(1);
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): Rectangle2D {
        const _Stroke = STRUCT_CLASS_BY_TYPE[2101100] as typeof Stroke;
        const _Vector2 = STRUCT_CLASS_BY_TYPE[2400000] as typeof Vector2;
        const strokeValue = objectJson["stroke"];
        const unpackedStroke = strokeValue != undefined ? _Stroke.unpack(1, strokeValue, _session) as Stroke : null;
        const widthValue = objectJson["width"];
        const unpackedWidth = widthValue != undefined ? _Vector2.unpack(1, widthValue, _session) as Vector2 : null;
        const heightValue = objectJson["height"];
        const unpackedHeight = heightValue != undefined ? _Vector2.unpack(1, heightValue, _session) as Vector2 : null;
        return new (STRUCT_CLASS_BY_TYPE[2411300] as typeof Rectangle2D)({
          stroke: unpackedStroke,
          width: unpackedWidth,
          height: unpackedHeight,
          _packedCache: [{ encoding: 1, isBytes: false, packed: objectJson }],
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(2, 2411300)] = new Rectangle2DJsonEncoder();

    class LengthJsonEncoder implements _JsonObjectEncoder {
      packObject(object: Length): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "LENGTH";
        objectJson["unit"] = LengthType[object.unit];
        objectJson["value"] = Number(object.value);
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): Length {

        return new (STRUCT_CLASS_BY_TYPE[1800001] as typeof Length)({
          unit: LengthType[objectJson["unit"]] as any,
          value: Number(objectJson["value"]),
          _packedCache: [{ encoding: 1, isBytes: false, packed: objectJson }],
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(2, 1800001)] = new LengthJsonEncoder();

    class Offset2JsonEncoder implements _JsonObjectEncoder {
      packObject(object: Offset2): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "OFFSET2";
        objectJson["type"] = Anchor[object.type];
        if (object.top != null) {
          objectJson["top"] = object.top.pack(1);
        }
        if (object.left != null) {
          objectJson["left"] = object.left.pack(1);
        }
        if (object.width != null) {
          objectJson["width"] = object.width.pack(1);
        }
        if (object.height != null) {
          objectJson["height"] = object.height.pack(1);
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): Offset2 {
        const _Length = STRUCT_CLASS_BY_TYPE[1800001] as typeof Length;
        const topValue = objectJson["top"];
        const unpackedTop = topValue != undefined ? _Length.unpack(1, topValue, _session) as Length : null;
        const leftValue = objectJson["left"];
        const unpackedLeft = leftValue != undefined ? _Length.unpack(1, leftValue, _session) as Length : null;
        const widthValue = objectJson["width"];
        const unpackedWidth = widthValue != undefined ? _Length.unpack(1, widthValue, _session) as Length : null;
        const heightValue = objectJson["height"];
        const unpackedHeight = heightValue != undefined ? _Length.unpack(1, heightValue, _session) as Length : null;
        return new (STRUCT_CLASS_BY_TYPE[2400020] as typeof Offset2)({
          type: Anchor[objectJson["type"]] as any,
          top: unpackedTop,
          left: unpackedLeft,
          width: unpackedWidth,
          height: unpackedHeight,
          _packedCache: [{ encoding: 1, isBytes: false, packed: objectJson }],
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(2, 2400020)] = new Offset2JsonEncoder();

    class Inset2JsonEncoder implements _JsonObjectEncoder {
      packObject(object: Inset2): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "INSET2";
        objectJson["base"] = Number(object.base);
        if (object.top != null) {
          objectJson["top"] = Number(object.top);
        }
        if (object.left != null) {
          objectJson["left"] = Number(object.left);
        }
        if (object.right != null) {
          objectJson["right"] = Number(object.right);
        }
        if (object.bottom != null) {
          objectJson["bottom"] = Number(object.bottom);
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): Inset2 {

        const topValue = objectJson["top"];
        const unpackedTop = topValue != undefined ? Number(topValue) : null;
        const leftValue = objectJson["left"];
        const unpackedLeft = leftValue != undefined ? Number(leftValue) : null;
        const rightValue = objectJson["right"];
        const unpackedRight = rightValue != undefined ? Number(rightValue) : null;
        const bottomValue = objectJson["bottom"];
        const unpackedBottom = bottomValue != undefined ? Number(bottomValue) : null;
        return new (STRUCT_CLASS_BY_TYPE[2400023] as typeof Inset2)({
          base: Number(objectJson["base"]),
          top: unpackedTop,
          left: unpackedLeft,
          right: unpackedRight,
          bottom: unpackedBottom,
          _packedCache: [{ encoding: 1, isBytes: false, packed: objectJson }],
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(2, 2400023)] = new Inset2JsonEncoder();

    class Corner2JsonEncoder implements _JsonObjectEncoder {
      packObject(object: Corner2): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "CORNER2";
        objectJson["base"] = Number(object.base);
        if (object.topLeft != null) {
          objectJson["topLeft"] = Number(object.topLeft);
        }
        if (object.topRight != null) {
          objectJson["topRight"] = Number(object.topRight);
        }
        if (object.bottomLeft != null) {
          objectJson["bottomLeft"] = Number(object.bottomLeft);
        }
        if (object.bottomRight != null) {
          objectJson["bottomRight"] = Number(object.bottomRight);
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): Corner2 {

        const topLeftValue = objectJson["topLeft"];
        const unpackedTopLeft = topLeftValue != undefined ? Number(topLeftValue) : null;
        const topRightValue = objectJson["topRight"];
        const unpackedTopRight = topRightValue != undefined ? Number(topRightValue) : null;
        const bottomLeftValue = objectJson["bottomLeft"];
        const unpackedBottomLeft = bottomLeftValue != undefined ? Number(bottomLeftValue) : null;
        const bottomRightValue = objectJson["bottomRight"];
        const unpackedBottomRight = bottomRightValue != undefined ? Number(bottomRightValue) : null;
        return new (STRUCT_CLASS_BY_TYPE[2400024] as typeof Corner2)({
          base: Number(objectJson["base"]),
          topLeft: unpackedTopLeft,
          topRight: unpackedTopRight,
          bottomLeft: unpackedBottomLeft,
          bottomRight: unpackedBottomRight,
          _packedCache: [{ encoding: 1, isBytes: false, packed: objectJson }],
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(2, 2400024)] = new Corner2JsonEncoder();

    class Axis2JsonEncoder implements _JsonObjectEncoder {
      packObject(object: Axis2): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "AXIS2";
        objectJson["base"] = Number(object.base);
        if (object.x != null) {
          objectJson["x"] = Number(object.x);
        }
        if (object.y != null) {
          objectJson["y"] = Number(object.y);
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): Axis2 {

        const xValue = objectJson["x"];
        const unpackedX = xValue != undefined ? Number(xValue) : null;
        const yValue = objectJson["y"];
        const unpackedY = yValue != undefined ? Number(yValue) : null;
        return new (STRUCT_CLASS_BY_TYPE[2400025] as typeof Axis2)({
          base: Number(objectJson["base"]),
          x: unpackedX,
          y: unpackedY,
          _packedCache: [{ encoding: 1, isBytes: false, packed: objectJson }],
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(2, 2400025)] = new Axis2JsonEncoder();

    class Axis3JsonEncoder implements _JsonObjectEncoder {
      packObject(object: Axis3): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "AXIS3";
        objectJson["base"] = Number(object.base);
        if (object.x != null) {
          objectJson["x"] = Number(object.x);
        }
        if (object.y != null) {
          objectJson["y"] = Number(object.y);
        }
        if (object.z != null) {
          objectJson["z"] = Number(object.z);
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): Axis3 {

        const xValue = objectJson["x"];
        const unpackedX = xValue != undefined ? Number(xValue) : null;
        const yValue = objectJson["y"];
        const unpackedY = yValue != undefined ? Number(yValue) : null;
        const zValue = objectJson["z"];
        const unpackedZ = zValue != undefined ? Number(zValue) : null;
        return new (STRUCT_CLASS_BY_TYPE[2400026] as typeof Axis3)({
          base: Number(objectJson["base"]),
          x: unpackedX,
          y: unpackedY,
          z: unpackedZ,
          _packedCache: [{ encoding: 1, isBytes: false, packed: objectJson }],
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(2, 2400026)] = new Axis3JsonEncoder();

    class Grid2JsonEncoder implements _JsonObjectEncoder {
      packObject(object: Grid2): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "GRID2";
        objectJson["columns"] = Number(object.columns);
        objectJson["rows"] = Number(object.rows);
        if (object.columnWidth != null) {
          objectJson["columnWidth"] = object.columnWidth.pack(1);
        }
        if (object.columnMinWidth != null) {
          objectJson["columnMinWidth"] = object.columnMinWidth.pack(1);
        }
        if (object.rowHeight != null) {
          objectJson["rowHeight"] = object.rowHeight.pack(1);
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): Grid2 {
        const _Length = STRUCT_CLASS_BY_TYPE[1800001] as typeof Length;
        const columnWidthValue = objectJson["columnWidth"];
        const unpackedColumnWidth = columnWidthValue != undefined ? _Length.unpack(1, columnWidthValue, _session) as Length : null;
        const columnMinWidthValue = objectJson["columnMinWidth"];
        const unpackedColumnMinWidth = columnMinWidthValue != undefined ? _Length.unpack(1, columnMinWidthValue, _session) as Length : null;
        const rowHeightValue = objectJson["rowHeight"];
        const unpackedRowHeight = rowHeightValue != undefined ? _Length.unpack(1, rowHeightValue, _session) as Length : null;
        return new (STRUCT_CLASS_BY_TYPE[2400021] as typeof Grid2)({
          columns: Number(objectJson["columns"]),
          rows: Number(objectJson["rows"]),
          columnWidth: unpackedColumnWidth,
          columnMinWidth: unpackedColumnMinWidth,
          rowHeight: unpackedRowHeight,
          _packedCache: [{ encoding: 1, isBytes: false, packed: objectJson }],
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(2, 2400021)] = new Grid2JsonEncoder();

    class GridSpan2JsonEncoder implements _JsonObjectEncoder {
      packObject(object: GridSpan2): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "GRID_SPAN2";
        objectJson["columns"] = Number(object.columns);
        objectJson["rows"] = Number(object.rows);
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): GridSpan2 {

        return new (STRUCT_CLASS_BY_TYPE[2400022] as typeof GridSpan2)({
          columns: Number(objectJson["columns"]),
          rows: Number(objectJson["rows"]),
          _packedCache: [{ encoding: 1, isBytes: false, packed: objectJson }],
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(2, 2400022)] = new GridSpan2JsonEncoder();

    class ScheduleJsonEncoder implements _JsonObjectEncoder {
      packObject(object: Schedule): any {
        const objectJson: { [key: string]: any } = {};
        objectJson["metatype"] = "SCHEDULE";
        objectJson["frequency"] = ScheduleFrequency[object.frequency];
        objectJson["interval"] = Number(object.interval);
        if (object.start != null) {
          objectJson["start"] = object.start.toString({ timeZoneName: 'never' });
        }
        if (object.end != null) {
          objectJson["end"] = object.end.toString({ timeZoneName: 'never' });
        }
        if (object.count != null) {
          objectJson["count"] = Number(object.count);
        }
        if (object.weekStart != null) {
          objectJson["weekStart"] = DayOfWeek[object.weekStart];
        }
        if (object.bySetPos.length > 0) {
          const packedBySetPos: any[] = [];
          for (const item of object.bySetPos) {
            packedBySetPos.push(Number(item));
          }
          objectJson["bySetPos"] = packedBySetPos;
        }
        if (object.byMonth.length > 0) {
          const packedByMonth: any[] = [];
          for (const item of object.byMonth) {
            packedByMonth.push(Month[item]);
          }
          objectJson["byMonth"] = packedByMonth;
        }
        if (object.byMonthDay.length > 0) {
          const packedByMonthDay: any[] = [];
          for (const item of object.byMonthDay) {
            packedByMonthDay.push(Number(item));
          }
          objectJson["byMonthDay"] = packedByMonthDay;
        }
        if (object.byYearDay.length > 0) {
          const packedByYearDay: any[] = [];
          for (const item of object.byYearDay) {
            packedByYearDay.push(Number(item));
          }
          objectJson["byYearDay"] = packedByYearDay;
        }
        if (object.byEaster.length > 0) {
          const packedByEaster: any[] = [];
          for (const item of object.byEaster) {
            packedByEaster.push(Number(item));
          }
          objectJson["byEaster"] = packedByEaster;
        }
        if (object.byWeekNo.length > 0) {
          const packedByWeekNo: any[] = [];
          for (const item of object.byWeekNo) {
            packedByWeekNo.push(Number(item));
          }
          objectJson["byWeekNo"] = packedByWeekNo;
        }
        if (object.byWeekDay.length > 0) {
          const packedByWeekDay: any[] = [];
          for (const item of object.byWeekDay) {
            packedByWeekDay.push(DayOfWeek[item]);
          }
          objectJson["byWeekDay"] = packedByWeekDay;
        }
        if (object.byHour.length > 0) {
          const packedByHour: any[] = [];
          for (const item of object.byHour) {
            packedByHour.push(Number(item));
          }
          objectJson["byHour"] = packedByHour;
        }
        if (object.byMinute.length > 0) {
          const packedByMinute: any[] = [];
          for (const item of object.byMinute) {
            packedByMinute.push(Number(item));
          }
          objectJson["byMinute"] = packedByMinute;
        }
        if (object.bySecond.length > 0) {
          const packedBySecond: any[] = [];
          for (const item of object.bySecond) {
            packedBySecond.push(Number(item));
          }
          objectJson["bySecond"] = packedBySecond;
        }
        return objectJson;
      }

      unpackObject(objectJson: any, _session: Session | null): Schedule {

        const startValue = objectJson["start"];
        const unpackedStart = startValue != undefined ? Temporal.Instant.from(startValue).toZonedDateTimeISO('UTC') : null;
        const endValue = objectJson["end"];
        const unpackedEnd = endValue != undefined ? Temporal.Instant.from(endValue).toZonedDateTimeISO('UTC') : null;
        const countValue = objectJson["count"];
        const unpackedCount = countValue != undefined ? Number(countValue) : null;
        const weekStartValue = objectJson["weekStart"];
        const unpackedWeekStart = weekStartValue != undefined ? DayOfWeek[weekStartValue] as any : null;
        const unpackedBySetPos: any[] = [];
        if (objectJson["bySetPos"] != undefined) {
          for (const item of objectJson["bySetPos"]) {
            unpackedBySetPos.push(Number(item))
          }
        }
        const unpackedByMonth: any[] = [];
        if (objectJson["byMonth"] != undefined) {
          for (const item of objectJson["byMonth"]) {
            unpackedByMonth.push(Month[item] as any)
          }
        }
        const unpackedByMonthDay: any[] = [];
        if (objectJson["byMonthDay"] != undefined) {
          for (const item of objectJson["byMonthDay"]) {
            unpackedByMonthDay.push(Number(item))
          }
        }
        const unpackedByYearDay: any[] = [];
        if (objectJson["byYearDay"] != undefined) {
          for (const item of objectJson["byYearDay"]) {
            unpackedByYearDay.push(Number(item))
          }
        }
        const unpackedByEaster: any[] = [];
        if (objectJson["byEaster"] != undefined) {
          for (const item of objectJson["byEaster"]) {
            unpackedByEaster.push(Number(item))
          }
        }
        const unpackedByWeekNo: any[] = [];
        if (objectJson["byWeekNo"] != undefined) {
          for (const item of objectJson["byWeekNo"]) {
            unpackedByWeekNo.push(Number(item))
          }
        }
        const unpackedByWeekDay: any[] = [];
        if (objectJson["byWeekDay"] != undefined) {
          for (const item of objectJson["byWeekDay"]) {
            unpackedByWeekDay.push(DayOfWeek[item] as any)
          }
        }
        const unpackedByHour: any[] = [];
        if (objectJson["byHour"] != undefined) {
          for (const item of objectJson["byHour"]) {
            unpackedByHour.push(Number(item))
          }
        }
        const unpackedByMinute: any[] = [];
        if (objectJson["byMinute"] != undefined) {
          for (const item of objectJson["byMinute"]) {
            unpackedByMinute.push(Number(item))
          }
        }
        const unpackedBySecond: any[] = [];
        if (objectJson["bySecond"] != undefined) {
          for (const item of objectJson["bySecond"]) {
            unpackedBySecond.push(Number(item))
          }
        }
        return new (STRUCT_CLASS_BY_TYPE[700001] as typeof Schedule)({
          frequency: ScheduleFrequency[objectJson["frequency"]] as any,
          interval: Number(objectJson["interval"]),
          start: unpackedStart,
          end: unpackedEnd,
          count: unpackedCount,
          weekStart: unpackedWeekStart,
          bySetPos: unpackedBySetPos,
          byMonth: unpackedByMonth,
          byMonthDay: unpackedByMonthDay,
          byYearDay: unpackedByYearDay,
          byEaster: unpackedByEaster,
          byWeekNo: unpackedByWeekNo,
          byWeekDay: unpackedByWeekDay,
          byHour: unpackedByHour,
          byMinute: unpackedByMinute,
          bySecond: unpackedBySecond,
          _session,
        });
      }
    }

    JSON_OBJECT_ENCODERS[getObjectKey(2, 700001)] = new ScheduleJsonEncoder();
}

loadEncoders();
